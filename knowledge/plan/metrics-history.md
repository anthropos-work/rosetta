# Metrics History

One row per shipped release (newest first). Aggregated at `/developer-kit:close-release` Phase 4b from the release's per-milestone `metrics.json`. Full per-release detail lives in `releases/{archive/}{VV.VV}-{codename}/metrics.json`.

(v1.4 was removed before shipping — see roadmap notes; the version number jumps v1.3b → v1.5.)

| Release | Codename | Shipped | Framework tests | Mirror tests | Gates | Flakes | Supply-chain |
|---|---|---|---|---|---|---|---|
| v2.3 | cue to cue | 2026-07-15 | **1831 rext Go total (+82 vs v2.2's 1749 — SAME like-for-like `grep -c '^func Test'` method; the 1772/1764 recorded at the v2.2 close used a DIFFERENT method and is NOT comparable)** — by milestone M217 1750 / M218 1784 (the `x/crypto v0.51→v0.52` indirect bump landed here) / M219 1821 / M220 1827 / M221 1831; `go vet` clean + 0 failures across all 6 modules. **Python 1341 (0 fail, 16 skip) across 5 sections via JUnit XML** (demo-stack 663 · stack-injection 260 · stack-core 182 · dev-stack 122 · stack-verify 114) — v2.2 was 668 across 3 sections; **like-for-like on the 3 SHARED sections 668→1105**; **M220 = the whole rext Python tree's FIRST end-to-end completion** (the 5-milestone dev-stack "spins forever" chronic LAND-NOW'd: one missing env var) | **TS e2e 151 (+27 vs v2.2's 124)** — 69 stack-verify/e2e + 82 playthroughs/e2e via `playwright test --list` (the canonical like-for-like method); M219's differently-measured 94 (unit/typecheck) is NOT used → **no false 124→94 regression filed** (itself a D17 count-artifact instance). Clerkenstein alignment **100% / 100% critical (27/27)** held (re-run M220 S6) | **presenter-speed / field-hardening** — a presenter swaps heroes in **under 5 s** on a demo that comes up green, fully-loaded, and remotely reachable by default. Root causes were **already measured in-repo and silently rotting** (two dead perf demo-patches refusing on sha-drift with stderr→`/dev/null`, a 4-place false "~2-5 s we can't shorten" latency claim never in a ledger). Headline gate **click→ACCESS < 5 s** set at M218, **re-proven live 8/8 on `billion` over the tailnet with NO flags** (maya-thriving **2.11 s** / dan-manager **1.31 s** vs **39.45 s / 38.30 s** baseline, ~18×); 3 story orgs incl. AI-readiness; remote **default-on** for demo (D-DESIGN-3 SUPERSEDES v2.2's D-DESIGN-1); `safety.md` **Part 3** (the exposure axis). **5 milestones M217 → { M218 ∥ M219 ∥ M220 } → M221.** The **D17 spine** (a status artifact that outlives the thing it describes, read as evidence) bit **~24×** across the release — told honestly, never sanded. close-release **YELLOW/0-blocking** (2 must-fix doc-staleness + the deferral/metrics paperwork FIXED); **triple-clean 3/3**; **4 non-gate tail carries → v2.4** (user sign-off); **zero platform-repo edits** (rosetta diff 108 files, all docs/planning) | 0 | GREEN — **0 net-new direct deps**; the ONLY dep change is `golang.org/x/crypto v0.51.0→v0.52.0` (indirect, behaviour-neutral, patched, landed M218); `govulncheck` clean all 6; zero `package.json`/`requirements` changes; all licenses permissive (MIT/BSD/Apache, no copyleft) |
| v2.1 | quick change | 2026-07-09 | **1764 rext Go total (+19 vs v2.0's 1745; every module flat-or-up)** — M209 skiller.*→public.* fake-Conn matcher rename (count-preserving) + harden (stack-snapshot +17, stack-seeding +1); M211 +1 (playthroughs RosterRefreshGate); by module alignment 52 / clerkenstein 270 / stack-secrets 163 / stack-seeding 792 / stack-snapshot 381 / playthroughs 106 | TS unit **103 (+3 vs v2.0's 100)** — stack-verify/e2e coverage-manifest 29→32 (the 15→14 service re-ground + the cross-port classifier dedup); playthroughs/e2e 58 flat | **skiller-in-app re-ground** — a field-hardening release triggered by the LANDED platform merge (skiller service + schema → app's `public` schema, table names unchanged; RPC → backend; skiller subgraph gone → **4 subgraphs**; repo/container removed). Re-fit rext tooling + corpus + stacks to the merged platform and PROVED /dev-up + /demo-up work cold. M208 re-sync + live de-risk · M209 rext re-point skiller.*→public.* + snapshot recapture + cache-key digest narrowing + MinRows floor · M210 flip 6 rext-facing tooling-doc bodies to public.* + arch half · **M211 (iterative, gate 6/6)** cache-migration recapture (real 42,790 taxonomy + 274 sims re-keyed, no prod access) + build-scratch-resync root-fix + `migrate-dev.sh` cold DB-init (M25-D9 RESOLVED); **M42 coverage GREEN both vantages + v2.0 Playthroughs 10/11 re-proven GREEN on the MERGED platform**. close-release GREEN (0 blocking; should-fix docs/count/toolchain FIXED); triple-clean 3/3; all **5** Clerkenstein gates held 100/100; **zero platform-repo edits** | 0 | GREEN post-bump (0 net-new deps — a schema re-point adds no imports, go.mod byte-identical to v2.0; **go1.25.11→go1.25.12** cleared 2 inherited stdlib advisories [GO-2026-5856 crypto/tls ECH MEDIUM-called, GO-2026-4970 os.Root HIGH-unreachable] → govulncheck clean all 6; npm 0 vulns; 0 GPL/AGPL) |
| v1.10 | method acting | 2026-06-27 | 1551 (+303 — rext Go total; **stack-seeding 444→706** [+262: M39 RosterIdentity/avatar/role-backfill +18, M41 ProfileSeeder +34, M42e curated_pools/hero_activity/orglogo/photo_avatar/identity-casbin +73, M42m FeedbackSeeder org-mirror +3, M44 Certificates/ProjectsSeeder +29, **M45 services/ai + blueprint + batchcache + cmd/gen-batch + GeneratedBatchSeeder +110**, M46 org-scale auto-fill/per-story/preview/--gen-batches-fence +29], **stack-snapshot 333→363** [+30: M40 directus serve-grant pkg +21, M42e simembeddings +7, M46 serve-grant deep-fetch closure +2], **clerkenstein 259→270** [+11: M39 roster org-name +5, M42e image-threading invariant +2, +4 recorded-vs-grep close-drift reconciled to the 270 ground-truth at the M45/M46 tags]; alignment 52 / stack-secrets 160 unchanged) | the **TS Playwright coverage harness** (rext `stack-verify/e2e/`, `@playwright/test ^1.49.0`, M42e — the FIRST non-Go rext dep; the per-vantage semantic gate) + Python growth at M43 (cockpit `cockpit.py` tests 27→**63** [+36 across 2 harden] + the demopatch `test_demopatch.py` 18→**43** [+25]); manager-namespace unit spec `coverage-manifest.unit.spec.ts` +17; `stack-injection` 117 unchanged; no suite decreased | **believable-profile + presenter-grade / scalable-generation** — v1.9 told the STORY; v1.10 makes each CHARACTER hold up under a close-up. A logged-in hero reads as a fully-fleshed person — profile identity (M39 org name/role/face) + the content-surface unblock (M40 the per-stack-Directus serve-grant: the library + activity feed stop emptying) + profile depth (M41 ProfileSeeder work-history/education/claimed-skill tail) — proven by a Playwright **SEMANTIC** coverage gate at BOTH vantages cold (M42e employee `(0,0,0,0,0)` · M42m manager `failingSections=0`, with the `demopatch` tool for platform-bound surfaces). **EXTENDED** with the presenter-grade layer: M43 cockpit UX polish (light launcher, one CTA, manifest download, overlay) · M44 whole-roster profile completeness (every member+manager baked: trajectory self-ratings + certificates + projects + avatars; DATA DENSITY) · **M45 a cheap-LLM generation engine** (`cmd/gen-batch` + a prompt-keyed cache + the CODE-owns-structure / AI-owns-content boundary; the first new dep) · M46 **org-scale fill** (a whole ~500/735-member org from one supporting-population descriptor) + a gen-batch preview CLI. M46's 5th gate face (the manager enterprise grids — a platform-bound org-scale render wall) cleared by a **demo-patch/recapture campaign** (ZERO canonical edits: T1 next-web pagination + FK indexes · B `roles.go` authz-skip · DD column-drift backfill · SG the Directus serve-grant deep-fetch **CLOSURE** + a prod-structure recapture, firewall public-only, **resolving DEF-M46-01**). close-release GREEN (0 blocking; docs 1 finding [orphan spec] FIXED); all **5** Clerkenstein gates held 100/100; proven live on demo-3; **zero platform-repo edits** | 0 | **1 NEW DEP — deliberate + sanctioned at M45** (`github.com/anthropos-work/ai@v1.40.1`, the `services/ai` wrapper transport; transitive Azure SDK + openai-go/v3, all MIT/BSD/Apache) — the user-acknowledged in-release inflection M45 is ABOUT (v1.8→v1.9 was 0-new-deps; the design-roadmap approved the LLM engine + this dep). M46 reuses it unchanged (0 new). Playwright `^1.49.0` dev/test-only (M42e). Python stdlib-only + pre-existing PyYAML test dep. rosetta corpus docs-only (no manifest). No GPL/AGPL |
| v1.9 | storytelling | 2026-06-23 | 1248 (+221 — rext Go total; **stack-seeding 259→444** [+185: PersonaSeeder/TaxonomyRefs/G14 M34, multi-org Stories engine M35, 6 dashboard seeders M36, roster/cockpit producers + M38-D8 M38], **clerkenstein 223→259** [+36: multi-identity Registry/selection + the 5th alignment DNA `clerk-multi-1` + FuzzLoadRoster M37]; alignment 52 / stack-snapshot 333 / stack-secrets 160 unchanged) | 283 across the two touched Python suites (demo-stack `tests/` 138→166 [+28], stack-injection `tests/` 113→117 [+4]; no suite decreased — the untouched rext python suites carry forward at their v1.8 counts) | **believable-demo-narrative** — turn the placeholder seeder (hollow: "User N", binary 85/35, zero verified skills, G14 invalid-enum dead rows) into a declarative **Stories & Heroes** engine: each story = one org with a thriving/struggling/manager **hero** trio, seeded via the real **verified-skill 7-table chain** so the individual **skill profile** (Must #1, M34) + the org **Workforce dashboard** (Must #2, M36) tell one coherent story (the claimed-vs-verified gap = the "aha"), plus a standalone **presenter cockpit** (login-as a hero + jump-to the right screen, M38) wired on **Clerkenstein multi-identity** (M37 — the 5th alignment surface). M34 verified-skill spine · M35 multi-org Stories model · M36 dashboard surfaces · M37 Clerkenstein multi-identity (seat-switch) · M38 presenter cockpit (incl close-fix M38-D8: vantage-faithful `org_role` three-write lockstep). close-release GREEN (0 blocking; docs 0 findings); all **5** Clerkenstein gates held 100/100; proven live on demo-3; **zero platform-repo edits** | 0 | GREEN (no new dependency — Go `go.mod`/`go.sum` diff `storytelling-m34~1..m38` EMPTY, byte-identical to v1.8; Python stdlib-only + pre-existing PyYAML test dep; rosetta docs-only; all deps MIT/BSD/Apache, no GPL/AGPL) |
| v1.8 | understudy | 2026-06-15 | 1027 (+0 — zero `.go` files touched; M26 is shell + python + docs) | 501 (+30 — M26 grew the two touched suites: demo-stack `tests/` 110→138, stack-injection `tests/` 111→113; every other Python suite unchanged) | **self-contained-demo** — give `stack-demo/` its OWN platform clone set so a box with only `stack-demo/` (no `stack-dev/`) runs a demo end-to-end. Single `section` milestone **M26** re-implements the orphaned `m26/self-contained-demo` branch (@`25ab855`, predates v1.6/v1.7 — unmergeable) onto current `main`: a NEW `ensure-clones.sh` bootstraps `stack-demo`'s peer clones from GitHub · the build SOURCE + the compose dir (`PLAT`, #D-MAIN) move to `stack-demo` so ALL services build from it · dev-image reuse gated behind `--reuse-dev-images` (OFF by default). Preserves v1.6 stack-secrets/M30 + M31 mkcert + M32 studio-desk. close-release GREEN (3 doc-coherence cleanups landed Fate-1; live field-bake = user-authorized follow-up); Clerkenstein gates held 100/100; **zero platform-repo edits** | 0 | GREEN (no new dependency — ext Go `go.mod`/`go.sum` diff EMPTY, Python stdlib-only + pre-existing PyYAML test dep, rosetta docs-only) |
| v1.7 | house lights | 2026-06-15 | 1027 (+0 — zero `.go` files touched; the five Go modules carry forward verbatim) | 471 (+12 — M31 +11 `FapiCertStep` in `demo-stack/tests/test_tooling.py` 99→110; M32 +1 regression in `stack-injection/tests/test_injection.py` 87→88) | **demo-UI hardening** — make a fresh browser at a demo render the working UI with zero manual fiddling: M31 automates a locally-trusted **mkcert** FAPI TLS cert into the bring-up (ONE branch in `up-injected.sh` step 3a-bis, openssl self-signed fallback factored into `gen_openssl_fapi_cert()`, `DEMO_NO_MKCERT=1` opt-out, non-fatal) so clerk-js's FAPI handshake stops hitting an untrusted cert and next-web stops blanking · M32 fixes the sibling studio-desk dead-`:9100`-redirect (base compose's `NODE_ENV=development` beat the image's baked production ENV through the additive override merge → `isProduction=false` → redirect to dead `:9100`; pin `NODE_ENV=production` + `FRONTEND_PORT=9000` in the studio-desk override to win the merge back to the single-port production `sendFile` path, no route gap #M32-D1) + the `:9100` doc/CORS sweep (un-offset `:9100` origin dropped). Values-blind (no cert/key body to stdout/log), non-fatal steps, Clerkenstein gates held 100/100; **zero platform-repo edits** | 0 | GREEN (no new dependency — M31 pure shell + stdlib tests, M32 stdlib generator + tests, rosetta docs-only; no go.sum/manifest change; prior posture unchanged from v1.6) |
| v1.6 | stage door | 2026-06-14 | 1027 (+160 — net-new `stack-secrets` Go section: 113@M27 → 160@M28; prior 4 modules unchanged @ 867) | 459 (+0 — Python held; the v1.6 growth surface is Go, not Python) | **secret-provisioning** — ingest a secret source (dir/zip, default `.agentspace/secrets`) → provision every repo's `.env` → a values-blind secret-coverage DNA (gene = repo×KEY, 6 repos / 55 genes, introspect+diff keep-listed gate) → demo-aware `check` wired non-fatally into `/dev-up` + `/demo-up` pre-flight (warn standard / fail critical / skip otherwise) → **field-baked LIVE** (M30: demo-3, 17 containers UP, Critical 100%, prod `DIRECTUS_TOKEN` armed in ZERO containers — the fix16/17 non-rearm class). 2 field bugs fixed Fate-1; Clerkenstein gates held 100/100; **zero platform-repo edits** | 0 | GREEN (`stack-secrets` stdlib-only — no go.sum, zero new third-party dep; prior modules' posture unchanged) |
| v1.5 | prop room | 2026-06-14 | 867 (+131) | 459 (+99) | **local-Directus release** — stacks serve their own captured public catalog locally (data plane local, asset plane prod) on `--local-content`: M21 structure capture (content-model DDL+PKs+serve-rows captured atomically; firewall structural-metadata class; redefined `stacksnap` exit codes) · M22 executed provisioning (per-stack Directus as a compose service on an offset port, idempotent, verified; the `EnvContract` firewall a load-bearing executed gate) · M23 content cutover (`DIRECTUS_BASE_ADDR` re-point demo+dev, studio-desk minted token, `directus_files` ref capture, cross-surface closure gene) · M24 docs convergence + 4 hygiene items (go1.25.11 pin, README-index guard, zero-critical-genes guard, `/project-stats` scope fix) · M25 field-bake (5/5 live done-bars GREEN, 4 field bugs fixed). Clerkenstein gates held 100/100 | 0 | GREEN (0 called CVEs; go1.25.11 clears the 12 stdlib advisories; all-permissive licenses) |
| v1.3b | dress rehearsal | 2026-06-09 | 43 (alignment, unchanged) | 210 (clerkenstein, unchanged) | **field-hardening — `/demo-up` now full/populated/verified/demoable** (the 14 `/demo-up` issues): M16 land-fixes+doc-truth · M17 re-run safety (idempotency) · M18 verification net · M19 frontend tier · M20 lifecycle convergence (auto-set-dress); stack-snapshot 224→231 + stack-seeding 236→252 → **736 Go test funcs (+23, all M17)** + Python 174→**360 collected** (net-new stack-verify 0→87, demo-stack 13→84, dev-stack 38→50, stack-injection 69→85); 4 net-new corpus docs; 100% data-DNA held; Clerkenstein gates 100/100; **zero platform-repo edits** | 0 | GREEN (0 called third-party CVEs; 12 stdlib @go1.25.3 → go1.25.11+ toolchain; all-permissive licenses) |
| v1.3 | stack party | 2026-06-07 | 43 (alignment, unchanged) | 210 (clerkenstein, unchanged) | **dev/demo convergence** — unified first-available-N registry (M12) + dev-as-peer (M13) + hard-renamed `stack-*` skills (M14) + code-cited `safety.md` with 7 fail-closed docs↔code drift guards (M15); stack-snapshot 224 + stack-seeding 236 → **713 Go test funcs (+20 vs v1.2's test-only 693)** + **+52 net-new Python** (the unified registry, 174 total); 100% data-DNA coverage held; Clerkenstein gates held 100/100; triple-clean 3/3 | 0 | GREEN (0 called third-party CVEs; 12 stdlib @go1.25.3 → go1.25.11+ toolchain; all-permissive licenses) |
| v1.2 | set dressing | 2026-06-07 | 46 (alignment, unchanged) | 218 (clerkenstein, unchanged) | **snapshot mechanism → 100% data-DNA coverage** (taxonomy M9b + Directus content M10); stack-snapshot 212 + stack-seeding 232 → **708 Go total (+73% vs v1.1)**; Clerkenstein gates held 100/100; triple-clean 3/3 | 0 | GREEN (0 third-party CVEs; 12 stdlib → go1.25.11+ toolchain; all-permissive licenses) |
| v1.1 | show floor | 2026-06-05 | 46 (alignment) | 218 (clerkenstein) | seeding stack 145 + demo tooling 87 → **409 Go total**; 3-layer prod-isolation guard (audit-proven) + data-DNA 100%/critical over 8 reachable surfaces; Clerkenstein gates held 100/100 | 0 | GREEN |
| v1.0 | body double | 2026-06-03 | 43 test + 3 fuzz (stdlib-only) | 123 test + 6 fuzz / 8 pkg / 3 DNAs | Go 22/22 · JS 9/9 · express 9/9 · drift 9/9 (all 100/100; triple-clean 3/3) | 0 | GREEN (zero external modules) |

_v1.0 "body double" is the baseline release — no prior to regress against._
| v2.4 casting call | 2026-07-18 | 7 (M222-M228) | 0 platform edits · rext seeders 96.8% cov · supply-chain GREEN · 14 demo-stack pre-existing/env-gated carries | M228 live 7/7, recruiter p95 1.27s |
