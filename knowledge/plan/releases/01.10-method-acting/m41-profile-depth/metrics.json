{
  "milestone": "M41",
  "slug": "profile-depth",
  "release": "v1.10 \"method acting\"",
  "shape": "section",
  "branch": "m41/profile-depth",
  "closed": "2026-06-25",
  "theme": "Profile depth seeding — a seeded hero (Maya Chen on demo-3) gets a believable work history + education timeline AND a deep, role-aligned skill set with a wide, obvious claimed-vs-verified gap. Before M41 the /profile timeline was empty (public.user_experiences / public.user_educations were 0 rows DB-wide, written by no seeder) and the skill set was shallow (preset verified:8 -> 8 distinct verified skills). A NEW ProfileSeeder (rext stack-seeding, surface 'profiles') writes per END-USER hero (managers skipped — no personal timeline): a companies row per distinct employer (uuid NOT NULL FK target), a 3-job role progression (user_experiences, current role to=NULL) backdated within the activity span with role-aligned titles, a degree (user_educations) backdated before the work history, and a ~60-skill claimed-but-unverified user_skills/user_skill_evidences tail (is_verified=false, no job_simulation_id, tied to the experiences via user_skill_experience/user_skill_education for the DB CHECK, user_level set, anthropos_level NULL). G5 also bumps the preset verified: knob 8->~30 for thriving heroes (Maya 30, Sara 28) so the profile 'overall' reads ~90 = ~30 verified + ~60 claimed — widening the visible gap (the demo's headline aha). Tooling + docs only — zero platform-repo edits; the /profile timeline reads ent.UserExperience / ent.UserEducation via TimelineGrouped(userID) unchanged — M41 only supplies the rows.",
  "code_location": "ext (code-of-record, SEPARATE repo, NOT merged by this close — the tag IS the record): rosetta-extensions @ tag method-acting-m41 (moved at close) -> 0346113 (build commit 81ef650 + review commit 18c4edb + harden commit 63bcceb + the close-fix commit 0346113). The tag moved 63bcceb -> 0346113 at close (the close added 1 adversarial test + the README profile-depth/test-count refresh). rosetta: m41/profile-depth merged --no-ff into release/01.10-method-acting at close.",
  "code_diff": "ext: git -C .agentspace/rosetta-extensions diff 81ef650^..method-acting-m41 — 7 files in stack-seeding (+~1950): seeders/profile.go (491 NEW — the ProfileSeeder: Seed/seedHeroProfile/seedClaimedTail + the deterministic value helpers companyFor/experienceTitle/degreeTitle/fieldOfStudyFor/jsonStringArray/combinedNamedPool/pickSkillNames + the prior-company/institute/location banks); seeders/profile_write.go (150 NEW — profileRows accumulator + flush() FK-ordered COPY [companies->experiences->educations->claimed user_skills] + the claimed-evidence per-row UPSERT with the ON CONFLICT WHERE is_verified=false guard + the column lists); seeders/profile_test.go (10 build funcs); seeders/profile_harden_test.go (24 funcs — 23 harden + the close AR-1); cmd/stackseed/main.go (+1 ProfileSeeder registration); presets/stories.seed.yaml (+verified bump) + presets/stories-maya.seed.yaml (+verified bump). go.mod/go.sum byte-identical (supply-chain GREEN). rosetta: git diff release/01.10-method-acting...m41/profile-depth — doc-half: corpus/ops/seeding-spec.md (the profile-depth layer paragraph: ProfileSeeder fan-out + the live-schema landmines + the never-clobber UPSERT guard + the from<=to-OR-NULL CHECK) + corpus/ops/demo/stories-spec.md (the NEW ### The profile-depth layer (v1.10 M41) + the #M41-D2..D6 tags) + CLAUDE.md (the stories-spec.md description extended for v1.10 M39/M41) + the plan files (overview/progress/decisions/spec-notes/kb-fidelity-audit + the audit-deferrals report + roadmap/state/metrics).",
  "go_test_funcs": {
    "by_module": {"stack-seeding": 496},
    "counting_method": "top-level 'func Test...' + 'func Fuzz...' across all _test.go per rext Go module (ground-truth grep). go test -race ./... -> stack-seeding 8 pkgs ok; go vet + gofmt clean. The M41 surface: seeders/profile_test.go 10 + seeders/profile_harden_test.go 24 = 34 ProfileSeeder funcs.",
    "baseline_M40_close": {"stack-seeding": 462},
    "delta": "stack-seeding 462 -> 496 (+34: build +9 [profile_test.go @ 81ef650 — work-history/education/claimed-tail writes, tail-distinct-from-verified, manager-skip, no-taxonomy-degrades, idempotent, multi-org] + review +1 [struggling-hero tail @ 18c4edb] + harden +23 [profile_harden_test.go @ 63bcceb — flush COPY/Exec error paths, never-clobber-verified guard, idempotent re-upsert, determinism, date-progression monotonicity, date-only truncation, title arc, company dedup, small/exact pool, json escaping, audit per-stack-isolated, empty-org fallback, blank-node-id skip x2, value-helper branches] + close +1 [TestSeedClaimedTail_EmptyEducationsNoPanic @ 0346113 — the Phase-2c adversarial empty-eduIDs modulo guard]). alignment/clerkenstein/stack-snapshot/stack-secrets unchanged by M41.",
    "note": "Total rext Go test funcs through M41 close: 1326 (alignment 52 + clerkenstein 264 + stack-seeding 496 + stack-snapshot 354 + stack-secrets 160). v1.9-close baseline 1248 -> +78 across M39 (+23) + M40 (+21) + M41 (+34)."
  },
  "coverage": {
    "stack-seeding/seeders [profile.go + profile_write.go]": "100.0% (per-function, both M41 files)",
    "note": "Harden Pass 1+2 drove both M41 files to 0 uncovered statements (75.9% avg -> 100.0%). The close AR-1 test exercises the previously-unhit education-branch-unreachable-when-eduIDs-empty path (the in-seeder len(eduIDs)>0 guard). No >2pp drop on any surface; the package residual sub-100% is pre-M41 error branches in surrounding seeders, out of scope."
  },
  "benchmarks": "n/a — no performance-sensitive hot path. The ProfileSeeder is DB-IO-bound bulk COPY + per-row evidence UPSERT; the value helpers are stdlib string-builders over bounded banks keyed by hashInt over the user uuid. No benchmark target.",
  "flake_count": 0,
  "flake_gate": "PASS — 5/5 sequential shuffled (-shuffle=on, -race) runs of the M41-touched stack-seeding/seeders package, 0 flakes; full suite also -race clean. Deterministic by construction (every seeded field picked by hashInt/deterministicUUID over bounded palettes keyed on the hero uuid; wall-clock audit columns [created_at/updated_at/acquired_at] excluded from the determinism contract by design — pinned by TestProfileSeeder_DeterministicAcrossRuns).",
  "supply_chain": "GREEN — 0 third-party Go deps added; stdlib-only posture preserved (profile.go/profile_write.go use context/fmt/strings/time only). go.mod/go.sum byte-identical across 81ef650^..method-acting-m41 (verified: no go.mod/go.sum in the changed set).",
  "alignment_gates": "n/a for M41 — the milestone touches stack-seeding (the seeder fleet), not Clerkenstein. The Clerkenstein alignment gates (100%/100% on the offline surfaces) were last verified at M39 close and are unaffected by M41 (no clerkenstein code changed).",
  "review_summary": "close GREEN — 8 findings, 0 blocking. A near-clean close: build was correct (0 production bugs across the harden Pass 1+2) and harden had already driven both M41 files to 100% statements + pinned the load-bearing invariants, so the review surfaced 0 scope gaps, 0 code-quality must/should-fixes, 0 doc INACCURACIES (the doc gaps were completeness, not errors), 0 test/flake gaps beyond AR-1. Phase 2 code review (Explore agent): no must/should-fix; 1 nice-to-have (combinedNamedPool uses rolePool.take(len) where iteration would do — left as-is, trivial, tiny slice); vet+gofmt clean; consistent with persona/users seeders (Seeder interface, deterministicUUID, COPY/flush, DependsOn ordering behind users+taxonomy+personas). Phase 2c adversarial: 3 scenarios — AR-1 (NEW test) empty-eduIDs round-robin modulo-by-zero HANDLED by the in-seeder len(eduIDs)>0 guard (code already correct; test pins the invariant); AR-2 never-clobber-verified + AR-3 partial-UNIQUE-under-round-robin already pinned by harden; recorded in decisions.md. Docs (Explore agent): 5 completeness findings — (1) seeding-spec from<=to CHECK completed to from<=to OR to IS NULL; (2) seeding-spec gained the never-clobber UPSERT-guard sentence (was only in stories-spec); (3) CLAUDE.md stories-spec.md description extended for v1.10 M39/M41; (4) stack-seeding/README.md seeders/ row now names ProfileSeeder; (5) README Status section gained the profile-depth layer + the test count reconciled 406 -> 496 (ground-truth, the handbook-count-drift fix). Decision triage: M41-D2/D3/D4/D5/D6 blended (already in build doc-half) + tagged (#M41-Dx) in stories-spec.md / seeding-spec.md; M41-D1/D7 + the Adversarial-review subsection archive (maintainer-only).",
  "deferral_audit": "GREEN — M41 introduced 0 new deferrals (its 7 decisions D1-D7 are all RESOLVED design choices; 0 code TODO/FIXME/HACK in the M41 product files; the spec-notes 'TODO:' lines are build-time confirmation scaffolding now resolved by the LIVE-SCHEMA CORRECTIONS + the demo-3 dry-insert validation; the 3 overview Out: items are release-sibling partitions / leave-as-is). The 1 open release-level deferral is inherited: DEF-M40-01 (KPI 'AI simulations completed' = 0 -> Fate-2 -> M42e/M42m, M40-D7) — a single deferral with a confirmed destination, unchanged since M40 close, NOT aged-out (both destination milestones still open, area not re-touched by M41, < 2 milestones / < 3 months old). 0 repeat patterns, 0 chronic, 0 aged-out, 0 escape-hatch. Report: audit-deferrals/deferral-audit-2026-06-25-m41-close.md.",
  "done_bar": "MET — G3 (ProfileSeeder work-history + education timeline: companies + user_experiences + user_educations) + G5 (verified depth bump 8->~30 for thriving heroes + the ~60-skill claimed-but-unverified tail tied to the experiences) all landed Fate-1 in tooling (the rext stack-seeding ProfileSeeder, code-of-record @ tag method-acting-m41 -> 0346113); both M41 files green (-race clean, vet/gofmt clean, 100% per-function statements); go.mod/go.sum byte-identical (supply-chain GREEN); flake gate 5x shuffle 0 flakes; ZERO platform-repo edits (the /profile timeline read path is unchanged — M41 only supplies rows). Live validation (build+harden-recorded): every emitted row [company -> experience(current+closed) -> education -> claimed user_skill -> guarded claimed-evidence UPSERT] dry-inserted against the live demo-3 schema inside a rolled-back transaction — all shapes satisfy the live CHECKs/FKs/enums; rollback verified clean (0 probe rows leaked). The fresh-/demo-up browser acceptance (login-as-Maya: a populated /profile timeline + the wide claimed-vs-verified gap) is the orchestrator's demo step.",
  "notes": "Third milestone of v1.10 'method acting' to close (after M39 + M40); the LAST section milestone of the release. The CODE-of-record lives in the rosetta-extensions authoring copy (a SEPARATE git repo) at tag method-acting-m41 -> 0346113 — this close merges ONLY the rosetta m41/profile-depth doc-half branch into release/01.10-method-acting (the rext tag stays the code record, same pattern as M39/M40 + v1.9 M34-M38). The two remaining milestones M42e (employee) + M42m (manager) are ITERATIVE (per-vantage Playwright 100%-coverage gates) — the release is NOT merged to main at this close; close-release runs after M42m. The inherited KPI=0 deferral is owned by exactly those two coverage milestones."
}
