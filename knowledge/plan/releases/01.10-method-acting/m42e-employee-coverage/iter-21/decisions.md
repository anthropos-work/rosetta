# iter-21 decisions

| ID | Decision | Rationale | Date |
|----|----------|-----------|------|
| D1 | Two manifest namespaces — employee CALIBRATED, manager AUTHORED (`calibrated:false`) | The prompt requires authoring BOTH the M42e employee and M42m manager namespaces now. The employee vantage is the one being swept this iter (demo-3, fully calibrated against the live render). The manager (M36 Workforce dashboard) sweep is P8/M42m (a later run); its descriptors are authored from `stories-spec.md` §"The Workforce dashboard surfaces (M36)" with `region-not-found = FAIL` so a wrong route-guess can't silently pass — the manager calibration pass tunes the selectors against the live render. | 2026-06-25 |
| D2 | Derive the persona role↔skills allow-set from the hero's OWN rendered role skill-panel at sweep time + a junk-pool denylist — NOT a hand-list and NOT a pg client | The prompt says "derive the allow-set from the hero's real role `job_role_skills` read at sweep time, not a hand-list." The platform renders the role's resolved skill panel on `/profile/skills` ("DevOps Engineer at {org}... N skills") — that IS `job_role_skills` resolved at sweep time. Reading it from the DOM satisfies the intent WITHOUT adding a Postgres/pg dependency (supply-chain stays Playwright-only — the M42e hard line). The flat-pool junk denylist (15Five/3D-Dental/…) catches the specific incoherence bug (the TOK-10/P1 root). | 2026-06-25 |
| D3 | Add template-identical SAMPLE rules to the crawl (`/sim/<slug>` cap 20, `/skill-path/<slug>/chapter` cap 12) | The employee/library vantage links ≈300 template-identical sim-detail + dozens of chapter pages — the first calibration sweep exploded past the 150 cap (test timed out, no report). Per coverage-protocol.md "a vantage that genuinely links a huge template-identical set may crawl a representative + boundary sample, PROVIDED the frontier where escapes/failures live exhausts." The sample fully crawls the first N (representative + the boundary cases that surface first, incl. the citation-bearing chapters), records the rest as `sampledOut` (disclosed), and `frontierRemaining` excludes sampled-out paths so `cappedAtFrontier` stays honest. Re-sweep frontier-EXHAUSTED at 62. | 2026-06-25 |
| D4 | The 2 persona failures (avatar menu==profile, org logo) are a LIVE RE-APPLY GAP → route to P8, do NOT hand-patch demo-3 | demo-3's `roster.json` was exported at 03:11, BEFORE iter-16 (org logo) + iter-18 (avatar) landed (~12:00+); it lacks the `picture`/`org_logo` fields, so the fake-fapi serves `image_url=""`. The Clerkenstein `resources.go` + `roster.go` seeder code IS correct (threads both). A fresh demo-up re-exports the roster with the current code → the menu avatar + org glyph thread clean. So the residual closes on the P8 fresh-demo-up; re-exporting the live demo-3 roster would be a hand-patch (against the reproducibility rule — fixes belong in the seeder/snapshot, proven on a fresh bring-up). The gate correctly catches this real staleness. | 2026-06-25 |
