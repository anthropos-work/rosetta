# iter-07 decisions

| ID | Decision | Rationale | Date |
|----|----------|-----------|------|
| D1 | Tighten the `/result` skip regex `/\/result\b/i` → segment-anchored `[/\/result\/[0-9a-f-]{6,}/i, /\/result\/[^/]+$/i]` (must-fix 3) | The bare-word `\b` form over-skips any future path that merely contains "result" as a segment; the segment-anchored forms require the `/result/<id>` deep-link structure (a result page always has a trailing session-id segment). Zero real pages changed this run (verified by the re-sweep still reaching all the same sim pages) — removes the latent risk before the raised-cap sweep. | 2026-06-25 |
| D2 | Add `cappedAtFrontier` + `frontierRemaining` + `maxPages` to `CoverageReport`; emit a runtime CAP-HIT warning; annotate `reachable` so it can't be misread as a true count (must-fix 4) | The run-1 verification mis-read a cap-saturated `reachable===80===maxPages` as a true page count. The new boolean makes "the residual is a FLOOR over a truncated slice" explicit + machine-readable; the console warning tells the operator to raise the cap until EXHAUSTED before grading the gate. A `(0,0)` over a truncated frontier is structurally not gate-met. | 2026-06-25 |
| D3 | Raise `COVERAGE_MAX_PAGES` 80→150; the frontier EXHAUSTED at reachable=87 (frontierRemaining=0) — the TRUE residual (must-fix 1) | The verdict estimated ~269 unreached sims, but the employee/`maya-thriving` vantage's in-app nav only LINKS 39 sim pages + 42 skill-paths + 3 profile + 2 library + 1 home = 87. The 307 sims exist in the library but aren't all crawl-reachable nav links from this vantage. The frontier genuinely empties at 87 — so `(failing, escapes)` is now the real, gate-able number, not a floor. TRUE residual = (failing=3, escapes=3). | 2026-06-25 |
| D4 | DIAGNOSED the 2 empty skill-paths to EXACT root cause; the `directus_versions` ROW-replay hypothesis is FALSIFIED (must-fix 2) | DOM probe: both empty skill-paths render a perpetual loading spinner (`<main>` = ant-flex spinner). Network probe: the `getSkillPath` GraphQL query returns a FEDERATION ERROR — `Failed to fetch from Subgraph 'skiller' at Path 'getSkillPath.chapters.@.jobSimulations.@.simulation.skills'` → skiller log `resolving Entity "Skill": skill not found K-AIFUNX-E658` → `Cannot return null for non-nullable field 'skills.name'`. The missing skill nulls the whole query → empty page. NOT directus_versions. The rendering sibling (`learn-python-data-engineering`) makes the same query and resolves clean. | 2026-06-25 |
| D5 | The missing skill is a STALE-CACHE gap, not a capture-logic bug; fix = taxonomy re-capture (in-rext, zero-platform) — routed to iter-08 | Prod has `K-AIFUNX-E658` "Ai Fundamentals" with `organization_id=NULL, deleted=false` (PUBLIC, created 2025-11-25) + 42790 public skills total. The cached taxonomy (captured 2026-06-09) has 42768 skills and does NOT contain it — 22 public skills (mostly AI-skilling additions 2026-06-11..18) are missing. The demo replays the cache faithfully (demo=42768=cache). Replay is TRUNCATE-then-COPY of the WHOLE surface, so a partial top-up isn't sound — the fix is a full re-capture (needs the sanctioned cold-start DSN + cache swap + full re-replay), an iter-08 capture-path operation. | 2026-06-25 |
| D6 | The newly-surfaced 3rd failure `/sim/get-ready-your-first-ai-simulation-experience-825/start` is a RUNTIME launch surface (crawl-scope class, like `/result/`) — routed to iter-08 | Probe: `<main>` is completely empty (`""`), title "AI Simulation | Start", only a `jobSimulationBySlug` query (success) + no failed requests. 38/39 sim pages render fine (text 768-1107); only the `/start` deep-link is empty — the client populates it only when a real simulation session is launched (a runtime action, not a seedable row). Per coverage-protocol.md "Scope = the vantage's reachable set" + the iter-04/05 result-page precedent: this is crawl-scope (a `skipPaths` rule for `/sim/.../start`), not a seed/serve-grant gap. | 2026-06-25 |
| D7 | The FULL escape set is confirmed at exactly 3 editorial citations (frontier-exhausted) — apply the allow-rule in iter-08 (must-fix 5) | The frontier-exhausted sweep confirms the escapes are exactly 3: strategy-business.com, en.wikipedia.org/wiki/Data_analysis, dremio.com — all editorial `<a href>` citations in REPLAYED `/skill-path/.../chapter` body copy (the chapter pages render full + pass; only the outbound link is flagged). The escape-rising-with-crawl-depth pattern stopped at 3 (no new ones beyond the cap), confirming the set is complete. Default per user pre-authorization: an ALLOW-RULE classifying external editorial citations inside replayed course content as valid (not escapes) + a presenter-notes list — applied in iter-08 (a metric-moving change deserving its own clean re-sweep). Do NOT strip/rewrite the citations. | 2026-06-25 |
