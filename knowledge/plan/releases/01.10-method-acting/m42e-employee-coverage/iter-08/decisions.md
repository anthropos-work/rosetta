# iter-08 decisions

| ID | Decision | Rationale | Date |
|----|----------|-----------|------|
| D1 | Land the iter-07 taxonomy re-capture (`SNAP-M42e-stale-taxonomy-recapture`): re-capture the public taxonomy from the sanctioned `marco_read` prod source (public-only firewall), swap the cache, re-replay into demo-3 | iter-07/D4-D5 diagnosed the 2 empty skill-paths to an EXACT federation error — skiller `skill not found K-AIFUNX-E658` on the non-nullable `getSkillPath.chapters.@.jobSimulations.@.simulation.skills.name`, nulling the whole query client-side. Root cause was a STALE cache (prod 42790 public skills, cache 42768, 22 missing incl `K-AIFUNX-E658`). Replay is TRUNCATE-then-COPY of the whole surface, so a partial top-up isn't sound — a full re-capture is the correct fix. In-rext, zero platform edit, sanctioned public-only read. Confirmed live: demo-3 now has 42790 skills incl `K-AIFUNX-E658`. | 2026-06-25 |
| D2 | Apply the sim-start skip rule (`SCOPE-M42e-sim-start-runtime`): add `/\/sim\/[^/]+\/start$/i` to the crawl `skipPaths` (alongside the `/result/` deep-link skips) | The `/sim/<slug>/start` deep-link is a RUNTIME simulation-launch surface — iter-07/D6 falsified it as a seedable/serve-grantable gap: the probe shows `<main>` is completely empty (`innerTextLen=0`), only a SUCCESSFUL `jobSimulationBySlug` query + zero failed requests; 38/39 sim INDEX pages render fine (the index renders the sim's detail). The client populates `<main>` only when a real simulation session is LAUNCHED (a runtime user action), so a seeded demo legitimately shows an empty launch shell. This is the SAME class as the iter-04/05 `/result/<sessionId>` deep-links — a runtime-computed surface, NOT content the demo can fill under the zero-platform-edit line. Per coverage-protocol.md "Scope = the vantage's reachable set," it's excluded from the crawl frontier: a presenter lands on the sim INDEX (which renders), not on the empty launch shell. The scope-out is HONEST (probe-falsified runtime surface), not gate-gaming — directly analogous to the iter-05 sim-result skip the protocol already sanctions. | 2026-06-25 |
| D3 | Apply the editorial-citation allow-rule (`ESCAPE-M42e-skillpath-external-articles`): an off-demo `<a href>` inside a replayed `/skill-path/.../chapter` body is a VALID editorial citation (a presenter note), not a gate escape | iter-07/D7 confirmed the FULL escape set is exactly 3 — strategy-business.com, en.wikipedia.org/wiki/Data_analysis, dremio.com — all editorial citations baked into the REPLAYED chapter body copy (the chapter pages render full + pass; only the outbound link is flagged). These are real course-content fidelity, not nav-chrome or baked-URL escapes. Stripping/rewriting them would DEGRADE content fidelity (the gate's whole point is real content). Per the user's pre-authorization, the rule classifies an external link on a `/skill-path/.../chapter` path as a valid citation (recorded as an `allowedCitation` → surfaced in a presenter-notes list so the presenter doesn't click it live), NOT counted in the escape total. The rule is NARROW + honest: a nav-chrome or baked-URL escape (e.g. a left-menu "Studio" → prod `studio.anthropos.work`) is NOT on a `/chapter` page and STILL fails the gate — so the allow-rule cannot mask a real escape. This is a disclose-not-hide design: the citation is honestly reported, just not as a gate failure. | 2026-06-25 |
| D4 | Add a per-page `[crawl]` progress heartbeat to `crawl.ts` (observability only; no behavior change) | The coverage sweep is one long Playwright test; with the `list`/`line` reporter it emitted NO stdout until the whole test finished (~10-15 min), which tripped an output-watchdog on the prior run-2 attempt (600s no-output → killed mid-sweep). The heartbeat emits one `console.log` line per scored page (`[crawl] N/cap q=Q VERDICT status path`), so a long sweep STREAMS and stays observable. Pure observability — it does not touch the crawl frontier, the scoring, or the report JSON. Compile-validated via `playwright test --list`. | 2026-06-25 |
