# M21 — Progress

**Status:** planned (not started). **Shape:** iterative (exit gate in `overview.md`).
**Build with:** `/developer-kit:build-mstone-iters` (creates `iter-01/` on first invocation).

## Running ledger
_Appended after each iter (tik = a standard iter toward the gate; tok = a strategy/retro iter). iter-01 is the
unconditional bootstrap tok. No `iter-NN/` subdirs exist until the first build invocation creates `iter-01/`._

_(no iters yet)_
