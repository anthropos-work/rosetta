# M35 — Progress

Section checklist (built by `/developer-kit:build-milestone`). Scope detail in `overview.md`.

- [ ] **`stories[]` blueprint** — per-hero `vantage`/`trajectory`/`skills`, per-story `org`/`narrative`; supersedes `stack.seed.yaml` for demo stacks
- [ ] **Multi-org parameterization** — per-story `OrgID` + `orgClerkID` threaded through the 4 seeders (org/users/identity/jobsim-sessions/assignments) + Clerkenstein org-claim alignment
- [ ] **`PersonaSeeder` roster scaling** — the 2-stories × 3-heroes v1 roster (Cervato + Solvantis)
- [ ] **Trajectory logic** — thriving (dense/rising/under-claim) vs struggling (sparse/low/over-claim)
- [ ] **Supporting-population fidelity** — `job_role_id`+name, ramped `joined_at`, names on non-hero members
- [ ] **Single-org default preserved** — existing `dev-min`/preset path still passes
- [ ] **Docs** — extend `stories-spec.md` (the model) + `seeding-spec.md` (blueprint) + `/stack-seed` SKILL.md
- [ ] **Tests** — `stack-seeding` suite green; closure gene green across all orgs

_Last updated: 2026-06-22 (scaffolded; not started)._
