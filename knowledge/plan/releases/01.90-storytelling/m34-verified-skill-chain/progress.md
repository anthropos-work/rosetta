# M34 — Progress

Section checklist (built by `/developer-kit:build-milestone`). Scope detail in `overview.md`.

- [ ] **G14 fix** — `jobsim_sessions.go`: valid `status`/`completion_status`/`result_status`/token + full `SIMULATION_TYPE_*` + continuous mid-skewed score + per-user growth arc + ASSESSMENT/HIRING share
- [ ] **`TaxonomyRefs` resolver** — real `skiller.skills.node_id` + `skillsByRole`, empty-pool fallback (mirror `contentref.go`)
- [ ] **`PersonaSeeder`** — the 7-table chain per (hero × skill), incl. `user_level` + `result_status` (the `seed.sql` omissions)
- [ ] **`users.go` patch** — real names + avatars + org-domain emails
- [ ] **Closure gene** — data-DNA: 0 dangling node_ids (mirror the M23 cross-surface gene)
- [ ] **Maya proof** — profile + Skill Spotlight chart render + closure green on a `--local-content` demo stack
- [ ] **Docs** — NEW `corpus/ops/demo/stories-spec.md` + `seeding-spec.md` / `safety.md` updates
- [ ] **Tests** — `stack-seeding` suite green; zero platform-repo edits

_Last updated: 2026-06-22 (scaffolded; not started)._
