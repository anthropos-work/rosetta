# iter-06 — Decisions

_Choices made while running the FINAL live cycle, with rationale._

| # | Decision | Rationale |
|---|----------|-----------|
| D-M221-06a | **Execution model: run the bring-up in a tmux foreground pty on the host, poll it in bounded steps from the Mac, grade only after the exit sentinel.** NOT nohup/`&`/a backgrounded-helper/a Monitor. | The build is ~30-45 min but the Bash tool caps a single call at 10 min, and killing a build mid-bake strands the demopatches (ships an unpatched image). tmux is a durable pty that survives SSH disconnects without being "detached/fire-and-forget": the agent stays the synchronous driver — bounded polls, journal heartbeats, waits inline, never re-invoked, grades only on `EXIT=0` + a fresh `autoverify.json`. The forbidden modes (nohup/`&`/detached-helper/Monitor/blocking-wait-re-invoke) all fire-and-forget then stall or grade prematurely; tmux + active polling does neither. (This cycle came up in ~7 min — warm layers+cache — so the tmux durability was insurance, not strictly needed; it would have been essential on a cold-image box.) |
| D-M221-06b | **F-M221-06b: `run-latency.sh` hardcodes an `http://` cockpit URL, which breaks against the M220 cockpit-HTTPS-fronting flip on a REMOTE demo — worked around by driving the harness SPEC directly with `https`, not by forking it.** | This cycle HTTPS-fronts the cockpit ("the presenter cockpit (:17700) is now fronted with HTTPS too"); `http://…:17700` returns 400 "HTTP request to an HTTPS server", so the wrapper's `COCKPIT_URL="http://${HOST}:…"` found no [Log in as] CTA. iter-04/05 predate the cockpit-HTTPS flip, so their http wrapper worked. The FIX-vs-FORK line: the wrapper only (a) computes the URL scheme and (b) runs the green gate — both of which I did by hand (verified `autoverify.json` green independently, supplied `https://…:17700`), then ran the SAME `tests/latency.spec.ts` + `lib/latency.ts`. So the measurement is the shipped harness's, not a reimplementation. A rext scheme-fix (use https when public-host-fronted) is routed forward. |
| D-M221-06c | **Gate 8 is MET with the live-academy run-state disclosed, NOT waived.** A LEFT-LIVE demo's NATIVE ant-academy necessarily carries 3 tracked dirty files while it serves the tailnet. | Gate 8 = "zero platform-repo EDITS" = no durable SOURCE edit to the canonical repos. The 12 containerized/baked platform clones are CLEAN (next-web-app's 6 demopatches revert after the image bakes); `cms ?? studio/` is the disclosed embedded repo (D-05h). The academy is the exception: it runs NATIVELY from its clone, and to accept the `billion.taildc510.ts.net` origin it must have the `next.config.js` dev-origins patch applied, plus its predev hook regenerates `catalog.json`/`content/index.md` (timestamp-only). All 3 revert on `--stop` (F5b, field-proven iter-05). Because the user asked to LEAVE THE DEMO LIVE, `--stop` hasn't run — so grading gate 8 at the CLEAN post-teardown state isn't available; the honest verdict is MET-with-the-run-state-disclosed (no durable edit; reverts on teardown), not a silent pass. |
| D-M221-06d | **F10 fully field-exercised this cycle (both halves) — closes the iter-04 residual rather than routing it forward a 3rd time.** | iter-04 F10 left `assert_ports_free` + the freshness-ABORT unexercised in the field. Both are cheap and safe to force without disturbing the live demo: `assert_ports_free` is a read-only port probe (ran against the live :13000 + free ports); the freshness abort ran in an ISOLATED `/tmp` sandbox (a copied file with a deliberately broken anchor), never touching a live clone. Doing them now — vs a 4th deferral — matches the "an unexercised path is a FINDING, not a pass" discipline. |
| D-M221-06e | **F4 (academy empty grid) recorded as a known cosmetic gap + routed forward — NOT fixed in-cycle.** | The catalog serves 2,705 (HTTP 200) but the grid renders 0 cards + the empty-state — a client-side render defect in the ant-academy repo (iter-04's diagnosis, reproduced). A fix needs ant-academy render-path investigation to become demopatch-shaped; it is NOT a gate condition and the demo's primary surfaces (cockpit + next-web) are fully functional. The task's guard ("do NOT let it balloon or block leaving the demo live") applies — record + route, don't chase. |
