# iter-05 — Decisions

_Fix + strategy choices made while landing F1/F2/F5 and re-proving, with rationale._

| # | Decision | Rationale |
|---|----------|-----------|
| D-M221-05a | **F1 is fixed in THREE places, not one.** (A) `workspaceRootFrom` now PREFERS an ancestor whose `.agentspace` holds `snapshots/` over a nearer empty marker; (B) `dev-setdress.sh` resolves the workspace cache root and passes an explicit `--store` on every replay; (C) `replay` is LOUD about an EMPTY store (the wrong-root smell). | iter-04 diagnosed the seam as `dev-setdress.sh:323` (no `--store`), but the ROOT is stacksnap's cwd walk-up preferring the nearest `.agentspace`. Fixing only the caller (B) leaves every OTHER caller exposed; fixing only the resolver (A) leaves the resolution cwd-dependent. Both + the D17 loud fence (C) make the resolution correct, deterministic, AND self-diagnosing. Each RED-fenced independently. |
| D-M221-05b | **The D17 "fail loud" is a LOUD non-fatal diagnostic on an EMPTY store — NOT a new fatal exit.** | The directive is "fail loud, not silently load 0." Making an empty store FATAL would break the M20 atomicity contract (a genuinely cold box with no capture yet must degrade gracefully to structural-only, not abort). So the empty-store path stays exit 5 (non-fatal skip) but now SAYS "the store is EMPTY — wrong root? the real cache may sit in a parent .agentspace". Loudness is the fence; fatality would regress graceful degradation. |
| D-M221-05c | **F5's real mechanism is a SUPERVISED RESPAWN, not a pidfile gap.** The reap now kills identity-matching ANCESTORS (the `next dev` supervisor), not just the socket-holding worker. | The teardown ALREADY reaped by port+identity (`reap_native_ports`), yet the academy survived on :13077 and teardown reported "free". A dev server supervises + RESPAWNS its worker: killing only the listener let `next dev` re-bind, and the re-probe raced past the respawn. Killing the identity-matching parent chain (stops at the first non-matching ancestor — never a blind pgroup kill, never crosses pid 1) removes the respawn source. RED-fenced with a real supervisor→worker respawn harness. |
| D-M221-05d | **F5b is a `--stop` path-scoped `git checkout`, NOT a gitignore.** | Gitignoring the two predev-hook-generated files (`code/public/catalog.json` + `content/index.md`) would edit the ant-academy repo's `.gitignore` — a forbidden repo edit. Restoring them to their committed state on `--stop` (a tooling action, idempotent no-op when pristine) leaves the clone git-clean for the gate-8 check without touching any tracked file's content. |
| D-M221-05e | **F2 (directus re-capture) is NOT pre-emptively re-captured — the F1 fix likely unblocks directus via M21 auto-provision, verified live.** | iter-04's F2 divergence (`b4cb55bc` wanted vs `ea2e187a` cached) was measured against the BROKEN store — the auto-provision found an empty store and could not apply the cached Structure. The directus cache HAS a `_structure.sql` artifact (confirmed on billion), so with F1 fixed the M21 auto-provision should bridge the bootstrapped-gap schema to the captured one. Re-capturing directus needs a privileged prod source; doing it speculatively before confirming F1 doesn't already fix it would be wasted (and risk). Measure live; re-capture only if it still misses. |
| D-M221-05g | **r3 was FIELD-FALSIFIED on billion; corrected to r4 IN-TIK (not routed).** Cycle 1 (r3) caught TWO residuals my unit fences hadn't modelled — the live box did what a model could not. (i) **F1 empty-subdir:** both resolvers checked the snapshots dir EXISTS, not that it is POPULATED; a prior broken run's `NewLocalFS` mkdir left an EMPTY `stack-demo/rosetta-extensions/.agentspace/snapshots`, which kept shadowing the real cache → `public.skills=0` again. (ii) **F5 detached supervisor:** reap_port's parent-walk could not reach the respawner — Next 16 spawns the `next-server` worker DETACHED (PPID≠supervisor), so the `next dev` supervisor SURVIVED teardown and respawned the worker on :13077 (I confirmed the exact tree live). | Both are trivially + certainly correctable and the tik's WHOLE PURPOSE is to land F1 + jump the gate — closing partial without even the corrected code forfeits the deliverable. **r4:** F1 requires a NON-EMPTY snapshots dir (both resolvers); F5 adds `reap_procs` (kill the supervisor by PORT-ANCHORED identity, tree-independent), wired supervisor-BEFORE-socket into teardown + the academy pre-bind. Both re-fenced against the FIELD-observed cases (RED-proven). The r3 **D17 loud-store diagnostic FIRED live** ("the store is EMPTY… a nearer EMPTY .agentspace shadowing the real cache") — it is what CAUGHT residual (i); and **F5b was LIVE-PROVEN** on the cycle-1 teardown (the academy clone left git-CLEAN). The "every fence RED-proven" + "prove it on the box" discipline paying for itself. |
| D-M221-05h | **`cms/studio` untracked ≠ a platform-repo edit (gate 8).** | The `cms` clone carries an untracked, populated `studio/` (the `anthropos-studio-room` embedded repo — the `make init-studio` submodule-pattern per CLAUDE.md; pre-existing, 18 entries, NOT created by this cycle). Gate 8 is "ZERO platform-repo EDITS" = no tracked source modified; an untracked embedded-repo dir is not an edit. The cms `.gitignore` has a "# Studio for local development" comment but no effective `studio/` ignore (an upstream cms gap, not ours to touch). Disclosed finding; gate-8 edit-count unaffected. |
| D-M221-05f | **CYCLE-2 (r4) REPROVE ≈ GATE PROVISIONALLY MET — the r4 correction worked on the live box.** A DEFAULT cold `up-injected.sh 1` (NO FLAGS) on billion, cold-proven (PG_VERSION mtime `05:07:48Z` > T0 `05:03:18Z`, from inside the container), came up GREEN (exit 0, autoverify all ✓). **Gate scoreboard: essentially 8/8 on this single cycle.** ① Gate 1 login: **Maya p95 2.29 s, Dan p95 1.65 s**, both ACCESS 5/5, over the tailnet HTTPS origin. ② Gate 2 catalog: **all 3 surfaces replayed** — taxonomy 330,261 + directus 11,986 + sim-embeddings 1,490; `public.skills 0→42,790`, `directus_collections=21` (the r4 empty-subdir fix resolved the store root to the REAL cache; directus unblocked itself via M21 auto-provision exactly as D-05e predicted). ③ 3 orgs, Northwind populated. ④⑤⑥ Dana filled (DB), **Ben STARTED 1/3**, **Aria COMPLETED 3/3**. ⑦ remote-by-default auto-discovered, no flag. ⑧ zero platform edits (cms `?? studio/` disclosed, D-05h). **F5/F5b/F12 also FIELD-PROVEN on the teardown**: the port-anchored supervisor reap killed the respawner (`:13077` left free, no survivor), the academy clone left git-clean, serve reset, lock released. **Caveat (M218/M219 lesson): this is ONE cold cycle — the gate demands REPRODUCIBILITY. Provisionally met, not done.** iter-06 (or next session) confirms a 2nd clean cold cycle + the browser-level 900-char check on Dana's page. **Note on process:** the orchestrator prematurely committed a close (`766c029`) telling the STALE r3-falsification story ("gate ~3/8, F1 falsified, routed to iter-06") while this agent was still correcting r3→r4 and re-proving; that close is corrected by the follow-up commit that carries this row. |
| — (orchestrator fallback note, reconciled) | The orchestrator pre-seeded a `D-M221-05f` anticipating the iter-05 agent would run out of budget "after landing the fixes but before grading." That did NOT happen: the agent diagnosed both residuals with live evidence, corrected r3→r4 (D-05g), and ran cycle 2 at r4. The cycle-1 falsification facts it recorded (`public.skills=0`, `green:false/warnings:3`, directus schema error) are accurate for CYCLE 1 and are preserved above; the "ran out of budget / routed to iter-06 unresolved" framing is superseded. | |
