# iter-04 — Decisions

_Choices made during the first live baseline cycle, with rationale._

| # | Decision | Rationale |
|---|----------|-----------|
| D-M221-04a | **Outcome = `closed-no-lift` (baseline-with-full-findings); land NO code fix this tik.** | The task frames this as a BASELINE tik whose primary deliverable is "capture EVERYTHING + report distance," and explicitly blesses `closed-no-lift`. Neither on-the-spot candidate was clean: the academy carry's root cause was disproven (D-04c) and the dominant fix (D-04b) needs a re-cycle. Landing + proving a rext code change in a baseline tik is exactly the ballooning the operating rules penalise ("paid for in HOURS of churn"). |
| D-M221-04b | **The snapshot store-root bug (F1) is IRONCLAD-diagnosed and routed to iter-05 as the #1 fix — NOT landed here.** Confirmed it via a controlled post-baseline replay (`STACKSNAP_STORE` pinned -> `public.skills 0 -> 42,790`). | It is the single dominant blocker (cascades to gates 2/4/5/6), with an exact seam (`dev-setdress.sh:323` sets no `--store`/`STACKSNAP_STORE`, so `workspaceRootFrom` resolves the shadowing empty `stack-demo/rosetta-extensions/.agentspace`). But shipping it needs a rext edit + RED-fence + re-tag + full re-cycle to prove — a second cycle that would balloon this baseline tik. De-risking it to certainty (the manual replay PROVES the fix works) is the higher-integrity move: iter-05 implements a known-good fix, not a hypothesis. |
| D-M221-04c | **Re-characterise `FIX-M221-academy-empty-catalog` — the carry's root cause is WRONG.** The symptom (empty grid, 351 chars) reproduces, but `/catalog.json` serves 2705 courses, `PUBLIC_CHAPTERS` is hardcoded non-empty, and the default lens is "For all" — it is a **client-side render defect**, not the empty `.agentspace/local-content/` the carry blamed. | Blindly "populating local-content" (the carry's proposed fix) would NOT fix an empty grid whose data is already present and reachable. A misdiagnosis shipped as a fix is the D17 shape. The real fix needs ant-academy render-path understanding (out of bounds -> a demo-patch/config seam) -> a route-forward, not an on-the-spot lift. |
| D-M221-04d | **C-6 RAM: 7.3 GiB is SUFFICIENT — decided, no code fix.** | Measured live: 15 containers ~880 MiB, 5.1 GiB available, swap 2/15 GiB, with taxonomy loaded; cold build + fast logins all succeeded. The 12 GiB "floor" is a conservative concurrent-BUILD guard, not a runtime need. An infra headroom bump is optional, not a milestone blocker. |
| D-M221-04e | **Drove the latency gate by invoking the spec directly with an HTTPS cockpit URL + `LATENCY_NO_GREEN_GATE`, bypassing `run-latency.sh`.** | The wrapper hardcodes `http://HOST:17700` (overwriting `LATENCY_COCKPIT_URL` on its own invocation line), but a public-host demo fronts the cockpit over HTTPS (`tailscale serve`; http -> 400). And the green gate legitimately refuses a red stack. Bypassing them gave a valid gate-1 measurement (login/ACCESS does not need the catalog). The wrapper's http-cockpit gap is itself a finding (iter-05 harness fix). |
| D-M221-04f | **Ran the manual confirming replay AFTER driving all gates against the honest as-is baseline** — never before. | The gate demands a DEFAULT no-flag bring-up. Measuring gates 4/5/6 against a hand-patched catalog would grade a stack that is not what `up-injected.sh` produced. Capture the honest baseline first; only then mutate for a diagnostic. |
