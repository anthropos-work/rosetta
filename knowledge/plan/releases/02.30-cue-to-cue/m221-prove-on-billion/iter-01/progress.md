# iter-01 (bootstrap tok) — progress

**Type:** tok (bootstrap) — authors TOK-01, the strategy for the gate-proving tiks.

## Close — 2026-07-15

**Outcome:** Authored TOK-01 — a fix-then-prove strategy in the overview-mandated dependency order
(host-isolation guard FIRST, then off-box code fixes, then the live cold-reset-to-seed battery on billion),
under the operating rules paid for in M218/M219 churn. KB-fidelity gate YELLOW (no blind areas; every
load-bearing carry verified against the code M221 runs).
**Type:** tok
**Status:** closed-fixed
**Gate:** N/A for tok
**Phase 5 grading:** (1) gate-met: n — (2) triggered-tok: n (bootstrap does not exit) — (3) re-scope: n —
(4) user-blocker: n — (5) cap-reached: n — (6) protocol-stop: n — Outcome: continue (bootstrap tok → loop into iter-02 tik)
**Decisions:** TOK-01 (milestone-root decisions.md)
**Side-deliverables:** the KB-fidelity gate fixed 1 stale finding inline (tailscale-serve.md still denied the
academy's LAN exposure in 3 places — the F-M220-5 gap, one layer up; corrected to match safety.md Part 3).
**Routes carried forward:** none new — the strategy consumes the existing M217–M220 carries in dependency order.
**Lessons:** the ordering is the strategy. Host-isolation is not one item among several; it is the precondition
that keeps the multi-cycle battery from corrupting its own evidence the way M219's did.

## Ledger
- iter-01 (tok/bootstrap): authored TOK-01 (fix-then-prove, host-isolation first) — see iter-01/progress.md
