# M201 Progress

This is an **`iterative`**, **USER-GUIDED** milestone — progress accrues per top-down pass toward the **exit
gate** (not a fixed section checklist). The gate + the iteration protocol live in [`overview.md`](overview.md).
Worked **conversationally** + via `/developer-kit:work-mstone-iters` (the user directs each pass). The gate:

> **The manifest corpus is comprehensively outlined, validated, and written as prose-intent YAML (one file per
> product) — covering the platform's products × their must-work user journeys, each use case carrying goal +
> actor + flow + intermediate/final expectations, structurally valid (the spec §5.3 validator passes, ids unique
> + both-way) — and the USER signs off the corpus as the complete-enough v2.0 coverage contract.**

**Status:** `planned` — not yet started. Next: work it **with the user** — `/developer-kit:work-mstone-iters`
(the first pass outlines the readiest product slice from the spec-notes starting outline, validates it, writes the
prose-intent YAML). Prose only — no code, no seed, no validator (those are M202). A use-case precondition the demo
lacks is **recorded + handed to the M202 dedicated-seed expansion**, not resolved here.

## Running ledger

_(Per-pass entries accrue here as the loop runs: the product/story slice outlined, what it was validated against
[the real platform surface / the manifest model / the §5.3 structure], the prose-intent YAML written, any
precondition handed to M202, and the user's steer for the next slice. The corpus converges toward the user's
"complete enough" sign-off.)_

| pass | date | product / story slice | validated against | precondition → M202? | user steer / sign-off |
|------|------|-----------------------|-------------------|----------------------|-----------------------|
| | | _(none yet)_ | | | |
