# iter-05 decisions

| ID | Decision | Rationale | Date |
|----|----------|-----------|------|
| D1 | **Fix the AI-sim launch deny via a post-seed Sentinel `Reload`** (added to `run-playthroughs.sh`), NOT a platform edit or a seed schema change. | Diagnosed: the FE gate `canStartAsOrganizationMember` reads `userMembership.organizationFeatures` → resolver `IsMemberAllowedToUseFeature` → Sentinel `OrgMembershipsAllowedToUseFeature` (g3 grouping-policy match on v0=org). The DB was correct (Pat's membership had the g3 FEATURE_JOB_SIMULATIONS grant + g2 org grant), but the running Sentinel casbin enforcer CACHES its policy in-memory — the seeded grants weren't seen until an explicit Reload. Confirmed: after a Reload RPC (HTTP 200), pt-employee launches the sim. The Reload is Sentinel's OWN RPC (zero platform edits), idempotent, non-fatal. This is a genuine seed-vs-running-enforcer nuance the harness must own (the seeding writes the grant; the runner activates it). | 2026-07-02 |
| D2 | Assert `ai-simulations.chat.UC1` at the **§5.8 launch boundary** (the sim launched + the launch confirmation rendered + the deny modal absent), NOT turn-by-turn or at a scored completion. | Spec §5.8: an AI sim drives a LIVE AI engine — the only thing provable under P6 is the launch/interactive-state boundary, not the AI conversation (forbidden to assert AI prose, P2). The completion/result terminal (a scored outcome) composes with the profile-side verified-skill (profile.verified.UC1, already green) for an assessment-typed sim. So the drivable, deterministic NON-voice truth for the sim journey is the launch. | 2026-07-02 |
| D3 | Pin a **chat/interview-typed** sim (the chat engine), not a voice sim. | The seeded catalog is heavily VOICE-typed ("Voice Call"/"Voice Mode" = the M206 mirror tier, OUT this release). M201 finding: the "interview" sim type reuses the SAME chat container (AISimulationChatContainer), not a distinct voice surface. So `ai-tools-adoption-developer-interview-f78` (described as a "conversation") is a NON-voice chat-engine sim — the in-scope modality (spec §5.8: chat/code/document playable as-is). | 2026-07-02 |
