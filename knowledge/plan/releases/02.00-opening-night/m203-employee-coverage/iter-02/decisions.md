# iter-02 decisions

| ID | Decision | Rationale | Date |
|----|----------|-----------|------|
| D1 | Declare the employee Profile UCs under the **M202 `profile` product** (stories `profile.verified` / `profile.growth`), not a new `profile-skills` product matching the M201 corpus id verbatim. | The M202 foundation manifest already established the `profile` product with `profile.foundation.UC1`. Keeping one product file per surface + adding stories keeps ids unique + both-way valid + the file cohesive. The M201 corpus ids are the prose CONTRACT (goal/flow/expectations), not a required id spelling — the implemented ids stay stable + traceable. |  2026-07-02 |
| D2 | Route `profile.self-evaluation.UC1` (the WRITE flow) forward to iter-03 rather than cram it into iter-02. | The "Re-rate skills" entry timed out under probe (120s) and is the FIRST mutating flow (must prove reset-to-seed). Landing it cleanly needs its own iter (anchor the control + assert at the persist boundary). Scope-creep tripwire: iter-02's planned target was the read journeys; don't accumulate a 3rd finicky line. Fate-3, named handler PT-M203-iter03-self-eval. | 2026-07-02 |
| D3 | Assert the verified/growth surface via **stat-label presence + chart presence** (`svg` within `<main>`), never datapoint values or chart geometry. | P2 (functional truth, not pixel truth): the observable outcome is "the verified-skill / claimed-vs-verified / growth surface RENDERED real data" — the Verified Skills + Role Skills + Skill Gaps stat labels + a drawn chart. The exact counts (8/10/5) and chart shape vary across captures → forbidden to assert. The antd surface has no headings/testids for these, so scoped-within-`<main>` getByText on the visible label is the sanctioned found-only landmark (§5.2). | 2026-07-02 |
