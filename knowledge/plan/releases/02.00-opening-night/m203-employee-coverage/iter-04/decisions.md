# iter-04 decisions

| ID | Decision | Rationale | Date |
|----|----------|-----------|------|
| D1 | Assert `skill-paths.legacy.UC1` at the **browse→open→Start→progress** boundary (the learning journey is real + drivable), NOT the terminal "complete → skill verifies". | The path's assessment (the verify-skill leg) is a VOICE sim (LiveKit → M206 mirror tier, OUT this release; spec §5.8). Per M201 P7 verification is EARNED via an assessment that COMPOSES with ai-simulations, and the verify OUTCOME is proven on the profile side (profile.verified.UC1 — already green). So the drivable NON-voice truth for Skill Paths is the learning journey (browse/open/enroll/progress). Asserting there is the sanctioned §5.8 boundary posture, not a shortfall. | 2026-07-02 |
| D2 | Scope `startButton` to `.first()` (document-order primary CTA). | The path detail renders TWO "Start" buttons (the top path-action CTA at y=264 + the first chapter's inline Start at y=369). A bare `getByRole('button',{name:/^Start$/})` is a strict-mode violation (§5.2 forbids ambiguous matches). Both navigate into the player; the primary CTA is document-order-first → `.first()` is the stable, correct disambiguation. | 2026-07-02 |
| D3 | Defer the **5-run reset-to-seed determinism gate** to milestone-close, run from the stack-demo CONSUMPTION clone. | The reset path (`run-playthroughs.sh --reset`) depends on the `stackseed` CLI, which is not on the AUTHORING copy's PATH — it installs in the per-stack consumption clone (the pinned-tag tooling). The right sequencing is: land all 3 gate journeys first (Profile ✓, Skill Paths ✓, AI Simulations next), THEN prove 0-false-fails over 5 cold reset runs across the whole suite. Fate-3, handler PT-M203-reset-gate. | 2026-07-02 |
| D4 | Add a `public-catalog` capability to `seed-worlds.yaml` for the skill-path precondition. | The skill-path catalog content is the REPLAYED public Directus content (the snapshot set-dress), shared across the demo orgs — NOT materialized by the pt-world roster seed. The validator requires every named precondition to resolve; `public-catalog` names the stack set-dress precondition honestly (distinct from the roster-seed capabilities like verified-skill). | 2026-07-02 |
