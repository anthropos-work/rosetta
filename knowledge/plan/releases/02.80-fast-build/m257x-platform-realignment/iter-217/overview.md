---
iter: 217
milestone: M257x
iteration_type: tik
status: closed-fixed
created: 2026-08-09
---

# iter-217 — the three censuses cannot read this repo's own emphasis idiom

**Active strategy:** [`TOK-08`](../decisions.md) — *census the mechanical classes; stop sampling them.*
A reading SAMPLES; a fence CENSUSES. This iter turns the census on the one **open reach hole** its own
family has carried since harden pass 48.

## Step 0 — re-survey (mandatory)

`TOK-08`'s next-target field is not a named site; the controlling backlog is the route registry.
`SURVEY-M257x-h48-the-censuses-cannot-see-a-bold-wrapped-operand` is open, was re-listed **unchanged**
at passes 49, 50 and 53, and pass 49 recorded it as *"now the only open reach hole of its family"*
after closing the hyphen sibling. Re-surveyed at HEAD `0c9cd79`: still open, still unrepaired
(`_MEASURED_RE` still requires whitespace or a hyphen between operand and noun), and all three ratchets
read **exact +0** — so any movement measured here is this iter's, not inherited slack.

## Cluster / target identified

`derivation_registry._MEASURED_RE` — the ONE regex all three literal censuses share
(`docstring_measurement_literals`, `comment_measurement_literals`,
`excluded_test_module_literals`). Its separator between the operand and the measured noun is
`[\s\-]+`, and its `of`-clause requires `\s+` after the second operand. **This repo bolds its
important figures.** `**292 of 704** modules` therefore matches nothing at all — the emphasis run
closes on the operand, and neither separator admits it.

This is the same shape twice before: iter-205 (the vocabulary was lower-case, the regex was not — every
capitalised noun invisible to all three at once) and harden pass 48 (the separator took whitespace only —
every attributive compound invisible). **Third time on one regex, one character-class over each time.**

## Hypothesis

Admitting a markdown emphasis run adjacent to the operand makes a currently-invisible population
visible to all three censuses at once, moving all three ratchets. The population is non-empty and
contains at least one figure that is stale or mis-worded — because that has been true of every
reach fix in this family.

## Expected lift

Not a `P`/`N` reading. The deliverable is a **census reach fix + a re-derived ceiling on each of the
three ratchets + arms that fence the widening in both directions**, per `TOK-08`.

## Phase plan

1. **Seal** the pre-registered numeric claims V1…V6 in this iter's FIRST commit, before any repair.
2. Widen `_MEASURED_RE` symmetrically (emphasis may close on the operand **or** open on the noun).
3. Re-derive all three ceilings; re-pin with a recorded reason.
4. Arms: the close leg, the mirror leg (sized, staged control), the backtick refusal (derived, not
   asserted), mutation control with sha-verified restore.
5. Adjudicate every new hit; disclose any false positive by name and size.

## Escalation conditions

- If the widening turns a **live fence RED** (not a ratchet — a finding), stop and route: iter-209's
  zero-false-REDs precondition governs any widening of a shipped guard.
- If backticks cannot be separated from emphasis by a rule the code can state, refuse the whole
  widening rather than ship a matcher whose scope is a judgement.

## Acceptable close-no-lift outcomes

A measured **zero** — the emphasis idiom is absent from all four site-kinds — would close this iter as
`closed-no-lift` **provided the instrument proves it fires** (`§9`: a census returning ZERO must prove
its instrument).
