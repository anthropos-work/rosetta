# Seat A — reading #26

**Instrument (proof of delivery):** `briefing-r26-AS-DELIVERED.md`, 243 lines,
sha256 `4178b00f0d8e98eabddb895969d6e4c58799a5b292d967f8a2cfca6772a18d3d` — re-computed at my open,
matches the value I was given. Frozen half + ADDENDUM read in full, top to bottom, before anything else.
`corpus/ops/platform-alignment.md` §5 (lines 249–1549, all 48 rules + Trap E + the trailing sections)
read in full.

## Clone refs re-derived at my open

Every ref below was re-derived with `git -C <clone> rev-parse` at the start of this reading. **No fetch,
no checkout, no state-changing git command was run** (§5 rule 41a).

| repo | path | HEAD | origin/main | vs ADDENDUM |
|---|---|---|---|---|
| app | `stack-demo/app` | `ad9f3c49` | `ad9f3c49` | matches |
| platform | `stack-demo/platform` | `0c91421d` | `0c91421d` | matches |
| next-web-app | `stack-demo/next-web-app` | `8297c684` | `f97ba659` | matches |
| sentinel | `stack-demo/sentinel` | `f2c46190` | `f2c46190` | matches |
| studio-desk | `stack-demo/studio-desk` | `41ee3575` | `41ee3575` | matches |
| ant-academy | `stack-demo/ant-academy` | `22df69dd` | `22df69dd` | matches |
| storage | `stack-demo/storage` | `4ce8ece5` | `9f8cb532` | matches |
| messenger | `stack-demo/messenger` | `fa47850d` | `e9421c68` | matches |
| cms | `stack-demo/cms` | `ca50c817` | `f38c0c4a` | matches |
| graphql-wundergraph | `stack-demo/graphql-wundergraph` | `60c229f3` | `60c229f3` | matches |
| roadrunner | `stack-demo/roadrunner` | `87d8d443` | `87d8d443` | matches |
| jobsimulation | `stack-demo/jobsimulation` | `462343b0` | `82cb66ec` | matches |
| **rext — per-stack CONSUMPTION clone (adjudicates)** | `stack-demo/rosetta-extensions` | **`09d06070`** | `4cb920aa` | matches |
| rext — authoring copy | `.agentspace/rosetta-extensions` | `944fc4a2` (`main`) | — | matches |
| app/studio (nested, own checkout) | `stack-demo/app/studio` | `aeec036a` | `aeec036a` | matches |
| cms/studio (nested, own checkout) | `stack-demo/cms/studio` | `aeec036a` | `aeec036a` | matches |

Corpus HEAD at my open: `3822501`.

**Search set enumerated** (§5 rule 44 step 0): `find stack-demo -name .git -maxdepth 4` returns **15**
git trees — the 13 named above plus the two nested `studio` checkouts. Every tree-wide zero in this
report names the trees it covers.

## Positive controls

| file | `wc -l` | read in full? |
|---|---|---|
| `corpus/architecture/external_services.md` | **883** | yes |
| `corpus/architecture/security_compliance.md` | **281** | yes |
| `corpus/services/roadrunner.md` | **206** | yes |
| `corpus/architecture/dependency_map.md` | **103** | yes |
| `corpus/services/TEMPLATE.md` | **46** | yes |
| **total** | **1519** | — |

All five counts match the assignment exactly.

---

## Blockers

### B1 — `dependency_map.md:19` attributes a claim to `storage.md:40,47`; `storage.md` says nothing of the kind, anywhere

- **Anchor:** `corpus/architecture/dependency_map.md:19`, in the `~~**Storage**~~` row:

  > While the service existed it declared `depends_on: postgres, redis` (both `service_healthy`) and
  > **read neither**: no `DB_CONNECTION`/`REDIS_ADDR` in its compose env, no redis in its `go.mod`,
  > **which is what its own doc says at [`storage.md:40,47`](../services/storage.md)**.

- **Why it is false:** the cited document contains **zero** occurrences of any of the three tokens the
  sentence attributes to it. Measured on `corpus/services/storage.md` (248 lines) with two independent
  instruments plus a positive control:

  ```
  command grep -c -E 'DB_CONNECTION|REDIS_ADDR|go\.mod' corpus/services/storage.md   -> 0   (rc=1)
  git grep    -c -E 'DB_CONNECTION|REDIS_ADDR|go\.mod' -- corpus/services/storage.md -> 0   (rc=1)
  command grep -c 'STORAGE_S3_BUCKET' corpus/services/storage.md                     -> 5   (control OK)
  ```

  The two cited lines resolve, and resolve onto unrelated constructs — the §5 rule-46 /
  rule-21 "anchor resolves but names the wrong construct" class:

  - `storage.md:40` = `> being when the clone advanced to `ad9f3c49`.)` — the closing half of a
    parenthetical about the demo build pin moving off `b948604f`.
  - `storage.md:47` = **a blank line** (it sits between the `## Architecture & Code Map` heading at `:46`
    and the `* **Codebase**` bullet at `:48`).

  The *substance* of the sentence is true — I verified it independently, which is what makes the false
  attribution the whole finding rather than a compound error. At `platform 0dab54d` the `storage` block
  (`docker-compose.yml:102-134`) declares `depends_on: redis {service_healthy}, postgresql
  {service_healthy}` (`:126-130`), its `environment:` (`:116-123`) sets neither `DB_CONNECTION` nor
  `REDIS_ADDR`, and `storage/go.mod` @ `4ce8ece5` has **0** redis requirements (`git grep -c -i redis
  4ce8ece5 -- go.mod` → rc 1; control: the same file lists `connectrpc.com/connect`, `colony`, `proto`,
  the AWS SDK).

  So a reader who follows the citation to check the claim finds a sentence about a git sha and a blank
  line, and has no way to tell whether the claim was ever sourced. This is exactly §5 rule 34 (a corpus
  repair moves line numbers and silently invalidates every citation below the edit) landing on content
  rather than on a blank line, which is the case rule 34 says `anchor_construct_guard` cannot catch.
  Note `storage.md:39-40` is itself text the immediately-preceding repair wrote ("Corrected M257x
  iter-102"), i.e. the citation target was displaced by the same sweep that produced the corpus under
  audit.
- **Confidence:** high.

### B2 — `external_services.md:565` (and `:598`) cite `markdownManager.go:19` for `mistral.NewMistral(...)`, a construct that no longer exists in `app`

- **Anchor:** `corpus/architecture/external_services.md:565`, the **Mistral (EU)** provider row, cell 4:

  > **OCR only** — `mistral.NewMistral(...)` in `app/internal/cms/studio/markdownManager.go:19` for
  > studio attachment → markdown

  and the same anchor restated at `external_services.md:598`, item 5 of *Routing: what is actually
  implemented*:

  > *Every* use of it in `app` is **OCR** — the cms domain's Go client
  > (`internal/cms/studio/markdownManager.go:19`, `studioManager.go:583`) …

- **Why it is false:** the cell names **no `app` ref**, so per the instrument it is graded at the
  checkout, `app` `ad9f3c49` (== `origin/main`). There:

  - `internal/cms/studio/markdownManager.go:19` is a **comment**:
    `// It used to take aiKey and then IGNORE it, reading os.Getenv("MISTRAL_API_KEY")`.
  - `mistral.NewMistral` does not exist anywhere in the repository:
    `git -C stack-demo/app grep -n 'NewMistral' ad9f3c49` → **rc 1, 0 hits** (whole tree, no pathspec).
    Positive control in the same pass: `git grep -c 'mistralocr' ad9f3c49` → 7 files, incl.
    `internal/cms/studio/markdownManager.go:3`.
  - The live construct is `mistralocr.New(aiKey)` at **`markdownManager.go:30`**, inside
    `func NewMarkdownManager(aiKey string) (*MarkdownManager, error)` at `:29`; the struct field is
    `ocr *mistralocr.Client` at `:14`.

  The anchor was exact at the **previous** demo pin: at `app` `b948604f`,
  `markdownManager.go:19` is `ai, err := mistral.NewMistral(nil, os.Getenv("MISTRAL_API_KEY"))`. So this
  is a claim that stopped being true when the clone advanced, in a cell whose siblings in the same table
  *do* carry pins (`:567` writes "@ `app` `b948604` v1.366.0" for the `main.go:770` anchor). The
  companion citation `studioManager.go:583` still resolves exactly
  (`markdownManager, err := NewMarkdownManager(os.Getenv("MISTRAL_API_KEY"))`), which makes the stale one
  harder to notice, not easier.

  The predicate the anchors support — *every Mistral use in `app` is OCR* — I verified separately and it
  **holds** at `ad9f3c49`: the only remaining non-OCR use, a Mistral client held solely for `Tokenize`,
  was removed and now routes to `internal/ai` (`markdownManager.go:46-50`; `internal/ai/tokenizer.go:35`
  records the removal as a comment). So the harm is bounded but real: a reader grepping the named symbol
  gets zero hits and cannot tell whether the corpus is stale or the claim is wrong.

- **Why I hesitated (BLOCKER vs MINOR):** taken narrowly this is line drift, which §5 rule 25 records the
  canonical grading rule as carving out to MINOR. I booked it BLOCKER because it is *not* drift: the
  named construct is **deleted repo-wide (0 hits at the graded ref)**, the anchor lands on a comment
  rather than on nearby code, and the defect is duplicated at a second site (`:598`) so a reader who
  checks one and gives up has still been misled twice. The delivered instrument also directs the tie to
  BLOCKER.
- **Confidence:** medium (high on the measurement; the medium is entirely about the grading rule for a
  claim that is exact at `b948604f`).

---

## Minors

- `external_services.md:390` — "**Playground** at `/graphql` for local development" and "**Introspection**
  enabled in dev mode" sit in *What the gateway provides*, which is **above** the HISTORICAL fence
  (the fence opens at `:418`). `:533` in the same file says the router's `:5050` playground "is gone
  locally". The prod-only framing at `:3`/`:368` mostly covers it; the words "for local development" do
  not.
- `external_services.md:487-495` — the `### Configuration` section falls just outside the fence at `:418`
  ("everything from here to the end of *Subgraph routing URLs*", which ends at `:486`), yet describes the
  **deleted** local compose build in present tense: `ENVIRONMENT=compose`, and "**Build Context**: the
  platform monorepo (`context: ..`)". Both were properties of the compose block `360efd4` removed.
- `external_services.md:636` — cites `config_template.ini:30-31`. The file is
  `app/studio/configs/config_template.ini` (there is no root-level copy at `aeec036a`); the line numbers
  are correct there (`:30 OPENAI_API_KEY = sk-aaa`, `:31 OPENAI_ENDPOINT = https://api.openai.com/v1/chat/completions`).
- `external_services.md:455` — the text attributed to `graphql-wundergraph/CLAUDE.md:39` in quotation
  marks (*"Since cms-in-app the platform compose `graphql` service builds from the production
  Dockerfile"*) is a paraphrase; the line actually reads "**The committed `schemas/backend.graphqls` is
  now load-bearing locally too.** Since the compose stack builds from the production Dockerfile, …".
  Anchor and substance are both right; only the quotation is not verbatim.
- `security_compliance.md:73` — "`UserMixin{}`, whose `Policy()` (`mixin.go:98`)". `mixin.go:98` is the
  doc comment; `func (UserMixin) Policy() ent.Policy {` is `:99`. Inconsistent with the sibling citation
  `mixin.go:126` in the same blockquote, which lands exactly on
  `func (OrganizationMixin) Policy() ent.Policy {`.
- `security_compliance.md:7` — the PM summary states the classification flatly ("AI Simulations **are**
  classified as **Limited Risk** under the EU AI Act") while the body fences the identical statement as
  "**what is STATED, not what this corpus asserts**" (`:252`) and says "**Do not cite this section as
  evidence of a Limited-Risk classification**" (`:248`). Read as a passive report of someone else's
  classification the summary is defensible, and §5 rules 47/48 (the binding 2026-08-06 decision) say a
  compliance item whose false assertion is already removed is closed, not re-litigated — so MINOR, not
  BLOCKER.
- `roadrunner.md:13-14` — "`roadrunner/terraform/main.tf:19` still reads `service_desired_count = 1` and
  **has not been touched since `87d8d44` (2026-06-19, before the fold)**". `87d8d44` is the repo's HEAD
  and did **not** touch that file; the last commit that did is `e45eb61` (2026-05-27). The sentence is
  vacuously true (nothing follows HEAD) but reads as naming the commit that set the value. The operative
  claims — value `= 1`, and the freeze date predating the fold — both verify.

---

## Positively cleared (audited zeros)

Every item below is labelled **ENUMERATED** (predicate derived independently from source, cardinality
compared) or **RE-DERIVED** (the document's own arithmetic re-computed). Only ENUMERATED is a clearance.

### `security_compliance.md` — the multi-tenancy fence (`:66-137`) — **ENUMERATED**

Re-derived the SET, not the sum (§5 rule 24), from `app` `ad9f3c49` via `git archive` into a scratch
tree, at the ref the passage itself names:

| predicate | corpus | measured |
|---|---|---|
| `.go` files in `internal/data/ent/schema/` | 139 | **139** |
| files declaring `ent.Schema` | 135 | **135** |
| the 4 non-schemas | (implied) | `database_types.go`, `mixin.go`, `skiller_mixins.go`, `skillpath_mixins.go` |
| files *mentioning* `OrganizationMixin{}` | 30 | **30** |
| of those, commented out | `user_resource.go:22` | **`user_resource.go:22`** — `// OrganizationMixin{},  // We need to work on this` |
| live `OrganizationMixin{}` users | 29 | **29** |
| `OrganizationIDMixin{}` users | 7 | **7** — `category`, `jobrole`, `similarity`, `skill`, `specialization`, `studio_document`, `studio_task` (the exact seven named) |
| plain `organization_id`, neither mixin | 18 | **18** (set matches member-for-member) |
| of the 18, policed | `org_membership.go`, `academy_feedback.go` | **both confirmed** |
| the 16 named unpoliced | 16 names | **all 16 present, no extras** |
| files declaring any `Policy()` | 4 | **4** — `mixin.go`, `org_membership.go`, `organization.go`, `user.go` |

Conjunct-level checks (§5 rule 17, the clause this fence has failed on three times): `organization.go:56`
= `func (Organization) Policy()`, `:96` = `rule.FilterSameOrganizations()`; `org_membership.go:172-188`
is the `Membership` policy, both arms terminating in `privacy.AlwaysDenyRule()`; `user.go:116` =
`func (User) Policy()`, user-scoped; `mixin.go:126` = `func (OrganizationMixin) Policy()`;
`UserMixin.Policy()` contains `rule.FilterOwnerRule()` in both arms. `OrganizationIDMixin` is defined at
`skiller_mixins.go:148-162` as "a plain, nullable `organization_id` UUID column + index. It intentionally
does NOT attach backend's OrganizationMixin privacy policy" — so all 7 carry the column via the mixin and
declare no `Policy()`; the corpus's "carry an organization_id" is true even though 3 of the 7
(`similarity`, `studio_task`, `studio_document`) contain no literal `"organization_id"` in their own file.
`user_resource.go` carries **no** `organization_id` (rc 1), so its exclusion from both the 31 and the 23
is correct. Arithmetic `135 − 31 − 23 = 81` **RE-DERIVED** and consistent with `academy_feedback.go`
being placed in the remainder at `:105`.

Tenant-scoped four (`:104`): `lab.go:63`, `academy_chapter.go:84`, `academy_skill_path.go:78` each
`field.String("tenant_eid")`; `skill_path_session.go:43` `field.UUID("tenant_id", uuid.UUID{})`. None
carries `organization_id` or either org mixin; the first three declare no `Policy()`; the fourth carries
`UserMixin{}` at `:26`. `academy_feedback.go:129` = `field.UUID("organization_id", …)`, `:64` =
`UserMixin{}`. Platform self-statements verify: `job_simulation_session.go:5` = "L2: NO Ent privacy
Policy; owner/org/tenant are plain fields"; `jobrole.go:18` / `category.go:15` = "no
UserMixin/OrganizationMixin and no Policy(), so the taxonomy stays globally readable". The claim is
"at least four" tenant-scoped, and the tree-wide grep finds more (`academy_path_embedding.go`,
`api_key.go`, `job_simulation_session.go`) — consistent with the weak form as written.

### `security_compliance.md` — EU AI Act rebuttal (`:231-250`) — **ENUMERATED**

`v3/validator/skills.go:53-62` = `calculateSkillScore`, counting `check.Success` booleans; `:75` =
`score = utils.RoundFloat(float32(passed)/float32(total)*100, 2)` — the division. `checkerEngines` has
exactly 6 occurrences repo-wide (`criterion.go:34,72,90`; `v3/validator/validator.go:43,60,595`), none of
them a lookup — "stored and never read" holds. The dispatch is the hardcoded switch at
`criterion.go:126`, `case check.EngineLlm:` at **`:127`**, `case check.EngineTextDiff:` at `:142`,
`default:` at `:153`. `NewLLMBulkChecker(c.logger)` is at **`:428`**.
`basevalidator/templates/checkValidationBulk.tmpl` exists and `:27` reads "assess whether the `<asset>`
shared by the user (the player) meets or does not meet the {{ .ChecksCount }} checks", with
`check_id` / `feedback` / `success` all present. `criterion.go:168` dispatches `validateCodeDiff`, whose
body `:450-475` sets `success` from `cdiff.Diff == ""` with no model. Both conjuncts of the corpus's
rebuttal, and its "most, not all" hedge, are supported.

### `security_compliance.md:147` — org switching — **ENUMERATED**

All six anchors (web + hiring): `useOrgSelection.tsx:94` = `await clerk.setActive({ organization: clerkOrgId });`,
`useResolveActiveOrg.tsx:107` = `.setActive({ organization: candidate })`,
`useActivateMembershipOrg.tsx:81` = `clerk.setActive({ organization: clerkOrgId })`. @ next-web-app `8297c684`.

### `external_services.md` — the AI-routing section (`:554-673`) — **ENUMERATED**

`internal/jobsimulation/ai/ai.go` @ `ad9f3c49` (no ref named in the block → checkout): `:30-33` the four
vendor consts; `:259-289` `getClient`; `:264-276` `azureClientEu` + `flag_use_azure_us` swap, logging and
keeping EU on lookup error; `:280-283` `AnthropicAws` and `Anthropic` both → `a.anthropicClient`;
`:130-141` `isThrottlingError` matching 429 from the OpenAI and Anthropic SDKs; `:150-155` and
`:296-302`/`:326` the two `vendor = Openai` retry overrides; `:80` `openai.NewOpenAI(openaiKey)`;
`:85-88` `config.WithRegion("eu-west-1")`. `security_compliance.md`'s parallel anchors (`:262-266`,
`:263-277`, `:129`/`:166`/`:325`, `:85-95`) resolve onto the same constructs — the two files use
different windows but do **not** contradict each other.

The `ai_vendor` default chain: `internal/cms/directus/collections/jobsimulation.go:905`
`AIVendor *AIVendor` (nullable); `:1302-1305` `aiVendor := simulation.Openai` + nil guard;
`internal/jobsimulation/simulator/ai/ai.go:58-59` maps `simulation.Openai` → `internalAi.Openai`;
`:114-115` the `default:` arm doing the same. `coursebuilder/bedrock.go:23`/`:29` the two model ids,
`:100` `return "anthropic-api"`, `:106-113` `newUnderlyingClient` (key set → `NewAnthropicClientWithModel`,
unset → Bedrock); `askengine/bedrock.go:25` and `jobsimulation/agent/report_agent.go:31` both
`eu.anthropic.claude-sonnet-4-6`; `app/main.go:770` `logger.Info("coursebuilder model backend", …)`
@ `b948604f`, the ref that cell names.

Studio-Room: `services/ai.py:627-664` = `class AnthropicProvider` … `return Anthropic(api_key=…)`;
`:704-724` = `get_client` with provider registry `{openai, azure, anthropic}` at `:705-708`; `:383` =
`return OpenAI(api_key=self.api_key)`; `gen.py:41-53` overrides only `*_API_KEY`/`*_ENDPOINT`.
The "all 30 `*_AI_*_MODEL` lines pin `azure`" claim is **ENUMERATED**: exactly **30** matching lines
across the **3** `configs/*.ini`, and every one begins `azure,` (`uniq -c` over the value field returns
8 distinct values, all `azure, …`). @ `app/studio` `aeec036a`, its own checkout.

Env-var counts (`:668-673`, pinned `5ba17044`) **ENUMERATED** by re-running the document's own command on
a `git archive` of that ref: `OPENAI_KEY` 5, `ANTHROPIC_API_KEY` 26, `AZURE_OPENAI_KEY` 13,
`AZURE_OPENAI_ENDPOINT_URL` 13, `OPENAI_ORG_ID`/`AZURE_OPENAI_ENDPOINT`/`AZURE_OPENAI_DEPLOYMENT` 0,
`OPENAI_API_KEY` 2 — identical at `ad9f3c49`.

Mistral scope: `tools/pdf2md.py:24` `from mistralai import Mistral`, `mistral-ocr-latest` at `:127`;
`r3.py:139`, `:190`, `:199-206` are the dispatch (`# STEP 2`). `git -C app/studio grep -ci mistral
aeec036a` → **22 lines / 3 files** (`requirements.txt`, `tools/pdf2md.py`, `tools/r3.py`) — exactly the
figure `dependency_map.md:25` publishes, and the studio tree is grepped at **its own ref**, not the host's
(§5 rule 44 mechanism 3).

LiveKit (`:723`) **ENUMERATED**: `calls/livekit.go:110` `agentName := "anthropos-agent"`, `:115`
`"anthropos-agent-chain"`, `:120` the EU branch, `:126` `fmt.Sprintf("anthropos-agent-%s", *location)`;
endpoints `azure-eu` `:111` / `azure-%s` `:127`. `anthropos-agent-eu` = **0 across all 14 platform trees
at their own refs** (positive control: `anthropos-agent` → 6 lines in `app`). `:727`'s voice-engine claim
holds: `jobsimulation.go:1595-1596` returns `SimulationVoiceEngineGptrealtime` on nil, and
`flag_use_realtime_openai` (`livekit.go:131-144`) changes only the **endpoint** to `openai-hosted`.

Taxonomy figures (`:687-699`) **ENUMERATED** against the manifest itself: `public_only: true`,
`predicate: "org-null"`, `captured_at 2026-06-29T14:43:00Z`, skills 42,790, job_roles 22,470,
skill_embeddings 42,790, job_role_embeddings 18,919, skills filter `organization_id IS NULL`.

### `external_services.md` — the gateway/subgraph history (`:365-495`) — **ENUMERATED**

Subgraph ladder read straight off `supergraph-config-prod.yaml` at each commit:
`749dc86~1` → **5**, `749dc86` (2026-06-24) → **4**, `7c17e63` (2026-07-21) → **3**,
`915da06` (2026-07-29) → **1**. `git show --name-status 915da06` marks **both** `schemas/cms.graphqls`
and `schemas/jobsimulation.graphqls` `D`, and that commit's own subject really does say "supergraph 2→1"
— the corpus's warning is correct and its correction is correct.

Subscriptions: no `subscription:`/`sse` in any `*.yaml` at `60c229f3` (control:
`grep -rln backend ./*.yaml` matches all three); `915da06~1:supergraph-config-prod.yaml:13` still reads
`protocol: "sse_post"`; `git log -S 'protocol: "ws"'` → **empty** (control: `-S sse_post` → **5**
commits); `bba862f` (2026-02-25) is **not** an ancestor of HEAD (rc 1) and
`git branch -a --contains` names only `remotes/origin/feat/use-web-socket`.
`schemas/backend.graphqls`: `type Mutation` at **`:4053`**, `type Query` at **`:4912`**, **no**
`type Subscription`, and exactly **6** `Subscription` substring hits, all Stripe/plan
(`activeSubscription`, `type PlanSubscription`, `stripeSubscriptionId` ×2, `stripeSubscriptionItemId`,
one doc line).

Ports/terraform: `terraform/locals.tf:8` `port = 8080`; `terraform/main.tf:48-49`
`"containerPort": ${local.port}` / `"hostPort": ${local.port}`; `config.prod.yaml:5`
`listen_addr: 0.0.0.0:8080`; `terraform/main.tf:20` `service_desired_count = 1`. `"5050:8080"` is the
local compose host mapping at `1e8e754:docker-compose.yml:14`, gone at `360efd4`.
`subgraphs.conf` = `BACKEND=v1.360.0`; `schemas/` holds `backend.graphqls` alone; the routing_url is
`http://backend.internal.anthropos:8080/graphql/query`.

Dockerfile eras: `2c85211^` has `context: ../graphql-wundergraph` and **no `dockerfile:` key**;
`2c85211:8` adds `dockerfile: Dockerfile.dev`; `63d285c` (2024-06-20) names the service `wundergraph`
with a git-URL context; `b56d731:22` still has `  graphql:` (parked at `profiles: [wundergraph-deprecated]`,
`:36`) and `360efd4` has none (control: `  backend:` at `:28`). `Dockerfile.dev` at `60c229f3`:
`wgc@0.104.0` `:7`, **one** schema `COPY` at `:18`, the replacement comment at `:19-20`, **one** `awk`
at `:23`, `ghcr.io/wundergraph/cosmo/router:0.275.0` at `:29`. `graphql-wundergraph/CLAUDE.md:39` does
assert the production Dockerfile, in `60c229f` dated 2026-07-30 titled "…correct the compose build path".
`depends_on` at `1e8e754:docker-compose.yml:19-27` names exactly **four** services, each
`condition: service_started`.

### `external_services.md` — Directus / compose posture — **ENUMERATED**

`docker-compose.yml` @ `0c91421` is 186 lines and declares **5** services (`sentinel`, `backend`,
`studio-desk`, `next-web-app`, `gotenberg`); `common.yml` adds `postgresql` + `redis` with **no**
`profiles:` key, and `sentinel` has none either → floor of 3, **7 effective**. At `0dab54d`: 8 declared,
**10 effective**, and `repos.yml` had **6** entries; at `0c91421` `repos.yml` has **4**
(app, sentinel, next-web-app, studio-desk). `backend`'s `environment:` runs `:46-94` and sets
`DIRECTUS_PUBLIC_BASE_ADDR` at **`:53`** and no `DIRECTUS_BASE_ADDR`; `.env_example:91-92` declares
`DIRECTUS_BASE_ADDR` + `DIRECTUS_TOKEN` and nothing else Directus-shaped. Frontend endpoints:
next-web-app build arg `:151` / runtime `:160`, studio-desk `:119` / `:135`, all four
`:8082/graphql/query`. `app/main.go:980-982` is the `DIRECTUS_BASE_ADDR` `log.Fatalf` @ `b948604f`, the
ref cited. `app/cms_reader_switch.go:28-29` carries the quoted "DIRECTUS_BASE_ADDR … a DIRECT domain call
— no proto round-trip, no re-threading, and no internal traffic to a standalone cms" verbatim.
The historical Directus block: `a2a3ee6^:docker-compose.yml:384` `image: directus/directus:10.10.1`,
`:386` `- 8055:8055`, `:409` `- ADMIN_PASSWORD=password`; `git log --all -S 'admin@example.com'` returns
nothing (control: `-S 'ADMIN_PASSWORD=password'` returns `a2a3ee6` + `0551001`). So the
retraction-of-the-retraction at `:142-148` is right on every measurable clause.
No `profiles:` key anywhere at `0dab54d` contains the `graphql` token (`:100,134,154,195,226,253,268`).

### `external_services.md:136,206-219` — the rext re-point — **ENUMERATED, at the pinned per-stack clone `09d06070`** (§5 rule 45)

`stack-injection/gen_injected_override.py:86` = `DIRECTUS_DATA_CONSUMERS = ("cms", "backend")`;
`:77-81` carries the inert-key rationale including both quoted fragments ("never matches it on a current
clone", "kept only so a ROLLBACK/older platform clone that still DEFINES the container gets re-pointed
too"); `:698-699` = `if with_directus and name in DIRECTUS_DATA_CONSUMERS:` /
`env.append(f"DIRECTUS_BASE_ADDR={DIRECTUS_INNETWORK_ADDR}")`; the dev twin
`stack-core/gen_override.py:58` is byte-identical on the tuple.
`stack-injection/tests/test_injection.py:1109` = `def test_backend_the_actual_reader_is_repointed(self):`,
and `test_only_cms_is_repointed_not_other_services` survives only inside that test's own comment.
The prior-pin claim also verifies: at `ab81527a`, `:84` is the tuple and `:669-670` the re-point.
I read the **pinned consumption clone**, not the authoring copy.

### `dependency_map.md` — **ENUMERATED**

`:7` every anchor: `AUTHORIZATION_ADDRESS` `:48`, `GOTENBERG_URL=http://gotenberg:3200` `:57`,
gotenberg's `profiles: [core, backend, all]` `:183`, `JUDGE0_BASE_URL` `:59`, `REDIS_ADDR` `:66`,
`SUPABASE_DB_CONN`/`COPILOT_DB_CONN` `:93-94`, `app/internal/converter/gotenberg.go:31` =
`http.NewRequestWithContext(ctx, "POST", gotenbergURL+"/forms/libreoffice/convert", &body)` @ `ad9f3c49`.
`git grep -n RPC_ADDR 0c91421d -- '*.yml' '*.yaml'` → **rc 1** (control: `AUTHORIZATION_ADDRESS` → 1 hit),
so "zero `*_RPC_ADDR`" holds. The quoted twin at `architecture_overview.md:331` exists verbatim.
`:13` `depends_on` at `:101-109` with the in-line comment at `:102-103` quoted correctly.
`:22` `0dab54d:docker-compose.yml:154` = `profiles: [customerio-sync, all]`, and the retraction of the
"second Brevo pusher" reading is **correct against the platform's own commit message**, which asserts the
opposite: `838d907`'s body says `make up-all` started "a second Brevo contact pusher alongside backend's
own", but at `0dab54d` `backend`'s env block sets **neither** `MESSENGER_ENABLED` nor
`CUSTOMERIO_SYNC_ENABLED` (grep over `:28-101` → rc 1), and `app/env_guards.go:56-63` + `:37-44` make
unset mean OFF on `ENVIRONMENT=development`. `service_taxonomy.md:101-103` carries the same retraction —
uniform, not split.
`:24` studio-desk `depends_on` `:138-140` is `backend` alone.
`:31` `jobsimulation/terraform/main.tf:15-40` **@ `6092c6d2`, the ref the cell names**, is exactly the
M810 decommission comment: module block deleted, `livekit.tf` / `chime.tf` / `ssm.tf` / atlas tracker
surviving, legacy-schema drop called out as a separate pending step. `cms/terraform/main.tf:39` =
`service_desired_count = 0`; `6efa1d5` (2026-08-04, merged `f38c0c4`) deletes
`.github/workflows/build-production.yml` with the quoted "the cms ECR repository is decommissioned
(M810)". Both-sides-and-assert-neither framing is faithful.
`:42/:48/:49/:50` the shared-library matrix, derived across all **7** Go repos at their pinned refs:
colony **7/7**, proto **7/7**, taxonomy **6/7** (absent only in roadrunner), ai **2/7** (`cms/go.mod:9`,
`jobsimulation/go.mod:11`, both `v1.40.2`), authn **0/7** — and authn is 0 in Go source too (0 files
importing `github.com/anthropos-work/authn` across all 7). `colony/authn` importers: app **129** `.go`
files @ `ad9f3c49`, cms **6**, jobsimulation **8** — the exact figures. `app/go.mod:20` = taxonomy @
`b948604f`; `sentinel/go.mod:21` = taxonomy `// indirect` @ `88bc5592`; messenger direct / storage
indirect as stated. `1e457fa70` = "refactor(ai): fold the ai library into app as internal/ai", and `ai`
is gone from `app/go.mod` at `ad9f3c49`; `internal/ai/module_import_guard_test.go` exists.
`:58` `messenger/internal/flow/flow.go:72` = `h.subServer.AddSubscriber("backend", sub.AddHandler(`;
`app/internal/messenger/flow/streams.go:65` = `StreamBackend = "backend"` and `:70` documents the
21-handler set (21 not 22, one commented); `main.go:1493` names it; `main.go:1445` = `if messengerEnabled {`;
`:1437` = "ALL OF IT is behind MESSENGER_ENABLED."; `:285` =
`messengerEnabled := mustSubsystemSwitch(envMessengerEnabled)`; `env_guards.go:61` =
`envMessengerEnabled = "MESSENGER_ENABLED"`; `docker-compose.yml:84-92` @ `0c91421` carries the quoted
"which default to OFF on a developer machine". The iter-102 correction of this cell is right.
`:59` the skiller row — **the SET, not the sum**: every `pubsub.NewPublisher` in `app` @ `b948604f`
(non-test) is exactly the five the cell enumerates (`main.go:287` with `serviceName` defaulting to
`"backend"` at `:213-215`, `:637` SKILLPATH, `:1039` CMS, `wiring.go:127` AI_USAGE, `:180` JOBSIMULATION);
the sixth `NewPublisher` in the tree is `redisstream.NewPublisher` in the dead-letter queue, a different
constructor. `SKILLER_STREAM` is in none of them, at `b948604f` **or** at `ad9f3c49`. The six-drift /
one-holds split is exact: at both `2035f9a4` and `ad9f3c49`, `:287` = `logger.Info("subsystem switches",`,
`:637` = `)`, `:1039` = `serverContext,`, `:1276` = `apiKeyManager,`, `wiring.go:127` = the asynq client,
`:180` = blank — while `internal/roles/roles.go:791` is
`func (r *RoleManager) SkillerSubscriber() *pubsub.Subscriber {` byte-identical at all three refs.
`git grep -n SKILLER_STREAM ad9f3c49 -- '*.go'` → **6 lines**; `git grep -l` with the pathspec → **3
files** (`main.go`, `subscriber_merge_test.go`, `subscriber_wiring.go`); without it → **6 files**.
`main.go:1537` = `Skiller: os.Getenv("SKILLER_STREAM"),` inside `streamNamesForSubscribers` (`:1534`).
`docker-compose.yml:71` = `SKILLER_STREAM=skiller`. Every clause, including the retraction of the
"not one of those line numbers resolves" universal, checks out.
`:60`/`:61` `messenger/internal/flow/flow.go:105` = `AddSubscriber("jobsimulation", …)`, `:109` =
`AddSubscriber("cms", …)`.

### `roadrunner.md` — **ENUMERATED**

`terraform/main.tf:19` `service_desired_count = 1`; `repos.yml:29-31` @ `2adcf71` is the roadrunner entry
and `2adcf71` really does have **9** entries; `d11a403`'s diff removes `- name: cms`,
`- name: jobsimulation` **and** `- name: roadrunner` while its message claims roadrunner's entry "was
already gone" — the corpus's "grade that commit by its diff, not its message" is exactly right.
`0dab54d` `repos.yml` = the 6 named; `0c91421` = the 4 named. jobsimulation's service is at
`2adcf71:docker-compose.yml:83` with `profiles: [graphql, jobsimulation, all]`.
Ports: `cmd/root.go:110` `port := cmp.Or(os.Getenv("PORT"), "8080")`, `:84`
`rpcPort := cmp.Or(os.Getenv("RPC_PORT"), "8081")`; compose @ `2adcf71` `:291-292` publish `10400:10400`
/ `10401:10401` and `:296-302` supply all nine env values in the table exactly as tabulated
(`ENVIRONMENT=development`, `JUDGE0_BASE_URL=http://52.48.139.23:2358`, `PORT=10400`,
`REDIS_ADDR=redis:6379`, `REDIS_STREAMS_INDEX=4`, `REDIS_WORKER_INDEX=0`, `RPC_PORT=10401`;
`JUDGE0_API_KEY` and `SENTRY_DSN` genuinely absent).
Go 1.25.0, asynq `v0.25.1`, `gorilla/websocket`. RPC surface = `Submission`, `SubmissionPackage`,
`SubmissionResult` (`internal/rpcsrv/rpc.go:20,43,60`). Worker `Concurrency: 10`; `MaxRetry(3)` on the
default queue (`runner.go:126`); `maxRetries = 15` with `time.Sleep(1 * time.Second)`;
`tasks.go` const `roadrunner:submissionresult`; `RoadrunnerSubmissionCompleted` published at
`runner.go:277-278`. **Zero** `*_test.go` files; `Dockerfile:18` = `RUN go test -v ./...`.
The three-string zero (`:24-27`) **ENUMERATED and refuted-in-the-right-direction**:
`ROADRUNNER_RPC_ADDR` / `RoadRunnerService` / `roadrunner:10401` = **0 Go hits** at `app b948604f`,
`jobsimulation 462343b0` **and** at `app ad9f3c49` (control: `JUDGE0_BASE_URL` → 1 hit in
`jobsimwiring/wiring.go`). The case-insensitive repo-wide sweep, run per clone **at its own current ref**,
reproduces the published figures exactly: **app 25 files, jobsimulation 8, platform 3, studio-desk 3,
next-web-app 1** (and 0 in ant-academy, cms, cms/studio, app/studio, graphql-wundergraph, messenger,
sentinel, storage). `jobsimulation` @ `462343b0`: `git grep -in roadrunner` → **14 lines / 8 files**,
exact-case → **5 lines / 3 files**, Go-only → the single comment at `internal/runner/runner.go:3`
("formerly the standalone \"roadrunner\" service"), all four figures exact.
`app/internal/jobsimulation/simulator/stream_handlers.go:30-34` carries the quoted "NOT stream handlers".
`ROADRUNNER_RPC_ADDR` is absent from `.env_example` @ `0c91421` (0 hits).
`jobsimulation/knowledge/operational.md:68` and `app/knowledge/plan/…/RE-PORT-CHECKLIST.md:10` both
contain the strings attributed to them (verified by match-count without displaying the file, to respect
the `knowledge/plan/**` bar).

### `TEMPLATE.md` — **cleared, no findings**

46 lines, a pure skeleton of placeholders. Its one substantive line, `### 2. Running in Docker` →
`docker compose up -d [service_name]`, is a placeholder rather than a runnable invocation, so it does not
trip §5 rule 30's "do not spell a dead command in runnable form". No factual claim to grade.

### Cross-file uniformity spot-checks (the §5 rule-21 cheapest class) — **ENUMERATED**

Grepped the whole `corpus/` tree plus root `CLAUDE.md` for the load-bearing claims my files carry. All
**uniform**, no split repairs: the tenancy figure (`31 of 135`, at `security_compliance.md:76,135` and
`architecture_overview.md:373-374`); the subgraph ladder and the **3 → 1** step
(`service_taxonomy.md:407`, `architecture_overview.md:130-131,222,290`, `external_services.md:368,382,485`,
`dependency_map.md:23`); "**five** containers" (`architecture_overview.md:12,71`,
`service_taxonomy.md:67,128`, `external_services.md:170`, `run_guide.md:88`, `setup_guide.md:435`);
`10400/10401` marked historical at every one of the five sites that mention it.
`security_compliance.md:185`'s bare `README.md:21` resolves correctly to the **sibling**
`corpus/architecture/README.md:21` ("cost tracking lives in `app`, not the `ai` library") — I nearly
booked this against the repo-root `README.md:21` before checking every `README.md` in the tree, which is
§5 rule 36's "which copy is the witness" trap.

---

## What I could not settle

- **`dependency_map.md:13`** — "`pgvector` in `extensions`", "the 23 jobsim run-state tables", "the cms
  similarity/Studio tables". These are live-database facts. No stack is up (and bringing one up is
  forbidden for this reading), and the corpus's own read path is `/db-query` against prod. A migration
  count over `app/terraform/migrations/` would be a proxy, not the predicate. **Settled by:** `\dx` and a
  table census on a running stack, or a scoped `/db-query`.
- **`dependency_map.md:27`** — "production terraform: `skiller_rpc_addr =
  http://backend.internal.anthropos:8081`". Only the *variable reference* is measurable
  (`messenger/terraform/main.tf:78-79` sets `SKILLER_RPC_ADDR` from `var.skiller_rpc_address`); the
  **value** lives in `infrastructure`'s `services.tf`, which has never been in any clone set — the same
  blind spot the cms/M810 rows declare. The corpus routes the derivation to `backend.md`, outside my
  file set. **Settled by:** an `infrastructure` clone.
- **GitHub archive states** — `roadrunner.md:37-38,43,56-58` asserts skiller archived 2026-07-01,
  skillpath 2026-07-31, roadrunner **not** archived, chronos **not** archived with last push 2026-04-23,
  and `external_services.md:3` says graphql-wundergraph was archived 2026-07-30. Archive status is org
  metadata; it is in no clone. **Settled by:** `gh api repos/anthropos-work/<name> --jq .archived`.
- **`security_compliance.md` infrastructure and legal rows** — VPC CIDR `10.0.0.0/16`, TLS 1.3 cipher
  set, AES-256/KMS, the 6-hourly three-destination backups, DPA v1.4, "18 approved sub-processors",
  90-day deletion, ECS 30 s health checks. None is derivable from any tree in the clone set, and §5
  rule 48 (binding user decision, 2026-08-06) forbids escalating the compliance half during delivery.
  Recorded as unmeasured, not as suspect.
- **`external_services.md:293-296`** — production Directus file storage ("Files stored in **S3**",
  "CDN delivery"). Infrastructure claim, no clone.
- **`external_services.md:439`** — "there is no subgraph healthcheck". True of the deleted local block I
  read at `1e8e754:19-27` (four `condition: service_started`, no `service_healthy`); I did not verify the
  prod task definition, so I have cleared only the compose half.

---

## Method notes (what I did that the instrument asks for, and one near-miss)

- Every `file:line` I graded was opened with `sed -n 'N-k,N+kp'`, never grepped-to-and-read-in-isolation
  (§5 rule 10). This is what produced B2: `grep 'markdownManager.go:19'` would have shown a real file and
  a real line number; only reading around it shows the line is a comment and the symbol is gone.
- Every absence claim in this report used **all three instruments** the addendum names — bare `grep`
  (this shell's `ugrep --ignore-files`), `git grep` at the tree's own ref, and explicit enumeration of the
  15 git trees including the two nested `studio` checkouts at `aeec036a` — with a positive control in the
  same pass. Two shell-quoting failures (`$R:path` mis-parsed as `$Rpath` under zsh, and `$c:supergraph`
  hitting a zsh history modifier) produced empty output that *looked* like absence; both were caught
  because stderr was read (`fatal: ambiguous argument…` / `bad substitution`) and re-run with `${R}:`.
  That is §5 rule 1 paying for itself twice in one reading.
- **Near-miss worth recording** (§5 rule 36): while deriving the tenancy set I ran
  `grep -ln 'UserMixin{}'` and got `category.go`, `jobrole.go`, `skill.go`, `specialization.go` among the
  hits — which would have made four of the seven `OrganizationIDMixin{}` schemas owner-policed and broken
  the corpus's 23. They are `CreatedByUserMixin{}`. The substring matched; the predicate did not. Had I
  stopped at the file list I would have booked a false BLOCKER against a fence that is correct.
- I did not read anything under `knowledge/plan/**` other than this output file, nor anything under
  `.agentspace/scratch/`, nor any other seat's output. The one `knowledge/plan/` path I needed to
  *verify* (`app/knowledge/plan/…/RE-PORT-CHECKLIST.md:10`, in the **app** repo) I checked with a
  match-count that never displayed the content.
