# Seat F — reading #26

**Instrument:** `/private/tmp/claude-501/-Users-marco-workspace-anthropos-rosetta/5de92915-24e3-4d62-9259-350a85d16e27/scratchpad/briefing-r26-AS-DELIVERED.md`
**sha256** `4178b00f0d8e98eabddb895969d6e4c58799a5b292d967f8a2cfca6772a18d3d` (243 lines, read in full).
Read `corpus/ops/platform-alignment.md` §5 in full (rules 1–48) before starting.

## Refs actually used (re-derived at this reading's open; **no fetch, no git state change**)

| repo | path | sha read | note |
|---|---|---|---|
| app | `stack-demo/app` | `ad9f3c49` | checkout **==** `origin/main`; also read `b948604f`, `9d00a313`, `2035f9a4` where a claim pins them |
| app/studio (nested, own checkout) | `stack-demo/app/studio` | `aeec036a` | grepped at **its own** ref, not app's |
| platform | `stack-demo/platform` | `0c91421d` | + `0dab54d`, `2adcf71`, `d11a403`, `838d907`, `b56d731`, `360efd4` |
| next-web-app | `stack-demo/next-web-app` | `8297c684` | + `origin/main` `f97ba659` |
| sentinel | `stack-demo/sentinel` | `f2c46190` | |
| storage | `stack-demo/storage` | `4ce8ece5` | + `origin/main` `9f8cb532`, `63bffc8`, `d3e6d32` |
| messenger | `stack-demo/messenger` | `fa47850d` | |
| cms | `stack-demo/cms` | `ca50c817` | + `origin/main`, `6efa1d5`, `f38c0c4` |
| graphql-wundergraph | `stack-demo/graphql-wundergraph` | `60c229f3` | = `origin/main`; + `915da06`, `915da06^` |
| roadrunner | `stack-demo/roadrunner` | `87d8d443` | |
| jobsimulation | `stack-demo/jobsimulation` | `462343b0` | + `origin/main` `82cb66ec`, `6092c6d2` |
| ant-academy | `stack-demo/ant-academy` | `22df69dd` | **DIRTY working tree** (4 demo-patched files: `code/public/catalog.json`, `code/src/components/UserMenu.jsx`, `code/src/lib/serverChapterBody.js`, `code/src/lib/serverTenant.js`). **Every ant-academy anchor below was read via `git show HEAD:<path>`, never from the working tree.** |
| **rosetta-extensions — per-stack CONSUMPTION clone (the tree that settles a "what the tooling does on a stack" claim, per addendum / §5 rule 45)** | `stack-demo/rosetta-extensions` | **`09d06070`** | every rext claim in this report was settled here |
| rosetta-extensions authoring copy | `.agentspace/rosetta-extensions` | `944fc4a2` | **not** used to settle anything |

Hard bars honoured: nothing read under rosetta's `knowledge/plan/**` or `.agentspace/scratch/`; no other seat's output sought. **Disclosure:** I *did* read `stack-demo/app/knowledge/plan/releases/09.00-support-in-app/m903-s3-custody/{overview,progress}.md` — that is the **platform's** repo, a different repository from the one the hard bar names, and `storage.md:25` cites it directly; `platform-alignment.md` §1 explicitly instructs reading `app/knowledge/plan/`.

## Positive controls

| file | `wc -l` | read in full? |
|---|---|---|
| `corpus/services/ant-academy.md` | 501 | yes |
| `corpus/architecture/architecture_overview.md` | 427 | yes |
| `corpus/services/storage.md` | 248 | yes |
| `corpus/services/sentinel.md` | 166 | yes |
| `corpus/services/academy-backend.md` | 153 | yes |
| **total** | **1495** | |

---

## Blockers

### B1 — `academy-backend.md:126` republishes the retracted "a demo academy serves its committed FS catalog" claim, which its own sibling doc explicitly refutes

- **Anchor:** `corpus/services/academy-backend.md:125-128`, verbatim:
  > **Demo caveat**: on a demo the frontend has **no `NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT`**, so it serves its
  > **committed FS catalog** and any rows `academy-seed` writes have **no reader** — i.e. `academy-seed` is **moot
  > on a demo**; the demo CTA is a real `/courses/<slug>` link

- **Why it is false:**
  1. **Ground truth.** `ant-academy` @ `22df69dd`, `git show HEAD:code/src/lib/serverTenant.js`:
     `:143-147` is `export async function getServerCatalogView()` whose body is
     `const view = (await getBackendCatalogView(eids)) ?? emptyCatalogView()`, and `:115-117`
     `emptyCatalogView()` returns `{ chapters: [], skillPaths: {}, series: [], bundles: PUBLIC_BUNDLES,
     catalogVersion: CATALOG_VERSION }` — **not** the FS catalog. The doc-comment at `:129-132` states the
     rule in-code: *"DB presence gates visibility … there is **NO FS-as-published fallback**. A null backend
     result (not-composed / outage) resolves to the EMPTY view rather than the committed FS catalog: the
     cutover is intentional, not reversible-on-error."* The only FS merge is the **dev-only** draft layer
     (`draftsEnabled()` → `mergeDrafts`, `:146`), which a demo does not enable.
  2. **A demo renders cards only because rext patches the fallback back in.** Pinned consumption clone
     `stack-demo/rosetta-extensions` `09d06070`, `demo-stack/ant-academy.sh:57-66`: the
     `academy-fs-published-fallback` demo-patch *"restores an FS-as-PUBLISHED catalog fallback (Option C)"*,
     applied to the ephemeral clone before launch, reverted on `--stop`, **NON-FATAL** — *"a refused/failed
     patch just leaves the grid empty."* (`FSPUB_PATCH` → `stack-injection/apply-academy-fs-published.sh`;
     manifest `demo-stack/patches/academy-fs-published-fallback/…yaml`.) The **unpatched** demo academy
     therefore renders **0 cards**, not an FS catalog.
  3. **Self-contradiction inside the corpus, in the sibling this doc links to.**
     `corpus/services/ant-academy.md:82-88` says the opposite in terms:
     *"**⚠️ It does not "fall back to the committed FS catalog"** — there is **no FS-as-published fallback**
     in the app … A demo grid renders cards only because the rext demo-patch **`academy-fs-published-fallback`**
     restores that removed fallback … if that patch is refused, the grid is empty."*
     Two files of one corpus, opposite claims, and the ground truth backs `ant-academy.md`.
- **Why it misleads real work:** a reader debugging an empty demo academy is told the FS catalog is served
  unconditionally, so they will look for a *rendering* bug rather than checking whether the (non-fatal,
  drift-refusable) demo-patch applied — the exact mis-attribution `ant-academy.md:192-197` records as the
  v2.4 **F4** carry.
- **Scope note (rule 19, not booked here because outside my file set):** the retracted form also survives at
  `corpus/ops/demo/content-stories-routes.md:23`, `:401`, `:456` and
  `corpus/ops/demo/content-stories-spec.md:301`, `:381`, `:385` — a half-repaired claim, repaired in
  `ant-academy.md` and left standing in six sibling sites plus this one.
- **Confidence:** high

### B2 — `sentinel.md:85` cites `architecture_overview.md:321` for a sentence that is at `:331`; `:321` is the **production** router line, i.e. the opposite model

- **Anchor:** `corpus/services/sentinel.md:85`, verbatim:
  > The correctly-scoped form is the model at [`architecture_overview.md:321`](../architecture/architecture_overview.md) — *"the only cross-process RPC edge out of backend on a core stack"*.
- **Why it is false:** `grep -n "only cross-process RPC edge out of backend" corpus/architecture/architecture_overview.md`
  returns **exactly one** hit — **`:331`** (`  → Connect-RPC to sentinel   (the only cross-process RPC edge out of
  backend on a core stack)`), inside the **"On a local stack"** fenced block that opens at `:329`.
  `architecture_overview.md:321` is `User → Vercel (Next.js) → Clerk (JWT) → ALB → Cosmo Router (port 8080)` —
  the first line of the **"In production"** block (`:318-325`). The anchor resolves and names the **wrong
  construct**, and the wrong construct is the *production* request flow the cited sentence is explicitly
  contrasted against. A reader following the citation lands on the router hop and reads the sentinel scoping
  as a claim about the prod path.
- **Cross-check that this is a citation defect and not a line-drift artifact:** the two blocks are 10 lines
  apart and both are inside the same section; `:321` has never carried the quoted text at rosetta HEAD.
- **Confidence:** high

### B3 — `architecture_overview.md:59` states the wrong Next.js pin: `^16.2.7` vs the measured `~16.2.12`, and the wrong range operator

- **Anchor:** `corpus/architecture/architecture_overview.md:59`, verbatim:
  > - **Frontend**: Next.js **16** + React 19 + TypeScript on Vercel (`next: ^16.2.7` across all four apps)
- **Why it is false:** the sentence names **no ref**, so it grades at the checkout. Measured at
  `next-web-app` `8297c684` (and identically at `origin/main` `f97ba659`) —
  `git show HEAD:apps/{hiring,integration,maintenance,web}/package.json | grep '"next":'` — all four read
  **`"next": "~16.2.12"`**. Both the operator (`~`, not `^`) and the version (`16.2.12`, not `16.2.7`) are
  wrong. `apps/mobile` declares no `next` (Expo), so the "four apps" denominator is right.
- **When it was true, and why that does not save it:** `^16.2.7` was correct from `74eb4f483` (2026-06-02)
  until `dd0de31fc` (2026-08-04, *"fix(security): patch Next.js and Clerk advisories"*), which is the commit
  that bumped it. The claim is two days stale at the checkout the reader has, and it is a **security** bump —
  a reader reconciling a lockfile against `^16.2.7` would conclude the tree is ahead of spec rather than that
  the doc is behind.
- **Confidence:** high

### B4 — `architecture_overview.md:308` — `markdownManager.go:11,19` resolves onto a closing paren and a comment about a *removed* behaviour

- **Anchor:** `corpus/architecture/architecture_overview.md:307-309`, verbatim:
  > **Mistral is not part of this routing chain** — but it *is* live in `app`:
  > `internal/cms/studio/markdownManager.go:11,19` builds a Mistral client from `MISTRAL_API_KEY` for
  > **Studio document OCR** (called from `studioManager.go:583`).
- **Why it is false:** the sentence names no ref (the ref-bearing clause `@ app b948604` two bullets up is a
  different block, and rule 33 confines a pin to its own wrapped sentence), so it grades at the checkout
  `ad9f3c49`. There, `git show HEAD:internal/cms/studio/markdownManager.go`:
  - `:10` = `"github.com/anthropos-work/app/internal/cms/studio/mistralocr"`; **`:11` = `)`** — the closing
    paren of the import block.
  - **`:19` = `// It used to take aiKey and then IGNORE it, reading os.Getenv("MISTRAL_API_KEY")`** — a
    comment documenting a bug that was **fixed**; the surrounding block (`:17-28`) says the env-read was
    removed and the constructor now uses the passed key.
  - The constructor is at **`:29-31`**: `return &MarkdownManager{ocr: mistralocr.New(aiKey)}, nil` — an
    app-internal OCR wrapper, not the shared `ai/mistral` provider the old line built.
  The anchors were **exactly right at `b948604f`** (`:11` = `"github.com/anthropos-work/ai/mistral"`,
  `:19` = `mistral.NewMistral(nil, os.Getenv("MISTRAL_API_KEY"))`), so this is drift the clone advance
  introduced, and the corpus did not re-point it.
- **Hesitation, stated as the instrument requires:** the *substance* survives —
  `studioManager.go:583` is byte-exact (`markdownManager, err := NewMarkdownManager(os.Getenv("MISTRAL_API_KEY"))`),
  so Mistral OCR *is* live in `app` and *is* keyed off `MISTRAL_API_KEY`. I hesitated between MINOR
  (line drift, substance intact) and BLOCKER. Booked BLOCKER because the instrument names
  *"wrong line anchors that point at the wrong construct"* as a BLOCKER, and because `:19` does not merely
  miss — it lands on prose asserting the **negation** of the sentence citing it (*"used to … reading
  os.Getenv(MISTRAL_API_KEY) … Both are fixed"*), which is the failure mode `platform-alignment.md` §5
  rule 10 exists for.
- **Confidence:** medium

---

## Minors

- `corpus/services/ant-academy.md:175` — *"a separate, unauthenticated `code/public/catalog.json` (~2,667 entries)"*.
  Measured at `ant-academy` `22df69dd` (`git show HEAD:code/public/catalog.json`, `len(d['courses'])`) = **2,692**
  (working tree, demo-patched: 2,722). Hedged with `~`, ~1 % off, no consequence.
- `corpus/services/ant-academy.md:302` — *"~26 Playwright e2e spec files (tests/e2e/)"*.
  Measured: `git ls-files 'code/tests/e2e/*.spec.js'` = **31** (and 31 repo-wide). Hedged with `~`, but 19 % low.
- `corpus/architecture/architecture_overview.md:340-352` — the blockquote opened at `:338` loses its `>`
  prefix for 13 lines and resumes at `:353`. It still renders (CommonMark lazy continuation keeps one
  paragraph inside the quote) so there is no factual consequence, but the block reads as broken in a plain
  editor and any line-based tooling that keys on `^>` will mis-scope it.

---

## Positively cleared (audited zeros)

Every item below was checked against source. Labels are as the instrument defines them.

### ENUMERATED (predicate re-derived from source, cardinality compared)

1. **`architecture_overview.md:373-381` — the multi-tenancy split (31 / 135 / 29 / 23 / 16).** This is the
   figure `platform-alignment.md` §5 rule 24 records as wrong in **three** consecutive generations, so I
   re-derived the **set**, not the sum, at `app` `ad9f3c49`:
   - schemas = files under `internal/data/ent/schema/*.go` containing `ent.Schema`: **135** (139 `.go` files
     minus `database_types.go`, `mixin.go`, `skiller_mixins.go`, `skillpath_mixins.go`, which declare no schema).
   - `OrganizationMixin{}`: **30** grep hits, of which `user_resource.go:22` is `// OrganizationMixin{},  // We need to work on this`
     → **29 live**. `+ Membership` (`org_membership.go:172 func (Membership) Policy()`) `+ Organization`
     (`organization.go:56 func (Organization) Policy()`) = **31**. ✔
   - `organization_id`-bearing = 45 files declaring `field.UUID/String("organization_id")` ∪ 7
     `OrganizationIDMixin{}` users = **52** distinct paths; minus everything with any policy source (29 live
     `OrganizationMixin` ∪ 4 own-`Policy()` files) = 25; minus `skiller_mixins.go` (a mixin file, not a schema)
     = 24; minus `academy_feedback.go` (real `UserMixin{}` at `:64`, owner-policed) = **23**. ✔
   - of those 23, the 7 `OrganizationIDMixin{}` users (`category`, `jobrole`, `similarity`, `skill`,
     `specialization`, `studio_document`, `studio_task`) removed → **16 neither-mixin**. ✔
   - **Trap I nearly fell into and name here:** a naive `grep -l "UserMixin{}"` returns 5 extra files —
     `CreatedByUserMixin{}` is a substring match and declares **no** `Policy()`. Counting those as policed
     yields 19, not 23, and would have produced a false BLOCKER. The corpus figure is right.
2. **`architecture_overview.md:80` — `authn` is imported by nothing.** `git grep -c "anthropos-work/authn" HEAD`
   across all **seven** Go clones (`app` `ad9f3c49`, `sentinel` `f2c46190`, `storage` `4ce8ece5`,
   `messenger` `fa47850d`, `cms` `ca50c817`, `roadrunner` `87d8d443`, `jobsimulation` `462343b0`) = **0/7**,
   whole-tree, not just `go.mod`. Positive control `anthropos-work/colony` in `go.mod` = **7/7**. ✔
   Scope stated: the two nested `studio` checkouts are Python and carry no `go.mod`, so they are out of the
   predicate rather than silently uncounted.
3. **`architecture_overview.md:12, 71, 156-165, 223` — the compose topology.** Parsed
   `platform` `0c91421d` `docker-compose.yml` (186 lines) **plus** its `include: common.yml`:
   declared services = `sentinel, backend, studio-desk, next-web-app, gotenberg` = **5**; effective topology
   with `common.yml`'s `postgresql, redis` = **7**. ✔ Only `backend` (`:110`), `studio-desk` (`:141`),
   `next-web-app` (`:168`) and `gotenberg` (`:183`) declare `profiles:`; `sentinel`, `postgresql`, `redis` do
   not → `core` starts **five containers** (`backend`, `gotenberg` + the floor), **two** of them our Go
   services. ✔ `Makefile:10` `PROFILE ?= core`. ✔ No `roadrunner`/`cms`/`jobsimulation`/`storage`/
   `messenger`/`customerio-sync` service anywhere. ✔
4. **`sentinel.md:26, 30-41` — the Casbin model.** `internal/authorization/casbin.go:13-45` @ `f2c46190`:
   request definitions `r,r2,r3,r4,r5,r6` = **6**; policy definitions `p,p2..p6` = **6**; role definitions
   `g,g2,g3` = **3**; matchers `m,m2..m6` = **6**. ✔ Each matcher row's description checked against its own
   expression (incl. `m6`'s `g3(p6.org, r6.sub)` and `m5`'s `keyMatch`). ✔
5. **`sentinel.md:40` — the four `MembershipRole` values.** `app/internal/data/ent/enum/membership.go:8-15`
   declares exactly `RoleAdmin`, `RoleMember`, `RoleCandidate`, `RoleContentCreator`. ✔
   `init_policy.sql:88-118` is the `content_creator` block (comment banner at `:88-93`, last p5 row at `:118`), and
   `internal/authorization/casbin_content_creator_test.go` exists. ✔
6. **`sentinel.md:43-48` — "there is no `manager` role."** `git grep -i manager HEAD -- init_policy.sql`
   returns **0**; the only occurrences are fixture strings in `internal/authorization/casbin_test.go` (4) and
   `internal/rpcsrv/rpc_test.go` (5). ✔
7. **`sentinel.md:5, 85, 89` — `AUTHORIZATION_ADDRESS` is set in exactly one block.**
   `git grep -n AUTHORIZATION_ADDRESS <ref> -- docker-compose.yml common.yml .env_example` returns exactly
   **1** line at **both** named refs, and at **`:48`** at both (`0dab54d` and `0c91421`). ✔ Zero `*_RPC_ADDR`
   anywhere in compose. ✔ `messenger` imports no authorization client (`git grep -c authorization HEAD -- '*.go'`
   in `messenger` `fa47850d` = 0). ✔
8. **`storage.md:217-233` — the historical env table.** The deleted `storage` block's `environment:` list is
   **7** entries at *both* `0dab54d` (`:117-123`) and `2adcf71` (`:204-210`), byte-identical:
   `AWS_DEFAULT_REGION`, `AWS_REGION`, `ENVIRONMENT=development`, `PORT=8300`, `RPC_PORT=8301`,
   `SERVICE_NAME=storage`, `STORAGE_S3_PUBLIC_BUCKET=production-storage-public…`. Every row of the doc's
   table matches, including the two "(empty)" rows (`STORAGE_S3_BUCKET`, `SENTRY_DSN`) — the block genuinely
   never set them — and the `ENVIRONMENT` row's own iter-102 correction. ✔ Binary defaults 8080/8081
   (`cmd/root.go:45-46`). ✔ `docker-compose.yml:210` @ `2adcf71` is the public-bucket line and `:324` is
   inside the *studio-desk* block, exactly as written. ✔
9. **`ant-academy.md:306-325` — the complete `isPublic` matcher.** Read all of
   `code/proxy.js:112-188` @ `22df69dd` and compared entry-for-entry: all 24 static patterns plus the two
   conditional spreads are in the doc's table, and the doc introduces none the code lacks. ✔
   `/library/[slug]` genuinely does not exist (only `app/(public)/library/page.jsx` and
   `app/(public)/free/page.jsx`). ✔
10. **`ant-academy.md:266-282` — `TopBar`'s seven surfaces.** Enumerated by mount site at `22df69dd`
    (`git grep -n TopBar HEAD -- code/src code/app`, excluding tests / CSS / `TopBarSearch`):
    `AcademyClient.jsx:1906` (mounted by `app/(authed)/page.jsx:151`, `courses/page.jsx:92`,
    `courses/[slug]/page.jsx:219` → 3 routes), `CourseClient.jsx:2091` + `:2141` (`/chapters/[slug]`),
    `LatestClient.jsx:128`, `BookmarksClient.jsx:508`, `MyActivityClient.jsx:161` = **7**;
    `MyCertificatesClient.jsx` imports no `TopBar`. ✔ `LanguageSelector` at `TopBar.jsx:76`, `role="menu"` at
    `LanguageSelector.jsx:88`, `role="menuitemradio"` at `:97`, over
    `SUPPORTED_LOCALES = ['en','it','es','fr','de','nl','pt']` (**7**) at `i18n/locale.js:10`. ✔
    `PublicHeader` has exactly **one** mount site, `app/(public)/library/page.jsx:28`; `LocaleSwitch` at
    `PublicHeader.jsx:20`; `/free`'s whole body is `redirect('/?tier=free')` at `free/page.jsx:18`. ✔
    Both provenance shas check out: `5b05b7d9` (2026-05-05) is the commit that **created**
    `LanguageSelector.jsx` *and* introduced it into `TopBar.jsx` (`git log -S`), and `e22f3230`
    (2026-05-18) is *"feat(i18n): add locale support for es, fr, de, nl, pt"* touching `i18n/locale.js`. ✔
11. **`ant-academy.md:224-228` — the five bring-up patches and their wiring lines.** Settled in the **pinned**
    clone `stack-demo/rosetta-extensions` `09d06070`: `demo-stack/ant-academy.sh` assigns
    `DEVORIGINS_PATCH` at **`:56`**, `FSPUB_PATCH` **`:66`**, `FSBODY_PATCH` **`:75`**, `FSPUBLIC_PATCH`
    **`:84`**, `COCKPIT_PATCH` **`:90`** — five, at exactly the lines named. All five manifests exist under
    `demo-stack/patches/<id>/<id>.yaml` and all five appliers under `stack-injection/`. ✔
12. **`storage.md` / `architecture_overview.md` — the `STORAGE_RPC_ADDR` counts, at every ref they name.**
    `git grep -n STORAGE_RPC_ADDR <ref> -- '*.go'`: at `9d00a313` and at `2035f9a` → **3**, all comments
    (`main.go:451`/`:504`, `internal/jobsimwiring/wiring.go:101`, `internal/storagens/callsites_test.go:189`);
    repo-wide at `9d00a313` → **29 lines across 18 files**; at `b948604f` → **15**, of which the
    `os.Getenv`/`getenv` **reads** are exactly **7** (`cmd/academy-asset-upload:129`, `cmd/academyImport:231`,
    `cmd/import:50`, `jobsimwiring/wiring.go:115`, `main.go:446`, `:524`, `:992`) — the `t.Setenv` hits in
    `main_test.go` correctly excluded. ✔ `git diff --stat 2035f9a4 ad9f3c49 -- main.go internal/storage/` is
    **empty**, as the doc asserts. ✔

### RE-DERIVED / anchor-verified (weaker clearance — stated as such)

Every `file:line` below was opened with `sed -n 'N-15,N+5p'`-style context, not grepped to:

- **`ant-academy.md:63-77`** (write path): `code/src/progress/store.js:150` `saveChapterProgress`, `:162`
  `UPSERT_CHAPTER_PROGRESS`, `:169` `beacon:`, `:202` `saveLastActivity`, `:210` `SET_LAST_ACTIVITY`,
  `:215` `beacon:`; `src/writeThrough/index.js:247` `navigator.sendBeacon`, `:259` `keepalive: true`;
  `app/api/academy/beacon/route.js:1-18` header comment quoted byte-exact, including *"a best-effort
  last-ditch flush for a write that would otherwise be lost if the tab closes mid-retry"*. All correct.
- **`ant-academy.md:150-164`**: `serverTenant.js:145` is byte-exact; `:115-117` returns the **five-key**
  shape; `:111-113` carries the quoted *"carries no tenant metadata …"*; `ucourses/catalog.js:961`
  `export const PUBLIC_BUNDLES = [` and `:31` `export const CATALOG_VERSION = '1.0'`;
  `AcademyClient.jsx:1363-1365` is the `scopedChapters.filter` → `if (chapters.length === 0) return []`
  drop. The "→ 0 cards" conclusion holds.
- **`ant-academy.md:299-304`**: `code/app/layout.jsx:1` imports **exactly** `{ Work_Sans, Instrument_Serif }`;
  `:41`/`:51` are the two font constructors; **no** DM Sans / JetBrains Mono anywhere; `academy.css:1`
  quoted correctly; `:132` `manifest: "/academy-manifest.json"`; `engines.node` = `">=22"`;
  `code/vercel.json` is exactly `{"framework":"nextjs"}`; 29 vendored `webfonts/*.woff2` + `css/all.min.css`
  present and **no** `@fortawesome` npm dependency (token-less install claim holds).
- **`ant-academy.md:362-385`** (demo auth model): `demo-stack/ant-academy.sh:576-583` @ rext `09d06070` is
  precisely the block stating the `e2e_persona` bypass is gone, and the launch `env` at `:584-585` sets
  neither `BENCHMARK_VISUAL_BYPASS` nor `NEXT_PUBLIC_E2E_AUTH`. Both fence tests exist
  (`demo-stack/tests/test_ant_academy.py`, `…_clerk_wiring.py`). `cockpit.py:812` (client-side
  `document.cookie = 'e2e_persona=…'`), `:1496` (`Set-Cookie` on the `/go` 302) and `:327` (names both)
  all resolve. `autoverify.sh` assert **(f)** exists at `stack-verify/live/autoverify.sh:459+`.
- **`ant-academy.md:26, 349-353, 488-491`**: `platform` `repos.yml` @ `0c91421d` lists **four** repos
  (`app`, `sentinel`, `next-web-app`, `studio-desk`) — no `ant-academy`; `ensure-clones.sh:20` and
  `:150-168` are phase **(d2)**, the explicit non-fatal ant-academy clone. Cosmo (`gpt-5.2`, `POST
  https://api.openai.com/v1/responses`, `localStorage('openai_api_key')`) verified at
  `code/ucourses/ucourse-engine/assistant/agent.js:12-13`, `:32`.
- **`architecture_overview.md:3, 20, 221-223, 251, 290, 318`**: `graphql-wundergraph/terraform/main.tf:20`
  `service_desired_count = 1`; `roadrunner/terraform/main.tf:19` `= 1`; `cms/terraform/main.tf:39` `= 0` at
  **both** cms refs; `6efa1d5`/`f38c0c4` are *"drop build-production — the cms ECR repository is
  decommissioned (M810)"*, 2026-08-04; `6092c6d2` (2026-08-04) is the jobsimulation ECS/ECR removal and
  `jobsimulation/terraform/main.tf:15-22` / `:24-40` resolve there **and** at `origin/main`; jobsimulation's
  `origin/main` really does carry **four** 2026-08-04 commits including PR **#439**;
  `915da06` deletes **both** `schemas/cms.graphqls` and `schemas/jobsimulation.graphqls` from a
  `supergraph-config-prod.yaml` that listed **three** subgraphs at `915da06^` (so **3→1**, and the commit
  subject really does say "2→1"); all three supergraph configs now list `backend` alone,
  `federation_version: =2.3.2`.
- **`architecture_overview.md:265-280, 302-309`**: `docker-compose.yml:48/57/59/66` and `:84-92`, `:100`,
  `:183` all byte-exact @ `0c91421`; `app/internal/converter/gotenberg.go:31` is the plain-HTTP
  `http.NewRequestWithContext(… "POST" … "/forms/libreoffice/convert")`;
  `internal/jobsimulation/ai/ai.go:259-277` is `getClient` defaulting to `azureClientEu` and swapping on
  `flag_use_azure_us`, `isThrottlingError` at `:129`, applied at `:166` and `:325`, Bedrock
  `config.WithRegion("eu-west-1")` at `:85-88`. The **five-arm** claim's latent arm checks out:
  `app/studio` grepped **at its own ref `aeec036a`** — all three `configs/*.ini` pin `azure` on every
  `*_AI_*_MODEL` line.
- **`architecture_overview.md:203-214`**: `d11a403`'s message really says roadrunner's *"repos.yml clone
  entry was already gone"*, and its `repos.yml` diff really deletes `- name: cms`, `- name: jobsimulation`
  **and** `- name: roadrunner`. `838d907` deletes the `storage:`, `customerio-sync:` and `messenger:`
  service blocks and removes `storage` + `messenger` from `repos.yml`.
- **`storage.md:7-40`**: `recording.go:12` and `anticheat/anticheat.go:30` (the storage-SDK imports) and
  `main.go:1048` `cmsStorage := internalstorage.NewClient(storageManager, storagens.CMS)` all resolve at
  `9d00a313`. `storage/terraform/main.tf` @ `9f8cb53` is exactly **18** lines; `:9-11` and `:13-16` and
  `:18` quoted byte-exact; `git grep -ci custody 9f8cb53` = **0**; `terraform/storage.tf:22-25` carries the
  M903-never-executed paragraph; **no** `moved` block exists; `63bffc8` did read `service_desired_count = 0`;
  the checkout `4ce8ece5` is **20** behind, its `main.tf` is **100** lines with the ECS module, and
  `storage.tf:23-24` there still says *"relocate the assets out of this module BEFORE M907"*, retired by
  `d3e6d32` (2026-08-05) whose body literally reads *"M903 never ran."* The app-side plan directory has
  `status: planned` and *"Planned, not started — no terraform applied, no branch."*
- **`storage.md:44, 62-94, 192, 228-231`**: `main.go:516/517` (env reads), `:518-523` (empty-bucket
  `log.Fatalf`), `:524/:525` (`NewManager`/`NewPublicManager`), `:529-535` (`verifyBucketAccess`), `:547`
  (`resource.NewManager`), `:1102` (`cmsStorage`), `internal/storage/service.go:22/:24` (the two env-var
  *names*), `env_guards.go:37-44` (`deployedEnvironment` returning false for `development`) — all exact at
  `ad9f3c49`. The ref-is-load-bearing claim is exact too: at `b948604f` `env_guards.go` does not exist and
  `:519-523` is the **public-storage clients** block, `:529-535` the **academy asset uploader**.
  `internal/storage/storage.go:193-200` @ `2035f9a` is `getKeyPath` branching on `s3Bucket != ""`;
  storage's own `internal/storage/storage.go:122` returns `("", nil)` on an empty bucket;
  `platform/README.md:81-87` @ `0c91421` is the live-AWS-credentials instruction.
- **`storage.md:48-52, 105-125, 129-169, 205, 242`**: Go **1.25.0** (`go.mod:3`); `storage`'s own `main.go`
  is **18** lines; **0** `*_test.go` files in the repo and `Dockerfile:18` is `RUN go test -v ./...`;
  no `//go:generate`, `gqlgen` only `// indirect`; `viant/afs v1.30.0`; all five RPC methods present in
  `internal/rpcsrv/rpcsrv.go` with the 15-minute presigned default at `:99-101`;
  `sdk/storage/client.go:10/:27` match the documented `NewClient(serverAddr, namespace)` shape; every
  path in the "Key directories" listing exists.
- **`sentinel.md:12-22, 50-64, 129-135`**: `go.mod:3` `go 1.26.0`, `Dockerfile:2` and `Dockerfile.dev:2`
  both `golang:1.26-bookworm`; `terraform/locals.tf:4-5` `service_cpu = 256` / `service_memory = 128`;
  every file in the "Key directories" listing exists; `init_policy.sql:63-66` is the
  `org:feature:taxonomy:write` omission note; `local_superadmin_grants.sql` exists.
- **`academy-backend.md:16-26, 50-104, 116-131, 141-145`**: `git describe --tags ad9f3c49` →
  **`v1.369.0-7-gad9f3c498`**, `CHANGELOG.md:5` `## v1.369.0 - 2026-08-05`, `git describe 9d00a313` →
  `v1.367.0`; `0e37771f` is PR **#903**, 2026-06-05, *"Academy backend v1.0 'ground truth'"*;
  `main.go:471-472` @ `9d00a313` is the two-manager construction; `cmd/academyImport/main.go:239-243` is
  the quoted v9.0-cutover comment and `:244-246` the empty-bucket hard error; the SDL
  `internal/web/backend/graphql/graph/schemas/academy.graphqls` and `resolver_academy.go` exist;
  all **11** `academy_*` Ent schema files exist and the doc's 6-per-user / 5-catalog split matches;
  `certificate.go:42` `^AAS-\d{4}-[A-HJKMNP-TV-Z2-9]{6}$` — the alphabet is exactly **30** symbols and
  `crypto/rand` is imported at `:5`; `content.go`/`content_admin.go:35` mount
  `/content/admin` behind `sharedTokenMiddleware(os.Getenv("ACADEMY_CONTENT_API_TOKEN"))` and
  `content.go:23` serves `GET /content/catalog.json`;
  `internal/worker/tasks/academy_embedding_refresh.go:10` defines the task type and two schema/manager
  comments pin it at **03:00**; `academy_skill_path.go:71` "Default true"; `academy_chapter_body.go:18`
  "ONE ROW PER (chapter_slug, locale)"; `academy_path_embedding.go:62` `extensions.vector(1536)` + `:70`
  `source_hash`; `cmd/academy-seed/main.go:51-56` declares exactly the six documented flags; every named
  test file (`sqlite_test.go`, `certificate_mint_test.go`, `entitlement_parity_test.go`, `merge_test.go`,
  `time_test.go`, `progress_aggregate_test.go`) exists.

---

## What I could not settle

1. **`architecture_overview.md:13, 236` — "≥42,790 skills / ≥22,470 job roles (public subset, measured
   2026-06-29)."** Not derivable from any clone; it is a live-DB measurement. Would be settled by a
   `/db-query` count of the public taxonomy rows with the same predicate the 2026-06-29 measurement used —
   and the predicate itself is what needs re-deriving, not the number (rule 24).
2. **`architecture_overview.md:3` — "the repo is ARCHIVED on GitHub (2026-07-30)"** (graphql-wundergraph),
   and the `cms` M810 prod state generally. Neither the GitHub `archived` flag nor `infrastructure`'s
   `services.tf` is in any clone. Nothing in the clone set contradicts the archive date —
   graphql-wundergraph's `origin/main` tip is `60c229f3`, dated **2026-07-30**, consistent with it — but a
   clone cannot confirm it. Settled by `gh api repos/anthropos-work/graphql-wundergraph --jq .archived`.
   The `cms` row's own *"report both, assert neither"* framing is the correct disposition and I did not
   book it.
3. **`architecture_overview.md:408` / `academy-backend.md` — "the 23 session/run tables moved to `public`."**
   I did not enumerate the jobsim run-state table set; it needs a migration-directory or
   `information_schema` derivation, and doing it from the Ent schema names alone would repeat exactly the
   set-vs-sum error rule 24 warns about. Left unclaimed rather than cleared.
4. **`ant-academy.md:302` — "1000+ Vitest tests."** Requires executing the suite; not attempted (read-only
   pass). The sibling figure in the same cell (`~26` e2e specs) is measurably off, which is a reason to
   distrust this one, but distrust is not a measurement.
5. **`sentinel.md:45` — the live-stack query `select distinct v2 from sentinel.casbin_rules where p_type='g2'`
   returning `admin`/`member`/`candidate` only.** No stack is up in this pass. The *static* half is cleared
   (ENUMERATED #6): `init_policy.sql` seeds exactly those three plus `content_creator`. Note the small
   tension the doc does not address — `content_creator` **is** seeded as a `g2` subject role
   (`init_policy.sql:95-100`), so the quoted live-query result would be a 4-value set on any stack whose
   seed includes the M-era block; I could not measure it, so I did not book it.
