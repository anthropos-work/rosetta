# Seat C — reading #25

**Refs actually read (stated per rule 26).** Ground-truth clones re-verified with `git rev-parse` at
this reading's open, all matching the ADDENDUM: `app` `ad9f3c49`, `platform` `0c91421d`,
`next-web-app` `8297c684`, `sentinel` `f2c46190`, `storage` `4ce8ece5`, `messenger` `fa47850d`,
`cms` `ca50c817`, `graphql-wundergraph` `60c229f3`, `roadrunner` `87d8d443`, `jobsimulation`
`462343b0`, `studio-desk` `41ee3575`, `ant-academy` `22df69dd`, `stack-demo/rosetta-extensions`
(pinned consumption clone) `09d06070`, `.agentspace/rosetta-extensions` (authoring copy) `944fc4a2`,
nested `stack-demo/app/studio` + `stack-demo/cms/studio` both `aeec036a`. Corpus tree clean
(`git status --porcelain corpus/` empty). **No fetch was performed by this seat.**

Where a claim named a ref I graded it there (`6092c6d2`, `82cb66e`, `2035f9a`, `b948604`, `9d00a313`,
`7177374`, `9f8cb532`, `f38c0c4a`, `2adcf71`, `0dab54d`, `d11a403`, `838d907`); where it named none I
graded at the checkout, and I say so wherever the two disagree.

**Rext tree used:** every `rosetta-extensions` claim below was graded at the **pinned per-stack clone
`stack-demo/rosetta-extensions @ 09d06070`** (rule 45). I additionally diffed the `alignment/`,
`clerkenstein/alignment/` and `stack-seeding/dna/` layouts against the authoring copy `944fc4a2` —
**identical**, so no claim in this report turns on which tree was read.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/architecture/alignment_testing.md` | 542 | yes |
| `corpus/architecture/platform-migration-status.md` | 330 | yes |
| `corpus/services/jobsimulation.md` | 309 | yes |
| `corpus/services/customerio-sync.md` | 165 | yes |
| `corpus/services/askengine.md` | 147 | yes |
| `corpus/services/intelligence.md` | 18 | yes |
| **total** | **1511** | matches the assignment |

Also read in full, as the briefing requires: `corpus/ops/platform-alignment.md` §5 (lines 249–1557).

**Search-pipeline controls run in-pass** (§5 rules 1–6, 44):
- Enumerated every git tree under `stack-demo` (`find … -name .git -maxdepth 4`) → **15** trees,
  including the two nested `studio` checkouts, and grepped each at **its own** HEAD.
- Positive control on `idPattern` returned 3 hits when a companion pattern returned 0 — which
  **caught a wrong-regex false absence of my own**: `git grep "func GateMet"` returned 0 because
  `GateMet` is a *method* (`func (r *Report) GateMet`). Re-run without the `func ` prefix: found.
- Second false absence caught the same way: `grep "1000000"` over `alignment/` returned 0; the
  constant is written `1_000_000` (Go digit separator). The corpus claim `1..1000000` is **correct**.
- All stderr read; no command's error output was swallowed.

---

## Blockers

### B1 — jobsimulation.md's "one occurrence anywhere in the clone set" is four files, in two repos

- **Anchor:** `corpus/services/jobsimulation.md:50` —
  *"**No `.tf` file in any clone names `http://backend.internal.anthropos:8081`** — 0 hits over all 44
  tracked `.tf` files in the 13 `stack-demo` repos at each clone's own HEAD (measured 2026-08-06) …
  **The literal's one occurrence anywhere in the clone set is a markdown KB page** —
  `app/knowledge/service-dependencies.md:52` @ `app` `ad9f3c49`"*
- **Why it is false:** I re-derived the SET rather than the sum (§5 rule 24), enumerating all 15 git
  trees and grepping each at its own ref. The two numbers the sentence *shows* both reproduce
  exactly — **44** tracked `.tf` files (app 5 · cms 4 · graphql-wundergraph 4 · jobsimulation 6 ·
  messenger 4 · next-web-app 2 · roadrunner 4 · rosetta-extensions 2 · sentinel 4 · storage 5 ·
  studio-desk 4 = 44), **59** filesystem `.tf` files, **0** `.tf` hits. The claim they support does
  not. The literal occurs in **4 files / 6 lines across 2 repos**:
  - `app/knowledge/service-dependencies.md:52` @ `ad9f3c49` (1 line — the one the doc names; the
    quoted sentence and the `:50` heading *"There are no external callers of app's RPC mux left."*
    both verify byte-exact, and it is past-tense as claimed)
  - `rosetta-extensions/stack-core/tests/fixtures/repair_leak/pre/corpus/services/cms.md:32`, `:157`
  - `rosetta-extensions/stack-core/tests/fixtures/repair_leak/post/corpus/services/cms.md:32`, `:157`
  - `rosetta-extensions/stack-core/tests/test_platform_predicate_guard.py:435`

  (graded at the pinned per-stack clone `09d06070`; the authoring copy `944fc4a2` gives the same 5
  lines.) `rosetta-extensions` is **inside the sentence's own denominator** — it is one of the 13
  repos and contributes 2 of the 44 `.tf` files — so "anywhere in the clone set" cannot be read to
  exclude it. This is the §5-rule-44 failure the paragraph is otherwise a model of: it names its
  trees for the `.tf` half and then makes an unqualified tree-wide uniqueness claim that was never
  measured over the same set.
- **Confidence:** medium. I hesitated because the three extra sites are guard *fixtures* (frozen
  copies of an older `corpus/services/cms.md`) plus one Python test string — arguably not what a
  reader means by "the clone set". I book it anyway because the sentence is an absolute quantifier
  over a set the same sentence pair had just defined to include them, and because the whole
  paragraph's rhetorical weight rests on the literal appearing exactly once.

### B2 — jobsimulation.md points "the correctly-scoped model wording" at the production Cosmo-Router line

- **Anchor:** `corpus/services/jobsimulation.md:146` — *"All at platform `0c91421`; the
  correctly-scoped model wording is [`architecture_overview.md:321`](../architecture/architecture_overview.md)."*
- **Why it is false:** the anchor resolves and is non-blank, and it names the wrong construct.
  `corpus/architecture/architecture_overview.md:321` is
  `User → Vercel (Next.js) → Clerk (JWT) → ALB → Cosmo Router (port 8080)` — the first line of the
  **production** request-flow fence (`:318` heads it *"**In production** (the router still exists
  there …)"*). The sentence citing it is about **a `core` local stack**: it has just corrected
  *"the only cross-process edge"* to *"the only cross-process **Connect-RPC** edge"* and enumerated
  the plain-HTTP ones. The passage that words *that* correctly is **`:331`** —
  `→ Connect-RPC to sentinel   (the only cross-process RPC edge out of backend on a core stack)` —
  inside the *"**On a local stack** (platform `2adcf71` deleted the router …)"* fence at `:327-336`.
  A reader who follows the citation lands on an assertion that a Cosmo Router exists, ten lines above
  the block that says it was deleted locally — the exact inversion of what the citing sentence sends
  them to check. (Both anchors verified at corpus HEAD; the file is unmodified in the working tree.)
- **Confidence:** high on the measurement (`:321` vs `:331` is a one-digit transposition and both
  lines are unambiguous). Medium on the grading: an intra-corpus citation is cheaper to fix than a
  platform-source one — but the briefing lists *"wrong line anchors that point at the wrong
  construct"* under BLOCKER, and §5 rule 46 records that upheld findings of exactly this class
  (wrong-construct intra-corpus citations) are what the anchor fence's 65 % reach was hiding.

---

## Minors

- `corpus/services/jobsimulation.md:160` — cites `cms/directus/collections/jobsimulation.go:1594-1597`
  for the nil-`voice_engine` → `gptrealtime` default. **No such path exists in either tree**:
  `cms/directus/` is not a directory (`cms` top level is `cmd ent internal knowledge studio terraform`
  + files). The real files are `cms/internal/directus/collections/jobsimulation.go` and, in the live
  fold, `app/internal/cms/directus/collections/jobsimulation.go` — and in **both**, `:1594-1597` is
  exactly `func voiceEngineFromDirectus(...)` / `if directusVoiceEngine == nil` / `return
  simulation.SimulationVoiceEngineGptrealtime` / `}`. Substance and line anchor are right; one path
  segment (`internal/`) is missing. Booked MINOR rather than BLOCKER because the anchor is byte-exact
  in two trees and the construct is the one claimed.
- `corpus/architecture/alignment_testing.md:3` — `**Last updated:** 2026-06-06`, while the file's last
  commit is **2026-08-06** (`00be1ac`) and its body carries `M218`, `M219` and *"Corrected M257x
  iter-85"*. The stamp is two months stale and self-contradicted by the text below it. Conservative
  direction (understates currency), and every substantive passage carries its own milestone marker.
- `corpus/architecture/alignment_testing.md:532-540` — the Layout block lists four `internal/`
  packages (`dna`, `outcome`, `compare`, `report`). There are **five**: `alignment/internal/canon` is
  omitted — and it is the package implementing the *"canonical and precision-preserving"* value
  comparison the same doc claims at `:125-126`. Omitted list member; the block is already selective
  (it omits `go.mod` and `README.md` too, which `:328-331` enumerates elsewhere).
- `corpus/architecture/platform-migration-status.md:93` — the `messenger` row's terraform citations
  name **no ref**: *"`messenger/terraform/main.tf:29` `service_desired_count = 0` in a module that is
  otherwise intact (121 lines) … the reason in-comment (`:19-25` …); the image and task definition
  stay declared as the rollback path (`:27-28`)."* All four resolve byte-exact at `origin/main`
  `e9421c68` and **none** resolve at the checked-out `fa47850d`, where the file is **111** lines,
  `:19` is `service_desired_count = 1`, `:29` is `container_definitions = <<EOF`, and `:27-28` are
  `service_discovery_namespace_id` / `monitoring_sns_topic_arn`. Its three sibling rows all pin
  theirs (`storage` → `9f8cb53`, `roadrunner` → `87d8d44`, `jobsimulation` → `82cb66e`), and the
  `app` row spends a paragraph on why a pin is not a label. Not booked as a BLOCKER: the substance is
  **true** at a legitimate ref, and the map's own fence resolves citations at `origin/main` first
  (`CITE_REF=auto`, §5 rule 41) — I ran it and it reports `0 unresolvable … 0 read from the WORKTREE`.
  The defect is the missing ref, not the fact.

---

## Positively cleared (audited zeros)

Labelled per the briefing. **ENUMERATED** = I derived the predicate's members from source and
compared cardinalities. **RE-DERIVED** = I only re-computed arithmetic the document displays.

### `corpus/services/askengine.md` — clean

- **ENUMERATED** — the table allowlist. `registry.go` declares **60** `TableDef`s
  (`grep -c 'Name: *"'` = 60), **57** `Schema: "public"` and **3** `Schema: "directus"`. Matches
  *"**60** `TableDef`s, **57** … **3** …"* and the *"~60 tables"* upstream.
- **ENUMERATED** — the `jobsimulation.*` transition-alias claim. `registry.go:183-195` is exactly the
  comment block asserting the dotted names *"RESOLVE TO THE PUBLIC TABLES"* and *"`jobsimulation`
  stays in rewriteSchemas until M710"*; every `jobsimulation.*` entry carries `Schema: "public"` with
  a `RealName`. `rewriteSchemas` exists at `sandbox.go:293`.
- **ENUMERATED** — the four test-file counts (`grep -c '^func Test'`): sandbox **49**, executor
  **13**, prompt **10**, followups **14**. All four exact.
- **ENUMERATED** — the `/ask` route set. `internal/web/backend/backend.go:171-188` mounts the group
  with CORS + Clerk `authn` (and it is a sibling of `/api`, skipping the OpenAPI validator, as
  claimed); all 11 documented routes are registered, none missing, none extra.
- **ENUMERATED** — the `main.go:467-471` Bedrock block at the named ref `ad9f3c49`: byte-exact,
  including the `return`. `func main()` is at `:229` and **no other `func` declaration exists between
  `:229` and `:467`** (`awk '/^func /'` over main.go returns only `:203`, `:211`, `:229`), so the
  "returns from `main` itself" reasoning holds. The log string
  `"bedrock client unavailable; talk-to-data disabled"` occurs **exactly once** in `app`'s Go source.
  `:1295` is the RPC `http.NewServeMux()`, `:1361` the Meta-HTTP-server comment, `:1438` inside the
  messenger-subscriber block — all three named constructs are where claimed.
- **ENUMERATED** — `NewBedrockClient` is at `bedrock.go:161` and has **exactly one** error return
  (`config.LoadDefaultConfig`); `DefaultModelID = "eu.anthropic.claude-sonnet-4-6"` / `DefaultRegion
  = "eu-west-1"` at `:25-26`.
- **ENUMERATED** — constants: `MaxInlineRows = 200`, `MaxCellLength = 400`, `QueryTimeout = 10s`
  (`executor.go:18,21,24`); `askEngineMaxConns = 6` (`main.go:167`); `COPILOT_DB_CONN` read at
  `main.go:351`; `maxAgenticIterations = 15` / `loopTimeout = 10 * time.Minute`
  (`handler.go:34,41`); `rules.md` is 146,219 bytes ≈ *"~146 KB"*; `OrgCheckFeaturePermission` at
  `handler.go:718`; `v1.267.0 - 2026-05-07` and `v1.340.0 - 2026-07-17` both in `CHANGELOG.md`.

### `corpus/services/customerio-sync.md` — clean

- **ENUMERATED** — every `app` anchor at the named ref `ad9f3c49`: `env_guards.go:62` =
  `envCustomerIOSyncEnabled = "CUSTOMERIO_SYNC_ENABLED"`; `:98-104` = `case "":` … `}` (the
  deployed-is-an-error branch); `mustSubsystemSwitch`'s `log.Fatalf` at `:87`; `main.go:286` the
  resolve, `:284` a **comment** (as the doc's own correction says), `:295` the condition, `:296` the
  `log.Fatalf`, `:395` `customeriosync.New(logger, copilotDB, os.Getenv("BREVO_KEY"))`, `:393-396`
  the copilotDB pool. `doc.go:4-5` carries *"the last of the Go services to be folded into app"*
  across the line break.
- **ENUMERATED** — the doc's own falsifiable pin claims. `git ls-tree b948604f -- env_guards.go`
  returns **empty** (the file genuinely did not exist there), and `env_guards.go` / `main.go` /
  `doc.go` are **byte-identical** between `ad9f3c49` and `2035f9a4` (sha-compared).
- **ENUMERATED** — the compose side at `0c91421`: no `customerio-sync` service (the 5 declared are
  `sentinel`, `backend`, `studio-desk`, `next-web-app`, `gotenberg`); the only profile tokens in the
  file are `{core, backend, all, studio-desk, frontend}`; `:84-92` is exactly the comment block the
  doc paraphrases, including *"ENVIRONMENT=development is what makes unset mean off"*, which
  `env_guards.go:37-44`'s `deployedEnvironment()` confirms mechanically.
- **ENUMERATED** — the historical compose block. At `838d907^`, `docker-compose.yml:136-154`
  declares `customerio-sync` with `context: git@github.com:…#main`, `ssh: ["default"]`, the two build
  args, `ports: ["8080:8080"]`, the `DB_CONNECTION_BACKEND` env and
  `profiles: [customerio-sync, all]` — so both *"built straight from a git URL"* and *"dropped it
  from `all`"* hold. The doc's quoted YAML is a faithful (abridged) rendering.
- **ENUMERATED** — *"7,392 of 7,397 rows"* is `internal/customeriosync/doc.go:30` verbatim.
- **ENUMERATED** — *"`brevo` occurs 0 times in `external_services.md`"* and its `##` section list.

### `corpus/services/intelligence.md` — clean

- **ENUMERATED** — `fdfa189` is *"chore: remove intelligence service from local dev orchestration"*
  (2026-04-17) and its diffstat is exactly `docker-compose.yml | 25 ---` + `repos.yml | 3 ---`, so
  *"removed from `platform/docker-compose.yml` and `platform/repos.yml`"* is right on both halves.
  `intelligence` is not in the 4-entry `repos.yml` at `0c91421` and no compose service depends on it.
- **Not settleable, and correctly framed as historical:** the port `:8080`, `/_meta`, the 5-minute
  ticker and `DB_CONNECTION_{BACKEND,SKILLER}` are properties of a repo that has never been in the
  clone set. The doc says so itself.

### `corpus/services/jobsimulation.md` — everything except B1/B2

- **ENUMERATED** — the M810 terraform claims **at their named ref**. At `6092c6d2`,
  `jobsimulation/terraform/main.tf` has **zero** occurrences of `service_desired_count` and no
  `module "jobsimulation"` block; `:15-22` is the M810 decommission comment naming the ECS service,
  task definition, ECR repo, IAM roles, security group, Cloud Map entry, log group and alarms;
  `:24-40` is the *"What this module still owns"* list (LiveKit + Chime buckets, SSM params, atlas
  tracker); `:38-40` is the *"dropping the legacy `jobsimulation` schema is a separate M810 step"*
  clause. All four anchors exact. **Worth flagging for the reader, not as a defect:** at the
  checked-out `462343b0` that file is 343 lines and still declares the module with
  `service_desired_count = 0` at `:40` — the ref-pin is doing real work here, and this row is the
  cleanest example in my file set of why rule 33 exists.
- **ENUMERATED** — the `cms` counter-evidence: `cms/terraform/main.tf` is **191** lines with
  `service_desired_count = 0` at `:39` at *both* `ca50c817` and `f38c0c4a`; `6efa1d5` (2026-08-04) is
  *"chore(ci): drop build-production — the cms ECR repository is decommissioned (M810)"* deleting
  exactly `.github/workflows/build-production.yml` (18 lines). Both facts, both directions, as
  claimed.
- **ENUMERATED** — the four `jobsimulation` origin/main commits dated **2026-08-04**: `6092c6d2`,
  `caf36c96` (*Merge pull request #439*), `1e40d184`, `82cb66ec`. The archive-state hedge is sound.
- **ENUMERATED** — the data claims: `20260722081626_jobsim_data_model.sql` has exactly **23**
  `CREATE TABLE` statements; `20260722104506.sql:2` creates `job_simulation_sessions` and `:79` is
  `DROP TABLE "sessions";`.
- **ENUMERATED** — the mirror-drop migration. `20260729133514.sql` is 64 lines; `:58-62` is the
  *"5. Drop the mirrors."* comment through the first `DROP TABLE`; `:15-23` the id remap; `:36-44`
  the two orphan-NULLing `UPDATE`s; `:62-63` both `DROP TABLE`s. **`grep -c "INSERT"` = 0** — the
  *"0 `INSERT`s"* claim is exactly right, and it is a case where the *case-insensitive* grep returns
  1 (`DROP FUNCTION … on_insert_local_jobsimulation_sessions_…` at `:64`). `intelligence.go:1700` is
  `query := m.ent.JobSimulationSession.Query()` in `app/internal/organization/`.
- **ENUMERATED** — `queries.resolvers.go:70` is `func (r *queryResolver) JobSimulationResult(...)`,
  i.e. the persisted-read resolver the claim names.
- **ENUMERATED** — the compose model at `0c91421`: `AUTHORIZATION_ADDRESS=http://sentinel:8087` at
  `:48`, `GOTENBERG_URL=http://gotenberg:3200` at `:57`, `JUDGE0_BASE_URL` at `:59`, gotenberg's
  `profiles: [core, backend, all]` at `:183`, `gotenberg.go:31` the consuming request, and
  `grep -c "_RPC_ADDR"` = **0** over both `docker-compose.yml` and `common.yml`. Five services
  declared, seven effective with the `include: common.yml` floor — both exact.
- **ENUMERATED** — the historical `2adcf71` block: `docker-compose.yml:83` the service, `:93-94` the
  published `8400:8400`/`8401:8401`, `:113` `PORT=8400`, `:119` `RPC_PORT=8401`, `:140`
  `profiles: [graphql, jobsimulation, all]`, `Makefile:10` `PROFILE ?= graphql`, `repos.yml:17-19`
  with `migrations: false # legacy`. Every one exact.
- **ENUMERATED** — the binary's own defaults: `cmd/root.go:77`/`:78` are the two `cmp.Or` port
  reads; both Dockerfiles `EXPOSE 8080`; the in-repo `CLAUDE.md:124-125` documents `:8080`/`:8081`.
- **ENUMERATED** — the cobra contract: `rootCmd` sets neither `SilenceUsage` nor `SilenceErrors`
  (0 hits in `cmd/`); `ENTRYPOINT ["./application"]` with **no** `CMD` in both Dockerfiles; exactly
  **four** `rootCmd.AddCommand` subcommands — `aggregate`, `clone-session`, `test-command`,
  `validate`; the repo's `CLAUDE.md:88` really does document the non-existent `go run . serve`.
- **ENUMERATED** — the fold's signature side: `app` has **no** cobra root (`spf13/cobra` is imported
  by exactly one `.go` file, `cmd/createTaxonomy/main.go`; the nested `app/studio` @ `aeec036a`
  returns 0 for `cobra` at **its own** ref, so this tree-wide zero names its sub-repos).
  `app/main.go:216` is `func main()` at `7177374`, `:229` at `ad9f3c49` — both as stated.
  `jobsimwiring.Wire`'s fatal is `main.go:670` at `9d00a313` and `:614` at `b948604`, both exact.
  `internal/jobsimulation/ai/ai.go:90` is the `can't load AWS config: %w` return;
  `internal/jobsimwiring/wiring.go:147-148` is the bare `return nil, err`.
- **ENUMERATED** — `docker-compose.yml:88-89`/`:90`/`:91` at the named ref `0dab54d` are exactly the
  comment, `volumes:` and the `$HOME/.aws/credentials` bind, and it is the only AWS bind in the file.
- **ENUMERATED** — the rext side, at the **pinned** clone `09d06070`: `DIRECTUS_DATA_CONSUMERS =
  ("cms", "backend")` in **both** twins (`stack-core/gen_override.py:58` and
  `stack-injection/gen_injected_override.py:86`); the demo emitter uses `!reset null`
  (`gen_injected_override.py:638`) and the dev emitter `!override []` (`gen_override.py:225-234`),
  with the `!override`-replaces-vs-merge rationale in-comment at `gen_override.py:149-153`;
  `pt-aisim-chat-launch` exists.
- **ENUMERATED** — misc: `hibiken/asynq v0.26.0`; worker concurrencies **10** and **25**
  (`internal/worker/worker.go:27,104`); the `cms` stream subscriber at `cmd/root.go:285` via the
  colony `pubsub.SubscriberServer` at `:121` (not `internal/worker/`); migrations `20260402145459`
  and `20260409131539_add_summary_to_extraction_results` both present; platform `05b4035` is
  *"chore(compose): add READONLY_DB_CONNECTION to jobsimulation service"*; `ROADRUNNER_RPC_ADDR` is
  set 0 times in `platform` and read 0 times in `app` (2 hits only in the frozen repo) → *"dead
  config"* holds; `internal/runner/runner.go`'s package doc really says *"formerly the standalone
  'roadrunner' service"*; `calls/livekit.go:131-135` is the `flag_use_realtime_openai` read **inside**
  `CreateAgentDispatch` (`:106`) and `:140-144` sets `anthropos-agent` + `openai-hosted` — exact in
  **both** the `app` and `jobsimulation` copies.
- **ENUMERATED** — `graphql-wundergraph` supergraph: `supergraph-config-prod.yaml` lists `backend`
  alone, `schemas/` holds `backend.graphqls` alone.

### `corpus/architecture/platform-migration-status.md` — everything except the messenger-ref MINOR

- **ENUMERATED** — the completeness derivation, re-derived as a SET (§5 rule 24). `git log -p
  --follow -- repos.yml` yields exactly the **14** names the doc lists, in that membership. The same
  command on `docker-compose.yml`, section-blind, yields **26** two-space keys; the single non-service
  is `app-network` (declared under `networks:` at `:185-186`), giving **25** services — including
  `nats`, `web-app`, `chromedp`, `simulator`, `realtime`. I then checked the *"three of the 25 are
  filed under the repo name"* clause independently: exactly `backend`, `graphql`, `wundergraph` lack
  a same-named row. Every one of the 25 and 14 has a row.
- **ENUMERATED** — the §1 state vocabulary is **eight** entries, matching assertion C.
- **ENUMERATED** — the whole `app` row at its pinned `2035f9a`: `terraform/main.tf:181`
  `service_desired_count = 1`; `repos.yml:14-17`; `docker-compose.yml:28`; and all **seven** wiring
  call sites resolve to the named constructor — `:690` `skiller.NewSkillerManager`, `:721`
  `jobsimwiring.Wire`, `:751` `skillpath.NewSessionManager`, `:1153` `appcms.Wire`, `:524`
  `internalstorage.NewManager`, `:1471` `msgadapters.Wire`, `:395` `customeriosync.New`. All seven
  `app/internal/{…}/` dirs exist; **`app/internal/roadrunner/` does not**, and
  `app/internal/jobsimulation/runner/` does, constructed at `jobsimwiring/wiring.go:123` =
  `jsrunner.NewRunnerManager(getenv("JUDGE0_API_KEY"), getenv("JUDGE0_BASE_URL"))`.
- **ENUMERATED** — the currency note. `git rev-list --count 2035f9a..ad9f3c49` = **5**; the five
  touch exactly `.claude/skills/publish/SKILL.md`, `CLAUDE.md`, `knowledge/deployment.md`,
  `terraform/main.tf`, `terraform/variables.tf` — **no Go source**; and the single `terraform/main.tf`
  hunk is a one-for-one error-message rewrite (net 0 lines), so `:181` resolves identically at both.
  The claim *"every anchor in this cell resolves to the same construct at either ref"* is measured,
  not asserted.
- **ENUMERATED** — the `storage` row, at its named `9f8cb53`: `terraform/main.tf` is **18** lines;
  `:9-11` is the *"ECS service … is GONE"* comment verbatim; `:13-16` the `prevent_destroy`
  rationale; `:18` the *"See outputs.tf"* line (i.e. **not** a custody clause, as the row insists);
  `git grep -i custody` = **no match**; `storage.tf:22-25` is the M903-superseded paragraph verbatim;
  **no `moved` blocks** anywhere. Config side: `STORAGE_RPC_ADDR` 0 in compose/common;
  `STORAGE_S3_BUCKET`/`_PUBLIC_BUCKET` at `docker-compose.yml:82`/`:83` with the reason at `:73-75`
  and the `depends_on` note at `:102-103`. Consumer side: `main.go:516,517,518-523,524,525,547,1102`
  all name the claimed constructs, and `internal/storage/service.go:22`/`:24` are the two env-var
  **name** constants, not bucket names.
- **ENUMERATED** — the row's two-ref `STORAGE_RPC_ADDR` census, which is the sharpest derivation in
  my file set and reproduces **exactly**. At `2035f9a`: **3** hits, all comments, at the three named
  files, and `main.go:504` does say *"the standalone service takes no traffic and STORAGE_RPC_ADDR is
  gone."* At `b948604`: **15** hits; I classified each — **7 env lookups** (`main.go` ×3,
  `jobsimwiring/wiring.go` ×1, one in each of the three `cmd/` tools), of which **6** spell
  `os.Getenv` and **1** uses the lowercase `getenv` helper; the other 8 are 2 doc comments, 2 error
  strings, **3** `t.Setenv` and **1** `t.Fatal` message. Every sub-count in that cell is right,
  including the deliberately-fussy *"counting all four as `t.Setenv` overstates the arrange side by
  one"*.
- **ENUMERATED** — the `roadrunner` row's blame claim, the one most likely to be wrong and is not.
  `terraform/main.tf:19` is `service_desired_count = 1`; `git blame -L 19,19` names **`84a4b4f1`
  (2025-12-15)**; HEAD is `87d8d44` (2026-06-19); `e45eb61` (2026-05-27) changed **line 11 only** and
  its message really does read *"Module contents are identical; this is a pure source-URL swap."*
  `d11a403`'s own message really does claim the clone entry *"was already gone"* while its own
  `repos.yml` diff deletes `- name: roadrunner` alongside cms and jobsimulation — the row's
  *"the message is wrong; the diff is the fact"* is correct.
- **ENUMERATED** — the compose-service counts the `roadrunner` row quotes: `d11a403^` **11**
  services, `d11a403` **8**, `838d907` **5** (I subtracted `app-network` from each raw key count).
- **ENUMERATED** — the `customerio-sync` row's refutation of the platform's own commit message.
  `838d907`'s body does say *"was still in the `all` profile, so `make up-all` started a second Brevo
  contact pusher alongside backend's own"*; `0dab54d:docker-compose.yml:154` is
  `profiles: [customerio-sync, all]`; and `backend`'s own pusher is gated at `main.go:394` behind a
  switch that is off when unset on `ENVIRONMENT=development`, so `make up-all` started **one**. The
  correction is right and the row is right to quote-not-endorse.
- **ENUMERATED** — `messenger`'s consumer side at `2035f9a`: imports at `main.go:15`, `:62`, `:63`;
  group-name literal comment at `:1416-1421`; `MESSENGER_ENABLED` resolved `:285`, read `:1445` and
  `:1552`; subscriber server `:1450`; `msgadapters.Wire` `:1471`; `msgsender.NewFromEnv` `:1473`;
  `env_guards.go:92-111` is precisely `resolveSubsystemSwitch`; `terraform/main.tf:415-416` and
  `:419-420` are the two prod `"true"` values. `messenger/cmd/root.go:120-140` does read all four
  RPC addrs.
- **ENUMERATED** — `repos.yml` history: **9** entries at `d11a403^` → **6** at `d11a403` → **4** at
  `838d907` and HEAD; the file is 28 lines; the header sentences cited at `:2-10` and `:12-13` are
  where claimed.
- **ENUMERATED** — 18 platform/other commit shas + dates spot-checked and all correct: `045857c`,
  `fdfa189`, `21429b7`, `a4db680`, `236771f`, `b56d731`, `360efd4`, `2adcf71`, `6060315`, `a2a3ee6`,
  `cb6ebf5`, `ef4b449`, `1474b1f`, `84862d1`, `b43b99a`, `c17cc9a`, `467965a`, `8770fe6`.
- **ENUMERATED (by running the fence)** — §4's three coverage numbers. I executed the documented
  recipe read-only; it printed *"assertion F resolved 96 citation(s) — 25 subject-checked, 70
  range-only, 1 outside any service block; 0 unresolvable; 0 read from the WORKTREE"* and
  `OK — … agree in both directions`, matching `:204-205` and `:222-223` **exactly**, and confirming
  the doc's own instruction to take them from the run.
- **RE-DERIVED only** — `graphql-wundergraph/terraform/main.tf:20 = 1`, `sentinel/terraform/main.tf:19
  = 1`, `common.yml:2/:22/:24`, `docker-compose.yml:1-2/:5/:18/:112/:119/:135/:141/:143/:151/:160/
  :168/:170-171/:183`, `subgraphs.conf = BACKEND=v1.360.0` — each opened and read, but they are
  single-value anchors with no set behind them.

### `corpus/architecture/alignment_testing.md` — clean apart from the two MINORs

- **ENUMERATED** — all five DNAs, parsed and counted from JSON at the pinned clone: `clerk-2.6.0`
  **14 caps / 27 genes**, `clerk-js-5` **6 / 9**, `clerk-multi-1` **5 / 9**, `clerk-deploy-1`
  **3 / 7**, `clerk-express-1` **5 / 13**. Every figure in the score table matches, in both columns.
- **ENUMERATED** — the load-bearing negative: **only `clerk-2.6.0` declares a `consumed_surface`**
  (15 entries); the other four declare none. So *"four of the five surfaces are warned about but not
  enforced"* is measured, not asserted, and `dna.go:72-80` shows `--if-declared` downgrading exactly
  that one case (`NO COVERAGE CLAIM` → exit 0) while `:82-96`'s declared-hole path is untouched.
- **ENUMERATED** — the three identity genes. `universal-user` occurs in exactly three DNAs, under
  exactly the three capabilities named: `DeployIdentity` (clerk-deploy-1), `ExtractIdentity`
  (clerk-express-1), `Me` (clerk-js-5). And `GetUser` really does carry two-sided `hero-a`/`hero-b`
  variants asking for two different roster users.
- **ENUMERATED** — the six snapshot operators. `stack-seeding/dna/snapshot.go:62` is
  `var validSnapshotOperators = map[string]bool{` holding exactly the six named, `cross-surface-closure`
  included. `data-dna.json:410` (taxonomy, `snapshot-seeded-m9b`) lists five — all but
  `cross-surface-closure`; `:439` (content, `snapshot-seeded-m10`) lists five — no `embedding-dim`,
  with `cross-surface-closure` in its place. Both exactly as documented.
- **ENUMERATED** — the coverage arithmetic's SET: `data-dna.json` holds **10** surfaces — 4
  `seeded-m7a`, 4 `seeded-m7c`, 1 `snapshot-seeded-m9b`, 1 `snapshot-seeded-m10` — and **none
  waived**, so *"100% coverage with nothing left waived"* and *"(4 seeded in M7a, ~6 planned)"* both
  hold. `dna.go:316-331` `Coverage()` counts `IsSeeded() || IsSnapshotSeeded()` over the non-waived
  denominator, verbatim as described.
- **ENUMERATED** — the two zero-critical guards, both halves: `dna.go:279-281` rejects a DNA with
  capabilities and zero critical genes at load time, and `compare.go:56-61` `GateMet` returns false
  when `minCritical > 0 && r.CriticalGenes == 0`. The doc's *"two-layer"* framing matches the code's
  own comment.
- **ENUMERATED** — the four equivalence operators (`OpExact`, `OpShape`, `OpNormalized`,
  `OpErrorClass`, `dna.go:55-58`), the id regex `^[A-Za-z0-9][A-Za-z0-9_-]*$` (`dna.go:21`), and
  `maxWeight = 1_000_000` with `weight must be in 1..%d` (`dna.go:24,269-270`).
- **ENUMERATED** — the toy reference. `dna.json` has 2 capabilities / **6** genes (`Add` critical ×3,
  `Greet` standard ×3) with variants `basic`/`empty-name`/`padded-name`, descriptions and inputs
  matching the doc's JSON excerpt verbatim; `alignment_test.go:21-22` sets `gateOverall = 80.0`,
  `gateCritical = 100.0`; `cmd/toyrun/main.go` and `golden/<Cap>/<variant>.json` exist as described.
  The printed score **re-derives**: weights 3·3 + 2·3 = 15, aligned 13 → 86.7 %, critical 3/3.
- **ENUMERATED** — `gate.sh:69` is `"$alignctl" dna coverage --dna "$base/$dna" --if-declared` with
  the rationale comment occupying exactly `:58-68` (so the note that `:61` is *inside* the comment is
  right); `run.go:133-136` is the `ExitRegressed = 2` / `ExitUnmeasurable = 3` const block and
  `unmeasurable()` spans `:139-153` returning `ExitUnmeasurable`, banner text matching the quote.
- **ENUMERATED** — the inert-workflow correction, the one the doc says took two tries.
  `clerkenstein/.github/workflows/alignment.yml` **is git-tracked**, and `:10-11` reads *"as a subdir
  workflow under clerkenstein/.github (not at the monorepo root), this is currently inert in the
  rosetta-extensions monorepo; it is kept illustrative of the per-mirror gate shape"* — verbatim.
  `alignment/` has `cmd/ examples/ internal/ go.mod README.md` and **no `scripts/`**;
  `clerkenstein/alignment/scripts/` holds `gate.sh` + `drift-check.sh` (+ `drift-test.sh`).
- **ENUMERATED** — the five runners `clerkrun`/`jsfapirun`/`expressrun`/`deployrun`/`multirun` and
  the five DNA files are exactly what `clerkenstein/alignment/{cmd,dna}/` contain, in both rext trees.
- **ENUMERATED** — `alignment/go.mod` is `module anthropos.dev/alignment` with **no `require`
  block** (the *"stdlib-only"* claim); `run.go:38` calls `d.Validate()` before `:70`
  `compare.Evaluate`, so *"cannot be scored at all"* is structurally true;
  `clerkenstein/go.mod:9` pins `github.com/clerk/clerk-sdk-go/v2 v2.6.0`.
- **RE-DERIVED only** — `dna.go:20-26` as the subcommand dispatch (the four `case` labels sit at
  `:20`, `:22`, `:24`, `:26`).

---

## What I could not settle

- **§3's org-wide census** (`platform-migration-status.md:126-133`): *"93 repositories"*, *"46 are
  named by no corpus document"*, and every `last push` date in the census table. These come from the
  GitHub org API; nothing in the clone set can reproduce them. The two derivable halves of the
  paragraph — the **4** `repos.yml` entries and the **28**-line file, and the 9→6→4 history — I did
  verify. Settling the rest needs `gh api orgs/anthropos-work/repos --paginate`.
- **Repo archive states** (`skiller` 2026-07-01, `skillpath` 2026-07-31, `intelligence` 2026-04-02,
  `graphql-wundergraph` 2026-07-30, and the "not archived" assertions for `chronos`, `roadrunner`,
  `cms`, `storage`, `messenger`). Not in git objects. The `jobsimulation` row's *report-both-assert-
  neither* handling is the correct shape and I could confirm its evidence (four post-"archive"
  commits including a PR merge); the others I take on trust. Settled by
  `gh api repos/anthropos-work/<r> --jq .archived`.
- **All production-terraform claims for `cms` and `customerio-sync`.** Both docs say the deciding
  declaration lives in `infrastructure`, which is in no clone set. I confirmed the two *contrary*
  in-repo facts for `cms` and found no third; the state genuinely is *unmeasurable*, and both files
  say so.
- **`storage`'s M903 plan-status claim** (`platform-migration-status.md:92`), which cites
  `app/knowledge/plan/releases/09.00-support-in-app/m903-s3-custody/`. I did **not** open it — hard
  bar 1 forbids `knowledge/plan/**` and I chose the conservative reading of the path rather than
  assume it meant only the rosetta copy. The parts of that sentence outside the plan directory
  (`storage.tf:22-25`'s supersession text, the absence of `moved` blocks, `custody` = 0) all verify.
- **Live measurements quoted as history:** M257x iter-24's *"96 Directus log lines, all 403"*
  (`jobsimulation.md:149`), M7b's *"100% / Critical 100% across the 4 seeded surfaces"* and the
  injected-column drift run (`alignment_testing.md:447-448`), and the M218 *"score was identical
  before and after the fix"*. Each needs a live stack; the code paths they rest on all check out.
- **`WrapQuery` "four documented bypass vectors"** (`askengine.md:44`) — noted, not booked. The
  function's own docstring says *"three sandbox-bypass vectors"* and then lists **four** bullets
  (quoted-identifier, comment-hidden, unresolved FROM/JOIN target, reserved CTE name). The corpus
  counted the list; the source's prose miscounts its own list. If anything is wrong here it is the
  platform, not the corpus, so there is nothing for a corpus repair to do.
