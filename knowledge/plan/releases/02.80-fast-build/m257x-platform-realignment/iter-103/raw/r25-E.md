# Seat E — reading #25

**Ground truth actually read** (re-verified `git rev-parse` at open, matches the ADDENDUM):
`app ad9f3c49` · `app/studio aeec036a` (own checkout) · `cms/studio aeec036a` (own checkout) ·
`platform 0c91421d` · `next-web-app 8297c684` (origin/main `f97ba659`) · `sentinel f2c46190` ·
`storage 4ce8ece5` · `messenger fa47850d` · `cms ca50c817` · `graphql-wundergraph 60c229f3` ·
`roadrunner 87d8d443` · `jobsimulation 462343b0` · `studio-desk 41ee3575` · `ant-academy 22df69dd` ·
**`stack-demo/rosetta-extensions 09d06070` (the per-stack CONSUMPTION clone — the adjudicating tree
for tooling claims, per addendum / §5 rule 45)**; authoring copy `.agentspace/rosetta-extensions
944fc4a2` read only to confirm two paths exist in both.

No fetch, no checkout, no write outside this file.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/services/studio-room.md` | 507 | yes |
| `corpus/services/backend.md` | 371 | yes |
| `corpus/architecture/ai_architecture.md` | 305 | yes |
| `corpus/services/next-web-app.md` | 164 | yes |
| `corpus/services/clerk-integration.md` | 128 | yes |
| `corpus/architecture/README.md` | 38 | yes |
| **total** | **1513** | |

(`next-web-app.md` and `architecture/README.md` have no trailing newline, so the renderer shows 165 /
39 lines against a `wc -l` of 164 / 38. Cosmetic; noted so the count is not read as a short read.)

Search-instrument discipline: every "absence" below was taken with **all three** instruments named in
the addendum — bare `grep` via `find … -print0 | xargs -0`, `git grep` at the tree's **own** ref, and an
explicit enumeration of the nested repos (`stack-demo/app/studio`, `stack-demo/cms/studio` each at
`aeec036a`). Positive controls run in the same pass are stated per finding.

---

## Blockers

### B1 — `app/internal/ai/ai.go` is asserted not to exist; it does. The shared `ai` library was folded into `app`.

- **Anchor:** `corpus/architecture/ai_architecture.md:99` —
  > "selection/fallback in each consumer's own wrapper — in `app` @ `5ba17044` that is
  > **`app/internal/jobsimulation/ai/ai.go:267,344`** and **`app/internal/skillerai/ai.go:347`**, *not* a
  > bare `app/internal/ai/ai.go` (**no such file** — that path now resolves only inside the frozen
  > `jobsimulation` husk repo)."

- **Why it is false:** at the ground-truth checkout `app ad9f3c49` (which the addendum records as level
  with `origin/main`), `git ls-tree ad9f3c49 internal/ai/ai.go` returns
  `100644 blob 650b259a45088a295feee99d885c7cbb4caf39bc  internal/ai/ai.go`. The directory holds 17
  entries including the provider subpackages `internal/ai/anthropic/` and `internal/ai/openai/`. It was
  created by `app 1e457fa70 refactor(ai): fold the ai library into app as internal/ai` (Tue 2026-08-04),
  and the file the corpus cites two clauses earlier now imports it:
  `app/internal/jobsimulation/ai/ai.go:13-15` @ `ad9f3c49` reads
  `"github.com/anthropos-work/app/internal/ai"`, `…/internal/ai/anthropic`, `…/internal/ai/openai`.
  The platform even ships a guard test documenting the move —
  `app/internal/ai/module_import_guard_test.go:14-25`: *"the import path this package used to live
  behind, before it was folded into app at tag v1.40.2"*.
  The pin does not save it: the parenthetical asserts **currency** (*"that path **now** resolves only
  inside …"*), and §5 rule 33 is explicit that a block asserting currency cannot be pinned into silence.
  (For completeness the pinned half is fine — at `5ba17044`, `internal/ai/ai.go` is genuinely absent and
  `internal/jobsimulation/ai/ai.go:267` / `:344` and `internal/skillerai/ai.go:347` are all
  `"flag_use_azure_us"`, verified.)
- **Confidence:** high

### B2 — "All Go services access AI through the shared `ai` library" — no live Go service imports it any more

- **Anchor:** `corpus/architecture/ai_architecture.md:95-96` —
  > "### Unified AI Library — All Go services access AI through the shared `ai` library, which provides:
  > - A single `ai.AI` interface across providers (OpenAI, Azure, Anthropic, Bedrock, Mistral)"

  No ref is named, so it grades at the checkouts.
- **Why it is false:** ENUMERATED over every `go.mod` in the clone set (15 git trees found by
  `find stack-demo -name .git -maxdepth 3`, each grepped at its **own** HEAD):
  `github.com/anthropos-work/ai` is required **only** by `stack-demo/cms` @ `ca50c817` and
  `stack-demo/jobsimulation` @ `462343b0` — the two **frozen husk** repos with no compose service and no
  `repos.yml` entry. It is **absent** from `app/go.mod` @ `ad9f3c49` (which lists `analytics-go`,
  `colony v0.35.2`, `proto v1.210.0`, `storage v0.15.2`, `taxonomy v1.2.0` — and carried
  `ai v1.40.2` at `b948604f`, so this is a change, not a mis-read) and absent from `sentinel/go.mod` @
  `f2c46190` (`colony`, `proto`, `taxonomy` indirect). `app/go.sum` @ `ad9f3c49` has **0**
  `anthropos-work/ai` lines against **2** at `b948604f`. Positive control in the same pass:
  `anthropos-work/colony` returns hits in both `app` and `sentinel`, so the pipeline was not broken.
  `app` and `sentinel` are the only Go repos in `repos.yml` @ platform `0c91421d`.
- **Confidence:** high

### B3 — the private-module enumeration still lists `ai`

- **Anchor:** `corpus/architecture/README.md:21` —
  > "The five internal Go libraries — but only **four are imported as private modules** (`ai`, `colony`,
  > `proto`, `taxonomy`)."
- **Why it is false:** same measurement as B2 — after `app 1e457fa70` the live set is **three**
  (`colony`, `proto`, `taxonomy`); `ai` is now in-tree at `app/internal/ai/` and gone from `go.mod`/
  `go.sum`. The claim's practical consequence is a build claim (root `CLAUDE.md` states these are
  *"pulled at Docker build via `GH_PAT`/`GOPRIVATE`"*), and that is no longer true of `ai`.
  **Why I hesitated:** the sentence's neighbouring clause scopes itself to *"no checked-out
  `go.mod`/`go.sum` requires …"*, and under that literal scope `ai` **is** still required by two
  checked-out `go.mod`s — the frozen `cms` and `jobsimulation` husks. Booked as a blocker because the
  list is presented as the live import picture and the reader's action (expect a private-module pull)
  is wrong.
- **Confidence:** medium

### B4 — the Next.js version and the quoted dependency literal are both wrong

- **Anchor:** `corpus/services/next-web-app.md:22` —
  > "**Next.js 16.2.7** (App Router, Turbopack), **React 19.2.7**, **Node ≥ 24**, **pnpm 10.30.3**. All
  > four apps (`apps/{web,hiring,integration,maintenance}/package.json`) declare `"next": "^16.2.7"` and
  > the lockfile resolves it"
- **Why it is false:** ENUMERATED `git grep -n '"next":' 8297c684 -- '*package.json'` — all four apps
  declare **`"next": "~16.2.12"`** (`apps/web/package.json:46`, `apps/hiring:45`, `apps/integration:28`,
  `apps/maintenance:9`; also `packages/ui:65` `~16.2.12`, `configs/i18n:16` `>=16.2.12`). The lockfile
  resolves **`next@16.2.12`** (`pnpm-lock.yaml:40,58,109,112,166 …`). Identical at origin/main
  `f97ba659`, so this is not a checkout-vs-origin artefact. The literal `^16.2.7` the corpus quotes is
  real but belongs to **sibling** packages — `@next/third-parties` (`apps/web:21`, `apps/hiring:27`,
  `apps/integration:10`), `@next/mdx` (`apps/web:68`, `apps/hiring:63`, `apps/integration:46`) and
  `configs/eslint-config/package.json:5,11`. Both the version and the range operator (`^` vs `~`) are
  wrong. The rest of the sentence checks out: React `^19.2.7`, `engines.node ">=24.0.0"`,
  `packageManager "pnpm@10.30.3"`, `UPGRADE-IMPACT-next16.md` present.
- **Confidence:** high

### B5 — `@clerk/nextjs` is asserted "aligned … all four on `^6.39.2`"; three of the four are on `^6.39.6`

- **Anchor:** `corpus/services/clerk-integration.md:103` —
  > "**SDK versions:** `@clerk/nextjs` is **aligned** across `next-web-app` (`apps/{web,hiring,integration}`)
  > and `ant-academy` (`code/package.json:52`) — all four on `^6.39.2`."
- **Why it is false:** `next-web-app` @ `8297c684`: `apps/web/package.json:10`, `apps/hiring:10`,
  `apps/integration:9` all read **`"@clerk/nextjs": "^6.39.6"`** — same at origin/main `f97ba659`; the
  bump landed in `next-web-app dd0de31fc "fix(security): patch Next.js and Clerk advisories"`.
  `ant-academy` @ `22df69dd` `code/package.json:52` reads **`"@clerk/nextjs": "^6.39.2"`** (that anchor
  is exact). So the four are **not** aligned, and the single version the doc names is wrong for three of
  them. This sits inside the one paragraph whose whole purpose is to flag SDK drift and to tell the
  reader when to re-verify — asserting alignment is the failure the paragraph exists to prevent.
  The paragraph's other two limbs are correct and were re-derived: `@clerk/clerk-expo`
  `next-web-app/apps/mobile/package.json:6` = `~2.6.18` vs `ant-academy/mobile/package.json:18` =
  `~2.19.36`; and `app/go.mod:31` @ `5ba17044` = `github.com/clerk/clerk-sdk-go/v2 v2.7.0` (still `v2.7.0`
  at `ad9f3c49`, line `:29`).
- **Confidence:** high

### B6 — "the literal's only occurrence anywhere in the clone set" is an unproven tree-wide zero

- **Anchors:** `corpus/services/backend.md:148-153` — *"The literal's only occurrence anywhere in the
  clone set is a markdown KB page — `app/knowledge/service-dependencies.md:52` @ `ad9f3c49`"* — and the
  twin at `corpus/services/backend.md:294` — *"the literal's sole occurrence is a past-tense markdown KB
  page"*.
- **Why it is false:** `http://backend.internal.anthropos:8081` occurs in **4 files / 6 lines** across
  the working trees, not one. Besides `stack-demo/app/knowledge/service-dependencies.md:52` (correct,
  and past-tense as described), it is in the **pinned per-stack `rosetta-extensions` consumption clone**
  (`09d06070`), which the same passage's own denominator counts as part of the clone set — the "44
  tracked `.tf` files in the 13-repo `stack-demo` clone set" figure only reaches 44 **with**
  `rosetta-extensions`' 2 `.tf` files (app 5 + cms 4 + gql-wg 4 + jobsim 6 + messenger 4 + next-web 2 +
  roadrunner 4 + sentinel 4 + storage 5 + studio-desk 4 = 42, + rext 2 = 44). The extra sites are
  `stack-core/tests/fixtures/repair_leak/pre/corpus/services/cms.md:32,157`,
  `…/repair_leak/post/corpus/services/cms.md:32,157` and
  `stack-core/tests/test_platform_predicate_guard.py:435`.
- **Why I hesitated (say so, per the instrument):** the extra occurrences are *test fixtures that are
  copies of this corpus* plus one guard-test string, so the passage's load-bearing conclusion — **no
  terraform anywhere names that address** — is untouched, and I verified that conclusion twice (0 hits
  across the 44 tracked `.tf` and the 59 filesystem `.tf`). I booked it because it is exactly the
  §5-rule-44 class the addendum makes standing instruction: a tree-wide zero that does not name its
  sub-trees, published inside a paragraph that is otherwise a model of stated method. The cheap repair
  is to scope it ("no platform repo", or "outside the tooling clone's own test fixtures").
- **Confidence:** medium

---

## Minors

- `corpus/services/backend.md:289` — *"Registered as a third RPC handler in `main.go` after Users and
  Organizations."* At both refs `lab.v1.LabSessionService` is the **last** handler on the mux
  (`main.go:1337-1338` @ `ad9f3c49`, `:1227-1228` @ `b948604f`), after Skiller (`:1306`/`:1196`),
  JobSimulation (`:1314`/`:1204`) and the conditional CMS. The same file's `:98` gives the correct
  enumeration, so this is a self-contradiction — but it sits inside the dated *Recent Feature Additions
  (Q1-Q2 2026)* changelog describing what PR #896 did at the time, and no reader action changes.
- `corpus/architecture/ai_architecture.md:34` — anchor drift. `internal/cms/studio/markdownManager.go:19`
  resolves **exactly** at `b948604f` (`ai, err := mistral.NewMistral(nil, os.Getenv("MISTRAL_API_KEY"))`),
  but the passage names no ref and at `ad9f3c49` line 19 is a doc-comment line; the construct moved to
  `internal/cms/studio/mistralocr` (`markdownManager.go:10,14`). The substantive claim — Go-side Mistral
  is OCR-only — still holds (`studioManager.go:583` still exact).
- `corpus/services/backend.md:307` — *"its 3 remaining occurrences are comments"* is true only under an
  unstated **Go-source** scope (`internal/jobsimwiring/wiring.go:101`,
  `internal/storagens/callsites_test.go:189`, `main.go:504` — all comments, correct). Repo-wide at
  `ad9f3c49` `STORAGE_RPC_ADDR` occurs **31** times (mostly `knowledge/**` markdown). Name the scope.
- `corpus/services/studio-room.md:315` — *"the in-code fallback is a literal `worklog/` only if unset"*.
  `gen.py:452` derives it as `path_key.replace('_path','')` → `worklog` (no trailing slash), and the
  trace-persist fallback at `gen.py:467` is `'workspace/trace'`.
- `corpus/architecture/ai_architecture.md:235` — the `ai_model` enum is cited as
  `jobsimulation.go:983-990`; the const block is `:979-991` (11 members), so the range names 8 of them.
  All three examples the sentence quotes (`gpt-5` `:987`, `gpt-4.1` `:985`, `anthropic-45-sonnet-aws`
  `:983`) do fall inside, so nothing is misread.
- `corpus/services/clerk-integration.md:99` — *"`org:admin` **and** `content_creator`"*. The real set is
  four tokens, both bare and prefixed:
  `STUDIO_ACCESS_ROLES = ['admin','org:admin','content_creator','org:content_creator']`
  (`studio-desk@41ee3575:src/index.ts:96`, duplicated at `app/services/userService.ts:16`).
- `corpus/services/next-web-app.md` and `corpus/architecture/README.md` lack a trailing newline (renderer
  shows one more line than `wc -l`). No factual consequence.

---

## Positively cleared (audited zeros)

Labelled per the instrument. **ENUMERATED** = I derived the predicate's set from source independently
and compared cardinalities. **RE-DERIVED** = I only re-ran the arithmetic the document shows.

**ENUMERATED**

- `backend.md:30` / `:98` — the RPC mux. Read `main.go` at *both* named refs: `ad9f3c49:1297-1338` and
  `b948604f:1185-1228` are byte-for-byte the same block at a 112-line offset. **Six** handlers total,
  **five** unconditional at exactly the cited lines (`:1187` Users, `:1188` Orgs, `:1196` Skiller,
  `:1204` JobSimulation, `:1228` LabSession) plus `CMSService` inside `if cmsRPCServer != nil`
  (`:1212-1214`). Three of the eight folded services have a handler; skillpath/roadrunner/storage/
  messenger/customerio-sync have none.
- `backend.md:36` — `SkillPathSessionService`: `git grep -c` over `*.go` at `ad9f3c49` exits 1 with **0**
  hits; positive control `JobSimulationService` in the same pass returns hits in 3 files. No
  `skillpath…v1connect` import anywhere.
- `backend.md:55-67` / `:317` — Redis Streams. Enumerated **every** `pubsub.NewPublisher` at both refs
  (excluding `_test`): 5 at `b948604f` (`main.go:287`, `:637`, `:1039`, `wiring.go:127`, `:180`) and the
  same 5 at `ad9f3c49` (`main.go:325`, `:746`, `:1149`, `wiring.go:132`, `:185`) — the sixth
  `NewPublisher` hit is `deadletterqueue`'s `redisstream.NewPublisher`, a different constructor, and at
  `ad9f3c49` a seventh is a comment in `internal/messenger/flow/streams.go:36`. Subscribers: 6 at
  `b948604f` (`main.go:1274,1276,1285,1303,1305,1320`) and 6 in the map at `ad9f3c49`
  (`subscriber_wiring.go:203-248`, `subs` capacity literally `6`), applied by the single loop at
  `main.go:1579-1581`. So **six subscribers / five publishers**, `skiller` consumer-only, exactly as
  claimed. `subscriber_wiring.go:112-113` carries the quoted self-stream comment verbatim; `:209` and
  `:248` are the cited `subs[…]` lines. The `internal/messenger/flow/flow.go:120,153,157`
  `AddSubscriber` calls are on messenger's **own dedicated** SubscriberServer (`flow.go:106-117` says
  so), consistent with `backend.md:15`.
- `backend.md:284` — **13** `ai_readiness_*` tables: `git ls-tree ad9f3c49 internal/data/ent/schema/`
  yields exactly 13 `ai_readiness_*.go` schema files, and the four the doc names as "the ones a 9 omits"
  (`recommendation`, `email_override`, `notification_log`, `notification_optout`) are present, with
  `live_snapshot` + `text_translation` also present as it says. 13 − 4 = 9.
- `backend.md:354` — migrations. `git ls-tree … terraform/migrations/ | grep -c '\.sql$'` = **170** at
  `ad9f3c49`, **169** at `b948604f`; **46** files sort after `20260531`; the last May migration is
  `20260529072659_add_lab_session.sql` sitting at position 124 of 170 (i.e. 46 back from the head); the
  named heads and the three predecessors match the sorted listing exactly. `git ls-tree <ref>
  migrations/` is empty at `ad9f3c49`, `b948604f` and `2035f9a4`. `atlas.hcl` confirms
  `dir = "file://terraform/migrations"` / `src = "ent://internal/data/ent/schema"`.
- `backend.md:138-143` — the `SKILLER_RPC_ADDR` count series. Enumerated in `docker-compose.yml`:
  **4** @ `0808b92` (`:89`, `:151`, `:204`, `:334`), **1** @ `0dab54d` (`:183`), **0** @ `0c91421`; and
  total `*_RPC_ADDR` 21 → 4 → **0**.
- `backend.md:144-148` — the terraform sweep. **44** tracked `.tf` files summed per-clone at each
  clone's own HEAD across all 15 git trees (`app 5, cms 4, gql-wg 4, jobsim 6, messenger 4, next-web 2,
  roadrunner 4, sentinel 4, storage 5, studio-desk 4, rext 2`), and **59** by raw `find`. Both give **0**
  for the address. The stated positive control reproduces exactly: `find … -name '*.tf' | xargs grep -l
  service_discovery_namespace_id` = **25**.
- `backend.md:39-41` — the three still-declared Connect services in the frozen repos all resolve:
  `storage@4ce8ece5:sdk/storage/v1/service.go` (`StorageServiceClient`),
  `messenger@fa47850d:internal/rpcsrv/rpcsrv.go` (`messenger.v1.MessengerService.*`),
  `roadrunner@87d8d443:cmd/root.go:87` (`NewRoadRunnerServiceHandler`).
- `backend.md:22-26` / `:171-182` — the subgraph series. `graphql-wundergraph@749dc86` (2026-06-24)
  deletes `schemas/skiller.graphqls`; `@7c17e63` (2026-07-21) drops skillpath; `@915da06` (2026-07-29)
  `--stat` shows **both** `schemas/cms.graphqls` (−762) and `schemas/jobsimulation.graphqls` (−860)
  deleted in one commit; `git ls-tree 60c229f3 schemas/` = `backend.graphqls` alone. The doc's warning
  that `915da06`'s own subject says "2→1" is correct — it does.
- `clerk-integration.md:37` / `:79` — **12** webhook event types, enumerated from
  `app/internal/clerk/events/events.go` (`grep -c '^\s*case "'` = 12: user.{created,updated,deleted},
  organization.{created,deleted,updated}, organizationInvitation.{accepted,created,revoked},
  organizationMembership.{created,deleted,updated}).
- `clerk-integration.md:41` — **8** Clerk locales, enumerated from the import list at
  `next-web-app@8297c684:apps/web/src/providers/Clerk.provider.tsx:9-18`
  (deDE, enUS, esES, frFR, itIT, jaJP, nlNL, ptPT).
- `clerk-integration.md:51` / `:91` — the colony/authn measurement. `git grep -l "colony/authn"` at
  `ad9f3c49` scoped to `*.go` = **129** (the doc's number, reproduced at `b948604f`, `2035f9a4` and
  `5ba17044` too); all-files = 134, non-test Go = 77 — so the doc's predicate is Go files, and it is
  right. `sentinel@f2c46190`, `storage@4ce8ece5`, `messenger@fa47850d` each = **0**. `repos.yml` @
  `0dab54d` lists 4 Go repos, @ `0c91421d` lists 2.
- `clerk-integration.md:21`/`:65`/`:73`/`:81` — `apps/web/src/app/api/metabase/route.ts:35` checks
  `authData.orgRole !== 'admin'` (bare form); `internal/authorization/authorization.go:20-22` declares
  exactly `OrgAddUserToRole` / `OrgReplaceUserRole` / `OrgRemoveUserFromRole`.
- `next-web-app.md:31-35` — app table. Package names and dev ports enumerated from each
  `apps/*/package.json` @ `8297c684`: `@anthropos/web-app` :3000, `@anthropos/hiring-app` :3001,
  `@anthropos/integration` :3002, `@anthropos/maintenance-app`, `@anthropos/mobile`.
- `next-web-app.md:70` / `:155` — **8** locales on disk, enumerated:
  `git ls-tree 8297c684 configs/i18n/messages/` = de, en, es, fr, it, ja, nl, pt.
- `next-web-app.md:75` — `git ls-files | grep -E 'proxy\.ts$|middleware\.ts$'` returns exactly
  `apps/{web,hiring,integration}/src/proxy.ts` and **no** `middleware.ts` anywhere (`git grep -l ''
  8297c684 -- '*middleware.ts'` empty; positive control on `'*proxy.ts'` returns the three). The repo's
  own `CLAUDE.md:55` states the Next-16 rename verbatim, and `:15` says "Next.js 16 App Router". Every
  named public-allowlist entry resolves in `apps/web/src/proxy.ts` (`:16,17,22,25,31,33,37`).
- `next-web-app.md:97-99` / `:154` — no `"storybook"` script in any `package.json`, no `.storybook/`
  dir; the only trace is `configs/tailwind/storybooks.css`. `git ls-files | grep -i dockerfile` =
  `Dockerfile.dev` alone.
- `studio-room.md:184` — `gen.py` registers **exactly nine** arguments, enumerated from
  `git show aeec036a:gen.py`: `add_argument` appears at `:484-492` and nowhere else; every flag,
  `dest`, default and `choices` in the doc's block matches. `postgen.py:396-398` confirms `--media`,
  `--simid`, `--target` are all `required=True`.
- `studio-room.md:206`/`:281` — the `template` key has **zero** non-test consumers: `git grep` at
  `aeec036a` for `['template']` / `"template"` / `get('template'` returns only
  `tests/unit/test_request_assembly.py:43,45,52,54`, which assert it is *stripped*. There is no
  top-level `templates/` dir in `git ls-tree aeec036a`.
- `studio-room.md:362-390` — the Mistral callout, re-measured with all three instruments and both nested
  checkouts. `git -C stack-demo/app/studio grep -ci mistral aeec036a` = **22 lines / 3 files**
  (`requirements.txt:1`, `tools/pdf2md.py:18`, `tools/r3.py:3`); `requirements.txt:8` is `mistralai`;
  `tools/pdf2md.py:24` `from mistralai import Mistral`, `:96` client, `:127` `mistral-ocr-latest`;
  `tools/r3.py:194` hard-requires `MISTRAL_API_KEY`. The stated instrument traps reproduce **exactly**:
  `app/studio/.gitignore:2` = `tools/`, and a recursive shell `grep -ri mistral stack-demo/app/studio`
  returns **1**; `app/.gitignore:79` = `studio/*`, and `git -C app grep -ci mistral ad9f3c49 -- studio/`
  returns **0**; `cms/.gitignore:129` = `studio/*` and `stack-demo/cms/studio` is the same repo at the
  same sha `aeec036a`. `services/ai.py:705-708` is the three-provider registry and `:1-2` imports only
  `openai`/`anthropic`. Go execs exactly two studio scripts, at the cited lines
  (`studioManager.go:119` `studio/gen.py`, `:1045` `studio/postgen.py`, identical at `b948604f` and
  `ad9f3c49`); `app/Dockerfile:45-46` copies `studio/` and pip-installs its requirements.
- `ai_architecture.md:15-37` — the routing retraction. `internal/jobsimulation/ai/ai.go` @ `ad9f3c49`:
  the vendor consts are exactly four at `:30-33`; `getClient` is `:259-289` and is a plain `switch`;
  `flag_use_azure_us` is read at `:267` and a **failed** lookup returns the EU client (`:270-273`);
  `AnthropicAws` and `Anthropic` both return `a.anthropicClient` (`:280-283`), built with
  `config.WithRegion("eu-west-1")` (`:87`); `isThrottlingError` → `vendor = Openai` at `:154` and
  `:300`. `CreateSpeech` is Azure-only by construction. `grep -rin 'bedrock|boto3'` over
  `stack-demo/app/studio` returns **0** by both a `find`-driven grep and `git grep` at `aeec036a`.
- `ai_architecture.md:53-66` / `:243-260` — the three defaults and the two doors. Every anchor resolves
  at `ad9f3c49`: `collections/jobsimulation.go:905` `AIVendor *AIVendor`; `:1297` `aiModel :=
  simulation.Gpt5Point1`; `:1302-1305` the nil→`simulation.Openai` replacement; `:1307` the
  `simulation.Sequence{` construction; `:969-975` a **five**-member vendor enum with `Azureglobal` at
  `:971`. `simulator/ai/ai.go`: `:20-26` `GetValidationAIVendorAndModel` → Azure + `GPT4Dot1` /
  `GPT4Dot1Mini`; cases at `:58`, `:69`, `:86`, `:102` — **four** — and `default:` at `:114-115` setting
  `internalAi.Openai`; model defaults `:65-66`, `:82-83`, `:126-127` = `GPT4Dot1`, `:98-99` =
  `Anthropic37SonnetAWS20250219`, `:110-111` = `Anthropic35Sonnet20241022`.
- `ai_architecture.md:264-283` — the evaluation section. `checkerEngines` is declared
  (`validator.go:43`), populated (`:60-61`) and passed (`:595`) but **only ever assigned** downstream
  (`basevalidator/criterion.go:34,72,90` — never dereferenced), so "stored and never read" is right.
  The real dispatch is the hardcoded switch at `criterion.go:126-127`; `NewLLMBulkChecker(c.logger)` at
  `:428`; `templates/checkValidationBulk.tmpl` exists; `validateCodeDiff` runs concurrently at `:168`
  and sets `success` deterministically at `:450-475`. `skills.go:40-51` is
  `calculateCompetencyLevelScore` with the `< 60 && isPassed → 20`, `>= 100 → 100`,
  `max(0, score*2-100)` shape **and** the literal `// TODO fix this formula`; `:53-62`
  `calculateSkillScore`; `:75` `passed/total*100`. `app/internal/skill/skill.go:617-623` is
  `convertLevelTo100`; `next-web-app@8297c684:packages/ui/src/Competency/CompetencyReadLevel.tsx:18`
  calls `getSkillScoreForSimulation`, defined as a plain scale in
  `packages/graphql/src/utils/skillScore.ts`. Note the corpus writes the path as
  `basevalidator/criterion.go`; the real path is
  `internal/jobsimulation/simulator/validation/basevalidator/criterion.go` (no `v3/` segment) and it is
  unique in the tree, so it resolves.
- `ai_architecture.md:115-135` — the studio slot table, enumerated from
  `configs/production_config.ini:26-36` and `configs/development_config.ini:26-36` at `aeec036a`: the
  two files are identical over that range and stable/experimental are identical to each other, exactly
  as claimed. `config_template.ini:39` is `EXECUTION_AI_STABLE_MODEL = azure, gpt-4o, none` and `:40`
  `CREATIVE_AI_STABLE_MODEL = azure, gpt-4o, none`; `gpt-5.2` appears in no `*_MODEL` slot in any of the
  three configs and only as a pricing entry at `services/ai.py:356` and `:508`. `max_tokens = 4000` in
  all three tracked configs.
- `next-web-app.md:37-61` — the RETRACTED-scoreboard note. `apps/web/src/context/UserStatusContext.tsx:
  144-148` computes `userHasAllHiringOrgs` from `membership.organization.publicMetadata.isHiring`, and
  `:168-172` is the `window.location.href = buildSwitchHandoffUrl({ targetProduct: 'hiring', … next:
  '/home' })` eject. `apps/hiring/src/components/containers/InsightsByMembersContainer.tsx:108` is the
  export, mounted at `apps/hiring/…/@tabs/ai-simulations/[simId]/page.tsx:14`. `/enterprise/
  activity-dashboard` does exist in both apps. On the tooling side — read at the **pinned per-stack
  clone `09d06070`**, not the authoring copy — `demo-stack/frontend/hiring.Dockerfile` exists and
  `demo-stack/up-injected.sh:1076-1085` is the `build_frontend_hiring` block with image `demo-$N-hiring`
  and port 3001.
- `platform` compose/Makefile anchors used across `backend.md` and `next-web-app.md`, all read at
  `0c91421d`: `:48` `AUTHORIZATION_ADDRESS=http://sentinel:8087`, `:57` `GOTENBERG_URL`, `:59`
  `JUDGE0_BASE_URL`, `:110` backend `profiles: [core, backend, all]`, `:143` `next-web-app:`, `:151`
  build-arg endpoint, `:160` runtime endpoint, `:165-167` `depends_on: backend`, `:183` gotenberg
  profiles; `Makefile:10` `PROFILE ?= core`, `:119-120` `up-frontend` = `--profile core --profile
  frontend`. Backwards refs check too: `:100` at `0dab54d` is the backend profiles line, `:236` at
  `0dab54d` is the endpoint build arg. Compose declares five services (sentinel, backend, studio-desk,
  next-web-app, gotenberg) plus `common.yml`'s postgresql/redis; `repos.yml` lists four entries.
- `studio-room.md:425` / `clerk-integration.md:93` / `backend.md:367` — `d11a403` is measurably the
  commit that removed `cms` and `jobsimulation` from both `docker-compose.yml` (services list goes from
  11 at `2adcf71` to 8) and `repos.yml` (`git log -S'name: cms' -- repos.yml`).

**RE-DERIVED (weaker — flagged as such)**

- `backend.md:130` — `42,790 + 794 = 43,584` and the `skill_embeddings = 43,584` cross-check. These are
  prod DB counts; I cannot enumerate them from a clone, so this is arithmetic only.
- `ai_architecture.md:103` — `2.6 → 2.19` = "thirteen minor versions apart". True as stated, but it is
  arithmetic over two literals I did verify.

---

## What I could not settle

- **`ai_architecture.md:187-192`** — the five LiveKit agent repos (`livekit-agent`,
  `-chain`, `-azure-us`, `-azure-eu`, `-azure-eu-fr`) and the claim that `azure-eu`/`azure-eu-fr`
  "dispatch nothing". None is in any clone set and I am barred from fetching. Settled only by an org
  repo listing plus a read of each agent's dispatch config.
- **`backend.md:68-88` (the M810 cms half)** — the doc says outright that it is not measurable from our
  clone set, and that is correct: `infrastructure` is in no clone. What I *could* confirm both sides of
  is exactly what it claims — `cms/terraform/main.tf:39` is still `service_desired_count = 0` at
  `ca50c817` **and** at `f38c0c4a`, while `jobsimulation`'s teardown is landed at the ref it names
  (`6092c6d2:terraform/main.tf:15-22` is the M810 deletion comment and `:24-40` the "what this module
  still owns" list, both byte-exact; note the **checkout** `462343b0` still carries the live
  `module "jobsimulation"` block at `:31`, so this row is only true at the pinned ref, which the doc
  does name).
- **`backend.md:117-135` and the "Live de-risk (measured 2026-07-08)" paragraph** — grounded on
  `app@c3c45e01` / `platform@0808b92` and on read-only prod queries. `0808b92` is reachable and its
  compose numbers reproduce; the prod row counts are not checkable from here.
- **`next-web-app.md:113-117`** — that `make up PROFILE=frontend` exits 1. The mechanism is exactly as
  described (`next-web-app` `depends_on: backend` at `:165-167`; `backend` is `profiles: [core, backend,
  all]` at `:110`; `frontend` selects neither), but I did not run compose — that would be a state change
  and the daemon may not be up. Settled by one `docker compose --profile frontend config`.
- **`ai_architecture.md:96`'s provider list for the shared library** ("OpenAI, Azure, Anthropic, Bedrock,
  Mistral") — the `anthropos-work/ai` repo is in no clone set, so I can only say that its in-`app`
  successor `internal/ai/` ships `anthropic/` and `openai/` subpackages and no mistral one (Mistral now
  lives at `internal/cms/studio/mistralocr`). Settled by cloning `ai` at `v1.40.2`.
