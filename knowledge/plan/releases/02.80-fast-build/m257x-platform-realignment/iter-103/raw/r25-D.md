# Seat D — reading #25

**Ground truth used (re-verified at this reading's open, per the ADDENDUM — no fetch performed):**
`stack-demo/app` `ad9f3c49` · `stack-demo/platform` `0c91421d` · `stack-demo/next-web-app` `8297c684`
(origin/main `f97ba659`) · `stack-demo/sentinel` `f2c46190` · `stack-demo/storage` `4ce8ece5` ·
`stack-demo/messenger` `fa47850d` (origin/main `e9421c68`) · `stack-demo/cms` `ca50c817` (origin/main
`f38c0c4a`) · `stack-demo/graphql-wundergraph` `60c229f3` · `stack-demo/roadrunner` `87d8d443` ·
`stack-demo/jobsimulation` `462343b0` (origin/main `82cb66ec`) · `stack-demo/studio-desk` `41ee3575` ·
`stack-demo/ant-academy` `22df69dd` · **pinned consumption** `stack-demo/rosetta-extensions` `09d06070`
(the tree that adjudicates tooling claims, §5 rule 45) · authoring `.agentspace/rosetta-extensions`
`944fc4a2` · nested `stack-demo/app/studio` and `stack-demo/cms/studio` both `aeec036a`.

Every rext claim below was graded against the **pinned per-stack clone `09d06070`**, and I say so per row.
Every tree-wide zero below names the trees it covers and was run with a positive control in the same pass.

## Positive controls

| file | `wc -l` | read in full? |
|---|---|---|
| `corpus/architecture/service_taxonomy.md` | 514 | yes |
| `corpus/services/clerkenstein.md` | 370 | yes |
| `corpus/services/graphql-wundergraph.md` | 280 | yes |
| `corpus/services/messenger.md` | 204 | yes |
| `corpus/architecture/frontend_architecture.md` | 116 | yes |
| `corpus/services/db-backup.md` | 31 | yes |
| **total** | **1515** | matches the assignment |

Search-pipeline controls actually run (§5 rules 1–6):
- `grep --include=*.go` **failed** in this shell (`ugrep`: *"no matches found: --include=*.go"* on an
  unquoted glob) and returned exit 0 with no output — I read the stderr and re-ran with `git grep -- '*.go'`.
  A first `SetCookie` control also returned empty; that was a **real** absence (cookies are emitted as raw
  `Set-Cookie` strings, never via `http.SetCookie`), confirmed by a wider case-insensitive `cookie` grep
  returning 20 hits.
- `git show $c:file` inside a zsh loop raised *"bad substitution"* on the `AFTER` half of the subgraph-ladder
  check; I noticed the stderr rather than reading the empty output as evidence, and re-derived the same fact
  from `git show --stat`.
- `MESSENGER_RPC_ADDR` absence measured across **all 15 git trees** under `stack-demo` (nested `app/studio`
  and `cms/studio` included, each at its own ref `aeec036a`), positive control `SKILLER_RPC` returning
  26/12/9/4/1/20 across those trees.

## Blockers

### B1 — `clerkenstein.md` says sentinel is "still on colony `v0.34.3`"; it is on `v0.35.2`

- **Anchor:** `corpus/services/clerkenstein.md:276` —
  > "`sentinel` and `storage` are still on `v0.34.3`, so the pin is not wrong for the whole platform, but a
  > `clerk-deploy-1` score taken against `v0.34.3` **is not measuring the binary under test**."
- **Why it is false:** measured at the reading's ground-truth checkout, `sentinel/go.mod:8` @ `f2c46190`
  (which is **level with `origin/main`**) reads `github.com/anthropos-work/colony v0.35.2`. It read
  `v0.34.3` only at the *superseded* sha `88bc5592`; commit `88036d7` *"chore(deps): update dependencies to
  latest versions"* (2026-08-03) bumped it. `storage` @ `4ce8ece5` **is** still `v0.34.3`, so exactly half
  the conjunction holds — and the half that fails is the only **live, built** service in the pair
  (`storage` has had no compose service and no `repos.yml` entry since `838d907`). Derivation, all seven Go
  clones at their own refs: app `v0.35.2`, sentinel `v0.35.2`, messenger `v0.35.2`, cms `v0.35.1`,
  jobsimulation `v0.35.1`, storage `v0.34.3`, roadrunner `v0.34.3`.
  The load-bearing conclusion the sentence draws — *"the pin is not wrong for the whole platform"* — is
  therefore materially weaker than stated: **no service a stack builds is on `v0.34.3` any more**, so the
  `clerk-deploy-1` artifact pin is behind *everything* live, not just `app`.
- **On the pin, because it decides the grade.** The blockquote's ref (*"At platform `2adcf71`"*) sits in the
  **preceding sentence**, and §5 rule 33 — quoted in my briefing — scopes a pin to *"the claim's own block —
  a markdown CELL in a table, a wrapped sentence in prose."* This sentence carries no ref, is present tense,
  and uses **"still"**, which rule 33 names explicitly as a currency assertion that *"cannot be pinned into
  silence."* A platform sha also cannot date a claim about `sentinel`'s `go.mod` in any case — different
  repo. The corpus's own twin site handles this correctly and is thereby **inconsistent** with mine:
  `shared_libraries.md:57` pins the identical figure (*"`sentinel v0.34.3` (`sentinel/go.mod:8` @
  `88bc5592`)"*), which grades TRUE at its pin.
- **Confidence:** high (measurement); the only judgement is pin scope, and rule 33 settles it against the
  passage.

### B2 — `service_taxonomy.md` still counts `ai` among the imported private Go modules; `app` dropped it and CI-guards its return

- **Anchor:** `corpus/architecture/service_taxonomy.md:509` —
  > "| **Shared Libraries** | **5 libraries, 4 imported** — colony, proto, ai, taxonomy (none of them in
  > `repos.yml`; they are pulled at Docker build). …"

  Same predicate, same file, at `:166`: *"**Shared Libraries** (imported as private Go modules … pulled at
  Docker build via `GH_PAT`/`GOPRIVATE`)"* heading a five-row table whose third row is **ai** (`:172`).
- **Why it is false:** `github.com/anthropos-work/ai` was **folded into `app` as `app/internal/ai`** at
  `1e457fa70` *"refactor(ai): fold the ai library into app as internal/ai"* (2026-08-04; ancestor of
  `ad9f3c49`), and the module requirement was **removed from `go.mod`**. Enumerated across all seven Go
  clones at their own refs (`git grep 'anthropos-work/ai ' HEAD -- go.mod`), positive control `proto`
  returning 7/7:

  | repo | `anthropos-work/ai` in `go.mod` |
  |---|---|
  | app `ad9f3c49` | **absent** (present at the superseded `b948604f` as `v1.40.2`) |
  | sentinel `f2c46190` | absent |
  | storage / messenger / roadrunner | absent |
  | cms `ca50c817` / jobsimulation `462343b0` | `v1.40.2` — both **frozen legacy**, no compose service, not in `repos.yml` |

  So `ai` is required by **zero** repos a stack clones or builds, and is not *"pulled at Docker build"* by
  anything. The removal is deliberate and fenced twice: `app/internal/ai/module_import_guard_test.go`
  (*"TestNoExternalAIModuleImports is a one-way door"*) and `app/.github/workflows/ai-module-guard.yml`,
  which merges `main` into every PR tree so a pre-fold branch is checked with a guard it does not carry.
  `app/internal/ai/` exists in-tree (`ai.go`, `anthropic/`, `openai/`, `messages.go`, …).
- **This is also a corpus self-contradiction, and the twin is the doc `:166` links to as the "Full
  reference".** `shared_libraries.md:126` (marked *"corrected M257x iter-102"*) already states: *"**No repo a
  stack builds** … `app` **dropped** the module at `1e457fa70`"*; `dependency_map.md:42` states *"the two Go
  repos a stack clones and builds at platform `0c91421` (`app`, `sentinel`) pull in **THREE** of them —
  colony, proto, taxonomy … ai **2/7**"*; `dependency_map.md:48` repeats it. The repair landed in those two
  files and was not propagated here — §5 rule 19's cross-file drift. (For the record and outside my file
  set, the un-repaired form also survives at `architecture_overview.md:80`, `corpus/README.md:29`,
  `corpus/architecture/README.md:21` and root `CLAUDE.md:248`/`:413`.)
- **Note on the count, per §5 rule 24:** I enumerated the SET from source before touching the arithmetic.
  The *"4 imported"* figure is wrong in **both** directions for the live set — `ai` is not imported, while
  `app/go.mod` @ `ad9f3c49` additionally requires `analytics-go v0.3.1` and `storage v0.15.2`, neither of
  which appears anywhere in this table.
- **Confidence:** high.

### B3 — `frontend_architecture.md` quotes a superseded Next.js pin nine lines below its own "re-derived at `8297c684`" block

- **Anchor:** `corpus/architecture/frontend_architecture.md:59` —
  > "**Framework**: **Next.js 16** (App Router, Turbopack), React 19 — `apps/web/package.json:46` reads
  > `"next": "^16.2.7"` (same in `apps/hiring` / `apps/integration` / `apps/maintenance`)"
- **Why it is false:** at `next-web-app` `8297c684` — the checkout, and the ref *this same file names at
  `:41` and `:43` as "the ref this stack builds"* — `apps/web/package.json:46` reads
  **`"next": "~16.2.12"`**. Both the version and the **range operator** differ (`~` allows patch only; `^`
  allows minor). Measured in all four apps: `web`, `hiring`, `integration`, `maintenance` all read
  `"next": "~16.2.12"`. `"^16.2.7"` was correct at `bb3313bc` — the sha the ADDENDUM supersedes — and
  `git log -S` shows `dd0de31fc` *"fix(security): patch Next.js and Clerk advisories"* moved it.
  The claim names no ref, is present tense (*"reads"*), and the anchor `:46` resolves to the `"next"` line
  but to a different value, so it is a wrong-value anchor, not mere adjacency drift.
- **Why this is a blocker and not staleness:** lines 41–50 of this same file are an explicit, correct
  re-derivation *at `8297c684`*, disclosing the `bb3313bc → 8297c684` move (41 commits / 192 files —
  I re-derived both: `git rev-list --count bb3313bc..HEAD` = 41, `git diff --name-only` = 192). A reader who
  has just been told the file was refreshed at `8297c684` will read `:59` as current. It is §5 rule 18(a) in
  its purest form: *the anchors verify; the surrounding prose does not.*
- **Twins outside my file set** (same predicate, same value): `architecture_overview.md:59`,
  `next-web-app.md:22`.
- **Confidence:** high.

## Minors

- `service_taxonomy.md:47` — cites `studio-desk/.env.example:45` for
  `VITE_GRAPHQL_ENDPOINT=http://localhost:8082/graphql/query`. At `studio-desk` `41ee3575` that variable is
  at **`:44`**; `:45` holds `VITE_ENVIRONMENT=production`. It **was** `:45` at the superseded `14a5442a`, and
  two commits moved it. **I hesitated and nearly booked this** — the briefing lists "wrong line anchors that
  point at the wrong construct" as a blocker class, and `:45` does name a different env var. I graded it
  MINOR because it is one-line drift induced by the clone advancing between the frozen table and this
  reading (§5 rule 41: *"a large behind count is not a large repair"*), the file/variable/value are all
  correct, and the correct line is adjacent — the claim's substance misleads nobody.
- `clerkenstein.md:3` — header reads *"**Status:** v0.3 (… v2.3 'cue to cue' M217 … **M218** …) · **Last
  updated:** 2026-07-14"*, while the body documents M219, M220, M224 and v2.8 M256 (iter-16/iter-25) work.
  Both header fields understate the document.
- `clerkenstein.md:21` — *"`rosetta-extensions` is ONE private monorepo with sections (`clerkenstein`,
  `demo-stack`, `stack-injection`, `stack-core`, `stack-seeding`, `alignment`)"* names **6**; the pinned
  clone `09d06070` has **11** top-level sections (adds `dev-stack`, `playthroughs`, `stack-secrets`,
  `stack-snapshot`, `stack-verify`). Not marked exhaustive; omitted list member.
- `clerkenstein.md:105` — the `cmd/` row names `mintpk` / `fake-fapi` / `fake-bapi`; the dir also holds
  `jwtkey` (pinned clone `09d06070`).
- `messenger.md:120` — *"the binary still constructs four Connect-RPC clients"*; `messenger/cmd/root.go:118-142`
  @ `fa47850d` constructs **five** client objects (`cms`, `users`, `organizations`, `skiller`,
  `jobsimulation`) over **four** addresses, `users` and `organizations` sharing `BACKEND_USERS_RPC_ADDR`.
  The doc's own parenthetical groups them ("backend users + organizations") and `:28` correctly says *"four
  addresses"*, so the arithmetic is defensible as written.
- `frontend_architecture.md:31-35` — the "Core Packages" table omits `packages/design`, which exists at
  `8297c684`. Table is not marked exhaustive.
- `graphql-wundergraph.md:64-66` — the enumeration of `Subscription`-substring hits in
  `schemas/backend.graphqls` names `activeSubscription`, `stripeSubscriptionId`, `type PlanSubscription`;
  the file also contains `stripeSubscriptionItemId` (6 hits total). The characterisation *"Stripe/plan field
  names"* still holds.

## Positively cleared (audited zeros)

Labelled per the briefing. **ENUMERATED** = I derived the predicate's set from source independently and
compared cardinalities. **RE-DERIVED** = I only re-computed arithmetic the document displays.

### `service_taxonomy.md`

- **ENUMERATED** — *"`docker-compose.yml` declares five services (seven in the effective topology …), the
  default profile is `core`, and `core` starts five"* (`:65-68`). `docker-compose.yml` @ `0c91421` declares
  `sentinel`, `backend`, `studio-desk`, `next-web-app`, `gotenberg` (5); `include: common.yml` adds
  `postgresql` (`common.yml:2`) and `redis` (`:24`) → 7; `core` selects backend + gotenberg + the 3-service
  floor = 5.
- **ENUMERATED** — the always-on floor and the `profiles:` census (`:120-123`). Exactly four of the five
  declared services carry a `profiles:` key, at `:110` (backend), `:141` (studio-desk), `:168`
  (next-web-app), `:183` (gotenberg); `sentinel` carries none, and neither `common.yml` service does.
- **ENUMERATED** — the legal profile-token set (`:489-490`, and `graphql-wundergraph.md:15`): `{all,
  backend, core, frontend, studio-desk}` = 5 at `0c91421`; at `0dab54d` it was 8 (adds `storage-legacy`,
  `customerio-sync`, `messenger`), matching `graphql-wundergraph.md:16`'s *"read 'the eight' until 838d907"*.
- **ENUMERATED** — `authn` imported by nothing (`:509`): 0 hits for `anthropos-work/authn` across all seven
  Go clones at their own refs, control `colony` = 7/7.
- **ENUMERATED** — studio-desk *"no framework"* (`:212-215`): 0 `react|vue|angular` entries in
  `package.json` (control: `vite` matches 3×), 0 `.tsx`/`.jsx` tracked files (control: 71 `.ts`).
- **ENUMERATED** — `gen.py` registers *exactly nine* arguments and there is no `--template` (`:262-268`):
  9 `add_argument` calls at `app/studio` `aeec036a` `gen.py:484-492`, matching the nine named one-for-one;
  `parse_argument` at `:18-28` uses `parse_known_args` and **merges unknowns into the dict** at `:22-26`.
  (I checked the runnable example at `:257`, which passes `--evaluation_skills` — an *unregistered* flag —
  and it is legitimate: it is consumed by that merge, and it is quoted verbatim from `studio/CLAUDE.md:12-14`.)
- **ENUMERATED** — studio-room Mistral scope (`:238`): `git -C app/studio grep -i mistral aeec036a` = **22
  hits / 3 files** exactly (`requirements.txt` 1, `tools/pdf2md.py` 18, `tools/r3.py` 3); `mistralai` is
  imported only at `tools/pdf2md.py:24` (`r3.py` only names the `MISTRAL_API_KEY` env var);
  `services/ai.py:1-2` imports only `openai`/`anthropic`, and the provider registry at `:705-708` lists
  openai/azure/anthropic only.
- **ENUMERATED** — `academy.graphqls` is 1 of 43 files in `app/internal/web/backend/graphql/graph/schemas/`
  @ `ad9f3c49` (`:286`); all three `supergraph-config-*.yaml` @ `60c229f3` carry a single `- name: backend`
  and `schemas/` holds one file.
- **ENUMERATED** — the subgraph ladder `5 → 4 (749dc86, 2026-06-24) → 3 (7c17e63, 2026-07-21) → 1 (915da06,
  2026-07-29)` (`:407`), derived by reading `supergraph-config-prod.yaml` at each commit's parent and each
  commit's own `--stat`; `915da06` deletes `cms.graphqls` **and** `jobsimulation.graphqls` in one commit and
  its subject line does say *"supergraph 2→1"*, which is the wrong figure the doc flags.
- **ENUMERATED** — the deleted-service port/profile rows (`:93-112`) at `0dab54d`: storage `8300/8301`
  `profiles: [storage-legacy]`, messenger `8200/8201` `profiles: [messenger]`, customerio-sync `8080`
  `profiles: [customerio-sync, all]`.
- ENUMERATED/verified anchors, each opened with surrounding context (§5 rule 10): `repos.yml:14-17` +
  `:18-20`; `docker-compose.yml:5`, `:18`, `:48`, `:53`, `:56`, `:57`, `:59`, `:84-92`, `:110`, `:112`,
  `:141`, `:168`, `:183`; `0dab54d:docker-compose.yml:56`, `:154`; `Makefile:10`;
  `app/env_guards.go:37-44` and `:92-111`; `app/main.go:394`; `app` `3e5bc33ef:main.go:387`;
  `app/internal/converter/gotenberg.go:31`; `app/internal/cms/studio/studioManager.go:119`;
  `cms_queries.graphqls:106` (+ `cms_mutations.graphqls:22` for `archiveStudioTask`);
  `graphql-wundergraph/terraform/main.tf:20` (`= 1`), `locals.tf:8` (`port = 8080`), `main.tf:48-49`,
  `config.prod.yaml:5`; `cms/terraform/main.tf:39` (`= 0`, in an intact 191-line module) and `6efa1d5`
  merged `f38c0c4`; ant-academy `code/src/graphql/server.js:14,18`,
  `code/src/lib/backendContent.js:36,102-103`, `code/src/lib/serverTenant.js:115-145` and `:145` (read at
  `HEAD` — the working tree carries uncommitted demo-patch edits to that file),
  `code/app/api/academy/beacon/route.js:36,41-55`, `code/tests/unit/next-scaffold.test.js:106,111`,
  `react-compiler-config.test.js:41`, `code/app/layout.jsx:132`, `public/academy-manifest.json`
  (`display: standalone`), 0 serwist/workbox deps, `RegisterServiceWorker.jsx` is a kill-switch, port 3077,
  mobile 8555 + Expo `~54.0.33`, `deploy-academy.yaml`, `NEXT_PUBLIC_FEATURE_TRAINING_COACH` default off,
  `gpt-5.2` (academy `CLAUDE.md:255`); the Directus history retraction (`a2a3ee6^:docker-compose.yml`
  `:384`/`:386`/`:409` all exact, and `admin@example.com` **unfound** in the platform's whole history with
  `ADMIN_PASSWORD` as a working positive control); `045857c`, `fdfa189`, `b56d731`, `360efd4`, `2adcf71`,
  `d11a403`+`ef32d4c`, `0dab54d`, `838d907`+`0c91421` all resolve with the stated subjects and dates.
- **RE-DERIVED only** — `:159`'s jobsimulation M810 row. The cell names `6092c6d2`, and
  `jobsimulation/terraform/main.tf:15-40` resolves **byte-exactly** there (the M810 decommission comment,
  56-line file). `6092c6d2` is **not** an ancestor of the checkout `462343b0` — where the file is 343 lines
  and `module "jobsimulation"` is still declared at `:31` with `service_desired_count = 0` — but it **is** an
  ancestor of `origin/main` `82cb66ec`. Per the briefing's *"a pin is a DATE, not an excuse"*, the claim is
  graded at the ref it names and is **true there**. Flagging the two-ref split because a reader grading the
  cell against the checkout alone gets the opposite answer.

### `clerkenstein.md`

- **ENUMERATED** (pinned clone `09d06070`) — every gene/capability count in the score table and the prose:
  `clerk-2.6.0` **27 genes / 14 capabilities**, `clerk-js-5` **9**, `clerk-multi-1` **9** (with a `Roster`
  capability of exactly 2 variants → *"Roster 2/2"*), `clerk-deploy-1` **7 / 3 capabilities**,
  `clerk-express-1` **13 genes / 5 capabilities**. Derived by parsing the DNA JSON, not by trusting the doc.
- **ENUMERATED** — the *"97.2% / 26-of-27"* figure at `:41`, which reads like an arithmetic error
  (26/27 = 96.3%) and is **correct**. `alignment/internal/compare/compare.go:85-101` scores by **weight**,
  and `dna.Criticality.Weight()` (`internal/dna/dna.go:38-49`) is critical=3 / standard=2 / optional=1.
  Total weight of `clerk-2.6.0` = **71**; `MembershipOrgIdentity` is `standard` with one variant = 2;
  `(71−2)/71` = **97.2%** exactly. Gate thresholds `GATE_OVERALL 95` / `GATE_CRITICAL 100`
  (`alignment/scripts/gate.sh:31-32`) → *"≥95 / =100 ⇒ MET"* is right.
- **ENUMERATED** — `r.Cookie(...)` is called **nowhere** in `clerkenstein/` (0 `.Cookie(` across its 164
  tracked files at `09d06070`); cookies exist only as emitted `Set-Cookie` directive strings
  (`shared/rsa.go:115,126`, `clerk-frontend/server.go:394-409`).
- **ENUMERATED** — the repo-root workflow claim: `clerkenstein/.github/workflows/alignment.yml` is tracked,
  and `rosetta-extensions/` has **no** root `.github` directory at all, so the workflow is inert as stated.
  `rosetta-extensions/alignment/` has **no** `scripts/` dir; `ALIGN_DIR` defaults to `$base/../../alignment`
  (`clerkenstein/alignment/scripts/gate.sh:30`, `drift-check.sh:16`).
- Anchors opened and confirmed (pinned clone unless noted): `alignment/cmd/alignctl/run.go:134-135`
  (`ExitRegressed = 2` / `ExitUnmeasurable = 3`); `clerk-backend/store.go:138` (`SeedOrgIdentity`) and
  `:151` (`LookupOrgEid`); `alignment/dna/clerk-2.6.0.json:131` containing the exact quoted sentence;
  `clerk-frontend/server.go:35-67` (the M220 block: `clerkJSFetchTimeout = 15 * time.Second`,
  `clerkJSClient` commented *"Explicitly NOT http.DefaultClient"*, `FAKE_FAPI_CLERKJS_CACHE`);
  `type Server` carrying `signedIn`/`signedOut`/`clientID`/`sessID`; `clientID = "client_clerkenstein"`
  (`:125`) and `sessID = "sess_clerkenstein"` minted in `establishLocked` (`:663-670`);
  `handleMe`/`handleToken`/`handleClient`/`handleSignOut` all taking `_ *http.Request`;
  `handleSelectIdentity` setting `signedIn=false; sessID=""` and deliberately **clearing** `signedOut`;
  all five named `server_test.go` tests present; `registry.go` `activeKey` / `Registry.active()` /
  `Registry.Select()`; `orgNameDefault`/`orgSlugDefault` = `"Clerkenstein Demo Org"`/`"clerkenstein-demo"`
  (`resources.go:50-51`); the conditional `if u.OrgIsHiring { orgPublicMeta["isHiring"] = true }`
  (`resources.go:275-277`) with the role emitted prefixed as `"org:"+u.OrgRole`; the roster producer
  (`stack-seeding/seeders/roster.go:77,155` ← `blueprint.ResolvedStory.IsHiringOrg()`) and the DB half
  (`seeders/org.go:68` → `organizations.is_hiring`); the BAPI/FAPI route split
  (`clerk-backend/server.go:47` vs `clerk-frontend/meorgmemberships_test.go`); `dotless-pk-rejected` in
  `clerk-express-1.json:24`; DNA source refs `clerk-sdk-go v2.6.0`, `@clerk/clerk-js ^5.52.3` /
  `@clerk/nextjs ^6.39.2`, `@clerk/express ^1.3.47`, `colony@v0.34.3`; and — in `studio-desk` `41ee3575` —
  `STUDIO_ACCESS_ROLES` at `src/index.ts:96` **and** `app/services/userService.ts:16`, both carrying the
  quoted comment.
- **ENUMERATED** — `clerk-js-5.json` has a `Me` capability but **no** gene for
  `/v1/me/organization_memberships`; the only DNA mentioning `organization_memberships` at all is
  `clerk-2.6.0.json` (the BAPI side). The *"not yet a measured gene"* caveat at `:145-147` is correct.

### `graphql-wundergraph.md`

**Zero blockers found in this file.** It is the only assigned file where I could not falsify anything.

- **ENUMERATED** — the "no subscriptions" claim (`:59-69`), which is a four-part conjunction and I tested
  every clause (§5 rule 17): no `subscription:` block in any of the three `supergraph-config-*.yaml`;
  `schemas/backend.graphqls` declares `type Mutation` (`:4053`) and `type Query` (`:4912`) and **no**
  `type Subscription`; the 6 `Subscription` substring hits are Stripe/plan field names; and `bba862f`
  (2026-02-25) is contained **only** by `remotes/origin/feat/use-web-socket`.
- **ENUMERATED** — the compose-build history at `:126-134`, the densest claim in the file and correct in
  every part: `63d285c` (2024-06-20) introduced the service as `wundergraph` with a `git@…` context and
  **no `dockerfile:` key**; `d92e84e` renamed it to `graphql` (I read the diff — it renames both the service
  and the `wundergraph`→`graphql` profile tokens); `a2a3ee6` moved the context to `../graphql-wundergraph`;
  `719befb` (13:59) still has no `dockerfile:` key and **is an ancestor of** `2c85211` (16:01), which added
  `dockerfile: Dockerfile.dev`; `67ba772` moved the context to `..`; `360efd4` deleted the block. 2026-02-27
  → 2026-07-31 is the *"final five months"*.
- **ENUMERATED** — `ci/update-subgraph.sh` carries **exactly one** `gh release download`, at `:9`, with
  `-R anthropos-work/app`.
- Verified anchors: `grep -c 5050 docker-compose.yml` = **0** at `0c91421`; `Makefile:10` = `PROFILE ?=
  graphql` at `2adcf71` and `PROFILE ?= core` at `0dab54d`; `docker-compose.yml:135`/`:160` (runtime) and
  `:119`/`:151` (build args) all resolve, as do the historical `:220`/`:236` @ `0dab54d` and `:334`/`:352` @
  `2adcf71`; `subgraphs.conf` = a single `BACKEND=v1.360.0`; `package.json` = the two-line stub with the
  `graphql-wundegraph` misspelling, repeated in the repo's own `CLAUDE.md` heading; router `0.275.0`,
  `wgc@0.104.0`, `node:22.11-alpine`, `federation_version: =2.3.2`, `listen_addr 0.0.0.0:8080`,
  `graphql_path /graphql`; `terraform/tests/` is the only test dir; README's *"only work on aarch based
  machines"* above `make run`; `app/internal/authorization/gqlauthz/gqlauthz.go:186` is the
  `unknown viewer: Forbidden` return, with the public/federation/dev-introspection exemption at `:175-176`,
  and `gqlauthz_test.go` carries both `"bare __typename is not exempt"` (`:132`) and
  `TestAnonymousRejectionLogsAtWarn` (`:176`); `app/internal/web/backend/backend.go:315` serves the Apollo
  Sandbox at `/graphql` and `:317` the endpoint at `/graphql/query`; `/api/health` at `:140`.
- Intra-corpus citations resolve to the right constructs: this file's `:193` really is the
  `http://localhost:5050 refuses the connection` sentence; `:114-117` / `:116-117` really are the
  *Build-time, static composition* bullets; `:84` really is the *Ports* bullet, as the *"Not `:84`"*
  correction at `:134` asserts.
- The archive-date snapshot at `:8` is consistent with the clone: last commit `60c229f` is 2026-07-30, none
  after. (Archive state itself is unmeasurable from a clone; the doc says so at `service_taxonomy.md:142`.)

### `messenger.md`

**Zero blockers found in this file**, and it contains the single most precise passage in my set.

- **ENUMERATED** — *"21 handlers — 22 `pubsub.EventHandler(…)` lines of which one … is commented out"*
  (`:17`). Counted at `messenger` `fa47850d`, `internal/flow/flow.go:72-104`: 21 live
  `pubsub.EventHandler(` calls, plus the commented `OrgJobSimulationAssignmentPastDueHandler` at `:83`
  carrying `// not implemented` = 22 lines. `AddSubscriber("backend", …)` opens at `:72` and closes at
  `:104`. §5 rule 24's *"`grep -c` counts commented-out code"* is exactly what this passage pre-empts.
- **ENUMERATED** — `MESSENGER_RPC_ADDR` in **no repo**: 0 across all 15 git trees under `stack-demo`,
  nested `app/studio` and `cms/studio` at `aeec036a` included, control `SKILLER_RPC` non-zero in 6 of them.
  The two `MESSENGER_RPC` substring hits in `app` are in `app/knowledge/plan/roadmap-support-in-app.md` and
  are not `_ADDR`.
- **RE-DERIVED and independently re-run** — the `git log -S` figures at `:23-27`, every one exact:
  `-S 'MESSENGER_RPC' 0c91421d` = **0**; `-S 'SKILLER_RPC' 0c91421d` = **7**; `git rev-list --count
  0c91421d` = **121**; with `--all`, **8 and 0**, and the 8th is `464dfe3`, contained only by
  `remotes/origin/feat/cms-in-app`.
- **ENUMERATED** — the two-tree prod/local split at `:40-52`, which is the sharpest thing in my file set and
  is right on both sides: at the demo clone `fa47850d`, `terraform/main.tf:19` = `service_desired_count = 1`
  and `:29` is `container_definitions = <<EOF` (no count declaration); at `origin/main` `e9421c68`,
  `:29` = `service_desired_count = 0` with the rollback-path comment at `:27-28` verbatim. The clone is
  **7 commits behind**, as claimed.
- Verified `app` anchors at the refs the passages name: `main.go:15`/`:62`/`:63` (the three
  `internal/messenger/*` imports) and `:295-300` (`:295` the condition, `:296` the `log.Fatalf`) @
  `ad9f3c49`; `:1450` (`pubsub.NewSubscriberServer`), `:1471` (`msgadapters.Wire`), `:1473`
  (`msgsender.NewFromEnv`), `:1416-1421` (the `cmp.Or(os.Getenv("SERVICE_NAME"), "messenger")` comment) @
  `ad9f3c49`; `:1095` (the `CMS_STREAM` publisher) and `:1478-1484` (the subscriber stream binding) @ the
  pinned `9d00a313`; `:1205-1211` (the *"additive + DORMANT … until the M809 re-point"* comment) @ the
  pinned `b948604`. `env_guards.go:61` = `envMessengerEnabled = "MESSENGER_ENABLED"`, and
  `git ls-tree b948604f -- env_guards.go` is **empty** — the file genuinely did not exist there, exactly as
  `:63-65` says. `git diff --stat 2035f9a4 ad9f3c49 -- main.go` is **empty**, so the `:51` claim that the
  anchors resolve at both refs holds.
- Verified `messenger` anchors: `cmd/root.go:118-142` (the client block), `:63`/`:64` (PORT 8080 /
  RPC_PORT 8081 defaults), `:107` (`REDIS_STREAMS_INDEX` default `"2"`), `:147` (`READONLY_DB_CONNECTION`);
  `internal/rpcsrv/rpcsrv.go:25-30` (both stubs returning `CodeUnimplemented`);
  `internal/flow/jobsimulations.go:140-151` (the 2 h / 12 h staleness guards);
  `internal/flow/assignments.go:828` inside `getSkillPath`; `internal/flow/flow.go:74-78` (the five
  `OrgSkillPath*` handlers); `go 1.25.0`, `brevo-go v1.1.3`, `liquid v1.8.1`; the three named `_test.go`
  files. `REDIS_WORKER_INDEX` is read **nowhere** in `messenger` (control: `REDIS_STREAMS_INDEX` = 3 hits),
  confirming `:181`.
- Verified the compose history behind `:52` and `:75`: `repos.yml:21-23` at `0dab54d` is the messenger
  entry; the profile was `[messenger, all]` at `d11a403` and `[messenger]` at `0dab54d`, carrying the
  in-file *"two consumers on one group"* comment — so *"that release also dropped it from `all`"* is right.
- Cross-file consistency: `corpus/services/README.md:44` states the same `MESSENGER_RPC_ADDR` / 21-handler
  facts in the same form, so `:32`'s claim that the twin was repaired holds.

### `frontend_architecture.md`

- **ENUMERATED — and this is the strongest positive result in my set.** The whole `:41-50` table
  reproduces **exactly**, re-derived from source at three refs with the document's own predicate
  (non-test `.ts`/`.tsx`, `e2e/` excluded). I enumerated the file set first and then counted, per §5 rule 24:

  | quantity | `bb3313bc` | `8297c684` | `f97ba659` |
  |---|---|---|---|
  | files naming `NEXT_PUBLIC_BACKEND_API_URL` | 21 | **22** | 22 |
  | occurrences | 29 | **31** | 31 |
  | `fetch(` sites in those files | 43 | **47** | 47 |
  | of which `packages/core-js` | 12 env / 25 calls | **12 env / 27 calls** | 12 / 27 |

  `new EventSource(` = **0** repo-wide (`git grep -c` rc=1, no output). `f97ba659` is exactly 4 commits
  ahead of the checkout, and `bb3313bc..8297c684` is exactly 41 commits / 192 files — both as claimed.
  `packages/core-js/src/talkToData/api.ts:214` carries the quoted *"uses POST … rules out EventSource"*.
- **ENUMERATED** — the prose enumeration at `:39` sums to exactly the 22 files I derived: 4 core-js clients
  + 3 AI-readiness + invite + reminder-unsubscribe + member analytics + 4 assignment-builder/ask (both
  `apps/web` and `apps/hiring`) + 3 Stripe + CSV bulk import + résumé import + test-run network panel + 2
  admin backfill tools = 22.
- **ENUMERATED** — `repos.yml` holds exactly **4** entries at `0c91421` and exactly **9** at `2adcf71`;
  ant-academy is in neither. 8 i18n locales at `configs/i18n/messages`: de, en, es, fr, it, ja, nl, pt.
- Verified: `docker-compose.yml:141` studio-desk profiles (and `:226` at `0dab54d`); `:161` runtime env /
  `:152` build arg for `NEXT_PUBLIC_BACKEND_API_URL`; `!apps/mobile` in `pnpm-workspace.yaml`; app ports
  3000 / 3001 / 3002 from each `dev` script and mobile's `expo start --port 3031`; `apps/maintenance` has no
  dev script, matching the table's `—`; `@anthropos/web-app` / `@anthropos/hiring-app` /
  `@anthropos/integration` filter names; `@anthropos/tsconfig` and `@anthropos/i18n` package names;
  `turbo ^2.9.6`; `packageManager: "pnpm@10.30.3"`; `engines.node: ">=24.0.0"`; `typescript ^5.9.3`;
  `check` / `check:lint` / `check:types` / `check:deprecations` all present; `clean:quick` dropping
  `node_modules`, `.next`, `.turbo`, `dist`; `UPGRADE-IMPACT-next16.md` present.

## What I could not settle

- **`db-backup.md` in its entirety.** The `db-backup` repo is in no clone set and in no `repos.yml` at any
  ref I read, so none of its five claims (6-hour cadence, S3 + Azure + Hetzner triple target, Go, "all
  schemas", Docker-scheduled) is checkable from ground truth. Nothing in it contradicts any other corpus
  passage I read (`service_taxonomy.md:164` and `:512` agree), and both its outbound links resolve. It
  would be settled by adding the repo to a clone set, or by an `infrastructure` clone showing the scheduled
  task. **I record it as unsettled rather than cleared** — an unverifiable file read as clean is exactly the
  wrong-audited-zero shape §5 rule 24 warns about.
- **Repo archive states** (`service_taxonomy.md:157/158/159/393`, `graphql-wundergraph.md:8`,
  `clerkenstein` n/a). Archive state lives in the GitHub org API, not in git objects; `gh` is not installed
  on this host and the repos are private. `service_taxonomy.md:142` says exactly this. The only clone-side
  evidence is "no commits after the asserted date", which holds for `graphql-wundergraph` (`60c229f3`,
  2026-07-30) and, as the doc itself records, **fails** for `jobsimulation`.
- **The taxonomy figures** (`service_taxonomy.md:79`, `:174`: ≥42,790 skills / ≥22,470 job roles). These are
  DB measurements; no stack was running and I did not query one. The passages correctly label them a
  measured *public subset* and refute the "60K / 18K" pair by link.
- **`clerkenstein.md:41-52`'s live alignment scores** (the four 100%s and the `@clerk/express`
  UNMEASURABLE row). I verified the DNAs, the gene counts, the weighting that yields 97.2%, the gate
  thresholds and the exit-code contract, but running `alignctl` would require a Node/`@clerk/express`
  toolchain and is a state-changing build — outside a read-only pass. The claim that the express surface is
  *frequently unmeasured* is structurally supported by `run.go:121-153`.
- **Whether `cms`'s prod ECS module has actually been destroyed** (`service_taxonomy.md:160`). The doc's own
  *"report both, assert neither"* is the right posture: both cited facts are real
  (`cms/terraform/main.tf:39` = `service_desired_count = 0` at both `ca50c817` and `origin/main f38c0c4a`;
  `6efa1d5` merged `f38c0c4` deleted the build-production workflow citing M810), and `infrastructure` is in
  no clone set. Settled only by an `infrastructure` clone or a live AWS read.
- **`clerkenstein.md:274-279`'s `app/go.mod` clause pinned "At platform `2adcf71`".** A *platform* sha
  cannot date a claim about the `app` repo's `go.mod` — the two move independently. The claim happens to be
  true at the `app` checkout (`colony v0.35.2`, `clerk-sdk-go/v2 v2.7.0`), so I did not book it; but the pin
  is non-resolving as written, which is what let B1's neighbouring sentence go stale unnoticed.
