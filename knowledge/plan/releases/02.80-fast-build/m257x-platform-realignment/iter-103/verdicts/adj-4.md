# adjudicator 4 — iter-103 verdicts (seats r25-F, r26-F, r25-G, r26-G)

**Trees I read** (no fetch, no git state change, zero edits outside this file):
`stack-demo/app` `ad9f3c49` (+ `b948604f`, `1e457fa70`, `5ba17044`), `stack-demo/platform` `0c91421d`,
`stack-demo/next-web-app` `8297c684` (+ `origin/main` `f97ba659`), `stack-demo/sentinel` `f2c46190`
(+ `88bc5592`), `stack-demo/graphql-wundergraph` `60c229f3`, `stack-demo/ant-academy` `22df69dd`
(read via `git show HEAD:` — working tree is demo-patched), and the **pinned per-stack consumption clone**
`stack-demo/rosetta-extensions` `09d06070` for every tooling claim. The authoring copy
`.agentspace/rosetta-extensions` `944fc4a2` settled nothing.
Corpus is unchanged between the seats' read (`3822501`) and mine (`bd9d40d`): `git diff --stat 3822501 HEAD -- corpus/` is empty.

---

r25-F B1 | corpus/services/academy-backend.md:125-128 | UPHELD | IN-SCOPE | Demo academy does not serve its committed FS catalog; unpatched it serves zero cards
   evidence: `ant-academy` `22df69dd` `git show HEAD:code/src/lib/serverTenant.js` — `:143-147` `getServerCatalogView()` is `(await getBackendCatalogView(eids)) ?? emptyCatalogView()`; `:115-117` `emptyCatalogView()` returns `{chapters:[], skillPaths:{}, series:[], bundles:PUBLIC_BUNDLES, catalogVersion:…}`; the doc-comment at `:128-132` states *"there is NO FS-as-published fallback … the cutover is intentional, not reversible-on-error."* The only FS merge is the dev-only `draftsEnabled()` draft layer (`:146`). In the pinned rext clone `09d06070`, `demo-stack/ant-academy.sh:57-66` says the `academy-fs-published-fallback` demo-patch *"restores an FS-as-PUBLISHED catalog fallback (Option C)"* and is NON-FATAL — so an unpatched demo renders 0 cards, and the stated causal mechanism ("no endpoint, **so** it serves its committed FS catalog") is inverted. Live self-contradiction with `corpus/services/ant-academy.md:82-88` (*"**⚠️ It does not 'fall back to the committed FS catalog'**"*), which denies this exact phrasing; both sides are asserted live, so rule 5's retraction carve-out does not apply.

r26-F B1 | corpus/services/academy-backend.md:125-128 | UPHELD | IN-SCOPE | Same predicate: the demo FS-catalog mechanism is the reverse of the code
   evidence: identical re-derivation to r25-F B1 above — `serverTenant.js:115-117`/`:128-132`/`:143-147` at `ant-academy` `22df69dd`, and `demo-stack/ant-academy.sh:57-66` at rext `09d06070`. Both seats land on the same anchor and the same false predicate.

r25-F B2 | corpus/services/sentinel.md:85 | UPHELD | IN-SCOPE | Citation to architecture_overview.md:321 lands on the production Cosmo-Router line, not the quoted model
   evidence: `grep -n "only cross-process RPC edge out of backend" corpus/architecture/architecture_overview.md` → **exactly one** hit, **`:331`** (`  → Connect-RPC to sentinel   (the only cross-process RPC edge out of backend on a core stack)`), inside the *"On a local stack"* block opened at `:327`. `corpus/architecture/architecture_overview.md:321` = `User → Vercel (Next.js) → Clerk (JWT) → ALB → Cosmo Router (port 8080)`, the first line of the *"In production"* block (`:318-325`). `sed -n '85p' corpus/services/sentinel.md` really does read `architecture_overview.md:321`. The anchor resolves and names the construct the cited sentence exists to contrast against.

r26-F B2 | corpus/services/sentinel.md:85 | UPHELD | IN-SCOPE | Same predicate: the quoted core-stack model is at :331; :321 is the prod router hop
   evidence: identical re-derivation — one hit at `:331`; `:321` is the production request-flow line. Same anchor, same false predicate as r25-F B2.

r25-F B3 | corpus/architecture/architecture_overview.md:59 | UPHELD | IN-SCOPE | The Next.js pin is `~16.2.12`, not `^16.2.7`; wrong operator and wrong patch
   evidence: `next-web-app` `8297c684` — `git show HEAD:apps/{hiring,integration,maintenance,web}/package.json | grep '"next":'` → all four `"next": "~16.2.12"` (`hiring:45`, `integration:28`, `maintenance:9`, `web:46`); `apps/mobile` declares no `next`, so the "four apps" denominator is right. Identical at `origin/main` `f97ba659` (`apps/web/package.json:46`). The sentence carries no ref, so it grades at the checkout. Twins (not booked here, named for a claim-scoped repair): `corpus/architecture/frontend_architecture.md:59` quotes `apps/web/package.json:46` verbatim as `"^16.2.7"` and `corpus/services/next-web-app.md:22` repeats it.

r26-F B3 | corpus/architecture/architecture_overview.md:59 | UPHELD | IN-SCOPE | Same predicate: `^16.2.7` vs measured `~16.2.12` at checkout and origin
   evidence: identical re-derivation at `next-web-app` `8297c684` and `f97ba659`. Same anchor, same false predicate as r25-F B3.

r25-F B4 | corpus/services/ant-academy.md:121-122, :63, :111, :134, :477 + corpus/architecture/architecture_overview.md:40 | UPHELD | IN-SCOPE | Corpus asserts an "academy subgraph" six times while two sites deny it exists
   evidence: ground truth — `graphql-wundergraph` `60c229f3`: `supergraph-config-{prod,dev,compose}.yaml` each carry a single `- name: backend` entry and `schemas/` holds one file, `backend.graphqls`; at `app` `ad9f3c49` the academy SDL is `internal/web/backend/graphql/graph/schemas/academy.graphqls`, one file inside the backend subgraph. Corpus denials, both live and in bold: `corpus/services/academy-backend.md:87` (*"**There is no separate 'academy subgraph'**"*) and `corpus/architecture/service_taxonomy.md:286` (same sentence plus the derivation). `ant-academy.md:121-122` contradicts itself inside one sentence — *"the supergraph, which is `backend` alone"* then *"consumes the platform's **academy subgraph**"*. Both sides asserted live → rule 5 self-contradiction, and ground truth backs the negation.

r26-F B4 | corpus/architecture/architecture_overview.md:307-309 | UPHELD | IN-SCOPE | `markdownManager.go:11,19` resolves onto a closing paren and a removed-behaviour comment
   evidence: the bullet names no ref (nearest pins are in other bullets), so it grades at `app` `ad9f3c49`. `git show HEAD:internal/cms/studio/markdownManager.go` — `:10` = `"…/internal/cms/studio/mistralocr"`, **`:11` = `)`**; **`:19` = `// It used to take aiKey and then IGNORE it, reading os.Getenv("MISTRAL_API_KEY")`**, inside the `:17-28` comment recording that the env read was **removed**; the constructor is `:29-31` `mistralocr.New(aiKey)`. At `b948604f` the anchors were byte-exact (`:11` = `"github.com/anthropos-work/ai/mistral"`, `:19` = `mistral.NewMistral(nil, os.Getenv("MISTRAL_API_KEY"))`), so this is uncorrected drift. Noted: the *substance* survives — `studioManager.go:583` is `NewMarkdownManager(os.Getenv("MISTRAL_API_KEY"))`, byte-exact — but the cited lines do not build the client and `:19` documents the negation of the cited mechanism, so the claim as written is false at its governing ref.

r25-G B1 | corpus/architecture/shared_libraries.md:57 | UPHELD | IN-SCOPE | No colony two-way split at 0c91421 — app and sentinel both pin v0.35.2
   evidence: `sentinel` checkout `f2c46190` **is** `origin/main` (`git rev-parse origin/main` → `f2c46190`); `git show HEAD:go.mod:8` = `github.com/anthropos-work/colony v0.35.2`, vs `git show 88bc5592:go.mod:8` = `v0.34.3`. `app` `ad9f3c49` `go.mod:15` = `colony v0.35.2`. Dates: `88bc5592` 2026-06-19, `88036d7`+`f2c4619` both **2026-08-03**, platform `0c91421d` **2026-08-05** — the bump landed two days *before* the state the sentence dates itself to, so the claim is false **at the ref it names**, not merely stale. The lead clause *"`sentinel` + `storage` → `v0.34.3`"* carries no ref at all and is false at the checkout; the block asserts currency (*"it is **still** two-way"*, *"now frozen, **not live**"*). Not ref-discipline: the `@ 88bc5592` pin dates the cell read, not the `0c91421` state assertion wrapped around it.

r26-G B1 | corpus/architecture/shared_libraries.md:57 | UPHELD | IN-SCOPE | Same predicate: the live colony split is gone; both live Go repos pin v0.35.2
   evidence: identical re-derivation — `sentinel/go.mod:8` @ `f2c46190` = `v0.35.2` (= `origin/main`, 2026-08-03) vs `@ 88bc5592` = `v0.34.3` (2026-06-19); `app/go.mod:15` @ `ad9f3c49` = `v0.35.2`; platform `0c91421d` is 2026-08-05. Same anchor, same false predicate as r25-G B1.

r25-G B2 | corpus/architecture/shared_libraries.md:85 | UPHELD | IN-SCOPE | No proto skew at 0c91421 — app and sentinel both pin v1.210.0
   evidence: `sentinel/go.mod:9` @ `f2c46190` (= `origin/main`) = `github.com/anthropos-work/proto v1.210.0`; `@ 88bc5592` = `v1.200.0`; `app/go.mod:16` @ `ad9f3c49` = `proto v1.210.0`. The bump is `88036d7`/`f2c4619` (2026-08-03), i.e. before platform `0c91421d` (2026-08-05). *"the **live** skew is two"* is a currency assertion dated to `0c91421`, and the lead clause *"sentinel `v1.200.0`"* carries no ref. A distinct, separately-repairable claim from B1 (different library, different row, different version numbers) — read both, not collapsed.

r26-G B2 | corpus/architecture/shared_libraries.md:85 | UPHELD | IN-SCOPE | Same predicate: the proto live skew is one value, not two
   evidence: identical re-derivation — `sentinel/go.mod:9` @ `f2c46190` = `v1.210.0`, `@ 88bc5592` = `v1.200.0`, `app/go.mod:16` @ `ad9f3c49` = `v1.210.0`. Same anchor, same false predicate as r25-G B2.

r25-G B3 | corpus/architecture/shared_libraries.md:128-130, :137, :162-164 | REJECTED | — | Claims `ai.AI`/Mistral-OCR-only/Anthropic-panics describe no readable copy of the library
   evidence: the `ai` module is in **no** clone set, but a readable copy of it **at the version this section's own Version-pin row names (`v1.40.2`)** is in the `app` clone: `git show 1e457fa70:internal/ai/ai.go` (the fold commit, *"refactor(ai): fold the ai library into app as internal/ai"*, 2026-08-04) declares **9** interface methods **including `ChatCompletionStream`** at `:10`; `git ls-tree -r 1e457fa70 -- internal/ai` contains `internal/ai/mistral/completion.go`, whose `ChatCompletion`/`ChatCompletionStream`/`CreateEmbeddings`/`CreateSpeech` are each `panic("not implemented")` (`:40`, `:44`, `:48`, `:52`) with a real `OCRProcess` at `:193`; `internal/ai/anthropic/completion.go` panics at `:180` and `:189`. All three booked claims hold verbatim there. What the seat measured — `app/internal/ai/` at `ad9f3c49` (8 methods, no `mistral/`, characterization tests asserting no panics) — is app's **post-fold, diverged in-tree fork**, not the `ai` library the section documents. The brief is explicit: do not uphold a "the library does not do X" booking taken against a tree that does not contain the library.
   class: wrong-tree — graded at `app` `ad9f3c49`'s diverged in-tree fork instead of the `ai` module at the `v1.40.2` the section names, readable in the same clone at `1e457fa70`, where all three claims are true.

r25-G B4 | corpus/services/hiring.md:24-25 | UPHELD | IN-SCOPE | "each off by −2 at 5ba17044" is wrong for the third anchor by 50 lines
   evidence: `app` — `git show ad9f3c49:internal/organization/manager.go`: `:450` `switch org.IsHiring {`, `:453` `antRole = enum.RoleCandidate`, `:537` `return fmt.Errorf("organization is not hiring")`. At `5ba17044` the same three constructs are `:448` (−2 ✓), `:451` (−2 ✓) and **`:487`** (−50 ✗) — `git show 5ba17044:internal/organization/manager.go | grep -n "organization is not hiring"` returns `453` (a comment), `486`, `487`. The line the stated −2 predicts, `5ba17044:535`, is `warnings := []*BulkInviteMemberMessage{}` inside `BulkImport` (`:533`), an unrelated function. The sentence exists precisely to tell a reader how to re-derive at the historical ref, and for one of its three anchors it sends them to unrelated code.

BOOKED=14 UPHELD=13 REJECTED=1 IN-SCOPE-UPHELD-BLOCKERS=13

## Predicate groups

- demo-academy-fs-catalog: r25-F B1 (corpus/services/academy-backend.md:125-128) + r26-F B1 (corpus/services/academy-backend.md:125-128) — a demo academy does NOT serve its committed FS catalog because the endpoint is unset; unpatched it serves the empty view (0 cards), and `ant-academy.md:82-88` denies this exact phrasing as a live claim.
- sentinel-cite-lands-on-prod-router: r25-F B2 (corpus/services/sentinel.md:85) + r26-F B2 (corpus/services/sentinel.md:85) — the intra-corpus citation names `architecture_overview.md:321` (the production Cosmo-Router hop) for a sentence that lives at `:331` and asserts the opposite topology.
- next-js-pin: r25-F B3 (corpus/architecture/architecture_overview.md:59) + r26-F B3 (corpus/architecture/architecture_overview.md:59) — the four Next apps pin `~16.2.12`, not `^16.2.7`; both the range operator and the patch are wrong.
- academy-subgraph-does-not-exist: r25-F B4 (corpus/services/ant-academy.md:121-122, :63, :111, :134, :477; corpus/architecture/architecture_overview.md:40) — the corpus asserts a "platform academy subgraph" as a live consumed construct while `academy-backend.md:87` and `service_taxonomy.md:286` state in bold that no such subgraph exists, and the supergraph declares one subgraph (`backend`).
- markdownmanager-anchors: r26-F B4 (corpus/architecture/architecture_overview.md:307-309) — `internal/cms/studio/markdownManager.go:11,19` names a closing paren and a comment documenting the removal of the very `os.Getenv("MISTRAL_API_KEY")` read the sentence attributes to it.
- colony-two-way-split: r25-G B1 (corpus/architecture/shared_libraries.md:57) + r26-G B1 (corpus/architecture/shared_libraries.md:57) — there is no colony split at platform `0c91421`; `app` and `sentinel` both pin `v0.35.2`, and sentinel's bump predates `0c91421` by two days.
- proto-live-skew: r25-G B2 (corpus/architecture/shared_libraries.md:85) + r26-G B2 (corpus/architecture/shared_libraries.md:85) — the proto "live skew is two" is one value; `app` and `sentinel` both pin `v1.210.0` at `0c91421`. Distinct from the colony predicate (different library, different row, separately repairable).
- hiring-offset-arithmetic: r25-G B4 (corpus/services/hiring.md:24-25) — the "each off by −2 at `5ba17044`" re-derivation instruction is wrong for `manager.go:537`, which is `:487` there (−50), and the predicted `:535` is unrelated code.
