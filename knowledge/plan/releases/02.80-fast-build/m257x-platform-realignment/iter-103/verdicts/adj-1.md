# adjudicator 1 — iter-103 verdicts

**Trees I read** (no fetch, no checkout, no state change; refs re-derived at my open):
`stack-demo/app` `ad9f3c49` (== `origin/main`; `b948604f` read only to characterise), `stack-demo/cms`
`ca50c817`, `stack-demo/platform` `0c91421d`, `stack-demo/app/studio` `aeec036a` (own checkout),
and — for the one tooling measurement below — the **pinned per-stack consumption clone**
`stack-demo/rosetta-extensions` **`09d06070`**, never the `.agentspace` authoring copy.
Every `file:line` I graded was opened and read around, not grepped-to.

---

r25-A B1 | corpus/architecture/dependency_map.md:19 | UPHELD | IN-SCOPE | dependency_map attributes a depends_on/REDIS_ADDR/go.mod statement to storage.md:40,47; storage.md makes it nowhere
   evidence: `corpus/services/storage.md` (248 lines) — `command grep -cE 'REDIS_ADDR|DB_CONNECTION|go\.mod|depends_on|redis|postgres'` → **0** (rc 1), and a python substring count over the decoded file gives `edis` 0, `ostgres` 0, `DB_CONNECTION` 0, `go.mod` 0, `depends_on` 0 (positive control `storage` → 49). The two cited lines resolve onto unrelated constructs: `:40` = `> being when the clone advanced to \`ad9f3c49\`.)` (the tail of an iter-102 parenthetical about the demo build pin) and `:47` = a **blank line** between the `## Architecture & Code Map` heading (`:46`) and the `* **Codebase**` bullet (`:48`). The nearest supporting text, `:44` *"Storage is stateless and owns no database"* and `:51` *"**Database**: none"*, supports "owns no DB" and says nothing about redis, `depends_on`, `DB_CONNECTION`/`REDIS_ADDR` or `go.mod` — so re-pointing the anchor cannot repair it; the attribution itself is false. The underlying platform facts are true (I confirmed the `storage` block's `depends_on` and env at `platform 0dab54d`), which is what makes the false attribution the whole finding.

r26-A B1 | corpus/architecture/dependency_map.md:19 | UPHELD | IN-SCOPE | same predicate as r25-A B1 — the cited doc contains none of the attributed statement, at any line
   evidence: identical re-derivation to the row above, run independently (`git grep -F` per token plus a raw byte count). The citing sentence is on `dependency_map.md:19` (confirmed by `grep -n "storage.md:40,47"`), and the link target resolves to `corpus/services/storage.md` from `corpus/architecture/`. `git log -S "which is what its own doc says at" -- corpus/architecture/dependency_map.md` → one commit (`47c9b7d`, M257x/49), and `git log -S 'redis' -- corpus/services/storage.md` → **no commits**: the sentence storage.md is credited with has never existed in that file at any ref.

r25-A B2 | corpus/architecture/external_services.md:565, :598 | UPHELD | IN-SCOPE | cites `mistral.NewMistral(...)` at `app/internal/cms/studio/markdownManager.go:19`; that symbol exists nowhere in `app`
   evidence: `git -C stack-demo/app grep -n 'NewMistral' ad9f3c49` → **rc 1, 0 hits repo-wide** (control: `mistralocr` → 7 files incl. `internal/cms/studio/markdownManager.go`); the nested `stack-demo/app/studio` @ `aeec036a` → rc 1 as well. I read the file: at `ad9f3c49` `markdownManager.go:19` is a **doc-comment line** (`// It used to take aiKey and then IGNORE it, reading os.Getenv("MISTRAL_API_KEY")`); the live construct is `return &MarkdownManager{ocr: mistralocr.New(aiKey)}, nil` at **`:30`**, inside `func NewMarkdownManager` (`:29`), field `ocr *mistralocr.Client` (`:14`). **Not pinned:** the cell names no `app` ref — the `aeec036a` inside it is the studio-room repo's ref, bound to a `git -C app/studio grep` command, and a ref of another repo cannot date an `app/internal/**` claim; the sibling row `:567` in the same table *does* carry `@ \`app\` \`b948604\``, so the table pins where it means to. Unpinned ⇒ graded at the checkout, which is also `origin/main`. (It was exact at `b948604f`; and the string survives at `stack-demo/cms/internal/studio/markdownManager.go:19` in the frozen pre-merge **cms** repo — a different repo and a different path from the one the corpus names. r25-A's secondary anchor `:599` is off by one: the restatement is on `:598`.)

r26-A B2 | corpus/architecture/external_services.md:565, :598 | UPHELD | IN-SCOPE | same predicate — the named Go constructor is deleted at the graded ref and the anchor lands on a comment
   evidence: identical re-derivation, run independently. `grep -n "markdownManager.go:19" corpus/architecture/external_services.md` → `:565` (the Mistral provider row) and `:598` (Routing item 5), so both sites carry the dead anchor. `git -C stack-demo/app rev-parse HEAD` and `origin/main` are both `ad9f3c49`, so there is no HEAD-vs-origin ambiguity to hide behind. The enclosing predicate (*every Mistral use in `app` is OCR*) is unaffected — this is a named-construct defect, not a mechanism defect.

r25-B B1 | corpus/services/ai-readiness.md:429, :431, :445 | UPHELD | IN-SCOPE | three `readiness.go` anchors for the two cycle-filter branches name different functions at the graded ref
   evidence: `git -C stack-demo/app grep -n 'func (m \*Manager) …' ad9f3c49 -- internal/aireadiness/` puts `keepInCycleStep1` at **`:710`**, `keepStartedMembers` at **`:730`**, `queryInCycleStep1Completers` at **`:684`**, `audienceScope` at `:655`, `cycleAudienceTags` at `:637`. Reading the cited ranges: `readiness.go:662-664` is the body of **`audienceScope`** (`return "", nil, nil` / `ids, err := m.audienceUserIDsForTags(...)`), not `keepInCycleStep1`; `:684` is **`queryInCycleStep1Completers`** — the very function the sentence contrasts `keepStartedMembers` against; `:638-652` is `cycleAudienceTags` plus the `audienceScope` doc comment, not `queryInCycleStep1Completers`. **Not pinned:** `corpus/services/ai-readiness.md` carries no `app` sha anywhere (`grep -nE 'ad9f3c|b948604|2035f9a'` → 0; the only pins in the file are `next-web-app` `8297c684` at `:308`/`:605`), and the header's *"app v1.351.1"* dates the package refactor, not these anchors. r25-B cites `:432` for the `\`:684\`` anchor; it is on corpus line **`:431`** (`:432` is the trailing half of the sentence) — same claim, corrected here.

r25-B B2 | corpus/services/ai-readiness.md:436, :441 | UPHELD | IN-SCOPE | `steps.go:915-938` / `:907-914` name a sessions query, not `queryReadinessStarters` or its quoted comment
   evidence: read `internal/aireadiness/steps.go` @ `ad9f3c49`. `:911-921` is the doc comment for **`queryReadinessSimCompleters`**, whose `func` is at `:922` and whose SQL (`:928-935`) is `SELECT DISTINCT s.owner_id, ars.step_type FROM public.job_simulation_sessions s JOIN public.ai_readiness_sims ars …` — a different table and a different predicate from the `SELECT DISTINCT user_id FROM public.ai_readiness_user_step_progresses … AND status <> 'not_started'` the corpus quotes. `func (m *Manager) queryReadinessStarters` is at **`:964`**, its SQL at `:969-974`; the sentence *"This DB signal is the only real 'has started' check"* is at **`:962`**, not in `:907-914`. So the range cited as proof of a *correction* holds evidence for a different proposition. Unpinned, same as above.

r26-B B2 | corpus/services/ai-readiness.md:429, :431, :436, :441, :445 | UPHELD | IN-SCOPE | all five `internal/aireadiness/` anchors resolve but name the wrong constructs at `ad9f3c49`
   evidence: the union of the two re-derivations above, measured in one pass — `readiness.go` `:662-664`→`audienceScope` (`keepInCycleStep1` is `:710`), `:684`→`queryInCycleStep1Completers` (`keepStartedMembers` is `:730`), `:638-652`→`cycleAudienceTags`+doc comment (`queryInCycleStep1Completers` is `:684`); `steps.go` `:915-938`→`queryReadinessSimCompleters` (`queryReadinessStarters` is `:964`), `:907-914`→the wrong doc comment (the quote is `:962`). This seat's five corpus line numbers are all exact (verified with `grep -n` on the anchor strings). Booking it as one finding rather than five is a fair call on cause and repair; for predicate accounting I split it across the two claim boundaries below, because the readiness.go trio and the steps.go pair are different sentences about different files.

r26-B B1 | corpus/services/skiller.md:19 | UPHELD | IN-SCOPE | asserts a production terraform value the corpus elsewhere declares unmeasurable and no longer asserts
   evidence: measured the literal across all 15 git trees at each tree's own ref: `backend.internal.anthropos:8081` → **0 hits in any `*.tf`** (tf census re-derived: app 5, cms 4, graphql-wundergraph 4, jobsimulation 6, messenger 4, next-web-app 2, roadrunner 4, rosetta-extensions 2, sentinel 4, storage 5, studio-desk 4, ant-academy 0, platform 0 = **44** tracked). `app/knowledge/service-dependencies.md:52` @ `ad9f3c49` carries it in the **past** tense under *"There are no external callers of app's RPC mux left."* And the corpus contradicts itself with both sides asserted live: `corpus/services/backend.md:144` — *"**No terraform in the clone set names `http://backend.internal.anthropos:8081`, and this doc no longer asserts that any does.**"* — plus `cms.md:55` (*"The production address is not stated here in either direction"*), `cms.md:196` (*"no 'still names' claim can be made here in either direction"*) and `jobsimulation.md:50`, against `skiller.md:19`'s flat, unpinned, present-tense *"`skiller_rpc_addr = http://backend.internal.anthropos:8081` in production terraform"*. Not a retraction — `skiller.md` asserts, it does not retract. (`dependency_map.md:27` carries the same assertion; neither A seat booked it, so it is not counted here.)

r26-B B3 | corpus/services/cms.md:55, :196 | UPHELD | IN-SCOPE | "the literal's one occurrence anywhere in the clone set" is six, at the sentence's own denominator
   evidence: `git grep -F 'backend.internal.anthropos:8081' HEAD` per clone at its own ref returns **6** occurrences, not 1 — `stack-demo/app` 1 (`knowledge/service-dependencies.md:52`, the one the corpus names) and **`stack-demo/rosetta-extensions` @ the pinned `09d06070` 5**: `stack-core/tests/fixtures/repair_leak/{pre,post}/corpus/services/cms.md:32` and `:157` (four) plus `stack-core/tests/test_platform_predicate_guard.py:435`. The scope word is the sentence's own — it counts *"all 44 tracked `.tf` files in the 13 `stack-demo` repos"*, and `rosetta-extensions` is one of those 13: without its 2 `.tf` files the census is 42, not 44 (I re-derived the 44 per-repo, above). The load-bearing half — **0 hits in any `.tf`** — is exactly right, and the five extras are frozen copies of this corpus's own older text; but the passage is a derivation-showing one whose force is a cardinality, and the cardinality is wrong at the scope it names.

BOOKED=9 UPHELD=9 REJECTED=0 IN-SCOPE-UPHELD-BLOCKERS=9

## Predicate groups

- **storage-attribution**: r25-A B1 (corpus/architecture/dependency_map.md:19) + r26-A B1 (corpus/architecture/dependency_map.md:19) — `dependency_map.md` credits `storage.md:40,47` with a `depends_on`/`DB_CONNECTION`/`REDIS_ADDR`/`go.mod` statement that appears nowhere in `storage.md` at any line, and never has.
- **mistral-constructor**: r25-A B2 (corpus/architecture/external_services.md:565 + :598) + r26-A B2 (corpus/architecture/external_services.md:565 + :598) — the corpus names `mistral.NewMistral(...)` at `app/internal/cms/studio/markdownManager.go:19`; at the graded ref `ad9f3c49` the symbol is 0-hits repo-wide and the line is a comment.
- **readiness-cycle-filter-anchors**: r25-B B1 (corpus/services/ai-readiness.md:429, :431, :445) + r26-B B2 (same three anchors) — the three `readiness.go` anchors for `keepInCycleStep1` / `keepStartedMembers` / `queryInCycleStep1Completers` land on `audienceScope`, on `queryInCycleStep1Completers` and on `cycleAudienceTags` respectively at `ad9f3c49`.
- **steps-starter-anchors**: r25-B B2 (corpus/services/ai-readiness.md:436, :441) + r26-B B2 (same two anchors) — `steps.go:915-938` and `:907-914` name `queryReadinessSimCompleters`, a `job_simulation_sessions` query, not `queryReadinessStarters` (`:964`) or its quoted comment (`:962`). Booked separately from the group above because they are a different sentence about a different file with a different repair; r26-B B2 bundles both groups into one finding.
- **skiller-prod-terraform**: r26-B B1 (corpus/services/skiller.md:19) — a flat present-tense assertion of a production terraform value that `backend.md:144`, `cms.md:55`, `cms.md:196` and `jobsimulation.md:50` all declare unmeasurable and explicitly no longer assert.
- **cms-literal-cardinality**: r26-B B3 (corpus/services/cms.md:55 + :196) — *"the literal's one occurrence anywhere in the clone set"* is 6, five of them in `stack-demo/rosetta-extensions`, which the same sentence's 44-`.tf` denominator counts as one of its 13 repos.
