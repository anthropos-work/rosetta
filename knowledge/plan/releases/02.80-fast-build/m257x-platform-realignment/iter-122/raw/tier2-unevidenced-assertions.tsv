# TIER 2 — every factual assertion in the clause-5 surface that carries NEITHER a citation
# NOR a hedge, enumerated. UNEVIDENCED, never FALSE: many of these are certainly true.
# Scope of both citation and hedge is the containing BLOCK, so this is a FLOOR.
# columns: path:line-range <TAB> block-kind <TAB> sentence
corpus/services/TEMPLATE.md:13-13	bullet	**Database**: [Database] (e.g., Postgres, Redis)
corpus/services/academy-backend.md:30-31	bullet	**Primary Goal**: Be the server-authoritative owner of the AI Academy's content catalog and every learner's study state, served to the `ant-academy` frontend over the `app` GraphQL subgraph.
corpus/services/academy-backend.md:43-46	bullet	**Design invariant**: **additive-only** — every table is NEW with a child-side forward edge to `User`; the legacy `skillpath`/`assignment`/`user_*` schemas are untouched.
corpus/services/academy-backend.md:43-46	bullet	Per-user tables carry `UserMixin` (owner-only privacy; every read/write is self-only and fails closed for unauthenticated callers).
corpus/services/academy-backend.md:43-46	bullet	Catalog tables have NO `UserMixin` (global content); their gating is a manager/resolver concern.
corpus/services/academy-backend.md:50-51	bullet	**Codebase**: `app/internal/academy/`.
corpus/services/academy-backend.md:50-51	bullet	Go, inside the `app` (backend) service.
corpus/services/academy-backend.md:50-51	bullet	Persistence via Ent into the **`public`** Postgres schema (`academy_*` tables).
corpus/services/academy-backend.md:65-78	bullet	**Ent tables** (`internal/data/ent/schema/`, `public` schema): *   **Per-user (UserMixin, owner-only)** — ⚠️ **these are Ent LABELS; the TABLE names are plural** (Ent pluralizes, so `SELECT … FROM academy_certificate` errors with *relation does not exist*): `academy_chapter_progresses` (jsonb completed-modules/sections; `UNIQUE(user_id, chapter_slug)` — the hottest dataset), `academy_last_activities` (per-user singleton resume pointer), `academy_chapter_times` (completion/reinforcement active-seconds split), `academy_certificates` (immutable; `cert_id` unique `AAS-YYYY-XXXXXX`; nullable `learner_email` never public), `academy_bookmarks` (kind-discriminated; global `external_id` idempotency key), `academy_feedbacks` (one editable row per (user, scope, target)).
corpus/services/academy-backend.md:65-78	bullet	*   **Global catalog (no UserMixin):** `academy_series`, `academy_skill_paths` (`paid` default true — the tier lives here; nullable `tenant_eid`; `lifecycle`), `academy_chapters` (metadata; own `tenant_eid`), `academy_chapter_bodies` (one row per `(chapter_slug, locale)`; `body` jsonb = {meta, modules[], references[]}; EN base + de/es/fr/it/nl/pt overlays), `academy_path_embeddings` (pgvector `extensions.vector(1536)`, `source_hash` skip-key).
corpus/services/academy-backend.md:79-81	bullet	**Certificate minting** (`certificate.go`): id `AAS-YYYY-XXXXXX` (`AAS` = "Anthropos Academy Skill"), a 6-char suffix from a 30-symbol ambiguity-free alphabet via `crypto/rand`.
corpus/services/academy-backend.md:79-81	bullet	Public verify validates the id **before** any DB lookup and maps to a minimized PII-free struct (never `learner_email`; empty name → "AI Academy Learner").
corpus/services/academy-backend.md:103-104	bullet	**Background task**: the nightly Asynq **`academy_embedding_refresh` @ 03:00** materializes `academy_path_embeddings` (skip via `source_hash = sha256(model | embed_text)`).
corpus/services/academy-backend.md:141-141	bullet	**Service Name**: none of its own — part of the `backend` service (`docker compose up -d backend`).
corpus/services/academy-backend.md:148-152	paragraph	Unit tests use an **in-memory SQLite (`enttest`) harness** (`sqlite_test.go`) — the academy Manager is pure Ent, so no real Postgres is needed (the one Postgres-only feature, `SELECT … FOR UPDATE`, is `t.Skip`'d under SQLite).
corpus/services/academy-backend.md:148-152	paragraph	Notable: `certificate_mint_test.go` (cert-id contract over 500 mints + PII rejection); `entitlement_parity_test.go` (cross-repo parity with the FE `getEntitlement`/`serverTenant.js` gates); `merge_test.go`/`time_test.go`/ `progress_aggregate_test.go` (monotonic-merge + keep-max-by-event-time semantics).
corpus/services/ai-labs.md:3-9	paragraph	**Not a standalone service — a set of `app`-monolith domains** (the service the platform calls "backend").
corpus/services/ai-labs.md:3-9	paragraph	**There IS a separate repo, and this doc used to deny it** (v2.8 M257x): `anthropos-work/AI-Labs` is the live Go control plane the sections below already call **`labs-api`** — the thing behind `anthroposlabs.com` that owns the Firecracker microVM lifecycle.
corpus/services/ai-labs.md:3-9	paragraph	What is in `app` is the *consumer* half.
corpus/services/ai-labs.md:3-9	paragraph	Code lives under `app/internal/labs/`, `app/internal/credits/`, `app/internal/payments/`, `app/internal/subscriptions/`, plus the top-level `app/stripe/` fixtures.
corpus/services/ai-labs.md:25-26	paragraph	Two independent concerns absorbed into `app`, joined today only by a shared *reuse target* (the credit ledger), not yet by code:
corpus/services/ai-labs.md:28-33	bullet	A user launches an ephemeral cloud sandbox (a microVM running an in-VM coding agent — Claude Code or OpenAI "codex") to work a lab scenario, metered in **USD per session**.
corpus/services/ai-labs.md:28-33	bullet	`app` owns the **tenant-facing view** (catalog content + session identity/budget/status/billing record + org spend caps); a **separate control plane, `labs-api`**, owns the microVM lifecycle, the per-session LiteLLM virtual key, and the live spend numbers.
corpus/services/ai-labs.md:44-45	bullet	**Codebase**: `app/internal/{labs,credits,payments,subscriptions}/` + `app/stripe/` (fixtures).
corpus/services/ai-labs.md:44-45	bullet	Go, part of the `app` monolith.
corpus/services/ai-labs.md:44-45	bullet	DB: PostgreSQL, **`public` schema**, Ent + Atlas.
corpus/services/ai-labs.md:48-51	bullet	`catalog.Manager` (slug catalog domain) + `ContentManager` (viewer-facing, **fail-closed** tenant-filtered reads; `ErrNotFound` is a hard-404 no-existence-oracle), `content_import.go` (idempotent manifest upsert), `s3_workspace_store.go` (S3-backed workspace/grader/solution assets — the solution is private, never served to learners).
corpus/services/ai-labs.md:52-55	bullet	**Tables**: `labs` (Ent `lab.go` — metadata: `slug`, `tenant_eid` nullable = org-scoped else public, `lifecycle`, `base_image`, `default_model`, `budget_usd`, S3 asset refs) and `lab_bodies` (Ent `lab_body.go` — one row per `(lab_slug, locale)`; EN base + 6 overlays; `{meta,brief,tutor,hints}` as one jsonb).
corpus/services/ai-labs.md:52-55	bullet	Migration `20260617203555_add_labs_catalog.sql`.
corpus/services/ai-labs.md:58-64	bullet	`labsession.Server` implements the `lab.v1.LabSessionService` Connect-RPC (Create/Get/List/Cancel/ReportEvent); `Manager` holds the business logic the GraphQL resolvers call directly.
corpus/services/ai-labs.md:58-64	bullet	`labs_budget.go` is the **cap/tier engine**: `LabsTier` (`essential`/`professional`/`frontier`) → `modelsForTier`, `LabsBudgetOptions` stored as JSON on `OrganizationSettings.Options[labs_budget]`, `CheckRequest` → typed `CapError{Reason}` (`model_not_in_tier`, `per_session_cap`, `org_total`, `user_monthly`, `user_lifetime`) → Connect `ResourceExhausted`.
corpus/services/ai-labs.md:58-64	bullet	`spend_reconciler.go` is a **30 s pull loop** that denormalizes `spend_usd`/`total_tokens`/`status` from labs-api into `lab_sessions` rows (+ an orphan-fail-out pass).
corpus/services/ai-labs.md:67-70	bullet	**Table**: `lab_sessions` (Ent `lab_session.go`) — deliberately **no `PrimaryKeyMixin`**: `id` is a **String** supplied by labs-api.
corpus/services/ai-labs.md:67-70	bullet	`user_id`, nullable `organization_id`, frozen `template` (= `Lab.slug`), `model`, `mode`, `status` (booting/ready/grading/stopped/failed/cancelled), immutable `budget_usd`, denormalized `spend_usd`/ `total_tokens`, `grade_result` jsonb.
corpus/services/ai-labs.md:67-70	bullet	Migrations `20260529072659`, `20260617120000`.
corpus/services/ai-labs.md:73-76	bullet	`credits.Manager`: `Balance` (lazy-seeds `organization_credits` at `DefaultSeedBalance=500` on first read), `Debit` (atomic conditional decrement + negative ledger row; `InsufficientCreditsError` → **HTTP 402**), `Refund` (positive row, **idempotent on `ref_id`**), `Purchase`, `Transactions` — every mutation in one `ent.WithTx` under `privacy.Allow`.
corpus/services/ai-labs.md:77-81	bullet	`cost.go` — the cost model: `creditCost` map (`course.build`=**5/chapter**, `course.refine`=**1**, `course.translate`=**1/locale**), `purchasePackages` (starter 50 / team 200 / scale 500), `PricePerCreditUSD` = **$0.45** (derived from Bedrock COGS × `MarginMarkup` **1.40**).
corpus/services/ai-labs.md:77-81	bullet	`reconcile.go` — `ReconcileOrphanedBuilds` (refunds crashed pre-terminal Course Builder builds, idempotent) + `ReconcileOrg`/`MarginAlert` (internal per-org realized-margin; never served on customer routes).
corpus/services/ai-labs.md:82-85	bullet	**Tables**: `organization_credits` (Ent `organization_credit.go` — per-org `balance`, UNIQUE on `organization_id` = the one-purse-per-org invariant) and `credit_transactions` (Ent `credit_transaction.go` — append-only: `action_type`, signed `credits_delta`, `ref_id`, `meta`; a **partial-unique** `(organization_id, action_type, ref_id) WHERE ref_id IS NOT NULL` = the idempotency guard).
corpus/services/ai-labs.md:88-94	bullet	`payments.Manager` (`stripe-go/v74`): `CreateCustomer`, `CreateCustomerCheckoutSession` (**subscription mode**, price by `lookup_key`, 7-day trial), portal session, `ParseEvent` (webhook signature via `STRIPE_ENDPOINT_SECRET`), `HandleStripeSubscriptionEvent` (upserts user `subscriptions`), `emitMetaPurchase` (Meta Conversions API).
corpus/services/ai-labs.md:88-94	bullet	`subscription_verifier.go` implements `organization.StripeSubscriptionVerifier` so **org seat licensing derives seats from the real Stripe quantity**, not client input.
corpus/services/ai-labs.md:88-94	bullet	`subscriptions.SubscriptionManager` handles user premium subs + **feature-usage quotas** (cover-letters / job-applications / job-simulations) via Sentinel.
corpus/services/ai-labs.md:95-96	bullet	**Tables**: `subscriptions` (user-scoped), `stripe_customers`, `org_subscriptions` (org seat licensing: `licenses`/seats, `source` = stripe|manual, Stripe ids, period).
corpus/services/ai-labs.md:95-96	bullet	Migration `20260626120000_add_org_subscriptions.sql`.
corpus/services/ai-labs.md:125-127	bullet	**Prerequisites**: runs **as part of `app`** (`go run .` / Docker).
corpus/services/ai-labs.md:125-127	bullet	Labs wire up only when `LABS_API_URL` is set (else the labs-api client is nil → in-memory idgen, reconciler no-op); credits seed lazily; payments need Stripe keys.
corpus/services/ai-labs.md:128-130	bullet	**Key env vars**: labs — `LABS_API_URL`, `LABS_API_PLATFORM_TOKEN`, `LABS_WORKSPACE_BUCKET`, `LABS_WORKSPACE_PREFIX`, `AWS_REGION`; Stripe — `STRIPE_SECRET_KEY`, `STRIPE_ENDPOINT_SECRET`; Meta — `META_PIXEL_ID`, `META_CONVERSION_API_TOKEN` (self-disables when unset).
corpus/services/ai-labs.md:134-135	bullet	**Dedicated cmds**: `cmd/labsimport` (idempotent catalog manifest loader), `cmd/labskey` (mint an org API key for `labs:write`), `cmd/labsdemo` (standalone local Labs-catalog server on `:8099`, in-memory store, dev only).
corpus/services/ai-labs.md:138-138	bullet	**Service Name**: none of its own — part of the `backend` service (`docker compose up -d backend`).
corpus/services/ai-labs.md:146-149	bullet	**Credits**: `cost_test.go` (`TestPricingDerivation` guards the $0.45/40 % derivation), `manager_test.go` (Balance/Debit/Refund/Purchase + concurrency + lazy-seed), `privacy_backstop_test.go` (`TestPrivacyBackstop_AutoScopesToCallerOrg` — proves the `OrganizationMixin` fences cross-org reads), `reconcile_orphan_test.go`.
corpus/services/ai-labs.md:150-151	bullet	**Subscriptions**: `subscriptions_test.go`.
corpus/services/ai-readiness.md:3-11	paragraph	**Package-refactor refresh 2026-07-23 (v2.7 "july jitter" M247)** — see the ⚠️ callout below: the domain moved out of `app/internal/workforce/` into its own **`app/internal/aireadiness/`** package (app `v1.351.1`), the archetype **scoring bands changed**, and several net-new subsystems landed (notifications, email overrides, one-click provisioning, auto-close, a recommendation engine).
corpus/services/ai-readiness.md:3-11	paragraph	The feature ships in **`app` v1.315+** (backend) + **`next-web-app` v2.89.0+** (UI).
corpus/services/ai-readiness.md:74-79	paragraph	It is a subsystem of the **`app` (backend) service** — its own package **`app/internal/aireadiness/`** (split out of `internal/workforce/`; see the ⚠️ package-refactor callout above) — not a standalone microservice.
corpus/services/ai-readiness.md:83-84	paragraph	The feature is off until an org turns it on. Two gates exist — but **they do not both apply to every surface**, and assuming they do sends you hunting for a PostHog problem the manager dashboard does not have:
corpus/services/ai-readiness.md:175-175	table-row	| 1 | `skill_mapping`    | self-map AI skills (framework modal) | **30** | ≥1 `user_skill_evidences` row for a skill node_id in the org's `ai_readiness_skills` set (presence-based; level ignored) |
corpus/services/ai-readiness.md:176-176	table-row	| 2 | `simulation`       | a job-simulation session | **40** | best ended/scored jobsim session whose sim_id ∈ `ai_readiness_sims` (`step_type='simulation'`); `(raw_score/100)×40` |
corpus/services/ai-readiness.md:177-177	table-row	| 3 | `interview`        | an interview (a sim, different config) | **30** | best ended/scored session whose sim_id ∈ `ai_readiness_sims` (`step_type='interview'`); `(raw_score/100)×30` |
corpus/services/ai-readiness.md:188-192	paragraph	The Step-2 sim resolves per-user by track (`resolveUserTrack`, business-wins); the interview (Step-3) is shared across tracks (`track = "both"`).
corpus/services/ai-readiness.md:188-192	paragraph	`defaultReadinessSims` is 3 entries: `{simulation, "tech", …}`, `{simulation, "business", …}`, `{interview, "both", …}`.
corpus/services/ai-readiness.md:188-192	paragraph	Cycles also gained a `launched_by`.
corpus/services/ai-readiness.md:194-197	paragraph	Per-step status lives in `ai_readiness_user_step_progresses` (`not_started`/`in_progress`/`completed`, `completed_at` set once, never re-updated).
corpus/services/ai-readiness.md:201-201	paragraph	All tables live in `app` (`public` schema); ent schemas under `app/internal/data/ent/schema/`:
corpus/services/ai-readiness.md:205-205	table-row	| `ai_readiness_cycles` | a diagnostic cycle per org | `status: active\|closed`, `start/end_date`, `final_score`, `closed_at`; **one active cycle per org** (partial unique index) |
corpus/services/ai-readiness.md:206-206	table-row	| `ai_readiness_steps` | org's ordered 3-step plan | `step_type`, `position`; default = all 3 canonical if no rows |
corpus/services/ai-readiness.md:207-207	table-row	| `ai_readiness_skills` | org's AI-skill set | `node_id` (taxonomy), `weight` (1.0 core / 0.5 enabling) — Step 1 scoring |
corpus/services/ai-readiness.md:208-208	table-row	| `ai_readiness_sims` | org's sim registry | `step_type` (simulation/interview), `sim_ref` (Directus sim id or `PLACEHOLDER-{slug}`) — Steps 2/3 |
corpus/services/ai-readiness.md:209-209	table-row	| `ai_readiness_user_step_progresses` | per-(org,user,step) progress (**plural** — the ent-generated table name; M219) | `status`, `completed_at` |
corpus/services/ai-readiness.md:210-210	table-row	| `ai_readiness_live_snapshots` | **live** per-member score (mutable, upserted) | `score/knowledge/usage/archetype/stage/...`; exposed to Talk-to-Data SQL |
corpus/services/ai-readiness.md:211-211	table-row	| `ai_readiness_snapshots` | **frozen** per-(cycle,user) snapshot at close (immutable) | `frozen_*` mirror of live |
corpus/services/ai-readiness.md:212-212	table-row	| `ai_readiness_diagnose_narratives` | persisted per-member LLM narrative | keyed `(org,user,cycle_ref,lang)` + `signals_hash` |
corpus/services/ai-readiness.md:213-213	table-row	| `ai_readiness_text_translations` | content-addressed translation cache | `source_hash`+`lang` |
corpus/services/ai-readiness.md:214-214	table-row	| `ai_readiness_recommendations` | per-member recommended actions (the What-to-do-next drawer's "Recommended actions") | **was missing from this doc until M219**; the demo seeds **0 rows** — the live read derives `people[].diagnosis.recommendations` instead |
corpus/services/ai-readiness.md:215-215	table-row	| `ai_readiness_email_overrides` (**net-new, M408**) | per-org email-copy override | one row per `(organization, email_type)`; `OrganizationMixin`-scoped; backs the workforce-admin email PUT/GET/DELETE + the preview renderer |
corpus/services/ai-readiness.md:216-216	table-row	| `ai_readiness_notification_logs` (**net-new, M400**; plural — the ent-generated table name) | notification send log | the invitation/reminder/launch/digest lifecycle audit |
corpus/services/ai-readiness.md:217-217	table-row	| `ai_readiness_notification_optouts` (**net-new, M400/M403**; plural — the ent-generated table name) | per-member unsubscribe | the reminder-cadence opt-out |
corpus/services/ai-readiness.md:218-218	table-row	| `organization_settings` (existing) | the enablement gate | `setting='ai_readiness'`, `is_enabled` |
corpus/services/ai-readiness.md:219-219	table-row	| **`public.interview_aggregated_reports`** | **the org's Step-3 interview AGGREGATE — the SOLE source of all four "AI Interview — breakdown" blocks** | `(organization_id, sim_id, report JSONB, session_count)`.
corpus/services/ai-readiness.md:237-237	table-row	| `catalog_kpis[]` `{id, value}` | **"How they use AI — at a glance"** (5 tiles) | ids `avg_adoption` / `avg_transformation` / `avg_originality` / `avg_depth` / `avg_ownership` (Adoption / Transformation / Originality / Depth·Creation / Critical ownership), each a **0-100 cohort average** (the M219 ids `avg_frequency` / `avg_breadth` / `avg_context_fit` were retired in the post-M246 platform rename — see *part 5* of § *The FILLED-ness contract* below).
corpus/services/ai-readiness.md:237-237	table-row	`usageDimensionsFromReports` **omits** any KPI that is absent or non-numeric — **omitting all of them is why the tile row did not render at all**, rather than rendering empty. |
corpus/services/ai-readiness.md:238-238	table-row	| `narrative.patterns[]` | **Strengths** | `evidence[0]` **IS** the rendered verbatim quote; `source_session_id` is what `resolveSessionAuthors` joins (`sessions → memberships`) to hydrate the quote's **author name + job role**. |
corpus/services/ai-readiness.md:239-239	table-row	| `narrative.unexpected[]` | **Unexpected angles** | **NO chart fallback exists** — the narrative is the only way this column can *ever* render. |
corpus/services/ai-readiness.md:240-240	table-row	| `narrative.insights[]` where `category` contains **`"risk"`** | **What holds them back** | The category string is **load-bearing**: `holdsBackFromInsights` filters on it. Get it wrong and the column silently empties again. |
corpus/services/ai-readiness.md:241-241	table-row	| `catalog_charts[]` `top_concerns` / `top_unexpected` | the **no-narrative fallbacks** | Back What-holds-them-back / Strengths when the LLM narrative is absent.
corpus/services/ai-readiness.md:246-248	bullet	every `source_session_id` / `session_ids` entry is a **REAL seeded Step-3 session id**, so quote attribution resolves to a **real seeded member** through the platform's own join — never a fabricated id, never a quote from nobody;
corpus/services/ai-readiness.md:256-256	bullet	`session_count` is the true number of seeded interviews;
corpus/services/ai-readiness.md:259-262	paragraph	The narrative prose itself is **code-owned demo copy** (like `aiReadinessInterviewPrompts`) — what a real aggregation LLM would have synthesised.
corpus/services/ai-readiness.md:259-262	paragraph	Fenced by `ai_readiness_interview_report_test.go`, which decodes the seeded row **through the platform's own contract** (transcribed structs), because *"the seeder wrote a row"* is not the proposition that matters — *"the row makes the four blocks render"* is.
corpus/services/ai-readiness.md:275-279	bullet	**REST/workforce API** (`app/internal/web/backend/api/`): `GET /api/workforce/ai-readiness` (→ the `AIReadinessResponse` aggregate the manager dashboard consumes), `/cycles` (GET/POST) **+ `/cycles/{cycleID}` (GET) + `/cycles/{cycleID}/close` (POST)**, `/steps-completion`, `/narrative` (POST, LLM diagnosis), `/compare`, `/export.csv`, and **net-new at the M247-refresh refactor: `/setup` (GET status + POST one-click provision)** plus the **M407/M408 email-preview + email-override admin** endpoints.
corpus/services/ai-readiness.md:280-281	bullet	**Background:** live-snapshot refresh (`RefreshLiveSnapshots`, worker task) **+ the net-new auto-close scheduler `CloseDueAIReadinessCycles`** (sweeps active cycles past `end_date`) + the notification workers (reminder/digest).
corpus/services/ai-readiness.md:285-286	paragraph	The refactor did more than move files — it consolidated several net-new platform subsystems that had **no prior corpus coverage**.
corpus/services/ai-readiness.md:285-286	paragraph	Documented here as platform facts; the demo-seeder consequences are v2.7 **M250** work.
corpus/services/ai-readiness.md:316-317	bullet	**The auto-close scheduler** — `CloseDueAIReadinessCycles` sweeps active cycles past `end_date` (cross-org under `privacy.Allow`).
corpus/services/ai-readiness.md:339-339	table-row	| **Manager** | **`AIReadinessClient`** — HeroCard (org score + dominant archetype + **Steps-Completion %**) + tabs **Snapshot** (archetype matrix + donuts + by-tag), **How-we-measure** (3-step ribbon + skill strengths/gaps + sims + **interview findings**), **What-to-do-next** (archetype action groups + per-person **Diagnose** drawer).
corpus/services/ai-readiness.md:341-341	table-row	| **Employee** | `AIReadinessHero` (the 3-step funnel; modes new/progress/done/archived) + `AIReadinessRailCard`.
corpus/services/ai-readiness.md:367-368	bullet	The `(new)` in the legacy path is a Next.js **route group** for the workforce refactor — *not* a version marker.
corpus/services/ai-readiness.md:396-400	paragraph	Enable for an org by inserting the `organization_settings` row (`setting='ai_readiness', is_enabled=true`) + `flag_ai_readiness` on in PostHog (or the local flag shim).
corpus/services/ai-readiness.md:396-400	paragraph	The member flow then needs `ai_readiness_skills` + `ai_readiness_sims` config + an active cycle; the dashboard reads `GET /api/workforce/ai-readiness`.
corpus/services/ai-readiness.md:396-400	paragraph	Tests: `app/internal/aireadiness/*_test.go` (the scoring/steps/cycle suites moved with the package; Postgres harness `testdb_test.go` via `internal/testsupport/pgtest`).
corpus/services/ai-readiness.md:404-407	paragraph	To make a **200-person demo org** show the AI-readiness manager dashboard **enabled**, with **78.4% (≈156 of the 199 frozen snapshots) having completed all 3 steps** (the shipped figure — see `seeding-spec.md`; this supersedes the earlier round "~80%/≈160" contract prose), plus **one hero "started"** and **one hero "completed"** — the seeder writes:
corpus/services/ai-readiness.md:410-410	bullet	`organization_settings` (`setting='ai_readiness'`, `is_enabled=true`) — the gate.
corpus/services/ai-readiness.md:411-411	bullet	`ai_readiness_steps` × 3 (skill_mapping/simulation/interview, positions 0/1/2) — optional (canonical default if absent).
corpus/services/ai-readiness.md:417-426	bullet	`ai_readiness_sims` × **3 track-keyed** (M250; was 2) — **two distinct-track simulations** (tech=`who-can-see-this-document-fc0`, business=`use-ai-to-turn-survey-data-into-a-leadership-email`) **+ one shared interview** (`ai-readiness-interview-d62`), each pinned to the platform's real default sim uuid, with the `track` column WRITTEN (the schema's `UNIQUE(org, step_type, track)` requires the tech+business pair — both `step_type=simulation` — to carry DISTINCT tracks).
corpus/services/ai-readiness.md:417-426	bullet	**Plus a net-new Directus set-dress** (`AIReadinessSimSkillsSeeder`) that resolves each wired sim's `directus.sequences.evaluation_skills` node-ids → `public.skills.name` and UPDATEs `directus.simulations.skills` for the 3 uuids — this is what fills step-2's **evaluated-skills list** and drives the tech/business label **name-heuristic** (`directus.simulations.skills` is genuinely NULL in capture, so replay alone leaves it empty — snapshot replay is replay-only, hence a net-new write step).
corpus/services/ai-readiness.md:435-438	paragraph	**Per-member (≈156 of 199 "completed"):** the underlying signals (≥1 `user_skill_evidences` for a configured skill; jobsim sessions for steps 2/3) **+** `ai_readiness_user_step_progresses` (3× `completed`) **+** an `ai_readiness_live_snapshots` upsert (`score≈100, stage=3, archetype` per the score).
corpus/services/ai-readiness.md:440-445	paragraph	**⚠️ A hero's stage is DERIVED FROM HER PERSONA KIND — and stage 0 was UNREACHABLE for an end-user until v2.8 M256 iter-26.** `aiReadinessStageFor` maps a hero: **manager → 0** (she reads the dashboard, she is not a funnel row), **struggling → 1**, and **everything else → 3**.
corpus/services/ai-readiness.md:447-450	bullet	**`Persona.AIReadiness`** — `""` (derive from trajectory, unchanged) | **`"not_started"`** (funnel **stage 0**: no evidence, no sessions, no progress row, no snapshot — `seedAIReadinessOrgFunnel` skips her entirely).
corpus/services/ai-readiness.md:456-457	bullet	An end-user hero must still declare `verified > 0` (`blueprint.validate()`), which is the same point from the other side: the field does not make a hero **blank**, it makes her **not-yet-diagnosed**.
corpus/services/ai-readiness.md:512-512	bullet	**cycle-scoped** — `opts.CycleID != nil` AND that cycle's `status == "closed"` (`:290-297`);
corpus/services/ai-readiness.md:513-514	bullet	**default (`CycleID == nil`)** — when the org has **no active cycle** and a **latest closed cycle** exists (`:307-312`).
corpus/services/ai-readiness.md:513-514	bullet	Only an org with neither falls through to `buildLiveResponse` (`:314`).
corpus/services/ai-readiness.md:579-579	table-row	| the **STARTED** hero | 1 / 3 | `progress` | **NOTHING.** `AIReadinessHero.tsx:88` `if (!air.deadline) return null;` |
corpus/services/ai-readiness.md:581-584	paragraph	The active cycle's `end_date` **must be in the future** (it *is* the member deadline) and its `participants_filter` must stay `{"all":true}` (a tag-scoped cycle returns a nil deadline for anyone outside the tags, silently re-hiding the surface for most of the org).
corpus/services/ai-readiness.md:589-592	paragraph	**What the manager then reads — and why that is the point.** With an active cycle present, `AIReadinessClient`'s `activeCycle?.id ?? latestClosedCycle?.id` resolves the **active** id, so the dashboard takes `buildLiveResponse`.
corpus/services/ai-readiness.md:596-596	table-row	| `howWeMeasure.interview` — **null** → present | the whole **Step-3** block + **"How they use AI"** + **"What holds them back"** + **"Strengths"** + **"Unexpected angles"** |
corpus/services/ai-readiness.md:600-602	paragraph	**Reported, not gated** — the milestone that owns login speed is a different one. Both paths fill `org.*`, `byTeam` (13), `people` (199), `howWeMeasure.{steps,skillInsights,simulations,cycleTotals}`.
corpus/services/ai-readiness.md:619-621	paragraph	**Full marks require holding EVERY configured skill.** The denominator is the whole repertoire, by design — "a larger configured set makes a full score harder to reach" (the platform's own comment).
corpus/services/ai-readiness.md:634-643	paragraph	**2. The readiness sims must be RESERVED — or an unrelated session silently scores a member.** The platform scores Steps 2/3 from **any** ended session whose `sim_id` is in the org's `ai_readiness_sims` set. It does **not** consult the step-progress row. The generic session seeders hash their `sim_id` out of the *same* ~50-id replayed content pool the readiness config draws from — so a member's **unrelated activity session** could land on the readiness sim by coincidence and score them against a step they never took.
corpus/services/ai-readiness.md:634-643	paragraph	That is exactly what happened to the STARTED hero: his funnel row said `interview: not_started` while the backend read an interview signal (score 21) off a stray activity session.
corpus/services/ai-readiness.md:634-643	paragraph	The two readiness refs now come from a **reserved tail** of the sims pool that no general picker can draw (`contentref.go`), making the sets **provably disjoint**.
corpus/services/ai-readiness.md:669-674	paragraph	**Also (a latent hazard closed while scaling #1):** the funnel's Step-1 evidence UPSERT is now **presence-preserving** — on a conflict with a row the verified-skill chain already wrote it asserts only that the row exists and is verified, and leaves `level` / `anthropos_level` / `user_level` alone.
corpus/services/ai-readiness.md:669-674	paragraph	Step 1 is presence-based (`queryUserAISkills` selects only `user_id, skill_id, is_verified`), so preserving is both correct and safer: with a member now mapping up to **31** skills (M250), the old clobbering upsert would have let the readiness seeder quietly restate a hero's claimed-vs-verified gap.
corpus/services/ai-readiness.md:676-686	paragraph	A net-new `ai_readiness_evidence.go` seeder distributes each completed member's sim **evaluated** node-ids as: one `validation_attempt_results` + one `validation_attempt_skill_results` per evaluated node-id (the score `computePerformanceBySkill` reads for the manager's dot ratings) + one **session-backed verified** `user_skill_evidences` per node-id (the profile's verified skill).
corpus/services/ai-readiness.md:676-686	paragraph	The evaluated node-ids are read **closure-safe** from `directus.sequences.evaluation_skills` JOINed to `public.skills` (unresolved ids drop, never fabricated — the same truthful source as the set-dress).
corpus/services/ai-readiness.md:676-686	paragraph	On a cold reset-to-seed this moved AI-sim `validation_attempt_results` **5 → 345** (+ 897 `validation_attempt_skill_results`, 787 session-backed verified evidences), flipping both the member-profile verified-skill distribution AND the manager per-skill dots to render real.
corpus/services/ai-readiness.md:688-696	paragraph	**5. Interview-findings must key on the CURRENT KPI ids (M250 iter-07).** The manager's "interview findings" tiles key on the platform's live `usageDimSpecs` — **avg_adoption / avg_transformation / avg_originality / avg_depth / avg_ownership** (Adoption / Transformation / Originality / Depth·Creation / Critical ownership).
corpus/services/ai-readiness.md:688-696	paragraph	The M219 seeder wrote the now-retired ids `avg_frequency / avg_breadth / avg_context_fit`; only `avg_depth` survived the post-M246 platform rename, so 4 of 5 tiles rendered empty.
corpus/services/ant-academy.md:5-5	paragraph	It delivers micro-chapters on AI engineering, Claude Code, agent frameworks, and related topics to anyone with an `@anthropos.work` email.
corpus/services/ant-academy.md:9-9	bullet	A companion **iOS / Android app** (Expo / React Native) that bundles the same content for offline reading
corpus/services/ant-academy.md:21-21	table-row	| **Technology Stack** | Next.js 16 App Router + React 19.2 (React Compiler enabled), Expo / React Native (mobile) |
corpus/services/ant-academy.md:22-22	table-row	| **Deployment** | **Vercel native** (no Docker, no docker-compose entry).
corpus/services/ant-academy.md:22-22	table-row	Mobile builds via Expo. |
corpus/services/ant-academy.md:24-24	table-row	| **Authentication** | Clerk (`@anthropos.work` domain gate + org-membership gate) |
corpus/services/ant-academy.md:25-25	table-row	| **Repository** | `git@github.com:anthropos-work/ant-academy.git` → `stack-dev/ant-academy/` |
corpus/services/ant-academy.md:32-37	bullet	**Key Functions**: - Serve chapter content as a Next.js App Router site at `/chapters/<slug>/` - ~~Cache chapters offline via a Serwist-built service worker~~ — **REMOVED at v0.5 M1.** `RegisterServiceWorker.jsx` is now a kill-switch that unregisters any surviving worker and deletes its caches; the web app is online-only - Bundle the same chapter JSON into the iOS / Android Expo app at build time - Provide an opt-in in-app AI assistant ("Cosmo", behind a feature flag) that talks to OpenAI directly from the browser - Author / publish / benchmark content via repo-local Claude skills (`.claude/skills/author-chapter`, `author-skill-path`, `author-podcast`, `author-cover`, `benchmark-chapter`, `build-index`, …)
corpus/services/ant-academy.md:59-59	paragraph	The **React app's** env lives at `code/.env.example` (Clerk + AI keys); the **repo-root `.env`** is only for authoring-side tooling (`AUTOCONTENT_API_KEY`, `OPENAI_API_KEY` for cover generation).
corpus/services/ant-academy.md:63-63	paragraph	Ant Academy is architecturally a **sibling of `studio-desk` and `next-web-app`** — a frontend product that **reuses platform identity** and is a **backend-authoritative read/WRITE GraphQL client** of the platform `app`'s **`backend`** subgraph — **there is no separate "academy subgraph"**; the academy types are one SDL file (`academy.graphqls`, 1 of 43) inside it. It has no backend of its own, but it does call one: it **reads** the catalog (below) and, since **v0.5 "direct line" M2**, **writes** per-user progress to the platform backend (chapter progress, last-activity, bookmarks, certificates, study-time, feedback) — the platform `app internal/academy` store is the sole source of truth (there is NO localStorage/IDB source-of-truth).
corpus/services/ant-academy.md:63-63	paragraph	The earlier "does not call backend services / read-only client" framing is retired (corrected v2.5 M231): progress persists via GraphQL mutations (`upsertChapterProgress[Batch]` / `setLastActivity`) to Ent tables `academy_chapter_progresses` / `academy_last_activities` / … in `app` (**plural** — Ent pluralizes; the singular forms are schema-file names, not table names).
corpus/services/ant-academy.md:119-119	bullet	No PostgreSQL schema of its own, no Atlas migrations
corpus/services/ant-academy.md:120-120	bullet	No Connect-RPC, no Redis Streams
corpus/services/ant-academy.md:121-124	bullet	**Provides** no GraphQL subgraph (it doesn't federate into the supergraph, which is `backend` alone) — but it **consumes** the platform's **`backend` subgraph** as a GraphQL *client* (see below); "no subgraph" ≠ "no GraphQL".
corpus/services/ant-academy.md:121-124	bullet	**There is no "academy subgraph" to consume either** — the academy types live inside `backend`; this bullet named one until M257x iter-108
corpus/services/ant-academy.md:125-127	bullet	Its rendered catalog is **NOT static repo JSON** — since v0.5.1 it is **DB-authoritative**, read from the platform academy backend over GraphQL.
corpus/services/ant-academy.md:129-131	paragraph	The platform-shared concerns are **Clerk** (identity — Ant Academy reuses the platform's Clerk app so engineers log in with the same identity) **and the academy backend** (`app internal/academy`, queried over GraphQL for the course catalog).
corpus/services/ant-academy.md:135-138	paragraph	**The home grid does NOT read the committed JSON.** Since ant-academy **v0.5.1 (M7)** the catalog the portal renders is **DB-authoritative** — queried from the **platform `backend` subgraph** (the academy types are served by `app`'s `internal/academy`; there is no separate academy subgraph) over GraphQL, not from the repo's JSON files.
corpus/services/ant-academy.md:169-170	bullet	**Endpoint unset** — `NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT` empty → `createServerGraphQLClient()` throws → `makeClient()` returns `null` → `getBackendCatalogView()` returns `null`.
corpus/services/ant-academy.md:172-172	bullet	**Empty DB** — the query succeeds but the app academy tables have no rows → `[]`.
corpus/services/ant-academy.md:175-175	bullet	**The grid READS** the catalog from `app internal/academy` **via GraphQL** (the DB-authoritative chain above).
corpus/services/ant-academy.md:182-184	paragraph	**The committed JSON** (`code/public/content/<series>/<skill-path>/*.json`) is the **authoring source** (published into the app academy DB via the repo's export path), the input to `build-catalog.mjs`, and the input to the dev **draft** layer — but it is **not** what the authenticated grid renders.
corpus/services/ant-academy.md:236-239	bullet	**`.env.local` dewire.** `stacksecrets provision` targets `ant-academy/code/.env.local` too and runs on **every** `/demo-up` (before the academy launch), copy-if-absent-**appending** the **real** Clerk app's keys.
corpus/services/ant-academy.md:236-239	bullet	On a fresh up `ant-academy.sh`'s truncating write overwrites them; on a re-up the early-exit skipped that write, so the appended real key sat **last** and — dotenv last-wins — **dewired** Clerkenstein (keyless / sign-in wall).
corpus/services/ant-academy.md:287-291	bullet	**Switching language on a CHAPTER reader** re-runs `resolveServerChapterBody(slug, 'it')` (the chapter RSC reads `?lang=` via `localeFromSearchParams` and threads the locale) → in a demo that hit the **same backend-null 404** as #3. So it is **fixed by the M238 chapter-body patch**, which serves the locale-aware FS body (the `it/` overlay where present, else the canonical-EN fall-through).
corpus/services/ant-academy.md:297-297	table-row	| **Framework** | Next.js 16 App Router + React 19.2 (React Compiler enabled, Turbopack default) |
corpus/services/ant-academy.md:303-303	table-row	| **Mobile** | Expo SDK 54 / React Native (Expo Router) |
corpus/services/ant-academy.md:304-304	table-row	| **Testing** | Vitest (happy-dom + node), Playwright (e2e). 1000+ Vitest tests + ~26 Playwright e2e spec files (tests/e2e/). |
corpus/services/ant-academy.md:316-316	table-row	| Catalog front door | `/`, `/latest(.*)`, `/chapters/(.*)`, `/courses`, `/courses/(.*)` | The M4 public catalog.
corpus/services/ant-academy.md:316-316	table-row	Anonymous visitors browse it; the RSC swaps in `getPublicCatalogView()` for sessionless requests.
corpus/services/ant-academy.md:316-316	table-row	The pages still live in the `(authed)` route group under `ClerkProvider` (the catalog island uses Clerk hooks) — the *middleware* is what lets anonymous traffic through |
corpus/services/ant-academy.md:317-317	table-row	| Public-launch listings | `/library`, `/library/(.*)`, `/free`, `/free/(.*)` | Phase-1 public launch: read-only catalog preview + the free-tier listing |
corpus/services/ant-academy.md:318-318	table-row	| Auth / gate pages | `/sign-in(.*)`, `/no-organization` | Clerk hosts the auth UI itself; `/no-organization` is the gate page a signed-in org-less user lands on |
corpus/services/ant-academy.md:319-319	table-row	| Certificate verification | `/verify/(.*)`, **`/api/verify/(.*)`** | Public-shareable cert verification — a recruiter on another device with no session resolves `academyCertificate(certId)` (a `@public` field) and gets the minimized PII-free projection |
corpus/services/ant-academy.md:320-320	table-row	| AI proxy | `/api/ai/chat` | Does its own cookie-based `auth()` server-side |
corpus/services/ant-academy.md:321-321	table-row	| Release provenance | **`/api/_meta(.*)`, `/api/meta(.*)`** | The academy's mirror of the Go services' `/_meta`, so uptime probes can read name/version/build-date without a session.
corpus/services/ant-academy.md:321-321	table-row	Both spellings, because `next.config.js` rewrites `_meta` → `meta` and the middleware sees the pre-rewrite path |
corpus/services/ant-academy.md:322-322	table-row	| Crawler / agent files | `/robots.txt`, `/sitemap.xml`, `/sitemap(.*)`, `/llms.txt`, `/llms-full.txt`, `/.well-known/(.*)` | Static, must be fetchable anonymously |
corpus/services/ant-academy.md:323-323	table-row	| Assets & machine indexes | `/local-content/(.*)`, `/catalog.json`, `/academy-manifest.json` | Public-by-design: `/local-content/*` for `<audio>` Range requests + cover previews, `/catalog.json` for the external Anthropos backend Talk-to-Data indexer, `/academy-manifest.json` for the PWA manifest.
corpus/services/ant-academy.md:324-324	table-row	| **Dev-only**, `DEV_LOGIN_ENABLED` | `/api/dev/login-as`, `/dev/accept` | The real-Clerk-user login shortcut; must be reachable before a session exists.
corpus/services/ant-academy.md:325-325	table-row	| **Dev-only**, `BENCHMARK_VISUAL_BYPASS=1` ∧ `NODE_ENV==='development'` | `/my-certificates`, `/my-activity`, `/bookmarks` | Opens the remaining authed-only surfaces for the benchmark/e2e Playwright pass.
corpus/services/ant-academy.md:325-325	table-row	`NODE_ENV` is whitelisted, not blacklisted, so unset/`test`/typos stay closed |
corpus/services/ant-academy.md:331-334	bullet	**An open route is not an open body.** Anonymous traffic on `/` is served `getPublicCatalogView()`, so no tenant content is ever in the payload; chapter **section bodies** are walled separately by the chapter RSC and the `/api/chapters/*` handler, which self-gate on tenancy (hard 404) and tier.
corpus/services/ant-academy.md:335-338	bullet	**These are middleware globs, not evidence a page exists.** `/library/*` and `/free/*` are matcher patterns, but the only real pages are `code/app/(public)/library/page.jsx` and `code/app/(public)/free/page.jsx`.
corpus/services/ant-academy.md:344-344	bullet	npm (web app uses npm, not pnpm)
corpus/services/ant-academy.md:345-345	bullet	pnpm — only if you also want to run the mobile app
corpus/services/ant-academy.md:346-346	bullet	Clerk credentials (use the platform's dev tenant — same `@anthropos.work` domain)
corpus/services/ant-academy.md:347-347	bullet	The FA Pro icons are vendored in the repo (`code/public/assets/fontawesome/`), so a fresh, token-less `npm install` succeeds and the app serves working icons.
corpus/services/ant-academy.md:347-347	bullet	`FONTAWESOME_NPM_AUTH_TOKEN` survives in `code/.env.example` but is **not** needed to install or run.
corpus/services/ant-academy.md:391-391	paragraph	The **app's** env file is `code/.env`, not the repo root:
corpus/services/ant-academy.md:410-410	paragraph	`code/.env.example` is the authoritative, fuller list (it also carries Sentry / Better Stack DSN vars and the `NEXT_PUBLIC_CLERK_SIGN_IN_URL` / `SIGN_UP_URL` family used to keep Clerk on the in-app sign-in page).
corpus/services/ant-academy.md:410-410	paragraph	Reuse the **same Clerk keys** as in `platform/.env` so dev login works across the platform and the academy with a single session.
corpus/services/ant-academy.md:433-433	paragraph	The mobile app bundles `code/public/content/` at build time via `pnpm run dev:bundle`.
corpus/services/ant-academy.md:446-446	paragraph	`ant-academy/.claude/skills/` ships **its own** set of skills focused on **authoring content** — not to be confused with the platform's `/ant-*` skills in Rosetta:
corpus/services/ant-academy.md:452-452	table-row	| `author-podcast` | Generate the `path-intro.mp3` companion audio (uses AutoContent API) |
corpus/services/ant-academy.md:453-453	table-row	| `author-cover` | Generate the `cover.{png,webp}` for a skill path (uses OpenAI `gpt-image-2`) |
corpus/services/ant-academy.md:454-454	table-row	| `benchmark-chapter` | Drive Playwright through a chapter for visual + content benchmarking |
corpus/services/ant-academy.md:461-461	paragraph	These are **isolated to the ant-academy repo** and are loaded only when working inside it. They share no state with the Rosetta corpus skills.
corpus/services/ant-academy.md:465-465	bullet	**Web**: Pushed to Vercel via `.github/workflows/deploy-academy.yaml`
corpus/services/ant-academy.md:466-466	bullet	**Vercel env sync**: `.github/workflows/sync-vercel-env.yml` mirrors env vars
corpus/services/ant-academy.md:467-467	bullet	**Mobile**: Expo build pipeline (outside platform CI)
corpus/services/ant-academy.md:468-468	bullet	**Coverage CI**: `.github/workflows/sidebar-coverage-tests.yaml`
corpus/services/ant-academy.md:470-470	paragraph	Releases use **Cocogitto** conventional-commit tagging (`cog.toml`).
corpus/services/ant-academy.md:475-475	bullet	**OpenAI (direct, browser, opt-in)**: The in-app "Cosmo" assistant — gated behind `NEXT_PUBLIC_FEATURE_TRAINING_COACH` (default OFF) — calls the **OpenAI Responses API** (`gpt-5.2`, `https://api.openai.com/v1/responses`) directly from the browser using a per-user `localStorage('openai_api_key')`.
corpus/services/ant-academy.md:475-475	bullet	It is OpenAI-only and does **not** route through the platform's shared `ai` library or the `/api/ai/chat` route.
corpus/services/ant-academy.md:475-475	bullet	(The separate server-side `/api/ai/chat` route handler does support both OpenAI and Anthropic with server keys, but Cosmo does not use it.)
corpus/services/ant-academy.md:476-476	bullet	**Studio Desk (loose link)**: `NEXT_PUBLIC_STUDIO_URL` can deep-link from the academy to the Studio Desk UI; nothing required at runtime.
corpus/services/ant-academy.md:495-495	paragraph	If you ever need to add a Docker profile (e.g. for an integration-test harness), follow studio-desk's containerized variant as the template.
corpus/services/askengine.md:3-5	paragraph	**Not a standalone service — a domain of the `app` monolith** (the service the platform calls "backend").
corpus/services/askengine.md:3-5	paragraph	Code lives in `app/internal/askengine/` (the engine) + `app/internal/web/backend/ask/` (the HTTP/SSE handler + agentic loop).
corpus/services/askengine.md:9-12	bullet	A non-technical stakeholder asks a question in any language; an **agentic LLM** (Claude Sonnet 4.6 on AWS Bedrock) writes SQL, runs it inside an **org-scoped read-only SQL sandbox**, and explains the results — never exposing raw schema or another tenant's data.
corpus/services/askengine.md:31-32	bullet	**Codebase**: `app/internal/askengine/` (pure SQL/prompt/LLM layers) + `app/internal/web/backend/ask/` (the Echo handler, agentic loop, conversation store).
corpus/services/askengine.md:31-32	bullet	Go, part of the `app` monolith.
corpus/services/askengine.md:33-38	bullet	**Database**: PostgreSQL.
corpus/services/askengine.md:33-38	bullet	Reads run against a **dedicated, small read pool** (`COPILOT_DB_CONN` DSN, `askEngineMaxConns=6`) so interactive queries can't be starved by heavy dashboard analytics; each query runs in a **read-only tx with `SET LOCAL statement_timeout=10s`**.
corpus/services/askengine.md:33-38	bullet	Own persistence tables in the **`public` schema**: `ask_conversations`, `ask_messages` (JSONB content blocks), `ask_query_examples` (RAG golden question→SQL pairs with a **pgvector** embedding, cosine retrieval), `ask_query_lessons` (captured failed/corrected queries), `ask_auto_rules` (cross-org auto-distilled ruleset).
corpus/services/askengine.md:64-67	bullet	**Key routes**: `POST /ask/stream` (the **SSE** agentic generation), `GET`/`POST /ask/conversations`, `GET /ask/conversations/:id`, `GET /ask/conversations/:id/stream` (SSE reattach to an in-flight run), `DELETE /ask/conversations/:id`, `POST /ask/messages/:messageId/feedback`, and the admin auto-rules routes (`POST /ask/admin/auto-rules/distill`, `GET`/`activate`/`DELETE`).
corpus/services/askengine.md:68-73	bullet	**The agentic loop** (`handler.go:runAgenticLoop`, `maxAgenticIterations=15`, `loopTimeout=10min`) runs in a **detached goroutine** (`context.WithoutCancel` + timeout) so a client disconnect doesn't abort generation; a per-conversation `streamRegistry` lets the client reattach.
corpus/services/askengine.md:68-73	bullet	Per iteration: `bedrock.Stream` → accumulate blocks → if `stop_reason != tool_use` finish, else `executeTool` each tool (`query_postgres`→`Engine.Execute`, `render_chart`, `course_builder`) and feed back the result.
corpus/services/askengine.md:68-73	bullet	Wire results are sanitized (`publicToolResult` strips raw error text).
corpus/services/askengine.md:74-76	bullet	**Authorization**: the `/ask` group mounts Clerk auth; **org-admin is resolved per-turn** via `OrgCheckFeaturePermission`, gating self-scoped table access + author mode.
corpus/services/askengine.md:115-115	bullet	**Command**: `go run .` in the app repo, or `make up` (ships inside the `backend` container).
corpus/services/askengine.md:116-118	bullet	**Key env vars**: `ASK_MODEL_ID` (default `eu.anthropic.claude-sonnet-4-6`), `AWS_REGION` (default `eu-west-1`), `COPILOT_DB_CONN` (the read-pool DSN), the standard AWS creds, and `AZURE_OPENAI_ENDPOINT_URL`/`AZURE_OPENAI_KEY` (embeddings).
corpus/services/askengine.md:116-118	bullet	Per-org config is DB-resident, not env: the `talk_to_data_custom_rules` org setting.
corpus/services/askengine.md:121-121	bullet	**Service Name**: none of its own — part of the `backend` service (`docker compose up -d backend`).
corpus/services/askengine.md:127-128	bullet	`sandbox_test.go` (49 tests, the bulk) — adversarial/security: each test names the specific SQL-injection or CTE-bypass vector it closes.
corpus/services/askengine.md:129-129	bullet	`executor_test.go` (13) — `crossValidate` sanity checks + cell normalization.
corpus/services/askengine.md:130-131	bullet	`prompt_test.go` (10) — workforce-vs-hiring framing, dynamic blocks, and **`RulesBlockIsIdentical`** (prompt-cache prefix stability).
corpus/services/askengine.md:132-135	bullet	`followups_test.go` (14) — the streaming `<followups>` extractor edge cases.
corpus/services/askengine.md:132-135	bullet	No real Bedrock is hit in unit tests — the SQL/prompt/followups layers are pure and table-driven.
corpus/services/askengine.md:132-135	bullet	(The handler package `internal/web/backend/ask/` carries its own tests: sanitize, stream_registry, examples, lessons, distill, feedback, store, embed.)
corpus/services/askengine.md:139-140	bullet	**Shipped v1.267.0 (2026-05-07)** — the initial "Talk to Data" with SSE streaming + conversation management + the Bedrock client + the SQL sandbox.
corpus/services/askengine.md:139-140	bullet	**Author mode** (`course_builder` tool) landed **v1.340.0 (2026-07-17)**.
corpus/services/backend.md:108-108	paragraph	**There is no `SkillPathSessionService`** — measured: **0** occurrences in Go source, and no `skillpath…v1connect` package is imported.
corpus/services/backend.md:108-108	paragraph	Skill-path session state lives in `public.skill_path_sessions` and is reached through the GraphQL subgraph and in-process calls, not over RPC.
corpus/services/backend.md:111-111	bullet	**HTTP** endpoints on port 8082 (local; 8080 in production) for webhooks and miscellaneous integrations — including `POST /api/webhook/directus`, which **fails closed** without `DIRECTUS_WEBHOOK_SECRET`
corpus/services/backend.md:115-115	bullet	**Talk to Data** (`internal/askengine`) — SSE-streaming natural-language Q&A over the platform's data, powered by Bedrock (Anthropic) with a SQL-validation sandbox.
corpus/services/backend.md:117-117	bullet	**Job-simulation feedback** (`internal/jobsimfeedback`) — post-session signals routed back to the skills domain (in-process since the skiller merge)
corpus/services/backend.md:118-118	bullet	**AI usage / cost tracking** (`internal/aiusage`) — central ledger driven by the `AI` Redis Stream
corpus/services/backend.md:125-128	paragraph	The standalone `skiller` microservice was **merged into `app`** (July 2026).
corpus/services/backend.md:125-128	paragraph	Verified 2026-07-08 against the re-synced stack-dev clone (`app@c3c45e01` v1.334.1, `platform@0808b92`), a live containerized bring-up + migrate, and read-only prod.
corpus/services/backend.md:130-135	bullet	**Domain → the `public` schema, table names unchanged (`skiller.X → public.X`).** The moved tables: `skills`, `job_roles`, `categories`, `specializations`, `skill_embeddings`, `job_role_embeddings`, `skill_translations`, `job_role_translations`, `job_role_skills`, `job_role_categories` (Ent models now in `app/internal/data/ent/schema/`; port migrations in `terraform/migrations/`, merge commit `1fc00c78 Deprecate skiller schema`).
corpus/services/backend.md:130-135	bullet	The legacy `skiller` DB schema still exists on prod as a **deprecated mirror** — `public.*` is authoritative.
corpus/services/backend.md:217-221	bullet	**Clean-bring-up prerequisite:** the merged migrations create the taxonomy vector columns as `extensions.vector(1536)` and a GIN-trigram index via `extensions.gin_trgm_ops`, so the **`extensions` schema (pgvector + `pg_trgm`) must be bootstrapped before `make migrate`** on a clean DB — else app `20260518125439` and cms `20250116133510` fail with `schema "extensions" does not exist`.
corpus/services/backend.md:236-236	bullet	**Codebase**: `app` (local) — repo `git@github.com:anthropos-work/app`
corpus/services/backend.md:238-238	bullet	**Database**: PostgreSQL `public` schema (Ent ORM + Atlas migrations)
corpus/services/backend.md:239-239	bullet	**Ports**: 8082 (HTTP/GraphQL — `PORT`), 8083 (Connect-RPC — `RPC_PORT`), 8084 (meta/health — `META_PORT`).
corpus/services/backend.md:311-311	bullet	**Bedrock task role policy statements** (v1.267.1): IAM additions for Bedrock model access from the prod ECS task role.
corpus/services/backend.md:318-318	bullet	**GraphQL Federation**: schemas at `internal/web/backend/graphql/graph/schemas/*.graphqls`.
corpus/services/backend.md:318-318	bullet	Federated into the supergraph as the `backend` subgraph — **the only one left**, and on a local stack the frontends now reach it directly at `:8082/graphql/query` rather than through a router.
corpus/services/backend.md:320-320	bullet	**HTTP** (port 8082): Clerk webhooks, payment webhooks, document upload/convert endpoints, "Talk to Data" SSE.
corpus/services/backend.md:325-325	bullet	Hiring App
corpus/services/backend.md:326-326	bullet	Mobile App
corpus/services/backend.md:327-327	bullet	Studio-Desk (for org-level metadata)
corpus/services/backend.md:333-333	bullet	**Directus** (`content.anthropos.work`) — the external content edge read by the in-process cms domain
corpus/services/backend.md:334-334	bullet	**Judge0** — sandboxed code execution, called directly (`JUDGE0_BASE_URL`) since roadrunner merged in
corpus/services/backend.md:335-335	bullet	**LiveKit / AWS Chime** — simulation voice + recording, for the in-process jobsim engine
corpus/services/backend.md:336-336	bullet	**Gotenberg** — Office → PDF conversion
corpus/services/backend.md:337-337	bullet	**PostgreSQL** (`public` schema), **Redis** (cache + streams)
corpus/services/backend.md:343-343	bullet	The one external producer left is **Directus**, whose webhooks feed the `cms` stream
corpus/services/backend.md:344-344	bullet	Each stream has exactly **one** subscriber with multiple handlers merged via `.AddHandler(...)` — colony keys by stream name, so a second `AddSubscriber` for the same stream silently overwrites the first
corpus/services/backend.md:368-368	paragraph	You'll need `platform/.env` reachable (or copy relevant vars).
corpus/services/backend.md:368-368	paragraph	The infra services should still run via Docker.
corpus/services/chronos.md:15-19	bullet	**Key Functions**: *   **Timer Scheduling**: Accept requests to trigger events at specific future times *   **Timer Cancellation**: Allow scheduled events to be cancelled before they fire *   **Event Triggering**: Poll for due timers and publish events via Redis Streams *   **Health Monitoring**: Sentry cron monitor integration for reliability tracking
corpus/services/chronos.md:22-22	bullet	**Codebase**: `stack-dev/chronos` (or `git@github.com:anthropos-work/chronos.git`)
corpus/services/chronos.md:26-26	bullet	**Database**: PostgreSQL (`search_path=chronos`)
corpus/services/chronos.md:42-42	paragraph	Chronos uses a simple single-table design for timer storage:
corpus/services/chronos.md:69-69	table-row	| Pending | `completed_at IS NULL AND cancelled_at IS NULL AND scheduled_at > now()` |
corpus/services/chronos.md:70-70	table-row	| Due | `completed_at IS NULL AND cancelled_at IS NULL AND scheduled_at <= now()` |
corpus/services/chronos.md:71-71	table-row	| Completed | `completed_at IS NOT NULL` |
corpus/services/chronos.md:72-72	table-row	| Cancelled | `cancelled_at IS NOT NULL` |
corpus/services/chronos.md:78-78	paragraph	**Service**: `ChronosService` (Connect RPC)
corpus/services/chronos.md:99-99	paragraph	When a timer fires, Chronos publishes a `TimerWentOff` event to Redis Streams:
corpus/services/chronos.md:118-118	paragraph	**Consumers**: Any service subscribed to the Chronos Redis Stream can react to timer events.
corpus/services/chronos.md:122-124	bullet	**Upstream Consumers**: *   **Jobsimulation**: Schedules simulation timeouts, deadline reminders *   Any service needing delayed/scheduled events
corpus/services/chronos.md:125-127	bullet	**Downstream Dependencies**: *   **PostgreSQL**: Timer persistence *   **Redis**: Event publishing via Redis Streams
corpus/services/chronos.md:158-158	bullet	**Batch & Locking**: Each tick fetches up to 10 due timers (`LIMIT 10`) selected with `FOR UPDATE SKIP LOCKED`, so multiple instances never double-fire the same timer; the due window is `scheduled_at < now() + interval '1 second'`.
corpus/services/chronos.md:165-168	bullet	**Prerequisites**: *   PostgreSQL running with `chronos` schema *   Redis available for pub/sub *   Environment variables: `DB_CONNECTION`, `REDIS_ADDR`
corpus/services/chronos.md:198-198	table-row	| `PORT` | HTTP server port | `8080` |
corpus/services/chronos.md:199-199	table-row	| `RPC_PORT` | RPC server port | `8081` |
corpus/services/chronos.md:200-200	table-row	| `DB_CONNECTION` | PostgreSQL connection string | `postgres://...?search_path=chronos` |
corpus/services/chronos.md:201-201	table-row	| `REDIS_ADDR` | Redis server address | `localhost:6379` |
corpus/services/chronos.md:202-202	table-row	| `REDIS_STREAMS_INDEX` | Redis DB index for streams | `2` |
corpus/services/chronos.md:203-203	table-row	| `SENTRY_DSN` | Sentry error tracking | `https://...@sentry.io/...` |
corpus/services/chronos.md:214-214	paragraph	**Scheduling a simulation timeout** (from Jobsimulation service):
corpus/services/clerk-integration.md:3-6	paragraph	**Single source of truth for how the Anthropos platform uses Clerk.** Other docs mention Clerk in their own context (per-service usage, ops/webhook setup); this page is the cross-cutting picture — what Clerk is used for, how, which repos depend on it, and how each one integrates.
corpus/services/clerk-integration.md:18-18	table-row	| **Authentication** | ✅ | Issues session JWTs; verified platform-wide against Clerk's JWKS.
corpus/services/clerk-integration.md:19-19	table-row	| **Identity / profile** | ✅ | User id (internal `eid` + Clerk subject), email, first/last name. |
corpus/services/clerk-integration.md:20-20	table-row	| **Organizations & membership** | ✅ | Clerk Organizations are the source of truth for which org a user belongs to. |
corpus/services/clerk-integration.md:22-22	table-row	| **Authorization (backend)** | ⚠️ indirect | Backend permission decisions are made by **Sentinel**, not Clerk.
corpus/services/clerk-integration.md:22-22	table-row	Clerk org roles are *synced into* Sentinel at webhook time, then Sentinel decides. |
corpus/services/clerk-integration.md:23-23	table-row	| **Authorization (frontend/standalone)** | ✅ direct | next-web-app, studio-desk, and ant-academy read Clerk's `org:admin` / membership **directly** to allow/deny features (no Sentinel). |
corpus/services/clerk-integration.md:25-25	paragraph	**One-line answer to "auth only, or authz too?"** — Clerk handles **authentication + identity + organization/role management** everywhere.
corpus/services/clerk-integration.md:25-25	paragraph	It is **not** the backend authorization engine (Sentinel is, fed by Clerk via sync), but the **frontend/standalone apps authorize directly off Clerk org roles/membership**.
corpus/services/clerk-integration.md:29-29	paragraph	Clerk's catalog is large; the platform uses a **focused subset**.
corpus/services/clerk-integration.md:32-32	bullet	**Authentication** — session JWT + JWKS verification (backend).
corpus/services/clerk-integration.md:32-32	bullet	Sign-in/up UI: prebuilt `<SignIn>` / `<SignUp>` + `<UserProfile>` (next-web-app), `<SignIn>` (ant-academy web); studio-desk delegates to the web app; ant-academy mobile uses a custom email+password form.
corpus/services/clerk-integration.md:33-33	bullet	**Session JWT as the API bearer** — `getToken()` is called with **no template** (the default token); the custom claims are baked into that default token via dashboard config.
corpus/services/clerk-integration.md:34-34	bullet	**Organizations + memberships** — active org, `setActive`, `publicMetadata.eid` → tenant id (core to multi-tenancy).
corpus/services/clerk-integration.md:35-35	bullet	**Org roles** (`admin` / `basic_member`) — coarse RBAC gating; synced into Sentinel.
corpus/services/clerk-integration.md:36-36	bullet	**Org invitations** — backend create/revoke (+ a hand-rolled bulk call); frontend list/accept.
corpus/services/clerk-integration.md:37-37	bullet	**Webhooks** (svix, 12 event types) — Clerk → Postgres + Sentinel sync.
corpus/services/clerk-integration.md:38-38	bullet	**Backend API** — org/membership/invitation CRUD, user-create CLI, `external_id` + metadata write-back, lookups.
corpus/services/clerk-integration.md:39-39	bullet	**User/org metadata** — `unsafeMetadata` (trial/stripe flags), `publicMetadata` (eid, isHiring, role).
corpus/services/clerk-integration.md:41-41	bullet	**Localization** — 8 locales (`@clerk/localizations`).
corpus/services/clerk-integration.md:43-44	paragraph	**Not used** (available but untouched) MFA / TOTP / passkeys · OAuth / social / SAML / Enterprise SSO (mobile is email+password only) · device & multi-session management · Clerk Billing (billing is Stripe) · custom org **permissions** / `has()` / `<Protect>` (only coarse admin/member) · Clerk-native impersonation / Actor Tokens · Waitlist / GoogleOneTap / Web3 · `@clerk/themes` · most prebuilt components (`<UserButton>`, `<OrganizationSwitcher>`, `<OrganizationProfile>`, … — org UI is custom).
corpus/services/clerk-integration.md:46-46	paragraph	**⚠️ Caveat:** ant-academy **mobile sign-in aborts on any second factor** (`ClerkSignInForm.tsx`), so enabling MFA in the Clerk dashboard would **break mobile login**.
corpus/services/clerk-integration.md:57-61	paragraph	A Clerk **sign-in token** (Backend API `POST /v1/sign_in_tokens`) is a one-shot ticket that a browser exchanges via `signIn.create({ strategy: 'ticket' })` for a **genuine session as the named user** — no password, no 2FA, no CAPTCHA.
corpus/services/clerk-integration.md:57-61	paragraph	Measured across the whole clone set with three independent instruments (`/usr/bin/grep -rn`, per-clone `git grep`, and a Python `os.walk`, per rule 44 — all three agree):
corpus/services/clerk-integration.md:71-74	paragraph	**Sites 2–5 mint a session for an arbitrary user found by email** (`users.getUserList({ emailAddress })` then `createSignInToken({ userId })`) — none of them is admin-scoped, and none of them is "admin impersonation".
corpus/services/clerk-integration.md:71-74	paragraph	Sites 2, 4 and 5 hard-404 outside local dev, so a **production-scoped** reading of the old sentence was defensible; the sentence was not production-scoped, and site 3 is not `NODE_ENV`-gated at all.
corpus/services/clerk-integration.md:108-108	bullet	Verifies the session JWT against Clerk's **JWKS** (`clerk-sdk-go/v2` `jwt.Verify` + `jwks.Client`, 1-minute leeway), then `jwt.Decode`s the claims.
corpus/services/clerk-integration.md:112-112	paragraph	To avoid Clerk API round-trips, the platform puts custom claims on the Clerk session token; `authn` reads them:
corpus/services/clerk-integration.md:116-116	table-row	| `eid` | `User.ID()` — internal Anthropos UUID (Clerk `external_id`) |
corpus/services/clerk-integration.md:117-117	table-row	| (JWT subject) | `User.AuthID()` — Clerk user id |
corpus/services/clerk-integration.md:118-118	table-row	| `email`, `firstname`, `lastname` | profile (lazy `user.Client.Get` fallback if absent) |
corpus/services/clerk-integration.md:119-119	table-row	| `org` (public-metadata map, its `eid`) | `Organization.ID()` — internal org UUID |
corpus/services/clerk-integration.md:120-120	table-row	| `org_id` | `Organization.AuthID()` — Clerk org id |
corpus/services/clerk-integration.md:121-121	table-row	| `org_role` | `Organization.AuthRole()` — returned **verbatim**.
corpus/services/clerk-integration.md:121-121	table-row	The backend session token carries the **bare** form (`admin` / `basic_member`); only the client-side `useAuth().orgRole` is prefixed (`org:admin`).
corpus/services/clerk-integration.md:123-123	paragraph	`AuthRole()` is **exposed as a getter and never enforced inside `authn`** — consumers decide what to do with it.
corpus/services/clerk-integration.md:130-130	table-row	| **Sentinel** | not used at all (no Clerk/authn import) | Sentinel's own Casbin policy store |
corpus/services/clerk-integration.md:131-131	table-row	| storage, messenger | — | no auth |
corpus/services/clerk-integration.md:132-132	table-row	| **next-web-app / studio-desk / ant-academy** | authenticate **and** authorize | **local app code** reading Clerk `org:admin` / membership |
corpus/services/clerk-integration.md:135-135	paragraph	The **backend `app`** receives **svix-verified Clerk webhooks** (`user.*`, `organization.*`, `organizationInvitation.*`, `organizationMembership.*`).
corpus/services/clerk-integration.md:137-137	bullet	**translates the Clerk role** (`admin` / `basic_member`) and **pushes it into Sentinel** (`OrgAddUserToRole` / `OrgReplaceUserRole` / `OrgRemoveUserFromRole`).
corpus/services/clerk-integration.md:143-143	paragraph	Clerk ships a **separate package per framework** (Go, Next.js, Express, browser-JS, Expo).
corpus/services/clerk-integration.md:143-143	paragraph	Each is a **thin adapter over Clerk's shared core** — one server-side core and one browser-side core — **not** a separate auth implementation.
corpus/services/clerk-integration.md:147-147	table-row	| **colony** (`/authn`) — the shared auth core; among the live Go services, imported by **`app` only** | `clerk-sdk-go/v2` | Verifies the session JWT (JWKS) + reads claims. sentinel / storage / messenger import it **0** times. |
corpus/services/clerk-integration.md:148-148	table-row	| **app** (backend) | `clerk-sdk-go/v2`, `svix-webhooks/go` | Authn (via colony) + org/membership/invitation Backend-API writes + svix-verified webhook sync → Postgres + Sentinel.
corpus/services/clerk-integration.md:148-148	table-row	Also carries the folded jobsimulation + cms domains, which authenticate through the same middleware. |
corpus/services/clerk-integration.md:150-150	table-row	| **storage, messenger** | — | No Clerk / no auth. |
corpus/services/clerk-integration.md:151-151	table-row	| **sentinel** | — | Does **not** use Clerk; pure Casbin authorization. |
corpus/services/clerk-integration.md:152-152	table-row	| **next-web-app** — `apps/web`, `apps/hiring`, `apps/integration` | `@clerk/nextjs` (+ `@clerk/localizations`) | Next.js App Router auth: `clerkMiddleware` route protection, `useAuth().getToken()` bearer, org/role gating (see deep-dive). |
corpus/services/clerk-integration.md:153-153	table-row	| **next-web-app** — `apps/mobile` | `@clerk/clerk-expo` | Expo / React Native session (paused PoC). |
corpus/services/clerk-integration.md:154-154	table-row	| **next-web-app** — `e2e` | `@clerk/testing`, `@clerk/backend` | **Test-only**: Playwright bypass token + programmatic user/session seeding. |
corpus/services/clerk-integration.md:155-155	table-row	| **studio-desk** | `@clerk/clerk-js` (frontend), `@clerk/express` (backend) | Vanilla-TS browser SDK + Express middleware; admin tooling gated on the `STUDIO_ACCESS_ROLES` set — `org:admin` **and** `content_creator`. |
corpus/services/clerk-integration.md:157-157	table-row	| **ant-academy** — mobile | `@clerk/clerk-expo` | Expo session-only gate (custom email+password form). |
corpus/services/clerk-integration.md:163-163	paragraph	Clerk credentials live in `platform/.env` (backend) and each app's own env. The shapes:
corpus/services/clerkenstein.md:12-18	paragraph	**This is a pointer.** The full, self-contained documentation now lives **in the `clerkenstein/` section's own knowledge base** (added in M2b): start at `.agentspace/rosetta-extensions/clerkenstein/knowledge/kb-index.md` — the **authoring copy**, per *One monorepo, two clone roles* below; a stack's pinned-tag clone carries the same tree at `stack-demo/rosetta-extensions/clerkenstein/…`.
corpus/services/clerkenstein.md:20-25	paragraph	**One monorepo, two clone roles.** `rosetta-extensions` is ONE private monorepo with sections (`clerkenstein`, `demo-stack`, `stack-injection`, `stack-core`, `stack-seeding`, `alignment`).
corpus/services/clerkenstein.md:20-25	paragraph	It is authored / built / tested / aligned in the **authoring copy** at `.agentspace/rosetta-extensions/`, then **tagged**, and consumed by each stack via that stack's own **pinned-tag** clone (`stack-demo/rosetta-extensions @ <tag>`).
corpus/services/clerkenstein.md:29-32	paragraph	Clerkenstein is a **drop-in mock of the Clerk library** the platform uses — the *same interface*, with all security and sync **disarmed**.
corpus/services/clerkenstein.md:29-32	paragraph	It lets **demo** environments create users / orgs / admins and log in/out with no Clerk friction (one universal credential, no live API, no webhooks, no rate limits), while platform repos keep "thinking" they use Clerk with **zero source changes**.
corpus/services/clerkenstein.md:34-35	paragraph	It is the **first mirror produced by the M0 alignment process** (not a hand-built mock): its fidelity is *measured* as a 0–100% alignment score against a Clerk **Alignment DNA**.
corpus/services/clerkenstein.md:84-90	paragraph	Those goldens are **hand-authored / hybrid** (decision M1-D1) — the reference behavior derived from the real libraries' documented + observed semantics (and, for `@clerk/express`, confirmed by driving the *genuine* SDK), **not** captured from a live, network-connected real-Clerk tenant.
corpus/services/clerkenstein.md:84-90	paragraph	So 100% means "the mirror reproduces the behavior we encoded as the reference," not "diffed byte-for-byte against a running Clerk instance." Re-capturing goldens against a live source on a Clerk version bump is the M1b drift loop's job. This is the right bar for a *demo* mock; it is not a conformance certificate against production Clerk.
corpus/services/clerkenstein.md:94-95	paragraph	The `clerkenstein/` section is organised **one dir per mocked dependency** (M2b reorg, decision M2b-D2) — every dir below is a subdir of `rosetta-extensions/clerkenstein/`, not a repo root:
corpus/services/clerkenstein.md:100-100	table-row	| `clerk-backend/` | `clerk-sdk-go/v2` | fake Backend API + the in-memory org store, merged |
corpus/services/clerkenstein.md:101-101	table-row	| `clerk-frontend/` | `@clerk/clerk-js` + `@clerk/nextjs` | fake Frontend API + publishable-key codec — **mints** JWTs |
corpus/services/clerkenstein.md:104-104	table-row	| `deploy/` | `colony/authn/provider/clerk` | the disarmed provider drop-in — **deployable** into a vendored colony fork (compiles against real `colony`, **pinned `v0.34.3` — behind `app`'s `v0.35.2`**, see the ⚠️ below) |
corpus/services/clerkenstein.md:105-105	table-row	| `cmd/` | — | standalone binaries: `mintpk` (authoritative publishable-key minter) · `fake-fapi` / `fake-bapi` (standalone fake servers for demos; `fake-fapi` loads `FAKE_FAPI_ROSTER` for M37 multi-identity) |
corpus/services/clerkenstein.md:106-106	table-row	| `alignment/` | — | the measurement harness: `cmd/{clerkrun,jsfapirun,multirun,expressrun,deployrun}` + `dna/` (five) + `golden{,-js,-multi,-express,-deploy}/` + `scripts/` |
corpus/services/clerkenstein.md:108-110	paragraph	The browser-login → backend-verify coherence chain runs through `shared`: `clerk-frontend` mints the HS256 universal-key JWT, `authn` verifies that exact token — pinned by the JS DNA's `SessionToken/decoded-identity` gene (operator `exact`).
corpus/services/clerkenstein.md:121-121	table-row	| `@clerk/express` · next-web SSR | `GET /v1/users/{userID}/organization_memberships` | `clerk-backend/` (registered since M2) |
corpus/services/clerkenstein.md:122-122	table-row	| **clerk-js** (`clerk.user.getOrganizationMemberships()`) | **`GET /v1/me/organization_memberships`** | `clerk-frontend/` (**added `fix/studio`**) |
corpus/services/clerkenstein.md:128-130	bullet	**The paginated envelope, not a bare array** — a `?paginated=true` request answers `{"response":{"data":[…],"total_count":N},…}`, because clerk-js destructures `{ data, total_count }` straight off it. A bare array **is** the *"Cannot destructure property 'data'"* throw.
corpus/services/clerkenstein.md:131-134	bullet	**`limit`/`offset` are honoured** — clerk-js sends `limit=10&offset=0` and pages when `total_count` exceeds what it received, so serving the full list against `limit=10` makes it chase a second page that disagrees with the first.
corpus/services/clerkenstein.md:131-134	bullet	`total_count` always reports the **true** total; an offset past the end is an empty **array** (never `null` — clerk-js maps over it).
corpus/services/clerkenstein.md:151-158	paragraph	**(v1.9 M37)** — `clerk-frontend` now holds a **users/orgs registry** (replacing the single `DefaultDemoUser`) + an **active-seat selection** so a demo can **switch the active browser identity** among the seeded heroes/orgs (the M35 stories roster) — the seat-switch the presenter cockpit's "login as" needs.
corpus/services/clerkenstein.md:151-158	paragraph	Selection is **server-authoritative** (the FAPI holds the active key, so the client view, `/v1/me`, the token mint, and the handshake cookies all resolve the same hero): `?__clerk_identity=<key>` on the handshake (the cockpit's [Login as] deep-link) + the `/v1/demo/{identities,select}` control plane.
corpus/services/clerkenstein.md:151-158	paragraph	Measured by the `clerk-multi-1` DNA (`alignment/cmd/multirun`, 9 genes, 100%/100%) — a *new measured surface* that holds while the existing four stay green.
corpus/services/clerkenstein.md:218-220	bullet	**Producer** — `RosterIdentity` (roster.go) gains `org_name`/`org_slug`, filled in `BuildRoster` from `st.Org.Name` + the single-sourced `orgSlugFor` (the **same** slug rule `OrgSeeder` writes to `public.organizations.slug`, so the roster-carried org and the seeded org can never disagree — #M39-D2).
corpus/services/clerkenstein.md:229-230	paragraph	Alignment held: the **multi-identity** (`clerk-multi-1`) + **JS/FAPI** (`clerk-js-5`) surfaces stay **9/9, 100%/100%** (the `DefaultDemoUser` goldens are unchanged — they take the default-name fallback).
corpus/services/clerkenstein.md:243-248	bullet	**The align-safety rule this pins — CONDITIONAL-EMIT (#M224-D-align).** A new FAPI field is emitted **only when its non-default value applies** (`if u.OrgIsHiring { pm["isHiring"] = true }`, else omit).
corpus/services/clerkenstein.md:243-248	bullet	Conditional-emit keeps every non-hiring org's `public_metadata` **byte-identically `{eid}`** — generalizing the `Picture`/`OrgLogo` `omitempty` pattern to non-string additions.
corpus/services/clerkenstein.md:249-251	bullet	Roster 2/2) — GREEN, no identity gene perturbed (the named `SessionToken/decoded-identity` critical/exact gene unaffected).
corpus/services/clerkenstein.md:252-255	bullet	**BAPI intentionally NOT wired (#M224-D-bapi).** The server derives hiring from the `public.organizations.is_hiring` **DB column**, not Clerk BAPI metadata; a `clerk-backend` change would add Go-SDK align surface for **no render benefit**.
corpus/services/clerkenstein.md:252-255	bullet	Optional, only if a server-side consumer ever reads `organization.publicMetadata.isHiring`.
corpus/services/clerkenstein.md:257-258	paragraph	**`@clerk/express` (M2c) added no new dir** — it's a *consumer* (a Node backend verifier we satisfy), so its support is **additive**: an RS256 path (RS256 minting in `shared/` + a real JWKS from `clerk-frontend/`
corpus/services/clerkenstein.md:259-265	bullet	`@clerk/express` verifies RS256-via-JWKS and rejects HS256, so the RS256 path is additive (the HS256 seams + M1/M2 gates stay green).
corpus/services/clerkenstein.md:259-265	bullet	Its `clerk-express-1` DNA includes the **`dotless-pk-rejected`** gene: `@clerk/backend`'s `assertValidPublishableKey` (run by `clerkMiddleware` on every request) rejects a pk whose decoded FAPI host has **no dot** — which is why the demo pk host is a **dotted** `127.0.0.1` (not `localhost`) and why a MagicDNS FQDN (`billion.taildc510.ts.net`, also dotted) validates natively (v2.2 M213).
corpus/services/clerkenstein.md:267-272	paragraph	**The deployment/injection surface (M3) *did* add `deploy/` + `cmd/`.** Unlike the `authn/` twin (which mocks the standalone `colony/authn` interface), the platform actually consumes `colony/authn/provider/clerk` *inside* the `colony` module.
corpus/services/clerkenstein.md:267-272	paragraph	So the **deployable** drop-in lives in `deploy/colony-authn/`: the disarmed provider — same package, same `Clerk` type, same `NewProvider(apiKey)` signature — compiled against the **real** `colony`, pinned at `v0.34.3`, so an injected demo app accepts Clerkenstein-minted tokens with zero source changes.
corpus/services/clerkenstein.md:292-293	paragraph	Making a demo reachable from another machine on a **Tailscale** tailnet (opt-in via `/demo-up --public-host <magicdns>`) touches three Clerkenstein-adjacent seams — all **gated** so an unset host is byte-identical:
corpus/services/clerkenstein.md:307-308	bullet	**clerk-js egress is overridable.** The FAPI proxies the clerk-js bundle from `cdn.jsdelivr.net` (its one outbound dependency); **`FAKE_FAPI_CLERKJS_CDN`** overrides that base so a locked-down network can point at a mirror.
corpus/services/clerkenstein.md:333-334	paragraph	A fourth seam — **the origins & links emission (M214)** — admits the MagicDNS/HTTPS origin everywhere a browser→backend or cross-surface call is gated, again all gated on the knob:
corpus/services/clerkenstein.md:336-340	bullet	**CORS + redirects → HTTPS MagicDNS.** The injected override appends `https://$HOST:{3000,3001,9000}+off` to the backend's `CORS_EXTRA_ORIGINS` (the `localhost` trio is kept), and emits studio-desk's `CLERK_SIGN_IN_URL`/`WEB_APP_URL` requireAuth fallback at `https://$HOST:3000+off`.
corpus/services/clerkenstein.md:336-340	bullet	**Per-port HTTPS**, because `tailscale serve` preserves the offset port (M213 D-PROXY-2) — the browser origin is `https://$HOST:<offsetport>`, not a port-less 443. One scheme predicate (`browser_scheme`) flips http→localhost / https→MagicDNS uniformly.
corpus/services/clerkenstein.md:341-344	bullet	**The bounded patch tail** rides the **existing** sha-pinned mechanism (never a canonical repo edit): ant-academy's `next dev` `allowedDevOrigins` admits the MagicDNS host (the `ant-academy-dev-origins` demo-patch, env-var indirection so the post-hash stays fixed), and studio-desk's SPA `VITE_CLERK_SIGN_IN_URL` bakes via a gitignored `.env.production.local` overlay (no Dockerfile ARG).
corpus/services/cms.md:96-96	paragraph	The CMS service is the **content layer of the platform** — it owns the authored, versioned, published **CONTENT / DEFINITIONS** and serves them to everyone else.
corpus/services/cms.md:98-98	bullet	**Serves content** to the rest of the platform via GraphQL Federation and internal RPC — **skill paths** (title, description, cover/video, curators a.k.a. "Meet the Experts", library categories, **chapters → steps**, the job-simulation steps inside a chapter, skills-to-verify, settings, versioning — the `skill_paths` Directus collection), **job-simulation blueprints** (the `simulations` collection + `sequences`, roles, tasks, validation criteria), and the **content library** (`library_categories`, `library_macro_categories`, `resource`) — all proxied through Directus with Anthropos-specific business logic on top.
corpus/services/cms.md:99-99	bullet	**Owns the Studio data model** — `StudioDocument` (simulation blueprints), `StudioTask` (generation jobs), and related entities for the content-authoring workflow.
corpus/services/cms.md:102-102	paragraph	Since cms-in-app it rides in the **`app`** image rather than the cms one.
corpus/services/cms.md:117-117	bullet	**Database**: ~~PostgreSQL `cms` schema~~ — **`public`, via `app`'s Ent**.
corpus/services/cms.md:117-117	bullet	The `cms` schema is a legacy husk since cms-in-app v8.0; the similarity + Studio tables moved to `public`
corpus/services/cms.md:190-190	paragraph	CMS acts as a proxy + business-logic layer over Directus:
corpus/services/cms.md:196-196	paragraph	Why this pattern: business rules and validation live in CMS, caching reduces Directus load, and the abstraction makes it easier to swap the storage backend later.
corpus/services/cms.md:200-200	bullet	**GraphQL**: since cms-in-app the schemas live with the rest of app's at `app/internal/web/backend/graphql/graph/schemas/*.graphqls`, served on the `backend` subgraph.
corpus/services/cms.md:200-200	bullet	The Directus webhook receiver moved to `POST /api/webhook/directus` on app's web server and **fails closed** without `DIRECTUS_WEBHOOK_SECRET` (the standalone receiver at `:8090/webhooks/` was unauthenticated).
corpus/services/cms.md:205-205	bullet	Next Web App (GraphQL)
corpus/services/cms.md:206-206	bullet	Studio-Desk (GraphQL for studio entities)
corpus/services/cms.md:214-214	bullet	Directus (content storage)
corpus/services/cms.md:215-218	bullet	PostgreSQL (Ent ORM, **`public` schema** — the cms tables were re-created there at cms-in-app v8.0; the legacy `cms` schema is non-authoritative.
corpus/services/cms.md:219-219	bullet	Redis (cache, Watermill streams)
corpus/services/cms.md:240-244	paragraph	**⚠️ HISTORICAL — `cd cms; make init-studio` is NOT the onboarding path any more.** Since cms-in-app v8.0 the studio-room pipeline is pulled into the **`app`** image by CI via the `additional_repo` mechanism (app v1.360.1) — see the **Studio** bullet of the *Where everything went* list in the banner at the top of this doc (**named, not pinned:** this said `:70-71`, which at M257x iter-120 was the **Events** bullet).
corpus/services/cms.md:240-244	paragraph	Work on this domain in **`app`**, not in the frozen `cms` repo.
corpus/services/cms.md:246-246	paragraph	The Python studio submodule had to be cloned **before** any docker build, otherwise `make up` failed with `"/studio": not found`:
corpus/services/cms.md:317-319	paragraph	**HISTORICAL — this is no longer how the shipped pipeline is refreshed.** Since cms-in-app v8.0 CI pulls `anthropos-studio-room` into the **`app`** image via `additional_repo`; there is no manual sync step, and the `cms` repo is not cloned by `make init`.
corpus/services/coursebuilder.md:3-5	paragraph	**Not a standalone service — a domain of the `app` monolith** (the service the platform calls "backend").
corpus/services/coursebuilder.md:3-5	paragraph	Course Builder ships inside `app`; there is no separate container, repo, or subgraph.
corpus/services/coursebuilder.md:3-5	paragraph	Code lives in `app/internal/coursebuilder/` (the engine) + `app/internal/web/backend/coursebuilder/` (the HTTP/SSE boundary).
corpus/services/coursebuilder.md:34-34	bullet	**Language**: Go (1.26.4) — part of the `app` monolith.
corpus/services/coursebuilder.md:41-47	bullet	**Key engine files**: `service.go` (the state machine + `ProductionConfig(author, grader, translator)` wiring — `TestProductionConfig_WiresPlanner` locks the nil-planner regression), `refine.go` (the load-bearing ≥ 90 loop: keep-best + rollback), `author.go` (single-shot author), `benchmark.go`/`patch.go`/`patchgen.go` (grader + patch machinery), `planner.go`/`path.go` (multi-chapter), `checkpoint.go` (resume), `schema.go` (the Academy chapter shape + validators), `normalize.go` (widget normalization + XSS sanitization), `bedrock.go`/`model.go`/`usage.go` (LLM adapter + `MockClient` + cost formula), `embed.go` (`//go:embed assets/*.md` rubric), `imagegen/` (cover images).
corpus/services/coursebuilder.md:86-94	bullet	⚠️ **`cost` is filtered off the wire BY DESIGN — do not build a client that waits for it.** `case cb.EventCost:` (`:2709`) returns an **empty** event name (`:2717`) under a D2 ruling (2026-07-13) recorded in the code itself: *"the terminal cost readout is COGS and MUST NOT reach the customer SSE stream … filtered here at the wire boundary by returning an empty event name"* — and the stream loop skips `writeSSE` whenever the name is empty (`:1478-1480`).
corpus/services/coursebuilder.md:86-94	bullet	The `EventCost` **kind** still exists internally, consumed by `awaitAndPersist`/`persistTerminal` and the ops surface; it is a wire filter, not a deleted event, which is exactly why grepping the enum finds it and a browser never does.
corpus/services/coursebuilder.md:86-94	bullet	**This bullet previously listed `cost` as a live wire event and omitted four that are live — `text`, `score`, `steering_received`, `steering_applied` (13 listed vs 16 real); corrected M257x iter-102.**
corpus/services/coursebuilder.md:107-110	bullet	**Prerequisites**: it runs **as part of `app`** (no separate binary/container).
corpus/services/coursebuilder.md:107-110	bullet	Needs a model backend — **either** `ANTHROPIC_API_KEY` (the production path) **or** AWS creds resolvable by the default SDK chain (Bedrock, `eu-west-1`) — plus Postgres (migrations applied), Redis (the Asynq worker), and a Clerk secret.
corpus/services/coursebuilder.md:107-110	bullet	The routes unmount only when **neither** backend can build a client; AWS creds alone are not a prerequisite.
corpus/services/coursebuilder.md:111-112	bullet	**Command**: `go run .` in the app repo, or the platform `make up` (Course Builder ships inside the `backend` container).
corpus/services/coursebuilder.md:119-122	bullet	**Operator-only cmd tools** (not in CI, cost money): `cmd/coursebuilder-liverun` (author one course against real Bedrock from a Markdown source; writes `chapter.json` + `run-report.json`) and `cmd/coursebuilder-e2e` (full seed→author→benchmark→publish proof against a **throwaway** Docker Postgres; `assertNotSharedDSN` refuses any shared/prod DSN).
corpus/services/coursebuilder.md:125-125	bullet	**Service Name**: none of its own — it is part of the `backend` service (`docker compose up -d backend`).
corpus/services/coursebuilder.md:132-136	paragraph	The deterministic `MockClient` (FIFO scripted responses, usage/cache accounting) stands in for the Bedrock `ModelClient` — no real LLM in unit tests.
corpus/services/coursebuilder.md:140-144	bullet	**Not behind a global feature flag** — GA to every organization, **org-admin-gated** (`courseBuilderAccessGate`).
corpus/services/coursebuilder.md:140-144	bullet	The only gate is graceful degradation: routes stay unmounted if **neither** model backend can build a client (no `ANTHROPIC_API_KEY` *and* no resolvable AWS creds) — a clean 404, never a half-wired endpoint.
corpus/services/coursebuilder.md:140-144	bullet	Per-sub-feature kill-switches: `COURSEBUILDER_PLANNER_ENABLED`, `CB_SOURCE_DISTILL`, `COURSEBUILDER_EMAILS_ENABLED`.
corpus/services/customerio-sync.md:32-33	paragraph	CustomerIO Sync is the **background data-sync domain** that pushes platform users into the marketing platform as contacts, for marketing automation, lifecycle email campaigns, and product analytics.
corpus/services/customerio-sync.md:35-36	paragraph	It's a one-directional pipeline: PostgreSQL `public` schema → the marketing API. No inbound traffic, no consumers inside the platform.
corpus/services/customerio-sync.md:40-40	bullet	**Live code**: `app/internal/customeriosync/` (`sync.go` the push, `store.go` the read, `sync_query.sql` the query, `doc.go` the port record) + the manual tool `app/cmd/customerio-resync`
corpus/services/customerio-sync.md:41-41	bullet	**Frozen repo**: `git@github.com:anthropos-work/customerio-sync` (private) — not deleted, but not built, cloned or deployed any more
corpus/services/customerio-sync.md:48-48	paragraph	Two things deliberately did not survive as-is, both recorded in `doc.go` rather than buried:
corpus/services/customerio-sync.md:50-50	bullet	**The sync window is now stateless.** The standalone held `lastSyncTime` in memory and advanced it per tick.
corpus/services/customerio-sync.md:50-50	bullet	`app`'s worker is multi-replica and restarts on every deploy, so each run reads a fixed overlap window instead (wider than the 10-minute schedule).
corpus/services/customerio-sync.md:50-50	bullet	Pushing a contact is idempotent (`CreateContact` with `UpdateEnabled`), so the overlap is free.
corpus/services/customerio-sync.md:51-51	bullet	**The scan is `database/sql`, not sqlc/pgx.** `app` carries no sqlc toolchain.
corpus/services/customerio-sync.md:53-56	paragraph	Two more structural moves: the standalone's 10-minute ticker became an **asynq scheduled task**, and the DB **view** it read (`public.customer_io_sync_table`, never under migration control) was replaced by `sync_query.sql` — verified row-for-row against the view, identical for 7,392 of 7,397 rows and correct where it differs.
corpus/services/customerio-sync.md:90-90	bullet	**PostgreSQL** (`public` schema) — read via `app`'s shared analytics pool, not a connection string of its own
corpus/services/customerio-sync.md:91-91	bullet	**Brevo API** (external — `BREVO_KEY` in `platform/.env`)
corpus/services/customerio-sync.md:100-101	paragraph	For protocol and field-mapping details, read the live code — `app/internal/customeriosync/`, whose `doc.go` is the port record — or clone the frozen repo for the pre-merge source:
corpus/services/customerio-sync.md:114-114	paragraph	The domain is inside `backend` and **off unless you say otherwise**:
corpus/services/db-backup.md:5-5	paragraph	db-backup performs **scheduled PostgreSQL backups** every 6 hours, storing dumps in three geographically separate locations for disaster recovery:
corpus/services/db-backup.md:8-8	bullet	**Azure Blob Storage** (secondary)
corpus/services/db-backup.md:18-18	table-row	| **Deployment** | Docker (scheduled, internal) |
corpus/services/db-backup.md:20-20	table-row	| **Backup Targets** | S3, Azure, Hetzner |
corpus/services/db-backup.md:21-21	table-row	| **Source** | PostgreSQL RDS (all schemas) |
corpus/services/db-backup.md:23-23	paragraph	The service runs on a timer, performing full database dumps and uploading them to all three storage providers.
corpus/services/db-backup.md:27-27	paragraph	db-backup runs in production and staging environments.
corpus/services/gotenberg.md:5-5	paragraph	Gotenberg is a **third-party stateless conversion service**.
corpus/services/gotenberg.md:11-11	bullet	**Image**: `gotenberg/gotenberg:8` (pinned major version 8)
corpus/services/gotenberg.md:15-15	bullet	**Statelessness**: No database, no Redis, no persistence.
corpus/services/gotenberg.md:34-34	paragraph	Gotenberg exposes a multi-route HTTP API. Anthropos uses:
corpus/services/gotenberg.md:42-42	paragraph	The backend service (`app`) is the only consumer.
corpus/services/gotenberg.md:46-46	bullet	**Function**: `ConvertToPDF(ctx, gotenbergURL, document, filename)` returns `[]byte` of the rendered PDF — **an in-memory intermediate, discarded by both callers** (see Role above)
corpus/services/gotenberg.md:50-50	bullet	**Env var**: `GOTENBERG_URL=http://gotenberg:3200` (injected via the backend's compose `environment:`)
corpus/services/gotenberg.md:54-54	paragraph	Gotenberg starts automatically with the default profile:
corpus/services/gotenberg.md:79-79	paragraph	Embedding it in the Go backend would bloat the image and complicate the build.
corpus/services/gotenberg.md:79-79	paragraph	Gotenberg packages that complexity behind a clean HTTP boundary and is well-maintained by the open-source community.
corpus/services/graphql-wundergraph.md:36-39	bullet	**Primary Goal**: Serve the platform's Apollo Federation v2 **supergraph** from a WunderGraph **Cosmo Router** at one endpoint.
corpus/services/graphql-wundergraph.md:36-39	bullet	Since **cms-in-app v8.0** the supergraph composes a **single subgraph** — `backend`.
corpus/services/graphql-wundergraph.md:36-39	bullet	Counting entries in `supergraph-config-prod.yaml` at each commit:
corpus/services/graphql-wundergraph.md:43-43	table-row	| `749dc86~1` | — | **5** | `backend`, `skiller`, `jobsimulation`, `cms`, `skillpath` |
corpus/services/graphql-wundergraph.md:79-79	bullet	**Codebase**: `graphql-wundergraph` (local) — repo `git@github.com:anthropos-work/graphql-wundergraph`
corpus/services/graphql-wundergraph.md:80-80	bullet	**Runtime**: prebuilt Go binary image `ghcr.io/wundergraph/cosmo/router:0.275.0` (pinned)
corpus/services/graphql-wundergraph.md:82-82	bullet	**Federation**: Apollo Federation v2, `federation_version: =2.3.2` (pinned)
corpus/services/graphql-wundergraph.md:83-83	bullet	**Database**: none — stateless gateway (no DB, no Redis)
corpus/services/graphql-wundergraph.md:90-92	paragraph	**There is no application source here.** `package.json` is a stub (`{"name":"graphql-wundegraph"}` — note the misspelling, carried in the repo).
corpus/services/graphql-wundergraph.md:110-112	paragraph	The supergraph `config.json` is **baked into the image at build time** by `wgc router compose -i supergraph-config.yaml -o config.json`.
corpus/services/graphql-wundergraph.md:114-115	bullet	Adding/changing a subgraph **or a single field** requires re-running `wgc compose` and **rebuilding + restarting** the image — there is **no hot reload**.
corpus/services/graphql-wundergraph.md:124-124	table-row	| `Dockerfile` | Uses the **committed `schemas/*.graphqls`** as-is | production CI build |
corpus/services/graphql-wundergraph.md:140-140	table-row	| `/graphql` | GraphQL | Unified federated endpoint.
corpus/services/graphql-wundergraph.md:140-140	table-row	Playground + introspection ON in compose/dev, OFF in prod. |
corpus/services/graphql-wundergraph.md:144-144	table-row	| `supergraph-update.yml` | GitHub Action | `workflow_dispatch` bumps subgraph versions in `subgraphs.conf`, re-downloads SDL from GitHub Releases, opens a PR. |
corpus/services/graphql-wundergraph.md:148-149	paragraph	Routing URLs use Docker **service names** on `app-network` (deliberately avoiding `host.docker.internal`/`extra_hosts` so it works on Docker Desktop *and* native Linux):
corpus/services/graphql-wundergraph.md:151-152	paragraph	**Historical — compose no longer has a router service to route.** Kept because the archived repo's configs still contain these rows and a reader will find them there.
corpus/services/graphql-wundergraph.md:156-156	table-row	| `backend` (the `app` service) | `http://backend:8082/graphql/query` | **the only surviving subgraph.** Named `backend`, maps to repo/service `app` — and now also serves everything the four folded subgraphs used to |
corpus/services/graphql-wundergraph.md:179-179	bullet	**Downstream (composed subgraphs)**: `app` (as `backend`) — and, historically, `jobsimulation` and `cms`.
corpus/services/graphql-wundergraph.md:180-180	bullet	~~**Compose `depends_on`**~~ — moot: there is no compose service.
corpus/services/graphql-wundergraph.md:180-180	bullet	Historically `backend`, `jobsimulation`, `cms`, **`storage`** (note `storage` was **not** a GraphQL subgraph but was in the startup-order list).
corpus/services/graphql-wundergraph.md:217-220	paragraph	So what the probe actually proves is *the server is up, routing, parsing GraphQL and enforcing authz*.
corpus/services/graphql-wundergraph.md:234-236	paragraph	The `__schema` form is the one that returns real `data` unauthenticated, and it is what the frontend's `pnpm codegen` relies on. It is gated on `colony.Development`, so on staging/prod it is refused like everything else.
corpus/services/graphql-wundergraph.md:238-239	paragraph	On `backend`, `/graphql` serves the Apollo Sandbox UI; CORS preflight and auth happen at `/query`.
corpus/services/graphql-wundergraph.md:250-250	paragraph	These are **Docker build args** (not runtime secrets) — they select which config files get baked in.
corpus/services/graphql-wundergraph.md:254-254	table-row	| `ENVIRONMENT` | `compose` | Picks `config.<ENVIRONMENT>.yaml` → `config.yaml` (router runtime behavior).
corpus/services/graphql-wundergraph.md:255-255	table-row	| `ENVIRONMENT_CONFIG` | `compose` | Picks `supergraph-config-<ENVIRONMENT_CONFIG>.yaml` → which routing URLs are baked in. |
corpus/services/graphql-wundergraph.md:256-256	table-row	| `CONFIG_PATH` | `config.yaml` | Tells the Cosmo router which config file to load. |
corpus/services/graphql-wundergraph.md:258-258	table-row	| `VERSION` / `ARCHITECTURE` / `APOLLO_ELV2_LICENSE` | — | Prod CI build args (ECR image tag, `linux/amd64`, accept Apollo ELv2 license). |
corpus/services/graphql-wundergraph.md:260-260	paragraph	The router itself is **stateless** — no DB/Redis/secret env vars at runtime (the compose service mounts `.env` via `env_file` but the router does not consume secrets).
corpus/services/graphql-wundergraph.md:264-266	paragraph	Schema correctness is enforced at compose time by `wgc` (federation composition will fail the build on an invalid supergraph).
corpus/services/graphql-wundergraph.md:271-271	bullet	**Pinned versions**: cosmo router `0.275.0`, `wgc 0.104.0`, federation `2.3.2`.
corpus/services/graphql-wundergraph.md:272-272	bullet	**Repo name is misspelled** `graphql-wundegraph` in `package.json` and the repo's own `CLAUDE.md` heading.
corpus/services/hiring.md:11-15	paragraph	A seeder that wrote the "obvious" table (`jobsimulation.sessions`) would produce a page that renders its chrome and its columns with **every score blank** — a green coverage sweep over an empty scoreboard.
corpus/services/hiring.md:87-87	paragraph	`is_hiring` must be set in **TWO** places, because the platform derives the org's type differently on each side:
corpus/services/hiring.md:107-108	bullet	**Client — Clerk `publicMetadata.isHiring = true`.** The **entire `apps/web` re-skin is derived client-side from Clerk, never from a GraphQL call:**
corpus/services/hiring.md:116-119	paragraph	So a demo org whose DB row says `is_hiring=true` but whose Clerk metadata omits `isHiring` renders as a **normal Workforce org** in the browser — the nav never relabels, the "Results" framing never appears.
corpus/services/hiring.md:116-119	paragraph	**⇒ M224 must extend Clerkenstein's fake Clerk API to emit `publicMetadata.isHiring = true`** for the hiring org. This is a rext change (the mock), **NOT a platform edit**.
corpus/services/hiring.md:172-175	bullet	**Hiring sims are `SIMULATION_TYPE_HIRING`-typed job simulations** — the same `jobsimulation` runtime the Workforce product uses, tagged as a hiring assessment.
corpus/services/hiring.md:172-175	bullet	The captured public snapshot carries **87 real `SIMULATION_TYPE_HIRING` sims** (published + public) — a rich pool; M223 picks **5** as the org's "positions" (real content, zero synth — M222 BA-6).
corpus/services/hiring.md:191-194	paragraph	The recruiter's scoreboard is `/enterprise/activity-dashboard → AI-Simulations → [simId]`: one row per candidate who took the sim, ranked by a comparable **score**.
corpus/services/hiring.md:191-194	paragraph	That score comes from **`app.public.job_simulation_sessions.score`** — the **canonical** session entity in the `app` service's `public` schema, read directly by the resolver.
corpus/services/hiring.md:211-211	paragraph	**The read-path, traced end-to-end (FE → GraphQL → resolver → Ent → table):**
corpus/services/hiring.md:215-215	table-row	| 1 | `apps/{web,hiring}/.../simulationScoreColumn.tsx:54,95-97` | renders `row.score` (the visible number).
corpus/services/hiring.md:295-295	bullet	**`public.memberships`** — the candidate must be **active** (`GetMemberships`; status `active`/`invited`).
corpus/services/hiring.md:297-306	paragraph	Before the mirror drop it was `public.local_jobsimulation_sessions` (score) + a co-written `jobsimulation.sessions` twin (so the federated non-null `Session!` resolved from the other subgraph, else the list NULL-bubbled).
corpus/services/hiring.md:297-306	paragraph	The mirror table is dropped and there is no second subgraph, so **both halves collapsed into `public.job_simulation_sessions`.** (This said *"neither table exists"* until M257x iter-52 — a flat contradiction of the **History** blockquote (**`:193-198`**) above, which is the adjudicated form: the mirror was dropped, `jobsimulation.sessions` was not, and no local stack creates that schema either way. It cited the then-`:157-159` — the `job_position` bullet, now `:173-175`, a different construct entirely — until M257x iter-98.
corpus/services/hiring.md:308-309	paragraph	**Org prerequisites:** `public.organizations.is_hiring = true` (§ *the gate*) + Clerk `publicMetadata.isHiring = true` (M224) + the **`OrgFeatureInsights` Casbin permission** substrate.
corpus/services/hiring.md:324-324	paragraph	`isHiringOrg` is **client-derived** (`useGetClerkOrganization.tsx:20-21`, above).
corpus/services/hiring.md:332-337	bullet	**Two `isEnterprise` definitions DIVERGE — and that is not a bug:** - **Nav:** `isEnterprise = Boolean(organization)` (`template.tsx:90`) → stays **TRUE** for a hiring org, so the enterprise nav still renders. - **Billing:** `isEnterprise = !isHiringOrg && organizationId` (`FreeTrialContainer.tsx:29`) → flips **FALSE**, so hiring orgs are **excluded from the Workforce free-trial** container.
corpus/services/hiring.md:361-364	bullet	(Before the merges it resolved from `jobsimulation.sessions` in a *different* subgraph, joined on the mirror's `jobsimulation_session_id`, and a missing twin NULL-bubbled the row out of the list.
corpus/services/hiring.md:365-367	bullet	**Surface:** route `/enterprise/activity-dashboard → AI-Simulations → [simId]` (list) + `.../[simId]/[userId]` (the per-candidate drill-down, reads the `public.validation_*` tables) — present in **both** `apps/web` and `apps/hiring`, but for a *genuine* hiring org only the `apps/hiring` copy is reachable (§ *The render path*).
corpus/services/hiring.md:404-408	paragraph	That held on the `billion` spike **only because that org had no client `publicMetadata.isHiring`** — client-side it read as a *workforce* org, so it never tripped the product-boundary guard.
corpus/services/hiring.md:404-408	paragraph	M224 **wired client `isHiring=true`** (D-DESIGN-1 — the org must *genuinely read as hiring*), and that flips the calculus:
corpus/services/hiring.md:449-458	paragraph	**FeedbackSeeder** has written app-side session rows on GENERIC sims since v1.10 M42m (then `public.local_jobsimulation_sessions` MIRROR rows; `public.job_simulation_sessions` since the M257 re-point) → unguarded it leaked a training sim + a 2nd session per candidate into the recruiter's list; **SuccessionSeeder** FKs each member's first population session (now skipped for the hiring org) → the FK VIOLATED and the whole seed reported *"failed"*.
corpus/services/hiring.md:449-458	paragraph	Both now consult `skipGenericActivityForHiringOrg`; the regression enumerates **all 8** generic seeders (#M228 F1/F2/F3).
corpus/services/hiring.md:449-458	paragraph	The lesson is in-code: **a new mirror-writing / session-FK seeder MUST be added to the guard's consult-list AND the enumerated `TestGenericActivitySeeders_SkipHiringOrg` table.**
corpus/services/intelligence.md:14-14	bullet	**Primary Goal**: Background data sync between Backend and Skiller schemas.
corpus/services/intelligence.md:15-15	bullet	**Codebase**: `git@github.com:anthropos-work/intelligence.git` (no longer cloned locally)
corpus/services/intelligence.md:17-17	bullet	**Connection**: Interfaced with Backend (`public`) and Skiller schemas via direct DB connections (`DB_CONNECTION_BACKEND`, `DB_CONNECTION_SKILLER` env vars).
corpus/services/jobsimulation.md:91-91	paragraph	Jobsimulation runs **AI-powered workplace simulations** end-to-end: it loads simulation **definitions** from CMS (the content layer), hosts the interactive **session** (voice via LiveKit, chat, code, documents), records the interaction, generates post-session insights, and reports outcomes via Redis Streams to the App (which now hosts the in-process skill-path engine, formerly the standalone skillpath service).
corpus/services/jobsimulation.md:121-121	bullet	**Interview extraction pipeline added**: new entity `interview_extraction_results` (migrations `20260402145459`, `20260409131539`) stores per-session `user_report`, `manager_report`, and `summary` JSON blobs linked to a `session_id`.
corpus/services/jobsimulation.md:121-121	bullet	Exposed via CSV export with language arg (see `internal/simulator/manager/interview_report_csv*.go`).
corpus/services/jobsimulation.md:126-126	bullet	**GraphQL**: schemas at `internal/graph/schemas/` (main contract: `schema.graphqls`). ~~Federated into the platform schema by Cosmo Router~~ — **the jobsimulation subgraph is folded into `backend`**; the supergraph is one subgraph (`backend.graphqls`).
corpus/services/jobsimulation.md:152-152	bullet	**Backend (app)** — user context, organization scoping
corpus/services/jobsimulation.md:154-154	bullet	**Sentinel** — authz
corpus/services/jobsimulation.md:158-158	bullet	**PostgreSQL**, **Redis** — base infra
corpus/services/jobsimulation.md:162-162	bullet	**LiveKit** — primary voice engine (`LIVEKIT_HOST_URL`, `LIVEKIT_RECORDINGS_BUCKET_NAME`)
corpus/services/jobsimulation.md:163-163	bullet	**AWS Chime SDK** — video/camera/screensharing recording (`CHIME_RECORDINGS_BUCKET_NAME=ant-prod-chime-demo`)
corpus/services/jobsimulation.md:165-165	bullet	**AssemblyAI** — EU voice transcription for call recordings (`ASSEMBLYAI_API_KEY`)
corpus/services/jobsimulation.md:166-166	bullet	**Bunny.net** — video stream hosting / tokenized playback (`BUNNY_REC_STREAM_API_KEY`, `BUNNY_TOKEN_HASH_KEY`)
corpus/services/jobsimulation.md:172-172	bullet	Producer: `jobsimulation` stream (session completed, insights generated)
corpus/services/jobsimulation.md:184-184	paragraph	**The cobra ROOT command's `RunE` *is* the server.** There is **no `serve` and no `run` subcommand.**
corpus/services/jobsimulation.md:186-187	bullet	The image is `ENTRYPOINT ["./application"]` with **no CMD**; when compose still declared the service it passed **no `command:`** either.
corpus/services/jobsimulation.md:198-199	paragraph	The root command sets neither `SilenceUsage` nor `SilenceErrors`.
corpus/services/jobsimulation.md:198-199	paragraph	So **any** error returned from `RunE` makes cobra print `Error: …` **followed by the full usage/help block**, then exit 1.
corpus/services/jobsimulation.md:217-218	paragraph	So on the container that actually runs the engine since the fold — `backend`; there is no `…-jobsimulation-1` container to inspect — the same underlying failure reads like this instead:
corpus/services/jobsimulation.md:226-228	paragraph	`head -3` is still right (the first line is still the whole story), but "it printed the CLI help" is, on `backend`, a report of something that cannot happen.
corpus/services/jobsimulation.md:247-247	paragraph	**With the path simply absent, `LoadDefaultConfig` returns `nil`.** The mount is the bug.
corpus/services/jobsimulation.md:249-250	bullet	**On a workstation** with a real `~/.aws/credentials` file, it works — which is why this never showed up in local dev and only bit a fresh Linux box.
corpus/services/jobsimulation.md:251-253	bullet	**In a demo/dev stack**, rext's **generated compose override drops the bind** (`volumes: !reset null` on the demo path; an `!override`-tagged empty list on the dev path).
corpus/services/jobsimulation.md:255-256	paragraph	⚠️ **A bare `volumes: []` does NOT remove it** — compose *merges* volume sequences and the inherited bind survives.
corpus/services/jobsimulation.md:255-256	paragraph	Only the `!reset` / `!override` tags remove it. Verified against the compose binary.
corpus/services/jobsimulation.md:258-265	paragraph	**Downstream when the mount kills it — and the blast radius GREW with the fold.** It no longer costs you one surface: `backend` is the only *application* container the `core` profile starts (`sentinel` is authz-only), so the whole platform goes with it. The AI-Simulations surface is gone and so is every other one; `:8082/graphql/query` is unreachable rather than "the jobsimulation subgraph erroring" — there is no jobsimulation subgraph, the supergraph is `backend` alone; the `pt-aisim-chat-launch` playthrough cannot pass; no session-completed events reach the Redis stream, so the in-process skill-path engine never sees completions.
corpus/services/jobsimulation.md:280-280	paragraph	**To work on the live engine, you run `app`, not this repo:**
corpus/services/messenger.md:13-15	paragraph	Messenger is the platform's **centralized notification subsystem** — a standalone service until the v9.0 fold, a domain inside `backend` since.
corpus/services/messenger.md:13-15	paragraph	It sends and schedules transactional emails, using **Brevo** (formerly Sendinblue) as the delivery backend and **Liquid** templating for the bodies.
corpus/services/messenger.md:75-75	bullet	**Email backend**: Brevo via `getbrevo/brevo-go v1.1.3`
corpus/services/messenger.md:76-76	bullet	**Templating**: `osteele/liquid v1.8.1`
corpus/services/messenger.md:115-115	paragraph	Messages carry user info, template ID, and template params; the body is rendered through Liquid against those params before the Brevo send.
corpus/services/messenger.md:124-127	bullet	**Downstream**: * **Brevo API** — outbound email delivery (`BREVO_KEY`) * **PostgreSQL** — read-only `public` schema access for org / whitelabel lookups * **Redis** — Watermill stream subscriber + scheduled-message storage
corpus/services/messenger.md:158-160	paragraph	The old first step — `cd platform && make dev S=messenger`, which stopped the container so the native process could take its port — is moot: there is no container, and the repo is not a sibling clone any more (`make init` does not fetch it), so `cd messenger` assumes you cloned it by hand.
corpus/services/messenger.md:179-179	table-row	| `PORT` | `8200` | HTTP port |
corpus/services/messenger.md:180-180	table-row	| `RPC_PORT` | `8201` | Connect-RPC port |
corpus/services/messenger.md:181-181	table-row	| `BREVO_KEY` | (empty) | Brevo API key. Empty → console sender.
corpus/services/messenger.md:182-182	table-row	| `REDIS_ADDR` | `redis:6379` | Redis address |
corpus/services/messenger.md:183-183	table-row	| `REDIS_STREAMS_INDEX` | `4` | Redis DB index for streams |
corpus/services/messenger.md:200-200	paragraph	The flow handlers have unit tests (`assignments_test.go`, `jobsimulations_test.go`, `organizations_test.go`) covering the suppression / whitelabel branches.
corpus/services/next-web-app.md:21-21	bullet	**Codebase**: `next-web-app` (local) — repo `git@github.com:anthropos-work/next-web-app`
corpus/services/next-web-app.md:25-25	bullet	**Database**: none (org scoping comes from Clerk session claims; data lives in backend services)
corpus/services/next-web-app.md:35-35	table-row	| **Mobile** | `@anthropos/mobile` | 3031 (Expo) | Expo / React Native PoC (**paused**); **excluded** from the pnpm workspace, uses `EXPO_PUBLIC_*` | ❌ |
corpus/services/next-web-app.md:67-67	table-row	| `@anthropos/graphql` | All data fetching: queries, React Query hooks by domain, server fetchers, **codegen output** (`src/__generated__`), `codegen.ts` |
corpus/services/next-web-app.md:69-69	table-row	| `@anthropos/core-js` | Shared constants, types, utils (e.g. `security/printToken`) |
corpus/services/next-web-app.md:76-76	bullet	**Observability proxies**: `/logpoint/*` → PostHog (EU); `/monitoring` tunnels Sentry/Better Stack events.
corpus/services/next-web-app.md:80-80	bullet	**Downstream**: the GraphQL endpoint on `backend` (`:8082/graphql/query`; in prod, the router), backend `app` API (`:8082`), Clerk, PostHog (EU), Sentry/Better Stack, Stripe (billing), Bunny CDN (thumbnails + Chime recordings, token-signed), Metabase (embedded analytics), Azure OpenAI/OpenAI (server AI routes).
corpus/services/next-web-app.md:81-81	bullet	**Upstream**: end users / browsers; WordPress (embeds `apps/integration`); Vercel (prod hosting); platform compose service `next-web-app` (containerized Workforce variant).
corpus/services/next-web-app.md:102-103	paragraph	Older Node fails with `WARN Unsupported engine`, and pnpm refuses to wipe `node_modules` in non-TTY shells (`ERR_PNPM_ABORTED_REMOVE_MODULES_DIR_NO_TTY`).
corpus/services/next-web-app.md:119-127	paragraph	The repo's own `Dockerfile.dev` (Node 24 alpine — it is the only Dockerfile in the repo) builds **only** `@anthropos/web-app` (`pnpm turbo build --filter=@anthropos/web-app`), and `apps/web` is the only frontend in platform compose.
corpus/services/next-web-app.md:119-127	paragraph	Integration / maintenance / mobile ship via Vercel only.
corpus/services/next-web-app.md:119-127	paragraph	`NEXT_PUBLIC_*` are baked at **build time**; on a remote VM set `PUBLIC_HOST` in `platform/.env` so the client bundle resolves the right hostname.
corpus/services/next-web-app.md:133-133	table-row	| `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY` / `CLERK_SECRET_KEY` | — | Clerk auth (client / server) |
corpus/services/next-web-app.md:135-135	table-row	| `NEXT_PUBLIC_BACKEND_API_URL` | `http://localhost:8082` | Backend (`app`) API base URL |
corpus/services/next-web-app.md:136-136	table-row	| `GRAPHQL_SCHEMA_FOR_GEN` | `http://localhost:8082/graphql/query` | Schema endpoint used by `graphql-codegen` (was `:5050/graphql`) |
corpus/services/next-web-app.md:137-137	table-row	| `NEXT_PUBLIC_HOSTING_URL` / `PUBLIC_HOST` | `http://localhost:3000` / `localhost` | Public hosting URL; `PUBLIC_HOST` parameterizes baked URLs in compose |
corpus/services/next-web-app.md:138-138	table-row	| `NEXT_PUBLIC_POSTHOG_KEY` / `_HOST` · `NEXT_PUBLIC_SENTRY_DSN` / `SENTRY_AUTH_TOKEN` | — | Analytics + error tracking (PostHog EU, Sentry/Better Stack) |
corpus/services/next-web-app.md:140-140	table-row	| `BUNNY_*` · `METABASE_*` · `PRINT_ROUTE_SECRET` | — | CDN, embedded analytics, signed PDF/print routes |
corpus/services/next-web-app.md:141-141	table-row	| `EXPO_PUBLIC_*` | — | Mobile (Expo) variants of the public vars |
corpus/services/roadrunner.md:76-76	paragraph	Roadrunner is the **code-execution proxy** for the platform.
corpus/services/roadrunner.md:76-76	paragraph	When a simulation includes a coding task, jobsimulation hands the user's source code to Roadrunner, which forwards it to **Judge0** (a sandboxed code-execution API) and returns the results (stdout, stderr, status, time).
corpus/services/roadrunner.md:78-78	paragraph	Roadrunner exists for one reason: it gives the platform a clean, language-agnostic boundary for running untrusted code without ever executing it in our own services or on our own infrastructure.
corpus/services/roadrunner.md:80-80	paragraph	It also runs an **Asynq** worker pool that polls Judge0 for results — **one task per single submission**, as § Async tasks below states.
corpus/services/roadrunner.md:117-117	table-row	| `Submission(runtime, source_code, stdin)` | Submit a single execution; returns a `token` |
corpus/services/roadrunner.md:118-118	table-row	| `SubmissionPackage(...)` | Submit one **multi-file** program in one call; returns a single `token` |
corpus/services/roadrunner.md:119-119	table-row	| `SubmissionResult(token)` | Poll for execution result (output, errors, status, time) |
corpus/services/roadrunner.md:123-123	bullet	The HTTP server (`PORT`, default `8080`) exposes only the `/_meta` health endpoint.
corpus/services/roadrunner.md:123-123	bullet	All code submission goes through Connect-RPC on `RPC_PORT` (default `8081`).
corpus/services/roadrunner.md:135-135	bullet	**Downstream**: Judge0 at `JUDGE0_BASE_URL=http://52.48.139.23:2358` (default in compose), Redis (Asynq backend)
corpus/services/roadrunner.md:136-136	bullet	**No database** — roadrunner owns no Postgres schema and stores no persistent state of its own. Judge0 holds submission state by token.
corpus/services/roadrunner.md:193-193	table-row	| `PORT` | `8080` | `10400` | HTTP health port (`/_meta` only) |
corpus/services/roadrunner.md:194-194	table-row	| `RPC_PORT` | `8081` | `10401` | Connect-RPC port |
corpus/services/roadrunner.md:195-195	table-row	| `JUDGE0_BASE_URL` | — none | `http://52.48.139.23:2358` | Judge0 API endpoint |
corpus/services/roadrunner.md:196-196	table-row	| `JUDGE0_API_KEY` | — none (required) | — not set here | Judge0 `X-Auth-Token`; the one Judge0 var the compose block never set — supplied via platform/.env |
corpus/services/roadrunner.md:197-197	table-row	| `SENTRY_DSN` | — none (optional) | — not set here | Sentry error-tracking DSN |
corpus/services/roadrunner.md:198-198	table-row	| `REDIS_ADDR` | — none | `redis:6379` | Redis address for Asynq |
corpus/services/roadrunner.md:199-199	table-row	| `REDIS_STREAMS_INDEX` | — none | `4` | Redis DB index for streams |
corpus/services/roadrunner.md:200-200	table-row	| `REDIS_WORKER_INDEX` | — none | `0` | Redis DB index for Asynq — **must parse as an int or the process exits immediately** |
corpus/services/roadrunner.md:201-201	table-row	| `ENVIRONMENT` | — none | `development` | Environment name |
corpus/services/roadrunner.md:205-205	paragraph	Roadrunner currently has NO test suite — there are zero `*_test.go` files, so `go test ./...` (also run at Docker build time, `Dockerfile` line 18) is a no-op that passes vacuously.
corpus/services/sentinel.md:7-7	paragraph	Sentinel does **not** handle authentication — that's Clerk's job. It also does not validate JWTs (the shared `authn` library does that in each consuming service).
corpus/services/sentinel.md:7-7	paragraph	Sentinel only answers *"is this subject allowed to perform this action on this object?"*.
corpus/services/sentinel.md:11-11	bullet	**Codebase**: `sentinel` (local) — repo `git@github.com:anthropos-work/sentinel`
corpus/services/sentinel.md:13-13	bullet	**Framework**: Connect-RPC, Casbin v3
corpus/services/sentinel.md:14-14	bullet	**Database**: PostgreSQL `sentinel` schema (single table: `casbin_rules`) — **no Ent ORM**
corpus/services/sentinel.md:16-16	bullet	**Profile**: always on (no `profiles:` declared in compose — runs with every `make up`)
corpus/services/sentinel.md:17-17	bullet	**No outbound RPC** to other platform services — sentinel is a leaf
corpus/services/sentinel.md:18-18	bullet	**No Redis, no GraphQL, no background workers** — stateless request/response only
corpus/services/sentinel.md:30-30	table-row	| `m` | User-tier quota | A user passes if they are in the policy's tier (`g(user, tier)`) OR the policy tier is `TIER_FREE` (free-tier policies act as an unconditional baseline, substituted from the proto enum `Tier_TIER_FREE`), AND the requested `count` <= the tier `max`. |
corpus/services/sentinel.md:39-39	bullet	`g(user, tier)` — `TIER_FREE` / `TIER_PREMIUM`
corpus/services/sentinel.md:73-73	table-row	| `CheckFeature` | User tier quota check |
corpus/services/sentinel.md:74-74	table-row	| `CheckOrganizationFeature` | Org-level feature quota check |
corpus/services/sentinel.md:75-75	table-row	| `AddUserToTier` / `RemoveUserFromTier` | Manage user tier groupings |
corpus/services/sentinel.md:76-76	table-row	| `GetQuotas` / `GetOrganizationQuotas` | Read policy quotas |
corpus/services/sentinel.md:77-77	table-row	| `OrgAddUserToRole` / `OrgRemoveUserFromRole` / `OrgReplaceUserRole` | Manage org role assignments |
corpus/services/sentinel.md:78-78	table-row	| `OrgClearAll` | Remove all `g2` + `g3` policies for an org |
corpus/services/sentinel.md:79-79	table-row	| `OrgCheckPermission` | Legacy org action check (read-only) |
corpus/services/sentinel.md:80-80	table-row	| `OrgAllowUserToUseFeature` / `OrgDisallowUserToUseFeature` | Manage `g3` membership feature access |
corpus/services/sentinel.md:81-81	table-row	| `OrgMembershipsAllowedToUseFeature` | List memberships with feature access |
corpus/services/sentinel.md:82-82	table-row	| `OrgGetOrganizationFeatureCredits` / `OrgSetOrganizationFeatureCredits` | Manage org feature credit budgets |
corpus/services/sentinel.md:90-90	bullet	**Downstream**: PostgreSQL (`sentinel` schema, table `casbin_rules`)
corpus/services/sentinel.md:91-91	bullet	**No outbound RPC** to other platform services
corpus/services/sentinel.md:101-101	paragraph	Sentinel is always part of any `make up` (no profile gate).
corpus/services/sentinel.md:101-101	paragraph	To restart just sentinel:
corpus/services/sentinel.md:125-125	paragraph	It works only against a local Postgres on port 5432, and relies on `init_policy.sql` being schema-qualified (`sentinel.casbin_rules`) so the seed lands in the right schema regardless of search_path.
corpus/services/sentinel.md:125-125	paragraph	For a non-local DB, run psql with your own DSN: `psql "$DB_URL" -f init_policy.sql`.
corpus/services/sentinel.md:144-144	paragraph	All tests use in-memory Casbin enforcers — no PostgreSQL or fixtures required.
corpus/services/sentinel.md:150-150	table-row	| `DB_CONNECTION` | yes | — | PostgreSQL DSN with `search_path=sentinel` |
corpus/services/sentinel.md:151-151	table-row	| `PORT` | no | `8087` | HTTP + Connect-RPC port (compose sets this explicitly; binary default is 8080) |
corpus/services/sentinel.md:152-152	table-row	| `ENVIRONMENT` | no | — | Environment name |
corpus/services/sentinel.md:153-153	table-row	| `SERVICE_NAME` | no | `sentinel` | Logging label |
corpus/services/sentinel.md:154-154	table-row	| `SENTRY_DSN` | no | — | Sentry error tracking |
corpus/services/sentinel.md:158-158	bullet	**Hot reload**: changes made directly to the `casbin_rules` table (e.g. via a migration or a manual fix) need a `Reload` RPC call to take effect.
corpus/services/sentinel.md:158-158	bullet	Changes made through Sentinel's own RPC methods (`OrgAddUserToRole` etc.) are picked up immediately.
corpus/services/skiller.md:66-67	bullet	**Localization**: per-skill / per-job-role translations (`skill_translations`, `job_role_translations`) across 8 `ContentLanguage`s carried over into `app`.
corpus/services/skillpath.md:64-68	bullet	**Session model.** The engine owns a hierarchical session: `SkillPathSession → ChapterSession → StepSession`, each carrying `progress` (0–100), `status` (`pending|active|completed|archived`), `duration`, `version`, and `*_at` timestamps (shared via `SkillPathMixin`).
corpus/services/skillpath.md:64-68	bullet	The player skill-path page reads this runtime via `getOrCreateSkillPathSession` — a **get-OR-create** that auto-materializes a blank `pending` session on first read, so an unseeded skill path renders empty, not 404.
corpus/services/skillpath.md:70-74	bullet	**Event-driven step completion (unchanged).** The engine subscribes to the **jobsimulation Redis stream** (start/end events) to update `StepSession` status when a simulation completes, and additionally calls jobsimulation **in-process** (`GetSessions`) on session create/upgrade to reconcile already-completed simulations.
corpus/services/skillpath.md:70-74	bullet	It publishes `EventSkillPathSessionUpdated` + `EventChapterStepSessionCompleted` to the `skillpath` Redis stream (consumed by `app`).
corpus/services/skillpath.md:70-74	bullet	All of this now runs in-process inside `app`.
corpus/services/storage.md:5-5	paragraph	Storage is the **centralized file/blob service** for the platform.
corpus/services/storage.md:42-42	paragraph	It has two parallel storage managers — **private** (internal files, recordings, documents) and **public** (CDN-served assets) — each backed by its own S3 bucket and accessed by namespace + UUID.
corpus/services/storage.md:59-59	table-row	| Private | `STORAGE_S3_BUCKET` | Internal data: session recordings, documents.
corpus/services/storage.md:60-60	table-row	| Public | `STORAGE_S3_PUBLIC_BUCKET` | Public assets served via CloudFront at `media.<root_domain>`. |
corpus/services/storage.md:105-105	paragraph	Namespaces are arbitrary strings (e.g., `jobsimulation`, `assets`).
corpus/services/storage.md:135-135	table-row	| `PutObject` | `data`, `metadata`, `namespace` | `key (UUID)`, `namespace` |
corpus/services/storage.md:136-136	table-row	| `GetObject` | `key`, `namespace` | `object (data + metadata)` |
corpus/services/storage.md:137-137	table-row	| `GetPresignedUrl` | `key`, `namespace`, `expiry_seconds` | `url` (default 15 min) |
corpus/services/storage.md:143-143	table-row	| `PutPublicObject` | `data`, `metadata`, `namespace` | `key (UUID)`, `namespace` |
corpus/services/storage.md:144-144	table-row	| `GetPublicObject` | `key`, `namespace` | `object (data + metadata)` |
corpus/services/storage.md:176-176	bullet	**Downstream**: AWS S3 (production), CloudFront (public bucket), `colony` shared library, `proto` for RPC contracts
corpus/services/storage.md:177-177	bullet	**No outbound RPC** to other platform services — storage is a leaf
corpus/services/storage.md:209-209	paragraph	The `storage sync` CLI moves objects between two configured backends (e.g., local FS → S3 for an initial seed):
corpus/services/storage.md:226-226	table-row	| `PORT` | `8300` | HTTP health port (binary default 8080, overridden in compose) |
corpus/services/storage.md:227-227	table-row	| `RPC_PORT` | `8301` | Connect-RPC port (binary default 8081, overridden in compose) |
corpus/services/storage.md:230-230	table-row	| `AWS_REGION` / `AWS_DEFAULT_REGION` | `eu-west-1` | AWS region (EU-first) |
corpus/services/storage.md:232-232	table-row	| `SERVICE_NAME` | `storage` | Logging label |
corpus/services/storage.md:233-233	table-row	| `SENTRY_DSN` | (empty) | Sentry error tracking |
corpus/services/studio-desk.md:5-5	paragraph	**Studio-Desk** is a specialized web application that empowers content creators to design job simulations and learning experiences.
corpus/services/studio-desk.md:22-22	table-row	| **Port(s)** | 9100 (frontend), 9000 (backend) - configurable via `.env` |
corpus/services/studio-desk.md:23-23	table-row	| **Authentication** | Clerk |
corpus/services/studio-desk.md:24-24	table-row	| **Repository** | Local `studio-desk/` (sibling repo cloned by `make init`) |
corpus/services/studio-desk.md:28-28	paragraph	Studio-Desk is a **full-stack TypeScript application** with:
corpus/services/studio-desk.md:126-126	bullet	**Backend AI layer**: multi-provider chain (`AI_PROVIDER_CHAIN`, default `azure-openai,openai`) across Azure OpenAI / OpenAI / Anthropic with circuit-breaker failover (timed-out providers rotate to end of chain).
corpus/services/studio-desk.md:126-126	bullet	Four model tiers (`thinking_slow`, `thinking_fast`, `fast`, `instant`); default tier configurable via `AI_DEFAULT_TIER` (`.env.example` uses `fast`; in-code fallback is `thinking_fast`).
corpus/services/studio-desk.md:126-126	bullet	Tier defaults: OpenAI/Azure `gpt-5.2` / `gpt-5-mini` / `gpt-5-nano`; Anthropic `claude-opus-4-5` / `claude-sonnet-4-5` / `claude-haiku-4-5`.
corpus/services/studio-desk.md:137-137	bullet	Design blueprint in Studio-Desk
corpus/services/studio-desk.md:140-140	bullet	Generated content returns to CMS/Directus
corpus/services/studio-desk.md:161-161	paragraph	Generated types are stored in `app/services/__generated__/` and provide type-safe GraphQL operations.
corpus/services/studio-desk.md:161-161	paragraph	GraphQL documents live in `app/services/graphql/` (`queries.ts`, `mutations.ts`).
corpus/services/studio-desk.md:165-165	paragraph	Studio-Desk works with these primary entities (stored via CMS → Directus):
corpus/services/studio-desk.md:175-175	bullet	Node.js v24+ (per `package.json` engines and `node:24-alpine` Docker base)
corpus/services/studio-desk.md:177-177	bullet	Clerk account (for authentication)
corpus/services/studio-desk.md:178-178	bullet	Access to the platform GraphQL endpoint (`backend` on `:8082`; the router no longer runs locally)
corpus/services/studio-desk.md:179-179	bullet	Access to the **cms domain** — served by that same `backend`, not a separate service
corpus/services/studio-desk.md:224-224	bullet	**Generate GraphQL types** (when schema changes):
corpus/services/studio-desk.md:235-235	bullet	Frontend: `http://localhost:9100` (Vite dev server, configurable via `FRONTEND_PORT`)
corpus/services/studio-desk.md:236-236	bullet	Backend: `http://localhost:9000` (Express API, configurable via `PORT`)
corpus/services/studio-desk.md:238-240	bullet	**Access the application**: - Development: `http://localhost:9100` (direct frontend access) - Backend API: `http://localhost:9000` (API server with `/api` routes)
corpus/services/studio-desk.md:272-272	paragraph	Serves the app from `http://localhost:9000` (backend serves frontend static files, or configured via `PORT`).
corpus/services/studio-desk.md:286-286	paragraph	Docker images are built automatically on tag push.
corpus/services/studio-desk.md:291-291	bullet	**Authentication**: Clerk (shared with main app)
corpus/services/studio-desk.md:292-292	bullet	**Data Layer**: GraphQL → the cms domain → Directus
corpus/services/studio-desk.md:304-304	bullet	Studio-Desk **creates** simulation blueprints
corpus/services/studio-desk.md:306-306	bullet	Communication via shared CMS/Directus storage
corpus/services/studio-desk.md:310-310	paragraph	**GraphQL errors**: Ensure **`backend`** is up on port 8082 (there is no router service locally any more):
corpus/services/studio-desk.md:317-317	paragraph	**Clerk authentication issues**: Verify Clerk keys in `.env` and ensure sign-in URLs match.
corpus/services/studio-desk.md:321-321	paragraph	**Copilot not working**: Check that `AI_PROVIDER_CHAIN` is set and the corresponding provider key(s) exist (`AI_OPENAI_API_KEY`/`OPENAI_KEY`, `AI_AZURE_KEY`, or `AI_ANTHROPIC_API_KEY`).
corpus/services/studio-desk.md:348-352	paragraph	**PM view.** In a demo, the studio's AI copilot / builder GENERATE returned a **500 on `POST /api/ai/completion`** — the studio backend held **no AI-provider key**.
corpus/services/studio-desk.md:348-352	paragraph	M252 fixes it with one wire: the demo studio container now **also reads studio-desk's own clone `.env`**, which carries the studio's AI-provider keys.
corpus/services/studio-desk.md:348-352	paragraph	The **auth model is unchanged** — the demo studio stays the **Clerkenstein-authenticated hero** (a logged-in org-admin hero passes the studio's `checkEnterpriseAndAdmin` gate); M252 touches **only** the AI-provider wiring.
corpus/services/studio-desk.md:410-413	paragraph	So the multi-second blank a presenter saw on a demo was **not** a slow render — it was **paint ordering**: the shell is built *behind* the awaits, and the dominant await (`canAccess`) 404s and burns ~3.9 s retrying.
corpus/services/studio-desk.md:415-416	paragraph	**The M253 fix (demo only, zero platform edits):** two sha-pinned demopatches on the M249 `build_frontend_studio_desk` ladder —
corpus/services/studio-desk.md:418-422	bullet	**`studio-desk-shell-first-paint`** injects the `.page-skeleton` DOM (header + sidemenu + content) **synchronously right after `preloadCriticalCSS()`**, *before* any await, so the dark shell paints from **CSS+DOM with zero network**.
corpus/services/studio-desk.md:418-422	bullet	It is **auth-independent** (it paints before `canAccess`), and **de-dups automatically**: `PageWrapper#init` wipes `document.body.innerHTML` and rebuilds its own skeleton, so the early shell is seamlessly replaced — no double skeleton.
corpus/services/studio-desk.md:423-424	bullet	**`studio-desk-no-thirdparty`** no-ops `Sentry.init` + `posthog.init` on the demo host (no reachable GlitchTip / no PostHog project on a Clerk-free demo).
corpus/services/studio-room.md:18-18	bullet	Takes a simulation blueprint (created in Studio-Desk)
corpus/services/studio-room.md:25-25	paragraph	Since **cms-in-app v8.0** it is embedded inside the **`app` (backend) container** — pulled into the image by CI (`additional_repo`, app v1.360.1) — and is triggered by an Asynq task rather than run directly by users; the Go side shells out to it as a subprocess when a generation job is enqueued.
corpus/services/studio-room.md:25-25	paragraph	Before the merge it rode in the cms container at `cms/studio/`.
corpus/services/studio-room.md:36-36	table-row	| **AI Providers** | OpenAI, Azure OpenAI, Anthropic |
corpus/services/studio-room.md:37-37	table-row	| **Repository** | `anthropos-studio-room`, pulled into the **`app`** image by CI (was cloned into the cms repo at `cms/studio/` before cms-in-app) |
corpus/services/studio-room.md:116-119	bullet	**AI Setup**: - Initialize AI services (OpenAI, Azure, Anthropic) - Configure model parameters (temperature, max tokens, thinking mode) - Set up usage tracking
corpus/services/studio-room.md:123-123	paragraph	The generation is orchestrated by **media-specific generator agents** (e.g., `SimulationGenerator`).
corpus/services/studio-room.md:143-143	paragraph	**Generation Modes** (`GenMode` enum):
corpus/services/studio-room.md:144-144	bullet	`GenMode.FAST`: Lightweight / low-latency steps
corpus/services/studio-room.md:145-145	bullet	`GenMode.STRICT`: Tightly-constrained, deterministic output
corpus/services/studio-room.md:146-146	bullet	`GenMode.EXECUTION`: Default mode (`DEFAULT = EXECUTION`)
corpus/services/studio-room.md:147-147	bullet	`GenMode.CREATIVE`: For content requiring creativity (dialogue, scenarios)
corpus/services/studio-room.md:148-148	bullet	`GenMode.REASONING`: For steps that benefit from deeper reasoning
corpus/services/studio-room.md:152-152	bullet	**Retry mechanism**: Auto-retry failed steps (configurable `max_retries`)
corpus/services/studio-room.md:164-164	paragraph	`--media`, `--simid`, and `--target` are all required for `postgen.py`.
corpus/services/studio-room.md:172-172	table-row	| `translation` | Translate to multiple languages | `internal_localization.json` inside the bundle |
corpus/services/studio-room.md:251-251	paragraph	AI models are configured per generation mode in `configs/{env}_config.ini`.
corpus/services/studio-room.md:317-317	paragraph	Generation state is checkpointed after each step to the configured `worklog_path` (set to `workspace/trace/` in the shipped configs; the in-code fallback is a literal `worklog/` only if unset).
corpus/services/studio-room.md:319-319	bullet	`{simid}_pre_generation.json` — the original request
corpus/services/studio-room.md:320-320	bullet	`{simid}_task_state.json` — the public task state
corpus/services/studio-room.md:322-322	paragraph	plus a per-run `{simid}_usage.json` usage trace.
corpus/services/studio-room.md:336-336	bullet	Python 3.9+ (runtime image when embedded in the `app` container: `python:3.11-slim`; it was the cms container before cms-in-app)
corpus/services/studio-room.md:337-337	bullet	AI API keys (OpenAI, Anthropic, or Azure)
corpus/services/studio-room.md:351-351	paragraph	**Requirements** (unpinned in `requirements.txt`):
corpus/services/studio-room.md:431-431	bullet	Receives a GraphQL mutation requesting generation
corpus/services/studio-room.md:434-434	bullet	Reads the resulting ZIP and imports it into Directus
corpus/services/studio-room.md:437-437	bullet	**Input**: Blueprints created in Studio-Desk, passed in by CMS
corpus/services/studio-room.md:438-438	bullet	**Output**: Generated content imported into Directus by CMS
corpus/services/studio-room.md:439-439	bullet	**Workflow**: Desk designs → CMS enqueues → Room generates → CMS imports into Directus
corpus/architecture/README.md:3-3	paragraph	This directory contains all documentation related to the Anthropos platform architecture.
corpus/architecture/ai_architecture.md:3-3	paragraph	This document describes the AI model inventory, provider routing, voice engine, recording architecture, and cost tracking across the Anthropos platform.
corpus/architecture/ai_architecture.md:15-17	paragraph	**⚠️ There is no ordered EU-first fallback chain.** The corpus published one for several releases ("Azure → Bedrock → Mistral → OpenAI → Anthropic"); **no such ladder exists in the code**, and **Mistral is not in the AI manager at all**.
corpus/architecture/ai_architecture.md:22-24	bullet	**The caller picks the vendor.** `ChatCompletion` / `Response` take a `vendor AIVendor` argument and hand it to `getClient` (`:259-289`) — a plain `switch` over the four vendor consts at `:30-33` (`azure`, `openai`, `anthropic-aws`, `anthropic`).
corpus/architecture/ai_architecture.md:25-27	bullet	**`azure` resolves to the EU deployment by default**, swapped to the US one only when the PostHog flag `flag_use_azure_us` evaluates true; a *failed* flag lookup logs and **keeps the EU client**.
corpus/architecture/ai_architecture.md:28-29	bullet	**`anthropic-aws` and `anthropic` both return the same Bedrock client**, constructed with `config.WithRegion("eu-west-1")`.
corpus/architecture/ai_architecture.md:28-29	bullet	There is **no** US-direct Anthropic branch in this manager.
corpus/architecture/ai_architecture.md:30-32	bullet	**The one automatic fallback is 429-only.** `isThrottlingError` sets `vendor = Openai` for the next retry attempt — i.e. **direct US OpenAI**.
corpus/architecture/ai_architecture.md:44-48	paragraph	**EU data residency still holds — but by a different mechanism than the ladder implied.** The posture rests on the *default clients* being EU-resident (Azure EU, Bedrock `eu-west-1`), not on a policy that walks EU options before US ones.
corpus/architecture/ai_architecture.md:44-48	paragraph	**Two** levers inside the manager can send a request outside the EU, **neither a region-health failover**: the `flag_use_azure_us` flag, and the 429 retry (which goes straight to direct OpenAI **without** trying another EU provider first).
corpus/architecture/ai_architecture.md:73-76	paragraph	The **outcome** claim — direct US OpenAI, first attempt, no error condition — survives unchanged.
corpus/architecture/ai_architecture.md:95-95	table-row	| **Transcription** | GPT-4o Transcribe | Azure EU (US via `flag_use_azure_us`) |
corpus/architecture/ai_architecture.md:96-96	table-row	| **Embeddings** | Text Embedding 3 Small | OpenAI |
corpus/architecture/ai_architecture.md:111-111	bullet	A single `ai.AI` interface across providers (OpenAI, Azure, Anthropic, Bedrock, Mistral)
corpus/architecture/ai_architecture.md:112-112	bullet	Per-provider client constructors that return provider token counts (`MetaData.Usage`)
corpus/architecture/ai_architecture.md:122-122	table-row	| **jobsimulation domain** *(in `app`)* | Simulation conversations (chat + voice), document analysis, code evaluation |
corpus/architecture/ai_architecture.md:124-124	table-row	| **cms domain** *(in `app`)* | Content generation, similarity matching, AI video (HeyGen), **and runs the full simulation generation pipeline** (Python studio-room embedded — see below) |
corpus/architecture/ai_architecture.md:125-125	table-row	| **Studio-Desk** | Copilot AI assistant for content authoring (multi-provider chain: Azure OpenAI / OpenAI / Anthropic via `AI_PROVIDER_CHAIN`) |
corpus/architecture/ai_architecture.md:126-126	table-row	**Runs as a subprocess inside the `app` (backend) container** since cms-in-app — the `anthropos-studio-room` project is pulled into the image by CI. |
corpus/architecture/ai_architecture.md:137-137	table-row	| FAST | azure / gpt-5-mini | azure / gpt-5-mini | none |
corpus/architecture/ai_architecture.md:138-138	table-row	| STRICT | azure / gpt-5-mini | azure / gpt-5-mini | none |
corpus/architecture/ai_architecture.md:139-139	table-row	| EXECUTION | azure / gpt-5.4 | azure / gpt-5.4 | none |
corpus/architecture/ai_architecture.md:140-140	table-row	| CREATIVE | azure / gpt-5.4 | azure / gpt-5.4 | low |
corpus/architecture/ai_architecture.md:141-141	table-row	| REASONING | azure / gpt-5.4 | azure / gpt-5.4 | medium |
corpus/architecture/ai_architecture.md:155-155	bullet	**Model**: Text Embedding 3 Small (OpenAI), 1536-dim
corpus/architecture/ai_architecture.md:156-156	bullet	**Data**: `public.skill_embeddings` = **42,790** rows and `public.job_role_embeddings` = **18,919** rows over the *public* taxonomy (`organization_id IS NULL`, measured 2026-06-29)
corpus/architecture/ai_architecture.md:157-157	bullet	**Process**: RAG matches user input to taxonomy using OpenAI (Azure EU) or Anthropic (Bedrock EU)
corpus/architecture/ai_architecture.md:158-158	bullet	**Caching**: Redis for frequent matches
corpus/architecture/ai_architecture.md:169-169	paragraph	As of 2026-Q2 (skiller migrations `20260417103036` and `20260417120309`), embeddings are stored in **dedicated tables**, not as columns on the entity tables.
corpus/architecture/ai_architecture.md:169-169	paragraph	Since the skiller→app merge (July 2026) these tables live in the **`public` schema** owned by `app` (ported from the legacy `skiller` schema):
corpus/architecture/ai_architecture.md:195-195	paragraph	The primary voice engine uses **LiveKit rooms** with **OpenAI GPT Realtime** agents:
corpus/architecture/ai_architecture.md:210-210	bullet	**Transcript**: Generated from LiveKit conversation events
corpus/architecture/ai_architecture.md:211-211	bullet	**Configuration**: Voice engine is selectable per simulation in CMS (`livekitgptrealtime`)
corpus/architecture/ai_architecture.md:217-217	table-row	| `elevenlabs` | Active (legacy default) | ElevenLabs conversational agents; still used by the call/reply pipeline (`getJobSimulationCallSignedUrl` / `getJobSimulationCallConversationToken`) and transcript improvement |
corpus/architecture/ai_architecture.md:218-218	table-row	| `gptrealtime` | Deprecated | Direct OpenAI Realtime without LiveKit |
corpus/architecture/ai_architecture.md:254-254	table-row	| **LiveKit** | Voice only | MP3 | Audio transcript and voice recording |
corpus/architecture/ai_architecture.md:255-255	table-row	| **AWS Chime SDK** | Camera + screensharing + mic | Composited MP4 (grid view) | Full video record of simulation |
corpus/architecture/ai_architecture.md:263-263	bullet	**Load**: Simulation definition fetched from CMS (actors, tasks, rubrics, AI model config per sequence)
corpus/architecture/ai_architecture.md:272-273	paragraph	The corpus asserted a single *"default: `gpt-5` via Azure"* for several releases.
corpus/architecture/ai_architecture.md:272-273	paragraph	**`gpt-5` is not a default in any of the three places a default is applied** — measured at `app` HEAD:
corpus/architecture/ai_architecture.md:300-300	paragraph	The inputs to it are not** — measured at `app` HEAD, M257x iter-38:
corpus/architecture/alignment_testing.md:7-10	paragraph	The motivating case is **Clerkenstein** (v1.0 milestone M1) — a drop-in mock of Clerk that the platform talks to *thinking it's the real Clerk*.
corpus/architecture/alignment_testing.md:12-15	paragraph	**Alignment testing** is how we measure it. An alignment test is a **differential test**: it feeds the *same* input to two engines — the **source** (the canonical one) and the **mirror** — and checks that they behave the same.
corpus/architecture/alignment_testing.md:23-23	table-row	| **alignment** | Do *two independent implementations* behave identically for the same input? |
corpus/architecture/alignment_testing.md:25-26	paragraph	The framework is **engine-agnostic and reusable** — it knows nothing about Clerk.
corpus/architecture/alignment_testing.md:25-26	paragraph	Clerkenstein is just its first consumer.
corpus/architecture/alignment_testing.md:28-31	paragraph	**It does not live in rosetta.** The harness is the `alignment/` section of **`rosetta-extensions`** (Go module `anthropos.dev/alignment`) — authored in the `.agentspace/rosetta-extensions/` copy, tagged, and consumed per-stack at that tag. rosetta ships this doc and the `/align-dna` + `/align-run` skills, and **no executable alignment code at all**; see *Where things live* below for the full split.
corpus/architecture/alignment_testing.md:35-38	bullet	There are two roles: - **Source target** — the canonical engine being mirrored, pinned to a version (e.g. Clerk's `clerk-sdk-go/v2 @ v2.6.0`). - **Mirror target** — our reimplementation (e.g. Clerkenstein).
corpus/architecture/alignment_testing.md:39-39	bullet	*(Axis 1.)* e.g. `CreateOrganization`.
corpus/architecture/alignment_testing.md:42-44	bullet	**Gene** — one **(capability × variant)** pair: the atomic unit of alignment.
corpus/architecture/alignment_testing.md:42-44	bullet	Its **id** is `<Capability>/<variant>` (e.g. `CreateOrganization/duplicate-name`) and is the join key across the DNA, the goldens, the outcomes, the test subtests, and the report.
corpus/architecture/alignment_testing.md:45-46	bullet	**Alignment DNA** — the officially-enumerated **complete set of genes** for a source target at a version.
corpus/architecture/alignment_testing.md:47-48	bullet	**Alignment score** — the weighted percentage of genes whose mirror outcome matches the source outcome. 100% = behaviorally indistinguishable across the entire DNA.
corpus/architecture/alignment_testing.md:55-56	paragraph	Alignment tests are **marked** so they can be parsed, listed, counted, and run as their own suite — separate from unit and integration.
corpus/architecture/alignment_testing.md:58-59	bullet	A **build tag** `//go:build alignment` gates the suite.
corpus/architecture/alignment_testing.md:58-59	bullet	Plain `go test ./...` skips it (the same separation unit/integration tests use); `go test -tags alignment ./...` runs it.
corpus/architecture/alignment_testing.md:60-61	bullet	The suite reports **one subtest per gene**, named by gene id, so `go test -tags alignment -json` maps results 1:1 back to the DNA — that's the "countable / parseable" property.
corpus/architecture/alignment_testing.md:68-68	bullet	`go test -tags alignment` — the developer/CI ergonomic (native runner, per-gene subtests).
corpus/architecture/alignment_testing.md:102-102	table-row	| `capability.criticality` | `critical` / `standard` / `optional` → default gene weight `3` / `2` / `1`.
corpus/architecture/alignment_testing.md:103-103	table-row	| `variant.operator` | The equivalence test (below). |
corpus/architecture/alignment_testing.md:104-104	table-row	| `variant.input` | The input passed to the runner for this gene (inlined JSON). |
corpus/architecture/alignment_testing.md:105-105	table-row	| `variant.normalize` | Dot-paths zeroed before comparison (only for `operator: normalized`). |
corpus/architecture/alignment_testing.md:106-106	table-row	| `variant.weight` | Optional explicit weight; overrides the criticality default. |
corpus/architecture/alignment_testing.md:123-123	table-row	| `error_class` | only the error class is compared (both error the same way / both succeed); value ignored. |
corpus/architecture/alignment_testing.md:133-140	paragraph	A runner is invoked as `RUNNER --target {source|mirror} --dna PATH` and prints, to stdout, a JSON map of gene id → outcome: ```json { "Greet/padded-name": { "value": "Hello, El Nino!", "error_class": null }, "Add/overflow":       { "value": null, "error_class": "overflow" } } ``` `value` is the capability's normalized return (any JSON, or `null` on error); `error_class` is a stable short string naming the error kind (or `null` on success).
corpus/architecture/alignment_testing.md:142-145	paragraph	The toy's is `examples/toy/cmd/toyrun`; **Clerkenstein ships its own** in the `clerkenstein/` **section of the `rosetta-extensions` monorepo** — not a repo of its own (`clerkenstein/alignment/cmd/{clerkrun,jsfapirun,expressrun,deployrun,multirun}`).
corpus/architecture/alignment_testing.md:149-150	paragraph	A real source like Clerk is a **live SaaS** — you can't hit it freely, offline, or deterministically.
corpus/architecture/alignment_testing.md:152-153	bullet	`alignctl capture` runs the runner with `--target source` and writes each gene's outcome to a **golden** file under `golden/<Capability>/<variant>.json`.
corpus/architecture/alignment_testing.md:160-160	bullet	**gene weight** = `variant.weight` if set, else the capability's criticality default (`3/2/1`).
corpus/architecture/alignment_testing.md:183-191	paragraph	`GET /v1/users/{id}` — which next-web's server-side `currentUser()` calls on **every authenticated render** — had no capability in any of the five DNAs, so Clerkenstein scored **100% critical / 100% overall / 0 divergences while its fake BAPI returned the wrong human for every hero**.
corpus/architecture/alignment_testing.md:198-198	table-row	| **What enforces it** | `DNA.Validate()` **rejects** a consumed endpoint that names no capability (or names one that doesn't exist).
corpus/architecture/alignment_testing.md:199-199	table-row	`gate.sh` runs it on every gate, **before** the score — but with **`--if-declared`** (see the row below), which changes what the *gate* enforces. |
corpus/architecture/alignment_testing.md:201-201	table-row	| **The escape hatch** | `covered_by` names a capability on **another** surface's DNA (e.g. `clerk-express-1:ClerkClientBAPI/get-organization`).
corpus/architecture/alignment_testing.md:205-207	bullet	It cannot discover consumption nobody wrote down.** This is a strictly weaker claim than the old "every consumed endpoint is present," and it is the true one. Adding a consumer without adding its endpoint to `consumed_surface` re-opens exactly the M218 hole.
corpus/architecture/alignment_testing.md:208-215	bullet	**Only `clerk-2.6.0` (the fake BAPI's HTTP surface — where the failure happened) declares a `consumed_surface` today.** The other four DNAs declare none, so the check **does not bind for them**; `gate.sh` prints a loud `NO COVERAGE CLAIM` warning on every run for each.
corpus/architecture/alignment_testing.md:219-224	paragraph	**The deeper lesson (M218 D15).** Three genes *did* name identity — `ExtractIdentity`, `Me`, `DeployIdentity` — and all three assert the variant **`universal-user`**: the stub itself.
corpus/architecture/alignment_testing.md:219-224	paragraph	**When a golden is captured from a mirror rather than derived from the source's contract, it ratifies whatever the mirror does — including its bugs.** The fix is two-sided assertions: `GetUser` now asks for hero **A** *and* hero **B** and requires **A ≠ B**, which no single stub identity can satisfy.
corpus/architecture/alignment_testing.md:230-233	bullet	**`/align-dna`** — *build & update alignment targets.* Given a source framework + version: pull the pinned source, enumerate the **consumed** capabilities and their variants, emit/update the DNA, diff the DNA across source versions, and scaffold test/golden stubs from it. This is where the capability × variant enumeration — the genome authoring — happens.
corpus/architecture/alignment_testing.md:234-236	bullet	**`/align-run`** — *measure alignment of two targets.* Given a DNA + a source version + a mirror: capture or replay the source goldens, run the mirror, compute the score, and surface the divergence report.
corpus/architecture/alignment_testing.md:240-240	paragraph	The executable harness (`rosetta-extensions/alignment/cmd/alignctl`):
corpus/architecture/alignment_testing.md:269-281	bullet	**"Clerkenstein is at 100%."** The Go surface sat at **97.2%** for a full milestone, on purpose: `MembershipOrgIdentity/real-org-eid` was a **deliberately RED** `standard` gene (M218 **D16**) — the fake BAPI fabricated the org's external id instead of returning the roster's real UUID.
corpus/architecture/alignment_testing.md:269-281	bullet	The BAPI now reports the roster's real `org_eid` (`Store.SeedOrgIdentity`/`LookupOrgEid`, wired from the roster at `cmd/fake-bapi`), behind a three-tier ladder — roster eid → demo-org eid → the historical stub — so the alignment runner (which mounts no roster) stays byte-identical and **exactly one gene moved**.
corpus/architecture/alignment_testing.md:269-281	bullet	Go surface: **97.2% → 100.0% / 100% critical, 27/27, no divergences.** The gene **stays in the DNA** as a permanent fence over the whole wiring path (roster JSON → `LoadRoster` → `seedRosterMemberships` → store → `organizationWithEid` → HTTP).
corpus/architecture/alignment_testing.md:295-298	paragraph	`rosetta-extensions/alignment/examples/toy/` is a self-contained proof: a `source` engine and a `mirror` engine that match **except for one intentional divergence** (`Greet/padded-name` — the source normalizes input whitespace, the mirror forgets to).
corpus/architecture/alignment_testing.md:315-316	paragraph	`go test -tags alignment ./examples/toy/...` passes (the toy gate is 80% overall / 100% critical, and the divergence is a non-critical gene) while logging the tolerated divergence.
corpus/architecture/alignment_testing.md:320-324	bullet	**M1 (Clerkenstein backend mirror)** runs the loop: `/align-dna` authors the **Clerk DNA** (`clerk@2.6.0` genome), then the build drives `/align-run`'s score up to its **exit gate** (100% critical / ≥95% overall) by closing diverging genes.
corpus/architecture/alignment_testing.md:320-324	bullet	The Clerk DNA, goldens, alignment tests, mirror, and runner all live in the **`clerkenstein/` section of the `rosetta-extensions` monorepo** — *not* a repo of its own, and not here.
corpus/architecture/alignment_testing.md:380-382	bullet	Its runner (`deployrun`) drives the **real platform consumer** (colony) the way `expressrun` drives the real `@clerk/express`.
corpus/architecture/alignment_testing.md:390-391	paragraph	Clerkenstein now drives **five DNAs via five runners** (`clerkrun`, `jsfapirun`, `expressrun`, `deployrun`, `multirun`) through the one `alignctl` — the clearest evidence the framework is surface-generic.
corpus/architecture/alignment_testing.md:395-405	paragraph	The behavioural DNAs (`clerk@2.6.0`, `clerk-js-5`, `clerk-express-1`, `clerk-multi-1`) measure **behavioural fidelity**: given an input, the mirror produces the same *outcome* as the source.
corpus/architecture/alignment_testing.md:395-405	paragraph	That is necessary but **not** the same as **deployability** — that the mock can be *injected into the running platform's exact consumption shape*. v1.0 proved the first and the v1.0 narrative ("a stand-in the platform can't tell apart") implied the second; **M3 (the first real demo bring-up) was the first test of the second, and it required building things the alignment never covered** — a vendored `colony` with the disarmed provider in the *concrete* package the platform builds against (not the standalone `authn.Provider` the Go DNA tested), and runnable fake-server binaries (the DNAs tested in-process `http.Handler`s).
corpus/architecture/alignment_testing.md:395-405	paragraph	Neither the tool nor a DNA was *buggy*; the **scope** of the genes didn't cover deployment, and a couple of seams were tested against *idealized interfaces* rather than the platform's actual ones (the Go authn DNA even pinned a different `colony` version than production).
corpus/architecture/alignment_testing.md:407-415	paragraph	The fix is a **deployment/injection dimension**, not a patch: the `clerk-deploy-1` DNA + `deployrun` runner score whether Clerkenstein's injection **artifact** (`deploy/colony-authn`, the disarmed `colony/authn/provider/clerk` drop-in) **compiles against the platform's real `colony`** and satisfies its contract (`clerk.NewProvider(...).GetUser` accepts a Clerkenstein token, rejects garbage/expired) — the runner *compiling* against real colony **is** the contract check, so a colony bump that breaks the seam turns the gate RED instead of surfacing during a demo.
corpus/architecture/alignment_testing.md:407-415	paragraph	The heavier end-to-end gene (a built platform app accepting a token over HTTP) is **deployment-gated** — proven once in M3, run where a demo stack exists — like the express gate's Node dependency.
corpus/architecture/alignment_testing.md:419-422	paragraph	v1.0 exercised the framework on **behaviour** (does the mirror return the same outcome as Clerk?) and M3 added **deployability** (does the injection artifact compile against the platform?).
corpus/architecture/alignment_testing.md:419-422	paragraph	This is the discipline that keeps the seeder fleet (M7c) from silently breaking when the platform's data model drifts.
corpus/architecture/alignment_testing.md:429-429	table-row	| Source = the live engine; **input → output** (two-sided) | **Source = the platform's live schema, introspected**; **output → schema** (one-sided — no input to compare) |
corpus/architecture/alignment_testing.md:433-438	paragraph	The data-DNA does **two jobs**: (1) it **enumerates the seedable surfaces** — the machine-readable *catalog of seeders to build* that drives M7c (4 seeded in M7a, ~6 planned); and (2) it is the **conformance gate + drift detector** — `introspect` captures each seeded surface's shape from a live (migrated) stack as the contract, `measure` runs the structural operators against a seeded stack (a weighted `Overall` + an unweighted `Critical`, exactly like the behavioural score), and `diff` re-introspects to flag drift (exit-code contract `0 none / 1 moved / 3 usage`, mirroring `drift-check.sh`).
corpus/architecture/alignment_testing.md:440-445	paragraph	**Where it breaks:** the operators are structural (FK/constraint/type), not value-equivalence, and the measurement is one-sided (seeder output vs the schema, not source vs mirror) — which is why this is a **separate harness** (`rosetta-extensions/stack-seeding/dna/`, the `datadna` CLI), not a new `alignctl` runner.
corpus/architecture/alignment_testing.md:440-445	paragraph	It connects directly to the stack's Postgres (offset port), never linking the platform.
corpus/architecture/alignment_testing.md:468-469	paragraph	The snapshot dimension extends the **same** data-DNA harness (`rosetta-extensions/stack-seeding/dna/`, the `datadna` CLI) rather than spawning a third one — it shares the gene/score/criticality machinery.
corpus/architecture/alignment_testing.md:471-474	bullet	`Coverage()` counts seeded **OR** snapshot-seeded over the non-waived denominator;
corpus/architecture/alignment_testing.md:510-511	paragraph	rosetta documents the discipline and ships the skills; **all executable machinery — the reusable harness *and* each mirror — lives in rosetta-extensions** and is consumed per-stack at a tag:
corpus/architecture/alignment_testing.md:515-515	table-row	| this doc — the alignment test class + method | `alignment/` — the reusable harness (`alignctl` + the toy) |
corpus/architecture/alignment_testing.md:516-516	table-row	| `/align-dna`, `/align-run` skills | each **mirror** section (e.g. `clerkenstein/`) — the mirror engine itself |
corpus/architecture/alignment_testing.md:517-517	table-row	| | the source's DNA(s) (the genome — e.g. Clerkenstein ships **five**, in `clerkenstein/alignment/dna/`) |
corpus/architecture/alignment_testing.md:518-518	table-row	| | the alignment tests + goldens + the engine's runner(s) (one per surface — `clerkrun`/`jsfapirun`/`expressrun`/`deployrun`/`multirun`) + the mirror's own `gate.sh`/`drift-check.sh` |
corpus/architecture/alignment_testing.md:520-528	paragraph	rosetta never contains executable alignment code — neither a specific mirror's source nor the reusable harness.
corpus/architecture/alignment_testing.md:520-528	paragraph	Both are sections of the **rosetta-extensions** monorepo, which carries two clone roles: an **authoring copy** at `.agentspace/rosetta-extensions/` (spawned on demand — where the `alignment/` harness, DNAs, goldens, and runners are built, tested, and **tagged**), and **per-stack consumption copies** `stack-*/rosetta-extensions @ <tag>` (each stack consumes the tooling at a pinned tag).
corpus/architecture/alignment_testing.md:520-528	paragraph	Policy: all executable stack tooling — the `alignment/` harness, seeders, injection, and each mirror — lives in rosetta-extensions, built and tagged in the authoring copy, then consumed per-stack; it is never scattered in the rosetta corpus or authored ad-hoc inside a stack dir. rosetta stays a read-only doc corpus plus dev-env skills.
corpus/architecture/alignment_testing.md:542-542	paragraph	Stdlib-only Go (module `anthropos.dev/alignment`) — builds and runs offline.
corpus/architecture/architecture_overview.md:6-6	paragraph	This document provides a high-level overview of the Anthropos platform architecture.
corpus/architecture/architecture_overview.md:10-10	paragraph	Anthropos is a B2B SaaS skills intelligence platform that helps companies **map, verify, and develop skills** using AI-powered workplace simulations.
corpus/architecture/architecture_overview.md:21-21	bullet	**CMS**: **The content layer** — owns the authored content & definitions (skill paths, simulation blueprints, the library) by wrapping Directus, plus the embedded Studio-Room AI content generation pipeline (Python — pulled into the **`app`** image by CI since cms-in-app; it rode in the cms container before the merge)
corpus/architecture/architecture_overview.md:36-38	bullet	**Studio Services**: Specialized tools for content creation: *   **Studio-Desk**: Web app where creators design job simulations *   **Studio-Room**: AI pipeline that generates content from those designs.
corpus/architecture/architecture_overview.md:36-38	bullet	**Embedded inside the `app` (backend) image** since cms-in-app — it rode in the cms container before that, and was never a standalone deployment.
corpus/architecture/architecture_overview.md:41-41	bullet	**Frontend**: Next.js **16** applications deployed on Vercel
corpus/architecture/architecture_overview.md:60-60	bullet	**Database**: PostgreSQL RDS (Multi-AZ) with Ent ORM. **Not a schema per service** — `app` owns `public` and is the only repo with migrations; `sentinel` keeps its own schema; the `cms`/`jobsimulation`/`skillpath` **schemas** are legacy husks — non-authoritative leftovers, not services (see the Database Separation section below)
corpus/architecture/architecture_overview.md:61-61	bullet	**Cache/Streams**: Redis ElastiCache (caching, pub/sub, job queues via Watermill)
corpus/architecture/architecture_overview.md:62-63	bullet	**APIs**: GraphQL Federation v2 (WunderGraph Cosmo Router — **prod only**; local dev talks to `backend` directly), gRPC/Connect-RPC (internal), Protocol Buffers
corpus/architecture/architecture_overview.md:64-64	bullet	**Auth**: Clerk (identity) + Casbin (authorization with RBAC/ABAC via Sentinel)
corpus/architecture/architecture_overview.md:65-65	bullet	**CMS**: Directus (self-hosted, headless)
corpus/architecture/architecture_overview.md:66-66	bullet	**Infrastructure**: AWS ECS EC2 (EU-West-1 primary), Terraform IaC, Vercel (frontend)
corpus/architecture/architecture_overview.md:67-67	bullet	**CI/CD**: GitHub Actions with self-hosted EU runners; Tailscale VPN for private access
corpus/architecture/architecture_overview.md:68-68	bullet	**Monitoring**: CloudWatch, Better Stack, Sentry, PostHog
corpus/architecture/architecture_overview.md:78-78	bullet	**Studio Services**: Studio-Desk (TypeScript, runs natively or in `studio-desk` profile); Studio-Room is embedded in the **`app` (backend) image** since cms-in-app — it was in the cms container before the merge.
corpus/architecture/architecture_overview.md:79-79	bullet	**External Services**: Clerk, Directus, GraphQL (**prod only**), AI providers, LiveKit, AWS Chime
corpus/architecture/architecture_overview.md:81-81	bullet	**Production-only / not in local compose**: db-backup, archived Chronos/Intelligence
corpus/architecture/architecture_overview.md:83-83	paragraph	Services communicate via **Connect-RPC/HTTP** for synchronous operations and **Redis Streams** (via Watermill) for asynchronous messaging.
corpus/architecture/architecture_overview.md:164-164	table-row	| **PostgreSQL** *(always on)* | Third-party image | Data store (custom image with `pgvector`) | — |
corpus/architecture/architecture_overview.md:165-165	table-row	| **Redis** *(always on)* | Third-party image | Cache, pub/sub, job queues | — |
corpus/architecture/architecture_overview.md:232-232	table-row	| **colony** | Platform framework: logging+Sentry, DB/Redis helpers, GraphQL/RPC servers, middleware, pub/sub (Watermill); also contains `authn` |
corpus/architecture/architecture_overview.md:235-235	table-row	| **authn** | Clerk JWT authentication — now shipped **inside colony** as `colony/authn` (standalone repo is legacy) |
corpus/architecture/architecture_overview.md:285-285	bullet	**Asynchronous**: Redis Streams for event-driven messaging (via Watermill pub/sub library)
corpus/architecture/architecture_overview.md:288-288	bullet	**Primary**: GraphQL — **`backend` directly on a local stack** (`:8082/graphql/query`), via the Cosmo Router in prod.
corpus/architecture/architecture_overview.md:292-292	bullet	**Clerk**: SDK-based (frontend) + JWT middleware (backend via `authn` library)
corpus/architecture/architecture_overview.md:293-293	bullet	**Directus**: Proxied via the cms **domain** inside `backend` (business logic layer)
corpus/architecture/architecture_overview.md:377-379	paragraph	The platform uses **shared database, shared schema**, with `organization_id` on **org-scoped** tables (**not** on every table — the taxonomy and other global reference data carry none by design).
corpus/architecture/architecture_overview.md:402-402	bullet	**Identity**: Clerk JWT includes org context; sessions are org-scoped
corpus/architecture/architecture_overview.md:408-408	paragraph	The platform uses a **Code-First** approach to data management, relying on strictly typed schemas in Go.
corpus/architecture/architecture_overview.md:412-412	bullet	**Definition**: Schemas are defined in Go code within `internal/data/ent/schema` or `internal/ent/schema`.
corpus/architecture/architecture_overview.md:417-421	bullet	**Workflow**: 1. **Define**: Engineers modify Ent schemas in Go. 2. **Generate**: `make gen` runs Ent codegen to update the Go client. 3. **Migration Diff**: Atlas compares the Go schema against the migration directory to create a new `.sql` migration plan. 4. **Apply**: `atlas migrate apply` executes pending migrations against the target database.
corpus/architecture/architecture_overview.md:425-425	bullet	`backend` service → `public` schema (including the skills taxonomy + embeddings ported from the old `skiller` schema, and the skill-path runtime state in `public.skill_path_sessions` ported from the old `skillpath` schema; the legacy `skiller` + `skillpath` schemas are no longer authoritative)
corpus/architecture/architecture_overview.md:426-426	bullet	*(legacy)* `cms` schema → non-authoritative; the similarity + Studio tables moved to `public` with cms-in-app v8.0
corpus/architecture/architecture_overview.md:427-427	bullet	*(legacy)* `jobsimulation` schema → non-authoritative; the 23 session/run tables moved to `public` with jobsim-in-app
corpus/architecture/architecture_overview.md:428-428	bullet	*(decommissioned)* `skillpath` schema → an empty legacy husk; the skill-path runtime state moved to `public.skill_path_sessions` when the skillpath service merged into `app` (M502→M507)
corpus/architecture/architecture_overview.md:429-429	bullet	`sentinel` service → `sentinel` schema (created manually during setup; sentinel does not run migrations)
corpus/architecture/architecture_overview.md:430-430	bullet	`extensions` schema → houses `pgvector` extension (required by the skill/job-role embeddings, now owned by `backend`)
corpus/architecture/architecture_overview.md:432-433	paragraph	[!IMPORTANT] **Manual Setup Required**: The platform does *not* automatically apply migrations on startup (to prevent accidental production overrides).
corpus/architecture/architecture_overview.md:432-433	paragraph	Developers must run `atlas migrate apply` manually when setting up a fresh environment or pulling schema changes.
corpus/architecture/architecture_overview.md:437-437	bullet	**Cloud**: AWS ECS EC2 (EU-West-1 primary); Vercel for frontend
corpus/architecture/architecture_overview.md:440-440	bullet	**CI/CD**: GitHub Actions with self-hosted EU runners; Tailscale VPN for private subnet access; Git tags trigger deployments
corpus/architecture/architecture_overview.md:441-441	bullet	**Monitoring**: CloudWatch (metrics, dashboards, alarms), Sentry (errors, performance, cron monitoring), PostHog (analytics), Better Stack (incident escalation, uptime)
corpus/architecture/architecture_overview.md:442-442	bullet	**Backups**: Full DB backups every 6 hours to S3, Azure, and Hetzner (Germany); RDS point-in-time recovery
corpus/architecture/dependency_map.md:9-9	paragraph	Since the monolith merge most of this matrix collapsed: `skiller`, `skillpath`, `roadrunner`, `jobsimulation`, `cms`, `storage`, `messenger` and `customerio-sync` are all domains inside **Backend (`app`)**, so their edges are in-process calls, not dependencies.
corpus/architecture/dependency_map.md:9-9	paragraph	The rows below that are struck through have **no compose service at all**.
corpus/architecture/dependency_map.md:14-14	table-row	| **Sentinel** | - | Postgres |
corpus/architecture/dependency_map.md:17-17	table-row	| ~~**Skillpath**~~ | **Merged into `app`** ("skillpath-in-app", M502→M507) — the skill-path engine's dependencies (cms content by ID, the jobsimulation Redis Stream, Sentinel) are now `app`'s, in-process | *(no standalone service)* |
corpus/architecture/dependency_map.md:20-20	table-row	| **Gotenberg** | - | - (stateless conversion service) |
corpus/architecture/dependency_map.md:38-38	table-row	| **db-backup** | - | Postgres, **S3**, **Azure**, **Hetzner** |
corpus/architecture/dependency_map.md:46-46	table-row	| **colony** | All Go services (logging, DB, Redis, middleware, pub/sub); also bundles `authn` |
corpus/architecture/dependency_map.md:62-62	table-row	| `skillpath` | App | App | Session updated, chapters completed — both producer and consumer live inside app since the skillpath→app merge (stream name retained) |
corpus/architecture/dependency_map.md:63-63	table-row	| ~~`roadrunner`~~ | — | **no producer or consumer** — roadrunner is merged into app, which calls Judge0 synchronously | ~~`RoadrunnerSubmissionCompleted`~~ |
corpus/architecture/dependency_map.md:66-66	paragraph	Jobsimulation no longer has chronos as a dependency.
corpus/architecture/dependency_map.md:71-71	paragraph	`Frontend` -> `Backend` -> `Sentinel`
corpus/architecture/dependency_map.md:72-72	bullet	The Backend validates requests using Sentinel.
corpus/architecture/dependency_map.md:73-73	bullet	**Studio Desk** authenticates directly via **Clerk**.
corpus/architecture/dependency_map.md:76-76	paragraph	`Frontend` -> `Backend` (`app` — the **jobsimulation domain**, in-process; there is no jobsimulation service to reach)
corpus/architecture/dependency_map.md:77-77	bullet	The jobsimulation engine fetches the simulation **definition** (the `simulations` content/blueprint) from the cms domain by ID — in-process since cms-in-app.
corpus/architecture/dependency_map.md:79-79	bullet	Voice flows go through LiveKit; video recordings via AWS Chime SDK.
corpus/architecture/dependency_map.md:82-82	paragraph	`Frontend` -> `app` (cms domain) -> `Directus`   *(was `Frontend -> CMS -> Directus` before cms-in-app)*
corpus/architecture/dependency_map.md:83-83	bullet	The cms **domain inside `app`** acts as the gateway to Directus content — `Frontend -> app (cms domain) -> Directus`.
corpus/architecture/dependency_map.md:83-83	bullet	It was a separate `CMS` service until cms-in-app v8.0; the hop is in-process now.
corpus/architecture/dependency_map.md:86-86	paragraph	`Studio Desk` → `app` (cms domain) → (in-process) `Studio Room`   *(was `Studio Desk → CMS → Studio Room` before cms-in-app)*
corpus/architecture/dependency_map.md:87-88	bullet	**Studio-Desk** (TypeScript) creates blueprints, sent to the **cms domain inside `app`** as `StudioDocument` rows over `backend`'s GraphQL endpoint (`:8082/graphql/query`).
corpus/architecture/dependency_map.md:89-90	bullet	The **cms domain** (Go, `app/internal/cms/`) creates `StudioTask` records and dispatches generation work — an in-process hop since cms-in-app v8.0, not a service call.
corpus/architecture/dependency_map.md:91-91	bullet	**Studio-Room** (Python, embedded inside the **`app`** container since cms-in-app) executes the generation pipeline against AI providers (OpenAI, Azure OpenAI, Anthropic — **not** Mistral; see the table above).
corpus/architecture/dependency_map.md:92-92	bullet	Final content is persisted via the **cms domain** (in `app`); **Directus** is the underlying storage backend.
corpus/architecture/dependency_map.md:95-95	paragraph	`App (jobsimulation domain)` -> `Redis Stream` -> `App (skill-path engine, in-process)`
corpus/architecture/dependency_map.md:96-96	bullet	When a user completes a simulation, the **jobsimulation domain inside `app`** publishes an event — both ends of this stream have been the same process since jobsim-in-app; the stream survives because the *name* was retained, not because a second service does.
corpus/architecture/dependency_map.md:97-97	bullet	The **skill-path engine — now inside `app`** (merged from the standalone skillpath service, M502→M507) — subscribes to the Jobsimulation stream and updates step/chapter/path progress **state** (`SkillPathSession → ChapterSession → StepSession`, in `public.skill_path_sessions`) — it owns no content, only the per-user progression state.
corpus/architecture/dependency_map.md:98-98	bullet	The engine reads the skill-path **content** structure (chapters → steps it tracks against) from the **cms domain in-process** — it was a Connect-RPC call to the CMS service before the merge — and calls **Sentinel** for authorization, which is still a real network hop.
corpus/architecture/dependency_map.md:101-101	paragraph	`Backend (app)` → `Gotenberg`
corpus/architecture/dependency_map.md:103-103	bullet	`GOTENBERG_URL=http://gotenberg:3200` is injected via the backend's compose env.
corpus/architecture/external_services.md:6-6	paragraph	This document describes all external services and third-party integrations used by the Anthropos platform.
corpus/architecture/external_services.md:6-6	paragraph	These are services the platform **depends on** but does not directly maintain in the core codebase.
corpus/architecture/external_services.md:10-10	paragraph	The Anthropos platform integrates with **four key external services**:
corpus/architecture/external_services.md:12-12	bullet	**Clerk** - Handles all user authentication and organization management (SaaS)
corpus/architecture/external_services.md:13-13	bullet	**Directus** - Stores and manages platform content (self-hosted via Docker)
corpus/architecture/external_services.md:15-15	bullet	**AI Providers** - OpenAI, Anthropic, and Azure for intelligent features
corpus/architecture/external_services.md:45-45	paragraph	Clerk is integrated across **all user-facing applications**:
corpus/architecture/external_services.md:73-73	paragraph	**Sentinel Service**:
corpus/architecture/external_services.md:74-74	bullet	Acts as the centralized **authorization** service (Casbin RBAC/ABAC)
corpus/architecture/external_services.md:75-75	bullet	Does NOT perform authentication and does NOT validate Clerk tokens — JWT validation is done in each consuming service via the shared `authn` library (now `colony/authn`)
corpus/architecture/external_services.md:78-78	paragraph	**Other Backend Services**:
corpus/architecture/external_services.md:81-81	bullet	Trust Sentinel's authorization decisions
corpus/architecture/external_services.md:91-91	paragraph	Clerk webhooks sync user and organization data to your local database.
corpus/architecture/external_services.md:91-91	paragraph	Without working webhooks, users created in Clerk won't appear locally.
corpus/architecture/external_services.md:99-99	paragraph	Then configure the webhook URL in Clerk Dashboard pointing to `https://<your-url>/api/webhook/clerk`.
corpus/architecture/external_services.md:102-102	bullet	Full localtunnel setup with Clerk configuration
corpus/architecture/external_services.md:103-103	bullet	More reliable alternatives (ngrok, Tailscale Funnel)
corpus/architecture/external_services.md:113-113	bullet	Clerk handles **GDPR compliance** and secure password storage
corpus/architecture/external_services.md:124-124	table-row	| **Type** | Self-hosted Headless CMS (lives in **production**) |
corpus/architecture/external_services.md:125-125	table-row	| **Address** | `https://content.anthropos.work` (the prod public instance) |
corpus/architecture/external_services.md:126-126	table-row	| **Purpose** | Content storage, media management, CMS |
corpus/architecture/external_services.md:159-159	bullet	**Headless CMS**: Manage content via REST/GraphQL APIs
corpus/architecture/external_services.md:160-160	bullet	**Database Abstraction**: Works directly with PostgreSQL
corpus/architecture/external_services.md:224-225	paragraph	**The cms domain inside `backend` acts as a smart proxy** between applications and Directus (it was the standalone CMS Service until cms-in-app folded it into `app`):
corpus/architecture/external_services.md:227-227	bullet	**Frontend/Studio-Desk** → GraphQL request to `backend:8082/graphql/query`
corpus/architecture/external_services.md:228-228	bullet	**cms domain** (`app/internal/cms/directus/`) → Translates to Directus API call
corpus/architecture/external_services.md:229-229	bullet	**Directus** → Queries PostgreSQL
corpus/architecture/external_services.md:230-230	bullet	**cms domain** ← Adds business logic, caching
corpus/architecture/external_services.md:231-231	bullet	**Frontend/Studio-Desk** ← Returns enriched data
corpus/architecture/external_services.md:236-236	bullet	Abstract Directus implementation details
corpus/architecture/external_services.md:237-237	bullet	Easier to migrate CMS in the future
corpus/architecture/external_services.md:273-273	paragraph	Directus uses a **dedicated PostgreSQL schema**:
corpus/architecture/external_services.md:280-280	bullet	`directus_files`: Media and file metadata
corpus/architecture/external_services.md:281-281	bullet	`directus_folders`: File organization
corpus/architecture/external_services.md:282-282	bullet	`directus_users`: CMS admin users (separate from Clerk)
corpus/architecture/external_services.md:287-291	paragraph	**Local Development**: there is no local Directus and no local uploads directory in the default posture.
corpus/architecture/external_services.md:287-291	paragraph	Image bytes are served from the **asset plane** — prod's anonymous public `<DIRECTUS_PUBLIC_BASE_ADDR>/assets/<uuid>` links, which browsers fetch token-less (now `app/internal/cms/directus/`).
corpus/architecture/external_services.md:287-291	paragraph	Even when the v1.5 local tooling serves the *data plane* (catalog rows) from a per-stack Directus, the *asset plane* stays on prod's public links so images stay real — no blob bytes are copied locally.
corpus/architecture/external_services.md:295-295	bullet	Directus handles upload to S3 automatically
corpus/architecture/external_services.md:308-308	paragraph	The cms domain connects to Directus via:
corpus/architecture/external_services.md:316-316	paragraph	**Code Integration** (`app/internal/cms/directus/`, compiled into `backend`):
corpus/architecture/external_services.md:334-336	paragraph	In the **default posture there is no local Directus to log into** — content comes from the production instance, which developers don't administer locally.
corpus/architecture/external_services.md:334-336	paragraph	There is no `localhost:8055` admin and **no `admin@example.com` / `password` login** (that earlier claim was tied to the fictional compose service above).
corpus/architecture/external_services.md:343-343	bullet	**Admin UI / REST / GraphQL**: `http://localhost:<offset-8055>/` , `…/items/{collection}` , `…/graphql`
corpus/architecture/external_services.md:347-347	paragraph	Directus can trigger webhooks on content changes:
corpus/architecture/external_services.md:350-350	bullet	Invalidate CMS service cache when content updates
corpus/architecture/external_services.md:354-354	paragraph	**Configuration**: Set up in Directus admin UI under Settings → Webhooks
corpus/architecture/external_services.md:366-366	table-row	| **Composition tool** | `wgc@0.104.0` (WunderGraph Cosmo CLI) — runs at Docker build time |
corpus/architecture/external_services.md:369-369	table-row	| **Repository** | `git@github.com:anthropos-work/graphql-wundergraph` |
corpus/architecture/external_services.md:373-373	bullet	**Federation v2**: Composes **one** subgraph — `backend`.
corpus/architecture/external_services.md:373-373	bullet	The count, read straight off `supergraph-config-prod.yaml` at each commit (`git show <sha>:supergraph-config-prod.yaml`):
corpus/architecture/external_services.md:377-377	table-row	| `749dc86~1` | — | **5** | `backend`, `skiller`, `jobsimulation`, `cms`, `skillpath` |
corpus/architecture/external_services.md:386-386	paragraph	The supergraph config now lists a single entry pointing at `http://backend.internal.anthropos:8080/graphql/query`, `schemas/` holds `backend.graphqls` alone, and `subgraphs.conf` tracks a single `BACKEND=v1.360.0` pin.
corpus/architecture/external_services.md:389-389	bullet	**Playground** at `/graphql` for local development
corpus/architecture/external_services.md:422-422	bullet	backend
corpus/architecture/external_services.md:423-423	bullet	jobsimulation
corpus/architecture/external_services.md:424-424	bullet	cms
corpus/architecture/external_services.md:425-425	bullet	storage
corpus/architecture/external_services.md:439-439	paragraph	The composed `config.json` is generated at image build time, so **any** subgraph SDL change means rebuilding the gateway.
corpus/architecture/external_services.md:459-459	paragraph	The gateway's `Dockerfile.dev` does multi-stage composition with the WunderGraph CLI — **as the archived repo still has it**, post-fold:
corpus/architecture/external_services.md:475-475	paragraph	The composed `config.json` is then served by the Cosmo router binary at runtime.
corpus/architecture/external_services.md:483-483	table-row	| backend | `http://backend:8082/graphql/query` — **the only one left** |
corpus/architecture/external_services.md:495-495	paragraph	Composition is **build-time and static** (the supergraph `config.json` is baked into the image; the router does not live-introspect subgraphs), so adding/changing a subgraph requires a rebuild + restart.
corpus/architecture/external_services.md:503-503	paragraph	**Next.js Apps**:
corpus/architecture/external_services.md:519-519	paragraph	**Studio-Desk**:
corpus/architecture/external_services.md:531-531	paragraph	Access GraphQL playground at:
corpus/architecture/external_services.md:538-538	bullet	Query **and mutation** testing (there is no `type Subscription` in `backend.graphqls` — see *What the gateway provides* above, so there is no subscription tab to exercise)
corpus/architecture/external_services.md:543-543	paragraph	When backend services add new GraphQL types or operations:
corpus/architecture/external_services.md:545-545	bullet	**Backend service** updates its GraphQL schema
corpus/architecture/external_services.md:546-546	bullet	~~**Restart Wundergraph**: `docker compose restart graphql`~~ — there is no `graphql` service locally any more; restart `backend`
corpus/architecture/external_services.md:547-547	bullet	**Studio-Desk**: Run `npm run codegen` to regenerate types
corpus/architecture/external_services.md:548-548	bullet	**Next.js apps**: Regenerate clients as needed
corpus/architecture/external_services.md:562-562	table-row	| **Azure OpenAI (EU)** | `vendor = Azure` from the caller | Jobsimulation domain, Backend (app — merged skiller domain), cms domain, Studio | GPT-5.x, GPT-4.1 for simulations and content |
corpus/architecture/external_services.md:563-563	table-row	| **Azure OpenAI (US)** | `vendor = Azure` **+ PostHog `flag_use_azure_us`** | same as above | The EU deployment's US twin — a flag flip, not a failure fallback |
corpus/architecture/external_services.md:583-587	bullet	**The caller picks the vendor.** `ChatCompletion` / `Response` take a `vendor AIVendor` argument and hand it to `getClient` (`:259-289`).
corpus/architecture/external_services.md:583-587	bullet	The four vendors are consts at `:30-33` — `azure`, `openai`, `anthropic-aws`, `anthropic`.
corpus/architecture/external_services.md:583-587	bullet	**When the authored content names no vendor, the caller's own default is `openai`** — that default lives in the cms content layer, not here; see the residency note below.
corpus/architecture/external_services.md:588-590	bullet	**`Azure` is EU by default**, swapped to the US deployment only when the PostHog flag `flag_use_azure_us` evaluates true (`:264-276`); if the flag lookup errors, the code logs and **keeps the EU client**.
corpus/architecture/external_services.md:591-592	bullet	**`AnthropicAws` and `Anthropic` both return `a.anthropicClient`** (`:280-283`) — the Bedrock client.
corpus/architecture/external_services.md:591-592	bullet	There is **no US-direct Anthropic branch** in this manager.
corpus/architecture/external_services.md:593-596	bullet	**The one automatic fallback is 429-only.** `isThrottlingError` matches an HTTP 429 from the OpenAI or Anthropic SDK (`:130-141`); the retry wrapper then sets `vendor = Openai` on the next attempt (`:150-155`, mirrored at `:296-302`/`:326`), i.e. **direct US OpenAI**.
corpus/architecture/external_services.md:609-609	bullet	the `flag_use_azure_us` PostHog flag;
corpus/architecture/external_services.md:610-610	bullet	the 429 retry, which switches to direct OpenAI **without** trying another EU provider first;
corpus/architecture/external_services.md:654-654	paragraph	AI services are configured via environment variables in `platform/.env`:
corpus/architecture/external_services.md:681-684	bullet	**Simulation Engine** (Jobsimulation): - AI-powered conversations (voice + chat) with configurable model per simulation - Voice calls via **LiveKit + GPT Realtime** agents - Document analysis and code evaluation
corpus/architecture/external_services.md:686-689	bullet	**Skills Matching** (Backend `app` — merged skiller domain): - Embeddings (Text Embedding 3 Small) over the taxonomy — `public.skill_embeddings` and `public.job_role_embeddings` - RAG for job role matching
corpus/architecture/external_services.md:706-709	bullet	**Studio-Desk Copilot**: - Uses a configurable multi-provider chain (Azure OpenAI / OpenAI / Anthropic) via backend proxy, with tier-based model selection and circuit-breaker failover (`AI_PROVIDER_CHAIN`, default `azure-openai,openai`) - Supports streaming responses for real-time interaction - Default models: `gpt-5.2` (OpenAI/Azure) or `claude-sonnet-4-5` / `claude-opus-4-5` (Anthropic)
corpus/architecture/external_services.md:725-725	table-row	| **Integration** | Jobsimulation service |
corpus/architecture/external_services.md:741-741	table-row	| **Integration** | Jobsimulation service |
corpus/architecture/external_services.md:743-743	paragraph	AWS Chime SDK captures the full simulation session (camera, screensharing, microphone) as a composited MP4 grid view.
corpus/architecture/external_services.md:743-743	paragraph	This runs in parallel with LiveKit's audio-only recording.
corpus/architecture/external_services.md:750-750	bullet	**Clerk**: `clerk.com` (free tier available)
corpus/architecture/external_services.md:766-766	paragraph	**For Next.js Apps**:
corpus/architecture/external_services.md:775-775	paragraph	**For Studio-Desk**:
corpus/architecture/external_services.md:782-782	paragraph	**For the cms domain (set on `backend`, which hosts it)**:
corpus/architecture/external_services.md:794-794	bullet	Configure production URLs in Clerk dashboard
corpus/architecture/external_services.md:800-800	bullet	Configure S3 for file storage
corpus/architecture/external_services.md:806-806	bullet	Configure production backend service URLs
corpus/architecture/external_services.md:827-827	paragraph	**"Cannot connect to Directus"** (default posture — reading prod):
corpus/architecture/external_services.md:828-830	bullet	**`backend`** reads Directus **live from prod** (the fetch is `app/internal/cms/directus/` running inside the `backend` container since cms-in-app); there is no local `directus` container to `ps`.
corpus/architecture/external_services.md:834-834	paragraph	**"Cannot connect to Directus"** (when running the local tooling, `--local-content` / demo):
corpus/architecture/external_services.md:848-848	paragraph	**"GraphQL endpoint not responding"**:
corpus/architecture/frontend_architecture.md:3-3	paragraph	This document outlines the architecture of the Anthropos **main customer-facing frontend** (`next-web-app`), organized as a **Monorepo** using **Turborepo** and **pnpm** workspaces.
corpus/architecture/frontend_architecture.md:21-21	table-row	| **Web** | `apps/web` | 3000 | Main user-facing app (dashboard, simulations, skill paths, onboarding, "Talk to Data" UI) |
corpus/architecture/frontend_architecture.md:24-24	table-row	| **Mobile** | `apps/mobile` | 3031 (Expo) | Expo / React Native mobile app — **paused PoC**; **excluded** from the pnpm workspace (`!apps/mobile` in `pnpm-workspace.yaml`), so not installed/built by `pnpm install` / `turbo dev` by default |
corpus/architecture/frontend_architecture.md:31-31	table-row	Recent work: `SkillCard` mini-card, `SkillCardCallout`, onboarding `SkillsRefinement` cluster card. |
corpus/architecture/frontend_architecture.md:32-32	table-row	| **GraphQL** | `packages/graphql` | Shared GraphQL definitions, generated hooks, and types.
corpus/architecture/frontend_architecture.md:32-32	table-row	Populated by `pnpm codegen` against the supergraph — which is now the single `backend` subgraph, served locally by `backend` itself. |
corpus/architecture/frontend_architecture.md:53-53	bullet	**Used For**: Content retrieval (via **CMS**), Simulation state, and aggregated data.
corpus/architecture/frontend_architecture.md:54-54	bullet	**Implementation**: `packages/graphql` contains the generated types and hooks.
corpus/architecture/frontend_architecture.md:55-55	bullet	**Code Generation**: Uses `graphql-codegen` to read the supergraph schema from the federated GraphQL endpoint (`backend` at `:8082/graphql/query` locally; the Cosmo Router at `:8080/graphql` in prod) and emits typed GraphQL documents into `packages/graphql/src/__generated__` via the client-preset (documents sourced from `src/query/**`).
corpus/architecture/frontend_architecture.md:61-61	bullet	**Package Manager**: pnpm 10.x (`packageManager: "pnpm@10.30.3"`)
corpus/architecture/frontend_architecture.md:62-62	bullet	**Node**: **v24+ required** (`engines.node: ">=24.0.0"` in `package.json`)
corpus/architecture/frontend_architecture.md:79-79	paragraph	Older Node will fail with `WARN Unsupported engine` and pnpm will refuse to wipe `node_modules` in non-TTY shells (`ERR_PNPM_ABORTED_REMOVE_MODULES_DIR_NO_TTY`).
corpus/architecture/frontend_architecture.md:83-83	paragraph	To run a specific app:
corpus/architecture/frontend_architecture.md:93-93	paragraph	GraphQL types are regenerated from the upstream subgraph schemas:
corpus/architecture/frontend_architecture.md:115-115	bullet	**Talk to Data**: SSE-streaming Q&A UI (counterpart to backend's `ask`/`askengine`)
corpus/architecture/platform-migration-status.md:51-51	table-row	| `running_but_unfederated` | the container still starts, but it owns no schema and is not a subgraph — a **husk**, not a service |
corpus/architecture/platform-migration-status.md:106-106	table-row	| `colony` | library | library | no | private Go module (framework + `colony/authn`); pulled at Docker build via `GH_PAT`/`GOPRIVATE`, never cloned by `make init` |
corpus/architecture/platform-migration-status.md:108-108	table-row	| `ai` | library | library | no | private Go module — the multi-provider `ai.AI` wrapper |
corpus/architecture/platform-migration-status.md:142-142	table-row	| `kb-certifications-iso27001` | 2026-07-07 | compliance KB; `security_compliance.md` cites no ISO-27001 programme |
corpus/architecture/platform-migration-status.md:143-143	table-row	| `livekit-agent-chain` | 2026-07-03 | one of **five** LiveKit agent repos.
corpus/architecture/platform-migration-status.md:143-143	table-row	`ai_architecture.md` documents the LiveKit *engine* and **none** of the agents |
corpus/architecture/platform-migration-status.md:154-154	table-row	| `transcoder` | 2025-11-11 | media transcoding — adjacent to `media-substrate-spec.md`'s Bunny.net path |
corpus/architecture/platform-migration-status.md:161-164	paragraph	**`auth` is not on this list, deliberately.** iter-01 measured it as a 3-commit, 8-hour spike marked *"Not yet deployed"*, with all human activity stopped 2026-06-18 and BetterAuth a dependency of nothing in the org. **Clerk is not being replaced.** Recording that here is the point: the scary-looking repo was measured, and the measurement is the deliverable.
corpus/architecture/platform-migration-status.md:206-206	table-row	Re-check at each platform ref bump |
corpus/architecture/platform-migration-status.md:212-216	paragraph	It moved again at M257x iter-102 (22/69 → **25/70**), because repairing the messenger row's cross-process claim added four citations.
corpus/architecture/security_compliance.md:3-3	paragraph	This document describes the security architecture, data protection measures, and compliance posture of the Anthropos platform.
corpus/architecture/security_compliance.md:14-14	bullet	No direct SSH to production; all access via Tailscale VPN
corpus/architecture/security_compliance.md:36-36	bullet	**Data subnets**: PostgreSQL RDS, Redis ElastiCache
corpus/architecture/security_compliance.md:40-40	bullet	**Tailscale VPN** for dev team and GitHub Actions self-hosted runners
corpus/architecture/security_compliance.md:55-55	bullet	Encryption enabled by default on all storage
corpus/architecture/security_compliance.md:146-146	bullet	Org-scoped tables carry an `organization_id` column
corpus/architecture/security_compliance.md:148-149	bullet	Cross-tenant reads are prevented at the query level **on those tables**; elsewhere isolation is enforced by Layer 2 (Sentinel) and by explicit query scoping, not by the ORM
corpus/architecture/security_compliance.md:179-179	table-row	| `:190-191` | **viewer has no active org** (`org == nil \|\| org.ID() == uuid.Nil`) | `next(ctx)` |
corpus/architecture/security_compliance.md:182-182	table-row	| `:209-219` | `@resolverAuthorized` — grants `authorization.Allow` **without calling Sentinel** | `next(ctx)` |
corpus/architecture/security_compliance.md:189-191	paragraph	**And there is no BLANKET authz middleware on the REST surface.** `git grep -nE 'AuthzMiddleware|EchoAuthzMiddleware' -- '*.go'` over `app` @ `ad9f3c49` returns **0**, and REST authorization is opt-in **per group or per handler**, never applied to the surface as a whole:
corpus/architecture/security_compliance.md:196-196	table-row	| `/ask` | `:171-173` | `cors` + `authn` — the `admin/auto-rules/*` routes are gated **per handler** by `requireAdmin`, which the source says in place (`:183-185`) |
corpus/architecture/security_compliance.md:198-198	table-row	| `/admin/backfill` | `:210-212` | `cors` + `authn` — *"gated on the admin Sentinel permission"* per handler (`:207-209`) |
corpus/architecture/security_compliance.md:199-199	table-row	| `/coursebuilder` | `:229-232` | `cors` + `authn` + **`cbGate`** |
corpus/architecture/security_compliance.md:200-200	table-row	| `/credits` | `:273-276` | `cors` + `authn` + **`cbGate`** |
corpus/architecture/security_compliance.md:213-214	bullet	**Sentinel is the centralized authorization *engine*** (Casbin RBAC/ABAC, PostgreSQL-backed policy store), and its policies are centrally managed and auditable — that part always held.
corpus/architecture/security_compliance.md:215-217	bullet	**It is not a blanket applied to every request.** The GraphQL middleware is a *viewer* gate with a narrow cross-user check bolted on; the REST surface has **no blanket authz middleware** — two of its six groups opt into one (`cbGate`), the rest authorize per handler or not at all.
corpus/architecture/security_compliance.md:226-226	bullet	**Clerk** JWT tokens include organization context
corpus/architecture/security_compliance.md:228-228	bullet	Organization switching **re-mints the session JWT with the new org claim — it is NOT a re-authentication.** It is a client-side `clerk.setActive({ organization })` call (`next-web-app/apps/{web,hiring}/src/hooks/useOrgSelection.tsx:94`, `useResolveActiveOrg.tsx:107`, `useActivateMembershipOrg.tsx:81`); no credential is re-presented and no sign-out occurs.
corpus/architecture/security_compliance.md:236-236	table-row	| **Full backups** | Every 6 hours → S3, Azure, Hetzner (Germany) |
corpus/architecture/security_compliance.md:241-241	table-row	| **CDN** | Worldwide (Vercel for frontend) |
corpus/architecture/security_compliance.md:243-243	paragraph	The `db-backup` service runs on a schedule, dumping PostgreSQL to three geographically separate locations for resilience.
corpus/architecture/security_compliance.md:264-264	table-row	| **PostHog** | Product analytics |
corpus/architecture/security_compliance.md:303-305	bullet	No customer data stored in US **by default** — but the residency guarantee is contingent on `flag_use_azure_us` being off, and that is a runtime flag, not a build-time property.
corpus/architecture/security_compliance.md:348-348	bullet	**AI**: OpenAI, Anthropic, Mistral
corpus/architecture/security_compliance.md:349-349	bullet	**Voice/Recording**: LiveKit, AWS Chime
corpus/architecture/security_compliance.md:350-350	bullet	**Infrastructure**: AWS, Vercel
corpus/architecture/security_compliance.md:351-351	bullet	**Auth**: Clerk
corpus/architecture/security_compliance.md:352-352	bullet	**Analytics**: PostHog, Sentry
corpus/architecture/security_compliance.md:353-353	bullet	**Email**: Brevo (Sendinblue)
corpus/architecture/service_taxonomy.md:6-6	paragraph	This document explains the three-tier service architecture of the Anthropos platform, categorizing all services by their deployment model, technology stack, and operational characteristics.
corpus/architecture/service_taxonomy.md:10-10	paragraph	The Anthropos platform is built from **three types of services**:
corpus/architecture/service_taxonomy.md:12-12	bullet	**Core Backend Services**: The main engine of the platform - containerized microservices that handle user data, skills, simulations, and business logic.
corpus/architecture/service_taxonomy.md:66-66	bullet	**Deployment**: Docker Compose with Makefile automation (local) / AWS ECS (production)
corpus/architecture/service_taxonomy.md:67-67	bullet	**Communication**: HTTP/RPC + Redis Streams
corpus/architecture/service_taxonomy.md:86-86	table-row	| **Sentinel** | 8087 | Authorization (Casbin RBAC/ABAC) | (always on — declares no `profiles:` key) | Local `../sentinel` |
corpus/architecture/service_taxonomy.md:87-87	table-row	| **Gotenberg** | 3200 | Office-doc → PDF conversion (LibreOffice) | core, backend, all | Third-party image `gotenberg/gotenberg:8` |
corpus/architecture/service_taxonomy.md:89-92	paragraph	Neither profile works *alone* — both services declare `depends_on: backend`, which their own profile does not select, so compose exits 1; stack them on `core`.
corpus/architecture/service_taxonomy.md:96-96	table-row	| **Studio-Desk** | 9000, 9100 | Studio design tool (containerized variant) | studio-desk, all | Local `../studio-desk` |
corpus/architecture/service_taxonomy.md:97-97	table-row	| **Next-Web-App** | 3000 | Frontend (containerized variant) | frontend, all | Local `../next-web-app` |
corpus/architecture/service_taxonomy.md:179-179	table-row	| **colony** | Platform framework: logging, DB/Redis, GraphQL/RPC servers, middleware, pub/sub (Watermill); also contains `authn` | `git@github.com:anthropos-work/colony.git` |
corpus/architecture/service_taxonomy.md:180-180	table-row	| **proto** | Protobuf definitions (single source of truth for RPC contracts) + hand-written domain types | `git@github.com:anthropos-work/proto.git` |
corpus/architecture/service_taxonomy.md:182-182	table-row	| **authn** | Clerk JWT authentication — now shipped **inside colony** as `colony/authn` (standalone repo is legacy) | `git@github.com:anthropos-work/authn.git` |
corpus/architecture/service_taxonomy.md:225-225	table-row	| **Port** | 9100 (frontend), 9000 (backend) - configurable via `.env` |
corpus/architecture/service_taxonomy.md:227-227	table-row	| **Authentication** | Clerk |
corpus/architecture/service_taxonomy.md:228-228	table-row	| **Location** | Local `../studio-desk` (sibling of platform, cloned by `make init`) |
corpus/architecture/service_taxonomy.md:249-249	table-row	| **Input** | Blueprints (StudioDocuments) created in Studio-Desk and stored via CMS |
corpus/architecture/service_taxonomy.md:250-250	table-row	| **Output** | Generated simulations and learning content; CMS persists results |
corpus/architecture/service_taxonomy.md:251-251	table-row	| **Repo** | `git@github.com:anthropos-work/anthropos-studio-room.git` |
corpus/architecture/service_taxonomy.md:252-252	table-row	| **Location** | Pulled into the **`app`** image by CI (`additional_repo`, app v1.360.1).
corpus/architecture/service_taxonomy.md:252-252	table-row	Before cms-in-app it was `cms/studio/`, cloned by `cd cms && make init-studio`. |
corpus/architecture/service_taxonomy.md:253-253	table-row	| **Runtime** | Baked into the `app` (backend) Docker image — Python deps installed alongside the Go binary |
corpus/architecture/service_taxonomy.md:260-261	paragraph	**Local development** (no backend container needed) — run the Python project directly from a clone of `anthropos-studio-room`:
corpus/architecture/service_taxonomy.md:279-280	paragraph	Before cms-in-app this lived at `cms/studio/`, synced with `cd cms && make update-studio`.
corpus/architecture/service_taxonomy.md:282-282	paragraph	**Relationship**: Studio-Desk creates the *design* (blueprint).
corpus/architecture/service_taxonomy.md:282-282	paragraph	The cms domain in `app` (Go) orchestrates `StudioTask` records; the studio-room Python code runs inside the same container to execute generation.
corpus/architecture/service_taxonomy.md:288-288	table-row	| **Technology** | Next.js 16 App Router + React 19.2 (web) + Expo / React Native (mobile) |
corpus/architecture/service_taxonomy.md:290-290	table-row	| **Purpose** | Internal learning portal — micro-chapters on AI engineering, agent frameworks, Claude Code, etc., for `@anthropos.work` employees |
corpus/architecture/service_taxonomy.md:291-291	table-row	| **Authentication** | Clerk (domain-gated to `@anthropos.work` + org-membership gate) |
corpus/architecture/service_taxonomy.md:292-292	table-row	| **Repo** | `git@github.com:anthropos-work/ant-academy.git` |
corpus/architecture/service_taxonomy.md:294-294	table-row	| **Deployment** | Vercel native (`.github/workflows/deploy-academy.yaml`) — **not** in docker-compose |
corpus/architecture/service_taxonomy.md:300-300	bullet	Companion iOS / Android app (Expo SDK 54) bundling the same chapters at build time
corpus/architecture/service_taxonomy.md:301-301	bullet	Opt-in in-app "Cosmo" AI assistant (`NEXT_PUBLIC_FEATURE_TRAINING_COACH`, default OFF) — calls the OpenAI Responses API (`gpt-5.2`) directly from the browser via a per-user `localStorage` key
corpus/architecture/service_taxonomy.md:302-302	bullet	Repo-local Claude skills (`.claude/skills/`) for authoring chapters, podcasts, covers, and benchmarks
corpus/architecture/service_taxonomy.md:326-326	bullet	**Hosting**: SaaS or third-party Docker images
corpus/architecture/service_taxonomy.md:336-336	table-row	| **Integration Points** | Frontend apps, Backend middleware, Studio-Desk |
corpus/architecture/service_taxonomy.md:337-337	table-row	| **SDK** | `@clerk/nextjs`, `@clerk/express`, `@clerk/clerk-js`, `@clerk/clerk-expo`, `clerk-sdk-go/v2` |
corpus/architecture/service_taxonomy.md:342-342	bullet	`CLERK_PUBLISHABLE_KEY` / `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY`
corpus/architecture/service_taxonomy.md:343-343	bullet	`CLERK_SECRET_KEY`
corpus/architecture/service_taxonomy.md:344-344	bullet	`CLERK_SIGN_IN_URL`
corpus/architecture/service_taxonomy.md:347-347	bullet	Next.js apps (Web, Hiring)
corpus/architecture/service_taxonomy.md:348-348	bullet	Mobile app (Expo / React Native, via `@clerk/clerk-expo`)
corpus/architecture/service_taxonomy.md:349-349	bullet	Studio-Desk
corpus/architecture/service_taxonomy.md:350-350	bullet	Backend services (authenticate via the `authn` library; authorization is Sentinel's job)
corpus/architecture/service_taxonomy.md:356-356	table-row	| **Type** | Third-party Headless CMS (self-hosted, **production**) |
corpus/architecture/service_taxonomy.md:357-357	table-row	| **Address** | `https://content.anthropos.work` (the prod public instance) |
corpus/architecture/service_taxonomy.md:358-358	table-row	| **Purpose** | Content storage and management (the public catalog + content library) |
corpus/architecture/service_taxonomy.md:359-359	table-row	| **Database** | PostgreSQL (dedicated `directus` schema) |
corpus/architecture/service_taxonomy.md:415-415	table-row	| **Type** | Third-party with custom config (WunderGraph Cosmo Router) |
corpus/architecture/service_taxonomy.md:417-417	table-row	| **Purpose** | Apollo Federation v2, unified GraphQL API gateway |
corpus/architecture/service_taxonomy.md:418-418	table-row	| **Repository** | `git@github.com:anthropos-work/graphql-wundergraph.git` |
corpus/architecture/service_taxonomy.md:424-424	bullet	Backend (`app`) — the only subgraph left.
corpus/architecture/service_taxonomy.md:424-424	bullet	CMS and Jobsimulation folded into it.
corpus/architecture/service_taxonomy.md:427-427	bullet	Next.js frontend applications
corpus/architecture/service_taxonomy.md:428-428	bullet	Studio-Desk
corpus/architecture/service_taxonomy.md:430-430	paragraph	Locally, both of those now consume `backend` directly at `:8082/graphql/query`.
corpus/architecture/service_taxonomy.md:438-438	bullet	**Asynchronous**: Redis Streams (e.g., `JOBSIMULATION_STREAM=jobsimulation`)
corpus/architecture/service_taxonomy.md:441-441	bullet	**Studio-Desk**: GraphQL via `VITE_GRAPHQL_ENDPOINT` — compose bakes `http://localhost:8082/graphql/query` (was `:5050/graphql` on the router)
corpus/architecture/service_taxonomy.md:442-443	bullet	**Studio-Room**: runs inside the `app` image, orchestrated from `app/internal/cms/studio/` — blueprint retrieval is in-process against the cms domain, not a call to a CMS service
corpus/architecture/service_taxonomy.md:446-446	bullet	**Authentication**: Clerk SDK/API
corpus/architecture/service_taxonomy.md:447-447	bullet	**Content Storage**: Directus API (via the cms **domain** in `app`, for core services)
corpus/architecture/service_taxonomy.md:477-477	paragraph	Or run everything in Docker:
corpus/architecture/service_taxonomy.md:495-495	table-row	| (none — default `docker compose up`) | postgresql, redis, sentinel only — **the floor**, the three services that declare no `profiles:` key and are therefore in *every* selection |
corpus/architecture/service_taxonomy.md:496-496	table-row	| `core` (the Makefile default — `PROFILE ?= core`) | the floor + backend, gotenberg |
corpus/architecture/service_taxonomy.md:497-497	table-row	| `backend` | the floor + backend, gotenberg |
corpus/architecture/service_taxonomy.md:499-499	table-row	| `frontend` / `studio-desk` | **exit 1** — each named service declares `depends_on: backend`, which its own profile does not select, so compose rejects the project |
corpus/architecture/service_taxonomy.md:522-522	table-row	| **Studio** | Studio-Desk + Studio-Room | TypeScript / Python | Studio-Desk standalone; Studio-Room is embedded in the **`app`** image, orchestrated from `app/internal/cms/studio/` (it was `cms/studio/` before cms-in-app) | Local directories |
corpus/architecture/service_taxonomy.md:524-524	table-row	| **Production-only** | db-backup | Go | ECS scheduled task | GitHub repo |
corpus/architecture/service_taxonomy.md:526-526	table-row	| **External** | Clerk, Directus, Cosmo Router (**prod only** — see the banner at the top of this file), AI providers, LiveKit, AWS Chime | Various | SaaS / Docker | Configuration-driven |
corpus/architecture/shared_libraries.md:29-29	table-row	| **colony** | The framework: DB/Redis, logging+Sentry, GraphQL/RPC servers, CORS, pub/sub, feature flags — and it now contains **authn** |
corpus/architecture/shared_libraries.md:31-31	table-row	| **ai** | A thin wrapper over OpenAI/Azure/Anthropic/Bedrock/Mistral behind one `ai.AI` interface |
corpus/architecture/shared_libraries.md:32-32	table-row	| **authn** | Clerk JWT authentication (now shipped **inside colony** as `colony/authn`; the standalone repo is legacy) |
corpus/architecture/shared_libraries.md:33-33	table-row	| **taxonomy** | The **node-id library** (`NodeID` type + ID generation/validation) — **not** a dataset |
corpus/architecture/shared_libraries.md:55-55	table-row	| **Module** | `github.com/anthropos-work/colony` |
corpus/architecture/shared_libraries.md:60-60	paragraph	The platform framework.
corpus/architecture/shared_libraries.md:64-64	table-row	| `colony` (root) | `NewDBStdConn`/`NewDBPool` (pgx v5 Postgres for Ent + raw pools, with a 30s **DB health monitor** that cancels the service context after 3 failed pings), `InitLogger` (slog + Sentry fan-out), `NewGQLHandlerServer` + `Apply*GraphqlMiddlewares`, `NewHTTPServer` (serves `GET /_meta` version/health JSON), `NewCORSHandler`, `Environment`, `NewVersionConfig` (resolves ECS task id) |
corpus/architecture/shared_libraries.md:65-65	table-row	| `colony/authn` | Clerk JWT auth (see **authn** below) — the live copy of authn |
corpus/architecture/shared_libraries.md:66-66	table-row	| `colony/authorization` | ⚠️ Go **package name is `authorizer`** — `NewSentinelAuthorizer` (Connect-RPC client to Sentinel's `AuthorizationService`), `Authorizer` interface, `Decision` type, ctx helpers |
corpus/architecture/shared_libraries.md:67-67	table-row	| `colony/pubsub` | Watermill over **Redis Streams**: `NewPublisher`/`NewSubscriberServer`, generic `EventHandler[T]`, poison-queue DLQ + 3× exponential-backoff retry, proto `eventsv1.Event` envelope |
corpus/architecture/shared_libraries.md:68-68	table-row	| `colony/redis` | go-redis/v9 `UniversalClient` factory (pings on startup) |
corpus/architecture/shared_libraries.md:69-69	table-row	| `colony/rpc` | h2c (cleartext HTTP/2) Connect-RPC server/client + `DefaultInterceptors` (request logging + proto `Validate()`) |
corpus/architecture/shared_libraries.md:70-70	table-row	| `colony/flags` | Feature flags: `PosthogChecker` (PostHog EU, 5-min polling) and `EnvChecker` |
corpus/architecture/shared_libraries.md:72-75	paragraph	**Notable**: Sentry only initializes when `ENVIRONMENT=production` *or* `FORCE_SENTRY` is set (dev = plain slog).
corpus/architecture/shared_libraries.md:72-75	paragraph	The GraphQL public-middleware per-IP rate limiter (`rate.NewLimiter(1,5)`) currently **only logs** — its reject path is commented out, so colony does **not** actually enforce GraphQL rate limiting today.
corpus/architecture/shared_libraries.md:83-83	table-row	| **Module** | `github.com/anthropos-work/proto` |
corpus/architecture/shared_libraries.md:84-84	table-row	| **Language** | Go (`go 1.25.0`); tooling: `buf` (CI pins `v1.57.0`), protoc-gen-go, protoc-gen-connect-go, goverter |
corpus/architecture/shared_libraries.md:90-90	bullet	**Generated** — `proto/<svc>/v1/*.proto` → `go/<svc>/v1/` (message structs) + `go/<svc>/v1/<svc>v1connect/` (Connect-RPC stubs, *do not edit*).
corpus/architecture/shared_libraries.md:91-91	bullet	**Hand-written** — `go/domain/<svc>/` idiomatic Go types (string enums, `time.Time`) plus **goverter**-generated converters. goverter fails codegen if a proto enum value has no matching domain const — the "three-file rule" (proto + domain const + `make gen`).
corpus/architecture/shared_libraries.md:123-123	table-row	| **Module** | `github.com/anthropos-work/ai` |
corpus/architecture/shared_libraries.md:128-130	paragraph	A thin wrapper exposing **one interface, `ai.AI`** (`ChatCompletion`, `ChatCompletionStream`, `Response`, `CreateEmbeddings`, `CreateSpeech`, `OCRProcess`, `AudioTranscriptions`, `Tokenize`, `GetEndpoint`) over per-provider constructors:
corpus/architecture/shared_libraries.md:134-134	table-row	| `openai.New` / `NewOpenAI` | OpenAI direct |
corpus/architecture/shared_libraries.md:135-135	table-row	| `openai.NewAzure` | Azure OpenAI (default API version `2025-04-01-preview`) |
corpus/architecture/shared_libraries.md:136-136	table-row	| `anthropic.NewAnthropic(cfg, key)` | **AWS Bedrock** (`cfg!=nil`, EU `eu.anthropic.*` model IDs) **or** Anthropic-direct (`key!=nil`) — one constructor for both |
corpus/architecture/shared_libraries.md:137-137	table-row	| `mistral.NewMistral` | Mistral — **OCR only** (chat/embeddings/speech `panic`) |
corpus/architecture/shared_libraries.md:175-175	table-row	| **Module (standalone)** | `github.com/anthropos-work/authn` — **legacy** (tag `v1.7.0`) |
corpus/architecture/shared_libraries.md:176-176	table-row	| **Live form** | `github.com/anthropos-work/colony/authn` (absorbed into colony) |
corpus/architecture/shared_libraries.md:179-181	paragraph	Provider-agnostic authentication: verifies bearer tokens (Clerk JWTs in practice) and injects a typed `User`/`Organization` into request context for `net/http`, Echo, and GraphQL servers.
corpus/architecture/shared_libraries.md:185-186	bullet	`authn.NewManager(providers…)` tries each provider in order; only **Clerk** and a **Dummy** (test) provider exist.
corpus/architecture/shared_libraries.md:187-191	bullet	Clerk flow (`provider/clerk`): `jwt.Verify` against Clerk JWKS, then `jwt.Decode` to read custom session claims (`eid`, `email`, `firstname`, `lastname`, `org`, `org_id`, `org_role`) — a performance optimization to avoid Clerk API round-trips.
corpus/architecture/shared_libraries.md:187-191	bullet	`User.ID()` returns the **internal Anthropos UUID** (`eid`); `AuthID()` returns the Clerk subject — the two-identity bridge.
corpus/architecture/shared_libraries.md:193-202	paragraph	### ⚠️ Correction: authn is effectively a colony sub-package now **No checked-out service imports the standalone `github.com/anthropos-work/authn`** — they all import `github.com/anthropos-work/colony/authn`.
corpus/architecture/shared_libraries.md:193-202	paragraph	The standalone repo is legacy/orphaned (and its `HTTPAuthnMiddleware` has a missing `return` after the websocket-skip that colony's copy fixed).
corpus/architecture/shared_libraries.md:193-202	paragraph	**Relationship to Sentinel is loose**: Sentinel does **not** import authn. authn only *authenticates* (who you are); `app` then maps the resulting User/Org IDs into Sentinel Connect-RPC *authorization* calls (`gqlauthz.go`).
corpus/architecture/shared_libraries.md:210-210	table-row	| **Module** | `github.com/anthropos-work/taxonomy` (README title: **"nodeid"**) |
corpus/architecture/shared_libraries.md:212-212	table-row	| **Version pin** | `v1.2.0` |
corpus/architecture/shared_libraries.md:215-226	paragraph	**Where it lives.** The repo is a **131-line** node-id library (`node.go`) and ships **no dataset**.
corpus/architecture/shared_libraries.md:215-226	paragraph	The taxonomy *data* is owned by and stored in **app**'s `public` Postgres schema (the merged skiller domain — formerly the standalone skiller service's schema), originally loaded from **external** CSV/JSON by the former skiller importers (`importSkills` / `importJobRole`); taxonomy CLIs now live under `app/cmd/` (e.g. `createTaxonomy`).
corpus/architecture/shared_libraries.md:247-247	paragraph	The whole product is the `NodeID` type and its generators/validators:
corpus/architecture/shared_libraries.md:249-251	bullet	`NodeID` (a `string`) with `MarshalGQL`/`UnmarshalGQL` — satisfies gqlgen's marshaler **structurally** (no gqlgen import), so malformed IDs are rejected at the GraphQL boundary.
corpus/architecture/shared_libraries.md:252-254	bullet	Constructors: `NewSkillID` (K), `NewSpecializationID` (S), `NewCategoryID` (C), `NewJobRoleID` (J), `NewIndustryID` (I).
corpus/architecture/shared_libraries.md:252-254	bullet	As of `v1.2.0` all except Industry take an `organization *string` so IDs can be **org-scoped** (org folded into the hash).
corpus/architecture/shared_libraries.md:256-261	paragraph	**Canonical ID format**: `<PREFIX>-<WORDPART>-<HASH>` matching `^[CSKJI]-[0-9A-Z_]{6}-[0-9A-F]{4}$` — PREFIX = C/S/K/J/I; WORDPART = 6 uppercased alphanumerics (X-padded; multi-word = 3 chars/word; `.`/`+`/`#` → `DOT`/`P`/`SHARP`); HASH = first 4 hex of SHA-1 of the sanitized name(+org).
corpus/architecture/shared_libraries.md:256-261	paragraph	Example: `NewSkillID("go") = K-GOXXXX-F63F`, `".net" = K-DOTNET-DDE9`, `"c#" = K-CSHAXX-5F5B`.
