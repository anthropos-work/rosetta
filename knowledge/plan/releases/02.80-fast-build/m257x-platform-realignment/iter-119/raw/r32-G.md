# Seat G — reading #32

**Ground truth used (re-verified at this reading's open, per the ADDENDUM):**
`app` `ad9f3c49`, `platform` `0c91421d`, `next-web-app` `8297c684`, `sentinel` `f2c46190`,
`studio-desk` `41ee3575`, `ant-academy` `22df69dd`, `storage` `4ce8ece5`, `messenger` `fa47850d`,
`cms` `ca50c817`, `graphql-wundergraph` `60c229f3`, `roadrunner` `87d8d443`,
`jobsimulation` `462343b0`, `app/studio` `aeec036a`.
`rosetta-extensions` — **pinned per-stack consumption clone `stack-demo/rosetta-extensions` `09d06070`**
(the tree that settles "what the tooling does on a stack", §5 rule 45); authoring copy
`.agentspace/rosetta-extensions` `43049308` used only to confirm the two agree. Corpus HEAD `5b23559`.

**Shell hazard hit and corrected mid-pass (worth recording):** in this zsh, `git show "$C:repos.yml"`
silently applies the `:r` history modifier and yields `<sha>epos.yml` → `fatal: ambiguous argument`.
Two of my derivations returned an *empty set* from this before I read stderr. Every absence claim below
was re-run with `"${C}:path"` and a positive control.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/services/hiring.md` | 483 | yes |
| `corpus/services/studio-desk.md` | 449 | yes |
| `corpus/architecture/shared_libraries.md` | 274 | yes |
| `corpus/services/next-web-app.md` | 164 | yes |
| `corpus/services/clerk-integration.md` | 128 | yes |
| `corpus/services/skiller.md` | 78 | yes |
| (protocol, read as instructed) `corpus/ops/platform-alignment.md` §5 | 2803 total | §5 in full |

---

## Blockers

### B1 — hiring.md says the demo's hiring image bakes FOUR demo-patches; it bakes SEVEN

- **Anchor:** `corpus/services/hiring.md:434-437` —
  > "**Four demo-patches on the hiring image make it land** — 2 net-new (`next-hiring-role-remap`,
  > `next-hiring-members-pagination`) + the 2 chained shared `urls.ts` patches (the Studio-eject kill)
  > — see [`../ops/demo/demopatch-spec.md`] **§ the four hiring-image patches**"

- **Why it is false:** measured in the **pinned per-stack clone** `stack-demo/rosetta-extensions`
  `09d06070`, `demo-stack/up-injected.sh` `build_frontend_hiring()` declares **seven** manifests and
  fingerprints all seven:
  - `:1096` `next-hiring-role-remap`
  - `:1102` `next-hiring-members-pagination`
  - `:1111` `next-web-studio-url`
  - `:1112` `next-web-public-website-url`
  - `:1117` `next-web-interview-flag-container`  ← added M232
  - `:1118` `next-web-interview-flag-result`     ← added M232
  - `:1122` `next-web-back-to-cockpit`           ← added M249
  - `:1124-1126` `next_web_patchset_fp "$rolemap" "$pagination" "$studio" "$pubweb"
    "$interview_flag_container" "$interview_flag_result" "$back_to_cockpit"`

  The authoring copy `43049308` is **byte-identical** over that range (`diff` empty), so this is not a
  pin-vs-authoring artifact.

  It is **also a corpus self-contradiction, inside the very file hiring.md points the reader at**:
  `corpus/ops/demo/demopatch-spec.md:162-164` says *"hiring's over **7** … the 2 `apps/hiring` patches +
  the shared `urls.ts` pair + the shared interview flag-gate pair + the shared Back-to-Cockpit item"*,
  and `:174` enumerates all seven by name under an explicit *"Corrected at v2.8 M255"* blockquote —
  while `demopatch-spec.md:293` still carries the old *"**bakes FOUR patches**, not two"* sentence.
  So the twin file was half-repaired and `hiring.md` was not repaired at all.

  Finally, the cross-reference target does not exist: `demopatch-spec.md` has **no** section named
  *"the four hiring-image patches"* (`grep -i "hiring-image patches|four hiring"` → 0; heading census
  taken and none matches).

  This is not cosmetic: `up-injected.sh:1088-1094` states the exact hazard in code — *"a manifest added
  there and forgotten here is invisible to the cache key, which is the exact bug the fingerprint exists
  to kill"*. A doc that fixes the hiring patch set at four is the same stale-list defect one layer out.
- **Confidence:** high

### B2 — hiring.md cites `service_taxonomy.md:62` for the Tier-1 **Database** bullet; `:62` is a heading, the bullet is `:68`

- **Anchor:** `corpus/services/hiring.md:47-49` —
  > "as the twins [`service_taxonomy.md`]'s Tier-1 **Database** characteristic bullet (**`:62`** —
  > *\"one schema, `public`, owned by `app`, which is the only repo with migrations\"*, the `cms` /
  > `jobsimulation` / `skillpath` schemas *\"legacy husks\"*)"

- **Why it is false:** at corpus HEAD `5b23559`, `corpus/architecture/service_taxonomy.md:62` is
  `### Tier 1: Core Backend Services (Dockerized Go Microservices)` — a section heading containing none
  of the quoted text. The quoted bullet is at **`:68`**
  (`- **Database**: PostgreSQL — **one schema, `public`, owned by `app`**, which is the only repo with
  migrations (`repos.yml:14-17`) … the `cms`, `jobsimulation` and `skillpath` schemas are legacy husks`).

  Provenance, which is why I book it rather than waive it: `:62` **was** the bullet at `cd16967`
  (iter-102, where hiring.md's own parenthetical says it was repaired from the previously-wrong `:52`)
  and at `f8be5a1` (iter-108). It was moved to `:68` by `b4bdbfc` — an M257x/115 repair of a *different*
  predicate in the same file. This is §5 rule 34 exactly: a corpus repair silently invalidated an
  intra-corpus citation into the file it edited, and the citation now names the wrong construct — the
  second generation of the same defect at the same anchor.
- **Hesitation (stated as the instrument requires):** the passage names the construct in prose
  (*"the Tier-1 **Database** characteristic bullet"*) **and** quotes its text, and its own parenthetical
  warns *"the bullet is named as well as numbered because `service_taxonomy.md` is edited concurrently
  and `:62` can move."* A reader loses seconds, not a conclusion. I book it because the frozen
  instrument lists *"wrong line anchors that point at the wrong construct"* as a BLOCKER and resolves
  doubt upward, and because the *repair* that produced it is the recurrence, not the number.
- **Confidence:** medium

---

## Minors

- `corpus/services/hiring.md:243` — cites `persona_write.go:69-71,143-167` for *"the three
  [`validation_*`] tables the `PersonaSeeder` also writes"*. Measured (`09d06070`, identical in the
  authoring copy): the three tables are listed at `:66-68`; `:69-71` is the *"the old
  `jobsimulation.sessions` step is REMOVED rather than re-pointed"* paragraph. The actual **writes** are
  `:92-94`. `:143-167` covers `sessionCols()` + `attemptResultCols()` only — `skillResultCols()` starts
  at `:169` and `criterionResultCols()` later. Claim true, citation off.
- `corpus/services/hiring.md:300` — in-file citation `` `:193-198` `` for *"the **History**
  blockquote"*. `:193` is a blank line; the blockquote is `:194-207`. Off by one at the start and
  truncated at the end.
- `corpus/services/hiring.md:302` — in-file citation `` `:173-175` `` for *"the `job_position` bullet"*.
  `:173` is the closing line of the preceding (87-HIRING-sims) bullet; the `job_position` bullet is
  `:174-186`. Off by one. Both this and the item above were produced by applying a `+23`/`+16` delta
  (the passage says so) rather than re-deriving — §5 rule 34's non-idempotent re-point.
- `corpus/architecture/shared_libraries.md:57` — *"`sentinel`'s `88036d7` … took it `v0.34.3 →
  v0.35.2`, **two commits past** the `88bc5592` this row used to cite."* Measured:
  `git -C sentinel rev-list --count 88bc5592..88036d7` = **1**. `88036d7` is the immediate child;
  `f2c46190` (the checkout) is two past. The load-bearing half — that `88036d7` is the commit that
  closed the split — is correct (`88bc5592` reads `colony v0.34.3` / `proto v1.200.0`; `f2c46190` reads
  `v0.35.2` / `v1.210.0`).
- `corpus/services/studio-desk.md:154` — *"as a build arg (`docker-compose.yml:119`) and again in the
  runtime environment (`:135`) … (it was `:204` at `0dab54d`)"*. One historical anchor is given for two
  current ones: at `0dab54d` the build arg is `:204` (✓) but the runtime env is `:220`, not `:204`.
- `corpus/services/studio-desk.md:211` — the sample `.env` block writes `FORCE_READ_ONLY=0` while the
  repo's `.env.example:116` (@ `41ee3575`) uses `FORCE_READ_ONLY=false`. Cosmetic; I did not verify
  whether the reader is truthiness-tolerant.

---

## Positively cleared (audited zeros)

Labelled per the briefing. **ENUMERATED** = I derived the predicate's members from source and compared
cardinalities. **RE-DERIVED** = I only re-ran the arithmetic the document displays (the weakest form).

### `hiring.md`

- **ENUMERATED — `public.job_simulation_sessions` "has 23 columns" and the "four required-and-undefaulted
  columns".** Read the whole `CREATE TABLE` (`20260722104506.sql:2-27`): 23 columns exactly; the only
  `NOT NULL`-without-`DEFAULT` columns are `owner_id` `:6`, `sim_id` `:7`, `sim_type` `:10`, `token`
  `:13` — every other `NOT NULL` carries a `DEFAULT`. Also confirmed **no later migration ALTERs that
  table** (only `20260722104506.sql` and `20260729133514.sql` name it), so 23 holds at HEAD.
- **ENUMERATED — the `completion_status` enum is exactly five lowercase members.**
  `enum/jobsimulation.go:29-35` (const block) + `Values()` `:37-43`; SDL `jobsimulations.graphqls:14`
  and `:128`. Also read the *unconditional* cast chain the doc claims: `ent/jobsimulationsession.go:181-186`
  (`_m.CompletionStatus = enum.SessionCompletionStatus(value.String)`, no membership check),
  `intelligence.go:1844`, and the gqlgen marshalers `graph.go:129546-129554` (app enum) and
  `:129392-129400` (proto-bound twin) — both bare `graphql.MarshalString(string(v))`. The doc's
  retraction (*"the row does not vanish, it renders wrong"*) is correct at every hop.
- **ENUMERATED — the "no read path reads `is_hiring`" split.** `grep -in hiring` over
  `resolver_queries.go` @ `ad9f3c49` returns **4** lines, all sim-TYPE filters (`:811`, `:843`, `:991`,
  `:992`); over `internal/organization/intelligence.go` returns **0**. Positive controls exactly as the
  doc publishes them: `OrgFeatureInsights` ×**8**, `JobSimulationSession` ×**44**. The content-library
  counter-example verifies too: `resolver_cms_queries.go:95` (inside `PrivateJobSimulations`, declared
  `:89`), `:210`, `:258`, `:295` all call `GetOrganizationIsHiring`, and `:99-103` is the
  `hiringLibraryTypes` / `workforceLibraryTypes` branch.
- **ENUMERATED — the whole read-path table (rows 1–7) and the best-attempt window.** Opened and read
  around every anchor: `intelligence.go:1700` (`m.ent.JobSimulationSession.Query()`), `:1702`
  (`SimID`), `:1709` (`OrganizationID`), `:1733` (call site), `:1739-1744` (`RowNumber == 1` reduction),
  `:1820` (`score := RoundFloat(…)`), `:1846` (`Score: &score`), `:2124` (decl of
  `usersBestOrFirstJobSimulationSession`), `:2158-2160` (the literal
  `ROW_NUMBER() OVER (PARTITION BY sim_id, owner_id ORDER BY score DESC …)`), `:885-886`
  (`insightsSortColumn` mapping `Completition` → `FieldCompletionStatus`), `:1738-1764` (group→reduce→sort).
  Ent schema `job_simulation_session.go:33` (`sim_id`), `:39` (`completion_status`), `:45`
  (`field.Float32("score").Default(0).Min(0).Max(100)`). `local_jobsimulation_session.go` does **not**
  exist at `ad9f3c49` (positive control: 3 paths match `job_simulation_session`).
- **ENUMERATED — the migration story.** `20260729133514.sql:58` is the literal comment
  *"-- 5. Drop the mirrors."*, `:62` is `DROP TABLE "local_jobsimulation_sessions";`;
  `20260722104506.sql:79` is a bare `DROP TABLE "sessions";` under `search_path=public`, `:2` creates
  `job_simulation_sessions`, `:29` is the `token` UNIQUE index, `:53` re-points the `anticheat_results`
  FK. `SET "score"` → **0 hits** over the whole migration set (positive control: 62 `SET ` lines).
  Only `CREATE SCHEMA` in the entire set is `auth` (`20230817154747_supabase_baseline.sql:2`);
  `atlas.hcl:8` pins `search_path=public`. `20240527131926.sql:7` really is the mirror's
  `jobsimulation_id`; `20250416091037.sql:5` really is the mirror's `anticheat_summary`.
  `20260722081626_jobsim_data_model.sql:336/355/376` create the three `validation_*` tables, middle one
  spelled `validation_attempt_skill_results`.
- **ENUMERATED — the `is_hiring` guard sites in `app`.** `manager.go:450` (`switch org.IsHiring`) /
  `:453` (`antRole = enum.RoleCandidate`) / `:537` (`return fmt.Errorf("organization is not hiring")`)
  at `ad9f3c49`; and the banner's **non-uniform** back-offsets to `5ba17044` are right — `:448`, `:451`,
  `:487` (I read all three at that ref). `siminvitationlink.go:62` guard / `:63` hard-error, inside
  `CreateOrganizationSimInvitationLink` (decl `:56`). The `HiringConfigSeeder` genuinely bypasses that
  RPC: `hiring_config.go:99` `CopyRowsIdempotent(ctx,"public","organization_sim_invitation_links",…)`.
- **ENUMERATED — the Casbin substrate.** `stack-seeding/seeders/policy_grants.go:82` =
  `{"default", "admin", "org:feature:insights"}`; the resolver gate is the first statement of
  `InsightsJobSimulationByMemberships` (`resolver_queries.go:1035`), membership status filter at
  `:1053` (`{StatusActive, StatusInvited}`), and `:1089`/`:1085` really are the *neighbouring*
  `InsightsJobSimulationBySessions`'s `GetMembership` and its own gate.
- **ENUMERATED — the snapshot figures.** Read the capture manifests directly:
  `.agentspace/snapshots/directus/ea2e187a…/directus.simulations.copy` has **307** rows of which
  **87** carry `SIMULATION_TYPE_HIRING` (the doc's 87 ✓), and **no `job_position` table is captured at
  all** (the doc's "0 rows" ✓).
- **ENUMERATED — the M228 regression enumerates 8 seeders.** `hiring_scope_test.go`
  `TestGenericActivitySeeders_SkipHiringOrg` table has exactly 8 entries: jobsim-sessions, activity,
  skillpath-sessions, assignments, hero-activity, persona, feedback, succession.
  `skipGenericActivityForHiringOrg` / `IsHiringOrg()` at `hiring_scope.go:45-46`. The compare floor is
  a real constant: `contentref.go:158` `const hiringComparableFloor = 6`, cross-fenced against the
  Playwright `RENDER_GATE_FLOOR` in `users_m227_test.go:215-225`.
- **ENUMERATED, AND IT NEARLY COST ME A FALSE BOOKING — "5 admins + 45 candidates".** I first derived
  `roleForIndex` (`users.go:571-582`, `adminCount = int(50 × 0.14) = 7`) over
  `stories.seed.yaml:249` (`size: 50, role_mix {admin 0.14, member 0.0, candidate 0.86}`) and had this
  written up as a BLOCKER: 7 + 43, not 5 + 45. **It is wrong.** `roleForHero` overrides the two
  *candidate* cockpit heroes (Cara, Cody) who ride declaration-order slots 2 and 3 *inside* the admin
  band, so the net is **5 real admins + 45 candidates** — which is precisely why rext commit
  `5d0297e` ("M226 iter-04: fix the hiring org count → exactly 5 admin + 45 candidate") moved the mix
  `0.10 → 0.14` in the first place. The corpus is correct; the arithmetic over the un-enumerated set
  was not. Recording it because it is §5 rule 24 in the wild, caught only by reading the commit that
  produced the number. (Relatedly, `role_mix ≈ 0.1 admin / 0.9 candidate` at `hiring.md:163` also
  stands: 5/50 = 0.1 is the *net* mix; only the raw knob is 0.14.)
- **ENUMERATED — all `next-web-app` anchors in this doc, at `8297c684`.**
  `UserStatusContext.tsx:141-173` / `:144-149` / `:168-172`; `useGetClerkOrganization.tsx:16-18`,
  `:20-21`; `useNavbarSections.tsx:161` (`showWorkforce = true`), `:249-256`, `:329-331`, `:340-343`,
  `:391-398`, `:475-482`, `:476`, `:568`; `orgGroups.ts:48-65` with `intelligence: isAdmin` at `:61`
  and **no `isHiringOrg` parameter** (grep = 0); `apps/hiring/.../UserStatusContext.tsx:125`,
  `:144-145`; `template.tsx:90` (`isEnterprise = Boolean(organization)`);
  `FreeTrialContainer.tsx:29` (`Boolean(!isHiringOrg && organizationId)`);
  `InsightsByMembersContainer.tsx:359` (`<Drawer`); `simulationScoreColumn.tsx:95-97`. The two
  `simulationScoreColumn.tsx` copies are **byte-identical** (`diff` empty). `showWorkforce={false}`
  occurs in exactly **two** places tree-wide, both `apps/hiring/.../template.tsx` (`:167`, `:248`) —
  the retraction at `hiring.md:341-352` is correct. **Zero** `(.)`/`(..)`-prefixed dirs exist anywhere
  under `apps/` (positive control: 46 `@tabs` paths), so the M231 intercepting-route retraction holds.
- **ENUMERATED — the useNavbarSections "was `:460` at `bb3313bc`" back-anchor.** Read at `bb3313bc`:
  `:459-466` is `enterpriseInsightsMenuItem`, `:460` the `isHiringOrg ? results : activityDashboard`
  label. Exact.
- **ENUMERATED — `repos.yml` cardinality.** 6 entries at `0dab54d`, **4** at `0c91421d` (app,
  sentinel, next-web-app, studio-desk); `app` at `:14-17` is the only `migrations: true`. `d11a403`
  removed cms/jobsimulation/roadrunner; `838d907` removed storage/messenger. `jobsimulation` is in
  neither.

### `studio-desk.md`

- **ENUMERATED — the "NOT a GraphQL client" correction, all four legs.** At `41ee3575`:
  `git grep -in graphql -- 'src/*'` = exactly **2** lines, `src/routes/skillpath.ts:374` and `:405`,
  both comments saying the opposite; `git grep -n 8082 -- 'src/*'` = **0** (positive control:
  `express` ×13 in `src/index.ts`); `src/index.ts` mounts `/api/dev` `:150`, `/api/ai` `:158`,
  `/api/skillpath` `:161`, `/api/youtube` `:164`; every `new GraphQLClient(` in the repo is one of the
  **four** frontend sites the doc names, at the exact lines
  (`content/simulationContentService.js:325`, `taxonomyService.ts:43`, `userPreferencesService.js:13`,
  `userService.ts:20`), fed by `app/services/config.ts:6`.
- **ENUMERATED — the `GCLOUD_SERVICE_ACCOUNT` vestigial claim and its own iter-115 correction.**
  `.env.example` is **131** lines; `:117` `YOUTUBE_API_KEY=`, `:118` the comment, `:119`
  `GCLOUD_SERVICE_ACCOUNT=`, `:120` **empty** — exactly as the doc's caveat describes.
  `terraform/main.tf:129` injects it. `git grep GCLOUD_SERVICE_ACCOUNT -- 'src/*'` = **0**; tree-wide
  the only hits are `.env.example`, `CHANGELOG.md`, `terraform/main.tf`.
- **ENUMERATED — the compose anchors, at three refs.** `0c91421`: `:119` build arg, `:135` runtime env,
  `:138-140` `depends_on: backend`, `:141` `profiles: [studio-desk, all]`. `0dab54d`: `:204`/`:220`,
  `:223-225`, `:226`. `2adcf71`: `depends_on` `:337-341` **including `cms` at `:340-341`** — the doc's
  historical claim is exact.
- **ENUMERATED — no datastore.** `package.json` @ `41ee3575` declares none of
  pg/postgres/prisma/sqlite/sqlite3/mysql/mysql2/mongodb/mongoose/knex/drizzle-orm/typeorm/sequelize.
  `app/services/studioDB.js` exists as a facade; `GET_USER_STUDIO_PREFERENCES` /
  `SET_USER_STUDIO_PREFERENCES` are real GraphQL documents (`queries.ts:205`, `mutations.ts:58`).
  The **only** tailscale mention in the repo is `app/core/main.ts:105` (the GlitchTip DSN comment).
- **ENUMERATED — the MPA boot model.** `main.ts`: `preloadCriticalCSS()` at **L97**, `Sentry.init` L104,
  `posthog.init` L117, then exactly **three** blocking awaits — `await withClerkErrorHandling(() =>
  clerk.load(), …, {timeout: 10000})` L181 (the doc's *"NOT its 10 s timeout"* ✓),
  `await l12nService.init()` L191, `await userService.canAccess()` L199 — and `new PageWrapper()` at
  **L206**. Order and count as published.
- **ENUMERATED — the entry-point set.** `vite.config.ts` `rollupOptions.input` declares exactly the 9
  prod entries the doc lists (`home.html` as `main`, sim-advanced/guided/simulation builders,
  builder-skill-path, generation, catalog, academy, skills) with `dev-accept` behind
  `isProduction ? {} : {…}`. `src/prompts/` sub-dirs are exactly `start/`,
  `sim-advanced-builder/`, `sim-guided-builder/`, `builder-skill-path/`, `documents/` + loose `*.md`;
  `tests/` is `{e2e,frontend,integration,unit,utils}`; `src/` is `{index.ts,lib,prompts,routes,services,types}`.
- **ENUMERATED — the small scalars.** `src/routes/skillpath.ts` is **61,080 bytes** and is the largest
  route (ai 5,895 / dev 4,092 / youtube 4,023) → *"~61KB, the largest backend route"* ✓.
  `app/public/l12n/` holds exactly **7** locales (de,en,es,fr,it,ja,nl) → *"7 languages"* ✓.
  `engines.node ">=24"` + `Dockerfile`/`Dockerfile.dev` both `FROM node:24-alpine` ✓.
  `src/index.ts:60` `process.env.PORT || 9100` ✓. `jest.config.js` declares two `projects`,
  `displayName: 'backend'` and `'frontend'` ✓. `STUDIO_ACCESS_ROLES` at `src/index.ts:96` is exactly
  `['admin','org:admin','content_creator','org:content_creator']`, consumed by
  `checkEnterpriseAndAdmin` (decl `:99`, used `:115`, wired `:133`) ✓.
  `youtube.ts:43` reads `YOUTUBE_API_KEY` only, `_mock: true` fallback at `:46` ✓.
  AI tier defaults verified in `src/services/ai/config.ts:20-39`; in-code `defaultTier` fallback is
  `'thinking_fast'` (`:182`) ✓.
- **ENUMERATED — the rext side (pinned clone `09d06070`).** All five `studio-desk-*` patch dirs exist
  and are wired at `up-injected.sh:894-903`; `stack-verify/e2e/run-studio-fcp.sh` exists;
  `gen_injected_override.py:399/436/439` carries the existence-guarded `studio-desk/.env` `env_file`;
  both named regression tests exist (`stack-injection/tests/test_injection.py:1657` and `:1714`).
- **ENUMERATED — the "router survives in production only" half.** `graphql-wundergraph` `60c229f3`
  still has `terraform/main.tf:20 service_desired_count = 1` and a live `config.prod.yaml` /
  release workflow, while the platform compose at `0c91421` declares no such service. Both halves hold.

### `shared_libraries.md`

- **ENUMERATED — the headline count, over all seven Go repos on disk at their pinned refs.** I read
  every `go.mod` rather than re-deriving the doc's sum: colony **7/7**, proto **7/7**, taxonomy **6/7**
  (missing only roadrunner), ai **2/7** (cms, jobsimulation), authn **0/7**. Every per-repo line anchor
  and version in the three tables verifies exactly: `app/go.mod:15` `colony v0.35.2` / `:16`
  `proto v1.210.0` / `:18` `taxonomy v1.2.0` @ `ad9f3c49`; `app/go.mod:20 taxonomy v1.2.0` @ `b948604f`;
  `sentinel/go.mod:8` `v0.35.2` / `:9` `v1.210.0` / `:21` `taxonomy v1.2.0 // indirect` @ `f2c46190`;
  `sentinel/go.mod:21` @ `88bc5592` likewise; storage `v0.34.3` / `v1.196.0` / `:25 // indirect`;
  messenger `v0.35.2` / `v1.210.0` / `:9` direct; cms `:9 ai v1.40.2` / `v0.35.1` / `v1.207.0` /
  `:13 taxonomy`; jobsimulation `:11 ai v1.40.2` / `v0.35.1` / `v1.205.0` / `:15 taxonomy`;
  roadrunner `:7 colony v0.34.3` / `proto v1.196.0`, no taxonomy, `main.go:7` imports colony and uses
  `NewVersionConfig` at `:17`.
- **ENUMERATED — the `ai` fold.** `git show ad9f3c49:go.sum | grep -c "anthropos-work/ai "` = **0**
  (positive control: colony ×2); `1e457fa70` is exactly *"refactor(ai): fold the ai library into app as
  internal/ai"*, dated 2026-08-04; `internal/ai/ai.go` is **21 lines** declaring precisely
  `type AI interface` + `type TokenEncoder interface` and nothing else; the one-way-door test's comment
  in `internal/ai/module_import_guard_test.go` is quoted **verbatim** (the `rosetta-extensions/stack-seeding`
  sentence).
- **ENUMERATED — the vendor-selection retraction.** `flag_use_azure_us` occurs at exactly **three**
  sites tree-wide in `app` @ `ad9f3c49`: `internal/jobsimulation/ai/ai.go:267`, `:344`, and
  `internal/skillerai/ai.go:347` — matching the doc's three, in the doc's order. `getClient` decls at
  `jobsimulation/ai/ai.go:259` and `skillerai/ai.go:332`; `isThrottlingError` decl `:129` used at
  `:166` and `:325` (jobsim) and decl `:128` used at `:176` (skillerai). All open-and-read, not grepped.
  `external_services.md:579` is exactly *"There is **no ordered EU-first fallback chain.**"*
- **ENUMERATED — the six Connect handlers at `b948604`.** `main.go` registers exactly six `mux.Handle`
  calls: `:1187` Users, `:1188` Organizations, `:1196` Skiller, `:1204` JobSimulation, `:1213` CMS
  inside `if cmsRPCServer != nil` (`:1212-1214`), `:1228` LabSession. And the absence claim holds at
  `ad9f3c49`: `SkillPathSessionService` appears **only** in `CLAUDE.md` and `knowledge/architecture.md`
  (0 Go source), `RoadRunnerService`/`roadrunnerv1connect` only in `knowledge/plan/**` markdown
  (positive control: `SkillerService` matches Go source). `skillpaths.go:27-31` carries the
  *"drop-in for the **removed** skillpath RPC client"* comment verbatim.
  `jobsimwiring/wiring.go:123` @ `9d00a313` is `jsrunner.NewRunnerManager(getenv("JUDGE0_API_KEY"),
  getenv("JUDGE0_BASE_URL"))`; `ROADRUNNER_RPC_ADDR` = **0** hits in `app` @ `9d00a313` and **0** in
  `platform` @ `0dab54d` (positive control: `JUDGE0_BASE_URL` matches), and `docker-compose.yml:118`
  @ `0dab54d` really is `AWS_REGION=eu-west-1`.
- **ENUMERATED — the `repos.yml` history claim (11 Go repos ever, 14 names ever).** Derived the union
  over all **10** commits that touched `repos.yml`: `type: go` = {app, chronos, cms, intelligence,
  jobsimulation, messenger, roadrunner, sentinel, skiller, skillpath, storage} = **11**; all names ever
  = 14. All four of skiller/skillpath/chronos/intelligence are present in the first revision
  `a2a3ee6`, and skillpath *is* the last of the four to go (`fdfa189` intelligence → `045857c` chronos
  → `21429b7` skiller → `a4db680` skillpath, *"decommission standalone skillpath service (M507)"*).
- **ENUMERATED — the taxonomy figures.** Read the capture manifest itself
  (`.agentspace/snapshots/taxonomy/5afc0bcc…/manifest.json`): `source: primary-read`,
  `public_only: true`, `predicate: org-null`, `captured_at 2026-06-29T14:43:00Z`, and the row counts
  `skills 42,790`, `job_roles 22,470`, `job_role_embeddings 18,919` — every number the doc publishes,
  including the "18K came from `job_role_embeddings`" diagnosis. Also
  `skill_translations 85,545` / `job_role_translations 43,550` / `job_role_skills 72,705`.
- **ENUMERATED — the authn absorption.** `colony/authn` importers: app **129** files, sentinel **0**,
  cms **6**, jobsimulation **8**, storage/messenger/roadrunner **0** — the doc's "6 and 8" is exact.
  The **standalone** `github.com/anthropos-work/authn` is imported by **0** files in all seven repos.
  `storage/internal/migration/migration.go:13` really does alias
  `proto/go/simulator/storage/v1` as `legacyStorage`.
- **RE-DERIVED — "at least 13" Connect-RPC services.** The named list counts to 13. The doc itself
  discloses that `proto` is in no clone set and the list is hand-enumerated from consumers, so this is
  a floor I cannot independently enumerate.

### `next-web-app.md`

- **ENUMERATED — the version block.** All four `apps/{web,hiring,integration,maintenance}` declare
  `"next": "~16.2.12"` (a tilde), `apps/mobile` declares none, `pnpm-lock.yaml` resolves `next@16.2.12`,
  react/react-dom `19.2.7`, `engines.node ">=24.0.0"`, `packageManager pnpm@10.30.3`, `turbo ^2.9.6`.
- **ENUMERATED — `proxy.ts`, not `middleware.ts`.** Exactly three files tree-wide:
  `apps/{web,hiring,integration}/src/proxy.ts`; **zero** `middleware.ts` anywhere. The repo's own
  `CLAUDE.md:55` states the Next-16 rename verbatim and `:15` says *"Next.js 16 App Router"*.
  Every route the doc names is in `isPublicRoute` (`proxy.ts:7-57`) and `/print` is HMAC-gated via
  `isPrintRoute` + `validatePrintToken` (`:59`, `:66-72`), with `PRINT_ROUTE_SECRET` real
  (`packages/core-js/src/security/printToken.ts`).
- **ENUMERATED — storybook and Dockerfile absence.** No `storybook` script, no `.storybook/`; the only
  trace is `configs/tailwind/storybooks.css` — exactly as written. Exactly **one** Dockerfile in the
  repo (`Dockerfile.dev`).
- **ENUMERATED — 8 locales.** `configs/i18n/messages/` = {de,en,es,fr,it,ja,nl,pt} = 8.
- **ENUMERATED — the compose facts.** `docker-compose.yml` @ `0c91421` declares exactly five services
  (`sentinel`, `backend`, `studio-desk`, `next-web-app`, `gotenberg`) plus `include: common.yml`;
  `next-web-app` at `:143`, build arg `:151`, runtime env `:160`, `depends_on: backend` `:165-167`,
  `profiles: [frontend, all]`; `backend` `profiles: [core, backend, all]` at `:110`;
  `Makefile:119-120` `up-frontend` = `--profile core --profile frontend`. The historical `:236`
  @ `0dab54d` is the build arg.
- **ENUMERATED — the hiring retraction's own citations.** `apps/hiring/.../InsightsByMembersContainer.tsx:108`
  is the component declaration; `.../ai-simulations/[simId]/page.tsx:14` mounts it. rext's
  `demo-stack/frontend/hiring.Dockerfile` exists and `up-injected.sh:1076-1085` is
  `build_frontend_hiring` (image `demo-$N-hiring` at `:1083`, port 3001 named at `:1079`).
- **ENUMERATED — data-layer / ports / workspace.** `packages/graphql/package.json` declares
  `graphql-request` + `@tanstack/react-query` and **no** apollo anywhere; `packages/ui` `antd ^6.3.7`;
  dev ports 3000/3001/3002 in the app `package.json`s and Expo `3031` in `dev:mobile`;
  `pnpm-workspace.yaml` explicitly excludes `!apps/mobile`; only `apps/web` + `apps/hiring` declare a
  `test` script.

### `clerk-integration.md`

- **ENUMERATED — "webhooks (svix, 12 event types)".** `internal/clerk/events/events.go` has exactly
  **12** `case "` arms, spanning precisely the four families the doc names: `user.{created,updated,
  deleted}`, `organization.{created,deleted,updated}`, `organizationInvitation.{accepted,created,
  revoked}`, `organizationMembership.{created,deleted,updated}`.
- **ENUMERATED — every SDK version in the M257x-iter-108 correction.** `apps/{web,hiring,integration}`
  all `@clerk/nextjs ^6.39.6`; `apps/maintenance` declares none (so it is correctly not a fourth
  surface); `ant-academy/code/package.json:52` `^6.39.2`; `next-web-app/apps/mobile/package.json:6`
  `@clerk/clerk-expo ~2.6.18` vs `ant-academy/mobile/package.json:18` `~2.19.36` (13 minors apart, ✓);
  `app/go.mod:31` @ `5ba17044` `clerk-sdk-go/v2 v2.7.0` (still `v2.7.0` at `ad9f3c49`, `:29`).
- **ENUMERATED — the used/not-used feature surface.** Over `apps/` + `packages/` @ `8297c684`:
  `<SignIn` 5 files, `<SignUp` 2, `<UserProfile` 3; and **0** for `<UserButton`,
  `<OrganizationSwitcher`, `<OrganizationProfile`, `<Protect`, `@clerk/themes`, `GoogleOneTap`,
  `Waitlist`, and Clerk `has()`. `@clerk/localizations ^4.13.10` present.
  studio-desk installs `@clerk/clerk-js` + `@clerk/express`; `e2e` installs `@clerk/backend` +
  `@clerk/testing`.
- **ENUMERATED — "`AuthRole()` has zero call sites on the allow/deny path".** Stronger than claimed:
  `AuthRole()` has **0** occurrences in `app`'s Go source at `ad9f3c49`. And the server-side bare-role
  claim verifies at the one site the doc names: `apps/web/src/app/api/metabase/route.ts:35`
  `authData.orgRole !== 'admin'`.
- **ENUMERATED — the ant-academy caveat.** `mobile/src/components/ClerkSignInForm.tsx:49-52`:
  `if (result.status === 'complete') { setActive(...) } else { setError('Sign-in needs another step
  that this app does not support yet.') }` — it does abort on any second factor.
  `code/proxy.js` is the middleware; `REQUIRE_ORGANIZATION_MEMBERSHIP` → `/no-organization` at
  `:91`, `:310-327`.
- **ENUMERATED — the "app alone imports colony/authn" measurement** (129 / 0 / 0 / 0), as above.

### `skiller.md`

- **ENUMERATED — the merged shape.** All six named Ent schema files exist under
  `internal/data/ent/schema/`; `internal/rpc/skillerrpc/skiller.go` implements exactly the five named
  externally-reached methods (`GetSkill:73`, `GetSkills:93`, `SearchSkill:116`, `MatchSkill:147`,
  `GetJobRole:187`); `graph/schemas/skiller_taxonomy.graphqls` exists and `categoryTree` /
  `fullCategoryTree` survive **only** in its `:7` comment saying they stay unported.
- **ENUMERATED — the `SKILLER_RPC_ADDR` history, which is the doc's sharpest claim.** At `d11a403^`
  compose sets `SKILLER_RPC_ADDR=http://backend:8083` (four blocks) **while** `CMS_RPC_ADDR` and
  `JOBSIMULATION_RPC_ADDR` still address the husks — so *"it already did before `d11a403`"* is exact.
  At `d11a403` the surviving `SKILLER_RPC_ADDR` (`:168`) sits in the **`messenger`** block (derived by
  awk-tracking the enclosing service key), and at `0c91421d` there are **zero** `*_RPC_ADDR` values
  (positive control: `AUTHORIZATION_ADDRESS=http://sentinel:8087` at `:48`).
- **ENUMERATED — the "0 hits over all 44 tracked `.tf` files in the 13 `stack-demo` repos" claim.**
  Re-derived the denominator per clone: app 5, cms 4, graphql-wundergraph 4, jobsimulation 6,
  messenger 4, next-web-app 2, roadrunner 4, rosetta-extensions 2, sentinel 4, storage 5, studio-desk 4
  = **44** (ant-academy 0, platform 0; the two nested `studio` repos hold 0). Searching each at its own
  ref for `backend.internal.anthropos` returns **0** (positive control: 4 `.tf` files in `app` match
  `aws`). The count and the zero both hold.
- **ENUMERATED — embeddings + localization.** `20260615130000_skiller_taxonomy.sql:51/241` create
  `job_role_embeddings` / `skill_embeddings`; `:61` is
  `CREATE INDEX … USING IVFFLAT ("small_embedding3")`; `extensions.vector(1536)` appears in the
  migration set; `skill_translations` `:253` and `job_role_translations` `:192` exist; the
  `ContentLanguage` SDL enum (`assignments.graphqls:44-53`) has exactly **8** members. The
  "`extensions` schema must pre-exist" claim is consistent with the independently-derived fact that the
  only `CREATE SCHEMA` in the whole set is `auth`.

---

## What I could not settle

- **`colony`'s own contents** — the package table (`colony/authn`, `colony/authorization` with Go
  package name `authorizer`, `colony/pubsub`, `colony/redis`, `colony/rpc`, `colony/flags`), the
  `go 1.25.0` / `golang:1.26-bookworm` line, the 30 s DB health monitor, the Sentry
  `ENVIRONMENT=production || FORCE_SENTRY` gate, and the *"`rate.NewLimiter(1,5)` currently only logs,
  its reject path is commented out"* claim. `colony` is a private Go module and is **in no clone set**.
  Settled by cloning `anthropos-work/colony` at `v0.35.2` and reading it.
- **`proto`'s service census** — same reason. The doc labels *"at least 13"* a floor and discloses the
  hand-enumeration; I could confirm the list counts to 13 but not that it is complete, nor the
  `buf.build/anthropos/platform` / no-`buf.work.yaml` / `go/simulator/*` claims (except the one
  downstream consequence, which does verify from `storage`).
- **`ai` (the standalone module) and `taxonomy` (the library)** — the `ai.AI` method list the doc
  publishes includes `ChatCompletionStream`, which the **in-app fold** (`app/internal/ai/ai.go`) does
  **not** declare; a characterization test comment (`internal/ai/anthropic/characterization_test.go:313`)
  says *"ChatCompletionStream left with the whole streaming path"*. That is consistent with the doc
  describing `github.com/anthropos-work/ai@v1.40.2` rather than the fold, but I cannot read v1.40.2 to
  confirm, so I did not book it. Likewise the taxonomy repo's *"131-line `node.go`"*, `v1.2.0`
  constructor set, the `^[CSKJI]-[0-9A-Z_]{6}-[0-9A-F]{4}$` format and the three worked ID examples —
  the repo is in no clone set. Settled by cloning `taxonomy@v1.2.0` and `ai@v1.40.2`.
- **Compose *runtime* behaviour of a single-profile selection** — `studio-desk.md:21` and
  `next-web-app.md:113-117` both assert `EXITS 1` with a quoted compose error. The static inputs check
  out (`studio-desk`/`next-web-app` `depends_on: backend`; `backend` is not in either profile), and
  root `CLAUDE.md` agrees, but I did not run Docker, and modern compose versions differ on whether a
  dependency's profile is auto-enabled. Settled by one `docker compose --profile studio-desk config -q`
  on this host.
- **Production-side assertions** — *"the Cosmo Router survives in production only"*
  (`studio-desk.md:146`), the skiller ECS/ECR teardown (`skiller.md:37-39`), and
  `skiller.md`'s deliberate non-assertion of the prod `skiller_rpc_addr`. The deciding declarations are
  in the `infrastructure` repo, which has never been in a clone set. What I *could* check —
  `graphql-wundergraph`'s own `terraform/main.tf:20 service_desired_count = 1` — is consistent with the
  doc, but is not the deciding artifact.
- **Directus bootstrap internals** — `studio-desk.md:123`'s `bootstrap/index.js:81` inside the pinned
  `directus/directus:11.6.1` image. Settled by `docker run --rm --entrypoint cat directus/directus:11.6.1
  /directus/bootstrap/index.js`.
- **Historical live measurements quoted as evidence** — the render-gate cohort sizes
  (`hiring.md:466`: 8/8/9/9/8 = 42), the recruiter p95 `1.27 s`, the studio FCP `4669 → 817 ms`, the
  `canAccess` `4049 → 38 ms`. These are past runs on `billion` / `demo-2`; nothing on disk can
  falsify them. I note only that 42 is arithmetically consistent with the seeded 45 candidates minus
  the assigned-only tail, and not with any other population figure in the file.
- **`hiring.md:172`'s "87 real HIRING sims" I could settle; `demopatch-spec.md:162`'s
  `up-injected.sh:1071-1075` anchor I could not repair** — that anchor is off (the seven manifests are
  `:1096-1122`, the fingerprint call `:1124-1126`), but `demopatch-spec.md` is outside my file set, so
  I record it as an observation supporting B1 rather than booking it here.
