# Seat C — reading #32

**Ground truth re-verified at this reading's open** (all shas match the ADDENDUM's superseded table):
`app` `ad9f3c49` · `platform` `0c91421d` · `next-web-app` `8297c684` · `sentinel` `f2c46190` ·
`studio-desk` `41ee3575` · `ant-academy` `22df69dd` · `storage` `4ce8ece5` (origin/main `9f8cb532`) ·
`messenger` `fa47850d` (origin/main `e9421c68`) · `cms` `ca50c817` (origin/main `f38c0c4a`) ·
`graphql-wundergraph` `60c229f3` · `roadrunner` `87d8d443` · `jobsimulation` `462343b0`
(origin/main `82cb66ec`) · `stack-demo/rosetta-extensions` `09d06070` ·
`.agentspace/rosetta-extensions` `43049308` · nested `stack-demo/app/studio` and `stack-demo/cms/studio`
both `aeec036a`.

**Trees I read**: platform evidence is from the `stack-demo/*` clones (and the nested `app/studio` repo
grepped at **its own ref** `aeec036a`, never at the host's HEAD). For `alignment_testing.md` I read the
**authoring copy** `.agentspace/rosetta-extensions` `43049308` — per §5 rule 50, a claim about a fence's
own *configuration or verdict* (DNA contents, gate thresholds, exit-code constants, coverage guards) is
settled by the tree that configuration lives in, and the authoring copy is where they are written. I
**also** diffed the layout against the pinned per-stack clone `stack-demo/rosetta-extensions`
`09d06070` and the two agree on every structural claim I checked, so no finding in that file turns on
which tree you pick.

**Pipeline hazards I hit and corrected mid-pass** (recorded because a silent one would have produced a
false absence): (a) `zsh` applies history-style modifiers after `$var:` — `"$r:repos.yml"` expands via
`${r:r}` and `"$R:terraform/main.tf"` via `${R:t}`, so four of my `git show` calls silently produced
*empty* output that `sed` then swallowed. Re-run with `"${r}:path"` they all resolved. (b) A
`git log -p`-based derivation of the compose service set returned **12** because my section tracker only
saw added lines; reconstructing all 80 historical file versions returned the true **25**. Both were
caught by positive controls, not by luck.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/architecture/alignment_testing.md` | 542 | yes |
| `corpus/architecture/ai_architecture.md` | 340 | yes |
| `corpus/architecture/platform-migration-status.md` | 330 | yes |
| `corpus/services/coursebuilder.md` | 161 | yes |
| `corpus/services/skillpath.md` | 117 | yes |
| `corpus/services/gotenberg.md` | 84 | yes |

All six read top-to-bottom, in full. **Coverage caveat, stated rather than hidden (§5 rule 8 — a skip
reads exactly like a pass):** `alignment_testing.md` was read last and its ground-truth derivation is
**thinner** than the other five — I verified its structural and enumerable claims (see clearances) but
did not re-derive its narrative history (M1/M1b/M2/M2c/M3/M7b/M9a milestone attributions) or its
historical live measurements (the M7b `100% / Critical 100%` on `demo-1`, the toy `86.7%` transcript).
Those are listed under *what I could not settle* rather than counted as clearances.

## Blockers

### B1 — Three intra-corpus self-citations in `ai_architecture.md` are 5 lines stale and land on the wrong construct

- **Anchors (one defect, three sites — repaired in one pass, so booked as one block):**
  - `corpus/architecture/ai_architecture.md:92` — *"The row's construction rule is enumerate the constants
    (**its OpenAI sibling at `:86`** is an exact enumeration of `internal/ai/openai/config.go:8-26`)"*
  - `corpus/architecture/ai_architecture.md:94` — *"a module **no repo a stack builds requires** since the
    fold at `1e457fa70` (**see `:95` below**)"*
  - `corpus/architecture/ai_architecture.md:295` — *"**NB this bullet is not a claim about the platform's
    Anthropic model SET** (that is **the table row at `:87`**, which is where the 4.6/Opus-4.8 omission
    lived)"*
- **Why it is false:** measured in the corpus itself at rosetta HEAD:
  - `:86` is prose — *"The **Default client** column is the client the vendor const resolves to — *not* a
    position in a"*. It is not a table row at all (the table header is `:89`). The **OpenAI row is `:91`**.
  - `:87` is the continuation of that same sentence — *"fallback order (there is none; see above)."*
    The **Anthropic row is `:92`** — i.e. the row the passage at `:295` is written *inside* cites itself
    five lines short.
  - `:95` is the **Transcription** table row (*"| **Transcription** | GPT-4o Transcribe | Azure EU (US via
    `flag_use_azure_us`) |"*), which says nothing about the `ai` module or the fold. The claim it is
    pointing at lives in *Unified AI Library*: heading `:98`, proposition `:100`, `go.mod` evidence
    `:106-109`.
  - **The offset is +5 in all three cases** (`86→91`, `87→92`, `95→100`), which is the signature of a
    five-line insertion above ~line 86 that was never re-pointed — §5 rule 34 exactly (*"line numbers move
    when YOU edit too"*). The control that this is drift and not my misreading: the **fourth** bare
    self-citation in the same file, `:114`'s *"the ⚠️ … at the head of this file (`:15-17`)"*, sits
    **above** the insertion point and resolves **correctly** (`:15-17` is the ⚠️ blockquote).
  - Both cited *targets* are, ironically, correct claims: `internal/ai/openai/config.go:8-26`
    @ `app` `ad9f3c49` is an 11-const block matching the 11 models the `:91` row lists, and the
    Anthropic row's own enumeration is exact (see clearances). Only the pointers are wrong.
- **Confidence:** high

### B2 — `ai_architecture.md` contradicts itself on voice-engine granularity nine lines apart; the code settles it against the earlier line

- **Anchor:** `corpus/architecture/ai_architecture.md:211` — *"**Configuration**: Voice engine is
  selectable **per simulation** in CMS (`livekitgptrealtime`)"*, against
  `corpus/architecture/ai_architecture.md:220` — *"**Engine choice is per SEQUENCE, from the CMS
  `voice_engine` field**"*.
- **Why it is false:** `voice_engine` is a **Sequence** field, not a simulation field, at `app`
  `ad9f3c49`:
  - `app/internal/cms/directus/collections/jobsimulation.go:834` declares `type Sequence struct {`, and
    `:911` is `VoiceEngine *SimulationVoiceEngine \`json:"voice_engine,omitempty"\`` — inside that struct
    (I derived the enclosing type by taking the last `^type … struct` before `:911`; the same struct holds
    `AIVendor` `:905` and `AIModel` `:906`, which the corpus itself correctly calls per-sequence at `:264`
    and `:277`).
  - It is read **per sequence**, inside the sequence loop: `:1350`
    `VoiceEngine: voiceEngineFromDirectus(seq.VoiceEngine),` — the same `seq` whose `seq.AIVendor` is
    defaulted at `:1303` before `sequences = append(…)` at `:1307`.
  - So `:220` is right and `:211` is the pre-correction phrasing left standing above it. This is the
    dominant failure mode §5 names (*a corrected statement contradicted by prose a few lines away*), with
    the sides inverted — the stale line is **above** the correction, which is why a reader scanning the
    *Active Engine* section meets the wrong one first.
  - **Materially misleading:** a multi-sequence simulation can carry a different engine per sequence; an
    operator told the setting is "per simulation" will look for one control and mis-diagnose a session
    that starts on ElevenLabs and continues on LiveKit.
- **Confidence:** medium-high. I hesitated because *"selectable per simulation in CMS"* can be read
  loosely as *"you set it on the simulation's CMS authoring surface"*. I booked it because the very next
  section **bolds** the opposite as a correction, which makes the pair a self-contradiction regardless of
  which reading you prefer, and because ground truth picks a side.

### B3 — `ai_architecture.md:264` cites a line range as "the enum" that omits 3 of the enum's 11 members

- **Anchor:** `corpus/architecture/ai_architecture.md:264` — *"Selected model from the CMS `ai_model` /
  `ai_vendor` fields per sequence (e.g. `gpt-5`, `gpt-4.1`, `anthropic-45-sonnet-aws` — **the enum is
  `app/internal/cms/directus/collections/jobsimulation.go:983-990`**)"*
- **Why it is false:** the sentence names no ref, so it grades at the checkout `ad9f3c49`. There, the
  `AIModel` const block is **`:979-991`**, with its **11** members at `:980-990`:

  | line | member |
  |---|---|
  | 980 | `Anthropic35SonnetAws = "anthropic-35-sonnet-aws"` |
  | 981 | `Anthropic37SonnetAws = "anthropic-37-sonnet-aws"` |
  | 982 | `Anthropic4SonnetAws = "anthropic-4-sonnet-aws"` |
  | 983–990 | the 8 the cited range covers |

  The cited range starts at `:983`, so it **drops the first three Anthropic members**. A reader deriving
  the legal `ai_model` set from the anchor gets **8 of 11** and would conclude
  `anthropic-35-sonnet-aws` / `anthropic-37-sonnet-aws` / `anthropic-4-sonnet-aws` are not authorable —
  yet `simulator/ai/ai.go:90-95` has an explicit `case` for each of the three, and `:98-99` makes
  `Anthropic37SonnetAWS20250219` the AnthropicAws-arm fallback. This is not line drift: the block is
  `:979-991` at the **older** `b948604` too, so `:983-990` was never the enum at any ref in the clone set.
- **Confidence:** medium. I hesitated because the anchor lands *inside* the right construct — it truncates
  rather than misses — and the corpus's three example values all happen to fall in range. I booked it
  because *"the enum is `<range>`"* is a **completeness** claim about a range (§5 rule 17: verify the
  predicate, not the arithmetic), and because every other enumeration anchor I checked in this same file
  (`completion.go:20-30`, `openai/config.go:8-26`, `speech.go:9-12`, `jobsimulation.go:1079-1085`,
  `:1594-1597`) is byte-exact — an off-by-3 range here is anomalous, not stylistic.

## Minors

- `ai_architecture.md:40` — *"`studio/tools/pdf2md.py:24` (`mistral-ocr-latest`)"*. At
  `stack-demo/app/studio` `aeec036a`, `:24` is `from mistralai import Mistral`; the literal
  `model="mistral-ocr-latest"` is at `:127` of the same file. The surrounding claim (Mistral is OCR-only
  in that CLI) is true and the reader lands in the right file; only the parenthetical's implied line is
  off. Low harm, so MINOR rather than a fourth blocker.
- `ai_architecture.md:131` — *"Measured at `app` HEAD against the shipping
  `studio/configs/production_config.ini:26-36`"*. The slot table is exactly right (verified line by line,
  plus `development_config.ini:26-36` identical and `config_template.ini:39`/`:40` carrying `gpt-4o`), but
  `stack-demo/app/studio` is its **own** checkout at `aeec036a`, invisible to `app` HEAD
  (`git -C stack-demo/app grep <anything> HEAD -- studio/` returns 0 for every predicate). The ref label
  names a tree that cannot answer; the numbers are correct.
- `coursebuilder.md:129` — *"`go test ./internal/coursebuilder/...` # ~55 test files"*. Measured 61
  `_test.go` files under that path @ `ad9f3c49`. Approximation, marked `~`.
- `coursebuilder.md:130` — *"~25 boundary test files"*. Measured 33 under
  `internal/web/backend/coursebuilder/` @ `ad9f3c49`. Approximation, marked `~`, but ~32 % low.
- `skillpath.md:107` — *"the body renders the literal string **"Coming soon"**"*. It renders the i18n key
  `t('enterprise.insights.comingSoon')` (next-web-app `8297c684`,
  `packages/…/InsightsBySkillPathStudentSimulationsContainer.tsx:137-139`). The **en** value is exactly
  `"Coming soon"` (`configs/i18n/messages/en/enterprise.json:2101`), so the claim is true for an English
  session and false for de/es/fr (`"Demnächst verfügbar"`, `"Próximamente"`, …). The load-bearing half —
  *the page is byte-identical whether or not you seed* — is verified: `userData` is hardcoded
  `null as unknown as MembershipEnriched` (`:31-34`) and the results table + totals block are both
  commented out (`:140-160`).
- `alignment_testing.md:3` — *"**Last updated:** 2026-06-06"*. The file has been modified at least five
  times since, most recently 2026-08-06 (`00be1ac`), and its own body cites M218, M219 and a *"Corrected
  M257x iter-85"* parenthetical — all later than the header's date. The error is in the conservative
  direction (a reader under-trusts a doc that is days old, not one that is stale), and no technical
  proposition depends on it, so MINOR; I note it because this is one of only two files in
  `corpus/architecture/**` + `corpus/services/**` that carries a `Last updated` header at all
  (the other is `clerkenstein.md`), which makes a stale one easy to miss.
- `coursebuilder.md:101` — *"colony pub/sub (author-lifecycle emails → messenger → Brevo)"*. `messenger`
  is a name that no longer denotes a process (`838d907` deleted the container; the domain runs in-process
  in `app` behind `MESSENGER_ENABLED`, unset = off on a developer machine). The pipeline shape is right if
  read as a domain; the sentence is the *named-consumer-list-survives-the-fold* shape §5 warns about. Not
  booked as a blocker because the map row this seat verified describes the same fold and the corpus uses
  "messenger" for the domain throughout.

## Positively cleared (audited zeros)

### ENUMERATED — predicate re-derived from source, cardinality compared

1. **`coursebuilder.md:77-94` — the 16 SSE wire event names, and `cost` not being one.** I did **not**
   re-derive the corpus's list; I enumerated the writers. Across the whole `app` repo @ `ad9f3c49` there
   are exactly two `writeSSE` call sites on the coursebuilder wire — `handler.go:1458`
   (`"session"`, right after `beginSSE` at `:1454`) and `:1479` (`renderEvent`'s name, guarded by
   `if name != ""` at `:1478-1480`). `renderEvent` (`:2604-2772`) returns **15** distinct non-empty
   names — `text` `:2607`, `score` `:2609`, `patch_applied` `:2614`, `patch_skipped` `:2620`, `stage`
   `:2637`/`:2639`, `outline` `:2654`, `progress` `:2661`, `preview_ready` `:2671`, `draft_kept` `:2678`,
   `error` `:2704`, `translation_ready` `:2724`, `rebuild_required` `:2732`, `steering_received` `:2744`,
   `steering_applied` `:2754`, `done` `:2768` — plus the `default:` arm re-emitting `text` at `:2770`
   (same name, not a 16th) and `case cb.EventCost:` `:2709` returning `"", nil` at `:2717`. **15 + session
   = 16**, `cost` filtered, every one of the 17 line anchors byte-exact.
2. **`platform-migration-status.md:75-81` — the completeness sets, both of them.** `git log -p --follow --
   repos.yml` over 10 commits yields exactly the **14** names the passage lists, no more and no fewer.
   For compose I reconstructed **all 80** historical versions of `docker-compose.yml` and parsed the keys
   under `services:`: exactly **25** service names, including the five pre-history ones the passage calls
   out (`nats`, `web-app`, `chromedp`, `simulator`, `realtime`); a section-blind pass returns **26**, and
   the one extra token is **`app-network`**, exactly as claimed. I then checked coverage: all 25 have rows,
   with three filed under the repo name (`backend`→`app`; `graphql` and `wundergraph`→
   `graphql-wundergraph`) — also exactly as the passage says.
3. **`platform-migration-status.md:92` — the `STORAGE_RPC_ADDR` counts, every sub-count.** Go grep at
   `2035f9a`: **3** hits, all comments (`main.go:504`, `internal/jobsimwiring/wiring.go:101`,
   `internal/storagens/callsites_test.go:189`) — I read `:504` and it does say *"the standalone service
   takes no traffic and STORAGE_RPC_ADDR is gone"*. Go grep at `b948604`: **15** hits, of which exactly
   **7 are env lookups** — `main.go` ×3 (`:446`, `:524`, `:992`), `wiring.go` ×1 (`:115`, the lowercase
   `getenv` helper), and one each in `cmd/import` `:50`, `cmd/academyImport` `:231`,
   `cmd/academy-asset-upload` `:129` — and the other **8** are 2 doc comments (`:28`, `:35`), 2 error
   strings (`:133`, `:235`), **3** `t.Setenv` (`:41`, `:52`, `:64`) and **1** `t.Fatal` message (`:47`).
   The cell's own hair-splitting — *"counting all four as `t.Setenv` overstates the arrange side by one"*
   and *"six of the seven are spelled `os.Getenv`"* — is correct in both directions.
4. **`ai_architecture.md:92` — the Anthropic model set.** `internal/ai/anthropic/completion.go:20-30`
   @ `ad9f3c49` holds **6** consts over **5** families, with 3.5 appearing twice (Bedrock-EU
   `eu.anthropic.claude-3-5-sonnet-20240620-v1:0` and direct-API `claude-3-5-sonnet-20241022`) exactly as
   the parenthetical says. 4.6 is at `:29`, and all three claimed production pins hold:
   `internal/askengine/bedrock.go:25`, `internal/jobsimulation/agent/report_agent.go:31`,
   `internal/coursebuilder/bedrock.go:29`; Opus 4.8 is `DefaultAuthorModelID` at
   `internal/coursebuilder/bedrock.go:23` and is indeed **not** in the `completion.go` block.
5. **`ai_architecture.md:91` — the OpenAI model set.** `internal/ai/openai/config.go:8-26` is an 11-const
   block; the row lists 11 and they correspond one-to-one.
6. **`ai_architecture.md:94` — the Speech set.** `internal/ai/speech.go:9-12` is the *entire* `SpeechModel`
   const block: `GPT4oMiniTTSS = "gpt-4o-mini-tts"` and `DefaultModel = GPT4oMiniTTSS`. One model, and it
   *is* `DefaultModel`.
7. **`ai_architecture.md:67-71` — five enum members vs four switch cases.** Directus `AIVendor`
   (`jobsimulation.go:969-975`) has **5** members; `simulator/ai/ai.go` has **4** vendor cases (`:58`
   `Openai`, `:69` `Azure`, `:86` `AnthropicAws`, `:102` `Anthropic`); `azureglobal` is at
   `jobsimulation.go:971` and is the one with no case; `:114-115` is `default: aiVendor =
   internalAi.Openai`. Every anchor exact, and the *outcome-coincides-but-doors-differ* distinction holds.
8. **`gotenberg.md:47` — the nine MIME types and the two call sites.** `extract.go:17-27` @ `9d00a313` is a
   9-entry map (`.xls`, `.xlsx`, `.ppt`, `.doc`, three OpenDocument, RTF ×2), and DOCX
   (`…wordprocessingml.document`) is genuinely absent. `ConvertToPDF` has exactly **2** non-definition Go
   call sites in the whole repo at both `9d00a313` and `ad9f3c49`: `extract.go:77` and
   `user_import_resume_2d.go:68`. Both discard the PDF in the next statement (`:81` and `:73`
   `ocrInput = pdfBytes`), so the *"never stored, never served"* framing is right.
9. **`coursebuilder.md:24` — 7 translate locales.** `internal/coursebuilder/translate.go:155`:
   `case "it", "fr", "de", "es", "nl", "ja", "pt":`.
10. **`skillpath.md:30-40` — `SkillPathSessionService` has 0 occurrences in Go source.** At `ad9f3c49` the
    identifier occurs **twice** in the whole repo, both in markdown (`CLAUDE.md:109`,
    `knowledge/architecture.md:28`) — the two the cell names as lagging planning docs, at the exact lines
    it names. Zero in `.go`.
11. **`skillpath.md:49` / the `repos.yml` count series.** 9 @ `2adcf71` (with no `skillpath`), 6 @
    `0dab54d`, 6 @ `d11a403`, **4** @ `838d907` and `0c91421` — and the `d11a403` diff really does delete
    `- name: cms`, `- name: jobsimulation` **and** `- name: roadrunner`, which is the diff the map's
    roadrunner row uses to refute that commit's own message.
12. **`platform-migration-status.md:90` — the compose service-block counts.** `d11a403^` = **11**,
    `d11a403` = **8**, `838d907` = `0c91421` = **5**. Exactly the three numbers the row publishes.
13. **`platform-migration-status.md:52` / `:181` — the state vocabulary.** §1 lists **8** states, and no
    row in the table carries `mid-fold` or `running_but_unfederated`, exactly as §1 claims.
14. **`ai_architecture.md:302-304` — `checkerEngines` is stored and never read.** The identifier has 6
    occurrences at `ad9f3c49` (`criterion.go:34,72,90`; `validator.go:43,60,595`) and **none** is an index
    or lookup — declaration, parameter, assignment, literal, pass-through. The dispatch really is the
    hardcoded `switch` at `criterion.go:126-127`.
15. **`ai_architecture.md:33-42` — Mistral is OCR-only.** In `stack-demo/app/studio` @ its **own** ref
    `aeec036a` the term returns **22 lines / 3 files** (`requirements.txt`, `tools/pdf2md.py`,
    `tools/r3.py`) — the exact figure the passage publishes — and `bedrock|boto3` returns **0 files**
    against a positive control of 94 files containing `import`. Go-side, `markdownManager.go:29-30` is
    `func NewMarkdownManager` returning `mistralocr.New(aiKey)`, and `studioManager.go:583` is its only
    caller.
16. **`alignment_testing.md:259-265` — the five DNAs' gene and capability counts.** I parsed all five
    JSON files in `.agentspace/rosetta-extensions` `43049308` and counted capabilities and variants
    rather than trusting the table: `clerk-2.6.0` **14 caps / 27 genes**, `clerk-js-5` **6 / 9**,
    `clerk-multi-1` **5 / 9**, `clerk-deploy-1` **3 / 7**, `clerk-express-1` **5 / 13** — all five
    exact, including the `13 genes / 5 capabilities` at `:359` for the express surface. The same pass
    settles `:208`'s *"only `clerk-2.6.0` declares a `consumed_surface` today"*: it has **15** entries
    and the other four have the key absent. And `:280`'s *"the gene stays in the DNA as a permanent
    fence"* holds — `MembershipOrgIdentity/real-org-eid` is still declared at
    `clerkenstein/alignment/dna/clerk-2.6.0.json:130,134`.
17. **`alignment_testing.md:475` — six snapshot-fidelity operators, and the five/five split.**
    `stack-seeding/dna/snapshot.go:62` is `var validSnapshotOperators = map[string]bool{` and the map
    has exactly the **six** members named (`:63-68`), including M23's
    `snapshot-cross-surface-closure`. `data-dna.json:410` is the **taxonomy** surface's `operators`
    array — five, all but `cross-surface-closure`, exactly as claimed — and `:439` is **content**'s —
    five, no `embedding-dim`, with `cross-surface-closure` in its place. `:505`'s *"NOTHING is left
    waived"* also holds: the file declares **10** surfaces and their statuses are
    `4× seeded-m7a · 4× seeded-m7c · 1× snapshot-seeded-m9b · 1× snapshot-seeded-m10` — **zero**
    `waived`.
18. **`alignment_testing.md:329-330` — the `alignment/` section has no `scripts/` dir.** Enumerated in
    **both** rext trees: `alignment/` holds exactly `README.md`, `cmd`, `examples`, `go.mod`,
    `internal` — the five the sentence lists, and no `scripts`. The scripts are Clerkenstein's
    (`clerkenstein/alignment/scripts/{gate,drift-check}.sh`, plus a `drift-test.sh` the doc does not
    claim to enumerate), and the five runners are exactly
    `cmd/{clerkrun,deployrun,expressrun,jsfapirun,multirun}` as `:144` and `:390` both say.

### RE-DERIVED — anchors and arithmetic I re-computed (weakest form of clearance)

Roughly 90 individual `file:line` anchors opened and read with surrounding context. Every one resolved to
the construct claimed, with **three exceptions, all booked above (B1/B3) or as minors**. Notable groups:

- **`platform-migration-status.md` compose citations** — `docker-compose.yml` @ `0c91421`: `:5`, `:18`,
  `:28`, `:48`, `:57`, `:59`, `:73-75`, `:82`, `:83`, `:84-92`, `:102-103`, `:112`, `:119`, `:135`,
  `:141`, `:143`, `:151`, `:160`, `:168`, `:170-171`, `:183`, `:185-186`, `:1-2`. **All 23 exact**, and the
  file really is **186** lines (271 at `0dab54d`; `838d907` is **+11/−96** on it, verified by `--numstat`).
- **`platform-migration-status.md` `app/main.go` @ `2035f9a`** — `:15`, `:62`, `:63`, `:285`, `:286`,
  `:394`, `:395`, `:504`, `:516`, `:517`, `:518-523`, `:524`, `:525`, `:547`, `:690`, `:721`, `:751`,
  `:1102`, `:1153`, `:1416-1421`, `:1445`, `:1450`, `:1471`, `:1473`, `:1552`. **All 25 exact**, including
  the seven wiring call sites the row uses to justify *"owns seven domains"*, and
  `app/internal/roadrunner/` genuinely does not exist while `internal/jobsimulation/runner` does
  (`jobsimwiring/wiring.go:123` = `jsrunner.NewRunnerManager(getenv("JUDGE0_API_KEY"),
  getenv("JUDGE0_BASE_URL"))`).
- **Terraform, and the ref that settles it.** `messenger/terraform/main.tf` is **111** lines at the
  *checkout* `fa47850d` (`:19` = `= 1`) and **121** lines at *origin/main* `e9421c68`, where `:29` is
  `service_desired_count = 0`, `:19-25` is the Redis-consumer-group/untargeted-apply comment and `:27-28`
  is the rollback-path sentence — **the row is exact at origin/main and would resolve wrongly at the
  checkout**. Same for `storage/terraform/main.tf`: 100 lines at `4ce8ece5`, **18** at `9f8cb532` where
  `:9-11`, `:13-16` and `:18` are word-for-word what the cell quotes. I am **clearing both**, because the
  document's own banner declares it measures against origin HEAD and the citation resolver's
  `CITE_REF=auto` ladder reads `origin/main` first; flagging the ref-dependence rather than booking it.
  Also exact: `app/terraform/main.tf:181` (`= 1`), `:415-416`, `:419-420`; `cms/terraform/main.tf:39`
  (`= 0`, at **both** refs); `sentinel:19`, `roadrunner:19`, `graphql-wundergraph:20` (all `= 1`);
  `jobsimulation/terraform/main.tf:15-22` and `:24-40` @ `82cb66e`;
  `storage/terraform/storage.tf:22-25` (the M903 supersession text), **zero** occurrences of *custody* in
  the storage repo @ `9f8cb532` against a working positive control, and **no `moved` blocks**.
- **The roadrunner blame chain** — `git blame -L 19,19` names **`84a4b4f` (2025-12-15)**, and `e45eb61`
  (2026-05-27) touched **line 11 only**, its body reading *"Module contents are identical; this is a pure
  source-URL swap."* Repo HEAD `87d8d44` (2026-06-19) is a CI change. The row's correction of
  `roadrunner.md` is itself correct.
- **Platform history shas** — `cb6ebf5`, `ef4b449`, `1474b1f`, `84862d1`, `b43b99a`, `c17cc9a`, `467965a`,
  `8770fe6`, `a2a3ee6` (which does delete `directus:` from compose), `6060315` (the `start_period: 120s`
  at `common.yml:22`), `045857c`, `fdfa189`, `21429b7`, `a4db680`, `236771f`, `b56d731`, `360efd4` — all
  resolve with the dates and subjects claimed. `915da06` is in **graphql-wundergraph**, not platform,
  which is where the row puts it, and its stat shows `schemas/cms.graphqls` deleted;
  `supergraph-config-prod.yaml` lists `backend` alone, `schemas/` holds `backend.graphqls` alone, and
  `subgraphs.conf` is `BACKEND=v1.360.0`.
- **`coursebuilder.md`** — `bedrock.go:98-104` / `:105-114`, `main.go:766-779` (comment `:766-769`, the two
  `logger.Warn(… routes disabled)` arms at `:774`/`:778`), `main.go:824-826` (`OPENAI_KEY` + `CB_IMAGE_MODEL`),
  `terraform/variables.tf:759-763` + `ssm.tf:328-333` + `main.tf:757-758`, and — impressively — the
  retraction's *own* claim that `variables.tf:631-645` is a cms-in-app comment block and `main.tf:555` is
  `"name": "DIRECTUS_BASE_ADDR"`. Also `DefaultSessionsPerOrgPerDay = 50` (`rate.go:28`),
  `DefaultMaxMonthlyCOGSUSD = 500.0` and `DefaultMaxDailyCOGSUSD = 0.0` (`budget.go:27`, `:41`),
  `coursebuilderWorkerConcurrency = 20` (`main.go:190`), `TypeCoursebuilderRun` / `MaxRetry(1)` /
  `Retention(6h)` (`run_task.go:49`, `:199`, `:204`), Go **1.26.4**, the whole route table
  (`handler.go:3160-3228`), and `COURSEBUILDER_OPENAI_IMAGE_KEY` surviving **only** in two in-repo
  markdown files. `git describe --tags`: `ad9f3c49` → `v1.369.0-7-gad9f3c498`, `2035f9a4` →
  `v1.369.0-2-g2035f9a40`, `5ba17044` → `v1.363.2`, `b948604` → `v1.366.0`, `9d00a313` → `v1.367.0` — the
  five version pins in my file set, all correct.
  - Worth naming: **`credits/cost.go`'s own package doc at `:29` says `course.refine → 5 credits`, and the
    corpus says 1.** The corpus is right — the live map at `:86-90` is `ActionCourseRefine: 1, // D1
    ruling`, and `ActionCourseRefine`'s doc at `:59-67` says *"a FLAT 1 CREDIT"*. The corpus read the
    constant, not the stale comment; that is the correct behaviour and is worth a clearance rather than
    silence.
- **`ai_architecture.md` routing/eval anchors** — `jobsimulation/ai/ai.go:30-33`, `:259-289`, `:87`
  (`config.WithRegion("eu-west-1")`), `:150-155`/`:297-301` (`if throttled { vendor = Openai }`);
  `skillerai/ai.go:347` and `jobsimulation/ai/ai.go:267,344` @ the pinned `5ba17044`;
  `simulator/ai/ai.go:20-26`, `:58-59`, `:65-66`, `:69`, `:82-83`, `:86`, `:98-99`, `:102`, `:110-111`,
  `:114-115`, `:126-127`; `jobsimulation.go:905`, `:1297`, `:1302-1305`, `:1307`, `:1079-1085`,
  `:1594-1597`; `calls/livekit.go:126`, `:131-135`, `:140-144` (`:142` agent-name reset **and** `:143`
  endpoint swap — the two-things claim is right); `validator.go:43,60-61,595`; `criterion.go:127`, `:168`,
  `:404` (`validateLLM`), `:428`, `:450-475`; `bulkChecks.go:56-58` (`check_id`/`success`/`feedback`) and
  `:118` (`ai.WithTemperature(0.0)`); `skills.go:40-51` (including the `// TODO fix this formula`
  comment), `:53-64`, `:75`; `skill/skill.go:617-623`; `CompetencyReadLevel.tsx:18` @ next-web-app
  `8297c684`. The fold commit `1e457fa70` is *"refactor(ai): fold the ai library into app as internal/ai"*
  dated 2026-08-04, and `github.com/anthropos-work/ai` is required by **cms** and **jobsimulation** go.mod
  only — not `app`, `sentinel`, `storage` or `messenger`. The two cross-file citations into
  `external_services.md` (`:567`, `:619-629`) both land on the constructs claimed.
- **`skillpath.md`** — `session.go:204-207` (the `cms-in-app deseam` comment and
  `u.cms.GetSkillPathDomain`), `skillpaths.go:87-95`, `intelligence.go:1144` +`:1159-1170` (the
  `SkillPathSession` query filtered by `skill_path_id`, `StatusIn(Active, Completed)` and the tenant
  predicate), `terraform/migrations/20260729133514.sql:62`/`:63` (the two `DROP TABLE` mirrors),
  the **three** migrations post-dating it (`20260731131307`, `20260731154527_academy_chapter_progress…`,
  `20260803143844_ai_readiness_recommendation_path`), no `local_skill_path_session` Ent schema, the
  `pending|active|completed|archived` enum (`enum/skill_path_session_status.go:12-15`), the 8 GraphQL
  operations in `schemas/skillpath_sessions.graphqls:70-82` (all eight present, `createSkillPathSession`
  `@deprecated` exactly as claimed), and `SKILLPATH_STREAM=skillpath` at `docker-compose.yml:72`.
- **`gotenberg.md`** — `docker-compose.yml:183` `profiles: [core, backend, all]`,
  `GOTENBERG_URL=http://gotenberg:3200` on the **`backend`** block (`:57`, block starts `:28`), the 90 s
  client timeout (`converter/gotenberg.go:13`), the endpoint at `:31`, and the `ConvertToPDF` signature.
- **`alignment_testing.md` code anchors** (authoring copy `43049308`) — `alignment/cmd/alignctl/run.go`
  `:133-136` is precisely `const ( ExitRegressed = 2; ExitUnmeasurable = 3 )` and `unmeasurable()` at
  `:139-153` returns `ExitUnmeasurable` after printing the boxed *"THIS IS NOT A PASSING SCORE"* banner
  the doc quotes verbatim; `cmd/alignctl/dna.go:20-26` is the `list|diff|validate|coverage` switch;
  `run.go:38` calls `d.Validate()` **before** `compare.Evaluate` at `:70`, which is exactly the
  *"cannot be scored at all"* claim at `:198`; `compare.go:43` carries
  `CriticalGenes int \`json:"critical_genes"\`` with the *"0 ⇒ vacuous 100"* comment, and `GateMet`
  (`compare.go:56-61`) is `if minCritical > 0 && r.CriticalGenes == 0 { return false }` — the doc's
  two-layer framing at `:167-174` uses the same *load/lint-time* vs *scoring-time* words the code
  comment does. `dna.go:21` is `idPattern = ^[A-Za-z0-9][A-Za-z0-9_-]*$`, `:24` is
  `maxWeight = 1_000_000` with the `1..maxWeight` check at `:269-270`, and `:276-280` is the
  reject-a-zero-critical-DNA guard. `gate.sh:69` is `"$alignctl" dna coverage --dna "$base/$dna"
  --if-declared` with the rationale at `:58-68` (and `:61` genuinely *inside* that comment, as the doc
  says of its own former anchor); its defaults are `ALIGN_DIR:-$base/../../alignment` `:30`,
  `RUNNER_PKG:-./cmd/clerkrun` `:33`, `DNA:-dna/clerk-2.6.0.json` `:35`. The inert workflow exists and
  is tracked at `clerkenstein/.github/workflows/alignment.yml`, and `:10-11` self-describes as
  *"currently inert"* in the words quoted. The toy's diverging gene is `Greet/padded-name`
  (`examples/toy/dna.json:23`, its own description says *"mirror diverges here"*) and the toy gate is
  `gateOverall = 80.0` / `gateCritical = 100.0` (`examples/toy/alignment_test.go:21-22`). Module is
  `anthropos.dev/alignment` with **no `require` block** — *"stdlib-only"* holds.
  - Method-lookup caution worth recording: my first search for `GateMet` used `func GateMet` and
    returned **nothing** — it is a method, `func (r *Report) GateMet`. A wrong-regex false absence
    (§5 rule 3), caught only because a broader grep contradicted it. Had I stopped there I would have
    booked the doc's `GateMet` claim as unsupported.

## What I could not settle

1. **`alignment_testing.md`'s narrative and historical measurements.** Its structural and enumerable
   claims are cleared above, but four classes are not: (a) the milestone attributions (which of
   M1/M1b/M2/M2c/M3/M7b/M9a/M10/M23/M37/M218/M219 introduced what) — settling these needs the rext
   commit history read per claim; (b) the **live** measurements it reports — the M7b `100% / Critical
   100%` against a seeded `demo-1` and the `diff` exit-1-on-injected-column proof (`:447-448`), which
   need a running stack; (c) the toy transcript at `:300-313` (`overall 86.7% / 5-of-6`), which I
   corroborated only indirectly — the diverging gene and the 80/100 gate are verified, the printed
   numbers were not re-run; (d) the **current** scores in the `:259-265` table. I verified the
   denominators (27/9/9/7/13 genes) and the express surface's dependency-gating machinery exactly, but
   a *score* is a run, and per §5 rule 50 a verdict recorded without its run is provenance-unstated
   rather than wrong. The doc is unusually careful about this itself — `:265` records express as
   **UNMEASURABLE / not a pass** rather than carrying a stale 100%.
2. **`ai_architecture.md:313-316` — "There is no 60/65/75/85/95 threshold ladder … in `app`, `cms`,
   `jobsimulation` or `next-web-app`."** The **positive** half is verified exactly
   (`calculateCompetencyLevelScore`, `skills.go:40-51`). The **negative** half is a four-tree absence
   claim; my probes were too coarse to establish it (7 files in `app` and 3 in `next-web-app` contain
   `>= 65|75|85|95`-shaped literals, none of which I traced to a scoring ladder). Settling it needs a
   scoped read of the scoring paths in each tree, at each tree's own ref, with the nested `studio` repos
   named — §5 rule 44's full instrument.
3. **`ai_architecture.md:94` — the standalone `ai` module's `speech.go:12-13` @ `v1.40.1`
   (`TTSV2`/`TTSV2HD`).** `github.com/anthropos-work/ai` is in no clone in this tree, so the claim is
   unverifiable from here. What I *can* confirm is the half that matters operationally: `app`'s folded
   `internal/ai/speech.go` contains neither const, and no repo a stack builds requires the module.
4. **All GitHub-org-API facts.** `platform-migration-status.md`'s archive assertions (skiller 2026-07-01,
   skillpath 2026-07-31, graphql-wundergraph 2026-07-30, intelligence 2026-04-02, chronos *not* archived
   with last push 2026-04-23), the §3 census (**93** repos, **46** unnamed by any corpus doc, and every
   `last push` date), and the five LiveKit agent repos all live in the org API, which git objects cannot
   answer. The **4-in-`repos.yml`** half of `:127` I did verify by enumeration. The `jobsimulation` row's
   own reasoning is sound and checkable in the clone — `origin/main` `82cb66ec` does carry four commits
   dated 2026-08-04 including a **merge-button** commit (`caf36c96`, committer `GitHub`), which an
   archived repo cannot accept — so *"report both, assert neither"* is the right posture and I second it.
5. **`platform-migration-status.md:87`/`:92` — the `app/knowledge/plan/**` evidence** (the M903
   `overview.md` front-matter `status: planned` and `progress.md`'s *"Planned, not started"*). I did not
   open these: the briefing's hard bar forbids reading anything under `knowledge/plan/**`, and while its
   intent is plainly the *rosetta* plan directory, I chose the conservative reading rather than argue the
   scope. The `storage` row's *other* M903 evidence — `terraform/storage.tf:22-25` and the absence of
   `moved` blocks — I did verify, and it supports the same conclusion independently.
6. **`ai_architecture.md:156` — `public.skill_embeddings` = 42,790 and `public.job_role_embeddings` =
   18,919 rows (measured 2026-06-29).** A live-DB measurement; no clone can settle it. The corpus's
   surrounding fence (18,919 is the *embedding row* count, not the role count; 42,790 is a floor) is
   internally consistent and consistent with `platform-migration-status.md:110`.
7. **`platform-migration-status.md` §4's reach numbers (25 / 70 / 1, 96 total).** These are a guard-run
   output and, per §5 rule 50, are settled by the fence tree its configuration lives in. The document
   itself says *"Take those three numbers from the run, never from this table"*, so I treated them as
   self-disclaiming rather than as a claim, and did not run the guard.

**BLOCKERS=3 MINORS=7**

---

### Note on the booking unit for B1

B1 covers **three** distinct false anchors (`ai_architecture.md:92`, `:94`, `:295`). I booked them as
**one** block rather than three because they are one defect with one repair — a single un-re-pointed
five-line insertion — and because rule 40's arithmetic note distinguishes blocker *blocks* from distinct
*anchors*, with readings counted in blocks. If this reading is combined with another under an
anchor-level denominator, B1 should be expanded to three. All three anchors are named explicitly above
precisely so a repairer cannot fix one and leave the others — which is the failure rule 40 measured at
38 sites.
