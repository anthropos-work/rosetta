# Seat C — reading #31

**Corpus HEAD:** rosetta `4d4530d` (branch `m257x/platform-realignment`), no in-scope file dirty.

**Ground-truth refs, re-verified at this reading's open (all match the ADDENDUM):**
`app` `ad9f3c49` · `app/studio` `aeec036a` · `platform` `0c91421d` · `next-web-app` `8297c684` ·
`sentinel` `f2c46190` · `studio-desk` `41ee3575` · `ant-academy` `22df69dd` · `storage` `4ce8ece5`
(origin/main `9f8cb532`) · `messenger` `fa47850d` (origin/main `e9421c68`) · `cms` `ca50c817`
(origin/main `f38c0c4a`) · `graphql-wundergraph` `60c229f3` · `roadrunner` `87d8d443` ·
`jobsimulation` `462343b0` (origin/main `82cb66ec`) · rext **pinned per-stack** `stack-demo/rosetta-extensions`
`09d06070` · rext **authoring** `.agentspace/rosetta-extensions` `43049308`.

**Which tree settled which claim.** All `alignment/` and `stack-seeding/dna/` claims in
`alignment_testing.md` were checked in **BOTH** rext trees and are **byte-identical** across them
(45 tracked files under `alignment/` in each, same content); I report the authoring copy's reading
and note the pinned clone agrees. The one guard-*verdict* claim
(`platform-migration-status.md` §4's citation counts) was run from the **authoring copy**, per
§5 rule 50 — its own stamp printed
`fence-tree: …/.agentspace/rosetta-extensions @ 430493087 · describe=fast-build-m257x-iter-101-17-g4304930`.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| corpus/architecture/alignment_testing.md | 542 | yes |
| corpus/architecture/ai_architecture.md | 340 | yes |
| corpus/architecture/platform-migration-status.md | 330 | yes |
| corpus/services/coursebuilder.md | 161 | yes |
| corpus/services/skillpath.md | 117 | yes |
| corpus/services/gotenberg.md | 84 | yes |
| (protocol, read as instructed) corpus/ops/platform-alignment.md §5 | 2803 total | §5 (249–1663) read in full |

**Search-instrument controls run in this pass** (§5 rules 1/2/44):
- One `git show "$r:repos.yml"` loop returned `entries=0` for **every** ref — zsh was applying the
  `:r` history modifier. stderr was read, the command was fixed, and the real answer (9/6/6/4)
  is in the clearances below. An unread stderr here would have produced four false zeros.
- A first slug-resolver reported **5 BAD** `#anchor` fragments. Positive control showed the
  resolver was collapsing whitespace runs (GitHub does not). Corrected resolver: **all 9 fragments
  in my files resolve OK**. The "finding" was the instrument.
- `bedrock|boto3` under `app/studio` measured with **two** instruments (`git grep` at
  `aeec036a` **and** `/usr/bin/grep -r` over the worktree): 0 / 0, against a positive control
  (`openai`) returning 68 / 68. NUL census over `app/studio`: 1 file, a `.docx` (4 NULs), not source.
  `git ls-files | git check-ignore --stdin --no-index` in `app/studio`: 4 tracked-but-ignored files.

---

## Blockers

### B1 — three intra-document self-citations in `ai_architecture.md` are each exactly 5 lines short of the construct they name

- **Anchor:** `corpus/architecture/ai_architecture.md:94` — *"a module **no repo a stack builds
  requires** since the fold at `1e457fa70` (see `:95` below)"*
  - sibling site `:92` — *"its OpenAI sibling at `:86` is an exact enumeration of
    `internal/ai/openai/config.go:8-26`"*
  - sibling site `:294-295` — *"**NB this bullet is not a claim about the platform's Anthropic model
    SET** (that is the table row at `:87`, which is where the 4.6/Opus-4.8 omission lived)"*
- **Why it is false:** measured in the file itself at rosetta `4d4530d`:
  - `:95` = `| **Transcription** | GPT-4o Transcribe | Azure EU (US via flag_use_azure_us) |` — a table
    row about Transcription. The fold statement the sentence points at ("*it is no longer a shared
    private module for any service a stack builds*") is at **`:100`**.
  - `:86` = `The **Default client** column is the client the vendor const resolves to — *not* a position in a`
    — prose, not a table row. The **OpenAI** row is **`:91`**.
  - `:87` = `fallback order (there is none; see above).` — prose. The **Anthropic** row, the one that
    "*is where the 4.6/Opus-4.8 omission lived*", is **`:92`**.
  All three are off by **exactly +5** in the same direction, i.e. one 5-line insertion above the
  *Model Families* table invalidated every self-citation below it — §5 rule 34 (*line numbers move
  when YOU edit too*) with three live instances. Two of the three sentences exist specifically to
  route a reader to the correct construct, so the anchor is the whole payload; the `:87` one lands on
  a half-sentence fragment while claiming to name a table row.
- **Confidence:** high

### B2 — `pdf2md.py:24` is cited as the site of `mistral-ocr-latest`; that literal is at `:127`, and `:24` is an import

- **Anchor:** `corpus/architecture/ai_architecture.md:40` — *"Python-side in the in-image studio tree
  at `studio/tools/pdf2md.py:24` (`mistral-ocr-latest`) — a standalone CLI on neither the AI
  manager's path nor the generation pipeline's"*
- **Why it is false:** at the nested repo's own ref (`stack-demo/app/studio` @ `aeec036a`; the host
  `app` ref sees nothing here — `git -C app grep … -- studio/` returns 0 for every predicate),
  `tools/pdf2md.py:24` is `from mistralai import Mistral`. The string `mistral-ocr-latest` occurs
  **exactly once in the whole nested repo**, at `tools/pdf2md.py:127` (`model="mistral-ocr-latest",`)
  — verified with `git grep -n "mistral-ocr-latest" aeec036a`.
- **Why I hesitated, and booked it anyway:** the substantive proposition (*Mistral is OCR-only; the
  Python side is this CLI*) is **true**, the file is right, and `:24` is where Mistral enters the
  file — so this could be read as an entry-point citation with a descriptive parenthetical. I booked
  it because the parenthetical is doing the work of naming the construct at the cited line, and
  because **this same bullet, three lines earlier, records M257x iter-115 repairing exactly this
  class** on the Go side (`markdownManager.go:19` → `:30`, "`:19` is a **doc-comment** line … not
  code"). By the document's own standard, an import line is not the OCR-model line.
- **Confidence:** medium

### B3 — the `messenger` prod-state citations are unpinned and resolve only at `origin/main`, not at the checked-out ref, where the same anchors say the opposite

- **Anchor:** `corpus/architecture/platform-migration-status.md:93` — *"**Prod — stopped, and here
  "scaled to zero" is still the right words:** `messenger/terraform/main.tf:29`
  `service_desired_count = 0` in a module that is otherwise intact (121 lines) … with the reason
  in-comment (`:19-25` …); the image and task definition stay declared as the rollback path
  (`:27-28`)"*
- **Why it is false / unsupportable as written:** the sentence names **no messenger ref** (the shas in
  the row — `838d907`, `0c91421` — are *platform* refs; §5 rule 33 scopes a pin to its own block), so
  per the briefing it grades at the **checkout**, `fa47850d`. There:
  - `terraform/main.tf` is **111** lines, not 121;
  - `:29` is `container_definitions = <<EOF`, not `service_desired_count`;
  - `:19` reads **`service_desired_count = 1`** — the *opposite* of the row's headline prod claim;
  - `:19-25` and `:27-28` are plain module arguments, not the comment blocks quoted.
  The claim is exactly right at `origin/main` **`e9421c68`** (121 lines; `:29` = `= 0`; `:19-25` the
  v9.0 comment; `:27-28` the rollback sentence) — `5dd9f80 chore(terraform): scale the standalone
  messenger to 0` lands *after* the checkout. So this is a citation-hygiene defect, not a
  world-model defect. What makes it bookable rather than cosmetic: the **`storage` row three lines
  above pins its ref explicitly** (`9f8cb53`) and states the governing rule in words — *"**State the
  ref or the sentence flips**"* — and the `jobsimulation` row pins `82cb66e`. This row is the one
  that doesn't, in the file whose opening promise is *"Every claim is cited to a sha or a
  `file:line`."*
- **Why I hesitated:** the ADDENDUM says both refs are legitimate for the four behind-origin clones,
  and the map's banner frames the whole reading at *origin* HEAD, so an adjudicator may rule the
  intended ref is `origin/main` and reject this. I booked it because under the briefing's stated
  rule (no ref named → grade at the checkout) the anchors are refutable, and because the repair is
  one word either way (add `@ messenger e9421c68`).
- **Confidence:** low

---

## Minors

- `corpus/architecture/ai_architecture.md:264` — *"the enum is
  `app/internal/cms/directus/collections/jobsimulation.go:983-990`"*. The `AIModel` const block is
  `:979-991` (members `:980-990`). The cited range starts mid-block and omits three of eleven members
  — `Anthropic35SonnetAws` (`:980`), `Anthropic37SonnetAws` (`:981`), `Anthropic4SonnetAws` (`:982`)
  — two of which this same document cites as the Anthropic fallback targets at `:287-293`. Not booked
  as a blocker because the anchor lands *inside* the right construct and all three examples the
  sentence gives (`anthropic-45-sonnet-aws` `:983`, `gpt-4.1` `:985`, `gpt-5` `:987`) are inside it.
- `corpus/architecture/ai_architecture.md:114` — *"this line went on publishing it **68 lines
  below**"* the `:15-17` retraction. The line is `:114`; the distance is 97–99 lines, not 68.
- `corpus/architecture/ai_architecture.md:65` — *"Full per-line derivation:
  `external_services.md:619-629` (item 4 of the four live EU-exit routes)"*. Item 4 spans
  `external_services.md:622-632`; the cited range opens three lines inside item 3's tail and
  truncates item 4 by three lines.
- `corpus/architecture/alignment_testing.md:3` — **Last updated: 2026-06-06**, in a document whose
  body records M218, M219 and an M257x iter-85 correction (July–August 2026). Header metadata only.
- `corpus/architecture/alignment_testing.md:373` — `unmeasurable()` given as `:139-153`; at
  `alignment/cmd/alignctl/run.go` the function is `:139-154` (`return ExitUnmeasurable` at `:153`,
  closing brace `:154`). The two constants (`:133-136`) are exact.
- `corpus/services/coursebuilder.md:46` — *"`embed.go` (`//go:embed assets/*.md` rubric)"*. There is
  no `assets/*.md` glob directive; `internal/coursebuilder/embed.go` carries five individual
  `//go:embed assets/<name>.md` lines (`:28`, `:31`, `:34`, `:37`, `:48`).
- `corpus/services/coursebuilder.md:129-130` — *"~55 test files"* / *"~25 boundary test files"*.
  Measured at `ad9f3c49`: `internal/coursebuilder` has **61** `*_test.go` (of 102 `.go`);
  `internal/web/backend/coursebuilder` has **33** (of 48). Both hedged with `~`; the second is ~30 % low.
- `corpus/services/coursebuilder.md:145` — *"(142 changelog lines)"*, pinned at `5ba17044`. No stated
  derivation, and none I tried reproduces 142 at that ref: SPEC.md `^\*\*` = **140**, the same test
  restricted to the `## Changelog` section = **71**, dated `^\*\*20yy-mm-dd` entries = **58**.
  `Waves 1→24` itself checks out (max wave number in SPEC.md @ `5ba17044` = 24).
- `corpus/architecture/platform-migration-status.md:115` — *"replaced by `jobsimulations`"*. The
  compose service key `84862d1` adds is `jobsimulation` (singular; `git show 84862d1 --
  docker-compose.yml` → `-  simulator:` / `+  jobsimulation:`). `jobsimulations` is the commit
  subject's wording and is not one of the 25 derived compose names the row set is built from.

---

## Positively cleared (audited zeros)

Labels are strict: **ENUMERATED** = I derived the predicate's members from source independently and
compared cardinalities; **RE-DERIVED** = I re-ran arithmetic the document displays (weakest form).

### `alignment_testing.md`
- **ENUMERATED** — the five Clerkenstein DNAs' gene/capability counts, parsed from the JSON in the
  authoring copy (identical in the pinned clone): `clerk-2.6.0` **14 caps / 27 genes**;
  `clerk-js-5` **6 / 9**; `clerk-multi-1` **5 / 9**; `clerk-deploy-1` **3 / 7**;
  `clerk-express-1` **5 / 13**. Every figure in the scores table (`:261-265`) matches.
- **ENUMERATED** — *"Only `clerk-2.6.0` … declares a `consumed_surface` today. The other four DNAs
  declare none"*: `len(consumed_surface)` = 15 / 0 / 0 / 0 / 0.
- **ENUMERATED** — the 15-endpoint `consumed_surface` contains `GET /v1/users/{userID}` with consumer
  *"next-web @clerk/nextjs currentUser() — SSR, every authenticated render"* → capability `GetUser`,
  and `GetUser` carries the two-sided `hero-a` / `hero-b` variants the M218-D15 note describes.
  `covered_by: clerk-express-1:ClerkClientBAPI/get-organization` exists verbatim.
- **ENUMERATED** — the six snapshot-fidelity operators: `alignment/…` → `stack-seeding/dna/snapshot.go:62`
  is `var validSnapshotOperators = map[string]bool{` with exactly the six named names.
  `data-dna.json:410` = taxonomy's 5 operators (no `cross-surface-closure`); `:439` = content's 5
  (no `embedding-dim`, plus `cross-surface-closure`). Both anchors land on the `"operators": [` key.
  The 10-surface catalog carries **zero** `waived` statuses → *"NOTHING is left waived"* holds.
- **ENUMERATED** — *"the reusable `alignment/` section has no `scripts/` dir at all (it holds `cmd/`,
  `internal/`, `examples/`, `go.mod`, `README.md`)"*: `git ls-files` under `alignment/` yields exactly
  those five top-level entries; `alignment/scripts` does not exist; `clerkenstein/alignment/scripts/`
  holds `gate.sh`, `drift-check.sh`, `drift-test.sh`.
- **ENUMERATED** — `gate.sh:69` is `"$alignctl" dna coverage --dna "$base/$dna" --if-declared`, the
  rationale comment is `:58-67` (`:61` inside it), `ALIGN_DIR` defaults to `$base/../../alignment`,
  `RUNNER_PKG=./cmd/clerkrun`, `DNA=dna/clerk-2.6.0.json`, thresholds 95/100. `dna coverage`'s exit
  contract (0 covered / 2 uncovered / 2-or-0-with-`--if-declared` on an undeclared surface) matches
  `cmd/alignctl/dna.go:72-103` line for line.
- **ENUMERATED** — `run.go:133-136` = `const ( ExitRegressed = 2; ExitUnmeasurable = 3 )`;
  `unmeasurable()` prints the boxed *"UNMEASURABLE … THIS IS NOT A PASSING SCORE"* banner.
- **ENUMERATED** — `clerkenstein/.github/workflows/alignment.yml` exists, is tracked, and `:10-11`
  says verbatim *"as a subdir workflow under clerkenstein/.github (not at the monorepo root), this is
  currently inert…"*; there is **no** `.github/workflows` at the rext monorepo root.
- **ENUMERATED** — `dna validate`'s contract: id regex `^[A-Za-z0-9][A-Za-z0-9_-]*$` (`dna.go:21`),
  `maxWeight = 1_000_000` (`:24`, enforced `:269-270`), `normalized` requires non-empty `normalize`
  (`:267`), zero-critical-genes rejection (`:279-281`), golden-path escape refusal
  (`outcome.go:38-46`). `GateMet` refuses a non-zero critical threshold at `CriticalGenes == 0`
  (`compare.go:56-57`); overall is weighted, critical is an unweighted count ratio (`:100-103`).
- **RE-DERIVED** — the toy worked example: `Add` critical(w3)×3 + `Greet` standard(w2)×3 ⇒ Σw = 15,
  aligned 13 ⇒ **86.7 %**, critical 100 %, 5/6 — matches the transcript at `:303-312`; the toy gate
  in `examples/toy/alignment_test.go` is 80.0/100.0.
- **RE-DERIVED** — the 97.2 % history: `MembershipOrgIdentity/real-org-eid` is a `standard` (w2) gene
  in a 71-weight DNA; `(71−2)/71 = 97.2 %`, and 26-of-27. (Weakest clearance; the *set* was
  enumerated above.)

### `ai_architecture.md`
- **ENUMERATED** — the four vendor consts at `internal/jobsimulation/ai/ai.go:30-33`
  (`azure`/`openai`/`anthropic-aws`/`anthropic`); `getClient` at `:259-289` is a plain switch with no
  ordering; the Azure arm keeps the EU client on a failed flag lookup (`:270-273`); `AnthropicAws`
  and `Anthropic` both return `a.anthropicClient` (`:280-283`), built with
  `config.WithRegion("eu-west-1")` (`:87`); the 429 path sets `vendor = Openai` (`:150-155`,
  `:166-167`). *"There is no ordered EU-first fallback chain"* holds.
- **ENUMERATED** — the OpenAI model row is an exact enumeration of `internal/ai/openai/config.go:8-26`:
  11 consts, 11 names listed. Speech: `internal/ai/speech.go:9-12` is the entire `SpeechModel` const
  block (`GPT4oMiniTTSS` + `DefaultModel`) — *"the whole set"* holds, and `TTSV2`/`TTSV2HD` are
  absent from `app`. Anthropic: `internal/ai/anthropic/completion.go:20-30` is six consts over five
  families (3.5 twice: Bedrock-EU + direct), with 4.6 at `:29`, and Opus 4.8 is **not** in that block
  — it is `coursebuilder/bedrock.go:23` `DefaultAuthorModelID`. 4.6 is the live pin at
  `askengine/bedrock.go:25`, `jobsimulation/agent/report_agent.go:31`, `coursebuilder/bedrock.go:29`.
- **ENUMERATED** — the `ai` fold: `1e457fa70 refactor(ai): fold the ai library into app as internal/ai`;
  at `ad9f3c49` neither `app/go.mod` nor `sentinel/go.mod` requires `github.com/anthropos-work/ai`;
  the only requirers are the frozen `cms` and `jobsimulation` husks (`v1.40.2`).
- **ENUMERATED** — Mistral is OCR-only: `internal/cms/studio/markdownManager.go:30` is exactly
  `return &MarkdownManager{ocr: mistralocr.New(aiKey)}, nil` inside `func NewMarkdownManager` at
  `:29`, and `:19` is the doc-comment *"It used to take aiKey and then IGNORE it"* — the correction
  the row records is right. `studioManager.go:583` is the call site. `git -C app/studio grep -ci
  mistral aeec036a` = **22 lines / 3 files**, matching the stated figure exactly.
  `grep bedrock|boto3` under `app/studio` = **0** (two instruments, positive control 68/68).
- **ENUMERATED** — the unset-`ai_vendor` route: `jobsimulation.go:905` `AIVendor *AIVendor` is on the
  **Directus DTO**; `:1302-1305` replaces nil with `simulation.Openai` **before** the sequence is
  built at `:1307`; `simulator/ai/ai.go:58-59` takes `case simulation.Openai:`. The `default:` arm is
  the separate door at `:114-115`, and `azureglobal` (`jobsimulation.go:971`) is the one enum member
  with no case (5 members `:970-974`, 4 cases `:58`/`:69`/`:86`/`:102`).
- **ENUMERATED** — the three simulation defaults: content side `:1297` `simulation.Gpt5Point1`
  (gpt-5.1) + `:1302` `simulation.Openai`; runtime `simulator/ai/ai.go:65-66`, `:82-83`, `:126-127`
  all `openai.GPT4Dot1`; scoring `GetValidationAIVendorAndModel` at `:20-26` = Azure + `GPT4Dot1` +
  `GPT4Dot1Mini`. The Anthropic fallback arms `:98-99` (3.7 Sonnet AWS) and `:110-111` (3.5 Sonnet)
  both resolve to the named constructs. **`gpt-5` is not a default anywhere** — confirmed.
- **ENUMERATED** — Studio-Room slots: `studio/configs/production_config.ini:26-36` matches the table
  row-for-row; `development_config.ini:26-36` is byte-identical; `config_template.ini:39`/`:40` are
  both `azure, gpt-4o, none`; `gpt-5.2` appears in **no** config (`grep configs/` = 0) and only at
  `services/ai.py:356,508`.
- **ENUMERATED** — the evaluation system: `checkerEngines` is stored (`validator.go:43`, `:60-61`)
  and passed (`:595` → `criterion.go:72` → `:90`) but **never looked up** — the dispatch is the
  hardcoded switch at `criterion.go:126-155` (`:127` `case check.EngineLlm:`). `validateCodeDiff`
  runs concurrently (`:168`) and sets `success` from a pure string compare (`:450-475`);
  `NewLLMBulkChecker(c.logger)` at `:428`; `checkValidationBulk.tmpl` exists; temperature 0.0.
  `skills.go:40-51` is `calculateCompetencyLevelScore` with the `20 / 100 / max(0, score*2-100)`
  shape and the `// TODO fix this formula` comment; `:75` is `passed/total*100`;
  `skill.go:617-623` is the plain-division `convertLevelTo100`; next-web
  `packages/ui/src/Competency/CompetencyReadLevel.tsx:18` is the mapping call. **No 60/65/75/85/95
  ladder exists.**
- **ENUMERATED** — `flag_use_realtime_openai`: read at `calls/livekit.go:131-135` **inside**
  `CreateAgentDispatch` (`:106`), effect at `:140-144` doing **two** things — `:142`
  `agentName = "anthropos-agent"` and `:143` `agentEndpoint = "openai-hosted"` — with `:126`
  having just set `anthropos-agent-<location>`. The two-effect correction is right.
- **ENUMERATED** — `internal/aiusage/ai_usage.go` exists and `Event_AiUsage` is published from
  `jobsimulation/ai/ai.go:207`, `skillerai/ai.go:236,302`, `jobsimulation/agent/report_agent.go:636`.
  `skill_embeddings` / `job_role_embeddings` ent schemas + the `small_embedding3` pgvector column
  with an IVFFLAT index exist (`schema/skiller_mixins.go:181`, `:191`).

### `platform-migration-status.md`
- **ENUMERATED** — the §2 completeness derivation, re-derived from the platform's own history rather
  than from the sums shown: `git log -p --follow -- repos.yml` yields exactly **14** distinct
  `- name:` values, and they are precisely the 14 listed (app · chronos · cms · graphql-wundergraph ·
  intelligence · jobsimulation · messenger · next-web-app · roadrunner · sentinel · skiller ·
  skillpath · storage · studio-desk); all 14 have rows. The section-blind 2-space-key pass over
  `docker-compose.yml`'s history returns **26**, of which the single non-service is `app-network`
  (`docker-compose.yml:185-186` @ `0c91421`) ⇒ **25** service names; every one has a row, and the
  three filed under a repo name are exactly `backend`→`app`, `graphql`/`wundergraph`→
  `graphql-wundergraph`. The commented-out `# simulator:` at `cb6ebf5` is correctly **not** counted.
- **ENUMERATED** — `repos.yml` membership across refs: **9** @ `2adcf71` (0 skillpath) → **6** @
  `ef32d4c`/`0dab54d` → **4** @ `0c91421` (`app` · `sentinel` · `next-web-app` · `studio-desk`), in a
  28-line file. Compose service counts: **11** @ `d11a403^` → **8** @ `d11a403` → **5** @ HEAD, and
  `d11a403`'s repos.yml diff deletes `cms`, `jobsimulation` **and** `roadrunner` — refuting that
  commit's own message, exactly as the `roadrunner` row says.
- **ENUMERATED** — every `docker-compose.yml` / `common.yml` / `repos.yml` anchor in the table, read
  against the 186-line file at `0c91421`: `:5`, `:18`, `:28`, `:48`, `:57`, `:59`, `:73-75`, `:82`,
  `:83`, `:84-92`, `:102-103`, `:112`, `:119`, `:135`, `:141`, `:143`, `:151`, `:160`, `:168`,
  `:170-171`, `:183`, `:185-186`, `:1-2`; `common.yml:2`, `:22` (`start_period: 120s`), `:24`;
  `repos.yml:2-10`, `:12-13`, `:14-17`, `:18-20`, `:23-25`, `:26-28`. **All 29 land on the named
  construct.** Zero `*_RPC_ADDR` anywhere in compose; `AUTHORIZATION_ADDRESS=http://sentinel:8087`
  is the single service address; `GOTENBERG_URL` and `JUDGE0_BASE_URL` are the two other
  cross-process edges — the row's own self-correction is right.
- **ENUMERATED** — the `app` row's currency claim: `2035f9a..origin/main` is **5** commits touching
  exactly `.claude/skills/publish/SKILL.md`, `CLAUDE.md`, `knowledge/deployment.md`,
  `terraform/main.tf`, `terraform/variables.tf` — **no Go source**; the `terraform/main.tf` hunk is a
  single in-place `error_message` rewrite, and `:181 service_desired_count = 1` resolves identically
  at both refs. All seven in-process wiring anchors resolve at the pinned `2035f9a`:
  `main.go:690` (`skiller.NewSkillerManager`), `:721` (`jobsimwiring.Wire`), `:751`
  (`skillpath.NewSessionManager`), `:1153` (`appcms.Wire`), `:524`/`:525` (storage), `:1471`
  (`msgadapters.Wire`), `:395` (`customeriosync.New`); all seven `internal/<domain>/` dirs exist and
  `internal/roadrunner/` does **not**, with the runner at `internal/jobsimulation/runner/`
  constructed at `jobsimwiring/wiring.go:123`.
- **ENUMERATED** — the `STORAGE_RPC_ADDR` two-ref claim, re-derived as a set rather than a count:
  at `2035f9a`, `git grep -n STORAGE_RPC_ADDR … -- '*.go'` = **3**, all comments
  (`main.go:504`, `jobsimwiring/wiring.go:101`, `storagens/callsites_test.go:189`). At `b948604`,
  **15** hits of which **7 are env lookups** (`main.go` ×3, `wiring.go` ×1, one in each of the three
  `cmd/` tools), 6 spelled `os.Getenv` and 1 a lowercase `getenv`; the other 8 split exactly
  2 doc-comments / 2 error strings / 3 `t.Setenv` / 1 `t.Fatal` message. Every sub-figure matches,
  including the deliberately-stated "counting all four test hits as `t.Setenv` overstates by one".
- **ENUMERATED** — `storage`'s prod half at `9f8cb532`: `terraform/main.tf` is **18** lines; `:9-11`
  and `:18` quote verbatim; `custody` occurs **0** times in the repo; `M903` occurs **once**, at
  `terraform/storage.tf:22`, with the quoted supersession text; there are **no** `moved` blocks
  (the one apparent hit is `re-moved {` inside `CLAUDE.md`); `d3e6d32` is
  *"correct the guard header, which contradicted the shipped design"*.
- **ENUMERATED** — `jobsimulation`'s M810: `terraform/main.tf:15-22` @ `82cb66e` is the decommission
  comment (no `service_desired_count` anywhere in `terraform/` at that ref), `:24-40` the
  what-it-still-owns block; `origin/main` carries exactly the four 2026-08-04 commits named
  (`6092c6d2`, `caf36c96` "Merge pull request #439" committer `GitHub`, `1e40d184`,
  `82cb66ec` v0.254.0). `cms/terraform/main.tf:39` is `service_desired_count = 0` at **both**
  `ca50c817` and `f38c0c4a`, and `6efa1d5` deletes `.github/workflows/build-production.yml` with the
  quoted subject — the "two facts pointing opposite ways" framing is exact.
- **ENUMERATED** — `roadrunner`: `terraform/main.tf:19 = 1`; `git blame -L 19,19` names **`84a4b4f`**
  (2025-12-15), which `--diff-filter=A` confirms is the commit that **added** the file; `e45eb61`
  (2026-05-27) changed **line 11 only**, a source-URL swap whose message says *"Module contents are
  identical"*; HEAD `87d8d44` (2026-06-19) touches only `.github/workflows/bump-version.yml`.
- **ENUMERATED** — `graphql-wundergraph`: `terraform/main.tf:20 = 1`; `supergraph-config-prod.yaml`
  lists `backend` alone; `schemas/` holds `backend.graphqls` alone; `subgraphs.conf` =
  `BACKEND=v1.360.0`; `915da06` (2026-07-29) is the fold. The supergraph history is
  5 → 4 → **3 (`7c17e63`, 2026-07-21 — skillpath's removal)** → **1 (`915da06`)**, which independently
  confirms `skillpath.md:42`'s "3 subgraphs at the time … 1 now".
- **ENUMERATED** — all 23 platform shas cited across the file resolve with the dates and subjects
  claimed (`838d907`, `d11a403`, `0dab54d`, `ef32d4c`, `2adcf71`, `6060315`, `a2a3ee6`, `21429b7`,
  `a4db680`, `045857c`, `fdfa189`, `236771f`, `b56d731`, `360efd4`, `1474b1f`, `84862d1`, `b43b99a`,
  `c17cc9a`, `cb6ebf5`, `ef4b449`, `467965a`, `8770fe6`, `0c91421`). `838d907` is **+11/−96** on
  `docker-compose.yml` (numstat) and the file went **271 → 186**; `0dab54d:docker-compose.yml:154`
  is `profiles: [customerio-sync, all]`; `repos.yml:9-10` @ `0dab54d` names infrastructure's
  `services.tf`; `repos.yml:18-23` @ `ef32d4c` shows storage+messenger both `migrations: false`.
  All three long commit-message quotations (`d11a403`, `838d907`, `6092c6d2`) are verbatim.
- **ENUMERATED (guard verdict, run from the authoring copy)** — §4's three coverage numbers
  reproduce **exactly**: `assertion F resolved 96 citation(s) — 25 subject-checked, 70 range-only,
  1 outside any service block; 0 unresolvable; 0 read from the WORKTREE`, exit **0 / OK**. The two
  documented invocations both run as written from the rosetta root. The state vocabulary in §1 is
  **eight** tokens, matching assertion C; no row carries `mid-fold`.
- **ENUMERATED** — all 9 `#anchor` fragments cited out of my three architecture files resolve
  (after the resolver control described above), including
  `platform-alignment.md#4-detection--six-signals-cheapest-first`,
  `#6-classification--the-map`, `#8-fence--so-it-cannot-silently-recur`,
  `shared_libraries.md#taxonomy-figures`, and
  `snapshot-spec.md#the-directus-content-surface-m10--the-second-real-surface`.

### `coursebuilder.md`
- **ENUMERATED** — the SSE wire contract, re-derived as a **set** and not a count:
  `renderEvent` (`handler.go:2604-2772` @ `ad9f3c49`) returns exactly **15** distinct non-empty
  names — `text score patch_applied patch_skipped stage outline progress preview_ready draft_kept
  error translation_ready rebuild_required steering_received steering_applied done` — at exactly the
  17 return sites the doc lists (`:2607 :2609 :2614 :2620 :2637 :2639 :2654 :2661 :2671 :2678 :2704
  :2724 :2732 :2744 :2754 :2768 :2770`), plus `session` written directly at `:1458` ⇒ **16**.
  `cost` is **not** among them: `case cb.EventCost:` at `:2709` returns `"", nil` at `:2717` under
  the D2 comment quoted verbatim in the doc, and the loop skips the write when the name is empty
  (`:1478-1480`). The `default:` arm at `:2770` re-emits `text`, not a 17th.
- **ENUMERATED** — the production-key chain at `ad9f3c49`: `variables.tf:759-763`
  `variable "anthropic_api_key"` — `sensitive = true`, **no default**; `ssm.tf:328-333` the
  SecureString parameter (and `ssm.tf:328` also resolves at `b948604f`); `main.tf:757-758` the
  `valueFrom` SSM-ARN injection. The two lines the doc says had drifted onto other subjects are
  exactly that: `variables.tf:631-645` is the cms-in-app secrets comment block and `main.tf:555` is
  `"name": "DIRECTUS_BASE_ADDR"`. `bedrock.go:98-104` is `ModelBackendName()`; `:105-114` is
  `newUnderlyingClient`; `main.go:766-779` @ `b948604` is the unmount comment plus the two
  `logger.Warn(… routes disabled)` arms at `:774`/`:778`; `main.go:824-826` @ `b948604` reads
  **`OPENAI_KEY`**, and `68c245123` is indeed the commit that deleted the
  `COURSEBUILDER_OPENAI_IMAGE_KEY` read (it now survives only in `README.md` +
  `GO-LIVE-RUNBOOK.md`, 0 Go hits).
- **ENUMERATED** — the constants: `DefaultSessionsPerOrgPerDay = 50` (`rate.go:28`),
  `DefaultMaxMonthlyCOGSUSD = 500.0` / `DefaultMaxDailyCOGSUSD = 0.0` (`budget.go:27`, `:41`),
  `coursebuilderWorkerConcurrency = 20` (`main.go:190` → `worker.NewCoursebuilderServer`),
  `TypeCoursebuilderRun` with `asynq.MaxRetry(1)` (`run_task.go:199`) and `Retention(6*time.Hour)`
  (`:204`), `DefaultAuthorModelID = "eu.anthropic.claude-opus-4-8"` (`:23`),
  `DefaultGraderModelID = "eu.anthropic.claude-sonnet-4-6"` (`:29`), `defaultModel = "gpt-image-2"`
  (`imagegen/openai.go:22`), **7 locales** (`translate.go:155`: `it fr de es nl ja pt`),
  `go 1.26.4`, `git describe --tags ad9f3c49` = **`v1.369.0-7-gad9f3c498`** and
  `2035f9a4` = **`v1.369.0-2-g2035f9a40`** — both quoted strings exact.
- **ENUMERATED, and it is the one that needed §5 rule 10** — the credit prices. The doc says
  `course.build`=5/chapter, `course.refine`=**1**, `course.translate`=1/locale. The **package
  doc-comment** at `internal/credits/cost.go:29` says *"course.refine → 5 credits per refine turn"*
  — which would have made the corpus look wrong if I had grepped and stopped. The authoritative map
  is `creditCost` at `:86-90`: `{build:5, refine:1 // D1 ruling: flat 1 credit per refine turn,
  translate:1}`, and `ActionCourseRefine`'s own docstring (`:59-67`) states the D1 flat-1 ruling.
  **The corpus is right and the platform's header comment is stale.**
- **ENUMERATED** — the route table (`handler.go:3143 Register`) contains every route the doc lists;
  the group is mounted at `backend.go:229` behind `cbGate := courseBuilderAccessGate(...)`
  (`:227`, defined `gate.go:27`); `cmd/coursebuilder-liverun` and `cmd/coursebuilder-e2e` exist with
  `assertNotSharedDSN` at `cmd/coursebuilder-e2e/main.go:380`; `askengine/coursebuilder_tool.go` and
  `ask.SetCourseBuildStarter` (`ask/handler.go:82`, wired `backend.go:259`) exist; the migration
  `terraform/migrations/20260717151144.sql` creates `course_builder_sessions`, `credit_transactions`
  **and** `organization_credits`; all five in-repo docs and all 15 named engine files exist;
  `TestProductionConfig_WiresPlanner` is in `production_config_test.go`.

### `skillpath.md`
- **ENUMERATED** — *"there is NO `SkillPathSessionService` anywhere"*: at `ad9f3c49`,
  `git grep -c … -- '*.go'` returns **0**, and the only two files in the whole repo carrying the
  string are `CLAUDE.md` and `knowledge/architecture.md` — cross-checked with a second instrument
  (`/usr/bin/grep -rl` over the worktree, same two files). Both planning anchors resolve at **both**
  refs the doc names: `CLAUDE.md:109` and `knowledge/architecture.md:28` each list the service in an
  RPC-mux enumeration, at `ad9f3c49` **and** at `2035f9a`.
- **ENUMERATED** — the in-process content read: `internal/skillpath/session.go:205-207` is the
  `// cms-in-app deseam: cms is in-process` comment immediately followed by
  `u.cms.GetSkillPathDomain(...)` at `:207`; `internal/skillpaths/skillpaths.go:88-95` is the twin.
- **ENUMERATED** — the manager-view retraction: `terraform/migrations/20260729133514.sql:62` and
  `:63` are `DROP TABLE "local_jobsimulation_sessions";` / `DROP TABLE "local_skill_path_sessions";`;
  no `local_*` Ent schema exists; and **exactly three** migrations post-date it at `9d00a313` —
  `20260731131307.sql`, `20260731154527_academy_chapter_progress_completed_at.sql`,
  `20260803143844_ai_readiness_recommendation_path.sql`. `InsightsSkillPathByMemberships` is at
  `internal/organization/intelligence.go:1144` and `:1159-1170` is the
  `SkillPathSession.Query()` with `SkillPathID` + `StatusIn(active, completed)` + the tenant `Or`.
- **ENUMERATED** — the "Coming soon" drill-down at next-web `8297c684`:
  `InsightsBySkillPathStudentSimulationsContainer.tsx:31-34` returns
  `null as unknown as MembershipEnriched` with the real query commented out; the results table
  (`:140-149`) and totals block (`:150-160`) are commented out; `:138` renders
  `t('enterprise.insights.comingSoon')`, whose `en` value is **"Coming soon"**. `apps/hiring` has
  **no** skill-paths route — its only two `skill-path` paths are placeholder images.
- **ENUMERATED** — all 8 GraphQL operations exist in
  `internal/web/backend/graphql/graph/schemas/skillpath_sessions.graphqls`, with
  `createSkillPathSession` carrying `@deprecated(reason: "Use upgradeSkillPathSessionToLatest
  instead")` at `:80`.

### `gotenberg.md`
- **ENUMERATED** — `gotenbergMimes` (`extract.go:17-27` @ `9d00a313`) is exactly **nine** entries —
  `.xls`, `.xlsx`, `.ppt`, `.doc`, `.odt`, `.ods`, `.odp`, and RTF under `application/rtf` **and**
  `text/rtf` — and **DOCX is not among them** (the openxmlformats entry is `spreadsheetml.sheet`).
  `:77-81` converts then immediately `ConvertFromReader(bytes.NewReader(pdf), "application/pdf")`;
  `:59` is the `viaGotenberg && gotenbergURL != ""` guard; `:43-45` the graceful-degradation comment.
  `user_import_resume_2d.go:68` is the DOCX-only ConvertToPDF, `:73` `ocrInput = pdfBytes`,
  `:69-74` the log-and-continue arm. `converter/gotenberg.go` has a 90 s client (`:13`),
  `ConvertToPDF(ctx, gotenbergURL, document, filename)` (`:16`) and the
  `/forms/libreoffice/convert` POST at `:31` — the latter also true at the checkout `ad9f3c49`.
  Compose: `:170-171` `gotenberg/gotenberg:8`, `:183` `profiles: [core, backend, all]`, and the
  doc's historical note that the anchor was `:268` at `0dab54d` is correct (`0dab54d:268` is that
  same profiles line).

---

## What I could not settle

- **GitHub org-API facts.** §3's *"93 repositories … 46 named by no corpus document"*, the 19 census
  rows' last-push dates, and every *"Repo ARCHIVED / NOT archived"* assertion
  (`graphql-wundergraph` 2026-07-30, `skiller` 2026-07-01, `skillpath` 2026-07-31, `intelligence`
  2026-04-02, `chronos` not archived) live in the GitHub API, not in git objects. The **4** in
  `repos.yml` I did enumerate. The `jobsimulation` row's own *"report both, assert neither"*
  handling of its archive state is, notably, the correct posture given that gap.
- **Live DB row counts.** `public.skill_embeddings = 42,790` / `public.job_role_embeddings = 18,919`
  (`ai_architecture.md:156`, measured 2026-06-29) and the `≥42,790 / ≥22,470` taxonomy floors need a
  prod read.
- **`skiller` migration ids** `20260417103036` / `20260417120309` (`ai_architecture.md:169`): the
  `skiller` repo is decommissioned and in no clone set, so neither id is checkable here.
- **The standalone `ai` module @ `v1.40.1`** (`ai_architecture.md:94`: `speech.go:12-13`,
  `TTSV2 = "tts-2"` / `TTSV2HD = "tts-2-hd"`). The module is pulled at Docker build, never cloned.
  Worth flagging for a future reading: the two frozen husks that still require it pin **`v1.40.2`**,
  not `v1.40.1`, so the version in the citation is not the one anything in this tree resolves.
- **`storage`'s M903 plan text** (`platform-migration-status.md:92`): it is cited at
  `app/knowledge/plan/releases/09.00-support-in-app/m903-s3-custody/`. I did not open it — the
  briefing's hard bar 1 forbids `knowledge/plan/**`, and I applied that path rule conservatively
  across repos rather than deciding it meant only rosetta's copy. Everything else in that row
  (the 18-line module, the zero `custody` hits, the single `M903` occurrence, `d3e6d32`) is
  enumerated above.
- **`coursebuilder.md:145`'s "142 changelog lines"** — booked as a minor rather than a blocker
  because the derivation is unstated; three plausible ones give 140 / 71 / 58 at the pinned ref.
- **B3's adjudication direction.** Whether `platform-migration-status.md:93`'s messenger terraform
  citations should be graded at the checkout (`fa47850d`, where they are refutable) or at
  `origin/main` (`e9421c68`, where they are exact) turns on a rule the ADDENDUM leaves open for the
  four behind-origin clones. Either verdict repairs the same way: name the ref in the cell.
