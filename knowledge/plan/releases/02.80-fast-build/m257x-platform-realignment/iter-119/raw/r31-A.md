# Seat A — reading #31

**Refs I graded against** (re-verified at this reading's open, all identical to the ADDENDUM's superseded table):
`platform 0c91421d` (== origin/main) · `app ad9f3c49` (== origin/main) · `app/studio aeec036a` (own checkout) ·
`next-web-app 8297c684` (origin/main `f97ba659`) · `sentinel f2c46190` · `studio-desk 41ee3575` ·
`ant-academy 22df69dd` · `storage 4ce8ece5` · `messenger fa47850d` · `cms ca50c817` (origin/main `f38c0c4a`) ·
`graphql-wundergraph 60c229f3` · `roadrunner 87d8d443` · `jobsimulation 462343b0` (origin/main `82cb66ec`) ·
`cms/studio aeec036a`.
**rosetta-extensions:** I read the **pinned per-stack consumption clone** `stack-demo/rosetta-extensions @ 09d06070`
for every tooling claim (per ADDENDUM / §5 rule 45); the authoring copy is at `43049308` and I did not grade
anything against it. Corpus HEAD: `04cbcfc` (working tree clean apart from this report's directory).

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/architecture/external_services.md` | 887 | yes |
| `corpus/services/jobsimulation.md` | 316 | yes |
| `corpus/services/customerio-sync.md` | 165 | yes |
| `corpus/services/askengine.md` | 147 | yes |
| `corpus/services/intelligence.md` | 18 | yes |

## Blockers

### B1 — `askengine.md` still calls the `ai` package "the shared `ai` library" — twice — after `app` folded it in-tree

- **Anchor:** `corpus/services/askengine.md:81` — *"…**PostgreSQL** (read-only, org-scoped); the / shared **`ai`**
  library (`CreateEmbeddings`, text-embedding-3-small, for RAG golden-example retrieval);"* (the phrase wraps
  `:80-81`).
  **Second site in the same file:** `corpus/services/askengine.md:113` — *"…and an embeddings provider for the shared
  `ai` / client."* (wraps `:113-114`).
- **Why it is false:**
  - **Against source.** `app` folded the library into its own tree at `1e457fa70` (*"refactor(ai): fold the ai library
    into app as internal/ai"*, 2026-08-04). At `app ad9f3c49` (= origin/main): `git grep "anthropos-work/ai" ad9f3c49
    -- go.mod` → **rc 1, zero hits**; `go.mod:14-18` requires exactly `analytics-go`, `colony`, `proto`, `storage`,
    `taxonomy`, and no other `anthropos-work/*` line exists in the file (`git grep anthropos-work ad9f3c49 -- go.mod`
    returns only `module …/app` + those five).
  - **And the very code this bullet describes imports the in-tree path.** The RAG embedding call the sentence cites is
    `app/internal/web/backend/ask/embed.go:23` `aiClient.CreateEmbeddings(...)`, and that file's import at
    `embed.go:8` is `github.com/anthropos-work/app/internal/ai` — as are `ask/examples.go:15` and
    `ask/handler.go:20`. There is **no** import of `github.com/anthropos-work/ai` anywhere in `internal/askengine/`
    or `internal/web/backend/ask/`. (Repo-wide the only file mentioning the old module path is
    `internal/ai/module_import_guard_test.go`, a one-way-door guard.)
  - **Self-contradiction, four ways.** The corpus has repaired exactly this predicate everywhere else and says so in
    writing: `corpus/architecture/external_services.md:554` (*"that interface is **no longer a shared private module
    for any service a stack builds**, and this sentence said 'the shared `ai` library' until M257x iter-115"*);
    `corpus/services/jobsimulation.md:168` (*"through one `ai.AI` interface that is **in-tree, not a shared
    module**"*, booked as *"a third instance the iter-113 enumeration did not book"*);
    `corpus/architecture/ai_architecture.md:100-104`; `corpus/architecture/shared_libraries.md:126` (*Imported by* =
    *"No repo a stack builds"*); `corpus/architecture/README.md:21` (*"`ai` is no longer one of them"*). I verified
    all five sibling sites carry the correction. **`askengine.md` is the surviving member of that predicate set** —
    exactly the leak-to-the-edge-of-the-repair-scope failure `platform-alignment.md` §5 rule 19/40 describes.
- **Confidence:** high

### B2 — LiveKit and AWS Chime are declared, present tense, to integrate with the "Jobsimulation **service**", which does not exist

- **Anchor:** `corpus/architecture/external_services.md:725` — `| **Integration** | Jobsimulation service |` (LiveKit
  property table), and the identical row at `corpus/architecture/external_services.md:741` (AWS Chime SDK property
  table). Neither table carries a HISTORICAL marker; both are present-tense "what this integration is wired to" rows.
- **Why it is false:**
  - **Against source.** There is no `jobsimulation` compose service at `platform 0c91421d`: the file declares five
    services (`sentinel:5`, `backend:28`, `studio-desk:112`, `next-web-app:143`, `gotenberg:170`) and no
    `jobsimulation` entry; `repos.yml` @ `0c91421d` lists four repos (app, sentinel, next-web-app, studio-desk).
    `d11a403` deleted the compose service **and** the `repos.yml` entry (measured: `d11a403^` had `jobsimulation:` at
    `:83`, `d11a403` does not). Both integrations run inside `backend`: LiveKit at
    `app/internal/jobsimulation/calls/livekit.go` (room + `EncodedFileType_MP3` egress at `:176`), Chime under
    `app/internal/jobsimulation/recording/`, wired by `app/internal/jobsimwiring/wiring.go`.
  - **Self-contradiction inside the same file and across the corpus.** ~160 lines above, this file's own AI-providers
    table deliberately writes **"Jobsimulation domain"** (`:562`, `:564`) and the *Architecture* section states
    *"There is no `cms`, `jobsimulation` or `roadrunner` container to start"* (`:173`).
    `corpus/architecture/dependency_map.md:76` says outright *"there is no jobsimulation service to reach"*, and
    `corpus/services/jobsimulation.md:3` heads the file *"⚠️ Merged into `app` — no longer a standalone service"*.
    Measured corpus-wide, present-tense *"Jobsimulation service"* survives at exactly these **two** sites (the only
    other two hits are `dependency_map.md:76`, which negates it, and `chronos.md:214`, inside an archived-service
    doc); *"Jobsimulation domain"* is used 7 times.
  - This is the *named-consumer list survives the merge that moved the consumer* class §5 documents: a reader
    consulting "where does LiveKit plug in?" is sent to a container/repo that `make init` does not clone and compose
    cannot start.
- **Why I hesitated:** it can be read as loose shorthand for "the jobsimulation domain", and the section *bodies* are
  correct (they cite `app` paths). I booked it because it is (a) a named service that does not exist, (b) present
  tense with no historical fence, and (c) contradicted by this same file's own careful "domain" wording and by
  `dependency_map.md`.
- **Confidence:** medium

## Minors

- `corpus/architecture/external_services.md:455` — the verbatim quote attributed to `graphql-wundergraph/CLAUDE.md:39`
  (*"Since cms-in-app the platform compose `graphql` service builds from the **production** Dockerfile"*) is byte-exact
  at **`:33`**, not `:39`. `:39` restates the same proposition in different words (*"Since the compose stack builds
  from the production Dockerfile, a schema change in `app` does not appear at `:5050`…"*), so the anchor supports the
  claim; only the quotation is mis-sited. Both lines were written in `60c229f` (2026-07-30) as the corpus says.
- `corpus/architecture/external_services.md:727` — *"There is no `anthropos-agent-eu` — the name appears nowhere in the
  platform (0 hits across all 15 trees at their own refs, and 0 in a `.gitignore`-blind filesystem grep)."* The
  headline claim is **true** for platform source (0 hits in all 13 platform repos + both nested `studio` checkouts at
  their own refs, positive control `anthropos` matching 10,754 lines in `app` alone). But the parenthetical
  measurement is not: `stack-demo/rosetta-extensions @ 09d06070` returns **7 lines in 7 files**, and a raw filesystem
  sweep of `stack-demo` returns the same 7 files. Every hit is a checked-in *copy of this corpus's own earlier prose*
  under `stack-core/tests/fixtures/` — so the substance stands and only the "15 trees / filesystem grep" figures are
  wrong.
- `corpus/services/jobsimulation.md:12` — *"still reads `service_desired_count = 0` in an otherwise-whole **191-line
  module**"*. 191 is the length of the whole file `cms/terraform/main.tf` @ `f38c0c4a`; the `module "cms"` block runs
  `:30-191` (162 lines). The load-bearing fact (`:39` = `service_desired_count = 0`, module intact) is correct at both
  `f38c0c4a` and the checkout `ca50c817`.
- `corpus/architecture/external_services.md:636` — cites `config_template.ini:30-31` with no directory; the file is
  `app/studio/configs/config_template.ini` (verified: `:30 OPENAI_API_KEY`, `:31 OPENAI_ENDPOINT =
  https://api.openai.com/v1/chat/completions`). Resolvable only from the surrounding `app/studio/configs/*.ini`
  context.
- `corpus/architecture/external_services.md:350` — *"Invalidate CMS service cache when content updates"*, present
  tense, in the Directus-webhook use-case list. Same predicate as B2 but a weaker site (generic bullet list, and
  `:225` immediately above correctly says *"it **was** the standalone CMS Service until cms-in-app folded it into
  `app`"*).
- `corpus/architecture/external_services.md:853-856` — the shell comment block opens `(docker-compose.yml:101-109 —
  redis, postgresql, sentinel;` and never closes the parenthesis. Cosmetic; the anchor itself is correct.
- `corpus/services/jobsimulation.md:150` — cites `architecture_overview.md:335` for *"the correctly-scoped model
  wording"*; `:335` is the heading line (*"**On a local stack** (platform `2adcf71` deleted the router…)"*) and the
  wording itself is at `:339`. Off by four, inside the same block.

## Positively cleared (audited zeros)

**ENUMERATED** (predicate re-derived from source independently, cardinality stated before any arithmetic):

- **Compose service set / profile floor** (`external_services.md:170-175`, `:305-306`, `:418`;
  `jobsimulation.md:56-57`, `:100-101`, `:275`). `platform 0c91421d docker-compose.yml` declares **five** services
  (`sentinel:5`, `backend:28`, `studio-desk:112`, `next-web-app:143`, `gotenberg:170`); `common.yml` adds
  `postgresql:2` + `redis:24` ⇒ **seven** effective. `profiles:` keys exist at exactly four lines (`:110`, `:141`,
  `:168`, `:183`), so `sentinel`/`postgresql`/`redis` are profile-less and in every selection; `core` adds
  `backend` + `gotenberg` ⇒ **five** containers. `Makefile:10` = `PROFILE ?= core`. At `0dab54d` the same enumeration
  gives **eight/ten**, exactly as the doc says.
- **`docker-compose.yml:110` / `:183`** (`external_services.md:172`) — both resolve to `profiles: [core, backend,
  all]` on `backend` and `gotenberg` respectively, i.e. the right construct for "the actual `core` members".
- **Frontend endpoints** (`external_services.md:197-198`, `:772`) — `:151` (next-web build arg), `:160` (runtime env),
  `:119` (studio-desk build arg), `:135` (runtime env), all four `…:8082/graphql/query`.
- **`backend` depends_on `:101-109`** = redis / postgresql / sentinel; the `$HOME/.aws` bind is the **only** one in the
  file (`:100` @ `0c91421d`, `:91` under `volumes: :90` with the comment at `:88-89` @ `0dab54d`, exactly as
  `jobsimulation.md:232-236` pins it).
- **`*_RPC_ADDR` history** (`jobsimulation.md:46-53`, `:127`, `:153`, `:156`; `:150`). At `d11a403^` the messenger
  block held `BACKEND_USERS_RPC_ADDR=http://backend:8083` (`:255`), `CMS_RPC_ADDR=http://cms:8091` (`:256`),
  `JOBSIMULATION_RPC_ADDR=http://jobsimulation:8401` (`:258`), `SKILLER_RPC_ADDR=http://backend:8083` (`:265`) — so
  `d11a403` moved **the middle two only**, and "there were never four to re-point" is exactly right. At `0c91421d`
  `grep _RPC_ADDR` returns **rc 1**; the one service address is `AUTHORIZATION_ADDRESS=http://sentinel:8087` (`:48`),
  plus `GOTENBERG_URL` (`:57`) and `JUDGE0_BASE_URL` (`:59`) over plain HTTP.
- **Supergraph subgraph counts** (`external_services.md:373-386`) — read straight off
  `supergraph-config-prod.yaml` at each commit: `749dc86~1` = **5**, `749dc86` (2026-06-24) = **4**, `7c17e63`
  (2026-07-21) = **3**, `915da06` (2026-07-29) = **1**. `git show --name-status 915da06` marks **both**
  `schemas/cms.graphqls` and `schemas/jobsimulation.graphqls` `D`; the commit subject really does read *"supergraph
  2→1"* against a three-entry tree. `subgraphs.conf` = `BACKEND=v1.360.0`; `schemas/` holds `backend.graphqls` alone.
- **No subscriptions** (`external_services.md:387`, `:538`) — all three `supergraph-config-*.yaml` at `60c229f3`:
  `grep -i "sse|subscription"` → **rc 1**, positive control `backend` matches all three files.
  `schemas/backend.graphqls`: `type Mutation` at **`:4053`**, `type Query` at **`:4912`**, **no** `type
  Subscription`; the `Subscription` substring occurs on exactly **6** lines, all Stripe/plan names. `git log -S
  'protocol: "ws"'` on mainline = **0**; positive control `sse_post` = **5** commits. `bba862f` exists, is **not** an
  ancestor of HEAD (rc 1) and lives only on `remotes/origin/feat/use-web-socket`.
- **Pre-drop router compose** (`external_services.md:420-430`, `:434-437`, `:445-451`, `:459-473`) — `2adcf71^1` **is**
  `1e8e754`; its `depends_on` at `:19-27` names exactly backend / jobsimulation / cms / storage, each
  `condition: service_started`; `"5050:8080"` at `:14`; `dockerfile: graphql-wundergraph/Dockerfile.dev` at lines 6-8.
  `464dfe3` is **not** an ancestor of `0c91421d` (rc 1) and sits on `origin/feat/cms-in-app`. `b56d731` still has
  `  graphql:` at `:22` behind `profiles: [wundergraph-deprecated]`; `360efd4` has none, with `  backend:` at `:28`
  as the positive control. The three-era build table reproduces exactly (`63d285c` git-URL context, no `dockerfile:`
  key → `719befb`; `2c85211` adds `dockerfile: Dockerfile.dev`; `67ba772` raises the context to `..`).
  `Dockerfile.dev` @ `60c229f3`: **one** schema `COPY` at `:18`, **one** `awk` at `:23`, the replacement comment at
  `:19-20`, `wgc@0.104.0` at `:7`, router image `0.275.0` at `:29`.
- **Router prod ports** (`external_services.md:367`) — `terraform/locals.tf:8` `port = 8080`; `terraform/main.tf:48-49`
  `containerPort`/`hostPort` = `${local.port}`; `config.prod.yaml:5` `listen_addr: 0.0.0.0:8080`;
  `terraform/main.tf:20` `service_desired_count = 1`.
- **The historical Directus compose service** (`external_services.md:146-147`) — `a2a3ee6^:docker-compose.yml` has
  `:384 image: directus/directus:10.10.1`, `:386 - 8055:8055`, `:409 - ADMIN_PASSWORD=password`; `git log --all -S
  'admin@example.com' -- docker-compose.yml` returns nothing, so *"only the email is unfound"* holds.
- **rext Directus re-point** (`external_services.md:136`, `:206-211`, `:219`; `jobsimulation.md:153`) — at the pinned
  clone `09d06070`: `stack-injection/gen_injected_override.py:86` and `stack-core/gen_override.py:58` both read
  `DIRECTUS_DATA_CONSUMERS = ("cms", "backend")`; `:77-81` carries the inert-key rationale **verbatim** as quoted;
  the re-point is at `:698-699`; `stack-injection/tests/test_injection.py:1109` is
  `def test_backend_the_actual_reader_is_repointed`. At the prior pin `ab81527a` the same constructs are at `:84` and
  `:669-670`, exactly as the doc's rot-warning says.
- **`app`-side cms/Directus wiring** — `app/main.go:980-982` @ **`b948604f`** is the `DIRECTUS_BASE_ADDR` `log.Fatalf`
  (at the checkout it has moved to `:1090/:1092`, which is why the pin matters); `cms_reader_switch.go:29` carries the
  quoted *"…no internal traffic to a standalone cms"*; `internal/cms/directus/` exists with the client files.
- **AI routing** (`external_services.md:581-643`) — `internal/jobsimulation/ai/ai.go` @ `ad9f3c49`: vendor consts
  `:30-33`; `getClient` `:259-289`; the `flag_use_azure_us` lookup + error-keeps-EU-client `:264-276`; `AnthropicAws`
  and `Anthropic` both → `a.anthropicClient` `:280-283`; `isThrottlingError` (429 only, OpenAI + Anthropic SDKs)
  `:130-141`; `throttled → vendor = Openai` at `:150-155` and mirrored `:299-301`/`:326`;
  `openai.NewOpenAI(openaiKey)` `:80`. The unset-vendor default: `internal/cms/directus/collections/jobsimulation.go`
  `:905` `AIVendor *AIVendor`, `:1302-1305` `aiVendor := simulation.Openai`;
  `internal/jobsimulation/simulator/ai/ai.go:58-59` maps it and the `default:` arm at `:114-115` does the same.
- **Bedrock model ids** — `coursebuilder/bedrock.go:23` opus-4-8, `:29` sonnet-4-6, `:106-113` the key-present /
  key-absent switch; `askengine/bedrock.go:25` sonnet-4-6; `jobsimulation/agent/report_agent.go:31` sonnet-4-6;
  `app/main.go:770` @ `b948604f` `coursebuilder.ModelBackendName()`.
- **Mistral is OCR-only** — `internal/cms/studio/markdownManager.go:30` `mistralocr.New(aiKey)` inside
  `NewMarkdownManager` (`:29`), field at `:14`, import at `:10`; `studioManager.go:583`; `NewMistral` = **0** hits
  repo-wide at `ad9f3c49` (positive control `mistralocr` = 7 files); `:19` really is a doc-comment line.
  In the **nested** `stack-demo/app/studio @ aeec036a`: `tools/pdf2md.py:24 from mistralai import Mistral`
  (`mistral-ocr-latest` at `:127`), and `tools/r3.py` **does** dispatch it (`:139`, `:190`, `:199-206`).
- **Studio-Room config** — all **30** `*_AI_*_MODEL` lines across the three `configs/*.ini` (10 each) pin `azure`;
  `gen.py:41-53` overrides only the **six** `*_API_KEY`/`*_ENDPOINT` names listed at `:45-48` and never the service
  selector; `services/ai.py:704-724` provider set `{openai, azure, anthropic}`, `:706-708`, bare
  `OpenAI(api_key=…)` at `:383`, `AnthropicProvider` at `:627-664`; `bedrock|boto3` = **0** hits in that tree
  (positive control `anthropic` matches 5+ files).
- **Env-var occurrence counts** (`external_services.md:668-677`) — re-derived at `app 5ba17044` over `*.go`:
  `OPENAI_KEY` 5 · `ANTHROPIC_API_KEY` 26 · `AZURE_OPENAI_KEY` 13 · `AZURE_OPENAI_ENDPOINT_URL` 13 ·
  `OPENAI_ORG_ID` 0 · `AZURE_OPENAI_ENDPOINT` 0 · `AZURE_OPENAI_DEPLOYMENT` 0 · `OPENAI_API_KEY` 2. Exact.
- **LiveKit agent names / endpoints** (`external_services.md:727`; `jobsimulation.md:167`) —
  `calls/livekit.go` @ `ad9f3c49`: `:110` bare default, `:115` `anthropos-agent-chain`, `:118-128` the branch,
  `:120` EU keeps the bare name, `:122-123` random pick from `euAgentEndpoints` (`:101-104` =
  `{azure-eu, azure-eu-fr}`), `:111` pre-branch default `azure-eu`, `:126` `anthropos-agent-%s`. The
  `flag_use_realtime_openai` block is `:131-135`/`:140-144` and does reset the name at `:142` and the endpoint at
  `:143`. Nil `voice_engine` → `gptrealtime` at
  `internal/cms/directus/collections/jobsimulation.go:1594-1597`; the frozen `cms` repo has **0** root-level
  `directus/` paths and 27 under `internal/directus/`, exactly as the doc's ⚠️ says.
- **jobsim data model** (`jobsimulation.md:39-45`, `:99`) — `20260722081626_jobsim_data_model.sql` contains exactly
  **23** `CREATE TABLE` statements, the first of which is `"sessions"`; `20260722104506.sql:2` creates
  `job_simulation_sessions` and `:79` `DROP TABLE "sessions"`.
- **the mirror drop** (`jobsimulation.md:134-139`) — `20260729133514.sql`: remap UPDATEs `:15-23`, orphan NULLs
  `:36-44`, *"5. Drop the mirrors."* comment at `:58`, `DROP TABLE` both mirrors `:62-63`. **Zero** real `INSERT`
  statements (`grep "INSERT INTO|^INSERT"` → rc 1; the single case-insensitive hit is the identifier
  `on_insert_local_jobsimulation_sessions_update_memberships` at `:64`).
  `app/internal/organization/intelligence.go:1700` = `m.ent.JobSimulationSession.Query()`.
  `jobsimulation/internal/graph/queries.resolvers.go:70` = the `JobSimulationResult` resolver, whose v2/v3 arm is a
  plain `GetValidationAttemptResultBySession` read.
- **jobsim ports / cobra contract** (`jobsimulation.md:100`, `:184-194`, `:297`) — `cmd/root.go:77` `PORT` default
  `8080`, `:78` `RPC_PORT` default `8081`; both Dockerfiles `EXPOSE 8080`; the in-repo `CLAUDE.md:124-125` documents
  the same pair and `:88` really does say `go run . serve`. Subcommands are exactly **four** — `aggregate`
  (`cmd/aggregate.go:57`), `clone-session` (`:43`), `test-command` (`:46`), `validate` (`:42`) — plus the root
  `Use: "jobimulation"`. The 8400/8401 pair @ `2adcf71`: service at `:83`, `profiles: [graphql, jobsimulation, all]`
  at `:140`, `PORT=8400` `:113`, `RPC_PORT=8401` `:119`, published `:93-94`; `Makefile:10` `PROFILE ?= graphql`;
  `repos.yml:17-19` with `migrations: false # legacy`.
- **`app` has no cobra root** (`jobsimulation.md:206-215`, `:242-245`) — `func main()` at `:216` @ `7177374`, `:212` @
  `b948604f`, `:229` @ `ad9f3c49`; the **only** `spf13/cobra` importer is `cmd/createTaxonomy/main.go`.
  `jobsimwiring.Wire` `:721` / `log.Fatalf` `:723` @ `ad9f3c49`, `:614` @ `b948604f`, `:670` @ `9d00a313` — all three
  resolve. `internal/jobsimulation/ai/ai.go:90` = `can't load AWS config: %w`;
  `internal/jobsimwiring/wiring.go:147-148` returns it unwrapped.
- **jobsim terraform teardown** (`jobsimulation.md:7-11`, `:69-79`) — `6092c6d2` is on `origin/main` `82cb66ec`
  (not on the checkout `462343b0`; the claim names the sha, so it is graded there). At `82cb66ec`
  `terraform/main.tf` is 56 lines, `service_desired_count` appears **nowhere** in any of the six `.tf` files
  (positive control `atlas` = 11 lines in `main.tf`), `:15-22` is the M810 decommission comment naming exactly the
  destroyed resources, `:24-40` the "what this module still owns" + atlas-tracker note, `:38-40` the
  schema-drop-is-separate note. The four 2026-08-04 commits are `6092c6d2`, `caf36c96` (PR #439), `1e40d184`,
  `82cb66ec`.
- **`backend.internal.anthropos:8081`** (`jobsimulation.md:54`) — **44** tracked `.tf` files across the
  `stack-demo` repos and **59** on a filesystem sweep; **0** hits in any `.tf` at any clone's own HEAD; the literal
  occurs on **6** lines total, **1** in `app` (`knowledge/service-dependencies.md:52`, past tense, under
  *"There are no external callers of app's RPC mux left."*) and 5 in rext — none terraform.
- **subscriber merge** (`jobsimulation.md:63-65`) — `internal/jobsimulation/simulator/stream_handlers.go:4` states the
  rule verbatim (*"MERGE them onto its EXISTING subscribers via `.AddHandler` (never a 2nd AddSubscriber — colony
  keys …)"*), and `simulator.go:1586`/`:1593` do it.
- **customerio-sync** — `internal/customeriosync/` = `doc.go`/`store.go`/`sync.go`/`sync_query.sql`(+test);
  `doc.go:4-5` carries *"Ported out of the customerio-sync repository (v0.19.3), the last of the / Go services to be
  folded into app"* with the wrap exactly where the doc says; `env_guards.go:62` `CUSTOMERIO_SYNC_ENABLED`;
  `env_guards.go:98-104` the deployed-unset error, `:87` the `log.Fatalf`; `main.go:284` is the **comment**, `:286`
  the resolution, `:295` the condition and `:296` the `log.Fatalf`, `:393-396` the shared `copilotDB` construction
  with `os.Getenv("BREVO_KEY")` at `:395`; `docker-compose.yml:84-92` @ `0c91421` is the deliberate no-value block.
- **the git-URL build pattern** (`customerio-sync.md:85`, `:164`) — re-derived over **86** commits touching
  `docker-compose.yml` on all refs: exactly **18** distinct repo names ever built from a `git@github.com:` context,
  and the doc's list matches name-for-name. After `a2a3ee6` (2026-02-27) only **two** kept one (customerio-sync,
  realtime); realtime's disappears at `c17cc9a` (2026-04-15). At `838d907^` there are **seven** `context:` lines and
  customerio-sync's is the only git URL. The deleted block matches (`context` `:138`, `ports 8080:8080` `:143-144`,
  `DB_CONNECTION_BACKEND` `:148`, `depends_on postgresql` `:151-153`).
- **`external_services.md` has no Brevo/Customer.io section** (`customerio-sync.md:151-156`) — `grep -ci brevo` = **0**
  (positive control `clerk` = 38); the `##` headings are exactly Clerk / Directus / the Cosmo router / AI Providers /
  LiveKit / AWS Chime plus five non-service sections.
- **askengine registry** (`askengine.md:47-51`) — `internal/askengine/registry.go` @ `ad9f3c49` has **60** `Name:`
  entries, **57** `Schema: "public"` and **3** `Schema: "directus"`; `:183-195` is the jobsimulation
  transition-alias comment naming `rewriteSchemas` and M710.
- **askengine constants & tests** — `askEngineMaxConns = 6` (`main.go:167`, applied `:374` to a pool built from
  `copilotDSN` at `:370`); `executor.go:24 QueryTimeout = 10 * time.Second` used by the `SET LOCAL
  statement_timeout` at `:97`; `MaxInlineRows = 200` `:18`, `MaxCellLength = 400` `:21`; `handler.go:34
  maxAgenticIterations = 15`, `:41 loopTimeout = 10 * time.Minute`, `runAgenticLoop` at `:473`; `rules.md` =
  **146,219 bytes**; test-function counts **49 / 13 / 10 / 14** for sandbox / executor / prompt / followups.
  The `/ask` group is at `backend.go:171` with **exactly** the eleven routes the doc lists (`:175-188`), and the
  source comment confirms it skips OpenAPI validation. Tags: `v1.267.0` = 2026-05-07, `v1.340.0` = 2026-07-17.
- **askengine Bedrock init** (`askengine.md:88-111`) — `main.go:467-471` @ `ad9f3c49` is byte-identical to the quoted
  block; `func main()` at `:229` and **no** other `func` declaration between `:229` and `:467`; the log string occurs
  **once** in `app`'s Go; `internal/askengine/bedrock.go:161` `NewBedrockClient` has exactly **one** error return
  (`config.LoadDefaultConfig`), and `Temperature: param.NewOpt(float64(0))` + `NewCacheControlEphemeralParam()` back
  the `bedrock.go` row. `:1295` is the Connect-RPC `http.NewServeMux()`, `:1361` the meta server.
- **`WrapQuery` "four documented bypass vectors"** (`askengine.md:44`) — worth naming because the source's own prose
  says *"three"* at `sandbox.go:350` and then enumerates **four** bullets (`:352-362`: quoted identifier · comment
  hiding · unresolved FROM target · reserved CTE name), each with its own error path in the body (`:376`, `:371`,
  `:382`, `:397`). **The corpus's count is the correct one**; the platform comment is the stale side.
- **intelligence.md** — `fdfa189` (2026-04-17) modifies **both** `docker-compose.yml` and `repos.yml`; the removed
  block (`fdfa189^:docker-compose.yml:61-82`) sets `DB_CONNECTION_BACKEND` (`:72`) and `DB_CONNECTION_SKILLER`
  (`:73`), and `- name: intelligence` disappears from `repos.yml` in the same commit.
- **Clerk / sentinel split** (`external_services.md:69`, `:73-81`, `:795-796`, `:822-823`) — sentinel @ `f2c46190`
  has **0** hits for `clerk` and **0** for `authn` in its Go (positive control `casbin` = 3 files) and **0** for
  `webhook`; `app/internal/web/backend/backend.go:130` is `/api/webhook/clerk`; `CLERK_WEBHOOK_SECRET` exists
  (`.env_example:55`, read at `app/main.go:567`). SDKs: `studio-desk/package.json:27-28` `@clerk/clerk-js` +
  `@clerk/express`; `next-web-app` web/hiring/integration `@clerk/nextjs`, `apps/mobile` `@clerk/clerk-expo`;
  `ant-academy/code` `@clerk/nextjs`, `ant-academy/mobile` `@clerk/clerk-expo`.
- **`NEXT_PUBLIC_GRAPHQL_ENDPOINT` does not exist in next-web-app** (`external_services.md:771`) — `git grep -c` →
  **rc 1**; positive control `NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT` matches `Dockerfile.dev` + three `.env.example`s +
  app source.
- **`storage-legacy` phase** (`external_services.md:859-869`) — at `0dab54d` `storage` really did carry its own block
  behind `profiles: [storage-legacy]` (`:134`), `messenger` `profiles: [messenger]` (`:195`), `customerio-sync`
  `profiles: [customerio-sync, all]` (`:154`); `838d907` deletes all three and drops `storage`+`messenger` from
  `repos.yml` (6 → 4 entries).
- **`d11a403` deleted cms + jobsimulation + roadrunner** from compose *and* `repos.yml` (9 → 6 entries), as
  `external_services.md:173` and `jobsimulation.md:16-17` assert.
- **`ai` fold sibling sites** — I checked the corpus claim in `external_services.md:554` that
  `ai_architecture.md` (`:98-104`), `shared_libraries.md` (`:9`, `:126`) and `architecture/README.md` (`:21`) all
  already carry the correction: they do. Likewise `jobsimulation.md:164`'s and `:167`'s claims about
  `ai_architecture.md:222` and `:233`/`:241`.

**RE-DERIVED only** (weaker clearance — I recomputed arithmetic the document displays without an independent
enumeration of the predicate): none. Every numeric clearance above was reached by enumerating the set first.

## What I could not settle

- **`external_services.md:3` — *"the repo is ARCHIVED on GitHub (2026-07-30)"*** (graphql-wundergraph). GitHub archive
  state is not visible from a clone. `jobsimulation.md:24-27` explicitly retracts the parallel claim for its own repo
  on exactly this ground ("the repo's GitHub archive state is not visible to this corpus", and four commits landed
  after the asserted archive date). For `graphql-wundergraph` the checkout is level with `origin/main` at `60c229f3`
  (2026-07-30) with nothing after, so the claim is at least *consistent* — but it is unmeasurable here, and the two
  files treat the same kind of claim with different standards. Settled by `gh api repos/anthropos-work/graphql-wundergraph --jq .archived`.
- **`intelligence.md:20` — *"Exposed an HTTP server on :8080 (PORT env override) serving only a /_meta health
  endpoint"***. The `intelligence` repo is in no clone set. All I can measure is the compose block that ran it
  (`fdfa189^:docker-compose.yml:61-82`), which sets `PORT=9002` and publishes **no** ports — consistent with a
  binary default of 8080 overridden by compose, but not evidence for it. Settled by cloning
  `anthropos-work/intelligence` and reading its `cmd/`.
- **Production teardown of `cms`'s ECS service.** Both `external_services.md:175` and `jobsimulation.md:12` say the
  deciding declaration lives in `infrastructure`, which has never been in a clone set. I confirmed the two measurable
  facts they cite point opposite ways (`cms/terraform/main.tf:39` = `service_desired_count = 0` in an intact module at
  both `ca50c817` and `f38c0c4a`; `6efa1d5` deleted `.github/workflows/build-production.yml` saying the ECR repo is
  decommissioned). Their "report both, assert neither" posture is the correct one and I have no basis to grade it
  further.
- **`external_services.md:707` Studio-Desk copilot chain semantics.** `AI_PROVIDER_CHAIN=azure-openai,openai` is the
  `.env.example:56` default and `src/services/ai/providerHealth.ts` is titled *"Provider Health & Circuit Breaker"*,
  so the shape is right; I did not trace whether failover is genuinely circuit-breaker-driven at runtime.
- **`external_services.md:293-296` (Directus prod S3 / CDN) and `:798-802` (prod deployment steps).** Production
  infrastructure; nothing in the clone set can confirm or refute it.
