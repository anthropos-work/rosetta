# Seat F — reading #31

Ground truth re-verified at this reading's open (per the ADDENDUM), all thirteen clones:
`app ad9f3c49` · `platform 0c91421d` · `next-web-app 8297c684` · `sentinel f2c46190` ·
`studio-desk 41ee3575` · `ant-academy 22df69dd` · `storage 4ce8ece5` (origin/main `9f8cb532`) ·
`messenger fa47850d` · `cms ca50c817` (origin/main `f38c0c4a`) · `graphql-wundergraph 60c229f3` ·
`roadrunner 87d8d443` · `jobsimulation 462343b0` (origin/main `82cb66ec`) ·
`app/studio aeec036a` (own checkout) · rext **pinned per-stack** `stack-demo/rosetta-extensions 09d06070`
and **authoring** `.agentspace/rosetta-extensions 43049308`.

Every rext claim below was read in **both** trees and they were byte-identical for the cited files;
the tree is named per finding where it matters.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| corpus/services/ant-academy.md | 503 | yes |
| corpus/architecture/architecture_overview.md | 435 | yes |
| corpus/services/storage.md | 248 | yes |
| corpus/services/roadrunner.md | 216 | yes |
| corpus/architecture/dependency_map.md | 103 | yes |
| corpus/services/TEMPLATE.md | 46 | yes |

(Also read in full, as the briefing directs: `corpus/ops/platform-alignment.md` §5, 2803 lines total.)

## Blockers

### B1 — ant-academy.md still opens by asserting the "internal-only / `@anthropos.work` employees" framing that its own §Integration Points RETRACTS as false

- **Anchor:** `corpus/services/ant-academy.md:31` — *"**Primary Goal**: Internal-only learning portal
  that delivers AI-engineering chapters to `@anthropos.work` employees…"*
  Same claim at `:5` (*"is the **internal learning portal** for Anthropos employees. It delivers
  micro-chapters … to anyone with an `@anthropos.work` email"*) and `:24`
  (*"**Authentication** | Clerk (`@anthropos.work` domain gate + org-membership gate)"*).
- **Why it is false:** the same document, at `:474`, books this exact proposition as **FALSE and
  removed** — *"⚠️ **'Domain-gated to `@anthropos.work` so external users cannot enter' is FALSE and
  was removed at M257x iter-115.'**"* — and adds that *"the corpus stated it in its own voice,
  present tense, with no attribution."* The retraction landed at ONE site; the twins at `:5`, `:24`
  and `:31` were left standing, so the file now contradicts itself across 469 lines. This is the
  §5 rule 19/40 class (a claim repaired at one site and left at another), not a legitimate
  retraction.
  Ground truth, all @ `ant-academy 22df69dd`, supports `:474` and refutes `:5/:24/:31`:
  - `code/src/lib/platformUrls.js:4` calls the Academy *"a **storefront** in front of the Anthropos
    platform"*; `:9-12` define **FLOW A — Account gate** (*"a new visitor registers"*), `:17-21`
    **FLOW B — Checkout gate** (*"contextually registers → signs in → subscribes for an anonymous
    visitor"*).
  - `code/src/lib/pricing.js:21` `export const STANDARD_YEARLY = { usd: 399, eur: 349 };`
  - `code/src/components/TopBar.jsx:77-88` renders `t('topBar.buyAcademy', 'Buy AI Academy')` +
    `platformCheckoutUrl()` **to anonymous visitors**.
  - `code/src/lib/schema.js:3` `const SITE_URL = 'https://aiacademy.anthropos.work';`
  - `knowledge/user-types.md` enumerates **Anonymous · Signed-in (free) · Subscriber ·
    Enterprise/Org member**; the detection list is `auth()`/`isInOrg()`/`billingPremium()` — **no
    `@anthropos.work` predicate anywhere**.
  - **There is no domain gate in the code.** `git grep -n "anthropos\.work" 22df69dd --
    code/proxy.js code/src/lib/*.js` returns only a dev-login log string, `app.anthropos.work`,
    `aiacademy.anthropos.work` and two e2e fixture emails — no `endsWith`/`emailAddress` domain
    check. `code/proxy.js:112-186` opens `/`, `/latest(.*)`, `/chapters/(.*)`, `/courses`,
    `/courses/(.*)`, `/library`, `/free` to anonymous traffic. (Positive control: `grep -c bucket
    corpus/services/storage.md` → 21, so the search pipeline was live.)
- **Confidence:** high

### B2 — architecture_overview.md carries the same retracted "internal / `@anthropos.work` employees" claim in two places

- **Anchor:** `corpus/architecture/architecture_overview.md:40` — *"**Ant Academy**
  (`ant-academy`): Internal learning portal (Next.js 16 + Expo mobile) for `@anthropos.work`
  employees."* Twin at `:260` — *"Internal learning portal for `@anthropos.work` employees
  (standalone, Vercel-deployed)"*.
- **Why it is false:** identical evidence to B1 (`platformUrls.js:4`, `pricing.js:21`,
  `TopBar.jsx:77-88`, `schema.js:3`, `knowledge/user-types.md`, `proxy.js:112-186`, all @
  `ant-academy 22df69dd`), and it directly contradicts `corpus/services/ant-academy.md:474`, which
  is the corpus's own adjudication of this claim. Booked separately from B1 because it is a
  different file and a claim-scoped repair that stopped at `ant-academy.md` would leave this one
  standing — the §5 rule 19 corollary (a claim leaks to the edge of the previous repair's scope and
  stops there). Note the same sentence also survives in root `CLAUDE.md` (outside this reading's
  scope; recorded under Minors).
- **Confidence:** high

### B3 — "four imported private modules … pulled at Docker build" names `ai`, which no repo a stack builds has required since `1e457fa70`

- **Anchor:** `corpus/architecture/architecture_overview.md:80` — *"4. **Shared Libraries**:
  **four** imported private modules — colony, proto, ai, taxonomy (not deployed; pulled at Docker
  build)."* The list item sits inside *"**Service Tiers** (local development reality, default `core`
  profile)"*, so the predicate is what a local stack's Docker builds pull.
- **Why it is false:**
  - `git -C stack-demo/app show ad9f3c49:go.mod | grep anthropos-work` → `analytics-go`,
    `colony v0.35.2`, `proto v1.210.0`, `storage v0.15.2`, `taxonomy v1.2.0`. **No `ai`.**
    At `b948604f` it *was* there (`go.mod:14 github.com/anthropos-work/ai v1.40.2`); `1e457fa70`
    (2026-08-04, *"refactor(ai): fold the ai library into app as internal/ai"*) removed it, and
    `internal/ai/module_import_guard_test.go` exists to keep it out.
  - `git -C stack-demo/sentinel show f2c46190:go.mod` → colony, proto, taxonomy (indirect). No `ai`.
  - `platform 0c91421d:repos.yml` has **4** entries (app, sentinel, next-web-app, studio-desk), so
    `app` and `sentinel` are the only Go builds. Enumerated `require` counts across all seven Go
    repos at their pinned refs: colony **7/7**, proto **7/7**, taxonomy **6/7** (not roadrunner),
    ai **2/7** (only the frozen `cms go.mod:9` / `jobsimulation go.mod:11`, neither cloned nor
    built), authn **0/7**.
  - It contradicts two sibling corpus statements: `corpus/architecture/dependency_map.md:42`
    (*"the two Go repos a stack clones and builds … pull in **THREE** of them — colony, proto,
    taxonomy … `app` **dropped** the `ai` module at `1e457fa70`"*) and
    `corpus/architecture/shared_libraries.md:24-25` (*"**four** of the five are pulled by at least
    one repo, and only **three** by the two repos a stack actually builds"*).
  - The same file's Shared-Libraries table (`:228-236`) repeats the framing: the header reads
    *"Imported as private Go modules"* and the `ai` row (`:234`) carries no note that it has no live
    puller left, while the `authn` row does.
- **I hesitated** because the phrase *"`authn` is not a fifth"* suggests the author meant "four of
  the five libraries are imported by *someone*", which is true. But the parenthetical *"pulled at
  Docker build"*, inside a list explicitly scoped to *"local development reality, default `core`
  profile"*, makes it a claim about what a local build fetches — and that is three, not four. The
  corpus elsewhere draws exactly this distinction and calls it load-bearing, so the flat "four" here
  is the wrong side of it.
- **Confidence:** medium

### B4 — roadrunner.md's own cross-reference `:124` lands on the LSP-proxy bullet, not on the quoted "Upstream consumers" line (and it is *above*, not "below")

- **Anchor:** `corpus/services/roadrunner.md:130` — *"…— *"NOT stream handlers."* Consistent with
  `:124` **below** (*"Upstream consumers: none (orphaned)"*)."*
- **Why it is false:** `corpus/services/roadrunner.md:124` reads *"The repo contains an experimental
  WebSocket LSP proxy (`internal/lsp/lsp.go`) that is NOT wired into any running server — there is
  no reachable LSP endpoint today."* The quoted sentence is at `:134`
  (*"**Upstream consumers**: **none (orphaned — see the banner at the top).**"*). The anchor
  therefore **resolves but names the wrong construct** (§5 rule 46's class, and the class three of
  iter-42's eighteen fell into), and it is doubly wrong: `:124` is six lines *above* the citing
  sentence, so "below" cannot be satisfied by it either. Line drift of exactly this kind is what
  §5 rule 34 predicts when a corpus repair changes a file's line count.
- **Confidence:** high

### B5 — "~26 Playwright e2e spec files" — there are 31

- **Anchor:** `corpus/services/ant-academy.md:304` — *"| **Testing** | Vitest (happy-dom + node),
  Playwright (e2e). 1000+ Vitest tests + ~26 Playwright e2e spec files (tests/e2e/). |"*
- **Why it is false:** enumerated from source, not re-derived from the document —
  `git -C stack-demo/ant-academy ls-tree -r 22df69dd --name-only code/tests/e2e/` returns 40 paths,
  of which **31** end in `.spec.js` (the other 9 are `_helpers.js`, `_academyBackendMock.js` and
  seven PNGs under `widgets.spec.js-snapshots/`). Note the naive
  `grep -c '\.spec\.js'` returns 38 because it also matches the snapshot directory name — anchoring
  the pattern is what gives 31. The `1000+ Vitest tests` half holds (≈2,771 `it(`/`test(` call sites
  across `code/tests/**`).
- **I hesitated** because the figure carries an explicit `~` hedge and no reader doing real work is
  materially misled (they would run `npm run test:e2e`). Under the instrument's *"wrong counts"* →
  BLOCKER rule and its instruction to resolve doubt upward, I booked it here rather than as a MINOR;
  a grader who reads `~` as licensing a 16 % understatement should reclassify it.
- **Confidence:** low

## Minors

- `corpus/services/roadrunner.md:13-14` — doubled word across the line break: *"…and has\n> has
  **not been touched since `84a4b4f`**…"*. (The claim itself is correct: `git blame -L 19,19
  87d8d443 -- terraform/main.tf` names `84a4b4f1`, 2025-12-15.)
- `corpus/architecture/architecture_overview.md:348-360` — thirteen lines inside the Request-Flow
  blockquote lost their `>` prefix (`:346-347` and `:361-373` have it). It still renders as one
  blockquote via CommonMark lazy continuation because there is no blank line in the run, so there is
  no factual consequence — but the marker damage is real and a later edit that inserts a blank line
  will split the quote.
- `corpus/services/ant-academy.md:391-395` and `:412` say the app's env file is `code/.env`
  (`cp .env.example .env`; *"set `REQUIRE_ORGANIZATION_MEMBERSHIP=0` in `code/.env`"*), while the
  same file's `:26`/`:386` and root `CLAUDE.md` say `code/.env.local` (*"the React app reads **only**
  from `code/.env.local`"*). Both files exist in Next.js's load order, so both work; the "only" in
  the out-of-scope file is the inaccurate half. Non-material, recorded because the two docs disagree.
- **Out of this reading's scope, recorded so it is not lost:** root `CLAUDE.md` still carries
  *"compose sets exactly one service address, `AUTHORIZATION_ADDRESS=http://sentinel:8087`"* — the
  exact sentence `corpus/architecture/architecture_overview.md:268-270` and
  `corpus/architecture/dependency_map.md:7` both explicitly **retract** (M257x iter-102). Same
  rule-19-corollary shape as B2: the repair stopped at the scope edge and did not reach the
  highest-propagation file in the tree.
- `corpus/services/TEMPLATE.md` — 46 lines, pure template, no gradeable propositions. Nothing found.

## Positively cleared (audited zeros)

**ENUMERATED** (predicate re-derived from source independently, cardinality compared):

- `platform 0c91421d` compose topology: **5** services declared in `docker-compose.yml` (sentinel,
  backend, studio-desk, next-web-app, gotenberg) + **2** in `common.yml` (postgresql, redis) = **7**
  effective; `repos.yml` **4** entries. `postgresql`/`redis`/`sentinel` declare **no `profiles:`
  key** → in every selection. Supports `architecture_overview.md:12`, `:71`, `:157`, `:223`,
  `dependency_map.md:31`, `roadrunner.md:52,73,146`, `storage.md:28,189`. Historic counts also
  enumerated: `0dab54d` → 8+2=10 services / 6 repo entries; `2adcf71` → 9 repo entries with
  `roadrunner` at `repos.yml:29-31`.
- Every compose line anchor in my files at `0c91421`: `:48` AUTHORIZATION_ADDRESS, `:56`
  ENVIRONMENT=development, `:57` GOTENBERG_URL, `:59` JUDGE0_BASE_URL, `:66` REDIS_ADDR, `:71`
  SKILLER_STREAM, `:73-79` the "These MUST be set" comment, `:82`/`:83` the two production buckets,
  `:84-92` the MESSENGER/CUSTOMERIO rationale, `:93-94` SUPABASE/COPILOT DSNs, `:100` the
  `$HOME/.aws/credentials` mount, `:101-109` + `:102-103` backend `depends_on`, `:138-140`
  studio-desk `depends_on`, `:183` gotenberg profiles. At `0dab54d`: `:117-123` (the **seven**
  storage env vars, enumerated by name), `:119` ENVIRONMENT, `:126-131` `depends_on`, `:154`
  customerio-sync profiles. At `2adcf71`: `:206`, `:210`, `:291-292`, `:296-302` (roadrunner).
- Shared-library `require` matrix across the **seven** Go repos at their pinned refs — colony 7/7,
  proto 7/7, taxonomy 6/7 (not roadrunner), ai 2/7, authn 0/7; `app/go.mod:20` @ `b948604f`,
  `sentinel/go.mod:21` @ `88bc5592`, `cms/go.mod:9`, `jobsimulation/go.mod:11` (both ai `v1.40.2`),
  messenger taxonomy **direct** / storage taxonomy **indirect**. `colony/authn` importer counts
  app **129** / cms **6** / jobsimulation **8** `.go` files.
- `STORAGE_RPC_ADDR` across four `app` refs: `9d00a313` 3 Go lines / 29 repo lines / 18 files;
  `2035f9a4` and `ad9f3c49` 3 Go lines (identical three comment sites — `main.go:504`,
  `internal/jobsimwiring/wiring.go:101`, `internal/storagens/callsites_test.go:189`); `b948604f`
  **15** Go lines of which **7** are live `os.Getenv` reads (enumerated:
  academy-asset-upload `:129`, academyImport `:231`, cmd/import `:50`, wiring.go `:115`, main.go
  `:446`, `:524`, `:992`). `git diff --stat 2035f9a4 ad9f3c49 -- main.go internal/storage/` is
  empty, exactly as `storage.md:29` says.
- Multi-tenancy split (`architecture_overview.md:381-388`): **135** entities (`internal/data/ent/
  client.go` declares 135 `*Client` fields — an independent derivation from the doc's
  "139 files minus 4"); `OrganizationMixin{}` appears on **30** lines of which
  `user_resource.go:22` is commented out → **29** live; `OrganizationIDMixin{}` **7**;
  files carrying a literal `"organization_id"` **48** (excluding the mixin definition), of which
  **22** lack `OrganizationMixin` and **4** of those are `OrganizationIDMixin` users → **18**
  neither-mixin, of which `org_membership.go` declares its own `Policy()` and `academy_feedback.go`
  is owner-filtered by `UserMixin` → **16** neither-mixin unpoliced, **16 + 7 = 23** unpoliced with
  an `organization_id`, and **29 + Membership + Organization = 31** org-filtering. Both figures the
  document states reproduce.
- Roadrunner's async contract: `queues.go` `DefaultQueue = "roadrunner:default"`,
  `tasks.go` `RoadrunnerSubmissionResult = "roadrunner:submissionresult"`,
  `runner.go:126` `asynq.MaxRetry(3)`, `worker.go:25` `Concurrency: 10`, `worker.go:48`
  `HandleSubmissionResultTask`, `runner.go:244` `maxRetries = 15`, `runner.go:273`
  `time.Sleep(1 * time.Second)`; `cmd/root.go:110`/`:84` the 8080/8081 `cmp.Or` defaults;
  **0** `*_test.go` files; `Dockerfile:18` `RUN go test -v ./...`; Go 1.25.0; asynq v0.25.1;
  gorilla/websocket v1.5.3.
- The three-string roadrunner absence, measured **per repo at its own ref** with the search set
  named: `ROADRUNNER_RPC_ADDR` / `RoadRunnerService` / `roadrunner:10401` → **0 Go hits** at both
  `app b948604f` and `jobsimulation 462343b0` (positive control `func main` → non-zero). The
  case-insensitive whole-clone sweep reproduces the document's file counts exactly: app **25**,
  jobsimulation **8**, platform **3**, studio-desk **3**, next-web-app **1**. The two named
  non-Go sites resolve: `jobsimulation/knowledge/operational.md:68` and
  `app/knowledge/plan/releases/07.00-jobsim-in-app/RE-PORT-CHECKLIST.md:10`.
- `SKILLER_STREAM` at `ad9f3c49`: **6** Go occurrences across **3** files (main.go,
  subscriber_merge_test.go, subscriber_wiring.go) and **6** files off the `*.go` pathspec — both
  numbers the cell claims. The 6-drift/1-holds anchor split is exact: `main.go:287/:637/:1039/:1276`
  and `wiring.go:127/:180` all drift as described, and `internal/roles/roles.go:791` is
  byte-identical `func (r *RoleManager) SkillerSubscriber() *pubsub.Subscriber {` at all three refs.
  No `NewPublisher` names `SKILLER_STREAM` at any ref.
- `messenger/internal/flow/flow.go:72` `AddSubscriber("backend", …)` over **21** live handlers
  (enumerated line by line, excluding the one commented-out `OrgJobSimulationAssignmentPastDue`
  handler at `:83`); `:105` jobsimulation; `:109` cms.
- `d11a403` graded by its **diff**, not its message: `git show d11a403 -- repos.yml` removes
  `- name: cms`, `- name: jobsimulation` **and** `- name: roadrunner` while the message says
  roadrunner's entry *"was already gone"*; the same commit deletes the cms, jobsimulation and
  roadrunner **compose services** (11 services → 8). And it moved **exactly two** of messenger's
  four `*_RPC_ADDR` values (`CMS_`, `JOBSIMULATION_`); `BACKEND_USERS_` and `SKILLER_` already read
  `http://backend:8083` at `d11a403^`, and `3e85fce` (2024-04-18) introduced
  `BACKEND_USERS_RPC_ADDR=http://backend:8081` — backend from birth, port-moved only.
- `no .tf file in any clone names http://backend.internal.anthropos:8081` — swept **all 15 git
  trees** including the two nested `studio` repos, restricted to `*.tf`: **0**. (Unrestricted the
  string exists in app/knowledge, gql-wg's CLAUDE.md + `supergraph-config-prod.yaml`, and rext test
  fixtures — which is why the `.tf` scoping is load-bearing.)
- `app/studio` @ its own ref `aeec036a`: `services/ai.py:704-709` is the whole provider registry
  (`openai`/`azure`/`anthropic`, no Mistral); `tools/pdf2md.py:24` `from mistralai import Mistral`
  and `:127` `model="mistral-ocr-latest"`; `git grep -ci mistral aeec036a` → **22** hits across
  **3** files (requirements.txt 1, pdf2md.py 18, r3.py 3); all **three** `configs/*.ini` pin
  `azure` on every `*_AI_*_MODEL` row.
- ant-academy write path @ `22df69dd`: `store.js:150/:162/:169/:202/:210/:215`,
  `writeThrough/index.js:247/:259`, `beacon/route.js:1-18` — all byte-exact, and the beacon route's
  own header states the same-origin/`sendBeacon`-cannot-set-Authorization mechanism the corpus
  attributes to it.
- ant-academy catalog chain: `serverTenant.js:145` is byte-exact
  `const view = (await getBackendCatalogView(eids)) ?? emptyCatalogView()`; `:115-117` returns the
  **five** keys `{chapters, skillPaths, series, bundles: PUBLIC_BUNDLES, catalogVersion:
  CATALOG_VERSION}`; `:111-113` is the bundles rationale; `:132` is *"the cutover is intentional,
  not reversible-on-error"*; `ucourses/catalog.js:31` `CATALOG_VERSION = '1.0'` and `:961`
  `export const PUBLIC_BUNDLES = [`. `serverChapterBody.js:51-67` (`resolveServerChapterBody` →
  `getBackendChapterBody` → `maybeResolveDraftBody` → `{notFound: true}`) and
  `app/not-found.jsx:43` *"You wandered off the trail."*
- ant-academy locale surface: `PublicHeader.jsx:20` mounts `LocaleSwitch` and has **exactly one**
  mount site, `app/(public)/library/page.jsx:28`; `app/(public)/free/page.jsx:18` is
  `redirect('/?tier=free')`; `i18n/locale.js:10` `SUPPORTED_LOCALES = ['en','it','es','fr','de',
  'nl','pt']`; `LanguageSelector.jsx:88` `role="menu"`, `:97` `role="menuitemradio"`;
  `TopBar.jsx:76` `<LanguageSelector />`. The **seven** TopBar surfaces reproduce exactly by mount
  site — `AcademyClient.jsx:1906` reached from `app/(authed)/page.jsx:151`,
  `courses/page.jsx:92`, `courses/[slug]/page.jsx:219`; plus `CourseClient.jsx:2091`/`:2141`,
  `LatestClient.jsx:128`, `BookmarksClient.jsx:508`, `MyActivityClient.jsx:161` — and
  `MyCertificatesClient.jsx` imports **no** TopBar. `app/(public)/` holds only `layout.jsx`,
  `library/page.jsx`, `free/page.jsx` → there is indeed **no `/library/[slug]`** route.
- ant-academy `isPublic` matcher: every group and every pattern in the `:315-325` table is present
  in `code/proxy.js:112-186`, in the same shape, including both `_meta` spellings, `/api/verify/`,
  and the two dev-only spreads gated on `DEV_LOGIN_ENABLED` / `VISUAL_BYPASS`
  (`NODE_ENV === 'development'`, whitelisted, `proxy.js:62-64`).
- ant-academy tech stack: `app/layout.jsx:1` imports **exactly** `{ Work_Sans, Instrument_Serif }`
  (`:41`, `:51`), `academy.css:1` says display+mono *"fall back to system fonts"*, `:132`
  `manifest: "/academy-manifest.json"`, `RegisterServiceWorker.jsx` is a kill-switch,
  `code/package.json` `engines.node >=22` and `"dev": "next dev --port 3077"`,
  `code/vercel.json` is `{"framework":"nextjs"}`, FA Pro vendored under
  `code/public/assets/fontawesome/`, mobile `expo ~54.0.33` with `dev:web` + `dev:bundle`,
  workflows `deploy-academy.yaml` / `sync-vercel-env.yml` / `sidebar-coverage-tests.yaml`, `cog.toml`.
  Cosmo: `assistant/agent.js:12` `https://api.openai.com/v1/responses`, `:13` `MODEL = 'gpt-5.2'`,
  `:32` `localStorage.getItem('openai_api_key')`, flag `NEXT_PUBLIC_FEATURE_TRAINING_COACH`.
- rext claims, read in **both** trees (`stack-demo/rosetta-extensions 09d06070` — the tree that
  settles what a stack does — and `.agentspace/rosetta-extensions 43049308`, byte-identical for
  these files): `demo-stack/ant-academy.sh` (717 lines) wires the **five** patch vars at `:56`,
  `:66`, `:75`, `:84`, `:90`, and `:576-583` is the M220 comment stating the bypass is gone;
  `demo-stack/cockpit.py:327`, `:812`, `:1496` are the three `e2e_persona` sites;
  `test_the_e2e_persona_bypass_is_gone_from_the_launch_env` and
  `test_e2e_persona_bypass_is_not_in_the_launch_env` both exist; all five named patch directories
  exist under `demo-stack/patches/`; `stack-verify/live/autoverify.sh:459` is assert *(f)* — *"THE
  ACADEMY RENDERS ITS CATALOG"*.
- Terraform, each at the ref the claim names: `roadrunner/terraform/main.tf:19` `= 1`, blamed to
  `84a4b4f1` (2025-12-15), with `87d8d443` touching only `.github/workflows/bump-version.yml`
  (3 insertions) and `e45eb61` (2026-05-27) a 1-line `terraform/main.tf` change;
  `graphql-wundergraph/terraform/main.tf:20` `= 1` with `private_subnets_ids` at `:31` and **no**
  public subnet named, `locals.tf:8 port = 8080`; `cms/terraform/main.tf:39` `= 0` at both
  `ca50c817` and `origin/main f38c0c4a`, with `6efa1d5` deleting `build-production.yml` under
  *"the cms ECR repository is decommissioned (M810)"*; `jobsimulation/terraform/main.tf` at
  `6092c6d2`/`82cb66ec` has the M810 comment at `:15-22` and the surviving-ownership block at
  `:24-40`, with `service_desired_count` absent — and `origin/main` carries **four** 2026-08-04
  commits including PR #439, exactly as `architecture_overview.md:20` says.
- `storage/terraform/main.tf` @ `9f8cb532` is **18** lines (`:9-11` the "GONE" comment, `:13-16`
  the prevent_destroy rationale, `:18` *"See outputs.tf…"*), `storage.tf:22-25` the superseded-M903
  note, **0** occurrences of *custody*, **0** `moved` blocks; the checked-out `4ce8ece5` is **20**
  behind, its `main.tf` is **100** lines with the ECS module and its `storage.tf` header still says
  *"relocate the assets out of this module BEFORE M907"*; `d3e6d32` (2026-08-05) contains
  *"M903 never ran."*; `63bffc8` reads `service_desired_count = 0`.
- `app` storage anchors: `main.go:504` comment, `:516/:517` env reads, `:518-523` the fatal,
  `:524/:525` the two managers, `:529-535` `verifyBucketAccess`, `:547` `resource.NewManager`,
  `:1102` `cmsStorage`, `env_guards.go:37-44` `deployedEnvironment()` (returns **false** for
  `development`), `internal/storage/service.go:22/:24` the env-var NAME constants,
  `internal/storage/storage.go:193-200` `getKeyPath` @ `2035f9a`, `main.go:1048` @ `9d00a313`,
  `recording.go:12` and `anticheat/anticheat.go:30` the storage-SDK imports. Platform
  `README.md:81-87` @ `0c91421` is the AWS-credentials instruction.
- `915da06` (2026-07-29, in **graphql-wundergraph**, not platform): its subject says
  *"supergraph 2→1"* while its body records *"remove the jobsimulation subgraph (supergraph 3->2)"* —
  the corpus's **3 → 1** and *"its own commit subject's '2→1' is wrong"* are both correct.
- `next-web-app 8297c684`: `next: "~16.2.12"` — a **tilde** — in exactly `apps/web`, `apps/hiring`,
  `apps/integration`, `apps/maintenance`; `apps/mobile` declares none; lockfile resolves `16.2.12`.
- `app` misc: `internal/converter/gotenberg.go:31` POSTs `gotenbergURL+"/forms/libreoffice/convert"`
  over plain `http`; `internal/jobsimwiring/wiring.go:118` @ `b948604` is
  `jsrunner.NewRunnerManager(…JUDGE0_BASE_URL)`; `internal/jobsimulation/ai/ai.go` `:85-88`
  `eu-west-1`, `:129` `isThrottlingError`, `:166`/`:325` its applications, `:262-276` the
  `azureClientEu`→`azureClientUs` swap on `flag_use_azure_us` (`:267`);
  `internal/cms/studio/markdownManager.go:29-30` `mistralocr.New(aiKey)` and
  `studioManager.go:583` `os.Getenv("MISTRAL_API_KEY")`; `DIRECTUS_WEBHOOK_SECRET` +
  `/api/webhook/directus` (`internal/web/backend/backend.go:138`, `:324`); the **23** jobsim
  entities corroborated by `app/knowledge/jobsimulation-domain.md:8,11,78-79`;
  **43** `.graphqls` files with `internal/web/backend/graphql/graph/schemas/academy.graphqls`
  among them (the doc's "1 of 43"); `cmd/academy-seed/` exists;
  `academy_chapter_progresses` / `academy_last_activities` are the real plural table names.
- Intra-corpus anchors that DO resolve: `dependency_map.md:22 → service_taxonomy.md:101-103`
  (the customerio-sync/asynq/OFF passage); `dependency_map.md:90 → :9/:15/:31`;
  `architecture_overview.md:296 → external_services.md:579` (the retraction) and
  `:299 → external_services.md:602-607` (the four-live-plus-one-latent enumeration). All six
  markdown `#fragment` targets used by my files resolve: `#taxonomy-figures` (an explicit
  `<a id>` at `shared_libraries.md:228`), `#routing-what-is-actually-implemented`,
  `#layer-1-database`, `#directus-headless-cms`, `#profiles`, `#external-service-integration`,
  `#the-content-model--db-authoritative-catalog-v051-m7`.
- `storage.md` really does contain **none** of `depends_on`, `DB_CONNECTION`, `REDIS_ADDR`,
  `go.mod`, `redis`, `postgres` at any line (case-insensitive; positive control `bucket` → 21), so
  `dependency_map.md:19`'s withdrawal of that attribution is itself correct.

**RE-DERIVED** (weaker — the document's own arithmetic recomputed, flagged as such):

- `dependency_map.md:19`'s *"seven variables"* — I re-derived the count **and** enumerated the set
  by name from `0dab54d:docker-compose.yml:117-123`; both give 7, and `:116` is indeed the
  `environment:` key. Counting this as ENUMERATED on the set, RE-DERIVED on the sum.
- `catalog.json` *"~2,667 entries"* — the real figure is **2,692** courses (parsed, not estimated).
  Within the `~` hedge at 0.9 %; not booked.

## What I could not settle

- **GitHub repository archive state** — `architecture_overview.md:3` (*"the repo is ARCHIVED on
  GitHub (2026-07-30)"* for graphql-wundergraph), `dependency_map.md:23`, `roadrunner.md:48`
  (skiller 2026-07-01, skillpath 2026-07-31), `roadrunner.md:53` (roadrunner *"NOT archived"*),
  `roadrunner.md:67` (chronos *"last push 2026-04-23"*). None is derivable from a clone. What I
  *can* say is that the one falsifiable consistency check passes: graphql-wundergraph's newest
  commit is `60c229f3`, 2026-07-30, so a 2026-07-30 archive is not contradicted the way
  jobsimulation's was. Settling these needs `gh api repos/anthropos-work/<name> --jq .archived`.
- **`architecture_overview.md:13` / `:236` — "≥42,790 skills / ≥22,470 job roles"** — a production
  DB measurement; no clone can settle it. Would need `/db-query` against the public taxonomy.
- **`ant-academy.md:90` — "a grid of 65 real cards"** and `:222`/`:290` "Proven live on `billion`" —
  live-stack observations, not statically checkable from the clone set.
- **`storage.md:92` — the defect-register citation** points into `knowledge/plan/`, which HARD BAR 1
  forbids me to open. Unverified by construction.
- **`storage.md:25` — the M903 plan's front-matter** (`app/knowledge/plan/releases/09.00-support-in-app/
  m903-s3-custody/`) is inside `app`'s own tree rather than rosetta's, so it is not covered by the
  hard bar — but I did not open it, having settled the surrounding claim from
  `storage/terraform/storage.tf:22-25` and `d3e6d32` directly. The `status: planned` /
  *"Planned, not started"* quotations remain unchecked.
- **`ant-academy.md:271` — "7 `role="menuitemradio"` options"** — I confirmed the `role` attribute at
  `LanguageSelector.jsx:97` and that `SUPPORTED_LOCALES` has 7 members, but did not trace the
  `LANGUAGES` array the `.map()` at `:92` iterates; if `LANGUAGES` and `SUPPORTED_LOCALES` ever
  diverge the count would too.
- **`storage.md:52` — "The repo's own CLAUDE.md documents different binary-level defaults"** is
  ambiguous rather than false: `storage/CLAUDE.md:12` gives *"8300 HTTP / 8301 RPC (local dev);
  8080 / 8081 (ECS)"* and its table at `:130-131` gives 8080/8081, which **matches** the binary
  (`cmd/root.go:45-46`). Read as "different from the 8300/8301 compose pair" the sentence is true;
  read as "the repo's doc disagrees with the binary" it is false. Not booked, but a sentence that
  needs a disambiguating read is worth a rewrite.
