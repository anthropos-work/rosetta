**Type:** tik · `iter_shape: reading` · **`TOK-08`'s GRADING READING — the USER's re-scope, graded.**

# iter-119 — `P = 22`. `TOK-08` is refuted, and the reading that refutes it also refutes the series it is measured against.

The full graded sheet is [`adjudication.md`](adjudication.md). This file is the close.

## The number, and the branch it fires

> ## `P = 22` · `N = 28` — the pre-registered **`P ≥ 19`** branch FIRES.
>
> **`TOK-08` — enumerate the mechanical classes exhaustively instead of sampling them — is REFUTED by its
> own arithmetic**, sealed by the user at `577446b` before a single sweep line was written, and restated
> unchanged in this iter's first commit `4d4530d` before the first seat was dealt.
>
> Per that rule: **STOP. No successor strategy is authored. There is no `TOK-09`.** The milestone goes
> back to the user for a **scope decision**.

## And the fall from 37 is NOT progress — the same reading proves it

`P` fell **37 → 22 (−40.5 %)**, just short of the 50 % the working branch needed. **That drop is
sampling, not drainage**, and this reading was designed to be able to tell the difference:

| | |
|---|---|
| corpus change between the two readings | **5 lines, in place, zero net** |
| iter-116 predicates **repaired** in between | **0** |
| iter-116's 37 **re-found** here (blind, on predicate) | **13 (35.1 %)** |
| predicates found here that iter-116 **never saw** | **9** |
| **union floor** | **≥ 46** |

> **`P` fell 37 → 22 while the measured floor ROSE 37 → ≥ 46.**

**This is the milestone's first test-retest measurement**, possible only because iter-119 is its first
true seat-level replicate — identical partition (reproduced at both refs as a control), identical clone
shas for a fourth consecutive reading, a corpus five in-place lines away. It was pre-registered as band #3
**with the low-overlap branch named in advance** as the outcome that *"would bear on the refutation more
than the primary itself does."* It did.

**Every `P` this milestone published — 22, 22, 24, 37, 22 — samples roughly a third of a standing pool.**
The series was read for weeks as though its movements described the corpus. Most of the movement is the
instrument. Two independent passes here recalled **63.6 %** and **77.3 %** *against their own union*, and
the union recovered **35.1 %** of a prior panel's adjudicated set.

## What `TOK-08` DID achieve — stated, because refuted is not worthless

- **Class 1 censused and closed:** 1,520 intra-corpus citations over 92 docs, 8 false, 8 repaired, fence
  green over **100 %** of the enumerated population, ratchet at zero.
- **Class 2 censused:** 1,070 candidates, 0 false, reach **675/1,070 = 63.1 %** over a denominator that
  now names its provenance after 119 non-citations were excluded, counted and named.
- **8 defects closed that four graded readings had missed** — all in the `#anchor` arm no reader
  exercises.

**And it still could not move the gate, because it was closing the wrong half of its own class.**

## Band #7 — the failure that IS the finding, and it was predicted in writing

**Intra-corpus citation defects: predicted ≤ 6, measured 8 (36 % of the pool, up from 27 % at
iter-116).** `D-M257x-117-2` said this at iter-117's close, before either census finished:

> *"the machine-reachable half of class 1 is largely DISJOINT from the half the readings book… of 387
> bare-pin lines only 4 are machine-resolvable. Little `P` movement should be expected."*

**The census closed the RESOLUTION half at 100 % reach. The CONSTRUCT half grew as a share of the pool.**
A machine can prove `cms.md:216`'s link resolves to a file that exists; it cannot prove the lines it names
hold the **Data** bullet rather than the **Domain** bullet. **Five of the eight were booked at iter-116
and are still false.**

## Graded by consequence, not by class

**`clerk-integration.md:40` — *"Clerk sign-in tokens are minted ONLY for app-native admin
impersonation."* There are two other live minting sites.** A reader auditing where privileged
impersonation tokens can be created is sent to one site when there are three. **Booked and upheld at
iter-116 as well; still in the corpus.** Second consecutive reading as a known, open, unrepaired
security-surface understatement. Reported first among individual findings for that reason.

## The 16 "small class" verdicts — the audit the user asked for

**iter-113 verdicted 1 `SMALL-CLASS-PROVEN` (P20) and 15 `SMALL-CLASS-ADJUDICATED`** (254 candidate
exclusions read by one agent — `FIX-M257x-iter113-adjudication-is-judgement`).

- **The sweep converted NONE of the 16 from judged to proven.** The censuses enumerate *citations*; a
  small-class verdict is a claim about a *ceiling*. **Neither census measures a ceiling**, so neither can
  promote a judged verdict. **P20 remains the only proven one, and it was proven at iter-113 by its own
  zero-headroom ceiling — not by `TOK-08`.**
- **This reading re-books 2 of the 15 judged (P23 Ant Academy, P08 the `⚠⚠ M51` anchor — its fifth
  generation) and 0 of the 1 proven.** Measured error rate of the judged verdicts: **≥ 13.3 %**. P23's
  exclusion list dismissed 12 candidates as publishing a different proposition; at least four publish
  exactly this one.

## Bands: 13 HELD of 15 — and that is about the coordinator, not the corpus

Series: 4/9 · 3/7 · 5/9 · 4/10 · 7/13 · 7/14 · **13/15**. All four magnitude bands held for the first
time — because they were cut on an honest centre (~34) that the refutation branch does not contain.
**Failures: #7 (intra-corpus citations, 8 > 6) and #8 (platform-drift share, 63.6 % > 60 %, narrowly, with
the classification boundary disclosed and the alternative counting shown).** Full table in
`adjudication.md`.

## Close — 2026-08-07

**Outcome:** **`P = 22` / `N = 28`. `TOK-08`'s pre-registered `P ≥ 19` branch FIRES — enumeration-first is
REFUTED**, and per the user's own rule **no successor strategy is authored**. The milestone returns to the
user for a scope decision. The reading separates *why*, and that separation is the deliverable: the
40.5 % fall in `P` is **sampling, not drainage** — **band #3 measured only 13 of iter-116's 37 re-found
(35.1 %) over a corpus 5 in-place lines away with 0 of those 37 repaired, plus 9 net-new, so the floor
ROSE to ≥ 46 while `P` fell.** This is the milestone's **first test-retest measurement**, taken on its
**first true seat-level replicate**, and it says every published `P` (22, 22, 24, 37, 22) samples about a
third of a standing pool. `TOK-08` did close class 1 at 100 % reach and class 2 at 63.1 %, fixing 8
defects four readings had missed — but band #7 **failed as predicted** (8 > 6; the construct half grew
27 % → 36 % of the pool), confirming `D-M257x-117-2`: **the census and the reader work opposite halves of
one class.** 14/14 seats, 0 lost, committed verbatim before adjudication; 4/4 verdicts unedited; 36 booked
→ 33 upheld / 3 rejected, **all ref-discipline, 0 wrong-tree**. All 14 clones identical at close and open.
**No repair taken inside the pass.**
**Type:** tik
**Status:** closed-fixed
**Gate:** NOT MET
**Phase 5 grading:** (1) gate-met: n — (2) triggered-tok: n (**a tok is FORBIDDEN here by the user's
binding instruction; the refutation branch explicitly bars a successor strategy**) — (3) re-scope: **y —
`TOK-08`'s own pre-registered stop condition fired at `P = 22 ≥ 19`; the rule routes the milestone to a
user scope decision** — (4) user-blocker: n — (5) cap-reached: n (1 tik this session) — (6) protocol-stop:
n — (7) budget-exhausted: n — Outcome: **exit-3**
**Decisions:** `D-M257x-119-1` (the fall in `P` is sampling, not drainage — and the floor rose while `P`
fell) · `D-M257x-119-2` (`P` is a weak floor: 35.1 % test-retest recall, ±2 on adjudicator granularity) ·
`D-M257x-119-3` (the census and the reader work opposite halves of the citation class — `D-M257x-117-2`
confirmed by measurement) · `D-M257x-119-4` (the sweep cannot promote a small-class verdict: a census
measures citations, a small-class verdict claims a ceiling)
**Side-deliverables:** none. This iter landed zero corpus edits and zero rext code by construction.
**Routes carried forward:**
- **THE MILESTONE NEEDS A USER SCOPE DECISION.** Both `TOK-07` (repair-and-read) and `TOK-08`
  (enumerate-then-read) have now been refuted by their own pre-registered arithmetic. **No ninth strategy
  is authored.**
- **`FIX-M257x-iter119-clerk-signin-token-claim-understates-surface`** → net-new, **security-surface**,
  open for a second reading. `clerk-integration.md:40`.
- **`FIX-M257x-iter119-instrument-recall-is-35pct`** → net-new. The reading instrument recovers ~1/3 of a
  known standing pool per pass. Any future gate design must account for it.
- **`FIX-M257x-iter113-adjudication-is-judgement`** → open, now with a **measured error rate (≥ 2 of 15)**
  and the finding that neither census can promote a judged verdict.
- **`FIX-M257x-iter116-intra-corpus-miscitation-is-the-largest-class`** → resolution half closed
  (iter-117); **construct half open and GROWING as a share (27 % → 36 %)**.
- `FIX-M257x-iter116-predicate-guard-takes-first-pin-in-block` → open, unfixed through two readings
- `FIX-M257x-iter118-bare-basename-needs-repo-disambiguation` · `FIX-M257x-iter116-induction-fences-do-not-scale` ·
  `FIX-M257x-iter115-anchor-guard-resolves-fixture-paths-live` · `FIX-M257x-iter111-staged-battery-dependency-is-underived` ·
  `FIX-M257x-iter111-buildbench-parse-json-is-a-noop-flag` · `FIX-M257x-iter108-stackcore-suite-hangs` ·
  `FIX-M257x-iter107-drift-fence-satisfiable-by-prose` (de-ranked) · `DEF-M257x-iter101-briefing-rext-tree`
  → all open
**Lessons:**
1. **A metric that has never been measured twice on an unchanged subject is not a metric yet.** Five
   readings of `P` were used to steer this milestone before anyone asked what a repeat reading returns.
   The answer — **35.1 %** — reinterprets all five. **Build the replicate into the series early, not at
   the point where the series is about to decide something.**
2. **A pre-registered threshold protects you exactly when the number is ambiguous.** `P = 22` is a 40.5 %
   fall and would have been trivially narratable as *"nearly working, one more sweep."* The rule was cut
   at 50 % by the user before any of it was known, and the band-#3 evidence shows the generous reading
   would have been **wrong on the facts**, not merely optimistic.
3. **A census closes the half of a class a machine can see, and can make the other half a LARGER share of
   what remains.** Predicted at iter-117, confirmed at 27 % → 36 %. **Say which half your fence reaches,
   before you claim the class.**
4. **Grading a strategy and grading its parts are different jobs.** `TOK-08` is refuted *and* it closed
   1,520 citations at 100 % reach and found 8 defects four readings had missed. Both go in the close; the
   refutation does not retroactively make the sweep worthless, and the sweep does not soften the
   refutation.
