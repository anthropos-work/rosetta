---
iter: 128
milestone: M257x
iteration_type: tik
status: in-progress
opened: 2026-08-07
---

# iter-128 — the three diseases, treated separately

**Run 81, tik 1.** Under **the user's directed scope of run 81**, which named three priorities in
consequence order and this iter follows that order.

## Active strategy reference

No `TOK-09` (`TOK-08`'s sealed rule **bars a successor**). `F4` binds: nothing here is `P`, is `N`, or is
a clause-5 verdict — **the census and the triage are INSTRUMENTS, not the grader.**

## Cluster / target identified

Three, and the run's framing is that they are **three different diseases with three different
treatments**, only one of which is urgent:

1. **`state.md` is 21 bytes over a 15,360 cap** and will block `close-milestone`. Fix it the way it has
   been fixed before — **move content to its owner, never trim prose**.
2. **Priority 1 (urgent): the ~11 false claims in the consequence class.** iter-124 printed `fix = 4` and
   recall-corrected it to ≈ 11. These are, by construction, sentences touching security, data-handling,
   identity, tenancy, residency, backup or access. Find them all, **repair by predicate with full
   enumeration**, check pair-halves, and **grade by consequence, not by class**.
3. **Priority 2: the 820 untriaged tier-2 members** — named *untriaged, not implied clean* at run 80.
   Triage on the same four fates with the **committed** predicate so the split stays reproducible, and
   **correct the printed numbers the way iter-124 corrected its own.**
4. **Priority 3: a clean whole-suite attestation** — run 80 declined to quote one under ~8× external
   contention. Take it on a quiet machine, or declare it unavailable with the reason.

## Pre-registered expectations

- The `cite` share of the 820 will be **high again** — but it is to be **reported, not assumed**, and
  R4's accuracy is to be **re-measured on this population** rather than imported from C1's.
- `fix` over the complement will read 0. **That is a floor of unknown height, not a measurement**, and
  must be published as such.

## Constraints

Zero platform-repo edits. `stack-demo/**` is **not to be touched** — `demo-1` is up and green — so every
clone is read at a ref via `git show`/`git grep`, never a working tree (five of thirteen are behind their
own origin). Clause 5 is **not** re-cut, reinterpreted, narrowed or argued. `rosetta-extensions` stays on
`main`; no tag is cut.
