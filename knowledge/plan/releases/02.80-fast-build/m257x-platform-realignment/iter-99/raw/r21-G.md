# Seat G — reading #21

Refs used (per ADDENDUM, re-verified at this seat's open):
`platform` `0c91421d` (level with origin/main) · `app` `b948604f` (v1.366.0; `origin/main` = `2035f9a40`, CHANGELOG top `v1.369.0`) ·
`next-web-app` `bb3313bc` · `studio-desk` `14a5442a` · `graphql-wundergraph` `60c229f3` · `ant-academy` `9c3843cd` ·
`rosetta-extensions` authoring copy on `main`. Named-ref reads used `git show <sha>:<path>` (`9d00a313`, `5ba17044`,
`0dab54d`, `2adcf71`, `d11a403`, `838d907`, `749dc86~1`, `749dc86`, `7c17e63`, `915da06`).

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/services/studio-desk.md` | 435 | yes |
| `corpus/services/hiring.md` | 434 | yes |
| `corpus/services/graphql-wundergraph.md` | 280 | yes |
| `corpus/services/academy-backend.md` | 149 | yes |
| `corpus/services/next-web-app.md` | 133 | yes |

(Also read, as the briefing requires: `corpus/ops/platform-alignment.md` §5, lines 249–948 of 2232.)

Search-instrument positive controls run in-pass:
- `find apps -type d -name "@tabs"` returned 3 hits before I accepted `find apps -type d \( -name '(.)*' … \)` returning
  0 (the "zero intercepting routes" claim).
- `git grep -c "DIRECTUS"` over `next-web-app` returned nothing while `git grep -c "METABASE"` in the same pass
  returned 3 files — so the DIRECTUS zero is a measurement, not a broken pipeline.
- `grep -c "^type Subscription" schemas/backend.graphqls` = 0 next to `grep -c "^type Query"` = 1 and
  `"^type Mutation"` = 1.
- Two shell failures were caught and re-run rather than read as evidence: `--include=*.go` was glob-expanded by zsh
  (re-run quoted), and `git show $r:file` was mangled by zsh `${r:…}` substitution (re-run with `"${r}:file"`). Both
  had produced empty/0 output that would have read exactly like absence.

Trees covered by my numbers: `stack-demo/{app,platform,next-web-app,studio-desk,graphql-wundergraph}` and
`.agentspace/rosetta-extensions`. I did **not** need the nested `stack-demo/app/studio` checkout for any claim in my
file set (no assigned claim cites a `studio/` path); `git -C stack-demo/app status` shows it as `?? studio/`, i.e. its
own untracked checkout, exactly as the ADDENDUM warns.

---

## Blockers

### B1 — `next-web-app.md` states the recruiter scoreboard lives in the dockerized `apps/web` — the premise `hiring.md` records as REFUTED

- **Anchor:** `corpus/services/next-web-app.md:32` — *"**NB the demo's recruiter candidate-comparison scoreboard is
  an `is_hiring` ORG-TYPE surface in the dockerized `apps/web`** (`/enterprise/activity-dashboard`), **not** this
  Vercel-only app"*.
- **Why it is false:** this is M222's premise, and `hiring.md` — the doc `next-web-app.md:32` itself points the reader
  at — records that M224's live render **falsified** it, in three separate places:
  - `hiring.md:53-58`: *"M222's dockerized-`apps/web` (`/enterprise/activity-dashboard`) premise was **falsified at
    M224**: a user whose Clerk memberships are **all** hiring orgs is **ejected out of `apps/web`** … so *"the org
    genuinely reads as hiring"* and *"the scoreboard is reachable in `apps/web`"* are **mutually exclusive**."*
  - `hiring.md:359-364`: same, plus *"⇒ … mutually exclusive on the real platform."*
  - `hiring.md:423-424`: *"⚠️ M222 inferred the scoreboard was reachable in `apps/web`; M224 rendering proved it is
    not for a *genuine* hiring org."*

  Ground truth confirms the refutation, not the surviving claim:
  `next-web-app/apps/web/src/context/UserStatusContext.tsx:141-173` computes `userHasAllHiringOrgs` from
  `membership.organization.publicMetadata.isHiring` (`:144-149`) and unconditionally sets
  `window.location.href = buildSwitchHandoffUrl({ targetProduct: 'hiring', … })` (`:169-172`); and
  `apps/web/src/hooks/useGetClerkOrganization.tsx:16-18` filters hiring orgs out of the workforce list. The symmetric
  guard in `apps/hiring/src/context/UserStatusContext.tsx:125,144-145` keeps her in the hiring app. All four anchors
  opened and read at `next-web-app` `bb3313bc`.

  This is rule 19's cross-file drift exactly: the claim was corrected in the file whose owner held it and left standing
  in the twin. **Corroborating second site in the same file:** `next-web-app.md:93-94` — *"Hiring / integration /
  maintenance / mobile are **not** containerized — they ship via Vercel only"* — while `hiring.md:366-374` records the
  demo building `apps/hiring` from the untouched clone *"as a second offset-port UI container (same recipe as
  `apps/web` + `studio-desk`)"*. (The `Dockerized?` column at `:32`/`:34` is defensible if read as *"in platform
  compose"*; the bolded NB at `:32` is not defensible under any reading.)
- **Confidence:** high.

### B2 — `hiring.md` cites `manager.go:485` as a `"organization is not hiring"` hard-error; at the checkout that line is a closing brace of a different function

- **Anchor:** `corpus/services/hiring.md:80-82` — *"as do `organization/manager.go:448` (a forced Clerk membership is
  created with role `candidate` instead of `member`) and `:485` + `siminvitationlink.go:62` (both **hard-error
  `"organization is not hiring"`** …)"*.
- **Why it is false:** at the checkout `app` `b948604f`:
  - `internal/organization/manager.go:485` is `}` — the closing brace of `forceUserToOrganizationInClerk` (declared at
    `:446`). The only `"organization is not hiring"` sites in that file are `:536` (`m.logger.Error`) and `:537`
    (`return fmt.Errorf`), guarded at `:535` (`if !org.IsHiring {`), inside `ForceUserToOrganization` (declared `:527`).
    `grep -rn "organization is not hiring" internal/` returns exactly `siminvitationlink.go:63`,
    `manager.go:455` (a comment), `manager.go:536`, `:537`, and two test lines.
  - `manager.go:448` is a **blank line**. `grep -n "IsHiring" internal/organization/manager.go` returns `450` and
    `535`; the candidate branch is `switch org.IsHiring {` at `:450` → `antRole = enum.RoleCandidate` at `:452`.
  - The third citation in the same sentence, `siminvitationlink.go:62`, *is* correct (`if !org.IsHiring {`, error at
    `:63`).
- **Refs I tried, and the hesitation:** both anchors resolve correctly at `app` `5ba17044` — the ref named in this
  doc's top re-grounding banner (`hiring.md:17`): there `:485` is `if !org.IsHiring {` and `:448` is `case true:` of the
  candidate switch. Under §5 rule 33 / the briefing's addendum, **a pin's scope is the claim's own block**, and this
  claim block (the numbered list item under *The org-type gate*, ~60 lines below the banner) names no ref — so it grades
  at the checkout. What decided it for me is that **every other `app` anchor in this document resolves at the
  checkout** — I opened and confirmed `intelligence.go:1700`, `:1820`, `:1844`, `:1846`, `:885-886`,
  `resolver_queries.go:1034/1035/1053/1080/1085/1089`, `resolver_cms_queries.go:95/99-103/210/258/295`,
  `job_simulation_session.go:39/45`, `enum/jobsimulation.go:29-35/37-43`, `ent/jobsimulationsession.go:181-186`,
  `graph.go:129546-129554/129392-129400`, `jobsimulations.graphqls:14/128`, `anticheat_result.go:24`,
  `askengine/registry.go:192`, `atlas.hcl:8`, and five migration anchors. So these two are not "pinned to an older
  date"; they are the pair the re-anchoring pass missed, and a reader will take them as current like all the others.
- **Confidence:** medium-high (high that the anchors are wrong at the checkout; medium on the grading, given the
  banner ref).

### B3 — `hiring.md` cites `service_taxonomy.md:52` as a corroborating twin; that line is about a different subject entirely

- **Anchor:** `corpus/services/hiring.md:38` — *"— as the twins [`service_taxonomy.md:52`](../architecture/service_taxonomy.md)
  and [`dependency_map.md:78`](../architecture/dependency_map.md) already said."* (supporting *"no local dev/demo stack
  creates the `jobsimulation` schema at all"*).
- **Why it is false:** `corpus/architecture/service_taxonomy.md:52` reads
  *"[`dependency_map.md`](./dependency_map.md)'s content-generation flow, which had it right all along."* — the tail of
  a blockquote about `app/internal/cms/studio/studioManager.go:119` exec'ing `studio/gen.py`. It says nothing about
  schemas, `jobsimulation`, or local stacks. The twin the sentence means is `service_taxonomy.md:62`
  (*"the `cms`, `jobsimulation` and `skillpath` schemas are legacy husks"*), with `:139` as a second candidate.
  The paired citation `dependency_map.md:78` **is** correct — it reads *"…or directly to the **`public`** schema (the
  legacy `jobsimulation` schema is non-authoritative)."* — which makes the miss a single-site drift, rule 34's exact
  class (an intra-corpus `<doc>.md:N` invalidated by an edit above it, landing on content and therefore reading as
  correct).
- **Confidence:** high.

### B4 — `graphql-wundergraph.md` self-cites `:84` for a claim that lives at `:114-117`

- **Anchor:** `corpus/services/graphql-wundergraph.md:134` — *"So for its **final five months** compose built
  `Dockerfile.dev` — which is why a subgraph SDL change rebuilt the router, as `:84` describes."*
- **Why it is false:** `graphql-wundergraph.md:84` is the **Ports** bullet — *"**Ports**: **8080 → 8080** (router
  `listen_addr 0.0.0.0:8080`, `graphql_path /graphql`). **There is no `5050` at platform HEAD**…"*. It describes ports
  and the absent `5050`, not rebuild-on-SDL-change. The construct actually described is at `:114-117`
  (*"Adding/changing a subgraph **or a single field** requires re-running `wgc compose` and **rebuilding +
  restarting**…"* / *"It *used to* rebuild whenever any subgraph schema changed, because the build context is the
  parent dir (`..`)…"*). This same document already carries two recorded self-citation drifts (`:86-88` notes a prior
  `:174-176` mis-cite corrected at iter-98), so the class is live in this file.
- **Confidence:** high on the anchor being wrong; I hesitated on BLOCKER-vs-MINOR because the consequence is a
  confused cross-reference rather than a false technical fact — booked BLOCKER per the instrument's explicit
  "wrong line anchors that point at the wrong construct" clause and its resolve-upward rule.

---

## Minors

- `graphql-wundergraph.md:176-178` — the historic-drift pairs mix two different constructs. Current pair
  `:135`/`:160` is runtime-env/runtime-env (verified). The stated historic pairs are not: at `0dab54d` the
  `NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT` **runtime** line is `:245` and `:236` is the **build arg**; at `2adcf71` the
  runtime line is `:361` and `:352` is the build arg. (`:220`/`:334` for studio-desk are correct runtime lines.)
  Enumerated with `git show <ref>:docker-compose.yml | grep -n`. Non-material because the sentence's own point is
  *"grade the construct, not the offset"* — and `next-web-app.md:48` uses `:236` correctly, as the build arg.
- `academy-backend.md:15` — *"(Consistent with :80-83 below.)"* — `:80` is blank and `:81-83` is the *How to find the
  API / Primary — GraphQL* bullet; the sentence about the endpoint name outliving the router is at `:84-85`.
- `academy-backend.md:117-127` — the **cmd binaries** list omits `cmd/academy-asset-upload`, which exists at the
  checkout (`ls cmd/ | grep -i academy` → `academy-asset-upload`, `academy-seed`, `academyImport`). Omitted list member.
- `graphql-wundergraph.md:105` — the key-files box lists `.github/workflows/` as *"release.yml …,
  supergraph-update.yml"*; `bump-version.yml` also exists. Omitted list member.
- `studio-desk.md:112` — *"Four model tiers … Tier defaults: OpenAI/Azure `gpt-5.2` / `gpt-5-mini` / `gpt-5-nano`"*
  lists three names for four tiers. Correct as a set of **distinct** values (`config.ts:20-31`: `thinking_slow` and
  `thinking_fast` both `gpt-5.2`; Anthropic `thinking_fast` and `fast` both `claude-sonnet-4-5`), but the shorthand
  can be read as a tier-ordered list.

---

## Positively cleared (audited zeros)

**ENUMERATED** (predicate re-derived from source independently, cardinality stated before any arithmetic):

- **The compose profile set = 5.** `grep -n "profiles:" docker-compose.yml common.yml` over the **resolved** include
  set (compose's `include: [common.yml]`) yields exactly four `profiles:` keys →
  `{core, backend, all, studio-desk, frontend}`, |5|. `graphql-wundergraph.md:15` says five and names them; correct.
  Its *"read 'the eight' until `838d907`"* also holds: `838d907` (2026-08-05) deletes `storage`, `messenger`,
  `customerio-sync` with their `storage-legacy`/`messenger`/`customerio-sync` profiles → 5+3 = 8.
- **The always-on floor = 3.** Enumerated as *services declaring no `profiles:` key* across BOTH files:
  `postgresql` + `redis` (`common.yml`, no key) and `sentinel` (`docker-compose.yml`, no key). Confirms
  `graphql-wundergraph.md:18-19` / `:190-192` ("exits 0 and starts only the always-on floor"), and is the resolved-
  include case §5 rule 30 warns about (a parser reading only `docker-compose.yml` would compute `{sentinel}`).
- **The subgraph ladder 5 → 4 → 3 → 1.** `git show '<ref>:supergraph-config-prod.yaml' | grep '^  - name:'` at
  `749dc86~1` (backend, skiller, jobsimulation, cms, skillpath = 5), `749dc86` (4), `7c17e63` (3), `915da06` (1).
  Dates match (`749dc86` 2026-06-24, `7c17e63` 2026-07-21, `915da06` 2026-07-29). `git show --name-status 915da06`
  marks **both** `schemas/cms.graphqls` and `schemas/jobsimulation.graphqls` `D`, and its own subject line does read
  *"supergraph 2→1"*. So `graphql-wundergraph.md:41-53` and `:157-164` are correct including the "its own subject line
  is wrong" claim. Present state: `supergraph-config-prod.yaml` lists `backend` alone; `schemas/` = `backend.graphqls`
  alone; `subgraphs.conf` = a single `BACKEND=v1.360.0`.
- **`repos.yml` entry counts 6 → 4.** `git show 0dab54d:repos.yml` enumerates app, sentinel, storage, messenger,
  next-web-app, studio-desk = 6; the checkout `0c91421` enumerates app, sentinel, next-web-app, studio-desk = 4,
  `838d907` having removed storage+messenger. `app` at `repos.yml:14-17` is the only `migrations: true`. Confirms
  `hiring.md:35`, `:179-180`, `:329`, `:36`.
- **`SET "score"` = 0 across the whole migration set** (`grep -rn 'SET "score"' terraform/migrations/ | wc -l` = 0) —
  the no-back-fill claim at `hiring.md:24-25` and `:172-173`.
- **The four required-and-undefaulted columns on `job_simulation_sessions`.** I enumerated every `NOT NULL` column in
  `20260722104506.sql:2-27` and partitioned by presence of `DEFAULT`: undefaulted = `owner_id` `:6`, `sim_id` `:7`,
  `sim_type` `:10`, `token` `:13` (|4|); every other `NOT NULL` (`id`, `created_at`, `updated_at`, `status`,
  `completion_status`, `interactions_progress`, `score`, `validation_version`) carries one. `token` UNIQUE at `:29`.
  Exactly what `hiring.md:232-234` claims.
- **The `SessionCompletionStatus` enum is closed at 5 lowercase members** — `enum/jobsimulation.go:29-35` (const block)
  and `:37-43` (`Values()`), both anchors exact; SDL declares the same five at `jobsimulations.graphqls:14` and `:128`.
  I also verified the **predicate**, not just the count: the Ent `assignValues` cast at
  `ent/jobsimulationsession.go:181-186` is unconditional (`_m.CompletionStatus = enum.SessionCompletionStatus(value.String)`)
  and the two gqlgen marshalers (`graph.go:129546-129554`, `:129392-129400`) are bare
  `graphql.MarshalString(string(v))` with no membership check — so `hiring.md:239-251`'s *"nothing rejects it"* is
  right, and its own retraction of the earlier "the row vanishes" wording is right.
- **The academy table set.** `grep "CREATE TABLE" terraform/migrations/*.sql` for `academy_` yields exactly the 11
  plural names `academy-backend.md:62-74` lists (plus the legacy `aiacademy_courses` it correctly calls legacy).
  DDL spot-checks confirm `academy_skill_paths.paid boolean NOT NULL DEFAULT true`, nullable `tenant_eid`,
  `lifecycle`, and `CREATE UNIQUE INDEX … ON academy_chapter_progresses (user_id, chapter_slug)`.
- **`STORAGE_RPC_ADDR` at the pinned ref `9d00a313` (v1.367.0) = 3 occurrences, all comments.**
  `git grep -n "STORAGE_RPC_ADDR" 9d00a313 -- '*.go'` → `internal/jobsimwiring/wiring.go:101`,
  `internal/storagens/callsites_test.go:189`, `main.go:451` — every one a `//` comment, zero reads. Exactly
  `academy-backend.md:57-60`. `main.go:471-472` at that ref constructs `NewManager` / `NewPublicManager`.
- **The certificate alphabet is 30 symbols.** `certIDRegex = ^AAS-\d{4}-[A-HJKMNP-TV-Z2-9]{6}$`
  (`internal/academy/certificate.go:42`); the class expands to 8+4+5+5+8 = 30 ambiguity-free symbols, 6 chars,
  `crypto/rand` (`:171`, `:178`). `academy-backend.md:75-77` correct.
- **Zero Next.js intercepting routes in `apps/`** (`find` with `(.)*` / `(..)*` / `(...)*` patterns, against a `@tabs`
  positive control returning 3). Confirms `hiring.md:411-413`'s M231 correction; and
  `apps/hiring/.../InsightsByMembersContainer.tsx:359` is indeed a plain `<Drawer`.
- **`next-web-app` has no Directus dependency at all** — `git grep -ln "DIRECTUS"` over the whole repo returns
  nothing (positive control `METABASE` → 3 files). Confirms `next-web-app.md:15`.
- **Studio-desk has no DB driver and no `DATABASE_URL`** — enumerated `dependencies` + `devDependencies` from
  `package.json`: no `pg`/`postgres`/`prisma`/`sqlite`/`mysql`/`mongo`/`knex`/`drizzle`/`typeorm`/`sequelize`.
  Confirms `studio-desk.md:279-287`. `app/services/studioDB.js` exists as a facade; `GET_/SET_USER_STUDIO_PREFERENCES`
  exist in `app/services/graphql/{queries,mutations}.ts`.
- **7 studio-desk locales** (`app/public/l12n/`: de, en, es, fr, it, ja, nl) — `studio-desk.md:120`.
  **8 next-web locales** (`configs/i18n/messages/`: de, en, es, fr, it, ja, nl, pt) — `next-web-app.md:44`, `:124`.
- **The `studio-desk` vite prod entry set** — `rollupOptions.input` in `vite.config.ts` lists exactly the nine
  `studio-desk.md:34` names, with `dev-accept` behind `isProduction ? {} : {…}`.
- **The eight generic seeders on the hiring guard's consult-list** — enumerated from
  `rosetta-extensions/stack-seeding/seeders/hiring_scope.go`'s doc block: PersonaSeeder, JobsimSessionsSeeder,
  SkillpathSessionsSeeder, AssignmentsSeeder, ActivitySeeder, HeroActivitySeeder, FeedbackSeeder, SuccessionSeeder
  = |8|. `hiring.md:405` correct.

**Exact-anchor reads confirmed** (opened the file and read ±15 lines around each; these are anchor verifications, not
set derivations, and I label them as the weaker clearance §5 rule 24 demands):

- `platform/docker-compose.yml` @ `0c91421`: studio-desk `depends_on: backend` `:138-140`, `profiles:` `:141`,
  `VITE_GRAPHQL_ENDPOINT` build arg `:119` / runtime `:135`; next-web-app build arg `:151` / runtime `:160`,
  `depends_on` `:165-167`; backend `profiles: [core, backend, all]` `:110`. `grep -c 5050` = 0.
  `Makefile:10` = `PROFILE ?= core`; `Makefile:119-120` = `--profile core --profile frontend`.
  Historic: studio-desk `depends_on` `:223-225` / `profiles` `:226` and build arg `:204` at `0dab54d`; studio-desk
  `depends_on backend + cms` at `:337-341` at `2adcf71`; the deleted `graphql` service's `depends_on` did include
  `storage` (`graphql-wundergraph.md:180`), and it published `5050:8080`.
- `app` @ `b948604f`: `intelligence.go:1700` (`m.ent.JobSimulationSession.Query()`), `:1820` (`score := RoundFloat`),
  `:1844` (blind re-cast), `:1846` (`Score: &score`), `:885-886` (`insightsSortColumn` mapping the `completition`
  misspelling back to `FieldCompletionStatus`), `:1751-1763` (the `score DESC / completition ASC / started_at DESC`
  default sort); `resolver_queries.go:1034/1035/1053/1080/1085/1089`; `resolver_cms_queries.go:95,99-103,210,258,295`;
  `siminvitationlink.go:62`; `job_simulation_session.go:39,45`; `anticheat_result.go:24`;
  `20260722104506.sql:2,12,13,29,53,79`; `20260729133514.sql:58-62`; `20260722081626_jobsim_data_model.sql:336/355/376`;
  `20230817154747_supabase_baseline.sql:2` (the only `CREATE SCHEMA` in the set, `auth`); `atlas.hcl:8`
  (`search_path=public`); `askengine/registry.go:192` (the M710 comment, and the entry below it really does map
  `jobsimulation.sessions → public.job_simulation_sessions`); `gqlauthz.go:186` (the `unknown viewer` rejection, with
  the `__typename`-not-exempt comment at `:75` and the `isIntrospectionOnlyQuery` `__schema`/`__type`-only test at
  `:92`); the `insightsJobSimulationByMemberships` SDL field at `queries.graphqls:189` carries **no** directive gate.
  `internal/academy/` contains every file named at `academy-backend.md:49-55` plus the six named test files;
  `academy_embedding_refresh` @ 03:00 exists (`internal/worker/tasks/academy_embedding_refresh.go:10`);
  `/content/catalog.json`, `/content/admin/{…}` and `ACADEMY_CONTENT_API_TOKEN` all exist.
  `0e37771f` = 2026-06-05, subject ends `(#903)`; `9d00a313` = v1.367.0.
- `app` @ `9d00a313` (the ref that block names): `cmd/academyImport/main.go:239-243` is the bucket-not-RPC comment +
  `os.Getenv(appstorage.EnvPublicBucket)`, `:244-246` the hard error. Quoted text matches the source comment.
- `next-web-app` @ `bb3313bc`: `useGetClerkOrganization.tsx:16-18` and `:20-21`; `UserStatusContext.tsx:141-173`,
  `:144-149`, `:168-172`; `apps/hiring/.../UserStatusContext.tsx:125,144-145`; `useNavbarSections.tsx:459-466` with
  `:460` the `isHiringOrg ? results : activityDashboard` label; `template.tsx:90` = `Boolean(organization)`;
  `FreeTrialContainer.tsx:29` = `Boolean(!isHiringOrg && organizationId)`; `packages/graphql/src/query/insights.ts:31-82`
  = the `insightsJobSimulationByMemberships` document; `simulationScoreColumn.tsx:95-97` renders `row.score` and the
  `apps/web` / `apps/hiring` copies are **byte-identical** (`diff` empty); `proxy.ts` exists in both apps and
  `middleware.ts` does not; repo `CLAUDE.md:15` = *"Next.js 16 App Router"*, `:55` = the `proxy.ts` rename note;
  one root Dockerfile (`Dockerfile.dev`, `node:24-alpine`, `pnpm turbo build --filter=@anthropos/web-app`); no
  `storybook` script and no `.storybook/`, only `configs/tailwind/storybooks.css`; ports 3000/3001/3002/3031 and
  `test` scripts only in `apps/web` + `apps/hiring`; `next ^16.2.7` in all four apps, React `^19.2.7`,
  `packageManager pnpm@10.30.3`, `engines.node >=24.0.0`, turbo `^2.9.6`.
- `studio-desk` @ `14a5442a`: `src/routes/youtube.ts:43` = `process.env.YOUTUBE_API_KEY` with the `_mock:true`
  fallback below it; `.env.example:120` + `terraform/main.tf:129` = `GCLOUD_SERVICE_ACCOUNT`, and `git grep` shows no
  `src/` read of it; `src/index.ts:96` = the four-member `STUDIO_ACCESS_ROLES`; `skillpath.ts` = 61,080 bytes (the
  "~61KB largest route"); `pingSnapshotCapability` at `:709` reading `/permissions/me/directus_versions/create`;
  `FORCE_READ_ONLY` at `:88`; `BUNNY_LIBRARY_ID`/`_API_KEY` at `:1356-1357`; in-code `PORT` fallback 9100
  (`src/index.ts:60`); `engines.node >=24` + `node:24-alpine`; `providerHealth.ts` rotates **timeouts only** to the
  end of the chain (fast failures do not rotate); `aiService.getCompletion` loops every configured provider within one
  request (`:108-146`); `app/core/main.ts:97` = `preloadCriticalCSS();`, `:105` = the GlitchTip/tailscale-funnel
  comment, `:206` = `new PageWrapper();`.
- `graphql-wundergraph` @ `60c229f3`: `terraform/main.tf:20` = `service_desired_count = 1`, `locals.tf` =
  eu-west-1 / project `wundergraph` / port 8080, `main.tf:26` = `/health`; `package.json` = the two-line stub with
  the `graphql-wundegraph` misspelling; `ci/update-subgraph.sh:9` = the single `gh release download … -R
  anthropos-work/app` (count = 1); pins `cosmo/router:0.275.0`, `wgc@0.104.0`, `node:22.11-alpine`,
  `federation_version: =2.3.2`; no `subscription:` block in any `supergraph-config-*.yaml`; `bba862f` exists **only**
  on `remotes/origin/feat/use-web-socket` (`git branch -a --contains`); `config.compose.yaml` playground/introspection
  `true` vs `config.prod.yaml` `false`; `-compose`/`-dev`/`-prod` only, no `-local`; `Makefile` `run`/`updatesubg`
  behave as described and README:11 is the aarch-only note.
- `platform` history: `d11a403` (2026-08-03) removes the `cms`/`jobsimulation`/`roadrunner` `repos.yml` entries and
  the roadrunner service; `838d907` (2026-08-05) removes `storage`/`messenger`/`customerio-sync` services + the two
  clone entries; `2adcf71` = the merge of `b56d731` + `360efd4`.

---

## What I could not settle

- **`graphql-wundergraph.md:8` — *"the repo is ARCHIVED on GitHub, 2026-07-30 (read-only)"***. Archive state lives in
  the GitHub org API, not in a clone, so I cannot confirm or refute it here. What the clone *is* consistent with:
  `origin/main` is `60c229f3` dated 2026-07-30 and nothing lands after it. Worth flagging that
  `service_taxonomy.md:139` states the opposite methodological rule for the sibling `jobsimulation` row —
  *"repo archive state: report both, assert neither … archive state is not visible to this corpus"* — so the corpus
  applies two different evidentiary standards to the same kind of claim. I did not book it because I cannot show the
  assertion is false, only that it is unsupportable from the clone set. Settled by one `gh api
  repos/anthropos-work/graphql-wundergraph --jq .archived`.
- **`hiring.md:154` (*"87 real `SIMULATION_TYPE_HIRING` sims"*) and `:158` (*"0 `job_position` rows"* in the captured
  snapshot)** — both are properties of the `.agentspace` snapshot cache, not of any repo in the ground-truth set.
  Settled by `stacksnap status` / a row count against the captured manifest.
- **`studio-desk.md:412` (*"skeleton-visible p95 4669 ms → 817 ms"*), `hiring.md:417-418` (*"recruiter p95 1.27 s"*,
  *"5/5"*), `hiring.md:379-384` (*"~8 candidates per position, scores 27–100"*)** — live-run measurements on
  `billion`/`demo-2`. Not reproducible from a static clone; settled only by re-running the named harnesses.
- **`hiring.md:211` — `persona_write.go:69-71` for the three `validation_*` tables.** In the authoring copy the three
  table-mapping comment lines are `:66-68`; `:69-71` is the adjacent "the old `jobsimulation.sessions` step is
  REMOVED" comment. I did **not** book this: `.agentspace/rosetta-extensions` is an uncontrolled moving `main`
  (§5 rule 26) rather than a pinned ref, so a 3-line offset there is not evidence about the corpus. The two
  load-bearing rext anchors in the same doc *do* resolve exactly — `persona_write.go:91`
  (`{"public", "job_simulation_sessions", sessionCols(), a.sessions}`), `sessionCols()` at `:152`, and
  `hiring_config.go:99` (`CopyRowsIdempotent(… "organization_sim_invitation_links" …)`). Settled by pinning the doc's
  rext citations to a tag.
- **`studio-desk.md:21` — *"it never depended on `graphql`"***. I verified studio-desk's `depends_on` at `0c91421`,
  `0dab54d` and `2adcf71` (backend, and formerly cms). I did not walk the full compose history back to the service's
  introduction, so the universal "never" is unverified rather than refuted.
