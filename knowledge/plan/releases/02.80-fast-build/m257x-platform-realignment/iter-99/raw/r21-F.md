# Seat F — reading #21

**Refs at the moment of measurement** (re-verified at open, per §5 rule 26):
app `b948604f` (origin/main `2035f9a40`, 4 ahead of `9d00a313` v1.367.0) · app/studio `aeec036a` ·
platform `0c91421d` (the ADDENDUM's supersession — **not** `0dab54df`) · next-web-app `bb3313bc` ·
sentinel `88bc5592` · storage `4ce8ece5` (origin/main `9f8cb53`) · messenger `fa47850d` ·
cms `ca50c817` (origin/main `f38c0c4`) · graphql-wundergraph `60c229f3` · roadrunner `87d8d443` ·
jobsimulation `462343b0` (origin/main `82cb66ec`) · studio-desk `14a5442a` · ant-academy `9c3843cd`.
Corpus read at working-tree HEAD `1937e1f` (branch `m257x/platform-realignment`).

**Instrument note (§5 rule 2, paid for in this pass).** My first `repos.yml`-history derivation returned
**0 names ever** — a *broken pipeline*, not an absence: this shell is zsh, and `"$c:repos.yml"` applies
the zsh **`:r` history modifier**, silently rewriting the pathspec to `<sha>epos.yml`. `2>/dev/null`
would have hidden it entirely. Re-run with `"${c}":repos.yml` it returns **14 names ever / 11 `type: go`**.
Every "0" below is accompanied by a positive control that returned non-zero on the same pipeline.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/services/ant-academy.md` | 457 | yes |
| `corpus/architecture/architecture_overview.md` | 417 | yes |
| `corpus/architecture/shared_libraries.md` | 247 | yes |
| `corpus/services/roadrunner.md` | 206 | yes |
| `corpus/architecture/dependency_map.md` | 103 | yes |
| `corpus/services/intelligence.md` | 18 | yes |
| **total** | **1448** | |

Protocol read first: `corpus/ops/platform-alignment.md` §5 (lines 249–1421), in full.

---

## Blockers

### B1 — `SKILLER_STREAM` at `2035f9a4`: 6 Go occurrences across **3** files, not 4

- **Anchor:** `corpus/architecture/dependency_map.md:59` — *"This cell also named origin/main `2035f9a4`
  until M257x iter-98, where **not one of those line numbers resolves** (`main.go:1276` is
  `apiKeyManager,`) and `SKILLER_STREAM` has **6** Go occurrences across 4 files, not one"*
- **Why it is false:** measured at the ref the sentence itself names, with three instruments:

  ```
  git -C stack-demo/app grep -n  SKILLER_STREAM 2035f9a4 -- '*.go'   → 6 lines
  git -C stack-demo/app grep -l  SKILLER_STREAM 2035f9a4 -- '*.go'   → 3 files
  git -C stack-demo/app grep -c  SKILLER_STREAM 2035f9a4 -- '*.go'   → main.go:2
                                                                       subscriber_merge_test.go:3
                                                                       subscriber_wiring.go:1
  ```
  The **occurrence count (6) is exactly right**; the **file cardinality is 3**, not 4. No alternative
  scope produces 4 either — repo-wide (all extensions) the file set is **6**
  (`knowledge/plan/releases/09.00-support-in-app/m905-subscriber-merge/decisions.md`,
  `knowledge/skiller-domain.md`, `main.go`, `subscriber_merge_test.go`, `subscriber_wiring.go`,
  `terraform/main.tf`). Positive control on the identical `-l | wc -l` pipeline: `CMS_STREAM` at the same
  ref returns **4** Go files, so the pipeline can and does report 4 when 4 is true.
  Search-hiding mechanisms ruled out: `git grep` sees gitignored-but-tracked files; the two known
  NUL-bearing files are `next-web-app/.../useCoursebuilder.ts` and `ant-academy/code/src/time/store.js`
  (neither in `app`, neither `.go`); the only nested repo under `app` is `app/studio` @ `aeec036`, which
  is Python. The Go scope inside `app` at that ref is fully covered.
  Everything else in the same cell verifies: `main.go:1276` @ `2035f9a4` **is** `apiKeyManager,`;
  `roles.go:791` **is** `func (r *RoleManager) SkillerSubscriber()` at both refs; no
  `pubsub.NewPublisher` names `SKILLER_STREAM` at either ref; `docker-compose.yml:71` @ `0c91421`
  **is** `SKILLER_STREAM=skiller`. The defect is the file cardinality alone — which is precisely the
  number §5 rule 24 exists to make people state correctly.
- **Confidence:** high

### B2 — "not opt-in: it is the stock `core` selection" contradicts two sibling passages and is false at platform HEAD

- **Anchor:** `corpus/architecture/dependency_map.md:58` — *"(b) Since the v9.0 messenger-in-app fold
  (`app` `9d00a313`), **`app` itself runs a second subscriber server** on messenger's *own* Redis
  consumer group and attaches it to this stream by name … **That one is not opt-in: it is the stock
  `core` selection.**"* (`:60` and `:61` extend the same takeover to the `jobsimulation` and `cms`
  stream rows.)
- **Why it is false:**
  1. **Self-contradiction, same file, 37 lines up.** `dependency_map.md:21` (Messenger row,
     Infrastructure cell) says the messenger domain is *"now `app`'s, and gated **OFF** on a developer
     machine behind `MESSENGER_ENABLED`"*. And `corpus/architecture/architecture_overview.md:30-32`
     says *"messenger and customerio-sync ride in the same container but stay **OFF** on a developer
     machine behind `MESSENGER_ENABLED` / `CUSTOMERIO_SYNC_ENABLED`, which compose deliberately does not
     set (`docker-compose.yml:84-92` says why)"*. One document cannot say both *"runs by default on a
     stock `core` stack"* and *"off unless you opt in"*.
  2. **Ground truth at platform HEAD.** At `app` `origin/main` (`2035f9a4`) the whole messenger block —
     sender, subscriber server **and** the `Subscribe()` that attaches to messenger's live consumer
     group — is wrapped in the switch: `main.go:285` `messengerEnabled := mustSubsystemSwitch(envMessengerEnabled)`,
     `main.go:1445` `if messengerEnabled {`, with `env_guards.go:61`
     `envMessengerEnabled = "MESSENGER_ENABLED"` and `subsystem_switch_test.go:99-111` pinning
     *"Flow.Subscribe() must be inside the MESSENGER_ENABLED guard"*. `platform/docker-compose.yml`
     @ `0c91421` **deliberately does not set it** (`:84-92`, verbatim: *"app gates them behind
     MESSENGER_ENABLED / CUSTOMERIO_SYNC_ENABLED, which default to OFF on a developer machine … To
     exercise either one locally, set it in .env — and know that messenger then attaches to the LIVE
     Redis consumer group"*). So on a stock `core` stack at HEAD the takeover does **not** happen.
  3. **The pin does not rescue it (§5 rule 33 part 4).** At the ref the block names, `9d00a313`, the
     claim was true — `MESSENGER_ENABLED` has **0 occurrences in the entire `app` tree** at that ref
     (positive control: `BREVO_KEY` returns 21 Go lines across 6 files at the same ref), and
     `main.go:1423` wires `msgadapters.Wire(...)` unconditionally. But the sentence is a **present-tense
     currency assertion** — *"it **is** the stock `core` selection"* — and rule 33 rules explicitly that
     a block asserting currency cannot be pinned into silence, "because the pin and the word are making
     opposite claims and only one of them is what the reader acts on."
  4. **It misleads in the dangerous direction.** The reader acts on it by believing a stock local stack
     is already attached to messenger's **live** Redis consumer group — the exact hazard compose's own
     comment says you must opt in to incur.
- **Confidence:** high (on the self-contradiction and on the HEAD measurement; medium that the author
  intended the pin to carry the whole sentence)

### B3 — `shared_libraries.md`'s summary says the services pull in five library repos; four is the measured and elsewhere-fenced number

- **Anchor:** `corpus/architecture/shared_libraries.md:3` — *"The Anthropos Go services share five
  internal libraries"* — and, load-bearing, `:11` — *"that shared plumbing lives in **five small repos
  that the services pull in like any third-party dependency**."*
- **Why it is false:** enumerated across **every** Go clone on disk (`app b948604f`, `sentinel 88bc5592`,
  `storage 4ce8ece5`, `messenger fa47850d`, `cms ca50c817`, `jobsimulation 462343b0`,
  `roadrunner 87d8d443`): `github.com/anthropos-work/authn` appears in **0 of 7** `go.mod` files;
  `colony` appears in **7 of 7** (positive control). Services pull **four** repos — colony, proto, ai,
  taxonomy. `authn` ships *inside* colony as `colony/authn`; its own repo is pulled by nobody.
  This contradicts, in order of authority:
  - the **same file** at `:167` — *"**No checked-out service imports the standalone
    `github.com/anthropos-work/authn`**"* — and at `:19`/`:150`;
  - `corpus/architecture/architecture_overview.md:80` — *"**four** imported private modules … **`authn`
    is not a fifth**: … 0 hits for `github.com/anthropos-work/authn` across all seven Go clones, against
    a positive control of `colony` required by all seven"* (I re-derived this independently; it is
    exactly right);
  - `corpus/architecture/dependency_map.md:49`;
  - root `CLAUDE.md` — *"five repos, of which **four are imported as private Go modules** … **`authn` is
    imported by nothing**"*, the highest-propagation site in the tree (§5 rule 19's corollary).
- **Why I hesitated:** `:3`/`:11` sit under *"High-Level Summary (For PMs & Non-Engineers)"*, the table
  row at `:19` immediately discloses the real state, and the *spirit* of `:11` (this plumbing is shared,
  not reimplemented) is true — the authn code does reach each binary, via colony. But `:11` makes a
  concrete, checkable claim about **how many repos the services pull in as dependencies**, and that
  claim is measurably wrong and is the exact claim four other corpus sites exist to correct. Per the
  briefing's rule I booked it up.
- **Confidence:** medium

---

## Minors

- `corpus/services/ant-academy.md:135` — *"`emptyCatalogView() = { chapters: [], skillPaths: {},
  series: [] }`"* is presented as a literal equality but omits two of five keys: source is
  `serverTenant.js:116` `return { chapters: [], skillPaths: {}, series: [], bundles: PUBLIC_BUNDLES,
  catalogVersion: CATALOG_VERSION }`, and `PUBLIC_BUNDLES` (`ucourses/catalog.js:956`) is **non-empty**
  and FS-derived. *I considered booking this a BLOCKER* — an exact-value claim rendered with `=` is
  false as written, and it slightly overstates "empty". I kept it MINOR because the documented
  consequence — chapters/skillPaths/series all empty ⇒ **0 cards** — is unaffected, and the file's own
  read-chain narrative is otherwise verbatim-correct (`:145` **is** literally
  `const view = (await getBackendCatalogView(eids)) ?? emptyCatalogView()`; the `:132` quote *"the
  cutover is intentional, not reversible-on-error"* is verbatim and inside the cited `:115-145` window).
- `corpus/services/ant-academy.md:258` — *"~26 Playwright e2e spec files (tests/e2e/)"*. Measured: **31**
  tracked `code/tests/e2e/*.spec.js` (`git ls-files`), 40 tracked files in the directory. Approximate
  marker present, but a 19 % undercount.
- `corpus/services/ant-academy.md:144` — *"`code/public/catalog.json` (~2,667 entries)"*. Measured:
  `courses` = **2,715** (keys: `version`, `generatedAt`, `languages` (7), `courses`).
- `corpus/services/ant-academy.md:402-411` — the repo-local skills table names 14 skills; the repo ships
  **15** (`.claude/skills/`: the 14 named plus **`export-path`**, plus a non-skill `_shared/`).
  `export-path` is the one the doc's own `:150-151` prose leans on (*"published into the app academy DB
  via the repo's export path"*).
- `corpus/architecture/architecture_overview.md:330-342` — the `>` blockquote prefix is lost mid-quote
  (lines 330–341 and the tail of 342 are bare). CommonMark lazy continuation keeps them inside the quote
  because there is no intervening blank line, so the render survives; formatting damage only.
- `corpus/services/roadrunner.md:13-14` — *"`roadrunner/terraform/main.tf:19` still reads
  `service_desired_count = 1` and **has not been touched since `87d8d44` (2026-06-19, before the fold)**"*.
  `87d8d44` is the repo's HEAD **and** `origin/main`, but it touched only
  `.github/workflows/bump-version.yml`; the file's last modification is `e45eb61` (2026-05-27). The
  literal reading ("nothing since `87d8d44` touched it") is true and the load-bearing fact (`:19` = `1`,
  unchanged since before the fold) is correct; the natural reading is not.
- `corpus/architecture/dependency_map.md:59` — *"Enumerated over **every publisher constructor** in `app`
  @ `b948604f` only"* lists five. There is a sixth publisher constructor,
  `internal/deadletterqueue/dead_letter_queue.go:38` `redisstream.NewPublisher(...)`, which republishes
  to a **dynamic** topic (`metadata["topic_poisoned"]`, `:65`). It cannot falsify the conclusion — it can
  only replay a message someone already published, and nothing publishes to `skiller` — but the
  enumeration is not "every publisher constructor".
- `corpus/architecture/architecture_overview.md:128` — the mermaid edge label
  `Gateway -.->|spawns studio/gen.py in-process| Room`. It is an `exec.CommandContext` **subprocess**
  (`app/internal/cms/studio/studioManager.go:1101`, invoked at `:119` as
  `runCommand(ctx, pyBin, ["studio/gen.py", …])`), i.e. in-*container*, not in-process. The prose
  elsewhere (`dependency_map.md:91`, `architecture_overview.md:38`) says "embedded inside the `app`
  image", which is right.

---

## Positively cleared (audited zeros)

Every item below is **ENUMERATED** unless explicitly labelled otherwise.

**Multi-tenancy fence — `architecture_overview.md:359-371`.** ENUMERATED, independently, without
reading the document's arithmetic first. Parsed all `type X struct{ ent.Schema }` declarations in
`app/internal/data/ent/schema/` (both the one-line and multi-line forms) → **135** schema types from 139
`.go` files (the 4 non-schemas being `mixin.go`, `skiller_mixins.go`, `skillpath_mixins.go`,
`database_types.go`). `OrganizationMixin{}`: **30** raw hits, of which `user_resource.go:22` is
`// OrganizationMixin{},  // We need to work on this` → **29 live**. Own-`Policy()` declarations in the
directory: exactly **5**, in **4** files — `organization.go:56`, `org_membership.go:172`, `user.go:116`,
and the two mixins in `mixin.go` (`:99`, `:126`). `Organization`'s query policy is
`DenyIfNoOrganizationInContext` + `FilterSameOrganizations()` (`:95-96`) — org-filtering; `Membership`'s
is `AllowCurrentOrgEdgesOrSkipRule` + `AllowCurrentUserOwnedOrgEdgesOrSkipRule` + `AlwaysDenyRule` —
org-scoped; `User`'s is `FilterSameUserRule` — **user**-scoped, correctly excluded. **29 + 2 = 31 of
135.** Second conjunct, derived separately: 18 schemas declare a plain
`field.UUID/String("organization_id")` with neither mixin; **2** of those are policed
(`org_membership.go` by its own `Policy()`, `academy_feedback.go` by `UserMixin{}`) → **16**; plus the
**7** `OrganizationIDMixin{}` users (`category`, `jobrole`, `skill`, `similarity`, `specialization`,
`studio_task`, `studio_document`) → **23 carry an `organization_id` with no policy at all**, of which 16
is the neither-mixin subset. Every figure, every named file, and the 16-name list in
`security_compliance.md:92-97` reproduce exactly. This is the fence §5 rule 24 says has been wrong three
generations running; at this reading it is right.

**Compose / `repos.yml` topology at platform `0c91421`.** ENUMERATED from source. `docker-compose.yml`
declares **5** services (`sentinel`, `backend`, `studio-desk`, `next-web-app`, `gotenberg`); `common.yml`
adds `postgresql` + `redis` → **7 effective**; `repos.yml` holds **4** entries. Clears
`dependency_map.md:31`, `architecture_overview.md:71`/`:157`/`:223`, `roadrunner.md:40-42`. Exactly one
service address is set — `AUTHORIZATION_ADDRESS=http://sentinel:8087` at `docker-compose.yml:48` — and
**zero** `*_RPC_ADDR` variables in any compose file (positive control: `ADDRESS` returns 1). Clears
`dependency_map.md:7`, `architecture_overview.md:266-270`. `backend`'s `depends_on` is `:101-109` with
the quoted comment verbatim at `:102-103`; `studio-desk`'s is `:138-140`, `backend` and only `backend`;
`GOTENBERG_URL=http://gotenberg:3200` at `:57`; `SKILLER_STREAM=skiller` at `:71`;
`AI_USAGE_STREAM=AI` at `:47`; the `MESSENGER_ENABLED`/`CUSTOMERIO_SYNC_ENABLED` rationale at `:84-92`;
`Makefile:10` `PROFILE ?= core`; no `:5050` anywhere.

**`repos.yml` history.** ENUMERATED across all **10** commits that ever touched it: **14** names ever,
**11** of them `type: go` — clears `shared_libraries.md:186`'s *"that set is 11"* and its naming of
`skiller`, `skillpath`, `chronos`, `intelligence` as the four extras, all four present in the first
revision `a2a3ee6` (which carries all 14) and `skillpath` the last of the four to be dropped
(`a4db680`, after `fdfa189` intelligence → `045857c` chronos → `21429b7` skiller). Also clears
`shared_libraries.md:23-24` (none of the five libraries is or has ever been in `repos.yml`) and
`roadrunner.md:39-42` (9 entries @ `2adcf71`, roadrunner at `:29-31`; 6 @ `0dab54d`; 4 now).

**`d11a403`'s diff vs its own message.** ENUMERATED. The commit message asserts roadrunner's *"repos.yml
clone entry was already gone"*; `git show d11a403 -- repos.yml` deletes `- name: cms`,
`- name: jobsimulation` **and** `- name: roadrunner`. The corpus's *"grade that commit by its diff, not
its message"* (`roadrunner.md:17-19`, `architecture_overview.md:211-213`) is correct in both files.

**Shared-library pins.** ENUMERATED per repo from each `go.mod` at its own ref. colony: app `v0.35.2`
(`go.mod:16`), sentinel `v0.34.3` (`:8`), storage `v0.34.3`, messenger `v0.35.2`, cms `v0.35.1`,
jobsimulation `v0.35.1`, roadrunner `v0.34.3`. proto: app `v1.210.0` (`:18`), sentinel `v1.200.0` (`:9`),
storage `v1.196.0`, messenger `v1.210.0`, cms `v1.207.0` (`:11`), jobsimulation `v1.205.0` (`:13`),
roadrunner `v1.196.0` (`:8`). ai `v1.40.2`: app, cms (`:9`), jobsimulation (`:11`) — and nowhere else.
taxonomy `v1.2.0`: app direct (`:20`), sentinel indirect (`:21`), storage indirect (`:25`), messenger
direct (`:9`), cms direct (`:13`), jobsimulation direct (`:15`), roadrunner **absent** → **6 of 7**, the
sole exception roadrunner, whose only shared-lib requires are colony + proto. Every value and every line
anchor in `shared_libraries.md:41`, `:69`, `:109-110`, `:186` and `dependency_map.md:50` resolves.
`roadrunner/main.go:7` imports colony; `main.go:17` uses `colony.NewVersionConfig`.

**`app`'s Connect handler registrations @ `b948604f`.** ENUMERATED — `mux.Handle` for Connect services
is exactly six: Users `:1187`, Organizations `:1188`, Skiller `:1196`, JobSimulation `:1204`, CMSService
`:1213` inside `if cmsRPCServer != nil` at `:1212-1214`, LabSession `:1228`. Neither
`SkillPathSessionService` nor any RoadRunner service is among them. Clears `shared_libraries.md:70`.
`skillpaths.go:27-31` carries the quoted *"the drop-in for the removed skillpath RPC client"*.

**`STORAGE_RPC_ADDR`.** ENUMERATED at the refs named: **3** Go lines at `9d00a313`, all comments —
`main.go:451`, `internal/jobsimwiring/wiring.go:101`, `internal/storagens/callsites_test.go:189`; **29
lines across 18 files** repo-wide at the same ref; same 3 Go lines at `origin/main` with `main.go:504`
carrying the exact quoted comment; still a live read at the checkout `b948604f`
(`internal/jobsimwiring/wiring.go:115` — `storage.NewClient(getenv("STORAGE_RPC_ADDR"), …)`). Clears
`architecture_overview.md:334-353` on every count, including its "state the ref you mean" caveat.
`storage/terraform/main.tf` @ `9f8cb53` is **18** lines with the "GONE (v9.0 'support-in-app')" comment
at `:9-11` and the `prevent_destroy` rationale at `:13-16`; `service_desired_count = 0` at the
intermediate `63bffc8:terraform/main.tf:38`.

**`ROADRUNNER_RPC_ADDR` / `RoadRunnerService` / `roadrunner:10401`.** ENUMERATED: **0** Go hits for all
three at `app b948604f` and `jobsimulation 462343b0`; 0 in platform @ `0dab54d`; positive control
`JUDGE0_BASE_URL` returns hits on the same pipeline. `docker-compose.yml:118` @ `0dab54d` is indeed
`AWS_REGION=eu-west-1`, as the corpus says. And the full §5-rule-44 tree sweep (all **15** git trees
including the two nested `studio` repos at `aeec036`) reproduces
`roadrunner.md:31`'s case-insensitive file counts **exactly**: app **25**, jobsimulation **8**, platform
**3**, studio-desk **3**, next-web-app **1** (plus roadrunner itself 23, rosetta-extensions 44; ant-academy,
cms, sentinel, storage, messenger, graphql-wundergraph and both `studio` repos all 0). jobsimulation's
**14 lines across 8 files** and **5 exact-case across 3** both reproduce; its Go source holds exactly one
mention, `internal/runner/runner.go:3`.

**`roadrunner` service internals.** ENUMERATED against `87d8d443`: ports `cmd/root.go:110`
`cmp.Or(os.Getenv("PORT"), "8080")` and `:84` `cmp.Or(os.Getenv("RPC_PORT"), "8081")`; the compose-supplied
10400/10401 at `docker-compose.yml:291-292`/`:298`/`:302` @ `2adcf71` with the env block at `:296-302`
matching the doc's table row-for-row (including `JUDGE0_API_KEY` and `SENTRY_DSN` "not set here");
`worker.go` `Concurrency: 10`; `queues.go:4` `DefaultQueue = "roadrunner:default"`;
`tasks.go:4` `RoadrunnerSubmissionResult = "roadrunner:submissionresult"`; `runner.go:126`
`asynq.MaxRetry(3)` inside `CreateSubmission` (`:48-136`, one enqueue per submission);
`runner.go:244` `maxRetries = 15` + `:273` `time.Sleep(1 * time.Second)`; the three RPC methods at
`internal/rpcsrv/rpc.go:20`/`:43`/`:60`; `internal/lsp/lsp.go` present and unreferenced by any server;
**0** `_test.go` files with `Dockerfile:18` `RUN go test -v ./...`; `languages.go:26` `case "py":`;
go.mod `go 1.25.0`, asynq `v0.25.1`, gorilla/websocket `v1.5.3`. `app`'s
`internal/jobsimulation/simulator/stream_handlers.go:30-34` carries the quoted *"NOT stream handlers"*
note verbatim.

**Terraform dispositions.** ENUMERATED. `jobsimulation/terraform/main.tf` @ `6092c6d2` — the ref the
claim names — has the `module "jobsimulation"` block **deleted**, `service_desired_count` absent from the
whole file (positive control: `desired_count` still matches, at `:19`, inside the comment), `:15-22`
exactly the decommission paragraph and `:24-40` exactly the surviving-ownership paragraph. Clears
`architecture_overview.md:20`/`:221` and `dependency_map.md:31`. `cms/terraform/main.tf:39` @ `ca50c817`
= `service_desired_count = 0`, while `6efa1d5` (merged `f38c0c4`, **2026-08-04**) deletes
`build-production.yml` with the message *"the cms ECR repository is decommissioned (M810)"* — both facts
stand, exactly as the "report both, assert neither" passages say.
`graphql-wundergraph/terraform/main.tf:20` = `service_desired_count = 1`;
`roadrunner/terraform/main.tf:19` = `= 1`.

**Router / supergraph.** ENUMERATED. `b56d731` + `360efd4` merged `2adcf71` on 2026-07-31.
`915da06` (2026-07-29) is a squash containing **both** the jobsimulation-subgraph removal (3→2) and the
cms fold (2→1), so the single commit is a **3 → 1** step while its own subject reads "2→1" — exactly as
`architecture_overview.md:222` and `:280` describe. The repo's `schemas/` holds one file,
`backend.graphqls`. `graphql-wundergraph`'s last commit is `60c229f` 2026-07-30 10:23, consistent with
the stated 2026-07-30 archive (no post-archive commits, unlike jobsimulation, whose four **2026-08-04**
commits the corpus correctly uses to refuse the archive claim). `backend` serves `/graphql/query`
(`internal/web/backend/backend.go:317`).

**Messenger's own subscribers.** ENUMERATED. `messenger/internal/flow/flow.go:72` is
`AddSubscriber("backend", …)` over exactly **21** live `pubsub.EventHandler` entries (one is commented
out — `OrgJobSimulationAssignmentPastDueHandler` — matching `app`'s own "21, not 22" note); `:105` is
`AddSubscriber("jobsimulation", …)`, `:109` is `AddSubscriber("cms", …)`. `app`'s
`internal/messenger/flow/streams.go:65` is `StreamBackend = "backend"` @ `9d00a313`, and
`internal/messenger` does not exist at the `b948604f` checkout at all — so the pin there is load-bearing,
not decoration.

**Jobsimulation's 23 run-state tables.** ENUMERATED: `jobsimulation/internal/ent/schema/` @ `462343b0`
holds 24 `.go` files of which `mixin.go` is not a schema → **23**, and all 23 have counterparts in `app`'s
`public`-schema directory. Clears `architecture_overview.md:179`/`:398` and `dependency_map.md:13`.

**Studio-Room / Mistral.** ENUMERATED at the nested repo's **own** ref (`app/studio` @ `aeec036`, invisible
to `git -C app grep`): `services/ai.py:704-709` — `get_client`'s `providers` dict is exactly
`{openai, azure, anthropic}`; `tools/pdf2md.py:24` is `from mistralai import Mistral`;
`git -C app/studio grep -ci mistral HEAD` sums to **22 lines / 3 files**
(`requirements.txt`, `tools/pdf2md.py`, `tools/r3.py`) — the corpus's figure exactly. All three
`app/studio/configs/*.ini` pin `azure` for every `*_AI_*_MODEL` row. `app`-side Mistral:
`internal/cms/studio/markdownManager.go:11` (`ai/mistral` import) and `:19`
(`mistral.NewMistral(nil, os.Getenv("MISTRAL_API_KEY"))`), called from `studioManager.go:583`.

**AI routing anchors** (`architecture_overview.md:292-299`). ENUMERATED at the checkout:
`internal/jobsimulation/ai/ai.go` `:259` `getClient`, `:262-276` the Azure branch defaulting to
`azureClientEu` and swapping to `azureClientUs` on `"flag_use_azure_us"`, `:129` `isThrottlingError`,
applied at `:166` and `:325`, `:85-88` `config.LoadDefaultConfig(ctx, config.WithRegion("eu-west-1"))`.

**`ant-academy`'s `isPublic` matcher** (`ant-academy.md:262-292`). ENUMERATED from
`code/proxy.js:112-188`: the base list is **25** patterns and the doc's table accounts for all 25 with
none invented — catalog front door 5, public-launch listings 4, auth/gate 2, cert verification 2, AI
proxy 1, release provenance 2, crawler files 6, assets/machine indexes 3 — plus the two conditional
blocks, whose gates are verbatim (`DEV_LOGIN_ENABLED`; `VISUAL_BYPASS = BENCHMARK_VISUAL_BYPASS === "1"
&& NODE_ENV === "development"`, whitelisted not blacklisted, `:62-64`). The org gate is
`!/^(0|false)$/i.test(raw ?? "")` (`:87-88`) — default ON, fail-closed, as claimed.

**Other `ant-academy` anchors.** ENUMERATED: `code/app/layout.jsx:1` imports **exactly**
`{ Work_Sans, Instrument_Serif }`, constructed at `:41` and `:51`, with `manifest:
"/academy-manifest.json"` at `:132` and `academy.css:1` carrying the verbatim *"Display + mono usages
fall back to system fonts"* — the "neither DM Sans nor JetBrains Mono" retraction is correct;
FA Pro is vendored (`code/public/assets/fontawesome/css/all.min.css` + 29 `webfonts/*.woff2`);
`vercel.json` is exactly `{"framework": "nextjs"}`; `engines.node >=22`; dev port **3077**
(`package.json:21`), mobile **8555**; Next `^16.2.6` / React `^19.2.5`; Expo `~54.0.33`; the three named
workflows all exist. `serverTenant.js:115-145` and `:132`; `serverChapterBody.js:52`/`:65`/`:67`;
`app/not-found.jsx:43` *"You wandered off the trail."*; `draftMode.js:46-48` `NODE_ENV === 'development'
&& isDraftOptIn(ACADEMY_SHOW_DRAFTS)`; `i18n/locale.js:10` `SUPPORTED_LOCALES` = the 7 named codes;
`components/TopBar.jsx:76` `<LanguageSelector />`; `LanguageSelector.jsx:88` `role="menu"` / `:97`
`role="menuitemradio"`; `views/public/PublicHeader.jsx:20` `<LocaleSwitch …>`, which
(`i18n/LocaleSwitch.jsx:10-17`) is the 2-way EN↔IT toggle. The **retraction's own provenance checks out**:
`5b05b7d9` (2026-05-05) added *both* `LanguageSelector.jsx` and its `TopBar.jsx` mount — despite a commit
subject about cover art that invites the opposite conclusion — and `e22f3230` (2026-05-18) is
*"feat(i18n): add locale support for es, fr, de, nl, pt"*. Cosmo:
`ucourses/ucourse-engine/assistant/agent.js:12` `https://api.openai.com/v1/responses`, `:13`
`MODEL = 'gpt-5.2'`, `:32` `localStorage.getItem('openai_api_key')`, gated by
`NEXT_PUBLIC_FEATURE_TRAINING_COACH` (`src/lib/featureFlags.js:8`).

**Storage's compose-vs-runtime disagreement** (`dependency_map.md:19`). ENUMERATED from `838d907`'s diff:
the deleted `storage` block declared `depends_on: redis (service_healthy), postgresql (service_healthy)`
and its env held **no** `DB_CONNECTION` and **no** `REDIS_ADDR`; `storage/go.mod` contains **0** `redis`
matches (positive control: 21 `aws` matches). The `storage-legacy` profile and the
"second Brevo contact pusher" phrasing both come verbatim from the platform's own commit message.

**RE-DERIVED only (weakest clearance, flagged as such):** the *"at least 13 Connect-RPC services"* list at
`shared_libraries.md:77-88` — I counted 13 named entries and confirmed `storage.md:115` is indeed
`### Connect-RPC (\`StorageService\`)` and that `storage/internal/migration/migration.go:13` imports
`go/simulator/storage/v1` as `legacyStorage`. I could not enumerate the predicate: `proto` is in **no**
clone set, exactly as the passage itself declares. The colony `authn` import counts at
`shared_libraries.md:150` **were** enumerated: **6** `.go` files in `cms ca50c817`, **8** in
`jobsimulation 462343b0`.

**`intelligence.md`.** `fdfa189` (2026-04-17) removes intelligence from **both** `docker-compose.yml` (25
lines) and `repos.yml` (3 lines), and the name never returns in the file's history — so *"no longer
cloned by `make init`"* holds. `045857c` likewise for chronos.

---

## What I could not settle

- **`ant-academy.md:74` — "a grid of 65 real cards."** A live-render measurement on a demo with the
  `academy-fs-published-fallback` demopatch applied. Statically the committed tree has **78** EN
  skill-path directories across 13 series; whatever filter yields 65 is applied at render. Settled only
  by a demo bring-up plus a DOM count, which is out of scope for a read-only pass.
- **`shared_libraries.md:40` / `:193` / `:185`** — colony's `go.mod` `go 1.25.0` and `golang:1.26-bookworm`
  build image, taxonomy's *"131-line node.go"*, and the `NodeID` format/example claims
  (`K-GOXXXX-F63F` etc.). The `colony`, `proto`, `ai`, `authn` and `taxonomy` repos are **in no clone
  set** — an absence the corpus itself discloses. Settled by cloning them at the pinned tags.
- **`intelligence.md:17-18`** — the `DB_CONNECTION_BACKEND` / `DB_CONNECTION_SKILLER` env vars, the
  `:8080` HTTP port, the `/_meta`-only surface and the 5-minute ticker. The `intelligence` repo is not
  cloned and its compose block was deleted at `fdfa189`; `git show fdfa189` would recover the compose
  half but not the ticker. Settled by cloning `anthropos-work/intelligence`.
- **GitHub archive states** — `graphql-wundergraph` "ARCHIVED 2026-07-30"
  (`architecture_overview.md:3`), `skiller` 2026-07-01 / `skillpath` 2026-07-31 and chronos's
  "last push 2026-04-23, NOT archived" (`roadrunner.md:38`, `:57`). Not visible from a clone; settled by
  `gh api repos/anthropos-work/<name> --jq .archived`. Note the corpus is **inconsistent in its own
  discipline** here rather than wrong: it refuses to assert jobsimulation's archive state (correctly,
  citing four post-date commits) while asserting three others flatly — but for `graphql-wundergraph` the
  clone at least contains no commit after the claimed archive date, so nothing contradicts it.
- **`roadrunner.md:6`** — *"the ~386-commit `app` bump"* (an M247 historical figure) and
  `architecture_overview.md:222`'s *"app v1.360.0"* attribution for cms-in-app. Both are claims about
  history rather than about a ref's contents; `app`'s shallow-ish local history and tag set would need a
  full fetch to settle.
- **`architecture_overview.md:409`** — the VPC `10.0.0.0/16`, ALB/subnet layout, 6-hourly backup cadence
  and 30-second ECS health checks. These live in `infrastructure`, which the corpus itself notes **has
  never been in any clone set**.
