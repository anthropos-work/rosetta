# Seat B — reading #21

**Refs actually read (re-verified at this seat's open, per §5 rule 26):**
app `b948604f` (origin/main `2035f9a4`, `git describe` = `v1.369.0-2-g2035f9a40`) ·
platform `0c91421d` (= origin/main) · next-web-app `bb3313bc` · sentinel `88bc5592` ·
storage `4ce8ece5` (origin/main `9f8cb532`) · messenger `fa47850d` · cms `ca50c817` ·
graphql-wundergraph `60c229f3` · roadrunner `87d8d443` (= origin/main) · jobsimulation `462343b0` ·
studio-desk `14a5442a` · ant-academy `9c3843cd` · **nested** app/studio `aeec036a`, cms/studio `aeec036a`
(grepped at their own refs) · rosetta-extensions authoring copy `5fb0915e`.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/services/ai-readiness.md` | 651 | yes |
| `corpus/services/cms.md` | 313 | yes |
| `corpus/services/storage.md` | 232 | yes |
| `corpus/services/coursebuilder.md` | 141 | yes |
| `corpus/services/README.md` | 88 | yes |
| `corpus/services/TEMPLATE.md` | 46 | yes |
| **total** | **1471** | |

Search-pipeline controls run in the same pass: `git grep -i messenger HEAD -- '*.go'` in `app` → 89
(a bare uppercase `"MESSENGER"` returned 0 — the wrong-regex trap, caught before it became a
finding); `git -C stack-demo/platform log -S 'SKILLER_RPC' --oneline 0c91421d` → 7 (paired positive
control for the 0 on `MESSENGER_RPC`); `git grep -ic storage 9f8cb53` in `storage` → non-empty
(paired control for the `custody` zero).

---

## Blockers

### B1 — README's index resolves a prod question the fenced map explicitly records as UNRESOLVED

- **Anchor:** `corpus/services/README.md:21` —
  *"but `roadrunner/terraform/main.tf:19` still reads `service_desired_count = 1` — **so it does still
  deploy**, unlike cms (`cms/terraform/main.tf:39` = 0)…"*
- **Why it is false / unsupportable:**
  - The *cited* half is true: `stack-demo/roadrunner/terraform/main.tf:19` reads
    `service_desired_count          = 1` at the checkout `87d8d443` **and** at `origin/main` (they are
    the same commit; `git rev-list --count HEAD..origin/main` = 0). `stack-demo/cms/terraform/main.tf:39`
    reads `= 0`. Both verified.
  - The **inference** is what the corpus forbids. The fenced map, which `CLAUDE.md` declares
    authoritative for exactly this question, says at
    `corpus/architecture/platform-migration-status.md:90`: *"**The prod contradiction is now explained
    but still not verified**… Meanwhile the authoritative rollback declaration lives in
    **infrastructure's `services.tf`** — a repo this map has never read"*, and adds that the `= 1`
    *"is not a decision about the fold; it predates it by seven months and nobody has been back"*
    (`git blame -L 19,19` → `84a4b4f`, 2025-12-15).
  - `corpus/services/roadrunner.md:19-20` states the same limit in the same words: *"This is the **one
    row where prod and the platform's own declaration contradict each other** — **recorded, not
    resolved**."*
  - It also contradicts **its own banner four lines above** — `README.md:14-18`: *"'No longer deploy
    separately' is a claim about PROD, and **it is not uniformly measurable**"*, and the parallel `cms`
    case at `cms.md:78-84` (*"**Do NOT extend 'the module block has not moved' to 'the rollback path is
    intact'**"*). The same artifact class (a service repo's own `terraform/main.tf`) is denied
    authority for `cms` and granted it for `roadrunner` **in the same sentence**.
  - Net: the index doc — the first thing a reader opens — converts a `desired_count` literal in a repo
    that does not own the ECS module declaration into an assertion about live production.
- **Confidence:** high

### B2 — README lists `roadrunner` among `app`'s folded domains, 16 lines after declaring it NOT folded

- **Anchor:** `corpus/services/README.md:37` —
  *"**The monolith.** … **plus** the folded skiller (taxonomy, matching, embeddings), skillpath,
  jobsimulation, cms and **roadrunner** domains — and the AI-readiness subsystem…"*
- **Why it is false:**
  - `README.md:11-12` enumerates the folded set as **seven**, and `roadrunner` is deliberately **not**
    in it: *"skiller, skillpath, jobsimulation, cms, storage, messenger and customerio-sync"*.
  - `README.md:20` then says in bold: *"**`roadrunner` is the eighth, and it is different: orphaned,
    not merged-and-undeployed.**"*
  - So `:37`'s list disagrees with `:11-12` in **both directions**: it *adds* roadrunner and *omits*
    storage / messenger / customerio-sync — a third, mutually inconsistent enumeration of the same set
    inside one 88-line file.
  - Ground truth on the "roadrunner domain": **`app/internal/roadrunner/` does not exist** — measured
    at the checkout (`ls -d internal/roadrunner` → No such file) **and** at origin/main
    (`git ls-tree --name-only origin/main internal/ | grep -i roadrunner` → empty). The Judge0 runner
    is `app/internal/jobsimulation/runner/`, constructed at
    `app/internal/jobsimwiring/wiring.go:118` (`jsrunner.NewRunnerManager(getenv("JUDGE0_API_KEY"),
    getenv("JUDGE0_BASE_URL"))`) at `b948604f`. `platform-migration-status.md:87` states this
    explicitly: *"**`app/internal/roadrunner/` does not exist**"*.
- **Hesitation (stated per the instrument):** "domain" is used loosely elsewhere in the corpus and the
  *execution* really did move in-process. But `:20` is a deliberate, bolded distinction and `:37`
  crosses it, so a reader taking `:37` at face value goes looking for a package that is not there.
- **Confidence:** medium

### B3 — `cost` is documented as an SSE wire event; the code filters it OFF the wire by design

- **Anchor:** `corpus/services/coursebuilder.md:77-79` (the offending token is on `:78`) —
  *"**SSE wire contract** (`event:` name / `data:` JSON): `session`, `stage`, `outline`, `progress`,
  `patch_applied`, `patch_skipped`, `preview_ready`, `draft_kept`, `translation_ready`,
  `rebuild_required`, `error`, **`cost`**, and an always-last `done`."*
- **Why it is false:** `app/internal/web/backend/coursebuilder/handler.go:2709-2717` @ `b948604f`
  (the checkout; the passage names no ref):

  ```go
  case cb.EventCost:
      // D2 ruling (2026-07-13): the terminal cost readout is COGS
      // and MUST NOT reach the customer SSE stream — Wave-6 posture
      // "COGS never on the wire" wins. Kept in the internal
      // EventKind enum for ops/awaitAndPersist/persistTerminal to
      // consume, but filtered here at the wire boundary by
      // returning an empty event name; StreamMessages skips the
      // writeSSE call when the name is empty.
      return "", nil
  ```

  `writeSSE` is only called with a non-empty name (`handler.go:1479`, guarded; the emitter comment at
  `:2716` says so). So `cost` is not merely absent from the wire — the platform treats putting it there
  as a **privacy/posture violation** it deliberately fenced. Documenting it as part of the wire
  contract inverts a stated ruling.
- **Same anchor, second defect — the enumeration is incomplete by 4 of 17.** Re-derived the *set* from
  `renderEvent` (`handler.go:2604-2760`) rather than checking the doc's list against itself: the live
  event names are `text`, `score`, `patch_applied`, `patch_skipped`, `stage`, `outline`, `progress`,
  `preview_ready`, `draft_kept`, `error`, `translation_ready`, `rebuild_required`,
  **`steering_received`**, **`steering_applied`**, plus `session` (`handler.go:1458`) and `done`
  (`:2768`). The doc omits **`text`**, **`score`**, **`steering_received`**, **`steering_applied`** —
  and `handler.go:3176-3183` documents the last two as the observable contract of `POST
  /sessions/:id/steer`. A client written from this list drops a quarter of the stream, including the
  token-delta channel.
- **Confidence:** high (for the `cost` half; the omission half alone I would have booked MINOR)

### B4 — storage.md's env table records `ENVIRONMENT` as unset by compose; compose set it to `development`

- **Anchor:** `corpus/services/storage.md:215` — `| `ENVIRONMENT` | (empty) | Environment name |`,
  under a table whose header (`:203-208`) states the middle column *"records what
  `docker-compose.yml` set on the `storage` service block"*.
- **Why it is false:** the `storage` service block set it explicitly, at both refs the surrounding
  prose names:
  - platform `0dab54d`, `docker-compose.yml:119` → `- ENVIRONMENT=development`
  - platform `2adcf71`, `docker-compose.yml:206` → `- ENVIRONMENT=development`
  (The `(empty)` convention is used correctly two rows up for `STORAGE_S3_BUCKET`, which the block
  genuinely never set — verified: `grep -n STORAGE_S3_BUCKET` on `0dab54d`'s compose returns only
  `:82`, the *backend* block.)
- **Why it is material rather than cosmetic:** `ENVIRONMENT` is the one variable this same document
  makes load-bearing — `:73-75` explains that both of app's storage boot guards run only
  `if deployedEnvironment()` and that *"`deployedEnvironment()` returns **false** for
  `ENVIRONMENT=development`"* (`app/env_guards.go:37-44` @ `2035f9a`, verified). The env table tells a
  reader the value was never supplied; the compose file supplied precisely the value that disarms the
  guards.
- **Hesitation:** the table sits under a `HISTORICAL` banner. I booked it anyway because the banner
  scopes *relevance*, not *accuracy*, and the column's contract is explicit.
- **Confidence:** medium

### B5 — `urls.ts:52` names the wrong constant (`AI_READINESS_URL` is at `:50`)

- **Anchor:** `corpus/services/ai-readiness.md:305` —
  *"`AI_READINESS_URL` (`packages/core-js/src/constants/urls.ts:52`), consumed by
  `packages/ui/src/NavBar/useNavbarSections.tsx`…"*
- **Why it is false:** `git grep -n AI_READINESS_URL -- packages/core-js/src/constants/urls.ts` @
  next-web-app `bb3313bc` → **`50:export const AI_READINESS_URL = '/ai-readiness';`**. Line `52` is
  `export const ORGANIZATION_FEEDBACK_URL = '/enterprise/organization-feedback';` — a different
  constant for a different route. The rest of the same sentence resolves exactly
  (`useNavbarSections.tsx:4` import, `:398-400` `aiReadinessMenuItem`, `:400` `key: AI_READINESS_URL`,
  `:547` `showAIReadiness ? aiReadinessMenuItem : null`), which is what makes the one wrong anchor
  easy to read straight past.
- **Hesitation:** two lines of drift; the constant name is correct and greppable. Booked per the
  instrument's *"wrong line anchors that point at the wrong construct"* clause.
- **Confidence:** medium

### B6 — an intra-corpus self-citation used to justify a correction lands on a blank line

- **Anchor:** `corpus/services/ai-readiness.md:46` — *"This paragraph stated completed work as
  outstanding and **contradicted `:458` of this same file**, which is already in the past tense;
  corrected M257x iter-46."*
- **Why it is false:** `corpus/services/ai-readiness.md:458` is an **empty line** (`:456-457` end the
  "Route 2 …" paragraph; `:459` opens the `> **✅ CORRECTED M219 …**` blockquote). The past-tense
  passage the sentence means is `:490` — *"M51 iter-09 bounds it with the
  `app-aireadiness-snapshot-loadmembers` app read-path demo-patch"* — roughly 32 lines further down.
  This is §5 rule 34's class exactly: a corpus repair moved a self-citation and it landed on a blank
  line. (Checked the sibling self-citations in the same seat's files as a control — `cms.md:211`'s
  *":44-47 above"* and `cms.md:235`'s *":70-71 in the banner"* **both resolve exactly**, so this is not
  a systemic convention problem, it is one drifted anchor.)
- **Hesitation:** navigational impact only; the correction it justifies is itself true (the manifest
  really does read `path: internal/aireadiness/readiness.go` — verified below). Booked because the
  citation is the *evidence* for the correction and it points at nothing.
- **Confidence:** medium

---

## Minors

- `corpus/services/storage.md:44` — *"the live code is `app/internal/storage/`"*, **unpinned**.
  `internal/storage/` exists at app `origin/main 2035f9a` but **not** at the checkout `b948604f`
  (`ls -d internal/storage` → No such file; `git ls-tree 2035f9a internal/` → present). The same
  document is scrupulous about this everywhere else (`:29` — *"every `app` path in this cell resolves
  at `2035f9a` only"*; `:58` — *"@ app `2035f9a`"*), so the unpinned bullet is an internal
  inconsistency rather than a wrong fact. True of the platform today.
- `corpus/services/README.md:41` — *"In prod the ECS service is **deleted**, not scaled to zero"*,
  **unpinned**. True at storage `origin/main 9f8cb53` (`terraform/main.tf` is 18 lines, no module
  block) and **false at the checked-out `4ce8ece5`** (100 lines, `module "storage"` with
  `service_desired_count = 1`) — the exact flip `storage.md:25` warns about in bold (*"**State the ref
  or this flips**"*). The index one-liner omits the ref the linked doc insists on.
- `corpus/services/coursebuilder.md:115` — *"`# ~25 boundary test files`"*; measured **33**
  (`ls internal/web/backend/coursebuilder/*_test.go | wc -l`). The companion figure is fine
  (*"~55"* vs measured 59).
- `corpus/services/coursebuilder.md:132` — *"`app` is **`v1.369.0`** @ origin/main `2035f9a4`"*;
  `git describe --tags origin/main` = **`v1.369.0-2-g2035f9a40`**, i.e. origin/main is two commits
  *past* the tag. "Six releases on" from v1.363.2 is correct.
- `corpus/services/ai-readiness.md:18-20` — *"the `WorkforceDirectory` interface —
  `LoadMembers`/`LoadMembersByUserIDs`"*. The interface (`app/internal/aireadiness/manager.go:38-48`)
  declares **four** methods: those two plus `BaseMembers` and `LevelsCount`. `LevelsCount`'s
  implementation is in `internal/workforce/manager.go:90`, not `members.go` as the sentence implies for
  the set as a whole (the two named ones *are* in `members.go:349`/`:353`).
- `corpus/services/ai-readiness.md:196-197` — *"`computeInterviewInsightsV2` … reads **exactly one
  table**"*. True of the aggregate source, but the same function calls `resolveSessionAuthors`
  (`how_we_measure.go:1093`), which joins `public.job_simulation_sessions` + `public.memberships` +
  `skiller.job_roles` — as the doc's own next table row (`:202`) says.
- `corpus/services/ai-readiness.md:583` — anchor `how_we_measure.go:253-261`. `:253-255` are the tail
  of the *previous* function (`performanceBySkill`); `computeCycleTotals`'s doc comment starts at
  `:256` and its signature is `:260`. Range names the right construct but starts three lines early.
- `corpus/services/cms.md` — the file's own self-citations and every platform anchor I sampled
  resolved; no minors found beyond those listed. `TEMPLATE.md` carries no factual claims.

---

## Positively cleared (audited zeros)

Labelled per the briefing. **ENUMERATED** = I derived the predicate's member set from source and
compared cardinalities. **RE-DERIVED** = I only re-computed arithmetic the document displays.

**ENUMERATED**

1. **`defaultReadinessSkills` = 31 (19 core @1.0 + 12 enabling @0.5), weight sum 25.0**
   (`ai-readiness.md:252, 356, 550`). Enumerated the literal slice at
   `app/internal/aireadiness/defaults.go:29-62` line by line: 19 entries at `1.0`, 12 at `0.5`, none
   commented out. 19 + 6 = 25.0. `defaultReadinessSims` is 3 — two `StepSimulation` (`tech`,
   `business`) + one `StepInterview` (`both`) — matching `:154` and `:361` including the track values.
2. **The five interview KPI ids** (`ai-readiness.md:201, 213-219, 620-624`). Enumerated
   `usageDimSpecs` (`how_we_measure.go:1138-1159`): exactly `avg_adoption`, `avg_transformation`,
   `avg_originality`, `avg_depth`, `avg_ownership` — 5, matching the doc against the code's *own stale
   comment* which still says "four" (`:1161`). Cross-enumerated the seed side:
   `rext stack-seeding/seeders/ai_readiness_interview_report.go:185-189` returns exactly those five ids
   (`:149` reads *"All five are always emitted here"*) — both anchors exact.
3. **`messenger` `AddSubscriber("backend", …)` = 21 live handlers** (`README.md:44`). Enumerated
   `messenger/internal/flow/flow.go:72-104` @ `fa47850`: 22 `pubsub.EventHandler(...)` lines of which
   **`:83` is commented out** (`// pubsub.EventHandler(h.OrgJobSimulationAssignmentPastDueHandler), //
   not implemented`). Live count = **21**. This is the §5 rule 24 trap (a `grep -c` would have
   returned 22) and the corpus is on the right side of it.
4. **`MESSENGER_RPC_ADDR` appears in no repo** (`README.md:44`). Enumerated all **12** clones at their
   own `HEAD` shas **plus both nested `studio` checkouts at `aeec036a`** (which `git grep` at the host
   ref cannot see): 0 everywhere. Paired control: `git -C stack-demo/platform log -S 'MESSENGER_RPC'
   --oneline 0c91421d` → **0**; `-S 'SKILLER_RPC'` → **7**.
5. **`STORAGE_RPC_ADDR`: 3 hits at `2035f9a`, all comments; 15 hits / 7 live env reads at `b948604`**
   (`storage.md:29`). Enumerated both greps. At `2035f9a`: `main.go:504`,
   `internal/jobsimwiring/wiring.go:101`, `internal/storagens/callsites_test.go:189` — the exact three
   paths the doc names, all comment lines. At `b948604`: 15 hits, of which the `os.Getenv`/`getenv`
   *reads* are `cmd/academy-asset-upload/main.go:129`, `cmd/academyImport/main.go:231`,
   `cmd/import/main.go:50`, `internal/jobsimwiring/wiring.go:115`, `main.go:446`, `:524`, `:992` =
   **7** (the four `t.Setenv` hits correctly excluded). Both figures exact.
6. **The subgraph ladder 5 → 4 → 3 → 1** (`cms.md:59-63, 197`). Enumerated
   `graphql-wundergraph:subgraphs.conf` at each named ref rather than trusting the counts:
   `749dc86^` = 5 → `749dc86` = 4 (SKILLER dropped) → `7c17e63` = 3 (SKILLPATH dropped) →
   `915da06^` = 3 (BACKEND, JOBSIMULATION, CMS) → `915da06` = **1**. And `915da06`'s diff deletes
   **both** `schemas/cms.graphqls` (−762) and `schemas/jobsimulation.graphqls` (−860) while its own
   subject line says *"(supergraph 2→1)"* — the corpus's warning about that subject is correct.
7. **`repos.yml` = 4 entries; compose = 5 services (7 with `common.yml`)** (`README.md:29-30`,
   `cms.md:27, 53`, `storage.md:28`). Enumerated `platform@0c91421d`: `repos.yml` → app, sentinel,
   next-web-app, studio-desk. `docker-compose.yml` service keys → sentinel, backend, studio-desk,
   next-web-app, gotenberg (5); `include: common.yml` adds postgresql + redis (7). Enumerated the
   `profiles:` keys: only 4 exist (`:110, :141, :168, :183`), so **sentinel, postgresql and redis are
   the always-on floor** — which makes `cms.md:252-253`'s and `storage.md:175`'s "a retired token exits
   0 and starts only the floor" correct, and neither spells a runnable invocation (§5 rule 30).
8. **Mistral is OCR-only on both sides** (`cms.md:95, 214-227`). Enumerated at the **nested repo's own
   ref**: `git -C stack-demo/app/studio grep -i mistral aeec036a` → **22 hits in exactly 3 files**
   (`requirements.txt`, `tools/pdf2md.py`, `tools/r3.py`) — the number and the file set both exact.
   `services/ai.py:705-708` is a three-entry provider dict (`openai`/`azure`/`anthropic`); `:1-2`
   imports only `openai`/`anthropic`. Go side: `studioManager.go:119` execs `studio/gen.py`, `:1045`
   execs `studio/postgen.py` — the only two, neither `pdf2md.py`. `tools/r3.py:139/:190/:199-206` does
   exec `pdf2md.py`, which is why the doc's *"nothing dispatches it"* withdrawal is right.
9. **`requirements.txt` is 9 packages and `python-docx` is not among them** (`cms.md:144-149`).
   Enumerated the file: openai, anthropic, rich, pyyaml, requests, jinja2, mistralai, pytest,
   pytest-asyncio = 9. No `python-docx`.
10. **`gen.py` registers exactly nine arguments and swallows unknowns** (`cms.md:279-285`). Enumerated
    `gen.py:484-492` — nine `parser.add_argument` calls, exactly the nine named, no `--template`;
    `parse_argument` at `:18-28` calls `parse_known_args()` and merges leftovers into the dict. Both
    anchors exact.
11. **`AIReadinessClient.tsx` contains zero PostHog references** (`ai-readiness.md:76-81`).
    `grep -c -i posthog` → **0**, `grep -c flag_ai_readiness` → **0** on a 655-line file; the org gate
    is at `:133-134` verbatim as quoted. Employee side `useAiReadinessActive.ts:22` and the constant at
    `aiReadiness.constants.ts:26` both exact.
12. **`HowWeMeasureTab.tsx` never renders `interviewQuestions`** (`ai-readiness.md:589-599`). File is
    **1,989** lines as stated; `grep -c interviewQuestions` → **0**; `:1879` opens the
    `{/* ===== C · Handled for you this cycle ===== */}` block, `:1903` is the label, and exactly three
    cells follow at `:1915` / `:1921` / `:1927` (`skillsMapped`, `handsOnMinutes`, `interviewMinutes`).
    The type field really does exist at `hooks/useAIReadiness.ts:274`. Every figure exact.
13. **Ent table names** (`ai-readiness.md:169-183`). Enumerated the generated `Table = "…"` constants:
    `ai_readiness_{cycles,steps,skills,sims,user_step_progresses,live_snapshots,snapshots,
    diagnose_narratives,text_translations,recommendations,email_overrides,notification_logs,
    notification_optouts}` + `interview_aggregated_reports` — all 14 present, including the three the
    doc flags as ent-pluralised. `interview_aggregated_report.go` carries exactly `sim_id`,
    `organization_id`, `report` (JSON), `session_count` as claimed, with a unique
    `(sim_id, organization_id)` index.
14. **`interactions.action_type` is exactly `{email, call}`** (`ai-readiness.md:587`).
    `internal/data/ent/interaction/interaction.go:94` → `case "email", "call":`. The
    `source_id <> target_id` CHECK is real (`schema/interaction.go:50`).
15. **CSV export is 15 columns** (`ai-readiness.md:275`). Enumerated `AIReadinessCSVColumns`
    (`internal/aireadiness/csv.go:17-32`) — 15 entries; the header comment confirms the 19 → 15 trim
    dropped the recommendation columns; UTF-8 BOM written at `:44`.
16. **`CompareCycles` returns 6 sections and requires both cycles closed** (`ai-readiness.md:267-269`).
    Enumerated `AIReadinessCompareResponse` (`compare.go:32-41`): Topline, Archetypes, Transitions,
    TeamDelta, SkillCoverage, ThemesShift (plus the two cycle refs) = 6 content sections;
    `:163` / `:170` reject any non-`closed` cycle.
17. **The credit costs** (`coursebuilder.md:103`). Re-derived the *set* from `creditCost`
    (`internal/credits/cost.go:86-90`): build **5**, refine **1**, translate **1**. Worth naming: the
    package header comment at `:29` says *"course.refine → 5 credits per refine turn"* — the platform's
    own stale prose — while the executable map and the const's doc comment both say 1. The corpus
    matches the executable map, i.e. it is right where the source contradicts itself.
18. **The coursebuilder route set** (`coursebuilder.md:73-76`). Enumerated `Register`
    (`handler.go:3143-3229`): 22 routes; every route the doc lists exists at the path and verb given.
    (It omits `/sessions/:id/credits`, `/cover/generate` and `DELETE /cover` — but the heading says
    *"Key routes"*.)
19. **27 service docs, exactly enumerated** (`README.md:1-3` and the four tables). Enumerated
    `ls corpus/services/*.md` minus README/TEMPLATE = **27**; every filename appears in exactly one of
    the four tables, and every table row names an existing file.
20. **`custody` occurs 0 times in `storage` @ `9f8cb53`** and no `moved` block exists
    (`storage.md:25`). `git grep -ic custody 9f8cb53` → empty, against a working `git grep -ic storage`
    control. `terraform/main.tf` is **18 lines** with no service block; `:18` reads verbatim *"See
    outputs.tf — consumers should reference these by output, never by literal name."* The M903 note is
    `storage.tf:22-25` verbatim, and `d3e6d32`'s commit body does contain *"M903 never ran."*
21. **`jobsimulation`'s ECS service is gone at `6092c6d2`** (`README.md:22`). The checkout `462343b0`
    still has `service_desired_count = 0` at `terraform/main.tf:40`, and `6092c6d2` is **not** an
    ancestor of it — so a ref-less read would have booked a false finding here. Read at the named ref:
    `git show 6092c6d2:terraform/main.tf` → 56 lines, `service_desired_count` absent entirely, and
    `:15-22` is exactly the *"M810 decommission"* comment block. The claim is correct **because** it
    names its ref.
22. **`storage`'s five RPC methods and the 15-minute presign default** (`storage.md:117-131`).
    Enumerated `internal/rpcsrv/rpcsrv.go`: `PutObject` `:28`, `GetObject` `:49`, `PutPublicObject`
    `:69`, `GetPresignedUrl` `:90`, `GetPublicObject` `:112` — five, matching both tables;
    `:101` `expiry = 15 * time.Minute` on a zero request.
23. **`storage` ships zero tests** (`storage.md:226`). `find . -name '*_test.go'` → **0**, and
    `Dockerfile:18` is `RUN go test -v ./...` — a build gate over nothing, as stated.
24. **`app/internal/cms/*` migration set** (`cms.md:44-47`).
    `terraform/migrations/20260724132049_cms_data_model.sql` creates exactly the six named tables
    (`similarities`, `studio_documents`, `studio_tasks`, `similarity_categories`,
    `similarity_features`, `similarity_skills`), all unqualified → `public`.

**RE-DERIVED** (weakest form of clearance, per §5 rule 24 — listed separately and *not* counted as
clearances)

- `ai-readiness.md:143` `score = 30+40+30 = 100`; `:348` `156/199 = 78.4 %`; `:550` `round(1.0/25.0*30)
  = 1`; `:560-562` `round(25/25*30) = 30` and `round(9.0/25.0*30) = 11` and the old `round(3/6.5*30) =
  14`; `:566-568` `held/25×100 = held×4` is integral for half-step weights. All reproduce. I enumerated
  the *denominator's* set (finding 1 above) rather than accepting 25.0 from the page.

---

## What I could not settle

1. **Whether `infrastructure/terraform/production/services.tf` still declares `module.roadrunner_euwest1`
   / `module.cms_euwest1`.** The `infrastructure` repo is in no clone set. This is the missing evidence
   for B1 and for the "assert neither" posture `cms.md:78-84` adopts. Settled by one read of that repo,
   or by a `terraform state list` against the production workspace.
2. **Whether the `graphql-wundergraph` GitHub repo is ARCHIVED** (`README.md:52`) and whether the
   **`chronos` repo's last push is 2026-04-23** (`README.md:79`). Both are GitHub-API facts about
   repository metadata; neither is derivable from a clone. `gh repo view --json isArchived,pushedAt`
   would settle both.
3. **The demo-seeder runtime claims in `ai-readiness.md`** — the 199 frozen snapshots, `78.4 %`, "the
   demo seeds 0 rows" into `ai_readiness_recommendations`, the `2.09 s` live recompute, the `24 ms`
   frozen read, `180 s → 19 ms` for the loadmembers patch. These are measurements against a running
   `billion`/demo-1 stack. I verified the *code* paths they describe (`buildResponseFromSnapshots` →
   `m.workforce.LoadMembers(ctx, orgID, "")` at `readiness.go:779`; the two frozen routes at `:290-297`
   and `:307-312`; the `keepInCycleStep1` / `keepStartedMembers` split at `:388` / `:390` and the strict
   `step_type=skill_mapping AND status=completed AND completed_at >= cycle.start` at `:638-652`) and
   they are all exactly as documented. The timings need a live stack.
4. **`app/knowledge/plan/**` inside the `app` clone.** `git grep` surfaced platform planning docs there
   (e.g. the `MESSENGER_IN_APP_SUBSCRIBER` gate history and an `m903-s3-custody` plan `storage.md:25`
   cites). I deliberately did **not** open them — the hard bar names the path pattern, and I preferred
   over-compliance to a judgement call. Every claim I graded is grounded in code, compose, terraform or
   git metadata instead. The one consequence: `storage.md:25`'s quotations from
   `app/knowledge/plan/releases/09.00-support-in-app/m903-s3-custody/` (`status: planned`, *"Planned,
   not started"*) are **unverified by me**, though everything around them checks out.
5. **`MESSENGER_ENABLED` / `CUSTOMERIO_SYNC_ENABLED` and `app/internal/storage/` at the *checkout*.**
   These are real at app `origin/main 2035f9a` (`env_guards.go:61`, `:62`; `main.go:285`, `:1445`,
   `:1552`) and **absent at `b948604f`**, which the addendum names as the ref for unpinned claims. I
   did **not** book `ai-readiness.md:263` or `README.md:44` for this, because the claims are true of the
   platform's current state and the checkout is a demo *build pin*, not the platform. I record the
   judgement so a reviewer can overrule it: under a strict reading of the addendum these are three
   further findings (with `storage.md:44`, which I booked as a MINOR).
