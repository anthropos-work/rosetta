# Seat C — reading #22

**Instrument:** `briefing-AS-DELIVERED.md` (222 lines) read in full, plus `corpus/ops/platform-alignment.md` §5
(lines 249–1428) read in full before starting.

**Refs at the moment of measurement** (re-derived at this seat's open, `git rev-parse --short=8 HEAD` per tree,
including nested repos, per the ADDENDUM):

| tree | sha | matches briefing? |
|---|---|---|
| `stack-demo/app` | `b948604f` | yes (checkout). `origin/main` = `2035f9a4`; `9d00a313` (v1.367.0) present |
| `stack-demo/app/studio` | `aeec036a` | yes — own checkout, greped at **its own ref** |
| `stack-demo/cms` | `ca50c817` | yes. `origin/main` = `f38c0c4a` |
| `stack-demo/cms/studio` | `aeec036a` | yes |
| `stack-demo/platform` | `0c91421d` | yes (ADDENDUM's superseded sha, not the stale `0dab54df`) |
| `stack-demo/next-web-app` | `bb3313bc` | yes. `origin/main` = `8297c684` |
| `stack-demo/sentinel` | `88bc5592` | yes |
| `stack-demo/storage` | `4ce8ece5` | yes |
| `stack-demo/messenger` | `fa47850d` | yes |
| `stack-demo/graphql-wundergraph` | `60c229f3` | yes |
| `stack-demo/roadrunner` | `87d8d443` | yes |
| `stack-demo/jobsimulation` | `462343b0` | yes. `origin/main` = `82cb66ec` (carries `6092c6d2`) |
| `stack-demo/studio-desk` | `14a5442a` | yes |
| `stack-demo/ant-academy` | `9c3843cd` | yes |
| `stack-demo/rosetta-extensions` (consumption) | `ab81527a` | — |
| `.agentspace/rosetta-extensions` (authoring, `main`) | `5fb0915e` | yes |

**Instrument-failure note (§5 rules 1/2, and it bit me):** in this shell `git -C <d> show $r:repos.yml`
silently expands as `${r:r}` + `epos.yml` (zsh history modifier `:r`), so the command **failed** and awk
read an empty file — five refs in a row reported `count=0`, which reads exactly like "repos.yml has no
entries". Caught only because I captured stderr to a file and read it. Every count below was re-run with
`"${r}:path"` quoting and a non-empty positive control.

## Positive controls

| file | `wc -l` | read in full? |
|---|---|---|
| `corpus/architecture/alignment_testing.md` | 542 | yes |
| `corpus/services/backend.md` | 318 | yes |
| `corpus/architecture/ai_architecture.md` | 305 | yes |
| `corpus/services/customerio-sync.md` | 147 | yes |
| `corpus/services/skillpath.md` | 112 | yes |
| `corpus/services/skiller.md` | 69 | yes |
| *(protocol, in scope, not assigned)* `corpus/ops/platform-alignment.md` | 2232 | §5 in full |

Total assigned: 1493 lines. Matches the briefing's stated total exactly.

---

## Blockers

### B1 — `customerio-sync.md` points a reader at an `external_services.md` section that does not exist, and re-publishes the fossil the same file retracts

- **Anchor:** `corpus/services/customerio-sync.md:140` —
  `* [External Services](../architecture/external_services.md) — Customer.io as an integrated SaaS`
- **Why it is false:**
  `corpus/architecture/external_services.md` has **no Customer.io section and no Brevo section**. Its
  top-level `##` headings are, enumerated from source: `High-Level Summary`, `Clerk`, `Directus`,
  `GraphQL Gateway — WunderGraph Cosmo Router`, `AI Providers`, `LiveKit`, `AWS Chime SDK`,
  `Development Setup Summary`, `Production Deployment`. A case-insensitive grep for `customer\.io|customerio`
  across that file returns **5 hits, all of them the `customerio-sync` *service name*** in
  compose/`repos.yml` prose (`:174`, `:306`, `:850`, `:859`, `:860`) — **zero** describing Customer.io as an
  integrated SaaS. A grep for `brevo` returns **0**.
  It also contradicts this same file 120 lines above: `customerio-sync.md:18-21` —
  *"**The name is a fossil.** The destination has been **Brevo**, not Customer.io, since long before the
  fold"* — verified against the platform's own package doc
  (`app/internal/customeriosync/doc.go:1-7` @ `app` `2035f9a`: *"the one-way push of platform users into
  **Brevo** … the service moved off Customer.io to Brevo long ago and never got renamed"*).
- **Confidence:** medium. Hesitation: it is one line of a "Related Documentation" list, so the blast
  radius is small — but it is a false claim about the corpus's own contents *and* a self-contradiction
  inside one file (§5 rule 21's cheapest class), and the corpus has explicitly retracted this framing.

### B2 — the `app`-owns-the-Redis-streams banner enumerates FOUR and asserts there is no fifth; there are FIVE

- **Anchor:** `corpus/services/backend.md:33-34` —
  `* **`app` owns the `skillpath`, `jobsimulation`, `cms` and `ai_usage` Redis Streams** — both producer and
  consumer in-process. **`skiller` is NOT a fifth member: it is consumer-only** (`:136`, `:264` below).`
- **Why it is false:** I enumerated the predicate *"stream to which `app` is both publisher and
  subscriber"* independently, over **every** publisher/subscriber constructor in Go source at
  `app` `b948604f` (`git grep -n "NewPublisher\|AddSubscriber" b948604f -- '*.go'`, `_test` excluded):

  | stream | publisher | subscriber |
  |---|---|---|
  | `backend` (`serviceName`) | `main.go:287` | `main.go:1320` (`backendSelfSub`) |
  | `SKILLPATH_STREAM` | `main.go:637` | `main.go:1274` |
  | `CMS_STREAM` | `main.go:1039` | `main.go:1303` |
  | `JOBSIMULATION_STREAM` | `internal/jobsimwiring/wiring.go:180` | `main.go:1285` |
  | `AI_USAGE_STREAM` | `internal/jobsimwiring/wiring.go:127` | `main.go:1305` |
  | `SKILLER_STREAM` | **none** | `main.go:1276` |

  The set has **cardinality 5**, not 4 — the banner omits **`backend`**, the app's own service stream.
  (The only other publisher constructor in the tree is `internal/deadletterqueue/dead_letter_queue.go:38`,
  a bare `redisstream.NewPublisher` that re-publishes onto a message's original topic and owns none, so it
  does not change the set.) The emphatic *"skiller is NOT a fifth member"* asserts the enumeration is
  complete at four, and it is not. The file's own `:264` states the correct set —
  *"four of the five application streams — `backend`, `skillpath`, `jobsimulation`, `cms` — plus the
  `AI`/`ai_usage` usage stream"* — so the two passages count *"fifth"* over two different sets.
- **Confidence:** low–medium. Hesitation: read charitably the banner enumerates *inherited* (merged-service)
  streams plus `ai_usage`, under which `backend`'s absence is by design, and the sentence points the reader
  to `:264` in the same breath. I book it because *"NOT a fifth member"* converts an illustrative list into
  a completeness claim, and a reader enumerating app-owned streams from the banner misses a real one.

### B3 — `jobsimulation`'s ECS service asserted **destroyed** from a terraform code deletion, under a standard the same bullet forbids for `cms`, and contradicting root `CLAUDE.md`

- **Anchor:** `corpus/services/backend.md:50-52` — `**`jobsimulation`'s ECS service is already destroyed** —
  `6092c6d2` deleted the `module "jobsimulation"` block with its task definition and ECR repository`
  (and `:12`, `jobsim-in-app (prod ECS teardown **M810 — LANDED**, `6092c6d2`)`).
- **Why it is unsupportable:** what is measurable is a **commit**, not an apply. At the ref the passage
  names, `6092c6d2` (present on `jobsimulation` `origin/main` `82cb66ec`, **not** an ancestor of the
  briefing's checkout `462343b0`, where `module "jobsimulation"` is still declared at
  `terraform/main.tf:31` with `service_desired_count = 0`), the anchors `:15-22` and `:24-40` resolve
  exactly as described — but they are a **comment block asserting** the destruction, plus the module's
  surviving buckets/SSM/atlas tracker. Whether `terraform apply` ran is not in any clone.
  Nine lines above, the same bullet imposes the opposite standard for the identical evidence class:
  `:44-47` — *"**Whether that infrastructure-side deletion has actually been applied is NOT MEASURABLE from
  any clone set we have** … So do not assert either way: what is measured is a `cms`-repo commit asserting
  the destruction"*. Root `CLAUDE.md` also still reads the other way: *"`module.jobsimulation_euwest1` is
  still declared in production terraform as the **rollback path** (no traffic) … teardown is **M810**"* —
  present tense, pending, against `backend.md:12`'s **LANDED**.
- **Confidence:** low. Hesitation: the two cases genuinely differ — `cms`'s module block has *not* moved and
  its only evidence is a commit message about the un-cloned `infrastructure` repo, whereas
  `jobsimulation`'s block really was deleted in a cloned repo. The finding is that "the terraform no longer
  creates it" was upgraded to "it is destroyed", which is one inference further than the file's own rule
  allows, and that `CLAUDE.md` still says pending.

### B4 — "compose now declares **five** services" is an unresolved-`include:` count, and collides with a different "five" in root `CLAUDE.md`

- **Anchor:** `corpus/services/backend.md:18-19` — `The last three lost their **containers** a day later, at
  platform `838d907` (merged `0c91421`, 2026-08-05): compose now declares **five** services and `repos.yml`
  **four** entries.`
- **Why it is misleading:** enumerated from source at platform `0c91421`, `docker-compose.yml` opens with
  `include:\n  - common.yml` (`:1-2`) and declares 5 service blocks (`sentinel` `:5`, `backend` `:28`,
  `studio-desk` `:112`, `next-web-app` `:143`, `gotenberg` `:170`); `common.yml` declares 2 more
  (`postgresql` `:2`, `redis` `:24`). **The compose project therefore declares SEVEN services**, and the two
  that the sentence's number silently drops are exactly two thirds of the always-on floor. That is the
  precise error `platform-alignment.md` §5 rule 30 was written about: *"a parser reading only
  `docker-compose.yml` computes `floor = {sentinel}` and concludes, wrongly and quietly, that a dead token
  starts nothing."* Compounding it, root `CLAUDE.md` states a **different five** for the same subject —
  *"`core` starts **five** containers — `backend`, `gotenberg` and the always-on floor (`postgresql`,
  `redis`, `sentinel`)"* — and the two fives name overlapping-but-unequal sets (compose-file five includes
  `studio-desk`/`next-web-app`; `core`'s five includes `postgresql`/`redis`).
- **Confidence:** low. Hesitation: the number is exactly right for `docker-compose.yml` alone (8 → 5 across
  `838d907`, re-derived), and the parallel with "`repos.yml` **four** entries" makes a file-scoped reading
  natural. Booked because the sentence names *"compose"*, not the file, and the corpus's own protocol
  identifies this unscoped count as a load-bearing trap.

---

## Minors

- `corpus/services/customerio-sync.md:37` vs `:89` — `:37` states the standalone's port as fact with its
  purpose (*"published 8080 (HTTP — health/metrics)"*); `:89` hedges the same fact (*"which most likely
  served health/metrics (**never verified**)"*). One of the two should carry the hedge.
- `corpus/architecture/ai_architecture.md:235` — *"the enum is
  `app/internal/cms/directus/collections/jobsimulation.go:983-990`"*. The `AIModel` enum block is
  `:979-991` (11 members); `:983-990` covers 8 of them and drops `Anthropic35SonnetAws`,
  `Anthropic37SonnetAws`, `Anthropic4SonnetAws`. All three of the row's *examples* do fall inside the cited
  range, so nothing downstream is wrong.
- `corpus/architecture/ai_architecture.md:99` — the anchors `jobsimulation/ai/ai.go:267,344` and
  `skillerai/ai.go:347` (@ `5ba17044`, verified: all three land on the `"flag_use_azure_us"` string
  literal) name the flag lookup rather than the vendor `switch` the sentence is about. Right files, right
  mechanism, slightly-off construct.
- `corpus/architecture/ai_architecture.md:60` — cites `external_services.md:619-629` for "item 4"; item 4
  begins at `:622`, so the range opens 3 lines inside item 3. Contains what it claims.
- `corpus/architecture/alignment_testing.md:534` — the Layout row reads
  `cmd/alignctl  run | capture | dna list|diff|validate|coverage  (`dna.go:20-26`)`, but `dna.go:20-26` is
  only the `dna` sub-dispatch; `run`/`capture` are dispatched in `main.go`. (`dna.go:20-26` does correctly
  cover `list`/`diff`/`validate`/`coverage`.)
- `corpus/architecture/alignment_testing.md:530-540` — the Layout block omits `alignment/internal/canon`
  (it exists and is imported by `cmd/alignctl/dna.go:10`). Illustrative block; omitted list member.
- `corpus/architecture/alignment_testing.md:373-377` — `unmeasurable()` cited as `:139-153`; the function
  is `:139-154` (the `return ExitUnmeasurable` is at `:153`, the closing brace at `:154`).
- `corpus/architecture/ai_architecture.md:268` — *"that map is stored and **never read**"* cites
  `validator.go:…,595`, and `:595` *is* a read of the field (it is passed to the criterion validator). The
  substantive claim — *the dispatch is a hardcoded switch, not the map* — is correct and independently
  verified (no `checkerEngines[` index exists anywhere in `app`).
- `corpus/architecture/ai_architecture.md:276` — `calculateSkillScore` cited as `skills.go:53-64`; the
  function is `:53-62` (`:64` opens `run`).
- `corpus/architecture/alignment_testing.md:333` — the link text names
  `clerkenstein/knowledge/alignment.md` (which exists, in rext) but the href is
  `../services/clerkenstein.md`, identical to the href of the parenthetical right after it.

---

## Positively cleared (audited zeros)

Labelled per the briefing. **ENUMERATED** = I derived the predicate's members from source independently and
compared cardinalities. **RE-DERIVED** = I only re-computed arithmetic the document itself displays.

### `alignment_testing.md`
- **ENUMERATED** — all five DNA gene/capability counts (`:261-265`). Parsed each
  `clerkenstein/alignment/dna/*.json` and counted capabilities and flattened variants independently:
  `clerk-2.6.0` **14 caps / 27 genes**, `clerk-js-5` **6/9**, `clerk-multi-1` **5/9**, `clerk-deploy-1`
  **3/7**, `clerk-express-1` **5/13**. Every published figure matches.
- **ENUMERATED** — *"Only `clerk-2.6.0` … declares a `consumed_surface` today"* (`:208-209`). Parsed all
  five: exactly one non-empty `consumed_surface` (15 endpoints), four absent.
- **ENUMERATED** — the five runners (`:144`, `:390`): `clerkenstein/alignment/cmd/` holds exactly
  `{clerkrun, deployrun, expressrun, jsfapirun, multirun}`.
- **ENUMERATED** — *"the reusable `alignment/` section has no `scripts/` dir at all (it holds `cmd/`,
  `internal/`, `examples/`, `go.mod`, `README.md`)"* (`:329-330`): `ls` returns exactly those five entries;
  `gate.sh`/`drift-check.sh` live in `clerkenstein/alignment/scripts/`.
- **ENUMERATED** — the six snapshot-fidelity operators (`:475-486`): `stack-seeding/dna/snapshot.go:62`
  `validSnapshotOperators` has exactly 6 members, and `data-dna.json:410` (taxonomy) lists 5 (all but
  `cross-surface-closure`) while `:439` (content) lists 5 (no `embedding-dim`, plus
  `cross-surface-closure`) — both anchors land on the operator array itself.
- **ENUMERATED** — *"NOTHING is left waived → 100% coverage"* (`:505`): all 10 surfaces in `data-dna.json`
  carry a `seeded-*` or `snapshot-seeded-*` status; **zero** `waived`. `Coverage()`
  (`stack-seeding/dna/dna.go:316-331`) counts `IsSeeded() || IsSnapshotSeeded()` over the non-waived
  denominator, exactly as described.
- **ENUMERATED** — the three `universal-user` genes (`:219-224`): `ExtractIdentity` (`clerk-express-1`),
  `Me` (`clerk-js-5`), `DeployIdentity` (`clerk-deploy-1`) — and `GetUser` really does carry
  `hero-a` + `hero-b`.
- **RE-DERIVED** — the toy score `86.7% / 100% critical (5/6)` (`:304`): `Add` critical (3 genes × w3 = 9),
  `Greet` standard (3 × w2 = 6), total 15, minus the one diverging w2 gene ⇒ 13/15 = 86.67%. Gate
  `80/100` confirmed at `examples/toy/alignment_test.go:21-22`.
- **RE-DERIVED** — the pre-M219 Go-surface `97.2% / 26-of-27` (`:261`, `:269`): 7 critical caps × 17 genes
  at w3 = 51, 10 standard genes at w2 = 20, total 71; minus one standard gene ⇒ 69/71 = 97.18%.
- Exact-anchor verifications (each opened and read with surrounding lines): `gate.sh:69` +
  rationale `:58-68` (and `:61` inside it); `run.go:133-136` (`ExitRegressed = 2`,
  `ExitUnmeasurable = 3`); `unmeasurable()`'s banner text verbatim; `dna.go:20-27` sub-dispatch;
  `dna.go:37-40`'s exit contract; `--if-declared` downgrading **only** the no-surface case
  (`dna.go:51,73-80`); `idPattern = ^[A-Za-z0-9][A-Za-z0-9_-]*$` (`dna.go:21`); `maxWeight = 1_000_000` +
  `weight must be in 1..%d` (`dna.go:23-24,269-270`); the zero-critical-genes rejection
  (`dna.go:279-281`) and its scoring-time twin (`compare.go:57`); `Overall`/`Critical` formulae
  (`compare.go:101-103` — `Critical` is a plain unweighted count ratio, as claimed); golden-path escape
  refusal (`outcome.go:44-46`); one-subtest-per-gene (`alignment_test.go:46-47`);
  `deploy/colony-authn`, `clerk-webhook/`, `cmd/fake-bapi`, `SeedOrgIdentity`/`LookupOrgEid`/
  `organizationWithEid`/`LoadRoster`/`seedRosterMemberships` all present.
- **ENUMERATED** — the CI claim (`:336-351`): `clerkenstein/.github/workflows/alignment.yml` **exists and
  is git-tracked** (`git ls-files` confirms), and its `:10-11` self-description matches the corpus quote
  word-for-word. Both halves of the doubly-corrected sentence are right.

### `backend.md`
- **ENUMERATED** — the RPC mux (`:66`). Every `mux.Handle` in `main.go` @ `b948604f`: `:1187` Users,
  `:1188` Organizations, `:1196` Skiller, `:1204` JobSimulation, `:1228` LabSession (5 unconditional),
  `:1213` CMS inside `if cmsRPCServer != nil` at `:1212-1214`. Cardinality and every anchor exact.
- **ENUMERATED** — *"There is no `SkillPathSessionService`… 0 occurrences in Go source"* (`:68`):
  `git grep -c ... -- '*.go'` returns no matching file; the only two hits in the whole repo are
  `CLAUDE.md:80` and `knowledge/architecture.md:28` (@ `b948604f`), and at `origin/main 2035f9a` they sit at
  `CLAUDE.md:109` / `architecture.md:28` — **both offsets the passage predicts, including its note that
  CLAUDE.md's line was `:80` when first measured.**
- **ENUMERATED** — the subgraph history (`:122-133`). `subgraphs.conf` per commit in
  `graphql-wundergraph`: @ `749dc86` = `{BACKEND, JOBSIMULATION, CMS, SKILLPATH}` (**4**), @ `7c17e63` =
  **3**, @ `915da06` = `{BACKEND}` (**1**). `915da06`'s diffstat deletes **both** `schemas/cms.graphqls`
  and `schemas/jobsimulation.graphqls` in one commit, and its own subject really does say *"2→1"* — the
  trap the corpus warns about, correctly.
- **ENUMERATED** — *"compose sets **none**"* of the four `*_RPC_ADDR` (`:31`, `:110-111`, `:241`).
  `git grep _RPC_ADDR` at platform `0c91421` returns hits only in `CLAUDE.md`; at `0dab54d` it returns
  exactly four, all on the `messenger` block (`:173`, `:174`, `:176`, `:183`), under the verbatim comment
  the corpus quotes (`:171-172`). `838d907`'s diff deletes that block.
- **ENUMERATED** — the 13 `ai_readiness_*` Ent tables (`:231`). `internal/data/ent/schema/` holds exactly 13
  `ai_readiness_*.go` files, and all four the passage names as "the ones a 9 omits"
  (`recommendation`, `email_override`, `notification_log`, `notification_optout`) are present, as are the
  two it says are *not* omissions (`live_snapshot`, `text_translation`).
- **ENUMERATED** — `internal/aireadiness/` is the engine and `internal/workforce/` holds **no** `readi*`
  file (`:229-230`): confirmed by `ls-tree` on both, with `manager.go`/`cycles.go`/`diagnosis.go`/
  `compare.go`/`csv.go` all present.
- **ENUMERATED** — every package named in the "Key directories" tree (`:167-223`) exists under
  `internal/` @ `b948604f`; `internal/aiacademy` is **absent** and `internal/academy/academy.go:6-9` states
  the removal in the words the corpus paraphrases. `rpc.go` exists at the repo root.
- **ENUMERATED** — `STORAGE_RPC_ADDR` has **0 read sites** and 3 comment occurrences (`:254`) — **at the
  ref the sentence names** (`origin/main 2035f9a`): Go-source hits are `main.go:504`,
  `internal/jobsimwiring/wiring.go:101`, `internal/storagens/callsites_test.go:189`, all comments; `:504`
  says *"STORAGE_RPC_ADDR is gone"* verbatim. (At the checkout `b948604f` there are still 5 live reads —
  `main.go:446`, `:524`, `:992`, `cmd/academyImport:231`, `cmd/academy-asset-upload:129`,
  `cmd/import:50`, `jobsimwiring:115` — which is why the ref-pin matters and is correct to have.)
  `internalstorage.NewManager`/`NewPublicManager` land exactly at `main.go:524`/`:525` @ `2035f9a`.
- Exact-anchor verifications: `docker-compose.yml:110` = `profiles: [core, backend, all]` @ `0c91421`;
  `docker-compose.yml:48` = `AUTHORIZATION_ADDRESS=http://sentinel:8087`; `Makefile:10` = `PROFILE ?= core`;
  gotenberg's `profiles: [core, backend, all]` (so `PROFILE=backend` really does add it); `PORT=8082`,
  `RPC_PORT=8083`, `META_PORT=8084` and published `8081/8082/8083`; `app/terraform/locals.tf:6`
  (`project = "backend"`) and `:8` (`rpc_port = 8081`), `main.tf:185-186`; `app/terraform/variables.tf:197`
  (`cms_rpc_address`) and `:230` (`storage_rpc_addr`), both **without defaults**;
  `messenger/terraform/variables.tf:77,82,87,92` — all four, all defaultless;
  `app/main.go:1205-1211`'s DORMANT comment verbatim and its 60 s write-budget note at `:1239-1240`;
  `main.go:743-746` `LABS_API_URL`; `cms/terraform/main.tf:39 service_desired_count = 0` @ `f38c0c4`;
  `6efa1d5`'s subject and body quoted verbatim; `graph/schemas/skiller_taxonomy.graphqls`;
  `atlas.hcl`'s `dir = "file://terraform/migrations"` + `src = "ent://internal/data/ent/schema"`;
  `git ls-tree b948604f migrations/` **empty** and `6a46e8445`'s subject exact; `Makefile` target
  `migrations: atlas migrate diff --env local`; migrations `20260518125439.sql` (app) and
  `20250116133510.sql` (cms) both present; Go **1.26**.4.
- **ENUMERATED** — the taxonomy figures (`:97-103`). Read the actual capture manifest
  `.agentspace/snapshots/taxonomy/*/manifest.json` (`source: primary-read`, `public_only: true`,
  `predicate: org-null`, `captured_at 2026-06-29T14:43Z`): `skills` **42,790**, `job_roles` **22,470**,
  `job_role_embeddings` **18,919**, `skill_embeddings` **42,790**, `categories` **23**,
  `specializations` **1,447** — every public figure in `backend.md`, `ai_architecture.md` and `skiller.md`
  reproduces exactly, each under the `organization_id IS NULL` filter recorded in the manifest itself.
  `43,584 = 42,790 + 794` also re-derives.

### `ai_architecture.md`
- **ENUMERATED** — the vendor consts and `getClient` (`:22-29`): `jobsimulation/ai/ai.go:30-33` declares
  exactly four (`azure`, `openai`, `anthropic-aws`, `anthropic`); `getClient` is `:259-289`; a failed
  `flag_use_azure_us` lookup returns the **EU** client (`:270-273`); `AnthropicAws` and `Anthropic` both
  return `a.anthropicClient` (`:280-283`); Bedrock is `config.WithRegion("eu-west-1")` at `:87`.
- **ENUMERATED** — the Directus `AIVendor` enum has **five** members (`:967-975`) against the runtime
  switch's **four** cases, with `azureglobal` at `jobsimulation.go:971` the uncased one (`:62-66`) — the
  cardinalities the passage claims, counted from source.
- **ENUMERATED** — every one of the eleven `simulator/ai/ai.go` anchors, read with context: `:20-26`
  (Azure + `GPT4Dot1` + `GPT4Dot1Mini` summarize), `:58-59`, `:65-66`, `:69`, `:82-83`, `:86`, `:98-99`
  (Claude **3.7** Sonnet AWS), `:102`, `:110-111` (Claude **3.5** Sonnet), `:114-115`
  (`default: aiVendor = internalAi.Openai`), `:126-127`. All exact, including the two Anthropic-arm
  defaults the passage adds at `:258-260`.
- **ENUMERATED** — the cms-DTO route: `jobsimulation.go:905` (`AIVendor *AIVendor`), `:1297`
  (`aiModel := simulation.Gpt5Point1`), `:1302-1305` (`aiVendor := simulation.Openai`), `:1307`
  (sequence built **after**) — the mechanism, and its ordering, exactly as corrected at M257x iter-48.
- **ENUMERATED** — the studio slot table (`:120-126`). `app/studio` greped **at its own ref `aeec036a`**:
  `configs/production_config.ini:26-36` and `development_config.ini:26-36` are byte-identical and stable ==
  experimental, matching all five published rows; `config_template.ini:37-41` still carries
  `gpt-4.1-mini / gpt-4.1 / gpt-4o / gpt-4o / o3`, with `:39` and `:40` being the two `gpt-4o` slots the
  blockquote names. `configs/` holds only `{config_template, development, production}` — no
  `local_config.ini` exists, so "shipping" is the right word.
- **ENUMERATED** — *"`gpt-5.2` appears in no studio config at all"* (`:130-131`). Greped the nested repo at
  `aeec036a`: 5 hits total — `services/ai.py:356`, `:508` (both pricing dicts, opened and read),
  `changelog.md:106`, `knowledge/concepts/tests-system.md:84`, `knowledge/operations/running-tests.md:32`.
  **Zero in any `configs/*.ini`.** The two knowledge docs claim `local_config.ini` uses gpt-5.2; that file
  does not exist, so the platform's own docs are the stale party, not the corpus.
- **ENUMERATED** — the Mistral claim (`:33-37`). Greped **each tree at its own ref** per the ADDENDUM.
  `app` @ `b948604f` Go: `internal/cms/studio/markdownManager.go:11,19,21`, `studioManager.go:531,583,585,589`,
  `xlsx.go:13` — all OCR. `app/studio` @ `aeec036a`: `requirements.txt` 1 + `tools/pdf2md.py` 18 +
  `tools/r3.py` 3 = **22 hits / 3 files**, which is the corpus's own published number, reproduced. `r3.py`'s
  three hits are its `MISTRAL_API_KEY` precondition for **PDF→Markdown**, so *"every use is OCR"* holds
  across all three files, not just the one cited.
- Exact-anchor verifications: `coursebuilder/bedrock.go:106-113` (the `ANTHROPIC_API_KEY` either/or,
  read with its comment); `grep -rin 'bedrock\|boto3'` over `app/studio` @ `aeec036a` = **0**;
  `calls/livekit.go:131-135` (flag read) and `:140-144` (`agentEndpoint = "openai-hosted"`), both inside
  `CreateAgentDispatch` (`:106`); `voice_engine` 4-member enum at `jobsimulation.go:1079-1085` and the
  nil→`gptrealtime` default at `:1594-1600`; `validator.go:43,60-61,595`; `criterion.go:127`, `:168`,
  `:428`, `:450-475`; `basevalidator/templates/checkValidationBulk.tmpl` exists, temperature **0.0** at
  `bulkChecks.go:118`, `check_id` field at `bulkChecks.go:18,56`; `check.EngineTextDiff` at
  `criterion.go:142`; `skills.go:40-51` including the literal `// TODO fix this formula` and all three
  branches; `skills.go:75`; `skill/skill.go:617-623 convertLevelTo100`;
  `packages/ui/src/Competency/CompetencyReadLevel.tsx:18`; `CreateSpeech` Azure-only
  (`jobsimulation/ai/ai.go:243-257`, *"at the moment we only support azure voice"*);
  `AI_PROVIDER_CHAIN` in `studio-desk` @ `14a5442a`; `internal/ai/ai.go` **absent from `app`** and
  **present in the frozen `jobsimulation` husk**, exactly as `:99` says; the vector DDL
  (`extensions.vector(1536)` + IVFFLAT, dedicated tables, FK to `skills`/`job_roles`) in
  `terraform/migrations/20260615130000_skiller_taxonomy.sql:51-61` and `:241-251`.
- Cross-doc anchors all resolve: `external_services.md:567` **is** the *Anthropic Direct* row (I initially
  mis-derived this as the Mistral row off a 5-line `sed` window and re-checked with numbered output — the
  citation is right); `external_services.md#routing-what-is-actually-implemented` (`:577`);
  `security_compliance.md#eu-ai-act` (`:226`); `shared_libraries.md#taxonomy` (`:179`) and the explicit
  `<a id="taxonomy-figures">` (`:201`); `platform-migration-status.md §3` (`:124`) which does list all
  **five** LiveKit agent repos (`:143-147`).

### `customerio-sync.md`
- **ENUMERATED** — the package (`:33`): `internal/customeriosync/` @ `2035f9a` holds exactly
  `{doc.go, store.go, sync.go, sync_query.sql, sync_test.go}` and `cmd/customerio-resync/main.go` exists.
- Exact-anchor verifications: `doc.go:4-7` (v0.19.3, *"the last of the Go services to be folded into app"*,
  the Brevo/name-fossil sentence, `public.customer_io_sync_table`); `env_guards.go:62`
  (`envCustomerIOSyncEnabled = "CUSTOMERIO_SYNC_ENABLED"`); `main.go:286` with the *"Resolve the
  outbound-effect switches FIRST, before anything connects to anything"* comment at `:281-284`;
  `main.go:295` (`log.Fatalf` when either switch is on and `BREVO_KEY` is empty); `main.go:393-396`
  (`customeriosync.New(logger, copilotDB, os.Getenv("BREVO_KEY"))` — shared pool, key at `:395`);
  `docker-compose.yml:84-92`, which really is the comment saying *"Pinning them to `false` here would
  override .env and make opting in impossible without editing this file."*
- **ENUMERATED** — the port narrative (`:39-49`) is faithful to `doc.go` line by line: stateless overlap
  window, `CreateContact` + `UpdateEnabled` idempotence, `database/sql` NULL consequence, the 10-minute
  ticker → asynq, and **7,392 of 7,397** rows verbatim.
- **ENUMERATED** — the historical compose block (`:60-79`) matches `0dab54d:docker-compose.yml:136-154`
  including `context: git@github.com:anthropos-work/customerio-sync.git#main`, `ssh: ["default"]`, the
  `8080:8080` publish, the `DB_CONNECTION_BACKEND` line and `profiles: [customerio-sync, all]` — so the
  "dropped from `all` too" clause is right. And it really was the **only** such build: every remaining
  build context at `0c91421` is a local sibling (`../sentinel`, `${APP_BUILD_CONTEXT:-../app}`,
  `../studio-desk`, `../next-web-app`; gotenberg is an image).

### `skillpath.md`
- **ENUMERATED** — the `repos.yml` counts (`:47`), the finding my broken pipeline nearly turned into a
  false absence. Re-run correctly: **9** @ `2adcf71` (`app cms jobsimulation sentinel storage messenger
  roadrunner next-web-app studio-desk` — and **no skillpath**, as claimed), **6** @ `0dab54d`, **4** @
  `838d907`/`0c91421`. All three published numbers exact.
- **ENUMERATED** — the three migrations that post-date `20260729133514` **at the ref the passage names**
  (`app` `9d00a313`): exactly `20260731131307.sql`,
  `20260731154527_academy_chapter_progress_completed_at.sql`,
  `20260803143844_ai_readiness_recommendation_path.sql`. (At the checkout `b948604f` there are only two —
  the ref-pin is load-bearing and correct.) The DROPs are at `:62` (`local_jobsimulation_sessions`) and
  `:63` (`local_skill_path_sessions`), and **no** `local_skill_path_session.go` Ent schema exists.
- **ENUMERATED** — `ContentLanguage` has exactly **8** members (`:58`):
  `assignments.graphqls:44-53` = english, italian, spanish, french, german, dutch, japanese, portuguese.
- Exact-anchor verifications: `internal/skillpath/session.go:205-207` (the `// cms-in-app deseam` comment
  and `u.cms.GetSkillPathDomain`), `internal/skillpaths/skillpaths.go:88-95`, `contentread.CmsContentReader`
  (`internal/cms/contentread/reader.go:37`), Ent `skill_path_session.go` + `skillpath_mixins.go`,
  `schemas/skillpath_sessions.graphqls`, `organization/intelligence.go:1144` and the `:1159-1170` query
  (skill_path_id + `StatusIn(Active, Completed)` + the tenant `Or(TenantIDIsNil, TenantID(org))`),
  `SKILLPATH_STREAM=skillpath` at `docker-compose.yml:72`, and the M506 record's verbatim
  *"No in-app RPC handler is served"*.
- **ENUMERATED** — the "Coming soon" claim (`:97-104`), verified in `next-web-app` @ `bb3313bc`:
  `InsightsBySkillPathStudentSimulationsContainer.tsx:31-34` returns `null as unknown as
  MembershipEnriched`, the results `<Table>` is commented out at `:140-149`, the totals block at
  `:150-160`, and `:137-139` renders `t('enterprise.insights.comingSoon')` → `"Coming soon"`
  (`configs/i18n/messages/en/enterprise.json:1862`). The sibling cohort container really does render a
  live `Table`. And `apps/hiring`'s activity-dashboard `@tabs/` holds only
  `{ai-simulations, interviews, default.tsx}` — **no skill-paths** — while `apps/web`'s holds
  `{ai-simulations, interviews, skill-paths, default.tsx}`.

### `skiller.md`
- **ENUMERATED** — every Ent schema file named (`:13-14`): `skill.go`, `jobrole.go`, `skill_embeddings.go`,
  `job_role_embeddings.go`, `category.go`, `specialization.go` all present, plus
  `skill_translation.go` / `job_role_translation.go` for the localization bullet.
- **ENUMERATED** — `categoryTree`/`fullCategoryTree` "dropped entirely" (`:27`): zero definitions across
  all `*.graphqls`; the only occurrence is the comment
  `skiller_taxonomy.graphqls:7` stating they *"stay unported — no consumers."*
- `internal/rpc/skillerrpc/` exists (`skiller.go`, `skiller_test.go`).
- The manifest-sourced figures and their provenance fields (`:43-47`) are the ENUMERATED clearance recorded
  under `backend.md` above — including that `18,919` really is the `job_role_embeddings` row count and not
  a role count, which is the whole basis of the "18K is refuted" claim.

---

## What I could not settle

- **Whether any M810 `terraform apply` has run**, for either `cms` or `jobsimulation`. Neither the
  `infrastructure` repo nor the platform's `services.tf` is in the clone set. This is what B3 is about, and
  the corpus says so itself for `cms`. Settled by: AWS console/`terraform state list`, or adding
  `infrastructure` to the clone set.
- `ai_architecture.md:86` — the OpenAI model inventory (`GPT-5.4, GPT-5.4-mini, GPT-5.2, GPT-5.1, GPT-5,
  GPT-5-mini, GPT-5-nano, GPT-4.1, GPT-4.1-mini, O3, O4-mini`). These constants live in the private
  `github.com/anthropos-work/ai` module, which `make init` does not clone. The subset reachable through
  `app` (`GPT4Dot1`, `GPT4Dot1Mini`, `GPT5`, `GPT5Mini`, `GPT5_1`, `GPT5_4`) is confirmed; the rest is
  unmeasurable here. Settled by a clone of `ai` at the pinned version.
- `ai_architecture.md:154` — *"skiller migrations `20260417103036` and `20260417120309`"*. **Zero hits
  across all 15 git trees** (I enumerated every tree including the two nested `studio` repos and greped
  each at its own ref, per the ADDENDUM). These are `skiller`-repo migration ids and `skiller` has never
  been in the clone set, so this is unmeasurable rather than refuted — the *substance* (dedicated
  embedding tables, dropped denormalized columns) is independently confirmed in
  `app/terraform/migrations/20260615130000_skiller_taxonomy.sql`. Settled by a `skiller` clone.
- `skiller.md:19` / `backend.md:112` — the production literal
  `http://backend.internal.anthropos:8081`. Every cloned service module *declares* its `*_rpc_addr`
  variable with no default; the value lives in the un-cloned `infrastructure` root module. `backend.md`
  says exactly this and is self-consistent; the value itself is corroborated only by `app`'s own
  `knowledge/service-dependencies.md:46`. Settled by the `infrastructure` repo.
- `alignment_testing.md:261-265` — the *current* scores (100%/100% on four surfaces). I verified the DNAs,
  the goldens' existence, the gate thresholds (`GATE_OVERALL=95` / `GATE_CRITICAL=100`,
  `gate.sh:31-32`) and the exit-code contract, but not the scores, which require running
  `gate.sh` per surface (and `expressrun` needs `@clerk/express` `node_modules`, i.e. would return
  `rc=3 UNMEASURABLE` here — which is itself consistent with what the doc says).
- `alignment_testing.md:447-448` — *"Proven live (M7b): … 100% / Critical 100% across the 4 seeded
  surfaces"*, and `:505`'s live-coverage claim. Both need a running seeded `demo-1` with Postgres on its
  offset port.
- `backend.md:98` — `public.job_roles (org NULL) = 22,490` (prod, 2026-07-08) vs the capture's **22,470**
  (2026-06-29). The 20-role drift is plausible and the doc discloses it, but note that `skills` is
  **42,790 in both**, i.e. zero drift over the same nine days. Not contradictory, but the two measurements
  drifting asymmetrically is worth a re-measure. Settled by a prod read.
- `skiller.md:29-30` — *"the old ECR repo is intentionally orphaned pending manual deletion"* and
  *"app PR #989"*. Not measurable from any clone (`skiller` absent; PR metadata needs `gh`).
