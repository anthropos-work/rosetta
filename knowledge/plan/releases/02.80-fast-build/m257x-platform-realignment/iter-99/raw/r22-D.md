# Seat D — reading #22

Ground truth re-verified at this seat's open (all match the briefing ADDENDUM):
`platform 0c91421d` · `app b948604f` (origin/main `2035f9a40`, post-v1.369.0) · `next-web-app bb3313bc`
· `sentinel 88bc5592` · `storage 4ce8ece5` (origin/main `9f8cb53`) · `messenger fa47850d` (origin/main
`e9421c6`) · `cms ca50c817` (origin/main `f38c0c4`) · `graphql-wundergraph 60c229f3` ·
`roadrunner 87d8d443` · `jobsimulation 462343b0` (origin/main `82cb66ec`) · `studio-desk 14a5442a` ·
`ant-academy 9c3843cd` · nested `stack-demo/app/studio` **and** `stack-demo/cms/studio` both at
`aeec036a` (grepped at their OWN ref throughout, per the addendum).

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/services/studio-room.md` | 507 | yes |
| `corpus/architecture/platform-migration-status.md` | 323 | yes |
| `corpus/services/jobsimulation.md` | 309 | yes |
| `corpus/services/sentinel.md` | 166 | yes |
| `corpus/services/askengine.md` | 124 | yes |
| `corpus/architecture/README.md` | 38 | yes |

(Also read, as the briefing instructs: `corpus/ops/platform-alignment.md` §5 — 2232 lines total, §5 read
in full from :249.)

---

## Blockers

### B1 — jobsimulation.md asserts a production terraform address that exists in NO terraform file, and that the one source naming it describes in the PAST tense as closed

- **Anchor:** `corpus/services/jobsimulation.md:49-50` —
  > "**compose sets no `*_RPC_ADDR` at all** now. Production terraform still names
  > `http://backend.internal.anthropos:8081`."

- **Why it is false:**
  - `git grep 'internal\.anthropos'` at `origin/main` across **all 12 clones** returns the string
    `http://backend.internal.anthropos:8081` **exactly once, anywhere**: `app/knowledge/service-dependencies.md:52`
    (@ app `2035f9a`). That sentence reads:
    *"`messenger` was the last one — it **used to** reach the users, cms, jobsimulation and skiller surfaces at
    `http://backend.internal.anthropos:8081`, and folding it in at v9.0 **closed that edge**."* (`:50-53`).
    It is a *markdown* file, and it says the opposite of the corpus claim.
  - It is in **no `.tf` file at all**. Restricted search `git grep 'internal\.anthropos' origin/main -- '*.tf'`
    over `app · messenger · cms · jobsimulation · storage · sentinel · roadrunner · graphql-wundergraph`
    returns **0 files** (positive control: those repos track 5 / 4 / … / 6 `.tf` files respectively, and the
    same command finds `backend.internal.anthropos:8080` in `graphql-wundergraph/supergraph-config-prod.yaml:6`,
    so the pipeline is live and the port differs).
  - The only prod terraform that *names the variable* is messenger's:
    `messenger/terraform/main.tf:92-93` = `"JOBSIMULATION_RPC_ADDR" : "${var.jobsimulation_rpc_address}"`, and
    `messenger/terraform/variables.tf:92-95` declares that variable **with no default**. The literal is
    supplied by `infrastructure`.
  - **The same file rules this out 25 lines later.** `jobsimulation.md:74-75`: *"Whether
    `infrastructure/terraform/production/services.tf` still declares `module.jobsimulation_euwest1` is not
    something this corpus can see — `infrastructure` has never been in the clone set."* The corpus asserts a
    present-tense fact about the exact artifact it declares unreadable — self-contradiction inside one file.
  - And the module it would configure is stopped anyway: `messenger/terraform/main.tf:29`
    `service_desired_count = 0`.
- **Confidence:** high

### B2 — "sentinel is the ONLY cross-process edge / the only service address compose sets" is refuted nine lines above the line it cites: `GOTENBERG_URL`

- **Anchor:** `corpus/services/sentinel.md:85` —
  > "It is also the only *service* address compose sets at all: there are **zero** `*_RPC_ADDR` variables
  > left, so `backend → sentinel` is the one cross-process edge a local stack has."

  **Two twin sites, same claim, same repair needed** (recorded here rather than booked separately, per
  §5 rule 19 — a claim does not respect a file boundary):
  - `corpus/services/jobsimulation.md:145-146` — *"`sentinel`, which is the **only** cross-process hop a
    local stack has left and the only service address `backend`'s compose entry carries
    (`docker-compose.yml:48`)"*
  - `corpus/architecture/platform-migration-status.md:93` — *"the only cross-process service address left in
    a local stack is `AUTHORIZATION_ADDRESS` on `backend`, pointing at sentinel (`docker-compose.yml:48`)"*

- **Why it is false:** all three cite `docker-compose.yml:48` @ platform `0c91421`. **Nine lines earlier, in
  the same `backend` `environment:` block**, `docker-compose.yml:57` sets
  `- GOTENBERG_URL=http://gotenberg:3200`. That is a second service address, addressing a second container:
  - `gotenberg` is a declared compose service with its own block (`docker-compose.yml:170-183`,
    `image: gotenberg/gotenberg:8`, `ports: 3200:3200`) and it is in the **default** profile —
    `:183` `profiles: [core, backend, all]`. It starts on a bare `make up`.
  - `app` reads it and calls it over the wire: `app/main.go:261` `GotenbergURL: os.Getenv("GOTENBERG_URL")`
    and `app/internal/web/backend/coursebuilder/handler.go:244`
    `newRoutingExtractor(os.Getenv("GOTENBERG_URL"))` (both @ `2035f9a`), consumed by
    `app/internal/converter/gotenberg.go:31`, which does
    `http.NewRequestWithContext(ctx, "POST", gotenbergURL+"/forms/libreoffice/convert", &body)` on a
    90-second `http.Client` (`:13`). That is a genuine cross-process HTTP hop from `backend` to another
    container, on the default profile.
  - `backend`'s block additionally carries `REDIS_ADDR=redis:6379` (`:66`) and two Postgres DSNs (`:93`,
    `:94`) — infra, but they too are addresses of other processes, so "the one cross-process edge" is wrong
    under any reading.
  - The narrow reading ("only *platform Go service* address") does not save it either: root `CLAUDE.md`
    classifies Gotenberg as a **Tier-1 core backend service**, and `sentinel.md:85`'s own next sentence
    enumerates `gotenberg` as one of the services it checked — so gotenberg was in scope of the sentence and
    was scored only on *not having `AUTHORIZATION_ADDRESS`*, never on *whether `backend` addresses it*.
  - The true statement the three sites were reaching for is the one they already make and that holds:
    **zero `*_RPC_ADDR` variables remain** (verified: `git grep '_RPC_ADDR' 0c91421 -- docker-compose.yml
    common.yml .env_example` → 0 hits; at `0dab54d` → 4 hits, all inside messenger's block, `:173/:174/:176/:183`).
    The over-generalisation from "no RPC edge" to "no cross-process edge" is the defect.
- **Confidence:** high (on the fact); I hesitated only over whether "service address" was meant narrowly —
  booked BLOCKER because *"the one cross-process edge a local stack has"* admits no narrow reading, and a
  reader mapping the local topology would omit a container that the default profile starts.

### B3 — askengine.md describes the Bedrock-init failure mode backwards: it does not "disable Talk-to-Data", it returns from `main()` and the whole backend exits

- **Anchor:** `corpus/services/askengine.md:88-89` —
  > "**Prerequisites**: runs **as part of `app`** (no dedicated `cmd/`); a failed Bedrock init **disables**
  > Talk-to-Data but doesn't crash `app`."

- **Why it is false:** at the checkout `app b948604` (the doc names no ref, so it grades at the checkout;
  identical at origin/main `2035f9a`):

  ```
  app/main.go:410   bedrockClient, err := askengine.NewBedrockClient(serverContext)
  app/main.go:411   if err != nil {
  app/main.go:412       logger.Error("bedrock client unavailable; talk-to-data disabled", "error", err)
  app/main.go:413       return
  app/main.go:414   }
  ```
  (`:467-471` @ `2035f9a`.)

  I read the surrounding lines rather than the matched line (§5 rule 10) and checked the enclosing
  construct three ways:
  - `git show b948604:main.go | grep -n '^func '` returns **only** `newEntClientFromDriver:186`,
    `withStatementTimeout:194`, `main:212`. There is no other top-level function between `:212` and `:410`.
  - `sed -n '212,410p' | grep -E 'func\(|go func|wg\.Go'` returns **nothing** — no closure, no goroutine
    literal, no `wg.Go` wrapper in that span.
  - Raw indentation: the `return` is at **two tabs** inside a **one-tab** `if`, i.e. the top level of
    `func main()`.

  So a failed `askengine.NewBedrockClient` **returns from `main()`** — the process ends, with exit status 0,
  before the RPC mux, the GraphQL server, the Echo router, the Asynq pools or the Redis subscribers are
  started. Nothing is "disabled"; everything is absent. This is precisely the *"a `backend` container that
  exited 0 in silence"* class the corpus's own protocol documents at
  `corpus/ops/platform-alignment.md` §5 rule 28, and the corpus has taken the platform's own misleading log
  string (`"talk-to-data disabled"`) as a description of behaviour.

  It also contradicts the same doc's `:8` framing of `app` as a monolith whose other domains are unaffected,
  and `jobsimulation.md:251-258`'s blast-radius reasoning (which correctly says that killing `backend`'s init
  takes the whole platform with it).
- **Confidence:** high

### B4 — the map's completeness census says 26 compose service names ever; a section-aware enumeration returns 25, and the 26th is the `app-network` *network*

- **Anchor:** `corpus/architecture/platform-migration-status.md:78-81` —
  > "…and *every service that has ever appeared in `docker-compose.yml`* (same command on that file →
  > **26** names, including the pre-history the clone set never knew: `nats`, `web-app`, `chromedp`,
  > `simulator`, `realtime`). Re-run those two commands to audit this table; **a name they return that has
  > no row is a gap.**"

- **Why it is false:** I re-derived the **set**, not the sum (§5 rule 24). Across all **80** commits that
  `git log --follow` reports for `docker-compose.yml`, tracking the current top-level YAML section and
  collecting only 2-space keys under `services:`:

  ```
  backend chromedp chronos cms customerio-sync directus gotenberg graphql intelligence jobsimulation
  messenger nats next-web-app postgresql realtime redis roadrunner sentinel simulator skiller skillpath
  storage studio-desk web-app wundergraph
  ```
  = **25**. A naive `grep -E '^[+-]  [a-z0-9_-]+:'` over the same history returns **26** — the extra token is
  `app-network`, which lives under `networks:` and is not a service (I confirmed the file has only three
  top-level keys ever: `include:`, `networks:`, `services:`, and that `app-network` is the only 2-space key
  outside `services:`).

  Two consequences: the stated count is wrong for the stated predicate ("every *service*"), and the passage's
  own audit instruction turns the error into a false alarm — a reader who re-runs it and applies *"a name
  they return that has no row is a gap"* will report `app-network` as a gap in the map.

  (The companion figure in the same paragraph is **correct** and I enumerated it independently: `repos.yml`
  has had exactly **14** names ever — `app · chronos · cms · graphql-wundergraph · intelligence ·
  jobsimulation · messenger · next-web-app · roadrunner · sentinel · skiller · skillpath · storage ·
  studio-desk` — and all 14 have rows.)
- **Confidence:** medium. **I hesitated between BLOCKER and MINOR** and booked BLOCKER: no row is actually
  missing, so nothing downstream is wrong — but this is the one passage in the corpus whose thesis is
  *"Completeness is measured, not asserted"*, the number is the measurement, and it is off because the SET
  the arithmetic ranged over admitted a non-member. Under-booking a wrong derived scalar in the fence
  document is the failure this instrument exists to fight.

---

## Minors

- `corpus/services/sentinel.md:22` — *"it doesn't participate in the federation gateway"*, present tense.
  The WunderGraph/Cosmo gateway was **deleted from the platform** at `2adcf71`; `corpus/architecture/README.md:19`
  (in this same reading's scope) states that explicitly. Stale predicate, no factual consequence for sentinel.
- `corpus/services/jobsimulation.md:160` — cites `cms/directus/collections/jobsimulation.go:1594-1597`.
  The real path is `cms/internal/directus/collections/jobsimulation.go`; the line anchor and the content are
  exactly right (`:1594` `func voiceEngineFromDirectus`, `:1595-1596` `if nil → SimulationVoiceEngineGptrealtime`).
  Missing `internal/` segment only.
- `corpus/services/jobsimulation.md:163` — cites `calls/livekit.go:131-135` / `:140-144`. Real path
  `internal/calls/livekit.go`; both anchors resolve exactly (the `IsFeatureEnabled("flag_use_realtime_openai")`
  call and the `agentName = "anthropos-agent"` / `agentEndpoint = "openai-hosted"` block), and both sit inside
  `CreateAgentDispatch` (`:106-222`) as claimed.
- `corpus/services/jobsimulation.md:25-27` — three lines lost their `>` blockquote prefix mid-banner
  (`2026-07-31; …` / `PR #439, …` / `Report both, assert neither. State: …`). CommonMark lazy continuation
  still renders them inside the quote, so this is cosmetic.
- `corpus/services/studio-room.md:349-359` — the `requirements.txt` code block presents `mistralai` as the
  3rd entry; in the file it is line **8** (after `jinja2`, before `pytest`). The nine-package set is identical
  and `requirements.txt:8` is cited correctly at `:365`, so this is a transcription-order blemish only.
- `corpus/architecture/platform-migration-status.md:305-307` — the enumeration of what `d11a403` restored onto
  `backend` omits `ELEVENLABS_TEMPLATE_AGENT_ID` / `ELEVENLABS_EU_TEMPLATE_AGENT_ID`, which the commit's own
  *Restore on backend* list names and which are live at `docker-compose.yml:54-55`. Omitted list members.
- `corpus/services/sentinel.md:43-45` — *"only as a fixture string in sentinel's own tests
  (`internal/authorization/casbin_test.go`, `internal/rpcsrv/rpc_test.go`)"* omits a third test file,
  `internal/authorization/manager_test.go:21` (`manager := "manager"`); and `internal/authorization/manager.go:20`
  contains the token as a **logger label** (`logger.With("service", "manager")`), not a role. The substantive
  claim — there is no `manager` Casbin role, 0 occurrences in `init_policy.sql` — is **correct**.

---

## Positively cleared (audited zeros)

**ENUMERATED** (predicate re-derived from source independently, cardinality compared):

1. `platform-migration-status.md:76-78` — repos.yml names ever = **14**, and the doc's list is set-identical.
   (`git log -p --follow -- repos.yml | grep '^[+-].*- name:' | sort -u`.)
2. `platform-migration-status.md:204-205` + `:220-222` — the fence's three reach numbers. I **ran** the guard
   (`python3 .agentspace/rosetta-extensions/stack-core/platform_alignment_guard.py
   corpus/architecture/platform-migration-status.md stack-demo/platform/repos.yml`): *"assertion F resolved 92
   citation(s) — 22 subject-checked, 69 range-only, 1 outside any service block; 0 unresolvable"*, **exit 0**.
   The doc's table (22 / 69 / +1) matches the guard exactly, and the §1 state vocabulary is **8** tokens as
   assertion C claims.
3. `platform-migration-status.md:87` — the `app` row's **seven** in-process domains and all seven wiring call
   sites at `app` origin/main `2035f9a`: skiller `:690`, jobsimulation `:721`, skillpath `:751`, cms `:1153`,
   storage `:524`, messenger `:1471`, customerio-sync `:395` — every one opened and read with surrounding
   context. `app/internal/roadrunner/` confirmed **absent**; `app/internal/jobsimulation/runner/` confirmed
   present; `jsrunner.NewRunnerManager(getenv("JUDGE0_API_KEY"), getenv("JUDGE0_BASE_URL"))` at
   `internal/jobsimwiring/wiring.go:123` confirmed.
4. `platform-migration-status.md:92` — the whole `STORAGE_RPC_ADDR` two-ref breakdown, re-derived rather than
   trusted. At `2035f9a`: **3** Go hits, **all comments** (`main.go:504`, `jobsimwiring/wiring.go:101`,
   `storagens/callsites_test.go:189`). At `b948604`: **15** Go hits, of which **7 env lookups**
   (`main.go` ×3 at `:446`/`:524`/`:992`, `jobsimwiring/wiring.go:115`, and one in each of the three `cmd/`
   tools) — **6 spelled `os.Getenv`, 1 a lowercase `getenv` helper**, exactly as claimed — and the other **8**
   split **2 doc comments / 2 error strings / 3 `t.Setenv` / 1 `t.Fatal` message**, also exactly as claimed.
   Also: `storage/terraform/main.tf` is **18 lines** @ `9f8cb53`, `:9-11` and `:13-16` and `:18` all say what
   the row quotes, `custody` = **0** occurrences repo-wide, `moved` blocks = **0**, and `storage.tf:22-25`
   carries the M903 supersession verbatim.
5. `sentinel.md:26` — the Casbin model, enumerated from `sentinel/internal/authorization/casbin.go:13-45`:
   **6** request definitions (`r`,`r2`..`r6`), **6** policy definitions (`p`,`p2`..`p6`), **3** role
   definitions (`g`,`g2`,`g3`), **6** matchers (`m`,`m2`..`m6`). Every per-matcher description in the table
   also checks out against the matcher text (including `m`'s `TIER_FREE` substitution from
   `authorizationv1.Tier_TIER_FREE.String()` and `m6`'s `g3(p6.org, r6.sub)`).
6. `sentinel.md:70-83` — the RPC surface. Enumerated **19** handler methods from
   `sentinel/internal/rpcsrv/rpc.go`; the doc's table names exactly those 19, with no invented method and none
   missing.
7. `askengine.md:47-48` — `app/internal/askengine/registry.go`: **60** `{Name:` entries, **57**
   `Schema: "public"`, **3** `Schema: "directus"`. I explicitly checked for commented-out entries
   (`grep -E '^\s*//.*\{Name:'` → none), so the `grep -c`-counts-dead-code trap (§5 rule 24) does not apply.
   `registry.go:183-195` is the jobsimulation transition-alias comment block as cited; `rewriteSchemas`
   (`sandbox.go:293-309`) holds `jobsimulation`, `skillpath`, `directus`, `skiller`.
8. `askengine.md:64-67` — the `/ask` route set, enumerated from the **actual registration** at
   `app/internal/web/backend/backend.go:171-188` (11 routes). All eleven match, **including the four admin
   routes**: the doc's `POST /ask/admin/auto-rules/distill` is correct and the platform's own doc-comments
   (`admin.go:77`, `:102`, `:129`, `:173`, which say `/admin/ask/…`) are the stale ones. Reading only the
   comments would have produced a false blocker.
9. `askengine.md:104-109` — test counts by `^func Test`: `sandbox_test.go` **49**, `executor_test.go` **13**,
   `prompt_test.go` **10**, `followups_test.go` **14**. All four exact.
10. `askengine.md:44` — *"`WrapQuery` (defends **four** documented bypass vectors)"*. The source's own prose at
    `sandbox.go:350` says *"three"* while the bullet list beneath it enumerates **four** (quoted identifier,
    comment-hidden target, unclassifiable FROM/JOIN target, reserved CTE name), and all four have live code
    (`:375-378`, `:371`, `:380-383`, `:395-398`). **The corpus is right and the platform comment is stale.**
11. `studio-room.md:184` — *"`gen.py` registers **exactly nine** arguments (`gen.py:484-492`)"*: exactly nine
    `parser.add_argument` calls at `gen.py:484-492` @ studio `aeec036a`, and the nine flags match the doc's
    block one-for-one (including `-f/--force` → `dest: forced`).
12. `studio-room.md:362-390` — the whole mistral finding, re-measured at the nested repo's **own** ref:
    `git -C stack-demo/app/studio grep -i mistral aeec036a` → **22** hits across **3** files
    (`requirements.txt` 1, `tools/pdf2md.py` 18, `tools/r3.py` 3); `pdf2md.py:24` `from mistralai import Mistral`,
    `:96` `Mistral(api_key=api_key)`, `:127` `model="mistral-ocr-latest"`; `r3.py:194` gates on
    `MISTRAL_API_KEY`; `requirements.txt:8` = `mistralai`. The registry at `services/ai.py:705-708` is exactly
    `{'openai','azure','anthropic'}` and `ai.py:1-2` imports only openai/anthropic. Both ignore rules verified:
    `app/studio/.gitignore:2` = `tools/`, `app/.gitignore:79` = `studio/*`, `cms/.gitignore:129` = `studio/*`.
    Go execs exactly two studio scripts: `studioManager.go:119` `studio/gen.py`, `:1045` `studio/postgen.py`,
    venv constants at `:94-96`. `app/Dockerfile:45-46` copies `studio/` and pip-installs the file.
13. `studio-room.md:141-146` — `GenMode` at `services/ai.py:35-42`: FAST, STRICT, EXECUTION, CREATIVE,
    REASONING, `DEFAULT = EXECUTION`. Exact.
14. `studio-room.md:82-85` + `:253` + `:257-261` — configs tracked = exactly **3**
    (`config_template.ini`, `development_config.ini`, `production_config.ini`); `max_tokens = 4000` in **all
    three**; the five `*_AI_STABLE_MODEL` rows and their `*_EXPERIMENTAL_MODEL` twins are byte-for-byte what
    the doc prints.
15. `studio-room.md:309-311` — blueprints: `tests/e2e/blueprints/` = exactly the five named
    (b2b, business, none_chat, none_voice, technical); `knowledge/development/asset-examples/blueprints/` =
    exactly three (code / micro / scenario).
16. `architecture/README.md:21` — *"`authn` is not a dependency of any service"*: `anthropos-work/authn`
    appears in **0** `go.mod`/`go.sum` across eight Go clones. Positive control in the same pass:
    `anthropos-work/colony` **is** in `app/go.mod`, and `colony/authn` is imported by app Go source. Also
    verified the README indexes all 10 non-README files in `corpus/architecture/` with none missing or extra.
17. `sentinel.md:85` / `platform-migration-status.md:93` — the `*_RPC_ADDR` census: **0** occurrences at
    `0c91421` across `docker-compose.yml`/`common.yml`/`.env_example`; **4** at `0dab54d`, all inside
    messenger's block (`:173`, `:174`, `:176`, `:183`), confirming *"messenger's compose block was the only
    place"*. `AUTHORIZATION_ADDRESS` = **1** occurrence at both refs, at `:48`.
    (This is the same paragraph B2 over-reaches in — the RPC half is right, the "only cross-process edge"
    half is not.)
18. Compose service-name history — the derivation that produced B4, plus each historical add/remove:
    `cb6ebf5` adds chromedp+web-app, `8770fe6` removes nats, `467965a` removes web-app, `1474b1f` adds
    simulator, `84862d1` removes it, `ef4b449` removes chromedp, `b43b99a` adds realtime, `c17cc9a` removes it
    — each checked as `present-at-commit` vs `present-at-parent`. (My first attempt at this loop returned
    all-zeros; I re-ran it as a shell function and got real answers — §5 rule 2, an empty result from a broken
    pipeline is not evidence.)

**RE-DERIVED** (I recomputed arithmetic the document displays; weaker, listed separately on purpose):

- `repos.yml` = **28** lines / **4** entries @ `0c91421`; `docker-compose.yml` = **186** lines, down from
  **271** at `0dab54d`; `838d907` numstat = **+11/−96** on compose and **+9/−12** on repos.yml.
- Compose service counts 11 (`d11a403^`) → 8 (`d11a403`) → 5 (`838d907`, `0c91421`), and 5 + 2 from
  `common.yml` = the "seven effective" at `jobsimulation.md:53`.
- Every compose `file:line` in the map and in jobsimulation.md/sentinel.md, at the ref each names —
  `:5`, `:18`, `:28`, `:48`, `:57`, `:59`, `:73-75`, `:82`, `:83`, `:84-92`, `:102-103`, `:112`, `:119`,
  `:135`, `:141`, `:143`, `:151`, `:160`, `:168`, `:170-171`, `:183` @ `0c91421`; `:88-91` @ `0dab54d`;
  `:83`, `:93-94`, `:113`, `:119`, `:140` + `Makefile:10` + `repos.yml:17-19` @ `2adcf71`. All resolve.
- All `app` anchors in the map and in jobsimulation.md, opened with context: `main.go:15/62/63/285/286/394/
  395/504/516/517/518-523/524/525/547/690/721/751/1102/1153/1204/1416-1421/1445/1450/1471/1473/1552`,
  `env_guards.go:92-111`, `terraform/main.tf:181/415-416/419-420`, `internal/storage/service.go:22/24`,
  `internal/jobsimwiring/wiring.go:101/123/147-148`, `internal/jobsimulation/ai/ai.go:90`,
  `internal/organization/intelligence.go:1700`, migrations `20260722104506.sql:2/:79` and
  `20260729133514.sql:15-23/36-44/58-62/62-63` (and its **0 INSERT statements** — the single case-insensitive
  `insert` hit is the function name `on_insert_local_jobsimulation_sessions_update_memberships` at `:64`).
- Every dated commit claim in the map and the two service docs: `cb6ebf5` `8770fe6` `467965a` `1474b1f`
  `84862d1` `ef4b449` `b43b99a` `c17cc9a` `a2a3ee6` `045857c` `fdfa189` `21429b7` `a4db680` `236771f`
  `915da06` `b56d731` `360efd4` `2adcf71` `d11a403` `6060315` `ef32d4c` `0dab54d` `838d907` `0c91421`
  `09631fb2` `05b4035` `6092c6d2` `6efa1d5` `f38c0c4` `84a4b4f` `e45eb61` `d3e6d32` — subjects and dates all
  as stated. Two worth naming: `e45eb61` really does touch **only line 11** of roadrunner's `main.tf` (the
  `@@ -8,7 +8,7 @@` hunk), and `git blame -L 19,19` really does name `84a4b4f` (2025-12-15) for
  `service_desired_count = 1` — the map's correction of its own earlier revision is right. And `d11a403`'s
  message really is wrong about roadrunner's clone entry: that very commit's `repos.yml` diff deletes
  `- name: roadrunner`.
- jobsimulation repo: `cmd/root.go:77/:78` (8080/8081), both Dockerfiles `EXPOSE 8080` +
  `ENTRYPOINT ["./application"]` with no `CMD`, exactly four subcommands (`aggregate`, `clone-session`,
  `test-command`, `validate`), repo `CLAUDE.md:88` really does document the non-existent `go run . serve`,
  `CLAUDE.md:124-125` documents :8080/:8081, `internal/runner/runner.go:1-3` header, worker
  `Concurrency: 10` / `25`, `internal/graph/queries.resolvers.go:70` is the persisted-read resolver,
  `cmd/root.go:285` subscribes the `cms` stream, migrations `20260402145459` / `20260409131539` exist.
- studio: `gen.py:18-28`, `:205-238` (three legacy names, exact warning strings), `:241-271` (allowed set =
  `blueprint, media, forced, interactive, branch, pipeline` + four `*_path`), `:273-282`, `:450-456`;
  `exporter.py:513-550` and `:518-519`; `model.py:59` + `:467-469`; `ai.py:383/530/664`;
  `taxonomy.py:11` `https://api.anthropos.work/api`; `CLAUDE.md:12-14`; `postgen.py:396-398` all
  `required=True`; the postgen module set (guidance/metadata/translation/toolkit + testing + exporter);
  `technical.json`'s key set; `.github/workflows/build-production.yml:29`
  `additional_repo: "anthropos-studio-room:studio"`; `python:3.11-slim`; asynq `Concurrency: 5` with
  `ai_video: 7` / `studio: 3`. Negative claims spot-checked with positive controls: no `dialogue.json`,
  `scenarios.json`, `simulation_bundle.zip`, `guidance.md`, and no GraphQL/Directus call anywhere in the
  studio tree at `aeec036a`.
- rext claims inside jobsimulation.md: `DIRECTUS_DATA_CONSUMERS = ("cms", "backend")`
  (`stack-injection/gen_injected_override.py:86`) and the two-path volume removal —
  `volumes: !reset null` on the demo emitter (`:652`) vs an `!override`-tagged empty list on the dev path
  (`stack-core/gen_override.py:149-153`). Both as documented.
- `corpus/ops/platform-alignment.md` **§5 rule 44** exists and is about exactly what `studio-room.md:390`
  cites it for (*"`git grep` at a named ref" is necessary and NOT sufficient — name the tree AND its ref,
  PER TREE*). `shared_libraries.md#taxonomy-figures` anchor exists (`:201`).

---

## What I could not settle

- **Every GitHub archive-state assertion.** `skiller` *"ARCHIVED 2026-07-01"*, `skillpath` *"ARCHIVED
  2026-07-31"*, `graphql-wundergraph` *"ARCHIVED 2026-07-30"*, `intelligence` *"ARCHIVED 2026-04-02"*,
  `chronos` *"NOT archived… last push 2026-04-23"* (`platform-migration-status.md:96-100`). Archive state
  lives in the org API, never in git objects — the map says exactly this for `cms` and `jobsimulation`
  (*"report both, assert neither"*) but asserts flatly for these five. Settled only by
  `gh api repos/anthropos-work/<repo> --jq .archived`, which is outside the clone set. Worth noting the map's
  own standard is asymmetric here; I did not book it because the assertions are dated, which is the
  *"snapshot with an expiry"* form the map itself sanctions.
- **§3's census scalars** (`platform-migration-status.md:126-133`): *93 repositories*, *46 named by no corpus
  document*, and every `last push` date in the table. Same instrument gap. The *4 in `repos.yml`* half I did
  enumerate and it is correct.
- **`platform-migration-status.md:92`'s citation into `app/knowledge/plan/releases/09.00-support-in-app/
  m903-s3-custody/`** (the `status: planned` front-matter and the *"Planned, not started"* progress line).
  My briefing's hard bar #1 forbids reading anything under `knowledge/plan/**`; the bar's stated rationale is
  the rosetta audit answer keys, and this is the *platform's* plan tree, but I chose to honour the bar
  literally rather than scope it myself. Everything else in that M903 sentence — the storage-repo side
  (`storage.tf:22-25`, zero `moved` blocks, zero `custody`, `d3e6d32`'s *"M903 never ran."*) — is verified.
- **Prod terraform for `roadrunner` and `customerio-sync`, and `infrastructure/terraform/production/services.tf`.**
  Never in the clone set; the map declares this itself (§5 rows 3 and the customerio-sync row). B1 is booked
  because that file makes a *positive present-tense* claim about this unreadable artifact rather than
  recording the gap the way its neighbours do.
- **The taxonomy figures** *"≥42,790 skills / ≥22,470 job roles"* (`platform-migration-status.md:110`). Needs a
  DB read (`/db-query`), which this seat does not have. The cross-reference to
  `shared_libraries.md#taxonomy-figures` resolves.
- **`messenger/terraform/main.tf:29` graded against the checkout vs origin/main.** The map's messenger row
  names no ref for it. At `origin/main` `e9421c6` it is exactly right (121 lines, `:29` =
  `service_desired_count = 0`, `:19-25` and `:27-28` as described). At the briefing's pinned **checkout**
  `fa47850d` the file is **111** lines and `:29` is `container_definitions = <<EOF`. I did **not** book this:
  the substance is true at the repo's current state, which is what a reader fetches, and the briefing's
  "grade at the checkout" rule exists for `app`, where checkout and origin/main genuinely disagree about a
  live claim. Recording it so the next reading can decide differently if it wants a ref pinned there.
