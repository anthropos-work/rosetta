corpus/architecture/alignment_testing.md:12-15  [R4 default — presumed citable]  **Alignment testing** is how we measure it. An alignment test is a **differential test**: it feeds the *same* input to two engines — the **source** (the canonical one) and the **mirror** — and checks that they behave the same.
corpus/architecture/alignment_testing.md:23-23  [R4 default — presumed citable]  | **alignment** | Do *two independent implementations* behave identically for the same input? |
corpus/architecture/alignment_testing.md:25-26  [R4 default — presumed citable]  Clerkenstein is just its first consumer.
corpus/architecture/alignment_testing.md:39-39  [R4 default — presumed citable]  *(Axis 1.)* e.g. `CreateOrganization`.
corpus/architecture/alignment_testing.md:42-44  [R4 default — presumed citable]  **Gene** — one **(capability × variant)** pair: the atomic unit of alignment.
corpus/architecture/alignment_testing.md:42-44  [R3 names a reachable artifact]  Its **id** is `<Capability>/<variant>` (e.g. `CreateOrganization/duplicate-name`) and is the join key across the DNA, the goldens, the outcomes, the test subtests, and the report.
corpus/architecture/alignment_testing.md:45-46  [R4 default — presumed citable]  **Alignment DNA** — the officially-enumerated **complete set of genes** for a source target at a version.
corpus/architecture/alignment_testing.md:47-48  [R4 default — presumed citable]  **Alignment score** — the weighted percentage of genes whose mirror outcome matches the source outcome. 100% = behaviorally indistinguishable across the entire DNA.
corpus/architecture/alignment_testing.md:55-56  [R4 default — presumed citable]  Alignment tests are **marked** so they can be parsed, listed, counted, and run as their own suite — separate from unit and integration.
corpus/architecture/alignment_testing.md:58-59  [R3 names a reachable artifact]  A **build tag** `//go:build alignment` gates the suite.
corpus/architecture/alignment_testing.md:58-59  [R3 names a reachable artifact]  Plain `go test ./...` skips it (the same separation unit/integration tests use); `go test -tags alignment ./...` runs it.
corpus/architecture/alignment_testing.md:60-61  [R4 default — presumed citable]  The suite reports **one subtest per gene**, named by gene id, so `go test -tags alignment -json` maps results 1:1 back to the DNA — that's the "countable / parseable" property.
corpus/architecture/alignment_testing.md:68-68  [R4 default — presumed citable]  `go test -tags alignment` — the developer/CI ergonomic (native runner, per-gene subtests).
corpus/architecture/alignment_testing.md:102-102  [R3 names a reachable artifact]  | `capability.criticality` | `critical` / `standard` / `optional` → default gene weight `3` / `2` / `1`.
corpus/architecture/alignment_testing.md:103-103  [R3 names a reachable artifact]  | `variant.operator` | The equivalence test (below). |
corpus/architecture/alignment_testing.md:104-104  [R3 names a reachable artifact]  | `variant.input` | The input passed to the runner for this gene (inlined JSON). |
corpus/architecture/alignment_testing.md:105-105  [R3 names a reachable artifact]  | `variant.normalize` | Dot-paths zeroed before comparison (only for `operator: normalized`). |
corpus/architecture/alignment_testing.md:106-106  [R3 names a reachable artifact]  | `variant.weight` | Optional explicit weight; overrides the criticality default. |
corpus/architecture/alignment_testing.md:123-123  [R4 default — presumed citable]  | `error_class` | only the error class is compared (both error the same way / both succeed); value ignored. |
corpus/architecture/alignment_testing.md:133-140  [R3 names a reachable artifact]  A runner is invoked as `RUNNER --target {source|mirror} --dna PATH` and prints, to stdout, a JSON map of gene id → outcome: ```json { "Greet/padded-name": { "value": "Hello, El Nino!", "error_class": null }, "Add/overflow":       { "value": null, "error_class": "overflow" } } ``` `value` is the capability's normalized return (any JSON, or `null` on error); `error_class` is a stable short string naming the error kind (or `null` on success).
corpus/architecture/alignment_testing.md:142-145  [R3 names a reachable artifact]  The toy's is `examples/toy/cmd/toyrun`; **Clerkenstein ships its own** in the `clerkenstein/` **section of the `rosetta-extensions` monorepo** — not a repo of its own (`clerkenstein/alignment/cmd/{clerkrun,jsfapirun,expressrun,deployrun,multirun}`).
corpus/architecture/alignment_testing.md:152-153  [R3 names a reachable artifact]  `alignctl capture` runs the runner with `--target source` and writes each gene's outcome to a **golden** file under `golden/<Capability>/<variant>.json`.
corpus/architecture/alignment_testing.md:160-160  [R3 names a reachable artifact]  **gene weight** = `variant.weight` if set, else the capability's criticality default (`3/2/1`).
corpus/architecture/alignment_testing.md:198-198  [R4 default — presumed citable]  | **What enforces it** | `DNA.Validate()` **rejects** a consumed endpoint that names no capability (or names one that doesn't exist).
corpus/architecture/alignment_testing.md:199-199  [R3 names a reachable artifact]  `gate.sh` runs it on every gate, **before** the score — but with **`--if-declared`** (see the row below), which changes what the *gate* enforces. |
corpus/architecture/alignment_testing.md:205-207  [R4 default — presumed citable]  It cannot discover consumption nobody wrote down.** This is a strictly weaker claim than the old "every consumed endpoint is present," and it is the true one. Adding a consumer without adding its endpoint to `consumed_surface` re-opens exactly the M218 hole.
corpus/architecture/alignment_testing.md:219-224  [R3 names a reachable artifact]  **The deeper lesson (M218 D15).** Three genes *did* name identity — `ExtractIdentity`, `Me`, `DeployIdentity` — and all three assert the variant **`universal-user`**: the stub itself.
corpus/architecture/alignment_testing.md:219-224  [R4 default — presumed citable]  **When a golden is captured from a mirror rather than derived from the source's contract, it ratifies whatever the mirror does — including its bugs.** The fix is two-sided assertions: `GetUser` now asks for hero **A** *and* hero **B** and requires **A ≠ B**, which no single stub identity can satisfy.
corpus/architecture/alignment_testing.md:234-236  [R3 names a reachable artifact]  **`/align-run`** — *measure alignment of two targets.* Given a DNA + a source version + a mirror: capture or replay the source goldens, run the mirror, compute the score, and surface the divergence report.
corpus/architecture/alignment_testing.md:240-240  [R3 names a reachable artifact]  The executable harness (`rosetta-extensions/alignment/cmd/alignctl`):
corpus/architecture/alignment_testing.md:269-281  [R3 names a reachable artifact]  **"Clerkenstein is at 100%."** The Go surface sat at **97.2%** for a full milestone, on purpose: `MembershipOrgIdentity/real-org-eid` was a **deliberately RED** `standard` gene (M218 **D16**) — the fake BAPI fabricated the org's external id instead of returning the roster's real UUID.
corpus/architecture/alignment_testing.md:269-281  [R3 names a reachable artifact]  The BAPI now reports the roster's real `org_eid` (`Store.SeedOrgIdentity`/`LookupOrgEid`, wired from the roster at `cmd/fake-bapi`), behind a three-tier ladder — roster eid → demo-org eid → the historical stub — so the alignment runner (which mounts no roster) stays byte-identical and **exactly one gene moved**.
corpus/architecture/alignment_testing.md:269-281  [R4 default — presumed citable]  Go surface: **97.2% → 100.0% / 100% critical, 27/27, no divergences.** The gene **stays in the DNA** as a permanent fence over the whole wiring path (roster JSON → `LoadRoster` → `seedRosterMemberships` → store → `organizationWithEid` → HTTP).
corpus/architecture/alignment_testing.md:295-298  [R3 names a reachable artifact]  `rosetta-extensions/alignment/examples/toy/` is a self-contained proof: a `source` engine and a `mirror` engine that match **except for one intentional divergence** (`Greet/padded-name` — the source normalizes input whitespace, the mirror forgets to).
corpus/architecture/alignment_testing.md:315-316  [R3 names a reachable artifact]  `go test -tags alignment ./examples/toy/...` passes (the toy gate is 80% overall / 100% critical, and the divergence is a non-critical gene) while logging the tolerated divergence.
corpus/architecture/alignment_testing.md:390-391  [R3 names a reachable artifact]  Clerkenstein now drives **five DNAs via five runners** (`clerkrun`, `jsfapirun`, `expressrun`, `deployrun`, `multirun`) through the one `alignctl` — the clearest evidence the framework is surface-generic.
corpus/architecture/alignment_testing.md:419-422  [R4 default — presumed citable]  This is the discipline that keeps the seeder fleet (M7c) from silently breaking when the platform's data model drifts.
corpus/architecture/alignment_testing.md:429-429  [R4 default — presumed citable]  | Source = the live engine; **input → output** (two-sided) | **Source = the platform's live schema, introspected**; **output → schema** (one-sided — no input to compare) |
corpus/architecture/alignment_testing.md:433-438  [R3 names a reachable artifact]  The data-DNA does **two jobs**: (1) it **enumerates the seedable surfaces** — the machine-readable *catalog of seeders to build* that drives M7c (4 seeded in M7a, ~6 planned); and (2) it is the **conformance gate + drift detector** — `introspect` captures each seeded surface's shape from a live (migrated) stack as the contract, `measure` runs the structural operators against a seeded stack (a weighted `Overall` + an unweighted `Critical`, exactly like the behavioural score), and `diff` re-introspects to flag drift (exit-code contract `0 none / 1 moved / 3 usage`, mirroring `drift-check.sh`).
corpus/architecture/alignment_testing.md:440-445  [R3 names a reachable artifact]  **Where it breaks:** the operators are structural (FK/constraint/type), not value-equivalence, and the measurement is one-sided (seeder output vs the schema, not source vs mirror) — which is why this is a **separate harness** (`rosetta-extensions/stack-seeding/dna/`, the `datadna` CLI), not a new `alignctl` runner.
corpus/architecture/alignment_testing.md:440-445  [R4 default — presumed citable]  It connects directly to the stack's Postgres (offset port), never linking the platform.
corpus/architecture/alignment_testing.md:468-469  [R3 names a reachable artifact]  The snapshot dimension extends the **same** data-DNA harness (`rosetta-extensions/stack-seeding/dna/`, the `datadna` CLI) rather than spawning a third one — it shares the gene/score/criticality machinery.
corpus/architecture/alignment_testing.md:471-474  [R4 default — presumed citable]  `Coverage()` counts seeded **OR** snapshot-seeded over the non-waived denominator;
corpus/architecture/alignment_testing.md:510-511  [R4 default — presumed citable]  rosetta documents the discipline and ships the skills; **all executable machinery — the reusable harness *and* each mirror — lives in rosetta-extensions** and is consumed per-stack at a tag:
corpus/architecture/alignment_testing.md:515-515  [R3 names a reachable artifact]  | this doc — the alignment test class + method | `alignment/` — the reusable harness (`alignctl` + the toy) |
corpus/architecture/alignment_testing.md:516-516  [R3 names a reachable artifact]  | `/align-dna`, `/align-run` skills | each **mirror** section (e.g. `clerkenstein/`) — the mirror engine itself |
corpus/architecture/alignment_testing.md:517-517  [R3 names a reachable artifact]  | | the source's DNA(s) (the genome — e.g. Clerkenstein ships **five**, in `clerkenstein/alignment/dna/`) |
corpus/architecture/alignment_testing.md:518-518  [R3 names a reachable artifact]  | | the alignment tests + goldens + the engine's runner(s) (one per surface — `clerkrun`/`jsfapirun`/`expressrun`/`deployrun`/`multirun`) + the mirror's own `gate.sh`/`drift-check.sh` |
corpus/architecture/alignment_testing.md:520-528  [R4 default — presumed citable]  rosetta never contains executable alignment code — neither a specific mirror's source nor the reusable harness.
corpus/architecture/alignment_testing.md:542-542  [R3 names a reachable artifact]  Stdlib-only Go (module `anthropos.dev/alignment`) — builds and runs offline.
corpus/architecture/platform-migration-status.md:51-51  [R4 default — presumed citable]  | `running_but_unfederated` | the container still starts, but it owns no schema and is not a subgraph — a **husk**, not a service |
corpus/architecture/platform-migration-status.md:108-108  [R3 names a reachable artifact]  | `ai` | library | library | no | private Go module — the multi-provider `ai.AI` wrapper |
corpus/architecture/platform-migration-status.md:154-154  [R3 names a reachable artifact]  | `transcoder` | 2025-11-11 | media transcoding — adjacent to `media-substrate-spec.md`'s Bunny.net path |
corpus/architecture/platform-migration-status.md:214-214  [R4 default — presumed citable]  Re-check at each platform ref bump |
corpus/architecture/platform-migration-status.md:220-224  [R4 default — presumed citable]  It moved again at M257x iter-102 (22/69 → **25/70**), because repairing the messenger row's cross-process claim added four citations.
corpus/services/ai-labs.md:32-33  [R3 names a reachable artifact]  Two independent concerns absorbed into `app`, joined today only by a shared *reuse target* (the credit ledger), not yet by code:
corpus/services/ai-labs.md:51-52  [R3 names a reachable artifact]  **Codebase**: `app/internal/{labs,credits,payments,subscriptions}/` + `app/stripe/` (fixtures).
corpus/services/ai-labs.md:51-52  [R3 names a reachable artifact]  Go, part of the `app` monolith.
corpus/services/ai-labs.md:65-68  [R4 default — presumed citable]  Migration `20260617203555_add_labs_catalog.sql`.
corpus/services/ai-labs.md:71-77  [R3 names a reachable artifact]  `labsession.Server` implements the `lab.v1.LabSessionService` Connect-RPC (Create/Get/List/Cancel/ReportEvent); `Manager` holds the business logic the GraphQL resolvers call directly.
corpus/services/ai-labs.md:71-77  [R3 names a reachable artifact]  `labs_budget.go` is the **cap/tier engine**: `LabsTier` (`essential`/`professional`/`frontier`) → `modelsForTier`, `LabsBudgetOptions` stored as JSON on `OrganizationSettings.Options[labs_budget]`, `CheckRequest` → typed `CapError{Reason}` (`model_not_in_tier`, `per_session_cap`, `org_total`, `user_monthly`, `user_lifetime`) → Connect `ResourceExhausted`.
corpus/services/ai-labs.md:71-77  [R3 names a reachable artifact]  `spend_reconciler.go` is a **30 s pull loop** that denormalizes `spend_usd`/`total_tokens`/`status` from labs-api into `lab_sessions` rows (+ an orphan-fail-out pass).
corpus/services/ai-labs.md:80-83  [R3 names a reachable artifact]  **Table**: `lab_sessions` (Ent `lab_session.go`) — deliberately **no `PrimaryKeyMixin`**: `id` is a **String** supplied by labs-api.
corpus/services/ai-labs.md:80-83  [R3 names a reachable artifact]  `user_id`, nullable `organization_id`, frozen `template` (= `Lab.slug`), `model`, `mode`, `status` (booting/ready/grading/stopped/failed/cancelled), immutable `budget_usd`, denormalized `spend_usd`/ `total_tokens`, `grade_result` jsonb.
corpus/services/ai-labs.md:80-83  [R3 names a reachable artifact]  Migrations `20260529072659`, `20260617120000`.
corpus/services/ai-labs.md:90-94  [R3 names a reachable artifact]  `cost.go` — the cost model: `creditCost` map (`course.build`=**5/chapter**, `course.refine`=**1**, `course.translate`=**1/locale**), `purchasePackages` (starter 50 / team 200 / scale 500), `PricePerCreditUSD` = **$0.45** (derived from Bedrock COGS × `MarginMarkup` **1.40**).
corpus/services/ai-labs.md:90-94  [R3 names a reachable artifact]  `reconcile.go` — `ReconcileOrphanedBuilds` (refunds crashed pre-terminal Course Builder builds, idempotent) + `ReconcileOrg`/`MarginAlert` (internal per-org realized-margin; never served on customer routes).
corpus/services/ai-labs.md:95-98  [R3 names a reachable artifact]  **Tables**: `organization_credits` (Ent `organization_credit.go` — per-org `balance`, UNIQUE on `organization_id` = the one-purse-per-org invariant) and `credit_transactions` (Ent `credit_transaction.go` — append-only: `action_type`, signed `credits_delta`, `ref_id`, `meta`; a **partial-unique** `(organization_id, action_type, ref_id) WHERE ref_id IS NOT NULL` = the idempotency guard).
corpus/services/ai-labs.md:101-107  [R3 names a reachable artifact]  `subscription_verifier.go` implements `organization.StripeSubscriptionVerifier` so **org seat licensing derives seats from the real Stripe quantity**, not client input.
corpus/services/ai-labs.md:101-107  [R3 names a reachable artifact]  `subscriptions.SubscriptionManager` handles user premium subs + **feature-usage quotas** (cover-letters / job-applications / job-simulations) via Sentinel.
corpus/services/ai-labs.md:108-109  [R3 names a reachable artifact]  **Tables**: `subscriptions` (user-scoped), `stripe_customers`, `org_subscriptions` (org seat licensing: `licenses`/seats, `source` = stripe|manual, Stripe ids, period).
corpus/services/ai-labs.md:108-109  [R4 default — presumed citable]  Migration `20260626120000_add_org_subscriptions.sql`.
corpus/services/ai-labs.md:138-140  [R3 names a reachable artifact]  **Prerequisites**: runs **as part of `app`** (`go run .` / Docker).
corpus/services/ai-labs.md:138-140  [R3 names a reachable artifact]  Labs wire up only when `LABS_API_URL` is set (else the labs-api client is nil → in-memory idgen, reconciler no-op); credits seed lazily; payments need Stripe keys.
corpus/services/ai-labs.md:141-143  [R3 names a reachable artifact]  **Key env vars**: labs — `LABS_API_URL`, `LABS_API_PLATFORM_TOKEN`, `LABS_WORKSPACE_BUCKET`, `LABS_WORKSPACE_PREFIX`, `AWS_REGION`; Stripe — `STRIPE_SECRET_KEY`, `STRIPE_ENDPOINT_SECRET`; Meta — `META_PIXEL_ID`, `META_CONVERSION_API_TOKEN` (self-disables when unset).
corpus/services/ai-labs.md:147-148  [R3 names a reachable artifact]  **Dedicated cmds**: `cmd/labsimport` (idempotent catalog manifest loader), `cmd/labskey` (mint an org API key for `labs:write`), `cmd/labsdemo` (standalone local Labs-catalog server on `:8099`, in-memory store, dev only).
corpus/services/ai-labs.md:151-151  [R3 names a reachable artifact]  **Service Name**: none of its own — part of the `backend` service (`docker compose up -d backend`).
corpus/services/ai-labs.md:159-162  [R3 names a reachable artifact]  **Credits**: `cost_test.go` (`TestPricingDerivation` guards the $0.45/40 % derivation), `manager_test.go` (Balance/Debit/Refund/Purchase + concurrency + lazy-seed), `privacy_backstop_test.go` (`TestPrivacyBackstop_AutoScopesToCallerOrg` — proves the `OrganizationMixin` fences cross-org reads), `reconcile_orphan_test.go`.
corpus/services/ai-labs.md:163-164  [R3 names a reachable artifact]  **Subscriptions**: `subscriptions_test.go`.
corpus/services/ant-academy.md:19-19  [R4 default — presumed citable]  A companion **iOS / Android app** (Expo / React Native) that bundles the same content for offline reading
corpus/services/ant-academy.md:31-31  [R4 default — presumed citable]  | **Technology Stack** | Next.js 16 App Router + React 19.2 (React Compiler enabled), Expo / React Native (mobile) |
corpus/services/ant-academy.md:32-32  [R4 default — presumed citable]  | **Deployment** | **Vercel native** (no Docker, no docker-compose entry).
corpus/services/ant-academy.md:32-32  [R4 default — presumed citable]  Mobile builds via Expo. |
corpus/services/ant-academy.md:35-35  [R3 names a reachable artifact]  | **Repository** | `git@github.com:anthropos-work/ant-academy.git` → `stack-dev/ant-academy/` |
corpus/services/ant-academy.md:73-73  [R1 scaffolding]  The earlier "does not call backend services / read-only client" framing is retired (corrected v2.5 M231): progress persists via GraphQL mutations (`upsertChapterProgress[Batch]` / `setLastActivity`) to Ent tables `academy_chapter_progresses` / `academy_last_activities` / … in `app` (**plural** — Ent pluralizes; the singular forms are schema-file names, not table names).
corpus/services/ant-academy.md:129-129  [R4 default — presumed citable]  No PostgreSQL schema of its own, no Atlas migrations
corpus/services/ant-academy.md:130-130  [R4 default — presumed citable]  No Connect-RPC, no Redis Streams
corpus/services/ant-academy.md:131-134  [R3 names a reachable artifact]  **Provides** no GraphQL subgraph (it doesn't federate into the supergraph, which is `backend` alone) — but it **consumes** the platform's **`backend` subgraph** as a GraphQL *client* (see below); "no subgraph" ≠ "no GraphQL".
corpus/services/ant-academy.md:131-134  [R3 names a reachable artifact]  **There is no "academy subgraph" to consume either** — the academy types live inside `backend`; this bullet named one until M257x iter-108
corpus/services/ant-academy.md:179-180  [R3 names a reachable artifact]  **Endpoint unset** — `NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT` empty → `createServerGraphQLClient()` throws → `makeClient()` returns `null` → `getBackendCatalogView()` returns `null`.
corpus/services/ant-academy.md:182-182  [R4 default — presumed citable]  **Empty DB** — the query succeeds but the app academy tables have no rows → `[]`.
corpus/services/ant-academy.md:246-249  [R3 names a reachable artifact]  On a fresh up `ant-academy.sh`'s truncating write overwrites them; on a re-up the early-exit skipped that write, so the appended real key sat **last** and — dotenv last-wins — **dewired** Clerkenstein (keyless / sign-in wall).
corpus/services/ant-academy.md:297-301  [R3 names a reachable artifact]  **Switching language on a CHAPTER reader** re-runs `resolveServerChapterBody(slug, 'it')` (the chapter RSC reads `?lang=` via `localeFromSearchParams` and threads the locale) → in a demo that hit the **same backend-null 404** as #3. So it is **fixed by the M238 chapter-body patch**, which serves the locale-aware FS body (the `it/` overlay where present, else the canonical-EN fall-through).
corpus/services/ant-academy.md:307-307  [R4 default — presumed citable]  | **Framework** | Next.js 16 App Router + React 19.2 (React Compiler enabled, Turbopack default) |
corpus/services/ant-academy.md:313-313  [R4 default — presumed citable]  | **Mobile** | Expo SDK 54 / React Native (Expo Router) |
corpus/services/ant-academy.md:314-314  [R4 default — presumed citable]  | **Testing** | Vitest (happy-dom + node), Playwright (e2e). 1000+ Vitest tests + ~26 Playwright e2e spec files (tests/e2e/). |
corpus/services/ant-academy.md:326-326  [R3 names a reachable artifact]  Anonymous visitors browse it; the RSC swaps in `getPublicCatalogView()` for sessionless requests.
corpus/services/ant-academy.md:331-331  [R3 names a reachable artifact]  Both spellings, because `next.config.js` rewrites `_meta` → `meta` and the middleware sees the pre-rewrite path |
corpus/services/ant-academy.md:332-332  [R3 names a reachable artifact]  | Crawler / agent files | `/robots.txt`, `/sitemap.xml`, `/sitemap(.*)`, `/llms.txt`, `/llms-full.txt`, `/.well-known/(.*)` | Static, must be fetchable anonymously |
corpus/services/ant-academy.md:335-335  [R3 names a reachable artifact]  `NODE_ENV` is whitelisted, not blacklisted, so unset/`test`/typos stay closed |
corpus/services/ant-academy.md:354-354  [R4 default — presumed citable]  npm (web app uses npm, not pnpm)
corpus/services/ant-academy.md:355-355  [R4 default — presumed citable]  pnpm — only if you also want to run the mobile app
corpus/services/ant-academy.md:357-357  [R3 names a reachable artifact]  `FONTAWESOME_NPM_AUTH_TOKEN` survives in `code/.env.example` but is **not** needed to install or run.
corpus/services/ant-academy.md:401-401  [R3 names a reachable artifact]  The **app's** env file is `code/.env`, not the repo root:
corpus/services/ant-academy.md:464-464  [R3 names a reachable artifact]  *(This line said *"the platform's `/ant-*` skills in Rosetta"* until run 81 — **Rosetta ships no `/ant-*` skill**; none of the 16 in `.claude/skills/` begins with `ant-`.)*
corpus/services/ant-academy.md:472-472  [R3 names a reachable artifact]  | `benchmark-chapter` | Drive Playwright through a chapter for visual + content benchmarking |
corpus/services/ant-academy.md:483-483  [R3 names a reachable artifact]  **Web**: Pushed to Vercel via `.github/workflows/deploy-academy.yaml`
corpus/services/ant-academy.md:484-484  [R3 names a reachable artifact]  **Vercel env sync**: `.github/workflows/sync-vercel-env.yml` mirrors env vars
corpus/services/ant-academy.md:485-485  [R4 default — presumed citable]  **Mobile**: Expo build pipeline (outside platform CI)
corpus/services/ant-academy.md:486-486  [R3 names a reachable artifact]  **Coverage CI**: `.github/workflows/sidebar-coverage-tests.yaml`
corpus/services/ant-academy.md:488-488  [R3 names a reachable artifact]  Releases use **Cocogitto** conventional-commit tagging (`cog.toml`).
corpus/services/ant-academy.md:493-493  [R3 names a reachable artifact]  It is OpenAI-only and does **not** route through the platform's shared `ai` library or the `/api/ai/chat` route.
corpus/services/ant-academy.md:493-493  [R3 names a reachable artifact]  (The separate server-side `/api/ai/chat` route handler does support both OpenAI and Anthropic with server keys, but Cosmo does not use it.)
corpus/services/ant-academy.md:494-494  [R3 names a reachable artifact]  **Studio Desk (loose link)**: `NEXT_PUBLIC_STUDIO_URL` can deep-link from the academy to the Studio Desk UI; nothing required at runtime.
corpus/services/ant-academy.md:513-513  [R4 default — presumed citable]  If you ever need to add a Docker profile (e.g. for an integration-test harness), follow studio-desk's containerized variant as the template.
corpus/services/chronos.md:15-19  [R4 default — presumed citable]  **Key Functions**: *   **Timer Scheduling**: Accept requests to trigger events at specific future times *   **Timer Cancellation**: Allow scheduled events to be cancelled before they fire *   **Event Triggering**: Poll for due timers and publish events via Redis Streams *   **Health Monitoring**: Sentry cron monitor integration for reliability tracking
corpus/services/chronos.md:22-22  [R3 names a reachable artifact]  **Codebase**: `stack-dev/chronos` (or `git@github.com:anthropos-work/chronos.git`)
corpus/services/chronos.md:26-26  [R4 default — presumed citable]  **Database**: PostgreSQL (`search_path=chronos`)
corpus/services/chronos.md:42-42  [R4 default — presumed citable]  Chronos uses a simple single-table design for timer storage:
corpus/services/chronos.md:69-69  [R4 default — presumed citable]  | Pending | `completed_at IS NULL AND cancelled_at IS NULL AND scheduled_at > now()` |
corpus/services/chronos.md:70-70  [R4 default — presumed citable]  | Due | `completed_at IS NULL AND cancelled_at IS NULL AND scheduled_at <= now()` |
corpus/services/chronos.md:71-71  [R4 default — presumed citable]  | Completed | `completed_at IS NOT NULL` |
corpus/services/chronos.md:72-72  [R4 default — presumed citable]  | Cancelled | `cancelled_at IS NOT NULL` |
corpus/services/chronos.md:78-78  [R4 default — presumed citable]  **Service**: `ChronosService` (Connect RPC)
corpus/services/chronos.md:99-99  [R4 default — presumed citable]  When a timer fires, Chronos publishes a `TimerWentOff` event to Redis Streams:
corpus/services/chronos.md:118-118  [R4 default — presumed citable]  **Consumers**: Any service subscribed to the Chronos Redis Stream can react to timer events.
corpus/services/chronos.md:122-124  [R4 default — presumed citable]  **Upstream Consumers**: *   **Jobsimulation**: Schedules simulation timeouts, deadline reminders *   Any service needing delayed/scheduled events
corpus/services/chronos.md:125-127  [R4 default — presumed citable]  **Downstream Dependencies**: *   **PostgreSQL**: Timer persistence *   **Redis**: Event publishing via Redis Streams
corpus/services/chronos.md:158-158  [R4 default — presumed citable]  **Batch & Locking**: Each tick fetches up to 10 due timers (`LIMIT 10`) selected with `FOR UPDATE SKIP LOCKED`, so multiple instances never double-fire the same timer; the due window is `scheduled_at < now() + interval '1 second'`.
corpus/services/chronos.md:165-168  [R3 names a reachable artifact]  **Prerequisites**: *   PostgreSQL running with `chronos` schema *   Redis available for pub/sub *   Environment variables: `DB_CONNECTION`, `REDIS_ADDR`
corpus/services/chronos.md:198-198  [R3 names a reachable artifact]  | `PORT` | HTTP server port | `8080` |
corpus/services/chronos.md:199-199  [R3 names a reachable artifact]  | `RPC_PORT` | RPC server port | `8081` |
corpus/services/chronos.md:200-200  [R3 names a reachable artifact]  | `DB_CONNECTION` | PostgreSQL connection string | `postgres://...?search_path=chronos` |
corpus/services/chronos.md:201-201  [R3 names a reachable artifact]  | `REDIS_ADDR` | Redis server address | `localhost:6379` |
corpus/services/chronos.md:202-202  [R3 names a reachable artifact]  | `REDIS_STREAMS_INDEX` | Redis DB index for streams | `2` |
corpus/services/chronos.md:214-214  [R4 default — presumed citable]  **Scheduling a simulation timeout** (from Jobsimulation service):
corpus/services/customerio-sync.md:32-33  [R4 default — presumed citable]  CustomerIO Sync is the **background data-sync domain** that pushes platform users into the marketing platform as contacts, for marketing automation, lifecycle email campaigns, and product analytics.
corpus/services/customerio-sync.md:40-40  [R3 names a reachable artifact]  **Live code**: `app/internal/customeriosync/` (`sync.go` the push, `store.go` the read, `sync_query.sql` the query, `doc.go` the port record) + the manual tool `app/cmd/customerio-resync`
corpus/services/customerio-sync.md:48-48  [R3 names a reachable artifact]  Two things deliberately did not survive as-is, both recorded in `doc.go` rather than buried:
corpus/services/customerio-sync.md:50-50  [R4 default — presumed citable]  **The sync window is now stateless.** The standalone held `lastSyncTime` in memory and advanced it per tick.
corpus/services/customerio-sync.md:50-50  [R3 names a reachable artifact]  `app`'s worker is multi-replica and restarts on every deploy, so each run reads a fixed overlap window instead (wider than the 10-minute schedule).
corpus/services/customerio-sync.md:50-50  [R4 default — presumed citable]  Pushing a contact is idempotent (`CreateContact` with `UpdateEnabled`), so the overlap is free.
corpus/services/customerio-sync.md:51-51  [R3 names a reachable artifact]  **The scan is `database/sql`, not sqlc/pgx.** `app` carries no sqlc toolchain.
corpus/services/customerio-sync.md:91-91  [R3 names a reachable artifact]  **Brevo API** (external — `BREVO_KEY` in `platform/.env`)
corpus/services/customerio-sync.md:100-101  [R3 names a reachable artifact]  For protocol and field-mapping details, read the live code — `app/internal/customeriosync/`, whose `doc.go` is the port record — or clone the frozen repo for the pre-merge source:
corpus/services/customerio-sync.md:114-114  [R3 names a reachable artifact]  The domain is inside `backend` and **off unless you say otherwise**:
corpus/services/db-backup.md:55-55  [R4 default — presumed citable]  | **Source** | PostgreSQL RDS |
corpus/services/graphql-wundergraph.md:38-41  [R4 default — presumed citable]  **Primary Goal**: Serve the platform's Apollo Federation v2 **supergraph** from a WunderGraph **Cosmo Router** at one endpoint.
corpus/services/graphql-wundergraph.md:38-41  [R3 names a reachable artifact]  Since **cms-in-app v8.0** the supergraph composes a **single subgraph** — `backend`.
corpus/services/graphql-wundergraph.md:38-41  [R3 names a reachable artifact]  Counting entries in `supergraph-config-prod.yaml` at each commit:
corpus/services/graphql-wundergraph.md:45-45  [R3 names a reachable artifact]  | `749dc86~1` | — | **5** | `backend`, `skiller`, `jobsimulation`, `cms`, `skillpath` |
corpus/services/graphql-wundergraph.md:81-81  [R3 names a reachable artifact]  **Codebase**: `graphql-wundergraph` (local) — repo `git@github.com:anthropos-work/graphql-wundergraph`
corpus/services/graphql-wundergraph.md:82-82  [R3 names a reachable artifact]  **Runtime**: prebuilt Go binary image `ghcr.io/wundergraph/cosmo/router:0.275.0` (pinned)
corpus/services/graphql-wundergraph.md:84-84  [R4 default — presumed citable]  **Federation**: Apollo Federation v2, `federation_version: =2.3.2` (pinned)
corpus/services/graphql-wundergraph.md:85-85  [R4 default — presumed citable]  **Database**: none — stateless gateway (no DB, no Redis)
corpus/services/graphql-wundergraph.md:92-94  [R3 names a reachable artifact]  **There is no application source here.** `package.json` is a stub (`{"name":"graphql-wundegraph"}` — note the misspelling, carried in the repo).
corpus/services/graphql-wundergraph.md:112-114  [R3 names a reachable artifact]  The supergraph `config.json` is **baked into the image at build time** by `wgc router compose -i supergraph-config.yaml -o config.json`.
corpus/services/graphql-wundergraph.md:116-117  [R4 default — presumed citable]  Adding/changing a subgraph **or a single field** requires re-running `wgc compose` and **rebuilding + restarting** the image — there is **no hot reload**.
corpus/services/graphql-wundergraph.md:126-126  [R3 names a reachable artifact]  | `Dockerfile` | Uses the **committed `schemas/*.graphqls`** as-is | production CI build |
corpus/services/graphql-wundergraph.md:142-142  [R3 names a reachable artifact]  | `/graphql` | GraphQL | Unified federated endpoint.
corpus/services/graphql-wundergraph.md:142-142  [R4 default — presumed citable]  Playground + introspection ON in compose/dev, OFF in prod. |
corpus/services/graphql-wundergraph.md:146-146  [R3 names a reachable artifact]  | `supergraph-update.yml` | GitHub Action | `workflow_dispatch` bumps subgraph versions in `subgraphs.conf`, re-downloads SDL from GitHub Releases, opens a PR. |
corpus/services/graphql-wundergraph.md:150-151  [R3 names a reachable artifact]  Routing URLs use Docker **service names** on `app-network` (deliberately avoiding `host.docker.internal`/`extra_hosts` so it works on Docker Desktop *and* native Linux):
corpus/services/graphql-wundergraph.md:153-154  [R4 default — presumed citable]  **Historical — compose no longer has a router service to route.** Kept because the archived repo's configs still contain these rows and a reader will find them there.
corpus/services/graphql-wundergraph.md:158-158  [R3 names a reachable artifact]  | `backend` (the `app` service) | `http://backend:8082/graphql/query` | **the only surviving subgraph.** Named `backend`, maps to repo/service `app` — and now also serves everything the four folded subgraphs used to |
corpus/services/graphql-wundergraph.md:182-182  [R3 names a reachable artifact]  **Downstream (composed subgraphs)**: `app` (as `backend`) — and, historically, `jobsimulation` and `cms`.
corpus/services/graphql-wundergraph.md:183-183  [R4 default — presumed citable]  ~~**Compose `depends_on`**~~ — moot: there is no compose service.
corpus/services/graphql-wundergraph.md:183-183  [R3 names a reachable artifact]  Historically `backend`, `jobsimulation`, `cms`, **`storage`** (note `storage` was **not** a GraphQL subgraph but was in the startup-order list).
corpus/services/graphql-wundergraph.md:258-258  [R4 default — presumed citable]  These are **Docker build args** (not runtime secrets) — they select which config files get baked in.
corpus/services/graphql-wundergraph.md:262-262  [R3 names a reachable artifact]  | `ENVIRONMENT` | `compose` | Picks `config.<ENVIRONMENT>.yaml` → `config.yaml` (router runtime behavior).
corpus/services/graphql-wundergraph.md:263-263  [R3 names a reachable artifact]  | `ENVIRONMENT_CONFIG` | `compose` | Picks `supergraph-config-<ENVIRONMENT_CONFIG>.yaml` → which routing URLs are baked in. |
corpus/services/graphql-wundergraph.md:264-264  [R3 names a reachable artifact]  | `CONFIG_PATH` | `config.yaml` | Tells the Cosmo router which config file to load. |
corpus/services/graphql-wundergraph.md:266-266  [R3 names a reachable artifact]  | `VERSION` / `ARCHITECTURE` / `APOLLO_ELV2_LICENSE` | — | Prod CI build args (ECR image tag, `linux/amd64`, accept Apollo ELv2 license). |
corpus/services/graphql-wundergraph.md:272-274  [R3 names a reachable artifact]  Schema correctness is enforced at compose time by `wgc` (federation composition will fail the build on an invalid supergraph).
corpus/services/graphql-wundergraph.md:279-279  [R3 names a reachable artifact]  **Pinned versions**: cosmo router `0.275.0`, `wgc 0.104.0`, federation `2.3.2`.
corpus/services/graphql-wundergraph.md:280-280  [R3 names a reachable artifact]  **Repo name is misspelled** `graphql-wundegraph` in `package.json` and the repo's own `CLAUDE.md` heading.
corpus/services/messenger.md:13-15  [R3 names a reachable artifact]  Messenger is the platform's **centralized notification subsystem** — a standalone service until the v9.0 fold, a domain inside `backend` since.
corpus/services/messenger.md:13-15  [R4 default — presumed citable]  It sends and schedules transactional emails, using **Brevo** (formerly Sendinblue) as the delivery backend and **Liquid** templating for the bodies.
corpus/services/messenger.md:75-75  [R3 names a reachable artifact]  **Email backend**: Brevo via `getbrevo/brevo-go v1.1.3`
corpus/services/messenger.md:76-76  [R3 names a reachable artifact]  **Templating**: `osteele/liquid v1.8.1`
corpus/services/messenger.md:115-115  [R4 default — presumed citable]  Messages carry user info, template ID, and template params; the body is rendered through Liquid against those params before the Brevo send.
corpus/services/messenger.md:164-166  [R4 default — presumed citable]  The old first step — `cd platform && make dev S=messenger`, which stopped the container so the native process could take its port — is moot: there is no container, and the repo is not a sibling clone any more (`make init` does not fetch it), so `cd messenger` assumes you cloned it by hand.
corpus/services/messenger.md:185-185  [R3 names a reachable artifact]  | `PORT` | `8200` | HTTP port |
corpus/services/messenger.md:186-186  [R3 names a reachable artifact]  | `RPC_PORT` | `8201` | Connect-RPC port |
corpus/services/messenger.md:187-187  [R3 names a reachable artifact]  | `BREVO_KEY` | (empty) | Brevo API key. Empty → console sender.
corpus/services/messenger.md:188-188  [R3 names a reachable artifact]  | `REDIS_ADDR` | `redis:6379` | Redis address |
corpus/services/messenger.md:189-189  [R3 names a reachable artifact]  | `REDIS_STREAMS_INDEX` | `4` | Redis DB index for streams |
corpus/services/messenger.md:206-206  [R3 names a reachable artifact]  The flow handlers have unit tests (`assignments_test.go`, `jobsimulations_test.go`, `organizations_test.go`) covering the suppression / whitelabel branches.
corpus/services/roadrunner.md:76-76  [R4 default — presumed citable]  Roadrunner is the **code-execution proxy** for the platform.
corpus/services/roadrunner.md:76-76  [R4 default — presumed citable]  When a simulation includes a coding task, jobsimulation hands the user's source code to Roadrunner, which forwards it to **Judge0** (a sandboxed code-execution API) and returns the results (stdout, stderr, status, time).
corpus/services/roadrunner.md:78-78  [R4 default — presumed citable]  Roadrunner exists for one reason: it gives the platform a clean, language-agnostic boundary for running untrusted code without ever executing it in our own services or on our own infrastructure.
corpus/services/roadrunner.md:80-80  [R4 default — presumed citable]  It also runs an **Asynq** worker pool that polls Judge0 for results — **one task per single submission**, as § Async tasks below states.
corpus/services/roadrunner.md:123-123  [R3 names a reachable artifact]  The HTTP server (`PORT`, default `8080`) exposes only the `/_meta` health endpoint.
corpus/services/roadrunner.md:123-123  [R3 names a reachable artifact]  All code submission goes through Connect-RPC on `RPC_PORT` (default `8081`).
corpus/services/roadrunner.md:135-135  [R3 names a reachable artifact]  **Downstream**: Judge0 at `JUDGE0_BASE_URL=http://52.48.139.23:2358` (default in compose), Redis (Asynq backend)
corpus/services/roadrunner.md:193-193  [R3 names a reachable artifact]  | `PORT` | `8080` | `10400` | HTTP health port (`/_meta` only) |
corpus/services/roadrunner.md:194-194  [R3 names a reachable artifact]  | `RPC_PORT` | `8081` | `10401` | Connect-RPC port |
corpus/services/roadrunner.md:195-195  [R3 names a reachable artifact]  | `JUDGE0_BASE_URL` | — none | `http://52.48.139.23:2358` | Judge0 API endpoint |
corpus/services/roadrunner.md:197-197  [R3 names a reachable artifact]  | `SENTRY_DSN` | — none (optional) | — not set here | Sentry error-tracking DSN |
corpus/services/roadrunner.md:198-198  [R3 names a reachable artifact]  | `REDIS_ADDR` | — none | `redis:6379` | Redis address for Asynq |
corpus/services/roadrunner.md:199-199  [R3 names a reachable artifact]  | `REDIS_STREAMS_INDEX` | — none | `4` | Redis DB index for streams |
corpus/services/roadrunner.md:200-200  [R3 names a reachable artifact]  | `REDIS_WORKER_INDEX` | — none | `0` | Redis DB index for Asynq — **must parse as an int or the process exits immediately** |
corpus/services/roadrunner.md:201-201  [R3 names a reachable artifact]  | `ENVIRONMENT` | — none | `development` | Environment name |
corpus/services/roadrunner.md:205-205  [R3 names a reachable artifact]  Roadrunner currently has NO test suite — there are zero `*_test.go` files, so `go test ./...` (also run at Docker build time, `Dockerfile` line 18) is a no-op that passes vacuously.
corpus/services/skiller.md:66-67  [R3 names a reachable artifact]  **Localization**: per-skill / per-job-role translations (`skill_translations`, `job_role_translations`) across 8 `ContentLanguage`s carried over into `app`.
