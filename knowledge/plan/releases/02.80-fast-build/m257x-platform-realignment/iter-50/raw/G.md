# Seat G — adversarial DIFF audit, M257x iter-50

**Subject:** `git diff 2fc633a..47c9b7d -- corpus/` (commit `47c9b7d` *"fix(M257x/49): the twelve repaired by CLAIM, with two more fences as the post-condition"*).
All line numbers below are **at HEAD `57dfbfd`** unless stated otherwise.

## Hunk coverage (positive control)

**reviewed 20 of 20 hunks across 11 of 11 files.**
(`git diff 2fc633a..47c9b7d -- corpus/ | grep -c '^@@'` → `20`. The task prompt said 19; the canonical
`-U3` count is 20. Per-file: ai_architecture 2, architecture_overview 2, dependency_map 1,
external_services 3, security_compliance 1, coverage-protocol 1, stories-spec 1, platform-alignment 2,
ai-readiness 2, graphql-wundergraph 1, hiring 4.)

### Per-file `wc -l` at HEAD

| lines | file |
|---:|---|
| 302 | `corpus/architecture/ai_architecture.md` |
| 349 | `corpus/architecture/architecture_overview.md` |
| 103 | `corpus/architecture/dependency_map.md` |
| 814 | `corpus/architecture/external_services.md` |
| 265 | `corpus/architecture/security_compliance.md` |
| 1144 | `corpus/ops/demo/coverage-protocol.md` |
| 840 | `corpus/ops/demo/stories-spec.md` |
| 1142 | `corpus/ops/platform-alignment.md` |
| 634 | `corpus/services/ai-readiness.md` |
| 265 | `corpus/services/graphql-wundergraph.md` |
| 398 | `corpus/services/hiring.md` |

### Ground truth consulted

`stack-demo/app` @ `5ba17044` (incl. `app/studio` @ `aeec036a`), `stack-demo/platform` @ `2adcf714`,
`stack-demo/graphql-wundergraph` @ `60c229f3`, `stack-demo/storage` @ `4ce8ece5`,
`stack-demo/jobsimulation` @ `462343b0` (corroboration only),
`.agentspace/rosetta-extensions` (authoring copy), and a **live** running stack
(`demo-1-postgresql-1`) for the one claim only a database can settle.

---

## BLOCKERS

| # | corpus `file:line` | the false claim (quoted) | what is true + ground-truth citation | reader harm |
|---|---|---|---|---|
| 1 | `corpus/architecture/external_services.md:672` (**LEAK**, +2 more sites) | "**Coexists with ElevenLabs**: LiveKit + OpenAI Realtime powers new sessions (**gated by `flag_use_realtime_openai`**)" | The flag gates no session creation and does not select LiveKit over ElevenLabs. It has **exactly one read in the entire platform**: `app/internal/jobsimulation/calls/livekit.go:131-135`, **inside** `CreateAgentDispatch` — i.e. on the LiveKit path the request has already entered. Its only effect (`:140-144`) is `agentName = "anthropos-agent"` + `agentEndpoint = "openai-hosted"`. `grep -rn "flag_use_realtime_openai" stack-demo/` returns 2 hits, both that same line (`app/` and the frozen `jobsimulation/` husk). **This is the exact claim the same commit retracted at `corpus/architecture/ai_architecture.md:207`** — and it survives **4 lines below the hunk this diff edited, in the same section of the same file**, plus `corpus/services/jobsimulation.md:123` and `:126`. | A reader auditing voice-engine selection or residency flips the flag expecting an ElevenLabs→LiveKit cutover; what it actually does is move a **live** voice session off the Azure-EU endpoint onto `openai-hosted` — the opposite of a benign rollout gate. The retraction is defeated at 3 of 4 sites. |
| 2 | `corpus/services/hiring.md:296` (also `:31`, `:158`) — **OVERSHOOT + wrong construct** | "…while `jobsimulation.sessions` **still exists, frozen and unwritten, until M710**" (in the **`## Local development`** section, whose opening sentence is *"To make a hiring org's comparison scoreboard render on a **demo/dev stack**"*); and at `:31` "that schema **survives, frozen, until M710**" unqualified. | On a dev/demo stack the `jobsimulation` schema **does not exist at all**. (a) `stack-demo/platform/repos.yml:17-19` — `jobsimulation: migrations: false`; only `app` has `migrations: true` (`:10-13`). (b) `app`'s migration set contains **0** references to `jobsimulation.` (`grep -rn "jobsimulation\." app/terraform/migrations/*.sql` → 0) and its **only** `CREATE SCHEMA` is `auth` (`20230817154747_supabase_baseline.sql:2`). (c) **Measured live**: `docker exec demo-1-postgresql-1 psql -U postgres -d postgres -tAc "select nspname from pg_namespace where nspname not like 'pg\_%'"` → `auth, directus, extensions, information_schema, public, sentinel` — **no `jobsimulation`**; `to_regclass('jobsimulation.sessions')` → **NULL**, control `to_regclass('public.job_simulation_sessions')` → `job_simulation_sessions`. (d) **The cited mechanism is attached to the wrong construct.** `app/internal/askengine/registry.go:185-196` reads: *"We keep the dotted `jobsimulation.*` LLM-facing names as **logical aliases that RESOLVE TO THE PUBLIC TABLES**… `jobsimulation` stays in `rewriteSchemas` until M710"* — `:192` is about an **alias entry in a name-rewrite map**, not about a physical schema surviving. | This is seeding guidance. A reader building a hiring seeder against a demo stack is told a schema exists that their stack has never had; a write or a verification query against `jobsimulation.sessions` errors with `relation does not exist`. The correction fixed a real error (the schema was never *dropped by app*) by asserting a stronger claim (it *survives*) that is false in the exact environment the section addresses. |
| 3 | `corpus/services/hiring.md:241-242` (**LEAK, same file**) | "**Neither table exists** and there is no second subgraph, so **both halves collapsed into `public.job_simulation_sessions`.**" | This is verbatim the claim the same commit retracted 83 lines above: `hiring.md:157-159` now says "**`jobsimulation.sessions` itself was NOT dropped** … So what is gone is the **mirror half** of the old pair, **not both halves**", and `:35` records "(This bullet read *"`jobsimulation.sessions` was dropped"* from iter-23 until M257x iter-49.)" Refutation as in blocker 2: no `app` migration touches the schema; `20260722104506.sql:79` is a bare `DROP TABLE "sessions"` under `search_path=public` (`app/atlas.hcl:8`). The repairer fixed **2 of 3** sites in one file. | The document now contradicts itself in two directions within 90 lines. A reader who lands on the write-set section (the section the doc's own headline tells them to read before seeding) gets the retracted version and no fence. |
| 4 | `corpus/architecture/dependency_map.md:19` — **OVERSHOOT** | "\| **Storage** \| **-** \| **S3** only — plus a local-filesystem fallback per bucket. **No Postgres, no Redis:** …" | `stack-demo/platform/docker-compose.yml:213-217` declares, on the `storage` service: `depends_on: redis {condition: service_healthy}, postgresql {condition: service_healthy}`. This table's **own header sentence** (`dependency_map.md:9`) states it is *"Sourced from `platform/docker-compose.yml` `depends_on:` declarations and environment variables"* — so both the `-` in the *Depends On (Direct)* cell and the bolded "No Postgres, no Redis" are false against the declared source. The unedited twin `corpus/architecture/service_taxonomy.md:418` still (correctly) says the `storage` profile starts "postgresql, redis, sentinel + the named service". **The runtime half of the correction is right** — `grep -c redis storage/go.mod` → 0, and no `DB_CONNECTION`/`REDIS_ADDR` in storage's compose `environment:` block (they are set inline for sentinel `:18`, backend `:105-114`, cms `:163-170`, messenger `:232-260`). | Someone trimming a stack or writing a minimal `storage` bring-up reads "No Postgres, no Redis" in the **dependency map** and drops both; compose then blocks on two `service_healthy` conditions that can never be met. The corpus is also now self-contradictory between `dependency_map.md:19` and `service_taxonomy.md:418`. |
| 5 | `corpus/services/hiring.md:209-210` — **OVERSHOOT ("only")** | "It is `NOT NULL` (`20260722104506.sql:13`), `UNIQUE` (`:29`) and carries **no default** — **the *only* required-and-undefaulted column in the table**" | There are **four**. In the very DDL cited: `20260722104506.sql:6` `"owner_id" uuid NOT NULL`, `:7` `"sim_id" uuid NOT NULL`, `:10` `"sim_type" character varying NOT NULL` — none carries a `DEFAULT`. (Every other `NOT NULL` column in the CREATE does: `id` `:3` `gen_random_uuid()`, `created_at`/`updated_at` `:4-5` `now()`, `status` `:11` / `completion_status` `:12` `'pending'`, `interactions_progress` `:16` `0`, `score` `:17` `0`, `validation_version` `:18` `2`.) | The "only" is a checkable property of the table stated in repaired text and refuted by the file it cites two lines earlier. Harm is bounded — `owner_id`/`sim_id`/`sim_type` are already listed as non-null in the same sentence — but per the seat's mandate, a new absolute ("the only") that measurement refutes is a blocker in the same way the original omission was. |

---

## MINORS (7)

1. **`external_services.md:540` — the corrected count leaked past the table.** The *OpenAI Direct (US)* provider row still says "**two ways in**: (a) `vendor = Openai` from the caller … (b) automatic on **HTTP 429**". The same commit's new item 5 (`:580-585`) establishes a **third** route to `https://api.openai.com` — Studio-Room's `openai` `TARGET SERVICE`. The adjacent *Anthropic Direct* row (`:541`) **does** name Studio-Room, which is exactly what the new item 5 calls "an *internal* inconsistency"; the fix landed on the list and not on the table 29 lines above it.
2. **`ai_architecture.md:207-208` — "all it does" is too strong.** "…and **all it does** is swap the dispatched **endpoint** to `openai-hosted`." `calls/livekit.go:140-144` also resets `agentName = "anthropos-agent"`, which for a US-located sequence changes the dispatched agent from `anthropos-agent-us` back to the bare agent. The residency point is correct; the absolute is not.
3. **`stories-spec.md:603` — self-inflicted, same-commit anchor drift.** The new blockquote cites the never-completes retraction at `services/ai-readiness.md:371,449-450`. `:371` resolves. `:449-450` did resolve **before** this commit (at `2fc633a` the retraction sat at `:448-449`) — but the same commit added net **+11** lines to `ai-readiness.md` above that point, so at HEAD `:449-450` lands on `effectiveCycleId = selectedCycle ?? …` / `cyclesQ.isFetched`, an unrelated retraction about the cycle-param pointer bug. The target is now `:459-462`.
4. **`ai_architecture.md:193` vs `:202`.** The unedited bullet says "Voice engine is **selectable per simulation** in CMS"; the new paragraph 9 lines below says "Engine choice is **per SEQUENCE**". The code says sequence: `VoiceEngine` is a field of the `Sequence` struct (`app/internal/cms/directus/collections/jobsimulation.go:911`, struct opens `:834`) and is consumed as `seq.VoiceEngine` (`:1350`). The new text is right; its neighbour was not updated.
5. **`ai_architecture.md:38-70` still enumerates FOUR EU exits.** It states "**Two** levers inside the manager" (`:38`), "`ANTHROPIC_API_KEY` is a **third** exit" (`:42`), and the unset-`ai_vendor` route (`:50`) — and never mentions Studio-Room's `openai` `TARGET SERVICE`, even though `:45-48` explicitly discusses Studio-Room's ini `TARGET SERVICE` as the selector. No false *total* is stated (the ordinals stop at "third"), so this is an omitted list member, not a false count — but it is the fourth file in the family and the only one the five-way correction did not reach.
6. **`coverage-protocol.md:620` — "the twin **twelve** lines below".** The correctly-named `ai_readiness_snapshots` twin is at `:633`, ~14–15 lines below the corrective sentence at `:618-619`.
7. **`ai_architecture.md:203` anchor is one line short.** `jobsimulation.go:1079-1085` is cited for "a 4-member enum"; `:1079` is the `type SimulationVoiceEngine string` declaration, the `const (` opens at `:1081`, the four members are `:1082-1085`, and the block closes at `:1086`. It resolves; the range just clips the closing paren.

---

## Audited zeros — hunks read in full and found clean

- **`architecture_overview.md`, both hunks.** (a) The four→**five** / "the **three** outside the manager" edit is arithmetically consistent (2 in-manager + 3 outside = 5) and matches `external_services.md:569` and `security_compliance.md:184`, both of which the same commit moved together. (b) The multi-tenancy correction — "**23** schemas carry an `organization_id` with no policy at all (16 is the *neither-mixin* subset of those 23, not the total)" — I **re-derived independently** in `app/internal/data/ent/schema/`: `grep -l 'ent.Schema' *.go` → **135**; `OrganizationMixin{}` → **30**; `OrganizationIDMixin{}` → **7**; `comm -23 <(all) <(either mixin) | xargs grep -l '"organization_id"'` → **18**, of which `org_membership.go` declares its own `Policy()` and `academy_feedback.go` carries `UserMixin{}` → **16**; 16 + 7 = **23**; 30 + `Membership` = **31**. Every figure matches, and the only files declaring any `Policy()` are `mixin.go`, `org_membership.go`, `organization.go`, `user.go` — as `security_compliance.md:92-97` says. **No leak**: a corpus-wide grep for the retracted "16 … with no policy at all" total returns only the corrected site.
- **`security_compliance.md`, its one hunk.** Same five/three edit; consistent with its own Layer-1 derivation, which I re-ran above.
- **`external_services.md`, item 5 (the new EU exit).** `app/studio/services/ai.py:704` `def get_client`, provider dict `{openai: OpenAIProvider, azure: AzureProvider, anthropic: AnthropicProvider}` at `:705-709`, function body ends `:724` — the cited `:704-724` and `:706-708` both resolve. `:383` is `return OpenAI(api_key=self.api_key)` inside `OpenAIProvider._initialize_client` (class opens `:334`), with **no `base_url`** — so it does default to `https://api.openai.com`. `configs/config_template.ini:30-31` are exactly `OPENAI_API_KEY` / `OPENAI_ENDPOINT = https://api.openai.com/v1/chat/completions`.
- **`external_services.md:668` (the LiveKit agent-name correction).** `calls/livekit.go:110` and `:120` are both `agentName = "anthropos-agent"` (default and the `LocationEu` branch); `:126` is `fmt.Sprintf("anthropos-agent-%s", *location)`, with the comment at `:125` naming `anthropos-agent-us` explicitly. `grep -rn "anthropos-agent" stack-demo/` returns **0** hits for `anthropos-agent-eu` anywhere in the platform. Corpus-wide, `anthropos-agent-eu` is now **gone from both** of its former sites — this retraction did *not* leak.
- **`ai_architecture.md:180` (the diagram edit).** `anthropos-agent [EU] / anthropos-agent-us` matches the code exactly.
- **`ai-readiness.md`, both hunks.** `queryReadinessStarters` at `steps.go:915` (body to `:938`) is verbatim `SELECT DISTINCT user_id FROM public.ai_readiness_user_step_progresses WHERE organization_id = $1 AND user_id = ANY($2::uuid[]) AND status <> 'not_started'`; the quoted docstring *"This DB signal is the only real 'has started' check"* is at `:913`, inside the cited `:907-914`. `keepStartedMembers` (`readiness.go:684`) calls it at `:692` and filters via `filterStartedMembers` — so the "**PROGRESS ROW**, not step-1 evidence" correction is right, and right **in both directions** as the text claims. The M219 2.09 s refutation is fenced at every site (`seeding-spec.md:496-498`, `ai-readiness.md:371` and `:459-462`, `CLAUDE.md:324`).
- **`coverage-protocol.md`, its one hunk.** `readiness.go:771` = `func (m *Manager) buildResponseFromSnapshots`, `:772` = `m.ListAIReadinessSnapshots(...)` — exact. `live_snapshots.go:54-57` is precisely the `RefreshLiveSnapshots` docstring identifying `ai_readiness_live_snapshots` as "the queryable mirror the askengine ('Talk to Data') reads". The table-name correction (`ai_readiness_live_snapshots` → `ai_readiness_snapshots`) is correct, and the companion `readiness.go:308-312` nil-`CycleID`→`buildResponseFromSnapshots` route resolves (`:309` `if m.activeCycle(...) == nil`, `:311` the call).
- **`stories-spec.md`, its one hunk** (content — see MINOR 3 for the one anchor). The refutation is accurate and `seeding-spec.md:496-498` resolves to the M219 retraction blockquote.
- **`graphql-wundergraph.md:170`.** `graphql-wundergraph/ci/update-subgraph.sh` contains **exactly one** `gh release download` (`grep -c` → 1), at line **9**, `-R anthropos-work/app`. Commit `915da06` ("fold cms subgraph into backend (supergraph 2→1)") explicitly records dropping "the gh-release-download in `ci/update-subgraph.sh`" for jobsimulation and deleting `schemas/cms.graphqls`. The correction is exactly right, including the "the two bullets above it already carried their historical fence" observation.
- **`hiring.md` hunks 1–3, the migration corrections** (the substance, minus blockers 2/3/5). `20260729133514.sql:13-23` is a link-id **remap** (`UPDATE organization_assignment_sessions SET session_id = l.skill_path_session_id …`), `:58-62` is *"-- 5. Drop the mirrors"* + `DROP TABLE "local_jobsimulation_sessions"`, and `grep -rn 'SET "score"' app/terraform/migrations/` → **0**, plus a looser `set +"?score"? *=` → 0. **There is no score back-fill** — the correction is right and the "back-fills" wording does not survive anywhere in the corpus. `20260722104506.sql:79` is a bare unqualified `DROP TABLE "sessions";`, `:2` creates `job_simulation_sessions`, `app/atlas.hcl:8` pins `search_path=public`, and the only `CREATE SCHEMA` set-wide is `auth`. `token` is `NOT NULL` at `:13` and `UNIQUE` at `:29`, and `rosetta-extensions/stack-seeding/seeders/persona_write.go:152-158` (`sessionCols()`) does list `"token"` — every anchor in the `token` addition resolves except the "only" (blocker 5).
- **`platform-alignment.md` hunk 1** (`hiring.md:86` → `hiring.md:93`). Content-faithful: pre-diff `hiring.md:86` and post-diff `hiring.md:93` are the **identical** line (*"> server-side BAPI (`clerk-backend/resources.go` `organizationWithEid`) emits its own `{eid}` copy but is **not** what the"*) — the +7 shift from this commit's own hunk 1 was applied correctly. A model re-pin.
- **`platform-alignment.md` hunk 2** (the `--audit-commit` self-indictment blockquote), to the extent it is checkable against rext: `--audit-commit` exists (`stack-core/repair_postcondition.py:509` flag, `:582` dispatch, `:418` `grade_audit_commit`); the "hermetic temp-git-repo fixture" exists and is what the text describes — `stack-core/tests/test_repair_postcondition_audit_mode.py:140-203`, class `TheModeWorksEndToEnd`, `_repo()` builds a `tempfile.TemporaryDirectory()` git repo with a corpus file, a ledger, and two differently-shaped commits, with the file's own comment at `:155` reading "It is hermetic, it never goes stale, and it exercises the whole [pipeline]". The live-tree probe survives as `_repo_available()` (`:48-58`).

---

## Unverified

1. **The five LiveKit agent repositories** (`ai_architecture.md:183-185`) — `gh` is unavailable and none of `livekit-agent*` is cloned. Unchanged by this diff; neither passed nor failed.
2. **`platform-alignment.md:1096-1103` process history** — "shipped `--audit-commit` with five end-to-end tests … all five passed … all five failed inside the same session", and "**the eighth consecutive iteration** in which the author of a rule broke it". These are recorded in `knowledge/plan/**`, which the briefing forbids me to open. The *artifacts* the narrative describes all exist (above); the *sequence of events* and the count "eighth" are unchecked.
3. **Whether Studio-Room's `openai` `TARGET SERVICE` is ever actually SELECTED.** Both shipped configs set every `*_AI_*_MODEL` to `azure` (`app/studio/configs/production_config.ini`, `development_config.ini`). The arm exists and would reach `api.openai.com` unconditionally, and the corpus says "**can** send a request outside the EU", so the claim stands as written — but this is a **latent** arm in the shipped configuration, not an observed live path. The identical caveat applies to item 3's `anthropic` arm, which the corpus has asserted since iter-48.
4. **Whether the `jobsimulation` schema physically exists in production.** Blocker 2 is measured against the `app` migration set, `repos.yml`, and a live **demo** stack. Production may still carry the schema created by the decommissioned service's own migrations — which is precisely why the unqualified "survives, frozen, until M710" needs an environment qualifier rather than deletion.
5. **`colony` / `proto` / `taxonomy`** are not cloned; no claim in this diff depended on them.
