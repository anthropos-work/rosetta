---
iter: 110
milestone: M257x
iteration_type: tok
tok_flavor: deliberate
status: in-progress
opened: 2026-08-06
---

# iter-110 — TOK-07: enumerate the predicate, not the anchor

**Type:** tok (**deliberate** — author-initiated, **not session-terminating**; same flavor and same
justification as `TOK-06`, whose entry states the rule)

## Why a tok now, graded out loud

**It is not the 3-no-prog streak, and the streak could not have fired.** The last three tiks are
iter-107 (`TOK-06` step 2 — no reading), iter-108 (step 3, the repair — no reading, and it says so in the
mandated words), and iter-109 (the read). Under §9's iter-type refinement an iter that took **no reading**
has an **UNMEASURED** metric, not an unmoved one. Exactly **one** of the last three tiks measured. The
trigger's precondition — three tiks that DID measure and did not move — is unestablished, so Phase 0
rule 2 falls through to tik and this tok is authored deliberately, as `TOK-04`, `TOK-05` and `TOK-06` all
were.

**It does not terminate the call**, for `TOK-06`'s reason rather than as a licence. A *triggered* tok
exits so the user can review a revision before the next tik commits to it. Here there is nothing
unreviewed to commit to: **iter-109 produced the measurement AND named the replacement**, and every
element of the sequence below is an item iter-109's own close already routed
(`FIX-M257x-iter109-read-union` with its binding change of scope,
`FIX-M257x-iter109-repair-scope-is-detection-bounded`, and the re-rank of
`FIX-M257x-iter107-drift-fence-satisfiable-by-prose`). This tok **sequences routed work**. It opens no new
territory, so it closes and the loop continues into tiks in the same call.

## Inputs

- iter-109 `adjudication.md` — `P = 24` / `N = 36` at corpus `ac48e5b`, the pre-registered `P ≥ 15` branch
- iter-109 `decisions.md` — `D-M257x-109-3` (premise refuted) and `D-M257x-109-4` (repair scope is
  detection-bounded)
- `TOK-06` (milestone-root `decisions.md`) — the strategy under revision
- iter-96 / iter-98 / iter-102 / iter-108 closes — the repair-multiplier series
- `hardening-ledger.md` pass 23–25 — the two routed items with teeth, and the second cap-without-stabilization

## Deliverable

A `TOK-07` entry in the milestone-root `decisions.md` that:

1. records what iter-109 refuted and what it **kept**;
2. states the binding constraint as a **measured** multiplier collapse, not an atmosphere;
3. gives the next tiks a step order with a named first step; and
4. **pre-registers its own falsification** before the next number exists.

## Phase plan

Strategy authoring only. No corpus repair, no fence authored, no reading taken, no clone fetched
(§5 rule 41a), no stack touched, no tag cut. `N` is **not** re-measured and **no `N` movement is claimed**.

## Escalation conditions

If the derivation had shown iter-109's finding was already covered by `TOK-06` as written — i.e. the
revision would be a restatement — this iter would close `closed-no-lift` with that falsification rather
than manufacture a seventh strategy. It does not: `TOK-06` puts the read last (kept) but scopes the repair
to the previous reading's detections (the defect), and nothing in its text constrains the repair's
denominator.

## Acceptable close-no-lift outcomes

n/a for the metric — a tok claims no metric delta by construction. The tok's own deliverable is the
strategy record; it closes `closed-fixed` if the entry lands with all four elements above, and
`closed-no-lift` if the derivation falsifies the need for it.
