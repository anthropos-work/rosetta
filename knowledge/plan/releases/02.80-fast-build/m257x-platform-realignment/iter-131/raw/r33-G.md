# Seat G — reading #33/#34 (M257x iter-131)

Ground truth actually read (stated per §5 rule 26 / addendum):
`app` **`ad9f3c49`** (checkout == origin/main) and, where a claim pinned it, `b948604f`, `5ba17044`,
`2035f9a`, `v1.363.2`/`5ba17044`; `platform` **`0c91421d`** (+ `0dab54d`, `838d907` for historical
cells); `next-web-app` **`8297c684`** (+ `bb3313bc`, `f97ba6599` where pinned); nested
`stack-demo/app/studio` at its OWN ref **`aeec036a`**; frozen `cms ca50c817`, `messenger fa47850d`
(+ `origin/main e9421c68`), `roadrunner 87d8d443`, `graphql-wundergraph 60c229f3`, `storage 4ce8ece5`;
rext **pinned per-stack clone `09d06070`** for tooling-on-a-stack claims, **authoring copy** only where
a claim named `63ce41a` (which resolves in the authoring copy and **not** in the pinned clone).

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/services/hiring.md` | 488 | yes |
| `corpus/architecture/org-repos.md` | 466 | yes |
| `corpus/architecture/ai_architecture.md` | 354 | yes |
| `corpus/services/next-web-app.md` | 188 | yes |
| `corpus/services/coursebuilder.md` | 161 | yes |
| `corpus/architecture/README.md` | 39 | yes |

Search-pipeline controls run in-pass: `git grep -il clerk -- corpus/ CLAUDE.md` → 64 files (non-zero);
`git grep -ic azure aeec036a` in the nested studio repo → 19 files (proves the nested-repo grep works
before I trust its zeros); `SET ` across the migration set → non-empty (before trusting `SET "score"` → 0);
`@tabs` in `apps/` → 46 (before trusting the intercepting-route zero).
**Two of my own commands failed silently mid-pass and returned zeros/blanks** — a `git -C $A` with an
unexpanded variable, and a `for spec … set --` loop that produced `lines=0` for four terraform files that
are 100–191 lines long. Both were re-run rather than believed. Recorded because it is §5 rule 2 live.

---

## Blockers

### B1 — `JUDGE0_BASE_URL` cited at `app/terraform/main.tf:638-639`; that line is an ElevenLabs webhook secret

- **Anchor:** `corpus/architecture/org-repos.md:227` — *"`services.tf:385` → `app/terraform/main.tf:638-639` `JUDGE0_BASE_URL`, consumed in-process at `app/internal/jobsimwiring/wiring.go:123`."*
- **Why it is false:** The sentence pins no ref, so it grades at the checkout. At `app` **`ad9f3c49`**,
  `terraform/main.tf:638` is `"valueFrom": "${aws_ssm_parameter.elevenlabs_webhook_secret.arn}"` and `:639`
  is a closing `},` — inside the `ELEVENLABS_WEBHOOK_SECRET` block that opens at `:637`.
  `JUDGE0_BASE_URL` is at **`terraform/main.tf:531`** (`"name": "JUDGE0_BASE_URL"`, `:532`
  `"value": "${var.judge0_base_url}"`). I also tried the frozen briefing sha **`b948604f`**, where it is at
  **`:329`**. The anchor therefore resolves at *neither* candidate ref and names a different secret at both.
  (`JUDGE0_API_KEY` is a third, separate site: `:693` @ `ad9f3c49`, `:491` @ `b948604f`.)
  The rest of the chain is correct and I verified it: `wiring.go:123` is
  `jsrunner.NewRunnerManager(getenv("JUDGE0_API_KEY"), getenv("JUDGE0_BASE_URL"))`. Only the terraform
  hop is mis-anchored — which is the load-bearing hop, since the whole § 5 point is *how the opaque tfvar
  reaches the app*.
- **Confidence:** high

### B2 — the hyper-studio `.env.example` borrow is cited at `secrets-spec.md:309`; it is at `:344`

- **Anchor:** `corpus/architecture/org-repos.md:370` — *"leaving it out entirely already misstated something else — [`secrets-spec.md:309`](../ops/secrets-spec.md) uses `../hyper-studio/.env.example` as the template for `app`'s five AWS Bedrock secret genes."*
- **Why it is false:** `corpus/ops/secrets-spec.md:309` is a row of a **different table, about a different
  repo**: `` | `platform/ANTHROPIC_API_KEY` · `platform/OPENAI_API_KEY` · `platform/LIVEKIT_API_KEY` ·
  `platform/LIVEKIT_API_SECRET` | standard | **required** | `key-present`, `nonempty` | `` — `platform`-repo
  secrets, no Bedrock, no hyper-studio. The claim's actual site is **`secrets-spec.md:344`**:
  *"**The 5 genes** (all on the `app` repo, target `app/.env`; the `../hyper-studio/.env.example` template):"*,
  with the Bedrock class headed at `:333` and `:351` naming the template again. A tree-wide
  `grep -rn hyper-studio corpus/` returns exactly 5 sites and `:309` is not among them.
  **The same wrong anchor has already propagated**: `corpus/architecture/platform-migration-status.md:160`
  also writes *"`secrets-spec.md:309` already borrows its `.env.example`"* — so this is the cross-file
  drift class (§5 rule 19), not an isolated typo. Booked as a BLOCKER rather than line drift because :309
  names a different repo's secrets in a different table, so a reader who opens it concludes the claim is
  unsupported.
- **Confidence:** high

### B3 — "the corpus documents no observability tier at all" is refuted by its own next sentence, and its stated derivation no longer reproduces

- **Anchor:** `corpus/architecture/org-repos.md:43` — *"the corpus documents **no observability tier at all** (`git grep -i grafana -- corpus/ CLAUDE.md` → **0 files**), and that repo holds both the platform's live outside-in monitoring and a **production read path** no safety doc enumerates. See [`observability.md`](../ops/observability.md)."*
- **Why it is false:** I ran the document's own command verbatim. `git grep -il grafana -- corpus/ CLAUDE.md`
  returns **3 files**, not 0: `corpus/architecture/org-repos.md`, `corpus/ops/README.md`, and
  **`corpus/ops/observability.md`** (9,034 bytes on disk). The last one is the file this very blockquote
  links **two lines later**, and which the register lists again at `:45`, `:77` and `:465`. So the sentence
  asserts, in the present tense, that no corpus doc covers observability while pointing at the corpus doc
  that covers observability — the *identical* self-refuting-universal-quantifier defect this same file
  books against the census at `:37` (*"**self-refuting**, since they say it while naming them"*) and that
  `ai_architecture.md:208-212` books against its own earlier text (*"A universal quantifier falsified by
  its own sentence is the defect, not the staleness"*).
  I hesitated because one can read the paragraph as reporting a census taken *before* `observability.md`
  was written. It does not say that: the clause is present-tense, the derivation command is offered as
  reproducible, and no ref or date scopes it. Per §5 rule 33 a block that asserts currency cannot be pinned
  into silence — and here there is no pin at all.
- **Confidence:** medium

### B4 — `mistral-ocr-latest` cited at `studio/tools/pdf2md.py:24`; `:24` is a bare SDK import, the literal is at `:127`

- **Anchor:** `corpus/architecture/ai_architecture.md:40` — *"Python-side in the in-image studio tree at `studio/tools/pdf2md.py:24` (`mistral-ocr-latest`) — a standalone CLI…"*
- **Why it is false:** Read at the nested repo's **own** ref (`stack-demo/app/studio` is its own checkout at
  **`aeec036a`**; grepping it from the `app` host ref returns 0 for everything, which is the addendum's
  trap). `tools/pdf2md.py:24` is `from mistralai import Mistral` — an import statement, no model id.
  `mistral-ocr-latest` is at **`:127`** (`model="mistral-ocr-latest",`), the sole occurrence in the tree.
  What makes this a BLOCKER rather than drift is that the *same bullet*, one clause earlier, retracts an
  anchor for exactly this reason — *"this row cited `:19` until M257x iter-115, and `:19` is a
  **doc-comment** line … not code"* — and is scrupulous about the Go half (`markdownManager.go:30` is
  verbatim the constructor body; `:29` is verbatim the `func` line; both re-verified correct). The bullet
  applies its own standard to one clause and violates it in the next.
  Everything else in the bullet is true and I verified it: `git -C app/studio grep -i mistral aeec036a` →
  **22 hits / 3 files** (exactly as claimed: `requirements.txt`, `tools/pdf2md.py`, `tools/r3.py`), and
  `bedrock|boto3` → **0** against a 19-file `azure` positive control.
- **Confidence:** medium

---

## Minors

- `corpus/services/hiring.md:245` — BA-4 cites `persona_write.go:69-71` for *"three tables … the `PersonaSeeder` also writes"*. At the named ref `63ce41a` (authoring copy — it does **not** resolve in the pinned `09d06070` clone), `:69-71` is the comment explaining that the *removed* `jobsimulation.sessions` step was not re-pointed. The three-table mapping list is `:66-68`; the actual writes are `:92-94`. Line drift onto adjacent prose in the same comment block; the claim is true.
- `corpus/services/hiring.md:219` and `:230` — the best-attempt window is cited as `intelligence.go:2158-2160` "ORDER BY score DESC". `:2158` is the `ROW_NUMBER() OVER (…)` format string, `:2159-2160` the `PARTITION BY sim_id, owner_id` args; `FieldScore` is at **`:2161`**, one line past the cited range. Off-by-one range end; construct correct.
- `corpus/services/coursebuilder.md:129-130` — "~55 test files" measures **61**, "~25 boundary test files" measures **33** at `ad9f3c49` (32 % high on the second). Tilde-hedged, non-material.
- `corpus/architecture/org-repos.md:381` — "this corpus's **34,417** across **90** files" measures **36,192 / 92** today; the delta is the register itself plus `observability.md`. Self-referential staleness, harmless.

---

## Positively cleared (audited zeros)

**ENUMERATED** — predicate re-derived from source independently, cardinality compared:

- `coursebuilder.md:77-85` **SSE wire contract, 16 names.** I did not check the doc's list; I enumerated
  every `writeSSE` call site in `handler.go` @ `ad9f3c49`: exactly **two** emit (`:1458` literal `"session"`,
  `:1479` via `renderEvent`), plus the declaration `:3131` and one comment. `renderEvent` (`:2604-2772`)
  returns **15** distinct non-empty names. 15 + 1 = 16. Every per-name anchor the doc lists resolves
  exactly, `cost` really does `return "", nil` at `:2717` under the quoted D2 comment, and `:1478-1480`
  really is the skip. This is the strongest clearance in my set.
- `coursebuilder.md:266-268`-adjacent / `hiring.md:266-268` **"four required-and-undefaulted columns."**
  Re-derived the SET from the DDL (`20260722104506.sql:2-27`) rather than trusting the four cited lines:
  NOT NULL *without* a DEFAULT is exactly `owner_id :6`, `sim_id :7`, `sim_type :10`, `token :13` — 4/4,
  line numbers exact, and every other NOT NULL column does carry a DEFAULT. Column count re-derived
  independently: **23**, matching `hiring.md:182`; no later migration ALTERs the table.
- `hiring.md:261-263` **`completion_status` is a closed 5-value enum.** `enum/jobsimulation.go:29-35` +
  `Values()` `:37-43` = pending/passed/failed/discarded/timedout, no `SIMULATION…` member. The SDL twins
  `jobsimulations.graphqls:14` and `:128` each declare exactly those five lowercase members, and both
  gqlgen marshalers (`graph.go:129546-129554`, `:129392-129400`) are bare `MarshalString` passthroughs with
  no membership check — so the doc's "INSERTs cleanly and travels verbatim to the browser" is right.
- `hiring.md:353-354` **`showWorkforce` is passed `false` in exactly two places, both in `apps/hiring`.**
  Enumerated all 17 `showWorkforce` occurrences repo-wide @ `8297c684`; the only `={false}` sites are
  `apps/hiring/…/template.tsx:167` and `:248`. `orgGroups.ts:48-65` genuinely takes no `isHiringOrg`
  parameter, `intelligence: isAdmin` at `:61`. The retraction holds.
- `hiring.md:459` **"the regression enumerates all 8 generic seeders."** Read the table in the **pinned**
  clone `09d06070` (`hiring_scope_test.go:81-95`): 8 rows exactly.
- `ai_architecture.md:92` **"five families over six consts."** Enumerated `internal/ai/anthropic/completion.go:20-30`
  @ `ad9f3c49`: 6 consts → families 3.5 (×2: Bedrock-EU + direct), 3.7, 4, 4.5, 4.6 = 5. Exact. And 4.6
  really is the live production pin at all three cited sites (`askengine/bedrock.go:25`,
  `report_agent.go:31`, `coursebuilder/bedrock.go:29`).
- `ai_architecture.md:91` **the OpenAI row.** `internal/ai/openai/config.go:8-26` holds exactly 11 consts;
  the row lists exactly those 11, one-to-one.
- `ai_architecture.md:94` **Speech "the whole set."** `internal/ai/speech.go:9-12` is the entire
  `SpeechModel` const block: `GPT4oMiniTTSS` + `DefaultModel` aliasing it. No TTS-v2 consts. Retraction correct.
- `ai_architecture.md:67-71` **five enum members vs four switch cases.** Directus `AIVendor` const block
  `:969-975` = 5 (`azureglobal` at `:971`); `simulator/ai/ai.go` vendor switch arms enumerated
  independently = `:58`, `:69`, `:86`, `:102` + `default: :114`. Exactly as claimed.
- `next-web-app.md:22` **all four apps declare `~16.2.12` (tilde).** Enumerated all four `package.json`
  files; `apps/mobile` declares no `next`. Also enumerated: `middleware.ts` exists **nowhere** (0 paths),
  three `src/proxy.ts` files, exactly one Dockerfile (`Dockerfile.dev`), 8 locale dirs
  (de en es fr it ja nl pt), production deploy matrix = web/hiring/integration (no maintenance),
  no `storybook` script and no `.storybook/` dir (only `configs/tailwind/storybooks.css`),
  `GRAPHQL_SCHEMA_FOR_GEN` = 4 hits at `f97ba6599`, all `.env.example`, with `codegen.ts:9` hardcoding
  the endpoint. Every one of these matched.
- `architecture/README.md:22` **the `ai`-library fold.** Enumerated `anthropos-work/ai` across every
  checked-out `go.mod`: absent from `app` and `sentinel`, present only in the frozen `cms`/`jobsimulation`
  husks (both `v1.40.2`). `app/internal/ai/` exists; `1e457fa70` is real and its subject is verbatim
  *"refactor(ai): fold the ai library into app as internal/ai"*. The `authn` half too: 0 hits across all
  eight clones' `go.mod`+`go.sum`, against a working `colony/authn` positive control.
- `hiring.md:44,206,383` **`repos.yml` counts.** 6 entries @ `0dab54d`, 4 @ `838d907`, 4 at the current
  `0c91421d`; `app` at `repos.yml:14-17` is the only `migrations: true`; `jobsimulation` in none of them.
- `ai_architecture.md:135-141` **the studio slot table.** Read at the nested repo's own ref: all five
  slots exact, stable and experimental byte-identical, `development_config.ini:26-36` `diff`-identical to
  production, `config_template.ini:39/40` both `gpt-4o`, and `gpt-5.2` genuinely appears in **no**
  `configs/*.ini` (only `services/ai.py:356,508` + changelog/knowledge prose).

**RE-DERIVED only** (weaker — arithmetic the document displays, not an independent set):

- `coursebuilder.md:148` "six releases on from `v1.363.2`" — `git describe` reproduces
  `v1.369.0-7-gad9f3c498` and `v1.369.0-2-g2035f9a40` exactly, `v1.363.2` is `5ba17044`, newest tag is
  `v1.369.0`.
- `org-repos.md:382` "18 + 6 + 3 = 27 service docs" — the total is right (`corpus/services/*.md` = 29,
  minus `README.md` and `TEMPLATE.md`), but the 18/6/3 split ranges over AKB, which I cannot read.

**Exact-anchor spot checks that all resolved to the described construct** (~120 in total; the ones worth
naming because they were the likeliest to have rotted): the whole `hiring.md` re-grounding banner including
its **non-uniform offset** claim — `manager.go :450→:448` (−2), `:453→:451` (−2), `:537→:487` (−50) — each
verified line-for-line at both `ad9f3c49` and `5ba17044`, which is the rare case of a doc explicitly
warning against the arithmetic shortcut and being right to; `20260729133514.sql:58-62` (*"5. Drop the
mirrors"* + the `DROP TABLE`), with `SET "score"` = **0** set-wide against a working control; the only
`CREATE SCHEMA` in the entire migration set is `auth`; `atlas.hcl:8` `search_path=public`;
`simulationScoreColumn.tsx` **byte-identical** across `apps/web` and `apps/hiring` (same `shasum`);
`coursebuilder`'s terraform trio (`variables.tf:759-763`, `ssm.tf:328-333`, `main.tf:757-758`) **and** its
retraction of the old `variables.tf:635-638`/`main.tf:555` pair (`:631-645` really is the cms-in-app
comment block, `:555` really is `DIRECTUS_BASE_ADDR`); the coursebuilder COGS defaults (monthly **500**,
daily **0 = off**) where the corpus is right and the repo's own `SPEC.md:307` is stale; the credit prices
(`creditCost` map `cost.go:86-90` = build 5 / refine **1** / translate 1) where again the corpus matches
the code and the package doc-comment at `:29` is stale; `livekit.go:140-144` doing **two** things
(`:142` agent-name reset, `:143` endpoint swap); `voiceEngineFromDirectus` pinned deliberately to `:1597`;
the evaluation-system chain (`skills.go:40-51` with its `// TODO fix this formula`, `:53-64`, `:75`;
`criterion.go:127/168/428/450-475`; `validator.go:43/60-61/595`); and every `next-web-app`/compose anchor
(`docker-compose.yml:110,112,135,143,151,160,165-167`, `Makefile:119-120`).

---

## What I could not settle

- **Most of `org-repos.md`.** Its load-bearing rows describe repos in **no** clone set — `infrastructure`
  (every `services.tf:NN` citation, including the `cms` M810 settlement, the ten-module production set at
  `13c248e6`, and the `storage`/`jobsimulation` assets-only survivals), `directus`, `judge0`, `metabase`,
  `AI-Labs`, the five `livekit-agent*` repos' own trees, `sim-qa`, `hyper-studio`, `ant-observability`,
  `anthropos-knowledge-base`. I verified every *platform-side* half of those claims that our clones can
  reach — the 7 sim-qa mutation anchors (`jobsimulations.graphqls:744/753/762/761/807/821` + `:750`
  `isTest`), `manager.go:445` `IsTest`, the `livekit.go` dispatch anchors, `jobsimulation.go:1085`
  `"livekitchain"`, `app/go.mod:12` and `:14`, `payments/handler.go:302-316`, the directus-emitter claims in
  the **pinned** rext clone (`gen_injected_override.py:325` pins the stock image, no `volumes:` in the
  block; `dev-setdress.sh:255` the same) — and all of them held. What would settle the rest is a clone of
  `infrastructure` at `13c248e6`; until then §§ 3–11 are largely unfalsifiable here and should be read as
  the document's own "prose-under-review" class.
- **`org-repos.md:145` — `messenger/terraform/main.tf:29 service_desired_count = 0`.** Ref-dependent and I
  deliberately did **not** book it. TRUE at `origin/main` **`e9421c68`** (`:29` is exactly that line, and
  `:25` cites the cms precedent); FALSE at the **checkout** `fa47850d`, where `:29` is
  `container_definitions = <<EOF` and the only `service_desired_count` is **`= 1`** at `:19`. The cell
  names no ref, so the letter of the rule grades it at the checkout — but the checkout is a stale local
  clone, and grading a true production claim against it is precisely the *"stale substrate manufactures
  evidence against a true claim"* failure this same document names at `:25`. Flagging rather than booking.
  The other three rows in that table are exact at the checkout: `cms:39 = 0`, `roadrunner:19 = 1`,
  `graphql-wundergraph:20 = 1`.
- **DB-population figures** — `skill_embeddings` 42,790 / `job_role_embeddings` 18,919 / 22,470 public job
  roles (`ai_architecture.md:156,161-164`), the 87 `SIMULATION_TYPE_HIRING` snapshot sims and 0
  `job_position` rows (`hiring.md:173-178`). These need a live DB or the capture manifest.
- **Live/render measurements** — the M228 5/5 render gate, p95 1.27 s, `billion` cold-run counts
  (`hiring.md:433-472`); `AI-Labs`' 96-template catalog; AKB's 14 unsourced 60K/18K sites.
- **`corpus/services/coursebuilder.md:92-94`** self-report *"previously listed 13 vs 16 real; corrected
  iter-102"*, and the several *"corrected M257x iter-NN"* provenance notes across all six files: the
  **current** state is verified in every case I checked, but the historical claim about what the text used
  to say is only settleable from `knowledge/plan/**`, which the briefing bars.

### One cross-scope observation, not booked (outside my assigned files)

Root **`CLAUDE.md`** still says *"**four** are imported as private Go modules (`ai`, `colony`, `proto`,
`taxonomy`)"* and lists `ai` under Shared Libraries as a live import. `corpus/architecture/README.md:22`
(mine) says **three**, `ai` folded in-tree at `1e457fa70` — and README.md is the one that measures
correct. `external_services.md:554` and `ai_architecture.md:100-110` also carry the corrected form. So the
surviving instance sits at the highest-propagation site in the tree and just outside this reading's scope
— the exact §5 rule 19 corollary (*"a claim leaks to the EDGE of the previous repair's scope and stops
there … check the instruction files first"*). Booking it against my own files would be wrong; naming it is
the useful thing.
