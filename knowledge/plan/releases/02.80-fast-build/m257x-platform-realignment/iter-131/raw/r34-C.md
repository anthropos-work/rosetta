# Seat C — reading #33/#34 (M257x iter-131)

## Ground truth actually read (re-derived at this seat's open)

Enumerated with `find stack-demo -name .git -maxdepth 4`, every tree at its own ref:

| tree | HEAD | origin/main |
|---|---|---|
| `stack-demo/app` | `ad9f3c498` | `ad9f3c498` |
| `stack-demo/app/studio` (nested, own repo) | `aeec036a5` | `aeec036a5` |
| `stack-demo/platform` | `0c91421df` | `0c91421df` |
| `stack-demo/next-web-app` | `8297c684c` | `f97ba6599` |
| `stack-demo/sentinel` | `f2c461903` | `f2c461903` |
| `stack-demo/studio-desk` | `41ee3575d` | `41ee3575d` |
| `stack-demo/ant-academy` | `22df69dd8` (DIRTY tree — read via `git show` only) | `22df69dd8` |
| `stack-demo/cms` | `ca50c8170` | `f38c0c4a4` |
| `stack-demo/cms/studio` (nested) | `aeec036a5` | — |
| `stack-demo/storage` | `4ce8ece52` | `9f8cb5322` |
| `stack-demo/messenger` | `fa47850d9` | `e9421c68f` |
| `stack-demo/jobsimulation` | `462343b05` | `82cb66ecc` |
| `stack-demo/roadrunner` | `87d8d4438` | `87d8d4438` |
| `stack-demo/graphql-wundergraph` | `60c229f39` | `60c229f39` |
| `stack-demo/rosetta-extensions` (pinned consumption) | `09d06070f` | `4cb920aac` |
| `.agentspace/rosetta-extensions` (authoring) | `f2ea567b3` | — |
| corpus (`rosetta`) HEAD | `a5324934c` | — |

Matches the ADDENDUM. **`infrastructure` is in no tree on this box** (`find / -type d -name infrastructure -path '*anthropos*'` → nothing; `stack-dev/` holds only `studio-desk`, `studio-room`, a handoff md). That fact is load-bearing for B1–B4.

For `rosetta-extensions` claims I read **both** trees and they were byte-identical on every anchor I checked (gate.sh, run.go, dna.go, snapshot.go, data-dna.json, the five DNAs, the alignment/ layout). Stated per finding below.

## Positive controls

| file | `wc -l` | read in full? |
|---|---|---|
| `corpus/architecture/alignment_testing.md` | 542 | yes |
| `corpus/services/backend.md` | 404 | yes |
| `corpus/services/cms.md` | 356 | yes |
| `corpus/services/clerk-integration.md` | 210 | yes |
| `corpus/services/skillpath.md` | 117 | yes |
| `corpus/services/gotenberg.md` | 84 | yes |

Protocol read: `corpus/ops/platform-alignment.md` §5 in full (lines 259–1890).

Search-pipeline positive controls run in-pass (§5 rule 2): `SessionManager` in `app` Go (non-zero) before booking `SkillPathSessionService` = 0; `exec.Command` (non-zero) before booking `"bash"` = 0; `ClerkProvider` (non-zero) before booking `UserButton`/`OrganizationSwitcher` absence. **One control fired and saved a false booking:** my first pass for Clerk prebuilt components used `<(SignIn|SignUp|…)[ />]` and returned 0 across `next-web-app` — the pattern was wrong (JSX opens `<SignIn\n`), and the corrected search found 11 real sites. An unqualified "0" there would have been a fabricated blocker.

---

## Blockers

### B1 — `backend.md:13` still says M810/cms is "NOT MEASURABLE … assert neither", and points at a bullet title its own file has retracted

- **Anchor:** `corpus/services/backend.md:13` — the `cms` row of the merge table:
  > `| [cms](./cms.md) | cms-in-app v8.0, app **v1.360.0** (prod teardown **M810 — NOT MEASURABLE here**; report both, assert neither — see the *M810 prod teardown is UNEVEN* bullet below) | …`
- **Why it is false:**
  - **The same file retracts it 63 lines later.** `backend.md:76` opens: *"**The M810 prod teardown has now LANDED for both — corrected M257x iter-127.** This bullet said the teardown was *"UNEVEN"* …"*, and `:78-80` cite `infrastructure` @ `13c248e6` and `infrastructure/terraform/production/services.tf:64-70`. So the row's instruction (*assert neither*) and its cross-reference target (*the "M810 prod teardown is UNEVEN" bullet*) are both dead: no bullet by that name exists any more — the only bullet it can mean opens by quoting "UNEVEN" **as the thing being withdrawn**.
  - **Five other corpus sites carry the resolved verdict**, all of them read directly: `platform-migration-status.md:88` (*"RESOLVED at M257x iter-123 — the ECS service is DESTROYED"*), `corpus/services/cms.md:8`, `architecture_overview.md:227`, `service_taxonomy.md:169`, `cms.md:78`. `backend.md:13` is the surviving pre-iter-123 twin — §5 rule 40's exact shape (the repair rewrote the passage that prompted it and left the restatement standing elsewhere in the file).
  - I could not clone `infrastructure` and do not need to: whichever way that repo reads, `backend.md:13` and `backend.md:76` cannot both be right, and the corpus-wide weight is on `:76`.
- **Confidence:** high

### B2 — `backend.md:86-90` asserts the opposite of `backend.md:78-80`, eight lines above it, and misattributes the position to the fenced map

- **Anchor:** `corpus/services/backend.md:86-90`:
  > `**Whether that infrastructure-side deletion has actually been applied is NOT MEASURABLE from any clone`
  > `set we have** — `infrastructure` has never been in one. So do not assert either way: what is measured is`
  > `a `cms`-repo commit asserting the destruction, and a `cms`-repo terraform block that still declares the`
  > `module. The fenced map states this limit explicitly and is authoritative`
  > `([`platform-migration-status.md`](../architecture/platform-migration-status.md), the `cms` row).`
- **Why it is false — three independent clauses, each refutable:**
  1. *"`infrastructure` has never been in one"* — the **same bullet, at `:78-79`**, states *"`infrastructure` @ `13c248e6` declares no `module "cms"`, and `infrastructure/terraform/production/services.tf:64-70` records the destruction."* A repo that was cloned and read at a named sha has been in a clone set. `platform-alignment.md` §5 rule 54 records the same event (*"iter-123 cloned `infrastructure` and measured one rule"*).
  2. *"So do not assert either way"* — the bullet's own headline (`:76`) **does** assert: *"has now LANDED for both."*
  3. *"The fenced map states this limit explicitly and is authoritative"* — it does not. Read at `corpus/architecture/platform-migration-status.md:88`: *"**RESOLVED at M257x iter-123 — the ECS service is DESTROYED.** `infrastructure` was cloned and read (`13c248e6`, 2026-08-07) … **The blocker this cell named for four iterations — *"the destruction happens in infrastructure's `services.tf`, which we cannot read"* — was a clone-set limit, not a measurement limit, and the fix was to clone the repo.**"* The citation points at a passage that says the reverse of what the citing sentence claims it says.
- **Why I hesitated (and booked anyway):** a reader could construe *"in no clone set"* as present-tense-true (it is not in `stack-demo`). But the text says **"has never been in one"** and draws a *measurement-impossibility* conclusion from it, then hangs an authority claim on a document that contradicts it. That is not a hedge; it is an instruction to disbelieve a measurement the corpus has already banked.
- **Confidence:** high

### B3 — `cms.md:14-17` tells the reader "Do not assert either way" six lines under the banner that asserts, with a measurement

- **Anchor:** `corpus/services/cms.md:14-17`:
  > `> registry that no longer exists."* **So the two measured facts in this repo point opposite ways** (a`
  > `> module block that still declares the service; a CI commit asserting the registry is already gone), and`
  > `> the deletion itself lands in `infrastructure`, **which has never been in any clone set we have.**`
  > `> **Do not assert either way** — see the scope note below, and the fenced map, which states the same limit.`
- **Why it is false:**
  - `cms.md:8`, **six lines above**: *"**RESOLVED at M257x iter-123/127 — the cms ECS service is DESTROYED.** `infrastructure` @ `13c248e6` declares **no `module "cms"` at all**, and `infrastructure/terraform/production/services.tf:64-70` records what the apply destroyed … **`cms/terraform/main.tf:39` is ORPHANED DEAD CODE**."*
  - `cms.md:34-36`, the "scope note below" this passage forwards to: *"Whether production's `module.cms_euwest1` rollback path has also been torn down (**M810**) is now MEASURED and the answer is **yes** — `infrastructure` `13c248e6` (2026-08-07)…"* The forward reference lands on a passage that says the opposite of the sentence forwarding to it.
  - *"the fenced map, which states the same limit"* — `platform-migration-status.md:88` states the **resolution** (quoted in B2), not the limit.
  - `cms.md:78-97` (the **Infrastructure** bullet) also says *"the ECS service is DESTROYED (iter-123, propagated iter-127)"* and *"**They never did**, and iter-123 settled it by cloning the repo this bullet said could not be read."* So one document holds both readings, and the false one is the one a reader hits first (banner, top of file).
  - Independently verified in the clone set: `cms/terraform/main.tf` @ `ca50c817` is **191 lines** with `service_desired_count = 0` at `:39` — exactly the "orphaned dead code" the resolved reading describes.
- **Confidence:** high

### B4 — `cms.md:218` cites `:18` of its own file for a sentence that is at `:16`, and forwards a claim `:8`/`:36` refute

- **Anchor:** `corpus/services/cms.md:218` (the *RPC* bullet under *Interface Discovery*):
  > `**And the production declaration is not measurable from this repo at all:** it lives in `infrastructure`, which has never been in any clone set — **as `:18` of this same file already says** — so no *"still names"* claim can be made here in either direction.`
- **Why it is false:**
  - **The self-citation names the wrong construct.** `sed -n '18p' corpus/services/cms.md` returns:
    `> [skiller](./skiller.md), [skillpath](./skillpath.md) and [jobsimulation](./jobsimulation.md) — **not the`
    — the "fourth engine consolidated into `app`" sentence, which says nothing about `infrastructure` or clone sets. The sentence being cited is at **`:16`**. This is §5 rule 34 in its own file: a repair moved the line and the citation was not re-derived.
  - **The claim it forwards is the B3 predicate.** *"which has never been in any clone set"* is contradicted by `cms.md:8` and `cms.md:36`, both of which read `infrastructure` @ `13c248e6`. The identical clause also stands at **`cms.md:61`** (*"the deciding declaration lives in `infrastructure`, which has never been in any clone set"*) and at **`backend.md:185-188`** and **`backend.md:327`** — five surviving sites of one retracted predicate.
- **Why I hesitated:** the *conclusion* of `:218` (the production RPC address is not stated here) may still be sound — iter-123's read settled the cms **module**, not the RPC address. But the stated *reason* is false, and a reader who follows `:18` to check it lands on an unrelated sentence and cannot tell that. Booked per the briefing's "when in doubt, BLOCKER."
- **Confidence:** high (anchor), medium (that the surrounding conclusion is also affected)

### B5 — `clerk-integration.md:115` cites `ant-academy.md:334` for the `DEV_LOGIN_ENABLED` row; `:334` is the AI-proxy row

- **Anchor:** `corpus/services/clerk-integration.md:114-115`:
  > `**Corpus cross-references for the harness half** (this page previously mentioned it nowhere):`
  > `[`ant-academy.md:334`](./ant-academy.md) (the `DEV_LOGIN_ENABLED` public-route pair), …`
- **Why it is false:** `sed -n '334p' corpus/services/ant-academy.md` returns
  `| AI proxy | `/api/ai/chat` | Does its own cookie-based `auth()` server-side |`.
  The `DEV_LOGIN_ENABLED` public-route pair is at **`ant-academy.md:338`**
  (`| **Dev-only**, `DEV_LOGIN_ENABLED` | `/api/dev/login-as`, `/dev/accept` | …`), confirmed by `grep -n DEV_LOGIN corpus/services/ant-academy.md` → single hit, `:338`.
  The two sibling citations in the same sentence are **correct** (`studio-desk.md:90` = *"Dev-only acceptance harness"*; `next-web-app.md#the-two-clerk-sign-in-token-minting-sites-in-this-repo-added-m257x-iter-121` = the heading at `next-web-app.md:156`), which is what makes this one hard to spot — an anchor-existence check passes, and the target is in the same table four rows up.
- **Why I hesitated:** four lines of drift inside one table is small, and the reader lands near the right place. Booked because the briefing names "wrong line anchors that point at the wrong construct" as a BLOCKER class outright, and because §5 rule 46 records that this class is precisely what the anchor fences cannot see when the wrong line holds prose.
- **Confidence:** high

---

## Minors

- `backend.md:19` — *"compose now declares **five** services"*, unqualified. `docker-compose.yml` @ `0c91421d` declares five (`sentinel:5`, `backend:28`, `studio-desk:112`, `next-web-app:143`, `gotenberg:170`), but `:1-2` is `include: common.yml`, which adds `postgresql` and `redis`, so the composed project declares **seven**. Every other corpus site qualifies it — `corpus/services/README.md:31` (*"5 services (7 effective, with `common.yml`'s postgresql + …)"*), `service_taxonomy.md:72`, `staging-clerk.md:386`, and **`cms.md:59` in my own file set** (*"the **five** services compose declares … (**seven** effective, once `include: common.yml` adds the `postgresql`/`redis` floor)"*). §5 rule 30 flags resolving `include:` as load-bearing precisely because the hidden two are the always-on floor. Non-material because the number matches the corpus-wide `docker-compose.yml`-only convention; noted because the qualifier is present everywhere else including my own file set.
- `alignment_testing.md:3` — `**Last updated:** 2026-06-06` while the body carries M218, M219 and an explicit *"Corrected M257x iter-85"* (line 377) — corrections dated 2026-08. Metadata only; no factual consequence.
- `backend.md:122` — cites `jobsimulation_feedback.go:26-46`; the `Fields()` block is `:25-45` at `ad9f3c498` (`:26` is `return []ent.Field{`, `:46` is blank). The claim it supports (no skill field, no skill edge) is correct — verified `Fields()` (`:25-45`) and `Edges()` (`:48-57`, one `user` edge only).
- `skillpath.md:107` — *"the body renders the literal string **"Coming soon"**"*. It renders `t('enterprise.insights.comingSoon')`; `configs/i18n/messages/en/enterprise.json:1891` resolves it to `"Coming soon"`, so the sentence is true in EN and loose about the mechanism.

---

## Positively cleared (audited zeros)

Labelled per the briefing. **ENUMERATED** = I derived the predicate's set from source independently and compared cardinalities. **RE-DERIVED** = I only re-computed arithmetic the document displays (weakest form).

### `gotenberg.md` — clean, no findings

- **ENUMERATED** — the two call sites. `git grep ConvertToPDF` over `app` @ `9d00a313`: exactly two non-test callers, at the cited lines. `extract.go:77` is `converter.ConvertToPDF(...)`, `:81` is `converter.ConvertFromReader(bytes.NewReader(pdf), "application/pdf")`; `user_import_resume_2d.go:68` is the `ConvertToPDF`, `:73` is `ocrInput = pdfBytes`. Both files are **byte-identical** at `9d00a313` and the checkout `ad9f3c498` (`diff` empty), so the unref'd bullets grade the same either way.
- **ENUMERATED** — the nine gotenberg MIME types. `extract.go:17-27` @ `9d00a313` holds exactly nine map entries: `.xls`, `.xlsx`, `.ppt`, `.doc`, `.odt`, `.ods`, `.odp`, and RTF under **both** spellings (`application/rtf`, `text/rtf`). Three OpenDocument ✓. `wordprocessingml.document` (DOCX) is **not** among them ✓ — the corpus's counter-intuitive claim is the true one.
- **ENUMERATED** — the compose block. `docker-compose.yml` @ `0c91421d` is **186 lines**; `gotenberg:` at `:170`, `profiles: [core, backend, all]` at **`:183`** exactly as cited. `GOTENBERG_URL=http://gotenberg:3200` on `backend` at `:57`. Ports `3200:3200`, `--api-timeout=60s`, `--libreoffice-restart-after=50` all present verbatim.
- **ENUMERATED** — the 90 s client timeout: `internal/converter/gotenberg.go:13` `var gotenbergClient = &http.Client{Timeout: 90 * time.Second}`; signature `ConvertToPDF(ctx, gotenbergURL, document, filename) ([]byte, error)` at `:16`; endpoint string built at `:31`.

### `skillpath.md` — clean, no findings

- **ENUMERATED** — *"there is NO `SkillPathSessionService` anywhere … 0 occurrences in Go source at `ad9f3c49`"*. `git -C stack-demo/app grep -c SkillPathSessionService ad9f3c498 -- '*.go'` → exit 1, zero hits, with `SessionManager` as a live positive control in the same pass. The only two hits tree-wide are the two the corpus itself names as the platform's own lagging docs: `CLAUDE.md:109` and `knowledge/architecture.md:28`, both at the cited lines.
- **ENUMERATED** — the `repos.yml` ladder *"9 @ `2adcf71`, 6 @ `0dab54d`, 4 since `838d907`"*. Derived per ref: `2adcf71` → app, cms, jobsimulation, sentinel, storage, messenger, roadrunner, next-web-app, studio-desk (**9**); `0dab54d` → app, sentinel, storage, messenger, next-web-app, studio-desk (**6**); `838d907` and `0c91421d` → app, sentinel, next-web-app, studio-desk (**4**). No `skillpath` at any ref ✓. (I also confirmed `d11a403` is the commit that deleted `cms`/`jobsimulation`/`roadrunner` from `repos.yml` — its own message says roadrunner's entry *"was already gone"*, which is testimony contradicted by its own diff; the corpus follows the diff, correctly.)
- **ENUMERATED** — the 8 GraphQL operations at `internal/web/backend/graphql/graph/schemas/skillpath_sessions.graphqls` @ `ad9f3c498`: `getOrCreateSkillPathSession` `:71`, `skillPathActiveSessions` `:72`, `skillPathCompletedSessions` `:73`, `completeSkillPathStep` `:77`, `uncompleteSkillPathStep` `:78`, `createSkillPathSession` `:79` **with `@deprecated` at `:80`** ✓, `upgradeSkillPathSessionToLatest` `:81`, `upgradeAllSkillPathSessionsToLatest` `:82`. All eight named in the corpus, none missing, none extra.
- **ENUMERATED** — the retraction's evidence. `terraform/migrations/20260729133514.sql:62` = `DROP TABLE "local_jobsimulation_sessions";`, `:63` = `DROP TABLE "local_skill_path_sessions";` — the corpus's line assignment is exact, in the right order. No `local_skill_path_session.go` Ent schema exists. **Three** migrations post-date it at `9d00a313`, and they are the three named (`20260731131307.sql`, `20260731154527_academy_chapter_progress_completed_at.sql`, `20260803143844_ai_readiness_recommendation_path.sql`) — I listed the directory rather than trusting the count.
- **ENUMERATED** — the "Coming soon" claim at `next-web-app` `8297c684`. `InsightsBySkillPathStudentSimulationsContainer.tsx:31-34`: `userData` is `useMemo(() => { /* return insightData?.rows[0]?.membership; */ return null as unknown as MembershipEnriched; }, [])` — hardcoded null with the real read commented out ✓; the results `<Table` (`:140`) and the totals block (`:150`) are both commented out ✓; the body renders `t('enterprise.insights.comingSoon')`. The cohort container `InsightsBySkillPathStudentsContainer` exists and is a real page ✓. `apps/hiring` has **no** `activity-dashboard/@tabs/skill-paths` route ✓ (listed the whole `apps/hiring` tree for `skill`).
- **ENUMERATED** — `intelligence.go:1144` is `func (m IntelligenceManager) InsightsSkillPathByMemberships`, and `:1159-1170` is the `m.ent.SkillPathSession.Query()` with `SkillPathID`, `StatusIn(Active, Completed)` and the `TenantIDIsNil() OR TenantID(organizationID)` predicate — all three clauses the corpus names, at the cited range.
- **ENUMERATED** — event names: `EventSkillPathSessionUpdated` (`internal/skillpath/session.go:167`) and `EventChapterStepSessionCompleted` (`:178`) both exist as published proto events. `SKILLPATH_STREAM=skillpath` survives in compose at `docker-compose.yml:72` ✓.
- **ENUMERATED** — both in-process content reads: `internal/skillpath/session.go:205-207` (the `// cms-in-app deseam` comment at `:205-206`, `u.cms.GetSkillPathDomain` at `:207`) and `internal/skillpaths/skillpaths.go:88-95` (same comment `:88-89`, `GetSkillPathDomain` at `:95`). Both ranges land on the construct.

### `clerk-integration.md` — one finding (B5); everything else cleared

- **ENUMERATED — the five sign-in-token minting sites, the highest-risk claim on the page.** I re-derived the SET rather than checking the count (§5 rule 24). Ran `git grep -i -E 'createSignInToken|sign_in_tokens|signInTokenCl'` at **each of the 15 git trees' own HEAD**, nested repos included (`app/studio`, `cms/studio` — both 0). Result: `app` 1 product site, `next-web-app` 2, `studio-desk` 1, `ant-academy` 1 = **5**, and no sixth anywhere. The one apparent extra, `graphql-wundergraph:schemas/backend.graphqls:788`, is a GraphQL **field** `signInTokenId: String!`, not a minting site. All five per-row anchors resolve to the construct: `app/internal/admin/impersonation/manager.go:101` (`m.signInTokenCl.Create`), `next-web-app/apps/web/src/app/api/dev/login-as/route.ts:79`, `next-web-app/e2e/auth.setup.ts:72`, `studio-desk/src/routes/dev.ts:83`, `ant-academy/code/app/api/dev/login-as/route.js:78` (read via `git show 22df69dd8:` because the tree is dirty). All four `DEV_LOGIN_ENABLED = process.env.NODE_ENV !== 'production'` gate lines resolve too (`devLogin.ts:28`, `dev.ts`→`devLogin.ts:33`, `devLogin.js:29`), and site 3 genuinely has none.
- **ENUMERATED** — the impersonation gate. `resolver_admin_audit.go:20-22` really does check `permission.ActionObjectTaxonomy` / `permission.UserActionWrite` — a **taxonomy-write** permission on an impersonation mutation, exactly as the corpus flags. `manager.go:1-4` is the audit-every-attempt header; `manager.go:9` is the literal *"is on the standard plan (Actor Tokens would require Enterprise)"*. The e2e second-factor bypass is stated in the source at `e2e/auth.setup.ts:55-62`, quoted accurately.
- **ENUMERATED** — *"three org roles, not two … all six constants"*. `internal/clerk/orgclient/roles/roles.go:12-17` @ `ad9f3c498` holds exactly six: `admin`/`org:admin`, `basic_member`/`org:basic_member`, `content_creator`/`org:content_creator`. Cardinality checked before the arithmetic.
- **ENUMERATED** — *"Webhooks (svix, **12** event types)"*. Derived the set from `internal/clerk/events/events.go`: `user.{created,updated,deleted}` (`:166`,`:172`,`:178`), `organization.{created,deleted,updated}` (`:185`,`:191`,`:197`), `organizationInvitation.{accepted,created,revoked}` (`:204`,`:210`,`:216`), `organizationMembership.{created,deleted,updated}` (`:223`,`:229`,`:235`) = **12**, and the four families named match.
- **ENUMERATED** — *"Localization — 8 locales"*. `apps/web/src/providers/Clerk.provider.tsx:10-17` imports `deDE, enUS, esES, frFR, itIT, jaJP, nlNL, ptPT` = **8**. (`apps/hiring` imports 7; the doc's number is the web app's.)
- **ENUMERATED** — the SDK-drift table, every cell. `apps/{web,hiring,integration}/package.json` all `"@clerk/nextjs": "^6.39.6"`; `ant-academy/code/package.json:52` `"^6.39.2"` ✓ (line-exact); `apps/maintenance/package.json` declares **no** `@clerk/*` ✓; `apps/mobile/package.json` `"@clerk/clerk-expo": "~2.6.18"` vs `ant-academy/mobile/package.json:18` `"~2.19.36"` ✓ (2.6→2.19 = thirteen minors ✓); `app/go.mod:31` @ `5ba17044` = `github.com/clerk/clerk-sdk-go/v2 v2.7.0` ✓ **at the pinned ref** (it has moved to `:29` at `ad9f3c498`, same version — the pin is what settles it).
- **ENUMERATED** — the mobile-OAuth retraction (the newest prose on the page, so the highest-risk under the ADDENDUM). `apps/mobile/app/(public)/sign-in.tsx:70` `<OAuthButton strategy='oauth_google'>` and `:75` `strategy='oauth_microsoft'`; `sign-up.tsx:86`/`:91` the same pair; `components/OAuthButton.tsx:29` `const { startOAuthFlow } = useOAuth({ strategy })`. Every offset exact. The retraction is correct in both halves.
- **ENUMERATED** — *"129 files in `app` vs **0** in `sentinel`"* for `colony/authn`. `git grep -l anthropos-work/colony/authn ad9f3c498 -- '*.go' | wc -l` → **129** exactly; `sentinel` @ `f2c46190` → 0; `storage` @ `4ce8ece5` → 0; `messenger` @ `fa47850d` → 0. All four figures reproduced from the source, not from the sentence.
- **ENUMERATED** — the Sentinel-sync anchors: `org_handler.go:229` = `o.authMan.OrgAddUserToRole(...)`, `:249` = `roles.TranslateRoleFromClerkToAnt(...)`; `OrgReplaceUserRole` at `:277` and `OrgRemoveUserFromRole` at `:305` also exist, so the three-call list is complete.
- **ENUMERATED** — `AuthRole()` has **0** call sites in `app` Go @ `ad9f3c498`, supporting *"zero call sites on the allow/deny path"*.
- **ENUMERATED** — the env-var shapes. `git grep -oE 'CLERK_[A-Z_]+'` over `app` Go yields exactly `CLERK_SECRET_KEY`, `CLERK_WEBHOOK_SECRET`, `CLERK_WEBHOOK_REDIRECT_URL`; `.env_example` @ `0c91421d` carries `CLERK_SECRET_KEY:3`, `CLERK_WEBHOOK_SECRET:55`, `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY:95`, `VITE_CLERK_PUBLISHABLE_KEY:106`; `EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY` in both mobile trees. Every name in the config block is real.
- **ENUMERATED** — the "Not used" list's load-bearing members. `<UserButton>` / `<OrganizationSwitcher>` / `<OrganizationProfile>` have **no production use** in any of the three frontends: `next-web-app`'s only hits are its own `FourOhFourOrganizationSwitcher` component; `ant-academy`'s are vitest `vi.mock` stubs and its own archived plan docs; `studio-desk`'s `OrganizationSwitcher` is its own class at `app/core/scaffold/orgSwitcher.js:5`. "org UI is custom" holds. `getToken(` appears 47 times in `next-web-app` and **none** passes a `template` ✓. `metabase/route.ts:35` checks the **bare** `orgRole !== 'admin'` ✓, which is the claim the doc hangs the prefixed-vs-bare warning on. `studio-desk`'s `STUDIO_ACCESS_ROLES` at `src/index.ts:96` and `app/services/userService.ts:16` does include `content_creator` ✓. `ant-academy/mobile/src/components/ClerkSignInForm.tsx:49-53` aborts on any `result.status !== 'complete'` ✓ — the MFA caveat is real.
- **ENUMERATED** — the three ops cross-refs, checked by re-running the doc's own suggested derivation (`grep -n sign_in_tokens corpus/ops/*.md`): `staging-clerk.md:58`, `staging_from_dump.md:421`, `staging-bringup.md:528` — **all three current** after the iter-130 move the paragraph itself records. The `next-web-app.md#…` slug matches its heading at `:156`; `shared_libraries.md#authn` = `:215`; `external_services.md#clerk-authentication-service` = `:21`. Only `ant-academy.md:334` fails (B5).

### `backend.md` — two findings (B1, B2); the rest cleared

- **ENUMERATED** — the RPC mux, at **both** refs the file names. @ `ad9f3c49` `main.go:1297-1338` holds exactly six `mux.Handle` calls: Users `:1297`, Organizations `:1298`, Skiller `:1306`, JobSimulation `:1314`, CMS `:1323` **inside `if cmsRPCServer != nil` at `:1322`**, LabSession `:1338`. @ `b948604f` `main.go:1185-1228`: `:1187`, `:1188`, `:1196`, `:1204`, `:1213` (guard at `:1212`), `:1228` — every per-handler offset in the *Role & Responsibility* bullet is exact. Three of the eight folded services (skiller, jobsimulation, cms) have a handler ✓; skillpath/roadrunner/storage/messenger/customerio-sync have none ✓.
- **ENUMERATED** — the three "their own muxes" citations, including the one the file retracts. `storage@4ce8ece5:cmd/root.go:62-66` really is `rpcMux := http.NewServeMux()` + `rpcMux.Handle(storagev1connect.NewStorageServiceHandler(rpcsrv.New(...), rpc.DefaultInterceptors))` ✓; `internal/rpcsrv/rpcsrv.go` exists ✓. The retracted citation is correctly characterised: `sdk/storage/v1/service.go` is **47 lines** ✓, `:13-17` is `type Service interface { GetObject, PutObject, GetPresignedUrl }` ✓, `:19`/`:34` are `NewService`/`NewPublicService` ✓, and `git grep -n 'Handler\|ServeMux' 4ce8ece5 -- sdk/storage/v1/service.go` returns **nothing** ✓ — the retraction's own control reproduces. `messenger@fa47850d:internal/rpcsrv/rpcsrv.go` names `messenger.v1.MessengerService` ✓; `roadrunner@87d8d443:cmd/root.go:87` is `rpcMux.Handle(roadrunnerv1connect.NewRoadRunnerServiceHandler(...))` ✓.
- **ENUMERATED** — the Redis-stream partition (*"six subscribers against five publishers"*), derived from the source both ways. **Publishers** @ `ad9f3c49`: `git grep pubsub.NewPublisher` gives exactly five non-test sites — `main.go:325` (self-stream, `serviceName` defaulting to `backend` at `:230-232` ✓), `:746` (`SKILLPATH_STREAM`), `:1149` (`CMS_STREAM`), `jobsimwiring/wiring.go:132` (`AI_USAGE_STREAM`), `:185` (`JOBSIMULATION_STREAM`). @ `b948604f` the same five at `:287`, `:637`, `:1039`, `wiring.go:127`, `:180` ✓. **Subscribers** @ `ad9f3c49`: `buildStreamSubscribers` at `subscriber_wiring.go:203` allocates `make(map[string]*pubsub.Subscriber, 6)` and fills six — SkillPath `:207`, **Skiller `:209`**, JobSimulation `:214`, CMS `:233`, AIUsage `:236`, Backend `:248` — applied by the single loop at `main.go:1579-1581` ✓. The `streamNames` struct is at `main.go:1534-1541` ✓ and the *"Backend is app's OWN self-stream"* comment is at `subscriber_wiring.go:112-113` ✓, verbatim. `skiller` has a subscriber and no publisher ✓ — 6 vs 5 reproduces from the set, not from the sentence. @ `b948604f`, `main.go:1276` is the standalone `AddSubscriber(os.Getenv("SKILLER_STREAM"), …)` ✓, and the doc's warning that no such standalone line exists at `ad9f3c49` is correct.
- **ENUMERATED** — the migration figures. Listed `terraform/migrations/*.sql` per ref: **170** @ `ad9f3c498`, **169** @ `b948604f` ✓. Head @ `ad9f3c498` = `20260803143844_ai_readiness_recommendation_path.sql` ✓; head @ `b948604f` = `20260731154527_academy_chapter_progress_completed_at.sql` ✓; the three preceding it are the three named ✓. **46** migrations sort after `20260531` ✓, and the last May one is `20260529072659_add_lab_session.sql` ✓ — so "46 back from the head" is right. Top-level `migrations/` is empty at both refs ✓, and `6a46e8445` is *"chore(migrations): remove obsolete atlas.sum file"*, 2026-06-18 ✓. `atlas.hcl` gives `dir = "file://terraform/migrations"` and `src = "ent://internal/data/ent/schema"` ✓.
- **ENUMERATED** — *"**13** `ai_readiness_*` ent tables"*. Derived from `CREATE TABLE` across `terraform/migrations/`: cycles, diagnose_narratives, email_overrides, live_snapshots, notification_logs, notification_optouts, recommendations, sims, skills, snapshots, steps, text_translations, user_step_progresses = **13** ✓, and the four the doc says a "9" omits (recommendations, email_overrides, and the notification pair) are exactly the four that make 9 → 13 ✓, with `live_snapshots`/`text_translations` correctly *not* on that list. `internal/aireadiness/` exists with `manager.go`… `compare.go`, `csv.go`, `cycles.go`, `diagnosis.go` ✓, and `internal/workforce/` contains **no** `readi*` file ✓.
- **ENUMERATED** — the six gqlauthz short-circuits, each read with surrounding context. `:160-161` unparsed op ✓, `:174-178` public/federation/dev-introspection ✓, `:190-191` no active org ✓, `:196-197` `errUnknownTarget` ✓, `:202-203` self-target ✓, `:209-219` `operationIsResolverAuthorized` ✓. The REST claim is correctly **scoped to the file it names**: `internal/web/backend/backend.go` declares exactly **6** groups (`:121`, `:171`, `:194`, `:210`, `:229`, `:273`), of which `/coursebuilder` `:229-232` and `/credits` `:273-276` add `cbGate` and the other four carry only cors/swagger/authn ✓; `gate.go:27-49` is `courseBuilderAccessGate` with `OrgCheckFeaturePermission` ✓. (I checked whether §5 rule 57's eleven-group correction bites here: `app` mounts 11 groups service-wide — `academy_embeddings_admin.go:41`, `content_admin.go:35`, `emailpreview/handler.go:66`, `labs_admin.go:31` plus the six — but `backend.md`'s sentence is bounded to `backend.go`, so its "6" is true at its stated scope. `security_compliance.md` is where the wider number belongs, and that is another seat's file.)
- **ENUMERATED** — the subgraph ladder, from the commits themselves. `749dc86` (2026-06-24) deletes `schemas/skiller.graphqls` ✓; `7c17e63` (2026-07-21) deletes `schemas/skillpath.graphqls` ✓; `915da06` (2026-07-29) deletes **both** `schemas/cms.graphqls` **and** `schemas/jobsimulation.graphqls` in one commit ✓, and its own subject really does say *"supergraph 2→1"* — the corpus's warning not to take the count from the subject line is verified against the diff. `schemas/` @ `60c229f3` holds one file, `backend.graphqls` ✓.
- **ENUMERATED** — storage-in-process. `main.go:524` = `internalstorage.NewManager(storageBucket)`, `:525` = `NewPublicManager(...)` ✓; `STORAGE_RPC_ADDR` has **3** occurrences in `app` Go @ `ad9f3c498` and **all three are comments** (`main.go:504`, `jobsimwiring/wiring.go:101`, `storagens/callsites_test.go:189`), with `:504` reading *"STORAGE_RPC_ADDR is gone"* ✓ — 0 read sites, derived not quoted.
- **ENUMERATED** — compose/edge anchors: `AUTHORIZATION_ADDRESS=http://sentinel:8087` at `docker-compose.yml:48` ✓, `GOTENBERG_URL` at `:57` ✓, `JUDGE0_BASE_URL` at `:59` ✓, backend's `profiles: [core, backend, all]` at `:110` ✓, gotenberg's at `:183` ✓, **zero** `*_RPC_ADDR` in the file ✓. `Makefile:10` = `PROFILE ?= core` ✓. `architecture_overview.md:343` is the *"On a local stack"* heading ✓ — the correctly-scoped wording it points at. `/graphql/query` is mounted at `backend.go:317` ✓.
- **ENUMERATED** — `internal/academy/academy.go:6-9` states the `aiacademy` removal in the source's own words ✓; `main.go:743-746` @ `b948604f` is the `if labsAPIURL := os.Getenv("LABS_API_URL"); labsAPIURL != ""` conditional ✓ (so "NOT unconditionally nil" is right); `graph/schemas/skiller_taxonomy.graphqls` and `ai_readiness.graphqls` both exist ✓; `DIRECTUS_WEBHOOK_SECRET` is read at `main.go:1199` under a *"fail-closed"* comment ✓; `go 1.26.4` ✓; `internal/ai/` exists and `1e457fa70` is *"refactor(ai): fold the ai library into app as internal/ai"* ✓; `jobsimulation_feedback` has **11** enum values `FUN_AND_USEFUL`…`CALL_ISSUES` ✓ with no skill field and a single `user` edge ✓.
- **Checked and NOT booked** — `backend.md:92-96` cites `jobsimulation/terraform/main.tf:15-22` and `:24-40` for the M810 module deletion. At the **checkout** `462343b0` those lines are an atlas-migration block and `module "jobsimulation"` still stands at `:31`; at **`82cb66ec`** (origin/main, i.e. after the `6092c6d2` the sentence names) `:15-22` is the *"M810 decommission — the standalone jobsimulation ECS service is gone … The `module "jobsimulation"` block … was deleted here"* comment and `:24-40` is *"What this module still owns"* — LiveKit/Chime buckets, `/production/jobsimulation/*` SSM, the atlas tracker, exactly as described. The block names its ref (`6092c6d2`) and the claim is true there, so under the briefing's *a pin is a date* rule it stands. Naming which ref would make it cheaper to check; not a defect.

### `cms.md` — two findings (B3, B4); the rest cleared

- **ENUMERATED** — the studio-room provider set, at the **nested repo's own ref** (`stack-demo/app/studio` @ `aeec036a5`), since `git -C stack-demo/app grep … -- studio/` is a guaranteed zero (`app/.gitignore:79` = `studio/*` ✓, verified). `services/ai.py:705-708` is `providers = {'openai': …, 'azure': …, 'anthropic': …}` — exactly three ✓; `:1-2` imports only `openai`/`anthropic` ✓. `requirements.txt` is **9** packages verbatim (openai, anthropic, rich, pyyaml, requests, jinja2, mistralai, pytest, pytest-asyncio) with `mistralai` at `:8` ✓ and **no `python-docx`** ✓.
- **ENUMERATED** — the mistral count, run the corrected §5-rule-44 way (`grep -ci`, summing counts, `-i` kept). `git -C stack-demo/app/studio grep -ci mistral aeec036a` → `requirements.txt:1`, `tools/pdf2md.py:18`, `tools/r3.py:3` = **22 hits in 3 files** ✓, matching the corpus exactly. `tools/pdf2md.py:24` `from mistralai import Mistral` ✓, client at `:96` ✓, `model="mistral-ocr-latest"` at `:127` ✓. `tools/r3.py:139` lists `pdf2md.py` among its scripts and `:190`/`:199-206` builds the exec command ✓ — so the withdrawal of the flat *"nothing dispatches it"* is itself correct.
- **ENUMERATED** — the shell-injection retraction, the page's most consequential claim. `studioManager.go:100-103` @ `ad9f3c498` is the caller's *"It MUST NOT be interpolated into a shell … No `bash -c`"* ✓; `:119` is `s.runCommand(ctx, pyBin, append([]string{"studio/gen.py"}, tokens...))` ✓; `:1096-1098` is the *"argv (exec) form — NEVER through a shell"* header ✓; `:1101` is `pycmd := exec.CommandContext(ctx, name, args...)` ✓. `git grep -n '"bash"' ad9f3c498 -- '*.go'` → **exit 1, zero**, with `exec.Command` as a live positive control ✓. And the "why it read as true" half checks out too: `cms@ca50c817:internal/studio/studioManager.go:967` is literally `pycmd := exec.Command("bash", "-c", command)` ✓. Two studio scripts execed, `:119` gen.py and `:1045` postgen.py @ `b948604f` ✓.
- **ENUMERATED** — `gen.py` @ `aeec036a`: `:484-492` is exactly **nine** `add_argument` calls (`-i/--interactive`, `-m/--media`, `-f/--force`, `--simid`, `--branch`, `--prompt`, `--annotations`, `--pipeline`, `--blueprint`) — counted from the source, no `--template` among them ✓ — and `parse_argument` at `:18-28` calls `parse_known_args()` then merges the leftovers into the dict ✓, which is the silent-swallow mechanism. `studio/CLAUDE.md:12-14` is the quoted command block ✓.
- **ENUMERATED** — the frozen-repo facts: `cms/terraform/main.tf` is **191** lines with `service_desired_count = 0` at `:39` ✓; `cms/go.mod:3` `go 1.26.4` ✓; `cms/cmd/root.go:77` `PORT`→8080 / `:78` `RPC_PORT`→8081 ✓; `cms/Dockerfile:2` `golang:1.26-bookworm` / `:23` `python:3.11-slim` ✓; the historical 8090/8091 pair is exactly `docker-compose.yml:169`/`:173` with ports at `:154-155` **at `2adcf71`** ✓ and absent thereafter ✓.
- **ENUMERATED** — `20260724132049_cms_data_model.sql` creates all six named tables (`similarities`, `studio_documents`, `studio_tasks`, `similarity_categories`, `similarity_features`, `similarity_skills`) ✓; `studio_document.go:9-11` is the *"caches a user-uploaded attachment converted to Markdown for AI context"* comment ✓ with `:13-14` confirming *"NO Ent privacy Policy"* ✓ — the data-classification retraction is sound; `REDIS_CMS_CACHE_INDEX` default 5 at `main.go:1107-1108` ✓; `markdownManager.go:10`/`:30` resolve **identically at both `2035f9a` and `ad9f3c498`** ✓, exactly as the pin note promises; `studioManager.go:531` is *"supported ocr content types for mistral ocr"* ✓ and `xlsx.go:13-14` states Mistral OCR rejects xlsx ✓.
- **RE-DERIVED only** (weaker, flagged as such) — cms.md's *"five services compose declares (seven effective)"* at `:59`. I re-listed both files and the numbers hold, but this is the same arithmetic the doc shows.

### `alignment_testing.md` — no blockers; one metadata minor

Every executable claim checked in **both** rext trees; they were byte-identical on all of them, so the pinned-vs-authoring distinction did not bite here — stated because the ADDENDUM requires it.

- **ENUMERATED** — the five-DNA score table, re-derived from the DNA JSON rather than from the table. Parsed each file and counted capabilities and variants: `clerk-2.6.0` **14 caps / 27 genes**, `clerk-js-5` **6 / 9**, `clerk-multi-1` **5 / 9**, `clerk-deploy-1` **3 / 7**, `clerk-express-1` **5 / 13** — all five rows exact.
- **ENUMERATED** — *"Only `clerk-2.6.0` … declares a `consumed_surface` today"*: parsed all five; exactly one has the key (15 entries), the other four have none ✓. Its entry shape is `{endpoint, consumer, capability}` ✓ and the three `covered_by` entries include the documented example `clerk-express-1:ClerkClientBAPI/get-organization` ✓.
- **ENUMERATED, then RE-DERIVED** — the 97.2% figure. First the set: `MembershipOrgIdentity/real-org-eid` is a **`standard`** gene, weight 2; the DNA's total weight is **71** (17 critical genes ×3 + 10 others ×2). Then the arithmetic: 69/71 = 97.18% ≈ **97.2%** ✓, and 27/27 → 100% ✓. Order matters here — the set first, per §5 rule 24.
- **ENUMERATED** — *"three genes did name identity — `ExtractIdentity`, `Me`, `DeployIdentity` — and all three assert the variant `universal-user`"*. Scanned all five DNAs for that variant id: exactly three hits, `Me` (clerk-js-5), `DeployIdentity` (clerk-deploy-1), `ExtractIdentity` (clerk-express-1) ✓ — the set, not the count. And the two-sided fix is real: `GetUser` (critical) carries variants `hero-a` and `hero-b` with **different** user ids ✓.
- **ENUMERATED** — the `--if-declared` distinction, which is the section's whole point. `clerkenstein/alignment/scripts/gate.sh:69` is `"$alignctl" dna coverage --dna "$base/$dna" --if-declared` ✓; the rationale is the comment block at `:58-68` ✓ and `:61` is indeed **inside** that comment ✓ (the doc's own note about where the pin used to point). Defaults: `RUNNER_PKG` → `./cmd/clerkrun` (`:33`), `DNA` → `dna/clerk-2.6.0.json` (`:35`), `ALIGN_DIR` → `$base/../../alignment` (`:30`) ✓ — all three as documented.
- **ENUMERATED** — the exit-code split. `alignment/cmd/alignctl/run.go:133-136` is `const ( ExitRegressed = 2; ExitUnmeasurable = 3 )` ✓; `unmeasurable()` spans `:139-153` and prints the boxed *"UNMEASURABLE — the runner could not execute. THIS IS NOT A PASSING SCORE."* banner ✓, returning `ExitUnmeasurable` ✓. `cmd/alignctl/dna.go:20-26` is the four-case switch `list|diff|validate|coverage` ✓.
- **ENUMERATED** — the layout claim *"the reusable `alignment/` section has no `scripts/` dir at all (it holds `cmd/`, `internal/`, `examples/`, `go.mod`, `README.md`)"*: `ls-tree alignment/` returns exactly those five entries ✓, in both trees. Five runners under `clerkenstein/alignment/cmd/` ✓, five DNAs under `clerkenstein/alignment/dna/` ✓. Module is `anthropos.dev/alignment` ✓.
- **ENUMERATED** — the CI correction-of-a-correction, both halves. `clerkenstein/.github/workflows/alignment.yml` **exists and is tracked** ✓, and `:10-11` is verbatim *"as a subdir workflow under clerkenstein/.github (not at the monorepo root), this is currently inert…"* ✓. `ls-tree HEAD .github/` at the monorepo root returns **nothing**, so *"rext has no `.github/workflows` at all"* was indeed false-in-the-other-direction ✓. Both stated verdicts survive.
- **ENUMERATED** — the DNA contract: `internal/dna/dna.go:21` `idPattern = '^[A-Za-z0-9][A-Za-z0-9_-]*$'` ✓, `:24` `maxWeight = 1_000_000` with `:269-270` enforcing `1..maxWeight` ✓, `:279-280` rejecting a DNA with capabilities and zero critical genes ✓, and `compare.go:56-60` `GateMet` refusing a non-zero critical threshold at `CriticalGenes == 0` ✓ — both halves of the documented two-guard defence exist. Weights `3/2/1` at `dna.go:40-45` ✓. The four operators are the four constants at `dna.go:55-58` ✓.
- **ENUMERATED** — the score semantics: `compare.go:101-103` is `Overall = pct(alignedW, sumW)` (weighted) and `Critical = pct(critAligned, critTotal)` (unweighted count) ✓ — the doc's parenthetical *"unweighted, unlike the overall"* is exactly right.
- **ENUMERATED, then RE-DERIVED** — the toy example. `examples/toy/dna.json` has `Add` critical (3 variants) + `Greet` standard (3) = 6 genes, total weight 3×3 + 2×3 = 15; one failing standard gene → 13/15 = **86.7%** ✓, `5/6 genes` ✓, `(exact, w2)` ✓, critical 100% ✓. Toy gate constants are `gateOverall = 80.0` / `gateCritical = 100.0` at `examples/toy/alignment_test.go:21-22` ✓.
- **ENUMERATED** — the six snapshot operators at `stack-seeding/dna/snapshot.go:62` (`validSnapshotOperators`, six entries incl. `snapshot-cross-surface-closure`) ✓; `data-dna.json:410` is the **taxonomy** operator list — five, all but `embedding-dim`… correction, all but `cross-surface-closure` ✓ — and `:439` is the **content** list — five, no `embedding-dim`, with `cross-surface-closure` ✓. Exactly as described, both anchors landing on the `"operators": [` key of the right surface.
- **ENUMERATED** — *"nothing left waived → 100% coverage"*: parsed every surface in `data-dna.json` — 4 `seeded-m7a`, 4 `seeded-m7c`, 1 `snapshot-seeded-m9b` (taxonomy), 1 `snapshot-seeded-m10` (content). **No `waived` status remains** ✓, and "4 seeded in M7a, ~6 planned" matches the 4 + 6 split ✓.

---

## What I could not settle

- **`infrastructure` @ `13c248e6`** — no clone of that repo exists on this box (searched the whole filesystem for an `anthropos`-pathed `infrastructure` dir). I therefore cannot verify `services.tf:64-70`, `:85-86` or `:88-94` first-hand. This does **not** weaken B1–B4: those are internal contradictions, and the resolved reading is the one carried by five corpus sites including the fenced map. To settle the underlying fact, re-clone `infrastructure` at `13c248e6` and read `terraform/production/services.tf`.
- **`colony` internals** — `clerk-integration.md:130-131` and `:145` describe `colony/authn`'s JWKS verification, 1-minute leeway, 401 behaviour and *"`AuthRole()` is exposed as a getter and never enforced inside `authn`"*. `colony` is a private Go module, pulled at Docker build, and is in no clone set, so none of it is checkable here. (`AuthRole()` having 0 call sites in `app` **is** checkable and holds.)
- **Prod-DB row counts** — `backend.md:145-149` (`42,790` public skills, `22,490` job roles, `43,584` embeddings, measured 2026-07-08) and `skillpath.md:27-28` (the legacy `skillpath` schema "holds 0 rows"). These are live-prod measurements; no clone can settle them. A `/db-query` read-only count would.
- **Historical alignment scores** — `alignment_testing.md:447` (*"against the M7a-seeded `demo-1`, `measure` reports 100% / Critical 100%"*) and the "MET / UNMEASURABLE" gate column are records of past runs, not properties of the checked-in tree. The DNAs, gates and exit-code machinery all check out; the run outcomes would need a live stack.
- **Third-party surface** — `gotenberg.md:64`'s `GET /health` on `gotenberg/gotenberg:8`. Upstream behaviour; not in any clone. Consistent with the vendor's docs, unverified here.
