# Seat A — reading #33/#34 (M257x iter-131)

**Ground truth re-verified at this reading's open** (all `git rev-parse --short=8 HEAD`, from the rosetta
root): app `ad9f3c49` · platform `0c91421d` · next-web-app `8297c684` · sentinel `f2c46190` · studio-desk
`41ee3575` · ant-academy `22df69dd` · storage `4ce8ece5` · messenger `fa47850d` · cms `ca50c817` ·
graphql-wundergraph `60c229f3` · roadrunner `87d8d443` · jobsimulation `462343b0` · rext **pinned per-stack**
`stack-demo/rosetta-extensions` `09d06070` · rext **authoring** `.agentspace/rosetta-extensions` `f2ea567b` ·
nested studio checkouts `stack-demo/app/studio` and `stack-demo/cms/studio` both `aeec036a`. Matches the
ADDENDUM exactly. `infrastructure` and `db-backup` are **in no clone set on this box** (`find . -maxdepth 4
-name .git` enumerated; 16 trees, neither present) — every claim resting on them is listed under *what I
could not settle*.

**Which tree settled what:** claims about tooling behaviour on a stack → the **pinned** clone `09d06070`
(stated per finding). The one fence I ran (`platform_alignment_guard.py`) I ran from the **authoring** copy
`f2ea567b` per §5 rule 50; it printed its own tree stamp
(`fence-tree: .agentspace/rosetta-extensions @ f2ea567b3`) and returned **OK OVER ITS REACH**.

## Positive controls

| file | `wc -l` | read in full? |
|---|---|---|
| `corpus/architecture/external_services.md` | 887 | yes |
| `corpus/architecture/platform-migration-status.md` | 362 | yes |
| `corpus/services/storage.md` | 248 | yes |
| `corpus/services/askengine.md` | 147 | yes |
| `corpus/services/db-backup.md` | 66 | yes |

Search-pipeline controls run during the pass, and three of them fired — recorded because a silent one would
have produced a false clearance:

- `git grep -o` is **not supported**: `git grep -oh -E "\bOPENAI_KEY\b" 5ba17044 -- '*.go' | wc -l` returned
  **0** for all eight env-var names. Re-run as `git grep -h -E NAME <ref> -- '*.go' | command grep -o -E
  '\bNAME\b' | wc -l` it returns the real counts. A 0 from the first form is indistinguishable from absence.
- The shell `grep` is `ugrep -G` (BRE): `grep -E 'anthropos-work/(ai|authn) '` matched **nothing** in six
  `go.mod` files that do contain the strings. Re-run without alternation it matched correctly.
- zsh does not word-split `set -- $spec` in a `for` loop, so a per-repo loop silently ran
  `git -C stack-demo/"sentinel f2c461903"` and returned empty for four repos with stderr suppressed. Redone
  explicitly.

---

## Blockers

### B1 — `graphql-wundergraph`'s **prod** state cell still reads `live-standalone` in the row whose own evidence says the router is DESTROYED in production

- **Anchor:** `corpus/architecture/platform-migration-status.md:96` —
  `` | `graphql-wundergraph` | live-standalone | decommissioned | no | **the router, dropped from local dev
  mid-milestone.** … **In prod it is DESTROYED — corrected M257x iter-124.** ``
- **Why it is false:** §1 of this same file (`:49`) defines `live-standalone` as *"its own process, still on
  the traffic path."* The row's own evidence cell states the opposite four clauses later, citing
  `infrastructure/terraform/production/services.tf:509-517` and the ECR hand-deletion on 2026-08-05. The twin
  in `corpus/architecture/org-repos.md:144` states it a third time: *"orphaned — `module.wundergraph_euwest1`
  **is deleted**"*. `external_services.md:3` (a file I also hold) states it a fourth: *"THE ROUTER IS GONE IN
  BOTH STATES."* iter-124's repair reached the **evidence** cell and not the **state** cell — the exact
  one-cell-repair class `platform-alignment.md` §5 rule 54 was written for, recurring inside the table rule 54
  cites as its exemplar. The state column is what a reader scans; the fence cannot see it (§4's own row-class
  table, `:230`, marks merged/decommissioned state cells *prose-under-review*, and I confirmed the guard
  returns GREEN over this file).
- **Confidence:** high

### B2 — `roadrunner`'s **prod** state cell reads `live-standalone` in the row that says there is no roadrunner ECS service

- **Anchor:** `corpus/architecture/platform-migration-status.md:90` —
  `` | `roadrunner` | live-standalone | decommissioned | no | … **`roadrunner` appears in NO terraform in
  `infrastructure` at all** … **There is no roadrunner ECS service, and `roadrunner/terraform/main.tf:19`
  describes nothing.** ``
- **Why it is false:** same mechanism as B1, same file, four rows up. `live-standalone` (`:49`) means *still
  on the traffic path*; the cell's own MEASURED-AT-LAST paragraph says no such service exists, and
  `org-repos.md:143` agrees (*"orphaned — **`roadrunner` appears in NO terraform in the repo**… There is no
  roadrunner ECS service"*). I confirmed from ground truth the half that IS checkable from a clone:
  `roadrunner/terraform/main.tf:19` is `service_desired_count = 1`, `git blame -L 19,19 87d8d443` names
  `84a4b4f1` (2025-12-15) — i.e. the `= 1` is real and, per the row's own measurement, instantiated by
  nothing. So the evidence is right and the state cell is the defect.
- **Confidence:** high

### B3 — §4's assertion C claims **nine** states "in §1"; §1 defines **eight**, and the ninth token a row actually uses is defined nowhere

- **Anchor:** `corpus/architecture/platform-migration-status.md:189` — `` | C | every state is one of the
  **nine** in §1 (markdown emphasis stripped before the check) | … `` — against the §1 table at `:47-56`.
- **Why it is false:** §1's table has exactly **eight** data rows, enumerated: `live-standalone` ·
  `merged-into-app` · `running_but_unfederated` · `mid-fold` · `decommissioned` · `net-new` · `external` ·
  `library` (`sed -n '49,56p'`, 8 rows; the whole table `47-57` is 10 lines = header + separator + 8). The
  `authn` row at `:109` uses **`library-unimported`** in *both* state cells, and that token appears **nowhere
  else in the file** (`grep -n 'library-unimported' …` → the single hit is `:109` itself). So the row that
  §4 points at for its vocabulary cannot validate the row §4's sibling assertion G was built to fix.
  The count "nine" is right about the **guard** and wrong about **§1**: the legal set is hard-coded in
  `stack-core/platform_alignment_guard.py` `ALLOWED_STATES` (authoring copy `f2ea567b`, `:89-99`) and does
  contain 9 including `library-unimported` — it is not parsed from §1 — which is precisely why the doc's own
  table could fall a member behind while the fence stayed GREEN (verified: I ran it; OK over its reach).
  Note the pinned per-stack clone `09d06070` still carries the **8**-member set (`:81-90`), so a stack running
  the shipped fence would go RED on `:109` — the doc, the authoring fence and the shipped fence hold three
  different vocabularies. `:109` itself says *"iter-130 added the `library-unimported` token"*; adding it to
  the guard without adding it to §1 is the whole defect.
- **Confidence:** high

### B4 — §5 still publishes *"unmeasurable is the state"* for `cms`'s production disposition, which the same file's `cms` row RESOLVED

- **Anchor:** `corpus/architecture/platform-migration-status.md:310` — *"…the destruction lands in
  **infrastructure**, a repo no clone set has, so *unmeasurable* is the state, not *unmoved*."* (the clause
  runs `:304-314`).
- **Why it is false:** the `cms` row at `:88` retracts exactly this: *"**RESOLVED at M257x iter-123 — the ECS
  service is DESTROYED.** `infrastructure` was cloned and read (`13c248e6`, 2026-08-07): there is **no
  `module \"cms\"` declaration anywhere in it** … **The blocker this cell named for four iterations — 'the
  destruction happens in infrastructure's `services.tf`, which we cannot read' — was a clone-set limit, not a
  measurement limit, and the fix was to clone the repo.**"* §3's census row at `:158` says the same
  (*"Reading it at iter-123 settled the `cms` row above"*), and `org-repos.md` §3 carries the derivation. One
  document, two live answers to *is cms's prod state measurable* — and §5 is the half a reader reaches when
  asking *where is the program*. §5's clause also still asserts the `cms` row *"reports both and asserts
  neither"*, which is a false description of `:88` as it now stands.
- **Confidence:** high

### B5 — §5 row 3 says the `roadrunner` disagreement *"is still one this map cannot settle without reading infrastructure"* — the `roadrunner` row settles it

- **Anchor:** `corpus/architecture/platform-migration-status.md:315-316` — *"**`roadrunner`** — still the row
  where a repo's own terraform and the platform's declaration disagree, and the disagreement is still one this
  map cannot settle without reading **infrastructure**."*
- **Why it is false:** `:90` says *"**MEASURED AT LAST (M257x iter-123, `infrastructure` `13c248e6`)** … The
  whole line of enquiry below is therefore SETTLED."* The same clause continues *"**`customerio-sync` now sits
  in the same blind spot**… Two rows, one missing input"* — describing a two-member class of which one member
  is no longer in it. A reader who reads §5 for *the rows to watch* is handed a resolved question as open,
  which is what `platform-alignment.md` §5 rule 47 (*close a routed item when its defect is repaired*) exists
  to prevent.
- **Confidence:** high

### B6 — the `roadrunner` row calls `infrastructure` *"a repo this map has never read"* twenty lines after saying it read it

- **Anchor:** `corpus/architecture/platform-migration-status.md:90` — *"Meanwhile the authoritative rollback
  declaration lives in **infrastructure's `services.tf`** — a repo this map has never read"* — against the same
  cell's *"MEASURED AT LAST (M257x iter-123, `infrastructure` `13c248e6`)"*.
- **Why it is false:** the two sentences are in the same table cell and cannot both be true. `:158` and
  `org-repos.md:71` both record the clone and the read (`terraform/production/services.tf`, 666 lines). This is
  a distinct sentence from B2 (which is the state cell) and from B5 (which is §5), so it is booked separately;
  it is the same retracted predicate reaching a third construct inside one row.
- **Confidence:** high

### B7 — the `jobsimulation` row directs the reader to treat `cms` as unasserted and `infrastructure` as unclonable

- **Anchor:** `corpus/architecture/platform-migration-status.md:89` — *"The `cms` row directly above **reports
  both and asserts neither**, and so must anything reading across from this one; the destruction itself lands
  in **infrastructure**, which is in no clone set."*
- **Why it is false:** the `cms` row directly above **does** assert — *"RESOLVED … the ECS service is
  DESTROYED"* (`:88`) — and `infrastructure` was cloned and read at `13c248e6` per `:88`, `:158` and
  `org-repos.md:71`. This row is load-bearing prose (*"do not read `cms` as standing still"*) and it now
  instructs the reader into the retracted reading. Same predicate as B4/B5/B6, fourth site inside one file.
- **Confidence:** high

### B8 — `external_services.md`'s PM summary still calls the router **"Prod-only"**, contradicting the banner ten lines above it

- **Anchor:** `corpus/architecture/external_services.md:14` — *"3. **GraphQL/Wundergraph** - Unifies all
  backend services into a single API. **Prod-only since platform `2adcf71`** — see the two-state note in that
  section"*.
- **Why it is false:** `:3` of the same file: *"**⚠️ THE ROUTER IS GONE IN BOTH STATES — corrected M257x
  iter-124.** … **And it is DESTROYED in production too**: `module.wundergraph_euwest1` is deleted from
  `infrastructure/terraform/production/services.tf` @ `13c248e6` … **This banner said 'in production the router
  is still declared' until iter-124**"*. `org-repos.md:144` and `platform-migration-status.md:96`'s evidence
  cell say the same. The list this line sits in also still frames the router as one of *"four key external
  services"* the platform *"integrates with"* (`:10-15`), present tense, and
  `:804-807` (*"### Wundergraph — Build and deploy as Docker container…"*) still reads as a live production
  deployment instruction. iter-124 repaired the banner and left the summary; a PM-facing summary is the
  highest-propagation paragraph in the file.
- **Confidence:** high

### B9 — the router's own overview table says **"prod-only"** in the row directly under the row that says *"it runs nowhere"*

- **Anchor:** `corpus/architecture/external_services.md:368` — *"| **Purpose** | Federated GraphQL API gateway
  — **over ONE subgraph (`backend`) since `915da06`**, and **prod-only** since platform `2adcf71` deleted it
  from local dev |"*.
- **Why it is false:** `:367`, the immediately preceding row of the same two-column table, reads *"**Past tense
  since M257x iter-124: it runs nowhere.**"* Two adjacent cells of one table, opposite claims. Ground truth
  for the half that is checkable from a clone confirms the *subgraph* half and refutes nothing about it
  (`supergraph-config-prod.yaml` lists `backend` alone at `60c229f3`; the 5→4→3→1 history reproduces exactly at
  `749dc86~1`/`749dc86`/`7c17e63`/`915da06`) — the defect is only the residency clause.
- **Confidence:** high

### B10 — `external_services.md` still publishes *"the prod-side state is UNMEASURABLE … never in a clone set"* for `cms`

- **Anchor:** `corpus/architecture/external_services.md:175` — *"so the two measured facts point opposite ways
  and the prod-side state is **UNMEASURABLE** (the deletion lands in `infrastructure`, never in a clone set)"*.
- **Why it is false:** retracted at `platform-migration-status.md:88` — the file `external_services.md:3` itself
  names as *"the fenced source of truth"* — which records the clone (`infrastructure` `13c248e6`, 2026-08-07),
  the absent `module "cms"`, and the verdict *"the CI commit was the correct signal, and `cms/terraform/main.tf:39`
  is ORPHANED DEAD CODE."* I verified the clone-visible half of that sentence: `cms/terraform/main.tf:39` is
  `service_desired_count = 0` at both `ca50c817` and `f38c0c4a`, and `6efa1d5` (2026-08-04) is
  *"chore(ci): drop build-production — the cms ECR repository is decommissioned (M810)"*. So both cited facts
  are real; the *"UNMEASURABLE"* conclusion drawn from them is the retracted part.
- **Confidence:** high

### B11 — `storage.md` publishes the same retracted *"UNMEASURABLE"* verdict for `cms`

- **Anchor:** `corpus/services/storage.md:175` — *"while `cms`'s ECS module survives **in cms's own repo** at
  `service_desired_count = 0` — but its prod state is **UNMEASURABLE** (`6efa1d5` deleted cms's build workflow
  …, and the deletion itself lands in `infrastructure`, never in a clone set)"*.
- **Why it is false:** identical to B10 — refuted by `platform-migration-status.md:88`, `:158` and
  `org-repos.md` §3. Booked separately because it is a different file with a different reader: the sentence
  sits in `storage.md`'s **Dependencies** section, immediately after the (correct) statement that
  jobsimulation's ECS service *is* destroyed, so a reader comparing the two folds is told one is measured and
  the other is unmeasurable when both are measured and both are destroyed.
- **Confidence:** high

### B12 — `db-backup`'s **prod** state cell reads `live-standalone` for a task nothing fires

- **Anchor:** `corpus/architecture/platform-migration-status.md:102` — `` | `db-backup` | live-standalone | — |
  no | production-only … **Deployed but not triggered** … `` .
- **Why it is false:** §1 `:49` defines `live-standalone` as *"its own process, still on the traffic path"*. The
  same cell says the EventBridge rule and target have been commented out since `7dd1b80` (2025-05-29) and that
  production pins that commit; `corpus/services/db-backup.md:21` states it as *"Everything needed to run a
  backup is deployed; nothing fires it"*, `security_compliance.md:315` as *"**Not currently running.**"*, and
  `service_taxonomy.md:528` as *"an ECS **task definition with no live trigger**"*. So the map's own state
  column asserts the one thing four sibling documents were repaired to deny. **Why I hesitated:** unlike B1/B2
  the correct token is not obvious — none of §1's eight fits a deployed-but-untriggered scheduled task — so
  this is arguably a vocabulary gap rather than a false claim. I book it as a BLOCKER because the cell as it
  stands is read as *running*, and it is the state column that the file's own §4 says nothing fences.
  (The `—` in the local column is **not** a defect: `NOT_APPLICABLE = {"—", "-", "n/a"}` is explicit in the
  guard, authoring copy `f2ea567b`, `platform_alignment_guard.py:94`.)
- **Confidence:** medium

---

## Minors

- `platform-migration-status.md:189-193` — the §4 assertion table lists rows **A, B, C, D, E, G, F**: `G`
  (added iter-130) was inserted before `F` instead of after it. No factual consequence.
- `platform-migration-status.md:93` — *"`messenger/terraform/main.tf:29` `service_desired_count = 0` in a
  module that is otherwise intact (121 lines)"* names **no messenger ref**. At the **checkout** `fa47850d` the
  file is 111 lines and `:29` is `container_definitions = <<EOF`; the claim (and the 121-line count) is exact
  at `origin/main` `e9421c68f`, which is the ref the guards' `CITE_REF=auto` ladder would pick and which
  `storage.md:23` establishes as the convention for a prod claim. Non-material, but it is the one citation in
  the row that flips construct between the two legitimate refs — the rule the `storage` row states in bold and
  this row does not follow.
- `platform-migration-status.md:214`/`:255` — the fence-reach figures printed in the doc (**25 / 70 / 1 = 96**)
  now measure **25 / 72 / 1 = 98**, plus a fourth bucket the doc's sample line has no column for (`10
  unclonable`). Explicitly pre-disclaimed at `:244` (*"Take those three numbers from the run, never from this
  table"*), so non-material — recorded only so the next reader does not re-derive it.
- `external_services.md:636` — cites `config_template.ini:30-31` without its directory; in studio-room
  `aeec036a` the path is `configs/config_template.ini` (`git show aeec036a:config_template.ini` → *"path …
  does not exist"*). Lines 30-31 there do say `OPENAI_API_KEY` / `OPENAI_ENDPOINT =
  https://api.openai.com/v1/chat/completions`, so the claim is correct and only the path is short — and the
  same paragraph writes the full `app/studio/configs/*.ini` four lines later.
- `storage.md:205` — *"`make setup`/`make gen` exist in the Makefile but are legacy no-ops"*. `make gen` is
  (`go generate ./...` with zero `//go:generate` directives, verified: `git grep 'go:generate' 4ce8ece5` →
  rc 1). `make setup` is `go install github.com/99designs/gqlgen@latest` (`Makefile:7`) — vestigial, but it
  does something. The sentence's own parenthetical gets it right.
- `corpus/services/db-backup.md:40` — *"db-backup `pg_dump`s the platform Postgres … and uploads the archive to
  **two** destinations"*, present tense, five lines above `:45` *"**It is not currently triggered by anything**"*.
  The banner disambiguates it; the Role paragraph on its own reads as a running job.

---

## Positively cleared (audited zeros)

**ENUMERATED** — predicate re-derived from source independently, cardinality compared:

1. **`repos.yml`'s historical name set (`platform-migration-status.md:75-76`, "14 names").** Derived
   independently: `git -C stack-demo/platform log -p --follow -- repos.yml | grep -E '^\+  - name: ' | sort -u`
   → **14**, and the 14 names match the doc's list element-for-element.
2. **`docker-compose.yml`'s historical service set (`:77-81`, "25 services, 26 section-blind, the extra is
   `app-network`").** Same command on that file → **26** two-space keys; the set is exactly the doc's list plus
   `app-network`, which `0c91421:docker-compose.yml:185-186` declares under `networks:`. All 25 have rows, and
   the three filed under a repo name (`backend`→`app`; `graphql`/`wundergraph`→`graphql-wundergraph`) are the
   three the doc names.
3. **The §1 state vocabulary (`:47-56`).** Enumerated before doing the arithmetic — **8**, not 9. This is B3;
   recording it here because the enumeration is the clearance method that found it.
4. **The supergraph subgraph history (`external_services.md:373-386`).** Counted `^  - name:` in
   `supergraph-config-prod.yaml` at each cited commit: `749dc86~1` = **5** (backend, skiller, jobsimulation,
   cms, skillpath), `749dc86` = **4**, `7c17e63` = **3**, `915da06` = **1**. Dates match. `git show
   --name-status 915da06` marks **both** `schemas/cms.graphqls` and `schemas/jobsimulation.graphqls` `D`, so
   the "3 → 1, not 2 → 1" correction is right and the commit-subject warning is warranted.
5. **"No subscriptions" (`external_services.md:387`).** Both legs enumerated at `60c229f3`: `git grep -nEi
   'sse|subscription' -- '*.yaml'` → rc 1 with a non-vacuous control (all three yamls match `backend`);
   `schemas/backend.graphqls` has `type Mutation` at **:4053**, `type Query` at **:4912**, no `type
   Subscription`, and **6** `Subscription` substrings, all Stripe/plan (`activeSubscription`,
   `type PlanSubscription`, `stripeSubscriptionId` ×3, `stripeSubscriptionItemId`). Mainline `git log -S
   'protocol: "ws"'` = **0** and `-S 'sse_post'` = **5**, exactly as published; `bba862f` is not an ancestor of
   HEAD and lives only on `remotes/origin/feat/use-web-socket`.
6. **The four AI env-var occurrence counts (`external_services.md:668-671`, "5 / 26 / 13 / 13").** Re-derived at
   the pinned ref `5ba17044` over `*.go` with word boundaries: **5 / 26 / 13 / 13**, and `OPENAI_ORG_ID` /
   `AZURE_OPENAI_ENDPOINT` / `AZURE_OPENAI_DEPLOYMENT` = **0 / 0 / 0**, `OPENAI_API_KEY` = **2**. Every figure
   matches. (This is the one where `git grep -o` returned all zeros; see the controls section.)
7. **The taxonomy figures (`external_services.md:691-704`).** Read the manifest the passage cites
   (`.agentspace/snapshots/taxonomy/5afc0bccf1df7ef538b643321fc6362f/manifest.json`): `public_only: true`,
   `predicate: org-null`, `captured_at 2026-06-29T14:43:00Z`, rows **skills 42790 · job_roles 22470 ·
   skill_embeddings 42790 · job_role_embeddings 18919**. Every number and both qualifiers match, including the
   18,919-is-an-embedding-count explanation for the refuted "18K roles".
8. **`app`'s module graph (`platform-migration-status.md:108`, `:109`, `external_services.md:554`).** Enumerated
   `anthropos-work` requires per repo at the refs the cells name: `app` `ad9f3c498` = analytics-go, colony,
   proto, storage, taxonomy (**no `ai`, no `authn`**, `go.sum` 0 hits each, colony control = 2); `sentinel`
   `f2c461903`, `storage` `9f8cb5322`, `messenger` `e9421c68f`, `roadrunner` `87d8d4438` = none of either;
   `cms` `f38c0c4a4` and `jobsimulation` `82cb66ecc` = `ai v1.40.2` (the only two, both frozen). `app/go.mod`'s
   sole `replace` is `getsentry/sentry-go/echo`, so `watermill-redisstream v1.4.5` (`:12`) is genuinely
   upstream-unreplaced, and `analytics-go v0.3.1` is at `:14`. `internal/ai` is imported by exactly **67** `.go`
   files. rext pins `ai v1.40.1` in `stack-seeding/go.mod:8` — identical in **both** rext trees.
9. **The compose service census at each cited ref.** Enumerated `^  <key>:` under `services:` per commit:
   `2adcf71` = **11**, `d11a403` = **8**, `838d907`/`0c91421` = **5**; `repos.yml` = 9 → 6 → **4**. Confirms
   `roadrunner`'s *"one of eleven / left eight / now five"* (`:90`), `storage.md:28`'s four entries, and
   `external_services.md:305`'s *"five services, seven effective"* (`common.yml` adds `postgresql` `:2` and
   `redis` `:24`, neither with a `profiles:` key, so the always-on floor is exactly {postgresql, redis,
   sentinel}).
10. **`d11a403`'s reach.** The commit subject names only `roadrunner`, but the diff removes `cms`,
    `jobsimulation` **and** `roadrunner` from both `repos.yml` and compose (its parent is `2adcf71`; before/after
    enumerated). `storage.md:175` and `platform-migration-status.md:88`/`:89` are therefore right to attribute
    both deletions to it — graded by diff, not by message.
11. **`askengine.md`'s registry (`:47-48`, "60 TableDefs, 57 public, 3 directus").** Enumerated at `ad9f3c49`:
    60 `Name:` entries, `Schema: "public"` = 57, `Schema: "directus"` = 3. `registry.go:183-195` is the
    jobsimulation transition-alias comment the sentence describes, and the entries below it do carry
    `Schema: "public"` with `RealName:` overrides — the "dotted name is not a physical schema" claim holds.
12. **`askengine.md`'s test counts (`:127-132`).** `grep -c '^func Test'`: sandbox **49**, executor **13**,
    prompt **10**, followups **14**. All four exact. `rules.md` is **146,219 bytes** ("~146 KB").
13. **The `/ask` route set (`askengine.md:64-67`).** Enumerated at `internal/web/backend/backend.go:171-188`:
    all 11 routes present with the documented verbs and paths, the group carries CORS + `EchoAuthnMiddleware`,
    and `:168-170` states the OpenAPI-skip reason in the platform's own words.
14. **The 30 studio-room model pins (`external_services.md:619`, `:640`).** `git grep -hE '_AI_[A-Z_]*MODEL'
    aeec036a -- 'configs/*.ini'` → **30** lines, **every one** beginning `azure,`. Three `.ini` files.
    `gen.py:45-48` is a six-name list (`AZURE_API_KEY`, `AZURE_ENDPOINT`, `OPENAI_API_KEY`, `OPENAI_ENDPOINT`,
    `ANTHROPIC_API_KEY`, `ANTHROPIC_ENDPOINT`) and the loop at `:50-53` overrides only those — never the
    service selector, so the "latent, not live" correction is right.

**Exact-citation clearances** (opened the cited file and read around the line; construct matches the sentence):

- `storage.md` — every `app` anchor at `ad9f3c49` (`main.go:504`, `:516-517`, `:518-523`, `:524`, `:525`,
  `:529-535`, `:547`, `:1102`; `env_guards.go:37-44`; `internal/storage/service.go:22`, `:24`;
  `internal/storage/storage.go:193-200`, byte-identical at `2035f9a4` per an empty `git diff --stat`), every
  platform anchor at `0c91421` (`docker-compose.yml:56`, `:73-79`, `:82`, `:83`, `:100`, `:102-103`;
  `README.md:81-87`), the historical compose block at `0dab54d` (`:117-123`, **seven** environment entries,
  ports `8300:8300`/`8301:8301`, `profiles: [storage-legacy]`) and at `2adcf71` (`:206`, `:210`, and `:324`
  genuinely inside the studio-desk block), the storage repo at `4ce8ece5` (Go 1.25, 18-line `main.go`, zero
  `_test.go`, `Dockerfile:18` `RUN go test -v ./...`, `internal/storage/storage.go:122`, the five RPC methods,
  the 15-minute presigned default, the SDK signatures, the directory map) and at `9f8cb5322` (18-line
  `terraform/main.tf` with `:9-11`/`:13-16`/`:18` quoted verbatim, `storage.tf:22-25` M903 text verbatim, zero
  `moved` blocks, **`custody` = 0 hits**, exactly **20** commits behind, `d3e6d32` = *"correct the guard
  header…"* 2026-08-05, and `4ce8ece5`'s own header still reading *"relocate the assets out of this module
  BEFORE M907"*). `STORAGE_RPC_ADDR` at `ad9f3c49` = **3 hits, all comments**, at the three named paths; at
  `b948604f` = **15 hits, 7 env reads** (3 in `main.go`, 1 in `jobsimwiring`, 1 in each of **three** `cmd/`
  tools), the remaining 8 being 2 doc comments, 2 error strings, 3 `t.Setenv` and 1 `t.Fatal` — the row's
  unusually precise breakdown is exactly right.
- `askengine.md` — `main.go:229` (`func main`), `:467-471` (the bedrock block, `return` at one level inside
  `main`, no intervening `func` between `:229` and `:467`), `:1295` (Connect-RPC mux), `:1361` (the meta-server
  comment); `askengine/bedrock.go:161` with its **single** error return at `:167`; `DefaultModelID` /
  `DefaultRegion` at `:25-26`; `executor.go:18`/`:21`/`:24` and the READ ONLY tx at `:90`/`:97`;
  `handler.go:34`/`:41` (15 turns, 10-minute detached loop); `main.go:167` `askEngineMaxConns = 6`, `:351`
  `COPILOT_DB_CONN`; `GetTalkToDataCustomRules` at `organization/manager.go:1325`; `OrgCheckFeaturePermission`
  at `handler.go:718`; tags `v1.267.0` = 2026-05-07 and `v1.340.0` = 2026-07-17. **I found no defect in this
  file.** One point worth recording rather than booking: `askengine.md:44` says `WrapQuery` *"defends four
  documented bypass vectors"* while `sandbox.go:350` says *"three"* and then enumerates **four** bullets — the
  corpus counted the enumeration, the source's own word is the stale one, so the corpus is right.
- `platform-migration-status.md` — all seven `app` wiring call sites (`:395`, `:524`, `:690`, `:721`, `:751`,
  `:1153`, `:1471`), the messenger consumer-group anchors (`:15`, `:62`, `:63`, `:285`, `:1416-1421`, `:1445`,
  `:1450`, `:1473`, `:1552`), `env_guards.go:92-111`, `terraform/main.tf:181`/`:415-416`/`:419-420`,
  `jobsimwiring/wiring.go:123`, `internal/converter/gotenberg.go:31`, `internal/payments/handler.go:302-316`,
  and the absence of `app/internal/roadrunner/` (the seven folded domain dirs exist; `roadrunner` does not).
  Platform: `:5`, `:18`, `:28`, `:48`, `:57`, `:59`, `:101-109`, `:110`, `:119`, `:135`, `:141`, `:143`,
  `:151`, `:160`, `:168`, `:170-171`, `:183`, `:185-186`; `common.yml:2`, `:22`, `:24`; `repos.yml:2-10`,
  `:12-13`, `:14-17`, `:18-20`, `:23-25`, `:26-28` (28 lines). Sibling repos: `jobsimulation/terraform/main.tf:15-22`
  and `:24-40` at `82cb66ecc` (verbatim), `cms/terraform/main.tf:39` at both cms refs,
  `messenger/terraform/main.tf:29`/`:19-25`/`:27-28` and `cmd/root.go:120-140` at `e9421c68f`,
  `roadrunner/terraform/main.tf:19` + `git blame` → `84a4b4f1` 2025-12-15 + `e45eb61`'s diff touching **line
  11 only**. Commit dates/subjects for `a2a3ee6`, `cb6ebf5`, `ef4b449`, `1474b1f`, `84862d1`, `b43b99a`,
  `c17cc9a`, `467965a`, `8770fe6` (4 days after the first commit), `21429b7`, `a4db680`, `045857c`, `fdfa189`,
  `6060315`, `d11a403`, `838d907`, `0dab54d`, `236771f`, `915da06`, `6092c6d2`, `6efa1d5` — all correct. App
  tag map: `5ba17044`=v1.363.2, `b948604f`=v1.366.0, `9d00a313`=v1.367.0, `2035f9a4`=v1.369.0-2 ("post-v1.369.0"),
  `ad9f3c49`=v1.369.0-7; `git diff --stat 2035f9a4 ad9f3c49` is exactly the five non-Go files the `app` row
  names, and the `terraform/main.tf` hunk is the single precondition-message rewrite it claims.
- `external_services.md` — the Directus wiring (`docker-compose.yml:53` at **both** `0dab54d` and `0c91421`,
  the `:46-94` environment block, `.env_example:91-92`, `app/main.go:980-982` at `b948604`), the **pinned**
  rext anchors `gen_injected_override.py:86` / `:77-81` / `:698-699`, `stack-core/gen_override.py:58` and
  `stack-injection/tests/test_injection.py:1109` (all four resolve byte-exact at `09d06070`, and the retired
  `test_only_cms_is_repointed_not_other_services` survives only in fixtures), the router build-era table
  (`63d285c` service key really is `wundergraph:`; `2c85211^` really has no `dockerfile:` key; `67ba772`
  2026-03-02; `b56d731` keeps `graphql:` at `:22` behind `wundergraph-deprecated` while `360efd4` deletes it
  and `backend:` sits at `:28`; `1e8e754` = `2adcf71^1` with `depends_on` at `:19-27` naming exactly backend /
  jobsimulation / cms / storage and `"5050:8080"` at `:14`; `464dfe3` is **not** an ancestor of `0c91421d` and
  lives only on `origin/feat/cms-in-app`; `graphql-wundergraph/CLAUDE.md:39` really does assert the opposite,
  written 2026-07-30 in `60c229f`), the router ports (`locals.tf:8`, `main.tf:48-49`, `config.prod.yaml:5`),
  `Dockerfile.dev` (`wgc@0.104.0` at `:7`, one schema COPY at `:18`, the comment at `:19-20`, one `awk` at
  `:23`, router image `0.275.0` at `:29`), the whole AI-routing block (`jobsimulation/ai/ai.go:30-33`,
  `:80`, `:130-141`, `:150-155`, `:259-289`, `:264-276`, `:280-283`, `:296-302`;
  `simulator/ai/ai.go:58-59` and `:114-115`; `cms/directus/collections/jobsimulation.go:905` and `:1302`),
  the Mistral-is-OCR-only claim (`markdownManager.go:10/:14/:29/:30`, `studioManager.go:583`,
  **`mistral.NewMistral` = 0 hits**, `studio/tools/pdf2md.py:24` + `mistral-ocr-latest` at `:127`,
  `r3.py:139`/`:190`/`:199-206` dispatching it as step 2), `coursebuilder/bedrock.go:23`/`:29`/`:106-113`,
  `askengine/bedrock.go:25`, `jobsimulation/agent/report_agent.go:31`, `main.go:770` at `b948604`,
  `studio/services/ai.py:383`/`:627`/`:704-724`, `configs/config_template.ini:30-31`, the entire LiveKit
  paragraph (`calls/livekit.go:101-104`, `:110`, `:111`, `:115`, `:118-128`, `:120`, `:122-123`, `:126`, and
  **`anthropos-agent-eu` = 0 hits**), `flag_use_realtime_openai` at `:133` changing the *endpoint* not the
  engine, `voiceEngineFromDirectus`'s nil default `gptrealtime` at `jobsimulation.go:1595-1596`,
  `backend.go:130`, `docker-compose.yml:101-109`, studio-desk's `AI_PROVIDER_CHAIN=azure-openai,openai`
  (`.env.example:56`) and `src/services/ai/config.ts:35-37` (`claude-opus-4-5-20251101`,
  `claude-sonnet-4-5-20250929`). The two in-file self-citations `:543` and `:546` and `:567` all land on the
  constructs they name.

**RE-DERIVED only** (weaker — recorded as such per §5 rule 24): the `98 = 25 + 72 + 1` guard-reach arithmetic
in `platform-migration-status.md:213-214`/`:255`; the `5 → 4 → 3 → 1` deltas in `external_services.md:377-380`
(the *set* at each commit is the ENUMERATED item 4 above; only the differences are re-derived).

---

## What I could not settle

1. **Every `infrastructure/**` citation.** `infrastructure` is in no clone set on this box (16 git trees
   enumerated; the guard itself reports `UNCLONABLE head 'infrastructure' x9`). So
   `platform-migration-status.md:88`, `:93`, `:96`, `:102`, `:158` and `external_services.md:3` cite
   `services.tf:64-70`, `:85-86`, `:88-94`, `:509-517`, `:521`, `:571`, `:622`, `:664` and
   `modules/core/storage/rds.tf` at `13c248e6` — none read. **Note the asymmetry this creates for B4/B5/B6/B7
   and B10/B11:** I cannot confirm the *content* of the iter-123 measurement, but the contradiction I book is
   internal — the corpus says both *"we read it and it is settled"* and *"we have never read it"*, and one of
   those is wrong regardless of what the repo says. Settled by: `git clone anthropos-work/infrastructure` at
   `13c248e6` and reading the eight cited line ranges.
2. **All of `db-backup.md`.** The repo is in no clone set (guard: `UNCLONABLE head 'db-backup' x1`), so
   `Dockerfile:1`/`:4-9`/`:19`, `backup.sh:8-10`/`:20`/`:33`, `terraform/main.tf:10-14`/`:13`/`:16-27`/`:51`/`:215`,
   `storage.tf:24-38`, `terraform/stage/main.tf` = 0 lines, the *"157 objects"* history sweep and `7dd1b80`
   are all unverified. The page is internally coherent and its retraction has propagated corpus-wide
   (`CLAUDE.md:250`, `dependency_map.md:38`, `architecture_overview.md:206`/`:454`, `security_compliance.md:13`/
   `:315`/`:322`, `service_taxonomy.md:173`/`:528`, `services/README.md:48` — all now say Bash / two targets /
   never Azure / untriggered), so there is no live contradiction to book; I simply cannot confirm the
   measurements. Settled by cloning `anthropos-work/db-backup` at `6e1fb15b`.
3. **`colony/go.mod:9`** (`platform-migration-status.md:165`, the watermill fork row). `colony` is a private
   module pulled at Docker build, never cloned. The `app` half of that sentence I did verify
   (`app/go.mod:12` = upstream `v1.4.5`, no `replace` anywhere but sentry).
4. **GitHub archive states** — `graphql-wundergraph` ARCHIVED 2026-07-30, `skiller` 2026-07-01, `skillpath`
   2026-07-31, `intelligence` 2026-04-02, `chronos` NOT archived. Archive state lives in the org API, not in
   git objects. The `jobsimulation` row (`:89`) is the only one that reports this honestly (*"REPORT BOTH,
   ASSERT NEITHER"*), and its reasoning — four commits dated 2026-08-04 including a PR merge, which an archived
   repo cannot take — I did confirm from the clone.
5. **The §3 census scalars** (`:126-133`): "93 repositories", "46 named by no corpus document", and every
   per-repo `last push` date; plus `AI-Labs`'s *"96 scenario templates, not the 15 its own README claims"*,
   `sim-qa`'s mutation count, `metabase`'s `desired_count = 1`, `hyper-studio`'s 827 commits. All need the
   GitHub org API or clones of repos not present. The two derived-from-`repos.yml` figures inside that
   paragraph (**4** now, **9** until `d11a403`, **6** until `838d907`) I did enumerate and they are exact.
6. **`analytics-go` carrying Stripe events *to Brevo*** (`:157`). I confirmed the call site
   (`app/internal/payments/handler.go:302-316`, inside the subscription-lifecycle path) and the pin
   (`app/go.mod:14`, `v0.3.1`); the destination is inside the un-cloned `analytics-go` module.
7. **Whether `merged-into-app` is the right token for `messenger` and `customerio-sync`.** §1 `:50` requires
   *"`app` owns the code and calls it **unconditionally**"* — **all three** conditions, emphasised. Both are
   gated behind `MESSENGER_ENABLED` / `CUSTOMERIO_SYNC_ENABLED`, which the rows themselves say are **off when
   unset on a developer machine** (`app/main.go:285`, `:286`; `env_guards.go:92-111`, all verified). In
   production both are pinned `"true"` (`app/terraform/main.tf:415-416`, `:419-420`, verified), so the prod
   cell survives on a reading of "unconditionally" as *in the deployed environment*. I did not book it because
   the definition is ambiguous rather than the cell wrong, but a one-clause fix to §1 `:50` would remove the
   ambiguity permanently. Settled by: deciding whether §1's "unconditionally" is scoped per-column.
