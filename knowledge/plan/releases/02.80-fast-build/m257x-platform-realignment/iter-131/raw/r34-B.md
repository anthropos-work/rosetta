# Seat B — reading #33/#34 (M257x iter-131)

Ground truth re-verified at this seat's open (all from the ADDENDUM's superseded table):
`app ad9f3c49` · `platform 0c91421d` · `next-web-app 8297c684` · `sentinel f2c46190` ·
`studio-desk 41ee3575` · `ant-academy 22df69dd` · `storage 4ce8ece5` · `messenger fa47850d` ·
`cms ca50c817` · `graphql-wundergraph 60c229f3` · `roadrunner 87d8d443` · `jobsimulation 462343b0` ·
`app/studio` + `cms/studio` both `aeec036a` · **pinned rext `stack-demo/rosetta-extensions 09d06070`** ·
**authoring rext `.agentspace/rosetta-extensions f2ea567b`**.
Where a claim is about tooling-on-a-stack I read the **pinned** clone and say so; where it is about
the mirror's own code/DNA I read **both** trees and they were identical on every predicate checked.
`corpus/ops/platform-alignment.md` §5 read in full (lines 259–1891) before starting.

## Positive controls

| file | `wc -l` | read in full? |
|---|---|---|
| `corpus/services/ai-readiness.md` | 719 | yes (720 lines rendered — final line has no trailing newline) |
| `corpus/services/clerkenstein.md` | 383 | yes |
| `corpus/services/chronos.md` | 252 | yes |
| `corpus/services/sentinel.md` | 171 | yes |
| `corpus/architecture/dependency_map.md` | 103 | yes |
| `corpus/services/README.md` | 90 | yes |
| (protocol) `corpus/ops/platform-alignment.md` | 3029 | §5 in full |

---

## Blockers

### B1 — `sentinel.md`'s published grep derivation does not reproduce: it returns ZERO, not "one unrelated hit"

- **Anchor:** `corpus/services/sentinel.md:5` — *"What survives, and was re-derived: messenger's Go
  source imports no authorization client (`git grep "authorization\|AUTHORIZATION_ADDRESS" fa47850d --
  '*.go' go.mod` **returns one unrelated hit**, against `colony` present as a positive control)"*
- **Why it is false:** I ran the exact published invocation in `stack-demo/messenger`, at the named ref:

  ```
  git grep "authorization\|AUTHORIZATION_ADDRESS" fa47850d -- '*.go' go.mod   → exit 1, 0 lines
  git grep -E "authorization|AUTHORIZATION_ADDRESS" fa47850d -- '*.go' go.mod → exit 1, 0 lines
  git grep -in "authoriz" fa47850d          (WHOLE TREE, no pathspec)        → 0 lines
  ```
  Positive control on the identical invocation form (`git grep -n "colony" fa47850d -- '*.go' go.mod`)
  returns hits in **9** files starting at `messenger/cmd/root.go:11`, so the pipeline is live and the
  ref/pathspec resolve. The clone is clean (`git status --porcelain` empty) at
  `fa47850d9c507d1928da7a38f7b37bac1bb8fabc`. There is **no** occurrence of the substring `authoriz`
  (case-insensitive) anywhere in the `messenger` repo at that ref, so the stated result cannot be
  produced by any reading of that command. (The nearest thing to "one unrelated hit" in that repo is
  the word *sentinel* at `pkg/aireadinessemail/override.go:378`, which is a **different pattern**.)
- **Why it matters rather than being cosmetic:** the sentence is the *load-bearing evidence* for a
  retraction — the passage explicitly re-derives *"what survives"* after conceding the previous
  present-tense claim expired. A reader auditing the retraction re-runs the command it ships, gets a
  different answer than the doc states, and cannot tell whether the corpus is stale or the predicate
  moved. §5 rule 24: a document that shows its own derivation is harder to audit, and here the
  derivation does not reproduce.
- **I hesitated because:** the *predicate* the number supports ("messenger's Go source imports no
  authorization client") is **TRUE**, and true more strongly than stated. Booked as a BLOCKER under the
  instrument's "wrong counts" clause and its resolve-doubt-upward rule; the repair is a one-word fix
  ("returns **no** hits").
- **Confidence:** high (that the command returns 0); medium on the BLOCKER/MINOR grading.

### B2 — `ai-readiness.md` states a universal about the workforce↔aireadiness split that the interface it cites refutes in its own doc comment

- **Anchor:** `corpus/services/ai-readiness.md:18-20` — *"**The only remaining dependency on
  `workforce` is the member directory** (the `WorkforceDirectory` interface — `LoadMembers`/
  `LoadMembersByUserIDs`, whose implementations **stayed** in `app/internal/workforce/members.go`)."*
  (No ref named in this block ⇒ graded at the checkout, `app ad9f3c49`.)
- **Why it is false**, three ways, in ascending order of severity:
  1. **The interface has FOUR methods, not two.** `app/internal/aireadiness/manager.go:40-51` declares
     `LoadMembers` (`:43`), `LoadMembersByUserIDs` (`:45`), **`BaseMembers`** (`:48`) and
     **`LevelsCount`** (`:50`).
  2. **`LevelsCount` is not a member-directory call, and the source says so in the sentence directly
     above the interface.** `manager.go:36-39`: *"WorkforceDirectory is the slice of the workforce
     manager this domain needs: the active-member directory … **and the org's skill-scale setting**."*
     `LevelsCount(ctx, orgID) float64` returns the org's `levels_count` and is consumed at
     `app/internal/aireadiness/readiness.go:770` — `maxLevel := int(m.workforce.LevelsCount(ctx, orgID))`
     → `hwm.MaxSkillLevel` (`:771`), which is what the Step-2 skill dots render out of. So the domain's
     remaining dependency on `workforce` is *the member directory **plus an org setting***, and the
     corpus's "only … is" is the exact quantifier the code contradicts.
  3. **The "implementations stayed in `members.go`" clause is wrong for the member it omits.**
     `LoadMembers`/`LoadMembersByUserIDs`/`BaseMembers` are at `internal/workforce/members.go:349`,
     `:353`, `:357` — but `LevelsCount` is at **`internal/workforce/manager.go:90`**.
  For calibration on the surrounding coupling: `internal/aireadiness` imports `internal/workforce` in
  **8 non-test files** (`auto_assign.go`, `breakdown_enrichment.go`, `freeze_backfill.go`,
  `live_snapshots.go`, `manager.go`, `readiness.go`, `recommendation_engine.go`,
  `steps_completion.go`) and uses `workforce.Member` as a type in **60** places.
- **Why it would mislead real work:** this paragraph is the M247 refactor's summary of what the split
  left behind, and the block immediately below it (`:37-48`, the D-07 demopatch re-anchor) reasons
  *from* it — *"the bounded swap `LoadMembers → LoadMembersByUserIDs` is now expressible at that
  interface call site, since `WorkforceDirectory` already exposes `LoadMembersByUserIDs`"*. Anyone
  planning further extraction, or re-anchoring the patch, is told the seam is two member-loading
  methods when it is four methods spanning two concerns and two files.
- **Confidence:** high.

---

## Minors

- `corpus/services/sentinel.md:43-48` — *"only as a fixture string in sentinel's own tests
  (`internal/authorization/casbin_test.go`, `internal/rpcsrv/rpc_test.go`)"*. The enumeration omits a
  third file: `internal/authorization/manager_test.go:21` `manager := "manager"` @ `f2c46190`. The
  predicate (tests only; zero hits in `init_policy.sql` — confirmed, exit 1) holds; only the list is
  short. Canonical carve-out class (omitted list member).
- `corpus/services/sentinel.md:31` vs `:41` — the same two-argument Casbin grouping is written
  `g3(org, user)` in the `m6` row and `g3(org, membership)` in the role-grouping list. Source is
  `g3 = _, _` used as `g3(p6.org, r6.sub)` (`sentinel/internal/authorization/casbin.go:33`, `:44`).
  No factual consequence, but the two rows disagree about the second argument's name.
- `corpus/services/ai-readiness.md:645-646` — *"`computeCycleTotals` (`how_we_measure.go:253-261`)"*.
  At `app ad9f3c49` the function's doc comment opens at `:256` and `func` is at `:260`; `:253-255` are
  the tail of the **previous** function (`return out, rows.Err()` / `}` / blank). The range does
  contain the declaration, so it is a 3-line range-start slip rather than a wrong construct.
- `corpus/services/ai-readiness.md:507` — *"Retracted here and at
  [`ops/demo/stories-spec.md:599`]"*. `stories-spec.md:599` is the retracted claim itself
  (*"an **active** cycle (the dashboard live-recomputes from signals) …"*); the retraction blockquote
  is `:603-608`. Same paragraph, 4 lines short of its target.
- `corpus/services/ai-readiness.md:573` — *"`deriveMode` (`useAIReadiness.ts:48-62`)"* is a **bare
  basename that is not unique in the repo**: `next-web-app @ 8297c684` has both
  `apps/web/src/hooks/useAIReadiness.ts` and `apps/web/src/components/ai-readiness/useAIReadiness.ts`.
  The citation is correct for the second (`deriveMode` at `:48`, closing `}` at `:62`, null deadline →
  `deadlinePassed = true` at `:55-57`), while **this same document** cites the *first* by full path at
  `:660` (`apps/web/src/hooks/useAIReadiness.ts:326`). Resolvable only by guessing; §5 rule 36's
  "resolve only on uniqueness" case.
- `corpus/services/ai-readiness.md:323-324` — *"`aireadiness.Manager` … is constructed with `db`,
  `ent`, `orgRepo`, `ai.AI`, a pub `EventPublisher`, the `academy.EmbeddingsManager`, and the
  `WorkforceDirectory` (member directory)"*. `NewManager` (`manager.go:135`) takes
  `(logger, db, aiClient, entClient, pub, academyEmbeddings, directory)` — **`orgRepo` is not a
  parameter**; it is derived inside from `entClient` (`:140`, `repository.NewEntRepository(entClient).Organization`),
  alongside `assignmentRepo` (`:141`), and `logger` is a parameter the list omits. I nearly booked this
  as a BLOCKER (an argument list that names a non-argument), but "constructed with" reads defensibly as
  a collaborator list rather than a signature. The trailing *"(member directory)"* gloss is B2's
  under-description repeated.
- `corpus/architecture/dependency_map.md:19` — *"`docker-compose.yml:126-131` for the `depends_on`"*
  @ `0dab54d`. The `depends_on:` block is `:126-130`; `:131` is the `# v9.0: NOT in the default
  profiles any more …` comment. One line long. (The sibling citation in the same sentence, `:117-123`
  for the seven-variable env block, is **exact** — I re-derived the set: `AWS_DEFAULT_REGION`,
  `AWS_REGION`, `ENVIRONMENT`, `PORT`, `RPC_PORT`, `SERVICE_NAME`, `STORAGE_S3_PUBLIC_BUCKET` = 7,
  with `:116` the `environment:` key exactly as the ⚠️ says.)

---

## Positively cleared (audited zeros)

**ENUMERATED** — predicate re-derived from source independently, cardinality compared:

- **`clerkenstein.md:21-22` — "eleven sections".** `git ls-tree` of both rext trees: `alignment`,
  `clerkenstein`, `demo-stack`, `dev-stack`, `playthroughs`, `stack-core`, `stack-injection`,
  `stack-secrets`, `stack-seeding`, `stack-snapshot`, `stack-verify` = **11**, identical in the pinned
  clone `09d06070` and the authoring copy `f2ea567b`. The self-correction ("this listed six until
  iter-129") is correct.
- **`clerkenstein.md:45-48,78` — every DNA gene count.** Parsed the five JSON DNAs at
  `f2ea567b:clerkenstein/alignment/dna/*` and summed variants per capability:
  `clerk-2.6.0` **27 genes / 14 capabilities**; `clerk-js-5` **9**; `clerk-multi-1` **9**;
  `clerk-express-1` **13 genes / 5 capabilities**; `clerk-deploy-1` **7**. All five match.
- **`clerkenstein.md:101-110` — the repo-structure table.** `ls-tree` of `clerkenstein/`: the eight
  named dirs are exactly `authn`, `clerk-backend`, `clerk-frontend`, `clerk-webhook`, `shared`,
  `deploy`, `cmd`, `alignment`; `alignment/` holds exactly `cmd/{clerkrun,deployrun,expressrun,jsfapirun,multirun}`,
  `dna/` (5 files), `golden{,-js,-multi,-express,-deploy}/` and `scripts/`.
- **`clerkenstein.md:177` — "`r.Cookie(...)` is called nowhere in `clerkenstein/`".** `git grep
  "\.Cookie(" f2ea567b -- clerkenstein/` → exit 1, 0 hits; positive control `http.Request` in the same
  scope returns hits across ≥5 files.
- **`ai-readiness.md:288-291, 412, 417, 614 — the 31-skill / 25.0-weight / 3-sim defaults.**
  Enumerated `defaultReadinessSkills` at `app ad9f3c49:internal/aireadiness/defaults.go:29-63`:
  **19** entries at weight 1.0 (`:31-49`) + **12** at 0.5 (`:51-62`) = **31**, Σ = 19 + 6 = **25.0**.
  `defaultReadinessSims` (`:75-79`) = **3**: `{StepSimulation,"tech"}`, `{StepSimulation,"business"}`,
  `{StepInterview,"both"}`.
- **`ai-readiness.md:321-322` — "CSV export is now 15 columns … dropped from the earlier 19".**
  `AIReadinessCSVColumns` (`csv.go:17-33`) has exactly **15** entries, and the source's own comment
  (`:13-16`) names the four dropped recommendation columns. (Note the *function* doc comment at `:35`
  still says "19-column" — that is the platform's stale comment, not the corpus's error.)
- **`ai-readiness.md:237, 249-255, 690-692` — the five usage-KPI ids.** `usageDimSpecs`
  (`how_we_measure.go:1160-1181`) = `avg_adoption`, `avg_transformation`, `avg_originality`,
  `avg_depth`, `avg_ownership` — **5**, in the order the corpus prints them. The seeder side matches:
  `stack-seeding/seeders/ai_readiness_interview_report.go:185-189` returns those same five, with the
  `:149` comment *"All five are always emitted here."* verbatim — **byte-identical in the pinned clone
  `09d06070` and the authoring copy `f2ea567b`.** (`usageDimensionsFromReports`'s own comment at
  `:1183` still says "four" — again the platform's stale text, not the corpus's.)
- **`README.md:46` — "`MESSENGER_RPC_ADDR` appears in no repo — every clone at its own named ref,
  nested repos included."** Enumerated the search set with `find stack-demo -name .git -maxdepth 4`
  = **15** git trees (including the two nested `studio` checkouts at `aeec036a`), grepped each at its
  **own** HEAD: 0/15. A broader `MESSENGER_RPC` returns 2 in `app`, both in
  `app/knowledge/plan/roadmap-support-in-app.md` and neither the `_ADDR` token. `git -C
  stack-demo/platform log -S 'MESSENGER_RPC' --oneline 0c91421d` = **0** commits; the doc's own
  positive control `-S 'SKILLER_RPC'` = **7**. Both reproduce exactly.
- **`README.md:46` — "21 live handlers".** Counted the `pubsub.EventHandler(...)` members of
  `AddSubscriber("backend", …)` at `messenger fa47850d:internal/flow/flow.go:72-104`, excluding the
  commented-out `OrgJobSimulationAssignmentPastDueHandler` at `:83`: **21**. `:105` and `:109` are the
  `jobsimulation` and `cms` subscribers, exactly as `dependency_map.md:60,61` cite them.
- **`sentinel.md:85` — "1 occurrence across `docker-compose.yml`, `common.yml` and `.env_example`".**
  `git grep -n AUTHORIZATION_ADDRESS 0c91421d -- <those three>` = **1** (`docker-compose.yml:48`);
  same at `0dab54df` (also `:48`, the ref `sentinel.md:5` pins). Positive controls: `PORT` = 6 lines in
  compose, `CLERK` = 10 lines in `.env_example`, so all three paths were actually searched.
- **`dependency_map.md:7` — "zero `*_RPC_ADDR` variables in any compose file at `0c91421`".**
  `git grep '_RPC_ADDR' 0c91421d -- docker-compose.yml common.yml` → exit 1; positive control `_ADDR`
  → 3 lines (`AUTHORIZATION_ADDRESS`, `REDIS_ADDR`, `DIRECTUS_PUBLIC_BASE_ADDR`).
- **`dependency_map.md:27` — "no `.tf` file in any clone names `http://backend.internal.anthropos:8081`".**
  Grepped all 15 trees at their own refs restricted to `-- '*.tf'`: **0 files**. Positive control: 44
  `.tf` files exist across 11 of the clones. The 14 tree-wide hits that do exist are all `.md`/`.py`/
  `.yaml` (app KB, rext guard + fixtures, graphql-wundergraph's `supergraph-config-prod.yaml`).
- **`dependency_map.md:42, 48-50` — the shared-library matrix.** Read `go.mod` at HEAD in each of the
  seven Go repos: colony **7/7**, proto **7/7**, taxonomy **6/7** (absent in `roadrunner`), ai **2/7**
  (`cms/go.mod:9` + `jobsimulation/go.mod:11`, both `v1.40.2`), standalone authn **0/7**. `app go.mod:20
  @ b948604f` = `taxonomy v1.2.0` (direct); `sentinel go.mod:21 @ 88bc5592` = `taxonomy v1.2.0 //
  indirect`; `messenger go.mod:9` direct / `storage go.mod:25` indirect — every cell as printed.
  `1e457fa70` is *"refactor(ai): fold the ai library into app as internal/ai"* (2026-08-04) and
  `internal/ai/module_import_guard_test.go` exists.
- **`dependency_map.md:49` — "app at 129 `.go` files @ `ad9f3c49`; the frozen cms/jobsimulation at 6
  and 8".** `git grep -l 'anthropos-work/colony/authn' HEAD -- '*.go' | wc -l` = **129 / 6 / 8**, exact.
- **`dependency_map.md:59` — the `skiller`-stream cell, the densest anchor block in my set.** All
  seven `b948604f` anchors resolve as claimed (`main.go:287` `NewPublisher(serviceName,…)`, `:637`
  `SKILLPATH_STREAM`, `:1039` `CMS_STREAM`, `:1276` `AddSubscriber(os.Getenv("SKILLER_STREAM"),…)`,
  `wiring.go:127` `AI_USAGE_STREAM`, `:180` `JOBSIMULATION_STREAM`, `roles.go:791` `SkillerSubscriber()`).
  The 6-drift/1-holds split reproduces exactly at **both** `2035f9a4` and `ad9f3c49`
  (`:287`→`logger.Info("subsystem switches",`, `:637`→`)`, `:1039`→`serverContext,`,
  `:1276`→`apiKeyManager,`, `wiring.go:127`→asynq client, `:180`→blank; `roles.go:791` byte-identical
  at all three). `git grep -n SKILLER_STREAM ad9f3c49 -- '*.go'` = **6 lines / 3 files**; off the
  pathspec = **6 files**. No `NewPublisher` names it at either newer ref; `main.go:1537` fills
  `Skiller:` in `streamNamesForSubscribers`. Compose still sets it (`docker-compose.yml:71`).
- **`dependency_map.md:58` — the messenger-in-app opt-in cell.** `streams.go:65`
  `StreamBackend = "backend"`; `main.go:1493` names the three literals; the whole block is behind
  `if messengerEnabled {` at `main.go:1445`, fed by `mustSubsystemSwitch(envMessengerEnabled)` at
  `:285`; `env_guards.go:61` is `envMessengerEnabled = "MESSENGER_ENABLED"`; `main.go:1437` reads
  *"ALL OF IT is behind MESSENGER_ENABLED."* verbatim; `docker-compose.yml:84-92` carries the
  "default to OFF on a developer machine" rationale. Every anchor exact @ `ad9f3c49`.
- **`README.md:31` / `dependency_map.md:31` — the topology counts.** At `0c91421d`
  `docker-compose.yml` declares **5** services (`sentinel:5`, `backend:28`, `studio-desk:112`,
  `next-web-app:143`, `gotenberg:170`) + `common.yml`'s `postgresql`/`redis` = **7 effective**;
  `repos.yml` = **4** entries. At `0dab54df`: **8** declared + 2 = **10**, `repos.yml` = **6**. Both
  pairs exact.
- **`README.md:28,44` — "`d11a403` removed roadrunner's local container **and** its `repos.yml`
  entry".** `repos.yml` at `d11a403^` contains `- name: roadrunner` (plus `cms`, `jobsimulation`);
  at `d11a403` all three are gone. Worth recording because **`d11a403`'s own commit message says the
  opposite** (*"its repos.yml clone entry was already gone"*) — the corpus is right and the commit
  message is testimony, not evidence (§5 rule 57).
- **`ai-readiness.md:203-219` — every ent table name.** All thirteen `Table = "ai_readiness_*"`
  constants match the doc's spellings, plurals included: `..._user_step_progresses`,
  `..._notification_logs`, `..._notification_optouts`, `..._live_snapshots`, `..._snapshots`,
  `..._diagnose_narratives`, `..._text_translations`, `..._recommendations`, `..._email_overrides`,
  `..._cycles`, `..._steps`, `..._skills`, `..._sims`; plus `interview_aggregated_reports`
  (`internal/data/ent/interviewaggregatedreport/…:34`). The partial unique "one active cycle per org"
  index is at `schema/ai_readiness_cycle.go:120-126`.
- **`ai-readiness.md:92-95, 97-108, 352-363, 370` — the two gates and the sole-consumer claims.**
  Repo-wide at `next-web-app 8297c684`, excluding `node_modules`: `flag_ai_readiness`/`AI_READINESS_FLAG`
  appear in exactly `aiReadiness.constants.ts:9,26` (declaration) + `useAiReadinessActive.ts:7,22`
  (the sole consumer) + one platform KB page. `AI_READINESS_URL` appears in exactly the two source
  files named (`urls.ts:51`, `useNavbarSections.tsx:4,403`) plus
  `knowledge/ai-readiness/frontend-architecture.md:15` — precisely as the corpus enumerates.
  `AIReadinessClient.tsx` contains **0** `posthog` and **0** `flag_ai_readiness`.
- **`ai-readiness.md:651` — "the platform's enum is exactly `{email, call}`".**
  `app/internal/data/ent/migrate/schema.go:2291` — `{Name: "action_type", Type: field.TypeEnum, Enums:
  []string{"email", "call"}}` on the interactions table; the `source_id <> target_id` CHECK is at
  `schema/interaction.go:50`.
- **`ai-readiness.md:656-658` — the "Handled for you" tile cells.**
  `HowWeMeasureTab.tsx` is **1989** lines; `:1879` opens the block, `:1903` the label, and exactly three
  cells follow — `skillsMapped :1915`, `handsOnMinutes :1921`, `interviewMinutes :1927`;
  `grep -c interviewQuestions` over the file = **0**. `apps/web/src/hooks/useAIReadiness.ts:326` is
  `interviewQuestions: number;` inside `export interface AIReadinessCycleTotals` (`:323-330`).
- **`ai-readiness.md:328-335, 340, 364-366` — the deleted legacy surface.** `dae0fb2f7` (2026-07-13,
  *"…drop orphaned container"*) deletes `AIReadinessContainer.tsx` (103) + `AIReadinessIntro.tsx` (220)
  + `AIReadinessView.tsx` (330) = **653** lines; no `enterprise/workforce/ai-readiness` path survives
  at `8297c684`; `WorkforceNewClient.tsx:125-151` lists exactly `growth`/`verification`/`talent`/
  `assignments`/`activity` (no readiness); `useWorkforceAIReadiness.ts:23-27` calls `'/ai-readiness'`
  with `searchParams: { tag }` and no cycle param; `e2e/specs/web.ai-readiness.spec.ts` exists.
- **`sentinel.md:26` — "6 request types, 6 policy types, 3 role groupings, 6 matchers".**
  `sentinel f2c46190:internal/authorization/casbin.go:13-45` — `r,r2..r6` (6), `p,p2..p6` (6),
  `g,g2,g3` (3), `m,m2..m6` (6). Exact.
- **`dependency_map.md:19` — "`storage.md` contains none of `depends_on`, `DB_CONNECTION`,
  `REDIS_ADDR`, `go.mod`, `redis` or `postgres` at any line".** `grep -cEi` over the 248-line file = **0**
  (grep exit 1); positive control `grep -ci storage` = 56. The withdrawal is correct.
- **`README.md:39` — the app-domain list.** `git ls-tree ad9f3c49 internal/` contains all seven of
  `skiller`, `skillpath`, `jobsimulation`, `cms`, `storage`, `messenger`, `customeriosync`; and
  `git ls-tree -r ad9f3c49 | grep -i roadrunner` = **0 paths in the whole tree**. Judge0 is wired at
  `internal/jobsimwiring/wiring.go:123` via `jsrunner "…/internal/jobsimulation/runner"` (import at
  `:34`).
- **`README.md:24,42` / `dependency_map.md:31` — jobsimulation's M810 teardown.** The claims pin
  `6092c6d2`, so they are graded there: that commit *"remove the jobsimulation ECS service and ECR
  repository (M810)"* deletes the `module "jobsimulation"` block, `service_desired_count` has **0**
  occurrences in `terraform/main.tf` (positive control: 1 at the parent `462343b0`), and `:15-40` is
  the comment block enumerating exactly the LiveKit/Chime buckets, the `/production/jobsimulation/*`
  SSM parameters and the atlas tracker. `6092c6d2` **is** on `origin/main` (`82cb66ec`) — the checkout
  `462343b0` is its parent, which is why a bare read of the checkout appears to contradict it and does
  not. `cms/terraform/main.tf:39` really is `service_desired_count = 0`;
  `roadrunner/terraform/main.tf:19` really is `= 1`, and `git log -L 19,19` gives `84a4b4f`
  (2025-12-15) as its last change — pre-fold, exactly as `README.md:21-23` says.
- **`README.md:54` — "ONE subgraph (`backend`) since `915da06`".** At `915da06` all three supergraph
  configs (`-compose`, `-dev`, `-prod`) list a single `- name: backend`. Also confirms the CLAUDE.md
  caution: that commit's own subject says "2→1" while its body records the 3→2 stage.
- **`chronos.md:122-127` — the historical-consumers retraction.**
  `app/internal/jobsimwiring/wiring.go:192` is verbatim *"…chronos/roadrunner/realtime clients are
  gone."*; `internal/jobsimulation/simulator/stream_handlers.go:30-31` is the quoted NOTE; both @
  `ad9f3c49`. `git grep -ci chronos 0c91421df` over the whole `platform` repo = **0** (positive
  control `sentinel` returns hits in 5 files). `045857c` is *"remove chronos service from
  orchestration…"* (2026-04-17). `worker.go:24-35` is the asynq import → `StartWorkers` → `NewServer`
  span it is offered for.
- **`sentinel.md:98-102` — the M1001 second Atlas pipeline.** `68272003` = *"feat(atlas): second Atlas
  pipeline for the sentinel schema (M1001)"* (2026-08-04); `app/atlas.hcl:50` `env "sentinel" {` …
  `:64` `revisions_schema = "sentinel"`; `app/Makefile:59-60` carries the quoted sentence verbatim;
  `sentinel @ f2c46190` has **no** `atlas.hcl` and **no** `terraform/migrations/`
  (`ls-tree | grep -iE 'atlas|migrations'` → exit 1), and `repos.yml` marks it `migrations: false`.
- **`clerkenstein.md:278-287` — the colony-pin drift.** `clerkenstein/go.mod:8` = `colony v0.34.3`
  (and `clerk-sdk-go/v2 v2.6.0`); `app/go.mod:15` = `colony v0.35.2` and `:29` =
  `clerk-sdk-go/v2 v2.7.0`; `sentinel/go.mod:8` = `colony v0.35.2` @ `f2c46190`, taken there by
  `88036d7` *"chore(deps): update dependencies to latest versions"* (2026-08-03); the frozen `storage`
  is still `v0.34.3`. The retraction ("false for `sentinel`") is itself correct.
- **`dependency_map.md:25` — the studio-room provider registry.** At `stack-demo/app/studio`
  **`aeec036a` (the nested repo's own ref, not the host's)**: `services/ai.py:705-708` is the entire
  `providers = {openai, azure, anthropic}` dict; `tools/pdf2md.py:24` is `from mistralai import
  Mistral`; `git grep -ci mistral aeec036a` = **22 hits across 3 files** (`requirements.txt` 1,
  `tools/pdf2md.py` 18, `tools/r3.py` 3) — the exact figure the cell prints.

**RE-DERIVED** (arithmetic the document displays; the weaker clearance):

- `ai-readiness.md:616,624` — `round(1.0/25.0*30)` = 1 and `round(0.5/25.0*30)` = 1 (the 1/30 floor);
  `round(9.0/25.0*30)` = **11**. `held/25×100 = held×4` is integral for half-step weights, as `:630-632`
  argues.
- `ai-readiness.md:404` — 156/199 = **78.39 %** ("78.4 %").
- `README.md`/`clerkenstein.md` merge arithmetic — 7 folded domains + roadrunner as the eighth;
  103+220+330 = 653.
- `ai-readiness.md:179-186` — the composite/axis formulas re-checked against
  `scoring.go:92-95` (`(step1+step2)/70×100`), `:100-105` (`step3×100/30`), `:119-120`
  (`t1+t2+t3`), and the archetype/bucket bands against `:26,30,46-57,63-78` (Champion ≥75/≥75;
  Standby ≤50/≤50; tie → Explorer; None 0-24 / Low 25-50 / Medium 51-74 / High 75-100).

---

## What I could not settle

- **Anything requiring the `infrastructure` repo.** `README.md:54` and `dependency_map.md:23,31` cite
  `infrastructure/terraform/production/services.tf:509-517` @ `13c248e6` for the router's production
  destruction, and `dependency_map.md:31` correctly labels the cms half UNMEASURABLE for the same
  reason. `find stack-demo -name .git -maxdepth 4` returns 15 trees and none is `infrastructure`, so I
  can neither confirm nor refute the line anchor. Settled by a read-only clone of `infrastructure` at
  `13c248e6`.
- **Anything requiring the `db-backup` repo.** `README.md:48` ("43-line Bash", two targets, schedule
  commented out since 2025-05-29) and `dependency_map.md:38` ("0 Azure hits across all 157 objects in
  the repo's history", `7dd1b80`). No `db-backup` clone exists.
- **The whole historical body of `chronos.md` (`:22-64`, `:74-166`, `:200-237`).** Go 1.25, sqlc,
  ports 8080/8081, the `timers` DDL + four indexes, the triggerer's `LIMIT 10` / `FOR UPDATE SKIP
  LOCKED` / 3 s timeout / 1-minute Sentry heartbeat, the RPC signatures, the CLI flags — none of it is
  checkable: the `chronos` repo is in no clone set and, per its own banner, no longer cloned by
  `make init`. Settled by cloning `anthropos-work/chronos`. The *decommission* half of the page **is**
  verified (see cleared list).
- **Repository-hosting state.** `README.md:81` ("the GitHub repo is NOT archived — last push
  2026-04-23") and `README.md:54` / `dependency_map.md:23` ("repo ARCHIVED 2026-07-30", "ECR
  hand-deleted 2026-08-05") are claims about GitHub/AWS, not about any tree. Settled by
  `gh repo view --json isArchived,pushedAt`.
- **Every runtime/DB measurement.** `ai-readiness.md`'s 2.09 s live recompute, 24 ms / 19 ms / 180 s
  frozen reads, 304 KB payload, 199 frozen snapshots, 165 `interview_extraction_results` rows, the
  "5 → 345 / +897 / 787" evidence fan-out, and `clerkenstein.md`'s 0.17–0.19 s / ~127 s / ~4.05 s
  figures. Settled only by a live stack.
- **`dependency_map.md:13` — "the 23 jobsim run-state tables".** `migrate/schema.go` declares 135
  tables in total at `ad9f3c49`; isolating the jobsim-origin subset needs a migration-provenance
  derivation I could not make non-arbitrary inside this pass. Settled by diffing the legacy
  `jobsimulation` schema's table set against `public`.
- **`sentinel.md:5` — "Ten other corpus sites already recorded the deletion, two of them in this same
  file".** A corpus-wide count I did not re-derive; it is an arithmetic claim about prose, not about
  the platform, and re-deriving it needs a stated predicate the sentence does not give.
- **Out-of-scope observation, recorded because it is a live cross-file rot and I stumbled on it while
  grading `ai-readiness.md:507`:** `corpus/ops/demo/stories-spec.md:605` says the 2.09 s retraction is
  *"recorded as retracted at … [`services/ai-readiness.md:390,458`]"*. At the current corpus HEAD
  `ai-readiness.md:390` is a sentence about the narrative sha256 cache and `:458` is a bullet about
  `trajectory` — neither is the retraction, which lives at `:433` and `:505`. `stories-spec.md` is not
  in my file set, so I have not booked it; flagging it so it is not lost.
