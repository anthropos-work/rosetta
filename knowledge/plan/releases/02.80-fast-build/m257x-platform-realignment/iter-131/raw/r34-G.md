# Seat G — reading #33/#34 (M257x iter-131)

**Refs I graded against** (re-verified at this reading's open, per the ADDENDUM):
`app` `ad9f3c49` · `app/studio` `aeec036a` · `platform` `0c91421d` · `next-web-app` `8297c684`
(origin/main `f97ba659`) · `sentinel` `f2c46190` · `studio-desk` `41ee3575` · `ant-academy` `22df69dd` ·
`storage` `4ce8ece5` · `messenger` `fa47850d` · `cms` `ca50c817` · `graphql-wundergraph` `60c229f3` ·
`roadrunner` `87d8d443` · `jobsimulation` `462343b0` · rext **pinned per-stack** `stack-demo/rosetta-extensions`
**`09d06070`** · rext **authoring** `.agentspace/rosetta-extensions` **`f2ea567b`**. All thirteen re-read with
`git -C … rev-parse` and matched the ADDENDUM exactly.

Where a passage names its own ref I graded at that ref (`b948604`, `2035f9a`, `63ce41a`, `0dab54d`,
`f97ba6599`); where it names none I graded at the checkout. Every rext claim below states which tree.

## Positive controls

| file | `wc -l` | read in full? |
|---|---|---|
| `corpus/services/hiring.md` | 488 | yes |
| `corpus/architecture/org-repos.md` | 466 | yes |
| `corpus/architecture/ai_architecture.md` | 354 | yes |
| `corpus/services/next-web-app.md` | 188 | yes |
| `corpus/services/coursebuilder.md` | 161 | yes |
| `corpus/architecture/README.md` | 39 | yes |

Search-pipeline positive controls run in the same pass (§5 rules 1/2): `git grep 'SET '` over the app
migration set → non-empty (so the `SET "score"` → 0 is a real zero); `git grep 'ADD COLUMN'` → non-empty
(so the "no later ALTER on `job_simulation_sessions`" is real); `git grep -il directus -- corpus/ CLAUDE.md`
→ 55 files (so the `grafana` result below is not a broken pipeline). Nested-repo trap handled: `app/studio`
and `cms/studio` were grepped at their **own** ref `aeec036a`, never at the host's. One command in this pass
was silently mangled by a zsh `${R:a}` modifier and printed `fatal:` to stderr — caught because stderr was
read, then re-run quoted; noted because it is exactly §5 rule 1.

---

## Blockers

### B1 — `ai.AI` is claimed to span **Mistral**; the fold explicitly removed Mistral from that interface

- **Anchor:** `corpus/architecture/ai_architecture.md:110-111` — *"**What the interface provides is
  unchanged:** — A single `ai.AI` interface across providers (OpenAI, Azure, Anthropic, Bedrock, Mistral)"*.
  The subject is unambiguous: the two sentences immediately above say *"`app` **folded the library into its
  own tree** at `1e457fa70` … the library lives at `app/internal/ai/`."*
- **Why it is false:**
  - `app/internal/ai/` @ `ad9f3c49` contains **exactly two** provider sub-packages — `anthropic/` and
    `openai/` (`git ls-tree -r ad9f3c49 internal/ai/`: `internal/ai/anthropic/*`, `internal/ai/openai/*`,
    and package-level files). There is **no** `internal/ai/mistral`.
  - The three constructors returning `ai.AI` are `internal/ai/openai/completion.go:24` `NewOpenAI`,
    `:53` `NewAzure`, and `internal/ai/anthropic/completion.go:48` `NewAnthropic`. No Mistral constructor
    exists anywhere in the tree.
  - The platform states the removal in its own words. `internal/cms/studio/mistralocr/mistralocr.go:1-11`
    (package doc): *"It used to be internal/ai/mistral, where it satisfied the nine-method ai.AI interface
    in order to expose exactly one working method… So this is what it always was: upload a document, get a
    signed URL for it, run OCR on that URL… **No interface**, no LLM methods, no tokenizer, and no panics."*
    So the fold **changed** what the interface provides — the claim *"unchanged"* is false on its own terms.
  - It also contradicts this same file twice: `:33-42` (*"Mistral is nowhere in this path… **OCR only**"*)
    and the *Mistral (EU)* row at `:93` (*"the cms domain's Go client"*). A Go client that is not on the
    `ai.AI` interface cannot be a provider "across" that interface.
- **Cross-file drift (not my partition, recorded for the repairer):** the identical false clause stands at
  `corpus/architecture/external_services.md:554` — *"Go services access AI behind one `ai.AI` interface
  (OpenAI, Azure, Anthropic, Bedrock, Mistral)"* — inside a paragraph that is otherwise a **corrected**
  statement of the very same fold. `CLAUDE.md`'s shared-libraries bullet carries it too. This is rule 19's
  half-repair shape: the fold was repaired everywhere **except** the parenthesis listing the providers.
- **Confidence:** high

### B2 — *"the only frontend in platform compose"* — the same file retracts that exact phrase 105 lines later, and it is false

- **Anchor:** `corpus/services/next-web-app.md:17` — *"`apps/web` is the only app with an in-repo Dockerfile
  and **the only frontend in platform compose**"*.
- **Why it is false:**
  - `platform` `0c91421d` `docker-compose.yml` declares `studio-desk` at `:112`, a browser UI on
    `"9100:9100"` (`:124`) wired to the GraphQL endpoint at `:135`
    (`VITE_GRAPHQL_ENDPOINT=http://localhost:8082/graphql/query`). It is a second frontend in platform
    compose.
  - **The same document already says so, in a correction that names the retracted phrase verbatim:**
    `:121-124` — *"`apps/web` is the only **`next-web-app`** app in platform compose — ⚠️ **not "the only
    frontend"**: `studio-desk` is a second compose frontend with its own browser UI (`platform`
    `0c91421df`, `docker-compose.yml:112`, `:135` …)"*.
  - So one passage asserts precisely the proposition the other quotes-and-refutes. A reader who stops at
    the *Key Functions* bullet — the page's summary — gets the refuted answer.
- **Why I did not book it as a MINOR:** it is not a phrasing nit; the corrected sentence exists in-file and
  was deliberately written to kill this wording, so this is a repair that reached one site (rule 40/54),
  and the summary bullet is the higher-propagation of the two.
- **Confidence:** high

### B3 — the Cosmo router is called *"prod-only"* eight lines after this corpus recorded that production destroyed it

- **Anchor:** `corpus/services/next-web-app.md:186` — *"[GraphQL Gateway](./graphql-wundergraph.md) — the
  federated gateway (**prod-only** since `2adcf71`)"*.
- **Why it is false / self-contradictory:**
  - `corpus/services/next-web-app.md:80`, in the same file: *"**in prod the router is destroyed — iter-124
    — and where the deployed frontends now point is Vercel runtime config this corpus cannot read**"*.
    *Destroyed in prod* and *prod-only* are incompatible readings of the same subject.
  - `corpus/architecture/org-repos.md:144` (my partition, and the adjudicated form) states it flatly:
    *"`module.wundergraph_euwest1` **is deleted**; `services.tf:509-517` says the apply destroyed 'its ECS
    service, task definition, target group, ALB rule (priority 810), Cloud Map entry, log group, ACM cert
    and the `wundergraph.anthropos.work` alias' … **so production-wundergraph is gone and this block is now
    inert**"*, and `:391-397` records the resolution — *"RESOLVED AT M257x iter-124 … AKB was right and this
    corpus was wrong."*
  - `corpus/ops/platform-alignment.md` §5 rule 54 names this exact residue: *"**24 further sites across 13
    files** kept asserting that the router 'survives in production only'."* This is one of them, surviving
    in a **Related Documentation** line — the lowest-attention part of the page and therefore the one a
    re-read of the repaired paragraph cannot find.
- **Confidence:** high

### B4 — *"the corpus documents no observability tier at all (`grep grafana` → 0 files)"* is refuted by the doc it links in the same sentence

- **Anchor:** `corpus/architecture/org-repos.md:43-45` — *"What the census got **right and understated**:
  `ant-observability` — the corpus documents **no observability tier at all** (`git grep -i grafana --
  corpus/ CLAUDE.md` → **0 files**), and that repo holds both the platform's live outside-in monitoring and
  a **production read path** no safety doc enumerates. See [`observability.md`](../ops/observability.md)."*
- **Why it is false:** re-run at rosetta HEAD, `git grep -il grafana -- corpus/ CLAUDE.md` returns **3
  files** — `corpus/architecture/org-repos.md`, `corpus/ops/README.md`, `corpus/ops/observability.md`.
  `corpus/ops/observability.md` exists (9,034 bytes) and **is the observability tier's documentation**;
  the sentence links to it while asserting it does not exist. Pipeline control in the same pass:
  `git grep -il directus -- corpus/ CLAUDE.md` → 55 files, so the 3 is a real 3.
  This is a **universal quantifier falsified by its own sentence** — the identical defect this document
  books against the corpus two rows above, at `:37` (*"**self-refuting**, since they say it while naming
  them"*). The paragraph is also framed as what the census got *right*, so the reader is told to trust it.
- **Why I hesitated:** one could read *"the corpus documented…"* as historical (pre-census). It is written
  in the present tense, the derivation command is printed unpinned in a document whose own §"Provenance"
  header is *"state the invocation with every count"*, and the closing *"See observability.md"* makes the
  historical reading unavailable to a reader.
- **Confidence:** high

### B5 — `secrets-spec.md:309` cited for the hyper-studio `.env.example`; that line is a different secret family entirely

- **Anchor:** `corpus/architecture/org-repos.md:370-371` — *"[`secrets-spec.md:309`](../ops/secrets-spec.md)
  uses `../hyper-studio/.env.example` as the template for `app`'s five AWS Bedrock secret genes. **The corpus
  borrows a file from a repo it never mentions.**"*
- **Why it is false:** `corpus/ops/secrets-spec.md:309` is a table row reading
  *"| `platform/ELEVENLABS_API_KEY` · `platform/MISTRAL_API_KEY` · `app/ELEVENLABS_API_KEY` ·
  `platform/SKILLER_OPENAI_KEY` | optional | optional | `key-present` |"* — no hyper-studio, no AWS, no
  Bedrock. `grep -n hyper-studio corpus/ops/secrets-spec.md` returns exactly **two** lines, `:344` and
  `:351`; the claim's real site is **`:344`** — *"**The 5 genes** (all on the `app` repo, target `app/.env`;
  the `../hyper-studio/.env.example` template):"*. The pin is 35 lines off and lands on a construct that
  says the opposite kind of thing (optional non-AWS genes).
- **Blast radius:** the same wrong pin has already propagated —
  `corpus/architecture/platform-migration-status.md:160` also says *"`secrets-spec.md:309` already borrows
  its `.env.example`"*. Repair both.
- **Confidence:** high

### B6 — `app/terraform/main.tf:638-639` cited for `JUDGE0_BASE_URL`; those lines are the ElevenLabs webhook secret

- **Anchor:** `corpus/architecture/org-repos.md:226-228` — *"It reaches the platform as an opaque tfvar:
  `infrastructure/terraform/production/variables.tf:523` `judge0_base_url` → `services.tf:385` →
  **`app/terraform/main.tf:638-639` `JUDGE0_BASE_URL`**, consumed in-process at
  `app/internal/jobsimwiring/wiring.go:123`."* The clause names no ref, so it grades at the checkout.
- **Why it is false:** at `app` `ad9f3c49`, `git grep -n JUDGE0 ad9f3c49 -- terraform/` returns exactly two
  hits — `terraform/main.tf:531` `"name": "JUDGE0_BASE_URL",` (`:532` `"value": "${var.judge0_base_url}"`)
  and `terraform/main.tf:693` `"name": "JUDGE0_API_KEY",`. Lines `:637-639` are
  `"name": "ELEVENLABS_WEBHOOK_SECRET"` / `"valueFrom": "${aws_ssm_parameter.elevenlabs_webhook_secret.arn}"`
  — a different variable, in the **secrets** block rather than the environment block. I checked the other
  three plausible refs so this cannot be stale-pin drift: `b948604` → `:329`, `2035f9a` → `:531`,
  `9d00a313` → `:374`. **`:638-639` names `JUDGE0_BASE_URL` at no ref in the repo's reachable history.**
- **Why it matters:** the sentence is the one-hop chain a reader follows to find where the Judge0 URL enters
  the task definition, and it lands them on an unrelated secret injection.
- **The other end of the same chain verifies:** `internal/jobsimwiring/wiring.go:123` is
  `runnerManager := jsrunner.NewRunnerManager(getenv("JUDGE0_API_KEY"), getenv("JUDGE0_BASE_URL"))` — exact.
- **Confidence:** high

### B7 — `pdf2md.py:24` is cited for the literal `mistral-ocr-latest`; that literal is at `:127`

- **Anchor:** `corpus/architecture/ai_architecture.md:39-41` — *"and Python-side in the in-image studio tree
  at `studio/tools/pdf2md.py:24` (`mistral-ocr-latest`) — a standalone CLI on neither the AI manager's path
  nor the generation pipeline's"*.
- **Why it is false:** at `app/studio` `aeec036a` (the nested repo's own ref — the host `app` ref returns 0
  for every predicate here, §5 rule 44), `tools/pdf2md.py:24` is `from mistralai import Mistral` — an import
  statement. `git grep -n mistral-ocr-latest aeec036a` returns exactly one hit: **`tools/pdf2md.py:127`**
  (`model="mistral-ocr-latest",`). The parenthesis names a string literal 103 lines from the cited line.
- **Why I hesitated (and booked it anyway):** the cited line *is* Mistral-related, so a reader is not sent
  to an unrelated subject the way B5/B6 send them. But the citation's whole job is to prove **which Mistral
  surface** this is — OCR rather than generation — and the OCR model id is the evidence for that; pointing
  at the import proves only that the file imports the SDK. The neighbouring anchors in the identical
  sentence (`markdownManager.go:30`, `studioManager.go:583`) are byte-exact, which is what makes this one
  read as a slip rather than a convention.
- **Confidence:** medium

### B8 — voice-engine selection described at the wrong granularity, nine lines above the file's own bolded correction

- **Anchor:** `corpus/architecture/ai_architecture.md:224` — *"**Configuration**: Voice engine is selectable
  **per simulation** in CMS (`livekitgptrealtime`)"*.
- **Why it is false:** the field is on the **sequence**, not the simulation.
  `app/internal/cms/directus/collections/jobsimulation.go:911` declares
  `VoiceEngine *SimulationVoiceEngine \`json:"voice_engine,omitempty"\`` on the sequence DTO — the same
  struct that carries `AIVendor` (`:905`) and `AIModel` (`:906`) — and the only read is `:1350`
  `VoiceEngine: voiceEngineFromDirectus(seq.VoiceEngine)`, i.e. **per `seq`**, inside the per-sequence
  loop. `git grep -n VoiceEngine` over that file returns no simulation-level field.
- **Self-contradiction:** `:233` of the same file says, in bold, *"**Engine choice is per SEQUENCE, from the
  CMS `voice_engine` field**"* and then pins it in full to `:1594-1597` — a sentence written to establish
  precisely the granularity `:224` gets wrong. Two adjacent statements, one bolded as the corrected form,
  the other left standing.
- **Why I hesitated:** *"selectable per simulation in CMS"* can be read loosely as *"configurable, in the
  CMS, for a simulation"*. I booked it because the file itself treats the sequence-vs-simulation distinction
  as load-bearing enough to bold and pin, and because a content author or seeder acting on `:224` looks for
  a field that does not exist at that level.
- **Confidence:** medium

---

## Minors

- `corpus/services/hiring.md:245` — cites `rosetta-extensions/stack-seeding/seeders/persona_write.go:69-71`
  for *"three tables … the `PersonaSeeder` also writes"*. At the named ref `63ce41a` (authoring copy; the
  pinned per-stack clone `09d06070` does not contain that commit, but the identical lines stand there too)
  `:69-71` is the comment *"The old `jobsimulation.sessions` step is REMOVED rather than re-pointed…"*. The
  three-table mapping comment is `:66-68` (off by 3) and the actual writes are `:92-94`. The **claim** is
  true — `:92-94` write `validation_attempt_results`, `validation_attempt_skill_results`,
  `validation_criterion_results` — only the pin is loose. Second range `:143-167` covers `sessionCols()` +
  `attemptResultCols()`, i.e. 2 of the 4 column builders.
- `corpus/architecture/ai_architecture.md:277` — *"the enum is …`jobsimulation.go:983-990`"*. At the named
  ref `2035f9a` the `AIModel` const block is `:979-991` (11 members, `:980-990`); `:983-990` is a strict
  sub-range starting mid-block. All three examples the sentence gives (`gpt-5` `:987`, `gpt-4.1` `:985`,
  `anthropic-45-sonnet-aws` `:983`) do fall inside it, so nothing misleads — the range just is not "the
  enum".
- `corpus/architecture/ai_architecture.md:325` — *"`calculateSkillScore` (`v3/validator/skills.go:53-64`)"*.
  The function is `:53-62`; `:64` is the declaration of the neighbouring `run`. `:75` (the
  `passed/total*100`) is exact.
- `corpus/services/coursebuilder.md:129-130` — *"~55 test files"* / *"~25 boundary test files"*. Measured at
  `ad9f3c49`: `internal/coursebuilder/` holds **61** `_test.go` files and
  `internal/web/backend/coursebuilder/` holds **33**. Both hedged with `~`, both under-counts; the
  neighbouring *"~100 Go files"* is exact (102).
- `corpus/architecture/org-repos.md:381` — *"≈1,773 lines against this corpus's **34,417 across 90 files**"*.
  Measured now: **91** tracked `corpus/**/*.md`, **36,102** lines. A self-measurement of a tree the same
  iteration was growing; the *"27 service docs"* in the next clause is exact (29 files in
  `corpus/services/` minus `README.md` + `TEMPLATE.md`).

---

## Positively cleared (audited zeros)

**ENUMERATED** — predicate re-derived from source independently, cardinalities compared:

- **`coursebuilder.md:77-94`, the 16 SSE event names.** I read `renderEvent`
  (`internal/web/backend/coursebuilder/handler.go:2604-2772` @ `ad9f3c49`) and enumerated every returned
  name rather than checking the doc's list: `text`(:2607) `score`(:2609) `patch_applied`(:2614)
  `patch_skipped`(:2620) `stage`(:2637/:2639) `outline`(:2654) `progress`(:2661) `preview_ready`(:2671)
  `draft_kept`(:2678) `error`(:2704) `translation_ready`(:2724) `rebuild_required`(:2732)
  `steering_received`(:2744) `steering_applied`(:2754) `done`(:2768) = **15**, plus `session` written
  directly at `:1458` right after `beginSSE` (`:1454`) = **16**. `case cb.EventCost:` at `:2709` returns an
  **empty** name at `:2717`, and the stream loop skips on empty at `:1478-1480`. The `default` arm at
  `:2770` re-emits `text`, not a 17th. **Every one of the twenty line anchors is exact.**
- **`hiring.md:266-268`, the four required-and-undefaulted columns.** I enumerated the DDL rather than
  trusting the four names: `terraform/migrations/20260722104506.sql:2-27` declares **23** columns; the
  `NOT NULL`-without-`DEFAULT` set is exactly `owner_id`(:6) `sim_id`(:7) `sim_type`(:10) `token`(:13) —
  every other `NOT NULL` carries a `DEFAULT`. `token` is `UNIQUE` at `:29`. `git grep 'ADD COLUMN'` over the
  migration set (positive control non-empty) shows no later `ALTER … job_simulation_sessions`, so 23 holds.
- **`hiring.md:261-263`, `completion_status` is a closed 5-value enum with no `SIMULATION…` member.** Go
  enum `internal/data/ent/enum/jobsimulation.go:29-35` (5 consts) + `Values()` `:37-43`; **both** SDL enums
  cited (`jobsimulations.graphqls:14` and `:128`) declare exactly the five lowercase members. The
  "nothing catches it" chain verifies end to end: `ent/jobsimulationsession.go:181-186` casts
  unconditionally, `intelligence.go:1844` re-casts, `graph.go:129546-129554` (and the proto twin
  `:129392-129400`) is a bare `graphql.MarshalString(string(v))`.
- **`hiring.md`'s whole read-path table (rows 1-7) and the cohort predicates.** `intelligence.go:1700`
  (`m.ent.JobSimulationSession.Query()`), `:1702` `SimID`, `:1709` `OrganizationID`, `:1733` call site,
  `:1739-1744` `RowNumber == 1`, `:1820` score read, `:1846` assignment, `:2124` declaration,
  `:2158-2160` `ROW_NUMBER() OVER (PARTITION BY sim_id, owner_id ORDER BY score DESC …)`,
  `:885-886` `insightsSortColumn` mapping `Completition→FieldCompletionStatus`,
  `ent/schema/job_simulation_session.go:33`/`:39`/`:45`, `anticheat_result.go:24`,
  `20260722104506.sql:53`/`:79`, `20260729133514.sql:58-62` (*"5. Drop the mirrors"* + the `DROP TABLE`),
  `askengine/registry.go:192`, `20260722081626_jobsim_data_model.sql:336/355/376`. **All exact.**
  `SET "score"` returns **0** across the migration set (positive control: `SET ` non-empty), and the only
  `CREATE SCHEMA` anywhere is `auth` at `20230817154747_supabase_baseline.sql:2`.
- **`hiring.md`'s `is_hiring` dual-write and blast radius.** `resolver_cms_queries.go:95,210,258,295`
  (`GetOrganizationIsHiring`) with `:99-103` selecting `hiringLibraryTypes`/`workforceLibraryTypes`, inside
  `PrivateJobSimulations` (declared `:89`); `organization/manager.go:450`/`:453`/`:537`;
  `siminvitationlink.go:62`/`:63`; `resolver_queries.go:1034`/`:1035`/`:1053`/`:1080` and the neighbouring
  `:1085`/`:1089`. On the frontend at `8297c684`: `useGetClerkOrganization.tsx:16-18`/`:20-21`;
  `UserStatusContext.tsx:144-148`/`:168-172` (web) and `:125`/`:144-145` (hiring);
  `useNavbarSections.tsx:161`, `:249-256`, `:329-331`, `:340-343`, `:391-398`, `:475-482`/`:476`, `:568`;
  `orgGroups.ts:48-65` with `intelligence: isAdmin` at `:61`. The M257x-iter-102 **retraction** verifies as
  an enumeration: `showWorkforce={false}` appears in exactly **two** places tree-wide, `apps/hiring/…/
  template.tsx:167` and `:248`, both in `apps/hiring` — so *"a recruiter loses Workforce Intelligence by
  being handed off to `apps/hiring`, not by the `is_hiring` flag inside `apps/web`"* is measured, not
  argued. `template.tsx:90` and `FreeTrialContainer.tsx:29` confirm the two `isEnterprise` definitions
  diverge as described.
- **`hiring.md:215` "byte-identical, same line numbers".** Both
  `apps/{web,hiring}/src/components/molecules/Table/Definitions/InsightsByJobSimulationByMembershipsRow/
  simulationScoreColumn.tsx` hash to `8b346c31d692f5e6b9ebbdff91bba939c8a4202a`. `:54` and `:95-97` are the
  same in both.
- **`hiring.md:466` "zero `(.)`/`(..)`-prefixed dirs anywhere in `apps/`".** `git ls-tree -r 8297c684 --
  apps/ | grep -cE '/\(\.\.?\)'` → **0**.
- **`ai_architecture.md`'s Model Families table.** Enumerated the const blocks, not the doc's lists:
  `internal/ai/openai/config.go:8-26` holds exactly **11** consts, matching the 11 models in the OpenAI row
  one-for-one; `internal/ai/anthropic/completion.go:20-30` holds **6** consts over **5** families with
  `Anthropic46SonnetAWS20251126 = "eu.anthropic.claude-sonnet-4-6"` at `:29` — and the three "current
  production pin" sites all verify (`askengine/bedrock.go:25`, `jobsimulation/agent/report_agent.go:31`,
  `coursebuilder/bedrock.go:29`), with Opus 4.8 at `coursebuilder/bedrock.go:23`;
  `internal/ai/speech.go:9-12` is the entire `SpeechModel` block (`GPT4oMiniTTSS` + `DefaultModel`).
- **`ai_architecture.md`'s routing section.** `jobsimulation/ai/ai.go:30-33` = exactly four vendor consts;
  `getClient` really is `:259-289`, a plain `switch` with the `flag_use_azure_us` lookup keeping the EU
  client on error; `simulator/ai/ai.go` has four cases (`:58`, `:69`, `:86`, `:102`) against a **five**-member
  Directus `AIVendor` enum (`jobsimulation.go:969-975`, `azureglobal` at `:971`), `default:` at `:114-115`
  setting `internalAi.Openai`; the three defaults table (`:1297` `Gpt5Point1`, `:1302` `Openai`;
  `:65-66`/`:82-83`/`:126-127` → `GPT4Dot1`; `GetValidationAIVendorAndModel` `:20-26` Azure/4.1/4.1-mini)
  and both Anthropic fallback arms (`:98-99`, `:110-111`) are exact. `markdownManager.go:29`/`:30` and the
  `:19` doc-comment retraction are exact, as is `studioManager.go:583`. `app/studio` @ `aeec036a`
  `grep -i mistral` → **22 lines / 3 files**, exactly as published.
- **`ai_architecture.md`'s studio slot table + the `config_template.ini` warning.**
  `studio/configs/production_config.ini:26-36` is precisely the ten `*_MODEL` lines, stable ≡ experimental,
  and the five slot values match cell-for-cell; `configs/config_template.ini:39`/`:40` are the two `gpt-4o`
  stable slots.
- **`ai_architecture.md`'s evaluation section.** `v3/validator/validator.go:43`, `:60-61`, `:595`;
  `basevalidator/criterion.go:127` (`case check.EngineLlm:`), `:168` (`validateCodeDiff` inside
  `checkResPool.Go`), `:428` (`NewLLMBulkChecker`), `:450-475` (pure string comparison);
  `check.EngineTextDiff` really exists (`criterion.go:142`); `v3/validator/skills.go:40-51` is
  `calculateCompetencyLevelScore` verbatim including the `// TODO fix this formula`, `:75` the
  `passed/total*100`.
- **`ai_architecture.md`'s LiveKit block + `org-repos.md` § 8's mechanism.**
  `calls/livekit.go:101-104` (`euAgentEndpoints`), `:110`/`:111` (defaults — so the azure-* READMEs really
  are contradicted), `:122-123` (random EU pick), `:126-127` (US sets **both** signals), `:131-135` (flag
  read), `:140-144` with `:142` `agentName = "anthropos-agent"` **and** `:143`
  `agentEndpoint = "openai-hosted"`, `:148` (`metadata["endpoint"]`); `jobsimulation.go:1085`
  (`SimulationVoiceEngineLivekitchain = "livekitchain"`).
- **`coursebuilder.md`'s backend-selection claim and its own retraction.** `bedrock.go:98-103`
  `ModelBackendName`, `:105-114` `newUnderlyingClient`; `terraform/variables.tf:759-763` (sensitive, no
  default), `ssm.tf:328-333` (SecureString), `main.tf:757-758` (SSM ARN injection) — and the retraction is
  correct in both directions: at `ad9f3c49` `main.tf:555` really is `"name": "DIRECTUS_BASE_ADDR"` and
  `variables.tf:631-645` really is the cms-in-app secrets comment block. At the pinned `b948604`:
  `main.go:766-769` is the comment, `:770` the backend log line, `:774`/`:778` the two
  `logger.Warn(… routes disabled)` arms, `:824-826` the `OPENAI_KEY` cover generator. `git describe --tags
  ad9f3c49` → `v1.369.0-7-gad9f3c498`, exactly as quoted. `COURSEBUILDER_OPENAI_IMAGE_KEY` survives only in
  two in-repo markdown files, zero in code. The `/coursebuilder` group is mounted at
  `internal/web/backend/backend.go:229`; `Register` is `handler.go:3143`; migration `20260717151144.sql`
  creates `course_builder_sessions`(:2) + `credit_transactions`(:40) + `organization_credits`(:56).
- **`coursebuilder.md:116-118`'s numbers, against the repo's own stale docs.** `budget.go:27`
  `DefaultMaxMonthlyCOGSUSD = 500.0` and `:41` `DefaultMaxDailyCOGSUSD = 0.0` — the corpus's *"default 0 =
  off"* is right and the in-repo `SPEC.md:1750` (*"default `50`"*) is the stale one. Credits: the
  authoritative map `internal/credits/cost.go:86-90` is `build 5 / refine 1 / translate 1`, matching the
  corpus; the file's own header comment at `:29` says *"5 credits per refine turn"* and is contradicted by
  its own const doc at `:59-67` and by the map. Corpus wins both.
- **`next-web-app.md`'s version block.** All four apps declare `"next": "~16.2.12"` (tilde, verified
  per-file), `apps/mobile` declares no `next`; `pnpm-lock.yaml` resolves `next@16.2.12`; root
  `package.json` gives `node >=24.0.0`, `pnpm@10.30.3`, `turbo ^2.9.6`. `proxy.ts` exists in
  web/hiring/integration and **`middleware.ts` exists nowhere**; the repo's own `CLAUDE.md:15` and `:55`
  read exactly as quoted. `configs/i18n/messages/` holds exactly **8** locales (de en es fr it ja nl pt).
  No `storybook` script and no `.storybook/`; the only trace is `configs/tailwind/storybooks.css`.
- **`next-web-app.md`'s compose + workflow anchors.** `docker-compose.yml:151` (build arg), `:160`
  (runtime env), `:165-167` (`depends_on: backend`), `:110` (`profiles: [core, backend, all]`), and the
  service set is exactly `sentinel`(:5) `backend`(:28) `studio-desk`(:112) `next-web-app`(:143)
  `gotenberg`(:170) — so the `:143` pin and the `exits 1` reasoning both hold. The historical
  *"it was `:236` at `0dab54d`"* verifies at that ref. `Makefile:119-120` is `up-frontend`. At
  `f97ba6599`: `GRAPHQL_SCHEMA_FOR_GEN` → **4** hits, all `.env.example`, and `codegen.ts:9` hardcodes the
  endpoint; `production.yaml`'s matrix is exactly web/hiring/integration. `apps/mobile/package.json:71-72`
  are the two `eas build` scripts (port 3031 at `:70`).
- **`next-web-app.md`'s two sign-in-token sites.** `apps/web/src/app/api/dev/login-as/route.ts:79`
  (`createSignInToken`), `apps/web/src/lib/devLogin.ts:28` (`DEV_LOGIN_ENABLED`), `apps/web/src/proxy.ts:56`
  (the same boolean adding both routes to the public list), `e2e/auth.setup.ts:72` and the `:57-62` quote —
  all exact, including *"skips both factors"*.
- **`org-repos.md`'s clone-set-reachable facts.** `repos.yml` @ `0c91421d` lists exactly **four** (`app`,
  `sentinel`, `next-web-app`, `studio-desk`) with `app` at `:14-17` the only `migrations: true` — so
  `hiring.md`'s "6 @ `0dab54d`, 4 since `838d907`" is exact (I read both refs). `app/go.mod:12` is
  **upstream** `github.com/ThreeDotsLabs/watermill-redisstream v1.4.5` with `watermill v1.5.2` at `:11` and
  **no `replace`**; `:14` is `analytics-go v0.3.1`, and `:14-18` is exactly
  `analytics-go, colony, proto, storage, taxonomy` — so `external_services.md:554`'s enumeration, which
  `org-repos.md:40` cites, is right. `payments/handler.go:302-316` is the Stripe subscription-lifecycle
  `analyticsManager.Track`. `jobsimulations.graphqls:750` is `isTest: Boolean` and
  `simulator/manager/manager.go:445` is `IsTest: isTest` — sim-qa's `is_test` claim holds. All seven sim-qa
  mutations resolve at the exact lines given (`:744 :753 :761 :762 :807 :821 :871`).
- **`org-repos.md` § 4's "no stack installs them", settled in the PINNED per-stack clone
  `stack-demo/rosetta-extensions` `09d06070`** (rule 45 — this is a claim about what the tooling does *on a
  stack*): `stack-injection/gen_injected_override.py:321-364` emits the directus block with
  `image: directus/directus:11.6.1` (`:325`) and **no `volumes:` key at all**; the dev twin
  `dev-stack/dev-setdress.sh:255` pins the same stock image
  (`DEV_SETDRESS_DIRECTUS_IMAGE:-directus/directus:11.6.1`).
- **`org-repos.md`'s other corpus-internal citations.** `ai-labs.md:4-8` does name the repo;
  `db-access.md:12` carries *"nobody queries with a write account"* verbatim; `safety.md:3` and `:36-37`
  do scope that document to the tooling; `toolchain_overview.md:91` is the AKB bullet and `:92-94` carries
  the *"full Anthropos context (product details, architecture…)"* quote; the five "already names AKB"
  sites (`setup_guide.md:322`, `staging-bringup.md:125`/`:151`, `staging-sync.md:112`,
  `toolchain_overview.md:91`) are exactly the pre-existing ones.
- **`architecture/README.md`.** The index lists all **11** non-README files in `corpus/architecture/` with
  no omission and no phantom. Its shared-libraries line verifies as an enumeration: `app/go.mod` and
  `sentinel/go.mod` @ `f2c46190` (colony `:8`, proto `:9`, taxonomy `:21` indirect) between them require
  only `colony`, `proto`, `taxonomy` of the five — **no `github.com/anthropos-work/ai`, no
  `…/authn`** — so *"only three are imported as private modules by a service a stack builds"* and *"`ai` is
  no longer one of them"* are both correct at `ad9f3c49`. Note this puts `architecture/README.md:22` in
  direct conflict with root `CLAUDE.md`'s *"four are imported as private Go modules (`ai`, `colony`,
  `proto`, `taxonomy`)"* — the corpus file is the correct side; the instruction file (rule 19's
  highest-propagation site) is the stale one, and it is outside this reading's scope.

**RE-DERIVED** (weaker — I recomputed arithmetic the document displays):

- `org-repos.md:4`/`:21` — 93 repos = 72 unarchived + 21 archived. I also summed the document's own
  enumerations for internal consistency: § 1 (26 unique) + § 2 (16 net-new) + § 12 archived (21, of which 5
  overlap § 1) + KBs (6) + live-but-unexamined (12, 1 overlap) + dormant (17) + `db-backup` = **93**. This is
  an internal-consistency check only; **the org listing itself is not in any clone set, so the SET behind
  the 93 is not something this reading can enumerate** (§5 rule 24 — I am recording the weak form
  deliberately rather than presenting it as a clearance).

---

## What I could not settle

- **Everything sourced from `infrastructure`, `directus`, `judge0`, `metabase`, `AI-Labs`, the five
  `livekit-agent*` repos, `sim-qa`, `hyper-studio`, `anthropos-knowledge-base`, `db-backup` and
  `analytics-go`'s own tree.** None is in any clone set on this box (13 top-level git trees under
  `stack-demo/`, enumerated with `find … -name .git -maxdepth 4`, plus the two nested `studio` checkouts).
  That covers the whole of `org-repos.md` §§ 3, 4 (the extension internals), 5-11 and most of § 2 — the
  `cms` M810 resolution, the ten `module "` declarations in `services.tf`, the four "orphaned" rows, the
  `livekit.toml` agent ids, the 96-vs-15 scenario count, the AKB line counts and its 14 unsourced
  taxonomy-figure sites. The document itself flags this (`:13`, `:22-25`) and its *platform-side* halves
  — every `app`-tree anchor it cites — did verify, which is the part I could reach. **Settling the rest
  needs `infrastructure` @ `13c248e6` and the nine other repos cloned into the reading's ground truth.**
- **`ai_architecture.md:94`'s standalone-`ai`-module claim** (`speech.go:12-13` @ `v1.40.1`,
  `TTSV2`/`TTSV2HD`). The standalone `github.com/anthropos-work/ai` module is not cloned — by the doc's own
  (verified) argument that nothing a stack builds requires it. Needs a clone at tag `v1.40.1`.
- **`ai_architecture.md:156`'s row counts** (`skill_embeddings` 42,790 / `job_role_embeddings` 18,919,
  measured 2026-06-29) and **`hiring.md:173`'s "87 real `SIMULATION_TYPE_HIRING` sims" / `:178`'s "0
  `job_position` rows"**. These are measurements of the production DB and of a captured snapshot; neither is
  readable from a clone set. Needs `/db-query` or the snapshot manifest.
- **`ai_architecture.md:169`'s skiller migration ids** (`20260417103036`, `20260417120309`) — the `skiller`
  repo is decommissioned and not cloned; those ids appear in no `app` migration filename.
- **`coursebuilder.md:24`'s "translate (7 locales)".** I could not find a locale allow-list anywhere in
  `internal/coursebuilder/` or `internal/web/backend/coursebuilder/` (`translateLocales` is a
  caller-supplied `[]string`, `service.go:1136`, `:3535`). Either the set lives in `SPEC.md`/the frontend,
  or it is unbounded. Needs the request-validation path or the UI's locale list to settle.
- **Whether `v3/validator/validator.go:595` counts as "never read"** for `checkerEngines`. The three anchors
  the corpus gives resolve exactly, and `:595` does *pass* the map into a call — so the "stored and never
  read" claim depends on the callee never dereferencing it, which I did not trace to a conclusion. The
  corpus's actual load-bearing point (the dispatch is the hardcoded `switch` at `criterion.go:127`/`:142`)
  is verified independently and stands either way.
