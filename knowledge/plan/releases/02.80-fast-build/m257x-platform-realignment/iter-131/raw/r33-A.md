# Seat A — reading #33/#34 (M257x iter-131)

## Ground truth actually read (re-verified at this seat's open)

| repo | path | HEAD | origin/main | dirty |
|---|---|---|---|---|
| app | `stack-demo/app` | `ad9f3c498` | `ad9f3c498` | 1 |
| app/studio (nested) | `stack-demo/app/studio` | `aeec036a5` | `aeec036a5` | 0 |
| platform | `stack-demo/platform` | `0c91421df` | `0c91421df` | 0 |
| next-web-app | `stack-demo/next-web-app` | `8297c684c` | `f97ba6599` | 0 |
| sentinel | `stack-demo/sentinel` | `f2c461903` | `f2c461903` | 0 |
| storage | `stack-demo/storage` | `4ce8ece52` | `9f8cb5322` | 0 |
| messenger | `stack-demo/messenger` | `fa47850d9` | `e9421c68f` | 0 |
| cms | `stack-demo/cms` | `ca50c8170` | `f38c0c4a4` | 1 |
| graphql-wundergraph | `stack-demo/graphql-wundergraph` | `60c229f39` | `60c229f39` | 0 |
| roadrunner | `stack-demo/roadrunner` | `87d8d4438` | `87d8d4438` | 0 |
| jobsimulation | `stack-demo/jobsimulation` | `462343b05` | `82cb66ecc` | 0 |
| studio-desk | `stack-demo/studio-desk` | `41ee3575d` | `41ee3575d` | 0 |
| ant-academy | `stack-demo/ant-academy` | `22df69dd8` | `22df69dd8` | **4 (dirty — read via `git show` only)** |
| rext **pinned per-stack** | `stack-demo/rosetta-extensions` | `09d06070f` | `4cb920aac` | 2 |
| rext **authoring** | `.agentspace/rosetta-extensions` | `f2ea567b3` | — | — |

Which tree settled what: tooling-on-a-stack claims → pinned `09d06070`; the one fence I ran
(`platform_alignment_guard`) → **authoring copy `f2ea567b3`** (rule 50). `infrastructure` and
`db-backup` are in **no** clone set on this box (confirmed: `ls stack-demo`, `ls stack-dev`,
`ls .agentspace`).

## Positive controls

| file | `wc -l` | read in full? |
|---|---|---|
| `corpus/architecture/external_services.md` | 887 | yes |
| `corpus/architecture/platform-migration-status.md` | 362 | yes |
| `corpus/services/storage.md` | 248 | yes |
| `corpus/services/askengine.md` | 147 | yes |
| `corpus/services/db-backup.md` | 66 | yes |

Protocol read: `corpus/ops/platform-alignment.md` §5 (lines 259–1892), in full.

---

## Blockers

### B1 — §4's assertion C says the state vocabulary has "nine" members; §1 defines eight, and the ninth token the table actually uses is defined nowhere

- **Anchor:** `corpus/architecture/platform-migration-status.md:189` —
  *"| C | every state is one of the **nine** in §1 (markdown emphasis stripped before the check) | a row invents a state, so the vocabulary stops meaning anything |"*
- **Why it is false:** §1's vocabulary table (`:47-56`) contains exactly **eight** rows, enumerated
  independently: `live-standalone` (`:49`), `merged-into-app` (`:50`), `running_but_unfederated`
  (`:51`), `mid-fold` (`:52`), `decommissioned` (`:53`), `net-new` (`:54`), `external` (`:55`),
  `library` (`:56`). Nothing between `:45` and `:70` defines a ninth. Yet the `authn` row (`:109`)
  sets **both** state cells to **`library-unimported`**, and its own prose says *"it is why iter-130
  added the `library-unimported` token"* — the token was added to the **fence**, not to §1. The
  fence's set is a hard-coded literal in the guard, not read from §1:
  `.agentspace/rosetta-extensions/stack-core/platform_alignment_guard.py:89-99` declares
  `ALLOWED_STATES` with nine members including `library-unimported`; the same file's own header
  comment at `:23` still says *"the documented **eight**-value vocabulary"*.
  So the row at `:189` is wrong about §1 in two ways at once, and a reader who meets
  `library-unimported` at `:109` and goes to §1 (*"How to read a row"*) to learn what it means finds
  no entry. I hesitated only because the arithmetic is off by one; I book it because §1 is the
  document's declared vocabulary and one of its live tokens is missing from it.
- **Confidence:** high

### B2 — the map contradicts itself about `cms`'s production state: the `cms` row asserts DESTROYED (measured); the `jobsimulation` row and §5 both say the `cms` row "asserts neither" and that the state is unmeasurable

- **Anchors (all in `corpus/architecture/platform-migration-status.md`):**
  - `:88` (the `cms` row) — *"**RESOLVED at M257x iter-123 — the ECS service is DESTROYED.**
    `infrastructure` was cloned and read (`13c248e6`, 2026-08-07): there is **no `module "cms"`
    declaration anywhere in it** … **the CI commit was the correct signal, and
    `cms/terraform/main.tf:39` is ORPHANED DEAD CODE**"*
  - `:89` (the `jobsimulation` row) — *"`cms` holds **two measured facts pointing opposite ways** …
    The `cms` row directly above **reports both and asserts neither**, and so must anything reading
    across from this one; the destruction itself lands in **infrastructure**, which is in no clone
    set."*
  - `:304-310` (§5 item 2) — *"while `cms` holds **two measured facts pointing opposite ways** … so
    the `cms` row **reports both and asserts neither** … the destruction lands in **infrastructure**,
    a repo no clone set has, so *unmeasurable* is the state, not *unmoved*."*
- **Why it is false:** these are flatly incompatible descriptions of the same cell, inside one
  fenced table plus its own summary section. `:88` asserts a measured verdict; `:89` and `:304-310`
  assert that no verdict exists **and** describe `:88`'s content incorrectly (`:88` does not "report
  both and assert neither" — it explicitly resolves). The two measured facts `:89`/`:304-310` still
  present as unresolved are the ones `:88` says were *"never contradictory."*
  I cannot settle which side is true from this box — `infrastructure` is in no clone set (the guard
  itself reports `UNCLONABLE head 'infrastructure' x9`), so `13c248e6` is unreadable here. But the
  direction is documented inside the protocol: `corpus/ops/platform-alignment.md:1768-1782` (rule 54)
  records that *"iter-123 cloned `infrastructure` and measured one rule that settled four standing
  questions at once"* and then *"repaired **three** of the four rows sharing that predicate"* — this
  is the un-repaired remainder of exactly that class, in the file rule 54 was written about.
  Two passages of one file asserting incompatible things is a finding regardless of which is right.
- **Confidence:** high

### B3 — `external_services.md` still publishes the retracted "cms's prod state is UNMEASURABLE" reading

- **Anchor:** `corpus/architecture/external_services.md:175` — *"`cms`'s is still declared **in its own
  repo** at `service_desired_count = 0` (`cms/terraform/main.tf:39`) — though `6efa1d5` (2026-08-04)
  deleted cms's build-production workflow saying *"the cms ECR repository is decommissioned (M810)"*,
  so the two measured facts point opposite ways and the prod-side state is **UNMEASURABLE** (the
  deletion lands in `infrastructure`, never in a clone set)"*
- **Why it is false:** contradicted by `corpus/architecture/platform-migration-status.md:88`, which
  the corpus itself designates the fenced index of truth for per-service state (`:11-13`, and this
  same file at `:3` points there — *"The fenced source of truth is `platform-migration-status.md`"*).
  That row says the question is **RESOLVED** and the ECS service **DESTROYED**, measured by cloning
  `infrastructure` at `13c248e6`, with `cms/terraform/main.tf:39` reclassified as **orphaned dead
  code**. Ground truth I could confirm from here: `cms/terraform/main.tf:39` is indeed
  `service_desired_count = 0` at both `ca50c8170` and `f38c0c4a4`, and `6efa1d5` (2026-08-04,
  *"chore(ci): drop build-production — the cms ECR repository is decommissioned (M810)"*) does delete
  the workflow — so the two facts are real; what is false is the conclusion *"UNMEASURABLE"*, which
  the corpus has since measured.
- **Confidence:** high

### B4 — `services/storage.md` publishes the same retracted "UNMEASURABLE" reading

- **Anchor:** `corpus/services/storage.md:175` — *"while `cms`'s ECS module survives **in cms's own
  repo** at `service_desired_count = 0` — but its prod state is **UNMEASURABLE** (`6efa1d5` deleted
  cms's build workflow saying the ECR *"is decommissioned (M810)"*, and the deletion itself lands in
  `infrastructure`, never in a clone set)."*
- **Why it is false:** same refutation as B3 — `platform-migration-status.md:88` measured it
  (`infrastructure` `13c248e6`) and this document's own *Related Documentation* chain routes readers
  to that map. Third site of one predicate, in a third file; rule 54's *"grade a correction by its
  reach"* is precisely what is failing here.
- **Confidence:** high

### B5 — the Cosmo router is described as surviving "prod-only", contradicting this file's own banner and the fenced map

- **Anchor (primary):** `corpus/architecture/external_services.md:368` — *"| **Purpose** | Federated
  GraphQL API gateway — **over ONE subgraph (`backend`) since `915da06`**, and **prod-only** since
  platform `2adcf71` deleted it from local dev |"*
- **Same predicate, same file, two further sites:**
  - `:14` — *"3. **GraphQL/Wundergraph** - Unifies all backend services into a single API. **Prod-only
    since platform `2adcf71`** — see the two-state note in that section"* (and `:10` still counts it
    as one of *"four key external services"* the platform *"integrates with"*).
  - `:804-807` — *"### Wundergraph / - Build and deploy as Docker container / - Configure production
    backend service URLs / - Enable caching and CDN if needed"* — unqualified live production-
    deployment instructions under *## Production Deployment*.
- **Why it is false:** this file's **own banner at `:3`** says *"**THE ROUTER IS GONE IN BOTH STATES
  — corrected M257x iter-124** … **And it is DESTROYED in production too**: `module.wundergraph_euwest1`
  is deleted from `infrastructure/terraform/production/services.tf` @ `13c248e6` … ECR hand-deleted
  **2026-08-05** … **This banner said *"in production the router is still declared"* until iter-124***.
  `platform-migration-status.md:96` says the same (*"**In prod it is DESTROYED — corrected M257x
  iter-124**"*). `:14`, `:368` and `:804-807` are the pre-iter-124 reading left standing 350–800
  lines below the retraction that removed it. This is the class `platform-alignment.md:1768-1787`
  (rule 54) names with `graphql-wundergraph` as its worked example.
  Verified from source that the *other* half of `:368` is correct — the supergraph really is one
  subgraph since `915da06` (`supergraph-config-prod.yaml` at `915da06` lists `backend` alone; 5→4→3→1
  across `749dc86~1`/`749dc86`/`7c17e63`/`915da06`) — which is what makes the false clause easy to
  read past.
- **Confidence:** high

### B6 — Ask Engine is documented as depending on "the shared `ai` library"; that module is imported by nothing `app` builds, and the code imports the in-tree package

- **Anchor:** `corpus/services/askengine.md:81` — *"the shared **`ai`** library (`CreateEmbeddings`,
  text-embedding-3-small, for RAG golden-example retrieval)"* (restated at `:113` — *"an embeddings
  provider for the shared `ai` client"*).
- **Why it is false:** at `app` `ad9f3c498` the ask handler imports the **in-tree** package, not the
  shared module — `internal/web/backend/ask/embed.go:8`, `internal/web/backend/ask/examples.go:15`
  and `internal/web/backend/ask/handler.go:20` all read
  `"github.com/anthropos-work/app/internal/ai"`, and `embed.go:23` is the `aiClient.CreateEmbeddings`
  call. `app/go.mod`'s `anthropos-work` requires at `ad9f3c498` are exactly `analytics-go`, `colony`,
  `proto`, `storage`, `taxonomy` — **no `ai`** — and `go.sum` carries **0** hits for it (control:
  `colony` = 2). `app/internal/ai/module_import_guard_test.go:18-38` is a one-way door against
  re-acquiring it.
  It also contradicts three sibling corpus passages that already carry the correction:
  `external_services.md:554` (*"this sentence said 'the shared `ai` library' until M257x iter-115"*),
  `platform-migration-status.md:108` (the `ai` row, `merged-into-app` / `merged-into-app`), and
  `shared_libraries.md`. Independently confirmed the module graph across the whole clone set: only
  the frozen `cms` `f38c0c4a4` and `jobsimulation` `82cb66ecc` still `require github.com/anthropos-work/ai`;
  `sentinel` `f2c461903`, `storage` `9f8cb5322`, `messenger` `e9421c68f`, `roadrunner` `87d8d4438` do not.
- **Confidence:** high

### B7 — the `anthropos-agent-eu` absence measurement does not reproduce: 7 hits, not 0

- **Anchor:** `corpus/architecture/external_services.md:727` — *"There is no `anthropos-agent-eu` —
  the name appears nowhere in the platform (**0 hits across all 15 trees at their own refs, and 0 in
  a `.gitignore`-blind filesystem grep**)."*
- **Why it is false:** I ran the §5 rule-44 recipe over every git tree under `stack-demo`
  (`find stack-demo -name .git -maxdepth 4` → **15** trees, so the denominator is right) and got
  **7 hits in the 15th tree**, `stack-demo/rosetta-extensions` @ `09d06070`:
  `stack-core/tests/fixtures/claim_twin_iter48/red/07.md:3`,
  `stack-core/tests/fixtures/mechanical/{green,red}/corpus/architecture/external_services.md:652`,
  `stack-core/tests/fixtures/repair_leak/{pre,post}/corpus/architecture/{ai_architecture.md,external_services.md}`.
  The authoring copy `f2ea567b3` carries the same 7. A `.gitignore`-blind filesystem grep of
  `stack-demo` also returns **7**, not 0. Positive control: `anthropos-agent` returns 6 in
  `stack-demo/app` and 8 in `stack-demo/jobsimulation`, so the pipeline is not broken.
  **Why I hesitated:** the *substantive* claim is TRUE — all 7 hits are archived snapshots of this
  corpus's own (retracted) prose inside rext test fixtures, and the name really is absent from all 14
  platform trees; the code half of the sentence is correct and I verified it
  (`app/internal/jobsimulation/calls/livekit.go:110`, `:115`, `:118-128`, `:120`, `:122-123`, `:126`,
  `:101-104`, `:111` all resolve exactly as written at `ad9f3c498`). I book it because the
  parenthetical is a *stated measurement with a stated instrument and a stated denominator* that a
  reader re-running it gets a different answer from — the exact failure §5 rules 2/44 exist to
  prevent, published inside a sentence whose credibility rests on it.
- **Confidence:** medium

### B8 — `architecture_overview.md:35` still calls db-backup "scheduled", contradicting `db-backup.md`'s whole page

- **Anchor:** `corpus/architecture/architecture_overview.md:35` — *"Production-only: **db-backup**
  (scheduled PostgreSQL backups)."* **(Anchor is outside my assigned file set — flagged because it
  directly contradicts an assigned file; may be a duplicate of another seat's finding.)**
- **Why it is false:** `corpus/services/db-backup.md:3` opens *"Everything this page said before
  2026-08-07 was wrong, and **the schedule has been OFF for over a year**"*, and `:15`/`:45`/`:53`
  state *"The schedule and its trigger are both commented out"* / *"It is not currently triggered by
  anything"* / *"**Schedule** | **none, currently**"*. `platform-migration-status.md:102`, the fenced
  index of truth, agrees: *"**Deployed but not triggered**"*. Every other surviving site in the corpus
  was repaired — `service_taxonomy.md:173`, `:528`, `dependency_map.md:38`,
  `architecture_overview.md:206`, `:454-459`, `security_compliance.md:13`, `:315`, `:322`,
  `services/README.md:48` and root `CLAUDE.md:250` all now say *"not on a schedule"* — `:35` of the
  same file whose `:206` carries the correction is the one that was missed. (I could not re-derive
  against the `db-backup` repo itself: it is in no clone set here.)
- **Confidence:** high

---

## Minors

- `corpus/architecture/platform-migration-status.md:213-214` and the sample output at `:250-256`:
  the fence's own numbers have drifted. Run from the **authoring copy** `f2ea567b3` (rule 50) against
  this corpus + `stack-demo/platform/repos.yml`, `platform_alignment_guard` prints
  `98 citation(s) — 25 subject-checked, 72 range-only, 1 outside any service block; 0 unresolvable;
  10 unclonable`, versus the document's `96 … 25 … 70 … 1` and a sample block that has no
  `unclonable` line at all (that class was added at iter-126). Booked MINOR only because `:244-247`
  explicitly says *"Take those three numbers from the run, never from this table."* The guard verdict
  itself is **`OK OVER ITS REACH`** — A/B/C/D/E/F/G all green, 10 citations into `db-backup` +
  `infrastructure` declared unreached.
- `corpus/architecture/platform-migration-status.md:115`: *"replaced by `jobsimulations` at
  `84862d1`"* — the compose service key added by that commit is **`jobsimulation`** (singular);
  `jobsimulations` is the wording of the commit *message*
  (*"feat: replace simulator with jobsimulations"*), not the service key. The same file's `:90`
  teaches *"a commit message is testimony, not evidence."* No factual consequence.
- `corpus/architecture/external_services.md:636`: cites `config_template.ini:30-31` bare, in a bullet
  whose other citations are full `app/studio/...` paths. There is no `config_template.ini` at the
  studio root (`git -C stack-demo/app/studio ls-tree --name-only aeec036a`); it is
  `configs/config_template.ini`, where `:30-31` are exactly
  `OPENAI_API_KEY = sk-aaa` / `OPENAI_ENDPOINT = https://api.openai.com/v1/chat/completions` and do
  support the claim. Four lines above, `:640` writes the same file correctly as
  `app/studio/configs/*.ini`.
- `corpus/services/askengine.md:134-135`: the handler-package test enumeration
  (*"sanitize, stream_registry, examples, lessons, distill, feedback, store, embed"*) is 8 of the 9
  `*_test.go` files in `internal/web/backend/ask/` at `ad9f3c498` — it omits
  `coursebuilder_tool_test.go`, the test for the tool the same document describes at `:19-20`/`:56-57`.
- `corpus/architecture/platform-migration-status.md:93` (messenger row): `messenger/terraform/main.tf:29`
  `service_desired_count = 0` / *"otherwise intact (121 lines)"* / `:19-25` / `:27-28` resolve
  **byte-exactly at `origin/main` `e9421c68f`** and **not at the checkout `fa47850d9`**, where the
  file is 111 lines and `:29` is `container_definitions = <<EOF`. The cell names no ref. Recorded as
  a MINOR rather than a blocker because origin/main is the ref the corpus's own citation guards
  resolve at (`CITE_REF=auto` → `origin/main` first, `platform_alignment_guard.cited_text`), the
  briefing's addendum declares both refs legitimate for the four behind-checkout clones, and the
  guard run is green over it. Adding the ref to the cell would remove the ambiguity.

---

## Positively cleared (audited zeros)

**ENUMERATED** (predicate re-derived from source independently, cardinality compared before any
arithmetic):

1. `platform-migration-status.md:75-81` — *"14 names ever in `repos.yml`"*. Re-derived:
   `git log -p --follow -- repos.yml` yields exactly `app · chronos · cms · graphql-wundergraph ·
   intelligence · jobsimulation · messenger · next-web-app · roadrunner · sentinel · skiller ·
   skillpath · storage · studio-desk` = **14**, member-for-member identical to the document's list,
   and all 14 have rows.
2. Same passage — *"25 compose service names ever; a section-blind pass returns 26, the extra token
   is `app-network`"*. Re-derived by parsing the `services:` block of **every** historical revision
   of `docker-compose.yml`: **25** names; the section-blind variant returns **26**, and
   `comm` shows the single extra member is `app-network`. All 25 map to rows (`backend`→`app`,
   `graphql`+`wundergraph`→`graphql-wundergraph`, exactly the three the document names).
3. `askengine.md:47-48` — *"**60** `TableDef`s, **57** `Schema: "public"`, **3** `Schema: "directus"`"*.
   Enumerated the `Schema:` values in `internal/askengine/registry.go` @ `ad9f3c498`: 57 / 3, sum 60.
   Also verified the *predicate*, not just the sum: `:183-195` is the transition-alias comment and
   `:192` says verbatim *"`jobsimulation` stays in rewriteSchemas until M710"*, and `:196` shows a
   dotted `jobsimulation.sessions` entry carrying `Schema: "public"` — i.e. a dotted name really is
   not evidence of a physical schema.
4. `askengine.md:127-132` — test counts. `grep -c '^func Test'`: `sandbox_test.go` **49**,
   `executor_test.go` **13**, `prompt_test.go` **10**, `followups_test.go` **14**. All four exact.
5. `external_services.md:640-643` — *"Every `*_AI_*_MODEL` line in all three `app/studio/configs/*.ini`
   pins the service to **azure**"*. Enumerated at `aeec036a`: **30** matching lines across
   `config_template.ini`, `development_config.ini`, `production_config.ini`; splitting each line's
   first value gives `30 azure`, `0` anything else. The conjunction, not just the count.
6. `platform-migration-status.md:92` (storage row) — the `b948604` breakdown. Enumerated all
   **15** `STORAGE_RPC_ADDR` hits in `*.go` and classified every one: 7 env lookups
   (`main.go` ×3 at `:446`/`:524`/`:992`, `internal/jobsimwiring/wiring.go:115`, one in each of
   `cmd/academy-asset-upload`, `cmd/academyImport`, `cmd/import`), 2 doc comments, 2 error strings,
   3 `t.Setenv`, 1 `t.Fatal` message = 8. Six of the seven are `os.Getenv`; `wiring.go:115` uses the
   lowercase `getenv` helper — exactly as stated. At `2035f9a` the same grep returns **3, all
   comments** (`main.go:504`, `jobsimwiring/wiring.go:101`, `storagens/callsites_test.go:189`).
7. `external_services.md:668-677` — the env-name census at `app` `5ba17044`. Re-derived with the
   document's own `\b<NAME>\b` semantics: `OPENAI_KEY` **5**, `ANTHROPIC_API_KEY` **26**,
   `AZURE_OPENAI_KEY` **13**, `AZURE_OPENAI_ENDPOINT_URL` **13**; `OPENAI_ORG_ID` /
   `AZURE_OPENAI_ENDPOINT` / `AZURE_OPENAI_DEPLOYMENT` **0**; `OPENAI_API_KEY` **2**. And the six
   bare names at `app/studio/gen.py:45-48` (nested repo, own ref `aeec036a`) are exactly
   `AZURE_API_KEY, AZURE_ENDPOINT, OPENAI_API_KEY, OPENAI_ENDPOINT, ANTHROPIC_API_KEY, ANTHROPIC_ENDPOINT`.
8. `external_services.md:373-386` — the subgraph-count table. Enumerated `- name:` in
   `supergraph-config-prod.yaml` at each named commit: `749dc86~1` **5**, `749dc86` **4**,
   `7c17e63` **3**, `915da06` **1**; dates 2026-06-24 / 2026-07-21 / 2026-07-29 all match, and
   `git show --name-status 915da06` marks **both** `schemas/cms.graphqls` and
   `schemas/jobsimulation.graphqls` `D`. The *"do not take the count from the commit subject"*
   caution is itself correct: `915da06`'s subject says *"supergraph 2→1"* against a 3-entry tree.
9. `external_services.md:387` — the no-subscriptions claim, both halves. (a) 0 `sse|subscription`
   hits across the three `*.yaml` at `60c229f3` (positive control: `backend` matches all three);
   `915da06~1:supergraph-config-prod.yaml:13` still reads `protocol: "sse_post"`; on mainline
   `log -S 'protocol: "ws"'` = **0** and `log -S sse_post` = **5** (with `--all` they are 1 and 7 —
   the extra `bba862f` sits only on `remotes/origin/feat/use-web-socket`,
   `merge-base --is-ancestor` rc **1**). (b) `schemas/backend.graphqls` has `type Mutation` at
   `:4053` and `type Query` at `:4912`, no `type Subscription`, and exactly **6** `Subscription`
   substring hits, all Stripe/plan names.
10. `platform-migration-status.md:108-109` (`ai` / `authn` rows) — the module graph, both directions,
    across every repo `repos.yml` declares plus the four frozen ones. `app/go.mod` @ `ad9f3c498`:
    `analytics-go, colony, proto, storage, taxonomy`; `go.sum` 0 for `ai` and 0 for `authn`
    (control `colony` = 2). Neither `sentinel` `f2c461903`, `storage` `9f8cb5322`, `messenger`
    `e9421c68f` nor `roadrunner` `87d8d4438` requires `ai` or `authn`; only frozen `cms` `f38c0c4a4`
    and `jobsimulation` `82cb66ecc` still require `ai v1.40.2`. `rosetta-extensions` requires
    `authn` in no `go.mod` and pins `ai v1.40.1` in `stack-seeding/go.mod:8` (verified in **both**
    the pinned `09d06070` and the authoring `f2ea567b` trees).
11. Compose/`repos.yml` cardinalities across the whole re-point sequence: services
    `d11a403^`=**11** → `d11a403`=**8** → `0dab54d`=**8** → `0c91421`=**5**; `repos.yml` entries
    **9 → 6 → 4 → 4**; `docker-compose.yml` **271** lines at `0dab54d` → **186** at `0c91421`;
    `838d907` is **+11/−96** on that file. Every figure in the map's banner (`:21-33`), the `storage`
    / `messenger` / `customerio-sync` rows, §3's *"4 in `repos.yml`"* and
    `external_services.md:305-306`'s *"five (seven effective) … eight/ten at `0dab54d`"* reproduces.
12. `anthropos-agent-eu` across all 15 git trees at their own refs + a gitignore-blind filesystem
    grep — this is the enumeration that produced **B7**; it also positively clears the claim's
    substantive half (0 in all 14 platform trees).
13. `platform-migration-status.md:92` — `STORAGE_RPC_ADDR` occurrences across `docker-compose.yml`,
    `common.yml` and `.env_example` @ `0c91421`: **0**, and `*_RPC_ADDR` as a class is **0** in all
    three (control: `DIRECTUS` = 3 in `.env_example`).

**Anchor-resolution clearances** (each citation opened with ±5–15 lines of surrounding context, per
§5 rule 10 — these prove the anchor names the construct the sentence describes, which is weaker than
an enumeration and is labelled as such):

- `platform-migration-status.md:87` — all seven wiring sites at `app` `2035f9a`: `:690`
  `skiller.NewSkillerManager`, `:721` `jobsimwiring.Wire`, `:751` `skillpath.NewSessionManager`,
  `:1153` `appcms.Wire`, `:524` `internalstorage.NewManager`, `:1471` `msgadapters.Wire`, `:395`
  `customeriosync.New`. Plus `app/terraform/main.tf:181 = 1`, `repos.yml:14-17`,
  `docker-compose.yml:28`, `app/internal/roadrunner/` **absent** while the other seven
  `app/internal/<domain>/` dirs exist, and `jobsimwiring/wiring.go:123`
  `jsrunner.NewRunnerManager(getenv("JUDGE0_API_KEY"), getenv("JUDGE0_BASE_URL"))`. The currency
  note also holds: `2035f9a..ad9f3c49` is exactly 5 commits touching
  `.claude/skills/publish/SKILL.md`, `CLAUDE.md`, `knowledge/deployment.md`, `terraform/main.tf`,
  `terraform/variables.tf` — **no Go source**, and `terraform/main.tf:181` is identical at both.
- `platform-migration-status.md:90` (roadrunner) — `terraform/main.tf:19 = 1`;
  `git blame -L 19,19` names **`84a4b4f1` (2025-12-15)**, not the file's most recent touch;
  `e45eb61` (2026-05-27) is `1 insertion(+), 1 deletion(-)` on line 11 with the quoted message
  *"Module contents are identical; this is a pure source-URL swap"*; HEAD `87d8d44` (2026-06-19)
  touches a workflow. The `d11a403^` compose really does still declare a `roadrunner:` block.
- `platform-migration-status.md:92` (storage) — `storage/terraform/main.tf` at `9f8cb53` is **18
  lines**, `:9-11` and `:13-16` quoted verbatim, `:18` is the outputs comment and not a custody
  clause, *custody* = **0** hits repo-wide (control: `bucket` matches 5+ files),
  `terraform/storage.tf:22-25` carries the M903 supersession verbatim, no `moved {` block exists,
  `d3e6d32` (2026-08-05) has the quoted subject. `app/internal/storage/service.go:22`/`:24` are the
  env-var *names*; `main.go:516-517`/`:518-523`/`:524-525`/`:547`/`:1102` all name the described
  constructs.
- `platform-migration-status.md:93` (messenger) — `app/main.go:15`/`:62`/`:63` imports,
  `:285`/`:1445`/`:1552` the `MESSENGER_ENABLED` switch, `:1416-1421` the consumer-group literal
  comment, `:1450` `pubsub.NewSubscriberServer`, `:1471` `msgadapters.Wire`, `:1473`
  `msgsender.NewFromEnv`; `env_guards.go:92-111` `resolveSubsystemSwitch`;
  `app/terraform/main.tf:415-416` `MESSENGER_ENABLED=true` and `:419-420`
  `CUSTOMERIO_SYNC_ENABLED=true`; `docker-compose.yml:48`/`:57`/`:59`/`:84-92`/`:102-103`/`:183`;
  `app/internal/converter/gotenberg.go:31`; `messenger/cmd/root.go:120-140` reading all four
  `*_RPC_ADDR`. The `gotenberg`-is-also-a-cross-process-edge correction is right.
- `platform-migration-status.md:96` — `supergraph-config-prod.yaml` = `backend` alone, `schemas/` =
  `backend.graphqls` alone, `subgraphs.conf` = `BACKEND=v1.360.0`.
- `platform-migration-status.md:111-118` — `common.yml:2`/`:22`/`:24`; and every historical date:
  `cb6ebf5` 2023-04-30, `8770fe6` 2023-05-04, `467965a` 2023-09-11, `1474b1f` 2023-12-01,
  `84862d1` 2024-05-29, `ef4b449` 2024-09-02 (which is indeed the commit that removes `chromedp`),
  `a2a3ee6` 2026-02-27, `b43b99a` 2026-02-26, `c17cc9a` 2026-04-15.
- `platform-migration-status.md:157`/`:165` — `app/go.mod:14` `analytics-go v0.3.1`,
  `internal/payments/handler.go:302-316` the Stripe-subscription `analytics.Track`;
  `app/go.mod:12` upstream `ThreeDotsLabs/watermill-redisstream v1.4.5` with **no `replace`** for it
  (the file's only `replace` is a sentry one at `:295`).
- `external_services.md:129-155`, `:239-263` — `docker-compose.yml:53` `DIRECTUS_PUBLIC_BASE_ADDR`
  at both `0dab54d` and `0c91421`; `.env_example:91-92` = `DIRECTUS_BASE_ADDR` + `DIRECTUS_TOKEN`;
  the un-retraction is right — `a2a3ee6^:docker-compose.yml:384` is
  `image: directus/directus:10.10.1`, `:386` `8055:8055`, `:409` `ADMIN_PASSWORD=password`, and
  `admin@example.com` is unfound in history (`log -S`, empty).
- `external_services.md:136`, `:206-220` — settled at the **pinned** rext `09d06070`:
  `stack-injection/gen_injected_override.py:86` `DIRECTUS_DATA_CONSUMERS = ("cms", "backend")`,
  `:77-81` the inert-key rationale verbatim, `:698-699` the re-point;
  `stack-core/gen_override.py:58` the identical tuple; `stack-injection/tests/test_injection.py:1109`
  `def test_backend_the_actual_reader_is_repointed`, with the old
  `test_only_cms_is_repointed_not_other_services` present only in an archived junit XML.
- `external_services.md:367`, `:420-473` — `terraform/locals.tf:8 port = 8080`,
  `terraform/main.tf:48-49` containerPort/hostPort, `config.prod.yaml:5 listen_addr: 0.0.0.0:8080`,
  image `0.275.0`, `wgc@0.104.0`; `2adcf71^1` **is** `1e8e754`, whose `docker-compose.yml:19-27`
  names exactly `backend, jobsimulation, cms, storage` each `service_started` and `:14` is
  `"5050:8080"`; `2c85211^` has no `dockerfile:` key and `2c85211` adds `Dockerfile.dev`;
  `b56d731:docker-compose.yml:22` still has `  graphql:` behind `profiles: [wundergraph-deprecated]`
  and `360efd4` has none (control `  backend:` at `:28`); `464dfe3` is on
  `remotes/origin/feat/cms-in-app` only (`--is-ancestor` rc 1); `graphql-wundergraph/CLAUDE.md:39`
  really does assert the opposite, written in `60c229f` 2026-07-30; `Dockerfile.dev` has one schema
  `COPY` at `:18`, the orphaned comment at `:19-20`, one `awk` at `:23`.
- `external_services.md:564-567`, `:577-643` — `report_agent.go:31`, `askengine/bedrock.go:25`,
  `coursebuilder/bedrock.go:23`/`:29`/`:106-113`, `cms/studio/markdownManager.go:10`/`:14`/`:29`/`:30`,
  `studioManager.go:583`, `mistral.NewMistral` **0 hits**, `studio/tools/pdf2md.py:24` +
  `:127 mistral-ocr-latest`, `tools/r3.py:139`/`:190`/`:199-206` (so *"`r3.py` DOES dispatch it"* is
  right), `services/ai.py:383`/`:627-664`/`:704-724`, `bedrock|boto3` **0 hits** under
  `app/studio` (control `openai` = 20 files), `jobsimulation/ai/ai.go:30-33`/`:80`/`:130-141`/
  `:150-155`/`:259-289`/`:264-276`/`:280-283`/`:296-302`/`:326`,
  `simulator/ai/ai.go:58-59` and its `default:` at `:114-115`,
  `cms/directus/collections/jobsimulation.go:905` (`AIVendor *AIVendor`) and `:1302-1305`
  (`aiVendor := simulation.Openai`), `app/main.go:770` @ `b948604`. The intra-file anchors are right
  too: `:567` is the Anthropic-Direct row, `:543` is the *"When backend services add new GraphQL
  types"* heading, `:546` is the struck `restart graphql` step, `:206-211` is the re-point blockquote.
- `external_services.md:707-709`, `:727`, `:731` — studio-desk `.env.example:56`
  `AI_PROVIDER_CHAIN=azure-openai,openai`, `src/services/ai/config.ts:21-39` defaults
  (`gpt-5.2` / `claude-opus-4-5-*` / `claude-sonnet-4-5-*`), circuit-breaker knobs at
  `.env.example:65,69`; `calls/livekit.go:101-128` as above; `voiceEngineFromDirectus`
  (`cms/directus/collections/jobsimulation.go:1595-1596`) returns `gptrealtime` on nil, and
  `flag_use_realtime_openai` (`livekit.go:133`) sets an agent name/endpoint, never an engine.
- `services/storage.md` — `recording.go:12` / `anticheat/anticheat.go:30` / `main.go:1048` all
  resolve at the ref the sentence names (`9d00a313`); `app/internal/storage/storage.go:193-200`
  `getKeyPath` branching on `s3Bucket != ""` @ `2035f9a`; `main.go:518-523` + `:529-535` +
  `env_guards.go:37-44` at `ad9f3c49`, and the stated *ref-flip* is real —
  `git ls-tree b948604f -- env_guards.go` is empty and at `b948604f` `:518-523` is the
  public-storage clients block and `:529-535` the academy asset uploader, exactly as claimed;
  `docker-compose.yml:56` `ENVIRONMENT=development`, `:82`/`:83` the production buckets,
  `:73-79` *"These MUST be set"*, `:100` the `$HOME/.aws/credentials` mount;
  platform `README.md:81-87` the live-credential instruction; `storage/internal/storage/storage.go:122`
  the empty-string presigned URL; `Dockerfile:18 RUN go test -v ./...` with **0** `*_test.go` in the
  repo and **0** `//go:generate`; `cmd/root.go:45-46` binary defaults 8080/8081; Go **1.25.0**;
  historical compose `:210`/`:206` @ `2adcf71` and `:119` @ `0dab54d`, `repos.yml:18-20` @ `0dab54d`,
  and `:324` @ `2adcf71` really is inside the studio-desk block.
- `services/askengine.md` — `main.go:467-471` verbatim with `func main() {` at `:229` and **no**
  intervening `func` declaration (I grepped the whole 229–467 span); `:1295` the `http.NewServeMux()`,
  `:1361` the meta HTTP server comment; the log string occurs **exactly once** in `app`'s Go;
  `askengine/bedrock.go:161` with its single `config.LoadDefaultConfig` error return;
  `executor.go:18`/`:21`/`:24` (`MaxInlineRows=200`, `MaxCellLength=400`, `QueryTimeout=10s`) and
  `:97` the `SET LOCAL statement_timeout`; `main.go:167` `askEngineMaxConns = 6`;
  `handler.go:34`/`:41`/`:248` (`maxAgenticIterations=15`, `loopTimeout=10*time.Minute`,
  `context.WithoutCancel`); `rules.md` = **146,219 bytes**; `prompt_test.go:176`
  `TestBuildSystemPrompt_RulesBlockIsIdentical`; `handler.go:718` `OrgCheckFeaturePermission`; all
  eleven `/ask` routes at `internal/web/backend/backend.go:171-188`, mounted with CORS + Clerk authn
  under the comment *"sibling group of /api that skips OpenAPI validation"*; all five `ask_*` tables
  present in `terraform/migrations/`.
- Fence run (authoring copy `f2ea567b3`, dirty state clean for the guard file):
  `platform_alignment_guard` on `platform-migration-status.md` + `stack-demo/platform/repos.yml`
  returns **OK OVER ITS REACH** — A/B/C/D/E/F/G green, 10 citations into `db-backup` +
  `infrastructure` declared UNCLONABLE rather than silently passed.

---

## What I could not settle

- **Everything in `corpus/services/db-backup.md` that cites the `db-backup` or `infrastructure`
  repos** — neither is in any clone set on this box (`stack-demo/`, `stack-dev/`, `.agentspace/` all
  checked). Unverified: the `6e1fb15b` = `v0.3.3` = HEAD identity, the 43-line Bash / 11-file /
  572-line / 337-Terraform-lines census, the `git log -S azure` and all-157-objects Azure sweep, the
  `rate(12 hours)` value, `terraform/main.tf:10-14` / `:16-27` / `:51` / `:215`, `storage.tf:24-38`,
  the empty `terraform/stage/main.tf`, `7dd1b80`'s date and subject,
  `infrastructure/terraform/production/services.tf:571`, and
  `infrastructure/modules/core/storage/rds.tf:6,19,78-89`. The page is internally consistent and its
  banner discloses its own limit (`:31-33`). **Settled by:** cloning `db-backup` and `infrastructure`.
- **B2/B3/B4's direction.** I can prove the corpus contradicts itself; I cannot prove which side is
  true, because `infrastructure` `13c248e6` is unreadable from here. **Settled by:** cloning
  `infrastructure` and re-running `grep -rn 'module "cms' terraform/ modules/`.
- **Anything asserting a GitHub *archive* state** — `platform-migration-status.md:96` (router
  archived 2026-07-30), `:97` (skiller 2026-07-01), `:98` (skillpath 2026-07-31), `:99` (chronos NOT
  archived), `:100` (intelligence 2026-04-02), and `:89`'s explicit *"not something this map can
  see"*. Archive state lives in the org API, never in git objects. **Settled by:**
  `gh api repos/anthropos-work/<name> --jq .archived`.
- **§3's org census scalars** — *"93 repositories … 46 named by no corpus document"* (`:126-133`) and
  every `last push` date in the census table. Requires the GitHub org API. The one derivable member —
  *"**4** are in `repos.yml`"* — I did enumerate, and it is right.
- **`corpus/services/storage.md:89-92`'s pointer** to
  `knowledge/plan/platform-defect-register.md`. Hard bar 1 forbids reading it; I could not check that
  `PLATFORM-M257x-compose-points-local-backend-at-the-PRODUCTION-S3-buckets` exists there. The
  *technical* content of that hazard note I did verify independently (compose sets both production
  buckets on `backend`, mounts real AWS credentials, and `ENVIRONMENT=development` disarms both boot
  guards) — the exposure is real as described.
- **`external_services.md:3`'s closing clause** — *"Where production's frontends now send GraphQL is
  NOT something this corpus can see — that is Vercel runtime configuration, in no clone set."* True
  as far as I can check, and unfalsifiable from here; recorded so it is not mistaken for a cleared
  claim.
