# iter-131 pre-registration — written BEFORE the read, at corpus `60edbd84`, platform `0c91421`

**Sealed in this iter's FIRST commit, before a single seat is dealt.** Nothing below is written, moved,
re-cut or softened after a number lands.

## What this reading IS, and what it is NOT

**It is NOT grading a strategy.** `TOK-08` was **refuted at iter-119** by its own pre-registered rule
(`P = 22`, against a `P ≥ 19` refutation branch sealed at `577446b`), and that rule **barred a successor
strategy**. There is no `TOK-09` and none is authored here. Runs 80–83 have proceeded under the **user's
direct instruction**, and run 83's brief is what asks for this reading.

**It is the first reading in eleven iters**, and it measures a corpus that has absorbed a great deal:
30 consequence-class sites, 13 false claims in C1, **46 fixes from the now-exhaustive 820-assertion
complement read**, the eleven-vs-six route-group correction, the `ai` row in the fenced map, the tier-1
re-pin work, and — this run — ~24 routed residual findings plus 10 sites of the `@anthropos.work`
predicate.

**The question is therefore narrow and answerable:** *after that volume of repair, does `P` move?*

## THE DISCLOSURE THAT MUST COME BEFORE THE NUMBER

### 1. `P` is a FLOOR over a suspicion-selected sample. It is not a corpus error rate.

**iter-119 measured this directly and it is the most important thing this milestone knows about its own
instrument.** On a corpus five in-place lines from its predecessor, with **zero** of iter-116's 37
predicates repaired in between:

| | |
|---|---|
| `P` | fell **37 → 22 (−40.5 %)** |
| iter-116's 37 re-found blind, on predicate | **13 (35.1 %)** |
| predicates found at iter-119 that iter-116 never saw | **9** |
| **union floor across the two readings** | **≥ 46** |

> **`P` fell while the measured floor ROSE.** Every `P` this milestone has published — 22, 22, 24, 37,
> 22 — samples roughly **a third** of a standing pool. **Most of the movement in that series is the
> instrument, not the corpus.**

**Binding consequence, pre-registered:** *a fall in `P` at this reading does NOT by itself demonstrate
drainage, and a flat `P` does NOT by itself demonstrate that the repair failed.* The close will say which
of the two it can and cannot distinguish, and it will not claim the flattering reading of an ambiguous
number.

### 2. A hunted sample over-states the population rate by ~19×. Do not present `P` as a rate.

The census measured the population directly: **0.70 %**, and **0.23 %** substrate-corrected — against a
band that had been built from **≥ 13.3 %**. A reading's `P` is a count over a *suspicion-selected*
sample: seats are told to hunt, and hunting finds. **`P` is a count of found defects, never a
denominator-bearing rate**, and no percentage will be computed from it in the close.

### 3. How `P` relates to the census's denominators — and where it does not

The census now has real denominators the readings never had. Measured at **this reading's own ref**
(`60edbd84`), not imported:

| census quantity | value at `60edbd84` |
|---|---|
| in-scope files | **41** |
| claiming units (non-heading blocks) | **2,614** |
| **tier-1 pairs** (claiming unit × citation) | **2,996**, over 1,053 distinct units = 40.3 % |
| **tier-2 unevidenced assertions** | **1,140** in 962 blocks = 36.8 % of units |
| assertion candidates (the liveness denominator) | **3,562** |
| the 820-assertion complement, read EXHAUSTIVELY for falsity (iter-129) | **`fix` = 46 = 5.6 %** |

**The relationship, stated so it cannot be implied later:**

- **`P` and tier-2 are NOT the same measurement and neither bounds the other.** Tier 2 counts
  **UNEVIDENCED**; `P` counts **FALSE**. A sentence can be uncited and true, or cited and false. The
  census says so on every run in its own KNOWN WEAKNESS block.
- **`P` is not a subset of the 1,140** and must not be reported as a share of it.
- **The one directly comparable number is iter-129's exhaustive complement read: `fix` = 46/820 = 5.6 %**
  — a *census* of falsity over a defined population. **If this reading's `P` is far below 46, that is
  evidence about the READING's recall, not about the corpus** — the two measure the same property by
  different methods, and the census is the one with a denominator.
- **`P` relates to nothing else on that list.** Stated plainly rather than left for a reader to assume a
  relationship the sheet never established.

### 4. `F4` binds — the enumerator must not become the grader

**Clause 5 is graded by the reading and by nothing else.** The census, the sweeps, the fences and the
complement read are *not* inputs to clause 5's verdict, however exhaustive they are and however many
defects they closed. Nothing found by this run's Priority-1 repair work is added to clause 5's
accounting, and nothing in clause 5's accounting is credited to the sweeps.

## THE METRIC

**`P` = the count of distinct false PREDICATES**, adjudicated, in-scope, upheld — the identical
definition used at iters 101, 103, 109, 116 and 119. **`N` = the distinct-anchor count**, reported
alongside as the continuity metric with its historical bands (`≤ 8` drains / `9–19` partial / `≥ 20` does
not drain). **If `P` and `N` disagree, `P` governs and the disagreement is the headline.**

**Both are FLOORS in every branch.** They are unions of two passes whose measured per-pass recall has run
33–83 %. **No branch of anything below is a gate verdict.** Clause 5 is met by a reading that returns
**zero**, and by nothing else — four user rulings, not reopened here.

**No point estimate of the pool is quoted.** Chapman stays retired. Only floors survive: **≥ 24 at
`8f04d3a`, ≥ 33 at `e6aed2e`, ≥ 36 at `ac48e5b`, ≥ 37 at `f581de09`, ≥ 46 at `194361e4`** (iter-119's
union floor, the highest yet).

## Bands — 13 of them, each stated so it CAN fail

Prior readings graded **4 of 9**, **3 of 7**, **5 of 9**, **4 of 10**, **7 of 13**, **7 of 14** and
**8 of 13**, with *mechanism* claims mostly holding and *magnitude* guesses mostly failing. That split is
the useful shape and these bands do not drift toward safe.

| # | prediction | band | kind |
|---|---|---|---|
| 1 | per-reading in-scope upheld BLOCKER count (n₁, n₂) | **[6, 30]** each | magnitude |
| 2 | **union `N`** (anchors) | **[14, 40]** | magnitude |
| **2p** | **union `P`** (predicates) — **the primary** | **[12, 34]** | magnitude |
| **3** | **overlap with iter-119's published 22 predicates**, matched on PREDICATE, blind | **[3, 12]** | mechanism |
| 3b | within-reading `m` between passes #33 and #34, as a **share of the union** | **[10 %, 55 %]** | mechanism |
| 4 | adjudicator upheld rate (raw) | **[72 %, 94 %]** | mechanism |
| 5 | the two passes' recalls against the union differ by | **≥ 12 points** | mechanism |
| 6 | **wrong-tree rejections** (the briefing-defect class) | **[0, 3]** | mechanism |
| 7 | **intra-corpus citation** defects among upheld in-scope blockers | **≤ 5** | mechanism |
| 8 | **platform-drift share** of upheld in-scope blockers | **[20 %, 60 %]** | mechanism |
| 9 | per-seat booked spread over the 14 seats (max − min) | **≤ 8** | magnitude |
| **10** | **repair-induced** — upheld blockers whose anchor sits in prose iters 120–130 wrote | **[1, 8]** | mechanism |
| 11 | **anchors per predicate** (`N`/`P`) | **[1.00, 1.45]** | mechanism |
| 12 | upheld in-scope anchors sitting in a **multi-pin block** | **[2, 12]** | mechanism |
| **13** | upheld blockers whose anchor is in **`org-repos.md`**, the net-new 466-line file no reading has ever seen | **[0, 4]** | mechanism |

## What each band is actually risking

**#2p is the primary; #2 is the secondary — recorded here so the order cannot be chosen afterwards.**

**#1 / #2 / #2p — the magnitude guesses, and they are NOT centred where the gate would like.** The honest
centre for `P` is **~20**: the corpus absorbed real repair, but iter-119 proved that a single reading
recovers only ~a third of a standing pool whose union floor was already **≥ 46**, and the subject is now
**9.7 % larger** than the one that returned 22. **A repair-driven fall and an instrument-driven fall look
identical in `P` alone** — which is exactly why band #3 exists and why the close will not read a fall as
drainage without it.

**#3 — the test-retest band, and its interpretation is AMBIGUOUS BY CONSTRUCTION here. Said in advance.**
iter-119 could read its 35.1 % overlap cleanly as *recall*, because nothing had been repaired between the
two readings. **This reading cannot**, because substantial repair HAS happened. So:
- **A LOW overlap (≤ 5)** is consistent with *the repair held* AND with *each reading samples a different
  slice*. **It does not discriminate**, and the close will say so rather than claim the favourable one.
- **A HIGH overlap (≥ 10)** is the sharp, unambiguous result: it would mean predicates that survived
  eleven iters of repair are still standing, i.e. **the repair is not reaching what the readings book.**
  That is the outcome worth the most and it is named here as such.
- The band is wide because the quantity is genuinely under-determined; a narrow band here would be
  theatre.

**#4 — the precision band.** Readings: 92.1 / 93.0 / 92.7 / 93.1 / 78.3 / 77.8 / 97.9 / 91.4 / 92.6 / …
Fails high at > 94 %, low at < 72 %. **Reported TWICE — raw, and with the `wrong-tree` class separated.**

**#5 — predicted to return, and it has now FAILED three readings running.** A fourth failure says the
mechanism is wrong rather than the tuning, and this sheet will say so rather than re-cut the band a fifth
time.

**#6 — the briefing defect, measured for the seventh time.** Series **4 → 1 → 1 → 0 → 0 → 0**. The
addendum's two-tree rule is unchanged, **but two things got harder this reading**: the rext trees are
**33 commits apart** (the widest gap yet) and `stack-demo/ant-academy` has a **dirty working tree**. If
this band fails, those are the two named suspects, pre-registered.

**#7 — RE-AIMED DOWN to `≤ 5`.** iter-116 measured 10, iter-119 measured 8 (36 % of its pool) against a
`≤ 6` band that FAILED. Class 1 is censused and fenced at 100 % reach with the ratchet at zero, and
iter-129 additionally repaired tier-1 citations wholesale. **Cutting the band DOWN after two consecutive
failures is deliberate**: if it fails a third time at a tighter cut, that is the strongest possible
statement of `D-M257x-117-2` — the machine and the reader are looking at different halves of one class —
and it will be reported as the finding, not as noise.

**#8 — held two-sided.** ~33 % (iter-109), ~38 % (iter-116) over a frozen clone set. Fifth consecutive
frozen reading.

**#9 — tests THIS reading's partition only.** The partition is **NOT** iter-119's (41 files / 11,922
lines vs 40 / 10,871), so seat-level numbers are **not comparable across readings**, and this band is
graded within-reading.

**#10 — the induction band, RE-CUT UPWARD and this is the sheet's most consequential re-cut.** History:
+48 lines → 2 induced (5.6 %); +177 → 9 (22.0 %); iters 117–118 wrote 5 lines → band `[0, 1]`, held.
**Iters 120–130 wrote +1,248 / −197 lines across 31 in-scope files.** At the measured 5.6–22 % rates that
volume predicts far more than one. **`[1, 8]` is an honest band on a large exposure, and a `0` would be a
first-class finding** — it would say induction is not proportional to volume at all, contradicting nine
consecutive cycles of measurement.

**#11 — anchors per predicate.** History: 1.09 → 1.50 → 1.50 → 1.108 → 1.27. Nothing has re-armed twins.

**#13 — NET-NEW, and it audits a genuine blind spot.** `org-repos.md` is **466 lines, authored at
iter-123, and no graded reading has ever covered it** — it did not exist when the last partition was
cut. It is 3.9 % of the in-scope corpus. **`≥ 5` would say that net-new prose enters at a defect density
well above the corpus average**, which would bear directly on how every future authored doc is treated.

## Binding conditions on the read itself

1. **The instrument is not touched.** Briefing byte-identical, sha `3858ec53…`, `git log --follow`
   showing one commit ever, **re-checked AFTER copying AND again after the addendum was appended** — all
   three printed in `ground-truth.md`, `diff` empty. **The known defect at line 37 is delivered as-is**
   and overridden in the addendum, never edited. Superseded ground truth goes in the ADDENDUM, **below**
   the frozen text, never above it.
2. **Clause 5 is not re-cut, narrowed, argued, or read met any other way.** Met only by a reading that
   returns **zero**. Four user rulings. **No branch of anything above is a gate verdict.**
3. **No repair inside the measuring pass.** Anything found routes; nothing is fixed. **This iter does not
   repair, full stop** — the tree stopped moving at iter-130's close and stays stopped until this reading
   is adjudicated and reported.
4. **Ground truth re-derived, not inherited** — every checkout's ref AND its `origin/main` AND its fetch
   time restated, guard family run with its own fence tree printed, the partition **reproduced at both
   refs as a control**, all before the seats are dealt.
5. **No clone is fetched while this reading is in flight** (§5 rule 41a). Fetch times are re-read at the
   close and published, so a mid-reading move is detectable rather than suspected.
6. **The upheld rate is reported TWICE** — raw, and with the `wrong-tree` class separated.
7. **Adjudicate before reporting `P` or `N`.** Always.
8. **Every seat is committed verbatim the moment it lands**, before adjudication.
9. **`P` and `N` are reported whatever they are.** A rise, a fall and a flat are each a real result. No
   rounding toward the answer the gate wants.
10. **No point estimate of the pool is quoted**, in this file, the close, or `state.md`.
11. **Grade by CONSEQUENCE, not by class.** If this reading surfaces a defect of the severity iter-115's
    inverted security property had, it is reported plainly and first, regardless of its bucket or its
    class's size.
12. **The dirty `ant-academy` clone is disclosed, not cleaned.** Cleaning a clone mid-reading would be a
    mutation of the substrate under measurement.
