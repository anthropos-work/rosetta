# Seat D — reading #18

> **Provenance note (orchestrator):** this seat's harness blocked its file write; it returned the
> full report as text and the orchestrator persisted it verbatim.

## Compact summary

B1 | corpus/architecture/dependency_map.md:58 | messenger-in-app subscriber called "not opt-in"; app gates it behind MESSENGER_ENABLED | high
B2 | corpus/services/roadrunner.md:114 | claims jobsimulation repo has one "roadrunner" mention; it has 14 across 8 files | high
BLOCKERS=2 MINORS=5

## Positive controls

| file | wc -l | read in full |
|---|---|---|
| service_taxonomy.md | 484 | yes |
| architecture_overview.md | 413 | yes |
| security_compliance.md | 281 | yes |
| roadrunner.md | 200 | yes |
| next-web-app.md | 133 | yes |
| dependency_map.md | 103 | yes |
| intelligence.md | 18 | yes |

All 13 clone refs re-derived at open and matched the addendum sheet exactly. Two of my own searches
were caught by in-pass controls and re-run: `grep 'UserMixin{}'` was matching `CreatedByUserMixin{}`,
and several `--include=*.go` invocations were eaten by zsh globbing into SILENT empty results.

## B1 — dependency_map.md:58

> "That one is not opt-in: it is the stock `core` selection."

At `app` origin/main the whole messenger-in-app block is gated: main.go:1445 `if messengerEnabled {`,
from main.go:285 `mustSubsystemSwitch(envMessengerEnabled)`, defined env_guards.go:61-62. main.go:1437
says it in the platform's own words: "ALL OF IT is behind MESSENGER_ENABLED. Gating only the sender
would not be enough: merely constructing this server and calling Subscribe() attaches app to
messenger's LIVE consumer group."

The cell pins `app 9d00a313`, where it WAS true — MESSENGER_ENABLED has 0 occurrences at 9d00a313 and
at checkout b948604f (control BREVO_KEY: 9 and 3 files). But it is written as standing present-tense
guidance and THREE sibling passages assert the opposite: dependency_map.md:21 (same table),
service_taxonomy.md:97-99, architecture_overview.md:30-32 — all citing docker-compose.yml:84-92,
which I read and which says exactly that. Those three are right at current app HEAD; :58 is the
outlier. Everything else in that cell is exact, including "21 live handlers" (22 EventHandler lines,
one commented out at flow.go:83).

Confidence: high

## B2 — roadrunner.md:114

> "at `jobsimulation 462343b0` the whole repo contains one `roadrunner` mention"

Measured at that exact ref: 14 hits across 8 files (CHANGELOG 6, CLAUDE.md:112,
internal/runner/runner.go:3, knowledge/{architecture:61, features:70, glossary:60/87, operational:68,
service-dependencies:23}). 8 hits / 7 files excluding CHANGELOG. Identical at that repo's origin/main.
Only the Go source count is 1 — the true and useful statement. Hesitated because the conclusion (no
handler, no event ref) is correct; booked because a reader running the obvious check gets a flatly
contradicting answer.

Confidence: high

## Minors

- roadrunner.md:25-26 — "zero hits outside CHANGELOG" is false (jobsimulation knowledge/operational.md:68
  carries ROADRUNNER_RPC_ADDR); the load-bearing "no read in any service's Go code" IS true at both refs.
- security_compliance.md:73 — mixin.go:98 is the doc comment; `func (UserMixin) Policy()` is at :99.
  Its twin mixin.go:126 uses the function line — two conventions for one construct type.
- dependency_map.md:27 — "(production terraform: skiller_rpc_addr = http://backend:8081)" is not
  derivable: all three terraform declarations have NO default, and the value lives in `infrastructure`,
  which dependency_map.md:31 itself calls UNMEASURABLE.
- roadrunner.md:13-14 — "not been touched since 87d8d44" names the repo HEAD; terraform/main.tf's last
  touch is e45eb61 (2026-05-27). Vacuously true, wrong commit.
- architecture_overview.md:334-336 — "3 hits repo-wide" is 3 in the source tree (all comments, verified)
  but 29 counting the in-repo knowledge/ docs.

## Positively cleared — ENUMERATED (the rule-24 target)

Derived the SET before any arithmetic, at checkout b948604f:

139 .go files -> 135 declare ent.Schema (the 4 that don't: database_types.go, mixin.go,
skiller_mixins.go, skillpath_mixins.go) · OrganizationMixin{} token in 30 files, 29 live
(user_resource.go:22 commented, line exact) · OrganizationIDMixin{} in 7 (names match exactly) ·
exactly 4 files declare any Policy() (mixin.go:99/:126, org_membership.go:172, organization.go:56,
user.go:116) · neither-mixin-with-organization_id = 18, minus org_membership.go (own policy) and
academy_feedback.go (bare UserMixin{} at :64) = 16 (all 16 names match) · headline SET = 31 =
29 union {Membership, Organization}.

Predicate checks beyond the arithmetic: no double-count (both Membership and Organization carry only
PrimaryKey/CreatedAt/UpdatedAt mixins); organization.go:96 really is FilterSameOrganizations();
org_membership.go:172-188 really is fail-closed; User correctly excluded (user-scoped). "No policy of
any kind" holds for all 23 — the trap that would break it is whether any of the 16 or the 7 carries a
bare UserMixin{}; word-anchored, NONE does. The corpus's 31 is right AND its derivation is right.

## Other ENUMERATED clearances

Compose topology (5 declared / 7 effective / core starts 5 / 5 legal tokens on 4 services / one
AUTHORIZATION_ADDRESS, zero *_RPC_ADDR; historical 8-and-10 at 0dab54d, 9 repos at 2adcf71) · the
subgraph ladder 5->4->3->1, with 915da06 deleting both cms.graphqls and jobsimulation.graphqls (the
"2->1" in its own subject really is wrong) · router port 8080, never 5050 · authn in 0 of 7 go.mods,
control colony in 7 of 7 · taxonomy direct in app/messenger, // indirect in storage/sentinel · storage
fold both sides (18-line main.tf at 9f8cb53; 0 Getenv reads at app origin/main vs 6 at checkout;
main.go:504 <-> :451 same comment line) · M810 jobsim terraform at 6092c6d2 · the entire AI-routing /
EU-AI-Act citation set (~20 anchors, every one on the named construct, including checkerEngines
assigned-never-indexed) · all three Clerk setActive hooks · next-web-app.md end-to-end incl. all
origin-HEAD-pinned claims · Studio-Desk 0 framework deps / 0 .tsx / 7 languages · Studio-Room 9 gen.py
args + parse_known_args + mistralai imported nowhere · all Ant Academy anchors · intelligence.md whole
file clean.

## What I could not settle

GitHub archive status for 7 repos (repository metadata, needs gh api) · the `infrastructure` tfvars
value for skiller_rpc_addr · taxonomy cardinalities (needs prod SELECT count(*)) · DPA/sub-processor
compliance facts. Also worth flagging: the demo's pinned app (b948604f) has no internal/messenger/ at
all — three app refs give three different answers to B1's question, and only one of the four corpus
passages names a ref.
