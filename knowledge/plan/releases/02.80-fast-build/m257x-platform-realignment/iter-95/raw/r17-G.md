# Seat G — reading #17 (adversarial diff seat)

## Positive controls

**Diff command run** (from `/Users/marco/workspace/anthropos/rosetta`):

```
git diff 8d6bb6c..HEAD -- corpus/ CLAUDE.md .claude/
```

| control | observed |
|---|---|
| files changed | **54** |
| insertions / deletions | **+1642 / −680** |
| raw diff lines (`| wc -l`) | **4272** |
| commits in range (`git log --oneline 8d6bb6c..HEAD`) | **23** (iter-83 → iter-94 + 4 harden passes + 1 state chore) |
| hunks reviewed | **every hunk of all 54 files, read top-to-bottom in four passes** (skills+CLAUDE.md · architecture · ops · services), plus the `+453` `platform-alignment.md` hunks |
| STEP 2 — `corpus/ops/platform-alignment.md` §5 read in full | **yes** (lines 249–1362 of 2139; `wc -l` = **2139**) |

**Ground-truth clones re-derived at open** — every checkout matches the addendum sheet exactly:

| clone | checkout | origin/main |
|---|---|---|
| `platform` | `0c91421d` | `0c91421d` |
| `app` | `b948604f` | `2035f9a4` |
| `next-web-app` | `bb3313bc` | `8297c684` |
| `storage` | `4ce8ece5` | `9f8cb532` |
| `messenger` | `fa47850d` | `e9421c68` |
| `cms` | `ca50c817` | `f38c0c4a` |
| `jobsimulation` | `462343b0` | `82cb66ec` |
| `sentinel` | `88bc5592` | `f2c46190` |
| `studio-desk` | `14a5442a` | `41ee3575` |
| `ant-academy` | `9c3843cd` | `22df69dd` |
| `roadrunner` / `graphql-wundergraph` | `87d8d443` / `60c229f3` | level |
| `rosetta-extensions` (authoring) | `6130bfd8` | level |
| `rosetta-extensions` (`stack-demo/`, consumption) | tag `fast-build-m257x-iter-58` | — |

**Search-pipeline controls run in the same pass** (§5 rules 1–4): every `grep`/`git grep` here was paired
with a pattern known to match. One derivation (the `repos.yml` historical name union) **returned 0 on its
first run** — a broken pipeline, not an absence; re-run against a file-driven loop it returned 14. Recorded
because it is the exact shape §5 rule 2 exists for.

---

## Blockers

### B1 — The repair proved `skiller` has no producer, then left the refuted claim standing in the stream table it was editing

- **Anchor:** `corpus/architecture/dependency_map.md:59` —
  `` | `skiller` | App | App | Skill score changes — both producer and consumer live inside app since the skiller→app merge (stream name retained) | ``
- **Why it is false:** the same commit range added `corpus/services/backend.md:252`, which states the
  opposite and is **correct**: *"`skiller` is the exception: `app` only SUBSCRIBES to it and nothing
  publishes to it … the fact was **deleted, not moved**."* I enumerated the predicate independently at the
  checkout `b948604f` rather than re-deriving the document's arithmetic. **Every `pubsub.NewPublisher`
  constructor in `app`** is: `main.go:287` (`serviceName` = `backend`), `main.go:637` (`SKILLPATH_STREAM`),
  `main.go:1039` (`CMS_STREAM`), `internal/jobsimwiring/wiring.go:127` (`AI_USAGE_STREAM`), `wiring.go:180`
  (`JOBSIMULATION_STREAM`) — **five, none of them `SKILLER_STREAM`.** The only Go occurrence of
  `SKILLER_STREAM` is `main.go:1276`, which is `subServer.AddSubscriber(...)`. The Producer cell reading
  `App` is false at the checkout and at origin/main.
  This is §5 rule 40's mechanism verbatim: the sweep **rewrote rows `:57`, `:58` and `:60` of this very
  table** (the `backend`, `jobsimulation` and `cms` stream rows, all `+` lines in the diff) and left the one
  row `backend.md` had just refuted untouched, one line apart.
- **Confidence:** high

### B2 — The same refuted claim was re-published verbatim in `CLAUDE.md`, in a sentence the repair rewrote

- **Anchor:** `CLAUDE.md:280` (last clause) — *"The `skiller`, `skillpath`, `jobsimulation` and `cms`
  streams have `app` on **both** ends"*
- **Why it is false:** identical ground truth to B1 — no `SKILLER_STREAM` publisher exists in `app` at
  `b948604f` or `2035f9a4`; `main.go:1276` is an `AddSubscriber`. Not an untouched survivor: the whole
  bullet is a `+` line in the diff (the repair rewrote the RPC half of the sentence and carried the stream
  half through unexamined). Booked separately from B1 because `CLAUDE.md` is, in this protocol's own words
  (§5 rule 19 corollary), *"the highest-propagation site in the tree"* — read by every agent before any doc.
- **Confidence:** high

### B3 — The protocol document's own `repos.yml` history figures were falsified by the commit this whole sweep is about

- **Anchor:** `corpus/ops/platform-alignment.md:1031-1032` — *"every `- name:` ever written in `repos.yml`,
  across all **9** commits that touched it — **14 ever, 6 now, 8 removed**."*
- **Why it is false:** re-derived from the artifact's history at the checkout `0c91421d` (§5 rule 24 —
  enumerate the set, do not re-run the sum):

  ```
  git log --format=%H -- repos.yml            ->  10 commits    (claim: 9)
  union of '^ +- name:' over all 10 revisions ->  14 names ever  OK
      app chronos cms graphql-wundergraph intelligence jobsimulation messenger
      next-web-app roadrunner sentinel skiller skillpath storage studio-desk
  git show HEAD:repos.yml | grep -c '^ +- name:'  ->  4 now      (claim: 6)
  => removed = 14 - 4 = 10                                       (claim: 8)
  ```

  `838d907` removed `storage` and `messenger` — the single event this entire 54-file sweep exists to
  propagate, and corrected at ~15 other sites inside the same diff (`CLAUDE.md`, `platform_repo.md`,
  `setup_guide.md`, `service_taxonomy.md`, `frontend_architecture.md`, `platform-migration-status.md` §3,
  `update_guide.md`, `staging_from_dump.md`, the three skill references…). The repair added **+453 lines to
  this exact file** and left this rung of the same predicate wrong. The figure is load-bearing: it is the
  *derived legal set* rule 37 tells a fence to build from, and `ops/README.md:36` + `CLAUDE.md` both cite
  this document as the authority for following the drift.
- **Confidence:** high

### B4 — `platform-alignment.md` still asserts, in the present tense, that `storage` is a declared compose service behind `profiles: [storage-legacy]`

- **Anchor:** `corpus/ops/platform-alignment.md:2080-2081` — *"because `storage` is **still declared** in
  the base compose under `profiles: [storage-legacy]`, so it is **not an orphan**, while not being in the
  default profile, so it is **not selected** either."*
- **Why it is false:** at the checkout `0c91421d`, `docker-compose.yml` is 186 lines and declares exactly
  five services — `sentinel:5`, `backend:28`, `studio-desk:112`, `next-web-app:143`, `gotenberg:170`. There
  is **no `storage` block and no `storage-legacy` token anywhere in the file** (`grep -n 'storage-legacy'
  docker-compose.yml common.yml` -> no matches, against a positive control of `grep -c 'profiles:'` -> 4).
  The four `profiles:` keys carry only `core`, `backend`, `all`, `studio-desk`, `frontend`. `838d907`
  deleted the block. A surviving `storage` container **is** an orphan now, which inverts the paragraph's
  conclusion for anyone debugging a stuck teardown — the exact operational cost the passage exists to
  prevent. The sentence asserts currency (*"is still declared"*), so per the addendum's rule 1 no
  neighbouring pin rescues it; and the repair edited this file from line 2105 onward, 24 lines below.
- **Confidence:** high

### B5 — The sweep re-anchored `rosetta-extensions` citations against TWO different clones, and one new anchor names a different mechanism

- **Anchor:** `corpus/architecture/external_services.md:208-209` — *"`rosetta-extensions/stack-injection/
  gen_injected_override.py:669-670` re-points every service in `DIRECTUS_DATA_CONSUMERS`, which is
  `("cms", "backend")` (`:84`)"*
- **Why it is false:** graded at the authoring copy (`.agentspace/rosetta-extensions`, `6130bfd8`) — the
  witness the addendum's rule 2 names, and the one §5 rule 36 records as `resolve()`'s own preference:
  - `:669-670` is inside the **`CORS_EXTRA_ORIGINS`** comment block (*"…cors.go honors /
    CORS_EXTRA_ORIGINS in non-production…"*) — a different mechanism entirely. The Directus re-point is at
    **`:698-699`** (`if with_directus and name in DIRECTUS_DATA_CONSUMERS:` -> `env.append(f"DIRECTUS_BASE_ADDR=…")`).
  - `DIRECTUS_DATA_CONSUMERS = ("cms", "backend")` is at **`:86`**, not `:84` (`:84` is a comment line).
  - The twin claim at `corpus/ops/demo/frontend-tier.md:621` cites `profile_for()` at `:420`; at the
    authoring copy `:420` is `eff["CLERK_SIGN_IN_URL"] = …`, the `profiles: !override` emission is `:425`,
    and `profile_for` is **defined** at `:33`.

  All three resolve exactly at the **`stack-demo/` consumption clone** (tag `fast-build-m257x-iter-58`:
  `:84`, `:420`, `:669`). But the **same sweep** moved `corpus/ops/demo/demo-up-defaults.md:55` from
  `up-injected.sh:2670` -> **`:2671`** — the **authoring** copy's line (consumption reads `:2670`) — and
  left `stack-injection/tests/test_injection.py:1109` (authoring; consumption reads `:1108`) untouched.
  **Two witnesses inside one pass; whichever clone is authoritative, one set of these edits moved a correct
  anchor to a wrong one.** That is §5 rule 36's *"two clones of one repo are one witness"*, committed by the
  repair that publishes the rule. The `:669-670` instance is the material one: it does not drift, it names
  the wrong construct.
- **Confidence:** medium-high (high on the internal inconsistency, which needs no external adjudication;
  medium on which clone the corpus intends, because no citing block pins a ref)

### B6 — "29 direct REST/SSE calls" is an exact grep-line count of a set that contains 8 non-call lines

- **Anchor:** `corpus/architecture/frontend_architecture.md:39` — *"there are direct REST/SSE calls,
  **29 of them across 21 non-test files** hitting `NEXT_PUBLIC_BACKEND_API_URL` … the four
  `packages/core-js` REST clients (… **12 sites** between them) … The long-standing *"~15 sites"*
  undercounted by half."*
- **Why it is false:** I enumerated the set at the ref the sentence pins (`next-web-app` `bb3313bc`) rather
  than re-deriving its arithmetic. `git grep -n NEXT_PUBLIC_BACKEND_API_URL bb3313bc` returns 44 lines / 33
  files; filtering to non-test app source gives **exactly 29 lines across exactly 21 files** — the
  document's arithmetic reproduces perfectly. **The predicate does not.** Eight of the 29 are not calls and
  cannot be: `packages/core-js/src/{coursebuilder,credits,talkToData,workforce}/api.ts` each contribute
  **three** lines — one `const BACKEND_URL = process.env…` read, one **error-message string literal**
  (*"…is not set — required by @anthropos/core-js/…"*), and one **header comment**. That is the "12 sites"
  figure: 12 grep lines, **4** actual reads. The real count of `process.env.NEXT_PUBLIC_BACKEND_API_URL`
  read sites is **21, one per file**. This is §5 rule 24 exactly — *"a `grep -c` over source counts
  commented-out code; enumerate the set, exclude what does not compile"* — and rule 17's *"a count can be
  exactly right while the claim it supports is false."* It also makes the accompanying verdict wrong:
  against 21 real sites, *"~15"* undercounted by **29%**, not *"by half"*, so the repair over-corrected the
  figure it replaced.
- **Confidence:** medium (I hesitated because "sites" could be read as grep hits — but the sentence's own
  antecedent is *"direct REST/SSE **calls**"*, and it uses the number to grade a prior claim)

### B7 — `CLAUDE.md`'s v9.0 banner replaced anchors that were correct at its own pin with anchors that resolve at neither its pin nor the checkout

- **Anchor:** `CLAUDE.md:222-223` — *"`app` serves object storage in-process (`app/main.go:524`, `:525`)
  and has **taken over messenger's own Redis consumer group** (`:1384`, `:1450`)"*, inside a blockquote
  whose only ref is *"(platform `0dab54d` / `app` **`9d00a313` v1.367.0**)"* at `:206`.
- **Why it is false:**

  | anchor | at checkout `b948604f` (main.go = 1361 lines) | at the block's own pin `9d00a313` | at origin/main `2035f9a4` |
  |---|---|---|---|
  | `main.go:524` | `os.Getenv("STORAGE_RPC_ADDR")` — **the storage RPC client**, the literal opposite of the claim | `var academyTenancy academy.TenancyResolver` | OK `storageManager := internalstorage.NewManager(...)` |
  | `main.go:525` | `os.Getenv("COMPANY_LOGO_STORAGE_PATH")` | `if orgManager != nil {` | OK `publicStorageManager := …NewPublicManager(...)` |
  | `main.go:1384` | **out of range** (file is 1361 lines) | `log.Fatalf("messenger-in-app: can't open its Redis client…")` | OK the messenger-in-app comment |
  | `main.go:1450` | **out of range** | `}` | OK `messengerSubServer = pubsub.NewSubscriberServer(` |

  The **pre-repair** text read `:471`, `:472`, which are **correct at `9d00a313`**
  (`storageManager := internalstorage.NewManager(storageBucket)` / `publicStorageManager := …`). The repair
  re-derived at `app` origin/main and updated the numbers **without updating the ref**, converting a correct
  citation into one that resolves nowhere the block names. Same defect at `CLAUDE.md:280`
  (*"`app` serves object storage in-process and says so at `app/main.go:504`"* — at the checkout `:504` is a
  jobsim-in-app comment about `BACKEND_USERS_RPC_ADDR`; the *"STORAGE_RPC_ADDR is gone"* comment is `:451`
  at `9d00a313` and `:504` only at origin/main).
  Every sibling document that used origin/main in this same sweep **says so in the block** —
  `platform-migration-status.md:81` (*"Anchors re-resolved M257x iter-87 at `app` origin/main `2035f9a`"*),
  `:92` (*"a Go grep at `app` origin/main `2035f9a`"*), `architecture_overview.md:337-341` (*"still closed at
  `app` **origin/main**, where … `main.go:504`"*, and it even flags that the comment stood at `:451` at
  `9d00a313`). `CLAUDE.md` alone carries origin/main anchors under a `9d00a313` pin, so the two files
  disagree about what `main.go:524` is.
- **Confidence:** medium (I hesitated because the anchors are correct at origin/main and §5 rule 33 scopes a
  pin to its own sentence — but the briefing grades an unpinned anchor at the **checkout**, where two of the
  four are out of range and one names the refuting construct; and the repair demonstrably *broke* a
  previously-correct citation)

---

## Minors

- `corpus/architecture/platform-migration-status.md:92` (`storage` row) — *"It was true up to `9f8cb53`^"*.
  At `9f8cb532^` (= `4a1cbf1`) `storage/terraform/main.tf` is **already 18 lines**; line 38 last held
  `service_desired_count = 0` up to `e6fac97^` (i.e. at `8e558ce`, 119 lines / `63bffc8`, the v0.15.5 tag).
  The validity boundary names a commit two moves too late.
- `corpus/architecture/architecture_overview.md:336` — *"`STORAGE_RPC_ADDR` has **zero** reads anywhere in
  `app` (3 hits **repo-wide** at `9d00a313`, every one a comment)"*. Repo-wide the grep returns ~29 hits
  (`knowledge/**`, `knowledge/tools/*.md`, …), many not comments. **3, all comments** is the **Go-source**
  count (`-- '*.go'` -> `main.go:451`, `internal/jobsimwiring/wiring.go:101`,
  `internal/storagens/callsites_test.go:189`). Scope word wrong, substantive claim right — and the
  `platform-migration-status.md` twin of the same sentence says *"a Go grep"* correctly.
- `corpus/architecture/service_taxonomy.md:38` and `corpus/architecture/architecture_overview.md:128` — the
  repaired mermaid edge label reads *"spawns studio/gen.py **in-process**"*. It is a **subprocess**:
  `app/internal/cms/studio/studioManager.go:119` -> `s.runCommand(ctx, pyBin, append([]string{"studio/gen.py"}, …))`
  -> `exec.CommandContext` at `:1101`. The edge *direction* is the fix and it is correct (the old
  `Room --> Desk` was backwards); only the label is self-contradictory. `cms.md` and `dependency_map.md`
  both say *"dispatched as a subprocess"*, so this is a lone outlier.
- `.claude/skills/stack-update/reference.md:96-104` — headed *"Repository update order (the make-driven
  `pull` order)"* and lists `platform` as step **1**, on the strength of *"`make pull` walks `repos.yml` in
  file order, so the whole list is four repos plus the `platform` clone you run it from."* `make pull`
  iterates `REPO_NAMES`, which is `awk '/^  - name:/{print $$3}' repos.yml` (`Makefile:13`) — `platform` is
  not an entry, so `make pull` never touches it. The parenthetical hedges it; the numbered list does not.
- `corpus/architecture/external_services.md:208` cites `DIRECTUS_DATA_CONSUMERS` at `:84` (authoring copy:
  `:86`) and `corpus/ops/demo/frontend-tier.md:621` cites `profile_for()` at `:420` (authoring copy: use
  site `:425`, definition `:33`). Line drift only — subsumed by B5, recorded separately because each is
  individually a MINOR under the canonical grading rule (§5 rule 25).
- `corpus/architecture/frontend_architecture.md:39` — *"origin/main `ad767d1b` reads 31 across 22"*.
  `ad767d1b` is an **ancestor** of `next-web-app` origin/main `8297c684`, 15 commits behind. Labelling a
  non-HEAD commit "origin/main" is §5 rule 26's *state the refs at the moment of measurement*; mitigated by
  the sentence's own *"treat any figure here as pinned, never current."*
- `.claude/skills/dev-for-dummies/reference.md:22-28` adds *"Ports that no longer exist on a stack — do not
  target them … `cms` (8090/8091), `jobsimulation` (8400/8401) and `roadrunner` went at `d11a403` … Nothing
  listens on the retired numbers"*, while the **unedited** lines `:52-54` immediately below still enumerate
  `cms jobsimulation skillpath … messenger storage … roadrunner graphql-wundergraph` as **Valid TARGET
  repos** ("must exist as `stack-demo/<repo>`"). Reconcilable (a target repo is a source tree, not a port)
  but the repair left them adjacent and unreconciled.

---

## Positively cleared (audited zeros)

**ENUMERATED** — predicate re-derived independently from source, cardinality stated before any arithmetic:

- **`docker-compose.yml` topology @ `0c91421d`.** 186 lines; **5** declared services (`sentinel:5`,
  `backend:28`, `studio-desk:112`, `next-web-app:143`, `gotenberg:170`) + `postgresql`/`redis` from
  `common.yml` = **7 effective**; exactly **4** `profiles:` keys yielding exactly **5** tokens (`core`,
  `backend`, `all`, `studio-desk`, `frontend`); `core` selects 5 containers; `all` selects all 7. Clears the
  5/7 and five-token claims in `CLAUDE.md`, `service_taxonomy.md:65-68` + `:451-455`,
  `platform_repo.md:60-66`, `external_services.md:172-175` + `:295-297`, `dev-up/reference.md:141-152`,
  `run_guide.md:88`, `setup_guide.md:435`, `staging-bringup.md:365`, `staging_from_dump.md:325`. Also the
  anchors `:48`, `:73-75`, `:82`, `:83`, `:84-92`, `:101-109`, `:102-103`, `:110`, `:112`, `:119`, `:135`,
  `:138-140`, `:141`, `:143`, `:151`, `:152`, `:160`, `:161`, `:165-167`, `:168`, `:170-171`, `:183`, and
  `:37` (the only `ARCH` build arg; `sentinel` has none) — all resolve.
- **Zero `*_RPC_ADDR` in any compose file** (`grep -rn RPC_ADDR docker-compose.yml common.yml Makefile` ->
  exit 1, against a positive control of `grep -c ADDRESS` -> 1). Exactly one service address,
  `AUTHORIZATION_ADDRESS=http://sentinel:8087` at `:48`. Clears the `backend -> sentinel`-is-the-last-edge
  claim across `CLAUDE.md:280`, `dependency_map.md:7`, `service_taxonomy.md:395`, `platform_repo.md:70-77`,
  `backend.md`, `cms.md`, `jobsimulation.md`, `messenger.md`.
- **`repos.yml` @ `0c91421d`** — 28 lines, **4** entries (`app:14-17`, `sentinel:18-20`,
  `next-web-app:23-25`, `studio-desk:26-28`), `app` the only `migrations: true`; header `:2-10` and `:12-13`
  as cited. Every line-range citation into it in the diff resolves.
- **`app` Redis publishers @ `b948604f`** — 5 `pubsub.NewPublisher` sites, no `SKILLER_STREAM` producer
  (this is what produces **B1/B2**; `backend.md:252` is the correct side and it clears).
- **`STORAGE_RPC_ADDR` Go hits @ `b948604`** — `platform-migration-status.md:92` claims *"15 hits, of which
  7 are env lookups … the other 8 are two doc comments, two error strings, three `t.Setenv` calls and one
  `t.Fatal` message … six of the seven are spelled `os.Getenv`; the seventh uses a lowercase `getenv`."*
  Enumerated: **15** total; env lookups `academy-asset-upload:129`, `academyImport:231`, `cmd/import:50`,
  `wiring.go:115` (lowercase `getenv`), `main.go:446`/`:524`/`:992` = **7**; the remaining 8 split exactly
  2/2/3/1 as stated. Model-quality derivation — it survives rule 24 in full.
- **`secret-dna.json`** (authoring copy) — **64** genes; per repo platform 32 / app 10 / studio-desk 7 /
  next-web-app 7 / ant-academy 6 / sentinel 2; status 44 required / 12 optional / 8 waived (5
  `waived-optional` + 1 each `-profile-gated`, `-aws-mount`, `-config`); required split 13 critical / 31
  standard; `version: fast-build-m256`; `profile: "graphql"` literally present. Clears the 56->64 change at
  all six sites (`CLAUDE.md`, `corpus/README.md`, `ops/README.md`, `secrets-spec.md`, `tailscale-serve.md`,
  `stack-secrets/SKILL.md`) **and** the new disclosure that the `profile` field is stale-but-inert
  (`secretdna/dna.go:233` load-check, `secretdna/catalog.go:17` print — both resolve).
- **Snapshot fidelity operators** — `stack-seeding/dna/snapshot.go:62` `validSnapshotOperators` has **6**
  members; `data-dna.json:410` (taxonomy) lists 5 without `cross-surface-closure`; `:439` (content) lists 5
  with it and without `embedding-dim`. Clears the five->six correction in `alignment_testing.md` and
  `snapshot-spec.md:349`.
- **`colony/authn` across the 7 Go clones** — `authn` = 0 in every `go.mod`, `colony` = 1 in every one.
  Clears `architecture_overview.md:170`, `service_taxonomy.md:479`, `clerk-integration.md:51`.
- **Studio-Room provider registry** — `app/studio/services/ai.py:705-708` is a three-entry dict (`openai` /
  `azure` / `anthropic`); `mistralai` occurs once repo-wide, in `requirements.txt`, imported nowhere. Clears
  the Mistral retraction in `dependency_map.md:59`+`:88`, `service_taxonomy.md:209`, `cms.md:95` +
  downstream-deps, and the Go-side OCR-only counter-claim (`markdownManager.go:10`/`:30`,
  `studioManager.go:531`/`:583`, `xlsx.go:13` @ `2035f9a4`). `app/studio/CHANGELOG.md` confirms v0.51.1.
- **`repos.yml` name union over history** — 14 ever / 4 now / 10 removed across 10 commits (produces **B3**).

**RE-DERIVED / anchor-resolved** (weaker — the arithmetic or the citation, not the set):

- Terraform trio: `jobsimulation/terraform/main.tf:15-22` (the M810 decommission comment: ECS service, task
  def, ECR repo) and `:24-40` / `:38-40` (what survives) @ `82cb66ec`; `storage/terraform/main.tf` 18 lines
  with `:9-11`/`:13-16`/`:18` @ `9f8cb532`, and `service_desired_count = 0` at `:38` @ the intermediate
  `63bffc8`; `messenger/terraform/main.tf:29` = 0 in a 121-line module with `:19-25`/`:27-28` @ `e9421c68`;
  `cms/terraform/main.tf:39` = 0 in a 191-line module @ both `ca50c817` and `f38c0c4a`; `6efa1d5` merged
  `f38c0c4` with the exact quoted subject. The **uneven-M810** repair — the sweep's largest new claim — is
  correct on every side, including its explicit *"UNMEASURABLE, report both, assert neither"* on `cms`.
- `app/main.go` @ `2035f9a4`: `:15`, `:62`, `:63`, `:285`, `:286`, `:394`, `:395`, `:504`, `:516-518`,
  `:524`, `:525`, `:547`, `:690`, `:721`, `:751`, `:1102`, `:1153`, `:1416-1421`, `:1445`, `:1450`, `:1471`,
  `:1473`, `:1552` — **all 23 resolve to the named construct**; plus `app/terraform/main.tf:181`/
  `:415-416`/`:419-420`, `env_guards.go:62`/`:92-111`, `internal/storage/service.go:22`/`:24`,
  `internal/customeriosync/doc.go:4`, `internal/messenger/adapters/skiller.go:11`. `main.go:1442` @ the
  pinned `9d00a313` is the `verifyConsumerGroupExists` call naming `StreamBackend` at `:1445`;
  `internal/messenger/flow/streams.go:65` is `StreamBackend = "backend"`.
- `messenger/internal/flow/flow.go:72`/`:105`/`:109` `AddSubscriber` sites, and the **21 live handlers** on
  the `backend` stream — counted excluding the commented-out `OrgJobSimulationAssignmentPastDueHandler` at
  `:83`, which is rule 24 applied correctly by the author.
- `next-web-app` @ `bb3313bc`: `AIReadinessClient.tsx:78`/`:153-154`/`:166`/`:599`,
  `UserStatusContext.tsx:141-173`. Gotenberg call sites @ `9d00a313`: `extract.go:59`/`:77`,
  `user_import_resume_2d.go:66-74`. Jobsim historical ports @ `2adcf71`: `:93-94`/`:113`/`:119`.
  `studio-desk/.env.example:45`; `app/internal/web/backend/graphql/graph/schemas/cms_queries.graphqls:106`.
- `roadrunner/terraform/main.tf:19` — `git blame -L 19,19` names **`84a4b4f` (2025-12-15)**, and `e45eb61`
  (2026-05-27) is a 1-line change. The repair's correction of its own earlier attribution is right.
- `Makefile:10` (`PROFILE ?= core`), `:13` (`REPO_NAMES` from `repos.yml`), `:31-47` (`pull`), `:120`
  (`up-frontend` = `--profile core --profile frontend`).
- rext: `test_injection.py:1109`, `up-injected.sh:2671`, `alignctl/run.go:133-136` + `:139-153`,
  `stack-verify/lib/services.sh:49-52`, `readiness.sh:251` — all resolve at the authoring copy.
- Intra-corpus: `service_taxonomy.md:321-330` and `:67-68` both cover the passages `external_services.md`
  and `service_taxonomy.md` cite them for.

---

## What I could not settle

- **Which `rosetta-extensions` clone the corpus intends as its citation witness (B5).** The addendum and §5
  rule 36 both say the authoring copy; the repair used the consumption clone for
  `gen_injected_override.py` and the authoring copy for `up-injected.sh` and `test_injection.py`. Settled by
  the corpus declaring the witness in the citing blocks, or by `anchor_construct_guard` being run over both
  clones and one of them going RED.
- **`cms`'s production teardown state.** The corpus is explicit that this is UNMEASURABLE — the destruction
  lands in `infrastructure/terraform/production/services.tf` and that repo has never been in a clone set. I
  confirmed both halves it reports (`cms/terraform/main.tf:39` = 0 at `f38c0c4a`; `6efa1d5`'s deletion of
  `build-production.yml` with the quoted body) and could not settle the third. The corpus's own *"report
  both, assert neither"* is the correct disposition.
- **Whether `platform_repo.md:239` is complete** — *"the cms and jobsim tables were re-created in `public`
  under `app/terraform/migrations/`."* I verified the `repos.yml` half; I did not enumerate
  `app/terraform/migrations/` to confirm the table re-creation is exhaustive, which would need a schema diff
  against the frozen `cms`/`jobsimulation` repos.
- **`platform-alignment.md`'s two reach figures** (*"`docker-compose.yml` citations — 23 of them"*, *"every
  other `file:line` … 66 citations"*, `platform-migration-status.md:203-204`). These are counts over the
  map's own text produced by a guard; re-deriving them means re-implementing the guard's citation regex,
  which I judged out of proportion. Settled by running `platform_alignment_guard.py` and reading its printed
  reach line.
