# Seat E — reading #18

> Provenance: harness blocked this seat's file write; returned as text, persisted by the orchestrator
> (blockers/minors/unsettled verbatim; the 23-item ENUMERATED clearance list condensed to its heads).

## Compact summary

B1 | corpus/services/storage.md:58 | PRIVATE storage manager claimed local-FS; compose points it at production bucket | high
B2 | corpus/services/storage.md:25 | "Custody transfer is M903 (:18)" — wrong anchor, and M903 was never executed | high
B3 | corpus/services/hiring.md:81 | manager.go:485 anchor lands on a different function's closing brace | medium
BLOCKERS=3 MINORS=6

## Positive controls

studio-room.md 483 · hiring.md 433 · graphql-wundergraph.md 279 · storage.md 210 ·
clerk-integration.md 128 · frontend_architecture.md 105 — total 1638. All read top-to-bottom in full.
Plus platform-alignment.md Sec5 (:249-1362 of 2139) read in full.

Refs: platform 0c91421d · app b948604f (origin 2035f9a4) · next-web bb3313bc (origin 8297c684) ·
storage 4ce8ece5 (origin 9f8cb532) · graphql-wundergraph 60c229f3 · sentinel 88bc5592 · cms ca50c817 ·
jobsimulation 462343b0 · studio-desk 14a5442a · ant-academy 9c3843cd · rext authoring 6130bfd8.

Search controls run in-pass: grep -c REDIS_ADDR->1 beside STORAGE_RPC_ADDR->0; 8082->8 beside 5050->0;
job_simulation_session.go found beside local_jobsimulation_session*.go->0; 'SET '->39 files beside
SET "score"->0. One zsh glob failure (--include=*.go unquoted) caught and re-run rather than read as
absence.

## B1 — storage.md:58 — PRIVATE manager claimed local-FS; compose binds it to the PRODUCTION bucket

Anchor: "In the platform compose, STORAGE_S3_PUBLIC_BUCKET is hardcoded to the production public
bucket, so locally the PUBLIC manager talks to real S3 ..., WHILE THE PRIVATE MANAGER USES LOCAL FS."

No ref named -> graded at platform 0c91421d, where compose sets BOTH buckets on the surviving backend
service:
  docker-compose.yml:82 -> STORAGE_S3_BUCKET=production-storage20240826131618541000000005
  docker-compose.yml:83 -> STORAGE_S3_PUBLIC_BUCKET=production-storage-public20240919130721114900000001

The block's own comment (:73-79) states the intent: "storage-in-app (v9.0): app owns the buckets
in-process... These MUST be set... with empty buckets app boots 'fine' and silently writes every
upload to the container's ephemeral disk." At app 2035f9a4, main.go:516-517 reads
os.Getenv(internalstorage.EnvBucket/EnvPublicBucket) and :524-525 builds NewManager(storageBucket) /
NewPublicManager(...); constants at internal/storage/service.go:22,:24 are those two names. Compose
`environment:` wins over `env_file:`, so on a stock `make up` the private manager is bound to the real
production private bucket — the one storage/terraform/storage.tf:40 annotates "Private corpus: CVs,
resources, attachments, reports."

The doc fences its Environment Variables table (:181-184) and its Local Development prose as
historical; THIS paragraph was left in the present tense and is the one that is now wrong. An engineer
trusting it will exercise upload paths against production storage believing they land in /tmp.
Confidence: high.

## B2 — storage.md:25 — M903 presented as the forward custody transfer, citing a line that says otherwise

Anchor: "...read from configuration (storage/terraform/main.tf:13). Custody transfer is M903 (:18)" —
graded at storage 9f8cb53, the ref the row's own table header (:23) names.

Two independent failures:
1. WRONG CONSTRUCT. terraform/main.tf at 9f8cb532 is exactly 18 lines (as the row itself correctly
   says). Line 18 is "# See outputs.tf — consumers should reference these by output, never by literal
   name." M903 does not occur anywhere in that file. (:13 IS right.)
2. SUBSTANCE INVERTED. The only M903 mention at that ref is terraform/storage.tf:22-25: "(An earlier
   plan, M903, instead proposed moved-ing these resources into a local module in the infrastructure
   repo. THAT WAS NEVER EXECUTED, and the shipped design supersedes it: the assets stay here and the
   module stays declared.)"

The corpus describes the OLDER state while citing the NEWER ref: at checkout 4ce8ece5, storage.tf:2
still reads '# Deletion guards (app v9.0 "support-in-app", M903)' and :23 still reads "That is what
M903's moved blocks are for..." — but that ref also has a 100-line main.tf with a live module "storage"
block and service_desired_count = 1 at :19, contradicting the same row's "18 lines and declares no
service block at all." THERE IS NO REF AT WHICH THE WHOLE ROW IS TRUE. Sec5 rule 31 — a plan reference
that expired and was carried forward by citation rather than re-measurement. Confidence: high.

## B3 — hiring.md:81 — manager.go:485 lands on a different function's closing brace

Anchor (:80-82): "...as do organization/manager.go:448 (a forced Clerk membership is created with role
candidate instead of member) and :485 + siminvitationlink.go:62 (both hard-error "organization is not
hiring"...)"

Graded at app b948604f (no ref named):
- manager.go:485 is `}` — the CLOSING BRACE of forceUserToOrganizationInClerk (declared :446). That
  function produces no such error.
- The error is at manager.go:536-537, inside ForceUserToOrganization (declared :527), guarded by
  `if !org.IsHiring {` at :535. (grep -rn "organization is not hiring" over app returns exactly those,
  plus siminvitationlink.go:63 and two test lines.)
- manager.go:448 is a BLANK LINE; the construct described is :449-452.
- siminvitationlink.go:62 is correct.

Both anchors WERE right at app 5ba17044 — the ref in this document's top banner (hiring.md:17). That
banner does not rescue them: Sec5 rule 33, a pin exempts a claim, never a neighbourhood, and this
sentence sits ~63 lines below in a different section. More decisively the file is INTERNALLY
INCONSISTENT — every other app anchor in it resolves at the checkout (I opened each: intelligence.go
:1700/:1820/:1844/:1846/:885-886; resolver_queries.go :1034/:1035/:1053/:1080/:1085/:1089;
resolver_cms_queries.go :95/:99-103; job_simulation_session.go :39/:45; enum/jobsimulation.go :29-35;
ent/jobsimulationsession.go :181-186; graph.go :129546-129554 and :129392-129400; anticheat_result.go
:24; askengine/registry.go:192). So the doc was re-anchored to a newer ref and this pair was missed.

Confidence: medium. Hesitated because this is line drift on an anchor true at a ref the file mentions.
Booked as BLOCKER because :485 does not merely drift — it names a DIFFERENT FUNCTION'S closing brace
(the "resolves but names the wrong construct" class), the sentence asserts a specific behaviour at a
line containing none, and the pin defence is unavailable given the rest of the file is current.

## Minors

- graphql-wundergraph.md:86 — self-citation drift: ":174-176 of this same doc already said
  localhost:5050 refuses the connection". :174-176 is now the Dependencies bullet; the 5050 sentence is
  at :192. Navigation only; the claim it supports is true (Sec5 rule 34).
- graphql-wundergraph.md:133 — same class: "as :84 describes". :84 is the Ports bullet; the rebuild
  passage is :115-116.
- hiring.md:78-79 — "PrivateJobSimulations branches ... (resolver_cms_queries.go:95,210,258,295)".
  Only :95 is in PrivateJobSimulations (:89); :210 is RecommendedUserSimulations (:190), :258
  SimilarSimulations (:237), :295 SearchSimulations (:278). All four are real GetOrganizationIsHiring
  sites; only the resolver name over-reaches.
- frontend_architecture.md:39 — headline counts exactly right, but the colon-introduced enumeration
  covers 26 of 29 non-test sites; unnamed: packages/ui/.../TestRunNetwork.tsx,
  packages/graphql/src/hooks/user/useManageSubscription.tsx,
  apps/web/src/app/(unauthenticated)/reminders/[token]/unsubscribe/page.tsx.
- studio-room.md:345-356 — the requirements.txt reproduction reorders the file: mistralai is listed 3rd
  but sits on line 8, after jinja2. Set and annotations correct.
- hiring.md:192 — read-path row 5 anchors "row_number() ORDER BY score DESC" at intelligence.go
  :1728-1735; that range holds the CALL (:1733), the SQL literal is at :2158-2169. Defensible as a
  call-site anchor; noted only because the quoted SQL isn't in the cited range.

## Positively cleared — ENUMERATED (23 items, heads only)

All ENUMERATED unless marked. (1) studio-room gen.py 9 add_argument at :484-492 + parse_known_args +
validate_blueprint_exclusivity :241-271 with the exact 10-token whitelist + translate_legacy_blueprint
:212-238. (2) mistral over app/studio/ -> exactly 1 hit (requirements.txt:8); ai.py:705-708 registry
{openai,azure,anthropic}; Go-side Mistral OCR-only. (3) worker.go:29 Concurrency 5, :31-33
{AiVideoQueue:7, StudioQueue:3}; python:3.11-slim in both Dockerfiles; exporter.py:514-548;
max_tokens 4000; GenMode 5 members DEFAULT=EXECUTION. (4) supergraph `- name:` counted per commit:
749dc86~1->5, 749dc86->4, 7c17e63->3, 915da06->1; --name-status marks BOTH cms and jobsimulation SDLs
D while the subject really reads "2->1". (5) five profile tokens derived from compose @0c91421d; floor
exactly {postgresql,redis,sentinel}; Makefile:10 PROFILE ?= core; 5050 -> 0. (6) "gh release download"
-> 1 at :9. (7) backend.graphqls has Mutation :4053, Query :4912, NO Subscription. (8) terraform
main.tf:20 desired_count 1; router 0.275.0; wgc 0.104.0; node:22.11-alpine; federation =2.3.2; the
package.json misspelling "graphql-wundegraph" real. (9) gqlauthz.go:186 exact. (10) hiring DDL
20260722104506.sql:3-25 — NOT NULL without DEFAULT is exactly {owner_id:6, sim_id:7, sim_type:10,
token:13}. (11) grep -in hiring on resolver_queries.go -> 4 all sim-TYPE; on intelligence.go -> 0.
(12) hiring_scope_test.go:81-95 exactly 8 entries, persona the sole positiveControl:false. (13) enum 5
members + SDL :14/:128 same five; the "nothing catches it" chain intact. (14) 20260729133514.sql:58 is
literally "-- 5. Drop the mirrors."; SET "score" -> 0 (control 39 files contain SET ); repos.yml 6 at
0dab54d, 4 at 0c91421. (15) next-web anchors: useGetClerkOrganization.tsx:20-21 exact;
useNavbarSections.tsx:459-466 sitting unconditionally in trackVerify -> "SURVIVES, only relabeled"
right; find apps -type d -name '(.)*' -> 0; the two simulationScoreColumn.tsx copies diff IDENTICAL.
(16) rext persona_write.go:91 exact; sessionCols() :152-159 includes "token" at :155; hiring_config.go
:99. (17) STORAGE_RPC_ADDR -> 0 in platform @0c91421d; at app 2035f9a4 exactly 3 Go hits ALL COMMENTS
(main.go:504, jobsimwiring/wiring.go:101, storagens/callsites_test.go:189); 0 *_test.go at both refs
while Dockerfile:18 is `RUN go test -v ./...`; prod split at :139 — jobsim 6092c6d2 M810, cms
terraform main.tf:39 desired_count 0, 6efa1d5 the ECR-decommissioned workflow deletion. (18)
clerk/events/events.go:165-235 exactly 12 case arms. (19) colony/authn: 129 files in app, 0 in
sentinel. (20) @clerk/nextjs ^6.39.2 aligned across all four web apps. (21) metabase/route.ts:35 bare
orgRole; 8 locales; ClerkSignInForm.tsx:49-53 aborts on any non-complete status. (22) compose :141
@0c91421 and :226 @0dab54d both `profiles: [studio-desk, all]`; repos.yml 4 at 0c91421, 9 at 2adcf71.
(23) NEXT_PUBLIC_BACKEND_API_URL enumerated not summed: 33 occurrences/24 files, minus 3 test files
(4 occurrences) = exactly 29 across 21 non-test files; at origin/main 8297c684 it is 31/22, so
"origin/main" has not gone stale.

RE-DERIVED only (weaker): clerk-integration.md:103's "thirteen minor versions apart" (19-6=13).

## What I could not settle

- graphql-wundergraph.md:8 "ARCHIVED on GitHub, 2026-07-30" — GitHub API property, absent from the
  clone; no network calls run. Settled by `gh api repos/anthropos-work/graphql-wundergraph --jq
  '.archived, .archived_at'`.
- graphql-wundergraph.md:237-238 — route registration lives in `colony`, a private Go module NOT in
  the clone set. Settled by a colony clone at the go.mod pin, or two curls against a running stack.
- hiring.md:153-158 — "87 real SIMULATION_TYPE_HIRING sims", "0 job_position rows". Properties of the
  snapshot cache / live DB. Settled by `stacksnap status` + a count, or /db-query.
- hiring.md:378-417 — M227/M228 render-gate numbers (5/5, cohorts 8/8/9/9/8, scores 27-100, p95 1.27s,
  4/4 flake). Live-stack measurements; instrument forbids state changes.
- storage.md:156 "(none are set in platform/.env)" — .env is git-ignored and a secret surface; I
  deliberately did not open it. IN TENSION with compose's own comment at docker-compose.yml:74: "AWS
  credentials come from .env — the same ones the standalone storage container used." One of the two is
  stale. Settled values-blind by `grep -c '^AWS_ACCESS_KEY_ID=' platform/.env` or /stack-secrets check.
- storage.md:29 parenthetical "storage's own 18-line main.go" — I sized terraform/main.tf (18 lines at
  9f8cb53), not the repo-root main.go; the coincidence makes me want the direct measurement before
  clearing or booking.
- Whether B3's two anchors are the only survivors of hiring.md's re-anchoring pass. I opened every app
  anchor in the file and found only these two stale, but did not machine-derive the set. Settled by
  anchor_construct_guard over hiring.md — and per Sec5 rule 41, only after git fetch in every clone.
