# Seat A — reading #17

Refs used (re-verified at open, matching the addendum sheet):
`app` checkout `b948604f` / origin/main `2035f9a4` · `platform` `0c91421d` (= origin/main) ·
`next-web-app` `bb3313bc` · `sentinel` `88bc5592` · `storage` `4ce8ece5` · `messenger` `fa47850d` ·
`cms` `ca50c817` · `graphql-wundergraph` `60c229f3` · `roadrunner` `87d8d443` · `jobsimulation` `462343b0` ·
`studio-desk` `14a5442a` · `ant-academy` `9c3843cd` · `rosetta-extensions` (authoring) `6130bfd8`.

Grading rule applied: a claim is settled at the ref its own **block** names (markdown cell / wrapped
sentence); a block naming no ref is graded at the checkout, except that a block asserting currency about
`app` is graded at `origin/main` (the checkout is 93 behind and the corpus's own `app` blocks pin
`2035f9a` / `9d00a313` for the v9.0 material).

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| corpus/architecture/external_services.md | 855 | yes |
| corpus/services/cms.md | 307 | yes |
| corpus/services/backend.md | 306 | yes |
| corpus/services/ai-labs.md | 156 | yes |
| corpus/services/academy-backend.md | 147 | yes |
| corpus/services/skiller.md | 69 | yes |
| corpus/services/TEMPLATE.md | 46 | yes |
| (scope read, not assigned) corpus/ops/platform-alignment.md §5 | 2139 (file) | §5 in full, lines 249–1362 |

---

## Blockers

### B1 — the deleted router's `depends_on` list is wrong at every ref it could name

- **Anchor:** `corpus/architecture/external_services.md:411-414` —
  > "From `docker-compose.yml` *before the drop*, the gateway `depends_on`:
  > - backend
  > - storage"

  and `:415` — "It starts after **these services** have reported 'started'…"

- **Why it is false:** I enumerated the `graphql:` service's `depends_on` block across **every commit that
  ever touched `platform/docker-compose.yml`** (`git log --format=%h -- docker-compose.yml`, then reading
  the block at each). The distinct sets, newest first, are:

  | first commit with that set | `depends_on` |
  |---|---|
  | `a4db680` | backend, jobsimulation, cms, storage |
  | `21429b7` | backend, jobsimulation, cms, skillpath, storage |
  | `781b03c` | backend, skiller, jobsimulation, cms, skillpath, storage |
  | `fdfa189` | … + chronos |
  | `c17cc9a` | … + intelligence |
  | `70c4136` / `48f87d7` / `0551001` / `e42e851` | earlier, smaller sets |

  **`{backend, storage}` is not among them — it never existed.** And the two readings of *"before the
  drop"* both fail differently:
  - `b56d731^` (last ref where the router was still a live compose member) → **four** deps:
    `git show b56d731^:docker-compose.yml` shows `backend`, `jobsimulation`, `cms`, `storage`, each
    `condition: service_started`.
  - `360efd4^` (immediately before the block was deleted) → **no `depends_on` at all**. `b56d731` had
    already stripped it and parked the block behind `profiles: [wundergraph-deprecated]`; the block at
    `360efd4^` runs `networks: - app-network` straight into `profiles: [wundergraph-deprecated]`.

  So the sentence at `:415` ("*starts after these services*") is also unsupportable for the state the
  section claims to describe.
- **Confidence:** high

### B2 — "`mistralai` … imported nowhere / the string's only occurrence in the whole repo" is refuted by the repo

- **Anchor:** `corpus/services/cms.md:95` —
  > "**There is no Mistral path in the Python engine.** `mistralai` is declared in `requirements.txt` and
  > imported nowhere — that declaration is the string's *only* occurrence in the whole repo."

- **Why it is false:** measured case-sensitively in **both** copies of the studio-room tree:
  - `stack-demo/app/studio/tools/pdf2md.py:24` — `from mistralai import Mistral`
  - `stack-demo/app/studio/requirements.txt:8` — `mistralai`
  - identical two hits in `stack-demo/cms/studio/` (`tools/pdf2md.py:24`, `requirements.txt:8`)

  So (a) the package **is** imported, and (b) the requirements line is **not** the only occurrence — it is
  one of two. The surrounding code is a working Mistral OCR path: `pdf2md.py:96` `Mistral(api_key=api_key)`,
  `:127` `model="mistral-ocr-latest"`, `:216` `load_mistral_api_key()`; and `tools/r3.py:194-195` *requires*
  `MISTRAL_API_KEY` and hard-errors without it before invoking the PDF→Markdown conversion. The claim's
  headline ("no Mistral path in the Python engine") is defensible only if "engine" is narrowed to
  `gen.py`'s provider registry — but the two conjuncts that follow it, which are what a reader would
  re-derive, are both measurably wrong.

  This matters because the same paragraph is the corpus's basis for routing Mistral as "Go-side, OCR-only",
  which `external_services.md:541`/`:573-575` repeats. The Go-side half is correct
  (`app/internal/cms/studio/markdownManager.go:11` imports `github.com/anthropos-work/ai/mistral`, `:19`
  `mistral.NewMistral(nil, os.Getenv("MISTRAL_API_KEY"))` at the checkout); the "and nowhere else" half is not.
- **Confidence:** high

### B3 — the `python-docx` retraction over-reaches into a false statement about the tree

- **Anchor:** `corpus/services/cms.md:145-149` —
  > "**`python-docx` is NOT among them** and never was: … **(The only `.docx` in the tree is a filename in
  > an authoring guideline and an asset-example README — no dependency.)**"

- **Why it is false:** the load-bearing half is right — neither `cms/studio/requirements.txt` nor
  `app/studio/requirements.txt` contains `python-docx` (both are byte-identical, 9 packages, verified with
  `cmp`). The parenthetical is not. Literal `.docx` in `app/studio` (excluding `.git`):
  - `tools/any2pdf.py:664` `'.docx': convert_docx_to_pdf,`
  - `tools/any2pdf.py:742` supported-formats banner
  - `tools/any2pdf.py:752` `if ext in ['.docx', '.doc']:`
  - `agents/simulation/guidelines.py:48` (the guideline the corpus names)
  - `knowledge/development/asset-examples/README.md:59` (the README the corpus names)

  **Three of five sit outside the two sites the sentence enumerates**, so "the only" is false. And "no
  dependency" is false in substance too: `tools/any2pdf.py:38` declares `'docx': 'python-docx'` in its
  dependency map, `:78-85` `pip install python-docx` **at runtime**, and `:709`/`:755` list it among the
  packages the tool asks the operator to install.
- **Confidence:** high. I hesitated because the sentence's *primary* claim (requirements.txt) is correct
  and the defect is in a parenthetical — but this is exactly the rule-20 over-reaching-retraction class
  (*"does not exist now"* → *"never existed / nowhere else"*), and a reader re-deriving the parenthetical
  finds it wrong within one grep, which then discredits the correct half.

### B4 — the corpus states two different production RPC addresses for the same `app` mux

- **Anchors (same file, two passages, plus two siblings):**
  - `corpus/services/backend.md:112` — "In prod terraform the address is **`http://backend:8081`**."
  - `corpus/services/backend.md:229` — "In production terraform the re-pointed pair is at
    **`http://backend.internal.anthropos:8081`**."
  - `corpus/services/skiller.md:19` — "`skiller_rpc_addr = **http://backend:8081**` in production terraform"
  - `corpus/services/cms.md:55` — "**`http://backend.internal.anthropos:8081`** in production."
    (and `cms.md:196` — "Production terraform still names `http://backend.internal.anthropos:8081`.")

- **Why it is a finding:** the two forms are asserted about the *same* target — the production address of
  `app`'s single Connect-RPC mux — and they are different hostnames. `backend.md` contradicts **itself**
  117 lines apart. This is the self-contradiction class the briefing says counts even when I cannot
  adjudicate.

  I could not settle which side is right from the clone set, and I checked properly rather than assuming:
  `skiller_rpc_addr` / `cms_rpc_address` / `jobsimulation_rpc_addr` / `backend_users_rpc_addr` are declared
  as **typed terraform variables with no defaults** in `cms/terraform/variables.tf:76`,
  `jobsimulation/terraform/variables.tf:163`, `messenger/terraform/variables.tf:77`,
  `app/terraform/variables.tf:197` — the *values* are supplied by `infrastructure`, which has never been in
  a clone set. The only production DNS evidence anywhere in the clones points at the longer form:
  `graphql-wundergraph/supergraph-config-prod.yaml:6` routes to
  `http://backend.internal.anthropos:8080/graphql/query`. That is suggestive, not decisive (a different
  port and a different consumer), so I book the contradiction rather than a correction.
- **Confidence:** high that the contradiction is real and live; medium on which side is correct.

---

## Minors

- `corpus/architecture/external_services.md:206-211` — the `--local-content` re-point anchors have drifted
  in the grading copy. At the **authoring** `rosetta-extensions` (`6130bfd8`, the ref the addendum makes
  authoritative) `DIRECTUS_DATA_CONSUMERS` is at `gen_injected_override.py:**86**` (cited `:84`) and the
  re-point is at `:**698**` (cited `:669-670`, which lands inside a CORS-origins comment). Both anchors
  resolve *exactly* at the in-tree pinned consumption clone `stack-demo/rosetta-extensions` (`:84` / `:669`),
  so this is post-hoc drift, not a wrong claim — the tuple really is `("cms", "backend")` and the mechanism
  is described correctly. `test_injection.py:1109` (`test_backend_the_actual_reader_is_repointed`) is
  exact. *I hesitated between MINOR and BLOCKER:* the instrument lists "wrong line anchors that point at
  the wrong construct" under BLOCKER, but here the construct is correct at a real checked-in witness and
  only the clone-selection rule makes it wrong.
- `corpus/services/cms.md:211` — "Consistent with the **Data** bullet, **:28-31** above." The Data bullet is
  at `:44-47`; `:28-31` is the `CMS_RPC_ADDR`/M809 text. Navigational cross-reference, target named in words.
- `corpus/services/cms.md:229` — "see the **Studio** bullet, **:52-53** in the banner at the top of this
  doc." The Studio bullet is at `:70-71`; `:52-53` is the RPC bullet.
- `corpus/services/academy-backend.md:15` — "(Consistent with **:74-76** below.)" `:73-75` is the
  *Certificate minting* bullet; the passage that actually corroborates the endpoint claim is `:80-83`
  ("The frontend reaches them at (`NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT`)").
- `corpus/architecture/external_services.md:14` — the PM summary still reads "GraphQL/Wundergraph — Unifies
  **all backend services** into a single API." Since `915da06` it composes exactly one subgraph. The same
  line does flag "Prod-only since platform `2adcf71`", so the damage is limited to the verb.
- `corpus/architecture/external_services.md:543`, `:608` — `config_template.ini:30-31` is cited without its
  directory; the file is `app/studio/configs/config_template.ini` (the lines themselves are exact:
  `:30 OPENAI_API_KEY`, `:31 OPENAI_ENDPOINT = https://api.openai.com/v1/chat/completions`).

---

## Positively cleared (audited zeros)

**ENUMERATED** — predicate re-derived from source independently, cardinality compared:

1. **Supergraph subgraph ladder** (`external_services.md:364-377`, `backend.md:113-124`, `cms.md:59-64`).
   Re-derived by counting `^  - name:` in `supergraph-config-prod.yaml` at each named sha:
   `749dc86^`=**5** (backend, skiller, jobsimulation, cms, skillpath) · `749dc86`=**4** · `7c17e63`=**3** ·
   `915da06`=**1** · HEAD=**1**. Dates confirm (2026-06-24 / 2026-07-21 / 2026-07-29). `915da06`'s
   `--name-status` shows `D schemas/cms.graphqls` **and** `D schemas/jobsimulation.graphqls`, and its own
   subject really does say "supergraph 2→1" — the corpus's caution is correct.
2. **No subscriptions in the supergraph** (`external_services.md:378`). `grep -rn 'sse\|subscription' *.yaml`
   → rc **1** (positive control `grep -rln backend *.yaml` → all three files). `915da06~1` still reads
   `protocol: "sse_post"` at `:13`. `git log -S 'protocol: "ws"'` → empty; `-S sse_post` → **5** commits.
   `bba862f` (2026-02-25) `merge-base --is-ancestor … HEAD` → rc **1**, `branch -a --contains` names only
   `remotes/origin/feat/use-web-socket`. `schemas/backend.graphqls`: `type Mutation` `:4053`,
   `type Query` `:4912`, **no** `type Subscription`; all **6** `Subscription` hits are Stripe/plan names.
3. **Compose service set at `0c91421`** (`external_services.md:168-175`, `:296-297`, `cms.md:53-54`,
   `backend.md:19`). `docker-compose.yml` declares **5** (`sentinel`, `backend`, `studio-desk`,
   `next-web-app`, `gotenberg`); `common.yml` adds `postgresql` + `redis` → **7** effective. At `0dab54d`:
   **8** + 2 = **10**. `repos.yml` at HEAD has **4** entries (app, sentinel, next-web-app, studio-desk).
   Only 4 services carry a `profiles:` key (`:110`, `:141`, `:168`, `:183`), so the profile-less floor is
   `{sentinel, postgresql, redis}` and no `graphql` token appears in any `profiles:` key at `0dab54d` or HEAD.
4. **`d11a403` / `838d907` deltas.** `d11a403` (2026-08-03) removed `jobsimulation`, `cms`, `roadrunner`
   from compose **and** their `repos.yml` entries (before/after enumerated). `838d907` (2026-08-05, merged
   `0c91421`) removed `storage`, `messenger`, `customerio-sync`. Matches every citation of them across all
   five service docs.
5. **`STORAGE_RPC_ADDR` at the refs the corpus pins** (`backend.md:242` @ `2035f9a`,
   `academy-backend.md:55-58` @ `9d00a313`). `git grep` at each ref returns exactly **3** occurrences, **all
   comments** (`internal/jobsimwiring/wiring.go:101`, `internal/storagens/callsites_test.go:189`,
   `main.go:504`/`:451`), zero reads. Constructors: `main.go:524`/`:525` @ `2035f9a` and `:471`/`:472` @
   `9d00a313` — both exact, and both differ from the checkout, where `main.go:524` is still an
   `os.Getenv("STORAGE_RPC_ADDR")`. The corpus pins are what make these true.
6. **`app` Redis-stream producer/consumer split** (`backend.md:252`). All 5 `pubsub.NewPublisher` sites
   enumerated: `main.go:287` (serviceName = `backend`), `:637` `SKILLPATH_STREAM`, `:1039` `CMS_STREAM`,
   `jobsimwiring/wiring.go:127` `AI_USAGE_STREAM`, `:180` `JOBSIMULATION_STREAM`. `SKILLER_STREAM` occurs
   **once** in Go, `main.go:1276`, and it is an `AddSubscriber`. The "4 of 5, skiller is the exception"
   split is exact.
7. **RPC mux registrations** (`backend.md:66`). `main.go:1185-1228`: `:1187` Users, `:1188` Organizations,
   `:1196` Skiller, `:1204` JobSimulation, `:1228` LabSession — **five unconditional** — plus
   `if cmsRPCServer != nil` at `:1212-1214`. **`SkillPathSessionService`: 0 occurrences in Go** (rc 1;
   positive control `JobSimulationService` → 5 files), and no `skillpath…v1connect` import (rc 1). `app`'s
   own docs *do* still list it, exactly where the corpus says: `CLAUDE.md:109` and
   `knowledge/architecture.md:28` @ `2035f9a`.
8. **13 `ai_readiness_*` tables** (`backend.md:219`). 13 Ent schema files; 13 distinct base table names in
   the migrations (the four the corpus names as commonly-omitted — `recommendations`, `email_overrides`,
   `notification_logs`, `notification_optouts` — are all present, as are `live_snapshots` and
   `text_translations`).
9. **8 `ContentLanguage`s** (`skiller.md:57-58`). `validTranslationLanguages` =
   {english, italian, spanish, french, german, dutch, japanese, portuguese}; the GraphQL
   `enum ContentLanguage` (assignments.graphqls:44-53) has the identical 8.
10. **Taxonomy figures** (`external_services.md:659-672`, `backend.md:63`, `skiller.md:43-52`). Read the
    manifest directly: `public_only: true`, `predicate: org-null`, `captured_at 2026-06-29`,
    `public.skills` **42,790**, `public.job_roles` **22,470**, `skill_embeddings` **42,790**,
    `job_role_embeddings` **18,919** — all four exact, and the org-null filters are on the rows.
11. **AI env-var occurrence counts** (`external_services.md:640-645`, pinned `5ba17044`). `git grep -oh`
    at that ref: `OPENAI_KEY` 5, `ANTHROPIC_API_KEY` 26, `AZURE_OPENAI_KEY` 13,
    `AZURE_OPENAI_ENDPOINT_URL` 13, `OPENAI_ORG_ID`/`AZURE_OPENAI_ENDPOINT`/`AZURE_OPENAI_DEPLOYMENT` 0,
    `OPENAI_API_KEY` 2. All eight match, and identically at HEAD.
12. **The studio `azure`-pinned config claim** (`external_services.md:591-592`, `:611-615`, `cms.md:95`).
    Enumerated: **30** `*_AI_*_MODEL` lines across the three `configs/*.ini`, and every one of the 30
    distinct values begins `azure`. `gen.py:41-53` overrides only `*_API_KEY`/`*_ENDPOINT` — never the
    service selector. `gen.py:484-492` registers exactly **nine** `add_argument` calls and the file's total
    `add_argument` count is **9**; `parse_argument` at `:18-28` uses `parse_known_args` and merges leftovers.
13. **cms compose ports at `2adcf71`** (`cms.md:113`). `8090:8090` `:154`, `8091:8091` `:155`, `PORT=8090`
    `:169`, `RPC_PORT=8091` `:173` — all four exact at that ref.
14. **Certificate id alphabet** (`academy-backend.md:73-75`). `certIDRegex = ^AAS-\d{4}-[A-HJKMNP-TV-Z2-9]{6}$`
    → 8+4+5+5+8 = **30** symbols, ambiguity-free, `crypto/rand` at `certificate.go:5`/`:171`.
15. **Credit-ledger constants** (`ai-labs.md:71-83`). `creditCost` = build 5 / refine 1 / translate 1;
    `purchasePackages` = starter 50 / team 200 / scale 500; `PricePerCreditUSD = 0.45` (`cost.go:142`);
    `MarginMarkup = 1.40` (`:133`); `DefaultSeedBalance int64 = 500` (`:231` — my first grep pattern
    `DefaultSeedBalance =` missed it because the declaration carries a type; §5 rule 3, caught by re-running).
    Partial-unique `ref_id IS NOT NULL` at `credit_transaction.go:119`. `checkout.session.completed`: rc 1
    at **both** the checkout and `origin/main`; the handled set at `api.go:319-327` is exactly
    `customer.created` + `customer.subscription.{created,updated,deleted}`.
16. **LiveKit agent names** (`external_services.md:695`). `calls/livekit.go:110` bare `anthropos-agent`,
    `:115` `-chain`, `:120` bare, `:126` `fmt.Sprintf("anthropos-agent-%s", *location)`. `anthropos-agent-eu`
    → 0 hits anywhere.
17. **Academy surface** (`academy-backend.md`). 11 academy Ent schema files; the plural table names are
    the ones in the migrations; `academy_embedding_refresh` registered as `"0 3 * * *"`
    (`internal/worker/worker.go:158`); `/content/catalog.json` (`content.go:23`), `/content/admin/*` +
    `ACADEMY_CONTENT_API_TOKEN` (`backend.go:286-287`); all six named test files exist; every named GraphQL
    query/mutation exists in `academy.graphqls`; `cmd/academy-seed`, `academyImport`, `coursebuilder-e2e`,
    `coursebuilder-liverun` all present.
18. **AI-routing mechanics** (`external_services.md:553-615`). `internal/jobsimulation/ai/ai.go`: vendor
    consts `:30-33`, `getClient` `:259-289`, Azure-EU-default + `flag_use_azure_us` `:264-276`,
    AnthropicAws≡Anthropic `:280-283`, `isThrottlingError` `:130-141`, the 429→`Openai` overrides at
    `:150-155` / `:296-302` / `:326`, `openai.NewOpenAI` `:80`. The caller-side default:
    `collections/jobsimulation.go:905` `AIVendor *AIVendor`, `:1302` `aiVendor := simulation.Openai`,
    `simulator/ai/ai.go:58-59` and its `default:` arm at `:114-115`. Every anchor names the construct claimed.
19. **Clerk SDK-per-app** (`external_services.md:69`). `@clerk/nextjs` in web/hiring/integration +
    `ant-academy/code`; `@clerk/clerk-expo` in `apps/mobile`; `@clerk/clerk-js` + `@clerk/express` in
    `studio-desk/package.json:27-28`.
20. **Historical compose-build eras** (`external_services.md:417-431`). `2c85211^` has no `dockerfile:` key;
    `2c85211` adds `dockerfile: Dockerfile.dev`; `1e8e754:6-8` reads
    `dockerfile: graphql-wundergraph/Dockerfile.dev`; `b56d731` still has `  graphql:` at `:22`; `360efd4`
    has none, `  backend:` at `:28`. `graphql-wundergraph/CLAUDE.md:39` really does assert the opposite,
    and `60c229f` really is titled "correct the compose build path" (2026-07-30).
21. **The retracted Directus compose service** (`external_services.md:143-148`). `a2a3ee6^` really does
    carry `:384 image: directus/directus:10.10.1`, `:386 8055:8055`, `:409 ADMIN_PASSWORD=password`;
    `a2a3ee6` is 2026-02-27; `git log -S "admin@example.com"` is empty — exactly the corpus's own carve-out.
22. **Dockerfile.dev composition** (`external_services.md:435-449`). One schema `COPY` at `:18`, the
    cms/jobsim comment at `:19-20`, one `awk` at `:23`, `wgc@0.104.0` at `:7`, runtime image
    `ghcr.io/wundergraph/cosmo/router:0.275.0` at `:29`. Router port claims: `terraform/locals.tf:8`
    `port = 8080`, `terraform/main.tf:20` `service_desired_count = 1`, `:48-49` container/host 8080,
    `config.prod.yaml:5` `listen_addr: 0.0.0.0:8080`; `5050:8080` appears only as the deleted local host
    mapping.

**RE-DERIVED only** (weaker — the document showed the derivation and I recomputed it):
- `external_services.md:175` / `cms.md:8`,`:72-84` / `backend.md:36-56` — the cms-vs-jobsimulation M810
  asymmetry. `cms/terraform/main.tf:39` = `service_desired_count = 0` in a **191**-line module; `6efa1d5`
  (2026-08-04) really does delete `.github/workflows/build-production.yml` under that subject;
  `6092c6d2` really does leave `terraform/main.tf` with no `module "jobsimulation"` block (only the
  `:15-22` comment recording the deletion and `:24-40` listing what survives). I did **not** independently
  enumerate the production `infrastructure` state, which is the corpus's own stated limit.

---

## What I could not settle

- **`graphql-wundergraph` is "ARCHIVED on GitHub (2026-07-30)"** (`external_services.md:3`). No network
  access; the clone shows no archive marker. Would be settled by
  `gh repo view anthropos-work/graphql-wundergraph --json isArchived`.
- **The production values of `skiller_rpc_addr` / `cms_rpc_address` / `jobsimulation_rpc_addr`** (B4). The
  variables exist with **no defaults** in four clones; the values live in
  `infrastructure/terraform/production/services.tf`, which has never been in a clone set.
- **`anthropos-work/AI-Labs` exists and is the live control plane** (`ai-labs.md:4-6`). Repo not in the
  clone set; the `app`-side consumer (`internal/labs/labsapi/client.go`, default `:7070`, bearer
  `LABS_API_PLATFORM_TOKEN`) is exactly as described, which is consistent with but not proof of the claim.
- **`app knowledge/plan/releases/06.00-shared-purse/`, milestones M600–M607** (`ai-labs.md:16`).
  **Not checked by design** — HARD BAR 1 forbids reading, grepping or listing anything under
  `knowledge/plan/**`, and I did not distinguish repos when applying it. The *observable* half of the same
  sentence is verified: no `checkout.session.completed` handler at either `app` ref, and
  `POST /credits/purchase` is gone (`internal/web/backend/credits/handler.go:12` records the Wave-13 removal).
- **Live-measurement anecdotes** — "96 all-403 lines in `backend`'s log" (`external_services.md:217`,
  `cms.md:106`), the 2026-07-08 prod row counts in `backend.md:97-99` (`43,584` total skills etc.), and the
  2026-08-05 `docker compose ps` return codes (`external_services.md:827-833`). These are dated
  observations against running systems, not against a ref; only re-running them settles them.
- **`AI_PROVIDER_CHAIN`, default `azure-openai,openai`** (`external_services.md:675`). There is no literal
  default string: `parseProviderChain()` (`studio-desk/src/services/ai/config.ts:123-134`) returns `null`
  when the var is unset and the loader then documents "fall back to legacy detection (Azure first if
  configured, then OpenAI)" (`:140-141`). The corpus's phrasing describes the *effective* order correctly,
  so I did not book it; a stricter reading would call the word "default" imprecise.
