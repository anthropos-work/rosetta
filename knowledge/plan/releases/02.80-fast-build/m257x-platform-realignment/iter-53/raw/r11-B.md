# Seat B — reading #11

Ground truth verified at the briefed shas before any claim was booked:
`app 5ba17044` · `app/studio aeec036a` · `platform 2adcf714` · `next-web-app bb3313bc` ·
`sentinel 88bc5592` · `storage 4ce8ece5` · `messenger fa47850d` · `cms ca50c817` ·
`graphql-wundergraph 60c229f3` · `roadrunner 87d8d443` · `jobsimulation 462343b0` ·
`studio-desk 14a5442a` · `ant-academy 9c3843cd` — all matched.
`corpus/ops/platform-alignment.md` §5 (lines 240–829) read in full before starting.

## Positive controls

| file | `wc -l` | read in full? |
|---|---|---|
| `corpus/services/ai-readiness.md` | 645 | yes |
| `corpus/architecture/ai_architecture.md` | 302 | yes |
| `corpus/services/graphql-wundergraph.md` | 265 | yes |
| `corpus/services/ai-labs.md` | 156 | yes |
| `corpus/services/messenger.md` | 128 | yes |
| `corpus/services/customerio-sync.md` | 75 | yes |
| `corpus/architecture/README.md` | 38 | yes |

---

## Blockers

### B1 — "Both recordings are stored in S3" contradicts `media-substrate-spec.md`'s "never in prod S3"; ground truth backs *this* file, so the twin is the false one

- **Anchor:** `corpus/architecture/ai_architecture.md:225` —
  > "Both recordings are stored in S3 and linked to the simulation session."

  Twin anchor: `corpus/ops/demo/media-substrate-spec.md:25` (section heading) and `:33-35` —
  > "### 1.1 The recorded call → a Bunny.net CDN reference (NOT an S3 blob)"
  > "**The bytes live on Bunny's CDN, never in the platform DB and never in prod S3** — which is why
  > S3 read access (the old `DEF-M10-01`) is neither necessary nor sufficient to exhibit a recording."

- **Why it is a finding:** these two are flatly incompatible about the **same artifact** (the Chime
  composited MP4). Measured against `app @ 5ba17044`:
  - `app/internal/jobsimulation/recording/chime.go:38` builds `destinationArn` =
    `fmt.Sprintf("arn:aws:s3:::%s", bucketName)`;
  - `:188` `SinkType: types.MediaPipelineSinkTypeS3Bucket` and `:264-265`
    `Type: types.ConcatenationSinkTypeS3Bucket` / `S3BucketSinkConfiguration` — the Chime media
    pipeline *and* the concatenation sink both write to S3;
  - `:341-359` `getMediaFromS3` does `s3Client.GetObject(Bucket: r.bucketName, Key:
    "concatenated/composited-video/<mediaPipelineId>.mp4")` — the composited MP4 is **read back out of
    prod S3**;
  - `:317-339` `GetMeetingMedia` is the only caller path, invoked from
    `internal/jobsimulation/simulator/manager/manager.go:3295`, immediately before
    `SaveBunnyVideoId` at `:3313`. So S3 is the *source* the Bunny upload reads from.
  - There is **no delete**: `grep -rn "DeleteObject" internal/jobsimulation/recording/` returns 0
    (positive control: `grep -rn "GetObject"` in the same tree returns the `:349` hit), and no
    lifecycle rule is expressed in the repo. The object persists in prod S3.

  So `ai_architecture.md:225` is **correct** and `media-substrate-spec.md:33-35`'s absolute *"never in
  prod S3"* is **false** — the repair belongs in the twin, not here. Booking it against my file because
  the contradiction is live in the corpus and a reader landing on either page is misled by the other.
  (`media-substrate-spec.md` is presumably outside my read scope; I opened only lines 23–50 of it to
  quote the twin, plus a `grep` for the two terms.)
- **Confidence:** high (on the ground-truth fact and on the contradiction; medium only on *which
  file the repairer should touch* — the honest fix may be to qualify both: the bytes are in prod S3,
  the *playable* asset a viewer fetches is on Bunny's CDN).

### B2 — `graphql-wundergraph.md:81` self-cites `:174-176`, which is a blank line, a heading and a blank line

- **Anchor:** `corpus/services/graphql-wundergraph.md:80-82` —
  > "**There is no `5050` at platform HEAD** — `grep -c 5050 docker-compose.yml` returns 0. The
  > host-`5050` mapping was published here in the present tense while **`:174-176` of this same doc**
  > already said `localhost:5050` refuses the connection; corrected M257x iter-46…"
- **Why it is false:** measured in the file itself —
  `:174` = blank, `:175` = `### Run in Docker — **no longer possible; there is no service to start**`,
  `:176` = blank. The sentence it points at (`"…has nothing to tail and \`http://localhost:5050\`
  refuses the connection."`) is at **`:178`**. The citation that is supposed to prove the corpus
  already contradicted itself resolves to nothing — which is exactly the shape §5 rule 21's
  "anchor resolves but names the wrong construct" class describes, in text written by a repair pass
  (rule 18: repair text is the highest-risk text).
  The *substantive* claim behind it is sound: `grep -c 5050 stack-demo/platform/docker-compose.yml`
  returns **0** at `2adcf714`, and there is no `graphql` service block (only profile-name mentions at
  `:81,140,187,218,309,384`).
- **Confidence:** high.

### B3 — `graphql-wundergraph.md:128` self-cites `:84` for the build-context explanation; `:84` is the "no application source here" blockquote

- **Anchor:** `corpus/services/graphql-wundergraph.md:127-128` —
  > "So for its **final five months** compose built `Dockerfile.dev` — which is why a subgraph SDL
  > change rebuilt the router, **as `:84` describes**."
- **Why it is false:** `:84-86` is the blockquote *"**There is no application source here.**
  `package.json` is a stub (`{"name":"graphql-wundegraph"}` …)"* — it says nothing about build
  contexts or SDL-triggered rebuilds. The passage that *does* describe it is `:110-111`:
  *"It used to rebuild whenever any subgraph schema changed, because the build context is the parent
  dir (`..`) holding all sibling repos."* A reader chasing the citation lands on an unrelated
  paragraph.
  (The claim itself is fine — `platform` history confirms `2c85211` 2026-02-27 "update docker-compose
  and Makefile for improved service orchestration", `67ba772` 2026-03-02 "adjust docker-compose context
  and dockerfile path for graphql service", `360efd4` 2026-07-31 "remove the router service and clone
  entry outright", merged `2adcf714`.)
- **Confidence:** high.

### B4 — `ai-readiness.md:47` self-cites `:458`, which is a bare `>` blockquote separator

- **Anchor:** `corpus/services/ai-readiness.md:46-47` —
  > "This paragraph stated completed work as outstanding and **contradicted `:458` of this same file,
  > which is already in the past tense**; corrected M257x iter-46."
- **Why it is false:** `:458` is a lone `  >` — the blank line between the retracted-claim paragraph
  (`:456-457`) and *"**What is actually true.**"* (`:459`). Nothing there is in any tense. The
  past-tense statement about the `app-aireadiness-snapshot-loadmembers` demo-patch that the sentence
  means is at **`:484`**: *"**In the demo**, M51 iter-09 bounds it with the
  `app-aireadiness-snapshot-loadmembers` app read-path demo-patch…"* — 26 lines further down.
  Again a repair-pass citation whose whole function is to prove an internal contradiction, pointing at
  whitespace.
  (The substance is verified: the manifest at
  `.agentspace/rosetta-extensions/demo-stack/patches/app-aireadiness-snapshot-loadmembers/app-aireadiness-snapshot-loadmembers.yaml`
  reads `# v2.7 M254 RE-POINT:` at `:33` and `path: internal/aireadiness/readiness.go` at `:42`, and
  `demo-stack/tests/test_aireadiness_snapshot_loadmembers_m254.py` exists.)
- **Confidence:** high.

### B5 — `ai-readiness.md:316` anchors `SHOW_SECONDARY_TABS` at `AIReadinessClient.tsx:69`; that line is an `import` — the constant is at `:78`

- **Anchor:** `corpus/services/ai-readiness.md:315-317` —
  > "a 4th manager tab, **Compare** (cycle deltas), is fully built but **hard-gated off** —
  > **`AIReadinessClient.tsx:69` `const SHOW_SECONDARY_TABS = false;`** strips it from the tab list."
- **Why it is false:** in
  `next-web-app @ bb3313bc`,
  `apps/web/src/app/(authenticated)/(verified)/ai-readiness/AIReadinessClient.tsx:69` is
  `import { PeopleDrawer } from './_components/PeopleDrawer';`.
  The constant is at **`:78`** and reads `const SHOW_SECONDARY_TABS: boolean = false;` (typed, not the
  bare form quoted); its only use is at `:599`
  (`(SHOW_SECONDARY_TABS && tab.key === 'compare')`). The *fact* (Compare hard-gated off) is correct;
  the anchor and the quoted source text are not.
- **Confidence:** high.

### B6 — `ai-readiness.md:459,461` anchors `effectiveCycleId` at `AIReadinessClient.tsx:137-138` and the `cyclesQ.isFetched` gate at `:150-154`; both name the wrong constructs

- **Anchor:** `corpus/services/ai-readiness.md:459-461` (inside the "✅ CORRECTED M219" blockquote) —
  > "The **CURRENT** dashboard (**`AIReadinessClient.tsx:137-138`**) computes
  > `effectiveCycleId = selectedCycle ?? activeCycle?.id ?? latestClosedCycle?.id` and gates the data
  > GET on `cyclesQ.isFetched` (**`:150-154`**)…"
- **Why it is false:** measured in the file —
  - `:137` is **blank** and `:138` is `const cyclesQ = useAIReadinessCycles({ enabled: featureOn });`.
    `effectiveCycleId` is declared at **`:153-154`**.
  - `:150-152` are the three `// Cycle preference: …` comment lines and `:153-154` is the
    `effectiveCycleId` assignment — i.e. the range cited for the *isFetched gate* is where
    `effectiveCycleId` actually lives. The gate itself is at **`:169`**
    (`enabled: featureOn && cyclesQ.isFetched` on `dataQ`) and **`:176`** (the same on `stepsQ`), with
    its explanatory comment at `:160-165` and the render-side wait at `:355`.

  The two anchors are shifted by ~16 lines and effectively swapped relative to what they claim. This
  passage is a *retraction* — it is the text a reader consults precisely to check that the old claim
  was wrong, so a mis-anchor here is the expensive kind.
- **Confidence:** high.

### B7 — `messenger.md:110` places `SKILLPATH_STREAM=skillpath` in the **messenger** env-var table; it is a **backend** env var and appears nowhere in the messenger repo

- **Anchor:** `corpus/services/messenger.md:110` (row of the *## Environment Variables* table, whose
  header reads `| Variable | Value (compose) | Description |`) —
  > "~~`SKILLPATH_RPC_ADDR`~~ | *(removed)* | **Gone from docker-compose** … — only the residual
  > `SKILLPATH_STREAM=skillpath` remains."
- **Why it is false (as placed):** `SKILLPATH_STREAM=skillpath` is at
  `stack-demo/platform/docker-compose.yml:64`, inside the **`backend`** service's `environment:` block
  (lines 43–67: `AI_USAGE_STREAM`, `CMS_STREAM`, `JOBSIMULATION_STREAM`, `SKILLER_STREAM`,
  `SKILLPATH_STREAM`, …). The messenger block is `:240-279` and contains no `SKILLPATH_*` at all. And
  in the messenger repo `@ fa47850d`, `grep -rn "SKILLPATH" .` returns **exit 1 / zero hits**
  (positive control in the same pass: `grep -rn "SKILLER" . | wc -l` = **2** —
  `cmd/root.go:135`, `terraform/main.tf:78`), so messenger neither sets nor reads it.
- **Why I hesitated:** the sentence's grammatical subject is *docker-compose*, and the residual **is**
  in docker-compose — so read pedantically the clause is true. But it sits as the description cell of a
  row in a table titled "Environment Variables" for messenger, with a "Value (compose)" column, and
  every other row in that table is a messenger variable. A reader debugging messenger's skill-path
  wiring will grep messenger for `SKILLPATH_STREAM` and find nothing. Booked per the briefing's
  "when in doubt, book as BLOCKER and say why".
- **Confidence:** medium.

---

## Minors

- `corpus/architecture/ai_architecture.md:232` — cites "the enum is
  `app/internal/cms/directus/collections/jobsimulation.go:983-990`" for `ai_model`. The `AIModel`
  const block is `:979-991`; the cited range starts mid-block and omits
  `anthropic-35-sonnet-aws` (`:980`), `anthropic-37-sonnet-aws` (`:981`), `anthropic-4-sonnet-aws`
  (`:982`). 11 members total; the range covers 8.
- `corpus/architecture/ai_architecture.md:57` — "Full per-line derivation:
  [`external_services.md:577-587`]" for the **unset-`ai_vendor`** route. In
  `external_services.md`, `:577-583` is item 3 (`ANTHROPIC_API_KEY` / Course Builder / Studio-Room);
  the unset-vendor derivation is item **4**, beginning at `:584`. The range is offset by ~7 lines.
- `corpus/services/graphql-wundergraph.md:59-61` — "the `Subscription` substring hits in that SDL are
  Stripe/plan field names — `activeSubscription`, `stripeSubscriptionId`, `type PlanSubscription`".
  Enumerated from `schemas/backend.graphqls`: there are **four** distinct identifiers —
  `PlanSubscription`, `activeSubscription`, `stripeSubscriptionId`, **`stripeSubscriptionItemId`**
  (6 raw hits). The class claim (all Stripe/plan names) holds; the list is not exhaustive.
- `corpus/services/customerio-sync.md:19-33` — the "### Compose definition" YAML block is presented
  verbatim but omits `env_file: [.env]` and `networks: [app-network]`, both present in
  `platform/docker-compose.yml:229-234`. Everything it *does* show matches byte-for-byte.
- `corpus/services/messenger.md:23-40` — the "Key directories" tree lists 5 of the 13 non-test files
  under `internal/flow/`; missing are `ai_readiness.go`, `content_assigned.go`, `content_completed.go`,
  `coursebuilder.go`, `invitation_reminders.go`. Labelled "Key directories", so illustrative rather
  than wrong — but `ai_readiness.go` is a whole notification lifecycle that `ai-readiness.md:256-258`
  says messenger consumes, and it is invisible here.

---

## Positively cleared (audited zeros)

Every item below is labelled with what I actually did. Per §5 rule 24, **RE-DERIVED alone is not a
clearance**; where a set is load-bearing I enumerated it from source and stated its cardinality before
any arithmetic.

### `corpus/services/ai-readiness.md`

- **ENUMERATED** — "31 readiness skills = 19 core @ 1.0 + 12 enabling @ 0.5", denominator **25.0**
  (`:247`, `:350`, `:544`). I listed every member of `defaultReadinessSkills`
  (`app/internal/aireadiness/defaults.go:29-63`) rather than counting a `grep`: core =
  `K-AGECOD-C896, K-AGEENG-13FC, K-AICOSM-B710, K-AIEVAQ-B95C, K-AISECA-14AF, K-AISTRX-CEA7,
  K-AISYSD-26C1, K-AIUSEA-7F89, K-AIWORD-00FE, K-ANTCLI-6DDA, K-CLACOW-D576, K-CLADES-AAE6,
  K-CODEXX-77C3, K-CONCRE-AC50, K-CUSAGE-CB3D, K-GENAPP-FBE8, K-LLMMLO-0241, K-RESAIG-DBA4,
  K-TASAUT-EC86` = **19**; enabling = `K-ADARES-2B17, K-ANATHI-9B6A, K-APIINT-57F2, K-BUSACU-3FF6,
  K-CLOCOM-CB7E, K-COSMAN-35F5, K-CRITHI-224F, K-DIGDAT-553C, K-INFEVA-A507, K-PYTHON-8B21,
  K-SQLXXX-2064, K-WRICOM-824E` = **12**. Total 31; weight 19·1.0 + 12·0.5 = **25.0**. No
  commented-out entries in the literal. The doc's derived scores then check out:
  `round(1.0/25·30)=1`, `round(0.5/25·30)=1`, `round(9.0/25·30)=11`, `25/25·30=30`.
- **ENUMERATED** — "3 default sims (2 track-keyed simulations + 1 shared interview)".
  `defaultReadinessSims` (`defaults.go:75-79`) has exactly 3 entries:
  `{StepSimulation,"tech",634b9ffd-…}`, `{StepSimulation,"business",a4113c6b-…}`,
  `{StepInterview,"both",6d6cdf39-…}`. The demo seeder mirrors all three uuids verbatim
  (`rext stack-seeding/seeders/ai_readiness_config.go:93-95`) with the slugs the doc names at
  `:357-358` (`ai_readiness_config.go:84-85`).
- **ENUMERATED** — the archetype bands (`:146-148`). `scoring.go:26` `archetypeHighBand = 75`,
  `:30` `archetypeLowCeil = 50`; `classifyArchetype` `:46-56` is exactly Champion ≥75/≥75 → Standby
  ≤50/≤50 → `usage >= knowledge` Explorer → else Hidden Talent (tie→Explorer, `:52` and the comment
  at `:38-39`). `bucketKnowledge` `:67-77` is None 0-24 / Low 25-50 / Medium 51-74 / High 75-100 —
  matching the doc's table and its "was none 0-10 / low 11-40 …" retraction.
- **ENUMERATED** — the 15-column CSV (`:270`). I read the `AIReadinessCSVColumns` literal
  (`csv.go:17-32`) and counted **15** entries. Worth recording as a near-miss: the function's own
  doc comment at `csv.go:35` still says *"writes the 19-column CSV"*, so a `grep`-and-quote pass
  would have "refuted" the corpus using the code's stale prose. The literal at `:13-16` names the
  four dropped columns and confirms 19→15. **The corpus is right and the code comment is wrong.**
- **ENUMERATED** — `course`-adjacent near-miss of the same shape in `ai-labs.md`, see below.
- **ENUMERATED** — every table named in the *Data model* section exists as an ent schema under
  `app/internal/data/ent/schema/`: `ai_readiness_cycle.go`, `_step.go`, `_skill.go`, `_sim.go`,
  `_user_step_progress.go`, `_live_snapshot.go`, `_snapshot.go`, `_diagnose_narrative.go`,
  `_text_translation.go`, `_recommendation.go`, `_email_override.go`, `_notification_log.go`,
  `_notification_optout.go`, `interview_aggregated_report.go`. The three "plural — the ent-generated
  table name" claims verified against the generated constants:
  `Table = "ai_readiness_user_step_progresses"`, `"ai_readiness_notification_logs"`,
  `"ai_readiness_notification_optouts"`. `ai_readiness_email_overrides` is indeed
  `OrganizationMixin`-scoped (`ai_readiness_email_override.go:34-38`).
  `interview_aggregated_reports` carries `(sim_id, organization_id, report jsonb, session_count)` plus
  `last_session_id`/`agent_model`, unique on `(sim_id, organization_id)`.
- **ENUMERATED** — the 5 usage KPI ids (`:201`, `:614-618`). `usageDimSpecs`
  (`how_we_measure.go:1138-1158`) is exactly
  `avg_adoption, avg_transformation, avg_originality, avg_depth, avg_ownership` — 5 entries, and the
  retired `avg_frequency`/`avg_breadth`/`avg_context_fit` return **0** hits repo-wide in
  `internal/aireadiness/`.
- **ENUMERATED** — `{email, call}` is the whole `action_type` enum
  (`internal/data/ent/interaction/interaction.go:92-94` `case "email", "call":`), matching `:581`.
  Also `source_id <> target_id` is enforced (schema header comment,
  `internal/data/ent/schema/interaction.go:3-4`).
- **ENUMERATED** — `flag_ai_readiness`'s **sole consumer**. Repo-wide over `next-web-app @ bb3313bc`,
  excluding `node_modules`, the only *use* site is
  `apps/web/src/components/ai-readiness/data/useAiReadinessActive.ts:22`; `:7` imports it and
  `aiReadiness.constants.ts:9,26` declare it. Confirms `:72` and `:311`. Note the constants file's own
  comment at `:23` says *"Consumed client-side via useFeatureFlagEnabled() in useAIReadiness.ts"* —
  stale platform prose that the corpus correctly does **not** repeat.
- **ENUMERATED** — `AIReadinessClient.tsx` has **0** occurrences of `posthog` (case-insensitive),
  confirming the ⚠️ at `:76-81`; and it gates on `orgEnabled` at `:133-134`, exactly as cited.
- **ENUMERATED** — the orphan surface really is gone: `AIReadinessContainer.tsx`,
  `AIReadinessIntro.tsx`, `AIReadinessView.tsx` return **0** hits from a `find` over the repo
  (excluding `node_modules`), and
  `apps/web/src/app/(authenticated)/(verified)/enterprise/workforce/` holds only
  `(new)/`, `WorkforceNewClient.tsx`, `layout.tsx`, `page.tsx` — no `ai-readiness` route dir.
  `WorkforceNewClient.tsx:124-151` lists exactly 5 tabs (growth / verification / talent /
  assignments / activity) and omits readiness. `useWorkforceAIReadiness.ts:23-27` still calls
  `'/ai-readiness'` with `searchParams: { tag }` and no `cycle` param.
- **RE-DERIVED + ENUMERATED** — every remaining `file:line` in the doc that I could open resolved to
  the named construct: `readiness.go:82` `queryUserAISkills` (selects exactly
  `user_id, skill_id, is_verified` — confirming `:598`), `:139` `computeTier1`, `:289`
  `GetAIReadinessWithOptions` with the two snapshot routes at `:290-297` / `:307-312` and the
  fall-through at `:314`, `:330` `computeOrgBreakdowns`, `:388` `keepInCycleStep1` vs `:390`
  `keepStartedMembers`, `:638` `queryInCycleStep1Completers`, `:664` `keepInCycleStep1`, `:684`
  `keepStartedMembers`, `:771/779` `buildResponseFromSnapshots` → `m.workforce.LoadMembers(ctx, orgID,
  "")`; `steps.go:915` `queryReadinessStarters` with the exact
  `SELECT DISTINCT user_id FROM public.ai_readiness_user_step_progresses … AND status <> 'not_started'`
  and the *"This DB signal is the only real 'has started' check"* comment at `:914`;
  `how_we_measure.go:260` `computeCycleTotals` and `:285-286`
  `FROM public.interactions i JOIN public.job_simulation_sessions s`; `:819`
  `computeInterviewInsightsV2`, `:986` `holdsBackFromInsights`, `:1093` `resolveSessionAuthors`,
  `:1164` `usageDimensionsFromReports`; `enum/organization_settings.go:47`
  `OrganizationSettingAIReadiness = "ai_readiness"`; `enum/ai_readiness.go:9-11` the three step types.
  next-web: `AIReadinessHero.tsx:88` `if (!air.deadline) return null;`,
  `components/ai-readiness/useAIReadiness.ts:48` `deriveMode` (null deadline ⇒ `deadlinePassed = true`
  at `:55-57`), `hooks/useAIReadiness.ts:274` `interviewQuestions: number;`,
  `packages/core-js/src/constants/urls.ts:52` `AI_READINESS_URL = '/ai-readiness'`,
  `packages/ui/src/NavBar/useNavbarSections.tsx:4` import / `:400` `key: AI_READINESS_URL` /
  `:547` `showAIReadiness ? aiReadinessMenuItem : null`.
- **ENUMERATED** — `HowWeMeasureTab.tsx` is **1989** lines (the doc says "1,989-line file"),
  `grep -c interviewQuestions` returns **0**, and the "Handled for you this cycle" block at `:1879`
  renders exactly three cells: `skillsMapped` `:1915`, `handsOnMinutes` `:1921`,
  `interviewMinutes` `:1927`. The `:593` claim holds precisely.
- **ENUMERATED** — the rext-side seeder contract: all seven named files exist
  (`ai_readiness_interview_report.go` + `_test.go`, `cockpit.go`, `seat_append_test.go`,
  `ai_readiness_funnel_test.go`, `ai_readiness_evidence.go`, `contentref.go`);
  `cockpit.go:122` `LegacyReadinessPaths` + `ValidateCockpitManifest`;
  `ai_readiness_funnel_test.go:230` `TestAIReadinessFunnelSeeder_DayZeroHeroGetsNoSignals`;
  `blueprint.go:200` `AIReadiness string \`yaml:"ai_readiness"\``, `:316`
  `AIReadinessNotStarted = "not_started"`, `:347` `IsAIReadinessNotStarted` read in exactly one place
  (`seeders/ai_readiness_funnel.go:205` `aiReadinessStageFor`), and `:618-619`
  `validateEnum(…, "", AIReadinessNotStarted)` — a genuinely closed enum, as `:387-388` claims.
- **Could not verify (see below):** the two measured latencies (2.09 s live / 24 ms frozen), the
  199/156/78.4 % population figures, and the `2026-07-22` commit `4c28365f` — all require a running
  demo or the platform's git history, not on disk here.

### `corpus/architecture/ai_architecture.md`

- **ENUMERATED** — the routing model. `app/internal/jobsimulation/ai/ai.go` has exactly **four**
  vendor consts at `:30-33` (`azure`, `openai`, `anthropic-aws`, `anthropic`); `getClient` occupies
  **`:259-289`** exactly, with `Azure` → `azureClientEu` swapped to `azureClientUs` only on
  `flag_use_azure_us` (`:267`) and a *failed* lookup returning the EU client (`:270-273`);
  `AnthropicAws` and `Anthropic` both return `a.anthropicClient` (`:280-283`). The 429 path is
  `isThrottlingError` (`:129-141`, OpenAI + Anthropic 429 only) → `throttled = true` (`:167`) →
  `vendor = Openai` (`:154`) on the next retry, mirrored in `Response` at `:297-299,:326`.
  `internal/ai/ai.go` **does not exist** in `app`; it exists only in the frozen `jobsimulation` husk
  (`stack-demo/jobsimulation/internal/ai/ai.go`) — exactly as `:96` says.
- **ENUMERATED** — the 5-vs-4 vendor asymmetry at `:59-63`. Directus `AIVendor` has **5** members
  (`internal/cms/directus/collections/jobsimulation.go:970-974`: azure, azureglobal, openai,
  anthropic-aws, anthropic); `GetAIVendorAndModel`'s switch has **4** cases
  (`simulator/ai/ai.go:58 Openai`, `:69 Azure`, `:86 AnthropicAws`, `:102 Anthropic`) plus
  `default:` at `:114-115` → `internalAi.Openai`. `azureglobal` is the uncased one.
- **ENUMERATED** — the three simulation defaults table (`:243-247`) and the two follow-on details:
  content-side `aiModel := simulation.Gpt5Point1` at `:1297` and `aiVendor := simulation.Openai` at
  `:1302` with the sequence built at `:1307`; runtime `default: aiModel = openai.GPT4Dot1` at
  `:65-66` / `:82-83` / `:126-127`; validation `GetValidationAIVendorAndModel` `:20-26` = Azure +
  `GPT4Dot1` + `GPT4Dot1Mini`; anthropic-aws default `:98-99` Claude 3.7 Sonnet AWS, anthropic
  `:110-111` Claude 3.5 Sonnet. `AIVendor *AIVendor` at `:905` is on the Directus DTO, not the domain
  `simulation.Sequence` — confirming the `:65-68` retraction.
- **ENUMERATED** — the studio slot table (`:117-123`). I diffed
  `app/studio/configs/production_config.ini:26-36` against `development_config.ini:26-36`: byte
  identical, and stable == experimental within each. Values match the corpus row-for-row
  (FAST/STRICT `azure, gpt-5-mini, none`; EXECUTION `gpt-5.4, none`; CREATIVE `gpt-5.4, low`;
  REASONING `gpt-5.4, medium`). `gpt-5.2` appears in **no** `configs/*.ini` (`grep` exit 1) and does
  appear at `studio/services/ai.py:356` and `:508` as pricing entries. `config_template.ini:39` and
  `:40` do carry `gpt-4o` for EXECUTION/CREATIVE stable. `grep -rin 'bedrock|boto3'` over
  `app/studio/` returns **0** (positive control: `grep -ric anthropic services/ai.py` = 8),
  confirming *"Studio-Room was never on Bedrock"* at `:45-48` / `:84`.
- **ENUMERATED** — the evaluation-system claims. `checkerEngines` is written at
  `validator.go:43` (field), `:60-61` (init) and passed at `:595`, and stored again at
  `basevalidator/criterion.go:34,72,90` — a repo-wide grep finds **no** index/read of it anywhere,
  so *"stored and never read"* holds. LLM checks route through `criterion.go:127`
  (`case check.EngineLlm:`) → `validateLLM` at `:175` → `NewLLMBulkChecker(c.logger)` at `:428`;
  `templates/checkValidationBulk.tmpl` exists; `bulkChecks.go:118` `ai.WithTemperature(0.0)`.
  `validateCodeDiff` runs concurrently at `:168` and its `success` comes from a pure string
  comparison (`cdiff.Diff == ""`, `:457-460`) — the deterministic minority the doc insists on.
  `skills.go:53-62` `calculateSkillScore` counts booleans, `:75` computes `passed/total*100`,
  `:40-51` `calculateCompetencyLevelScore` is exactly `20` when `<60 && isPassed`, `100` at `>=100`,
  else `max(0, score*2-100)`, and carries the literal `// TODO fix this formula` at `:49`.
  `app/internal/skill/skill.go:617-623` `convertLevelTo100` is `100 * score / levelsCount`;
  `packages/ui/src/Competency/CompetencyReadLevel.tsx:18` is the `getSkillScoreForSimulation` call.
- **ENUMERATED** — the voice claims. `SimulationVoiceEngine` is a **4**-member enum
  (`jobsimulation.go:1081-1086`: gptrealtime / elevenlabs / livekitgptrealtime / livekitchain);
  `voiceEngineFromDirectus` at `:1594-1596` returns `gptrealtime` on nil.
  `flag_use_realtime_openai` is read at `calls/livekit.go:131-135` **inside**
  `CreateAgentDispatch` (which opens at `:106`), and its only effect is `:140-144`
  (`agentName = "anthropos-agent"`, `agentEndpoint = "openai-hosted"`) — exactly the ⚠️ at `:207-212`.
  `getJobSimulationCallSignedUrl` `:815` and `getJobSimulationCallConversationToken` `:833` both
  exist in `app/…/schemas/jobsimulations.graphqls`.
- **ENUMERATED** — the misc rows: `mistral.NewMistral(...)` at
  `internal/cms/studio/markdownManager.go:19` and the call at `studioManager.go:583`;
  `internal/coursebuilder/bedrock.go:110-113` is the key-present/absent either-or (comment `:105-108`);
  `internal/aiusage/ai_usage.go` exists; `app/studio` is a subprocess of the `app` image
  (`app/Dockerfile:25` `os/exec python3 studio/gen.py`, `:45-46` COPY + pip install);
  `studio-desk/.env.example:57` `AI_PROVIDER_CHAIN=azure-openai,openai`; HeyGen present under
  `internal/cms/`; the `:15-17` ⚠️ block is where `:96` says it is.

### `corpus/services/graphql-wundergraph.md`

- **ENUMERATED** — the subgraph ladder (`:36-41`). I re-derived the **SET at each commit**, not the
  count, by `git show <ref>:supergraph-config-prod.yaml`:
  `749dc86^` = {backend, skiller, jobsimulation, cms, skillpath} = **5**;
  `749dc86` = {backend, jobsimulation, cms, skillpath} = **4**;
  `7c17e63^` = 4 → `7c17e63` = {backend, jobsimulation, cms} = **3**;
  `915da06^` = **3** → `915da06` = {backend} = **1**. So cms-in-app is a **3 → 1** step, and
  `git show --name-status 915da06` marks **both** `schemas/cms.graphqls` and
  `schemas/jobsimulation.graphqls` as `D` in the same commit, while its own subject line reads
  *"fold cms subgraph into backend (supergraph 2→1)"* — the corpus's "the subject line is wrong" is
  correct.
- **ENUMERATED** — head-of-file table. `supergraph-config-prod.yaml` lists `backend` alone;
  `schemas/` holds `backend.graphqls` alone; `subgraphs.conf` is the single line `BACKEND=v1.360.0`;
  `terraform/main.tf:20` `service_desired_count = 1` with `locals.tf:7-8`
  `project = "wundergraph"`, `port = 8080`; `package.json` is literally
  `{"name": "graphql-wundegraph"}`. On `platform @ 2adcf714`: `grep -c 5050 docker-compose.yml` = **0**,
  no `graphql` service block, no `graphql-wundergraph` entry in `repos.yml` (which lists exactly
  app / cms / jobsimulation / sentinel / storage / messenger / roadrunner / next-web-app /
  studio-desk); the frontends point at `:8082/graphql/query` at `docker-compose.yml:334` and `:352`
  — both cited line numbers exact.
- **ENUMERATED** — no subscriptions. `schemas/backend.graphqls` declares `type Mutation` (`:4053`)
  and `type Query` (`:4912`) and **no** `type Subscription`; no `supergraph-config-*.yaml` contains a
  `subscription:` key (grep exit 1); `git branch -a --contains bba862f` returns only
  `remotes/origin/feat/use-web-socket`.
- **ENUMERATED** — `ci/update-subgraph.sh` contains **exactly one** `gh release download`
  (`grep -c` = 1), on line **9**, with `-R anthropos-work/app`. Pins verified in both Dockerfiles:
  `node:22.11-alpine`, `wgc@0.104.0`, `ghcr.io/wundergraph/cosmo/router:0.275.0`;
  `federation_version: =2.3.2`; `config.compose.yaml:5,13` `listen_addr: 0.0.0.0:8080` +
  `graphql_path: /graphql` with playground/introspection **true**, and `config.prod.yaml` the same
  addresses with both **false**.
- **ENUMERATED** — the smoke-test section. `app/internal/authorization/gqlauthz/gqlauthz.go:175-176`
  is the three-way exemption (`isPublicOnlyOperation` ∥ `isFederationQuery` ∥
  `colony.Development && isIntrospectionOnlyQuery`) and `:186` is the
  `"unknown viewer: Forbidden"` return the doc quotes verbatim; `gqlauthz_test.go:132`
  `"bare __typename is not exempt"` and `:176` `TestAnonymousRejectionLogsAtWarn` both exist. The
  `app` CHANGELOG's top entry `v1.363.2` is literally *"log anonymous viewer rejections at Warn, not
  Error"*, so the whole passage is current, not historical.
- Platform commit refs all resolve at `2adcf714`: `b56d731` / `360efd4` (both 2026-07-31),
  `d92e84e`, `63d285c`, `719befb`, `a2a3ee6`, `2c85211`, `67ba772`.

### `corpus/services/ai-labs.md`

- **ENUMERATED** — the credit cost model, and this is the file's sharpest near-miss.
  `creditCost` (`internal/credits/cost.go:86-90`) has exactly three entries:
  `course.build: 5`, `course.refine: **1**`, `course.translate: 1` — matching the corpus's
  "`course.build`=5/chapter, `course.refine`=**1**, `course.translate`=1/locale".
  **The file's own header comment at `:29` says `course.refine → 5 credits per refine turn`** — so a
  reader who greps the doc-comment instead of the literal will "refute" a correct corpus line.
  `cost_test.go:10-15` settles it: *"Refine is 1 by the D1 ruling … NOT 5; the map said 5 until…"*,
  asserting `int64(1)`. Also confirmed: `PricePerCreditUSD = 0.45` (`:142`),
  `MarginMarkup = 1.40` (`:133`), `DefaultSeedBalance int64 = 500` (`:231`), and
  `purchasePackages` (`:200-204`) = starter 50 / team 200 / scale 500.
- **ENUMERATED** — labs tiers + cap reasons. `LabsTier` = {essential, professional, frontier}
  (`labs_budget.go:26-28`) with `modelsForTier` = haiku / haiku+sonnet / haiku+sonnet+opus+codex
  (`:42-48`); `CapReason` = exactly the five the doc names — `model_not_in_tier`, `per_session_cap`,
  `org_total`, `user_monthly`, `user_lifetime` (`:247-251`). `spend_reconciler.go:60`
  `Interval: 30 * time.Second`. `labsapi/client.go:6` documents the default `:7070`.
- **ENUMERATED** — surfaces. `/credits/purchase` really is gone: `handler.go:12` states
  *"POST /credits/purchase was removed in Wave 13"*, and only `GET /credits/balance` +
  `GET /credits/transactions` remain (`handler.go:3-4,66,99`). The Stripe webhook
  (`api.go:315-323`) handles `customer.created` and
  `customer.subscription.{created,updated,deleted}` and **nothing else** — no
  `checkout.session.completed` anywhere in `internal/` — so the "un-built shared-purse grant" claim
  stands. `/v1/labs` is group-mounted with `apiKeyAuthMiddleware(apiKeys, "labs:write")`
  (`internal/web/backend/labs_admin.go:31`).
- **ENUMERATED** — structure. All named ent schemas exist (`lab.go`, `lab_body.go`,
  `lab_session.go`, `organization_credit.go`, `credit_transaction.go`, `org_subscription.go`,
  `subscription.go`, `stripe_customer.go`); all five named migrations exist by filename
  (`20260617203555_add_labs_catalog.sql`, `20260529072659_add_lab_session.sql`,
  `20260617120000_add_lab_session_model.sql`, `20260717151144.sql`,
  `20260626120000_add_org_subscriptions.sql`); `cmd/{labsimport,labskey,labsdemo}` all present and
  `labsdemo/main.go:115` defaults to `:8099`; `go.mod:62` `stripe-go/v74 v74.30.0`.
  `lab_session.go:21-28` explicitly omits `PrimaryKeyMixin` with the reason the doc gives, `:36`
  declares `field.String("id")`, and `:86-88` is the exact six-value status enum
  (booting/ready/grading/stopped/failed/cancelled). `internal/payments/` has no `_test.go`,
  confirming *"Payments has no Go unit test"*.
- **ENUMERATED** — `app` is at **`v1.363.2`** (`CHANGELOG.md:5`), matching `:18`.
- **ENUMERATED** — "Credits has NO GraphQL". The only `credit` hits across
  `internal/web/backend/graphql/graph/schemas/` are `setOrganizationFeatureCredits`
  (`mutations.graphqls:190-193` — a Sentinel *feature-quota* mutation, not the ledger) and a prose
  word in `academy.graphqls:162`. No ledger type/query/mutation. `labs.graphqls` carries the queries
  and mutations the doc lists (`:63,69,74,79,84,149,151` / `:100,111`).

### `corpus/services/messenger.md`

- **ENUMERATED** — the whole *Environment Variables* table against `platform/docker-compose.yml:240-279`:
  ports `8200:8200` `:250` / `8201:8201` `:251`; `BACKEND_USERS_RPC_ADDR=http://backend:8083` `:255`;
  `CMS_RPC_ADDR=http://cms:8091` **`:256`** (doc cites `:256` ✓);
  `JOBSIMULATION_RPC_ADDR=http://jobsimulation:8401` **`:258`** (doc cites `:258` ✓);
  `REDIS_WORKER_INDEX=0` `:261`; `REDIS_STREAMS_INDEX=4` `:262`; `RPC_PORT=8201` `:263`;
  `SKILLER_RPC_ADDR=http://backend:8083` `:265`; **no** `SKILLPATH_RPC_ADDR`;
  `profiles: [messenger, all]` `:279`; `depends_on` = redis, postgresql, backend, cms, jobsimulation
  `:268-278`.
- **ENUMERATED** — `REDIS_WORKER_INDEX` is set in compose and **not read by the code**:
  `grep -rn "REDIS_WORKER_INDEX"` over the whole `messenger` repo returns **zero** hits, while
  `cmd/root.go:107` reads only `REDIS_STREAMS_INDEX`. Confirms `:105`.
- **ENUMERATED** — the fallback triple at `:112`: `cmd/root.go:63` `PORT` → `"8080"`, `:64`
  `RPC_PORT` → `"8081"`, `:107` `REDIS_STREAMS_INDEX` → `"2"`. All three line numbers exact.
- **ENUMERATED** — `internal/rpcsrv/rpcsrv.go:25-30` is exactly the two `CodeUnimplemented` stubs
  (`CancelScheduledMessage` `:25-27`, `Schedule` `:28-30`) with `Send` implemented from `:31`.
  Worth noting the platform's own bug the corpus does *not* repeat: `:26` returns the message
  *"MessengerService.**Send** is not implemented"* from `CancelScheduledMessage`.
- **ENUMERATED** — `internal/flow/jobsimulations.go:142` (`>2h` ended) and `:148` (`>12h` started with
  no end) are exactly the staleness guards described at `:60`, inside the cited `:140-151` span;
  `internal/flow/flow.go:72` `AddSubscriber("backend", …)` with the five `OrgSkillPath*` handlers at
  `:74-78` inside the cited `:72-87`; `internal/flow/assignments.go:827-830` `getSkillPath` reads
  through `h.cms.GetSkillPath` — confirming *"skill-path data is read via the CMS client"*.
- **ENUMERATED** — `cmd/root.go:147` `READONLY_DB_CONNECTION` with the comment *"Copilot read-only DB
  connection"*, and `CHANGELOG.md:111-119` `v0.34.0 - 2026-04-24` carries both the whitelabel features
  and *"rename Copilot DB connection to Readonly DB"* — the `:44` claim is exact.
  `go.mod` = `go 1.25.0`, `getbrevo/brevo-go v1.1.3`, `osteele/liquid v1.8.1`.
- **ENUMERATED** — the four RPC clients at `cmd/root.go:120-142` (users, organizations, skiller,
  jobsimulation) plus the cms client used at `assignments.go:828`, matching the `:64` list, and
  `app/main.go:1196-1202` carries the exact *"Additive + DORMANT: external callers (messenger) keep
  hitting the standalone cms via CMS_RPC_ADDR until the M809 re-point"* the `:107` row quotes.

### `corpus/services/customerio-sync.md`

- **ENUMERATED** — the compose block at `platform/docker-compose.yml:220-238` matches every field the
  doc shows: `context: git@github.com:anthropos-work/customerio-sync.git#main`, `ssh: ["default"]`,
  `args: VERSION: dev / GH_ACCESS_TOKEN: $GH_PAT`, `ports: "8080:8080"`,
  `DB_CONNECTION_BACKEND=postgresql://postgres@postgresql:5432/postgres?sslmode=disable&search_path=public`,
  `profiles: [customerio-sync, all]`, `depends_on: postgresql: condition: service_healthy`.
- **ENUMERATED** — *"unique among Anthropos services"*. I enumerated **every** `context:` in
  `docker-compose.yml`: `:7 ../sentinel`, `:30 ../app`, `:85 ../jobsimulation`, `:146 ../cms`,
  `:191 ../storage`, **`:222 git@github.com:…customerio-sync.git#main`**, `:242 ../messenger`,
  `:283 ../roadrunner`, `:313 ../studio-desk`, `:346 ../next-web-app` — 10 contexts, exactly one
  remote. And `repos.yml` has no `customerio` entry (grep exit 1).

### `corpus/architecture/README.md`

- **ENUMERATED** — index completeness. `ls corpus/architecture/` yields 10 files besides `README.md`;
  the README links exactly those 10 (`ai_architecture`, `alignment_testing`, `architecture_overview`,
  `dependency_map`, `external_services`, `frontend_architecture`, `platform-migration-status`,
  `security_compliance`, `service_taxonomy`, `shared_libraries`). No orphan, no dead link.
- **ENUMERATED** — the `authn` claim at `:21`. Across all seven checked-out Go repos
  (app, sentinel, storage, messenger, cms, jobsimulation, roadrunner),
  `grep -c 'anthropos-work/authn'` returns **0** in every `go.mod` *and* every `go.sum`
  (positive control in the same pass: `anthropos-work/colony` = 1 in each of app/sentinel/storage/
  messenger). And it *is* imported from inside colony: `app/main.go:86-87`
  `github.com/anthropos-work/colony/authn` + `…/authn/provider/clerk`,
  `app/internal/web/backend/backend.go:35`. The four private modules named are the four actually
  required: `ai v1.40.2`, `colony v0.35.2`, `proto v1.210.0`, `taxonomy v1.2.0` in `app`.
  (Incidental corroboration of §5 rule 23's colony-pin note: app+messenger are on `colony v0.35.2`
  while sentinel+storage are on `v0.34.3`.)
  `rosetta-extensions/stack-core/platform_alignment_guard.py` (cited at `:15`) exists.

---

## What I could not settle

1. **The two AI-readiness latency numbers** — "LIVE `GET /api/workforce/ai-readiness` → 200 · 2.09 s ·
   304 KB" and "the frozen read answers 200 in 24 ms" (`ai-readiness.md:463,472,517,530`), plus the
   "180 s timeout" at `:482` and "180 s → 19 ms" at `:485`. These require a running demo stack. The
   doc itself says *"Re-measure before relying on either number"*, which is the right disclosure —
   but nothing on disk can confirm or refute them. Settled by: a `run-latency.sh` / `autoverify`
   receipt from a cold reset-to-seed on a live stack.
2. **The seeded population figures** — "78.4% (≈156 of the 199 frozen snapshots)", "byTeam (13)",
   "people (199)", "165 rows" for `interview_extraction_results`, "0 rows" for
   `conversation_extractions` (`ai-readiness.md:342-344,530,194-195`). All are counts from a seeded
   database. Settled by: a `psql` count against a freshly seeded demo.
3. **The `public.skill_embeddings` = 42,790 / `job_role_embeddings` = 18,919 / 22,470 job-role
   figures** (`ai_architecture.md:105,138,142-147`) — production-DB measurements, not derivable from
   the clones. Settled by: a `/db-query` read-only count.
4. **The skiller migration ids** `20260417103036` and `20260417120309` (`ai_architecture.md:151`).
   Neither filename exists under `app/terraform/migrations/` (which holds no `202604170*` files at
   all — the nearest are `20260414085836.sql`, `20260415120000.sql`, `20260422123242.sql`). The doc
   attributes them to the **skiller** repo, which is decommissioned and not in the ground-truth clone
   set, so I could not confirm or refute; I deliberately did **not** book it, because a missing
   migration in `app` is exactly what "these were *skiller* migrations" predicts. Settled by: a clone
   of `anthropos-work/skiller`, or `SELECT version FROM atlas_schema_revisions`.
5. **`anthropos-work/AI-Labs` as a live repo** (`ai-labs.md:5`), the **five LiveKit agent repos**
   (`ai_architecture.md:183-189`), and **"repo ARCHIVED on GitHub, 2026-07-30"**
   (`graphql-wundergraph.md:8`). All are GitHub-org facts; nothing offline can check them. Settled by:
   `gh repo list anthropos-work` / `gh api repos/anthropos-work/graphql-wundergraph --jq .archived`.
6. **Whether the LiveKit MP3 (as opposed to the Chime MP4) is stored in S3** — relevant to B1's
   *"Both"*. I found no LiveKit→S3 upload path in `internal/jobsimulation/calls/livekit.go`
   (`grep -n "storage\."` returns nothing) and did not chase the egress config. The Chime half of B1
   is settled; the LiveKit half is not. Settled by: reading the LiveKit egress/room-composite config
   or a bucket listing.
7. **`app` knowledge-plan release `06.00-shared-purse` / milestones M600–M607**
   (`ai-labs.md:16`). I did not open it — the briefing bars `knowledge/plan/**` and I applied that
   bar conservatively rather than deciding a different repo's tree was exempt. Settled by: another
   seat, or an explicit lift of the bar for `stack-demo/app/knowledge/`.
8. **Commit `4c28365f` ("Refactor AI Readiness domain…", 2026-07-22)** and app version `v1.351.1`
   (`ai-readiness.md:16-17`, `:242`). The *outcome* is fully verified (the `aireadiness` package
   exists with every named file, and `internal/workforce/` retains `members.go` + the KPI files), but
   I did not walk `app`'s git history to confirm the sha or the version at which it landed.
