# Seat G — reading #12 (adversarial diff seat)

Scope: `git diff 1255998..0e35b1a -- corpus/ CLAUDE.md .claude/` (436 diff lines, 11 files, 0 hunks in
`CLAUDE.md` or `.claude/`). Method: read every hunk plus its surrounding context in the **current** file,
then open every `file:line` anchor in the platform clones at the briefed shas.

## Positive controls

| file | `wc -l` | read |
|---|---|---|
| `corpus/architecture/architecture_overview.md` | 353 | hunks + §§236–312 in full; whole-file greps for the touched claims |
| `corpus/architecture/dependency_map.md` | 103 | **in full** |
| `corpus/architecture/external_services.md` | 831 | hunks + §§538–640, 682–690 in full; whole-file greps |
| `corpus/architecture/security_compliance.md` | 281 | hunks + §§55–230 in full |
| `corpus/architecture/shared_libraries.md` | 242 | hunk + §§130–155 |
| `corpus/ops/platform-alignment.md` | 1219 | hunk + §§665–710 (rule 24 entire) |
| `corpus/services/ai-readiness.md` | 645 | hunk + §§395–445 |
| `corpus/services/hiring.md` | 416 | **all five hunks + §§22–40, 60–80, 110–125, 150–168, 244–260, 298–320** |
| `corpus/services/jobsimulation.md` | 229 | hunks + §§92–135 |
| `corpus/services/roadrunner.md` | 171 | hunks + §§40–100 |
| `corpus/services/studio-room.md` | 473 | hunk + §§25–45, 255–270, 378–392 |

Honest note: for the two 800+/1200-line files I read the hunk neighbourhoods exhaustively and swept the
rest with targeted greps, not cover-to-cover. Every anchor I book below I opened myself.

---

## Blockers

### B1 — The corrected item 3 re-affirms a mechanism the platform explicitly forbids: `ANTHROPIC_API_KEY` does **not** reach Studio-Room

- **Anchor:** `corpus/architecture/external_services.md:576-583`
  > "3. setting `ANTHROPIC_API_KEY`, which flips **Course Builder** off Bedrock onto Anthropic's
  > first-party API (`coursebuilder/bedrock.go:106-113`) **and supplies Studio-Room the credential
  > its `anthropic` `TARGET SERVICE` needs** … **This item is live on the Course Builder half only.** Its
  > Studio-Room half is latent **for exactly the reason item 5 is**: no shipped `configs/*.ini` selects
  > `anthropic` either"
- **Why it is false:** the studio subprocess is launched with `pycmd.Env = studioSubprocessEnv()`
  (`app/internal/cms/studio/studioManager.go:1102`). `studioSubprocessEnv` (`:1063-1087`) **strips every
  bare AI name from the inherited environment** — `ANTHROPIC_API_KEY`, `ANTHROPIC_ENDPOINT`,
  `OPENAI_API_KEY`, `OPENAI_ENDPOINT`, `AZURE_API_KEY`, `AZURE_ENDPOINT` (`:1064-1071`, loop at
  `:1073-1080`) — and re-injects each bare name **only** from its dedicated `CMS_STUDIO_*` source
  (`:1081-1085`). The header comment at `:1055-1062` states the intent verbatim: *"the coursebuilder's own
  key ships as ANTHROPIC_API_KEY … Without the strip, a missing CMS_STUDIO_ANTHROPIC_API_KEY would silently
  hand Studio the coursebuilder's key instead of failing."* There is a unit test whose sole purpose is to
  enforce the opposite of the corpus sentence:
  `app/internal/cms/studio/subprocess_env_test.go:45-52`
  (`TestStudioSubprocessEnv_NoDedicatedKeyMeansNoKey` — sets `ANTHROPIC_API_KEY=coursebuilder-key`,
  `CMS_STUDIO_ANTHROPIC_API_KEY=""`, and **fails** if the child sees `ANTHROPIC_API_KEY`).
  So: setting `ANTHROPIC_API_KEY` supplies Studio-Room **nothing**; only `CMS_STUDIO_ANTHROPIC_API_KEY`
  does. The iter-52 edit had the evidence in hand (it opened `app/studio/configs/*.ini` and `gen.py`) and
  instead of catching the false premise it **endorsed it** and named config-selection as *"exactly the
  reason"* the arm is latent — when the credential half is independently, and more fundamentally, dead.
  This is a correction that overshot: it repaired the count while ratifying the wrong mechanism.
- **Confidence:** high

### B2 — `security_compliance.md:68` still says "only **30** use `OrganizationMixin{}`", eight lines above the correction that says 29

- **Anchor:** `corpus/architecture/security_compliance.md:67-68`
  > "of **135** Ent schemas (139 `.go` files, 4 of which declare no schema), only **30** use
  > `OrganizationMixin{}` — the one that carries the privacy `Policy()` (`mixin.go:126`)."
  and again at `:108` — "`OrganizationMixin{}` in 30".
- **Why it is false:** the same blockquote, at `:76` and `:82-88` (both **new** in this diff), now states
  the live count is **29** and calls the 30 an over-count. Ground truth confirms 29:
  `command grep -l 'OrganizationMixin{}' app/internal/data/ent/schema/*.go` → 30 files, and
  `app/internal/data/ent/schema/user_resource.go:22` is
  `// OrganizationMixin{},  // We need to work on this` — commented out, inside
  `func (UserResource) Mixin()` (`:16-24`). So `:68` is a live false claim **and** a self-contradiction
  inside a single blockquote. This is exactly the failure the seat brief names: the correction left a
  paraphrase of the old false claim standing in the same file. It is load-bearing — `:68`'s "30" is the
  first number a reader meets, and `:70`'s arithmetic ("Seven use `OrganizationIDMixin{}` … a further 18")
  is presented as a partition of the 30.
- **Confidence:** high

### B3 — New `jobsimulation.md` text contradicts untouched `ai_architecture.md:210`, and `ai_architecture.md` is the wrong side

- **Anchors:**
  - new: `corpus/services/jobsimulation.md:129` — "`flag_use_realtime_openai` … sets the endpoint to
    `openai-hosted` **and resets the agent name to the bare `anthropos-agent`** (`:140-144`), silently
    overriding a US session's `anthropos-agent-us`"
  - untouched: `corpus/architecture/ai_architecture.md:209-210` — "…(`calls/livekit.go:131-135` read,
    `:140-144` effect), and **all it does** is swap the dispatched **endpoint** to `openai-hosted`."
- **Why it is false:** `app/internal/jobsimulation/calls/livekit.go:140-144` is
  `if isRealtimeOpenaiFlagEnabled { … agentName = "anthropos-agent"; agentEndpoint = "openai-hosted" }` —
  **two** assignments. The agent-name reset at `:142` clobbers `:126`'s
  `agentName = fmt.Sprintf("anthropos-agent-%s", *location)`. `ai_architecture.md`'s "all it does" is
  false, and the diff created a live corpus contradiction by fixing one side only — while
  `jobsimulation.md:129` explicitly points the reader at `ai_architecture.md` for the fuller picture.
- **Confidence:** high

### B4 — The repaired `platform/.env` AI block drops the EU/US Azure keys that build the very clients the residency section is about

- **Anchor:** `corpus/architecture/external_services.md:616-635` (the whole fenced block + the new
  "Re-derive with `command grep -rho …`" paragraph)
- **Why it is false / unsupportable:** the block is introduced as *"AI services are configured via
  environment variables in `platform/.env`"* and the appended paragraph presents itself as the
  re-derivation of that set ("the four names above return 5 / 26 / 13 / 13"). The four counts re-derive
  exactly (I ran the stated command at `app 5ba17044`: 5 / 26 / 13 / 13; `OPENAI_ORG_ID`,
  `AZURE_OPENAI_ENDPOINT`, `AZURE_OPENAI_DEPLOYMENT` → 0; `OPENAI_API_KEY` → 2). **The arithmetic is right
  and the set is wrong** — rule 24 committed inside the repair. `platform/.env_example` (platform
  `2adcf714`) declares sixteen AI names, and the ones omitted include the load-bearing ones:
  - `AZURE_OPENAI_KEY_EU` / `AZURE_OPENAI_ENDPOINT_URL_EU` and `AZURE_OPENAI_KEY_US` /
    `AZURE_OPENAI_ENDPOINT_URL_US` (`.env_example:17-18,21-22`) — these are the **only** inputs to
    `azureClientEu` / `azureClientUs`, built at `app/internal/jobsimwiring/wiring.go:138-139`. The document
    spends `:544-612` on `getClient` swapping `azureClientEu → azureClientUs`, then hands the reader a
    configuration block that configures neither.
  - also omitted: `AZURE_OPENAI_KEY_RESULTS`/`_URL_RESULTS`, `AZURE_OPENAI_KEY_VOICE`/`_URL_VOICE`,
    `ANTHROPIC_ENDPOINT`, `MISTRAL_API_KEY` (the OCR provider this same file documents at `:539`),
    `CMS_AZURE_OPENAI_KEY`/`_URL`, `OPENAI_API_KEY`/`OPENAI_ENDPOINT`, `AZURE_API_KEY`/`AZURE_ENDPOINT`,
    and the entire `CMS_STUDIO_*` family. All confirmed present in Go: `AZURE_OPENAI_KEY_EU` 1,
    `_URL_EU` 1, `_KEY_US` 1, `_URL_US` 1, `_KEY_RESULTS` 2, `_KEY_VOICE` 4, `ANTHROPIC_ENDPOINT` 1,
    `MISTRAL_API_KEY` 2, `CMS_AZURE_OPENAI_KEY` 1, `CMS_STUDIO_OPENAI_API_KEY` 1.
  A reader configuring from this block gets a stack whose documented EU/US split, results model, voice
  model, OCR and Studio AI are all unconfigured.
- **Hesitation:** the block is arguably illustrative rather than exhaustive, and the pre-diff version was
  also short. I book it BLOCKER because the diff *added* a paragraph asserting a re-derivation, which
  converts an illustration into a claim about the set.
- **Confidence:** medium

### B5 — The new gen.py note tells the reader to set bare `AZURE_API_KEY` / `AZURE_ENDPOINT` / `OPENAI_ENDPOINT` in `platform/.env`, where they are stripped

- **Anchor:** `corpus/architecture/external_services.md:634-635`
  > "**This block covers `app`'s Go only**; `app/studio/gen.py:45-47` reads the separate bare names
  > `AZURE_API_KEY` / `AZURE_ENDPOINT` / `OPENAI_ENDPOINT`."
- **Why it misleads:** the sentence is literally true of `gen.py` (`app/studio/gen.py:45-48` lists
  `AZURE_API_KEY`, `AZURE_ENDPOINT`, `OPENAI_API_KEY`, `OPENAI_ENDPOINT`, `ANTHROPIC_API_KEY`,
  `ANTHROPIC_ENDPOINT`) — but it sits inside the **`platform/.env` configuration** section, and every one
  of those bare names is **removed from the child environment** by `studioSubprocessEnv`
  (`app/internal/cms/studio/studioManager.go:1063-1085`, applied at `:1102`) unless the corresponding
  `CMS_STUDIO_*` is set. Putting `AZURE_API_KEY=…` in `platform/.env` is a silent no-op. The correction
  named the consumer and omitted the interposed remap, which is the only thing that makes the names
  actionable. Same root cause as B1.
- **Confidence:** medium

### B6 — New self-anchor `hiring.md:157-159` points at the mirror-drop sentence, not at the adjudication it cites

- **Anchor:** `corpus/services/hiring.md:252-254`
  > "(This said *"neither table exists"* until M257x iter-52 — a flat contradiction of **`:157-159`**
  > above, which is the adjudicated form: the mirror was dropped, `jobsimulation.sessions` was not, and no
  > local stack creates that schema either way.)"
- **Why it is wrong:** `hiring.md:157-159` reads *"…the score lived on `public.local_jobsimulation_sessions`,
  a `Float32` **MIRROR** that shadowed a `jobsimulation.sessions` row. That migration **re-pointed the
  referencing assignment-session link ids** and **dropped the mirror** (`:58-62`) — it did *not* back-fill"*.
  It contains only the *first* of the three adjudicated facts. The claim actually cited —
  "`jobsimulation.sessions` itself was NOT dropped" and "on a local dev/demo stack it is not" — lives at
  `:162-166`. The cited lines exist; they do not say the thing. A reader chasing the citation to settle the
  contradiction lands on the half that is not in dispute.
- **Confidence:** medium

---

## Minors

- `corpus/architecture/security_compliance.md:83` and `corpus/ops/platform-alignment.md:686`: the stated
  derivation `grep -c 'OrganizationMixin{}' schema/*.go` does **not** "return 30" — with multiple file
  operands `grep -c` prints one `file:count` line per file (139 lines here). `grep -l … | wc -l` returns 30.
  In a passage whose entire teaching point is *"re-derive it; do not quote it"*, publishing a
  non-reproducing command is a reproducibility defect.
- `corpus/architecture/architecture_overview.md:248` and `corpus/architecture/security_compliance.md:201`
  both cite `external_services.md:569` for "enumerates **four live** ways". Line 569 now reads *"This list
  **previously said five**, counting a latent arm as live"* — i.e. the anchor lands on the retracted count.
  The enumeration head moved to `:570`; the numbered list starts at `:574`.
- Three different line ranges are cited across the corpus for the same `getClient` Azure case block:
  `architecture_overview.md:256` → `:262-276`; `security_compliance.md:209` → `:263-277`;
  `external_services.md:555` → `:264-276`. The block is `app/internal/jobsimulation/ai/ai.go:263-277`
  (`case Azure:` … `return client, nil`). Only one of the three is exact.
- `corpus/services/jobsimulation.md:103`: "NULLs the orphans (`:36-44`)" is a partial description of step 3.
  `20260729133514.sql:30-34` **DELETEs** whole `organization_assignment_sessions` rows with no surviving
  canonical reference, and `:49` is a bare `DELETE FROM "personal_assignment_sessions"`. The new sentence
  correctly kills "back-fills" (`INSERT` count is genuinely 0 — the single case-insensitive hit is the
  function name `on_insert_…` at `:64`), but describes a destructive step as a nulling step.
- `corpus/services/hiring.md:31-33`: "…**no local dev/demo stack creates it at all** … — **as the twins**
  [`service_taxonomy.md:52`] and [`dependency_map.md:78`] **already said**." Neither twin says that.
  `service_taxonomy.md:52` calls the `jobsimulation` schema an existing "legacy husk";
  `dependency_map.md:78` calls it "non-authoritative". Both presuppose it exists. The attribution
  over-claims agreement for a stronger, newer assertion.
- `corpus/services/hiring.md:68-72`: the parenthetical opened at `:68` (`(both **hard-error …**`) now
  swallows three sentences and closes mid-thought at `:72` (`…until M257x iter-52). And the client
  re-skin…`). Balanced, but unreadable.
- `corpus/architecture/external_services.md:579-580` and `:600-601`: the appended `⚠️` blocks begin on the
  line immediately after a sentence with no blank line, so they render as a run-on continuation
  ("Added M257x iter-49. ⚠️ **This arm is NOT reachable…**").
- `corpus/services/studio-room.md:388`: "Its outbound calls are to the **AI providers** … plus the skills
  taxonomy service" reads as exhaustive and omits `app/studio/agents/simulation/export.py:51`
  (`requests.get(image_url, stream=True)` — a third-party asset fetch for generated images). The
  correction is a clear improvement on the refuted "its only outbound API call is …", but under-enumerates.
- `corpus/architecture/external_services.md:635` names three of the four bare names at `gen.py:45-47`; the
  cited range also contains `OPENAI_API_KEY` (`:47`), and `ANTHROPIC_API_KEY`/`ANTHROPIC_ENDPOINT` follow
  at `:48`.
- `corpus/architecture/dependency_map.md:19`: "The compose ordering edge and the runtime data path
  disagree here **by design**" — intent asserted with no cited evidence (nothing in compose or the storage
  repo says the edge is deliberate).
- `corpus/services/ai-readiness.md:415-416`: "…and **reads no step-1 evidence signal at all**, which this
  sentence claimed until M257x iter-49" — dangling antecedent (the sentence claimed the *opposite*).

---

## Positively cleared (audited zeros)

**ENUMERATED** (predicate re-derived independently from source, cardinality stated before arithmetic):

1. **`app/studio/configs/*.ini` all pin `azure`.** Three tracked `.ini` files; `_AI_.*_MODEL` matches
   **30** lines (10 + 10 + 10); `grep -v azure` over those 30 returns **0**. Confirms
   `external_services.md:541,581,602`, `architecture_overview.md:252`, `security_compliance.md:205`.
2. **`gen.py` never overrides the service selector.** `app/studio/gen.py:41-53` — `load_services_settings`
   overrides exactly the six `secrets_keys` (`*_API_KEY` / `*_ENDPOINT`); the service token is
   `config_parts[0]` parsed from the ini at `:68,77`. Confirms `external_services.md:602-604`.
3. **31 = 29 + 2, out of 135.** Independently enumerated: `grep -l 'ent.Schema' schema/*.go` → **135**
   (139 files, 4 non-schema); `OrganizationMixin{}` in 30 files, minus the commented
   `user_resource.go:22` → **29**; files declaring their own `Policy()` are exactly four —
   `mixin.go` (`UserMixin` `:99`, `OrganizationMixin` `:126`), `org_membership.go:172`
   (`rule.AllowCurrentOrgEdgesOrSkipRule()` `:179`), `organization.go:56`
   (`rule.FilterSameOrganizations()` at **`:96`**, `Mixin()` at `:17-22` carries neither org mixin),
   `user.go:116` (user-scoped: `DenyNotSameUser`, `FilterSameUserRule`). 29 + Membership + Organization
   = **31**. The repaired derivation is correct and every anchor in it is exact.
4. **23 = 16 + 7.** Ran the document's own `comm`/`xargs` derivation: neither-mixin ∧ `"organization_id"`
   → **18**, minus `org_membership.go` (own policy) and `academy_feedback.go` (`UserMixin`) → **16**, and
   the 16 filenames listed at `security_compliance.md:92-97` match the measured set exactly.
   `OrganizationIDMixin{}` → **7**, and the 7 named at `:99-100` match. `user_resource.go` does not join
   the 23: it has no `"organization_id"` field (its org link is `edge.From("organization", …)` at `:44`),
   so the repair's arithmetic survives the mixin being removed from the policed set.
5. **`storage` compose facts.** `depends_on:` at `platform/docker-compose.yml:213`, `redis` `:214-215`,
   `postgresql` `:216-217`, both `condition: service_healthy` — the anchor `:213-217` is exact. The
   `environment:` block (`:204-211`) carries no `DB_CONNECTION`/`REDIS_ADDR`, and neither name appears in
   `.env` or `.env_example` at all. `storage/go.mod` has no redis (only `jackc/pgx/v5 // indirect`).
   `service_taxonomy.md:418` is exactly the profile row listing postgresql + redis for `storage`.
   `storage.md:14` and `:21` are exactly the two sentences cited.
6. **`colony/authn` importers.** Repo-by-repo file counts: app 129, **cms 6**, **jobsimulation 8**,
   sentinel 0, storage 0, messenger 0, roadrunner 0. `shared_libraries.md:145`'s "6 and 8" is exact and
   the enumeration is complete.
7. **LiveKit agent names.** `calls/livekit.go` — `:110` bare default, `:115`
   `agentName = "anthropos-agent-chain"` under `if chain {` (comment `:114` names the `livekitchain`
   engine), `:120` bare EU, `:126` `fmt.Sprintf("anthropos-agent-%s", *location)`, flag read `:131-135`,
   effect `:140-144`. `anthropos-agent-eu` returns rc=1 (no match) across all of `stack-demo/`, with
   `anthropos-agent-us` as the positive control (4 files). Every anchor in the new sentence is exact.
8. **`voice_engine` nil default.** `app/internal/cms/directus/collections/jobsimulation.go:1594-1597` is
   `func voiceEngineFromDirectus` / `if directusVoiceEngine == nil` /
   `return simulation.SimulationVoiceEngineGptrealtime` / `}`. Exact. Also confirmed the neighbouring
   anchors: `:905` `AIVendor *AIVendor` (nullable), `:1302` `aiVendor := simulation.Openai`.
9. **The four NOT-NULL-without-DEFAULT columns.** `app/terraform/migrations/20260722104506.sql` —
   `owner_id` `:6`, `sim_id` `:7`, `sim_type` `:10`, `token` `:13`; every other `NOT NULL` column
   (`:3,4,5,11,12,16,17,18`) carries a `DEFAULT`. Exactly four, exactly those line numbers.
   `hiring.md:218-219` is correct.
10. **`HiringConfigSeeder` bypasses the RPC.** `rosetta-extensions/stack-seeding/seeders/hiring_config.go:99`
    is `c.CopyRowsIdempotent(ctx, "public", "organization_sim_invitation_links", cols, rows, "id")`; the
    file contains no RPC call at all, and its header (`:13-37`) documents the direct-write design. The
    iter-52 correction at `hiring.md:69-72` and `:118-120` is right, and it correctly refuted the
    downstream consequence at `:118-120`.
11. **No `jobsimulation` schema on a local stack.** `platform/repos.yml:17-19` is exactly
    `- name: jobsimulation` / `type: go` / `migrations: false # legacy…`; `app`'s migration set contains
    exactly one `CREATE SCHEMA` (`20230817154747_supabase_baseline.sql:2`, `auth`); zero occurrences of
    `jobsimulation.` in any migration; `platform/postgresql/` contains only a Dockerfile (no init SQL).
    `askengine/registry.go:192` is the `// \`jobsimulation\` stays in rewriteSchemas until M710` comment
    inside an alias table whose entries all carry `Schema: "public"` with `RealName:` remaps — so the
    new qualification ("cited for the M710 horizon only … not evidence the schema is present") is exact.
12. **The mirror-drop migration.** `20260729133514.sql` — FK-column remaps `:15-23`, orphan NULLing
    `:36-44`, `DROP TABLE "local_jobsimulation_sessions"` `:62`, `DROP TABLE "local_skill_path_sessions"`
    `:63`. Zero `INSERT` statements. `intelligence.go:1700` is exactly
    `query := m.ent.JobSimulationSession.Query().`.
13. **Roadrunner.** `SubmissionPackage` (`internal/rpcsrv/rpc.go:43-58`) calls
    `CreateSubmission(ctx, "zip", nil, codePackagePtr, nil)` → one Judge0 `POST /submissions` with the zip
    in `additional_files` → one token. `CreateSubmission` (`internal/runner/runner.go:126-127`) enqueues
    exactly one `RoadrunnerSubmissionResult` task; `HandleSubmissionResultTask` (`:237-274`) polls
    `GetSubmission` in a loop with `maxRetries = 15` and `time.Sleep(1 * time.Second)`. Both corrected
    lines (`roadrunner.md:49`, `:87`) are true and consistent with `§ Async tasks` at `:97`.
14. **AI-readiness cycle branch.** `app/internal/aireadiness/readiness.go` — `if cyc != nil` at `:387`,
    `keepInCycleStep1` call `:388`, `keepStartedMembers` call `:390`; definitions at `:662-664` and
    `:684`; `queryInCycleStep1Completers` at `:638`, with `StepTypeEQ(enum.StepSkillMapping)` `:647`,
    `StatusEQ(enum.StepCompleted)` `:648`, `CompletedAtGTE(since)` `:649` — all inside the cited
    `:638-652`. `computeOrgBreakdowns` at `:330`. The iter-52 correction (wrong filter named) is right and
    every anchor lands on the right construct.
15. **`AnthropicProvider` / provider dispatch in studio.** `app/studio/services/ai.py` — imports `:1-2`,
    `return OpenAI(api_key=…)` `:383`, `return AzureOpenAI(…)` `:530`, `class AnthropicProvider` `:627`,
    `return Anthropic(api_key=…)` `:664`, `get_client` provider dict `:704-709`, dispatch `:713`. Every
    number in the new `studio-room.md:388` and in `external_services.md:541` is exact. `bedrock|boto3`
    under `app/studio/` → **0** hits.
16. **The hiring gate anchors** (retained but inside rewritten sentences): `siminvitationlink.go:62` is the
    `return nil, fmt.Errorf("organization is not hiring")`; `organization/manager.go:448` is
    `antRole = enum.RoleCandidate`; `:485` is the `logger.Error("organization is not hiring")` (the
    `fmt.Errorf` is `:486`).

**RE-DERIVED** (weakest clearance — I recomputed the arithmetic the document itself displays):

- `external_services.md:630-631`'s counts 5 / 26 / 13 / 13 and the 0 / 0 / 0 / 2 removals. Reproduced
  exactly with the stated command. **This clearance is void as support for the claim** — see B4: the
  arithmetic is right, the set is not.
- The "four live + one latent" bookkeeping across `external_services.md:570-605`,
  `architecture_overview.md:249-253` and `security_compliance.md:201-206` is internally consistent (items
  1–4 live, item 5 latent, no surviving "five" anywhere in `corpus/` — grepped). But see B1: the *content*
  of item 3's live/latent split rests on a false mechanism.

---

## What I could not settle

- **Whether `to_regclass('jobsimulation.sessions')` is actually NULL on a running demo stack**
  (`hiring.md:312-313`). The inference from `repos.yml:17-19` + the single `CREATE SCHEMA auth` is sound
  for a stack built from these repos, but a stack restored from a prod `pg_dump` or an older volume would
  still carry the schema. Settled by one `psql` against a live `demo-N`.
- **Whether `platform/.env_example:47`'s bare `OPENAI_API_KEY` is stale.** Given the strip in
  `studioSubprocessEnv`, it looks like a cms-container-era leftover superseded by
  `CMS_STUDIO_OPENAI_API_KEY` — which would make the corpus's *removal* right for the wrong reason. A
  platform-repo commit message or a live container `env` dump would settle it.
- **Whether `configs/local_config.ini` exists in any deployed image.** `gen.py:31` defaults
  `ENVIRONMENT` to `local` and reads `configs/{env}_config.ini`; only `config_template`, `development` and
  `production` are tracked, and `studio-room.md:266` says `local_*`/`test_*` are gitignored. The corpus's
  hedge ("no *shipped* config selects `openai`") is therefore correct as written, but the runtime
  reachability of the latent arm depends on what the app image actually ships and what `ENVIRONMENT` is
  set to in the backend container. Settled by `docker exec backend ls studio/configs`.
- **`hiring.md:99` / `jobsimulation.md:99`'s `queries.resolvers.go:70`** — that line is the
  `JobSimulationResult` resolver *signature*; the "plain Ent SELECTs over `validation_attempt_results`"
  happen one layer down in `simManager`. The anchor is defensible as "the entry point" but does not itself
  show the SELECTs. Untouched by this diff, so not booked.
