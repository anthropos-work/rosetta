# Seat C — reading #12

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| corpus/architecture/alignment_testing.md | 521 | yes |
| corpus/architecture/architecture_overview.md | 353 | yes |
| corpus/services/cms.md | 254 | yes |
| corpus/services/sentinel.md | 166 | yes |
| corpus/services/clerk-integration.md | 128 | yes |
| corpus/services/README.md | 79 | yes |
| corpus/services/db-backup.md | 31 | yes |

(Protocol read: `corpus/ops/platform-alignment.md` §5, lines 240–834, in full.)

Ground truth opened this pass: `stack-demo/app` @ `5ba17044`, `stack-demo/app/studio` @ `aeec036a`,
`stack-demo/platform` @ `2adcf714`, `stack-demo/next-web-app` @ `bb3313bc`, `stack-demo/sentinel` @
`88bc5592`, `stack-demo/cms` @ `ca50c817`, `stack-demo/jobsimulation` @ `462343b0`,
`stack-demo/roadrunner` @ `87d8d443`, `stack-demo/graphql-wundergraph` @ `60c229f3`,
`stack-demo/studio-desk` @ `14a5442a`, `stack-demo/ant-academy` @ `9c3843cd`, `stack-demo/messenger`
@ `fa47850d`, `stack-demo/storage` @ `4ce8ece5`, `.agentspace/rosetta-extensions` @ `f93ff73`
(clean tree).

---

## Blockers

### B1 — `cms.md` lists `python-docx` as a studio-room Python dependency; it is not in `requirements.txt`

- **Anchor:** `corpus/services/cms.md:110` —
  > `  requirements.txt         openai, anthropic, mistralai, rich, pyyaml, python-docx, requests, jinja2, pytest, pytest-asyncio (see studio/requirements.txt)`

- **Why it is false:** the parenthetical explicitly cites the file, so this is a checkable
  enumeration, not prose. `stack-demo/app/studio/requirements.txt` @ `aeec036a` contains exactly
  **nine** entries — `openai`, `anthropic`, `rich`, `pyyaml`, `requests`, `jinja2`, `mistralai`,
  `pytest`, `pytest-asyncio`. `stack-demo/cms/studio/requirements.txt` (the same submodule, same sha
  `aeec036`) is byte-identical. `python-docx` appears **nowhere** in the studio tree: a
  case-insensitive `grep -ril docx` over the whole `app/studio/` directory (90 `.py` files) returns
  only two prose hits — `agents/simulation/guidelines.py:48` (a prompt string telling the model *not*
  to emit `.docx`) and `knowledge/development/asset-examples/README.md:59` (a sample asset filename).
  Positive control in the same pass: `grep -n mistralai requirements.txt` → `8:mistralai`.
- **Confidence:** high

### B2 — `cms.md` names Mistral as a provider the Studio-Room **Python** pipeline generates against; the Python pipeline has no Mistral path at all

- **Anchors:** `corpus/services/cms.md:61` —
  > `the Go side dispatches generation work, the Python code executes it against OpenAI / Anthropic / Mistral.`

  and `corpus/services/cms.md:173` —
  > `* AI providers (Anthropic, OpenAI, Mistral — used by `cms/studio/` Python pipeline)`

- **Why it is false:** the studio-room provider registry is
  `app/studio/services/ai.py:705-708` — `providers = {'openai': OpenAIProvider, 'azure':
  AzureProvider, 'anthropic': AnthropicProvider}` — and `get_client` raises `ValueError(f"Unknown AI
  service: …")` for anything else. A case-insensitive `grep -ril mistral` over the entire
  `app/studio/` tree (90 `.py` files) matches **exactly one file: `requirements.txt`.** The
  dependency is declared and never imported. Positive control in the same pass: `grep -ril anthropic`
  → `gen.py`, `requirements.txt`, `CLAUDE.md`, `postgen.py`, `tests/unit/test_ai_service.py`.
  Mistral in this domain is a **Go**-side client, not a pipeline provider:
  `app/internal/cms/studio/markdownManager.go:11` imports `github.com/anthropos-work/ai/mistral` and
  `:19` builds `mistral.NewMistral(nil, os.Getenv("MISTRAL_API_KEY"))` for **document OCR**, called
  from `studioManager.go:583`.
  Both lines also **omit `azure`**, which is the only provider any shipped config actually selects
  (all 30 `*_AI_*_MODEL` lines across `app/studio/configs/{config_template,development_config,production_config}.ini`
  read `azure, …`).
  This is additionally a **corpus self-contradiction**: `corpus/architecture/architecture_overview.md:260-263`
  states *"Mistral is not part of this routing chain … `internal/cms/studio/markdownManager.go:11,19`
  builds a Mistral client … for Studio document OCR … a separate, single-purpose provider"*, and
  `corpus/architecture/external_services.md:596` states the Studio-Room provider set is
  `{openai, azure, anthropic}`.
- **Confidence:** high

### B3 — `alignment_testing.md` anchors the coverage-gate call at `gate.sh:61`; line 61 is a comment, the call is at `:69`

- **Anchor:** `corpus/architecture/alignment_testing.md:193` —
  > `⚠ **Not the same as the bare command — do not conflate them.** `gate.sh:61` calls `alignctl dna coverage --dna … --if-declared`.`

- **Why it is false:** at ground truth (`.agentspace/rosetta-extensions` @ `f93ff73`, clean tree),
  `clerkenstein/alignment/scripts/gate.sh:61` is
  `# over a DNA that is missing a consumed endpoint is exactly the thing that let Clerkenstein report 100% while`
  — a line inside the explanatory comment block. The invocation is at **`:69`**:
  `"$alignctl" dna coverage --dna "$base/$dna" --if-declared`. The anchor was correct when written
  (`git show dd65ad0:clerkenstein/alignment/scripts/gate.sh` puts the call at line 61) and drifted by
  8 lines when M219 (`9974faa`) inserted the exit-code contract block at the top of the file. It now
  resolves to the wrong construct.
- **Why I hesitated:** the anchor lands inside the comment block that documents that very call, so a
  reader is 8 lines away from the truth rather than lost. Booked as a blocker because the rubric names
  "wrong line anchors that point at the wrong construct" explicitly, and because this is the *one*
  citation in the section that exists to stop a reader from conflating the gate's behaviour with the
  bare command's — i.e. it is the load-bearing one.
- **Confidence:** high (on the fact); medium (on materiality)

### B4 — `services/README.md` says "three of the four … (cms, jobsimulation, roadrunner)" still start containers, but roadrunner is not one of "the four"

- **Anchor:** `corpus/services/README.md:20-21` —
  > `And **three of the four** (cms, jobsimulation, roadrunner) still start CONTAINERS locally in the default`
  > `` `graphql` profile as unfederated husks — *merged* is not *gone from compose*. ``

- **Why it is false:** the same blockquote defines its own antecedent 9 lines earlier at `:11-12` —
  *"**Four** services in this index — skiller, skillpath, jobsimulation and cms — are **folded into
  `app`**"* — and then at `:15` explicitly excludes roadrunner from that set: *"**`roadrunner` is the
  fifth, and it is different: orphaned, not merged-and-undeployed.**"* Roadrunner is therefore not a
  member of "the four", and only **two** of the four (cms, jobsimulation) start containers.
  Verified against `stack-demo/platform/docker-compose.yml` @ `2adcf71`: the service keys are
  `cms:` at `:144`, `jobsimulation:` at `:83`, `roadrunner:` at `:281`, all three carrying `graphql`
  in `profiles:` (`:187`, `:140`, `:309`); skiller and skillpath have no compose entry and no
  `repos.yml` entry. The sentence's *extension* (which containers start) is right; its *set* is
  wrong, in the direction that re-absorbs roadrunner into the merged group the two lines above just
  separated it from.
- **Why I hesitated:** a careful reader who has just read `:15` will reconcile it as "three of the
  five". Booked because it is a self-contradiction inside a single blockquote, which
  `platform-alignment.md` §5 rule 21 identifies as the dominant residual class, and because the whole
  point of `:15-18` is that roadrunner's row is *not* like the merged ones.
- **Confidence:** medium

---

## Minors

- `corpus/architecture/alignment_testing.md:513` — the Layout block's `cmd/alignctl  run | capture |
  dna list|diff|validate` omits `dna coverage`, which the same document introduces at `:245` and
  which `gate.sh:69` runs on every gate. (The tooling's own `dnaCmd` usage string,
  `alignment/cmd/alignctl/dna.go:16`, has the identical omission — so the doc is faithful to a stale
  string, not inventing one.)
- `corpus/architecture/alignment_testing.md:514-518` — the Layout block lists four `internal/`
  packages (`dna`, `outcome`, `compare`, `report`); there are **five**: `internal/canon` (canonical
  JSON — the "precision-preserving" comparison the doc claims at `:120-121`) is missing.
- `corpus/services/sentinel.md:44` — *"only as a fixture string in sentinel's own tests
  (`internal/authorization/casbin_test.go`, `internal/rpcsrv/rpc_test.go`)"* is an incomplete
  enumeration: `internal/authorization/manager_test.go:21` (`manager := "manager"`, used as a
  `p2` sub_role at `:31`) is a third. The substantive claim ("there is no `manager` Casbin role") is
  correct — `grep -rn manager init_policy.sql` returns rc=1, zero hits.
- `corpus/architecture/alignment_testing.md:325` — link text `knowledge/alignment.md` targets
  `../services/clerkenstein.md`; the named repo file does exist
  (`.agentspace/rosetta-extensions/clerkenstein/knowledge/alignment.md`) but is not what the link
  opens. Both links in the sentence resolve to the same corpus page.

---

## Positively cleared (audited zeros)

**ENUMERATED** (predicate enumerated independently from source, cardinalities compared):

1. **All five Clerk DNA gene/capability counts** (`alignment_testing.md:254-258`, `:351`, `:368`,
   `:375`, `:496`). Parsed each `clerkenstein/alignment/dna/*.json` with `json.load` and counted
   `len(capabilities)` and `sum(len(variants))`:
   `clerk-2.6.0` **14 caps / 27 genes**; `clerk-js-5` **6 / 9**; `clerk-multi-1` **5 / 9**;
   `clerk-deploy-1` **3 / 7**; `clerk-express-1` **5 / 13**. Five runner dirs exist
   (`clerkrun`, `jsfapirun`, `expressrun`, `deployrun`, `multirun`) and five golden dirs
   (`golden`, `golden-js`, `golden-multi`, `golden-deploy`, `golden-express`).
2. **"Only `clerk-2.6.0` declares a `consumed_surface` today"** (`:201-204`). Enumerated the key
   across all five files: present in `clerk-2.6.0` (**15** endpoints), **absent** from the other four.
   The `covered_by` example the doc quotes verbatim (`clerk-express-1:ClerkClientBAPI/get-organization`)
   is present at `clerk-2.6.0.json`'s `GET /v1/organizations/{orgID}` row. `GET /v1/users/{userID}`
   is present and maps to capability `GetUser`, whose variants are **`hero-a` / `hero-b`** (the
   two-sided assertion the doc describes at `:216-217`).
3. **The three `universal-user` identity genes** (`:213`). Enumerated by capability:
   `clerk-express-1:ExtractIdentity` (critical), `clerk-js-5:Me` (standard),
   `clerk-deploy-1:DeployIdentity` (critical). Exactly three, exactly those capabilities.
4. **The multi-tenancy fence — the set, not the sum** (`architecture_overview.md:299-307`). This is
   the §5 rule-24 trap, so I enumerated the predicate before doing any arithmetic:
   - denominator: `grep -l 'ent.Schema' *.go` in `app/internal/data/ent/schema/` → **135** files out
     of 139 `.go` files; total occurrences of `ent.Schema` is also **135**, so no file declares two
     schemas. The four non-schema files are `database_types.go`, `mixin.go`, `skiller_mixins.go`,
     `skillpath_mixins.go`.
   - `OrganizationMixin{}`: **30** grep hits, of which `user_resource.go:22` is
     `// OrganizationMixin{},  // We need to work on this` — commented out. Live set = **29**
     (matched on a line whose *stripped* prefix is `OrganizationMixin{}`, so commented lines are
     excluded by construction).
   - own `Policy()` declarations across the schema dir: exactly **three** —
     `org_membership.go:172`, `organization.go:56`, `user.go:116` (plus the two mixin policies in
     `mixin.go:99`/`:126`). Read each: `Membership` → `rule.AllowCurrentOrgEdgesOrSkipRule()` +
     `DenyMismatchedOrganization` (org-filtering); `Organization` → `rule.DenyIfNoOrganizationInContext()`
     + `rule.FilterSameOrganizations()` at `:96` (org-filtering); `User` → `DenyIfNoUserInContext` +
     `FilterSameUserRule` (**user**-filtering, correctly excluded).
   - ⇒ **29 + Membership + Organization = 31 of 135.** Matches.
   - `organization_id` bearers: **51** schema files. Minus the 31 org-policed ⇒ **24**. Of those 24,
     `academy_feedback.go` carries `UserMixin{}` (owner-filtered, not org) ⇒ **23 with no policy of
     any kind.** Of those 23, exactly **7** carry `OrganizationIDMixin{}` (`category`, `jobrole`,
     `skill`, `similarity`, `specialization`, `studio_task`, `studio_document`) ⇒ **16 with neither
     mixin.** Every figure in `architecture_overview.md:299-307` and the parenthetical *"16 is the
     neither-mixin subset of those 23, not the total"* is correct.
5. **Sentinel's Casbin model shape** (`sentinel.md:26`, `:37-41`). Read
   `internal/authorization/casbin.go:13-45` in full: request definitions `r, r2..r6` = **6**; policy
   definitions `p, p2..p6` = **6**; role definitions `g, g2, g3` = **3**; matchers `m, m2..m6` = **6**.
   Arities match the doc's `g(user,tier)` / `g2(org,user,role)` / `g3(org,membership)`.
6. **The four `MembershipRole` values** (`sentinel.md:40`). `app/internal/data/ent/enum/membership.go`
   lines 8–15: `RoleAdmin`, `RoleMember`, `RoleCandidate`, `RoleContentCreator` — exactly four, and
   the cited line range is exact. `init_policy.sql` seeds `content_creator` in a dedicated block at
   **`:88-118`** (counted line by line: header comment at 88, last row `simulation_type_training` at
   118, `;` at 119) and `internal/authorization/casbin_content_creator_test.go` exists.
   `grep -rn manager init_policy.sql` → **rc=1, zero hits**.
7. **`AUTHORIZATION_ADDRESS` set in exactly three compose blocks** (`sentinel.md:85`).
   `grep -n AUTHORIZATION_ADDRESS docker-compose.yml` → **45** (inside `backend:`, 28–82), **99**
   (inside `jobsimulation:`, 83–143), **160** (inside `cms:`, 144–188). No fourth. `messenger:`
   (240–279) sets none and its `depends_on` names `redis`, `postgresql`, `backend`, `cms`,
   `jobsimulation` — not sentinel; `grep -rn 'authorization|sentinel' messenger/**/*.go` yields one
   hit, a prose comment (`pkg/aireadinessemail/override.go:378`). `sentinel/go.mod` + `go.sum`
   contain **zero** `clerk` matches.
8. **The 12 Clerk webhook event types** (`clerk-integration.md:37`, `:79`). Enumerated the switch in
   `app/internal/clerk/events/events.go:121-190`: `user.{created,updated,deleted}`,
   `organization.{created,deleted,updated}`,
   `organizationInvitation.{accepted,created,revoked}`,
   `organizationMembership.{created,deleted,updated}` = **12**.
9. **The 8 Clerk locales** (`clerk-integration.md:41`). `apps/web/src/providers/Clerk.provider.tsx:9-18`
   imports `deDE, enUS, esES, frFR, itIT, jaJP, nlNL, ptPT` = **8**. (`apps/hiring` imports 7 — the
   doc's "8 locales" is the maximal surface, which is the fair reading.)
10. **The 9 `gen.py` arguments** (`cms.md:223-225`). `app/studio/gen.py:484-492` are exactly nine
    `parser.add_argument` calls — `-i/--interactive`, `-m/--media`, `-f/--force`, `--simid`,
    `--branch`, `--prompt`, `--annotations`, `--pipeline`, `--blueprint`. No `--template`.
    `parse_argument` occupies `:18-28` and calls `parse_known_args()` then merges the leftovers into
    the args dict — so a stray flag is silently swallowed, exactly as documented.
11. **27 service docs, index complete** (`services/README.md`). `ls corpus/services/*.md` → 29 files;
    minus `README.md` + `TEMPLATE.md` = **27**. The index's four sections enumerate 10 + 5 + 8 + 4 =
    **27**, and the two sets are identical (no doc missing, no phantom row).
12. **The six Directus content collections** (`cms.md:59`). Read the `NewCollection[...](dc, "…")`
    literal in each of `app/internal/cms/directus/collections/`: `"skill_paths"` (skillpath.go:29),
    `"simulations"` (jobsimulation.go:33), `"sequences"` (sequence.go:21), `"library_categories"`
    (librarycategory.go:44), `"library_macro_categories"` (librarymacrocategory.go:37), `"resource"`
    (resource.go:24). All six exist with those exact names.
13. **The six cms tables re-created in `public`** (`cms.md:26-28`).
    `app/terraform/migrations/20260724132049_cms_data_model.sql` has exactly six `CREATE TABLE`
    statements: `similarities`, `studio_documents`, `studio_tasks`, `similarity_categories`,
    `similarity_features`, `similarity_skills` — the same six names, same order-independent set.
14. **"Six Go services on a bare `make up`"** (`architecture_overview.md:60`). Enumerated every
    `profiles:` key in `docker-compose.yml`: `graphql` appears on `backend`(:81),
    `jobsimulation`(:140), `cms`(:187), `storage`(:218), `roadrunner`(:309), `gotenberg`(:384);
    `sentinel:`(:5) declares **no** `profiles:` so it always starts. Go services = sentinel, backend,
    jobsimulation, cms, storage, roadrunner = **6**; gotenberg is the third-party image. No `graphql`
    router service exists, and `repos.yml` has no `graphql-wundergraph` entry.
15. **The four Next apps on `^16.2.7`** (`architecture_overview.md:48`).
    `grep -rn '"next":' next-web-app/apps/*/package.json` → `web`, `hiring`, `integration`,
    `maintenance`, all `^16.2.7`; `apps/mobile` is Expo and declares none. Four, all aligned.

**RE-DERIVED** (weaker — I re-computed arithmetic the document itself displays):

- The toy alignment score (`alignment_testing.md:296-306`). Weights: `Add` critical ⇒ 3×3 = 9,
  `Greet` standard ⇒ 2×3 = 6, total 15; aligned 9 + 4 = 13 ⇒ 86.67 %, critical 3/3 = 100 %.
  I also **executed** it (`go run ./cmd/alignctl run --dna examples/toy/dna.json …`) and the output is
  byte-identical to the doc's transcript including the divergence line
  `FAIL Greet/padded-name  (exact, w2)` and the mirror string `"Hello,   El  Nino  !"`. The toy gate
  in `examples/toy/alignment_test.go:21-22` is `80.0` / `100.0` as documented, and the file carries
  `//go:build alignment` at line 1.
- "thirteen minor versions apart" (`clerk-integration.md:103`): `~2.19.36` − `~2.6.18` ⇒ 19 − 6 = 13.
- "all 30 `*_AI_*_MODEL` lines pin `azure`": 10 lines × 3 `.ini` files; a `grep '_MODEL = ' | grep -v '= azure'`
  returns empty.

**Other exact anchors opened and confirmed** (line-resolved *and* read for construct):

`app/internal/jobsimwiring/wiring.go:118` (= `jsrunner.NewRunnerManager(…JUDGE0_BASE_URL)`) ·
`app/main.go:1196-1202` (the verbatim *"additive + DORMANT … until the M809 re-point"*) ·
`app/internal/skillpath/session.go:205-207` (`u.cms.GetSkillPathDomain`, with the
`// cms-in-app deseam` comment on 205) · `app/internal/jobsimulation/ai/ai.go:85-88` (`config.WithRegion("eu-west-1")`),
`:129` (`func isThrottlingError`), `:166` + `:325` (both `if isThrottlingError(err)`),
`:262-276` (the `switch vendor` / `case Azure` block with `flag_use_azure_us`) ·
`app/internal/cms/studio/markdownManager.go:11,19` · `app/internal/cms/studio/studioManager.go:583` ·
`app/go.mod:31` (`clerk-sdk-go/v2 v2.7.0`) · `graphql-wundergraph/terraform/main.tf:20` (`= 1`) ·
`cms/terraform/main.tf:39` (`= 0`) · `jobsimulation/terraform/main.tf:40` (`= 0`) ·
`roadrunner/terraform/main.tf:19` (`= 1`) · `sentinel/terraform/locals.tf:4-5` (256 / 128) ·
`sentinel/go.mod:3` (`go 1.26.0`), `Dockerfile:2` + `Dockerfile.dev:2` (`golang:1.26-bookworm`) ·
`cms/go.mod:3` (`go 1.26.4`), `cms/Dockerfile:2` (`golang:1.26-bookworm`) + `:23` (`python:3.11-slim`) ·
`cms/cmd/root.go:242` (`/webhooks/` mounted with **no** secret ⇒ unauthenticated, on `PORT=8090`) ·
`app/main.go:1076-1080` + `internal/web/backend/backend.go:137-138,324` (the replacement
`POST /api/webhook/directus`, fail-closed on `DIRECTUS_WEBHOOK_SECRET`) ·
`platform/repos.yml:14-16` (cms, `migrations: false # legacy`) ·
`docker-compose.yml:256` (messenger's `CMS_RPC_ADDR=http://cms:8091`) ·
`graphql-wundergraph@915da06` (2026-07-29; squashed subject says "2→1", body confirms it also removed
the jobsimulation subgraph ⇒ **3→1**, exactly as the corpus corrects it), `749dc86` (skiller, 5→4),
`7c17e63` (skillpath, 4→3), and all three `supergraph-config-*.yaml` now list a single `- name: backend` ·
`platform@2adcf71` (2026-07-31, merge of `b56d731`+`360efd4`, both present) ·
`platform@045857c` (chronos removal) + `fdfa189` (intelligence removal) ·
`clerkenstein/.github/workflows/alignment.yml:10-11` (the "currently inert" self-description, quoted
verbatim and correctly) · `clerkenstein/clerk-backend/resources.go:38` (`organizationWithEid`, the
three-tier ladder), `store.go:138` (`SeedOrgIdentity`), `:151` (`LookupOrgEid`),
`cmd/fake-bapi/main.go:50` (`seedRosterMemberships`), `clerk-frontend/registry.go:187` (`LoadRoster`) ·
`alignment/internal/dna/dna.go:230-319` (Validate: id regex, unique gene ids, `normalized` requires
`normalize`, weight `1..1_000_000`, **no-critical-gene rejection**, and the consumed-surface binding),
`internal/compare/compare.go:56-60` (`GateMet` refuses a non-zero critical threshold at
`CriticalGenes == 0`), `:101-103` (Overall weighted, Critical an unweighted count ratio),
`internal/outcome/outcome.go:38-46` (golden path escape guard),
`cmd/alignctl/run.go:38` (Validate before the runner executes) and `:130-131`
(`ExitRegressed = 2` / `ExitUnmeasurable = 3`) · `alignment/go.mod` (module `anthropos.dev/alignment`,
no `require` block ⇒ stdlib-only) · `stack-seeding/dna/data-dna.json` (10 surfaces, statuses
`seeded-m7a` ×4, `snapshot-seeded-m9b` taxonomy, `snapshot-seeded-m10` content, `seeded-m7c` ×4 —
**nothing waived**), `dna/snapshot.go:36-48` (the five `snapshot-*` operators), `:364` `CapturedFromManifest`,
`dna/fidelity_probe.go` `PgFidelityProbe`, `cmd/datadna/main.go:63,290` `measure-snapshot`, and the
Directus predicate `private = false AND tenant_id IS NULL AND status = 'published'`
(`stack-snapshot/cmd/stacksnap/main_test.go:113`, `firewall/firewall.go:76`) ·
`stack-injection/gen_injected_override.py:53` (`DIRECTUS_DATA_CONSUMERS = ("cms", "backend")` — both,
as `cms.md:72` claims) · `studio-desk/src/index.ts:96` + `app/services/userService.ts:16`
(`STUDIO_ACCESS_ROLES`) · `next-web-app/apps/web/src/app/api/metabase/route.ts:35`
(`authData.orgRole !== 'admin'` — the bare form) · `ant-academy/code/proxy.js`
(`clerkMiddleware`, `REQUIRE_ORGANIZATION_MEMBERSHIP`, `/no-organization`) ·
`ant-academy/mobile/src/components/ClerkSignInForm.tsx:49-52` (aborts on any non-`complete` status) ·
`app/.github/workflows/build-production.yml:29` (`additional_repo: "anthropos-studio-room:studio"`) +
`app/Dockerfile:42-46` · app `CHANGELOG.md` v1.360.0/.1/.2/.3 all dated 2026-07-29, with .3 =
"restore public (unauthenticated) access to library content queries" and .1 = "pull studio via
additional_repo" · `app/knowledge/jobsimulation-domain.md:8,11` (the **23** jobsim entities).

---

## What I could not settle

- **The live alignment scores in `alignment_testing.md:254-258`** (Go 100 %/100 % 27/27, JS 100 %,
  multi 100 %, deploy 100 %, express UNMEASURABLE). `gate.sh` cannot run in this sandbox:
  `go build ./cmd/clerkrun` needs the private `github.com/anthropos-work/colony v0.34.3` and the
  script forces `GOPROXY=off`, so the build dies at `module lookup disabled by GOPROXY=off`. The
  *structure* of the claims is fully verified (gene counts, gate thresholds 95/100, the `real-org-eid`
  gene still present in `clerk-2.6.0.json` under `MembershipOrgIdentity` with `criticality: standard`,
  the exit-code split in `run.go`); only the numbers are unmeasured. **Settled by:** a network-enabled
  run of `clerkenstein/alignment/scripts/gate.sh` for each of the five surfaces.
- **GitHub archive status** of the `jobsimulation` (`architecture_overview.md:15`),
  `graphql-wundergraph` (`:3`, `services/README.md:43`) and `chronos`
  (`services/README.md:70`, "NOT archived, last push 2026-04-23") repos. Not derivable from a local
  clone. **Settled by:** `gh repo view anthropos-work/<repo> --json isArchived,pushedAt`.
- **The taxonomy figures** `≥42,790 skills` / `≥22,470 job roles` (`architecture_overview.md:13`, `:204`).
  Needs a read of the prod `public` schema. **Settled by:** `/db-query` against the taxonomy tables.
- **`corpus/services/db-backup.md` in its entirety.** The `db-backup` repo is not among the 13
  ground-truth clones, and nothing in `stack-demo/platform` references it (no compose service, no
  `repos.yml` entry — consistent with the doc's own "production-only" framing). Every claim in the
  file (Go, 6-hourly, S3 + Azure + Hetzner, full dumps of all schemas) is therefore **unverified, not
  cleared**. **Settled by:** adding the `db-backup` repo (or the production terraform) to ground truth.
- **`colony`'s `clerk-sdk-go` pin** (`clerk-integration.md:103`, the
  `CHECK-M257x-iter22-clerk-sdk-drift` note). `colony` is a private module pulled at Docker build and
  is not cloned; I confirmed `app/go.mod:31` = `v2.7.0` and that `cms`/`jobsimulation` carry it as
  `// indirect` at the same version, but not colony's own declaration. **Settled by:** cloning
  `anthropos-work/colony` and reading its `go.mod`.
