# Seat E — reading #11

Ground-truth shas verified before reading (all match the briefing):
`app 5ba17044` · `app/studio aeec036a` · `platform 2adcf714` · `next-web-app bb3313bc` ·
`sentinel 88bc5592` · `storage 4ce8ece5` · `messenger fa47850d` · `cms ca50c817` ·
`graphql-wundergraph 60c229f3` · `roadrunner 87d8d443` · `jobsimulation 462343b0` ·
`studio-desk 14a5442a` · `ant-academy 9c3843cd` · rext authoring copy @ `f93ff73`.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| corpus/architecture/service_taxonomy.md | 440 | yes |
| corpus/services/hiring.md | 416 | yes |
| corpus/architecture/shared_libraries.md | 242 | yes |
| corpus/services/storage.md | 175 | yes |
| corpus/services/askengine.md | 121 | yes |
| corpus/architecture/dependency_map.md | 103 | yes |

(Also read in full as instructed: `corpus/ops/platform-alignment.md` §5, lines 240–829.)

Every search below was run with stderr visible and with a paired positive control in the same
pass; the controls are named inline where an absence is load-bearing.

---

## Blockers

### B1 — `shared_libraries.md` enumerates "12 Connect-RPC services" and omits `StorageService`

- **Anchor:** `corpus/architecture/shared_libraries.md:77-82` —
  > "**12 Connect-RPC services are defined**: `UsersService`, `OrganizationsService`,
  > `CMSService`, `JobSimulationService`, `SkillerService` (all served by app since the merges — one RPC mux),
  > `SkillPathSessionService` (…), `LabSessionService` (…), `AuthorizationService` (Sentinel),
  > `MessengerService`, `RoadRunnerService`, `RealtimeService`, `ChronosService` (archived service, contract still present)."

- **Why it is false:** the list is 12 names and `StorageService` is not among them, but it is a real,
  live, currently-**served** Connect-RPC service defined in `proto`:
  - import path `github.com/anthropos-work/proto/go/storage/v1/storagev1connect` — enumerated
    independently by sweeping every `proto/go/**v1connect` import across all seven Go repos
    (positive control: the same sweep returns `cmsv1connect`, `labv1connect`,
    `authorizationv1connect`, … i.e. the pipeline works);
  - **served**: `storage/cmd/root.go:63` `rpcMux.Handle(storagev1connect.NewStorageServiceHandler(rpcsrv.New(storageManager, storagePublicManager), rpc.DefaultInterceptors))`;
  - **consumed**: `storage/sdk/storage/client.go:11,28` `storagev1connect.NewStorageServiceClient(…)`,
    and by `app` (`app/internal/app/users/services/import_profile_test.go:158`,
    `app/main.go:983` `storage.NewClient(os.Getenv("STORAGE_RPC_ADDR"), storagens.CMS)`).
- **It also self-contradicts the corpus** (the cheapest class, §5 rule 21): my own
  `corpus/services/storage.md:67` heads its interface section **"### Connect-RPC (`StorageService`)"** and
  tabulates its five methods — all five verified present at `storage/internal/rpcsrv/rpcsrv.go:28,49,69,90,112`.
  And `shared_libraries.md:70`, four lines above the list, already says proto is
  *"Imported by every live Go service that does RPC (app, sentinel, storage, messenger)"* — naming
  `storage` as an RPC service while the enumeration below has no service for it.
- The count is therefore wrong (≥13, not 12) **and** the SET is wrong; the omitted member is the
  one that is unambiguously alive today, while four of the twelve listed are explicitly dead or
  archived (`SkillPathSessionService`, `RoadRunnerService`, `ChronosService`, `RealtimeService`).
- **Confidence:** high.

### B2 — the Event-Streams table names Messenger on only 1 of the 3 streams it actually subscribes to

- **Anchor:** `corpus/architecture/dependency_map.md:58` —
  > "| `backend` | App | App (cms **domain** in `app`; the `cms` husk also still subscribes until platform M810) | User/org updates |"

  and the same defect at `corpus/architecture/dependency_map.md:61` —
  > "| `cms` | App (+ **Directus webhooks** …) | App (the cms similarity/Studio handlers + the jobsim handlers, merged onto ONE subscriber) | … |"

- **Why it is false:** `messenger` subscribes to **three** streams, all hardcoded string literals in
  one function (`messenger/internal/flow/flow.go`, `func (h *Manager) Subscribe()`):
  - `:72` `h.subServer.AddSubscriber("backend", sub.AddHandler(…))` — **~20 handlers**, the largest
    set in the file: `OrgSkillPathAssignedHandler`, `OrgJobSimulationAssignedHandler`,
    `OrgContentAssignedHandler`, `OrganizationMemberInvitedHanler`, `AIReadinessCompletedHandler`,
    `CourseBuildCompletedHandler`, …
  - `:105` `AddSubscriber("jobsimulation", …)`
  - `:109` `AddSubscriber("cms", sub.AddHandler(pubsub.EventHandler(h.CmsStudioTaskJobSimulationCompletedHandler)))`

  The table lists *"Messenger (if running)"* only on the `jobsimulation` row (`:60`) and omits it from
  both the `backend` row and the `cms` row. The `backend` stream is the platform's **primary
  email-notification path**; a reader debugging "why did no assignment email fire" would read this
  table and conclude messenger is not a consumer of it.
- Producer/consumer side-checks, so the row is not otherwise wrong: `app` publishes and
  self-subscribes the `backend` stream via `SERVICE_NAME` (`app/main.go:1311`
  `subServer.AddSubscriber(serviceName, backendSelfSub)`, `docker-compose.yml:60 SERVICE_NAME=backend`,
  with the folded cms Studio handler attached at `:1309`), and the `cms` husk still subscribes
  (`cms/cmd/root.go:192`). Both of those parts of the row are correct — only the consumer set is short.
- **Confidence:** high.

### B3 — "`cms` reaches Directus … (the only service the compose gives these)" — `backend` gets both vars and **requires** one

- **Anchor:** `corpus/architecture/service_taxonomy.md:290-292` —
  > "`cms` reaches Directus over the network via `DIRECTUS_BASE_ADDR` / `DIRECTUS_PUBLIC_BASE_ADDR` env
  > vars (**the only service the compose gives these**) — which point at the **production** instance …"

- **Why it is false:**
  1. `backend` loads `env_file: .env` (`platform/docker-compose.yml:41-42`), and `platform/.env`
     **defines `DIRECTUS_BASE_ADDR`** (verified values-blind, key names only). So the compose does give
     it to `backend` — just through `env_file` rather than an inline `environment:` entry. This is
     exactly the live measurement `platform-alignment.md` §5 (iter-23) records
     (`backend` → `https://content.anthropos.work` on `demo-1`).
  2. `app` does not merely receive it, it **hard-requires** it: `app/main.go:971`
     `directusBaseAddr := os.Getenv("DIRECTUS_BASE_ADDR")` → `:973`
     `log.Fatalf("DIRECTUS_BASE_ADDR is required to wire the cms-in-app managers; the external Directus edge is unconfigured")`.
     It also reads `DIRECTUS_PUBLIC_BASE_ADDR` at `app/main.go:1045`. `platform/CLAUDE.md` states the
     same ("cms activates on `DIRECTUS_BASE_ADDR` … it comes from `.env`").
  3. Naming `cms` (a `running_but_unfederated` husk that takes no traffic) as *the* Directus reader is
     the §5 iter-23 "named-consumer list survives the merge" class — and it **contradicts this same
     file 20 lines later**, at `:309-310` (*"Frontend/Studio-Desk → `backend` :8082/graphql/query (cms
     **domain**, `app/internal/cms/directus/`) → Directus API"*) and `:313` (*"The **cms domain inside
     `backend`** acts as a smart proxy/adapter"*).
- I hesitated only because the paragraph's *purpose* — retracting the old "there is a local Directus
  compose service" claim — is correct and well-sourced. The parenthetical is a separate, false claim
  that would send a "why is my stack reading prod content?" debug to the wrong container.
- **Confidence:** high.

### B4 — `dependency_map.md`'s shared-library "Used By" rows under-enumerate, contradicting `shared_libraries.md`

- **Anchor:** `corpus/architecture/dependency_map.md:50` —
  > "| **taxonomy** | **node-id library** (not data): direct — app, messenger; indirect — storage, sentinel |"

- **Why it is false:** enumerated from every repo's `go.mod` at the pinned shas —
  | repo | taxonomy require |
  |---|---|
  | app | `go.mod:20` `v1.2.0` (direct) |
  | messenger | `go.mod:9` `v1.2.0` (direct) |
  | **cms** | **`go.mod:13` `v1.2.0` (direct — no `// indirect`)** |
  | **jobsimulation** | **`go.mod:15` `v1.2.0` (direct — no `// indirect`)** |
  | sentinel | `go.mod:21` `// indirect` |
  | storage | `go.mod:25` `// indirect` |
  | roadrunner | absent |

  The row names 4 consumers where there are 6, and both omissions are **direct** requires in repos
  the default `graphql` profile still builds and starts (`docker-compose.yml:83`, `:144`).
- **It contradicts the corpus's own "full reference"**, which `dependency_map.md:42` links to:
  `shared_libraries.md:181` says *"directly: app, messenger — **and the still-running `cms` +
  `jobsimulation` husk containers**, which require it directly in their own `go.mod`
  (`cms/go.mod:13`, `jobsimulation/go.mod:15`, both `v1.2.0`, neither marked `// indirect`) … **6 of
  the 7 live Go service repos**"*. That side is exactly right; `dependency_map.md:50` is the wrong one.
- **The same under-enumeration recurs twice more in the same table**, so a repair must sweep all three
  rows (§5 rule 19):
  - `:48` **ai** — *"app — i.e. every folded domain"*; but `cms/go.mod:9` and `jobsimulation/go.mod:11`
    both require `ai v1.40.2` directly, which `shared_libraries.md:105` states explicitly.
  - `:49` **authn** — *"Imported via `colony/authn` by app"*; but `shared_libraries.md:145` records the
    cms and jobsimulation husks importing `colony/authn` directly, which I re-derived: **6** Go files
    in `cms` (`cmd/root.go`, `internal/auth/authManager.go`, `internal/graph/handler.go`,
    `internal/graph/mutations.resolvers.go`, `internal/graph/simulation.resolvers.go`,
    `internal/user/userManager.go`) and **8** in `jobsimulation` — matching that doc's "6 and 8" exactly.
- **Confidence:** medium-high. (Medium only because one could argue the table is scoped to "live"
  services — but the same table's rows `:15`–`:18` document the husks as still-started containers, and
  the taxonomy row's own words are an explicit direct/indirect split, not a liveness filter.)

### B5 — "each consumer's own `internal/ai/ai.go` wrapper" names a path that does not exist in `app`

- **Anchor:** `corpus/architecture/shared_libraries.md:125-128` —
  > "Vendor selection lives in each consumer's own `internal/ai/ai.go` wrapper: an EU Azure client by
  > default, a US Azure client gated by the PostHog flag `flag_use_azure_us`, and an Azure→direct-OpenAI
  > fallback on HTTP 429."

- **Why it is false:** `app/internal/ai/` **does not exist** (`ls: app/internal/ai: No such file or
  directory`). Since the merges `app` is the only live Go consumer of the `ai` library, and its two
  vendor-selection wrappers are at **`app/internal/skillerai/ai.go`** (`getClient` at `:332`, the
  `flag_use_azure_us` check at `:345-353`) and **`app/internal/jobsimulation/ai/ai.go`**
  (`getClient` at `:259`, flag at `:265-267`); there is a third at
  `app/internal/jobsimulation/simulator/ai/ai.go`. The only `internal/ai/ai.go` on disk anywhere is in
  the **`jobsimulation` husk repo** — i.e. the pre-merge path. Same §5 iter-23 class as B3.
- I hesitated because the *substance* is exactly right (verified: EU Azure default → `flag_use_azure_us`
  → US Azure; `isThrottlingError` / `http.StatusTooManyRequests` at `skillerai/ai.go:126,131` and
  `jobsimulation/ai/ai.go:127,132`), and the sentence is phrased generically ("each consumer's own"),
  so it is a wrong *path* rather than a wrong *mechanism*. Booked as a BLOCKER per the briefing's
  tie-break, because a reader following it finds nothing and the real locations are non-obvious.
- **Confidence:** medium.

---

## Minors

- `shared_libraries.md:70` — LabSession anchored at `app/main.go:1218`; `:1218` is the
  `labv1connect.NewLabSessionServiceHandler(...)` call and `:1219` is the `mux.Handle`. The sentence
  says "registers", so the strictly-correct anchor is `:1219`. Non-material — the other five
  (`:1178`, `:1179`, `:1187`, `:1195`, `:1204`) are exact `mux.Handle` lines and the count of six is right.
- `askengine.md:101-106` enumerates four test files; the package has five —
  `internal/askengine/anthropic_test.go` (1 test) is unlisted. The passage says "the bulk", so this
  is not a false claim, but the list reads as exhaustive.
- `hiring.md:66` attributes `resolver_cms_queries.go:95,210,258,295` to `PrivateJobSimulations`.
  Only `:95` is inside it (`PrivateJobSimulations` spans `:89-111`); `:210`, `:258`, `:295` are the
  identical `GetOrganizationIsHiring` call inside three *other* resolvers. The claim being made
  (the content-library path reads `is_hiring`) is true and in fact broader than stated.
- `hiring.md:175` read-path row 5 anchors `intelligence.go:1728-1735` for
  "`row_number() ORDER BY score DESC` per candidate". The range contains only the *call site*
  (`:1733` `m.usersBestOrFirstJobSimulationSession(…)`); the window function itself is at
  `intelligence.go:2158-2168` (inside the func declared at `:2124`). The claim is true; the anchor is
  one hop away from the construct it describes.
- `hiring.md:173` anchors `simulationScoreColumn.tsx:54`; `:54` is `accessorKey,` and the
  `accessorKey = 'score'` default is at `:33`. The paired `:95-97` anchor is exact.
- `service_taxonomy.md:195` prescribes
  `python gen.py --media simulation --prompt "…" --evaluation_skills "…" --branch stable`, five lines
  above a boxed warning that unregistered flags are *"silently swallowed"*. `--evaluation_skills` is
  **not** one of the nine registered `add_argument`s — it survives only through
  `parse_argument`'s `parse_known_args` unknown-arg merge (`app/studio/gen.py:18-28`), which is the
  same mechanism the warning describes as a footgun. Both statements are individually true; read
  together they look self-contradictory unless you already know the merge.

---

## Positively cleared (audited zeros)

**ENUMERATED** — predicate re-derived from source independently, cardinality compared:

1. **`service_taxonomy.md:55-56,433` — "the default `graphql` profile starts six Go services plus
   Gotenberg, three of which are unfederated husks."** Enumerated every `profiles:` key in
   `platform/docker-compose.yml`: `graphql` ∈ {backend `:81`, jobsimulation `:140`, cms `:187`,
   storage `:218`, roadrunner `:309`, gotenberg `:384`}, plus `sentinel` which declares **no**
   `profiles:` and is therefore always on. = 6 Go + Gotenberg; husks = jobsimulation, cms, roadrunner = 3. ✔
2. **`service_taxonomy.md:339` — the subgraph ladder 5 → 4 (`749dc86`) → 3 (`7c17e63`) → 1 (`915da06`),
   and "cms-in-app is the 3 → 1 step, not 2 → 1".** Enumerated `supergraph-config-prod.yaml` at each
   parent commit: `749dc86^` = {backend, skiller, jobsimulation, cms, skillpath} = 5;
   `7c17e63^` = {backend, jobsimulation, cms, skillpath} = 4; `915da06^` = {backend, jobsimulation, cms}
   = 3; HEAD = {backend} = 1. Dates 2026-06-24 / 2026-07-21 / 2026-07-29 all match. **Note the corpus
   is right against the platform's own file**, whose in-repo comment still says "supergraph 2→1" — the
   corpus's explicit refutation of "2 → 1" is correct because `915da06` is a squash carrying the
   jobsimulation-subgraph removal too. ✔
3. **`shared_libraries.md:41,69,104,181` — every shared-library version pin.** Enumerated all seven
   `go.mod`s: colony app/messenger `v0.35.2`, cms/jobsimulation `v0.35.1`, sentinel/storage/roadrunner
   `v0.34.3`; proto app/messenger `v1.210.0`, cms `v1.207.0`, jobsimulation `v1.205.0`, sentinel
   `v1.200.0`, storage/roadrunner `v1.196.0`; ai `v1.40.2` in app/cms/jobsimulation only; taxonomy
   `v1.2.0` in 6 of 7 repos, roadrunner the sole exception. Every value **and** every set matches. ✔
4. **`shared_libraries.md:70` — "`app/main.go` registers six Connect handlers … and neither
   `SkillPathSessionService` nor a RoadRunner service is among them."** Enumerated every
   `v1connect.New*ServiceHandler` call in the whole `app` repo: exactly six, at `:1178`, `:1179`,
   `:1187`, `:1195`, `:1204`, `:1218` — the only other hit repo-wide is a comment
   (`internal/rpc/skillerrpc/skiller.go:56`). Also confirmed `ROADRUNNER_RPC_ADDR` is read by **no**
   Go code in `app` (positive control: `JUDGE0_BASE_URL` returns
   `app/internal/jobsimwiring/wiring.go:118`). ✔
5. **`shared_libraries.md:145` — "the cms and jobsimulation husk repos still import `colony/authn`
   directly (6 and 8 files)."** Enumerated: cms = 6 Go files, jobsimulation = 8 Go files. Exact. ✔
6. **`shared_libraries.md:202-209` / `service_taxonomy.md:112` — the taxonomy figures.** Enumerated
   directly from the capture manifest
   (`.agentspace/snapshots/taxonomy/5afc0bccf1df7ef538b643321fc6362f/manifest.json`):
   `skills` **42,790**, `job_roles` **22,470**, `job_role_embeddings` **18,919**, all with
   `filter: organization_id IS NULL`, `source: primary-read`, `public_only: true`,
   `predicate: org-null`, `captured_at 2026-06-29T14:43Z`. Every number, the provenance metadata, and
   the "18K came from `job_role_embeddings`" diagnosis reproduce exactly. ✔
7. **`hiring.md:216-219` — "`token` … is one of **four** required-and-undefaulted columns (`owner_id`
   `:6`, `sim_id` `:7`, `sim_type` `:10`, `token` `:13`; every other `NOT NULL` column in the DDL
   carries a `DEFAULT`)."** This is the file's own displayed derivation, so I enumerated the *set*
   rather than checking the *sum* (§5 rule 24): read all 24 column lines of
   `app/terraform/migrations/20260722104506.sql:2-27` and classified each. NOT NULL without DEFAULT =
   exactly {`owner_id` 6, `sim_id` 7, `sim_type` 10, `token` 13}; all eight other NOT NULL columns
   (`id`, `created_at`, `updated_at`, `status`, `completion_status`, `interactions_progress`, `score`,
   `validation_version`) do carry DEFAULTs; the remaining twelve are NULL-able. **Both conjuncts hold.**
   `token`'s UNIQUE index is at `:29` as claimed. ✔
8. **`hiring.md:212-213` — "`completion_status` … a closed 5-value enum — exactly `pending` /
   `passed` / `failed` / `discarded` / `timedout`".** Enumerated the const block
   `app/internal/data/ent/enum/jobsimulation.go:29-35` (5 members) and `Values()` at `:37-43` (5
   entries), plus both SDL declarations `jobsimulations.graphqls:14` and `:128` (5 lowercase members
   each). No `SIMULATION…`-prefixed member exists. ✔
9. **`hiring.md:64` positive controls — "`OrgFeatureInsights` ×8, `JobSimulationSession` ×44".**
   Re-measured: `grep -c OrgFeatureInsights resolver_queries.go` = **8**;
   `grep -c JobSimulationSession internal/organization/intelligence.go` = **44**. Also re-ran the two
   greps the sentence is about: `grep -in hiring` over `resolver_queries.go` returns 4 hits, all
   sim-TYPE filters (`:811`, `:843`, `:991`, `:992`); over `intelligence.go` returns **0**. ✔
10. **`hiring.md:140` — "87 real `SIMULATION_TYPE_HIRING` sims (published + public)".** Enumerated the
    captured `directus.simulations.copy` payload (307 rows) by the `type` column:
    HIRING **87**, ASSESSMENT 98, TRAINING 121, INTERVIEW 1 (sums to 307), and all 87 are
    `status=published` ∧ not private. `:144`'s "0 `job_position` rows" also holds — the
    `job_position` table has no entry in the capture manifest at all. ✔
11. **`storage.md:67-82` — the five `StorageService` RPC methods.** Enumerated every
    `func (s *RPCServer)` in `storage/internal/rpcsrv/rpcsrv.go`: `PutObject :28`, `GetObject :49`,
    `PutPublicObject :69`, `GetPresignedUrl :90`, `GetPublicObject :112` — exactly the doc's
    3 private + 2 public split, with the 15-minute presigned default confirmed at `:100-102`. ✔
12. **`askengine.md:61-64` — the `/ask` route list.** Enumerated all eleven `askRouter.*` registrations
    at `app/internal/web/backend/backend.go:170-188`. All eleven are in the doc, including the admin
    quartet **`/ask/admin/auto-rules/*`** — and the doc is **right where the platform's own handler
    doc-comments are wrong** (`internal/web/backend/ask/admin.go:77,102,129,173` say
    `/admin/ask/auto-rules/…`, but `e.Group("/ask", …)` at `:170` makes the real path
    `/ask/admin/auto-rules/…`). The CORS + Clerk-authn + skips-OpenAPI-validation framing matches the
    group's own comment verbatim. ✔
13. **`askengine.md:47-48` — the "~60 `TableDef`s" registry.** Enumerated `{Name:` literals in
    `app/internal/askengine/registry.go` = **60**, with `Schema:` distribution 57 `public` / 3
    `directus`, 16 of the names dot-qualified `jobsimulation.*` (resolving to `public` via `RealName`),
    plus 6 `skiller.*` + 1 `skillpath.*` aliases generated at `:550-576`. Matches. ✔
14. **`dependency_map.md:19` + `storage.md:14,21,155-156,169` — the whole storage picture.**
    Enumerated: `depends_on` at `docker-compose.yml:213-217` (redis + postgresql, both
    `service_healthy`); **no** `DB_CONNECTION`/`REDIS_ADDR` in storage's compose env (`:204-210`); no
    `redis` require in `storage/go.mod`; `STORAGE_S3_PUBLIC_BUCKET` hardcoded at `:210` and `:324`
    genuinely inside the studio-desk block; **0** `*_test.go` files repo-wide while `Dockerfile:18` is
    `RUN go test -v ./...`; **0** `//go:generate` directives; gqlgen present only as `// indirect`;
    binary port defaults `8080`/`8081` at `cmd/root.go:45-46`; fallback paths
    `/tmp/anthropos-storage/` (`internal/storage/storage.go:36`) and `/tmp/anthropos-public-storage/`
    (`:174`); `GetPresignedUrl` returns `"", nil` on an empty bucket at `:122`. Every one holds. ✔
15. **`service_taxonomy.md:299-305` — the correction-of-a-correction on the removed Directus compose
    service.** Re-derived the *correction* (§5 iter-22), not just the anchor:
    `git show a2a3ee6^:docker-compose.yml` → `:384 image: directus/directus:10.10.1`,
    `:386 8055:8055`, `:409 ADMIN_PASSWORD=password`, and **no** `ADMIN_EMAIL` line anywhere — which is
    precisely the doc's carve-out that only the `admin@example.com` *email* is unfound. `a2a3ee6` is
    dated 2026-02-27 as stated. ✔
16. **`service_taxonomy.md:200-206` — "`gen.py`'s parser registers exactly nine arguments … and
    `parse_argument` uses `parse_known_args`".** Enumerated: `app/studio/gen.py:484-492` = exactly 9
    `add_argument` calls, matching the doc's list one-for-one; `:19` `args, unknown =
    parser.parse_known_args()` with the leftover merge at `:22-26`. No `--template` anywhere. ✔
17. **`service_taxonomy.md:150-153` — Studio-Desk has no framework.** Enumerated: `grep -niE
    '"(react|vue|angular|@angular|svelte)' package.json` → 0 matches (positive control: 90 quoted
    tokens in the file, `"next"`-style deps present in sibling repos), and `find` for `*.tsx`/`*.jsx`
    outside `node_modules` → **0**. Also cleared `:163` "Multi-language support (7 languages)" —
    `tools/sim-blueprint-gen/lib/parseArgs.js:12-20` enumerates exactly 7 locales, and `:161`
    "GPT-5.x" — `src/services/ai/config.ts:27-32` defaults are `gpt-5.2`/`gpt-5-mini`/`gpt-5-nano`. ✔
18. **`service_taxonomy.md:224,227-230` — the Ant Academy platform-dependency block.** Every anchor
    resolves to the exact construct: `code/src/graphql/server.js:14` (the endpoint read) and `:18`
    (the `throw`); `backendContent.js:102-103`; `serverTenant.js:115-145` incl. the verbatim
    *"there is NO FS-as-published fallback"* / *"not reversible-on-error"*;
    `tests/unit/next-scaffold.test.js:106,111` and `react-compiler-config.test.js:41` (serwist
    removal; `grep -niE 'serwist|workbox' package.json` → 0, positive control `"next": "^16.2.6"`);
    `public/academy-manifest.json` `"display": "standalone"` declared at `app/layout.jsx:132`;
    the demo-patch `demo-stack/patches/academy-fs-published-fallback` exists in rext. The Cosmo claim
    is exact too — `ucourses/ucourse-engine/assistant/agent.js:12-13,32`:
    `https://api.openai.com/v1/responses`, `MODEL = 'gpt-5.2'`,
    `localStorage.getItem('openai_api_key')`, and `app/api/ai/chat/route.js:10-11` independently
    states *"First-party Cosmo doesn't call this endpoint (it calls OpenAI directly from the browser)"*. ✔
19. **`service_taxonomy.md:181` — Studio-Room is pulled in by CI via `additional_repo`, not a
    submodule.** `app/.github/workflows/build-production.yml:29-30`
    `additional_repo: "anthropos-studio-room:studio"` / `additional_repo_commit: latest`, with the
    in-file rationale *"our reusable build workflow does NOT init git submodules"*. The corpus is
    right and `app/Dockerfile:42`'s "git submodule" comment is the stale one. `:176` "Python 3.11" is
    confirmed by `app/Dockerfile:28` / `Dockerfile.dev:26` `FROM python:3.11-slim`. ✔
20. **`hiring.md` — the entire `file:line` anchor set.** I opened and read the surroundings of all of
    them; **every one resolves to the exact construct named**, which is unusual and worth recording:
    `20260729133514.sql:58-62` (*"5. Drop the mirrors."* → `DROP TABLE "local_jobsimulation_sessions"`
    at `:62`); `SET "score"` = **0** hits set-wide (positive control: `DROP TABLE` returns hits);
    the only `CREATE SCHEMA` in the whole migration set is `auth`; `atlas.hcl:8` `search_path=public`;
    `20260722104506.sql:2` (CREATE), `:12`, `:13`, `:29`, `:53`, `:79` (`DROP TABLE "sessions"`);
    `20260722081626_jobsim_data_model.sql:336/355/376`; `20250416091037.sql:5` (`anticheat_summary`);
    `ent/schema/job_simulation_session.go:39,45`; `ent/schema/anticheat_result.go:24`;
    `ent/jobsimulationsession.go:181-186`; `intelligence.go:885-886, 1700, 1738-1751, 1820, 1844, 1846`
    and the exact three-key sort default `score DESC / completition_status ASC /
    session_started_at DESC` at `:1751-1763`; `resolver_queries.go:1034, 1035, 1053, 1080, 1085, 1089`;
    `resolver_cms_queries.go:95, 99-103`; `organization/manager.go:448, 485`;
    `siminvitationlink.go:62`; `graph.go:129546-129554` + the proto-bound twin `:129392-129400`;
    `jobsimulations.graphqls:14, :128`; `useGetClerkOrganization.tsx:16-18, 20-21`;
    `useNavbarSections.tsx:459-466, 460`; `template.tsx:90`; `FreeTrialContainer.tsx:29`;
    `apps/web/.../UserStatusContext.tsx:144-149, 168-172`; `apps/hiring/.../UserStatusContext.tsx:125,
    144-145`; `packages/graphql/src/query/insights.ts:31-82`; `InsightsByMembersContainer.tsx:359`
    (a plain Ant `<Drawer>`); **0** intercepting-route dirs under `apps/` (positive control: 539 dirs,
    and `(protected)`/`(authenticated)`-style paren dirs *do* match); rext
    `persona_write.go:91, 152-158` and `hiring_config.go:99`. `organizations.is_hiring boolean NOT NULL
    DEFAULT false` confirmed via `20240821123449.sql:2` + the `20250129095034.sql:2` rename +
    `ent/schema/organization.go:39`. ✔
21. **`askengine.md` scalars.** `rules.md` = **146,219 bytes** (≈146 KB); `MaxInlineRows=200`
    (`executor.go:18`), `MaxCellLength=400` (`:21`), `QueryTimeout = 10 * time.Second` (`:24`, applied
    as `SET LOCAL statement_timeout` at `:97`); `askEngineMaxConns = 6` (`main.go:149`, applied `:335`);
    `maxAgenticIterations = 15` (`handler.go:34`), `loopTimeout = 10 * time.Minute` (`:41`),
    `context.WithoutCancel` at `:248`; `DefaultModelID = "eu.anthropic.claude-sonnet-4-6"` /
    `DefaultRegion = "eu-west-1"` (`bedrock.go:25-26`), `stripAnthropicAuthMiddleware` (`:185,228,252`);
    all five `ask_*` Ent schemas present; test counts **49 / 13 / 10 / 14** exact; the release
    versions `v1.267.0 - 2026-05-07` (`CHANGELOG.md:1538`) and `v1.340.0 - 2026-07-17` (`:586`). ✔

**RE-DERIVED only** (weaker — flagged as such per §5 rule 24): none of the clearances above rest on
re-computing an arithmetic the document displays. Where a document showed its own derivation
(items 7, 3, 6, 13) I enumerated the underlying set first and stated its cardinality before comparing.

---

## What I could not settle

- **`shared_libraries.md:77-82`'s remaining eleven names, and whether the true proto service count is
  13 or higher.** The `proto` module is not cloned in `stack-demo/` and is absent from the local
  `GOMODCACHE` (only `ai@v1.40.1` is there). I could confirm ten `*v1connect` packages by their import
  paths across the seven repos (backend/users, backend/organizations, cms, jobsimulation, lab,
  messenger, roadrunner, sentinel/authorization, skiller, storage) — `RealtimeService`,
  `ChronosService` and `SkillPathSessionService` are imported by nothing here, so their existence in
  `proto` is unverified in either direction. Settling it needs a clone of
  `anthropos-work/proto` and an enumeration of `proto/**/*.proto` `service` declarations. **B1 does
  not depend on this** — `StorageService` is proven present and served.
- **`dependency_map.md:27` — "production terraform: `skiller_rpc_addr = http://backend:8081`".**
  `messenger/terraform/variables.tf:87` declares `skiller_rpc_address` with **no default**; the value
  is supplied from the `anthropos-work/infrastructure` repo, which is not cloned. It is consistent
  with `app/terraform/locals.tf:8 rpc_port = 8081`, but not verifiable here.
- **`shared_libraries.md:48-59` — the colony package inventory, the 30 s DB health monitor, and
  "the GraphQL rate limiter's reject path is commented out".** `colony` is a private module, not
  cloned and not in the module cache. Settling needs a clone at `v0.35.2`.
- **`shared_libraries.md:188` — "the repo is a **131-line** node-id library (`node.go`)"** and the
  `NodeID` format/example claims at `:224-229`. `taxonomy` is likewise not cloned. (The three worked
  examples — `K-GOXXXX-F63F`, `K-DOTNET-DDE9`, `K-CSHAXX-5F5B` — are internally consistent with the
  stated `^[CSKJI]-[0-9A-Z_]{6}-[0-9A-F]{4}$` regex, which is the most I can say.)
- **`hiring.md:361-400` — the M227/M228 render-gate numbers** (5/5 positions, ~8 candidates each,
  scores 27–100, recruiter p95 1.27 s, "4/4 flake"). These are live-run measurements on the `billion`
  stack; the harness (`stack-verify/e2e/tests/render-hiring-comparison.spec.ts`) is a rext artifact and
  the numbers cannot be re-derived without a bring-up.
- **`service_taxonomy.md:3,67,330,336` — "the `graphql-wundergraph` GitHub repo was archived
  2026-07-30"** (and the parallel archive dates for `skiller`, `skillpath`, `jobsimulation`). GitHub
  archive status is not observable from a local clone; only the *absence* from `repos.yml` and
  `docker-compose.yml` is (and that is confirmed).
