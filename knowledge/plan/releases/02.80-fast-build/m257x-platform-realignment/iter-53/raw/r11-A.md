# Seat A — reading #11

Ground truth read at the briefed shas: `stack-demo/app` @ `5ba17044`, `stack-demo/app/studio` @ `aeec036a`,
`stack-demo/platform` @ `2adcf714`, `stack-demo/graphql-wundergraph` @ `60c229f3`,
`stack-demo/next-web-app` @ `bb3313bc`, `stack-demo/studio-desk` @ `14a5442a`,
`stack-demo/ant-academy` @ `9c3843cd`, `.agentspace/rosetta-extensions` @ `f93ff73` (main).

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| corpus/architecture/external_services.md | 831 | yes |
| corpus/architecture/security_compliance.md | 281 | yes |
| corpus/services/backend.md | 271 | yes |
| corpus/services/academy-backend.md | 141 | yes |
| corpus/services/coursebuilder.md | 139 | yes |
| corpus/services/skiller.md | 66 | yes |
| corpus/services/TEMPLATE.md | 46 | yes |

Search-pipeline hygiene: every `grep`/`comm` below was run with stderr visible. Two engine rejections were
caught and repaired mid-pass (zsh refusing an unquoted `--include=*.go` glob, and zsh's `${s}:path` git-rev
substitution) — in both cases the *first* run returned an empty/garbage result that would have read as
"no matches". Each absence claim below is paired with a positive control that returned non-zero.

---

## Blockers

### B1 — `external_services.md` states the pre-drop router `depends_on` as two services; the compose said four

- **Anchor:** `corpus/architecture/external_services.md:409-412` —
  > From `docker-compose.yml` *before the drop*, the gateway `depends_on`:
  > - backend
  > - storage

- **Why it is false:** the `graphql` service's `depends_on` in `platform/docker-compose.yml`, at the last
  state before the drop (`b56d731^`), is **four** entries, not two:

  ```
  depends_on:
    backend:        condition: service_started
    jobsimulation:  condition: service_started
    cms:            condition: service_started
    storage:        condition: service_started
  ```

  Verified at `platform` `git show b56d731^:docker-compose.yml` (graphql block, lines 5-29 of the services
  map) and identically at `git show 1e8e754:docker-compose.yml`. Neither of the two candidate readings of
  *"before the drop"* yields `backend, storage`:
  - `b56d731^` / `1e8e754` (the live block) → **backend, jobsimulation, cms, storage**;
  - `b56d731` itself (the block parked behind `profiles: [wundergraph-deprecated]`) → **no `depends_on` key
    at all** — verified `git show b56d731:docker-compose.yml`, `  graphql:` at `:22`, block ends at
    `profiles: [wundergraph-deprecated]` with no `depends_on`.
  - and going further back only *adds* entries: `2c85211^` lists backend, skiller, jobsimulation,
    intelligence, cms, skillpath, storage.

  So there is no commit in the history at which the enumeration in the doc was true. This is the
  "a correction can be INCOMPLETE rather than wrong" class (§5, M257x iter-23): the two names it does give
  are real, and the row reads as freshly verified. The consequence is live even though the section is
  fenced HISTORICAL — the very next sentence (`:413`) reasons *from* this list ("It starts after these
  services have reported 'started'"), and a reader reconstructing why the local router raced its subgraphs
  is told the router did not wait on `jobsimulation` or `cms` when in fact it did.
- **Why I hesitated:** the passage is inside a `> HISTORICAL` fence that says "do not follow any of it as a
  local-development instruction". Booked as a BLOCKER anyway because it is a **wrong enumeration of a
  concrete file's contents**, presented as a derivation, and the corpus elsewhere leans on this section as
  the authority on the router's pre-deletion shape.
- **Confidence:** high (on the fact); the grading is medium-high.

### B2 — `backend.md` describes a top-level `migrations/` directory in `app` that does not exist

- **Anchor:** `corpus/services/backend.md:253` —
  > Versioned Atlas migrations live in `terraform/migrations/` (per `atlas.hcl`: `dir =
  > "file://terraform/migrations"`, source `ent://internal/data/ent/schema`), **not in the top-level
  > `migrations/` dir (which holds only `atlas.sum`)**.

- **Why it is false:** at `app` @ `5ba17044` there **is no top-level `migrations/` directory**:
  - `ls -a migrations/` → `ls: migrations/: No such file or directory` (exit 1);
  - `git ls-files | grep -E '^migrations/'` → **0 rows** (so it is not a `.gitignore` artefact — it is not
    tracked either). Positive control in the same pass: `git ls-files | grep -i atlas.sum` returns exactly
    one row, **`terraform/migrations/atlas.sum`**;
  - top-level directories at HEAD are `cmd/ docs/ internal/ knowledge/ scripts/ stripe/ studio/ terraform/
    testdata/` — no `migrations/`.

  The `atlas.hcl` half of the sentence is correct and I verified it directly (`atlas.hcl`:
  `src = "ent://internal/data/ent/schema"`, `dir = "file://terraform/migrations"`). It is the
  *disambiguating parenthetical* that is false: it asserts the existence of a directory, and asserts its
  contents, and a reader who follows it (`cat app/migrations/atlas.sum`, or who treats a missing
  `migrations/` as a broken clone) is misled by a sentence written specifically to orient them. This is the
  briefing's "wrong file path / named thing that does not exist" class.
- **Why I hesitated:** the load-bearing claim of the sentence (where migrations actually live) is right, and
  the falsehood is in the contrast clause. That is exactly the shape §5 rule 18(a) says to expect
  ("the errors will not be in the `file:line` anchors — those verify — but in the surrounding prose"), so I
  booked it rather than downgrading it.
- **Confidence:** high.

---

## Minors

- `corpus/services/backend.md:39` — cites `main.go:1178-1218` for "the mux registers **five** handlers
  unconditionally", but the fifth (`labSessionPath, labSessionHandler` → `mux.Handle`) is at **`:1219`**,
  one line outside the quoted range. The count of five is correct (`:1178`, `:1179`, `:1187`, `:1195`,
  `:1219`), as is the conditional `CMSService` at `:1203-1205`.
- `corpus/architecture/security_compliance.md:72-73` — anchors `UserMixin`'s `Policy()` at `mixin.go:98`;
  the `func (UserMixin) Policy() ent.Policy {` line is **`:99`** (`:98` is its doc comment). Its sibling
  anchor in the same blockquote, `mixin.go:126` for `OrganizationMixin.Policy()`, *is* the `func` line —
  so the two anchors use different conventions three lines apart.
- `corpus/architecture/security_compliance.md:233` — `skills.go:53-64` for `calculateSkillScore`; the
  function is **`:53-62`** (`:63` blank, `:64` is the `run` signature). `:75` for the division is exact.
- `corpus/architecture/external_services.md:429` — the verbatim quote *"Since cms-in-app the platform
  compose `graphql` service builds from the **production** Dockerfile"* is attributed to
  `graphql-wundergraph/CLAUDE.md:39`; that exact string is at **`:33`** (the table row). `:39` restates the
  same assertion in different words, so the claim survives, but the quoted line is not the anchored one.
  The rest of the sentence (written 2026-07-30 in `60c229f`, titled *"correct the compose build path"*)
  verifies exactly.
- `corpus/services/coursebuilder.md:113-114` — "`~55` test files" (actual: **59** at
  `internal/coursebuilder/*_test.go`, **61** including subdirectories) and "`~25` boundary test files"
  (actual: **32** at `internal/web/backend/coursebuilder/*_test.go`). The second is ~28 % low. "~100 Go
  files" for the engine is fine (98 top-level / 102 with subdirs).
- `corpus/services/backend.md:25` vs `:218` — the banner says *"`app` owns the `skiller`, `skillpath`,
  `jobsimulation`, `cms` and `ai_usage` Redis Streams"*, omitting the `backend` stream that `:218`
  correctly names as one of the five application streams. `main.go:1311`
  (`subServer.AddSubscriber(serviceName, backendSelfSub)`, `SERVICE_NAME=backend`) confirms `:218`. Reads as
  a scoped list (merged services only), so non-material, but the two sentences give different sets.

---

## Positively cleared (audited zeros)

**ENUMERATED** (predicate re-derived from source, cardinality stated before any arithmetic):

1. **`security_compliance.md:66-137` — the whole multi-tenancy fence.** I enumerated the sets, not the sums.
   - `ls *.go | wc -l` in `app/internal/data/ent/schema` = **139**; `grep -l 'ent.Schema' *.go | wc -l` =
     **135**; the 4 non-schemas are `database_types.go`, `mixin.go`, `skiller_mixins.go`,
     `skillpath_mixins.go`. Matches `:68` and `:107`.
   - `grep -lE 'OrganizationMixin\{\}' *.go` = **30 files**; I printed the matching line of every one:
     29 are live `OrganizationMixin{},` members, and `user_resource.go:22` is
     `// OrganizationMixin{},  // We need to work on this` — **commented out**. Live set = **29**. Matches
     `:76`, `:82-88`, `:135`.
   - `OrganizationIDMixin{}` = **7**, and they are exactly the seven named at `:99-100`
     (`category`, `jobrole`, `similarity`, `skill`, `specialization`, `studio_document`, `studio_task`).
   - The document's own `comm`/`xargs` derivation returns **18**; I printed the 18 and diffed them against
     the 16 the doc names: the difference is exactly `org_membership.go` + `academy_feedback.go`, the two
     the doc subtracts and for the reasons it gives. So 16 + 7 = **23** carrying `organization_id` with no
     policy — verified as a *set*, not a total.
   - The "19th hit, `skiller_mixins.go`" caution at `:109` is real: I re-ran the bare-glob variant and it
     returns **19**, the extra file being `skiller_mixins.go`. (It escapes the mixin filter because it
     writes `func (OrganizationIDMixin) Fields()`, never the literal `OrganizationIDMixin{}`.)
   - `grep -nE '\) Policy\(\)' *.go` = **exactly 4 files** — `mixin.go` (×2, `:99` UserMixin, `:126`
     OrganizationMixin), `org_membership.go:172`, `organization.go:56`, `user.go:116`. Matches `:110-112`.
   - **I checked the CONJUNCTION, not just the total** (§5 rule 17/24): `Membership.Policy()` is
     `org_membership.go:172-188` and does end in `privacy.AlwaysDenyRule()` in both branches;
     `Organization.Policy()` is `organization.go:56` with `rule.FilterSameOrganizations()` at `:96` and uses
     neither mixin; `User.Policy()` (`user.go:116`) filters by user (`rule.DenyNotSameUser()`), correctly
     excluded. I then went looking for the *set-expansion* trap the rule warns about: neither
     `skiller_mixins.go` nor `skillpath_mixins.go` introduces any org policy (both are explicit that they
     port a **plain** column *"NOT backend's OrganizationMixin (which carries a privacy policy)"*), so no
     schema is org-filtered by an indirect route. And `user_resource.go` — the one whose mixin is commented
     out — declares **no `organization_id` field**; it reaches Organization by an *edge*
     (`edge.From("organization", …)`) and is policed by `UserMixin`, so the doc's closing
     *"`user_resources` is user-scoped, NOT org-scoped by the ORM"* is right. **31 is correct, and this
     time by the correct derivation.**
2. **`external_services.md:362-375` — the per-commit subgraph table.** I read
   `supergraph-config-prod.yaml` at each cited rev and counted `^  - name:` entries:
   `749dc86^`→**5** (backend, skiller, jobsimulation, cms, skillpath), `749dc86`→**4**, `7c17e63`→**3**,
   `915da06`→**1**, `HEAD`→**1**. Dates confirmed: `749dc86` 2026-06-24, `7c17e63` 2026-07-21, `915da06`
   2026-07-29. `git log --diff-filter=D` over the four subgraph SDLs confirms `915da06` deleted **both**
   `schemas/jobsimulation.graphqls` and `schemas/cms.graphqls`. The **3 → 1** claim and the explicit warning
   *"do not take the count from `915da06`'s own commit subject (2→1)"* are both correct — the subject does
   say 2→1 and the tree it was committed against does list three.
3. **`external_services.md:376` — "no subscriptions".** `grep -rn "sse\|subscription" *.yaml` → 0 with
   stderr visible; positive control `grep -rln backend *.yaml` matches all three. `git log -S 'protocol:
   "ws"'` → **0 commits**; positive control `git log -S sse_post` → **5**. `bba862f` is real
   (2026-02-25) but `git merge-base --is-ancestor bba862f HEAD` → **rc 1** and
   `git branch -a --contains bba862f` names only `remotes/origin/feat/use-web-socket`.
   `schemas/backend.graphqls` has `type Mutation` at **`:4053`**, `type Query` at **`:4912`**, and **no**
   `type Subscription`; all **6** `Subscription` substring hits are Stripe/plan identifiers.
4. **`external_services.md:170-172` — "nine containers on the default `graphql` profile".** I enumerated
   every service and every `profiles:` key: `common.yml` declares `postgresql` + `redis` with **no**
   `profiles:`; `docker-compose.yml` declares 11 services of which `sentinel` (`:5`) also has no
   `profiles:`, and `graphql` appears in the profile list of `backend`(`:81`), `jobsimulation`(`:140`),
   `cms`(`:187`), `storage`(`:218`), `roadrunner`(`:309`), `gotenberg`(`:384`). Excluded:
   `customerio-sync`(`:238`), `messenger`(`:279`), `studio-desk`(`:342`), `next-web-app`(`:369`).
   2 + 7 = **9**, and the seven named are the seven that start.
5. **`external_services.md:630-635` — the env-name counts.** Ran the document's own command on `app` @
   `5ba17044`: `OPENAI_KEY`=**5**, `ANTHROPIC_API_KEY`=**26**, `AZURE_OPENAI_KEY`=**13**,
   `AZURE_OPENAI_ENDPOINT_URL`=**13**, `OPENAI_ORG_ID`=**0**, `AZURE_OPENAI_ENDPOINT`=**0**,
   `AZURE_OPENAI_DEPLOYMENT`=**0**, `OPENAI_API_KEY`=**2** — and I opened both `OPENAI_API_KEY` hits
   (`internal/cms/studio/studioManager.go:1049`, `:1067`) and they are the studio-subprocess remap, as
   stated. All eight exact.
6. **`external_services.md:601-605` / `:541` — the Studio-Room latency claim.** All `*_AI_*_MODEL` lines in
   `app/studio/configs/*.ini` = **30**, and **30 of 30** pin `azure` (`uniq -c` over the values returns one
   bucket). `gen.py:41-53` overrides only `*_API_KEY`/`*_ENDPOINT` and never the service selector.
   `grep -rn "bedrock|boto3"` under `app/studio/` = **0** — so *"Studio-Room was never on Bedrock"* holds.
   `services/ai.py`: `AnthropicProvider` spans **`:627-664`** exactly; `get_client`'s provider dict is
   `:704-724` with the `openai` arm at `:706-708`; `return OpenAI(api_key=self.api_key)` at **`:383`**;
   `config_template.ini:30-31` is the `api.openai.com` pair. The four-live-plus-one-latent count is right.
7. **`external_services.md:649-662` and `skiller.md:40-49` — the taxonomy figures.** I opened the capture
   manifest `.agentspace/snapshots/taxonomy/5afc0bccf1df7ef538b643321fc6362f/manifest.json` and read its
   keys: `source=primary-read`, `public_only=True`, `predicate=org-null`,
   `captured_at=2026-06-29T14:43:00Z`. Per-table row counts: `public.skills` **42,790**, `public.job_roles`
   **22,470**, `public.skill_embeddings` **42,790**, `public.job_role_embeddings` **18,919**,
   `categories` 23, `specializations` 1,447 — each with its `organization_id IS NULL` filter recorded.
   Every figure quoted in both files matches, including the "18,919 is the *embedding* row count
   transcribed onto the role count" diagnosis.
8. **`backend.md:183-185` — 13 `ai_readiness_*` tables.** `ls internal/data/ent/schema | grep -i readi` =
   **13**, and the four the doc says a "9" omits (`recommendation`, `email_override`, `notification_log`,
   `notification_optout`) are all present, as are `live_snapshot` + `text_translation` which it says are
   *not* omissions. `internal/aireadiness/` exists with `manager.go`/`cycles.go`/`diagnosis.go`/
   `compare.go`/`csv.go`; `ls internal/workforce | grep -i readi` → **0** (exit 1), so *"not
   `internal/workforce/`, which contains no `readi*` file at HEAD"* holds.
9. **`academy-backend.md` — the whole Ent-table list.** `ls internal/data/ent/schema | grep -i academy` =
   **11 files**, matching the 6 per-user + 5 catalog the doc lists, with no extras.
   `grep -hoE 'CREATE TABLE "academy_[a-z_]+"' terraform/migrations/*.sql | sort -u` returns the **11
   plural** table names — confirming the doc's load-bearing *"these are Ent LABELS; the TABLE names are
   plural"* warning. Also verified: `/content/catalog.json` (`internal/web/backend/content.go:23`),
   the `ACADEMY_CONTENT_API_TOKEN`-guarded `/content/admin` group (`content_admin.go:35`),
   `academy_embedding_refresh` @ 03:00 (`internal/worker/tasks/academy_embedding_refresh.go:10`,
   `internal/academy/embeddings.go:108`), all four `cmd/` binaries, `academy-seed`'s six flags verbatim,
   `academyImport`'s `--manifest`/`--content-root`/`--checkpoint`, PR `0e37771f` (2026-06-05,
   *"Academy backend v1.0 'ground truth'"*), and `internal/academy/academy.go:6-9` — the exact four lines
   `backend.md:126-129` cites for the `aiacademy` removal.
10. **`coursebuilder.md` — the SSE contract and route table.** The 13 `event:` names the doc lists are
    exactly the 13 event-name literals in `internal/web/backend/coursebuilder/*.go` (`sort -u`, no extras,
    none missing). Every route in the doc's list appears in `handler.go:Register` (`:2842`, routes at
    `:2859-2926`). Credits verified from `internal/credits/cost.go:87-89`: `course.build`=**5**,
    `course.refine`=**1**, `course.translate`=**1**. `DefaultSessionsPerOrgPerDay = 50` (`rate.go:28`),
    `DefaultMaxMonthlyCOGSUSD = 500.0` / `DefaultMaxDailyCOGSUSD = 0.0` (`budget.go:27`,`:41`),
    `coursebuilderWorkerConcurrency = 20` (`main.go:172`), `TypeCoursebuilderRun = "coursebuilder_run"` +
    `MaxRetry(1)` + `Retention(6*time.Hour)` (`run_task.go:49`,`:199`,`:204`), `go 1.26.4`.
11. **`external_services.md:685` — the LiveKit agent names.** `calls/livekit.go`: `:110`
    `agentName := "anthropos-agent"`, `:115` `"anthropos-agent-chain"`, `:120` the EU re-assert, `:126`
    `fmt.Sprintf("anthropos-agent-%s", *location)`, and `agentEndpoint` carrying `azure-eu`/`azure-%s`.
    `grep -rn "anthropos-agent-eu"` over the whole `app` tree → **0**. And `flag_use_realtime_openai`
    (`:133`) sets `agentEndpoint = "openai-hosted"` — it selects an *endpoint*, not an engine, exactly as
    `:689` says; the nil `voice_engine` default is `gptrealtime`
    (`internal/cms/directus/collections/jobsimulation.go:1595-1596`).
12. **`external_services.md:729-730`** — `NEXT_PUBLIC_GRAPHQL_ENDPOINT` in `next-web-app` → **0 hits**,
    with `NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT` as the positive control (3+ hits incl.
    `packages/graphql/src/server/server.graphql.ts:4`). And **no** `graphql-wundergraph` entry in
    `platform/repos.yml` (I read the whole file; 9 repos, none of them the router).
13. **`security_compliance.md:147`** — all three hooks exist in **both** `apps/web` and `apps/hiring`, at
    the exact cited lines: `useOrgSelection.tsx:94`, `useResolveActiveOrg.tsx:107`,
    `useActivateMembershipOrg.tsx:81`, each a `clerk.setActive({ organization: … })`. Six files, six exact
    anchors.

**Anchors opened and read in context (not clearances on their own, but each resolved to the construct
claimed — I read ±10 lines around every one):** `app/main.go:735-738`, `:756-762`, `:816-819`, `:971-973`,
`:1178-1219`, `:1233-1234`; `app/terraform/variables.tf:635-638`, `ssm.tf:328-333`, `main.tf:555`;
`app/internal/coursebuilder/bedrock.go:23,29,98-103,105-114`;
`app/internal/jobsimulation/ai/ai.go:30-33,80,85-88,129,150-155,259-289,264-276,280-283`;
`.../v3/validator/skills.go:53-62,75` and `validator.go:60-61` (**one** engine registered);
`.../basevalidator/criterion.go:34,72,90,127,168,428,450-475` (`checkerEngines` stored, never read — 3
occurrences total, all writes); `templates/checkValidationBulk.tmpl` exists;
`app/internal/cms/directus/collections/jobsimulation.go:905,1302`;
`.../simulator/ai/ai.go:58-59,114-115`; `app/internal/cms/studio/markdownManager.go:19`,
`studioManager.go:583` (Mistral: **0** hits anywhere in `app/internal` outside `internal/cms/`);
`app/internal/web/backend/backend.go:130`; `graphql-wundergraph/terraform/locals.tf:8`,
`main.tf:20,48-49`, `config.prod.yaml:5`, `Dockerfile.dev:7,18,19-20,23,29`, `subgraphs.conf`;
`platform/docker-compose.yml:43-67` (**0** `DIRECTUS_*`), `:164-165`, `:255-265`, `:318,334,352,361`,
and the 4 `SKILLER_RPC_ADDR=http://backend:8083` occurrences (`:62,121,174,265` — "all four" is exact);
`rosetta-extensions/stack-injection/gen_injected_override.py:53,636-637` and
`tests/test_injection.py:1051`. The three-era Dockerfile table (`63d285c` 2024-06-20 named `wundergraph`
with a git-URL context and no `dockerfile:` key; `2c85211` 2026-02-27 introducing `dockerfile:
Dockerfile.dev`; `67ba772` 2026-03-02 raising the context; `360efd4` 2026-07-31 deleting the block) all
reproduce, including the `"5050:8080"` host mapping and `b56d731`'s `wundergraph-deprecated` parking.

**`TEMPLATE.md`** (46 lines) is a pure placeholder skeleton with no factual claims to grade — nothing to
book either way.

---

## What I could not settle

- **`backend.md:71-74`** — the 2026-07-08 production row counts (`public.skills` 43,584 total / 794
  org-private, `job_roles` 22,490, `skill_embeddings` 43,584). These are read-only prod measurements; the
  ground-truth clones cannot reproduce them and no local stack was running. The *public-capture* half of the
  same paragraph (42,790 / 22,470 / 18,919) I did verify against the manifest. Settled by a `/db-query`
  read against prod.
- **`external_services.md:3`** — *"the repo is ARCHIVED on GitHub (2026-07-30)"*. Archive status is a GitHub
  API property, not visible in the clone. Settled by `gh repo view anthropos-work/graphql-wundergraph
  --json isArchived`.
- **`security_compliance.md`** infrastructure/compliance section (VPC CIDR, subnet layout, TLS 1.3 cipher
  set, AES-256/KMS, 6-hour backups to S3/Azure/Hetzner, 90-day retention, "DPA v1.4", "18 approved
  sub-processors", "monthly patch updates", "ECS health checks every 30 seconds"). None of this is derivable
  from the 13 briefed clones — the terraform in `app/terraform/` covers the app task definition only, and
  there is no VPC/network module or DPA artefact in scope. Settled by the platform infra terraform repo and
  the legal/DPA source.
- **`external_services.md:528`** — the shared `ai` library's provider set (`OpenAI, Azure, Anthropic,
  Bedrock, Mistral` behind one `ai.AI`). `anthropos-work/ai` is a private Go module pulled at Docker build
  and is not one of the ground-truth clones. Settled by reading that module at the pin in `app/go.mod`.
- **`coursebuilder.md:129-130`** — *"Waves 1→24 (142 changelog lines)"* and *"the parallel-author pipeline
  was built then deleted (2026-07-21)"*. The `app` clone here is a shallow-history working copy for the
  merge shas I needed; I did not attempt a full `git log --follow` reconstruction of the coursebuilder
  package's wave history, so the 142 and the deletion date are unverified. Settled by
  `git log --oneline -- internal/coursebuilder/` on a full clone.
