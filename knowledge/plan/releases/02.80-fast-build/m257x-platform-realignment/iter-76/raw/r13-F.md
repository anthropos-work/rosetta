# Seat F — reading #13

Corpus HEAD: `1937e1f` (branch `m257x/platform-realignment`).
Ground truth re-verified at open: `app b948604ff` (v1.366.0) · `platform 0dab54d` · `next-web-app bb3313bc0` ·
`ant-academy 9c3843cd` · `studio-desk 14a5442` · `jobsimulation 462343b0` · `cms ca50c817` · `sentinel 88bc5592` ·
`storage 4ce8ece5` · `messenger fa47850d` · `roadrunner 87d8d443` · rext authoring copy on `main`.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/services/ant-academy.md` | 436 | yes |
| `corpus/services/studio-desk.md` | 435 | yes |
| `corpus/architecture/shared_libraries.md` | 242 | yes |
| `corpus/services/jobsimulation.md` | 233 | yes |
| `corpus/services/skillpath.md` | 107 | yes |
| `corpus/architecture/frontend_architecture.md` | 105 | yes |
| (protocol, read first) `corpus/ops/platform-alignment.md` §5 | 1680 total | §5 (240–1145) in full |

Search-pipeline controls run in the same pass: `grep` for `UsersServiceHandler` (3 hits) alongside the
zero-returning `SkillPathSessionServiceHandler` / `RoadRunnerServiceHandler`; `JUDGE0_BASE_URL` (hits at
`app 9d00a313` + `platform/docker-compose.yml:59`) alongside the zero-returning `ROADRUNNER_RPC_ADDR`;
`colony/authn` in `app` (129 files) alongside the zero-returning standalone `anthropos-work/authn`.
All stderr read; no engine rejections.

---

## Blockers

### B1 — ant-academy's architecture diagram shows `cms` and `jobsimulation` as live Tier-1 Docker services

- **Anchor:** `corpus/services/ant-academy.md:90-94` (+ the `Desk --> Core` edge at `:101`)
  ```
      subgraph Core["Core Backend (Tier 1, Docker)"]
          App[app]
          CMS[cms]
          Jobsim[jobsimulation]
      end
  ```
- **Why it is false:** at platform `0dab54d` there is no `cms` and no `jobsimulation` compose service and no
  `repos.yml` entry for either. `platform/docker-compose.yml` declares exactly eight services
  (`sentinel`, `backend`, `storage`, `customerio-sync`, `messenger`, `studio-desk`, `next-web-app`,
  `gotenberg`) plus `postgresql` + `redis` via `include: common.yml`; `platform/repos.yml` (31 lines) lists
  six repos (`app`, `sentinel`, `storage`, `messenger`, `next-web-app`, `studio-desk`) and its own header
  comment says the five folded repos *"own no local schema, no compose service and no clone entry here."*
  This also self-contradicts `corpus/services/jobsimulation.md:72` (*"there is no `jobsimulation` compose
  service"*), `corpus/architecture/shared_libraries.md:42`, and the root `CLAUDE.md` merge banner.
  This is the §5 "arrow in a mermaid diagram" class — no drift vocabulary in the text, so no grep reaches it.
- **Confidence:** high

### B2 — the academy setup section directs the reader to `code/.env`; the whole rest of the corpus (and the tooling) says `code/.env.local`

- **Anchor:** `corpus/services/ant-academy.md:324` — *"The **app's** env file is `code/.env`, not the repo
  root:"* followed by `cp .env.example .env` (`:328`); reinforced at `:59` (*"The **React app's** env lives
  at `code/.env.example`"*) and `:345` (*"set `REQUIRE_ORGANIZATION_MEMBERSHIP=0` in `code/.env`"*).
- **Why it is false:** the ant-academy clone at `9c3843cd` ships `code/.env.example` and carries
  `code/.env.local`; there is no `code/.env`. Five other corpus/instruction sites say `.env.local` and one
  of them says explicitly that `.env` is *absent*:
  - `CLAUDE.md:268` — *"requires its own `.env` file at `ant-academy/code/.env.local` (not the repo root —
    the React app reads **only** from `code/.env.local`)"*
  - `CLAUDE.md:469` — `cp .env.example .env.local   # ... the app reads code/.env.local`
  - `corpus/ops/secrets-spec.md:61` — `ant-academy/code/.env.local  # the exact file Next.js precedence reads (.env is absent)`
  - `corpus/ops/setup_guide.md:393` — `/stack-secrets` *"writes `ant-academy/code/.env.local`"*
  - `.claude/skills/stack-secrets/SKILL.md:58`
  It is materially harmful, not cosmetic: Next.js dotenv precedence puts `.env.local` **above** `.env`, and
  `stacksecrets provision` + `demo-stack/ant-academy.sh` both write `.env.local`. A reader who follows this
  section puts Clerk keys in `code/.env` and has them silently overridden — the exact last-wins trap this
  same file describes at `:204-206` and `:210-213`.
- **Confidence:** high

### B3 — `shared_libraries.md` proto row: "`repos.yml:14-19` still clones them and `docker-compose.yml:83,144` still builds them"

- **Anchor:** `corpus/architecture/shared_libraries.md:69` — *"The husk repos **still carry their own
  `go.mod`** — `repos.yml:14-19` still clones them and `docker-compose.yml:83,144` still builds them"*
- **Why it is false:** the cell names **no ref**, so it is graded at the checkout (`platform 0dab54d`).
  There: `repos.yml` has no `cms` and no `jobsimulation` entry at all, and `:14-19` reads
  `schema: public` / `- name: sentinel` / `type: go` / `migrations: false` / `- name: storage` / `type: go`.
  `docker-compose.yml:83` is `- STORAGE_S3_PUBLIC_BUCKET=production-storage-public…` and `:144` is
  `GH_ACCESS_TOKEN: $GH_PAT` (inside `customerio-sync`'s build args). Neither builds anything named
  cms/jobsimulation, and neither service exists in the file. Both anchors were correct at `2adcf71`
  (`repos.yml:14-19` was the cms + jobsimulation block; `docker-compose.yml:83` was `jobsimulation:`), which
  is why the drift is invisible — but that ref is not named here.
  Self-contradicts the **Imported by** row two lines above (`:42`): *"are **gone from compose** at `0dab54d`
  (`d11a403`)"*. The version-pin values themselves are correct (measured: app/messenger `v1.210.0`,
  cms `v1.207.0`, jobsim `v1.205.0`, sentinel `v1.200.0`, storage/roadrunner `v1.196.0`).
- **Confidence:** high

### B4 — `shared_libraries.md` ai row: "the still-running `cms` and `jobsimulation` husk containers"

- **Anchor:** `corpus/architecture/shared_libraries.md:105` — *"…**and, as their own direct `go.mod`
  requires, the still-running `cms` and `jobsimulation` husk containers** (`cms/go.mod:9`,
  `jobsimulation/go.mod:11`, both `v1.40.2`)"*
- **Why it is false:** the `go.mod` half is exactly right (`cms/go.mod:9` and `jobsimulation/go.mod:11` both
  require `github.com/anthropos-work/ai v1.40.2`) — but there are **no such containers**. At platform
  `0dab54d` neither service is declared in `docker-compose.yml` nor listed in `repos.yml`. Unpinned,
  present-tense (*"still-running"*), and contradicts `:42` in the same table.
- **Confidence:** high

### B5 — `shared_libraries.md` taxonomy row: same "still-running husk containers" + "7 live Go service repos"

- **Anchor:** `corpus/architecture/shared_libraries.md:181` — *"**and the still-running `cms` +
  `jobsimulation` husk containers** … **6 of the 7 live Go service repos** (app, sentinel, storage,
  messenger, cms, jobsimulation, roadrunner)"*
- **Why it is false:** same container refutation as B4. Additionally the enumerated set is wrong: the live
  Go service repos at `0dab54d` are **four** — `app`, `sentinel`, `storage`, `messenger` (`repos.yml`); `cms`,
  `jobsimulation` and `roadrunner` are frozen legacy clones with no compose service and no clone entry.
  The row's own arithmetic (6 of 7, exception = roadrunner) checks out over a set that is not the live set —
  §5 rule 24 exactly: the sum is right, the SET is wrong. The `go.mod` citations are correct
  (`cms/go.mod:13`, `jobsimulation/go.mod:15`, both `v1.2.0`, neither `// indirect`; sentinel `:21` and
  storage `:25` both `// indirect`).
- **Confidence:** high

### B6 — `shared_libraries.md` cites `external_services.md:546` for the EU-first-ladder retraction; `:546` says something else entirely

- **Anchor:** `corpus/architecture/shared_libraries.md:125` — *"(And what the consumers do is not an EU-first
  fallback *ladder* either — `external_services.md:546` retracts that chain.)"*
- **Why it is false:** `corpus/architecture/external_services.md:546` reads
  `> `app` image as an `additional_repo` (app v1.360.1) — the source lives in` — the middle of the
  `app/studio/**`-is-an-in-image-path blockquote (`:544-550`). It has nothing to do with provider routing.
  The retraction is at **`:554`**: *"There is **no ordered EU-first fallback chain.** The corpus asserted one
  for several releases ("Azure → Bedrock → Mistral → OpenAI → Anthropic"); no such ladder exists in the
  code."* The anchor resolves to a real line naming the wrong construct — the §5 rule 21 class-2 defect that
  an existence-only anchor check cannot see. Note the same dead `:546` anchor is cited from at least four
  further sites outside my file set (`CLAUDE.md:245`, `CLAUDE.md:259`, `security_compliance.md:7`,
  `security_compliance.md:198`, `architecture_overview.md:246`), so the repair is claim-scoped, not
  file-scoped.
- **Confidence:** high

### B7 — jobsimulation's banner asserts, in present tense and unpinned, that the husk still starts locally

- **Anchor:** `corpus/services/jobsimulation.md:11-14`
  > **⚠️ But locally the husk still starts.** `docker-compose.yml:83` @ platform `2adcf71` still defines a
  > `jobsimulation` service **in the default `graphql` profile**, and `repos.yml:17-19` still lists the repo
  > … State: `running_but_unfederated`; container teardown is **M810**.
- **Why it is false:** the `2adcf71` pin sits inside the **middle** sentence only, and that sentence *is*
  true there (verified: `git show 2adcf71:docker-compose.yml` line 83 = `  jobsimulation:`;
  `git show 2adcf71:repos.yml` lines 17-19 = the jobsimulation entry with `migrations: false # legacy`).
  But per §5 rule 33 a pin's scope is the claim's own block, and the surrounding sentences are separate,
  unpinned claims: the **heading sentence** *"But locally the husk still starts"* and the **state sentence**
  *"State: `running_but_unfederated`"*. Both are false at `0dab54d` — there is no `jobsimulation` compose
  service (`d11a403` deleted it), no `repos.yml` entry, and the `graphql` profile itself was renamed `core`.
  Rule 33 part 3 applies directly: a block asserting currency cannot be pinned into silence.
  This is the highest-propagation part of the document and it contradicts `:35`, `:72` and `:203` of the
  same file, which all say the service is gone. It is the §5 iter-21 signature inverted — the stale claim
  is in the **banner**, the correction buried in the body.
- **Confidence:** high

### B8 — jobsimulation "Ports" bullet claims the platform compose sets `PORT=8400`/`RPC_PORT=8401` and publishes `8400:8400`/`8401:8401`

- **Anchor:** `corpus/services/jobsimulation.md:71` — *"**Ports**: 8400 (GraphQL/HTTP), 8401 (Connect-RPC) —
  **as deployed by the platform**, which sets `PORT=8400` / `RPC_PORT=8401` and publishes `8400:8400` /
  `8401:8401` (`platform/docker-compose.yml`)."*
- **Why it is false:** unpinned, present tense, and cites `platform/docker-compose.yml` by name. At
  `0dab54d` that file contains **zero** occurrences of `8400` or `8401`; the only `RPC_PORT` values are
  `8083` (backend, `:69`), `8301` (storage, `:121`) and `8201` (messenger, `:181`). There is no
  jobsimulation service to carry them. It contradicts the very next bullet, `:72`
  (*"**Profile**: none — there is no `jobsimulation` compose service."*). A reader adding a stack offset to
  8400/8401 as the bullet instructs would probe a port nothing listens on.
- **Confidence:** high

### B9 — the Migrations recipe cites `repos.yml:17-19` as the jobsimulation `migrations: false` entry

- **Anchor:** `corpus/services/jobsimulation.md:224-226`
  ```
  make migrate   # NOT `S=jobsimulation`. `repos.yml:17-19` sets `migrations: false`, so the
                 # migrating set is `app` ALONE. …
  ```
- **Why it is false:** unpinned. At platform `0dab54d`, `jobsimulation` is **not in `repos.yml` at all**, so
  no entry sets `migrations: false` for it; `repos.yml:17-19` is `type: go` / `migrations: false` /
  `- name: storage` — the *sentinel* and *storage* rows. The conclusion (*the migrating set is `app` alone*)
  is correct, but the evidence anchor asserts a `repos.yml` membership that no longer exists — the same
  false-membership claim B3 makes, from the other direction.
- **Confidence:** high

### B10 — "`0dab54d`'s compose declares nine services"

- **Anchor:** `corpus/services/jobsimulation.md:35` — *"`0dab54d`'s compose declares nine services and
  `jobsimulation` is not one of them."*
- **Why it is false:** ENUMERATED at the named ref. `platform/docker-compose.yml` declares **8** services
  (`sentinel`, `backend`, `storage`, `customerio-sync`, `messenger`, `studio-desk`, `next-web-app`,
  `gotenberg`); its `include: common.yml` (line 1-2) adds `postgresql` and `redis`, giving **10** resolved.
  Neither reading is 9. (Resolving `include:` is load-bearing here — §5 rule 30's own corollary.)
- **Confidence:** high on the count. **I hesitated** because the conclusion the count supports (*jobsimulation
  is not among them*) is true; I booked it as a BLOCKER because a reader who counts nine and finds eight or
  ten has no way to tell which service the corpus thinks exists that does not, and because the number is the
  kind a later pass will cite forward.

### B11 — skillpath says `CMS_RPC_ADDR` "addresses the husk"; compose points it at `backend`

- **Anchor:** `corpus/services/skillpath.md:57` — *"**No `CMS_RPC_ADDR` hop** since cms-in-app (that env var
  still exists, but it addresses the husk for messenger's pre-M809 path)."*
- **Why it is false:** unpinned. At `platform 0dab54d`, `docker-compose.yml:174` reads
  `- CMS_RPC_ADDR=http://backend:8083`, under a comment at `:171-172`: *"cms + jobsimulation are folded into
  app: all four RPC edges are the one backend mux."* There is no husk container to address. This is the §5
  rule 31 expired-refutation class verbatim, and it directly contradicts a corpus sibling —
  `corpus/services/jobsimulation.md:119`: *"compose's `CMS_RPC_ADDR` reads `http://backend:8083` (measured at
  platform `0dab54d`)"* — and `jobsimulation.md:98`'s *"M809 has landed."*
- **Confidence:** high

### B12 — `app/CLAUDE.md:72` cited as still listing `SkillPathSessionService`; line 72 is a code fence

- **Anchor:** `corpus/services/skillpath.md:34-36` — *"(`app/CLAUDE.md:72` and `app/knowledge/architecture.md:28`
  still list the service — Trap C …)"*
- **Why it is false:** at `app b948604`, `CLAUDE.md:72` is a bare ` ``` ` closing a migrations bash block
  (`:70` is a comment, `:71` is `atlas migrate apply --dir file://terraform/migrations`, `:74` is
  `## Architecture`). The claim's actual home is **`app/CLAUDE.md:80`**, the RPC-mux bullet that does name
  `SkillPathSessionService`. The twin anchor `app/knowledge/architecture.md:28` verifies exactly.
  The substantive claim (the planning docs lag the code) is correct; the anchor names the wrong construct.
- **Confidence:** medium. **I hesitated** — this is arguably line drift (a MINOR class under a stricter rule),
  but the cited line is a code fence, not the construct, so a reader opening it learns nothing.

### B13 — `docker-compose.yml:311` for the studio-desk profile is past the end of the file

- **Anchor:** `corpus/architecture/frontend_architecture.md:11` — *"**Studio-Desk** is in `repos.yml` (so
  `make init` clones it) and *does* have a `studio-desk` compose profile (`docker-compose.yml:311`)."*
- **Why it is false:** the sentence names no ref (the `@ 2adcf71` pin is in the **next** sentence, about
  `repos.yml`, so per §5 rule 33 it does not carry). Graded at `platform 0dab54d`,
  `docker-compose.yml` is **271 lines** — `:311` does not exist. The service block is at `:197` and its
  `profiles: [studio-desk, all]` at `:226`. (`:311` was `  studio-desk:` at `2adcf71`, which is why it looks
  plausible.) The claim itself is true; the anchor points off the end of the file.
- **Confidence:** medium

### B14 — `docker-compose.yml:362` for `NEXT_PUBLIC_BACKEND_API_URL` is past the end of the file

- **Anchor:** `corpus/architecture/frontend_architecture.md:39` — *"~15 sites hitting
  `NEXT_PUBLIC_BACKEND_API_URL` (`:8082`, `docker-compose.yml:362`)"*
- **Why it is false:** same mechanism as B13, different anchor and a different sentence from the one carrying
  the paragraph's `2adcf71` pin. At `0dab54d` the file is 271 lines; the variable lives at `:237` (build arg)
  and `:246` (environment), both `http://${PUBLIC_HOST:-localhost}:8082`. `:362` was correct at `2adcf71`.
- **Confidence:** medium

---

## Minors

- `corpus/services/ant-academy.md:137` — `emptyCatalogView() = { chapters: [], skillPaths: {}, series: [] }`.
  Actual (`code/src/lib/serverTenant.js:120-122`) also returns `bundles: PUBLIC_BUNDLES` and
  `catalogVersion: CATALOG_VERSION`. Abbreviation; the "0 cards" conclusion is unaffected.
- `corpus/services/ant-academy.md:147` — `/catalog.json` *"(~2,667 entries)"*. Measured at `9c3843cd`: the
  `courses` array holds **2,715** (7 languages). Content grows, and the figure carries a `~`.
- `corpus/services/ant-academy.md:246` — *"~26 Playwright e2e spec files (tests/e2e/)"*. Measured: **31**
  `*.spec.js` in `code/tests/e2e/`.
- `corpus/architecture/frontend_architecture.md:39` — *"~15 sites hitting `NEXT_PUBLIC_BACKEND_API_URL`"*.
  Measured at `bb3313bc`: **23 files / 32 line-occurrences** across `apps/` + `packages/` (excluding
  `node_modules`).
- `corpus/services/jobsimulation.md:164-168` — the diagnostic recipe
  `docker logs demo-<N>-jobsimulation-1 2>&1 | head -3` names a container that does not exist at `0dab54d`.
  The mechanism it teaches is still real, but the bind now sits on **`backend`**
  (`platform/docker-compose.yml:90-91` — the only AWS bind in the file, which the section correctly asserts).
- `corpus/services/jobsimulation.md:211` — `make dev S=jobsimulation` names a compose service that no longer
  exists.
- `corpus/services/studio-desk.md:21` — the **Service Overview** cell states `depends_on` **`backend` + `cms`**
  pinned `@ platform 2adcf71`. Verified TRUE at that ref (`docker-compose.yml:337-341` is exactly that
  `depends_on` block). At `0dab54d` it is `backend` alone. Correctly pinned, so not booked as a blocker, but
  it sits in a present-tense property table describing how the service runs.
- `corpus/services/skillpath.md:97` — *"the body renders the literal string **"Coming soon"**"*. It renders
  the next-intl key `t('enterprise.insights.comingSoon')`
  (`apps/web/src/components/containers/InsightsBySkillPathStudentSimulationsContainer.tsx:138`), not a
  literal. Everything else in that bullet verifies exactly.

---

## Positively cleared (audited zeros)

**ENUMERATED** (predicate re-derived from source independently, cardinality stated before any arithmetic):

- `platform/repos.yml` @ `0dab54d` — **6** entries: `app`, `sentinel`, `storage`, `messenger`,
  `next-web-app`, `studio-desk`. Clears `shared_libraries.md:23-24` (*"None of these are cloned by `make
  init` — absent from `platform/repos.yml`"*), `ant-academy.md:26/293/424` (ant-academy not in `repos.yml`),
  and `frontend_architecture.md:11` (studio-desk **is** in `repos.yml`).
- `platform/repos.yml` @ `2adcf71` — **9** entries (`app`, `cms`, `jobsimulation`, `sentinel`, `storage`,
  `messenger`, `roadrunner`, `next-web-app`, `studio-desk`), 0 skillpath. Clears `skillpath.md:45` and
  `frontend_architecture.md:11`'s *"exactly **9** entries"*.
- Shared-library pins, all seven repos' `go.mod` read directly: colony `app`/`messenger` **v0.35.2**,
  `sentinel`/`storage`/`roadrunner` **v0.34.3**, `cms`/`jobsimulation` **v0.35.1**; proto app/messenger
  **v1.210.0**, cms **v1.207.0**, jobsim **v1.205.0**, sentinel **v1.200.0**, storage/roadrunner **v1.196.0**;
  ai **v1.40.2** in app/cms/jobsimulation only; taxonomy **v1.2.0** direct in app/messenger/cms/jobsim,
  `// indirect` in sentinel/storage, absent from roadrunner. Every cited `go.mod:N` resolves
  (`cms/go.mod:9,:13`, `jobsimulation/go.mod:11,:15`, `roadrunner/go.mod:7`). Clears the *values* of
  `shared_libraries.md:41`, `:69`, `:104`, `:105`, `:181` (the *sets* are B3/B4/B5).
- `app`'s Connect handler registrations @ `b948604` — **six** `mux.Handle` calls in `main.go`, five
  unconditional at `:1187`, `:1188`, `:1196`, `:1204`, `:1228`, plus `CMSService` at `:1213` inside
  `if cmsRPCServer != nil {` (`:1212-1214`). Every one of the five line anchors in
  `shared_libraries.md:70` lands on the exact `mux.Handle` it names. Clears `shared_libraries.md:70`,
  `jobsimulation.md:37`, `jobsimulation.md:98`.
- `SkillPathSessionService` in `app` Go source @ `b948604`: **0** occurrences; no `skillpath…v1connect`
  import; no `RoadRunnerServiceHandler`. Clears `skillpath.md:30-33` and `shared_libraries.md:79`.
- `ROADRUNNER_RPC_ADDR`: **0** at `app 9d00a313` and **0** in `platform/` @ `0dab54d` (positive control
  `JUDGE0_BASE_URL` returns hits in both). `docker-compose.yml:118` is `- AWS_REGION=eu-west-1`, exactly as
  the parenthetical says. Clears `shared_libraries.md:79`.
- `app/terraform/migrations/20260722081626_jobsim_data_model.sql`: **23** `CREATE TABLE` statements
  (`sessions`, `actors`, `interactions`, `validation_*`×6, `anticheat_*`×2, `recordings`,
  `chime_recordings`, `code_submissions`, `collaborative_assets`, `interview_extraction_results`, …).
  Clears `jobsimulation.md:24` / `:70`'s "23 run-state tables".
- `app/terraform/migrations/20260729133514.sql`: **0** `INSERT` statements (the single case-insensitive
  "insert" hit is the function name `on_insert_local_jobsimulation_sessions_update_memberships` at `:64`).
  `:15-23` re-points the two FKs, `:36-44` NULLs orphans, `:58` is the *"5. Drop the mirrors."* comment,
  `:62`/`:63` DROP `local_jobsimulation_sessions` / `local_skill_path_sessions`. Every anchor in
  `jobsimulation.md:105-110` and `skillpath.md:83` lands. Clears both, including the "0 INSERTs" retraction.
- `code/proxy.js`'s `isPublic` matcher enumerated in full (`:112-187`) against the eight-row table at
  `ant-academy.md:250-267`: every pattern in the table is in the matcher and every matcher entry is in the
  table, including the two conditional spreads (`DEV_LOGIN_ENABLED`, `VISUAL_BYPASS`) and both `_meta`
  spellings. `/api/chapters/*` is correctly **absent**. Clears `ant-academy.md:250-280`.
- Studio-desk `vite.config.ts` `rollupOptions.input`: **9** prod entries (`home`, `sim-advanced-builder`,
  `builder-skill-path`, `simulation-builder`, `sim-guided-builder`, `generation`, `catalog`, `academy`,
  `skills`) + `dev-accept` behind `isProduction ? {} : …`. Exactly the set at `studio-desk.md:34`.
- `next-web-app` monorepo scalars, each read from source: `apps/web/package.json:46` = `"next": "^16.2.7"`
  (identical in hiring/integration/maintenance — 4 apps); root `packageManager: "pnpm@10.30.3"`;
  `engines.node: ">=24.0.0"`; `!apps/mobile` in `pnpm-workspace.yaml`; **8** locale dirs in
  `configs/i18n/messages` (de,en,es,fr,it,ja,nl,pt); dev ports 3000/3001/3002 from each app's `dev` script
  and 3031 from `apps/mobile`'s `dev:mobile`; workspace names `@anthropos/tsconfig`, `@anthropos/i18n`,
  `@anthropos/web-app`, `@anthropos/hiring-app`, `@anthropos/integration`; `UPGRADE-IMPACT-next16.md` present.
  Clears `frontend_architecture.md:21-25`, `:31-35`, `:48-51`, `:75-77`.

**RE-DERIVED / anchor-verified** (weaker — I opened the cited lines and their surroundings, but did not
independently enumerate a predicate):

- `ant-academy.md:136` — `serverTenant.js:145` is verbatim
  `const view = (await getBackendCatalogView(eids)) ?? emptyCatalogView()`; the *"cutover is intentional, not
  reversible-on-error"* comment sits at `:134`, inside the cited `:115-145` window.
- `ant-academy.md:139-142` — all three failure legs read in `backendContent.js` (`:63-68` client-null,
  `:94-108` catch→null) and `graphql/server.js:14-18` (throws when `NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT` unset).
- `ant-academy.md:157` — `draftMode.js:48` is `NODE_ENV === 'development' && isDraftOptIn(ACADEMY_SHOW_DRAFTS)`,
  whitelisted not blacklisted, with the hard-block documented at `:16-25`.
- `ant-academy.md:174-178` — `serverChapterBody.js:51-67`: `getBackendChapterBody` → `maybeResolveDraftBody`
  → `return { notFound: true }`; `app/not-found.jsx:43` = `You wandered off the trail.`
- `ant-academy.md:244` — `code/app/layout.jsx:132` is `manifest: "/academy-manifest.json"`.
- `ant-academy.md:196` + `:401-403` — all four named demopatches exist in rext
  (`academy-fs-published-fallback`, `-public`, `-chapter-body`, `ant-academy-dev-origins`), plus
  `stack-injection/apply-academy-fs-published-body.sh` and `demo-stack/ant-academy.sh`.
  `stack-verify/live/autoverify.sh` assert **(f)** exists at `:459-521` and probes both `/library/` **and**
  `/` (so it is not the §5 rule 11 defect the protocol describes; that was repaired).
- `ant-academy.md:408` — `ucourses/ucourse-engine/assistant/agent.js:12-13` =
  `https://api.openai.com/v1/responses` + `MODEL = 'gpt-5.2'`; `:32` reads
  `localStorage.getItem('openai_api_key')`. `mobile/package.json:22` = `"expo": "~54.0.33"`.
  `code/package.json`: `next ^16.2.6`, `react ^19.2.5`, `marked ^17.0.0`, `engines.node >=22`,
  `vercel.json` = `{"framework":"nextjs"}`. FA Pro vendored at `code/public/assets/fontawesome/{css,webfonts}`.
- `studio-desk.md:107` — `src/routes/skillpath.ts` is **61,080 bytes** (~61 KB, the largest route);
  `src/routes/youtube.ts:43` is exactly `const apiKey = process.env.YOUTUBE_API_KEY;` with the `_mock: true`
  fallback three lines down; `.env.example:120` = `GCLOUD_SERVICE_ACCOUNT=`; `terraform/main.tf:129` =
  `"name": "GCLOUD_SERVICE_ACCOUNT",`; no `src/` code reads it.
- `studio-desk.md:112` — `src/services/ai/config.ts:20-38` gives OpenAI/Azure `gpt-5.2` (slow+fast) /
  `gpt-5-mini` / `gpt-5-nano` and Anthropic `claude-opus-4-5-…` / `claude-sonnet-4-5-…` (fast too) /
  `claude-haiku-4-5-…`; `:182` is `defaultTier: (env.AI_DEFAULT_TIER as ModelTier) || 'thinking_fast'` and
  `.env.example:61` is `AI_DEFAULT_TIER=fast` — the doc's in-code-vs-example split is exactly right.
  `.env.example:57` = `AI_PROVIDER_CHAIN=azure-openai,openai`.
- `studio-desk.md:161` — `package.json:7-8` `"node": ">=24"`; `Dockerfile:1` and `Dockerfile.dev:1` both
  `FROM node:24-alpine`. `:172` — `src/index.ts:60` is `process.env.PORT || 9100`. `:305` —
  `src/index.ts:96` `STUDIO_ACCESS_ROLES = ['admin','org:admin','content_creator','org:content_creator']`,
  redirect to `WEB_APP_URL` at `:119-120`.
- `studio-desk.md:140` — `docker-compose.yml:204` @ `0dab54d` is the
  `VITE_GRAPHQL_ENDPOINT: ${VITE_GRAPHQL_ENDPOINT:-http://localhost:8082/graphql/query}` build arg.
- `studio-desk.md:387-393` — `app/core/main.ts:97` `preloadCriticalCSS();`, `:104` `Sentry.init`,
  `:117` `posthog.init`, `:182` `clerk.load()`, `:191` `l12nService.init()`, `:199` `canAccess()`,
  `:206` `new PageWrapper();`. `:105` is the sole "tailscale funnel" line (the GlitchTip comment).
- `jobsimulation.md:28` — `20260722104506.sql:2` `CREATE TABLE "job_simulation_sessions" (`, `:79`
  `DROP TABLE "sessions";`. `:32` — `docker-compose.yml:176` = `- JOBSIMULATION_RPC_ADDR=http://backend:8083`
  (and `:173`/`:174`/`:57` give the other three, all `backend:8083`).
- `jobsimulation.md:91` — `jobsimulation/go.mod:24` `hibiken/asynq v0.26.0` (also `app/go.mod:40`).
  `:103` — `jobsimulation/internal/graph/queries.resolvers.go:70` is the `JobSimulationResult` resolver, whose
  body reads persisted results via the manager (no LLM/engine recompute). `:123` —
  `jobsimulation/internal/runner/runner.go`'s package doc reads *"formerly the standalone 'roadrunner' service"*.
  `:130` — `…/cms/directus/collections/jobsimulation.go:1594-1597` returns
  `SimulationVoiceEngineGptrealtime` on nil (identical line numbers in both the `cms` repo and
  `app/internal/cms/`). `:133` — `calls/livekit.go:131-135` reads `flag_use_realtime_openai`, `:140-144`
  sets `agentName = "anthropos-agent"` + `agentEndpoint = "openai-hosted"`.
- `jobsimulation.md:119` — `app/main.go:980-982` is the `DIRECTUS_BASE_ADDR` `log.Fatalf`.
- `jobsimulation.md:203-204` — the always-on floor is `{sentinel}` in `docker-compose.yml` (the only service
  with no `profiles:` key) plus `postgresql` + `redis` from `common.yml` — exactly the three the bullet names,
  and `include:` is resolved. Matches §5 rule 30.
- `skillpath.md:14/57` — `app/internal/skillpath/session.go:205-207` is the
  `// cms-in-app deseam: cms is in-process` comment + `u.cms.GetSkillPathDomain(...)`;
  `app/internal/skillpaths/skillpaths.go:88-95` is its twin. `:26` — both Ent schema files exist and there is
  **no** `local_skill_path_session.go`. `:42` — `skillpath_sessions.graphqls` exists at the cited path.
  `:76-77` — `intelligence.go:1144` is `InsightsSkillPathByMemberships` and `:1159-1170` is the
  `m.ent.SkillPathSession.Query()` with `SkillPathID` + `StatusIn(Active, Completed)` + the tenant `Or`.
  `:92-98` — `InsightsBySkillPathStudentSimulationsContainer.tsx:31-34` hardcodes
  `return null as unknown as MembershipEnriched`, the results table (`:141-150`) and totals block
  (`:151-163`) are commented out, and `:138` renders the comingSoon key.

---

## What I could not settle

- **`colony` and `taxonomy` and `proto` internals.** These repos are not in the ground-truth clone set (they
  are private Go modules pulled at Docker build). So `shared_libraries.md:40` (`go 1.25.0` / `golang:1.26-bookworm`),
  the `colony` package table at `:46-54`, the rate-limiter note at `:56-59`, the `authn` provider/claims
  description at `:153-159`, the "12 Connect-RPC services are defined" set at `:77-83`, the buf-v1 layout at
  `:91-94`, taxonomy's `go 1.21.0` / "131-line `node.go`" / the `NodeID` format and worked examples at
  `:215-229` — none can be checked here. The listed 12 service names sum to 12, but per §5 rule 24 that is
  arithmetic over an unverified set. Cloning `anthropos-work/{colony,proto,taxonomy}` would settle all of it.
- **The taxonomy figures** (`shared_libraries.md:202-203`: 22,470 public roles / 42,790 public skills /
  18,919 `job_role_embeddings` rows). These require a prod or seeded-stack DB read
  (`select count(*) … where organization_id is null`), which I did not perform. The document ships its
  derivation command and its provenance manifest path — that is the right shape, but I did not run it.
- **"the Cosmo Router survives in production only"** (`studio-desk.md:132`, `:164`, `:46`;
  `frontend_architecture.md:39` / `:44`, *"the Cosmo Router at `:8080/graphql` in prod"*). The corpus is
  internally consistent on this, and platform `0dab54d` confirms the *local* half (no `graphql` service,
  no `graphql-wundergraph` in `repos.yml`, backend serves `:8082/graphql/query`). The *production* half needs
  the `infrastructure` terraform repo, which is not in the clone set. Note the root `CLAUDE.md` phrases the
  same event as *"DELETED from the platform"*, which a hurried reader may take as "deleted everywhere" — but
  that is a phrasing tension, not a demonstrable contradiction, so I did not book it.
- **`ant-academy.md:12/74` — "a grid of 65 real cards"** and **`:246` "1000+ Vitest tests"**. Both are live-
  measurement claims (a running demo; a full `vitest run`). I did not stand a stack up or run the suite.
- **`studio-desk.md:412` / `:423` latency numbers** (skeleton p95 4669 → 817 ms; `canAccess` 4049 → 38 ms;
  FCP 6936 → 2152 ms on `billion`). Host-specific measurements; unreproducible from a static read, and the
  corpus does state the environment with each number as its own protocol requires.
- **`jobsimulation.md:47-50`** — `module.jobsimulation_euwest1` still declared in
  `infrastructure/terraform/production/services.tf` and owning the LiveKit/Chime S3 buckets. Needs the
  `infrastructure` repo. Likewise `jobsimulation/terraform/main.tf:40 service_desired_count = 0` — I did not
  open the jobsimulation clone's terraform.
