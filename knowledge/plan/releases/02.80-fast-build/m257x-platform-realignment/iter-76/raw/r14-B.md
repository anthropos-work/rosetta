# Seat B — reading #14

**Refs at the moment of measurement** (§5 rule 26):
corpus HEAD `b8a1fb0` (working tree clean apart from the untracked briefing + this iter dir) ·
app `b948604ff` (v1.366.0; `origin/main` = `717737471` = `v1.367.0-4-g717737471`) ·
platform `0dab54d` (level with origin/main) · next-web-app `bb3313bc0` ·
graphql-wundergraph `60c229f` · rosetta-extensions authoring copy @ `main`.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/services/ai-readiness.md` | 645 | yes |
| `corpus/architecture/ai_architecture.md` | 302 | yes |
| `corpus/services/graphql-wundergraph.md` | 265 | yes |
| `corpus/services/ai-labs.md` | 156 | yes |
| `corpus/services/coursebuilder.md` | 140 | yes |
| `corpus/services/customerio-sync.md` | 75 | yes |
| `corpus/architecture/README.md` | 38 | yes |

(Also read, as the briefing directs and as in-scope reference: `corpus/ops/platform-alignment.md` §5,
lines 240–1146 of 1680.)

---

## Blockers

### B1 — `graphql-wundergraph.md` says the `graphql` profile "survives in compose and is now simply the default profile". At platform HEAD there is no `graphql` profile, and the default is `core`.

- **Anchor:** `corpus/services/graphql-wundergraph.md:13-14` —
  > "**There is no `:5050` on a local stack.** The `graphql` *profile name* survives in compose and is now
  > simply the default profile — it no longer names a router service."

- **Why it is false:**
  - Measured at platform `0dab54d` (the ground-truth checkout, level with `origin/main`).
    `grep -n "profiles" stack-demo/platform/docker-compose.yml` returns exactly eight lines
    (`:100`, `:131` [a comment], `:134`, `:154`, `:195`, `:226`, `:253`, `:268`). The full set of profile
    tokens declared is **`core`, `backend`, `all`, `storage-legacy`, `customerio-sync`, `messenger`,
    `studio-desk`, `frontend`**. `common.yml` (36 lines, the only `include:`) declares **no** `profiles:`
    key at all. **The token `graphql` appears in no `profiles:` list anywhere.**
    (Positive control on the same pass: `grep -c "backend" docker-compose.yml` → 14; the four `graphql`
    substring hits in that file are all `VITE_GRAPHQL_ENDPOINT` / `NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT`
    *values* at `:204`, `:220`, `:236`, `:245`.)
  - The default profile is **`core`**, not `graphql`: `stack-demo/platform/Makefile:10` `PROFILE ?= core`,
    consumed at `:65-66` `up: … $(COMPOSE) --profile $(PROFILE) up --build -d --remove-orphans`.
  - **The pin does not exempt this sentence.** The paragraph names `2adcf71`, but the clause asserts
    currency — *"survives"*, *"is **now** simply the default profile"* — and the table three lines above
    is headed *"a fresh local stack @ platform **origin HEAD**"*. Per `platform-alignment.md` §5 rule 33,
    a block that asserts currency cannot be pinned into silence, and a pin's scope is the claim's own
    block.
  - **It contradicts two other corpus passages, one of them in my own file set.**
    - `corpus/services/customerio-sync.md:14`: *"Not in the default profile, which is `core`, **not
      `graphql`**: `0dab54d` renamed it."*
    - `corpus/ops/platform-alignment.md` §5 rule 30 (measured at `0dab54d` by name): *"A `--profile`
      naming the **retired `graphql` token** therefore exits 0 and starts three containers"* — and it
      derives the always-on floor as *"the services declaring no `profiles:` key"*, which I re-derived
      independently: `postgresql` and `redis` (`common.yml`) and `sentinel` (`docker-compose.yml:5-27`,
      no `profiles:` key).
- **Confidence:** high

### B2 — `graphql-wundergraph.md` tells the reader `make up` starts the `graphql` profile

- **Anchor:** `corpus/services/graphql-wundergraph.md:177` —
  > "`make up` starts the `graphql` **profile**, which since `2adcf71` contains no router."

- **Why it is false:** same ground truth as B1. `make up` runs `--profile $(PROFILE)` with
  `PROFILE ?= core` (`stack-demo/platform/Makefile:10`, `:65-66` @ `0dab54d`). There is no `graphql`
  profile to start. The block names `2adcf71` only inside a subordinate clause about the router; the
  main clause is a present-tense instruction to the reader under the heading *"### Run in Docker"*, and
  the whole page's banner grades itself against *"platform origin HEAD"*.
  Worse, the sentence is the **operating instruction** on this page: a reader who follows it and then
  sees three containers come up (postgresql + redis + sentinel — the always-on floor, §5 rule 30) will
  debug the application rather than the invocation. §5 rule 30's second corollary — *"do not spell a dead
  command in runnable form, even to warn about it"* — applies directly here.
- **Confidence:** high

### B3 — `graphql-wundergraph.md:168` cites `docker-compose.yml:334,352`; the file is 271 lines

- **Anchor:** `corpus/services/graphql-wundergraph.md:166-168` —
  > "**locally they hit `backend` directly** at `:8082/graphql/query` (`docker-compose.yml:334,352`),
  > because the router service no longer exists in compose."

- **Why it is false:** the block names **no ref**, so per the briefing it grades at the checkout
  `0dab54d`, where `wc -l stack-demo/platform/docker-compose.yml` = **271**. Lines 334 and 352 do not
  exist. **Refs tried:**
  - `0dab54d` (the checkout) — out of range; the two lines the sentence means are `:220`
    (`VITE_GRAPHQL_ENDPOINT=http://localhost:8082/graphql/query`) and `:236`
    (`NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT: http://${PUBLIC_HOST:-localhost}:8082/graphql/query`), with
    `:204` and `:245` as their build-arg/env twins.
  - `2adcf71` (the ref this page names elsewhere) — the 387-line file there has exactly
    `334: - VITE_GRAPHQL_ENDPOINT=…` and `352: NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT: …`. So the anchor was
    correct once and drifted.
- **Confidence:** high. **Why I hesitated:** some grading rules carve out plain line drift as MINOR. I
  booked it because the anchor is not merely off-by-a-few — it is **beyond EOF at the ref the claim is
  graded at**, so a reader following it gets nothing at all, and because §5 rule 33's own mechanic (a
  neighbouring row's ref does not date this row) is precisely what makes the `2adcf71` resolution
  unavailable to this sentence.

### B4 — `ai-readiness.md:316` pins `SHOW_SECONDARY_TABS` at `AIReadinessClient.tsx:69`; `:69` is an import

- **Anchor:** `corpus/services/ai-readiness.md:315-316` —
  > "a 4th manager tab, **Compare** (cycle deltas), is fully built but **hard-gated off** —
  > `AIReadinessClient.tsx:69` `const SHOW_SECONDARY_TABS = false;` strips it from the tab list."

- **Why it is false:** at next-web-app `bb3313bc`,
  `apps/web/src/app/(authenticated)/(verified)/ai-readiness/AIReadinessClient.tsx` (655 lines) has
  `const SHOW_SECONDARY_TABS: boolean = false;` at **`:78`** (its only other use is `:599`
  `(SHOW_SECONDARY_TABS && tab.key === 'compare')`). Line `:69` is
  `import { PeopleDrawer } from './_components/PeopleDrawer';` — a different construct entirely.
  The *substance* of the claim (Compare is built and hard-gated off) is **correct**; only the anchor is
  wrong. This is §5 rule 21's "resolves but names the wrong construct" class, which the corpus's own
  anchor check cannot catch.
- **Confidence:** high

### B5 — `ai-readiness.md:459-461` — both `AIReadinessClient.tsx` anchors in the M219 correction block name the wrong constructs

- **Anchor:** `corpus/services/ai-readiness.md:459-461` —
  > "The **CURRENT** dashboard (`AIReadinessClient.tsx:137-138`) computes
  > `effectiveCycleId = selectedCycle ?? activeCycle?.id ?? latestClosedCycle?.id` and gates the data GET
  > on `cyclesQ.isFetched` (`:150-154`)"

- **Why it is false:** measured at next-web-app `bb3313bc` in the same 655-line file:
  - `effectiveCycleId = selectedCycle ?? activeCycle?.id ?? latestClosedCycle?.id` is at **`:153-154`**.
    `:137` is blank and `:138` is `const cyclesQ = useAIReadinessCycles({ enabled: featureOn });`.
  - The data-GET gate `enabled: featureOn && cyclesQ.isFetched` is at **`:169`** (inside the
    `const dataQ = useAIReadiness({…})` call at `:166-170`; the sibling `stepsQ` gate is `:176`).
    `:150-154` is the `// Cycle preference:` comment plus the `effectiveCycleId` assignment — i.e. the
    range the *first* citation should have had.
  Again the substance is correct (the current dashboard waits for `/cycles` then passes `?cycle=`), and
  the neighbouring anchor in the same file — `:133-134`
  `const { orgEnabled } = useAiReadinessEnabled(true);` / `const featureOn = orgEnabled === true;` — is
  **exactly right**, which is what makes the two wrong ones read as verified.
- **Confidence:** high

### B6 — `ai-readiness.md:195-197` says `computeInterviewInsightsV2` "reads **exactly one table**"; it reads three, and the doc's own next paragraph says so

- **Anchor:** `corpus/services/ai-readiness.md:195-197` —
  > "`app/internal/aireadiness/how_we_measure.go` (`computeInterviewInsightsV2` …) reads **exactly one
  > table**, and decodes its `report` JSONB:"

- **Why it is false:** at `app` `b948604f`, `computeInterviewInsightsV2`
  (`internal/aireadiness/how_we_measure.go:819`) makes three DB calls in its body:
  - `:825` `m.queryInterviewAggregatedReports(ctx, orgID, since)` → `interview_aggregated_reports` (the
    one the sentence means),
  - `:838` `m.countInterviewSessionsInCycle(ctx, orgID, userIDs, since)` — a raw sessions count,
  - `:880` `m.resolveSessionAuthors(ctx, orgID, srcIDs)` (`:1093`) — which joins `sessions → memberships`.

  The doc **states the third one itself**, five lines later, at `:202`: *"`source_session_id` is what
  `resolveSessionAuthors` joins (`sessions → memberships`) to hydrate the quote's author name + job
  role."* So the passage contradicts itself inside one section — §5 rule 21's cheapest detectable class.
- **Confidence:** low. **Why I hesitated:** in context the emphatic "exactly one table" is scoping the
  *content* source (refuting the `conversation_extractions` red herring), and the joins are disclosed
  three lines down, so a careful reader is not misled about behaviour. I booked it per the briefing's
  resolve-doubt-upward rule and because self-contradiction is a finding on its own terms; a reader who
  quotes the emphatic sentence forward (§5 rule 27's "quoting a prior finding forward is not evidence")
  would carry a false predicate.

---

## Minors

- `graphql-wundergraph.md:81-82` — *"`:174-176` of this same doc already said `localhost:5050` refuses
  the connection"*. The `localhost:5050` sentence is at **`:178`**; `:174-176` is a blank line, the
  `### Run in Docker` heading, and another blank. Intra-corpus citation drift (§5 rule 34).
- `graphql-wundergraph.md:128` — *"which is why a subgraph SDL change rebuilt the router, as `:84`
  describes"*. `:84` is the *"There is no application source here"* blockquote; the build-context-is-the-
  parent-dir statement is at `:110-111`.
- `graphql-wundergraph.md:223-224` — *"On `backend`, `/graphql` serves the Apollo Sandbox UI; CORS
  preflight and auth happen at `/query`."* The path is `/graphql/query`, not `/query`
  (`app/internal/web/backend/backend.go:315` mounts `playground.ApolloSandboxHandler(…, "/graphql/query")`
  at `/graphql`; `:317` mounts the graph handler at `/graphql/query`). The Sandbox mount is also
  dev-only (`:312` *"only expose it in local"*), which the sentence does not say.
- `ai_architecture.md:96` — *"this line went on publishing it 68 lines below"*, referring to the ⚠️ at
  `:15-17`. `:96 − :17 = 79`. The figure reconciles with no plausible reference line; stale after edits.
- `ai_architecture.md:232` — *"the enum is `app/internal/cms/directus/collections/jobsimulation.go:983-990`"*.
  The `AIModel` const block is `:979-991` (11 members); `:983-990` covers 8 of them and starts mid-block.
- `ai_architecture.md:273` — *"`calculateSkillScore` (`v3/validator/skills.go:53-64`)"*. The function is
  `:53-62`; `:64` is the next function's signature (`func (s skillsValidator) run`).
- `ai_architecture.md:207-210` — *"all it does is swap the dispatched **endpoint** to `openai-hosted`"*.
  `internal/jobsimulation/calls/livekit.go:140-144` also resets `agentName = "anthropos-agent"`, which
  matters on the US-location branch (`:126` had set it to `anthropos-agent-us`).
- `ai-readiness.md:575-578` — *"`computeCycleTotals` (`how_we_measure.go:253-261`)"*. The function is at
  `:260` with its doc comment at `:256-259`; `:253-255` are the tail of the previous function.
- `ai-readiness.md:18-20` — the `WorkforceDirectory` interface is described as
  *"`LoadMembers`/`LoadMembersByUserIDs`"*; it declares a third method, `BaseMembers`
  (`internal/aireadiness/manager.go:46`).
- `coursebuilder.md:115` — *"`~25` boundary test files"*. Measured:
  `ls internal/web/backend/coursebuilder/*_test.go | wc -l` = **33**. (The engine's *"~55"* measures 59,
  which is inside "~".)
- `coursebuilder.md:48` — *"`ssm.tf:328-334`"*. The `aws_ssm_parameter "anthropic_api_key"` resource is
  `:328-333`.
- `customerio-sync.md:19-33` — the block headed *"### Compose definition"* omits `env_file: - .env`
  (`docker-compose.yml:145-146`) and `networks: - app-network` (`:150-151`) present in the real service
  at `0dab54d`. Presented as the definition rather than an excerpt.
- `architecture/README.md:19` — lists *"the WunderGraph Cosmo GraphQL gateway"* among
  "the third-party integrations … how each is configured and consumed", with no hint that it is gone
  from local dev. Not false (it is still `service_desired_count = 1` in production per
  `graphql-wundergraph/terraform/main.tf:20`), but this index line is the one place in the family that
  does not carry the deletion banner every sibling doc leads with.

---

## Positively cleared (audited zeros)

**ENUMERATED** (predicate re-derived from source independently, cardinality stated before arithmetic):

- **The subgraph ladder** (`graphql-wundergraph.md:36-48`, `:154-158`). I counted
  `^  - name:` in `supergraph-config-prod.yaml` at each named commit in the `graphql-wundergraph` clone:
  `749dc86^` = **5** (backend, skiller, jobsimulation, cms, skillpath) · `749dc86` = **4** · `7c17e63` =
  **3** · `915da06` = **1**. Dates match (`2026-06-24` / `2026-07-21` / `2026-07-29`).
  `git show --name-status 915da06` marks **both** `schemas/cms.graphqls` and
  `schemas/jobsimulation.graphqls` `D`, and its subject line really does say *"supergraph 2→1"* while
  the tree it landed on listed three. **The 3→1 claim and the "subject line is wrong" claim are both
  correct.** At HEAD `supergraph-config-prod.yaml` lists `backend` alone, `schemas/` holds
  `backend.graphqls` alone, `subgraphs.conf` is a single `BACKEND=v1.360.0`.
- **No subscriptions anywhere** (`graphql-wundergraph.md:54-64`).
  `grep -rn subscription supergraph-config-*.yaml` → 0 hits (positive control: `federation_version`
  matched in all three). `grep -n "^type Subscription" schemas/backend.graphqls` → 0; the six
  `Subscription` substring hits are `activeSubscription` `:1528`, `type PlanSubscription` `:1555`,
  `stripeSubscriptionId` `:4385`/`:4418`, `stripeSubscriptionItemId` `:4389`, and a doc-comment `:4391`
  — exactly the Stripe/plan names the doc names. `bba862f` exists on
  `remotes/origin/feat/use-web-socket` only.
- **`ci/update-subgraph.sh` carries exactly one `gh release download`, `-R anthropos-work/app`**
  (`graphql-wundergraph.md:171`). `grep -c` = 1, at `:9`.
- **Router pins** (`:75-77`, `:256`): `ghcr.io/wundergraph/cosmo/router:0.275.0` (`Dockerfile:22`,
  `Dockerfile.dev:29`), `wgc@0.104.0`, `federation_version: =2.3.2` in all three supergraph configs,
  `node:22.11-alpine`. Playground/introspection `true` in `config.compose.yaml` + `config.dev.yaml`,
  `false` in `config.prod.yaml`. `listen_addr 0.0.0.0:8080`, `graphql_path /graphql`. `package.json` is
  `{"name": "graphql-wundegraph"}` (misspelling carried). `grep -c 5050 docker-compose.yml` = **0** at
  `0dab54d` (and **5** at `2adcf71^`, so the "there is no 5050 now" claim is a real transition).
  `terraform/main.tf:20` = `service_desired_count = 1`.
  `repos.yml` at `2adcf71^` carried `- name: graphql-wundergraph / type: node-npm`; at `2adcf71` there
  is no such entry (9 `name:` entries, none matching).
- **The GraphQL default-closed probe** (`graphql-wundergraph.md:192-198`).
  `app/internal/authorization/gqlauthz/gqlauthz.go` — the anonymous exemption set is `:175-177`
  (`isPublicOnlyOperation || isFederationQuery || (environment == colony.Development &&
  isIntrospectionOnlyQuery)`) and `:186` is `return errorResponse(fmt.Errorf("unknown viewer: %w",
  errForbidden))` — the rejection the doc quotes. `gqlauthz_test.go:132` `"bare __typename is not
  exempt"` and `:176` `TestAnonymousRejectionLogsAtWarn` both exist.
- **The AI vendor manager** (`ai_architecture.md:19-34`). `internal/jobsimulation/ai/ai.go`: vendor
  consts at **`:30-33`** (`azure`/`openai`/`anthropic-aws`/`anthropic` — four, no Mistral),
  `getClient` at **`:259-289`** — a plain switch; the `Azure` arm reads `flag_use_azure_us` at `:265-269`
  and on a lookup **error returns the EU client** (`:270-273`, the shadowed
  `client := a.azureClientEu`); `AnthropicAws` and `Anthropic` both return `a.anthropicClient`; the
  only `config.WithRegion` in the file is `"eu-west-1"` at `:87`. The 429 lever is
  `:150-155` (`var throttled bool` → `if throttled { vendor = Openai }`), fed by `isThrottlingError`
  (`:129`) at `:166-167` and `:325-326`. Mistral's only `app` sites are
  `internal/cms/studio/markdownManager.go:19` and `studioManager.go:583` — both OCR.
- **The unset-`ai_vendor` route** (`ai_architecture.md:50-63`, `:83`).
  `internal/cms/directus/collections/jobsimulation.go:905` is `AIVendor *AIVendor` on the **Directus
  DTO**; `:1302-1305` replaces nil with `simulation.Openai` **before** the sequence is built at `:1307`;
  `:1297` defaults the model to `simulation.Gpt5Point1`. The Directus enum has **five** members
  (`:969-975`, `azureglobal` at `:971`); `simulator/ai/ai.go` has **four** cases (`:58` Openai, `:69`
  Azure, `:86` AnthropicAws, `:102` Anthropic) with `default:` at `:114-115` → `internalAi.Openai`.
  So the "5 members / 4 cases / `azureglobal` is the one with no case" arithmetic **and** its set are
  both right.
- **The three simulation model defaults** (`ai_architecture.md:243-257`). `simulator/ai/ai.go`
  `:20-26` `GetValidationAIVendorAndModel` → Azure / `GPT4Dot1` / summarize `GPT4Dot1Mini`;
  `:65-66`, `:82-83`, `:126-127` all default `openai.GPT4Dot1`; `:98-99` anthropic-aws →
  `Anthropic37SonnetAWS20250219`; `:110-111` anthropic → `Anthropic35Sonnet20241022`.
- **The evaluation system** (`ai_architecture.md:261-280`). `checkerEngines` is declared
  (`v3/validator/validator.go:43`), populated (`:60-61`) and passed (`:595`) — and inside
  `basevalidator` it is only stored (`criterion.go:34`, `:72`, `:90`) and **never dereferenced**
  (repo-wide grep for `checkerEngines` returns exactly those six sites). The real dispatch is the
  hardcoded switch at `criterion.go:126-155` (`case check.EngineLlm:` `:127`, `case check.EngineTextDiff:`
  `:142`, `default:` errors). `:168` runs `validateCodeDiff` concurrently with `validateLLM` (`:175`);
  `:428` `NewLLMBulkChecker(c.logger)`; `bulkChecks.go:118` `ai.WithTemperature(0.0)`;
  `templates/checkValidationBulk.tmpl` exists; the readback struct is `check_id`/`success`/`feedback`
  (`bulkChecks.go:56-58`). `validateCodeDiff` is `:450-475` and sets `success` from
  `cdiff.Diff == ""`. `skills.go:53-62` counts booleans, `:75` computes `passed/total*100`,
  `:40-51` `calculateCompetencyLevelScore` is exactly `20` at `<60 && isPassed`, `100` at `>=100`, else
  `max(0, score*2-100)`, carrying `// TODO fix this formula` at `:49`. `skill.go:617-623`
  `convertLevelTo100` is a plain `100 * score / levelsCount`;
  `packages/ui/src/Competency/CompetencyReadLevel.tsx:18` is the mapping call.
- **Studio-Room slots** (`ai_architecture.md:112-133`). `studio/configs/production_config.ini:26-36` is
  exactly the five stable + five experimental rows the table shows, and
  `development_config.ini:26-36` is byte-identical to them. `config_template.ini:39`
  (`EXECUTION_AI_STABLE_MODEL = azure, gpt-4o, none`) and `:40` (`CREATIVE_AI_STABLE_MODEL = azure,
  gpt-4o, none`) are the only shipping-vs-scaffold `gpt-4o` slots, and the template's stable column is
  `gpt-4.1-mini`/`gpt-4.1`/`gpt-4o`/`gpt-4o`/`o3` as described. `gpt-5.2` appears in
  `studio/services/ai.py:356` and `:508` as pricing entries and in **no** `*_MODEL` slot.
  `grep -rin 'bedrock|boto3' studio/` = **0** (positive control: `azure` = 10 hits in
  `studio/services/ai.py`).
- **`ai_architecture.md:96`'s pinned anchors.** At `app` `5ba17044` (the ref the sentence names),
  `internal/jobsimulation/ai/ai.go:267` and `:344` and `internal/skillerai/ai.go:347` all read
  `"flag_use_azure_us"`. `app/internal/ai/ai.go` does **not** exist; `jobsimulation/internal/ai/ai.go`
  does — exactly as the parenthetical says.
- **Embedding tables** (`ai_architecture.md:151-169`).
  `terraform/migrations/20260615130000_skiller_taxonomy.sql:51-61` and `:241-251` create
  `job_role_embeddings` / `skill_embeddings` with `extensions.vector(1536)`, a UUID FK to
  `job_roles`/`skills`, and an **IVFFLAT** index on `small_embedding3`. (The PK is
  `bigint … GENERATED BY DEFAULT AS IDENTITY` rather than the doc's `BIGSERIAL` — functionally the same
  thing, not worth a finding.)
- **LiveKit voice** (`ai_architecture.md:180`, `:202-212`).
  `internal/jobsimulation/calls/livekit.go:110` `agentName := "anthropos-agent"`, `:126`
  `anthropos-agent-%s` for a non-EU location; `flag_use_realtime_openai` is read at **`:131-135`** and
  takes effect at **`:140-144`**, both **inside** `CreateAgentDispatch` (`:106`). The `voice_engine`
  enum is a 4-member type at `internal/cms/directus/collections/jobsimulation.go:1079-1086`, and
  `voiceEngineFromDirectus` (`:1594-1600`) returns `gptrealtime` on nil.
  `getJobSimulationCallSignedUrl` / `getJobSimulationCallConversationToken` both exist as mutations, and
  ElevenLabs is present across ten `internal/` files including
  `jobsimulation/simulator/manager/elevenLabsManager.go`.
- **AI-readiness's whole platform surface** (`ai-readiness.md`). Every one of the following was
  enumerated from `app` `b948604f`:
  - the package split is real (`internal/aireadiness/` with `readiness.go`, `scoring.go`, `csv.go`,
    `steps.go`, `narrative.go`, `how_we_measure.go`, `cycles.go`, `compare.go`, `diagnosis.go`,
    `provision.go`, `defaults.go`, `emailoverride/`, `emailpreview/`, `notifications/`,
    `testdb_test.go`), `internal/workforce/members.go` stayed, commit `4c28365f` is
    *"Refactor AI Readiness domain: migrate workforce dependencies to aireadiness package"*, 2026-07-22;
  - `enum/organization_settings.go:47` `OrganizationSettingAIReadiness = "ai_readiness"`;
    `enum/ai_readiness.go:9-11` the three step types;
  - **the archetype bands**: `scoring.go:26` `archetypeHighBand = 75`, `:30` `archetypeLowCeil = 50`,
    `classifyArchetype` `:46-57` = Champion (both ≥75) / Standby (both ≤50) / `usage >= knowledge` →
    Explorer (so an exact tie IS Explorer) / else Hidden Talent; buckets `:63-66`
    None 0-24 / Low 25-50 / Medium 51-74 / High 75-100 — **every clause matches**;
  - **the 31-skill default**: `defaults.go:31-49` = 19 entries at weight 1.0, `:51-62` = 12 at 0.5
    (cardinality counted before the sum; 19 + 12·0.5 = **25.0**), and `defaultReadinessSims` `:75-79` is
    exactly 3 rows `{simulation,tech}`, `{simulation,business}`, `{interview,both}`;
  - **the two membership filters**: `readiness.go:387-391` branches `keepInCycleStep1` (`:388`, defined
    `:662-664`) vs `keepStartedMembers` (`:390`, `:684`), and `queryInCycleStep1Completers`
    (`:638-652`) really does demand `StepTypeEQ(StepSkillMapping)` **AND** `StatusEQ(StepCompleted)`
    **AND** `CompletedAtGTE(since)` — the strict one. `steps.go:915-938` `queryReadinessStarters` is
    `SELECT DISTINCT user_id FROM public.ai_readiness_user_step_progresses … AND status <> 'not_started'`
    with the comment *"This DB signal is the only real 'has started' check"* at `:913`. **The corpus's
    M257x iter-52 correction is right and its predecessor was wrong**;
  - **the three routes into the frozen path**: `GetAIReadinessWithOptions` `:289`, cycle-scoped closed
    `:290-297`, default no-active-cycle `:307-312`, fall-through `:314` — all four anchors exact;
    `computeOrgBreakdowns` `:330`; `computeTier1` `:139` with the platform's own comment
    *"a larger configured set makes a full score harder to reach"* at `:133-134`;
    `buildResponseFromSnapshots` `:771` calls `m.workforce.LoadMembers(ctx, orgID, "")` at `:779`;
  - **the 13 `ai_readiness_*` tables** the doc lists — I enumerated
    `CREATE TABLE "ai_readiness…"` across `terraform/migrations/` and got exactly the 13 the table
    names, no more and no fewer; plus the *one active cycle per org* partial unique index
    (`20260618160827.sql:18`, `… ("organization_id") WHERE ((status)::text = 'active'::text)`);
  - `interview_aggregated_reports` (`20260722081626_jobsim_data_model.sql:47-58`) carries exactly the
    named columns `sim_id`, `organization_id`, `report jsonb`, `session_count`;
  - **the 5 KPI ids**: `how_we_measure.go:1139-1155` `usageDimSpecs` = `avg_adoption`,
    `avg_transformation`, `avg_originality`, `avg_depth`, `avg_ownership`, and
    `grep -rn avg_frequency|avg_breadth|avg_context_fit internal/aireadiness/` returns **0** — the
    retirement claim holds. (`usageDimensionsFromReports`'s own comment at `:1161` still says *"four"*;
    the corpus's "5 tiles" is the correct number and the source comment is the stale one.);
  - `interactions.action_type` is **exactly** `{email, call}` —
    `internal/data/ent/interaction/interaction.go:92-99` `case "email", "call": return nil`; and the DB
    really does enforce `CHECK (source_id <> target_id)`
    (`20260722081626_jobsim_data_model.sql:94`);
  - **CSV = 15 columns**: `internal/aireadiness/csv.go:17-33`, counted = **15**. (The function's own
    doc comment at `:35` still says *"19-column"*; the corpus is right and the source comment is stale
    — `:13-16` explains the trim.);
  - `compare.go:18-23` names exactly the six sections the doc lists, and `:20` is
    *"Transition — **4×4 matrix**"*; `AIReadinessCompareResponse` `:35-40` has those six fields;
  - `CloseDueAIReadinessCycles` at `cycles.go:554` (sweeps `EndDateLTE(now)` on active cycles);
    `RefreshLiveSnapshots` at `live_snapshots.go:71`; `WorkforceDirectory` at `manager.go:38-46` with
    both `LoadMembers` and `LoadMembersByUserIDs`; narrative uses `openai.GPT5Mini`
    (`narrative.go:39`, `:85`);
  - every REST path the doc lists exists in `internal/web/backend/api/api.yaml` (`:661` data, `:690`
    narrative, `:719` steps-completion, `:751` cycles, `:792` setup, `:827` cycles/{cycleID}, `:852`
    export.csv, `:873` compare, `:907` cycles/{cycleID}/close), and the four GraphQL operations exist in
    `schemas/ai_readiness.graphqls` (`:8`, `:73`, `:78`, `:85`).
- **AI-readiness's next-web surface.** `AIReadinessClient.tsx` contains **0** occurrences of `posthog`
  and **0** of `flag_ai_readiness` (the exact measurement the doc claims), and gates on
  `useAiReadinessEnabled(true)` at `:133`. `useAiReadinessActive.ts:22` is the flag read and `:25`/`:29`
  are the `=== true` / conditional-query lines the doc's TS snippet reproduces;
  `aiReadiness.constants.ts:26` declares `AI_READINESS_FLAG = 'flag_ai_readiness'`.
  `AI_READINESS_URL = '/ai-readiness'` at `urls.ts:52`, imported at `useNavbarSections.tsx:4`, built at
  `:398-400`, gated at `:547` — and a repo-wide grep finds the constant in **exactly those two**
  non-`node_modules` files. `WorkforceNewClient.tsx:125-151` is the 5-tab array with no readiness tab.
  `useWorkforceAIReadiness.ts:23-27` calls `'/ai-readiness'` with `searchParams: { tag }` — **no**
  `cycle`, no `/cycles`. `AIReadinessHero.tsx:88` is `if (!air.deadline) return null;`.
  `components/ai-readiness/useAIReadiness.ts:48-62` is `deriveMode`, treating a null deadline as
  `deadlinePassed = true`. `HowWeMeasureTab.tsx` is **1,989 lines**, `grep -c interviewQuestions` = **0**,
  and `:1879`/`:1903`/`:1915`/`:1921`/`:1927` are precisely the C-block comment, the label, and the three
  cells (`skillsMapped`, `handsOnMinutes`, `interviewMinutes`). `hooks/useAIReadiness.ts:274` is
  `interviewQuestions: number;`. **This anchor set is the most exact in my file set** — which is why B4
  and B5 stand out.
- **`dae0fb2f7`** (`ai-readiness.md:280-283`) deleted `AIReadinessContainer.tsx` (103),
  `AIReadinessIntro.tsx` (220) and `AIReadinessView.tsx` (330) — **103+220+330 = 653**, exactly the
  "−653 lines" claimed — on 2026-07-13, and there is no `ai-readiness` directory under
  `enterprise/workforce/` at `bb3313bc`. `e2e/specs/web.ai-readiness.spec.ts` exists.
- **The AI-readiness rext anchors.**
  `demo-stack/patches/app-aireadiness-snapshot-loadmembers/app-aireadiness-snapshot-loadmembers.yaml:42`
  is `path: internal/aireadiness/readiness.go` and `:33` opens *"v2.7 M254 RE-POINT"* — both exactly as
  `ai-readiness.md:43-45` claims. `demo-stack/tests/test_aireadiness_snapshot_loadmembers_m254.py`,
  `stack-seeding/seeders/ai_readiness_interview_report.go`, `ai_readiness_evidence.go`,
  `seat_append_test.go`, `contentref.go` and `cockpit.go` (with `LegacyReadinessPaths` `:122`/`:135` and
  `ValidateCockpitManifest` `:161`) all exist;
  `ai_readiness_funnel_test.go:230 TestAIReadinessFunnelSeeder_DayZeroHeroGetsNoSignals` exists;
  `blueprint/blueprint.go:200` declares `AIReadiness string` and `:316`
  `AIReadinessNotStarted = "not_started"`.
- **AI Labs + credits.** `internal/{labs,credits,payments,subscriptions}/` + `stripe/` all exist;
  `credits/cost.go:86-90` `creditCost` = build **5**, refine **1**, translate **1**;
  `:133` `MarginMarkup = 1.40`; `:142` `PricePerCreditUSD = 0.45`; `:231`
  `DefaultSeedBalance int64 = 500`. **NB the source's own package comment at `cost.go:29` says
  `course.refine → 5`, which is stale — the corpus's "1" matches the map and the `ActionCourseRefine`
  doc-comment's D1 ruling at `:59-67`.** `labs_budget.go:26-28` is
  `essential`/`professional`/`frontier`; `:247-251` are exactly the five `CapReason` values;
  `spend_reconciler.go:60` `Interval: 30 * time.Second`; `labsapi/client.go:6` default `:7070`;
  `labcontent/labcontent.go:22` *"exactly one base (LocaleEN) body row and 0..6 overlay rows"* = the
  doc's "EN base + 6 overlays"; all five named migrations exist. `app` at `5ba17044` **is** tagged
  `v1.363.2`. `checkout.session.completed` appears in **0** Go files at `origin/main` (positive control:
  `customer.subscription.created` hits `internal/payments/handler.go` and
  `internal/web/backend/api/api.go`), and `POST /credits/purchase` is documented as removed in Wave 13
  at `internal/web/backend/credits/handler.go:12`. The
  `knowledge/plan/releases/06.00-shared-purse/` milestone directories exist in the app repo.
- **Course Builder.** `internal/coursebuilder/` = **102** `.go` files (the doc's "~100");
  `bedrock.go:98-103` `ModelBackendName()` returns `"anthropic-api"`/`"bedrock"` and `:105-114` is
  `newUnderlyingClient`; `main.go:770` is the one startup log line, `:766-769` the comment, `:774` and
  `:778` the two `logger.Warn(… routes disabled)` arms, `:824-826` the `OPENAI_KEY` cover client —
  **all five anchors exact at the ref the passage names (`b948604` v1.366.0)**.
  `terraform/variables.tf:635-639` declares `anthropic_api_key` `sensitive = true` with no default;
  `ssm.tf:328-333` creates the parameter; `main.tf:555` injects `ANTHROPIC_API_KEY`.
  `68c24512` really does delete the `os.Getenv("COURSEBUILDER_OPENAI_IMAGE_KEY")` read from `main.go`,
  and the name survives only in `internal/coursebuilder/GO-LIVE-RUNBOOK.md:35` and `README.md:429` —
  exactly the "stale in-repo markdown" the doc names. `go.mod` = **go 1.26.4**.
  `main.go:173 coursebuilderWorkerConcurrency = 20` → `worker.NewCoursebuilderServer(...)` at `:899`.
  `budget.go:27` `DefaultMaxMonthlyCOGSUSD = 500.0`, `:41` `DefaultMaxDailyCOGSUSD = 0.0`,
  `rate.go:28` `DefaultSessionsPerOrgPerDay = 50`. `bedrock.go:23`/`:29` default
  `eu.anthropic.claude-opus-4-8` / `eu.anthropic.claude-sonnet-4-6`, envs at `:36`/`:41`.
  **Translate = 7 locales**: `translate.go:155` `case "it", "fr", "de", "es", "nl", "ja", "pt":`
  — counted, 7.
- **CustomerIO Sync.** `docker-compose.yml:154` = `profiles: [customerio-sync, all]` at `0dab54d`
  (exact line), `:143-144` `ports: - "8080:8080"`, `:138`
  `context: git@github.com:anthropos-work/customerio-sync.git#main` with `ssh: ["default"]` and
  `GH_ACCESS_TOKEN: $GH_PAT`, `:148` the `DB_CONNECTION_BACKEND` DSN with `search_path=public`,
  `:151-153` `depends_on: postgresql: condition: service_healthy`. `repos.yml` at `0dab54d` lists six
  repos (`app`, `sentinel`, `storage`, `messenger`, `next-web-app`, `studio-desk`) — **no
  `customerio-sync`**, so "not cloned locally by `make init`" holds. **This file is the one that gets
  the profile question right** (see B1).
- **`architecture/README.md`'s index.** All ten non-README files it names exist in
  `corpus/architecture/`, and the directory holds exactly those ten plus `README.md` — the index is
  complete in both directions. `rosetta-extensions/stack-core/platform_alignment_guard.py`,
  `rosetta-extensions/alignment/` and `rosetta-extensions/stack-seeding/dna/` all exist in the
  authoring copy.

**RE-DERIVED** (arithmetic the document itself displays, re-computed — the weakest clearance, listed
separately per §5 rule 24):

- `ai-readiness.md:544` — `round(1.0/25.0*30)` = 1.2 → **1**; `:555` `round(9.0/25.0*30)` = 10.8 → **11**.
  The denominator `25.0` I re-derived from the *set* (see the ENUMERATED entry) rather than from the sum
  the doc shows.
- `ai-readiness.md:342-343` — `156/199` = 78.39 %, consistent with the "78.4%" claimed. I could **not**
  enumerate the seeded population itself (no live stack).

---

## What I could not settle

- **Every live DB row count.** `ai_architecture.md:138` (`skill_embeddings` = 42,790,
  `job_role_embeddings` = 18,919, measured 2026-06-29) and `:144-146` (22,470 public job roles) are
  pinned production measurements; there is no DB in this environment. Settled only by re-running the
  documented query against prod under `/db-query`.
- **Every live latency/probe number in `ai-readiness.md`** — the 2.09 s live recompute, the 24 ms frozen
  read, the 180 s `?cycle=<closed>` timeout, the 180 s → 19 ms demo-patch delta, and the
  *"`/home` body contains 'AI Readiness' → NO"* measurement at `:110-112`. Settled only by a cold
  reset-to-seed on a running stack.
- **`graphql-wundergraph.md:8`** — *"the repo ARCHIVED on GitHub, 2026-07-30 (read-only)"*. Not
  observable from a local clone; settled by `gh repo view anthropos-work/graphql-wundergraph --json
  isArchived,archivedAt`.
- **`ai-labs.md:5`** — *"`anthropos-work/AI-Labs` is the live Go control plane"*. Not in the ground-truth
  clone set; settled by `gh repo view anthropos-work/AI-Labs`.
- **`ai_architecture.md:151`** — the skiller migration ids `20260417103036` / `20260417120309`. The
  `skiller` repo is not in the clone set, and neither id appears under `app/terraform/migrations/`
  (where the ported tables arrive via `20260615130000_skiller_taxonomy.sql`). Settled by a `skiller`
  clone.
- **`ai_architecture.md:83`'s model inventory list** (GPT-5.4 … O4-mini). These are provider-catalog
  names rather than constants I can enumerate from a single `app` symbol; I verified the four
  Anthropic names against the Directus `AIModel` enum but not the eleven OpenAI ones.
- **`ai-readiness.md:199-205`'s `report`-key → render mapping.** I confirmed the reader
  (`usageDimensionsFromReports`, `holdsBackFromInsights`, `resolveSessionAuthors`) and the 5 KPI ids,
  but not that `holdsBackFromInsights` filters on a category string *containing* `"risk"` — that needs a
  read of `how_we_measure.go:986` onward, which I ran out of budget for. Nothing I saw contradicts it.
- **`ai_architecture.md:199` vs `:204`** — the doc calls ElevenLabs the *"active (legacy) default"* for
  the call/reply pipeline four words after stating that a nil `voice_engine` yields `gptrealtime`. The
  two senses of "default" are different and both may be true; I did not trace the call/reply pipeline's
  engine selection far enough to say whether the sentence is a contradiction or a compression. Settled
  by reading `internal/jobsimulation/simulator/simulator.go`'s voice-engine dispatch.
