# Seat A — reading #14

Refs at the moment of measurement (re-verified at open, all match the briefing):
`app b948604f` (v1.366.0) · `platform 0dab54df` · `next-web-app bb3313bc` · `sentinel 88bc5592` ·
`storage 4ce8ece5` · `messenger fa47850d` · `cms ca50c817` · `graphql-wundergraph 60c229f3` ·
`roadrunner 87d8d443` · `jobsimulation 462343b0` · `studio-desk 14a5442a` · `ant-academy 9c3843cd` ·
`.agentspace/rosetta-extensions` authoring `main` @ `1334a4f5`.

Grading policy I applied to anchor errors (stated so it is auditable): an anchor whose cited line
lands on an **unrelated construct in a different function/section, and is wrong at every ref the
passage names** is a BLOCKER; an anchor that has merely **drifted within its own neighbourhood**, or
that is **correct at the ref its own block pins**, is a MINOR. Where I hesitated I say so per finding.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| corpus/architecture/external_services.md | 840 | yes |
| corpus/architecture/security_compliance.md | 281 | yes |
| corpus/services/backend.md | 271 | yes |
| corpus/services/messenger.md | 146 | yes |
| corpus/services/academy-backend.md | 141 | yes |
| corpus/services/skiller.md | 66 | yes |
| corpus/services/TEMPLATE.md | 46 | yes |

(Also read in full, as the briefing instructs but not as an assigned file:
`corpus/ops/platform-alignment.md` §5, 1680 lines total.)

Search positive controls run in-pass: `grep -rn "anthropos-agent-eu"` returned 0 in platform source
while `grep -rln "anthropos-agent"` returned 3 platform files; `grep -c ivfflat` returned 0 **and the
pipeline was broken, not the corpus** — the SQL is uppercase `IVFFLAT`, found on the case-insensitive
re-run (this is exactly §5 rule 3, and it would have produced a false BLOCKER).

---

## Blockers

### B1 — `core` profile does not start nine containers, and three of the seven named services no longer exist in compose

- **Anchor:** `corpus/architecture/external_services.md:169-174` —
  > "The default `core` profile is **not** just Postgres + `backend`: it starts **nine** containers —
  > `postgresql` + `redis` (from the included `common.yml`, profile-less so they always start) and
  > seven application services, `sentinel` · `backend` · `jobsimulation` · `cms` · `storage` ·
  > `roadrunner` · `gotenberg`. The `jobsimulation` and `cms` containers are **merged-into-`app`
  > husks kept live as the rollback path** (teardown is M810)"

  Same defect carried into the mermaid at `:180` (`CMSHusk[cms :8090-8091 — merged-into-app husk,
  rollback path]`) and `:194` (`CMSHusk -.->|DIRECTUS_BASE_ADDR from compose| Directus`).
- **Why it is false:** At platform `0dab54d` `docker-compose.yml` declares exactly eight services —
  `sentinel:5`, `backend:28`, `storage:102`, `customerio-sync:136`, `messenger:156`, `studio-desk:197`,
  `next-web-app:228`, `gotenberg:255` — and `common.yml` two more (`postgresql:2`, `redis:24`).
  **There is no `cms`, no `jobsimulation` and no `roadrunner` service at all**: `platform/d11a403`
  (2026-08-03, *"chore(compose): drop roadrunner, prune dead env, repoint messenger"*) deletes all
  three (`git show d11a403 -- docker-compose.yml | grep '^-  '` → `-  jobsimulation:`, `-  cms:`,
  `-  roadrunner:`). `storage` carries `profiles: [storage-legacy]` (`docker-compose.yml:134`) with an
  in-file comment *"v9.0: NOT in the default profiles any more"*. The `core` selection is therefore
  `postgresql` + `redis` + `sentinel` (all three profile-less) + `backend` (`:100`) + `gotenberg`
  (`:268`) = **five** containers, not nine, and four of the seven named application services are
  unstartable or not selected.
- **Self-contradiction:** the same document says the opposite twice — `:135` correctly cites
  `d11a403` as the commit that removed the standalone `cms` service from compose, and `:295-296`
  states *"There is **no `cms` container left to start**"*.
- **Confidence:** high

### B2 — the `cms` container is presented as live and inspectable, including a runnable command naming three non-existent services

- **Anchors:**
  - `corpus/architecture/external_services.md:722-724` — "`docker compose up -d backend` # NOT
    `graphql` … **and NOT `cms`** (that container is an unfederated HUSK that serves no subgraph)"
  - `:727-728` — "The platform compose has no `directus` service to start; **`cms` points
    `DIRECTUS_BASE_ADDR` at `content.anthropos.work`.**"
  - `:798-799` — "`docker compose logs backend` (not `directus`, and **not `cms` — that container is a
    merged husk** that no longer serves `backend`'s content reads)"
  - `:821` — "`docker compose ps backend cms jobsimulation storage`"
- **Why it is false:** as B1 — `cms`, `jobsimulation` and `roadrunner` are not compose services at
  platform `0dab54d`. `:821` is a **runnable instruction**, not a warning, and three of its four
  arguments name services compose does not know; it cannot succeed. `:727-728`'s claim that "`cms`
  points `DIRECTUS_BASE_ADDR`" is doubly wrong: the only `DIRECTUS_*` key in the whole compose file is
  `docker-compose.yml:53 DIRECTUS_PUBLIC_BASE_ADDR` on **`backend`** (`grep -n DIRECTUS
  docker-compose.yml` returns exactly one line), which the same document states correctly at
  `:132-136`. This is §5 rule 30 ("do not spell a dead command in runnable form") violated in an
  instruction rather than a warning.
- **Confidence:** high

### B3 — "platform `0dab54d`'s compose declares nine services" — it declares eight (ten with the include)

- **Anchor:** `corpus/architecture/external_services.md:295-296` — "There is **no `cms` container left
  to start** — platform `0dab54d`'s compose **declares nine services** and `cms` is not one of them"
- **Why it is false:** `docker-compose.yml` declares **8** services (enumerated in B1); with
  `include: common.yml` the project has **10**. Neither reading is nine. The likely origin is the
  §5 rule 24 trap — `grep -cE '^  [a-z][a-z0-9_-]*:$' docker-compose.yml` returns **9**, because it
  also counts `app-network:` at `:271`, which is a **network** key under `networks:`, not a service.
  The predicate the count supports ("`cms` is not one of them") is TRUE; only the cardinality is wrong.
- **Confidence:** medium-high. I hesitated because the load-bearing conclusion is correct, but a wrong
  count is the explicit BLOCKER class and this one sits inside the sentence that retracts B1/B2.

### B4 — `docker-compose.yml:352` / `:361` do not exist; the file is 271 lines, and the same doc gives the right anchors

- **Anchor:** `corpus/architecture/external_services.md:738-739` —
  > "# NB the var is WUNDERGRAPH, not GRAPHQL … Set on the image at **docker-compose.yml:352 (build
  > arg) and :361 (runtime env)**."
- **Why it is false:** `wc -l stack-demo/platform/docker-compose.yml` = **271**. The
  `NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT` build arg is at `docker-compose.yml:236` and the runtime env at
  `:245`. `external_services.md:198` states exactly those two numbers correctly (*"`docker-compose.yml:236`/`:245`
  for next-web-app"*), so the document contains both the right and the wrong anchor for the same fact.
- **Confidence:** high

### B5 — `gen_injected_override.py:636-637` names the wrong construct at every ref tried

- **Anchor:** `corpus/architecture/external_services.md:210` — "`rosetta-extensions/stack-injection/gen_injected_override.py:636-637`
  re-points every service in `DIRECTUS_DATA_CONSUMERS`"
- **Why it is false:** at the authoring copy's `main` (`1334a4f5`) the re-point is
  `gen_injected_override.py:669-670` (`if with_directus and name in DIRECTUS_DATA_CONSUMERS:` →
  `env.append(f"DIRECTUS_BASE_ADDR={DIRECTUS_INNETWORK_ADDR}")`); `:636-637` lands in the middle of the
  **CORS** comment block for `backend`. At the only ref the surrounding blockquote pins (rext
  `f9ac72f`, named at `:213`) the re-point is at **`:598`** and `:636-637` is
  `lines += hiring_lines(n, offset, platform_dir, host=host)`. So the anchor is wrong at **both** refs
  and points at unrelated code in both.
- **Confidence:** medium. Hesitation: `rosetta-extensions` is a fast-moving tooling repo and line drift
  there is expected — but this one does not resolve at the pinned ref either, which is what moves it
  out of the MINOR class I applied to its two siblings.

### B6 — "make up # default graphql profile" contradicts the correction printed 112 lines above it

- **Anchor:** `corpus/services/backend.md:228` — "`make up                # default graphql profile — recommended`"
- **Why it is false:** `platform/Makefile:9-10` reads `# Default profile` / `PROFILE ?= core`, and
  `0dab54d`'s own subject is *"chore(compose): run without the standalone storage; **rename graphql -> core**"*.
  There is no `graphql` profile in `docker-compose.yml` at `0dab54d` (`grep -n 'profiles:'` returns
  `[core, backend, all]`, `[storage-legacy]`, `[customerio-sync, all]`, `[messenger]`,
  `[studio-desk, all]`, `[frontend, all]`, `[core, backend, all]`). `backend.md:116` in the same file
  states this explicitly: *"The default profile is `core`, not `graphql`: `0dab54d` renamed it.
  Corrected M257x iter-68"* — i.e. the correction landed and its twin four screens down survived. This
  is §5 rule 29's named false predicate (*a `graphql` profile exists*) still live.
- **Confidence:** high

### B7 — "all four occurrences in the merged `docker-compose.yml`" — `SKILLER_RPC_ADDR` occurs once

- **Anchor:** `corpus/services/backend.md:77-79` — "Consumers keep the env var, re-pointed:
  `SKILLER_RPC_ADDR=http://backend:8083` locally (**all four occurrences in the merged
  `docker-compose.yml`**)"
- **Why it is false:** `grep -c SKILLER_RPC_ADDR stack-demo/platform/docker-compose.yml` at `0dab54d`
  = **1** (line 183, on `messenger`). Measured across refs: `2adcf71` → 4, `0808b92` → 4,
  `0dab54d` → **1**. `d11a403` deleted the three other carriers (`cms`, `jobsimulation`, `roadrunner`)
  along with their services.
- **Ref consideration:** the section lead at `:59-61` says *"Verified 2026-07-08 … (`app@c3c45e01`
  v1.334.1, `platform@0808b92`)"*, at which the claim was true. Under §5 rule 33 a pin's scope is the
  claim's **own block**, and this is a different block two paragraphs down; the same section also
  asserts currency (*"This is the authoritative, verified statement of the merged shape — the contract
  the v2.1 re-ground grades against"*), which rule 33 says cannot be pinned into silence.
- **Confidence:** medium (booked up because of the currency assertion; a reader is told to expect four
  occurrences and will find one).

### B8 — messenger's compose `depends_on` does not include `cms` or `jobsimulation`

- **Anchor:** `corpus/services/messenger.md:101` — "Messenger depends on **`backend`, `cms`,
  `jobsimulation`** at startup (compose `depends_on`), so bringing it up implicitly brings the rest of
  the stack."
- **Why it is false:** `docker-compose.yml:186-192` at `0dab54d` gives messenger
  `depends_on: redis (service_healthy), postgresql (service_healthy), backend (service_started)` —
  three entries, none of which is `cms` or `jobsimulation`, and neither of those is a service at all
  (B1). The same file states the truth twice: `:125` — *"there is no `cms` container left to address"*
  — and `:126` — *"the husk container is gone"*.
- **Confidence:** high

---

## Minors

- `corpus/architecture/external_services.md:830` — "Consistent with **:512** above, where the same
  correction is already recorded." `:512` is "`- Schema exploration`"; the `docker compose restart
  graphql` → `restart backend` correction is at `:521`.
- `corpus/architecture/external_services.md:210` — `DIRECTUS_DATA_CONSUMERS` is cited at `(:53)`; at
  rext `main` `1334a4f5` it is `gen_injected_override.py:84`. Correct at the block's pinned
  `f9ac72f` (`:53`), so MINOR rather than part of B5. Value `("cms", "backend")` is exact at both refs.
- `corpus/architecture/external_services.md:219` — `stack-injection/tests/test_injection.py:1051` for
  `test_backend_the_actual_reader_is_repointed`; at rext `main` the test is at `:1108` (`:1051` is
  inside `test_studio_desk_no_env_file_without_platform_dir`). At the pinned `f9ac72f` the test is at
  `:1005` and `:1051` falls inside its body, so the pin covers it.
- `corpus/architecture/security_compliance.md:7` and `:198` — both cite `external_services.md:546` for
  *"no ordered EU-first fallback chain"*; `:546` is inside the `app/studio/**`-is-an-in-image-path
  blockquote. The sentence is at `external_services.md:554`. (Off by the 8 lines that blockquote adds
  — §5 rule 34.)
- `corpus/architecture/security_compliance.md:201` — cites `external_services.md:570` as the site that
  "enumerates **four live** ways a request leaves the EU"; `:570` is the 429-retry sentence, the
  enumeration is at `:579`.
- `corpus/architecture/security_compliance.md:185` — "consistent with `README.md:21`". Root
  `README.md:21` reads *"Project Rosetta is a **documentation repository** - not the Anthropos platform
  itself. It contains:"*. The `app/internal/aiusage`-not-the-`ai`-library statement is at
  `corpus/architecture/README.md:23`.
- `corpus/services/backend.md:43` — `app/CLAUDE.md:72` is cited as still listing
  `SkillPathSessionService`; at `app b948604` that line is `atlas migrate apply --dir file://terraform/migrations`
  inside a bash block. The list is at `app/CLAUDE.md:80`. The sibling anchor in the same sentence,
  `app/knowledge/architecture.md:28`, is exact.
- `corpus/services/backend.md:253` — "not in the top-level `migrations/` dir (which holds only
  `atlas.sum`)". At `app b948604` there is **no** top-level `migrations/` directory (`git ls-files |
  grep '^migrations/'` → empty; `ls migrations/` → No such file or directory), and the only `atlas.sum`
  in the repo is `terraform/migrations/atlas.sum`. The operative half of the sentence
  (`terraform/migrations/` is authoritative) is correct, which is why this is MINOR and not a BLOCKER.
- `corpus/services/academy-backend.md:15` — "(Consistent with :74-76 below.)" `:74-76` is the
  `## Interface Discovery` heading, a blank line and a bullet stub; the endpoint statement is at
  `:77-80`.
- `corpus/services/academy-backend.md:20` — "`app` SemVer, **currently** `v1.363.2` @ `5ba17044`".
  True at the named ref (`git log -1 5ba17044` → `chore(version): v1.363.2`), but the word *currently*
  asserts currency inside the same block as the pin while the checkout is **v1.366.0** and
  `origin/main` is past **v1.367.0** — §5 rule 33's "a block that asserts currency cannot be pinned
  into silence". Booked MINOR because the version is illustrative, not load-bearing, and the block
  carries its own sha.
- `corpus/services/TEMPLATE.md` — no factual claims of any kind; nothing to grade.

---

## Positively cleared (audited zeros)

**ENUMERATED** (predicate re-derived from source independently, cardinality stated before arithmetic):

- **The multi-tenancy fence, `security_compliance.md:66-135`.** Derived from
  `app/internal/data/ent/schema/` at `b948604`: **139** `.go` files; **135** contain `ent.Schema`
  (the 4 that do not are `database_types.go`, `mixin.go`, `skiller_mixins.go`, `skillpath_mixins.go`);
  `grep -lE '(^|[^A-Za-z])OrganizationMixin\{\}'` → **30 files**, of which
  `user_resource.go:22` is `// OrganizationMixin{},  // We need to work on this` → **29 live**;
  `OrganizationIDMixin{}` → **7** (`category`, `jobrole`, `similarity`, `skill`, `specialization`,
  `studio_document`, `studio_task` — the exact seven named); the `comm`/`xargs` derivation restricted
  to `ent.Schema` files → **18**, and the bare-`*.go` variant → **19** with the extra hit being
  `skiller_mixins.go`, exactly as the doc says. Of the 18, `org_membership.go` declares its own
  `Policy()` and `academy_feedback.go` carries `UserMixin{}` **at line 64** — I checked this one twice
  because a lines-1-30 grep found no mixin and would have produced a false BLOCKER. The remaining
  **16** match the document's list name-for-name. Files declaring any `Policy()` = exactly **4**
  (`mixin.go`, `org_membership.go`, `organization.go`, `user.go`); none of the three self-policing
  schemas is in the 29, so **29 + Membership + Organization = 31** with no double-count, and
  `16 + 7 = 23` unpoliced-with-`organization_id`. Anchors all resolve to the right construct:
  `mixin.go:126` (`func (OrganizationMixin) Policy()`), `mixin.go:98` (the UserMixin `Policy()` doc
  comment, func at `:99`), `org_membership.go:172`, `organization.go:56`/`:96`, `user.go:116`,
  `user_resource.go:22`, `job_simulation_session.go:5`, `jobrole.go:18`, `category.go:15`.
  `user_resource.go` contains no `organization_id` at all, so the "user-scoped, NOT org-scoped"
  sentence holds.
- **The supergraph 5→4→3→1 table, `external_services.md:363-376`.** Read straight off
  `git show <sha>:supergraph-config-prod.yaml` in `graphql-wundergraph`: `749dc86^` lists
  backend/skiller/jobsimulation/cms/skillpath = **5**; `749dc86` (2026-06-24) = **4**; `7c17e63`
  (2026-07-21) = **3**; `915da06` (2026-07-29) = **1**. `git show --name-status 915da06` marks **both**
  `schemas/cms.graphqls` and `schemas/jobsimulation.graphqls` `D`, and that commit's own subject really
  does read *"supergraph 2→1"* — the trap the doc warns about is real and its warning is correct.
- **No subscriptions, `external_services.md:377`.** `grep -rn "sse\|subscription" *.yaml` in
  `graphql-wundergraph` → 0 (stderr read, rc=1); positive control `grep -rln backend *.yaml` → all
  three configs. `schemas/backend.graphqls` has `type Mutation` at **`:4053`** and `type Query` at
  **`:4912`** and no `type Subscription`; `grep -c Subscription` = **6**, all Stripe/plan names.
  `subgraphs.conf` = `BACKEND=v1.360.0`; `schemas/` holds `backend.graphqls` alone.
- **Router port 8080 everywhere.** `terraform/locals.tf:8` `port = 8080`; `terraform/main.tf:48-49`
  `"containerPort": ${local.port}` / `"hostPort": ${local.port}`; `config.prod.yaml:5`
  `listen_addr: 0.0.0.0:8080`; `terraform/main.tf:20 service_desired_count = 1`.
- **`Dockerfile.dev` composition, `external_services.md:434-448`.** `wgc@0.104.0` at `:7`, the single
  schema `COPY` at **`:18`**, the fold comment at `:19-20`, the single `awk` at **`:23`**, runtime image
  `ghcr.io/wundergraph/cosmo/router:0.275.0` at `:29`. All exact.
- **The three-era build-config history, `external_services.md:416-430`.** `2c85211^`'s `graphql:`
  block has `context: ../graphql-wundergraph` and **no `dockerfile:` key**; `1e8e754:docker-compose.yml:8`
  is `dockerfile: graphql-wundergraph/Dockerfile.dev`; `b56d731:docker-compose.yml` still has
  `  graphql:` at **`:22`**, `360efd4` has none and `  backend:` at **`:28`**;
  `graphql-wundergraph/CLAUDE.md:39` really does assert the opposite, in `60c229f` dated 2026-07-30.
  Every date I re-read (`2adcf71`/`b56d731`/`360efd4` 2026-07-31, `a2a3ee6`/`2c85211` 2026-02-27,
  `67ba772` 2026-03-02, `63d285c` 2024-06-20, `d11a403`/`0dab54d` 2026-08-03) matches.
- **The retracted-then-un-retracted Directus compose service, `external_services.md:143-148`.**
  `git show a2a3ee6^:docker-compose.yml` → `:384 image: directus/directus:10.10.1`, `:386 - 8055:8055`,
  `:409 - ADMIN_PASSWORD=password`. Exactly the three numbers claimed; the `admin@example.com` email
  is indeed absent.
- **The AI-routing section, `external_services.md:529-614`.** `internal/jobsimulation/ai/ai.go`:
  vendor consts at **`:30-33`** (`azure`, `openai`, `anthropic-aws`, `anthropic`); `getClient` at
  **`:259-289`**; the `flag_use_azure_us` swap at **`:264-276`** with the error path returning the EU
  client; `AnthropicAws` and `Anthropic` both → `a.anthropicClient` at **`:280-283`**; `isThrottlingError`
  at **`:129-141`**; the `vendor = Openai` retry override at **`:150-155`** and mirrored at
  **`:296-302`**/**`:326`**; Bedrock `eu-west-1` at **`:85-88`**; `openai.NewOpenAI` at **`:80`**.
  Content-layer default: `internal/cms/directus/collections/jobsimulation.go:905` `AIVendor *AIVendor`
  and **`:1302`** `aiVendor := simulation.Openai`; `simulator/ai/ai.go:58-59` and the `default:` arm at
  **`:114-115`** both map to `internalAi.Openai`. Mistral OCR only at
  `internal/cms/studio/markdownManager.go:19` and `studioManager.go:583`. Model ids:
  `jobsimulation/agent/report_agent.go:31`, `askengine/bedrock.go:25`, `coursebuilder/bedrock.go:23`
  and `:29` — all exact. `coursebuilder/bedrock.go:106-113` is the `ANTHROPIC_API_KEY` switch;
  `main.go:770` is the `ModelBackendName()` boot log.
- **The Studio-Room latency claim (the four-live-plus-one-latent count).** `app/studio/configs/` holds
  three `.ini` files; `grep "_AI_.*_MODEL"` → **30** lines and every one begins `azure,`
  (`grep -v azure` → empty; value histogram printed and inspected). `gen.py:41-53` overrides only
  `*_API_KEY`/`*_ENDPOINT` (`:45-47` names the six), never the service selector.
  `services/ai.py:627` is `class AnthropicProvider(AIProvider):`, `:383` is
  `return OpenAI(api_key=self.api_key)`, `:704-724` is `get_client` with the
  `{openai, azure, anthropic}` dict at `:706-708`; `config_template.ini:30-31` is the
  `OPENAI_API_KEY`/`OPENAI_ENDPOINT=https://api.openai.com/v1/chat/completions` pair.
  `grep -rniE "bedrock|boto3"` under `app/studio/` → **0** (positive control: `anthropic` → 34).
  `git ls-files studio` in `app` → **0** tracked files, so the "in-image path" note is right too.
- **The env-var re-derivation at `external_services.md:639-644`.** `command grep -rho '\b<NAME>\b'
  --include='*.go'` in `app` returns **5 / 26 / 13 / 13** for `OPENAI_KEY` / `ANTHROPIC_API_KEY` /
  `AZURE_OPENAI_KEY` / `AZURE_OPENAI_ENDPOINT_URL`, and **0 / 0 / 0** for `OPENAI_ORG_ID` /
  `AZURE_OPENAI_ENDPOINT` / `AZURE_OPENAI_DEPLOYMENT`, `OPENAI_API_KEY` → **2**. Exact.
- **LiveKit agent names, `external_services.md:694`.** `internal/jobsimulation/calls/livekit.go:110`
  and `:120` are the bare `anthropos-agent`, `:115` is `anthropos-agent-chain`, `:126` is
  `fmt.Sprintf("anthropos-agent-%s", *location)`. `anthropos-agent-eu` appears **nowhere** in platform
  source (the only hits in the tree are rext test fixtures that are copies of the corpus).
- **The taxonomy numbers.** `.agentspace/snapshots/taxonomy/5afc0bccf1df7ef538b643321fc6362f/manifest.json`
  parsed: `public_only: true`, `predicate: org-null`, `captured_at 2026-06-29T14:43Z`, and row counts
  `skills 42790`, `job_roles 22470`, `skill_embeddings 42790`, `job_role_embeddings 18919` — every
  figure quoted in `external_services.md:658-671`, `backend.md:36/74`, `skiller.md:40-49` matches, and
  the 18,919-is-the-embedding-count explanation for the refuted "18K roles" is verifiable in the same
  file.
- **`backend.md` RPC mux.** `app/main.go` at `b948604`: mux at `:1185`, `UsersService` `:1187`,
  `OrganizationsService` `:1188`, `SkillerService` `:1196`, `JobSimulationService` `:1204`,
  `if cmsRPCServer != nil` `:1212-1214`, `LabSessionService` `:1228` — five unconditional + one
  conditional, exactly as claimed. `SkillPathSessionService` → **0** Go occurrences (positive control
  `SkillerService` → 7). `rpc.NewServer(..., rpc.WithWriteTimeout(60*time.Second))` at `:1243`.
  `LABS_API_URL` conditional at `:743-746`. The `DIRECTUS_BASE_ADDR` boot fatal at `:980-982`.
  The stale in-source comment *"until the M809 re-point"* is at `:1205-1211`.
- **`backend.md` Redis streams.** `main.go` `AddSubscriber` sites: `SKILLPATH_STREAM :1274`,
  `SKILLER_STREAM :1276`, `JOBSIMULATION_STREAM :1285`, `CMS_STREAM :1303`, `AI_USAGE_STREAM :1305`,
  `serviceName` (backend) `:1320` — five application streams plus the AI usage stream, one subscriber
  each.
- **`backend.md` directory map + AI-readiness count.** All 40 `internal/` paths listed exist;
  `internal/workforce/` contains no `readi*` file; `ls internal/data/ent/schema/ai_readiness*.go` =
  **13**, and the 13 minus the four the doc names as omissions leaves exactly the nine it calls the
  original set. `internal/academy/academy.go:6-9` is the aiacademy-removal statement, verbatim.
  `atlas.hcl` has `dir = "file://terraform/migrations"` and `src = "ent://internal/data/ent/schema"`;
  `terraform/migrations` holds 171 tracked files. `go.mod` → `go 1.26.4`.
  `categoryTree`/`fullCategoryTree` appear in the SDL tree only inside a comment saying they *"stay
  unported — no consumers"*.
- **`backend.md`/`messenger.md` RPC addresses.** `docker-compose.yml:171-172` is the comment
  *"cms + jobsimulation are folded into app: all four RPC edges are the one backend mux"* and
  `:173-183` sets `BACKEND_USERS_RPC_ADDR`, `CMS_RPC_ADDR` (`:174`), `JOBSIMULATION_RPC_ADDR` (`:176`),
  `SKILLER_RPC_ADDR` (`:183`) all to `http://backend:8083`.
- **The whole messenger v9.0 banner, at the refs it names.** `messenger a0ec933` (v0.42.1, one commit
  past the ground-truth clone) has `terraform/main.tf:29 service_desired_count = 0` with the
  rollback-path comment at `:27-28` and the cms precedent at `:25` — exactly as quoted. At
  `app 9d00a313` v1.367.0: `main.go:15/61/62` are the three `internal/messenger/{flow,adapters,sender}`
  imports; `:1386-1387` is `pubsub.NewSubscriberServer(msgflow.ConsumerGroup, …)`; `:1423`/`:1425` is
  `msgadapters.Wire(… msgsender.NewFromEnv(logger))`; `:1330-1340` is the take-over-the-group comment;
  `:1362-1365` is the `cmp.Or(os.Getenv("SERVICE_NAME"), "messenger")` literal rationale. `repos.yml:21-23`
  is messenger; `docker-compose.yml:156` / `profiles: [messenger]` at `:195`, and `0dab54d`'s in-file
  comment gives the two-consumers-on-one-group reason for dropping it from `all`.
- **messenger code anchors.** `go 1.25.0`; `getbrevo/brevo-go v1.1.3` (go.mod:11);
  `osteele/liquid v1.8.1` (go.mod:14); `internal/rpcsrv/rpcsrv.go:25-30` = the two `CodeUnimplemented`
  stubs; `cmd/root.go:63/:64/:107/:147` = PORT 8080 / RPC_PORT 8081 / REDIS_STREAMS_INDEX 2 /
  `READONLY_DB_CONNECTION`; `REDIS_WORKER_INDEX` → **0** Go occurrences; `internal/flow/flow.go:72-87`
  = `AddSubscriber("backend", …)` with the five `OrgSkillPath*` handlers; `jobsimulations.go:140-151`
  = the 2 h / 12 h staleness guards; `assignments.go:828` = `h.cms.GetSkillPath` inside `getSkillPath`.
- **Academy table names — the plural-vs-label warning.** Read from Ent's generated `Table =` constants:
  `academy_chapter_progresses`, `academy_last_activities`, `academy_chapter_times`,
  `academy_certificates`, `academy_bookmarks`, `academy_feedbacks`, `academy_series`,
  `academy_skill_paths`, `academy_chapters`, `academy_chapter_bodies`, `academy_path_embeddings` —
  all eleven exactly as documented, and `academy_certificate` singular indeed does not exist.
- **Academy surface.** Every query/mutation named at `academy-backend.md:81-88` is present in
  `schemas/academy.graphqls` (`academyProgress:640` … `upsertAcademyFeedback:719`, with
  `academyCertificate @public` at `:654`); `resolver_academy.go` exists; `/content/catalog.json` and
  the `sharedTokenMiddleware(os.Getenv("ACADEMY_CONTENT_API_TOKEN"))` group at
  `internal/web/backend/content_admin.go:35`; `academy_embedding_refresh` @ 03:00 named in
  `internal/academy/embeddings.go:108` and `schema/academy_path_embedding.go:16`;
  `internal/aireadiness/suggested_path.go` exists; `cmd/academy-seed` flags = exactly
  `user-email/user-id/fixture/reset/dry-run/list`; `cmd/academyImport` = `manifest/content-root/checkpoint`
  (+ `dry-run`, `metadata-only`); `0e37771f` = *"Academy backend v1.0 "ground truth" … (#903)"*.
  The `internal/academy/` file list and the four named test files all exist.
- **skiller.md.** `skill.go`, `jobrole.go`, `skill_embeddings.go`, `job_role_embeddings.go`,
  `category.go`, `specialization.go` all present; `internal/rpc/skillerrpc/` present;
  `skiller_taxonomy.graphqls` present; `CREATE INDEX … USING IVFFLAT` on both
  `job_role_embeddings` and `skill_embeddings` in
  `terraform/migrations/20260615130000_skiller_taxonomy.sql:61,251`; `extensions.vector(1536)` in the
  same tree; `enum ContentLanguage` in `assignments.graphqls:44-53` has exactly **8** values
  (english, italian, spanish, french, german, dutch, japanese, portuguese).
- **security_compliance.md's EU-AI-Act retraction.** `skills.go:53-62` is `calculateSkillScore`
  counting booleans and `:75` is the division; `criterion.go:126-127` is the hardcoded
  `switch critCheck.Engine { case check.EngineLlm:` dispatch; `:428` is
  `bulkChecker := NewLLMBulkChecker(c.logger)`; `templates/checkValidationBulk.tmpl:27` asks the model to
  *"assess whether the `<asset>` … meets or does not meet"*; `:168` dispatches `validateCodeDiff` and
  `:450-475` sets `success` from a pure string comparison. `checkerEngines` is assigned
  (`criterion.go:90`, `validator.go:60/595`) and never indexed — the "stored and never read" claim
  holds. The org-switch anchors `useOrgSelection.tsx:94`, `useResolveActiveOrg.tsx:107`,
  `useActivateMembershipOrg.tsx:81` resolve to `clerk.setActive({ organization: … })` in **both**
  `apps/web` and `apps/hiring`.
- **`/graphql` really is the playground.** `internal/web/backend/backend.go:315`
  `e.Any("/graphql", … ApolloSandboxHandler(…, "/graphql/query"))` and `:317` the endpoint — so
  `external_services.md:508` is right, and so is the `/graphql` → `/graphql/query` distinction in the
  banner at `:3`.
- **`app/internal/web/backend/backend.go:130`** is `"/api/webhook/clerk"` in the authn-skip list —
  the right construct for the "sync is backend's job" claim at `external_services.md:790`;
  `DIRECTUS_WEBHOOK_SECRET` fail-closed at `main.go:1088-1089` and the route at `backend.go:324`.
- **`corpus/services/TEMPLATE.md`** — read in full; it is a pure skeleton with no factual assertions.

**RE-DERIVED (weaker — recorded as such, not as clearance):** none. Every clearance above enumerated
its own set from source rather than re-computing a sum the document displayed.

---

## What I could not settle

- **`corpus/architecture/security_compliance.md:261`** — "Data Processing Agreement (DPA v1.4) with
  **18** approved sub-processors", and `:266-272`'s partial list (which names 11). No DPA artifact
  exists in this tree and no platform repo carries one. Settled by: the signed DPA, or a
  sub-processor register.
- **`security_compliance.md:21-43, 151-173, 177-188`** — the VPC CIDR, subnet layout, TLS 1.3 cipher
  set, AES-256/KMS, the 6-hourly three-destination backup, the DR site, the 90-second/30-second ECS
  health-check cadence and the 90-day CloudWatch retention. These live in the `infrastructure`
  terraform repo, which is not among the 13 ground-truth clones. Settled by: adding
  `anthropos-work/infrastructure` to the clone set.
- **`corpus/services/messenger.md:95-96`** — "`make up PROFILE=messenger` alone **EXITS 1**". The
  premise is verifiable (messenger's `depends_on: backend` at `docker-compose.yml:191-192`, `backend`
  in `[core, backend, all]`), but the exit code itself needs a Docker daemon; I ran no containers.
  Settled by: one `docker compose --profile messenger config` on a live daemon.
- **`corpus/services/skiller.md:25-27`** — "the skiller ECS service and its terraform module were
  removed from production … `app`'s internal app→skiller RPC path is retired (**app PR #989**)". The
  PR number and the ECR-orphan status are not checkable from the clones; the *code* half (no skiller
  container, `SkillerService` served by `app`) is verified above.
- **`corpus/architecture/external_services.md:3`** — "**the repo is ARCHIVED on GitHub (2026-07-30)**"
  for `graphql-wundergraph`. Repository archival state is not visible in a clone. Settled by:
  `gh repo view anthropos-work/graphql-wundergraph --json isArchived`.
- **Whether `academy-backend.md:100`'s "Downstream: the **storage** service (`STORAGE_RPC_ADDR`…)"
  is stale.** At the `b948604` checkout `STORAGE_RPC_ADDR` is alive (15 Go occurrences, incl.
  `cmd/academyImport/main.go:231` and `cmd/academy-asset-upload/main.go:129`) and `storage` is still a
  compose service (`profiles: [storage-legacy]`), so the passage is defensible **at the graded ref** —
  but `platform-alignment.md` §5 rule 32 records that at `app 9d00a313` v1.367.0 the same file names
  `STORAGE_RPC_ADDR` nowhere. I did not book it: the passage names no ref, the rule says grade at the
  checkout, and at the checkout it is true. Flagging it because it will become a finding at the next
  pin advance.
