# Seat D — reading #16

Refs at open (verified, not inherited): `app` `b948604f`, `platform` `0dab54df`, `next-web-app` `bb3313bc`,
`sentinel` `88bc5592`, `storage` `4ce8ece5`, `messenger` `fa47850d`, `cms` `ca50c817`,
`graphql-wundergraph` `60c229f3`, `roadrunner` `87d8d443`, `jobsimulation` `462343b0`,
`studio-desk` `14a5442a`, `ant-academy` `9c3843cd`, `rosetta-extensions` (authoring) `4e6b64d3`.
`app` `origin/main` is 4 commits past `9d00a313` (v1.367.0) — matches the briefing.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| corpus/services/studio-room.md | 474 | yes |
| corpus/architecture/architecture_overview.md | 381 | yes |
| corpus/services/cms.md | 270 | yes |
| corpus/services/roadrunner.md | 199 | yes |
| corpus/services/clerk-integration.md | 128 | yes |
| corpus/services/gotenberg.md | 82 | yes |
| corpus/services/intelligence.md | 18 | yes |

Also read (protocol, in scope): `corpus/ops/platform-alignment.md` §5 in full (lines 240–1191, 1726 total).

Search-pipeline controls run this pass (§5 rules 1/2):
- One zsh glob failure was caught and re-run quoted (`--include=*.go` was expanded by the shell;
  stderr read: `(eval):1: no matches found`). Every subsequent grep used `--include='*.go'`.
- `grep -rni "mistral"` over `app/studio` returned 0 → control `grep -rni "anthropic"` returned **20**.
- `grep -rn "RoadrunnerSubmissionCompleted" stack-demo/jobsimulation` returned 0 → controls:
  `find … -name '*.go' | wc -l` = **325**, `grep -rl "package " --include='*.go'` = **325**,
  `grep -rina "roadrunner"` = **1** (a comment). Pipeline sound.
- `grep -rn "anthropos-work/authn" …/go.mod` returned 0 → control `anthropos-work/colony` returned **4**.

---

## Blockers

### B1 — cms.md says the studio-room Python pipeline runs against Mistral; it has no Mistral path at all

- **Anchor:** `corpus/services/cms.md:67` — *"**Runs the AI generation pipeline** in-process. The Python
  project `anthropos-studio-room` is pulled into the image and dispatched as a subprocess; the Go side
  dispatches generation work, the Python code executes it against **OpenAI / Anthropic / Mistral**."*
  **Twin site, same claim:** `corpus/services/cms.md:184` — *"AI providers (Anthropic, OpenAI, **Mistral** —
  used by `cms/studio/` Python pipeline)"*.
- **Why it is false:** the pipeline's provider registry is closed and has three members.
  `app/studio/services/ai.py:704-710`:
  ```python
  def get_client(engine, target_override=None):
      providers = {
          'openai': OpenAIProvider,
          'azure': AzureProvider,
          'anthropic': AnthropicProvider
      }
  ```
  and `:715-716` raises `ValueError(f"Unknown AI service: {target_engine['service']}")` for anything else.
  The provider classes are `OpenAIProvider` (`:334`), `AzureProvider` (`:490`), `AnthropicProvider` (`:627`) —
  there is no Mistral class. Across the whole studio tree the token `mistral` occurs **exactly once**, in
  `app/studio/requirements.txt:8` (an unpinned, unimported dependency); `grep -rnia "mistral" .
  --exclude-dir=.git` returns that one line, with the `anthropic` positive control returning 20.
  `cms/studio/` is byte-identical (`cmp` rc=0, md5 `20a5329a…`), so the legacy repo is no different.
  Mistral **is** live in the platform, but on the **Go** side of the cms domain and for OCR only —
  `app/internal/cms/studio/markdownManager.go:11,19` (`mistral.NewMistral(nil, os.Getenv("MISTRAL_API_KEY"))`),
  called from `studioManager.go:583`. Also a corpus self-contradiction in three places:
  `corpus/services/studio-room.md:36` (*"AI Providers | OpenAI, Azure OpenAI, Anthropic"*), `:112`, `:388`
  (*"`services/ai.py` instantiates OpenAI / AzureOpenAI / Anthropic clients"*), and
  `corpus/architecture/external_services.md:541` (*"**Mistral (EU)** | direct client, not via the AI manager |
  cms domain **only** | **OCR only**"*).
- **Confidence:** high.

### B2 — roadrunner.md asserts jobsimulation still consumes the submission-completed event; nothing consumes it

- **Anchor:** `corpus/services/roadrunner.md:113` — *"On completion the worker publishes a
  `RoadrunnerSubmissionCompleted` event (carrying the Judge0 token) to Redis Streams
  (`REDIS_STREAMS_INDEX`) via colony pubsub; **jobsimulation consumes it as the async signal that
  execution finished**."*
- **Why it is false:** the publish half is real (`roadrunner/internal/runner/runner.go:276-278`), but the
  consumer does not exist at any live ref. `grep -rna "RoadrunnerSubmissionCompleted"` returns **0** in
  `stack-demo/jobsimulation` (@`462343b0`, v0.253.2) and **0** in `stack-demo/app` (@`b948604`) — controls
  above prove the pipeline. The removal is documented in platform source:
  `app/internal/jobsimulation/simulator/stream_handlers.go:30-34` — *"NOTE (jobsim-in-app re-sync to
  v0.253.0): chronos, realtime, and the roadrunner-submission pubsub event were all removed upstream …
  The session timeout and code-submission result are now delayed/queued Asynq TASKS
  (HandleSessionTimeoutTask on TimersQueue, HandleCodeSubmissionResultTask on CodeRunQueue), registered on
  the worker — NOT stream handlers. So there is no Chronos/Realtime/Roadrunner stream to expose here."*
  Corroborated at `app/internal/jobsimwiring/wiring.go:116-117` (*"IN-PROCESS; replaces the removed
  roadrunner RPC edge — resync to jobsim main v0.253.0, which deleted chronos + realtime + the
  roadrunner-submission event"*). The sentence also contradicts its own file twice: `roadrunner.md:117`
  (*"**Upstream consumers**: **none (orphaned — see the banner at the top).**"*) and the banner at `:9-10`
  (*"**nothing calls the roadrunner service any more**"*).
- **Why I hesitated:** `:57` scopes the body with *"Everything below describes the service as built, not as
  used."* That legitimately covers roadrunner's own publish behaviour — but the offending clause is a
  **present-tense claim about a different service's current behaviour**, and it is the exact statement the
  banner and the Dependencies section were written to retract. Booked as a blocker per the doubt rule.
- **Confidence:** medium-high.

### B3 — architecture_overview.md lists `authn` among the shared libraries "imported by services"; no service imports it

- **Anchor:** `corpus/architecture/architecture_overview.md:74` — *"4. **Shared Libraries**: colony, authn,
  proto, ai, taxonomy (not deployed, **imported by services**)"*. Same over-claim in the blockquote heading
  the shared-libraries table, `:215` — *"> **Imported as private Go modules** — **not** cloned by `make init`."*
  (the table it heads contains the `authn` row).
- **Why it is false:** no module file anywhere in the platform requires the standalone library.
  `grep -n "anthropos-work/authn"` over `app/go.mod`, `sentinel/go.mod`, `storage/go.mod`,
  `messenger/go.mod`, `cms/go.mod`, `jobsimulation/go.mod`, `roadrunner/go.mod` → **0 hits, rc=1**;
  positive control `anthropos-work/colony` → `sentinel/go.mod:8 v0.34.3`, `storage/go.mod:7 v0.34.3`,
  `messenger/go.mod:7 v0.35.2`, `app/go.mod:16 v0.35.2`. `app`'s other three are present and pinned
  (`app/go.mod:14 ai v1.40.2`, `:18 proto v1.210.0`, `:20 taxonomy v1.2.0`) — so the list is right about
  four of five and wrong about the fifth. Corpus self-contradiction: root `CLAUDE.md` states *"**`authn`
  is imported by nothing**"*; `corpus/architecture/shared_libraries.md` states *"`authn` is a dependency of
  no service"*; `corpus/services/clerk-integration.md:51` and `:91` state the same in this very audit scope
  (*"among the live Go services, imported by **`app` only**"* — meaning `colony/authn`, the in-colony
  package, not the repo).
- **Why I hesitated:** the `authn` **row** of the same table (`:222`) does disclose *"now shipped inside
  colony as `colony/authn` (standalone repo is legacy)"*, so a careful reader can reconstruct the truth.
  But `:74` and `:215` are the summary lines a reader acts on, and the corpus has explicitly retracted this
  elsewhere. Booked per the doubt rule.
- **Confidence:** medium.

---

## Minors

- `corpus/services/cms.md:78` — *"The app-Postgres **`cms.studio_*`** tables (`StudioDocument` / `StudioTask`)
  are 100% customer data and are never captured"*. Stale schema qualifier: the same file's `:28-31` says
  `studio_documents` / `studio_tasks` were re-created in **`public`** and the `cms` schema is *"legacy — no
  longer authoritative"*, and `app/terraform/migrations/20260724132049_cms_data_model.sql` creates them
  unqualified. No operational consequence — the capture is allow-listed, and `grep -rn
  "studio_document\|studio_task"` over `.agentspace/rosetta-extensions` returns 0, so nothing in the tooling
  keys on either name.
- `corpus/architecture/architecture_overview.md:25-28` and `:176-182` — the two "off by default (opt-in via
  Docker profile)" lists name only Storage / Messenger / CustomerIO Sync. Compose also declares the
  `frontend` profile (`docker-compose.yml:253`, `next-web-app`) and `studio-desk` (`:226`). Omitted list
  member.
- `corpus/architecture/architecture_overview.md:156` — the Backend row names *"the **cms**, **jobsimulation**
  and **roadrunner** domains"* and omits **skillpath**, which the same document names correctly at `:13` and
  `:68-70`.
- `corpus/services/roadrunner.md:117` — *"code execution having moved in-process to
  `jobsimulation/internal/runner/`"*. At the current shas that path is the **frozen legacy repo**; the live
  code is `app/internal/jobsimulation/runner/` — which this file's own banner (`:6-8`) states.
- `corpus/services/intelligence.md:5` — *"removed … in **mid-2026**"*. The cited commit `fdfa189` is dated
  **2026-04-17** (`git show -s fdfa189`); the section head at `:12` calls the same period *"pre-2026-Q2"*.
- `corpus/services/studio-room.md:61` — the directory tree is headed `studio-room/`. The repo is
  `anthropos-studio-room` and its in-image root is `app/studio/` — points the doc itself makes at `:9` and
  `:337-341`.

---

## Positively cleared (audited zeros)

**ENUMERATED** (predicate re-derived from source independently, cardinality stated before arithmetic):

- `architecture_overview.md:328` — *"only **31 of 135** schemas"*. Set re-derived, not sum:
  `grep -h "ent\.Schema" *.go` in `app/internal/data/ent/schema/` = **135** declarations
  (112 multi-line + 23 one-line `type X struct{ ent.Schema }`), one per file (139 files − 4 non-schema:
  `database_types.go`, `mixin.go`, `skiller_mixins.go`, `skillpath_mixins.go`).
  `OrganizationMixin{}` occurs **30** times; **exactly one is commented out** — `user_resource.go:22`
  `// OrganizationMixin{},  // We need to work on this` — so the live set is **29** distinct files.
  Schemas declaring their own `Policy()` outside `mixin.go`: **3** — `org_membership.go:172` (Membership,
  org-scoped: `AllowCurrentOrgEdgesOrSkipRule`), `organization.go:56` (Organization, `Query:
  rule.FilterSameOrganizations()`), and `user.go:116` (**User — filters by USER**
  `rule.FilterSameUserRule()`, correctly excluded). Neither `org_membership.go` nor `organization.go` is in
  the 29, so the union is disjoint: **29 + 2 = 31**. The doc's parenthetical composition is right in every
  part, including the commented-out anchor.
- `clerk-integration.md:37` — *"Webhooks (svix, **12** event types)"*. Enumerated every `case` in
  `app/internal/clerk/events/events.go:166-235`: user.created/updated/deleted, organization.created/
  deleted/updated, organizationInvitation.accepted/created/revoked, organizationMembership.created/
  deleted/updated = **12**.
- `clerk-integration.md:41` — *"Localization — 8 locales"*. `next-web-app/apps/web/src/providers/
  Clerk.provider.tsx:9-18` imports **8** (deDE, enUS, esES, frFR, itIT, jaJP, nlNL, ptPT). (`apps/hiring`
  imports 7 — no ptPT; the claim is unqualified but true of the main app.)
- `studio-room.md:181` / `cms.md:238` — *"registers **exactly nine** arguments (`gen.py:484-492`)"*.
  Enumerated: 9 `add_argument` calls at exactly 484–492, no others in the file.
- `cms.md:116-121` — the 9-package `requirements.txt` and **`python-docx` is NOT among them**. Enumerated
  both files; `cmp` rc=0 and md5 identical (`20a5329a7b7268cda9abf0dc76a8ca29`), so *"the two
  `requirements.txt` are byte-identical"* (`cms.md:224`) is exact, not approximate.
- `architecture_overview.md:172` / `:208` — *"its **23** run-state tables"*. `grep -ci 'CREATE TABLE'
  app/terraform/migrations/20260722081626_jobsim_data_model.sql` = **23**.
- `cms.md:28-30` — the six cms tables re-created in `public` with the same names. Enumerated the migration:
  exactly `similarities`, `studio_documents`, `studio_tasks`, `similarity_categories`,
  `similarity_features`, `similarity_skills` — the same six, no more, no fewer.
- `cms.md:35` / `architecture_overview.md:210` — *"**eight** services … ten in the effective topology"* /
  *"8 services remain"*. Enumerated top-level keys under `services:`: sentinel(5), backend(28), storage(102),
  customerio-sync(136), messenger(156), studio-desk(197), next-web-app(228), gotenberg(255) = **8**;
  `common.yml` adds postgresql + redis = **10**.
- `architecture_overview.md:12`/`:66`/`:151-152` — *"five containers … the last three declare no `profiles:`
  key"*. Enumerated all `profiles:` keys in `docker-compose.yml`: 7 of them (`:100 :134 :154 :195 :226 :253
  :268`) for 8 services → **`sentinel` has none**; `common.yml` declares none for postgresql/redis. `core`
  selects backend + gotenberg + the 3-service floor = **5**. `Makefile:10` `PROFILE ?= core` ✓.
- `cms.md:12-14` — *"6 entries … **four** `*_RPC_ADDR` … **all four** read `http://backend:8083`"*.
  `repos.yml` = app, sentinel, storage, messenger, next-web-app, studio-desk (**6**); `grep -n "_RPC_ADDR"`
  = `:173 BACKEND_USERS`, `:174 CMS`, `:176 JOBSIMULATION`, `:183 SKILLER` — **4**, all `backend:8083`.
- `clerk-integration.md:51`/`:91` — *"129 files in `app` vs **0** in each of the other three"*.
  `grep -rl "colony/authn" --include='*.go'`: app **129**, sentinel **0**, storage **0**, messenger **0**.
- `studio-room.md:36`/`:388` — the provider set. Enumerated `get_client`'s registry (3) and the three
  `AIProvider` subclasses at `services/ai.py:334, 490, 627`.
- `architecture_overview.md:272` — *"all three `app/studio/configs/*.ini` pin `azure`"*. Read all three in
  full: 10 `*_AI_*_MODEL` rows each, every one `azure, …` — 30/30.
- `architecture_overview.md:315-317` — the ref-split storage claim. At `9d00a313` (v1.367.0)
  `git grep STORAGE_RPC_ADDR -- '*.go'` returns 3 hits and **all three are comments** (`wiring.go:101`,
  `storagens/callsites_test.go:189`, `main.go:451`) → **zero reads**, and `main.go:451` reads verbatim
  *"now — the standalone service takes no traffic and STORAGE_RPC_ADDR is gone."* At the checkout `b948604`
  it **is** read (`internal/jobsimwiring/wiring.go:115`, plus `main.go:446,524,992` and three `cmd/`
  binaries). Both halves of the doc's "state the ref you mean" clause hold.
- `roadrunner.md:70`/`:174-184` and `cms.md:85` — the historical compose-supplied ports **at `2adcf71`**.
  Read `git show 2adcf71:docker-compose.yml`: roadrunner ports `:291-292`, `PORT=10400` `:298`,
  `RPC_PORT=10401` `:302`, env block `:296-302`; cms ports `:154-155`, `PORT=8090` `:169`,
  `RPC_PORT=8091` `:173`. Every anchor exact.

**Exact `file:line` anchors opened and read with surrounding context (§5 rule 10) — all resolved to the
construct claimed:** `roadrunner/terraform/main.tf:19` (=1) · `roadrunner/cmd/root.go:84`,`:110` ·
`cms/terraform/main.tf:39` (=0) · `cms/go.mod:3` · `cms/cmd/root.go:77`,`:78` · `cms/Dockerfile:2`,`:23` ·
`cms/cmd/root.go:242` (`/webhooks/`, no auth wrapper) · `jobsimulation/terraform/main.tf:40` (=0) ·
`graphql-wundergraph/terraform/main.tf:20` (=1) · `app/main.go:1205-1211` (the stale "DORMANT … until the
M809 re-point" comment — present exactly as quoted, and correctly flagged stale) ·
`app/internal/skillpath/session.go:205-207` · `app/internal/jobsimwiring/wiring.go:115`,`:118` ·
`app/internal/cms/studio/studioManager.go:94-96`,`:119`,`:583` ·
`app/internal/cms/studio/markdownManager.go:11`,`:19` · `app/internal/cms/worker/worker.go:29`
(`Concurrency: 5`) and `queues`: `AiVideoQueue: 7` / `StudioQueue: 3`, literals `"ai_video"` / `"studio"` ·
`app/internal/jobsimulation/ai/ai.go:85-88`,`:129`,`:166`,`:262-276`,`:325` ·
`app/internal/data/ent/schema/user_resource.go:22` · `docker-compose.yml:93`,`:131-133`,`:134`,`:174`,`:268` ·
`gen.py:18-28`,`:205-238`,`:241-271`,`:273-282`,`:450-456`,`:484-492` ·
`agents/simulation/postgen/exporter.py:513-550`,`:518-519` · `agents/simulation/model.py:59`,`:467-469` ·
`services/ai.py:1-2`,`:383`,`:530`,`:664` · `services/taxonomy.py:11`
(`BASE_URL = "https://api.anthropos.work/api"`) · `studio/CLAUDE.md:12-14` ·
`next-web-app/apps/mobile/package.json:6` (`~2.6.18`) · `ant-academy/mobile/package.json:18` (`~2.19.36`) ·
`ant-academy/code/package.json:52` (`^6.39.2`) · `app/go.mod:31` (`clerk-sdk-go/v2 v2.7.0`, identical at
`5ba17044` and at the checkout) · `apps/web/src/app/api/metabase/route.ts:35`
(`authData.orgRole !== 'admin'`) · `studio-desk/src/index.ts:96` + `app/services/userService.ts:16`
(`STUDIO_ACCESS_ROLES`) · `ant-academy/mobile/src/components/ClerkSignInForm.tsx:48-51` (aborts with
*"Sign-in needs another step that this app does not support yet."*) · `external_services.md:555` (the
retraction sentence — the anchor is exact) and `:577-582` (the four-live-plus-one-latent enumeration).

**Also cleared by measurement, worth naming because prior readings got them wrong:**
- `cms.md:41-46` / `architecture_overview.md:209`,`:262` — the **3 → 1** subgraph step. `git show
  915da06^:supergraph-config-prod.yaml` lists **backend, jobsimulation, cms**; `git show
  915da06:supergraph-config-prod.yaml` lists **backend** alone, and the `--stat` shows `schemas/cms.graphqls`
  and `schemas/jobsimulation.graphqls` both deleted in that one commit. The commit's own subject says
  *"supergraph 2→1"*; the corpus is right and the subject line is wrong, exactly as documented. Ladder
  confirmed: `749dc86` deletes `schemas/skiller.graphqls` (5→4), `7c17e63` deletes `schemas/skillpath.graphqls`
  (4→3).
- `roadrunner.md:16-19` / `architecture_overview.md:198-200` — *"grade that commit by its diff, not its
  message."* `git show d11a403 -- repos.yml` removes `- name: cms`, `- name: jobsimulation` **and**
  `- name: roadrunner`, while the message asserts roadrunner's entry *"was already gone"*. The corpus reading
  is correct.
- `architecture_overview.md:3` — `b56d731` + `360efd4` are the two commits merged by `2adcf71`
  (`git log --oneline` confirms the exact order and subjects).
- `gotenberg.md` — every claim checked and correct: `profiles: [core, backend, all]` at `docker-compose.yml:268`;
  `app/internal/converter/gotenberg.go` `ConvertToPDF(ctx, gotenbergURL, document, filename) ([]byte, error)`
  with `&http.Client{Timeout: 90 * time.Second}` and `POST {url}/forms/libreoffice/convert`;
  `GOTENBERG_URL=http://gotenberg:3200` set **only** on `backend` (`:57`), so *"the only consumer"* holds;
  the compose `command` block matches the file verbatim including `--libreoffice-restart-after=50`.
- `roadrunner.md` async section — `MaxRetry(3)` (`internal/runner/runner.go:126`), queue
  `"roadrunner:default"` (`worker/queues/queues.go`), `Concurrency: 10` (`worker/worker.go`),
  `maxRetries = 15` + `time.Sleep(1 * time.Second)` (`runner.go:244,273`), task-type constant
  `"roadrunner:submissionresult"`, **0** `*_test.go` files, `Dockerfile:18 RUN go test -v ./...`,
  `strconv.Atoi(os.Getenv("REDIS_WORKER_INDEX"))` at `cmd/root.go:68`, `godotenv/autoload` at `main.go:9`,
  language map takes `py` not `python`, no vendored `.proto`. All correct.
- `clerk-integration.md:44` "Not used" list — spot-checked with a live positive control: `<SignIn` **5+ hits**,
  `<UserProfile` **3 hits**, while `<Protect` = **0** and `<UserButton` = **0** across `next-web-app/apps` and
  `ant-academy`; the `OrganizationSwitcher` hits are a local component (`FourOhFourOrganizationSwitcher`), not
  Clerk's. `getToken()` is called with no template argument at every site sampled.
- `cms.md:78` — `DIRECTUS_DATA_CONSUMERS` really does name both:
  `rosetta-extensions/stack-injection/gen_injected_override.py:84` `= ("cms", "backend")`.
- `cms.md:74` — *"[jobsimulation] has no `DIRECTUS_BASE_ADDR` of its own"*: **0** case-insensitive `directus`
  hits in the whole `jobsimulation` clone (control: 325/325 Go files match `package `).

**RE-DERIVED only (weaker clearance, flagged as such):**
- `architecture_overview.md:332-333` — *"**23** schemas carry an `organization_id` with no policy at all
  (**16** is the neither-mixin subset)"*. Internally consistent (16 + the 7 `OrganizationIDMixin{}` users =
  23) and consistent with its cited source `security_compliance.md:91-98,108-128`, whose derivation is
  30 mixin / 7 id-only / 18 bare-with-neither, minus the 2 of the 18 that declare their own `Policy()`
  (`organization.go`, `org_membership.go`) → 16. I independently confirmed the **7** `OrganizationIDMixin{}`
  users (`category, jobrole, similarity, skill, specialization, studio_document, studio_task`) and the
  `skiller_mixins.go:152` definition, but my own raw predicate (files with a literal `"organization_id"`,
  minus OrganizationMixin users, minus own-policy) lands at 21–22 rather than 18, because their derivation
  additionally subtracts `UserMixin{}` carriers. **I did not reproduce 16 from scratch.** See "could not
  settle".

---

## What I could not settle

1. **`intelligence.md:18`** — *"Exposed an HTTP server on `:8080` (PORT env override) serving only a `/_meta`
   health endpoint … all real work ran on a 5-minute async ticker."* The `intelligence` repo is not cloned
   and is not in `repos.yml`. The one corroborating artifact I could reach is the deleted compose block
   (`git show fdfa189 -- docker-compose.yml`), which sets `PORT=9002` and `DB_CONNECTION_BACKEND` /
   `DB_CONNECTION_SKILLER` — consistent with the doc's env-var names and its "PORT env override" framing,
   but silent on the `:8080` default, `/_meta`, and the 5-minute ticker. **Settled by:** cloning
   `anthropos-work/intelligence` and reading its `cmd/`.
2. **GitHub archive states.** Six claims depend on them and none is reachable offline: jobsimulation ARCHIVED
   2026-07-31 (`architecture_overview.md:20`), graphql-wundergraph ARCHIVED 2026-07-30 (`:3`), roadrunner
   **NOT** archived (`roadrunner.md:36,67`), chronos NOT archived / last push 2026-04-23 (`roadrunner.md:49-51`),
   skiller 2026-07-01 and skillpath 2026-07-31 (`roadrunner.md:32`). **Settled by:** `gh api
   repos/anthropos-work/<name> --jq .archived,.pushed_at` for each.
3. **The `ai` and `taxonomy` shared libraries' contents** — `architecture_overview.md:221` (*"OpenAI, Azure,
   Anthropic, **Bedrock**, Mistral"* behind one `ai.AI` interface) and `:223` (*"taxonomy is a **node-id
   library**, not a dataset"*). Neither repo is cloned (both are pulled at Docker build via `GH_PAT`); I can
   confirm only the pins `ai v1.40.2` / `taxonomy v1.2.0` in `app/go.mod:14,20`. **Settled by:** reading the
   module sources in the Go module cache or cloning them.
4. **The 16/23 unpoliced split** above — I could re-derive the 7 and the internal arithmetic but not the 16
   independently. **Settled by:** running `security_compliance.md:126-128`'s own `comm`/`xargs` derivation
   verbatim and comparing the produced **file list** (not the count) against the nine names the doc prints —
   which is the §5 rule 24 shape and the reason I am flagging rather than clearing it.
5. **Actual prod ECS state.** Every prod claim I checked is graded against *declared* terraform
   (`service_desired_count`), which is a declaration, not a running-service measurement. That is the corpus's
   own convention and I applied it consistently, but `roadrunner.md:20-21`'s *"the one row where prod and the
   platform's own declaration contradict each other — recorded, not resolved"* stays unresolved by
   construction. **Settled by:** `aws ecs describe-services` against the prod cluster.

### Out-of-scope observation for the orchestrator (not booked — not in my file set)

Root **`CLAUDE.md`** asserts, in the storage/messenger fold banner: *"Prod compute for both is **stopped**
(`storage/terraform/main.tf:38`, `messenger/terraform/main.tf:29`, each `service_desired_count = 0`)."*
Measured at the pinned clones: `storage/terraform/main.tf:19` and `messenger/terraform/main.tf:19` each read
`service_desired_count = **1**`, and that is the **only** occurrence of the setting in either repo
(`grep -rn "service_desired_count" storage/ messenger/` → one hit each). The cited anchors resolve to
unrelated lines — `storage/terraform/main.tf:38` is `"containerPort": ${local.port},` inside the
`container_definitions` heredoc, and `messenger/terraform/main.tf:29` is `container_definitions = <<EOF`.
Both files are longer than the cited lines (100 and 111 lines), so this is a wrong-construct anchor plus a
wrong value, not line drift off the end. **None of my seven assigned files repeats it** — `cms.md:7`
(`cms/terraform/main.tf:39` = 0), `roadrunner.md:13` (`roadrunner/terraform/main.tf:19` = 1) and
`architecture_overview.md:20` (`jobsimulation/terraform/main.tf:40` = 0) are each exactly right. Flagging it
here because `CLAUDE.md` is the highest-propagation site in the tree (§5 rule 19's corollary) and this is a
load-bearing claim in the v9.0 fold narrative.
