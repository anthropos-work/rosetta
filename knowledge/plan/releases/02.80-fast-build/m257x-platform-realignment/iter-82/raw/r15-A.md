# Seat A — reading #15

**Refs used (stated at the moment of measurement, §5 rule 26):** corpus HEAD `1937e1f`
(branch `m257x/platform-realignment`); `app` `b948604f` (v1.366.0, `describe` confirms) with
`5ba17044` (v1.363.2) and `9d00a313` (v1.367.0) both reachable in the same clone;
`platform` `0dab54df`; `next-web-app` `bb3313bc`; `sentinel` `88bc5592`; `storage` `4ce8ece5`;
`messenger` `fa47850d` (= **v0.42.0**); `cms` `ca50c817`; `graphql-wundergraph` `60c229f3`;
`roadrunner` `87d8d443`; `jobsimulation` `462343b0`; `studio-desk` `14a5442a`;
`ant-academy` `9c3843cd`; `.agentspace/rosetta-extensions` `4e6b64d3` (`main`).

## Positive controls

| file | `wc -l` | read in full? |
|---|---|---|
| `corpus/architecture/external_services.md` | 842 | yes |
| `corpus/architecture/security_compliance.md` | 281 | yes |
| `corpus/services/jobsimulation.md` | 273 | yes |
| `corpus/services/academy-backend.md` | 147 | yes |
| `corpus/services/messenger.md` | 146 | yes |
| `corpus/services/skiller.md` | 66 | yes |
| `corpus/services/TEMPLATE.md` | 46 | yes |

Search-pipeline controls run in-pass, per §5 rules 1–2: `git grep -oh` silently returned **0**
for a pattern I knew matched (`\bpackage\b` in `*.go`), so every occurrence count below was
re-taken against an extracted worktree with `command grep -rhoE` and a `package → 2991` positive
control. Two zsh failures (`$n[` array-subscript expansion; `~1`/`^` in a loop variable) printed
to stderr and were read, not swallowed. Every "0 hits" claim below is paired with a control that
returned non-zero.

---

## Blockers

### B1 — `external_services.md` cites two rext anchors that resolve to the wrong construct

- **Anchor:** `corpus/architecture/external_services.md:208-211` —
  *"`rosetta-extensions/stack-injection/gen_injected_override.py:636-637` re-points every service in
  `DIRECTUS_DATA_CONSUMERS`, which is **`("cms", "backend")`** (`:53`)."*
- **Why it is false:** the sentence names no ref, so it grades at the checkout
  (`.agentspace/rosetta-extensions` @ `4e6b64d3`, the designated ground truth). Measured there:
  - `DIRECTUS_DATA_CONSUMERS = ("cms", "backend")` is at **`gen_injected_override.py:84`**, not `:53`.
    Line `:53` is `INJECTED = {"backend": "app", "cms": "cms", "jobsimulation": "jobsimulation"}` —
    a different constant, preceded at `:50-52` by a comment about test pins.
  - The re-point is at **`:669-670`** (`if with_directus and name in DIRECTUS_DATA_CONSUMERS:` →
    `env.append(f"DIRECTUS_BASE_ADDR={DIRECTUS_INNETWORK_ADDR}")`). Lines `:636-637` are the comment
    *"Per-service env overrides, collected and emitted as ONE `environment:` block …"* — the right
    file, the wrong construct, and it reads as correct to anyone who does not open it (§5 rule 10).
- **Confidence:** high (the measurement); medium on grading, because `rosetta-extensions` is an
  authoring copy on a moving `main` — but it is the ref this reading was given, and the passage
  pins nothing.

### B2 — `external_services.md` points the replacement-test citation at a different test

- **Anchor:** `corpus/architecture/external_services.md:217-219` — *"That test is gone, replaced by
  `test_backend_the_actual_reader_is_repointed` (`stack-injection/tests/test_injection.py:1051`),
  which asserts the opposite."*
- **Why it is false:** measured at `.agentspace/rosetta-extensions` `4e6b64d3`,
  `def test_backend_the_actual_reader_is_repointed` is at **`tests/test_injection.py:1108`**
  (its explanatory comment naming `test_only_cms_is_repointed_not_other_services` is at `:1109`).
  Line `:1051` sits inside `test_studio_desk_no_env_file_without_platform_dir` (declared at `:1050`)
  — an unrelated assertion about `env_file` ordering. A reader opening `:1051` to check the
  retraction reads a test that has nothing to do with Directus re-pointing.
- **Confidence:** high (measurement); medium on grading, same reason as B1.

### B3 — `academy-backend.md`'s own cross-reference lands on the certificate-minting bullet

- **Anchor:** `corpus/services/academy-backend.md:15` — *"The env-var *name* outlived the router; in
  production the router is still declared. **(Consistent with :74-76 below.)**"*
- **Why it is false:** `academy-backend.md:74-76` is the tail of the **Certificate minting** bullet
  (`:74` *"suffix from a 30-symbol ambiguity-free alphabet via `crypto/rand` …"*, `:75` the PII-free
  verify projection, `:76` a blank line) — nothing about the router, the subgraph or
  `NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT`. The passage the sentence is corroborating itself against is at
  **`:80-83`** (*"**There is no separate 'academy subgraph'** … The frontend reaches them at
  (`NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT`)"*). This is exactly §5 rule 34's class: an intra-corpus
  `<doc>.md:N` citation that an edit moved, which still lands on content and so reads as correct.
- **Confidence:** medium. **Why I hesitated:** the underlying factual claim (the endpoint is
  `backend`'s own `:8082/graphql/query` since `2adcf71`; the router is still declared in prod) is
  **true** and independently verified — `graphql-wundergraph/terraform/main.tf:20`
  `service_desired_count = 1`, and `platform` `0dab54d` has no `graphql` service. So the defect is a
  dangling pointer with no factual consequence, which the grading rubric's MINOR clause
  ("a broken markdown structure with no factual consequence") could arguably cover. I booked it up
  per the briefing's tie-break, and because a self-consistency cross-reference that points at the
  wrong paragraph is precisely the cheapest-to-detect class (§5 rule 21) and the one the corpus has
  been repaired for repeatedly.

---

## Minors

- `external_services.md:10,14` — the PM summary still calls the gateway one of *"four key external
  services"* that *"Unifies all backend services into a single API"*, while the same page documents
  it as **one** subgraph and **prod-only**, and separately catalogs LiveKit + AWS Chime as external
  services. Stale framing, qualified in the same bullet; no anchor is wrong.
- `security_compliance.md:234` — `skills.go:53-64` for `calculateSkillScore`: the function is
  `:53-62`; `:63` is blank and `:64` is the next function's header. Two-line overshoot.
- `security_compliance.md:73` — `mixin.go:98` for `UserMixin`'s `Policy()`: `:98` is the doc comment,
  `:99` is `func (UserMixin) Policy() ent.Policy {`. (Its twin `mixin.go:126` for
  `OrganizationMixin` is exact.)
- `security_compliance.md:196` — `ai.go:262-266` / `:85-88` are cited bare, with no repo-relative
  path, while the sibling bullet at `:209` gives the full
  `app/internal/jobsimulation/ai/ai.go`. Both ranges resolve correctly in that file.
- `jobsimulation.md:79` — *"consistent with the **Data** bullet, :31-38 above"*: the Data bullet is
  `:32-38`; `:31` is the last line of the Domain/engine bullet.
- `messenger.md` rpcsrv row (informational, platform-side not corpus-side):
  `internal/rpcsrv/rpcsrv.go:26` returns *"messenger.v1.MessengerService.**Send** is not
  implemented"* from `CancelScheduledMessage` — a copy-paste in the platform, correctly summarised
  by the corpus as "returns `Unimplemented`".

---

## Positively cleared (audited zeros)

Labelled per the briefing. **ENUMERATED** = I derived the predicate's set from source independently
and compared cardinalities; **RE-DERIVED** = I only re-computed arithmetic the document displays.

### ENUMERATED

1. **`security_compliance.md:66-137` — the multi-tenancy fence, the passage that has been wrong four
   times.** I enumerated the set, not the sum (§5 rules 17/24), at **both** `5ba17044` (the ref the
   blockquote names at `:107`) and the checkout `b948604f`; the two are identical:
   - `internal/data/ent/schema/*.go` = **139** files; **135** declare `ent.Schema`; the **4** that do
     not are `database_types.go`, `mixin.go`, `skiller_mixins.go`, `skillpath_mixins.go`.
   - `OrganizationMixin{}` grep-hits = **30**; `user_resource.go:22` is
     `// OrganizationMixin{},  // We need to work on this` — the **only** commented occurrence in the
     directory — so the live set is **29**.
   - `OrganizationIDMixin{}` = **7**, and they are exactly the seven named: `category`, `jobrole`,
     `similarity`, `skill`, `specialization`, `studio_document`, `studio_task`.
   - Files declaring any `Policy()` = **exactly four**: `mixin.go`, `org_membership.go`,
     `organization.go`, `user.go`. Inside `mixin.go` there are exactly **two** `Policy()` funcs
     (`UserMixin` `:99`, `OrganizationMixin` `:126`) out of **15** mixin types — so no third
     org-filtering mixin exists and the set cannot be larger. **None** of the 29 declares its own
     `Policy()`.
   - Neither-mixin schemas carrying `"organization_id"` = **18**; of those exactly **two** are
     otherwise policed (`org_membership.go` own `Policy()`, `academy_feedback.go` `UserMixin{}`),
     leaving **16** — and all 16 named in `:92-97` match my list one-for-one.
   - `user_resource.go` declares **no** `organization_id`, so it correctly falls outside the 23.
   - **Therefore 29 + `Membership` + `Organization` = 31, and 16 + 7 = 23.** Both figures are
     correct, and this time by the correct derivation.
   Supporting anchors all exact: `mixin.go:126`, `organization.go:56` + `:96`
   (`rule.FilterSameOrganizations()`), `org_membership.go:172-188` (both blocks end
   `privacy.AlwaysDenyRule()`; `AllowCurrentOrgEdgesOrSkipRule` at `:179`/`:183`), `user.go:116`,
   `job_simulation_session.go:5` (verbatim), `jobrole.go:18` / `category.go:15`.
2. **`external_services.md:364-377` — the subgraph-count history.** Re-derived the SET from
   `supergraph-config-prod.yaml` at each commit rather than trusting the table:
   `749dc86^` = **5** (backend, skiller, jobsimulation, cms, skillpath); `749dc86` = **4**;
   `7c17e63` = **3**; `915da06` = **1**. `git show --name-status 915da06` marks **both**
   `schemas/cms.graphqls` and `schemas/jobsimulation.graphqls` `D`. `915da06`'s own subject really
   does read *"supergraph 2→1"* against a 3-entry tree. The doc's warning is correct.
3. **`external_services.md:378` — "no subscriptions".** `schemas/backend.graphqls` @ `60c229f3`:
   `type Mutation` at `:4053`, `type Query` at `:4912`, **no** `type Subscription`; the **6**
   `Subscription` substrings are all Stripe/plan (`:1528`, `:1555`, `:4385`, `:4389`, `:4391`,
   `:4418`). All **three** `supergraph-config-*.yaml` carry no `sse`/`subscription` (positive control:
   all three match `backend`). `915da06~1:supergraph-config-prod.yaml:13` still reads
   `protocol: "sse_post"`. `git log -S 'protocol: "ws"'` from HEAD = **0**; `-S 'sse_post'` = **5**,
   exactly as claimed. `bba862f` is **not** an ancestor (rc 1) and `branch -a --contains` names only
   `remotes/origin/feat/use-web-socket`.
4. **`external_services.md:640-645` — the AI env-var occurrence counts at `app` `5ba17044`.**
   Extracted the tree (2129 `.go` files) and re-counted with the document's own predicate:
   `OPENAI_KEY` **5**, `ANTHROPIC_API_KEY` **26**, `AZURE_OPENAI_KEY` **13**,
   `AZURE_OPENAI_ENDPOINT_URL` **13**; `OPENAI_ORG_ID` / `AZURE_OPENAI_ENDPOINT` /
   `AZURE_OPENAI_DEPLOYMENT` all **0**; `OPENAI_API_KEY` **2**, both in
   `internal/cms/studio/studioManager.go` (`:1049` comment, `:1067` the subprocess remap). Every
   figure reproduces.
5. **`external_services.md:591-592,611-615` — "all 30 `*_AI_*_MODEL` lines pin `azure`".**
   Enumerated all three `app/studio/configs/*.ini`: 10 + 10 + 10 = **30** `*_AI_*_MODEL` lines, and
   **every one** has `azure` as its first field. `gen.py:41-53`'s `secrets_keys` list contains only
   `*_API_KEY`/`*_ENDPOINT` names — the service selector is not overridable from env. So the
   `anthropic` and `openai` arms really are latent.
6. **`platform` compose topology (asserted in `external_services.md:168-176,296-297`,
   `jobsimulation.md:43-44,231-232`).** `docker-compose.yml` @ `0dab54d` declares **8** services
   (`sentinel:5`, `backend:28`, `storage:102`, `customerio-sync:136`, `messenger:156`,
   `studio-desk:197`, `next-web-app:228`, `gotenberg:255`); `common.yml` adds `postgresql` + `redis`
   → **10** effective. `profiles:` keys exist on exactly **7** services — `postgresql`, `redis` and
   `sentinel` have **none**, so the always-on floor is 3 and `core` = floor + `backend` (`:100`) +
   `gotenberg` (`:268`) = **5**. No `cms`/`jobsimulation`/`roadrunner`/`graphql`/`directus` service.
7. **`repos.yml` @ `0dab54d` = 6 entries** — `app`, `sentinel`, `storage`, `messenger`,
   `next-web-app`, `studio-desk`, in that order; the messenger entry is exactly `:21-23`
   (`messenger.md:17`) and `jobsimulation.md:12`'s enumeration matches.
8. **`jobsimulation.md:32` — "23 session/run tables".** `terraform/migrations/20260722081626_jobsim_data_model.sql`
   contains **23** `CREATE TABLE` statements, and the next migration `20260722104506.sql` creates
   `job_simulation_sessions` at `:2` and `DROP TABLE "sessions"` at `:79` — both exact.
9. **`jobsimulation.md:114-119` / the mirror drop.** `20260729133514.sql`: FK remaps `:15-23`,
   orphan NULLs `:36-44`, `-- 5. Drop the mirrors.` `:58`, `DROP TABLE`s `:62-63`. **Zero** `^\s*INSERT`
   statements (a naive `grep -ci insert` returns 1 — the `on_insert_…` function name at `:64` — which
   is why I anchored the regex). `internal/organization/intelligence.go:1700` is
   `query := m.ent.JobSimulationSession.Query().`.
10. **`skiller.md:40-49` + `external_services.md:659-672` — the taxonomy figures.** Read
    `.agentspace/snapshots/taxonomy/5afc0bccf1df7ef538b643321fc6362f/manifest.json` directly:
    `public_only: true`, `predicate: org-null`, `source: primary-read`, `captured_at 2026-06-29`;
    `public.skills` **42790**, `public.job_roles` **22470**, `public.skill_embeddings` **42790**,
    `public.job_role_embeddings` **18919**, each with the org-null filter recorded. Exact.
11. **`external_services.md:69` — the per-app Clerk SDKs.** `apps/web`, `apps/hiring`,
    `apps/integration` and `ant-academy/code` all pin `@clerk/nextjs`; `apps/mobile` pins
    `@clerk/clerk-expo`; `studio-desk` pins `@clerk/clerk-js` **and** `@clerk/express`.
12. **`skiller.md:54-55` — "8 `ContentLanguage`s".** `assignments.graphqls:44-53` enumerates exactly
    **8**: english, italian, spanish, french, german, dutch, japanese, portuguese.
13. **`external_services.md:545-551` — `app/studio/**` is in no `app` commit.**
    `git ls-tree b948604f studio` returns empty while the directory exists on disk (positive
    control: the same `ls-tree` lists 34 top-level entries). The retraction is correct.
14. **`academy-backend.md:56-58` — storage-in-app.** At `9d00a313`, `main.go:471-472` constructs
    `internalstorage.NewManager` / `NewPublicManager`; `internal/storage/service.go:22`
    `EnvBucket = "STORAGE_S3_BUCKET"`. `STORAGE_RPC_ADDR` has exactly **3** occurrences in `*.go`
    at that ref (`internal/jobsimwiring/wiring.go:101`, `internal/storagens/callsites_test.go:189`,
    `main.go:451`) and **all three are comments** — zero reads. `cmd/academyImport/main.go:239-243`
    and `:244-246` are verbatim what the doc quotes.
15. **`messenger.md:16` — the messenger fold in `app` @ `9d00a313`.** `main.go:15` `msgflow`,
    `:61` `msgadapters`, `:62` `msgsender`; `:1330-1340` the take-over-the-group comment;
    `:1362-1365` the literal-group-name rationale; `:1386-1387` `pubsub.NewSubscriberServer(msgflow.ConsumerGroup, …)`;
    `:1423` `msgadapters.Wire(…)` with `msgsender.NewFromEnv` at `:1425`; `:1095` the `CMS_STREAM`
    publisher; `:1478-1484` the subscriber stream binding. Every one exact.

### RE-DERIVED (weaker clearance — arithmetic only, or a single anchor opened and read in context)

- `app/main.go:980-982` `log.Fatalf` on empty `DIRECTUS_BASE_ADDR` @ `b948604` — exact
  (`external_services.md:203,294`; `jobsimulation.md:132`).
- Every `app/internal/jobsimulation/ai/ai.go` anchor in `external_services.md:559-575` and
  `security_compliance.md:207-212`: `:30-33` vendor consts, `:130-141` `isThrottlingError`,
  `:150-155` and `:296-302`/`:326` the retry vendor override, `:259-289` `getClient`, `:264-276`
  (and `:263-277`) the `flag_use_azure_us` swap, `:280-283` both Anthropic arms → `anthropicClient`,
  `:80` `openai.NewOpenAI`, `:85-88` Bedrock `eu-west-1`, `:166` the ChatCompletion throttle probe.
- The US-default chain: `cms/directus/collections/jobsimulation.go:905` `AIVendor *AIVendor`,
  `:1302` `aiVendor := simulation.Openai`; `simulator/ai/ai.go:58-59` and the `default:` arm at
  `:114-115`.
- Provider rows: `coursebuilder/bedrock.go:23,29` (Opus 4.8 / Sonnet 4.6), `:100` `"anthropic-api"`,
  `:106-113`/`:109-112` `newUnderlyingClient`; `jobsimulation/agent/report_agent.go:31`;
  `askengine/bedrock.go:25`; `cms/studio/markdownManager.go:19` + `studioManager.go:583` (Mistral,
  OCR only); `main.go:770` `coursebuilder.ModelBackendName()`.
- Studio-Room: `services/ai.py:627-664` `AnthropicProvider`, `:704-724` `get_client` with the
  3-provider dict at `:706-708`, `:383` the bare `OpenAI(api_key=…)`, `config_template.ini:30-31`
  the `api.openai.com` endpoint, `gen.py:41-53`/`:45-47`.
- EU AI Act blockquote: `skills.go:53`/`:75`; `basevalidator/criterion.go:127` (`case check.EngineLlm`),
  `:428` `NewLLMBulkChecker`, `:142` `case check.EngineTextDiff` → `:168` `validateCodeDiff`,
  `:450-475` the pure `cdiff.Diff == ""` comparison; `templates/checkValidationBulk.tmpl` exists and
  contains both the *"meets or does not meet"* instruction and the `check_id`/`feedback`/`success`
  JSON shape; `checkerEngines` is registered once (`v3/validator/validator.go:60-61`), stored
  (`criterion.go:34`,`:72`,`:90`) and never read — the doc's "cite the DISPATCH, not that map" holds.
- Org switching: `useOrgSelection.tsx:94`, `useResolveActiveOrg.tsx:107`,
  `useActivateMembershipOrg.tsx:81` — all `clerk.setActive({ organization })`, in **both**
  `apps/web` and `apps/hiring`.
- Router/compose history: `terraform/locals.tf:8` `port = 8080`, `main.tf:20` `= 1`, `:48-49` the
  8080→8080 port mapping, `config.prod.yaml:5` `listen_addr: 0.0.0.0:8080`; `Dockerfile.dev:7`
  `wgc@0.104.0`, `:18` the single schema COPY, `:19-20` the fold comment, `:23` the single `awk`,
  `:29` `router:0.275.0`; `subgraphs.conf` = `BACKEND=v1.360.0`; `schemas/` holds only
  `backend.graphqls`; `graphql-wundergraph/CLAUDE.md:39` really does assert the opposite build path.
  Compose eras: `63d285c` (2024-06-20, service named `wundergraph`, git-URL context, **no**
  `dockerfile:` key), `2c85211^` no key → `2c85211` `dockerfile: Dockerfile.dev`,
  `1e8e754:6-8` `dockerfile: graphql-wundergraph/Dockerfile.dev`, `b56d731` still has `graphql:` at
  `:22` behind `profiles: [wundergraph-deprecated]`, `360efd4` has none (control: `backend:` at `:28`).
  `"5050:8080"` existed only as the local host mapping and is **0** occurrences at `0dab54d`.
- `jobsimulation` legacy repo: `terraform/main.tf:40` `service_desired_count = 0`;
  `cmd/root.go:77`/`:78` the 8080/8081 defaults; both Dockerfiles `EXPOSE 8080`; compose @ `2adcf71`
  `:83` service, `:93-94` ports, `:113` `PORT=8400`, `:119` `RPC_PORT=8401`, `:140`
  `profiles: [graphql, jobsimulation, all]`; `Makefile:10` `PROFILE ?= graphql` @ `2adcf71` vs
  `?= core` @ `0dab54d`; `repos.yml:17-19` @ `2adcf71`; `internal/runner/runner.go` with the
  *"formerly the standalone 'roadrunner' service"* header; `internal/graph/schemas/` holds
  schema/mutations/queries/subscriptions/activites; `internal/worker/worker.go:27` `Concurrency: 10`
  and `:104` `Concurrency: 25`; `cmd/root.go:284-285` the colony `AddSubscriber` on the self stream
  and `cms`; `interview_report_csv{,_test}.go`; `hibiken/asynq v0.26.0`; commits `09631fb2` and
  platform `05b4035` both exist with the quoted subjects; `queries.resolvers.go:70` is
  `JobSimulationResult`.
- `messenger` @ `fa47850d`: `rpcsrv.go:25-30` (both stubs `CodeUnimplemented`), `cmd/root.go:63`
  `PORT`→8080, `:64` `RPC_PORT`→8081, `:107` `REDIS_STREAMS_INDEX`→"2", `:147`
  `READONLY_DB_CONNECTION`; `go 1.25.0`, `brevo-go v1.1.3`, `liquid v1.8.1`; ports 8200/8201 in
  compose `:165-167`; `profiles: [messenger]` at `:195`; `depends_on` redis/postgresql/backend at
  `:186-192`; all four RPC addresses `http://backend:8083` at `:173`,`:174`,`:176`,`:183` under the
  comment at `:171-172`; `flow/jobsimulations.go:140-151` the 2 h / 12 h staleness guards verbatim;
  `flow/flow.go:72-87` the `AddSubscriber("backend", …)` `OrgSkillPath*` handlers;
  `flow/assignments.go:828` `h.cms.GetSkillPath` inside `getSkillPath`; the whole "Key directories"
  tree matches the repo. `app/main.go:1205-1211` @ `b948604` does still carry the stale
  *"DORMANT … until the M809 re-point"* comment the doc flags.
- Academy: all 11 `academy_*` tables exist with the **plural** names the doc insists on;
  `UNIQUE(user_id, chapter_slug)` (`20260603182114:35`), `UNIQUE(chapter_slug, locale)`
  (`20260604061339:15`), `"paid" boolean NOT NULL DEFAULT true` (`20260604035956:30`), locale
  overlays `de/es/fr/it/nl/pt` over an `en` base (`academy_chapter_body.go:18-19,57-59`);
  `academy.graphqls` + `resolver_academy.go`; `GET /content/catalog.json` (`web/backend/content.go:23`)
  and the `POST /content/admin` group behind `sharedTokenMiddleware(os.Getenv("ACADEMY_CONTENT_API_TOKEN"))`
  (`content_admin.go:35`); `academy_embedding_refresh` @ 03:00
  (`internal/worker/tasks/academy_embedding_refresh.go:10`); no academy Connect-RPC (`internal/rpc/`
  holds only `client.go`, `intercepters.go`, `server.go`, `skillerrpc/`); `cmd/academy-seed`,
  `cmd/academyImport`, `cmd/coursebuilder-e2e`, `cmd/coursebuilder-liverun` all exist; PR #903 =
  `0e37771f`, dated **2026-06-05**; `git describe 5ba17044` = **v1.363.2**.
- Skiller: `internal/rpc/skillerrpc/skiller.go` implements `GetSkill` `:73`, `GetSkills` `:93`,
  `SearchSkill` `:116`, `MatchSkill` `:147`, `GetJobRole` `:187` (the rest are 4-line stubs pinned by
  `TestServer_BackendOnlyMethodsUnimplemented`); `skiller_taxonomy.graphqls` exists and its **only**
  mention of `categoryTree`/`fullCategoryTree` is `:7`'s *"stay unported — no consumers"*;
  `skill_embeddings`/`job_role_embeddings` are `extensions.vector(1536)` with `USING IVFFLAT`
  (`20260615130000_skiller_taxonomy.sql:53,61,243,251`); `skill_translation.go` /
  `job_role_translation.go` present.
- `corpus/architecture/README.md:21` really is the shared-libraries bullet ending *"cost tracking
  lives in `app`, not the `ai` library"* — so `security_compliance.md:185`'s `README.md:21` resolves.
  `coursebuilder.md:48` really does end *"so the shipped path is the direct API."*
  `external_services.md:555`, `:543`, `:577-582` and `:522` all resolve to what cites them.
- `corpus/services/TEMPLATE.md` carries **no** factual claims — it is pure boilerplate. Its
  Downstream/Upstream convention (*Downstream = services this calls*, *Upstream = services that call
  this*) is the convention `academy-backend.md:99-105` actually follows, so there is no
  cross-file conflict there.

---

## What I could not settle

- **`messenger.md:9-17` — the ground-truth clone is one commit behind the corpus's own pin, and the
  claim flips across that commit.** The banner pins itself: *"Re-derived at platform `0dab54d` /
  `app` `9d00a313` v1.367.0 / **`messenger` `a0ec933`**"*. At `a0ec933` (v0.42.1, on `origin/main`)
  `terraform/main.tf:29` is **exactly** `service_desired_count = 0` and `:27-28` is verbatim
  *"The image and task definition stay declared: this is the rollback path. Restoring service is a
  one-line revert plus an apply, not a re-provision."* — the row is **true at its pin**. But the
  clone I was given is `fa47850d` (v0.42.0), where the same file has `service_desired_count = 1`
  at `:19` and the `:29` anchor lands on `container_definitions = <<EOF`.
  I did **not** book this, because a pin is a date (§5 rule 33 / the briefing) and this one resolves.
  Two things a repairer should weigh, though, and I cannot decide either from here:
  (1) rule 33 says a pin's scope is *the claim's own block — a markdown CELL in a table*, and this
  pin sits in the introducing sentence, not the cell; (2) the identical claim appears in root
  `CLAUDE.md` (*"Prod compute for both is **stopped** (`storage/terraform/main.tf:38`,
  `messenger/terraform/main.tf:29`, each `service_desired_count = 0`)"*), which is **outside my
  file set** and, as far as I read it, carries no `messenger` ref of its own — the §5 rule 19
  corollary (a claim leaks to the edge of the previous repair's scope, and the instruction file is
  the highest-propagation site). Settling it needs a decision about which messenger ref the corpus
  is graded at, plus a re-fetch of `stack-demo/messenger`.
- **Production infrastructure claims.** `security_compliance.md:20-31` (VPC CIDR `10.0.0.0/16`,
  which subnet holds the Cosmo Router, Tailscale/MFA posture), `:151-162` (6-hourly backups to
  S3/Azure/Hetzner, DR site), `:166-173`, `:258-272` (DPA v1.4, **18** sub-processors, 90-day
  auto-deletion), and `jobsimulation.md:56-59` /
  `skiller.md:19` (`module.jobsimulation_euwest1` in `infrastructure/terraform/production/services.tf`,
  its ownership of the LiveKit/Chime buckets, `skiller_rpc_addr = http://backend:8081`) all live in
  the `infrastructure` repo, which is not among the 13 ground-truth clones. **What would settle
  them:** a read-only clone of `anthropos-work/infrastructure` at a stated sha, plus the signed DPA.
- **`external_services.md:3` — "the repo is ARCHIVED on GitHub (2026-07-30)"** (and the same claim
  for `jobsimulation` at `jobsimulation.md:20,77`). Archival is a GitHub repo-setting, invisible in a
  clone. **What would settle it:** `gh repo view anthropos-work/graphql-wundergraph --json isArchived`.
- **`external_services.md:264-274,284-288` — the production Directus schema/collections and its S3 +
  CDN file storage.** No prod DB or Directus instance is reachable from this reading.
  **What would settle it:** a read-only `\dt directus.*` via `/db-query`, or a `directus_collections`
  listing from `content.anthropos.work`.
- **`external_services.md:675-677` — Studio-Desk copilot default models** (`gpt-5.2`,
  `claude-sonnet-4-5`/`claude-opus-4-5`) and the circuit-breaker failover. I confirmed
  `AI_PROVIDER_CHAIN=azure-openai,openai` in `studio-desk/.env.example:57`, but did not enumerate the
  tier→model table in the provider source. **What would settle it:** reading
  `studio-desk`'s AI-service provider registry end to end.
