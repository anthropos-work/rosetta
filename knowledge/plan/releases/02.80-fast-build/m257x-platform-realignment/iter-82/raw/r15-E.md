# Seat E — reading #15

Ground-truth shas re-verified at open: `app` `b948604f`, `platform` `0dab54df`, `next-web-app` `bb3313bc`,
`sentinel` `88bc5592`, `storage` `4ce8ece5`, `messenger` `fa47850d`, `cms` `ca50c817`,
`graphql-wundergraph` `60c229f3`, `roadrunner` `87d8d443`, `jobsimulation` `462343b0`,
`studio-desk` `14a5442a`, `ant-academy` `9c3843cd`, `rosetta-extensions` `4e6b64d3` (main).
All match the briefing table. `app` refs `9d00a313` (v1.367.0) and `5ba17044` (v1.363.2) both exist in the
local clone and were used where a passage names them.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/architecture/service_taxonomy.md` | 459 | yes |
| `corpus/services/hiring.md` | 425 | yes |
| `corpus/architecture/platform-migration-status.md` | 263 | yes |
| `corpus/services/storage.md` | 201 | yes |
| `corpus/services/askengine.md` | 124 | yes |
| `corpus/architecture/dependency_map.md` | 103 | yes |

Protocol read: `corpus/ops/platform-alignment.md` §5 (lines 240–520 in full; §5 runs 240–1193).

Search-pipeline hygiene: three of my greps failed loudly and were re-run — `--include=*.go` unquoted
(zsh glob), `749dc86^` (zsh `bad substitution`). Each failure was read from stderr and repeated with
quoting plus a positive control (`CMS_RPC_ADDR` → 1 hit in compose; `colony` → 1 hit in storage's
`go.mod`; `@tabs` dirs → 3 hits in `apps/`). No empty result in this report comes from a failed command.

---

## Blockers

### B1 — `dependency_map.md:19` cites `storage.md:14,21`; both lines are markdown punctuation, not claims

- **Anchor:** `corpus/architecture/dependency_map.md:19` — *"Storage **reads** neither: no
  `DB_CONNECTION`/`REDIS_ADDR` in its compose env, no redis in its `go.mod`, which is what its own doc
  says at [`storage.md:14,21`](../services/storage.md)."*
- **Why it is false:** `corpus/services/storage.md:14` is `>` — an empty blockquote continuation line.
  `corpus/services/storage.md:21` is `> |---|---|` — a markdown table separator row. Neither line says
  anything about a database, Redis, or storage's compose env. The substantive facts are true and the
  target document *does* state them — at `storage.md:37` (*"Storage is stateless and owns no database"*)
  and `storage.md:44` (*"**Database**: none — all state lives in S3"*) — so this is a citation that
  drifted off its subject, almost certainly when the ⚠️⚠️ v9.0 block was inserted at `storage.md:15-33`.
  Corroborating source: `stack-demo/platform/docker-compose.yml:116-123` (storage's whole `environment:`
  block — `AWS_DEFAULT_REGION`, `AWS_REGION`, `ENVIRONMENT`, `PORT`, `RPC_PORT`, `SERVICE_NAME`,
  `STORAGE_S3_PUBLIC_BUCKET`; no `DB_CONNECTION`, no `REDIS_ADDR`) and `stack-demo/storage/go.mod`
  (`grep -in redis` → exit 1, with `colony` → 1 as the positive control).
  This is precisely the failure §4 of `platform-migration-status.md` says assertion D cannot see: the row
  cites *something*, so a citation-presence check passes over a dead anchor.
- **Confidence:** high

### B2 — `storage.md:8-9` cites `anticheat.go:34`; at every candidate ref line 34 is an unrelated third-party import

- **Anchor:** `corpus/services/storage.md:8-9` — *"The jobsimulation and cms domains run **in-process
  inside `backend`** (`app/internal/jobsimulation/recording/recording.go:12`, `anticheat.go:34`,
  `app/main.go:1048` `internalstorage.NewClient(storageManager, storagens.CMS)` @ `app` `9d00a313`
  v1.367.0)"*
- **Why it is false:** the block names its ref (`app` `9d00a313`). At `9d00a313`,
  `app/internal/jobsimulation/anticheat/anticheat.go:34` is `"github.com/sourcegraph/conc/pool"`. The
  storage reference the citation is reaching for is at `:30`
  (`storagev1 "github.com/anthropos-work/storage/sdk/storage/v1"`), with the manager's storage field at
  `:44` and the actual call at `:751` (`a.storage.GetObject`). The anchor is equally wrong at the
  checkout `b948604` — identical file, `:34` is the same `conc/pool` import there. So it resolves at
  neither ref this document could plausibly mean. The two sibling anchors in the same parenthesis are
  exact (`recording.go:12` = the `storagev1` import; `main.go:1048` = the verbatim
  `cmsStorage := internalstorage.NewClient(storageManager, storagens.CMS)`), which is what makes the
  third one read as checked.
- **Confidence:** high

### B3 — `platform-migration-status.md:71` puts the `b948604` skillpath wiring call on a blank line

- **Anchor:** `corpus/architecture/platform-migration-status.md:71` — *"…skillpath `:698`
  (`skillpath.NewSessionManager`) … (`app` @ `9d00a313` v1.367.0 — **re-resolved M257x iter-68**; the
  first four stood at `:581`/`:612`/`:643`/`:1043` at `b948604` v1.366.0 and at `:573`/`:604`/`:634`/`:1034`
  at `5ba17044` v1.363.2, **which is what one working day costs a line-number citation**)"*
- **Why it is false:** at `b948604`, `git show b948604:main.go | grep -n skillpath.NewSessionManager`
  returns **642**, not 643; `sed -n '643p'` on that ref returns a blank (tab-only) line. The three
  companions in that same triple are exact at `b948604` (`:581` skiller, `:612` jobsimwiring.Wire,
  `:1043` appcms.Wire), and all four are exact at `5ba17044` (`:573`/`:604`/`:634`/`:1034`, verified
  line by line) and at `9d00a313` (`:637`/`:668`/`:698`/`:1099`, verified line by line). So one of the
  twelve anchors in the cell is off by one, and it is off inside the clause whose whole rhetorical
  purpose is that line-number citations decay.
- **Why I hesitated:** an off-by-one that still lands inside the same statement's blank separator is
  cosmetically small. I booked it because this document's §4 explicitly stakes its non-compose citations
  on *"resolution + range"*, and a blank line satisfies "resolves" while carrying nothing — the exact
  failure mode assertion F was created to catch on the compose side.
- **Confidence:** medium

### B4 — "the bucket names are constants in `app/internal/storage/service.go:22`, `:24`" — those constants are env-var NAMES, and the claim stands in two files

- **Anchors (same claim, two files — cross-file, per §5 rule 19):**
  - `corpus/services/storage.md:26` — *"consumed at `:494` and `:1048`; bucket names are constants in
    `app/internal/storage/service.go:22`, `:24`."*
  - `corpus/architecture/platform-migration-status.md:76` — *"the bucket names are constants in
    `app/internal/storage/service.go:22`, `:24`."*
- **Why it is false:** at `9d00a313` (the ref both blocks name), `app/internal/storage/service.go:19-25`
  is a `const` block introduced by the comment *"// Environment variables."* at `:17`:
  `:22` is `EnvBucket = "STORAGE_S3_BUCKET"` and `:24` is `EnvPublicBucket = "STORAGE_S3_PUBLIC_BUCKET"`.
  Those constants hold **env-var names**, not bucket names. The bucket names are read at runtime —
  `app/main.go:463` `storageBucket := os.Getenv(internalstorage.EnvBucket)` and `:464`
  `storagePublicBucket := os.Getenv(internalstorage.EnvPublicBucket)` — and are supplied externally:
  `stack-demo/platform/docker-compose.yml:82` `STORAGE_S3_BUCKET=production-storage20240826131618541000000005`,
  `:83` `STORAGE_S3_PUBLIC_BUCKET=production-storage-public20240919130721114900000001`. `main.go:461-462`
  says so in words: *"The terraform that supplies these names ships from a different repo on a different
  clock than this code."* Reading the corpus sentence at face value — bucket names hardcoded in Go — is
  the opposite of the deploy hazard `main.go:457-470` exists to guard (`log.Fatalf` when the env is empty
  outside a developer machine).
- **Why I hesitated:** a charitable reading is *"the [env vars for the] bucket names are constants"*, and
  the anchors do land on the right two lines. I booked it because the two readings have opposite
  operational consequences and the claim now stands identically in two documents.
- **Confidence:** medium

### B5 — `hiring.md:52` asserts the scoreboard lives in `apps/web`; `hiring.md:344` says that premise was falsified

- **Anchor:** `corpus/services/hiring.md:52-53` — *"That scoreboard lives in the **dockerized `apps/web`**
  (`/enterprise/activity-dashboard`), renders **from seedable data alone**, and **survives the `is_hiring`
  flip** — so the demo can show it **without a platform edit** (M222 D1, the release go/no-go)."*
- **Why it is false / contradictory:** the same file, at `:344`, opens the render-path section with
  *"**M224 proved the render — and it does NOT land in `apps/web`.**"* and at `:353-355` concludes
  *"⇒ 'reads as hiring in the browser' and 'scoreboard reachable in `apps/web`' are mutually exclusive on
  the real platform. (This falsified M222's `apps/web` premise — #M224-D-TOK02.)"*, and again at
  `:414-415` *"⚠️ M222 inferred the scoreboard was reachable in `apps/web`; M224 rendering proved it is
  not for a *genuine* hiring org."* Ground truth backs the later passages: `next-web-app` at `bb3313bc`
  has `apps/web/src/context/UserStatusContext.tsx:144-148` computing `userHasAllHiringOrgs` from
  `publicMetadata.isHiring` and `:168-172` firing
  `buildSwitchHandoffUrl({ targetProduct: 'hiring', … })`, with the symmetric guard at
  `apps/hiring/src/context/UserStatusContext.tsx:125` and `:144-145`. A genuine hiring org's recruiter is
  ejected out of `apps/web`. `:52` carries no in-place retraction marker while every other superseded
  claim in this document does (`:25`, `:41`, `:78`, `:126`, `:260`, `:311-312`, `:322-323`), and it sits
  in **Role & Responsibility** — the first substantive paragraph a reader hits.
- **Confidence:** medium-high (the contradiction is certain; which sentence is "the" defect is an
  editorial call, so I cite both anchors per the self-contradiction rule)

### B6 — `platform-migration-status.md:76` mis-composes its own 15-hit breakdown: three `t.Setenv` calls, not four

- **Anchor:** `corpus/architecture/platform-migration-status.md:76` — *"`git -C stack-demo/app grep -n
  STORAGE_RPC_ADDR b948604 -- '*.go'` returns **15** hits, of which **7 are env lookups** … (the other 8
  are two doc comments, two error strings and **four `t.Setenv` calls**)."*
- **Why it is false:** I enumerated the set rather than the sum. The 15 hits at `b948604` are:
  `cmd/academy-asset-upload/main.go:28` (doc comment), `:129` (`os.Getenv`), `:133` (`fmt.Errorf`);
  `cmd/academy-asset-upload/main_test.go:41` (`t.Setenv`), **`:47` (`t.Fatal("expected error when
  STORAGE_RPC_ADDR is unset")` — a failure-message string, not a `t.Setenv`)**, `:52` (`t.Setenv`),
  `:64` (`t.Setenv`); `cmd/academyImport/main.go:35` (doc comment), `:231` (`os.Getenv`), `:235`
  (`fmt.Errorf`); `cmd/import/main.go:50` (`os.Getenv`); `internal/jobsimwiring/wiring.go:115`
  (`getenv`); `main.go:446`, `:524`, `:992` (`os.Getenv`). That is **3** `t.Setenv` calls and a fourth
  test-side *string*. Everything else in the cell reproduces exactly: 15 total, 7 env lookups
  (`main.go` ×3, `wiring.go` ×1, one per `cmd/` tool), 2 doc comments, 2 error strings, and "six of the
  seven spelled `os.Getenv`, the seventh a lowercase `getenv` helper" — all verified.
- **Why I hesitated:** the load-bearing numbers (15, 7, and the 3-comments-only result at `9d00a313`) are
  all correct, and the residual 8 is right in total. Only the composition of the residual is wrong, and no
  reader's decision turns on it. I booked it as a BLOCKER because "wrong counts" is explicitly a BLOCKER
  in the rubric and because a *derivation shown in full* invites exactly the rule-24 trust it does not
  earn here.
- **Confidence:** low

---

## Minors

- `corpus/services/hiring.md:204` — *"the `PersonaSeeder` also writes (`persona_write.go:69-71,143-167`)"*.
  The three `validation_*` write steps are at `rosetta-extensions/stack-seeding/seeders/persona_write.go:92-94`;
  `:69-71` is comment prose about a **removed** `jobsimulation.sessions` step, and `:143-167` is a closing
  brace plus `sessionCols()` (`:152-159`) and `attemptResultCols()` (`:161-167`). The claim itself is TRUE
  and the file is right, so it is an imprecise range rather than a false statement.
- `corpus/services/hiring.md:181` — `simulationScoreColumn.tsx:54` lands on `accessorKey,` inside the
  column definition. The paired `:95-97` is exact (`const { score, … } = info.row.original;` →
  `formatNumberToString(score, 0)`).
- `corpus/architecture/platform-migration-status.md:62-65` — the derivation *"every service that has ever
  appeared in `docker-compose.yml` … → **26** names"*. Re-running the stated command reproduces 26
  exactly, but the 26 includes `app-network`, which is a **network**, not a service (it sits at the same
  indent under `networks:`). So the *count* is reproducible and the *predicate* ("services") is off by one
  member. The five pre-history names the sentence calls out (`nats`, `web-app`, `chromedp`, `simulator`,
  `realtime`) are all genuinely present.
- `corpus/services/hiring.md:71-78` — `organization/manager.go:448` and `:485` resolve **only** at
  `5ba17044`, the ref the document's `:17` banner names (`:448` = `switch org.IsHiring {` inside
  `forceUserToOrganizationInClerk`; `:485` = `if !org.IsHiring {` guarding the
  `"organization is not hiring"` error). At the checkout `b948604` those constructs have moved to `:448→:452`
  and `:485→:536`, so both anchors land on a blank line and a closing brace. **Not booked** — the pinned
  ref settles it — but the cell is one platform commit from being a B3-shaped defect.
- `corpus/architecture/service_taxonomy.md:67` — *"8081-8083 (container: HTTP 8082, RPC 8083, meta 8084)"*.
  Accurate but easy to misread: compose maps `8081:8081`/`8082:8082`/`8083:8083` while `META_PORT=8084`
  (`docker-compose.yml:64` region) is **not** published, so the meta port named in the parenthetical is
  unreachable from the host.

---

## Positively cleared (audited zeros)

**ENUMERATED** (predicate re-derived from source, cardinalities compared):

- `service_taxonomy.md:55-57` + `:452` — *"`docker-compose.yml` declares eight services (ten in the
  effective topology once `include: common.yml` adds `postgresql`/`redis`), … `core` starts five."*
  Enumerated: 8 top-level service keys in `docker-compose.yml` (sentinel, backend, storage,
  customerio-sync, messenger, studio-desk, next-web-app, gotenberg) + 2 in `common.yml` = 10; `core`
  selects `backend` (`:100`) + `gotenberg` (`:268`) + the three keys that declare no `profiles:`
  (`sentinel` `:5`, `postgresql` `common.yml:2`, `redis` `common.yml:24`) = 5.
- `service_taxonomy.md:354` — the subgraph ladder **5 → 4 (`749dc86`) → 3 (`7c17e63`) → 1 (`915da06`)**.
  Counted `^  - name:` in `supergraph-config-prod.yaml` at each ref: `749dc86~1`=5, `749dc86`=4,
  `7c17e63`=3, `915da06`=1, current file=1. `git show --name-status 915da06` confirms **`D
  schemas/cms.graphqls`** *and* **`D schemas/jobsimulation.graphqls`** in that one commit — so "3 → 1" is
  right and the commit's own "2→1" subject line is the wrong figure, exactly as the corpus warns.
- `platform-migration-status.md:60-62` — *"`git log -p --follow -- repos.yml` → **14** names"*. Re-derived:
  app, chronos, cms, graphql-wundergraph, intelligence, jobsimulation, messenger, next-web-app,
  roadrunner, sentinel, skiller, skillpath, storage, studio-desk. Exactly the 14 listed, in the same set.
- `platform-migration-status.md:74` — *"the compose file at `d11a403^` still declares a `roadrunner:`
  service block (it was one of eleven there; eight remain)"*. Counted at `d11a403~1`: 11 services +
  `app-network`; today 8 services + `app-network`. `roadrunner` is in the former list, absent from the
  latter.
- `platform-migration-status.md:112-113` — *"the 6 are enumerated from the file … a 31-line `repos.yml`
  @ `0dab54d`"*. `repos.yml` is 31 lines and holds exactly app, sentinel, storage, messenger,
  next-web-app, studio-desk.
- `hiring.md:225-227` — *"one of **four** required-and-undefaulted columns (`owner_id` `:6`, `sim_id`
  `:7`, `sim_type` `:10`, `token` `:13`; every other `NOT NULL` column in the DDL carries a `DEFAULT`)"*.
  Walked all 24 columns of `20260722104506.sql:2-27`: exactly those four are `NOT NULL` with no
  `DEFAULT`; `id`/`created_at`/`updated_at`/`status`/`completion_status`/`interactions_progress`/`score`/
  `validation_version` all default; the rest are nullable. `:29` is the `UNIQUE INDEX` on `token`.
- `hiring.md:24-25` / `:165` — *"`SET \"score\"` has **0** hits across the entire migration set"*. 0 hits,
  with `SET "` → 8 as the positive control.
- `hiring.md:40` / `:320-321` — *"the only `CREATE SCHEMA` in the entire migration set is `auth`"*. One
  hit set-wide: `20230817154747_supabase_baseline.sql:2`.
- `hiring.md:69-70` — the `grep -in hiring` claim + positive controls. `resolver_queries.go` @ `b948604`:
  `hiring` appears only as sim-type filters (`:811`, `:843`, `:991-992`); `intelligence.go`: **0** hits.
  Controls land one per file as written: `OrgFeatureInsights` ×8 in `resolver_queries.go`,
  `JobSimulationSession` ×44 (lines) in `intelligence.go`. Stable across `b948604`/`5ba17044`/`9d00a313`.
- `hiring.md:402-403` — *"zero `(.)`/`(..)`-prefixed dirs exist anywhere in `apps/`"*. `find ./apps -type d`
  for those prefixes → 0, with `@tabs` parallel-route dirs → 3 as the positive control.
- `askengine.md:47-48` — *"**60** `TableDef`s, **57** with `Schema: \"public\"` and **3** with
  `Schema: \"directus\"`"*. Counted `{Name: "` entries = 60 and `Schema:` lines = 60 (so no `TableDef`
  omits the field); 57 public + 3 directus = 60.
- `dependency_map.md:50` — *"taxonomy … direct — app, messenger; indirect — storage, sentinel"*. Read all
  four `go.mod`s: `app:20` and `messenger:9` direct; `storage:25` and `sentinel:21` carry
  `// indirect`. `dependency_map.md:48` (`ai` → app only) also holds — `anthropos-work/ai` appears in
  `app/go.mod:14` and in none of the other three.
- `service_taxonomy.md:160-161` — *"0 react/vue/angular entries in `package.json`, 0 `.tsx`/`.jsx`"*.
  `grep -c '"react"' package.json` = 0; `.tsx` files outside `node_modules` = 0.
- `service_taxonomy.md:238` — *"`code/package.json` has no `serwist`/`workbox` dependency"*. 0 hits.
- `storage.md:195` — *"no `*_test.go` files in the repo"*. 0; `Dockerfile:18` is `RUN go test -v ./...`.

**RE-DERIVED** (the document's own displayed arithmetic/anchors re-checked, not independently enumerated):

- Every `docker-compose.yml` citation across all six files resolved at `0dab54d`: `:1-2` (include),
  `:5`/`:18` (sentinel + `search_path=sentinel`), `:28`, `:53` (`DIRECTUS_PUBLIC_BASE_ADDR`), `:57`
  (`GOTENBERG_URL`), `:59` (`JUDGE0_BASE_URL`), `:82-83` (storage buckets on backend), `:92-99` + `:93`
  (backend `depends_on` + the storage-removed comment), `:100`, `:126-130`, `:131-133`, `:134`,
  `:136-138`, `:154`, `:156`, `:171-172`, `:173`, `:174`, `:176`, `:183`, `:195`, `:197`, `:223-225`,
  `:226`, `:228`, `:245`, `:253`, `:255-256`, `:268`; `common.yml:2`, `:22`, `:24`.
- `repos.yml:9-10`, `:11-14`, `:15-17`, `:18-20`, `:21-23`, `:26-28`, `:29-31` — all exact.
- Terraform desired-counts: `app:44`=1, `sentinel:19`=1, `graphql-wundergraph:20`=1, `roadrunner:19`=1
  (last touched `e45eb61`, HEAD `87d8d443` — the untouched-since claim holds), `cms:39`=0,
  `jobsimulation:40`=0, and — **at the refs those cells name** — `storage@63bffc8:38`=0 with the
  ordering comment at `:29-33` and `prevent_destroy` at `:35-37`, `messenger@a0ec933:29`=0 with the
  rollback note at `:27-28`. Note the *checkouts* (`storage` `4ce8ece5`, `messenger` `fa47850d`) still
  read `= 1` at `:19`; the rows are true only because they pin newer refs, which they do explicitly.
- `storage.md:26` / `platform-migration-status.md:76` — `STORAGE_RPC_ADDR` at `9d00a313`: exactly 3 Go
  hits, all comments (`main.go:451`, `jobsimwiring/wiring.go:101`, `storagens/callsites_test.go:189`);
  0 occurrences in `docker-compose.yml`, `common.yml`, `.env_example`; `storage/main.go` is 18 lines.
- `platform-migration-status.md:71` — `app/internal/roadrunner/` genuinely does not exist (`internal/`
  listing at `9d00a313`), and the Judge0 runner is at `jobsimwiring/wiring.go:123` @ `9d00a313` /
  `:118` @ `b948604`, both exact.
- `hiring.md` app anchors: `intelligence.go:1700`, `:1738-1751`, `:1820`, `:1844`, `:1846`, `:885-886`;
  `resolver_queries.go:1034`, `:1035`, `:1053`, `:1080`, `:1085`, `:1089`; `resolver_cms_queries.go:95`,
  `:99-103`, `:210`, `:258`, `:295`; `siminvitationlink.go:62`; `ent/schema/job_simulation_session.go:39`,
  `:45`; `ent/enum/jobsimulation.go:29-35`, `:37-43`; `ent/jobsimulationsession.go:181-186`;
  `graph.go:129392-129400` and `:129546-129554` (both bare `graphql.MarshalString(string(v))`, no
  membership check); `schemas/jobsimulations.graphqls:14`, `:128`; `ent/schema/anticheat_result.go:24`;
  `atlas.hcl:8`; migrations `20260722104506.sql:2/:13/:29/:53/:79`, `20260729133514.sql:58-62`,
  `20260722081626_jobsim_data_model.sql:336/355/376`, `20250416091037.sql:5`. All exact.
- `hiring.md` frontend anchors at `bb3313bc`: `useGetClerkOrganization.tsx:16-18`, `:20-21`;
  `useNavbarSections.tsx:459-466`, `:460`; `template.tsx:90`; `FreeTrialContainer.tsx:29`;
  `UserStatusContext.tsx:144-149`, `:168-172`; `apps/hiring/.../UserStatusContext.tsx:125`, `:144-145`;
  `apps/hiring/.../InsightsByMembersContainer.tsx:359` (`<Drawer`). All exact.
- `hiring.md` rext anchors at `4e6b64d3`: `persona_write.go:91`, `:152`(+`token` at `:155`);
  `hiring_config.go:99` (`CopyRowsIdempotent` into `organization_sim_invitation_links`);
  `hiring_scope.go:45-46` (`skipGenericActivityForHiringOrg` → `IsHiringOrg()`);
  `hiring_scope_test.go:75` (`TestGenericActivitySeeders_SkipHiringOrg`);
  `stack-verify/e2e/tests/render-hiring-comparison.spec.ts`.
- `askengine.md`: `DefaultModelID = "eu.anthropic.claude-sonnet-4-6"` + `DefaultRegion = "eu-west-1"`
  (`bedrock.go:25-26`), `Temperature: param.NewOpt(float64(0))`, `stripAnthropicAuthMiddleware`
  (`:252`), `MaxInlineRows = 200` / `MaxCellLength = 400` / `QueryTimeout = 10 * time.Second` driving
  `SET LOCAL statement_timeout` (`executor.go:18/21/24/97`), `askEngineMaxConns = 6` (`main.go:150`,
  applied `:336`), `COPILOT_DB_CONN` (`main.go:313`), `rules.md` = 146 219 bytes (~146 KB),
  `maxAgenticIterations = 15` / `loopTimeout = 10 * time.Minute` (`handler.go:34`, `:41`), the `/ask`
  Echo group (`internal/web/backend/backend.go:171`), `rewriteSchemas` (`sandbox.go:293`, consumed
  `:317`), `registry.go:183-195` (the jobsimulation transition-alias comment), and the four test
  counts **49 / 13 / 10 / 14** (`grep -c '^func Test'`). Every number in this file checked out.
- `service_taxonomy.md:311-313` — the Directus un-retraction. At `a2a3ee6~1`: `:384`
  `image: directus/directus:10.10.1`, `:386` `- 8055:8055`, `:409` `- ADMIN_PASSWORD=password`. And
  `git log --all -S 'admin@example.com' -- docker-compose.yml` returns nothing, so *"only the
  `admin@example.com` email is unfound in history"* is right too.
- `service_taxonomy.md:351` — router port. `terraform/locals.tf:8` `port = 8080`; `terraform/main.tf:48-49`
  `containerPort`/`hostPort` = `${local.port}`; `config.prod.yaml:5` `listen_addr: 0.0.0.0:8080`; and the
  current `supergraph-config-prod.yaml`/`schemas/`/`subgraphs.conf` are `backend`-alone /
  `backend.graphqls` / `BACKEND=v1.360.0`.
- `service_taxonomy.md:234-241` — every `ant-academy` anchor at `9c3843cd`: `graphql/server.js:14`, `:18`
  (it does throw); `backendContent.js:36`, `:102-103`; `serverTenant.js:145`;
  `app/api/academy/beacon/route.js:36`, `:41`; `layout.jsx:132`; `tests/unit/next-scaffold.test.js:106`,
  `:111`.
- `storage.md:45-46`, `:154`, `:182` — ports `8300`/`8301` 1:1 in compose; `storage.go:122` is the
  `S3Bucket == ""` guard whose next line returns `("", nil)`; and at the pinned `2adcf71`, `:210` is
  `STORAGE_S3_PUBLIC_BUCKET=production-storage-public…` while `:324` genuinely sits in the studio-desk
  block. `viant/afs v1.30.0` is in `go.mod:17`.
- `dependency_map.md` — the `AI_USAGE_STREAM=AI` env var (`:47`), `app/internal/converter/gotenberg.go`
  (exists), studio-desk's `AI_PROVIDER_CHAIN` (`.env.example:57`), and the `:9`/`:15`/`:31` internal
  back-references (all point at the passages `:90` says they do).
- `hiring.md:38-39`'s twins — `service_taxonomy.md:52` and `dependency_map.md:78` both do say what the
  citing sentence claims they say.

---

## What I could not settle

- **GitHub-side facts.** *"Repo ARCHIVED on GitHub"* for `jobsimulation`/`skillpath`/`skiller`/
  `graphql-wundergraph`/`intelligence`, *"NOT archived"* for `chronos`/`roadrunner`/`cms`, the
  **93-repo** org count and the **46 named by no corpus document** figure
  (`platform-migration-status.md:73`, `:74`, `:80-84`, `:110-117`, `:123-141`) — all require the GitHub
  org API. Settled by: `gh repo list anthropos-work --limit 200 --json name,isArchived,pushedAt`.
- **`repos.yml:9-10`'s rollback pointer to infrastructure's `services.tf`** — the `infrastructure` repo is
  not among the ground-truth clones, so the `roadrunner` prod contradiction
  (`platform-migration-status.md:74`, §5 row 2) stays exactly where that row says it stays: explained but
  unverified. Settled by cloning `anthropos-work/infrastructure` and reading `services.tf`.
- **`service_taxonomy.md:436` / `:439-441` — "`frontend` / `studio-desk` / `messenger` exit 1" and
  "retired tokens exit 0 and start the floor".** The `depends_on: backend` edges are confirmed in compose
  (`:250-252`, `:223-225`, `:186-194`) and the floor's absence of a `profiles:` key is confirmed, but the
  exit codes are a Docker Compose runtime behaviour I did not execute. Settled by
  `docker compose --profile frontend config --services; echo $?` and the same for each retired token.
- **Snapshot-population claims in `hiring.md:146-151`** — *"87 real `SIMULATION_TYPE_HIRING` sims"* and
  *"0 `job_position` rows"* in the captured snapshot. These live in the `.agentspace` capture cache /
  prod, not in any ground-truth clone. Settled by `stacksnap status` against a populated cache.
- **`platform-migration-status.md:71`'s forward risk, noted not booked.** `app`'s `origin/main` is four
  commits past `9d00a313`, and those four are `refactor(ai): fold the ai library into app as
  internal/ai` + `test(ai): ban the external ai module path`. `dependency_map.md:48` and
  `service_taxonomy.md:120` describe `ai` as a private Go module imported by `app` — **true at the
  checkout** (`app/go.mod:14` `github.com/anthropos-work/ai v1.40.2`) and therefore not a finding today,
  but it is the next `mid-fold` row, and nothing in the corpus names it yet.
