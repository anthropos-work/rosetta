# Seat C — reading #15

**Refs at the moment of measurement (re-derived at open, re-checked at close):**
`app b948604f` (v1.366.0; `origin/main` = `71773747`, 4 commits past `9d00a313` v1.367.0) ·
`platform 0dab54df` · `next-web-app bb3313bc` · `sentinel 88bc5592` · `storage 4ce8ece5` ·
`messenger fa47850d` · `cms ca50c817` · `graphql-wundergraph 60c229f3` · `roadrunner 87d8d443` ·
`jobsimulation 462343b0` · `studio-desk 14a5442a` · `ant-academy 9c3843cd` ·
`.agentspace/rosetta-extensions 4e6b64d3` (authoring copy, `main`).
Corpus branch `m257x/platform-realignment` @ `1937e1f`.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| corpus/architecture/alignment_testing.md | 524 | yes |
| corpus/services/clerkenstein.md | 370 | yes |
| corpus/services/backend.md | 271 | yes |
| corpus/services/sentinel.md | 166 | yes |
| corpus/services/next-web-app.md | 133 | yes |
| corpus/services/README.md | 81 | yes |
| corpus/services/db-backup.md | 31 | yes |

Protocol read: `corpus/ops/platform-alignment.md` §5 (lines 240–1191) in full.

Search-pipeline positive controls run in the same passes as every negative result below:
`git grep -c "META_PORT" 9d00a313` → 1 (before booking the `STORAGE_RPC_ADDR` zero);
`grep -rc "^package" messenger --include='*.go'` → non-zero (before booking messenger's zero
`authorization` hits); `grep -rc "http.Request" clerk-frontend/server.go` → 17 (before booking the
zero `.Cookie(` hits); `git grep -n "E2E_TEST_EMAIL"` returned hits in the same invocation that
returned zero for `DIRECTUS` in `next-web-app`. Every search's stderr was read (no `2>/dev/null`
swallowing on any negative-result search).

---

## Blockers

### B1 — `alignment_testing.md` states the express runner "exits rc=2 and produces NO score — and nothing in the tooling treats that as a failure"; source says rc=3 with a loud banner, and the same file says so four times

- **Anchor:** `corpus/architecture/alignment_testing.md:360-366` (offending text at `:361-362`):
  > "⚠ **This surface is DEPENDENCY-GATED, and today it is frequently UNMEASURED (corrected in M218).**
  > The runner needs `@clerk/express` `node_modules` to build. **Without them it exits rc=2 and produces NO
  > score** — and *nothing in the tooling treats that as a failure*. So on a box that lacks the Node modules,
  > this gate silently contributes **nothing** … Routed forward as `TEST-M219-expressrun-dep-gate`: a missing
  > dependency must **fail loud**."

- **Why it is false:**
  `.agentspace/rosetta-extensions/alignment/cmd/alignctl/run.go:133-136` declares
  ```go
  const (
      ExitRegressed    = 2
      ExitUnmeasurable = 3
  )
  ```
  and `run.go:139-146` (`func unmeasurable`) prints
  `"UNMEASURABLE — the runner could not execute. THIS IS NOT A PASSING SCORE."` to stderr before
  returning it. The comment block immediately above (`run.go:125-132`) is a first-person account of
  the *old* behaviour and states the rule the fix encodes. So the unmeasurable exit is **3**, not 2,
  and the tooling emphatically *does* treat it as a failure.

  It also contradicts **its own document in four places**: `:239-241` (*"3 = UNMEASURABLE … 2 and 3
  were the SAME code until M219"*), `:260` (*"rc=**3**, no score *(was rc=2 … split at M219)*"*),
  `:282-286` (*"✅ RESOLVED M219 … `alignctl run` now splits the codes"*) — and cross-file,
  `corpus/services/clerkenstein.md:45-47` (*"⚠️ **rc=2 is now `ExitRegressed` — a MEASURED regression**
  (`alignment/cmd/alignctl/run.go:134-135`); **do not read a 2 as a missing Node module**"*).

  This is exactly the failure mode `clerkenstein.md:46` warns about: a reader who lands on
  `alignment_testing.md`'s M2c bullet — the longest and most emphatic treatment of the express
  surface in the corpus — is told to read a 2 as "missing Node module", which is now the code for a
  real measured regression. The bullet is stale-from-M218 prose that survived the M219 repair of
  every other site.

- **Confidence:** high

---

### B2 — `backend.md`'s citation `app/CLAUDE.md:72` resolves to a closing code fence; the construct it names is at `:80`

- **Anchor:** `corpus/services/backend.md:43`:
  > "**⚠️ `app`'s OWN docs still list it** (`app/CLAUDE.md:72`, `app/knowledge/architecture.md:28`), which
  > is where this corpus previously got the claim."

- **Why it is false:** at `app b948604f`, `CLAUDE.md:72` is ` ``` ` — the closing fence of the
  migration-commands block that starts at `:68` (`make migrations # runs: atlas migrate diff --env local`).
  There is no service list anywhere near it. `SkillPathSessionService` appears **once** in that file, at
  `CLAUDE.md:80`:
  > `2. **RPC server** (port 8081) — one Connect-RPC mux carrying \`BackendUsersService\`, …, \`SkillPathSessionService\`, …`

  Also checked at `9d00a313` (v1.367.0): still `:80`, so no ref in the ground-truth set makes `:72`
  correct. The sibling anchor `app/knowledge/architecture.md:28` **is** correct and does list it.

  Booked as a BLOCKER rather than a MINOR because the passage's whole job is to send the reader to
  the platform's own stale doc and show it disagrees with `main.go`; an anchor 8 lines off lands on
  an unrelated code fence and reads as a fabricated citation, which is the strongest kind of doubt
  to cast on a correct warning. I hesitated because a pure line-drift is normally a MINOR — but this
  one drifted onto a different *construct*, which the briefing names as a BLOCKER example.

- **Confidence:** high

---

### B3 — `backend.md`: "all four occurrences in the merged `docker-compose.yml`" — the ground-truth platform has **one**

- **Anchor:** `corpus/services/backend.md:77-79`:
  > "**RPC re-pointed** — the `SkillerService` Connect-RPC surface is served **by app itself**
  > (`internal/rpc/skillerrpc/`). Consumers keep the env var, re-pointed:
  > `SKILLER_RPC_ADDR=http://backend:8083` locally (**all four occurrences** in the merged
  > `docker-compose.yml`), `http://backend:8081` in prod terraform."

- **Why it is false:** enumerated at `platform 0dab54d` (the ground-truth ref):
  `grep -n SKILLER_RPC_ADDR docker-compose.yml` → **exactly one** hit, `docker-compose.yml:183`, inside
  `messenger`'s block. The other three went away with the services that carried them: `cms` and
  `jobsimulation` were deleted from compose at `d11a403`, and `backend`'s own self-loop was dropped by
  the same commit (its message: *"SKILLER_RPC_ADDR — self-loop, removed at M709"*). I confirmed the
  claim was true at the ref the section's preamble names: at `platform@0808b92` the count is **4**
  (`:89`, `:151`, `:204`, `:334`).

  **Why I booked it despite that pin.** The pin lives in the section preamble (`:58-61`,
  *"Verified 2026-07-08 against … `platform@0808b92`"*), not in this bullet — and §5 rule 33 scopes a
  pin to *the claim's own block*, a wrapped sentence in prose. More decisively, rule 33's third part:
  a block that **asserts currency** cannot be pinned into silence, and the same preamble asserts
  exactly that — *"This **is** the authoritative, verified statement of the merged shape — the contract
  the v2.1 re-ground grades against."* A reader treating this fact-sheet as the contract and grepping
  compose finds 1 of the promised 4 and has no way to tell which of the two numbers is stale.
  I hesitated because the section is honestly dated and a strict rule-33 read of a *bullet under a
  dated preamble* is at the aggressive edge of the rule; hence medium confidence.

- **Confidence:** medium

---

### B4 — `alignment_testing.md`: the snapshot-fidelity gene class is "five two-sided operators"; `dna/snapshot.go` defines **six**

- **Anchor:** `corpus/architecture/alignment_testing.md:463-470`:
  > "- a **snapshot-fidelity gene class** (`dna/snapshot.go`) — **five** two-sided operators over a
  > `FidelityProbe` … : **`snapshot-row-count`** …, **`snapshot-structural`** …,
  > **`snapshot-referential`** …, **`snapshot-embedding-dim`** …, and **`snapshot-public-only`** …"

- **Why it is false:** I enumerated the constants from the cited file rather than re-checking the
  document's own list. `.agentspace/rosetta-extensions/stack-seeding/dna/snapshot.go` declares **six**:
  `OpSnapshotRowCount` (`:36`), `OpSnapshotStructural` (`:39`), `OpSnapshotReferential` (`:42`),
  `OpSnapshotEmbeddingDim` (`:45`), `OpSnapshotPublicOnly` (`:48`) and
  **`OpSnapshotCrossSurfaceClosure = "snapshot-cross-surface-closure"` (`:56`)** — added at M23,
  documented in the same file at `:49-55`, dispatched at `:172-173`, implemented at `:303-313`, and
  registered in the operator allow-set at `:68`.

  The sixth is not theoretical: `stack-seeding/dna/data-dna.json`'s **content** surface lists it in
  its `operators` array (see the MINOR at `:482` below), so a reader building or auditing a snapshot
  DNA from this doc's list will omit a live operator.

  I hesitated because the sentence sits in a section describing what **M9a** added, where five was
  correct — but it cites `dna/snapshot.go` as the class's location in the present tense, and rule 24
  is explicit that the incompleteness is in the SET, not the arithmetic.

- **Confidence:** medium

---

## Minors

- `corpus/services/backend.md:253` — *"not in the top-level `migrations/` dir (which holds only `atlas.sum`)"*.
  At `app b948604f` there is **no** top-level `migrations/` dir (`git ls-files | grep -c '^migrations/'` → 0);
  it was deleted by `6a46e8445 chore(migrations): remove obsolete atlas.sum file`. The only `atlas.sum`
  is `terraform/migrations/atlas.sum`. The operative half of the sentence (migrations live in
  `terraform/migrations/`, per `atlas.hcl`) is correct and verified.
- `corpus/services/sentinel.md:44` — *"only as a fixture string in sentinel's own tests
  (`internal/authorization/casbin_test.go`, `internal/rpcsrv/rpc_test.go`)"* omits a third test file:
  `internal/authorization/manager_test.go:21` (`manager := "manager"`). The predicate ("only in tests")
  holds; the enumeration is short by one.
- `corpus/services/sentinel.md:15` — *"per its own README/CLAUDE.md"*: `sentinel` ships **no** `README.md`
  (`git ls-files` — CHANGELOG.md, CLAUDE.md, no README). The 8080 default is real: `CLAUDE.md:101` and
  `cmd/root.go:47` (`port := cmp.Or(os.Getenv("PORT"), "8080")`).
- `corpus/services/clerkenstein.md:21` — the section list `(clerkenstein, demo-stack, stack-injection,
  stack-core, stack-seeding, alignment)` names **6 of 11**. Measured: `alignment, clerkenstein,
  demo-stack, dev-stack, playthroughs, stack-core, stack-injection, stack-secrets, stack-seeding,
  stack-snapshot, stack-verify`. Root `CLAUDE.md` gives a *different* partial list (9 — it omits
  `playthroughs` and `stack-secrets`), so the two sites disagree with each other as well as with the tree.
- `corpus/services/clerkenstein.md:105` — the `cmd/` row lists `mintpk` / `fake-fapi` / `fake-bapi`;
  `clerkenstein/cmd/` also contains `jwtkey`.
- `corpus/services/clerkenstein.md:3` — **"Last updated: 2026-07-14"** while the body documents
  v2.8 M256 iter-16/iter-25 (`:184-209`) and M257x iter-23 (`:274-279`), both later.
- `corpus/architecture/alignment_testing.md:3` — **"Last updated: 2026-06-06"** while the body documents
  M218/M219 (late July 2026) throughout and M23's cross-surface operator indirectly.
- `corpus/architecture/alignment_testing.md:482` — *"carrying four operators (no `embedding-dim`)"* for the
  content surface; `stack-seeding/dna/data-dna.json`'s content entry declares **five**:
  `snapshot-row-count, snapshot-structural, snapshot-referential, snapshot-public-only,
  snapshot-cross-surface-closure`. Booked MINOR rather than BLOCKER only because the JSON's **own**
  `note` field also says four — i.e. the corpus faithfully mirrors a stale note in the source.
- `corpus/architecture/alignment_testing.md:514-522` — the Layout block lists `internal/{dna,outcome,compare,report}`
  and omits `internal/canon` (present, and it is what makes the "canonical and precision-preserving"
  comparison at `:120-121` true).
- `corpus/services/next-web-app.md:49` — names `apps/web/src/proxy.ts` "(and `apps/hiring/src/proxy.ts`)";
  `apps/integration/src/proxy.ts` also exists.
- `corpus/services/next-web-app.md:39-44` — the "Shared packages (`packages/` + `configs/`)" table lists
  `@anthropos/graphql`, `@anthropos/ui`, `@anthropos/core-js`; `packages/design` (`@anthropos/design`) is
  a fourth workspace package and is absent.

---

## Positively cleared (audited zeros)

**ENUMERATED** (predicate re-derived from source independently; cardinalities compared):

- `services/README.md:22-23` — *"at `0dab54d` compose declares **8** services (10 effective, with
  `common.yml`'s `postgresql` + `redis`) and `repos.yml` carries **6** entries"*. Derived: compose
  services = `{sentinel, backend, storage, customerio-sync, messenger, studio-desk, next-web-app,
  gotenberg}` = 8 (`app-network` is under `networks:`, correctly excluded); `common.yml` adds
  `postgresql`, `redis` (and its own `app-network` alias) → 10; `repos.yml` = `{app, sentinel, storage,
  messenger, next-web-app, studio-desk}` = 6.
- `services/README.md:15-18` + `:35` — the roadrunner "orphaned, not merged-and-undeployed" row.
  `roadrunner/terraform/main.tf:19` = `service_desired_count = 1`; `cms/terraform/main.tf:39` = 0;
  `jobsimulation/terraform/main.tf:40` = 0 — all three anchors land exactly on the assignment. I also
  tested the *predicate* ("the one row where prod and `repos.yml` contradict each other") by walking
  every service: only roadrunner is simultaneously absent from `repos.yml` and desired-count 1.
- `services/README.md:20-23` — *"platform `d11a403` (2026-08-03) deleted all three from `docker-compose.yml`
  **and** from `repos.yml`"*. Diffed `d11a403^` → `d11a403`: compose 11 services → 8 (cms, jobsimulation,
  roadrunner removed); `repos.yml` 9 entries → 6 (same three removed). Date confirmed 2026-08-03.
  Note the commit's *own message* claims roadrunner's `repos.yml` entry "was already gone" — the diff
  refutes the commit, and the corpus is the one that is right.
- `backend.md:39` — the RPC-mux enumeration. Read `main.go:1180-1235` @ `b948604f` in full, not just the
  cited lines: `:1185` `mux := http.NewServeMux()`; Users `:1187`; Organizations `:1188`; Skiller `:1196`;
  JobSimulation `:1204`; LabSession `:1228`; and `if cmsRPCServer != nil { … }` at `:1212-1214`. Five
  unconditional + one conditional, exactly as claimed, and `:1185-1228` bounds the block.
- `backend.md:41` — *"There is no `SkillPathSessionService` — measured: **0** occurrences in Go source, and
  no `skillpath…v1connect` package is imported."* `git grep -n "SkillPathSessionService" b948604f -- '*.go'`
  → rc=1, 0 hits; `git grep -n "skillpath.*v1connect" b948604f -- '*.go'` → rc=1. Positive control in the
  same pass: `SkillerService` → 4 files. Both conjuncts of the claim tested (§5 rule 17).
- `backend.md:195` + `:208` — the messenger address set and the storage fold. `docker-compose.yml:173-183`
  sets all four (`BACKEND_USERS_RPC_ADDR`, `CMS_RPC_ADDR`, `JOBSIMULATION_RPC_ADDR`, `SKILLER_RPC_ADDR`)
  to `http://backend:8083`, under the comment at `:171-172`; `app/main.go:1205-1211` @ `b948604` carries
  the stale *"until the M809 re-point"* comment verbatim. For storage I enumerated `STORAGE_RPC_ADDR`
  **repo-wide** at `9d00a313`: excluding `knowledge/**` docs, exactly **3** occurrences —
  `internal/jobsimwiring/wiring.go:101`, `internal/storagens/callsites_test.go:189`, `main.go:451` — and
  all three are comments, so **0 read sites**, and `main.go:451` does say *"STORAGE_RPC_ADDR is gone"*.
  `main.go:471`/`:472` @ `9d00a313` are `internalstorage.NewManager` / `NewPublicManager`. Every element
  of the claim holds at the ref it names. (At the *checkout* `b948604f` the same grep returns 3 live
  read sites — `main.go:446`, `:524`, `:992` — which is why the ref-pin here is load-bearing and correct.)
- `backend.md:63-65` — the ten moved skiller tables. Located each `CREATE TABLE` in
  `app/terraform/migrations/`: 9 of 10 in `20260615130000_skiller_taxonomy.sql`, `skills` in the
  supabase baseline. Merge commit `1fc00c78 Deprecate skiller schema …` (2026-06-29) confirmed.
- `backend.md:80-91` + `:16-20` — the subgraph ladder. Enumerated `schemas/` at each named ref:
  `749dc86` (2026-06-24, deletes `skiller.graphqls`) leaves `{backend, cms, jobsimulation, skillpath}` = 4;
  `7c17e63` (2026-07-21, deletes `skillpath.graphqls`) leaves 3; `915da06` (2026-07-29) deletes **both**
  `cms.graphqls` and `jobsimulation.graphqls` in one commit → `{backend}` = 1. The **3 → 1** figure is
  right and the commit's own subject line ("2→1") is the wrong one, exactly as the corpus warns.
- `backend.md:185` — **13** `ai_readiness_*` tables. `git ls-files internal/data/ent/schema | grep -ci readiness`
  → 13, and all four named "omissions" (`recommendation`, `email_override`, `notification_log`,
  `notification_optout`) plus both named non-omissions (`live_snapshot`, `text_translation`) are present;
  13 − 4 = 9 reconciles the passage's own subtraction.
- `backend.md:218` — the stream set. Derived from `AddSubscriber` call sites in `main.go` rather than from
  the doc's list: `SKILLPATH_STREAM` (`:1274`), `SKILLER_STREAM` (`:1276`), `JOBSIMULATION_STREAM` (`:1285`),
  `CMS_STREAM` (`:1303`), `AI_USAGE_STREAM` (`:1305`), `serviceName` i.e. backend (`:1320`) — five
  application streams plus the AI usage stream, with the "never a 2nd AddSubscriber" rule stated in the
  source's own comments at `:1282` and `:1290`.
- `sentinel.md:5`, `:85`, `:89` — *"`AUTHORIZATION_ADDRESS` set in exactly one block"*. Grepped all three
  named files (`docker-compose.yml`, `common.yml`, `.env_example`): **1** hit, `docker-compose.yml:48`.
  Tested the second conjunct too: `messenger` has **zero** `authorization` references across its 42 Go
  files (positive control run in the same invocation) and no `sentinel` line in `go.mod`.
- `sentinel.md:26` — *"6 request types, 6 policy types, 3 role groupings, 6 matchers"*. Counted in
  `internal/authorization/casbin.go:14-45`: `r,r2..r6` = 6; `p,p2..p6` = 6; `g,g2,g3` = 3; `m,m2..m6` = 6.
  Each of the six matcher-table rows also matches its source line semantically.
- `sentinel.md:68-83` — the RPC method table. Enumerated every exported method on `RPCHandler` in
  `internal/rpcsrv/rpc.go` (19: CheckFeature, CheckOrganizationFeature, AddUserToTier, RemoveUserFromTier,
  GetQuotas, GetOrganizationQuotas, OrgCheckPermission, OrgAddUserToRole, OrgRemoveUserFromRole,
  OrgReplaceUserRole, OrgClearAll, OrgAllowUserToUseFeature, OrgDisallowUserToUseFeature,
  OrgMembershipsAllowedToUseFeature, OrgGetOrganizationFeatureCredits, OrgSetOrganizationFeatureCredits,
  Reload, Check, BulkCheck) — the table covers the set exactly, no extras, no omissions.
- `sentinel.md:40` + `:43-48` — the role model. `app/internal/data/ent/enum/membership.go:8-15` spans
  exactly the four `MembershipRole` consts; `init_policy.sql:88-118` is exactly the `content_creator`
  block (banner comment at 88, last `p5` row at 118); `internal/authorization/casbin_content_creator_test.go`
  exists; `grep -c manager init_policy.sql` → **0**.
- `sentinel.md:12`, `:22`, `:122-125`, `:129` — `go.mod:3` `go 1.26.0`; `Dockerfile:2` and `Dockerfile.dev:2`
  both `FROM golang:1.26-bookworm AS build`; `terraform/locals.tf:4-5` `service_cpu = 256` /
  `service_memory = 128`; `Makefile:3` hard-codes
  `psql "postgresql://postgres@localhost:5432/postgres?sslmode=disable" < init_policy.sql` and never reads
  `DB_CONNECTION`; `init_policy.sql:63-66` is the taxonomy:write NOTE; `local_superadmin_grants.sql` exists.
  No `ent` package anywhere in the repo (single-table claim holds).
- `next-web-app.md:22` — all **four** apps declare `"next": "^16.2.7"` (web, hiring, integration,
  maintenance); React `^19.2.7`; `engines.node ">=24.0.0"`; `packageManager pnpm@10.30.3`; turbo `^2.9.6`;
  `UPGRADE-IMPACT-next16.md` present.
- `next-web-app.md:44` — **8** locales: `ls configs/i18n/messages` → de, en, es, fr, it, ja, nl, pt.
- `next-web-app.md:49` — read `apps/web/src/proxy.ts` end-to-end (82 lines). `middleware.ts` exists nowhere
  in the repo; every one of the seven named allowlist entries is present in `createRouteMatcher` (the claim
  says "includes", and the real list is longer); `/print` is HMAC-gated via `validatePrintToken`
  (`packages/core-js/src/security/printToken.ts:16`, `:49`, reading `PRINT_ROUTE_SECRET`); repo
  `CLAUDE.md:55` does say Next 16 renamed the convention.
- `next-web-app.md:86-90` — the `PROFILE=frontend` exit-1 claim. `docker-compose.yml:250-252` is
  `depends_on: backend: condition: service_started` and `:253` is `profiles: [frontend, all]`, while
  `backend` is `profiles: [core, backend, all]` at `:100` — so `frontend` alone selects a service whose
  dependency is unselected. `Makefile:119-120` is `up-frontend: … --profile core --profile frontend`.
- `next-web-app.md:103-105`, `:123`, `:115-116`, `:71-72` — `apps/web/.env.example:62`
  `NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT=http://localhost:8082/graphql/query`, `:63` backend API
  `http://localhost:8082`, `:36` `GRAPHQL_SCHEMA_FOR_GEN=http://localhost:8082/graphql/query`;
  `packages/graphql/codegen.ts:8` carries the `// was 'http://localhost:5050/graphql'` trail; only
  `Dockerfile.dev` at root while repo `CLAUDE.md:82-84` still claims two; `test` script exists in
  `apps/web` + `apps/hiring` only; no `.storybook/` dir, no `storybook` script, and
  `configs/tailwind/storybooks.css` is the sole survivor. Zero `DIRECTUS` references repo-wide.
- `clerkenstein.md:41-44` + `alignment_testing.md:256-260` — every gene/capability count, derived by
  parsing the five DNA JSONs rather than by trusting the tables: `clerk-2.6.0` 14 caps / **27** variants;
  `clerk-js-5` 6 / **9**; `clerk-multi-1` 5 / **9**; `clerk-deploy-1` 3 / **7**; `clerk-express-1` 5 / **13**.
  Every source-version string matches too (`clerk-sdk-go/v2 v2.6.0`, `@clerk/clerk-js ^5.52.3` /
  `@clerk/nextjs ^6.39.2`, `@clerk/express ^1.3.47`, `colony@v0.34.3`).
- `clerkenstein.md:41` / `alignment_testing.md:264` — the **97.2%** figure. Re-derived the *weighted sum*
  from the DNA (weights 3/2/1 by criticality): total weight **71**, `MembershipOrgIdentity` weight **2**,
  69/71 = **97.2%** exactly. 7 critical capabilities / 17 critical genes.
- `clerkenstein.md:274-279` — the colony/clerk-sdk drift. `app/go.mod` `colony v0.35.2`; `sentinel` and
  `storage` both `v0.34.3`; `app` `clerk-sdk-go/v2 v2.7.0`. (Also measured, not claimed: `messenger` is
  on `v0.35.2`.)
- `clerkenstein.md:160-182` — the single-seat limitation, verified in code not prose:
  `clerk-frontend/registry.go:24` one `activeKey`, `:67` `active()`, `:75` `Select()`;
  `clerk-frontend/server.go:102/107/108/109` one each of `signedIn`/`signedOut`/`clientID`/`sessID`;
  `:125` `clientID: "client_clerkenstein"`; `establishLocked` at `:663-670` sets
  `sessID = "sess_clerkenstein"`; `handleSelectIdentity` at `:627-652` sets `signedIn = false; sessID = ""`.
  And the sharpest sub-claim — *"`r.Cookie(...)` is called **nowhere** in `clerkenstein/`"* — grepped
  `.Cookie(` across every `.go` file in the section: rc=1, zero hits, against a positive control of 17
  `http.Request` occurrences in the same file.
- `clerkenstein.md:206-209` — all **five** named tests exist in `clerk-frontend/server_test.go`.
- `clerkenstein.md:140-143` — the studio-desk both-forms claim: `src/index.ts:96` and
  `app/services/userService.ts:16` both hold
  `['admin', 'org:admin', 'content_creator', 'org:content_creator']`, each preceded (at `:95` / `:15`) by
  the exact comment *"Both the prefixed (org:*) and bare role keys are accepted."*
- `clerkenstein.md:59-62`, `:97-106`, `:211-227`, `:232-241`, `:302-312`, `:358` —
  `clerk-backend/store.go:138` (`SeedOrgIdentity`) / `:151` (`LookupOrgEid`);
  `alignment/dna/clerk-2.6.0.json:131` is the M219 description sentence quoted;
  every dir in the repo-structure table exists, and `alignment/` really does hold
  `cmd/{clerkrun,deployrun,expressrun,jsfapirun,multirun}` + 5 DNAs + `golden{,-deploy,-express,-js,-multi}`
  + `scripts/`; `stack-seeding/seeders/roster.go` carries `org_eid:47`, `org_name:53`, `org_slug:54`,
  `org_is_hiring:77`, `BuildRoster:98`, `orgSlugFor`, `IsHiringOrg()`; `resources.go:50-51` are the
  `orgNameDefault`/`orgSlugDefault` constants with the exact literals; `deploy/colony-authn/` exists;
  `clerk-frontend/key.go:28` `MintPublishableKey`; `server.go:59` `clerkJSFetchTimeout = 15 * time.Second`,
  `:65-68` the "Explicitly NOT http.DefaultClient" `clerkJSClient`, `:72` `FAKE_FAPI_CLERKJS_CACHE`,
  `:29` `FAKE_FAPI_CLERKJS_CDN`; `clerkenstein/.github/workflows/alignment.yml` is tracked and there is
  **no** `.github/workflows` at the monorepo root — so "git-tracked but inert" is exactly right.
- `clerkenstein.md:119-122` — the BAPI≠FAPI pair: `clerk-backend/server.go:47`
  `GET /v1/users/{userID}/organization_memberships` and `clerk-frontend/server.go:186`
  `GET /v1/me/organization_memberships`; `meorgmemberships_test.go` present.
- `alignment_testing.md:103-107`, `:155`, `:162-169`, `:193` — `internal/dna/dna.go:21`
  `idPattern = "^[A-Za-z0-9][A-Za-z0-9_-]*$"`, `:24` `maxWeight = 1_000_000` enforced at `:269-270`,
  weights 3/2/1 at `:41-45`, and `:280` is the *"no critical genes: a DNA must declare at least one
  `critical` capability"* rejection. `compare.go:43` exposes `critical_genes` and `:57` is
  `if minCritical > 0 && r.CriticalGenes == 0` — the vacuous-100% defence, pinned by
  `TestGateMetVacuousCritical`. `cmd/alignctl/run.go:38` calls `d.Validate()` before any scoring.
- `alignment_testing.md:194`, `:203-206` — `gate.sh:69` is literally
  `"$alignctl" dna coverage --dna "$base/$dna" --if-declared`, with the rationale comment at `:58-67`
  and `:61` inside it, exactly as the doc says. And **only** `clerk-2.6.0.json` carries a
  `consumed_surface` key (15 entries); the other four DNAs have no such key — I derived this from the
  JSON top-level keys, not from the doc.
- `alignment_testing.md:288-311`, `:516` — the toy worked example, re-derived end to end: DNA is
  `Add` (critical, 3 variants) + `Greet` (standard, 3 variants); weights 3·3 + 2·3 = 15; the single
  divergence `Greet/padded-name` costs 2 → 13/15 = **86.7%**, critical 3/3 = **100%**, and the report
  line's `(exact, w2)` is the standard-criticality weight. `examples/toy/alignment_test.go:21-22` sets
  the 80/100 gate; `//go:build alignment` is line 1; `cmd/alignctl/dna.go:20-27` is the
  `list|diff|validate|coverage` switch; module is `anthropos.dev/alignment`; `examples/toy/cmd/toyrun` exists.
- `alignment_testing.md:479-487` — `data-dna.json` surface statuses: taxonomy = `snapshot-seeded-m9b`,
  content = `snapshot-seeded-m10`, four `seeded-m7a` (organizations, users, memberships, casbin-grant),
  four `seeded-m7c` — **nothing `waived`**, so the "100% coverage, nothing left waived" claim holds.
  The content note's public predicate `private=false AND tenant_id IS NULL AND status='published'` is
  verbatim in the JSON.
- `services/README.md:31`, `backend.md:13` — cms-in-app at **app v1.360.0**: corroborated independently by
  `graphql-wundergraph@915da06:subgraphs.conf` reading `BACKEND=v1.360.0`.
- `services/README.md:45` (`2adcf71`, 2026-07-31), `:72`/`:73` (`045857c`, `fdfa189`, both 2026-04-17) —
  all commit shas resolve in `platform` with the stated dates and subjects.

**RE-DERIVED** (recomputed the document's own displayed arithmetic — the weak clearance, listed
separately per §5 rule 24):

- `backend.md:70-76` — the taxonomy figures (42,790 public skills of 43,584 total incl. 794 org-private).
  The arithmetic is internally consistent and the "floors, not totals" caveat is present, but these are
  **prod measurements** I have no read path to; I could not enumerate the set. Treat as unverified, not
  cleared.
- `backend.md:185` 13 − 4 = 9 — the subtraction reconciles, and I separately ENUMERATED the 13.

---

## What I could not settle

1. **`corpus/services/db-backup.md` in its entirety (31 lines).** There is no `db-backup` clone in the
   ground-truth set, no `db-backup` reference anywhere in `stack-demo/platform` (grepped, zero hits),
   and the doc itself says the service is production-only. Every claim in it — Go, Docker-scheduled,
   6-hour cadence, S3 + Azure + Hetzner targets, "PostgreSQL RDS (all schemas)" — is unfalsifiable from
   here. **What would settle it:** a clone of `anthropos-work/db-backup` (or the `infrastructure` repo's
   module for it) added to the ground-truth set. I flag this as a structural gap: a 31-line doc that no
   reading in this series can grade is a permanent audited-zero of the least trustworthy kind (§5 rule 22).
2. **Prod-terraform RPC literals.** `backend.md:79` (`http://backend:8081`) and `backend.md:195`
   (`http://backend.internal.anthropos:8081`). The service repos parameterise these
   (`cms/terraform/main.tf:106-107`, `jobsimulation/terraform/main.tf:120-121`,
   `messenger/terraform/main.tf:78-79` all read `"${var.skiller_rpc_addr}"` / `"${var.skiller_rpc_address}"`),
   so the literals live in the un-cloned `infrastructure` repo. **What would settle it:** the
   `infrastructure` repo, or the rendered task definitions.
3. **`services/README.md:45` — "repo ARCHIVED"** for `graphql-wundergraph`, and **`:72`** — chronos's
   repo "is NOT archived (last push 2026-04-23)". Both are GitHub *repository-metadata* claims; a local
   clone cannot express archived-ness. **What would settle it:** `gh repo view --json isArchived,pushedAt`
   for each.
4. **A phase note, not a finding.** `backend.md:5` (*"Five former microservices now run inside `app`"*)
   is unpinned and therefore graded at the checkout `b948604f` (v1.366.0), where exactly five are folded —
   **true**. `backend.md:208` pins `9d00a313` (v1.367.0) for storage-in-process — **also true**. The two
   describe different phases and the corpus is right at both refs, so I booked nothing. But a reader who
   takes the banner as current will be one release behind on storage and messenger, and the file gives no
   signal that the banner is the *older* of its two vantages. Worth a routed clarification rather than a
   blocker.
5. **Out of my file set, but measured and material — routing it rather than dropping it.** Root
   `CLAUDE.md` asserts *"Prod compute for both is **stopped** (`storage/terraform/main.tf:38`,
   `messenger/terraform/main.tf:29`, each `service_desired_count = 0`)"*. Measured at the ground-truth
   clones: **both files put `service_desired_count = 1` at line `:19`**, and neither `:38` nor `:29` is a
   desired-count line (`storage/terraform/main.tf:38` is inside a `container_definitions` JSON heredoc).
   Wrong value, wrong line, in the repo's highest-propagation instruction file (§5 rule 19's corollary).
   Whoever owns `CLAUDE.md` this pass should re-derive it; I did not book it as one of my findings
   because it is outside my assignment.
