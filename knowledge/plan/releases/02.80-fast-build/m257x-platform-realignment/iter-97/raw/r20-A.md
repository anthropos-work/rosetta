# Seat A — reading #20

Corpus under audit: `rosetta` @ `00be1ac`, tree clean (working tree matches; only the untracked
`iter-97/` directory present).
Ground-truth clones re-derived at open and matching the ADDENDUM sheet exactly:
`platform 0c91421d` · `app b948604f` (origin/main `2035f9a4`) · `app/studio aeec036a` ·
`cms/studio aeec036a` · `next-web-app bb3313bc` · `storage 4ce8ece5` · `messenger fa47850d` ·
`ant-academy 9c3843cd` · `jobsimulation 462343b0` · `cms ca50c817` · `sentinel 88bc5592` ·
`studio-desk 14a5442a` · `roadrunner 87d8d443` · `graphql-wundergraph 60c229f3` ·
`.agentspace/rosetta-extensions 6130bfd8` (authoring) · `stack-demo/rosetta-extensions ab81527a`
(tag `fast-build-m257x-iter-58`, the older consumption clone).

Search set enumerated with `find stack-demo -name .git -maxdepth 4` — **15 trees**, the two nested
`studio` repos included. Every absence below names the trees it covers and carries a positive
control in the same pass.

## Positive controls

| file | `wc -l` | read in full? |
|---|---|---|
| `corpus/architecture/external_services.md` | 880 | yes |
| `corpus/services/cms.md` | 311 | yes |
| `corpus/services/jobsimulation.md` | 309 | yes |
| `corpus/services/ai-labs.md` | 156 | yes |
| `corpus/services/academy-backend.md` | 147 | yes |
| `corpus/services/skiller.md` | 69 | yes |
| `corpus/services/TEMPLATE.md` | 46 | yes |
| **total** | **1,918** | |

---

## Blockers

### B1 — `external_services.md:870` cites `:522`, which is a code comment; the correction it names is at `:546`

- **Anchor:** `corpus/architecture/external_services.md:870` — *"> Consistent with :522 above, where the
  same correction is already recorded."*
- **Why it is false:** the correction referred to (*"~~**Restart Wundergraph**: `docker compose restart
  graphql`~~ — there is no `graphql` service locally any more; restart `backend`"*) is at
  **`external_services.md:546`**. Line **522** is `// Queries in app/graphql/*.graphql`, a comment inside
  the Studio-Desk TypeScript code fence at `:520-527`. Measured against the file at rosetta `00be1ac`.
  **This is repair-induced line drift (§5 rule 34):** `git show 00be1ac^:corpus/architecture/external_services.md
  | sed -n '522p'` returns the correction verbatim — the immediately-preceding repair added 24 lines above
  it and did not re-point the self-citation.
- **Confidence:** high

### B2 — `cms.md:211` cites the "Data" bullet at `:28-31`; the Data bullet is at `:44-47`

- **Anchor:** `corpus/services/cms.md:211` — *"the legacy `cms` schema is non-authoritative. Consistent
  with the **Data** bullet, :28-31 above)"*
- **Why it is false:** `cms.md:44-47` is the Data bullet (*"**Data** — `similarities`,
  `similarity_categories`, … re-created in the **`public` schema** by
  `app/terraform/migrations/20260724132049_cms_data_model.sql`"* — migration verified present in
  `stack-demo/app/terraform/migrations/`). Lines **28-31** are the M809 / `CMS_RPC_ADDR` banner text
  (*"Nor is there a `CMS_RPC_ADDR` any more: M809 re-pointed it at `http://backend:8083` on the
  `messenger` block…"*) — a different claim about a different subject. A reader following the
  cross-reference to check the consistency it asserts lands on unrelated text.
- **Confidence:** high

### B3 — `cms.md:233` cites the "Studio" bullet at `:52-53`; the Studio bullet is at `:70-71`

- **Anchor:** `corpus/services/cms.md:233` — *"see the **Studio** bullet, :52-53 in the banner at the top
  of this doc."*
- **Why it is false:** the Studio bullet is `cms.md:70-71` (*"**Studio** — the Python
  `anthropos-studio-room` project is now pulled into the **`app`** image via the CI `additional_repo`
  mechanism (app v1.360.1)"*). Lines **52-53** read *"**The M809 re-point had already landed** … `cms` is
  not among the **five** services compose declares at platform `0c91421`"* — the RPC bullet, not the
  Studio one. Same class as B2, in the same file.
- **Confidence:** high

### B4 — `jobsimulation.md:95` cites the "Data" bullet at `:31-38`; the Data bullet is at `:39-45`

- **Anchor:** `corpus/services/jobsimulation.md:95` — *"The legacy `jobsimulation` schema is **not
  authoritative** — consistent with the **Data** bullet, :31-38 above"*
- **Why it is false:** `:31-38` spans *"This is the same pattern as the earlier skiller-in-app…"*, *"Where
  everything went:"* and the whole **Domain / engine** bullet, ending at
  *"`app/internal/jobsimwiring/wiring.go`."*. The **Data** bullet begins at **`:39`** and runs to `:45`.
  **Repair-induced:** at `00be1ac^` the same citation did reach the Data bullet (`:36-38` held its first
  three lines); the repair inserted 3 lines above and the anchor now misses the bullet entirely.
- **Confidence:** high

### B5 — `academy-backend.md:15` cites `:74-76`, which is the certificate-minting paragraph

- **Anchor:** `corpus/services/academy-backend.md:15` — *"The env-var *name* outlived the router; in
  production the router is still declared. (Consistent with :74-76 below.)"*
- **Why it is false:** `:73-75` is *"**Certificate minting** (`certificate.go`): id `AAS-YYYY-XXXXXX` …
  a 6-char suffix from a 30-symbol ambiguity-free alphabet via `crypto/rand`"* and `:76` is blank —
  nothing about the router, the subgraph or `NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT`. The passage the sentence
  claims consistency with is at **`:80-83`** (*"**Primary — GraphQL, on the `app` (backend) subgraph.** …
  **There is no separate "academy subgraph"** … The frontend reaches them at
  (`NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT`)"*). This file was **not** touched by `00be1ac` (last touched at
  `328ece5`), so unlike B1/B4 this one is pre-existing.
- **Confidence:** high

### B6 — `external_services.md:209-211` — both `rosetta-extensions` anchors name the wrong construct at the authoring ref

- **Anchor:** `corpus/architecture/external_services.md:209-211` —
  *"`rosetta-extensions/stack-injection/gen_injected_override.py:669-670` re-points every service in
  `DIRECTUS_DATA_CONSUMERS`, which is **`("cms", "backend")`** (`:84`)."*
- **Why it is false:** the citing block pins no ref, so per the addendum it grades against the
  **authoring copy** `.agentspace/rosetta-extensions` @ `6130bfd8`. There:
  - `DIRECTUS_DATA_CONSUMERS = ("cms", "backend")` is at **`:86`**, not `:84`. Line `:84` is
    `# (DIRECTUS_PUBLIC_BASE_ADDR) is deliberately NOT re-pointed — it stays on prod public links so` —
    an anchor that resolves but states, near enough, the opposite of the sentence citing it.
  - the re-point is at **`:698-699`** (`if with_directus and name in DIRECTUS_DATA_CONSUMERS:` /
    `env.append(f"DIRECTUS_BASE_ADDR={DIRECTUS_INNETWORK_ADDR}")`). Line `:669-670` is a **CORS** comment
    (`# CORS_EXTRA_ORIGINS in non-production (a runtime hook, no platform change), so feed it this stack's` /
    `# offset frontend origins. …`) inside the `if name == "backend":` block.
  - Derivation: `grep -n 'DIRECTUS_DATA_CONSUMERS' stack-injection/gen_injected_override.py` → `79, 86, 698`;
    file is 891 lines.
  - **Both anchors DO resolve at the older `stack-demo/rosetta-extensions` clone** (`ab81527a`, tag
    `fast-build-m257x-iter-58`): `:84` and `:669-670` exactly. That is the drift, and it is the case the
    addendum's rule 2 exists to catch.
  - The third anchor in the same passage, `stack-injection/tests/test_injection.py:1109`
    (`def test_backend_the_actual_reader_is_repointed`), **is correct at the authoring ref** — so the
    passage is half-drifted, which is worse than uniformly drifted.
- **Confidence:** medium — I hesitated because the line numbers are true at the pinned consumption clone
  a stack actually runs, and because the *substance* (`("cms","backend")`, the re-point, the replaced test)
  is correct at both refs. Booked because the briefing's grading rule names "wrong line anchors that point
  at the wrong construct" as a BLOCKER, the addendum names the authoritative copy explicitly, and `:84`
  now reads as a denial of the claim it is cited for.

### B7 — `external_services.md:3` asserts a GitHub archive state the same repair declared unmeasurable from a clone

- **Anchor:** `corpus/architecture/external_services.md:3` — *"In *production* the router is still
  declared (`graphql-wundergraph/terraform/main.tf:20` `= 1`), though **the repo is ARCHIVED on GitHub
  (2026-07-30)**."* Restated at `:418` (*"the archived repo still contains these configs"*), `:459`
  (*"as the archived repo still has it"*) and `:479`.
- **Why it is unsupportable:** GitHub archive state is a property of the GitHub API, not of a clone.
  The **same commit** (`00be1ac`) that produced this tree retracted the identical claim for a sibling
  repo — `jobsimulation.md:24-27` now reads *"**The repo's GitHub archive state is not visible to this
  corpus** — this banner asserted an archive on 2026-07-31 … Report both, assert neither"* — and
  `cms.md:18` applies the same epistemic limit to the `infrastructure` repo (*"which has never been in
  any clone set we have"*). The predicate was corrected at one site and left standing, with a hard date
  and bold emphasis, at four sites in a file the very same commit edited. Measured: `graphql-wundergraph`
  has no commits after `60c229f 2026-07-30`, so nothing in the clone set *refutes* the date — but nothing
  in the clone set can *establish* it either, which is exactly the standard the corpus just adopted.
- **Confidence:** medium — I hesitated because the claim is not demonstrably false, only unsupportable,
  and the briefing folds "unsupportable against ground truth" into BLOCKER while §5 rule 19's
  half-repair argument is the aggravating factor. Booked on the self-contradiction limb.

---

## Minors

- `corpus/services/jobsimulation.md:25-27` — three lines added by `00be1ac` **lost the `>` blockquote
  prefix** inside the merge banner (`2026-07-31; \`origin/main\` then landed …`, `PR #439, …`,
  `Report both, assert neither. State: …`). CommonMark lazy continuation keeps them inside the quote, so
  the render is unharmed; the source is malformed and the next edit that inserts a blank line above them
  will silently eject three lines of a retraction out of its banner.
- `corpus/architecture/external_services.md:565` and `:598` describe the Go Mistral client as
  `mistral.NewMistral(...)` at `app/internal/cms/studio/markdownManager.go:19` — **true at the checkout
  `b948604f`** (verified: `:19` is `ai, err := mistral.NewMistral(nil, os.Getenv("MISTRAL_API_KEY"))`) —
  while `cms.md:215-218`, edited in the *same* commit, states the current `origin/main` `2035f9a` form
  (`markdownManager.go:10` imports `internal/cms/studio/mistralocr`, `:30` is
  `return &MarkdownManager{ocr: mistralocr.New(aiKey)}, nil`; both verified). Neither is false at its own
  ref, so this is not a blocker — but the `external_services` sites carry **no ref at all**, which is the
  §5 rule 19 pattern one edit away from becoming one.
- `corpus/services/jobsimulation.md:203` labels `7177374` as "**`origin/main`**". At this reading
  `origin/main` is `2035f9a4`, where `func main()` is at `:229`, not `:216`. The sha itself resolves
  correctly (`git show 7177374:main.go` → `216:func main() {`), as do the two companions (`9d00a313` →
  `:216`, `b948604` → `:212`), so the substance holds; only the "origin/main" label has expired.
- `corpus/architecture/external_services.md:455` presents a **paraphrase in quotation marks** as
  `graphql-wundergraph/CLAUDE.md:39`'s text: the corpus writes *"Since cms-in-app the platform compose
  `graphql` service builds from the **production** Dockerfile"*; the file actually reads *"Since the
  compose stack builds from the production Dockerfile, a schema change in `app` does **not** appear at
  `:5050` until you regenerate and commit that file here."* Same claim, different words; the line number
  and the adjudication are right.
- `corpus/architecture/external_services.md:14` — the PM-level summary still reads *"GraphQL/Wundergraph -
  Unifies **all backend services** into a single API"* while `:368`/`:373`/`:406` establish the supergraph
  is **one** subgraph. Stale-but-harmless phrasing in a section that immediately points at the two-state
  note.

---

## Positively cleared (audited zeros)

Labelled per the briefing. Only **ENUMERATED** counts as a clearance.

### ENUMERATED

- **`core` starts five containers; compose declares five services / seven effective at `0c91421`.**
  Enumerated `^  [a-z…]*:` over `docker-compose.yml` + `common.yml`: `sentinel:5`, `backend:28`,
  `studio-desk:112`, `next-web-app:143`, `gotenberg:170` (5) + `postgresql`, `redis` from `common.yml`
  (7 effective). `profiles:` appears at `:110`, `:141`, `:168`, `:183` only → the floor
  (`postgresql`,`redis`,`sentinel`) is profile-less, `core` = `{backend, gotenberg}`, total started = 5.
  At `0dab54d` the same enumeration gives **8 declared / 10 effective** — matching
  `external_services.md:306` and its twins in `cms.md:53` / `jobsimulation.md:52`. *(`external_services.md`
  `:172`/`:183` anchors resolve to each service's own `profiles: [core, backend, all]` line — correct.)*
- **`app` env-var counts at the pinned ref `5ba17044`.** `git grep -o -w <NAME> 5ba17044 -- '*.go'`:
  `OPENAI_KEY 5 · ANTHROPIC_API_KEY 26 · AZURE_OPENAI_KEY 13 · AZURE_OPENAI_ENDPOINT_URL 13 ·
  OPENAI_ORG_ID 0 · AZURE_OPENAI_ENDPOINT 0 · AZURE_OPENAI_DEPLOYMENT 0 · OPENAI_API_KEY 2` — all eight
  match `external_services.md:665-670` exactly.
- **"all 30 `*_AI_*_MODEL` lines pin `azure`"** (`external_services.md:567`, `:616`, `:637`). Enumerated
  over `app/studio/configs/*.ini` @ `aeec036a`: **30** matching lines, **0** non-`azure`. The three files
  are `config_template.ini`, `development_config.ini`, `production_config.ini`.
- **`STORAGE_RPC_ADDR` "3 remaining occurrences, all comments, zero reads" at `app 9d00a313`**
  (`academy-backend.md:57-58`). `git grep -n STORAGE_RPC_ADDR 9d00a313 -- '*.go'` → exactly 3:
  `internal/jobsimwiring/wiring.go:101`, `internal/storagens/callsites_test.go:189`, `main.go:451` — all
  three inside `//` comments. `main.go:471-472` are the two in-process manager constructions, as cited.
- **The subgraph ladder 5 → 4 → 3 → 1** (`external_services.md:376-386`). Enumerated `- name:` per commit
  in `supergraph-config-prod.yaml`: `749dc86^` = backend/skiller/jobsimulation/cms/skillpath (**5**);
  `749dc86` = **4**; `7c17e63` = **3**; `915da06^` = **3**; `915da06` = **1**. `git show --name-status
  915da06` marks **both** `schemas/cms.graphqls` and `schemas/jobsimulation.graphqls` `D`, and the commit
  *subject* really does say "supergraph 2→1" against a 3-entry tree — the corpus's warning is exact.
- **No `type Subscription` anywhere in the composed SDL.** `grep -n '^type Subscription'
  schemas/backend.graphqls` → 0; `type Mutation` at **`:4053`**, `type Query` at **`:4912`** as cited;
  the **6** `Subscription` substring hits are `activeSubscription`, `type PlanSubscription`,
  `stripeSubscriptionId` ×2, `stripeSubscriptionItemId`, and one doc-comment mention. `grep -n
  'sse\|subscription' supergraph-config-{compose,dev,prod}.yaml` → 0, with the positive control
  `grep -l backend` matching all three. `git log -S 'protocol: "ws"'` → 0 commits (control: `sse_post`
  → 5); `git merge-base --is-ancestor bba862f HEAD` → rc **1**, `git branch -a --contains bba862f` →
  `remotes/origin/feat/use-web-socket` only.
- **The pre-drop `depends_on` was FOUR services.** `git show 1e8e754:docker-compose.yml` `:19-27` =
  `backend`, `jobsimulation`, `cms`, `storage`, each `condition: service_started`; `:6-8` =
  `build:`/`context: ..`/`dockerfile: graphql-wundergraph/Dockerfile.dev`; `:14` = `"5050:8080"`.
  `2adcf71^` **is** `1e8e754`. `a4db680` = *"chore: decommission standalone skillpath service (M507)"*,
  2026-07-21, and its `depends_on` reads the same four. `464dfe3` is **not** an ancestor of `0c91421d`
  (rc 1) and lives only on `remotes/origin/feat/cms-in-app`.
- **The three Dockerfile eras.** `2c85211^` has **no** `dockerfile:` key; `2c85211` adds
  `dockerfile: Dockerfile.dev`; `b56d731` still declares `graphql:` at **`:22`** with
  `dockerfile: graphql-wundergraph/Dockerfile.dev` at `:25`, parked behind `profiles:
  [wundergraph-deprecated]` at `:36`; `360efd4` has no `graphql:` key (rc 1) with the positive control
  `backend:` at **`:28`**.
- **The taxonomy figures.** Read straight out of
  `.agentspace/snapshots/taxonomy/5afc0bccf1df7ef538b643321fc6362f/manifest.json`
  (`public_only: true`, `predicate: org-null`, `captured_at 2026-06-29T14:43:00Z`, `source: primary-read`):
  `skills 42790` / `job_roles 22470` / `skill_embeddings 42790` / `job_role_embeddings 18919`, each with
  its `organization_id IS NULL` filter present in the manifest. Matches `external_services.md:684-697`
  and `skiller.md:43-52` including the "18,919 is the embedding row count, transcribed onto the role
  count" diagnosis.
- **The 23 jobsim run-state tables.** `grep -c '^CREATE TABLE'
  app/terraform/migrations/20260722081626_jobsim_data_model.sql` → **23**, and `sessions` is among them;
  `20260722104506.sql:2` is `CREATE TABLE "job_simulation_sessions"` and `:79` is `DROP TABLE "sessions";`.
  `jobsimulation.md:39-45` is exact.
- **`20260729133514.sql` contains 0 `INSERT`s.** `grep -in 'INSERT INTO'` → rc 1; the single
  case-insensitive `insert` hit is `:64 DROP FUNCTION IF EXISTS
  on_insert_local_jobsimulation_sessions_update_memberships()` — a function *name*, not a statement.
  `:58` is `-- 5. Drop the mirrors.`, `:62-63` `DROP TABLE` both mirrors, `:15-23` the two id remaps,
  `:36-44` the orphan NULLing. `jobsimulation.md:130-135` verified in every clause including the
  M257x-iter-52 retraction of "back-fills then DROPs".
- **`ContentLanguage` has exactly 8 members** (`skiller.md:57-58`): `english, italian, spanish, french,
  german, dutch, japanese, portuguese` in `assignments.graphqls:44-53`.
- **`lab_bodies` = EN base + 6 overlays** (`ai-labs.md:52`): `academy`-style enumeration in
  `internal/data/ent/schema/lab_body.go:48-49` — `de/es/fr/it/nl/pt` (6) plus `en`.
- **The 30-symbol certificate alphabet** (`academy-backend.md:73-75`): `certIDRegex =
  ^AAS-\d{4}-[A-HJKMNP-TV-Z2-9]{6}$` → 8+4+5+5+8 = **30** symbols, ambiguity-free, `crypto/rand`
  imported at `certificate.go:5`.
- **`app/studio` mistral surface: 22 hits / 3 files** (`cms.md:95`, `cms.md:224`).
  `git -C stack-demo/app/studio grep -i mistral aeec036a` → **22**, in `requirements.txt`,
  `tools/pdf2md.py`, `tools/r3.py`; `requirements.txt:8` is `mistralai`; `git -C stack-demo/app grep
  mistral HEAD -- studio/` → **0**, confirming the nested-repo hole the corpus names (`app/.gitignore:79`
  = `studio/*`). `gen.py` never imports `tools` (rc 1); every `pdf2md` reference lives inside `tools/`.
  `bedrock|boto3` under `app/studio` → **0** with the positive control `openai` matching 5 files.
- **`requirements.txt` is 9 packages, byte-identical across both copies, and has no `python-docx`**
  (`cms.md:144-149`, `:265-266`): `shasum` of `stack-demo/cms/studio/requirements.txt` and
  `stack-demo/app/studio/requirements.txt` both `384da9c7…`; the 11 lines are the 9 named packages plus
  2 blanks; `grep -i docx` → rc 1 in both.
- **`NEXT_PUBLIC_GRAPHQL_ENDPOINT` does not exist in `next-web-app`** (`external_services.md:764-765`).
  Established against all three mechanisms the addendum names: `git grep NEXT_PUBLIC_GRAPHQL_ENDPOINT
  HEAD` → rc 1 with the control `NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT` matching 5+ files;
  `git ls-files | git check-ignore --stdin --no-index` → **0** gitignored-but-tracked files in this clone;
  and the known NUL-bearing file read by hand in Python — `apps/web/src/hooks/useCoursebuilder.ts`,
  50,433 bytes, the string absent in both UTF-8 and UTF-16LE. *(Aside, not a corpus finding: that file
  measures **1** NUL byte at this checkout, not the 1,178 the ADDENDUM states.)*
- **`credits`/`labs` scalars** (`ai-labs.md:71-83`): `creditCost` map = `build 5 · refine 1 ·
  translate 1`; `purchasePackages` = `starter 50 · team 200 · scale 500`; `MarginMarkup = 1.40`;
  `PricePerCreditUSD = 0.45`; `DefaultSeedBalance` pinned to `int64(500)` by `cost_test.go:100`; the
  partial-unique index `credittransaction_organization_id_action_type_ref_id … WHERE (ref_id IS NOT NULL)`
  at `20260717151144.sql:52`. **Note for the record:** `cost.go:29`'s *comment* says *"course.refine → 5
  credits per refine turn"* — the corpus's **1** is right and the platform's own comment is the stale one
  (`creditCost[ActionCourseRefine] = 1`, `// D1 ruling: flat 1 credit per refine turn`). Reading only the
  comment would have produced a false finding here.
- **Stripe webhook event set** (`ai-labs.md:109-111`): `internal/web/backend/api/api.go:319-327` handles
  `customer.created` + `customer.subscription.{created,updated,deleted}` and nothing else;
  `git grep checkout.session.completed origin/main -- '*.go'` → rc 1 (the doc asserts currency, so graded
  at `origin/main`, not the checkout), control `customer.created` = 3 hits in the same file.
- **`repos.yml` = 4 entries** (`cms.md:27`, `jobsimulation.md:15`): `app`, `sentinel`, `next-web-app`,
  `studio-desk`. At `0dab54d` it was **6** (adding `storage`, `messenger`); `d11a403`'s `repos.yml` diff
  removes exactly `- name: cms`, `- name: jobsimulation`, `- name: roadrunner`.
- **Compose sets zero `*_RPC_ADDR` at HEAD** (`cms.md:30`, `jobsimulation.md:48-49`): `grep -n _RPC_ADDR
  docker-compose.yml` → rc 1; at `0dab54d` four (all `http://backend:8083`, all on the `messenger`
  block); at `2adcf71` `CMS_RPC_ADDR=http://cms:8091` and `JOBSIMULATION_RPC_ADDR=http://jobsimulation:8401`.
  The only service address left is `AUTHORIZATION_ADDRESS=http://sentinel:8087` at `:48`, as
  `jobsimulation.md:146` says.

### RE-DERIVED (weaker — I recomputed a displayed arithmetic or resolved a cited anchor without independently enumerating the predicate)

Every one of the following `file:line` citations was opened and the surrounding 10-20 lines read
(`sed -n 'N-15,N+5p'`), and every one names the construct claimed:

- `external_services.md`: `docker-compose.yml:53` (@ both `0dab54d` and `0c91421`), `:46-94`,
  `:101-109`, `:110`, `:119`, `:135`, `:151`, `:160`, `:183`; `.env_example:91-92`;
  `app/main.go:980-982` and `:770` @ `b948604`; `app/cms_reader_switch.go` (quote verbatim);
  `cms/terraform/main.tf:39`; `graphql-wundergraph/terraform/main.tf:20` + `:48-49`,
  `terraform/locals.tf:8`, `config.prod.yaml:5`, `Dockerfile.dev:18`/`:19-20`/`:23`,
  `CLAUDE.md:39`; `git show a2a3ee6^:docker-compose.yml` `:384`/`:386`/`:409` (image `10.10.1`,
  `8055:8055`, `ADMIN_PASSWORD=password`) — and `git log --all -S 'admin@example.com' -- docker-compose.yml`
  returns nothing while the control `-S ADMIN_PASSWORD` returns 2 commits, so the "only the email is
  unfound" clause holds; `internal/jobsimulation/ai/ai.go` `:30-33`, `:80`, `:130-141`, `:150-155`,
  `:259-289`, `:264-276`, `:280-283`, `:296-302`, `:326`;
  `internal/cms/directus/collections/jobsimulation.go:905` and `:1302-1305`;
  `internal/jobsimulation/simulator/ai/ai.go:58-59` and `:114-115`;
  `internal/coursebuilder/bedrock.go:23`, `:29`, `:106-113`; `internal/askengine/bedrock.go:25`;
  `internal/jobsimulation/agent/report_agent.go:31`; `internal/cms/studio/markdownManager.go:19`,
  `studioManager.go:583`; `app/studio/tools/pdf2md.py:24`, `services/ai.py:383`, `:627-664`, `:704-724`,
  `:706-708`, `configs/config_template.ini:30-31`, `gen.py:41-53` and `:45-47`;
  `calls/livekit.go:110`/`:115`/`:120`/`:126` (and `anthropos-agent-eu` absent at `app` HEAD, control:
  the bare form matches 6 lines); `collections/jobsimulation.go:1596` (nil `voice_engine` →
  `gptrealtime`); `internal/web/backend/backend.go:130`; studio-desk `.env.example:57`
  `AI_PROVIDER_CHAIN=azure-openai,openai` and `src/services/ai/config.ts:21-37` (`gpt-5.2`,
  `claude-opus-4-5-…`, `claude-sonnet-4-5-…`).
- `cms.md`: `cms/go.mod:3` `go 1.26.4`; `cms/cmd/root.go:77`/`:78`; `cms/Dockerfile:2`/`:23`;
  `cms/terraform/main.tf` = **191** lines with `:39` `service_desired_count = 0` (also at cms
  `origin/main f38c0c4a`); `2adcf71` compose `:154-155`/`:169`/`:173` (8090/8091);
  `app/internal/skillpath/session.go:205-207`; `app/main.go:1205-1211` (the stale
  *"additive + DORMANT … until the M809 re-point"* comment, still present at `b948604`);
  `app/.gitignore:79`; `app/internal/cms/studio/studioManager.go:119`, `:531`, `xlsx.go:13`;
  `markdownManager.go:10`/`:30` @ `origin/main 2035f9a`; `services/ai.py:1-2` and `:705-708`;
  `tools/pdf2md.py:96`/`:127`; `gen.py:18-28` and `:484-492` (exactly nine `add_argument` calls, none
  named `--template`); `studio/CLAUDE.md:12-14`; migration
  `20260724132049_cms_data_model.sql` present.
- `jobsimulation.md`: `jobsimulation/terraform/main.tf` at `6092c6d2`/`origin/main` — `service_desired_count`
  **absent** (rc 1), `:15-22` the M810 decommission comment enumerating exactly what was destroyed,
  `:24-40` the "what this module still owns" block (livekit/chime/ssm + the atlas tracker), `:38-40` the
  legacy-schema step; the four 2026-08-04 commits `6092c6d2`, `caf36c96` (PR #439), `1e40d184`,
  `82cb66ec`; `cmd/root.go:77`/`:78` + `EXPOSE 8080` in both Dockerfiles + in-repo `CLAUDE.md:88`
  (`go run . serve`) and `:124-125`; `hibiken/asynq v0.26.0`; `09631fb2` subject; migrations
  `20260402145459` / `20260409131539`; `internal/simulator/manager/interview_report_csv{,_test}.go`;
  platform `05b4035` (READONLY_DB_CONNECTION); `2adcf71` compose `:83`/`:93-94`/`:113`/`:119`/`:140`,
  `Makefile:10` `PROFILE ?= graphql`, `repos.yml:17-19`; `0dab54d` compose `:88-89`/`:90`/`:91` (the
  `$HOME/.aws/credentials` bind, the only one in the file); `app/main.go:1204`;
  `internal/jobsimulation/ai/ai.go:90`; `jobsimwiring/wiring.go:147-148` **at the ref the sentence pins
  (`9d00a313`)**, where it is `if err != nil {` / `return nil, err` (at the `b948604` checkout the same
  construct is `:142-143`); `app/main.go:670` @ `9d00a313` and `:614` @ `b948604`, both the
  `log.Fatalf("jobsim-in-app: engine wiring failed …")`; the sole `spf13/cobra` Go import is
  `cmd/createTaxonomy/main.go`; `internal/graph/queries.resolvers.go:70`
  (`JobSimulationResult`, a persisted `GetResultBySession` read, no recompute);
  `app/internal/organization/intelligence.go:1700` `m.ent.JobSimulationSession.Query()`;
  `internal/runner/runner.go` header *"formerly the standalone 'roadrunner' service"*;
  `calls/livekit.go:131-135` / `:140-144` (the flag read starts one line earlier at `:130`; the flag
  name, the `openai-hosted` endpoint and the agent-name reset are all inside the cited windows).
- `ai-labs.md`: `app` `v1.363.2` at `5ba17044` (tag confirmed); all five migrations present
  (`20260617203555_add_labs_catalog`, `20260529072659_add_lab_session`,
  `20260617120000_add_lab_session_model`, `20260717151144`, `20260626120000_add_org_subscriptions`);
  `internal/labs/{catalog,session,labsapi,adapter}`; `lab_session.go:21-36` (no `PrimaryKeyMixin`,
  `field.String("id")`); `labs_budget.go:26-28` tiers and `:247-251` the five `CapReason`s;
  `spend_reconciler.go:60` `Interval: 30 * time.Second`; `labsapi/client.go:6` default `:7070` and
  `main.go:745` reading `LABS_API_PLATFORM_TOKEN`; `stripe/stripe-go/v74 v74.30.0`;
  `cmd/labs{demo,import,key}` with `labsdemo` on `:8099`; `/credits/{balance,transactions}` present and
  `POST /credits/purchase` removed (`credits/handler.go:12`); `/v1/labs` + `/v1/labs/bodies` behind
  `apiKeyAuthMiddleware(..., "labs:write")` and `GET /v1/labs/:slug/workspace.tar.gz` outside the group;
  `labs.graphqls` / `billing.graphqls` / `organizations.graphqls`; every named test file exists,
  `internal/payments` has none, `TestPricingDerivation` at `cost_test.go:103` and
  `TestPrivacyBackstop_AutoScopesToCallerOrg` at `privacy_backstop_test.go:25`.
- `academy-backend.md`: `0e37771f` = *"Academy backend v1.0 'ground truth' … (#903)"*, 2026-06-05;
  `internal/aiacademy` gone; all 11 `academy_*` Ent schema files and the plural table names in the
  migrations; `academy_path_embedding.go:62` `extensions.vector(1536)`; `certificate.go`;
  `content.go`/`body.go`/`content_import.go`/`embeddings.go`/`asset.go`; `academy.graphqls` +
  `resolver_academy.go`; `content.go:23` `GET /content/catalog.json` and `content_admin.go:35`
  `/content/admin` behind `sharedTokenMiddleware(os.Getenv("ACADEMY_CONTENT_API_TOKEN"))`; the nightly
  `academy_embedding_refresh` @ 03:00; `cmd/academyImport/main.go:239-243` and `:244-246` @ `9d00a313`;
  `cmd/academy-seed` flags `--user-email/--user-id/--fixture/--reset/--dry-run/--list`; every named test
  file present.
- `skiller.md`: `internal/data/ent/schema/{skill,jobrole,skill_embeddings,job_role_embeddings,category,
  specialization}.go`; `internal/rpc/skillerrpc/`; `schemas/skiller_taxonomy.graphqls` (whose `:7` comment
  records `categoryTree`/`fullCategoryTree` as deliberately unported); IVFFLAT indexes at
  `20260615130000_skiller_taxonomy.sql:61`/`:251` with `extensions.vector(1536)` at `:53`;
  `skill_translations`/`job_role_translations`; the two intra-corpus anchors
  `backend.md#skiller-in-app-merge--fact-sheet-v21-quick-change` (heading present at `backend.md:83`) and
  `shared_libraries.md#taxonomy` / `#taxonomy-figures` (both present, the latter as an explicit
  `<a id=…>`).
- `TEMPLATE.md`: pure boilerplate, no factual claim to grade. Read in full; nothing to book.

---

## What I could not settle

1. **`graphql-wundergraph`'s and `jobsimulation`'s GitHub archive state** (B7). Requires a GitHub API
   read (`gh api repos/anthropos-work/<name> --jq .archived`), which no clone can answer. The clone-side
   evidence is only consistent-with, not confirming: `graphql-wundergraph` has no commit after
   `60c229f 2026-07-30`.
2. **`cms.md:196` / `jobsimulation.md:49-50` — "Production terraform still names
   `http://backend.internal.anthropos:8081`."** The only terraform that declares these is
   `messenger/terraform/main.tf:74-84`, and it declares them as **variables** (`var.cms_rpc_address`,
   `var.jobsimulation_rpc_address`, …). The literal lives in `infrastructure`, which the corpus itself
   says has never been in a clone set. Not gradeable here.
3. **`cms.md:19` / `external_services.md:175` — whether `module.cms_euwest1` still exists in
   production.** Both docs explicitly decline to assert it and say why; I agree it is unmeasurable from
   this clone set, and I confirmed the two facts they *do* cite (`cms/terraform/main.tf:39` = 0 at both
   the checkout and cms `origin/main`; `6efa1d5` merged `f38c0c4`, 2026-08-04, deleting
   `.github/workflows/build-production.yml` — the file is still present at the checkout `ca50c817`,
   consistent with the commit being ahead of it).
4. **`ai-labs.md:5` — the existence of the `anthropos-work/AI-Labs` repo**, and **`:16`'s** reference to
   `app`'s `knowledge/plan/releases/06.00-shared-purse/`. The first needs GitHub; the second sits under a
   `knowledge/plan/**` path, which HARD BAR 1 forbids me to open. Everything the doc derives *from* that
   plan directory (no `checkout.session.completed`, no labs↔credits linkage, `/credits/purchase` removed)
   I verified independently against Go source at `origin/main`.
5. **`external_services.md:848-858` — the runtime behaviour of `docker compose ps <retired-service>`**
   (rc 1 "no such service" for `cms`/`jobsimulation`/`storage`/`messenger`/`customerio-sync` at HEAD,
   rc 0 for `storage` at `0dab54d`). The compose *files* support it exactly (`storage` had its own block
   with `profiles: [storage-legacy]` at `0dab54d:134` and no block at HEAD), but the exit codes are a
   docker-daemon measurement I did not take.
6. **`jobsimulation.md:116` — PR `#395` (`feat/remove-chronos-and-realtime`)**. The commit `09631fb2` and
   its subject verify; the PR number needs GitHub.
7. **`ai-labs.md:102` — "Credits has NO GraphQL — REST only."** True of `internal/credits` (no credit
   ledger types in any `*.graphqls`), but `queries.graphqls:98`
   `organizationFeatureCredits` / `mutations.graphqls:190` `setOrganizationFeatureCredits` do exist. Those
   are the Sentinel feature-usage quota surface, a different subsystem the same doc describes at `:91-92`,
   so I read the claim as correct-but-collidable rather than false. Flagged for an adjudicator, not booked.

### Out of scope, but worth routing

While checking `cms.md`/`jobsimulation.md`'s production-terraform claims I read
`stack-demo/messenger/terraform/main.tf` and found **one `service_desired_count`, at `:19`, and its value
is `1`**. The repo root `CLAUDE.md` asserts *"Prod compute for both is **stopped**
(`storage/terraform/main.tf:38`, `messenger/terraform/main.tf:29`, each `service_desired_count = 0`)"* —
wrong line and wrong value for the messenger half, at the checkout `fa47850d`. `CLAUDE.md` is not in my
file set, so I have not booked it; it is the corpus's highest-propagation file and §5 rule 19's corollary
says to check it first.
