# Seat F — reading #19

Corpus under audit: `rosetta` @ `00be1ac`, tree clean (only this iter's untracked output dir).
Ground truth re-derived at open per the ADDENDUM sheet; every clone's ref stated per finding.
Nested-repo enumeration run before any absence claim:

```
stack-demo/{ant-academy, app, app/studio, cms, cms/studio, graphql-wundergraph, jobsimulation,
            messenger, next-web-app, platform, roadrunner, rosetta-extensions, sentinel,
            storage, studio-desk}
```

## Positive controls

| file | `wc -l` | read in full? |
|---|---|---|
| `corpus/services/ant-academy.md` | 448 | yes |
| `corpus/services/studio-desk.md` | 435 | yes |
| `corpus/services/chronos.md` | 245 | yes |
| `corpus/architecture/shared_libraries.md` | 242 | yes |
| `corpus/services/askengine.md` | 124 | yes |
| `corpus/services/skillpath.md` | 112 | yes |
| **total** | **1,606** | |

Also read in full as instructed: `corpus/ops/platform-alignment.md` §5 (lines 249–1402).

---

## Blockers

### B1 — ant-academy's Fonts row names two fonts the app does not load, and omits the one it does

- **Anchor:** `corpus/services/ant-academy.md:255` —
  > `| **Fonts** | DM Sans + Instrument Serif + JetBrains Mono (via `next/font/google`) + Font Awesome Pro **icons self-hosted/vendored in the repo** … |`

- **Why it is false** (ground truth `stack-demo/ant-academy` @ `9c3843cd`, the checkout; the passage
  names no ref so it grades there):
  - `code/app/layout.jsx:1` — `import { Work_Sans, Instrument_Serif } from "next/font/google";`
    **Two** families, and neither is DM Sans or JetBrains Mono. `Work_Sans` — the family that IS
    loaded — is not named anywhere in the corpus row.
  - `code/app/layout.jsx:36-40`, the comment directly above the loader, states the opposite of the
    corpus row explicitly: *"Single Google font, matches apps/web in next-web-app (Work Sans, all
    weights). Display + mono usages fall back to system fonts via the CSS-side font-family stacks in
    academy.css and ucourses/ucourse-engine/styles.css. The **legacy** `--font-dm-sans` /
    `--font-instrument-serif` / `--font-jetbrains-mono` CSS vars are **still aliased** to keep the
    existing `var(--font-…)` chains working."* — i.e. DM Sans and JetBrains Mono survive only as
    **CSS variable names**, not as loaded webfonts.
  - `code/academy.css:1` seconds it: *"Fonts: `next/font/google` injects `--work-sans-font` via
    app/layout.jsx… Display + mono usages fall back to system fonts."*
  - The repo actively fences against the corpus's claim: `code/tests/unit/m1-app-shell.test.js:48`
    asserts *"imports Work Sans via next/font/google (single family, matches next-web-app)"*, and
    `:238`/`:245`/`:360-361` assert the CSS must **not** contain a bare `DM Sans` font-family.
  - Absence established per tree at its own ref, with a positive control in the same pass:
    `git -C stack-demo/ant-academy grep -in "DM_Sans\|JetBrains" HEAD -- code/ mobile/` returns
    **zero** `next/font` imports (the only `DM Sans` hits are an inline `font-family` in
    `code/public/assets/academy-icon.svg:3` and a Chart.js option in
    `ucourses/ucourse-engine/content-widgets/ChartRenderer.jsx`; the only `JetBrains` hits are the
    legacy var name at `layout.jsx:39` and chapter prose). Positive control in the same invocation:
    `Work_Sans` → 2 files.
  - The `code/tools/apply-v3-metadata.mjs`-adjacent NUL/gitignore holes do not apply here:
    `layout.jsx` and `academy.css` were read directly, not inferred from a tree-wide zero.

- **Why I did not book it MINOR:** the row is the reader's only statement of the app's typography,
  it names **two fonts that are not fetched** and **omits the one that is**, and the repo carries a
  test whose whole purpose is to stop somebody re-adding what the corpus says is already there. A
  reader matching the academy's fonts (or debugging why `--font-dm-sans` resolves to Work Sans)
  is sent the wrong way by every clause of it.
- **Confidence:** high

### B2 — "the cockpit no longer sets the `e2e_persona` cookie" is false at HEAD, and the same blockquote contradicts it four lines later

- **Anchor:** `corpus/services/ant-academy.md:321-326` —
  > `⚠ **CORRECTION (v2.3.2, 2026-07-15): the cockpit's per-hero [Academy] link was REMOVED — the
  > cockpit is now login-only** (per user request), so it **no longer sets the `e2e_persona`
  > cookie**. … reaching the demo academy as a member **now** needs the cookie set by other means`

  contradicted, inside the same blockquote, at `corpus/services/ant-academy.md:331-332` —
  > `Zero academy-repo edits: the flags live in the gitignored `code/.env.local`; **the cookie is set
  > by the standalone cockpit panel**.`

- **Why it is false** (ground truth `.agentspace/rosetta-extensions` @ **`6130bfd8`** — the authoring
  copy, per addendum rule 2; the citing block pins no rext ref). The **date-pin does not rescue this
  block**: it asserts currency three times (*"is now login-only"*, *"no longer sets"*, *"now needs …
  by other means"*), which addendum rule 1 / §5 rule 33 says cannot be pinned into silence.

  One half of the correction is right; the load-bearing half is wrong:
  - **Right:** `demo-stack/cockpit.py:1317-1321` — *"LOGIN is the ONLY per-hero CTA (user request,
    2026-07-15). The former secondary [Academy] link (M53 F6) was removed … The academy_url helper +
    the academy-cookie JS seam are **retained for now** … but nothing renders them."*
  - **Wrong:** the cockpit still sets `e2e_persona`, by **two live paths** added after the
    correction was written (M234, v2.5):
    1. **Client-side, from the Content-stories tab.** `demo-stack/cockpit.py:983-987` renders the
       academy content-product CTA as `<a class="btn academy" href="{href}"
       data-academy-persona="member">`; `:1369-1370` emits `_ACADEMY_JS` whenever
       `academy or content_needs_academy`; `_ACADEMY_JS` at `:806-812` does
       `document.cookie = 'e2e_persona=' + encodeURIComponent(persona) + '; path=/; SameSite=Lax'`.
    2. **Server-side, on the `/go` deeplink.** `demo-stack/cockpit.py:1493-1496` sends
       `Set-Cookie: e2e_persona={persona}; Path=/; SameSite=Lax`, with the persona supplied by
       `content_target` at `:325-329` (`return academy_url(base, path), "member"`) for an
       `app_base == "academy"` product — explicitly *"works with JS off."*
  - The precondition for both is met on a stock demo, not hypothetical: the **canonical**
    `stack-seeding/presets/content-manifest.json:451-454` carries the product
    `{"id": "skill-path-new", "name": "Academy", "app_base": "academy"}` with a link-bearing session
    (`"player_result_path": "/courses/ai-foundations"`), so `_content_uses_academy()`
    (`cockpit.py:1211-1216`) is **true** and the seam renders. `demo-stack/tests/test_cockpit.py:2426`
    pins it (`assertIn('data-academy-persona="member"', out)`) and `:2855` pins the `/go`
    `Set-Cookie`.

  So the corpus's own `:331-332` is the accurate sentence and `:322-326` is the stale one — the
  exact *"corrected at one site, left standing at another"* class the addendum warns is 8-of-9 of the
  induced residual. Note the **tooling's own docstrings are stale in the opposite direction**
  (`cockpit.py:40-46`, `:1256-1261`, `:1453-1454`, and the `--academy-base` help at `:1636` all still
  describe a per-hero [Academy] link) — so a reader who checks the tooling's prose rather than
  `:1317-1321` gets a third answer.

- **Why I hesitated:** the block carries a date, and half of what it says is true. I booked BLOCKER
  because (a) the sentence asserts currency, which voids the pin; (b) it is self-contradicted inside
  its own blockquote, which is a finding regardless of which side is right; and (c) the operational
  instruction it gives — *"reaching the demo academy as a member now needs the cookie set by other
  means"* — sends a presenter hunting for a workaround that the shipped cockpit already provides.
- **Confidence:** high (that the two corpus sentences contradict each other); high (that the cockpit
  sets the cookie via the content tab at `6130bfd8`).

---

## Minors

- `corpus/services/ant-academy.md:135` — writes `emptyCatalogView() = { chapters: [], skillPaths: {}, series: [] }` as a literal equality. `code/src/lib/serverTenant.js:115-117` also returns `bundles: PUBLIC_BUNDLES` (a **non-empty** curated array, `code/ucourses/catalog.js:956`) and `catalogVersion`. The "0 cards" conclusion is unaffected; the stated shape is not the returned shape.
- `corpus/services/ant-academy.md:49` — the repo-layout tree annotates `code/tools/` as `# offline-parity CLI`. That directory holds exactly one file, `code/tools/apply-v3-metadata.mjs`; the offline-parity harness is an archived milestone (`knowledge/plan/releases/archive/00.02-new-terrain/m6b-offline-parity-harness/`), not a CLI shipped there.
- `corpus/services/ant-academy.md:258` — *"~26 Playwright e2e spec files (tests/e2e/)"*. Measured **31** (`git ls-files 'code/tests/e2e/*.spec.js'` @ `9c3843cd`). The companion *"1000+ Vitest tests"* is a safe lower bound (~2,830 `it(`/`test(` call sites).
- `corpus/services/ant-academy.md:234` — *"`TopBar` is on `/`, `/chapters/*`, `/latest`, `/bookmarks` and `/my-activity`"*. Omits `/courses` and `/courses/[slug]`, which render the same `AcademyClient` (`code/app/(authed)/courses/page.jsx:92`, `courses/[slug]/page.jsx:219` → `AcademyClient.jsx:1906` `<TopBar`). Strengthens rather than contradicts the bullet's point.
- `corpus/services/ant-academy.md:402` — the "other content-pipeline helpers" list omits `export-path`, a real skill (`ant-academy/.claude/skills/export-path/`, and the one the repo-root `.env.example:12-14` exists to configure). Unlike `:37` this list carries no ellipsis.
- `corpus/services/ant-academy.md:144` — *"`code/public/catalog.json` (~2,667 entries)"*. The tracked file currently holds **2,715** `courses` entries.
- `corpus/services/ant-academy.md:336` vs root `CLAUDE.md:292` — this doc says *"The **app's** env file is `code/.env`"* (correct: the repo's own `.env.example:1` says *"NOT for the React app — that lives in code/.env"*), while `CLAUDE.md:292` says the React app *"reads **only** from `code/.env.local`"*. Next.js loads both, so neither path breaks; the "only" is the over-claim, and it lives in the highest-propagation file in the tree.
- `corpus/services/studio-desk.md:171-200` — the sample `.env` block lists `VITE_WEB_APP_URL` but not `WEB_APP_URL`, which is the variable the backend gate actually reads (`src/index.ts:119`, `res.redirect(webAppUrl as string)`); `.env.example:48` declares both. A reader copying the block gets a redirect to `undefined` for a non-eligible user. The same block writes `FORCE_READ_ONLY=0` where `.env.example:117` writes `false` (both falsy).
- `corpus/services/chronos.md:5` — *"removed … in mid-2026"*; the two commits it then cites are both **2026-04-17** (`platform 045857c`, `jobsimulation 09631fb2`).

---

## Positively cleared (audited zeros)

Every row below states which instrument was used. Per §5 rule 24, RE-DERIVED alone is the weakest
form of clearance; where a set was at stake I enumerated it from source before touching arithmetic.

### `corpus/services/ant-academy.md`

- **ENUMERATED** — `:262-281`, the *complete* `isPublic` matcher. I enumerated all **25** static
  patterns + the 2 `DEV_LOGIN_ENABLED` + the 3 `VISUAL_BYPASS` entries from
  `code/proxy.js:112-188` @ `9c3843cd` and compared to the table group by group: catalog front door,
  public-launch listings, auth/gate, cert verification (incl. `/api/verify/(.*)`), AI proxy, release
  provenance (both `_meta` spellings), crawler files, assets/machine indexes, and both dev-only
  groups. **Exact match, no member missing in either direction.** `VISUAL_BYPASS`'s conjunction
  (`BENCHMARK_VISUAL_BYPASS === "1"` ∧ `NODE_ENV === 'development'`, `proxy.js:62-63`) is stated
  correctly, including the whitelist-not-blacklist claim.
- **ENUMERATED** — `:288` *"`/api/chapters/*` is not in the matcher"*: absent from the enumerated
  list above. `:291` *"There is no `/library/[slug]` route"*: `find code/app -name page.jsx`
  yields 14 routes; the only library page is `code/app/(public)/library/page.jsx`.
- **ENUMERATED** — `:134-135` the catalog chain: `serverTenant.js:145` is byte-for-byte
  `const view = (await getBackendCatalogView(eids)) ?? emptyCatalogView()`; the three failure legs
  and the "no FS-as-published fallback" claim are the file's own docstring (`:119-142`), and the
  quoted *"the cutover is intentional, not reversible-on-error"* is at `serverTenant.js:132` —
  inside the cited `:115-145` window.
- **ENUMERATED** — `:154-155` `draftsEnabled()` = `NODE_ENV === 'development'` ∧ `ACADEMY_SHOW_DRAFTS ∈ {1,true}`
  (`src/lib/draftMode.js`, `isDraftOptIn = /^(1|true)$/i`), whitelisted-not-blacklisted as claimed.
- **ENUMERATED** — `:171-176` the chapter-body chain: `src/lib/serverChapterBody.js`
  `resolveServerChapterBody(slug, locale)` opens `const body = await getBackendChapterBody(slug, locale)`;
  the 404 copy *"You wandered off the trail."* is at `code/app/not-found.jsx:43`.
- **ENUMERATED** — `:194-198` the **five** bring-up patches and their wire lines. Verified against
  rext `6130bfd8` `demo-stack/ant-academy.sh`: `:56` DEVORIGINS, `:66` FSPUB, `:75` FSBODY,
  `:84` FSPUBLIC, `:90` COCKPIT — **all five exact**, and all five patch dirs exist under
  `demo-stack/patches/`. `:216`'s autoverify assert *(f)* is at `stack-verify/live/autoverify.sh:459`.
- **ENUMERATED** — `:228-238` the language switch. `SUPPORTED_LOCALES` is 7 entries at
  `src/i18n/locale.js:10`; `LocaleSwitch` is the 2-way EN↔IT `<Link>` mounted at
  `src/views/public/PublicHeader.jsx:20`; `LanguageSelector` (role="menu" at `:88`,
  `role="menuitemradio"` at `:97`) is mounted at `src/components/TopBar.jsx:76`. Both provenance
  shas check out: `5b05b7d9` (2026-05-05) is the commit that added `LanguageSelector` to
  `TopBar.jsx` (`git log -S'LanguageSelector' -- code/src/components/TopBar.jsx` returns exactly
  it, despite its cover-pipeline subject line), and `e22f3230` (2026-05-18) is
  *"feat(i18n): add locale support for es, fr, de, nl, pt"*.
- **ENUMERATED** — `:63` the write side: `code/app/api/academy/beacon/route.js` exists;
  `upsertChapterProgress`/`setLastActivity` are real documents; and the **plural** table claim is
  right at the source — `app/internal/data/ent/academylastactivity/academylastactivity.go:46`
  `Table = "academy_last_activities"`, with `app/internal/aireadiness/recommendation_signals.go:250`
  selecting `FROM public.academy_chapter_progresses`. `app/cmd/academy-seed` exists.
- **ENUMERATED** — `:26`/`:305`/`:436` "not in `repos.yml`": `stack-demo/platform` @ `0c91421`
  `repos.yml` lists exactly four entries (app, sentinel, next-web-app, studio-desk); ant-academy is
  in none of the ten historical revisions either.
- **ENUMERATED** — `:420` Cosmo: `code/ucourses/ucourse-engine/assistant/agent.js:12-13` pins
  `https://api.openai.com/v1/responses` + `gpt-5.2`, `:32` reads
  `localStorage.getItem('openai_api_key')`, gated by `NEXT_PUBLIC_FEATURE_TRAINING_COACH`
  (`src/lib/featureFlags.js:8`).
- **ENUMERATED** — the tech-stack table's checkable cells: `next ^16.2.6` / `react ^19.2.5` /
  `marked ^17` / `engines.node >=22` (`code/package.json`), `dev` = `next dev --port 3077`,
  `vercel.json` = exactly `{"framework":"nextjs"}`, manifest wired at `code/app/layout.jsx:132`,
  99 vendored files under `code/public/assets/fontawesome/`, Expo `~54.0.33`, and mobile
  `dev:web` → `scripts/dev-server.sh` with `PORT=8555` (`:7`, `:52`).
- **ENUMERATED** — `:34` the service-worker kill-switch (`code/src/components/RegisterServiceWorker.jsx:14`,
  `:43`) and `:252`'s org-gate default (`proxy.js:87-88`, `isOrgGateEnabled = !/^(0|false)$/i.test(raw ?? "")`
  — default ON, fail-closed on typos, exactly as written).

### `corpus/services/studio-desk.md`

- **ENUMERATED** — `:21` the compose facts, at the ref the cell names (`0c91421`):
  `docker-compose.yml:138-140` is `depends_on: / backend: / condition: service_started` — **backend
  alone** — and `:141` is `profiles: [studio-desk, all]`. The historical half also checks: at
  `2adcf71`, `:337-341` is the two-entry `depends_on` including `cms`.
- **ENUMERATED** — `:140` the endpoint: `docker-compose.yml:119` (build arg) and `:135` (runtime
  environment) both carry `http://localhost:8082/graphql/query` @ `0c91421`.
- **ENUMERATED** — `:34` the Vite entry set. `vite.config.ts:28-42` declares exactly the nine prod
  entries the doc lists (`home`, `sim-advanced-builder`, `builder-skill-path`, `simulation-builder`,
  `sim-guided-builder`, `generation`, `catalog`, `academy`, `skills`) plus the
  `isProduction ? {} : {dev-accept}` conditional — **no member added or missing.**
- **ENUMERATED** — `:107` the Skill Path Builder cell, every clause: `src/routes/skillpath.ts` is
  61,080 bytes (~61 KB) and the largest of the four routes (`ai.ts` 5,895, `dev.ts` 4,092,
  `youtube.ts` 4,023); `pingSnapshotCapability` + `directus_versions` are real
  (`skillpath.ts:596-620`); `src/routes/youtube.ts:43` is exactly
  `const apiKey = process.env.YOUTUBE_API_KEY;` with the `_mock: true` fallback at `:45-47`;
  `.env.example:120` is `GCLOUD_SERVICE_ACCOUNT=`; `terraform/main.tf:129` is
  `"name": "GCLOUD_SERVICE_ACCOUNT",`. The *"no code in `src/` reads it"* half is a **measured
  absence**: `git -C stack-demo/studio-desk grep -c GCLOUD_SERVICE_ACCOUNT HEAD -- src/` → 0 with
  positive control `YOUTUBE_API_KEY` → 2 in the same pass; studio-desk has **no nested repos**
  (`find . -name .git -maxdepth 3` → `./.git` only), so the tree-wide zero has no sub-repo hole.
- **ENUMERATED** — `:112` the AI layer: `.env.example:57` `AI_PROVIDER_CHAIN=azure-openai,openai`,
  `.env.example:61` `AI_DEFAULT_TIER=fast`, in-code fallback `'thinking_fast'`
  (`src/services/ai/config.ts:182`); four tiers at `src/services/ai/types.ts:21`; tier models
  `gpt-5.2 / gpt-5-mini / gpt-5-nano` (`config.ts:20-31`) and
  `claude-opus-4-5 / claude-sonnet-4-5 / claude-haiku-4-5` (`config.ts:34-38`).
- **ENUMERATED** — `:279-287` the no-datastore claim, tested as the **conjunction** it is: zero DB
  drivers in `package.json` (`pg|postgres|prisma|sqlite|mysql|mongo|knex|drizzle|typeorm|sequelize`
  → no match), `app/services/studioDB.js` present as a facade, and the prefs round-trip real
  (`app/services/graphql/queries.ts:205`, `mutations.ts:58`, consumed by
  `app/services/userPreferencesService.js:1-2`). The Tailscale clause holds: a whole-tree
  `git grep -in tailscale HEAD` returns only `app/core/main.ts:105` (the funnel comment) and `:107`
  (the GlitchTip DSN).
- **ENUMERATED** — `:305` the access gate: `src/index.ts:96`
  `const STUDIO_ACCESS_ROLES = ['admin', 'org:admin', 'content_creator', 'org:content_creator'];`,
  `checkEnterpriseAndAdmin` at `:99`, the redirect target `process.env.WEB_APP_URL` at `:119`.
- **ENUMERATED** — `:313-320` the prod-eject: exactly **three** hardcoded `https://app.anthropos.work`
  sites, in exactly the two named files (`app/core/scaffold/pageWrapper.js:149`,
  `app/core/scaffold/userProfile.js:148` and `:302`).
- **ENUMERATED** — `:388-393` the boot ladder anchors: `app/core/main.ts:97` `preloadCriticalCSS();`
  and `:206` `new PageWrapper();`.
- **ENUMERATED** — `:346`/`:366-367` the rext side @ `6130bfd8`: `frontend_lines()` at
  `stack-injection/gen_injected_override.py:379` emits the existence-guarded
  `env_file: ["<abs>/studio-desk/.env"]` at `:449`, and both named regression tests exist
  (`stack-injection/tests/test_injection.py:1657`, `:1714`). `stack-verify/e2e/run-studio-fcp.sh`
  exists; the five studio/academy patch dirs exist. `:109`'s Directus wiring is real
  (`dev-stack/dev-setdress.sh:310` `ADMIN_TOKEN=local-directus-token-…`, image pinned
  `directus/directus:11.6.1` at `gen_injected_override.py:325`).
- **ENUMERATED** — `:132` *"the Cosmo Router survives in production only"*:
  `stack-demo/graphql-wundergraph` @ `60c229f3` still declares `service_desired_count = 1`
  (`terraform/main.tf:20`), while the repo is absent from every current `repos.yml` and compose.
- **ENUMERATED** — `:161` Node v24+ (`package.json:6-8` `">=24"`, `Dockerfile:1` and
  `Dockerfile.dev:1` `FROM node:24-alpine`), `:172` in-code `PORT` fallback 9100
  (`src/index.ts:60`), `:120` seven languages (`app/public/l12n/` = de/en/es/fr/it/ja/nl),
  `:231` Jest two projects (`jest.config.js:26-47`, `displayName: 'backend'` / `'frontend'`),
  and the whole scripts block (`test`, `test:e2e`, `test:e2e:headed`, `type-check`, `lint`, `check`).
- **ENUMERATED** — `:419-422` the Clerkenstein FAPI route: `/v1/me/organization_memberships` is
  served (rext `clerkenstein/clerk-frontend/meorgmemberships_test.go`), and studio's
  `canAccess()` (`app/services/userService.ts:208`) does call
  `this.clerk?.user?.getOrganizationMemberships()`.

### `corpus/services/chronos.md`

- **ENUMERATED** — the decommission provenance, both commits, both trees: platform `045857c`
  (2026-04-17, *"chore: remove chronos service from orchestration…"*) touches exactly
  `docker-compose.yml` (−37) and `repos.yml` (−4); jobsimulation `09631fb2` (2026-04-17) has the
  verbatim subject the doc quotes. `chronos` is absent from `repos.yml` at every ref from
  `045857c` onward, and its go-repo membership in the pre-`045857c` revisions is confirmed.
- **ENUMERATED** — `:9`'s forward pointer: `app/internal/jobsimwiring/worker.go` @ `b948604f` — the
  `github.com/hibiken/asynq` import is at `:24` and `asynq.NewServer` at `:35`, so the cited
  `:24-35` window brackets the Asynq worker construction inside `backend` as claimed.

### `corpus/architecture/shared_libraries.md`

- **ENUMERATED** — every `go.mod` pin in the colony / proto / ai / taxonomy rows, read at the exact
  ref each cell names: `app/go.mod:16` colony `v0.35.2`, `:18` proto `v1.210.0`, `:20` taxonomy
  `v1.2.0`, `:14` ai `v1.40.2` @ `b948604f`; `sentinel/go.mod:8` `v0.34.3`, `:9` `v1.200.0`,
  `:21` `v1.2.0 // indirect` @ `88bc5592`; storage `v0.34.3`/`v1.196.0`/`:25 // indirect` @
  `4ce8ece5`; messenger `v0.35.2`/`v1.210.0`/`:9` direct @ `fa47850d`; cms `:9` ai `v1.40.2`,
  colony `v0.35.1`, `:11` proto `v1.207.0`, `:13` taxonomy @ `ca50c817`; jobsimulation `:11`/`:13`/`:15`
  @ `462343b0`; roadrunner `go.mod:7` colony `v0.34.3`, `:8` proto `v1.196.0`, and **no** ai/taxonomy.
  **All exact.** The taxonomy row's *"6 of 7"* is a re-derived **set**, not a re-derived sum:
  the seven on-disk Go repos are app · sentinel · storage · messenger · cms · jobsimulation ·
  roadrunner, and roadrunner is the sole non-importer.
- **ENUMERATED** — `:181`'s history claim, the one most likely to be hand-assembled. I derived the
  `type: go` set from **every** revision of `repos.yml` (10 commits), not from the current file:
  `a2a3ee6` **11** {app, chronos, cms, intelligence, jobsimulation, messenger, roadrunner, sentinel,
  skiller, skillpath, storage} → `fdfa189` 10 (intelligence out) → `045857c` 9 (chronos) →
  `21429b7` 8 (skiller) → `a4db680` 7 (skillpath) → `d11a403` 4 → `838d907` 2. The union **is**
  11, all four extra names **are** in the first revision, and **skillpath is indeed the last of the
  four to go, dropped by `a4db680`** — every clause of a four-clause sentence.
- **ENUMERATED** — `:70`'s handler count, from source rather than from the doc's own list. Every
  `mux.Handle` in `app/main.go` @ `b948604f`: `:1187` Users, `:1188` Organizations, `:1196` Skiller,
  `:1204` JobSimulation, `:1213` CMS (inside `if cmsRPCServer != nil` at `:1212-1214`), `:1228`
  LabSession. **Exactly six, exactly five unconditional**, and no seventh anywhere in the file.
- **ENUMERATED** — `:79`'s two absences, each at the ref its own sentence names, with positive
  controls: `ROADRUNNER_RPC_ADDR` is **0** in `app` @ `9d00a313` (control `JUDGE0_BASE_URL` → 3
  files in the same pass), **0** in the nested `stack-demo/app/studio` @ `aeec036` (control
  `import` → hits), and **0** in `platform` @ `0dab54d` (control `JUDGE0` → 1). The parenthetical
  *"this line long cited `docker-compose.yml:118`… that line sets `AWS_REGION`"* is correct **at the
  ref the sentence pins**: `0dab54d:docker-compose.yml:118` is `- AWS_REGION=eu-west-1`.
  `internal/jobsimwiring/wiring.go:123` @ `9d00a313` is exactly
  `runnerManager := jsrunner.NewRunnerManager(getenv("JUDGE0_API_KEY"), getenv("JUDGE0_BASE_URL"))`,
  and `internal/skillpaths/skillpaths.go:27-31` @ `b948604f` is exactly the
  *"drop-in for the removed skillpath RPC client"* comment + `sessionReader`.
- **ENUMERATED** — `:202-203` the taxonomy figures, re-derived from the manifest rather than from the
  table: `.agentspace/snapshots/taxonomy/5afc0bcc…/manifest.json` gives `public.skills` **42,790**,
  `public.job_roles` **22,470**, `public.job_role_embeddings` **18,919**, each with
  `filter: organization_id IS NULL`, `source: primary-read`, `public_only: true`,
  `predicate: org-null`, `captured_at: 2026-06-29T14:43:00Z`. **Every figure and every provenance
  field exact**, including the *"18K came from job_role_embeddings"* diagnosis.
- **DERIVED (stronger than RE-DERIVED)** — `:224-229` the NodeID format. Rather than re-read the
  regex I recomputed the three worked examples: `SHA1("GO")[:4]` = **F63F**, `SHA1("DOT NET")[:4]`
  = **DDE9**, `SHA1("C SHARP")[:4]` = **5F5B** — matching `K-GOXXXX-F63F`, `K-DOTNET-DDE9`,
  `K-CSHAXX-5F5B` exactly, and confirming the stated `.`→`DOT` / `#`→`SHARP` sanitisation and the
  3-chars-per-word + X-pad WORDPART rule simultaneously.
- **ENUMERATED** — the colony package table + the two "Notable" claims, against
  `~/go/pkg/mod/github.com/anthropos-work/colony@v0.34.3` (sentinel's live pin; the row is unpinned
  and colony is not cloned — see *"could not settle"* for the version caveat): `authn`,
  `authorization` (**Go package name really is `authorizer`**), `pubsub`, `redis`, `rpc`, `flags`
  all present; `NewDBStdConn`/`NewDBPool`/`InitLogger`/`NewGQLHandlerServer`/`NewHTTPServer`
  (serving `/_meta` at `http.go:20`)/`NewCORSHandler`/`NewVersionConfig` all present; DB health
  monitor is a **30 s** ticker (`db.go:46`) failing to *"too many ping failures"* (`:62`);
  `NewSentinelAuthorizer` at `authorization/sentinel.go:26`; pubsub has `PoisonQueue` DLQ
  (`server.go:68`) + `MaxRetries: 3` exponential backoff (`:143-148`); `flags/posthog.go:13`
  `pollingInterval = 5 * time.Minute` against `https://eu.i.posthog.com` (`:39`); `rpc/server.go:24`
  `h2c.NewHandler`; `redis/redis.go:12,32` UniversalClient + startup `Ping`. Sentry gating is
  `logging.go:47` `if !conf.Environment.IsProduction() && os.Getenv("FORCE_SENTRY") == ""` — exactly
  the stated OR. The rate limiter is `rate.NewLimiter(1, 5)` (`graphql.go:239`) with **both** reject
  paths commented out (`:254-255`, `:265-266`) — *"only logs"* is precisely right.
- **ENUMERATED** — the `ai` interface and constructors, against `ai@v1.40.1`: `ai.go:8-18` declares
  exactly the nine methods listed (`ChatCompletion`, `ChatCompletionStream`, `Response`,
  `CreateEmbeddings`, `CreateSpeech`, `OCRProcess`, `AudioTranscriptions`, `Tokenize`,
  `GetEndpoint`) — no tenth, none missing. `openai.New`/`NewOpenAI`/`NewAzure`
  (`openai/completion.go:25,29,48`), `AzureDefaultAPIVersion = "2025-04-01-preview"` (`:43`),
  `anthropic.NewAnthropic(cfg *aws.Config, key *string)` (`anthropic/completion.go:45`) with EU
  `eu.anthropic.*` model IDs (`:21-24`), `mistral.NewMistral` with **4** `panic`s. The asymmetry
  claim is exact: Anthropic's `ChatCompletionStream` (`:177`) and `CreateSpeech` (`:186`) **panic**
  while `CreateEmbeddings` (`:181`) returns an error — the doc names the right two. JSON prefill
  `{"` at `:106`/`:143`. Retry `Attempts(10)` + `BackOffDelay`, never on `401/403/404` or
  `context.Canceled`/`DeadlineExceeded` (`retry.go:14-40`).
- **ENUMERATED** — the two `ai` corrections at `:118-128`: cost lives in
  `app/internal/aiusage/ai_usage.go` (its `AiUsageHandler` consumes `eventsv1.Event_AiUsage` at
  `:98`), and vendor selection lives in the consumer wrapper — `flag_use_azure_us` appears in
  `app/internal/jobsimulation/ai/ai.go:267,344` and `app/internal/skillerai/ai.go:347`, **not** in
  the `ai` library. The cross-reference `external_services.md:579` **resolves to the right
  construct**: that line is *"There is no ordered EU-first fallback chain."* (I checked this
  because a wrong-line citation is one of the three named residual classes; five other corpus sites
  and root `CLAUDE.md:269`/`:283` cite the same `:579`, so the family is uniform.)
- **ENUMERATED** — `:145` the authn importer counts: `git grep -l "colony/authn" -- '*.go'` gives
  **6** files @ `cms ca50c817` and **8** @ `jobsimulation 462343b0` — exact. `:168`'s
  *"Sentinel does not import authn"* is a measured absence with a control: 0 hits for
  `colony/authn|anthropos-work/authn` @ `88bc5592`, control `anthropos-work/colony` → 2 files.
  `app` imports `colony/authn` in 129 files.
- **ENUMERATED** — `:23-31` the consumption model: none of the five appear in any `repos.yml`
  revision; `stack-dev/` contains no `colony`/`proto`/`ai`/`authn`/`taxonomy` directory;
  `docker-compose.yml:12` and `:39` pass `GH_ACCESS_TOKEN: $GH_PAT`; `app/Dockerfile.dev:11` is the
  `url.…insteadOf` rewrite and `:13` `ENV GOPRIVATE=github.com/anthropos-work/*`; **no `go.work` is
  tracked in any of the seven Go repos.**
- **ENUMERATED** — `:94`'s example: `storage/internal/migration/migration.go:13` @ `4ce8ece5` is
  `legacyStorage "github.com/anthropos-work/proto/go/simulator/storage/v1"`.
- **ENUMERATED** — `:191-193`: `app/cmd/createTaxonomy/main.go` exists, `app/internal/taxonomy/`
  (the distinct service-local package `:231-232` warns about) exists.

### `corpus/services/askengine.md`

- **ENUMERATED** — `:47-48` the registry, re-derived as a set: `internal/askengine/registry.go` has
  **57** `Schema: "public"` + **3** `Schema: "directus"` = **60**. The cited `:183-195` is *exactly*
  the transition-alias comment block (`// ---- Jobsimulation (folded into public — jobsim-in-app
  M709) ----` through `// job_simulation_sessions.`), i.e. the anchor names the right construct, not
  merely an existing line.
- **ENUMERATED** — `:64-67` the route list, derived from the **registrations** rather than the
  handler docstrings. `internal/web/backend/backend.go:170` `askRouter := e.Group("/ask", cors…, authn…)`
  then `:174-188` register `/stream`, `/conversations` (GET+POST), `/conversations/:id`,
  `/conversations/:id/stream`, `/conversations/:id` (DELETE), `/messages/:messageId/feedback`,
  `/admin/auto-rules/distill`, `/admin/auto-rules`, `/admin/auto-rules/:id/activate`,
  `/admin/auto-rules/:id`. The corpus's `/ask/admin/auto-rules/…` is **correct**; it is the
  *source comments* in `internal/web/backend/ask/admin.go:77,102,129,173` that write the segments
  the other way round (`/admin/ask/…`). Reading only the comment would have produced a false
  finding — §5 rule 10 in action.
- **ENUMERATED** — `:68` `maxAgenticIterations = 15` (`handler.go:34`), `loopTimeout = 10 * time.Minute`
  (`:41`), `context.WithoutCancel` (`:38`), `streamRegistry` (`:57`); `:33-35` `COPILOT_DB_CONN`
  (`main.go:313`) + `askEngineMaxConns = 6` (`main.go:150`, applied `:336`) +
  `QueryTimeout = 10 * time.Second` driving `SET LOCAL statement_timeout` (`executor.go:24,97`).
- **ENUMERATED** — `:41` `DefaultModelID = "eu.anthropic.claude-sonnet-4-6"` and
  `DefaultRegion = "eu-west-1"` (`bedrock.go:25-26`), `stripAnthropicAuthMiddleware` (`:185`),
  `ASK_MODEL_ID`/`AWS_REGION` overrides (`:162-163`); `:52` `MaxInlineRows = 200` /
  `MaxCellLength = 400` (`executor.go:18,21`); `:21` `rules.md` = **146,219 bytes** (~146 KB).
- **ENUMERATED** — `:104-109` the four test counts, counted from source: `sandbox_test.go` **49**,
  `executor_test.go` **13**, `prompt_test.go` **10**, `followups_test.go` **14** — all four exact.
- **ENUMERATED** — `:36-38` the five persistence tables map 1:1 to the Ent schemas
  (`askconversation`, `askmessage`, `askqueryexample`, `askquerylesson`, `askautorule`);
  `:19` the three tools are real (`prompt.go:278` `query_postgres`, `:297` `render_chart`,
  `coursebuilder_tool.go:28` `course_builder`); `:43-46` `ValidateSQL` (`sandbox.go:163`),
  `BuildCTEs` (`:227`), `WrapQuery` (`:363`), the three `OrgStrategy` arms (`:257-268`) and the
  `$1`/`$2` binding comment (`:423`).
- **ENUMERATED** — `:116-117` the ship dates: `app` tag commits `3c6bbb4a3` *"chore(version):
  v1.267.0"* dated **2026-05-07** and `4df992893` *"v1.340.0"* dated **2026-07-17** — both exact.

### `corpus/services/skillpath.md`

- **ENUMERATED** — `:30-33` the *"NO `SkillPathSessionService` anywhere"* claim, established per
  tree at each tree's own ref across the **whole** clone set including the two nested
  `anthropos-studio-room` copies: app `b948604f` **2 files, both markdown**
  (`CLAUDE.md:80`, `knowledge/architecture.md:28`) and **0 in `*.go`**; sentinel, storage,
  messenger, cms, jobsimulation, roadrunner, next-web-app, studio-desk, ant-academy,
  graphql-wundergraph, platform, `app/studio` @ `aeec036`, `cms/studio` @ `aeec036` — **0 each**.
  `skillpath…v1connect` → **0** in `app`'s Go. This is the claim the addendum's nested-repo warning
  exists for, so I did not rely on a host-ref `git grep`.
- **ENUMERATED** — `:34-36` the platform-doc lag, at the ref the parenthetical names: at `app`
  origin/main **`2035f9a`** the service is listed at `CLAUDE.md:109` and
  `knowledge/architecture.md:28` — and the note's own self-warning checks out too, since at the
  checkout `b948604f` the CLAUDE.md line is `:80`.
- **ENUMERATED** — `:14` / `:59` the in-process cms read: `app/internal/skillpath/session.go:205-207`
  is exactly the `// cms-in-app deseam: cms is in-process` comment + the
  `u.cms.GetSkillPathDomain(...)` call, and `u.cms` really is typed
  `contentread.CmsContentReader` (`session.go:50`, interface at
  `internal/cms/contentread/reader.go:37`) — so the corpus names the right type, not just the right
  line. `app/internal/skillpaths/skillpaths.go:88-95` is the twin block. **Both anchors exact.**
- **ENUMERATED** — `:77-79` the manager reader: `internal/organization/intelligence.go:1144` is
  `func (m IntelligenceManager) InsightsSkillPathByMemberships(...)`, and `:1159-1170` is exactly
  the `m.ent.SkillPathSession.Query()` block filtered by `SkillPathID`,
  `StatusIn(Active, Completed)`, `HasUserWith(user.IDIn(...))` and the
  `TenantIDIsNil() ∨ TenantID(org)` tenant predicate — every conjunct the sentence claims.
- **ENUMERATED** — `:84-89` the retraction, including its own self-correction:
  `app/terraform/migrations/20260729133514.sql:62` is `DROP TABLE "local_jobsimulation_sessions";`
  and `:63` is `DROP TABLE "local_skill_path_sessions";`; **no** `local_*` Ent schema exists among
  the 139 files in `internal/data/ent/schema/`; and **exactly three** migrations post-date it at
  `9d00a313` — `20260731131307.sql`, `20260731154527_academy_chapter_progress_completed_at.sql`,
  `20260803143844_ai_readiness_recommendation_path.sql` (`atlas.sum` is not one).
- **ENUMERATED** — `:47` the repos.yml counts, all three at their named refs: `2adcf71` → **9**,
  `0dab54d` → **6**, `838d907` → **4** entries. `:49` `SKILLPATH_STREAM=skillpath` survives at
  `docker-compose.yml:72` @ `0c91421`. `:59`'s *"No compose file sets any `*_RPC_ADDR` now"* is a
  measured absence with a control: 0 hits for `_RPC_ADDR` in `docker-compose.yml` at HEAD, control
  `ADDRESS` → 1 (`AUTHORIZATION_ADDRESS`).
- **ENUMERATED** — `:97-104` the unimplemented drill-down, graded at the ref the sentence names
  (next-web-app **origin/main** `8297c684`):
  `apps/web/src/components/containers/InsightsBySkillPathStudentSimulationsContainer.tsx:31-34` is
  `const userData = useMemo(() => { /* return insightData?.rows[0]?.membership; */ return null as
  unknown as MembershipEnriched; }, [])`; the results `<Table>` (`:140-149`) and the totals block
  (`:150-160`) are both commented out; the body renders
  `t('enterprise.insights.comingSoon')`, which `configs/i18n/messages/en/enterprise.json:1891`
  resolves to **"Coming soon"**. (Strictly it is a translation key, not a literal — non-material in
  EN, and I am not booking it.)
- **ENUMERATED** — `:22-26` the code locations: `app/internal/skillpath/{session.go,
  session_domain.go, repository/}`, `app/internal/skillpaths/`,
  `internal/data/ent/schema/{skill_path_session.go, skillpath_mixins.go}`, and
  `internal/web/backend/graphql/graph/schemas/skillpath_sessions.graphqls` — all present.

---

## What I could not settle

- **The four un-cloned shared libraries.** `proto`, `taxonomy` and standalone `authn` are pulled as
  private Go modules and exist in neither the clone set nor the local module cache
  (`~/go/pkg/mod/github.com/anthropos-work/` holds only `ai@v1.40.1` and `colony@v0.34.3`). So
  `shared_libraries.md`'s `:68` (buf CI pins `v1.57.0`), `:77-83` (**"12 Connect-RPC services are
  defined"** — the list is internally consistent at 12, but I could not enumerate the predicate from
  `proto` itself), `:91-94` (`buf.yaml` single-module layout, `go/simulator/*` ≈10 generated
  packages with no source `.proto`), `:143` (authn tag `v1.7.0`), `:179-180` (taxonomy `go 1.21.0`,
  zero external deps) and `:188` (**"131-line"** `node.go`) are **unverified, not cleared**.
  What would settle them: `GOPRIVATE=… go mod download github.com/anthropos-work/{proto,taxonomy,authn}`,
  or a read-only clone of each into the ground-truth set.
- **Version skew on the two libraries I could read.** The colony and `ai` package-inventory
  clearances above were taken at `colony@v0.34.3` and `ai@v1.40.1`; `app` pins `colony v0.35.2` and
  `ai v1.40.2`. The corpus rows are unpinned, so strictly they grade at what `app` builds, which I
  could not read. Everything I checked matched, but a behaviour introduced or removed in
  `v0.35.x`/`v1.40.2` would be invisible to this pass.
- **`chronos.md:13-245`** — the entire historical body (schema DDL, `ScheduleTimer`/
  `CancelScheduledTimer` shapes, the triggerer's `LIMIT 10` + `FOR UPDATE SKIP LOCKED` + 3-second
  run timeout + 1-minute Sentry heartbeat, ports 8080/8081, the env-var and CLI-flag tables). The
  `chronos` repo is not cloned anywhere on this box (`stack-dev/` holds only `studio-desk`,
  `studio-room` and a handoff file). Only the decommission header was gradeable. A clone of
  `anthropos-work/chronos` at any ref would settle all of it.
- **`chronos.md:3`** — *"the GitHub repo is NOT archived"*. Needs a `gh api repos/anthropos-work/chronos`
  call; I made no network requests.
- **Live measurements quoted from prior milestones**, which I can neither reproduce nor refute
  without a running stack: `ant-academy.md:74` (*"a grid of 65 real cards with 0 Draft chips"*),
  `:189-190` (the 404→200 proof on `billion`), `:203` (*"0 course cards on `/library` over an HTTP
  200"*), `studio-desk.md:412` (*"p95 4669 ms → 817 ms"*, 5/5 cold loads) and `:423`
  (*"canAccess 4049 → 38 ms, FCP 6936 → 2152 ms"*). The harnesses named (`run-studio-fcp.sh`,
  `autoverify.sh` assert *(f)*) all exist; only the numbers are unreproduced.
- **`ant-academy.md:63`'s *"there is NO localStorage/IDB source-of-truth"*.** I confirmed the write
  path (beacon route + the two mutations + the Ent tables) but did not exhaustively rule out a
  second client-side source of truth; the repo carries `src/lib/migratePersistence.js` and a
  `fake-indexeddb` dev dependency, which is consistent with a *migrated-away-from* IDB but which I
  did not read end-to-end. A read of `src/progress/store.js` + `useProgressSync.js` would settle it.
- **`studio-desk.md:21`'s *"Asking for `studio-desk` as the only profile exits 1"*.** Structurally
  entailed (the profile selects `studio-desk`, whose `depends_on: backend` is `profiles: [core,
  backend, all]`), and consistent with `CLAUDE.md`'s profile table, but I did not run
  `docker compose --profile studio-desk config` to observe the exit code — §5 rule 30 says grade a
  documented command on what it *selects*, and I graded this one on structure only.
