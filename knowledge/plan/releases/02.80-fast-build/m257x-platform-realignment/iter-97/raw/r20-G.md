# Seat G — reading #20 (adversarial diff seat)

## Positive controls

**Diff measured myself:** `git diff b7e6642..HEAD -- corpus/ CLAUDE.md .claude/`
→ **23 files changed, 230 insertions(+), 79 deletions(-)**
→ **48 hunks** at `-U6` (53 at the default `-U3`). I read the `-U6` rendering top-to-bottom in full
(1,099 lines) and opened the surrounding passage in the *current* file for every hunk.

| file | wc -l (at HEAD) | read in full? |
|---|---|---|
| CLAUDE.md | 578 | yes (hunk regions + §Architecture 180–300 + profile table) |
| corpus/architecture/ai_architecture.md | 305 | yes |
| corpus/architecture/alignment_testing.md | 542 | yes (hunk + §Where things live 508–535 + all `rosetta` mentions) |
| corpus/architecture/architecture_overview.md | 413 | yes (hunks + 280–305) |
| corpus/architecture/dependency_map.md | 103 | yes |
| corpus/architecture/external_services.md | 880 | yes (hunks + 130–145, 240–320, 540–640, 690–700) |
| corpus/architecture/platform-migration-status.md | 307 | yes |
| corpus/architecture/security_compliance.md | 281 | yes |
| corpus/architecture/service_taxonomy.md | 484 | yes (hunks + 120–150, 200–215, 315–332) |
| corpus/architecture/shared_libraries.md | 242 | yes (hunks + 180–220) |
| corpus/ops/demo/stories-spec.md | 840 | yes (hunk + 595–610) |
| corpus/ops/platform-alignment.md | 2178 | yes (hunks + 196–225, 1030–1050, 1218–1262, 1330–1355) |
| corpus/ops/safety.md | 1077 | yes (hunks + 195–300) |
| corpus/ops/seeding-spec.md | 620 | yes (hunk + 88–110) |
| corpus/services/README.md | 83 | yes |
| corpus/services/backend.md | 318 | yes (hunks + 25–45, 95–160, 240–270) |
| corpus/services/cms.md | 311 | yes (hunks + 88–100, 205–230) |
| corpus/services/jobsimulation.md | 309 | yes (hunks + 10–35, 85–100) |
| corpus/services/messenger.md | 189 | yes |
| corpus/services/roadrunner.md | 200 | yes |
| corpus/services/skiller.md | 69 | yes |
| corpus/services/storage.md | 232 | yes (hunks + 15–90, 170–220) |
| corpus/services/studio-room.md | 506 | yes (hunks + 78–95, 340–390) |

**Ground truth re-derived at open** (matches the ADDENDUM exactly): platform `0c91421`, app `b948604ff`
(origin/main `2035f9a4`), **app/studio `aeec036`**, **cms/studio `aeec036`**, next-web-app `bb3313bc0`,
storage `4ce8ece` (origin/main `9f8cb532`), messenger `fa47850` (origin/main `e9421c68`), ant-academy
`9c3843cd`, jobsimulation `462343b0` (origin/main `82cb66ec`), cms `ca50c81` (origin/main `f38c0c4a`),
sentinel `88bc559`, studio-desk `14a5442`, roadrunner `87d8d44`, graphql-wundergraph `60c229f`,
rext authoring `6130bfd`. Corpus at `00be1ac`, tree clean.

**Absence method (ADDENDUM):** every tree enumerated with
`find stack-demo -name .git -maxdepth 4`, each grepped **at its own ref**; nested `app/studio` and
`cms/studio` grepped separately; positive control run in the same pass on every absence claim.

---

## Blockers

### B1 — `external_services.md` corrected its `:543` anchor in two *other* files and left the in-file self-citation pointing at the Schema-Updates section

- **Anchor:** `corpus/architecture/external_services.md:614` —
  *"off it there (`` `:543` `` above; 0 hits for `bedrock|boto3` under `app/studio/`; corrected M257x iter-48)."*
- **Why it is false:** The repair inserted **+25 net lines** into this file above line 614, moving the
  **Anthropic Direct (first-party API)** provider row from `:543` to `:567`. It then updated *every
  cross-file* citation to that row — `ai_architecture.md:51`, `ai_architecture.md:87`,
  `security_compliance.md:218` all went `543 → 567` in this same commit. The **in-file** reference at
  `:614` was not touched. Measured now: `external_services.md:543` is
  `"When backend services add new GraphQL types or operations:"` — the *GraphQL Schema Updates*
  section (`sed -n '540,556p'`), which has nothing to do with Studio-Room, Bedrock, or
  `ANTHROPIC_API_KEY`. At `b7e6642` the same text at old-line 589 read `` `:543` `` and **was
  correct** (`git show b7e6642:…| sed -n '543p'` → the Anthropic Direct row). So this anchor was true
  before the repair and is false after it — induced squarely by the repair.
- **Confidence:** high

### B2 — the repair refuted "app produces the `skiller` stream" at four sites and left it standing in `backend.md`'s own top banner, 100 lines above one of its own corrections

- **Anchor:** `corpus/services/backend.md:33-34` — *"**`app` owns the `skiller`, `skillpath`,
  `jobsimulation`, `cms` and `ai_usage` Redis Streams** — both producer and consumer are in-process."*
- **Why it is false:** This commit's central new finding is the opposite. It writes, in four places:
  `CLAUDE.md:280` (*"**`skiller` does not:** … nothing publishes to it"*),
  `dependency_map.md:59` (*"`app` runs a live subscriber on a stream nothing publishes to"*),
  `backend.md:136` (*"only as a **consumer**: nothing in `app` publishes to it"*), and
  `backend.md:264` (*"**`skiller` is the exception: `app` only SUBSCRIBES to it**"*).
  Measured independently at both app refs:
  `git -C stack-demo/app grep -n 'NewPublisher' b948604f -- '*.go'` → 6 sites
  (`internal/deadletterqueue/dead_letter_queue.go:38`, `internal/jobsimwiring/wiring.go:127`, `:180`,
  `main.go:287`, `:637`, `:1039`); **none names `SKILLER_STREAM`**. `git grep -n 'SKILLER_STREAM'
  b948604f -- '*.go'` → exactly one hit, `main.go:1276`, an `AddSubscriber`. Same predicate at
  `2035f9a4`: publishers at `main.go:325`, `:746`, `:1149`, `wiring.go:132`, `:185` — still none.
  So `backend.md:33` is false against source **and** self-contradicts `backend.md:136` and
  `backend.md:264` in the same document. This is the dominant failure class the brief names, and it
  survives inside the very file the repair edited twice.
- **Confidence:** high

### B3 — the new `skiller` stream row in `dependency_map.md` names TWO refs and then anchors; every anchor resolves at only one of them, and its cardinality claim is false at the other

- **Anchor:** `corpus/architecture/dependency_map.md:59` — *"Enumerated over every publisher
  constructor in `app` @ `b948604f` **and** origin/main `2035f9a4`: `backend` (`main.go:287`),
  `SKILLPATH_STREAM` (`:637`), `CMS_STREAM` (`:1039`), `AI_USAGE_STREAM` + `JOBSIMULATION_STREAM`
  (`internal/jobsimwiring/wiring.go:127`, `:180`) — `SKILLER_STREAM` is not among them; **its one Go
  occurrence**, `main.go:1276`, is an `AddSubscriber` (handler: `internal/roles/roles.go:791`)."*
- **Why it is false:** Resolved every anchor at both named refs. At `2035f9a4` they land on unrelated
  constructs: `main.go:287` = `logger.Info("subsystem switches",`; `:637` = a bare `)`;
  `:1039` = `serverContext,`; `:1276` = `apiKeyManager,`; `wiring.go:127` =
  `asynqClient := jsworkerclient.NewClient(...)`; `:180` = blank. They resolve **only** at
  `b948604f`. Worse, the cardinality is ref-specific and the sentence claims both refs:
  `git -C stack-demo/app grep -n 'SKILLER_STREAM' 2035f9a4 -- '*.go'` returns **six** hits
  (`main.go:1532`, `main.go:1537` — the live `Skiller: os.Getenv("SKILLER_STREAM")` field —
  `subscriber_merge_test.go:907`, `:1015`, `:1038`, `subscriber_wiring.go:165`), so *"its one Go
  occurrence"* is false at the second ref this very cell names. The corpus states the prohibition
  twice in this same commit — `platform-migration-status.md`'s `storage` row and `storage.md`'s
  consumer cell both say *"naming a second ref inside this cell makes every anchor in it ungradeable
  (the citation resolver reads one ref per block and reports `ambiguous` … M257x run-53)"* — and
  `backend.md:264` carries the identical enumeration **correctly pinned to `b948604f` alone**. The
  repair broke the rule at exactly one of the two sites.
- **Confidence:** high

### B4 — `CLAUDE.md`'s rewritten Communication-Patterns bullet now pins `b948604f`, at which both of its neighbouring storage claims are FALSE

- **Anchor:** `CLAUDE.md:280` — *"**backend → storage is no longer mid-fold either** —
  `STORAGE_RPC_ADDR` is set nowhere *and* **read nowhere**; `app` serves object storage in-process and
  says so at **`app/main.go:504`**. … **`skiller` does not:** `app` subscribes to it
  (`app/main.go:1276` **@ `b948604f`**) …"*
- **Why it is false:** The repair added the explicit `@ b948604f` pin to this bullet. Graded at that
  ref — the only ref the block names, and the corpus's own rule — both storage claims fail:
  `git -C stack-demo/app grep -n STORAGE_RPC_ADDR b948604f -- '*.go'` returns **15 hits, 7 of them
  live `os.Getenv`/`getenv` reads** (the corpus states this itself, in
  `platform-migration-status.md`'s `storage` row), so *"read nowhere"* is false there; and
  `git show b948604f:main.go | sed -n '504p'` is
  `// jobsim-in-app (M709): app's OWN users + organizations RPC handlers …` — a jobsim comment, not
  the storage note. Both are true only at `2035f9a4`, where `:504` reads *"the standalone service
  takes no traffic and STORAGE_RPC_ADDR is gone."* The bullet therefore names two effective refs and
  is ungradeable — the exact hazard `storage.md` warns about in this same commit (*"**State the ref
  or the sentence flips**"*, *"a ref-less command contradicts this sentence on the clone a reader
  actually has"*). The repair imported that hazard into the corpus's index of truth.
- **Confidence:** high

### B5 — `platform-alignment.md` mechanically shifted a broken anchor instead of re-deriving it; `backend.md:187` is a directory listing inside a code block

- **Anchor:** `corpus/ops/platform-alignment.md:1345` — *"The refuting citation iter-21 trusted was
  `corpus/services/backend.md:187` — a corpus line asserting messenger points *all four* addresses at
  `backend:8083`."*
- **Why it is false:** `sed -n '187p' corpus/services/backend.md` →
  `  authorization/            Sentinel client` — a line of the *Key directories* code block. No
  address, no messenger, no `8083`. The repair changed this from `:175` to `:187`, i.e. it added the
  file's +12-line growth to a number that was **already wrong**: `git show
  b7e6642:corpus/services/backend.md | sed -n '175p'` is the *same* `authorization/ Sentinel client`
  line. So a broken anchor was re-published as freshly re-derived. The actual construct described
  lives at `backend.md:104-112` / `:241`. The irony is on the page: six lines later this same passage
  says *"Anchors are cheap to verify and they were all fine."*
- **Confidence:** high

### B6 — the new "S3 PRIVATE bucket — ⚠️ unclassified" rows contradict the guard registry they claim to project; `s3-private` is still `PerStackIsolated` in code

- **Anchor:** `corpus/ops/safety.md:203` — *"| **S3 PRIVATE bucket** | ⚠️ **unclassified — the guard
  does not cover it** |"*, and `corpus/ops/safety.md:207` — *"**`S3-private` was in this row and has
  been REMOVED** — it is not per-stack-isolated at platform `0c91421`"*. Same pair at
  `corpus/ops/seeding-spec.md:98` / `:102`.
- **Why it is false:** The table's own preamble (`safety.md:196-198`) says *"Every store a stack might
  touch **is classified** (`isolation.IsolationClass`) … per-stack stores are listed for
  documentation + dry-run preview"* — i.e. the table is a projection of the code registry. Measured
  in the authoring copy (`6130bfd8`, the ref the ADDENDUM says governs rext citations):
  `stack-seeding/isolation/isolation.go:106` still reads
  `{Name: "s3-private", Class: PerStackIsolated, Note: "S3 private bucket; falls back to local /tmp on demo"}`.
  The store **is** classified, and it **is** classified `PerStackIsolated`. What is true — and what the
  repair actually measured — is that `PreflightEnv` forces only the public bucket
  (`isolation/audit.go:146`, verified: `fixed["STORAGE_S3_PUBLIC_BUCKET"] = ""` is the whole forced
  set). "The guard does not cover it" is correct; **"unclassified"** and **"has been REMOVED … it is
  not per-stack-isolated"** are not — they describe a code change that did not happen, and a reader
  running the documented dry-run preview (`isolation.Stores()`) will see `s3-private
  PerStackIsolated` and conclude the safety contract is stale in the other direction. The underlying
  hazard (prod private bucket on `backend`, `docker-compose.yml:82`, `$HOME/.aws/credentials` mounted
  at `:100`, `README.md:81-87`) is fully corroborated — this blocker is about the *classification*
  claim only. I hesitated because the safety consequence is described correctly; I book it BLOCKER
  because a security-contract table that disagrees with the registry it projects is exactly the
  drift that table exists to prevent.
- **Confidence:** medium-high

### B7 — newly authored rule 44 states a measurement that is wrong by three orders of magnitude

- **Anchor:** `corpus/ops/platform-alignment.md:1236` — *"`next-web-app/apps/web/src/hooks/useCoursebuilder.ts`
  **(50,433 bytes, 1,178 NULs)**"*
- **Why it is false:** Measured three ways on both the working tree and the git blob at
  `bb3313bc`: byte count **50,433** ✔, NUL count **1** (not 1,178).
  `python3 -c "d=open(p,'rb').read(); print(len(d), d.count(b'\x00'))"` → `50433 1`;
  `tr -dc '\000' < … | wc -c` → `1`. The file has **1,179 lines** — the quoted figure is a
  line/newline count relabelled as a NUL count. The *predicate* the passage rests on survives
  (`file(1)` reports `data`, and both `grep -I` and `git grep` skip it — verified), so the rule is
  sound and only its number is wrong; but it is a fresh measurement in a passage whose whole subject
  is instruments that silently mis-count, and the wrong figure has already been copied outward.
- **Confidence:** high (measurement), low materiality — stated so the seat is not read as
  over-weighting it.

### B8 — rule 44's prescribed per-tree loop is mislabelled and case-sensitive; run on the rule's own worked predicate it returns 2, not 22

- **Anchor:** `corpus/ops/platform-alignment.md:1254-1258` —
  `printf '%-34s hits=%-4s ref=%s\n' "$d" "$(git -C "$d" grep -c "$TERM" HEAD 2>/dev/null | wc -l | tr -d ' ')" …`
- **Why it is false:** `git grep -c` emits one line **per matching file** (`HEAD:path:count`), so
  `| wc -l` counts *files*, not hits — yet the field is printed as `hits=`. And the command drops
  `-i`, which the prose three lines above uses (`git -C app/studio grep -i mistral aeec036a` → 22).
  I ran the snippet verbatim with `TERM=mistral`:
  `stack-demo/app/studio  hits=2  ref=aeec036`. The doc's own worked example, three lines earlier,
  says the honest instrument returns **22** and calls the low answer *"a false clearance [that] minted
  an 'imported nowhere' claim that stood in four documents"*. A reader who follows the prescribed
  remediation reproduces a smaller version of the very failure the rule exists to prevent. (Line 1260
  compounds it: the `check-ignore` step sits *outside* the `for` loop, so `$d` is whatever the loop
  left behind.) I hesitated — it is a shell snippet, not a factual claim — but it is prescriptive,
  it is new in this commit, and it disagrees with the number the same rule publishes.
- **Confidence:** medium

---

## Minors

- `corpus/services/jobsimulation.md:25-27` — the three inserted lines lost the `>` blockquote prefix
  mid-banner. CommonMark lazy continuation keeps them inside the quote, so it still renders; the
  formatting is inconsistent with every other line of the block.
- `corpus/ops/platform-alignment.md:209` — *"Measured 2026-08-01: `jobsimulation` and `skillpath` were
  archived **2026-07-31**"* survives unfenced, while the repaired
  `platform-migration-status.md:89` explicitly redirects the reader to *this file* (*"census signal 6,
  [`platform-alignment.md`]"*) for archive state. Defensible as a **dated** snapshot under the
  repair's own new rule, which is why it is a minor and not a blocker — but the pointer lands a
  reader on the unqualified form of the claim just retracted.
- `corpus/ops/platform-alignment.md:1260` — `git -C "$d" ls-files | git -C "$d" check-ignore …` is
  outside the loop; `$d` is unbound on a fresh shell.
- `corpus/architecture/architecture_overview.md:297-299` still describes Mistral in `app` as the Go
  cms OCR client alone. Not false (it does not claim exclusivity in the Python tree), but it is the
  one page in the Mistral family the repair did not extend with the `tools/pdf2md.py` finding that
  five neighbours now carry.
- `corpus/services/studio-room.md:346-355` — the illustrative `requirements.txt` block lists
  `mistralai` third; the real file has it at line 8 (the doc cites `requirements.txt:8` correctly
  elsewhere).
- `corpus/services/storage.md:212` — the `Compose value` cell still reads `(empty)` for
  `STORAGE_S3_BUCKET`. The HISTORICAL preamble at `:203-206` and the new Description text both
  disambiguate it, but the cell alone reads as a live value.

---

## Positively cleared (audited zeros)

**ENUMERATED** (predicate re-derived from source independently, cardinalities compared):

1. **`SKILLER_STREAM` has no publisher in `app`** — enumerated *all* `NewPublisher` call sites at
   `b948604f` (6) and `2035f9a4` (7, incl. one comment), and all `Event_SkillerCustomJobRoleCreated`
   references (handler only). Zero publishers either ref. The substantive claim behind B2/B3/B4 is
   **correct**; only its anchoring is not.
2. **`MESSENGER_RPC_ADDR` appears in no repo** — grepped **every** tree at its own ref including the
   two nested `studio` repos: 0 hits everywhere. (Positive control `RPC_ADDR` in the same pass: app 68
   files, cms 10, jobsimulation 10, messenger 2, rext 26.) The bare-`MESSENGER_RPC` hits in `app` are
   two lines of `knowledge/plan/roadmap-support-in-app.md` quoting the string as a search term.
3. **`git log -S` counts in `messenger.md` / `services/README.md`** — `platform` at `0c91421d` has
   **121** commits; `-S 'MESSENGER_RPC'` → **0**; `-S 'SKILLER_RPC'` → **7**; with `--all` → **0 and
   8**, the 8th being `464dfe3`, which `git branch -a --contains` places on `origin/feat/cms-in-app`
   only. Every figure in the repaired sentence reproduces exactly.
4. **The `mistralai` finding** — `git -C stack-demo/app/studio grep -in mistral aeec036a` → **22 hits
   / 3 files** (requirements.txt 1, tools/pdf2md.py 18, tools/r3.py 3). `tools/pdf2md.py:24` =
   `from mistralai import Mistral`; `:96` = `client = Mistral(api_key=api_key)`; `:127` =
   `model="mistral-ocr-latest"`; `r3.py:194` = the `MISTRAL_API_KEY` gate; `requirements.txt:8` =
   `mistralai`. `app/.gitignore:79` = `studio/*`; `cms/.gitignore:129` = `studio/*`;
   `app/studio/.gitignore:2` = `tools/`. Three instruments reproduce **1 / 0 / 22** exactly.
   `gen.py` imports only console/format/agents/services.ai/postgen; `studioManager.go:119` execs
   `studio/gen.py` only; `app/Dockerfile:45-46` copies `studio/` and pip-installs. **All correct.**
5. **Rule 44's "12 non-empty gitignored-but-tracked text files"** — ran
   `git ls-files | git check-ignore --stdin --no-index` per tree: 13 paths total (ant-academy 2,
   app 2, app/studio 4, cms/studio 4, roadrunner 1), of which `roadrunner/cmd/.DS_Store` is binary →
   **12 text files**. Correct. `app/studio` = **177** tracked files, HEAD `aeec036`. Correct.
6. **The pre-drop gateway `depends_on`** — `2adcf71^1` = `1e8e754`; `docker-compose.yml:19-27` names
   **four** services (`backend`, `jobsimulation`, `cms`, `storage`), each `condition:
   service_started`. `a4db680^` additionally named `skillpath`; `21429b7^` additionally named
   `skiller`. `git merge-base --is-ancestor 464dfe3 0c91421d` exits **1**, and `git branch -a
   --contains` returns `origin/feat/cms-in-app` alone. Every clause of the new hunk verified.
7. **The Directus two-route claim** — `git grep DIRECTUS 0c91421d -- docker-compose.yml common.yml`
   → exactly one hit, `docker-compose.yml:53 DIRECTUS_PUBLIC_BASE_ADDR`, inside the environment block
   that runs `:46-94`; `.env_example:91-92` = `DIRECTUS_BASE_ADDR` + `DIRECTUS_TOKEN` and nothing
   else. Correct in both directions.
8. **`backend.internal.anthropos:8081`** — `local.project = "backend"` (`app/terraform/locals.tf:6`
   @ `b948604f`), `local.rpc_port = 8081` (`locals.tf:8`, mapped `main.tf:185-186`),
   `service_discovery_namespace_id` passed at `main.tf:58`, and the literal attested at
   `app/knowledge/service-dependencies.md:45-46` (*"reaches the users, cms, jobsimulation and skiller
   RPC surfaces at `http://backend.internal.anthropos:8081`"*). Every cloned service module declares
   its `*_rpc_addr` with **no default** — `messenger/terraform/variables.tf:77,82,87,92` @
   `fa47850d`, `app/terraform/variables.tf:197,230` @ `b948604f` — all four verified line by line.
   **Sweep for survivors:** the only remaining `backend:8081` in the corpus is
   `backend.md:116`, which is the sentence explicitly *about* the unqualified form. Clean sweep.
9. **The M903 / storage-terraform correction** — `storage/terraform/main.tf` @ `9f8cb532` is **18
   lines**, `:9-11` the GONE comment, `:13-16` the `prevent_destroy` reasoning, `:18` = *"See
   outputs.tf — consumers should reference these by output, never by literal name."* (**not** a
   custody clause). `git grep -i custody 9f8cb532` → **0**. `git grep M903 9f8cb532` → exactly one
   hit, `terraform/storage.tf:22`, and lines 22-25 are the quoted supersession verbatim. No `moved`
   blocks. `d3e6d32` (2026-08-05) body says *"M903 never ran."* At the checked-out `4ce8ece5` the
   file is **100 lines** and still declares `module "storage"`. Every clause verified.
10. **go.mod pins** — app colony `:16` v0.35.2 / proto `:18` v1.210.0 / taxonomy `:20` v1.2.0 @
    `b948604f`; sentinel colony `:8` v0.34.3 / proto `:9` v1.200.0 / taxonomy `:21` v1.2.0 `// indirect`
    @ `88bc5592`; storage `:7`/`:8`/`:25 // indirect` @ `4ce8ece5`; messenger `:7`/`:8`/`:9` @
    `fa47850d`. `repos.yml` @ `0c91421d` has exactly **4** entries (app `:14-17`, sentinel `:18-20`,
    next-web-app `:23-25`, studio-desk `:26-28`) with the header at `:2-10`/`:12-13`. Every number in
    the three rewritten `shared_libraries.md` rows and the `dependency_map.md` taxonomy row is right.
11. **The alignment-framework relocation** — `alignment/go.mod` @ `6130bfd8` = `module
    anthropos.dev/alignment`; rosetta tracks **4** executable files outside `stack-*`/`.agentspace`,
    none alignment-related; `.claude/skills/align-{dna,run}/SKILL.md` exist. The new paragraph agrees
    with the pre-existing *Where things live* section at `:508-535` — no contradiction introduced.
12. **jobsimulation / cms archive + M810 evidence** — jobsimulation `origin/main` carries exactly four
    2026-08-04 commits (`6092c6d2`, `caf36c96` = *Merge pull request #439*, committer `GitHub`,
    `1e40d184`, `82cb66ec` v0.254.0); `jobsimulation/terraform/main.tf` @ `82cb66ec` is 56 lines with
    the M810 comment at `:15-22` and the survives-because block at `:24-40`. cms `6efa1d5`
    (2026-08-04) deletes `.github/workflows/build-production.yml` (18 lines) with the quoted subject
    and body; `cms/terraform/main.tf:39` = `service_desired_count = 0` in a **191-line** module.
    Every figure checks.
13. **Compose anchors** — `docker-compose.yml` @ `0c91421`: `:48` AUTHORIZATION_ADDRESS, `:53`
    DIRECTUS_PUBLIC_BASE_ADDR, `:59` JUDGE0_BASE_URL, `:71` SKILLER_STREAM, `:73-79` the MUST-be-set
    comment, `:82`/`:83` the two production buckets, `:100` the `.aws/credentials` mount, `:102-103`
    the depends_on note. `README.md:81-87` @ `0c91421d` is the AWS-credentials instruction. All exact.
14. **The storage hazard's app anchors** @ `2035f9a4` — `main.go:516,517` bucket reads, `:518-523`
    the empty-bucket `log.Fatalf`, `:524,525` the two managers, `:529-535` `verifyBucketAccess`,
    `:547` `resource.NewManager`, `:1102` `cmsStorage`, `:504` the STORAGE_RPC_ADDR comment;
    `internal/storage/storage.go:193-200` `getKeyPath` branching on `s3Bucket != ""`;
    `env_guards.go:37-44` `deployedEnvironment()` returning false for `development`. All exact, and
    `storage.md` correctly declares that every `app` path in the cell resolves at `2035f9a` only.
15. **Mistral Go-side anchors at the checkout** — `markdownManager.go:19` @ `b948604f` =
    `mistral.NewMistral(nil, os.Getenv("MISTRAL_API_KEY"))` (matches the unpinned citations in
    `external_services.md:565` and `ai_architecture.md:34`), and `:10`/`:30` @ `2035f9a4` =
    `mistralocr` import / `mistralocr.New(aiKey)` (matches `cms.md`'s explicitly-pinned citation).
    `studioManager.go:531` and `:583` resolve at both. `services/ai.py:705-708` is the three-entry
    provider dict; `ai.py:1-2` imports openai/anthropic only. All correct.
16. **Inbound-citation drift sweep** — script-enumerated **every** `<file>.md:<line>` citation in
    `corpus/`, `CLAUDE.md` and `.claude/` whose target is one of the 23 changed files, and resolved
    each against the file at HEAD. All the re-pointed `external_services.md` citations (`:579` ×6,
    `:567` ×3, `:602-607` ×2, `:619-629`) land on the intended constructs; `seeding-spec.md:496-498
    → :497-499` is correct (+1 shift); `storage.md:25` and `storage.md:40,47` still resolve. **The
    only two failures found are B1 (in-file, missed) and B5 (`backend.md:187`).**

**RE-DERIVED only** (weakest clearance — recorded as such): the `d11a403`/`838d907`/`0dab54d` compose
commit ordering, and the "5 declared services / 7 effective" counts, which I checked against the
mainline `git log … -- docker-compose.yml` replay rather than re-parsing every intermediate compose file.

---

## What I could not settle

- **Whether `jobsimulation` is archived on GitHub.** The repair's *"report both, assert neither"* is
  the correct disposition from a clone — archive state lives only in the org API. Settled by
  `gh api repos/anthropos-work/jobsimulation --jq .archived`, which I have no network path to. The
  same question is open, untouched, for `skiller` (claimed 2026-07-01), `skillpath` (2026-07-31) and
  `graphql-wundergraph` (2026-07-30); of those, only `graphql-wundergraph` is in the clone set, and
  its last commit is `60c229f` **2026-07-30**, i.e. the same day as its claimed archive — consistent,
  not contradictory, so I did not book it.
- **Whether the `infrastructure` repo has applied M810 for cms.** Correctly declared UNMEASURABLE by
  the corpus; `infrastructure` has never been in a clone set. Settled only by reading that repo.
- **The literal value of `skiller_rpc_addr` in production.** Attested in prose at
  `app/knowledge/service-dependencies.md:46` and derivable from `locals.tf`, but the assignment
  itself lives in the un-cloned `infrastructure` root module — which `backend.md:118` says, while
  `dependency_map.md:27` and `skiller.md:19` state the literal flatly without that caveat. I did not
  book this: the value is corroborated, and the caveat is one click away.
- **Whether `platform_predicate_guard.py` / `platform_alignment_guard.py` currently pass** against
  the repaired corpus. Running the rext guards was outside this seat's read-only remit.
