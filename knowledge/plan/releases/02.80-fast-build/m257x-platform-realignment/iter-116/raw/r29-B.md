# Seat B — reading #29/#30 (M257x iter-116)

## Refs I graded against (stated at the moment of measurement, §5 rule 26)

Re-derived at this reading's open, `2026-08-07`, and identical to the addendum's superseded table:

| tree | HEAD | origin/main |
|---|---|---|
| `stack-demo/app` | `ad9f3c49` | `ad9f3c49` (level) |
| `stack-demo/app/studio` (nested, own checkout) | `aeec036a` | `aeec036a` |
| `stack-demo/cms` | `ca50c817` | `f38c0c4a` (behind) |
| `stack-demo/cms/studio` (nested, own checkout) | `aeec036a` | `aeec036a` |
| `stack-demo/platform` | `0c91421d` | `0c91421d` (level) |
| `stack-demo/next-web-app` | `8297c684` | `f97ba659` (behind) |
| `stack-demo/sentinel` | `f2c46190` | `f2c46190` |
| `stack-demo/storage` | `4ce8ece5` | `9f8cb532` (behind) |
| `stack-demo/messenger` | `fa47850d` | `e9421c68` (behind) |
| `stack-demo/jobsimulation` | `462343b0` | `82cb66ec` (behind) |
| `stack-demo/graphql-wundergraph` | `60c229f3` | `60c229f3` |
| `stack-demo/roadrunner` | `87d8d443` | `87d8d443` |
| `stack-demo/studio-desk` | `41ee3575` | `41ee3575` |
| `stack-demo/ant-academy` | `22df69dd` | `22df69dd` |
| `stack-demo/rosetta-extensions` (**pinned per-stack — the tree that settles "what the tooling does on a stack"**) | `09d06070` | `4cb920aa` |
| `.agentspace/rosetta-extensions` (authoring copy — used for nothing in this report) | `1dc1eb82` | — |

Every `rosetta-extensions` claim in this report was settled **against the pinned per-stack clone
`stack-demo/rosetta-extensions` @ `09d06070`** (addendum's override of frozen line 37 / §5 rule 45). I ran no
guard, so no fence-tree disclosure applies (§5 rule 50).

Nested repos enumerated before any tree-wide claim (§5 rule 44):
`find stack-demo -name .git -maxdepth 4` → **15** trees (13 top-level + `app/studio` + `cms/studio`).
Every `docx` / `mistral` / `MESSENGER_RPC` count below names the tree it covers.

I fetched nothing (§5 rule 41a — no lane may fetch while a reading is in flight).

## Positive controls

| file | `wc -l` | read in full? |
|---|---|---|
| `corpus/services/ai-readiness.md` | 712 | yes |
| `corpus/services/cms.md` | 336 | yes |
| `corpus/services/chronos.md` | 245 | yes |
| `corpus/services/academy-backend.md` | 160 | yes |
| `corpus/services/README.md` | 90 | yes |

(Search-pipeline positive controls run in the same passes: `git -C stack-demo/platform log -S 'SKILLER_RPC'
0c91421d` returned **7** against `MESSENGER_RPC`'s **0**; `STORAGE_RPC` returned 35/6/10/35 across four trees
against `MESSENGER_RPC_ADDR`'s 0 everywhere; `git -C stack-demo/app/studio grep -ci mistral aeec036a` returned
**22 lines / 3 files**, which is the §5 rule 44 worked example reproducing exactly.)

---

## Blockers

### B1 — `cms.md`'s python-docx retraction over-reaches into a false tree-wide absence, and `tools/` refutes it again

- **Anchor:** `corpus/services/cms.md:153-154` — *"(The only `.docx` in the tree is a filename in an authoring
  guideline and an asset-example README — no dependency.)"*
- **Why it is false:** measured at the nested repo's **own** ref (`stack-demo/app/studio` @ `aeec036a`; identical
  at `stack-demo/cms/studio` @ `aeec036a`), `git grep -cil docx` returns **5 files, not 2**:

  | file | occurrences |
  |---|---|
  | `agents/simulation/guidelines.py` | 1 (the authoring guideline the doc names) |
  | `knowledge/development/asset-examples/README.md` | 1 (the asset-example README the doc names) |
  | `knowledge/.../scenario-simulation/internal_localization.json` | 7 |
  | `knowledge/.../scenario-simulation/simulation.json` | 3 |
  | **`tools/any2pdf.py`** | **42** |

  And the *"no dependency"* half is the load-bearing error. `tools/any2pdf.py` **declares, imports and installs
  python-docx**: `:38` `'docx': 'python-docx',  # For reading DOCX files` inside its dependency-check dict;
  `:82` `import docx`; `:85` `subprocess.check_call([sys.executable, "-m", "pip", "install", "python-docx"])`
  on `ImportError`; `:95` `doc = docx.Document(input_path)`. Its module docstring (`:5`) says it converts
  *"DOCX, TXT, MD, CSV, RTF, HTML, images"*.

  It is not dead code, and this document already knows that: **`any2pdf.py` is step 1 of the very `tools/r3.py`
  chain `cms.md:229` credits** — `tools/r3.py:139` is
  `scripts = ["any2pdf.py", "pdf2md.py", "md2cleanMd.py"]` inside `check_dependencies()`, and the doc cites
  `r3.py:139` five lines later as evidence for the *mistral* half.

  The correct statement — *"`python-docx` is not in `requirements.txt`"* — is true and I confirmed it
  (`requirements.txt` is verbatim 9 packages: openai · anthropic · rich · pyyaml · requests · jinja2 ·
  mistralai · pytest · pytest-asyncio, byte-identical in both studio checkouts). The parenthetical generalises
  it to the whole tree and to "no dependency", and both are refuted by the same `tools/` directory whose
  invisibility this document's own mistral bullet was written to correct.
- **Why I hesitated:** the sentence sits inside a code-fence annotation and the primary claim around it is
  correct. I booked it because it is a **tree-wide absence claim** stated in the corpus's own most-fenced idiom
  ("the only X in the tree"), it is measurably false by an independent enumeration, and it would tell a reader
  auditing the image's Python supply chain that python-docx is nowhere — while a shipped CLI pip-installs it at
  runtime.
- **Confidence:** high

### B2 — the iter-115 repair's three same-file pins were invalidated by that same commit, and the passage documenting pin-rot now carries three fresh rotted pins

- **Anchor:** `corpus/services/ai-readiness.md:56-58` — *"…re-derived at iter-115, `:496` is the **closing line
  of the `> **✅ CORRECTED M219 …**` blockquote** (`:476-496`) … **The block opens at `:498` and the quoted
  parenthetical is at `:500`.** **The pin is deleted rather than re-derived to `:498`**"*
- **Why it is false:** measured at rosetta HEAD in the file itself:

  | claim | stated | measured |
  |---|---|---|
  | `✅ CORRECTED M219` blockquote | `:476-496` | opens **`:512`**, closes **`:536`** |
  | `⚠⚠ M51 iter-08/09` block opens | `:498` | **`:538`** |
  | the quoted parenthetical `(now aireadiness/readiness.go, formerly workforce/ai_readiness.go:512)` | `:500` | **`:540`** |

  At HEAD, `:476` is prose about the progress table, `:496` is prose about the M51 strategy, `:498` is
  *"⚠️ **That premise was refuted at M219…"*, and `:500` is the `stories-spec.md:599` cross-reference.

  The cause is measurable and is the point: at `9d31cf1^` **all three were exact**
  (`git show 9d31cf1^:corpus/services/ai-readiness.md` puts the blockquote at `476`, the block at `498`, the
  parenthetical at `500`). Commit `9d31cf1` (*"fix(M257x/115): repair ai-readiness … the third-generation pin is
  RETIRED, not re-derived"*) is `47 insertions(+), 7 deletions(-)` on this one file, and every inserted line is
  in the header callout **above** the anchors — a net **+40** shift that exactly accounts for `498 → 538`.
  So the repair whose entire subject is *"a same-file line pin in a document that grows will rot again"*
  published three pins that its own diff rotted, in the same commit. This is §5 rule 34 (*line numbers move when
  YOU edit too*) failing inside the passage that teaches it — a fourth generation, not a third.
- **Why I hesitated:** the passage explicitly retires the *navigational* pin and tells the reader to search the
  construct, so a reader following the instruction is unharmed. I booked it because the three numbers are
  stated as present-tense measurements ("`:496` **is** the closing line", "The block **opens** at `:498`"),
  they are the sole evidence offered for the retirement decision, and a reader who checks the reasoning finds
  none of it holds.
- **Confidence:** high

### B3 — `cms.md`'s self-citation to the "Data bullet" names the Domain bullet

- **Anchor:** `corpus/services/cms.md:216` — *"legacy `cms` schema is non-authoritative. Consistent with the
  **Data** bullet, :44-47 above)"*
- **Why it is false:** in the same file, `:44` is `> Where everything went:`, `:45` is `>`, and `:46-47` are the
  **Domain** bullet (`app/internal/cms/` … `wiring.go`) — which says nothing about a schema. The **Data**
  bullet (the `similarities` / `studio_documents` / *"re-created in the `public` schema"* one this sentence is
  asserting consistency with) is `:48-51`.
- **Confidence:** high

### B4 — `cms.md`'s self-citation to the "Studio bullet" names the Events bullet

- **Anchor:** `corpus/services/cms.md:240` — *"see the **Studio** bullet, :70-71 in the banner at the top of
  this doc."*
- **Why it is false:** `:70-71` are the **Events** bullet's third and fourth lines (*"…Directus webhooks land on
  `POST /api/webhook/directus`, which now **fails closed** without `DIRECTUS_WEBHOOK_SECRET`"*). The **Studio**
  bullet — the one that says CI pulls `anthropos-studio-room` into the `app` image via `additional_repo`, which
  is the claim `:240` is sending the reader to — is `:75-76`.
- **Confidence:** high

### B5 — `CLAUDE.md` says the frontend monorepo is Next.js 15; it is 16, and my assigned index says 16

- **Anchor:** `CLAUDE.md:275` — *"**Frontend Applications**: Next.js 15 monorepo on Vercel (`next-web-app`…)"*
- **Why it is false:** measured at `next-web-app` `8297c684`, `"next": "~16.2.12"` in **all five** package.json
  that pin it — `apps/web:46`, `apps/hiring:45`, `apps/integration:28`, `apps/maintenance:9`,
  `packages/ui:65`. My assigned `corpus/services/README.md:55` states *"The Next.js **16** monorepo on Vercel"*
  and is correct; `CLAUDE.md` also contradicts **itself** at `:265` and `:490`, which both say Next.js 16 (for
  ant-academy, measured `"next": "^16.2.6"` at `22df69dd:code/package.json:64` — also 16).
- **Why I hesitated / disclosure:** the anchor is **outside this reading's declared scope**
  (`corpus/services/**` + `corpus/architecture/**`). I booked it anyway because it is a direct contradiction of
  a claim in my assigned file, it is measurable, and `CLAUDE.md` is the highest-propagation site in the tree
  (§5 rule 19's corollary). Exclude it from the in-scope count if the coordinator scopes strictly.
- **Confidence:** high

---

## Minors

- `corpus/services/cms.md:60` and `:201` — the literal `http://backend.internal.anthropos:8081` is at
  `app/knowledge/service-dependencies.md:**53**` @ `ad9f3c49`, not `:52`. `:52` is the preceding line of the
  same wrapped sentence (*"reach the users, cms, jobsimulation and skiller surfaces at"*), so the block is
  right and the line is one short. Both sites carry the same off-by-one.
- `corpus/services/ai-readiness.md:639` — `how_we_measure.go:253-261` begins **inside the previous function**:
  `:253` is `return out, rows.Err()` and `:254` the closing `}` of `computePerformanceBySkill`.
  `computeCycleTotals`'s doc-comment is `:256-259` and its signature `:260`. The range does contain the named
  construct, so it resolves; it just starts three lines early.
- `corpus/services/ai-readiness.md:653` — `export interface AIReadinessCycleTotals` is `:323-**331**` at
  `next-web-app` `8297c684`, not `:323-330` (`:331` is the closing `}`). The load-bearing part — `:326` =
  `interviewQuestions: number;` — is exact.
- `corpus/services/ai-readiness.md:316-317` — *"`aireadiness.Manager` … is constructed with `db`, `ent`,
  `orgRepo`, `ai.AI`, a pub `EventPublisher`, the `academy.EmbeddingsManager`, and the `WorkforceDirectory`"*.
  `NewManager` (`manager.go:135`) takes `logger, db, aiClient, entClient, pub, academyEmbeddings, directory` —
  `orgRepo` (and `assignmentRepo`) are **derived inside** from `entClient`, and `logger` is a parameter the
  list omits. Seven items listed, seven parameters, but not the same seven.
- `corpus/services/cms.md:78-79` — *"in an otherwise-whole 191-line module"*: **191 is the file's** line count
  (`cms/terraform/main.tf`, identical at `ca50c817` and `f38c0c4a`); the `module "cms"` block is a subset of it.
- `corpus/services/chronos.md:5` — *"in mid-2026"*: both cited commits are **2026-04-17**
  (platform `045857c`, jobsimulation `09631fb2`).
- `corpus/services/chronos.md:22`, `:171`, `:190` — `stack-dev/chronos` does not exist on this box
  (`stack-dev/` holds only `HANDOFF-ant-mini.md`, `studio-desk`, `studio-room`), which is exactly what the
  doc's own banner predicts (not in `repos.yml`, not cloned by `make init`). The paths are aspirational rather
  than false; noting so a repair does not "fix" them into something worse.
- `corpus/services/README.md:13` — *"carry a merge banner **at the top**"*: `storage.md`'s banner is at `:7`,
  under a `## Role & Responsibility` heading, rather than in the first three lines like the other six. Cosmetic.

---

## Positively cleared (audited zeros)

**ENUMERATED** — predicate re-derived independently from source, cardinality compared:

1. **`corpus/services/README.md:31-32` — "5 services declared, 7 effective, `repos.yml` carries 4."**
   `git -C stack-demo/platform show 0c91421d:docker-compose.yml` declares exactly `sentinel`, `backend`,
   `studio-desk`, `next-web-app`, `gotenberg` = **5**; `common.yml` adds `postgresql` + `redis` = **7**;
   `repos.yml` = `app`, `sentinel`, `next-web-app`, `studio-desk` = **4**.
2. **`README.md:11` — the seven folded domains.** `git -C stack-demo/app ls-tree ad9f3c49 internal/<d>/` is
   non-empty for **all seven** (`skiller`, `skillpath`, `jobsimulation`, `cms`, `storage`, `messenger`,
   `customeriosync`).
3. **`README.md:39` — "`app/internal/roadrunner/` exists at no ref; no path in the whole tree matches
   `roadrunner`."** `git ls-tree -r --name-only ad9f3c49 | grep -ci roadrunner` = **0**. Judge0 execution is
   `internal/jobsimulation/runner/{runner.go,languages.go}`, wired at `jobsimwiring/wiring.go:123`
   (`runnerManager := jsrunner.NewRunnerManager(getenv("JUDGE0_API_KEY"), getenv("JUDGE0_BASE_URL"))`) with the
   import alias at `:34` — the anchor names the construct.
4. **`README.md:46` — `MESSENGER_RPC_ADDR` appears in no repo.** Enumerated across **all 15 git trees at each
   tree's own HEAD**, nested repos included: `MESSENGER_RPC_ADDR` = **0 everywhere**. (The looser
   `MESSENGER_RPC` returns 2, both in `app/knowledge/plan/roadmap-support-in-app.md`, and neither is the
   variable.) Positive control in the same pass: `STORAGE_RPC` = 35/6/10/35 across four trees. Platform history
   control: `log -S 'MESSENGER_RPC' 0c91421d` = **0** vs `-S 'SKILLER_RPC'` = **7**.
5. **`README.md:46` — `messenger/internal/flow/flow.go:72-104` @ `fa47850`, `AddSubscriber("backend", …)`,
   21 live handlers.** `:72` is the `AddSubscriber("backend"` call, `:104` its closing `))`. Counted inside the
   block: **21** live `pubsub.EventHandler(...)` entries plus **one commented out**
   (`OrgJobSimulationAssignmentPastDueHandler`) — the count excludes what does not compile (§5 rule 24).
6. **`README.md:3` — "Every service doc in this directory, enumerated."** `corpus/services/` holds **27**
   `.md` besides `README.md`/`TEMPLATE.md`, and the index's four tables list exactly those 27 filenames.
7. **`ai-readiness.md:281`, `:405-410` — "31 readiness skills (19 core @1.0 + 12 enabling @0.5), denominator
   25.0" and "3 default sims".** `internal/aireadiness/defaults.go` @ `ad9f3c49`: `defaultReadinessSkills`
   entries `:31-49` = **19** at `1.0`, `:51-62` = **12** at `0.5` → 31, Σw = **25.0**; `defaultReadinessSims`
   `:76-78` = **3** (`{StepSimulation,"tech"}`, `{StepSimulation,"business"}`, `{StepInterview,"both"}`).
   Arithmetic then re-checked: `round(1.0/25×30)=1`, `round(0.5/25×30)=1`, `round(9.0/25×30)=11`,
   `round(25/25×30)=30` — all four figures the doc prints.
8. **`ai-readiness.md:230`, `:242-248`, `:681-685` — the five current usage KPI ids.**
   `how_we_measure.go:1160-1181` declares `usageDimSpecs` with exactly `avg_adoption`, `avg_transformation`,
   `avg_originality`, `avg_depth`, `avg_ownership` — **5**, matching the seeder's
   `stack-seeding/seeders/ai_readiness_interview_report.go:185-189` (pinned rext `09d06070`) and the
   `:149` comment *"All five are always emitted here."*
9. **`ai-readiness.md:194-212` — the data-model table.** All 13 `ai_readiness_*` table names enumerated from
   the generated ent (`Table = "…"`): cycles, diagnose_narratives, email_overrides, live_snapshots,
   notification_logs, notification_optouts, recommendations, sims, skills, snapshots, steps,
   text_translations, user_step_progresses — **every plural form the doc asserts**, plus
   `interview_aggregated_reports` with fields `(sim_id, organization_id, report JSON, session_count)`.
10. **`ai-readiness.md:268-272` — the REST surface.** All ten routes enumerated from
    `internal/web/backend/api/server.gen.go:2396-2406`: `/workforce/ai-readiness`, `/narrative` (POST),
    `/steps-completion`, `/cycles` (GET+POST), `/setup` (GET+POST), `/cycles/:cycleID`, `/export.csv`,
    `/compare`, `/cycles/:cycleID/close`. No extras, none missing.
11. **`ai-readiness.md:306-308` — the 6-section `AIReadinessCompareResponse`.** `compare.go:32` declares
    exactly `Topline`, `Archetypes`, `Transitions`, `TeamDelta`, `SkillCoverage`, `ThemesShift` (plus the two
    cycle refs), and `:163`/`:170` reject any cycle whose `Status != "closed"`.
12. **`ai-readiness.md:86-88` — "sole consumer repo-wide" of `flag_ai_readiness`.** `git grep AI_READINESS_FLAG
    8297c684` returns 4 lines in 2 files: the declaration (`aiReadiness.constants.ts:9` comment + `:26` const)
    and the single use (`useAiReadinessActive.ts:7` import + `:22` `useFeatureFlagEnabled(...)`).
13. **`ai-readiness.md:345-356` — "`AI_READINESS_URL` lives in exactly two source files."**
    `git grep AI_READINESS_URL 8297c684 | grep -v node_modules` = 4 hits in 3 files: `urls.ts:51`,
    `useNavbarSections.tsx:4` + `:403`, and the KB page `knowledge/ai-readiness/frontend-architecture.md:15`
    — exactly the enumeration the doc publishes, including its correction that `:52` is `TALK_TO_DATA_URL`.
14. **`cms.md:100`, `:218-232` — "Mistral is OCR-only on both sides."** `services/ai.py:705-708` is the
    three-entry provider dict; `:1-2` imports only `openai`/`anthropic`. `git -C stack-demo/app/studio grep -ci
    mistral aeec036a` = **22 lines across 3 files** (`requirements.txt` 1, `tools/pdf2md.py` 18, `tools/r3.py`
    3) — the rule-44 worked example reproducing. Go side: `markdownManager.go:10` imports
    `internal/cms/studio/mistralocr`, `:30` `mistralocr.New(aiKey)`; `studioManager.go:531` is the
    *"supported ocr content types for mistral ocr"* comment, `:583` the `NewMarkdownManager(os.Getenv(
    "MISTRAL_API_KEY"))` call, `xlsx.go:13` the *"Mistral OCR doesn't accept xlsx"* comment. All exact.
15. **`cms.md:292-310` — the argv/`bash -c` inversion retraction.** `git grep -n '"bash"' ad9f3c49 -- '*.go'`
    over the whole `app` tree = **0**. `studioManager.go:100-103` is the *"MUST NOT be interpolated into a
    shell … No `bash -c`"* comment, `:119` the `gen.py` argv dispatch, `:122-124` the venv note, `:126`/`:129`
    the fixed-argv venv+pip calls, `:1096-1098` the `runCommand` contract, `:1101` `exec.CommandContext`. The
    frozen counter-example is real: `cms` `ca50c817:internal/studio/studioManager.go:967` **is**
    `exec.Command("bash", "-c", command)`. Both `studio/gen.py` (`:119`) and `studio/postgen.py` (`:1045`)
    resolve at the ref the sentence names, `b948604f`.
16. **`academy-backend.md:65-78` — the Ent-label-vs-table-name warning.** Every plural table name confirmed
    from generated ent `Table =` constants: `academy_bookmarks`, `academy_certificates`,
    `academy_chapter_bodies`, `academy_chapter_progresses`, `academy_chapter_times`, `academy_feedbacks`,
    `academy_last_activities`, `academy_series`, `academy_skill_paths`, `academy_path_embeddings`
    (`extensions.vector(1536)` at `schema/academy_path_embedding.go:62`).
17. **`academy-backend.md:61-64` — "`STORAGE_RPC_ADDR` is gone from the Go source (3 remaining occurrences at
    that ref, all comments, zero reads)."** At the ref the block names (`9d00a313`),
    `git grep -n STORAGE_RPC_ADDR 9d00a313 -- '*.go'` = exactly **3**, all `//` comments
    (`jobsimwiring/wiring.go:101`, `storagens/callsites_test.go:189`, `main.go:451`), no `Getenv`.
    `main.go:471-472` is the two-manager construction; `internal/storage/service.go:22`/`:24` confirm
    `EnvBucket = "STORAGE_S3_BUCKET"` / `EnvPublicBucket = "STORAGE_S3_PUBLIC_BUCKET"`.

**RE-DERIVED** (weaker — arithmetic the document itself displays, re-computed):

- `ai-readiness.md:397` — 156/199 = **78.39 %**, consistent with the printed 78.4 %.
- `ai-readiness.md:172` — `score = 30+40+30 = 100`; confirmed against `readiness.go:29-31`
  (`step1MaxScore=30`, `step2MaxScore=40`, `step3MaxScore=30`), `scoring.go:120` (`score = t1+t2+t3`),
  `:93` (`knowledge = (step1+step2)/70*100`), `:102` (`usage = t3*100/30`).

**Other exact anchors opened and confirmed** (each read with ±15 lines of surroundings, §5 rule 10) —
`AIReadinessClient.tsx` `:78`/`:135`/`:155-156`/`:171`/`:601` and its 0 `posthog` / 0 `flag_ai_readiness`
occurrences · `useAiReadinessActive.ts` `:19-32`/`:22`/`:25`/`:29`/`:32` · `AIReadinessHero.tsx:88` ·
`components/ai-readiness/useAIReadiness.ts:48-62` (`deriveMode`; note this is the *components* file, not the
*hooks* file that holds `AIReadinessCycleTotals` — the bare filename is ambiguous and both resolutions in the
doc are the right one) · `useWorkforceAIReadiness.ts:23-27` (and `grep -ci cycle` over that file = **0**, so the
"no `cycle` param at all" claim is enumerated) · `WorkforceNewClient.tsx:125-151` · `HowWeMeasureTab.tsx`
`:1879`/`:1903`/`:1915`/`:1921`/`:1927`, file = **1,989** lines, `grep -c interviewQuestions` = **0** ·
`useNavbarSections.tsx` `:4`/`:401-408`/`:403`/`:569` · `urls.ts:51` · `dae0fb2f7` deleting exactly
103+220+330 = **653** lines and no `/enterprise/workforce/ai-readiness` route surviving ·
`readiness.go` `:139`/`:289`/`:290-297`/`:307-312`/`:314`/`:330`/`:388`/`:390`/`:684`/`:710`/`:730` ·
`steps.go` `:541` (`isAIReadinessEnabled` **is** declared in steps.go), `:922`, `:962`, `:964`, `:969-974`,
`:309-326` · `how_we_measure.go:285-287` · `enum/organization_settings.go:47` · `enum/ai_readiness.go` ·
`scoring.go` `archetypeHighBand = 75` / `archetypeLowCeil = 50` / bands `0-24 / 25-50 / 51-74 / 75-100` ·
`csv.go` `AIReadinessCSVColumns` = **15** entries (the platform's own doc-comment still says "19-column"; the
corpus is right and the platform comment is stale) · `emailoverride.go:29` (the corrected
`app/internal/messenger/aireadinessemail` import) and `:33` (the platform's own `pkg/` shorthand), with
`git ls-tree ad9f3c49 internal/messenger/aireadinessemail/` = **9 files** · interaction `action_type` validator
= exactly `{"email","call"}` · `session.go:205-207` · `main.go:1205-1211` @ `b948604` · `d11a403` moving
**exactly two** variables on the messenger block (`BACKEND_USERS_RPC_ADDR` and `SKILLER_RPC_ADDR` already read
`http://backend:8083` at `d11a403^`) · compose at `0c91421d` setting **0** `*_RPC_ADDR` · `915da06` deleting
both `cms.graphqls` and `jobsimulation.graphqls` with `subgraphs.conf` going `{BACKEND,JOBSIMULATION,CMS}` → 
`{BACKEND}`, i.e. **3 → 1**, and the ladder 5→4 at `749dc86` (skiller) → 3 at `7c17e63` (skillpath) ·
`cms/cmd/root.go:77`/`:78`, `cms/go.mod:3`, `cms/Dockerfile:2`/`:23`, `cms/ent/schema/studio{Document,Task}.go` ·
`2adcf71:docker-compose.yml` `:154-155`/`:169`/`:173` for the historical 8090/8091 · `6efa1d5`'s body quoted
verbatim and correctly · `roadrunner/terraform/main.tf:19` = `1`, `git blame -L 19,19` → `84a4b4f1`
**2025-12-15** · `cms/terraform/main.tf:39` = `0` at both `ca50c817` and `f38c0c4a` ·
`graphql-wundergraph/terraform/main.tf:20` = `1` · `045857c` and jobsim `09631fb2` (PR **#395**,
`feat/remove-chronos-and-realtime`, confirmed via merge `500b9761`) · `jobsimwiring/worker.go:24-35` ·
`0e37771f` (*"Academy backend v1.0 'ground truth' … (#903)"*, 2026-06-05) · `git describe --tags ad9f3c49` =
`v1.369.0-7-gad9f3c498`, `CHANGELOG.md:5` = `## v1.369.0 - 2026-08-05` · `academyImport/main.go:239-243` and
`:244-246` @ `9d00a313` · `serverTenant.js:115-145` (the *"cutover is intentional, not reversible-on-error"*
line is `:132`) and `ant-academy.md:82-88` really does state the opposite in bold ·
`demo-stack/patches/academy-fs-published-fallback/` present in the pinned clone ·
`app-aireadiness-snapshot-loadmembers.yaml:42` = `path: internal/aireadiness/readiness.go` and `:33` = the
*"v2.7 M254 RE-POINT"* header, with `demo-stack/tests/test_aireadiness_snapshot_loadmembers_m254.py` present ·
`aiReadinessStageFor` (`ai_readiness_funnel.go:205-226`) mapping exactly manager→0 / struggling→1 /
default→3 with `IsAIReadinessNotStarted()` checked first and read in **exactly one** non-test place, and
`ai_readiness` validated as a **closed enum** at `blueprint.go:618` · `DIRECTUS_DATA_CONSUMERS = ("cms",
"backend")` in the pinned clone (`stack-core/gen_override.py:58`, `stack-injection/gen_injected_override.py:86`)
· `platform-migration-status.md:101` really is the `customerio-sync` row · `838d907` deleting the
`storage-legacy`, `customerio-sync` and `messenger` profiles and touching `repos.yml`.

---

## Explicitly NOT booked, with the reason (so a later pass does not re-litigate)

- **`README.md:24` — "jobsimulation's `service_desired_count` no longer appears in
  `jobsimulation/terraform/main.tf` at all — `:15-22`."** At the **checkout** `462343b0` this is FALSE
  (`module "jobsimulation"` is intact and `service_desired_count = 0` sits at `:40`). It is **TRUE at the ref
  the sentence itself names**, `6092c6d2` (*"chore(terraform)!: remove the jobsimulation ECS service and ECR
  repository (M810)"*, 2026-08-04), where `service_desired_count` occurs **0** times and `:15-22` is the M810
  decommission comment — and equally true at `origin/main` `82cb66ec`. `6092c6d2` is not an ancestor of the
  checkout but is of `origin/main`. A pin is a date (frozen instrument, §5 rule 33): **not a finding.**
- **`README.md:43` — "In prod [storage's] ECS service is **deleted**, not scaled to zero — the module survives
  only to keep the buckets/CDN under `prevent_destroy`."** At the **checkout** `4ce8ece5`,
  `storage/terraform/main.tf` is **100 lines** and still declares `module "storage"` with
  `service_desired_count = 1`. At **`origin/main` `9f8cb532`** it is **18 lines** and says exactly what the
  corpus says (`e6fac97` *"feat(terraform): drop the ECS service, keep the assets, export them as outputs"*).
  The corpus's own citation convention resolves at `origin/main` first (`CITE_REF=auto`, §5 rule 41), and the
  addendum states both refs are legitimate for this repo. **Not booked** — but I am recording the split
  because the same shape hits `messenger/terraform/main.tf:29`, which is `container_definitions = <<EOF` at the
  checkout `fa47850d` and `service_desired_count = 0` at `origin/main` `e9421c68`. Any future repair that
  advances these checkouts must not read the pre-fetch tree as a refutation.
- **`README.md:20-25` vs `CLAUDE.md`'s "Eight services … folded into `app`".** README calls roadrunner *"the
  eighth, and it is different: orphaned, not merged-and-undeployed"* and `README:39` says *"`roadrunner` is NOT
  one of them"*. Measured, both are defensible about different objects: there is no `app/internal/roadrunner/`
  package (README is right), while Judge0 execution genuinely runs in-process inside the jobsim domain
  (CLAUDE.md's "folded" is right about the capability). README states the distinction in its own text.
  **Not a contradiction.**
- **`chronos.md:186` — "`docker compose up -d chronos` will fail with 'no such service'."** This is a *service*
  token, not a *profile* token, so §5 rule 30 / the `platform_predicate_guard` G1/G3 class does not apply:
  compose really does error on an undeclared service name, and `chronos` is declared nowhere at `0c91421d`.
  **True as written.**
- **`cms.md:257-258` — the "asking for a cms profile exits 0 and starts postgresql, redis and sentinel"
  note.** Verified: at `0c91421d`, `sentinel` (`docker-compose.yml:5`) declares no `profiles:` key, and
  `common.yml` gives `postgresql`/`redis` none either — so the always-on floor is exactly those **three**. The
  doc names the token without spelling a runnable invocation, which is the §5 rule 30 corollary. **Correct.**

---

## What I could not settle

- **`chronos.md` §§ "Architecture & Code Map" → "Usage Example" (`:21-245`) — essentially the whole body.**
  The `chronos` repo is in no clone set (13 top-level trees enumerated; chronos is not one), and `stack-dev/`
  holds no clone of it. So Go 1.25, sqlc-not-Ent, Cobra, `search_path=chronos`, ports 8080/8081, the `timers`
  DDL and its four indexes, the `ChronosService` RPC pair, `EventTimerWentOff`, the triggerer's `LIMIT 10` /
  `FOR UPDATE SKIP LOCKED` / 3 s timeout / 1-minute Sentry heartbeat, the env-var table and the two CLI flags
  are all **unverifiable from here**. What settles them: a clone of `anthropos-work/chronos` at a named sha, or
  the `proto` module (also never cloned) for the `chronosv1` message shapes. I flag this because the doc's
  *decommission banner* is fully verified while ~90 % of its body is not — a reader could mistake the one for
  the other.
- **`README.md:54`, `:81` and the `graphql-wundergraph` / `chronos` archive states** (*"repo ARCHIVED"* /
  *"NOT archived … last push 2026-04-23"*). GitHub repository metadata is not in any clone. Settled by a
  `gh api repos/anthropos-work/<name> --jq .archived` at a recorded instant — and per §5 rule 49 that answer is
  timestamped, not standing.
- **`cms.md:16-19`, `:83-89` and `README.md:15-17` — whether `infrastructure/terraform/production/services.tf`
  still declares `module.cms_euwest1` / the `customerio-sync` prod half.** The `infrastructure` repo has never
  been in any clone set; the corpus says so and asserts neither direction, which is the honest state. Settled
  only by adding that repo to the clone set.
- **`ai-readiness.md:3-7` — "ships in `app` v1.315+ / `next-web-app` v2.89.0+".** A "+"-ranged version claim
  has no single ref to grade at; I did not try to bisect for the introducing tag.
- **Measured-runtime numbers in `ai-readiness.md`** — the 2.09 s live recompute, the 24 ms frozen read, the
  180 s → 19 ms demo-patch delta, 199 frozen snapshots, `byTeam` (13), `interview_extraction_results` (165
  rows), `validation_attempt_results` 5 → 345. These are live-stack measurements against a demo that is not up
  here. The doc says *"Re-measure before relying on either number; do not re-derive them from prose"*, which
  is the right disclosure; I am recording that I did not re-measure.
