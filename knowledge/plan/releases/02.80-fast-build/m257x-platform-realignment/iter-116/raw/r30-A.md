# Seat A — reading #30 (M257x iter-116)

Ground truth re-verified at this seat's open (all match the ADDENDUM):
`app ad9f3c49` · `platform 0c91421d` · `next-web-app 8297c684` · `sentinel f2c46190` ·
`studio-desk 41ee3575` · `ant-academy 22df69dd` · `storage 4ce8ece5` · `messenger fa47850d` ·
`cms ca50c817` · `graphql-wundergraph 60c229f3` · `roadrunner 87d8d443` · `jobsimulation 462343b0` ·
`stack-demo/rosetta-extensions 09d06070` (pinned consumption clone — the adjudicating tree for
tooling-on-a-stack claims) · `.agentspace/rosetta-extensions 1dc1eb82` (authoring copy) ·
`stack-demo/app/studio aeec036a` (own checkout).

Trees enumerated for any tree-wide claim: **15** (13 `stack-demo` repos + the two nested
`studio` checkouts). Searches used `/usr/bin/grep` (never the `ugrep --ignore-files` shell alias)
and `git grep` at each tree's own ref; stderr was read on every pass.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/architecture/external_services.md` | 887 | yes (1–300, 300–596, 596–887) |
| `corpus/services/jobsimulation.md` | 316 | yes |
| `corpus/services/customerio-sync.md` | 165 | yes |
| `corpus/services/askengine.md` | 147 | yes |
| `corpus/services/intelligence.md` | 18 | yes |

(`wc -l` reports 316 for `jobsimulation.md`; the file's last content line is 317 because the final
line carries no trailing newline. Not a finding, recorded so the control is auditable.)

## Blockers

### B1 — `anthropos-agent-eu` "0 hits across all 15 trees" is a false measurement: one of the 15 has 7

- **Anchor:** `corpus/architecture/external_services.md:727` —
  > "There is no `anthropos-agent-eu` — the name appears nowhere in the platform (**0 hits across all
  > 15 trees at their own refs, and 0 in a `.gitignore`-blind filesystem grep**)."

- **Why it is false:** the tree census the sentence itself invokes is
  `find stack-demo -name .git -maxdepth 4` → **15** trees, and one of those 15 is
  `stack-demo/rosetta-extensions`. Measured at that tree's own ref (`09d06070`, the pinned
  consumption clone):

  ```
  git -C stack-demo/rosetta-extensions grep -c "anthropos-agent-eu" HEAD   →  7
  ```

  The seven are real, tracked, non-empty text files:
  `stack-core/tests/fixtures/claim_twin_iter48/red/07.md:3`,
  `stack-core/tests/fixtures/mechanical/{green,red}/corpus/architecture/external_services.md:652`,
  `stack-core/tests/fixtures/repair_leak/{pre,post}/corpus/architecture/ai_architecture.md:{152,168}`,
  `stack-core/tests/fixtures/repair_leak/{pre,post}/corpus/architecture/external_services.md:652`.
  The authoring copy `.agentspace/rosetta-extensions` @ `1dc1eb82` returns **7** as well, and a
  `.gitignore`-blind filesystem grep over `stack-demo` returns those same files — so the second
  clause ("0 in a `.gitignore`-blind filesystem grep") is false on the same evidence. Both halves of
  the parenthetical are zero only over 14 trees, never 15.

  The corpus's own convention elsewhere counts `rosetta-extensions` inside such a census: the
  structurally identical measurement at `corpus/services/jobsimulation.md:54` reports
  *"the literal does occur in the clone set — **6 times**"*, and I re-derived that 6 as **1 in
  `stack-demo/app` + 5 in `stack-demo/rosetta-extensions`**. So the tooling clone is inside the
  denominator there and outside it here, in a pair of sentences that are otherwise the same
  instrument.

- **Why I hesitated / what survives:** the *substantive* claim is **TRUE** — the string occurs in no
  platform source; all seven hits are copies of the corpus's own **retracted prose** parked as guard
  test fixtures. A reader is not misled about LiveKit. What is wrong is the derivation the sentence
  displays, and the cost is exactly §5 rule 19's: a re-deriving reader gets **7**, not 0, and must
  adjudicate a correct retraction rather than measure. Because the briefing lists "wrong counts"
  first among BLOCKERs and §5 rule 24 is specifically about the SET an arithmetic ranges over, I book
  it rather than minor it. Correct forms would be *"0 across the 14 platform trees"*, or *"the only
  hits are this corpus's own retracted prose in `rosetta-extensions` test fixtures."*
- **Confidence:** medium

### B2 — "eight other sites" for the `app/internal/cms/directus/collections/…` spelling is not reproducible

- **Anchor:** `corpus/services/jobsimulation.md:164` —
  > "The corpus spells this path correctly at **eight** other sites and this was the sole outlier"

- **Why it is false:** enumerated over the whole corpus (`/usr/bin/grep -rn` over `corpus/`, so the
  `.gitignore` wrapper cannot undercount):

  | predicate | occurrences | "other" (excluding the citing sentence) |
  |---|---|---|
  | `app/internal/cms/directus/collections/jobsimulation.go` | **7** | **6** |
  | `internal/cms/directus/collections/jobsimulation.go` (with or without the `app/` prefix) | **8** | **7** |
  | `app/internal/cms/directus` (directory, any suffix) | 15 | 14 |

  The seven correctly-prefixed sites are `ai_architecture.md:61,221,222,264,277`,
  `external_services.md:624`, and the citing sentence in `jobsimulation.md:164` itself. The eighth
  member of the looser predicate is `external_services.md:566`, which writes the path
  repo-relative (`internal/cms/directus/collections/jobsimulation.go:1302`, no `app/` prefix).
  No reading of "this path" yields **eight *other*** sites; the number 8 is reachable only as the
  **total** under the looser predicate, i.e. the word "other" is what makes the sentence wrong.

- **Why I hesitated:** this is a corpus-meta count (how consistently the corpus spells one path),
  not a platform fact — materiality for a reader doing platform work is near zero, and the adjacent
  clause *"this was the sole outlier"* does hold (`external_services.md:566` is repo-relative but
  resolvable; the retracted `cms/directus/collections/…` form resolves in no clone, which I
  confirmed: `git -C stack-demo/cms ls-files | grep -c '^directus/'` = **0**, and the frozen repo's
  own copy is `cms/internal/directus/collections/jobsimulation.go`, 1 file). I book it per the
  briefing's tie-break because it is a stated cardinality that does not survive re-derivation.
- **Confidence:** low–medium

## Minors

- `corpus/services/askengine.md:104` — *"the Asynq pools and the Redis subscribers (`:1438` onward)"*.
  At `app ad9f3c49`, `main.go:1438` is a **comment line** inside the `MESSENGER_ENABLED` rationale
  block; the messenger `pubsub.NewSubscriberServer` is `:1450` (declared `:1444`) and **app's own**
  `subServer := pubsub.NewSubscriberServer(` is `:1376` — i.e. *before* 1438, so "onward" excludes
  the primary subscriber. The Asynq pools start at `:726` (`jobsimwiring.StartWorkers`), also before
  1438. No factual consequence: the load-bearing claim (everything constructed after the `:471`
  `return` never runs) is true for all of them, since every one is > `:471`.
- `corpus/services/intelligence.md:5` — *"removed … in mid-2026"*; `fdfa189` is dated **2026-04-17**
  (verified: `chore: remove intelligence service from local dev orchestration`, touching
  `docker-compose.yml` and `repos.yml`). April is early-2026 rather than mid. Harmless.
- `corpus/architecture/external_services.md:455` — the text attributed to
  `graphql-wundergraph/CLAUDE.md:39` inside quotation marks (*"Since cms-in-app the platform compose
  `graphql` service builds from the **production** Dockerfile"*) is a **paraphrase**, not the line.
  The actual `:39` reads: *"**The committed `schemas/backend.graphqls` is now load-bearing locally
  too.** Since the compose stack builds from the production Dockerfile, a schema change in `app` does
  **not** appear at `:5050` until you regenerate and commit that file here."* The substance
  (CLAUDE.md:39 asserts the compose builds from the production Dockerfile, and is wrong) is exactly
  right, and `60c229f` is confirmed dated **2026-07-30**, subject *"docs: backend is the sole
  subgraph — correct the compose build path (#295)"*.
- `corpus/services/jobsimulation.md:12` — *"Measured at `cms` `origin/main` `f38c0c4a`
  (2026-08-06)"*. `f38c0c4a`'s commit date is **2026-08-04**; the parenthetical reads naturally as
  the *measurement* date, but sits where every neighbouring parenthetical in the same banner is a
  *commit* date (`6efa1d5` … 2026-08-04). Ambiguous, not false — the substance re-derives exactly
  (191-line module, `:39` = `service_desired_count = 0`).

## Positively cleared (audited zeros)

**ENUMERATED** — predicate re-derived from source independently, cardinality compared:

1. `customerio-sync.md:85` — the git-URL build-context history. Re-derived from **every** commit
   touching `docker-compose.yml` (`for c in $(git log --format=%H -- docker-compose.yml)`), extracting
   `context: git@…` and de-duplicating → **18 distinct repos**, and the list matches the document's
   name-for-name (app, chronos, cms, customerio-sync, graphql, graphql-wundergraph, graphqltmp,
   intelligence, jobsimulation, messenger, realtime, roadrunner, sentinel, simulator, skiller,
   skillpath, storage, studio-desk). Also enumerated: `a2a3ee6` (2026-02-27, exact subject) leaves
   **exactly two** git-URL contexts (`customerio-sync`, `realtime`); `c17cc9a` (2026-04-15) removes
   `realtime`; at `838d907^` there are **7** build contexts and `customerio-sync` is the only git URL.
   Every clause of the correction holds.
2. `askengine.md:47-48` — `TableRegistry`: enumerated `{Name: "` in `internal/askengine/registry.go`
   → **60** `TableDef`s, **0** of them commented out, **0** lacking a `Schema:` field;
   `Schema: "public"` = **57**, `Schema: "directus"` = **3**. The dotted `jobsimulation.*` names
   (16 of them) all carry `Schema: "public"` + a `RealName:`, and `skiller.*`/`skillpath.*` are
   **generated aliases** appended after the literal (`:551-577`), not members of the 60 — so the
   count is the right one to publish. `registry.go:183-195` is exactly the transition-alias comment
   block quoted.
3. `askengine.md:127-132` — test counts. `^func Test` per file: `sandbox_test.go` **49**,
   `executor_test.go` **13**, `prompt_test.go` **10**, `followups_test.go` **14**. All four exact.
4. `external_services.md:373-386` — the supergraph table. Enumerated `- name:` in
   `supergraph-config-prod.yaml` at each named commit: `749dc86~1` → **5** (backend, skiller,
   jobsimulation, cms, skillpath), `749dc86` (2026-06-24) → **4**, `7c17e63` (2026-07-21) → **3**,
   `915da06` (2026-07-29) → **1**. Names removed at each step match. `915da06`'s own subject really
   does read "supergraph 2→1" against a 3-entry tree, exactly as the caution says. `schemas/` holds
   `backend.graphqls` alone; `subgraphs.conf` = `BACKEND=v1.360.0`; prod routing_url =
   `http://backend.internal.anthropos:8080/graphql/query`.
5. `external_services.md:387` — the no-subscriptions claim, both halves. (a) `grep -rn
   "sse\|subscription" graphql-wundergraph/*.yaml` → rc 1 over **7** yaml files; positive control
   `grep -rln backend *.yaml` → the **3** supergraph configs. `915da06~1:supergraph-config-prod.yaml`
   still reads `protocol: "sse_post"` (`:12-13`); `git log -S 'protocol: "ws"'` → **0**, control
   `sse_post` → **5**; `bba862f` (2026-02-25) is **not** an ancestor of HEAD (rc 1) and
   `git branch -a --contains` names `remotes/origin/feat/use-web-socket` and nothing else. (b)
   `type Mutation` `:4053`, `type Query` `:4912`, **no** `type Subscription`; the six `Subscription`
   hits are lines 1528/1555/4385/4389/4391/4418, all Stripe/plan. **I extended the predicate past the
   document**: `app`'s own live schemas (43 `.graphqls` under
   `internal/web/backend/graphql/graph/schemas/`) also contain **no** `type Subscription`, so the
   clause "or on `backend` directly" holds against the current source, not just the frozen SDL.
6. `external_services.md:668-677` — the env-name census at the named ref `5ba17044` (a real tag
   commit, v1.363.2). `OPENAI_KEY` **5** · `ANTHROPIC_API_KEY` **26** · `AZURE_OPENAI_KEY` **13** ·
   `AZURE_OPENAI_ENDPOINT_URL` **13** · `OPENAI_ORG_ID`/`AZURE_OPENAI_ENDPOINT`/
   `AZURE_OPENAI_DEPLOYMENT` **0/0/0** · `OPENAI_API_KEY` **2**. Identical at `ad9f3c49`.
   `app/studio/gen.py:45-48` at the studio tree's **own** ref `aeec036a` is exactly the six-name
   `secrets_keys` list, and `:41-53` is the whole `load_services_settings` override loop — the
   iter-115 range correction is right.
7. `external_services.md:619,640` — the studio config census: **3** shipped `configs/*.ini`
   (`config_template`, `development_config`, `production_config`), **30** `*_AI_*_MODEL` lines across
   them, and **30 of 30** begin `azure`. The `openai`/`anthropic` arms therefore really are latent.
8. `external_services.md:691-704` — the taxonomy manifest. Opened
   `.agentspace/snapshots/taxonomy/5afc0bccf1df7ef538b643321fc6362f/manifest.json`:
   `public_only: true`, `predicate: org-null`, `captured_at 2026-06-29`, `skills 42790`,
   `job_roles 22470`, `skill_embeddings 42790`, `job_role_embeddings 18919`. Every figure exact,
   including the transcription hypothesis for the refuted "18K roles".
9. `jobsimulation.md:39` — `20260722081626_jobsim_data_model.sql` contains exactly **23**
   `^CREATE TABLE` statements, and every table named in the prose is among them. The follow-on
   migration `20260722104506.sql` creates `job_simulation_sessions` at **`:2`** and
   `DROP TABLE "sessions"` at **`:79`** — both anchors exact.
10. `jobsimulation.md:54` — the terraform sweep. **44** tracked `.tf` across the 13 `stack-demo`
    repos (per-repo: app 5, cms 4, gw 4, jobsim 6, messenger 4, next-web 2, roadrunner 4, rext 2,
    sentinel 4, storage 5, studio-desk 4, ant-academy 0, platform 0), **0** hits for
    `http://backend.internal.anthropos:8081`; **59** `.tf` on a raw filesystem sweep; the literal
    occurs **6** times in the clone set (1 × `app`, 5 × `rosetta-extensions`), **none** terraform;
    `app/knowledge/service-dependencies.md:52` carries it in the past tense under the heading
    *"There are no external callers of app's RPC mux left."* at `:50`.
11. `jobsimulation.md:47-52` / `:127` / `:153` — the "MIDDLE TWO" correction. Measured on the
    **messenger block** at `d11a403^` (`docker-compose.yml:255-265`): `BACKEND_USERS_RPC_ADDR` and
    `SKILLER_RPC_ADDR` **already** read `http://backend:8083`, while `CMS_RPC_ADDR` read
    `http://cms:8091` and `JOBSIMULATION_RPC_ADDR` read `http://jobsimulation:8401`. `d11a403` moved
    exactly those two. `0c91421` has **zero** `*_RPC_ADDR` lines anywhere (rc 1). The "all four"
    version really was wrong.
12. `external_services.md:170-175, 304-306` — compose cardinality. At `0c91421`, `docker-compose.yml`
    declares **5** services (sentinel 5, backend 28, studio-desk 112, next-web-app 143, gotenberg
    170) and `common.yml` adds `postgresql`+`redis` → **7** effective; at `0dab54d` it was **8**/10.
    `profiles:` keys exist only at `:110` (backend), `:141`, `:168`, `:183` (gotenberg) — so `:110`
    and `:183` are precisely the two `core` membership declarations the prose cites, and the
    profile-less floor is `postgresql`, `redis`, `sentinel`. No `graphql` token in any `profiles:`
    key at `0dab54d` either.
13. `jobsimulation.md:184-194` — the cobra contract of the frozen binary: `rootCmd` (`cmd/root.go:59`,
    `Use: "jobimulation"`), exactly **four** `AddCommand`s (`aggregate`, `clone-session`,
    `test-command`, `validate`), **zero** `SilenceUsage`/`SilenceErrors` anywhere, `EXPOSE 8080` in
    both Dockerfiles, `ENTRYPOINT ["./application"]` with no `CMD`, and the repo's own
    `CLAUDE.md:88` really does document the non-existent `go run . serve`.
14. `external_services.md:554` / `jobsimulation.md:168` — the `ai`-library fold. `1e457fa70`
    (2026-08-04, exact subject). `app/go.mod` requires exactly **five** `anthropos-work` modules and
    they sit at **`:14-18`** (`analytics-go`, `colony`, `proto`, `storage`, `taxonomy`) — "and nothing
    else" holds across the whole file. Neither `app` nor `sentinel` requires
    `github.com/anthropos-work/ai`; the frozen `cms` and `jobsimulation` husks still do (1 each).
    **67** `.go` files import `github.com/anthropos-work/app/internal/ai"` exactly.
15. `jobsimulation.md:134-139` — the mirror drop. `20260729133514.sql` has **0** `INSERT INTO`
    (the single case-insensitive `insert` hit is the function name
    `on_insert_local_jobsimulation_sessions_update_memberships` at `:64`), so the "0 INSERTs"
    correction is right; `:15-23` remaps the two link ids, `:36-44` NULLs orphans, `:58-62` is the
    *"5. Drop the mirrors."* comment, `:62-63` are the two `DROP TABLE`s, and
    `internal/organization/intelligence.go:1700` is `query := m.ent.JobSimulationSession.Query()`.
16. `askengine.md:44` — *"defends four documented bypass vectors"*. The platform's **own** comment at
    `internal/askengine/sandbox.go:350` says *"three"* and then lists **four** bullets, and the
    function implements four refusals (`ErrQuotedIdentifier`, `stripCommentsAndStrings`,
    `ErrUnresolvedFromTarget`, `ErrReservedCTEName`). The corpus is right and the source comment is
    the stale one — recording this because a re-deriver who greps the comment will read "three" and
    be tempted to book the corpus.

**Anchor-resolution clearances** (every `file:line` below opened and read with surrounding context,
at the ref the passage names — checkout where none is named):

- `askengine.md`: `main.go:467-471` (bedrock block, `return` inside `func main()` at `:229`, and I
  confirmed **no** intervening `func` declaration between `:229` and `:467`), `:1295` RPC mux,
  `:1361` meta server; `bedrock.go:161` (`NewBedrockClient`, exactly **one** error return —
  `config.LoadDefaultConfig` at `:165-168`); `DefaultModelID`/`DefaultRegion` `bedrock.go:25-26`;
  `askEngineMaxConns = 6` `main.go:167`, applied `:374`; `COPILOT_DB_CONN` `main.go:351`;
  `MaxInlineRows=200`/`MaxCellLength=400` `executor.go:18,21`; `maxAgenticIterations=15`
  `handler.go:34`, `loopTimeout=10m` `:41`; `rules.md` = **146,219 bytes**; the `/ask` route set is
  byte-for-byte the eight routes registered at `internal/web/backend/backend.go:171-188`;
  `OrgCheckFeaturePermission` at `ask/handler.go:718`; the five `ask_*` Ent schemas all exist;
  `crossValidate` covers percent/negative/`user_id`-GROUP BY **and** member-count > headcount
  (`executor.go:270`); `ValidateSQL` rejects `SET`, non-SELECT/CTE, DML, system catalogs, `public.`
  prefix, multi-statement, in that order; followups are "2-4" per `prompt.go:134`; shipped
  `v1.267.0` = **2026-05-07**, author mode `v1.340.0` = **2026-07-17** (both tags dated exactly).
- `customerio-sync.md`: `838d907` subject verbatim, merged `0c91421` (PR #26), both **2026-08-05**;
  `internal/customeriosync/doc.go:4-5` (v0.19.3 / "the last of the Go services to be folded into
  app", wrapping the line break) and the 7,392-of-7,397 figure at `:30`; `env_guards.go:62`
  (`envCustomerIOSyncEnabled`), `:87` (`mustSubsystemSwitch`'s `log.Fatalf`), `:98-104`
  (`case "":` → error when deployed); `main.go:284` is indeed only the **comment**, `:286` the
  resolve, `:295` the condition and `:296` the `log.Fatalf`, `:393-396` the copilotDB manager,
  `:395` `os.Getenv("BREVO_KEY")`; `docker-compose.yml:84-92` @ `0c91421` is exactly the
  no-variables-here rationale block; `deployedEnvironment()` (`env_guards.go:37`) treats
  `development` as not-deployed, which is what makes "unset = off on a laptop" true;
  `external_services.md` really does contain **0** `brevo` and has exactly the six `##`
  per-service sections named.
- `external_services.md`: `graphql-wundergraph/terraform/locals.tf:8` (`port = 8080`),
  `terraform/main.tf:48-49` (containerPort/hostPort), `config.prod.yaml:5`, `terraform/main.tf:20`
  (`= 1`); `Dockerfile.dev:7` (`wgc@0.104.0`), `:18` (one schema COPY), `:19-20` (the fold comment),
  `:23` (one awk), `:29` (`cosmo/router:0.275.0`); `2adcf71^1` **is** `1e8e754` and its
  `docker-compose.yml:19-27` names exactly backend/jobsimulation/cms/storage, each
  `condition: service_started`, with `"5050:8080"` at `:14`; `464dfe3` is **not** an ancestor of
  `0c91421d` and lives only on `origin/feat/cms-in-app`; the three build eras verify
  (`63d285c` 2024-06-20, service then named `wundergraph`, **no** `dockerfile:` key; `719befb`
  13:59 precedes `2c85211` 16:01 on 2026-02-27, which adds `dockerfile: Dockerfile.dev`; `67ba772`
  2026-03-02 raises the context to `..`); `b56d731` keeps `  graphql:` at `:22` behind
  `profiles: [wundergraph-deprecated]` and `360efd4` has no such key with `  backend:` at `:28`;
  `a2a3ee6^:docker-compose.yml` `:384`/`:386`/`:409` are exactly
  `image: directus/directus:10.10.1` / `8055:8055` / `ADMIN_PASSWORD=password`;
  `docker-compose.yml:53` (`DIRECTUS_PUBLIC_BASE_ADDR`, the **only** `DIRECTUS_*` in the file at
  both `0dab54d` and `0c91421`) inside the `environment:` block `:46-94`; `.env_example:91-92`;
  `:151`/`:160` and `:119`/`:135` all four `:8082/graphql/query`; `app/main.go:980-982` @ the
  **named pin** `b948604` (at `ad9f3c49` the same construct is `:1090-1092` — the pin is doing real
  work); `gen_injected_override.py:86` + `:77-81` + `:698-699` and `stack-core/gen_override.py:58`
  **at the pinned per-stack clone `09d06070`**, plus `test_injection.py:1109`
  (`test_backend_the_actual_reader_is_repointed`); `cms/terraform/main.tf:39` = `0`;
  `6efa1d5` (2026-08-04) deletes `.github/workflows/build-production.yml` with the quoted subject
  and the quoted "registry that no longer exists" rationale; `6092c6d2` deletes
  `module "jobsimulation"`; `report_agent.go:31`, `askengine/bedrock.go:25`,
  `coursebuilder/bedrock.go:23,29,106-113`; `markdownManager.go:10,14,29,30` (and `:19` is indeed a
  doc-comment), `studioManager.go:583`, `NewMistral` **0** hits repo-wide;
  `app/studio/tools/pdf2md.py:24` + `mistral-ocr-latest` at `:127`, `r3.py:139,190,199-206`,
  `services/ai.py:383,627-664,704-724`, `config_template.ini:30-31` — all at the studio tree's own
  ref `aeec036a`; `collections/jobsimulation.go:1302` (`aiVendor := simulation.Openai`);
  `jobsimulation/ai/ai.go:30-33,130-141,150-155,259-289,264-276,280-283,296-302,326`;
  `app/main.go:770` @ `b948604`; `calls/livekit.go:101-104,110,111,115,118-128,120,122-123,126`;
  `docker-compose.yml:101-109` (redis/postgresql/sentinel) and `:48`,`:57`,`:59`,`:183`;
  `backend.go:130`; `directus/directus:11.6.1` + `port = 8055 + n*offset` + `ADMIN_EMAIL=admin@<stack>.example.com`
  (`dev-setdress.sh:308`) in the pinned rext; studio-desk's `AI_PROVIDER_CHAIN=azure-openai,openai`
  (`.env.example:56`) and its `gpt-5.2` / `claude-{sonnet,opus}-4-5` model pins; studio-desk uses
  `@clerk/clerk-js`+`@clerk/express`, ant-academy `@clerk/nextjs`; `sentinel` has **0** webhook
  references, so "not Sentinel, which … exposes no webhook route" holds.
- `jobsimulation.md`: `6092c6d2` (subject verbatim, 2026-08-04) and the surviving module ownership at
  `terraform/main.tf:15-22`/`:24-40` **at the named ref** — the checkout `462343b0` still carries the
  block, which is why the pin is load-bearing and correctly used; `82cb66ec`, `1e40d184`,
  `caf36c96` (= merged PR #439), `6092c6d2` are exactly the **four 2026-08-04 commits** on
  `origin/main`; `repos.yml` @ `0c91421` has exactly the **4** entries named; `d11a403` (2026-08-03)
  removes `cms`/`jobsimulation`/`roadrunner`; `2adcf71`'s `docker-compose.yml:83`, `:93-94`, `:113`,
  `:119`, `:140`, `Makefile:10` (`PROFILE ?= graphql`), `repos.yml:17-19` all resolve exactly;
  `cmd/root.go:77,78`; `internal/worker/worker.go:27` (10) and `:104` (25); `09631fb2` subject
  verbatim; asynq `v0.26.0`; migrations `20260402145459` + `20260409131539`; platform `05b4035`
  (`chore(compose): add READONLY_DB_CONNECTION to jobsimulation service`);
  `internal/graph/queries.resolvers.go:70` = `func (r *queryResolver) JobSimulationResult`, which
  reaches persisted `validation_attempt_results` via `GetValidationAttemptResultBySession` with no
  recompute; `internal/runner/runner.go`'s package doc contains the quoted *"formerly the standalone
  'roadrunner' service"*; `app/main.go:1204` @ `b948604` is the
  `NewJobSimulationServiceHandler` registration; `main.go:216`/`:212`/`:229` across the three named
  refs, `:721`/`:723` @ `ad9f3c49`, `:614` @ `b948604`, **and `:670` @ `9d00a313` really is the
  `log.Fatalf`** while `:670` @ `ad9f3c49` really is `skillerAzureEndpointEu = &v` — the two
  differently-pinned `:670`s in this file are both correct and do **not** contradict each other;
  the only `spf13/cobra` import in `app` is `cmd/createTaxonomy/main.go`;
  `jobsimwiring/wiring.go:147-148` returns unwrapped and `jobsimulation/ai/ai.go:90` is
  `can't load AWS config: %w`; `docker-compose.yml:88-91` @ `0dab54d` is the **only** `$HOME/.aws`
  bind; `app/internal/converter/gotenberg.go:31`; `voiceEngineFromDirectus` at `:1594` with the nil
  branch at `:1595-1596`; `calls/livekit.go:131-135` + `:140-144` (`:142` name, `:143` endpoint) —
  the flag really does reset a US session's agent name, so the iter-115 correction is right;
  the frozen repo's `internal/graph/schemas/` really does contain
  `activites.graphqls` (source typo, quoted faithfully), `mutations`, `queries`, `subscriptions`,
  `schema`; Redis producer `jobsimulation` / consumer `cms` matches `cmd/root.go:281-287`.
- `intelligence.md`: `fdfa189` exists with the quoted subject and touches exactly
  `docker-compose.yml` (-25) and `repos.yml` (-3); no service in the current compose references it.

**Explicitly NOT booked** (checked, and the flat reading would have been the error):

- `external_services.md:3` — *"the repo is ARCHIVED on GitHub (2026-07-30)"* for
  `graphql-wundergraph`. Unverifiable from a clone, and `jobsimulation.md:24-27` establishes that
  archive state is not visible to this corpus. But the two are **not** in conflict: the jobsimulation
  retraction exists because that clone's `origin/main` carries four commits *after* the asserted
  archive date, which is a refutation; `graphql-wundergraph`'s `origin/main` last commit is
  `60c229f3` dated **2026-07-30**, i.e. consistent. The corpus also demonstrably owns the instrument
  (`platform-migration-status.md:99` reports chronos as *not* archived, "last push 2026-04-23 — the
  org disagrees", which can only come from the org API). Dated snapshot, not a defect.
- `jobsimulation.md:100` — the 8400/8401 "historical" framing. All four `2adcf71` anchors resolve and
  `0c91421` has no such service; correct as written.
- `external_services.md:566` writing the path repo-relative (`internal/cms/…`) rather than
  `app/internal/cms/…`. The file uses both conventions deliberately (`:597-598` likewise), and the
  path resolves; not the outlier class `jobsimulation.md:164` describes.

## What I could not settle

- **GitHub archive state** for `graphql-wundergraph` (`external_services.md:3`, `:369`) and the
  `customerio-sync` / `jobsimulation` repos. Settled only by
  `gh api repos/anthropos-work/<repo> --jq .archived`; no network in this pass. The clone evidence is
  *consistent* with the gw claim and *refutes* a flat jobsimulation claim (which the corpus already
  retracts).
- **`intelligence.md:18`** — the port/`/_meta`/5-minute-ticker claims about the retired Intelligence
  binary. The `intelligence` repo is in no clone set (not in `repos.yml`, not under `stack-demo/`),
  so nothing here can grade them. Would need `gh repo clone anthropos-work/intelligence`.
- **`customerio-sync.md:44,96`** — *"The standalone published 8080 (HTTP — health/metrics)"*. The
  historical compose block's `ports: ["8080:8080"]` is confirmed at `838d907^:docker-compose.yml`,
  but what that port *served* is unverifiable from any clone; the doc already labels it
  *"most likely … (never verified)"*, which is the honest form.
- **Production topology** claims that live in `infrastructure` (`services.tf`,
  `module.jobsimulation_euwest1`, `module "cms_euwest1"`). That repo has never been in a clone set;
  every passage that touches it in my files already says so and asserts neither side.
- **`external_services.md:743`** — *"a composited MP4 grid view"* for AWS Chime. Not reachable from
  the clones I read at reasonable cost; would need `app/internal/jobsimulation/recording/`
  end-to-end.
