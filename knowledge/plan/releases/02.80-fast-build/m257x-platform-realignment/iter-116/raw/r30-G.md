# Seat G — reading #29/#30 (M257x iter-116)

**Ground truth read at (re-verified at this reading's open):** app `ad9f3c49`, platform `0c91421d`,
next-web-app `8297c684`, sentinel `f2c46190`, studio-desk `41ee3575`, ant-academy `22df69dd`,
storage `4ce8ece5`, messenger `fa47850d`, cms `ca50c817`, graphql-wundergraph `60c229f3`,
roadrunner `87d8d443`, jobsimulation `462343b0`, app/studio `aeec036a`,
**rext pinned per-stack clone `stack-demo/rosetta-extensions` `09d06070`**, rext authoring copy
`.agentspace/rosetta-extensions` `1dc1eb82`. Historical pins graded at the ref the claim itself names
(`b948604f`, `5ba17044`, `9d00a313`, `88bc5592`, `0dab54d`, `2adcf71`, `d11a403^`, `838d907`).

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/services/hiring.md` | 483 | yes |
| `corpus/services/studio-desk.md` | 449 | yes |
| `corpus/architecture/shared_libraries.md` | 274 | yes |
| `corpus/services/next-web-app.md` | 164 | yes |
| `corpus/services/clerk-integration.md` | 128 | yes |
| `corpus/services/skiller.md` | 78 | yes |

Also read in full as protocol input: `corpus/ops/platform-alignment.md` §5 (2803 lines total; §5 = :249–1406).

---

## Blockers

### B1 — the hiring image bakes SEVEN demopatches, not four

- **Anchor:** `corpus/services/hiring.md:434-436` —
  *"Four demo-patches on the hiring image make it land — 2 net-new (`next-hiring-role-remap`,
  `next-hiring-members-pagination`) + the 2 chained shared `urls.ts` patches (the Studio-eject kill) —
  see [`../ops/demo/demopatch-spec.md`] § **the four hiring-image patches**"*
- **Why it is false:** measured in **both** rext trees (per §5 rule 45 the pinned per-stack clone is
  what a stack runs; the authoring copy agrees byte-for-byte on this function).
  `demo-stack/up-injected.sh` `build_frontend_hiring()` declares **seven** manifests and issues
  **seven** `"$demopatch" apply` calls:
  - `:1096` `next-hiring-role-remap`
  - `:1102` `next-hiring-members-pagination`
  - `:1111` `next-web-studio-url`
  - `:1112` `next-web-public-website-url`
  - `:1117` `next-web-interview-flag-container`  ← **[M232]**, comment at `:1113-1116`
  - `:1118` `next-web-interview-flag-result`     ← **[M232]**
  - `:1122` `next-web-back-to-cockpit`           ← **[M249]**, comment at `:1119-1121`

  and the patch-set fingerprint at `:1124-1126` is a **7**-manifest union (the doc's sibling
  `demopatch-spec.md:300` calls it a *"4-manifest patch-set fingerprint union"*, which is the same
  stale count). Derivation, run in each tree:
  `awk '/^build_frontend_hiring\(\)/,/^}/' demo-stack/up-injected.sh | grep -c '"\$demopatch" apply'`
  → **7** at `09d06070` **and** at `1dc1eb82`. The revert trap at `:1202` reverts all seven.
- **Corroborating self-contradiction inside the corpus:** `demopatch-spec.md:254` already states that
  M249's `next-web-back-to-cockpit` is *"SHARED, so it bakes into BOTH the web and hiring images"*, while
  `demopatch-spec.md:293` still says the hiring image *"bakes FOUR patches"* — the two sentences cannot
  both be true, and `hiring.md:434` propagates the wrong one plus a cross-reference to a
  section title (*"the four hiring-image patches"*) built on it.
- **Blast radius:** a reader re-pinning a patch, or reasoning about why a hiring image was/was not
  rebuilt (the §5-bis fingerprint hazard the doc itself raises), will check four manifests and miss three.
- **Confidence:** high.

### B2 — `service_taxonomy.md:62` names a section heading, not the Database bullet it is cited for

- **Anchor:** `corpus/services/hiring.md:47-49` (anchor at `:48`, repeated at `:55`) —
  *"as the twins `service_taxonomy.md`'s Tier-1 **Database** characteristic bullet (`:62` — "one schema,
  `public`, owned by `app`, which is the only repo with migrations", the `cms` / `jobsimulation` /
  `skillpath` schemas "legacy husks")"*
- **Why it is false:** `corpus/architecture/service_taxonomy.md:62` is
  `### Tier 1: Core Backend Services (Dockerized Go Microservices)` — a heading. The quoted Database
  characteristic bullet is at **`service_taxonomy.md:68`**:
  `- **Database**: PostgreSQL — **one schema, `public`, owned by `app`**, which is the only repo with
  migrations (`repos.yml:14-17`) … the `cms`, `jobsimulation` and `skillpath` schemas are legacy husks`.
  Derivation: `grep -n 'one schema, \`public\`' corpus/architecture/service_taxonomy.md` → `68`.
- **This is the third generation of the same rot in this one citation.** The passage records that it
  cited `:52` until iter-102 and was moved to `:62`; it has now drifted again to `:68`. This is exactly
  §5 rule 34's class (a corpus repair silently invalidating intra-corpus citations below the edit) and
  §5 rule 21's class 3 (*an anchor that resolves but names the wrong construct*).
- **Why I hesitated:** the passage explicitly hedges — *"The bullet is named as well as numbered because
  `service_taxonomy.md` is edited concurrently and `:62` can move"* — and the **substance** of the claim
  (the twin says X) is TRUE at `:68`. Under the frozen instrument's tie-break (*"when in doubt, book it
  as a BLOCKER and say why you hesitated"*) I book it; an adjudicator may downgrade this to a MINOR as
  line drift. The sibling citation in the same sentence, `dependency_map.md:78`, **resolves exactly**.
- **Confidence:** high on the measurement, medium on the grade.

### B3 — `persona_write.go:69-71,143-167` points at neither the write steps nor the three column builders

- **Anchor:** `corpus/services/hiring.md:243` —
  *"reads `public.validation_attempt_results` / `validation_attempt_skill_results` /
  `validation_criterion_results` — three tables … the `PersonaSeeder` also writes
  (`persona_write.go:69-71,143-167`)"*
- **Why it is false:** measured in `stack-demo/rosetta-extensions` `09d06070` (identical in the
  authoring copy `1dc1eb82`):
  - `persona_write.go:69-71` is **not** the three-table mapping. `:69` is a bare `//`; `:70-71` are the
    prose *"The old `jobsimulation.sessions` step is REMOVED rather than re-pointed: it wrote the SAME /
    a.sessions rows under the SAME id …"*. The mapping that names the three validation tables is
    **`:66-68`** (off by three).
  - `persona_write.go:143-167` is `}` (end of `flush`) + `// --- column lists …` + `sessionCols()`
    (`:152-159`, the **session** columns, not a validation table) + `attemptResultCols()` (`:161-167`).
    `skillResultCols()` starts at `:169` and `criterionResultCols()` later still — so the cited range
    covers **one of the three** builders and includes one that is not a validation table at all.
  - The actual writes the sentence is about are the step table at **`:92-94`**:
    `{"public", "validation_attempt_results", …}` / `{"public", "validation_attempt_skill_results", …}` /
    `{"public", "validation_criterion_results", …}` — none of which is inside either cited range.
- **Why I hesitated:** the substantive claim (*the `PersonaSeeder` writes those three tables*) is TRUE,
  the file is right, and `:69-71` is only three lines off. It is the *range* `:143-167` that is
  structurally wrong rather than merely drifted. Same class as B2.
- **Confidence:** medium.

---

## Minors

- `corpus/architecture/shared_libraries.md:57` — *"`sentinel`'s `88036d7` … took it `v0.34.3 → v0.35.2`,
  **two commits past** the `88bc5592` this row used to cite."* Measured:
  `git -C stack-demo/sentinel rev-list --count 88bc5592..88036d7` → **1**
  (`88bc5592..f2c46190` → 2, i.e. the *checkout* is two past, the **bump commit** is one past). The pins
  themselves (`v0.34.3 → v0.35.2`, `proto v1.200.0 → v1.210.0`) verify exactly.
- `corpus/services/hiring.md:31-32` and `:195-196` — `20260729133514.sql:58-62` is described as the range
  that *"re-points the referencing rows (the assignment-session link ids) **and then** `DROP TABLE`"*.
  `:58-62` is only step 5 (the comment block + the `DROP TABLE "local_jobsimulation_sessions"` at `:62`);
  the re-point of `js_session_id`/`session_id` is step 2 at `:13-23`. The quoted string *"5. Drop the
  mirrors."* is at `:58`, so the anchor itself resolves; the range simply does not contain half of what
  the sentence attributes to it. No factual consequence.
- `corpus/services/hiring.md:302-303` — *"the `job_position` bullet, now `:173-175`"*. That bullet begins
  at `:174`; `:173` is the tail of the preceding bullet. Self-referential provenance note only.
- `corpus/services/hiring.md:288-292` — *"The `completition` misspelling exists **only** in the GraphQL
  sort-field enum …, its GraphQL member and a JSON tag."* It also appears as Go identifiers/comments
  (`intelligence.go:1689` parameter `completitionStatus`, `:1712-1714`, `assignments.go:115,167,256-276`,
  `repository/assignments.go:449`). Non-material: the load-bearing half of the claim — *it never reaches
  SQL* — is TRUE (`insightsSortColumn`, `intelligence.go:885-886`, maps it to
  `jobsimulationsession.FieldCompletionStatus`, and the `json:"completition_status"` tag at `:1676` is on
  the hand-built result DTO at `:1672-1687`, not a scan target).

---

## Positively cleared (audited zeros)

Labelled per the briefing: **ENUMERATED** = predicate re-derived from source independently and
cardinalities compared; **RE-DERIVED** = only the arithmetic the document displays was recomputed.

### `hiring.md`
- **ENUMERATED** — `public.job_simulation_sessions` has **23** columns
  (`20260722104506.sql:2-25`, counted from the DDL: id, created_at, updated_at, owner_id, sim_id,
  organization_id, tenant_id, sim_type, status, completion_status, token, started_at, ended_at,
  interactions_progress, score, validation_version, scheduled_timeout, timer_id, timer_reminder_id,
  language, result_status, chime_status, is_test).
- **ENUMERATED** — the **four** required-and-undefaulted columns are exactly `owner_id` `:6`,
  `sim_id` `:7`, `sim_type` `:10`, `token` `:13`; every other `NOT NULL` column in that DDL carries a
  `DEFAULT` (id/created_at/updated_at/status/completion_status/interactions_progress/score/
  validation_version). `token` is `UNIQUE` at `:29`. Both conjuncts of the sentence tested (§5 rule 17).
- **ENUMERATED** — `SET "score"` has **0** hits across all **172** files in
  `app/terraform/migrations/` (positive control: `SET ` matches in many files, e.g. `20231025090211.sql`).
  So the mirror drop genuinely did not back-fill.
- **ENUMERATED** — `SessionCompletionStatus` is a closed **5**-value enum (`enum/jobsimulation.go:29-35`,
  `Values()` `:37-43`): pending/passed/failed/discarded/timedout, no `SIMULATION…` member. The SDL twins
  (`jobsimulations.graphqls:14`, `:128`) both declare the same five lowercase members.
- **ENUMERATED** — `grep -in hiring` over `resolver_queries.go` returns **4** lines, all sim-TYPE filters
  (`:811`, `:843`, `:991`, `:992`); over `internal/organization/intelligence.go` returns **0**. Positive
  controls reproduce exactly: `OrgFeatureInsights` ×**8**, `JobSimulationSession` ×**44**.
- **ENUMERATED** — `TestGenericActivitySeeders_SkipHiringOrg` (`hiring_scope_test.go:75-96`) enumerates
  exactly **8** generic seeders: jobsim-sessions, activity, skillpath-sessions, assignments,
  hero-activity, persona, feedback, succession. `hiring.md`'s *"all 8"* is right.
- **ENUMERATED** — the seed preset's *"5 admins + 45 candidates"* is the correct **effective** population:
  `stories.seed.yaml:249` sets `size: 50, role_mix {admin 0.14, candidate 0.86}`; the block's own
  `:211-218` derivation shows `int(50*0.14)=7` minus the 2 candidate-hero overrides = **5 real admins +
  45 candidates**. `hiringComparableFloor = 6` (`contentref.go:158`) matches the *"≥40 → ≥6"* retune.
- Every `app`-side `file:line` in the read-path table opened and confirmed to name the stated construct:
  `resolver_queries.go:1034` (decl), `:1035` (the Casbin gate, first statement), `:1053`
  (status ∈ {active,invited}), `:1080`, `:1085`, `:1089` (`GetMembership` in the *neighbouring*
  resolver — the disambiguation is correct); `intelligence.go:1700` (`m.ent.JobSimulationSession.Query()`),
  `:1702` `SimID`, `:1709` `OrganizationID`, `:1733` (call site), `:1739-1744` (`RowNumber == 1`),
  `:1738-1764` (best-attempt + the 3-key sort), `:1820` (`score := RoundFloat(...)`), `:1844`, `:1846`,
  `:2124` (decl), `:2158-2160` (`ROW_NUMBER() OVER (PARTITION BY sim_id, owner_id ORDER BY score DESC…)`),
  `:885-886`; `ent/schema/job_simulation_session.go:33/39/45`;
  `ent/jobsimulationsession.go:181-186` (unconditional cast); `graph.go:129546-129554` +
  `:129392-129400` (bare `graphql.MarshalString`, no membership check);
  `anticheat_result.go:24` (`field.Enum("summary")`); `20260722104506.sql:12/53/79`;
  `20240527131926.sql:7`; `20250416091037.sql:5`; `20260722081626_jobsim_data_model.sql:336/355/376`;
  `20230817154747_supabase_baseline.sql:2`; `atlas.hcl:8`; `askengine/registry.go:192`.
- **ENUMERATED** — `CREATE SCHEMA` appears exactly **once** in the whole 172-file migration set, for
  `auth`. So *"no `app` migration touches the `jobsimulation` schema"* and *"the only `CREATE SCHEMA` is
  `auth`"* both hold.
- **ENUMERATED** — `next-web-app` `8297c684`: `useNavbarSections.tsx:475-482` (`enterpriseInsightsMenuItem`,
  label ternary at `:476`), `:329-331`, `:340-343`, `:249-256`, `:391-398`, `:161`, `:568`;
  `orgGroups.ts:48-65` with `intelligence: isAdmin` at `:61` and **no** `isHiringOrg` parameter;
  `showWorkforce={false}` occurs in exactly **2** places, **both** in `apps/hiring`
  (`template.tsx:167`, `:248`) — `git grep -n showWorkforce HEAD -- apps packages` returns 17 lines, of
  which those two are the only `false` passes. The M257x-iter-102 retraction of the
  "Workforce Intelligence is gated off" clause is correct.
- The historical pin verifies: at `next-web-app` `bb3313bc` the same construct is at `:459-466` with the
  label at `:460`, exactly as the passage states.
- `UserStatusContext.tsx:141-173` / `:144-149` / `:168-172` (web) and `:125` / `:144-145` (hiring) all
  name the stated constructs; `useGetClerkOrganization.tsx:20-21` matches the quoted code **verbatim**;
  `:16-18` is the hiring-org filter.
- The two `simulationScoreColumn.tsx` copies are **byte-identical** (`diff` empty), as claimed.
- rext (pinned `09d06070`): `persona_write.go:91` `{"public","job_simulation_sessions",sessionCols(),…}`;
  `sessionCols()` at `:152` with `"token"` inside `:152-158`; `hiring_config.go:99` `CopyRowsIdempotent`
  into `public.organization_sim_invitation_links`; `RoleMix{Admin,Member,Candidate,AdminEmails}`
  (`blueprint.go:453-458`); `hiring_scope.go:45-46`; `readHiringSimPool` (`contentref.go:187,200`);
  `hiring_funnel.go:167-177` (one position per candidate, even round-robin);
  `render-hiring-comparison.spec.ts`; `RENDER_GATE_FLOOR`.

### `studio-desk.md`
- **ENUMERATED** — at `studio-desk` `41ee3575`, `git grep -in graphql HEAD -- 'src/*'` returns **exactly
  two** lines, `src/routes/skillpath.ts:374` and `:405`, both comments saying the backend does *not* use
  GraphQL; `git grep -n 8082 HEAD -- 'src/*'` returns **0** (positive control: `directus` matches
  `src/index.ts` ×9, `src/routes/skillpath.ts` ×78). The M257x-iter-115 correction is sound.
- **ENUMERATED** — `new GraphQLClient(` occurs at exactly **4** sites, all under `app/` (the frontend):
  `userService.ts:20`, `taxonomyService.ts:43`, `userPreferencesService.js:13`,
  `content/simulationContentService.js:325`; all read `config.GRAPHQL_ENDPOINT`, defined at
  `app/services/config.ts:6` from `VITE_GRAPHQL_ENDPOINT`.
- **ENUMERATED** — `src/index.ts` mounts exactly four API routers, at the cited lines: `:150` `/api/dev`,
  `:158` `/api/ai`, `:161` `/api/skillpath`, `:164` `/api/youtube`.
- **ENUMERATED** — `.env.example` is **131** lines; `:117 YOUTUBE_API_KEY=`, `:118` the comment,
  `:119 GCLOUD_SERVICE_ACCOUNT=`, `:120` **empty** — the M257x-iter-115 note about `:120` resolving to a
  blank line is exactly right. `terraform/main.tf:129` injects it. `git grep GCLOUD HEAD -- 'src/*'`
  returns **0**, so *"read by no code in `src/`"* holds (tree-wide it appears only in `.env.example`,
  `CHANGELOG.md`, `terraform/main.tf`).
- **ENUMERATED** — `vite.config.ts` `rollupOptions.input` declares exactly the **9** prod entries listed
  (`home`, `sim-advanced-builder`, `builder-skill-path`, `simulation-builder`, `sim-guided-builder`,
  `generation`, `catalog`, `academy`, `skills`) plus `dev-accept` behind `isProduction ? {} : {…}`.
- **ENUMERATED** — `app/public/l12n/` holds exactly **7** locales (de, en, es, fr, it, ja, nl).
- **ENUMERATED** — the **three** prod-eject sites: `app/core/scaffold/pageWrapper.js:149` (logo link),
  `app/core/scaffold/userProfile.js:148` (Back control), `:302` (logout).
- **ENUMERATED** — no DB driver in `package.json` (none of pg/postgres/prisma/sqlite/mysql/mongo/knex/
  drizzle/typeorm/sequelize) and `git grep DATABASE_URL HEAD` returns **0** (exit 1). The
  "no local datastore" claim holds. `app/services/studioDB.js` exists as the facade;
  `GET_/SET_USER_STUDIO_PREFERENCES` are real GraphQL documents
  (`graphql/queries.ts:205`, `graphql/mutations.ts:58`).
- **ENUMERATED** — the only Tailscale-funnel mention is `app/core/main.ts:105/107` (GlitchTip DSN).
- Anchors confirmed: `main.ts:97` `preloadCriticalCSS()`, `:206` `new PageWrapper()`;
  `src/index.ts:96` `STUDIO_ACCESS_ROLES = ['admin','org:admin','content_creator','org:content_creator']`,
  `:99` `checkEnterpriseAndAdmin`; `src/index.ts:60` `PORT || 9100`; `youtube.ts:43` +
  `_mock: true` at `:46`; `skillpath.ts:44-47`; `index.ts:303-310` (`pingSnapshotCapability`,
  `directus_versions`); `skillpath.ts` = 61,080 bytes (largest route);
  `ai/config.ts:20-39` (gpt-5.2 / gpt-5-mini / gpt-5-nano; claude-opus-4-5 / -sonnet-4-5 / -haiku-4-5)
  and `:182` in-code default tier `thinking_fast` vs `.env.example:60` `fast`;
  Node `>=24` + `node:24-alpine`.
- Platform compose confirmed at **three** refs: `0c91421` `:119`/`:135`/`:138-140`/`:141`;
  `0dab54d` `:204`/`:223-225`/`:226`; `2adcf71` `:337-341` (the `cms` dependency that used to exist).
- rext: all five studio-desk demopatch dirs exist and `build_frontend_studio_desk()` applies exactly
  **5**; `frontend/hiring.Dockerfile`; `run-studio-fcp.sh`; both pinned regression test names
  (`test_injection.py:1657`, `:1714`); `local-directus-token-` in `dev-setdress.sh:310`;
  the autoverify container-side provider-key check (`stack-verify/live/autoverify.sh:528+`).
- The one claim I could **not** settle is disclosed below (`directus/directus:11.6.1` internals).

### `shared_libraries.md`
- **ENUMERATED** — the whole `go.mod` matrix re-derived across the **seven** Go repos on disk, each at its
  own checkout: colony **7/7**, proto **7/7**, taxonomy **6/7** (roadrunner is the sole exception),
  ai **2/7** (cms `:9`, jobsimulation `:11`), authn **0/7**. Every stated line number and version string
  matched: app `:15/:16/:18`, sentinel `:8/:9/:21`, storage `:7/:8/:25`, messenger `:7/:8/:9`,
  cms `:9/:10/:11/:13`, jobsimulation `:11/:12/:13/:15`, roadrunner `:7/:8`.
- **ENUMERATED** — the taxonomy row's pinned anchors hold **at their pinned refs**: at `app` `b948604f`,
  `go.mod:20` is `taxonomy v1.2.0`; at `sentinel` `88bc5592`, `go.mod:21` is `taxonomy v1.2.0 // indirect`.
  (At the current checkouts the app line has moved to `:18` — the pin is what saves the claim.)
- **ENUMERATED** — `app/main.go` at `b948604f` registers exactly **six** Connect handlers
  (`grep -c 'v1connect.New.*ServiceHandler'` → 6): `:1187` Users, `:1188` Organizations, `:1196` Skiller,
  `:1204` JobSimulation, `:1212-1214` CMSService inside `if cmsRPCServer != nil`, `:1228` LabSession.
  Neither `SkillPathSessionService` nor a RoadRunner handler is among them. The same six stand at HEAD
  (`:1297/:1298/:1306/:1314/:1323/:1337`).
- **ENUMERATED** — `repos.yml`'s whole history (10 commits touching it): the first revision `a2a3ee6`
  declares **11** `type: go` entries (app, cms, jobsimulation, skiller, skillpath, chronos, sentinel,
  intelligence, storage, messenger, roadrunner) — matching *"that set is 11"* — and all four of
  skiller/skillpath/chronos/intelligence are present there. `a4db680` (skillpath) is chronologically the
  **last** of the four removals, as stated. HEAD `0c91421d` carries 4 entries with `app` at `:14-17` as
  the only `migrations: true`.
- **ENUMERATED** — `ROADRUNNER_RPC_ADDR`: 0 hits at `app` `9d00a313` (positive control `JUDGE0_BASE_URL`
  → 4 files) and 0 at `platform` `0dab54d` (positive control `_RPC_ADDR` → 4 in `docker-compose.yml`);
  `docker-compose.yml:118` @ `0dab54d` is indeed `AWS_REGION=eu-west-1`.
- **ENUMERATED** — `app/internal/ai/ai.go` is exactly **21** lines and declares only `type AI interface`
  and `type TokenEncoder interface` — no Azure client, no PostHog flag, no 429 handling; and
  `git ls-tree b948604f internal/ai` is **empty**, so the directory genuinely did not exist at that ref.
  `git show ad9f3c49:go.mod | grep -c anthropos-work/ai` → 0, `go.sum` → 0. `1e457fa70` is dated
  **2026-08-04** and the `module_import_guard_test.go` comment is quoted verbatim.
- **ENUMERATED** — the two vendor-selection sites and every anchor in that blockquote:
  `internal/jobsimulation/ai/ai.go` `getClient` `:259`, `flag_use_azure_us` `:267` and `:344`,
  `isThrottlingError` declared `:129` used `:166`/`:325`; `internal/skillerai/ai.go` `getClient` `:332`,
  flag `:347`, `isThrottlingError` `:128` used `:176`.
- **ENUMERATED** — `sentinel` has **never** required `anthropos-work/ai` (checked across its go.mod
  history; 95 commits touch `go.mod`).
- **ENUMERATED** — cms imports `colony/authn` in **6** `.go` files @ `ca50c817`, jobsimulation in **8**
  @ `462343b0`.
- Confirmed: `roadrunner/main.go:7` imports colony and `:17` uses `colony.NewVersionConfig`;
  `storage/internal/migration/migration.go:13` imports `go/simulator/storage/v1` as `legacyStorage`;
  `app/internal/aiusage/ai_usage.go` (`switch model` at `:36`, `Event_AiUsage` at `:98`) and
  `app/internal/askengine/bedrock.go` (raw `anthropic-sdk-go` + bedrock + tool loop) both exist and match.
- The two intra-corpus citations resolve to the right construct: `storage.md:129` =
  `### Connect-RPC (\`StorageService\`)`; `external_services.md:579` = *"There is **no ordered EU-first
  fallback chain.**"*
- The `SkillPathSessionService`-removed claim: `app/internal/skillpaths/skillpaths.go:27-31` carries the
  quoted *"the drop-in for the removed skillpath RPC client"* comment + the `sessionReader` interface;
  `jobsimwiring/wiring.go:123` @ `9d00a313` is `jsrunner.NewRunnerManager(getenv("JUDGE0_API_KEY"),
  getenv("JUDGE0_BASE_URL"))`.
- **RE-DERIVED only** — *"At least 13 Connect-RPC services"*: the prose names exactly 13, and the doc is
  explicit that `proto` is in no clone set so this is a floor, not a count. I could not enumerate the
  predicate (the source of truth is unreadable here); the doc says so itself.

### `next-web-app.md`
- **ENUMERATED** — all four Next apps declare `"next": "~16.2.12"` (web/hiring/integration/maintenance);
  `apps/mobile` declares none; `pnpm-lock.yaml` resolves `next@16.2.12` and `react@19.2.7`. Root
  `package.json`: `node >=24.0.0`, `pnpm@10.30.3`, `turbo ^2.9.6`. `UPGRADE-IMPACT-next16.md` and
  `knowledge/next15-adoption-plan.md` both exist; `CLAUDE.md:15` = *"Turborepo · pnpm · Next.js 16 App
  Router · TypeScript strict."*
- **ENUMERATED** — `proxy.ts` exists in web/hiring/integration and `git grep -l HEAD -- '*middleware.ts'`
  returns **nothing**; the repo's own `CLAUDE.md:55` states the Next-16 rename verbatim.
- **ENUMERATED** — **one** Dockerfile tracked in the repo (`Dockerfile.dev`, `FROM node:24-alpine`,
  `pnpm turbo build --filter=@anthropos/web-app` at `:34`).
- **ENUMERATED** — **8** locale dirs under `configs/i18n/messages/` (de,en,es,fr,it,ja,nl,pt).
- **ENUMERATED** — no `storybook` script anywhere and no `.storybook/` dir; the only trace is
  `configs/tailwind/storybooks.css`, exactly as stated.
- **ENUMERATED** — platform `0c91421` `docker-compose.yml` declares exactly the five services listed
  (`sentinel:5`, `backend:28`, `studio-desk:112`, `next-web-app:143`, `gotenberg:170`); `:151` build arg,
  `:160` runtime env, `:165-167` `depends_on: backend`, `:110` `profiles: [core, backend, all]`;
  `Makefile:119-120` `up-frontend` → `--profile core --profile frontend`; at `0dab54d` `:236` is the
  build arg. The `PROFILE=frontend` exits-1 mechanism is structurally confirmed.
- Anchors: `apps/hiring/.../InsightsByMembersContainer.tsx:108` = `export function
  InsightsByMembersContainer()`; the hiring `[simId]/page.tsx:14` = `<InsightsByMembersContainer />`;
  `apps/web/.../template.tsx:90` = `const isEnterprise = Boolean(organization)`;
  `FreeTrialContainer.tsx:29` = `Boolean(!isHiringOrg && organizationId)`. Ports 3000/3001/3002/3031 all
  verified from the apps' own `dev` scripts. `security/printToken` exists.
- rext: `demo-stack/frontend/hiring.Dockerfile` exists and `up-injected.sh:1076-1085` brackets
  `build_frontend_hiring()` (image `demo-$N-hiring`, port 3001).

### `clerk-integration.md`
- **ENUMERATED** — `colony/authn` is imported by **129** `.go` files in `app` and **0** in each of
  sentinel / storage / messenger.
- **ENUMERATED** — exactly **12** webhook event types in `app/internal/clerk/events/events.go`
  (`grep -c '^\s*case "'` → 12): user.{created,updated,deleted}, organization.{created,deleted,updated},
  organizationInvitation.{accepted,created,revoked}, organizationMembership.{created,deleted,updated}.
- **ENUMERATED** — **8** locales imported from `@clerk/localizations` in
  `apps/web/src/providers/Clerk.provider.tsx:9-18` (deDE,enUS,esES,frFR,itIT,jaJP,nlNL,ptPT).
- **ENUMERATED** — the SDK-drift table is exactly right at each clone's own HEAD:
  `@clerk/nextjs` `^6.39.6` in web/hiring/integration, **absent** in maintenance, `^6.39.2` at
  `ant-academy/code/package.json:52`; `@clerk/clerk-expo` `~2.6.18` at
  `next-web-app/apps/mobile/package.json:6` vs `~2.19.36` at `ant-academy/mobile/package.json:18`
  (thirteen minors apart); `clerk-sdk-go/v2 v2.7.0` at `app/go.mod:31` **@ the pinned `5ba17044`**
  (still v2.7.0 at HEAD, where it has moved to `:29`).
- **ENUMERATED** — `AuthRole()` has **0** call sites anywhere in `app` (`git grep -c 'AuthRole()' HEAD --
  '*.go'` → 0), so *"zero call sites on the allow/deny path"* is if anything understated.
- **ENUMERATED** — no `template:` argument on any `getToken` call; the single `getToken({…})` site
  (`apps/hiring/.../TokenValidationClient.tsx:34-36`) passes only `skipCache`.
- **ENUMERATED** — the "not used" list holds: `@clerk/themes` 0, `<Protect` 0, `UserButton` 0,
  `OrganizationProfile` 0, `GoogleOneTap` 0, `Waitlist` 0. `OrganizationSwitcher` returns 7 hits, and
  **all seven are the repo's own `FourOhFourOrganizationSwitcher`**, not Clerk's component — so *"org UI
  is custom"* survives the near-miss. Positive control: `<SignIn` 5, `<UserProfile` 3, `<SignUp` 3, all
  of which the doc lists as USED.
- Confirmed: `apps/web/.../api/metabase/route.ts:35` checks `authData.orgRole !== 'admin'` (the bare
  form); `e2e/package.json:17-18` declares `@clerk/backend` + `@clerk/testing`; studio-desk declares
  `@clerk/clerk-js ^5.52.3` + `@clerk/express ^1.3.47`; `app/go.mod` declares `svix/svix-webhooks`;
  `ant-academy/code/proxy.js` runs `clerkMiddleware` with the `REQUIRE_ORGANIZATION_MEMBERSHIP` →
  `/no-organization` gate (default on, `:91-96`, `:310-327`).

### `skiller.md`
- **ENUMERATED** — the `.tf` claim reproduces exactly: **44** tracked `.tf` files across the **13**
  top-level `stack-demo` clones (app 5, jobsimulation 6, storage 5, sentinel 4, messenger 4, cms 4,
  studio-desk 4, graphql-wundergraph 4, roadrunner 4, rosetta-extensions 2, next-web-app 2, platform 0,
  ant-academy 0), and `backend.internal.anthropos` has **0** hits among them. (Tree-wide the string does
  exist — `app/knowledge/service-dependencies.md:52`, `graphql-wundergraph/CLAUDE.md:18` +
  `supergraph-config-prod.yaml:6`, and rext test fixtures — but none is a `.tf`, which is precisely the
  scope the sentence claims. Positive control: `skiller_rpc_addr` **does** appear in
  jobsimulation/messenger/cms `.tf` files, so the search set was live.)
- **ENUMERATED** — at `d11a403^` compose sets `SKILLER_RPC_ADDR=http://backend:8083` in all four service
  blocks (`:62`, `:121`, `:174`, `:265`) while `CMS_RPC_ADDR=http://cms:8091` and
  `JOBSIMULATION_RPC_ADDR=http://jobsimulation:8401` still address the husks — so *"it already did before
  `d11a403`"* is exactly right; `d11a403` re-points only the other two.
- **ENUMERATED** — `838d907` is merged as `0c91421`, committed **2026-08-05**, as stated.
- **ENUMERATED** — **8** `ContentLanguage` members (`assignments.graphqls:44-53`).
- Confirmed: all six named Ent schema files exist; `app/internal/rpc/skillerrpc/` exists;
  `.../graph/schemas/skiller_taxonomy.graphqls` exists and its `:7` records `categoryTree` /
  `fullCategoryTree` as unported ("no consumers"); `skill_embeddings` / `job_role_embeddings` created in
  `public` with `extensions.vector(1536)` + `USING IVFFLAT` (`20260615130000_skiller_taxonomy.sql:51-61`,
  `:240-249`); `skill_translation.go` + `job_role_translation.go` present; `text-embedding-3-small`
  referenced in `app`.
- The taxonomy-figures statement (`≥22,470` / `≥42,790`, "18K refuted / 60K unverified") is stated
  identically in `skiller.md:52-61` and `shared_libraries.md:229-245` — **no contradiction between the
  twins**, and the arithmetic it shows (18,919 = `job_role_embeddings` rows) is internally consistent.
  I could not re-derive the counts (prod capture — see below), so this is **RE-DERIVED, not ENUMERATED**.

### Cross-document anchors checked (all resolve to the named construct)
`backend.md:123` (`## Skiller-in-app merge — fact-sheet (v2.1 "quick change")`),
`stories-spec.md:657` (`### The M223 hiring chain — two seeders …`),
`seeding-spec.md:375` (`### The recruiter vantage — the hiring org + candidate-comparison funnel …`),
`demopatch-spec.md:479` (`## 8. Additive-UI injection …`),
`latency-budget.md:349` + `:416`, `storage.md:129`, `external_services.md:579`,
`dependency_map.md:78`, `shared_libraries.md#taxonomy-figures` (the `<a id>` at `:228`).
The **only** cross-document anchor that fails is `service_taxonomy.md:62` (B2).

---

## What I could not settle

- **`colony`, `proto`, `taxonomy`, `authn` internals.** These four repos are private Go modules and are
  in **no** clone set (correctly stated by the document itself). So every claim about colony's package
  contents (`NewDBStdConn`, the 30 s health monitor, the commented-out rate-limiter reject path, the
  `authorizer` package-name gotcha), proto's `buf v1.57.0` CI pin and `go/simulator/*` layout, taxonomy's
  *"131-line `node.go`"* and `NodeID` format, and authn's `v1.7.0` tag / missing-`return` bug is
  **unverifiable from this tree**. Settling them needs those repos cloned (or a `go mod download` cache).
  I verified the *consumer-visible* halves that are reachable (the `go.mod` requires, the
  `legacyStorage` import of `go/simulator/storage/v1`, `roadrunner`'s `colony.NewVersionConfig` use).
- **Prod-only facts.** `skiller.md` / `hiring.md`'s *"in production the `jobsimulation` schema survives,
  frozen, until M710"*, the cms/skiller ECS teardown state, and `studio-desk.md`'s *"the Cosmo Router
  survives in production only"*. The last one is at least **consistent** with
  `graphql-wundergraph/terraform/main.tf:20` `service_desired_count = 1`, but the deciding declarations
  live in the `infrastructure` repo, which has never been in a clone set — the corpus says so and does
  not over-claim.
- **Snapshot-derived counts.** *"87 real `SIMULATION_TYPE_HIRING` sims"*, *"0 `job_position` rows"*, and
  the `≥42,790 / ≥22,470` taxonomy floors all come from `.agentspace/snapshots/…/manifest.json`, which is
  not readable as ground truth here. Settling them needs the capture manifest or a live stack DB.
- **Measured demo timings.** *"recruiter p95 click→ACCESS 1.27 s"*, *"5/5 (Talent-Mgr 8, BD-Lead 8,
  Inside-Sales 9, Project-Mgr 9, AWS-Security 8)"*, *"skeleton-visible p95 4669 → 817 ms"*,
  *"canAccess 4049 → 38 ms"*. Settling them needs a live `billion` / `demo-N` run. I note the per-position
  numbers sum to **42**, against 45 candidates minus the assigned-only hero — plausible but unchecked.
- **`directus/directus:11.6.1` internals** — `studio-desk.md:123` cites `bootstrap/index.js:81` inside the
  pinned upstream image. Settling it needs `docker run --entrypoint cat` against that image tag.
- **`app` PR #989** (`skiller.md:39`) — PR numbers are not resolvable from a bare clone.
