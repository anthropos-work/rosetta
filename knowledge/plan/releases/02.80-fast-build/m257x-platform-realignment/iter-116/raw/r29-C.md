# Seat C — reading #29/#30 (M257x iter-116)

## Refs I graded against (re-derived at this reading's open, matching the ADDENDUM)

| repo | path | HEAD | origin/main |
|---|---|---|---|
| app | `stack-demo/app` | `ad9f3c49` | `ad9f3c49` |
| app/studio (nested, own repo) | `stack-demo/app/studio` | `aeec036a` | — |
| platform | `stack-demo/platform` | `0c91421d` | `0c91421d` |
| next-web-app | `stack-demo/next-web-app` | `8297c684` | `f97ba659` |
| sentinel | `stack-demo/sentinel` | `f2c46190` | `f2c46190` |
| storage | `stack-demo/storage` | `4ce8ece5` | `9f8cb532` |
| messenger | `stack-demo/messenger` | `fa47850d` | `e9421c68` |
| cms | `stack-demo/cms` | `ca50c817` | `f38c0c4a` |
| jobsimulation | `stack-demo/jobsimulation` | `462343b0` | `82cb66ec` |
| roadrunner | `stack-demo/roadrunner` | `87d8d443` | `87d8d443` |
| graphql-wundergraph | `stack-demo/graphql-wundergraph` | `60c229f3` | `60c229f3` |
| studio-desk | `stack-demo/studio-desk` | `41ee3575` | `41ee3575` |
| ant-academy | `stack-demo/ant-academy` | `22df69dd` | `22df69dd` |
| rext — **pinned per-stack** | `stack-demo/rosetta-extensions` | `09d06070` | `4cb920aa` |
| rext — authoring copy | `.agentspace/rosetta-extensions` | `1dc1eb82` | — |

**Which rext tree settled what (§5 rule 45 / rule 50):** every `alignment_testing.md` code claim was
checked in the **authoring copy `1dc1eb82`**; `gate.sh` was **diffed against the pinned clone `09d06070`
and is byte-identical over the cited window**, so the verdict does not depend on the choice. The
`platform_alignment_guard` run I report below was executed **from the authoring copy** (the runner
printed `fence-tree: /Users/marco/workspace/anthropos/rosetta/.agentspace/rosetta-extensions @
1dc1eb824 · describe=fast-build-m257x-iter-101-15-g1dc1eb8`), which is the tree its configuration lives
in — rule 50.

**Instrument note:** one search in this pass returned `0` from a *broken pipeline* (zsh expanded
`$r:repos.yml` as the `:r` history modifier, so `git show` failed and every count read `0`). Caught by
reading stderr + a positive control (§5 rules 1/2) and re-run correctly. No count in this report comes
from a command whose stderr I did not read.

## Positive controls

| file | `wc -l` | read in full? |
|---|---|---|
| `corpus/architecture/alignment_testing.md` | 542 | yes |
| `corpus/architecture/ai_architecture.md` | 340 | yes |
| `corpus/architecture/platform-migration-status.md` | 330 | yes |
| `corpus/services/coursebuilder.md` | 161 | yes |
| `corpus/services/skillpath.md` | 117 | yes |
| `corpus/services/gotenberg.md` | 84 | yes |

(`skillpath.md` renders 118 numbered lines in a reader because its last line carries no trailing
newline; `wc -l` counts 117.)

---

## Blockers

### B1 — `ai_architecture.md:92` cites the OpenAI table row as `:86`; `:86` is prose, the row is `:91`

- **Anchor:** `corpus/architecture/ai_architecture.md:92` — *"The row's construction rule is **enumerate
  the constants** (its OpenAI sibling at `:86` is an exact enumeration of
  `internal/ai/openai/config.go:8-26`)"*
- **Why it is false:** *"its OpenAI sibling"* is unambiguously the OpenAI row of this same table.
  Measured at rosetta HEAD:
  - `:86` = `The **Default client** column is the client the vendor const resolves to — *not* a position in a`
  - `:87` = `fallback order (there is none; see above).`
  - `:89`/`:90` = the table header + separator
  - `:91` = `| **OpenAI (Azure EU + Direct US)** | GPT-5.4, GPT-5.4-mini, …`
  So the anchor lands on a wrapped prose sentence, five lines above the row it names. The *substantive*
  claim is correct and I re-derived it: `internal/ai/openai/config.go:8-26` @ `ad9f3c49` declares
  exactly 11 model consts (`GPT4Dot1`, `GPT4Dot1Mini`, `O3`, `O4Mini`, `GPT5`, `GPT5Mini`, `GPT5Nano`,
  `GPT5_1`, `GPT5_2`, `GPT5_4`, `GPT5_4_Mini`) and the row lists exactly those 11. Only the intra-doc
  anchor is wrong.
- **Shared cause with B2/B3:** all three are exactly **−5 lines**, the signature of `platform-alignment.md`
  §5 rule 34 (an edit inserted ~5 lines above the table and the intra-corpus self-citations were never
  re-derived from `git diff -U0`).
- **Confidence:** high

### B2 — `ai_architecture.md:94`'s *"see `:95` below"* lands on the Transcription table row, not the ai-module-fold paragraph

- **Anchor:** `corpus/architecture/ai_architecture.md:94` — *"…they belong to the **standalone**
  `github.com/anthropos-work/ai` module … — a module **no repo a stack builds requires** since the fold
  at `1e457fa70` (see `:95` below); they were dropped in the fold…"*
- **Why it is false:** `:95` = `| **Transcription** | GPT-4o Transcribe | Azure EU (US via
  `flag_use_azure_us`) |` — a table row about speech-to-text, which says nothing about the module fold.
  The passage that substantiates the claim is `:100` (*"Go services access AI through one `ai.AI`
  interface — but **it is no longer a shared private module for any service a stack builds**"*) and
  `:106-109`. Applying the same −5 drift as B1/B3, `:95` was `:100` before the insertion, which pins the
  cause exactly.
  I confirmed the underlying claim independently: at `ad9f3c49` neither `app/go.mod` nor
  `sentinel/go.mod` (`f2c46190`) requires `github.com/anthropos-work/ai` (positive control:
  `anthropos-work/colony` returns 1 hit in `app/go.mod`), while `cms/go.mod:9` @ `f38c0c4a` and
  `jobsimulation/go.mod:11` @ `82cb66ec` both require `v1.40.2`; `1e457fa70` is
  *"refactor(ai): fold the ai library into app as internal/ai"* (2026-08-04). The claim is right; the
  pointer is not.
- **Confidence:** high

### B3 — `ai_architecture.md:295` cites the Anthropic table row as `:87`; `:87` is prose, the row is `:92`

- **Anchor:** `corpus/architecture/ai_architecture.md:295` — *"**NB this bullet is not a claim about the
  platform's Anthropic model SET** (that is the table row at `:87`, which is where the 4.6/Opus-4.8
  omission lived)"*
- **Why it is false:** `:87` is `fallback order (there is none; see above).` — the tail of the prose
  sentence introducing the table. The Anthropic row is `:92`. A reader chasing *"the table row at :87"*
  to check the model set lands on a sentence about the *Default client* column.
  The set claim itself is correct and I **enumerated** it:
  `app/internal/ai/anthropic/completion.go:20-30` @ `ad9f3c49` holds exactly 6 consts over 5 families
  (3.5-AWS `:21`, 3.5-direct `:22`, 3.7 `:23`, 4 `:24`, 4.5 `:25`, 4.6 `:29`), and Opus 4.8 is outside
  that block at `internal/coursebuilder/bedrock.go:23`. Only the anchor is wrong.
- **Confidence:** high

### B4 — `ai_architecture.md:131` attributes `studio/configs/production_config.ini` to `app` HEAD; that path is in **no** `app` commit

- **Anchor:** `corpus/architecture/ai_architecture.md:131` — *"Measured at `app` HEAD against the
  shipping `studio/configs/production_config.ini:26-36`"*
- **Why it is false:**
  ```
  $ git -C stack-demo/app show ad9f3c49:studio/configs/production_config.ini
  fatal: path 'studio/configs/production_config.ini' exists on disk, but not in 'ad9f3c49'
  $ git -C stack-demo/app ls-tree --name-only ad9f3c49 studio      # → empty
  $ git -C stack-demo/app show ad9f3c49:.gitignore | sed -n '78,79p'
  # Python studio runtime (anthropos-studio-room) — pulled at build via additional_repo, like cms
  studio/*
  ```
  The file lives in the **nested `anthropos-studio-room` checkout** at its own ref `aeec036a`
  (`stack-demo/app/studio`). This is precisely the mechanism `platform-alignment.md` §5 rule 44 records
  (*"`git -C app grep <anything> HEAD -- studio/` returns 0 for every predicate — a guaranteed zero that
  reads like evidence"*, which *"minted an 'imported nowhere' claim that stood in four documents"*), and
  `external_services.md` carries an explicit corpus-wide callout for it: *"**`app/studio/**` is an
  IN-IMAGE path, and it is in no `app` commit.**"* **This same file gets it right 89 lines earlier**
  (`:42`: *"`git -C app/studio grep -i mistral aeec036a`, 22 hits / 3 files"*), so `:131` is the
  unrepaired twin of a predicate the corpus has already adjudicated.
  The slot table itself is correct — I verified `configs/production_config.ini:26-36` @ `aeec036a`
  line-for-line (FAST/STRICT `azure, gpt-5-mini, none`; EXECUTION `azure, gpt-5.4, none`; CREATIVE
  `…, low`; REASONING `…, medium`; stable == experimental; `development_config.ini:26-36` identical).
- **Why I hesitated:** the file *is* present on disk under the `app` workspace, so a reader using the
  filesystem rather than `git show` would find it. I book it because *"at `app` HEAD"* is a git-ref
  phrase, the ref resolution fails hard, and the corpus fences this exact class in two other documents.
- **Confidence:** medium

### B5 — `ai_architecture.md:326` calls it *"the shared `ai` library"*, contradicting this file's own :100-108 retraction

- **Anchor:** `corpus/architecture/ai_architecture.md:326` — *"…fed by `Event_AiUsage` messages that the
  AI-consuming services publish over Redis Streams (**the shared `ai` library** itself only returns
  provider token counts)"*
- **Why it is false / self-contradictory:** `:100-101` of the same file states *"Go services access AI
  through one `ai.AI` interface — **but it is no longer a shared private module for any service a stack
  builds**"*, and `:106-108` proves it (`1e457fa70`; at `ad9f3c49` neither `app/go.mod` nor
  `sentinel/go.mod` requires `github.com/anthropos-work/ai`; the library lives at `app/internal/ai/`).
  The retracted descriptor is then republished 218 lines later, in the section a reader lands on when
  asking *"where is AI cost tracked?"*. The passage at `:104-107` explicitly instructs *"**When you
  repair this predicate, repair both sites or neither** (`TOK-07` rule 3)"* and names the exact harm —
  *"a reader who met that sentence first got the pre-fold answer with no indication a correction
  existed"* — which is what `:326` now does inside the very file that says it.
  (The rest of `:326` is correct: `internal/aiusage/ai_usage.go` exists, `Event_AiUsage` is handled at
  `:98` and published from `internal/jobsimulation/ai/ai.go:207`,
  `internal/jobsimulation/agent/report_agent.go:636` and `internal/skillerai/ai.go:236,302` over
  `colony/pubsub`.)
- **Why I hesitated:** it is one adjective, and the mechanical claim around it is true. I book it
  because the briefing makes self-contradiction a finding on its own, and because it is a member of the
  single class (`repaired one twin, left the other`) that §5 rule 21 measured as 8 of 9 repair-induced
  defects.
- **Confidence:** medium-high

---

## Minors

- `ai_architecture.md:264` — *"the enum is `app/internal/cms/directus/collections/jobsimulation.go:983-990`"*.
  The `AIModel` const block @ `ad9f3c49` is `:977-991` (consts `:980-990`); `:983-990` omits
  `Anthropic35SonnetAws`, `Anthropic37SonnetAws`, `Anthropic4SonnetAws`. All three examples the sentence
  gives are inside the cited range, so nothing is misstated — the range just under-covers the enum.
- `ai_architecture.md:40` — *"`studio/tools/pdf2md.py:24` (`mistral-ocr-latest`)"*. `:24` @ `aeec036a` is
  `from mistralai import Mistral`; the `model="mistral-ocr-latest"` literal is at `:127`. The file and
  the OCR-only characterisation are right; the anchor points at the import rather than the model call —
  the same distinction this file itself draws when it retracted `markdownManager.go:19` for `:30`.
- `ai_architecture.md:65` — `external_services.md:619-629` over-ranges: item 4 of the four EU-exit routes
  begins at `:622`; `:619-621` is item 3's tail. `:567` (the Anthropic-Direct provider row) is exact.
- `ai_architecture.md:311` — *"`calculateSkillScore` (`v3/validator/skills.go:53-64`)"*. The function is
  `:53-62`; `:64` is `func (s skillsValidator) run(…)`. (`:75` for `passed/total*100` is exact.)
- `alignment_testing.md:3` — *"**Last updated:** 2026-06-06"* while the body carries M218/M219 material
  and an explicit *"Corrected M257x iter-85"*; `git log` shows the file last changed 2026-08-06
  (`00be1ac`). Stale in the safe direction, but the header contradicts its own body.
- `alignment_testing.md:536-538` (Layout block) — lists `internal/dna`, `internal/outcome`,
  `internal/compare`, `internal/report`; `alignment/internal/` also holds **`canon`** (the
  precision-preserving canonical-JSON package the doc describes in prose at `:125-126`). Omitted list
  member.
- `coursebuilder.md:129-130` — *"~55 test files"* / *"~25 boundary test files"*. Measured at `ad9f3c49`:
  **61** `_test.go` under `internal/coursebuilder/` and **33** under
  `internal/web/backend/coursebuilder/`. Both are `~`-hedged, but both are low by 11 % / 32 %.
- `skillpath.md:107` — *"the body renders the literal string **\"Coming soon\"**"*. It renders
  `t('enterprise.insights.comingSoon')`; `configs/i18n/messages/en/enterprise.json:1891` resolves it to
  `"Coming soon"`. Substantively right, "literal" is not.

---

## Positively cleared (audited zeros)

**ENUMERATED** — predicate re-derived from source independently, cardinalities compared:

1. **`gotenberg.md:47` — "the nine MIME types `docconv` can't read directly (`extract.go:17-27`)".**
   `gotenbergMimes` @ `app` `9d00a313` has exactly 9 entries (`:18-26`): xls, xlsx, ppt, doc, odt, ods,
   odp, `application/rtf`, `text/rtf`. DOCX is genuinely absent, as claimed.
2. **`gotenberg.md:42` — "The backend service (`app`) is the only consumer".** Grepped `GOTENBERG`
   across **all 15 git trees** in `stack-demo` at each tree's own ref (nested `app/studio` and
   `cms/studio` @ `aeec036a` included): `app` 48, `platform` 12 (compose), `rosetta-extensions` 96
   (tooling), **0 everywhere else**.
3. **`platform-migration-status.md:75-81` — the row-set completeness derivation.**
   `git log -p --follow -- repos.yml` yields exactly **14** distinct `- name:` values ever (all 14 have
   rows). The same command on `docker-compose.yml` yields **26** two-space keys, of which `app-network`
   is the `networks:` entry (`docker-compose.yml:185-186` @ `0c91421` — verified) ⇒ **25** services, and
   I checked all 25 map to a row (`backend`→`app`, `graphql`/`wundergraph`→`graphql-wundergraph`).
4. **`platform-migration-status.md:92` — the `STORAGE_RPC_ADDR` split.** At `2035f9a`: **3** hits in
   `*.go`, all three comments (`internal/jobsimwiring/wiring.go:101`,
   `internal/storagens/callsites_test.go:189`, `main.go:504`). At `b948604`: **15** hits, and the
   document's own 7/8 decomposition reproduces exactly — 7 env lookups (`main.go` ×3 at `:446`/`:524`/
   `:992`, `wiring.go:115`, one in each of `cmd/academy-asset-upload`, `cmd/academyImport`, `cmd/import`),
   and the other 8 are 2 doc comments, 2 error strings, 3 `t.Setenv` and 1 `t.Fatal` message. The
   "six spelled `os.Getenv`, the seventh a lowercase `getenv` helper" detail also holds (`wiring.go:115`).
5. **`platform-migration-status.md:87` — the "five commits, no Go source" currency note.**
   `git rev-list --count 2035f9a4..ad9f3c49` = **5**; `git diff --stat` touches exactly
   `.claude/skills/publish/SKILL.md`, `CLAUDE.md`, `knowledge/deployment.md`, `terraform/main.tf`,
   `terraform/variables.tf`; **0** `.go` files; and the single `terraform/main.tf` hunk is one
   `error_message` string rewritten in place. Every claim in that sentence is exact.
6. **`platform-migration-status.md:92` — "asking compose for a retired token … starts only the always-on
   floor (postgresql, redis, sentinel)".** Derived the floor from source: the services declaring **no**
   `profiles:` key @ `0c91421` are `sentinel` (`docker-compose.yml:5-26`) plus `postgresql`
   (`common.yml:2`) and `redis` (`common.yml:24`) — exactly 3; `backend` `:110`, `studio-desk` `:141`,
   `next-web-app` `:168` and `gotenberg` `:183` all declare one.
7. **`platform-migration-status.md:90` — the compose-service counts across the pruning commits.**
   Enumerated the `services:` keys: `d11a403^` = **11**, `d11a403` = **8**, `0c91421` = **5**. And
   `git show d11a403 -- repos.yml` does delete `- name: roadrunner` alongside cms and jobsimulation, so
   the row's *"the message is wrong; the diff is the fact"* is correct.
8. **`skillpath.md:49` — the `repos.yml` entry counts.** `2adcf71` = **9**, `ef32d4c` = 6,
   `0dab54d` = **6**, `838d907` = **4**, `0c91421` = 4. (This is the count my first, broken pipeline
   returned as `0` for every ref.)
9. **`coursebuilder.md:77-94` — the 16 SSE event names.** Enumerated `renderEvent`
   (`internal/web/backend/coursebuilder/handler.go:2604-2772` @ `ad9f3c49`): the switch returns exactly
   **15** distinct names at the exact lines cited (`text` 2607, `score` 2609, `patch_applied` 2614,
   `patch_skipped` 2620, `stage` 2637/2639, `outline` 2654, `progress` 2661, `preview_ready` 2671,
   `draft_kept` 2678, `error` 2704, `translation_ready` 2724, `rebuild_required` 2732,
   `steering_received` 2744, `steering_applied` 2754, `done` 2768 always last, `default:` re-emitting
   `text` at 2770), plus `session` written directly at `:1458` right after `beginSSE` = **16**.
   `case cb.EventCost:` `:2709` returns `""` at `:2717` and the loop skips `writeSSE` at `:1478-1480`.
   Every anchor in that bullet is exact.
10. **`ai_architecture.md:34-42` — "*Every* use of Mistral in `app` is OCR, never generation".**
    Enumerated at each tree's own ref: Go side `internal/cms/studio/markdownManager.go:30` (constructor
    body inside `func NewMarkdownManager` at `:29`; `:19` is the doc-comment, exactly as the doc says)
    and `studioManager.go:583`. Python side, `app/studio` @ `aeec036a`: `git grep -ci mistral` sums to
    **22 lines across 3 files** — the document's own figure — and I read all three: `requirements.txt:8`
    (`mistralai`), `tools/pdf2md.py` (18, the OCR CLI), `tools/r3.py` (3, all `MISTRAL_API_KEY`
    presence checks for the PDF→Markdown step). No generation call anywhere. `bedrock|boto3` under
    `app/studio` @ `aeec036a`: **0**.
11. **`ai_architecture.md:67-70` — the four-case switch vs five-member Directus enum.**
    `internal/jobsimulation/simulator/ai/ai.go` @ `ad9f3c49`: `case simulation.Openai:` `:58`,
    `Azure` `:69`, `AnthropicAws` `:86`, `Anthropic` `:102`, `default:` `:114-115` → `internalAi.Openai`.
    `internal/cms/directus/collections/jobsimulation.go:967-975` declares **five** `AIVendor` consts;
    `Azureglobal` (`:971`) is the one with no case. Exact.
12. **`ai_architecture.md:299-306` — "`checkerEngines` … stored and never read".** All 6 occurrences of
    the identifier in Go @ `ad9f3c49` (`v3/validator/validator.go:43,60,595`;
    `basevalidator/criterion.go:34,72,90`) are a field decl, a map literal, a pass-through argument, a
    parameter and an assignment — **no map lookup anywhere**. The real dispatch is the hardcoded switch
    at `criterion.go:126-155` (`case check.EngineLlm:` `:127`, `case check.EngineTextDiff:` `:142`),
    `NewLLMBulkChecker(c.logger)` at `:428`, `validateCodeDiff` launched concurrently at `:168` and
    implemented `:450-475` as a pure `cdiff.Diff == ""` comparison. `bulkChecks.go:118` is
    `ai.WithTemperature(0.0)`.
13. **`alignment_testing.md:259-265` — the five DNA gene/capability counts.** Parsed each DNA in the
    authoring copy: `clerk-2.6.0` 14 caps / 27 genes, `clerk-js-5` 6/9, `clerk-multi-1` 5/9,
    `clerk-deploy-1` 3/7, `clerk-express-1` 5/13 — all five exact.
14. **`alignment_testing.md:208-211` — "Only `clerk-2.6.0` declares a `consumed_surface`".** Parsed all
    five: `clerk-2.6.0` has 15 entries, the other four have none. The `covered_by` example the doc gives
    (`clerk-express-1:ClerkClientBAPI/get-organization`) is verbatim in the file.
15. **`alignment_testing.md:219-224` — "Three genes named identity — `ExtractIdentity`, `Me`,
    `DeployIdentity` — and all three assert `universal-user`".** Enumerated every capability of all five
    DNAs: `universal-user` occurs exactly three times, on `DeployIdentity` (clerk-deploy-1),
    `ExtractIdentity` (clerk-express-1) and `Me` (clerk-js-5). `GetUser` now carries variants
    `hero-a` + `hero-b`, as the fix claims.
16. **`alignment_testing.md:328-330` — "the reusable `alignment/` section has no `scripts/` dir at all
    (it holds `cmd/`, `internal/`, `examples/`, `go.mod`, `README.md`)".** `ls-tree` returns exactly
    those five and no `scripts/`, in **both** rext trees.
17. **`alignment_testing.md:505` — "NOTHING is left waived → 100% coverage".** Parsed
    `stack-seeding/dna/data-dna.json`: 10 surfaces, statuses `{seeded-m7a: 4, seeded-m7c: 4,
    snapshot-seeded-m9b: 1, snapshot-seeded-m10: 1}`, **zero** waived — and `dna.go:316-325`'s
    `Coverage()` does count seeded OR snapshot-seeded over a non-waived denominator.
18. **`alignment_testing.md:475-486` — "six two-sided operators (`snapshot.go:62`)".**
    `validSnapshotOperators` at `:62-69` holds exactly the six named. `data-dna.json:410-415` gives the
    taxonomy surface five (all but `cross-surface-closure`); `:439-445` gives content five (no
    `embedding-dim`, plus `cross-surface-closure`). Both exact.

**RE-DERIVED** — arithmetic the document itself displays, recomputed (weaker, per §5 rule 24):

- `alignment_testing.md:304-312` (toy run) — 6 genes: `Add` critical (3 genes × w3 = 9) + `Greet`
  standard (3 × w2 = 6) = 15; the one divergence is `Greet/padded-name` (w2) ⇒ 13/15 = **86.7 %**,
  critical 100 %, 5/6 aligned. Matches the pasted output including `(exact, w2)`.
- `alignment_testing.md:269` — the historical **97.2 %**: `clerk-2.6.0` has 17 critical genes (w3) +
  10 standard (w2) = weight 71; failing `MembershipOrgIdentity/real-org-eid` (w2) ⇒ 69/71 = 97.18 %.
- `platform-migration-status.md:204-205, 212-224` — I *ran* the fence rather than trusting the table:
  `platform_alignment_guard.py` @ authoring copy `1dc1eb824` reports
  `assertion F resolved 96 citation(s) — 25 subject-checked, 70 range-only, 1 outside any service
  block; 0 unresolvable; 0 read from the WORKTREE` and `OK — … agree in both directions`, exit 0.
  25 + 70 + 1 = 96, and all three figures match the recipe block the document prints. §1's state
  vocabulary is 8 terms, matching assertion C.

**Individually re-resolved and correct** (anchor + construct, not just existence). Every one of these
was opened with `sed`-style context, not grepped to:

- **`gotenberg.md`** — the whole file is clean. `docker-compose.yml:183` `profiles: [core, backend, all]`
  @ `0c91421`; the re-anchoring note is right too (`:268` @ `0dab54d` *was* that line, and the file is
  186 lines). `extract.go:77`→`:81` convert-then-discard; `:59` and `:43-45` the empty-`GOTENBERG_URL`
  degrade; `user_import_resume_2d.go:68` / `:73` (`ocrInput = pdfBytes`) / `:69-74`; the 90 s client
  timeout (`converter/gotenberg.go:13`), the signature, and `POST /forms/libreoffice/convert` (`:31`);
  `GOTENBERG_URL=http://gotenberg:3200` at `docker-compose.yml:57`.
- **`skillpath.md`** — `session.go:205-207` (the `cms-in-app deseam` comment → `GetSkillPathDomain`);
  `skillpaths.go:88-95`; **`SkillPathSessionService` = 0 occurrences in Go @ `ad9f3c49`** (positive
  control `SessionManager` returns hits), while `app/CLAUDE.md:109` and `app/knowledge/architecture.md:28`
  both still list it — exactly the Trap-C framing the bullet gives; all 8 GraphQL operations exist in
  `skillpath_sessions.graphqls` with `createSkillPathSession` `@deprecated`;
  `intelligence.go:1144` + `:1159-1170` (skill_path_id + `StatusIn(Active, Completed)` + tenant
  predicate); migration `20260729133514.sql:62`/`:63` drop both mirrors and **three** migrations
  post-date it at `9d00a313`, named correctly; `SKILLPATH_STREAM=skillpath` survives at
  `docker-compose.yml:72`; the per-member route at next-web-app `8297c684` is confirmed unbuilt
  (`userData` hardcoded `null`, results table + totals commented out `:140-160`).
- **`platform-migration-status.md`** — every `file:line` I could reach resolved to the named construct:
  `app/terraform/main.tf:181` `= 1`; `repos.yml:14-17`, `:18-20`, `:23-25`, `:26-28`, `:2-10`, `:12-13`
  (and `:9-10` @ `0dab54d` really does name infrastructure's `services.tf`); the seven `app/main.go`
  wiring sites at `2035f9a` (`395`, `524`, `525`, `547`, `690`, `721`, `751`, `1102`, `1153`, `1450`,
  `1471`, `1473`, `285`, `286`, `394`, `1416`, `504`) — every one exact;
  `app/env_guards.go:92-111`; `app/terraform/main.tf:415-416`/`:419-420`;
  `internal/storage/service.go:22`/`:24` (env-var *names*, as stated); `jobsimwiring/wiring.go:123`;
  `internal/roadrunner/` genuinely absent; `storage/terraform/main.tf` is 18 lines with `:9-11`,
  `:13-16`, `:18` as quoted and `custody` = **0** hits at `9f8cb532` (positive control `bucket`
  returns hits); `storage/terraform/storage.tf:22-25` (M903, 1 occurrence, no `moved` blocks) and
  `d3e6d32`'s message contains *"M903 never ran."*; the M903 plan front-matter really is
  `status: planned`; `messenger/terraform/main.tf:19-25`/`:27-28`/`:29` = 0 in a 121-line module **at
  `origin/main` `e9421c68`** (see caveat below); `messenger/cmd/root.go:120-140` reads all four addrs;
  **zero `*_RPC_ADDR` anywhere in compose** and `AUTHORIZATION_ADDRESS=http://sentinel:8087` at `:48`
  (and it really is a Connect-RPC edge — `internal/authorization/sentinel.go:34`);
  `cms/terraform/main.tf:39` = 0 **and** `6efa1d5`'s message + the deleted
  `.github/workflows/build-production.yml` both confirmed; `jobsimulation/terraform/main.tf:15-22` and
  `:24-40` @ `82cb66e` plus the four 2026-08-04 commits including the `GitHub`-committed PR #439 merge;
  `roadrunner/terraform/main.tf:19` = 1 with `git blame` naming **`84a4b4f` (2025-12-15)** and `e45eb61`
  confirmed as a pure source-URL swap; the wundergraph row (one subgraph, `schemas/backend.graphqls`
  alone, `subgraphs.conf` = `BACKEND=v1.360.0`, `terraform/main.tf:20` = 1); `common.yml:2`/`:22`/`:24`
  and `docker-compose.yml:1-2`; and every dated compose-history commit
  (`6060315`, `a2a3ee6` — which does delete the `directus:` block, `cb6ebf5`, `ef4b449`, `1474b1f`,
  `84862d1`, `b43b99a`/`c17cc9a` — which add and remove `realtime:`, `467965a`, `8770fe6`,
  `045857c`, `fdfa189`, `21429b7`, `a4db680`, `b56d731`, `360efd4`, `236771f`, and `915da06` in the
  *wundergraph* repo).
- **`coursebuilder.md`** — `terraform/variables.tf:759-763` (sensitive, no default), `ssm.tf:328-333`
  (SecureString; verified at **both** `b948604f` and `ad9f3c49`, as the parenthetical claims),
  `main.tf:757-758` (SSM ARN injection), and the two *retracted* anchors are as described
  (`variables.tf:631-645` is the cms-in-app secrets comment block; `main.tf:555` is
  `"name": "DIRECTUS_BASE_ADDR"`); `bedrock.go:23`/`:29`/`:98-103`/`:109-114`;
  `main.go:770` (the `ModelBackendName()` log) and `:766-779` with the two
  `logger.Warn(… routes disabled)` arms at `:774`/`:778`; `main.go:824-826` reading `OPENAI_KEY` +
  `CB_IMAGE_MODEL`, and `COURSEBUILDER_OPENAI_IMAGE_KEY` deleted by `68c24512` and surviving only in
  `internal/coursebuilder/{README,GO-LIVE-RUNBOOK}.md`; migration `20260717151144.sql` creating
  `course_builder_sessions` + `credit_transactions` + `organization_credits` with every named column
  present; all 20 engine files/docs present; `DefaultMaxMonthlyCOGSUSD = 500.0`,
  `DefaultMaxDailyCOGSUSD = 0.0`, `DefaultSessionsPerOrgPerDay = 50`,
  `coursebuilderWorkerConcurrency = 20`, `TypeCoursebuilderRun = "coursebuilder_run"`, credits
  `course.build` = 5/chapter, `course.refine` = flat 1, `course.translate` = per requested locale;
  the whole route table (including `cancel/publish/unpublish/duplicate/translate/cover`,
  `PATCH …/draft`, `DELETE …`) and the `cors + authn + courseBuilderAccessGate` group at
  `backend.go:227-233`; Go **1.26.4**; and the version arithmetic —
  `git describe ad9f3c49` = `v1.369.0-7-gad9f3c498`, `2035f9a4` = `v1.369.0-2-g2035f9a40`,
  `5ba17044` = `v1.363.2` (2026-07-31).
- **`alignment_testing.md`** — `gate.sh:69` with the rationale comment at `:58-68` (`:61` inside it) and
  defaults `RUNNER_PKG=./cmd/clerkrun`, `DNA=dna/clerk-2.6.0.json`, `ALIGN_DIR` → `../../alignment`,
  gate 95/100 — **byte-identical in the pinned clone `09d06070`**; `run.go:133-136`
  (`ExitRegressed = 2`, `ExitUnmeasurable = 3`) and `unmeasurable()` `:139-153` with the exact banner
  text quoted; `dnaCoverage` implementing the `--if-declared` semantics *precisely* as the table
  describes (structural errors → 2, empty `ConsumedSurface` → the `NO COVERAGE CLAIM` warning, 0 under
  the flag and 2 without); `dna.go:20-27` (`list|diff|validate|coverage`); `idPattern` =
  `^[A-Za-z0-9][A-Za-z0-9_-]*$`, `maxWeight = 1_000_000`, criticality weights 3/2/1, the no-critical-gene
  rejection at `dna.go:274-280` **and** `GateMet`'s scoring-time twin at `compare.go:56-61` with
  `critical_genes` on the report; golden path `<Capability>/<variant>.json` (`outcome.go:41-43`);
  `datadna` exit contract `0/1/3`; the directus public predicate
  `private = false AND tenant_id IS NULL AND status = 'published'` (`fidelity_probe.go:117`);
  `//go:build alignment` on the toy suite and its 80 % gate; `examples/toy/cmd/toyrun`; the five runners
  and five DNAs; `clerkenstein/deploy/colony-authn/clerk.go`; `clerkenstein/knowledge/alignment.md` and
  `architecture.md`; and `clerkenstein/.github/workflows/alignment.yml` existing, git-tracked, with
  `:10-11` saying *"currently inert"* verbatim.

---

## What I could not settle

- **GitHub-org facts.** Every archive assertion (`skiller` 2026-07-01, `skillpath` 2026-07-31,
  `graphql-wundergraph` 2026-07-30, `intelligence` 2026-04-02, `chronos` NOT archived) and the §3 census
  (**93** repos, **46** unnamed, the 19-row "unarchived and pushed since 2025" table) need
  `gh api repos/anthropos-work/<r> --jq .archived` and a repo listing. The map already labels this
  prose-under-review, and the `jobsimulation` row's *"report both, assert neither"* is the correct
  posture given the four 2026-08-04 commits I did verify.
- **Prod terraform in `infrastructure`.** The `cms`/`roadrunner`/`customerio-sync` disagreements turn on
  `services.tf` in a repo that is in no clone set. Unmeasurable here, and the map says so.
- **DB row counts.** `public.skill_embeddings` = 42,790 / `job_role_embeddings` = 18,919 (dated
  2026-06-29) and the ≥42,790 / ≥22,470 taxonomy floors need a prod read. The *table shapes* I did
  verify against `terraform/migrations/20260615130000_skiller_taxonomy.sql:51-61` and `:241-251`
  (BIGSERIAL-equivalent identity PK, `extensions.vector(1536)`, FK, IVFFLAT index) — all correct.
- **`ai_architecture.md:169`** cites skiller migrations `20260417103036` / `20260417120309`; the
  `skiller` repo is not in the clone set and neither id appears anywhere in `app` @ `ad9f3c49`. Not
  refutable from here.
- **`ai_architecture.md:94`'s `speech.go:12-13 @ v1.40.1`** (`TTSV2` / `TTSV2HD` in the *standalone*
  `github.com/anthropos-work/ai` module). The module is not cloned. The in-app side is confirmed:
  `app/internal/ai/speech.go:9-12` is the entire `SpeechModel` block and holds only `GPT4oMiniTTSS` +
  `DefaultModel`.
- **`coursebuilder.md:145` "142 changelog lines".** The predicate is not stated. My closest reading —
  lines mentioning coursebuilder in `CHANGELOG.md` @ `5ba17044` — returns **146**; lines matching
  `wave [0-9]` returns 50. I cannot tell whether 142 is wrong or my predicate is. Not booked.
- **Historical run results** (`measure` 100 %/100 % on the M7a-seeded `demo-1`; the M218 four-of-five
  re-measure) are timestamped observations of a surface I cannot re-observe — §5 rule 49.
- **A ref-choice caveat I decided NOT to book.** `platform-migration-status.md:93` cites
  `messenger/terraform/main.tf:29` `service_desired_count = 0` *"in a module that is otherwise intact
  (121 lines)"*. That is exact at **`origin/main` `e9421c68`** and false at the **checkout `fa47850d`**,
  where the file is 111 lines and `:29` is `container_definitions = <<EOF` (`= 1` sits at `:19`). The
  frozen instrument says an unpinned claim grades at the checkout; but the document's banner declares it
  *"Re-measured 2026-08-05 against platform origin HEAD"*, its sibling `storage` row explicitly grades at
  `9f8cb53` (that repo's origin/main), the `jobsimulation` row at `82cb66e` (likewise), and
  `CITE_REF=auto` resolves origin/main first. Booking it would be exactly the pinned/dated over-book the
  briefing warns about, so I record it here instead. **If the adjudicator's rule is checkout-first, this
  is a blocker.**
