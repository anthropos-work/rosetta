# Seat E — reading #30 (M257x iter-116)

**Refs actually observed at this reading's open** (re-derived, not inherited):
`app ad9f3c49` · `platform 0c91421d` · `next-web-app 8297c684` (origin/main `f97ba659`) ·
`sentinel f2c46190` · `storage 4ce8ece5` · `messenger fa47850d` (origin/main `e9421c68`) ·
`cms ca50c817` (origin/main `f38c0c4a`) · `graphql-wundergraph 60c229f3` · `roadrunner 87d8d443` ·
`jobsimulation 462343b0` (origin/main `82cb66ec`) · `studio-desk 41ee3575` · `ant-academy 22df69dd` ·
nested `app/studio` and `cms/studio` both `aeec036a` ·
`stack-demo/rosetta-extensions 09d06070` (pinned consumption clone) ·
`.agentspace/rosetta-extensions 1dc1eb82` (authoring copy).
Every tree I grepped is named per-number below. `gh` and network access were unavailable on this box —
one claim could not be settled because of it (see *What I could not settle*).

## Positive controls

| file | `wc -l` | read in full? |
|---|---|---|
| `corpus/services/studio-room.md` | 512 | yes |
| `corpus/services/backend.md` | 396 | yes |
| `corpus/services/graphql-wundergraph.md` | 280 | yes |
| `corpus/services/messenger.md` | 207 | yes |
| `corpus/architecture/frontend_architecture.md` | 116 | yes |
| `corpus/architecture/README.md` | 38 | yes |

## Blockers

All six sit in one document (`corpus/services/backend.md`) and five of them in one construct — the
**Key directories** code map at `:245-301`, which names no ref and is therefore graded at the checkout
`ad9f3c49`. I book them separately because they have separate anchors and separate repairs, but they
are one class: *a package gloss that names a technology or a role the package does not have*. Every
one was measured with a positive control in the same pass.

### B1 — `rpc.go` is NOT "the top-level wire-up", and the same document says so 289 lines earlier
- **Anchor:** `corpus/services/backend.md:319` — "**Connect-RPC**: `rpc.go` is the top-level wire-up. Look there for the implemented services."
- **Why it is false:** `app@ad9f3c49:rpc.go` is 383 lines containing **only two handler
  implementations** — `backendRPCServer` (`GetUser`, `CanPerformFeatureAction`,
  `GetExperiencePoints`; `rpc.go:22-179`) and `orgRPCServer` (`GetOrganizationRoles`,
  `GetUserOrganizations`, `GetOrganizationDetails`, `GetOrganizationSetting(s)`; `:183-383`). It
  contains **no `http.NewServeMux`, no `mux.Handle`, no `rpc.NewServer`** — i.e. no wire-up at all.
  The wire-up is in `main.go`: `mux := http.NewServeMux()` at `main.go:1295` and the six
  registrations at `:1297-1338`, served by `rpc.NewServer(mux, cfg.RPCPort, …)` at `main.go:1353`.
  A reader who follows this sentence finds **2 of the 6 services on the mux** and none of the
  registration; the other four are implemented in `internal/rpc/skillerrpc/`,
  `internal/jobsimulation/rpcsrv/`, `internal/cms/rpcsrv/` and `internal/labs/session/`.
  **This also self-contradicts the same file**: `:30-31` anchors the mux at "`main.go:1297-1338`
  @ `app` `ad9f3c49`; `:1185-1228` @ the demo pin `b948604f`", and `:106` repeats the `main.go`
  enumeration. Both of those anchors verify exactly; this sentence points somewhere else.
  (Trees read: `stack-demo/app` at `ad9f3c49` and `b948604f`.)
- **Confidence:** high

### B2 — `internal/worker/` is the **Asynq** task worker, not "Redis Streams consumers (Watermill)"
- **Anchor:** `corpus/services/backend.md:299` — "`  worker/                   Redis Streams consumers (Watermill)`"
- **Why it is false:** `app@ad9f3c49:internal/worker/worker.go:16` imports
  `github.com/hibiken/asynq`, and the package is an Asynq server + cron registry (e.g.
  `customerIOSyncCron = "*/10 * * * *"` at `:21`, an `asynqLoggerAdapter` at `:23-40`). Measured
  over the whole package: **`asynq` appears in 20 files** (`git -C stack-demo/app grep -ci asynq
  ad9f3c49 -- internal/worker/`), while `watermill|pubsub` appears in **exactly one file, 3 lines**
  — `internal/worker/tasks/tasks.go:27,39,86`, and all three are a `pubsub.Publisher`
  *dependency injected for emitting* events, not a subscriber.
  The Watermill/Redis-Streams **consumers** are elsewhere and this same file cites them correctly:
  `subServer.AddSubscriber(...)` in `main.go` (`:1274-1320` @ `b948604f`) and the map built by
  `buildStreamSubscribers` in `subscriber_wiring.go:203-248` @ `ad9f3c49`, applied by the single
  loop at `main.go:1579-1581`. So the gloss both misnames the package and duplicates a role the
  document has already correctly located.
- **Confidence:** high

### B3 — `internal/templates/` is a generic prompt-template renderer with **zero** email consumers
- **Anchor:** `corpus/services/backend.md:294` — "`  templates/                Email / message templates`"
- **Why it is false:** `app@ad9f3c49:internal/templates/` is a **single 15-line file**,
  `templates.go`, exposing one function — `Render(t *template.Template, name string, data any)`
  over `text/template`. Enumerated its importers with
  `git -C stack-demo/app grep -ln 'app/internal/templates' ad9f3c49 -- '*.go'` → **7 files, none of
  them email**: `internal/embeddings/embeddings.go`, `internal/jobrole/jobrole.go`,
  `internal/jobrole/jobrole_ant908_test.go`, `internal/rag/rag.go`, `internal/skill/skill.go`,
  `internal/skilltaxonomy/skill.go`, `internal/translation/translation.go`. These are AI
  prompt-template call sites. The real transactional-email templates live under
  `internal/messenger/` (`message/`, `brevo/`, `console/`, `aireadinessemail/`) — which this same
  document's banner names at `:15` ("transactional email (Brevo + Liquid) … `internal/messenger/`").
  A reader sent to `internal/templates/` to change an email body would be editing the RAG /
  skill-taxonomy prompt path.
- **Confidence:** high

### B4 — `internal/app/` is the user-import pipeline; the "component wire-up" it describes was deleted
- **Anchor:** `corpus/services/backend.md:257` — "`  app/                      Component wire-up`"
- **Why it is false:** at `app@ad9f3c49`, `internal/app/` contains **only** `internal/app/users/`
  — `importer/chain2d/{agent,extract,extractjson,patch,pipe,review,skills,skills_match,verify}.go`,
  `importer/model/*`, `models/`, `services/` — i.e. the résumé/profile 2-D import chain (its
  `services` package imports `internal/app/users/importer/model` and `internal/companysearch`).
  There is no wire-up type there. The construct the gloss describes was removed by commit
  `1db398ea9` *"refactor: remove the \*app.App service-locator (explicit dependencies)"*, which
  `git merge-base --is-ancestor 1db398ea9 ad9f3c49` confirms is an ancestor of the graded ref.
  Wire-up in this repo is `main.go` plus `internal/jobsimwiring/wiring.go` — the latter of which the
  same block already lists correctly at `:278`.
- **Confidence:** high

### B5 — `internal/analytics/` contains **zero** PostHog references
- **Anchor:** `corpus/services/backend.md:256` — "`  analytics/                PostHog / internal analytics`"
- **Why it is false:** `git -C stack-demo/app grep -ci 'posthog' ad9f3c49 -- 'internal/analytics/'`
  returns **rc=1, no output** — and the same command with the positive control `organization`
  returns hits in `analytics.go:13`, `analytics_test.go:4`, `detail.go:3`, so the pipeline works.
  The package's own header comment (`internal/analytics/analytics.go:15-30`) says what it is: *"the
  Member activity analytics **read model** … the summary KPIs, the stacked activity series, and the
  time split"*, sourced from `jobsimulation.sessions`, `public.skill_path_sessions`,
  `public.academy_chapter_progresses` and `public.lab_sessions`. PostHog lives in other packages
  entirely — `internal/clerk/events/`, `internal/invitations/feature_flag.go`,
  `internal/jobsimulation/ai/ai.go`, etc. The "internal analytics" half of the gloss is right; the
  "PostHog" half sends a reader looking for the analytics vendor integration to a package that has
  none of it.
- **Confidence:** medium — I hesitated because half the gloss is correct and it is a terse code-map
  entry; I booked it because the false half is the *searchable* half and it is checkable in one
  command.

### B6 — `internal/companysearch/` contains **zero** LinkedIn references; its external source is logo.dev
- **Anchor:** `corpus/services/backend.md:269` — "`  companysearch/            Company search (LinkedIn / external sources)`"
- **Why it is false:** `git -C stack-demo/app grep -ci 'linkedin' ad9f3c49 --
  'internal/companysearch/'` returns **rc=1, no output**; positive control `company` over the same
  path returns `companysearch.go:9`, `companysearch_test.go:12`, `logodev.go:1`. The package is
  three files: `companysearch.go` (a `CompanySearchManager` over `repository.CompanyRepository` +
  a `BrandSearcher`), `logodev.go` (`LogoDevManager` — a logo.dev HTTP client exposing
  `BrandSearch` / `LogoURL`) and a test. LinkedIn import lives in `internal/linkedin/`, which the
  same block lists separately and correctly at `:281`.
- **Confidence:** medium — same hesitation as B5: the "Company search" half is right and this is a
  one-line gloss. Booked because the attribution is measurably wrong and it names a *different
  package's* subject.

## Minors

- `corpus/services/backend.md:247` — "`cmd/  CLIs (bootstrap-org, migrations utilities)`". `cmd/` holds
  31 command dirs at `ad9f3c49`; `bootstrap-org` is one of them, but there is no migrations utility
  there — migrations are driven from the Makefile (`make migrations` → `atlas migrate diff --env
  local`, verified at `app@ad9f3c49:Makefile:43-44`). Non-material.
- `corpus/services/backend.md:245-301` — the Key-directories block omits `storage/`, `storagens/`,
  `publicstorage/`, `messenger/`, `customeriosync/` and `skillpath/`, all of which exist at
  `ad9f3c49` and all of which this file's own merge banner (`:14-16`, `:10`) names as folded-in
  domains. Omitted list members in a block that is not claimed exhaustive.
- `corpus/services/studio-room.md:25` — "the Go side **shells out** to it as a subprocess". Idiomatic,
  but this document's whole point at `:35` and `:344-346` is that the dispatch is argv/exec form and
  **never** goes through a shell (`runCommand`, `app@ad9f3c49:internal/cms/studio/studioManager.go:1099-1101`).
  Phrasing tension only; no factual consequence.
- `corpus/services/studio-room.md:351-362` — the `requirements.txt` block reorders `mistralai` from its
  real position (line 8) to third so it can carry an inline comment. The file at
  `app/studio@aeec036a:requirements.txt` reads openai, anthropic, rich, pyyaml, requests, jinja2,
  (blank), mistralai, (blank), pytest, pytest-asyncio. Cosmetic.
- `corpus/services/messenger.md:123` — "the binary still constructs **four** Connect-RPC clients — CMS,
  backend users + organizations, skiller, and jobsimulation (`cmd/root.go:118-142`)". At
  `messenger@fa47850d:cmd/root.go` that range constructs **five** clients (`cmsV1Client` `:118`,
  `usersClient` `:123`, `organizationsClient` `:128`, `skillerClient` `:133`,
  `jobsimulationClient` `:138`) over **four** env addresses (`BACKEND_USERS_RPC_ADDR` is read
  twice). The doc's own grouping "backend users + organizations" acknowledges this; "four
  addresses" (`:28`) is the exact form. Undercount of clients only.
- **Out of this reading's scope but worth routing** (my file is the *correct* side, so I did not book
  it): `corpus/architecture/README.md:21` says only **three** shared libs are imported as private
  modules by a service a stack builds (`colony`, `proto`, `taxonomy`) and that `ai` was folded
  in-tree at `1e457fa70`. That is **true** — measured over every `go.mod` at each clone's own HEAD:
  `app@ad9f3c49` and `sentinel@f2c46190` require colony/proto/taxonomy and **not**
  `github.com/anthropos-work/ai`; only the frozen `cms@ca50c817` and `jobsimulation@462343b0` husks
  still require `ai v1.40.2`; and `1e457fa70` is *"refactor(ai): fold the ai library into app as
  internal/ai"*. Root `CLAUDE.md:248` and `CLAUDE.md:413` still say **four**, naming `ai`. The
  defect is in `CLAUDE.md`, which is outside `corpus/services/**` + `corpus/architecture/**`.

## Positively cleared (audited zeros)

Labelled per the briefing. **ENUMERATED** = I derived the predicate's members from source
independently and compared cardinalities; **RE-DERIVED** = I only re-ran arithmetic the document
displays. I did not rely on RE-DERIVED alone for any clearance below.

**`corpus/services/backend.md`**
- **ENUMERATED** — "The mux carries **six** handlers in total (`main.go:1297-1338` @ `ad9f3c49`;
  `:1185-1228` @ `b948604f`)" and the per-handler list at `:106`. I read both ranges in full:
  Users `1297`/`1187`, Organizations `1298`/`1188`, Skiller `1306`/`1196`, JobSimulation
  `1314`/`1204`, CMS behind `if cmsRPCServer != nil` `1322-1324`/`1212-1214`, LabSession
  `1338`/`1228`. Six total, five unconditional. Exact at both refs.
- **ENUMERATED** — "**0** occurrences of `SkillPathSessionService` in `app` Go source at `ad9f3c49`"
  (`:36`, `:108`). `git grep -c` returns rc=1/no output; positive control `JobSimulationService`
  returns 3 files.
- **ENUMERATED** — the stream set (`:63-75`, `:342`): every `pubsub.NewPublisher` in the repo at
  `b948604f` is `main.go:287` (serviceName/`backend`), `:637` (SKILLPATH_STREAM), `:1039`
  (CMS_STREAM), `jobsimwiring/wiring.go:127` (AI_USAGE_STREAM), `:180` (JOBSIMULATION_STREAM) —
  **5**; every `AddSubscriber` is `main.go:1274, 1276, 1285, 1303, 1305, 1320` — **6**, the extra
  being `SKILLER_STREAM` at `:1276` with no publisher. At `ad9f3c49` the publishers are at `325`,
  `746`, `1149`, `wiring.go:132`, `:185` and the subscriber side is the map at
  `subscriber_wiring.go:203-248` (`subs[d.Streams.Skiller]` `:209`, `subs[d.Streams.Backend]`
  `:248`, its comment at `:112-113` reading *"Backend is app's OWN self-stream"*), applied at
  `main.go:1579-1581`, with `streamNames` at `:1534-1541` and `SERVICE_NAME` defaulting to
  `backend` at `:230-232`. Six subscribers / five publishers — exact.
- **ENUMERATED** — "**13** `ai_readiness_*` ent tables" (`:309`). 13 schema files under
  `internal/data/ent/schema/`, and 13 distinct `CREATE TABLE "ai_readiness*"` names across
  `terraform/migrations/*.sql`. All four names the passage says a "9" omits
  (`recommendations`, `email_overrides`, `notification_logs`, `notification_optouts`) are present,
  as are `live_snapshots` and `text_translations`.
- **ENUMERATED** — the migration figures at `:379`: **170** `.sql` at `ad9f3c49`, **169** at
  `b948604f`, **46** dated after 2026-05-31, head `20260803143844_ai_readiness_recommendation_path.sql`
  / `20260731154527_academy_chapter_progress_completed_at.sql`, the three preceding it
  (`20260731131307`, `20260729133514`, `20260728103254`), and the last May migration
  `20260529072659_add_lab_session.sql` sitting 46 back. `git ls-tree <ref> migrations/` is empty at
  both refs (`:377`), and `atlas.hcl` really does carry `dir = "file://terraform/migrations"` +
  `src = "ent://internal/data/ent/schema"`.
- **ENUMERATED** — the `http://backend.internal.anthropos:8081` census at `:155-177` and `:319`.
  Ran the §5-rule-44 loop over **all 15 git trees** (13 clones + the two nested `studio` repos),
  each at its own HEAD: **1** in `app` (`knowledge/service-dependencies.md:52`, past tense, under
  the heading *"There are no external callers of app's RPC mux left."* at `:50`) and **5** in the
  **pinned** `stack-demo/rosetta-extensions@09d06070` (4 in
  `stack-core/tests/fixtures/repair_leak/{pre,post}/corpus/services/cms.md` at `:32` and `:157`,
  plus `stack-core/tests/test_platform_predicate_guard.py:435`) = **6**, none in terraform.
  Independently: **44** tracked `.tf` across the 13 repos and **59** raw `.tf` on disk, **0** files
  matching, with the document's own positive control `service_discovery_namespace_id` returning
  **25** files.
- **ENUMERATED** — `SKILLER_RPC_ADDR` count-by-ref at `:147-154`: **4** in `docker-compose.yml`
  @ `0808b92` (and they really are on the `backend` `:89`, `jobsimulation` `:151`, `cms` `:204` and
  `messenger` `:334` blocks), **1** @ `0dab54d` (`:183`, messenger's), **0** @ `0c91421`. `d11a403`'s
  own diff moves only `CMS_RPC_ADDR` and `JOBSIMULATION_RPC_ADDR` to `http://backend:8083` — it does
  not touch `SKILLER_RPC_ADDR`, exactly as the passage says.
- **ENUMERATED** — every `file:line` in the M810 / storage / terraform paragraphs:
  `cms/terraform/main.tf:39` `service_desired_count = 0` @ `f38c0c4a`; `6efa1d5` really is
  *"chore(ci): drop build-production — the cms ECR repository is decommissioned (M810)"* and its
  body carries the quoted `services.tf` sentence; `jobsimulation/terraform/main.tf:15-22` @ the
  **named** ref `6092c6d2` is the M810 decommission comment (the module block is gone there) and
  `:24-40` is the what-it-still-owns list — note the checkout `462343b0` predates that commit and
  still shows `module "jobsimulation"` at `:31`, which is why the pin matters;
  `storage@4ce8ece5:cmd/root.go:62-66`; `sdk/storage/v1/service.go` is 47 lines with the interface at
  `:13-17` and `NewService`/`NewPublicService` at `:19`/`:34` and **zero** `Handler|ServeMux` tokens;
  `messenger@fa47850d:internal/rpcsrv/rpcsrv.go` declares `MessengerService`;
  `roadrunner@87d8d443:cmd/root.go:87`; `app/terraform/locals.tf:6` `project = "backend"` and `:8`
  `rpc_port = 8081`, `main.tf:58`, `:185-186`, `variables.tf:197`/`:230` @ `b948604f` with
  `cms_rpc_address` moved to `:309` and `storage_rpc_addr` **deleted** (replaced by
  `storage_s3_bucket`/`storage_s3_public_bucket`) at `ad9f3c49`;
  `messenger/terraform/variables.tf:77,82,87,92`.
- **ENUMERATED** — `STORAGE_RPC_ADDR` has 0 read sites and 3 comment occurrences in Go
  (`main.go:504`, `jobsimwiring/wiring.go:101`, `storagens/callsites_test.go:189`); `main.go:504`
  literally reads *"STORAGE_RPC_ADDR is gone"*; `:524`/`:525` are `internalstorage.NewManager` /
  `NewPublicManager`; and `git diff --stat 2035f9a4 ad9f3c49 -- main.go` is **empty**, so the
  document's byte-identical claim holds.
- **ENUMERATED** — compose anchors at `:56-61`, `:240`: `docker-compose.yml:48`
  `AUTHORIZATION_ADDRESS=http://sentinel:8087`, `:57` `GOTENBERG_URL`, `:59` `JUDGE0_BASE_URL`,
  `:110` backend's `profiles: [core, backend, all]` (`:100` at `0dab54d`), `:183` gotenberg's; and
  **0** `*_RPC_ADDR` anywhere in `docker-compose.yml`, `common.yml` **or** `.env_example` (which
  exists, 108 lines — I checked existence so the zero is not a missing-file artifact).
- **ENUMERATED** — the subgraph ladder at `:195-206`: `supergraph-config-prod.yaml` lists 5 names at
  `749dc86~1`, 4 at `749dc86`, 3 at `7c17e63`, 1 at `915da06`; `915da06 --name-status` marks both
  `schemas/cms.graphqls` and `schemas/jobsimulation.graphqls` as `D`. Dates and subjects match.
- **ENUMERATED** — `app/CLAUDE.md:109` and `app/knowledge/architecture.md:28` @ `ad9f3c49` both still
  list `SkillPathSessionService`, exactly as `:110` says, so the "grade against `main.go`" warning is
  correctly grounded.
- Also verified by reading the cited lines: `main.go:743-746` (the `LABS_API_URL` conditional),
  `main.go:1205-1211` (the stale *"additive + DORMANT … until the M809 re-point"* comment),
  `internal/academy/academy.go:6-9`, `internal/converter/gotenberg.go:31`,
  `internal/authorization/gqlauthz/gqlauthz.go:186`, the 60 s RPC write timeout at `main.go:1353`,
  `DIRECTUS_WEBHOOK_SECRET` fail-closed at `main.go:1198-1199`, `graph/schemas/skiller_taxonomy.graphqls`,
  `c3c45e01` = v1.334.1, `1fc00c78` = *"Deprecate skiller schema…"*, migrations `20260518125439`
  (app) and `20250116133510` (cms), Go 1.26 in `go.mod`, and the "third RPC handler after Users and
  Organizations" clause at `:314` — which is **historically correct**: at the introducing commit
  `9ecade240` (PR #896) the mux carried exactly Users `:240`, Organizations `:243`, LabSession `:249`.
  Also `corpus/architecture/architecture_overview.md:335` and the
  `shared_libraries.md#taxonomy-figures` anchor both resolve.

**`corpus/services/graphql-wundergraph.md`**
- **ENUMERATED** — the profile-token set (`:15`, `:190`): every `profiles:` key in
  `docker-compose.yml` @ `0c91421` is `[core, backend, all]` `:110`, `[studio-desk, all]` `:141`,
  `[frontend, all]` `:168`, `[core, backend, all]` `:183` → **{core, backend, all, studio-desk,
  frontend} = 5**, and `common.yml` declares none, so the always-on floor really is
  {postgresql, redis, sentinel}. The "this list read *the eight*" clause also checks: at `0dab54d`
  the keys add `storage-legacy`, `customerio-sync`, `messenger` → 8. `Makefile:10` reads
  `PROFILE ?= core`. `grep -c 5050 docker-compose.yml` = **0**.
- **ENUMERATED** — the subgraph ladder table at `:41-46` (same derivation as above, from
  `supergraph-config-prod.yaml` at each named commit) and the `915da06` subject line really does say
  "2→1" while its parent tree lists three.
- **ENUMERATED** — `schemas/` holds `backend.graphqls` alone; `subgraphs.conf` is the single line
  `BACKEND=v1.360.0` (checked by `od -c`, one trailing `\n`); `backend.graphqls` declares
  `type Mutation` `:4053` and `type Query` `:4912` and **no** `type Subscription` — the 6
  `Subscription` substring hits are `activeSubscription` `:1528`, `type PlanSubscription` `:1555`,
  `stripeSubscriptionId` `:4385`/`:4418`, `stripeSubscriptionItemId` `:4389` and a doc comment
  `:4391`, exactly the classes the passage names. `bba862f` is contained only by
  `remotes/origin/feat/use-web-socket`. The `subscription: protocol: "sse_post"` block existed on the
  `jobsimulation` entry alone at `915da06~1` and exists nowhere now.
- **ENUMERATED** — `terraform/main.tf:20` `service_desired_count = 1`; `locals.tf` gives
  `aws_region = "eu-west-1"`, `project = "wundergraph"`, `port = 8080`; `package.json` is exactly
  `{"name": "graphql-wundegraph"}` and `CLAUDE.md`'s heading carries the same misspelling;
  Dockerfile + Dockerfile.dev both pin `node:22.11-alpine`, `wgc@0.104.0`,
  `ghcr.io/wundergraph/cosmo/router:0.275.0`; all three configs pin `federation_version: =2.3.2`;
  `config.compose.yaml`/`.dev` have playground+introspection **on**, `config.prod.yaml` **off**, all
  with `listen_addr: 0.0.0.0:8080`, `graphql_path: /graphql`, CORS and `max_request_body_size: 35MB`;
  `ci/update-subgraph.sh:9` is the **only** `gh release download` and it is `-R anthropos-work/app`;
  `terraform/tests/` is the only test dir; the `repos.yml` entry deleted at `2adcf71` really was
  `type: node-npm`. Routing URLs `http://backend:8082/graphql/query` (compose),
  `host.docker.internal:8082` (dev), `backend.internal.anthropos:8080` (prod) all match, as do the
  struck historical `jobsimulation:8400/query` and `cms:8090/query` at `915da06~1`.
- **ENUMERATED** — the `:274` correction: `CLAUDE.md`'s *Version Tracking* section at `60c229f3`
  (`:70-79`) is byte-consistent with `subgraphs.conf`, and `CLAUDE.md:85` really does still say
  `wgc router compose -i supergraph-config-local.yaml` while `supergraph-config-*.yaml` at that ref
  is only `-compose`, `-dev`, `-prod`. The retraction is correct on both halves.
- **ENUMERATED** — the compose-history paragraph at `:126-134`: `63d285c`, `719befb`, `a2a3ee6`,
  `2c85211`, `67ba772`, `d92e84e` (whose diff renames the service `wundergraph` → `graphql`, not
  merely the profile), `b56d731` and `360efd4` (the second parent of the merge `2adcf71`) all exist
  with the subjects described. Its self-citations resolve: this doc's `:114-117` are the
  Build-time bullets, `:116-117` the struck `make up` one, `:84` the Ports bullet, `:193` the
  `localhost:5050` sentence.
- **ENUMERATED** — `app@ad9f3c49:internal/authorization/gqlauthz/gqlauthz.go:186` is the
  `unknown viewer: Forbidden` return, guarded by `isPublicOnlyOperation || isFederationQuery ||
  (Development && isIntrospectionOnlyQuery)` at `:175-176`; both named regression tests exist
  (`gqlauthz_test.go:132` *"bare __typename is not exempt"*, `:176`
  `TestAnonymousRejectionLogsAtWarn`).

**`corpus/services/messenger.md`**
- **ENUMERATED** — "**21** handlers — 22 `pubsub.EventHandler(…)` lines of which one,
  `OrgJobSimulationAssignmentPastDueHandler`, is commented out" (`:17`). Read
  `messenger@fa47850d:internal/flow/flow.go:66-108` line by line: 22 `pubsub.EventHandler(` lines in
  `72-104`, the one at `:83` commented `// not implemented`, and it is exactly that handler → 21
  live. `AddSubscriber("backend", …)` is at `:72`; the `OrgSkillPath*` handlers really are `:74-78`.
- **ENUMERATED** — `MESSENGER_RPC_ADDR` occurs in **no** tree. Ran the loop over all 15 git trees at
  their own refs: 0 everywhere for the exact token (the 2 hits my broader `MESSENGER_RPC` probe found
  are `app:knowledge/plan/roadmap-support-in-app.md:480` and `:768`, both markdown prose asserting the
  absence). Positive control `MESSENGER_ENABLED`: 28 in `app`, 3 in `platform`. And on the platform
  repo at `0c91421d`: **121** commits total, `-S 'MESSENGER_RPC'` → **0**, `-S 'SKILLER_RPC'` → **7**;
  with `--all` → 0 and 8, the 8th being `464dfe3`, which `git merge-base --is-ancestor` confirms is
  **not** an ancestor of `0c91421d`. Every figure in that callout is exact, including the scope
  caveat.
- **ENUMERATED** — the two-sided terraform row (`:46`, `:53`): at `fa47850d`,
  `terraform/main.tf:19` is `service_desired_count = 1` and `:29` is `container_definitions` (no
  desired-count declaration at all); at `e9421c68` (= origin/main), `:29` is
  `service_desired_count = 0` and `:27-28` is the rollback-path comment. The document's warning that
  grading the prod row against the demo clone inverts the answer is correct, and the clone is
  **7** commits behind (`git rev-list --count fa47850d..e9421c68`).
- **ENUMERATED** — the consumer row: `app@ad9f3c49:main.go:15` `msgflow`, `:62` `msgadapters`,
  `:63` `msgsender`; `:1450` `pubsub.NewSubscriberServer(msgflow.ConsumerGroup, …)` inside
  `if messengerEnabled` `:1445`; `:1471` `msgadapters.Wire(`; `:1473` `Sender:
  msgsender.NewFromEnv(logger)`; `:1416-1421` the literal-consumer-group comment naming
  `TestMessengerConsumerGroupIsMessengers`. `env_guards.go:61` is
  `envMessengerEnabled = "MESSENGER_ENABLED"`, the file is byte-identical at `2035f9a4`, and
  `git ls-tree b948604f -- env_guards.go` is **empty** — so the document's "the ref is not optional
  on this anchor" note is itself correct.
- **ENUMERATED** — `app/main.go:295-300`: the condition `(messengerEnabled ||
  customerIOSyncEnabled) && os.Getenv("BREVO_KEY") == ""` is at `:295` and the `log.Fatalf` at
  `:296`, with the comment above saying the console fallback was deliberately not ported.
- **ENUMERATED** — messenger's own anchors: `cmd/root.go:63` `PORT` default 8080, `:64` `RPC_PORT`
  8081, `:107` `REDIS_STREAMS_INDEX` default "2", `:118-142` the client block, `:147`
  `READONLY_DB_CONNECTION`; `internal/rpcsrv/rpcsrv.go:25-30` = `CancelScheduledMessage` +
  `Schedule`, both `CodeUnimplemented`; `internal/flow/jobsimulations.go:140-151` = the 2 h / 12 h
  staleness guards; `internal/flow/assignments.go:828` is inside `getSkillPath` (`:827`) and is the
  `h.cms.GetSkillPath` call; `go.mod` = `go 1.25.0`, `getbrevo/brevo-go v1.1.3`,
  `osteele/liquid v1.8.1`; `REDIS_WORKER_INDEX` appears **nowhere** in the messenger repo (positive
  control `REDIS_STREAMS_INDEX` returns 3 sites) while compose set it at `docker-compose.yml:179` @
  `0dab54d`. Every file in the *Key directories* block exists.
- **ENUMERATED** — the compose side: `repos.yml:21-23` @ `0dab54d` is the messenger entry; the block
  published `8200:8200` / `8201:8201`; `profiles: [messenger]` with the in-file comment about two
  consumers on one Redis group; `depends_on: backend` at `:191-192` (which is why the profile alone
  exits 1); `docker-compose.yml:84-92` @ `0c91421` is exactly the "deliberately NO variables here"
  comment the document cites; `3e85fce` introduced `BACKEND_USERS_RPC_ADDR=http://backend:8081`, so
  "never addressed anything but `backend`, only ever moved ports" holds. `app/main.go:1095`
  (CMS_STREAM publisher) and `:1478-1484` (the stream binding) both resolve **at the ref the sentence
  names**, `9d00a313`.

**`corpus/services/studio-room.md`**
- **ENUMERATED** — "`gen.py` registers **exactly nine** arguments (`gen.py:484-492`)". Nine
  `parser.add_argument` calls, lines 484–492, and each flag/default/dest in the doc's option block
  matches the source verbatim (including `dest: forced` and `--annotations` default `"{}"`).
- **ENUMERATED** — the `--template` callout. `parse_argument` is `gen.py:18-28` and does fold unknown
  `--key value` pairs in via `parse_known_args`; `translate_legacy_blueprint` (`:212`, with its
  `_LEGACY_TEMPLATE_DEFAULTS` dict at `:205`, through `:238`) pops `template` and back-fills defaults
  for exactly `micro challenge` / `scenario challenge` / `collaborative challenge`, warning *"is
  ignored; asset type is now inferred from task interactions"* otherwise;
  `validate_blueprint_exclusivity` is `:241-271` with the allow-set the doc lists. And the "zero
  consumers" claim holds: I enumerated every `template` occurrence in the repo — the only reads are
  the legacy-blueprint pop, tests of it, the argparse description string `:483`, and unrelated
  prompt/jinja variables. `asset_type` is derived from task interactions
  (`agents/simulation/prep.py:95-112` → `infer_asset_type`), never from a template name.
- **ENUMERATED** — the mistralai callout. Ran §5 rule 44's loop: `git -C stack-demo/app/studio grep
  -ci mistral aeec036a` = **22 lines across 3 files** — `requirements.txt:1`, `tools/pdf2md.py:18`,
  `tools/r3.py:3` — matching the published 22/3 exactly. `requirements.txt:8` is `mistralai`;
  `tools/pdf2md.py:24` is `from mistralai import Mistral`, `:96` `client = Mistral(api_key=api_key)`,
  `:127` `model="mistral-ocr-latest"`; `tools/r3.py:194` is the `MISTRAL_API_KEY` hard requirement.
  The two ignore rules are real: `app/studio/.gitignore:2` = `tools/`, `app/.gitignore:79` =
  `studio/*`, `cms/.gitignore:129` = `studio/*`. `services/ai.py:705-708` is the
  `{'openai','azure','anthropic'}` provider map and `:1-2` imports only openai/anthropic; the client
  constructors sit at `:383`, `:530`, `:664`. `app/Dockerfile:45-46` copies `studio/` and pip-installs
  `studio/requirements.txt`. Go execs exactly two studio scripts —
  `studioManager.go:119` (`studio/gen.py`) and `:1045` (`studio/postgen.py`) — both through
  `runCommand` (`:1099-1101`, argv/exec, no shell), with the venv consts at `:93-96`. All exact at
  `ad9f3c49`.
- **ENUMERATED** — the post-gen target set: `SimulationPostGenerator._get_generation_plan` dispatches
  on exactly `toolkit`, `guidance`, `metadata`, `translation`; `testing.py` exists as a module and is
  not dispatchable; export is done by `exporter.py`. `postgen.py:396-398` makes `--media`, `--simid`
  and `--target` all `required=True`.
- **ENUMERATED** — the output-structure claim: `exporter.py:514-550` is `_create_export_package`, with
  the two zip paths at `:518-519`, the `collaborative_`/`asset_`/`internal_` preservation filter at
  `:537`, `simulation.json` written at `:541`, re-archive at `:547`, `rmtree` at `:550`; and
  `sim_source_id` at `:517` is why the zip is named for `source_id` with a sim-id fallback.
  `work_paths` really is built at `gen.py:450-456` from the four `*_path` config keys, with the bare
  `worklog` fallback. State files `{simid}_pre_generation.json` / `_task_state.json` are written at
  `gen.py:116-117` and `_usage.json` at `:458`. The only cache is `model.py:59` `self._cache = {}`
  used at `:467-469`.
- **ENUMERATED** — configs: the three tracked configs are `config_template.ini`,
  `development_config.ini`, `production_config.ini`, all with `max_tokens = 4000`;
  `worklog_path = workspace/trace`; the `[SERVICES]` rows are exactly the gpt-5-mini / gpt-5.4 set the
  doc prints plus the matching `*_EXPERIMENTAL_MODEL` rows; `configs/local_*` and `configs/test_*` are
  gitignored (`.gitignore:5-6`); env override is implemented in `load_services_settings`
  (`gen.py:41-53`) rather than via configparser interpolation; `GenMode` at `services/ai.py:35-42`
  has exactly FAST/STRICT/EXECUTION/CREATIVE/REASONING with `DEFAULT = EXECUTION`.
- **ENUMERATED** — the asynq wiring: `internal/cms/worker/worker.go:29` `Concurrency: 5`,
  `:31-34` `Queues{ai_video: 7, studio: 3}`; `app/Dockerfile:28` is `FROM python:3.11-slim`;
  `additional_repo: "anthropos-studio-room:studio"` really lands at **v1.360.1**
  (`851cf3fb1` / build-production.yml `:29`), and `git diff --stat v1.360.0 v1.360.1` shows the
  submodule→additional_repo swap. `tests/e2e/blueprints/technical.json` matches the printed key set
  field-for-field; `knowledge/development/asset-examples/blueprints/` holds exactly the three legacy
  shapes. `cert.py` is the certification-benchmark CLI and contains **0** hits for
  certificate/signing/private_key/x509. The repo has **0** files mentioning graphql or directus
  outside `changelog.md`, and `services/taxonomy.py:11` is
  `BASE_URL = "https://api.anthropos.work/api"`.

**`corpus/architecture/frontend_architecture.md`**
- **ENUMERATED** — the whole `NEXT_PUBLIC_BACKEND_API_URL` table at `:44-48`. I built the file set
  independently (`git grep -l … -- '*.ts' '*.tsx'`, dropping `e2e/` and `*.test.*`/`*.spec.*`) and
  summed per file: at `8297c684` → **22 files / 31 occurrences / 47 `fetch(` sites**, with the four
  `packages/core-js` clients contributing **12 env / 27 calls** (coursebuilder 3/20, credits 3/3,
  talkToData 3/3, workforce 3/1). At `bb3313bc` the same derivation returns **21 / 29 / 43**, and at
  `origin/main f97ba659` the file count is **22**. Every cell reproduces. The prose enumeration of
  *which* surfaces they are also matches the 22-file list one-for-one. `new EventSource(` is **0**
  tree-wide, and `packages/core-js/src/talkToData/api.ts:214` really carries the quoted reason.
- **ENUMERATED** — apps/ports/packages: `apps/{web,hiring,integration,mobile,maintenance}`; dev ports
  3000 / 3001 / 3002 from each `package.json`, `expo start --port 3031` at
  `apps/mobile/package.json:70`; `!apps/mobile` in `pnpm-workspace.yaml`;
  `configs/i18n/messages/` holds exactly the **8** locales de/en/es/fr/it/ja/nl/pt;
  `packages/graphql/src/__generated__` and `src/query` both exist.
- **ENUMERATED** — `apps/web/package.json:46` is `"next": "~16.2.12"` (tilde, that patch) and
  `apps/{hiring,integration,maintenance}` carry the same string while `apps/mobile` declares none;
  root `package.json` has `engines.node ">=24.0.0"`, `packageManager "pnpm@10.30.3"`,
  `turbo ^2.9.6`, `typescript ^5.9.3`, and the `check` / `check:lint` / `check:types` /
  `check:deprecations` scripts; `UPGRADE-IMPACT-next16.md` exists.
- **ENUMERATED** — `repos.yml` holds **4** entries at `0c91421` (app, sentinel, next-web-app,
  studio-desk) and **9** at `2adcf71`; `docker-compose.yml:141` is studio-desk's
  `profiles: [studio-desk, all]` (it was `:226` at `0dab54d`); ant-academy is in neither.
  `docker-compose.yml:161`/`:152` are next-web-app's runtime env / build arg for
  `NEXT_PUBLIC_BACKEND_API_URL`. `packages/graphql/src/hooks/useGraphql.tsx:23` injects
  `Authorization: Bearer ${token}`; `packages/graphql/codegen.ts` uses the client preset, documents
  `src/query/**`, output `./src/__generated__/`, schema `http://localhost:8082/graphql/query`.

**`corpus/architecture/README.md`**
- **ENUMERATED** — the index is complete and exact: `ls corpus/architecture/` returns 10 non-README
  files and all 10 are linked, with no dead links.
- **ENUMERATED** — the `:21` shared-libraries claim (three private modules, `ai` folded in-tree,
  `authn` a dependency of nothing). Read every `go.mod` at each clone's own HEAD: `app` and
  `sentinel` require colony/proto/taxonomy only; `cms` and `jobsimulation` additionally require
  `ai v1.40.2`; `roadrunner`/`storage`/`messenger` require no `ai`; **no** repo requires
  `github.com/anthropos-work/authn`. `1e457fa70` is *"refactor(ai): fold the ai library into app as
  internal/ai"* and `app/internal/ai/` exists.
- **ENUMERATED** — `rosetta-extensions/stack-core/platform_alignment_guard.py`,
  `rosetta-extensions/alignment/` and `stack-seeding/dna/` all exist in **both** the pinned
  consumption clone `09d06070` and the authoring copy `1dc1eb82`.

## What I could not settle

1. **`graphql-wundergraph.md:8` — "the repo **ARCHIVED on GitHub, 2026-07-30** (read-only)".** This is
   a claim about GitHub repository state, not about any tree in the clone set. Neither `gh` nor
   `timeout` is installed on this box and I had no network path, so I could not measure it. It would
   be settled in one command: `gh api repos/anthropos-work/graphql-wundergraph --jq '{archived,
   archived_at}'`. Nothing in the clone contradicts it (the checkout `60c229f3` is level with the last
   thing pushed), so I am not booking it — but it is unverified rather than cleared.
2. **`backend.md:136-143` — the prod taxonomy counts** (`public.skills WHERE organization_id IS NULL`
   = 42,790 of 43,584; `job_roles` 22,490; `categories` 23; `specializations` 1,447;
   `skill_embeddings` 43,584; and the 2026-06-29 snapshot figures 42,790 / 22,470 / 18,919). These are
   read-only production DB measurements dated 2026-07-08 and are not reproducible from any clone. They
   are internally consistent and correctly framed as floors; settling them needs `/db-query` against
   prod.
3. **`messenger.md:103` — "Recent work in **v0.34.0** added whitelabel support".** The whitelabel code
   (`internal/flow/whitelabel.go`, `READONLY_DB_CONNECTION` at `cmd/root.go:147`) is present and
   behaves as described, but I did not walk `messenger`'s tag history to confirm the version
   attribution. `git -C stack-demo/messenger log --oneline v0.33.x..v0.34.0 -- internal/flow/whitelabel.go`
   would settle it.
4. **`backend.md:19` — "compose now declares **five** services".** True of `docker-compose.yml` alone
   (sentinel, backend, studio-desk, next-web-app, gotenberg) and false of the effective topology,
   which is **seven** once `include: common.yml` adds postgresql and redis. Three sibling docs
   (`service_taxonomy.md:72`, `dependency_map.md:31`, `cms.md:58`) all spell out the
   "five declared / seven effective" pair explicitly; this one says only "declares five". I read it as
   consistent with the corpus's established convention rather than as a defect, but I am recording the
   ambiguity because a reader who has not seen the sibling phrasing could take "compose declares five"
   as the whole compose project.
