# Seat B — reading #24

## Refs at the moment of measurement (re-derived at this reading's open)

| repo | path | sha read |
|---|---|---|
| app | `stack-demo/app` | `b948604f` (checkout, v1.366.0) — `origin/main` = `2035f9a40` (v1.369.0), **93 commits ahead** |
| platform | `stack-demo/platform` | `0c91421d` — level with `origin/main` (the addendum's superseding value; **not** `0dab54df`) |
| next-web-app | `stack-demo/next-web-app` | `bb3313bc` — `origin/main` = `8297c684c`, **41 ahead** |
| sentinel | `stack-demo/sentinel` | `88bc5592` |
| storage | `stack-demo/storage` | `4ce8ece5` — `origin/main` = `9f8cb532`, **20 ahead** |
| messenger | `stack-demo/messenger` | `fa47850d` — `origin/main` = `e9421c68`, **7 ahead** |
| cms | `stack-demo/cms` | `ca50c817` — `origin/main` = `f38c0c4a`, **2 ahead** |
| graphql-wundergraph | `stack-demo/graphql-wundergraph` | `60c229f3` |
| roadrunner | `stack-demo/roadrunner` | `87d8d443` — level with `origin/main` |
| jobsimulation | `stack-demo/jobsimulation` | `462343b0` — `origin/main` = `82cb66ec`, **4 ahead** |
| studio-desk / ant-academy | | `14a5442a` / `9c3843cd` |
| app/studio + cms/studio (nested, own checkouts) | | `aeec036a` (both) — grepped **at their own ref**, never through the host |
| rosetta-extensions (authoring copy) | `.agentspace/rosetta-extensions` | `main` @ `09d06070f` |

Corpus HEAD: `1937e1f` (branch `m257x/platform-realignment`, clean at open).

## Positive controls

| file | `wc -l` | read in full? |
|---|---|---|
| `corpus/services/ai-readiness.md` | 651 | yes |
| `corpus/services/cms.md` | 313 | yes |
| `corpus/services/storage.md` | 232 | yes |
| `corpus/services/coursebuilder.md` | 141 | yes |
| `corpus/services/README.md` | 88 | yes |
| `corpus/services/TEMPLATE.md` | 46 | yes |
| **total** | **1471** | |

Search-pipeline positive controls run in the same passes as every absence claim below:
`git grep -c MESSENGER <ref>` returned 21 (app) / 3 (platform) / 2 (next-web-app) while
`MESSENGER_RPC_ADDR` returned 0 everywhere; `git grep -ic storage 9f8cb53` returned hits in the same
pass that `custody` returned 0; `git -C stack-demo/platform log -S 'SKILLER_RPC' 0c91421d` returned 7
while `-S 'MESSENGER_RPC'` returned 0. No search below rests on an unread stderr — every `git grep`
exit code was inspected (`exit=1` = no match, distinguished from `exit=128`).

---

## Blockers

### B1 — `urls.ts:52` names a different constant at every ref I can read

- **Anchor:** `corpus/services/ai-readiness.md:305` —
  > "**`/ai-readiness` is the only readiness route the navbar links** — `AI_READINESS_URL`
  > (`packages/core-js/src/constants/urls.ts:52`), consumed by …"

- **Why it is false:** at the pinned ground-truth checkout `next-web-app bb3313bc`,
  `packages/core-js/src/constants/urls.ts:50` is `export const AI_READINESS_URL = '/ai-readiness';`
  and **`:52` is `export const ORGANIZATION_FEEDBACK_URL = '/enterprise/organization-feedback';`**
  (`:49` `WORKFORCE_URL`, `:51` `TALK_TO_DATA_URL`, `:53` `INSIGHTS_URL`).
  At `origin/main` (`8297c684c`) the constant sits at **`:51`**, so `:52` is `TALK_TO_DATA_URL` there.
  I then swept the file's history rather than trusting two samples: across the **20 most recent
  commits that touch `packages/core-js/src/constants/urls.ts`**, `AI_READINESS_URL` is at line **50**
  (3 commits), line **41** (`6479ac4c4`, the AI-Readiness v3.0 FE commit that introduced it), and
  **absent** before that. It has never been at 52 at any ref reachable from this clone.
  The anchor resolves — which is why an anchor-existence check passes it — but it names the wrong
  construct, and a reader following it lands on the org-feedback route constant.
- **Confidence:** high.
- **Why I hesitated:** it is a two-line drift inside a contiguous block of adjacent `export const`
  declarations, and rule 25 records that the canonical grading rule carved *line drift* out as MINOR.
  I booked it BLOCKER because the briefing I was issued lists "wrong line anchors that point at the
  wrong construct" under BLOCKER explicitly, because the line resolves to a **different named
  constant** rather than to whitespace, and because it is wrong at **both** refs — unlike the
  `useAIReadiness.ts:326` case below, which is right at one and is therefore booked MINOR.

---

## Minors

- `corpus/services/ai-readiness.md:595` — "`apps/web/src/hooks/useAIReadiness.ts:326`" for
  `interviewQuestions: number;`. That anchor resolves **only at `next-web-app origin/main`**; at the
  pinned checkout `bb3313bc` the field is at **`:274`** and `:326` is `headers: {` inside a `fetch`
  call. The claim itself (the field exists in the FE type and is drawn by nothing) is TRUE at both
  refs. Not booked as a blocker because it resolves correctly at one available ref.
- `corpus/services/ai-readiness.md:307` — "A repo-wide grep finds the constant in exactly those
  **two** non-`node_modules` files." Measured `git -C stack-demo/next-web-app grep -ln AI_READINESS_URL HEAD`
  = **3** files: `packages/core-js/src/constants/urls.ts`, `packages/ui/src/NavBar/useNavbarSections.tsx`,
  and `knowledge/ai-readiness/frontend-architecture.md`. The proposition the count supports (one
  declaration site, one nav consumer) holds; the count as stated does not.
- `corpus/services/README.md:27-28` — "`838d907` … **then did the same to the last three**"
  (i.e. deleted from `docker-compose.yml` *and* from `repos.yml`). `838d907` removed the `storage`
  and `messenger` clone entries; **`customerio-sync` never had a `repos.yml` entry** — it built
  straight from `context: git@github.com:anthropos-work/customerio-sync.git#main`
  (`docker-compose.yml:220-222` @ `2adcf71`), and `git log -S customerio-sync -- repos.yml` returns
  only `838d907` itself, which *added* the string in a comment. The same file's own customerio-sync
  row (`:45`) is correct — it claims only "container deleted at `838d907`".
- `corpus/services/coursebuilder.md:114-115` — "~55 test files" / "~25 boundary test files".
  Measured at `app b948604`: `internal/coursebuilder` has **61** `_test.go` files (of 102 `.go`
  files — the "~100 Go files" at `:30` is right) and `internal/web/backend/coursebuilder` has **33**.
  Both hedged with "~", so non-material, but 25→33 is a 32 % understatement.
- `corpus/services/coursebuilder.md:46` — "`embed.go` (`//go:embed assets/*.md` rubric)" is written
  in backticks as a literal directive. `internal/coursebuilder/embed.go` carries **five individual**
  directives (`assets/chapter-guideline.md` `:28`, `assets/source-policy.md` `:31`,
  `assets/content-format.md` `:34`, `assets/scoring-rubric.md` `:37`, `assets/widget-guidelines.md`
  `:48`); there is no glob form in the file.
- `corpus/services/ai-readiness.md:582` — "`computeCycleTotals` (`how_we_measure.go:253-261`)".
  At `app b948604` the doc comment starts at `:256` and the signature is at `:260`; `:253-255` are
  the closing lines of the *preceding* function. The range contains the construct, so a reader is
  not misled, but the start is three lines early.
- `corpus/services/ai-readiness.md:424` — "`queryReadinessStarters` (`aireadiness/steps.go:915-938`)".
  The function spans `:915-939`; `:938` is `return out, rows.Err()`, `:939` the closing brace.
  Off-by-one on the close only.
- `corpus/services/ai-readiness.md:277-278` — "`aireadiness.Manager` … is constructed with `db`,
  `ent`, `orgRepo`, `ai.AI`, a pub `EventPublisher`, the `academy.EmbeddingsManager`, and the
  `WorkforceDirectory`". The real signature is
  `NewManager(logger *slog.Logger, db *sql.DB, aiClient ai.AI, entClient *ent.Client, pub EventPublisher, academyEmbeddings *academy.EmbeddingsManager, directory WorkforceDirectory)`
  (`internal/aireadiness/manager.go`) — `orgRepo` is **derived inside** the constructor
  (`repository.NewEntRepository(entClient).Organization`), not passed, and `logger`, which *is* a
  parameter, is omitted from the list. Reads as a field list rather than a parameter list.
- `corpus/services/cms.md:196` — "Production terraform still names
  `http://backend.internal.anthropos:8081`." Not derivable from the clone set: messenger's
  `terraform/main.tf:74-75` sets `CMS_RPC_ADDR` to `${var.cms_rpc_address}` and
  `terraform/variables.tf:77-80` declares that variable with **no default**, so the literal lives in
  `infrastructure` — the repo this same doc (`:82-84`) correctly says "has never been in the clone
  set". The address does appear in `app/knowledge/service-dependencies.md:46`, so the claim is
  corroborated but not from terraform. Flagged because the doc elsewhere holds itself to
  "report both, assert neither" on exactly this repo boundary.

---

## Out-of-partition observations (NOT counted in the totals above)

Recorded because rule 19's corollary says a repaired claim leaks to the edge of the previous sweep's
scope and stops there, and root `CLAUDE.md` is the highest-propagation site in the tree. **These
anchors are outside my assigned six files and outside this reading's declared scope
(`corpus/services/**` + `corpus/architecture/**`); they are deliberately excluded from `BLOCKERS=`
so the seat-to-seat count stays comparable.**

1. Root `CLAUDE.md` (storage/messenger fold banner): *"Prod compute for both is **stopped**
   (`storage/terraform/main.tf:38`, `messenger/terraform/main.tf:29`, each
   `service_desired_count = 0`)."* Measured:
   - `storage/terraform/main.tf` @ the pinned `4ce8ece5` — `service_desired_count = **1**` at line
     **19**; the file is 100 lines. @ `origin/main 9f8cb53` the file is **18 lines and declares no
     service block at all** (no `service_desired_count` anywhere).
   - `messenger/terraform/main.tf` @ the pinned `fa47850d` — `service_desired_count = **1**` at line
     **19**.
   So the claim is false at every ref, and at **both** the value and the line number. It directly
   contradicts my assigned `corpus/services/README.md:41` and `corpus/services/storage.md:25`, which
   are the *correct* side ("the ECS service is **DELETED, not scaled to zero**", measured at storage
   `9f8cb53`). Same family as rule 33's "the pin crosses a row boundary".
2. Root `CLAUDE.md` (Frontend Applications): *"Next.js 15 monorepo on Vercel (`next-web-app`)"*.
   Measured `next-web-app bb3313bc`: `apps/web/package.json:46` and `apps/hiring/package.json:45`
   both pin `"next": "^16.2.7"`. My assigned `corpus/services/README.md:53` says "Next.js **16**"
   and is correct.

---

## Positively cleared (audited zeros)

Labelled per the briefing. **ENUMERATED** = I derived the predicate's member set from source and
compared cardinalities. **RE-DERIVED** = I only re-computed arithmetic the document displays.

### ENUMERATED

- **`corpus/services/README.md:29-30` — "5 services / 7 effective / 4 `repos.yml` entries."**
  Enumerated `docker-compose.yml` @ `0c91421d`: `sentinel`, `backend`, `studio-desk`,
  `next-web-app`, `gotenberg` = **5**; `include: common.yml` (`:1-2`) adds `postgresql` + `redis`
  = **7**. `repos.yml` = `app`, `sentinel`, `next-web-app`, `studio-desk` = **4**. Also enumerated
  the always-on floor independently: `postgresql`/`redis` (common.yml) and `sentinel` declare **no
  `profiles:` key**, so the retired-token → "exits 0, starts three" claim in `cms.md:252-253` and
  `storage.md:175` is correct as stated.
- **`corpus/services/README.md:44` — messenger's "21 live handlers" in `AddSubscriber("backend", …)`.**
  Enumerated `messenger/internal/flow/flow.go:72-104` @ `fa47850d` line by line: 21 live
  `pubsub.EventHandler(...)` members — and the count **correctly excludes** the commented-out
  `OrgJobSimulationAssignmentPastDueHandler` at `:83`. This is exactly the `grep -c`-counts-dead-code
  trap of §5 rule 24, and the corpus does not fall into it.
- **`corpus/services/README.md:44` — "`MESSENGER_RPC_ADDR` appears in no repo."** Grepped **all 12
  clones at their own named refs plus both nested `studio` checkouts at `aeec036a`**: 0 hits
  everywhere, with `MESSENGER` (positive control) returning 21/3/2 in app/platform/next-web-app in
  the same pass. `git -C stack-demo/platform log -S 'MESSENGER_RPC' --oneline 0c91421d` = **0**;
  `-S 'SKILLER_RPC'` = **7**. Every figure in that cell reproduces exactly.
- **`corpus/services/README.md` — "27 service docs", and the index enumerates all of them.**
  `corpus/services/*.md` = 29 files; minus `README.md` + `TEMPLATE.md` = **27**. README's four tables
  hold 10 + 5 + 8 + 4 = **27** rows. Every intra-directory and `../` link in `README.md` resolves to
  an existing file (checked all, 0 missing).
- **`corpus/services/cms.md:61-63` — the subgraph ladder 5 → 4 → 3 → 1.** Enumerated
  `graphql-wundergraph:schemas/` at each named ref: `749dc86~1` = {backend, cms, jobsimulation,
  skiller, skillpath} = 5; `749dc86` = 4; `7c17e63` = 3; **`915da06` = {backend} = 1**, and that one
  commit deletes `cms.graphqls` (−762) **and** `jobsimulation.graphqls` (−860). The corpus's "3 → 1,
  **not** 2 to 1" is right and its own subject line ("supergraph 2→1") is the wrong figure the corpus
  warns about.
- **`corpus/services/cms.md:95 / :144-149` — studio-room's provider set and requirements.**
  `services/ai.py:705-708` @ `aeec036a` = a three-entry dict {openai, azure, anthropic};
  `:1-2` imports only `openai`/`anthropic`. `requirements.txt` enumerated = **9** packages
  (openai, anthropic, rich, pyyaml, requests, jinja2, mistralai@`:8`, pytest, pytest-asyncio);
  **`python-docx` is absent** as claimed. `git -C stack-demo/app/studio grep -i mistral aeec036a`
  = **22 lines / 3 files** (requirements.txt 1, tools/pdf2md.py 18, tools/r3.py 3) — the exact
  figures cited. Grepped at the nested repo's own ref, not through `app` (which returns 0 by
  construction: `app/.gitignore:79` = `studio/*`, verified).
- **`corpus/services/ai-readiness.md:252-256, 356 — "31 skills = 19 core @1.0 + 12 enabling @0.5,
  denominator 25.0" and "3 default sims".** Enumerated `internal/aireadiness/defaults.go` @
  `b948604` line by line: `defaultReadinessSkills` = 19 entries at weight 1.0 (`:31-49`) + 12 at 0.5
  (`:51-62`) = **31**; weight sum = 19 + 6 = **25.0**. `defaultReadinessSims` = **3**
  ({StepSimulation,"tech"}, {StepSimulation,"business"}, {StepInterview,"both"}). Set derived from
  source first, arithmetic second.
- **`corpus/services/ai-readiness.md:275 — "CSV export is now 15 columns … dropped from the earlier
  19."** Enumerated `AIReadinessCSVColumns` in `internal/aireadiness/csv.go`: **15** string literals
  (`:18-32`), and the constant's own comment at `:13-16` names the four dropped recommendation
  columns. Worth flagging the inverse trap: `csv.go:35`'s function doc **still says "the 19-column
  CSV"** — a stale *platform* comment. The corpus matches the code, not the comment.
- **`corpus/services/ai-readiness.md:201, 213-219, 620-624 — the five usage KPI ids.**
  Enumerated `usageDimSpecs` (`internal/aireadiness/how_we_measure.go:1138-1159`): exactly **5**
  specs — avg_adoption / avg_transformation / avg_originality / avg_depth / avg_ownership, each
  banding at 61/41. Enumerated the seeder side independently:
  `stack-seeding/seeders/ai_readiness_interview_report.go:185-189` emits exactly those **5** ids,
  with `:149` reading "All five are always emitted here." Same inverse trap here: the platform's own
  comment at `how_we_measure.go:1161` still says "the **four** 'at a glance' tiles"; the corpus's
  "five" matches the code, and the doc explicitly records its own earlier "four" as corrected.
- **`corpus/services/coursebuilder.md:103 — credits `course.build`=5, `course.refine`=1,
  `course.translate`=1/locale.** Enumerated the map `creditCost` in `internal/credits/cost.go:86-90`:
  `{ActionCourseBuild: 5, ActionCourseRefine: 1, ActionCourseTranslate: 1}`. Third instance of the
  same trap: the package doc at `:29` says "course.refine → **5** credits per refine turn". The
  corpus matches the map (and `:59-67`'s D1 ruling), not the stale header.
- **`corpus/services/coursebuilder.md:101-102 — COGS defaults 500 monthly / 0 daily.**
  `internal/web/backend/coursebuilder/budget.go:27` `DefaultMaxMonthlyCOGSUSD = 500.0`; `:41`
  `DefaultMaxDailyCOGSUSD = 0.0` with the comment "DISABLED (0) unless an operator explicitly sets
  the env". Fourth instance: `internal/coursebuilder/SPEC.md:302` and `:2040` both say the daily
  default is **50**. The corpus tracks the Go constant.
- **`corpus/services/coursebuilder.md:77-79 — the 13 SSE event names.** Enumerated each of
  `session · stage · outline · progress · patch_applied · patch_skipped · preview_ready ·
  draft_kept · translation_ready · rebuild_required · error · cost · done` against
  `internal/web/backend/coursebuilder/`; all 13 present, `writeSSE` at `handler.go:3131`,
  the `"session"` emit at `:1458`.
- **`corpus/services/ai-readiness.md:593-595 — "grep -c interviewQuestions over that 1,989-line file
  returns 0."** `HowWeMeasureTab.tsx` @ `bb3313bc` = **1989** lines exactly;
  `grep -c interviewQuestions` = **0**; the "Handled for you this cycle" block opens at `:1879`,
  label `:1903`, and holds exactly **three** cells — `skillsMapped` `:1915`, `handsOnMinutes` `:1921`,
  `interviewMinutes` `:1927`. Every figure in that passage reproduces.
- **`corpus/services/storage.md:29 — "3 hits, all comments" @ `2035f9a` vs "15 hits, 7 live env
  reads" ref-less.** Ran both. `git -C stack-demo/app grep -n STORAGE_RPC_ADDR 2035f9a -- '*.go'`
  = **3**, all comments, at exactly the three cited anchors (`main.go:504`,
  `internal/jobsimwiring/wiring.go:101`, `internal/storagens/callsites_test.go:189`). Ref-less on the
  checkout = **15** hits, of which **7** are live env reads (`cmd/academy-asset-upload/main.go:129`,
  `cmd/academyImport/main.go:231`, `cmd/import/main.go:50`, `jobsimwiring/wiring.go:115`,
  `main.go:446`, `:524`, `:992`). Both numbers exact, and the doc's warning that the ref is
  load-bearing is itself correct: `internal/storage/` **does not exist at all** at `b948604`
  (only `publicstorage` + `storagens`), so every `app/internal/storage/...` anchor in that cell is
  resolvable *only* because the cell pins `2035f9a`.
- **`corpus/services/storage.md:25 — the storage prod cell.** `terraform/main.tf` @ `9f8cb53` is
  **18 lines** exactly; `:13` is the `prevent_destroy`-read-from-configuration sentence; `:18` is
  the outputs.tf line quoted verbatim; `git grep -i custody 9f8cb53` = **0** (positive control in
  the same pass returned hits); no `moved {` block anywhere in the repo; the M903 supersession text
  is at `storage.tf:22-25`; and the "state the ref or this flips" half checks out too —
  `4ce8ece5:terraform/main.tf` is **100 lines** and still declares the ECS module, while
  `4ce8ece5:terraform/storage.tf:23-24` still reads "relocate the assets out of this module BEFORE
  M907", retired by `d3e6d32` (2026-08-05) whose body says "M903 never ran."

### ENUMERATED (anchor-by-anchor citation sweep — every one opened with ±15 lines of context)

All of the following resolved to the construct named, at the ref the passage names (or at the
checkout where none is named). None is a re-derivation of displayed arithmetic.

- `app` @ `b948604`: `main.go:1205-1211` (the "Additive + DORMANT … until the **M809** re-point"
  comment), `internal/skillpath/session.go:205-207`, `internal/data/ent/enum/organization_settings.go:47`,
  `internal/aireadiness/{readiness.go:139,:289,:290-297,:307-312,:314,:330,:388,:390,:638-652,:662-664,:684,:771/:779}`,
  `steps.go:907-914`, `how_we_measure.go:285-287`, `scoring.go` (`archetypeHighBand=75`,
  `archetypeLowCeil=50`, `classifyArchetype` incl. the exact-tie→Explorer branch, bands
  None 0-24 / Low 25-50 / Medium 51-74 / High 75-100, `knowledge=((s1+s2)/70)×100`,
  `usage = s3×100/30`, `computeTier2 = raw×40/100`), `internal/coursebuilder/bedrock.go:98-104`
  and `:105-114`, `main.go:766-769`, `:770`, `:774`, `:778`, `:824-826`,
  `terraform/variables.tf:635-638`, `terraform/ssm.tf:328-333`, `terraform/main.tf:555`,
  `internal/web/backend/backend.go:229` (`e.Group("/coursebuilder", …)`) + `:227` (`cbGate`),
  `:138`/`:324` (`/api/webhook/directus`) + `main.go:1089` (`DIRECTUS_WEBHOOK_SECRET`, fail-closed),
  `internal/data/ent/schema/interaction.go:50` (`source_id <> target_id`, mirrored in
  `terraform/migrations/20260722081626_jobsim_data_model.sql:94`), `action_type` enum = **{email,
  call}** exactly (`migrate/schema.go:2289`), `terraform/migrations/20260717151144.sql` (creates
  `course_builder_sessions` + `credit_transactions` + `organization_credits`),
  `terraform/migrations/20260724132049_cms_data_model.sql`, `.gitignore:79`,
  `internal/cms/studio/studioManager.go:119`/`:1045`/`:531`/`:583`, `xlsx.go:13`, all 15
  `ai_readiness_*` + `interview_aggregated_report` ent schemas, the whole `/workforce/ai-readiness`
  REST surface (`api.yaml:661,690,719,751,792,827,852,873,907`), `ai_readiness.graphqls` (all four
  named operations + the 30/40/30 split), `4c28365f` (2026-07-22) and v1.351.1 = `3df85366`.
- `app` @ `2035f9a`: `main.go:504,:516,:517,:518-523,:524,:525,:529-535,:547,:1102`,
  `internal/storage/service.go:22`/`:24`, `internal/storage/storage.go:193-200` + `:47`
  (`DefaultTmpPath = "/tmp/anthropos-storage/"`) and `doc.go:80`, `env_guards.go:37-44`,
  `internal/cms/studio/markdownManager.go:10`/`:30`.
- `app` @ `9d00a313`: `main.go:1048` = `cmsStorage := internalstorage.NewClient(storageManager, storagens.CMS)`.
- `app` @ all three refs: `internal/jobsimulation/recording/recording.go:12` and
  `internal/jobsimulation/anticheat/anticheat.go:30` (both the storage-SDK import).
- `platform` @ `0c91421`: `docker-compose.yml:73-79`, `:82`, `:83`, `:100`; `README.md:81-87`;
  `STORAGE_RPC_ADDR` = **0** occurrences across `docker-compose.yml` + `common.yml` + `.env_example`.
  @ `2adcf71`: `docker-compose.yml:154-155`, `:169`, `:173`, `:210` (and `:324` genuinely is inside
  the *studio-desk* block, as the doc says). `d11a403` (2026-08-03) removes cms + jobsimulation +
  roadrunner from **both** compose and `repos.yml` — verified against the diff, **not** against the
  commit message, which wrongly claims roadrunner's clone entry "was already gone".
  `838d907` (2026-08-05, merged `0c91421`) removes storage/messenger/customerio-sync service blocks
  and the storage+messenger clone entries. `045857c` (chronos) and `fdfa189` (intelligence) both
  touch compose + `repos.yml`. `0dab54d` is where `STORAGE_S3_BUCKET` first appears on `backend`.
- Legacy repos: `roadrunner/terraform/main.tf:19` = `service_desired_count = **1**`;
  `cms/terraform/main.tf:39` = **0** in a **191**-line module; `jobsimulation` @ **`6092c6d2`**
  (2026-08-04) — `service_desired_count` appears **nowhere** and `:15-22` is the M810 decommission
  header, exactly as cited; `cms` `6efa1d5` (merged `f38c0c4`, 2026-08-04) deletes
  `.github/workflows/build-production.yml` under the ECR-decommissioned subject; storage repo has
  **0** `*_test.go` files and `Dockerfile:18` = `RUN go test -v ./...`; `storage/go.mod` = `go 1.25.0`;
  `storage/cmd/root.go:45-46` = binary defaults 8080/8081; `cms/go.mod:3` = `go 1.26.4`;
  `cms/cmd/root.go:77`/`:78`; `cms/Dockerfile:2` (`golang:1.26-bookworm`) / `:23` (`python:3.11-slim`)
  / `:40` (`pip install -r studio/requirements.txt`).
- `next-web-app` @ `bb3313bc`: `dae0fb2f7` (2026-07-13) deletes the three named components summing
  **−653** lines (103 + 220 + 330; the commit's 662 total includes 9 in `HowWeMeasureTab.tsx`) and
  **no `enterprise/workforce/ai-readiness` route directory exists at HEAD or origin/main**, so the
  404 claim holds; `useAiReadinessActive.ts:22`; `aiReadiness.constants.ts:26`;
  `AIReadinessClient.tsx` = **0** `posthog` / **0** `flag_ai_readiness` occurrences, `:78`
  (`SHOW_SECONDARY_TABS = false`), `:133-134`, `:153-154`, `:166-170`, `:599`;
  `useNavbarSections.tsx:4`/`:398-400`/`:547`; `WorkforceNewClient.tsx:125-151` (five tabs, no
  readiness); `useWorkforceAIReadiness.ts:23-27` (no `cycle` param, no `/cycles` call);
  `AIReadinessHero.tsx:88`; `components/ai-readiness/useAIReadiness.ts:48-62`;
  `e2e/specs/web.ai-readiness.spec.ts`; `next` = `^16.2.7`; the FE tile names "Depth / Creation"
  (`en/enterprise.json:305`) and "Critical ownership" (`:341`).
- `app/studio` @ `aeec036a`: `gen.py:484-492` = exactly **nine** `add_argument` calls with no
  `--template`; `gen.py:18-28` = `parse_known_args` + merge-the-leftovers (so `--template foo`
  parses and is silently absorbed); `CLAUDE.md:12-14`; `tools/pdf2md.py:24`, `:96`, `:127`;
  `tools/r3.py:139`, `:190`, `:199-206`; `changelog.md` top entry = **v0.51.1**.
- `rosetta-extensions` @ `main`: `stack-seeding/seeders/{ai_readiness_interview_report.go,
  ai_readiness_evidence.go, ai_readiness_funnel.go (aiReadinessStageFor:205, seedAIReadinessOrgFunnel:166),
  contentref.go, cockpit.go (LegacyReadinessPaths:135, ValidateCockpitManifest:161, the
  `/ai-readiness` + `/home` catalog rows at :94/:99), seat_append_test.go,
  ai_readiness_funnel_test.go:230 (`…_DayZeroHeroGetsNoSignals`), ai_readiness_interview_report_test.go}`;
  `blueprint/blueprint.go:200` (`AIReadiness string \`yaml:"ai_readiness"\``);
  `demo-stack/patches/app-aireadiness-snapshot-loadmembers/…yaml:33` ("v2.7 M254 RE-POINT") and
  `:42` (`path: internal/aireadiness/readiness.go`);
  `demo-stack/tests/test_aireadiness_snapshot_loadmembers_m254.py`;
  `stack-verify/e2e/lib/coverage-manifest.ts`; `stack-seeding/presets/stories.seed.yaml`.

### RE-DERIVED only (weakest form of clearance — recorded as such)

- `corpus/services/ai-readiness.md:552`/`:561` — `round(1.0/25.0*30)=1/30` and `round(9.0/25.0*30)=11/30`.
  I re-computed the displayed arithmetic and confirmed the denominator 25.0 by enumerating
  `defaultReadinessSkills` (above), but I did **not** exercise `computeTier1` against a seeded org,
  so the *rendered* 1/30 and 11/30 are not independently observed.
- `corpus/services/ai-readiness.md:348-349` — "78.4 % (≈156 of the 199 frozen snapshots)".
  156/199 = 78.39 %. The denominator (199) and the numerator both come from the doc; I did not
  re-derive either from the seeder.
- `corpus/services/coursebuilder.md:30` — "~100 Go files": measured 102.

---

## What I could not settle

- **`README.md:52` — "repo ARCHIVED" for `graphql-wundergraph`.** GitHub archive status is not
  derivable offline. Worth a look precisely because the *same file* (`:79`) records that the corpus
  called `chronos` archived and "the org disagrees" — the identical claim shape has already been
  refuted once in this index. Settled by one `gh repo view anthropos-work/graphql-wundergraph --json isArchived`.
- **`README.md:79` — chronos "last push 2026-04-23".** Same reason; the repo is not in the clone set.
- **`README.md:15-17` / `:45` — customerio-sync's prod half.** The doc says so itself
  ("its terraform lives in a repo that has never been in any clone set"). Confirmed only that the
  repo is absent and that the compose build context was a GitHub URL. Settled by a clone of
  `customerio-sync` or `infrastructure`.
- **`cms.md:82-84` — whether `infrastructure/terraform/production/services.tf` still declares
  `module.cms_euwest1`.** The doc correctly declines to assert; I can only confirm the two
  in-repo facts it cites point opposite ways (`cms/terraform/main.tf:39` = 0, i.e. block intact;
  `6efa1d5` asserting the ECR is already gone). Both verified.
- **`coursebuilder.md:130` — "Waves 1→24 (142 changelog lines)".** Wave 24 exists
  (`internal/coursebuilder/SPEC.md:383,398,432`); the "142 changelog lines" figure names no file and
  `internal/coursebuilder/` has no `CHANGELOG.md`, so I could not identify the counted set. Untested
  rather than refuted. (`v1.363.2` = `5ba170448`, 2026-07-31, and `v1.369.0` = `116645387` reachable
  from `origin/main 2035f9a4` both verify.)
- **`ai-readiness.md:7` — "ships in `app` v1.315+ / `next-web-app` v2.89.0+".** `v1.315.0` exists
  (`4f9b49bcb`); I did not attempt to establish that it is the *first* release carrying the feature.
- **Calibration note, not a finding.** Several corpus claims are TRUE at a repo's `origin/main` and
  FALSE at the pinned ground-truth checkout, because five of the twelve clones are behind: `app` by
  93, `next-web-app` by 41, `storage` by 20, `messenger` by 7, `jobsimulation` by 4, `cms` by 2.
  Concretely — at `app b948604` there is **no `internal/customeriosync/` package and no
  `MESSENGER_ENABLED` / `CUSTOMERIO_SYNC_ENABLED` at all** (both land in `HEAD..origin/main`), yet
  `README.md:12`, `:44` and `:45` describe them as live; and `app/internal/storage/` does not exist
  at the checkout. I did **not** book any of these, because (a) the platform clone — which the
  addendum confirms is level with `origin/main` — corroborates the env-var names in its own compose
  comments (`docker-compose.yml:84-92` @ `0c91421`), and (b) the corpus is describing the world,
  which has moved past this checkout. This is rule 26's "an input that can change without appearing
  in a diff": the ground-truth pin for `app` is 93 commits and three folds behind the corpus, and a
  seat that graded strictly against it would book roughly a dozen false positives. Stating it here
  so the next reading can decide deliberately rather than by seat temperament.

---

**BLOCKERS=1 MINORS=9** (plus 2 out-of-partition observations, excluded from the counts).
