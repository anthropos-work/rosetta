# Seat B — reading #23

## Refs actually used (stated at the moment of measurement, §5 rule 26)

Re-derived at this reading's open, and identical to the ADDENDUM:

| repo | ref read | note |
|---|---|---|
| app | `b948604f` (v1.366.0) | checkout; `origin/main` = **`2035f9a4`** (`git describe` → `v1.369.0-2-g2035f9a40`), 93 commits ahead |
| platform | `0c91421d` | level with `origin/main` (ADDENDUM's supersession confirmed; the frozen table's `0dab54df` is its parent-of-parent) |
| next-web-app | `bb3313bc` | `origin/main` = `8297c684c`, **41 ahead** |
| sentinel | `88bc5592` · storage `4ce8ece5` (`origin/main` **`9f8cb53`, 20 ahead**) · messenger `fa47850d` · cms `ca50c817` · graphql-wundergraph `60c229f3` · roadrunner `87d8d443` · jobsimulation `462343b0` (`6092c6d2` is **1 ahead**, not an ancestor) · studio-desk `14a5442a` · ant-academy `9c3843cd` | |
| nested | `stack-demo/app/studio` and `stack-demo/cms/studio` both `aeec036a` | grepped at their **own** ref, never through `app`'s HEAD |
| rosetta-extensions | `.agentspace/rosetta-extensions`, authoring copy on `main` | |

Instrument note: `set -- $pair` **does not word-split in zsh**, which silently produced an all-zero
"positive control" column in one sweep. Caught it, re-ran every cross-clone sweep with `while read`
plus a tree-size control (`git ls-tree -r --name-only | wc -l`) so a zero can be distinguished from a
broken pipeline (§5 rules 1/2). Every "0 occurrences" below carries such a control.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/services/ai-readiness.md` | 651 | yes |
| `corpus/services/cms.md` | 313 | yes |
| `corpus/services/storage.md` | 232 | yes |
| `corpus/services/coursebuilder.md` | 141 | yes |
| `corpus/services/README.md` | 88 | yes |
| `corpus/services/TEMPLATE.md` | 46 | yes |
| **total** | **1471** | |

`corpus/ops/platform-alignment.md` §5 (rules 1–35) read in full before starting, as instructed.

---

## Blockers

### B1 — `AI_READINESS_URL` cited at `urls.ts:52`; it is at `:50`, and `:52` declares a *different* constant

- **Anchor:** `corpus/services/ai-readiness.md:305` —
  > `AI_READINESS_URL`
  > (`packages/core-js/src/constants/urls.ts:52`), consumed by `packages/ui/src/NavBar/useNavbarSections.tsx`

- **Why it is false:** the anchor resolves at **neither** legitimate next-web ref, which is the
  briefing's own stated finding condition.
  - checkout `bb3313bc`: `packages/core-js/src/constants/urls.ts:50` is
    `export const AI_READINESS_URL = '/ai-readiness';`. **Line 52 is
    `export const ORGANIZATION_FEEDBACK_URL = '/enterprise/organization-feedback';`** — a resolving
    line naming the wrong construct (§5 rule 21, class 2).
  - `origin/main` `8297c684c`: the constant is at `:51`. Still not `:52`.
  - Both measured with `git grep -n 'export const AI_READINESS_URL' <ref> -- packages/core-js/src/constants/urls.ts`.
  - The two sibling anchors in the same bullet are exact and were cleared:
    `useNavbarSections.tsx:4` (`AI_READINESS_URL,` in the import list), `:398-400`
    (`const aiReadinessMenuItem: MenuItemProps = { label: …, key: AI_READINESS_URL,`), `:547`
    (`showAIReadiness ? aiReadinessMenuItem : null,`). So this is an isolated wrong number in an
    otherwise-correct bullet — the shape that survives readings.
- **Confidence:** high

### B2 — "Production terraform still names `http://backend.internal.anthropos:8081`" — that literal is in **zero** terraform files anywhere, and the doc elsewhere declares this exact fact unmeasurable

- **Anchor:** `corpus/services/cms.md:196` —
  > `838d907` deleted the messenger service and that variable with it, so compose sets it nowhere.
  > **Production terraform still names `http://backend.internal.anthropos:8081`.**

  Same claim, present tense, at `corpus/services/cms.md:55`: *"`http://backend.internal.anthropos:8081`
  in production."*

- **Why it is false / unsupportable:**
  1. **Enumerated absence, with controls.** `git grep -n 'backend.internal.anthropos:8081' <ref>` run
     against all 13 clone×ref pairs plus both nested `studio` repos at `aeec036a`. It returns hits in
     exactly **one** file, in **no** repo's terraform:
     `stack-demo/app/knowledge/service-dependencies.md:46` (@ `b948604f`) / `:52` (@ `2035f9a`) — a
     **markdown KB page**. A narrowed sweep (`… -- '*.tf'`) over app/cms/messenger/jobsimulation/
     storage/roadrunner/sentinel returns **0**. Control: the identical sweep for
     `backend.internal.anthropos` (no port) returns 2 hits in `graphql-wundergraph@60c229f3`
     (`CLAUDE.md:18`, `supergraph-config-prod.yaml:6`) — both at **:8080**, so the pipeline works and
     the `:8081` zero is real.
  2. **The only terraform that could carry it does not.** `messenger/terraform/main.tf:74-75` sets
     `"name": "CMS_RPC_ADDR"`, `"value": "${var.cms_rpc_address}"`, and
     `messenger/terraform/variables.tf:77-80` declares `variable "cms_rpc_address"` with **no
     default** — the value comes from the caller in `infrastructure`.
  3. **Ground truth at app `origin/main` says the opposite.**
     `app/knowledge/service-dependencies.md:50-53` @ `2035f9a`: *"**There are no external callers of
     app's RPC mux left.** `messenger` was the last one — it **used to** reach the users, cms,
     jobsimulation and skiller surfaces at `http://backend.internal.anthropos:8081`, and folding it in
     at v9.0 **closed that edge**."* And `:58-60`: *"the `messenger` and `customerio-sync` ECS services
     and their **terraform modules are gone**."* A closed edge whose module is gone cannot be "still
     named" by production terraform.
  4. **Self-contradiction inside the same file.** `cms.md:82-84` states: *"Whether
     `infrastructure/terraform/production/services.tf` still declares `module.cms_euwest1` is **not
     visible to this corpus** — the `infrastructure` repo has never been in the clone set."* The doc
     forbids exactly this assertion nine lines later and then makes it, in the present tense, with no
     ref (so §5 rule 33's currency clause applies — it cannot be pinned into silence either).
- **Why I hesitated:** the sentence is *plausible* and was probably true when written; a reader could
  read "production terraform" loosely as "the production wiring". I book it because it is (a)
  unsupportable from any ground truth we hold, (b) contradicted by the newest ground truth, and (c)
  contradicted by its own file's scope disclaimer — the M810/rollback-path question is precisely
  where this corpus has been burned, and a confident present-tense prod claim here is the highest-cost
  kind of wrong.
- **Confidence:** medium-high

### B3 — `useAIReadiness.ts:326` names a fetch `headers:` block; the `interviewQuestions` type is at `:274`

- **Anchor:** `corpus/services/ai-readiness.md:595` —
  > The field exists in the API and in the FE's TypeScript type,
  > `apps/web/src/hooks/useAIReadiness.ts:326`, and is drawn by nothing

- **Why it is false:** the passage names **no ref**, so it grades at the checkout (`bb3313bc`), where
  `apps/web/src/hooks/useAIReadiness.ts:274` is `interviewQuestions: number;` inside
  `export interface AIReadinessCycleTotals` (`:271-278`). **Line 326 is `headers: {`**, inside the
  `fetch(url.toString(), { …init, headers: { Authorization: …` helper — a resolving line naming an
  unrelated construct. Measured:
  `git grep -n 'interviewQuestions: number' bb3313bc -- apps/web/src/hooks/useAIReadiness.ts` → `:274`.
  The anchor is correct only at next-web `origin/main` `8297c684c` (**41 commits ahead** of the pinned
  ground-truth clone), where the same grep returns `:326`. The passage does not name that ref, and the
  clone a reader has is the pinned one.
- **Note (why this is not nit-picking):** the rest of this passage is exact and was cleared —
  `HowWeMeasureTab.tsx` is **1989** lines, `grep -c interviewQuestions` over it returns **0**, and
  `:1879` / `:1903` / `:1915` / `:1921` / `:1927` land precisely on the `Handled for you this cycle`
  comment, its label, and the three rendered cells (`skillsMapped`, `handsOnMinutes`,
  `interviewMinutes`). One anchor in the set was authored a ref later than the rest.
- **Confidence:** medium

### B4 — the storage HAZARD block's app anchors are unpinned and resolve only at `origin/main`; at the pinned app ref the cited file **does not exist**

- **Anchor:** `corpus/services/storage.md:73-75` —
  > Nothing warns: app's two boot guards (`main.go:518-523` empty-bucket fatal, `:529-535`
  > `verifyBucketAccess`) both run only `if deployedEnvironment()`, and `deployedEnvironment()`
  > returns **false** for `ENVIRONMENT=development` (`app/env_guards.go:37-44`).

- **Why it is false at the ref it grades at:** the HAZARD blockquote (`:60-80`) names **no `app` ref**.
  Its only pin is `@ platform 0c91421`, attached to the compose claim; under §5 rule 33 a pin's scope
  is its own block, so the app anchors are unpinned and grade at the checkout `b948604f`. There:
  - `git -C stack-demo/app ls-tree b948604 -- env_guards.go` returns **nothing** — **`app/env_guards.go`
    does not exist**, and `git grep -n 'func deployedEnvironment' b948604` exits 1 (control: the same
    grep at `2035f9a` returns `env_guards.go:37`). The entire "nothing warns" argument is
    uncheckable on the clone the demo pins.
  - `main.go:518-523` @ `b948604` is the *public-storage clients* block
    (`publicStorageClients, storagePublicClient := publicstorage.NewClients(os.Getenv("STORAGE_RPC_ADDR"), …)`);
    `:529-535` is the *academy asset uploader* block. Both resolve, both name the wrong construct.
  - All three are exact at `app` `origin/main` `2035f9a` (`:518-523` = the `log.Fatalf` empty-bucket
    guard, `:529-535` = `verifyBucketAccess`, `env_guards.go:37-44` = `func deployedEnvironment()`
    with `case "", "development", "dev", "local", "test": return false`).
- **Same family, same file (fold into the repair, not booked separately):** `storage.md:44` —
  *"the live code is `app/internal/storage/`"* — `stack-demo/app/internal/storage` **does not exist**
  at the checkout (`ls` errors; it exists at `2035f9a`, where I read `service.go:22`/`:24` and
  `storage.go:193-200`).
- **Why I hesitated:** the claims are all TRUE at `app` `origin/main`, and the world they describe is
  the real one. I book it because **this document polices its own ref discipline everywhere else and
  drops it exactly here**: `:29` says *"every `app` path in this cell resolves at `2035f9a` only"*
  and *"a block that names two refs makes every anchor in it ungradeable (M257x run-53)"*; `:58` pins
  `@ app 2035f9a`; `:35` explicitly tells the reader `b948604` is *"the demo's pinned build ref"*. A
  reader is therefore told to expect pins, then handed a dangling one for the single most
  safety-relevant claim in the file (a local stack writing into the **production** bucket).
- **Confidence:** medium

---

## Minors

- `corpus/services/ai-readiness.md:307` — *"A repo-wide grep finds the constant in exactly those two
  non-`node_modules` files."* Measured at `bb3313bc`: **three** tracked files contain
  `AI_READINESS_URL` — `packages/core-js/src/constants/urls.ts:50`,
  `packages/ui/src/NavBar/useNavbarSections.tsx:4` and `:400`, **and**
  `knowledge/ai-readiness/frontend-architecture.md:15`. (`node_modules` is untracked, so the
  qualifier is inert.) The material point — one declaration site, one consumer site — holds; the
  third hit is prose.
- `corpus/services/coursebuilder.md:114-115` — *"~55 test files"* / *"~25 boundary test files"*.
  Measured at `b948604`: `find internal/coursebuilder -name '*_test.go'` = **61**;
  `find internal/web/backend/coursebuilder -name '*_test.go'` = **33**. Both under-stated (the second
  by ~32 %), both hedged with `~`.
- `corpus/services/coursebuilder.md:46` — *"`embed.go` (`//go:embed assets/*.md` rubric)"*.
  `internal/coursebuilder/embed.go` carries **five discrete** directives
  (`assets/chapter-guideline.md`, `source-policy.md`, `content-format.md`, `scoring-rubric.md`,
  `widget-guidelines.md` at `:28/:31/:34/:37/:48`) — there is no glob form in the file.
- `corpus/services/README.md:41` — *"In prod the ECS service is **deleted**, not scaled to zero"*
  (storage). True at storage `9f8cb53`; at the pinned ground-truth checkout `4ce8ece5`
  `terraform/main.tf` is **100 lines** and still declares `module "storage"` with
  `service_desired_count = 1` (`:19`). Unpinned in the index; `storage.md:25` discloses the split
  correctly ("**State the ref or this flips**"), the index does not.
- `corpus/services/storage.md:25` — *"…their `prevent_destroy` guards, which are read from
  configuration (`storage/terraform/main.tf:13`)"*. At `9f8cb53`, `:13` opens the paragraph
  (*"The module deliberately still exists…"*); the *"it is read from configuration"* clause is at
  `:15`. Right paragraph, off-by-two on the sentence.

---

## Positively cleared (audited zeros)

Labelled per the briefing. **ENUMERATED = I derived the predicate's set from source independently
and compared cardinalities. RE-DERIVED = I only re-computed displayed arithmetic** (the weakest form).

### ENUMERATED

| claim | anchor | what I enumerated |
|---|---|---|
| `defaultReadinessSkills` = **31** = 19 core @1.0 + 12 enabling @0.5, denominator **25.0** | `ai-readiness.md:252-253`, `:356`, `:550` | extracted every `{"K-…", w}` entry from the `var` block: **31** entries, **19** at `1.0`, **12** at `0.5`, **0** other weights, **0** commented-out lines in the block (§5 rule 24's `grep -c` trap checked explicitly), weight sum = **25** |
| `defaultReadinessSims` is 3: `{simulation,"tech"}`, `{simulation,"business"}`, `{interview,"both"}` | `ai-readiness.md:154-155` | the whole `var` block: exactly 3 entries, uuids `634b9ffd…` / `a4113c6b…` / `6d6cdf39…` — and the demo seeder's `ai_readiness_config.go:93-95` carries the **same three uuids**, so the "pinned to the platform's real default sim uuid" claim (`:361-364`) holds by construction |
| CSV export is **15** columns, trimmed from 19 | `ai-readiness.md:275-276` | `AIReadinessCSVColumns` enumerated: **15** strings; `csv.go:13-16` states the 19→15 trim and names the four dropped recommendation columns; `csv_test.go` locks the contract; BOM at `:44`, `csvSafe` at `:170` |
| the interactions `action_type` enum is **exactly `{email, call}`** | `ai-readiness.md:587` | `internal/data/ent/interaction/interaction.go:94` — `ActionTypeValidator` is `switch at { case "email", "call": return nil; default: error }`. **Near-miss worth recording:** `internal/jobsimulation/entconverter/converter_gen.go` maps *five* proto values (Call/ChatMessage/Email/StorageUpload/ValidationRequest), which reads like a refutation until you open the ent validator (§5 rule 10) |
| the supergraph ladder **5 → 4 (`749dc86`) → 3 (`7c17e63`) → 1 (`915da06`)**, "not 2 to 1" | `cms.md:60-63`, `README.md:52` | enumerated `name:` entries in `supergraph-config-prod.yaml` at each ref: `{backend,skiller,jobsimulation,cms,skillpath}` → `{backend,jobsimulation,cms,skillpath}` → `{backend,jobsimulation,cms}` → `{backend}`. `915da06`'s own **subject line says "supergraph 2→1"** — the corpus is right and the commit subject is the trap it warns about |
| messenger's `AddSubscriber("backend", …)` has **21 live handlers** at `flow.go:72-104` @ `fa47850` | `README.md:44` | counted every uncommented `pubsub.EventHandler(...)` in `:72-104`: 21, with `:83` (`OrgJobSimulationAssignmentPastDueHandler`) correctly **excluded** as commented-out |
| `MESSENGER_RPC_ADDR` appears in **no repo** | `README.md:44` | `git grep` at each clone's own named ref, **12 clones + both nested `studio` repos**, each with a tree-size control (3094/11/3306/44/40/61/223/29/39/514/841/4521 files): **0** everywhere. `git -C stack-demo/platform log -S 'MESSENGER_RPC' --oneline 0c91421d` = **0**; the doc's own control `-S 'SKILLER_RPC'` = **7** — both reproduce exactly |
| the 13 SSE event names | `coursebuilder.md:77-79` | all 13 (`session/stage/outline/progress/patch_applied/patch_skipped/preview_ready/draft_kept/translation_ready/rebuild_required/error/cost/done`) present in `internal/web/backend/coursebuilder/`; emitter at `handler.go:3136` `fmt.Fprintf(w, "event: %s\ndata: %s\n\n", …)` |
| the 6 cms tables re-created in `public` | `cms.md:44-46` | `20260724132049_cms_data_model.sql` `CREATE TABLE` lines: exactly `similarities`, `studio_documents`, `studio_tasks`, `similarity_categories`, `similarity_features`, `similarity_skills` — the same 6, same names |
| the 13 `ai_readiness_*` table names, plurals included | `ai-readiness.md:169-181` | enumerated `CREATE TABLE "ai_readiness…"` across `terraform/migrations/`: cycles, diagnose_narratives, email_overrides, live_snapshots, notification_logs, notification_optouts, recommendations, sims, skills, snapshots, steps, text_translations, **user_step_progresses** — every "plural, the ent-generated name" annotation is correct |
| studio-room `mistral` = **22 hits in 3 files** | `cms.md:95` | `git -C stack-demo/app/studio grep -i mistral aeec036a` → 22 / `requirements.txt`, `tools/pdf2md.py`, `tools/r3.py`. `requirements.txt` is **9 packages verbatim in the stated order**, `python-docx` absent, and `cms/studio`'s copy is **byte-identical** (same `shasum`) |
| the three-provider registry, and that Mistral is OCR-only on both sides | `cms.md:95`, `:213-227` | `services/ai.py:705-708` is the 3-entry dict; `:1-2` imports only `openai`/`anthropic`; `tools/pdf2md.py:24/:96/:127`; `tools/r3.py:139/:190/:199-206` **does** exec it (the withdrawal at `:225` is correct); Go side execs exactly two scripts, `studioManager.go:119` (`studio/gen.py`) and `:1045` (`studio/postgen.py`) @ `b948604f` |
| `repos.yml` = **4** entries; compose declares **5** services (7 with the `common.yml` floor) | `README.md:29-30`, `cms.md:27`, `:53-54`, `storage.md:28` | `repos.yml`: app, sentinel, next-web-app, studio-desk. `docker-compose.yml`: sentinel, backend, studio-desk, next-web-app, gotenberg. `common.yml`: postgresql, redis |
| `usageDimSpecs` is the 5-id set, in the stated order | `ai-readiness.md:201`, `:620-624` | `how_we_measure.go:1138-1158`: `avg_adoption`, `avg_transformation`, `avg_originality`, `avg_depth`, `avg_ownership` — exactly the corpus's order; `usageDimensionsFromReports:1178-1182` does `if !ok { continue }`, so the "omits any absent KPI" mechanism is as described. The seeder emits the same five at `ai_readiness_interview_report.go:185-189`, and `:149` reads *"All five are always emitted here."* |
| the readiness held-skill distribution | `ai-readiness.md:559-563` | `aiReadinessHeldCount` (`:498-521`): hero stage 3 → `poolLen` (31 → 30/30); hero stage 1 → `aiReadinessStartedHeroSkills` = **9** ("Nine of the org's nineteen core skills"); population stage 3 → `[19, 31]`, stage 2 → `[3, 19]`, stage 1 → `[1, 3]` — the three ranges the corpus prints |
| `aiReadinessStageFor` mapping | `ai-readiness.md:385-387` | the whole function: `not_started → 0`, `IsManager() → 0`, `TrajectoryStruggling → 1`, `default → 3`. No "unspecified middle", as claimed |
| coursebuilder credit costs | `coursebuilder.md:103` | `internal/credits/cost.go:86-90` — `creditCost` map: build **5**, refine **1**, translate **1**. **Near-miss:** the doc-comment at `:29` says *"course.refine → 5 credits per refine turn"*, contradicting the map three lines below; `cost_test.go:15` asserts `1`. The corpus follows the code, not the stale comment |

### Exact-anchor clearances (opened and read the surrounding lines, §5 rule 10)

`storage.md`: `storage/terraform/main.tf` at `9f8cb53` is **exactly 18 lines**, declares no service
block, and `:18` is verbatim *"See outputs.tf — consumers should reference these by output, never by
literal name."*; **`custody` = 0** occurrences at that ref (control: `storage` matches 27 files);
`moved` occurs once, in the `storage.tf:22-25` M903 comment, quoted verbatim and correctly; at the
checkout `4ce8ece5` `storage.tf:23-24` **does** still read *"relocate the assets out of this module
BEFORE M907"* and `d3e6d32` (2026-08-05) says *"M903 never ran."* — the whole two-ref disclosure is
right. `Dockerfile:18` = `RUN go test -v ./...`; **0** `*_test.go` files; no `//go:generate`; gqlgen
`// indirect`; `CLAUDE.md:12` documents 8080/8081 as the binary defaults against compose's 8300/8301;
`storage.go:122` is the `if s.S3Bucket == ""` presigned-URL guard; all 5 RPC methods exist in
`internal/rpcsrv/rpcsrv.go`; the repo layout block matches the tree.
`app` @ `2035f9a`: `main.go:504` comment, `:516-517` env reads, `:524-525` manager constructors,
`:547` `resource.NewManager`, `:1102` `cmsStorage`, `internal/storage/service.go:22`/`:24`
(`EnvBucket`/`EnvPublicBucket` — env-var **names**, as the doc insists), `storage.go:193-200`
`getKeyPath`. `STORAGE_RPC_ADDR` @ `2035f9a -- '*.go'` = **3 hits, all comments**, at exactly the three
paths named; ref-less on the checkout = **15 hits, 7 live env reads** (main.go ×3, academy-asset-upload,
academyImport, import, jobsimwiring) — both numbers reproduce.
`app` @ `9d00a313`: `main.go:1048` `internalstorage.NewClient(storageManager, storagens.CMS)`;
`recording/recording.go:12` and `anticheat/anticheat.go:30` are the storage-SDK imports (and the
"there is no `anticheat.go` beside `recording.go`" warning is correct).
`platform` @ `0c91421`: `docker-compose.yml:82`/`:83` production buckets on **backend**, `:73-79`
comment containing *"These MUST be set"*, `:100` the `$HOME/.aws/credentials:ro` mount,
`README.md:81-87` the AWS-credential instruction. `0dab54d` is the commit that added **both** bucket
lines. Historical: `:169`/`:173`/`:154-155` (cms PORT/RPC_PORT/published ports) and `:210` (storage
public bucket) all exact **@ `2adcf71`**, and `:324` there really is inside the *studio-desk* block.
`d11a403` removed `jobsimulation`, `cms`, `roadrunner` from **both** `docker-compose.yml` and
`repos.yml` (its own body's *"its repos.yml clone entry was already gone"* is wrong about roadrunner;
the corpus follows the diff, which is right). `838d907` (2026-08-05, merged `0c91421`) subject and
body match the quotes.
Terraform: `roadrunner/terraform/main.tf:19` = `service_desired_count = 1`; `cms/terraform/main.tf:39`
= `0` in a **191-line** module; **at `6092c6d2`** `service_desired_count` occurs **0** times in
jobsimulation's terraform and `main.tf:15-22` is exactly the M810-decommission comment block (at the
checkout `462343b0` it is still `:40 = 0`, so this row is right **only** because it names its ref).
`6efa1d5` / `f38c0c4` (2026-08-04) subject + body quoted correctly.
`ai-readiness.md`: `4c28365f` subject and date; `organization_settings.go:47`; `enum/ai_readiness.go`
step types; `scoring.go` `archetypeHighBand=75` / `archetypeLowCeil=50` / `classifyArchetype`
(tie → Explorer) / bands **None 0-24 · Low 25-50 · Medium 51-74 · High 75-100**;
`readiness.go` `:139` `:289` `:290-297` `:307-312` `:314` `:330` `:388` `:390` `:638-652` `:662-664`
`:684` all land on the named constructs, and `queryInCycleStep1Completers` really does demand
`StepSkillMapping` ∧ `StepCompleted` ∧ `CompletedAtGTE(since)`; `steps.go:907-914` contains *"This DB
signal is the only real 'has started' check"* and `:915-938` is the
`SELECT DISTINCT user_id … status <> 'not_started'` raw SQL — so the `keepInCycleStep1` /
`keepStartedMembers` correction at `:412-434` is right in **both** directions;
`how_we_measure.go:285-287` is `FROM public.interactions i JOIN public.job_simulation_sessions s`;
`interactions` FK→`actors` ×2 with `CHECK (source_id <> target_id)`; `ai_readiness_cycles` partial
unique index `ON ("organization_id") WHERE status = 'active'`; narrative model is `openai.GPT5Mini`;
`AIReadinessClient.tsx` has **0** `posthog` and **0** `flag_ai_readiness`, `:78` `:133-134` `:153-154`
`:166-170` `:599` exact; `useAiReadinessActive.ts:22` + `:25` match the doc's 4-line code block;
`aiReadiness.constants.ts:26`; `WorkforceNewClient.tsx:125-151` is the 5-tab array with no readiness
tab; `useWorkforceAIReadiness.ts:23-27` has **0** occurrences of `cycle`; `AIReadinessHero.tsx:88` is
`if (!air.deadline) return null;`; `deriveMode` is at `components/ai-readiness/useAIReadiness.ts:48-62`
and does treat a null deadline as passed (`: true`); `dae0fb2f7` (2026-07-13) deleted the three named
components summing to **exactly 653** deletions; `/enterprise/workforce/ai-readiness` has no route
directory; the demopatch manifest `:42` `path: internal/aireadiness/readiness.go` and `:33`
*"v2.7 M254 RE-POINT"*, plus `test_aireadiness_snapshot_loadmembers_m254.py`, `cockpit.go`
`LegacyReadinessPaths`/`ValidateCockpitManifest`, `seat_append_test.go`,
`TestAIReadinessFunnelSeeder_DayZeroHeroGetsNoSignals`, `Persona.AIReadiness` +
`AIReadinessNotStarted = "not_started"`, `ai_readiness_evidence.go`, `AIReadinessSimSkillsSeeder` all
exist as named.
`coursebuilder.md`: 102 Go files / all 5 in-repo docs / go 1.26.4 / ent schema / migration creating
all three tables; `bedrock.go:98-104` + `:105-114`; `main.go:770` the backend log line, `:766-769` the
comment, `:774` + `:778` the two `logger.Warn(… routes disabled)` arms, `:824-826` the `OPENAI_KEY`
cover client — **every one exact at the ref the doc pins (`b948604`)**; `terraform/variables.tf:635-638`
(sensitive, no default), `ssm.tf:328-334`, `main.tf:555`; `COURSEBUILDER_OPENAI_IMAGE_KEY` survives
only in `GO-LIVE-RUNBOOK.md` + `README.md`, and `68c245123` **did** delete its `os.Getenv` read
(despite a subject line about `HTTPAcademyPublisher` — verified via `git log -S`);
`DefaultAuthorModelID = "eu.anthropic.claude-opus-4-8"`, `DefaultGraderModelID =
"eu.anthropic.claude-sonnet-4-6"`, imagegen `defaultModel = "gpt-image-2"`;
`coursebuilderWorkerConcurrency = 20` (`main.go:173`), `MaxRetry(1)` + `Retention(6h)`
(`run_task.go:199`, `:204`); `DefaultSessionsPerOrgPerDay = 50`; monthly COGS default **500**, daily
default **0 = off**; `e.Group("/coursebuilder", …)` + `courseBuilderAccessGate`; `handler.go:3143`
`Register` mounting every route the doc lists; `assertNotSharedDSN` in `cmd/coursebuilder-e2e`;
`TestProductionConfig_WiresPlanner`; `SetCourseBuildStarter` on the `ask` handler wired at
`backend.go:259`; and **0** imports of `github.com/anthropos-work/ai`, `taxonomy` or `internal/cms`
anywhere under `internal/coursebuilder/` (the "does not call the shared `ai` library" claim).
`TEMPLATE.md`: pure template, no factual claims — nothing to grade.

### RE-DERIVED only (weakest clearance — flagged as such)

- `ai-readiness.md:348-350` — **78.4 % ≈ 156 of 199**. 156/199 = 78.39 % ✓. I did **not** enumerate the
  set: `aiReadinessCompletedShare = 0.80` with a per-member hash (`memberInShare`), so the realized
  156 requires running the seeder. Arithmetic checks; the *set* is unverified.
- `ai-readiness.md:550`, `:561` — `round(1.0/25.0*30) = 1` and `round(9.0/25.0*30) = 11` ✓ (the
  denominator 25.0 itself **is** enumerated, above).
- `coursebuilder.md:132` — *"six releases on"* from v1.363.2 to v1.369.0: 6 minor bumps (but **7** tag
  objects: v1.364.0, v1.364.1, v1.365.0, v1.366.0, v1.367.0, v1.368.0, v1.369.0). Both readings are
  defensible; `5ba17044` **is** v1.363.2 and `2035f9a4` **is** `v1.369.0-2`.

---

## What I could not settle

- **`coursebuilder.md:130` — "142 changelog lines".** Pinned at `5ba17044`, so I read
  `internal/coursebuilder/SPEC.md` there. The `## Changelog` section runs to EOF (no following `##`);
  I get **1505** lines in the section (1421 non-blank), **58** `**2026-…**` dated entries, **123**
  `- ` bullets, and **209** lines matching any entry-ish prefix. No counting rule I tried yields 142.
  *Max wave = 24, so "Waves 1→24" **is** verified.* The doc itself says the count *"is not re-derived
  here"*. Settled by: naming the counting predicate next to the number.
- **`README.md:79` — chronos "the GitHub repo is NOT archived (last push 2026-04-23)".** No `chronos`
  clone exists in `stack-demo/`, and repo-archived state is a GitHub API fact. Settled by:
  `gh repo view anthropos-work/chronos --json isArchived,pushedAt`.
- **`README.md:52` — graphql-wundergraph "repo ARCHIVED".** Same class; the clone exists at
  `60c229f3` but archived-ness is not a git fact. (Note the mild tension with the chronos row, which
  makes exactly this distinction — "the corpus called it archived; the org disagrees".)
- **`cms.md:78-84` / `storage.md:161` — whether `infrastructure/terraform/production/services.tf`
  still declares `module.cms_euwest1`.** The corpus is right that this is invisible: `infrastructure`
  is in no clone set. Both files handle it correctly ("report both, assert neither") — which is
  precisely why **B2**, in the same file, is a defect.
- **`storage.md:25`'s pointer to `app/knowledge/plan/releases/09.00-support-in-app/m903-s3-custody/`
  (`status: planned`).** Not opened — my instructions bar `knowledge/plan/**`, and I read the glob
  conservatively rather than assume it meant only the rosetta corpus's copy. The *repo-side* half of
  that same cell (the `storage.tf:22-25` comment saying M903 *"was never executed"*, and `d3e6d32`'s
  *"M903 never ran."*) **is** verified, so the claim is corroborated from the side I could read.
- **`ai-readiness.md:194` — `interview_extraction_results` "165 rows"** and the several live-measured
  latencies (`2.09 s`, `24 ms`, `19 ms`, `180 s`, `304 KB`). Runtime measurements against a live demo
  stack; not derivable from source. The doc says *"Re-measure before relying on either number; do not
  re-derive them from prose"*, which is the right disposition.
- **`ai-readiness.md:195-197` — `computeInterviewInsightsV2` "reads exactly one table".** I confirmed
  it is the only reader of `public.interview_aggregated_reports` (`how_we_measure.go:1055`) and that
  `interview_extraction_results` is read by a *different* consumer (`recommendation_signals.go:191`),
  which supports the claim's point. I did not trace every query inside the ~350-line function to prove
  the exclusivity conjunct (§5 rule 17 — a conjunction needs every clause tested), so I record it as
  supported rather than cleared.

---

## One cross-cutting observation for the repairer

Three of my four blockers (**B1**, **B3**, **B4**) and one minor (README:41) are the *same mechanical
class*: an anchor or present-tense claim authored against a repo's **`origin/main`** while the
corpus's pinned ground-truth clone sits behind it — app `b948604` vs `2035f9a` (93 commits),
next-web `bb3313bc` vs `8297c684c` (41), storage `4ce8ece5` vs `9f8cb53` (20). Where the author
noticed, they pinned explicitly and at length (`storage.md:25`, `:29`, `:35`, `:58`; `README.md:22`'s
`6092c6d2`; every `coursebuilder.md` anchor `@ b948604`) — and **every one of those verified**. The
defects are exactly the sites where the pin was *omitted*. That is §5 rule 26 ("an input that can
change without appearing in a diff is not a controlled input") one level in, and it is
machine-checkable: an anchor that resolves at `origin/main` but not at the pinned ref, in a block
naming no ref, is a derivable predicate — a rule-27 row-one candidate. **B1 is the exception and the
control**: `urls.ts:52` resolves at *neither* ref, so it is an ordinary wrong number, and it is the
one I rate `high`.
