# Seat E — reading #24

**Corpus HEAD:** `1937e1f` (branch `m257x/platform-realignment`)
**Ground truth re-verified at this reading's open** (per the ADDENDUM):
`app b948604f` · `platform 0c91421d` · `next-web-app bb3313bc` · `sentinel 88bc5592` ·
`storage 4ce8ece5` · `messenger fa47850d` · `cms ca50c817` · `graphql-wundergraph 60c229f3` ·
`roadrunner 87d8d443` · `jobsimulation 462343b0` · `studio-desk 14a5442a` · `ant-academy 9c3843cd` ·
nested `app/studio` **and** `cms/studio` both `aeec036a` ·
rext authoring `.agentspace/rosetta-extensions @ main 09d0607` ·
rext per-stack `stack-demo/rosetta-extensions @ tag fast-build-m257x-iter-58 (ab81527)`.

**Instrument note.** Where a passage names a ref I graded at that ref (§5 rule 33). Two claims in this
partition are pinned to refs *ahead of* the checkout — `jobsimulation` `6092c6d2` and `app` `origin/main
2035f9a4` — and both were graded at the named ref via `git show <sha>:<path>`; both hold.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/architecture/service_taxonomy.md` | 496 | yes |
| `corpus/services/clerkenstein.md` | 370 | yes |
| `corpus/architecture/security_compliance.md` | 281 | yes |
| `corpus/services/ai-labs.md` | 156 | yes |
| `corpus/services/clerk-integration.md` | 128 | yes |
| `corpus/services/intelligence.md` | 18 | yes |
| **total** | **1449** | — |

Protocol read first: `corpus/ops/platform-alignment.md` §5 in full (2272-line file; §5 = `:249`–`:1460`).

---

## Blockers

### B1 — the archive-state fence cites the two rows that do NOT make its point, and names a non-retraction as the retraction

- **Anchor:** `corpus/architecture/service_taxonomy.md:131-133` —
  > "This note exists because rows `service_taxonomy.md:137`/`:138` published the flat form two rows
  > above `:139`, a cell retracting exactly that predicate. (The file is named explicitly as of M257x
  > iter-100: a bare `rows :137/:138` reads as an anchor into the document cited immediately before it,
  > which is a different file.)"

- **Why it is false:** measured in the file itself (`grep -n ARCHIVED corpus/architecture/service_taxonomy.md`):

  | line | row | carries a flat `ARCHIVED <date>`? |
  |---|---|---|
  | `:137` | **Chronos** — *"Removed from local dev orchestration \| **no** \| Platform commit `045857c`"* | **no — the cell contains no archive assertion at all** |
  | `:138` | **Intelligence** — *"Removed from local dev orchestration \| **no** \| Platform commit `fdfa189`"* | **no — same** |
  | `:139` | **Skiller** — *"repo legacy/decommissioned, **ARCHIVED 2026-07-01**"* | **yes** |
  | `:140` | **Skillpath** — *"repo legacy, **ARCHIVED 2026-07-31**"* | **yes** |
  | `:141` | **Jobsimulation** — *"**repo archive state: report both, assert neither** … an archived repo is read-only"* | **this is the retraction** |

  So every clause of the sentence names the wrong construct: the flat form is published at `:139`/`:140`
  (not `:137`/`:138`), those two rows are indeed *"two rows above"* — but two rows above **`:141`**, and
  `:139` is not *"a cell retracting exactly that predicate"*, it is one of the two cells **asserting**
  it. A reader following the note lands on Chronos and Intelligence, which demonstrate nothing, and on
  Skiller, which demonstrates the opposite of what the sentence says it demonstrates.

  This is aggravated rather than mitigated by the parenthetical: iter-100 re-pointed the *file* half of
  this citation (the anchors used to read as pointers into `platform-migration-status.md`) and left the
  *line* half unmoved — exactly §5 rule 34's mechanism, in the note whose only job is to fence a
  predicate the corpus cannot measure. The underlying content claims are all sound: `045857c` and
  `fdfa189` are real (`git -C stack-demo/platform show --stat` — chronos and intelligence removed from
  `docker-compose.yml` + `repos.yml`, both 2026-04-17), and the Jobsimulation retraction is correct
  (`git -C stack-demo/jobsimulation log 462343b0..origin/main` → four commits **all dated 2026-08-04**,
  including merged PR #439). The defect is purely the anchor set — which is the class §5 rule 21 books
  as "resolves but names the wrong construct".

- **Confidence:** high

### B2 — the credit ledger's build price is documented in the wrong unit (`5/chapter` vs 5 per build)

- **Anchor:** `corpus/services/ai-labs.md:76` —
  > "`cost.go` — the cost model: `creditCost` map (`course.build`=**5/chapter**, `course.refine`=**1**,
  > `course.translate`=**1/locale**), …"

- **Why it is false:** `app/internal/data/… ` no — `app/internal/credits/cost.go` at the checkout
  (`b948604f`). The map is:

  ```
  cost.go:86   var creditCost = map[ActionType]int64{
  cost.go:87       ActionCourseBuild:     5,
  cost.go:88       ActionCourseRefine:    1, // D1 ruling: flat 1 credit per refine turn
  cost.go:89       ActionCourseTranslate: 1,
  ```

  and the file's own doc comment **immediately above it** defines the unit, explicitly and by
  contrast — `cost.go:78-81`:

  > "`creditCost` is the single source of truth for how many credits **one unit** of each billable action
  > consumes. A *"unit"* is action-specific: **for `course.build` one unit is one build**; for
  > `course.translate` one unit is one locale."

  The consumer confirms it: `internal/coursebuilder/SPEC.md:2672` bills *"Start a session (any of the 3
  start paths) → `ActionCourseBuild` → **units** (depth-tiered — §6.1: simple 1, advanced 2)"*, and
  `GO-LIVE-RUNBOOK.md:36` — *"Credit price of a `simple`-depth build (the default tier). Debited as
  `ceil(price/5)` = **1 unit** of `ActionCourseBuild`."* The multiplier is **build depth**, never chapter
  count.

  The independent arithmetic in the same file rules it out too: `PricePerCreditUSD` is *derived* as
  `round2(bedrockCostUSD[build] / creditCost[build] * MarginMarkup)` = `round2(1.60/5 × 1.40)` = `0.45`
  (`cost.go:126-142`, guarded by `TestPricingDerivation`, `cost_test.go:103`). `bedrockCostUSD[build] =
  1.60` is documented as *"avg $1.60 (range $0.92–$2.69), Opus author + Sonnet grader"* — a **whole
  session**, not a chapter. If build were 5 credits *per chapter*, the published $/credit would be wrong
  by the chapter count.

  Why this misleads a reader doing real work: the passage is the sizing reference for an org's purse. At
  `5/chapter`, a 20-chapter course reads as **100 credits ($45)**; it is actually **5 credits ($2.25)** —
  a 20× error, in the direction that makes the product look unaffordable. The two *sibling* units in the
  same parenthesis (`refine`=1, `translate`=1/locale) are exactly right, which is what makes the wrong
  one read as verified.

- **Confidence:** high

### B3 — the multi-tenancy fence opens with the grep count (30) presented as the use count, which its own derivation refutes 16 lines later (29)

- **Anchor:** `corpus/architecture/security_compliance.md:67-68` —
  > "Measured at `app` HEAD: of **135** Ent schemas (139 `.go` files, 4 of which declare no schema), only
  > **30** use `OrganizationMixin{}` — the one that carries the privacy `Policy()` (`mixin.go:126`)."

- **Why it is false:** I enumerated the set independently at the checkout (`b948604f`), in
  `app/internal/data/ent/schema/`:

  ```
  ls *.go | wc -l                            -> 139
  grep -l 'ent.Schema' *.go | wc -l          -> 135      (the 4 non-schemas: database_types.go,
                                                          mixin.go, skiller_mixins.go, skillpath_mixins.go)
  grep -n 'OrganizationMixin{}' *.go | grep -v 'OrganizationIDMixin{}'   -> 30 lines, in 30 files
  ```

  The 30th is `user_resource.go:22` — `// OrganizationMixin{},  // We need to work on this` — i.e.
  **commented out and not compiled**. So **29 schemas use it**, which is precisely what the same
  blockquote states three times elsewhere: `:76` (*"**29** `OrganizationMixin{}` users"*), `:84-85`
  (*"one of them, `user_resource.go:22`, is **commented out** …, so the live count is **29**"*), and
  `:135` (*"the **29** live `OrganizationMixin{}` users"*).

  The consequence is not cosmetic: the *"So: **31** schemas auto-filter by ORGANIZATION"* at `:76` is
  `29 + Membership + Organization`. Read from `:68` the arithmetic is `30 + 2 = 32` — which is exactly
  the wrong total M257x iter-52 shipped and had refuted by two blind readers, per this document's own
  `:86-88`. The opening sentence of a fence the document itself flags as **"wrong FOUR times"** (`:114`)
  states the number the rest of the blockquote exists to correct, and it is the sentence a reader quotes.

  It is also the literal defect §5 rule 24 was written for: *"**A `grep -c` over source counts
  commented-out code.** Enumerate the set, exclude what does not compile, and state the cardinality
  before the arithmetic."*

  Everything downstream of the sentence is correct and I cleared it by enumeration — see *Positively
  cleared* below (31 = 29 + `Membership` + `Organization`; 7 `OrganizationIDMixin` users; 18 plain-column
  schemas of which 16 unpoliced; only 4 files declare any `Policy()`; the 16-name list is exact).

- **Confidence:** medium. **Why I hesitated:** the document self-corrects at `:83-88`, the *total* (31)
  is right, and §5 rule 25 records that the canonical grading rule carved *undercount* out as MINOR. I
  book it because (a) it is the load-bearing count in a **compliance** fence, (b) `:68` and `:76` are in
  one blockquote and are arithmetically incompatible without the `:83` bridge, which is the
  self-contradiction class rule 21 calls the cheapest in any corpus, and (c) the briefing resolves doubt
  upward.

---

## Minors

- `corpus/architecture/service_taxonomy.md:3` — the banner asserts the router repo *"**is** ARCHIVED on
  GitHub (2026-07-30)"* in flat present tense. That is the exact form this file's own archive-state note
  forbids at `:124-133` (*"Read every date here as 'asserted on', never as 'is'"* — and `gh` is not
  installed on this host, the repos are private, so no clone can measure it). `:375-376` states the same
  fact correctly hedged, so the file carries both forms.
- `corpus/services/clerkenstein.md:21` — the section enumeration is incomplete: it lists six
  (`clerkenstein`, `demo-stack`, `stack-injection`, `stack-core`, `stack-seeding`, `alignment`). The
  authoring clone holds **eleven** code sections — the six above plus `dev-stack`, `playthroughs`,
  `stack-secrets`, `stack-snapshot`, `stack-verify` (+ `knowledge/`). Root `CLAUDE.md` lists nine.
- `corpus/services/clerkenstein.md:3` — *"**Last updated:** 2026-07-14"* and a Status line that stops at
  v2.3 M217/M218, while the body documents M219 (`:54-62`), M220 (`:305-313`), v2.4 M224 (`:232-255`) and
  v2.8 M256 (`:160`, `:184`). Stale metadata on an otherwise current page.
- `corpus/services/clerkenstein.md:105` and `:282-284` — the `cmd/` enumeration (*"standalone binaries:
  `mintpk` … · `fake-fapi` / `fake-bapi`"*) omits `jwtkey`; the dir holds `fake-bapi`, `fake-fapi`,
  `jwtkey`, `mintpk`.
- `corpus/architecture/security_compliance.md:201` — *"[`external_services.md:602-607`] **enumerates**
  four live ways a request leaves the EU"*: `:602-607` is the paragraph that *states* the count
  (*"**Four** things can send a request outside the EU … plus a **fifth arm**"*); the enumeration itself
  begins after `:608`.
- `corpus/architecture/security_compliance.md:234` — the range `skills.go:53-64` overshoots
  `calculateSkillScore`, which is `:53-62`; `:64` is the next function's signature (`func (s
  skillsValidator) run`). The companion anchor `:75` is exact.
- `corpus/architecture/service_taxonomy.md:380` — cites *"`:67-68` above (*"There is no `graphql`
  profile"*)"*; the quoted sentence begins on `:68` (`:67` ends the previous clause). Within range, so
  harmless.

---

## Positively cleared (audited zeros)

**ENUMERATED** — predicate re-derived from source independently, cardinality stated before arithmetic:

1. **The multi-tenancy split** (`security_compliance.md:66-135`) — *everything except `:68`*. Derived in
   `app/internal/data/ent/schema/` @ `b948604f`: 139 `.go`, **135** with `ent.Schema` (the 4 non-schemas
   named and matched); **7** `OrganizationIDMixin{}` users and the doc's seven names are **the exact
   set** (`category`, `jobrole`, `similarity`, `skill`, `specialization`, `studio_document`,
   `studio_task`); the doc's own `comm`/`xargs` derivation returns **18**; removing the two policed ones
   (`org_membership.go`, `academy_feedback.go`) leaves **16**, and the doc's 16-name list is a **set-exact
   match** — I diffed name by name, not by count. Only **four** files declare any `Policy()`
   (`mixin.go`, `org_membership.go`, `organization.go`, `user.go`) — verified with a filename-level
   enumeration, not a count. `OrganizationIDMixin` (`skiller_mixins.go:152-162`) declares an
   `Optional().Nillable()` `organization_id` and **no** `Policy` (grep exit 1), so `16 + 7 = 23`
   "org column, no policy" holds *as a predicate*. **31 = 29 + `Membership` + `Organization`** confirmed.
2. **Anchors inside that fence, each opened and read in context:** `mixin.go:126` = `func
   (OrganizationMixin) Policy()` (exact); `mixin.go:98-99` = UserMixin's `Policy()`, whose Query policy
   really does carry `rule.FilterOwnerRule()`; `org_membership.go:172-188` = `func (Membership) Policy()`
   with `rule.AllowCurrentOrgEdgesOrSkipRule()` at `:179` and ending `privacy.AlwaysDenyRule()` at
   `:185`; `organization.go:56` = `func (Organization) Policy()` and `rule.FilterSameOrganizations()` at
   **`:96`** (exact); `user.go:116` = `func (User) Policy()`; `job_simulation_session.go:5` = *"L2: NO Ent
   privacy Policy; owner/org/tenant are plain fields"* (exact); `jobrole.go:18` / `category.go:15` = the
   globally-readable taxonomy comment (both exact).
3. **The subgraph ladder** (`service_taxonomy.md:389`) — re-derived by counting `name:` in
   `supergraph-config-prod.yaml` at each named commit in `stack-demo/graphql-wundergraph`: **5 → 4**
   (`749dc86`) **→ 3** (`7c17e63`) **→ 1** (`915da06`). `git show --stat 915da06` deletes
   **`schemas/cms.graphqls` (762 lines) AND `schemas/jobsimulation.graphqls` (860 lines) in one commit**,
   so the corpus is right that cms-in-app is the **3 → 1** step and that the commit's own subject line
   (*"supergraph 2→1"*) is the wrong figure — the subject still reads that way, and so does the surviving
   comment at `supergraph-config-prod.yaml:4`.
4. **The compose/profile topology** (`service_taxonomy.md:65-110`, `:462-479`, `:489-496`) — read
   `docker-compose.yml` (186 lines) + `common.yml` (36) at platform `0c91421` end-to-end.
   `docker-compose.yml` declares **five** services (`sentinel`, `backend`, `studio-desk`,
   `next-web-app`, `gotenberg`); `include: common.yml` adds `postgresql` + `redis` → **seven**; exactly
   **four** carry a `profiles:` key, so the floor is `{postgresql, redis, sentinel}` = **3** and `core`
   starts **5**. Legal token set = `{all, backend, core, frontend, studio-desk}` — **five**. `frontend`
   and `studio-desk` each `depends_on: backend` (`:138-140`, `:165-167`), which their own profile does
   not select. `Makefile:10` = `PROFILE ?= core` (exact).
5. **Zero `*_RPC_ADDR`** (`service_taxonomy.md:407`) — `grep -n RPC_ADDR docker-compose.yml common.yml`
   returns **exit 1**; positive control `grep -c PORT docker-compose.yml` = 6. The single service address
   is `AUTHORIZATION_ADDRESS=http://sentinel:8087` at **`:48`** (exact).
6. **The deletion history** — `d11a403` (2026-08-03) removes the `jobsimulation`, `cms` and `roadrunner`
   service blocks (diffed the service list before/after); `838d907` (2026-08-05, merged `0c91421`)
   removes `storage`, `messenger`, `customerio-sync` and drops `storage` + `messenger` from `repos.yml`
   (which held six entries before, four after); `customerio-sync` was **never** in `repos.yml` and **was**
   in the `all` profile until deletion — all four sub-claims verified against `838d907^`. The deleted
   ports are exactly as documented: storage `8300/8301`, messenger `8200/8201`, customerio-sync `8080`.
   `docker-compose.yml:84-92` is the `MESSENGER_ENABLED`/`CUSTOMERIO_SYNC_ENABLED` comment block (exact).
7. **`authn` is imported by nothing** (`service_taxonomy.md:491`) — 0 `anthropos-work/authn` hits in
   `go.mod` across **all seven** Go clones (app, sentinel, storage, messenger, cms, jobsimulation,
   roadrunner); control — `colony` is required by **all seven**. And `colony/authn` file counts
   (`clerk-integration.md:51`, `:91`): app **129 `.go` files** (exact — 132 files total, of which 3 are
   markdown; I checked the predicate before reporting the number), sentinel/storage/messenger **0** each.
8. **Alignment DNA gene/capability counts** (`clerkenstein.md:41-44`, `:74`) — parsed all five DNA JSONs
   and summed `variants` per capability: `clerk-2.6.0` **27 genes / 14 capabilities**, `clerk-js-5` **9**,
   `clerk-multi-1` **9**, `clerk-deploy-1` **7**, `clerk-express-1` **13 genes / 5 capabilities**. Gate
   thresholds `95 / 100` confirmed at `alignment/scripts/gate.sh:31-32`.
9. **Clerk webhook event types** (`clerk-integration.md:37`, `:79`) — `internal/clerk/events/events.go`
   switches on **exactly 12** literals: `user.{created,updated,deleted}`,
   `organization.{created,deleted,updated}`, `organizationInvitation.{accepted,created,revoked}`,
   `organizationMembership.{created,deleted,updated}`.
10. **8 Clerk locales** (`clerk-integration.md:41`) — `apps/web/src/providers/Clerk.provider.tsx:9-18`
    imports `deDE, enUS, esES, frFR, itIT, jaJP, nlNL, ptPT` = **8**. (`apps/hiring` imports 7 — the
    superset is web's.)
11. **`gen.py` has exactly nine arguments** (`service_taxonomy.md:244-250`) — `git -C stack-demo/app/studio
    grep -c add_argument aeec036a -- gen.py` = **9**, and the nine flags match the doc's list
    one-for-one; `parse_known_args` at `gen.py:19` inside `parse_argument`. There is no `--template`.
12. **Mistral is OCR-only in studio-room** (`service_taxonomy.md:220`) — grepped the **nested repo at its
    own ref** (`aeec036a`, per ADDENDUM mechanism 3): `git -C stack-demo/app/studio grep -ci mistral
    aeec036a` → `requirements.txt:1`, `tools/pdf2md.py:18`, `tools/r3.py:3` = **22 lines / 3 files**, the
    exact figure the doc publishes. `services/ai.py:705-708` is the whole provider registry
    (`openai`/`azure`/`anthropic` — exact); `ai.py:1-2` imports only `openai`/`anthropic`;
    `tools/pdf2md.py:24` is `from mistralai import Mistral` (exact).
13. **`checkout.session.completed` is absent from shipped code** (`ai-labs.md:18`, `:110`) — every hit in
    `app` is under `knowledge/plan/**` (planning docs), zero in Go; positive control
    `customer.subscription` returns live hits in `internal/organization/licensing.go`.
    **No labs↔credits linkage**: `git grep internal/credits -- internal/labs/` exits **1**; positive
    control lists 10+ importers, all coursebuilder / licensing / backend wiring — so *"meters Course
    Builder only"* holds. `POST /credits/purchase` removed in Wave 13 (`internal/web/backend/credits/
    handler.go:12`).
14. **`r.Cookie(...)` is called nowhere in `clerkenstein/`** (`clerkenstein.md:173`) — `git grep` at the
    authoring ref returns **7** `Cookie` hits total across the section, all `Set-Cookie` *emission* sites
    or a test/JS header; zero `.Cookie(` reads. (Bare `grep` alone would have been unsound here — §5
    rule 44.)
15. **AI-Labs code surface** — all five migrations exist by exact filename
    (`20260617203555_add_labs_catalog.sql`, `20260529072659_add_lab_session.sql`,
    `20260617120000_add_lab_session_model.sql`, `20260717151144.sql`,
    `20260626120000_add_org_subscriptions.sql`); all six Ent schemas exist; `LabsTier`
    essential/professional/frontier and all five `CapReason` literals match
    (`labs_budget.go:26-28`, `:247-251`); labs-api default `:7070` + `LABS_API_PLATFORM_TOKEN`
    (`labsapi/client.go:6`, `:43`; `main.go:744-745`); reconciler `Interval: 30 * time.Second`
    (`spend_reconciler.go:60`); `ReportEvent` allowlist `status`/`grade`
    (`labsession.go:383-384`, `:402`, `:418`); `stripe-go/v74 v74.30.0` (`go.mod:62`);
    `cmd/labsdemo` on `:8099` (`main.go:115`); all 12 named test files exist and
    `internal/payments` really has **0** Go tests; `TestPricingDerivation` (`cost_test.go:103`) and
    `TestPrivacyBackstop_AutoScopesToCallerOrg` (`privacy_backstop_test.go:25`) both exist.
    `MarginMarkup = 1.40` (`cost.go:133`), `PricePerCreditUSD = 0.45` (`:142`),
    `DefaultSeedBalance int64 = 500` (`:231`), packages starter 50 / team 200 / scale 500 (`:201-203`).
    *"Credits has NO GraphQL"* holds — the four `credit` hits in the SDL are `FeatureType` quota fields
    (`resolver_queries.go:518` → `OrgCheckFeaturePermission`) and one prose word in `academy.graphqls`,
    not the ledger.
16. **Clerk SDK version drift** (`clerk-integration.md:103`) — `@clerk/nextjs ^6.39.2` in **all four**
    named surfaces (`apps/{web,hiring,integration}` + `ant-academy/code/package.json:52`, exact line);
    `@clerk/clerk-expo ~2.6.18` at `next-web-app/apps/mobile/package.json:6` (exact) vs `~2.19.36` at
    `ant-academy/mobile/package.json:18` (exact) — **thirteen** minor versions; `app/go.mod:31` =
    `clerk-sdk-go/v2 v2.7.0` (exact) — and `colony v0.35.2` in `app`/`messenger`, `v0.34.3` in
    `sentinel`/`storage`, matching `clerkenstein.md:274-279` exactly.

**RE-DERIVED** (weaker — arithmetic the document itself displays, re-computed):

- `31 = 29 + 2` and `23 = 16 + 7` in `security_compliance.md` — but note I did **not** stop here; the
  SET behind both is enumerated at item 1 above, which is what actually clears them.
- "thirteen minor versions apart" (`2.6 → 2.19`) in `clerk-integration.md:103`.

**Other anchors opened and confirmed exact** (not enumerations, so listed separately):
`studio-desk/.env.example:45`; `app/internal/web/backend/graphql/graph/schemas/cms_queries.graphqls:106`
(`studioTask`); `app/internal/cms/studio/studioManager.go:119` (`exec studio/gen.py`);
`app/studio/CLAUDE.md:12-14`; `graphql-wundergraph/terraform/main.tf:20` (`= 1`), `locals.tf:8`
(`port = 8080`), `main.tf:48-49`, `config.prod.yaml:5`; `cms/terraform/main.tf:30`/`:39` (191 lines,
`module "cms"` at `service_desired_count = 0`) and `6efa1d5` → `f38c0c4` (2026-08-04) deleting
`build-production.yml` with the M810 body the corpus quotes; `jobsimulation/terraform/main.tf:15-40`
**at `6092c6d2`** (the `module "jobsimulation"` block is genuinely deleted there; the comment block is
exactly the LiveKit/Chime/SSM/atlas inventory the corpus describes — the checkout `462343b0` is the
commit *before* it, which is why the block is still present on disk);
`platform a2a3ee6^:docker-compose.yml:384/:386/:409` (the historical Directus service — image tag, port
and password all exact, and `admin@example.com` genuinely unfound via `git log --all -S`);
`app/internal/jobsimulation/ai/ai.go:263-277` (the `flag_use_azure_us` swap), `:85-88`
(`config.WithRegion("eu-west-1")`), `isThrottlingError` at `:129`/`:166`/`:325`;
`app/internal/coursebuilder/bedrock.go:100` (`return "anthropic-api"`) and `:109-112`;
`skills.go:75` (the division); `criterion.go:127`/`:168`/`:428`/`:450-475`;
`basevalidator/templates/checkValidationBulk.tmpl:27` (*"meets or does not meet"*) — and
`checkerEngines` really is stored-and-never-read on `CriterionValidator` (`:34`, `:72`, `:90`, no
`c.checkerEngines` read anywhere); `next-web-app/apps/{web,hiring}/src/hooks/useOrgSelection.tsx:94`,
`useResolveActiveOrg.tsx:107`, `useActivateMembershipOrg.tsx:81` — **all three exact in both apps**;
`apps/web/src/app/api/metabase/route.ts:35` (bare `'admin'`);
`ant-academy/code/src/graphql/server.js:14,18` (throws when unset), `src/lib/serverTenant.js:115-145`,
`src/lib/backendContent.js:36,102-103`, `app/api/academy/beacon/route.js:36,41-55`,
`app/layout.jsx:132` (`manifest:`), `public/academy-manifest.json` (`display: standalone`),
`tests/unit/next-scaffold.test.js:106,111`, `react-compiler-config.test.js:41`, `code/CLAUDE.md:255`
(`gpt-5.2` Responses API from the browser); `ant-academy/mobile/src/components/ClerkSignInForm.tsx:49-53`
(*"Sign-in needs another step that this app does not support yet"* — the second-factor abort is real);
`studio-desk/src/index.ts:96` and `app/services/userService.ts:16` (both the 4-role
`STUDIO_ACCESS_ROLES`); `clerkenstein/alignment/cmd/alignctl/run.go:134-135`
(`ExitRegressed = 2` / `ExitUnmeasurable = 3`), `clerk-backend/store.go:138`/`:151`,
`alignment/dna/clerk-2.6.0.json:131`, `clerk-frontend/server.go:186` + `clerk-backend/server.go:47`
(the BAPI/FAPI membership twins), `establishLocked` at `:663-670` minting `"sess_clerkenstein"`,
`handleSelectIdentity` at `:627-647` clearing `signedIn`/`sessID`, all five named `server_test.go`
tests present, `ALIGN_DIR` default `$base/../../alignment` (`gate.sh:30`), and the rext `alignment/`
section genuinely having no `scripts/` dir;
`platform fdfa189` (2026-04-17, `docker-compose.yml` −25 / `repos.yml` −3) and `045857c` — both exact,
so `intelligence.md` is clean on every claim a clone can settle.
Intra-corpus: `frontend_architecture.md:11`, `studio-desk.md:20`/`:21`,
`platform-migration-status.md:89`, `external_services.md:567`/`:579`,
`external_services.md#routing-what-is-actually-implemented`,
`shared_libraries.md#taxonomy-figures`, `coursebuilder.md:48`, and
`corpus/architecture/README.md:21` (which does carry *"cost tracking lives in `app`, not the `ai`
library"* — I nearly booked this as a wrong-construct anchor before reading the whole line, §5 rule 10).

---

## What I could not settle

- **`corpus/services/intelligence.md:18`** — *"Exposed an HTTP server on :8080 (PORT env override)
  serving only a `/_meta` health endpoint … all real work ran on a 5-minute async ticker."* The
  `intelligence` repo is in no clone set, so the code default cannot be read. Adjacent evidence is
  **consistent but not confirming**: at `fdfa189^` the compose service sets `PORT=9002` and publishes
  **no** `ports:` mapping at all — which the doc's own *"PORT env override"* clause anticipates, so this
  is not a refutation. Settled by cloning `anthropos-work/intelligence` and reading its `main.go`.
- **GitHub archive state** for `skiller`, `skillpath`, `graphql-wundergraph` (`service_taxonomy.md:3`,
  `:139`, `:140`, `:377`) — unmeasurable from any clone, as `:124-133` itself states; `gh` is not
  installed and the repos are private. Settled only by `gh api repos/anthropos-work/<repo> --jq
  .archived`. (The *Jobsimulation* case is the exception and it **is** settled — four post-dated commits
  refute a flat archive claim, and the row already says so.)
- **`ai-labs.md:5-6`** — *"`anthropos-work/AI-Labs` is the live Go control plane … behind
  `anthroposlabs.com`"*. No clone; consistent with `internal/labs/labsapi/client.go`'s description of an
  external control plane, but the repo's existence and language are unverifiable here.
- **`ai-labs.md:16`** — *"milestones M600–M607, all planned"*. Two of them (`m601`, `m601b`) surfaced
  incidentally in `app`'s tree during an unrelated grep; I did not enumerate the range, to stay clear of
  the plan-directory bar.
- **`clerk-integration.md:52`** — *"`jwt.Verify` + `jwks.Client`, 1-minute leeway"*. `colony` is a
  private Go module pulled at Docker build; it is in no clone set. Settled by reading
  `colony/authn/provider/clerk` at `v0.35.2`.
- **`security_compliance.md:7`, `:261`** — *"DPA v1.4 … 18 approved sub-processors"*, and the whole
  infrastructure block (`:21-31`, `:38-43`, `:155-160`, `:181-184`). No terraform for the *platform*
  VPC/RDS is in any clone (only per-service modules), so none of it is checkable here.

### One cross-file observation, outside my partition

`security_compliance.md:7` and `:198` both cite **`external_services.md:579`** for *"no ordered EU-first
fallback chain"*, and `:579` is exactly that sentence. Root **`CLAUDE.md`** cites
**`corpus/architecture/external_services.md:546`** for the identical claim; `:546` is a struck
*"Restart Wundergraph: `docker compose restart graphql`"* line. My assigned files hold the correct
anchor; the drifted one is in the repo's highest-propagation instruction file (§5 rule 19's corollary —
*"check the instruction files first"*). Not booked, because it is not in my file set.
