# Seat F — reading #24

Ground truth re-verified at this reading's open (all `git rev-parse --short=8 HEAD`):
`app b948604f` · `platform 0c91421d` (per the ADDENDUM, **not** `0dab54df`) · `next-web-app bb3313bc` ·
`sentinel 88bc5592` · `storage 4ce8ece5` · `messenger fa47850d` · `cms ca50c817` ·
`graphql-wundergraph 60c229f3` · `roadrunner 87d8d443` · `jobsimulation 462343b0` ·
`studio-desk 14a5442a` · `ant-academy 9c3843cd` · `rosetta-extensions` authoring copy `09d0607` ·
nested `app/studio` and `cms/studio` both `aeec036a`.

Where a passage names a ref that is not the checkout (`app 9d00a313`, `app 2035f9a4` = `origin/main`,
`app origin/main`, `platform 2adcf71` / `0dab54d`, `storage 9f8cb53` / `63bffc8`,
`jobsimulation 6092c6d2`, `cms 6efa1d5` / `f38c0c4`), I graded at **that** ref via
`git -C <clone> show <sha>:<path>` (§5 rule 33). All named refs were reachable in the clones.

**Shell hazards hit and worked around (recorded so the numbers are auditable):**
zsh applies the `:r` history modifier to `"$c:repos.yml"`, silently turning ten `git show`s into ten
`fatal:` lines and an empty file that reads exactly like "no matches" — caught only by reading stderr
(§5 rule 1). Every absence below carries a same-pass positive control.

## Positive controls

| file | `wc -l` | read in full? |
|---|---|---|
| `corpus/services/ant-academy.md` | 457 | yes |
| `corpus/architecture/architecture_overview.md` | 417 | yes |
| `corpus/architecture/shared_libraries.md` | 247 | yes |
| `corpus/services/roadrunner.md` | 206 | yes |
| `corpus/architecture/dependency_map.md` | 103 | yes |
| `corpus/services/db-backup.md` | 31 | yes |
| **total** | **1461** | |

---

## Blockers

### B1 — `SKILLER_STREAM` file count at `origin/main` is wrong at either scope (3 Go files, or 6 repo-wide — never 4)

- **Anchor:** `corpus/architecture/dependency_map.md:59` — *"…and `SKILLER_STREAM` has **6** Go
  occurrences across 4 files, not one — so the sentence was false at the second of the two refs it
  cited…"*
- **Why it is false:** measured at `app` `2035f9a4` (= `origin/main`, the ref the cell names), with the
  `'*.go'` pathspec the sentence itself scopes to:

  ```
  git -C stack-demo/app grep -n  SKILLER_STREAM 2035f9a4 -- '*.go'   →  6 lines
  git -C stack-demo/app grep -ln SKILLER_STREAM 2035f9a4 -- '*.go'   →  3 files
  ```
  The three files are `main.go` (`:1532`, `:1537`), `subscriber_merge_test.go` (`:907`, `:1015`,
  `:1038`) and `subscriber_wiring.go` (`:165`). The occurrence count **6 is exactly right**; the file
  count **4 is not**. Dropping the `'*.go'` pathspec gives **6 files** (adding
  `knowledge/plan/releases/09.00-support-in-app/m905-subscriber-merge/decisions.md`,
  `knowledge/skiller-domain.md` and `terraform/main.tf`) — so **4 is unsupportable at both the scope
  the sentence declares and the only other scope available**, and the two true numbers happen to be
  the same digit (6) at different places in the sentence, which is how the wrong one got in.
  Positive control in the same pass: `git grep -ln CMS_STREAM 2035f9a4 -- '*.go'` → 4 files, so the
  pipeline distinguishes 3 from 4.
  This is §5 rule 17/24 exactly — a count right in one dimension while the claim it supports is
  false — inside a cell whose whole purpose is to correct an earlier miscount.
- **Confidence:** high

### B2 — the same cell asserts "not one of those line numbers resolves", then names one that does — at both refs

- **Anchor:** `corpus/architecture/dependency_map.md:59` — *"…its one Go occurrence, `main.go:1276`, is
  an `AddSubscriber` (handler: `internal/roles/roles.go:791`). **One ref, deliberately.** This cell
  also named origin/main `2035f9a4` until M257x iter-98, where **not one of those line numbers
  resolves** (`main.go:1276` is `apiKeyManager,`)…"* — and, three sentences later in the same cell:
  *"…and `internal/roles/roles.go:791` is `SkillerSubscriber()` at each."*
- **Why it is false:** `internal/roles/roles.go:791` is a member of the line-number set the sentence
  quantifies over, and it **does** resolve at `2035f9a4`:

  ```
  git -C stack-demo/app show 2035f9a4:internal/roles/roles.go | sed -n '791p'
  → func (r *RoleManager) SkillerSubscriber() *pubsub.Subscriber {
  ```
  Identical construct at `b948604f` (verified: `:789` `}`, `:790` blank, `:791` the `func`). So the
  true split is **6 of 7 anchors drift, 1 holds** — `main.go:287`→`:325`, `:637`→`:746`,
  `:1039`→`:1149`, `wiring.go:127`→`:132`, `:180`→`:185`, `main.go:1276`→`apiKeyManager,`, and
  `roles.go:791` unchanged. The cell contradicts itself inside one markdown cell, and the two halves
  point a reader in opposite directions about whether the surviving anchor can be trusted.
- **Why I hesitated (per the grading rule):** the cell self-corrects later in the same cell, so a
  reader who finishes it is not left wrong. I booked it because §5 rule 21 makes intra-document
  self-contradiction the cheapest and most-missed class, because the *first* half is the emphatic one
  ("**not one**", bolded) and reads as the finding, and because the briefing says to resolve doubt
  upward.
- **Confidence:** medium

---

## Minors

- `corpus/services/ant-academy.md:258` — *"~26 Playwright e2e spec files (tests/e2e/)"*. Measured:
  `ls code/tests/e2e/*.spec.js | wc -l` → **31** (the directory also holds `_helpers.js`,
  `_academyBackendMock.js` and a `widgets.spec.js-snapshots/` dir, none of them specs). Tilde-qualified
  magnitude for a growing test tree; no operational consequence. The sibling claim *"1000+ Vitest
  tests"* in the same cell is a floor and holds comfortably (244 test files, ~2,701 `it(`/`test(` sites).
- `corpus/services/ant-academy.md:145` — *"a separate, unauthenticated `code/public/catalog.json`
  (~2,667 entries)"*. Measured: the committed file parses to `courses: 2715` (and `languages: 7`).
  Same class — a tilde-qualified count of regenerating authored content.
- `corpus/services/ant-academy.md:135` — *"`emptyCatalogView() = { chapters: [], skillPaths: {},
  series: [] }`"*. Source (`code/src/lib/serverTenant.js:115-117`) returns **five** keys: those three
  plus `bundles: PUBLIC_BUNDLES` and `catalogVersion: CATALOG_VERSION`. The conclusion the passage
  draws from it (**0 cards**) is unaffected — `bundles` carries no tenant metadata and is not a grid
  card — so this is a truncated quotation, not a wrong mechanism.
- `corpus/services/ant-academy.md:411` — the repo-local-skills table's catch-all row names seven
  helpers; the repo also ships `export-path` (`ant-academy/.claude/skills/export-path`), unnamed. The
  row is explicitly a partial list ("Other content-pipeline helpers"), so this is an omitted list
  member, not a false claim.

## Positively cleared (audited zeros)

Labelled per the briefing. **ENUMERATED** = I derived the predicate's member set from source
independently and compared cardinalities. **RE-DERIVED** = I only re-ran the arithmetic the document
itself shows (the weakest clearance).

**ENUMERATED**

1. `architecture_overview.md:363-369` — *"31 of 135 schemas"*, *"the **29** live `OrganizationMixin{}`
   users — a 30th is commented out at `user_resource.go:22`"*, *"**23** schemas carry an
   `organization_id` with no policy at all (16 is the neither-mixin subset)"*. This is the fence §5
   rules 17/24 record as having failed **four** times, so I derived every set rather than any sum:
   139 `.go` files in `app/internal/data/ent/schema/`, minus 4 non-schema files (`database_types.go`,
   `mixin.go`, `skillpath_mixins.go`, `skiller_mixins.go`) = **135** schemas. Live `OrganizationMixin{}`
   = **29** (`grep -c` returns 30; `user_resource.go:22` is `// OrganizationMixin{},  // We need to work
   on this`, read in context at `:18-24`). Own org-filtering `Policy()`: `organization.go:56` and
   `org_membership.go` — `user.go:116` also declares one but it is `FilterSameUserRule`, user-scoped,
   correctly excluded. **29+2 = 31.** For the unpoliced side: 51 schema files carry an
   `organization_id`; subtracting the 31 policed leaves **24**; of those, `academy_feedback.go` carries
   `UserMixin{}`, whose `Policy()` (`mixin.go:99`) is a real policy — so **23** carry one with *no
   policy of any kind*, and 16 of the 23 have neither org mixin (the other 7 are the
   `OrganizationIDMixin{}` users). My 16 matched `security_compliance.md`'s named list file-for-file.
   **I nearly booked this as a blocker**: a first pass matched `CreatedByUserMixin{}` with the pattern
   `UserMixin{}` and reported 5 policy-bearing files where there is 1, which would have made the
   corpus's 23/16 read as 24/17. Opening each `Mixin()` body refuted my own measurement. Recording it
   because it is the shape rule 24 warns about, arriving from the auditor's side.
2. `architecture_overview.md:12, 71, 156-165` — *"five containers"* / *"the always-on floor"*. Derived
   from compose at `0c91421d`: `docker-compose.yml` declares 5 services (sentinel, backend,
   studio-desk, next-web-app, gotenberg); `common.yml` (pulled in by `include:`) declares postgresql +
   redis = **7 in the effective topology**. `profiles:` appears at exactly `:110` `[core, backend, all]`
   (backend), `:141` `[studio-desk, all]`, `:168` `[frontend, all]`, `:183` `[core, backend, all]`
   (gotenberg) — so postgresql, redis and **sentinel** declare none and are in every selection.
   `core` therefore = backend + gotenberg + the 3-service floor = **5**, of which **2** are ours
   (`Makefile:10` `PROFILE ?= core`). Also confirms `:184-186` — `next-web-app` (`:165-167`
   `depends_on: backend`) and `studio-desk` (`:138-140`, same) cannot be selected alone.
3. `architecture_overview.md:25-33, 71, 188-195, 223` — the `838d907` / `0c91421` support-service
   deletion. Verified from the platform log (`838d907` *"drop the storage, messenger and
   customerio-sync containers"*, 2026-08-05, merged `0c91421`, 2026-08-05): `repos.yml` at `0c91421`
   holds **4** entries (app, sentinel, next-web-app, studio-desk) against **6** at `0dab54d` and **9**
   at `2adcf71`; no `storage-legacy` / `messenger` / `customerio-sync` profile token survives anywhere
   in compose. Compose services at `0dab54d` were 8 declared + 2 = **10 effective**, matching
   `dependency_map.md:31`.
4. `architecture_overview.md:80` — *"0 hits for `github.com/anthropos-work/authn` across all seven Go
   clones, against a positive control of `colony` required by all seven"*. Ran exactly that, per clone
   at its own pinned sha: authn 0/7, colony 7/7 (`go.mod:16/8/7/7/10/12/7`). Also confirmed
   `shared_libraries.md:173` — sentinel imports no `colony/authn` in any `.go` file (positive control:
   colony itself in 2 sentinel Go files).
5. `architecture_overview.md:329-355` — the STORAGE_RPC_ADDR retraction, at each ref it names.
   At `app 9d00a313`: exactly **3** Go hits, all comments — `main.go:451`,
   `internal/jobsimwiring/wiring.go:101`, `internal/storagens/callsites_test.go:189`; repo-wide
   **29 lines across 18 files**. At `app origin/main` (`2035f9a4`): still 3, all comments, and
   `main.go:504` reads verbatim *"now — the standalone service takes no traffic and STORAGE_RPC_ADDR
   is gone."* At the older `b948604f` the variable **is** read live, at
   `internal/jobsimwiring/wiring.go:115` — exactly as the passage says. `storage/terraform/main.tf` at
   `9f8cb53` is **18 lines**, `:9-11` carrying the *"The ECS service that used to live here is GONE
   (v9.0 'support-in-app')"* comment and `:13-16` the `prevent_destroy` rationale.
6. `architecture_overview.md:281-300` — the AI-routing paragraph, every anchor opened with context at
   `app b948604f`: `internal/jobsimulation/ai/ai.go` `getClient` `:262-276` (switch → `azureClientEu`
   default → `flag_use_azure_us` → `azureClientUs`), `isThrottlingError` declared `:129` and applied at
   `:166` and `:325`, Bedrock `config.WithRegion("eu-west-1")` at `:85-88`; Mistral OCR at
   `internal/cms/studio/markdownManager.go:11` (import) and `:19`
   (`mistral.NewMistral(nil, os.Getenv("MISTRAL_API_KEY"))`), called from `studioManager.go:583`.
   Cross-file citation `external_services.md:579` resolves to *"There is **no ordered EU-first fallback
   chain.**"* and `:602-607` to the four-live-plus-one-latent paragraph.
7. `architecture_overview.md:179, 398` — *"its 23 run-state tables"*. Enumerated the ported jobsim
   schemas in `app/internal/data/ent/schema/` by their own `jobsim-in-app` provenance marker: **23**
   files (actor, activity_event, ai_interaction, anticheat_evidence, anticheat_result,
   chime_recording, code_submission, collaborative_asset, conversation_extraction, interaction,
   interview_aggregated_report, interview_extraction_result, job_simulation_session, realtime_call,
   recording, taskcheck, tasksubcheck, validation, validation_attempt_result,
   validation_attempt_skill_result, validation_check_result, validation_criterion_result,
   validation_test_run).
8. `architecture_overview.md:3, 251, 280, 308, 311, 317` — the router's two states. `2adcf71`,
   `b56d731` and `360efd4` are all 2026-07-31; `graphql-wundergraph/terraform/main.tf:20`
   `service_desired_count = 1` and `locals.tf:8` `port = 8080` (so *"Cosmo Router (port 8080)"* holds);
   `supergraph-config-prod.yaml` lists **one** subgraph, `subgraphs.conf` is the single line
   `BACKEND=v1.360.0`, `schemas/` holds only `backend.graphqls`; `915da06` (2026-07-29) is the commit,
   and its body confirms the 3→2→1 sequence its own squashed subject renders as "2→1". No `5050`
   anywhere in `docker-compose.yml`/`common.yml` (positive control: `8082` appears 8 times).
9. `shared_libraries.md:41, 69, 109-110, 186` — every version pin, read from each repo's `go.mod` at
   its own sha. colony: app `:16` v0.35.2, sentinel `:8` v0.34.3, storage v0.34.3, messenger v0.35.2,
   cms + jobsimulation **v0.35.1** (the "third pin" the row describes), roadrunner `:7` v0.34.3.
   proto: app `:18` v1.210.0, sentinel `:9` v1.200.0, storage v1.196.0, messenger v1.210.0, cms
   v1.207.0, jobsimulation v1.205.0, roadrunner v1.196.0. ai: v1.40.2 in app, `cms/go.mod:9`,
   `jobsimulation/go.mod:11` — and **absent from roadrunner**, whose only shared-lib requires are
   colony + proto. taxonomy v1.2.0 at `app/go.mod:20`, `sentinel/go.mod:21 // indirect`,
   `storage/go.mod:25 // indirect`, `messenger/go.mod:9` direct, `cms/go.mod:13`,
   `jobsimulation/go.mod:15` — **6 of the 7 on-disk Go repos**, roadrunner the sole exception.
10. `shared_libraries.md:186` — *"that set is 11"* / the four extra `type: go` names. Derived the
    **historical** vocabulary from `repos.yml`'s whole git history (10 commits touch it): **14 distinct
    names ever**, of which **11 are `type: go`** — app, chronos, cms, intelligence, jobsimulation,
    messenger, roadrunner, sentinel, skiller, skillpath, storage. All four of skiller/skillpath/
    chronos/intelligence are present in the first revision `a2a3ee6`, and `a4db680` (2026-07-21,
    *"decommission standalone skillpath service (M507)"*) is the commit that removes the last of them.
11. `shared_libraries.md:70, 83` — the RPC-handler enumeration. Every `NewPublisher`/handler site in
    `app` at `b948604f`: mux registrations at `main.go:1187` Users, `:1188` Organizations, `:1196`
    Skiller, `:1204` JobSimulation, `:1213` CMS inside `if cmsRPCServer != nil` at `:1212-1214`, and
    LabSession created `:1227` / **registered `:1228`** — **six**, with no `SkillPathSessionService` and
    no RoadRunner service. `internal/skillpaths/skillpaths.go:27-31` carries the quoted *"the drop-in
    for the removed skillpath RPC client"*. `jsrunner.NewRunnerManager` is at
    `internal/jobsimwiring/wiring.go:123` at `9d00a313` and `:118` at `b948604` — both the refs the
    corpus names, each correct at its own. `ROADRUNNER_RPC_ADDR`: **0** in `app` Go at `9d00a313` and
    **0** in platform at `0dab54d` (positive control: `JUDGE0_BASE_URL` → 1 Go file;
    `AWS_REGION` → `docker-compose.yml` 2 hits); `docker-compose.yml:118` at `0dab54d` is indeed
    `AWS_REGION=eu-west-1`, as the parenthetical says.
12. `shared_libraries.md:97-99` — `storage/internal/migration/migration.go:13` really does import
    `legacyStorage "github.com/anthropos-work/proto/go/simulator/storage/v1"` (used at `:93`).
13. `roadrunner.md` — the whole port/env/worker surface, from the repo and from compose at the ref each
    claim names. `cmd/root.go:84` `cmp.Or(os.Getenv("RPC_PORT"), "8081")` and `:110`
    `cmp.Or(os.Getenv("PORT"), "8080")`; at `platform 2adcf71` the deleted block published
    `10400:10400`/`10401:10401` at `:291-292` and set `ENVIRONMENT`/`JUDGE0_BASE_URL`/`PORT=10400`/
    `REDIS_ADDR`/`REDIS_STREAMS_INDEX=4`/`REDIS_WORKER_INDEX=0`/`RPC_PORT=10401` at `:296-302` — with
    **no** `JUDGE0_API_KEY` and **no** `SENTRY_DSN`, exactly as the two "not set here" rows say.
    Worker: `Concurrency: 10`, queue `roadrunner:default`, task type `roadrunner:submissionresult`,
    the single `asynq.NewTask(..., asynq.MaxRetry(3))` at `runner.go:126` inside the one
    `CreateSubmission`, and `maxRetries = 15` with `time.Sleep(1 * time.Second)` at `:244`/`:273`.
    `languages.go:26` is `case "py":` with no `"python"`. **Zero** `*_test.go` files, and
    `Dockerfile:18` is `RUN go test -v ./...`.
14. `roadrunner.md:24-32, 120` — the scoped-to-Go retraction. **0** Go hits at `app b948604f` and
    `jobsimulation 462343b0` for all three of `ROADRUNNER_RPC_ADDR`, `RoadRunnerService`,
    `roadrunner:10401`. The case-insensitive whole-clone sweep reproduces the row **exactly**:
    app **25**, jobsimulation **8**, platform **3**, studio-desk **3**, next-web-app **1** (and 0 in
    cms/sentinel/storage/messenger). In `jobsimulation`, Go source holds exactly one mention —
    `internal/runner/runner.go:3`, *"…(formerly the standalone \"roadrunner\" service)."* — while
    `grep -in` over the clone returns **14 lines across 8 files**, of which **5 across 3** are
    exact-case. `app/internal/jobsimulation/simulator/stream_handlers.go:30-34` carries the
    *"NOT stream handlers"* note.
15. `roadrunner.md:15-20, 39-42, 47-49` — the `repos.yml`/compose history. At `2adcf71`, roadrunner's
    entry is at `repos.yml:29-31` reading *"legacy — folded into app; backend calls Judge0 directly"*,
    one of **9** entries; jobsimulation's is at `:17-19`, and its compose service starts at
    `docker-compose.yml:83` with `profiles: [graphql, jobsimulation, all]` at `:140`. `d11a403`'s diff
    removes `- name: cms`, `- name: jobsimulation` **and** `- name: roadrunner` — the message is wrong,
    the diff is the fact, exactly as the passage insists. `repos.yml` = 6 entries at `0dab54d`, 4 at
    `0c91421`.
16. `dependency_map.md:7, 13, 24, 50` — `docker-compose.yml:48` is `AUTHORIZATION_ADDRESS=http://sentinel:8087`
    and it is the **only** service address left: **zero** `*_RPC_ADDR` in `docker-compose.yml` or
    `common.yml` at `0c91421`. Backend's `depends_on:` is `:101-109` with the in-line comment at
    `:102-103` quoted verbatim (*"storage, messenger and customerio-sync are not services any more —
    this one container serves all three in-process."*). studio-desk's is `:138-140`, `backend` alone.
    `SKILLER_STREAM=skiller` at `:71`; `GOTENBERG_URL=http://gotenberg:3200` at `:57`.
17. `dependency_map.md:58` — *"21 live handlers"* on messenger's `backend` subscriber. Counted the
    `pubsub.EventHandler(...)` members inside `flow.go:72`'s `AddSubscriber("backend", …)` block
    (`:74-103`), **excluding** the one commented out at `:83`: **21**. Corroborated in-source —
    `app/internal/messenger/flow/streams.go:70` calls it *"the 21-handler set"*. `flow.go:105`
    (jobsimulation) and `:109` (cms) are the other two subscribers. The takeover guard is at
    `app/main.go:1442` at `9d00a313` (`verifyConsumerGroupExists(… StreamBackend, StreamJobSimulation,
    StreamCMS)`), with `StreamBackend = "backend"` at `internal/messenger/flow/streams.go:65`.
18. `dependency_map.md:59` — the consumer-only finding itself (as distinct from B1/B2). Enumerated
    **every** publisher constructor in `app` Go at `b948604f`: `main.go:287` (`serviceName`), `:637`
    (`SKILLPATH_STREAM`), `:1039` (`CMS_STREAM`), `jobsimwiring/wiring.go:127` (`AI_USAGE_STREAM`),
    `:180` (`JOBSIMULATION_STREAM`), plus `internal/deadletterqueue/dead_letter_queue.go:38` (the DLQ,
    a `redisstream.NewPublisher`, naming no stream env var). **None** names `SKILLER_STREAM`, whose one
    Go occurrence is the `AddSubscriber` at `main.go:1276` handled by
    `internal/roles/roles.go:791 SkillerSubscriber()`. Holds at `2035f9a4` too.
19. `dependency_map.md:25` — Studio-Room's provider registry. At the **nested** `app/studio` checkout
    `aeec036a` (invisible to a `git -C stack-demo/app grep`, per the ADDENDUM), `services/ai.py:705-708`
    is the whole `providers = {openai, azure, anthropic}` dict; `tools/pdf2md.py:24` is
    `from mistralai import Mistral`; and `git -C stack-demo/app/studio grep -i mistral aeec036a`
    returns **22 lines across 3 files** (requirements.txt 1, tools/pdf2md.py 18, tools/r3.py 3) —
    the figure quoted, at the ref quoted.
20. `dependency_map.md:19` — the storage `depends_on` vs data-path split, including the cross-file
    citation: `storage.md:40` is *"Storage is stateless and owns no database…"* and `:47` is
    *"**Database**: none — all state lives in S3…"*. Both anchors resolve **to the construct claimed**,
    not merely to an existing line.
21. `ant-academy.md:255` — the font row, which the passage flags as newly repaired (§5 rule 18 makes
    that the highest-risk text). `code/app/layout.jsx:1` imports exactly
    `{ Work_Sans, Instrument_Serif }` and is the file's **only** `next/font/google` line; `:41` and
    `:51` are the two constructors; `code/academy.css:1` says *"Display + mono usages fall back to
    system fonts."* — so *"Neither DM Sans nor JetBrains Mono is loaded"* holds (the surviving
    `--font-dm-sans` / `--font-jetbrains-mono` names are aliases, per `layout.jsx:38-40`). FA Pro is
    vendored at `code/public/assets/fontawesome/{css/all.min.css, webfonts/*.woff2}` with **no**
    `@fortawesome` dependency in `code/package.json`, so the token-less-install claim holds.
22. `ant-academy.md:227-240` — the language-switch verdict, the other newly-repaired block.
    `src/i18n/locale.js:10` is `SUPPORTED_LOCALES = ['en','it','es','fr','de','nl','pt']` (**7**);
    `src/components/TopBar.jsx:76` mounts `<LanguageSelector />`; `src/views/public/PublicHeader.jsx:20`
    mounts the 2-way `<LocaleSwitch />`. Both components exist and are distinct — so the retraction
    ("there is a dropdown after all") is the correct direction.
23. `ant-academy.md:117-167, 171-190` — the DB-authoritative read chain, file by file.
    `app/(authed)/page.jsx:80-88` `resolveCatalogView()` branching to `getPublicCatalogView()` /
    `getServerCatalogView()`; `serverTenant.js:144` is literally
    `const view = (await getBackendCatalogView(eids)) ?? emptyCatalogView()`; `:131-132` carries the
    quoted *"the cutover is intentional, not reversible-on-error"* — so the cited window `:115-145` is
    right and names the right constructs. `graphql/server.js:14,18` reads
    `NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT` and throws when unset (failure leg 1);
    `backendContent.js:102-103` reads `academyCatalogSeries` + `academyCatalogSkillPaths`;
    `serverChapterBody.js:51-52,67` is the `getBackendChapterBody` → `{ notFound: true }` path and
    `app/not-found.jsx:43` is *"You wandered off the trail."* `draftMode.js:16-25` confirms the
    whitelisted-`development` hard block. On the backend side `app/internal/academy` exists and
    `academyCatalogSeries` is served from `internal/web/backend/graphql/graph/resolver_academy.go`.
24. `ant-academy.md:262-292` — the `isPublic` matcher table. Read the whole `createRouteMatcher([...])`
    block in `code/proxy.js:112-188` and compared it row-for-row: every group in the table is present,
    both `_meta` spellings, `/api/verify/(.*)`, `/api/ai/chat`, the two conditional dev-only spreads
    (`DEV_LOGIN_ENABLED`, `VISUAL_BYPASS`), and **no** `/api/chapters/*`. The "these are globs, not
    pages" caveat holds: `code/app/(public)/library/` contains only `page.jsx` — there is **no**
    `/library/[slug]` — while `code/app/(authed)/courses/[slug]` exists.
25. `ant-academy.md:194-218` — the M245 durability block, against the rext authoring copy at `main`.
    `demo-stack/ant-academy.sh` assigns its **five** patch variables at exactly `:56` (dev-origins),
    `:66` (fs-published), `:75` (fs-published-body), `:84` (fs-published-public), `:90`
    (back-to-cockpit); `:576-583` is the M220 comment explaining that `BENCHMARK_VISUAL_BYPASS` /
    `NEXT_PUBLIC_E2E_AUTH` are gone from the launch env; and `stack-verify/live/autoverify.sh:459` is
    assert *(f)*, *"THE ACADEMY RENDERS ITS CATALOG"*. `ant-academy.md:330-331`'s cockpit anchors also
    hold: `demo-stack/cockpit.py:812` (client-side `document.cookie = 'e2e_persona=...'`), `:1496`
    (`Set-Cookie` on the `/go` 302) and `:327` (the comment naming both).
26. `ant-academy.md:23, 251-260, 419-424` — port 3077 (`code/package.json:21`
    `next dev --port 3077`), mobile `dev:web` + `dev:bundle`, `engines.node >=22` (`:8`),
    `code/vercel.json` = exactly `{"framework":"nextjs"}`, `academy-manifest.json:7`
    `"display":"standalone"`, `proxy.js:241` `headers.set("x-locale", lang)`, and all four named
    workflows + `cog.toml` present.
27. `architecture_overview.md:164, 221-222` and `dependency_map.md:31` — the prod dispositions, each
    at the ref it names. `platform/postgresql/Dockerfile` builds pgvector `v0.4.4` into the image
    (so *"custom image with `pgvector`"* holds). `jobsimulation` at `6092c6d2` (2026-08-04): the
    `module "jobsimulation"` block **is** deleted, the file is 56 lines, `:15-22` is the M810 comment
    naming the destroyed ECS service / task definition / ECR repository, `:24-40` is the surviving
    LiveKit+Chime+SSM+atlas ownership, and the literal `service_desired_count` appears **nowhere**
    (the one `grep` hit at `:19` is the substring `desired_count = 0` inside prose). `cms` at its
    checkout: `terraform/main.tf:39` `service_desired_count = 0`, and `6efa1d5`
    (*"drop build-production — the cms ECR repository is decommissioned (M810)"*) merged as `f38c0c4`,
    both 2026-08-04 — the "report both, assert neither" framing is the honest one.

**RE-DERIVED (weak — recorded as such)**

- `shared_libraries.md:229-234` — the NodeID format's three worked examples. The `taxonomy` repo is in
  no clone set, so this is not settleable against source; but the documented algorithm reproduces all
  three exactly, which is more than an arithmetic re-run: SHA-1 of the *sanitized* name, first 4 hex
  uppercased — `"GO"` → `F63F` (`K-GOXXXX-F63F`), `"DOT NET"` → `DDE9` (`K-DOTNET-DDE9`),
  `"C SHARP"` → `5F5B` (`K-CSHAXX-5F5B`). Three independent hits rule out coincidence and confirm the
  sanitization the paragraph describes (uppercase, symbol expansion, space-separated words).
- `shared_libraries.md:77-88` — the Connect-RPC service list. The named services total exactly 13,
  consistent with *"at least 13"*; but the row correctly says this is a floor hand-enumerated from
  consumers, `proto` being in no clone set, so no independent enumeration was possible.
- `corpus/services/db-backup.md` (all 31 lines) — the file makes no `file:line` citation, names no
  repo in any clone set, and `db-backup` appears in **none** of the 14 names `repos.yml` has ever
  carried (consistent with its "production-only" framing). Nothing in it is falsifiable from the
  ground truth available; nothing in it contradicts another corpus claim I read. Not a clearance so
  much as an absence of surface.

## What I could not settle

- **GitHub archive states.** Three of my files assert them: `architecture_overview.md:3` and
  `dependency_map.md:23` (*graphql-wundergraph archived 2026-07-30*), and `roadrunner.md:37-38, 43, 57`
  (*skiller archived 2026-07-01, skillpath 2026-07-31; roadrunner and chronos NOT archived, chronos
  last push 2026-04-23*). Archive state is repository metadata, not tree content, so no clone can
  answer it, and `gh` is **not installed on this host** (`command not found`). Settled by one
  `gh api repos/anthropos-work/<repo> --jq '.archived, .pushed_at'` per repo. Worth flagging as a
  class: `architecture_overview.md:20` and `roadrunner.md:46` explicitly declare jobsimulation's
  archive state *"not visible to this corpus"* — a correct epistemic stance that the
  graphql-wundergraph and skiller/skillpath assertions do not adopt, on identically-unmeasurable
  evidence. I am not booking that as a finding because I cannot show the assertions are wrong, only
  that they are unfalsifiable from here.
- **`ant-academy.md:74` — "a grid of 65 real cards"** (and `:203`'s "0 course cards on `/library`").
  Both are runtime observations of a live demo stack with the FS-published demo-patches applied; no
  static artifact in the clone set yields 65. Settled by a `/demo-up` plus a card count on
  `:3077+offset`.
- **`shared_libraries.md:40, 48-59, 68, 96, 112-140, 148, 169-170, 183-185, 193`** — everything
  internal to `colony`, `proto`, `ai` and `taxonomy` (colony's `go 1.25.0` / `golang:1.26-bookworm`,
  its seven package descriptions, the commented-out rate-limiter reject path, buf `v1.57.0`, the
  absent `buf.work.yaml`, the `ai.AI` method set and the Anthropic `panic`s, authn `v1.7.0` and its
  missing `return`, taxonomy's `go 1.21.0` / zero deps / 131-line `node.go`). These four repos are
  private Go modules pulled at Docker build and are in **no** clone set — the document says so itself
  at `:23-29`. Only their *pins* (cleared above), and the consumer-side facts, are measurable from
  here. Settled by cloning the four libraries at the pinned tags.
- **`architecture_overview.md:406-414` and `db-backup.md`** — infrastructure claims (VPC `10.0.0.0/16`,
  Multi-AZ RDS, the 6-hour backup cadence, the S3/Azure/Hetzner triple target, 30 s ECS health checks).
  The `infrastructure` repo has never been in a clone set. Settled by adding it, or by a terraform
  state read.
