# Seat C — reading #23

**Refs actually used at this reading** (re-verified at open, per §5 rule 26):
`app b948604f` (checkout; `origin/main` = `2035f9a40`, newest tag `v1.369.0`) · `app/studio aeec036a`
(own checkout) · `platform 0c91421d` (= `origin/main`, per the ADDENDUM; the frozen table's `0dab54df`
is stale) · `next-web-app bb3313bc` · `sentinel 88bc5592` · `storage 4ce8ece5` · `messenger fa47850d` ·
`cms ca50c817` (`f38c0c4`/`6efa1d5` present as *descendants*, not ancestors) ·
`graphql-wundergraph 60c229f3` · `roadrunner 87d8d443` · `jobsimulation 462343b0` (`6092c6d2` present
as a *descendant*; `origin/main` = `82cb66ec`) · `studio-desk 14a5442a` · `ant-academy 9c3843cd` ·
`rosetta-extensions` authoring copy `09d0607` on `main`.

Note on ref selection, stated in the open because it changes how several rows below read: **the `app`
checkout `b948604f` (v1.366.0) predates the v9.0 "support-in-app" fold.** `internal/storage/`,
`internal/messenger/` and `internal/customeriosync/` **do not exist** at `b948604f`; all three exist at
`origin/main 2035f9a`. Where a passage names no `app` ref but describes the post-fold state, I graded
it at `2035f9a` and said so per-claim rather than booking ~15 anchor "failures" that are an artifact of
the pin. Every such anchor **resolved exactly** at `2035f9a`.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/architecture/alignment_testing.md` | 542 | yes |
| `corpus/services/backend.md` | 318 | yes |
| `corpus/architecture/ai_architecture.md` | 305 | yes |
| `corpus/services/customerio-sync.md` | 147 | yes |
| `corpus/services/skillpath.md` | 112 | yes |
| `corpus/services/skiller.md` | 69 | yes |
| **total** | **1493** | |

Search-pipeline controls run in the same pass: (a) a `zsh` `$var:path` history-modifier bug silently
turned three `git show` calls into `fatal: ambiguous argument` — caught only because stderr was read
(§5 rule 1); re-run with `"${var}":path`. (b) `git grep -c 'module "jobsimulation"' origin/main` returned
**1** for a string that exists only inside a **comment** — the rule-24 "`grep -c` counts what does not
compile" trap, caught by opening the file. (c) Positive controls used: `SkillerService` (4 hits) against
`SkillPathSessionService` (0 hits); `import` in `app/studio` against `bedrock|boto3` (0 hits).

---

## Blockers

### B1 — `backend.md:19` states the compose project declares five services; it declares seven

- **Anchor:** `corpus/services/backend.md:19` — *"compose now declares **five** services and `repos.yml`
  **four** entries."*
- **Why it is false:** `platform/docker-compose.yml` @ `0c91421` opens with `include:` / `- common.yml`
  (`docker-compose.yml:1-2`). The project therefore declares **seven** services: the five in
  `docker-compose.yml` (`sentinel:5`, `backend:28`, `studio-desk:112`, `next-web-app:143`,
  `gotenberg:170`) **plus `postgresql` and `redis` from `common.yml`**. `repos.yml`'s "four" is exactly
  right (`app`, `sentinel`, `next-web-app`, `studio-desk` — enumerated), which is what makes the
  adjacent "five" read as an equally exact count.
  This is the miscount `corpus/ops/platform-alignment.md` §5 rule 30 names as load-bearing: *"a parser
  reading only `docker-compose.yml` computes `floor = {sentinel}` and concludes, wrongly and quietly,
  that a dead token starts nothing."* `postgresql`, `redis` and `sentinel` declare **no `profiles:` key**
  (verified: the only four `profiles:` keys in `docker-compose.yml` are at `:110`, `:141`, `:168`, `:183`;
  `common.yml` has none), so they *are* the always-on floor and a reader deriving it from `:19` gets it
  wrong.
  **It also contradicts two passages in my own file set**: `backend.md:276` (*"`make up PROFILE=backend`
  … also starts postgresql, redis, sentinel, gotenberg"*) and `customerio-sync.md:104` (*"compose exits 0
  and starts only the always-on floor (`postgresql`, `redis`, `sentinel`)"*) — both of which treat
  `postgresql` and `redis` as compose services of this project.
- **Confidence:** high

### B2 — `backend.md:301` says the most recent migrations are from May 2026; the newest are July 2026

- **Anchor:** `corpus/services/backend.md:301` — *"the most recent set of migrations (May 2026) touched
  simulation-type definitions and content JSON defaults."*
- **Why it is false:** enumerated `git -C stack-demo/app ls-tree --name-only b948604f terraform/migrations/`.
  The newest four are `20260724132049_cms_data_model.sql`,
  `20260724164346_ai_readiness_freeze_how_we_measure.sql`,
  `20260728103254_ai_readiness_snapshot_frozen_matched_sources.sql`, `20260729133514.sql`,
  `20260731131307.sql`, `20260731154527_academy_chapter_progress_completed_at.sql` — i.e. **2026-07-31**,
  not May. (Six May-2026 migrations exist, but they are not the most recent set.) Nor did the newest set
  touch "simulation-type definitions and content JSON defaults": they drop the two `local_*` session
  mirrors, add the cms data model, and add academy/AI-readiness columns.
  **Self-contradiction inside my own file set:** `skillpath.md:85-88` cites
  `app/terraform/migrations/20260729133514.sql:62-63` and then names *three* migrations that post-date it
  at `app 9d00a313` (`20260731131307.sql`, `20260731154527_…`, `20260803143844_…`) — all verified present
  at that ref. A reader who trusts `backend.md:301` concludes the schema has been static since May.
- **Confidence:** high

### B3 — `backend.md:33-34` omits the `backend` stream from `app`'s owned set, making "fifth member" an undercount

- **Anchor:** `corpus/services/backend.md:33-34` — *"**`app` owns the `skillpath`, `jobsimulation`, `cms`
  and `ai_usage` Redis Streams** — both producer and consumer in-process. **`skiller` is NOT a fifth
  member: it is consumer-only**"*
- **Why it is false:** enumerated every publisher constructor and every `AddSubscriber` in `app @ b948604f`.
  `app` **publishes to the `backend` stream** — `main.go:287` `pub, err := pubsub.NewPublisher(serviceName,
  redisClientStream)` with `SERVICE_NAME=backend` (`platform/docker-compose.yml:70`) — **and subscribes to
  it**, `main.go:1320` `subServer.AddSubscriber(serviceName, backendSelfSub)`. The producer+consumer set is
  therefore **five** — `backend`, `skillpath`, `jobsimulation`, `cms`, `ai_usage` — and `skiller` would be a
  *sixth*, not a fifth.
  **The same file says so 231 lines later**: `backend.md:264` — *"app is both producer and consumer of four
  of the five application streams — `backend`, `skillpath`, `jobsimulation`, `cms` — plus the `AI`/`ai_usage`
  usage stream."* The two passages disagree about whether `backend` is in the set, and `:34` cross-references
  `:264` as its own authority.
  Material because the same bullet warns that *"a second `AddSubscriber` on one stream overwrites the first"*
  — a reader working from the four-member list has an incomplete inventory of exactly the thing the warning
  is about.
- **Why I hesitated:** `:33` is a summary bullet that links to the fuller statement, and the canonical
  grading rule carves out "omitted list member" as MINOR. I booked it because the sentence makes an explicit
  **cardinality** assertion ("NOT a fifth member") that is wrong, not merely a short list.
- **Confidence:** medium

### B4 — `skiller.md:19` presents `skiller_rpc_addr = http://backend.internal.anthropos:8081` as a production-terraform assignment; no such identifier or literal exists in the clone set

- **Anchor:** `corpus/services/skiller.md:19` — *"`skiller_rpc_addr = http://backend.internal.anthropos:8081`
  in production terraform."*
- **Why it is false:** (a) **The identifier is wrong.** The terraform variable is
  `skiller_rpc_address` — `messenger/terraform/variables.tf:87` @ `fa47850d`, consumed at
  `messenger/terraform/main.tf:78-79` as `"name": "SKILLER_RPC_ADDR"` / `"value": "${var.skiller_rpc_address}"`.
  There is no `skiller_rpc_addr` variable in `app`, `messenger`, `cms`, `jobsimulation`, `storage` or
  `sentinel` (searched all six at HEAD; the only hits are `CHANGELOG.md` prose and Go `os.Getenv` calls).
  (b) **The assignment is not in any clone.** `messenger/terraform/variables.tf:87-90` declares the variable
  with **no default**, and the literal `internal.anthropos` appears in **zero** `.tf` files across the whole
  clone set — only in `app/knowledge/service-dependencies.md:46`.
  **`backend.md:118-121` states the correct form of this exact claim and contradicts `skiller.md`:**
  *"the literal value is set in the un-cloned `infrastructure` root module: every cloned service module
  *declares* its `*_rpc_addr` variable with **no default** … so the address is derived, not read from a
  `.tf` in the clone set."* `skiller.md` writes it as a readable `key = value` assignment with none of that
  qualification.
- **Why I hesitated:** the *value* (`http://backend.internal.anthropos:8081`) is corroborated and correct,
  and `skiller_rpc_addr` is a substring of `skiller_rpc_address`, so a sloppy grep still lands. I booked it
  because the corpus's own canonical statement of this claim explicitly says the assignment is **not**
  findable in a clone, and a reader sent to "production terraform" to change it will not find it.
- **Confidence:** medium

### B5 — `ai_architecture.md:35` anchors `mistral-ocr-latest` at `pdf2md.py:24`; that line is the import, the literal is at `:127`

- **Anchor:** `corpus/architecture/ai_architecture.md:35` — *"Python-side in the in-image studio tree at
  `studio/tools/pdf2md.py:24` (`mistral-ocr-latest`)"*
- **Why it is false:** at `app/studio aeec036a` (the nested repo's **own** ref — grepping it at the `app`
  ref returns 0 for everything, per the ADDENDUM), `tools/pdf2md.py:24` is
  `from mistralai import Mistral`. The literal `mistral-ocr-latest` is at **`tools/pdf2md.py:127`**
  (`model="mistral-ocr-latest",`) — 103 lines away, and the only occurrence in the tree.
  The surrounding claims all check out and are strong: `git -C app/studio grep -i mistral aeec036a` returns
  exactly **22 hits / 3 files** (`requirements.txt`, `tools/pdf2md.py`, `tools/r3.py`) as claimed, and every
  Go-side use is OCR (`internal/cms/studio/markdownManager.go:19`, `studioManager.go:583` — both verified
  exactly).
- **Why I hesitated:** `:24` is a legitimate anchor for *"Mistral is used here"*, and the substantive claim
  (Mistral is OCR-only, never generation) is TRUE and independently verified. I booked it under §5 rule 21's
  class 3 — *an anchor that resolves but names the wrong construct* — because the parenthetical names a
  specific string literal and asserts it is at that line.
- **Confidence:** medium

---

## Minors

- `alignment_testing.md:3` — **`Last updated: 2026-06-06`** is stale by ~2 months: the file documents M218,
  M219 and carries an explicit *"Corrected M257x iter-85"* at `:377`.
- `alignment_testing.md:536-539` — the Layout block lists `internal/dna`, `internal/outcome`,
  `internal/compare`, `internal/report` but omits **`internal/canon`**, which exists
  (`.agentspace/rosetta-extensions/alignment/internal/canon`) and is what makes the doc's own
  "precision-preserving canonical comparison" claim (`:125-126`) true.
- `alignment_testing.md:373` — `unmeasurable()` cited as `(:139-153)`; the function is `:139-154`
  (`:153` is `return ExitUnmeasurable`, `:154` the closing brace). Body range is right.
- `backend.md:114` — *"registers in the Cloud Map service-discovery namespace `internal.anthropos`
  (`app/terraform/main.tf:58`)"*. `:58` is `service_discovery_namespace_id = var.service_discovery_namespace_id`
  — it supports "registers in a service-discovery namespace" but does **not** name `internal.anthropos`
  (that literal is only in `app/knowledge/*.md`).
- `ai_architecture.md:235` — *"the enum is `…/jobsimulation.go:983-990`"*. The `AIModel` const block is
  `:979-991`, members `:980-990`; the cited window omits `anthropic-35-sonnet-aws`, `anthropic-37-sonnet-aws`
  and `anthropic-4-sonnet-aws` (all three of which the doc's own routing table at `:258-260` relies on).
  All three of the doc's cited examples do fall inside the window.
- `ai_architecture.md:276` — `calculateSkillScore` cited as `v3/validator/skills.go:53-64`; the function is
  `:53-62` (`:64` is the next function's signature).
- `ai_architecture.md:130-131` — *"`gpt-5.2` … only as a pricing entry in `studio/services/ai.py:356,508`"*.
  Both pricing anchors are exact, and the load-bearing half (*appears in no studio config*) is TRUE
  (enumerated: 3 `.ini` files, 0 hits). But `gpt-5.2` also appears at `changelog.md:106`,
  `knowledge/concepts/tests-system.md:84` and `knowledge/operations/running-tests.md:32` — so *"only"* is
  three sites too strong.
- `ai_architecture.md:213-214` — *"all it does is swap the dispatched **endpoint** to `openai-hosted`"*.
  `calls/livekit.go:140-144` also resets `agentName` to `"anthropos-agent"` (`:142`), undoing the
  `anthropos-agent-us` selection made at `:126`. The residency conclusion is unaffected.

---

## Positively cleared (audited zeros)

Labelled per the briefing. **ENUMERATED** = I derived the predicate's set from source independently and
compared cardinalities. **RE-DERIVED** = I only re-computed arithmetic the document displays.

**ENUMERATED**

1. `skillpath.md:47` — repos.yml counts `9 @ 2adcf71 / 6 @ 0dab54d / 4 since 838d907`. Listed the `- name:`
   entries at all five refs (`2adcf71`, `d11a403`, `0dab54d`, `838d907`, `0c91421`): 9 / 6 / 6 / 4 / 4. Exact.
2. `skillpath.md:30-31` + `backend.md:68` — **0** occurrences of `SkillPathSessionService` in Go source
   across the clone set. Checked all 8 Go clones (`app`, `sentinel`, `storage`, `messenger`, `cms`,
   `jobsimulation`, `roadrunner`, `graphql-wundergraph`) **and** both nested `studio` repos at `aeec036a`.
   Positive control `SkillerService` returned 4 files. Zero confirmed in every tree.
3. `backend.md:66` — the RPC mux registers five handlers unconditionally at `main.go:1185-1228` @ `b948604f`.
   Opened the block: `:1187` Users, `:1188` Organizations, `:1196` Skiller, `:1204` JobSimulation, `:1228`
   LabSession, plus `if cmsRPCServer != nil` at `:1212-1214`. **Every anchor exact.**
4. `backend.md:264` — the Redis-stream publisher/subscriber enumeration. Grepped every `pubsub.NewPublisher`
   and `AddSubscriber` in `app @ b948604f`: publishers at `main.go:287` (`serviceName`), `:637`
   (`SKILLPATH_STREAM`), `:1039` (`CMS_STREAM`), `jobsimwiring/wiring.go:127` (`AI_USAGE_STREAM`), `:180`
   (`JOBSIMULATION_STREAM`); `SKILLER_STREAM` occurs **once** in Go, `main.go:1276`, an `AddSubscriber`. No
   `NewPublisher` names `SKILLER_STREAM` at `b948604f` **or** at `2035f9a` (re-checked; its 6 publishers are
   `wiring.go:132,185`, `main.go:325,746,1149` + one in a comment). Six `AddSubscriber` calls, six distinct
   streams — the "exactly one subscriber per stream" claim at `:266` holds.
5. `backend.md:22-26` + `:122-133` — the supergraph 3 → 1. `graphql-wundergraph/subgraphs.conf` at
   `915da06^` = `BACKEND`/`JOBSIMULATION`/`CMS` (three); at `915da06` = `BACKEND` alone; at HEAD `60c229f3`,
   `schemas/` contains exactly `backend.graphqls`. `749dc86` (2026-06-24) deletes `schemas/skiller.graphqls`;
   `7c17e63` (2026-07-21) deletes `schemas/skillpath.graphqls`; `915da06` (2026-07-29) deletes **both**
   `cms.graphqls` and `jobsimulation.graphqls`. All three dates and all three file deletions exact.
6. `backend.md:231` — **13** `ai_readiness_*` Ent tables. Listed `internal/data/ent/schema/ai_readiness_*.go`
   @ `b948604f`: 13 files. Independently derived the distinct `"ai_readiness…"` table names from
   `terraform/migrations/` (excluding index/FK/constraint names): also 13, and the four the doc names as the
   "omissions a 9 misses" (`recommendations`, `email_overrides`, `notification_logs`, `notification_optouts`)
   are all present, while `live_snapshots` and `text_translations` are indeed among the original set.
7. `backend.md:231` — *"~10 `/api/workforce/ai-readiness*` REST handlers"*. Enumerated
   `^  /workforce/ai-readiness` paths in `internal/web/backend/api/api.yaml` @ `b948604f`: **9**, plus the
   separately-registered `/api/workforce/ai-readiness/cycles/{id}/email-preview`
   (`internal/web/backend/emailpreview/handler.go:1`) = **10**.
8. `backend.md:230` — *"not `internal/workforce/`, which contains no `readi*` file at HEAD"*. Listed all 26
   files in `internal/workforce/` @ `b948604f`: none matches `readi*`. `internal/aireadiness/` exists with
   `manager.go`, `cycles.go`, `diagnosis.go`, `compare.go`, `csv.go` all present.
9. `alignment_testing.md:259-265` — the five-DNA score table's denominators. Parsed all five DNA files in the
   authoring copy and counted capabilities + variants: `clerk-2.6.0` **14 caps / 27 genes**, `clerk-js-5`
   **6 / 9**, `clerk-multi-1` **5 / 9**, `clerk-deploy-1` **3 / 7**, `clerk-express-1` **5 / 13**. All five
   pairs match the table exactly.
10. `alignment_testing.md:208-211` — *"Only `clerk-2.6.0` … declares a `consumed_surface` today."* Parsed all
    five: `clerk-2.6.0` declares 15 entries; the other four declare none. Exact.
11. `alignment_testing.md:219-224` + `:276-281` — the two M218/M219 genes exist and are shaped as described:
    `GetUser` is **critical** with variants `hero-a` **and** `hero-b` (the two-sided A≠B fix), and
    `MembershipOrgIdentity/real-org-eid` is present and **`standard`** (as the doc insists). The `covered_by`
    example quoted at `:201` (`clerk-express-1:ClerkClientBAPI/get-organization`) is verbatim in the file.
12. `alignment_testing.md:475-486` — **six** snapshot operators. `stack-seeding/dna/snapshot.go:62` is
    `var validSnapshotOperators = map[string]bool{` with exactly six members ending in
    `OpSnapshotCrossSurfaceClosure`. `data-dna.json:410` (taxonomy) lists **five** — all but
    `cross-surface-closure`; `:439` (content) lists **five** — no `embedding-dim`, with
    `cross-surface-closure`. Both anchors land on the `"operators"` key itself.
13. `alignment_testing.md:471-474` + `:505` — *"nothing left waived → 100% coverage"*. Enumerated all 10
    surfaces in `data-dna.json`: 4 `seeded-m7a`, 4 `seeded-m7c`, `taxonomy` `snapshot-seeded-m9b`, `content`
    `snapshot-seeded-m10`. **Zero waived.** The "4 seeded in M7a" figure is also exact.
14. `alignment_testing.md:28-31`, `:328-331`, `:513-518`, `:532-540` — the layout split. `alignment/` contains
    exactly `README.md cmd examples go.mod internal` (**no `scripts/`**, as claimed); module line is
    `module anthropos.dev/alignment`. `clerkenstein/alignment/cmd/` contains exactly the five named runners;
    `dna/` exactly the five named DNAs; `scripts/` holds `gate.sh`, `drift-check.sh` (+ `drift-test.sh`).
15. `skiller.md:43-47` / `ai_architecture.md:141` / `backend.md:9,63` — the taxonomy figures. Read the
    read-only capture manifest at `.agentspace/snapshots/taxonomy/5afc0bcc…/manifest.json` directly:
    `captured_at 2026-06-29T14:43:00Z`, `source: primary-read`, `public_only: true`, `predicate: org-null`;
    `skills` **42,790** (`organization_id IS NULL`), `job_roles` **22,470**, `skill_embeddings` **42,790**
    (filter `skill_id IN (… WHERE organization_id IS NULL)`), `job_role_embeddings` **18,919**,
    `categories` 23, `specializations` 1,447. Every figure the three files quote is exact, including
    `ai_architecture.md:141`'s pairing of 42,790 skill-embeddings with 18,919 job-role-embeddings, and
    `:146-149`'s claim that 18,919 is the *embedding* row count transcribed onto the role count.
16. `skiller.md:20-24` — *"Locally that variable is now set by nothing: the last block to carry it was
    `messenger`'s, and platform `838d907` (merged `0c91421`, 2026-08-05) deleted that service."*
    `SKILLER_RPC_ADDR` at `0dab54d` is at `docker-compose.yml:183`, inside the block whose
    `SERVICE_NAME=messenger` (`:181`) and `profiles: [messenger]` (`:194`). `838d907` is dated
    **Wed Aug 5 2026** and its own message enumerates the removal. At `0c91421`, `git grep -n "_ADDR"` over
    `docker-compose.yml` + `common.yml` returns only `AUTHORIZATION_ADDRESS`, `DIRECTUS_PUBLIC_BASE_ADDR`
    and `REDIS_ADDR` — **zero `*_RPC_ADDR`**, confirming `skillpath.md:59` and `backend.md:29-32,241` too.
17. `customerio-sync.md` (whole file) — every `app` anchor resolved **exactly** at `origin/main 2035f9a`:
    `internal/customeriosync/doc.go:4` is the *"the last of the Go services to be folded into app"* line
    (and carries `v0.19.3` and the `7,392 of 7,397 rows` figure verbatim); `env_guards.go:62` is
    `envCustomerIOSyncEnabled = "CUSTOMERIO_SYNC_ENABLED"`; `main.go:286` is the
    `mustSubsystemSwitch(envCustomerIOSyncEnabled)` call *before* any connection; `:295` the
    `BREVO_KEY == ""` fatal guard; `:393-396` the `copilotDB` construction; `:395`
    `customeriosync.New(logger, copilotDB, os.Getenv("BREVO_KEY"))`. `docker-compose.yml:84-92` @ `0c91421`
    is exactly the *"deliberately have NO variables here"* comment block. The historical compose snippet
    matches `0dab54d:docker-compose.yml:136-154`, and *"it was the **only** service built that way"* is
    ENUMERATED: of the seven `context:` lines at `0dab54d`, only customerio-sync's is a git URL.
18. `backend.md:36-56` — the M810 split. `cms/terraform/main.tf:39` is `service_desired_count = 0` at **both**
    `ca50c817` and the cited `f38c0c4`; `6efa1d5` (2026-08-04) deletes exactly
    `.github/workflows/build-production.yml` (18 lines) with the quoted subject, and both quoted body
    sentences are verbatim. `6092c6d2`'s `terraform/main.tf:15-22` is precisely the comment recording the
    `module "jobsimulation"` deletion (ECS service, task definition, ECR repository, IAM roles, security
    group, Cloud Map entry, log group, alarms) and `:24-40` is precisely the *"What this module still owns"*
    block naming `livekit.tf`, `chime.tf`, `ssm.tf` and the atlas tracker. **Both anchor ranges are exact at
    the ref the claim names.**
19. `skillpath.md:74-79` — `InsightsSkillPathByMemberships` is at `internal/organization/intelligence.go:1144`
    and its `SkillPathSession.Query()` with `skill_path_id` + `StatusIn(Active, Completed)` + the tenant
    predicate spans **exactly `:1159-1170`**. Counted line-by-line.
20. `skillpath.md:81-92` — the mirror retraction. `terraform/migrations/20260729133514.sql:62` is
    `DROP TABLE "local_jobsimulation_sessions";` and `:63` is `DROP TABLE "local_skill_path_sessions";`
    (exact). No `local_*` file exists among the 139 files in `internal/data/ent/schema/`. The three
    migrations named as post-dating it **all exist at the cited ref `9d00a313`** (only two of them at the
    checkout `b948604f` — the pin is doing real work here and is correct).
21. `ai_architecture.md:19-43` — the routing mechanism. `getClient` is `internal/jobsimulation/ai/ai.go:259-289`
    (exact); the four vendor consts are `:30-33` (exact); the Azure arm reads `"flag_use_azure_us"` at `:267`
    and a **failed** lookup returns the EU client (`:270-273`); `AnthropicAws` and `Anthropic` both return
    `a.anthropicClient`, built with `config.WithRegion("eu-west-1")` (`:87`); the 429 path is
    `throttled → vendor = Openai` at `:150-155`/`:166-167` and `:297-299`/`:325-326` — **nothing else moves a
    request off its vendor**. The `@ 5ba17044` anchors at `:99` also resolve exactly (`ai.go:267`, `:344`,
    `skillerai/ai.go:347` are all the `"flag_use_azure_us"` literal), and `internal/ai/ai.go` **does not
    exist** in `app` while it **does** exist in the frozen `jobsimulation` husk — exactly as stated.
22. `ai_architecture.md:53-66` + `:246-260` — the three defaults and the two doors.
    `cms/directus/collections/jobsimulation.go:905` is `AIVendor *AIVendor` on the **DTO**; `:1297` is
    `aiModel := simulation.Gpt5Point1`; `:1302-1305` replaces nil vendor with `simulation.Openai` **before**
    the sequence is built at `:1307`; `:971` is `Azureglobal AIVendor = "azureglobal"`. The Directus vendor
    enum has **five** members (`:970-974`) and `simulator/ai/ai.go`'s switch has **four** cases — `:58`
    `Openai`, `:69` `Azure`, `:86` `AnthropicAws`, `:102` `Anthropic` — with `default:` at `:114-115` setting
    `internalAi.Openai`. Model defaults verified at `:65-66`, `:82-83`, `:126-127` (all `GPT4Dot1`),
    `:98-99` (Claude 3.7 Bedrock), `:110-111` (Claude 3.5), and `:20-26`
    (`GetValidationAIVendorAndModel` = Azure / GPT4Dot1 / GPT4Dot1Mini). **Every one of these fifteen anchors
    is exact.**
23. `ai_architecture.md:262-283` — the evaluation system. `checkerEngines` is declared at
    `v3/validator/validator.go:43`, populated `:60-61`, and passed at `:595` — but `validateLLM` builds its own
    checker (`basevalidator/criterion.go:428` `NewLLMBulkChecker(c.logger)`), so the map is never *consulted*
    for dispatch; the dispatch is the hardcoded `switch critCheck.Engine` at `criterion.go:126-127`.
    `check.EngineTextDiff` is the `:142` arm; `:168` runs `validateCodeDiff` concurrently; `:450-475` sets
    `success` from `cdiff.Diff == ""`. `bulkChecks.go:118` is `ai.WithTemperature(0.0)` and the template
    `checkValidationBulk.tmpl` reads back `check_id`/`feedback`/`success`. `skills.go:40-51` is
    `calculateCompetencyLevelScore` with the exact 20 / 100 / `max(0, score*2-100)` formula **and** the
    `// TODO fix this formula` comment at `:49`; `:75` is `passed/total*100`. `skill.go:617-623` is
    `convertLevelTo100`; `next-web-app` `packages/ui/src/Competency/CompetencyReadLevel.tsx:18` is the
    `getSkillScoreForSimulation` call.
24. `ai_architecture.md:115-135` — the studio slot table. `studio/configs/production_config.ini:26-36` is
    exactly the ten `*_MODEL` lines, and stable **is** byte-identical to experimental;
    `development_config.ini:26-36` is identical to both; `config_template.ini:39`/`:40` are the two `gpt-4o`
    `*_STABLE_MODEL` slots. *"`gpt-4o` appears in no `*_MODEL` slot of any **shipping** studio config"* is
    TRUE — enumerated all three `.ini` files; the two shipping hits are the `# TARGET MODEL:` **comment** at
    `:25`, not a slot. `configs/` holds exactly three files.
25. `alignment_testing.md:158-179` + `:301-313` — the toy score. Enumerated the toy DNA: `Add` critical (3
    variants, w3) + `Greet` standard (3 variants, w2) = 6 genes, Σw = 15. One divergence
    (`Greet/padded-name`, w2) ⇒ 13/15 = **86.7% overall**, critical 3/3 = **100%**, **5/6 genes** — matching
    the transcript including the `(exact, w2)` annotation. `compare.go:101-103` confirms `Overall` is weighted
    and `Critical` is the **unweighted** `pct(critAligned, critTotal)`, as `:162-164` claims.
26. `alignment_testing.md:167-174` — the zero-critical guard is genuinely two-layer:
    `dna/dna.go:279-281` rejects a DNA with capabilities but zero critical genes at load time, and
    `compare/compare.go:56-60` `GateMet` returns false when `minCritical > 0 && r.CriticalGenes == 0`.
    `Report.CriticalGenes` is a real JSON field (`compare.go:43`). `dna.go:21` is the exact id regex
    `^[A-Za-z0-9][A-Za-z0-9_-]*$`; `dna.go:24` `maxWeight = 1_000_000` with the `1..%d` message at `:270`.
27. `alignment_testing.md:199-200` + `:335-351` — `gate.sh:69` is verbatim
    `"$alignctl" dna coverage --dna "$base/$dna" --if-declared`, and `:58-68` is exactly the rationale comment
    (so the doc's own correction of the old `:61` anchor is right). `gate.sh:33` defaults `RUNNER_PKG` to
    `./cmd/clerkrun`, `:35` `DNA` to `dna/clerk-2.6.0.json`, `:30` `ALIGN_DIR` to `$base/../../alignment`
    (same at `drift-check.sh:16`). `clerkenstein/.github/workflows/alignment.yml` **exists and is
    git-tracked**, its `:10-11` is the quoted *"currently inert"* sentence verbatim, and
    `.github/workflows` at the monorepo **root does not exist** — the doc's "inert ≠ absent" framing is
    exactly right.
28. `alignment_testing.md:244-246` + `:373-378` — `alignment/cmd/alignctl/run.go:133-136` is the const block
    `ExitRegressed = 2` / `ExitUnmeasurable = 3`; `unmeasurable()` prints the boxed
    *"UNMEASURABLE — the runner could not execute. THIS IS NOT A PASSING SCORE."* banner and returns
    `ExitUnmeasurable`. `dna.go:20-27` carries the four subcommand cases including `coverage`.
    `run.go:22` is `--source golden|live`, default `golden`.
29. `backend.md:161-162` — ports and profile. `platform/docker-compose.yml` @ `0c91421` sets `PORT=8082`
    (`:65`), `RPC_PORT=8083` (`:69`), `META_PORT=8084` (`:64`); publishes `8081/8082/8083` (`:41-43`) with
    nothing bound to 8081 locally; `profiles: [core, backend, all]` is at **`:110`** (inside the `backend`
    block, which runs `:28`–`:110`) and was at **`:100`** at `0dab54d` — both verified.
    `platform/Makefile:10` is `PROFILE ?= core`.
30. `backend.md:299` — migrations plumbing. `app/atlas.hcl` has `src = "ent://internal/data/ent/schema"` and
    `dir = "file://terraform/migrations"`; `git ls-tree <ref> migrations/` is **empty at both** `b948604f`
    and `2035f9a`; `6a46e8445` (2026-06-18, *"chore(migrations): remove obsolete atlas.sum file"*) deletes
    exactly one file, `migrations/atlas.sum`.
31. `backend.md:254` — the storage fold, at the ref it names (`2035f9a`): `main.go:524`/`:525` are
    `internalstorage.NewManager(storageBucket)` / `NewPublicManager(storagePublicBucket)`; `main.go:504` is
    the *"STORAGE_RPC_ADDR is gone"* comment. `STORAGE_RPC_ADDR` has **3** occurrences in Go at that ref —
    `main.go:504`, `jobsimwiring/wiring.go:101`, `storagens/callsites_test.go:189` — **all comments, 0 read
    sites**, exactly as claimed.
32. `skillpath.md:14,59` — the in-process cms read. `internal/skillpath/session.go:205-207` is the
    `// cms-in-app deseam: cms is in-process` comment + `u.cms.GetSkillPathDomain(...)`, and
    `internal/skillpaths/skillpaths.go:88-95` is its twin. **Both ranges exact.**
    `SkillPathMixin` (`skillpath_mixins.go`) carries `duration`, `progress` (Min 0/Max 100), `status`,
    `version`, `started_at`/`ended_at`/`archived_at` — matching `:62-64`; the GraphQL enum
    `SkillPathSessionStatus` is `pending|active|completed|archived`; all eight named operations exist at
    `skillpath_sessions.graphqls:71-82`.
33. `skillpath.md:97-104` — the unbuilt drill-down. `next-web-app@bb3313bc`
    `apps/web/src/components/containers/InsightsBySkillPathStudentSimulationsContainer.tsx` has
    `userData` = `return null as unknown as MembershipEnriched` (hardcoded), the results table and totals
    block **commented out**, and renders `t('enterprise.insights.comingSoon')` — which resolves to
    **"Coming soon"** in `configs/i18n/messages/en/enterprise.json:1862`. Verified end to end.
34. `skiller.md:12-27` — every named path exists at `b948604f`: `internal/data/ent/schema/{skill,jobrole,
    skill_embeddings,job_role_embeddings,category,specialization}.go`, `internal/rpc/skillerrpc/`,
    `internal/web/backend/graphql/graph/schemas/skiller_taxonomy.graphqls`. `categoryTree`/`fullCategoryTree`
    are **not implemented** — the only Go/GraphQL trace is `skiller_taxonomy.graphqls:7`'s own comment saying
    they *"stay unported — no consumers."* `skill_translation.go` / `job_role_translation.go` exist and
    `ContentLanguage` has **exactly 8** members (`assignments.graphqls:44-53`). The vector shape is
    `extensions.vector(1536)` + `entsql.IndexType("ivfflat")` (`skiller_mixins.go:184`, `:193`).
35. `backend.md:66,71,241` — the RPC/HTTP surface details: the RPC server is built with
    `rpc.WithWriteTimeout(60*time.Second)` (`main.go:1243`); the Directus webhook handler is constructed
    from `os.Getenv("DIRECTUS_WEBHOOK_SECRET")` under a `fail-closed` comment (`main.go:1088-1089`); the
    stale *"until the M809 re-point"* comment the doc flags is exactly `main.go:1205-1211`;
    `AUTHORIZATION_ADDRESS=http://sentinel:8087` is `docker-compose.yml:48`. `main.go:743-746` is exactly the
    `if labsAPIURL := os.Getenv("LABS_API_URL"); labsAPIURL != ""` conditional, so `:80`/`:236`'s
    *"NOT unconditionally nil"* correction is right.
36. `backend.md:175` — `internal/academy/academy.go:6-9` states verbatim that the catalog tables are the
    askengine feed and that *"the legacy internal/aiacademy sync and its aiacademy_courses read-model were
    removed"*. Range exact. Go version `1.26.4` ⇒ `:159`'s "Go 1.26" is right.

**RE-DERIVED (weaker — recorded as such)**

- `backend.md:98` — `42,790 + 794 = 43,584`. Arithmetic only; the prod-side 2026-07-08 counts are not
  reachable from any clone (see below).
- `alignment_testing.md:261` — `27/27` and the 97.2% → 100% narrative. I enumerated the DNA's denominators
  (above) but did **not** run `alignctl`, so the *scores* are re-derived from the document, not measured.

---

## What I could not settle

1. **Prod-only measurements.** `backend.md:96-103`'s 2026-07-08 prod counts (`public.job_roles` 22,490,
   `public.skill_embeddings` 43,584, 794 org-private skills) need a read against production. The
   2026-06-29 snapshot manifest corroborates the *public* half exactly (see cleared #15) and the doc's own
   "taxonomy grows over time" framing makes the 22,470→22,490 drift coherent, but I cannot confirm the
   43,584 figures. Settled by a `/db-query` read.
2. **`app` PR numbers and skiller-repo migrations.** `skiller.md:30` (app PR #989),
   `backend.md:236` (PR #896), `ai_architecture.md:154` (skiller migrations `20260417103036`,
   `20260417120309`). The `skiller` repo is not in the clone set and PR numbers are not derivable from a
   clone; neither migration name exists under `app/terraform/migrations/`. Settled by `gh` or a skiller clone.
3. **`ai_architecture.md:278-283`'s "there is no 60/65/75/85/95 threshold ladder."** An absence claim across
   four repos. My first attempt matched minified vendor JS (868 KB of noise) — reported here rather than
   silently dropped, per §5 rule 1. The *positive* half (the real conversion is
   `calculateCompetencyLevelScore`) is fully verified, so the substantive claim stands; the exhaustive
   absence does not. Settled by a source-only, vendor-excluded sweep.
4. **The `ai` shared-library model inventory** (`ai_architecture.md:84-91`). `anthropos-work/ai` is a private
   Go module pulled at Docker build, not cloned, so the GPT-5.4/5.2/5.1/O3/O4-mini and Claude 4.5/4/3.7/3.5
   const lists are unverifiable here. `CreateSpeech` being Azure-only **is** verified
   (`jobsimulation/ai/ai.go:243-257`, with the source's own *"at the moment we only support azure voice"*).
5. **`backend.md:44-46`'s "NOT MEASURABLE from any clone set we have."** Not booked, but worth routing: at
   `app` `origin/main`, commit `febbd936d` (2026-08-05, *"messenger's and storage's ECR repos were destroyed,
   not preserved"*) records a live `aws ecr describe-repositories --region eu-west-1` returning **six**
   repositories — `production-backend`, `production-db-backup`, `production-directus`, `production-metabase`,
   `production-sentinel`, `production-studio-desk` — with **neither `production-cms` nor
   `production-jobsimulation`** among them, and names Infra PR #3253 as the authority. That is evidence about
   the infrastructure side sitting **inside a clone we have**. I did not book it because (a) the sentence
   names no `app` ref and so grades at the checkout `b948604f`, where that commit does not exist, and (b) the
   ECR half is not the whole of "the deletion has been applied". But the hedge is likely now over-cautious,
   and the same commit is independent support for `:50-51`'s flat "`jobsimulation`'s ECS service is already
   destroyed" — which is why I dropped a draft blocker against that asymmetry.
6. **Ref-selection residue.** `backend.md:14,16` (`internal/storage/`, `internal/customeriosync/`) and the
   whole of `customerio-sync.md` describe packages that **do not exist at the pinned `app b948604f`** and do
   exist at `origin/main 2035f9a`. Under a literal reading of the briefing's *"if the passage names no ref,
   grade it against the checkout"* these are ~15 failed anchors; under the ADDENDUM's *"if a passage's line
   anchor resolves at neither, that is a finding"* they are clean, since every one resolves at `2035f9a`.
   I took the second reading and booked none. Flagging it because the choice is worth **-15/+0** on this
   seat's count, and the next reading should not silently take the other one.
