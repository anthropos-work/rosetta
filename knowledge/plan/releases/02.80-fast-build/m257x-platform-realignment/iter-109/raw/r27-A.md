# Seat A — reading #27

**Ground truth re-derived at this reading's open** (all shas matched the ADDENDUM's superseded table
exactly; no clone moved during the read):

| repo | path | sha read |
|---|---|---|
| app | `stack-demo/app` | `ad9f3c49` (checkout, `== origin/main`) — pinned claims also read at `b948604f`, `5ba17044` |
| app/studio (studio-room, nested repo) | `stack-demo/app/studio` | `aeec036a` (own checkout) |
| platform | `stack-demo/platform` | `0c91421d` — also read `0dab54d`, `838d907`, `d11a403`, `1e8e754`, `b56d731`, `360efd4`, `2c85211`, `a4db680`, `63d285c`, `a2a3ee6` |
| next-web-app | `stack-demo/next-web-app` | `8297c684` |
| sentinel | `stack-demo/sentinel` | `f2c46190` |
| storage / messenger / cms / graphql-wundergraph / roadrunner / jobsimulation | as listed | `4ce8ece5` / `fa47850d` / `ca50c817` (+ `f38c0c4a`) / `60c229f3` / `87d8d443` / `462343b0` (+ `82cb66ec`) |
| studio-desk | `stack-demo/studio-desk` | `41ee3575` |
| ant-academy | `stack-demo/ant-academy` | `22df69dd` |
| **rosetta-extensions — pinned per-stack consumption clone (the settling tree for tooling-on-a-stack claims)** | `stack-demo/rosetta-extensions` | **`09d06070`** (also read the prior pin `ab81527a`) |
| rosetta-extensions — authoring copy (settles fence *configuration* claims) | `.agentspace/rosetta-extensions` | `680e8529` |

Search-instrument note (ADDENDUM's three-mechanism rule): every tree-wide zero below was taken with
`git grep` **at each tree's own ref**, enumerated across all 15 git trees found by
`find stack-demo -name .git -maxdepth 3`, including the two nested `studio` checkouts at `aeec036a`.
A positive control was run in the same pass for every zero.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/architecture/external_services.md` | 883 | yes |
| `corpus/architecture/ai_architecture.md` | 310 | yes |
| `corpus/services/coursebuilder.md` | 161 | yes |
| `corpus/services/clerk-integration.md` | 128 | yes |
| `corpus/architecture/README.md` | 38 | yes |
| **total** | **1520** | |

---

## Blockers

### B1 — `ai_architecture.md:34` cites `markdownManager.go:19`, a doc-comment line — the exact anchor its twin retracted

- **Anchor:** `corpus/architecture/ai_architecture.md:34` —
  > "*Every* use of it in `app` is **OCR**, never generation: Go-side
  > in the cms domain (`internal/cms/studio/markdownManager.go:19`, `studioManager.go:583`), …"

- **Why it is false:** At `app` `ad9f3c49`, `internal/cms/studio/markdownManager.go:19` is
  `// It used to take aiKey and then IGNORE it, reading os.Getenv("MISTRAL_API_KEY")` — a line of the
  doc comment on `NewMarkdownManager`. The Mistral OCR client is constructed at **`:30`**
  (`return &MarkdownManager{ocr: mistralocr.New(aiKey)}, nil`), inside `func NewMarkdownManager` at
  `:29`; the field `ocr *mistralocr.Client` is at `:14` and the import at `:10`. The file is
  byte-identical at `b948604`, so no ref choice rescues `:19`.

  This is also a direct **corpus self-contradiction**, and an unusually explicit one: its twin
  `corpus/architecture/external_services.md:565` names `:30` **and specifically retracts `:19`** —
  "*the `:19` this row used to cite is a **doc-comment** line, not code*". `external_services.md:599`
  cites `:30` a second time. So one of the two files was repaired and the other left standing — §5
  rule 19's cross-file drift class.

- **Confidence:** high

### B2 — `external_services.md:554` still calls `ai` "the **shared** library"; it was folded into `app` and is required by no `go.mod` a stack builds

- **Anchor:** `corpus/architecture/external_services.md:554` —
  > "All Go services access AI through the shared `ai` library, which provides **unified provider
  > access** behind one `ai.AI` interface (OpenAI, Azure, Anthropic, Bedrock, Mistral)."

- **Why it is false:** `1e457fa70` (2026-08-04, *"refactor(ai): fold the ai library into app as
  internal/ai"*) moved the library in-tree. Measured at the refs above:
  - `app/go.mod` @ `ad9f3c49` requires `colony v0.35.2`, `proto v1.210.0`, `taxonomy v1.2.0` and
    **no `github.com/anthropos-work/ai`**; `sentinel/go.mod` @ `f2c46190` likewise.
  - `git ls-tree ad9f3c49 internal/ai/` returns 17 entries (`ai.go`, `openai/`, `anthropic/`, …) and
    **67 `.go` files** import `github.com/anthropos-work/app/internal/ai`.
  - The only trees still requiring the module are the frozen `cms` (`go.mod:9`) and `jobsimulation`
    (`go.mod:11`) husks — neither has a compose service or a `repos.yml` entry.
  - The only two Go repos a stack builds are `app` and `sentinel` (`repos.yml` @ `0c91421`), and
    `sentinel` uses no AI at all, so "**All** Go services access AI through …" is doubly wrong.

  Corpus twins that already say the opposite, all three of them: `ai_architecture.md:95-99` ("**it is
  no longer a shared private module for any service a stack builds**"), `shared_libraries.md:8`
  ("`ai` was folded into `app` as `app/internal/ai` at `1e457fa70` … which dropped its module
  requirement"), and my own `architecture/README.md:21` ("only **three** are imported as private
  modules … **`ai` is no longer one of them**"). `external_services.md` is the surviving pre-repair
  site, and it even links the reader to `shared_libraries.md#ai`, the file that refutes it.

- **Confidence:** medium-high. I hesitated because "the shared `ai` library" can be read as a
  colloquial name rather than a claim about module boundaries — but the word is the load-bearing one
  here (it is what routes the reader at the `→ Shared Libraries` link), and three sibling documents
  have already been corrected on exactly it.

### B3 — `ai_architecture.md:87` Model Families omits Claude Sonnet 4.6, the Anthropic model actually in production

- **Anchor:** `corpus/architecture/ai_architecture.md:87` —
  > "| **Anthropic (Bedrock EU + Direct US)** | Claude 4.5 Sonnet, Claude 4 Sonnet, Claude 3.7 Sonnet,
  > Claude 3.5 Sonnet | Bedrock `eu-west-1` …"

- **Why it is false:** I enumerated the predicate independently rather than re-deriving the list.
  `git grep -n 'ai\.Model *= *"' ad9f3c49 -- 'internal/ai/**'` returns **six** Anthropic consts in
  `internal/ai/anthropic/completion.go`:

  | line | const | value |
  |---|---|---|
  | `:21` | `Anthropic35SonnetAWS20241022` | `eu.anthropic.claude-3-5-sonnet-20240620-v1:0` |
  | `:22` | `Anthropic35Sonnet20241022` | `claude-3-5-sonnet-20241022` |
  | `:23` | `Anthropic37SonnetAWS20250219` | `eu.anthropic.claude-3-7-sonnet-20250219-v1:0` |
  | `:24` | `Anthropic4SonnetAWS20250514` | `eu.anthropic.claude-sonnet-4-20250514-v1:0` |
  | `:25` | `Anthropic45SonnetAWS20251126` | `eu.anthropic.claude-sonnet-4-5-20250929-v1:0` |
  | **`:29`** | **`Anthropic46SonnetAWS20251126`** | **`eu.anthropic.claude-sonnet-4-6`** |

  Five distinct families; the table lists four. The missing one is **Sonnet 4.6**, and it is not an
  obscure member — it is the model three separate live `app` domains pin by name:
  `internal/jobsimulation/agent/report_agent.go:31` (`defaultAgentModel`),
  `internal/askengine/bedrock.go:25` (`DefaultModelID`) and
  `internal/coursebuilder/bedrock.go:29` (`DefaultGraderModelID`). The corpus names it in both
  `external_services.md:564` and `coursebuilder.md:51`, so this is also an intra-corpus
  inconsistency, in a document whose own line 3 says it "describes the AI model **inventory**".
  (Course Builder's author model `eu.anthropic.claude-opus-4-8` is likewise absent, but it is a bare
  string rather than an `ai.Model` const, so I do not rest the finding on it.)

- **Confidence:** medium. I hesitated because this is formally an "omitted list member" — but the
  omitted member is the platform's *current* production Anthropic model while the four listed are its
  predecessors, so a reader taking the inventory at face value gets the live state exactly backwards.

### B4 — `ai_architecture.md:89` names two speech models — "TTS v2 HD, TTS v2" — that exist in no tree

- **Anchor:** `corpus/architecture/ai_architecture.md:89` —
  > "| **Speech** | GPT-4o Mini TTS, TTS v2 HD, TTS v2 | Azure voice client (`CreateSpeech` is
  > Azure-only) |"

- **Why it is false:** The only speech model in the platform is `gpt-4o-mini-tts`:
  `app/internal/ai/speech.go:10` `GPT4oMiniTTSS SpeechModel = "gpt-4o-mini-tts"` and `:11`
  `DefaultModel SpeechModel = GPT4oMiniTTSS` — the whole `SpeechModel` const block. Nothing named
  "TTS v2", "TTS v2 HD", `tts-1`, `tts-1-hd`, `ttsv2` or `tts-hd` exists anywhere.

  Instruments used, per the ADDENDUM: `git grep -in 'tts'` at **every one of the 15 git trees** in
  `stack-demo` at each tree's own ref (`app` 164 lines, `ant-academy` 1012, `next-web-app` 32,
  `studio-desk` 24, `jobsimulation` 23, `rosetta-extensions` 3, `platform` 2, both nested
  `studio` checkouts @ `aeec036a` 1 each, `roadrunner` 1, `storage` 1, `cms`/`messenger`/`sentinel`/
  `graphql-wundergraph` 0) — a live, non-zero control in eleven trees. Extracting the distinct `tts`
  substrings from `app` yields only `gpt-4o-mini-tts`, `gpt-4o-transcribe` (`internal/ai/openai/
  transcriptions.go:17`) and prose. Independent corroboration from the deployment side:
  `app/terraform/ssm.tf:193`, `variables.tf:483,487,514` and `platform/.env_example:28-31` all
  describe the voice Azure resource as one that "must hold a **gpt-4o-mini-tts** deployment", naming
  no other.

  The rest of the row is correct — `CreateSpeech` really is Azure-only
  (`internal/jobsimulation/ai/ai.go:243-257`: `// at the moment we only support azure voice`, then an
  unconditional `a.azureVoiceClient.CreateSpeech`, ignoring the `vendor` argument).

- **Confidence:** medium. Residual I cannot close: the *standalone* `github.com/anthropos-work/ai`
  module (still required by the frozen `cms`/`jobsimulation` husks) is not in the clone set, so if
  those names ever existed they would have lived there — but the husks' own callers use
  `ai.GPT4oMiniTTSS` (`jobsimulation/internal/simulator/outbound/prompts/replyAudio.go:72`), which
  argues against it.

### B5 — `coursebuilder.md:48`: two of three terraform anchors name the wrong construct at the ref that settles them

- **Anchor:** `corpus/services/coursebuilder.md:48` —
  > "**In production the key is required** — `terraform/variables.tf:635-638` declares it `sensitive`
  > with no default, `ssm.tf:328-334` creates the parameter and `main.tf:555` injects it — so the
  > shipped path is the direct API."

- **Why it is false:** This sentence names **no ref**, so per the briefing it grades at the checkout
  `ad9f3c49`. There:
  - `app/terraform/variables.tf:635-638` is a **comment block about cms-in-app** ("*cms-in-app: env +
    secret inputs for the folded cms (Directus content, HeyGen + Bunny Stream AI video…)*"). The
    `variable "anthropic_api_key"` declaration is at **`:759-762`** (`sensitive = true`, no default).
  - `app/terraform/main.tf:555` is `"name": "DIRECTUS_BASE_ADDR"`. The `ANTHROPIC_API_KEY` task-env
    injection is at **`:757`**.
  - Only `ssm.tf:328-334` still resolves (`resource "aws_ssm_parameter" "anthropic_api_key"`,
    `:328-333`).

  All three were correct at `app` `b948604f` (`variables.tf:635` = `variable "anthropic_api_key" {`,
  `:638` = `sensitive = true`; `main.tf:555` = `"name": "ANTHROPIC_API_KEY"`), which is where they
  were measured. The bullet's *earlier* sentence carries a pin (`logged at main.go:770 @ app b948604
  v1.366.0`) but §5 rule 33 scopes a pin to "the claim's own block — a … wrapped sentence in prose",
  and this is a separate sentence. The substantive claim ("in production the key is required, so the
  shipped path is the direct API") is **true** — only the anchors are stale.

- **Confidence:** medium. I hesitated on two grounds: (a) whether a markdown *bullet* counts as one
  "block", which would let the neighbouring pin cover these anchors; and (b) that this is formally
  line drift. Against (a): every other citation in this same file is individually pinned
  (`main.go:766-779 @ b948604`, `main.go:824-826 @ b948604`), so the un-pinned form reads as current
  by the document's own convention. Against (b): `variables.tf:635-638` does not merely drift, it
  lands on a *different subject entirely* (cms secrets), which is the "resolves but names the wrong
  construct" class §5 rule 21 counts.

---

## Minors

- `ai_architecture.md:60` — cites `external_services.md:619-629` as "item 4 of the four live EU-exit
  routes". Item 4 actually spans `external_services.md:622-632`; the range opens 3 lines inside item 3
  and closes 3 lines short of item 4's end. (Intra-corpus line drift, §5 rule 34.)
- `ai_architecture.md:240` — "the enum is `app/internal/cms/directus/collections/jobsimulation.go:983-990`".
  The `AIModel` const block is `:979-991` and holds **11** members; `:983-990` covers 8 of them, omitting
  `Anthropic35SonnetAws` (`:980`), `Anthropic37SonnetAws` (`:981`) and `Anthropic4SonnetAws` (`:982`).
  Identical at `b948604` and `ad9f3c49`.
- `ai_architecture.md:104` — "this line went on publishing it **68 lines below**" the ⚠️ at `:15-17`.
  The measured distance is 89 lines (104 − 15). Self-referential arithmetic invalidated by a later edit.
- `ai_architecture.md:193` — "none of which appear in `repos.yml`, in any corpus document, or in the
  deployment picture below" is falsified by the paragraph itself (which names all five) and by its own
  next sentence's pointer: `platform-migration-status.md:143-147` enumerates all five agent repos. The
  intent ("this corpus had never named one before now") is clear, so I did not book it as a blocker.
- `ai_architecture.md:219-220` — "all it does is swap the dispatched **endpoint** to `openai-hosted`".
  `app/internal/jobsimulation/calls/livekit.go:142` *also* resets `agentName` to the bare
  `anthropos-agent`, which is a real change on the US path (`anthropos-agent-us` → `anthropos-agent`,
  set at `:126`). The residency point the sentence makes is correct.
- `ai_architecture.md:296` — "(the shared `ai` library itself only returns provider token counts)":
  the same stale adjective as B2, inside the file whose own `:95-99` retracts it. Booked here rather
  than as a second blocker because it is a parenthetical about provenance, not a routing instruction.
- `coursebuilder.md:129-130` — "~55 test files" / "~25 boundary test files". Measured at `ad9f3c49`:
  `internal/coursebuilder/` holds **61** `_test.go` files (of 102 `.go`), `internal/web/backend/
  coursebuilder/` holds **33** (of 48). The `~100 Go files` at `:30` is right (102).
- `external_services.md:455` — the text attributed to `graphql-wundergraph/CLAUDE.md:39` in quotation
  marks ("*Since cms-in-app the platform compose `graphql` service builds from the production
  Dockerfile*") is a paraphrase; the line reads "*Since the compose stack builds from the production
  Dockerfile, a schema change in `app` does not appear at `:5050` …*". The substance — that the
  archived repo's CLAUDE.md asserts the opposite of this section, at `:39`, in `60c229f` dated
  2026-07-30 — is exactly right.
- `clerk-integration.md:99` — studio-desk's `STUDIO_ACCESS_ROLES` is a **4**-member set
  (`['admin', 'org:admin', 'content_creator', 'org:content_creator']`, at `src/index.ts:96` and
  `app/services/userService.ts:16`), described as "`org:admin` **and** `content_creator`".

---

## Positively cleared (audited zeros)

### ENUMERATED — predicate re-derived from source, cardinalities compared

1. **`external_services.md:668-673` — the four AI env-var occurrence counts.** Re-derived at the
   named ref with `git grep -o -w <NAME> 5ba17044 -- '*.go'` (note: `\b` silently returns 0 under
   this git's ERE — caught by a `ZZZNOPE` control that also returned 0, then fixed with `-w`, which
   made the control meaningful). Measured: `OPENAI_KEY` **5**, `ANTHROPIC_API_KEY` **26**,
   `AZURE_OPENAI_KEY` **13**, `AZURE_OPENAI_ENDPOINT_URL` **13**; `OPENAI_ORG_ID` /
   `AZURE_OPENAI_ENDPOINT` / `AZURE_OPENAI_DEPLOYMENT` **0 / 0 / 0**; `OPENAI_API_KEY` **2**, and both
   are the studio subprocess remap (`internal/cms/studio/studioManager.go:1049` and `:1067`). Every
   figure exact.
2. **`external_services.md:373-386` — the subgraph-count table.** Enumerated `- name:` per commit,
   not re-added from the doc: `749dc86^` = 5 (backend/skiller/jobsimulation/cms/skillpath),
   `749dc86` = 4, `7c17e63` = 3, `915da06` = 1, HEAD = 1. Dates 2026-06-24 / 2026-07-21 / 2026-07-29
   exact. `915da06`'s own subject really does say "supergraph 2→1" against a 3-entry tree — the
   caution at `:384` is correct.
3. **`external_services.md:387` — "no subscriptions".** (a) `git grep -i 'sse\|subscription' HEAD --
   '*.yaml'` = 0 across all 7 yaml files, with the stated positive control (`backend` matches exactly
   the 3 supergraph configs). (b) `schemas/backend.graphqls` (5795 lines): `type Mutation` at
   **`:4053`**, `type Query` at **`:4912`**, no `type Subscription`; exactly **6** `Subscription`
   substrings, all Stripe/plan (`activeSubscription :1528`, `type PlanSubscription :1555`,
   `stripeSubscriptionId :4385,:4418`, `stripeSubscriptionItemId :4389`, one doc-comment `:4391`).
   (c) `git log -S 'protocol: "ws"'` (HEAD) = empty; the `sse_post` control = exactly **five**
   commits. `bba862f`: `git merge-base --is-ancestor bba862f HEAD` → rc **1**, `git branch -a
   --contains` names only `remotes/origin/feat/use-web-socket`. `915da06^` still reads
   `protocol: "sse_post"`.
4. **`external_services.md:171-175`, `:306` — the compose service census.** At platform `0c91421`,
   `docker-compose.yml` declares **five** services (`sentinel :5`, `backend :28`, `studio-desk :112`,
   `next-web-app :143`, `gotenberg :170`) plus `common.yml`'s `postgresql`/`redis` = **seven**
   effective; `sentinel` carries no `profiles:` key, so the always-on floor is exactly the stated
   three, and `core` = floor + `backend` (`:110`) + `gotenberg` (`:183`) = **five containers**. At
   `0dab54d` it was **eight/ten** (storage `:102` behind `profiles: [storage-legacy] :134`,
   customerio-sync `:136`, messenger `:156`), exactly as the doc says. `d11a403` deleted the `cms`,
   `jobsimulation` **and** `roadrunner` service blocks and their three `repos.yml` entries (verified
   from the diff, not from the commit subject, which names only roadrunner).
5. **`coursebuilder.md:77-94` — the 16 SSE event names.** Enumerated `renderEvent`'s returns
   independently at `ad9f3c49`: `text :2607`, `score :2609`, `patch_applied :2614`,
   `patch_skipped :2620`, `stage :2637`/`:2639`, `outline :2654`, `progress :2661`,
   `preview_ready :2671`, `draft_kept :2678`, `error :2704`, `translation_ready :2724`,
   `rebuild_required :2732`, `steering_received :2744`, `steering_applied :2754`, `done :2768` = **15
   distinct**, plus `session` written directly at `:1458` after `beginSSE` (`:1454`) = **16**. The
   `default` arm at `:2770` re-emits `text` (not a 17th). `case cb.EventCost:` at `:2709` returns `""`
   at `:2717`; the loop skips `writeSSE` on an empty name at `:1478-1480`. Function span `:2604-2772`
   exact. Every anchor on the money.
6. **`clerk-integration.md:37` — "svix, 12 event types".** `internal/clerk/events/events.go` switches
   on exactly 12: `user.created :166`, `user.updated :172`, `user.deleted :178`,
   `organization.created :185`, `organization.deleted :191`, `organization.updated :197`,
   `organizationInvitation.accepted :204`, `.created :210`, `.revoked :216`,
   `organizationMembership.created :223`, `.deleted :229`, `.updated :235`.
7. **`clerk-integration.md:41` — "8 locales".** `next-web-app/apps/web/src/providers/Clerk.provider.tsx:10-18`
   imports `deDE, enUS, esES, frFR, itIT, jaJP, nlNL, ptPT` = **8**.
8. **`clerk-integration.md:103` — the SDK-drift block.** Every figure exact at each clone's own HEAD:
   `apps/{web,hiring,integration}` `@clerk/nextjs ^6.39.6` (`8297c684`); `ant-academy
   code/package.json:52` `^6.39.2` (`22df69dd`); `apps/maintenance` declares none (I enumerated
   `apps/` = web, hiring, integration, maintenance, mobile); `apps/mobile/package.json:6`
   `@clerk/clerk-expo ~2.6.18` vs `ant-academy/mobile/package.json:18` `~2.19.36` (13 minors);
   `app/go.mod:31 @ 5ba17044` = `clerk-sdk-go/v2 v2.7.0` (line 31 exact).
9. **`clerk-integration.md:51`, `:91` — the `colony/authn` import census.** 129 `.go` files in `app`
   @ `ad9f3c49`; **0** in `sentinel`, `storage`, `messenger`. Control: `anthropos-work/colony` matches
   2 files in `sentinel`, so the pipeline was live. `repos.yml` @ `0c91421` lists exactly two Go
   repos (`app`, `sentinel`).
10. **`architecture/README.md:21` — three private modules, `ai` out, `authn` nowhere.** Enumerated
    `anthropos-work/(ai|colony|proto|taxonomy|authn)` across all seven Go repos' `go.mod`: `app` and
    `sentinel` require colony + proto + taxonomy only; `ai` survives only in `cms/go.mod:9` and
    `jobsimulation/go.mod:11`; `authn` appears in **no** `go.mod` **or `go.sum`** of any of the seven.
11. **`ai_architecture.md:86` — the OpenAI model list.** `internal/ai/openai/config.go:8-26` declares
    exactly **11** models (GPT4.1, GPT4.1-mini, O3, O4-mini, GPT5, GPT5-mini, GPT5-nano, GPT5.1,
    GPT5.2, GPT5.4, GPT5.4-mini); the row lists the same 11. (Its sibling Anthropic row is B3.)
12. **`ai_architecture.md:122-141` + `external_services.md:567,619,640` — the 30 studio slot lines.**
    Enumerated `_AI_.*_MODEL` across `app/studio/configs/*` @ `aeec036a`: **30** lines over 3 files,
    **every one** pinning `azure`. `production_config.ini:26-36` and `development_config.ini:26-36`
    are identical, and the stable/experimental columns match the table row-for-row.
    `gpt-5.2` appears in no config (only `services/ai.py:356,508` pricing); `gpt-4o` appears only in
    the non-shipping `config_template.ini:39` and `:40`.
13. **`external_services.md:687-700` + `ai_architecture.md:146,150-155` — the taxonomy figures.**
    Read the manifest itself: `public_only: true`, `predicate: "org-null"`, `captured_at
    2026-06-29T14:43Z`; `skills` **42,790**, `job_roles` **22,470**, `skill_embeddings` **42,790**,
    `job_role_embeddings` **18,919**. Both the "18K roles is refuted" and "60K skills is unverified,
    not refuted" framings follow from `public_only`.
14. **`external_services.md:136,209,219` — the rext `DIRECTUS_DATA_CONSUMERS` block**, read in the
    **pinned per-stack clone `09d06070`** (the tree that settles what a stack runs, ADDENDUM rule 45):
    `stack-injection/gen_injected_override.py:86` and `stack-core/gen_override.py:58` both
    `= ("cms", "backend")`; the inert-key rationale sits at `:77-81` with the quoted phrases verbatim;
    the re-point is at `:698-699`; `stack-injection/tests/test_injection.py:1109` is
    `def test_backend_the_actual_reader_is_repointed`, and the old
    `test_only_cms_is_repointed_not_other_services` survives only as a mention inside that test's own
    comment. The historical claim also checks out: at the prior pin `ab81527a` the same constructs sit
    at `:669-670` and `:84`.
15. **`ai_architecture.md:63-66` — five enum members, four switch cases.**
    `cms/directus/collections/jobsimulation.go:969-975` declares 5 `AIVendor` members;
    `jobsimulation/simulator/ai/ai.go` has 4 cases (`Openai :58`, `Azure :69`, `AnthropicAws :86`,
    `Anthropic :102`); the uncased one is `Azureglobal` at `jobsimulation.go:971`.
16. **`coursebuilder.md:118` — the credit prices.** The executable map
    `internal/credits/cost.go:86-90` is `{course.build: 5, course.refine: 1, course.translate: 1}`.
    Worth recording that the *package doc comment* at `:29` says "course.refine → **5** credits per
    refine turn" — the corpus follows the **code**, and the code's own `:88` comment ("D1 ruling: flat
    1 credit per refine turn") plus `:59-67` confirm 1 is correct. The corpus is right and the source
    prose is the thing that is stale.
17. **`external_services.md:565` / `ai_architecture.md:37` — the Mistral surface.**
    `git grep -ci mistral aeec036a` in `stack-demo/app/studio` (its **own** ref, per the nested-repo
    rule) = **22 lines / 3 files** (`requirements.txt`, `tools/pdf2md.py`, `tools/r3.py`) — exactly as
    published. `mistral.NewMistral` is 0 hits repo-wide in `app` @ `ad9f3c49`. `pdf2md.py:24`
    (`from mistralai import Mistral`), `:127` (`model="mistral-ocr-latest"`), and `r3.py:139`, `:190`,
    `:199-206` all resolve; so does the "`r3.py` DOES dispatch it" correction.

### RE-DERIVED — arithmetic/anchors the document itself displays (weaker clearance)

- `external_services.md:3,367` — router prod state: `graphql-wundergraph/terraform/main.tf:20`
  `service_desired_count = 1`; `locals.tf:8` `port = 8080`; `main.tf:48-49` container/host 8080;
  `config.prod.yaml:5` `listen_addr: 0.0.0.0:8080`; `"5050:8080"` really was compose-only
  (`1e8e754:docker-compose.yml:14`).
- `external_services.md:420-455` — the three build eras. `2adcf71` parents are `1e8e754` + `360efd4`;
  `1e8e754:docker-compose.yml:19-27` names exactly backend/jobsimulation/cms/storage (set unchanged
  since `a4db680`, 2026-07-21); `2c85211^` has `context:` with **no** `dockerfile:` key while
  `2c85211` adds `Dockerfile.dev`; `63d285c` (2024-06-20) names the service `wundergraph` and builds
  from a git URL; `b56d731` still has `  graphql:` at `:22`, `360efd4` has none and `  backend:` at
  `:28`.
- `external_services.md:459-473` — `Dockerfile.dev`: `wgc@0.104.0` at `:7`, one schema `COPY` at
  `:18`, the fold comment at `:19-20`, one `awk` at `:23`, runtime image
  `ghcr.io/wundergraph/cosmo/router:0.275.0` at `:29`.
- `external_services.md:483-485` — routing URLs `http://backend:8082/graphql/query`,
  `http://jobsimulation:8400/query`, `http://cms:8090/query` (the latter two at `915da06^`).
- `external_services.md:145-148` — the historical Directus compose service:
  `a2a3ee6^:docker-compose.yml:384` `image: directus/directus:10.10.1`, `:386` `8055:8055`, `:409`
  `ADMIN_PASSWORD=password`; `git log --all -S 'admin@example.com'` = empty. The
  "correction-of-a-correction" is right on all four counts.
- `external_services.md:132,249,256,197-198,767-768,849` — compose/`.env_example` anchors:
  `docker-compose.yml:53` (`DIRECTUS_PUBLIC_BASE_ADDR`, sole `DIRECTUS_*` in an `environment:` block
  running `:46-94`) at **both** `0dab54d` and `0c91421`; `.env_example:91-92` = `DIRECTUS_BASE_ADDR` +
  `DIRECTUS_TOKEN`; `:151`/`:160` next-web build-arg/runtime; `:119`/`:135` studio-desk, all four
  `:8082/graphql/query`; `:101-109` backend `depends_on` = redis/postgresql/sentinel.
  `NEXT_PUBLIC_GRAPHQL_ENDPOINT` = 0 hits in `next-web-app` (control: `NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT`
  matches `Dockerfile.dev` + the app `.env.example`s).
- `external_services.md:533` — `/graphql` is the Apollo Sandbox and `/graphql/query` the endpoint
  (`app/internal/web/backend/backend.go:315`, `:317`).
- `external_services.md:388-390` — `config.compose.yaml` has `playground_enabled: true`,
  `introspection_enabled: true`, `graphql_path: /graphql`, `apollo_compatibility_flags` (3 flags).
- `external_services.md:175` — `cms/terraform/main.tf:39` `service_desired_count = 0` at **both**
  `ca50c817` and origin/main `f38c0c4a`; `6efa1d5` (2026-08-04) is *"drop build-production — the cms
  ECR repository is decommissioned (M810)"*; `6092c6d2` (2026-08-04) is *"remove the jobsimulation
  ECS service and ECR repository (M810)"*, and the surviving module file at `82cb66ec:terraform/
  main.tf:15-40` states exactly the buckets/SSM/atlas-tracker carve-out the corpus describes.
- `external_services.md:564,567` + `coursebuilder.md:48-52` — `askengine/bedrock.go:25`,
  `report_agent.go:31`, `coursebuilder/bedrock.go:23,29`, `:98-104`, `:105-114`; `main.go:770`,
  `:766-779` (comment `:766-769`, warns at `:774`/`:778`), `:824-826` (`OPENAI_KEY` cover generator) —
  all exact at `b948604`, the ref they name. `app/main.go:980-982` `log.Fatalf` on an empty
  `DIRECTUS_BASE_ADDR` likewise.
- `external_services.md:581-600` + `ai_architecture.md:22-32,53-66,253-265` — the whole AI-manager
  derivation at `ad9f3c49`: consts `:30-33`; `getClient :259-289`; azure EU/US + `flag_use_azure_us`
  `:264-276` (error path keeps EU, `:270-272`); `AnthropicAws`/`Anthropic` → the same client
  `:280-283`; `isThrottlingError :129-141`; retry sets `vendor = Openai` at `:150-155` and
  `:296-302`/`:326`; `openai.NewOpenAI(openaiKey)` at `:80`; `config.WithRegion("eu-west-1")` at
  `:87`. Content side: `jobsimulation.go:905` (`AIVendor *AIVendor`), `:1297` (`Gpt5Point1`),
  `:1302-1305`, `:1307`. Runtime: `simulator/ai/ai.go:58-59`, `:65-66`, `:82-83`, `:98-99`,
  `:110-111`, `:114-115`, `:126-127`, `:20-26`.
- `ai_architecture.md:210-220` + `external_services.md:723,727` — `jobsimulation.go:1079-1085` is a
  4-member `SimulationVoiceEngine` enum; `:1594-1600` supplies `gptrealtime` on nil;
  `calls/livekit.go:110`/`:120` bare `anthropos-agent`, `:115` chain, `:126` US-suffixed, and
  `anthropos-agent-eu` is 0 hits; `flag_use_realtime_openai` has exactly **one** site, `:133`, inside
  `CreateAgentDispatch` (`:106`), effect at `:140-144`.
- `ai_architecture.md:269-290` — `validator.go:43,60-61,595` (`checkerEngines` stored, and never read:
  its only other sites are `criterion.go:34,72,90`, all declaration/assignment);
  `criterion.go:127` (`case check.EngineLlm:`), `:168` (concurrent `validateCodeDiff`), `:428`
  (`NewLLMBulkChecker`), `:450-475` (pure string comparison); `basevalidator/templates/
  checkValidationBulk.tmpl` exists; `skills.go:40-51` (20 / 100 / `max(0, score*2-100)` with the
  `// TODO fix this formula`), `:53-62` `calculateSkillScore`, `:75` `passed/total*100`;
  `skill.go:617-623` `convertLevelTo100`; `CompetencyReadLevel.tsx:18`.
- `ai_architecture.md:95-99` — `1e457fa70` (2026-08-04) exists with that subject; `internal/ai/ai.go`
  exists at `ad9f3c49` (so the "**NB that file DOES exist**" repair is correct);
  `jobsimulation/ai/ai.go:267`, `:344` and `skillerai/ai.go:347` are all `"flag_use_azure_us"` at
  `5ba17044`; `skillerai/ai.go` genuinely mirrors the four consts + `isThrottlingError` + `getClient`.
- `coursebuilder.md:30-59,73-76,107-122,140-152` — `coursebuildersession.go` Ent schema;
  `terraform/migrations/20260717151144.sql` creates `course_builder_sessions` + `credit_transactions`
  with the listed columns; `go 1.26.4`; 102 `.go` files; `ProductionConfig(author, grader,
  translator)` at `service.go:589` + `TestProductionConfig_WiresPlanner`; `//go:embed assets/*.md`
  (`embed.go:28,31,34`); `imagegen/openai.go`; `CoursebuilderSrv` at `internal/worker/worker.go:223`
  with `coursebuilderWorkerConcurrency = 20` (`main.go:190`); `TypeCoursebuilderRun`,
  `asynq.MaxRetry(1)` (`run_task.go:199`), `asynq.Retention(6*time.Hour)` (`:204`);
  `courseBuilderAccessGate` (`gate.go:27`, used `backend.go:227`); `Register` at `handler.go:3143`
  and every listed route present; `cmd/coursebuilder-{liverun,e2e}` with `assertNotSharedDSN`
  (`cmd/coursebuilder-e2e/main.go:378`); `askengine/coursebuilder_tool.go` →
  `SetCourseBuildStarter` (`ask/handler.go:82`, wired `backend.go:259`);
  `COURSEBUILDER_MAX_MONTHLY_COGS_USD` default **500** (`budget.go:27`),
  `COURSEBUILDER_MAX_DAILY_COGS_USD` default **0 = off** (`:41`),
  `DefaultSessionsPerOrgPerDay = 50` (`rate.go:28`); `COURSEBUILDER_OPENAI_IMAGE_KEY` removed from Go
  at `68c245123` and surviving only in `README.md:429` / `GO-LIVE-RUNBOOK.md:35`. The version block is
  exact to the character: `git describe --tags ad9f3c49` → `v1.369.0-7-gad9f3c498`, `2035f9a4` →
  `v1.369.0-2-g2035f9a40`, `5ba17044` → `v1.363.2`, and Wave 24 is the highest wave in `SPEC.md`.
- `clerk-integration.md:21,73,79,81,46` — `apps/web/src/app/api/metabase/route.ts:35` really tests
  `authData.orgRole !== 'admin'` (the bare form); `OrgAddUserToRole`/`OrgReplaceUserRole`/
  `OrgRemoveUserFromRole` and `OrgCheckUserPermission`/`CheckFeature` all present;
  `ant-academy mobile/src/components/ClerkSignInForm.tsx:49` proceeds only on
  `result.status === 'complete'`, so any second factor aborts.
- `external_services.md:339,265` + `ai_architecture` local-Directus claims — in the pinned rext
  `09d06070`: `directus/directus:11.6.1` (`gen_injected_override.py:309,325`;
  `dev-stack/dev-setdress.sh:255`) and `ADMIN_EMAIL=admin@<stack>.example.com`
  (`dev-setdress.sh:308`).
- `external_services.md:703-705` — studio-desk `AI_PROVIDER_CHAIN=azure-openai,openai`
  (`.env.example:56`), tiers `gpt-5.2` / `claude-opus-4-5-20251101` / `claude-sonnet-4-5-20250929`
  (`src/services/ai/config.ts:21-37`), circuit breaker at `src/services/ai/providerHealth.ts`.
- `architecture/README.md:13,15` — `rosetta-extensions/alignment/`, `stack-seeding/dna/` and
  `stack-core/platform_alignment_guard.py` all exist in **both** rext trees (pinned `09d06070` and
  authoring `680e8529`).

---

## What I could not settle

1. **`external_services.md:3` — "the repo is ARCHIVED on GitHub (2026-07-30)".** Archive status is a
   GitHub API property; I have no network. The local clone is at `60c229f3` and its last commit is
   dated 2026-07-30, which is consistent but not probative. Settled by `gh api
   repos/anthropos-work/graphql-wundergraph --jq .archived`.
2. **`ai_architecture.md:159` — "skiller migrations `20260417103036` and `20260417120309`".** The
   `skiller` repo is decommissioned and not in the clone set; neither filename exists under
   `app/terraform/migrations/`. Settled by a clone of `skiller` at any ref, or by locating the ported
   `job_role_embeddings` / `skill_embeddings` DDL in `app`'s migration set.
3. **`ai_architecture.md:196` — "`azure-eu` and `azure-eu-fr` were measured at M257x iter-01 as
   dispatching nothing".** The five `livekit-agent*` repos are in no clone set. What I *can* measure
   cuts the other way on the literal reading: `calls/livekit.go:101-104` defines
   `euAgentEndpoints = {"azure-eu", "azure-eu-fr"}` and `:122-123` selects one at random into
   `metadata["endpoint"]`, so those *endpoint tokens* are certainly dispatched. Settled by cloning
   `livekit-agent-azure-eu` / `-eu-fr` and reading what they subscribe to.
4. **`ai_architecture.md:89` residual (see B4).** Whether "TTS v2 HD / TTS v2" ever existed in the
   *standalone* `github.com/anthropos-work/ai` module (v1.40.2, still required by the frozen
   `cms`/`jobsimulation`) is unmeasurable — that module is in no clone set.
5. **`coursebuilder.md:145` — "142 changelog lines".** `internal/coursebuilder/` has no `CHANGELOG`
   file; `SPEC.md` has a `## Changelog` section starting at `:19` but I could not find a delimitation
   that yields 142. Settled by naming the file and the counting rule beside the figure.
6. **`clerk-integration.md:21,65,67` — what `AuthRole()` returns and that it is "never enforced inside
   `authn`".** `colony` is a private Go module, not cloned; `AuthRole` has **0** hits in `app`'s Go at
   `ad9f3c49`, which is consistent with `:73`'s "zero call sites on the allow/deny path" but says
   nothing about the getter's own body. Settled by a `colony` clone at `v0.35.2`.
7. **`architecture/README.md:15` — that the alignment guard fences `repos.yml` membership "in both
   directions".** I confirmed `stack-core/platform_alignment_guard.py` exists in both rext trees but
   did not execute it or read its assertion set; per §5 rule 50 a verdict about that fence is settled
   only by a run from the **authoring copy** with its tree + sha + dirty state recorded, which is
   outside a read-only audit's remit.
