# Seat C — reading #27

**Ground truth actually read** (re-verified at open, all matching the ADDENDUM's superseded table):
platform `0c91421d` (= origin/main) · app `ad9f3c49` (= origin/main) · next-web-app `8297c684`
(origin `f97ba659`) · sentinel `f2c46190` · studio-desk `41ee3575` · ant-academy `22df69dd` ·
storage `4ce8ece5` (origin `9f8cb532`) · messenger `fa47850d` (origin `e9421c68`) · cms `ca50c817`
(origin `f38c0c4a`) · jobsimulation `462343b0` (origin `82cb66ec`) · roadrunner `87d8d443` ·
graphql-wundergraph `60c229f3` · nested `app/studio` + `cms/studio` each `aeec036a`.

**rosetta-extensions — which tree settled what.** I read **both**: pinned per-stack
`stack-demo/rosetta-extensions @ 09d06070` and authoring `.agentspace/rosetta-extensions @ 680e8529`.
For every path `alignment_testing.md` cites, the two trees are **byte-identical** — I compared the
git tree objects directly: `alignment/` = `a64430e9…` in both, `clerkenstein/alignment/` =
`5293227b…` in both, `stack-seeding/dna/` = `44a1f052…` in both. So no tree ambiguity arises for
this seat's findings; quoted line numbers below hold in both clones.

**Search-instrument disclosure.** Tree-wide zeros in this report were taken with `git grep` at each
tree's own HEAD across **all 15 git checkouts under `stack-demo/`** (13 top-level + the two nested
`studio` repos), never at a host ref. Mechanism 1 (gitignored-but-tracked) is covered because
`git grep` sees those files. Mechanism 2 (NUL-bearing): the two known NUL files
(`next-web-app/apps/web/src/hooks/useCoursebuilder.ts`, `ant-academy/code/src/time/store.js`) are
neither `.tf` nor in any predicate I asserted a zero for. Positive controls were run alongside every
zero and are named inline.

## Positive controls

| file | `wc -l` | read in full? |
|---|---|---|
| `corpus/architecture/alignment_testing.md` | 542 | yes |
| `corpus/architecture/platform-migration-status.md` | 330 | yes |
| `corpus/services/jobsimulation.md` | 312 | yes |
| `corpus/services/customerio-sync.md` | 165 | yes |
| `corpus/services/askengine.md` | 147 | yes |
| `corpus/services/intelligence.md` | 18 | yes |
| *(total)* | **1514** | |

---

## Blockers

### B1 — `jobsimulation.md` cites a cms path that exists in **neither** clone, and points at the frozen repo rather than the live copy

- **Anchor:** `corpus/services/jobsimulation.md:160` —
  > *"…**when that field is nil the default is `gptrealtime`** (`cms/directus/collections/jobsimulation.go:1594-1597`), not ElevenLabs"*

- **Why it is false:** There is no `cms/directus/` directory. `git ls-tree -d --name-only ca50c817`
  in `stack-demo/cms` returns `.agentspace .github cmd ent internal knowledge terraform` — the file
  is at **`cms/internal/directus/collections/jobsimulation.go`**. The *live* copy (the one a reader
  should be sent to, since cms is folded into `app`) is
  **`app/internal/cms/directus/collections/jobsimulation.go`**
  (`git -C stack-demo/app grep -n "func voiceEngineFromDirectus" HEAD` → that path, `:1594`).
  The **claim and the line range are exactly right in both files** — `:1594` is
  `func voiceEngineFromDirectus(...)`, `:1595-1596` is `if directusVoiceEngine == nil { return
  simulation.SimulationVoiceEngineGptrealtime }` — but the *path* resolves nowhere.
  The corpus writes this file's path correctly at **12 other sites** and nowhere else uses the
  `cms/directus/…` form: `ai_architecture.md:56,211,240,253`, `external_services.md:566,624`,
  `snapshot-spec.md:565,665` etc. all use `app/internal/cms/directus/…` or
  `cms/internal/directus/…`. `external_services.md:318` even carries the explicit warning
  *"`app/internal/cms/directus/` (NOT the frozen cms repo's `internal/directus/`)"* — which the
  offending citation silently violates in the direction that warning exists to prevent.
- **Confidence:** medium. **Why I hesitated:** the fact is true and the line numbers resolve
  byte-exactly in *both* real files, so nothing a reader *learns* here is wrong; only the path they
  would `cd` to is. I booked it because "wrong file paths" is the first enumerated BLOCKER class, and
  because the truncated form points at the decommissioned repo rather than the live one.

### B2 — customerio-sync was **not** the only compose service built from a git URL; the pattern was the platform norm before `a2a3ee6`

- **Anchor:** `corpus/services/customerio-sync.md:163-164` —
  > *"It was the **only** compose service built that way, and `838d907` took the pattern with it — do
  > not reach for it as a precedent when adding a service."*

  (restated at `:85-86`: *"**It was the only service built that way, and the pattern died with it**"*)

- **Why it is false:** re-derived over the full history of `docker-compose.yml`
  (`for c in $(git log --follow --format=%H -- docker-compose.yml); do git show $c:docker-compose.yml
  | grep -E 'context:\s*(git@|https://|github)'; done | sort -u`, in `stack-demo/platform`) —
  **21 distinct git-URL build contexts** have existed, including
  `app`, `chronos`, `cms`, `graphql-wundergraph`, `intelligence`, `jobsimulation`, `messenger`,
  `realtime`, `roadrunner`, `sentinel`, `simulator`, `skiller`, `skillpath`, `storage`,
  `studio-desk`. Building from `git@github.com:anthropos-work/<repo>.git#main` was the **default
  pattern** until `a2a3ee6` (2026-02-27, *"add Makefile, repos.yml, and switch to local Dockerfile.dev
  builds"*) moved every service to a local sibling context. And it was not unique even *after* that:
  at `a2a3ee6` **two** services kept a git URL —
  `git show a2a3ee6:docker-compose.yml | grep -n "context:"` shows
  `:393 context: git@github.com:anthropos-work/customerio-sync.git#main` **and**
  `:458 context: git@github.com:anthropos-work/realtime.git#main`. `realtime` held it until
  `c17cc9a` (2026-04-15). Only from that date was customerio-sync the sole one.
- **Confidence:** low. **Why I hesitated:** the *contemporaneous* reading — "the only one still built
  that way when it was deleted" — is true (at `0dab54d`, `git show 0dab54d:docker-compose.yml | grep
  -n "context:"` shows exactly one git URL), and `:85-86`'s second clause ("every **remaining**
  compose build takes a local sibling directory as its context") is correct. But `:163-164` sits in a
  section explicitly framed as history (*"the pattern was intentional while it lasted"*) and states
  the superlative flatly, where it is measurably wrong by an order of magnitude.

---

## Minors

- `corpus/architecture/alignment_testing.md:200` — *"its rationale is the comment block at `:58-68`"*.
  The comment block in `clerkenstein/alignment/scripts/gate.sh` is **`:58-67`**; `:68` is
  `echo "==> capability-coverage check ($dna)"`, not a comment. The load-bearing part of the same
  parenthesis (`:61` is inside the comment; `:69` is the `--if-declared` call) is exactly right.
- `corpus/architecture/alignment_testing.md:532-540` — the Layout block enumerates four `internal/`
  packages (`dna`, `outcome`, `compare`, `report`); the tree holds **five** —
  `git ls-tree --name-only HEAD alignment/internal/` returns `canon compare dna outcome report`.
  `internal/canon` is what implements the *"canonical and precision-preserving"* comparison the doc
  claims at `:125-126`, so it is an omitted list member rather than a wrong one; the block also
  silently omits `go.mod`/`README.md`, so it is evidently not offered as exhaustive.
- `corpus/services/askengine.md:103` — *"the Redis subscribers (`:1438` onward)"*. `app/main.go:1438`
  @ `ad9f3c49` is mid-comment (`// enough: merely constructing this server and calling Subscribe()…`).
  App's own subscriber server is constructed at **`:1376`** (`subServer := pubsub.NewSubscriberServer(`)
  and messenger's at **`:1450`**. The sentence's substance — everything after the `:471` `return`
  never runs — is unaffected, and "onward" is doing real work.
- `corpus/services/jobsimulation.md:50` — *"The literal does occur in the clone set — **6 times**,
  none of them terraform; the count is derived once in `backend.md` and **not restated here**."* The
  sentence restates the count in the same clause that says it does not.

---

## Positively cleared (audited zeros)

Labelled per the briefing. **ENUMERATED** = I built the predicate's set from source and compared
cardinalities. **RE-DERIVED** = I re-computed arithmetic the document itself shows.

### `platform-migration-status.md`

- **ENUMERATED — the row set's own completeness derivation (§2, `:75-81`).** Re-derived the *set*,
  not the sum. `git log -p --follow -- repos.yml | grep -E '^\+\s+- name:'` → **14** distinct names
  (app · chronos · cms · graphql-wundergraph · intelligence · jobsimulation · messenger ·
  next-web-app · roadrunner · sentinel · skiller · skillpath · storage · studio-desk) — matches, and
  **all 14 have rows**. The compose-history sweep (keys under `services:` only, across every commit
  touching `docker-compose.yml`) → **25** names including `nats`, `web-app`, `chromedp`, `simulator`,
  `realtime` — matches, **all 25 have rows** under the three documented aliasings (`backend`→`app`;
  `graphql`/`wundergraph`→`graphql-wundergraph`). The stated section-blind overcount of **26** with
  `app-network` as the single extra is correct (`docker-compose.yml:185-186` declares it under
  `networks:`).
- **ENUMERATED — the fence's own three coverage numbers (§4, `:204-205`, `:222-223`).** Ran the guard
  from the **authoring copy** (rule 50 — a verdict is settled by the tree its configuration lives in):
  `python3 .agentspace/rosetta-extensions/stack-core/platform_alignment_guard.py
  corpus/architecture/platform-migration-status.md stack-demo/platform/repos.yml`.
  Output verbatim: `fence-tree: …/.agentspace/rosetta-extensions @ 680e8529f ·
  describe=fast-build-m257x-iter-101-6-g680e852` / `assertion F resolved 96 citation(s) — 25
  subject-checked, 70 range-only, 1 outside any service block; 0 unresolvable; 0 read from the
  WORKTREE` / `OK — … agree in both directions`, **exit 0**. Matches the document's 25 / 70 / 1
  exactly. (25+70+1 = 96 is the only arithmetic here and it is trivially consistent.)
- **ENUMERATED — every `docker-compose.yml` / `common.yml` / `repos.yml` anchor in the table.** Read
  the whole of each file at `0c91421` with line numbers and checked each cited line names the claimed
  construct: `:28` `backend:` · `:48` `AUTHORIZATION_ADDRESS=http://sentinel:8087` · `:57`
  `GOTENBERG_URL` · `:59` `JUDGE0_BASE_URL` · `:73-75` storage-in-app comment · `:82`/`:83` the two
  bucket vars · `:84-92` the MESSENGER/CUSTOMERIO rationale · `:102-103` the `depends_on` comment ·
  `:5` `sentinel:` · `:18` `search_path=sentinel` · `:112`/`:141`/`:119`/`:135` studio-desk ·
  `:143`/`:168`/`:151`/`:160` next-web-app · `:170-171`/`:183` gotenberg · `:185-186` networks ·
  `:1-2` the include. `repos.yml` is **28 lines / 4 entries**; `:2-10`, `:12-13`, `:14-17`, `:18-20`,
  `:23-25`, `:26-28` all land as claimed. `common.yml:2` postgresql, `:22` `start_period: 120s`,
  `:24` redis. And `0dab54d:docker-compose.yml:154` = `profiles: [customerio-sync, all]`.
- **ENUMERATED — the seven in-process wiring call sites in the `app` row.** Every one resolves at the
  named pin `2035f9a`: `:690` `skiller.NewSkillerManager` · `:721` `jobsimwiring.Wire` · `:751`
  `skillpath.NewSessionManager` · `:1153` `appcms.Wire(` · `:524` `internalstorage.NewManager` ·
  `:1471` `msgadapters.Wire(` · `:395` `customeriosync.New(`. All seven `app/internal/<domain>/`
  directories exist at that ref; **`app/internal/roadrunner/` does not** (`git cat-file -t` → fatal),
  and `internal/jobsimulation/runner` does, matching the row's correction. The currency note is
  measured: `git rev-list --count 2035f9a..origin/main` = **5**, and `git diff --stat` over those five
  touches exactly `.claude/skills/publish/SKILL.md`, `CLAUDE.md`, `knowledge/deployment.md`,
  `terraform/main.tf`, `terraform/variables.tf` — **no Go source**, and `main.go` is byte-identical
  between `2035f9a` and `ad9f3c49` (`diff -q` clean). The single `terraform/main.tf` hunk is a
  one-line in-place rewrite of the `atlas_sentinel_dev_url` precondition **error message**, exactly as
  stated, so `app/terraform/main.tf:181` (`service_desired_count = 1`) is stable at both refs. All
  four named refs carry their claimed tags: `5ba17044`=v1.363.2, `b948604`=v1.366.0,
  `9d00a313`=v1.367.0, `2035f9a`=`v1.369.0-2-g2035f9a40` (i.e. post-v1.369.0).
- **ENUMERATED — the `storage` row's `STORAGE_RPC_ADDR` two-ref split, the hardest cell in the file.**
  At `2035f9a`: `git grep -n STORAGE_RPC_ADDR 2035f9a -- '*.go'` → exactly **3**, all comments, at
  `main.go:504`, `internal/jobsimwiring/wiring.go:101`, `internal/storagens/callsites_test.go:189` —
  and `main.go:504` reads *"…the standalone service takes no traffic and STORAGE_RPC_ADDR is gone."*
  At `b948604`: **15** hits, and I classified every one rather than trusting the sum —
  **7 env lookups** (`main.go:446,524,992`; `jobsimwiring/wiring.go:115`; `cmd/import/main.go:50`;
  `cmd/academyImport/main.go:231`; `cmd/academy-asset-upload/main.go:129`), i.e. main.go ×3 +
  wiring ×1 + one in each of the three `cmd/` tools; **8 others** = 2 doc comments (`:28`, `:35`),
  2 error strings (`:133`, `:235`), **3** `t.Setenv` (`main_test.go:41,52,64`) and **1** `t.Fatal`
  message (`:47`). Six of the seven lookups are `os.Getenv`; the seventh (`wiring.go:115`) is the
  lowercase `getenv` helper. Every clause of that sentence, including the "+1 overstates by one"
  correction, is exact.
- **ENUMERATED — the `storage` prod-teardown cell, at the ref it names (`9f8cb53`).**
  `terraform/main.tf` is **18 lines**; `:9-11` is the verbatim *"The ECS service that used to live
  here is GONE (v9.0 …)"*; `:13-16` is the `prevent_destroy`-read-from-configuration rationale; `:18`
  is *"See outputs.tf — consumers should reference these by output, never by literal name."* — and
  is indeed **not** a custody clause. `git grep -n -i custody 9f8cb532` returns **nothing, rc=1**
  (positive control in the same pass: `git grep -ci storage 9f8cb532` returns hits in
  `.github/workflows/tests.yml`, `CHANGELOG.md`, … — the pipeline works). M903 occurs once, at
  `terraform/storage.tf:22-25`, in the exact words quoted, and there are no `moved` blocks. `d3e6d32`
  exists with the quoted subject and the body *"M903 never ran."*
  ⚠ **Ref note:** these hold at `origin/main` `9f8cb532`, which is the ref the cell names; at the
  **checkout** `4ce8ece5` `terraform/main.tf` is still 100 lines with `service_desired_count = 1` at
  `:19`. See *What I could not settle*.
- **ENUMERATED — the `messenger` row's consumer side.** `app/main.go` at `ad9f3c49` (= `2035f9a` for
  this file): `:15` `msgflow`, `:62` `msgadapters`, `:63` `msgsender` (I printed `:12-20` and `:58-66`
  rather than trusting a range); `:1450` `pubsub.NewSubscriberServer(`; `:1471` `msgadapters.Wire(`;
  `:1473` `msgsender.NewFromEnv(logger)`; `:1416-1421` is exactly the "the group name is messenger's
  … Pinned by TestMessengerConsumerGroupIsMessengers" comment; `:285`/`:286` the two switch
  resolutions; `:1445`/`:1552` the two `if messengerEnabled` guards. `app/env_guards.go:92-111` is
  precisely `func resolveSubsystemSwitch` end-to-end, with `case "":` → error when `deployed`, `nil`
  otherwise. `app/terraform/main.tf:415-416` = `"name": "MESSENGER_ENABLED"` / `"value": "true"`.
  `messenger/cmd/root.go:120-140` reads all four RPC addresses (CMS, BACKEND_USERS ×2, SKILLER,
  JOBSIMULATION) at **both** messenger refs.
- **ENUMERATED — the `jobsimulation` M810 cell.** `jobsimulation/terraform/main.tf` @ `82cb66ec`:
  `:15-22` is the M810 decommission header naming ECS service / task definition / ECR repository /
  IAM roles / security group / Cloud Map / log group / alarms, and `:24-40` is the "what this module
  still owns" block (livekit.tf, chime.tf, ssm.tf, the atlas tracker, `:38-40` the separate
  schema-drop step). `git log 462343b0..origin/main` returns **exactly four commits, all dated
  2026-08-04**: `6092c6d2` (the terraform delete, subject as quoted), `caf36c96` (*Merge pull request
  #439*, committer `GitHub`), `1e40d184`, `82cb66ec` (v0.254.0) — the archive-state argument is sound.
- **ENUMERATED — the `roadrunner` row's line-provenance correction.** `git blame -L 19,19` on
  `roadrunner/terraform/main.tf` names **`84a4b4f1` (2025-12-15)**, not `e45eb61`; `git show e45eb61`
  is +1/−1 on **line 11 only**, a `infrastructure-modules.git` → `infrastructure.git//modules` source
  swap whose message says verbatim *"Module contents are identical; this is a pure source-URL swap."*
  Repo HEAD is `87d8d44` (2026-06-19). And the commit-message-vs-diff claim: `git show d11a403 --
  repos.yml` deletes `- name: cms`, `- name: jobsimulation`, `- name: roadrunner` — refuting
  `d11a403`'s own *"its repos.yml clone entry was already gone"*. The service counts are exact:
  `d11a403^` = **11** compose services, `d11a403` = **8**, `838d907`/HEAD = **5**.
- **ENUMERATED — the `graphql-wundergraph` row.** `terraform/main.tf:20` = `= 1`;
  `supergraph-config-prod.yaml` lists `backend` alone; `git ls-files` shows `schemas/backend.graphqls`
  as the only file under `schemas/`; `subgraphs.conf` is the single line `BACKEND=v1.360.0`;
  `915da06` (2026-07-29) is the fold commit; `b56d731` and `360efd4` are both ancestors of `2adcf71`
  (PR #23 `chore/drop-wundergraph`) and between them delete the compose service and the `repos.yml`
  entry.
- **ENUMERATED — every platform commit sha + date the file cites.** All 23 checked with
  `git log -1 --format='%ad | %s' --date=short`: `b56d731` `360efd4` `2adcf71` `21429b7` `a4db680`
  `045857c` `fdfa189` `a2a3ee6` `cb6ebf5` `ef4b449` `1474b1f` `84862d1` `b43b99a` `c17cc9a` `467965a`
  `8770fe6` `6060315` `236771f` `d11a403` `838d907` `0c91421` `0dab54d` `ef32d4c` — **every date
  matches** and every subject supports its claim. `a2a3ee6` really does remove the `directus:` block
  (present at `a2a3ee6^:383`, absent after). `6060315` is an ancestor of `ef32d4c`.
- **ENUMERATED — the two commit-message quotations.** `838d907`'s body contains *"was still in the
  `all` profile, so `make up-all` started a second Brevo contact pusher alongside backend's own"* and
  *"one `--profile` flag from a second writer on the prod bucket and a second consumer on messenger's
  Redis group"*, plus the *Kept* paragraph ending *"Nothing in that block addressed a deleted
  container"* — all as quoted. `d11a403`'s body contains *"silently dropped env that app still reads
  in-process"* and the restore list (`JUDGE0_BASE_URL`, `DIRECTUS_PUBLIC_BASE_ADDR`,
  `REDIS_WORKER_INDEX`, LiveKit, Chime, the `~/.aws/credentials` mount) — §5 row 5 is accurate.
- **RE-DERIVED — `docker-compose.yml` 271 → 186 lines and `838d907` = +11/−96 on that file.**
  Both exact. `repos.yml` entry counts 9 (`d11a403^`) → 6 (`0dab54d`) → 4 (HEAD) also exact.
- **ENUMERATED — `mid-fold` is carried by no row** (scanned all 32 rows); the five services whose
  departure the §3 note says do not move the "46" (`cms`, `jobsimulation`, `roadrunner`, `storage`,
  `messenger`) each have a `corpus/services/*.md`.

### `jobsimulation.md`

- **ENUMERATED — the `http://backend.internal.anthropos:8081` census (`:50`), re-derived
  independently rather than inherited.** Per-tree `git grep -Fci` at each of the **15** checkouts'
  own HEADs: total **6** matching lines — `stack-demo/app` 1, `stack-demo/rosetta-extensions` 5, all
  others 0. `.tf`-restricted (`-- '*.tf'`): **0 files in every tree**. Tracked `.tf` files across the
  13 top-level clones: **44** (app 5, cms 4, graphql-wundergraph 4, jobsimulation 6, messenger 4,
  next-web-app 2, roadrunner 4, rosetta-extensions 2, sentinel 4, storage 5, studio-desk 4,
  ant-academy 0, platform 0). Raw filesystem sweep `find stack-demo -name '*.tf' -type f | wc -l`:
  **59**. Every figure in that passage — 0 / 44 / 0 / 59 / 6 — reproduces exactly. And
  `app/knowledge/service-dependencies.md:52` @ `ad9f3c49` is verbatim the past-tense sentence quoted,
  under the heading *"There are no external callers of app's RPC mux left."* (`:50`).
- **ENUMERATED — the 23-table figure and the `sessions` rename.**
  `grep -cE '^CREATE TABLE' app/terraform/migrations/20260722081626_jobsim_data_model.sql` = **23**,
  and I listed all 23 names (they include `sessions`, `actors`, `interactions`, `validation_*`,
  `anticheat_*`, `recordings`, `chime_recordings`, `code_submissions`). In the next migration
  `20260722104506.sql`, `:2` is `CREATE TABLE "job_simulation_sessions" (` and `:79` is
  `DROP TABLE "sessions";` — both exact.
- **ENUMERATED — the mirror-drop migration (`:130-135`).**
  `app/terraform/migrations/20260729133514.sql` is 64 lines. `:15-23` are the two link-id remap
  `UPDATE`s; `:36-44` the two orphan-NULLing `UPDATE`s; `:58-61` the *"5. Drop the mirrors."* comment
  with `:62-63` the two `DROP TABLE`s of `local_jobsimulation_sessions` / `local_skill_path_sessions`.
  The **0-`INSERT`** claim survives the wrong-regex trap: a naive `grep -ci insert` returns **1**, but
  the single hit is the *function name* `on_insert_local_jobsimulation_sessions_update_memberships`
  at `:64`; `grep -niE 'insert[[:space:]]+into'` returns **nothing** (control: `grep -ci drop` = 10).
  `app/internal/organization/intelligence.go:1700` is `query := m.ent.JobSimulationSession.Query().`.
- **ENUMERATED — the `2adcf71` historical block (`:20-23`).** `docker-compose.yml:83` @ `2adcf71` is
  `jobsimulation:`, `:93-94` publish `8400:8400`/`8401:8401`, `:113` `PORT=8400`, `:119`
  `RPC_PORT=8401`, `:140` `profiles: [graphql, jobsimulation, all]`; `Makefile:10` is
  `PROFILE ?= graphql`; `repos.yml:17-19` is the jobsimulation entry with
  `migrations: false # legacy — folded into app (jobsim-in-app)`. Every anchor lands.
- **ENUMERATED — the binary's own contract (`:96`, `:180-190`, `:293`).** `cmd/root.go:77`/`:78` are
  `cmp.Or(os.Getenv("PORT"), "8080")` / `cmp.Or(os.Getenv("RPC_PORT"), "8081")`; both `Dockerfile`
  and `Dockerfile.dev` are `EXPOSE 8080` + `ENTRYPOINT ["./application"]` with no `CMD`; the cobra
  `Use:` set is exactly `{aggregate, clone-session, test-command, validate}` beside the root
  (`jobimulation`); the repo's own `CLAUDE.md:88` really does document the non-existent
  `go run . serve`.
- **ENUMERATED — the `backend` fatal-path anchors, at each of the three refs they name.**
  `app/main.go`: `:721` `jobsimwiring.Wire(` and `:723` `log.Fatalf("jobsim-in-app: engine wiring
  failed …")` @ `ad9f3c49`; `:668`/`:670` the same pair @ `9d00a313`; `:612`/`:614` @ `b948604`.
  `:229` is `func main()` @ `ad9f3c49`, `:216` @ both `7177374` and `9d00a313`, `:212` @ `b948604`;
  `7177374` is dated 2026-08-04 and `git rev-list --count 7177374..ad9f3c49` = **38**. At `ad9f3c49`,
  `:670` is indeed `skillerAzureEndpointEu = &v`. The only `spf13/cobra` importer in the whole app
  repo is `cmd/createTaxonomy/main.go` (`git grep -ln`). `internal/jobsimulation/ai/ai.go:90` is
  `return nil, fmt.Errorf("can't load AWS config: %w", err)` and
  `internal/jobsimwiring/wiring.go:147-148` is the unwrapped `return nil, err` — both @ `9d00a313`,
  the ref that sentence pins.
- **ENUMERATED — the AWS-bind landmine (`:228-231`), at the pinned ref `0dab54d`.** `:88-89` the
  jobsim-in-app comment, `:90` `volumes:`, `:91` the bind — and `grep -c 'HOME/.aws'` over that whole
  file = **1**, so "the only AWS bind in the file" holds. (At HEAD it has moved to `:100`; the
  passage pins `0dab54d`, so it is graded there.)
- **ENUMERATED — the tooling claim at `:149`.** `DIRECTUS_DATA_CONSUMERS = ("cms", "backend")` in
  **both** twins — `stack-core/gen_override.py:58` and `stack-injection/gen_injected_override.py:86`
  — identical in the pinned per-stack clone and the authoring copy. The injected twin's comment
  independently records the *"96 Directus lines … every one a 403"* measurement.
- **ENUMERATED — the remaining source anchors.** `internal/graph/queries.resolvers.go:70` is
  `func (r *queryResolver) JobSimulationResult(...)`; `internal/runner/runner.go`'s package doc reads
  *"(formerly the standalone \"roadrunner\" service)"*; `internal/calls/livekit.go:131-135` is the
  `flag_use_realtime_openai` read and `:140-144` sets `agentEndpoint = "openai-hosted"` **and**
  `agentName = "anthropos-agent"`, overriding the `anthropos-agent-%s` US name built at `:126`;
  `cmd/root.go:284-285` registers the jobsim + `cms` subscribers on the colony `SubscriberServer`;
  `internal/simulator/manager/interview_report_csv.go` exists; worker concurrencies are **10**
  (`worker.go:27`) and **25** (`:104`); `go.mod` pins `hibiken/asynq v0.26.0`; `09631fb2` and PR #395
  (`feat/remove-chronos-and-realtime`, merge `500b9761`) both exist; migrations `20260402145459` and
  `20260409131539_add_summary_to_extraction_results` both create/extend
  `interview_extraction_results`; platform `05b4035` (2026-04-24) adds `READONLY_DB_CONNECTION`.
  `architecture_overview.md:335` is the correctly-scoped sentence it points at.
- **ENUMERATED — the cms cross-reference (`:12`).** `cms/terraform/main.tf` is **191 lines** at both
  `ca50c817` and `f38c0c4a`, with `:39` `service_desired_count = 0` at both; `6efa1d5` exists with
  the quoted subject, and its body contains *"which destroys the ECS service and the production-cms
  ECR repository"* and *"would try to push an image into a registry that no longer exists."*

### `customerio-sync.md`

- **ENUMERATED — every `app` anchor at the ref the file pins (`ad9f3c49`).**
  `internal/customeriosync/doc.go:4-5` is verbatim *"Ported out of the customerio-sync repository
  (v0.19.3), the last of the / Go services to be folded into app"* — and the sentence does wrap the
  break, exactly as the parenthetical says. `env_guards.go:62` is
  `envCustomerIOSyncEnabled = "CUSTOMERIO_SYNC_ENABLED"`; `:87` is the `log.Fatalf("subsystem switch:
  %v", err)` inside `mustSubsystemSwitch`; `:98-104` is the `case "":`/`if deployed` error branch.
  `main.go:286` resolves the switch, `:284` is a **comment** (as the file's own correction says),
  `:295` is the condition and `:296` the `log.Fatalf`, `:393-396` is the `copilotDB` construction,
  `:395` is `customeriosync.New(logger, copilotDB, os.Getenv("BREVO_KEY"))`. The pin's justification
  checks out too: `git ls-tree b948604f -- env_guards.go` is **empty** (the file did not exist), and
  `git diff --stat 2035f9a ad9f3c49 -- env_guards.go` is empty (byte-identical).
  `app/terraform/main.tf:419-420` is `"name": "CUSTOMERIO_SYNC_ENABLED"` / `"value": "true"`.
  `internal/customeriosync/` holds `doc.go store.go sync.go sync_query.sql` (+ a test);
  `cmd/customerio-resync` exists.
- **ENUMERATED — the port record (§ *What changed in the port*).** Every claim is in `doc.go`:
  the stateless overlap window with `CreateContact` + `UpdateEnabled` idempotence (`:38-44`), the
  `database/sql`-not-sqlc NULL consequence `""`/`0`/`false` (`:46-52`), the 10-minute ticker →
  asynq scheduler (`:15`), the view→`sync_query.sql` replacement **verified at 7,392 of 7,397 rows**
  (`:29-31`), the shared `copilotDB` pool (`:72-74`), and `--dry-run` constructing no Brevo client
  (`:68`).
- **ENUMERATED — the `external_services.md` cross-claim (`:151-156`).** `grep -n "^## "` returns
  exactly the sections named — Clerk, Directus, the Cosmo router, AI Providers, LiveKit, AWS Chime,
  beside four non-service sections; and `grep -ci brevo` = **0** (control in the same pass:
  `grep -ci clerk` = 38, so the pipeline is live). The "no Customer.io section, no Brevo section"
  claim is true in both directions.
- **ENUMERATED — the historical compose block (`:67-81`).** Matches
  `0dab54d:docker-compose.yml:136-154` clause for clause: the `git@…#main` context, `ssh: ["default"]`,
  `VERSION`/`GH_ACCESS_TOKEN` args, `8080:8080`, the `DB_CONNECTION_BACKEND` DSN with
  `search_path=public`, the `postgresql` healthy dependency, and `profiles: [customerio-sync, all]`.
  (The *uniqueness* gloss around it is B2.)

### `askengine.md`

- **ENUMERATED — the whole numeric spine, each from its own definition site @ `ad9f3c49`.**
  `main.go:167` `askEngineMaxConns = 6`; `internal/askengine/executor.go:18` `MaxInlineRows = 200`,
  `:21` `MaxCellLength = 400`, `:24` `QueryTimeout = 10 * time.Second` (and `:97` issues the
  `SET LOCAL statement_timeout` inside the tx); `internal/web/backend/ask/handler.go:34`
  `maxAgenticIterations = 15`, `:41` `loopTimeout = 10 * time.Minute`;
  `internal/askengine/bedrock.go:25` `DefaultModelID = "eu.anthropic.claude-sonnet-4-6"`, `:26`
  `DefaultRegion = "eu-west-1"`, `Temperature: param.NewOpt(float64(0))`; `rules.md` is
  **146,219 bytes** (≈146 KB).
- **ENUMERATED — the allowlist registry, set-first.** Built the set from
  `internal/askengine/registry.go`: **60** `Name:` entries; `grep -oE 'Schema:\s*"[a-z]+"' | sort |
  uniq -c` returns exactly `57 public` + `3 directus` and **no third schema** — so "60 / 57 / 3, and
  `directus` is the only second *physical* schema" is verified as a predicate, not just a sum.
  `registry.go:183-195` is precisely the jobsimulation transition-alias comment block, ending
  *"`jobsimulation` stays in rewriteSchemas until M710"*, and `:196` shows the alias mechanism in the
  form the doc describes (`{Name: "jobsimulation.sessions", Schema: "public", RealName:
  "job_simulation_sessions"}`).
- **ENUMERATED — the `/ask` route table.** `internal/web/backend/backend.go:171-188` registers
  exactly, and only, the eleven routes the doc lists: `POST /stream`, `GET`/`POST /conversations`,
  `GET /conversations/:id`, `GET /conversations/:id/stream`, `DELETE /conversations/:id`,
  `POST /messages/:messageId/feedback`, and the four `admin/auto-rules` routes. The group carries
  `cors` + `authnEcho` middleware, and the comment at `:168-170` states the OpenAPI-validation skip
  and its SSE reason.
- **ENUMERATED — the fatal-Bedrock claim, including the negative half.** `main.go:467-471` is the
  exact five-line snippet the doc reproduces. `awk 'NR>=229 && NR<=467 && /^func /'` over `main.go`
  returns **only `229: func main() {`** — so the "no other `func` declaration falls between" claim is
  established by enumeration, not assumed. The construct anchors after it resolve: `:1295`
  `mux := http.NewServeMux()`, `:1361` the `// Meta HTTP Server (healthcheck, _meta, _asynq)` header
  immediately above `metaServer := meta.NewServer(...)`. And the log-string uniqueness:
  `git grep -c "bedrock client unavailable; talk-to-data disabled" HEAD -- '*.go'` → `main.go:1`,
  i.e. **exactly once**. `bedrock.go:161` is `func NewBedrockClient` and its **only** error return
  is `config.LoadDefaultConfig` at `:165-168` — the "how narrow the path is" paragraph is exact.
- **ENUMERATED — the test counts** (`grep -cE '^func Test'`): `sandbox_test.go` **49**,
  `executor_test.go` **13**, `prompt_test.go` **10**, `followups_test.go` **14** — all four exact.
  `sandbox.go` really declares `ValidateSQL` (`:163`), `BuildCTEs` (`:227`), `WrapQuery` (`:363`);
  `registry.go:15-20` declares `StrategyDirect/Indirect/Global`; `profile_histories` is at `:118`
  with the self-scoping note at `:54`; the three tool names `query_postgres` / `render_chart` /
  `course_builder` all exist. `OrgCheckFeaturePermission` is called per-turn at `handler.go:718`.
- **ENUMERATED — the shipped-version claims.** `git log -1 v1.267.0` = 2026-05-07; `v1.340.0` =
  2026-07-17. Both exact.

### `alignment_testing.md`

- **ENUMERATED — the five DNAs' gene and capability counts, computed from the JSON rather than
  re-added from the doc.** `clerk-2.6.0` 14 caps / **27** genes · `clerk-js-5` 6 / **9** ·
  `clerk-multi-1` 5 / **9** · `clerk-deploy-1` 3 / **7** · `clerk-express-1` 5 / **13**. Every one
  matches the score table at `:261-265` and the M2c prose at `:359`.
- **ENUMERATED — the `consumed_surface` claim, which is the one this section exists to make.**
  Counted per DNA: `clerk-2.6.0` = **15** entries; the other four = **0**. So *"Only `clerk-2.6.0`
  declares a `consumed_surface` today"* is verified as a set property. Within those 15,
  `GET /v1/users/{userID}` (consumer *"next-web @clerk/nextjs currentUser() — SSR, every
  authenticated render"*) maps to capability `GetUser` — the M218 fix — and the `covered_by` example
  the doc quotes, `clerk-express-1:ClerkClientBAPI/get-organization`, is present verbatim.
- **RE-DERIVED — the 97.2% figure at `:261`/`:269`, and the SET behind it.** Enumerated the 27 genes
  with their derived weights (critical 3 / standard 2 / optional 1, no explicit `weight` overrides):
  total weight **71**. `MembershipOrgIdentity/real-org-eid` is a **standard** gene, weight 2 —
  `(71−2)/71 = 97.183%` → the published **97.2%**, at 26-of-27. Both the arithmetic and the gene it
  ranges over check out.
- **RE-DERIVED — the toy worked example at `:300-316`.** `examples/toy/dna.json` has 2 capabilities
  (`Add` critical, `Greet` standard) × 3 variants = **6 genes**, total weight 3·3 + 2·3 = 15. The one
  divergence is `Greet/padded-name` (weight 2): `13/15 = 86.667%` → **86.7%**, critical `3/3 = 100%`,
  `5/6 genes aligned`, `Add 3/3`, `Greet 2/3` — every number in the pasted transcript. The values are
  real too: `golden/Greet/padded-name.json` is `"Hello, El Nino!"`, and `mirror.go:28-30` returns
  `"Hello, " + name + "!"` unnormalized against input `"  El  Nino  "`. `alignment_test.go:21-22`
  sets `gateOverall = 80.0` / `gateCritical = 100.0`, as stated. The doc's inline DNA example
  (`:78-96`) matches the real file with only descriptions elided.
- **ENUMERATED — the layout and "no `scripts/`" claims.**
  `git ls-tree --name-only HEAD alignment/` = `README.md cmd examples go.mod internal` — exactly the
  five the doc names at `:329-330`, and **no `scripts/`**. `clerkenstein/alignment/cmd/` holds
  exactly `clerkrun deployrun expressrun jsfapirun multirun`; `clerkenstein/alignment/dna/` holds
  exactly the five DNA files. `alignment/go.mod` is `module anthropos.dev/alignment` with no
  `require` block (stdlib-only, as claimed).
- **ENUMERATED — the CI-inertness passage (`:335-351`), including its negative half.**
  `git ls-tree --name-only HEAD .github/workflows/` at the repo root returns **nothing**, and
  `git ls-files | grep workflows/` returns exactly one path:
  `clerkenstein/.github/workflows/alignment.yml`. Its `:10-11` reads verbatim *"as a subdir workflow
  under clerkenstein/.github (not at the monorepo root), this is currently inert in the
  rosetta-extensions monorepo; it is kept illustrative of the per-mirror gate shape."* Both halves of
  the twice-corrected claim ("exists and is tracked" / "never runs") are true.
- **ENUMERATED — the exit-code split.** `alignment/cmd/alignctl/run.go:133-136` is the `const` block
  declaring `ExitRegressed = 2` / `ExitUnmeasurable = 3`; `:139-153` is `func unmeasurable`, whose
  banner reads *"UNMEASURABLE — the runner could not execute. THIS IS NOT A PASSING SCORE."* and
  *"Do NOT record this run as a pass"*. `gate.sh:5-11` documents the same 0/2/3 contract.
  `datadna`'s package doc (`stack-seeding/cmd/datadna/main.go:21`) gives `diff: 0 = no drift, 1 =
  schema moved (drift found), 3 = usage error` — matching `:438`'s *"0 none / 1 moved / 3 usage"* and
  `drift-check.sh`'s own `exit 1` (drift) / `exit 3` (usage) / `exit 0`.
- **ENUMERATED — the coverage-check semantics (`:198-201`, `:252-254`).** `dna.go`'s `dnaCoverage`
  loads, runs `Validate()` first ("*a DNA that names a nonexistent capability is a coverage LIE, not
  a warning*", exit 2), then on `len(d.ConsumedSurface) == 0` prints `NO COVERAGE CLAIM` and returns
  **0 only under `--if-declared`**, else **2**; a declared-but-uncovered endpoint returns **2**
  unconditionally. `run.go:38` calls `d.Validate()` before any scoring. `dna.go:20-26` is the four
  `case` labels `list|diff|validate|coverage`, matching the Layout line's `(dna.go:20-26)`.
- **ENUMERATED — the zero-critical guard (`:167-174`) and the DNA contract (`:108-112`).**
  `internal/dna/dna.go:21` `idPattern = ^[A-Za-z0-9][A-Za-z0-9_-]*$`; `:24` `maxWeight = 1_000_000`;
  `:279-281` rejects a DNA with capabilities and zero critical genes, with the vacuous-100% rationale
  in-comment; `internal/compare/compare.go:43` carries `CriticalGenes int json:"critical_genes"` and
  `:57` is `if minCritical > 0 && r.CriticalGenes == 0 { … }` — both guards exist, on both sides,
  exactly as described. Criticality weights 3/2/1 are `Criticality.Weight()` at `dna.go:38-49`.
- **ENUMERATED — the snapshot dimension.** `stack-seeding/dna/snapshot.go:62` is
  `var validSnapshotOperators` with exactly **six** members
  (`snapshot-row-count`, `-structural`, `-referential`, `-embedding-dim`, `-public-only`,
  `-cross-surface-closure`) — the count *and* the names. `data-dna.json:410` is the taxonomy
  surface's operator list, **five** members, all but `cross-surface-closure`, under
  `status: snapshot-seeded-m9b`; `:439` is the directus/`simulations` surface's, **five** members,
  no `embedding-dim`, with `cross-surface-closure`, under `snapshot-seeded-m10`. Across all **10**
  surfaces in the file **no status is `waived`**, so *"NOTHING is left waived → 100% coverage"*
  holds, and `Coverage()` really does count `IsSeeded() || IsSnapshotSeeded()` over a non-waived
  denominator. `CapturedFromManifest` (`datadna/main.go:312`) and `NewPgFidelityProbe` (`:331`) are
  wired into `measure-snapshot` as described.
- **ENUMERATED — the M218 D15 gene set (`:219-224`).** Searched all five DNAs: the three
  `universal-user` variants are `Me/universal-user` (clerk-js-5), `DeployIdentity/universal-user`
  (clerk-deploy-1), `ExtractIdentity/universal-user` (clerk-express-1) — exactly the three
  capabilities named — and the two-sided fix is present as `GetUser/hero-a` + `GetUser/hero-b` in
  `clerk-2.6.0`.
- **ENUMERATED — `gate.sh`'s defaults (`:328-332`).** `RUNNER_PKG` default `./cmd/clerkrun`, `DNA`
  default `dna/clerk-2.6.0.json`, `ALIGN_DIR` default `../../alignment`, thresholds 95/100 —
  all four documented in `gate.sh:14-22`. `:69` is the `--if-declared` coverage call.
- **ENUMERATED — "rosetta ships no executable alignment code."** `git ls-files` over the rosetta repo
  returns **4** `.go|.sh|.py` files in total, all milestone-evidence scripts under `knowledge/plan/`
  (I read no content — filenames only, from a whole-repo extension filter); `corpus/` contains none,
  and `.claude/skills/` contains `align-dna` and `align-run` as claimed.

### `intelligence.md`

- **ENUMERATED — the archival claim.** Platform `fdfa189` exists, dated 2026-04-17, subject
  *"chore: remove intelligence service from local dev orchestration"* — matching both the sha and the
  quoted message. `git -C stack-demo/platform grep -in "intelligence" HEAD` returns **nothing** (rc
  read explicitly; the same pipeline returns hits for other tokens in the same session), so there is
  no compose service, no `repos.yml` entry, and no `depends_on` naming it — the file's three
  negative claims all hold. The `public`/`skiller` schema framing and the skiller→app note are
  consistent with the fenced map's `skiller` row.

---

## What I could not settle

- **GitHub archive state** for `graphql-wundergraph` (asserted ARCHIVED 2026-07-30), `skiller`
  (2026-07-01), `skillpath` (2026-07-31), `intelligence` (2026-04-02) and `chronos` (asserted **NOT**
  archived, last push 2026-04-23) — `platform-migration-status.md:96-100` and
  `intelligence.md`. Archive state lives only in the GitHub org API, never in git objects; I made no
  network calls. Settled by `gh api repos/anthropos-work/<repo> --jq .archived`. (The map's own
  `jobsimulation` row already models the right treatment of this class.)
- **The §3 census scalars** — *"93 repositories … 4 in `repos.yml` … 46 named by no corpus document"*
  and the 18-row unarchived/pushed-since-2025 table (`platform-migration-status.md:126-158`). The 4
  I re-derived from `repos.yml`; the 93 and the 46 are GitHub-org-API facts dated 2026-08-01 and are
  not derivable offline. Settled by re-running the org listing.
- **`storage`'s M903 plan-status claim** (`platform-migration-status.md:92`) — *"the plan text itself
  lives in `app/knowledge/plan/releases/09.00-support-in-app/m903-s3-custody/` … self-declares
  `status: planned`"*. That path is under `knowledge/plan/**`, which HARD BAR 1 forbids me to read,
  list, or grep; I made no attempt. Everything *else* in that sentence's neighbourhood is verified
  (the `storage.tf:22-25` supersession text and `d3e6d32`'s *"M903 never ran."*), so the claim is
  corroborated from the storage repo even though its cited artifact is out of my reach. Settled by a
  seat without that bar, or by reading it in the `app` clone directly.
- **A genuine grading-rule ambiguity I declined to book, stated so the adjudicator can rule.** Four
  ground-truth clones are checked out **behind** `origin/main`, and two of my files make claims that
  are **true at `origin/main` and false at the checkout**:
  - `platform-migration-status.md:93` (messenger row) — *"`messenger/terraform/main.tf:29`
    `service_desired_count = 0` in a module that is otherwise intact (121 lines) … the reason
    in-comment (`:19-25`) … the image and task definition stay declared as the rollback path
    (`:27-28`)"*. At `origin/main` `e9421c68` every one of those is **exact**. At the checkout
    `fa47850d` the file is **111 lines** and `:19` is `service_desired_count = 1`. The cell names no
    messenger ref, so the frozen instrument's fallback ("grade against the checkout") would make it
    false; the document's own banner ("Re-measured 2026-08-05 against platform origin HEAD
    `0c91421`"), the platform commits it cites (all 2026-08-05), and the corpus fences' `CITE_REF=auto`
    ladder (origin/main first) all point at `e9421c68`. The scale-to-zero commit is `5dd9f80`
    (2026-08-04), inside the window the map claims to have measured.
  - `platform-migration-status.md:92` (storage row) — same shape; there the cell *does* name
    `9f8cb53`, so the named-ref rule settles it cleanly in the corpus's favour.

    I judged these to be **staleness in the reading apparatus, not corpus defects**, and booking them
    would be precisely the instrument-manufactured false positive §5 rule 45 records. Flagging rather
    than silently deciding.
- **Two historical/measured-once claims with no re-derivable artifact:** `jobsimulation.md:87`'s role
  narrative and `alignment_testing.md:447`'s *"against the M7a-seeded `demo-1`, `measure` reports
  100% / Critical 100% across the 4 seeded surfaces"* — the surfaces and operators are verified in
  `data-dna.json`, but the live measurement needs a running `demo-1`. Likewise
  `platform-migration-status.md:110`'s taxonomy figures (≥42,790 / ≥22,470) need DB access; the
  passage correctly points at the fence that owns them.
- **`intelligence.md:18`'s port/health-endpoint detail** (*":8080, PORT override, only `/_meta`,
  5-minute async ticker"*) — the `intelligence` repo has never been in the clone set, so no ref
  settles it. Settled by cloning `anthropos-work/intelligence` and reading `cmd/`.
