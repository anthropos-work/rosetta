# Seat A — reading #28

**Ground truth read at (re-verified at open, per the ADDENDUM):** app `ad9f3c49` · platform `0c91421d` ·
next-web-app `8297c684` · sentinel `f2c46190` · studio-desk `41ee3575` · ant-academy `22df69dd` ·
storage `4ce8ece5` · messenger `fa47850d` · cms `ca50c817` · graphql-wundergraph `60c229f3` ·
roadrunner `87d8d443` · jobsimulation `462343b0` · **nested** `stack-demo/app/studio` `aeec036a` (own
checkout, grepped at its own ref) · **rext consumption clone** `stack-demo/rosetta-extensions`
`09d06070` · **rext authoring copy** `.agentspace/rosetta-extensions` `680e8529`.

Every `rosetta-extensions` claim in my file set is a claim about **what the tooling does on a stack**,
so I graded all of them against the **pinned per-stack clone `09d06070`** (ADDENDUM / §5 rule 45). I
also confirmed the prior-pin anchors at `ab81527a` in that same clone.

**Instrument note (§5 rule 1/2).** Two searches in this pass returned a false zero and were caught by a
positive control before anything was written down: (a) `git show "$ref:path"` in zsh — `$ref:t` is a
history modifier, so the ref/path silently fused and every `git show` failed while `2>/dev/null`
swallowed the error; (b) `git grep -E "\bNAME\b"` returns **0 for every name** on this platform (BSD ERE
has no `\b`) — `-P` (or `-w`) is required. Both were re-run under control before any count below.

## Positive controls

| file | `wc -l` | read in full? |
|---|---|---|
| `corpus/architecture/external_services.md` | 883 | yes |
| `corpus/architecture/ai_architecture.md` | 310 | yes |
| `corpus/services/coursebuilder.md` | 161 | yes |
| `corpus/services/clerk-integration.md` | 128 | yes |
| `corpus/architecture/README.md` | 38 | yes |
| *(total)* | **1520** | — |

Also read in full as instructed: `corpus/ops/platform-alignment.md` §5 (lines 249–1668).

---

## Blockers

### B1 — `ai_architecture.md:34` cites a doc-comment line as the Mistral OCR client site; its twin in `external_services.md` says in so many words that this anchor is wrong

- **Anchor:** `corpus/architecture/ai_architecture.md:34` —
  > "*Every* use of it in `app` is **OCR**, never generation: Go-side in the cms domain
  > (`internal/cms/studio/markdownManager.go:19`, `studioManager.go:583`)…"

- **Why it is false:** The block names no `app` ref (the only ref in it, `aeec036a`, scopes the
  *studio* half), so it grades at the checkout `ad9f3c49`. At `ad9f3c49`,
  `app/internal/cms/studio/markdownManager.go:19` is the **middle of a doc comment**:

  ```
  17  // NewMarkdownManager builds a manager that OCRs documents with aiKey.
  18  //
  19  // It used to take aiKey and then IGNORE it, reading os.Getenv("MISTRAL_API_KEY")
  ...
  29  func NewMarkdownManager(aiKey string) (*MarkdownManager, error) {
  30      return &MarkdownManager{ocr: mistralocr.New(aiKey)}, nil
  ```

  The Mistral client is constructed at **`:30`**. `:19` *was* correct at `app` `b948604`
  (`ai, err := mistral.NewMistral(nil, os.Getenv("MISTRAL_API_KEY"))`), and the fold to
  `internal/cms/studio/mistralocr` moved it — I confirmed `NewMistral` is **0 hits repo-wide** at
  `ad9f3c49` (`git grep -c` rc=1; positive control `mistralocr` returns 5 files).

  It is also a straight **corpus self-contradiction**: `external_services.md:565` states, of this exact
  citation, *"the `:19` this row used to cite is a **doc-comment** line, not code"*, and
  `external_services.md:598` cites `markdownManager.go:30`. This is the cross-file half-repair class
  (§5 rule 19) — one twin fixed, the other left standing.

- **Confidence:** high

---

### B2 — `ai_architecture.md:87` "Model Families" omits the two Anthropic models the platform actually pins today (Sonnet 4.6, Opus 4.8), while its sibling OpenAI row is an exact enumeration

- **Anchor:** `corpus/architecture/ai_architecture.md:87` —
  > "| **Anthropic (Bedrock EU + Direct US)** | Claude 4.5 Sonnet, Claude 4 Sonnet, Claude 3.7 Sonnet,
  > Claude 3.5 Sonnet | Bedrock `eu-west-1` … for **Course Builder** it is the selector …"

- **Why it is false:** I enumerated both model const blocks in `app` at `ad9f3c49`.

  The **OpenAI row is exact**: `internal/ai/openai/config.go:10-25` declares exactly the eleven the row
  lists — `GPT4Dot1`, `GPT4Dot1Mini`, `O3`, `O4Mini`, `GPT5`, `GPT5Mini`, `GPT5Nano`, `GPT5_1`,
  `GPT5_2`, `GPT5_4`, `GPT5_4_Mini`. So the table's construction rule is "enumerate the constants."

  The **Anthropic row is one short of its own const block and misses the production defaults**.
  `internal/ai/anthropic/completion.go:20-30` declares **six** constants across **five** families, the
  newest being `:29 Anthropic46SonnetAWS20251126 ai.Model = "eu.anthropic.claude-sonnet-4-6"` — **Claude
  Sonnet 4.6, absent from the row.** It is not a dormant constant: it is the pinned default of three
  separate `app` packages —
  `internal/askengine/bedrock.go:25 DefaultModelID = "eu.anthropic.claude-sonnet-4-6"`,
  `internal/jobsimulation/agent/report_agent.go:31 defaultAgentModel = "eu.anthropic.claude-sonnet-4-6"`,
  and `internal/coursebuilder/bedrock.go:29 DefaultGraderModelID = "eu.anthropic.claude-sonnet-4-6"`.
  **Claude Opus 4.8** is missing too — `internal/coursebuilder/bedrock.go:23 DefaultAuthorModelID =
  "eu.anthropic.claude-opus-4-8"` — and the row's *own* Default-client cell names Course Builder as the
  reason the Direct-US door exists. Studio-Room's Anthropic arm adds a third gap:
  `app/studio/services/ai.py:630-632` declares its approved set as *"Claude 4.5+ tiers (Haiku 4.5,
  Sonnet 4.6, Opus 4.7)"* with `claude-opus-4-7` priced at `:659` — none of the three appear either.

  The same corpus names the missing models elsewhere: `external_services.md:564` and
  `coursebuilder.md:49,51` both give `eu.anthropic.claude-opus-4-8` / `eu.anthropic.claude-sonnet-4-6`.
  A reader consulting the document `coursebuilder.md:161` and `architecture/README.md:23` point at as
  *the* "model inventory" would conclude the platform's newest Anthropic model is Sonnet 4.5.

- **Confidence:** medium-high. Hesitation: it is an *omitted list member*, which some grading rules
  class as MINOR. I book it because the omitted members are the current production defaults in three
  packages, because the sibling row on the same table is a complete enumeration (so the omission is not
  a stated editorial cut), and because the row's own prose points at the consumer whose models are
  missing.

---

### B3 — `external_services.md:723` says the EU/US split is "not on the agent name" — refuted by the very line the same sentence cites

- **Anchor:** `corpus/architecture/external_services.md:723` —
  > "There is no `anthropos-agent-eu` — the name appears nowhere in the platform, and the eu/us split
  > lives on the **endpoint** (`azure-eu` / `azure-us`), not on the agent name."

- **Why it is false:** `app/internal/jobsimulation/calls/livekit.go` @ `ad9f3c49` (the passage names no
  `app` ref):

  ```
  119  if *location == LocationEu {
  120      agentName = "anthropos-agent"
  121      // Randomly select an agent name from euAgentNames
  122      randIdx := rand.Intn(len(euAgentEndpoints))
  123      agentEndpoint = euAgentEndpoints[randIdx]
  124  } else {
  125      // we use anthropos-agent-us and azure-us for the us location
  126      agentName = fmt.Sprintf("anthropos-agent-%s", *location)
  127      agentEndpoint = fmt.Sprintf("azure-%s", *location)
  ```

  The agent **name** is `anthropos-agent` for EU and `anthropos-agent-us` for US — the split is on the
  name as well as the endpoint. The same sentence says so eleven words earlier ("the US one is suffixed
  `anthropos-agent-us` (`:126`)"), so this is a **one-sentence self-contradiction** as well as a false
  claim against the cited line.

  The parenthetical is also wrong about the endpoint set: EU does **not** resolve to `azure-eu`, it
  resolves to a *random* member of `euAgentEndpoints = {"azure-eu", "azure-eu-fr"}` (`:101-104`,
  `:122-123`). `azure-eu-fr` is a live EU endpoint this corpus names nowhere in this passage.

- **Confidence:** medium. Hesitation: a charitable reading is "there is no *symmetric* `-eu` suffix",
  which is true. But the words assert the split is *not* on the name, and this sentence is itself a
  correction ("Corrected M257x iter-49"), i.e. §5 rule 18's highest-risk text.

---

### B4 — `ai_architecture.md:219` "all it does is swap the dispatched endpoint" — the flag arm also overrides the agent name

- **Anchor:** `corpus/architecture/ai_architecture.md:216-220` —
  > "The flag is read **inside** `CreateAgentDispatch` … (`calls/livekit.go:131-135` read, `:140-144`
  > effect), and **all it does is swap the dispatched endpoint to `openai-hosted`**."

- **Why it is false:** the cited effect block does two things, not one —
  `app/internal/jobsimulation/calls/livekit.go:140-144` @ `ad9f3c49`:

  ```
  140  if isRealtimeOpenaiFlagEnabled {
  141      // if the feature flag is enabled, use the livekit with openai key and not azure key
  142      agentName = "anthropos-agent"
  143      agentEndpoint = "openai-hosted"
  144  }
  ```

  `:142` **resets the agent name**. On a US-located session — where `:126` had just set
  `anthropos-agent-us` — flipping the flag silently re-routes the dispatch to the bare `anthropos-agent`
  agent as well as to the `openai-hosted` endpoint. Anyone debugging "which agent picked this call up"
  from this paragraph would look at the wrong agent. The residency conclusion the paragraph draws is
  unaffected; the completeness claim ("all it does") is not true.

- **Confidence:** medium. Hesitation: the load-bearing residency point is correct and the miss is one
  assignment. I book it because "all it does is X" is an explicit completeness assertion about a block
  the sentence cites by line, and because the flag's second effect is exactly the fact B3 shows the
  corpus is already confused about.

---

### B5 — `coursebuilder.md:48`: two of the three terraform anchors resolve at `b948604` but name unrelated constructs at the checkout, in a sentence that carries no ref

- **Anchor:** `corpus/services/coursebuilder.md:48` —
  > "**In production the key is required** — `terraform/variables.tf:635-638` declares it `sensitive`
  > with no default, `ssm.tf:328-334` creates the parameter and `main.tf:555` injects it — so the
  > shipped path is the direct API."

- **Why it is false:** the sentence names no ref of its own. The bullet's only pin (`@ app b948604
  v1.366.0`) sits one sentence upstream, attached to the `main.go:770` claim — and §5 rule 33 is
  explicit that "a pin's scope is the claim's own block — … a wrapped sentence in prose", so this
  sentence grades at the checkout `ad9f3c49`. Measured at both refs:

  | anchor | at `b948604` | at `ad9f3c49` (checkout) |
  |---|---|---|
  | `terraform/variables.tf:635-638` | `variable "anthropic_api_key" { … sensitive = true }` ✔ | a **cms-in-app comment block** ("the Studio AZURE_/OPENAI_/ANTHROPIC_ keys+endpoints are read by the Python studio/gen.py subprocess"). The variable is at **`:759`** ✘ |
  | `terraform/ssm.tf:328-334` | `resource "aws_ssm_parameter" "anthropic_api_key"` ✔ | identical, `:328` ✔ |
  | `terraform/main.tf:555` | `"name": "ANTHROPIC_API_KEY"` ✔ | `"name": "DIRECTUS_BASE_ADDR"` — the ANTHROPIC_API_KEY injection is at **`:757`** ✘ |

  The two Go anchors in the same bullet (`bedrock.go:98-104`, `:105-114`) resolve **identically at both
  refs**, so a reader has no signal that only the terraform half is dated. A reader opening
  `main.tf:555` at HEAD lands on a Directus variable and would read the corpus as wrong about the whole
  production-key claim (which is in fact true — the variable, parameter and injection all exist).

- **Confidence:** low. Hesitation stated plainly: the most likely authorial intent is that the bullet's
  `b948604` pin covers all five anchors, and under that reading all five verify. I book it because
  rule 33 was derived precisely to close the "the pin crosses a row/cell boundary" hole, because the
  same file re-anchors other sections explicitly at `ad9f3c49` (`:77-78`, `:147`) and so demonstrably
  distinguishes refs per section, and because the briefing resolves this doubt upward.

---

## Minors

- `external_services.md:455` — cites `graphql-wundergraph/CLAUDE.md:39` and presents as a verbatim quote
  *"Since cms-in-app the platform compose `graphql` service builds from the **production** Dockerfile"*.
  That exact string is at **`:33`** (the "Platform compose" table row) @ `60c229f3`; `:39` asserts the
  same thing in different words ("Since the compose stack builds from the production Dockerfile…"). The
  substantive point — that the archived repo's own doc contradicts the section — holds.
- `ai_architecture.md:60` — cites `external_services.md:619-629` as "item 4 of the four live EU-exit
  routes". Item 4 actually spans `external_services.md:622-632`; `:619-621` is the tail of item 3.
  Intra-corpus line drift (§5 rule 34).
- `ai_architecture.md:240` — "the enum is `…/jobsimulation.go:983-990`". The `AIModel` const block at
  `ad9f3c49` is `:979-991` (11 members); `:983-990` covers 8 of them (it does contain all three examples
  the sentence gives, which is why this is cosmetic).
- `ai_architecture.md:281` — "`calculateSkillScore` (`v3/validator/skills.go:53-64`)". The function is
  `:53-62`; `:64` is the start of `func (s skillsValidator) run`.
- `external_services.md:849-852` — the bash comment block opens `up (docker-compose.yml:101-109 …` and
  never closes the parenthesis before the fenced block ends. Cosmetic; every fact inside it verifies.
- `external_services.md:14` — the PM summary still reads "**GraphQL/Wundergraph** - Unifies all backend
  services into a single API", which the banner three lines above retracts (one subgraph since
  `915da06`). The row does carry a "Prod-only since platform `2adcf71` — see the two-state note"
  pointer, so no factual consequence.
- `clerk-integration.md:99` — "gated on the `STUDIO_ACCESS_ROLES` set — `org:admin` **and**
  `content_creator`". Measured at studio-desk `41ee3575`, `src/index.ts:96` and
  `app/services/userService.ts:16` both declare **four** members: `['admin', 'org:admin',
  'content_creator', 'org:content_creator']`. Both role *kinds* are named, so this is an incomplete
  enumeration rather than a wrong one — but the doc elsewhere (`:21`, `:65`) makes the bare-vs-prefixed
  distinction load-bearing, so listing one form of each is worth tightening.
- `ai_architecture.md:159` — "(skiller migrations `20260417103036` and `20260417120309`)". No migration
  with either stamp exists under `app/terraform/migrations/` at `ad9f3c49`. The claim is explicitly
  about the *skiller* repo, which is not in the clone set, so this is not gradeable rather than false —
  recorded here so the next pass does not spend budget on it.

---

## Positively cleared (audited zeros)

**ENUMERATED** (predicate re-derived from source independently, cardinalities compared):

- `external_services.md:668-673` — the four AI env names in `app` @ `5ba17044`, `*.go` only. Re-derived
  with `git grep -P -o "\bNAME\b"`: **5 / 26 / 13 / 13**, exactly as published; `OPENAI_ORG_ID`,
  `AZURE_OPENAI_ENDPOINT`, `AZURE_OPENAI_DEPLOYMENT` = **0**; `OPENAI_API_KEY` = **2**, and both are the
  studio subprocess remap (`internal/cms/studio/studioManager.go:1049`, `:1067`) exactly as claimed.
  (Covers `stack-demo/app` tracked `*.go` at that ref; `stack-demo/app/studio` holds **0** `.go` files,
  so the nested-repo hole does not apply to this predicate.)
- `external_services.md:373-386` — the whole subgraph-count table. Read straight off
  `supergraph-config-prod.yaml` at each sha in `graphql-wundergraph` `60c229f3`: `749dc86~1`→**5**
  (backend, skiller, jobsimulation, cms, skillpath), `749dc86` (2026-06-24)→**4**, `7c17e63`
  (2026-07-21)→**3**, `915da06` (2026-07-29)→**1**. `git show --name-status 915da06` marks **both**
  `schemas/cms.graphqls` and `schemas/jobsimulation.graphqls` `D`, and that commit's subject really does
  read *"supergraph 2→1"* against a tree listing three. `subgraphs.conf` = `BACKEND=v1.360.0`;
  `schemas/` holds `backend.graphqls` alone. **cms-in-app was the 3→1 step**, confirmed.
- `external_services.md:387` — the no-subscriptions claim, both halves. (a) `sse|subscription` returns
  **0** across all three `supergraph-config-*.yaml` (positive control: `backend` matches all three);
  `git log -S 'protocol: "ws"'` on **mainline** returns **0** while `sse_post` returns **5** — exactly
  as published (with `--all` they are 1 and 7, and the 1 is `bba862f`, which
  `merge-base --is-ancestor … HEAD` rejects rc=1 and `branch -a --contains` places on
  `remotes/origin/feat/use-web-socket` alone). (b) `schemas/backend.graphqls` has `type Mutation` at
  `:4053`, `type Query` at `:4912`, **no** `type Subscription`, and exactly **6** `Subscription` hits,
  all Stripe/plan. Extended the check to `backend` itself: **0** `type Subscription` in
  `app/internal/web/backend/graphql/graph/schemas/*` at `ad9f3c49` (positive control matched).
- `external_services.md:640` / `:619-620` — "all 30 `*_AI_*_MODEL` lines pin `azure`". Enumerated at
  `stack-demo/app/studio` **`aeec036a`** (the nested repo's own ref — invisible to `git grep` at the
  `app` ref): 10 + 10 + 10 = **30** lines across the three `configs/*.ini`, **0** selecting
  `openai`/`anthropic`.
- `ai_architecture.md:125-131` + `:135-140` — the slot table. `production_config.ini:26-36` and
  `development_config.ini:26-36` are **byte-identical** (`diff` empty), stable == experimental, values
  as tabled. `config_template.ini:39` is `EXECUTION_AI_STABLE_MODEL = azure, gpt-4o, none` and `:40` is
  `CREATIVE_AI_STABLE_MODEL = azure, gpt-4o, none`; `gpt-5.2` appears in **no** config, only at
  `services/ai.py:356` and `:508` as pricing rows.
- `ai_architecture.md:86` — the OpenAI Model Families row: exactly the eleven constants at
  `internal/ai/openai/config.go:10-25`, in that order.
- `external_services.md:687-700` + `ai_architecture.md:146,150-155` — the taxonomy figures. Read the
  named manifest directly: `public_only: true`, `predicate: org-null`, `captured_at
  2026-06-29T14:43Z`; `public.skills` **42,790**, `public.job_roles` **22,470**, `public.skill_embeddings`
  **42,790**, `public.job_role_embeddings` **18,919** — every figure exact, including the
  18,919-transcribed-onto-the-role-count diagnosis.
- `coursebuilder.md:77-85` — the SSE wire contract. Enumerated `renderEvent`
  (`internal/web/backend/coursebuilder/handler.go:2604-2772` @ `ad9f3c49`): **15** distinct non-empty
  names at exactly the 15 line anchors published, the `default:` arm re-emitting `text` at `:2770`, plus
  `session` written directly at `:1458` after `beginSSE` = **16**. `case cb.EventCost:` at `:2709`
  returns `"", nil` at `:2717` and the loop skips `writeSSE` when the name is empty at `:1478-1480`.
  Every one of the 18 anchors in this bullet lands on its named construct.
- `clerk-integration.md:37` — "Webhooks (svix, **12** event types)". Enumerated
  `app/internal/clerk/events/events.go:166-235`: `user.{created,updated,deleted}`,
  `organization.{created,deleted,updated}`,
  `organizationInvitation.{accepted,created,revoked}`,
  `organizationMembership.{created,deleted,updated}` = **12**, and exactly the four families `:79` names.
- `clerk-integration.md:41` — "**8** locales". `apps/web/src/providers/Clerk.provider.tsx:9-18` imports
  `deDE, enUS, esES, frFR, itIT, jaJP, nlNL, ptPT` = 8.
- `clerk-integration.md:103` — every version claim, at each clone's own HEAD.
  `apps/{web,hiring,integration}/package.json:10` (and `:9` for integration) all `@clerk/nextjs ^6.39.6`
  @ `8297c684`; `ant-academy code/package.json:52` `^6.39.2` @ `22df69dd`; `apps/maintenance` has a
  `package.json` and declares **no** `@clerk/nextjs`; `apps/mobile/package.json:6` `~2.6.18` vs
  `ant-academy/mobile/package.json:18` `~2.19.36` (thirteen minors apart);
  `app/go.mod:31 @ 5ba17044` = `github.com/clerk/clerk-sdk-go/v2 v2.7.0`.
- `clerk-integration.md:51` — `colony/authn` imports: **129** files in `app` @ `ad9f3c49`, **0** in
  `sentinel` @ `f2c46190` (positive control: `anthropos-work/colony` matches **13** files in sentinel).
  `repos.yml` @ `0c91421d` lists exactly **two** Go repos (`app`, `sentinel`) and four total;
  `repos.yml` @ `0dab54d` listed **four** Go repos (`app`, `sentinel`, `storage`, `messenger`) — the
  parenthetical is right too. `storage` and `messenger` have **0** `clerk` hits at their refs.
- `architecture/README.md:21` — swept **all 15 git trees** in the clone set (including both nested
  `studio` checkouts and both rext clones) for `anthropos-work/authn` in `go.mod`/`go.sum`: **0
  everywhere** (positive control `anthropos-work/colony` = 1 in each of the four Go repos'
  `go.mod`). `github.com/anthropos-work/ai`: **0** in `app` and `sentinel`, **1** each in the frozen
  `cms` and `jobsimulation` husks — exactly as stated. `1e457fa70` is real
  ("refactor(ai): fold the ai library into app as internal/ai", 2026-08-04) and `app/internal/ai/ai.go`
  exists at `ad9f3c49`.
- `external_services.md:136`, `:206-211`, `:219` — the whole `--local-content` re-point claim, graded in
  the **pinned per-stack clone `09d06070`**: `stack-injection/gen_injected_override.py:86` =
  `DIRECTUS_DATA_CONSUMERS = ("cms", "backend")`; `:77-81` is verbatim the inert-key/ROLLBACK
  justification the corpus quotes; `:698-699` is the re-point; `stack-core/gen_override.py:58` is the
  identical tuple; `stack-injection/tests/test_injection.py:1109` is
  `test_backend_the_actual_reader_is_repointed` and its docstring says what the corpus says it says. The
  prior-pin anchors also check: at `ab81527a`, `:84` and `:669-670`. The retired
  `test_only_cms_is_repointed_not_other_services` survives only in a `.agentspace/test-results` XML
  artifact and in checked-in corpus *fixtures*, not in any test source.
- `external_services.md:171-175`, `:197-198`, `:249`, `:304-306`, `:849` — the compose picture at
  platform `0c91421d`. **5** declared services (`sentinel`, `backend`, `studio-desk`, `next-web-app`,
  `gotenberg`) + **2** from `include: common.yml` (`postgresql`, `redis`) = **7** effective; at
  `0dab54d` it was **8/10**. `postgresql`, `redis` and `sentinel` declare **no** `profiles:` key, so the
  `core` selection is exactly those three + `backend` + `gotenberg` = **five containers**.
  `docker-compose.yml:110` and `:183` are the `profiles: [core, backend, all]` lines of `backend` and
  `gotenberg` respectively; `:151`/`:160` and `:119`/`:135` are the build-arg/runtime-env pairs, all
  four `:8082/graphql/query`; `:53` is the single `DIRECTUS_*` variable in the `environment:` block that
  runs `:46-94`; `:101-109` is `depends_on` = redis, postgresql, sentinel; `.env_example:91-92` is
  `DIRECTUS_BASE_ADDR` + `DIRECTUS_TOKEN`.
- `external_services.md:173`, `:828` + `clerk-integration.md:93` — "deleted by `d11a403`, with their
  `repos.yml` entries". `d11a403`'s *subject* names only roadrunner, but the diff removes the
  `jobsimulation`, `cms` **and** `roadrunner` compose blocks and all three `repos.yml` entries in one
  commit. Verified rather than inherited.
- `external_services.md:420-430`, `:434-437`, `:441-455` — the router's compose history.
  `2adcf71^1` = `1e8e7540`; at `1e8e754` the gateway's `depends_on` (`:19-27`) names exactly **four**
  (`backend`, `jobsimulation`, `cms`, `storage`), each `service_started`, unchanged since `a4db680`
  (2026-07-21, "decommission standalone skillpath service"); `"5050:8080"` is the local host mapping
  only. `464dfe3` is **not** an ancestor of `0c91421d` (rc=1) and lives on
  `remotes/origin/feat/cms-in-app` alone. The three build eras verify: `2c85211^` has `context:
  ../graphql-wundergraph` and **no** `dockerfile:` key; `2c85211` adds `dockerfile: Dockerfile.dev`;
  `63d285c` (2024-06-20) really does name the service `wundergraph:`; `b56d731` still has `  graphql:`
  at `:22` behind `profiles: [wundergraph-deprecated]` with `dockerfile:
  graphql-wundergraph/Dockerfile.dev` at `:25`; `360efd4` has no `graphql:` key and `  backend:` at
  `:28` (the published positive control).
- `external_services.md:367`, `:459-473` — router pins and build. `terraform/locals.tf:8 port = 8080`;
  `terraform/main.tf:48-49` maps `local.port` → `local.port`; `config.prod.yaml:5 listen_addr:
  0.0.0.0:8080`; `terraform/main.tf:20 service_desired_count = 1`; image
  `ghcr.io/wundergraph/cosmo/router:0.275.0` and `wgc@0.104.0` in both Dockerfiles. `Dockerfile.dev` has
  exactly **one** schema `COPY` at `:18`, the orphaned fold comment at `:19-20`, and one `awk` at `:23`.
- `external_services.md:175` — the two terraform rows, and they really do point opposite ways.
  `cms/terraform/main.tf:39` @ `ca50c817` is `service_desired_count = 0`; `6efa1d5` (2026-08-04) deletes
  `.github/workflows/build-production.yml` with the subject *"the cms ECR repository is decommissioned
  (M810)"*. For jobsimulation the claim pins `6092c6d2` and is graded there: at `6092c6d2` the
  `module "jobsimulation"` block **is** deleted (the checkout `462343b0` still has it at `:31`, and
  `6092c6d2` is an ancestor of `origin/main` but not of the checkout — the pin is doing real work here).
- `external_services.md:564-567` + `coursebuilder.md:48-53,113-114` — every model/anchor.
  `coursebuilder/bedrock.go:23` = Opus 4.8, `:29` = Sonnet 4.6, `:98-104` = `ModelBackendName`,
  `:105-114` = `newUnderlyingClient` (all four resolve at **both** `b948604` and `ad9f3c49`);
  `askengine/bedrock.go:25` and `report_agent.go:31` = Sonnet 4.6; `imagegen/openai.go:22 defaultModel =
  "gpt-image-2"`; `main.go:770` = the `ModelBackendName()` log line and `main.go:824-826` =
  `NewOpenAIImageClient(os.Getenv("OPENAI_KEY"), …)` at `b948604`.
  `COURSEBUILDER_OPENAI_IMAGE_KEY` survives in exactly **2** places at `ad9f3c49`, both in-repo markdown
  (`internal/coursebuilder/{GO-LIVE-RUNBOOK,README}.md`), **0** in `*.go` — precisely the corpus's claim.
- `external_services.md:581-599` + `ai_architecture.md:22-32` — the AI manager, all anchors at
  `ad9f3c49`: `internal/jobsimulation/ai/ai.go:30-33` (four vendor consts), `:80`
  (`openai.NewOpenAI(openaiKey)`), `:129-141` (`isThrottlingError`, 429 only), `:150-155` and
  `:296-302`/`:326` (the two retry sites setting `vendor = Openai`), `:259-289` (`getClient`),
  `:264-276` (EU default + `flag_use_azure_us` + keep-EU-on-error), `:280-283` (both Anthropic vendors →
  the same Bedrock client). `CreateSpeech` (`:243-257`) really is Azure-only.
- `external_services.md:622-632` + `ai_architecture.md:53-66` — the unset-`ai_vendor` route.
  `internal/cms/directus/collections/jobsimulation.go:905` = `AIVendor *AIVendor` on the **DTO**;
  `:1302-1305` = `aiVendor := simulation.Openai` before the `simulation.Sequence` is built at `:1307`;
  `:1297` = `aiModel := simulation.Gpt5Point1`. Downstream, `simulator/ai/ai.go:58-59` is
  `case simulation.Openai:` → `internalAi.Openai` and `:114-115` is the separate `default:` door. The
  Directus `AIVendor` enum has **5** members (`:969-975`, `azureglobal` at `:971`) against the switch's
  **4** cases (`:58`, `:69`, `:86`, `:102`) — the five-vs-four claim is exact.
- `ai_architecture.md:253-265` — the three simulation defaults and the two Anthropic-arm sub-defaults:
  `simulator/ai/ai.go:20-26` (`GetValidationAIVendorAndModel` → Azure/GPT4Dot1/GPT4Dot1Mini),
  `:65-66`/`:82-83`/`:126-127` (all three `gpt-4.1`), `:98-99` (Claude 3.7 Sonnet AWS),
  `:110-111` (Claude 3.5 Sonnet). Every anchor names its construct.
- `ai_architecture.md:269-288` — the evaluation system. `v3/validator/validator.go:43,60-61,595` are the
  only three `checkerEngines` sites in the v3 validator and none of them **reads** the map for dispatch
  (its three further sites in `basevalidator/criterion.go:34,72,90` are param→field storage, also never
  consulted); the real dispatch is the hardcoded `switch critCheck.Engine` at `criterion.go:126-155`
  with `case check.EngineLlm:` at `:127`; `:428` is `NewLLMBulkChecker(c.logger)`; `:168` runs
  `validateCodeDiff` concurrently with the LLM pass; `:450-475` sets `success` from a pure string
  comparison. `skills.go:75` is `passed/total*100`; `:40-51` is `calculateCompetencyLevelScore` with
  `20` / `100` / `max(0, score*2-100)` and the literal `// TODO fix this formula` at `:49`.
  `internal/skill/skill.go:617-623` is `convertLevelTo100`.
- `coursebuilder.md:35-40,56-59,63-76,107-122,140-152` — the domain facts.
  `internal/data/ent/schema/coursebuildersession.go` exists; `terraform/migrations/20260717151144.sql`
  creates `course_builder_sessions` (`:2`), `credit_transactions` (`:40`) and `organization_credits`
  (`:56`). `run_task.go:49 TypeCoursebuilderRun`, `:197-204` `Queue(QueueCoursebuilderRun)` +
  `MaxRetry(1)` + `Timeout(timeout)` + `Retention(6*time.Hour)`; `worker.go:223-243` `CoursebuilderSrv`
  with `main.go:190 coursebuilderWorkerConcurrency = 20`. `handler.go:3143 func (h *Handler)
  Register(g *echo.Group)` mounts every route named in the doc; `backend.go:229-233` mounts the group
  under `/coursebuilder` behind `cors + EchoAuthnMiddleware + cbGate`. Budget/credits:
  `budget.go:27 DefaultMaxMonthlyCOGSUSD = 500.0`, `:41 DefaultMaxDailyCOGSUSD = 0.0`,
  `rate.go:28 DefaultSessionsPerOrgPerDay = 50`, and `credits/cost.go:86-90` = build **5**, refine
  **1**, translate **1** — note the *doc comment* at `credits/cost.go:29` says "5 credits per refine
  turn" and is stale in `app`'s own source; the corpus follows the compiled map and is right (§5 rule 24
  — I nearly booked this backwards). `git describe --tags`: `ad9f3c49` → `v1.369.0-7-gad9f3c498`,
  `2035f9a4` → `v1.369.0-2-g2035f9a40`, `5ba17044` → `v1.363.2` (2026-07-31). `author.go:112
  defaultAuthorMaxTokens = 32000`.
- `external_services.md:614-617`, `:873` + `ai_architecture.md:51,60,87` — the **in-file** anchors.
  `external_services.md:567` is the Anthropic-Direct row, `:543` is the "When backend services add new
  GraphQL types" heading, `:546` is the struck `docker compose restart graphql` line, `:206-211` is the
  `--local-content` blockquote — all four resolve to the constructs their citers name. (`:619-629` is
  the one that has drifted — Minors.)
- `external_services.md:565` — the studio-side Mistral claims, graded at the nested repo's own ref
  `aeec036a`: `tools/pdf2md.py:24` = `from mistralai import Mistral`; `tools/r3.py:139`, `:190`,
  `:199-206` really do dispatch it as step 2; `git grep -ci mistral` over that tree = **22 lines / 3
  files** (`requirements.txt`, `tools/pdf2md.py`, `tools/r3.py`) — exactly the figure
  `ai_architecture.md:37` publishes. `bedrock|boto3` under `app/studio/`: **0** by both the nested
  `git grep` at `aeec036a` **and** a bare recursive `grep`, with `openai` as positive control.
- `external_services.md:634-642` — the Studio-Room `openai` arm and why it is latent.
  `services/ai.py:704-724` (`get_client`, providers `{openai, azure, anthropic}` at `:706-708`), `:383`
  (`return OpenAI(api_key=self.api_key)` — bare, no Azure endpoint), `config_template.ini:30-31`
  (`OPENAI_ENDPOINT = https://api.openai.com/v1/chat/completions`), `services/ai.py:627-664`
  (`AnthropicProvider`), `gen.py:41-53` (overrides only the six `*_API_KEY`/`*_ENDPOINT` names at
  `:45-48`, never the service selector).
- `clerk-integration.md:21,46,96-101` — `apps/web/src/app/api/metabase/route.ts:35` checks
  `authData.orgRole !== 'admin'` (the **bare** form); `ant-academy mobile/src/components/
  ClerkSignInForm.tsx:49-53` aborts on any non-`complete` status ("Sign-in needs another step that this
  app does not support yet"); `code/proxy.js` uses `clerkMiddleware` (`:264`) with
  `REQUIRE_ORGANIZATION_MEMBERSHIP` → `/no-organization` (`:91`, `:96`, `:131`); studio-desk installs
  `@clerk/clerk-js ^5.52.3` + `@clerk/express ^1.3.47`; `e2e/package.json:17-18` installs
  `@clerk/backend` + `@clerk/testing`.
- `architecture/README.md:13,15` — `stack-core/platform_alignment_guard.py`,
  `stack-core/platform_predicate_guard.py`, `alignment/` and `stack-seeding/dna/` all exist in **both**
  rext trees (`09d06070` and `680e8529`).

**RE-DERIVED** (I re-computed arithmetic the document itself displays — the weakest clearance, listed
separately on purpose):

- `external_services.md:604-643` — "four live + one latent" EU-exit routes. I re-walked the five listed
  items and each has an independently verified mechanism above; I did **not** independently enumerate
  the space of all possible exits, so this is a re-derivation of the document's own list, not a
  census of the predicate.
- `coursebuilder.md:147-148` — "six releases on from `v1.363.2`" (363→369). Arithmetic only; I did not
  enumerate the intervening tags.

---

## What I could not settle

- **`external_services.md:3` — "the repo is ARCHIVED on GitHub (2026-07-30)".** Archive status is GitHub
  metadata; nothing in the clone set carries it. Settled only by `gh repo view
  anthropos-work/graphql-wundergraph --json isArchived`.
- **`external_services.md:175` — the cms prod-side state.** The corpus itself declares this
  **UNMEASURABLE** from our clones (the destruction lands in `infrastructure`, which has never been
  cloned). I confirmed both measured facts it cites, and both really do point opposite ways; I take no
  position on the prod state, as the passage instructs.
- **`ai_architecture.md:192-197` — the five LiveKit agent repos** (`livekit-agent`,
  `livekit-agent-chain`, `livekit-agent-azure-us`, `livekit-agent-azure-eu`,
  `livekit-agent-azure-eu-fr`). None is in the clone set. I can corroborate the *platform side*: the
  dispatch names `anthropos-agent`, `anthropos-agent-chain`, `anthropos-agent-<location>` and the
  endpoints `azure-eu`, `azure-eu-fr`, `azure-us`, `openai-hosted` all exist at
  `calls/livekit.go:101-148`. The repo list needs `gh repo list anthropos-work`.
- **`ai_architecture.md:159` — skiller migrations `20260417103036` / `20260417120309`.** The `skiller`
  repo is decommissioned and not in the clone set; neither stamp exists under
  `app/terraform/migrations/`. The *content* of the claim (dedicated `skill_embeddings` /
  `job_role_embeddings` tables in `public`, IVFFLAT, 1536-dim) is corroborated by
  `app/terraform/migrations/20260615130000_skiller_taxonomy.sql` and by the taxonomy manifest, but the
  two migration stamps themselves are not gradeable here.
- **`coursebuilder.md:129-130` — "~55 test files, ~25 boundary test files".** Approximations with a `~`;
  I did not treat them as assertions.
- **`external_services.md:213-220` — the historical `demo-1` measurement** (96 all-403 lines in
  `backend`'s log on 2026-08-01). A time-scoped observation of a mutable surface; per §5 rule 49 I
  cannot refute or confirm it with a later snapshot, and I did not try. The *code* half of the claim
  (which test replaced which) verifies in the pinned rext clone.
