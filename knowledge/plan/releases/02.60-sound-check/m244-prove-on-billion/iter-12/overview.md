---
iter: 12
milestone: M244
iteration_type: tok
tok_flavor: triggered
status: closed
created: 2026-07-22
---

# iter-12 — triggered tok (consolidate after 5 tiks circling gate b)

## Type
tok (triggered) — the primary metric (gate parts discharged) last moved at iter-06 (2→3) and has stayed **3/8** across iters 07-08-09-10-11 = **five consecutive tiks with zero metric delta** (the skill's literal tok-trigger floor). The re-survey guard's escape requires the metric to have *moved* since the last tik's close; it has not (still 3/8), so the trigger is not stale → author the tok.

## Step 0 — re-survey (mandatory)
Metric re-confirmed **3/8** (a ORG-CLEAN, e serve-reap, g interview alignment — all re-verified LIVE on the fresh m244 seed in iter-11). Gate (b) is live **45/47** (the closest bucket, one fix away). Billion is GREEN at the m244 pin (rext consumption tag → e30df85). The streak holds; not stale.

## Honest framing — the strategy is SOUND, the metric is a coarse-bucket artifact
This is NOT a stalled-strategy tok. TOK-01 ("staged cold billion bring-up → discharge gate parts a–h one-cluster-per-tik") has produced real, load-bearing progress on EVERY one of the five tiks — but the primary metric is **binary per gate**, and iters 07–11 all worked the single hardest bucket (gate b) plus a gate-d measurement + an inherited-carry cleanup:
- iter-07: gate-b voice cells → player-presence-only (denom 49→47).
- iter-08: gate-b interview player-ack → demopatch scoped to isManagerScope.
- iter-09: gate-d measured (falsification: anon /library empty).
- iter-10: inherited reap-17700 carry discharged (cleanup).
- iter-11: **the gate-(h) cold reset-to-seed DONE GREEN at m244** + gates (a)/(e)/(g) re-verified live + the LOAD-BEARING cross-check divergence fixed (49→47, the sweep was unrunnable without it) + gate-b **fully root-caused** (interview-player ack renders ~25 s late; the fix is named).

None flipped a gate BUCKET (gate b only counts at 47/47, and it's at 45/47), so the coarse metric reads a flat 3/8 over real progress. The tok's job here is **consolidation + sharpening**, not revision.

## Distance-to-gate context
3/8. Gate b is 45/47 (one well-understood fix away). Gates c/f/h/d are OPEN but now **unblocked** by the green m244 seed (live verifications from the peer harness; only gate b's fix and gate d's academy demopatch need image work).

## Next-tik direction (run 6, iter-13)
Apply the gate-b fix, then sweep the cheap remaining gates:
1. **iter-13 (tik): land gate (b) 47/47 → 4/8.** Scope the `next-web-interview-flag-container` FETCH demopatch to `isManagerScope` (mirror iter-08's result-gate scope) so the interview PLAYER skips the slow extraction fetch it doesn't need → the ack renders promptly → the existing settle+retry lands both player pairs. Recompute the demopatch sha256 + add a regression test + re-tag/push rext, then re-pin billion + re-bake next-web/hiring + re-sweep (settle+retry) → expect 47/47. Confirm the slow-fetch cause first (a quick probe: does scoping the container gate make the player ack render fast?). Fallback if the container-scope doesn't fix it: a shape-aware settle-robustness (wait-for-`<main>` on into-main shapes; don't break the portal case).
2. **iter-14 (tik): gate (c)** — the 40 live-browser specs (24 stack-verify + 16 Playthroughs) green from the peer against the green seed → 5/8.
3. **iter-15 (tik): gate (f)** — the 3 v2.3 drift-carries burned in live (PROBE-M218-c3 Cosmo cms/Directus 403 re-check + F-M220-4 ant-academy re-run + BURNIN-M221 public-host mechanism) → 6/8.
4. Then **gate (h)** (v2.6 fixes live + p95<5s hero vantages) + **gate (d)** (anon /library ant-academy demopatch) + DEF-M239-01 (ENOSPC loud-build-fail).

## Recorded as
TOK-02 in the milestone-root decisions.md.
