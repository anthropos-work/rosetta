# M244 — Progress

Iterative milestone (the closer). Primary metric: the multi-part exit gate (a–h) met cold on `billion`.

## Running ledger

- iter-01 (tok/bootstrap): TOK-01 authored (staged cold billion bring-up → gate-parts a–h one-per-tik); Phase 0b KB-fidelity YELLOW (denominator=49, spec=40); pre-flight rung zero mapped (billion bare, pin=m243) — see iter-01/progress.md
- iter-02 (tik): Foundation GREEN — re-pinned billion to m243 (rung-zero), gate (a) ORG-CLEAN PASS (23 fixtures/0 tokens live), gate (e) DEF-M226-01 serve-reap CONFIRMED (7→0 ports), cold reset-to-seed BRINGUP_EXIT=0 + fresh green autoverify (12:51Z) + all peer origins serving + 42,790 skills/1,644 sessions/49-pair manifest. Metric 0/8→2/8. — see iter-02/progress.md

- iter-03 (tik/tooling-iter): gate (b) — fixed the content-stories runner's bash-3.2 parse wall (unrunnable on macOS peers) + tailnet cold-render flakiness (settle-budget + per-pair warm retry); 3 rext fixes shipped+tagged+pushed (`sound-check-m244-content-sweep-robustness`→01206e7). Gate (b) 37 cold → **46/49 reliable**; 3 DETERMINISTIC residuals (voice/interview render, full DB content) route to gate (g) + a voice-presence-only decision. closed-fixed-partial. Metric 2/8 (b not green). — see iter-03/progress.md
- iter-04 (tik): gate (g) — LIVE-PROVED the interview report renders **EMPTY** on billion (seeded depth/adoption/sentiment absent behind "View Report"); the plan-section-id alignment assertion would be **RED**. The S-8/S-9 gap is a real defect (not just unproven); intv-voice-pass residual is the same failure. Root-cause surface identified (seed-plan vs flag-demopatch); fix routed forward (0 platform edits). closed-no-lift. Ended on a user-requested pause. — see iter-04/progress.md
- iter-05 (tik): gate (g) FIX — ROOT CAUSE = `directus.simulations_extraction` (the interview PLAN) **never captured by the snapshot** (0 rows on demo) → `interviewExtractionPlan` null → report empty. FIX: added the table to the directus capture surface (`stack-snapshot`, parent-scoped public-only), tested + pushed (rext `e74e563`, tag moved). Plan loaded to the demo → report render 0→520 (plan is load-bearing). NOT green: full section render (behind "Explore Key Moments") + the alignment assertion + the live capture-path exercise routed. **intv-voice-pass/player is a SEPARATE ack-page issue** (gate b stayed 46/49). closed-fixed-partial. — see iter-05/progress.md
- iter-06 (tik): **gate (g) DISCHARGED** — the plan-section-id alignment assertion (`TestInterviewPlanSectionAlignment` + golden `interview-plan-sections.json` + drift-teeth test) ADDED + GREEN + pushed (rext `0dec8ff`, tag → `70d75cb`). It CAUGHT a real defect: intv-voice-fail was pinned to a v1.3-era session (breadth/context_fit/frequency orphans) vs the sim's v1.4 plan. FIX: re-pinned `43a92fc0 → 05dae0f7` (a v1.4-clean genuine FAILED interview) via `content-capture --only` (read-only prod, scrubbed, cleanliness-green); both projection goldens regenerated. Live-confirmed on billion (DB-level): pass clone 0-orphans; the assertion catches the m243-seeded fail clone's live drift, resolved by the re-pin on the cold reset-to-seed. **2/8 → 3/8.** closed-fixed. — see iter-06/progress.md
- iter-07 (tik): **gate (b) advanced** — ground-truthed the billion sweep (46/49; the 3 residuals are ALL player-vantage: 2 voice + 1 intv-ack; managers all land) + dispositioned the 2 Bunny-absent voice cells to **PLAYER-presence-only** (denominator 49→47). Built the per-session mechanism full-stack: contentsession `player_result_unavailable`+reason, the projection `player_presence_only` (manager view kept, not a drop), buildPairs presence-only branch, cockpit partition-by-verdict + withheld-player disclosure, both goldens regenerated. Go module + 21 TS specs + cockpit 159/6 green. rext `1968fd2`, tag → `6f52f0c`. Live 47/47 lands at gate (h). Metric stays 3/8. closed-fixed. — see iter-07/progress.md
- iter-08 (tik): **gate (b) #3 resolved** — root-caused the `intv-voice-pass/player` "no ack text" (mainLen 0) to the M232 `next-web-interview-flag-result` demopatch opening the interview render gate for the PLAYER scope: a PASSED interview (evaluation_status=passed) rendered the player-scope InterviewReport EMPTY, a FAILED one fell through to the ACK. But an interview has no player scored report (the sweep's own `player-interview` shape says the player sees only the ack). FIX: scoped the demo-widen to `isManagerScope` (player → ack pass+fail; manager report / gate g untouched); post_sha256 recomputed + sha-method-verified + a regression test; demopatch(12)+tooling+build(254) green. rext `f46e082`, tag → `8756ec0`. **All 3 gate-(b) residuals now resolved.** Metric stays 3/8 (live 47/47 at gate h). closed-fixed. — see iter-08/progress.md
- iter-09 (tik): **gate (d) MEASURED** (live, anon, billion academy :13077) — `/free` renders **43 real cards** (✓, but 2 Draft chips vs expected 0), anon **`/library` renders an EMPTY grid** (✗, 0 cards, header only). The twin does NOT both-render → gate (d) NOT met. Root cause: the `/library` grid reads a TENANT-FILTERED catalog (`ServerCatalogContext`) that is empty for an anon demo user (`publicSource.js` refuses the FS-catalog import to avoid cross-tenant leakage); `/free` reads a public source. The m244 re-seed will NOT fix it — the fix is an ant-academy demopatch/config to feed the FS catalog to the anon `/library` grid (routed). closed-no-lift (documented falsification). — see iter-09/progress.md
- iter-10 (tik, cleanup): **inherited carry reap-17700 (M239-D13) DISCHARGED** — `test_a_RACED_listener_exits_silently` false-failed on a box with a live demo-1 cockpit on :17700 (test-isolation collision, 0 product/reap.sh defect). Fixed per the D13 recipe: default `_reap_with_stubs` to a guaranteed-free `_free_port()` (never 17700). PROVEN load-bearing — with :17700 held locally the RACED test now passes (would have refused before). test_reap.py 41/41. rext `b38ad75`, tag → `7dbad4b`. 1 of 3 inherited defers landed. closed-fixed. — see iter-10/progress.md
- iter-12 (tok/triggered): **TOK-02 — consolidate + sharpen TOK-01** after 5 no-metric-delta tiks (07–11) all working the gate-b cluster. The strategy is SOUND (real progress every iter); the flat 3/8 is a coarse binary-per-gate artifact (gate b only counts at 47/47, sits at 45/47). Run-6 direction set: iter-13 lands gate b 47/47 (scope the interview container FETCH demopatch to isManagerScope, mirror iter-08; re-bake + re-sweep), then iters 14–15 sweep gates (c)/(f) live, then (h)/(d)/DEF-M239-01. Exit tok-fired. — see iter-12/progress.md
- iter-11 (tik): **the gate-(h) cold reset-to-seed — DONE GREEN at the m244 pin** (re-pin b38ad75 from origin → teardown --purge → `up-injected.sh --public-host` under setsid-nohup → BRINGUP_EXIT=0 + fresh-green autoverify 19:31:10Z + all origins serving; baked SSR map confirms the correct SCOPED iter-08 demopatch). Gates **(a)/(e)/(g) re-verified LIVE** on the fresh seed. Fixed the **LOAD-BEARING** cross-check divergence (49→47; it exit-2'd before the sweep could run) + a defensive EN/IT grader (rext `e30df85`, tag → 0c514e6/e30df85, pushed). Gate (b) live **45/47** — the 2 interview-PLAYER pairs render the ack ~25 s late (probe-confirmed EN ack, mainLen 332 after a long wait) while settle returns early on the nav shell; root-caused + routed to iter-12 (rext/demopatch fix, NOT a platform edit). closed-fixed-partial. Metric stays 3/8. — see iter-11/progress.md
- iter-19 (tok/triggered, run 7): **TOK-03 — HOLD TOK-01/02 + sequence the narrow final push.** The unconditional 3-no-prog floor fired (iter-16/17/18 all flat 5/8 on the binary metric); re-survey confirmed the metric genuinely didn't move BUT the strategy is demonstrably sound (iter-18 = the most productive tik). TOK-01/02 HOLD (coarse-binary-per-gate artifact TOK-02 pre-registered; no revision). Sequenced: gate (h) completion → 6/8 · gate (f) → 7/8 · gate (c) Playthroughs LAST → 8/8 GATE MET · DEF-M239-01. Exit tok-fired; run 8 resumes at gate-h completion. — see iter-19/overview.md
- iter-18 (tik, run 7): **gate (c) stack-verify HALF COMPLETE on billion + a real Bedrock provisioning fix.** Led with talk-to-data-m239 (the decisive probe): RED on billion — flag gate loads but the Bedrock round-trip never streams. Root cause = billion's demo had NO Bedrock creds (values-blind; local secrets HAVE them) — the v2.6 "not all provisioned" defect class. FIXED values-blind (synced 4 Bedrock lines local→billion source+app+platform .env, force-recreated backend) → talk-to-data GREEN (11.2s, live member-count). Then finished the discrete half: FINDING — the "retrofit" is ENV-WIRING not per-spec code (specs already read full-URL base env; localhost defaults only). Built+validated `run-discrete.sh` (rext `6aacc32`, m244 tag moved+pushed). **ALL 10 discrete gate specs GREEN on billion** (persona/m224/talk-to-data/cockpit-overlay/enterprise-surfaces/smoke/verify-members-B/verify-activity-dashboard/render-hiring[1-sim asserted 9/9 candidates]/m220[6/6, incl. the now-passing "KNOWN RED" academy test = iter-15 cross-check]). ⇒ gate-c stack-verify half COMPLETE (coverage iter-16 + 10 discrete). 4/6 gate-h fixes proven as byproducts (talk-to-data/library/cockpit/content-fidelity). Metric stays 5/8 (gate c needs Playthroughs; gate h needs 3 more). closed-fixed. — see iter-18/progress.md
- iter-17 (tik, run 6): **gate (c) discrete specs — 2 driveable ones green + retrofit map.** Ran the remote-capable discrete stack-verify specs on billion: **persona-check PASS** (maya-thriving role-skills/avatar/org all coherent) + **m224-candidate-heroes PASS 3/3** (Cody candidate + Rae recruiter hiring views render real data). FINDING: only 3 of ~13 discrete specs carry a host env; the rest are localhost-only (offset-only: talk-to-data/enterprise-surfaces; no-host: smoke/verify-*/calibrate*/render-hiring/cockpit-overlay/m220) → the gate-(c) discrete half needs a **remote-capability retrofit** (host+scheme env, the M219 pattern) before they can run against the tailnet — routed to run 7. Gate (c) NOT ticked. Metric stays 5/8. closed-fixed-partial. cap-reached (tik 5/5). — see iter-17/progress.md
- iter-16 (tik, run 6): **gate (c) coverage half GREEN on billion.** Recalibrated the STALE `ANT_ACADEMY_HOME_SECTION` marker (iter-15 finding): the literal `AI Academy` is only in the non-visible `<title>` (visible heading "Academy"), so it false-FAILED a home rendering 483 cards. Moved to the M42e CARDINALITY shape (`kind:'both'` + `a[href*="/chapters/"]` + `minCount 12` + kept the 400 anti-blank floor) — STRONGER (an empty catalog OR keyless blank = 0 cards → FAIL; the old token would've passed the empty `/library` "The AI Academy library" header). Unit 54/54 + tsc clean. Both hero coverage sweeps now **GATE MET ✅** (employee 62/150, manager 70/150; 0 failing/persona/escape/notReached/crossPort). Harness-only (rext `2a71e08`, tag moved; no billion re-pin). Gate (c) NOT yet ticked (discrete stack-verify specs + 16 Playthroughs remain). Metric stays 5/8. closed-fixed. — see iter-16/progress.md
- iter-15 (tik, run 6): **gate (d) DISCHARGED — the anon academy renders real cards live on billion.** Root-caused (iter-09) to `serverTenant.js::getPublicCatalogView` (`new Set()`) being left unpatched by M230's authed-only FSPUB fallback. Authored a CHAINED sibling demopatch **`academy-fs-published-public`** (mirrors FSPUB on the anon line, `new Set()` public-only, strips `_draft`) + a native helper + `ant-academy.sh` wiring (apply after / revert before FSPUB). Chain-tested locally (pristine↔post1↔post2 + order-guard REFUSE). Applied on billion via `next dev` HMR (NO Docker re-bake): re-pinned rext clone b38ad75→8391843, serverTenant.js e0f48e81→41cc2d7e. Anon browser renders: home **483 cards**, `/library/` **37+ cards** (bodyLen 92998, 0 errors), `/free` **42 cards**, **0 Draft chips** (the iter-09 `/free` 2-chip sub-finding RESOLVED). rext `8391843`, tag moved (peels 8391843). **Metric 4/8 → 5/8.** NEW finding: the gate-(c) coverage cross-port has a SECOND blocker — the `ANT_ACADEMY_HOME_SECTION` marker `['AI Academy']` is stale vs the home's real "Academy" heading (content IS there) → routed iter-16. closed-fixed. — see iter-15/progress.md
- iter-14 (tik, run 6): **gate (c) coverage half MEASURED — green modulo the academy cross-port.** Drove `coverage.spec.ts` both hero vantages on billion (COVERAGE_HOST + https): employee reachable 59/150, manager 70/150; BOTH with failingSections 0 / personaFailures 0 / escapes 0 / notReached 0 / frontier EXHAUSTED — the ONLY failure (both) is the identical cross-port follow `/home` → `:13077/` (ant-academy home empty). **Pivotal finding: the (c)↔(d) COUPLING** — that failure IS the gate-(d) academy-empty root cause (iter-09), so gate (c) coverage can't go green until the academy demopatch lands. Re-orders the remaining work: gate (d) NEXT (unblocks c). Also established the SEED ORDER (24 stack-verify + d/f/h on the demo seed FIRST; the 16 Playthroughs need the pt-world reset → run LAST). Metric stays 4/8. closed-no-lift (complete measurement + characterization). — see iter-14/progress.md
- iter-13 (tik, run 6): **gate (b) DISCHARGED — 47/47 live on billion.** Probe-first (TOK-02 mandate) DISPROVED the preferred demopatch fix: the extraction fetch feeds only props, is NOT in the container `BaseLoading` gate, returns 200, and is irrelevant to ack timing — the ack paints ~5.4 s (warm; iter-11's "~25 s" was a cold server). Real cause = the nav shell (126-128 chars) clears `MIN_SETTLED_CHARS` (120), so `settle()` early-exits on the chrome plateau before the ack paints. Took TOK-02's FALLBACK: **shape-aware settle** (`contentReady(shape)` gates player-interview on the ack in `<body>`; other 5 shapes unchanged). Harness-side → **NO billion re-pin / re-bake**. +4 mutation-verified unit specs (48/48), tsc clean; live re-sweep **LANDED 47/47**. rext `2bb0473`, tag moved+pushed (peels `^{}` → 2bb0473 on origin). **Metric 3/8 → 4/8.** closed-fixed. — see iter-13/progress.md

- iter-20 (tik, run 8): **gate (h) COMPLETE — metric 5/8 → 6/8.** All 6 v2.6 fixes now proven live on billion + p95<5s both heroes. (1) Refreshed the stale >12h autoverify → fresh green (0 warnings, ts 00:34:22Z, STACK_DIR set, run as devops). (2) **p95 click→ACCESS < 5s BOTH heroes**: employee (maya) **1.46s** / manager (dan) **1.31s**, 5/5 reached ACCESS each (run-latency.sh, https, gate 5000, fresh green-gate; M236 UTC age-check fixed in local 6aacc32). (3) **academy course-start + `?lang=it`**: fresh employee `run-coverage.sh` → GATE MET + probeAcademyChapter "chapter body (slug accessing-open-models) + ?lang=it OK, HTTP 200" (browser render-assert; a raw curl-grep false-positived on the error-boundary bundle — D1). (4) **M241 EN/IT language toggle**: content-manifest 21 bilingual tuples (EN10/IT13) + cockpit :17700 renders "Content stories" + "EN/IT" + 28 data-lang cells. 0 platform edits, 0 code changes (pure live verification). closed-fixed. — see iter-20/progress.md

- iter-21 (tik, run 8): **gate (f) 2/3 burned in live on billion** (metric stays 6/8 — gate f ticks only at 3/3). **PROBE-M218-c3 ✅** — 0 cms/Directus content-path 403s on the live Cosmo router (all 38 "403" log lines are `status:200` substrings; a live getSkillPaths query schema-validated, not 403; content renders via router→cms→Directus). **F-M220-4 ✅** — re-ran ant-academy.sh on the live public-host demo: after `--stop` a stale supervisor respawned a next-server on :13077, the relaunch's reap killed the respawner + rebound + re-applied all 4 academy demopatches; academy serves home 200/974KB + chapter EN 200 + `?lang=it` 200 + catalog 200. **BURNIN-M221** feasibility-assessed (reduced-profile `/dev-up 2 --public-host` fits RAM; from-scratch ≥1h) → routed iter-22 (NOT a blocker). closed-fixed-partial. — see iter-21/progress.md

- iter-22 (tik, run 8): **BURNIN-M221 investigation — dev workspace BUILT + reduced-profile hypothesis FALSIFIED** (metric stays 6/8; no dev stack came up). Built the from-scratch dev workspace on billion (`/home/devops/panorama/stack-dev/` = platform + 10 repos + values-blind dev .env). Attempting `dev-stack up 2 --profile backend --public-host` falsified iter-21's reduced-profile plan: the `backend` profile is invalid (`depends_on cms`, undefined in-profile) AND publishes no ports (so `--public-host` fronts nothing) → a faithful `--public-host` burn-in needs the **FULL graphql profile**, which won't fit billion's 7.3GB RAM alongside demo-1 (~4.3GB) → needs **demo-1 torn down first** (SAFE — all demo-seed gates done + recorded). Feasible + characterized, NOT a blocker. billion left clean (0 dev-2 containers, demo-1 green 17/17). Routed: playthroughs first (iter-23, lock in gate c → 7/8), BURNIN completion (iter-24). closed-no-lift. — see iter-22/progress.md

## Next-iter queue (RESUME HERE — gate (c) Playthroughs → 7/8, then BURNIN-M221 → 8/8)
- **gate (h) COMPLETE ✅ (iter-20)** — all 6 v2.6 fixes proven live + p95<5s both heroes (employee 1.46s / manager 1.31s). talk-to-data / library / cockpit / content-fidelity (iter-18) + academy course-start + `?lang=it` (fresh coverage sweep) + M241 EN/IT toggle (manifest 21 bilingual + cockpit render). Metric 6/8.
- **gate (c) 16 Playthroughs (RESUME HERE) → 7/8** — gate-c stack-verify half is DONE (iter-16 coverage + iter-18 discrete). The 16 Playthroughs run on pt-world (reset-to-seed — replaces the demo seed; all demo-seed gates are done + recorded so this is now safe). Lower-risk deterministic harness (`playthroughs/e2e`); run against billion over the tailnet → gate (c) ticks **7/8**. Do this FIRST (lock in the tick) before the heavier BURNIN.
- **gate (f) BURNIN-M221 → 8/8** — the LAST carry. Dev workspace already BUILT (iter-22: `/home/devops/panorama/stack-dev/` = platform + 10 repos + values-blind dev .env). Completion = **tear down demo-1** (free RAM — safe, all demo gates done) → `dev-stack up 2 --profile graphql --public-host billion.taildc510.ts.net` (the FULL profile — the `backend` profile is invalid + port-less, iter-22 D1) → verify the dev stack comes up + is tailnet-reachable. Background the build (durable heartbeats + final verdict; NEVER kill mid-build). → gate (f) ticks 8/8 = GATE MET. If it OOMs even with demo down / needs a platform edit → surface a blocker with specifics, don't fabricate.
- **gate (c) tick: the 16 Playthroughs LAST** (pt-world reset destroys the demo seed — iter-14 D1). Gate-c stack-verify half is ALREADY complete (iter-18); only the Playthroughs remain. Ticks **8/8 = GATE MET**.
- **DEF-M239-01** (ENOSPC loud-build-fail) as budget allows.
- NB the binary-per-gate metric under-reports (iters 14/16/17/18 all did real gate work at a flat 5/8); TOK-02 diagnosed this as sound-strategy-coarse-metric. The 3-no-prog floor (iter-16/17/18 all no-metric-delta) fires the skill's **unconditional** tok-trigger at **iter-19 = TOK-03** (a triggered tok — consolidates + sharpens the final push; the strategy HOLDS, no revision). Run 8 resumes with a fresh tik at gate-h completion under TOK-03.
- **gate (d) `/library` FIX (iter-09 finding):** an ant-academy demopatch/config so the anon `/library` grid serves the FS catalog. + a `/free` Draft-chip sub-finding (2 vs 0). Re-seed-independent. `/free` half already ✓ (43 cards).
- Inherited carries: DEF-M239-01 (ENOSPC loud-build-fail) OPEN. reap-17700 LANDED (iter-10). DEF-M240-01 dispositioned (iter-07).

## Carry / disposition tracking (gate parts a–h + inherited)
- (a) ORG-CLEAN ✅ (iter-11) · (b) content-stories ✅ **47/47 live (iter-13)** · (d) academy twin ✅ **anon /library+/free render real cards live (iter-15)** · (e) DEF-M226-01 ✅ (serve-reap 7→0, iter-11) · (g) interview report renders LIVE ✅ (iter-06) — **5/8 discharged, all live on billion**.
- (b) content-stories: **DISCHARGED — LANDED 47/47 live on billion (iter-13)**. The 2 interview-PLAYER residuals resolved by shape-aware settle (`contentReady` gates player-interview on the ack in `<body>`, not on the chrome plateau). Probe DISPROVED the demopatch fetch-cause → harness-only fix, no re-bake. Both pairs `ok=True mainLen=205 "interview acknowledgement rendered"`.
- (d) academy twin: **DISCHARGED (iter-15)** — the `academy-fs-published-public` chained demopatch makes the anon `getPublicCatalogView` render the FS-published catalog. Live on billion (browser): home 483 / `/library/` 37+ / `/free` 42 cards, 0 Draft chips (`/free` 2-chip sub-finding resolved). Applied via `next dev` HMR (no re-bake).
- (c) 40 specs: **stack-verify HALF COMPLETE (iter-16 coverage + iter-18 all 10 discrete gate specs GREEN on billion)**. REMAINING for the gate-(c) tick: ONLY the 16 Playthroughs LAST (pt-world reset). NOT ticked. · **(f) 2/3 burned in (iter-21): PROBE-M218-c3 ✅ + F-M220-4 ✅. Remaining: BURNIN-M221 — dev workspace BUILT (iter-22); completion = tear down demo-1 + FULL graphql dev-2 --public-host (reduced profile falsified: backend depends_on cms + port-less). → iter-24.** · **(h) DISCHARGED ✅ (iter-20)** — all 6 v2.6 fixes proven live on billion (talk-to-data/library/cockpit/content-fidelity iter-18 + academy course-start + `?lang=it` fresh sweep + M241 EN/IT toggle) + **p95<5s both heroes** (employee 1.46s / manager 1.31s). **6/8 discharged.**
- Inherited: **reap-17700 ✅ LANDED (iter-10)**. **DEF-M240-01 ✅ dispositioned (iter-07)**. DEF-M239-01 (ENOSPC loud-build-fail) OPEN. Durable standing-8 (M238-D5) still Fate-2.
- Enabling foundation: **billion demo-1 GREEN at the m244 pin (iter-11)**. rext consumption tag `sound-check-m244-content-sweep-robustness` → **2a71e08** (iter-16 coverage marker recalibration + iter-15 academy anon-view demopatch + iter-13 shape-aware settle; peels `^{}` → 2a71e08 on origin). Billion's rext clone at **8391843** (re-pinned iter-15 for the academy fix; the iter-16 coverage-marker + iter-13 settle fixes are HARNESS-only, run from the local authoring copy → not billion build inputs, so the clone stays at 8391843). The academy clone's serverTenant.js at post2 (FSPUBLIC applied, native, no re-bake).
