# M244 — Progress

Iterative milestone (the closer). Primary metric: the multi-part exit gate (a–h) met cold on `billion`.

## Running ledger

- iter-01 (tok/bootstrap): TOK-01 authored (staged cold billion bring-up → gate-parts a–h one-per-tik); Phase 0b KB-fidelity YELLOW (denominator=49, spec=40); pre-flight rung zero mapped (billion bare, pin=m243) — see iter-01/progress.md
- iter-02 (tik): Foundation GREEN — re-pinned billion to m243 (rung-zero), gate (a) ORG-CLEAN PASS (23 fixtures/0 tokens live), gate (e) DEF-M226-01 serve-reap CONFIRMED (7→0 ports), cold reset-to-seed BRINGUP_EXIT=0 + fresh green autoverify (12:51Z) + all peer origins serving + 42,790 skills/1,644 sessions/49-pair manifest. Metric 0/8→2/8. — see iter-02/progress.md

- iter-03 (tik/tooling-iter): gate (b) — fixed the content-stories runner's bash-3.2 parse wall (unrunnable on macOS peers) + tailnet cold-render flakiness (settle-budget + per-pair warm retry); 3 rext fixes shipped+tagged+pushed (`sound-check-m244-content-sweep-robustness`→01206e7). Gate (b) 37 cold → **46/49 reliable**; 3 DETERMINISTIC residuals (voice/interview render, full DB content) route to gate (g) + a voice-presence-only decision. closed-fixed-partial. Metric 2/8 (b not green). — see iter-03/progress.md
- iter-04 (tik): gate (g) — LIVE-PROVED the interview report renders **EMPTY** on billion (seeded depth/adoption/sentiment absent behind "View Report"); the plan-section-id alignment assertion would be **RED**. The S-8/S-9 gap is a real defect (not just unproven); intv-voice-pass residual is the same failure. Root-cause surface identified (seed-plan vs flag-demopatch); fix routed forward (0 platform edits). closed-no-lift. Ended on a user-requested pause. — see iter-04/progress.md
- iter-05 (tik): gate (g) FIX — ROOT CAUSE = `directus.simulations_extraction` (the interview PLAN) **never captured by the snapshot** (0 rows on demo) → `interviewExtractionPlan` null → report empty. FIX: added the table to the directus capture surface (`stack-snapshot`, parent-scoped public-only), tested + pushed (rext `e74e563`, tag moved). Plan loaded to the demo → report render 0→520 (plan is load-bearing). NOT green: full section render (behind "Explore Key Moments") + the alignment assertion + the live capture-path exercise routed. **intv-voice-pass/player is a SEPARATE ack-page issue** (gate b stayed 46/49). closed-fixed-partial. — see iter-05/progress.md
- iter-06 (tik): **gate (g) DISCHARGED** — the plan-section-id alignment assertion (`TestInterviewPlanSectionAlignment` + golden `interview-plan-sections.json` + drift-teeth test) ADDED + GREEN + pushed (rext `0dec8ff`, tag → `70d75cb`). It CAUGHT a real defect: intv-voice-fail was pinned to a v1.3-era session (breadth/context_fit/frequency orphans) vs the sim's v1.4 plan. FIX: re-pinned `43a92fc0 → 05dae0f7` (a v1.4-clean genuine FAILED interview) via `content-capture --only` (read-only prod, scrubbed, cleanliness-green); both projection goldens regenerated. Live-confirmed on billion (DB-level): pass clone 0-orphans; the assertion catches the m243-seeded fail clone's live drift, resolved by the re-pin on the cold reset-to-seed. **2/8 → 3/8.** closed-fixed. — see iter-06/progress.md
- iter-07 (tik): **gate (b) advanced** — ground-truthed the billion sweep (46/49; the 3 residuals are ALL player-vantage: 2 voice + 1 intv-ack; managers all land) + dispositioned the 2 Bunny-absent voice cells to **PLAYER-presence-only** (denominator 49→47). Built the per-session mechanism full-stack: contentsession `player_result_unavailable`+reason, the projection `player_presence_only` (manager view kept, not a drop), buildPairs presence-only branch, cockpit partition-by-verdict + withheld-player disclosure, both goldens regenerated. Go module + 21 TS specs + cockpit 159/6 green. rext `1968fd2`, tag → `6f52f0c`. Live 47/47 lands at gate (h). Metric stays 3/8. closed-fixed. — see iter-07/progress.md
- iter-08 (tik): **gate (b) #3 resolved** — root-caused the `intv-voice-pass/player` "no ack text" (mainLen 0) to the M232 `next-web-interview-flag-result` demopatch opening the interview render gate for the PLAYER scope: a PASSED interview (evaluation_status=passed) rendered the player-scope InterviewReport EMPTY, a FAILED one fell through to the ACK. But an interview has no player scored report (the sweep's own `player-interview` shape says the player sees only the ack). FIX: scoped the demo-widen to `isManagerScope` (player → ack pass+fail; manager report / gate g untouched); post_sha256 recomputed + sha-method-verified + a regression test; demopatch(12)+tooling+build(254) green. rext `f46e082`, tag → `8756ec0`. **All 3 gate-(b) residuals now resolved.** Metric stays 3/8 (live 47/47 at gate h). closed-fixed. — see iter-08/progress.md
- iter-09 (tik): **gate (d) MEASURED** (live, anon, billion academy :13077) — `/free` renders **43 real cards** (✓, but 2 Draft chips vs expected 0), anon **`/library` renders an EMPTY grid** (✗, 0 cards, header only). The twin does NOT both-render → gate (d) NOT met. Root cause: the `/library` grid reads a TENANT-FILTERED catalog (`ServerCatalogContext`) that is empty for an anon demo user (`publicSource.js` refuses the FS-catalog import to avoid cross-tenant leakage); `/free` reads a public source. The m244 re-seed will NOT fix it — the fix is an ant-academy demopatch/config to feed the FS catalog to the anon `/library` grid (routed). closed-no-lift (documented falsification). — see iter-09/progress.md
- iter-10 (tik, cleanup): **inherited carry reap-17700 (M239-D13) DISCHARGED** — `test_a_RACED_listener_exits_silently` false-failed on a box with a live demo-1 cockpit on :17700 (test-isolation collision, 0 product/reap.sh defect). Fixed per the D13 recipe: default `_reap_with_stubs` to a guaranteed-free `_free_port()` (never 17700). PROVEN load-bearing — with :17700 held locally the RACED test now passes (would have refused before). test_reap.py 41/41. rext `b38ad75`, tag → `7dbad4b`. 1 of 3 inherited defers landed. closed-fixed. — see iter-10/progress.md
- iter-12 (tok/triggered): **TOK-02 — consolidate + sharpen TOK-01** after 5 no-metric-delta tiks (07–11) all working the gate-b cluster. The strategy is SOUND (real progress every iter); the flat 3/8 is a coarse binary-per-gate artifact (gate b only counts at 47/47, sits at 45/47). Run-6 direction set: iter-13 lands gate b 47/47 (scope the interview container FETCH demopatch to isManagerScope, mirror iter-08; re-bake + re-sweep), then iters 14–15 sweep gates (c)/(f) live, then (h)/(d)/DEF-M239-01. Exit tok-fired. — see iter-12/progress.md
- iter-11 (tik): **the gate-(h) cold reset-to-seed — DONE GREEN at the m244 pin** (re-pin b38ad75 from origin → teardown --purge → `up-injected.sh --public-host` under setsid-nohup → BRINGUP_EXIT=0 + fresh-green autoverify 19:31:10Z + all origins serving; baked SSR map confirms the correct SCOPED iter-08 demopatch). Gates **(a)/(e)/(g) re-verified LIVE** on the fresh seed. Fixed the **LOAD-BEARING** cross-check divergence (49→47; it exit-2'd before the sweep could run) + a defensive EN/IT grader (rext `e30df85`, tag → 0c514e6/e30df85, pushed). Gate (b) live **45/47** — the 2 interview-PLAYER pairs render the ack ~25 s late (probe-confirmed EN ack, mainLen 332 after a long wait) while settle returns early on the nav shell; root-caused + routed to iter-12 (rext/demopatch fix, NOT a platform edit). closed-fixed-partial. Metric stays 3/8. — see iter-11/progress.md
- iter-13 (tik, run 6): **gate (b) DISCHARGED — 47/47 live on billion.** Probe-first (TOK-02 mandate) DISPROVED the preferred demopatch fix: the extraction fetch feeds only props, is NOT in the container `BaseLoading` gate, returns 200, and is irrelevant to ack timing — the ack paints ~5.4 s (warm; iter-11's "~25 s" was a cold server). Real cause = the nav shell (126-128 chars) clears `MIN_SETTLED_CHARS` (120), so `settle()` early-exits on the chrome plateau before the ack paints. Took TOK-02's FALLBACK: **shape-aware settle** (`contentReady(shape)` gates player-interview on the ack in `<body>`; other 5 shapes unchanged). Harness-side → **NO billion re-pin / re-bake**. +4 mutation-verified unit specs (48/48), tsc clean; live re-sweep **LANDED 47/47**. rext `2bb0473`, tag moved+pushed (peels `^{}` → 2bb0473 on origin). **Metric 3/8 → 4/8.** closed-fixed. — see iter-13/progress.md

## Next-iter queue (RESUME HERE — run 6, tik 2 = iter-14)
- **gate (b) DONE (iter-13)** — 47/47 live on the m244 seed; rext consumption tag `sound-check-m244-content-sweep-robustness` → **2bb0473** (adds shape-aware settle on top of the iter-11 cross-check + EN/IT grader). Harness-only fix; billion's build pin untouched.
- **gate (c) 40 live-browser specs (RESUME HERE — iter-14):** 24 stack-verify + 16 Playthroughs (incl. M243's `assignment-assign.spec.ts`). Drive from the peer harness against the green m244 seed; reuse the settle+retry template for tailnet flakiness. → 5/8.
- **gate (f) 3 drift-carries · (h) v2.6 fixes + p95<5s** — OPEN, unblocked by the green m244 seed (drive from the peer harness).
- **gate (d) `/library` FIX (iter-09 finding):** an ant-academy demopatch/config so the anon `/library` grid serves the FS catalog. + a `/free` Draft-chip sub-finding (2 vs 0). Re-seed-independent. `/free` half already ✓ (43 cards).
- Inherited carries: DEF-M239-01 (ENOSPC loud-build-fail) OPEN. reap-17700 LANDED (iter-10). DEF-M240-01 dispositioned (iter-07).

## Carry / disposition tracking (gate parts a–h + inherited)
- (a) ORG-CLEAN ✅ (re-verified LIVE on the fresh m244 seed, iter-11) · (b) content-stories ✅ **47/47 live on billion (iter-13)** · (e) DEF-M226-01 ✅ (serve-reap 7→0, re-confirmed at iter-11 teardown) · (g) interview report renders LIVE ✅ (both intv MANAGER pairs landed on the fresh seed + alignment assertion green iter-06) — **4/8 discharged, all live on the m244 seed**.
- (b) content-stories: **DISCHARGED — LANDED 47/47 live on billion (iter-13)**. The 2 interview-PLAYER residuals resolved by shape-aware settle (`contentReady` gates player-interview on the ack in `<body>`, not on the chrome plateau). Probe DISPROVED the demopatch fetch-cause → harness-only fix, no re-bake. Both pairs `ok=True mainLen=205 "interview acknowledgement rendered"`.
- (d) academy twin: MEASURED (iter-09) — `/free` ✓ 43 cards, anon `/library` ✗ empty grid (needs an ant-academy demopatch, re-seed-independent). NOT met.
- (c) 40 specs · (f) 3 drift-carries · (h) v2.6 fixes + p95<5s — OPEN, now **unblocked** by the green m244 seed (drive from the peer harness in iter-14+).
- Inherited: **reap-17700 ✅ LANDED (iter-10)**. **DEF-M240-01 ✅ dispositioned (iter-07)**. DEF-M239-01 (ENOSPC loud-build-fail) OPEN. Durable standing-8 (M238-D5) still Fate-2.
- Enabling foundation: **billion demo-1 GREEN at the m244 pin, cold reset-to-seed, fresh green autoverify 2026-07-22T19:31:10Z (iter-11)**. rext consumption tag `sound-check-m244-content-sweep-robustness` → **2bb0473** (iter-13 shape-aware settle on top of the iter-11 cross-check mirror + EN/IT grader; peels `^{}` → 2bb0473 on origin). Billion's build-time pin is the run-5-era commit — the iter-13 fix is HARNESS-only (not a build input), so no re-pin/re-bake was needed; a re-pin will come at gate (d)'s academy demopatch.
