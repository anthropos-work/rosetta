# iter-18 — decisions

## D1 — held the retrofit as the iter-18 target (did NOT substitute a metric-moving gate)
The orchestrator/TOK-02 routed iter-18 = the gate-(c) discrete retrofit, with the stated aim of "not being a 3rd no-metric-delta tik (16/17/18 → a forced tok at iter-19)." Re-survey (this iter) found the premise doesn't hold either way: the retrofit does NOT tick the binary-per-gate metric (gate c also needs the 16 Playthroughs, seed-order-forced LAST), so a retrofit tik is a no-metric-delta tik regardless. I checked the two metric-MOVING alternatives and neither is a clean single-tik tick: **gate (f)** needs a real `/dev-up --public-host` remote **dev** burn-in (BURNIN-M221 — a separate heavy bring-up), and **gate (h)** had a real blocker (see D2). So NO clean single-tik metric-mover exists — the coarse binary metric stays flat this tik no matter the target (exactly TOK-02's characterization). Given that, the highest-value reliable iter-18 is the retrofit: foundational, unblocks the gate-c discrete half AND the gate-h talk-to-data/cockpit spec proofs, and its own spec-run (talk-to-data-m239) definitively answered the Bedrock question. Held the target; accept the iter-19 tok-trigger as a structural coarse-metric artifact, not a stalled strategy.

## D2 — the talk-to-data Bedrock provisioning gap: a real v2.6 defect, fixed values-blind (NOT a platform edit)
Running talk-to-data-m239 against billion FIRST (the decisive probe) reproduced the exact Bedrock-less failure the spec's own comment predicts: the **flag gate PASSED** (talk-to-data loads, composer renders, Send enables) but the **Bedrock round-trip never streamed** ("chat did not grow") — because billion's demo had **NO Bedrock creds** anywhere (values-blind check: source `.agentspace/secrets/app/.env`, provisioned `stack-demo/app/.env`, and the bridged `stack-demo/platform/.env` all lacked `AWS_ACCESS_KEY_ID`/`AWS_SECRET_ACCESS_KEY`/`AWS_REGION`/`CLAUDE_CODE_USE_BEDROCK`). The LOCAL secrets source HAS them → this is the exact **"not all gets built and provisioned as expected"** defect class v2.6 exists to catch, and it's a `/stack-secrets`-shaped provisioning gap, **not a platform edit or a user-blocker**.

**Fix (values-blind, surgical — replicates M239's provision+bridge without a full re-bring-up):** extracted the 4 Bedrock lines from LOCAL `.agentspace/secrets/app/.env` to a temp file (grep→file, never echoed), scp'd it to billion, and appended them idempotently (newline-guarded, per-key skip-if-present) to billion's **source** `.agentspace/secrets/app/.env`, the **provisioned** `stack-demo/app/.env`, and the **bridge target** `stack-demo/platform/.env` (the M239 `bridge_bedrock_creds` app→platform target the backend reads). Then `docker compose … up -d --no-deps --force-recreate backend` (targeted recreate, ~30s backend blip, seed untouched, other containers untouched). Backend's live env then carried the 4 keys (values-blind confirmed); **talk-to-data-m239 re-run GREEN (11.2s) — a manager asked "how many members?" → a live Bedrock member-count answer.** Discharges the gate-(c) talk-to-data spec AND the gate-(h) "talk-to-data live answer" proof. The value never entered agent context at any step.

## D3 — the "remote-capability retrofit" is ENV-WIRING, not per-spec code (iter-17 overstated it)
iter-17 mapped ~9 discrete specs as "localhost-only" needing a per-spec host+scheme code retrofit. On inspection, **every** discrete gate spec ALREADY reads a full-URL base env var (talk-to-data/enterprise → `ROSETTA_E2E_BASE_URL`/`ROSETTA_E2E_FAPI_URL`; verify-* → `VERIFY_APP`/`VERIFY_FAPI`; render-hiring → `RENDER_APP`/`RENDER_FAPI`; cockpit-overlay → `RB_COCKPIT_URL`; smoke → relative paths against playwright.config `baseURL`; m220 → `ROSETTA_E2E_*_BASE`) — only with localhost DEFAULTS. So **no per-spec code change is needed**; the real gap was knowing each spec's env-var names. Captured the mapping in a reusable **`run-discrete.sh`** runner (the run-coverage.sh HOST+SCHEME+OFFSET pattern, exporting every spec's base-env aliases, serial `--workers=1`). Harness-only (runs from the authoring copy; not a billion build input → no billion re-pin). m220-session-and-egress is held out of the default set — it also needs the studio-desk base served over the tailnet, which a --public-host demo does not serve by default.
