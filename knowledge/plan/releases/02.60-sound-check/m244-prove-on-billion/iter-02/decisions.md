# iter-02 — decisions (local)

- **D1 — supervise the remote bring-up as a background remote-foreground process.** Launched `up-injected.sh` under a single `ssh devops@billion` session (backgrounded on this workstation via `run_in_background`, teed to `~/m244-bringup.log`), polled + heartbeated. One driver; nothing detached on-host; never killed mid-build. BRINGUP_EXIT=0.
- **D2 — adopt the devops/panorama workspace (Step-0 substitution).** Re-survey found the secrets + snapshot cache + rext clone already present under `devops@/home/devops/panorama/` from a prior (M238-era) session. Re-pinned that clone to m243 and reused it rather than building a from-scratch workspace + scp-ing 1.5G. Same TOK-01 strategy; only the named next-target changed. billion realities recorded: DB is single `postgres`; `devops` is the docker-group deploy user (`ssh devops@billion` direct); marco is sudo-only (no docker group).
- **D3 — gate (a) run as the content-story fixture cleanliness go-test.** "ORG-CLEAN" is the milestone's name for the `stack-seeding/contentsession` + `scrub` suites (no literal tool named ORG-CLEAN). Read-only, no DB, run `-count=1` on billion's m243 clone (byte-identical fixtures). 23 fixtures, 0 surviving source-org/PII tokens.
- **D4 — gate (e) tested at the stale-stack teardown.** The pre-existing stale demo-1 gave a free live test of DEF-M226-01: captured `tailscale serve status` before/after `rosetta-demo down 1 --purge`; 7 offset serve ports → 0. Claim holds (tested + passing, not dropped).
