# iter-14 — decisions

## D1 — seed ordering: demo-seed gates before the pt-world Playthroughs (no demo re-bring-up)
`run-playthroughs.sh --reset` full-TRUNCATEs then seeds pt-world (3 private orgs) + swaps the fake-FAPI roster to pt-world seats — it REPLACES the content/stories demo world. The 24 stack-verify specs + gates (d)/(f)/(h) all read the demo/stories world (heroes maya/dan, the content catalog). So the pt-world reset must be the LAST seed-dependent action, or every demo-seed gate would need an expensive demo re-bring-up afterward. iter-14 drove the coverage sweep read-only on the current demo seed (untouched); the Playthroughs are routed last.

## D2 — the coverage failure is gate-(d)-owned, NOT a coverage/stack-verify defect; do NOT weaken the gate
Both hero vantages' coverage sweeps are green except one cross-port follow: `/home` → the ant-academy home (:13077), which renders empty. This is the exact gate-(d) academy-empty root cause iter-09 characterized (anon academy grid reads a tenant-filtered/empty catalog; `publicSource.js` refuses the FS import). Two choices considered: (a) mark the academy cross-port a documented exception so the coverage gate passes, or (b) treat it as a real blocker and fix the academy. Chose (b): the academy IS part of the demo and genuinely should render — weakening the coverage gate's teeth to pass a real empty page is the false-PASS class this whole harness exists to prevent. So gate (c) coverage stays RED until the gate-(d) academy demopatch lands; the fix unblocks both. This re-orders the remaining work (gate d next).
