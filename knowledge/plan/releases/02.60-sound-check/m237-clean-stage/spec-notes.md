# M237 — Spec notes

Topic → doc → code triples + clone-freshness / defect-re-triage findings accumulate here during build.

## Clone-freshness mechanism
- `rext demo-stack/ensure-clones.sh` — the fetch-verified freshness assertion; the suppressed-stderr host-key failure (billion `root`) that gave 12-vs-202; the real pin model (`pinned` vs `stale-by-neglect`).

## R1 pristine sweep
- F-M236-CLOSE-2: enumerate all 14 patch manifests, not the hard-coded 3.

## Fresh-clone billion re-triage
- The confirmed-defect ledger: #1 menu (was clone-staleness) · #2 academy-language · #4 library-empty — which survive a correct build.

_(will accumulate topic → doc → code triples + prove-by-render findings during build)_
