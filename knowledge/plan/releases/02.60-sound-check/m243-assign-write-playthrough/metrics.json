{
  "milestone": "M243",
  "title": "assign-WRITE Playthrough",
  "shape": "section",
  "close_status": "closed-complete",
  "closed": "2026-07-22",
  "platform_repo_edits": 0,
  "tests": {
    "go_repo_test_funcs": "2005 (unchanged vs M242 — M243 MODIFIED an existing manifest test in place, added no new func: corpus_test.go's TestRealCorpus_ManagerCoverageIsPresent had its M204-era pin REVERSED (UC1 was 'must stay a declared TODO' -> now 'must be a non-TODO pt-assignment-assign'); the manifest package holds 3 test funcs at both build-parent 73d37d5 and close HEAD 2ef5962). Go manifest fresh -count=1 GREEN; go vet + gofmt clean",
    "ts_playthroughs_unit_specs": "77 combined unit-spec run (url-shapes.unit + stack-env.unit), was 73 -> 77 (+4). Breakdown: url-shapes.unit.spec.ts 60 -> 64 (+4: the dedicated 'isOnAssignments — cross-predicate edge + interaction depth (M243 harden)' block — mutual-exclusion vs the other 4 manager landing routes both directions, sub-tab-no-leak, path-relative page.url()/waitForURL parity, and an adversarial fuzz grid) + stack-env.unit.spec.ts 13 (unchanged)",
    "ts_playthroughs_live_specs": "16 @pt: live-browser Playthrough spec files, was 15 -> 16 (+1: e2e/tests/assignment-assign.spec.ts, @pt:pt-assignment-assign). Executed LIVE GREEN at build (demo-1, 7.9s) + DB-verified (organization_assignments skill_path/active 6->7, the exact assign). Its COLD re-drive on billion is M244's declared scope (roadmap Out: + M244 exit-gate (h)), not a close gap",
    "ptvalidate_static_gate": "GREEN — 7 products / 16 use cases / 16 live Playthroughs / 0 TODO (was 15 live / 1 TODO; M243 filled the sole in-manifest TODO). 16 @pt: spec files, 16 distinct @pt: tags — exact parity",
    "typecheck": "tsc --noEmit clean (0 errors)",
    "mutation_verified": "the crux (proven at harden, re-confirmed at close, no re-run): (1) the live assign-WRITE assertion is the read-back FLIP — expect.poll(() => assignableCount()).toBe(before - 1) — NOT a closed modal; a write that silently fails leaves the count at `before` -> the 20s poll times out RED (fail-closed). (2) host-anchoring ASSIGNMENTS_URL (a regression NO pre-existing test caught) reddened EXACTLY the new path-relative unit test, 63 others green; restored -> 64. (3) the two Go honesty-teeth: renaming the org-unassigned-member capability reddens TestRealCorpus_ValidatesAgainstSeedWorlds; flipping UC1 to TODO reddens TestRealCorpus_ManagerCoverageIsPresent (verify-then-restore, no commit)",
    "flake_gate": "GREEN — combined TS unit-spec run 77 passed 5/5 sequential, identical, deterministic (pure-logic route/predicate tests, no browser, no shared mutable state). Go manifest deterministic (fresh -count=1). No Python touched by M243"
  },
  "flake_count": 0,
  "coverage": {
    "method": "qualitative / branch-path + mutation-verified (same convention as M237-M242; no line-coverage tool wired in this rext section). Go manifest package already 100.0% statements (maximal) — the harden work was proving the honesty-teeth BITE, done empirically, not a line-bump",
    "touched_language_fns": "TS: AssignmentsPage (the first MUTATING page object — openSkillPathsTab / assignSkillPathButtons / assignableCount / openAssignBuilderForFirstAssignable / pickFirstSkillPath [antd-v6 rc-virtual-list keyboard-pick] / confirmAssign / memberRow / targetMemberName [best-effort non-blocking]) + ASSIGNMENTS_URL / isOnAssignments (single-sourced, segment-anchored). Go: corpus_test.go M204-pin reversal (wantManagerPTs += pt-assignment-assign; UC1 must-be-non-TODO)",
    "surface": "the assign-WRITE journey end-to-end: manager login -> /enterprise/assignments -> Skill Paths tab -> open the assign builder for an unassigned member -> keyboard-pick a public-catalog skill path + accept the pre-filled deadline -> Assign -> the assignable-affordance count drops by exactly ONE (the target's cell flipped from affordance to assigned title — the write landed and was read back through the real members query)",
    "residual_uncovered": "the COLD live re-drive on billion (reset-to-seed) rides M244's e2e execution — the build already proved it GREEN + DB-verified on demo-1. A full live re-break-the-write (break the mutation while a target still exists) was NOT re-run at close: it needs a destructive re-seed of a shared stack (stackseed not on PATH), disproportionate for a docs-only close given the build-time DB proof exists"
  },
  "code_of_record_tag": "sound-check-m243-assign-write-playthrough @ 2ef5962 (annotated tag on origin, peels to 2ef5962; origin/main also at 2ef5962 — verified synced. UNCHANGED by close: the close made no rext code change, so no tag move)",
  "build_commits_rext": "b327e78 (M243: assign-WRITE Playthrough pt-assignment-assign — manifest UC1 fill + seed-worlds org-unassigned-member + assignments-page.ts + assignment-assign.spec.ts + ASSIGNMENTS_URL/isOnAssignments + corpus_test.go M204-pin reversal)",
  "harden_commits_rext": "2ef5962 (harden: isOnAssignments cross-predicate edge + interaction depth — +4 unit tests; mutation-verified net-new protection + the two Go honesty-teeth)",
  "close_commits_rext": "NONE — the close found 0 code fixes (clean close); all rext code is unchanged from harden HEAD 2ef5962",
  "build_commits_corpus": "1aa0d1f (M243: assign-WRITE Playthrough — docs + plan, 16 live / 0 TODO) + 1bd6bf6 (harden: record hardening pass)",
  "close_commits_corpus": "6f87016 (close Phase 1b/2c/6: deferral audit YELLOW + adversarial review + Final Review ledger)",
  "hardening": "1 pass (2026-07-22) — closed the isOnAssignments orthogonal-dimension parity gap (the M204 final-harden block covered the 5 older manager predicates but not M243's net-new one) + empirically mutation-verified the read-back FLIP and the two Go honesty-teeth. 0 production bugs, 0 new failures. See progress.md '## M243: Hardening'",
  "deferral_audit": "YELLOW — 0 new M243 deferrals AND 1 standing carry RESOLVED Fate-1: M243 LANDED DEF-M235-03 / M204 assign-WRITE (the ~10-routing item, fresh-dated KEEP-DEFERRED-WITH-SIGNOFF 2026-07-20 with M243 as its named expiry 'land it or DROP'). The 4 remaining inherited items (M238-D5 standing-9 + DEF-M240-01 + DEF-M239-01 + M239-D13) stay already-fated -> M244 (now-adjacent terminal expiry). No RED, no blockers. Report: audit-deferrals/deferral-audit-2026-07-22.md",
  "close_fixes": [
    "NONE code-side — a clean close. The review artifacts produced: the deferral audit report, the decisions.md 'Adversarial review (close Phase 2c)' subsection (count-delta read-back vs pagination/re-sort [S1] + refetch-transient [S2] + antd keyboard mis-pick [S3], all verified fail-closed, no code change), and the progress.md '## M243: Final Review' ledger. Committed as corpus 6f87016"
  ],
  "delivered": {
    "assign_write_playthrough": "LANDED — pt-assignment-assign, the FIRST MUTATING Playthrough: a manager assigns a skill path to a member with a deadline and the assignment LANDS (a real public.organization_assignments row written via app.createOrganizationAssignments + read back through the real members surface as the assignable-affordance count dropping by exactly one). Fills the last in-manifest TODO — corpus 15 -> 16 live Playthroughs, 0 TODO",
    "page_object": "LANDED — e2e/lib/assignments-page.ts (AssignmentsPage), the first page object carrying MUTATING methods, same find-only landmark discipline (<main>-scoped table tbody tr; role/visible-text anchors; no CSS/testid); antd-v6 rc-virtual-list Select committed by keyboard (ArrowDown+Enter — D4)",
    "url_shape": "LANDED — ASSIGNMENTS_URL + isOnAssignments single-sourced in url-shapes.ts, segment-anchored so it matches /enterprise/assignments + its content sub-tabs but NOT an /enterprise/assignments-report look-alike (the M203 '-'-boundary hazard)",
    "precondition_lockstep": "LANDED — UC1 names seed.preconditions: [public-catalog, org-unassigned-member]; org-unassigned-member added to seed-worlds.yaml pt-world capabilities in lockstep (no seed CODE change — Org A already provides 34/40 unassigned targets). A future 'assign to everyone' seed change trips ptvalidate, not a mystery live failure",
    "delivers_docs": "corpus/ops/demo/playthroughs.md (count 15->16 / 0 TODO + the assign-WRITE subsection + the M243 page-object bullet) + README.md + CLAUDE.md count updates"
  },
  "notes": "Sixth and last of the v2.6 post-barrier fix fan-out (the one net-new hero journey); realizes the reserved M238; closes the ~10-routing DEF-M235-03 / M204 in-manifest TODO that had ridden 5 releases. A clean complete section close — 0 code fixes at close, all rext code unchanged from harden HEAD. This is where the release's anti-toothlessness thesis is sharpest: the FIRST Playthrough whose action mutates real state, so the read-back (affordance-count FLIP, not a closed modal) IS the proof. Only M244 (prove-on-billion, iterative terminal) remains in v2.6."
}
