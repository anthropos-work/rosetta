# M241 — Decisions

_(implementation choices with rationale accumulate here during build)_

- **User decision (2026-07-20): language → EN-only fallback per tuple.** M241 opens with a read-only prod pool-count query (IT sessions per requirement tuple); toggle where IT exists, EN-only where absent. No blocking. Recorded at design time; carried here for build traceability.
- **D-M241-1 (Section 1, the toggle model — set by the pool query).** The pool query revealed the current 13 pins are 11 IT + 2 EN, all mis-labeled `english` by the hardcoded `sessLanguageEnglish`. §2 fixes the label (each session → its real language). For the toggle: each **requirement tuple** `(sim_type, modality, passed)` carries its existing pin PLUS a counterpart-language variant **sourced via the existing `SelectionSQL` predicates** (public-anchored + type-matched + non-manager + completed; score-band 70–95 for passed cells) **IF that predicate returns ≥1 believable candidate**. Where it returns none (scarce/empty counterpart pool), the tuple is single-language and its toggle is hidden/disabled — the user-decision fallback. Confirmed single-language: **INTERVIEW/voice/fail (EN=0)**. This is a reproducible rule (the sourcing predicate decides), not an arbitrary subset. The cockpit toggle is a global EN|IT control that re-targets each tuple's login-and-land to the selected-language variant, falling back where absent.
- **PRE-EXISTING (surfaced in M241 §3, NOT M241-caused): 6 red `demo-stack/tests/test_cockpit.py` tests.** Confirmed identical failures at the release base `ae0e869` (ran the base cockpit.py against the suite). All in ACADEMY + OVERLAY-JS surfaces, none in the content-stories/language surface M241 touches: `TestOverlayJs.test_inflight_window_is_30s` + `test_localstorage_access_is_guarded` (M218 removed the 30 s window + a try-block; tests not updated), and 4 academy-link tests (`TestAcademyCatalogEntryEdges` ×2, `TestAcademyLink.test_academy_link_renders_per_hero_when_base_set`, `TestServedPanelWithAcademy.test_root_serves_academy_link` — M238/M239 academy changes). My §3 cockpit changes added **0 new failures** (128 tests, same 6). Flagged for the orchestrator: these belong to M238/M239/M244 surfaces (Fate-3), not M241 — fixing them here would be scope-creep into unrelated milestones. The M244 prove-on-billion closer or a bugfix should adopt them.
- **KB-1 (Phase 0b, YELLOW, incidental):** `session-clone-spec.md` §3 does not document that `content_stories_write.go` writes the session `language` (currently hardcoded `sessLanguageEnglish` → M241 flips to `cs.Language`). Add a one-line write-side language note in Phase 5. `content-stories-spec.md` is the milestone's Delivers target; the session-clone note is a discoverability cross-ref, not the primary delivery.
