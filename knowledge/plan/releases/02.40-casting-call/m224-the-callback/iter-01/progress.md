# iter-01 — progress

**Type:** tok (bootstrap) — protocol: `corpus/ops/demo/coverage-protocol.md` §"Iter type selection" (the
bootstrap tok authors the strategy + frames the baseline; does NOT terminate the call).

## What this iter did
1. Loaded the milestone context (overview/spec-notes/progress/decisions) + the render-loop protocol + the M222
   read-model doc + confirmed the M223 seeders write the mirror pair.
2. Created the milestone branch `m224/the-callback` from `release/02.40-casting-call`.
3. **Phase 0b KB-fidelity gate → GREEN** (report `../kb-fidelity-audit.md`). Fixed one stale-misleading pointer in
   `hiring.md` (isHiring target = FAPI `clerk-frontend/`, not BAPI) inline; recorded the topic→doc→code triples +
   the pinned wiring facts in `spec-notes.md`.
4. **Authored TOK-01** (milestone-root `decisions.md`): the **recruiter-render-first** strategy + iter-02's target.

## Close — 2026-07-16

**Outcome:** Bootstrap strategy authored (TOK-01, `new-direction`); KB-fidelity GREEN; baseline framed as
UNMEASURED (presumed 0 rows — surface never rendered on a local stack). No gate metric moved (a tok is strategy
work, not a fix attempt).
**Type:** tok (bootstrap)
**Status:** closed-fixed
**Gate:** N/A for tok
**Phase 5 grading:** (1) gate-met: n — (2) triggered-tok: n (this is a BOOTSTRAP tok — does not exit) — (3) re-scope: n — (4) user-blocker: n — (5) cap-reached: n — (6) protocol-stop: n — Outcome: continue (into iter-02)
**Decisions:** TOK-01 (milestone-root); D1 (iter-local: hiring.md FAPI-pointer fix as a Phase 0b applied fix)
**Side-deliverables:** hiring.md FAPI/BAPI wiring correction (a KB-fidelity applied fix, not a metric-lift fix).
**Routes carried forward:** iter-02 = enabling-scaffold + baseline-render tik (recruiter seat + Clerkenstein
isHiring wiring + render-probe + LOCAL demo-up + measure + attribute). The 2 candidate `/profile` heroes route to
a later tik (In-scope polish, not the gate metric). The drill-down `validation_*` seeding routes to whichever tik
needs it (only if the assessed candidate hero's surface reads it).
**Lessons:** The M223 seed already writes the score source; the render risk is therefore concentrated in the
render-GATE + the Clerkenstein identity (isHiring re-skin), not the data. The bootstrap tok deliberately front-loads
"reach + measure + attribute" so the first FIX lands against a measured gap, not a guess (the M219 trap).
