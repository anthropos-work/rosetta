# iter-03 — decisions (iter-local)

| # | Decision | Rationale | Date |
|---|----------|-----------|------|
| D1 | The recruiter hero is `vantage: manager` (→ `roleForHero`→admin), declared first so `personaUserIndexFor` rides her on slot 1 = the admin band. | An admin recruiter (a) reads the candidate-comparison scoreboard (inherits `org:feature:insights` from the p3 admin policy via her g2 grant — no net-new grant, per hiring.md), and (b) is correctly SKIPPED by the `HiringFunnelSeeder` (which auditions only `roleForIndex==candidate` slots) — recruiters don't take assessments. Slot-1 keeps `roleForHero`/`roleForIndex`/the funnel all agreeing. | 2026-07-16 |
| D2 | Add a dedicated `curatedTalent` (recruiting) skill family rather than mapping the recruiter to an existing family or exempting managers from the classifier guard. | A manager hero renders a **modest personal `/profile`** (persona.go: 3-8 verified + a claimed tail from her role's curated pool), so her role IS used to draw skills — `curatedNone` would give generic (general-family) skills and, more importantly, **fails the shipped-preset guard** `TestShippedPresets_EveryHeroRoleClassifies` (which requires a specific curated family per shipped hero). A recruiting family gives Rae coherent talent-acquisition skills. No existing family fits a recruiter without incoherence (sales≠recruiting). | 2026-07-16 |
| D3 | iter-03 lands **only the recruiter seat** (the gate-unlocking unit); the 2 candidate `/profile` heroes defer to a post-gate tik. | The gate is the recruiter scoreboard (≥40 rows/sim × 5), which needs the recruiter seat + isHiring + the M223 funnel (already seeds 45 candidates × 5 sims) — NOT candidate heroes (those are the separate `/profile` exemplar deliverable). The candidate heroes need intricate candidate-role + funnel-stage hero-awareness (a hero at an admin-band slot with an end-user vantage in a candidate-only org) — real work best done once the gate render is proven. Front-loads the gate (recruiter-render-first, TOK-01). | 2026-07-16 |
