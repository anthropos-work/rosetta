# iter-02 — decisions (iter-local)

| # | Decision | Rationale | Date |
|---|----------|-----------|------|
| D1 | Emit org `public_metadata.isHiring` **only when true** (omit the key otherwise), rather than always writing `isHiring: <bool>`. | The alignment goldens are captured from the existing (non-hiring) identities, whose org public_metadata is `{eid}`. Always-writing `isHiring:false` adds a key → changes the `shape`-graded `Client/signed-in` + `Me/universal-user` responses → risks an `/align-run` RED (or forces a golden re-capture). Conditional-emit keeps every non-hiring shape byte-identical → both gates stayed 100%/100%. | 2026-07-16 |
| D2 | Do NOT extend the **BAPI** (`clerk-backend`) to emit `isHiring` this milestone. | Phase 0b established the server derives hiring from the `public.organizations.is_hiring` **DB column**, not Clerk BAPI metadata; the browser re-skin (the render-relevant path) reads the **FAPI**. A BAPI change is optional + adds align surface (the Go-SDK DNA) for no render benefit. Documented as optional. | 2026-07-16 |
| D3 | Decompose TOK-01's "enabling-scaffold + baseline-render" iter-02 into **iter-02 (isHiring wiring), iter-03 (hero seats + funnel-awareness), iter-04 (bring-up + probe + baseline)** — same TOK-01 strategy, no re-scope. | Re-survey showed that framing is genuinely 2–3 iters of work (an align-guarded wiring + a seeder/funnel change + a heavy Docker bring-up + a new render-probe). The scope-creep discipline: finish a clean, independently-verifiable unit and route the rest. iter-02's isHiring wiring is self-contained + carries the milestone's single highest-risk guarded change (`/align-run`), so it is the right first unit. | 2026-07-16 |
