# M225 — Spec notes

_Technical detail accumulated during the milestone: file:line maps, command transcripts, measured numbers._

## Auto-set-dress: job_position replay
_Folding the `directus.job_position` replay + 5-sim capture into the default `/demo-up` bring-up pass._

## Hiring coverage manifest
_Wired into `manifestFor(vantage, expectedOrg, identityKey)` (org-conditional dispatch); candidate persona
self-consistency (role↔skills↔score); the compare-surface sections; 0 prod-eject._

## Hiring playthrough + pt-world
_`playthroughs/manifest/hiring.yaml` (recruiter compares candidates on a shared sim); the hiring org into the
decoupled `pt-world` seed; the GREEN run record._
