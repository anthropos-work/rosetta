---
iteration_type: tok
tok_flavor: bootstrap
status: closed-fixed
---

# iter-01 — bootstrap tok

**Type:** tok (bootstrap — iter-01 of M228). Authors TOK-01, the initial strategy the M228 tiks follow. Does NOT
terminate the invocation; the loop continues into iter-02 (a tik) under TOK-01.

## Inputs
- The milestone `overview.md` (the retuned 7-condition gate + the 4 M227-correction render checks), `roadmap.md`
  M228 entry, the protocol doc (`verification.md` + `coverage-protocol.md` + `latency-budget.md`).
- The **M226 proven billion recipe** (`../m226-opening-night/`: TOK-01 + iter-02..05 + retro): the exact
  teardown→cutover→default-cold-up→measure-from-Mac→attribute→fix-via-tooling flow + findings F1–F5.
- The **billion recon** (this iter) — see `../spec-notes.md § billion recon`.

## Phase 0b — KB-fidelity
GREEN (see `../kb-fidelity-audit.md`). The 4 M227 corrections aligned across corpus + `casting-call-m227-sections`.

## Strategy authored
`TOK-01: reprove-corrected-hiring-on-billion` — recorded in `../decisions.md`.
