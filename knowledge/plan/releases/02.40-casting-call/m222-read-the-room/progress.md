# M222 — Progress

_Section checklist, derived from `overview.md` § Scope.In. To be worked by `/developer-kit:build-milestone`._

## Sections

- [ ] **S0 — KB-fidelity gate** (pre-milestone; `/developer-kit:audit-kb-fidelity`)
- [ ] **S1 — Author `corpus/services/hiring.md`** (the BLIND AREA — the hiring model, before any code)
- [ ] **S2 — The `is_hiring` gate thread** (blueprint `Org.IsHiring` field + `narrative: hiring` + `org.go`
      one-value change + the deterministic hiring `OrgID`)
- [ ] **S3 — Render-probe BA-1/BA-2/BA-3/BA-6** (throwaway hand-seed vs the live dockerized `apps/web`)
- [ ] **S4 — Record the GO/NO-GO + the exact seeder-output contract** (score from `sessions.score` alone, or a
      `validation_*`/eval row per session?) — **the make-or-break go/no-go; ESCALATE if `apps/hiring`-only**
- [ ] **S5 — Registrations** in `seeding-spec.md`/`stories-spec.md`/`README.md` (the 4th story + the `is_hiring` gate)
