# iter-08 — decisions

## D1 — the M236 Fate-3 handoff is the user-authorized route for ALL live-coupled work
The run-3 ruling authorized folding the live proof + the new coverage plumbing + the M230 carry-forward live
items into M236 (Fate-3). This iter EDITS `m236-prove-on-billion/overview.md`'s `In:` list to own them
explicitly (not "add a manifest section" — AUTHOR the seat-login coverage/Playthrough plumbing + CALIBRATE
against a live render + run the (session×action)-lands proof) and RECORDS the resolution + handoff in the
M235 milestone `decisions.md`. This is the correct three-fate mechanism (annotate-attach the target's plan).

## D2 — cleanup-iter shape: 0 production seeder code, doc + plan routing only
iter-08 modifies NO rext production code — the 3 non-sim sections are already built (iters 05–07). Its work
product is process-quality: the M236 plan edit, the milestone resolution record, and the corpus doc sync
(`content-stories-spec.md` §2+§6, `content-stories-routes.md` §7 — the non-sim sections are now BUILT, the
live proof is M236). So it commits ONLY to the rosetta repo (no rext tag).

## D3 — the milestone pragmatic-closes; the offline scope is exhausted
After this iter every offline-buildable non-sim surface under TOK-01 is landed + unit-proven. The only
remaining gate items (the live browser proof + the per-section live calibration) legitimately require a
running stack → M236. Per the user's "then close" mandate the milestone PRAGMATIC-CLOSES; EXIT_REASON:
protocol-stop. No live gate faked; no platform-repo edit.
