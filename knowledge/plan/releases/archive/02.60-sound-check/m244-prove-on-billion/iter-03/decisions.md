# iter-03 — decisions (local)

- **D1 — reclassify iter-03 as a TOOLING-ITER.** The planned target ("run gate b") hit a broken pipeline (the runner won't parse on the macOS peer), so per the protocol's tooling-iter shape the iter ships the tooling fix AND uses it in the same iter. 2 planned lines (fix runner → run sweep), not scope creep.
- **D2 — per-pair warm RETRY over an ever-higher flat settle.** The tailnet cold-render flakiness could be masked by a huge settle budget, but a flat 20s made the 49-pair sweep ~28 min for the same result a reload-retry gets in ~6 min. The retry (reload → re-visit hot) is the robust, cheap fix; the base settle stays tight (12s). Both env-gated (`CONTENT_SETTLE_MS`, `CONTENT_PAIR_RETRIES`), localhost unaffected.
- **D3 — the 3 residuals are DETERMINISTIC, confirmed by re-run diffing.** Flaky = the failing SET changes run-to-run (most of the original 12 did). Deterministic = the same SET persists across configs incl. 2 retries (these 3 did). All 3 have full DB result content → a render issue on voice/interview result surfaces, routed forward (gate g + a voice-presence-only decision), NOT patched blindly here.
- **D4 — 0 platform edits.** All fixes are in rext `stack-verify/e2e` (the sweep harness). The residual render issue, if it needs a platform-source touch, routes to a sha-pinned demopatch or ESCALATES — never a platform-repo edit.
