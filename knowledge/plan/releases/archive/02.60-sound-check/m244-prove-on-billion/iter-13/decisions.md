# iter-13 — decisions

## D1 — probe-first turned the preferred fix into its falsification (TOK-02 fallback taken)
TOK-02's PREFERRED fix (scope the `next-web-interview-flag-container` FETCH demopatch to `isManagerScope`, mirror iter-08) rested on the hypothesis that the unscoped container fetch makes the interview PLAYER block on a slow extraction fetch. Both a static source read and a live billion network+DOM probe DISPROVED it: the extraction fetches feed only props (not the `BaseLoading` gate), the extraction legs return 200 and are irrelevant to ack timing, and the ack paints at ~5.4 s (warm) regardless. So the demopatch would have re-baked two images for zero effect. The mandated probe-first is exactly what saved that. Took TOK-02's explicit FALLBACK: shape-aware settle-robustness in the sweep runner.

## D2 — the fix is HARNESS-side, so NO billion re-pin / re-bake (deviation from TOK-02's cost plan, justified)
TOK-02's run-6 plan assumed the demopatch path and budgeted a 20-50 min re-bake of next-web + hiring on billion. The actual fix lives in `stack-verify/e2e/lib/content-result-page.ts` — the content-stories HARNESS, which runs on THIS workstation and drives billion's UNMODIFIED app over the tailnet. It is not a billion build input, so gate (b) is discharged with NO billion re-pin and NO image bake. Billion's build-time rext pin is deliberately left at its run-5-era commit (build-irrelevant to this fix); the consumption tag on origin is moved so the tooling-under-test is current. A billion re-pin will come naturally at gate (d) (the academy demopatch, a real build input).

## D3 — shape-aware, player-interview-only (minimal blast radius) + mutation-verified teeth
`settle()` gains a `contentReady(shape)` guard that only gates `player-interview` (its terse ack renders in `<body>` outside `<main>`, and the nav shell alone clears the 120 floor). All five other shapes render substantial `<main>` and stay `contentReady === true` — behaviour identical, pinned by a `player-scored` blast-radius test. The ack regex is hoisted to one module const so settle() and assert() cannot drift. Teeth proven by mutation: removing the guard fails the REGRESSION + never-paints specs (2 of 4); the fast-path + blast-radius specs correctly stay green (they don't depend on the guard).
