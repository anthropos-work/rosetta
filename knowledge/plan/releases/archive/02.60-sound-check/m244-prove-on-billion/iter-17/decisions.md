# iter-17 — decisions

## D1 — run the remote-capable discrete specs now; route the localhost-only retrofit rather than hack it in on the final tik
gate (c)'s "24 stack-verify specs" are heterogeneous: only 3 (persona-check, m224-candidate-heroes, probe-navigation) carry a host env and can target billion's tailnet HTTPS today; the rest were authored localhost-only. Two choices: (a) start retrofitting host+scheme into the localhost-only specs now, or (b) run the driveable ones + map the retrofit as a routed finding. Chose (b): the retrofit is a rext tooling effort across ~9 specs (each needs a host/scheme var + the https-fapi handling M219 established) that cannot land cleanly on a single final tik, and starting it half-way would leave the tree mid-retrofit at the cap. Ran the 2 gate-relevant driveable specs (both GREEN — persona + hiring vantages proven live) and documented the retrofit as the concrete run-7 gate-(c) task. This mirrors iter-14's "measure before sequencing" discipline: the map is the deliverable, and it turns a vague "run the discrete specs" into a named tooling job.
