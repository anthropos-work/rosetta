---
iter: 09
milestone: M244
iteration_type: tik
status: closed-no-lift
active_strategy: TOK-01
created: 2026-07-22
---

# iter-09 — gate (d): measure the anon academy /library+/free twin (verifiable without the gate-h re-seed)

**Type:** tik under **TOK-01**. Target: gate **(d)** — "the anonymous academy `/library`+`/free` twin renders
real cards (S-1)." Chosen because it is verifiable against the CURRENT billion (m243) — the academy render is
independent of the m244 content-story changes, so it needs no cold reset-to-seed. (The gate-(h) cold
reset-to-seed itself — the critical path for gates b-live/c/f/h — is a large op deferred to a fresh run.)

## Acceptable close-no-lift outcome
A complete measurement of gate (d) with a documented finding — even if the twin does not both-render — is a
first-class outcome (the protocol working: measure → characterize → route).
