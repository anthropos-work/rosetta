# iter-05 — decisions (local)

- **D1 — fix at the CAPTURE surface, not a seed.** `directus.simulations_extraction` (the interview extraction PLAN) is public sim-definition content that BELONGS in the snapshot capture (like `simulations`, `roles`, `sim_tasks`). It was simply omitted from the directus surface. So the correct, durable fix is to add it to the capture surface (parent-scoped to public sims), NOT to special-case-seed it in the ContentStorySeeder. This makes EVERY demo's interview reports render, not just content-story sims. 0 platform edits.
- **D2 — manual data-load as a partial proof; the live capture-path is routed.** billion has no prod access (can't capture) and the demo Postgres isn't reachable from this workstation (can't replay remotely), and no local psql exists. To PROVE the plan is load-bearing without a full re-pin+re-capture+re-replay cycle, I pulled the real plan from prod (postgres MCP → saved file → base64 → inline-SQL insert) into the demo's Directus and observed the render change (0→520). This proves the DATA fix; the live capture-path (my committed fix, exercised end-to-end) is routed to the next cold bring-up / a re-pin cycle.
- **D3 — the intv-voice-pass/player residual is SEPARATE.** Re-running gate (b) after the plan load stayed 46/49, confirming the player ack-page failure is unrelated to the extraction plan. Routed as its own item (likely a sweep-assertion mismatch: a PASSED interview renders the result shell, not the ack regex the player-interview shape expects).
- **0 platform edits.** All work is rext tooling (stack-snapshot capture surface) + a manual verification data-load (not a shipped artifact).
