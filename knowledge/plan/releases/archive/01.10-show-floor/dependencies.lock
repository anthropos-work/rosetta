# v1.1 "show floor" — dependency lockfile (supply-chain record, 2026-06-05)
# What was actually in this release, for future supply-chain incident response.
# All deps are Go modules (resolved + hashed in each module's go.sum). No npm runtime deps
# (the @clerk/express gate npm-installs the SDK at CI time; not shipped). Licenses: BSD/MIT/Apache (permissive).

## rosetta/test/alignment (the alignment framework)
stdlib-only — no third-party modules.

## rosetta-extensions/stack-seeding (the v1.1 seeding stack — direct deps)
github.com/jackc/pgx/v5        v5.9.2     # MIT — Postgres driver (COPY); the platform's own driver
gopkg.in/yaml.v3               v3.0.1     # MIT/Apache — blueprint parsing
# indirect: jackc/pgpassfile, jackc/pgservicefile, jackc/puddle/v2, golang.org/x/{sync,text} (all permissive)

## rosetta-extensions/clerkenstein (the Clerk mock — direct deps)
github.com/anthropos-work/colony      v0.34.3    # private (platform) — the disarmed-authn deploy target
github.com/clerk/clerk-sdk-go/v2      v2.6.0     # MIT — the source SDK the Go gate measures against
github.com/google/uuid                v1.6.0     # BSD-3
github.com/svix/svix-webhooks         v1.93.0    # MIT — svix (webhook signing) the mock mirrors
# @clerk/express ^1.3.47 (MIT) — npm-installed at CI/gate time, NOT shipped in the repo

# CVE audit: no known high/critical CVEs in the above at release time (all current/recent versions).
# Integrity hashes: see each module's go.sum (rosetta-extensions/{stack-seeding,clerkenstein}/go.sum).
