# iter-02 decisions

| ID | Decision | Rationale | Date |
|----|----------|-----------|------|
| D1 | Fix `run-playthroughs.sh` to NOT pass `--reporter=list` + reconcile inline with `ptreport` after the run (in-scope Fate-1). | A Playwright CLI `--reporter` flag REPLACES the config's whole reporter list, so `--reporter=list` silently suppressed the config's `['json',{outputFile:./report/last-run.json}]` reporter → `last-run.json` stayed stale → `ptreport` (the four-state reconciliation the milestone gate reads) was decoupled from the actual run. This is load-bearing for M204's gate (which needs ptreport over 5 reset runs) and was a latent M202/M203 defect. The fix is small + correct + verified (fresh JSON, manager UCs reconcile `[PASS]`). Documented as a foundation lesson in `playthroughs.md`. | 2026-07-02 |
| D2 | Manager UCs use product id `workforce-intelligence` + the exact M201-corpus story/UC ids (`skills-funnel.UC1`, `roster.UC1`); `actor.hero: pt-manager`, `actor.entitlement: enterprise`, `seed.world: pt-world`. | Faithful to the M201 manifest corpus (the build+regression contract) — the coverage milestone implements against the declared ids verbatim so traceability holds both ways. `pt-manager` + `enterprise` resolve in `seed-worlds.yaml` (precondition-coverage green). | 2026-07-02 |
| D3 | Funnel anchored on the WI SPA default landing view (`Skills Mapped`/`Skills Verified` stat labels + `% of mapped` coverage + a `<main>` svg), NOT a tab route. | Probe finding: the WI dashboard tabs (Growth / Skills & Verification / …) are client-side state — the URL stays `/enterprise/workforce`. The mapped→verified funnel + org-scale gap render on the default view. Anchoring on visible stat labels (§5.2 sanctioned antd landmark) + chart presence asserts the OUTCOME (P2), never exact org-scale counts. | 2026-07-02 |
