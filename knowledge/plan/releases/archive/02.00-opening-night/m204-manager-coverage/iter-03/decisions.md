# iter-03 decisions

| ID | Decision | Rationale | Date |
|----|----------|-----------|------|
| D1 | `drillIntoActiveContent()` waits on `page.waitForURL(ACTIVITY_DRILLDOWN_URL)` after the row click, not a synchronous `page.url()` read. | The drill is a client-side SPA navigation → `page.url()` lags the click. The first run read the stale landing URL (a green-but-wrong-adjacent race — the page HAD navigated by the failure snapshot). `waitForURL` auto-waits for the segment-anchored drill-down route, so the caller's assertions land on the settled detail. | 2026-07-02 |
| D2 | `memberResultRows()` scopes to the PAGE-level single results table (`page.locator('table tbody tr')`), NOT `main()`. | Probed: the drill-down detail renders its per-member results table OUTSIDE `<main>` (the page has 2 `<main>`; `table.closest('main')` is false; the table sits in a plain-div layout, unlike the profile/roster surfaces which ARE in `<main>`). The detail carries exactly ONE `<table>` (the breakdown), disambiguated by the surface (we're on the drill-down route, under the "Simulation Results" heading + User column) — so a page-level table anchor is correct + unambiguous here; `main()` scope found the wrong/empty main. Within §5.2 (scope-within-a-named-surface, disambiguate by visible landmark). | 2026-07-02 |
| D3 | `assignment-monitoring.assign-and-track.UC1` (the assign WRITE half) declared with `playthrough: TODO` (a build-reference gap), NOT built. | UC1 is a two-backend org-admin WRITE flow — out of M204's declared 3 manager journeys (which cover the MONITORING/drill-down half, UC2). Declaring it `TODO` keeps the manifest an honest build reference (P5) without failing the suite (a `TODO` is `unimplemented`, not `failing` — `NoRegressions()` gate unaffected). Fate-2: covered by the manifest as a tracked gap. | 2026-07-02 |
