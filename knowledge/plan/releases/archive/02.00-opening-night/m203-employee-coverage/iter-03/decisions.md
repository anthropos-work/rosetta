# iter-03 decisions

| ID | Decision | Rationale | Date |
|----|----------|-----------|------|
| D1 | **Substitute the iter target** from `profile.self-evaluation.UC1` (iter-02's route-forward) to `profile.timeline.UC1`. | Phase 1 Step 0 re-survey: the M203 exit gate's Profile journey is "verified-skill chart + claimed-vs-verified gap + **work/education timeline**". The timeline is the third GATE-relevant Profile piece and was uncovered; self-eval is a non-gate M201-corpus extra. Landing the gate-relevant piece first is the higher-value move under the same TOK-01 strategy (still a deterministic read). NOT a re-scope — the strategy holds; only the named next-target shifted. | 2026-07-02 |
| D2 | Route `profile.self-evaluation.UC1` forward again (a non-gate extra) rather than force it in iter-03. | The self-rate control's click is intercepted (the "Rate your skills…" modal is in the DOM but stays hidden on click — a pointer-events/overlay actionability quirk). Solving it cleanly needs its own iter (a real user-gesture / actionability fix, OR asserting at the interactive-state boundary). It's not one of the 3 gate journeys, so it doesn't block the gate. Fate-3, handler PT-M203-selfeval. | 2026-07-02 |
| D3 | Assert the timeline via **section presence (Work/Education) + ≥1 real dated entry** (a "Mon YYYY - (Present\|Mon YYYY)" range), never job titles/employers/dates. | P2: the observable outcome is "the work/education timeline rendered with real entries" — the sections + a populated dated entry. The specific history (Meridian Labs, Drift Analytics, Edinburgh) is seeded content that varies → forbidden to assert. antd renders the section labels as text (no headings/testids), so scoped-within-<main> visible-text is the sanctioned found-only landmark (§5.2). | 2026-07-02 |
