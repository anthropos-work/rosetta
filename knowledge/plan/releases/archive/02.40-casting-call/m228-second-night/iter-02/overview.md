---
iteration_type: tik
status: closed-fixed
---

# iter-02 — first corrected-data cold bring-up + measurement (tik)

**Active strategy:** TOK-01 `reprove-corrected-hiring-on-billion`.
**Cluster / target:** the FIRST live billion measurement of the M227-corrected data — teardown the M226 demo, cut rext
→ `casting-call-m227-sections`, default cold `up-injected.sh 1`, measure the retuned 7-condition gate + the 4
M227-correction render checks from this Mac, and ATTRIBUTE every gap to its surface (TOK-01 steps 1–5).
**Hypothesis:** the corrected data (1-sim/candidate, external emails, hiring-only, matched avatars) renders live; the
last breakages are cross-machine (M215/M221) — attribute before fixing.
**Expected lift:** 0/7 → measure the true gate distance; findings routed to iter-03.
**Phase plan:** teardown → cutover → default cold up (synchronous) → measure C1–C7 + fix#2/#4 from this Mac → attribute.
**Escalation conditions:** an un-patchable platform-render gate → escalate; a bring-up that never comes up → user-blocker.
**Acceptable close-no-lift:** n/a — the measurement itself is the deliverable.
