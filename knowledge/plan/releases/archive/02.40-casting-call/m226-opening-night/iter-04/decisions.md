# iter-04 — intra-iter decisions

| # | Decision | Rationale | Date |
|---|----------|-----------|------|
| D1 | **Finding-2 fix = a one-line preset bump** `role_mix.admin 0.1 → 0.14` (candidate 0.9 → 0.86) in the hiring story, NOT a hero-slot move or a seeder-logic change. | The gate is explicit (exactly 5+45) and the gap is patchable (rext preset), so fix it (not escalate). The bump makes the admin band int(50×0.14)=7 slots → 5 real admins survive the 2 candidate-hero overrides (Cara/Cody at slots 2,3). SAFE: no hero moves (AuthID/Eid derive from slot idx — untouched), no role changes (Cara/Cody MUST stay candidates for the funnel), Rae still slot-1 (the assigner/firstAdminMembership — hiring_config/funnel unaffected). A hero-slot move would ripple into hero identities; a seeder-logic compensation would affect all orgs — both over-engineered for the gate. | 2026-07-17 |
| D2 | Update `presets_test.go` to assert `admin==0.14` + `int(size*admin)==7` (the 7-slot band → 5 net admins), not the old `0.1/0.9`. | The old test asserted the ratio (0.1) as a proxy for "5 admins", but 0.1 never produced 5 admins (the exact bug). The new assertion documents WHY 0.14 (the 2-candidate-hero compensation) so a future edit can't silently revert it. Seeding Go tests green. | 2026-07-17 |
| D3 | Prove both fixes via a **full DEFAULT cold re-bring-up** (`down 1 --purge` → `up-injected.sh 1` NO FLAGS), not a surgical re-apply. | The gate demands reproducibility on a DEFAULT `/demo-up` — the serve fix (Finding-1) must front :13001 IN THE DEFAULT PATH (not via a hand-run serve re-apply) and the seed must produce 5+45 from a cold reset-to-seed. Only a full default bring-up proves that. | 2026-07-17 |
| D4 | **Finding-3** (:13001 orphan bind-conflict on re-bring-up #1) = an ARTIFACT of iter-03's surgical serve re-apply, NOT the default flow. Recovered by clearing the orphan; the default flow self-heals (proven on re-bring-up #2). Route a hardening nice-to-have (pre-bind reap clears stale serve fronts on offset ports). | The surgical re-apply added :13001 to the live serve but not the on-disk reset plan; the teardown's stale plan didn't clear it. A full default bring-up writes a reset plan including :13001 → subsequent teardown→bring-up cycles are clean. Not gate-relevant. | 2026-07-17 |
| D5 | **Finding-4** (C2 harness insights-capture race on a fresh stack) — fix the harness robustness (poll/extend the `insightsByJobSimulations` capture wait) in iter-05, NOT a product change. | The `insightsByJobSimulations` query fires + returns the 5 sims (2/5 runs captured list=5 + PASSED 42×5; C3-Rae renders the Results scoreboard; DB shows 42/sim). The harness sometimes misses the capture window on a cold stack — a measurement-reliability flake (M215/M221 "the harness is the fragile thing"), not a product regression. Needs fixing so the reproducibility 2nd cycle's C2 is reliably automated-green. | 2026-07-17 |
