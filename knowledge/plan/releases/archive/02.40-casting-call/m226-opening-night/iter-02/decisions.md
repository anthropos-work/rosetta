# iter-02 — intra-iter decisions

| # | Decision | Rationale | Date |
|---|----------|-----------|------|
| D1 | Pin billion's WHOLE platform clone set to the local M225 `stack-demo/clones.lock.json` SHAs before bring-up (faithful reprove substrate). | The local GREEN M225 proof built next-web-app @ `23bdbb5db` (`v2.106.1-1`), the EXACT ref the 4 demo-patches' `pre_sha256` target. billion is at `v2.108.0` (2 minors ahead) → sha-drift (patches would lean on anchor self-heal) + a next-web-app/app version-skew GraphQL schema-mismatch risk. Pinning the whole set to the lock SHAs gives a consistent snapshot = the local green substrate = REPROVE-at-final-code faithful. `--purge` keeps the clones (only wipes demo data), so re-pin after teardown. | 2026-07-17 |
| D2 | Cut billion's rext consumption clone + `~/panorama/.agentspace/rext.tag` over to **`casting-call-m225-harden`** (verify `sections`↔`harden` test-only first). | rext.tag SoT is `~/panorama/.agentspace/rext.tag` = `cue-to-cue-m221-r4`. `-harden` is the code-of-record (REPROVE-at-final-code); confirm the harden pass added only tests (no runtime change) vs the `-sections` consumption tag. | 2026-07-17 |
| D3 | Teardown the stale v2.3.2 `demo-1` with `rosetta-demo down 1 --purge` (reaps containers + cockpit :17700 + academy :13077 + serve). | 7.3 GiB RAM can't co-host two full 2-UI stacks; the stale demo squats the base ports + serve config. Cold reset-to-seed wants a fresh DB → `--purge`. Reuse N=1. | 2026-07-17 |
| D4 | **Finding-1 fix = add `("hiring", 3001)` to `gen_tailscale_serve.py` `UI_BROWSER_FACING`** (+ the recruiter latency vantage in run-latency.sh). Committed+tagged `casting-call-m226-serve-hiring`, APPLIED+PROVEN in iter-03. | The hiring 2nd app (M224) was never fronted over tailscale-serve → :13001 unreachable from a peer → the recruiter/candidate conditions (2,3,5) BLOCKED cross-machine. Tooling-only, 0 platform edits. In UI_BROWSER_FACING (not its own axis) because its lifecycle == next-web's (dropped under --no-ui). only_ports=None on the demo path, so the registry add takes effect. Empirically confirmed + sanity-checked (107 serve tests pass). | 2026-07-17 |
| D5 | **Finding-2 = the hiring org seeds 3 admin + 47 candidate, not exactly 5+45** — routed to iter-03 for a seeder-fix-vs-gate-wording decision (leans seeder-fix: the gate is the contract). | The 3 hiring heroes occupy population slots 0,1,2 (admin-index range); Rae(0)=admin but Cara(1)+Cody(2) are candidate heroes → override 2 would-be-admin slots → 3+47 deterministically. The seed preset's own stated intent ("→ 5+45 at size 50") is NOT achieved — by its own doc this is a bug, not just a gate-wording nit. Functional intent (recruiters read the scoreboard; 45+ candidates audition) is met. | 2026-07-17 |
