# M44 Decisions

Implementation decisions with rationale (recorded during build). Design-time decisions live in
[`overview.md`](overview.md) + the research note
[`.agentspace/scratch/roadmap-research-2026-06-26.md`](../../../../.agentspace/scratch/roadmap-research-2026-06-26.md).

| ID | Decision | Rationale | Date |
|----|----------|-----------|------|
| D1 | Non-thriving hero: keep a **minimal 2–3 verified** set (not truly-zero) but write **NO claimed-side `user_level`** for the self-rating-incomplete state | The Skill-Spotlight chart hides below 2 datapoints (`SkillScoreChart.tsx`); 2–3 keeps it rendering (visibly sparse, not broken) while the absent self-rating shows the "hasn't done the initial self-assessment" story. Resolves Open-Q1. | 2026-06-26 |
| D2 | Manager trajectory: **flat current-state** (3–8 verified L1–L2 skills, no growth arc) | The user's explicit default; a manager demos a real-but-modest personal profile, not an improvement narrative. Resolves Open-Q2. | 2026-06-26 |
| D3 | Bulk-member depth applies to **EVERY member** (avatar already present; + a shallow career: 3 short-tenure experiences, 1 education, ~6-skill claimed tail) | The user wants `/enterprise/members` to look full. Depth stays shallow by design (deep narratives are M46/LLM). Resolves Open-Q3. | 2026-06-26 |
| D4 | **Shared `ProfileSeeder` path** extended in-place (not a separate seeder) for managers + bulk members, branching on role/hero-kind | Reuses the company/timeline/skill machinery + the closure gate + the isolation/audit pattern; one seeder keeps the DAG simple. Resolves Open-Q4. | 2026-06-26 |
| D5 | Projects surface name = **`"projects"`**; certificates = **`"certificates"`** | Verified collision-free against the registry (`main.go:239-256` surfaces: org, users, identity, jobsim_sessions, skillpath_sessions, assignments, taxonomy, content, activity, personas, profiles, membership_skills, tags, target_roles, succession, feedback, population_evidence, hero_activity). Resolves Open-Q5. | 2026-06-26 |
| D6 | **Avatar half of §D already satisfied** — `photoAvatarDataURI` runs for every population user since M42e P4 (`users.go:156`) | Surfaced in Phase-0b; not a new write. §D's remaining work is verification + the bulk-member shallow career. Fate-1 already-landed (no re-do). | 2026-06-26 |
| KB-1 | Phase-0b KB-fidelity verdict: **GREEN** (0 blind areas, 0 stale load-bearing claims) | The "managers skipped" doc claims are current-truth M44 §C deliberately changes (a Delivers), not staleness. Report: `kb-fidelity-audit.md`. | 2026-06-26 |
| D7 (build addendum — supersedes D6) | **`/enterprise/members` reads `public.memberships.picture_url`, NOT `users.picture`** — fill BOTH | D6 was render-WRONG: a render-check found `/enterprise/members` showing silhouettes even though `users.picture` was 340/341 full. The page renders `<NamedAvatar src={member.pictureUrl}>` → GraphQL `pictureUrl` resolves `_Membership_pictureUrl → obj.PictureURL` = `memberships.picture_url` (was 0/341). Fix (rext tag `method-acting-m44-profile-completeness-fix1`): `UsersSeeder` writes the SAME `photoAvatarDataURI(uid)` into `memberships.picture_url` for every member — fresh seed via the COPY (`memCols += picture_url`), re-seed via an idempotent UPDATE backfill (`backfillMembershipPictures`, since `CopyRowsIdempotent`'s `ON CONFLICT (id) DO NOTHING` can't heal an existing row). Render-verified on demo-3: `memberships.picture_url` 0→340/341, 0 mismatch vs `users.picture`, 20/20 rows show photos, 0 silhouettes. Zero platform edit, no new deps. | 2026-06-26 |
