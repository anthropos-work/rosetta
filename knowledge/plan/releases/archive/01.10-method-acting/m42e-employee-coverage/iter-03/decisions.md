# iter-03 decisions

| ID | Decision | Rationale | Date |
|----|----------|-----------|------|
| D1 | **Adopted the prior-attempt rext WIP as this tooling-iter's deliverable** (uncommitted `crawl.ts` + `coverage.spec.ts` changes): `networkidle`->`domcontentloaded` + bounded settle, inline screenshots (`onPage` hook), seed-vs-discovered scoring. Committed + tagged it (`method-acting-m42e-iter03`). | The WIP was precisely the D2/D4 fix iter-02 routed forward; it was never committed (so it is iter-03's in-flight work, not a discard). Per Phase 4 Step 0, a tooling-iter ships the capability AND uses it; adopting + committing the authored fix is exactly that. NOT a forbidden discard of state — it is committed, not reset. | 2026-06-25 |
| D2 | **The fix works: re-sweep `(failing=8, escapes=1)` over reachable=80, down from `(44,1)`.** 36 of the 44 baseline "failures" were the `networkidle` flake. | Phase D re-measurement against live demo-3. The TRUE residual is 8 genuine failures + 1 real escape (D3-D5). The metric moved `(44,1)` -> `(8,1)`. | 2026-06-25 |
| D3 | **1 of the 8 is a harness false-positive (`/`), not a page failure.** The root has no `<main>`, so the assertion read the whole `<body>` (189016 chars incl. an inlined Next.js i18n JSON blob); the error-sentinel `"Something went wrong"` matched inside the `errorSettingUpGPTRealtime` **translation string**, not a rendered error. Routed to iter-04 (assertion-tuning): sentinel-check the visible main region only / normalize-or-drop the `/` root. | Probe-confirmed (a direct Playwright probe found `hasMain=false` + the sentinel match inside serialized JSON). Folded into `coverage-protocol.md` measurement conventions (protocol-evolution). | 2026-06-25 |
| D4 | **5 of the 8 are `/sim/.../result/<uuid>` pages rendering `empty (text=0)`.** Per-session sim **result** deep-links. Routed forward (Fate-3, iter-04, handler `SEED-M42e-sim-result-pages`): fix surface = `stack-seeding` (the result data) OR crawl-scope (per-UUID result deep-links may be out-of-vantage and should be excluded from the reachable set). | A real empty-page cluster discovered by the deeper (now-complete) crawl. The seed-vs-scope question (is a per-session result page a coverage commitment for the employee vantage?) is iter-04's triage. | 2026-06-25 |
| D5 | **2 of the 8 are empty skill-path index pages** (`/skill-path/prompt-engineering-fundamentals`, `/skill-path/foundation-of-artificial-intelligence`) — `empty (text=0)` while ~20 sibling skill-path indexes rendered full (2k-24k text). Routed forward (Fate-3, iter-04, handler `SEED-M42e-empty-skillpaths`): fix surface = `stack-seeding` / `stack-snapshot` serve-grant (these two skill-paths' content was not replayed/seeded). | A real content gap in 2 specific skill-paths, distinct from the harness cluster. Leverage-first (TOK-01): iter-04 targets the larger of the two empty-page clusters (the 5 sim-results vs the 2 skill-paths) first. | 2026-06-25 |
| D6 | **The 1 escape persists** (`strategy-business.com` in the feedback skill-path chapter, D3 from iter-02). Still routed forward (handler `ESCAPE-M42e-skillpath-external-article`). | Unchanged by the harness fix (a content link, not a wait-strategy artifact). | 2026-06-25 |
| D7 | **Pre-existing tooling debt noted:** the `typecheck` npm script references `tsc` but `typescript` is not a devDependency. Not introduced this iter; compile-validation uses `playwright test --list` (esbuild) instead. Route forward (low priority) — does not block the sweep. | Observed at iter open; recorded so it isn't mistaken for an iter-03 regression. | 2026-06-25 |
