# iter-09 decisions

| ID | Decision | Rationale | Date |
|----|----------|-----------|------|
| D1 | Seed a Sentinel Casbin **g3 feature grant** per membership (`FEATURE_JOB_SIMULATIONS`) in `stack-seeding` — a new `seedCasbinFeatureGrants` helper in `identity.go` (reusing `resolveCasbinTable` + the idempotent WHERE-NOT-EXISTS `casbinGrantSQL`), wired per-member in `users.go` + for the demo identity in `identity.go`. | **Corrects iter-08 D2's dishonest sim-start skip.** The `/sim/<slug>/start` page renders the org-member deny modal (empty `<main>`) when `userMembership.organizationFeatures` lacks `FeatureJobSimulations`. That field is resolved by `app` (`resolver_organizations.go:149` → `OrgManager.IsMemberAllowedToUseFeature` → `authMan.OrgMembershipsAllowedToUseFeature` → **Sentinel RPC** → `GetFilteredNamedGroupingPolicy("g3", 0, org)` + `slices.Contains(ids, membership.ID)`). The gate is **NOT** `app.organization_features` (0-rows even in normal operation — the symptom the verdict named); it is Sentinel's Casbin grouping policy `g3 = _, _` (org, membership) stored as `p_type='g3', v0=org, v1=membership, v2..v5=''` (`casbin.go` model + `OrgAllowUserToUseFeature` → `AddNamedGroupingPolicy("g3", org, membership)`). Live demo-3 had **0 g3 rows**. Seeding 1 g3 grant per membership (mirroring the per-member g2 grant) makes every seeded employee able to START a sim — a demo employee SHOULD be able to. ZERO platform edit (a Sentinel-policy seed, fully in-rext). | 2026-06-25 |
| D2 | **Remove `SIM_START_SKIP`** from `coverage.spec.ts` — the `/sim/<slug>/start` pages are back IN SCOPE and scored. (The sim-RESULT skip stays — those are genuinely runtime-computed.) | The gate-honesty audit (`verify-gate.md`) proved iter-08's skip rested on a FALSE justification (it was entitlement-gated, i.e. SEEDABLE, not runtime-computed). With D1 seeding the entitlement, `/start` renders its real launch UI and must be scored like any page. Re-adding the skip would re-introduce the dishonest scope-out. | 2026-06-25 |
| D3 | A casbin seed applied to a **LIVE** stack requires a **Sentinel policy reload** (restart the demo's sentinel container, or the `Reload` RPC) before the grant takes effect. | Sentinel's Casbin enforcer calls `e.LoadPolicy()` **once at startup** (`NewEnforcer` in `casbin.go`) with **no watcher / auto-reload**. A raw INSERT into `casbin_rules` (the seeder's path) is invisible to the running in-memory enforcer until it reloads. During a fresh `/demo-up` the seed precedes Sentinel start, so this never bites — it bites only a **re-seed of a running stack** (the Phase C re-apply). After re-seeding demo-3, `organizationFeatures` stayed `null`; restarting `demo-3-sentinel-1` (re-runs `LoadPolicy()` on startup) made it return `["FEATURE_JOB_SIMULATIONS"]` and the deny modal vanished — the launch UI rendered (`bodyTextLen=625`, deny-modal sentinel count → 0). Demo-local container op, zero platform/source edit. | 2026-06-25 |
| D4 | Harness fix — the per-page content-emptiness read **falls back to `<body>` innerText when `<main>` is present but below the density floor**. | The sim `/start` launch surface (`AISimulationStartWithoutSession`) renders OUTSIDE `<main>` (`mainLen=0`, visible launch UI ~625 chars in `<body>`). The iter-03/06 `<main>`-only read would FALSE-FAIL it as empty. The fallback (a Tier-2 escalation per the protocol's assertion-shape rule) reads the live `<body>` innerText (still VISIBLE-only, so the inlined i18n table the iter-03 fix excluded stays excluded) and takes the larger — preserving the `<main>`-preference (nav-chrome exclusion) for the common case. | 2026-06-25 |
| D5 | Harness fix — raise the per-page bounded settle ceiling **1.5s → 4s** (`waitForLoadState('networkidle')`). | The 1.5s ceiling was too short for the heavy DATA-GRID pages (the library catalogs — 22 skill-paths, 307 sims). After a cold start (the D3 sentinel restart cleared the GraphQL caches), `/library/skill-paths` rendered only **1 of 22** cards within 1.5s, so the BFS extracted ~1 outbound link and the frontier **COLLAPSED to 8 pages** (vs the 93-page exhausted frontier). A probe confirmed 4s renders the full 22-card grid. `networkidle` returns as soon as the network quiets, so fast SPA routes still proceed in well under the ceiling; only busy grids use more of it; a never-idle long-polling page just hits the ceiling and proceeds (the `.catch`). Correct-over-fast: the gate's correctness depends on full link discovery. | 2026-06-25 |
