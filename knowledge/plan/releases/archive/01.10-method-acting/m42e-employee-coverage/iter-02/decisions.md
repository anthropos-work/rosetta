# iter-02 decisions

| ID | Decision | Rationale | Date |
|----|----------|-----------|------|
| D1 | The baseline metric is `(failing=44, escapes=1)` over `reachable=80` (employee/`maya-thriving`). | First sweep against live demo-3; emitted `coverage-out/employee/coverage-report.json`. The number is the honest baseline even though the failures are dominated by a harness flake (D2). | 2026-06-25 |
| D2 | The 44 failures are a **harness capability gap**, NOT empty pages: the crawl waits `networkidle`, which **never settles** on this app (long-lived ws/polling connections), so every `page.goto` eats its full 30s timeout and the 600s test budget exhausts before later pages are reached -> they record as `empty/error`. **Probe-confirmed:** `/home`, `/dashboard`, `/skill-path/.../chapter` all return http=200 with real content (textLen 534-885) under `domcontentloaded` but `networkidle=NEVER-IDLE(6s)`. | The protocol's measurement convention: "a moved number with no fix = a flake to investigate, not a lift." This is the protocol's named **tooling-iter trigger** (the sweep can't classify pages because the crawler's wait strategy lacks a feature). Routed to iter-03 (tooling-iter): swap `waitUntil: 'networkidle'` -> `'domcontentloaded'` + a bounded settle (a short `networkidle` race with a fallback timeout), and raise the per-test budget / shrink `maxPages` so the sweep finishes within budget. | 2026-06-25 |
| D3 | The 1 escape is real: `/skill-path/how-to-give-feedback-for-managers-fundamentals-and-practice/chapter` renders an external article link `https://www.strategy-business.com/article/...` (seeded skill-path content body links out). Routed forward as a content/escape item (Fate-3, iter >= 04). | A genuine out-of-demo escape per the gate's (c) clause -- but it's an **editorial content link inside replayed skill-path body copy**, not a nav-chrome/baked-URL escape. The fix surface is content (the replayed skill-path body) or an allow-rule decision, distinct from the dominant cluster; route it forward so iter-03 stays scoped to the harness fix. | 2026-06-25 |
| D4 | Crawl saturated at `maxPages: 80` (the cap) -- the BFS exploded into every `/sim/*` + `/skill-path/.../chapter` the library links to. The employee **core** pages (`/home`, `/profile`, `/profile/activities`, `/profile/skills`, `/library`, `/library/*`, the `/skill-path/*` index pages) all rendered real content. | The reachable set is much larger than the seed paths because the library is fully seeded (M39-M41 + G6 serve-grants). iter-03 should re-evaluate `maxPages` vs the genuine reachable set once the `networkidle` flake is removed (the true page count + true failing count are only knowable after the wait-strategy fix). | 2026-06-25 |
