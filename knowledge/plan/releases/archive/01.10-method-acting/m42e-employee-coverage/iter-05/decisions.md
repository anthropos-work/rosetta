# iter-05 decisions

| ID | Decision | Rationale | Date |
|----|----------|-----------|------|
| D1 | **Shipped the result-deep-link `skipPaths` rule** (`RESULT_DEEP_LINK_SKIP = [/\/result\/[0-9a-f-]{8,}/i, /\/result\b/i]`) in `coverage.spec.ts`, wired into the crawl's `skipPaths`. Validated: skips the 5 `/sim/.../result/<uuid>` links, keeps skill-paths/activities/regular sims. | iter-04 D2/D3 routed this here: per-session computed-result deep-links are outside the seeded demo's meaningfully-reachable set. A spec-level (per-vantage) skip, not a universal crawl rule -- it is vantage-scoping. | 2026-06-25 |
| D2 | **The fix works: re-sweep `(failing=3, escapes=2)`, down from `(8,1)` on `failing`.** 0 `/result/` pages scored (confirmed). The 5 sim-result empties are gone from the reachable set. | Phase D re-measurement vs live demo-3. `failing` 8 -> 3 exactly as predicted (cleared all 5 result empties). | 2026-06-25 |
| D3 | **`escapes` rose 1 -> 2, but this is a deeper-crawl discovery, NOT a regression.** The 5 freed frontier slots (reachable still caps at 80) let the BFS reach more `/skill-path/.../chapter` pages; a 2nd pre-existing external article link surfaced: `/skill-path/python-data-analysis-.../chapter -> en.wikipedia.org/wiki/Data_analysis` (alongside the iter-02 `strategy-business.com` one). | Both escapes are the SAME class: external editorial links inside replayed skill-path **chapter body copy** (seeded content), not nav-chrome/baked-URL escapes. The skip-rule did not cause them; it exposed one that was always there beyond the cap. Routed forward as one cluster. | 2026-06-25 |
| D4 | **Residual after iter-05: 3 failures + 2 escapes**, in 3 clusters routed forward: (a) `/` sentinel false-positive (assertion-tune, handler `ASSERT-M42e-root-sentinel`); (b) 2 empty skill-path indexes `prompt-engineering-fundamentals` + `foundation-of-artificial-intelligence` (real content gap, `stack-seeding`/serve-grant, handler `SEED-M42e-empty-skillpaths`); (c) 2 external skill-path-chapter article links (content/allow-rule, handler `ESCAPE-M42e-skillpath-external-articles`). | The clean, fully-triaged residual. iter-06 picks the next highest-leverage cluster (the 2 escapes + the `/` false-positive are both harness/content-tune; the 2 empty skill-paths are the real production content gap). | 2026-06-25 |
