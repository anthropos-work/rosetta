# iter-04 decisions

| ID | Decision | Rationale | Date |
|----|----------|-----------|------|
| D1 | **The 5 sim-result empties are a RUNTIME-COMPUTED surface, NOT a seedable row -> the planned `stack-seeding` fix is FALSIFIED.** The result page (`/sim/<slug>/result/<sessionId>`) reads `GET_SIMULATION_RESULT -> jobSimulationResult.evaluationStatus` keyed by sessionId; the container guards `!resultData || evaluationStatus !== Pending` and renders an empty `<main>` when there is no result. A **backdated seeded** session has **no `jobSimulationResult`** -- the evaluation is computed server-side by the jobsimulation AI pipeline from the session transcript (cf. `useRecalculateEvaluationResult`), never written by a seed. | Read the result route + `AISimulationResultContainer` + `useGetResult` (READ-ONLY, zero platform edits). The evaluation-result fan-out (result + interactions + interaction-calls + tasks) is a computed artifact, not a seedable structural row like `jobsimulation.sessions`. Seeding a believable one would mean synthesizing the AI pipeline's output across many coupled tables -- brittle, and still gated on the `evaluationStatus` computed field. | 2026-06-25 |
| D2 | **Correct fix surface = crawl-scope (exclude per-session `/sim/.../result/<uuid>` deep-links from the employee reachable set), NOT seeding.** Per coverage-protocol "Scope = the vantage's reachable set." A presenter demoing the employee vantage lands on `/profile/activities` (which renders fine -- text=998, lists the sims); drilling into a specific historical session's COMPUTED AI evaluation is a runtime-only surface a seeded demo cannot fill without having RUN the evaluation. These deep-links are not a legitimate demo coverage commitment. | The protocol's close-no-lift-with-falsification path: a complete investigation ending in characterization is a first-class iter outcome. Excluding a runtime-computed deep-link from the *reachable set* is a gate-definition refinement, not a dodge -- the gate is over the pages a vantage can MEANINGFULLY reach in a seeded demo. | 2026-06-25 |
| D3 | **The crawl-scope exclusion is a HARNESS change -> routed to a tooling-iter (iter-05), handler `TOOL-M42e-iter05-result-scope`.** Add a `skipPaths` pattern (`/sim/.../result/`) to the employee sweep so per-session result deep-links are not enqueued/scored; re-sweep. | A harness capability change (a skip rule) -- the protocol routes harness changes to a tooling-iter (same as iter-03's wait-strategy fix). iter-04 does NOT land it (that would be a 2nd, harness, line of work in a production-fix tik -> scope-creep tripwire). | 2026-06-25 |
| D4 | **Zero-edit line held.** The only path to make these result pages non-empty WITH the links in scope is a platform change (compute/return an evaluation result for a seeded session) -- forbidden this release. So the in-scope resolution is crawl-scope (D2/D3), and the platform-only alternative is explicitly NOT taken. | The release's defining hard line: no platform-repo edits. This is NOT a re-scope-trigger exit (there IS a viable in-scope fix -- crawl-scope -- so the milestone is not blocked); it's a routed-forward tooling fix. | 2026-06-25 |
