# iter-06 decisions

| ID | Decision | Rationale | Date |
|----|----------|-----------|------|
| D1 | **Adopted the `crawl.ts` `textContent` -> `innerText` assertion fix** (uncommitted WIP in the rext tree). The non-emptiness/sentinel assertion now reads `innerText()` (visible text) instead of `textContent()` (which serializes hidden + inlined content). | `textContent` pulled in next-web's inlined i18n JSON translation table whose VALUES include "Something went wrong" -> the error-sentinel false-matched the root `/`. `innerText` structurally excludes the table. Same WIP-adoption discipline as iter-03/05. | 2026-06-25 |
| D2 | **The `innerText` fix flipped `/` from `error=True,text=189016` (false-positive) to `empty=True,text=0` (true reading).** The false-positive is GONE -- but the fix exposed that `/` is genuinely a `<main>`-less, visibly-empty pre-redirect/loading shell, not a real content page. | Phase D re-sweep #1. The fix worked (no more false error flag); `/` now reads its real (empty) visible text. | 2026-06-25 |
| D3 | **Dropped `/` from `EMPLOYEE_SEED_PATHS`, replaced with `/home`** (the real authenticated landing, renders text=347 ok). The login handshake still uses `/` via `loginAs` landingPath (independent of crawl seeds). | `/` is a redirect/loading shell that renders empty at the `/` path (it does not path-redirect away, so the seed-drop logic didn't catch it). `/home` is the real landing and is also reached via nav. Scoring the empty `/` shell was a false-empty; dropping it is the correct seed-list fix (iter-03 D4 seed-vs-discovered principle). | 2026-06-25 |
| D4 | **Final re-sweep `(failing=2, escapes=3)`** -- `/` cleared (failing 3 -> 2). The 2 remaining failures are the genuinely-empty skill-paths (`prompt-engineering-fundamentals`, `foundation-of-artificial-intelligence`). | Phase D re-sweep #2. The `/` cluster is fully resolved; the residual is now a single real production content gap (2 empty skill-paths) + the external-chapter-link escape class. | 2026-06-25 |
| D5 | **escapes rose 2 -> 3** (a 3rd external chapter link: `/skill-path/learn-python-data-engineering/chapter -> dremio.com`). NOT a regression -- the deeper crawl (freed `/` slot) reached one more chapter. All 3 escapes are the SAME class (external editorial article links in replayed skill-path CHAPTER body copy: strategy-business.com, en.wikipedia.org, dremio.com). | The well-understood deeper-crawl-discovery pattern (iter-05 D3). These are pre-existing seeded-content links beyond the original page cap, surfaced as the crawl deepens -- routed forward as one cluster (`ESCAPE-M42e-skillpath-external-articles`). | 2026-06-25 |
