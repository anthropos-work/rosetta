# iter-16 decisions

| ID | Decision | Rationale | Date |
|----|----------|-----------|------|
| D1 | Avatar real-photo sourcing is a USER-BLOCKER (SEVERITY: blocker) | No available source is BOTH license-clean (copyright) AND consent-clean (model release for fictional-persona depiction of a real identifiable person). CC0/Unsplash/Pexels: clean copyright, NO model consent. Released stock: model release forbids misleading/fictional-persona use + not free. Per the run prompt's hard rule, STOP and surface a user-blocker rather than use questionable images. See milestone-root decisions.md AVATAR-LICENSING-BLOCKER for the 3 user options. | 2026-06-25 |
| D2 | Org logo = deterministic offline monogram data URI (`orgLogoDataURI`) | The design-plan default (decision #4). A generated mark is the HONEST choice for a FICTIONAL org — no real company's trademark to infringe, no real entity whose consent is at stake. Offline-safe (data URI, no CDN fetch), deterministic per org name, mirrors avatar.go. Seeded into `organizations.logo_url` (the column existed, NULL before). | 2026-06-25 |
| D3 | The Clerkenstein userRes/orgRes top-menu IMAGE threading is held as ONE unit behind the avatar blocker | The menu avatar (userRes image) is blocked on the avatar-licensing decision; the userRes + orgRes image fields ride the SAME `resources.go` change + an alignment-golden re-capture. Doing only the orgRes half would leave a half-wired Clerkenstein change + golden churn for a feature whose companion can't ship. So the in-app org logo (logo_url seed) lands now (no Clerkenstein touch), and the top-menu image threading waits for the avatar decision. | 2026-06-25 |
