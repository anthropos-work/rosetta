---
iter: 29
iteration_type: tik
iter_shape: standard
status: closed-fixed
opened: 2026-07-30
---

# iter-29 — the org-prepared Playthrough, and an absence assertion that could not fail

**Active strategy reference:** `TOK-01` move 3's onboarding clause. iter-28 routed
`ONBOARD-M256-prepared-playthrough` as *"one probe run wide"*; this spends that probe and lands the UC.

## Step 0 — re-survey (mandatory)

| checked | reading |
|---|---|
| onboarding | **3 of 5**; `standard.UC2`'s capability landed + proven at iter-28 |
| the routed residual | one measurement: does the prepared variant's relabelled **`Start`** COMPLETE onboarding or ADVANCE? |
| clauses 1 + 2 | MET (196×3, 0 flake; controls 26/28, MUTATES 10) |

## Cluster / target identified

`onboarding.enterprise-workforce-standard.UC2` — the Playthrough. Everything else it needs is landed.

## Hypothesis

`Start` advances into the curated flow's *"confirm the role, refine the suggested skills"*, giving a mutating
journey whose pre-state (the summary INSTEAD of the import form) is already a proven cross-vantage control.

## Expected lift

clause 3: onboarding **3 of 5 → 4 of 5**; clause 2: controls 26/28 → 27/29, MUTATES 10 → 11.

## Phase plan

- **A — the one measurement** (drive `Start`, read the DB and the screen).
- **B — the spec + its in-line control**, page objects added for the variant.
- **C — mutants**, including iter-27's standing **Q1** ("delete the action — does anything fail?").
- **D — the gate** (3 × cold) + docs.

## Escalation conditions

- If `Start` neither completes nor advances into a measurable state → route again with the new measurement.
- **If a read-back cannot be made to fail under the Q1 mutant, REMOVE it rather than weaken it.** An absence
  assertion that a mid-hydration page satisfies is worse than no assertion: it reads as proof.

## Acceptable close-no-lift outcomes

Landing the journey halves that are mutation-proven while routing a half that cannot yet be asserted honestly.
