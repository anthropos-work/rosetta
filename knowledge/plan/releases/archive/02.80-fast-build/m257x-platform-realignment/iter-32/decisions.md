# iter-32 decisions

## D-M257x-32-1 — the tok question was deferred to the measurement, not argued

Phase 0's strict reading made iter-32 a **triggered tok**: iters 29, 30 and 31 show no movement on the
headline clause-2 number. Rather than argue the trigger away or comply with it, the iter recorded the
disagreement in `overview.md` **before** measuring, noted that the skill's own stale-trigger clause
requires re-running the primary measurement before revising a strategy anyway — so tok and tik prescribe
an identical first action — and let the number decide.

The number decided: `25 → 27`, both points attributable. **Trigger stale; iter-32 was a tik.** Had it
returned `25 / 5 / 1`, the overview had already committed to calling that a complete iter and handing
iter-33 a properly-evidenced tok. Neither outcome was available for post-hoc framing.

## D-M257x-32-2 — the runtime collapse is recorded as a candidate, NOT an attribution

The suite now runs in 4 min 50 s where iter-25 measured *"65 of 209 in ~35 min"*. A plausible mechanism
exists — iter-24's Directus re-point removed a 96-line 403 storm from every page load — and it is
**deliberately not asserted**. Nothing in this iter measured why the runtime changed, and M257x has
already had one inference of exactly this shape (iter-28's hydrating-grid candidate for
`pt-assignment-assign`) refuted one iter later by iter-29.

Routed as `CHECK-M257x-iter32-suite-runtime-collapse`. What IS asserted is the measurement itself, and the
separable fact that *"209 specs"* was never a duration proxy: ~31 are Playthroughs, the rest unit specs at
0–1 ms.

## D-M257x-32-3 — no rext re-pin

No rext runtime source changed this iter — no source of any kind was modified. Per the standing rule
(re-pin only on runtime-source change), the pin stays `fast-build-m257x-iter-31b`, matching the
consumption clone and the tag on origin. The only edits this iter are the milestone dir and one protocol
rule.

## D-M257x-32-4 — the protocol doc gains §5 rule 16 in this iter's commit

The "unread metric ≠ unmoved metric" lesson generalises past this milestone, so per the protocol-evolution
rule it lands in `corpus/ops/platform-alignment.md` in the same commit rather than being routed as a doc
task. Its second half — *re-measure your own cost estimates* — is the more transferable half: an inherited
budget wrong by ~8× had been scheduling the work for seven hand-offs.

## Side note — parallel evidence-gathering during dead wall-clock (NOT iter-32 scope)

While the binding run was in flight, five **read-only** audit agents were launched over the 40 clause-5
files (`corpus/services/*.md` + `corpus/architecture/*.md`) against a freshly-derived ground-truth brief
(`.agentspace/scratch/work-m257x/iter33-groundtruth.md`). This is **evidence for iter-33**, not work in
iter-32's scope: no tree mutation, no bearing on this iter's close status, and the scope-creep tripwire
does not fire because no second line of *investigation* was opened in this iter — the wall-clock was
otherwise idle.

The brief derives its ground truth fresh rather than inheriting it, and surfaces one item no prior sweep
could have covered: **`graphql-wundergraph` was deleted from both `repos.yml` and `docker-compose.yml` at
`2adcf71` (2026-07-31) — mid-milestone.** There is no router in a local stack any more; local dev points
straight at `backend`. Every clause-5 sweep to date predates that change.
