# iter-101 — the reading, taken on a repaired instrument and an unmoved pool

**Shape:** tik · **the MEASURING pass — no repair inside it.**

## Why this iter exists

iter-99 returned `N = 28` at a **78.3 %** upheld rate — the first break in a band four readings had held to
within 1.0 point — and could not separate three candidate mechanisms at n=1: a genuinely harder residual, a
briefing gap, and adjudicator variance. iter-100 repaired the fence that had been GREEN over 65 % of its
subject (`anchor_construct_guard`, reach 360 → **528 of 555**, 0 findings, mutant-proven) and named **6
wrong-construct citations no reading had ever found**.

This iter takes the reading. The instrument is untouched during it, clause 5 is not re-cut, and nothing is
repaired inside the measuring pass.

## What makes this reading different from the four before it

**It is a replicate.** iter-100 changed **8 citation anchors across 5 files** and did **not** pay down
iter-99's 28, which are routed as `FIX-M257x-iter99-read-union`. The corpus moved 10,276 → **10,278** lines
and the recomputed 7-seat partition comes out **identical**. So the instrument is being run twice over
materially the same subject, and reading-to-reading variance becomes measurable rather than argued.

That buys one band no prior reading could state: **#3 — overlap with iter-99's published 28**, measured
blind (those anchors live under `knowledge/plan/**`, which every seat is hard-barred from reading). Its two
tails point opposite ways, and one of them moves the *estimator*: strong overlap means the Chapman
independence assumption is violated and `N̂ ≈ 45.1` is an over-estimate.

## The known instrument defect, delivered unfixed on purpose

`briefing-iter76-AS-RUN.md:37` names the rext **authoring** copy as "the tooling"; a rext claim is settled
in the **pinned per-stack clone**. In iter-99 two independent seats followed it correctly and both bookings
were rejected — the instrument manufacturing identical false bookings by construction, 4 of that reading's
10 rejections.

**It is not edited.** Editing it would break the comparability the replicate exists to establish, and
measuring its cost is the only way to answer the question iter-99 left open. `N` is post-adjudication and
this class is caught by adjudication, so `N` is uncontaminated; the **upheld rate is not**, so it is
reported twice — raw, and with the `wrong-tree` class separated. Routed as
`DEF-M257x-iter101-briefing-rext-tree` under protocol rule 45.

## Exit condition for the iter

The reading returns a number. `N = 0` meets clause 5; anything else leaves the gate at 4 of 5 and routes.
No other reading of clause 5 is available — four user rulings.
