# Seat C — reading #24

## Refs actually in hand at this reading (re-verified at open)

| repo | path | sha measured |
|---|---|---|
| app | `stack-demo/app` | `b948604f` (v1.366.0) — checkout; `origin/main` = `2035f9a4` |
| app/studio | `stack-demo/app/studio` | `aeec036a` (own checkout — grepped at its own ref) |
| platform | `stack-demo/platform` | `0c91421d` (per ADDENDUM; NOT `0dab54df`) |
| next-web-app | `stack-demo/next-web-app` | `bb3313bc` |
| sentinel / storage / messenger | | `88bc5592` / `4ce8ece5` / `fa47850d` |
| cms / graphql-wundergraph | | `ca50c817` / `60c229f3` |
| roadrunner / jobsimulation | | `87d8d443` / `462343b0` |
| studio-desk / ant-academy | | `14a5442a` / `9c3843cd` |
| rosetta-extensions (authoring) | `.agentspace/rosetta-extensions` | `09d06070` (main) |

Ref rule applied per briefing §"Which ref settles an `app` claim": a claim naming a ref is graded at
that ref; an unpinned claim at the checkout `b948604f`.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/architecture/alignment_testing.md` | 542 | yes |
| `corpus/services/backend.md` | 318 | yes |
| `corpus/architecture/ai_architecture.md` | 305 | yes |
| `corpus/services/customerio-sync.md` | 147 | yes |
| `corpus/services/skillpath.md` | 112 | yes |
| `corpus/services/skiller.md` | 69 | yes |
| **total** | **1493** | |

### Two of my own search pipelines failed silently and were caught by controls

Recording these because §5 rule 2 exists for exactly this, and both would have produced a
confidently-wrong "audited zero":

1. `git grep -E "\b(60|100)\b"` returned **0** on a tree where the pattern demonstrably matches.
   Cause: `git grep -E` is POSIX ERE and **does not support `\b`** — an engine rejection presenting
   as absence. Re-run with `-P`; control then returned hits. All "no such construct exists" claims
   below were re-measured with `-P` plus a same-pass positive control.
2. `git show $r:repos.yml` in **zsh** silently applied the `:r` history modifier, producing
   `git show 2adcf71epos.yml` → `fatal: ambiguous argument`. Stderr was read (rule 1); fixed with
   `${r}:repos.yml`.

---

## Blockers

### B1 — `backend.md` banner claims all eight merged services' RPC surfaces are on `app`'s mux; four are not, and the same file refutes one 38 lines later

- **Anchor:** `corpus/services/backend.md:29-30` —
  > "* **All of their Connect-RPC surfaces are served on `app`'s single RPC mux — and nothing outside
  > the process calls them.**"

  "their" is unambiguous: it is the third bullet under a table headed *"**Eight former microservices
  now run inside `app`**"* (`:5`) listing skiller, skillpath, roadrunner, jobsimulation, cms,
  storage, messenger, customerio-sync — and the two preceding bullets use the same referent
  (*"All of their tables live in `public`"*, `:27`).

- **Why it is false:** I enumerated the mux at **both** legitimate refs.

  At the checkout `b948604f`, `app/main.go:1185-1228` registers exactly six handlers —
  `UsersService` (`:1187`), `OrganizationsService` (`:1188`), `SkillerService` (`:1196`),
  `JobSimulationService` (`:1204`), `CMSService` (conditional, `:1212-1214`), `LabSessionService`
  (`:1228`). At `origin/main` `2035f9a` — the newest ref, i.e. *after* the storage/messenger fold —
  the set is **identical**: `main.go:1297, :1298, :1306, :1314, :1323, :1338`, with the
  `*v1connect` import block at `:109-114` naming only organizations / users / cms / jobsimulation /
  lab / skiller.

  So the surfaces of **four of the eight** are not on the mux:

  - **skillpath** — not re-hosted, *dropped*. This same file says so at **`backend.md:68`**:
    *"**There is no `SkillPathSessionService`** — measured: **0** occurrences in Go source"* — which
    I confirmed (`git grep "SkillPathSessionService" b948604f -- '*.go'` → 0; hits only in
    `app/CLAUDE.md` and `app/knowledge/architecture.md`). `corpus/services/skillpath.md:30` states
    it in the strongest available terms: *"It was not re-hosted in `app`; it was DROPPED."*
    **The banner and the body of the same document assert opposite things.**
  - **storage**, **messenger**, **roadrunner** — no handler is registered anywhere in `app`.
    `git grep "NewStorageServiceHandler|NewMessengerServiceHandler|NewRoadrunnerServiceHandler|storagev1connect|messengerv1connect|roadrunnerv1connect" 2035f9a -- '*.go'` returns **only** two
    test-file *client* references (`internal/app/users/services/import_profile_test.go:25,158`) and
    two comment lines in `internal/storagens/callsites_test.go:249,252` — **zero handler
    registrations**. Positive control in the same pass: `skillerv1connect` returns hits.
    Independently corroborated by this same file: `:254` says storage is *"in-process … not a
    service hop"* via `internalstorage.NewManager`, and `:256` says Judge0 is *"called directly
    (`JUDGE0_BASE_URL`)"* — i.e. no roadrunner RPC surface either.

  The bullet's **second** half (*"nothing outside the process calls them"*, the four `*_RPC_ADDR`
  deletions, `backend → sentinel` the only remaining edge) is **correct** — I verified 4 → 1 → 0
  `*_RPC_ADDR` occurrences in compose at platform `0808b92` / `0dab54d` / `0c91421`. That is what
  makes this dangerous: a true, well-cited second clause lends the false first clause its authority.
  A reader integrating against `backend:8083` would look for a skill-path session or object-storage
  RPC that has not existed since M506 / v9.0.

- **Confidence:** high

### B2 — `ai_architecture.md` derives the embeddings row counts with `organization_id IS NULL`, a predicate neither table has — contradicting its own DDL block 20 lines below

- **Anchor:** `corpus/architecture/ai_architecture.md:141` —
  > "- **Data**: `public.skill_embeddings` = **42,790** rows and `public.job_role_embeddings` =
  > **18,919** rows over the *public* taxonomy (`organization_id IS NULL`, measured 2026-06-29)"

- **Why it is false:** the **counts are right and the predicate is not** — rule 17/24's exact shape,
  so I re-derived the *set*, not the sum.

  Neither table has an `organization_id` column:
  - `app/internal/data/ent/schema/skill_embeddings.go:13-21` says so in its own doc comment —
    *"Ported GLOBAL from skiller … **no UserMixin/OrganizationMixin and no Policy(), so the row is
    not org-filtered**"* — and its `Fields()` declares only `skill_id`.
    `job_role_embeddings.go` carries the identical comment and only `job_role_id`.
  - The shipped DDL agrees: `app/terraform/migrations/20260615130000_skiller_taxonomy.sql:241-247`
    creates `skill_embeddings(id, small_embedding3, skill_id)` and `:51-57`
    `job_role_embeddings(id, small_embedding3, job_role_id)`. No `organization_id`.
  - **The same file contradicts itself at `:157-169`**, where it prints both tables' DDL — three
    columns each, no `organization_id`. A reader who runs the stated derivation gets
    `column "organization_id" does not exist`, not a number.

  The corpus's own capture proves the real predicate is a **join, not a filter**. The manifest the
  sibling doc cites (`skiller.md:44-46`) is on disk:
  `.agentspace/snapshots/taxonomy/5afc0bccf1df7ef538b643321fc6362f/manifest.json` records for
  `public.skill_embeddings` the filter
  `"skill_id" IN (SELECT id FROM "public"."skills" WHERE organization_id IS NULL)` with an explicit
  `"public_via": "public.skills"` — and `row_count: 42790`; `job_role_embeddings` likewise via
  `public.job_roles`, `row_count: 18919`. So the **numbers reproduce exactly**; the stated
  derivation does not.

  It also collides with the corpus twin: `backend.md:99` gives `public.skill_embeddings` = **43,584**
  inside a list of public-predicate prod measurements. Both figures can be true (43,584 = whole
  table, 42,790 = the public-parent subset), but the two documents name the same table with two
  numbers and neither states the join that separates them — so the reader cannot reconcile them from
  the corpus alone.

- **Why I hesitated (stated per the grading rule):** the arithmetic is not merely plausible, it is
  *exactly* reproducible against a checked-in manifest, and the intent ("the public subset") is
  correct. I booked it because the failure is precisely the one the protocol names as the
  hardest-to-see — a visible, checkable derivation ranging over a set that cannot exist — and
  because the same file's own DDL block refutes it without any ground-truth read at all
  (§5 rule 21's cheapest class).

- **Confidence:** medium

---

## Minors

- `backend.md:19` — *"compose now declares **five** services"*. Exactly right for
  `docker-compose.yml` (sentinel, backend, studio-desk, next-web-app, gotenberg) but the compose
  **project** resolves `include: common.yml` and totals **seven** (+ postgresql, redis). The
  corpus's own canonical form carries the qualifier — `service_taxonomy.md:66`: *"declares five
  services (seven in the effective topology, once `include: common.yml` adds the …)"*. §5 rule 30
  makes this exact omission load-bearing; and the number **5** collides with a different five
  (`core` starts five containers), which is the confusion the qualifier prevents. Not booked as a
  blocker: the scalar is correct for the file the commit edited, and this file's own `:276` shows it
  knows postgresql/redis are compose services.
- `backend.md:33-34` — the banner names the app-owned streams as `skillpath`, `jobsimulation`,
  `cms`, `ai_usage` and adds *"`skiller` is **NOT** a fifth member"*, but `:264` correctly gives
  `backend`, `skillpath`, `jobsimulation`, `cms` **+** `ai_usage`. The banner set silently drops the
  **`backend`** stream, of which `app` is also both producer (`main.go:287`) and consumer
  (`main.go:1320`). Enumerated: 5 publishers / 6 subscribers, `SKILLER_STREAM` subscriber-only.
- `ai_architecture.md:36` — `studio/tools/pdf2md.py:24` is `from mistralai import Mistral`; the
  parenthesized model literal `mistral-ocr-latest` is at **`:127`** of that same file (verified at
  `app/studio` `aeec036a`). Anchor resolves and names a real Mistral use; only the parenthetical
  sits elsewhere.
- `ai_architecture.md:60` — cites `external_services.md:619-629` for *"item 4 of the four live
  EU-exit routes"*. Item 4 actually runs **622-632**; `619-621` is the tail of item 3. Three-line
  drift of the §5 rule 34 class (the range still overlaps the target).
- `ai_architecture.md:235` — *"the enum is `…/jobsimulation.go:983-990`"*. The `AIModel` const block
  is **`:979-991`** (members `:980-990`); the cited range covers 8 of the 11 members, omitting
  `Anthropic35SonnetAws`/`Anthropic37SonnetAws`/`Anthropic4SonnetAws`. It does bracket the three
  examples the sentence gives.
- `ai_architecture.md:213-215` vs `external_services.md:605` — this file names
  `flag_use_realtime_openai` as *"residency-relevant … a path the routing section above does not
  otherwise cover"*, while the section it defers to states flatly *"**Four** things can send a
  request outside the EU"*. The two are reconcilable (AI-provider routing vs LiveKit agent
  dispatch) but the scope boundary is never stated, so the two enumerations read as competing.
- `backend.md:114` — *"registers in the Cloud Map service-discovery namespace `internal.anthropos`
  (`app/terraform/main.tf:58`)"*. Line 58 is
  `service_discovery_namespace_id = var.service_discovery_namespace_id` — real service-discovery
  wiring, but the literal `internal.anthropos` has **0 hits** anywhere under `app/terraform` at
  `b948604f` (it appears only in `app/knowledge/**`). The passage is self-aware about this at `:118`
  (*"set in the un-cloned `infrastructure` root module"*), which is why this is a minor. The name
  itself is independently corroborated outside `app`:
  `graphql-wundergraph@60c229f3:supergraph-config-prod.yaml` routes to
  `http://backend.internal.anthropos:8080/graphql/query`.
- `customerio-sync.md:140` — *"[External Services] — Customer.io as an integrated SaaS"*.
  `external_services.md` has **0** occurrences of "Customer.io" as a SaaS integration and **0** of
  "Brevo"; it names `customerio-sync` only as a deleted compose service (`:174`, `:306`, `:850`,
  `:859-860`). The pointer also re-publishes the "Customer.io" framing this same file's banner
  (`:18-21`) declares a fossil.
- `alignment_testing.md:532-540` — the Layout block lists `internal/{dna,outcome,compare,report}`
  and omits **`internal/canon/`**, which exists (`canon.go`, `canon_test.go`) and is imported by
  `cmd/alignctl/dna.go:10`.
- `alignment_testing.md:3` — *"Last updated: 2026-06-06"* on a page whose body carries an
  M257x **iter-85** correction (`:378`) and M218/M219 rewrites.

---

## Positively cleared (audited zeros)

**ENUMERATED** — predicate re-derived from source independently, cardinalities compared:

- **Every DNA's gene and capability count** (`alignment_testing.md:261-265, 359, 383`). Parsed all
  five JSONs in `clerkenstein/alignment/dna/`: clerk-2.6.0 **14 caps / 27 genes**, clerk-js-5
  **6 / 9**, clerk-multi-1 **5 / 9**, clerk-deploy-1 **3 / 7**, clerk-express-1 **5 / 13**. All five
  match. Criticality split re-derived: 17 critical + 10 standard genes ⇒ weight 71; dropping the
  weight-2 `MembershipOrgIdentity/real-org-eid` gives 69/71 = **97.18 %**, the stated 97.2 %.
- **`consumed_surface` declared by exactly one DNA** (`:208-211`) — `clerk-2.6.0.json` only; the
  other four return 0.
- **The identity-stub gene set** (`:219-224`) — `ExtractIdentity/universal-user`,
  `Me/universal-user`, `DeployIdentity/universal-user` are the three, and `GetUser` carries exactly
  two variants `hero-a` + `hero-b` (the two-sided A≠B fix).
- **`app`'s Redis-stream ownership** (`backend.md:264`) — every `NewPublisher`/`AddSubscriber` in Go
  source at `b948604f`: publishers `main.go:287` (serviceName=`backend`), `:637` `SKILLPATH_STREAM`,
  `:1039` `CMS_STREAM`, `jobsimwiring/wiring.go:127` `AI_USAGE_STREAM`, `:180`
  `JOBSIMULATION_STREAM`; subscribers `:1274, :1276, :1285, :1303, :1305, :1320`. `SKILLER_STREAM`
  occurs once and is an `AddSubscriber`. Consumer-only claim holds.
- **The subgraph collapse 4 → 3 → 1** (`backend.md:122-129`). `subgraphs.conf` at
  `graphql-wundergraph@749dc86` = BACKEND/JOBSIMULATION/CMS/SKILLPATH; at `@7c17e63` = three; at
  `@915da06` = **BACKEND alone**, and `schemas/` holds only `backend.graphqls`. `915da06`'s diff
  deletes **both** `cms.graphqls` and `jobsimulation.graphqls` while its subject line says "2→1" —
  the trap the corpus warns about is real.
- **`*_RPC_ADDR` in compose across refs** (`backend.md:106-111, 241`) — `SKILLER_RPC_ADDR` 4 @
  `0808b92`, **1** @ `0dab54d`, **0** @ `0c91421`; at `0dab54d` all four live vars sit on the
  `messenger` block at `:173-183`, all `http://backend:8083`, under the comment *"cms + jobsimulation
  are folded into app: all four RPC edges are the one backend mux"*. The `http://cms:8091` /
  `http://jobsimulation:8401` split is real at `2adcf71`.
- **`repos.yml` entry counts** (`skillpath.md:47`) — **9** @ `2adcf71` (0 skillpath), **6** @
  `0dab54d`, **4** @ `838d907`/`0c91421` (app, sentinel, next-web-app, studio-desk).
- **13 `ai_readiness_*` tables** (`backend.md:231`) — 13 Ent schema files and 13 `CREATE TABLE`
  statements; the four named as "the ones a 9 omits" (`recommendations`, `email_overrides`,
  `notification_logs`, `notification_optouts`) are all present.
- **The AI-readiness 30/40/30 split** (`backend.md:229`) — `internal/aireadiness/readiness.go:29-31`,
  `step1MaxScore = 30` / `step2MaxScore = 40` / `step3MaxScore = 30`, comment naming
  skill-mapping / simulation / interview.
- **The Directus `AIVendor` enum is 5, the runtime switch has 4 cases** (`ai_architecture.md:62-66`)
  — enum `jobsimulation.go:969-975` (`azure`, `azureglobal`, `openai`, `anthropic-aws`,
  `anthropic`); switch cases at `simulator/ai/ai.go:58, 69, 86, 102`; `azureglobal` (`:971`) is the
  uncased one.
- **No 60/65/75/85/95 threshold ladder** (`ai_architecture.md:278`) — searched `app`, `cms`,
  `jobsimulation`, `next-web-app` with `-P` for the ordered pattern; 0 hits in all four, with a
  same-pass positive control returning hits. The real conversion is `skills.go:40-51`
  (`20` when `<60 && isPassed`, `100` at `>=100`, else `max(0, score*2-100)`, with
  `// TODO fix this formula`) and `skillScore.ts:1-6`, a plain `levelsCount * score / 100`.
- **8 `ContentLanguage` members** (`skiller.md:57-58`) — `assignments.graphqls:44-53`: english,
  italian, spanish, french, german, dutch, japanese, portuguese.
- **`0` Bedrock/boto3 in the studio tree; 22 Mistral hits / 3 files** (`ai_architecture.md:37, 49`)
  — grepped at `app/studio`'s **own** ref `aeec036a` (the ADDENDUM's nested-repo trap):
  `bedrock|boto3` → 0 with a working control; `mistral` → exactly **22 lines across
  `requirements.txt`, `tools/pdf2md.py`, `tools/r3.py`**.
- **Studio slot table** (`ai_architecture.md:120-135`) — `production_config.ini:26-36` is exactly
  the five stable + five experimental lines quoted; `development_config.ini:26-36` byte-identical;
  `gpt-4o` appears in no `*_MODEL` slot of either shipping config and does appear at
  `config_template.ini:39` and `:40`; `gpt-5.2` appears in no config, only `services/ai.py:356,508`.
- **`STORAGE_RPC_ADDR` has 0 read sites at the ref the claim names** (`backend.md:254`). Graded at
  `origin/main 2035f9a` as pinned: exactly **3** Go-source occurrences, all comments —
  `main.go:504` (*"STORAGE_RPC_ADDR is gone"*), `jobsimwiring/wiring.go:101`,
  `storagens/callsites_test.go:189`. Note for the record: at the **checkout** `b948604f` the same
  variable has **five live `os.Getenv` read sites** (`main.go:446, :524, :992`,
  `jobsimwiring/wiring.go:115`, plus three CLIs) — so this claim is true *only* at its pin, exactly
  as rule 33 provides.

**Exact-anchor checks (line opened and read with surrounding context, all resolved correctly):**

`alignment_testing.md` — `alignctl/run.go:133-136` (`ExitRegressed=2` / `ExitUnmeasurable=3`),
`:139-153` (`unmeasurable()` + banner); `alignctl/dna.go:20-26`; `gate.sh:69` (verbatim
`alignctl dna coverage --dna "$base/$dna" --if-declared`) with the rationale comment at `:58-68`;
`clerkenstein/.github/workflows/alignment.yml` exists, is git-tracked, and `:10-11` reads verbatim
as quoted, with **no** `.github` at the monorepo root; `alignment/` holds exactly
`cmd/ examples/ internal/ go.mod README.md` and **no** `scripts/`; `ALIGN_DIR` default
`../../alignment`; `stack-seeding/dna/snapshot.go:62` (`validSnapshotOperators`, six members);
`data-dna.json:410` (taxonomy, five operators, no `cross-surface-closure`) and `:439` (content, five,
no `embedding-dim`, plus `cross-surface-closure`); the directus public predicate
`private=false AND tenant_id IS NULL AND status='published'`; **nothing waived** across all 10
surfaces (4 `seeded-m7a`, 2 `snapshot-seeded`, 4 `seeded-m7c`); toy score re-derived 13/15 = 86.7 %.

`backend.md` — `main.go:1185-1228` and each of `:1187, :1188, :1196, :1204, :1212-1214, :1228`;
`:743-746` (`LABS_API_URL` conditional); `:1205-1211` (the now-stale DORMANT comment, verbatim);
`terraform/locals.tf:6, :8`, `main.tf:185-186`; `messenger/terraform/variables.tf:77, 82, 87, 92`
(all four, no defaults) and `app/terraform/variables.tf:197, 230`; `app/CLAUDE.md:109` and
`app/knowledge/architecture.md:28` at `2035f9a` (both still list `SkillPathSessionService`, and both
carry the 60 s write-timeout sentence); `internal/academy/academy.go:6-9`; `go.mod` `go 1.26.4`;
`git ls-tree b948604f migrations/` empty; `atlas.hcl` `dir = "file://terraform/migrations"`;
`docker-compose.yml:110` (`profiles: [core, backend, all]`), `:48`
(`AUTHORIZATION_ADDRESS`), `Makefile:10` (`PROFILE ?= core`); `cms/terraform/main.tf:39`
(`service_desired_count = 0`) at **`f38c0c4`** as pinned, with the module block still declared, and
`cms@6efa1d5`'s subject **and body** quoted correctly; `jobsimulation@6092c6d2`'s
`terraform/main.tf:15-22` and `:24-40` resolve **at that commit** (the checkout `462343b0`
pre-dates it — grading at the named ref was load-bearing here); commits `1fc00c78`, `6a46e8445`,
`d11a403`, `749dc86`, `7c17e63`, `915da06` all resolve with the quoted subjects and dates;
migrations `20260518125439.sql` and `cms/…/20250116133510.sql` exist;
`POST /api/webhook/directus` + `DIRECTUS_WEBHOOK_SECRET` fail-closed (`main.go:1088-1089`); every
directory in the Key-directories tree exists.

`ai_architecture.md` — `jobsimulation/ai/ai.go:30-33` (four vendor consts), `:259-289` (`getClient`,
incl. the failed-flag-lookup-keeps-EU branch at `:270-273`), `:87`
(`config.WithRegion("eu-west-1")`), `:129/:153-154` (429 → `vendor = Openai`), `:243-253`
(`CreateSpeech` Azure-only, *"at the moment we only support azure voice"*), `:267/:344` and
`skillerai/ai.go:347` (`flag_use_azure_us`); **no `app/internal/ai/` package exists**;
`coursebuilder/bedrock.go:106-113`; `cms/directus/collections/jobsimulation.go:905` (`AIVendor
*AIVendor` on the **DTO**), `:971`, `:1079-1085` (4-member voice enum), `:1297` (`Gpt5Point1`),
`:1302-1305` → `:1307`, `:1594-1600`; `simulator/ai/ai.go:20-26, 58-59, 65-66, 69, 82-83, 86,
98-99, 102, 110-111, 114-115, 126-127` — **all eleven correct**;
`validator.go:43, 60-61, 595` plus `checkerEngines` measured **stored and never indexed**;
`criterion.go:127` (`case check.EngineLlm`), `:142` (`EngineTextDiff`, the only other engine),
`:162-175` (concurrent pool), `:428`, `:450-475`; `checkValidationBulk.tmpl` exists;
`bulkChecks.go:118` (temperature 0.0) and `:18/:56` (`check_id`); `skills.go:40-51, 53-64, 75`;
`skill/skill.go:617-623`; `CompetencyReadLevel.tsx:18`; `calls/livekit.go:131-135` (flag read) and
`:140-144` (`agentEndpoint = "openai-hosted"`), both inside `CreateAgentDispatch` (`:106`), with
`anthropos-agent` / `anthropos-agent-us` at `:120`/`:126`; `internal/aiusage/ai_usage.go` +
`Event_AiUsage`; `external_services.md:567` is indeed the **Anthropic Direct** row.

`customerio-sync.md` — graded at `origin/main 2035f9a`, where the package exists (it does **not** at
the `b948604f` checkout, which pre-dates v9.0): `internal/customeriosync/{doc.go, store.go, sync.go,
sync_query.sql}` and `cmd/customerio-resync`; `doc.go:4` carries the quoted *"the last of the Go
services to be folded into app"*; `env_guards.go:62`; `main.go:286` (resolved before any
connection), `:284` (the *"off on a laptop and fatal in production"* comment), `:295` (the
`BREVO_KEY` fatal), `:393-396`/`:395` (`customeriosync.New(logger, copilotDB, …BREVO_KEY)`, the
shared `copilotDB` pool); `docker-compose.yml:84-92` at `0c91421` is **exactly** the deliberate
no-value comment block; the HISTORICAL YAML matches `0dab54d:docker-compose.yml:136-152` including
`context: git@github.com:…#main` and `ports 8080:8080`; `--dry-run` (`cmd/customerio-resync/main.go:33`)
returns at `:60` **before** the Brevo client is built at `:71`; `838d907`'s own body confirms the
`all`-profile removal and the 8080 port.

`skillpath.md` — `internal/skillpath/session.go:205-207` (comment verbatim + `GetSkillPathDomain`);
`internal/skillpaths/skillpaths.go:88-95`; `internal/skillpath/{session.go, session_domain.go,
repository/}`; ent `skill_path_session.go` + `skillpath_mixins.go`, and **no**
`local_skill_path_session.go`; `schemas/skillpath_sessions.graphqls`;
`organization/intelligence.go:1144` (`InsightsSkillPathByMemberships`) and `:1159-1170` (the
`SkillPathSession` query, `skill_path_id` + `StatusIn(Active, Completed)` + tenant predicate);
`terraform/migrations/20260729133514.sql:62` `DROP TABLE "local_jobsimulation_sessions"` and **`:63`**
`DROP TABLE "local_skill_path_sessions"` — line-exact; the **three** later migrations at
`9d00a313` are exactly `20260731131307.sql`,
`20260731154527_academy_chapter_progress_completed_at.sql`,
`20260803143844_ai_readiness_recommendation_path.sql` (the third exists **only** at `9d00a313`, not
at the checkout — the pin matters); `SKILLPATH_RPC_ADDR` gone from `app/terraform` with
`CMS_RPC_ADDR` (`main.tf:249`) as the positive control; `InsightsBySkillPathStudentSimulationsContainer.tsx:31-34`
has `userData` hardcoded `null` with the real query commented out, tables commented at `:140`/`:150`,
and `:138` renders `t('enterprise.insights.comingSoon')` → `en/enterprise.json:1862` = **"Coming
soon"**; `apps/hiring` has no skill-paths route.

`skiller.md` — all six named Ent schema files plus both translation schemas exist;
`internal/rpc/skillerrpc/`; `categoryTree`/`fullCategoryTree` survive **only** inside a comment
(`skiller_taxonomy.graphqls:7`, *"stay unported — no consumers"*); the snapshot manifest's four
quoted fields verify **exactly** (`source: primary-read`, `public_only: true`, `predicate: org-null`,
`captured_at: 2026-06-29T14:43:00Z`) with `skills 42790 / job_roles 22470 / job_role_embeddings
18919` — so *"18,919 is the `job_role_embeddings` row count, a different table"* is confirmed at
source; `skiller_rpc_addr` is a real terraform variable name (`cms/terraform:76,107`,
`jobsimulation/terraform:121,163`; messenger spells it `skiller_rpc_address`).

**RE-DERIVED only (weakest clearance — arithmetic the document itself displays):**
the toy example's 86.7 % (13/15) and the Go DNA's 97.2 % (69/71). Both re-derived from the
independently-enumerated gene sets above, so they are backed by an ENUMERATED base rather than
standing alone.

---

## What I could not settle

- **Production DB row counts** (`backend.md:96-103`: `public.skills` 43,584 total / 794 org-private,
  `job_roles` 22,490, `categories` 23, `specializations` 1,447, `skill_embeddings` 43,584). No prod
  access this pass. Settled by a read-only `/db-query` against prod. The *public* halves are
  independently corroborated by the checked-in capture manifest (42,790 / 22,470 / 18,919), and the
  document itself reconciles the 22,490 vs 22,470 drift at `:100-101`.
- **`customerio-sync.md:48`** — *"identical for 7,392 of 7,397 rows"* (`sync_query.sql` vs the
  `public.customer_io_sync_table` view). Requires the prod view; not measurable offline.
- **Whether M810's infrastructure-side deletion has been applied** (`backend.md:44-46`). The doc
  itself declares this NOT MEASURABLE from any clone set, and I confirm the premise: the
  `infrastructure` repo is not in the clone set. Both halves it *does* assert are verified — the
  `cms` module block still declares itself at `service_desired_count = 0`, and `6efa1d5`'s body
  asserts the destruction.
- **The five LiveKit agent repos** (`ai_architecture.md:186-192`). No GitHub access; consistent with
  the corpus twin `platform-migration-status.md:143-147`, which lists all five with dates. Settled
  by `gh repo list anthropos-work`.
- **`skiller.md:29-30`** — *"app PR #989"* and the ECR-orphaning claim: not measurable from clones.
- **The M506 platform record** quoted at `skillpath.md:32-33` (*"No in-app RPC handler is served"*).
  It lives under `app/knowledge/plan/**`; I did not open it, to stay inside the letter of hard bar
  #1. The claim it supports is independently confirmed at 0 occurrences in Go source, so nothing
  turns on it.
- **`alignment_testing.md:261-265`'s live scores** (100 % on four surfaces, UNMEASURABLE on
  `clerk-express-1`). I verified the DNA denominators, the gate thresholds (`gate.sh` 95/100), the
  exit-code split and the dependency gate, but did not execute `alignctl` — the goldens' *pass*
  state is asserted, not measured here. Settled by one `alignment/scripts/gate.sh` run per surface.
