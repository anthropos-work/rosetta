# Seat G — reading #23

**Refs at this reading (re-verified at open, per §5 rule 26):** corpus HEAD `1937e1f`
(branch `m257x/platform-realignment`); platform `0c91421d` (level with `origin/main`, per the
ADDENDUM's superseding row); app `b948604f`; next-web-app `bb3313bc`; sentinel `88bc5592`;
storage `4ce8ece5`; messenger `fa47850d`; cms `ca50c817`; graphql-wundergraph `60c229f3`;
roadrunner `87d8d443`; jobsimulation `462343b0`; studio-desk `14a5442a`; ant-academy `9c3843cd`;
nested `stack-demo/{app,cms}/studio` both `aeec036a`; rosetta-extensions authoring copy on `main`.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/services/studio-desk.md` | 435 | yes |
| `corpus/services/hiring.md` | 434 | yes |
| `corpus/services/graphql-wundergraph.md` | 280 | yes |
| `corpus/services/academy-backend.md` | 149 | yes |
| `corpus/services/next-web-app.md` | 133 | yes |
| **total** | **1431** | — |

Search-pipeline controls run in the same pass (§5 rules 1–4): `grep -rn 'SET "score"'` over
`app/terraform/migrations/` returned 0 — paired with `grep -rn "CREATE SCHEMA"` over the *same*
directory, which returned 1 (`20230817154747_supabase_baseline.sql:2`), so the empty result is an
absence, not a broken pipeline. `git grep` was used (never bare `grep`) for every tracked-file
predicate in `stack-demo/*` because this shell's `grep` is `ugrep --ignore-files`. Two zsh
"bad substitution" failures were caught by reading stderr and re-run under `bash -c` — the first
run's silent zeros were discarded, not booked.

---

## Blockers

### B1 — `next-web-app.md:32` republishes the M222 `apps/web` premise that `hiring.md` retracts three times, and names it as the retraction's own target

- **Anchor:** `corpus/services/next-web-app.md:32` —
  > "**NB the demo's recruiter candidate-comparison scoreboard is an `is_hiring` ORG-TYPE surface in
  > the dockerized `apps/web`** (`/enterprise/activity-dashboard`), **not** this Vercel-only app — the
  > full hiring org-type + read-model is [`hiring.md`](hiring.md)"

  Supporting site in the same file: `corpus/services/next-web-app.md:14` — "The hiring **org-type**
  (`is_hiring`) re-skins `apps/web` **and exposes the recruiter candidate-comparison read-model**".

- **Why it is false:**

  **(a) The corpus contradicts itself, and the twin names this exact file.** `hiring.md` retracts
  this claim in three separate places:
  - `corpus/services/hiring.md:53` — "**not in `apps/web`**. M222's dockerized-`apps/web`
    (`/enterprise/activity-dashboard`) premise was **falsified at M224**".
  - `corpus/services/hiring.md:353` — "**M224 proved the render — and it does NOT land in `apps/web`.**"
  - `corpus/services/hiring.md:423` — the Cross-references entry *for `next-web-app.md`*:
    "**⚠️ M222 inferred the scoreboard was reachable in `apps/web`; M224 rendering proved it is not
    for a *genuine* hiring org — the demo serves the real `apps/hiring` as a second container.**"

    So `hiring.md` points at `next-web-app.md` and warns the reader that its content is retracted,
    while `next-web-app.md` still asserts it. This is §5 rule 19's *half-repaired corpus*: the claim
    was fixed in the file its owner held and left standing in the twin.

  **(b) Ground truth agrees with `hiring.md`, not with `next-web-app.md`.** Measured at
  next-web-app `bb3313bc`:
  - `apps/web/src/context/UserStatusContext.tsx:141-172` — a `useEffect` computes
    `userHasAllHiringOrgs` at `:144-149` from
    `membership?.organization?.publicMetadata?.isHiring`, and at `:167-171` fires
    `window.location.href = buildSwitchHandoffUrl({ targetProduct: 'hiring', … })`. A user whose
    memberships are all hiring orgs is ejected out of `apps/web`.
  - `apps/web/src/hooks/useGetClerkOrganization.tsx:16-18` — `regularOrganizationList` filters
    `!organization.publicMetadata?.isHiring`, so a hiring org is removed from the workforce list.
  - `apps/hiring/src/context/UserStatusContext.tsx:125` + `:144-145` — the symmetric guard
    (`targetProduct: 'workforce'`) keeps a genuine recruiter *in* `apps/hiring`.
  - The scoreboard container that actually renders the comparison drawer for a hiring org is
    `apps/hiring/src/components/containers/InsightsByMembersContainer.tsx:359` (`<Drawer`), which
    `hiring.md:415` cites.

  A reader of `next-web-app.md` doing real work — wiring a demo, or debugging an empty recruiter
  scoreboard — is sent to the one app the platform's own guard makes unreachable for that org type.

- **Why I hesitated (per the briefing):** `apps/web` *does* still contain a byte-identical copy of
  `simulationScoreColumn.tsx` and a `/enterprise/activity-dashboard` route, and the *client re-skin*
  (`useNavbarSections.tsx:460`, label `results` vs `activityDashboard`) really is an `apps/web`
  mechanism. So the sentence is not wrong about every clause. But its load-bearing clause — where
  *the demo's* recruiter scoreboard lives — is the one `hiring.md` books as falsified, and it is the
  clause a reader acts on. **BLOCKER.**

- **Confidence:** high

### B2 — `hiring.md` defines the comparable-candidate cohort by `jobsimulation_id`, a column that exists only on the mirror the same document says was DROPPED

- **Anchors:**
  - `corpus/services/hiring.md:160` — "A candidate is comparable to another when they
    share the same `jobsimulation_id` — the sim IS the position for scoreboard purposes."
  - `corpus/services/hiring.md:198` — "Candidates are **comparable** when they share the same
    `jobsimulation_id` + `organization_id` — that pair defines **one comparable cohort** (one
    scoreboard)."

- **Why it is false:**

  **(a) `public.job_simulation_sessions` has no `jobsimulation_id` column.** Its full DDL is
  `app/terraform/migrations/20260722104506.sql:2-27`; the 23 columns are `id, created_at,
  updated_at, owner_id, sim_id, organization_id, tenant_id, sim_type, status, completion_status,
  token, started_at, ended_at, interactions_progress, score, validation_version, scheduled_timeout,
  timer_id, timer_reminder_id, language, result_status, chime_status, is_test`. The Ent constant
  confirms it: `app/internal/data/ent/jobsimulationsession/jobsimulationsession.go:28` —
  `FieldSimID = "sim_id"`. `git grep jobsimulation_id` over
  `app/internal/data/ent/schema/` returns **0**.

  **(b) `jobsimulation_id` was a column of the DROPPED mirror.** It was created on
  `local_jobsimulation_sessions` at `app/terraform/migrations/20240527131926.sql:7`
  (`"jobsimulation_id" uuid NOT NULL`) with its index at `:21`, and it went with the table at
  `app/terraform/migrations/20260729133514.sql:62` (`DROP TABLE "local_jobsimulation_sessions";`,
  under the comment `-- 5. Drop the mirrors.` at `:58`). This is precisely the table
  `hiring.md:169-175` and `hiring.md:264` spend two blockquotes telling the reader is gone — the
  document repairs the *table* name and leaves the *mirror's column* name standing as the cohort key.

  **(c) The real cohort key is `sim_id` + `organization_id`.** In
  `app/internal/organization/intelligence.go`, `InsightsJobSimulationByMemberships` filters
  `jobsimulationsession.SimID(jobSimulationId)` at `:1702` and
  `jobsimulationsession.OrganizationID(organizationID)` at `:1709`; the best-attempt window in
  `usersBestOrFirstJobSimulationSession` is
  `ROW_NUMBER() OVER (PARTITION BY sim_id, owner_id ORDER BY score DESC NULLS LAST, …)` at
  `intelligence.go:2158-2168` (the format args are `FieldSimID, FieldOwnerID, FieldScore, …`), with
  the attempts count over the same partition at `:2170-2173`.

  **(d) The document contradicts itself.** Its own minimal write-set names the right column twice —
  `hiring.md:225` ("`owner_id`, `sim_id`, `sim_type`, **`token`**") and `hiring.md:233`
  ("`sim_id` `:7`"). So one section tells a seeder author to write `sim_id` and another tells them
  the comparison key is `jobsimulation_id`. This is exactly the failure mode the document's own
  headline warns about — "a seeder built from the old shape writes to nothing" (`hiring.md:169`) —
  reproduced in the document that warns about it.

- **Confidence:** high

---

## Minors

- `corpus/services/hiring.md:192` — table row 5 cites `intelligence.go:1728-1735` for
  "best-attempt: `row_number() ORDER BY score DESC` per candidate". Lines 1725-1732 are the
  `onlyAssignments` session-id gathering; the only relevant line in the cited range is `:1733`, the
  *call* to `m.usersBestOrFirstJobSimulationSession(...)`. The `ROW_NUMBER()` literal is at
  `intelligence.go:2158`. The anchor resolves and brackets the call site, so it is not misleading in
  substance — but it names a neighbour, not the construct.
- `corpus/services/hiring.md:211` — cites `persona_write.go:69-71,143-167` for where the
  `PersonaSeeder` writes the three `validation_*` tables. At the rext authoring copy on `main`,
  `:69-71` is a comment paragraph about a *removed* `jobsimulation.sessions` step (the three
  table-mapping comment lines are `:66-68`), and the actual write steps are
  `persona_write.go:92-94`; `:143-167` spans a closing brace plus `sessionCols()`/`attemptResultCols()`.
  Low weight because rext `main` is a moving ref with no pin.
- `corpus/services/hiring.md:81` vs `:130` — the same `CreateOrganizationSimInvitationLink`
  hard-error is cited as `siminvitationlink.go:63` in one place and `siminvitationlink.go:62` in the
  other. `:62` is `if !org.IsHiring {`, `:63` is the `return nil, fmt.Errorf("organization is not
  hiring")`. Both resolve inside the construct; only the internal inconsistency is the defect.
- `corpus/services/graphql-wundergraph.md:177` — "they were `:220`/`:236` at `0dab54d`". Measured at
  platform `0dab54d`: `:220` is studio-desk's **runtime** `VITE_GRAPHQL_ENDPOINT`, but `:236` is
  next-web-app's **build arg** `NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT` — its runtime env at that ref is
  `:245`. The pair mixes the two constructs the sentence had just distinguished. Mitigated by the
  clause's own "grade the construct, not the offset".
- `corpus/services/next-web-app.md:48` — "…and again in the runtime environment (`:160`),
  re-anchored at platform `0c91421` (it was `:236` at `0dab54d`)". Same issue: at `0dab54d` the
  runtime env was `:245`; `:236` was the build arg (correctly `:151` today).
- `corpus/services/academy-backend.md:15` — "(Consistent with :80-83 below.)". `academy-backend.md:80`
  is a blank line and `:80-83` is the "How to find the API / Primary — GraphQL" bullet, which says
  nothing about the production router — the clause the parenthetical is defending. The
  `NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT` mention it is presumably pointing at is `:85`.
- `corpus/services/studio-desk.md:112` — "Tier defaults: OpenAI/Azure `gpt-5.2` / `gpt-5-mini` /
  `gpt-5-nano`" lists three model names immediately after declaring **four** tiers. The distinct-value
  set is correct (`thinking_slow` and `thinking_fast` are both `gpt-5.2`;
  `src/services/ai/config.ts:21-24`), but the 4-vs-3 juxtaposition reads as an omitted member.
- `corpus/services/studio-desk.md:112` — "multi-provider chain (`AI_PROVIDER_CHAIN`, default
  `azure-openai,openai`)". There is no code default: `parseProviderChain()` returns `nil` when the
  var is unset (`src/services/ai/config.ts:124-125`) and `loadAIServiceConfig` then falls back to
  *azure, openai, anthropic* in that order (`:165-169`). `azure-openai,openai` is the
  `.env.example:57` value, which is a documented default rather than an in-code one. Harmless because
  the doc's own M252 section (`studio-desk.md:352-355`) describes the legacy fallback correctly.

---

## Positively cleared (audited zeros)

Labelled per the briefing. **ENUMERATED** = I derived the predicate's members from source and
compared cardinalities. **RE-DERIVED** = I recomputed only the arithmetic the document displays.

### ENUMERATED

- **`graphql-wundergraph.md:15` — "the **five** [profiles] that exist are `core`, `backend`, `all`,
  `studio-desk`, `frontend`."** Enumerated every `profiles:` key across *both* compose files after
  resolving `include:` (`docker-compose.yml:1-2` includes `common.yml`, §5 rule 30's load-bearing
  step): `docker-compose.yml:110,141,168,183`; `common.yml` declares **no** `profiles:` key at all.
  Distinct tokens = exactly those five. **And the always-on floor derivation holds**: the services
  with no `profiles:` key are `sentinel` (`docker-compose.yml:5`), `postgresql` (`common.yml:2`) and
  `redis` (`common.yml:24`) — three, exactly as `:19` and `:191` claim, so a `graphql` token exits 0
  and starts three containers rather than zero.
- **`graphql-wundergraph.md:15-18` — "This list read *the eight* until platform `838d907`,
  2026-08-05."** Enumerated at `0dab54d`: tokens `core, backend, all, storage-legacy,
  customerio-sync, messenger, studio-desk, frontend` = **8**. `838d907` is dated 2026-08-05
  ("drop the storage, messenger and customerio-sync containers").
- **`graphql-wundergraph.md:41-46` — the subgraph ladder 5 → 4 → 3 → 1.** Enumerated
  `^  - name:` in `supergraph-config-prod.yaml` at each named ref in the archived repo:
  `749dc86^` = 5 (`backend, skiller, jobsimulation, cms, skillpath` — the corpus's list, member for
  member); `749dc86` = 4; `7c17e63` = 3; `915da06` = 1. Dates match (2026-06-24 / 2026-07-21 /
  2026-07-29). `git show --name-status 915da06` marks **both** `schemas/cms.graphqls` and
  `schemas/jobsimulation.graphqls` as `D`, and that commit's subject really does read
  "supergraph 2→1" — so the corpus's claim that the subject line is wrong is itself correct.
- **`graphql-wundergraph.md:59-69` — the retracted SSE subscriptions clause.** Enumerated:
  `grep -c "^type Subscription" schemas/backend.graphqls` = **0**; the 6 `Subscription` substring
  hits are `activeSubscription` (`:1528`), `type PlanSubscription` (`:1555`), `stripeSubscriptionId`
  (`:4385`, `:4418`), `stripeSubscriptionItemId` (`:4389`) and a doc comment (`:4391`) — the exact
  Stripe/plan set the corpus names. `grep -rn subscription supergraph-config-*.yaml` = 0 across all
  three variants.
- **`graphql-wundergraph.md:180` — the historical `depends_on` list.** Enumerated at `2adcf71^`:
  the `graphql` service's `depends_on` is exactly `backend, jobsimulation, cms, storage`
  (`docker-compose.yml:19-27`), and `storage` is indeed not a subgraph in any
  `supergraph-config-*.yaml`. Also `ports: - "5050:8080"` at `:14`, `profiles: [graphql, all]` at `:28`.
- **`graphql-wundergraph.md:126-134` — the "which Dockerfile did compose use" history.** Enumerated
  each named commit's diff to `docker-compose.yml` at platform: `63d285c` (2024-06-20) introduces
  `wundergraph:`; `d92e84e` (2024-06-20) renames it `graphql:`; `a2a3ee6` (2026-02-27) moves the
  context `git@…` → `../graphql-wundergraph` **and adds `dockerfile: Dockerfile.dev` to sentinel /
  intelligence / app / skiller but NOT to graphql**; `719befb` (2026-02-27) touches only profiles;
  `2c85211` (2026-02-27, after `719befb` in `--date-order`) adds `dockerfile: Dockerfile.dev` to
  graphql; `67ba772` (2026-03-02) moves the context to `..`. Every step of the narrative holds,
  including the counter-intuitive one (a2a3ee6's *subject line* says "switch to local Dockerfile.dev
  builds" while its graphql hunk does no such thing — the corpus got this right and the subject line
  is the trap).
- **`graphql-wundergraph.md:181` — "`ci/update-subgraph.sh:9` carries **exactly one** `gh release
  download`, `-R anthropos-work/app`."** `grep -c "gh release download"` = 1; line 9 is that line.
- **`hiring.md:231-235` — "one of **four** required-and-undefaulted columns."** Enumerated every
  `NOT NULL` column in `20260722104506.sql:3-26` and split by presence of `DEFAULT`: undefaulted =
  `owner_id` `:6`, `sim_id` `:7`, `sim_type` `:10`, `token` `:13` (4); defaulted = `id` `:3`,
  `created_at` `:4`, `updated_at` `:5`, `status` `:11`, `completion_status` `:12`,
  `interactions_progress` `:16`, `score` `:17`, `validation_version` `:18` (8). The conjunction the
  sentence actually asserts — "every other `NOT NULL` column in the DDL carries a `DEFAULT`" —
  is true of all 8, so both clauses hold (§5 rule 17). `token`'s UNIQUE index is at `:29`.
- **`hiring.md:229-231` — the closed 5-value `completion_status` enum.** Enumerated
  `enum/jobsimulation.go:29-35`: `pending, passed, failed, discarded, timedout` — five, no
  `SIMULATION…` member; `Values()` at `:37-43` returns the same five. The SDL declares them at
  `jobsimulations.graphqls:14` (`enum SessionCompletionStatus`) and `:128` (`enum
  SimulationSessionCompletionStatus`). The "nothing rejects a bad value" chain also holds end to end:
  no CHECK in the DDL (`:12` is a bare `character varying NOT NULL DEFAULT 'pending'`);
  `ent/jobsimulationsession.go:181-186` casts unconditionally; the gqlgen marshalers at
  `graph.go:129546-129554` and the proto-bound twin at `:129392-129400` are both bare
  `graphql.MarshalString(string(v))` with no membership check.
- **`hiring.md:72-78` — the "`resolver_queries.go` insights path does not read `is_hiring`" claim,
  including both its positive controls.** `grep -in hiring` over
  `internal/web/backend/graphql/graph/resolver_queries.go` = 4 lines, all sim-TYPE
  (`"simulation_type_hiring"` at `:811`/`:843`, `ActivityDashboardSimulationTypeSimulationTypeHiring`
  + `simulation.Hiring` at `:991-992`) — no `is_hiring` read. Over
  `internal/organization/intelligence.go` = **0**. Positive controls reproduce exactly:
  `OrgFeatureInsights` = 8 in the resolver (8 lines *and* 8 occurrences);
  `JobSimulationSession` = 44 lines in `intelligence.go` (54 raw occurrences — the document's "×44"
  is the `grep -c` line count, which is the conventional reading).
- **`hiring.md:79-86` — the four sites that DO read the column.** Enumerated
  `git grep GetOrganizationIsHiring`: exactly 4 call sites in `resolver_cms_queries.go` at
  `:95, :210, :258, :295` (plus the definition in `internal/cms/user/userManager.go:22-23`) — the
  corpus's list, member for member; the branch is `:99-103`. The two hard-error sites are
  `organization/manager.go:537` and `siminvitationlink.go:62-63`, and the candidate-role fork is
  `manager.go:450-453`.
- **`hiring.md:355-357` (M228 fix) — "the regression enumerates **all 8** generic seeders."**
  Enumerated the table in `rosetta-extensions/stack-seeding/seeders/hiring_scope_test.go:75-95`:
  `jobsim-sessions, activity, skillpath-sessions, assignments, hero-activity, persona, feedback,
  succession` = 8, with `feedback` and `succession` carrying the M228 F2/F3 and F1 comments the
  corpus describes.
- **`hiring.md:29-33`, `:178-181`, `:329-334` — the `repos.yml` entry counts.** Enumerated:
  `0dab54d` = 6 (`app, sentinel, storage, messenger, next-web-app, studio-desk`);
  HEAD `0c91421` = 4 (`app, sentinel, next-web-app, studio-desk`) and `838d907` is the commit that
  made it so; `d11a403` is the commit that removed `cms`, `jobsimulation` and `roadrunner` (9 → 6).
  `app` is at `repos.yml:14-17` and is the only `migrations: true` entry. `app/atlas.hcl:8` pins
  `search_path=public`, and the only `CREATE SCHEMA` in the whole migration set is `auth`
  (`20230817154747_supabase_baseline.sql:2`).
- **`academy-backend.md:56-60` — "`STORAGE_RPC_ADDR` is gone from the Go source (3 remaining
  occurrences at that ref, all comments, zero reads)."** Enumerated at the ref the claim names
  (`app` `9d00a313`, per §5 rule 33): `git grep -n STORAGE_RPC_ADDR 9d00a313 -- '*.go'` returns
  exactly 3 — `internal/jobsimwiring/wiring.go:101`, `internal/storagens/callsites_test.go:189`,
  `main.go:451` — and all three are `//` comments. No `os.Getenv` read survives.
- **`academy-backend.md:61-74` — the Ent-label-vs-plural-table warning.** Enumerated the 11
  `academy_*` `CREATE TABLE` statements across the migration set: `academy_bookmarks,
  academy_certificates, academy_chapter_bodies, academy_chapter_progresses, academy_chapter_times,
  academy_chapters, academy_feedbacks, academy_last_activities, academy_path_embeddings,
  academy_series, academy_skill_paths` — 11 tables against 11 schema files in
  `internal/data/ent/schema/`, and the 6/5 per-user vs catalog split the doc gives is exactly right.
  `extensions.vector(1536)` confirmed at `20260710142023_academy_path_embeddings.sql:7`.
- **`academy-backend.md:75-77` — the certificate id contract.** `internal/academy/certificate.go:42`
  is `^AAS-\d{4}-[A-HJKMNP-TV-Z2-9]{6}$`; I counted the character class member by member and it is
  **30** symbols (A–H = 8, JKMN = 4, PQRST = 5, VWXYZ = 5, 2–9 = 8) — ambiguity-free (no I, L, O, U,
  0, 1). `crypto/rand` at `:5` and `:171`.
- **`studio-desk.md:279-287` — "studio-desk has no database of any kind."** Enumerated the negative
  set the sentence lists: `git grep` for each of `pg|postgres|prisma|sqlite|mysql|mongo|knex|drizzle|
  typeorm|sequelize` in `package.json` = 0 hits, and `DATABASE_URL` across `src/` = 0. The claimed
  replacements all exist: `src/routes/skillpath.ts` (61,080 bytes — the largest route, confirming the
  "~61KB" and "largest" claims by `git ls-tree -l`), and
  `GET_USER_STUDIO_PREFERENCES`/`SET_USER_STUDIO_PREFERENCES` in
  `app/services/graphql/{queries.ts:205,mutations.ts:58}`. The Tailscale claim is exact: the *only*
  case-insensitive `tailscale` hit in the whole repo is `app/core/main.ts:105`, the GlitchTip
  ingest comment.
- **`studio-desk.md:34` — the prod entry set in `vite.config.ts` `rollupOptions.input`.** Enumerated
  `vite.config.ts:29-37`: `home, sim-advanced-builder, builder-skill-path, simulation-builder,
  sim-guided-builder, generation, catalog, academy, skills` = 9, matching the doc's 9 exactly, with
  `dev-accept` gated behind `isProduction` at `:41`.
- **`studio-desk.md:120` — "Multi-language support (7 languages)."** Enumerated
  `app/public/l12n/`: `de, en, es, fr, it, ja, nl` = 7.
- **`studio-desk.md:309-320` — "hardcoded the production app host … in **three** places."**
  Enumerated `git grep app.anthropos.work -- app/core/scaffold`: exactly 3 —
  `pageWrapper.js:149` (the logo `<a href>`), `userProfile.js:148` (the menu button) and
  `userProfile.js:302` (the logout redirect) — the three roles the doc assigns them.
- **`next-web-app.md:22` — "All four apps … declare `\"next\": \"^16.2.7\"`."** Enumerated all four
  `apps/{web,hiring,integration,maintenance}/package.json`: all four read `"next": "^16.2.7"`.
  React `^19.2.7`, `engines.node ">=24.0.0"`, `packageManager: pnpm@10.30.3`, `turbo ^2.9.6` all
  confirmed in the root `package.json`.
- **`next-web-app.md:44` — "8 locales: de, en, es, fr, it, ja, nl, pt."** Enumerated
  `configs/i18n/messages/*`: exactly those 8 directories.
- **`next-web-app.md:49` — the auth-edge file and public allowlist.** Enumerated:
  `git ls-files | grep -E '(proxy|middleware)\.ts$'` returns `apps/{web,hiring,integration}/src/proxy.ts`
  and **no** `middleware.ts` anywhere. Every allowlist member the doc names is present in
  `apps/web/src/proxy.ts`'s `isPublicRoute`: `/login` `:22`, `/sign-up` `:25`, `/checkout` `:16`,
  `/free-trial` `:17`, `/monitoring` `:31`, `/print` `:33`, `/api/bunny/thumbnail` `:37` — and
  `/print` is separately HMAC-gated by `isPrintRoute` `:59` + `validatePrintToken`. The repo's own
  `CLAUDE.md:15` and `:55` say what the doc quotes them as saying.
- **`next-web-app.md:71-72`, `:123` — the two "it does not exist" claims.** Enumerated: no
  `storybook` script in any `package.json` and no `.storybook/` directory; the sole surviving trace is
  `configs/tailwind/storybooks.css`. `git ls-files | grep -i dockerfile` returns exactly one path,
  `Dockerfile.dev`, repo-wide.
- **`next-web-app.md:115-116` — "turbo test → jest in `apps/web` and `apps/hiring` (integration &
  maintenance have no test script)."** Enumerated per-app `package.json`: web `"test": "jest"`,
  hiring `"test": "jest"`, integration none, maintenance none.

### Exact-anchor verifications (opened the file and read ±15 lines, per METHOD)

Every one of the following resolved to the construct the corpus names. Listing them because §5
rule 20 records that *introduced anchors verify while the prose around them does not* — this is the
half that verified.

- Platform compose @ `0c91421`: studio-desk `depends_on: backend` `:138-140`, `profiles` `:141`,
  build arg `:119`, runtime env `:135`; next-web build arg `:151`, runtime env `:160`,
  `depends_on` `:165-167`, backend `profiles` `:110`. `grep -c 5050 docker-compose.yml` = 0.
  `Makefile:10` = `PROFILE ?= core`; `Makefile:119-120` = `up-frontend:` →
  `--profile core --profile frontend`.
- Platform compose @ `0dab54d`: studio-desk `depends_on` `:223-225`, `profiles` `:226`, build arg
  `:204`. @ `2adcf71`: studio-desk `depends_on` incl. `cms` `:337-341`. @ `2adcf71^`:
  `VITE_GRAPHQL_ENDPOINT` = `http://localhost:5050/graphql` `:343` — so "was `:5050/graphql` when the
  router existed" is true.
- `app`: `intelligence.go:1700` (`m.ent.JobSimulationSession.Query()`), `:1820` (`score := RoundFloat(…)`),
  `:1844` (the blind `SessionCompletionStatus` re-cast), `:1846` (`Score: &score`), `:885-886`
  (`insightsSortColumn` mapping `InsightsSortFieldCompletitionStatus` → `FieldCompletionStatus`),
  `:1751-1763` (the `score DESC / completition_status ASC / session_started_at DESC` default sort);
  `resolver_queries.go:1034` (decl), `:1035` (Casbin gate), `:1053` (status ∈ {active,invited}),
  `:1080` (manager call), `:1085`/`:1089` (the *neighbouring* resolver's gate and `GetMembership` —
  the disambiguation the corpus makes is correct);
  `ent/schema/job_simulation_session.go:39` (`completion_status`), `:45` (`field.Float32("score")`);
  `ent/schema/anticheat_result.go:24` (`field.Enum("summary")`);
  `20260722104506.sql:2/6/7/10/12/13/29/53/79`; `20260729133514.sql:58-62`;
  `20260722081626_jobsim_data_model.sql:336/355/376` (the three `validation_*` tables, including the
  corpus's correction that the middle one is `validation_attempt_skill_results`);
  `20250416091037.sql:5` (`anticheat_summary` added to the mirror);
  `askengine/registry.go:192` (the M710 alias comment — and it *is* an alias map resolving to
  `Schema: "public", RealName: "job_simulation_sessions"`, not evidence the schema exists);
  `gqlauthz.go:186` + the two named tests (`gqlauthz_test.go:132` "bare __typename is not exempt",
  `:176` `TestAnonymousRejectionLogsAtWarn`);
  `main.go:471-472` @ `9d00a313` (the two storage managers) with
  `internal/storage/service.go:22/24` giving `EnvBucket = "STORAGE_S3_BUCKET"` /
  `EnvPublicBucket = "STORAGE_S3_PUBLIC_BUCKET"`;
  `cmd/academyImport/main.go:239-243` and the `:244-246` hard-error @ `9d00a313` — both exact,
  quote included. `local_jobsimulation_session.go` confirmed absent (positive control: 2 files match
  `job_simulation_session`).
- `app` version claim: `git describe --tags origin/main` = `v1.369.0-2-g2035f9a40`, so
  `academy-backend.md:20`'s "**`v1.369.0`** @ origin/main `2035f9a4`" is correct, and `:57`'s
  `9d00a313 = v1.367.0` is the tagged version commit (2026-08-04).
- `next-web-app`: `apps/web/.../UserStatusContext.tsx:141-172`, `:144-149`, `:167-171`;
  `useGetClerkOrganization.tsx:20-21`, `:16-18`; `packages/ui/src/NavBar/useNavbarSections.tsx:460`
  inside `enterpriseInsightsMenuItem` `:459-466`; `apps/hiring/.../UserStatusContext.tsx:125,144-145`;
  `template.tsx:90` (`isEnterprise = Boolean(organization)`) vs
  `FreeTrialContainer.tsx:29` (`Boolean(!isHiringOrg && organizationId)`) — the divergence is real;
  `packages/graphql/src/query/insights.ts:31-82`; `InsightsByMembersContainer.tsx:359` (`<Drawer`,
  in `apps/hiring`); `simulationScoreColumn.tsx` byte-identical between `apps/web` and `apps/hiring`
  (`diff -q` clean) with `:95-97` destructuring `score` (`:54` is the `accessorKey` binding, defaulted
  to `'score'` at `:33` — a weaker but not wrong anchor).
- `studio-desk`: `src/routes/youtube.ts:43` (`process.env.YOUTUBE_API_KEY`, `_mock: true` fallback at
  `:46`) and it is the **only** YouTube credential read in `src/`; `.env.example:120` +
  `terraform/main.tf:129` for `GCLOUD_SERVICE_ACCOUNT` with **zero** `src/` reads;
  `src/index.ts:96` `STUDIO_ACCESS_ROLES` (all four members as quoted), `:99`
  `checkEnterpriseAndAdmin`, `:119-120` the `WEB_APP_URL` redirect; `src/index.ts:60`
  `process.env.PORT || 9100` feeding `app.listen(backendPort)` at `:294`; `app/core/main.ts:97`
  (`preloadCriticalCSS()`) and `:206` (`new PageWrapper()`) with the three blocking awaits between
  them exactly as the doc's ladder draws; `jest.config.js` `projects: [{displayName:'backend'},
  {displayName:'frontend'}]`; `package.json` `engines.node ">=24"` + `node:24-alpine` in both
  Dockerfiles; `src/index.ts:305` `pingSnapshotCapability()` boot check against `directus_versions`;
  `src/services/aiService.ts:10` circuit-breaker rotation-on-timeout;
  `app/services/config.ts:7` `WEBAPP_URL: import.meta.env.VITE_WEB_APP_URL` (the M249 claim).
- `graphql-wundergraph`: `terraform/main.tf:20` `service_desired_count = 1`;
  `terraform/locals.tf:2,7,8` + `main.tf:26` (eu-west-1, project "wundergraph", port 8080,
  `/health`); `package.json` = `{"name": "graphql-wundegraph"}` (misspelling carried);
  `config.compose.yaml` `listen_addr: 0.0.0.0:8080`, `graphql_path: /graphql`,
  `playground_enabled`/`introspection_enabled` true, `max_request_body_size: 35MB`;
  all three `supergraph-config-*.yaml` list `backend` alone with routing URLs
  `http://backend:8082/graphql/query` (compose), `host.docker.internal:8082` (dev),
  `backend.internal.anthropos:8080` (prod) — and there is no `-local` variant;
  `subgraphs.conf` = one line `BACKEND=v1.360.0`; `schemas/` = `backend.graphqls` alone;
  router `0.275.0` / `wgc@0.104.0` / `node:22.11-alpine` / `federation_version: =2.3.2`;
  `repos.yml` `type: node-npm` present at `2adcf71^` and gone after.
  Intra-corpus anchors resolve: `graphql-wundergraph.md:193` is the `localhost:5050` refusal, and
  `:174-176` is indeed the compose line-number caveat the doc says it is.
- `rosetta-extensions` (authoring `main`): `stack-injection/gen_injected_override.py` `frontend_lines()`
  emits the existence-guarded absolute `env_file` for studio-desk only when `platform_dir` is given
  (`:446-449`), with the compose-precedence reasoning the doc reproduces; the two named regression
  tests exist; `clerk-frontend/server.go:186` now registers
  `GET /v1/me/organization_memberships` (the M253 `canAccess` 404 fix) and
  `clerk-frontend/resources_test.go:99-115` pins the conditional `public_metadata.isHiring`
  (M224); `stack-seeding/seeders/persona_write.go:91` writes
  `{"public","job_simulation_sessions",sessionCols(),…}` with `sessionCols()` at `:152` listing 18
  columns; `hiring_config.go:99` `CopyRowsIdempotent(… "organization_sim_invitation_links" …)`.

### RE-DERIVED only (weakest clearance — flagged as such)

- `hiring.md:194` step-6/7 chain ("`Score` ← the session's own `score` column — not a mirror's").
  I confirmed each cited line and that no mirror entity remains, but the *end-to-end* claim that the
  browser number is this column is a render-path assertion I could not exercise without a live stack.

---

## What I could not settle

- **`graphql-wundergraph.md:8` — "the repo … ARCHIVED on GitHub, 2026-07-30 (read-only)."** `gh` is
  not installed on this host (`bash: gh: command not found`) and I made no network call. The local
  clone gives no archive signal. *Settled by:* `gh repo view anthropos-work/graphql-wundergraph
  --json isArchived,archivedAt`.
- **`hiring.md:153-158` — "the captured public snapshot carries **87** real `SIMULATION_TYPE_HIRING`
  sims" and "the captured snapshot has **0** `job_position` rows (the prod '443' was never
  captured)."** These range over the `.agentspace` snapshot cache, which is not in the ground-truth
  set and which I did not open. *Settled by:* `stacksnap status` / a count over the cached
  taxonomy + directus manifests.
- **`studio-desk.md:109` — "`bootstrap/index.js:81` in the pinned `directus/directus:11.6.1`."**
  Inside a third-party container image; no local copy. *Settled by:*
  `docker run --rm directus/directus:11.6.1 sed -n '75,90p' /directus/dist/cli/commands/bootstrap/index.js`
  or equivalent.
- **Every measured latency/perf number** — `studio-desk.md:392` (`clerk.load()` ~140 ms,
  `canAccess` ~3.9 s), `:412` (skeleton p95 4669 → 817 ms), `:424-425` (4049 → 38 ms, FCP
  6936 → 2152 ms), `hiring.md:395` (recruiter p95 1.27 s), `hiring.md:410` (the 5/5 per-position
  cohort sizes 8/8/9/9/8). All require a live stack + the named harnesses
  (`rext stack-verify/e2e/run-studio-fcp.sh`, `render-hiring-comparison.spec.ts`). Not contradicted
  by anything I read.
- **`academy-backend.md:99-100` — the nightly Asynq schedule "@ 03:00."** The task type
  `academy_embedding_refresh` and the 03:00 figure appear consistently in five in-repo comments
  (`internal/worker/tasks/academy_embedding_refresh.go:10`, `internal/academy/embeddings.go:108`,
  `ent/schema/academy_path_embedding.go:16`, `internal/web/backend/academy_embeddings_admin.go:23`,
  the repo's own `CLAUDE.md:189`), but I did not locate the cron *registration* that sets the hour, so
  the schedule is corroborated by comments rather than by the scheduler. *Settled by:* reading the
  Asynq periodic-task registration in `internal/worker/`.
