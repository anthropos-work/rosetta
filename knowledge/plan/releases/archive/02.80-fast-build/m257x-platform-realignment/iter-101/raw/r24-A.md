# Seat A — reading #24

## Refs at this reading (re-derived at open, §5 rule 26)

| tree | sha |
|---|---|
| `stack-demo/platform` | `0c91421d` (matches ADDENDUM; the frozen table's `0dab54df` is stale) |
| `stack-demo/app` | `b948604f` — `origin/main` = `2035f9a4` (`v1.369.0-2`) |
| `stack-demo/app/studio`, `stack-demo/cms/studio` | `aeec036a` (own checkouts — grepped at their OWN ref) |
| `stack-demo/next-web-app` | `bb3313bc` — `origin/main` = `8297c684` |
| `stack-demo/sentinel` | `88bc5592` · `storage` `4ce8ece5` · `messenger` `fa47850d` (origin/main `e9421c68`) |
| `stack-demo/cms` | `ca50c817` · `graphql-wundergraph` `60c229f3` · `roadrunner` `87d8d443` |
| `stack-demo/jobsimulation` | `462343b0` — `origin/main` = `82cb66ec` |
| `stack-demo/studio-desk` | `14a5442a` · `ant-academy` `9c3843cd` |
| `stack-demo/rosetta-extensions` (pinned per-stack — the tree that ADJUDICATES tooling claims, §5 rule 45) | `ab81527a` |
| `.agentspace/rosetta-extensions` (authoring copy) | `09d06070` |
| rosetta HEAD | `04cbcfc3` |

Enumerated the search set with `find stack-demo -name .git -maxdepth 4` — **15** git trees, nested
`app/studio` + `cms/studio` included. Every tree-wide zero below names the trees it covers.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/architecture/external_services.md` | 883 | yes |
| `corpus/services/chronos.md` | 245 | yes |
| `corpus/services/messenger.md` | 189 | yes |
| `corpus/architecture/frontend_architecture.md` | 105 | yes |
| `corpus/services/gotenberg.md` | 84 | yes |
| (protocol, read before starting) `corpus/ops/platform-alignment.md` §5 | 2272 (whole file) | §5 in full |

Total assigned = 1506 lines, as briefed.

---

## Blockers

### B1 — `external_services.md` contradicts itself on how many services the `--local-content` re-point targets, and cites the contradicting passage as its own support

- **Anchor:** `corpus/architecture/external_services.md:136` —
  > "…so `backend` is the only consumer left and **per-service re-point tooling has ONE target, not
  > two** — see the ⚠️ under *Architecture* below."

  against `corpus/architecture/external_services.md:206` —
  > "**The `--local-content` re-point targets BOTH `cms` and `backend`.**"

  and `:210` — "…re-points every service in `DIRECTUS_DATA_CONSUMERS`, which is **`("cms", "backend")`**".

- **Why it is false:** The two sentences assert incompatible cardinalities (one vs two) about the
  *same* mechanism, ~70 lines apart in the same file — and `:136` explicitly routes the reader to
  `:206` ("see the ⚠️ under *Architecture* below") as corroboration, so the reader lands on the
  refutation of the sentence that sent them there. Ground truth, read in the tree that adjudicates a
  tooling claim (the **pinned per-stack** clone `stack-demo/rosetta-extensions @ ab81527a`, §5 rule
  45): `stack-injection/gen_injected_override.py:84` is
  `DIRECTUS_DATA_CONSUMERS = ("cms", "backend")` — **two** members — and `:669-670` is
  `if with_directus and name in DIRECTUS_DATA_CONSUMERS: env.append(f"DIRECTUS_BASE_ADDR=…")`.
  So `:206`/`:210` are literally correct about the tuple and `:136` is wrong about it.
  Conversely `:136` is correct about *effect*: the loop's `name` ranges over the platform's compose
  services, and `cms` has not been one since `d11a403` (verified: `git -C stack-demo/platform show
  0c91421d:docker-compose.yml` declares exactly `sentinel`, `backend`, `studio-desk`,
  `next-web-app`, `gotenberg`), so exactly one service is re-pointed on a live stack. Both halves
  are defensible in isolation; as written the document asserts both and reconciles neither, and the
  bolded `:206` headline reads as a present-tense claim about a service that no longer exists.
- **Confidence:** high (needs no ground-truth read at all — §5 rule 21's cheapest class; the
  ground-truth read only decides *which* half to keep).

### B2 — `chronos.md` gives ports `8080`/`8081` and `REDIS_STREAMS_INDEX=2`; the platform ran chronos on `8500`/`8501` (and `=4`) for its entire life

- **Anchor:** `corpus/services/chronos.md:27` — "* **Ports**: `8080` (HTTP/health), `8081` (RPC)";
  reinforced by the env table at `:198-199` (`PORT` → `8080`, `RPC_PORT` → `8081`) and `:202`
  (`REDIS_STREAMS_INDEX` → `2`).
- **Why it is false:** The only measurable ground truth for chronos is the platform's own compose
  block, and it never held those numbers at any commit. Read at all four commits that ever touched
  it (`git -C stack-demo/platform show <sha>:docker-compose.yml`):
  - `3330ef6` *"feat: add chronos service"* — `ports: "8500:8500"`, `"8501:8501"`; `PORT=8500`,
    `RPC_PORT=8501`
  - `081a350`, `b43b99a`, `a2a3ee6` — identical ports; `REDIS_STREAMS_INDEX` `2` → `4`
  - `045857c^` (the last state before removal, block at `:339-368`) — `"8500:8500"`, `"8501:8501"`,
    `PORT=8500`, `RPC_PORT=8501`, `REDIS_STREAMS_INDEX=4`, `SERVICE_NAME=chronos`

  The doc's stated purpose is "preserved for historical context", and a reader reconstructing the
  historical stack from it gets ports that never existed there — and, since `8080` was
  `customerio-sync`'s published port in the same file, would infer a collision that never happened.
  The sibling doc uses the identical field the other way round: `messenger.md:62` gives the
  **compose-published** `8200`/`8201` in its **Ports** bullet and separates the binary defaults
  ("The binary's own defaults are 8080/8081 (`cmd/root.go:63`, `:64`)"), so the convention a reader
  brings to this field is "what the service listened on in the stack".
- **Why I hesitated (stated per the grading rule):** `chronos` is in **no** clone set — measured:
  `find . -maxdepth 3 -iname '*chronos*'` returns only `corpus/services/chronos.md`, and no
  `stack-*/chronos` exists — so the binary's *built-in* defaults cannot be read. If they are
  `8080`/`8081` and `2` (exactly messenger's colony-convention defaults, `cmp.Or(os.Getenv("PORT"),
  "8080")` at `messenger/cmd/root.go:63` and `REDIS_STREAMS_INDEX` default `"2"` at `:107`), then
  the numbers are defensible *as defaults* and the defect reduces to an unlabelled field. That is
  the reading I lean to on the merits — but the doc never says "default", the Architecture field is
  not the defaults field anywhere else in the corpus, and nothing anywhere records the ports the
  platform actually used. Booked per "when in doubt, book it as a BLOCKER."
- **Confidence:** low.

---

## Minors

- `corpus/services/messenger.md:163` — the env table's middle column is headed *"Value in the
  deleted compose block"* and the banner at `:153-155` pins it to `0dab54d`, but the `BREVO_KEY` row
  says `(empty)`. Measured at `0dab54d`, the `messenger` block (`docker-compose.yml:156-195`) sets
  **no** `BREVO_KEY` at all — it arrived through `env_file: .env`. The substance is stated correctly
  elsewhere (`:114`, "`BREVO_KEY` **must be blanked** in `platform/.env`"), so no operator
  consequence.
- `corpus/services/messenger.md:108` (and `:28`) — "the binary still constructs **four**
  Connect-RPC clients". `messenger/cmd/root.go:118-142` @ `fa47850d` constructs **five**
  (`cmsV1Client`, `usersClient`, `organizationsClient`, `skillerClient`, `jobsimulationClient`) over
  **four** distinct env addresses (`usersClient` and `organizationsClient` share
  `BACKEND_USERS_RPC_ADDR`). The enumeration that follows says "backend users + organizations", so
  the intent is address-distinct clients; "four addresses" at `:28` is exactly right.
- `corpus/architecture/external_services.md:144` — cites `service_taxonomy.md:332-339` for the
  repaired twin. `:332` is a table row (`| **Database** | PostgreSQL (dedicated `directus` schema) |`);
  the repaired blockquote runs `:334-342`. Line drift of two.
- `corpus/architecture/external_services.md:219` — `test_injection.py:1109` names the first comment
  line inside `test_backend_the_actual_reader_is_repointed`; the `def` is at `:1108` (pinned rext
  `ab81527a`). Off by one, lands inside the right construct.
- `corpus/architecture/external_services.md:636` — cites `config_template.ini:30-31`. No top-level
  `config_template.ini` exists at `aeec036a` (`git show aeec036a:config_template.ini` → *does not
  exist*); the file is `configs/config_template.ini`, where `:30-31` are exactly
  `OPENAI_API_KEY` / `OPENAI_ENDPOINT = https://api.openai.com/v1/chat/completions`. Path prefix
  only; the doc spells it correctly at `:640` (`app/studio/configs/*.ini`).
- `corpus/architecture/external_services.md:14` — "GraphQL/Wundergraph - **Unifies all backend
  services into a single API**" sits against `:368`/`:373` ("over **ONE** subgraph (`backend`)").
  Stale phrasing in the PM summary; the prod-only caveat is already attached.
- `corpus/architecture/external_services.md:350` — "Invalidate **CMS service** cache when content
  updates" keeps standalone-service language for what is now the cms *domain* inside `backend`
  (the doc's own `:300` banner draws the distinction).
- `corpus/architecture/external_services.md:451` — "It **only** parked the `graphql` block behind a
  `wundergraph-deprecated` profile". Verified `b56d731:docker-compose.yml:36` is
  `profiles: [wundergraph-deprecated]` ✓ — but the same commit also deleted the block's four-service
  `depends_on` (present at `1e8e754:19-27`, absent at `b56d731:22-36`), so "only" understates by one
  edit. No consequence for the claim being made.
- `corpus/architecture/frontend_architecture.md:97` — heading "## Recent UX Work (**May 2026**)" is
  three months stale at this reading (2026-08-06).

---

## Positively cleared (audited zeros)

**ENUMERATED** — predicate enumerated from source independently, cardinalities compared:

1. `external_services.md:373-386` supergraph counts. Read `supergraph-config-prod.yaml` at each named
   commit: `749dc86^` → **5** (backend, skiller, jobsimulation, cms, skillpath); `749dc86` → **4**;
   `7c17e63` → **3**; `915da06` → **1**. Dates 2026-06-24 / 2026-07-21 / 2026-07-29 all match.
   `915da06 --name-status` marks **both** `schemas/cms.graphqls` and `schemas/jobsimulation.graphqls`
   `D`, so "cms **and** jobsimulation removed together" holds and the 3→1 (not 2→1) correction is
   right — and the commit subject really does say "supergraph 2→1", as warned at `:384`.
2. `external_services.md:387` "no subscriptions". `git grep -nE 'sse|subscription' 60c229f3 -- '*.yaml'`
   → rc **1** (positive control `backend` matches all **three** yamls). Mainline-scoped
   `git log --oneline 60c229f3 -S 'protocol: "ws"'` → **0**; positive control `sse_post` → **5** —
   both exactly as published. `bba862f` (2026-02-25) is `merge-base --is-ancestor` rc **1** and
   `branch -a --contains` names only `remotes/origin/feat/use-web-socket`. `schemas/backend.graphqls`:
   `type Mutation` at `:4053`, `type Query` at `:4912`, **no** `type Subscription`, and exactly
   **six** `Subscription` hits, all Stripe/plan names (`:1528`, `:1555`, `:4385`, `:4389`, `:4391`,
   `:4418`).
3. `external_services.md:668-673` env-var occurrence counts. Re-derived on an independently extracted
   `app@5ba17044` tree with `command grep -rhoE '\b<NAME>\b' --include='*.go' | wc -l`:
   `OPENAI_KEY` **5**, `ANTHROPIC_API_KEY` **26**, `AZURE_OPENAI_KEY` **13**,
   `AZURE_OPENAI_ENDPOINT_URL` **13**; `OPENAI_ORG_ID` / `AZURE_OPENAI_ENDPOINT` /
   `AZURE_OPENAI_DEPLOYMENT` **0**; `OPENAI_API_KEY` **2**. Every figure reproduces.
4. `external_services.md:687-700` taxonomy figures. Parsed
   `.agentspace/snapshots/taxonomy/5afc0bcc…/manifest.json`: `"public_only": true`,
   `"predicate": "org-null"`, `captured_at 2026-06-29`; `skills` **42790**, `job_roles` **22470**,
   `skill_embeddings` **42790**, `job_role_embeddings` **18919**. All four match, and the "18,919 is
   the embedding count transcribed onto the role count" diagnosis is visible in the data.
5. `external_services.md:567`/`:619`/`:640` "all 30 `*_AI_*_MODEL` lines pin `azure`". Enumerated at
   `app/studio @ aeec036a` (its OWN ref — a host-ref `git grep` returns 0 by construction):
   `git grep -n '_AI_.*_MODEL' aeec036a -- 'configs/*.ini'` → exactly **30** lines across
   `configs/{config_template,development_config,production_config}.ini`, every one starting `azure,`.
6. `external_services.md:597-600` "*Every* use of Mistral in `app` is OCR". Enumerated:
   `git -C stack-demo/app grep -in mistral b948604f -- '*.go'` returns 8 lines, all in
   `internal/cms/studio/{markdownManager.go,studioManager.go,xlsx.go}`. Nested tree at its own ref:
   `app/studio @ aeec036a` → **22** lines across `requirements.txt`, `tools/pdf2md.py`, `tools/r3.py`;
   `git grep -in mistral aeec036a -- gen.py` → rc **1**. The "`r3.py` DOES dispatch it as step 2"
   correction verifies (`:139` script list, `:190` `pdf2md_script`, `:199-206` `step2_cmd`, under the
   `# STEP 2` comment at `:189`).
7. `external_services.md:723` "there is no `anthropos-agent-eu` — the name appears nowhere in the
   platform". Swept **all 15** git trees at their own refs: 0 hits in every platform repo
   (the only hits, 7, are in `rosetta-extensions`, i.e. the tooling/corpus recording the correction).
   Positive control `anthropos-agent`: app 6, jobsimulation 8. The live set is exactly
   `livekit.go:110`/`:120` (EU bare), `:126` (`-us`), `:115` (`-chain`).
8. `gotenberg.md:47` "Call sites (two)". `git grep -n ConvertToPDF 9d00a313 -- '*.go'` → the
   definition (`internal/converter/gotenberg.go:16`) plus exactly **two** callers,
   `internal/web/backend/coursebuilder/extract.go:77` and
   `internal/worker/tasks/user_import_resume_2d.go:68`. `extract.go:17-27` holds exactly **nine**
   `gotenbergMimes` keys, and DOCX
   (`…wordprocessingml.document`) is genuinely absent — confirmed by the code's own comment at `:41`.
   Consumer sweep across all 15 trees: `GOTENBERG` appears only in `app`, `platform` (compose) and
   `rosetta-extensions` (tooling).
9. `frontend_architecture.md:39` "29 of them across 21 non-test files". Enumerated
   `git grep -c NEXT_PUBLIC_BACKEND_API_URL bb3313bc -- 'apps/*/src/**' 'packages/*/src/**'`,
   dropping `.test.` and the one `.md`: **21 files / 29 occurrences**, and the four `core-js` clients
   are 3+3+3+3 = **12** as claimed. The forward-looking figure verifies too: at the named ref
   `ad767d1b` it is **22 / 31**, exactly as published.
10. `frontend_architecture.md:35` "8 locales (de, en, es, fr, it, ja, nl, pt)". Enumerated from
    `configs/i18n/messages/*` → exactly those eight.
11. `frontend_architecture.md:11` "`repos.yml` holds exactly **4** entries … it was 9 at `2adcf71`".
    Enumerated: `0c91421` → app, sentinel, next-web-app, studio-desk (**4**); `2adcf71` → **9**
    (app, cms, jobsimulation, sentinel, storage, messenger, roadrunner, next-web-app, studio-desk).
    ant-academy in neither.
12. `external_services.md:171-175` / `:304-306` compose census. Enumerated at `0c91421d`: **5**
    services (`sentinel`, `backend`, `studio-desk`, `next-web-app`, `gotenberg`) + `common.yml`'s
    `postgresql`/`redis` = **7** effective; at `0dab54d`: **8** + 2 = **10**. `postgresql`, `redis`
    and `sentinel` declare no `profiles:` key, so the "three profile-less" floor is right and the
    §5 rule-30 reading holds. `d11a403` removed cms/jobsimulation/roadrunner from both compose and
    `repos.yml`; `838d907` removed storage/customerio-sync/messenger from compose and
    storage+messenger from `repos.yml` — verified by diffing the service key lists at each ref.
13. `messenger.md:17` "**21** handlers — 22 `pubsub.EventHandler(…)` lines of which one …
    commented out". Enumerated `flow.go:72-104` @ `fa47850d`: 22 `pubsub.EventHandler(` lines, the
    one at `:83` (`OrgJobSimulationAssignmentPastDueHandler`) commented `// not implemented` → **21**
    live. `:74-78` are exactly the five `OrgSkillPath*` handlers.
14. `messenger.md:23-26` the `MESSENGER_RPC` absence. Re-ran the published commands verbatim:
    `log -S 'MESSENGER_RPC' 0c91421d` → **0**; positive control `-S 'SKILLER_RPC'` → **7**; with
    `--all` → **0** and **8**. `git rev-list --count 0c91421d` = **121**, so "the platform's whole
    121-commit history" is exact. And `RPC_ADDR` across `docker-compose.yml` + `common.yml` +
    `.env_example` at `0c91421d` → **zero** matches in all three (the doc's "no compose file sets any
    `*_RPC_ADDR` at all").

**RE-DERIVED / anchor-verified** (opened the file and read ±15 lines around every anchor; not a
predicate enumeration, so weaker — §5 rule 24):

- Platform compose anchors at `0c91421`: `:53` `DIRECTUS_PUBLIC_BASE_ADDR`, block `:46-94`,
  `:84-92` (the messenger-in-app comment, exactly those nine lines), `:101-109` (`depends_on` →
  redis/postgresql/sentinel), `:110` + `:183` (`profiles: [core, backend, all]` on backend and
  gotenberg), `:119`/`:135` (studio-desk build arg + runtime env), `:151`/`:152`/`:160`/`:161`
  (next-web-app), `:171` `gotenberg/gotenberg:8`, `:180` `3200:3200`, 186 lines total.
  At `0dab54d`: `:53` `DIRECTUS_PUBLIC_BASE_ADDR` ✓, `:226` studio-desk profiles ✓, `:268` gotenberg
  profiles ✓, `repos.yml:21-23` = the messenger entry ✓, messenger block `ports 8200/8201` +
  `profiles: [messenger]` ✓. `.env_example:91-92` = `DIRECTUS_BASE_ADDR`/`DIRECTUS_TOKEN`, and
  `DIRECTUS_PUBLIC_BASE_ADDR` is absent from it ✓.
- Router history: `1e8e754:19-27` four `service_started` deps (backend, jobsimulation, cms, storage)
  and lines 6-8 `dockerfile: graphql-wundergraph/Dockerfile.dev`; `b56d731` still has `  graphql:` at
  `:22`, `360efd4` does not (positive control `  backend:` at `:28`); `63d285c` (2024-06-20, service
  named `wundergraph`, `context:` only, no `dockerfile:` key); `2c85211^` no key vs `2c85211` with
  it; `67ba772` (2026-03-02) raises the context `../graphql-wundergraph` → `..`; `a4db680`
  (2026-07-21) is where the four-service set begins. `graphql-wundergraph/CLAUDE.md:39` really does
  assert the opposite of the corpus section (the corpus's quotation is a paraphrase, not verbatim —
  the substance matches). `terraform/locals.tf:8 port = 8080`, `terraform/main.tf:48-49` 8080→8080,
  `config.prod.yaml:5 listen_addr: 0.0.0.0:8080`, `main.tf:20 service_desired_count = 1`,
  `subgraphs.conf` = `BACKEND=v1.360.0`, `schemas/` = `backend.graphqls` alone,
  `Dockerfile.dev:18` (one schema COPY) / `:19-20` (comment) / `:23` (one awk) /
  `:7 wgc@0.104.0` / `:29 cosmo/router:0.275.0`. Dead routing URLs `http://jobsimulation:8400/query`
  and `http://cms:8090/query` confirmed at `915da06^`.
- Historical directus block: `a2a3ee6^:docker-compose.yml` `:384 directus/directus:10.10.1`,
  `:386 8055:8055`, `:409 ADMIN_PASSWORD=password`; `a2a3ee6` dated 2026-02-27. The "it did exist"
  correction-of-the-correction is right.
- `app` @ `b948604f`: `main.go:980-982` `log.Fatalf` on empty `DIRECTUS_BASE_ADDR`;
  `main.go:770` `coursebuilder.ModelBackendName()`; `main.go:1205-1211` the *"additive + DORMANT …
  until the M809 re-point"* comment; `internal/jobsimulation/ai/ai.go` `:30-33` (four vendor consts),
  `:80` (`openai.NewOpenAI`), `:130-141` (`isThrottlingError`, 429 only), `:150-155` + `:296-302` +
  `:326` (the `vendor = Openai` retry), `:259-289` (`getClient`), `:264-276` (EU default, US on
  `flag_use_azure_us`, error → keep EU), `:280-283` (both Anthropic arms → the Bedrock client);
  `internal/cms/directus/collections/jobsimulation.go:905` (`AIVendor *AIVendor`) and `:1302-1305`
  (`aiVendor := simulation.Openai`); `simulator/ai/ai.go:58-59` and the `default:` arm at `:114-115`;
  `coursebuilder/bedrock.go:23`/`:29`/`:106-113`; `askengine/bedrock.go:25`;
  `jobsimulation/agent/report_agent.go:31`; `cms/studio/markdownManager.go:19` +
  `studioManager.go:583`; `calls/livekit.go:110/115/120/126` and `:131-144`
  (`flag_use_realtime_openai` changes the **endpoint**, never the engine);
  `internal/web/backend/backend.go:130` (`/api/webhook/clerk`) and `:315`/`:317` (Apollo Sandbox at
  `/graphql`, query at `/graphql/query`). Voice-engine nil default → `Gptrealtime`
  (`jobsimulation.go:1595-1596`).
- `app` at the refs the passages name: `9d00a313` — `main.go:1095` (`CMS_STREAM` publisher),
  `:1478-1484` (stream binding), `internal/web/backend/coursebuilder/extract.go:77-81` + `:17-27` +
  `:59` + `:43-45`, `internal/worker/tasks/user_import_resume_2d.go:68-74`/`:69-74`,
  `internal/converter/gotenberg.go:13` (90 s client) + `:16` (signature) + `:31` (the route).
  `origin/main 2035f9a` — `main.go:15`/`:62`/`:63` (the three `internal/messenger/*` imports),
  `:1450` (second subscriber server), `:1471` (`msgadapters.Wire`), `:1473`
  (`msgsender.NewFromEnv`), `:1416-1421` (the literal consumer-group comment),
  `:295` (the `BREVO_KEY` `log.Fatalf`), `env_guards.go:61` (`envMessengerEnabled`).
- `messenger` @ `fa47850d`: `cmd/root.go:63`/`:64`/`:107`/`:118-142`/`:147`;
  `internal/rpcsrv/rpcsrv.go:25-30` (both stubs `CodeUnimplemented`);
  `internal/flow/jobsimulations.go:140-151` (>2 h ended / >12 h started);
  `internal/flow/assignments.go:828` inside `getSkillPath`; `go.mod` go 1.25.0, `brevo-go v1.1.3`,
  `liquid v1.8.1`; `REDIS_WORKER_INDEX` read by no code (0 hits). `terraform/main.tf` at the ref the
  banner names (`e9421c6`): `:29 service_desired_count = 0`, `:27-28` the rollback-path comment,
  `:25` the cms precedent — all exact. `2adcf71` compose confirms `CMS_RPC_ADDR=http://cms:8091` and
  `JOBSIMULATION_RPC_ADDR=http://jobsimulation:8401` on the messenger block (`:256`, `:258`).
- `cms` @ `ca50c817` `terraform/main.tf:39 service_desired_count = 0`; `6efa1d5` (2026-08-04) deletes
  `.github/workflows/build-production.yml` with the M810 subject; jobsimulation `6092c6d2`
  (2026-08-04) removes the `module "jobsimulation"` block (present at `6092c6d2^:31`, gone after).
- `rosetta-extensions` @ the **pinned** `ab81527a`: `gen_injected_override.py:84` + `:669-670`;
  `stack-injection/tests/test_injection.py:1108-1120` (`test_backend_the_actual_reader_is_repointed`,
  asserting the opposite of the deleted `test_only_cms_is_repointed_not_other_services`).
- `next-web-app` @ `bb3313bc`: `pnpm-workspace.yaml` `'!apps/mobile'`; `apps/web/package.json:46`
  `"next": "^16.2.7"` (same in hiring/integration/maintenance); root `package.json` turbo `^2.9.6`,
  typescript `^5.9.3`, `node >=24.0.0`, `packageManager: "pnpm@10.30.3"`, `clean:quick`, `codegen`;
  workspace names `@anthropos/web-app` / `hiring-app` / `integration` / `@anthropos/tsconfig` /
  `@anthropos/i18n`; dev ports 3000 / 3001 / 3002 and `dev:mobile … --port 3031`;
  `packages/graphql/codegen.ts` (`schema: 'http://localhost:8082/graphql/query'`,
  `documents: ['src/query/**/*.{ts,tsx}']`, `./src/__generated__/`, preset `client`);
  `NEXT_PUBLIC_GRAPHQL_ENDPOINT` → rc 1 (positive control `NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT` = 15
  occurrences); `UPGRADE-IMPACT-next16.md` present; commit velocity 247–441/month over Mar–Jul 2026,
  so "300+/month" holds.
- Clerk SDKs (`external_services.md:69`): `@clerk/nextjs ^6.39.2` in apps/web + hiring + integration
  **and** ant-academy `code/`; `@clerk/clerk-expo` in apps/mobile and ant-academy `mobile/`;
  studio-desk `@clerk/clerk-js ^5.52.3` + `@clerk/express ^1.3.47`. Sentinel exposes no webhook route
  (only three CI-workflow filenames match; positive control `casbin` → 24 files).
- studio-desk copilot (`:703-705`): `.env.example:57 AI_PROVIDER_CHAIN=azure-openai,openai`,
  circuit-breaker timeout/cooldown keys present, `src/services/ai/config.ts` pins `gpt-5.2` and
  `claude-opus-4-5-…` / `claude-sonnet-4-5-…`.
- studio-room (`app/studio @ aeec036a`, own ref): `services/ai.py:627-664` (`AnthropicProvider`),
  `:704-724` (`providers = {openai, azure, anthropic}`), `:383`/`:706-708` (bare `OpenAI(api_key=…)`),
  `configs/config_template.ini:30-31` (`https://api.openai.com/v1/chat/completions`),
  `gen.py:41-53` overriding only `*_API_KEY` / `*_ENDPOINT`, `tools/pdf2md.py:24` +
  `:127 model="mistral-ocr-latest"`. `bedrock|boto3` under `app/studio` → **0** (positive control
  `anthropic` → 34 lines), so "*Studio-Room was never on Bedrock*" holds.
- In-file anchors in `external_services.md`: `:543` is the *"When backend services add new GraphQL
  types"* heading, `:546` the restart-`backend` correction, `:567` the **Anthropic Direct** row — all
  three resolve to the constructs the text names.
- `chronos.md`: platform `045857c` touched both `docker-compose.yml` and `repos.yml` ✓ (2026-04-17);
  jobsimulation `09631fb2` exists on `main` with the quoted subject ✓ (2026-04-17);
  `app/internal/jobsimwiring/worker.go:24-35` spans the `asynq` import through `asynq.NewServer` ✓;
  no `chronos` service or clone entry survives ✓.

---

## What I could not settle

1. **`chronos`'s binary defaults** (drives B2's confidence). The repo is in no clone set, so
   `cmd/root.go` cannot be read. What would settle it: one `git clone
   git@github.com:anthropos-work/chronos` and a read of the `cmp.Or(os.Getenv("PORT"), …)` lines. If
   they are `8080`/`8081`, B2 collapses to "label the field as *defaults*, and record that the
   platform ran it on 8500/8501".
2. **`external_services.md:3` — "the repo is ARCHIVED on GitHub (2026-07-30)"** for
   `graphql-wundergraph`. Not decidable from a clone; needs `gh repo view anthropos-work/graphql-wundergraph
   --json isArchived`. (The corresponding *negative* claim for chronos is asserted elsewhere in the
   corpus and is equally unmeasured here.)
3. **`external_services.md:175` vs root `CLAUDE.md` on jobsimulation's prod teardown.**
   `external_services.md:175` says *"jobsimulation's ECS service is already destroyed … M810 landed
   for that row"*, citing `6092c6d2` in the **jobsimulation** repo (verified: the `module
   "jobsimulation"` block is gone there). Root `CLAUDE.md` says `module.jobsimulation_euwest1` *"is
   still declared in production terraform as the rollback path … teardown is **M810**"*. These may be
   two different artifacts — the service repo's own terraform root vs the `infrastructure` repo's —
   and `infrastructure` is in no clone set, which is exactly the reason the same paragraph gives for
   calling cms's prod state **UNMEASURABLE**. I did not book it because I would be adjudicating
   between two artifacts I can only see one of; settling it needs the `infrastructure` repo.
4. **`external_services.md:388`/`:390`** — "Apollo-compatibility flags enabled for stricter
   validation behavior" and "Introspection enabled in dev mode". Neither names a config key; the
   router's `config.compose.yaml` would settle both, but the claims are too unspecific to grade.
5. **`chronos.md`'s data model, RPC surface, triggerer behaviour** (`:44-64`, `:80-95`, `:154-160`,
   `:99-116`) — schema DDL, `FOR UPDATE SKIP LOCKED`, `LIMIT 10`, the 1 s due window, the
   `TimerWentOff` proto shape. All require the `chronos` and `proto` repos, neither of which is
   checked out. Nothing in the corpus contradicts them.
6. **`messenger.md:88`** — "Recent work in **v0.34.0** added whitelabel support". The whitelabel code
   is present at `fa47850d` (`internal/flow/whitelabel.go`), but attributing it to that tag needs
   `git tag --contains` on a tag list I did not enumerate; the version claim is unverified.
