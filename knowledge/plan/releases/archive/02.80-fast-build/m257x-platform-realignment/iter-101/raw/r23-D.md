# Seat D — reading #23

## Refs actually measured at this reading (re-derived at open)

| repo | path | sha |
|---|---|---|
| app | `stack-demo/app` | `b948604f` (checkout); `origin/main` = `2035f9a4` |
| app/studio (nested, own checkout) | `stack-demo/app/studio` | `aeec036a` |
| cms/studio (nested, own checkout) | `stack-demo/cms/studio` | `aeec036a` |
| platform | `stack-demo/platform` | `0c91421d` (= `origin/main`) |
| sentinel | `stack-demo/sentinel` | `88bc5592` |
| storage | `stack-demo/storage` | `4ce8ece5` (checkout); `origin/main` = `9f8cb53` |
| messenger | `stack-demo/messenger` | `fa47850d` (checkout); `origin/main` = `e9421c68`, **7 commits ahead** |
| cms | `stack-demo/cms` | `ca50c817` (checkout); `origin/main` = `f38c0c4a`, 2 ahead |
| jobsimulation | `stack-demo/jobsimulation` | `462343b0` (checkout); `origin/main` = `82cb66ec`, 4 ahead |
| roadrunner | `stack-demo/roadrunner` | `87d8d443` (= `origin/main`) |
| graphql-wundergraph | `stack-demo/graphql-wundergraph` | `60c229f3` |
| next-web-app / studio-desk / ant-academy | — | `bb3313bc` / `14a5442a` / `9c3843cd` |

Trees named per measurement below. The two nested `studio` checkouts were grepped at **their own ref**
(`git -C stack-demo/app/studio grep … aeec036a`), never through the host repo — `git -C stack-demo/app grep
… -- studio/` returns 0 for every predicate (`app/.gitignore:79` = `studio/*`), which is not evidence.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/services/studio-room.md` | 507 | yes |
| `corpus/architecture/platform-migration-status.md` | 323 | yes |
| `corpus/services/jobsimulation.md` | 309 | yes |
| `corpus/services/sentinel.md` | 166 | yes |
| `corpus/services/askengine.md` | 124 | yes |
| `corpus/architecture/README.md` | 38 | yes |
| **total** | **1467** | |

Search-pipeline positive controls run in the same passes: `git -C stack-demo/app grep -c
"anthropos-work/colony" HEAD -- go.mod` → 1 (against the 0-returning `authn` predicate);
`grep -c REDIS_ADDR docker-compose.yml` → 1 (against the 0-returning `STORAGE_RPC_ADDR` predicate);
`git grep -ic mistral aeec036a` → 22/3 files (against the 1-hit bare-`grep` reading). No search's stderr
was swallowed; every `rc=` was read.

---

## Blockers

### B1 — sentinel.md: "the only *service* address compose sets at all" is false — `GOTENBERG_URL` is one, on `backend`, in the same default profile

- **Anchor:** `corpus/services/sentinel.md:85` — *"It is also the only **service** address compose sets at
  all: there are **zero** `*_RPC_ADDR` variables left, so `backend → sentinel` is the one cross-process edge
  a local stack has. No other declared service sets it — the only ones left to check are `gotenberg`,
  `studio-desk` and `next-web-app` …"*
- **Why it is false:** at platform `0c91421` `backend`'s `environment:` block carries at least four
  cross-process service addresses, not one:
  - `docker-compose.yml:57` — `GOTENBERG_URL=http://gotenberg:3200`. `gotenberg` is a **declared compose
    service** (`docker-compose.yml:170-171`) in the **same default `core` profile as `backend`**
    (`:183` `profiles: [core, backend, all]`), so `make up` starts both and the edge is live on a stock
    stack. `app` reads the variable at two live call sites — `main.go:244` (`GotenbergURL:
    os.Getenv("GOTENBERG_URL")`) and `internal/web/backend/coursebuilder/handler.go:244` @ `app b948604`
    (identical at `2035f9a`, `main.go:261`) — and the consumer is `app/internal/converter/gotenberg.go`.
  - `docker-compose.yml:66` — `REDIS_ADDR=redis:6379` (`redis` is a compose service, `common.yml:24`).
  - `docker-compose.yml:93`, `:94` — `SUPABASE_DB_CONN` / `COPILOT_DB_CONN` → `postgresql`
    (`common.yml:2`).

  The `AUTHORIZATION_ADDRESS`/`*_RPC_ADDR` half of the sentence is exactly right (measured: 1 and 0
  occurrences across `docker-compose.yml` + `common.yml` + `.env_example`); the generalisation from
  *"the only RPC address"* to *"the only service address / the one cross-process edge"* is what breaks.
  The correct, qualified form exists elsewhere in the corpus:
  `corpus/architecture/architecture_overview.md:321` — *"the only cross-process **RPC** edge out of backend
  on a core stack"*.

  **It also contradicts the corpus twice with no ground-truth read at all** (the §5-rule-21 class):
  `corpus/services/gotenberg.md:50` — *"`GOTENBERG_URL=http://gotenberg:3200` (injected via the backend's
  compose `environment:`)"* — and `corpus/architecture/dependency_map.md:103` — *"`GOTENBERG_URL=…` is
  injected via the backend's compose env."* And it contradicts **itself inside the same sentence**, which
  names `gotenberg` as one of the declared services.
- **Confidence:** high

### B2 — jobsimulation.md: same over-claim — "the only cross-process hop a local stack has left"

- **Anchor:** `corpus/services/jobsimulation.md:145-146` — *"They are satisfied in-process inside `backend` —
  with one exception, `sentinel`, which is the **only** cross-process hop a local stack has left and the only
  service address `backend`'s compose entry carries (`docker-compose.yml:48`)."*
- **Why it is false:** identical refutation to B1. `backend → gotenberg` is a second cross-process hop on a
  stock `core` stack (`docker-compose.yml:57` sets the address, `:170-171` declares the service, `:183` puts
  it in `core`, `app/main.go:244` @ `b948604` reads it), and `docker-compose.yml:57`/`:66`/`:93`/`:94` are
  four more service addresses on `backend`'s compose entry. Booked separately from B1 because it is a
  distinct anchor in a distinct file with distinct wording — a claim-scoped repair that fixes one and not
  the other is the half-repair failure §5 rule 19 describes.
- **Confidence:** high

### B3 — platform-migration-status.md: same over-claim, and it contradicts this file's own `gotenberg` row

- **Anchor:** `corpus/architecture/platform-migration-status.md:93` (`messenger` row) — *"… so **all four are
  now set by no compose file at all** — the only cross-process service address left in a local stack is
  `AUTHORIZATION_ADDRESS` on `backend`, pointing at sentinel (`docker-compose.yml:48`)."*
- **Why it is false:** identical refutation to B1/B2. Sharper here because **the same file, twenty rows
  down, states the counter-evidence**: line 105 (`gotenberg` row) — *"third-party image,
  `docker-compose.yml:170-171` (`gotenberg/gotenberg:8`), default `core` profile (`:183` …)"*. A service in
  the default profile that `backend` addresses by compose-network name over HTTP is a cross-process service
  address by any reading. The clause that is true — *"all four `*_RPC_ADDR` are now set by no compose file
  at all"* — is verified (0 occurrences of `_RPC_ADDR` across `docker-compose.yml`, `common.yml`,
  `.env_example`, positive control `REDIS_ADDR` → 1); the trailing generalisation is not.
- **Confidence:** high

### B4 — platform-migration-status.md: the `messenger` prod-terraform anchor resolves at no ref this reading was given, and reads `= 1` where the map says `= 0`

- **Anchor:** `corpus/architecture/platform-migration-status.md:93` — *"**Prod — stopped, and here "scaled to
  zero" is still the right words:** `messenger/terraform/main.tf:29` `service_desired_count = 0` in a module
  that is otherwise intact (**121 lines**), the cms precedent again, with the reason in-comment (`:19-25` …);
  the image and task definition stay declared as the rollback path (`:27-28`)."*
- **Why it is false:** the cell **names no ref**, so per the instrument it grades against the checkout. At
  the ground-truth checkout `stack-demo/messenger` `fa47850d`, `terraform/main.tf` is **111 lines**;
  **line 29 is `container_definitions = <<EOF`**; `:19-25` and `:27-28` are ordinary module arguments
  (`service_cpu`, `health_check_path`, `vpc_id`, …), not the comment block described; and
  `service_desired_count` is at **line 19** with the value **`1`** — the opposite prod state from the one
  asserted. Refs tried, and what each gives:

  | ref | main.tf lines | `service_desired_count` |
  |---|---|---|
  | `fa47850d` (the audit's ground-truth checkout) | 111 | `= 1`, at line **19** |
  | `5dd9f80` … `e9421c68` (`origin/main`, 7 commits ahead) | 121 | `= 0`, at line **29**; `:19-25` + `:27-28` are exactly the comments described |

  So every anchor in the cell is correct at messenger `origin/main` `e9421c68` and none of them is correct
  at the tree this reading was told to read. I hesitated between MINOR and BLOCKER, and booked BLOCKER for
  three reasons: (a) the wrong half is not a line number but a **prod state** — a reader opening the
  stack-demo workspace sees a service declared to run where the map says it is stopped; (b) the map is
  otherwise scrupulous about this, and the **storage row in the adjacent cell says so in its own words** —
  *"State the ref or the sentence flips"*, and *"naming a second ref inside this cell makes every anchor in
  it ungradeable"* — so this is the one sibling cell that did not take the file's own rule; (c) §5 rule 36's
  witness-tree requirement is unmet: nothing in the cell says which copy of `messenger` settles it. The
  repair is a ref-pin, not a re-anchor.
- **Confidence:** medium (the substance holds at messenger `origin/main`; what fails is the ref-free anchor
  and the tree the instrument names)

---

## Minors

- `corpus/architecture/platform-migration-status.md:78-81` — the completeness derivation *"every service
  that has ever appeared in `docker-compose.yml` (same command on that file → **26** names)"* plus the audit
  recipe *"a name they return that has no row is a gap"*. Re-derived: `git log -p --follow --
  docker-compose.yml | grep -E "^\+  [a-z0-9_-]+:$" | sort -u` returns exactly 26, but **one is
  `app-network`** (a *network*, not a service) and **two are the same service under two names** (`graphql`
  and `wundergraph`, both covered by the single `graphql-wundergraph` row). So the true service cardinality
  is 25 / 24-distinct, and a reader following the file's own instruction flags two false gaps. The
  `repos.yml` half of the same paragraph (**14** names, all 14 with rows) re-derives exactly.
- `corpus/services/jobsimulation.md:203` — *"`app/main.go:216` (@ `origin/main` `7177374` …)"*. The sha is
  right (`7177374:main.go:216` is `func main()`), but `7177374` is **no longer `origin/main`** — that is now
  `2035f9a4`, where `main.go:216` is `if strings.Contains(dsn, "://")`. Label staleness, not a wrong pin.
- `corpus/services/jobsimulation.md:49-50` — *"Production terraform still names
  `http://backend.internal.anthropos:8081`."* The only prod terraform in the clone set that sets
  `JOBSIMULATION_RPC_ADDR` is `messenger/terraform/main.tf:92-93` @ `origin/main`, and it sets it from
  `var.jobsimulation_rpc_address`, which is declared **with no default** (`variables.tf:92-95`); the literal
  is supplied by `infrastructure`, a repo this corpus states repeatedly it has never read
  (`platform-migration-status.md:90`, `:101`, §5 row 3). The literal *does* appear in the platform's own
  `app/knowledge/service-dependencies.md:46`, so the claim is plausibly sourced — but "production
  terraform" mis-attributes it. Same sentence at `corpus/services/backend.md:241` (out of my partition).
- `corpus/services/sentinel.md:15` — *"The sentinel binary's default is 8080 (per its own README/CLAUDE.md)"*.
  The `sentinel` repo @ `88bc5592` has **no `README.md`** (repo root: `CHANGELOG.md`, `CLAUDE.md`,
  `Dockerfile`, `Dockerfile.dev`, `Makefile`, `cmd`, `cog.toml`, `go.mod`, `go.sum`, `init_policy.sql`,
  `internal`, `knowledge`, `local_superadmin_grants.sql`, `main.go`, `singularity-manifest.md`, `terraform`).
  The claim itself is right — `CLAUDE.md:101` documents PORT default 8080, and `cmd/root.go:47` is
  `cmp.Or(os.Getenv("PORT"), "8080")`.
- `corpus/services/sentinel.md:44` — the *"only as a fixture string in sentinel's own tests"* list names
  `casbin_test.go` and `rpc_test.go` but omits `internal/authorization/manager_test.go:21`
  (`manager := "manager"`). The claim ("only in tests") still holds; the list is short one member.
- `corpus/services/sentinel.md:5` vs `:89` — line 5 speaks of messenger's compose block in the present
  tense (*"its compose block sets no `AUTHORIZATION_ADDRESS` and declares no `depends_on: sentinel`"*) while
  line 89 correctly records that `messenger` **is not a compose service any more** (deleted at `838d907`).
  The `AUTHORIZATION_ADDRESS` clause immediately before it is ref-pinned to `0dab54d`; this sentence is not,
  and the pin's scope is its own block.
- `corpus/services/jobsimulation.md:160`, `:163`, `:133` — three `file:line` citations drop a leading path
  segment: `cms/directus/collections/jobsimulation.go:1594-1597` is really
  `cms/**internal**/directus/collections/jobsimulation.go` (lines exactly right — `:1596` returns
  `SimulationVoiceEngineGptrealtime` for a nil field); `calls/livekit.go:131-135`/`:140-144` is really
  `internal/calls/livekit.go` (lines exactly right, inside `CreateAgentDispatch` at `:106`); and
  `intelligence.go:1700` is `app/**internal/organization**/intelligence.go` (line exactly right).
  `jobsimulation.md`'s own "Key directories" block lists neither `internal/calls/` nor `internal/runner/`.
- `corpus/services/studio-room.md:348-359` — the `requirements.txt` code block reorders `mistralai` to third;
  in the file @ `aeec036a` it is **line 8**, after `jinja2`. The *set* is exact (9 packages, unpinned), and
  the prose elsewhere cites `requirements.txt:8` correctly.
- `corpus/services/askengine.md:104-109` — the test-file list (`sandbox` 49 / `executor` 13 / `prompt` 10 /
  `followups` 14, all four re-derived exactly) omits `internal/askengine/anthropic_test.go` (1 `func Test`).
  The sentence hedges with *"the bulk"*, so this is presentational.

---

## Positively cleared (audited zeros)

**ENUMERATED** — predicate re-derived from source independently, cardinality stated before any arithmetic:

- `studio-room.md:184` *"`gen.py` registers **exactly nine** arguments (`gen.py:484-492`)"* — enumerated the
  `parser.add_argument` calls in `gen.py` @ `aeec036a`: **9**, occupying lines **484–492** exactly, one per
  line, matching the documented flags one-for-one (`-i/--interactive`, `-m/--media`, `-f/--force` with
  `dest="forced"`, `--simid`, `--branch{stable,experimental}`, `--prompt`, `--annotations` default `"{}"`,
  `--pipeline` default `linear`, `--blueprint`). No `--template` / `-t` among them.
- `studio-room.md:362-374` the whole `mistralai` blockquote — enumerated at the **nested repo's own ref**:
  `git -C stack-demo/app/studio grep -ic mistral aeec036a` → `requirements.txt:1`, `tools/pdf2md.py:18`,
  `tools/r3.py:3` = **22 hits / 3 files**, matching the stated 22/3 and the stated per-file split.
  `pdf2md.py:24` is `from mistralai import Mistral`; `:96` `client = Mistral(api_key=api_key)`; `:127`
  `model="mistral-ocr-latest"`; `r3.py:194` `if "MISTRAL_API_KEY" not in env`. `tools/` holds exactly
  `any2pdf.py`, `pdf2md.py`, `md2cleanMd.py`, `r3.py` — the documented chain. Go execs exactly **two**
  studio scripts (`git grep -nE '"studio/[a-z0-9_]+\.py"' b948604 -- '*.go'` → 2:
  `studioManager.go:119` `studio/gen.py`, `:1045` `studio/postgen.py`). `services/ai.py:705-708` is
  `{'openai','azure','anthropic'}` and `ai.py:1-2` imports only `openai`/`anthropic`. The three-instrument
  caveat re-measured: recursive `grep -ril mistral stack-demo/app/studio/` → **1** file
  (`requirements.txt`) because `app/studio/.gitignore:2` is `tools/`; `app/.gitignore:79` is `studio/*`;
  `cms/.gitignore:129` is `studio/*`; and `stack-demo/cms/studio` is the **same** repo at the **same** sha
  `aeec036a`. Every number in that blockquote reproduces.
- `platform-migration-status.md:76-78` *"14 names, all 14 have rows"* — `git log -p --follow -- repos.yml |
  grep '^+.*- name:' | sort -u` → exactly the 14 listed; each has a row.
- `platform-migration-status.md:92` the `STORAGE_RPC_ADDR` two-ref measurement — enumerated at both refs.
  @ `2035f9a`: **3** hits, all three comments (`main.go:504`, `internal/jobsimwiring/wiring.go:101`,
  `internal/storagens/callsites_test.go:189`). @ `b948604`: **15** hits, of which **7 are env lookups**
  (`main.go` ×3 at `:446`/`:524`/`:992`, `jobsimwiring/wiring.go:115`, and one in each of `cmd/import`,
  `cmd/academyImport`, `cmd/academy-asset-upload`), **6 of the 7 spelled `os.Getenv`** and the seventh a
  lowercase `getenv` helper, with the residual 8 = 2 doc comments + 2 error strings + **3** `t.Setenv` +
  **1** `t.Fatal` message. Every sub-count in that cell is exact, including the "+1 overstated" correction.
- `platform-migration-status.md:87` *"Owns **seven** domains … each with **its own** wiring call site"* —
  enumerated all seven directories under `internal/` @ `2035f9a` (cms, jobsimulation, skiller, skillpath,
  storage, messenger, customeriosync: 7/7 present) and resolved all seven `main.go` anchors at `2035f9a`:
  `:690` `skiller.NewSkillerManager`, `:721` `jobsimwiring.Wire`, `:751` `skillpath.NewSessionManager`,
  `:1153` `appcms.Wire`, `:524` `internalstorage.NewManager`, `:1471` `msgadapters.Wire`, `:395`
  `customeriosync.New`. `internal/roadrunner/` does not exist; `internal/jobsimulation/runner/` does, and
  `jobsimwiring/wiring.go:123` is `jsrunner.NewRunnerManager(getenv("JUDGE0_API_KEY"),
  getenv("JUDGE0_BASE_URL"))` — five lines later than at `b948604` (`:118`), which is the number
  `d11a403`'s own message quotes.
- `askengine.md:47-48` *"**60** `TableDef`s, **57** `Schema: "public"`, **3** `Schema: "directus"`"* —
  enumerated the SET, not the sum: `{Name: "` entries in `internal/askengine/registry.go` @ `b948604` = **60**,
  **0 of them without an explicit `Schema:`**, split 57/3. All 19 dotted names enumerated: 16
  `jobsimulation.*` (each `Schema: "public"` + `RealName:` → the public table) + 3 `directus.*`. The
  `skiller.*` / `skillpath.*` transition aliases the cell invokes are real but built **programmatically**
  (`registry.go:551-577`), so `TableRegistry` the *map* holds ~93 keys against 60 distinct defs — the
  document's "60 TableDefs" is the right unit for the claim it makes.
- `askengine.md:104-109` test counts — `grep -cE '^func Test'` per file @ `b948604`: sandbox **49**,
  executor **13**, prompt **10**, followups **14**. All four exact.
- `sentinel.md:26` *"6 request types, 6 policy types, 3 role groupings, 6 matchers"* — enumerated
  `ModelConf` in `internal/authorization/casbin.go:13-45`: `r,r2..r6` = 6; `p,p2..p6` = 6; `g,g2,g3` = 3;
  `m,m2..m6` = 6. Every matcher's body matches its table row (incl. `m` substituting
  `Tier_TIER_FREE.String()` and `m6` using `g3(p6.org, r6.sub)`).
- `sentinel.md:70-83` the 19-row RPC table — enumerated `func (s *RPCHandler)` in `internal/rpcsrv/rpc.go`:
  **19** methods, and the set is exactly the documented set (Check, BulkCheck, CheckFeature,
  CheckOrganizationFeature, Add/RemoveUserFromTier, Get(Organization)Quotas, OrgCheckPermission,
  OrgAdd/Remove/ReplaceUserRole, OrgClearAll, OrgAllow/DisallowUserToUseFeature,
  OrgMembershipsAllowedToUseFeature, OrgGet/SetOrganizationFeatureCredits, Reload).
- `sentinel.md:43-48` *"There is no `manager` role"* — `grep -ci manager init_policy.sql` → **0**. The four
  `MembershipRole` values are exactly `app/internal/data/ent/enum/membership.go:8-15` (admin, member,
  candidate, content_creator) and `init_policy.sql:88-118` is the `content_creator` block, first line to
  last row.
- `jobsimulation.md:39-45` *"the 23 session/run tables"* — `grep -cE '^CREATE TABLE'` in
  `terraform/migrations/20260722081626_jobsim_data_model.sql` @ `b948604` → **23**, and the set includes
  `sessions`, which `20260722104506.sql:2` replaces with `job_simulation_sessions` and `:79` drops
  (`DROP TABLE "sessions";`).
- `jobsimulation.md:130-135` the mirror-drop migration — `20260729133514.sql` re-read whole:
  `:15-23` the two id remaps, `:36-44` the orphan NULLing, `:58-62` the *"5. Drop the mirrors."* comment,
  `:62-63` `DROP TABLE "local_jobsimulation_sessions"` / `"local_skill_path_sessions"`, and **0
  `INSERT INTO`** (the single case-insensitive `insert` match is the function name at `:64`, which is why
  a `grep -ci` would have said 1 — the predicate, not the count).
- `platform-migration-status.md:25-27` and `:129-131` the entry/line counts — `repos.yml` = **28 lines / 4
  entries** @ `0c91421`; **6** @ `0dab54d` and `d11a403`; **9** @ `2adcf71`. `docker-compose.yml` = **186**
  lines @ HEAD, **271** @ `0dab54d`; `git show --numstat 838d907` = `11 96 docker-compose.yml`. Compose
  service blocks: **5** @ `0c91421` (+2 from `common.yml`), **8** @ `d11a403`, **11** @ `2adcf71` — the
  three counts the `roadrunner` row quotes.
- `corpus/architecture/README.md` — the index enumerates **all 10** non-README files in
  `corpus/architecture/`, with no orphans in either direction. Its `authn` claim re-derived across the whole
  clone set: `anthropos-work/authn` in `go.mod`/`go.sum` → **0** in every one of app, sentinel, storage,
  messenger, cms, jobsimulation, roadrunner, graphql-wundergraph, with `anthropos-work/colony` → 1 in
  `app/go.mod` as the positive control, and `colony/authn` imported in app Go source.

**RE-DERIVED** (weaker — arithmetic the document itself displays, re-computed):

- `platform-migration-status.md:204-205` / `:220-221` — ran the guard itself:
  `python3 .agentspace/rosetta-extensions/stack-core/platform_alignment_guard.py
  corpus/architecture/platform-migration-status.md stack-demo/platform/repos.yml` → exit 0, *"assertion F
  resolved 92 citation(s) — 22 subject-checked, 69 range-only, 1 outside any service block; 0 unresolvable;
  0 read from the WORKTREE"*. The table's 22 / 69 / 1 match the run, and 22+69+1 = 92. Labelled RE-DERIVED,
  not ENUMERATED: I did not independently enumerate the citation set the guard ranges over, and note the
  guard's own reach does not cover terraform citations — which is exactly the gap B4 sits in.
- Every non-guarded `file:line` in the four larger files that I opened individually resolved and named the
  construct claimed. Named because they are load-bearing and all held: platform `docker-compose.yml`
  `:5/:18/:28/:48/:57/:82/:83/:73-75/:102-103/:84-92/:112/:119/:135/:141/:143/:151/:160/:168/:170-171/:183`,
  `common.yml:2/:22/:24`, `repos.yml:2-10/:12-13/:14-17/:18-20/:23-25/:26-28`, and @ `2adcf71`
  `docker-compose.yml:83/:93-94/:113/:119/:140` + `Makefile:10` (`PROFILE ?= graphql`) + `repos.yml:17-19`;
  @ `0dab54d` `docker-compose.yml:48/:88-89/:90/:91` (the sole AWS bind) and the four
  `*_RPC_ADDR=http://backend:8083` at `:173/:174/:176/:183`; app `main.go:15/62/63/216(@7177374)/
  244/285/286/394/395/504/524/525/547/670/690/721/751/980-982/1102/1153/1204/1416-1421/1445/1450/1471/1473/
  1552` and `:212`/`:614` @ `b948604`, `env_guards.go:92-111`, `terraform/main.tf:181/415-416/419-420`,
  `internal/storage/service.go:22/:24`, `internal/jobsimwiring/wiring.go:101/123/147-148`,
  `internal/jobsimulation/ai/ai.go:90`, `internal/organization/intelligence.go:1700`; storage
  `terraform/main.tf` (18 lines, `:9-11`, `:13-16`, `:18`) + `storage.tf:22-25` + `custody` → **0
  occurrences** + **0 `moved` blocks** + `M903` → exactly 1 occurrence + commit `d3e6d32` (*"M903 never
  ran."*), all @ `9f8cb53` as the cell names; jobsimulation `terraform/main.tf:15-22` and `:24-40` @
  `82cb66e` with `service_desired_count` gone from the file and the four 2026-08-04 commits present
  (`6092c6d2`, `caf36c96` = *Merge pull request #439*, committer `GitHub`, `1e40d184`, `82cb66ec`); cms
  `terraform/main.tf:39 = 0` at both its checkout and origin/main, plus commit `6efa1d5` merged `f38c0c4`
  with the quoted M810 body; roadrunner `terraform/main.tf:19 = 1` with `git blame -L 19,19` → `84a4b4f`
  (2025-12-15) and `e45eb61` (2026-05-27) touching **line 11 only** (module source-URL swap); sentinel
  `go.mod:3`, `Dockerfile:2`, `Dockerfile.dev:2`, `terraform/locals.tf:4-5` (256/128), `init_policy.sql:63-66`
  and `:88-118`, `Makefile` `initdb` hard-coded DSN, `local_superadmin_grants.sql` p3/p4 blocks;
  graphql-wundergraph `supergraph-config-prod.yaml` (one subgraph, `backend`), `schemas/` (one file),
  `subgraphs.conf` (`BACKEND=v1.360.0`), `terraform/main.tf:20 = 1`, and `915da06` (*"fold cms subgraph into
  backend (supergraph 2→1)"*, 2026-07-29); jobsimulation repo `cmd/root.go:77`/`:78`,
  `internal/graph/queries.resolvers.go:70`, `internal/runner/runner.go:1-3`, `internal/calls/livekit.go:106`/
  `:131-135`/`:140-144`, `internal/worker/worker.go:27` (10) / `:104` (25), four subcommands whose `Use:`
  strings are exactly `aggregate`/`clone-session`/`test-command`/`validate`, no `SilenceUsage`/
  `SilenceErrors`, `ENTRYPOINT ["./application"]` with no `CMD`, `CLAUDE.md:88` `go run . serve`;
  askengine `bedrock.go:25/26`, `executor.go:18/21/24/90/97`, `main.go:150` (`askEngineMaxConns = 6`),
  `rules.md` = 146,219 bytes, `handler.go:34/41/473`, all 11 `/ask` routes at
  `internal/web/backend/backend.go:171-188`, `prompt_test.go:176` `RulesBlockIsIdentical`, CHANGELOG
  `v1.267.0 - 2026-05-07` and `v1.340.0 - 2026-07-17`; studio-room `gen.py:18-28`/`:41-53`/`:205-238`/
  `:241-271`/`:273-282`/`:450-456`, `postgen.py:396-398` (three `required=True`), `exporter.py:513-550` and
  `:518-519`, `model.py:59`/`:467-469`, all three `configs/*.ini` at `max_tokens = 4000`,
  `studioManager.go:94-96`/`:119`/`:1045`, `markdownManager.go:26` `OCRProcess`, `Dockerfile:45-46`,
  `.github/workflows/build-production.yml:29` `additional_repo: "anthropos-studio-room:studio"`, and
  CHANGELOG `v1.360.1` = *"pull studio via additional_repo like cms"*.
- Platform commit dates spot-checked for every sha the map and the two service docs cite: `045857c`,
  `fdfa189` (both 2026-04-17), `21429b7` (2026-07-07), `a4db680` (2026-07-21), `a2a3ee6` (2026-02-27),
  `cb6ebf5` (2023-04-30), `8770fe6` (2023-05-04), `467965a` (2023-09-11), `1474b1f` (2023-12-01),
  `84862d1` (2024-05-29), `ef4b449` (2024-09-02), `b43b99a` (2026-02-26), `c17cc9a` (2026-04-15),
  `236771f` (2026-07-29), `2adcf71` (2026-07-31), `d11a403`/`6060315`/`ef32d4c`/`0dab54d` (2026-08-03),
  `838d907` (2026-08-05, PR #26), `05b4035` (2026-04-24). All correct.
- `jobsimulation.md:52-53` *"five services … (**seven** effective, once `include: common.yml` adds the
  `postgresql`/`redis` floor)"* and `:267-268` *"exits 0 and starts only postgresql, redis and sentinel"* —
  both hold: `sentinel`, `postgresql` and `redis` declare no `profiles:` key at `0c91421`.

---

## What I could not settle

- **GitHub archive state** for `chronos` (*"NOT archived"*), `intelligence`, `skiller`, `skillpath`,
  `graphql-wundergraph` (*"ARCHIVED 2026-07-30"*), `storage`/`messenger`/`roadrunner`/`cms` (*"not
  archived"*). Archive state lives in the org API, never in the git objects — the map says exactly this in
  the `jobsimulation` row. Settled only by `gh api repos/anthropos-work/<repo> --jq .archived`.
- **`platform-migration-status.md:126-131`** — *"**93 repositories** in `anthropos-work` … **46** are named
  by no corpus document"*. The `4 in repos.yml` half re-derives exactly; 93 and 46 need the org API plus a
  corpus-wide mention index. Not attempted.
- **`platform-migration-status.md:92`'s M903 plan-directory sub-claim** — *"`app/knowledge/plan/releases/
  09.00-support-in-app/m903-s3-custody/` @ app `2035f9a` … self-declares `status: planned` … and *'Planned,
  not started — no terraform applied, no branch'* in its `progress.md`"*. **Deliberately not checked**: my
  instructions bar reading, listing or grepping anything under `knowledge/plan/**` without scoping that to a
  repository, and I would rather leave a sub-claim unsettled than test the boundary of a hard bar. Every
  *other* element of that sentence's evidence chain — the storage-repo side — verified exactly (M903 occurs
  once, in `storage/terraform/storage.tf:22-25`; commit `d3e6d32` says *"M903 never ran"*; no `moved` blocks;
  `custody` appears 0 times).
- **`platform-migration-status.md:88`, `:90`, `:101` — the `infrastructure`-repo blind spot.** `cms`'s
  rollback declaration, `roadrunner`'s `= 1`-vs-fold contradiction and `customerio-sync`'s prod state all
  turn on `infrastructure/terraform/production/services.tf`, which is in no clone set. The map records this
  honestly rather than papering over it; I am recording that I could not close it either. The same gap is
  what makes MINOR #3 above (`http://backend.internal.anthropos:8081`) unsettleable rather than refutable.
- **`sentinel.md:97`** — *"Without it, sentinel crash-loops with `pq: no schema has been selected`."* A
  runtime behaviour; settled only by starting sentinel against a Postgres with no `sentinel` schema.
- **`studio-room.md`'s runtime/pipeline behaviour claims** (retry-on-failure, resume-from-checkpoint,
  per-step async execution, the `{simid}_pre_generation.json` / `{simid}_task_state.json` /
  `{simid}_usage.json` triple on disk). The code paths that write them exist and the config's
  `worklog_path = workspace/trace` is right; proving the on-disk shape needs an actual generation run,
  which costs LLM calls.
