# Seat A — reading #23

**Refs at this reading (re-derived at open, per §5 rule 26).**
platform `0c91421d` (level with `origin/main`; the frozen table's `0dab54df` superseded by the addendum),
app `b948604f` (its `origin/main` = `2035f9a4`, 93 commits ahead; v1.367.0 = `9d00a3131`),
next-web-app `bb3313bc` (origin/main `8297c684`), sentinel `88bc5592`, storage `4ce8ece5`,
messenger `fa47850d` (origin/main `e9421c68`), cms `ca50c817` (origin/main `f38c0c4a`),
graphql-wundergraph `60c229f3`, roadrunner `87d8d443`, jobsimulation `462343b0` (origin/main `82cb66ec`),
studio-desk `14a5442a`, ant-academy `9c3843cd`, nested `app/studio` + `cms/studio` both `aeec036a`,
per-stack `stack-demo/rosetta-extensions` `ab81527a` (tag `fast-build-m257x-iter-58`).

**Search-instrument discipline (§5 rule 44).** Every tree-wide zero below was taken with `git grep`
per tree **at that tree's own ref**, over the 15 git trees enumerated by
`find stack-demo -name .git -maxdepth 4` (the two nested `studio` checkouts included), each with a
positive control in the same pass and stderr read. Gitignored-but-tracked census: 13 such paths
(ant-academy 2, app 2, app/studio 4, cms/studio 4, roadrunner 1) — all visible to `git grep`.
NUL-byte census over `next-web-app`: measured with `tr -dc '\000' < f | wc -c`; the only NUL-bearing
**text** file is `apps/web/src/hooks/useCoursebuilder.ts` (**1** byte) — `git grep` still saw it here,
and it contains 0 `NEXT_PUBLIC_BACKEND_API_URL` and 0 `fetch(`, so it does not move any count in B1.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/architecture/external_services.md` | 883 | yes |
| `corpus/services/chronos.md` | 245 | yes |
| `corpus/services/messenger.md` | 189 | yes |
| `corpus/architecture/frontend_architecture.md` | 105 | yes |
| `corpus/services/gotenberg.md` | 84 | yes |
| **total** | **1506** | |

---

## Blockers

### B1 — "29 direct REST/SSE calls" counts env-var *mentions*, not calls; the real count is ~45

- **Anchor:** `corpus/architecture/frontend_architecture.md:39` — verbatim:

  > "There are **no** direct Connect/gRPC calls from the frontend — **but there are direct REST/SSE
  > calls**, **29 of them across 21 non-test files** hitting `NEXT_PUBLIC_BACKEND_API_URL` … : the four
  > `packages/core-js` REST clients (course-builder, credits, Talk-to-Data, workforce — **12 sites
  > between them**), … (Measured at `next-web-app` `bb3313bc`, the ref this stack builds; … origin/main
  > `ad767d1b` reads **31 across 22** … The long-standing *"~15 sites"* undercounted by half.)"

- **Why it is false.** The arithmetic is exactly right and the SET it ranges over is not the set the
  sentence names — §5 rule 24 in its pure form. Both published pairs reproduce **byte-exactly** under
  one predicate, and that predicate is *"occurrences of the literal string
  `NEXT_PUBLIC_BACKEND_API_URL` in non-test `.ts`/`.tsx` files"*, **not** *"REST/SSE call"*:

  ```
  git grep -n NEXT_PUBLIC_BACKEND_API_URL <ref> -- '*.ts' '*.tsx' | grep -v '\.test\.ts' | grep -v ':e2e/'
    bb3313bc  -> 29 lines / 21 files      (the sentence's "29 across 21")
    ad767d1b  -> 31 lines / 22 files      (the sentence's "31 across 22")
  ```

  In the four `packages/core-js` clients those "12 sites" are **3 per file, and none of the three is a
  call**: a header comment, the `const BACKEND_URL = process.env.NEXT_PUBLIC_BACKEND_API_URL` read, and
  a `'… is not set — required by @anthropos/core-js/…'` error-message string. Measured
  (`next-web-app` `bb3313bc`), the outbound-request sites in those same four files are:

  | file | env-var mentions ("sites" as counted) | `await fetch(` / `new EventSource(` |
  |---|---|---|
  | `packages/core-js/src/coursebuilder/api.ts` | 3 | **18** (`:413`, `:449`, `:497`, `:592`, `:645`, `:678`, `:705`, `:726`, `:745`, `:831`, `:865`, `:885`, …) |
  | `packages/core-js/src/credits/api.ts` | 3 | **3** (`:115`, `:133`, `:159`) |
  | `packages/core-js/src/talkToData/api.ts` | 3 | **3** (`:45`, `:225`, `:290`) |
  | `packages/core-js/src/workforce/api.ts` | 3 | **1** (`:56`) |
  | | **12** — the figure published | **25** — the figure claimed |

  The decisive detail: `workforce/api.ts` (**1** REST call) and `coursebuilder/api.ts` (**18**) each
  contribute exactly **3** to the published total. The counted quantity is not even *monotone* in the
  quantity claimed, so this is a wrong predicate rather than a loosely-worded one. Across all 21 files
  the `fetch(`/`EventSource(` total is **45**, against the 29 published — e.g.
  `apps/web/src/app/(authenticated)/(verified)/ai-readiness/_components/EmailPreviewModal.tsx` carries
  **one** env mention and **four** `await fetch(` (`:168`, `:200`, `:248`, `:295`).

  Materiality: the sentence exists precisely to repair an undercount — it says so
  (*"The long-standing '~15 sites' undercounted by half"*) — and is the corpus's only sizing of the
  non-GraphQL frontend surface. It undercounts that surface again, in the same direction, by ~35 %.

- **Confidence:** high. **Why I hesitated:** "site" *could* be read charitably as "reference site", in
  which case 29/21 and 31/22 are correct. I booked it because the sentence's grammatical subject is
  unambiguous — *"direct REST/SSE calls, **29 of them**"* — and because the workforce-vs-coursebuilder
  asymmetry above shows the counted quantity cannot stand in for the claimed one.

---

## Minors

- `corpus/architecture/external_services.md:455` — the verbatim quote attributed to
  `graphql-wundergraph/CLAUDE.md:39` (*"Since cms-in-app the platform compose `graphql` service builds
  from the **production** Dockerfile"*) lives at **`:33`** (the *Platform compose* table row). `:39` is a
  different sentence asserting the same proposition (*"Since the compose stack builds from the production
  Dockerfile, a schema change in `app` does not appear at `:5050`…"*), so the corpus's *claim* holds at
  both lines; only the quote attribution drifts. Rest of that paragraph verified: `60c229f` is dated
  2026-07-30, subject *"docs: backend is the sole subgraph — correct the compose build path (#295)"*.
- `corpus/services/messenger.md:42` — the **prod** cell cites `messenger/terraform/main.tf:29`
  `service_desired_count = 0` and `:27-28` for *"image and task definition stay declared"*. Both resolve
  **byte-exact at `e9421c6`** (= messenger `origin/main`), which the blockquote's lead sentence
  (`:37-38`) declares. They do **not** resolve at the audit's own ground-truth checkout `fa47850d`,
  where `:19` reads `service_desired_count = 1` and `:29` is `container_definitions = <<EOF` (the
  scale-to-0 landed in `5dd9f80`, after the checkout). Booked MINOR, not BLOCKER: the ref is declared,
  the citation is exact there, `CITE_REF=auto` grades at `origin/main` (§5 rule 41), and the twin claim
  at `external_services.md:175` behaves identically (verified: cms `f38c0c4a:terraform/main.tf:39` = 0;
  jobsimulation `82cb66ec` has **no** `module "jobsimulation"` block at all). The residual nit is §5
  rule 33's own rule — the pin sits outside the **cell** it dates.
- `corpus/services/messenger.md:163` — the `BREVO_KEY` row's middle column is headed *"Value in the
  deleted compose block"* and reads *"(empty)"*, but the `messenger` compose block at `0dab54d` never
  set `BREVO_KEY` at all (measured: 0 hits for `BREVO` in `0dab54d:docker-compose.yml`). It arrived
  through `env_file: .env` — `.env_example:82` `BREVO_KEY=`. Effective value unchanged; attribution wrong.
- `corpus/architecture/external_services.md:219` — `stack-injection/tests/test_injection.py:1109` is one
  line past `def test_backend_the_actual_reader_is_repointed(self):` at **`:1108`** (it lands on the
  test's first comment line). Line drift inside the named construct. The rest of that paragraph is
  byte-exact at the pinned per-stack clone `ab81527a` — `gen_injected_override.py:84`
  `DIRECTUS_DATA_CONSUMERS = ("cms", "backend")`, `:669` the `if with_directus and name in …`, `:670`
  the `DIRECTUS_BASE_ADDR` append — and `test_only_cms_is_repointed_not_other_services` survives only
  as a reference inside that comment, i.e. is indeed "gone" as a test.
- `corpus/services/messenger.md:108` — *"the binary still constructs **four** Connect-RPC clients"*.
  `cmd/root.go:118-142` constructs **five** client objects (`cmsV1Client`, `usersClient`,
  `organizationsClient`, `skillerClient`, `jobsimulationClient`) over **four** distinct addresses —
  `usersClient` and `organizationsClient` both read `BACKEND_USERS_RPC_ADDR`. The doc's own enumeration
  writes them as *"CMS, backend users + organizations, skiller, and jobsimulation"*, which groups them
  correctly, so the reading is defensible; only the noun ("clients") is loose.
- `corpus/architecture/external_services.md:849-852` — unbalanced parenthesis inside the bash comment
  block: it opens `... are up (docker-compose.yml:101-109 — ...` and never closes. Cosmetic; the
  anchor itself is exact (`:101` `depends_on:` → `:109` the `sentinel` condition).

---

## Positively cleared (audited zeros)

Legend per the briefing: **ENUMERATED** = predicate re-derived from source and cardinalities compared;
**RE-DERIVED** = only the document's own displayed arithmetic recomputed. Only ENUMERATED is a clearance.

### `corpus/architecture/external_services.md`

- **ENUMERATED** — the `core` selection starts **five** containers (`:170-172`). Compose at `0c91421`
  declares 5 services (`sentinel:5`, `backend:28`, `studio-desk:112`, `next-web-app:143`,
  `gotenberg:170`); `profiles:` keys exist at **only** `:110`, `:141`, `:168`, `:183`, so `sentinel` is
  profile-less, and `common.yml`'s `postgresql`/`redis` declare none either → floor = 3, `core` members
  = `backend` (`:110`) + `gotenberg` (`:183`) = 5. Both cited anchors name the exact `profiles:` line
  that establishes membership.
- **ENUMERATED** — *"five services (seven effective … eight/ten at `0dab54d`)"* (`:304-306`). At
  `0dab54d` compose declares 8 (`sentinel, backend, storage, customerio-sync, messenger, studio-desk,
  next-web-app, gotenberg`) + 2 from `common.yml` = 10.
- **ENUMERATED** — `d11a403` deleted the `cms`, `jobsimulation` and `roadrunner` compose services **and**
  their `repos.yml` entries (parent had 11 services / 9 repo names; child has 8 / 6); `838d907` deleted
  `storage`, `messenger`, `customerio-sync` and dropped `storage` + `messenger` from `repos.yml`
  (customerio-sync was never there — the doc says exactly that). `:173-174` correct in both halves.
- **ENUMERATED** — the subgraph-count table (`:376-381`), read off `supergraph-config-prod.yaml` per
  commit: `749dc86~1` = 5 (backend, skiller, jobsimulation, cms, skillpath) · `749dc86` = 4 ·
  `7c17e63` = 3 · `915da06` = **1**. Dates 2026-06-24 / 2026-07-21 / 2026-07-29 all match.
  `git show --name-status 915da06` marks **both** `schemas/cms.graphqls` and
  `schemas/jobsimulation.graphqls` as `D`, and the commit subject really does read *"supergraph 2→1"*
  against a three-entry tree — the warning at `:384` is correct.
- **ENUMERATED** — the whole "no subscriptions" paragraph (`:387`), all six sub-claims:
  `grep -rn 'sse\|subscription' *.yaml` → rc 1 with positive control `grep -rln backend *.yaml` → 3 files;
  `915da06~1:supergraph-config-prod.yaml` still reads `protocol: "sse_post"`;
  `git log -S 'protocol: "ws"'` → empty, control `-S sse_post` → 5 commits;
  `bba862f` is **not** an ancestor (rc 1) and `git branch -a --contains` names only
  `remotes/origin/feat/use-web-socket`; `schemas/backend.graphqls` has `type Mutation` at `:4053`,
  `type Query` at `:4912`, **no** `type Subscription`, and exactly **6** `Subscription` hits, all Stripe/plan
  field names (`:1528`, `:1555`, `:4385`, `:4389`, `:4391`, `:4418`).
- **ENUMERATED** — the router's port/terraform row (`:3`, `:367`): `terraform/main.tf:20`
  `service_desired_count = 1`; `terraform/locals.tf:8` `port = 8080`; `terraform/main.tf:48-49`
  `containerPort`/`hostPort` = `${local.port}`; `config.prod.yaml:5` `listen_addr: 0.0.0.0:8080` and
  `:13` `graphql_path: /graphql`. `"5050:8080"` existed only as the local host mapping and is absent at HEAD.
- **ENUMERATED** — the pre-deletion `depends_on` set (`:420-425`): at `1e8e754`, `docker-compose.yml:19-27`
  names exactly `backend`, `jobsimulation`, `cms`, `storage`, each `condition: service_started`; the set
  has read that way since `a4db680` (2026-07-21, *"decommission standalone skillpath service (M507)"*).
  The `464dfe3` warning (`:434-437`) holds: it names only **two** (`backend`, `storage`), is dated
  2026-07-28, `merge-base --is-ancestor 464dfe3 0c91421d` exits **1**, and it lives only on
  `remotes/origin/feat/cms-in-app`.
- **ENUMERATED** — the three-era Dockerfile table (`:445-451`): `63d285c` (2024-06-20, service named
  `wundergraph`, `context: git@github.com:…#main`, **no** `dockerfile:` key) and `719befb` likewise;
  `2c85211^` no key → `2c85211` `dockerfile: Dockerfile.dev`; `67ba772` (2026-03-02) raised the context;
  `b56d731:docker-compose.yml` still has `  graphql:` at **`:22`** while `360efd4` has none (rc 1) with
  the stated positive control `  backend:` at **`:28`**; `1e8e754` lines 6-8 read
  `dockerfile: graphql-wundergraph/Dockerfile.dev`.
- **ENUMERATED** — `Dockerfile.dev`: one schema `COPY` at `:18`, the replacing comment at `:19-20`, one
  `awk` at `:23`, `wgc@0.104.0` at `:7`, runtime `ghcr.io/wundergraph/cosmo/router:0.275.0` at `:29`.
  Retired subgraph URLs at `:484`/`:485` reproduce exactly from `915da06~1`
  (`http://jobsimulation:8400/query` + its `sse_post` block; `http://cms:8090/query`).
- **ENUMERATED** — no `graphql` token in **any** `profiles:` key at `0dab54d` (the 7 keys are
  `core/backend/all` ×2, `storage-legacy`, `customerio-sync/all`, `messenger`, `studio-desk/all`,
  `frontend/all`), and `Makefile:10` `PROFILE ?= core`. `:418` correct.
- **ENUMERATED** — Directus wiring: `docker-compose.yml:53` is the **only** `DIRECTUS_*` line in
  `backend`'s `environment:` block at **both** `0dab54d` and `0c91421`; that block runs `:46-94` exactly;
  `.env_example:91-92` are `DIRECTUS_BASE_ADDR` + `DIRECTUS_TOKEN` and nothing else Directus-related.
  `app/main.go:980-982` is the `Getenv`/`if`/`log.Fatalf` triple, exact at `b948604`.
- **ENUMERATED** — both frontends' four GraphQL anchors (`:197`): `docker-compose.yml:151` (build arg)
  and `:160` (runtime env) for next-web-app; `:119` (build arg) and `:135` (runtime env) for studio-desk;
  all four carry `:8082/graphql/query`. `:768`'s `:151`/`:160` split is the same pair, consistent.
- **ENUMERATED** — the AI provider table and routing section, every anchor opened with surrounding
  context: `report_agent.go:31` `defaultAgentModel = "eu.anthropic.claude-sonnet-4-6"`;
  `askengine/bedrock.go:25` `DefaultModelID`; `coursebuilder/bedrock.go:23` Opus-4-8 / `:29` Sonnet-4-6 /
  `:106-113` the `ANTHROPIC_API_KEY`-present→first-party, absent→Bedrock switch; `main.go:770`
  `coursebuilder.ModelBackendName()`; `jobsimulation/ai/ai.go` `:30-33` (the four vendor consts), `:80`
  (`openai.NewOpenAI`), `:130-141` (`isThrottlingError`), `:150-155` and `:296-302`/`:326` (the
  throttled→`Openai` override), `:259-289` (`getClient`), `:264-276` (the `flag_use_azure_us` arm that
  keeps the EU client on lookup error), `:280-283` (**both** `AnthropicAws` and `Anthropic` →
  `a.anthropicClient`); `cms/directus/collections/jobsimulation.go:905` `AIVendor *AIVendor` and
  `:1302-1305` `aiVendor := simulation.Openai`; `simulator/ai/ai.go:58-59` and its `default:` at `:114-115`.
- **ENUMERATED** — *"all 30 `*_AI_*_MODEL` lines pin `azure`"* (`:567`, `:619`, `:640`). Enumerated from
  source: `config_template.ini` 10 (`:37-41`, `:43-47`), `development_config.ini` 10 (`:26-30`, `:32-36`),
  `production_config.ini` 10 (`:26-30`, `:32-36`) = **30**, every one `azure`. `gen.py:41-53` overrides
  only `*_API_KEY`/`*_ENDPOINT` — never the service selector; `config_template.ini:30-31` are
  `OPENAI_API_KEY` / `OPENAI_ENDPOINT=https://api.openai.com/v1/chat/completions`;
  `services/ai.py:383` `return OpenAI(api_key=self.api_key)`, `:627-664` the whole `AnthropicProvider`
  class, `:704-724` `get_client` with `:706-708` the `{openai, azure, anthropic}` dict.
- **ENUMERATED** — the Mistral row (`:565`, `:597-599`). `git grep -in mistral` over `app` `b948604f`
  returns **8** lines, all OCR (`markdownManager.go:11,19,21`, `studioManager.go:531,583,585,589`,
  `xlsx.go:13`); over `app/studio` `aeec036a` it returns **22** lines / 3 files
  (`requirements.txt` 1, `tools/pdf2md.py` 18, `tools/r3.py` 3) — the figure §5 rule 44 publishes.
  `pdf2md.py:24` is `from mistralai import Mistral`; `r3.py:139` the script list, `:190`
  `pdf2md_script = get_script_path("pdf2md.py")`, `:199-206` the `step2_cmd` build. So *"`r3.py` DOES
  dispatch it"* is right and the narrower survivor is right.
- **ENUMERATED** — *"0 hits for `bedrock|boto3` under `app/studio/`"* (`:616`): `git grep -Ecin
  "bedrock|boto3" HEAD` at the **nested repo's own ref** `aeec036a` → rc 1, positive control
  `anthropic` → 13 files.
- **ENUMERATED** — the env-var counts at the pin `5ba17044` (`:668-673`): `OPENAI_KEY` **5**,
  `ANTHROPIC_API_KEY` **26**, `AZURE_OPENAI_KEY` **13**, `AZURE_OPENAI_ENDPOINT_URL` **13**;
  `OPENAI_ORG_ID` / `AZURE_OPENAI_ENDPOINT` / `AZURE_OPENAI_DEPLOYMENT` all **0**; `OPENAI_API_KEY` **2**.
  (`\b…\b` correctly does not let `AZURE_OPENAI_ENDPOINT` match inside `…_URL`.)
- **ENUMERATED** — the taxonomy block (`:687-700`). The manifest at the cited path parses to
  `public_only: true`, `predicate: org-null`, `captured_at 2026-06-29T14:43:00Z`, and row counts
  `skills 42790`, `job_roles 22470`, `skill_embeddings 42790`, `job_role_embeddings 18919` — every
  figure, including the transcription diagnosis for the refuted "18K roles".
- **ENUMERATED** — LiveKit agent names (`:723`): `calls/livekit.go:110` and `:120` bare
  `"anthropos-agent"`, `:115` `"anthropos-agent-chain"`, `:126`
  `fmt.Sprintf("anthropos-agent-%s", *location)`. `anthropos-agent-eu` = **0 across all 12 platform
  trees + the 2 nested studio repos** (positive control `anthropos-agent`: app 6, jobsimulation 8). The
  only non-zero tree is `stack-demo/rosetta-extensions` (7) — Rosetta's own tooling, which the claim's
  wording (*"nowhere in the platform"*) correctly excludes.
- **ENUMERATED** — voice engine (`:727`): `jobsimulation.go:1595-1596` returns
  `SimulationVoiceEngineGptrealtime` on nil, and `flag_use_realtime_openai` (`livekit.go:131-143`) sets
  an *endpoint* (`openai-hosted`), never an engine. ElevenLabs credentials are live in compose (`:54-55`).
- **ENUMERATED** — Studio-Desk copilot row (`:703`): `studio-desk/.env.example:57`
  `AI_PROVIDER_CHAIN=azure-openai,openai`; `src/services/ai/config.ts:21-22,28-29` `gpt-5.2`, `:35-36`
  `claude-opus-4-5-…` / `claude-sonnet-4-5-…`; circuit-breaker present.
- **ENUMERATED** — the three in-file self-anchors at `:614-617` and `:873` all resolve to the constructs
  named: `:543` = *"When backend services add new GraphQL types or operations:"*, `:546` = the struck
  `docker compose restart graphql` correction, `:567` = the **Anthropic Direct** table row.
- **ENUMERATED** — the two terraform survivors (`:175`): `cms/terraform/main.tf:39`
  `service_desired_count = 0` (true at checkout `ca50c817` **and** origin/main `f38c0c4a`); `6efa1d5`
  (2026-08-04) deletes `.github/workflows/build-production.yml` with the quoted subject;
  `6092c6d2` (2026-08-04) really does delete the `module "jobsimulation"` block, and at jobsimulation
  origin/main `82cb66ec` no `module "…"` remains. The asymmetric verdict — cms *UNMEASURABLE*,
  jobsimulation *already destroyed* — is the state I measured.
- **ENUMERATED** — playground routing (`:533`): `app/internal/web/backend/backend.go:315` registers
  `/graphql` → `playground.ApolloSandboxHandler(…, "/graphql/query")` behind a local-dev guard, `:317`
  registers `/graphql/query`. `backend.go:130` is `"/api/webhook/clerk"` in the authn skip list.
- **ENUMERATED** — `:850-853`: `cms`, `jobsimulation`, `storage`, `messenger`, `customerio-sync` are
  none of them compose services at `0c91421`, and `backend`'s `depends_on` at `:101-109` names exactly
  redis / postgresql / sentinel.

### `corpus/services/chronos.md`

- **ENUMERATED** — platform `045857c` is dated **2026-04-17**, subject *"chore: remove chronos service
  from orchestration and update related documentation"*, and touches `CLAUDE.md`, `README.md`,
  `docker-compose.yml` **and** `repos.yml` — both halves of `:5`.
- **ENUMERATED** — jobsimulation `09631fb2` (2026-04-17) carries the quoted subject **verbatim**, is on
  `main`, and was merged by `500b9761` *"Merge pull request **#395** from
  anthropos-work/**feat/remove-chronos-and-realtime**"* — every element of `:7`.
- **ENUMERATED** — `app/internal/jobsimwiring/worker.go:24-35` spans the `hibiken/asynq` import (`:24`)
  through `StartWorkers` and its first `asynq.NewServer` (`:35`), supporting `:9`'s "those Asynq workers
  now run in the `backend` process". No `chronos` entry survives in `repos.yml`.
- Note: nothing in this file was found wrong. The bulk of it (schema DDL, triggerer behaviour, ports,
  CLI flags) is listed under *What I could not settle* — no chronos clone exists in the ground-truth set.

### `corpus/services/messenger.md`

- **ENUMERATED** — the handler count at `:17`. `internal/flow/flow.go:72` opens
  `AddSubscriber("backend", …)` and `:104` closes it; inside that exact range there are **22** lines
  matching `pubsub.EventHandler(` (`:74-78`, `:80-84`, `:86`, `:89`, `:91-92`, `:94`, `:96-99`,
  `:101-103`), of which **`:83` is commented out** and is precisely
  `OrgJobSimulationAssignmentPastDueHandler` → **21 live**. Exactly as published, including the named
  commented-out handler. `:108`'s `flow.go:74-78` is the five `OrgSkillPath*` handlers.
- **ENUMERATED** — `MESSENGER_RPC_ADDR` = **0 across all 15 git trees at their own refs**
  (the 12 platform clones + the pinned rext clone + both nested `studio` repos). The only `MESSENGER_RPC`
  hits anywhere are 2 prose lines in `app/knowledge/plan/roadmap-support-in-app.md`, neither the
  `_ADDR` spelling. Positive control `SKILLER_RPC` in the same pass: app 22, cms 12, jobsimulation 9,
  messenger 4, platform 1, rext 17.
- **ENUMERATED** — the `-S` controls at `:23-27`: platform history at `0c91421d` is **121** commits;
  `log -S MESSENGER_RPC` → **0**; `log -S SKILLER_RPC` → **7**; with `--all` → **8 and 0**, and the 8th
  is `464dfe3`, which is on `origin/feat/cms-in-app` only. Every number reproduces.
- **ENUMERATED** — *"no compose file sets any `*_RPC_ADDR` at all"* (`:30`, `:108`):
  `grep -n _RPC_ADDR docker-compose.yml common.yml .env_example` → rc 1, positive control
  `AUTHORIZATION_ADDRESS=http://sentinel:8087` at `docker-compose.yml:48`. At `0dab54d` the deleted
  block set exactly four, all `http://backend:8083` (`:173`, `:174`, `:176`, `:183`).
- **ENUMERATED** — `cmd/root.go`: `:63` `PORT`→`8080`, `:64` `RPC_PORT`→`8081`, `:107`
  `REDIS_STREAMS_INDEX`→`2`, `:147` `READONLY_DB_CONNECTION`, `:118-142` the client block.
  `REDIS_WORKER_INDEX` is read **nowhere** in the messenger repo (rc 1, control above) — `:166` correct.
- **ENUMERATED** — `internal/rpcsrv/rpcsrv.go:25-30` is exactly the two `CodeUnimplemented` stubs
  (`CancelScheduledMessage`, `Schedule`), with `Send` starting at `:31`.
  `internal/flow/jobsimulations.go:140-151` is exactly the 2 h / 12 h staleness pair.
  `internal/flow/assignments.go:828` is the `h.cms.GetSkillPath(...)` call inside `getSkillPath` (`:827`).
- **ENUMERATED** — the app-side fold anchors at `:43`, `:104`, `:149`, `:168`, each read at the ref its
  own sentence names. At `app` **origin/main** `2035f9a4`: `main.go:15` `msgflow`, `:62` `msgadapters`,
  `:63` `msgsender`; `:1450` `messengerSubServer = pubsub.NewSubscriberServer(`; `:1471`
  `msgadapters.Wire(`; `:1473` `Sender: msgsender.NewFromEnv(logger)`; `:1416-1421` the *"group name is
  messenger's, and it is a LITERAL on purpose … `cmp.Or(os.Getenv("SERVICE_NAME"), "messenger")`"*
  comment; `env_guards.go:61` `envMessengerEnabled = "MESSENGER_ENABLED"`; `:295` the
  `BREVO_KEY`-required `log.Fatalf`. At **`9d00a313` v1.367.0**: `main.go:1095` the `CMS_STREAM`
  publisher, `:1478-1484` the `streamNamesForSubscribers` binding. At **`b948604` v1.366.0**:
  `main.go:1205-1211` the *"additive + DORMANT … until the M809 re-point"* comment — correctly flagged
  by the corpus as now stale **in `app`**.
- **ENUMERATED** — compose facts: ports `8200:8200`/`8201:8201`, `REDIS_STREAMS_INDEX=4`,
  `REDIS_WORKER_INDEX=0`, `profiles: [messenger]` with the "two consumers on one Redis group" comment,
  `depends_on: backend` (so the profile alone would exit 1), `repos.yml:21-23` — all exact at `0dab54d`.
  `docker-compose.yml:84-92` at `0c91421` is precisely the `MESSENGER_ENABLED`/`CUSTOMERIO_SYNC_ENABLED`
  "deliberately NO variables here" comment `:133` cites.
- **ENUMERATED** — `838d907` (2026-08-05) is the direct parent of merge `0c91421` (2026-08-05) and
  carries the quoted subject; `go.mod` pins `getbrevo/brevo-go v1.1.3`, `osteele/liquid v1.8.1`,
  `go 1.25.0`; `internal/messenger/console/` exists.

### `corpus/architecture/frontend_architecture.md`

- **ENUMERATED** — `repos.yml` holds exactly **4** entries at `0c91421` (`app`, `sentinel`,
  `next-web-app`, `studio-desk`) and exactly **9** at `2adcf71`; ant-academy is in neither.
  `docker-compose.yml:141` is `profiles: [studio-desk, all]` at `0c91421`, and the same key was at
  **`:226`** at `0dab54d` — both halves of `:11`.
- **ENUMERATED** — the apps table: `apps/web` `@anthropos/web-app` port **3000**, `apps/hiring`
  `@anthropos/hiring-app` **3001**, `apps/integration` `@anthropos/integration` **3002** (each from its
  own `"dev": "next dev --port …"`), `apps/mobile` `expo start --port 3031`, `apps/maintenance` no dev
  port. `pnpm-workspace.yaml:11` `- '!apps/mobile'`.
- **ENUMERATED** — `configs/i18n/messages` holds exactly the **8** locales named, in order:
  `de en es fr it ja nl pt`.
- **ENUMERATED** — toolchain row: `apps/web/package.json:46` `"next": "^16.2.7"` and the identical pin in
  hiring/integration/maintenance; root `"turbo": "^2.9.6"`, `"typescript": "^5.9.3"`,
  `"packageManager": "pnpm@10.30.3"`, `"engines": { "node": ">=24.0.0" }`;
  `check` / `check:lint` / `check:types` / `check:deprecations` all present; `clean:quick` drops exactly
  `node_modules`, `.next`, `.turbo`, `dist`; `UPGRADE-IMPACT-next16.md` exists at repo root.
- **ENUMERATED** — codegen (`:44`): `packages/graphql/codegen.ts:8`
  `schema: 'http://localhost:8082/graphql/query'` (with `// was 'http://localhost:5050/graphql'`),
  `:10` `documents: ['src/query/**/*.{ts,tsx}']`, `:12` output `./src/__generated__/`,
  `preset: 'client'` with `plugins: []` — consistent with "React Query hooks are hand-authored".
  The prod side is `config.prod.yaml:5` + `:13` → `:8080/graphql`.
- **ENUMERATED** — `docker-compose.yml:161` (runtime env) and `:152` (build arg) are exactly
  `NEXT_PUBLIC_BACKEND_API_URL`, and both resolve to `:8082`.
- **RE-DERIVED only** — *"300+ commits / month"* (`:99`): May 247, Jun 340, Jul 441 (2026). Soft claim,
  supported by the recent months; not booked.

### `corpus/services/gotenberg.md`

- **ENUMERATED** — *"Call sites (two)"*: `git grep ConvertToPDF -- '*.go'` at **both** `9d00a313` and the
  checkout returns the definition (`internal/converter/gotenberg.go:15-16`) plus exactly two callers,
  `internal/web/backend/coursebuilder/extract.go:77` and
  `internal/worker/tasks/user_import_resume_2d.go:68`.
- **ENUMERATED** — *"nine MIME types"* (`:47`): `extract.go:17-27` is `gotenbergMimes` with exactly **9**
  entries — `.xls`, `.xlsx`, `.ppt`, `.doc`, the three OpenDocument types, and RTF under **both**
  `application/rtf` and `text/rtf`. DOCX
  (`…wordprocessingml.document`) is genuinely absent, and `:41`'s own comment routes it to `docconv` —
  every clause of the sentence holds.
- **ENUMERATED** — the discard claim (`:7`): `extract.go:77-81` converts then immediately
  `converter.ConvertFromReader(bytes.NewReader(pdf), "application/pdf")`;
  `user_import_resume_2d.go:66-74` sets `ocrInput = pdfBytes` and does nothing else with it, DOCX-only,
  behind `!converter.HasReadableText(body)`. Neither stores or serves the PDF.
- **ENUMERATED** — graceful degradation (`:48`): `extract.go:59` gates on `gotenbergURL != ""`,
  `:43-45` is the "A blank gotenbergURL degrades gracefully" comment,
  `user_import_resume_2d.go:69-74` logs the failure and OCRs the original bytes.
- **ENUMERATED** — `converter/gotenberg.go:13` `&http.Client{Timeout: 90 * time.Second}`;
  `:16` the exact `ConvertToPDF(ctx, gotenbergURL, document, filename) ([]byte, error)` signature;
  `:31` `gotenbergURL+"/forms/libreoffice/convert"`.
- **ENUMERATED** — compose: the quoted block matches `docker-compose.yml:170-183` byte-for-byte
  (`gotenberg/gotenberg:8`, `--api-port=3200`, `--api-timeout=60s`, `--libreoffice-restart-after=50`,
  `"3200:3200"`), `profiles: [core, backend, all]` at **`:183`** at `0c91421` and at **`:268`** at
  `0dab54d`, and the file is **186** lines — every element of the re-anchoring note at `:14`.
  `GOTENBERG_URL=http://gotenberg:3200` is set in `backend`'s `environment:` at `:57`.
- **ENUMERATED** — *"the only consumer"* (`:42`): `gotenberg` per-tree at each tree's own ref is non-zero
  **only** in `app` (48) and `platform` (12, the compose block); 0 in cms, jobsimulation, messenger,
  storage, sentinel, roadrunner, next-web-app, studio-desk, ant-academy, graphql-wundergraph and both
  nested `studio` repos.

---

## What I could not settle

- **`external_services.md:3`** — *"the repo is ARCHIVED on GitHub (2026-07-30)"*. Archive status is a
  GitHub-API property, not a property of any clone. **Settled by:** `gh repo view
  anthropos-work/graphql-wundergraph --json isArchived,archivedAt`.
- **`external_services.md:175`** — the doc's own verdict that the cms prod-side state is *UNMEASURABLE*
  because the ECS deletion would land in `infrastructure`, which is in no clone set. I confirmed both
  halves of the contradiction it describes and could not go further. **Settled by:** a clone of
  `anthropos-work/infrastructure`, or `aws ecs describe-services` against the prod cluster.
- **`chronos.md:26-27`, `:40-64`, `:154-160`, `:194-210`** — the DB schema DDL, the `search_path=chronos`
  claim, ports 8080/8081, the triggerer's `LIMIT 10` / `FOR UPDATE SKIP LOCKED` / 3-second-timeout /
  Sentry-heartbeat behaviours, the env-var table and the CLI flags. **No `chronos` clone exists in the
  ground-truth set** and the repo is (correctly, per `repos.yml`) not cloned by `make init`, so none of
  this is checkable from here. Everything in the file that *is* checkable — the two decommission commits,
  the PR number and branch, the Asynq successor anchor, the absence from `repos.yml` — verified clean.
  **Settled by:** `git clone git@github.com:anthropos-work/chronos` and reading
  `internal/database/schema.sql`, `internal/triggerer/triggerer.go`, `cmd/root.go`.
- **`chronos.md:3`** — *"the GitHub repo is NOT archived"*. Same instrument as the first item.
- **`external_services.md:279-283`, `:293-297`** — the Directus collection list and the production
  S3/CDN file-storage description. Both describe the prod Directus instance, which no clone contains.
  **Settled by:** an authenticated read of `content.anthropos.work`'s `/collections`, or the prod
  Directus deployment config.
- **`messenger.md:86-88`** — *"Recent work in v0.34.0 added whitelabel support"*. The whitelabel code and
  `READONLY_DB_CONNECTION` both verified present; I did not bisect which tag introduced them.
  **Settled by:** `git -C stack-demo/messenger log --oneline v0.33.x..v0.34.0 -- internal/flow/whitelabel.go`.
- **`gotenberg.md:64-65`** — the `/health` response shape (`{"status":"up"…}`). Upstream-image
  behaviour; no running stack was available and this reading is read-only. **Settled by:**
  `curl -s localhost:3200/health` against a live `core` stack.
