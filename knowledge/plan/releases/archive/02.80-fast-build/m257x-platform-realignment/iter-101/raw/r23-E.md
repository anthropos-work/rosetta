# Seat E — reading #23

**Refs at this reading (re-verified at open, `git rev-parse`):**
app `b948604f` · **platform `0c91421d`** (the ADDENDUM's superseding sha; the frozen table's `0dab54df` is stale) ·
next-web-app `bb3313bc` · sentinel `88bc5592` · storage `4ce8ece5` · messenger `fa47850d` · cms `ca50c817` ·
graphql-wundergraph `60c229f3` · roadrunner `87d8d443` · jobsimulation `462343b0` · studio-desk `14a5442a` ·
ant-academy `9c3843cd` · `stack-demo/app/studio` **`aeec036a`** (own checkout) ·
rext **pinned per-stack** `stack-demo/rosetta-extensions` `ab81527a` (tag `fast-build-m257x-iter-58`) and
authoring `.agentspace/rosetta-extensions` `09d06070`.

Per §5 rule 45 I graded every rext claim against the **pinned per-stack clone** and re-checked the
authoring copy as a control — on every anchor I opened, the two trees were byte-identical, so no
finding turns on which tree adjudicates.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/architecture/service_taxonomy.md` | 496 | yes |
| `corpus/services/clerkenstein.md` | 370 | yes |
| `corpus/architecture/security_compliance.md` | 281 | yes |
| `corpus/services/ai-labs.md` | 156 | yes |
| `corpus/services/clerk-integration.md` | 128 | yes |
| `corpus/services/intelligence.md` | 18 | yes |
| **total** | **1449** | |

Search-pipeline controls run in the same pass: `grep -n RPC_ADDR docker-compose.yml` → **rc=1 / 0 hits**
with the paired positive control `grep -n AUTHORIZATION_ADDRESS` → **1 hit at `:48`** (so the zero is a
measurement, not a broken pipe). `git grep '\.Cookie(' HEAD -- clerkenstein/` → **0**, control
`git grep -c http.Request` → **hits in 5+ files**. `git grep organization_memberships clerk-js-5.json`
→ **0**, control: the same file's 6 capabilities enumerate. Trees named per §5 rule 44: the
`mistral`/`gpt-5.2` predicates were measured at **`stack-demo/app/studio` @ `aeec036a`** and with a bare
`grep` for the gitignored `code/ucourses/` tree, because `git grep` at the host ref returns a
guaranteed 0 there.

---

## Blockers

### B1 — the archive-state caveat's own row anchors are off by two, and point at rows that contain no archive assertion

- **Anchor:** `corpus/architecture/service_taxonomy.md:132-133` —
  *"This note exists because rows `service_taxonomy.md:137`/`:138` published the flat form two rows
  above `:139`, a cell retracting exactly that predicate."*
- **Why it is false:** measured in the file itself (`grep -n ARCHIVED corpus/architecture/service_taxonomy.md`):
  - `:137` = **Chronos** — `| **Chronos** | Removed from local dev orchestration | **no** | Platform commit `045857c` |`. Contains **no** `ARCHIVED` date at all.
  - `:138` = **Intelligence** — likewise, **no** `ARCHIVED` date.
  - `:139` = **Skiller** — *"repo legacy/decommissioned, **ARCHIVED 2026-07-01**"*. This **is** the flat form the note says lives at `:137`/`:138`.
  - `:140` = **Skillpath** — *"repo legacy, **ARCHIVED 2026-07-31**"*. The second flat-form row.
  - `:141` = **Jobsimulation** — *"repo archive state: report both, assert neither"*. **This** is the retracting cell the note attributes to `:139`.

  Only two lines in the whole file carry an `ARCHIVED <date>` in that table (`:139`, `:140`), and the
  note names neither. Every anchor in the sentence is shifted by exactly **+2**, so the caveat
  simultaneously (a) accuses two rows that make no archive claim and (b) mislabels a row that
  *publishes* the flat form as the row that *retracts* it — the exact inversion the note exists to
  prevent. The immediately following parenthetical shows the passage was edited at M257x iter-100 to
  qualify *which file* `:137/:138` meant, without re-deriving the line numbers — §5 rule 34 (*line
  numbers move when YOU edit too*) in its pure form.
- **Confidence:** high

### B2 — "only 30 use `OrganizationMixin{}`" is the commented-out-line miscount the same blockquote exists to warn about, and it contradicts the file three times

- **Anchor:** `corpus/architecture/security_compliance.md:67-68` — *"Measured at `app` HEAD: of **135**
  Ent schemas (139 `.go` files, 4 of which declare no schema), only **30** use `OrganizationMixin{}` —
  the one that carries the privacy `Policy()` (`mixin.go:126`)."*
- **Why it is false:** re-derived the SET (not the sum) at `app` `b948604f`, in
  `internal/data/ent/schema/`:
  - `grep -l 'OrganizationMixin{}' *.go | wc -l` → **30 files**;
  - one of the 30, `user_resource.go:22`, is `// OrganizationMixin{},  // We need to work on this` — a
    **comment**, opened and read in place. So the schemas that *use* the mixin number **29**, not 30.

  The predicate in the sentence is *"use"*, and 29 use it. The same blockquote then says the opposite
  twice — `:76` (*"**29** `OrganizationMixin{}` users"*) and `:84-85` (*"one of them, `user_resource.go:22`,
  is **commented out**…, so the live count is **29**"*) — as does `:135` (*"the **29** live
  `OrganizationMixin{}` users"*). `:76` opens with *"**So:** 31 schemas auto-filter…"*, an inference
  that does not follow from the 30 stated nine lines above it. Everything else in the fence
  re-derived exactly (see *Positively cleared*), which is what isolates this line.
- **Why I hesitated (stated per the grading rule):** the passage does own the correction 17 lines
  down, so one could read `:67`'s 30 as a deliberate "naive count, then the fix". I booked it because
  (a) `:67` is the sentence a reader takes as *the measurement* — it is prefixed *"Measured at `app`
  HEAD"* and it is the ⚠️ headline of a fence whose stated history is being **wrong four times**;
  (b) the error direction is *toward "isolation is handled"*, the direction the paragraph at `:114-119`
  names as the recurring one; and (c) `:76`'s "So:" is arithmetically unreachable from `:67`.
- **Confidence:** medium

### B3 — `README.md:21` names an unrelated sentence in every file that citation can resolve to

- **Anchor:** `corpus/architecture/security_compliance.md:185` — *"**AI Token Tracking** | Centralized
  usage, latency, and cost tracking in **`app/internal/aiusage`** — **not** the shared `ai` library,
  which only wraps providers (consistent with `README.md:21` + `ai_architecture.md`)"*
- **Why it is false:** the citation is bare, so I resolved it at every `README.md` it could mean:
  - repo-root `README.md:21` = *"Project Rosetta is a **documentation repository** - not the Anthropos platform itself. It contains:"* — nothing about AI, cost tracking, or `aiusage`.
  - `corpus/README.md:21` = `### [Architecture](./architecture/)`.
  - `corpus/services/README.md:21` = a `roadrunner/terraform/main.tf:19` sentence.
  - `corpus/architecture/README.md:21` = the `shared_libraries.md` index row.

  `grep -rn aiusage --include=README.md .` returns **exactly one** line in the whole tree:
  `corpus/architecture/README.md:**23**` (*"…and cost tracking (`app/internal/aiusage`)"*). So the
  claim's corroborating anchor is off by two lines **and** unqualified as to file. The substantive
  claim is true (`app/internal/aiusage/ai_usage.go` exists at `b948604f`), which is why I hesitated —
  but a citation offered as the proof of "consistent with" that lands on the repo's tagline is the
  wrong-construct class §5 rule 46 records as upheld, and a reader following it learns nothing and
  cannot tell whether the consistency was ever checked.
- **Confidence:** medium

### B4 — the "always-on floor" is stated as three services in three places and enumerated as two in the one place that enumerates it

- **Anchor:** `corpus/architecture/service_taxonomy.md:109-111` —
  *"**Base services (no profile, always on with any `make up`)**: - **PostgreSQL** :5432 … - **Redis**
  :6379 …"* — exactly two bullets.
- **Why it is false:** the predicate in that heading — *no profile, always on with any `make up`* — is
  satisfied by **three** services at platform `0c91421`, and the same file says so three times:
  - `:68` — *"`core` starts five: `backend`, `gotenberg` and **the three always-on base services**"*;
  - `:465` — *"postgresql, redis, sentinel only — **the floor**, the three services that declare no `profiles:` key"*;
  - `:489` — *"**the three always-on base services** (`postgresql`, `redis`, `sentinel`)"*.

  Verified against source: `platform/docker-compose.yml` @ `0c91421` declares `sentinel` (`:5`) with
  **no `profiles:` key** — the only one of its five services without one (`backend :110`,
  `studio-desk :141`, `next-web-app :168`, `gotenberg :183` all have one) — and `common.yml` adds
  `postgresql`/`redis`, also profile-less. So the floor is `{postgresql, redis, sentinel}`.

  A reader who takes `:109`'s list literally computes `core` = backend + gotenberg + 2 = **four**
  containers against the file's own repeated **five**, and — the load-bearing consequence — mis-sizes
  the "a retired profile token exits 0 and starts the floor" mechanism at `:474-479`, which is exactly
  the failure §5 rule 30 was written for (*"three presents as a partially-working stack"*, and the
  corpus previously recorded it as "zero").
- **Why I hesitated:** this is an **omitted list member**, and it is defensible to read "Base
  services" as *base infrastructure* (postgres/redis) with sentinel documented one table above at `:80`
  as *"(always on — declares no `profiles:` key)"*. I booked it because the file itself uses "base
  services" for all three at `:489`, so the two readings are the file disagreeing with itself about
  its own term, and because the arithmetic at `:68` does not close under the enumerated form.
- **Confidence:** low

---

## Minors

- `corpus/services/clerkenstein.md:3` — *"**Last updated:** 2026-07-14"* and the `Status:` enumeration
  stopping at *"v2.3 'cue to cue' M217 … + M218"*, while the body documents **M219** (`:54-62`),
  **M220** (`:305-313`), **M224 / v2.4 "casting call"** (`:232-255`), **v2.8 M256** (`:160`, `:184`) and
  **v2.8 M257x iter-23** (`:274`). Stale metadata; no factual consequence in the body.
- `corpus/services/clerkenstein.md:279-280` — **markdown damage**: line `:280` (*"That drop-in is
  **identity-agnostic** …"*) sits at column 0 with **no blank line** after the `>` blockquote that ends
  at `:279`, so CommonMark lazy-continuation absorbs the sentence into the ⚠️ drift warning. Same
  splice-a-blockquote-into-running-prose shape §5 rule 20 records; here it only changes attribution.
- `corpus/services/clerkenstein.md:21` — the rext section list *"(`clerkenstein`, `demo-stack`,
  `stack-injection`, `stack-core`, `stack-seeding`, `alignment`)"* names **6**; the pinned clone has
  **11** (`+ dev-stack, playthroughs, stack-core, stack-secrets, stack-snapshot, stack-verify`), and root
  `CLAUDE.md` names **9**. No exhaustivity is claimed, so non-material.
- `corpus/services/clerkenstein.md:105` — the `cmd/` row lists `mintpk` / `fake-fapi` / `fake-bapi`;
  the dir holds a fourth binary, `jwtkey`.
- `corpus/architecture/service_taxonomy.md:37` — the mermaid edge label
  `Academy -->|GraphQL - academy subgraph| Backend` says "academy subgraph" while `:389` measures the
  supergraph at *"**`backend` alone (1)**"*. `academy.graphqls` is a schema **inside** the `backend`
  subgraph. Corpus-wide phrasing convention (also in `CLAUDE.md`), so booked low.
- `corpus/architecture/security_compliance.md:73` — *"whose `Policy()` (`mixin.go:98`)"*: `:98` is the
  doc comment `// Policy defines the privacy policy of the UserMixin.`; the func is `:99`. (Its sibling
  `mixin.go:126` **is** the func, exactly.)
- `corpus/architecture/service_taxonomy.md:268` — of the three anchors offered for *"the course catalog
  is DB-authoritative"*, `backendContent.js:36` is `import { createServerGraphQLClient } …`. The
  load-bearing evidence is the file's docblock at `:29-32` and the reads at `:102-103` (both correct),
  plus `serverTenant.js:145` (correct, and decisive).
- `corpus/architecture/security_compliance.md:196-197` — bare `ai.go:262-266` / `:85-88` with no repo
  path; the file is only named 15 lines later (`:211`). Both resolve correctly in
  `app/internal/jobsimulation/ai/ai.go`.
- `corpus/services/intelligence.md:5` — *"removed … in mid-2026"*; `fdfa189` is dated **2026-04-17**.
  Both files it names (`docker-compose.yml`, `repos.yml`) were touched by that commit, as claimed.

---

## Positively cleared (audited zeros)

**ENUMERATED** — predicate re-derived from source independently, cardinality compared:

1. **Compose topology @ platform `0c91421`** (`service_taxonomy.md:65-68, :104-111, :462-479`).
   `docker-compose.yml` = **5** declared services (sentinel, backend, studio-desk, next-web-app,
   gotenberg); `include: common.yml` adds postgresql + redis → **7** effective; **4** services carry a
   `profiles:` key; the legal token set is exactly `{all, backend, core, frontend, studio-desk}` = **5**;
   `Makefile:10` `PROFILE ?= core`. No cms / jobsimulation / roadrunner / storage / messenger /
   customerio-sync / graphql service of any kind. Zero `*_RPC_ADDR` variables; one
   `AUTHORIZATION_ADDRESS=http://sentinel:8087` at `:48`.
2. **The `838d907` deletion narrative** (`:93-102`). At `0dab54d` storage published **8300/8301**,
   messenger **8200/8201**, customerio-sync **8080**, and customerio-sync's profile list **was**
   `[customerio-sync, all]` — so `make up-all` did start it. `838d907` deleted all three services and
   dropped `storage` + `messenger` from `repos.yml` (diff read); `customerio-sync` was never in it.
   `docker-compose.yml:84-92` is exactly the MESSENGER_ENABLED / CUSTOMERIO_SYNC_ENABLED comment block.
3. **The pre-`d11a403` state** (`:115-122`). At `d11a403~1`, `repos.yml` listed 9 repos including cms,
   jobsimulation, roadrunner; all three carried `profiles: [graphql, …]`, i.e. the then-default —
   so a bare `make up` did start them.
4. **The subgraph ladder** (`:389`). `supergraph-config-prod.yaml` at four refs: `749dc86~1` = **5**
   (backend, skiller, jobsimulation, cms, skillpath) → `749dc86` (2026-06-24) = **4** → `7c17e63`
   (2026-07-21) = **3** → `915da06` (2026-07-29) = **1**. `git show --name-status 915da06` deletes
   **both** `schemas/cms.graphqls` and `schemas/jobsimulation.graphqls`. The corpus's **3 → 1** (not the
   commit subject's "2→1") is the correct reading.
5. **Router prod facts** (`:3`, `:386`). `graphql-wundergraph/terraform/main.tf:20` = `service_desired_count = 1`;
   `terraform/locals.tf:8` = `port = 8080`; `main.tf:48-49` = containerPort/hostPort `${local.port}`;
   `config.prod.yaml:5` = `listen_addr: 0.0.0.0:8080`. And `2adcf71~1:docker-compose.yml:14` = `"5050:8080"`
   — a **host mapping only**, so *"5050 was never a production port"* holds.
6. **The Directus retraction-of-a-retraction** (`:344-353`). `a2a3ee6~1:docker-compose.yml` `:384`
   `image: directus/directus:10.10.1`, `:386` `8055:8055`, `:409` `ADMIN_PASSWORD=password`, all exact;
   `git log -S admin@example.com -- docker-compose.yml` → **0** commits, matching *"only the email is
   unfound"*. `a2a3ee6` is dated 2026-02-27.
7. **`authn` imported by nothing** (`:491`, `clerk-integration.md:51,91`). All **7** Go clones enumerated:
   `anthropos-work/authn` in `go.mod` → **0/7**; control `colony` → **7/7**. `colony/authn` **source**
   imports: app **129** files, sentinel **0**, storage **0**, messenger **0** (cms 6 / jobsimulation 8,
   both frozen legacy) — matching the doc's own 129-vs-0 figure exactly.
8. **The multi-tenancy fence** (`security_compliance.md:66-137`), re-derived at `b948604f` as a SET:
   **139** `.go` files; **135** declare `ent.Schema`; the 4 that do not are `database_types.go`,
   `mixin.go`, `skiller_mixins.go`, `skillpath_mixins.go`. `OrganizationIDMixin{}` = **7**, and they are
   *exactly* the named seven (category, jobrole, similarity, skill, specialization, studio_document,
   studio_task). The doc's own `comm`/`xargs` derivation returns **18**; the bare `*.go` variant returns
   **19** (as warned). Removing `org_membership.go` (own `Policy()`) and `academy_feedback.go`
   (`UserMixin{}` at `:64`) leaves **16**, set-identical to the 16 the doc names. `16 + 7 = 23`. Files
   declaring **any** `Policy()` = exactly **4**: organization.go, mixin.go, user.go, org_membership.go.
   Line anchors all exact: `mixin.go:126` (OrganizationMixin Policy), `organization.go:56` + `:96`
   (`rule.FilterSameOrganizations()`), `org_membership.go:172-188` ending in `privacy.AlwaysDenyRule()`
   with `rule.AllowCurrentOrgEdgesOrSkipRule()` at `:179`, `user.go:116`, `user_resource.go:22`.
   `job_simulation_session.go:5` quotes verbatim. **31 = 29 + Membership + Organization is the right
   total, reached by the right derivation** — the only defect is `:67`'s "30" (B2).
9. **EU-AI-Act retraction** (`security_compliance.md:231-245`). `skills.go:53-62` counts booleans, `:75`
   divides. `validator.go:60-62` registers **exactly one** engine (`check.EngineLlm`), and
   `checkerEngines` is **stored and never read** (6 occurrences, all declaration/assignment/pass-through).
   The live dispatch is `criterion.go:126-155` → `validateLLM` → `NewLLMBulkChecker` (`:428`), sending
   `checkValidationBulk.tmpl` (*"assess whether the `<asset>` … meets or does not meet"*, returning
   `{"check_id","feedback","success"}` at `:92-105`). `EngineTextDiff` → `validateCodeDiff` (`:168`),
   which at `:450-475` sets `success` from `if cdiff.Diff == ""` — no model. Both sets appended. The
   *"most, not all"* framing is exactly right.
10. **AI residency** (`security_compliance.md:196-221`). `internal/jobsimulation/ai/ai.go:85-88`
    (`config.WithRegion("eu-west-1")`), `:263-277` (`azureClientEu` → `azureClientUs` on
    `flag_use_azure_us`, EU kept on lookup error), `isThrottlingError` at `:129`/`:166`/`:325`.
    `coursebuilder/bedrock.go:100` returns `"anthropic-api"`; `:109-112` is `newUnderlyingClient` →
    `NewAnthropicClientWithModel`. All three `app/studio/configs/*.ini` pin **azure** for every one of
    the 10 model roles — so the "fifth, latent arm" claim holds. `external_services.md:567`, `:579`,
    `:602-607` and `coursebuilder.md:48` (*"so the shipped path is the direct API"*) all resolve.
11. **Org switching is a re-mint, not a re-auth** (`security_compliance.md:147`). All six anchors exact,
    in **both** apps: `apps/{web,hiring}/src/hooks/useOrgSelection.tsx:94`, `useResolveActiveOrg.tsx:107`,
    `useActivateMembershipOrg.tsx:81` — every one a `clerk.setActive({ organization })`.
12. **Clerkenstein alignment DNA** (`clerkenstein.md:41-45, :71-79, :106`). Parsed all five DNA JSONs:
    `clerk-2.6.0` **14 capabilities / 27 genes**, `clerk-js-5` **9**, `clerk-multi-1` **9**,
    `clerk-deploy-1` **7**, `clerk-express-1` **13 genes / 5 capabilities** — every published figure.
    `alignment/cmd/` = exactly `{clerkrun, deployrun, expressrun, jsfapirun, multirun}`; `golden{,-js,
    -multi,-express,-deploy}/` all present; `dna/` = 5. `alignctl/run.go:134-135` = `ExitRegressed = 2` /
    `ExitUnmeasurable = 3`. `clerk-2.6.0.json:131` contains the quoted *"M219 landed the fix … taking the
    Go surface 97.2% -> 100%"*. `clerk-backend/store.go:138` = `SeedOrgIdentity`, `:151` = `LookupOrgEid`.
13. **The single-seat disclosure** (`clerkenstein.md:160-209`) — the one I most expected to be
    overstated, and it is exact. `registry.go` holds one `activeKey` (`active()` `:67`, `Select()` `:75`);
    `server.go:99-110` `type Server` holds one `signedIn`/`signedOut`/`clientID`/`sessID`; `:125`
    `clientID: "client_clerkenstein"`; `establishLocked` (`:663`) mints `sessID = "sess_clerkenstein"` at
    `:665`. `handleClient` (`:241`), `handleToken` (`:467`), `handleMe` (`:488`), `handleSignOut` (`:554`)
    all take `_ *http.Request`. `git grep '\.Cookie('` over `clerkenstein/` = **0** with a live positive
    control. All five named `server_test.go` tests exist (`:256, :286, :390, :427, :461`); the
    `POST /v1/client/sessions` + `_method` route is registered at `:194`; `meorgmemberships_test.go` exists
    and `GET /v1/me/organization_memberships` is registered at `:186`. `clerk-js-5.json` has a `Me`
    capability (`universal-user`, `unauthenticated`) and **0** occurrences of `organization_memberships`
    — the declared DNA gap is real.
14. **M220 clerk-js bounding** (`clerkenstein.md:305-313`). `clerk-frontend/server.go:35-67` is precisely
    the block: `clerkJSFetchTimeout = 15 * time.Second` (`:59`), `clerkJSClient` with the literal comment
    *"Explicitly NOT http.DefaultClient"* (`:65-68`), `FAKE_FAPI_CLERKJS_CACHE`, and the no-`http.Get(`
    assertion.
15. **Clerk feature surface** (`clerk-integration.md:37, :41, :99, :103`). Clerk webhook event types in
    `app/internal/clerk/events/events.go` = **exactly 12** (3 user.\* + 3 organization.\* + 3
    organizationInvitation.\* + 3 organizationMembership.\*). `@clerk/localizations` imports in
    `apps/web/src/providers/Clerk.provider.tsx:10-18` = **8** locales (deDE enUS esES frFR itIT jaJP nlNL ptPT).
    `@clerk/nextjs ^6.39.2` in all four surfaces (web/hiring/integration + `ant-academy/code/package.json:52`);
    `@clerk/clerk-expo` `~2.6.18` (`apps/mobile/package.json:6`) vs `~2.19.36`
    (`ant-academy/mobile/package.json:18`) — **13 minor versions**, exact. `app/go.mod:31` = `clerk-sdk-go/v2 v2.7.0`.
    `STUDIO_ACCESS_ROLES` at `studio-desk/src/index.ts:96` and `app/services/userService.ts:16`, both
    `['admin','org:admin','content_creator','org:content_creator']`.
16. **Studio-Room** (`service_taxonomy.md:220, :244-250`). `services/ai.py:705-708` is the whole provider
    registry (`openai`/`azure`/`anthropic`); `ai.py:1-2` imports only openai + anthropic;
    `tools/pdf2md.py:24` = `from mistralai import Mistral`; `git -C app/studio grep -ci mistral aeec036a`
    = **22 lines / 3 files**, matching the published figure. `gen.py` registers **exactly nine**
    `add_argument` calls, the nine named, and uses `parse_known_args` at `:19` — so *"no `--template`
    flag, silently swallowed"* holds. Python **3.11** confirmed at `app/Dockerfile:28`;
    `additional_repo: "anthropos-studio-room:studio"` at `app/.github/workflows/build-production.yml:29`.
17. **Ant Academy** (`service_taxonomy.md:261-274`). Next `^16.2.6`, React `^19.2.5`, `next dev --port 3077`,
    expo `~54.0.33`, `--port 8555`. `graphql/server.js:14` reads the endpoint and `:18` **throws** when
    unset. `serverTenant.js:145` = `getBackendCatalogView(eids) ?? emptyCatalogView()`, with the quoted
    *"NO FS-as-published fallback"* / *"not reversible-on-error"* inside `:115-145`. Zero `serwist`/
    `workbox` in `code/package.json`; `next-scaffold.test.js:106` + `:111` and
    `react-compiler-config.test.js:41` are the three regression fences named; manifest `display: standalone`
    + `layout.jsx:132`. Cosmo: `NEXT_PUBLIC_FEATURE_TRAINING_COACH` (`featureFlags.js:8`, default off) and
    `code/ucourses/ucourse-engine/assistant/agent.js:13` `const MODEL = 'gpt-5.2'` — found only by **bare**
    grep (that tree is gitignored, so `git grep` returns a false zero; §5 rule 44 mechanism 1).
18. **Jobsimulation M810 / CMS rollback rows** (`service_taxonomy.md:141-143`). Graded at the ref each
    cell names: at **`6092c6d2`**, `jobsimulation/terraform/main.tf` is **56 lines**, the `module
    "jobsimulation"` block is gone, and `:15-40` is exactly the comment enumerating the LiveKit bucket,
    the Chime bucket, the SSM parameters and the atlas tracker. `origin/main` carries **4** commits dated
    2026-08-04 including merged PR #439 — as the cell says. `cms/terraform/main.tf` is **191** lines with
    `module "cms"` at `:30` and `service_desired_count = 0` at `:39`; `6efa1d5` / `f38c0c4` exist, dated
    2026-08-04. `roadrunner/terraform/main.tf:19` = `service_desired_count = 1`.
19. **AI Labs + credits** (`ai-labs.md`, whole file). `creditCost` map = build **5**, refine **1**,
    translate **1** (note the file's own header comment at `cost.go:29` says refine = 5 and is *stale*;
    the map and `cost_test.go:15` both say 1, so **the corpus is right and the source comment is wrong**).
    `purchasePackages` starter 50 / team 200 / scale 500 (`:201-203`); `MarginMarkup = 1.40` (`:133`);
    `PricePerCreditUSD = 0.45` (`:142`); `DefaultSeedBalance int64 = 500` (`:231`). `LabsTier` =
    essential/professional/frontier; the five `CapReason` values exact; reconciler `Interval: 30 * time.Second`;
    labsapi default `:7070` + `LABS_API_PLATFORM_TOKEN` (`main.go:745`); `labsdemo` `:8099`; `cmd/{labsdemo,
    labsimport,labskey}`. `LabSession` explicitly declines `PrimaryKeyMixin` with a String `id`. All five
    named migrations exist. Stripe webhook (`api.go:317-327`) handles **exactly** `customer.created` +
    `customer.subscription.{created,updated,deleted}` — **no** `checkout.session.completed` in any Go
    source; `/api/webhook/stripe` is authn-skipped at `backend.go:129`; `stripe-go/v74 v74.30.0`.
    `POST /credits/purchase was removed in Wave 13` is the literal comment at
    `internal/web/backend/credits/handler.go:12`. Credit-ledger GraphQL: **0** hits for
    `organizationCredit|creditTransaction|creditBalance` across `graphql/graph/schemas/` (the two
    `credit` hits are `setOrganizationFeatureCredits` and academy prose) — *"Credits has NO GraphQL"* holds.
    `lab_bodies`: `(lab_slug, locale)` unique, EN base + **6** overlays (de/es/fr/it/nl/pt).
20. **Link integrity across all 6 assigned files** — every one of the markdown `.md` targets resolves
    (0 missing, scripted over all 6 files), and every `#fragment` resolves, including
    `shared_libraries.md#taxonomy-figures`, which is an explicit `<a id=…>` at `shared_libraries.md:201`
    rather than a heading (a naive heading-slug check reports it MISSING — it is not).
21. **Intelligence** (`intelligence.md`). `fdfa189` = *"chore: remove intelligence service from local dev
    orchestration"*, 2026-04-17, touching **both** `docker-compose.yml` and `repos.yml`. At `fdfa189~1`
    the service existed with `DB_CONNECTION_BACKEND` / `DB_CONNECTION_SKILLER` exactly as documented.
    `045857c` (Chronos) verified likewise.

**RE-DERIVED (weaker — recorded as such):** the `29 + 2 = 31` and `16 + 7 = 23` sums in
`security_compliance.md`. I state these separately from the SET enumerations in item 8 above; the
enumerations are the clearance, the sums are not.

---

## What I could not settle

- **`service_taxonomy.md:139-140` archive dates** (Skiller 2026-07-01, Skillpath 2026-07-31) and
  `:3`/`:378` (graphql-wundergraph archived 2026-07-30). The file's own note at `:124-129` is right that
  archive state lives only in the GitHub org API: `gh` is not installed on this host and the repos are
  private, so no clone can measure it. The `graphql-wundergraph` clone's last commit is `60c229f` dated
  **2026-07-30**, which is *consistent* with (not proof of) the date. Settled only by
  `gh api repos/anthropos-work/<repo> --jq .archived`.
- **`ai-labs.md:5` — `anthropos-work/AI-Labs` exists as a live repo.** Not in any clone set here, and
  `gh` is unavailable. Settled by an org repo listing.
- **`ai-labs.md:16` — `app knowledge/plan/releases/06.00-shared-purse/`, M600–M607 all planned.** My
  hard bar #1 forbids reading `knowledge/plan/**`; I read that conservatively and did not open the app
  repo's copy either. (An incidental `git grep` did surface `checkout.session.completed` **only** under
  that path and nowhere in Go source, which is what the claim asserts, but I did not open the files.)
- **`clerk-integration.md:52` — "1-minute leeway" on JWKS verification.** That code is in `colony/authn`,
  a private Go module pulled at Docker build; no `colony` clone exists in this tree. Settled by reading
  `colony@v0.35.2 authn/provider/clerk`.
- **`service_taxonomy.md:156` — ≥42,790 skills / ≥22,470 job roles, measured 2026-06-29.** Requires a DB
  read; not attempted (out of scope, and the figure is explicitly pinned to a measurement date).
- **`service_taxonomy.md:469` — `--profile frontend` / `--profile studio-desk` "exit 1".** The
  `depends_on: backend` edges are verified in compose (`:138-140`, `:165-167`) and `backend` is not in
  either profile, so the mechanism is right; I did not run `docker compose --profile frontend config` to
  confirm the exit code, since that is the difference between *parses* and *selects* the corpus itself
  insists on. Settled by one `docker compose --profile <name> config --services` on a Docker-enabled host.
- **`security_compliance.md:261` — DPA v1.4 / 18 sub-processors; `:12` 90-day auto-deletion;
  `:155` 6-hourly triple-destination backups.** Legal/ops facts with no artifact in any clone here.
- **`intelligence.md:18` — the service's default `:8080` and 5-minute ticker.** No `intelligence` clone.
  Note the compose that *did* run it set `PORT=9002` (`fdfa189~1:docker-compose.yml:75`), which the doc's
  *"(PORT env override)"* phrasing accommodates rather than contradicts.
