# Seat F — reading #23

**Refs at this reading (re-verified at open, `git rev-parse --short=8 HEAD` per clone):**
app `b948604f` (origin/main `2035f9a4`), platform `0c91421d` (level with origin/main), next-web-app
`bb3313bc`, sentinel `88bc5592`, storage `4ce8ece5`, messenger `fa47850d`, cms `ca50c817`,
graphql-wundergraph `60c229f3`, roadrunner `87d8d443`, jobsimulation `462343b0`, studio-desk `14a5442a`,
ant-academy `9c3843cd`, nested `app/studio` + `cms/studio` both `aeec036a`,
rosetta-extensions authoring copy `.agentspace/rosetta-extensions` on `main` @ `09d0607`,
per-stack consumption clone `stack-demo/rosetta-extensions` @ `ab81527`.

**Instrument note.** One search pipeline broke silently during this pass and was caught by reading
stderr: in zsh `git show "$c:repos.yml"` expands `:r` as a history modifier, producing
`<sha>epos.yml` and a `fatal:` on stderr with an empty stdout — i.e. an "absence" that was an engine
error (§5 rule 1). Re-run as `git show "${c}":repos.yml`. Every enumeration below was run with a
positive control in the same pass.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/services/ant-academy.md` | 457 | yes |
| `corpus/architecture/architecture_overview.md` | 417 | yes |
| `corpus/architecture/shared_libraries.md` | 247 | yes |
| `corpus/services/roadrunner.md` | 206 | yes |
| `corpus/architecture/dependency_map.md` | 103 | yes |
| `corpus/services/db-backup.md` | 31 | yes |
| (total) | 1461 | — |

---

## Blockers

### B1 — `/free` renders no `PublicHeader`; it is a bare `redirect()`, so the 2-way LocaleSwitch is on `/library` only

- **Anchor:** `corpus/services/ant-academy.md:229-230` — *"`src/i18n/LocaleSwitch.jsx` **is** a 2-way
  EN↔IT toggle `<Link>` that sets `?lang=it` on the current path — but it is mounted **only in the
  public-storefront header** (`src/views/public/PublicHeader.jsx:20`, i.e. `/library` + `/free`)."*
- **Why it is false:** `PublicHeader` is imported by exactly **one** page in the whole app —
  `git -C stack-demo/ant-academy grep -ln PublicHeader 9c3843cd -- 'code/**'` returns
  `code/app/(public)/library/page.jsx` and the component's own file, and nothing else.
  `code/app/(public)/free/page.jsx` is **19 lines long and renders nothing**: its only import from
  `next/navigation` is `redirect`, and the default export is

  ```js
  export default function Page() {
      redirect('/?tier=free')
  }
  ```

  with an in-file comment (`:5-8`) stating *"`/free` used to render a standalone flat 'Free chapters'
  list … It now redirects to the real catalog filtered to the free tier (`?tier=free`)"*. A visitor at
  `/free` therefore lands on `/`, which is served by `code/app/(authed)/page.jsx` → `AcademyClient.jsx`
  → `TopBar` → `LanguageSelector` — i.e. the **7-locale dropdown**, the exact component the same bullet
  is at pains to distinguish from `LocaleSwitch`. The sentence gets the switcher's surface wrong in the
  direction that the bullet exists to correct.
- **Confidence:** high

### B2 — the `TopBar` surface list omits `/courses` and `/courses/[slug]` — the doc's own prescribed CTA route

- **Anchor:** `corpus/services/ant-academy.md:234` — *"and `TopBar` is on `/`, `/chapters/*`,
  `/latest`, `/bookmarks` and `/my-activity`."*
- **Why it is false:** enumerated from source at `ant-academy 9c3843cd`. `TopBar` is imported by
  `code/src/views/academy/AcademyClient.jsx:28`, `code/src/views/course/CourseClient.jsx:58`,
  `BookmarksClient.jsx`, `LatestClient.jsx`, `MyActivityClient.jsx` and directly by
  `code/app/(authed)/chapters/[slug]/page.jsx`. **`AcademyClient` is the view for three routes, not
  one**: `code/app/(authed)/page.jsx` (`/`), `code/app/(authed)/courses/page.jsx` (`:5`
  `import AcademyClient …`) and `code/app/(authed)/courses/[slug]/page.jsx` (`:8`
  `import AcademyClient …`). So the measured surface set is **seven** routes —
  `/`, `/courses`, `/courses/[slug]`, `/chapters/*`, `/latest`, `/bookmarks`, `/my-activity` — and the
  list names five, dropping both `/courses` routes.
  This is material rather than pedantic for two reasons the same document supplies: `:292` instructs
  *"Link a course CTA at `/courses/<slug>`, never `/library/<slug>`"* — so the omitted routes are the
  ones a demo actually lands on — and the whole bullet exists to settle *where the language switcher
  is*, which is precisely what the incomplete list gets wrong.
- **Confidence:** medium (an omitted list member, but the omitted members are the load-bearing ones)

### B3 — `SKILLER_STREAM` at `2035f9a4` is 6 Go occurrences across **3** Go files, not 4

- **Anchor:** `corpus/architecture/dependency_map.md:59` — *"`SKILLER_STREAM` has **6** Go occurrences
  across 4 files, not one"* (the sentence names `2035f9a4` as its ref).
- **Why it is false:** measured at the ref the sentence names.
  `git -C stack-demo/app grep -n SKILLER_STREAM 2035f9a4 -- '*.go'` returns **6 lines** in
  **3 files**: `main.go:1532`, `main.go:1537`, `subscriber_merge_test.go:907`,
  `subscriber_merge_test.go:1015`, `subscriber_merge_test.go:1038`, `subscriber_wiring.go:165`.
  `git grep -l … -- '*.go'` prints those three paths and no fourth. The **6** is exactly right; the
  file count is not. (The number 4 does not correspond to any scoping I could reproduce: repo-wide,
  all file types, the count is **6** files — `main.go`, `subscriber_merge_test.go`,
  `subscriber_wiring.go`, `terraform/main.tf`, `knowledge/skiller-domain.md`,
  `knowledge/plan/releases/09.00-support-in-app/m905-subscriber-merge/decisions.md`.)
  Three-instrument absence check per the addendum: the nested repo `stack-demo/app/studio @ aeec036a`
  returns **0** for the predicate at its own ref; `git grep` sees gitignored-but-tracked files; no
  NUL-bearing Go source exists in `app`. So the 3 is not an artefact of a hidden file.
  Booked because this is a correction sentence — text written to repair a previously-wrong count, and
  the class §5 rule 18 identifies as the highest-risk in the corpus.
- **Confidence:** high

### B4 — `internal/ai/ai.go` does not exist in `app`, the only live consumer; the corpus's own twins name the real path

- **Anchor:** `corpus/architecture/shared_libraries.md:130-131` — *"Vendor selection lives in each
  consumer's own `internal/ai/ai.go` wrapper: an EU Azure client by default, a US Azure client gated by
  the PostHog flag `flag_use_azure_us`, and an Azure→direct-OpenAI fallback on HTTP 429."*
- **Why it is false:** the same document's **Imported by** row two lines above (`:110`) states the live
  consumer set is *"**`app` alone among the services a stack runs**"*. At `app b948604f` there is **no
  `internal/ai/` package at all** — `git ls-files | grep '^internal/ai'` matches only
  `internal/aireadiness/…`, and `git cat-file -e b948604f:internal/ai/ai.go` fails. The three
  mechanics quoted are real but live at **`internal/jobsimulation/ai/ai.go`**
  (`flag_use_azure_us` at `:267`/`:344`, `isThrottlingError` at `:129`). `internal/ai/ai.go` exists
  only in the **frozen `jobsimulation` repo** (`462343b0`), which nothing clones or builds.
  Self-contradiction inside the corpus: `architecture_overview.md:292` writes *"Measured at
  `app/internal/jobsimulation/ai/ai.go`"* and `external_services.md:581` writes *"all in
  `app/internal/jobsimulation/ai/ai.go`"*. A reader following this row greps for a path that resolves
  nowhere in the tree a stack builds.
- **Confidence:** medium (the mechanism is right; the path — the thing a reader would act on — is not)

### B5 — the `emptyCatalogView()` literal is quoted as an equality and is missing two keys, one of them non-empty

- **Anchor:** `corpus/services/ant-academy.md:134-135` — *"so on **any** failure the catalog becomes
  `emptyCatalogView() = { chapters: [], skillPaths: {}, series: [] }` → **0 cards**."*
- **Why it is false:** `ant-academy 9c3843cd`, `code/src/lib/serverTenant.js:115-117`:

  ```js
  function emptyCatalogView() {
    return { chapters: [], skillPaths: {}, series: [], bundles: PUBLIC_BUNDLES, catalogVersion: CATALOG_VERSION }
  }
  ```

  Five keys, not three — and `bundles` is **not** empty: `PUBLIC_BUNDLES` is a populated exported array
  (`code/ucourses/catalog.js:956`), and the function's own doc-comment two lines above (`:111-113`)
  singles it out — *"`bundles` (PUBLIC_BUNDLES) carries no tenant metadata and is the one piece not yet
  modeled in the backend catalog, so it passes through verbatim."* So the "empty view" is not an empty
  object, which is exactly the nuance someone debugging the demo empty-grid or authoring the
  `academy-fs-published-fallback` demo-patch is looking at.
- **Why I hesitated:** the *conclusion* (`→ 0 cards`) is correct, and this is an abbreviated quotation
  rather than an inverted mechanism. I booked it a BLOCKER because the passage uses `=` to assert an
  exact value, the asserted value is not the code's value, and the omitted member has a stated
  behavioural consequence — so it is not "no factual consequence" under the MINOR definition.
- **Confidence:** low

---

## Minors

- `corpus/services/ant-academy.md:258` — *"~26 Playwright e2e spec files (tests/e2e/)"*. Measured at
  `9c3843cd`: `git ls-files code/tests/e2e | grep -c '\.spec\.js$'` → **31** (30 excluding the one
  `_`-prefixed helper spec). Hedged with `~`, and a Testing-table row, so non-material — but stale by
  ~19 %.
- `corpus/services/ant-academy.md:144` — *"a separate, unauthenticated `code/public/catalog.json`
  (~2,667 entries)"*. Measured: the committed `catalog.json` (`generatedAt 2026-07-21`) has
  `courses` = **2,715** entries over 7 languages. Hedged with `~`; 1.8 % low.
- `corpus/services/roadrunner.md:31` — the case-insensitive per-clone sweep *"returns hits in app (25
  files), jobsimulation (8), platform (3), studio-desk (3), next-web-app (1)"* reproduces **exactly**,
  but the sentence says *"every clone"* while omitting `roadrunner` itself (23 files) and
  `rosetta-extensions` (44 files, consumption clone `ab81527`). Reads correctly in context (*"no
  **other** platform repo"*), so cosmetic.
- `corpus/services/roadrunner.md:13-14` — *"`roadrunner/terraform/main.tf:19` still reads
  `service_desired_count = 1` and has not been touched since `87d8d44` (2026-06-19, before the fold)"*.
  The value and anchor verify exactly. The subject of *"has not been touched"* is ambiguous: `87d8d44`
  is the repo HEAD (2026-06-19), while the **file** was last touched at `e45eb61` (2026-05-27). True
  either way, but the sentence reads as if `87d8d44` touched `main.tf`.
- Cross-file, outside this reading's scope but worth routing: root `CLAUDE.md` says the academy *"reads
  only from `code/.env.local`"*, while `ant-academy.md:345,366` (correctly, and matching
  `code/.env.example`) instructs `cp .env.example .env` in `code/`. Next.js reads both; the *"only"* in
  the higher-propagation file is the wrong half.

---

## Positively cleared (audited zeros)

**ENUMERATED** — predicate re-derived from source independently, cardinality stated before arithmetic:

1. `architecture_overview.md:363-370` **multi-tenancy split (31 of 135 / 23 / 16)** — the passage §5
   rule 24 exists for. Derived the SET, not the sum: `internal/data/ent/schema/` holds 139 `.go` files,
   **135** of which embed `ent.Schema` (the 4 non-schemas are `database_types.go`, `mixin.go`,
   `skiller_mixins.go`, `skillpath_mixins.go`). `grep -n 'OrganizationMixin{}'` returns **30** files;
   `user_resource.go:22` is `// OrganizationMixin{},  // We need to work on this` → live set **29**.
   Only **four** files declare any `Policy()`: `mixin.go`, `org_membership.go` (`:172`,
   `AllowCurrentOrgEdgesOrSkipRule`), `organization.go` (`:56`, `FilterSameOrganizations()` at `:96`),
   `user.go` (`:116` — filters by **user** via `FilterSameUserRule`, correctly excluded). So
   29 + Membership + Organization = **31** ✓. Union of {literal `"organization_id"`} ∪
   {`OrganizationIDMixin{}` users} minus the 31 = **24**, minus `academy_feedback.go` (policed by
   `UserMixin`) = **23** ✓, of which 7 are the `OrganizationIDMixin` users
   (category/jobrole/similarity/skill/specialization/studio_document/studio_task) and **16** carry
   neither mixin ✓ — my 16 match `security_compliance.md`'s named 16 file-for-file.
2. `shared_libraries.md:186` **taxonomy repo-history set (11 go repos ever)** — derived from
   `repos.yml`'s whole history (11 commits touch it): **14 names ever**, of which `type: go` is
   **11** — app, chronos, cms, intelligence, jobsimulation, messenger, roadrunner, sentinel, skiller,
   skillpath, storage ✓. All four of skiller/skillpath/chronos/intelligence are present in the first
   revision `a2a3ee6` ✓; ordering of removals is fdfa189 (intelligence) → 045857c (chronos) →
   21429b7 (skiller) → **a4db680 (skillpath, last)** ✓.
3. **All shared-library pins, all seven Go clones, per `go.mod` line** — app `ai v1.40.2:14`,
   `colony v0.35.2:16`, `proto v1.210.0:18`, `taxonomy v1.2.0:20`; sentinel `colony v0.34.3:8`,
   `proto v1.200.0:9`, `taxonomy v1.2.0 // indirect:21`; storage `:7/:8/:25`; messenger `:7/:8/:9`;
   cms `:9/:10/:11/:13`; jobsimulation `:11/:12/:13/:15`; roadrunner `:7/:8` (colony+proto only, no
   taxonomy → the "6 of 7" claim ✓). Every line anchor in `shared_libraries.md:41,69,109,110,186` and
   `dependency_map.md:50` resolves exactly.
4. `architecture_overview.md:80` **"0 hits for `github.com/anthropos-work/authn` across all seven Go
   clones, positive control `colony` in all seven"** — reproduced exactly (0/7 standalone; colony
   required by 7/7; `colony/authn` imported by app 129 files, cms 6, jobsimulation 8 — matching
   `shared_libraries.md:150`'s "6 and 8").
5. `shared_libraries.md:70` **"six Connect handlers, five unconditional"** — `git grep 'ServiceHandler('
   b948604f -- '*.go'` returns exactly six sites: `main.go:1187/1188/1196/1204/1213/1227`, with `:1213`
   inside `if cmsRPCServer != nil` (`:1212-1214`) and no `SkillPathSessionService` / RoadRunner among
   them ✓.
6. `dependency_map.md:59` **publisher enumeration @ `b948604f`** — all five `pubsub.NewPublisher` sites
   are `main.go:287/:637/:1039` + `jobsimwiring/wiring.go:127/:180`; `SKILLER_STREAM` has exactly one
   Go occurrence (`main.go:1276`, an `AddSubscriber`, handler `internal/roles/roles.go:791` =
   `func (r *RoleManager) SkillerSubscriber()`) ✓. (Only the *file count* in the same cell is wrong —
   B3.)
7. `dependency_map.md:58` **"21 live handlers"** on messenger's `backend` subscriber — `flow.go:72-104`
   contains 22 `pubsub.EventHandler(` lines, one of which (`:83`,
   `OrgJobSimulationAssignmentPastDueHandler`) is **commented out** → **21 live** ✓, and `app`'s own
   `internal/messenger/flow/streams.go:70` @ `9d00a313` calls it *"the 21-handler set"*. A naive
   `grep -c` here returns 22 — §5 rule 24 in miniature.
8. `dependency_map.md:7,13,24,31` **compose topology** — at `0c91421`: `docker-compose.yml` declares
   **5** services (sentinel, backend, studio-desk, next-web-app, gotenberg) + `common.yml` 2
   (postgresql, redis) = **7 effective**; `repos.yml` has **4** entries. At `0dab54d` it was 8+2 = **10**
   and **6** ✓. Exactly one service address (`AUTHORIZATION_ADDRESS`, `:48`) and **zero** `*_RPC_ADDR`
   in either compose file (positive control: `ADDRESS` → 1 hit) ✓. `:101-109` is `backend`'s
   `depends_on` with the `:102-103` in-line comment quoted verbatim ✓; `:138-140` is studio-desk's
   single `backend` edge ✓.
9. `roadrunner.md` **historical anchors** — `repos.yml @ 2adcf71` has 9 entries with roadrunner at
   `:29-31` (comment *"legacy — folded into app; backend calls Judge0 directly"*) and jobsimulation at
   `:17-19` ✓; `repos.yml @ 0dab54d` has exactly the 6 named ✓; `docker-compose.yml @ 2adcf71` has
   jobsimulation at `:83` with `profiles: [graphql, jobsimulation, all]` ✓ and roadrunner's
   `:291-292` ports / `:296-302` env with `PORT=10400` at `:298` and `RPC_PORT=10401` at `:302` ✓;
   `d11a403`'s diff removes `- name: cms`, `- name: jobsimulation` **and** `- name: roadrunner` while
   its message claims roadrunner's entry *"was already gone"* ✓ — the corpus's "grade it by the diff"
   is correct.
10. `roadrunner.md` **repo internals @ `87d8d443`** — `cmd/root.go:84` `cmp.Or(os.Getenv("RPC_PORT"),
    "8081")` and `:110` `cmp.Or(os.Getenv("PORT"), "8080")` ✓; go 1.25.0 ✓; asynq v0.25.1 ✓;
    `queues.DefaultQueue = "roadrunner:default"` ✓; `tasks.RoadrunnerSubmissionResult =
    "roadrunner:submissionresult"` ✓; `Concurrency: 10` ✓; `asynq.MaxRetry(3)` at `runner.go:126`
    inside `CreateSubmission` ✓; `maxRetries = 15` with `time.Sleep(1 * time.Second)` ✓; three RPC
    methods Submission/SubmissionPackage/SubmissionResult ✓; **zero** `*_test.go` files and
    `Dockerfile` line **18** is `RUN go test -v ./...` ✓; language map has `"py"`, no `"python"` ✓;
    no vendored proto dir ✓; `internal/lsp/lsp.go` present ✓.
11. `roadrunner.md:24-32` **the Go-scoped zero** — `ROADRUNNER_RPC_ADDR` / `RoadRunnerService` /
    `roadrunner:10401` all return **0** Go hits at both `app b948604f` and `jobsimulation 462343b0`
    (positive control: `judge0` → 32 Go hits in app) ✓; `jobsimulation`'s Go source has exactly one
    `roadrunner` mention (`internal/runner/runner.go:3`, a comment) ✓ and case-insensitively **14
    lines across 8 files, 5 exact-case across 3** ✓; `knowledge/operational.md:68` is the
    `ROADRUNNER_RPC_ADDR` table row ✓; `app/internal/jobsimulation/simulator/stream_handlers.go:30-34`
    is the NOTE ending *"NOT stream handlers."* ✓; `ROADRUNNER_RPC_ADDR` is absent from platform HEAD
    including `.env_example` (positive control `JUDGE0` → 3 hits) ✓.
12. `architecture_overview.md:20,221` **jobsimulation M810** — `6092c6d2` exists, is reachable from
    `origin/main` via `caf36c96` (PR #439), and at that ref `terraform/main.tf` is **56 lines** with
    `:15-22` the M810 header (*"The `module "jobsimulation"` block … was deleted here, which destroys
    the ECS service, task definition, ECR repository…"*) and `:24-40` the surviving-ownership list
    (livekit.tf / chime.tf / ssm.tf / atlas tracker) ✓. `origin/main` is exactly **4** commits ahead of
    the `462343b0` checkout, all dated **2026-08-04**, PR #439 among them ✓ — the corpus's figure
    reproduces to the commit. (Note for the record: the *checkout* still carries the module at `:31`
    with `service_desired_count = 0` at `:40`; the claim is pinned to `6092c6d2` and is graded there
    per §5 rule 33.)
13. `architecture_overview.md:330-355` **storage** — at `9f8cb53` (on storage's `origin/main`, 21
    commits past the checkout) `terraform/main.tf` is **18 lines**, `:9-11` is *"The ECS service that
    used to live here is GONE (v9.0 'support-in-app')"* and `:13-16` the `prevent_destroy` reasoning ✓;
    `63bffc8` did read `service_desired_count = 0` ✓. `STORAGE_RPC_ADDR` @ `9d00a313`: exactly **3** Go
    lines, all comments — `main.go:451`, `internal/jobsimwiring/wiring.go:101`,
    `internal/storagens/callsites_test.go:189` — and **29 lines / 18 files** repo-wide ✓; at
    `origin/main` `main.go:504` is the quoted comment ✓; at `b948604f` `wiring.go:115` still *reads*
    it ✓.
14. `architecture_overview.md:280`, `:222` **supergraph 3 → 1** — `915da06`'s subject says *"2→1"* while
    its diff deletes both the `jobsimulation` and `cms` subgraph entries and rewrites the README line
    *"Composes 3 subgraph services"* ✓. `graphql-wundergraph/terraform/main.tf:20` = `= 1` ✓ and
    `locals.tf:8` `port = 8080` ✓ (the `:311` prod-flow diagram). `cms/terraform/main.tf:39` = `= 0` ✓,
    and `6efa1d5` / `f38c0c4` exist with the quoted subjects ✓ — both sides of the "report both, assert
    neither" pair are real.
15. `architecture_overview.md:281-300` **AI routing** — `app/internal/jobsimulation/ai/ai.go`
    `:262-276` is `getClient`'s Azure arm (default `azureClientEu`, `flag_use_azure_us` →
    `azureClientUs`) ✓, `:129` is `func isThrottlingError`, applied at `:166` and `:325` ✓, `:85-88`
    is the Bedrock `config.WithRegion("eu-west-1")` ✓; `internal/cms/studio/markdownManager.go:11`
    imports `ai/mistral` and `:19` builds it from `MISTRAL_API_KEY`, called at `studioManager.go:583` ✓;
    all **three** `app/studio/configs/*.ini` pin `azure` on every `*_AI_*_MODEL` row (read at the nested
    repo's own ref `aeec036`) ✓; `external_services.md:579` is the retraction sentence and `:602-607`
    the four-live-plus-one-latent paragraph ✓.
16. `dependency_map.md:25` **studio-room providers** — at the nested `app/studio @ aeec036a`,
    `services/ai.py:704-709` is the whole `get_client` provider registry
    (`openai`/`azure`/`anthropic`, no mistral) ✓ and `tools/pdf2md.py:24` is
    `from mistralai import Mistral` ✓; the case-insensitive `mistral` sweep at that ref returns
    **22 lines / 3 files** — the corpus's figure, and the number the host repo's `git grep` reports as
    **0**.
17. `shared_libraries.md:23-31` **"absent from `platform/repos.yml`"** — none of colony/proto/ai/authn/
    taxonomy appears in any of the 14 names ever written to `repos.yml` ✓; `docker-compose.yml` passes
    `GH_ACCESS_TOKEN: $GH_PAT` (`:12`, `:39`) ✓; `app/Dockerfile.dev:11` is the `x-access-token …
    insteadOf` rewrite and `:13` `ENV GOPRIVATE=github.com/anthropos-work/*` ✓; **no `go.work` is
    tracked in any of the seven Go clones** ✓.
18. `shared_libraries.md:207-208` **taxonomy figures** — read the capture manifest directly
    (`.agentspace/snapshots/taxonomy/5afc0bcc…/manifest.json`): `source: primary-read`,
    `public_only: true`, `predicate: org-null`, `captured_at 2026-06-29T14:43Z`;
    `public.skills` **42,790**, `public.job_roles` **22,470**, `public.job_role_embeddings` **18,919**,
    each with `filter: organization_id IS NULL` ✓ — all three numbers and the provenance line are exact.
19. `ant-academy.md:262-281` **the complete `isPublic` matcher** — enumerated from
    `code/proxy.js:112-188` and compared row-for-row against the table: 25 static patterns + the 2
    `DEV_LOGIN_ENABLED` + the 3 `VISUAL_BYPASS` entries, and the table lists **all thirty**, with the
    right grouping and the right gates (`VISUAL_BYPASS = BENCHMARK_VISUAL_BYPASS === "1" && NODE_ENV
    === "development"` at `:62-64` — whitelisted, as claimed; `ORG_GATE_ENABLED` fail-closed at
    `:88-92`). `/api/chapters/*` is indeed absent ✓. The only two real `(public)` pages are
    `library/page.jsx` and `free/page.jsx` and there is no `/library/[slug]` ✓ (the per-course page is
    `(authed)/courses/[slug]/page.jsx`).
20. `ant-academy.md:117-152` **the DB-authoritative read chain** — `serverTenant.js:143-146` is
    literally `const view = (await getBackendCatalogView(eids)) ?? emptyCatalogView()` ✓ with
    `getPublicCatalogView()` the empty-eid variant at `:163-165` ✓; the *"the cutover is intentional,
    not reversible-on-error"* quote is at `:132`, inside the cited `:115-145` window ✓;
    `backendContent.js:54-68` `makeClient()` returns `null` when `createServerGraphQLClient` throws,
    `:94-96` `getBackendCatalogView` returns `null` on a null client and `:108` on a query error ✓;
    `graphql/server.js:14,18` reads `NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT` and throws when unset ✓;
    `academyCatalogSeries` + `academyCatalogSkillPaths` are the read surface ✓.
21. `ant-academy.md:169-190` **the chapter-body half** — `serverChapterBody.js:51-67`:
    `resolveServerChapterBody` is `const body = await getBackendChapterBody(slug, locale)` → the
    dev-only `maybeResolveDraftBody` (gated on `draftsEnabled()` ∧ `isFsDraftSlug`) → `{ notFound: true }`
    ✓; `code/app/not-found.jsx:43` is *"You wandered off the trail."* ✓.
22. `ant-academy.md:194-199, 211-218, 318-341` **rext claims, graded at the ref the passage names
    (`main`, authoring copy @ `09d0607`)** — `demo-stack/ant-academy.sh` declares the five patch paths
    at exactly `:56` (dev-origins), `:66` (fs-published), `:75` (fs-published-body), `:84`
    (fs-published-public), `:90` (back-to-cockpit) ✓; `:576-583` is the M220 comment and the launch at
    `:584-585` sets `NODE_ENV=development` and **neither** `BENCHMARK_VISUAL_BYPASS` nor
    `NEXT_PUBLIC_E2E_AUTH` ✓; both named tests exist —
    `demo-stack/tests/test_ant_academy.py:346` and
    `demo-stack/tests/test_ant_academy_clerk_wiring.py:102` ✓; `cockpit.py:812` sets the `e2e_persona`
    cookie client-side, `:1496` as a `Set-Cookie` on the `/go` 302, `:327` names both ✓;
    `stack-verify/live/autoverify.sh:459` is assert **(f)** *"THE ACADEMY RENDERS ITS CATALOG (M245…)"* ✓.
23. `ant-academy.md` **repo facts @ `9c3843cd`** — `engines.node >= 22` ✓; `dev` script
    `next dev --port 3077` ✓; mobile `:8555` ✓; `next ^16.2.6` / `react ^19.2.5` /
    `babel-plugin-react-compiler` ✓; Expo `~54.0.33` ✓; `code/vercel.json` is exactly
    `{"framework": "nextjs"}` ✓; `layout.jsx:1` imports exactly `{ Work_Sans, Instrument_Serif }`,
    `:41`/`:51` are those two declarations, `:132` is `manifest: "/academy-manifest.json"` (whose
    `display` is `standalone`), and `academy.css:1` says *"Display + mono usages fall back to system
    fonts"* — **neither DM Sans nor JetBrains Mono is loaded anywhere** ✓; FA Pro vendored at
    `code/public/assets/fontawesome/` (99 files, 29 `.woff2`, `css/all.min.css` present) with
    `FONTAWESOME_NPM_AUTH_TOKEN` surviving only at `code/.env.example:97` ✓;
    `RegisterServiceWorker.jsx` is a documented kill-switch ✓; Cosmo is
    `ucourses/ucourse-engine/assistant/agent.js` `API_URL = 'https://api.openai.com/v1/responses'`,
    `MODEL = 'gpt-5.2'`, key from `localStorage('openai_api_key')`, gated on
    `NEXT_PUBLIC_FEATURE_TRAINING_COACH` ✓; every named workflow + `cog.toml` exists ✓;
    `build-catalog.mjs` lives at `.claude/skills/build-index/` ✓; every skill in the table exists ✓;
    `code/app/api/academy/beacon/route.js` ✓; `upsertChapterProgress[Batch]` are real mutations in
    `internal/web/backend/graphql/graph/schemas/academy.graphqls:694-698` and the Ent tables pluralize
    to `academy_chapter_progresses` / `academy_last_activities` from
    `academy_chapter_progress.go` / `academy_last_activity.go` ✓ — the parenthetical about singular
    schema-file names vs plural tables is exactly right.
24. `ant-academy.md:236-238` **the two dated commits** — pickaxed rather than trusted:
    `git log -S LanguageSelector -- code/src/components/TopBar.jsx` returns **exactly one** commit,
    `5b05b7d9` (2026-05-05), and `--stat` confirms it adds `LanguageSelector.jsx` (+66) and
    `TopBar.jsx` (+55) despite its covers-flavoured subject; `git log -S "'pt'" --
    code/src/i18n/locale.js` returns **exactly** `e22f3230` (2026-05-18) ✓. `locale.js:10` is the
    7-entry `SUPPORTED_LOCALES` ✓, `PublicHeader.jsx:20` is `<LocaleSwitch …>` ✓, `TopBar.jsx:76` is
    `<LanguageSelector />` ✓, and `LanguageSelector.jsx:88/:97` render `role="menu"` /
    `role="menuitemradio"` ✓. The retraction in this bullet is correct on its own terms — only the two
    surface enumerations around it are not (B1/B2).
25. `dependency_map.md:19,58,60,61,102,103` **misc anchors** — `storage.md:40` and `:47` both do say
    storage owns no database ✓; `messenger/internal/flow/flow.go:72/:105/:109` are the three
    `AddSubscriber` calls ✓; `app/internal/messenger/flow/streams.go:65` @ `9d00a313` is
    `StreamBackend = "backend"` and `main.go:1442` @ that ref is the `verifyConsumerGroupExists` guard
    ✓; `app/internal/converter/gotenberg.go:31` posts to `/forms/libreoffice/convert` and compose `:57`
    injects `GOTENBERG_URL=http://gotenberg:3200` ✓; `/api/webhook/directus` +
    `DIRECTUS_WEBHOOK_SECRET` are real (`internal/web/backend/backend.go:137-138`, `:324`) ✓;
    `AI_USAGE_STREAM=AI` at compose `:47` ✓; the postgres image really does build `pgvector` ✓;
    `customerio-sync` was in `profiles: [customerio-sync, all]` at `0dab54d` and `make up-all` exists ✓;
    `architecture_overview.md:128`'s *"spawns studio/gen.py in-process"* resolves to
    `internal/cms/studio/studioManager.go:119` ✓.

**RE-DERIVED** (arithmetic the document displays, re-computed — the weakest clearance):

- `shared_libraries.md:77-88` — the named Connect-RPC service list totals **13**, consistent with
  *"at least 13 … a floor, not a count"*. I could not enumerate the predicate: `proto` is in **no**
  clone set, which the row itself discloses.
- `architecture_overview.md:12,71,157` — "five containers / two of them ours" recomputed from the
  compose selection (`core` → backend + gotenberg; floor = the three services with no `profiles:` key).
  This one is close to ENUMERATED: I derived the floor from the absence of a `profiles:` key across
  both `docker-compose.yml` and the `include:`d `common.yml`.

---

## What I could not settle

- **`corpus/services/db-backup.md` in its entirety (31 lines).** There is no `db-backup` clone in the
  ground-truth set and no `db-backup` entry in any revision of `repos.yml`, so every claim in it — the
  6-hour cadence, Go, the S3/Azure/Hetzner triple, *"PostgreSQL RDS (all schemas)"*, *"runs on a
  timer, performing full database dumps"* — is unmeasurable here. It is *internally* consistent and
  consistent with `architecture_overview.md:413` and `dependency_map.md:38`. **What would settle it:**
  the `db-backup` repo (or the `infrastructure` repo) in the clone set — its scheduler config and
  uploader targets. Flagging it because the file carries no ref pin, no `file:line`, and no
  prose-under-review marker, which by §5 rule 27 is the shape most likely to have quietly expired.
- **GitHub archive states** — `roadrunner.md:43,74` (*"NOT archived"*), `:37-38` (skiller archived
  2026-07-01, skillpath 2026-07-31), `:56-57` (chronos not archived, last push 2026-04-23),
  `architecture_overview.md:3` (graphql-wundergraph archived 2026-07-30). No network access; a local
  clone cannot report a repo's archived flag. `ant-academy.md`'s and `jobsimulation.md`'s *"not visible
  to this corpus"* framing is the honest one and the roadrunner/chronos lines assert more than that.
  **What would settle it:** `gh repo view --json isArchived` per repo.
- **Colony / proto / ai / taxonomy internals** — `shared_libraries.md:40,48-54,56-59,68,96-99,148,
  184,193,220-234` (colony's package inventory, the `rate.NewLimiter(1,5)` commented reject path, the
  Sentry gate, buf `v1.57.0`, the `~10` sourceless `go/simulator/*` packages, authn tag `v1.7.0`,
  taxonomy's 131-line `node.go` and its ID grammar/examples). None of the five library repos is in any
  clone set — by design, and the document says so. Only the *consumer-visible* halves were checkable
  (all of which passed: the pins, `legacyStorage`, `colony/authn` import counts, the absence of the
  standalone `authn`). **What would settle it:** cloning the four private modules at the pinned tags.
- **`ant-academy.md:74`'s "a grid of 65 real cards"** and `:189-190`/`:244-245`'s *"proven live on
  `billion`"* HTTP results — live-demo measurements with no static counterpart. The committed
  `catalog.json` (2,715 language-expanded entries) is explicitly *not* the grid's source, so it cannot
  stand in. **What would settle it:** a `demo-N` bring-up with `ACADEMY_DEMO_FS_PUBLISHED=1` and a
  card count off `/library`.
- **`architecture_overview.md:401`'s `extensions` schema** — the platform's postgres image
  demonstrably builds `pgvector` (`platform/postgresql/Dockerfile`), but *which schema the extension is
  created in* is decided by a migration I did not trace. **What would settle it:** the
  `CREATE EXTENSION` statement in `app/terraform/migrations/`.
- **`architecture_overview.md:409,414`** (VPC `10.0.0.0/16`, public/private subnet split, 30 s ECS
  health checks with automated rollback) — all live in `anthropos-work/infrastructure`, which the
  corpus itself notes *"has never been in any clone set"*.
