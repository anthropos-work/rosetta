corpus/services/academy-backend.md:43-46  [R3 names a reachable artifact]  **Design invariant**: **additive-only** — every table is NEW with a child-side forward edge to `User`; the legacy `skillpath`/`assignment`/`user_*` schemas are untouched.
corpus/services/academy-backend.md:43-46  [R4 default — presumed citable]  Catalog tables have NO `UserMixin` (global content); their gating is a manager/resolver concern.
corpus/services/academy-backend.md:50-51  [R3 names a reachable artifact]  **Codebase**: `app/internal/academy/`.
corpus/services/academy-backend.md:50-51  [R3 names a reachable artifact]  Go, inside the `app` (backend) service.
corpus/services/academy-backend.md:86-88  [R3 names a reachable artifact]  **Certificate minting** (`certificate.go`): id `AAS-YYYY-XXXXXX` (`AAS` = "Anthropos Academy Skill"), a 6-char suffix from a 30-symbol ambiguity-free alphabet via `crypto/rand`.
corpus/services/academy-backend.md:110-111  [R3 names a reachable artifact]  **Background task**: the nightly Asynq **`academy_embedding_refresh` @ 03:00** materializes `academy_path_embeddings` (skip via `source_hash = sha256(model | embed_text)`).
corpus/services/academy-backend.md:148-148  [R3 names a reachable artifact]  **Service Name**: none of its own — part of the `backend` service (`docker compose up -d backend`).
corpus/services/academy-backend.md:155-159  [R3 names a reachable artifact]  Unit tests use an **in-memory SQLite (`enttest`) harness** (`sqlite_test.go`) — the academy Manager is pure Ent, so no real Postgres is needed (the one Postgres-only feature, `SELECT … FOR UPDATE`, is `t.Skip`'d under SQLite).
corpus/services/ai-readiness.md:3-11  [R3 names a reachable artifact]  **Package-refactor refresh 2026-07-23 (v2.7 "july jitter" M247)** — see the ⚠️ callout below: the domain moved out of `app/internal/workforce/` into its own **`app/internal/aireadiness/`** package (app `v1.351.1`), the archetype **scoring bands changed**, and several net-new subsystems landed (notifications, email overrides, one-click provisioning, auto-close, a recommendation engine).
corpus/services/ai-readiness.md:3-11  [R3 names a reachable artifact]  The feature ships in **`app` v1.315+** (backend) + **`next-web-app` v2.89.0+** (UI).
corpus/services/ai-readiness.md:74-79  [R3 names a reachable artifact]  It is a subsystem of the **`app` (backend) service** — its own package **`app/internal/aireadiness/`** (split out of `internal/workforce/`; see the ⚠️ package-refactor callout above) — not a standalone microservice.
corpus/services/ai-readiness.md:83-84  [R4 default — presumed citable]  The feature is off until an org turns it on. Two gates exist — but **they do not both apply to every surface**, and assuming they do sends you hunting for a PostHog problem the manager dashboard does not have:
corpus/services/ai-readiness.md:175-175  [R4 default — presumed citable]  | 1 | `skill_mapping`    | self-map AI skills (framework modal) | **30** | ≥1 `user_skill_evidences` row for a skill node_id in the org's `ai_readiness_skills` set (presence-based; level ignored) |
corpus/services/ai-readiness.md:188-192  [R4 default — presumed citable]  The Step-2 sim resolves per-user by track (`resolveUserTrack`, business-wins); the interview (Step-3) is shared across tracks (`track = "both"`).
corpus/services/ai-readiness.md:188-192  [R4 default — presumed citable]  `defaultReadinessSims` is 3 entries: `{simulation, "tech", …}`, `{simulation, "business", …}`, `{interview, "both", …}`.
corpus/services/ai-readiness.md:188-192  [R4 default — presumed citable]  Cycles also gained a `launched_by`.
corpus/services/ai-readiness.md:194-197  [R3 names a reachable artifact]  Per-step status lives in `ai_readiness_user_step_progresses` (`not_started`/`in_progress`/`completed`, `completed_at` set once, never re-updated).
corpus/services/ai-readiness.md:205-205  [R3 names a reachable artifact]  | `ai_readiness_cycles` | a diagnostic cycle per org | `status: active\|closed`, `start/end_date`, `final_score`, `closed_at`; **one active cycle per org** (partial unique index) |
corpus/services/ai-readiness.md:206-206  [R3 names a reachable artifact]  | `ai_readiness_steps` | org's ordered 3-step plan | `step_type`, `position`; default = all 3 canonical if no rows |
corpus/services/ai-readiness.md:207-207  [R3 names a reachable artifact]  | `ai_readiness_skills` | org's AI-skill set | `node_id` (taxonomy), `weight` (1.0 core / 0.5 enabling) — Step 1 scoring |
corpus/services/ai-readiness.md:208-208  [R3 names a reachable artifact]  | `ai_readiness_sims` | org's sim registry | `step_type` (simulation/interview), `sim_ref` (Directus sim id or `PLACEHOLDER-{slug}`) — Steps 2/3 |
corpus/services/ai-readiness.md:209-209  [R3 names a reachable artifact]  | `ai_readiness_user_step_progresses` | per-(org,user,step) progress (**plural** — the ent-generated table name; M219) | `status`, `completed_at` |
corpus/services/ai-readiness.md:210-210  [R3 names a reachable artifact]  | `ai_readiness_live_snapshots` | **live** per-member score (mutable, upserted) | `score/knowledge/usage/archetype/stage/...`; exposed to Talk-to-Data SQL |
corpus/services/ai-readiness.md:211-211  [R4 default — presumed citable]  | `ai_readiness_snapshots` | **frozen** per-(cycle,user) snapshot at close (immutable) | `frozen_*` mirror of live |
corpus/services/ai-readiness.md:212-212  [R4 default — presumed citable]  | `ai_readiness_diagnose_narratives` | persisted per-member LLM narrative | keyed `(org,user,cycle_ref,lang)` + `signals_hash` |
corpus/services/ai-readiness.md:213-213  [R3 names a reachable artifact]  | `ai_readiness_text_translations` | content-addressed translation cache | `source_hash`+`lang` |
corpus/services/ai-readiness.md:214-214  [R4 default — presumed citable]  | `ai_readiness_recommendations` | per-member recommended actions (the What-to-do-next drawer's "Recommended actions") | **was missing from this doc until M219**; the demo seeds **0 rows** — the live read derives `people[].diagnosis.recommendations` instead |
corpus/services/ai-readiness.md:217-217  [R4 default — presumed citable]  | `ai_readiness_notification_optouts` (**net-new, M400/M403**; plural — the ent-generated table name) | per-member unsubscribe | the reminder-cadence opt-out |
corpus/services/ai-readiness.md:218-218  [R4 default — presumed citable]  | `organization_settings` (existing) | the enablement gate | `setting='ai_readiness'`, `is_enabled` |
corpus/services/ai-readiness.md:237-237  [R3 names a reachable artifact]  | `catalog_kpis[]` `{id, value}` | **"How they use AI — at a glance"** (5 tiles) | ids `avg_adoption` / `avg_transformation` / `avg_originality` / `avg_depth` / `avg_ownership` (Adoption / Transformation / Originality / Depth·Creation / Critical ownership), each a **0-100 cohort average** (the M219 ids `avg_frequency` / `avg_breadth` / `avg_context_fit` were retired in the post-M246 platform rename — see *part 5* of § *The FILLED-ness contract* below).
corpus/services/ai-readiness.md:237-237  [R4 default — presumed citable]  `usageDimensionsFromReports` **omits** any KPI that is absent or non-numeric — **omitting all of them is why the tile row did not render at all**, rather than rendering empty. |
corpus/services/ai-readiness.md:239-239  [R4 default — presumed citable]  | `narrative.unexpected[]` | **Unexpected angles** | **NO chart fallback exists** — the narrative is the only way this column can *ever* render. |
corpus/services/ai-readiness.md:240-240  [R3 names a reachable artifact]  | `narrative.insights[]` where `category` contains **`"risk"`** | **What holds them back** | The category string is **load-bearing**: `holdsBackFromInsights` filters on it. Get it wrong and the column silently empties again. |
corpus/services/ai-readiness.md:241-241  [R3 names a reachable artifact]  | `catalog_charts[]` `top_concerns` / `top_unexpected` | the **no-narrative fallbacks** | Back What-holds-them-back / Strengths when the LLM narrative is absent.
corpus/services/ai-readiness.md:256-256  [R4 default — presumed citable]  `session_count` is the true number of seeded interviews;
corpus/services/ai-readiness.md:259-262  [R4 default — presumed citable]  The narrative prose itself is **code-owned demo copy** (like `aiReadinessInterviewPrompts`) — what a real aggregation LLM would have synthesised.
corpus/services/ai-readiness.md:259-262  [R3 names a reachable artifact]  Fenced by `ai_readiness_interview_report_test.go`, which decodes the seeded row **through the platform's own contract** (transcribed structs), because *"the seeder wrote a row"* is not the proposition that matters — *"the row makes the four blocks render"* is.
corpus/services/ai-readiness.md:275-279  [R3 names a reachable artifact]  **REST/workforce API** (`app/internal/web/backend/api/`): `GET /api/workforce/ai-readiness` (→ the `AIReadinessResponse` aggregate the manager dashboard consumes), `/cycles` (GET/POST) **+ `/cycles/{cycleID}` (GET) + `/cycles/{cycleID}/close` (POST)**, `/steps-completion`, `/narrative` (POST, LLM diagnosis), `/compare`, `/export.csv`, and **net-new at the M247-refresh refactor: `/setup` (GET status + POST one-click provision)** plus the **M407/M408 email-preview + email-override admin** endpoints.
corpus/services/ai-readiness.md:280-281  [R4 default — presumed citable]  **Background:** live-snapshot refresh (`RefreshLiveSnapshots`, worker task) **+ the net-new auto-close scheduler `CloseDueAIReadinessCycles`** (sweeps active cycles past `end_date`) + the notification workers (reminder/digest).
corpus/services/ai-readiness.md:285-286  [R4 default — presumed citable]  The refactor did more than move files — it consolidated several net-new platform subsystems that had **no prior corpus coverage**.
corpus/services/ai-readiness.md:285-286  [R3 names a reachable artifact]  Documented here as platform facts; the demo-seeder consequences are v2.7 **M250** work.
corpus/services/ai-readiness.md:339-339  [R4 default — presumed citable]  | **Manager** | **`AIReadinessClient`** — HeroCard (org score + dominant archetype + **Steps-Completion %**) + tabs **Snapshot** (archetype matrix + donuts + by-tag), **How-we-measure** (3-step ribbon + skill strengths/gaps + sims + **interview findings**), **What-to-do-next** (archetype action groups + per-person **Diagnose** drawer).
corpus/services/ai-readiness.md:341-341  [R3 names a reachable artifact]  | **Employee** | `AIReadinessHero` (the 3-step funnel; modes new/progress/done/archived) + `AIReadinessRailCard`.
corpus/services/ai-readiness.md:367-368  [R4 default — presumed citable]  The `(new)` in the legacy path is a Next.js **route group** for the workforce refactor — *not* a version marker.
corpus/services/ai-readiness.md:396-400  [R4 default — presumed citable]  Enable for an org by inserting the `organization_settings` row (`setting='ai_readiness', is_enabled=true`) + `flag_ai_readiness` on in PostHog (or the local flag shim).
corpus/services/ai-readiness.md:396-400  [R3 names a reachable artifact]  The member flow then needs `ai_readiness_skills` + `ai_readiness_sims` config + an active cycle; the dashboard reads `GET /api/workforce/ai-readiness`.
corpus/services/ai-readiness.md:396-400  [R3 names a reachable artifact]  Tests: `app/internal/aireadiness/*_test.go` (the scoring/steps/cycle suites moved with the package; Postgres harness `testdb_test.go` via `internal/testsupport/pgtest`).
corpus/services/ai-readiness.md:404-407  [R3 names a reachable artifact]  To make a **200-person demo org** show the AI-readiness manager dashboard **enabled**, with **78.4% (≈156 of the 199 frozen snapshots) having completed all 3 steps** (the shipped figure — see `seeding-spec.md`; this supersedes the earlier round "~80%/≈160" contract prose), plus **one hero "started"** and **one hero "completed"** — the seeder writes:
corpus/services/ai-readiness.md:410-410  [R4 default — presumed citable]  `organization_settings` (`setting='ai_readiness'`, `is_enabled=true`) — the gate.
corpus/services/ai-readiness.md:411-411  [R4 default — presumed citable]  `ai_readiness_steps` × 3 (skill_mapping/simulation/interview, positions 0/1/2) — optional (canonical default if absent).
corpus/services/ai-readiness.md:417-426  [R3 names a reachable artifact]  `ai_readiness_sims` × **3 track-keyed** (M250; was 2) — **two distinct-track simulations** (tech=`who-can-see-this-document-fc0`, business=`use-ai-to-turn-survey-data-into-a-leadership-email`) **+ one shared interview** (`ai-readiness-interview-d62`), each pinned to the platform's real default sim uuid, with the `track` column WRITTEN (the schema's `UNIQUE(org, step_type, track)` requires the tech+business pair — both `step_type=simulation` — to carry DISTINCT tracks).
corpus/services/ai-readiness.md:435-438  [R3 names a reachable artifact]  **Per-member (≈156 of 199 "completed"):** the underlying signals (≥1 `user_skill_evidences` for a configured skill; jobsim sessions for steps 2/3) **+** `ai_readiness_user_step_progresses` (3× `completed`) **+** an `ai_readiness_live_snapshots` upsert (`score≈100, stage=3, archetype` per the score).
corpus/services/ai-readiness.md:440-445  [R3 names a reachable artifact]  **⚠️ A hero's stage is DERIVED FROM HER PERSONA KIND — and stage 0 was UNREACHABLE for an end-user until v2.8 M256 iter-26.** `aiReadinessStageFor` maps a hero: **manager → 0** (she reads the dashboard, she is not a funnel row), **struggling → 1**, and **everything else → 3**.
corpus/services/ai-readiness.md:447-450  [R3 names a reachable artifact]  **`Persona.AIReadiness`** — `""` (derive from trajectory, unchanged) | **`"not_started"`** (funnel **stage 0**: no evidence, no sessions, no progress row, no snapshot — `seedAIReadinessOrgFunnel` skips her entirely).
corpus/services/ai-readiness.md:456-457  [R4 default — presumed citable]  An end-user hero must still declare `verified > 0` (`blueprint.validate()`), which is the same point from the other side: the field does not make a hero **blank**, it makes her **not-yet-diagnosed**.
corpus/services/ai-readiness.md:512-512  [R3 names a reachable artifact]  **cycle-scoped** — `opts.CycleID != nil` AND that cycle's `status == "closed"` (`:290-297`);
corpus/services/ai-readiness.md:513-514  [R3 names a reachable artifact]  **default (`CycleID == nil`)** — when the org has **no active cycle** and a **latest closed cycle** exists (`:307-312`).
corpus/services/ai-readiness.md:513-514  [R3 names a reachable artifact]  Only an org with neither falls through to `buildLiveResponse` (`:314`).
corpus/services/ai-readiness.md:579-579  [R3 names a reachable artifact]  | the **STARTED** hero | 1 / 3 | `progress` | **NOTHING.** `AIReadinessHero.tsx:88` `if (!air.deadline) return null;` |
corpus/services/ai-readiness.md:581-584  [R4 default — presumed citable]  The active cycle's `end_date` **must be in the future** (it *is* the member deadline) and its `participants_filter` must stay `{"all":true}` (a tag-scoped cycle returns a nil deadline for anyone outside the tags, silently re-hiding the surface for most of the org).
corpus/services/ai-readiness.md:589-592  [R4 default — presumed citable]  **What the manager then reads — and why that is the point.** With an active cycle present, `AIReadinessClient`'s `activeCycle?.id ?? latestClosedCycle?.id` resolves the **active** id, so the dashboard takes `buildLiveResponse`.
corpus/services/ai-readiness.md:596-596  [R3 names a reachable artifact]  | `howWeMeasure.interview` — **null** → present | the whole **Step-3** block + **"How they use AI"** + **"What holds them back"** + **"Strengths"** + **"Unexpected angles"** |
corpus/services/ai-readiness.md:600-602  [R3 names a reachable artifact]  **Reported, not gated** — the milestone that owns login speed is a different one. Both paths fill `org.*`, `byTeam` (13), `people` (199), `howWeMeasure.{steps,skillInsights,simulations,cycleTotals}`.
corpus/services/ai-readiness.md:619-621  [R4 default — presumed citable]  **Full marks require holding EVERY configured skill.** The denominator is the whole repertoire, by design — "a larger configured set makes a full score harder to reach" (the platform's own comment).
corpus/services/ai-readiness.md:634-643  [R3 names a reachable artifact]  The two readiness refs now come from a **reserved tail** of the sims pool that no general picker can draw (`contentref.go`), making the sets **provably disjoint**.
corpus/services/ai-readiness.md:669-674  [R3 names a reachable artifact]  **Also (a latent hazard closed while scaling #1):** the funnel's Step-1 evidence UPSERT is now **presence-preserving** — on a conflict with a row the verified-skill chain already wrote it asserts only that the row exists and is verified, and leaves `level` / `anthropos_level` / `user_level` alone.
corpus/services/ai-readiness.md:669-674  [R4 default — presumed citable]  Step 1 is presence-based (`queryUserAISkills` selects only `user_id, skill_id, is_verified`), so preserving is both correct and safer: with a member now mapping up to **31** skills (M250), the old clobbering upsert would have let the readiness seeder quietly restate a hero's claimed-vs-verified gap.
corpus/services/ai-readiness.md:688-696  [R4 default — presumed citable]  **5. Interview-findings must key on the CURRENT KPI ids (M250 iter-07).** The manager's "interview findings" tiles key on the platform's live `usageDimSpecs` — **avg_adoption / avg_transformation / avg_originality / avg_depth / avg_ownership** (Adoption / Transformation / Originality / Depth·Creation / Critical ownership).
corpus/services/ai-readiness.md:688-696  [R3 names a reachable artifact]  The M219 seeder wrote the now-retired ids `avg_frequency / avg_breadth / avg_context_fit`; only `avg_depth` survived the post-M246 platform rename, so 4 of 5 tiles rendered empty.
corpus/services/askengine.md:3-5  [R3 names a reachable artifact]  **Not a standalone service — a domain of the `app` monolith** (the service the platform calls "backend").
corpus/services/askengine.md:3-5  [R3 names a reachable artifact]  Code lives in `app/internal/askengine/` (the engine) + `app/internal/web/backend/ask/` (the HTTP/SSE handler + agentic loop).
corpus/services/askengine.md:31-32  [R3 names a reachable artifact]  **Codebase**: `app/internal/askengine/` (pure SQL/prompt/LLM layers) + `app/internal/web/backend/ask/` (the Echo handler, agentic loop, conversation store).
corpus/services/askengine.md:31-32  [R3 names a reachable artifact]  Go, part of the `app` monolith.
corpus/services/askengine.md:33-38  [R4 default — presumed citable]  **Database**: PostgreSQL.
corpus/services/askengine.md:33-38  [R3 names a reachable artifact]  Reads run against a **dedicated, small read pool** (`COPILOT_DB_CONN` DSN, `askEngineMaxConns=6`) so interactive queries can't be starved by heavy dashboard analytics; each query runs in a **read-only tx with `SET LOCAL statement_timeout=10s`**.
corpus/services/askengine.md:68-73  [R3 names a reachable artifact]  **The agentic loop** (`handler.go:runAgenticLoop`, `maxAgenticIterations=15`, `loopTimeout=10min`) runs in a **detached goroutine** (`context.WithoutCancel` + timeout) so a client disconnect doesn't abort generation; a per-conversation `streamRegistry` lets the client reattach.
corpus/services/askengine.md:68-73  [R3 names a reachable artifact]  Per iteration: `bedrock.Stream` → accumulate blocks → if `stop_reason != tool_use` finish, else `executeTool` each tool (`query_postgres`→`Engine.Execute`, `render_chart`, `course_builder`) and feed back the result.
corpus/services/askengine.md:68-73  [R4 default — presumed citable]  Wire results are sanitized (`publicToolResult` strips raw error text).
corpus/services/askengine.md:115-115  [R3 names a reachable artifact]  **Command**: `go run .` in the app repo, or `make up` (ships inside the `backend` container).
corpus/services/askengine.md:116-118  [R3 names a reachable artifact]  **Key env vars**: `ASK_MODEL_ID` (default `eu.anthropic.claude-sonnet-4-6`), `AWS_REGION` (default `eu-west-1`), `COPILOT_DB_CONN` (the read-pool DSN), the standard AWS creds, and `AZURE_OPENAI_ENDPOINT_URL`/`AZURE_OPENAI_KEY` (embeddings).
corpus/services/askengine.md:121-121  [R3 names a reachable artifact]  **Service Name**: none of its own — part of the `backend` service (`docker compose up -d backend`).
corpus/services/askengine.md:127-128  [R3 names a reachable artifact]  `sandbox_test.go` (49 tests, the bulk) — adversarial/security: each test names the specific SQL-injection or CTE-bypass vector it closes.
corpus/services/askengine.md:129-129  [R3 names a reachable artifact]  `executor_test.go` (13) — `crossValidate` sanity checks + cell normalization.
corpus/services/askengine.md:130-131  [R3 names a reachable artifact]  `prompt_test.go` (10) — workforce-vs-hiring framing, dynamic blocks, and **`RulesBlockIsIdentical`** (prompt-cache prefix stability).
corpus/services/askengine.md:132-135  [R3 names a reachable artifact]  `followups_test.go` (14) — the streaming `<followups>` extractor edge cases.
corpus/services/askengine.md:132-135  [R4 default — presumed citable]  No real Bedrock is hit in unit tests — the SQL/prompt/followups layers are pure and table-driven.
corpus/services/askengine.md:132-135  [R3 names a reachable artifact]  (The handler package `internal/web/backend/ask/` carries its own tests: sanitize, stream_registry, examples, lessons, distill, feedback, store, embed.)
corpus/services/askengine.md:139-140  [R3 names a reachable artifact]  **Shipped v1.267.0 (2026-05-07)** — the initial "Talk to Data" with SSE streaming + conversation management + the Bedrock client + the SQL sandbox.
corpus/services/backend.md:110-110  [R4 default — presumed citable]  **There is no `SkillPathSessionService`** — measured: **0** occurrences in Go source, and no `skillpath…v1connect` package is imported.
corpus/services/backend.md:113-113  [R3 names a reachable artifact]  **HTTP** endpoints on port 8082 (local; 8080 in production) for webhooks and miscellaneous integrations — including `POST /api/webhook/directus`, which **fails closed** without `DIRECTUS_WEBHOOK_SECRET`
corpus/services/backend.md:117-117  [R3 names a reachable artifact]  **Talk to Data** (`internal/askengine`) — SSE-streaming natural-language Q&A over the platform's data, powered by Bedrock (Anthropic) with a SQL-validation sandbox.
corpus/services/backend.md:126-126  [R3 names a reachable artifact]  **AI usage / cost tracking** (`internal/aiusage`) — central ledger driven by the `AI` Redis Stream
corpus/services/backend.md:133-136  [R3 names a reachable artifact]  The standalone `skiller` microservice was **merged into `app`** (July 2026).
corpus/services/backend.md:133-136  [R3 names a reachable artifact]  Verified 2026-07-08 against the re-synced stack-dev clone (`app@c3c45e01` v1.334.1, `platform@0808b92`), a live containerized bring-up + migrate, and read-only prod.
corpus/services/backend.md:225-229  [R3 names a reachable artifact]  **Clean-bring-up prerequisite:** the merged migrations create the taxonomy vector columns as `extensions.vector(1536)` and a GIN-trigram index via `extensions.gin_trgm_ops`, so the **`extensions` schema (pgvector + `pg_trgm`) must be bootstrapped before `make migrate`** on a clean DB — else app `20260518125439` and cms `20250116133510` fail with `schema "extensions" does not exist`.
corpus/services/backend.md:244-244  [R3 names a reachable artifact]  **Codebase**: `app` (local) — repo `git@github.com:anthropos-work/app`
corpus/services/backend.md:247-247  [R3 names a reachable artifact]  **Ports**: 8082 (HTTP/GraphQL — `PORT`), 8083 (Connect-RPC — `RPC_PORT`), 8084 (meta/health — `META_PORT`).
corpus/services/backend.md:319-319  [R3 names a reachable artifact]  **Bedrock task role policy statements** (v1.267.1): IAM additions for Bedrock model access from the prod ECS task role.
corpus/services/backend.md:326-326  [R3 names a reachable artifact]  **GraphQL Federation**: schemas at `internal/web/backend/graphql/graph/schemas/*.graphqls`.
corpus/services/backend.md:326-326  [R3 names a reachable artifact]  Federated into the supergraph as the `backend` subgraph — **the only one left**, and on a local stack the frontends now reach it directly at `:8082/graphql/query` rather than through a router.
corpus/services/backend.md:333-333  [R4 default — presumed citable]  Hiring App
corpus/services/backend.md:334-334  [R4 default — presumed citable]  Mobile App
corpus/services/backend.md:335-335  [R4 default — presumed citable]  Studio-Desk (for org-level metadata)
corpus/services/backend.md:341-341  [R3 names a reachable artifact]  **Directus** (`content.anthropos.work`) — the external content edge read by the in-process cms domain
corpus/services/backend.md:342-342  [R3 names a reachable artifact]  **Judge0** — sandboxed code execution, called directly (`JUDGE0_BASE_URL`) since roadrunner merged in
corpus/services/backend.md:343-343  [R4 default — presumed citable]  **LiveKit / AWS Chime** — simulation voice + recording, for the in-process jobsim engine
corpus/services/backend.md:344-344  [R4 default — presumed citable]  **Gotenberg** — Office → PDF conversion
corpus/services/backend.md:351-351  [R3 names a reachable artifact]  The one external producer left is **Directus**, whose webhooks feed the `cms` stream
corpus/services/backend.md:352-352  [R4 default — presumed citable]  Each stream has exactly **one** subscriber with multiple handlers merged via `.AddHandler(...)` — colony keys by stream name, so a second `AddSubscriber` for the same stream silently overwrites the first
corpus/services/backend.md:376-376  [R3 names a reachable artifact]  You'll need `platform/.env` reachable (or copy relevant vars).
corpus/services/backend.md:376-376  [R4 default — presumed citable]  The infra services should still run via Docker.
corpus/services/clerkenstein.md:20-25  [R3 names a reachable artifact]  **One monorepo, two clone roles.** `rosetta-extensions` is ONE private monorepo with sections (`clerkenstein`, `demo-stack`, `stack-injection`, `stack-core`, `stack-seeding`, `alignment`).
corpus/services/clerkenstein.md:94-95  [R3 names a reachable artifact]  The `clerkenstein/` section is organised **one dir per mocked dependency** (M2b reorg, decision M2b-D2) — every dir below is a subdir of `rosetta-extensions/clerkenstein/`, not a repo root:
corpus/services/clerkenstein.md:106-106  [R3 names a reachable artifact]  | `alignment/` | — | the measurement harness: `cmd/{clerkrun,jsfapirun,multirun,expressrun,deployrun}` + `dna/` (five) + `golden{,-js,-multi,-express,-deploy}/` + `scripts/` |
corpus/services/clerkenstein.md:243-248  [R4 default — presumed citable]  **The align-safety rule this pins — CONDITIONAL-EMIT (#M224-D-align).** A new FAPI field is emitted **only when its non-default value applies** (`if u.OrgIsHiring { pm["isHiring"] = true }`, else omit).
corpus/services/clerkenstein.md:243-248  [R3 names a reachable artifact]  Conditional-emit keeps every non-hiring org's `public_metadata` **byte-identically `{eid}`** — generalizing the `Picture`/`OrgLogo` `omitempty` pattern to non-string additions.
corpus/services/clerkenstein.md:249-251  [R3 names a reachable artifact]  Roster 2/2) — GREEN, no identity gene perturbed (the named `SessionToken/decoded-identity` critical/exact gene unaffected).
corpus/services/clerkenstein.md:252-255  [R4 default — presumed citable]  Optional, only if a server-side consumer ever reads `organization.publicMetadata.isHiring`.
corpus/services/clerkenstein.md:346-349  [R3 names a reachable artifact]  **The bounded patch tail** rides the **existing** sha-pinned mechanism (never a canonical repo edit): ant-academy's `next dev` `allowedDevOrigins` admits the MagicDNS host (the `ant-academy-dev-origins` demo-patch, env-var indirection so the post-hash stays fixed), and studio-desk's SPA `VITE_CLERK_SIGN_IN_URL` bakes via a gitignored `.env.production.local` overlay (no Dockerfile ARG).
corpus/services/next-web-app.md:21-21  [R3 names a reachable artifact]  **Codebase**: `next-web-app` (local) — repo `git@github.com:anthropos-work/next-web-app`
corpus/services/next-web-app.md:35-35  [R3 names a reachable artifact]  | **Mobile** | `@anthropos/mobile` | 3031 (Expo) | Expo / React Native PoC (**paused**); **excluded** from the pnpm workspace, uses `EXPO_PUBLIC_*` | ❌ |
corpus/services/next-web-app.md:67-67  [R3 names a reachable artifact]  | `@anthropos/graphql` | All data fetching: queries, React Query hooks by domain, server fetchers, **codegen output** (`src/__generated__`), `codegen.ts` |
corpus/services/next-web-app.md:69-69  [R3 names a reachable artifact]  | `@anthropos/core-js` | Shared constants, types, utils (e.g. `security/printToken`) |
corpus/services/next-web-app.md:76-76  [R3 names a reachable artifact]  **Observability proxies**: `/logpoint/*` → PostHog (EU); `/monitoring` tunnels Sentry/Better Stack events.
corpus/services/next-web-app.md:81-81  [R3 names a reachable artifact]  **Upstream**: end users / browsers; WordPress (embeds `apps/integration`); Vercel (prod hosting); platform compose service `next-web-app` (containerized Workforce variant).
corpus/services/next-web-app.md:102-103  [R3 names a reachable artifact]  Older Node fails with `WARN Unsupported engine`, and pnpm refuses to wipe `node_modules` in non-TTY shells (`ERR_PNPM_ABORTED_REMOVE_MODULES_DIR_NO_TTY`).
corpus/services/next-web-app.md:119-127  [R3 names a reachable artifact]  The repo's own `Dockerfile.dev` (Node 24 alpine — it is the only Dockerfile in the repo) builds **only** `@anthropos/web-app` (`pnpm turbo build --filter=@anthropos/web-app`), and `apps/web` is the only frontend in platform compose.
corpus/services/next-web-app.md:119-127  [R4 default — presumed citable]  Integration / maintenance / mobile ship via Vercel only.
corpus/services/next-web-app.md:119-127  [R3 names a reachable artifact]  `NEXT_PUBLIC_*` are baked at **build time**; on a remote VM set `PUBLIC_HOST` in `platform/.env` so the client bundle resolves the right hostname.
corpus/services/next-web-app.md:135-135  [R3 names a reachable artifact]  | `NEXT_PUBLIC_BACKEND_API_URL` | `http://localhost:8082` | Backend (`app`) API base URL |
corpus/services/next-web-app.md:136-136  [R3 names a reachable artifact]  | `GRAPHQL_SCHEMA_FOR_GEN` | `http://localhost:8082/graphql/query` | Schema endpoint used by `graphql-codegen` (was `:5050/graphql`) |
corpus/services/next-web-app.md:138-138  [R3 names a reachable artifact]  | `NEXT_PUBLIC_POSTHOG_KEY` / `_HOST` · `NEXT_PUBLIC_SENTRY_DSN` / `SENTRY_AUTH_TOKEN` | — | Analytics + error tracking (PostHog EU, Sentry/Better Stack) |
corpus/services/next-web-app.md:140-140  [R3 names a reachable artifact]  | `BUNNY_*` · `METABASE_*` · `PRINT_ROUTE_SECRET` | — | CDN, embedded analytics, signed PDF/print routes |
corpus/services/sentinel.md:7-7  [R4 default — presumed citable]  Sentinel only answers *"is this subject allowed to perform this action on this object?"*.
corpus/services/sentinel.md:11-11  [R3 names a reachable artifact]  **Codebase**: `sentinel` (local) — repo `git@github.com:anthropos-work/sentinel`
corpus/services/sentinel.md:14-14  [R3 names a reachable artifact]  **Database**: PostgreSQL `sentinel` schema (single table: `casbin_rules`) — **no Ent ORM**
corpus/services/sentinel.md:16-16  [R4 default — presumed citable]  **Profile**: always on (no `profiles:` declared in compose — runs with every `make up`)
corpus/services/sentinel.md:17-17  [R4 default — presumed citable]  **No outbound RPC** to other platform services — sentinel is a leaf
corpus/services/sentinel.md:18-18  [R4 default — presumed citable]  **No Redis, no GraphQL, no background workers** — stateless request/response only
corpus/services/sentinel.md:30-30  [R3 names a reachable artifact]  | `m` | User-tier quota | A user passes if they are in the policy's tier (`g(user, tier)`) OR the policy tier is `TIER_FREE` (free-tier policies act as an unconditional baseline, substituted from the proto enum `Tier_TIER_FREE`), AND the requested `count` <= the tier `max`. |
corpus/services/sentinel.md:39-39  [R3 names a reachable artifact]  `g(user, tier)` — `TIER_FREE` / `TIER_PREMIUM`
corpus/services/sentinel.md:73-73  [R4 default — presumed citable]  | `CheckFeature` | User tier quota check |
corpus/services/sentinel.md:74-74  [R4 default — presumed citable]  | `CheckOrganizationFeature` | Org-level feature quota check |
corpus/services/sentinel.md:75-75  [R3 names a reachable artifact]  | `AddUserToTier` / `RemoveUserFromTier` | Manage user tier groupings |
corpus/services/sentinel.md:76-76  [R3 names a reachable artifact]  | `GetQuotas` / `GetOrganizationQuotas` | Read policy quotas |
corpus/services/sentinel.md:77-77  [R3 names a reachable artifact]  | `OrgAddUserToRole` / `OrgRemoveUserFromRole` / `OrgReplaceUserRole` | Manage org role assignments |
corpus/services/sentinel.md:78-78  [R3 names a reachable artifact]  | `OrgClearAll` | Remove all `g2` + `g3` policies for an org |
corpus/services/sentinel.md:79-79  [R4 default — presumed citable]  | `OrgCheckPermission` | Legacy org action check (read-only) |
corpus/services/sentinel.md:80-80  [R3 names a reachable artifact]  | `OrgAllowUserToUseFeature` / `OrgDisallowUserToUseFeature` | Manage `g3` membership feature access |
corpus/services/sentinel.md:81-81  [R4 default — presumed citable]  | `OrgMembershipsAllowedToUseFeature` | List memberships with feature access |
corpus/services/sentinel.md:82-82  [R3 names a reachable artifact]  | `OrgGetOrganizationFeatureCredits` / `OrgSetOrganizationFeatureCredits` | Manage org feature credit budgets |
corpus/services/sentinel.md:90-90  [R3 names a reachable artifact]  **Downstream**: PostgreSQL (`sentinel` schema, table `casbin_rules`)
corpus/services/sentinel.md:91-91  [R4 default — presumed citable]  **No outbound RPC** to other platform services
corpus/services/sentinel.md:101-101  [R4 default — presumed citable]  Sentinel is always part of any `make up` (no profile gate).
corpus/services/sentinel.md:101-101  [R4 default — presumed citable]  To restart just sentinel:
corpus/services/sentinel.md:125-125  [R3 names a reachable artifact]  It works only against a local Postgres on port 5432, and relies on `init_policy.sql` being schema-qualified (`sentinel.casbin_rules`) so the seed lands in the right schema regardless of search_path.
corpus/services/sentinel.md:125-125  [R4 default — presumed citable]  For a non-local DB, run psql with your own DSN: `psql "$DB_URL" -f init_policy.sql`.
corpus/services/sentinel.md:150-150  [R3 names a reachable artifact]  | `DB_CONNECTION` | yes | — | PostgreSQL DSN with `search_path=sentinel` |
corpus/services/sentinel.md:151-151  [R3 names a reachable artifact]  | `PORT` | no | `8087` | HTTP + Connect-RPC port (compose sets this explicitly; binary default is 8080) |
corpus/services/sentinel.md:152-152  [R3 names a reachable artifact]  | `ENVIRONMENT` | no | — | Environment name |
corpus/services/sentinel.md:153-153  [R3 names a reachable artifact]  | `SERVICE_NAME` | no | `sentinel` | Logging label |
corpus/services/sentinel.md:154-154  [R3 names a reachable artifact]  | `SENTRY_DSN` | no | — | Sentry error tracking |
corpus/services/sentinel.md:158-158  [R4 default — presumed citable]  **Hot reload**: changes made directly to the `casbin_rules` table (e.g. via a migration or a manual fix) need a `Reload` RPC call to take effect.
corpus/services/sentinel.md:158-158  [R4 default — presumed citable]  Changes made through Sentinel's own RPC methods (`OrgAddUserToRole` etc.) are picked up immediately.
corpus/services/storage.md:5-5  [R4 default — presumed citable]  Storage is the **centralized file/blob service** for the platform.
corpus/services/storage.md:105-105  [R3 names a reachable artifact]  Namespaces are arbitrary strings (e.g., `jobsimulation`, `assets`).
corpus/services/storage.md:135-135  [R3 names a reachable artifact]  | `PutObject` | `data`, `metadata`, `namespace` | `key (UUID)`, `namespace` |
corpus/services/storage.md:136-136  [R3 names a reachable artifact]  | `GetObject` | `key`, `namespace` | `object (data + metadata)` |
corpus/services/storage.md:137-137  [R3 names a reachable artifact]  | `GetPresignedUrl` | `key`, `namespace`, `expiry_seconds` | `url` (default 15 min) |
corpus/services/storage.md:143-143  [R3 names a reachable artifact]  | `PutPublicObject` | `data`, `metadata`, `namespace` | `key (UUID)`, `namespace` |
corpus/services/storage.md:144-144  [R3 names a reachable artifact]  | `GetPublicObject` | `key`, `namespace` | `object (data + metadata)` |
corpus/services/storage.md:177-177  [R4 default — presumed citable]  **No outbound RPC** to other platform services — storage is a leaf
corpus/services/storage.md:209-209  [R4 default — presumed citable]  The `storage sync` CLI moves objects between two configured backends (e.g., local FS → S3 for an initial seed):
corpus/services/storage.md:226-226  [R3 names a reachable artifact]  | `PORT` | `8300` | HTTP health port (binary default 8080, overridden in compose) |
corpus/services/storage.md:227-227  [R3 names a reachable artifact]  | `RPC_PORT` | `8301` | Connect-RPC port (binary default 8081, overridden in compose) |
corpus/services/storage.md:230-230  [R3 names a reachable artifact]  | `AWS_REGION` / `AWS_DEFAULT_REGION` | `eu-west-1` | AWS region (EU-first) |
corpus/services/storage.md:232-232  [R3 names a reachable artifact]  | `SERVICE_NAME` | `storage` | Logging label |
corpus/services/storage.md:233-233  [R3 names a reachable artifact]  | `SENTRY_DSN` | (empty) | Sentry error tracking |
corpus/services/studio-room.md:18-18  [R4 default — presumed citable]  Takes a simulation blueprint (created in Studio-Desk)
corpus/services/studio-room.md:25-25  [R3 names a reachable artifact]  Since **cms-in-app v8.0** it is embedded inside the **`app` (backend) container** — pulled into the image by CI (`additional_repo`, app v1.360.1) — and is triggered by an Asynq task rather than run directly by users; the Go side shells out to it as a subprocess when a generation job is enqueued.
corpus/services/studio-room.md:25-25  [R3 names a reachable artifact]  Before the merge it rode in the cms container at `cms/studio/`.
corpus/services/studio-room.md:36-36  [R4 default — presumed citable]  | **AI Providers** | OpenAI, Azure OpenAI, Anthropic |
corpus/services/studio-room.md:37-37  [R3 names a reachable artifact]  | **Repository** | `anthropos-studio-room`, pulled into the **`app`** image by CI (was cloned into the cms repo at `cms/studio/` before cms-in-app) |
corpus/services/studio-room.md:116-119  [R4 default — presumed citable]  **AI Setup**: - Initialize AI services (OpenAI, Azure, Anthropic) - Configure model parameters (temperature, max tokens, thinking mode) - Set up usage tracking
corpus/services/studio-room.md:123-123  [R4 default — presumed citable]  The generation is orchestrated by **media-specific generator agents** (e.g., `SimulationGenerator`).
corpus/services/studio-room.md:143-143  [R4 default — presumed citable]  **Generation Modes** (`GenMode` enum):
corpus/services/studio-room.md:144-144  [R3 names a reachable artifact]  `GenMode.FAST`: Lightweight / low-latency steps
corpus/services/studio-room.md:145-145  [R3 names a reachable artifact]  `GenMode.STRICT`: Tightly-constrained, deterministic output
corpus/services/studio-room.md:146-146  [R3 names a reachable artifact]  `GenMode.EXECUTION`: Default mode (`DEFAULT = EXECUTION`)
corpus/services/studio-room.md:147-147  [R3 names a reachable artifact]  `GenMode.CREATIVE`: For content requiring creativity (dialogue, scenarios)
corpus/services/studio-room.md:148-148  [R3 names a reachable artifact]  `GenMode.REASONING`: For steps that benefit from deeper reasoning
corpus/services/studio-room.md:152-152  [R4 default — presumed citable]  **Retry mechanism**: Auto-retry failed steps (configurable `max_retries`)
corpus/services/studio-room.md:164-164  [R3 names a reachable artifact]  `--media`, `--simid`, and `--target` are all required for `postgen.py`.
corpus/services/studio-room.md:172-172  [R3 names a reachable artifact]  | `translation` | Translate to multiple languages | `internal_localization.json` inside the bundle |
corpus/services/studio-room.md:251-251  [R3 names a reachable artifact]  AI models are configured per generation mode in `configs/{env}_config.ini`.
corpus/services/studio-room.md:317-317  [R3 names a reachable artifact]  Generation state is checkpointed after each step to the configured `worklog_path` (set to `workspace/trace/` in the shipped configs; the in-code fallback is a literal `worklog/` only if unset).
corpus/services/studio-room.md:319-319  [R3 names a reachable artifact]  `{simid}_pre_generation.json` — the original request
corpus/services/studio-room.md:322-322  [R3 names a reachable artifact]  plus a per-run `{simid}_usage.json` usage trace.
corpus/services/studio-room.md:336-336  [R3 names a reachable artifact]  Python 3.9+ (runtime image when embedded in the `app` container: `python:3.11-slim`; it was the cms container before cms-in-app)
corpus/services/studio-room.md:337-337  [R4 default — presumed citable]  AI API keys (OpenAI, Anthropic, or Azure)
corpus/services/studio-room.md:351-351  [R3 names a reachable artifact]  **Requirements** (unpinned in `requirements.txt`):
corpus/services/studio-room.md:431-431  [R4 default — presumed citable]  Receives a GraphQL mutation requesting generation
corpus/services/studio-room.md:434-434  [R4 default — presumed citable]  Reads the resulting ZIP and imports it into Directus
corpus/services/studio-room.md:437-437  [R4 default — presumed citable]  **Input**: Blueprints created in Studio-Desk, passed in by CMS
corpus/services/studio-room.md:438-438  [R4 default — presumed citable]  **Output**: Generated content imported into Directus by CMS
corpus/services/studio-room.md:439-439  [R4 default — presumed citable]  **Workflow**: Desk designs → CMS enqueues → Room generates → CMS imports into Directus
