# adj-3 — verdicts for seats `r25-E` + `r26-E` (iter-103)

**Trees I read** (no fetch, no checkout, no write outside this file):
`stack-demo/app ad9f3c49` (+ historical `5ba17044`, `b948604f`, `1e457fa70`) ·
`stack-demo/app/studio aeec036a` (own checkout — grepped at its OWN ref, never through `app`) ·
`stack-demo/cms/studio aeec036a` · `stack-demo/platform 0c91421d` ·
`stack-demo/next-web-app 8297c684` (+ `origin/main f97ba659`) · `stack-demo/ant-academy 22df69dd` ·
`stack-demo/sentinel f2c46190` · `stack-demo/cms ca50c817` · `stack-demo/jobsimulation 462343b0` ·
`stack-demo/messenger fa47850d` · `stack-demo/storage 4ce8ece5` · `stack-demo/roadrunner 87d8d443` ·
`stack-demo/graphql-wundergraph 60c229f3` · **`stack-demo/rosetta-extensions 09d06070` — the PINNED
per-stack consumption clone, the tree I used for every tooling/clone-set claim** (the authoring copy
`.agentspace/rosetta-extensions` was not used for any verdict).
Corpus read at working-tree state, `rosetta` HEAD `bd9d40d` (the seats graded `3822501a`); every anchor
line number below resolves at the line the seat names, with the seat's quoted text byte-matching.

---

r25-E B1 | corpus/architecture/ai_architecture.md:99 | UPHELD | IN-SCOPE | "no such file `app/internal/ai/ai.go`; that path now resolves only in jobsimulation" — it exists in app
   evidence: `stack-demo/app` `git ls-tree ad9f3c49 internal/ai/` → 17 entries incl. `internal/ai/ai.go` (blob `650b259a`, 21 lines, `type AI interface`) plus `internal/ai/anthropic/` + `internal/ai/openai/`. Created by `1e457fa70` "refactor(ai): fold the ai library into app as internal/ai" (2026-08-04), an ancestor of `ad9f3c49` (2026-08-06). `internal/ai/module_import_guard_test.go:14-31` states the fold in words. The pin `@5ba17044` (2026-07-31) is real and its *file:line* half is exact (I opened `internal/jobsimulation/ai/ai.go:267,344` and `internal/skillerai/ai.go:347` at `5ba17044` — all three are `"flag_use_azure_us"`), and at `5ba17044` `internal/ai/ai.go` is genuinely absent while `jobsimulation@462343b0:internal/ai/ai.go` exists. What defeats the pin is the trailing clause's own tense: *"that path **now** resolves **only** inside the frozen `jobsimulation` husk repo"* is (a) present-tense, (b) a claim about the whole clone set, which an `app`-scoped pin cannot date, and (c) in live contradiction with `corpus/architecture/shared_libraries.md:126`, which asserts as CURRENT (and "corrected M257x iter-102") that *"`app/internal/ai/` carries the library in-tree"*. Two corpus passages assert incompatible present states; neither frames the other as retracted. NB the sentence's substantive point survives — vendor selection is NOT in `internal/ai/` (`git grep flag_use_azure_us ad9f3c49 -- 'internal/ai/*'` → 0); only the bolded existence assertion is false.

r25-E B2 | corpus/architecture/ai_architecture.md:95-96 | UPHELD | IN-SCOPE | "All Go services access AI through the shared `ai` library" — no live Go service imports it
   evidence: enumerated `github.com/anthropos-work/ai` in every `go.mod` at each clone's own HEAD across the 13 `stack-demo` git trees. Requirers: `cms@ca50c817:go.mod:9` and `jobsimulation@462343b0:go.mod:11` ONLY — the two frozen husks, neither in `repos.yml @ 0c91421d` (which lists exactly `app`, `sentinel`, `next-web-app`, `studio-desk`) and neither with a compose service. `app@ad9f3c49:go.mod` lists `analytics-go`, `colony v0.35.2`, `proto v1.210.0`, `storage v0.15.2`, `taxonomy v1.2.0` — no `ai`; `go.sum` has **0** `anthropos-work/ai` lines vs **2** at `b948604f`, where `go.mod:14` still read `ai v1.40.2`. `sentinel@f2c46190` never required it. `app`'s consumer now imports the in-tree copy: `internal/jobsimulation/ai/ai.go:13-15` @ `ad9f3c49` imports `github.com/anthropos-work/app/internal/ai{,/anthropic,/openai}`. Positive control same pass: `anthropos-work/colony` resolves in `app:15` and `sentinel:8`. The claim is unpinned and present-tense, so it grades at the checkouts.

r25-E B3 | corpus/architecture/README.md:21 | UPHELD | IN-SCOPE | index still lists `ai` among the four internal libs "imported as private modules"; live set is three
   evidence: same enumeration as B2 — the live imported set is `colony`, `proto`, `taxonomy` (three). The index sentence contradicts the very page it indexes: `corpus/architecture/shared_libraries.md:126` ("Imported by") reads **"No repo a stack builds (corrected M257x iter-102…)"** and rules the husks out explicitly — *"a `go.mod` require in a repo nothing compiles is not a live import."* Both are asserted live; neither retracts the other. I checked the strongest counter, which is the sentence's own operationalisation ("no checked-out `go.mod`/`go.sum` requires …"): under that literal test `ai` does survive, because three checked-out `go.mod`s still require it — `cms:9`, `jobsimulation:11`, and `rosetta-extensions@09d06070:stack-seeding/go.mod:8` (`v1.40.1`, which is exactly why `app`'s guard test says the repo "was deliberately left in place"). That is why I opened the indexed page: the corpus has already ruled that test inapplicable at `shared_libraries.md:126`, so the index is the stale half.

r25-E B4 | corpus/services/next-web-app.md:22 | UPHELD | IN-SCOPE | "Next.js 16.2.7 … all four apps declare `"next": "^16.2.7"`" — they declare `~16.2.12`
   evidence: `stack-demo/next-web-app` `git grep -n '"next":' 8297c684 -- '*package.json'` → `apps/hiring:45`, `apps/integration:28`, `apps/maintenance:9`, `apps/web:46` all `"next": "~16.2.12"` (plus `packages/ui:65` `~16.2.12`, `configs/i18n:16` `>=16.2.12`). `pnpm-lock.yaml` resolves `next@16.2.12` (`:10780 next@16.2.12:`, and every peer string). Identical at `origin/main f97ba659`, so not a checkout artefact. The literal `^16.2.7` the corpus quotes is real but belongs to sibling packages: `@next/third-parties` (`apps/web:21`, `hiring:27`, `integration:10`), `@next/mdx` (`web:68`, `hiring:63`, `integration:46`), `@next/eslint-plugin-next` (`configs/eslint-config:5`). Both the scalar and the range operator are wrong. The bullet names no ref (the file pins `8297c684` at `:44`, `:75`, `:98`), so it grades at the checkout. Same-sentence controls all hold: `react ^19.2.7`, root `engines.node ">=24.0.0"`, `packageManager "pnpm@10.30.3"`.

r25-E B5 | corpus/services/clerk-integration.md:103 | UPHELD | IN-SCOPE | "`@clerk/nextjs` aligned — all four on `^6.39.2`"; three of the four are `^6.39.6`
   evidence: `next-web-app@8297c684`: `apps/web/package.json:10`, `apps/hiring:10`, `apps/integration:9` all `"@clerk/nextjs": "^6.39.6"` — identical at `origin/main f97ba659`. `ant-academy@22df69dd:code/package.json:52` = `"^6.39.2"` (that one anchor exact). So the named scalar is wrong for 3 of the 4 and "aligned" is false as written. The clause carries no ref (the `@5ba17044` pin later in the same bullet scopes the Go SDK clause only, which I re-checked and cleared: `app/go.mod:31` @ `5ba17044` = `clerk-sdk-go/v2 v2.7.0`, still v2.7.0 at `ad9f3c49:29`).

r25-E B6 | corpus/services/backend.md:148-153, corpus/services/backend.md:294 | UPHELD | IN-SCOPE | "the literal's only/sole occurrence anywhere in the clone set" — six tracked sites, not one
   evidence: I re-derived the universe the paragraph itself defines. `stack-demo` holds exactly **13** top-level git trees; tracked `.tf` per tree: app 5, cms 4, graphql-wundergraph 4, jobsimulation 6, messenger 4, next-web-app 2, roadrunner 4, sentinel 4, storage 5, studio-desk 4, **rosetta-extensions 2**, ant-academy 0, platform 0 = **44** — so the doc's own denominator is only reachable *with* the pinned rext clone. Raw `find` gives **59** `.tf` and the positive control `service_discovery_namespace_id` gives **25**, both reproducing the doc exactly. `git grep 'backend.internal.anthropos:8081' HEAD` at each tree's own ref returns **6 lines / 4 files / 2 trees**: `app HEAD:knowledge/service-dependencies.md:52` (the one named), plus `rosetta-extensions@09d06070` `stack-core/tests/fixtures/repair_leak/{pre,post}/corpus/services/cms.md:32,157` and `stack-core/tests/test_platform_predicate_guard.py:435`. Both nested `studio` checkouts at `aeec036a` return 0. The load-bearing half (*no terraform names it*) is TRUE and I verified it by both mechanisms; the exhaustiveness sentence is nevertheless false at the refs it names, and it is published twice.

r26-E B1 | corpus/architecture/ai_architecture.md:99 | UPHELD | IN-SCOPE | same predicate as r25-E B1 — `app/internal/ai/ai.go` asserted absent; it exists at HEAD
   evidence: as r25-E B1 above (re-derived once, reused). `git ls-tree ad9f3c49 internal/ai/ai.go` → blob `650b259a`; guard test `internal/ai/module_import_guard_test.go:18` `const externalModule = "github.com/anthropos-work/ai"` / `:23` `inTreePath = "github.com/anthropos-work/app/internal/ai"`; contradicts live `corpus/architecture/shared_libraries.md:126`.

r26-E B2 | corpus/architecture/ai_architecture.md:95-96, corpus/services/backend.md:313 | UPHELD | IN-SCOPE | "All Go services access AI through the shared `ai` library" / "AI providers via the shared `ai` library"
   evidence: as r25-E B2 (single re-derivation). I additionally opened the second anchor the seat carries: `corpus/services/backend.md:313` reads *"AI providers via the shared `ai` library (embeddings + skill matching — merged skiller domain)"* — unpinned, and false at `ad9f3c49` for the same measured reason (`app/go.mod` has no `anthropos-work/ai`; `go.sum` 0 lines; the library is in-tree at `internal/ai/`). The seat's sub-claim about the provider list I did NOT rely on: the `anthropos-work/ai` repo is in no clone set, so only the in-tree successor is measurable (`git ls-tree ad9f3c49 internal/ai/` has `anthropic/` + `openai/` and no mistral package; Mistral OCR now lives at `internal/cms/studio/mistralocr`).

r26-E B3 | corpus/architecture/README.md:21 | UPHELD | IN-SCOPE | same predicate as r25-E B3 — index keeps `ai` in the imported-private-module four; live set is three
   evidence: as r25-E B3 above, incl. the `rosetta-extensions@09d06070:stack-seeding/go.mod:8` (`ai v1.40.1`) counter I checked and the `shared_libraries.md:126` contradiction that settles it.

r26-E B4 | corpus/services/next-web-app.md:22 | UPHELD | IN-SCOPE | same predicate as r25-E B4 — `next` is `~16.2.12`, not `^16.2.7`
   evidence: as r25-E B4 (single re-derivation): four app manifests `~16.2.12` at `8297c684` and at `f97ba659`; lockfile `next@16.2.12`; `^16.2.7` is the `@next/*` sibling range.

r26-E B5 | corpus/services/clerk-integration.md:103 | UPHELD | IN-SCOPE | same predicate as r25-E B5 — three of the four `@clerk/nextjs` declarations are `^6.39.6`
   evidence: as r25-E B5 (single re-derivation).

r26-E B6 | corpus/services/backend.md:148-153, corpus/services/backend.md:294 | UPHELD | IN-SCOPE | same predicate as r25-E B6 — "only/sole occurrence in the clone set" is false at the doc's own denominator
   evidence: as r25-E B6 (single re-derivation: 13 trees, 44 tracked `.tf` reachable only with rext, 6 tracked hits in 4 files). The seat's additional `graphql-wundergraph@60c229f3` near-miss (`supergraph-config-prod.yaml:6`, `CLAUDE.md:18`) is port **8080**, a different literal, and I did not count it.

r26-E B7 | corpus/services/studio-room.md:67 | UPHELD | IN-SCOPE | `cert.py` described as "Certificate / signing helpers"; it is the certification-benchmark CLI
   evidence: `stack-demo/app/studio` (studio-room's own checkout, `aeec036a`) `git show aeec036a:cert.py` — 76 lines; module docstring: *"CLI: python cert.py [--branch stable|experimental] [--no-judge] [--max-concurrent N] — Run the certification phase against `benchmark/certification/test-set/*.json`"*, then version-alignment across `configs/*.ini`, minor-version bump, per-blueprint generate+benchmark, aggregate to `benchmark/certification/version/<version>.md`. Its only project import is `from benchmark.certification.runner import run_certification`; no certificate/key/signing symbol anywhere in the file. Corroborating surface present at the same ref: `benchmark/certification/{runner.py,report.py,version.py,test-set/,current-set/,version/}` and `.claude/skills/run-certification/`. "Certification" ≠ "certificate"; the tree comment is a plain factual error.

---

BOOKED=13 UPHELD=13 REJECTED=0 IN-SCOPE-UPHELD-BLOCKERS=13

---

## Predicate groups

- **app-internal-ai-exists**: r25-E B1 (corpus/architecture/ai_architecture.md:99) + r26-E B1 (corpus/architecture/ai_architecture.md:99) — "`app/internal/ai/ai.go` is **no such file**, and that path now resolves only inside the frozen `jobsimulation` husk repo." It exists at `ad9f3c49` as the folded `ai.AI` interface, and `shared_libraries.md:126` says so in the present tense.
- **ai-is-an-external-private-module**: r25-E B2 (corpus/architecture/ai_architecture.md:95-96) + r26-E B2 (corpus/architecture/ai_architecture.md:95-96, corpus/services/backend.md:313) + r25-E B3 (corpus/architecture/README.md:21) + r26-E B3 (corpus/architecture/README.md:21) — "`github.com/anthropos-work/ai` is a shared/private Go module imported by the live Go services." No repo a stack builds requires it since `1e457fa70`; the only requirers are the two frozen husks and the rext seeding module. (Same root cause as the group above — the un-propagated `1e457fa70` fold — but a distinct proposition: one is a file-existence claim about `app`'s tree, the other a `go.mod`-requirement claim, repaired by different edits. Three anchors, four bookings.)
- **next-version-and-range**: r25-E B4 (corpus/services/next-web-app.md:22) + r26-E B4 (corpus/services/next-web-app.md:22) — "all four `apps/*` declare `"next": "^16.2.7"` and the lockfile resolves it." They declare `~16.2.12` and the lockfile resolves `16.2.12`; `^16.2.7` is the `@next/*` sibling range.
- **clerk-nextjs-aligned-on-6392**: r25-E B5 (corpus/services/clerk-integration.md:103) + r26-E B5 (corpus/services/clerk-integration.md:103) — "`@clerk/nextjs` is aligned across four surfaces, all on `^6.39.2`." Three of the four are `^6.39.6`.
- **sole-occurrence-in-clone-set**: r25-E B6 (corpus/services/backend.md:148-153 + :294) + r26-E B6 (corpus/services/backend.md:148-153 + :294) — "`http://backend.internal.anthropos:8081`'s only/sole occurrence anywhere in the clone set is one markdown KB page." Six tracked lines in four files across two trees, under the paragraph's own 13-repo/44-`.tf` definition of the clone set. Two anchors, both publishing it.
- **cert-py-is-signing**: r26-E B7 (corpus/services/studio-room.md:67) — "`cert.py` = Certificate / signing helpers." It is the certification-benchmark CLI; nothing in it touches certificates, keys or signing.
