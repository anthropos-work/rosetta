# Seat G — reading #26

**Briefing sha256 (proof of delivery):** `4178b00f0d8e98eabddb895969d6e4c58799a5b292d967f8a2cfca6772a18d3d`
(`briefing-r26-AS-DELIVERED.md`, 243 lines, read in full top-to-bottom before any other action).
`corpus/ops/platform-alignment.md` §5 (lines 249–1549) also read in full, as the briefing requires.

**Corpus HEAD at this reading:** rosetta `3822501`.

## Clone refs — re-derived at my open (not copied from the sheet)

| repo | path | sha I measured | matches addendum? |
|---|---|---|---|
| app | `stack-demo/app` | `ad9f3c49` | ✅ (superseded table) |
| platform | `stack-demo/platform` | `0c91421d` | ✅ (superseded table) |
| next-web-app | `stack-demo/next-web-app` | `8297c684` (origin/main `f97ba659`) | ✅ |
| sentinel | `stack-demo/sentinel` | `f2c46190` (= origin/main) | ✅ |
| studio-desk | `stack-demo/studio-desk` | `41ee3575` | ✅ |
| ant-academy | `stack-demo/ant-academy` | `22df69dd` | ✅ |
| storage | `stack-demo/storage` | `4ce8ece5` | ✅ (unchanged) |
| messenger | `stack-demo/messenger` | `fa47850d` | ✅ (unchanged) |
| cms | `stack-demo/cms` | `ca50c817` | ✅ (unchanged) |
| graphql-wundergraph | `stack-demo/graphql-wundergraph` | `60c229f3` | ✅ (unchanged) |
| roadrunner | `stack-demo/roadrunner` | `87d8d443` | ✅ (unchanged) |
| jobsimulation | `stack-demo/jobsimulation` | `462343b0` | ✅ (unchanged) |
| **rext — per-stack CONSUMPTION clone (adjudicates)** | `stack-demo/rosetta-extensions` | **`09d06070`** | ✅ |
| rext — authoring copy | `.agentspace/rosetta-extensions` | `944fc4a2` on `main` | ✅ |
| nested: app/studio | `stack-demo/app/studio` | `aeec036a` (own checkout) | ✅ |
| nested: cms/studio | `stack-demo/cms/studio` | `aeec036a` (own checkout) | ✅ |

Per addendum rule 45, **every `rosetta-extensions` claim below was settled in the pinned per-stack clone
`09d06070`** (spot-checked identical in the authoring copy where noted). I ran **no** `git fetch`/`pull`
(§5 rule 41a) and made **zero** edits outside this file.

**Search-set enumeration** (§5 rule 44 — I enumerated every git tree before any absence claim):
15 trees under `stack-demo/`, including the two nested untracked `studio` checkouts at their own ref.

## Positive controls

| file | `wc -l` | read in full? |
|---|---|---|
| `corpus/services/hiring.md` | **481** | yes |
| `corpus/services/studio-desk.md` | **435** | yes |
| `corpus/architecture/shared_libraries.md` | **274** | yes |
| `corpus/services/coursebuilder.md` | **161** | yes |
| `corpus/services/skillpath.md` | **117** | yes |
| `corpus/services/gotenberg.md` | **84** | yes |
| **total** | **1552** | — |

(`studio-desk.md` and `coursebuilder.md` render one line past `wc -l` in a line-numbered read — no trailing
newline on the final line. Not a finding.)

---

## Blockers

### B1 — colony "Version pin": the two-way live split is GONE; both live Go repos pin `v0.35.2`

- **Anchor:** `corpus/architecture/shared_libraries.md:57` — the colony **Version pin** cell:
  > "**Split — there is no single pin.** `app` + `messenger` → **`v0.35.2`**; **`sentinel` + `storage` → `v0.34.3`** … **At `0c91421` it is still two-way but between different repos — `app v0.35.2` (`app/go.mod:16` @ `b948604f`) vs `sentinel v0.34.3` (`sentinel/go.mod:8` @ `88bc5592`)**"

- **Why it is false.** Measured at this reading's ground truth, `sentinel` is at **`f2c46190`, which IS
  `origin/main`** (I re-derived `git rev-parse --short=8 origin/main` → `f2c46190`; `88bc5592` is 2 commits
  behind and an ancestor):

  ```
  sentinel/go.mod:8  (HEAD = f2c46190)   github.com/anthropos-work/colony v0.35.2
  sentinel/go.mod:8  (@ 88bc5592)        github.com/anthropos-work/colony v0.34.3
  app/go.mod:15      (HEAD = ad9f3c49)   github.com/anthropos-work/colony v0.35.2
  ```

  The two Go repos a stack clones and builds now pin the **same** colony version. There is no split, the
  cell's headline (*"Split — there is no single pin"*) is false for the live set, and the sentence
  asserting it is **still** two-way **at `0c91421`** — the platform ref this reading measures at — is false.

- **Why the ref-pin does not save it (§5 rule 33, and the briefing's own grading rule).** Three independent
  reasons, and any one is sufficient:
  1. The **leading clause carries no ref at all** — "`sentinel` + `storage` → `v0.34.3`". The briefing:
     *"If the passage names no ref, grade it against the checkout."* The checkout says `v0.35.2`.
  2. The block **asserts currency** ("it is **still** two-way", "…are now **frozen, not live**"). Rule 33:
     a block asserting currency *"cannot be pinned into silence, because the pin and the word are making
     opposite claims and only one of them is what the reader acts on."*
  3. It pins the assertion to **`0c91421`**, the ref this reading derives from. Rule 33: *"a pin naming the
     ref the checker derived from exempts nothing."*

  Rule 33's mechanical test applies cleanly: remove `@ 88bc5592` and the sentence reads identically — the
  ref is decoration and the sentence is a standing claim.

- **What a reader does wrong with it.** A reader planning a colony bump, or reading the platform's shared-lib
  skew, concludes the two live Go services are one minor version apart and budgets a reconciliation that is
  already done. This is the same row §5 rule 33 already cites as a worked example of pin-scope failure
  (*"`shared_libraries.md`'s colony **Version pin** row"*), returning in a new costume — §5 rule 20's
  *"a claim repaired at one site and left standing at another"* / *"a claim that was refuted once can come back."*

- **Confidence:** high.

### B2 — proto "Version pin": the "live skew is two" is now one; sentinel pins `v1.210.0`, same as app

- **Anchor:** `corpus/architecture/shared_libraries.md:85` — the proto **Version pin** cell:
  > "one pin per repo: app/messenger `v1.210.0`, **sentinel `v1.200.0`**, storage `v1.196.0` … **At `0c91421` the live skew is two: `app v1.210.0` (`app/go.mod:18` @ `b948604f`) and `sentinel v1.200.0` (`sentinel/go.mod:9` @ `88bc5592`)**"

- **Why it is false.**

  ```
  sentinel/go.mod:9  (HEAD = f2c46190 = origin/main)   github.com/anthropos-work/proto v1.210.0
  sentinel/go.mod:9  (@ 88bc5592)                      github.com/anthropos-work/proto v1.200.0
  app/go.mod:16      (HEAD = ad9f3c49)                 github.com/anthropos-work/proto v1.210.0
  ```

  Both live Go repos pin **`v1.210.0`**. The live skew is **one value, not two** — there is no skew.

- **Same three reasons as B1** apply verbatim: the leading clause "`sentinel v1.200.0`" carries no ref and
  grades at the checkout; the block asserts a **live** state; and it pins that state to `0c91421`, the ref
  the reading derives from.

- **Why I am confident this is corpus, not clone-noise.** §5 rule 41a is explicit that *"it has since been
  fetched" is not a repair, it is a NEW ground truth — the next pass re-derives against the moved refs and
  measures what the move injected.* This reading is that next pass; both B1 and B2 are exactly what the move
  injected, and they are live-state assertions a reader acts on.

- **Confidence:** high.

---

## Minors

- `corpus/services/studio-desk.md:107` — "`GCLOUD_SERVICE_ACCOUNT` is declared in **`.env.example:120`**".
  Measured: it is at **`.env.example:119`**; line `120` is blank. The claim itself is TRUE (declared in
  `.env.example`, injected by `terraform/main.tf:129` ✓ exact, read by no code in `src/` ✓ — verified with
  bare `grep` **and** `git grep` at HEAD, studio-desk having no nested repos). Off-by-one onto a blank line;
  the class `anchor_construct_guard` catches. *Hesitation:* the caller says resolve doubt upward, but this is
  textbook single-line drift with no factual consequence, which the canonical rule (§5 rule 25) carves out as
  MINOR explicitly.

- `corpus/architecture/shared_libraries.md:96` — the intra-corpus cite "[`storage.md:115`] documents [`StorageService`] **in full**". `storage.md:115` is
  `  rpcsrv/rpcsrv.go            Connect-RPC handler implementations` — a one-line entry inside a fenced
  directory-listing block. The section that documents `StorageService` in full is **`storage.md:129`**
  (`### Connect-RPC (\`StorageService\`)`), 14 lines later. The claim is true; the anchor is not the
  construct. *Hesitation: I initially graded this a BLOCKER and downgraded after re-counting the block —
  the cited line does at least name the Connect-RPC handler file, so it is a near-miss rather than an
  unrelated construct. §5 rule 34's class (an intra-corpus citation that drifted and "landed on content and
  read as correct").*

- `corpus/services/hiring.md:241` — "the three `validation_*` tables … the `PersonaSeeder` also writes
  (`persona_write.go:69-71`,`143-167`)". In the pinned rext clone `09d06070`,
  `stack-seeding/seeders/persona_write.go:69` is a bare `//` and `:70-71` is prose about the **REMOVED**
  legacy `jobsimulation.sessions` step. The three tables are **mapped** at `:66-68` and actually **written**
  at `:92-94` (`{"public", "validation_attempt_results", …}` etc.). The `:143-167` half is approximately
  right (it covers `sessionCols()` `:152-159` + `attemptResultCols()` `:161-167`, but `skillResultCols()`
  starts at `:169`, outside the range). Claim true, anchor 3 lines short of its own construct.

- `corpus/services/coursebuilder.md:48` — "`terraform/variables.tf:635-638` declares it `sensitive` with no
  default, `ssm.tf:328-334` creates the parameter and `main.tf:555` injects it". At the **current checkout
  `ad9f3c49`** those are `variables.tf:759-763` and `main.tf:757-758`; `:635-638` is a comment block about
  the cms/Studio keys and `:555` is `"name": "DIRECTUS_BASE_ADDR"`. **However — both resolve BYTE-EXACT at
  `app` `b948604f`**, the ref named earlier in the same bullet (`main.go:770` @ `app` `b948604` v1.366.0):
  `variables.tf:635` = `variable "anthropic_api_key" {`, `main.tf:555` = `"name": "ANTHROPIC_API_KEY",`.
  Also `ssm.tf:328-334` overshoots EOF by one line (the file is 333 lines; the resource is `:328-333`).
  **This is a deliberate judgement call and I flag it for the adjudicator.** Under a strict reading of
  rule 33 (*"a pin's scope is … a wrapped sentence in prose"*) the terraform anchors sit in a *different
  sentence* from the pin and would grade at the checkout → BLOCKER. I declined because (a) the substantive
  claim ("in production the key is required") is TRUE at **both** refs, and (b) booking it would be booking
  the clone move rather than the corpus — precisely the mechanism §5 rule 45 records as *manufacturing false
  bookings by construction*. The principled line I drew: **B1/B2 are blockers because their falsified clauses
  carry no ref at all and assert a live state; this one carries an exact ref one clause away.**

---

## Positively cleared (audited zeros)

Labelled per the briefing. **ENUMERATED** = I derived the predicate's set from source independently and
compared cardinalities. **RE-DERIVED** = I only re-computed arithmetic the document displays (the weakest
clearance).

### `hiring.md` — ENUMERATED unless noted

- **The 5-value `completion_status` enum + its `Values()`** — `ent/enum/jobsimulation.go:29-35` is the const
  block (pending/passed/failed/discarded/timedout, exactly 5, no `SIMULATION…` member) and `:37-43` is
  `Values()`. **ENUMERATED** (read the whole type, not just the cited lines).
- **The four required-and-undefaulted columns** — I enumerated every `NOT NULL` column of
  `20260722104506.sql`'s `CREATE TABLE "job_simulation_sessions"` and checked each for a `DEFAULT`: exactly
  4 lack one — `owner_id :6`, `sim_id :7`, `sim_type :10`, `token :13`; all 8 other `NOT NULL` columns carry
  a `DEFAULT`. The doc's "every other `NOT NULL` column in the DDL carries a `DEFAULT`" is **exact**.
  **ENUMERATED.**
- **"23 columns"** — I counted the DDL (23) *and* checked for later `ALTER TABLE "job_simulation_sessions"`
  across the whole migration set (0 hits, with a positive control: `ALTER TABLE "memberships"` = 4).
  Also cross-checked against the Ent schema (20 fields + 3 mixin columns = 23). **ENUMERATED.**
- **The `grep -in hiring` derivation at `:90-91`** — re-ran both greps: `resolver_queries.go` returns
  exactly 4 hits, all sim-TYPE (`simulation_type_hiring` ×3, `SimulationTypeHiring` ×1); `intelligence.go`
  returns 0. Both stated positive controls reproduce **exactly**: `OrgFeatureInsights` ×**8**,
  `JobSimulationSession` ×**44**. **ENUMERATED.**
- **The whole read-path table + best-attempt window** — every anchor opened with surrounding context:
  `resolver_queries.go:1034` (decl), `:1035` (Casbin gate, first statement), `:1053` (status ∈
  {active,invited}), `:1080` (manager call), `:1085`/`:1089` (the *neighbouring* `BySessions` gate and its
  `GetMembership` — the doc's distinction is exact); `intelligence.go:1700`, `:1702`, `:1709`, `:1733`,
  `:1739-1744`, `:1738-1764`, `:1820`, `:1844`, `:1846`, `:2124`, `:2158-2160`, `:885-886`;
  `ent/schema/job_simulation_session.go:33`/`:39`/`:45`; `ent/jobsimulationsession.go:181-186`;
  `graph.go:129546-129554` **and** the proto-bound twin `:129392-129400`; `jobsimulations.graphqls:14`
  and `:128`; `anticheat_result.go:24`; `20260722081626_jobsim_data_model.sql:336/355/376`;
  `20260722104506.sql:2/6/7/10/12/13/29/53/79`; `20260729133514.sql:58-62`/`:62`;
  `20240527131926.sql:7`; `20250416091037.sql:5`; `askengine/registry.go:192`;
  `20230817154747_supabase_baseline.sql:2`. **All resolve to the named construct.** **ENUMERATED.**
- **`SET "score"` = 0 hits across the migration set** — reproduced, with a positive control
  (`SET "` = 8 lines set-wide). **ENUMERATED.**
- **Only `CREATE SCHEMA` in the whole migration set is `auth`** — 1 hit, `supabase_baseline.sql:2`.
  **ENUMERATED.**
- **`repos.yml` = 6 @ `0dab54d`, 4 since `838d907`** — `git show` at both refs: 6 named entries → 4.
  `838d907` = *"chore(compose): drop the storage, messenger and customerio-sync containers"*, merged
  `0c91421`. **ENUMERATED.**
- **next-web-app surface claims** (all at `8297c684`, the demo build pin the doc names):
  `useGetClerkOrganization.tsx:16-18`/`:20-21`; `UserStatusContext.tsx:141-173`/`:144-149`/`:168-172`
  (web) and `:125`/`:144-145` (hiring); `useNavbarSections.tsx:161`, `:249-256`, `:329-331`, `:340-343`,
  `:391-398`, `:475-482`, `:476`, `:568`; `orgGroups.ts:48-65`/`:61` (and it genuinely takes **no**
  `isHiringOrg` parameter); `template.tsx:90`; `FreeTrialContainer.tsx:29`. **ENUMERATED.**
- **The M257x-iter-102 Workforce-Intelligence RETRACTION** — I re-derived it rather than trusting it:
  `git grep 'showWorkforce'` at HEAD returns **exactly two** `showWorkforce={false}` sites, both in
  `apps/hiring/.../template.tsx` at **`:167`** and **`:248`**. The retraction is correct. **ENUMERATED.**
- **Zero intercepting-route dirs in `apps/`** — `find` for `(.)`/`(..)`-prefixed dirs = 0, with a positive
  control (21 parenthesized route-group dirs exist). The `<Drawer>` at
  `apps/hiring/.../InsightsByMembersContainer.tsx:359` is a plain Ant Drawer. **ENUMERATED.**
- **`simulationScoreColumn.tsx` byte-identical across `apps/web` and `apps/hiring`** — `cmp` reports
  identical, 168 lines each. `:95-97` is the `row.score` read. **ENUMERATED.**
- **rext (pinned `09d06070`)**: `persona_write.go:91`, `:152`, `:152-158` (`token` at `:155`);
  `hiring_config.go:99` (`CopyRowsIdempotent` into `organization_sim_invitation_links`);
  `blueprint.go:453-458` = `RoleMix{Admin, Member, Candidate, AdminEmails}`; `hiring_scope.go:46`
  → `st.IsHiringOrg()`; **"all 8 generic seeders"** — I enumerated both the doc-comment list (8 names) and
  the files consulting the guard (8 non-test files: activity, assignments, feedback, hero_activity,
  jobsim_sessions, persona, skillpath_sessions, succession). `TestGenericActivitySeeders_SkipHiringOrg`
  exists at `hiring_scope_test.go:75`. `hiringComparableFloor = 6`, `RENDER_GATE_FLOOR`,
  `render-hiring-comparison.spec.ts`, `next-hiring-role-remap`, `next-hiring-members-pagination` all exist.
  **ENUMERATED.**
- **The two corpus twins it cites** — `service_taxonomy.md:62` is indeed the **Database** characteristic
  bullet quoted, and `dependency_map.md:78` is indeed the *"directly to the `public` schema (the legacy
  `jobsimulation` schema is non-authoritative)"* bullet. The iter-102 self-correction of the former
  (`:52` → `:62`) is right. **ENUMERATED.**
- **The GraphQL endpoint** — no `graphql` compose service (5 services in `docker-compose.yml`); `backend`
  publishes `:8082`; `app` serves `/graphql/query` at `internal/web/backend/backend.go:317`. **ENUMERATED.**

### `studio-desk.md` — ENUMERATED unless noted

- `docker-compose.yml:138-140` (`depends_on: backend`), `:141` (`profiles: [studio-desk, all]`), `:119`
  (build arg), `:135` (runtime env) — **all exact at platform `0c91421`**; and the historical
  `:337-341 @ 2adcf71` really does carry the extra `cms:` dependency. `backend`'s own
  `profiles: [core, backend, all]` at `:110` confirms why a lone `--profile studio-desk` is an invalid
  project. **ENUMERATED.**
- **The 9 prod Vite entries + the dev-only 10th** — `vite.config.ts` `rollupOptions.input` lists exactly
  the nine named, with `dev-accept` behind `isProduction ? {} : {…}`. **ENUMERATED.**
- **The three hardcoded prod-host sites** — `git grep 'app.anthropos.work' -- app/core/scaffold` returns
  **exactly three**: `pageWrapper.js:149` (logo link), `userProfile.js:148` (menu Back),
  `userProfile.js:302` (logout). Matches the doc's "three places" and its role labels. **ENUMERATED.**
- **The boot-order model** — `preloadCriticalCSS()` at **L97**, `new PageWrapper()` at **L206**, with
  exactly three intervening blocking awaits in `initializeApp` in the stated order:
  `withClerkErrorHandling`/clerk.load (`:181`), `l12nService.init()` (`:191`),
  `userService.canAccess()` (`:199`). **ENUMERATED.**
- `youtube.ts:43` reads `YOUTUBE_API_KEY` only and serves `_mock: true`; `terraform/main.tf:129` is the
  `GCLOUD_SERVICE_ACCOUNT` name entry; `skillpath.ts` is 61,080 bytes (the largest route, "~61KB");
  `src/index.ts:60` `PORT || 9100`; `STUDIO_ACCESS_ROLES` at `src/index.ts:96` with exactly the four
  role forms named; `checkEnterpriseAndAdmin` at `:99` redirecting to `WEB_APP_URL`;
  `config.ts:182` `AI_DEFAULT_TIER || 'thinking_fast'`; `.env.example:56` `AI_PROVIDER_CHAIN=azure-openai,openai`
  and `:60` `AI_DEFAULT_TIER=fast`; the four tiers and their OpenAI/Azure/Anthropic model ids;
  **7** l12n languages (de/en/es/fr/it/ja/nl); `src/routes/` = exactly {ai,dev,skillpath,youtube}.ts;
  `src/prompts/` = exactly the 5 named dirs + loose `*.md`; jest `projects:` = backend + frontend;
  every npm script quoted exists. **ENUMERATED.**
- **"No local datastore"** — `package.json` declares none of the 11 DB drivers named (regex positive-
  controlled against `"express"`/`"vite"`, which match). `studioDB.js` exists as a façade.
  **ENUMERATED.**
- **"The repo's only Tailscale-funnel mention is `main.ts:105`"** — two instruments (bare `grep -rn` and
  `git grep -in` at HEAD) agree: `main.ts:105` (the funnel comment) + `:107` (the GlitchTip DSN), same
  construct, no nested repos under `studio-desk`. **ENUMERATED.**
- **rext (pinned `09d06070`)** — all five studio-desk demopatch dirs exist
  (`studio-desk-{back-to-cockpit,logo-url,logout-url,no-thirdparty,shell-first-paint}`);
  `build_frontend_studio_desk` ladder referenced; `gen_injected_override.py:379 frontend_lines()` emits the
  existence-guarded `env_file` ending `/studio-desk/.env`; **both** named regression tests exist in
  `stack-injection/tests/test_injection.py`; `stack-verify/live/autoverify.sh:528` carries the studio
  container AI-key check; `stack-verify/e2e/run-studio-fcp.sh` exists; the two secret-DNA genes are
  `repo: studio-desk`, `status: required`, `criticality: standard` — exactly "required · standard".
  Clerkenstein serves the FAPI `/v1/me/organization_memberships`. **ENUMERATED.**
- **"the Cosmo Router survives in production only"** — consistent with the fenced map
  (`platform-migration-status.md:96`: *"In prod it is still declared — `graphql-wundergraph/terraform/main.tf:20` `= 1`"*).
  Not a self-contradiction. *Cleared as consistent, not independently measured (no `infrastructure` clone).*

### `shared_libraries.md` — ENUMERATED unless noted

- **The 7/7 · 7/7 · 6/7 · 2/7 · 0/7 matrix (line 23-24)** — I did **not** re-derive the sum from the doc's
  set; I enumerated the set myself: `git grep 'anthropos-work/(colony|proto|ai|authn|taxonomy)' HEAD -- go.mod`
  across all seven on-disk Go repos at their own refs. Result: colony 7/7, proto 7/7, taxonomy 6/7 (sole
  exception `roadrunner`), ai 2/7 (`cms` + `jobsimulation` only), authn 0/7. **Every figure exact.**
  **ENUMERATED.**
- **"that set is 11" (every `type: go` repo ever in `repos.yml`)** — I replayed all **10** commits that ever
  touched `repos.yml` and parsed each revision: the union of `type: go` names is exactly **11**
  (app, chronos, cms, intelligence, jobsimulation, messenger, roadrunner, sentinel, skiller, skillpath,
  storage); the union of *all* names is 14. All four extras (skiller/skillpath/chronos/intelligence) are
  present in the first revision `a2a3ee6`, and skillpath is the last of them to go, dropped by `a4db680`.
  **ENUMERATED.**
- **The six Connect handlers @ `b948604`** — I enumerated `mux.Handle` across the whole of `main.go` at that
  ref: exactly **6**, at `:1187`, `:1188`, `:1196`, `:1204`, `:1213` (inside `if cmsRPCServer != nil` at
  `:1212-1214`), `:1228`. Every doc anchor exact; neither `SkillPathSessionService` nor a RoadRunner service
  is among them. **ENUMERATED.**
- **The taxonomy-figures correction — the most load-bearing derivation in the file** — I opened the
  snapshot manifest itself (`.agentspace/snapshots/taxonomy/5afc0bcc…/manifest.json`) and read the row
  counts: `skills` **42,790**, `job_roles` **22,470**, `job_role_embeddings` **18,919** — all three exact,
  and the manifest's own header confirms `source: primary-read`, `public_only: true`,
  `predicate: org-null`, `captured_at: 2026-06-29`. The "18K came from `job_role_embeddings`" diagnosis is
  supported by the data. **ENUMERATED.**
- **"At least 13 Connect-RPC services… the named ones"** — the named list is exactly 13. Note the doc is
  honest that this is a floor and unverifiable (`proto` is in no clone set). **ENUMERATED (the list), could
  not settle (the floor).**
- `app/go.mod:16`/`:18`/`:20` @ `b948604f` = colony/proto/taxonomy ✓ exact; `1e457fa70` is
  *"refactor(ai): fold the ai library into app as internal/ai"*; `ad9f3c49:go.mod` has no `ai` line and
  `go.sum` has **0**; `internal/ai/ai.go` is **21 lines**; `module_import_guard_test.go` exists;
  `internal/ai/` did **not exist** at `b948604f` (verified by `git ls-tree`, with a positive control).
  **ENUMERATED.**
- **The vendor-selection sites** — `AIManager.getClient` at `jobsimulation/ai/ai.go:259` and
  `skillerai/ai.go:332`; `flag_use_azure_us` at `:267`, `:344`, `skillerai/ai.go:347`; `isThrottlingError`
  declared `jobsimulation/ai/ai.go:129` used `:166`/`:325`, `skillerai/ai.go:128` used `:176`. **Every one
  exact at `ad9f3c49`.** **ENUMERATED.**
- **authn** — cms imports `colony/authn` in exactly **6** `.go` files @ `ca50c817`, jobsimulation in
  exactly **8** @ `462343b0`; the **standalone** `github.com/anthropos-work/authn` is imported by **0** of
  the seven repos; `sentinel` imports colony (2 files) but **not** `colony/authn`; `app` imports
  `colony/authn` in 129 files; `gqlauthz.go` exists. **ENUMERATED.**
- `external_services.md:579` is exactly *"There is **no ordered EU-first fallback chain.**"* ✓;
  `GH_ACCESS_TOKEN: $GH_PAT` at `docker-compose.yml:12`/`:39`; `GOPRIVATE` + `insteadOf` in
  `app/Dockerfile:11,13`; `legacyStorage "…/proto/go/simulator/storage/v1"` really is imported by
  `storage/internal/migration/migration.go:13`; none of the five libraries has a compose entry (0 hits).
  **ENUMERATED.**

### `coursebuilder.md` — ENUMERATED unless noted

- **The SSE wire contract — "16 names, and `cost` is NOT one of them"** — this is the file's most
  derivation-heavy claim and I re-derived the **SET**, not the count. I enumerated **every** `writeSSE(`
  call site in `handler.go`: exactly two (`:1458` hardcoded `"session"`; `:1479` fed by `renderEvent`), plus
  the definition at `:3131`. Then I enumerated every non-empty name `renderEvent` (`:2604-2772`) returns:
  15 distinct — text, score, patch_applied, patch_skipped, stage, outline, progress, preview_ready,
  draft_kept, error, translation_ready, rebuild_required, steering_received, steering_applied, done.
  15 + `session` = **16**, and the `default` arm at `:2770` really does re-emit `text` (not a 17th).
  **Every one of the 15 line anchors is exact.** The `EventCost` filter (`:2709` → `return "", nil` at
  `:2717`) and the `if name != ""` guard at `:1478-1480` are exact, and the D2 comment is quoted
  faithfully. **ENUMERATED.**
- **The credit costs** — the doc says `course.build`=5/chapter, `course.refine`=**1**,
  `course.translate`=1/locale. The `creditCost` map at `internal/credits/cost.go:86-90` is
  `{build:5, refine:1, translate:1}`. **Note: the platform's own header comment at `cost.go:29` says
  "course.refine → 5 credits per refine turn" and is stale — the corpus is right and the source comment is
  wrong.** I graded the map, not the comment (§5 rules 10/17). **ENUMERATED.**
- `bedrock.go:98-104` (`ModelBackendName()` → `"anthropic-api"`/`"bedrock"`) and `:105-114`
  (`newUnderlyingClient` → `askengine.NewAnthropicClientWithModel` when the key is set) — exact;
  `main.go:770` @ `b948604` is the backend log line; `main.go:766-779` @ `b948604` — comment `:766-769`,
  the two `logger.Warn(… routes disabled)` arms at `:774` and `:778` — **exact**;
  `main.go:824-826` @ `b948604` reads `OPENAI_KEY` for the cover generator — exact.
  **ENUMERATED.**
- **`COURSEBUILDER_OPENAI_IMAGE_KEY` "survives only in stale in-repo markdown"** — measured: 0 Go hits,
  2 markdown hits (`internal/coursebuilder/README.md`, `GO-LIVE-RUNBOOK.md`), and `68c245123` does delete
  the `os.Getenv("COURSEBUILDER_OPENAI_IMAGE_KEY")` read. **ENUMERATED.**
- **The version/ref discipline paragraph** — `git describe --tags ad9f3c49` → **`v1.369.0-7-gad9f3c498`**
  and `git describe --tags 2035f9a4` → **`v1.369.0-2-g2035f9a40`**, both character-exact; `v1.369.0` is
  the newest tag. **ENUMERATED.**
- `20260717151144.sql` creates exactly the three named tables (`:2`, `:40`, `:56`); `coursebuildersession.go`
  Ent schema exists; `DefaultSessionsPerOrgPerDay = 50` (`rate.go:28`); `COURSEBUILDER_MAX_MONTHLY_COGS_USD`
  default **500** (`budget.go:50`); `CoursebuilderSrv` + `TypeCoursebuilderRun`; both operator-only `cmd/`
  tools + `assertNotSharedDSN`; `go 1.26.4`. **ENUMERATED.**
- File-count hedges: "~100 Go files" → **102** ✓; "~55 test files" → 61; "~25 boundary test files" → 33.
  Hedged with `~`; not booked. *RE-DERIVED.*

### `skillpath.md` — ENUMERATED unless noted

- **"There is NO `SkillPathSessionService` anywhere … 0 occurrences in Go source"** — I ran this across
  **all 15 git trees** (including both nested `studio` checkouts at `aeec036a`) per §5 rule 44. Only `app`
  (2 hits) and `rosetta-extensions` (6, all prose) return anything; in `app` the two hits are exactly
  `CLAUDE.md` and `knowledge/architecture.md`, and `git grep -- '*.go'` returns **0** with a working
  positive control (`SkillerService` → 3+ Go files). The doc's Trap-C framing is exactly right.
  **ENUMERATED.**
- **`app/CLAUDE.md:109` and `app/knowledge/architecture.md:28`** both still list
  `SkillPathSessionService`, and both hold **at `ad9f3c49` *and* at `2035f9a`** — the doc's dual-ref claim
  is exact. **ENUMERATED.**
- **The three migrations that post-date `20260729133514.sql` at `9d00a313`** — enumerated from the tree:
  exactly three, exactly the names given (`20260731131307.sql`,
  `20260731154527_academy_chapter_progress_completed_at.sql`,
  `20260803143844_ai_readiness_recommendation_path.sql`). **ENUMERATED.**
- **The manager-view retraction** — `intelligence.go:1144` is the `InsightsSkillPathByMemberships` decl and
  `:1159-1170` is the `m.ent.SkillPathSession.Query()` with `SkillPathID` + `StatusIn(active, completed)` +
  the tenant predicate. `DROP TABLE "local_skill_path_sessions"` really is `20260729133514.sql:63` (and
  `local_jobsimulation_sessions` `:62`), and no `local_skill_path_session.go` Ent schema exists.
  **ENUMERATED.**
- **The "Coming soon" drill-down**, verified at `next-web-app` **`8297c684`** (the tree the doc names):
  `InsightsBySkillPathStudentSimulationsContainer.tsx:33` returns `null as unknown as MembershipEnriched`
  (hardcoded null ✓), the results table `:140-149` and the totals block `:150-160` are commented out ✓, and
  the body renders `t('enterprise.insights.comingSoon')` → **"Coming soon"** in
  `configs/i18n/messages/en/enterprise.json`. (The doc's word "literal" is loose — it is an i18n key — but
  the rendered output is exactly as stated; not booked.) `origin/main` really is `f97ba659`.
  **ENUMERATED.**
- `session.go:205-207` and `skillpaths.go:88-95` both carry the `// cms-in-app deseam: cms is in-process`
  comment and the `GetSkillPathDomain` call; both Ent schema files exist; `skillpath_sessions.graphqls`
  exists; `repos.yml` = **9** entries @ `2adcf71` with **0** skillpath entries (the single `skillpath`
  string there is inside a comment); `SKILLPATH_STREAM=skillpath` survives. **ENUMERATED.**

### `gotenberg.md` — ENUMERATED

- **The `:268` → `:183` re-anchor (§5 rule 42's own worked example) is now CORRECT**:
  `docker-compose.yml:183` is `profiles: [core, backend, all]` and the file is **186** lines.
  **ENUMERATED.**
- **"the nine MIME types `docconv` can't read directly (`extract.go:17-27`) … DOCX is not among them"** —
  I enumerated `gotenbergMimes` at `9d00a313`: exactly **9** entries — xls, xlsx, ppt, doc, odt, ods, odp,
  `application/rtf`, `text/rtf`. The doc's gloss ("the three OpenDocument types, and RTF under both its MIME
  spellings") and the DOCX exclusion are both exact. **ENUMERATED.**
- **"Call sites (two)"** — `git grep 'ConvertToPDF('` at `9d00a313` returns exactly the definition plus
  **two** call sites: `extract.go:77` and `user_import_resume_2d.go:68`. **ENUMERATED.**
- `extract.go:77-81` (convert then immediately `ConvertFromReader(bytes.NewReader(pdf), "application/pdf")`),
  `:59`, `:43-45`; `user_import_resume_2d.go:68-74` (DOCX-only, `ocrInput = pdfBytes`) and `:69-74`
  (failure only logs); the 90 s client timeout (`gotenberg.go:13`); the `ConvertToPDF` signature; the
  `/forms/libreoffice/convert` path; `GOTENBERG_URL=http://gotenberg:3200` at `docker-compose.yml:57`.
  **All exact.** **ENUMERATED.**

---

## What I could not settle

1. **Anything inside the five shared-library repos themselves.** `colony`, `proto`, `ai`, `authn` and
   `taxonomy` are private Go modules **absent from every clone set** (correctly, as
   `shared_libraries.md` itself states) — so I could measure *who requires them and at what version* but
   **not** their contents. Unsettled: colony's package inventory and `go 1.25.0`/`golang:1.26-bookworm`,
   the `rate.NewLimiter(1,5)` "only logs" claim, the Sentry `ENVIRONMENT=production || FORCE_SENTRY` gate,
   proto's buf-v1 layout / "no `buf.work.yaml`" / "~10 `go/simulator` packages", every `ai` constructor +
   panic + retry claim, the standalone-authn "missing `return`" defect, taxonomy's "131-line `node.go`"
   and the `NodeID` format/examples, and `chronos`'s `v0.30.1` colony pin. **Per the briefing this is a
   could-not-settle, not a blocker.** What would settle it: clone the five at the pinned versions
   (`GOPRIVATE` + `GH_PAT`), or read the module cache.
2. **Prod-only state.** `graphql-wundergraph` "survives in production only", `jobsimulation` schema
   "frozen until M710", messenger/storage ECS states. `infrastructure` has never been in a clone set (the
   corpus says so itself). Consistent with the fenced map; not independently measured.
3. **Snapshot/DB-derived hiring figures.** "87 real `SIMULATION_TYPE_HIRING` sims", "0 `job_position` rows
   (the prod 443 was never captured)", "the `skillpath` schema holds 0 rows". The taxonomy manifest was
   readable and checked out exactly; I found no equivalent captured artifact for the HIRING-sim pool.
   Settle with a `stacksnap status` on a replayed stack or the capture manifest for the sim surface.
4. **Live-measured numbers**, none of which a static read can confirm: the M228 render-gate results
   (5/5, per-position cohorts 8/8/9/9/8, scores 27–100, recruiter p95 1.27 s), the M253 studio
   first-paint (4669 → 817 ms; `canAccess` 4049 → 38 ms; FCP 6936 → 2152 ms), and the "2.09 s"/latency
   figures referenced in passing. Settle by re-running `run-studio-fcp.sh` /
   `render-hiring-comparison.spec.ts` on a cold stack.
5. **Third-party image internals** — `directus/directus:11.6.1` `bootstrap/index.js:81` (the `ADMIN_TOKEN`
   bootstrap) cited in `studio-desk.md:109`. Settle by `docker run --rm --entrypoint sh` on the pinned image.
6. **One deliberate judgement call, flagged for adjudication:** the `coursebuilder.md:48` terraform anchors
   (booked MINOR, reasoning in the Minors section). If this reading's grader holds that rule 33's pin scope
   is the *sentence* rather than the *bullet*, that MINOR becomes a BLOCKER. I state my line explicitly so
   the two readings can be compared rather than averaged.

## Method notes worth carrying forward

- **§5 rule 1 caught me once**: `grep -rn 'showWorkforce' apps packages --include=*.tsx` died with
  `(eval):1: no matches found: --include=*.tsx` — zsh glob expansion, not "no matches". Had I not read
  stderr I would have booked a false absence on the load-bearing `showWorkforce={false}` enumeration, which
  is the evidence for the iter-102 Workforce-Intelligence retraction. I re-ran it as `git grep` at HEAD.
- **§5 rule 10 caught me twice.** (a) I first miscounted `storage.md:115` as `storage/storage.go` and was
  about to book a BLOCKER on a construct that is actually `rpcsrv/rpcsrv.go`; re-reading the surrounding
  block corrected it and moved the finding to MINOR. (b) The `coursebuilder.md` terraform anchors looked
  flatly wrong at the checkout and are byte-exact at the ref the same bullet names — checking the *other*
  ref before booking is what kept a false BLOCKER out.
- **§5 rule 17 paid off in the opposite direction**: `internal/credits/cost.go`'s own header comment
  contradicts its `creditCost` map on `course.refine`. The corpus matches the **map**. Grading the comment
  would have produced a false BLOCKER against a correct doc.
- **The dominant residual class in my six files is the one rule 41a predicts**: a claim that was true when
  written, falsified by a clone advancing under it. B1 and B2 are both that, and both sit in the same
  document (`shared_libraries.md`), in the same construct (a **Version pin** table row), one row apart —
  a fence keyed on *"a version-pin row whose unpinned clause disagrees with the checkout"* would catch
  both mechanically.
