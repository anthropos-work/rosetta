# Seat B — reading #26

**Instrument (proof of delivery):** `briefing-r26-AS-DELIVERED.md`
sha256 `4178b00f0d8e98eabddb895969d6e4c58799a5b292d967f8a2cfca6772a18d3d` (243 lines).

**Ground truth re-derived at this seat's open** (`git rev-parse --short=8 HEAD`, no fetch, no state change —
matches the briefing ADDENDUM exactly):
`app ad9f3c49` · `platform 0c91421d` · `next-web-app 8297c684` · `sentinel f2c46190` ·
`studio-desk 41ee3575` · `ant-academy 22df69dd` · `storage 4ce8ece5` · `messenger fa47850d` ·
`cms ca50c817` · `graphql-wundergraph 60c229f3` · `roadrunner 87d8d443` · `jobsimulation 462343b0` ·
`stack-demo/app/studio` + `stack-demo/cms/studio` **aeec036a** (own checkouts) ·
`stack-demo/rosetta-extensions 09d06070` (**the pinned per-stack consumption clone — the tree I used to
settle every tooling claim**, per ADDENDUM / §5 rule 45) · `.agentspace/rosetta-extensions 944fc4a2`
(authoring copy — **not** used to adjudicate).

Read `corpus/ops/platform-alignment.md` §5 in full (lines 249–1548) before starting.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/services/ai-readiness.md` | 668 | yes |
| `corpus/services/cms.md` | 313 | yes |
| `corpus/services/chronos.md` | 245 | yes |
| `corpus/services/ai-labs.md` | 158 | yes |
| `corpus/services/README.md` | 88 | yes |
| `corpus/services/skiller.md` | 69 | yes |
| **total** | **1541** | |

Search-pipeline controls run in the same pass: `SKILLER_RPC` returned 26/9/1/4/12/20 hits across six
clones while `MESSENGER_RPC_ADDR` returned 0 everywhere (so the zero is a measurement, not a broken
pipeline); `git log -S 'SKILLER_RPC' 0c91421d` returned **7** commits against **0** for `MESSENGER_RPC`.
All tree-wide numbers below enumerate the search set with
`find stack-demo -name .git -maxdepth 4` → **15 git trees** (13 top-level + the 2 nested `studio`
checkouts) and grep each **at its own ref**.

---

## Blockers

### B1 — `skiller.md:19` asserts a production terraform value that exists in no clone, and that its own sibling doc explicitly declares unmeasurable

- **Anchor:** `corpus/services/skiller.md:19` —
  > `http://backend:8083` locally; `skiller_rpc_addr = http://backend.internal.anthropos:8081` in production
  > terraform.

  Present tense, no ref pin. The sentence immediately after it (`:20-22`) is about the *local* variable
  only, so the production half stands as a current claim.

- **Why it is false / unsupportable:**
  1. **The literal appears in ZERO `.tf` files anywhere.** Measured with `git grep -F` per clone at its own
     HEAD over all 15 trees: `backend.internal.anthropos:8081` → **0 hits in `*.tf`** in every tree. The
     tracked `.tf` census is 44 files across the 13 top-level clones (0 in the two nested `studio` repos),
     59 by raw filesystem sweep.
  2. **The three repos that declare `skiller_rpc_addr` at all give it no value.** `cms/terraform/main.tf:106-107`,
     `jobsimulation/terraform/main.tf:120-121` and `messenger/terraform/main.tf:78-79` set
     `"SKILLER_RPC_ADDR" = "${var.skiller_rpc_addr}"` (messenger: `var.skiller_rpc_address`), and the
     corresponding `variable` blocks — `cms/terraform/variables.tf:76-79`,
     `jobsimulation/terraform/variables.tf:163-166`, `messenger/terraform/variables.tf:87-90` — declare
     `type = string` with **no default**. The value is supplied by `infrastructure`, which has never been
     in any clone set. All three of those services are undeployed anyway (`cms` desired_count = 0;
     jobsimulation's module block deleted at `6092c6d2`; messenger's container deleted at `838d907`).
  3. **The platform's own KB puts the address in the PAST tense.**
     `app/knowledge/service-dependencies.md:50-53` @ `ad9f3c49`: *"**There are no external callers of app's
     RPC mux left.** `messenger` was the last one — it used to reach the users, cms, jobsimulation and
     skiller surfaces at `http://backend.internal.anthropos:8081`, and folding it in at v9.0 closed that
     edge."*
  4. **Direct corpus self-contradiction, inside this reading's own file set.** `cms.md:55` — *"**The
     production address is not stated here in either direction:** no `.tf` file in any clone names
     `http://backend.internal.anthropos:8081`"* — and `cms.md:196` — *"**And the production declaration is
     not measurable from this repo at all** … so no *'still names'* claim can be made here in either
     direction."* `skiller.md:19` makes exactly the claim `cms.md` says cannot be made.
- **Confidence:** high (the contradiction and the 0-hit measurement are both certain; what production
  *actually* declares remains unknowable from this clone set, which is the point).

### B2 — `ai-readiness.md` — five line anchors into `app/internal/aireadiness/` resolve at the checkout but name the WRONG constructs, one of them the opposite kind of query

- **Anchors** (all unpinned → graded at the checkout `ad9f3c49` per the briefing's ref rule):

  | corpus line | cited anchor | what it actually is @ `ad9f3c49` | where the named construct really is |
  |---|---|---|---|
  | `:429` | `readiness.go:662-664` for **`keepInCycleStep1`** | inside `audienceScope` (`return "", nil, nil` / `ids, err := m.audienceUserIDsForTags(…)`) | `keepInCycleStep1` is `:708` (comment) / `:710` (func) |
  | `:431` | `readiness.go:684` for **`keepStartedMembers`** | `func (m *Manager) queryInCycleStep1Completers(…)` | `keepStartedMembers` is `:730` |
  | `:436` | `steps.go:915-938` for **`queryReadinessStarters`** | `queryReadinessSimCompleters` — a query over `public.job_simulation_sessions`, **not** the progress table | `queryReadinessStarters` is `:964` |
  | `:441` | `steps.go:907-914` for the quote *"This DB signal is the only real 'has started' check"* | the tail of a different function + the `queryReadinessSimCompleters` doc comment | the quoted sentence is at `steps.go:962` |
  | `:445` | `readiness.go:638-652` for **`queryInCycleStep1Completers`** | `cycleAudienceTags` + the `audienceScope` doc comment | `queryInCycleStep1Completers` is `:684` |

- **Why it matters rather than being cosmetic:** `:436` is the load-bearing citation of a *correction*.
  The corpus sentence says the filter reads *"a row in the **progress** table, never a `user_skill_evidences`
  row"* and cites `steps.go:915-938` as proof — and at the checkout that range is the function that reads
  **sessions**. A reader following the anchor lands on evidence for a different proposition (§5 rule 46:
  an anchor that resolves but names the wrong construct; §5 rule 10: reading only the cited line).
- **Verified counter-check (why I hesitated):** all five resolved **exactly** at `b948604f`, the `app` sha
  the frozen instrument table names — `queryInCycleStep1Completers` `:638`, `keepInCycleStep1` `:664`,
  `keepStartedMembers` `:684`, `queryReadinessStarters` `steps.go:915`. This is pure line drift injected by
  the clone advancing between readings (rule 41a: *"it has since been fetched" is not a repair, it is a NEW
  ground truth*), and the constructs are named by name in the same sentences. I book it because the
  delivered instrument lists *"wrong line anchors that point at the wrong construct"* as a BLOCKER
  example, and because one of the five now points at a semantically opposite query.
  Booked as **one** finding, not five, because it is one cause and one repair.
- **Confidence:** high (measured at both refs).

### B3 — `cms.md:55` and `cms.md:196` — "the literal's ONE occurrence anywhere in the clone set" is 6, measured against the same denominator the sentence itself uses

- **Anchor:** `corpus/services/cms.md:55` — *"the literal's one occurrence anywhere is a past-tense markdown
  KB page, `app/knowledge/service-dependencies.md:52`"*; and `cms.md:196` — *"The literal's one occurrence
  anywhere in the clone set is a **markdown KB page**…"*
- **Why it is false:** `git grep -F 'backend.internal.anthropos:8081' HEAD` per clone at its own ref returns
  **6** occurrences, not 1 — `stack-demo/app` = 1 (the KB page, correctly named) and
  **`stack-demo/rosetta-extensions` @ `09d06070` = 5**:
  `stack-core/tests/fixtures/repair_leak/pre/corpus/services/cms.md:32` and `:157`,
  `stack-core/tests/fixtures/repair_leak/post/corpus/services/cms.md:32` and `:157`, and
  `stack-core/tests/test_platform_predicate_guard.py:435`.
  The scope word is the sentence's own: the *same* sentence counts *"all 44 tracked `.tf` files in the 13
  `stack-demo` repos"*, and `rosetta-extensions` **is** one of those 13 (I re-derived the 44: app 5, cms 4,
  graphql-wundergraph 4, jobsimulation 6, messenger 4, next-web-app 2, roadrunner 4, **rosetta-extensions 2**,
  sentinel 4, storage 5, studio-desk 4, ant-academy 0, platform 0 = 44 ✓; the 59 raw-filesystem figure
  reproduces too ✓).
- **Why I hesitated (and booked it anyway):** the load-bearing half — *no `.tf` file in any clone names it* —
  is **exactly right**, and the five extra hits are frozen copies of this corpus's own older `cms.md` used as
  guard-test fixtures plus one test-body string. A reader is not misled about production. But the passage
  is a derivation-showing one whose whole force is a cardinality, and §5 rule 24 is explicit that the
  incompleteness of such a passage lives in the SET, not the arithmetic. `grep`ing it as instructed returns
  6, which teaches the reader the corpus is unreliable at exactly the place it is being most careful.
- **Confidence:** medium.

---

## Minors

- `ai-readiness.md:84` — cites `AIReadinessClient.tsx:133-134` for the quoted
  `const { orgEnabled } = useAiReadinessEnabled(true)`; at the checkout `8297c684` that statement is at
  **`:135`** (`:133-134` are comment lines). It was exactly `:133` at `bb3313bc`. Line drift, +2.
- `ai-readiness.md:478` — cites `AIReadinessClient.tsx:153-154` for `effectiveCycleId = selectedCycle ??
  activeCycle?.id ?? latestClosedCycle?.id`; at `8297c684` it is **`:155-156`** (was `:153-154` at
  `bb3313bc`). +2.
- `ai-readiness.md:480` — cites `:166-170` for the `cyclesQ.isFetched` gate; at `8297c684`
  `enabled: featureOn && cyclesQ.isFetched` is at **`:171`** (was `:169` at `bb3313bc`). +2.
- `ai-readiness.md:335` — cites `:599` as where `SHOW_SECONDARY_TABS` is read by the tab filter; at
  `8297c684` it is **`:601`** (`:599` is `tab.key === 'how' ||`). The companion `:78` anchor in the same
  sentence is still exact at both refs. +2.
  *(These four are the same clone-advance drift as B2 but only ±2 lines and inside the same statement
  block, so they are booked as MINOR rather than folded into B2. Note the doc explicitly re-derived its
  `urls.ts` / `useNavbarSections` / `HowWeMeasureTab` anchors at `8297c684` and those are all byte-exact —
  the four above are the ones the re-derivation did not reach.)*
- `chronos.md:186` spells `docker compose up -d chronos` in **runnable** form while warning that it fails.
  The statement is accurate (`chronos` is not a compose service, so it errors rather than silently
  no-op'ing), but §5 rule 30's corollary — *do not spell a dead command in runnable form, even to warn
  about it* — applies to the shape.
- `chronos.md:22` gives the codebase as `stack-dev/chronos`; the doc's own banner (`:9`) says the repo is no
  longer cloned by `make init`, and `repos.yml` @ `0c91421d` confirms it (4 entries). Harmless historical
  framing, flagged only because the path reads as a live location.

**Out-of-partition observation (not booked against my files, but a real cross-file split):**
`corpus/ops/demo/stories-spec.md:466` says the `AIReadinessFunnelSeeder` writes *"`jobsimulation.actors` +
`jobsimulation.interactions`"*. The pinned seeder writes **`public`**:
`stack-seeding/seeders/ai_readiness_funnel.go:878` `CopyRowsIdempotent(ctx, "public", "actors", …)` and
`:886` `CopyRowsIdempotent(ctx, "public", "interactions", …)` @ `09d06070`. **`ai-readiness.md:599-600` (my
file) is the correct side** — the schema qualifier there is `public`, and the app migration
`20260722081626_jobsim_data_model.sql:82` creates `interactions` in `public`. Routing per §5 rule 19
(repair by claim, not by file).

---

## Positively cleared (audited zeros)

Every item below was **ENUMERATED** unless it says RE-DERIVED.

**`corpus/services/README.md`**
- **ENUMERATED** — the seven folded domains all exist at `app ad9f3c49`:
  `internal/{skiller,skillpath,jobsimulation,cms,storage,messenger,customeriosync}/` — 7 for 7.
- **ENUMERATED** — *"`app/internal/roadrunner/` exists at no ref — at `ad9f3c49` no path in the whole tree
  matches `roadrunner`"*: `git ls-tree -r --name-only ad9f3c49 | grep -ci roadrunner` = **0**.
- **ENUMERATED** — Judge0 absorbed as `internal/jobsimulation/runner/` (`languages.go`, `runner.go`), wired
  at `internal/jobsimwiring/wiring.go:123` = `runnerManager := jsrunner.NewRunnerManager(getenv("JUDGE0_API_KEY"),
  getenv("JUDGE0_BASE_URL"))` — the cited line is exactly that call.
- **ENUMERATED** — `roadrunner/terraform/main.tf:19` `service_desired_count = 1` ✓ (also `= 1` at that
  repo's `origin/main`); `cms/terraform/main.tf:39` `= 0` ✓ (both refs).
- **ENUMERATED** — jobsimulation's M810 row, graded **at the ref the cell names** (`6092c6d2`): at that
  commit `service_desired_count` appears **nowhere** in `terraform/main.tf` (`git grep` exits 1) and
  `:15-22` is the *"M810 decommission — the standalone jobsimulation ECS service is gone"* comment block.
  ✓ Exactly as claimed. (At the *checkout* `462343b0` the module block is still present at `:40` — the pin
  is doing real work here and the row is right to carry it.)
- **ENUMERATED** — compose @ `0c91421d` declares **5** services (`sentinel :5`, `backend :28`,
  `studio-desk :112`, `next-web-app :143`, `gotenberg :170`) + `common.yml`'s `postgresql :2` / `redis :24`
  = **7 effective**; `repos.yml` carries **4** entries (app, sentinel, next-web-app, studio-desk). ✓
- **ENUMERATED** — `d11a403` (2026-08-03) removed **all three** of cms / jobsimulation / roadrunner from
  `docker-compose.yml` **and** from `repos.yml` (I read the full `repos.yml` diff: all three `- name:`
  blocks deleted). Worth recording: the commit's own message says roadrunner's clone entry *"was already
  gone"* — **the commit message is wrong and the corpus is right.**
- **ENUMERATED** — messenger row: `messenger/internal/flow/flow.go:72-104` @ `fa47850` is exactly
  `h.subServer.AddSubscriber("backend", sub.AddHandler(…))` closing at `:104`, and I counted the members:
  **21 live** `pubsub.EventHandler(...)` entries with exactly one commented out
  (`OrgJobSimulationAssignmentPastDueHandler`) — 21 is the live count, not the raw `grep -c`.
- **ENUMERATED** — `MESSENGER_RPC_ADDR` appears in **no** repo (0 across all 15 trees, exact string);
  `git -C stack-demo/platform log -S 'MESSENGER_RPC' --oneline 0c91421d` = **0**, positive control
  `-S 'SKILLER_RPC'` = **7**. Both numbers reproduce exactly.
- **ENUMERATED** — the `platform-migration-status.md:101` cross-reference lands on the `customerio-sync`
  row and that row does carry the *"prod half is asserted from `app`'s side only"* statement.
- **ENUMERATED** — graphql row: `915da06` is *"fold cms subgraph into backend"* and
  `supergraph-config-prod.yaml` at `915da06` lists `backend` alone ✓ ONE subgraph; `2adcf71` (2026-07-31)
  is PR #23 *"drop-wundergraph"* ✓.
- Cited platform shas all resolve with the stated subjects/dates: `045857c`, `fdfa189`, `d11a403`,
  `838d907`, `0c91421`, `2adcf71`, `0dab54d`.
- `storage.md` row's *"ECS service deleted, not scaled to zero; module survives to keep buckets/CDN under
  `prevent_destroy`"*: **true at storage `origin/main` `9f8cb532`** (18-line `main.tf`, module block gone,
  comment states exactly that). At the *checkout* `4ce8ece5` the 100-line module with
  `service_desired_count = 1` is still present. I cleared it because the claim is about **production**, and
  the corpus's own citation ladder (`CITE_REF=auto` → `origin/main` first, §5 rule 41) resolves there. Flagged
  here rather than booked so the discrepancy is on the record.

**`corpus/services/skiller.md`**
- **ENUMERATED** — `app/internal/rpc/skillerrpc/` exists (`skiller.go`, `skiller_test.go`);
  `internal/web/backend/graphql/graph/schemas/skiller_taxonomy.graphqls` exists.
- **ENUMERATED** — the six named Ent schema files all exist: `skill.go`, `jobrole.go`,
  `skill_embeddings.go`, `job_role_embeddings.go`, `category.go`, `specialization.go`.
- **ENUMERATED** — *"`categoryTree` / `fullCategoryTree` were dropped entirely (not ported)"*: the **only**
  occurrence in the graphqls tree is `skiller_taxonomy.graphqls:7`, a comment saying they *"stay unported —
  no consumers."* No query definition exists.
- **ENUMERATED** — 8 `ContentLanguage`s: `internal/data/ent/schema/skiller_mixins.go:264-273`
  `validTranslationLanguages` = {english, italian, spanish, french, german, dutch, japanese, portuguese} = **8**;
  tables `skill_translations` / `job_role_translations` created at
  `terraform/migrations/20260615130000_skiller_taxonomy.sql:253` / `:192`.
- **ENUMERATED** — embeddings: `extensions.vector(1536)` + `USING IVFFLAT` on both `skill_embeddings` and
  `job_role_embeddings` (`20260615130000_skiller_taxonomy.sql:53`, `:61`, `:243`, `:251`).
- **ENUMERATED** — the taxonomy floors: I opened the capture manifest itself.
  `.agentspace/snapshots/taxonomy/<digest>/manifest.json` carries `source: primary-read`,
  `public_only: true`, `predicate: org-null`, `captured_at 2026-06-29T14:43:00Z`, and
  `public.skills` `row_count: 42790` with `filter: organization_id IS NULL` — every field the doc names,
  verbatim. Consistent with `shared_libraries.md:234-235` (18K **refuted**, 60K **unverified**).
- **ENUMERATED** — platform `21429b7` (2026-07-07) is the commit that removed skiller from compose +
  `repos.yml`.

**`corpus/services/ai-labs.md`** *(the cleanest file in my set — every scalar I checked reproduced)*
- **ENUMERATED** — `git describe --tags ad9f3c49` → **`v1.369.0-7-gad9f3c498`** ✓ (the doc's exact string);
  `git describe --tags 2035f9a4` → **`v1.369.0-2-g2035f9a40`** ✓; newest tag is `v1.369.0` ✓.
- **ENUMERATED** — *"no `checkout.session.completed` webhook (0 occurrences in Go source)"*:
  `git grep 'checkout.session.completed' ad9f3c49 -- '*.go'` = **0**; the only hits tree-wide are planning
  markdown. And I verified the **predicate**, not just the count: the webhook switch at
  `internal/web/backend/api/api.go:317-390` handles exactly `customer.created` +
  `customer.subscription.{created,updated,deleted}` and nothing else — matching the doc's enumeration.
- **ENUMERATED** — *"`internal/labs/` imports `internal/credits` nowhere"* = **0** hits.
- **ENUMERATED** — `internal/web/backend/credits/handler.go:12` is *"POST /credits/purchase was removed in
  Wave 13"* — the cited line, exactly. `Register()` mounts **two** routes (`/balance`, `/transactions`),
  matching the doc; the platform's own *"the three /credits/* routes"* comment at `:29` is the stale one.
- **ENUMERATED** — org-admin gating is real: `internal/web/backend/backend.go:273-277` mounts
  `e.Group("/credits", cors…, authnEcho.EchoAuthnMiddleware…, cbGate)` with `:223` stating *"both
  /coursebuilder and /credits are org-admin gated."*
- **ENUMERATED** — the cost model: `creditCost` = {build 5, refine 1, translate 1} (`cost.go:88-90`);
  `MarginMarkup = 1.40`; `PricePerCreditUSD = 0.45`; `purchasePackages` = starter 50 / team 200 / scale 500;
  `DefaultSeedBalance int64 = 500`. (Note the doc took the **map**, not the package doc-comment above it,
  which still says *"course.refine → 5 credits"* — the corpus is right and the code comment is stale.)
  `TestPricingDerivation` exists at `cost_test.go:103`.
- **ENUMERATED** — every named table has an Ent schema: `lab.go`, `lab_body.go`, `lab_session.go`,
  `organization_credit.go`, `credit_transaction.go`, `subscription.go`, `stripe_customer.go`,
  `org_subscription.go`. All five named migrations exist by filename.
  `organization_credits` UNIQUE on `organization_id` (`organization_credit.go:86-87`);
  `credit_transactions` partial-unique `(organization_id, action_type, ref_id) WHERE ref_id IS NOT NULL`
  (`credit_transaction.go:117-119`, and the DDL at `20260717151144.sql:52`).
- **ENUMERATED** — `lab_sessions` deliberately omits `PrimaryKeyMixin` and takes a **String** `id` supplied
  by labs-api (`lab_session.go:22-40`, comment verbatim); `lab_bodies` = EN base + **6** overlays
  (de/es/fr/it/nl/pt — `lab_body.go:12-13`, `:48-49`).
- **ENUMERATED** — `LabsTier` {essential, professional, frontier}; the five `CapReason`s
  (`labs_budget.go:247-251`) exactly as listed; `spend_reconciler.go:60` `Interval: 30 * time.Second`;
  labs-api default `:7070` (`labsapi/client.go:6`); `LABS_API_PLATFORM_TOKEN` read at `main.go:855`;
  `cmd/{labsimport,labskey,labsdemo}` all exist, labsdemo on `:8099` (`cmd/labsdemo/main.go:115`);
  `LabSessionService` methods Create/Get/List/Cancel/ReportEvent all present; the RPC wrapped in
  `authn.HTTPAuthnMiddleware` at `main.go:1338`; `/v1/labs` + `/v1/labs/bodies` behind
  `apiKeyAuthMiddleware(apiKeys, "labs:write")` and `GET /v1/labs/:slug/workspace.tar.gz` outside it
  (`labs_admin.go:31-40`); `stripe-go/v74 v74.30.0` (`go.mod:64`).
- **ENUMERATED** — the `v6.0` shared-purse release exists as a knowledge-plan release with milestone dirs
  **m600 … m607** (+ `m601b`), i.e. M600–M607 as claimed. (Directory listing only.)
- Corroboration for the *"there IS a separate repo"* retraction at `:4-6`: `lab_session.go:6` cites
  *"See AI-Labs/docs/PHASE_B_LABSESSION.md."*

**`corpus/services/cms.md`**
- **ENUMERATED** — `cms/terraform/main.tf` is **191 lines** with `service_desired_count = 0` at `:39` ✓.
  `6efa1d5` exists with the subject *"chore(ci): drop build-production — the cms ECR repository is
  decommissioned (M810)"*, merged `f38c0c4` (2026-08-04), and it deletes
  `.github/workflows/build-production.yml` (18 lines). Its body contains, **verbatim**, both phrases the
  corpus quotes: *"M810 deletes `module "cms_euwest1"` from the platform's services.tf, which destroys the
  ECS service and the production-cms ECR repository"* and *"would try to push an image into a registry that
  no longer exists."* The *"report both, assert neither"* framing is exactly right.
- **ENUMERATED** — the subgraph ladder, re-derived from `supergraph-config-prod.yaml` at four refs:
  `749dc86~1` = **5** {backend, skiller, jobsimulation, cms, skillpath} → `749dc86` = **4** →
  `7c17e63` = **3** {backend, jobsimulation, cms} → `915da06` = **1** {backend}. So *"3 → 1, not 2 → 1"*
  is correct and `915da06`'s own subject line ("2→1") is the wrong one. `915da06`'s diffstat deletes
  **both** `schemas/cms.graphqls` (−762) and `schemas/jobsimulation.graphqls` (−860).
- **ENUMERATED** — the studio-room block, at the nested repo's **own** ref `aeec036a` (the host `app` ref
  returns 0 for every predicate — the trap the ADDENDUM names): `services/ai.py:705-708` is exactly the
  three-entry `providers = {'openai':…, 'azure':…, 'anthropic':…}` dict; `:1-2` imports only
  `openai`/`anthropic`; `tools/pdf2md.py:24` `from mistralai import Mistral`, client at `:96`,
  `model="mistral-ocr-latest"` at `:127`; `tools/r3.py:139` names `pdf2md.py` in the dependency list, `:190`
  resolves it, `:199-206` builds and runs the step-2 command. `git -C stack-demo/app/studio grep -ci mistral
  aeec036a` sums to **22 hits across 3 files** (requirements.txt 1, tools/pdf2md.py 18, tools/r3.py 3) —
  the doc's number, exactly.
- **ENUMERATED** — `requirements.txt` is **9 packages verbatim** in the doc's order, with `mistralai` at
  line 8, and **no `python-docx`**. The `cms/studio` and `app/studio` copies are **byte-identical**
  (sha256 `a4e780b7…` both).
- **ENUMERATED** — the Go Mistral side: `internal/cms/studio/markdownManager.go:10` imports
  `internal/cms/studio/mistralocr` and `:30` is `return &MarkdownManager{ocr: mistralocr.New(aiKey)}, nil`
  — **at BOTH `2035f9a` (the named pin) and `ad9f3c49`**, as the doc claims. `studioManager.go:531` is the
  `// supported ocr content types for mistral ocr` comment, `:583` the `NewMarkdownManager(os.Getenv(...))`
  call, `xlsx.go:13` the *"Mistral OCR doesn't accept xlsx"* comment. `app/.gitignore:79` is `studio/*`.
- **ENUMERATED** — *"Go execs two studio scripts and neither is `pdf2md.py`"*: `studioManager.go:119`
  `"studio/gen.py"` and `:1045` `"studio/postgen.py"` @ `b948604f`, the ref the sentence names.
- **ENUMERATED** — `gen.py:484-492` registers **exactly nine** arguments (`-i/--interactive`, `-m/--media`,
  `-f/--force`, `--simid`, `--branch`, `--prompt`, `--annotations`, `--pipeline`, `--blueprint`) and
  `parse_argument` at `gen.py:18-28` calls `parse_known_args` and merges the leftovers — so a stray
  `--template` is absorbed silently. `studio/CLAUDE.md:12-14` is the quoted `python gen.py …` block.
- **ENUMERATED** — `app/main.go:1205-1211` @ `b948604` is precisely the *"Additive + DORMANT: external
  callers (messenger) keep hitting the standalone cms via CMS_RPC_ADDR until the M809 re-point"* comment.
- **ENUMERATED** — `app/internal/skillpath/session.go:205-207` @ `ad9f3c49` is
  `// cms-in-app deseam: cms is in-process …` + `u.cms.GetSkillPathDomain(ctx, skillPathId, version)` —
  exact.
- **ENUMERATED** — the six cms tables re-created in `public` by
  `terraform/migrations/20260724132049_cms_data_model.sql`: `similarities`, `similarity_categories`,
  `similarity_features`, `similarity_skills`, `studio_documents`, `studio_tasks` — 6 for 6.
  `app/internal/cms/` carries directus/similarity/studio/library/importer/exporter/aivideo/contentread/
  jobsimimport/rpcsrv/worker + `wiring.go` — all present.
- **ENUMERATED** — `cms/go.mod:3` `go 1.26.4`; `cms/cmd/root.go:77` `cmp.Or(os.Getenv("PORT"), "8080")` and
  `:78` `RPC_PORT`/`8081`; `cms/Dockerfile:2` `FROM golang:1.26-bookworm AS build`, `:23`
  `FROM python:3.11-slim`. The 8090/8091 pair: at `2adcf71` `docker-compose.yml:154-155` publishes
  `8090:8090`/`8091:8091`, `:169` `PORT=8090`, `:173` `RPC_PORT=8091` — **all four anchors exact at the
  named ref**, and gone at `0c91421d`.
- **ENUMERATED** — `REDIS_CMS_CACHE_INDEX` default DB **5** (`app/main.go:1107-1108`);
  `POST /api/webhook/directus` mounted at `internal/web/backend/backend.go:324` with
  `DIRECTUS_WEBHOOK_SECRET` fail-closed (`main.go:1198-1199`, `backend.go:137`).
- **ENUMERATED** — `DIRECTUS_DATA_CONSUMERS = ("cms", "backend")` at `stack-core/gen_override.py:58` in the
  **pinned** rext clone `09d06070` — the doc's *"`DIRECTUS_DATA_CONSUMERS` now names both"* holds on the
  tree a stack actually runs.
- The `.tf` census supporting `:55`/`:196` — **44 tracked / 59 raw / 0 hits in `*.tf`** — RE-DERIVED and
  **reproduces exactly**; only the *"one occurrence anywhere"* clause fails (B3).

**`corpus/services/ai-readiness.md`**
- **ENUMERATED** — the package refactor: `internal/aireadiness/` exists and contains every file the rename
  table promises (`readiness.go`, `scoring.go`, `csv.go`, `steps.go`, `narrative.go`, `cycles.go`,
  `compare.go`, `diagnosis.go`, `provision.go`, `defaults.go`, `how_we_measure.go`, `manager.go`,
  `recommendation_engine.go`, `recommendation_signals.go`, `emailoverride/`, `emailpreview/`,
  `notifications/`, `testdb_test.go`) and **no `how_we_measure_v2.go`** ✓. `LoadMembers` /
  `LoadMembersByUserIDs` both **stayed** in `internal/workforce/members.go:349` / `:353` ✓. Commit
  `4c28365f` exists with the exact quoted subject, 2026-07-22 ✓.
- **ENUMERATED** — gate 1: `internal/data/ent/enum/organization_settings.go:47` is
  `OrganizationSettingAIReadiness OrganizationSetting = "ai_readiness"` — the cited line, exactly.
- **ENUMERATED** — gate 2: `useAiReadinessActive.ts:22` is
  `const rawFlag = useFeatureFlagEnabled(AI_READINESS_FLAG);` and
  `aiReadiness.constants.ts:26` is `export const AI_READINESS_FLAG = 'flag_ai_readiness';` — both exact,
  and the surrounding code matches the doc's quoted `stickyFlag.current === true` mechanism.
  `AIReadinessClient.tsx` contains **0** `posthog` and **0** `flag_ai_readiness` (verified with the file's
  other greps succeeding in the same pass).
- **ENUMERATED** — the archetype math, read whole: `scoring.go:26` `archetypeHighBand = 75`, `:30`
  `archetypeLowCeil = 50`, `classifyArchetype` at `:46-57` giving Champion (both ≥75) / Standby (both ≤50) /
  Explorer (`usage >= knowledge`, so an exact tie **is** Explorer, as the doc says) / Hidden Talent; bands
  `None 0-24 / Low 25-50 / Medium 51-74 / High 75-100` at `:63-66`.
- **ENUMERATED** — `defaults.go`: `defaultReadinessSkills` is **31** entries, **19 @ 1.0** (`:31-49`) +
  **12 @ 0.5** (`:51-62`) — I enumerated the set, not the `grep -c`. Denominator **25.0** follows.
  `defaultReadinessSims` is exactly **3**: {simulation,"tech"}, {simulation,"business"}, {interview,"both"} ✓.
- **ENUMERATED** — CSV is **15** columns: `AIReadinessCSVColumns` at `csv.go:17-33` has 15 members.
  (The function's own doc comment at `:35` still says *"19-column"* — the corpus took the **set**, not the
  comment, which is the right call and is exactly what §5 rule 24 asks for.)
- **ENUMERATED** — all **13** named tables exist in `terraform/migrations/`: the 7 from `20260618160827.sql`,
  `ai_readiness_diagnose_narratives`, `ai_readiness_text_translations`, `ai_readiness_recommendations`,
  `ai_readiness_email_overrides`, `ai_readiness_notification_logs`, `ai_readiness_notification_optouts`.
  *"one active cycle per org (partial unique index)"* is literally
  `CREATE UNIQUE INDEX "aireadinesscycle_organization_id" … WHERE ((status)::text = 'active'::text)`.
  `interview_aggregated_reports` carries `(organization_id, sim_id, report jsonb, session_count)` ✓.
- **ENUMERATED** — the interview KPI set: `usageDimSpecs` (`how_we_measure.go:1160-1181`) is exactly the
  five ids `avg_adoption, avg_transformation, avg_originality, avg_depth, avg_ownership`; the seeder's
  `aiReadinessUsageKPIs` (`ai_readiness_interview_report.go:185-189` @ `09d06070`) emits exactly those five,
  and `:149` reads *"All five are always emitted here."* — the anchors and the count both hold on **both**
  sides. The M250 "was four, is five" correction is right.
- **ENUMERATED** — `how_we_measure.go:285-287` is verbatim
  `FROM public.interactions i` / `JOIN public.job_simulation_sessions s ON i.session_id = s.id` /
  `JOIN public.ai_readiness_sims ars`; `computeCycleTotals` is inside the cited `:253-261` window (`:260`).
- **ENUMERATED** — the `{email, call}` claim: `internal/data/ent/interaction/interaction.go:92-99`
  `ActionTypeValidator` accepts **exactly** `"email"` and `"call"`. (The broader `interactions.ActionType`
  Go type carries five constants — Call/ChatMessage/Email/StorageUpload/ValidationRequest — so the claim is
  true **for this table's ent enum**, which is what the sentence is about. Checking only the Go type would
  have produced a false booking.) `interaction_source_target_must_be_different_check CHECK (source_id <>
  target_id)` exists at `20260722081626_jobsim_data_model.sql:94`.
- **ENUMERATED** — 6–11 interview turns: `aiReadinessMinTurns = 6` / `aiReadinessMaxTurns = 11`
  (`ai_readiness_funnel.go:584-585` @ `09d06070`), and the writes go to **`public.actors`** / **`public.interactions`**
  (`:878`, `:886`) — the corpus's schema qualifier is right.
- **ENUMERATED** — the M219 correction block's next-web anchors, all at `8297c684`:
  `AIReadinessHero.tsx:88` `if (!air.deadline) return null;` ✓ exact;
  `deriveMode` at `apps/web/src/components/ai-readiness/useAIReadiness.ts:48-62` ✓ exact **and** treating a
  null deadline as passed (`:55-57`) — note the corpus's bare filename disambiguates correctly between the
  two `useAIReadiness.ts` files (§5 rule 36's uniqueness hazard, navigated right);
  `useWorkforceAIReadiness.ts:23-27` calls `/ai-readiness` with `searchParams: { tag }` and **no** `cycle`
  param ✓; `WorkforceNewClient.tsx:125-151` is the 5-tab items array with no readiness tab ✓.
- **ENUMERATED** — the surfaces block: `urls.ts:51` **is** `AI_READINESS_URL` at `8297c684` and `:52` **is**
  `TALK_TO_DATA_URL`; at `bb3313bc` the constant was at `:50` and `:52` was `ORGANIZATION_FEEDBACK_URL` —
  the doc's own `:52`-was-never-right correction reproduces exactly, at both refs.
  `useNavbarSections.tsx:4` (import), `:401-408` (`aiReadinessMenuItem`), `:403` (`key:`), `:569` (the
  `showAIReadiness ? … : null` gate) — all four exact. The repo-wide grep claim is exact too: **two**
  source files + the KB page `knowledge/ai-readiness/frontend-architecture.md:15`, nothing else.
  `e2e/specs/web.ai-readiness.spec.ts` exists.
- **ENUMERATED** — `dae0fb2f7` (2026-07-13) deletes `AIReadinessContainer.tsx` (103),
  `AIReadinessIntro.tsx` (220) and `AIReadinessView.tsx` (330) = **653** lines ✓ the doc's "−653".
- **ENUMERATED** — the "Handled for you" parenthetical, which is the most anchor-dense claim in the file
  and is **entirely correct** at `8297c684`: `HowWeMeasureTab.tsx:1879` opens
  `{/* ===== C · Handled for you this cycle ===== */}`, label `:1903`, then exactly three cells —
  `skillsMapped` `:1915`, `handsOnMinutes` `:1921`, `interviewMinutes` `:1927`; the file is **1,989** lines;
  `grep -c interviewQuestions` over it = **0**; and `apps/web/src/hooks/useAIReadiness.ts:326` is
  `interviewQuestions: number;` inside `export interface AIReadinessCycleTotals` at `:323-330`.
- **ENUMERATED** — `GetAIReadinessWithOptions` at `readiness.go:289`; route 1 at `:290-297`; route 2 at
  `:307-312`; the fall-through `buildLiveResponse` at `:314`; `computeOrgBreakdowns` at `:330`;
  `computeTier1` at `:139`; `keepInCycleStep1` selected at `:388` and `keepStartedMembers` at `:390`.
  (Only the five ranges in B2 have drifted.)
- **ENUMERATED** — the rext side, in the **pinned** clone `09d06070`: `LegacyReadinessPaths()`
  (`cockpit.go:135`) + `ValidateCockpitManifest` (`:161`, which reads the legacy map at `:162`);
  `TestAIReadinessFunnelSeeder_DayZeroHeroGetsNoSignals` (`ai_readiness_funnel_test.go:219`);
  `seat_append_test.go`; `contentref.go`; `ai_readiness_evidence.go`;
  `AIReadinessSimSkillsSeeder` (`seeders/ai_readiness_sim_skills.go`);
  `stack-verify/e2e/lib/coverage-manifest.ts`; the demopatch manifest
  `app-aireadiness-snapshot-loadmembers.yaml` with `path: internal/aireadiness/readiness.go` at **`:42`**
  and the `# v2.7 M254 RE-POINT:` header at **`:33`** — both exact — and
  `demo-stack/tests/test_aireadiness_snapshot_loadmembers_m254.py`. So the D-07 *"the re-anchor has LANDED"*
  paragraph is true on the tree a stack runs.
- **ENUMERATED** — `Persona.AIReadiness` (`blueprint/blueprint.go:200`) with the closed enum
  `AIReadinessNotStarted = "not_started"` (`:316`) and a near-miss test (`stories_test.go:202`);
  `aiReadinessStageFor` (`seeders/ai_readiness_funnel.go:205-226`) is precisely
  not_started → 0, manager → 0, struggling → 1, default → 3, with the declared check **before** the derived
  default; `seedAIReadinessOrgFunnel` at `:166`.
- **RE-DERIVED** — the demo funnel figure *"78.4% (≈156 of the 199 frozen snapshots)"* is consistent with
  `seeding-spec.md:492` and `stories-spec.md:466` (156 / 21 / 22). Labelled RE-DERIVED, not ENUMERATED: I
  cross-checked corpus twins, not a live DB.
- **RE-DERIVED** — the *"5 Playthroughs"* count at `:647`: `playthroughs.md:106` puts the `ai-readiness`
  product at **4**, and `pt-onboarding-aireadiness-guided` (M256 iter-26) is the 5th readiness *journey*,
  living under the onboarding product. 4 + 1 = 5 reads consistently. Not booked.

---

## What I could not settle

1. **Whether production terraform still names `http://backend.internal.anthropos:8081` (B1's other half).**
   The declaration lives in `infrastructure/terraform/production/services.tf`; that repo has never been in
   any clone set. What would settle it: a read-only clone of `anthropos-work/infrastructure`, or a
   `terraform output` / task-definition dump. Note the corpus already has the right posture for this in
   `cms.md` — *report both, assert neither* — and `skiller.md` is the site that breaks it.
2. **Almost all of `chronos.md`'s body (lines 13–245).** The `chronos` repo is not in `repos.yml` and is not
   in `stack-demo/`, so Go 1.25, sqlc, Cobra, the `timers` DDL and its four indexes, the four timer states,
   the `ScheduleTimer`/`CancelScheduledTimer` signatures, the `EventTimerWentOff` proto, the triggerer's
   1 s interval / 3 s timeout / `LIMIT 10` / `FOR UPDATE SKIP LOCKED` / Sentry heartbeat, the env-var and
   CLI-flag tables and both code examples are **all unverifiable from this clone set**. I verified only what
   reaches outside it: platform `045857c`, jobsimulation `09631fb2` (exact subject match), and
   `app/internal/jobsimwiring/worker.go:24-35` (the asynq import through the standard pool — a fair anchor
   for *"those Asynq workers now run in the `backend` process"*). What would settle the rest: a clone of
   `anthropos-work/chronos` at the last-push ref.
3. **GitHub repository archive states.** `README.md:79` (*chronos NOT archived, last push 2026-04-23*) and
   `:52` (*graphql-wundergraph repo ARCHIVED*) are GitHub API facts, not git-tree facts. I made no network
   call. What would settle them: `gh api repos/anthropos-work/{chronos,graphql-wundergraph}` → `.archived`,
   `.pushed_at`.
4. **`customerio-sync` claims in `README.md:45`** (Postgres `public` → Brevo; the "built straight from a
   GitHub URL" compose pattern). The repo has never been cloned; the compose service was deleted at
   `838d907`, so even the historical block is only reachable through git history I did not walk. The doc's
   own banner (`:14-17`) already discloses this as one-sided.
5. **`anthropos-work/AI-Labs` as a live repo** (`ai-labs.md:5`). No clone. Indirect corroboration only:
   `app/internal/data/ent/schema/lab_session.go:6` cites *"See AI-Labs/docs/PHASE_B_LABSESSION.md."*
6. **Every live-timing number in `ai-readiness.md`** — 2.09 s live recompute, 24 ms frozen read, 180 s
   timeout, 180 s → 19 ms demo-patch, and the M250/M254 LIVE-GREEN gate results. These need a running
   demo stack; the doc itself says *"Re-measure before relying on either number."* Same for
   `ai-labs.md`'s Bedrock COGS point estimates ($1.60 / $0.35), which are measurements, not code.
7. **`skiller.md:30` "app PR #989"** — PR numbers are not derivable from the local clone's log.
8. **Whether the four `AIReadinessClient.tsx` ±2 minors were correct when written.** They were exact at
   `bb3313bc` and are +2 at `8297c684`; I cannot tell which ref the author read, only that the file's
   *other* anchors carry an explicit `8297c684` re-derivation note and these do not.
