# Seat A — reading #25

## Ground truth actually read (per the ADDENDUM, which supersedes the frozen table)

Re-derived at this seat's open with `git -C <clone> rev-parse --short=8 HEAD`:

| repo | path | sha I read |
|---|---|---|
| app | `stack-demo/app` | `ad9f3c49` (checkout, == origin/main) — plus `b948604f`, `5ba17044`, `2035f9a4` where a claim pins them |
| app/studio (studio-room, NESTED own checkout) | `stack-demo/app/studio` | `aeec036a` |
| platform | `stack-demo/platform` | `0c91421d` |
| next-web-app | `stack-demo/next-web-app` | `8297c684` |
| sentinel | `stack-demo/sentinel` | `f2c46190` (+ `88bc5592` where pinned) |
| storage / messenger / cms / graphql-wundergraph / roadrunner / jobsimulation | as listed | `4ce8ece5` / `fa47850d` / `ca50c817` / `60c229f3` / `87d8d443` / `462343b0` |
| studio-desk | `stack-demo/studio-desk` | `41ee3575` |
| ant-academy | `stack-demo/ant-academy` | `22df69dd` |
| **rosetta-extensions — PINNED PER-STACK CONSUMPTION CLONE (the tree that adjudicates)** | `stack-demo/rosetta-extensions` | **`09d06070`** (+ `ab81527a` where a claim pins the prior pin) |

**I read the PINNED per-stack rext clone, not the authoring copy** (addendum / §5 rule 45). Every rext
anchor below was resolved in `stack-demo/rosetta-extensions`.

Instrument notes: this shell's `grep` is a `ugrep --ignore-files` function; I used `command grep` or
`git grep` throughout and ran a positive control beside every zero. One live instance of the
false-absence trap surfaced in my own pipeline: `git grep -E '\bNAME\b'` returns **0** on this git
(2.50.1, Apple) because the BRE/ERE engine does not honour `\b`; `-P` and `-w` both return the true
count. Every "0" I report was taken with `-w`/`-P` and a matching positive control.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/architecture/external_services.md` | 883 | yes |
| `corpus/architecture/security_compliance.md` | 281 | yes |
| `corpus/services/roadrunner.md` | 206 | yes |
| `corpus/architecture/dependency_map.md` | 103 | yes |
| `corpus/services/TEMPLATE.md` | 46 | yes |
| **total** | **1519** | (matches the assignment) |

I also read `corpus/ops/platform-alignment.md` §5 in full (rules 1–48, Trap E, and the trailing
sections), as the briefing requires.

---

## Blockers

### B1 — `dependency_map.md` attributes a statement to `storage.md:40,47` that `storage.md` does not make anywhere, at any line

- **Anchor:** `corpus/architecture/dependency_map.md:19` — the ~~Storage~~ row:
  > "While the service existed it declared `depends_on: postgres, redis` (both `service_healthy`) and
  > **read neither**: no `DB_CONNECTION`/`REDIS_ADDR` in its compose env, no redis in its `go.mod`,
  > **which is what its own doc says at [`storage.md:40,47`](../services/storage.md)**."

- **Why it is false:**
  - **The anchors resolve to nothing relevant.** `corpus/services/storage.md:40` is
    `> being when the clone advanced to \`ad9f3c49\`.)` — the closing half of a parenthetical about
    which sha the *demo build pin* is. `corpus/services/storage.md:47` is a **blank line**.
  - **The cited document makes no such statement at any line.** Measured with three independent
    instruments over `corpus/services/storage.md` (248 lines, 0 NUL bytes measured with
    `tr -dc '\000' < f | wc -c`):
    - `command grep -inE "redis|postgres|db_conn|depends"` → **0 lines**
    - the `ugrep` shell function, `grep -in redis` → **0 lines**
    - `python3` substring count over the decoded UTF-8: `edis` **0**, `ostgres` **0**,
      `DB_CONNECTION` **0**, `go.mod` **0**, `depends_on` **0**
    - positive control on the same file and the same pipelines: `storage` → **49**.
  - **It was never true.** `git log -S 'no redis in' -- corpus/services/storage.md` and
    `git log -S 'REDIS_ADDR' -- corpus/services/storage.md` and `git log -S 'Postgres' -- …` each
    return **zero commits** — `storage.md` has never contained the sentence it is being credited
    with. At the citing commit `904502c` the two anchors read
    *"Storage is stateless and owns no database…"* (`:40`) and *"**Database**: none…"* (`:47`), which
    support "owns no DB" but say nothing about Redis, `REDIS_ADDR`, `DB_CONNECTION`, `depends_on`, or
    `go.mod`. Those two lines have since drifted to `:44` and `:51` (the iter-102 repair added lines
    above them), so today the citation lands on a parenthetical and a blank line.
  - **The underlying facts are correct — only the attribution is not**, which is what makes this
    dangerous rather than merely sloppy: at `platform 0dab54d` the `storage` block really does declare
    `depends_on: redis (service_healthy), postgresql (service_healthy)`
    (`docker-compose.yml:126-130`), its `environment:` block (`:116-123`) really does carry no
    `DB_CONNECTION`/`REDIS_ADDR`, and `stack-demo/storage/go.mod` really has no redis requirement
    (`command grep -in redis storage/go.mod` → 0). A reader who follows the citation to check the
    surprising claim finds a blank line and has no way to tell whether the claim was verified or
    invented.

- **Confidence:** high. (I hesitated only over whether this is "line drift", which the protocol treats
  as MINOR — it is not: the target sentence does not exist anywhere in the cited file, at any ref, so
  re-pointing the anchor cannot repair it.)

### B2 — `external_services.md` cites a Go constructor, `mistral.NewMistral(...)`, that exists nowhere in `app` at the ref that grades it

- **Anchor:** `corpus/architecture/external_services.md:565`, the **Mistral (EU)** provider row:
  > "**OCR only** — `mistral.NewMistral(...)` in `app/internal/cms/studio/markdownManager.go:19` for
  > studio attachment → markdown"

  Restated at `corpus/architecture/external_services.md:599` (*Routing* item 5):
  > "the cms domain's Go client (`internal/cms/studio/markdownManager.go:19`, `studioManager.go:583`)"

- **Why it is false:** Neither cell names an `app` ref, so both grade at the checkout, `ad9f3c49`
  (addendum; briefing §"Which ref settles an `app` claim"). The `aeec036a` that appears later in the
  same cell is the **studio-room** repo's ref and is explicitly bound to a `git -C app/studio grep`
  command — it cannot date an `app/internal/**` claim, and a pin naming another repo's ref is not a
  pin on this one.
  - `git -C stack-demo/app grep -n "NewMistral" HEAD` → **0 hits, repo-wide**. Bare
    `command grep -rn "NewMistral" --include="*.go"` → 0 as well. Positive control:
    `git grep -n "mistralocr.New" HEAD` → `internal/cms/studio/markdownManager.go:30`.
  - `markdownManager.go:19` at `ad9f3c49` is a **comment inside a doc comment**:
    `// It used to take aiKey and then IGNORE it, reading os.Getenv("MISTRAL_API_KEY")`. The actual
    OCR-client construction is `return &MarkdownManager{ocr: mistralocr.New(aiKey)}, nil` at **`:30`**,
    inside `func NewMarkdownManager` at `:29`; the struct field is `ocr *mistralocr.Client` (`:14`).
  - The claim **was** true at `b948604f` — the stale sha the frozen instrument's table named:
    `git -C stack-demo/app grep -n "NewMistral" b948604f` →
    `internal/cms/studio/markdownManager.go:19: ai, err := mistral.NewMistral(nil, os.Getenv("MISTRAL_API_KEY"))`.
    The construct was refactored away when the clone advanced; the corpus was not.
  - Refs tried: `ad9f3c49` (false), `b948604f` (true), `aeec036a` (not an `app` ref — the nested
    studio-room checkout has no Go at all).
  - The **substance** survives — Mistral is still OCR-only in the cms studio domain — so this is a
    named-construct/anchor defect, not a mechanism defect.

- **Confidence:** medium. I hesitated because the passage is true at the sha the *previous* reading's
  ground-truth table named, and because the enclosing claim (Mistral = OCR only) is unaffected. I book
  it because (a) the addendum is explicit that unpinned `app` claims grade at `ad9f3c49`, (b) a named
  Go symbol that returns 0 hits repo-wide is the briefing's own BLOCKER example, and (c) the same
  wrong anchor appears twice, so a reader hunting the constructor fails twice.

---

## Minors

- `corpus/architecture/external_services.md:455` — the passage quotes
  *"Since cms-in-app the platform compose `graphql` service builds from the **production** Dockerfile"*
  and attributes it to `graphql-wundergraph/CLAUDE.md:39`. The verbatim string is at **`:33`** (the
  *Platform compose* table row); `:39` is the *Consequences* bullet, which asserts the same thing in
  different words (*"Since the compose stack builds from the production Dockerfile, a schema change in
  `app` does not appear at `:5050`…"*). Non-material because the reader landing on `:39` still finds
  the false claim the corpus is warning about — but the quotation marks promise `:39` and `:33`
  delivers. (`stack-demo/graphql-wundergraph` @ `60c229f3`, `CLAUDE.md` 89 lines.)
- `corpus/architecture/security_compliance.md:233` — `…validator/skills.go:53-64` for
  `calculateSkillScore`. The function is `:53-62`; `:63` is blank and `:64` is the *next* function's
  signature (`func (s skillsValidator) run(...)`). The cited behaviour (counts booleans) is at
  `:54-60`, and the `:75` divide is exact. Two-line overshoot, no factual consequence.
- `corpus/services/roadrunner.md:195` — *"`go test ./...` (also run at Docker build time, `Dockerfile`
  line 18)"*. `stack-demo/roadrunner/Dockerfile:18` is `RUN go test -v ./...` (the `-v` is dropped in
  the quote). Line number and the vacuous-pass conclusion are both correct (0 `*_test.go` files
  measured by `find . -name "*_test.go" | wc -l`).

---

## Positively cleared (audited zeros)

Labelled per the briefing: **ENUMERATED** = I derived the predicate's set from source independently
and compared cardinalities; **RE-DERIVED** = I only re-ran the arithmetic the document displays.

### `security_compliance.md` — the multi-tenancy fence (the one the protocol says has been wrong four times)

**ENUMERATED, and it is correct at `app ad9f3c49`.** I derived every set independently before touching
any sum (§5 rule 24), and separately re-ran the document's own `comm`/`xargs` derivation:

- `ls *.go` in `internal/data/ent/schema/` → **139**; `grep -l 'ent.Schema'` → **135**; the 4 non-schema
  files are exactly `database_types.go`, `mixin.go`, `skiller_mixins.go`, `skillpath_mixins.go`. ✔
- `OrganizationMixin{}` mentions → **30 files**, printed in full; **exactly one is commented**,
  `user_resource.go:22` (`// OrganizationMixin{},  // We need to work on this`) → live users **29**. ✔
  (I checked the *whole* enumerated list, not `grep -c`.)
- `OrganizationIDMixin{}` → **7**, and they are byte-for-byte the seven the doc names: `category`,
  `jobrole`, `similarity`, `skill`, `specialization`, `studio_document`, `studio_task`. The mixin is
  defined at `skiller_mixins.go:152`, declares `field.UUID("organization_id", …)` at `:158`, and
  declares **no `Policy()`** (`grep -n ") Policy()" skiller_mixins.go` → 0). ✔
- neither-mixin ∩ `"organization_id"` → **18**, printed in full. Removing the two the doc excuses
  (`org_membership.go`, `academy_feedback.go`) leaves **exactly** the 16 files the doc names, in the
  same set. ✔  The doc's own caveat also reproduces: the bare `*.go` glob returns **19**, the 19th
  being `skiller_mixins.go`. ✔
- Files declaring any `Policy()` → **exactly 4** (`mixin.go`, `org_membership.go`, `organization.go`,
  `user.go`) at **both** `ad9f3c49` and the pinned `5ba17044`. ✔
- **The conjunction the previous three generations failed** (§5 rule 17/24) — I tested every clause:
  `org_membership.go:172-188` really does declare its own policy ending
  `privacy.AlwaysDenyRule()` (`:185`) with `rule.AllowCurrentOrgEdgesOrSkipRule()` at `:179`/`:183`;
  `organization.go:56` declares `Policy()` using **neither mixin**, with
  `rule.FilterSameOrganizations()` at `:96`; `user.go:116` declares its own and filters by *user*;
  `academy_feedback.go` carries `UserMixin{}` at `:64` (owner filter `rule.FilterOwnerRule()` at
  `mixin.go:103`/`:113`) and an `organization_id` at `:129`. So **29 + Membership + Organization = 31**
  org-filtering, **16 + 7 = 23** with `organization_id` and no policy, remainder **81**. ✔
- The four tenant-scoped remainder members all resolve to the named construct:
  `lab.go:63`, `academy_chapter.go:84`, `academy_skill_path.go:78` each
  `field.String("tenant_eid")`; `skill_path_session.go:43` `field.UUID("tenant_id", …)`, and that
  fourth does carry `UserMixin{}` (`:26`) while the first three declare no `Policy()`. ✔
- `mixin.go:126` = `func (OrganizationMixin) Policy()`. ✔  `job_simulation_session.go:5`,
  `jobrole.go:18`, `category.go:15` all quote correctly. ✔
- `5ba17044` re-measurement (139 / 30 / 7 / four Policy files) reproduces exactly. ✔

### `security_compliance.md` — the EU AI Act retraction

**ENUMERATED.** `checkerEngines` really is stored and never read for dispatch (its only sites are
`criterion.go:34,72,90` and `validator.go:43,60,595` — a field decl, a param, an assignment, a field, an
init and a pass-through); the live dispatch is the hardcoded `switch critCheck.Engine` with
`case check.EngineLlm:` at `criterion.go:127` and `case check.EngineTextDiff:` at `:142`;
`validateLLM` → `NewLLMBulkChecker(c.logger)` at `:428`;
`basevalidator/templates/checkValidationBulk.tmpl` exists and its `:27` reads *"assess whether the
`<asset>` shared by the user (the player) meets or does not meet the … checks"* with
`check_id`/`feedback`/`success` fields. The "most, not all" carve-out holds:
`criterion.go:168` dispatches `validateCodeDiff`, defined `:450-476`, which sets `success` from
`cdiff.Diff == ""` — a pure string comparison, no model. `calculateSkillScore` at `:53` counts
booleans and `:75` divides.

### `security_compliance.md` — org-switch, residency, coursebuilder, aiusage

- **ENUMERATED**: all six org-switch anchors resolve to `clerk.setActive({ organization: … })` and are
  byte-identical in `apps/web` **and** `apps/hiring` — `useOrgSelection.tsx:94`,
  `useResolveActiveOrg.tsx:107`, `useActivateMembershipOrg.tsx:81` (next-web-app `8297c684`).
- **ENUMERATED**: `ai.go:262-266` (Azure EU default), `:85-95` (`config.WithRegion("eu-west-1")` at
  `:87`, `anthropic.NewAnthropic` at `:92`), `:129`/`:166`/`:325` (`isThrottlingError` decl + both call
  sites), `coursebuilder/bedrock.go:109-112` + `:100` (`return "anthropic-api"`).
- `README.md:21` for the aiusage claim resolves, on the same-directory reading, to
  `corpus/architecture/README.md:21`, which does say *"cost tracking lives in `app`, not the `ai`
  library"*. Cleared. `coursebuilder.md:48` does end *"so the shipped path is the direct API."* ✔

### `external_services.md` — router / supergraph / compose

- **ENUMERATED**: the subgraph-count table re-derived straight off
  `git show <sha>:supergraph-config-prod.yaml`: `749dc86^` = **5** (backend, skiller, jobsimulation,
  cms, skillpath), `749dc86` = **4**, `7c17e63` = **3**, `915da06` = **1** — and
  `git show --name-status 915da06` marks **both** `schemas/cms.graphqls` and
  `schemas/jobsimulation.graphqls` `D`, while `915da06~1` lists three. So *"cms-in-app was the 3 → 1
  step"* and *"do not take the count from the commit subject"* both hold.
- **ENUMERATED**: no subscriptions. `grep -rn "sse\|subscription" *.yaml` → nothing (positive control
  `grep -rln backend *.yaml` → the three supergraph configs); `schemas/backend.graphqls` (5795 lines)
  has `type Mutation` at **:4053**, `type Query` at **:4912**, **no `type Subscription`**, and exactly
  **6** `Subscription` hits, all Stripe/plan names. `git log -S 'protocol: "ws"'` → 0 (positive
  control `-S 'sse_post'` → **5** commits); `bba862f` is **not** an ancestor (rc 1) and lives only on
  `remotes/origin/feat/use-web-socket`.
- **ENUMERATED**: `terraform/main.tf:20` `service_desired_count = 1`; `locals.tf:8` `port = 8080`;
  `main.tf:48-49` container/host 8080; `config.prod.yaml:5` `listen_addr: 0.0.0.0:8080`;
  `1e8e754:docker-compose.yml:14` `"5050:8080"` — so 5050 was only the host mapping.
- **ENUMERATED**: the `Dockerfile.dev` era table. `63d285c` (2024-06-20, service named `wundergraph`,
  `context: git@…#main`, **no `dockerfile:` key**) → `2c85211^` still keyless → `2c85211`
  (2026-02-27) adds `dockerfile: Dockerfile.dev` → `67ba772` (2026-03-02) raises the context to `..`
  → `b56d731` still has `  graphql:` at `:22` under `profiles: [wundergraph-deprecated]` →
  `360efd4` has no such key (positive control: `  backend:` at `:28`). `1e8e754 == 2adcf71^1` and its
  `depends_on` names **four** services at `:19-27`, each `service_started`. `a4db680` (2026-07-21)
  is where that set stabilised. `464dfe3` is **not** an ancestor of `0c91421d` (rc 1) and lives only
  on `remotes/origin/feat/cms-in-app`.
- **ENUMERATED**: `Dockerfile.dev` has exactly one schema `COPY` (`:18`), one `awk` (`:23`), the
  orphaned comment at `:19-20`, `wgc@0.104.0` (`:7`) and
  `ghcr.io/wundergraph/cosmo/router:0.275.0` (`:29`).
- **ENUMERATED**: compose at `0c91421d` is 186 lines and declares **five** services (`sentinel:5`,
  `backend:28`, `studio-desk:112`, `next-web-app:143`, `gotenberg:170`); `common.yml` adds
  `postgresql`/`redis` with **no `profiles:` key**, and `sentinel` has none either → floor of three,
  `core` = five containers. `backend`'s `core` membership is at `:110`, gotenberg's at `:183`. At
  `0dab54d` it was eight declared / ten effective. `repos.yml` = **4** entries. Both frontends'
  four endpoints (`:151`, `:160`, `:119`, `:135`) are all `…:8082/graphql/query`.
- **ENUMERATED**: `docker-compose.yml:53` = `DIRECTUS_PUBLIC_BASE_ADDR` at **both** `0dab54d` and
  `0c91421`, and it is the **only** `DIRECTUS_*` in `backend`'s `environment:` block (which runs
  `:46-94`); `.env_example:91-92` declares `DIRECTUS_BASE_ADDR` + `DIRECTUS_TOKEN` and **not**
  `DIRECTUS_PUBLIC_BASE_ADDR`.
- **ENUMERATED**: the retracted-retraction about the old Directus service —
  `a2a3ee6^:docker-compose.yml` really has `:384 image: directus/directus:10.10.1`,
  `:386 8055:8055`, `:409 ADMIN_PASSWORD=password`, and `a2a3ee6` removes the block.
- **ENUMERATED** (pinned rext `09d06070`): `stack-injection/gen_injected_override.py:86`
  `DIRECTUS_DATA_CONSUMERS = ("cms", "backend")`, the inert-key rationale verbatim at `:77-81`, the
  re-point at `:698-699`; the dev twin `stack-core/gen_override.py:58`; the replacement test
  `test_backend_the_actual_reader_is_repointed` at `stack-injection/tests/test_injection.py:1109`.
  The prior-pin anchors also resolve: at `ab81527a`, `:84` and `:669-670`. The in-file cross-reference
  `:206-211` lands on the block it names.
- **ENUMERATED**: `app/main.go:980-982` @ `b948604f` really is the `DIRECTUS_BASE_ADDR` `log.Fatalf`;
  `main.go:770` @ `b948604f` really is `coursebuilder.ModelBackendName()`.
- **ENUMERATED**: the AI-routing mechanics at `ad9f3c49` — `ai.go:30-33` the four vendor consts,
  `:259-289` `getClient`, `:264-276` the `flag_use_azure_us` swap that keeps the EU client on error,
  `:280-283` both Anthropic arms returning `a.anthropicClient`, `:130-141` `isThrottlingError`,
  `:150-155` + `:296-302`/`:326` the 429→Openai retry, `:80` `openai.NewOpenAI`;
  `collections/jobsimulation.go:905` `AIVendor *AIVendor` and `:1302-1305`
  `aiVendor := simulation.Openai`; `simulator/ai/ai.go:58-59` and its `default:` arm at `:114-115`.
- **ENUMERATED**: the env-var grep block. I ran the corpus's **own** documented command verbatim
  (`command grep -rho '\b<NAME>\b' --include='*.go'`) and separately `git grep -h -o -w` at
  `5ba17044`: **5 / 26 / 13 / 13**, `OPENAI_ORG_ID`/`AZURE_OPENAI_ENDPOINT`/`AZURE_OPENAI_DEPLOYMENT`
  **0/0/0**, `OPENAI_API_KEY` **2** — identical at `5ba17044` and at the checkout.
  `app/studio/gen.py:41-53` overrides only `*_API_KEY`/`*_ENDPOINT`, never the service selector.
- **ENUMERATED**: **all 30** `*_AI_*_MODEL` lines across the three `app/studio/configs/*.ini` pin
  `azure` (30 lines, 30 azure — I listed them); `config_template.ini:30-31` is
  `OPENAI_API_KEY`/`OPENAI_ENDPOINT = https://api.openai.com/v1/chat/completions`;
  `services/ai.py:627`/`:664` bound `AnthropicProvider`; `:704-724` is the whole provider registry
  with `:706-708` the three arms; `:383` is the bare `OpenAI(api_key=…)`.
- **ENUMERATED**: `tools/pdf2md.py:24` `from mistralai import Mistral` (+ `mistral-ocr-latest` at
  `:127`); `tools/r3.py:139`, `:190`, `:199-206` really do dispatch it as step 2.
  `git -C app/studio grep -i mistral aeec036a` → **22 hits / 3 files** (requirements.txt, pdf2md.py,
  r3.py) — exactly the figure two corpus files quote. `bedrock|boto3` under `app/studio` → **0** by
  two instruments, positive control `anthropic` → 5+ files.
- **ENUMERATED across ALL 15 git trees** (including the two nested `studio` checkouts, enumerated with
  `find … -name .git`): `anthropos-agent-eu` = **0 in every platform clone**. The only 7 hits in the
  whole workspace are inside `rosetta-extensions`' *test fixtures*, which are frozen copies of the old
  corpus text — not the platform. So *"the name appears nowhere in the platform"* holds.
  `calls/livekit.go:110`/`:120` = `"anthropos-agent"`, `:115` = `-chain`, `:126` = the `-%s` that
  yields `-us`; `flag_use_realtime_openai` (`:133`) selects an **endpoint**, not an engine.
- **ENUMERATED**: `voiceEngineFromDirectus` nil → `SimulationVoiceEngineGptrealtime`
  (`collections/jobsimulation.go:1595-1596`).
- **ENUMERATED**: `backend.go:315` mounts `playground.ApolloSandboxHandler` at `/graphql` and `:317`
  the query handler at `/graphql/query` — so the `:8082/graphql` sandbox line is right.
  `backend.go:130` is the `/api/webhook/clerk` authn-skip entry; sentinel imports `colony/authn` in
  **0** Go files.
- **ENUMERATED**: per-app Clerk SDKs — `apps/{web,hiring,integration}/package.json` `@clerk/nextjs`,
  `apps/mobile/package.json` `@clerk/clerk-expo`, studio-desk `@clerk/clerk-js` + `@clerk/express`.
  Studio-Desk defaults `gpt-5.2` / `claude-opus-4-5-*` / `claude-sonnet-4-5-*`
  (`src/services/ai/config.ts:20-39`), `AI_PROVIDER_CHAIN=azure-openai,openai` (`.env.example:56`).
  `NEXT_PUBLIC_GRAPHQL_ENDPOINT` → **0** hits in next-web-app (positive control
  `NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT` → many).
- **ENUMERATED**: `cms/terraform/main.tf:39` `service_desired_count = 0`; `6efa1d5` deletes
  `.github/workflows/build-production.yml` with that exact subject and **is** on cms `origin/main`
  (`f38c0c4`, the merge the corpus names); `6092c6d2` **is** on jobsimulation `origin/main` and at
  that ref the `module "jobsimulation"` block is gone, replaced by the `:15-40` comment. Both
  M810 rows are correctly stated and correctly *not* generalised to each other.

### `roadrunner.md`

- **ENUMERATED**: `roadrunner/terraform/main.tf:19` `= 1`; the repo tip **is** `87d8d44` (2026-06-19)
  and `origin/main == 87d8d44`, so "not touched since" holds (the file's own last change is older,
  `e45eb61`).
- **ENUMERATED**: `repos.yml` at `2adcf71` has **9** entries with roadrunner at `:29-31` and
  jobsimulation at `:17-19`; `d11a403`'s diff removes `- name: cms`, `- name: jobsimulation` **and**
  `- name: roadrunner`, while its message says roadrunner's entry *"was already gone"* — the corpus's
  "grade the commit by its diff, not its message" is exactly right. `0dab54d` → 6 entries,
  `0c91421` → 4.
- **ENUMERATED**: the 10400/10401 pair is compose-supplied at `2adcf71` (`:291-292` published,
  `PORT=10400` at `:298`, `RPC_PORT=10401` at `:302`, env block `:296-302` carrying exactly the seven
  variables the table's "compose supplied" column lists, with `JUDGE0_API_KEY`/`SENTRY_DSN` absent);
  the binary's own defaults are `cmd/root.go:110` (`8080`) and `:84` (`8081`).
- **ENUMERATED**: the Go zeros. `ROADRUNNER_RPC_ADDR` / `RoadRunnerService` / `roadrunner:10401` →
  **0** in `*.go` at `app b948604f`, `app ad9f3c49` **and** `jobsimulation 462343b0` (positive
  control: `git -C jobsimulation grep -ci roadrunner 462343b0 -- '*.go'` → 1 file).
  `jobsimulation` `git grep -in roadrunner 462343b0` → **14 lines across 8 files**, exact-case **5
  across 3** — the corpus's numbers to the digit; `internal/runner/runner.go:3` is the
  *"formerly the standalone 'roadrunner' service"* comment.
- **ENUMERATED across all clones at their own refs**: case-insensitive `roadrunner` file counts —
  app **25**, jobsimulation **8**, platform **3**, studio-desk **3**, next-web-app **1**, everything
  else 0 (sentinel, cms, storage, messenger, graphql-wundergraph, ant-academy, and the nested
  `app/studio`). Exactly the corpus's figures.
- **ENUMERATED**: `jobsimulation/knowledge/operational.md:68` tabulates `ROADRUNNER_RPC_ADDR`;
  `app`'s `…/RE-PORT-CHECKLIST.md:10` names `RoadRunnerService`;
  `app/internal/jobsimulation/simulator/stream_handlers.go:30-34` carries the quoted
  *"NOT stream handlers."*
- **ENUMERATED**: the code map. `find -name "*.go"` returns precisely the tree the doc draws
  (`cmd/root.go`, `cmd/runcode/{runcode,launch}.go`, `internal/lsp/lsp.go`, `internal/rpcsrv/rpc.go`,
  `internal/runner/{runner,languages}.go`, `internal/worker/{worker.go,client,queues,tasks}`,
  `main.go`); `lsp` is imported by nothing outside itself; `rpcsrv` never mentions asynq; the three
  RPC methods are at `rpc.go:20`, `:43`, `:60`; `queues.DefaultQueue = "roadrunner:default"` and the
  single `asynq.NewTask(… MaxRetry(3))` sits inside `CreateSubmission` (`runner.go:48`→`:126`);
  `Concurrency: 10` (`worker.go:25`); `maxRetries = 15` + `time.Sleep(1 * time.Second)`
  (`runner.go:244`, `:273`); `RoadrunnerSubmissionCompleted` published at `:277-278`;
  `tasks.go` holds the one constant `"roadrunner:submissionresult"`; `case "py":` with no `"python"`;
  `godotenv/autoload` at `main.go:9`; `strconv.Atoi(os.Getenv("REDIS_WORKER_INDEX"))` returning an
  error at `cmd/root.go:68-71`; no vendored `.proto`, `proto v1.196.0` in `go.mod`; Go **1.25.0**,
  asynq **v0.25.1**, gorilla/websocket present; **0** `*_test.go` files.

### `dependency_map.md`

- **ENUMERATED**: `*_RPC_ADDR` → **0** across *both* compose files at `0c91421d` (positive control:
  the `_ADDR`/`_URL` list returns 10+); `AUTHORIZATION_ADDRESS` `:48`, `GOTENBERG_URL` `:57`,
  `JUDGE0_BASE_URL` `:59`, `REDIS_ADDR` `:66`, `SUPABASE_DB_CONN`/`COPILOT_DB_CONN` `:93-94`,
  `backend.depends_on` `:101-109`, the in-line comment at `:102-103` verbatim, the messenger/customerio
  rationale at `:84-92`, studio-desk `depends_on` `:138-140`, `SKILLER_STREAM` `:71`,
  `0dab54d:docker-compose.yml:154` `profiles: [customerio-sync, all]`. `service_taxonomy.md:101-103`
  resolves to the matching retraction.
- **ENUMERATED**: `app/internal/converter/gotenberg.go:31` is a plain
  `http.NewRequestWithContext(ctx, "POST", gotenbergURL+"/forms/libreoffice/convert", …)` — plain
  HTTP, not Connect-RPC, and the endpoint the §6 flow names.
- **ENUMERATED**: the shared-library matrix, measured `go.mod` by `go.mod` over the seven Go repos on
  disk — colony **7/7**, proto **7/7**, taxonomy **6/7 (roadrunner is the miss)**, ai **2/7 (cms,
  jobsimulation, both `v1.40.2` at `cms/go.mod:9` and `jobsimulation/go.mod:11`)**, authn **0/7**.
  `app/go.mod` required `ai v1.40.2` at `b948604f:go.mod:14`; `1e457fa70` (2026-08-04) removes that
  line; `internal/ai/module_import_guard_test.go` exists. `colony/authn` importers: app **129** `.go`
  files @ `ad9f3c49`, cms **6**, jobsimulation **8**; standalone `authn` imported by **0** files in
  all seven. `app/go.mod:20` @ `b948604f` = taxonomy; `sentinel/go.mod:21` @ `88bc5592` = taxonomy
  (indirect).
- **ENUMERATED**: the `backend` stream row's whole re-derivation.
  `messenger/internal/flow/flow.go:72` is the literal `AddSubscriber("backend", …)` and the block
  carries exactly **21** live `pubsub.EventHandler` entries with **1** commented out;
  `app/internal/messenger/flow/streams.go:65` `StreamBackend = "backend"`; `main.go:1493` names it;
  `main.go:1445` `if messengerEnabled {`; `:285` `mustSubsystemSwitch(envMessengerEnabled)`;
  `env_guards.go:61` `envMessengerEnabled = "MESSENGER_ENABLED"`; `main.go:1437` the verbatim
  *"ALL OF IT is behind MESSENGER_ENABLED."* — and **all of these hold identically at `2035f9a4`**,
  as the cell claims. `main.go` really is **1361** lines at `b948604f` (so `:1442` cannot exist
  there) and 1639 at `ad9f3c49` (where `:1442` is a comment). `flow.go:105`/`:109` are the
  jobsimulation/cms subscribers.
- **ENUMERATED**: the `skiller` row's 6-drift/1-holds split, which is the sharpest claim in my file
  set and is **exactly right**. At `b948604f`: `main.go:287` `NewPublisher(serviceName…)`, `:637`
  `SKILLPATH_STREAM`, `:1039` `CMS_STREAM`, `wiring.go:127` `AI_USAGE_STREAM`, `:180`
  `JOBSIMULATION_STREAM`, `:1276` the lone `SKILLER_STREAM` `AddSubscriber`. At `ad9f3c49` those six
  read `logger.Info("subsystem switches",` / `)` / `serverContext,` / `apiKeyManager,` /
  an asynq-client construction / a blank line — and `internal/roles/roles.go:791` is
  `func (r *RoleManager) SkillerSubscriber() *pubsub.Subscriber {` **byte-identical at
  `b948604f`, `2035f9a4` and `ad9f3c49`**. `SKILLER_STREAM` at `ad9f3c49`: **6 `*.go` lines across 3
  files** (`main.go`, `subscriber_merge_test.go`, `subscriber_wiring.go`) and **6 files** with no
  pathspec. No `NewPublisher` names it at any ref; `main.go:1537` is the `Skiller:` field of
  `streamNamesForSubscribers`. Every number and every retraction in that cell verifies.
- **ENUMERATED**: `jobsimulation/terraform/main.tf:15-40` at `6092c6d2` is the surviving comment
  block naming the LiveKit/Chime buckets, the SSM parameters and the atlas tracker.
- **ENUMERATED**: `services/ai.py:705-708` is the whole Studio-Room provider registry (no Mistral),
  consistent with `external_services.md`'s narrower *"nothing in the **pipeline** dispatches it"* —
  the two files are **not** in contradiction, because `external_services.md` refutes only the
  unqualified *"nothing dispatches it"* and `dependency_map.md`'s form carries the qualifier.

### `TEMPLATE.md`

46 lines, a pure skeleton with placeholder tokens (`[path/to/repo]`, `[service_name]`, `[command]`).
No platform assertion to grade. Its `docker compose up -d [service_name]` recipe is a placeholder, not
a runnable dead token, so it does not trip the retired-token rule. **Clean.**

---

## What I could not settle

- **GitHub archive states.** `external_services.md:3` (*"the repo is ARCHIVED on GitHub
  (2026-07-30)"*), `roadrunner.md:38` (*"skiller 2026-07-01, skillpath 2026-07-31"*), `roadrunner.md:74`
  (roadrunner *"**not** archived"*), `roadrunner.md:56-58` (chronos *"NOT archived, last push
  2026-04-23"*). None of these is derivable from a clone; `roadrunner.md:46` says as much for
  jobsimulation. Settled only by `gh api repos/anthropos-work/<name> --jq .archived`, which needs
  network and is outside a read-only clone audit.
- **cms's production ECS disposition.** The corpus explicitly declares it UNMEASURABLE (the
  destruction lands in `infrastructure`, never in a clone set) and reports both measured facts without
  asserting either. I confirmed both facts and agree it is unmeasurable here.
- **`external_services.md:855-865`'s live measurement** (*"measured 2026-08-05 against
  `stack-demo/platform`: `docker compose ps storage` exited 0 while `ps cms` returned rc 1"*) is a
  historical runtime observation at `0dab54d`. The static premise (storage declared behind
  `profiles: [storage-legacy]`, cms/jobsimulation absent) verifies; the exit codes themselves cannot
  be re-taken now that `838d907` has deleted the block.
- **`security_compliance.md`'s infrastructure claims** — VPC CIDR `10.0.0.0/16`, TLS 1.3 cipher list,
  AES-256/KMS, 6-hourly backups to S3/Azure/Hetzner, 90-day CloudWatch retention, DPA v1.4 with 18
  sub-processors, 30-second ECS health checks. None is derivable from any clone in this set
  (`infrastructure` and `db-backup` are not cloned). They would be settled by the `infrastructure`
  repo's terraform and the DPA document.
- **`security_compliance.md:22`** lists "Cosmo Router" in the production public subnets. Consistent
  with `graphql-wundergraph/terraform/main.tf:20` `= 1`, but the subnet placement itself lives in
  `infrastructure` and is unreadable here.
