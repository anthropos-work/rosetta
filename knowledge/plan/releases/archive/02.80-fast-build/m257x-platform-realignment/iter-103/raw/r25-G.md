# Seat G — reading #25

## Refs actually read (ground truth, verified at this reading's open)

| tree | ref read | note |
|---|---|---|
| `stack-demo/app` | `ad9f3c49` (checkout == `origin/main`) | plus pinned reads at `b948604f`, `9d00a313`, `5ba17044`, `2035f9a4`, `1e457fa70` |
| `stack-demo/platform` | `0c91421d` | plus pinned reads at `0dab54d`, `2adcf71`, `a2a3ee6`, `a4db680` |
| `stack-demo/next-web-app` | `8297c684` | plus `bb3313bc` where a passage pins it |
| `stack-demo/sentinel` | `f2c46190` (== `origin/main` == `clones.pin.json`) | plus `88bc5592` where a passage pins it |
| `stack-demo/studio-desk` | `41ee3575` | |
| `stack-demo/storage` · `messenger` · `cms` · `jobsimulation` · `roadrunner` · `graphql-wundergraph` | `4ce8ece5` · `fa47850d` · `ca50c817` · `462343b0` · `87d8d443` · `60c229f3` | |
| `stack-demo/rosetta-extensions` (**pinned per-stack consumption clone — the tree that settles a tooling claim**, addendum / §5 rule 45) | `09d06070` | authoring copy `.agentspace/rosetta-extensions` `944fc4a2` read ONLY to confirm the two trees agree byte-for-byte on `persona_write.go` |
| nested repos `stack-demo/app/studio`, `stack-demo/cms/studio` | `aeec036a` each | enumerated via `find stack-demo -name .git -maxdepth 4`; no `ai`/`colony`/`proto`/`taxonomy`/`authn` clone exists anywhere in the set |

**Instrument note (§5 rule 1/2, paid for in this pass):** one of my early loops used unquoted `$r:terraform/...`
in zsh, where `:t` is a history modifier — it silently rewrote the path and returned **fatal + empty** for four
refs. I only caught it because a *previous* command had already printed the very line the loop said did not
exist. Every absence claim below was re-run quoted, with a positive control in the same pass.

## Positive controls

| file | `wc -l` | read in full? |
|---|---|---|
| `corpus/services/hiring.md` | 481 | yes |
| `corpus/services/studio-desk.md` | 435 | yes |
| `corpus/architecture/shared_libraries.md` | 274 | yes |
| `corpus/services/coursebuilder.md` | 161 | yes |
| `corpus/services/skillpath.md` | 117 | yes |
| `corpus/services/gotenberg.md` | 84 | yes |
| **total** | **1552** | matches the assignment |

---

## Blockers

### B1 — the colony "two-way pin split" between `app` and `sentinel` does not exist at the ground truth

- **Anchor:** `corpus/architecture/shared_libraries.md:57` (colony **Version pin** row) —
  > "**At `0c91421` it is still two-way but between different repos — `app v0.35.2` (`app/go.mod:16` @ `b948604f`) vs `sentinel v0.34.3` (`sentinel/go.mod:8` @ `88bc5592`)**"

- **Why it is false:**
  - `sentinel` at its **checkout**, at **`origin/main`**, and at the **demo build pin** is `f2c46190` —
    all three the same commit (`stack-demo/clones.pin.json` → `"sentinel": "f2c461903de0…"`;
    `git log 88bc5592..origin/main` returns exactly `88036d7` + `f2c4619`).
  - `sentinel/go.mod:8` @ `f2c46190` reads **`github.com/anthropos-work/colony v0.35.2`** — *identical* to
    `app/go.mod:15` @ `ad9f3c49` (`colony v0.35.2`). **There is no split at all**, two-way or otherwise.
  - The cited `88bc5592` is dated **2026-06-19**; `f2c46190` is **2026-08-03**; platform `0c91421` — the ref
    the sentence itself names as the date of the claim — is **2026-08-05**. So the sentinel bump had landed
    **two days before** the state the sentence asserts.
- **Why I did not treat the pin as an exemption:** the block asserts *currency* in its own words ("**At
  `0c91421` it is **still** two-way**", "…are now frozen, **not live**"), which is exactly the case
  `platform-alignment.md` §5 rule 33 removes the pin exemption for — *"a block that asserts currency cannot be
  pinned into silence, because the pin and the word are making opposite claims and only one of them is what
  the reader acts on."* The `@ 88bc5592` pin dates the **cell read**, not the **platform-state assertion**
  wrapped around it, and the two disagree by 47 days across a real bump.
- **Reader harm:** this row is the corpus's answer to *"which colony does each service build against?"* A
  reader debugging a colony-behaviour difference between `backend` and `sentinel` is sent hunting a version
  skew that does not exist on the stack they are running.
- **Confidence:** high.

### B2 — the same row's twin: the proto "live skew is two" is likewise gone

- **Anchor:** `corpus/architecture/shared_libraries.md:85` (proto **Version pin** row) —
  > "**At `0c91421` the live skew is two: `app v1.210.0` (`app/go.mod:18` @ `b948604f`) and `sentinel v1.200.0` (`sentinel/go.mod:9` @ `88bc5592`)**"

- **Why it is false:** `sentinel/go.mod:9` @ `f2c46190` (checkout, `origin/main`, and the demo build pin)
  reads **`github.com/anthropos-work/proto v1.210.0`** — the same pin `app` carries (`app/go.mod:16`
  @ `ad9f3c49`). The "live skew" is **one** value, not two. Same mechanism as B1: an assertion about the
  state *at platform `0c91421`* evidenced by a sentinel read from before the repo moved. The word "**live**"
  is the currency assertion here.
- Booked separately from B1 because it is a separately-repairable claim in a different section, and a
  repairer who fixes only the colony row leaves the identical falsehood standing 28 lines down (§5 rule 19).
- **Confidence:** high.

### B3 — the `ai` section describes an interface, a provider and a failure mode that no readable copy of the library has

- **Anchors:** `corpus/architecture/shared_libraries.md:128-130`, `:137`, `:162-164`
  - `:128-130` — "A thin wrapper exposing **one interface, `ai.AI`** (`ChatCompletion`,
    **`ChatCompletionStream`**, `Response`, `CreateEmbeddings`, …)"
  - `:137` — "| `mistral.NewMistral` | Mistral — **OCR only** (chat/embeddings/speech `panic`) |"
  - `:162-164` — "only OpenAI/Azure implement embeddings, speech, OCR, transcription, **streaming**;
    **Anthropic `ChatCompletionStream`/`CreateSpeech` `panic`**"

- **Why it is false against ground truth:** the `ai` repo is in **no clone set** (verified by enumerating every
  `.git` under `stack-demo`). The **only readable copy** is `app/internal/ai/`, which this same document
  points the reader at (`:126` — *"`app/internal/ai/` carries the library in-tree"*). At `app` `ad9f3c49`:
  - `internal/ai/ai.go` is **21 lines** and declares **8** interface methods — `ChatCompletionStream` is
    **not among them**;
  - `internal/ai/mistral/` **does not exist** (`2b3a65cf0` re-homed it to `internal/cms/studio/mistralocr` as
    a plain `New`/`Process` HTTP client); the repo's own doc says so in terms —
    `knowledge/ai-library/architecture.md:97` *"**Mistral is no longer a provider here.**"*;
  - `internal/ai/anthropic/` contains **zero** `panic(` in non-test code, and its characterization test states
    it: *"This package now contains no `panic("not implemented")` at all"*
    (`internal/ai/anthropic/characterization_test.go:316`); `architecture.md:82` — *"**no method in this
    library panics.**"*, `:74` — *"**There is no streaming.**"*
- **Why I hesitated, stated plainly:** all three claims **are true at the standalone module tag `v1.40.2`** —
  I confirmed it by reading the fold commit `1e457fa70`, which is that tag verbatim (`ai.go:10` declares
  `ChatCompletionStream`; `anthropic/completion.go:180` and `:189` panic; `internal/ai/mistral` exists). And
  the section's **Version pin** row names `v1.40.2`. So a "it is pinned" defence exists. I book it anyway
  because §5 rule 33 scopes a pin to **its own block** — a table *cell*, not the prose paragraphs below the
  table — and the prose here carries no ref while the document simultaneously tells the reader the library
  now lives in `app/internal/ai/`. A reader who follows that pointer finds an interface with one fewer
  method, no Mistral, and no panics.
- **Confidence:** medium.

### B4 — "each off by −2 at `5ba17044`" is false for one of the three anchors, by 50 lines

- **Anchor:** `corpus/services/hiring.md:24-25` (the RE-GROUNDED banner) —
  > "`internal/organization/manager.go:450` / `:453` / `:537` resolve at `ad9f3c49` and are each **off by −2**
  > at `5ba17044`."

- **Why it is false:** measured in `stack-demo/app`:

  | content | @ `ad9f3c49` | @ `5ba17044` | actual offset |
  |---|---|---|---|
  | `switch org.IsHiring {` | 450 | 448 | −2 ✓ |
  | `antRole = enum.RoleCandidate` | 453 | 451 | −2 ✓ |
  | `return fmt.Errorf("organization is not hiring")` | **537** | **487** | **−50** ✗ |

  `git show 5ba17044:internal/organization/manager.go \| grep -n "organization is not hiring"` returns
  `453` (a comment), `486`, `487`. At `5ba17044:535` — where the stated −2 lands — sits
  `warnings := []*BulkInviteMemberMessage{}` inside `BulkImport`, an unrelated function.
- **Reader harm:** this sentence exists *specifically* to tell a reader how to re-derive the anchors at the
  historical ref. Following it for the third anchor lands on unrelated code, which is the failure the banner
  was written to prevent. It is also a derived scalar contradicting source with no corpus twin — §5 rule 21's
  third class, the one only a source read catches.
- **Confidence:** high.

---

## Minors

- `corpus/services/coursebuilder.md:48` — `terraform/variables.tf:635-638` and `main.tf:555` resolve
  **only at `b948604`** (`variable "anthropic_api_key"` at `:635`; `"name": "ANTHROPIC_API_KEY"` at `:555`);
  at the checkout `ad9f3c49` they are `:759-762` and `:757`. The bullet names `@ app b948604 v1.366.0` one
  sentence earlier, so the reading is recoverable — but the terraform sentence carries no ref of its own.
  Also `ssm.tf:328-334`: the resource block is `:328-333`.
- `corpus/services/coursebuilder.md:48` — `ModelBackendName` is cited `(:98-104)`; the function is `:98-103`
  (`:104` is blank).
- `corpus/services/coursebuilder.md:129-130` — "~55 test files" measures **61**, "~25 boundary test files"
  measures **33**, at every ref I checked (`5ba17044`, `b948604`, `9d00a313`, `ad9f3c49`). Both are hedged
  with `~`, but 25→33 is a third low.
- `corpus/services/studio-desk.md:199` (and `:107`) — `GCLOUD_SERVICE_ACCOUNT` is declared at
  `.env.example:119`, not `:120` (`:120` is a blank line). The substantive claim — *declared in
  `.env.example` + injected by `terraform/main.tf:129`, read by no code in `src/`* — is **exact**
  (`grep GCLOUD_SERVICE_ACCOUNT` over `src/` returns 0 with `YOUTUBE_API_KEY` as the positive control).
- `corpus/architecture/shared_libraries.md:96` — "`storage.md:115` … documents in full". `storage.md:115` is
  a line of the **key-directories code map** (`rpcsrv/rpcsrv.go   Connect-RPC handler implementations`);
  the `### Connect-RPC (StorageService)` section with the method table is at `storage.md:129`.
- `corpus/services/hiring.md:241` — `persona_write.go:69-71` names the *"the old `jobsimulation.sessions`
  step is REMOVED rather than re-pointed"* comment; the three `validation_*` mappings are `:66-68` and the
  actual writes are `:92-94`. `:143-167` covers `sessionCols()` + `attemptResultCols()` only —
  `skillResultCols()` starts at `:169`. The claim itself (*the PersonaSeeder also writes those three
  tables*) is **true**. Identical in the pinned clone `09d06070` and the authoring copy `944fc4a2` (diffed).
- `corpus/services/hiring.md:161` — "M223's hiring story simply sets `role_mix ≈ 0.1 admin / 0.9 candidate`".
  The shipped preset is `role_mix: { admin: 0.14, member: 0.0, candidate: 0.86 }`
  (`stack-seeding/presets/stories.seed.yaml:249` @ `09d06070`), and the file annotates it in terms:
  *"admin: 0.14 (**NOT 0.10**) — M226 iter-04 (the billion-proof count fix)"*. Hedged by `≈`, so minor.

---

## Positively cleared (audited zeros)

Labels are the briefing's: **ENUMERATED** = I derived the predicate's set from source independently and
compared cardinalities; **RE-DERIVED** = I only re-ran the arithmetic the document displays (the weakest
clearance a report can contain).

### ENUMERATED

- **`shared_libraries.md:22-25` — the 5×7 dependency matrix.** I did **not** re-add the document's numbers;
  I dumped `grep -n "anthropos-work/"` from **each of the seven** Go repos' `go.mod` at its own ref and built
  the matrix myself: colony **7/7**, proto **7/7**, taxonomy **6/7** (absent only from `roadrunner`), ai
  **2/7** (`cms/go.mod:9`, `jobsimulation/go.mod:11`, both `v1.40.2`), authn **0/7**. Every cell matches,
  including the `// indirect` markings the row calls out per repo. `app` @ `ad9f3c49` carries **no** `ai`
  line and `go.sum` has **zero** — the fold at `1e457fa70` (2026-08-04) is real.
- **`shared_libraries.md:213` — "that set is 11" (Go repos ever in `repos.yml`).** Derived by replaying every
  one of the **10** commits that ever touched `repos.yml` and emitting `(name, type)` pairs:
  **14 names ever**, of which `next-web-app` (node-pnpm), `studio-desk` (node-npm) and
  **`graphql-wundergraph` (node-npm)** are not Go → **11 `type: go`** exactly. `skiller`/`skillpath`/
  `chronos`/`intelligence` are all four present in the first revision `a2a3ee6`; `skillpath` is the last
  dropped, at `a4db680`. Every clause holds.
- **`shared_libraries.md:58,85,86,213` — the clone-set membership arithmetic.** `repos.yml` @ `0c91421`
  lists **4** entries (app, sentinel, next-web-app, studio-desk), of which **2** are `type: go`; **6** @
  `0dab54d`; **9** @ `2adcf71`. The `roadrunner/main.go:7` colony import and `roadrunner/go.mod:7` `v0.34.3`
  both resolve.
- **`shared_libraries.md:86` — "six Connect handlers @ `b948604`".** Read `main.go:1184-1228` at that ref and
  enumerated the `mux.Handle` calls: Users `:1187`, Organizations `:1188`, Skiller `:1196`, JobSimulation
  `:1204`, LabSession `:1228`, plus `CMSService` inside `if cmsRPCServer != nil` at `:1212-1214`. Six, five
  unconditional, and **neither** `SkillPathSessionService` nor a RoadRunner service among them. Exact.
- **`shared_libraries.md:99` — `ROADRUNNER_RPC_ADDR` is read nowhere.** 0 hits in `app` @ `9d00a313` and 0 in
  `platform` @ `0dab54d`, with `JUDGE0_BASE_URL` as the positive control (4 files) and
  `jobsimwiring/wiring.go:123` reading `jsrunner.NewRunnerManager(getenv("JUDGE0_API_KEY"),
  getenv("JUDGE0_BASE_URL"))`. The parenthetical *"this line long cited `docker-compose.yml:118`; that line
  sets `AWS_REGION`"* is also exact at `0dab54d`.
- **`shared_libraries.md:177,194,200` — the authn claims.** `git grep '"github.com/anthropos-work/authn'`
  over all seven Go repos returns **0** everywhere; `colony/authn` importers: `cms` **6** `.go` files,
  `jobsimulation` **8** — both figures exact; `sentinel` has **zero** `authn` occurrences in `.go` at
  `f2c46190`.
- **`coursebuilder.md:77-94` — the 16 SSE event names.** This is the passage that shows its own derivation,
  so I enumerated the set rather than the sum: I read `renderEvent` whole (`handler.go:2604-2772` @
  `ad9f3c49`) and listed the distinct non-empty names it returns → **15**, plus `session` written directly at
  `:1458` right after `beginSSE` → **16**. Every per-name line anchor (`:2607`, `:2609`, `:2614`, `:2620`,
  `:2637`/`:2639`, `:2654`, `:2661`, `:2671`, `:2678`, `:2704`, `:2724`, `:2732`, `:2744`, `:2754`, `:2768`,
  and the `default` arm's second `text` at `:2770`) lands on the exact `return`. The `cost` filter is real:
  `:2709` → `return "", nil` at `:2717`, and the stream loop's `if name != ""` guard is `:1478-1480`.
  `grep -n writeSSE` returns exactly the two call sites the passage implies. Nothing off by one.
- **`coursebuilder.md:262-268` — the four required-and-undefaulted columns** (this claim is re-stated in
  `hiring.md:262-264` and I graded it there): I read the whole `CREATE TABLE` at
  `20260722104506.sql:2-27` and classified **every** `NOT NULL` column by whether it carries a `DEFAULT`.
  Twelve are `NOT NULL`; exactly **four** lack a default — `owner_id` `:6`, `sim_id` `:7`, `sim_type` `:10`,
  `token` `:13` — and `token`'s `UNIQUE` index is `:29`. The table has **23** columns (`:3-25`), matching
  `hiring.md:178`.
- **`hiring.md:91` — the "no read path reads `is_hiring`" refutation and its positive controls.**
  `grep -in hiring` over `resolver_queries.go` @ `ad9f3c49` returns only `:811`, `:843`, `:991-992` — all
  sim-TYPE strings; over `internal/organization/intelligence.go` it returns **0**. Positive controls as
  published: `OrgFeatureInsights` **×8** in the resolver, `JobSimulationSession` **×44** in
  `intelligence.go`. Both exact.
- **`hiring.md:339-350` — the RETRACTED "Workforce Intelligence is gated off" clause.** I re-derived the
  gating chain rather than trusting the retraction: `orgSectionVisibility({isAdmin, showStudio})`
  (`orgGroups.ts:48-65`) takes **no** `isHiringOrg` parameter and returns `intelligence: isAdmin` at `:61`;
  `enterpriseWorkforceMenuItem` is `:391-398`; its gate is `showWorkforce ?` at `:568`; the default is
  `showWorkforce = true` at `:161`; and a tree-wide grep for `showWorkforce={false}` returns **exactly two**
  sites, **both** in `apps/hiring` (`template.tsx:167`, `:248`). The retraction is correct in every clause.
- **`hiring.md:459` — "zero `(.)`/`(..)`-prefixed dirs exist anywhere in `apps/`".** Counted 0 path segments
  matching `/\((\.|\.\.|\.\.\.)\)` under `apps/` @ `8297c684`, with `@tabs` (**46**) and `(authenticated)`
  (**394**) as positive controls proving the pattern class is reachable.
- **`hiring.md:452` — "the regression enumerates all 8 generic seeders".** Counted the `cases` table in
  `stack-seeding/seeders/hiring_scope_test.go:76-96` @ the **pinned** clone `09d06070`: jobsim-sessions,
  activity, skillpath-sessions, assignments, hero-activity, persona, feedback, succession = **8**, matching
  the eight `hiring_scope.go:24-27` names.
- **`gotenberg.md:47` — "two call sites".** `git grep ConvertToPDF` @ `9d00a313` returns exactly two Go call
  sites (`extract.go:77`, `user_import_resume_2d.go:68`) plus the definition and one markdown mention.
  The nine gotenberg-routed MIME types are `extract.go:17-27` — I counted the map entries (9: xls, xlsx,
  ppt, doc, odt, ods, odp, and RTF under **both** spellings) and confirmed DOCX is **not** among them.
- **`skillpath.md:30-40` — "there is NO `SkillPathSessionService` anywhere".** `git grep` @ `ad9f3c49`
  returns **2** hits total, both markdown (`CLAUDE.md:109`, `knowledge/architecture.md:28`), **0** in
  `*.go` — and I opened both markdown lines to confirm they are the stale RPC-mux listings the passage
  describes. `SKILLPATH_STREAM=skillpath` really is still in compose (`docker-compose.yml:72` @ `0c91421`).
- **`skillpath.md:87-91` — the mirror-drop retraction.** `20260729133514.sql:62` = `DROP TABLE
  "local_jobsimulation_sessions"`, `:63` = `DROP TABLE "local_skill_path_sessions"`; the migrations that
  post-date it are exactly the three named (`20260731131307`, `20260731154527_academy…`,
  `20260803143844_ai_readiness…`) — enumerated by `ls-tree` at both `9d00a313` and `ad9f3c49`. No
  `local_skill_path_session.go` schema exists.
- **`studio-desk.md:34` — the vite entry set.** Read `rollupOptions.input` whole: 9 prod entries, matching
  the nine named, with `dev-accept` behind `isProduction ? {} : {…}`. Exact.
- **`studio-desk.md:279-287` — "no datastore of any kind".** Independently: `package.json` matches **0** of
  `pg|postgres|prisma|sqlite|mysql|mongo|knex|drizzle|typeorm|sequelize`; `DATABASE_URL` returns **0**
  tree-wide; `tailscale` returns **exactly one** hit, `app/core/main.ts:105`, and it is the GlitchTip DSN
  comment as described.

### RE-DERIVED (weaker — recorded as such)

- `coursebuilder.md:145-152` — `git describe --tags ad9f3c49` → `v1.369.0-7-gad9f3c498` and
  `2035f9a4` → `v1.369.0-2-g2035f9a40`, both exactly as printed; `5ba17044` → `v1.363.2`.
- `coursebuilder.md:113-118` — env defaults re-read at their constants (`DefaultAuthorModelID`
  `eu.anthropic.claude-opus-4-8`, `DefaultGraderModelID` `eu.anthropic.claude-sonnet-4-6`,
  `DefaultMaxMonthlyCOGSUSD = 500.0`, `DefaultMaxDailyCOGSUSD = 0.0`, `DefaultSessionsPerOrgPerDay = 50`,
  `coursebuilderWorkerConcurrency = 20`, credits build **5** / refine **1** / translate **1**, 7 overlay
  locales in `IsOverlayLocale`).
- `hiring.md` read-path table rows 1–7 and every `intelligence.go` anchor (`:1700`, `:1702`, `:1709`,
  `:1733`, `:1739-1744`, `:1820`, `:1844`, `:1846`, `:2124`, `:2158-2160`, `:885-886`), the
  `resolver_queries.go` gates (`:1034`, `:1035`, `:1053`, `:1080`, `:1085`, `:1089`), the
  `resolver_cms_queries.go` hiring branch (`:95,210,258,295`, `:99-103`), the enum block
  (`enum/jobsimulation.go:29-35`, `Values()` `:37-43`), the Ent schema (`job_simulation_session.go:33/39/45`),
  the gqlgen marshalers (`graph.go:129546-129554` and the proto twin `:129392-129400`), the SDL
  (`jobsimulations.graphqls:14`, `:128`), `anticheat_result.go:24`, `20260722104506.sql:53/79`,
  `20260722081626_jobsim_data_model.sql:336/355/376`, `20250416091037.sql:5`, `20240527131926.sql:7`,
  `atlas.hcl:8`, `20230817154747_supabase_baseline.sql:2`. All resolve on the exact construct claimed.
  `SET "score"` = **0** hits set-wide (positive control: `SET ` matches in many migrations); `CREATE SCHEMA`
  = **1** hit set-wide, `auth`.
- `hiring.md` next-web anchors: `useGetClerkOrganization.tsx:16-18/20-21`, `useNavbarSections.tsx:161`,
  `:249-256`, `:329-331`, `:340-343`, `:391-398`, `:475-482`/`:476` @ `8297c684` **and** `:459-466`/`:460`
  @ `bb3313bc` (the doc's own "was `:460`" claim — exact in both directions),
  `apps/web/.../UserStatusContext.tsx:141-173`/`:144-149`/`:168-172`,
  `apps/hiring/.../UserStatusContext.tsx:125`/`:144-145`, `template.tsx:90`, `FreeTrialContainer.tsx:29`,
  `InsightsByMembersContainer.tsx:359`, `insights.ts:31-82`, `useTablePagination(initialLimit = 20)`.
  The two `simulationScoreColumn.tsx` copies are **byte-identical** — same sha (`8b346c31d692`) — which is a
  stronger check than "same line numbers".
- `studio-desk.md` compose anchors @ `0c91421`: `:119` build arg, `:135` runtime env, `:138-140` `depends_on:
  backend`, `:141` `profiles: [studio-desk, all]`; and the historical `:337-341` `depends_on backend + cms`
  @ `2adcf71`. All exact.
- `studio-desk.md` rext claims, read at the **pinned** clone `09d06070`: all five studio patch dirs exist
  (`studio-desk-{back-to-cockpit,logo-url,logout-url,no-thirdparty,shell-first-paint}`),
  `gen_injected_override.py:379` `def frontend_lines(`, the existence-guarded `…/studio-desk/.env` `env_file`
  at `:436-439`, both named regression tests in `stack-injection/tests/test_injection.py`, and
  `stack-verify/e2e/run-studio-fcp.sh`.
- `hiring.md` rext claims @ `09d06070`: `hiring_config.go:99` `CopyRowsIdempotent(… "organization_sim_invitation_links" …)`,
  `persona_write.go:91` writing `{"public","job_simulation_sessions", sessionCols(), …}`, `sessionCols()` at
  `:152`, `localSessionCols()` **deleted** (surviving only as a string inside a fence-test fixture),
  `hiringComparableFloor = 6` (`contentref.go:158`) with the `RENDER_GATE_FLOOR` drift fence
  (`users_m227_test.go:215`), `readHiringSimPool` (`contentref.go:209`),
  `RoleMix{Admin, Member, Candidate, AdminEmails}` (`blueprint.go:453-458`), the Meridian Talent 4th story,
  `render-hiring-comparison.spec.ts`, and both `next-hiring-*` patch dirs.
- `shared_libraries.md:42-47` — `GH_ACCESS_TOKEN: $GH_PAT` at `docker-compose.yml:12`/`:39`; both app
  Dockerfiles carry the `insteadOf` rewrite (`:11`) and `GOPRIVATE` (`:13`); **no** `go.work` is tracked in
  any of the seven Go repos.
- `studio-desk.md:132` / `:46` — "the Cosmo Router survives in production only" is supported by
  `graphql-wundergraph/terraform/main.tf:20` `service_desired_count = 1` @ `60c229f3`, and is uniform with
  four sibling corpus sites (`architecture_overview.md:71`, `external_services.md:368`,
  `next-web-app.md:162`, `backend.md:299`). No self-contradiction.

---

## What I could not settle

- **`shared_libraries.md:56, 64-75, 84, 112-115, 211-212, 220, 247-261`** — every claim about the *internals*
  of `colony`, `proto`, `ai` and `taxonomy` (colony's `go 1.25.0` and package inventory, the
  `colony/authorization` package being named `authorizer`, proto's buf-v1 layout and `go/simulator/*` stub
  count, taxonomy's `go 1.21.0` / zero deps / 131-line `node.go` / the `NodeID` format and its worked
  examples). **None of these four repos is cloned anywhere in the tree** (I enumerated every `.git` under
  `stack-demo`, nested repos included). Circumstantial support only: `app` imports
  `colony/authorization` under the alias `newAuthz` (`internal/authorization/sentinel.go:13`), which is
  consistent with a package name that differs from its directory; and
  `storage/internal/migration/migration.go:13` really does import `proto/go/simulator/storage/v1` as
  `legacyStorage`. **What would settle it:** a clone (or vendored copy) of the four library repos at the
  pins the `go.mod`s name.
- **`shared_libraries.md:234-241`** — the taxonomy counts (**22,470** public job roles, **42,790** public
  skills, **18,919** `job_role_embeddings` rows). Needs a DB read against the captured snapshot manifest or a
  live stack; out of reach read-only. The *shape* of the correction (a floor, not a total) is internally
  coherent and I found no corpus site contradicting it.
- **`hiring.md:169-170, 174`** — "**87** real `SIMULATION_TYPE_HIRING` sims" and "the captured snapshot has
  **0** `job_position` rows (the prod '443' was never captured)". Snapshot-cache measurements; no artifact in
  the read-only scope carries them.
- **`hiring.md:426-431, 456-465`** — the live render-gate figures on `billion` (5/5, per-position cohorts
  8/8/9/9/8, recruiter p95 1.27 s) and **`studio-desk.md:412, 423`** (skeleton p95 4669 → 817 ms; canAccess
  4049 → 38 ms). Live-run measurements; not reproducible read-only. The *derived constants* they gate on
  (`hiringComparableFloor = 6`, `RENDER_GATE_FLOOR`) do verify.
- **`studio-desk.md:109`** — `bootstrap/index.js:81` inside the pinned `directus/directus:11.6.1` image.
  Requires pulling the image; out of scope.
- **`coursebuilder.md:101`** — "colony pub/sub (author-lifecycle emails → **messenger** → Brevo)". The
  `messenger` hop now runs in-process inside `app` (v9.0 fold), so naming it as a downstream *service*
  is the §5 "named-consumer list survives the merge" shape. I did not book it: `internal/web/backend/
  coursebuilder/notify.go` does exist and gates on `COURSEBUILDER_EMAILS_ENABLED`, and the sentence reads as
  naming the *domain* rather than a container. Flagging it here rather than silently dropping it.
