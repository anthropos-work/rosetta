# Seat E — reading #26

**Instrument (proof of delivery):** `briefing-r26-AS-DELIVERED.md`, 243 lines,
sha256 `4178b00f0d8e98eabddb895969d6e4c58799a5b292d967f8a2cfca6772a18d3d` (re-checked at my open with
`shasum -a 256`; matches the value in the task). Read in full, frozen half + ADDENDUM. Also read
`corpus/ops/platform-alignment.md` §5 in full (lines 249–1547, rules 1–48 + Trap E + the trailing
sub-sections).

**Corpus ref graded:** `rosetta` HEAD `3822501a` (branch `m257x/platform-realignment`).

## Clone refs re-derived at my open (I did NOT fetch anything — §5 rule 41a)

| tree | checkout | `origin/main` | matches addendum? |
|---|---|---|---|
| `stack-demo/app` | `ad9f3c49` | `ad9f3c49` (level) | ✅ superseded table |
| `stack-demo/app/studio` (own checkout) | `aeec036a` | `aeec036a` | ✅ |
| `stack-demo/cms/studio` (own checkout) | `aeec036a` | `aeec036a` | ✅ |
| `stack-demo/platform` | `0c91421d` | `0c91421d` (level) | ✅ superseded table |
| `stack-demo/next-web-app` | `8297c684` | `f97ba659` | ✅ superseded table |
| `stack-demo/sentinel` | `f2c46190` | level | ✅ |
| `stack-demo/studio-desk` | `41ee3575` | level | ✅ |
| `stack-demo/ant-academy` | `22df69dd` | level | ✅ |
| `stack-demo/storage` | `4ce8ece5` | `9f8cb532` | ✅ unchanged six |
| `stack-demo/messenger` | `fa47850d` | `e9421c68` | ✅ |
| `stack-demo/cms` | `ca50c817` | `f38c0c4a` | ✅ |
| `stack-demo/graphql-wundergraph` | `60c229f3` | `60c229f3` | ✅ |
| `stack-demo/roadrunner` | `87d8d443` | `87d8d443` | ✅ |
| `stack-demo/jobsimulation` | `462343b0` | `82cb66ec` | ✅ |
| `stack-demo/rosetta-extensions` (**pinned per-stack consumption clone — the adjudicating tree per addendum / §5 rule 45**) | `09d06070` | `4cb920aa` | ✅ |
| `.agentspace/rosetta-extensions` (authoring copy) | `944fc4a2` on `main` | — | ✅ |

Every ref matched the addendum's superseded table at open; nothing moved under me and I ran no
`fetch`/`pull`/state-changing git command.

## Positive controls

| file | `wc -l` | read in full? |
|---|---|---|
| `corpus/services/studio-room.md` | 507 | yes |
| `corpus/services/backend.md` | 371 | yes |
| `corpus/architecture/ai_architecture.md` | 305 | yes |
| `corpus/services/next-web-app.md` | 164 | yes |
| `corpus/services/clerk-integration.md` | 128 | yes |
| `corpus/architecture/README.md` | 38 | yes |
| **total** | **1513** | — |

(All six match the expected counts exactly. `next-web-app.md` and `architecture/README.md` have no
trailing newline, so the last rendered line number is one higher than `wc -l` — 165 and 39
respectively. Not a finding, recorded so the number is reproducible.)

Search-instrument controls run in this pass: bare `grep` (this shell's `ugrep -G --ignore-files`),
`git grep` at each tree's own ref, explicit enumeration of the two nested `studio` checkouts at
`aeec036a`, and a raw `find | xargs /usr/bin/grep` (no ignore rules). Positive controls used:
`service_discovery_namespace_id` over `*.tf` (19 files by `git grep`/ugrep, **25** by the raw
filesystem grep — the doc's own number, and the discrepancy is mechanism 1 of §5 rule 44);
`anthropos-work/colony` in `sentinel` (13 files); `JobSimulationService` in `app` Go source.

---

## Blockers

### B1 — `app/internal/ai/ai.go` is asserted not to exist; it exists, and it is the folded `ai` library

- **Anchor:** `corpus/architecture/ai_architecture.md:99` —
  *"selection/fallback in each consumer's own wrapper — in `app` @ `5ba17044` that is
  **`app/internal/jobsimulation/ai/ai.go:267,344`** and **`app/internal/skillerai/ai.go:347`**, *not* a
  bare `app/internal/ai/ai.go` (**no such file** — that path now resolves only inside the frozen
  `jobsimulation` husk repo)."*
- **Why it is false:** `app/internal/ai/ai.go` **exists** at the checkout ref `ad9f3c49`
  (`git -C stack-demo/app ls-tree ad9f3c49 internal/ai/ai.go` → 1 entry; `:8-17` declares the
  `type AI interface`). It arrived at `1e457fa70` — *"refactor(ai): fold the ai library into app as
  internal/ai"*, 2026-08-04, an ancestor of `ad9f3c49` and **not** of `b948604f`. The package's own
  guard test states the fold in words: `internal/ai/module_import_guard_test.go:14-31` —
  *"the import path this package used to live behind, before it was folded into app at tag v1.40.2"*,
  `inTreePath = "github.com/anthropos-work/app/internal/ai"`, and *"the module requirement was dropped
  from go.mod."* Measured: `git show ad9f3c49:go.mod | grep anthropos-work` returns
  `analytics-go`, `colony`, `proto`, `storage`, `taxonomy` — **no `ai`**; at `b948604f` the same grep
  returns `ai v1.40.2`. The consumer this passage is about now imports the in-tree copy:
  `internal/jobsimulation/ai/ai.go:13-15` imports `github.com/anthropos-work/app/internal/ai`.
- **Why the `@5ba17044` pin does not exempt it (§5 rule 33, part 3):** the *file:line* citations
  (`ai.go:267,344`, `skillerai/ai.go:347`) do resolve at `5ba17044` — I checked, all three are the
  `"flag_use_azure_us"` literal — so those are fine. The offending clause is the parenthetical, and it
  **asserts currency**: *"that path **now** resolves only inside the frozen `jobsimulation` husk repo."*
  A pin and the word *now* make opposite claims, and the reader acts on *now*. The clause is false at
  the checkout, at `origin/main`, and at `2035f9a4`.
- **Confidence:** high

### B2 — "All Go services access AI through the shared `ai` library" — no live Go service imports it any more

- **Anchor:** `corpus/architecture/ai_architecture.md:95-96` — *"All Go services access AI through the
  shared `ai` library, which provides: A single `ai.AI` interface across providers (OpenAI, Azure,
  Anthropic, Bedrock, Mistral)…"* (restated at `corpus/services/backend.md:313`, *"AI providers via the
  shared `ai` library"*).
- **Why it is false:** enumerated `github.com/anthropos-work/ai ` in `go.mod` at each clone's own HEAD:

  | repo | ref | requires `anthropos-work/ai`? |
  |---|---|---|
  | app | `ad9f3c49` | **no** |
  | sentinel | `f2c46190` | no |
  | storage | `4ce8ece5` | no |
  | messenger | `fa47850d` | no |
  | roadrunner | `87d8d443` | no |
  | graphql-wundergraph | `60c229f3` | no (not Go-AI) |
  | **cms** | `ca50c817` | yes — `go.mod:9`, `v1.40.2` (frozen husk, no compose service, not in `repos.yml`) |
  | **jobsimulation** | `462343b0` | yes — `go.mod:11`, `v1.40.2` (same) |

  Positive control in the same pass: `anthropos-work/colony ` resolves in `app` (`go.mod:15`) and
  `sentinel` (`go.mod:8`). So the only requirers are the two decommissioned husks; every **live** Go
  service either uses the in-tree `app/internal/ai` (app) or no AI at all (sentinel). The sub-claim
  *"a single `ai.AI` interface across providers (OpenAI, Azure, Anthropic, Bedrock, Mistral)"* is also
  not true of the in-tree copy: `git ls-tree ad9f3c49 internal/ai/` has `anthropic/` and `openai/`
  only, and Mistral OCR moved to its own package `internal/cms/studio/mistralocr` (imported at
  `internal/cms/studio/markdownManager.go:10`).
- **Confidence:** high

### B3 — "four of the five internal Go libraries are imported as private modules (`ai`, colony, proto, taxonomy)" — it is three

- **Anchor:** `corpus/architecture/README.md:21` — *"The five internal Go libraries — but only **four
  are imported as private modules** (`ai`, `colony`, `proto`, `taxonomy`). **`authn` is not a dependency
  of any service**…"*
- **Why it is false:** same measurement as B2. At the checkout refs, `ai` is in exactly the position this
  sentence reserves for `authn`: no live service's `go.mod` requires it (`app` `ad9f3c49` requires
  `colony`, `proto`, `taxonomy`, plus `analytics-go` and `storage`; `sentinel` `f2c46190` requires
  `colony` and `proto`). `ai` was folded into `app` as `internal/ai` at `1e457fa70` (tag v1.40.2,
  2026-08-04) and the module requirement dropped. The correct count of the five that are still imported
  as private modules is **three** — `colony`, `proto`, `taxonomy`.
- **Why I did not treat this as covered by B2:** it is a separate file, a separate enumerated set, and
  the sentence is the corpus's index-level statement of the library picture — the highest-propagation
  site of the three.
- **Confidence:** high

### B4 — Next.js version and range are both wrong; `^16.2.7` is the `@next/*` sibling packages' range, not `next`'s

- **Anchor:** `corpus/services/next-web-app.md:22` — *"**Next.js 16.2.7** (App Router, Turbopack),
  **React 19.2.7**, **Node ≥ 24**, **pnpm 10.30.3**. All four apps
  (`apps/{web,hiring,integration,maintenance}/package.json`) declare `"next": "^16.2.7"` and the
  lockfile resolves it."*
- **Why it is false:** measured at `next-web-app` **`8297c684`** — the very ref this file pins twice
  elsewhere (`:44`, `:75`, `:98`) — all four apps declare **`"next": "~16.2.12"`** (tilde, .12), not
  `"^16.2.7"`:
  `apps/web/package.json`, `apps/hiring/package.json`, `apps/integration/package.json`,
  `apps/maintenance/package.json`. `pnpm-lock.yaml` resolves **`next@16.2.12`** throughout (e.g. the
  `@sentry/nextjs` and `next-intl` peer strings all read `next@16.2.12`). What *is* `^16.2.7` is a
  different field: `@next/third-parties` and `@next/mdx` in the three app manifests, and
  `@next/eslint-plugin-next` / `eslint-config-next` in `configs/eslint-config/package.json` — the
  wrong-field-name failure of §5 rule 3. The other three scalars in the same sentence are correct
  (`"react": "^19.2.7"` ✓; root `engines.node ">=24.0.0"` ✓; `packageManager: pnpm@10.30.3` ✓), which is
  what makes the wrong one credible.
- **Confidence:** high

### B5 — `@clerk/nextjs` "all four on `^6.39.2`" — three of the four are `^6.39.6`

- **Anchor:** `corpus/services/clerk-integration.md:103` — *"`@clerk/nextjs` is **aligned** across
  `next-web-app` (`apps/{web,hiring,integration}`) and `ant-academy` (`code/package.json:52`) — all four
  on `^6.39.2`."*
- **Why it is false:** measured at the refs the addendum names —
  `next-web-app` `8297c684`: `apps/web`, `apps/hiring`, `apps/integration` all declare
  `"@clerk/nextjs": "^6.39.6"`;
  `ant-academy` `22df69dd`: `code/package.json:52` declares `"@clerk/nextjs": "^6.39.2"` (that one
  anchor is exact). So the stated common version is wrong for three of the four, and the load-bearing
  word **"aligned"** no longer holds as written. The rest of the same bullet checks out and I cleared
  it: `next-web-app/apps/mobile/package.json:6` = `~2.6.18` ✓, `ant-academy/mobile/package.json:18` =
  `~2.19.36` ✓ ("thirteen minor versions apart" ✓), `app/go.mod:31` @ `5ba17044` =
  `github.com/clerk/clerk-sdk-go/v2 v2.7.0` ✓ (still v2.7.0 at `ad9f3c49`, now at `:29`).
- **Why I hesitated:** `^6.39.2` and `^6.39.6` are the same caret family and a lockfile would resolve
  them identically, so "aligned" is arguably defensible in spirit. The published *scalar* is not — and
  this bullet exists precisely to be a version ledger that gates a Clerkenstein-DNA re-verification, so
  a wrong scalar here is acted on.
- **Confidence:** high (on the scalar), medium (on "aligned")

### B6 — "the literal's only occurrence anywhere in the clone set" is false by the paragraph's own definition of the clone set

- **Anchor:** `corpus/services/backend.md:148-149` — *"…**59** files → **0** (positive control on
  `service_discovery_namespace_id`: 25 files). **The literal's only occurrence anywhere in the clone set
  is a markdown KB page** — `app/knowledge/service-dependencies.md:52` @ `ad9f3c49`…"*. Restated at
  `corpus/services/backend.md:294` — *"the literal's sole occurrence is a past-tense markdown KB page"*.
- **Why it is false:** the paragraph defines its universe two sentences earlier as *"the **44** tracked
  `.tf` files in the 13-repo `stack-demo` clone set"*. I re-derived that set: the 13 top-level git trees
  under `stack-demo` hold exactly 44 tracked `.tf` files — app 5, cms 4, graphql-wundergraph 4,
  jobsimulation 6, messenger 4, next-web-app 2, roadrunner 4, sentinel 4, storage 5, studio-desk 4,
  **rosetta-extensions 2**, ant-academy 0, platform 0. **44 is only reachable by counting
  `rosetta-extensions`.** Grepping the exact literal `backend.internal.anthropos:8081` with `git grep`
  at each tree's own ref (nested repos included) returns **six** tracked sites, not one:
  - `app` `HEAD:knowledge/service-dependencies.md:52` (the one the doc names)
  - `rosetta-extensions` (pinned clone `09d06070`) `stack-core/tests/fixtures/repair_leak/post/corpus/services/cms.md:32` and `:157`
  - `rosetta-extensions` `stack-core/tests/fixtures/repair_leak/pre/corpus/services/cms.md:32` and `:157`
  - `rosetta-extensions` `stack-core/tests/test_platform_predicate_guard.py:435`

  A raw filesystem grep adds an untracked seventh,
  `stack-demo/rosetta-extensions/demo-stack/stacks/demo-1/clones/app/knowledge/service-dependencies.md`.
  Separately, the near-miss a reader will hit first: `graphql-wundergraph` `60c229f3`
  `supergraph-config-prod.yaml:6` and `CLAUDE.md:18` both carry
  `http://backend.internal.anthropos:8080` — same host, different port, and a live production
  `routing_url`.
- **Why I hesitated:** the load-bearing half — *"no terraform in the clone set names
  `http://backend.internal.anthropos:8081`"* — is **TRUE and I verified it by both mechanisms the
  paragraph names**, including the 44/59/25 figures, all of which reproduced exactly. The surviving
  occurrences are tooling test fixtures (copies of old corpus prose) rather than platform source. I
  booked it anyway because the sentence is an explicit *exhaustiveness* claim, it appears twice, and the
  reader who re-runs the grep the paragraph invites gets six hits and starts adjudicating instead of
  measuring (§5 rule 19).
- **Confidence:** medium

### B7 — `cert.py` is described as "Certificate / signing helpers"; it is the certification-benchmark CLI

- **Anchor:** `corpus/services/studio-room.md:67` — in the Project Structure tree:
  `├── cert.py             # Certificate / signing helpers`
- **Why it is false:** `git -C stack-demo/app/studio show aeec036a:cert.py` is a 76-line file whose own
  module docstring reads *"CLI: python cert.py [--branch stable|experimental] [--no-judge]
  [--max-concurrent N] — Run the certification phase against
  `benchmark/certification/test-set/*.json`"*, and it does exactly four things: align the `version`
  field across `configs/*.ini`, bump the minor version, generate + benchmark every test-set blueprint,
  and aggregate into `benchmark/certification/version/<version>.md`. Its only import is
  `from benchmark.certification.runner import run_certification`. Nothing in it touches certificates,
  keys, or signing. The repo also carries the corroborating surface: `.claude/skills/run-certification/`
  and `benchmark/certification/{runner.py,report.py,version.py,test-set/,current-set/,version/}`.
- **Why I hesitated:** it is a one-line comment inside an illustrative directory tree, so a reader who
  opens the file is corrected immediately, and no other claim in the corpus depends on it. Booked as a
  BLOCKER per the instrument's tie-break rule: it is a false statement about what a named file does, and
  "certificate / signing" sends a security-minded reader hunting for a signing path that does not exist.
- **Confidence:** medium

---

## Minors

- `corpus/services/backend.md:19` — *"compose now declares **five** services"*. `docker-compose.yml`
  itself declares five (`sentinel`, `backend`, `studio-desk`, `next-web-app`, `gotenberg`), but it opens
  with `include: common.yml` (`:1-2`), which adds `postgresql` and `redis`, so the compose **project**
  declares **seven**. The corpus elsewhere states this precisely (`service_taxonomy.md:66` — *"declares
  five services (seven in the effective topology, once `include: common.yml` adds the…)"*); this
  unqualified form is the one §5 rule 30 warns about. Worse, the number collides: root `CLAUDE.md`'s
  *"`core` starts **five** containers"* names a **different** five (backend, gotenberg, postgresql,
  redis, sentinel).
- `corpus/services/backend.md:38-39` — *"…still **declare** a Connect service … on their own muxes:
  storage's `StorageService` (`storage@4ce8ece5:sdk/storage/v1/service.go`)…"*. That file is the
  **client SDK wrapper** (`func NewService(c storagev1connect.StorageServiceClient, namespace string)`),
  not a mux registration. The construct the sentence describes is
  `storage@4ce8ece5:cmd/root.go:63` (`rpcMux.Handle(storagev1connect.NewStorageServiceHandler(`). The
  roadrunner sibling anchor (`cmd/root.go:87`) is exact and messenger's (`internal/rpcsrv/rpcsrv.go`,
  the server impl; its mux is `cmd/root.go:84`) is defensible, which makes the storage one the odd row.
  The claim itself is true.
- `corpus/architecture/ai_architecture.md:34` — `internal/cms/studio/markdownManager.go:19` is cited as
  a Mistral-OCR use site. At `b948604f` that line **is** `mistral.NewMistral(nil,
  os.Getenv("MISTRAL_API_KEY"))`; at the checkout `ad9f3c49` the file was rewritten onto
  `internal/cms/studio/mistralocr` and `:19` is now a comment line (it does still contain the string
  `MISTRAL_API_KEY`, so it is not a *wrong* landing so much as a drifted one). The sentence names no
  ref. Same drift at `external_services.md:565`, which is outside my file set.
- `corpus/architecture/ai_architecture.md:235` — *"the enum is
  `app/internal/cms/directus/collections/jobsimulation.go:983-990`"*. The `AIModel` const block is
  `:979-991` (members `:980-990`); the cited range omits `anthropic-35-sonnet-aws` (`:980`),
  `anthropic-37-sonnet-aws` (`:981`) and `anthropic-4-sonnet-aws` (`:982`). It does contain all three
  examples the sentence names, so it reads as "the members I quoted" rather than "the enum".
- `corpus/architecture/ai_architecture.md:60` — cites `external_services.md:619-629` as *"item 4 of the
  four live EU-exit routes"*; item 4 begins at `external_services.md:622` (`:619-621` are the tail of
  item 3).
- `corpus/services/studio-room.md:348-359` — the reproduced `requirements.txt` lists `mistralai` third;
  the file's actual order at `aeec036a` is `openai, anthropic, rich, pyyaml, requests, jinja2,
  mistralai, pytest, pytest-asyncio`. Every package named is present and the count is right.
- `corpus/services/clerk-integration.md:99` — *"gated on the `STUDIO_ACCESS_ROLES` set — `org:admin`
  **and** `content_creator`"*. The set has four members at `studio-desk` `41ee3575`
  (`src/index.ts:96`, `app/services/userService.ts:16`): `['admin', 'org:admin', 'content_creator',
  'org:content_creator']`. An abbreviation rather than an error, but the prefixed/bare pair is exactly
  the distinction this document spends two rows teaching (`:21`, `:65`).

---

## Positively cleared (audited zeros)

**ENUMERATED** — predicate re-derived from source independently, cardinality compared:

1. `studio-room.md:184` *"`gen.py` registers **exactly nine** arguments (`gen.py:484-492`)"* —
   enumerated the `parser.add_argument` calls at `aeec036a`: nine, at `:484`–`:492` inclusive, with
   `argparse.ArgumentParser(...)` at `:483` and `parse_argument(parser)` at `:493`. No `--template`.
2. `studio-room.md:362-374` (the whole `mistralai` callout) — `git grep -ci mistral aeec036a` at the
   **nested repo's own ref** returns 3 files / **22** matching lines (`requirements.txt` 1,
   `tools/pdf2md.py` 18, `tools/r3.py` 3). `pdf2md.py:24` `from mistralai import Mistral` ✓, `:96`
   `Mistral(api_key=…)` ✓, `:127` `model="mistral-ocr-latest"` ✓, `r3.py:194`
   `if "MISTRAL_API_KEY" not in env` ✓. `services/ai.py:705-708` = `{'openai','azure','anthropic'}` ✓,
   `ai.py:1-2` imports only `openai`/`anthropic` ✓. `app/studio/.gitignore:2` = `tools/` ✓,
   `app/.gitignore:79` = `studio/*` ✓, `cms/.gitignore:129` = `studio/*` ✓, and I confirmed
   `stack-demo/cms/studio` is the same repo at the same sha `aeec036a`. `app/Dockerfile:45-46` copies
   `studio/` and pip-installs ✓. Go execs exactly two studio scripts —
   `studioManager.go:119` (`studio/gen.py`) and `:1045` (`studio/postgen.py`) — **at both `b948604f`
   (the pinned ref) and `ad9f3c49`** ✓. `pdf2md` is referenced nowhere outside `tools/` ✓.
3. `backend.md:29-34` / `:98` *"six handlers in total; only three of the eight merged services"* —
   enumerated `mux.Handle` at `main.go:1297-1338` @ `ad9f3c49` **and** `:1185-1228` @ `b948604f`.
   Both ranges are exact and both give Users, Organizations, Skiller, JobSimulation, CMS
   (`if cmsRPCServer != nil`, `:1212-1214` / `:1322-1324`), LabSession — six, three of them folded.
   `storagev1connect` appears only in two `_test.go` files, so storage genuinely has no handler.
4. `backend.md:36` / `:100` *"`SkillPathSessionService`: **0** occurrences in `app` Go source"* —
   `git grep -n SkillPathSessionService ad9f3c49 -- '*.go'` → 0 (positive control
   `JobSimulationService` → hits in 3+ files); the only two occurrences tree-wide are
   `app/CLAUDE.md:109` and `app/knowledge/architecture.md:28`, **exactly** the two anchors `:102`
   names, and both byte-identical at `2035f9a4` ✓. No `skillpath…v1connect` package is imported
   (enumerated all `*v1connect` import identifiers: authorization, cms, jobsimulation, lab,
   organizations, skiller, storage, users).
5. `backend.md:55-66` / `:317` *"five both-ways streams, six subscribers, five publishers"* —
   enumerated every `pubsub.NewPublisher` at both refs: `main.go:325/:746/:1149` +
   `jobsimwiring/wiring.go:132/:185` @ `ad9f3c49`, and `main.go:287/:637/:1039` +
   `wiring.go:127/:180` @ `b948604f` — five each, exactly as printed. Subscriber side:
   `buildStreamSubscribers` (`subscriber_wiring.go:203-251`) builds six entries, `subs[d.Streams.Skiller]`
   at `:209` and `subs[d.Streams.Backend]` at `:248`, applied by the single loop at `main.go:1579-1581`;
   `streamNamesForSubscribers` at `:1534-1541`; `SERVICE_NAME` defaults to `backend` at `:230-232`; the
   quoted comment is verbatim at `subscriber_wiring.go:112-113`. `skiller` is subscribed and never
   published ✓.
6. `backend.md:284` *"**13** `ai_readiness_*` ent tables"* — enumerated `CREATE TABLE …
   "ai_readiness…"` across **all 170** migration files at `ad9f3c49`: exactly 13 distinct tables, zero
   `DROP TABLE`. All four named as the omissions from a "9" are present
   (`recommendations`, `email_overrides`, `notification_logs`, `notification_optouts`), and
   `live_snapshots` + `text_translations` are indeed among the other nine. `internal/aireadiness/`
   exists with `manager.go`/`cycles.go`/`diagnosis.go`/`compare.go`/`csv.go`; `internal/workforce/`
   contains **no** `readi*` file; `graph/schemas/ai_readiness.graphqls` exists.
7. `backend.md:352` migrations — enumerated the file set, not the sum: **170** `.sql` at `ad9f3c49`,
   **169** at `b948604f`; **46** with a `202606`-or-later prefix; `20260529072659_add_lab_session.sql`
   sits at position **124/170**, i.e. exactly 46 back from the head. Head at `ad9f3c49` =
   `20260803143844_ai_readiness_recommendation_path.sql` ✓; head at `b948604f` =
   `20260731154527_academy_chapter_progress_completed_at.sql` ✓ with the three predecessors named ✓.
   `migrations/` at top level is empty at `b948604f`, `2035f9a4` **and** `ad9f3c49` ✓;
   `6a46e8445` is *"chore(migrations): remove obsolete atlas.sum file"*, 2026-06-18 ✓;
   `atlas.hcl` has `dir = "file://terraform/migrations"` and `src = "ent://internal/data/ent/schema"` ✓.
8. `backend.md:144-148` the terraform derivation — enumerated per clone: **44** tracked `.tf` in the
   13 repos, **59** on the filesystem, **0** hits for the literal by `git grep`, by ugrep and by
   `find | xargs /usr/bin/grep`; the positive control `service_discovery_namespace_id` returns exactly
   **25** files by the raw filesystem mechanism the sentence attributes it to. Every terraform anchor
   in the derivation resolves: `app/terraform/locals.tf:6` `project = "backend"` (identical at
   `ad9f3c49`), `:8` `rpc_port = 8081`, `main.tf:58` `service_discovery_namespace_id`,
   `main.tf:185-186`, `variables.tf:197`/`:230` @ `b948604f`, and at `ad9f3c49`
   `cms_rpc_address` at `:309` with `storage_rpc_addr` **deleted** and `storage_s3_bucket` (`:349`) /
   `storage_s3_public_bucket` (`:354`) in its place; `messenger/terraform/variables.tf:77,82,87,92`.
   (The one thing inside this block I booked is B6 — the *"only occurrence"* sentence, not the numbers.)
9. `backend.md:307` *"`STORAGE_RPC_ADDR` has 0 read sites — its 3 remaining occurrences are comments"* —
   enumerated in Go source at `ad9f3c49`: exactly three, all comments
   (`main.go:504`, `internal/jobsimwiring/wiring.go:101`, `internal/storagens/callsites_test.go:189`),
   and `:504` reads *"the standalone service takes no traffic and STORAGE_RPC_ADDR is gone."*
   `main.go:524/:525` are the two manager constructors ✓, and `main.go` is **byte-identical**
   (sha256 `868273840c…`) between `2035f9a4` and `ad9f3c49`, exactly as `:307` claims.
10. `backend.md:171-182` the subgraph history — enumerated `schemas/` at each named commit:
    `749dc86` (2026-06-24, *"remove skiller subgraph…"*) deletes `skiller.graphqls`, leaving **4**;
    `7c17e63` (2026-07-21) deletes `skillpath.graphqls`, leaving **3**; `915da06` (2026-07-29) deletes
    **both** `cms.graphqls` and `jobsimulation.graphqls` in one commit → **1**, while its own subject
    line says "2→1", exactly as the corpus warns. `60c229f3` holds `backend.graphqls` alone.
11. `clerk-integration.md:37` *"Webhooks (svix, **12** event types)"* — enumerated the switch in
    `app/internal/clerk/events/events.go`: 12 cases at `:166,172,178,185,191,197,204,210,216,223,229,235`
    (`user.{created,updated,deleted}`, `organization.{created,deleted,updated}`,
    `organizationInvitation.{accepted,created,revoked}`,
    `organizationMembership.{created,deleted,updated}`).
12. `clerk-integration.md:51` / `:91` *"129 files in `app` vs 0 in `sentinel`"* — enumerated
    `colony/authn` importers: `app` `ad9f3c49` = **129** `.go` files (134 files counting non-Go);
    `sentinel` `f2c46190`, `storage` `4ce8ece5`, `messenger` `fa47850d` = **0** each. Positive control:
    `anthropos-work/colony` in `sentinel` = 13 files, so the pipeline was live.
13. `ai_architecture.md:62-66` *"the Directus enum has **five** members and the switch **four** cases,
    so `azureglobal` is the one with no case"* — enumerated both sets from source:
    `jobsimulation.go:969-975` = `{Azure, Azureglobal(:971), Openai, AnthropicAws, Anthropic}`;
    `simulator/ai/ai.go` cases at `:58`, `:69`, `:86`, `:102` + `default:` at `:114-115`. Every other
    anchor in that section resolves to the named construct: `:20-26`
    (`GetValidationAIVendorAndModel` → Azure / GPT4Dot1 / GPT4Dot1Mini), `:65-66`, `:82-83`,
    `:98-99` (Claude 3.7 Bedrock), `:110-111` (Claude 3.5), `:126-127`;
    `jobsimulation.go:905` `AIVendor *AIVendor`, `:1297` `simulation.Gpt5Point1`, `:1302-1305`
    `aiVendor := simulation.Openai`, `:1307` sequence construction. `getClient` is `:259-289` with the
    four consts at `:30-33`, `flag_use_azure_us` at `:267`, the failed-lookup-keeps-EU branch at
    `:270-273`, `config.WithRegion("eu-west-1")` at `:87`, `isThrottlingError` at `:129/:166/:325`.
14. `ai_architecture.md:267-268` *"`checkerEngines` … stored and **never read**"* — enumerated all six
    occurrences tree-wide (`basevalidator/criterion.go:34,72,90`;
    `v3/validator/validator.go:43,60,595`): field, parameter, assignment, field, construction,
    pass-through. Never dereferenced for dispatch — the dispatch is the hardcoded
    `switch critCheck.Engine { case check.EngineLlm: …}` at `criterion.go:126-127`. `:428`
    `NewLLMBulkChecker(c.logger)` ✓, `:168` `validateCodeDiff` in a sibling pool goroutine to `:175`
    `validateLLM` ✓, `:450-475` sets `success` from a string comparison ✓,
    `templates/checkValidationBulk.tmpl` exists ✓, `ai.WithTemperature(0.0)` at `bulkChecks.go:118` ✓,
    `skills.go:40-51` (with its `// TODO fix this formula`) ✓, `:53-62`/`:75` ✓,
    `skill.go:617-623` `convertLevelTo100` ✓, and the frontend mapping
    (`CompetencyReadLevel.tsx:18` → `packages/graphql/src/utils/skillScore.ts`, a plain
    `Math.round((levelsCount * score) / 100)`) ✓ — no 60/65/75/85/95 ladder anywhere on that path.
15. `ai_architecture.md:113-136` the studio slot table — enumerated all 10 `*_MODEL` rows in
    `production_config.ini:26-36` and `development_config.ini:26-36`: byte-identical to each other and
    stable == experimental, exactly as tabulated. `config_template.ini:37-41` carries the older
    `gpt-4.1-mini / gpt-4.1 / gpt-4o / gpt-4o / o3` column, with `:39` and `:40` the two `gpt-4o` slots
    the blockquote names. `gpt-5.2` appears in **no** `configs/*` file — only `services/ai.py:356` and
    `:508` (pricing) plus two knowledge docs and the changelog ✓. `max_tokens = 4000` in all three
    tracked configs ✓. `grep -in -E 'bedrock|boto3'` over `app/studio` returns **0** by both `git grep`
    at `aeec036a` and a raw filesystem grep (positive control: `anthropic` → 13 files) ✓.
16. `next-web-app.md:70` *"8 locales"* — enumerated `configs/i18n/messages/`: `de en es fr it ja nl pt`
    = 8 ✓. `:97-99` *"no `storybook` script and no `.storybook/` dir"* — enumerated: 0 `"storybook"`
    scripts, and the single residual path is `configs/tailwind/storybooks.css`, exactly as stated ✓.
    `:119` *"the only Dockerfile in the repo"* — `git ls-tree -r 8297c684 | grep -i dockerfile` →
    `Dockerfile.dev` only ✓.
17. `architecture/README.md` index completeness — enumerated `corpus/architecture/`: 10 non-README
    files, all 10 indexed, none missing, none dangling.

**Other anchors opened and confirmed by reading the surrounding lines** (not full-set enumerations, so
not clearances in the strong sense, but each verified in situ with `sed -n 'N-15,N+5p'`):
`studio-room.md` — `gen.py:18-28`, `:205-238`, `:241-271`, `:273-282`, `:450-456`,
`exporter.py:513-550` and `:518-519`, `model.py:59` + `:467-469`, `services/taxonomy.py:11`
(`api.anthropos.work`), `CLAUDE.md:12-14`, `postgen.py:396-398` (all three flags `required=True`),
the postgen target modules (`guidance/metadata/toolkit/translation` + `testing`, with `exporter`
deliberately not a target), the state-file trio, the four `work_paths`, `usage.checkpoint()` /
`get_report()` (`services/ai.py:140`, `:183`), `worker.go:29` `Concurrency: 5` and
`worker.go:32-33` `AiVideo: 7` / `Studio: 3`, `python:3.11-slim` (`Dockerfile:28`),
`build-production.yml:28-30` `additional_repo: "anthropos-studio-room:studio"`, and app CHANGELOG
`v1.360.1` = *"pull studio via additional_repo like cms"*.
`backend.md` — `docker-compose.yml:48/:57/:59/:110/:183` @ `0c91421`, zero `*_RPC_ADDR` in
`docker-compose.yml`+`common.yml` (positive control: 1 `ADDRESS`), `repos.yml` = 4 entries,
`Makefile:10 PROFILE ?= core`, `main.go:743-746` (labs conditional), `main.go:1195-1199` +
`internal/web/backend/backend.go:137-138,324` (`/api/webhook/directus` fail-closed on
`DIRECTUS_WEBHOOK_SECRET`), `main.go:1353` `rpc.WithWriteTimeout(60*time.Second)`,
`internal/academy/academy.go:6-9`, every `internal/` directory in the code map (all present),
`rpc.go` at the repo root, Go `1.26.4`, `20260518125439.sql` (app) and `cms` `20250116133510.sql`,
`storage@4ce8ece5:cmd/root.go:63`, `messenger@fa47850d:cmd/root.go:84` +
`internal/rpcsrv/rpcsrv.go:26,29`, `roadrunner@87d8d443:cmd/root.go:87`, and `d11a403` deleting the
`cms` + `jobsimulation` compose services.
`ai_architecture.md` — `coursebuilder/bedrock.go:106-113`, `studioManager.go:583`,
`jobsimulation.go:1079-1085` (4-member voice enum) and `:1594-1600` (nil → `gptrealtime`),
`calls/livekit.go:131-135` / `:140-144` inside `CreateAgentDispatch` (`:106`) with
`agentName = "anthropos-agent"` / `agentEndpoint = "openai-hosted"`, the `job_role_embeddings` /
`skill_embeddings` DDL (`extensions.vector(1536)`, IVFFLAT) in
`terraform/migrations/20260615130000_skiller_taxonomy.sql`, and the self-citation `:15-17`.
`next-web-app.md` — `UserStatusContext.tsx:144-148` / `:168-172`,
`InsightsByMembersContainer.tsx:108`, the hiring `[simId]/page.tsx:14`,
`proxy.ts` in web/hiring/integration with **no** `middleware.ts` anywhere,
next-web-app `CLAUDE.md:15` and `:55` verbatim, `docker-compose.yml:143/:151/:160/:165-167`,
`Makefile:119-120`, the public-route allowlist (all seven named routes present; `/print`
HMAC-gated at `:59,66-70`), test scripts in `web`+`hiring` only, `graphql-request` +
`@tanstack/react-query` with no Apollo, `turbo ^2.9.6`. In the **pinned** rext clone `09d06070`
(the adjudicating tree, §5 rule 45): `demo-stack/frontend/hiring.Dockerfile` exists and
`demo-stack/up-injected.sh:1076-1085` is the `build_frontend_hiring` block with
`img="demo-$N-hiring"` and port 3001 — all four sub-claims exact.
`clerk-integration.md` — `apps/web/src/app/api/metabase/route.ts:35` (`authData.orgRole !== 'admin'`),
`studio-desk` `src/index.ts:96` + `app/services/userService.ts:16` (`STUDIO_ACCESS_ROLES`),
`ant-academy` `code/proxy.js:5,25,37,91,96` (`REQUIRE_ORGANIZATION_MEMBERSHIP` → `/no-organization`).

**RE-DERIVED only (weakest clearance — recorded as such per §5 rule 24):**
`backend.md:129-131` — 42,790 public + 794 org-private = 43,584 total reproduces arithmetically. The
*set* is a production table I cannot enumerate from any clone, so this is not a clearance.

---

## What I could not settle

- **Every production-DB row count** in `backend.md:128-135` and `ai_architecture.md:141`
  (42,790 / 43,584 / 22,490 / 22,470 / 18,919 / 23 / 1,447). Settled only by a read-only prod query,
  which is outside a read-only corpus audit. Note one internal gap worth a repairer's attention rather
  than a booking: `ai_architecture.md:141` asserts `public.skill_embeddings = 42,790` **over the public
  subset, measured 2026-06-29**, but the 2026-06-29 capture as quoted in `backend.md:133` lists only
  *"42,790 skills / 22,470 job roles / 18,919 job-role embeddings"* — no `skill_embeddings` figure — and
  the 2026-07-08 prod measurement in `backend.md:131` gives `public.skill_embeddings = 43,584`
  (i.e. the **total**, not the public subset). The two are reconcilable (one embedding per skill) but the
  corpus never states the reconciliation.
- `ai_architecture.md:154` — the *skiller* migrations `20260417103036` and `20260417120309`. Neither
  exists in `app/terraform/migrations/`, and the `skiller` repo is in **no** clone set, so the
  attribution is unverifiable from here. The *shape* they are cited for (dedicated embedding tables,
  `extensions.vector(1536)`, IVFFLAT, FK to `job_roles`/`skills`) I did confirm, in the ported
  `20260615130000_skiller_taxonomy.sql`.
- `ai_architecture.md:187-192` — the five LiveKit agent repositories and their dispatch behaviour.
  Settled only by `gh api orgs/anthropos-work/repos`; the clone set contains none of them. The
  platform-side half (`anthropos-agent` / `anthropos-agent-<location>`) I did verify at
  `calls/livekit.go:126-127`.
- `backend.md:68-88` — whether M810's infrastructure-side deletion of `module.cms_euwest1` has been
  applied. The document itself declares this unmeasurable (`infrastructure` is in no clone set) and
  states both measured facts without asserting either; I confirmed both halves it *can* be graded on
  are correctly framed, and did not book it.
- `next-web-app.md:80` / `:162` and `backend.md:299` — *"in prod, the router"* / *"the Cosmo Router is
  prod-only now"*. `graphql-wundergraph` `60c229f3` still carries a `supergraph-config-prod.yaml`
  pointing at `http://backend.internal.anthropos:8080/graphql/query`, which is consistent, but whether
  the router is actually deployed in production is not derivable from any tree here.
- Whether `github.com/anthropos-work/ai`'s own repo still exposes `Mistral`/`Bedrock` constructors
  (relevant to B2's sub-clause). The `ai` repo is in no clone set; I graded the sub-clause only against
  `app`'s in-tree copy, where it does not hold.

---

## One cross-file observation for the repairer (not booked here — anchors outside my file set)

The `ai`-library fold (`app` `1e457fa70`, tag v1.40.2, 2026-08-04) has **not** propagated anywhere in
the corpus that I touched, and it reaches past my three anchors (B1/B2/B3). Two further live sites I saw
while checking cross-references: `corpus/services/backend.md:313` (*"AI providers via the shared `ai`
library"*, folded into B2 above) and `corpus/architecture/external_services.md:565`
(*"`mistral.NewMistral(...)` in `app/internal/cms/studio/markdownManager.go:19`"* — the call moved to
`internal/cms/studio/mistralocr` in the same refactor). Root `CLAUDE.md`'s **Shared Libraries** block
also states the four-imported-modules set. Per §5 rule 19's corollary this is a **predicate**, not a
claim — *"`ai` is an externally-imported private Go module"* — and it should be swept tree-wide
(including `CLAUDE.md`, the highest-propagation site) rather than repaired file-by-file.
