# Seat B — reading #25

**Refs I graded against** (re-verified at this reading's open, `git rev-parse --short=8 HEAD` per tree):

| tree | sha | note |
|---|---|---|
| `stack-demo/app` | `ad9f3c49` | = `origin/main`; `b948604f` still reachable (used where a claim pins it) |
| `stack-demo/app/studio` | `aeec036a` | own checkout, invisible to `git -C app grep` |
| `stack-demo/cms` | `ca50c817` | checkout; `origin/main` = `f38c0c4a` |
| `stack-demo/cms/studio` | `aeec036a` | own checkout |
| `stack-demo/platform` | `0c91421d` | level with origin |
| `stack-demo/next-web-app` | `8297c684` | checkout; `origin/main` = `f97ba659` |
| `stack-demo/jobsimulation` | `462343b0` | checkout; `origin/main` = `82cb66ec` (contains `6092c6d2`) |
| `stack-demo/storage` | `4ce8ece5` | checkout; `origin/main` = `9f8cb532` |
| `stack-demo/messenger` | `fa47850d` · `stack-demo/roadrunner` `87d8d443` · `stack-demo/graphql-wundergraph` `60c229f3` · `stack-demo/sentinel` `f2c46190` · `stack-demo/studio-desk` `41ee3575` · `stack-demo/ant-academy` `22df69dd` | |
| `stack-demo/rosetta-extensions` | **`09d06070`** (`fast-build-m257x-iter-101`) — **the per-stack PINNED consumption clone; this is the tree I used to settle every tooling claim** (addendum / §5 rule 45) | |
| `.agentspace/rosetta-extensions` | `944fc4a2` — authoring copy, **not** used to settle anything | |

## Positive controls

| file | `wc -l` | read in full? |
|---|---|---|
| `corpus/services/ai-readiness.md` | 668 | yes |
| `corpus/services/cms.md` | 313 | yes |
| `corpus/services/chronos.md` | 245 | yes |
| `corpus/services/ai-labs.md` | 158 | yes |
| `corpus/services/README.md` | 88 | yes |
| `corpus/services/skiller.md` | 69 | yes |
| **total** | **1541** | matches the assignment |

Search positive controls run in-pass: `MESSENGER_RPC_ADDR` returned 0 across all 15 git trees (13 top-level +
2 nested `studio`), each at its own ref — paired with `SKILLER_RPC_ADDR` in the same loop, which returned
21/9/12/4/20/1 in app/jobsim/cms/messenger/rext/platform. `git -C stack-demo/platform log -S 'MESSENGER_RPC'
0c91421d` → **0**; control `-S 'SKILLER_RPC'` → **7**. Both figures are exactly what
`services/README.md:44` publishes.

---

## Blockers

### B1 — three `readiness.go` anchors for the two cycle-filter branches name the WRONG FUNCTION at the graded ref

- **Anchor:** `corpus/services/ai-readiness.md:429` (and `:432`, `:445`)
  - `:429` — *"for an active cycle, the membership filter is `keepInCycleStep1` (`readiness.go:388`, **`:662-664`**)"*
  - `:432` — *"`keepStartedMembers` is the **other** branch — the lifetime filter used when there is **no** active cycle (`:390`, **`:684`**)"*
  - `:445` — *"`queryInCycleStep1Completers` (**`readiness.go:638-652`**) demands `step_type = skill_mapping` **AND** `status = completed` **AND** `completed_at >= cycle.start`"*
- **Why it is false:** measured at `app` **`ad9f3c49`** (the checkout **and** `origin/main`; the passage pins
  no ref, so the instrument grades it here):

  | claim's anchor | what is actually there @ `ad9f3c49` | where the named construct really is |
  |---|---|---|
  | `readiness.go:662-664` → `keepInCycleStep1` | inside **`audienceScope`** (`:655`) — `if len(tags) == 0 { return "", nil, nil }` / `ids, err := m.audienceUserIDsForTags(...)` | `func (m *Manager) keepInCycleStep1` is at **`:710`** |
  | `readiness.go:684` → `keepStartedMembers` | **`func (m *Manager) queryInCycleStep1Completers(...)`** — i.e. it lands on **the very function the sentence is contrasting `keepStartedMembers` against** | `func (m *Manager) keepStartedMembers` is at **`:730`** |
  | `readiness.go:638-652` → `queryInCycleStep1Completers` | `func cycleAudienceTags` (`:637`) + the `audienceScope` doc comment | `queryInCycleStep1Completers` is at **`:684`** |

  Derived with `git -C stack-demo/app grep -n 'func (m \*Manager) keepInCycleStep1\|…keepStartedMembers\|…queryInCycleStep1Completers' HEAD -- internal/aireadiness/`, then read with `sed -n` around each line.
  The *semantics* the passage states are correct — I confirmed `readiness.go:387-391` really does branch
  `cyc != nil → keepInCycleStep1` / `else → keepStartedMembers`, and `:684-692` really does filter on
  `AIReadinessUserStepProgress` — so this is an **anchor** defect, not a mechanism defect.
- **Why I hesitated (state it, per the instrument):** all three anchors are **byte-exact at `b948604f`**
  (`queryInCycleStep1Completers` `:638`, `keepInCycleStep1` `:664`, `keepStartedMembers` `:684`) — the sha the
  frozen briefing table names, superseded by the addendum. Neighbouring anchors in the same file that sit
  *above* the changed region (`:139` `computeTier1`, `:289` `GetAIReadinessWithOptions`, `:330`
  `computeOrgBreakdowns`, `:388`/`:390` the call sites) are identical at both refs and verify. So the cause is
  the clone advance, not a bad hand-derivation. I book it because the addendum is explicit that only a claim
  **pinned** at `b948604` is graded there, and because §5 rule 46 is exactly this class: *an anchor that
  resolves but names the wrong construct*. `:684` is the sharp one — a reader following it concludes the doc
  has the two branches backwards, which is the error the passage says it corrected at iter-52.
- **Confidence:** medium (the measurement is certain; the grading turns on which `app` ref settles an
  unpinned claim)

### B2 — two `steps.go` anchors for `queryReadinessStarters` land inside a different query at the graded ref

- **Anchor:** `corpus/services/ai-readiness.md:436-438` and `:441`
  - `:436-438` — *"`queryReadinessStarters` (**`aireadiness/steps.go:915-938`**) is `SELECT DISTINCT user_id FROM public.ai_readiness_user_step_progresses … AND status <> 'not_started'`"*
  - `:441` — *"The platform states this itself at **`steps.go:907-914`** (*"This DB signal is the only real 'has started' check"*)"*
- **Why it is false:** at `app` `ad9f3c49`, `internal/aireadiness/steps.go:911-921` is the doc comment for
  **`queryReadinessSimCompleters`** and `:922-955` is its body, whose SQL is
  `SELECT DISTINCT s.owner_id, ars.step_type FROM public.job_simulation_sessions s JOIN public.ai_readiness_sims ars …`
  — a **different table, different columns, different predicate** from the one the corpus quotes. The string
  *"This DB signal is the only real 'has started' check"* is at **`:962`**, and
  `func (m *Manager) queryReadinessStarters` is at **`:964`** (its quoted SQL at `:969-974`).
  So the cited range does not contain either the function or the quotation.
- **Why I hesitated:** identical to B1 — both anchors are exact at `b948604f` (`:907-914` = the quoted
  comment; `:915` = the function). Same clone-advance cause.
- **Confidence:** medium

---

## Minors

- `corpus/services/ai-readiness.md:84` — `AIReadinessClient.tsx` *"(`:133-134`, `const { orgEnabled } = useAiReadinessEnabled(true)`)"*. At `next-web-app` `8297c684` that statement is at **`:135`**; `:133-134` are its preceding comment. Exact at `bb3313bc`. (Not covered by the file's own `8297c684` pin at `:308`, which scopes itself to *"the three bullets below"*.)
- `corpus/services/ai-readiness.md:335` — *"read by the tab filter at `:599`"*. At `8297c684` the `SHOW_SECONDARY_TABS` read is at **`:601`**; `:599` is `tab.key === 'how' ||` inside the same `.filter()`. Exact at `bb3313bc`.
- `corpus/services/ai-readiness.md:480` — `effectiveCycleId = …` cited at `:153-154`; at `8297c684` it is **`:155-156`** (`:153-154` are its comment).
- `corpus/services/ai-readiness.md:481` — *"gates the data GET on `cyclesQ.isFetched` (`:166-170`)"*; at `8297c684` the `useAIReadiness({…})` call is `:168-172` and the gate line is **`:171`**.
- `corpus/services/ai-readiness.md:270` — *"validated against `messenger/pkg/aireadinessemail` placeholders"*. The live validator import is **`app/internal/messenger/aireadinessemail`** (`internal/aireadiness/emailoverride/emailoverride.go:29`); `messenger/pkg/aireadinessemail/` survives only in the frozen legacy `messenger` repo the same doc says is folded in. (`app`'s own comment at `emailoverride.go:33` carries the same stale path, so the corpus inherited it.)
- `corpus/services/chronos.md:5` — *"removed … in mid-2026"*; `platform 045857c` is dated **2026-04-17**. Cosmetic.
- `corpus/services/ai-readiness.md:163` — *"a member carries a `stage` ∈ {1,2,3} (0 = none/done)"*. `classifyV2` (`scoring.go:119-138`) never emits 0 (it initialises `stage := 1`); stage 0 is a *seeder* concept (`aiReadinessStageFor`). The parenthetical's *"done"* half has no referent I could find. Ambiguous phrasing, no consequence for a reader following the seeding contract.

---

## Positively cleared (audited zeros)

**ENUMERATED** (predicate re-derived from source independently, cardinality compared before any arithmetic):

- `services/README.md:30` — *"`docker-compose.yml` declares **5** services (7 effective, with `common.yml`'s `postgresql` + `redis`), and `repos.yml` carries **4** entries"*. Read `0c91421d:docker-compose.yml` → `sentinel, backend, studio-desk, next-web-app, gotenberg` = 5; `common.yml` → `postgresql, redis`; `repos.yml` → `app, sentinel, next-web-app, studio-desk` = 4.
- `services/README.md:37` — the **seven** folded domains. `git ls-tree -d HEAD internal/{skiller,skillpath,jobsimulation,cms,storage,messenger,customeriosync}` → all 7 present @ `ad9f3c49`. **`roadrunner`: `git ls-files | grep -i roadrunner` → 0 AND `find stack-demo/app -iname '*roadrunner*'` → 0** (so the claim holds for untracked paths too); Judge0 wiring confirmed at `internal/jobsimwiring/wiring.go:123` (`jsrunner.NewRunnerManager(getenv("JUDGE0_API_KEY"), getenv("JUDGE0_BASE_URL"))`), under a comment at `:121-122` that names the removed roadrunner RPC edge.
- `services/README.md:44` — messenger's **21 live handlers** at `messenger/internal/flow/flow.go:72-104` @ `fa47850`. Counted the `pubsub.EventHandler(...)` members of the `AddSubscriber("backend", …)` block, **excluding the commented-out `OrgJobSimulationAssignmentPastDueHandler` at `:83`** (§5 rule 24: `grep -c` counts commented-out code) → exactly 21. `MESSENGER_RPC_ADDR` 0/15 trees with a live control.
- `services/README.md:22` / `cms.md:8,73` — `roadrunner/terraform/main.tf:19` = `service_desired_count = 1`; `cms/terraform/main.tf:39` = `= 0` in a **191**-line file (`wc -l` on the blob); `jobsimulation/terraform/main.tf` at **`6092c6d2`** contains **no** `service_desired_count` (`git grep` exit 1) and `:15-22` is the M810 comment block — the pinned ref makes the claim true, and the *checkout* `462343b0` still carries the module, which is why the pin matters.
- `services/README.md:52` / `cms.md:60-63,197` — the supergraph ladder. `graphql-wundergraph` `915da06` deletes **both** `schemas/cms.graphqls` (−762) **and** `schemas/jobsimulation.graphqls` (−860); `749dc86` deleted `skiller.graphqls` (5→4); `7c17e63` deleted `skillpath.graphqls` (4→3). `git ls-files` at HEAD shows `schemas/backend.graphqls` alone. **3 → 1** confirmed from the tree, not from the commit subject (which says 2→1).
- `cms.md:44-46` — the six re-created tables. `grep 'CREATE TABLE'` on `app/terraform/migrations/20260724132049_cms_data_model.sql` → exactly `similarities, studio_documents, studio_tasks, similarity_categories, similarity_features, similarity_skills`.
- `cms.md:55,196` — *"0 hits over all **44** tracked `.tf` files in the 13 `stack-demo` repos … and 0 again over the **59** `.tf` files a raw filesystem sweep finds"*. Re-derived the SET: per-repo `git ls-files '*.tf' | wc -l` summed to **44** (nested `studio` repos hold 0); `find stack-demo -name '*.tf' -type f | wc -l` = **59**. `backend.internal.anthropos` → 0 in both, with a `service_desired_count` positive control over the same file set. The literal's one occurrence is `app/knowledge/service-dependencies.md:52`, in the past tense, under *"There are no external callers of app's RPC mux left."* — read in full.
- `cms.md:95` — the studio provider set. `app/studio` @ `aeec036a` (its **own** ref — `git -C app grep … -- studio/` is a guaranteed 0, `app/.gitignore:79` = `studio/*`): `services/ai.py:705-709` is `{'openai','azure','anthropic'}`, `:1-2` imports only `openai`/`anthropic`; `git grep -i mistral aeec036a` → **22 hits / 3 files** (`requirements.txt:8`, `tools/pdf2md.py`, `tools/r3.py`); `pdf2md.py:24` `from mistralai import Mistral`, client `:96`, `model="mistral-ocr-latest"` `:127`. Go execs **exactly two** `.py`: `git grep '\.py' b948604f -- '*.go'` returns only `studioManager.go:119` (`studio/gen.py`) and `:1045` (`studio/postgen.py`) — the set, not just the sum.
- `cms.md:144-149` — `requirements.txt` is **9** packages, `mistralai` at `:8`, **no `python-docx`**; and `diff` proves `app/studio/requirements.txt` and `cms/studio/requirements.txt` byte-identical (the `:267` claim).
- `cms.md:279-285` — `gen.py:484-492` registers **exactly nine** `add_argument` calls, matching the nine named; `parse_argument` at `gen.py:18-28` calls `parse_known_args()` and merges leftovers into the dict, so `--template` is absorbed.
- `cms.md:113` — `cms/cmd/root.go:77` / `:78` are the `cmp.Or(os.Getenv("PORT"),"8080")` / `"8081"` defaults; at platform **`2adcf71`** `docker-compose.yml:154-155` publishes `8090:8090`/`8091:8091` and `:169`/`:173` set `PORT=8090`/`RPC_PORT=8091` — all four exact.
- `ai-labs.md:78,20,17-19` — `internal/credits/cost.go`: `creditCost` = `{build:5, refine:1, translate:1}` (the package doc at `:29` says *"5 credits per refine turn"* and is stale — the corpus follows the **map**, which is right); `MarginMarkup = 1.40` `:133`; `PricePerCreditUSD = 0.45` `:142`; `purchasePackages` `:200-204` = starter 50 / team 200 / scale 500; `DefaultSeedBalance int64 = 500` `:231`. `git describe --tags ad9f3c49` → **`v1.369.0-7-gad9f3c498`**, verbatim. `checkout.session.completed` → **0** in Go source (control: `customer.subscription` → 9 hits); `internal/labs/` imports `internal/credits` **nowhere**; `internal/web/backend/credits/handler.go:12` is the *"POST /credits/purchase was removed in Wave 13"* line and `Register` mounts **only** `/balance` + `/transactions`.
- `ai-labs.md:111-113` — the Stripe webhook switch. Enumerated the top-level `case` arms of the `switch event.Type` in `internal/web/backend/api/api.go:317-…`: **`customer.created`** and **`customer.subscription.{created,updated,deleted}`**, and nothing else.
- `ai-labs.md:53-70,105-110,134-135` — all five migrations exist by exact filename; `lab_body.go:12-13` states EN base + **6** overlays (`de/es/fr/it/nl/pt`); `LabsTier` = essential/professional/frontier (`labs_budget.go:26-28`); the five `CapReason` constants (`:247-251`); reconciler `Interval: 30 * time.Second` (`spend_reconciler.go:60`); labs-api default `:7070` + `LABS_API_PLATFORM_TOKEN` (`labsapi/client.go:6`, `main.go:855`); `cmd/labsdemo/main.go:115` `envOr("LABSDEMO_ADDR", ":8099")`; `/v1/labs` group + `labs:write` + `workspace.tar.gz` at `internal/web/backend/labs_admin.go:31,40`; `stripe-go/v74 v74.30.0` (`go.mod:64`); `LabSessionService` Create/Get/List/Cancel/ReportEvent all present in `internal/labs/session/labsession.go`; every named test file exists.
- `ai-readiness.md:150-155` — the archetype bands. `scoring.go:26` `archetypeHighBand = 75`, `:30` `archetypeLowCeil = 50`, `classifyArchetype` `:46-56` (Champion / Standby / `usage >= knowledge` → Explorer / default Hidden Talent), `bucketKnowledge` bands **None 0-24 / Low 25-50 / Medium 51-74 / High 75-100**. Axis math `computeKnowledgeAxis` `(s1+s2)/70×100` `:92-95`; `computeUsageAxis` `t3×100/30` `:100-105`; `classifyV2` `score = t1+t2+t3`, `stage ∈ {1,2,3}`.
- `ai-readiness.md:257-260,369,563` — **19 core @ 1.0 + 12 enabling @ 0.5 = 31**, denominator **25.0**. Counted the literals in `internal/aireadiness/defaults.go` (`grep -c '1\.0}'` = 19, `grep -c '0\.5}'` = 12) *and* read the whole list. `defaultReadinessSims` is exactly **3** rows (`{simulation,tech}`, `{simulation,business}`, `{interview,both}`) at `:75-79`.
- `ai-readiness.md:280` — *"CSV export is now 15 columns … dropped from the earlier 19"*. `AIReadinessCSVColumns` (`csv.go:17-33`) has **15** members and the row builder (`:93-109`) writes 15. The function's own doc comment at `csv.go:35` still says *"19-column"* — the corpus is right and the **code** is stale.
- `ai-readiness.md:174-188` — every one of the **14** named tables exists: 7 in `20260618160827.sql`, then `…_diagnose_narrative`, `…_text_translation`, `…recommendations`, `m408_…email_overrides`, `m400_…notifications` (×2), and `interview_aggregated_reports` in `20260722081626_jobsim_data_model.sql` with exactly the four columns quoted. *One active cycle per org* is the partial unique index `aireadinesscycle_organization_id … WHERE status = 'active'` (`20260618160827.sql:18`).
- `ai-readiness.md:206,209,637-641` — the **five** live KPI ids `avg_adoption/avg_transformation/avg_originality/avg_depth/avg_ownership` are `usageDimSpecs` (`how_we_measure.go:1160-1180`); `usageDimensionsFromReports` (`:1186-1220`) `continue`s on a non-numeric unmarshal and on a missing id (so "omits"); `holdsBackFromInsights` (`:1008`) filters `strings.Contains(strings.ToLower(in.Category), "risk")` at `:1016`.
- `ai-readiness.md:218-224` — the seeder side, in the **pinned** rext clone `09d06070`: `stack-seeding/seeders/ai_readiness_interview_report.go:185-189` emits exactly those five ids, and `:149` reads *"All five are always emitted here."*
- `ai-readiness.md:311-322` — `urls.ts:51` = `AI_READINESS_URL` @ `8297c684`; `:52` = `TALK_TO_DATA_URL` there and `ORGANIZATION_FEEDBACK_URL` @ `bb3313bc`. I re-derived the **set** the doc asserts: over **all 37 commits** touching that file the constant sits at line **41, 50 or 51 only** (32 commits predate it) — never `:52`. Repo-wide `git grep AI_READINESS_URL 8297c684` (minus `node_modules`) = exactly `urls.ts:51`, `useNavbarSections.tsx:4`, `:403`, plus the KB page `knowledge/ai-readiness/frontend-architecture.md:15` — the doc's enumeration, complete. `useNavbarSections.tsx:401-408` is `aiReadinessMenuItem`, `:569` is `showAIReadiness ? aiReadinessMenuItem : null`.
- `ai-readiness.md:290-291,299` — `next-web-app` `dae0fb2f7` (2026-07-13) deletes `AIReadinessContainer.tsx` (103) + `AIReadinessIntro.tsx` (220) + `AIReadinessView.tsx` (330) = **653** lines, exactly the figure quoted; and `git ls-files | grep 'enterprise/workforce/ai-readiness'` → **0**, so the route really is gone.
- `ai-readiness.md:605-612` — `HowWeMeasureTab.tsx` @ `8297c684` is **1,989** lines; `:1879` is the `{/* ===== C · Handled for you this cycle ===== */}` comment; `:1903` the label; `:1915/:1921/:1927` the three cells (`skillsMapped` / `handsOnMinutes` / `interviewMinutes`); `grep -c interviewQuestions` over the file → **0**. `apps/web/src/hooks/useAIReadiness.ts:326` is `interviewQuestions: number;` inside `AIReadinessCycleTotals` (`:323-330`). Every number in that parenthetical verifies.
- `ai-readiness.md:82-86` — `AIReadinessClient.tsx` @ `8297c684`: `grep -ci posthog` → **0**, `grep -c flag_ai_readiness` → **0**.
- `ai-readiness.md:598-600` — the `interactions` enum. `ActionTypeValidator` (`internal/data/ent/interaction/interaction.go:92-99`) accepts `"email", "call"` and nothing else; the DB CHECK `interaction_source_target_must_be_different_check CHECK (source_id <> target_id)` is in `20260722081626_jobsim_data_model.sql:94`. Turn count `6-11` = `aiReadinessMinTurns = 6` / `aiReadinessMaxTurns = 11` (rext `ai_readiness_funnel.go:584-585`).
- `ai-readiness.md:572-576` — the held-skill bands. `aiReadinessStartedHeroSkills = 9` (`ai_readiness_funnel.go:529`), `aiReadinessCoreSkills = 19` (`ai_readiness_config.go:29`), pool = 31 → stage 3 `19…31`, stage 2 `3…19`, stage 1 `1…3` (`aiReadinessHeldCount` `:506-514`). The seeder's **own** doc comment at `:448-452` still prints the pre-M250 bands (3 / 5 / 3…5 / 1…3) — the corpus is right and the code comment is stale.
- `ai-readiness.md:397-407` — `aiReadinessStageFor` (`ai_readiness_funnel.go:205-217`): `IsAIReadinessNotStarted() → 0`, `IsManager() → 0`, `TrajectoryStruggling → 1`, `default → 3`, with the not-started case checked **first** — exactly the ordering + mapping claimed. `Persona.AIReadiness` field at `blueprint.go:200`, constant `AIReadinessNotStarted = "not_started"` at `:316`; `seat_append_test.go` and `ai_readiness_funnel_test.go:230` (`TestAIReadinessFunnelSeeder_DayZeroHeroGetsNoSignals`) both exist in the pinned clone.
- `ai-readiness.md:43-45` — the demopatch re-anchor. `demo-stack/patches/app-aireadiness-snapshot-loadmembers/app-aireadiness-snapshot-loadmembers.yaml:42` is `path: internal/aireadiness/readiness.go` and `:33` opens *"v2.7 M254 RE-POINT:"*; `demo-stack/tests/test_aireadiness_snapshot_loadmembers_m254.py` exists. All three in the **pinned** clone.
- `ai-readiness.md:462-467` — the two frozen routes. `readiness.go:289` is `GetAIReadinessWithOptions`; `:290-297` the cycle-scoped closed branch; `:307-312` the no-active-cycle → latest-closed branch; `:314` the `buildLiveResponse` fall-through. Exact at **both** app refs.
- `ai-readiness.md:594-600` — `computeCycleTotals` declared at `how_we_measure.go:260` inside the cited `:253-261`; the interview query at `:283-289` with `FROM public.interactions i / JOIN public.job_simulation_sessions s` at exactly `:285-286`.
- `ai-readiness.md:16,20,27-35,356` — `4c28365f` ("Refactor AI Readiness domain…", 2026-07-22) is contained in **`v1.351.1`** (`git describe --contains` → `v1.351.1~1^2`); every renamed file exists under `internal/aireadiness/` (incl. `testdb_test.go`, `emailoverride/`, `emailpreview/`, `notifications/`) and `internal/workforce/members.go` **stayed**.
- `ai-readiness.md:241-250,264,272-276,282-283` — GraphQL `aiReadinessEnabled` / `aiReadinessUserPlanProgress` / `aiReadinessSkills` / `completeAiReadinessSkillMapping` all in `ai_readiness.graphqls`; all nine REST paths in `api.yaml` (`/ai-readiness`, `/narrative`, `/steps-completion`, `/cycles`, `/setup`, `/cycles/{cycleID}`, `/export.csv`, `/compare`, `/cycles/{cycleID}/close`) plus the M407/M408 email routes at `internal/web/backend/emailpreview/handler.go:70-73`; `CompareCycles` returns **6** sections and hard-errors unless **both** cycles are `closed` (`compare.go:163,170`); `NewManager(logger, db, aiClient, entClient, pub, academyEmbeddings, directory)` matches the wiring list; narrative model is `openai.GPT5Mini` (`narrative.go:85`).
- `skiller.md:13-27,53-58` — every named Ent schema file exists under `internal/data/ent/schema/`; `internal/rpc/skillerrpc/skiller.go` implements all five named methods (plus eight more, which the doc does not claim to enumerate — it says *"externally-reached"*); `graph/schemas/skiller_taxonomy.graphqls` exists; `skiller_rpc_addr` variables are declared (no default) in cms/jobsim/messenger terraform, and the `http://backend.internal.anthropos:8081` value is corroborated by `app/knowledge/service-dependencies.md:52`.
- `chronos.md:6-9` — `platform 045857c` *"remove chronos service from orchestration"* (2026-04-17); `jobsimulation 09631fb2` *"remove Chronos references … Asynq integration for session timeout management"* (2026-04-17) on branch `feat/remove-chronos-and-realtime`, merged by `500b9761` (**PR #395**); the Asynq pools are `app/internal/jobsimwiring/worker.go` (`asynq` imported `:24`, `StartWorkers` `:27`, standard pool `:34-40`).
- `chronos.md:186` — *"`docker compose up -d chronos` will fail with 'no such service'"*. This is a **service-name** selection, not a `--profile` token, so §5 rule 30's silent-no-op trap does not apply: `chronos` is absent from both `docker-compose.yml` and `common.yml`, so the command really does error. Correctly distinguished from `cms.md:252-253`, which states the *opposite* behaviour for a retired **profile** token and is likewise right.
- `services/README.md:45` — customerio-sync pushes to **Brevo**, Customer.io name a fossil: `internal/customeriosync/doc.go:1-7` says exactly that, including the `public.customer_io_sync_table` fossil view.
- `services/README.md:53,56` — Next.js **16** on both: `apps/web/package.json:46` and `apps/hiring/package.json:45` `"next": "~16.2.12"`; `ant-academy/code/package.json:64` `"next": "^16.2.6"`.
- `cms.md:110,26-30` / `services/README.md:26-28` — `platform d11a403` (2026-08-03) removes `- name: cms`, `- name: jobsimulation`, `- name: roadrunner` from `repos.yml` and their compose blocks; `838d907` (2026-08-05, merged `0c91421`, subject *"drop the storage, messenger and customerio-sync containers"*) removes `- name: storage`, `- name: messenger`. Compose at HEAD sets **zero** `*_RPC_ADDR` and one service address, `AUTHORIZATION_ADDRESS=http://sentinel:8087` (`docker-compose.yml:48`).
- `cms.md:11-19,78-84` — `cms 6efa1d5` (merged `f38c0c4`, 2026-08-04) deletes `.github/workflows/build-production.yml` (18 lines) with the body the doc quotes verbatim, while `cms/terraform/main.tf:39` still declares `= 0` — the *"two measured facts point opposite ways"* framing is accurate on both halves.
- `cms.md:65-68,195` — the Directus webhook: `app/main.go:1199` builds `cmswebhooks.Handler(os.Getenv("DIRECTUS_WEBHOOK_SECRET"), …)`, mounted at `internal/web/backend/backend.go:324` on `/api/webhook/directus` and Clerk-skipped at `:137-138`; the standalone was `cms/cmd/root.go:242` `Handle("/webhooks/", … webhooks.Handler(dirClient, pub, aivideoManager))` — no secret argument, i.e. unauthenticated.
- `cms.md:106` — `app/cms_reader_switch.go` exists; the pinned rext clone has `DIRECTUS_DATA_CONSUMERS = ("cms", "backend")` (`stack-core/gen_override.py:58`), i.e. it does name both.
- `cms.md:101` — `app/internal/skillpath/session.go:205-207` is exactly the `// cms-in-app deseam: cms is in-process` comment + `u.cms.GetSkillPathDomain(...)`.
- `cms.md:56-58` — `app/main.go:1205-1211` @ **`b948604`** is verbatim the *"Additive + DORMANT … until the M809 re-point"* comment (a pinned claim, graded at its pin).

**RE-DERIVED only** (arithmetic the document displays; the weakest clearance — flagged as such):

- `ai-readiness.md:361` — `156/199 = 78.39 %`.
- `ai-readiness.md:566,574` — `round(1.0/25.0×30) = 1`; `round(9.0/25.0×30) = round(10.8) = 11`.
- `ai-readiness.md:581` — `held/25×100 = held×4`, integral for half-step weights.
- `ai-readiness.md:647` — *"5 Playthroughs"* reconciles with `playthroughs.md:106` (4 in the `ai-readiness` product, table `:415-418`) **plus** `pt-onboarding-aireadiness-guided`, which `playthroughs.md:128` books under the onboarding product. No contradiction, but I did not independently enumerate the manifests.

---

## What I could not settle

- **`services/README.md:41` (storage row): *"In prod the ECS service is **deleted**, not scaled to zero — the module survives only to keep the buckets/CDN under `prevent_destroy`."*** This is **TRUE at `storage` `origin/main` `9f8cb532`** (`terraform/main.tf` is 18 lines, the `module "storage"` block replaced by the comment the corpus paraphrases) and **FALSE at the frozen checkout `4ce8ece5`** (100 lines, `module "storage"` present with `service_desired_count = 1` at `:19`). The claim names no ref; the addendum calls both legitimate. I did **not** book it, because origin/main is where the platform actually is and the corpus's sibling claim (*"18 lines"*, in `CLAUDE.md`) matches it — but the grading rule as written points the other way, so I record it here rather than silently choosing.
- **`skiller.md:19`** — *"`skiller_rpc_addr = http://backend.internal.anthropos:8081` in production terraform"*. The **variable** is declared in `cms`, `jobsimulation` and `messenger` terraform with no default; the **value** is assigned in `infrastructure/…/services.tf`, a repo that has never been in any clone set (the same limit `cms.md:18,196` records). Corroborated only indirectly by `app/knowledge/service-dependencies.md:52`. Reading `infrastructure` would settle it.
- **`chronos.md` §Architecture/Data-model/Triggerer (`:22-63`, `:154-160`, `:194-210`)** — the `chronos` repo is not in `stack-demo/`, so the schema DDL, the `LIMIT 10` + `FOR UPDATE SKIP LOCKED` batch behaviour, the 3-second run timeout, the Sentry heartbeat, the env-var table and the CLI-flag table are all unverifiable from here. Nothing contradicts them; a `chronos` clone would settle them.
- **`chronos.md:3` / `README.md:79`** — *"The GitHub repo is NOT archived (last push 2026-04-23)"*, and `README.md:52` *"repo ARCHIVED"* for `graphql-wundergraph`. Both are GitHub-API facts; I made no network call. Consistent with `platform-migration-status.md:96,99`.
- **`ai-labs.md:4-6`** — *"`anthropos-work/AI-Labs` is the live Go control plane"*. That repo is in no clone set. Everything on the `app` side of the seam (the `labsapi` client, the bearer token, the `ReportEvent` callback) verifies; the existence and language of the control plane do not.
- **`ai-readiness.md:200`** — *"`interview_extraction_results` (165 rows, written by the `SuccessionSeeder`)"* and the several *"measured on `billion`/demo-1"* latency figures (2.09 s, 24 ms, 19 ms, 180 s). These are live-stack measurements; only a running stack settles them. The doc's own instruction (*"Re-measure before relying on either number"*) is the right posture.
- **`cms.md:33-35,82-84`** — whether `infrastructure/terraform/production/services.tf` still declares `module.cms_euwest1`. The doc explicitly declines to assert either way and says why; I agree it is unmeasurable from this clone set, and I confirmed the two in-repo facts it cites on both sides.
