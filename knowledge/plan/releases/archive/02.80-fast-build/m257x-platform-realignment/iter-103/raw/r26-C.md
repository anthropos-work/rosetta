# Seat C — reading #26

**Instrument delivered:** `briefing-r26-AS-DELIVERED.md`
sha256 `4178b00f0d8e98eabddb895969d6e4c58799a5b292d967f8a2cfca6772a18d3d` (243 lines).

**Ground truth re-verified at this reading's open** (no `git fetch`/`pull`/state change was run at any
point). Every ref matched the briefing's ADDENDUM exactly:

| tree | ref read |
|---|---|
| `stack-demo/app` | `ad9f3c49` (== `origin/main`); pinned reads also taken at `2035f9a4`, `9d00a313`, `b948604`, `7177374` |
| `stack-demo/platform` | `0c91421d`; pinned reads at `0dab54d`, `2adcf71`, `838d907`, `d11a403`, `ef32d4c` |
| `stack-demo/sentinel` | `f2c46190` |
| `stack-demo/storage` | checkout `4ce8ece5`; `origin/main` `9f8cb532` (the cell's own pin) |
| `stack-demo/messenger` | checkout `fa47850d`; `origin/main` `e9421c68` |
| `stack-demo/cms` | checkout `ca50c817`; `origin/main` `f38c0c4a`; commit `6efa1d5` |
| `stack-demo/jobsimulation` | checkout `462343b0`; `origin/main` `82cb66ec`; commit `6092c6d2` |
| `stack-demo/graphql-wundergraph` | `60c229f3` (+ `915da06`) |
| `stack-demo/roadrunner` | `87d8d443` (+ blame to `84a4b4f`, `e45eb61`) |
| `stack-demo/next-web-app` / `studio-desk` / `ant-academy` | `8297c684` / `41ee3575` / `22df69dd` |
| `stack-demo/rosetta-extensions` **(the pinned per-stack consumption clone — the tree I used to adjudicate every tooling claim, per addendum rule 45)** | `09d06070` |
| `.agentspace/rosetta-extensions` (authoring copy — used only for the layout cross-check and the one guard run the doc's own recipe names) | `944fc4a2` |
| `stack-demo/app/studio`, `stack-demo/cms/studio` (nested, own checkouts) | `aeec036a` |

**Search-instrument discipline.** Every tree-wide absence claim below was measured with all three
instruments named in the addendum: a per-tree `git grep` at each tree's **own** HEAD across all **15**
git trees under `stack-demo` (the 13 top-level repos **plus** the two nested `studio` checkouts,
enumerated with `find stack-demo -name .git -maxdepth 4`), a raw filesystem `grep -ar`, and a positive
control in the same pass. Two engine rejections were caught and re-run rather than read as zeros: this
shell's `grep` is `ugrep` and rejected `--include=*.go` ("no matches found") and `timeout` does not
exist on this box — both would have printed as clean absences.

## Positive controls

| file | `wc -l` | read in full? |
|---|---|---|
| `corpus/architecture/alignment_testing.md` | 542 | yes |
| `corpus/architecture/platform-migration-status.md` | 330 | yes |
| `corpus/services/jobsimulation.md` | 309 | yes |
| `corpus/services/customerio-sync.md` | 165 | yes |
| `corpus/services/askengine.md` | 147 | yes |
| `corpus/services/intelligence.md` | 18 | yes |
| **total** | **1511** | |

I also read `corpus/ops/platform-alignment.md` §5 in full (lines 249–1557) before starting, as the
instrument requires.

---

## Blockers

### B1 — a uniqueness claim ("one occurrence anywhere in the clone set") refuted by 5 more tracked instances

- **Anchor:** `corpus/services/jobsimulation.md:50` —
  > "**No `.tf` file in any clone names `http://backend.internal.anthropos:8081`** — 0 hits over all 44
  > tracked `.tf` files in the 13 `stack-demo` repos at each clone's own HEAD (measured 2026-08-06), and
  > 0 again over the 59 `.tf` files a raw filesystem sweep of that workspace finds. **The literal's one
  > occurrence anywhere in the clone set is a markdown KB page** — `app/knowledge/service-dependencies.md:52`
  > @ `app` `ad9f3c49` …"

- **Why it is false.** The first half is **correct and I re-derived it independently** (44 tracked `.tf`
  files, 59 raw, 0 hits — see the cleared list). The **uniqueness sentence is not**. Measured with the
  exact literal, per tree, at each tree's own ref:

  | tree | ref | tracked hits on `http://backend.internal.anthropos:8081` |
  |---|---|---|
  | `stack-demo/app` | `ad9f3c49` | 1 — `knowledge/service-dependencies.md:52` |
  | **`stack-demo/rosetta-extensions`** | **`09d06070`** | **5** |
  | all 13 other trees (incl. both nested `studio` repos) | own HEADs | 0 |

  The five are, at rext `09d06070`:
  `stack-core/tests/test_platform_predicate_guard.py:435`,
  `stack-core/tests/fixtures/repair_leak/pre/corpus/services/cms.md:32` and `:157`,
  `stack-core/tests/fixtures/repair_leak/post/corpus/services/cms.md:32` and `:157`.
  Total **6**, not one. (A raw filesystem `grep -ar` returns the same 6.)

- **Why "clone set" cannot be read as excluding `rosetta-extensions`.** The sentence's *own* denominator
  settles it: "all 44 tracked `.tf` files in the 13 `stack-demo` repos". There are exactly 13 top-level
  clones under `stack-demo`, and `rosetta-extensions` is one of them. Its per-tree tracked `.tf` count is
  **2** (`git -C stack-demo/rosetta-extensions ls-files '*.tf' | wc -l`), and the eleven others sum to
  **42**. The number 44 is only reachable *with* `rosetta-extensions` counted. So the document counts
  `rosetta-extensions` as a member of "the clone set" in the first clause and excludes it in the second.

- **Confidence:** high. (I hesitated only over whether "clone set" might colloquially mean "the platform
  repos"; the 44/42 arithmetic above removes the ambiguity, and the sentence is a `one`/`anywhere`
  uniqueness claim, which is the strongest form available.)

### B2 — `app/main.go:670` is an unpinned anchor that lands on an unrelated `os.Getenv` at the ref it must be graded at

- **Anchor:** `corpus/services/jobsimulation.md:208` (last sentence of the "signature did NOT survive the
  fold" blockquote) —
  > "The jobsim wiring is fatal by design at `app/main.go:670` (`:614` @ `b948604`)."

- **Why it is false.** The parenthetical pins only the **`:614`** alternative, to `b948604`. The
  **`:670`** claim names no ref of its own, so it is graded at the checkout (`ad9f3c49`, which is also
  `origin/main`). Measured there:

  ```
  $ grep -n "jobsim-in-app: engine wiring failed" stack-demo/app/main.go
  723:		log.Fatalf("jobsim-in-app: engine wiring failed (is jobsim env provisioned?): %v", err)

  $ sed -n '670p' stack-demo/app/main.go
  	if v := os.Getenv("SKILLER_AZURE_OPENAI_KEY"); v != "" {
  ```

  `:670` names a **Skiller Azure key lookup**, not the jobsim wiring fatal. The `:670` figure is correct
  **only at `9d00a313`** (v1.367.0), which I confirmed
  (`git show 9d00a313:main.go | grep -n "engine wiring failed"` → `670`). `9d00a313` is named in a
  *different, earlier* sentence of the same blockquote; §5 rule 33 fixes a pin's scope at "the claim's own
  block — … a wrapped sentence in prose", so a neighbouring sentence's ref does not date this one. The
  `:614 @ b948604` half is correct and I verified it.

  Two aggravating facts, both inside the same blockquote. (a) The immediately preceding sentence
  *explicitly warns* "do not read the pinned line offset as current" about the **other** anchor
  (`main.go:216`), which reads as an assurance that the anchors here have been currency-checked —
  and that one has: `:216 @ 7177374` ✓, `:212 @ b948604` ✓, `:216 @ 9d00a313` ✓, `:229 @ ad9f3c49` ✓, all
  four verified as `func main() {`. (b) The **same claim recurs 30 lines later at `:237-238`** and *is*
  pinned there — "dies at `log.Fatalf` in `app/main.go:670` … (`app` `9d00a313` v1.367.0; the fatal is
  `:614` @ `b948604`)" — which is correct. One twin pinned, one twin left bare, is the half-repair
  signature §5 rules 19/40 describe.

- **Confidence:** medium. (Hesitation, stated as the instrument requires: the number *is* right at a ref
  named elsewhere in the same blockquote, and a charitable reader could carry it over. I book it because
  the instrument names "wrong line anchors that point at the wrong construct" as BLOCKER, because
  `ad9f3c49` is both the checkout *and* origin HEAD — the two refs a reader is most likely to open — and
  because the construct at `:670` there is not merely displaced, it is about an unrelated subsystem.)

---

## Minors

- `corpus/architecture/alignment_testing.md:3` — "**Last updated:** 2026-06-06" contradicts the file's own
  body, which narrates M218, M219, M23 and a "Corrected M257x iter-85" retraction. `git log` on the file:
  `00be1ac` 2026-08-06, `ae5c1db` 2026-08-05, `b4b6db8` 2026-08-05. The header under-claims freshness
  (misleading toward distrust of correct content), so non-material.
- `corpus/architecture/alignment_testing.md:532-540` — the Layout block lists `internal/dna`,
  `internal/outcome`, `internal/compare`, `internal/report` and omits **`internal/canon`**, which exists in
  both rext trees (`ls alignment/internal/` → `canon compare dna outcome report`). Omitted list member.
- `corpus/architecture/alignment_testing.md:200` — "its rationale is the comment block at `:58-68`". At
  rext `09d06070`, `clerkenstein/alignment/scripts/gate.sh` has the comment block at **`:58-67`**; `:68` is
  the `echo "==> capability-coverage check ($dna)"` line. The two anchors that carry the claim are both
  exact: `:61` is inside the comment ✓ and `:69` is the `--if-declared` call ✓.
- `corpus/services/askengine.md:104` — "the Asynq pools and the Redis subscribers (`:1438` onward)". At
  `ad9f3c49`, `:1438` is a **comment line** inside the messenger-subscriber rationale, and the *first*
  Redis subscriber construction is `pubsub.NewSubscriberServer` at **`:1376`** (messenger's second server
  is at `:1450`). The claim it supports — that nothing after the `return` at `:467-471` runs — is true of
  all of them, and I verified the sibling anchors `:1295` (Connect-RPC `http.NewServeMux`) and `:1361`
  (`meta.NewServer`) as exact.
- `corpus/services/jobsimulation.md:225-229` — the AWS-bind anchors (`:88-89` comment, `:90` `volumes:`,
  `:91` the bind) are **exactly right at their stated pin `0dab54d`**, which I verified, so this is not a
  finding under rule 33. Recorded because the surrounding sentence is present-tense ("The bind survived
  the fold and moved onto `backend`") and at platform HEAD `0c91421` the same three have moved to
  `:97-98` / `:99` / `:100` — the bind itself is still there, unchanged.
- `corpus/services/customerio-sync.md:67-81` — the block labelled "**HISTORICAL**" reproduces the deleted
  compose service but omits two keys the real `838d907` diff shows: `env_file: [- .env]` and
  `networks: [- app-network]`. Everything it *does* show is byte-accurate, including
  `profiles: [customerio-sync, all]`, which I verified against `git show 838d907 -- docker-compose.yml`.
- `corpus/services/intelligence.md:5` — "removed from `platform/docker-compose.yml` and
  `platform/repos.yml` in **mid-2026**"; `fdfa189` is dated **2026-04-17**
  (`chore: remove intelligence service from local dev orchestration`), which
  `platform-migration-status.md:100` states precisely. Cosmetic imprecision, and the two files do not
  actually disagree about a fact.

---

## Positively cleared (audited zeros)

Labelled per the instrument. **ENUMERATED** = I derived the predicate's set from source independently and
compared cardinalities. **RE-DERIVED** = I only re-computed arithmetic the document itself displays.

### `platform-migration-status.md`

- **ENUMERATED** — "**14** names ever in `repos.yml`". Derived with
  `git log -p --follow -- repos.yml | grep -E '^[+-]\s*- name:' | sort -u` → exactly the 14 the document
  lists, in the same set: app · chronos · cms · graphql-wundergraph · intelligence · jobsimulation ·
  messenger · next-web-app · roadrunner · sentinel · skiller · skillpath · storage · studio-desk. I then
  checked each of the 14 against the row table: **all 14 have rows**.
- **ENUMERATED** — "**25** service names ever in `docker-compose.yml`; a section-blind pass returns **26**
  and the one extra token is `app-network`". Same command on that file returns 26 two-space keys; the only
  non-service among them is `app-network`. I verified `docker-compose.yml:185-186` @ `0c91421` is
  `networks:` / `app-network:`. I then mapped **all 25** service names onto rows: every one is covered, and
  **exactly three** are filed under a repo name rather than the compose key (`backend`→`app`;
  `graphql` and `wundergraph`→`graphql-wundergraph`), as the document says.
- **ENUMERATED** — the §4 fence numbers. I *ran* the guard the document's own recipe names
  (`python3 .agentspace/rosetta-extensions/stack-core/platform_alignment_guard.py
  corpus/architecture/platform-migration-status.md stack-demo/platform/repos.yml`). Output, byte-for-byte
  as documented at `:222-223`: `assertion F resolved 96 citation(s) — 25 subject-checked, 70 range-only,
  1 outside any service block; 0 unresolvable; 0 read from the WORKTREE (no ref resolved)`, then
  `OK — … agree in both directions`, **exit 0**.
- **ENUMERATED** — the `storage` row's `STORAGE_RPC_ADDR` census, which is the most load-bearing
  arithmetic in the file and is right in every particular. At `2035f9a`: **3** Go hits, **all comments**
  (`main.go:504`, `internal/jobsimwiring/wiring.go:101`, `internal/storagens/callsites_test.go:189`) — I
  opened all three. At `b948604`: **15** hits, of which **7 are env lookups** — `main.go` ×3 (`:446`,
  `:524`, `:992`), `wiring.go` ×1 (`:115`), and one in **each** of the three `cmd/` tools
  (`academy-asset-upload:129`, `academyImport:231`, `import:50`) — and the residual **8** decomposes
  exactly as claimed: 2 doc comments (`:28`, `:35`), 2 error strings (`:133`, `:235`), **3** `t.Setenv`
  (`:41`, `:52`, `:64`) and **1** `t.Fatal` message (`:47`). The sub-claim that "six of the seven are
  spelled `os.Getenv`; the seventh uses a lowercase `getenv` helper" is also exact — `wiring.go:115` is
  `getenv("STORAGE_RPC_ADDR")`.
- **ENUMERATED** — `storage/terraform/main.tf` is **18 lines** at the cell's own pin `9f8cb532`, declares
  no `module` at all, and `:9-11`, `:13-16`, `:18` are precisely the three passages quoted (I read the
  whole file at both refs; the checkout `4ce8ece5` is the pre-fold 100-line version, which is why the
  cell pins). `custody` occurs **0** times at `9f8cb532` and at the checkout; there are **0** `moved`
  blocks; and `storage/terraform/storage.tf:22-25` is exactly the M903-superseded passage.
  `d3e6d32` is dated 2026-08-05 with the quoted subject.
- **ENUMERATED** — `messenger/terraform/main.tf` at `origin/main` `e9421c68`: **121 lines**, `:29` is
  `service_desired_count = 0`, `:19-25` is the consumer-group/untargeted-apply rationale, `:27-28` is the
  rollback-path comment. And `messenger/cmd/root.go:120-140` does read **all four** `*_RPC_ADDR` variables
  (`CMS_RPC_ADDR` :120, `BACKEND_USERS_RPC_ADDR` :125 and :130, `SKILLER_RPC_ADDR` :135,
  `JOBSIMULATION_RPC_ADDR` :140) — the range is exact.
- **ENUMERATED** — every `app/main.go` wiring anchor in the `app` row, read at its stated pin `2035f9a`:
  `:395` `customeriosync.New` · `:524`/`:525` `internalstorage.NewManager`/`NewPublicManager` · `:547`
  resource manager · `:690` `skiller.NewSkillerManager` · `:721` `jobsimwiring.Wire` · `:751`
  `skillpath.NewSessionManager` · `:1102` `cmsStorage` · `:1153` `appcms.Wire` · `:1471`
  `msgadapters.Wire`. Also `:15`/`:62`/`:63` (the three messenger imports), `:285`/`:286` (the two
  switches), `:394`, `:504`, `:516`/`:517`, `:518-523` (the empty-bucket fatal), `:1416`, `:1445`,
  `:1450`, `:1473`, and `app/internal/storage/service.go:22`/`:24` (the env-var **names**, as the row
  correctly insists). **Nine of nine, plus fifteen supporting anchors — every one on the right construct.**
- **ENUMERATED** — every compose anchor in the table at `0c91421`: `:1-2` include · `:5` sentinel ·
  `:18` `search_path=sentinel` · `:28` backend · `:73-75` · `:82`/`:83` · `:102-103` · `:112` studio-desk ·
  `:119`/`:135` VITE · `:141` `profiles: [studio-desk, all]` · `:143` next-web-app · `:151`/`:160`
  NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT · `:168` `profiles: [frontend, all]` · `:170-171` gotenberg ·
  `:183` `profiles: [core, backend, all]` · `:185-186` networks. Plus `common.yml:2`, `:22`
  (`start_period: 120s`), `:24`.
- **ENUMERATED** — the per-repo terraform scalars: `app/terraform/main.tf:181` `= 1`;
  `sentinel/terraform/main.tf:19` `= 1`; `cms/terraform/main.tf:39` `= 0` (191-line module, identical at
  the checkout and at `f38c0c4a`); `graphql-wundergraph/terraform/main.tf:20` `= 1`;
  `roadrunner/terraform/main.tf:19` `= 1` with `git blame -L 19,19` naming **`84a4b4f`** (2025-12-15) and
  `e45eb61` (2026-05-27) touching **one line** (`--stat`: 1 insertion, 1 deletion), exactly as the row's
  correction says. `app/terraform/main.tf:415-416`/`:419-420` are the `MESSENGER_ENABLED` /
  `CUSTOMERIO_SYNC_ENABLED` `"true"` pairs.
- **ENUMERATED** — the graphql-wundergraph row: `supergraph-config-prod.yaml` lists **`backend` alone**,
  `schemas/` holds **`backend.graphqls` alone**, `subgraphs.conf` is `BACKEND=v1.360.0`, and `915da06`
  (2026-07-29) is *"fold cms subgraph into backend (supergraph 2→1) — cms-in-app v8.0 (#293)"*.
- **ENUMERATED** — the `cms` row's "two measured facts pointing opposite ways": `cms/terraform/main.tf:39`
  is still `service_desired_count = 0` at `f38c0c4a`, **and** `6efa1d5` (2026-08-04, merged `f38c0c4`
  = PR #351) deletes `.github/workflows/build-production.yml` (`--stat`: 18 deletions) under the quoted
  subject. Both true; the row's "report both, assert neither" is the correct verdict.
- **ENUMERATED** — every platform commit sha/date/subject the file cites: `fdfa189` 2026-04-17 ·
  `045857c` 2026-04-17 · `21429b7` 2026-07-07 · `a4db680` 2026-07-21 (M507) · `d11a403` 2026-08-03 ·
  `236771f` 2026-07-29 · `2adcf71` 2026-07-31 (PR #23) · `0dab54d` 2026-08-03 · `b56d731`+`360efd4`
  2026-07-31 · `838d907` 2026-08-05 / `0c91421` PR #26 · `a2a3ee6` 2026-02-27 (its diff **does** remove
  `directus:` from compose) · `cb6ebf5` 2023-04-30 · `8770fe6` 2023-05-04 ("four days after the first
  commit" ✓) · `6060315` 2026-08-03 (postgres 120 s start_period).
- **ENUMERATED** — `repos.yml` @ `0c91421` is **28 lines / 4 entries** (app · sentinel · next-web-app ·
  studio-desk), and the line ranges `:2-10`, `:12-13`, `:14-17`, `:18-20`, `:23-25`, `:26-28` are all
  exact. At `0dab54d` it is **6** entries including storage and messenger, as §5 row 1 says.
- **RE-DERIVED only** (flagged as the weak form): the §4 coverage split 25 + 70 + 1 = 96. The guard run
  above upgrades this to ENUMERATED.
- **NOT settled:** §3's "**93 repositories**" and "**46** named by no corpus document" require the GitHub
  org API. The "**4** in `repos.yml`" half is ENUMERATED and correct.

### `jobsimulation.md`

- **ENUMERATED** — `jobsimulation/terraform/main.tf` at `82cb66ec` (post-`6092c6d2`): `service_desired_count`
  occurs **0** times anywhere in the file, `:15-22` is exactly the M810-decommission comment enumerating
  the eight destroyed resources, `:24-40` is "What this module still owns", and `:38-40` is the
  atlas-tracker/schema-drop paragraph. `6092c6d2` is *"chore(terraform)!: remove the jobsimulation ECS
  service and ECR repository (M810)"*, 2026-08-04.
- **ENUMERATED** — the **23** tables: `grep -c '^CREATE TABLE'
  app/terraform/migrations/20260722081626_jobsim_data_model.sql` → **23**. The rename:
  `20260722104506.sql:2` is `CREATE TABLE "job_simulation_sessions"` and `:79` is `DROP TABLE "sessions";`
  — both exact.
- **ENUMERATED** — the mirror-drop migration, which is the passage most likely to have been
  quoted-forward. `20260729133514.sql` is **64 lines**; `:15-23` are the two id-remap `UPDATE`s, `:36-44`
  the two orphan-NULL `UPDATE`s, `:58-62` the "5. Drop the mirrors." comment + first `DROP`, `:62-63` both
  `DROP TABLE`s. I grepped for real `INSERT` statements: **zero** (a naive `grep -ci insert` returns 1 and
  is a false positive on the trigger-function name `on_insert_local_…`, which is the §5-rule-24 trap in
  miniature). `internal/organization/intelligence.go:1700` is
  `query := m.ent.JobSimulationSession.Query().` — exact.
- **ENUMERATED** — the compose/profile facts at `0c91421`: `docker-compose.yml` declares **5** services
  (sentinel, backend, studio-desk, next-web-app, gotenberg), `common.yml` adds postgresql + redis = **7**
  effective. No `jobsimulation` service, no `jobsimulation` profile, no `repos.yml` entry.
- **ENUMERATED** — the historical `2adcf71` claims, every one exact: `docker-compose.yml:83`
  `jobsimulation:` · `:93`/`:94` `"8400:8400"`/`"8401:8401"` · `:113` `PORT=8400` · `:119`
  `RPC_PORT=8401` · `:140` `profiles: [graphql, jobsimulation, all]` · `Makefile:10` `PROFILE ?= graphql`
  · `repos.yml:17-19` the `jobsimulation` entry marked `migrations: false # legacy`.
- **ENUMERATED** — the cobra startup contract: `cmd/root.go:62` is the root `RunE`; the subcommands are
  exactly `aggregate`, `clone-session`, `test-command`, `validate` (four `Use:` declarations besides the
  root's); **neither** `SilenceUsage` nor `SilenceErrors` is set anywhere in `cmd/`; both Dockerfiles are
  `ENTRYPOINT ["./application"]` with no `CMD` and `EXPOSE 8080`; `cmd/root.go:77`/`:78` are
  `cmp.Or(os.Getenv("PORT"), "8080")` / `cmp.Or(os.Getenv("RPC_PORT"), "8081")`; and the in-repo
  `CLAUDE.md:88` really does document `go run . serve`, a command that does not exist.
- **ENUMERATED** — `internal/worker/worker.go:27` `Concurrency: 10` and `:104` `Concurrency: 25`;
  `go.mod:24` `github.com/hibiken/asynq v0.26.0`; `05b4035` (2026-04-24) is
  *"chore(compose): add READONLY_DB_CONNECTION to jobsimulation service"*.
- **ENUMERATED** — `internal/calls/livekit.go`: `:131-135` is the `flag_use_realtime_openai`
  `IsFeatureEnabled` call and `:140-144` is the `if …Enabled { agentName = "anthropos-agent";
  agentEndpoint = "openai-hosted" }` block — and both sit **inside** `CreateAgentDispatch`, which starts
  at `:106`. The doc's emphasis that the flag *resets the agent name* is exactly what the code does.
- **ENUMERATED** — the nil-voice-engine default: `voiceEngineFromDirectus` is at `:1594`, the
  `if directusVoiceEngine == nil { return …Gptrealtime }` at `:1595-1597`. (Path note: the corpus writes
  `cms/directus/collections/jobsimulation.go`; the file lives at `internal/directus/collections/…` in the
  `cms` repo and `internal/cms/directus/collections/…` in `app`. Both resolve under the obvious
  `internal/` elision, so I did not book it.)
- **ENUMERATED** — `internal/graph/queries.resolvers.go:70` is the `JobSimulationResult` query resolver,
  whose body is Ent/manager SELECTs with no engine or LLM call — the "persisted read" claim holds.
- **ENUMERATED** — the cross-process-edge correction at `:146`: `AUTHORIZATION_ADDRESS=http://sentinel:8087`
  at `docker-compose.yml:48`, `GOTENBERG_URL=http://gotenberg:3200` at `:57`, gotenberg's
  `profiles: [core, backend, all]` at `:183`, `JUDGE0_BASE_URL` at `:59`, and
  `app/internal/converter/gotenberg.go:31` is the `http.NewRequestWithContext(… gotenbergURL +
  "/forms/libreoffice/convert" …)` call. **Zero** `*_RPC_ADDR` variables in compose at `0c91421`.
- **ENUMERATED** — `app/main.go:1204 @ b948604` is
  `mux.Handle(jobsimulationv1connect.NewJobSimulationServiceHandler(jobsimDj.RPCServer))`;
  `app/main.go:614 @ b948604` is the jobsim-wiring `log.Fatalf`; `app/main.go:980-982 @ b948604` is the
  `DIRECTUS_BASE_ADDR` empty-check + `log.Fatalf`; `app/cms_reader_switch.go` exists;
  `jobsimulation/internal/runner/runner.go`'s package doc says *"formerly the standalone 'roadrunner'
  service"*. The only `spf13/cobra` **import** in `app` is `cmd/createTaxonomy/main.go` (the other
  `git grep` hits are `go.mod`, `go.sum` and two markdown/lock files — not imports).
- **ENUMERATED** — the four `func main()` anchors in the `:202-204` blockquote: `:216 @ 7177374`,
  `:212 @ b948604`, `:216 @ 9d00a313`, `:229 @ ad9f3c49`. All four exact. (Only the `:670` in the
  sentence below them fails — B2.)

### `customerio-sync.md` — clean; every anchor exact

- **ENUMERATED** — `app/internal/customeriosync/` holds `doc.go`, `store.go`, `sync.go`,
  `sync_query.sql` (+ `sync_test.go`), and `app/cmd/customerio-resync/main.go` exists. `doc.go:4-5`
  carries *"the last of the Go services to be folded into app"* across the line break, exactly as
  described. `doc.go:30` carries the **7,392 of 7,397** row comparison.
- **ENUMERATED** — `app/env_guards.go:62` is `envCustomerIOSyncEnabled = "CUSTOMERIO_SYNC_ENABLED"`;
  `:87` is `mustSubsystemSwitch`'s `log.Fatalf`; `:98-104` is `resolveSubsystemSwitch`'s
  `case "": if deployed { … }`. `app/main.go:284` is a **comment** (as the corrected text insists),
  `:286` the resolution, `:295` the `BREVO_KEY` condition, `:296` its `log.Fatalf`, `:393-396` the
  `copilotDB` construction with `:395` `os.Getenv("BREVO_KEY")`. `git ls-tree b948604f -- env_guards.go`
  is empty, confirming the file did not exist at the demo's former pin — the exact reason the banner
  gives for re-pinning.
- **ENUMERATED** — `docker-compose.yml:84-92` @ `0c91421` is precisely the nine-line
  MESSENGER/CUSTOMERIO comment block. `838d907` is *"chore(compose): drop the storage, messenger and
  customerio-sync containers"* (2026-08-05), merged `0c91421` = PR #26; its diff deletes the
  `customerio-sync:` block whose `profiles:` line reads **`[customerio-sync, all]`** — so the profile and
  `all`-membership claims are both exact, and so is the `context: git@github.com:…#main` build pattern.
- **ENUMERATED** — the `external_services.md` cross-file claim, which is a self-contradiction check the
  document invites: its `##` sections are **Clerk, Directus, GraphQL Gateway — WunderGraph Cosmo Router,
  AI Providers, LiveKit, AWS Chime SDK** (plus non-per-service sections), there is **no** Customer.io and
  **no** Brevo section, and `grep -ci brevo` over that file returns **0**. The retraction is correct in
  both directions it claims.
- **ENUMERATED** — the asynq move: `internal/worker/tasks/customerio_sync.go:11` declares
  `TypeCustomerIOSync`, `:14` records the 10-minute ticker → asynq scheduler port, and
  `internal/worker/customerio_sync_schedule_test.go` pins the cron against `customeriosync.SyncInterval`.

### `askengine.md` — clean; one source-side inconsistency worth naming

- **ENUMERATED** — `registry.go`: **60** `TableDef` entries (60 `Name:` fields), **57** `Schema: "public"`
  and **3** `Schema: "directus"` — and `directus` is the only second *physical* schema, since every
  `jobsimulation.*` / `skiller.*` / `skillpath.*` entry carries `Schema: "public"` with a `RealName`.
  `registry.go:183-195` is exactly the jobsim-transition-alias comment block the sentence cites.
- **ENUMERATED** — the constants: `handler.go:34` `maxAgenticIterations = 15`, `:41`
  `loopTimeout = 10 * time.Minute`; `main.go:167` `askEngineMaxConns = 6`, `:351` `COPILOT_DB_CONN`;
  `executor.go:18` `MaxInlineRows = 200`, `:21` `MaxCellLength = 400`, `:24` `QueryTimeout = 10 * time.Second`
  applied via `SET LOCAL statement_timeout` at `:97`; `bedrock.go:25` `DefaultModelID =
  "eu.anthropic.claude-sonnet-4-6"`, `:26` `DefaultRegion = "eu-west-1"`, `:161` `func NewBedrockClient`
  with `stripAnthropicAuthMiddleware` wired at `:185`. `rules.md` is **146,219 bytes** ("~146 KB" ✓).
- **ENUMERATED** — the four test-file counts are exact: `sandbox_test.go` **49**, `executor_test.go`
  **13**, `prompt_test.go` **10**, `followups_test.go` **14** `func Test…` declarations.
- **ENUMERATED** — the five `public`-schema tables have Ent schemas: `askautorule.go`,
  `askconversation.go`, `askmessage.go`, `askqueryexample.go`, `askquerylesson.go`.
- **ENUMERATED** — the `main.go:467-471` block quoted in the doc is **byte-identical** to source at
  `ad9f3c49`; `func main()` is at `:229`; and I confirmed the load-bearing sub-claim by enumeration —
  `awk '/^func /' ` over `:229`–`:467` returns **exactly one** hit, `229: func main() {`, so the `return`
  really does return from `main`. The downstream anchors `:1295` (RPC mux) and `:1361` (meta server) are
  exact (`:1438` is the minor above).
- **ENUMERATED, and it corrects a stale *source* comment rather than the corpus** — the `/ask` route
  table. `internal/web/backend/ask/admin.go`'s own doc comments say
  `POST /admin/ask/auto-rules/distill`, which would make the corpus's `/ask/admin/auto-rules/distill`
  look wrong. It is not: `backend.go:171` mounts `e.Group("/ask", …)` and `:185-188` register
  `/admin/auto-rules/*` **on that group**, so the real paths are the corpus's. Every one of the eleven
  routes listed at `:64-67` resolves. The Go comments are the stale side.
- **ENUMERATED** — "**four** documented bypass vectors" in `WrapQuery`. `sandbox.go:350` opens with the
  word *"three"* and then **enumerates four** bullets (quoted-identifier, comment-hidden table,
  unresolvable FROM/JOIN target, reserved CTE name), each with a distinct sentinel error I confirmed in
  the function body. The corpus's **four** matches the enumeration; the source's prose numeral is the
  stale one. Worth recording because the naive read ("source says three, corpus says four") would book a
  false positive.
- **ENUMERATED** — the release dates: `CHANGELOG.md:1681` `## v1.267.0 - 2026-05-07` and `:729`
  `## v1.340.0 - 2026-07-17`.

### `alignment_testing.md` — clean; every tooling anchor read in the **pinned per-stack clone** `09d06070` (and cross-checked identical in the authoring copy `944fc4a2`)

- **ENUMERATED** — the five DNA gene/capability counts, derived by parsing the JSON rather than trusting
  the table: `clerk-2.6.0` **14 caps / 27 genes**; `clerk-js-5` **6 / 9**; `clerk-multi-1` **5 / 9**;
  `clerk-deploy-1` **3 / 7**; `clerk-express-1` **5 / 13**. All five match the document exactly.
- **ENUMERATED** — "**Only `clerk-2.6.0`** … declares a `consumed_surface` today." Parsed per file:
  `clerk-2.6.0` has **15** entries; the other four have **0**. The claim is exactly right, and so is the
  consequence it draws (four of five surfaces warned-but-not-enforced).
- **ENUMERATED** — the `--if-declared` distinction, which the document is at pains to keep separate from
  the bare command. `gate.sh:69` is `"$alignctl" dna coverage --dna "$base/$dna" --if-declared`; in
  `alignment/cmd/alignctl/dna.go`, `dnaCoverage`'s `--if-declared` branch returns **0** *only* for the
  "declares no consumed surface at all" case (`:77-78`), and every other failure path returns **2**
  including the declared-but-uncovered one. The document's framing — "a declared hole is fenced; an
  undeclared surface is only warned about" — is precisely the code.
- **ENUMERATED** — `run.go:133-136` is the `const ( ExitRegressed = 2; ExitUnmeasurable = 3 )` block and
  `:139-153` is `func unmeasurable(…)` through `return ExitUnmeasurable`, printing the boxed
  *"UNMEASURABLE — the runner could not execute. THIS IS NOT A PASSING SCORE."* banner. `run.go:38` calls
  `d.Validate()` before scoring, and `dna.go:283`/`:287-290` is `validateConsumedSurface`.
- **ENUMERATED** — the zero-critical-genes guard is genuinely two-sided: `dna.go:280` emits
  *"no critical genes: a DNA must declare at least one `critical` capability"*, and
  `compare.go:57` refuses a non-zero critical threshold when `r.CriticalGenes == 0`, with
  `critical_genes` marshalled at `compare.go:43`. Both halves exist, as claimed.
- **ENUMERATED** — the id/weight contract: `dna.go:21` `idPattern = "^[A-Za-z0-9][A-Za-z0-9_-]*$"`,
  `dna.go:24` `maxWeight = 1_000_000`, `:269-270` "weight must be in 1..%d". The four operators are
  `dna.go:55-58` (`exact`, `shape`, `normalized`, `error_class`).
- **ENUMERATED** — the toy example's numbers, re-derived from its DNA rather than from the pasted output:
  2 capabilities (`Add` critical ×3 variants, `Greet` standard ×3) = **6 genes**; weights 3·3 + 2·3 = 15;
  one failing standard gene (w2) ⇒ overall (15−2)/15 = **86.666… → 86.7 %**, critical 3/3 = **100 %**,
  "5/6 genes aligned" — matching the transcript including the `w2` annotation. The divergence is real:
  `examples/toy/source/source.go:28` normalizes with `strings.Join(strings.Fields(name), " ")` and the
  mirror has no such call. The toy gate really is `gateOverall = 80.0 / gateCritical = 100.0`
  (`alignment_test.go:21-22`), and `gate.sh:31-32` defaults are `95` / `100`, matching "gate ≥95 / =100".
- **ENUMERATED** — the inert-CI retraction, the claim this file has been wrong about twice.
  `clerkenstein/.github/workflows/alignment.yml` **exists and is git-tracked**
  (`git ls-files` returns it), and its `:10-11` reads *"as a subdir workflow under clerkenstein/.github
  (not at the monorepo root), this is currently inert in the rosetta-extensions monorepo; it is kept
  illustrative of the per-mirror gate shape"* — the quotation is faithful and the line range is exact.
- **ENUMERATED** — the layout claims: `alignment/` holds exactly `cmd/`, `internal/`, `examples/`,
  `go.mod`, `README.md` and has **no `scripts/` dir**; the scripts live at
  `clerkenstein/alignment/scripts/{gate,drift-check}.sh` (plus `drift-test.sh`) with
  `RUNNER_PKG` defaulting to `./cmd/clerkrun` (`:33`), `DNA` to `dna/clerk-2.6.0.json` (`:35`), and
  `ALIGN_DIR` to `$base/../../alignment` (`:30`). Five runners
  (`clerkrun`, `jsfapirun`, `expressrun`, `deployrun`, `multirun`) and five DNAs exist. `go.mod` is
  `module anthropos.dev/alignment` with **no `require` block** — "stdlib-only" holds.
  `clerkenstein/knowledge/alignment.md` and `architecture.md` both exist.
  `alignment/cmd/alignctl/dna.go:20-26` is the `list|diff|validate|coverage` switch (the `coverage`
  `case` is at `:26`; its `return` is `:27`).
- **ENUMERATED** — the data/snapshot dimension: `stack-seeding/dna/snapshot.go:62` is
  `var validSnapshotOperators = map[string]bool{` listing exactly **six** operators, the six the document
  names, with `cross-surface-closure` among them. `data-dna.json:410` is the taxonomy surface's
  `"operators": [` opening a **five**-member list = all six *minus* `cross-surface-closure`; `:439` is the
  content surface's `"operators": [` opening a **five**-member list = the same five *minus*
  `embedding-dim` *plus* `cross-surface-closure` — exactly as described. The directus predicate
  `private=false AND tenant_id IS NULL AND status='published'` is in the surface's own note at `:460`.
  `dna.CapturedFromManifest` (`snapshot.go:370`) and `PgFidelityProbe` (`fidelity_probe.go:20`) exist, and
  `datadna`'s exit contract is `0 ok / 1 drift / 3 usage` (`cmd/datadna/main.go:40-42`, `:100`) —
  matching "mirroring `drift-check.sh`".

### `intelligence.md`

- **ENUMERATED (platform side)** — `fdfa189` is *"chore: remove intelligence service from local dev
  orchestration"*, and `intelligence` is absent from `repos.yml` and `docker-compose.yml` at `0c91421`.
  Its row in `platform-migration-status.md:100` agrees.
- **NOT settled** — the historical role, port `:8080`, the `/_meta`-only surface, the 5-minute ticker and
  the `DB_CONNECTION_BACKEND`/`DB_CONNECTION_SKILLER` variables. The `intelligence` repo has never been in
  the clone set; nothing in `stack-demo` can settle these. They are flagged as historical by the file
  itself, so I did not book them — but they are unverified, not verified.

---

## What I could not settle

1. **The 93-repo org census and the "46 named by no corpus document"** (`platform-migration-status.md:126-131`).
   Needs the GitHub org API (`gh api orgs/anthropos-work/repos`), which I did not call. The `4 in
   repos.yml` half is ENUMERATED and correct. The §3 census table's per-repo "last push" dates are in the
   same class.
2. **Repo archive states** — `skiller` (2026-07-01), `skillpath` (2026-07-31), `intelligence`
   (2026-04-02), `graphql-wundergraph` (2026-07-30), and the "not archived" assertions for `chronos`,
   `cms`, `roadrunner`, `storage`, `messenger`. Archive state lives in the org API, never in git objects —
   which the `jobsimulation` row states outright and correctly. Settled by
   `gh api repos/anthropos-work/<name> --jq .archived`.
3. **Anything in `infrastructure`** — `services.tf`'s `module.jobsimulation_euwest1` /
   `module.cms_euwest1` / the roadrunner rollback declaration, and the `customerio-sync` and
   `intelligence` prod terraform. That repo has never been in a clone set. Both files say so and decline
   to assert; I am recording that their *declining* is the correct verdict, not that the underlying fact
   is known.
4. **`app/knowledge/plan/releases/09.00-support-in-app/m903-s3-custody/`** (cited by
   `platform-migration-status.md:92` for `status: planned` / *"Planned, not started"*). Path falls under
   `knowledge/plan/**`; I applied the instrument's HARD BAR 1 literally and did not open, list or grep it,
   even though the tree in question is a platform clone rather than this repo's audit directory. The
   surrounding claims that *are* readable — no `moved` blocks, zero `custody` occurrences,
   `storage.tf:22-25`'s supersession text, `d3e6d32`'s *"M903 never ran"* — all check out, so the
   unverified part is only the plan file's own front-matter.
5. **`messenger`'s prod terraform graded at the checkout.** `messenger/terraform/main.tf:29 = 0` and the
   "121 lines" figure are true at `origin/main` `e9421c68` and **false at the checkout** `fa47850d`
   (111 lines, `= 1` at `:19`), and the cell names no messenger-repo ref — unlike the `storage` cell,
   which pins `9f8cb53` explicitly. I did **not** book this: the cell describes post-fold prod state,
   `CITE_REF=auto` resolves `origin/main` first (§5 rule 41), and booking it would be the rule-45 failure
   in which a briefing's tree choice manufactures a finding. Recording it so the adjudicator can decide.
   The cheap repair is one sha in the cell, exactly as the `storage` row already does.
