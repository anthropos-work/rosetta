# Seat F — reading #25

## Ground truth actually read (refs verified at this seat's open)

| repo | path | sha I read |
|---|---|---|
| app | `stack-demo/app` | `ad9f3c49` (checkout == `origin/main`); pinned claims also read at `b948604f`, `9d00a313`, `2035f9a4` |
| platform | `stack-demo/platform` | `0c91421d`; pinned claims also read at `0dab54d`, `2adcf71`, `d11a403`, `838d907` |
| next-web-app | `stack-demo/next-web-app` | `8297c684` (checkout); `origin/main` `f97ba659` also read |
| sentinel | `stack-demo/sentinel` | `f2c46190` |
| storage | `stack-demo/storage` | `4ce8ece5` (checkout); pinned claims read at `9f8cb53`, `63bffc8`, `d3e6d32` |
| messenger | `stack-demo/messenger` | `fa47850d` |
| cms | `stack-demo/cms` | `ca50c817`; `6efa1d5` / `f38c0c4` also read |
| graphql-wundergraph | `stack-demo/graphql-wundergraph` | `60c229f3` (== `origin/main`) |
| jobsimulation | `stack-demo/jobsimulation` | `462343b0` (checkout); pinned claims read at `6092c6d2`, `origin/main` `82cb66ec` |
| roadrunner | `stack-demo/roadrunner` | `87d8d443` |
| ant-academy | `stack-demo/ant-academy` | `22df69dd` — **NB the working tree is dirty** (4 files modified by an applied demo-patch: `serverTenant.js`, `serverChapterBody.js`, `UserMenu.jsx`, `catalog.json`). **Every ant-academy anchor below was read via `git show HEAD:<path>`, never from the working tree.** |
| rosetta-extensions (**per-stack consumption clone — the tree that settles a "what the tooling does on a stack" claim**, per §5 rule 45 / the addendum) | `stack-demo/rosetta-extensions` | `09d06070` |
| rosetta-extensions (authoring copy — **not** used to settle anything here) | `.agentspace/rosetta-extensions` | `944fc4a2` |

## Positive controls

| file | `wc -l` | read in full? |
|---|---|---|
| `corpus/services/ant-academy.md` | 501 | yes |
| `corpus/architecture/architecture_overview.md` | 427 | yes |
| `corpus/services/storage.md` | 248 | yes |
| `corpus/services/sentinel.md` | 166 | yes |
| `corpus/services/academy-backend.md` | 153 | yes |
| (total) | **1495** | |

Search-pipeline positive controls run in the same pass:
- `git grep -c AUTHORIZATION_ADDRESS 0c91421 -- docker-compose.yml` → 1 (against `_RPC_ADDR` → 0), so the 0 is a measured 0.
- `git grep -c STORAGE_S3 HEAD -- docker-compose.yml` → 2 (against `STORAGE_RPC_ADDR` → 0 in the same three files).
- `git grep -c custody 9f8cb53` → 0 against a control term that does match in the same tree.
- All platform-source greps used `git grep` at a named ref, not bare `grep` (the shell's `grep` is `ugrep --ignore-files`).

---

## Blockers

### B1 — `academy-backend.md` says the demo academy "serves its committed FS catalog"; there is no FS fallback, and `ant-academy.md` explicitly retracts that exact phrasing

- **Anchor:** `corpus/services/academy-backend.md:125-128` —
  > "**Demo caveat**: on a demo the frontend has **no `NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT`**, so it serves its **committed FS catalog** and any rows `academy-seed` writes have **no reader** — i.e. `academy-seed` is **moot on a demo**"
- **Why it is false:**
  - Measured at `ant-academy` `22df69dd` (via `git show HEAD:code/src/lib/serverTenant.js`, i.e. **not** the patched working tree):
    - `:145` — `const view = (await getBackendCatalogView(eids)) ?? emptyCatalogView()`
    - `:163-165` — `getPublicCatalogView()` is the same shape: `(await getBackendCatalogView(new Set())) ?? emptyCatalogView()`
    - `:128-132` (the function's own doc comment) — *"there is NO FS-as-published fallback … A null backend result (not-composed / outage) resolves to the EMPTY view rather than the committed FS catalog: the cutover is intentional, not reversible-on-error."*
    A stock frontend with no endpoint therefore serves **zero cards**, not the committed FS catalog. The mechanism stated is the reverse of the code.
  - The corpus already knows this and says so, in words that name this sentence's error: `corpus/services/ant-academy.md:82-88` — *"**⚠️ It does *not* 'fall back to the committed FS catalog'** — there is **no FS-as-published fallback** in the app … A demo grid renders cards only because the rext demo-patch **`demo-stack/patches/academy-fs-published-fallback`** *restores* that removed fallback on the demo's ephemeral clone … if that patch is refused, the grid is empty."* That patch exists at `stack-demo/rosetta-extensions` `09d06070`: `demo-stack/patches/academy-fs-published-fallback/academy-fs-published-fallback.yaml`, wired at `demo-stack/ant-academy.sh:66` and env-gated on `ACADEMY_DEMO_FS_PUBLISHED` (`ant-academy.sh:565`).
  - So the corpus asserts both "it falls back to the FS catalog" and "it does not fall back to the FS catalog", and source settles it against `academy-backend.md`. The *conclusion* ("`academy-seed` is moot on a demo") happens to survive; the **reason given for it is inverted**, which is what a reader debugging an empty demo academy would act on.
- **Confidence:** high

### B2 — `sentinel.md` cites `architecture_overview.md:321` for a quote that lives at `:331`; `:321` is the PRODUCTION router line and states the opposite topology

- **Anchor:** `corpus/services/sentinel.md:85` —
  > "The correctly-scoped form is the model at [`architecture_overview.md:321`](../architecture/architecture_overview.md) — *"the only cross-process RPC edge out of backend on a core stack"*."
- **Why it is false:** measured in the corpus at this HEAD:
  - `corpus/architecture/architecture_overview.md:321` = `User → Vercel (Next.js) → Clerk (JWT) → ALB → Cosmo Router (port 8080)` — the **production** request flow, i.e. exactly the router hop that does *not* exist on a `core` stack.
  - The quoted string is at `:331` = `  → Connect-RPC to sentinel   (the only cross-process RPC edge out of backend on a core stack)`, inside the **"On a local stack"** block that opens at `:327`.
  - `architecture_overview.md` itself computes the right number: `:276` says the correct form is *"55 lines apart in one file"* from `:276` → `331 − 276 = 55`. So the sibling document agrees the anchor is `:331`; only `sentinel.md` points at `:321`.
  - This is the "anchor resolves but names the wrong construct" class, in its worst form: the line it lands on asserts the router hop the sentence is citing it to deny.
- **Confidence:** high

### B3 — the `next` pin is quoted as `^16.2.7`; the measured pin is `~16.2.12`, at both the checkout and origin

- **Anchor:** `corpus/architecture/architecture_overview.md:59` —
  > "- **Frontend**: Next.js **16** + React 19 + TypeScript on Vercel (`next: ^16.2.7` across all four apps)"
- **Why it is false:** the sentence names no ref, so it is graded at the checkout `next-web-app` `8297c684`. Measured (`git grep -n '"next":' HEAD -- 'apps/*/package.json'`):
  - `apps/hiring/package.json:45` `"next": "~16.2.12"`
  - `apps/integration/package.json:28` `"next": "~16.2.12"`
  - `apps/maintenance/package.json:9` `"next": "~16.2.12"`
  - `apps/web/package.json:46` `"next": "~16.2.12"`
  (plus `packages/ui/package.json:65`, same). Identical at `origin/main` `f97ba659`. Both the range operator (`^` vs `~`) and the patch (`.7` vs `.12`) are wrong — `^16.2.7` and `~16.2.12` are not even the same admissible range.
  - **This is a predicate, not one site**: the same false string stands at `corpus/architecture/frontend_architecture.md:59` (which cites `apps/web/package.json:46` **by `file:line` and quotes it verbatim** — the cited line reads `~16.2.12`) and at `corpus/services/next-web-app.md:22`. I book it once, at my own anchor, and name the twins so the repair is claim-scoped (§5 rule 19).
- **Why I hesitated:** a version pin is a "derived scalar" — the material claim ("Next.js 16") is true, and the canonical grading treats staleness gently. I book it because the corpus does not merely say "16": it publishes an exact pin *with an operator*, and one twin publishes it as a verbatim quote of a line that says something else.
- **Confidence:** high (on the measurement); medium on materiality

### B4 — "the platform **academy subgraph**" is asserted five times in `ant-academy.md` and once in `architecture_overview.md`, while two other corpus files state flatly that no such subgraph exists — and one of the six sentences contradicts itself

- **Anchor:** `corpus/services/ant-academy.md:121-122` —
  > "**Provides** no GraphQL subgraph (it doesn't federate into the supergraph, which is `backend` alone) — but it **consumes** the platform's **academy subgraph** as a GraphQL *client* (see below); "no subgraph" ≠ "no GraphQL""
  Same claim at `ant-academy.md:63`, `:111` (the mermaid edge label `catalog: GraphQL academy subgraph`), `:134`, `:477`, and at `corpus/architecture/architecture_overview.md:40`.
- **Why it is false / contradictory:**
  - `corpus/services/academy-backend.md:87` — "**There is no separate "academy subgraph"** — these types live in the `app`/`backend` federation subgraph — **the only subgraph left**."
  - `corpus/architecture/service_taxonomy.md:286` — the same sentence, with the derivation: all three of `graphql-wundergraph`'s supergraph configs at `60c229f3` carry a single `- name: backend` entry, and the academy types are one SDL file inside it.
  - Ground truth agrees with the negation: at `app` `ad9f3c49` the academy SDL is `internal/web/backend/graphql/graph/schemas/academy.graphqls` — one file inside the **backend** subgraph's schema dir; there is no academy subgraph anywhere, and platform `2adcf71` deleted the router locally.
  - `ant-academy.md:121-122` is self-contradictory inside one sentence: it says the supergraph "is `backend` alone" and then names an "academy subgraph" it consumes.
- **Why I hesitated:** "academy subgraph" can be read as shorthand for "the academy types on the app subgraph", and two of the six sites gloss it with `(app internal/academy)`. I book it because the corpus itself designates the phrase as wrong, in bold, in two files — so a reader who has read those two and then reads these six is left adjudicating, which is the exact cost §5 rule 19 warns about.
- **Confidence:** medium

---

## Minors

- `corpus/architecture/architecture_overview.md:308-309` — "`internal/cms/studio/markdownManager.go:11,19` builds a Mistral client from `MISTRAL_API_KEY`". The claim names no ref, so it grades at the checkout `ad9f3c49`, where `:11` is the closing `)` of the import block and `:19` is a comment describing a **removed** `os.Getenv("MISTRAL_API_KEY")` read; the construct is now `:30` (`mistralocr.New(aiKey)`), with the env read at the cited `studioManager.go:583` (verified). The anchors were **exactly right** at `b948604f` and `9d00a313` (`:11` = `"github.com/anthropos-work/ai/mistral"`, `:19` = `mistral.NewMistral(nil, os.Getenv("MISTRAL_API_KEY"))`) and drifted with the `mistralocr` refactor between `9d00a313` and `2035f9a`. Substance still true → line drift, booked MINOR rather than BLOCKER.
- `corpus/services/ant-academy.md:302` — "~26 Playwright e2e spec files (tests/e2e/)". Measured at `ant-academy` `22df69dd`: **31** `*.spec.js` files in `code/tests/e2e/` (38 tracked paths there in total, incl. 2 helpers and 7 snapshot PNGs). ~19 % undercount on an approximated figure.
- `corpus/services/sentinel.md:43-45` — "only as a fixture string in sentinel's own tests (`internal/authorization/casbin_test.go`, `internal/rpcsrv/rpc_test.go`)". `internal/authorization/manager_test.go:21` also carries it (`manager := "manager"`, used as a `g2` role at `:30`/`:37`). Omitted list member; the load-bearing claim ("there is no `manager` Casbin role") is **verified true** — 0 occurrences in `init_policy.sql`.
- `corpus/services/sentinel.md:15` — "The sentinel binary's default is 8080 (per its own README/CLAUDE.md)". There is **no `README.md`** in the sentinel repo at `f2c46190` (`git ls-tree -r --name-only HEAD | grep -i readme` → empty); only `CLAUDE.md`. The 8080 figure itself is corroborated by `terraform/locals.tf:3` `port = 8080`.
- `corpus/architecture/architecture_overview.md:340-352` — 13 lines lost the `>` blockquote prefix in the middle of the storage blockquote that opens at `:338` and resumes at `:353`. CommonMark lazy continuation keeps it rendering inside the quote, so no factual consequence; the source is inconsistent.

---

## Positively cleared (audited zeros)

**ENUMERATED** (predicate re-derived from source independently; cardinality stated before any arithmetic):

1. **`architecture_overview.md:373-381` — the multi-tenancy split (31 / 135 / 29 / 23 / 16).** This is the passage §5 rule 24 records as wrong in three consecutive generations, so I enumerated every set rather than re-adding the document's sum.
   - Denominator: `internal/data/ent/schema/` holds **139** `.go` files; **135** contain `ent.Schema` — the 4 that do not are `mixin.go`, `skiller_mixins.go`, `skillpath_mixins.go`, `database_types.go`. → **135** ✓
   - `OrganizationMixin{}`: **30** raw hits, of which `user_resource.go:22` is `// OrganizationMixin{},  // We need to work on this` (commented out, does not compile) → **29 live** ✓ (the corpus states both the 29 and the commented 30th, at the right file:line).
   - Schemas declaring their own `Policy()`: `mixin.go` (UserMixin, OrganizationMixin), `org_membership.go:172` (Membership), `organization.go:56` (Organization), `user.go:116` (User). `User`'s policy filters by **user** (`rule.DenyIfNoUserInContext` / `FilterSameUserRule`), not organization, so it is correctly excluded from an *org*-filtering count. → **29 + Membership + Organization = 31** ✓
   - Carriers of an `organization_id` column = union of {29 OrganizationMixin} ∪ {7 `OrganizationIDMixin{}` — `category`, `jobrole`, `similarity`, `skill`, `specialization`, `studio_document`, `studio_task`} ∪ {direct `field.X("organization_id")` decls, minus the 4 non-schema helpers} = **54**. Minus the 31 policed → **24** with an organization_id and no *org* policy.
   - **The 24→23 step is the one that would have caught me out, and it holds:** `academy_feedback.go:64` embeds `UserMixin{}`, which **does** declare a `Policy()` (`mixin.go:99`) — so under the document's own predicate, *"no policy at all"*, it drops out. `CreatedByUserMixin` (used by `category`/`jobrole`/`skill`/`specialization`) declares **no** Policy (`skiller_mixins.go:99-113`), so those stay in. → **23 total, of which 7 are OrganizationIDMixin and 16 are neither-mixin** — exactly the corpus's figures, including the parenthetical *"(16 is the neither-mixin subset of those 23, not the total)"*. Re-derived identically at `b948604`, `9d00a313` and `ad9f3c49`. **CORRECT.**
2. **`architecture_overview.md:80` — "`authn` is not a fifth [imported library]".** Enumerated all seven Go clones (`app`, `sentinel`, `storage`, `messenger`, `cms`, `jobsimulation`, `roadrunner`): `github.com/anthropos-work/authn` = **0 files, all seven trees**; `github.com/anthropos-work/colony` present in **7/7** `go.mod` (the stated positive control). **CORRECT.**
3. **`architecture_overview.md:12/71/156-165` — the five-container `core` set and the always-on floor.** Enumerated `docker-compose.yml` @ `0c91421` (186 lines, `include: common.yml`): 5 services declared (`sentinel`, `backend`, `studio-desk`, `next-web-app`, `gotenberg`) + 2 in `common.yml` (`postgresql`, `redis`) = **7 effective**; the services with **no `profiles:` key** are exactly `postgresql`, `redis`, `sentinel`; `backend` and `gotenberg` carry `profiles: [core, backend, all]` (`:110`, `:183`). `Makefile:10` `PROFILE ?= core`. `frontend`/`studio-desk` both `depends_on: backend` (`:165-167`, `:138-140`). `repos.yml` = 4 entries. **CORRECT**, including `roadrunner.md`'s row wording "5 services remain declared, 7 in the effective topology" (`architecture_overview.md:223`).
4. **`storage.md:29` + `architecture_overview.md:344-349` — the `STORAGE_RPC_ADDR` census, at three refs.** `git grep -n STORAGE_RPC_ADDR <ref> -- '*.go'`: `9d00a313` → **3**, all comments (`main.go:451`, `internal/jobsimwiring/wiring.go:101`, `internal/storagens/callsites_test.go:189`); `2035f9a` → **3**, all comments (`main.go:504`, same two others); `ad9f3c49` → the identical 3; repo-wide at `9d00a313` → **29 lines / 18 files**; `b948604f` → **15** hits of which exactly **7** are live env reads (`cmd/academy-asset-upload/main.go:129`, `cmd/academyImport/main.go:231`, `cmd/import/main.go:50`, `internal/jobsimwiring/wiring.go:115`, `main.go:446`, `:524`, `:992`) — the rest being comments, error strings and `t.Setenv`. `git diff --stat 2035f9a4 ad9f3c49 -- main.go internal/storage/` is empty, and `git rev-list --count 2035f9a4..ad9f3c49` = 5. **CORRECT on every number.**
5. **`storage.md:64-94` — the production-bucket hazard, both halves.** `docker-compose.yml` @ `0c91421`: `:82` `STORAGE_S3_BUCKET=production-storage…005`, `:83` `STORAGE_S3_PUBLIC_BUCKET=production-storage-public…001`, rationale at `:73-79` (*"These MUST be set"*), `:56` `ENVIRONMENT=development`, `:100` `$HOME/.aws/credentials:ro`, and `README.md:81-87` does instruct live AWS keys in `.env`. The private line is **absent** at `2adcf71` and **present** at `0dab54d` — so "arrived with `0dab54d`" is exact. App side @ `ad9f3c49`: `main.go:516/:517` the two `os.Getenv`s, `:518-523` the empty-bucket `log.Fatalf`, `:524/:525` `NewManager`/`NewPublicManager`, `:529-535` `verifyBucketAccess`, both gated on `deployedEnvironment()` = `env_guards.go:37-44`, which returns **false** for `"development"`. And the ref-is-load-bearing clause verifies: at `b948604f`, `env_guards.go` does not exist and `:518-523` / `:529-535` are the public-storage-clients and academy-asset-uploader blocks. **CORRECT.**
6. **`storage.md:25` / `architecture_overview.md:340-343` — the storage terraform state.** At `9f8cb53`, `terraform/main.tf` is **18 lines**, `:9-11` is the *"The ECS service that used to live here is GONE (v9.0 'support-in-app')"* comment, `:13-16` the `prevent_destroy`-is-read-from-configuration rationale, `:18` *"See outputs.tf …"*; **`custody` occurs 0 times in the whole repo at that ref**; `storage.tf:22-25` carries the M903-never-executed note; there is **no `moved` block**. At the checked-out `4ce8ece5` (`git rev-list --count 4ce8ece5..9f8cb53` = **20**) `main.tf` is **100 lines** with `service_desired_count = 1` and `storage.tf:24` still reads *"relocate the assets out of this module BEFORE M907"*; `d3e6d32` is dated 2026-08-05. `63bffc8` did read `service_desired_count = 0`. **CORRECT, including the "state the ref or this flips" warning.**
7. **`architecture_overview.md:20/221` — jobsimulation's M810 teardown.** The claim pins `6092c6d2`, so it is graded there: at `6092c6d2` `terraform/main.tf` has **0** occurrences of `service_desired_count`, `:15-22` is the M810 comment naming the destroyed ECS service / task definition / ECR repository, and `:24-40` is the "what this module still owns" block (LiveKit + Chime buckets, `/production/jobsimulation/*` SSM, atlas tracker). At the **checkout** `462343b0` the module block and `service_desired_count = 0` are still present — so the pin is doing real work and the corpus is right to state it. The archive-state hedge also checks: `origin/main` (`82cb66ec`) carries **four** commits dated **2026-08-04**, one of them the merge of PR **#439**. **CORRECT.**
8. **`architecture_overview.md:222` + `storage.md:175` — cms's "report both, assert neither".** `cms/terraform/main.tf:39` `service_desired_count = 0` at `ca50c817`; `6efa1d5` (2026-08-04, merged `f38c0c4`) is titled *"drop build-production — the cms ECR repository is decommissioned (M810)"* and its body names `module "cms_euwest1"`. Both measured facts exist, and they do point opposite ways. **CORRECT.**
9. **`sentinel.md:26-41` — the Casbin model shape.** Enumerated `internal/authorization/casbin.go` `ModelConf` (`:13-45`): **6** request definitions (`r`…`r6`), **6** policy definitions (`p`…`p6`), **3** role definitions (`g`, `g2`, `g3`), **6** matchers (`m`…`m6`); every per-matcher description matches its expression, including `m`'s `p.tier == '<Tier_TIER_FREE>'` substitution and `m6`'s `g3(p6.org, r6.sub)`. **CORRECT.**
10. **`sentinel.md:5/85/89` — sentinel's caller set.** `AUTHORIZATION_ADDRESS` = **1** occurrence across `docker-compose.yml`, `common.yml`, `.env_example` at both `0dab54d` and `0c91421`, on the `backend` block, `docker-compose.yml:48`; `*_RPC_ADDR` = **0** across the same three files. `messenger` @ `fa47850d`: **0** hits for `authorization` in `*.go` and **0** for `AUTHORIZATION_ADDRESS` anywhere. **CORRECT.**
11. **`ant-academy.md:306-323` — the complete `isPublic` matcher.** Enumerated `code/proxy.js:112-188` @ `22df69dd` and compared entry-for-entry with the table: every listed pattern is present, in the stated groups, and no matcher entry is missing from the table. The two dev-only spreads are gated exactly as described (`DEV_LOGIN_ENABLED`; `VISUAL_BYPASS` = `BENCHMARK_VISUAL_BYPASS==='1' && NODE_ENV==='development'`, `:62-64`, whitelisted not blacklisted). `/api/chapters/*` is indeed **not** in the matcher. The route inventory confirms `/library/[slug]` does not exist and `/courses/[slug]` does. **CORRECT.**
12. **`ant-academy.md:250-289` — the two language switchers.** `src/i18n/LocaleSwitch.jsx` is mounted at exactly one site, `PublicHeader.jsx:20`, and `PublicHeader` at exactly one site, `code/app/(public)/library/page.jsx:28`; `code/app/(public)/free/page.jsx:18` is `redirect('/?tier=free')`. `LanguageSelector.jsx:88` `role="menu"`, `:97` `role="menuitemradio"`, over `LANGUAGES = SUPPORTED_LOCALES.map(...)` with `SUPPORTED_LOCALES` = 7 at `src/i18n/locale.js:10`; mounted at `TopBar.jsx:76`. **The seven `TopBar` routes re-derived by mount site**: `/`, `/courses`, `/courses/[slug]` (all three render `AcademyClient`, `page.jsx:151` / `courses/page.jsx:92` / `courses/[slug]/page.jsx:219`, `TopBar` at `AcademyClient.jsx:1906`), `/chapters/[slug]` (`CourseClient.jsx:2091`, `:2141`), `/latest` (`LatestClient.jsx:128`), `/bookmarks` (`BookmarksClient.jsx:508`), `/my-activity` (`MyActivityClient.jsx:161`); `MyCertificatesClient.jsx` imports no `TopBar`. **CORRECT — all eleven anchors byte-exact.**
13. **`ant-academy.md:154-164` — the five-key `emptyCatalogView()`.** `serverTenant.js:115-117` returns `{ chapters: [], skillPaths: {}, series: [], bundles: PUBLIC_BUNDLES, catalogVersion: CATALOG_VERSION }`; `PUBLIC_BUNDLES` is a populated exported array at `ucourses/catalog.js:961`; `CATALOG_VERSION = '1.0'` at `:31`; the "drops every path whose `scopedChapters` filter is empty" mechanism is `AcademyClient.jsx:1363-1365`. **CORRECT.**
14. **`ant-academy.md:65-77` — the write path is the client harness, not the beacon route.** `code/src/progress/store.js`: `saveChapterProgress` at `:150`, `UPSERT_CHAPTER_PROGRESS` at `:162`, `beacon:` option at `:169`; `saveLastActivity` at `:202`, `SET_LAST_ACTIVITY` at `:210`, `beacon:` at `:215`. `src/writeThrough/index.js:247` `navigator.sendBeacon`, `:259` `keepalive: true`. `app/api/academy/beacon/route.js:1-18` carries the quoted *"best-effort last-ditch flush…"* comment. **CORRECT — this is a recently-rewritten passage and it holds.**
15. **`ant-academy.md:224-248` + `:362-385` — the rext-side demo wiring** (settled at the **pinned per-stack clone** `stack-demo/rosetta-extensions` `09d06070`, not the authoring copy). `demo-stack/ant-academy.sh` declares the five patch paths at `:56`, `:66`, `:75`, `:84`, `:90`; `:576-583` is the M220 comment stating the `e2e_persona` bypass is gone from the launch env, and the launch `env` at `:584-585` sets neither `BENCHMARK_VISUAL_BYPASS` nor `NEXT_PUBLIC_E2E_AUTH`; both named tests exist (`demo-stack/tests/test_ant_academy.py:346`, `demo-stack/tests/test_ant_academy_clerk_wiring.py:102`); the cockpit still sets the cookie at `cockpit.py:812` (client-side) and `:1496` (`Set-Cookie` on the `/go` 302), named at `:327`; `stack-verify/live/autoverify.sh:459` is assert **(f)** "THE ACADEMY RENDERS ITS CATALOG". All five patch manifests exist under `demo-stack/patches/`. **CORRECT.**
16. **`academy-backend.md` — everything except B1.** `main.go:471-472` @ `9d00a313` = `NewManager`/`NewPublicManager`; `cmd/academyImport/main.go:239-243` the bucket-not-RPC comment + read, `:244-246` the hard error; `git describe --tags ad9f3c49` → `v1.369.0-7-gad9f3c498` and `CHANGELOG.md:5` `## v1.369.0 - 2026-08-05`; `0e37771f` is 2026-06-05, PR #903, *"Academy backend v1.0 'ground truth'"*. **The Ent pluralization warning is exact** — generated `Table` constants: `academy_chapter_progresses`, `academy_last_activities`, `academy_chapter_times`, `academy_certificates`, `academy_bookmarks`, `academy_feedbacks`, `academy_series`, `academy_skill_paths`, `academy_chapters`, `academy_chapter_bodies`, `academy_path_embeddings`. `academy_path_embedding.go:62` `extensions.vector(1536)`. GraphQL: every named query and mutation exists in `schemas/academy.graphqls:640-719`, with `academyCertificate` and the catalog queries carrying `@public`. REST: `content.go:23` `GET /content/catalog.json`, `content_admin.go:35` the `/content/admin` group behind `sharedTokenMiddleware(os.Getenv("ACADEMY_CONTENT_API_TOKEN"))`. Task type `academy_embedding_refresh` at `internal/worker/tasks/academy_embedding_refresh.go:10`, 03:00 stated in-source at `embeddings.go:108`. All named cmd binaries and test files exist. **CORRECT.**

**RE-DERIVED** (I re-computed arithmetic the document itself displays — the weakest clearance, listed separately on purpose): the `d11a403` repos.yml diff (`git show d11a403 -- repos.yml` deletes `- name: cms`, `- name: jobsimulation` **and** `- name: roadrunner`, against a commit message that says roadrunner's entry "was already gone" — the corpus's *"the diff is the fact"* is right); `915da06`'s subject line does say "2→1" while its body deletes both the jobsimulation and cms subgraph entries; `graphql-wundergraph/terraform/main.tf:20` `= 1` and `locals.tf:8` `port = 8080`; `roadrunner/terraform/main.tf:19` `= 1`; all platform sha↔date↔subject pairs quoted in the three files (`2adcf71`/`b56d731`/`360efd4` 2026-07-31, `d11a403`/`0dab54d` 2026-08-03, `838d907`/`0c91421` 2026-08-05).

---

## What I could not settle

- **`architecture_overview.md:3`** — *"the [`graphql-wundergraph`] repo is ARCHIVED on GitHub (2026-07-30)"*. GitHub archive state is not readable from a clone and I did not fetch. The clone's newest commit is `60c229f3`, dated 2026-07-30, which is *consistent* with it — but consistency is not the measurement, and the same document (`:20`) correctly refuses to assert archive state for `jobsimulation` on exactly this ground. Two files, one asserting and one refusing, on evidence of the same kind. Settled by `gh api repos/anthropos-work/graphql-wundergraph --jq .archived`.
- **`architecture_overview.md:13/236`** — "≥42,790 skills / ≥22,470 job roles". A DB measurement; no stack was up and `/db-query` was out of scope for a read-only audit.
- **`ant-academy.md:90/302`** — "a grid of **65** real cards" and "**1000+** Vitest tests". Both need execution (a live demo render; `npm test`), not a source read.
- **`storage.md:89-92`** — the escalated-item / defect-register references (`DEF-M257x-iter80-storage-prod-bucket`, `knowledge/plan/platform-defect-register.md`). **Not checked: hard bar 1 forbids reading under `knowledge/plan/**`.** Likewise `storage.md:25`'s citation of `app/knowledge/plan/releases/09.00-support-in-app/m903-s3-custody/` front-matter — that path is in the *platform* clone rather than this repo, but I declined it to stay unambiguously inside the bar. The rest of that cell (the `storage.tf:22-25` M903 note, the absent `moved` block) was verified directly and is correct.
- **`ant-academy.md:296/472`** — "`@anthropos.work` domain restriction is enforced in the Clerk app". Clerk dashboard state; not in any clone.
- **`architecture_overview.md:419`** — "VPC (10.0.0.0/16) … public subnets (ALB, Cosmo Router)". The `infrastructure` repo has never been in the clone set (the document says as much elsewhere), so no terraform here can confirm or refute it.
- **`ant-academy.md:389` vs root `CLAUDE.md`** — this doc says the app's env file is `code/.env` (`cp .env.example .env`), while `CLAUDE.md` says the React app "reads **only** from `code/.env.local`". Next.js loads both, so `CLAUDE.md`'s "only" is the wrong side; I did not book it because the defective anchor is outside `corpus/**` and outside this reading's scope, and the in-scope sentence is correct. Flagged here so a claim-scoped repair sees it.
