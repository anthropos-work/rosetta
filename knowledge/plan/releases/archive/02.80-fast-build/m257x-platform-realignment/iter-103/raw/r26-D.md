# Seat D — reading #26

**Instrument (proof of delivery):** `briefing-r26-AS-DELIVERED.md`
sha256 `4178b00f0d8e98eabddb895969d6e4c58799a5b292d967f8a2cfca6772a18d3d` (243 lines), read in full
before any corpus file was opened. `corpus/ops/platform-alignment.md` §5 (lines 249–1548) read in full.

**Corpus HEAD at reading:** rosetta `ab3abe36` (working tree clean for `corpus/**`).

**Ground-truth refs, re-verified at this open (ADDENDUM supersedes the frozen table):**

| repo | path | sha measured | matches addendum? |
|---|---|---|---|
| app | `stack-demo/app` | `ad9f3c49` | yes |
| platform | `stack-demo/platform` | `0c91421d` | yes |
| next-web-app | `stack-demo/next-web-app` | `8297c684` | yes |
| sentinel | `stack-demo/sentinel` | `f2c46190` | yes |
| studio-desk | `stack-demo/studio-desk` | `41ee3575` | yes |
| ant-academy | `stack-demo/ant-academy` | `22df69dd` | yes |
| storage / messenger / cms / graphql-wundergraph / roadrunner / jobsimulation | `stack-demo/*` | `4ce8ece5` / `fa47850d` / `ca50c817` / `60c229f3` / `87d8d443` / `462343b0` | yes |
| **rosetta-extensions — pinned per-stack CONSUMPTION clone (adjudicates tooling claims, rule 45)** | `stack-demo/rosetta-extensions` | `09d06070` | yes |
| rosetta-extensions — authoring copy | `.agentspace/rosetta-extensions` | `944fc4a2` | yes |
| nested `anthropos-studio-room` (own checkouts) | `stack-demo/app/studio`, `stack-demo/cms/studio` | `aeec036a` (both) | yes |

**No `git fetch` / `pull` / state-changing git command was run.** No file under `knowledge/plan/**`
(rosetta) and none under `.agentspace/scratch/` was read. (Note: a `git grep` for `MESSENGER_RPC` in
`stack-demo/app` surfaced two hits whose *paths* live in **the `app` repo's own** `knowledge/plan/`
tree — a different repository, not this audit's answer-key directory. Only the two grep result lines
were seen; the file was not opened.)

**Which trees each "zero" covers:** every absence claim below was measured across **all 15 git trees**
enumerated by `find stack-demo -name .git -maxdepth 4`, each at **its own HEAD**, including the two
nested `studio` repos, and each with a positive control in the same pass.

## Positive controls

| file | `wc -l` | read in full? |
|---|---|---|
| `corpus/architecture/service_taxonomy.md` | 514 | yes |
| `corpus/services/clerkenstein.md` | 370 | yes |
| `corpus/services/graphql-wundergraph.md` | 280 | yes |
| `corpus/services/messenger.md` | 204 | yes |
| `corpus/architecture/frontend_architecture.md` | 116 | yes |
| `corpus/services/db-backup.md` | 31 | yes |
| **total** | **1515** | |

---

## Blockers

### B1 — `frontend_architecture.md` quotes a `next` version literal that is false at the ref the same file declares authoritative

- **Anchor:** `corpus/architecture/frontend_architecture.md:59` —
  > `* **Framework**: **Next.js 16** (App Router, Turbopack), React 19 — \`apps/web/package.json:46\` reads \`"next": "^16.2.7"\` (same in \`apps/hiring\` / \`apps/integration\` / \`apps/maintenance\`); the repo carries an \`UPGRADE-IMPACT-next16.md\``
- **Why it is false:** The block names **no ref**, so per the instrument it grades at the checkout,
  `next-web-app` `8297c684`. There:
  `git show HEAD:apps/web/package.json | grep -n '"next"'` → **`46:    "next": "~16.2.12",`**.
  Same value in all three siblings the sentence generalises to
  (`apps/hiring/package.json:45`, `apps/integration/package.json:28`,
  `apps/maintenance/package.json:9` — all `"next": "~16.2.12"`).
  The quoted `"^16.2.7"` is exactly what line 46 read at the **older** `bb3313bc`
  (`git show bb3313bc:apps/web/package.json` → `46:    "next": "^16.2.7",`) — the ref the addendum
  supersedes. The caret→tilde change is not cosmetic: `^16.2.7` admits minor bumps, `~16.2.12` admits
  patch only, so a reader reasoning about the upgrade envelope gets the opposite answer.
  **It self-contradicts, in the same file, eighteen lines above:** `:41` states the re-derivation was
  taken at ``next-web-app` **`8297c684`** (the ref this stack builds, 2026-08-05; 41 commits / 192
  files past the `bb3313bc` the old figures came from)` — so the file declares `8297c684`
  authoritative and then quotes a `bb3313bc`-only literal without a pin.
- **Confidence:** medium — the fact is certain (high); I hesitated on BLOCKER-vs-MINOR because a
  dependency-range literal drifting one release could be read as "stale-but-harmless phrasing". Booked
  as a BLOCKER under the instrument's tie-break and because the anchor's own file explicitly refreshed
  its *other* numbers at `8297c684` in the immediately preceding paragraph, which makes the surviving
  quote read as freshly verified.

### B2 — `clerkenstein.md` says `sentinel` is "still on `colony v0.34.3`"; it is on `v0.35.2`

- **Anchor:** `corpus/services/clerkenstein.md:276` —
  > `\`sentinel\` and \`storage\` are still on \`v0.34.3\`, so the pin is not wrong for the whole platform, but a \`clerk-deploy-1\` score taken against \`v0.34.3\` **is not measuring the binary under test**.`
- **Why it is false:** At `sentinel` `f2c46190` (**= `origin/main`**, so both candidate refs agree),
  `git show HEAD:go.mod | grep colony` → **`github.com/anthropos-work/colony v0.35.2`**.
  It moved at `88036d7` *"chore(deps): update dependencies to latest versions"* (**2026-08-03**); at
  the frozen table's older `88bc5592` it did read `v0.34.3`, which is where the sentence came from.
  Measured across all seven Go clones at their own refs: `app` v0.35.2, `sentinel` **v0.35.2**,
  `messenger` v0.35.2, `cms` v0.35.1, `jobsimulation` v0.35.1, `storage` v0.34.3, `roadrunner` v0.34.3.
  **The pin is a scope: the sentence names a ref (`2adcf71`) only in the *preceding* sentence, about
  `app/go.mod`; a pin's scope is the claim's own wrapped sentence (§5 rule 33), and this is a separate
  one.** The consequence is material, not clerical: the clause exists to *soften* the drift warning
  ("not wrong for the whole platform"), and the softening now rests on `storage` and `roadrunner`
  alone — both **frozen legacy repos with no compose service and no `repos.yml` entry** since
  `838d907` / `d11a403`. Every service a demo actually builds is past the artifact's `v0.34.3` pin, so
  the mitigation the sentence offers no longer exists. (The `deploy/colony-authn` artifact pin itself
  is correct: `stack-demo/rosetta-extensions/clerkenstein/go.mod:8` = `colony v0.34.3`, pinned clone
  `09d06070`; identical in the authoring copy `944fc4a2`.)
- **Confidence:** high.

### B3 — `service_taxonomy.md`'s `studio-desk/.env.example:45` anchor lands on the wrong line

- **Anchor:** `corpus/architecture/service_taxonomy.md:47` —
  > `(\`studio-desk/.env.example:45\` bakes \`VITE_GRAPHQL_ENDPOINT=http://localhost:8082/graphql/query\`; …`
- **Why it is false:** At `studio-desk` `41ee3575` (the checkout, level with `origin/main`; the block
  names no ref):
  `git show HEAD:.env.example | grep -n VITE_GRAPHQL_ENDPOINT` → **`44:`**.
  Line **45** is `VITE_ENVIRONMENT=production` — a different construct. It *was* `:45` at the frozen
  table's `14a5442a`, so this is drift introduced by the clone advance, not an original error.
  Booked because the instrument's BLOCKER definition names "wrong line anchors that point at the wrong
  construct" explicitly, and because the surrounding claim (the Desk→Backend generation edge) is one
  the same passage introduces as a *correction* of a previously-backwards diagram — text the briefing's
  own rule 18 flags as the highest-risk class.
- **Confidence:** high on the measurement; I hesitated on grading — the canonical rule referenced in
  §5 rule 25 carves "line drift" out as MINOR, while the instrument **as delivered** puts a wrong-construct
  anchor in the BLOCKER list and resolves doubt upward. Graded by the delivered instrument.

### B4 — `service_taxonomy.md` lists `ai` among the private Go modules "pulled at Docker build"; no built service imports it, directly or transitively

- **Anchors:**
  - `corpus/architecture/service_taxonomy.md:166` — `**Shared Libraries** (imported as private Go modules — **not** cloned by \`make init\`; pulled at Docker build via \`GH_PAT\`/\`GOPRIVATE\`)` (heading over a five-row table whose second row is `ai`)
  - `corpus/architecture/service_taxonomy.md:509` — `**5 libraries, 4 imported** — colony, proto, ai, taxonomy (none of them in \`repos.yml\`; they are pulled at Docker build)`
- **Why it is false:** ENUMERATED over all seven Go clones at their own refs
  (`git show HEAD:go.mod | grep 'anthropos-work'`):

  | repo | requires `anthropos-work/ai`? |
  |---|---|
  | `app` `ad9f3c49` | **no** |
  | `sentinel` `f2c46190` | **no** |
  | `storage` `4ce8ece5` | no |
  | `messenger` `fa47850d` | no |
  | `roadrunner` `87d8d443` | no |
  | `cms` `ca50c817` | yes (`ai v1.40.2`) |
  | `jobsimulation` `462343b0` | yes (`ai v1.40.2`) |

  Not transitive either: `anthropos-work/ai` appears **0 times** in `app`'s and `sentinel`'s **`go.sum`**
  (positive control in the same pass: `anthropos-work/colony` appears 2× in `app/go.sum`). The only two
  repos that require it — `cms` and `jobsimulation` — have **no compose service, no `repos.yml` entry,
  and are not cloned by `make init`** (this very file says so at `:114-117` and `:160-161`), so **no
  Docker build the platform performs at `0c91421` pulls `ai` at all**. `app` has no `internal/ai`
  package either (`git ls-tree HEAD internal/ | grep '^ai'` → empty), which is consistent with the row's
  own "vendor selection lives in the consumers" note but not with "pulled at Docker build".
  Supporting, same measurement: the "5 libraries" set is also no longer the set of private modules the
  built services import — `app/go.mod` requires `anthropos-work/storage v0.15.2` and
  `anthropos-work/analytics-go v0.3.1`, neither of which appears in the table.
- **Confidence:** medium — the measurement is high-confidence; I hesitated because the sentence never
  says "imported *by app*", and `ai` is genuinely imported *somewhere*. Booked because the falsifiable
  half ("pulled at Docker build via `GH_PAT`/`GOPRIVATE`") is a build-mechanism claim a reader debugging
  a `GOPRIVATE` failure would act on, and it is false for one of the four named rows.

### B5 — `clerkenstein.md`'s currency header contradicts the file's own body and its git history

- **Anchor:** `corpus/services/clerkenstein.md:3` —
  > `**Status:** v0.3 (v1.0 "body double" · M1 + M2 + M2b + M2c \`@clerk/express\` + M3 deploy/injection; … v2.3 "cue to cue" M217 self-healing demopatch gate + **M218 the roster-aware fake BAPI**) · **Last updated:** 2026-07-14`
- **Why it is false:** The file's own body documents work **after** M218 and after that date —
  `:54-62` ("✅ RESOLVED at M219"), `:305-312` ("M220 closed it"), `:232-255` ("v2.4 'casting call'
  M224"), `:160-209` ("documented v2.8 M256 pre-flight", "D81, v2.8 M256 iter-16/iter-25"), and
  `:274` ("v2.8 M257x iter-23"). And the repository refutes the date directly:
  `git log -1 --format='%h %ad' --date=short -- corpus/services/clerkenstein.md` → **`328ece5
  2026-08-05`** (the M257x/81 repair pass), with `b925199` 2026-08-02 before it. A three-week-stale
  currency line sitting above content that cites milestones a full release later tells a reader the
  page has not been touched since M218, which is the exact judgement the line exists to support.
  This is the self-contradiction class the briefing calls the cheapest and least-checked.
- **Confidence:** medium — metadata rather than mechanism, which is why I hesitated; booked under the
  instrument's tie-break because it is a corpus claim falsified by the corpus's own history and by the
  same file's body.

---

## Minors

- `corpus/services/clerkenstein.md:21` — the monorepo section enumeration reads
  `(\`clerkenstein\`, \`demo-stack\`, \`stack-injection\`, \`stack-core\`, \`stack-seeding\`, \`alignment\`)`;
  the pinned clone `09d06070` (and the authoring copy) actually carry **12** top-level dirs —
  the six named plus `dev-stack`, `stack-secrets`, `stack-snapshot`, `stack-verify`, `playthroughs`,
  `knowledge`. Omitted list members, non-material (root `CLAUDE.md` lists a different, also-partial 9).
- `corpus/architecture/frontend_architecture.md:29-35` — the "Core Packages" table omits
  `packages/design`, a real workspace dir at `8297c684`. Curated subset, harmless.
- `corpus/architecture/frontend_architecture.md:93` — "regenerated from the upstream subgraph
  **schemas**" (plural) where `packages/graphql/codegen.ts:9` introspects the single `backend`
  endpoint; the same file states the singular correctly at `:32`. Phrasing only.
- `corpus/services/messenger.md:120` — "the binary still constructs **four** Connect-RPC clients";
  `cmd/root.go:118-142` constructs **five** client objects (`cmsV1Client`, `usersClient`,
  `organizationsClient`, `skillerClient`, `jobsimulationClient`). The sentence's own enumeration names
  all five and groups users+organizations (they share one env var, `BACKEND_USERS_RPC_ADDR`), so the
  set is right and only the scalar is loose. Non-material.

---

## Positively cleared (audited zeros)

Labelled per the briefing. **ENUMERATED** = I derived the predicate's set from source independently and
compared cardinalities. **RE-DERIVED** = I only re-computed arithmetic the document displays (the weakest
clearance).

**ENUMERATED**

1. **Subgraph ladder** (`graphql-wundergraph.md:41-46`, `service_taxonomy.md:407`) — counted
   `^  - name:` in `supergraph-config-prod.yaml` at each named commit *and* its parent, in
   `graphql-wundergraph` `60c229f3`: `749dc86~1` = **5** (backend, skiller, jobsimulation, cms,
   skillpath) → `749dc86` (2026-06-24) = **4** (skiller gone) → `7c17e63` (2026-07-21) = **3**
   (skillpath gone) → `915da06` (2026-07-29) = **1**. `git show --stat 915da06` marks
   `schemas/cms.graphqls` **and** `schemas/jobsimulation.graphqls` deleted in that one commit, and its
   subject really does say *"supergraph 2→1"* — the corpus's "3→1, and the subject line is wrong" is
   exactly right.
2. **Compose service set and profile floor** (`service_taxonomy.md:65-67`, `:119-129`, `:481-490`) —
   `docker-compose.yml` @ `0c91421` declares **5** services (`sentinel:5`, `backend:28`,
   `studio-desk:112`, `next-web-app:143`, `gotenberg:170`); `common.yml` adds `postgresql:2` +
   `redis:24` → **7** effective. Exactly **4** carry a `profiles:` key — `backend:110`,
   `studio-desk:141`, `next-web-app:168`, `gotenberg:183` — and `sentinel` carries none, as do
   `common.yml`'s two. `core` therefore starts **5**. Every line number verbatim.
3. **`repos.yml` = 4 entries** @ `0c91421` (`app`/`sentinel`/`next-web-app`/`studio-desk`), `app` at
   `:14-17` with `migrations: true`/`schema: public`, `sentinel` at `:18-20` with `migrations: false`
   — and **9** entries at `2adcf71` (`frontend_architecture.md:11`, `service_taxonomy.md:62`).
4. **Zero `*_RPC_ADDR` in the platform** (`service_taxonomy.md:425`, `messenger.md:30`, `:120`,
   `:168-172`) — `grep -c '_RPC_ADDR'` = **0** in `docker-compose.yml`, `common.yml` **and**
   `.env_example` @ `0c91421`; positive control `AUTHORIZATION_ADDRESS` = 1 in the same file.
5. **`MESSENGER_RPC_ADDR` occurs in no repo** (`messenger.md:20-27`) — exact-string `git grep` = 0
   across all 15 trees at their own refs, nested `studio` repos included. Positive control
   `SKILLER_RPC` in the same pass: app 26, jobsimulation 9, platform 1, messenger 4, cms 12,
   rext 20. The document's own historical control reproduces exactly:
   `platform log -S 'MESSENGER_RPC' 0c91421d` = **0** over **121** commits (`rev-list --count` = 121);
   `-S 'SKILLER_RPC'` = **7**; with `--all`, 8 and 0, the 8th being **`464dfe3`** on a non-main branch.
6. **Messenger flow handler count** (`messenger.md:17`) — `internal/flow/flow.go:72-104` @ `fa47850d`
   contains **22** `pubsub.EventHandler(` lines, of which exactly one — `:83`,
   `OrgJobSimulationAssignmentPastDueHandler` — is commented out `// not implemented` → **21** live,
   on the `backend` subscriber (`AddSubscriber("backend", …)` at `:72`). The `OrgSkillPath*` five are
   at `:74-78` as cited.
7. **Alignment DNA gene/capability counts** (`clerkenstein.md:41-44`, `:74`, `:106`) — parsed the five
   JSONs in the **pinned** clone `09d06070` (identical in the authoring copy): `clerk-2.6.0` = **14
   capabilities / 27 genes**; `clerk-deploy-1` = 3 / **7**; `clerk-express-1` = **5 / 13**;
   `clerk-js-5` = 6 / **9**; `clerk-multi-1` = 5 / **9**. Every figure the page publishes.
   `clerk-js-5.json` has a `Me` capability and no `organization_memberships` gene (that substring
   occurs only in `clerk-2.6.0.json`), matching the "not yet a measured gene" callout at `:145-147`.
8. **`r.Cookie(` is called nowhere in `clerkenstein/`** (`clerkenstein.md:173`) — 0 hits in the pinned
   clone; positive control `Cookie` = 7 hits in the same tree.
9. **`authn` is imported by no service** (`service_taxonomy.md:509`) — `anthropos-work/authn` = 0 in
   all seven Go clones' `go.mod`; control `anthropos-work/colony` = 1 in all seven. The document's
   "0 hits across all seven Go clones; control — colony is required by all seven" is exact.
10. **Academy SDL is 1 of 43** (`service_taxonomy.md:286`) —
    `git ls-tree HEAD internal/web/backend/graphql/graph/schemas/` @ `app` `ad9f3c49` = **43** files,
    exactly one named `academy.graphqls`. All three `supergraph-config-*.yaml` @ `60c229f3` carry a
    single `- name: backend`, and `schemas/` holds only `backend.graphqls`.
11. **`next-web-app` REST surface** (`frontend_architecture.md:39-50`) — the whole re-derivation
    reproduces at `8297c684` over non-test `.ts`/`.tsx` with `e2e/` excluded: **22** files naming
    `NEXT_PUBLIC_BACKEND_API_URL`, **31** occurrences, **47** `fetch(` call sites in those files, of
    which the four `packages/core-js` clients contribute **12 env / 27 calls** (coursebuilder 3/20,
    credits 3/3, talkToData 3/3, workforce 3/1). The 22 files map one-for-one onto the prose's own
    itemisation. `new EventSource(` = **0** across all 15 trees at their own refs — and I checked the
    addendum's NUL trap directly: `apps/web/src/hooks/useCoursebuilder.ts` does carry exactly **1** NUL
    byte (`tr -dc '\000' | wc -c` = 1) and is therefore skipped by both `grep -I` and `git grep`, but a
    `grep -a` on it returns 0 for `EventSource`, 0 for `NEXT_PUBLIC_BACKEND_API_URL` and 0 for `fetch(`,
    so it changes none of the four numbers. `packages/core-js/src/talkToData/api.ts:214` reads exactly
    *"uses POST so we can send a JSON body — that rules out EventSource"* (in a docstring at `:212-219`).
    No `connectrpc`/`grpc-web` anywhere in the repo; control `graphql-request` = 36 files.
12. **jobsimulation M810 landed** (`service_taxonomy.md:159`) — the anchor is graded at the ref the
    claim names. `6092c6d2` (2026-08-04, *"remove the jobsimulation ECS service and ECR repository
    (M810)"*) is **not** an ancestor of the checkout `462343b0` but **is** an ancestor of
    `origin/main` `82cb66ec`; at `6092c6d2` the file is 56 lines, the `module "jobsimulation"` block is
    gone, and `:15-40` is precisely the decommission comment naming livekit.tf / chime.tf / ssm.tf and
    the surviving atlas tracker. At the checkout the module block is still present at `:31` with
    `service_desired_count = 0` — which is why the pin matters and why the corpus is right to carry it.
13. **cms M810 is genuinely two-sided** (`service_taxonomy.md:160`) — `cms/terraform/main.tf` is
    **191** lines with `module "cms"` at `:30` and `service_desired_count = 0` at `:39`, at **both**
    `ca50c817` and `origin/main` `f38c0c4a`; and `6efa1d5` (2026-08-04) deletes
    `.github/workflows/build-production.yml` (18 lines) with a body stating M810 deletes
    `module.cms_euwest1` and destroys the ECS service and the production-cms ECR repository.
    `f38c0c4` is the merge, 2026-08-04. Both halves true, exactly as the row says.
14. **messenger prod/local split** (`messenger.md:40-52`) — the "which tree settles which row" note is
    correct and checks out both ways: at the demo clone `fa47850d`, `terraform/main.tf:19` reads
    `service_desired_count = 1` and there is no `:29` declaration; at `origin/main` `e9421c68`
    (**7** commits ahead, `rev-list --count` = 7) `:29` reads `= 0` with the rollback-path comment at
    `:27-28`. Grading the prod row at the checkout does invert the verdict.
15. **`ARCHIVED` / archive-state discipline** (`service_taxonomy.md:142-151`) — the row numbers it
    self-cites are exact: `:157` Skiller, `:158` Skillpath, `:159` Jobsimulation. `gh` is not on this
    host and the repos are private, so the note's "no clone can measure it" is itself correct.

**Also ENUMERATED / verbatim-verified, listed compactly** (each opened with `sed`-style context, not
grepped-to-a-line): `docker-compose.yml` `:18` search_path=sentinel · `:48` AUTHORIZATION_ADDRESS ·
`:53` DIRECTUS_PUBLIC_BASE_ADDR · `:56` ENVIRONMENT=development (still `:56`, also `:56` at `0dab54d`) ·
`:57` GOTENBERG_URL · `:59` JUDGE0_BASE_URL · `:84-92` the messenger/customerio rationale comment ·
`:119`/`:135` studio-desk build-arg + env · `:151`/`:160` next-web build-arg + env · `:152`/`:161`
BACKEND_API_URL build-arg + env · `:141`/`:168`/`:183` profiles · `common.yml:2`/`:24` +
`bitnamilegacy/redis:latest` + pgvector in `postgresql/Dockerfile` · `Makefile:10 PROFILE ?= core` ·
`grep -c 5050 docker-compose.yml` = **0** · `0dab54d:docker-compose.yml:154 profiles:
[customerio-sync, all]`, `:197`/`:226` studio-desk, `:166-167`/`:177`/`:181`/`:195` messenger ·
`a2a3ee6^:docker-compose.yml` `:384 directus/directus:10.10.1`, `:386 8055:8055`, `:409
ADMIN_PASSWORD=password`, and `admin@example.com` genuinely unfound · `app` `env_guards.go:37-44`
deployedEnvironment, `:61` MESSENGER_ENABLED, `:92-111` resolveSubsystemSwitch (and `env_guards.go`
really is **absent** at `b948604f`) · `app/main.go` `:15`/`:62`/`:63` imports, `:295`/`:296` BREVO_KEY
fatal, `:394` customerIOSyncEnabled, `:1416-1421` consumer-group literal, `:1450`/`:1471`/`:1473`,
and `diff --stat 2035f9a4 ad9f3c49 -- main.go` **empty** · `app` `3e5bc33ef:main.go:387` the
pre-switch gate · `app/main.go:1095` CMS_STREAM publisher and `:1478-1484` the stream-name struct
**@ `9d00a313`** (both are different constructs at `ad9f3c49`, where they sit at `:1149`/`:1539` —
the pin is doing real work) · `app/main.go:1205-1211` @ `b948604` the DORMANT comment ·
`app/internal/converter/gotenberg.go:31` · `cms_queries.graphqls:106 studioTask` ·
`internal/cms/studio/studioManager.go:119` execs `studio/gen.py` ·
`gqlauthz.go:186` the `unknown viewer` response + both named regression tests ·
`app/.github/workflows/build-production.yml:29 additional_repo: "anthropos-studio-room:studio"` ·
`app/go.mod` `clerk-sdk-go/v2 v2.7.0` (so the "not v2.6.0" note holds) · studio-room @ `aeec036a`:
`services/ai.py:705-708` the 3-provider registry, `ai.py:1-2`, `tools/pdf2md.py:24 from mistralai`,
`git grep -ci mistral` = **22 hits / 3 files**, and `gen.py` registering exactly **nine**
`add_argument` calls (`:484-492`) with `parse_known_args` at `:19` · studio-desk `41ee3575`:
**0** react/vue/angular entries in `package.json`, **0** `.tsx`/`.jsx` (control: 71 `.ts`), 7 language
flags, `STUDIO_ACCESS_ROLES` at `src/index.ts:96` **and** `app/services/userService.ts:16` with the
quoted comment · ant-academy `22df69dd`: `code/src/graphql/server.js:14,18`, `backendContent.js:36`
and `:102-103`, `serverTenant.js:115-145` (incl. the *"NO FS-as-published fallback … not
reversible-on-error"* text) and `:145`, `beacon/route.js:36` and `:41-55`,
`next-scaffold.test.js:106,111`, `react-compiler-config.test.js:41`, `layout.jsx:132 manifest`,
`academy-manifest.json display: standalone`, no serwist/workbox, port 3077 / 8555, Expo `~54.0.33`,
`NEXT_PUBLIC_FEATURE_TRAINING_COACH` default OFF, `gpt-5.2` · graphql-wundergraph `60c229f3`:
`package.json` = `{"name":"graphql-wundegraph"}` (misspelling real), router image
`ghcr.io/wundergraph/cosmo/router:0.275.0`, `wgc@0.104.0` on `node:22.11-alpine`,
`federation_version: =2.3.2`, `terraform/main.tf:20 = 1`, `locals.tf:8 port = 8080`,
`main.tf:48-49` 8080→8080, `config.prod.yaml:5 listen_addr` + `:13 graphql_path: /graphql` +
playground/introspection false (true in `config.compose.yaml`), `subgraphs.conf` = a single
`BACKEND=v1.360.0`, `ci/update-subgraph.sh:9` = **exactly one** `gh release download -R
anthropos-work/app`, **no** `subscription:` block in any supergraph config, **no** `type Subscription`
in `schemas/backend.graphqls` (Query + Mutation present), `bba862f` reachable **only** from
`remotes/origin/feat/use-web-socket`, tests only under `terraform/tests/`, `Makefile` targets `run` /
`updatesubg`, `supergraph-update.yml` on `workflow_dispatch`, and the historical routing URLs
`http://jobsimulation:8400/query` / `http://cms:8090/query` verbatim at `915da06~1` ·
rext pinned `09d06070`: `alignment/cmd/alignctl/run.go:134-135` = `ExitRegressed = 2` /
`ExitUnmeasurable = 3`, `clerkenstein/clerk-backend/store.go:138 SeedOrgIdentity` and `:151
LookupOrgEid`, `clerkenstein/alignment/dna/clerk-2.6.0.json:131` containing *"M219 landed the fix …
taking the Go surface 97.2% -> 100%"*, five DNAs, five `cmd/*run` runners,
`clerkenstein/alignment/scripts/{gate,drift-check,drift-test}.sh` with `ALIGN_DIR` defaulting to
`$base/../../alignment` and the rext `alignment/` section carrying **no** `scripts/` dir,
`clerkenstein/.github/workflows/alignment.yml` tracked, `clerk-frontend/registry.go` `activeKey` /
`active()` / `Select()` / `org_name` / `org_slug` / `org_is_hiring` / `toDemoUser`,
`clerk-frontend/server.go` `clientID: "client_clerkenstein"` (`:125`), `sessID = "sess_clerkenstein"`
minted in `establishLocked` (`:663-670`), the `signedOut` sticky flag, `handleClient`/`handleToken`/
`handleMe` taking `_ *http.Request`, `handleSignOut` ignoring its param, `:35-68` the M220 bounded
clerk-js block (`clerkJSFetchTimeout = 15 * time.Second`, `clerkJSClient` commented *"Explicitly NOT
http.DefaultClient"*, `FAKE_FAPI_CLERKJS_CACHE`), all **five** named `server_test.go` tests present,
`meorgmemberships_test.go` present, `clerk-frontend/key.go:28 MintPublishableKey`,
`clerk-frontend/resources.go:255 orgMemberships()` with the conditional `if u.OrgIsHiring` emit and
the `orgNameDefault = "Clerkenstein Demo Org"` / `orgSlugDefault = "clerkenstein-demo"` fallbacks,
`stack-seeding/seeders/roster.go` producer fields + `BuildRoster` + `orgSlugFor`,
`ResolvedStory.IsHiringOrg()`, `demo-stack/patches/academy-fs-published-fallback` present, and
`directus/directus:11.6.1` as the per-stack image · platform commit identities all correct
(`b56d731`+`360efd4`→`2adcf71` 2026-07-31; `d11a403` 2026-08-03; `0dab54d` 2026-08-03;
`838d907`→`0c91421` 2026-08-05) · `roadrunner/terraform/main.tf:19 service_desired_count = 1`
("orphaned, not absent") · messenger `go.mod` Go 1.25 / brevo-go v1.1.3 / liquid v1.8.1,
`cmd/root.go:63`/`:64`/`:107`/`:118-142`/`:147`, `rpcsrv.go:25-30`,
`internal/flow/jobsimulations.go:140-151` (2 h / 12 h guards), `internal/flow/assignments.go:828`
inside `getSkillPath` at `:827`, and the three named flow test files.

**RE-DERIVED only (weakest clearance — flagged as such)**

- `frontend_architecture.md:41-50`'s `bb3313bc` column (21 / 29 / 43 / 12-env-25-calls). I re-derived
  the **`8297c684`** column from source (see ENUMERATED #11) but only spot-checked the historical
  column, since the addendum's ground truth is the newer ref.
- `clerkenstein.md:41-44`'s *scores* (100% / 100% per surface). I enumerated the gene and capability
  **counts** from the DNAs; I did **not** run `alignctl`, so the percentages are re-derived from the
  document's own arithmetic (`27/27`, `9/9`, `7/7`) rather than measured.

---

## What I could not settle

1. **`corpus/services/db-backup.md` in its entirety (31 lines).** There is no `db-backup` clone in the
   ground-truth set — `find stack-demo -name .git -maxdepth 4` returns 15 trees and none is it — and
   the token appears nowhere in `platform` (`grep -ci db-backup` = 0; control `backup` = 0 in platform,
   317 in `app`). So the 6-hour schedule, the S3/Azure/Hetzner triple target, "Go", "Docker (scheduled,
   internal)", "PostgreSQL RDS (all schemas)" and the RDS-PITR framing are **all unverifiable from this
   tree**. The file was last touched 2026-03-02 (`f075ba9`) — five months, across the entire
   consolidation programme — and it is the only assigned file the recent repair passes never opened.
   Two smaller internal tensions I can flag but not adjudicate: it says "all schemas" while `repos.yml`
   @ `0c91421` and `service_taxonomy.md:62` say the application data is now one schema (`public`) plus
   `sentinel`; and `service_taxonomy.md:512` calls its deployment an "ECS scheduled task" where
   `db-backup.md:18` says "Docker (scheduled, internal)". **What would settle it:** cloning
   `anthropos-work/db-backup` (its `terraform/`, its cron/schedule definition, its uploader targets),
   or reading `infrastructure`'s `services.tf`.
2. **Production-side router state** (`service_taxonomy.md:3`, `:396`, `:404`;
   `graphql-wundergraph.md:7`; `frontend_architecture.md:39`, `:55`). `terraform/main.tf:20 = 1` is
   measured and true *in the repo*, but whether that module is still **applied** in production is not
   visible: `infrastructure` has never been in a clone set. The corpus is appropriately careful here
   (it says "still declared"), and the `:8080` / "never `5050` in prod" claims are fully sourced —
   only "the router is still running" is unmeasurable. Same limitation the `cms` M810 row states
   about itself.
3. **GitHub archive state** for `graphql-wundergraph` (2026-07-30), `skiller` (2026-07-01) and
   `skillpath` (2026-07-31). `gh` is not installed on this host and the repos are private, so the
   anonymous REST path is closed too — exactly as `service_taxonomy.md:142-147` discloses. The
   `graphql-wundergraph` clone is at least *consistent* with its assertion: no commit after
   2026-07-30 (`60c229f3` is dated 2026-07-30). I did not check the other two clones' post-date
   commits, since neither repo is in the clone set.
4. **Taxonomy cardinalities** (`service_taxonomy.md:174`: ≥42,790 skills / ≥22,470 job roles) — a
   database measurement, not derivable from any clone. Cross-referenced to
   `shared_libraries.md#taxonomy-figures`, which is out of my file set.
5. **`service_taxonomy.md:62`'s "the `cms`, `jobsimulation` and `skillpath` schemas are legacy husks"**
   — requires a live DB. The *terraform* side corroborates it for `jobsimulation` (its atlas tracker
   at `6092c6d2:terraform/main.tf:44-56` still targets `search_path=jobsimulation`, and the comment at
   `:38-40` says dropping the schema is a separate, pending M810 step), but "husk" is a claim about
   contents.
6. **`clerkenstein.md`'s five alignment SCORES as currently-true** (see the RE-DERIVED note). The page
   itself says the `clerk-deploy-1` 7/7 should be re-run against `v0.35.2` before being quoted as
   current — and B2 above shows the platform has moved *further* since that caveat was written, so
   the caveat now applies more broadly than the page states.

---

**Summary:** BLOCKERS = 5, MINORS = 4.
