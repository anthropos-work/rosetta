# Seat F — reading #29/#30 (M257x iter-116)

## Ground truth actually used (re-verified at this reading's open)

| repo | path | sha read |
|---|---|---|
| app | `stack-demo/app` | `ad9f3c49` (checkout == origin/main); also read at pinned refs `b948604f`, `9d00a313`, `2035f9a4` |
| app/studio (nested, own checkout) | `stack-demo/app/studio` | `aeec036a` |
| platform | `stack-demo/platform` | `0c91421d`; also read at `0dab54d`, `2adcf71`, `d11a403`, `838d907` |
| next-web-app | `stack-demo/next-web-app` | `8297c684` |
| sentinel | `stack-demo/sentinel` | `f2c46190`, `88bc5592` |
| storage | `stack-demo/storage` | checkout `4ce8ece5`; pinned reads at `9f8cb53` (= origin/main) |
| messenger | `stack-demo/messenger` | `fa47850d` |
| cms | `stack-demo/cms` | `ca50c817` |
| graphql-wundergraph | `stack-demo/graphql-wundergraph` | `60c229f3`, `915da06` |
| roadrunner | `stack-demo/roadrunner` | `87d8d443`, `84a4b4f`, `e45eb61` |
| jobsimulation | `stack-demo/jobsimulation` | checkout `462343b0`; pinned reads at `6092c6d2` (ancestor of origin/main `82cb66ec`, **not** of the checkout) |
| studio-desk | `stack-demo/studio-desk` | `41ee3575` |
| ant-academy | `stack-demo/ant-academy` | `22df69dd` — **working tree is dirty** (4 files modified by an applied demo-patch); every claim graded via `git show 22df69dd:<path>`, not the tree |
| rosetta-extensions (per-stack, pinned — settles "what the tooling does on a stack") | `stack-demo/rosetta-extensions` | `09d06070` |
| rosetta-extensions (authoring copy) | `.agentspace/rosetta-extensions` | `1dc1eb82` — read only to confirm the two rext trees agree on `demo-stack/ant-academy.sh` (they are byte-identical over the cited range) |

## Positive controls

| file | `wc -l` | read in full? |
|---|---|---|
| `corpus/services/ant-academy.md` | 503 | yes |
| `corpus/architecture/architecture_overview.md` | 435 | yes |
| `corpus/services/storage.md` | 248 | yes |
| `corpus/services/roadrunner.md` | 216 | yes |
| `corpus/architecture/dependency_map.md` | 103 | yes |
| `corpus/services/TEMPLATE.md` | 46 | yes |

Search positive control: `grep -c "Storage" corpus/services/storage.md` → 6 (non-zero), run in the same pass as the six-token absence test used for B-clearance of `dependency_map.md:19`.

---

## Blockers

### B1 — ant-academy is described as an INTERNAL-ONLY portal for `@anthropos.work` employees, which the same file retracts 443 lines later and which source refutes

- **Anchor:** `corpus/services/ant-academy.md:31` —
  > "**Primary Goal**: Internal-only learning portal that delivers AI-engineering chapters to `@anthropos.work` employees"

  Co-anchors, same file, same proposition:
  - `:5` — "**Ant Academy** … is the **internal learning portal** for Anthropos employees. It delivers micro-chapters … **to anyone with an `@anthropos.work` email**."
  - `:298` — "`@anthropos.work` domain restriction is enforced in the Clerk app."

- **Why it is false:** `ant-academy.md:474` — in this same document — already books the proposition as false and cites the measurement:
  > "⚠️ **'Domain-gated to `@anthropos.work` so external users cannot enter' is FALSE and was removed at M257x iter-115.** … A product that sells a $399/yr subscription to an anonymous visitor through a public checkout funnel is not one external users cannot enter. **This document contradicted itself 213 lines earlier** …"

  I re-derived :474's evidence independently at `ant-academy` `22df69dd` and every item holds:
  - `code/src/lib/pricing.js:21` — `export const STANDARD_YEARLY = { usd: 399, eur: 349 };`
  - `code/src/lib/platformUrls.js:4` — *"The Academy is a storefront in front of the Anthropos platform"*; `:17-21` **FLOW B — Checkout gate**, *"Checkout contextually registers → signs in → subscribes **for an anonymous visitor**"*
  - `code/src/components/TopBar.jsx:77` `{anonymous ? (` → `:85` `{t('topBar.buyAcademy', 'Buy AI Academy')}` and `:93` `{t('topBar.logIn', 'Log in')}` — a purchase CTA rendered **to anonymous visitors**
  - `code/src/lib/schema.js:3` — `const SITE_URL = 'https://aiacademy.anthropos.work';`
  - `knowledge/user-types.md` — the four user types are **Anonymous · Signed-in (free) · Subscriber · Enterprise/Org member**, detected by `auth().userId`, `isInOrg()`, `billingPremium()`. I read the whole "How each type is detected" block: **no `@anthropos.work` predicate appears anywhere in it.**
  - `code/proxy.js:112-181` — the `isPublic` matcher (enumerated in full, 25 static patterns + 2 dev-only groups) makes `/`, `/courses`, `/courses/(.*)`, `/chapters/(.*)`, `/latest(.*)`, `/library`, `/free` anonymous-reachable.

  So the surviving sentences are false against source, **and** self-contradictory against `:474`, `:316-317` (*"Phase-1 public launch"*), `:298`'s own *"The public surface is much wider than 'a few auth pages'"*, and `:262`'s literal phrase *"public-storefront header"*. This is the rule-19/rule-40 half-repair class: iter-115 repaired the **Integration Points** bullet and left the summary, the Role & Responsibility bullet and the Auth row standing.

  On `:298` specifically: the Clerk *dashboard* config is not measurable from any clone, so I cannot falsify "enforced in the Clerk app" directly — but the repo ships an anonymous→checkout→**Subscriber** path with no domain predicate anywhere in its own detection list, which makes the assertion unsupportable from the code and squarely contradicted by `:474`. I book it as a co-anchor rather than a separate finding.

- **Confidence:** high (for `:5` / `:31`); the `:298` clause is medium because the deciding artifact is SaaS config outside the clone set.

### B2 — the same false "internal learning portal for `@anthropos.work` employees" claim, un-retracted, in `architecture_overview.md`

- **Anchor:** `corpus/architecture/architecture_overview.md:40` —
  > "**Ant Academy** (`ant-academy`): Internal learning portal (Next.js 16 + Expo mobile) for `@anthropos.work` employees."

  Co-anchor: `:260` — "| **Ant Academy** | Next.js 16 + Expo | Internal learning portal for `@anthropos.work` employees (standalone, Vercel-deployed) |"

- **Why it is false:** identical evidence to B1 (`pricing.js:21`, `platformUrls.js:17-21`, `TopBar.jsx:77-93`, `schema.js:3`, `knowledge/user-types.md`, `proxy.js:112-181`, all @ `ant-academy` `22df69dd`). Booked separately from B1 because it is a different file with a different owner, and because a repair that fixes only `ant-academy.md` reproduces exactly the cross-file drift `platform-alignment.md` §5 rule 19 measures (5 of iter-39's 8 self-inflicted defects were of this shape). For the repairer's benefit, the same predicate also survives at `corpus/README.md:55`, `corpus/architecture/service_taxonomy.md:290`, `corpus/architecture/frontend_architecture.md:9`, `corpus/ops/setup_guide.md:385` and root `CLAUDE.md:265` — enumerated by `grep -rn` over `corpus/` + `CLAUDE.md`, all outside my assigned set and reported here only so the sweep is scoped by predicate rather than by partition.
- **Confidence:** high.

### B3 — `roadrunner.md` cites its own `:124` for a sentence that is at `:134`; `:124` is the WebSocket-LSP bullet

- **Anchor:** `corpus/services/roadrunner.md:130` — "… — *\"NOT stream handlers.\"* Consistent with `:124` below (*\"Upstream consumers: none (orphaned)\"*)."
- **Why it is false:** measured in the file itself. `roadrunner.md:124` reads *"The repo contains an experimental WebSocket LSP proxy (`internal/lsp/lsp.go`) that is NOT wired into any running server — there is no reachable LSP endpoint today."* The quoted sentence — "**Upstream consumers**: **none (orphaned — see the banner at the top).**" — is at `roadrunner.md:134`. The anchor resolves to a real line that names the wrong construct: the §5 rule-21 class (*"an anchor that resolves but names the wrong construct"*), produced by rule 34's mechanism (a repair that changed the file's line count without re-deriving the intra-corpus line map).
- **Why I hesitated:** the quotation is inline, so a reader who follows the pointer and finds the LSP bullet can recover in seconds; blast radius is small. The briefing's grading rubric names "wrong line anchors that point at the wrong construct" explicitly under BLOCKER and instructs me to resolve doubt upward, so I book it rather than demote it.
- **Confidence:** medium.

---

## Minors

- `corpus/services/roadrunner.md:13-14` — doubled word across the line wrap: *"still reads `service_desired_count = 1` and **has** / **has** not been touched since `84a4b4f`"*. The claim itself verifies (see clearances).
- `corpus/services/ant-academy.md:304` — "~26 Playwright e2e spec files (tests/e2e/)". Measured at `22df69dd`: **31** `*.spec.js` files under `code/tests/e2e/` (a `git ls-tree -r` returning 38 paths, 7 of which are `widgets.spec.js-snapshots/*.png`). Hedged with "~" and non-load-bearing, so MINOR rather than BLOCKER.
- `corpus/architecture/architecture_overview.md:348-360` — the blockquote opened at `:346` drops its `> ` prefix for thirteen lines and resumes at `:361`. Markdown lazy continuation keeps it inside the quote (there is no intervening blank line), so it renders correctly; formatting damage only, no factual consequence.
- `corpus/architecture/dependency_map.md:19` — cites `docker-compose.yml:126-131` for storage's `depends_on` at `0dab54d`. The `depends_on:` block is `:126-130`; `:131` is the first line of the `profiles:` rationale comment. Off-by-one at the range's tail; the seven-variable env claim it serves is exactly right (see clearances).
- `corpus/services/storage.md:25` — cites `storage/terraform/main.tf:13` for *"`prevent_destroy` … read from configuration"*. At `9f8cb53` `:13` opens that paragraph (*"The module deliberately still exists rather than being deleted wholesale…"*); the quoted phrase is at `:15`. Paragraph-level anchor rather than sentence-level.

---

## Positively cleared (audited zeros)

Labels are as the briefing defines them. **ENUMERATED** = I derived the predicate's set from source independently and compared cardinalities. **RE-DERIVED** = I only re-computed arithmetic the document displays (the weakest clearance).

### `corpus/architecture/architecture_overview.md`

- **ENUMERATED — "31 of 135 schemas auto-filter by organization" (`:382-385`).** This is the claim §5 rule 24 records three seats getting wrong, so I derived the SET, not the sum. `internal/data/ent/schema/` @ `ad9f3c49` holds **139** `.go` files; **4** contain no `ent.Schema` (`database_types.go`, `mixin.go`, `skiller_mixins.go`, `skillpath_mixins.go`) → **135** schemas. `git grep -n 'OrganizationMixin{}'` returns **30** lines, of which `user_resource.go:22` is `// OrganizationMixin{},  // We need to work on this` — commented out → **29 live**. Schemas declaring their own `Policy()`: `Membership` (`org_membership.go:172`, org-scoped via `AllowCurrentOrgEdgesOrSkipRule`), `Organization` (`organization.go:56`, `rule.FilterSameOrganizations()`), and `User` (`user.go:116`) — **User filters by USER** (`rule.FilterSameUserRule()`), not org, so it correctly does not count. 29 + 2 = **31**. Every figure holds.
- **ENUMERATED — "23 schemas carry an `organization_id` with no policy at all (16 is the neither-mixin subset)" (`:386-387`).** Union of `field.UUID|String("organization_id")` declarers and `OrganizationIDMixin{}` users = 52 files; minus the 31 policed = 25; minus `skiller_mixins.go` (a mixin-definition file, not a schema) = **24** schemas carrying an `organization_id` and not org-policed. Of those, **7** carry `OrganizationIDMixin{}` (`category`, `jobrole`, `similarity`, `skill`, `specialization`, `studio_document`, `studio_task`) and **17** carry neither mixin; of the 17, exactly **one** (`academy_feedback.go:64`) carries a bare `UserMixin{}`, which *does* declare a `Policy()` (`mixin.go:99`) → 16 with no policy of any kind. 16 + 7 = **23**. ⚠ Method note that changed my verdict: a first pass matching `UserMixin{}` as a substring falsely flagged `category/jobrole/skill/specialization` because they carry **`CreatedByUserMixin{}`**, which has no `Policy()`. Anchoring the match at line-start reversed a would-be blocker into a clearance.
- **ENUMERATED — "0 hits for `github.com/anthropos-work/authn` across all seven Go clones, against a positive control of `colony` required by all seven" (`:80`).** Measured across `app`/`sentinel`/`storage`/`messenger`/`cms`/`roadrunner`/`jobsimulation` `go.mod` at each clone's own ref: authn **0/7**, colony **7/7**.
- **ENUMERATED — the Next.js version parenthetical (`:59`).** At `next-web-app` `8297c684`, `"next": "~16.2.12"` in exactly `apps/web`, `apps/hiring`, `apps/integration`, `apps/maintenance`; `apps/mobile/package.json` declares no `next`; `pnpm-lock.yaml` resolves `next@16.2.12`. Tilde, not caret, in all four.
- **ENUMERATED — "five containers" / the always-on floor (`:12`, `:71`, `:156-157`, `:183-186`).** `docker-compose.yml` @ `0c91421` declares 5 services; only `backend` (`:110`), `studio-desk` (`:141`), `next-web-app` (`:168`) and `gotenberg` (`:183`) carry a `profiles:` key. `common.yml` declares `postgresql` + `redis`, neither with `profiles:`, and `sentinel` (`docker-compose.yml:5`) has none — so the floor is exactly those three. `Makefile:10` is `PROFILE ?= core`. `studio-desk` `depends_on: backend` at `:138-140` and `next-web-app` at `:165-167`, neither selected by its own profile.
- **ENUMERATED — every compose anchor in the Communication-Patterns block (`:266-284`).** `:48` `AUTHORIZATION_ADDRESS=http://sentinel:8087`, `:57` `GOTENBERG_URL=http://gotenberg:3200`, `:59` `JUDGE0_BASE_URL`, `:66` `REDIS_ADDR`, `:84-92` the messenger/customerio rationale comment, `:183` gotenberg's `core` profile. `grep -c "_RPC_ADDR"` over `docker-compose.yml` + `common.yml` @ `0c91421` = **0**, so the "zero `*_RPC_ADDR` variables" half holds and the retraction of "exactly one service address" is correct.
- **ENUMERATED — the storage retraction block (`:346-373`).** `STORAGE_RPC_ADDR` in Go at `9d00a313`: exactly **3**, all comments (`main.go:451`, `internal/jobsimwiring/wiring.go:101`, `internal/storagens/callsites_test.go:189`); repo-wide at that ref **29 lines across 18 files** — both figures exact. `main.go:504` @ `ad9f3c49` is the *"the standalone service takes no traffic and STORAGE_RPC_ADDR is gone"* comment; `main.go:451` @ `9d00a313` is the same comment, so the "line moved without the code moving" claim is right. `storage/terraform/main.tf` @ `9f8cb53` is **18** lines with `:9-11` and `:13-16` reading verbatim as quoted. `internal/jobsimwiring/wiring.go:115` @ `b948604` is the live `getenv("STORAGE_RPC_ADDR")` read — the pinned counter-example holds.
- **ENUMERATED — the AI-routing anchors (`:306-317`).** @ `ad9f3c49`: `internal/jobsimulation/ai/ai.go` `getClient` at `:259` defaults `azureClientEu` and swaps `azureClientUs` on `flag_use_azure_us` (within the cited `:262-276`); `isThrottlingError` at `:129`, applied at `:166` and `:325`; `config.WithRegion("eu-west-1")` at `:88` (within `:85-88`). `internal/cms/studio/markdownManager.go:29-30` builds `mistralocr.New(aiKey)`; `studioManager.go:583` is `NewMarkdownManager(os.Getenv("MISTRAL_API_KEY"))`. The cross-file citations resolve: `external_services.md:579` is *"There is **no ordered EU-first fallback chain.**"*, `:602-607` is the four-live-plus-one-latent paragraph.
- **ENUMERATED — the pinned terraform rows.** `graphql-wundergraph/terraform/main.tf:20` = `service_desired_count = 1` and the module passes `private_subnets_ids = var.platform_private_subnets_ids` with no public subnet named (clears `:3` and `:427`). `cms/terraform/main.tf:39` = `service_desired_count = 0`. `roadrunner/terraform/main.tf:19` = `= 1`. `jobsimulation/terraform/main.tf` **@ the ref the claim names, `6092c6d2`**: the `module "jobsimulation"` block is deleted, `service_desired_count` appears only inside the `:15-22` M810 comment, and `:24-40` enumerates the surviving LiveKit/Chime/SSM/atlas ownership — exactly as `:20` and `:221` state. ⚠ Worth stating because it is a live trap: at the **checkout** `462343b0` the module block is still present at line 31 with `service_desired_count = 0`; `6092c6d2` is an ancestor of `origin/main` (`82cb66ec`) but **not** of the checkout, so a ref-less read of that file contradicts a true, correctly-pinned claim.
- **ENUMERATED — `915da06` is a 3→1 step (`:222`, `:294`).** The squashed merge contains both *"remove the jobsimulation subgraph (supergraph 3->2)"* and *"fold cms subgraph into backend (supergraph 2->1)"*; its own subject line says "2→1", exactly as the corpus warns.
- **ENUMERATED — the jobsimulation archive-state hedge (`:20`).** `jobsimulation` `origin/main` carries exactly **4** commits dated 2026-08-04, one of them `caf36c96 Merge pull request #439`. Reporting both and asserting neither is the correct disposition.
- **RE-DERIVED — the platform commit subjects.** `0dab54d` = *"run without the standalone storage; rename graphql -> core"* (2026-08-03); `d11a403` = *"drop roadrunner, prune dead env, repoint messenger"* (2026-08-03); `838d907` = *"drop the storage, messenger and customerio-sync containers"* (2026-08-05); `0c91421` = merge of PR #26 (2026-08-05).

### `corpus/services/ant-academy.md`

- **ENUMERATED — the `isPublic` matcher table (`:315-325`).** I read `code/proxy.js:112-181` @ `22df69dd` in full and matched every pattern to a table row: 25 static patterns across the 8 non-dev groups, plus `DEV_LOGIN_ENABLED → ["/api/dev/login-as","/dev/accept"]` and `VISUAL_BYPASS → ["/my-certificates","/my-activity","/bookmarks"]`. **No pattern is unaccounted for and no table row is invented.** The `VISUAL_BYPASS` predicate is `BENCHMARK_VISUAL_BYPASS === "1" && NODE_ENV === "development"` (`:62-64`) — whitelisted, as the row says; the org gate is `!/^(0|false)$/i.test(raw ?? "")` (`:87-88`) — default ON, fail-closed, as `:298` says.
- **ENUMERATED — the `TopBar` "SEVEN routes, not five" correction (`:273-280`).** `git grep TopBar` @ `22df69dd` yields exactly six mounting view components: `AcademyClient.jsx:1906`, `CourseClient.jsx:2091`/`:2141`, `LatestClient.jsx:128`, `BookmarksClient.jsx:508`, `MyActivityClient.jsx:161`. Their route mounts are `AcademyClient` → `app/(authed)/page.jsx:151`, `courses/page.jsx:92`, `courses/[slug]/page.jsx:219`; `CourseClient` → `chapters/[slug]/page.jsx:226`; `LatestClient` → `latest/page.jsx:37`; `BookmarksClient` → `bookmarks/page.jsx:23`; `MyActivityClient` → `my-activity/page.jsx:21`. That is the seven named routes and no others; `MyCertificatesClient.jsx` imports no `TopBar`. The full route inventory (14 `page.jsx` files) contains no eighth `TopBar` surface.
- **ENUMERATED — the language-switch block (`:260-286`).** `LocaleSwitch` has exactly one mount, `PublicHeader.jsx:20`; `PublicHeader` has exactly one mount, `app/(public)/library/page.jsx:28`; `app/(public)/free/page.jsx:18` is `redirect('/?tier=free')`. `LanguageSelector.jsx:88` is `<div className="language-selector-dropdown" role="menu">` and `:97` is `role="menuitemradio"`; `TopBar.jsx:76` is `<LanguageSelector />`; `locale.js:10` is `SUPPORTED_LOCALES = ['en','it','es','fr','de','nl','pt']` — **7**.
- **ENUMERATED — the `emptyCatalogView()` five-key correction (`:152-166`).** `serverTenant.js:145` **@ ref `22df69dd`** is byte-exact `const view = (await getBackendCatalogView(eids)) ?? emptyCatalogView()`; `:115-117` returns the five-key object; `:111-113` carries the quoted *"carries no tenant metadata…"* rationale; `ucourses/catalog.js:31` is `CATALOG_VERSION = '1.0'` and `:961` is `export const PUBLIC_BUNDLES = [`. ⚠ Load-bearing method note: the **working tree** of `stack-demo/ant-academy` is dirty — `serverTenant.js`, `serverChapterBody.js`, `UserMenu.jsx`, `catalog.json` all carry an applied demo-patch, and a tree read of `:145` shows the `ACADEMY_DEMO_FS_PUBLISHED` branch, which would falsely refute a true claim. The document's own `:87` (patch "reverted on `--stop`") predicts exactly this state.
- **ENUMERATED — the write-path retraction (`:65-77`).** `code/src/progress/store.js:150` `saveChapterProgress`, `:162` `requester(UPSERT_CHAPTER_PROGRESS, …)`, `:169` the `beacon:` option; `:202` `saveLastActivity`, `:210` `SET_LAST_ACTIVITY`, `:215` its `beacon:`. `src/writeThrough/index.js:247` is the `navigator.sendBeacon` branch and `:259` `keepalive: true` (the `fetch` fallback opens at `:254` — the pair brackets the mechanism). `app/api/academy/beacon/route.js:1-18` says verbatim *"a best-effort last-ditch flush for a write that would otherwise be lost if the tab closes mid-retry"* and states the `Authorization`-header rationale. The retraction is correct prose, not a self-contradiction.
- **ENUMERATED — the five demo-patches and their wiring lines (`:226-230`).** In the **pinned per-stack clone** `stack-demo/rosetta-extensions` `09d06070` (and byte-identical in the authoring copy `1dc1eb82`), `demo-stack/ant-academy.sh` assigns `DEVORIGINS_PATCH` at `:56`, `FSPUB_PATCH` at `:66`, `FSBODY_PATCH` at `:75`, `FSPUBLIC_PATCH` at `:84`, `COCKPIT_PATCH` at `:90`. All five anchors land on the assignment line.
- **ENUMERATED — the Clerkenstein/`e2e_persona` block (`:364-387`).** `ant-academy.sh:576-583` is the M220 comment block, and the launch line immediately after passes only `devorigin_env`/`fspub_env`/`NODE_ENV=development` — **neither** `BENCHMARK_VISUAL_BYPASS` nor `NEXT_PUBLIC_E2E_AUTH` is in the launch env (the only other mentions in the file are the `:443-448` history comment). `cockpit.py:812` sets the cookie client-side, `:1496` as a `Set-Cookie` on the `/go` 302, and `:327` names both — so "the cockpit DOES still set the cookie at two live paths, and it is inert" is right on both halves.
- **ENUMERATED — `academy.graphqls`, 1 of 43 (`:63`, `:136`).** `git ls-files "*.graphqls"` in `app` returns exactly **43**, including `internal/web/backend/graphql/graph/schemas/academy.graphqls`. There is no separate academy subgraph.
- **ENUMERATED — the fonts row (`:301-302`).** `code/app/layout.jsx:1` imports exactly `{ Work_Sans, Instrument_Serif }`; `:41` and `:51` are the two constructors; `:132` is `manifest: "/academy-manifest.json"`; `code/academy.css:1` says *"Display + mono usages fall back to system fonts."* No DM Sans, no JetBrains Mono. `code/vercel.json` is exactly `{"framework":"nextjs"}`; `engines.node` is `>=22`; `mobile/package.json` pins `expo ~54.0.33`; `next ^16.2.6` / `react ^19.2.5` / `reactCompiler: true`.
- **ENUMERATED — the repos.yml exclusion (`:26`, `:351`, `:491`).** `platform/repos.yml` @ `0c91421` has exactly four entries (`app`, `sentinel`, `next-web-app`, `studio-desk`); `ant-academy` is not among them.
- **ENUMERATED — the seed/table claims (`:63`, `:79`).** `app/cmd/academy-seed/{main.go,fixtures.go}` exists @ `ad9f3c49`; `internal/data/ent/migrate/schema.go:809` names the table `academy_chapter_progresses` (plural), and `academy_last_activity.go` is the schema-file name behind `academy_last_activities`.
- **ENUMERATED — the `:474` quotation of the upstream repo.** `ant-academy` `22df69dd` `CLAUDE.md:11` reads *"AI Academy is an internal learning portal for **Anthropic** employees (`@anthropos.work` domain)"* — the corpus reproduces the upstream typo verbatim, so the attribution is exact. (This is what makes B1 an inherited-but-restated error rather than an invented one.)
- **RE-DERIVED — `/catalog.json` "~2,667 entries" (`:176`).** The committed file @ `22df69dd` contains 2,692 `"slug"` occurrences (some nested), consistent with a "~"-hedged figure. Not booked.

### `corpus/services/storage.md`

- **ENUMERATED — the `b948604f` vs `ad9f3c49` split (`:29`, `:34-38`).** At `b948604f`, `git grep STORAGE_RPC_ADDR -- '*.go'` returns **15** lines, of which **7** are live `os.Getenv`/`getenv` reads (`cmd/academy-asset-upload/main.go:129`, `cmd/academyImport/main.go:231`, `cmd/import/main.go:50`, `internal/jobsimwiring/wiring.go:115`, `main.go:446`, `:524`, `:992`). At `2035f9a4` and `ad9f3c49` it returns **3**, all comments. `git diff --stat 2035f9a4 ad9f3c49 -- main.go internal/storage/` is **empty**, so the "byte-identical across the two refs" claim holds and the cell's whole ref argument is sound.
- **ENUMERATED — the hazard block (`:64-86`).** @ `app` `ad9f3c49`: `main.go:516`/`:517` are the two `os.Getenv(internalstorage.Env*Bucket)` reads; `:518-523` is the empty-bucket `log.Fatalf` guarded by `deployedEnvironment()`; `:524`/`:525` are `NewManager`/`NewPublicManager`; `:529-535` is the `verifyBucketAccess` guard, also `deployedEnvironment()`-gated; `:547` is `resource.NewManager(...)`; `:1102` is `cmsStorage := internalstorage.NewClient(storageManager, storagens.CMS)`. `env_guards.go:37-44` is `deployedEnvironment()`, returning **false** for `"development"`. `internal/storage/service.go:22`/`:24` are `EnvBucket`/`EnvPublicBucket` — env-var **names**, exactly as the cell insists. @ platform `0c91421`: `docker-compose.yml:82`/`:83` are the two production buckets, `:73-79` the *"These MUST be set"* comment, `:56` `ENVIRONMENT=development`, `:100` the `$HOME/.aws/credentials` bind. `platform/README.md:81-87` @ `0c91421` does instruct putting live AWS keys in `.env`. **The hazard is real as described.**
- **ENUMERATED — the pinned counter-example (`:81-84`).** At `b948604f`, `git ls-tree -- env_guards.go` returns nothing (file absent), `main.go:518-523` is the public-storage clients block and `:529-535` the academy asset uploader — precisely the two wrong constructs the cell names.
- **ENUMERATED — the prod row (`:25`).** `storage/terraform/main.tf` @ `9f8cb53` is 18 lines, declares no service block, `:18` reads exactly *"See outputs.tf — consumers should reference these by output, never by literal name."*, and `git grep -ci custody 9f8cb53` returns **nothing** (0 occurrences). `terraform/storage.tf:22-25` carries the M903 supersession verbatim; there is no `moved` block anywhere in `*.tf`. At the checkout `4ce8ece5`, `main.tf` is **100** lines and still declares the ECS module, and `4ce8ece5..9f8cb53` is **20** commits — both stated figures exact. `d3e6d32` is *"docs(terraform): correct the guard header, which contradicted the shipped design."*
- **ENUMERATED — the historical env table (`:224-233`).** @ `0dab54d`, the deleted `storage` block's `environment:` is `:116` and its seven entries are `:117-123` (`AWS_DEFAULT_REGION`, `AWS_REGION`, `ENVIRONMENT=development`, `PORT=8300`, `RPC_PORT=8301`, `SERVICE_NAME=storage`, `STORAGE_S3_PUBLIC_BUCKET=production-…`). @ `2adcf71`, `:206` is `ENVIRONMENT=development` and `:210` is the public bucket; `:324` sits inside the studio-desk block (ports 9000/9100). The `repos.yml:18-20` @ `0dab54d` cell is the `- name: storage` entry. `STORAGE_RPC_ADDR` count over `docker-compose.yml` + `common.yml` + `.env_example` @ `0c91421`: **0**.
- **ENUMERATED — the repo-level claims.** `go 1.25.0`; `main.go` is **18** lines; `Dockerfile:18` is `RUN go test -v ./...`; **0** `*_test.go` files; the repo's own `CLAUDE.md:120`/`:238-239` document `8080`/`8081` as the binary defaults against compose's `8300`/`8301`; `internal/storage/storage.go:193-200` @ `2035f9a` is `getKeyPath` branching on `s3Bucket != ""`; `storage.go:122` is the `if s.S3Bucket == ""` guard returning `"", nil` from `GetPresignedUrl`.

### `corpus/services/roadrunner.md`

- **ENUMERATED — the `84a4b4f` provenance correction (`:12-23`).** `git blame -L 19,19 87d8d443 -- terraform/main.tf` names `84a4b4f1` (Mattia Sasso, 2025-12-15). `git show --stat 87d8d443` touches exactly one file, `.github/workflows/bump-version.yml`, **3 insertions**. `e45eb61` changes `terraform/main.tf` by 1 insertion / 1 deletion (the module-source swap). Every part of this correction is right, including the reason the old sha was vacuous.
- **ENUMERATED — the Go-scope claim and its retracted repo-wide form (`:34-42`).** `ROADRUNNER_RPC_ADDR`, `RoadRunnerService` and `roadrunner:10401` each return **0** Go hits at `app b948604f` and `jobsimulation 462343b0`. Case-insensitive file counts at each clone's own ref: **app 25, jobsimulation 8, platform 3, studio-desk 3, next-web-app 1** — reproduced exactly; `cms`, `sentinel`, `storage`, `messenger` return 0, so the enumeration is complete rather than merely correct on its members.
- **ENUMERATED — the compose/`repos.yml` history (`:25-30`, `:44-53`, `:87`).** @ `2adcf71`: `repos.yml` holds **9** entries with roadrunner at `:29-31`; `docker-compose.yml:291-292` publishes `10400:10400`/`10401:10401`, `:298` `PORT=10400`, `:302` `RPC_PORT=10401`, and `:296-302` is the env block. `git show d11a403 -- repos.yml` deletes `- name: cms`, `- name: jobsimulation` **and** `- name: roadrunner` — so "grade that commit by its diff, not its message" is measured, not asserted. @ `0dab54d` `repos.yml` has **6** entries (app, sentinel, storage, messenger, next-web-app, studio-desk); @ `0c91421` **4**. `ROADRUNNER_RPC_ADDR` over compose + `.env_example` @ `0dab54d`: **0**.
- **ENUMERATED — the binary/worker facts (`:87`, `:128`, `:169`, `:205`).** `cmd/root.go:110` `cmp.Or(os.Getenv("PORT"),"8080")` and `:84` `cmp.Or(os.Getenv("RPC_PORT"),"8081")`; `internal/worker/worker.go:25` `Concurrency: 10`; `internal/runner/runner.go:126` `asynq.MaxRetry(3)`, `:244` `maxRetries = 15`, `:273` `time.Sleep(1 * time.Second)`; `internal/worker/tasks/tasks.go:4` `= "roadrunner:submissionresult"`; `internal/runner/languages.go:26` `case "py":` with no `"python"`; `Dockerfile:18` `RUN go test -v ./...` with **0** `*_test.go` files; `internal/lsp/lsp.go` exists and is referenced by nothing outside itself.
- **ENUMERATED — the severed-consumer claim (`:130`).** At `jobsimulation 462343b0` the Go source has exactly one `roadrunner` mention, `internal/runner/runner.go:3` (a comment); `git grep -in roadrunner 462343b0` returns **14 lines across 8 files**, and the exact-case form **5 across 3** — all four figures reproduced. `app/internal/jobsimulation/simulator/stream_handlers.go:30-34` @ `ad9f3c49` is the NOTE block ending *"— NOT stream handlers."*

### `corpus/architecture/dependency_map.md`

- **ENUMERATED — the shared-library matrix (`:42`, `:48-50`).** Across the seven Go clones at their own refs: colony **7/7**, proto **7/7**, taxonomy **6/7** (absent only from roadrunner), ai **2/7** (`cms/go.mod:9` and `jobsimulation/go.mod:11`, both `v1.40.2`), authn **0/7**. `app/go.mod:20` @ `b948604f` is `taxonomy v1.2.0`; `sentinel/go.mod:21` @ `88bc5592` is `taxonomy v1.2.0 // indirect`. `1e457fa70` is *"refactor(ai): fold the ai library into app as internal/ai"* and `internal/ai/module_import_guard_test.go` exists. `colony/authn` importer counts: **app 129**, cms 6, jobsimulation 8 — exact.
- **ENUMERATED — the messenger `backend` subscriber, "21 live handlers" (`:58`).** `messenger/internal/flow/flow.go:72` is `AddSubscriber("backend", …)`; the block `:72-104` contains **22** `pubsub.EventHandler(` occurrences, of which `:83` (`OrgJobSimulationAssignmentPastDueHandler`) is **commented out** — so **21 live**. This is the exact `grep -c`-counts-commented-code trap §5 rule 24 warns about, and the corpus is on the right side of it. `:105` and `:109` are the jobsimulation and cms subscribers, as the stream rows cite.
- **ENUMERATED — the messenger-in-app gating (`:58`).** @ `app` `ad9f3c49`: `internal/messenger/flow/streams.go:65` is `StreamBackend = "backend"`; `main.go:1437` is *"ALL OF IT is behind MESSENGER_ENABLED."*; `main.go:1445` is `if messengerEnabled {`; `main.go:1493` names all three `msgflow.Stream*` literals; `main.go:285` is `mustSubsystemSwitch(envMessengerEnabled)`; `env_guards.go:61` is `envMessengerEnabled = "MESSENGER_ENABLED"`. At `b948604f`, `main.go` is **1361** lines, so the retracted `:1442` citation indeed does not exist there.
- **ENUMERATED — the "6 drift / 1 holds" skiller-stream split (`:59`).** At `b948604f`: `main.go:287` `NewPublisher(serviceName, …)`, `:637` `SKILLPATH_STREAM`, `:1039` `CMS_STREAM`, `:1276` `AddSubscriber(os.Getenv("SKILLER_STREAM"), roleManager.SkillerSubscriber())`, `wiring.go:127` `AI_USAGE_STREAM`, `:180` `JOBSIMULATION_STREAM`. At `ad9f3c49` the same six read `logger.Info("subsystem switches",` / `)` / `serverContext,` / `apiKeyManager,` / an asynq-client construction / a blank line — six drifted, exactly as claimed. `internal/roles/roles.go:791` is `func (r *RoleManager) SkillerSubscriber() *pubsub.Subscriber {` **byte-identical at all three refs** — the seventh holds. `SKILLER_STREAM` @ `ad9f3c49`: **6** Go lines across **3** files (`main.go`, `subscriber_merge_test.go`, `subscriber_wiring.go`) and **6** files with no pathspec; `main.go:1537` fills the `Skiller` field of `streamNamesForSubscribers` and no `NewPublisher` names it. `docker-compose.yml:71` @ `0c91421` still sets `SKILLER_STREAM=skiller`.
- **ENUMERATED — the withdrawn `storage.md` attribution (`:19`).** `corpus/services/storage.md` contains **none** of `depends_on`, `DB_CONNECTION`, `REDIS_ADDR`, `go.mod`, `redis`, `postgres` at any line (case-insensitive), against a positive control of 6 hits for `Storage` in the same file and the same tool. The withdrawal is correct. The seven-variable enumeration at `0dab54d:117-123` reproduces exactly (see storage.md clearances).
- **ENUMERATED — the service-count arithmetic (`:31`).** @ `0c91421`: **5** services declared in `docker-compose.yml` + `postgresql`/`redis` from `common.yml` = **7** effective, and **4** `repos.yml` entries. @ `0dab54d`: 8 declared + 2 = **10** effective, **6** entries. Both pairs exact.
- **ENUMERATED — Studio-Room's provider registry (`:25`).** `app/studio` is its **own** checkout at `aeec036a` (invisible to `git -C app grep`); grepped at its own ref, `services/ai.py:705-708` is `providers = {'openai','azure','anthropic'}` — no Mistral — and `tools/pdf2md.py:24` is `from mistralai import Mistral`. `git grep -i mistral aeec036a` returns **22 lines across 3 files** (`requirements.txt`, `tools/pdf2md.py`, `tools/r3.py`) — the figure the cell publishes, reproduced at the nested repo's own ref.
- **ENUMERATED — remaining compose anchors (`:7`, `:13`, `:22`, `:24`).** `:93-94` are `SUPABASE_DB_CONN`/`COPILOT_DB_CONN`; `docker-compose.yml:101-109` is backend's whole `depends_on` (redis, postgresql, sentinel) with the in-line note at `:102-103`; `0dab54d:docker-compose.yml:154` is `profiles: [customerio-sync, all]`; `docker-compose.yml:138-140` is studio-desk's single-edge `depends_on: backend`. `app/internal/converter/gotenberg.go:31` @ `ad9f3c49` is the plain-HTTP `POST …/forms/libreoffice/convert`. `DIRECTUS_WEBHOOK_SECRET` is read at `main.go:1199`.
- **ENUMERATED — the intra-corpus cross-references.** `:90`'s "`:9`/`:15`/`:31` above" all land on the cited constructs; `:22`'s `service_taxonomy.md:101-103` lands inside the correct blockquote.

### `corpus/services/TEMPLATE.md`

- **Read in full (46 lines); nothing to book.** It is a placeholder skeleton with no factual assertions about the platform. `:36-40`'s `docker compose up -d [service_name]` is a bracketed placeholder, not a retired profile/service token, so §5 rule 30 (*do not spell a dead command in runnable form*) is not engaged.

---

## What I could not settle

- **GitHub archive states.** `architecture_overview.md:3` (*"the repo is ARCHIVED on GitHub (2026-07-30)"* for `graphql-wundergraph`), `dependency_map.md:23` (same), `roadrunner.md:47-48` (skiller archived 2026-07-01, skillpath 2026-07-31), `roadrunner.md:66-68` (chronos **not** archived, last push 2026-04-23) and `roadrunner.md:84` (roadrunner **not** archived). Neither `gh` nor `timeout` is on this box, and archive state is not derivable from a clone. **Settled by:** `gh repo view anthropos-work/<repo> --json isArchived,archivedAt` — five one-line calls. I note that `roadrunner.md:56` and `architecture_overview.md:20` handle the *same* class correctly by declaring it unmeasurable, so the corpus is inconsistent about whether this class is assertable — but I cannot say which side is right without the API.
- **`architecture_overview.md:13`, `:236` — "≥42,790 skills / ≥22,470 job roles" (public subset, measured 2026-06-29).** A live-DB measurement; not derivable from any clone. Settled by re-running the `/db-query` count against the public taxonomy.
- **`ant-academy.md:90` / `:235` — "a grid of 65 real cards with 0 Draft chips"** and `:250`'s durability behaviour. These are live-render measurements on a running `billion` demo; the FS catalog is present in-repo but the rendered card count depends on the patched resolver at runtime. Settled by a cold `/demo-up` + the M245 autoverify assert `(f)` (which I confirmed exists at `stack-verify/live/autoverify.sh:459-479` in the pinned clone).
- **`ant-academy.md:304` — "1000+ Vitest tests".** Requires executing the suite; I verified only the Playwright half (which is where the MINOR is).
- **`ant-academy.md:298` — "`@anthropos.work` domain restriction is enforced in the Clerk app".** The deciding artifact is Clerk dashboard configuration, outside every clone. I booked it as a co-anchor of B1 on the strength of the file's own retraction plus the shipped anonymous→subscriber funnel, but a Clerk allowlist could in principle coexist with that code; **settled by** reading the Clerk instance's restrictions/allowlist settings.
- **`storage.md:25` — the `app/knowledge/plan/...m903-s3-custody/` front-matter quotes.** That path is under `knowledge/plan/**` inside the `app` clone. I treated the hard bar as path-shaped and did **not** open it; the surrounding measurable claims (no `moved` block, `custody` count 0, `storage.tf:22-25` supersession text) all verified independently, so the cell's conclusion stands on evidence I did read.
- **`architecture_overview.md:419` — "`extensions` schema → houses `pgvector`".** `common.yml:2-4` shows `postgresql` builds from a local `postgresql/` context, so a custom image is confirmed, but the schema/extension placement itself needs a live `\dx` against a running stack.
