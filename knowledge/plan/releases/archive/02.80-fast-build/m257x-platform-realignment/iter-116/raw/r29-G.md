# Seat G — reading #29/#30 (M257x iter-116)

## Refs actually read (stated at the moment of measurement)

Re-derived at this seat's open with `git -C <clone> rev-parse --short=8 HEAD`; all match the
ADDENDUM's superseded table:

| clone | sha read |
|---|---|
| `stack-demo/app` | `ad9f3c49` (pinned claims also read at `b948604`, `9d00a313`, `5ba17044`) |
| `stack-demo/platform` | `0c91421d` (pinned claims also read at `0dab54d`, `d11a403`, `d11a403^`, `2adcf71`, `838d907`, `a2a3ee6`, `a4db680`) |
| `stack-demo/next-web-app` | `8297c684` (pinned claims also read at `bb3313bc`) |
| `stack-demo/sentinel` | `f2c46190` (pinned claims also read at `88bc5592`, `88036d7`) |
| `stack-demo/studio-desk` | `41ee3575` |
| `stack-demo/ant-academy` | `22df69dd` |
| `stack-demo/{storage,messenger,cms,jobsimulation,roadrunner,graphql-wundergraph}` | `4ce8ece5 / fa47850d / ca50c817 / 462343b0 / 87d8d443 / 60c229f3` |
| `stack-demo/rosetta-extensions` (**pinned per-stack consumption clone — the tree I used for every rext claim**) | `09d06070` |
| `.agentspace/rosetta-extensions` (authoring copy — used only to confirm the two trees are byte-identical on `persona_write.go`) | `1dc1eb82` |

Nested repos enumerated (`find stack-demo -name .git -maxdepth 4`): **15 git trees**, including
`stack-demo/app/studio` and `stack-demo/cms/studio` @ `aeec036a`. My `.tf` census below names all 15.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/services/hiring.md` | 483 | yes |
| `corpus/services/studio-desk.md` | 449 | yes |
| `corpus/architecture/shared_libraries.md` | 274 | yes |
| `corpus/services/next-web-app.md` | 164 | yes |
| `corpus/services/clerk-integration.md` | 128 | yes |
| `corpus/services/skiller.md` | 78 | yes |

**Search positive controls run in the same passes** (§5 rules 1–2):
- `git grep -in graphql -- 'src/*'` in `studio-desk` returned 2 → paired with `git grep -cin directus -- 'src/*'` which returned `src/index.ts:9`, `src/routes/skillpath.ts:78`. Pipeline live.
- `git grep -c ROADRUNNER_RPC_ADDR` in platform @ `0dab54d` returned 0 → paired with `CMS_RPC_ADDR`, which returned 2 files. Pipeline live.
- `grep -rn 'SET "score"'` over `app/terraform/migrations/` returned 0 → paired with `grep -rln 'SET "'`, which returned 3+ files. Pipeline live.
- **One search DID silently fail and I caught it**: `for c in $(git log --format=%H …); do git show "$c:repos.yml"` returns *empty* under zsh, because zsh applies the `:r` history modifier to `$c`. The empty result looked exactly like "no `type: go` entries ever". Re-run as `"${c}:repos.yml"` it returns the real answer (11). Recording it because it is §5 rule 1 in the flesh.

---

## Blockers

### B1 — `clerk-integration.md` asserts sign-in tokens are used **only** for admin impersonation; there are two more live minting sites, and the corpus documents both elsewhere

- **Anchor:** `corpus/services/clerk-integration.md:40` —
  > `- **Sign-in tokens** — **only** for app-native admin impersonation (chosen over Enterprise-tier Actor Tokens).`
- **Why it is false:** Clerk's `signInTokens.createSignInToken` Backend-API call is made at **three**
  independent sites across the clone set, not one:
  1. `stack-demo/app/internal/admin/impersonation/manager.go:101` — `m.signInTokenCl.Create(ctx, &clerkSignInToken.CreateParams{…})`, the admin-impersonation feature the doc names (import `clerkSignInToken "github.com/clerk/clerk-sdk-go/v2/signintoken"` at `:29`). This half is correct.
  2. `stack-demo/next-web-app/apps/web/src/app/api/dev/login-as/route.ts:79` — `const { token } = await client.signInTokens.createSignInToken({ userId: user.id, expiresInSeconds: 600 });`, with the comment at `:78` *"Mint a one-time sign-in token"* and `:85` handing the ticket to `/dev/accept`. Measured @ `next-web-app` `8297c684`.
  3. `stack-demo/studio-desk/src/routes/dev.ts:83` — the identical call, ticket handed to `/dev-accept.html` at `:89-91`. Measured @ `studio-desk` `41ee3575`.

  Neither (2) nor (3) is "app-native admin impersonation": both are the dev-login harness that signs
  you in **as an arbitrary user found by email** (`:72` `const user = users?.[0]`), and neither is
  admin-scoped.

  **It is also a corpus self-contradiction.** Two of my own assigned files describe the same
  mechanism: `studio-desk.md:110` calls `dev-accept.html` *"the 'log in as a real Clerk user'
  ticket→session exchanger"*, and `next-web-app.md:75`'s public allowlist is derived from
  `apps/web/src/proxy.ts:56`, which lists `'/api/dev/login-as(.*)'` and `'/dev/accept(.*)'` —
  i.e. the very endpoints that mint sign-in tokens.
- **Confidence:** medium-high. I hesitated because the surrounding section is a *product* feature
  census and one could argue a dev-only harness is out of scope — but the word is **only**, the
  section already enumerates dev-facing surfaces elsewhere (`Webhooks`, `Backend API`, `user-create
  CLI`), and the two extra sites are checked-in production code paths in two different repos.

### B2 — `studio-desk.md` recommends `MOCK_CLERK` as the local-dev-without-Clerk path; the source says, three times and emphatically, that it is **not** an interactive-dev path

- **Anchor:** `corpus/services/studio-desk.md:319` —
  > `**Local dev without real Clerk**: Set `MOCK_CLERK=true` (backend) and `VITE_MOCK_CLERK=true` (frontend) in `.env` to bypass Clerk auth — do not use in production.`
- **Why it is false / misleading:** at `studio-desk` `41ee3575` the repo says the opposite in three
  places, each naming the sanctioned alternative the corpus never mentions:
  - `src/index.ts:50-54`: *"MOCK_CLERK bypasses Clerk for the headless automated test suite (the skillpath integration tests + the Playwright e2e suite), where there is no browser to complete an OAuth login. **It is NOT the path for interactive / agentic dev** — for that, sign in as a REAL user via `/api/dev/login-as` (no Google, no 2FA)."* `:56` prints `⚠️ MOCK_CLERK is enabled — Clerk auth is bypassed (test-harness mode)`.
  - `.env.example:23-27`: *"MOCK_CLERK / VITE_MOCK_CLERK bypass Clerk for the AUTOMATED TEST HARNESS only … **They are NOT a path for interactive dev**: the synthetic user's fake org never matches real platform data, so **GraphQL-backed content won't load**. To work as a real signed-in user, use the DEV LOGIN above."*
  - `app/core/main.ts:133`: `console.warn('VITE_MOCK_CLERK is enabled — synthetic test-harness auth (not for interactive dev; use /api/dev/login-as).')`

  So a developer following the corpus recipe gets a studio whose GraphQL-backed content does not
  load, and will debug the platform rather than the invocation. The doc does mention
  `dev-accept.html` once, buried in the Project Structure tree at `:110` as *"Dev-only acceptance
  harness … not in the prod build"*, and **never** as the local-dev recipe; `/api/dev/login-as`
  appears nowhere in the file (`src/index.ts:150` mounts it: `app.use('/api/dev', createDevRouter(…))`,
  and it is the only `/api` router mounted *without* `authMiddleware, adminMiddleware`).
- **Confidence:** medium. I hesitated because the literal sub-clause *"to bypass Clerk auth"* is
  true — `MOCK_CLERK=true` does bypass Clerk. What is false is the **heading and purpose**
  (*"Local dev without real Clerk"*), which is precisely the use the source forbids by name. Booked
  as a BLOCKER per the briefing's doubt rule: it is a documented procedure that does not do the job
  it is documented for.

### B3 — `hiring.md`'s `persona_write.go` anchor lands on a comment about the write the seeder **removed**, not on the three validation writes it claims

- **Anchor:** `corpus/services/hiring.md:243` —
  > `… three tables (all in **`public`**, `20260722081626_jobsim_data_model.sql:336/355/376`; …) the `PersonaSeeder` also writes (`persona_write.go:69-71,143-167`).`
- **Why it is false:** settled at the **pinned per-stack consumption clone**
  `stack-demo/rosetta-extensions` `09d06070` (§5 rule 45); I also diffed it against the authoring
  copy `1dc1eb82` — the file is **byte-identical**, so the tree choice does not change the answer.
  - `stack-seeding/seeders/persona_write.go:69-71` is **comment prose about the write that was DELETED**:
    `69 //`, `70 // The old \`jobsimulation.sessions\` step is REMOVED rather than re-pointed: it wrote the SAME`,
    `71 // a.sessions rows under the SAME id as the \`public.job_simulation_sessions\` step below, so`.
    That is the opposite construct from *"the PersonaSeeder also writes"*.
  - The **actual writes** are the `steps` table at `:91-94` —
    `91 {"public", "job_simulation_sessions", sessionCols(), a.sessions},`
    `92 {"public", "validation_attempt_results", attemptResultCols(), a.attemptResults},`
    `93 {"public", "validation_attempt_skill_results", skillResultCols(), a.skillResults},`
    `94 {"public", "validation_criterion_results", criterionResultCols(), a.criterionResults},`.
    (The doc cites `:91` correctly **elsewhere**, at `hiring.md:310`, which is what makes `:69-71` a
    drift rather than a misunderstanding.) The nearest correct comment anchor would be `:66-68`.
  - The second half, `:143-167`, is also short: `143` is the closing `}` of `flush()`, `145` the
    `// --- column lists ---` header, `152-159` `sessionCols()`, `161-167` `attemptResultCols()`.
    It therefore covers the column list for **one** of the three validation tables;
    `skillResultCols()` is at `:169` and `criterionResultCols()` further down — both outside the
    cited range.
- **Confidence:** medium. I hesitated because the **substantive claim is TRUE** — the PersonaSeeder
  really does write all three tables — and only the citation is wrong. Booked because the instrument
  names "wrong line anchors that point at the wrong construct" as a BLOCKER, and because a reader
  following `:69-71` reads a paragraph explaining a write that no longer happens.

### B4 — `shared_libraries.md` says the colony-split-closing commit is "two commits past `88bc5592`"; it is **one**

- **Anchor:** `corpus/architecture/shared_libraries.md:57` (colony **Version pin** cell) —
  > `` `sentinel`'s `88036d7` *"chore(deps): update dependencies to latest versions"* took it `v0.34.3 → v0.35.2`, **two commits past** the `88bc5592` this row used to cite. ``
- **Why it is false:** measured in `stack-demo/sentinel`:
  - `git rev-list --count 88bc5592..88036d7` → **1**
  - `git log --oneline 88bc5592..88036d7` → a single line, `88036d7 chore(deps): update dependencies to latest versions`
  - `git log --oneline -8` shows the linear chain `f2c4619 ← 88036d7 ← 88bc559`, i.e. `88036d7` is
    the **immediate child** of `88bc559`. (It is the *checkout* `f2c46190` that is two past
    `88bc5592`, which is presumably where the 2 came from.)
  - The claim the sentence carries **is** correct at both refs and I verified it:
    `git show 88bc5592:go.mod` → `colony v0.34.3`; `git show 88036d7:go.mod` → `colony v0.35.2`.
- **Confidence:** medium-low. The measurement is unambiguous; what I am unsure of is **materiality** —
  the load-bearing half of the cell (which commit closed the split, and from what to what) is exactly
  right, and nothing downstream depends on the distance. I booked it rather than minor-ing it because
  the instrument lists "wrong counts" as a BLOCKER class and because commit distances are the kind of
  decoration a later reader re-derives an anchor from.

---

## Minors

- `hiring.md:213` — `simulationScoreColumn.tsx:54` is cited for *"renders `row.score`"*; at
  `next-web-app` `8297c684` line 54 is the bare property `accessorKey,` inside the returned column
  def. The binding the sentence means is the default `accessorKey = 'score'` at **`:33`**; the render
  is the co-cited `:95-97`, which is exact. (The *"byte-identical, same line numbers"* half of the
  cell I verified with `cmp` — the two copies are identical and both 168 lines.)
- `hiring.md:300` — the intra-corpus back-reference `(**`:193-198`**)` to the **History** blockquote:
  `:193` is a **blank line** and the blockquote runs `:194-207`, so the cited range starts one line
  early and stops nine short. Rule 34's own class.
- `hiring.md:302` — *"the `job_position` bullet, now `:173-175`"*: `:173` is the closing line of the
  **preceding** bullet (`(real content, zero synth — M222 BA-6)`); the `job_position` bullet is
  `:174-185`.
- `next-web-app.md:89` — the inline comment writes `engines.node ">=24"`; `package.json:13-14`
  declares `"node": ">=24.0.0"`. Cosmetic; the "Node 24+" statement is right.
- Out-of-scope but worth routing: root **`CLAUDE.md`** still says *"five repos, of which **four are
  imported as private Go modules** (`ai`, `colony`, `proto`, `taxonomy`)"*, which
  `shared_libraries.md:3-12` correctly **refutes** (`ai` was folded into `app` at `1e457fa70` and is
  a `require` in **no** repo a stack builds — I verified `git show ad9f3c49:go.mod | grep -c
  anthropos-work/ai` → 0 and the same for `go.sum`). The surviving false site is at the corpus's
  highest-propagation file, i.e. exactly rule 19's "leaks to the edge of the previous repair's scope"
  corollary. **Not booked against my files** — `shared_libraries.md` is the correct side.

---

## Positively cleared (audited zeros)

### ENUMERATED (predicate re-derived from source, cardinality compared before arithmetic)

1. **`shared_libraries.md:22-25` — "colony 7/7, proto 7/7, taxonomy 6/7 (all but roadrunner), ai 2/7 (cms + jobsimulation), authn 0/7."**
   I enumerated the SET first (`stack-demo` Go repos on disk = app, sentinel, storage, messenger,
   cms, jobsimulation, roadrunner = **7**), then grepped every `go.mod` at its own HEAD. Result
   matches exactly, including the direct/indirect and version values quoted in three separate rows:
   colony `app:15 v0.35.2 / sentinel:8 v0.35.2 / storage:7 v0.34.3 / messenger:7 v0.35.2 / cms:10
   v0.35.1 / jobsimulation:12 v0.35.1 / roadrunner:7 v0.34.3`; proto `app:16 v1.210.0 / sentinel:9
   v1.210.0 / storage:8 v1.196.0 / messenger:8 v1.210.0 / cms:11 v1.207.0 / jobsimulation:13
   v1.205.0 / roadrunner:8 v1.196.0`; taxonomy present in all but roadrunner; `ai v1.40.2` in cms:9
   and jobsimulation:11 only; `authn` in none.
2. **`shared_libraries.md:213` — "that set is 11" (`type: go` entries across `repos.yml`'s whole history).**
   Derived over all 10 commits that ever touched `repos.yml`: `app chronos cms intelligence
   jobsimulation messenger roadrunner sentinel skiller skillpath storage` = **11**. All four extras
   (skiller, skillpath, chronos, intelligence) are in the first revision `a2a3ee6` ✓, and skillpath
   is the **last** of the four to leave (`fdfa189` intelligence → `045857c` chronos → `21429b7`
   skiller → `a4db680` skillpath) ✓. Names-ever (any type) = 14.
3. **`shared_libraries.md:86` / `:99` — "app/main.go registers six Connect handlers @ `b948604`."**
   Read at the named ref (`git show b948604:main.go`): Users `:1187`, Organizations `:1188`, Skiller
   `:1196`, JobSimulation `:1204`, LabSession `:1228` unconditional, plus `CMSService` inside
   `if cmsRPCServer != nil {` at `:1212-1214`. Six, and **no** `SkillPathSessionService` and no
   RoadRunner handler in the mux. `ROADRUNNER_RPC_ADDR`: 0 in `app` @ `9d00a313`, 0 in platform @
   `0dab54d` (control: `CMS_RPC_ADDR` hits at both).
4. **`shared_libraries.md:152-160` — the three `flag_use_azure_us` sites.**
   `git grep -n flag_use_azure_us` over `app` @ `ad9f3c49` returns **exactly three**:
   `internal/jobsimulation/ai/ai.go:267`, `:344`, `internal/skillerai/ai.go:347` — the same three the
   cell names. `getClient` at `jobsimulation/ai/ai.go:259` and `skillerai/ai.go:332` ✓;
   `isThrottlingError` declared `:129`/`:128`, used `:166`,`:325`/`:176` ✓. `internal/ai/ai.go` is
   **21 lines** ✓ and `internal/ai/module_import_guard_test.go` exists with the quoted
   rosetta-extensions comment ✓.
5. **`hiring.md:264-266` — "one of FOUR required-and-undefaulted columns … every other NOT NULL column carries a DEFAULT."**
   I read the whole `CREATE TABLE` at `20260722104506.sql:1-27` and classified all 23 columns rather
   than trusting the four names. NOT NULL without DEFAULT = `owner_id:6`, `sim_id:7`, `sim_type:10`,
   `token:13` — **exactly four**; every other NOT NULL (`id`, `created_at`, `updated_at`, `status`,
   `completion_status`, `interactions_progress`, `score`, `validation_version`) carries a DEFAULT.
   `token` UNIQUE at `:29` ✓.
6. **`hiring.md:180` — "`public.job_simulation_sessions` has 23 columns."**
   Enumerated from the DDL = 23, cross-checked against the Ent schema (20 `field.*` + the 3-member
   `PrimaryKeyMixin/CreatedAtMixin/UpdatedAtMixin` = 23), and confirmed no later migration adds a
   column to that table.
7. **`hiring.md:92-93` — the four numbers in the `is_hiring` derivation.**
   `grep -in hiring internal/web/backend/graphql/graph/resolver_queries.go` → 4 hits, all sim-TYPE
   (`simulation_type_hiring` strings at `:811`,`:843`; the `ActivityDashboardSimulationTypeSimulationTypeHiring`
   case at `:991-992`) — no `is_hiring` read ✓. `grep -in hiring internal/organization/intelligence.go`
   → **0** ✓. Positive controls quoted by the doc: `OrgFeatureInsights` ×**8** ✓,
   `JobSimulationSession` ×**44** ✓ — both reproduce exactly.
8. **`hiring.md:259-261` — "a closed 5-value enum, exactly pending/passed/failed/discarded/timedout."**
   `ent/enum/jobsimulation.go:29-35` const block and `Values()` at `:37-43` — five members, no
   `SIMULATION…` member ✓; SDL `jobsimulations.graphqls:14` (`SessionCompletionStatus`) and `:128`
   (`SimulationSessionCompletionStatus`) each declare five lowercase members ✓; the gqlgen marshal at
   `graph.go:129546-129554` and its proto-bound twin at `:129392-129400` are bare
   `graphql.MarshalString(string(v))` with no membership check ✓; Ent's `assignValues` casts
   unconditionally at `jobsimulationsession.go:181-186` ✓.
9. **`clerk-integration.md:51`, `:91` — "129 files in `app` vs 0 in `sentinel`."**
   `git grep -l colony/authn -- '*.go'` per repo: app **129**, sentinel **0**, storage **0**,
   messenger **0** ✓. And `git grep -c clerk -- '*.go'` = 0 for all three of sentinel/storage/messenger,
   which is the `:74`/`:94` claim ✓.
10. **`clerk-integration.md:37` — "Webhooks (svix, 12 event types)."**
    `app/internal/clerk/events/events.go` `case "…"` arms enumerated: `user.{created,updated,deleted}`,
    `organization.{created,deleted,updated}`, `organizationInvitation.{accepted,created,revoked}`,
    `organizationMembership.{created,deleted,updated}` = **12** ✓; `svix-webhooks v1.99.1` in
    `app/go.mod:65` ✓.
11. **`clerk-integration.md:103` — the SDK-version drift table.** Per-clone at each clone's own HEAD:
    `@clerk/nextjs` `^6.39.6` in `apps/{web,hiring,integration}` and **absent** from
    `apps/maintenance` ✓; `ant-academy/code/package.json:52` `^6.39.2` ✓;
    `next-web-app/apps/mobile/package.json:6` `~2.6.18` vs `ant-academy/mobile/package.json:18`
    `~2.19.36` ✓; `app/go.mod:31` @ `5ba17044` = `clerk-sdk-go/v2 v2.7.0` ✓ (still `v2.7.0` at
    `ad9f3c49`, at `:29`).
12. **`clerk-integration.md:44` — the "Not used" prebuilt-component list.** Counted over `apps/*` +
    `packages/*` @ `8297c684`: `<UserButton` **0**, `<OrganizationSwitcher` **0**,
    `<OrganizationProfile` **0**, `<Protect` **0**; `<SignIn` 5, `<SignUp` 3, `<UserProfile` 3 —
    exactly the used/unused split the doc draws ✓. `getToken()` is called with no `template` anywhere
    (the one call with an argument, `apps/hiring/…/TokenValidationClient.tsx:34`, passes
    `{ skipCache: true }`) ✓ — that is the `:33` claim.
13. **`studio-desk.md:47-60` — the "NOT a GraphQL client" correction.** `git grep -in graphql -- 'src/*'`
    @ `41ee3575` returns **exactly two** lines, `src/routes/skillpath.ts:374` and `:405`, both comments
    saying the opposite ✓; `git grep -n 8082 -- 'src/*'` → **0** ✓; the four `new GraphQLClient(` sites
    are **all** in `app/` (frontend): `userService.ts:20`, `taxonomyService.ts:43`,
    `userPreferencesService.js:13`, `content/simulationContentService.js:325` ✓, all fed by
    `app/services/config.ts:6` reading `VITE_GRAPHQL_ENDPOINT` ✓; the four `/api` routers are
    `:150 /api/dev`, `:158 /api/ai`, `:161 /api/skillpath`, `:164 /api/youtube` ✓.
14. **`studio-desk.md:88` — the `GCLOUD_SERVICE_ACCOUNT` "vestigial" claim, all three conjuncts.**
    `.env.example` is **131 lines**, `:119` is the declaration, `:120` is **blank**, `:117` is
    `YOUTUBE_API_KEY=`, `:118` the comment ✓; `terraform/main.tf:129` injects it ✓; and
    `git grep GCLOUD_SERVICE_ACCOUNT -- 'src/*'` → **0** (repo-wide it is only `.env.example`,
    `CHANGELOG.md`, `terraform/main.tf`) ✓.
15. **`studio-desk.md:35` — the prod vite entry set.** `vite.config.ts:28-42` `rollupOptions.input`
    enumerated: home, sim-advanced-builder, builder-skill-path, simulation-builder, sim-guided-builder,
    generation, catalog, academy, skills = **9**, plus `dev-accept` behind `...(isProduction ? {} : …)`
    ✓ — matches the doc's list member-for-member.
16. **`next-web-app.md:22` — the Next version.** All four of `apps/{web,hiring,integration,maintenance}`
    declare `"next": "~16.2.12"` (tilde, not caret) and `apps/mobile` declares none ✓; `pnpm-lock.yaml`
    resolves `next@16.2.12` ✓; react `19.2.7`, `packageManager: pnpm@10.30.3`, `turbo ^2.9.6`,
    `engines.node ">=24.0.0"` ✓; `UPGRADE-IMPACT-next16.md` and `knowledge/next15-adoption-plan.md`
    both present ✓; only `Dockerfile.dev` at the repo root ✓.
17. **`next-web-app.md:75` — "`middleware.ts` exists nowhere in the repo."**
    `git grep -l "" HEAD -- '*middleware.ts'` → **0** files; `proxy.ts` exists in `apps/{web,hiring,integration}` ✓;
    the repo's own `CLAUDE.md:55` says verbatim *"Next 16 renamed the `middleware.ts` convention → `proxy.ts`"* ✓
    and `CLAUDE.md:15` says *"Next.js 16 App Router"* ✓ (the `:153` claim).
18. **`next-web-app.md:97-99` — "no `storybook` script and no `.storybook/` dir; the only trace is `configs/tailwind/storybooks.css`."**
    Enumerated: no `"storybook"` key in the root or any app `package.json`, no `.storybook` directory,
    `configs/tailwind/storybooks.css` present ✓.
19. **`next-web-app.md:155` — "8 locales on disk."** `configs/i18n/messages/` = `de en es fr it ja nl pt` = **8** ✓
    (and `apps/web/src/providers/Clerk.provider.tsx:10-17` imports 8 `@clerk/localizations`, which is
    `clerk-integration.md:41`'s claim; `apps/hiring`'s copy imports 7 — the doc's "8" holds for the
    surface it describes).
20. **`skiller.md:23-25` — "0 hits over all 44 tracked `.tf` files in the 13 `stack-demo` repos."**
    Re-derived per tree over **all 15 git trees** (`find … -name .git`), each at its own ref:
    `git ls-files '*.tf'` sums to **44** (app 5, cms 4, graphql-wundergraph 4, jobsimulation 6,
    messenger 4, next-web-app 2, roadrunner 4, rosetta-extensions 2, sentinel 4, storage 5,
    studio-desk 4; ant-academy/platform/both `studio` sub-repos = 0), and
    `git grep -c "backend.internal.anthropos" -- '*.tf'` = **0** in every one ✓.

### RE-DERIVED (weakest form — arithmetic the document itself displays; I re-computed it)

- `shared_libraries.md:93-104` — *"At least 13 Connect-RPC services"*: the named list contains 13
  members. I could **not** enumerate the predicate independently, because `proto` is a private module
  in no clone set — which is exactly what the passage says about itself (*"a floor, not a count"*).
  Recording it as RE-DERIVED, not as a clearance.

### Anchors resolved and confirmed to name the right construct (not a clearance on their own, but recorded)

`app` @ `ad9f3c49` unless noted: `manager.go:450/:453/:537`; `siminvitationlink.go:62/:63`;
`resolver_cms_queries.go:95,210,258,295` + `:99-103`; `resolver_queries.go:1034/1035/1053/1080/1085/1089`;
`intelligence.go:1700/1702/1709/1733/1738-1764/1739-1744/1820/1844/1846/2124/2158-2160/885-886`;
`ent/schema/job_simulation_session.go:33/39/45`; `ent/schema/anticheat_result.go:24`;
`20260722104506.sql:2/6/7/10/12/13/29/53/79`; `20260729133514.sql:58-62`;
`20240527131926.sql:7`; `20250416091037.sql:5`; `20260722081626_jobsim_data_model.sql:336/355/376`;
`atlas.hcl:8`; `askengine/registry.go:192`; `internal/skillpaths/skillpaths.go:27-31`;
`jobsimwiring/wiring.go:123` @ `9d00a313`; only `CREATE SCHEMA` in the set is `auth`
(`20230817154747_supabase_baseline.sql:2`); `SET "score"` = 0 set-wide.
`next-web-app` @ `8297c684`: `useGetClerkOrganization.tsx:16-18/20-21`;
`useNavbarSections.tsx:161/249-256/329-331/340-343/391-398/475-482/476/568`; `orgGroups.ts:48-65/:61`
(and `showWorkforce={false}` appears in **exactly two** places, both `apps/hiring/.../template.tsx:167`
and `:248` — enumerated); `apps/web/.../UserStatusContext.tsx:141-173/144-149/168-172`;
`apps/hiring/.../UserStatusContext.tsx:125/144-145`; `template.tsx:90`; `FreeTrialContainer.tsx:29`;
`InsightsByMembersContainer.tsx:108` (hiring, the export) and `:359` (hiring, the plain Ant `<Drawer`);
`…/[simId]/page.tsx:14`; `insights.ts:31`; **no** `(.)`/`(..)` intercepting-route dirs anywhere in `apps/`.
Same file @ the pinned `bb3313bc`: `useNavbarSections.tsx:460` / `:459-466` — both exact.
`platform` @ `0c91421`: compose declares exactly `sentinel, backend, studio-desk, next-web-app,
gotenberg` (`common.yml` adds `postgresql`, `redis`); `:110`, `:119`, `:135`, `:138-140`, `:141`,
`:143`, `:151`, `:160`, `:165-167`; `Makefile:119-120`; `repos.yml` = **4** entries with `app` at
`:14-17` `migrations: true`; **zero** `*_RPC_ADDR` at HEAD. @ `0dab54d`: `:204`, `:223-225`, `:226`,
`:236` and **6** repos.yml entries; @ `2adcf71`: studio-desk `depends_on` `:337-341` incl. `cms`;
@ `d11a403^`: `SKILLER_RPC_ADDR=http://backend:8083` **already**, while `CMS_RPC_ADDR` and
`JOBSIMULATION_RPC_ADDR` still pointed at the husks — i.e. `skiller.md:19-20` is exactly right.
`rosetta-extensions` @ pinned `09d06070`: `hiring_config.go:99` (`CopyRowsIdempotent` into
`public.organization_sim_invitation_links`); `persona_write.go:91`, `:152`, `:152-158` (`token` in
`sessionCols()`); `skipGenericActivityForHiringOrg` at `hiring_scope.go:45-46` consulted by **8**
seeder files (activity, assignments, feedback, hero_activity, jobsim_sessions, persona,
skillpath_sessions, succession — the doc's "all 8"); `TestGenericActivitySeeders_SkipHiringOrg` at
`hiring_scope_test.go:75`; `blueprint.go:453` `RoleMix{Admin, Member, Candidate, AdminEmails}`;
`demo-stack/frontend/hiring.Dockerfile` + `up-injected.sh:1076-1085` (image `demo-$N-hiring`);
`stack-verify/e2e/tests/render-hiring-comparison.spec.ts`.

---

## What I could not settle

- **`hiring.md:172` — "the captured public snapshot carries 87 real `SIMULATION_TYPE_HIRING` sims"**
  and `:176` "**0 `job_position` rows** (the prod '443' was never captured)". These are properties of
  a snapshot cache under `.agentspace/snapshots/`, not of any clone. Settled only by
  `stacksnap status` / reading the manifest on a live capture.
- **`shared_libraries.md:56, 64-75, 220` — every claim about colony's and taxonomy's *internals*** (the
  `go 1.25.0` / `golang:1.26-bookworm` pair, the seven `colony/*` package descriptions, the
  `rate.NewLimiter(1,5)` reject path being commented out, taxonomy's `node.go` being **131 lines**,
  the `NodeID` regex and the three worked examples). Neither library is in any clone set
  (`repos.yml` has 4 entries; neither appears in `find stack-demo -name .git`), so nothing here can
  read them. Settled by cloning `anthropos-work/{colony,taxonomy}` at the pins the doc names.
- **`shared_libraries.md:93-104` — whether 13 is the true Connect-RPC service count.** Same cause:
  `proto` is in no clone set. The doc marks it a floor; I can neither confirm nor extend the list.
- **`skiller.md:7, 37-39` — "not in production ECS", "the skiller ECS service and its terraform module
  were removed from production", "the old ECR repo is intentionally orphaned", "app PR #989".** The
  `skiller` repo is not cloned and `infrastructure` has never been in a clone set, so no `.tf` in this
  tree can decide it — the same unmeasurability the *same file* invokes at `:21-28` to refuse asserting
  `skiller_rpc_addr`. I did **not** book this as a self-contradiction because
  `platform-migration-status.md:97` states skiller's prod row the same way (`decommissioned`, *"Repo
  ARCHIVED 2026-07-01"*), so the corpus is at least uniform; but it is an asymmetric epistemic
  standard inside one 78-line file and is worth a deliberate decision rather than drift.
- **`hiring.md:428-467` — the render-gate results** (5/5, scores 27–100, `≥40 → ≥6` floor retune,
  p95 1.27 s, "4/4 flake") and **`studio-desk.md:404` — "skeleton-visible p95 4669 ms → 817 ms"**.
  Live measurements on `billion` / `demo-2`; not re-derivable from a static tree.
- **`clerk-integration.md:33` — "the custom claims are baked into that default token via dashboard config."**
  A Clerk-dashboard fact with no code representation in any clone. I confirmed the *consumer* side
  (`getToken()` never passes a template; `colony/authn` reads `eid`/`org`/`org_role`), but not the
  dashboard configuration itself.
