# Seat E — reading #29/#30 (M257x iter-116)

**Ground truth re-verified at this reading's open** (all match the ADDENDUM):
`platform 0c91421d` (= origin/main) · `app ad9f3c49` (= origin/main) · `next-web-app 8297c684` (origin/main `f97ba659`) ·
`sentinel f2c46190` · `studio-desk 41ee3575` · `ant-academy 22df69dd` · `storage 4ce8ece5` (origin/main `9f8cb532`) ·
`messenger fa47850d` (origin/main `e9421c68`) · `cms ca50c817` (origin/main `f38c0c4a`) · `graphql-wundergraph 60c229f3` ·
`roadrunner 87d8d443` · `jobsimulation 462343b0` (origin/main `82cb66ec`) ·
**nested** `app/studio` and `cms/studio` both `aeec036a` ·
`rosetta-extensions` **pinned per-stack** `09d06070`, **authoring** `1dc1eb82`.

Trees named per finding. Nested `studio` repos grepped at their **own** ref (`aeec036a`), never at the host's.

## Positive controls

| file | `wc -l` | read in full? |
|---|---|---|
| corpus/services/studio-room.md | 512 | yes |
| corpus/services/backend.md | 396 | yes |
| corpus/services/graphql-wundergraph.md | 280 | yes |
| corpus/services/messenger.md | 207 | yes |
| corpus/architecture/frontend_architecture.md | 116 | yes |
| corpus/architecture/README.md | 38 | yes |

(Also read in full, as the briefing directs: `corpus/ops/platform-alignment.md` §5, lines 249–1662.)

---

## Blockers

### B1 — messenger.md claims Redis holds "scheduled-message storage"; no such storage exists, and the same file says scheduling is unimplemented

- **Anchor:** `corpus/services/messenger.md:127` — *"* **Redis** — Watermill stream subscriber + scheduled-message storage"*
- **Why it is false:** Enumerated every non-test Go file in `messenger@fa47850d` (28 files, listed via `git ls-files '*.go' | grep -v _test`). Redis is constructed in exactly **two** places, both for the Watermill stream client: `cmd/root.go:108` (`redis.NewClient(os.Getenv("REDIS_ADDR"), redisStreamsIndex)`, feeding `pubsub.NewSubscriberServer` at `:113`) and `cmd/trigger.go:23` (the publisher CLI). A sweep for any key-write op (`.Set(`, `.HSet(`, `.SetEx(`, `.ZAdd(`, `.LPush(`, `Expire(`) over all non-test Go returns **one** hit — `internal/flow/organizations.go:225`, `h.cache.Set(...)`, which is the in-process `patrickmn/go-cache` built at `cmd/root.go:144`, not Redis.
  There is nothing to store: `internal/rpcsrv/rpcsrv.go:25-30` returns `CodeUnimplemented` for **both** `CancelScheduledMessage` and `Schedule`. The only "scheduling" anywhere is Brevo-side — `internal/messenger/brevo/brevo.go:289`, `scheduledAt := time.Now().Add(30 * time.Second)`, handed to Brevo as `SendSmtpEmail.ScheduledAt` (`:305`).
  **Self-contradiction inside the same file:** `messenger.md:17` says *"(Scheduling RPCs exist in the proto but are not yet implemented — they return Unimplemented.)"* and `:112-113` book both methods as `Unimplemented` stubs. `:127` names a storage surface for a feature the file elsewhere says does not exist.
- **Confidence:** high

### B2 — the code map puts Liquid rendering in `internal/messenger/message/`, where there is none

- **Anchor:** `corpus/services/messenger.md:90` — *"    message/                 Message types + Liquid rendering"*
- **Why it is false:** `git -C stack-demo/messenger grep -n liquid fa47850d -- '*.go'` (non-test) returns **three** files and **none** is `internal/messenger/message/`: `internal/flow/assignments.go:18,489`, `internal/flow/whitelabel.go:8,17`, `internal/messenger/console/console.go:16,26,33,71`. Reading the whole package confirms it: `internal/messenger/message/message.go` is `New` / `NewWithDefaultSender` / `DefaultSenderUser` / `ConvertProps` (JSON→`map[string]any`); `validator.go` is `ValidateMessage` / `ValidateUser`; `errors.go` is error values. No template engine, no rendering.
  This is the "anchor resolves but names the wrong construct" class (§5 rule 21) applied to a directory: a reader looking for the rendering path is sent to the one package that does not do it, and away from `flow/whitelabel.go` — which the same file's `:98` correctly annotates *"Per-org whitelabel rendering (subject + body)"*.
- **Confidence:** high

### B3 — the send mechanism is described backwards: Brevo renders the template server-side; the corpus says the body is rendered locally before the send

- **Anchor:** `corpus/services/messenger.md:115` — *"Messages carry user info, template ID, and template params; the body is rendered through Liquid against those params before the Brevo send."*
- **Why it is false:** `messenger@fa47850d:internal/messenger/brevo/brevo.go:288-308` builds `brevo.SendSmtpEmail{… TemplateId: templateId, Params: props}` and posts it at `:310` (`TransactionalEmailsApi.SendTransacEmail`). No body is composed locally on that path — the template id and the parameter map go to Brevo and **Brevo** renders. The repo says so itself, in the doc comment on the one function that *does* render locally: `internal/flow/whitelabel.go:13-15` — *"renderWhitelabelBody parses and renders a Liquid template … **mirroring how Brevo exposes template parameters as `{{ params.xxx }}` inside the email body**."* Local Liquid rendering exists on exactly two paths: the **whitelabel / custom-body** composition (`flow/whitelabel.go:16-24`, `flow/assignments.go:489`), whose output is passed onward as `custom_subject`/`custom_body` params, and the **console sender used only when `BREVO_KEY` is empty** (`internal/messenger/console/console.go:71`). The general claim states the special case as the rule.
  The same over-generalisation seeds `:14-15` (*"using **Brevo** … as the delivery backend and **Liquid** templating for the bodies"*).
- **Confidence:** medium (the whitelabel path *is* Liquid-before-send, so the sentence is half-true; it is booked because it is written as the mechanism for all sends, and because it is the second site of B2's error)

### B4 — `app` has had TWO Atlas pipelines since sentinel-in-app (M1001); backend.md documents only one, and the second is invisible everywhere in the corpus

- **Anchor:** `corpus/services/backend.md:377` — *"Versioned Atlas migrations live in `terraform/migrations/` (per `atlas.hcl`: `dir = "file://terraform/migrations"`, source `ent://internal/data/ent/schema`). … apply with `atlas migrate apply --env local` (or `make migrate S=app`)."*
- **Why it is false:** The sentence names no ref, so it is graded at the checkout `app ad9f3c49`. `git -C stack-demo/app show ad9f3c49:atlas.hcl` declares **two** envs. `env "local"` is quoted correctly. Below it, under the header *"sentinel-in-app v10.0 / M1001 — the SECOND Atlas pipeline"*, `env "sentinel"` declares `src = "file://terraform/sentinel/schema.sql"`, `dir = "file://terraform/migrations-sentinel"`, `revisions_schema = "sentinel"` — and its own comment says *"app takes custody of the `sentinel` schema (one table: casbin_rules, the platform's Casbin policy store) … a SEPARATE env with its OWN source, migration directory, atlas.sum and revisions table."* The directory is real and populated: `terraform/migrations-sentinel/20260804151548_adopt_casbin_rules.sql` + `terraform/migrations-sentinel/atlas.sum` + `terraform/sentinel/schema.sql`. `app` CHANGELOG **v1.369.0 (2026-08-05)** records *"(atlas) second Atlas pipeline for the sentinel schema (M1001)"* and *"(sentinel) seed the default policy set from app, and wire it into pgtest (M1002)"*.
  Operationally material: the documented commands (`atlas migrate apply --env local`, `make migrate S=app`, which the platform Makefile derives from `repos.yml`'s `migrations: true` set = `app` only) apply **only** the public pipeline. Nothing in the corpus tells a reader the sentinel pipeline exists — `grep -rn "sentinel-in-app\|migrations-sentinel\|M1001" corpus/ CLAUDE.md` returns **0**, while `corpus/ops/setup_guide.md:457-461` and `corpus/ops/idempotency.md:38` still describe `casbin_rules` as created by sentinel's own adapter and seeded only by `init_policy.sql`.
- **Confidence:** medium (hesitated because the quoted `atlas.hcl` fragment is verbatim-correct for `env "local"`; booked because the sentence is a scoping claim — *where app's versioned Atlas migrations live* — and it is now wrong for one of two directories, in the file that is the corpus's authority on `app` migrations)

### B5 — `lab.v1.LabSessionService` is the SIXTH handler on `app`'s mux, not "a third … after Users and Organizations"

- **Anchor:** `corpus/services/backend.md:314` — *"Registered as a third RPC handler in `main.go` after Users and Organizations."*
- **Why it is false:** Read the whole registration block at both refs the file itself names. At `app b948604f` (`main.go:1185-1228`): `usersv1connect` `:1187`, `organizationsv1connect` `:1188`, `skillerv1connect` `:1196`, `jobsimulationv1connect` `:1204`, `cmsv1connect` `:1212-1214` (conditional), `labv1connect` `:1227-1228` — LabSession is **sixth**. Identical shape at `ad9f3c49` (`:1297-1338`), LabSession at `:1337-1338`.
  Same-file contradiction, twice: `backend.md:30` says *"The mux carries **six** handlers in total"* and enumerates LabSession among them; `backend.md:106` gives the per-line enumeration with LabSession last, at `:1228`.
  It **was** true at the commit the bullet names: `9ecade240` (2026-05-29, *"add LabSession Ent entity + Connect-RPC service (Phase B PR 2) (#896)"*) registered it at `main.go:249`, immediately after Users (`:240`) and Organizations (`:243`), with nothing else on the mux.
- **Confidence:** medium (hesitated hard — the bullet cites a PR, which is a form of dating, and the briefing warns that over-booking a dated claim is the most common rejection. Booked because the sentence is present-tense, sits in a bullet the corpus has since amended in place — *"The real HTTP client has since **LANDED**"*, with a `b948604` pin — and because a reader enumerating the mux from this line gets a count the same file refutes twice)

---

## Minors

- `backend.md:19` — *"compose now declares **five** services"* is unqualified. `docker-compose.yml` @ `0c91421` declares five (`sentinel:5`, `backend:28`, `studio-desk:112`, `next-web-app:143`, `gotenberg:170`), but the compose **project** declares seven once `include: common.yml` (`:1-2`) adds `postgresql` and `redis`. The sibling site carries the qualifier — `service_taxonomy.md:72`: *"declares five services (seven in the effective topology, once `include: common.yml` adds the …)"* — and §5 rule 30 makes the `include:` resolution load-bearing. Worse, `core` also starts exactly **five containers** (a *different* five), so the bare number invites the wrong reading.
- `backend.md:92` — `jobsimulation/terraform/main.tf:15-22` carries no ref of its own. It resolves exactly at the `6092c6d2` named one clause earlier (and at origin/main `82cb66ec`), but the **checked-out** clone is `462343b0`, where `6092c6d2` is *not* an ancestor and `:15-22` is the `data "atlas_migration"` block — the silent-slide the map itself warns about. The map's twin cell (`platform-migration-status.md:89`) does carry `@ 82cb66e`.
- `backend.md:310`, `backend.md:313` — *"(`feat/hiring-talk-to-data` branch)"* and *"(`feat/taxonomy-translations` branch)"*. Both merged and deleted: `520f24d47 Merge pull request #878 from anthropos-work/feat/hiring-talk-to-data`, `6ca1a4a89 Merge pull request #877 from anthropos-work/feat/taxonomy-translations`; neither appears in `git branch -r` (105 remote branches present, so the listing is not empty). `feat/company-context-m1m2` (`:312`) *does* still exist.
- `messenger.md:181` — the table's middle column is headed *"Value in the deleted compose block"*, and the `BREVO_KEY` row gives `(empty)`. No compose file ever set `BREVO_KEY`: `git -C stack-demo/platform log -S 'BREVO_KEY' --all -- docker-compose.yml` returns **nothing**, and `grep BREVO 0dab54d:docker-compose.yml` is empty. The value arrives via `env_file: .env` from `.env_example:82` (`BREVO_KEY=`). Effect is right; provenance is not.
- `messenger.md:123` — *"the binary still constructs four Connect-RPC clients"*. `cmd/root.go:118-142` constructs **five** client objects (cms, users, organizations, skiller, jobsimulation) over **four** distinct addresses (users and organizations share `BACKEND_USERS_RPC_ADDR`). The four-way grouping is defensible for addresses, not for clients.
- `frontend_architecture.md:32` / `:54` say `packages/graphql` holds *"generated hooks"*, while `:55` says *"React Query hooks are hand-authored on top of these typed documents."* The code sides with `:55`: `packages/graphql/src/__generated__/` @ `8297c684` holds only `fragment-masking.ts`, `gql.ts`, `graphql.ts`, `index.ts` (0 files matching `useQuery|useMutation`); the hooks live in the hand-written `packages/graphql/src/hooks/**`. `codegen.ts` uses `preset: 'client'` with `documentMode: 'documentNodes'`, which emits documents, not hooks.
- `graphql-wundergraph.md:177` — the historical offsets pair unlike constructs. The current pair `:135`/`:160` is runtime-env/runtime-env; the quoted history `:220`/`:236` (@`0dab54d`) and `:334`/`:352` (@`2adcf71`) is runtime-env/**build-arg**. The matching runtime lines are `:245` and `:361`. All four numbers resolve; the pairing does not carry over.
- `studio-room.md:35` cites `runCommand` at `:1099-1101` while `:345` cites `:1096-1101` for the same construct (`:1096-1098` is the doc comment, `:1099` the signature). Both land; the doc disagrees with itself by three lines.

---

## Positively cleared (audited zeros)

**ENUMERATED** — predicate re-derived from source independently, cardinality compared:

1. `frontend_architecture.md:41-50`'s whole derivation table, at `next-web-app 8297c684`. I built the file set myself (`git grep -l NEXT_PUBLIC_BACKEND_API_URL`, then `.ts`/`.tsx`, `e2e/` and `*.test.*` excluded) rather than trusting the doc's: **22 files** (matching the 22 the `:39` prose enumerates, item for item), **31** env occurrences, **47** `fetch(` sites, core-js four = **12 env / 27 calls**. Re-ran at `origin/main f97ba659`: identical 22/31/47, as `:50` claims. `bb3313bc..8297c684` = **41** commits / **192** files, as stated. `new EventSource(` = 0 across the tree **and** in the NUL-bearing `apps/web/src/hooks/useCoursebuilder.ts` (1 NUL byte, measured with `tr -dc '\000' | wc -c`), which `git grep` skips; `packages/core-js/src/talkToData/api.ts:214` carries the quoted reason verbatim.
2. `graphql-wundergraph.md:41-53` subgraph ladder. Enumerated `name:` entries in `supergraph-config-prod.yaml` at each commit myself: `749dc86~1`=**5** (backend/skiller/jobsimulation/cms/skillpath), `749dc86`=**4**, `7c17e63`=**3**, `915da06`=**1**. `git show --name-status 915da06` marks **both** `schemas/cms.graphqls` and `schemas/jobsimulation.graphqls` `D`; its subject reads *"supergraph 2→1"* and is wrong, exactly as the doc says.
3. Profile-token set. Enumerated every `profiles:` key across `docker-compose.yml` **and** `common.yml` @ `0c91421`: `{core, backend, all, studio-desk, frontend}` = **five**; `postgresql`, `redis`, `sentinel` declare none → always-on floor. At `0dab54d` the same enumeration gives **eight** (adds `storage-legacy`, `customerio-sync`, `messenger`), all three removed by `838d907`. Confirms `graphql-wundergraph.md:15-19`, `:190-192`, `backend.md:240`.
4. `messenger.md:17`'s handler count. Read `flow.go:70-104` @ `fa47850d` and counted by hand: **22** `pubsub.EventHandler(` lines in the `backend` subscriber, of which `:83` (`OrgJobSimulationAssignmentPastDueHandler`) is commented out `// not implemented` → **21 live**. Exact.
5. `messenger.md:20-31`'s absence proof, re-run with all three instruments named in §5 rule 44. Enumerated all **15** git trees under `stack-demo` (13 top-level + the two nested `studio` checkouts) and grepped each at its own HEAD: `MESSENGER_RPC_ADDR` = **0** everywhere. The 2 `MESSENGER_RPC` hits in `app` are `knowledge/plan/roadmap-support-in-app.md:480,768`, both asserting its absence. `git -C stack-demo/platform log -S 'MESSENGER_RPC' --oneline 0c91421d` = **0** over **121** commits; positive control `SKILLER_RPC` = **7**; `--all` → **8** and **0**, the 8th being `464dfe3` on `remotes/origin/feat/cms-in-app`. Every figure exact.
6. `backend.md:157-176`'s two-mechanism terraform derivation. Counted **44** tracked `.tf` across the 13 top-level clones (per-repo: app 5, cms 4, gql 4, jobsim 6, messenger 4, next-web 2, roadrunner 4, rext 2, sentinel 4, storage 5, studio-desk 4, ant-academy 0, platform 0) and **59** raw filesystem `.tf`. `http://backend.internal.anthropos:8081` occurs **6** times, **0** in any `.tf`: 1 in `app/knowledge/service-dependencies.md:52` (past tense, under *"There are no external callers of app's RPC mux left."*) and 5 in `rosetta-extensions` **@ the pinned `09d06070`** — 4 in `stack-core/tests/fixtures/repair_leak/{pre,post}/corpus/services/cms.md:32,157` plus `stack-core/tests/test_platform_predicate_guard.py:435`. `internal.anthropos` in `.tf` = **0**.
7. `backend.md:379`'s migration figures. Counted `terraform/migrations/*.sql` myself: **170** @ `ad9f3c49`, **169** @ `b948604f`; **46** with a `>= 202606` prefix; `20260529072659_add_lab_session.sql` is #124 of 170, i.e. 46 back from the head; heads and the three predecessors are byte-exact as listed. `git ls-tree <ref> migrations/` empty at all three refs; `6a46e8445` is *"remove obsolete atlas.sum file"* (2026-06-18).
8. `backend.md:309`'s **13** `ai_readiness_*` tables. Enumerated twice and independently — `internal/data/ent/schema/ai_readiness_*.go` = 13 files, and `CREATE TABLE "ai_readiness…"` across `terraform/migrations/*.sql` = 13 distinct table names. The four named as "what a 9 omits" and the two named as *not* omissions are all correct members.
9. `backend.md:342` / `:63-75` Redis-stream shape. Enumerated every `pubsub.NewPublisher` call in Go at both refs: @ `b948604f` → `main.go:287, 637, 1039` + `internal/jobsimwiring/wiring.go:127, 180` (**5**); @ `ad9f3c49` → `main.go:325, 746, 1149` + `wiring.go:132, 185` (**5**). Subscribers enumerated from `buildStreamSubscribers` (`subscriber_wiring.go:203-250`): SkillPath, Skiller, JobSimulation, CMS, AIUsage, Backend (**6**). `SKILLER_STREAM` in Go @ `b948604f` = exactly one line, `main.go:1276`, an `AddSubscriber`; no publisher names it at `b948604f`, `2035f9a4` or `ad9f3c49`. Six subscribers / five publishers, skiller consumer-only — confirmed.
10. `backend.md:29-34`, `:106` mux enumeration — read the full block at both named refs; all six handlers and all six line anchors exact, `CMSService` conditional on `if cmsRPCServer != nil`. `SkillPathSessionService` = **0** occurrences in `app` Go @ `ad9f3c49` (positive control `JobSimulationService` = 14), present only in `app/CLAUDE.md:109` and `app/knowledge/architecture.md:28`, exactly as the doc says.
11. `studio-room.md:365-395`'s mistral claim, at the nested repo's **own** ref. `git -C stack-demo/app/studio grep -ci mistral aeec036a` → `requirements.txt:1`, `tools/pdf2md.py:18`, `tools/r3.py:3` = **22 across 3 files**. `requirements.txt:8` = `mistralai`; `pdf2md.py:24` = `from mistralai import Mistral`; `:96` client; `:127` `model="mistral-ocr-latest"`; `r3.py:194` the `MISTRAL_API_KEY` gate. `services/ai.py:705-708` registry = `{openai, azure, anthropic}`; `:1-2` imports only `openai`/`anthropic`. `.gitignore:2` = `tools/`; `app/.gitignore:79` and `cms/.gitignore:129` = `studio/*`. Every ignore-mechanism claim reproduced.
12. `studio-room.md:186`'s **nine** `gen.py` arguments — counted `parser.add_argument` at `gen.py:484-492`: exactly nine, and the doc's option list matches flag-for-flag including `dest: forced` and the `{stable,experimental}` choices. `template` has **zero** consumers outside `translate_legacy_blueprint`'s `pop` and two unit tests.
13. `messenger.md:29-31` / `backend.md:319`'s "`d11a403` moved two, not four". Enumerated `*_RPC_ADDR` on messenger's block at `d11a403^` (`:255-265`: users `backend:8083`, cms `cms:8091`, jobsim `jobsimulation:8401`, skiller `backend:8083`) and at `d11a403` (all four `backend:8083`). `BACKEND_USERS_RPC_ADDR` traced to birth: introduced `3e85fce` (2024-04-18) as `http://backend:8081`, only ever the port moved. At `0c91421` `git grep _RPC_ADDR -- docker-compose.yml common.yml .env_example` = **0**.
14. `architecture/README.md:21`'s shared-library split. Read every `go.mod` in the clone set at its own ref: `ai` required only by `cms` and `jobsimulation` (both frozen husks); `authn` required by **none**; `app` carries the code in-tree at `internal/ai/` since `1e457fa70` (*"fold the ai library into app as internal/ai"*, 2026-08-04). Every file the index links exists; both rext paths it names exist in the **pinned** clone `09d06070`.

**RE-DERIVED** (weaker — arithmetic the document displays, re-computed): none reported on its own; every count above was re-derived from an independently enumerated set first.

Additional exact-anchor clearances, opened and read with surrounding context (not grepped to): `graphql-wundergraph/terraform/main.tf:20`, `locals.tf` (project/port), `subgraphs.conf` (byte-exact `BACKEND=v1.360.0`), both Dockerfiles (`cosmo/router:0.275.0`, `wgc@0.104.0`, `node:22.11-alpine`), `ci/update-subgraph.sh:9` (exactly one `gh release download -R anthropos-work/app`), `config.{compose,dev,prod}.yaml`, repo `CLAUDE.md:85` (the stale `-local.yaml`) and `:1` (the `graphql-wundegraph` misspelling), the full compose-history chain `63d285c → d92e84e → a2a3ee6 → 719befb → 2c85211 → 67ba772 → 360efd4`, the `graphql` service's `depends_on` at `2adcf71^` (backend/jobsimulation/cms/storage), `docker-compose.yml:48/57/59/110/119/135/151/160/183`, `Makefile:10`, `repos.yml` (4 @ `0c91421`, 9 @ `2adcf71`, messenger at `:21-23` @ `0dab54d`), `app/main.go:15/62/63/230-232/295-300/504/524/525/743-746/1195-1199/1205-1211/1353/1444-1485/1534-1541/1579-1581`, `app/subscriber_wiring.go:112-113/203-250`, `app/internal/authorization/gqlauthz/gqlauthz.go:82-97/175-186` + `gqlauthz_test.go:132/176`, `app/internal/web/backend/backend.go:315/317/137-138/324`, `app/internal/rpc/server.go:39` + `main.go:1353` (10 s default → 60 s), `app/atlas.hcl` env `local`, `app/terraform/{locals.tf:6,8, main.tf:58,185-186, variables.tf:197,230 @ b948604f; :309 + storage_s3_* @ ad9f3c49}`, `app/Dockerfile:28/45-46`, `app/.github/workflows/build-production.yml:28-30` (`additional_repo: "anthropos-studio-room:studio"`, v1.360.1 = `851cf3f`), `cms 6efa1d5`/`f38c0c4` (quoted commit body verbatim-correct) and `cms/terraform/main.tf:39`, `jobsimulation 6092c6d2` + `terraform/main.tf:15-22`/`:24-40` at that ref, `messenger/terraform/main.tf:19 @ fa47850d` (=1) and `:29 @ e9421c68` (=0) with the `:27-28` rollback comment, `messenger/cmd/root.go:63,64,107,118-142,147`, `internal/flow/{flow.go:74-78, jobsimulations.go:140-151, assignments.go:828}`, `internal/rpcsrv/rpcsrv.go:25-30`, `next-web-app` `apps/web/package.json:46` (`~16.2.12`), root `package.json` (`>=24.0.0`, `pnpm@10.30.3`), `pnpm-workspace.yaml` `!apps/mobile`, all app ports (3000/3001/3002/3031), 8 i18n locales, `packages/graphql/codegen.ts`, and — in `app/studio @ aeec036a` — `gen.py:{14-16,18-28,116-117,205-238,241-271,273-282,450-456,484-492}`, `services/ai.py:{1-2,35-42,383,530,664,705-708}`, `postgen.py:395-399`, `cert.py:1-19`, `agents/simulation/model.py:{59,467-469}`, `agents/simulation/postgen/exporter.py:{513-550,518-519}`, all three `configs/*.ini` (`max_tokens = 4000`, the five stable + five experimental model rows, `worklog_path = workspace/trace`), `tests/e2e/blueprints/technical.json` (the doc's excerpt is key-for-key correct), `services/taxonomy.py:11`, and `app/internal/cms/{studio/studioManager.go:93-96,119,1045,1096-1101 ; worker/worker.go:27-34}` (`Concurrency: 5`, `AiVideoQueue: 7`, `StudioQueue: 3`).

One near-miss worth recording, because it looks like a finding and is not: `studio-room.md:67-68` claims *"0 hits for certificate/signing/private_key/x509"*. A plain substring grep at `aeec036a` returns **3** (`story.py:123` *"Designing"*, `story.py:1071` *"assigning"*, `knowledge/development/gaps.md:333` *"designing"*). With word boundaries — the predicate the sentence actually means — it is **0**. Cleared, not booked.

---

## What I could not settle

- **`graphql-wundergraph.md:8`** — *"the repo: **ARCHIVED on GitHub, 2026-07-30** (read-only)"*. Archive state lives in the GitHub org API, never in git objects. Settled only by `gh api repos/anthropos-work/graphql-wundergraph --jq .archived`. (Note the clone gives no counter-evidence here: HEAD `60c229f3` predates 2026-07-30 and nothing has landed since — unlike `jobsimulation`, where four post-archive-date commits exist.)
- **`backend.md:137-143`** — the prod taxonomy counts (`public.skills` org-NULL = 42,790 of 43,584; `job_roles` = 22,490; `categories` = 23; `specializations` = 1,447; `skill_embeddings` = 43,584, measured 2026-07-08). No DB access in this reading. Settled by a read-only prod query.
- **`backend.md:90`** — *"`jobsimulation`'s ECS service is **already destroyed**"*. What I measured is that `6092c6d2` deletes the `module "jobsimulation"` block from that repo's terraform root, which is real and exact. Whether the corresponding `infrastructure` apply has run is not visible from any clone (`infrastructure` is in no clone set) — the same limit the adjacent `cms` bullet (`:84-86`) explicitly refuses to assert past. I did **not** book this: the fenced map (`platform-migration-status.md:89`, `:271`) takes the identical position, so it is a corpus-wide reading rather than a local contradiction, and the sentence's evidence clause is measured. It is flagged here because the two rows are graded by different standards from the same kind of evidence. Settled by reading `infrastructure`'s `services.tf`, or by `aws ecs describe-services`.
- **`messenger.md:53`** and the `cms`/`roadrunner` prod rows generally — every `service_desired_count` I checked is a *declaration* in the service repo, never an applied state.
