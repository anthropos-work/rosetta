# Seat D — reading #29/#30 (M257x iter-116)

**Ground truth re-derived at this reading's open** (matches the ADDENDUM exactly):
`app ad9f3c49` · `platform 0c91421d` · `next-web-app 8297c684` · `sentinel f2c46190` ·
`studio-desk 41ee3575` · `ant-academy 22df69dd` · `storage 4ce8ece5` · `messenger fa47850d` ·
`cms ca50c817` · `graphql-wundergraph 60c229f3` · `roadrunner 87d8d443` · `jobsimulation 462343b0` ·
`stack-demo/rosetta-extensions 09d06070` (pinned consumption) · `.agentspace/rosetta-extensions 1dc1eb82`
(authoring) · nested `stack-demo/app/studio aeec036a`.

**Which tree I read, per claim class:** claims about what the *tooling does on a stack* were checked in
**both** rext trees (they are byte-identical on every anchor I opened — I state this rather than assume
it); claims about a *fence/DNA configuration or score* were settled in the **authoring copy**
(`1dc1eb82`), per §5 rule 50. `stack-demo/app/studio` was grepped **at its own ref `aeec036a`**, never
through `app`'s HEAD (§5 rule 44).

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/architecture/service_taxonomy.md` | 523 | yes |
| `corpus/services/clerkenstein.md` | 374 | yes |
| `corpus/architecture/security_compliance.md` | 293 | yes |
| `corpus/services/sentinel.md` | 166 | yes |
| `corpus/services/ai-labs.md` | 158 | yes |
| `corpus/services/db-backup.md` | 31 | yes |

(Total 1,545 lines. `corpus/ops/platform-alignment.md` §5 read in full, lines 249–1663, as instructed.)

---

## Blockers

### B1 — the "private-module set a stack builds" is stated as THREE; `app` requires FIVE, and the corpus already says five elsewhere

- **Anchor:** `corpus/architecture/service_taxonomy.md:175` —
  > "**Shared Libraries** (imported as private Go modules — **not** cloned by `make init`; pulled at Docker
  > build via `GH_PAT`/`GOPRIVATE`). **`ai` is NO LONGER among them** … **the live private-module set a
  > stack builds is colony, proto, taxonomy**."

  Second site, same claim, same file: `:518` — "**5 libraries, 3 imported by a service a stack builds** —
  colony, proto, taxonomy".

- **Why it is false:** I enumerated the set from source rather than re-checking the sum.
  `git -C stack-demo/app show ad9f3c49:go.mod` — the **direct** require block, lines 14–18, is:

  ```
  14: github.com/anthropos-work/analytics-go v0.3.1
  15: github.com/anthropos-work/colony       v0.35.2
  16: github.com/anthropos-work/proto        v1.210.0
  17: github.com/anthropos-work/storage      v0.15.2
  18: github.com/anthropos-work/taxonomy     v1.2.0
  ```

  All five are real build inputs, not tidy leftovers:
  - `anthropos-work/storage` is imported by **32** `.go` files in `app` (e.g.
    `app/internal/academy/asset.go:11` `storagev1 "github.com/anthropos-work/storage/sdk/storage/v1"`;
    `app/internal/cms/wiring.go:36` `"github.com/anthropos-work/storage/sdk/storage"`).
  - `anthropos-work/analytics-go` is imported by **2** (`app/internal/payments/handler.go:10`,
    `app/internal/tracking/handler.go:4-5`).
  - `app/Dockerfile.dev:13` sets `ENV GOPRIVATE=github.com/anthropos-work/*` and `:11` installs the
    `GH_ACCESS_TOKEN` `insteadOf` rewrite — so **all five** are pulled through `GH_PAT` at image build.
    That is exactly the operational question this paragraph exists to answer.
  - `sentinel` (`f2c46190:go.mod`) requires colony + proto + taxonomy(indirect) — so no other clone
    supplies the missing two; they are `app`'s.

  **And the corpus contradicts itself on the point.**
  `corpus/architecture/external_services.md:554` states the correct enumeration verbatim:
  *"`app/go.mod:14-18` requires `analytics-go`, `colony`, `proto`, `storage`, `taxonomy` and nothing
  else"*. Two sibling architecture docs, opposite answers, no retraction between them.

  Note the trap this sits in: the corpus has (correctly) been removing the **`storage` service** from
  compose and `repos.yml`, and `clerkenstein.md:279` even calls it *"the frozen `storage` repo alone,
  which nothing clones or builds."* The **`storage` Go module** is a different object and `app` builds
  against it every time. Conflating them is precisely the "named-consumer list survives the merge"
  failure mode §5 records.

- **Why I hesitated:** `:518`'s phrasing ("5 libraries, 3 imported…") is defensible if the domain is
  silently restricted to the five rows of the table above it. `:175`'s is not — it names *the live
  private-module set a stack builds*, unqualified, and it is the sentence a reader with a failing
  `GH_PAT` build will act on. Booked on `:175`; `:518` listed as the second site.
- **Confidence:** high

### B2 — `service_taxonomy.md:403` points at the wrong blockquote for "the archive-state note"

- **Anchor:** `corpus/architecture/service_taxonomy.md:403` —
  > "the GitHub repo was **archived 2026-07-30** (a dated snapshot — see the archive-state note above the
  > *Archived / merged* table, **`:142`**; the clone is consistent with it, no commit after that date)."
- **Why it is false:** the archive-state note is the blockquote at **`service_taxonomy.md:148-160`**
  (*"Every `ARCHIVED <date>` in this table is a DATED SNAPSHOT, not a derived fact…"*). Line **142** is
  inside a **different** blockquote (`:139-146`), about a **different** proposition — the
  merged-into-app-vs-gone-from-compose phase split: *"so a bare `make up` started all three as
  unfederated husks — the `running_but_unfederated` state in…"*. The anchor resolves and names the wrong
  construct — §5 rule 46's class, and the same file warns at `:156-160` that same-file line pins into this
  growing table have already rotted twice ("**search the row name**"), then leaves this one live 240 lines
  above.
- The surrounding facts *are* right: `graphql-wundergraph`'s last commit at `60c229f3` (== `origin/main`)
  is **2026-07-30**, consistent with the asserted archive date.
- **Why I hesitated:** the prose description ("the archive-state note above the *Archived / merged*
  table") is unambiguous, so a reader recovers in seconds. I book it because the instrument names
  wrong-construct anchors as blockers and because this corpus's own adjudicators have upheld ≥7 of them.
- **Confidence:** medium

### B3 — clerkenstein.md says four FAPI handlers "discard (or ignore) the `*http.Request`"; one of them reads it, and the same file says so 40 lines earlier

- **Anchor:** `corpus/services/clerkenstein.md:171-176` —
  > "- **The read path takes NO request input.** `handleMe`, `handleToken`, `handleClient` and
  > `handleMeOrganizationMemberships` **all discard (or ignore) the `*http.Request`** and answer from
  > `activeUserLocked()`."
- **Why it is false:** three of the four do literally discard it — `clerk-frontend/server.go:241`
  `handleClient(w http.ResponseWriter, _ *http.Request)`, `:467` `handleToken(…, _ *http.Request)`,
  `:488` `handleMe(…, _ *http.Request)`. The fourth does **not**: `:512`
  `handleMeOrganizationMemberships(w http.ResponseWriter, r *http.Request)` binds `r` and reads it —
  `:527` `strconv.Atoi(r.URL.Query().Get("offset"))` and `:531`
  `strconv.Atoi(r.URL.Query().Get("limit"))` — to page the response. (Identical in **both** rext trees:
  pinned `09d06070` and authoring `1dc1eb82`.)
- **Self-contradiction:** the same document, at `:131-134`, asserts the opposite as a *correctness
  requirement*: *"**`limit`/`offset` are honoured** — clerk-js sends `limit=10&offset=0` and pages when
  `total_count` exceeds what it received, so serving the full list against `limit=10` makes it chase a
  second page that disagrees with the first."* Honouring them requires reading the request.
- **Why I hesitated:** the paragraph's load-bearing conclusion ("per-browser `storageState` cannot isolate
  two identities"; the seat is process-wide) is **true and unaffected** — the sibling clause
  `r.Cookie(...)` **is** called nowhere in `clerkenstein/` (measured: 0 hits for `.Cookie(`, with
  `Cookie` present in 5 files as the positive control). So the harm is bounded. I book it because the
  sentence states a code property that is false at a site it names by hand, and the corpus already
  contains the refutation.
- **Confidence:** high (on the falsity); medium (on materiality)

### B4 — `sentinel.md:85` cites `architecture_overview.md:335` for a quote that lives at `:339`

- **Anchor:** `corpus/services/sentinel.md:85` — "The correctly-scoped form is the model at
  [`architecture_overview.md:335`](../architecture/architecture_overview.md) — *"the only cross-process
  RPC edge out of backend on a core stack"*."
- **Why it is false:** `corpus/architecture/architecture_overview.md:335` is
  `**On a local stack** (platform `2adcf71` deleted the router — **there is no `:5050`**):` — a heading
  introducing a fenced block. The quoted string is at **`:339`**
  (`  → Connect-RPC to sentinel   (the only cross-process RPC edge out of backend on a core stack)`);
  `grep -n "only cross-process RPC edge"` returns exactly one hit in that file, at 339.
- **Corroborating that this is drift, not a convention:** the twin sentence at
  `service_taxonomy.md:434` cites the *same* document for the *same* quote and carries **no line
  number** — the deliberate de-anchoring §5 rule 34 prescribes. `sentinel.md` kept the number and it
  rotted.
- **Why I hesitated:** 4 lines, same block, quote visible immediately below. Same class as B2, booked the
  same way for consistency.
- **Confidence:** medium

### B5 — `security_compliance.md:197` cites `README.md:21`; the supporting statement is `corpus/architecture/README.md:23`, and `:21` is about a different subject

- **Anchor:** `corpus/architecture/security_compliance.md:197` — "**AI Token Tracking** | Centralized
  usage, latency, and cost tracking in **`app/internal/aiusage`** — **not** the shared `ai` library,
  which only wraps providers (consistent with **`README.md:21`** + `ai_architecture.md`)."
- **Why it is false:** `aiusage` occurs in exactly **one** README in the corpus —
  `corpus/architecture/README.md:**23**` (*"…and cost tracking (`app/internal/aiusage`)"*), the
  `ai_architecture.md` bullet. `corpus/architecture/README.md:21` is the **`shared_libraries.md`**
  bullet, a different subject. Under the other plausible resolution of a bare `README.md`, the repo-root
  `README.md:21` reads *"Project Rosetta is a **documentation repository** - not the Anthropos platform
  itself. It contains:"* — entirely unrelated. Neither resolution supports the citation.
- The substantive claim is **correct** (`app/internal/aiusage` exists; `app/go.mod` @ `ad9f3c49` requires
  no `anthropos-work/ai` module at all — it was folded in-tree at `1e457fa70`).
- **Why I hesitated:** the citation is decorative — nothing a reader believes changes. Booked for the
  same reason as B2/B4: an anchor that resolves onto a different construct, and here with an ambiguous
  file besides.
- **Confidence:** low–medium

---

## Minors

- `corpus/services/sentinel.md:5` — the **published derivation does not reproduce**. It says
  `git grep "authorization\|AUTHORIZATION_ADDRESS" fa47850d -- '*.go' go.mod` *"returns one unrelated
  hit"*. Measured: **zero** — `authorization`/`Authorization` (case-insensitive) occurs **0 times
  anywhere** in `stack-demo/messenger` at `fa47850d`. Positive controls in the same pass: 42 tracked
  `.go` files; `git grep -c "func " fa47850d -- '*.go'` returns hits; `colony` in `go.mod` returns 1.
  The conclusion (*messenger imports no authorization client*) is **strengthened**, not weakened — but a
  reader who runs the published command and gets 0 has no way to tell a broken pipeline from a stronger
  result (§5 rule 2).
- `corpus/services/clerkenstein.md:105` — the `cmd/` row enumerates `mintpk` · `fake-fapi` / `fake-bapi`;
  the directory also holds **`jwtkey`** (4 binaries, not 3). Omitted list member.
- `corpus/services/ai-labs.md:109-110` — the labs REST write surface is given as `POST /v1/labs` +
  `/v1/labs/bodies` + `GET /v1/labs/:slug/workspace.tar.gz`; `app/internal/web/backend/labs_admin.go:34`
  also registers **`POST /v1/labs/:slug/workspace`** (`h.UploadWorkspace`) inside the same
  `labs:write` group. Omitted list member.
- `corpus/services/sentinel.md:15` — "The sentinel binary's default is 8080 (per its own
  **README**/CLAUDE.md)". `git ls-tree -r f2c46190` shows **no `README.md`** in the sentinel repo. The
  fact is right and is in `CLAUDE.md:101` and `cmd/root.go:47` (`cmp.Or(os.Getenv("RPC_PORT"), "8081")`
  is a different port; the HTTP default is `8080`).
- `corpus/architecture/service_taxonomy.md:38` — the mermaid edge label reads *"runs studio/gen.py
  **in-process**, argv exec — no shell"* while the correcting blockquote 16 lines below (`:54-57`) says
  the cms domain *"runs the pipeline as a **subprocess of its own container**"*. Same container, but
  "in-process" and "subprocess" are opposite words for the mechanism; the diagram is the part a skimmer
  reads.

---

## Positively cleared (audited zeros)

**ENUMERATED** — predicate re-derived from source, cardinalities compared:

1. **`security_compliance.md:78-147` — the whole multi-tenancy fence** (the one that has been wrong four
   times). I extracted `internal/data/ent/schema/` at `ad9f3c49` and enumerated the *sets*, not the sums:
   - **139** `.go` files; **135** contain `ent.Schema`; the 4 that do not are exactly
     `database_types.go`, `mixin.go`, `skiller_mixins.go`, `skillpath_mixins.go`. ✔
   - `OrganizationMixin{}` mentioned in **30** files; `user_resource.go:22` reads
     `// OrganizationMixin{},  // We need to work on this` — commented, so **29 live users**. ✔
   - `OrganizationIDMixin{}` in **7**, and the named list matches byte-for-byte (`category`, `jobrole`,
     `similarity`, `skill`, `specialization`, `studio_document`, `studio_task`). ✔
   - Plain `"organization_id"` with neither mixin: **18**, and the doc's own derivation command
     reproduces it. Its documented gotcha reproduces too — the bare `*.go` glob returns **19**, and the
     extra file is exactly `skiller_mixins.go`. ✔
   - Of those 18, the two policed ones are `org_membership.go` (own `Policy()` at **`:172-188`**, ending
     `privacy.AlwaysDenyRule()` at `:185`, `rule.AllowCurrentOrgEdgesOrSkipRule()` at `:179/:183`) and
     `academy_feedback.go` (`UserMixin{}` at **`:64`**, whose `Policy()` at `mixin.go:98` applies
     `rule.FilterOwnerRule()` at `:103/:113`) → the remaining **16** are the exact 16 the doc names. ✔
   - **31 org-filtering** = 29 + `Membership` + `Organization` (`organization.go:56` `Policy()`,
     `rule.FilterSameOrganizations()` at **`:96`**, neither mixin). `User` (`user.go:116`) filters by
     user and is correctly excluded. ✔
   - **Only four** files in the directory declare `Policy()` — `mixin.go`, `org_membership.go`,
     `organization.go`, `user.go` — re-measured at `ad9f3c49`, not only at the pinned `5ba17044`. ✔
   - `135 − 31 − 23 = 81` remainder ✔, and the four tenant-scoped members all resolve to the right
     construct: `lab.go:63`, `academy_chapter.go:84`, `academy_skill_path.go:78` are
     `field.String("tenant_eid")`; `skill_path_session.go:43` is `field.UUID("tenant_id", uuid.UUID{})`;
     none carries `organization_id`; the first three declare no `Policy()`; `skill_path_session.go:26`
     carries `UserMixin{}`. `academy_feedback.go:129` is `field.UUID("organization_id", …)`. ✔
   - The platform's self-statements resolve: `job_simulation_session.go:5`, `jobrole.go:18`,
     `category.go:15`. ✔
2. **`service_taxonomy.md:71-135` — the compose topology.** `0c91421:docker-compose.yml` declares
   exactly five services (`sentinel:5`, `backend:28`, `studio-desk:112`, `next-web-app:143`,
   `gotenberg:170`); exactly four carry `profiles:` (`:110`, `:141`, `:168`, `:183`); `sentinel` carries
   none, and `common.yml`'s `postgresql:2` / `redis:24` carry none → floor = 3, `core` = 5, effective
   topology = 7. `repos.yml` lists 4 repos, `app` alone `migrations: true` (`:14-17`), `sentinel`
   `migrations: false` (`:18-20`), `Makefile:10` `PROFILE ?= core`. Legal profile tokens = exactly
   `{all, backend, core, frontend, studio-desk}`.
3. **`service_taxonomy.md:416` — the subgraph ladder.** Counted `- name:` in
   `supergraph-config-prod.yaml` at each named ref: `749dc86^`→**5**, `749dc86`(2026-06-24, skiller)→**4**,
   `7c17e63`(2026-07-21, skillpath)→**3**, `915da06`(2026-07-29)→**1**. `git show --name-status 915da06`
   deletes **both** `schemas/cms.graphqls` and `schemas/jobsimulation.graphqls` — so **3→1**, and the
   commit's own subject line does say "2→1". At `60c229f3` all three configs carry one `- name: backend`
   and `schemas/` holds one file.
4. **`sentinel.md:26-41` — the Casbin model.** `internal/authorization/casbin.go:14-44`: request defs
   `r,r2..r6` = **6**; policy defs `p,p2..p6` = **6**; role defs `g,g2,g3` = **3**; matchers `m,m2..m6`
   = **6**. Groupings match (`g(sub,tier)`, `g2(org,sub,role)`, `g3(org,membership)`).
5. **`sentinel.md:70-83` — the RPC surface.** `internal/rpcsrv/rpc.go` declares exactly **19**
   `RPCHandler` methods; the table's grouped list is the same 19, no more, no fewer.
6. **`sentinel.md` "no `manager` role".** `git grep -ci manager f2c46190 -- init_policy.sql` → **0**
   (control: `candidate` → 11 lines). It survives only in test files.
7. **`clerkenstein.md:41-45,71-79` — the DNA gene counts.** Parsed all five DNAs in the **authoring**
   tree: `clerk-2.6.0` **14 capabilities / 27 genes**; `clerk-deploy-1` 3/**7**; `clerk-express-1`
   **5**/**13**; `clerk-js-5` 6/**9**; `clerk-multi-1` 5/**9**. All match.
8. **`security_compliance.md:22-34` — the public-subnet retraction.** Enumerated all eight service
   terraform trees at their own checkouts: `public_subnet` occurs **0** times in every one; **8/8** pass
   `private_subnets_ids = var.platform_private_subnets_ids` (router at
   `graphql-wundergraph@60c229f3:terraform/main.tf:31`, `base_service` at `:11`, `use_fargate = false`
   at `:13`).
9. **`ai-labs.md:20` — the shared-purse retraction.** `git describe --tags ad9f3c49` →
   **`v1.369.0-7-gad9f3c498`** exactly; newest tag `v1.369.0`; `git describe 2035f9a4` →
   `v1.369.0-2-g2035f9a40` exactly; `checkout.session.completed` occurs **0** times in Go source
   (control: `customer.subscription` hits 3 files); `internal/labs/` imports `internal/credits`
   **nowhere** (control: it imports other `app/internal` packages); `credits/handler.go:12` carries the
   Wave-13 removal note; `knowledge/plan/releases/06.00-shared-purse/` exists with m600…m607.
10. **`ai-labs.md` cost model.** `internal/credits/cost.go:86-90` — `course.build` **5**,
    `course.refine` **1**, `course.translate` **1**; `:200-204` starter **50** / team **200** / scale
    **500**; `:133` `MarginMarkup = 1.40`; `:142` `PricePerCreditUSD = 0.45`; `:231`
    `DefaultSeedBalance = 500`. (The *platform's own* docstring at `cost.go:29` says "5 credits per
    refine turn" and contradicts its own map — a platform defect, not a corpus one; the corpus follows
    the map, which is correct.)
11. **`service_taxonomy.md:247` — Studio-Room's Mistral surface**, grepped at the **nested repo's own
    ref** `aeec036a` (never through `app` HEAD): `git -C stack-demo/app/studio grep -ci mistral aeec036a`
    → **22 lines / 3 files** (`requirements.txt` 1, `tools/pdf2md.py` 18, `tools/r3.py` 3). The only
    `import` is `tools/pdf2md.py:24` `from mistralai import Mistral`; `r3.py`'s three hits are
    `MISTRAL_API_KEY` env checks, not an import. `services/ai.py:705-708` is the whole provider registry
    (`openai`/`azure`/`anthropic`); `ai.py:1-2` imports only `openai`/`anthropic`.
12. **`service_taxonomy.md:271-277` — `gen.py` has no `--template`.** `gen.py` registers exactly **nine**
    `add_argument` calls (`:484-492`), the exact nine named, and `parse_argument` uses
    `parse_known_args` (`:18-19`).
13. **`service_taxonomy.md:518` — `authn` is a dependency of nothing.** Zero `anthropos-work/authn`
    requires across **all seven** Go clones; control — `colony` is required by all seven.

**RE-DERIVED** — arithmetic the document itself displays, recomputed (weakest form of clearance):

- `clerkenstein.md:41` — *"Was 97.2% / 26-of-27"*. 26/27 genes is 96.3%, so the number looked wrong. It
  is not: `alignment/internal/compare/compare.go:101` scores `alignedW/sumW`, and
  `internal/dna/dna.go:38-48` defaults weights to critical **3** / standard **2** / optional **1**. Total
  weight of `clerk-2.6.0` = **71**; the failing `MembershipOrgIdentity/real-org-eid` gene is `standard`,
  weight 2 → `(71−2)/71` = **97.2 %**. Exact. Gate `≥95 / =100` confirmed at
  `clerkenstein/alignment/scripts/gate.sh:31-32`.

**Individually verified anchors that resolved to the right construct** (opened, read around, not merely
grepped) — a non-exhaustive list of the ones most likely to rot:
`docker-compose.yml:48,53,56,57,59,84-92,110,141,168,183` · `0dab54d:docker-compose.yml:56,154` ·
`common.yml:2,24` · `Makefile:10` · `repos.yml:14-17,18-20` ·
`app/env_guards.go:37-44,92-111` · `app/main.go:394` · `app@3e5bc33ef:main.go:387` ·
`app/internal/cms/studio/studioManager.go:119,1096-1098` · `app/internal/converter/gotenberg.go:31` ·
`cms_queries.graphqls:106,107` + `cms_mutations.graphqls:22` (the M257x-iter-115 file split — correct) ·
`academy.graphqls` = 1 of 43 files ·
`app/internal/jobsimulation/ai/ai.go:85-95,129,166,263-277,325` ·
`app/internal/coursebuilder/bedrock.go:100,109-112` ·
`.../v3/validator/skills.go:53-62,75` + `validator.go:60` (exactly one engine registered) ·
`.../basevalidator/criterion.go:127,168,428,450-475` + `checkerEngines` stored and never read ·
`next-web-app apps/{web,hiring}/src/hooks/useOrgSelection.tsx:94`, `useResolveActiveOrg.tsx:107`,
`useActivateMembershipOrg.tsx:81` ·
`sentinel go.mod:3,8`, `Dockerfile:2`, `Dockerfile.dev:2`, `terraform/locals.tf:4-5`,
`init_policy.sql:63-66,88-118`, `local_superadmin_grants.sql` ·
`app/internal/data/ent/enum/membership.go:8-15` ·
`graphql-wundergraph terraform/main.tf:20,31,48-49`, `locals.tf:8`, `config.prod.yaml:5` ·
`cms/terraform/main.tf:39` (191 lines) + `6efa1d5`→`f38c0c4` ·
`jobsimulation/terraform/main.tf:15-40` **graded at the ref the cell names, `6092c6d2`**, where it is
exactly the "what this module still owns" block — at the *checkout* `462343b0` the module still exists,
which is a pin working as designed, not a finding ·
`a2a3ee6^:docker-compose.yml:384,386,409` (the historical Directus service) ·
`2adcf71^:docker-compose.yml:14` (`"5050:8080"`) ·
`d11a403` changed exactly **two** `*_RPC_ADDR` values · `BACKEND_USERS_RPC_ADDR` only ever moved ports
(`:8081`→`:8083`, introduced at `3e85fce`) · `AUTHORIZATION_ADDRESS` = **1** occurrence across
`docker-compose.yml`+`common.yml`+`.env_example`; `*_RPC_ADDR` = **0** in every compose file ·
`studio-desk .env.example:44,45`, `src/index.ts:96`, `app/services/userService.ts:16` (both with the
quoted comment) ·
`ant-academy code/src/graphql/server.js:14,18`, `src/lib/serverTenant.js:115-145`,
`src/lib/backendContent.js:36,102-103`, `app/api/academy/beacon/route.js:36,41-55`,
`app/layout.jsx:132`, `public/academy-manifest.json`, `tests/unit/next-scaffold.test.js:106,111`,
`tests/unit/react-compiler-config.test.js:41`, `code/package.json` (next ^16.2.6 / react ^19.2.5, no
serwist/workbox) ·
rext `alignment/cmd/alignctl/run.go:134-135`, `clerk-backend/store.go:138,151`,
`alignment/dna/clerk-2.6.0.json:131`, `clerk-frontend/server.go:35-68,102-107,554-559,637-657,663-670`,
`registry.go:24,67,75`, `resources.go:37-43,50-51`, `key.go:28`,
`server_test.go:256,286,390,427,461`, `stack-seeding/seeders/roster.go:47,53,54,77` ·
`app/internal/labs/session/labsession.go:58,90,252,278,333,386`,
`labs_budget.go:26-28,247-251`, `spend_reconciler.go:60` (30 s), `labsapi/client.go:6` (`:7070`),
`main.go:855` (`LABS_API_PLATFORM_TOKEN`), `internal/web/backend/labs_admin.go:31-40`,
`internal/data/ent/schema/lab_session.go:21-40` (no `PrimaryKeyMixin`, String id),
`lab_body.go:11-15,46-52` (EN + de/es/fr/it/nl/pt = 6 overlays), migrations
`20260529072659`, `20260617120000`, `20260617203555`, `20260626120000`, `20260717151144`,
`cmd/{labsimport,labskey,labsdemo}` + `:8099`, `internal/web/backend/api/api.go:319-327` (the exact
Stripe event set), `backend.go:129` (`/api/webhook/stripe`), `go.mod:64` (`stripe-go/v74`).

**One near-miss I deliberately did NOT book:** `ai-labs.md:104` says *"Credits has NO GraphQL — REST
only."* `grep -i credit` over `internal/web/backend/graphql/graph/schemas/*.graphqls` returns hits in
three files — but they are `setOrganizationFeatureCredits` / `organizationFeatureCredits`, Sentinel's
**org feature credit budgets**, a different subsystem from the `internal/credits` ledger. The claim
holds. Recording it because the count *looks* like a refutation and is not (§5 rule 17 in the opposite
direction).

---

## What I could not settle

- **`corpus/services/db-backup.md` in its entirety (31 lines).** There is no `db-backup` clone in
  `stack-demo/`, and `find stack-demo -name .git -maxdepth 4` confirms the search set does not contain
  one. Every claim in the file (6-hour cadence, S3 + Azure + Hetzner targets, Go, "PostgreSQL RDS (all
  schemas)", ECS-scheduled deployment) is therefore **unverifiable from this tree** — I can only say it
  is *internally consistent* with `security_compliance.md:167,174` and `service_taxonomy.md:173,521`.
  What would settle it: the `anthropos-work/db-backup` repo in the clone set, or `infrastructure`'s
  scheduled-task definition. **A reader should treat this file as prose-under-review** (§5 rule 27 row
  three), and it is not marked as such.
- **Repo *archive* state** — every `ARCHIVED <date>` in `service_taxonomy.md:162-170` and the router
  banner. `gh` is not installed on this host and the repos are private, so the GitHub org API is closed;
  no clone carries the bit. The file already discloses this (`:148-153`), and the disclosure is correct.
  I verified only the falsifiable half — `graphql-wundergraph`'s last commit is 2026-07-30 (consistent),
  and `jobsimulation` carries four commits dated 2026-08-04 past the asserted archive date (which is
  exactly what that row already retracts).
- **`cms`'s M810 prod state** — `cms/terraform/main.tf:39` still declares `service_desired_count = 0` in
  an intact 191-line module *and* `6efa1d5` deletes the build workflow saying the ECR repo is
  decommissioned. Both measured; the deciding artifact (`infrastructure`'s `services.tf`) has never been
  in a clone set. The corpus's "report both, assert neither" is the right posture and I could not improve
  on it.
- **`anthropos-work/AI-Labs`** (`ai-labs.md:5`) — not in the clone set. Corroborated only indirectly:
  `app/internal/labs/session/labsession.go:2,6` and `schema/lab_session.go:6` reference
  `AI-Labs/docs/PHASE_B_LABSESSION.md`, and `terraform/main.tf:657` wires `LABS_API_PLATFORM_TOKEN`.
  Consistent with the claim; not proof of the repo's existence or liveness.
- **`security_compliance.md`'s non-code compliance facts** — "DPA v1.4", "18 approved sub-processors",
  the sub-processor list, "90-day auto-deletion", TLS/KMS/CloudTrail/Better-Stack rows, the VPC CIDR and
  DR-site claims. These live in contracts and in `infrastructure`, neither of which this tree contains.
  Per §5 rule 48 I did not escalate anything legal; I note only that these rows are unfenced prose
  sitting beside a heavily-fenced tenancy blockquote, which is the mixing rule 27 warns about.
- **The `cms` / `jobsimulation` / `skillpath` "legacy husk" schemas** (`service_taxonomy.md:68`) — needs a
  live database. Not measurable from a clone.
