# Seat A — reading #29/#30 (M257x iter-116)

## Ground truth actually read (stated per rule 26)

Re-derived at this reading's open, and identical to the addendum's superseding table:

| clone | HEAD | notes |
|---|---|---|
| `stack-demo/app` | `ad9f3c49` | also read at `b948604`, `9d00a313`, `7177374`, `5ba17044` where a claim pinned them |
| `stack-demo/app/studio` (nested, own checkout) | `aeec036a` | grepped at **its own ref**, never the host's |
| `stack-demo/platform` | `0c91421d` | also read at `0dab54d`, `d11a403`, `838d907`, `2adcf71`, `1e8e754`, `a4db680`, `2c85211`, `b56d731`, `360efd4`, `a2a3ee6`, `c17cc9a`, `fdfa189` |
| `stack-demo/next-web-app` | `8297c684` | |
| `stack-demo/sentinel` | `f2c46190` | |
| `stack-demo/storage` | `4ce8ece5` | |
| `stack-demo/messenger` | `fa47850d` | |
| `stack-demo/cms` | `ca50c817` (origin/main `f38c0c4a`) | both read |
| `stack-demo/cms/studio` (nested) | `aeec036a` | |
| `stack-demo/graphql-wundergraph` | `60c229f3` | |
| `stack-demo/roadrunner` | `87d8d443` | |
| `stack-demo/jobsimulation` | `462343b0` (origin/main `82cb66ec`) | both read; `6092c6d2` read where a claim pinned it |
| `stack-demo/studio-desk` | `41ee3575` | |
| `stack-demo/ant-academy` | `22df69dd` | |
| **`stack-demo/rosetta-extensions` (pinned per-stack consumption clone)** | **`09d06070`** | **this is the tree I graded every rext claim at** (addendum / §5 rule 45); also read `ab81527a` where a claim pinned it |
| `.agentspace/rosetta-extensions` (authoring copy) | `1dc1eb82` | not used to settle any claim in this report |

Tree enumeration used for every "tree-wide" statement: `find stack-demo -name .git -maxdepth 4` → **15 git trees**
(the 13 named clones + the two nested `studio` checkouts). Nested repos were grepped at `aeec036a`, never
through the host ref.

## Positive controls

| file | `wc -l` | read in full? |
|---|---|---|
| `corpus/architecture/external_services.md` | 887 | yes |
| `corpus/services/jobsimulation.md` | 316 | yes |
| `corpus/services/customerio-sync.md` | 165 | yes |
| `corpus/services/askengine.md` | 147 | yes |
| `corpus/services/intelligence.md` | 18 | yes |

Also read in full as instructed: `corpus/ops/platform-alignment.md` §5 (lines 249–1663).

---

## Blockers

### B1 — `askengine.md` still calls the `ai` package "the shared `ai` library" — the fourth unrepaired instance of a predicate the corpus has already retracted in three siblings

- **Anchors:**
  - `corpus/services/askengine.md:81` — *"**Downstream**: **AWS Bedrock** (Claude Sonnet 4.6, eu-west-1); **PostgreSQL** (read-only, org-scoped); the shared **`ai`** library (`CreateEmbeddings`, text-embedding-3-small, for RAG golden-example retrieval)"*
  - `corpus/services/askengine.md:113` — *"…a Postgres with the platform schema, and an embeddings provider for the shared `ai` client."*

- **Why it is false (ground truth):**
  - `app` folded the library into its own tree at **`1e457fa70`** (*"refactor(ai): fold the ai library into app as internal/ai"*, 2026-08-04), which **is an ancestor of `ad9f3c49`** (`git merge-base --is-ancestor 1e457fa70 ad9f3c49` → rc 0). So the fold is in force at the ref this document itself pins two lines above (`askengine.md:89`, *"Measured at `app` `ad9f3c49` (`== origin/main`, 2026-08-06)"*). The pin does not save either sentence.
  - The Ask-Engine embedding path imports the **in-tree** package, not a module: `app/internal/web/backend/ask/embed.go:8` → `"github.com/anthropos-work/app/internal/ai"`, and `CreateEmbeddings` is called at `embed.go:23` on that package's `ai.AI`. Same import at `ask/examples.go:15` and `ask/handler.go:20`. There is **no** other `anthropos-work/*ai*` import anywhere in `internal/askengine/` or `internal/web/backend/ask/`.
  - `app/go.mod` @ `ad9f3c49` does **not** require `github.com/anthropos-work/ai` (`grep -c` → 0). Enumerated across all 15 trees at their own refs, the **only** two `go.mod`s that still require the module are the frozen `cms` (`ca50c817`) and `jobsimulation` (`462343b0`) husks — neither of which `repos.yml` @ `0c91421d` lists.
- **Why it is also a corpus self-contradiction:** two of my own assigned files state the opposite, emphatically and with the repair history attached —
  - `external_services.md:554`: *"⚠️ **that interface is no longer a shared private module for any service a stack builds, and this sentence said 'the shared `ai` library' until M257x iter-115.**"*
  - `jobsimulation.md:168`: *"through one `ai.AI` interface that is **in-tree, not a shared module** … (This bullet said *"the shared `ai` library"* until M257x iter-115 … **a third instance the iter-113 enumeration did not book**)"*.
    `askengine.md` is the **fourth** instance, and it is unrepaired at two sites. Exactly rule 19's cross-file drift: the claim was repaired where its owner held it and left standing in a file nobody's partition covered.
- **Confidence:** high.

### B2 — `askengine.md:104`'s `:1438` anchor names a mid-sentence comment, not "the Asynq pools and the Redis subscribers"

- **Anchor:** `corpus/services/askengine.md:104` — *"Everything constructed **after** it never runs: the Connect-RPC mux (`:1295`), the meta HTTP server (`:1361`), the Echo router, the Asynq pools and the Redis subscribers (`:1438` onward)."*
- **Why it is false:** at `app` `ad9f3c49` (the ref this bullet pins at `:89`), `main.go:1436-1443` is a **prose comment** explaining why the messenger subsystem sits behind `MESSENGER_ENABLED`; `:1438` is the fragment *"// enough: merely constructing this server and calling Subscribe() attaches app to"*. It is neither an Asynq pool nor a subscriber construction. Measured:
  - **Asynq worker pools** are constructed at `main.go:767` (`appWorker := worker.NewServer(redisAddr)`), `:827` (`skillerWorker`), `:1009` (`cbWorker`) and started at `:791/:793`, `:828/:830`, `:1010/:1012`, `:1210/:1212` — all **hundreds of lines before** `:1438`, and three of the four before even `:1295`.
  - **`app`'s own Redis subscriber server** is `subServer := pubsub.NewSubscriberServer(` at **`main.go:1376`** (under the comment `// PubSub subscribers` at `:1375`), its single `AddSubscriber` at `:1580`, and its `Run` at `:1583/:1585`. The messenger-only one is `:1450` (`Run` at `:1599/:1601`).
  - So *"the Redis subscribers (`:1438` onward)"* both **mis-anchors** (comment, not construct) and **under-covers** (it excludes `:1376`, the only unconditional subscriber server).
  - Positive control on the same derivation: the sibling anchors in the same sentence do resolve — `:1295` is `mux := http.NewServeMux()` (the Connect-RPC mux) and `:1361` is the `// Meta HTTP Server (healthcheck, _meta, _asynq)` label directly above `metaServer := meta.NewServer(...)` at `:1362`.
- **Why I hesitated (stated per the briefing):** the *load-bearing* proposition — that everything after the `return` at `main.go:470` never runs — is **true**, and `:1438` does sit inside the tail of `main()`. The defect is confined to the anchor. I book it because §5 rule 21 names *"an anchor that resolves but names the wrong construct"* as its own blocker class, and because a reader following `:1438` to find "the Asynq pools" lands ~670 lines away from them.
- **Confidence:** medium.

---

## Minors

- `corpus/architecture/external_services.md:727` — *"the name appears nowhere in the platform (**0 hits across all 15 trees at their own refs**…)"*. The substantive claim is **correct** (`anthropos-agent-eu` exists in no platform source; the US branch builds it with `fmt.Sprintf("anthropos-agent-%s", *location)` only on the non-EU arm). But the stated derivation does not reproduce: swept across all 15 trees at their own refs, `stack-demo/rosetta-extensions` @ `09d06070` returns **7** hits — all inside `stack-core/tests/fixtures/**`, which are checked-in *copies of this corpus's own former prose*, not platform code. Positive control in the same pass: `anthropos` matched 4,933 / 10,754 / 5,630 / … lines across the trees, so the pipeline was live. Narrow the sentence to the platform clone set, or say "0 outside the tooling's corpus fixtures".
- `corpus/services/jobsimulation.md:164` — *"The corpus spells this path correctly at **eight** other sites and this was the sole outlier."* Measured at corpus HEAD `85f6f1c`: `internal/cms/directus/collections/jobsimulation.go` occurs on **8 lines total**, of which **this one is one** — so **7 other sites** (`ai_architecture.md:61,221,222,264,277`, `external_services.md:566,624`). The "sole outlier" half is correct; the count is off by one (it appears to count the repaired site itself). Same count at `cec0ddb`, the commit that wrote the sentence, so this is not date drift.
- `corpus/services/jobsimulation.md:19` — *"(The entry list read "6 … storage, messenger" at `0dab54d`; `838d907` removed those two **a day later**.)"* The 6-entry list at `0dab54d` is exactly right (app, sentinel, storage, messenger, next-web-app, studio-desk). But `0dab54d` is dated **2026-08-03** and `838d907` **2026-08-05** — two days, not one.
- `corpus/architecture/external_services.md:14` — PM summary still reads *"**GraphQL/Wundergraph** - Unifies all backend services into a single API."* The rest of the page establishes the supergraph is **one** subgraph (`backend`) since `915da06`, and `sentinel` was never federated. Stale framing, immediately qualified by the "Prod-only since `2adcf71`" clause, so cosmetic rather than material.
- `corpus/architecture/external_services.md:136` — *"The ⚠️ under *Architecture* below (`:206-211`)…"*. The block at `:206-211` carries **no ⚠️ glyph** (the glyphs in this file are at `:3`, `:132`, `:300`, `:432`, `:554`, `:639`, `:727`). The line range and its content claim ("states that two-member tuple in bold") are both correct — `:209` does carry `**`("cms", "backend")`**`.
- `corpus/architecture/external_services.md:853` — the comment inside the bash fence opens `(docker-compose.yml:101-109 — …` and never closes the parenthesis before the fence ends. Formatting only.
- `corpus/services/customerio-sync.md:67-81` — the "HISTORICAL" compose block is an **abridgement**: the real block at `838d907^` (`docker-compose.yml:136-154`) also carries `env_file: [.env]`, `networks: [app-network]` and `profiles: [customerio-sync, all]`. Everything shown is byte-accurate; nothing shown is wrong. Noted only because the block is presented without an elision marker.

---

## Positively cleared (audited zeros)

Labelled per the briefing. **ENUMERATED** = I derived the predicate's set from source independently and compared
cardinalities. **RE-DERIVED** = I only re-computed arithmetic the document displays (the weakest clearance).

### ENUMERATED

1. **Compose service set / "five services, seven effective"** (`external_services.md:168-175`, `:306`; `jobsimulation.md:56-57`). Enumerated `^  [a-z-]*:` in `docker-compose.yml` @ `0c91421d` → `sentinel, backend, studio-desk, next-web-app, gotenberg` = **5**; `common.yml` adds `postgresql` (`:2`) + `redis` (`:24`) = **7 effective**. Enumerated the same at `0dab54d` → **8/10** (matches the doc's "eight/ten at `0dab54d`"). `common.yml` declares **no** `profiles:` key anywhere, so the profile-less floor is exactly `{postgresql, redis, sentinel}` — as claimed.
2. **The `core` members' anchors.** `docker-compose.yml:110` and `:183` @ `0c91421d` are both `profiles: [core, backend, all]`, under `backend:` (`:28`) and `gotenberg:` (`:170`) respectively. The anchors name the *evidence* for core membership, correctly.
3. **Profile token set.** Enumerated every `profiles:` value @ `0c91421d`: `{core, backend, all, studio-desk, frontend}` — **five tokens**, no `graphql`, no `storage-legacy`, no `messenger`, no `customerio-sync`, no `jobsimulation`. At `0dab54d` `storage` was `profiles: [storage-legacy]` (`:134`), `messenger` `[messenger]` (`:195`), `customerio-sync` `[customerio-sync, all]` (`:154`) — exactly the two-state story `external_services.md:859-869` and `customerio-sync.md:43` tell.
4. **`repos.yml` membership.** @ `0c91421d`: `app, sentinel, next-web-app, studio-desk` = **4** (`jobsimulation.md:15`). @ `0dab54d`: **6**, adding `storage` + `messenger` (`jobsimulation.md:18`). `d11a403`'s repos.yml diff removes exactly `- name: cms`, `- name: jobsimulation`, `- name: roadrunner` (`jobsimulation.md:17`); `838d907`'s removes `storage` + `messenger`.
5. **`*_RPC_ADDR` history.** Enumerated every `RPC_ADDR` line per ref. `d11a403^`: the messenger block (`:255-265`) holds `BACKEND_USERS_RPC_ADDR=http://backend:8083`, `CMS_RPC_ADDR=http://cms:8091`, `JOBSIMULATION_RPC_ADDR=http://jobsimulation:8401`, `SKILLER_RPC_ADDR=http://backend:8083` — so `d11a403` moved **the middle two**, exactly as `jobsimulation.md:48-51`, `:127` and `:153` insist (against `service_taxonomy.md`'s former "all four"). @ `0c91421d`: **zero** `*_RPC_ADDR` anywhere; the only service address is `AUTHORIZATION_ADDRESS=http://sentinel:8087` (`:48`).
6. **The gotenberg / Judge0 correction** (`jobsimulation.md:150`). Verified `GOTENBERG_URL=http://gotenberg:3200` (`:57`), `JUDGE0_BASE_URL` (`:59`), `gotenberg` core at `:183`, consumed at `app/internal/converter/gotenberg.go:31` (the `http.NewRequestWithContext` POST to `…/forms/libreoffice/convert`). The doc's explicit refusal of the root-`CLAUDE.md` phrasing ("compose sets exactly one service address") is the **correct** side of that disagreement.
7. **Supergraph subgraph counts** (`external_services.md:373-386`). Enumerated `- name:` in `supergraph-config-prod.yaml` at each named commit: `749dc86^` = **5**, `749dc86` = **4**, `7c17e63` = **3**, `915da06` = **1**, `60c229f3` = **1**. `915da06 --name-status` marks **both** `schemas/cms.graphqls` and `schemas/jobsimulation.graphqls` `D`. `subgraphs.conf` = `BACKEND=v1.360.0`; `git ls-files schemas/` = `schemas/backend.graphqls` alone. The "do not take the count from the commit subject (2→1)" caution is correct.
8. **No subscriptions.** (a) `grep -rn "sse\|subscription" *.yaml` in `graphql-wundergraph` returns nothing (positive control `grep -rln backend *.yaml` → the three supergraph configs). `915da06^:supergraph-config-prod.yaml:13` still reads `protocol: "sse_post"`. `git log -S 'protocol: "ws"'` → **0** commits (control: `-S sse_post` → **5**). `bba862f`: `merge-base --is-ancestor` rc **1**, `branch -a --contains` names only `remotes/origin/feat/use-web-socket`. (b) enumerated type declarations in `schemas/backend.graphqls`: `type Mutation {` `:4053`, `type Query {` `:4912`, **no** `type Subscription`; `Subscription` occurs on exactly **6** lines, all Stripe/plan (`activeSubscription` `:1528`, `type PlanSubscription` `:1555`, `stripeSubscriptionId` `:4385`/`:4418`, `stripeSubscriptionItemId` `:4389`, and the doc-comment `:4391`).
9. **Router port + prod declaration.** `terraform/locals.tf:8` `port = 8080`; `terraform/main.tf:48-49` `"containerPort": ${local.port}` / `"hostPort": ${local.port}`; `config.prod.yaml:5` `listen_addr: 0.0.0.0:8080`; `terraform/main.tf:20` `service_desired_count = 1`. `"5050:8080"` was only the compose host mapping (`1e8e754:docker-compose.yml:14`).
10. **The pre-deletion `depends_on` set.** `1e8e754` (`= 2adcf71^1`, verified) `docker-compose.yml:19-27` names **four** services each `condition: service_started`: backend, jobsimulation, cms, storage — and the same four at `a4db680`. `464dfe3` is **not** an ancestor of `0c91421d` (rc 1) and lives only on `remotes/origin/feat/cms-in-app`.
11. **The three Dockerfile eras.** `2c85211^` has `context: ../graphql-wundergraph` with **no** `dockerfile:` key; `2c85211` adds `dockerfile: Dockerfile.dev`; `1e8e754` lines 6-8 read `build:` / `context: ..` / `dockerfile: graphql-wundergraph/Dockerfile.dev`; `b56d731:docker-compose.yml` still has `  graphql:` at `:22`; `360efd4` has none (control: `  backend:` at `:28`). `graphql-wundergraph/CLAUDE.md:39` does assert the opposite, and it was written in `60c229f` dated **2026-07-30** titled *"…correct the compose build path"* — the corpus's "do not re-correct from it" warning is sound.
12. **`Dockerfile.dev` composition.** One schema `COPY` at `:18`, the orphaned comment at `:19-20`, one `awk` at `:23`, `wgc@0.104.0` at `:7`, runtime image `ghcr.io/wundergraph/cosmo/router:0.275.0` at `:29`. The fenced snippet in the doc is faithful.
13. **Historical subgraph routing URLs.** `915da06^:supergraph-config-compose.yaml` → `backend http://backend:8082/graphql/query`, `jobsimulation http://jobsimulation:8400/query` (**the only entry that ever carried `subscription: protocol: "sse_post"`**), `cms http://cms:8090/query`. Exactly the table at `:481-485`.
14. **The `ai`-fold enumeration** (`external_services.md:554`). `app/go.mod:14-18` @ `ad9f3c49` = `analytics-go`, `colony`, `proto`, `storage`, `taxonomy` — and nothing else from `anthropos-work` in that range; `anthropos-work/ai` = 0 hits. Files importing exactly `"github.com/anthropos-work/app/internal/ai"` (with the closing quote) = **67** — the doc's figure, not the looser 83 that a prefix match returns. Swept `go.mod` across all 15 trees: only `cms` and `jobsimulation` still require the module.
15. **Provider-table code anchors.** `internal/jobsimulation/agent/report_agent.go:31` `defaultAgentModel = "eu.anthropic.claude-sonnet-4-6"`; `internal/askengine/bedrock.go:25` `DefaultModelID`; `internal/coursebuilder/bedrock.go:23` Opus 4.8 / `:29` Sonnet 4.6; `:106-113` the `ANTHROPIC_API_KEY`-present → first-party / absent → Bedrock switch; `app/main.go:770` `logger.Info("coursebuilder model backend", …, coursebuilder.ModelBackendName())` @ `b948604` (= **v1.366.0**, tag verified).
16. **Mistral is OCR-only.** `internal/cms/studio/markdownManager.go:10` import, `:14` `ocr *mistralocr.Client`, `:29` `func NewMarkdownManager`, `:30` `mistralocr.New(aiKey)`; `studioManager.go:583` the sole call site. `mistral.NewMistral` = **0 hits repo-wide** at `ad9f3c49`; `:19` is indeed a doc-comment line. In the **nested** studio tree at `aeec036a`: `tools/pdf2md.py:24` `from mistralai import Mistral`, `:127` `model="mistral-ocr-latest"`, and `tools/r3.py` **does** dispatch it (`:139` dependency list, `:190` `get_script_path("pdf2md.py")`, `:199-206` the step-2 command) — so the doc's own retraction of *"nothing dispatches it"* is right, and so is the surviving claim that no Go caller and no `gen.py` path reaches it (`git -C stack-demo/app/studio grep -i mistral aeec036a` shows only `tools/`).
17. **The routing mechanics** (`external_services.md:577-643`). `internal/jobsimulation/ai/ai.go`: vendor consts `:30-33` (four: `azure`, `openai`, `anthropic-aws`, `anthropic`); `getClient` `:259-289`; the Azure/PostHog `flag_use_azure_us` branch `:264-276` with the *keep-EU-on-error* return at `:272`; `AnthropicAws` **and** `Anthropic` both → `a.anthropicClient` `:280-283`; `isThrottlingError` `:129-141` matching only HTTP 429 from the OpenAI/Anthropic SDKs; the 429 retry setting `vendor = Openai` at `:150-155` and mirrored at `:297-301`/`:326`; `openai.NewOpenAI(openaiKey)` at `:80`. Caller default: `internal/cms/directus/collections/jobsimulation.go:905` `AIVendor *AIVendor` (nullable) and `:1302-1305` `aiVendor := simulation.Openai`; `internal/jobsimulation/simulator/ai/ai.go:58-59` maps it, `:114-115` is the `default:` arm doing the same.
18. **The "four live + one latent" count.** Enumerated `^[A-Z_]*_AI_[A-Z_]*_MODEL` across **all three** `configs/*.ini` at `aeec036a`: **30** lines (10 per file), **every one** pinned to `azure`. `gen.py:41-53` overrides only the six `*_API_KEY`/`*_ENDPOINT` names listed at `:45-48` — never the service selector. `services/ai.py:704-724` `get_client` with providers `{openai, azure, anthropic}` (`:706-708`), `:383` `return OpenAI(api_key=self.api_key)`, `:627-664` `AnthropicProvider`. `configs/config_template.ini:30-31` = `OPENAI_API_KEY` / `OPENAI_ENDPOINT=https://api.openai.com/v1/chat/completions`. `bedrock|boto3` under the studio tree = **0** (control: `anthropic` = 34). So the arms are unreachable as shipped exactly as the doc says.
19. **The env-var re-derivation** (`external_services.md:668-677`), re-derived **at the ref it pins** (`5ba17044`) with its own stated method (`grep -rho '\b<NAME>\b' --include='*.go'`): `OPENAI_KEY` **5**, `ANTHROPIC_API_KEY` **26**, `AZURE_OPENAI_KEY` **13**, `AZURE_OPENAI_ENDPOINT_URL` **13**; `OPENAI_ORG_ID`/`AZURE_OPENAI_ENDPOINT`/`AZURE_OPENAI_DEPLOYMENT` **0** each (the word-boundary correctly separates `AZURE_OPENAI_ENDPOINT` from `…_URL`); `OPENAI_API_KEY` **2**. Every figure reproduces.
20. **Taxonomy figures.** Read the manifest at `.agentspace/snapshots/taxonomy/5afc0bccf1df7ef538b643321fc6362f/manifest.json`: `public_only: true`, `predicate: "org-null"`, `captured_at 2026-06-29T14:43:00Z`; `public.skills` **42,790**, `public.job_roles` **22,470**, `public.skill_embeddings` **42,790**, `public.job_role_embeddings` **18,919**. Every number, and the "18,919 is the embedding count transcribed onto the role count" diagnosis, is exact.
21. **LiveKit agent naming** (`external_services.md:727`, `jobsimulation.md:167`). `calls/livekit.go` @ `ad9f3c49`: `:101-104` `euAgentEndpoints = {"azure-eu","azure-eu-fr"}`; `:110` default `agentName = "anthropos-agent"`; `:111` default `agentEndpoint = "azure-eu"`; `:115` chain agent; `:118-128` the location branch — `:120` bare EU name, `:122-123` `rand.Intn` over the two EU endpoints, `:126` `fmt.Sprintf("anthropos-agent-%s", *location)`; `:131-135` the `flag_use_realtime_openai` read, `:140-144` its override with `:142` name and `:143` `"openai-hosted"`. Both files' retractions are correct in every clause. (See the minor above on the "15 trees" parenthetical.)
22. **Voice-engine nil default.** `internal/cms/directus/collections/jobsimulation.go:1594` `func voiceEngineFromDirectus`, `:1595-1596` the nil branch returning `simulation.SimulationVoiceEngineGptrealtime`. The frozen `cms` repo @ `ca50c817` has **0** tracked paths under `directus/` at its root; its own copy is `internal/directus/collections/jobsimulation.go`. The same-fact twin at `ai_architecture.md:222` now carries the **identical** path and range (`:1594-1597`), so the pair really is repaired on both sides.
23. **jobsimulation's M810 teardown**, graded at the ref the claim names (`6092c6d2`) and re-checked at `origin/main` `82cb66ec`: `service_desired_count` = **0 occurrences** in `terraform/main.tf`; `:15-22` is the M810 deletion note enumerating ECS service / task definition / ECR repo / IAM roles / SG / Cloud Map / log group / alarms; `:24-40` is the "what this module still owns" block (livekit.tf, chime.tf, ssm.tf) with `:38-40` the atlas-tracker / schema-drop carve-out. (At the *checkout* `462343b0` the module block is still present with `service_desired_count = 0` at `:40` — the pin is doing real work here and is correct.) `origin/main` took exactly **four** commits on 2026-08-04: `6092c6d2`, `caf36c96` (merge of PR #439), `1e40d184`, `82cb66ec`.
24. **The cms counter-example.** `cms/terraform/main.tf:39` `service_desired_count = 0` in a **191-line** file at *both* `ca50c817` and `origin/main` `f38c0c4a`; `6efa1d5` (2026-08-04) deletes exactly `.github/workflows/build-production.yml` (18 lines) under the subject *"drop build-production — the cms ECR repository is decommissioned (M810)"*. Two measured facts pointing opposite ways, exactly as `jobsimulation.md:12` and `external_services.md:175` report — and neither asserts a verdict.
25. **The 23 jobsim tables.** Enumerated `^CREATE TABLE` in `app/terraform/migrations/20260722081626_jobsim_data_model.sql` → **23**, including every name the doc lists. `20260722104506.sql:2` `CREATE TABLE "job_simulation_sessions"`, `:79` `DROP TABLE "sessions"` — so `public.sessions` really does not exist.
26. **The mirror drop.** `20260729133514.sql`: `:15-23` the two FK remaps, `:36-44` the orphan NULLs, `:58-61` the *"5. Drop the mirrors."* comment, `:62-63` `DROP TABLE "local_jobsimulation_sessions"` / `"local_skill_path_sessions"`. **0 `INSERT` statements** — the single case-insensitive `insert` match in the file is the function name `on_insert_local_jobsimulation_sessions_update_memberships` at `:64`, which is a `DROP FUNCTION`. `internal/organization/intelligence.go:1700` is `query := m.ent.JobSimulationSession.Query().`.
27. **The `$HOME/.aws` landmine**, graded at the refs each sentence pins. @ `0dab54d`: comment `:88-89`, `volumes:` `:90`, the bind `:91` — the **only** `$HOME` bind in the file, under `backend`. @ `9d00a313`: `jsai.NewAIManager` at `wiring.go:142-146` with `if err != nil { return nil, err }` at **`:147-149`** (so `:147-148` is right), `internal/jobsimulation/ai/ai.go:90` `can't load AWS config: %w`, and `main.go:670` `log.Fatalf("jobsim-in-app: engine wiring failed …")`. @ `b948604` the same fatal is `:614`. @ `ad9f3c49` it is `:723` with `Wire` at `:721` — and `:670` there is `skillerAzureEndpointEu = &v`, exactly as `jobsimulation.md:215` warns.
28. **`func main()` line drift** (`jobsimulation.md:208`). `:216` @ `7177374` **and** @ `9d00a313`; `:212` @ `b948604`; `:229` @ `ad9f3c49`. `git rev-list --count 7177374..ad9f3c49` = **38**. Every one of the four refs and the commit distance is exact. The only `spf13/cobra` import in `app` @ `ad9f3c49` is `cmd/createTaxonomy/main.go`.
29. **The frozen cobra contract.** `jobsimulation` @ `462343b0`: `cmd/root.go:77` `cmp.Or(os.Getenv("PORT"), "8080")`, `:78` `RPC_PORT`/`8081`; `Dockerfile:34` and `Dockerfile.dev:35` `ENTRYPOINT ["./application"]` with no `CMD`; subcommands enumerated from `Use:` = `aggregate`, `clone-session`, `test-command`, `validate` (root is `jobimulation` [sic]) — **four**, none of them `serve`; the repo's own `CLAUDE.md:88` really does say `go run . serve`. `internal/worker/worker.go:27` `Concurrency: 10` and `:104` `Concurrency: 25`. `cmd/root.go:285` `subServer.AddSubscriber(cmp.Or(os.Getenv("CMS_STREAM"),"cms"), cmsSubscriber)` — so "consumer: `cms`, wired in `cmd/root.go`, not `internal/worker/`" is right. `go.mod` `hibiken/asynq v0.26.0`; `09631fb2` is the chronos-removal commit; migrations `20260402145459.sql` + `20260409131539_add_summary_to_extraction_results.sql` and `internal/simulator/manager/interview_report_csv{,_test}.go` all exist.
30. **The 8400/8401 history.** @ `2adcf71`: `docker-compose.yml:83` the `jobsimulation:` block, `:93-94` `"8400:8400"`/`"8401:8401"`, `:113` `PORT=8400`, `:119` `RPC_PORT=8401`, `:140` `profiles: [graphql, jobsimulation, all]`; `Makefile:10` `PROFILE ?= graphql` (vs `PROFILE ?= core` @ `0c91421d`); `repos.yml:17-19` the `jobsimulation` entry marked `migrations: false # legacy`. Every anchor exact.
31. **The `DIRECTUS_DATA_CONSUMERS` tuple**, read in the **pinned per-stack clone** `stack-demo/rosetta-extensions` @ `09d06070`: `stack-injection/gen_injected_override.py:86` `DIRECTUS_DATA_CONSUMERS = ("cms", "backend")`, the inert-key rationale at `:77-81` (quoted accurately, both fragments), the re-point at `:698-699`; the dev twin `stack-core/gen_override.py:58` identical; `stack-injection/tests/test_injection.py:1109` `def test_backend_the_actual_reader_is_repointed`, and `test_only_cms_is_repointed_not_other_services` survives only as a comment. The prior-pin claim also holds: at `ab81527a` those constructs are at `:84` and `:669-670`.
32. **Directus compose facts.** `docker-compose.yml:53` `DIRECTUS_PUBLIC_BASE_ADDR=https://content.anthropos.work` at **both** `0dab54d` and `0c91421d`, inside `backend`'s `environment:` block running `:46-94`; `.env_example:91-92` = `DIRECTUS_BASE_ADDR` + `DIRECTUS_TOKEN` and nothing else Directus-ish; there is **no** `directus` service in any of these compose files. `app/cms_reader_switch.go` and `app/internal/cms/directus/` both exist at `ad9f3c49`; `app/main.go:980-982` @ `b948604` is the `DIRECTUS_BASE_ADDR` `log.Fatalf`. The `a2a3ee6^` archaeology also reproduces: `git show a2a3ee6^:docker-compose.yml` really does carry a `directus/directus:10.10.1` service — so the "that retraction over-corrected" correction is itself correct.
33. **Frontend endpoint anchors.** `docker-compose.yml:151` (build arg) / `:160` (runtime env) for `NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT`; `:119` / `:135` for `VITE_GRAPHQL_ENDPOINT`; all four `…:8082/graphql/query`. `NEXT_PUBLIC_GRAPHQL_ENDPOINT` = **0 hits** in `next-web-app` @ `8297c684` (control: `NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT` in 15 files). `backend.go:315` mounts the Apollo Sandbox at `/graphql` and `:317` the handler at `/graphql/query` — so both the playground and the endpoint claims are right. `backend.go:101-109` is `backend`'s `depends_on` (redis, postgresql, sentinel).
34. **Clerk SDK per app.** `studio-desk/package.json:27-28` `@clerk/clerk-js` + `@clerk/express`; `next-web-app` `apps/web|hiring|integration/package.json` `@clerk/nextjs`, `apps/mobile` `@clerk/clerk-expo`; `ant-academy/code/package.json:52` `@clerk/nextjs`; `app/go.mod:29` `clerk-sdk-go/v2`. `sentinel` @ `f2c46190` has **zero** `clerk`/`authn` hits in `go.mod` or `*.go` — the "Sentinel does not validate Clerk tokens" claim holds. `backend.go:130` is the `/api/webhook/clerk` authn-skip entry. `studio-desk/.env.example:56` `AI_PROVIDER_CHAIN=azure-openai,openai`.
35. **The whole of `customerio-sync.md`'s port record**, against `app/internal/customeriosync/doc.go` @ `ad9f3c49`: `:4-5` *"the last of the Go services to be folded into app"* (wrapping the line break, as the doc says); the Brevo fossil + `public.customer_io_sync_table` at `:6-7`; the stateless sync window at `:38-44`; `database/sql`-not-sqlc at `:46-52`; **7,392 of 7,397 rows** at `:29-31`; the asynq scheduler at `:15`; the shared-`copilotDB` rationale at `:70-78`. Package files = `doc.go, store.go, sync.go, sync_query.sql, sync_test.go` + `cmd/customerio-resync`. Switch mechanics: `env_guards.go:62` `envCustomerIOSyncEnabled`, `main.go:286` `mustSubsystemSwitch`, `main.go:284` the comment (not the mechanism), `main.go:295` the condition / `:296` the `log.Fatalf`, `main.go:395` `os.Getenv("BREVO_KEY")`, `main.go:393-396` on `copilotDB`, `env_guards.go:92-105` `resolveSubsystemSwitch` with `case "":` at `:98` erroring when `deployed`, `mustSubsystemSwitch`'s `log.Fatalf` at `:87`. `cmd/customerio-resync/main.go:33` `flag.Bool("dry-run", …)`. Compose comment block `:84-92` @ `0c91421d`.
36. **The 18 git-URL build contexts** (`customerio-sync.md:85`, `:164`) — re-derived the **set**, not the sum: enumerated every `anthropos-work/<repo>` appearing in a `context:` git URL across **every** commit touching `docker-compose.yml` (`git log --all`), de-duplicated → **18**, and the names match the published list character-for-character (app, chronos, cms, customerio-sync, graphql, graphql-wundergraph, graphqltmp, intelligence, jobsimulation, messenger, realtime, roadrunner, sentinel, simulator, skiller, skillpath, storage, studio-desk). The rest of the derivation also reproduces: `a2a3ee6^` = **14** git-URL contexts, `a2a3ee6` = **2** (customerio-sync, realtime), `c17cc9a^` = **2**, `c17cc9a` = **1**; at `838d907^` customerio-sync is the only git-URL context among **7** build-bearing services. The superlative is correctly narrowed to "at the end".
37. **`external_services.md` has no Brevo/Customer.io section** (`customerio-sync.md:151-156`). `grep -ci brevo` → **0**; the per-service `##` sections are exactly Clerk (`:21`), Directus (`:118`), the Cosmo router (`:358`), AI Providers (`:552`), LiveKit (`:719`), AWS Chime (`:735`).
38. **The Ask-Engine surface.** `internal/askengine/registry.go`: `Schema: "public"` on **57** `TableDef`s, `Schema: "directus"` on **3** → **60**; `:183-195` is the jobsim transition-alias comment; `rewriteSchemas` (`sandbox.go:293-301`) does still contain `"jobsimulation"` (with the M710 note at `:299`). `sandbox.go` `ValidateSQL:163`, `BuildCTEs:227`, `WrapQuery:363`, `StrategyGlobal/Direct/Indirect` `:257/:259/:268`, `$1`/`$2` documented at `:423`, `profile_histories` as the self-scoped example. `executor.go:18` `MaxInlineRows = 200`, `:21` `MaxCellLength = 400`, `:24` `QueryTimeout = 10 * time.Second` applied as `SET LOCAL statement_timeout` at `:97`. `handler.go:34` `maxAgenticIterations = 15`, `:41` `loopTimeout = 10 * time.Minute`, `:248` `context.WithoutCancel` + timeout. `main.go:167` `askEngineMaxConns = 6`, `:370-377` its own pool from the **same** `COPILOT_DB_CONN` DSN as `copilotDB` (`:351`, `:357-364`) — which makes the "dedicated small read pool" (askengine) and "shared analytics pool" (customerio-sync) statements *both* true and non-contradictory. `rules.md` = **146,219 bytes** (~146 KB). Routes enumerated from `backend.go:171-188` — all nine `/ask/*` routes match. `bedrock.go:161` `NewBedrockClient` has **exactly one** error return (`config.LoadDefaultConfig` at `:165-168`); the log string *"bedrock client unavailable; talk-to-data disabled"* occurs exactly once; `main.go:467-471` is the block, and the only top-level `func`s in `main.go` are `:203`, `:211`, `:229` — so *"no other `func` declaration falls between `:229` and `:467`"* is exactly right. Test counts re-derived by `^func Test`: `sandbox_test.go` **49**, `executor_test.go` **13**, `prompt_test.go` **10**, `followups_test.go` **14**. `OrgCheckFeaturePermission` (`ask/handler.go:718`), `GetTalkToDataCustomRules` (`organization/manager.go:1333`), `talk_to_data_custom_rules` (`ent/enum/organization_settings.go:23`) all exist. Tags: `v1.267.0` = 2026-05-07, `v1.340.0` = 2026-07-17 — both dates exact.
39. **`intelligence.md`.** `fdfa189` (2026-04-17, *"remove intelligence service from local dev orchestration"*) touches **both** `docker-compose.yml` (−25) **and** `repos.yml` (−3) — the doc's "removed from both" is right, and `repos.yml` existed by then (created at `a2a3ee6`). At `fdfa189^` the block (`:61-82`) carries exactly `DB_CONNECTION_BACKEND` and `DB_CONNECTION_SKILLER`, `restart: on-failure`, `profiles: [graphql, all]`, and **no published ports**. Nothing in this 18-line file is contradicted.
40. **Media claims.** `calls/livekit.go:176` `FileType: livekit.EncodedFileType_MP3` (LiveKit audio-only, MP3); `recording/chime.go:351` `"concatenated/composited-video/%s.mp4"` (Chime composited MP4). Both `external_services.md:729` and `:743` hold.

### RE-DERIVED (weaker — flagged as such)

- `external_services.md:604-643`'s "four live + one latent" arithmetic. I enumerated the *configs* (item 18 above, ENUMERATED) but the partition of the five listed items into "live" vs "latent" is the document's own reasoning, which I re-checked rather than independently re-derived.
- `customerio-sync.md:55`'s "identical for 7,392 of 7,397 rows" — re-derived only against `doc.go:29-31`, which is where the figure comes from. I did not re-run the comparison; no database was available.

---

## What I could not settle

1. **`external_services.md:3` — *"the repo is ARCHIVED on GitHub (2026-07-30)"* (graphql-wundergraph).** GitHub archive state is not visible from a clone, and this corpus has an explicit convention saying so — `jobsimulation.md:24-27` refuses exactly this claim for its own repo (*"The repo's GitHub archive state is not visible to this corpus"*), and points at `platform-migration-status.md`'s unmeasurable-claims convention. For graphql-wundergraph the available evidence is at least **consistent**: `HEAD == 60c229f3`, dated 2026-07-30, with no later commit on any local ref. I did not book it because I cannot show it false and because the date makes it a dated measurement; but the two files apply opposite standards to the same kind of claim, and a coordinator with network access could settle it with one `gh repo view --json isArchived`.
2. **`intelligence.md:18` — the `:8080` default port, the `/_meta`-only surface, and the 5-minute ticker.** The `intelligence` repo is in no clone set and is not cloned by `make init`. The compose block at `fdfa189^` sets `PORT=9002` and publishes nothing, which is consistent with (but does not establish) a binary default of 8080. Settle by cloning `anthropos-work/intelligence` and reading `cmd/`.
3. **`customerio-sync.md:143-145`'s own scope note** — whether customerio-sync's ECS task / image / terraform module were torn down. The doc explicitly declines to assert this; `infrastructure` has never been in a clone set, and there is no `customerio-sync` clone either. Correctly disclosed, not a finding.
4. **`external_services.md:707`'s Studio-Desk default models** (`gpt-5.2`, `claude-sonnet-4-5` / `claude-opus-4-5`). I verified `AI_PROVIDER_CHAIN`'s default (`studio-desk/.env.example:56`) and the tier/circuit-breaker framing against `studio-desk/knowledge/02-studio-desk/backend/ai-service.md`, but did not enumerate the model constants in the TS source; treat that clause as unaudited rather than cleared.
5. **`jobsimulation.md:133`'s `internal/graph/queries.resolvers.go:70`.** It resolves in the **frozen** `jobsimulation` repo @ `462343b0` (`func (r *queryResolver) JobSimulationResult`, whose body does Ent SELECTs). The doc names no repo, and the *live* engine is `app/internal/jobsimulation/`. I did not book it — the frozen repo is the natural reading inside this file's own "Key directories" frame — but a reader chasing the live path would need the `app`-side twin.
