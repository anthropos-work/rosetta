# Seat F — reading #30 (M257x iter-116)

Ground truth read at the ADDENDUM's superseded shas, re-verified at open:
`platform 0c91421` · `app ad9f3c498` · `next-web-app 8297c684c` · `sentinel f2c4619` ·
`studio-desk 41ee357` · `ant-academy 22df69dd` · `storage 4ce8ece` (origin/main `9f8cb53`) ·
`messenger fa47850` · `cms ca50c81` (origin/main `f38c0c4`) · `graphql-wundergraph 60c229f` ·
`roadrunner 87d8d44` · `jobsimulation 462343b0` (origin/main `82cb66ec`) ·
`stack-demo/app/studio aeec036` (own checkout) ·
rext **pinned per-stack** `stack-demo/rosetta-extensions 09d0607` and **authoring**
`.agentspace/rosetta-extensions 1dc1eb8` — both read where a rext claim was graded, and both agreed
on every anchor I checked.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/services/ant-academy.md` | 503 | yes |
| `corpus/architecture/architecture_overview.md` | 435 | yes |
| `corpus/services/storage.md` | 248 | yes |
| `corpus/services/roadrunner.md` | 216 | yes |
| `corpus/architecture/dependency_map.md` | 103 | yes |
| `corpus/services/TEMPLATE.md` | 46 | yes |

Search-pipeline positive controls run in-pass: `git grep -c 'func ' ad9f3c49 -- '*.go'` in `app` → 2222
files (non-zero); `git grep -n AUTHORIZATION_ADDRESS 0c91421` in `platform` → 2 hits while
`RPC_ADDR` → 1 (a CLAUDE.md prose line only). Two of my own probes returned false zeros and were
caught by controls and re-run: a `${a}package.json` path built from `git ls-tree` output without a
separator (reported "no next" for all five `apps/*`), and a `grep '^[^/]*UserMixin{}'` that matched
`CreatedByUserMixin{}` as a substring and would have manufactured a false blocker against the
multi-tenancy fence. Both corrected before any number was written down.

---

## Blockers

### B1 — `architecture_overview.md` lists `ai` among the private modules pulled at Docker build; `app` dropped it and no built repo requires it

- **Anchor:** `corpus/architecture/architecture_overview.md:80` —
  *"4. **Shared Libraries**: **four** imported private modules — colony, proto, ai, taxonomy (not
  deployed; pulled at Docker build)."*
- **Why it is false:** The passage names no ref, so it is graded at the checkouts. The only two Go
  repos a stack clones and builds at `platform 0c91421` are `app` and `sentinel` (`repos.yml` has
  four entries: app, sentinel, next-web-app, studio-desk).
  - `app/go.mod` @ `ad9f3c49` requires `analytics-go`, `colony v0.35.2`, `proto v1.210.0`,
    `storage v0.15.2`, `taxonomy v1.2.0` — **no `github.com/anthropos-work/ai`**.
  - `sentinel/go.mod` @ `f2c46190` requires `colony v0.35.2`, `proto v1.210.0`, and
    `taxonomy v1.2.0 // indirect` — **no `ai`**.
  - `app` `1e457fa70` (2026-08-04, *"refactor(ai): fold the ai library into app as internal/ai"*,
    verified an ancestor of `ad9f3c49`) deletes the line `- github.com/anthropos-work/ai v1.40.2`
    from `go.mod`. The requirement survives only in the frozen `cms/go.mod:9` and
    `jobsimulation/go.mod:11` (`v1.40.2`), neither of which is in `repos.yml`, cloned by
    `make init`, or built by any compose service.
  - Measured across the seven Go clones at their own refs: `ai` **2/7**, and both are frozen repos.
  So a Docker build pulls **three** shared modules, not four, and `ai` is not one of them.
- **Self-contradiction, inside this reading's own file set:**
  `corpus/architecture/dependency_map.md:42` — *"the two Go repos a stack clones and builds at
  platform `0c91421` (`app`, `sentinel`) pull in **THREE** of them — colony, proto, taxonomy … `app`
  **dropped** the `ai` module at `1e457fa70`"* — and `dependency_map.md:48`, whose `ai` row reads
  *"**No repo a stack builds**"*. Two of my six assigned files state opposite things about the same
  build; `dependency_map.md` is the side that measures.
- **Confidence:** high

### B2 — `roadrunner.md` cites `:124` for a sentence that is at `:134`, and calls a line above it "below"

- **Anchor:** `corpus/services/roadrunner.md:130` — *"… Consistent with `:124` below (\"Upstream
  consumers: none (orphaned)\")."*
- **Why it is false:** `roadrunner.md:124` is a different bullet entirely — *"The repo contains an
  experimental WebSocket LSP proxy (`internal/lsp/lsp.go`) that is NOT wired into any running
  server…"*. The quoted text is at `roadrunner.md:134`
  (*"**Upstream consumers**: **none (orphaned — see the banner at the top).**"*), ten lines further
  down. The anchor therefore **resolves and names the wrong construct** — the failure class
  `platform-alignment.md` §5 rule 21 counts separately from a dead anchor, because the line exists
  and reads plausibly. The direction word is wrong too: `:124` is *above* `:130`, not "below", so a
  reader following the pointer lands upstream of the citing sentence and finds an unrelated claim.
- **Why I hesitated:** this is an intra-corpus citation, not a platform anchor, and the substantive
  claim it supports (roadrunner is orphaned) is true and verified. §5 rule 25 records that the
  canonical grading rule carved *line drift* out as MINOR. I booked it as a BLOCKER because the
  frozen instrument in my briefing lists *"wrong line anchors that point at the wrong construct"*
  under BLOCKER explicitly, and because this is not drift — `:134` has been ten lines below `:124`
  since the bullet was written, so the pointer was wrong when authored.
- **Confidence:** medium-high

### B3 — `ant-academy.md` names `upsertChapterProgressBatch` in the academy's write path; that mutation was removed from the client

- **Anchor:** `corpus/services/ant-academy.md:63` — *"progress persists via GraphQL mutations
  (`upsertChapterProgress[Batch]` / `setLastActivity`) to Ent tables `academy_chapter_progresses` /
  `academy_last_activities` …"*
- **Why it is false:** measured at `ant-academy 22df69dd`,
  `code/src/graphql/query/academyProgress.js:68-70` reads:
  *"(v0.5 M2 §6: UPSERT_CHAPTER_PROGRESS_BATCH removed — it was the FE outbox flush, consumed only
  by the deleted sync transport. Progress is now an immediate per-chapter write through
  UPSERT_CHAPTER_PROGRESS above.)"* — and the module exports only `GET_ACADEMY_PROGRESS`,
  `UPSERT_CHAPTER_PROGRESS` and `SET_LAST_ACTIVITY`. `git grep upsertChapterProgressBatch` @
  `22df69dd` hits exactly two places: that removal note plus the **stale module header** at `:18-19`
  (*"progress flushes via the BATCH mutation"*, which the code three lines of comment later refutes),
  and `code/src/graphql/schema.graphql:2207`, i.e. the **backend's** SDL, not a client call site.
  The academy issues the non-batch form only.
- **Self-contradiction, twice in the same file:** the blockquote immediately beneath it
  (`ant-academy.md:65-77`) enumerates the write path as exactly two mutations —
  `UPSERT_CHAPTER_PROGRESS` at `store.js:162` and `SET_LAST_ACTIVITY` at `store.js:210` (both
  verified byte-exact) — and `ant-academy.md:483-484` restates it as
  *"`upsertChapterProgress`/`setLastActivity` mutations"*, with no `[Batch]`.
- **Why I hesitated:** the bracket notation `[Batch]` reads as "optionally", and the mutation does
  still exist server-side, so a reader is not sent after a name that has no referent anywhere — only
  after one the academy does not use. I booked it because the surrounding sentence is specifically
  about *how the academy persists progress*, and because the corpus appears to have inherited the
  repo's own stale header comment rather than the code (§5 rule 27: the platform's narrative lags
  its config).
- **Confidence:** medium

### B4 — `CLAUDE.md` says the academy React app reads *only* `code/.env.local`; the corpus's own service doc and the repo both say `code/.env`

- **Anchor:** `CLAUDE.md:294` — *"**Ant Academy** requires its own `.env` file at
  `ant-academy/code/.env.local` (not the repo root — **the React app reads only from
  `code/.env.local`**)."*
- **Contradicted by my assigned file:** `corpus/services/ant-academy.md:391` — *"The **app's** env
  file is `code/.env`, not the repo root"* — with the recipe `cp .env.example .env` at `:395` and
  `REQUIRE_ORGANIZATION_MEMBERSHIP=0` prescribed *"in `code/.env`"* at `:412`.
- **Why the corpus file is right and `CLAUDE.md` is wrong:** measured at `ant-academy 22df69dd`, the
  repo's own guide says `code/.env`: `CLAUDE.md:164` — *"Copy `.env.example` to `.env` and fill in."*
  — and `CLAUDE.md:129` — *"…with `CLERK_SECRET_KEY` set in `code/.env`."* `code/.gitignore:22-24`
  ignores `.env`, `.env.local` **and** `.env.*.local`, i.e. all three are expected to exist locally.
  Next.js loads `.env.$(NODE_ENV).local`, `.env.local`, `.env.$(NODE_ENV)` **and** `.env`, so "reads
  only from `code/.env.local`" is false as a statement about the framework. The narrower sibling
  claim in `corpus/ops/secrets-spec.md:61` — *"the exact file Next.js precedence reads (`.env` is
  absent)"* — is defensible, because on a provisioned demo `.env` genuinely is absent; `CLAUDE.md`
  generalises it into a property of the app.
- **Why I hesitated:** the anchor is **outside this reading's declared scope** (`corpus/services/**`
  + `corpus/architecture/**`), and the in-scope side is the correct one — so booking it puts a
  finding on a file no seat was dealt. I booked it anyway because the instrument says a
  self-contradiction is a finding regardless of which side is right, and because §5 rule 19's
  corollary names `CLAUDE.md` as the tree's highest-propagation site, read by every agent before any
  doc is: a reader who takes `CLAUDE.md` literally will not put keys where `ant-academy.md` tells
  them to.
- **Confidence:** medium

---

## Minors

- `corpus/services/roadrunner.md:13-14` — **doubled word across the line break**: `:13` ends
  *"…`service_desired_count = 1` and has"* and `:14` begins *"has **not been touched since**…"*.
- `corpus/services/ant-academy.md:304` — *"~26 Playwright e2e spec files (tests/e2e/)"*. Measured at
  `22df69dd`: `code/tests/e2e/` holds **31** `*.spec.js` files (the directory has 40 entries, of
  which 2 are `_`-prefixed helpers and 7 are PNGs under `widgets.spec.js-snapshots/`). An undercount
  behind a `~`, with no consequence for a reader.
- `corpus/services/ant-academy.md:176` — *"`code/public/catalog.json` (~2,667 entries)"*. Measured at
  `22df69dd`, the file's top-level `courses` array has **2,692** members (`version 1.0`,
  `generatedAt 2026-07-28`, 7 languages). Hedged with `~`; the claim it supports (thousands of
  entries served while the grid renders 0 cards) is unaffected.
- `corpus/architecture/architecture_overview.md:348-360` — **broken blockquote continuation**: the
  block opens `>` at `:346-347`, then `:348` through `:360` carry no `>` prefix before `:361`
  resumes with `> v9.0")…`. Markdown lazy continuation keeps it inside the quote, so there is no
  rendering break today, but any future edit that inserts a blank line into that span will silently
  split the block. No factual consequence.
- `corpus/architecture/architecture_overview.md:228` — the Shared-Libraries table's header
  blockquote says *"Imported as private Go modules"* over a five-row table that includes `authn`,
  which `:80` of the same file and `dependency_map.md:49` both state is imported by nothing
  (measured: 0 `go.mod` requires and 0 Go-source imports of the standalone module across all seven
  Go clones). Same family as B1; folded here because the `authn` row's own cell states the truth.

---

## Positively cleared (audited zeros)

**ENUMERATED** — predicate re-derived from source independently, cardinality compared:

1. **`architecture_overview.md:381-388`, the multi-tenancy fence** — the one §5 rule 24 says three
   seats got wrong. Re-derived from `app` `internal/data/ent/schema/` @ `ad9f3c49`, set first:
   139 `.go` files, 4 of which contain no `ent.Schema` (`database_types.go`, `mixin.go`,
   `skiller_mixins.go`, `skillpath_mixins.go`) → **135** schemas, each declaring exactly one
   (`grep -c 'ent\.Schema'` sums to 135 with no file >1). `OrganizationMixin{}` appears **30** times,
   one commented out at `user_resource.go:22` → **29 live**. All `Policy()` declarations in the
   directory: `UserMixin`, `OrganizationMixin` (both mixins), `Membership`
   (`org_membership.go:172`, `AllowCurrentOrgEdgesOrSkipRule`), `Organization`
   (`organization.go:56`, `FilterSameOrganizations()` at `:96`), `User` (`user.go:116`, which
   filters by **user** — `DenyNotSameUser` / `FilterSameUserRule`, correctly excluded). **31 = 29 + 2.**
   Then the unpoliced side: 45 files declare an `organization_id` field, 7 use `OrganizationIDMixin{}`
   (`category`, `jobrole`, `similarity`, `skill`, `specialization`, `studio_document`, `studio_task`),
   union 52; minus the 30 policed members that carry the column (`Organization` carries none), minus
   `skiller_mixins.go` (a mixin, not a schema) = 24; minus `academy_feedback.go`, the one that carries
   a live `UserMixin{}` policy = **23 with no policy at all**, of which **16** use neither org mixin.
   Every figure in the passage reproduces. I re-checked the mixin lists of all seven
   `OrganizationIDMixin` users by hand after a substring-matching grep falsely flagged four of them
   as `UserMixin` carriers — they carry `CreatedByUserMixin{}`, which declares no `Policy()`.
2. **43 `.graphqls` SDL files in `app` @ `ad9f3c49`**, `academy.graphqls` among them — clears
   `ant-academy.md:63` (*"1 of 43"*) and `architecture_overview.md:40` (*"the academy types are one
   SDL file … there is no separate academy subgraph"*).
3. **23 jobsim run-state tables** (`dependency_map.md:13`, `architecture_overview.md:179`, `:221`) —
   `terraform/migrations/20260722081626_jobsim_data_model.sql` @ `ad9f3c49` has exactly 23
   `CREATE TABLE` statements (`sessions`, `interview_aggregated_reports`, `actors`, `interactions`,
   `activity_events`, `ai_interactions`, `anticheat_results`, `anticheat_evidences`,
   `chime_recordings`, `code_submissions`, `collaborative_assets`, `conversation_extractions`,
   `interview_extraction_results`, `realtime_calls`, `recordings`, `task_checks`, `task_sub_checks`,
   `validation_attempt_results`, `validation_attempt_skill_results`, `validation_criterion_results`,
   `validation_check_results`, `validation_results`, `validation_test_runs`).
4. **`dependency_map.md:42`'s library matrix** — colony **7/7**, proto **7/7**, taxonomy **6/7**
   (absent from roadrunner), ai **2/7**, authn **0/7**, derived per repo at its own HEAD.
5. **`dependency_map.md:58`'s "21 live handlers"** — `messenger/internal/flow/flow.go:72` is the
   `AddSubscriber("backend", …)` literal; the handler list runs `:74-:103` with `:83`
   (`OrgJobSimulationAssignmentPastDueHandler`) commented out → 21 compiling handlers.
6. **`dependency_map.md:7`'s "zero `*_RPC_ADDR` variables"** — `git grep RPC_ADDR 0c91421` across
   `platform` returns hits only in `CLAUDE.md:34-35` (prose); `docker-compose.yml`, `common.yml` and
   `.env_example` have none. All five neighbouring service addresses resolve exactly:
   `AUTHORIZATION_ADDRESS` `:48`, `GOTENBERG_URL` `:57`, `JUDGE0_BASE_URL` `:59`, `REDIS_ADDR` `:66`,
   the two DSNs `:93-94`; `depends_on` `:101-109`; the in-line comment `:102-103`; the
   MESSENGER/CUSTOMERIO rationale `:84-92`; gotenberg's `profiles: [core, backend, all]` at `:183`;
   studio-desk's `depends_on: backend` at `:138-140`.
7. **`ant-academy.md:308-325`'s public-route table** — every pattern in `code/proxy.js`'s
   `createRouteMatcher` @ `22df69dd` (`:112-188`) is present in the table and every table row is in
   the matcher, including both `_meta` spellings, both dev-only conditional arms, and the
   `VISUAL_BYPASS` conjunct (`BENCHMARK_VISUAL_BYPASS === "1" && NODE_ENV === "development"`,
   `:62-64`). `/api/chapters/*` is correctly absent.
8. **`architecture_overview.md:59`'s Next version claim** — `next: "~16.2.12"` in exactly the four
   `apps/*` named (`web`, `hiring`, `integration`, `maintenance`); `apps/mobile` declares none;
   lockfile resolves `16.2.12` @ `next-web-app 8297c684`. (`packages/ui` and `configs/i18n` also
   declare it, but the claim is scoped to `apps/*` and is right.)
9. **`ant-academy.md:226-231`'s five demo patches** — `demo-stack/ant-academy.sh` wires them at
   `:56` (`ant-academy-dev-origins`), `:66` (`academy-fs-published`), `:75`
   (`academy-fs-published-body`), `:84` (`academy-fs-published-public`), `:90`
   (`ant-academy-back-to-cockpit`), **identically in the pinned per-stack clone `09d0607` and the
   authoring copy `1dc1eb8`**; all five directories exist under `demo-stack/patches/`. The launcher
   sets no `NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT` (its only mention is the comment at `:58`) and
   `ant-academy/code/.env.example:31` ships it empty, so `ant-academy.md:195-197`'s "legs 1 and 3"
   diagnosis holds. `:576-583`'s e2e-persona-gone comment and `cockpit.py:812` / `:1496` / `:327`
   resolve byte-exact in both trees, and both named fence tests exist
   (`demo-stack/tests/test_ant_academy.py`, `…_clerk_wiring.py`).
10. **`storage.md:29`'s `STORAGE_RPC_ADDR` census** — 3 hits at `2035f9a4` and at `ad9f3c49`, all
    comments, at exactly `main.go:504`, `internal/jobsimwiring/wiring.go:101`,
    `internal/storagens/callsites_test.go:189`; **15** hits at `b948604f`, of which **7** are live
    env reads (`cmd/academy-asset-upload/main.go:129`, `cmd/academyImport/main.go:231`,
    `cmd/import/main.go:50`, `wiring.go:115`, `main.go:446`, `:524`, `:992`);
    `git diff --stat 2035f9a4 ad9f3c49 -- main.go internal/storage/` is empty, as the cell asserts.
    `architecture_overview.md:354-357`'s twin (3 Go hits at `9d00a313` — `main.go:451`,
    `wiring.go:101`, `callsites_test.go:189` — against **29 lines / 18 files** repo-wide) also
    reproduces exactly.
11. **`dependency_map.md:59`'s skiller-stream cell** — the 6-drift/1-holds split is exactly right:
    at `b948604f`, `main.go:287`/`:637`/`:1039` and `wiring.go:127`/`:180` are the five
    `NewPublisher` sites and `main.go:1276` is `subServer.AddSubscriber(os.Getenv("SKILLER_STREAM"), …)`;
    at `ad9f3c49` all six read exactly the replacement text the cell quotes, while
    `internal/roles/roles.go:791` is `func (r *RoleManager) SkillerSubscriber() *pubsub.Subscriber {`
    at all three refs. `SKILLER_STREAM` @ `ad9f3c49`: **6 Go occurrences across 3 files**
    (`main.go`, `subscriber_merge_test.go`, `subscriber_wiring.go`) and **6 files** repo-wide; no
    `NewPublisher` names it.
12. **`roadrunner.md:38-41`'s repo-wide sweep** — case-insensitive, per clone at its own ref:
    app **25**, jobsimulation **8**, platform **3**, studio-desk **3**, next-web-app **1** files.
    Enumerated over every `.git` in `stack-demo` including the two nested `studio` checkouts (both 0)
    — see §"could not settle" for the one thing the list omits.
13. **`roadrunner.md:130`'s jobsimulation scope** — `git grep -in roadrunner 462343b0` returns
    **14 lines across 8 files**, of which **5 exact-case across 3**, and exactly **one** Go mention,
    `internal/runner/runner.go:3`. `app/internal/jobsimulation/simulator/stream_handlers.go:30-34` @
    `ad9f3c49` is the quoted note verbatim, ending *"NOT stream handlers."*
14. **`dependency_map.md:25`'s studio-room provider registry** — `app/studio` @ its **own** ref
    `aeec036a`: `services/ai.py:704-709` is the whole `providers` dict (`openai`, `azure`,
    `anthropic` — no Mistral), `tools/pdf2md.py:24` is `from mistralai import Mistral`, and
    `git grep -ci mistral aeec036a` sums to **22** lines over **3** files
    (`requirements.txt`, `tools/pdf2md.py`, `tools/r3.py`).
15. **Compose/`repos.yml` cardinalities** (`dependency_map.md:31`, `architecture_overview.md:223`) —
    at `0c91421`: 5 services declared in `docker-compose.yml` (sentinel, backend, studio-desk,
    next-web-app, gotenberg) + 2 in `common.yml` (postgresql, redis) = **7 effective**, **4**
    `repos.yml` entries. At `0dab54d`: 10 and 6. At `2adcf71`: **9** `repos.yml` entries with
    roadrunner at `:29-31` and jobsimulation at `:17-19`.
16. **The supergraph count** — `915da06^`'s `supergraph-config-dev.yaml` lists **three** subgraphs
    (backend, jobsimulation, cms); `915da06` takes it to **one**. Clears
    `architecture_overview.md:222`/`:294`'s *"3→1, and its own commit subject's '2→1' is wrong"*
    against the commit body, which stages the jobsimulation removal in the same commit.

Also verified exactly (anchor + surrounding construct read, not grepped): every `app` anchor in
`storage.md:29` and `:62-86` (`main.go:516-517`, `:518-523`, `:524-525`, `:529-535`, `:547`, `:1102`;
`internal/storage/service.go:22`/`:24`; `internal/storage/storage.go:193-200`; `env_guards.go:37-44`),
the `storage` repo's own `internal/storage/storage.go:122` (`GetPresignedUrl` → `("", nil)`),
`storage/terraform/main.tf` at `9f8cb53` (18 lines, `:9-11` and `:13-16` quoted verbatim, `custody`
0 occurrences, no `moved` block) versus `4ce8ece5` (100 lines, module declared, `storage.tf:23-24`
still saying *"relocate the assets out of this module BEFORE M907"*, retired by `d3e6d32` whose body
says *"M903 never ran."*), `roadrunner`'s `cmd/root.go:84`/`:110` port defaults and the `2adcf71`
compose block (`:291-292`, `:296-302`), `git blame -L 19,19 87d8d443 -- terraform/main.tf` → `84a4b4f`
(the commit that added the file) with `87d8d44` touching one file / 3 insertions and `e45eb61`
changing line 11, `6092c6d2` (real, on `jobsimulation`'s `origin/main`, comment block at `:15-40`,
`service_desired_count` gone from terraform), `cms/terraform/main.tf:39 = 0` alongside `6efa1d5`'s
build-workflow deletion, `graphql-wundergraph/terraform/main.tf:20 = 1` with `private_subnets_ids` at
`:31` and no public subnet anywhere in its terraform, `d11a403`'s message-vs-diff mismatch on
`repos.yml`, the two-of-four `d11a403` RPC re-point (`CMS_RPC_ADDR` `cms:8091`→`backend:8083`,
`JOBSIMULATION_RPC_ADDR` `jobsimulation:8401`→`backend:8083`, the other two already at `backend:8083`
at `d11a403^`, `BACKEND_USERS_RPC_ADDR` at `backend:8081` from `3e85fce`), `app`'s
`internal/jobsimulation/ai/ai.go` routing anchors (`:85-88`, `:129`, `:166`, `:262-276`, `:325`),
`internal/cms/studio/markdownManager.go:29-30` + `studioManager.go:583`, and — on the academy side —
`store.js:150/162/169/202/210/215`, `writeThrough/index.js:247/259`, `beacon/route.js:1-18`,
`serverTenant.js:115-117/143-146`, `ucourses/catalog.js:31/961`, `AcademyClient.jsx:1363-1365/1906`,
every one of the eight `<TopBar` mount sites and the three `AcademyClient` render sites,
`LanguageSelector.jsx:88/97`, `i18n/locale.js:10`, `PublicHeader.jsx:20` with its single mount at
`library/page.jsx:28`, `free/page.jsx:18`, `5b05b7d9` (2026-05-05) and `e22f3230` (2026-05-18),
`layout.jsx:1/41/51/132`, `academy.css:1`, `platformUrls.js:1-32`, `pricing.js:21`, `schema.js:3`,
`knowledge/user-types.md`'s four types, the repo's `CLAUDE.md:11`, `agent.js:12/13/32`,
`serverChapterBody.js:51-67`, `not-found.jsx:43`, `draftMode.js:39/46-49`, `proxy.js:241`, and the
Ent tables `academy_chapter_progresses` / `academy_last_activities`.

`corpus/services/TEMPLATE.md` (46 lines) is a pure skeleton — no platform claim, no anchor, no count.
Its one gradeable line, `:36` *"`[service_name]` (in platform `docker-compose.yml`)"*, is a
placeholder, not an assertion. **Nothing to book.**

---

## What I could not settle

- **`ant-academy.md:90` — *"a grid of 65 real cards"* and the M238/M245 *"proven live on `billion`"*
  claims.** These are runtime observations on a stack I did not bring up. Settled only by a cold
  `/demo-up` on `billion` plus the `autoverify.json` assert (f) the same passage describes.
- **GitHub archive states** (`architecture_overview.md:3` for `graphql-wundergraph`, 2026-07-30;
  `roadrunner.md:47-48` for skiller 2026-07-01 / skillpath 2026-07-31; `roadrunner.md:84`'s
  *"(not archived)"*; `roadrunner.md:66-68` for chronos). Not derivable from a clone. The one
  *consistency* check available is negative and passes: `graphql-wundergraph`'s `origin/main` stops
  at 2026-07-30, so nothing contradicts its claimed archive date — unlike `jobsimulation`, whose
  four 2026-08-04 commits are exactly why `architecture_overview.md:20` refuses to assert an archive.
  Settled by `gh api repos/anthropos-work/<name> --jq .archived`.
- **`roadrunner.md:40-41`'s sweep list omits two trees it swept over.** *"a case-insensitive sweep of
  **every clone** at its own ref"* names five repos; my enumeration over all 15 `.git` trees under
  `stack-demo` also returns `roadrunner` itself (**23** files) and
  `stack-demo/rosetta-extensions` (**47** files). Neither omission changes the point being made
  (the retraction of *"no other platform repo references roadrunner"*) — the subject repo naming
  itself is uninformative and rext is tooling, not a platform clone — so I did not book it. It is
  recorded here because *"every clone"* is a universal and its stated set is a proper subset;
  if the sentence is meant to be re-derivable, it needs its scope word.
- **`architecture_overview.md:13`/`:236`'s `≥42,790 skills / ≥22,470 job roles`.** A production-DB
  measurement, unreachable from the clone set. The corpus is internally consistent on it and
  explicitly marks the "60K/18K" figures as refuted/unverified; settled only by a `/db-query`.
- **`storage.md:92`'s pointer into `knowledge/plan/platform-defect-register.md`.** Barred by the
  briefing's hard bar 1; I did not open it, so the existence of that register entry is unverified
  by me.
