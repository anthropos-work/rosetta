# Seat B — reading #30 (M257x iter-116)

## Refs I graded against (recorded at the reading's open, re-checked at close)

| repo | path | HEAD | origin/main |
|---|---|---|---|
| app | `stack-demo/app` | `ad9f3c49` | `ad9f3c49` (level) |
| app/studio (nested, own checkout) | `stack-demo/app/studio` | `aeec036a` | — |
| platform | `stack-demo/platform` | `0c91421d` | level |
| next-web-app | `stack-demo/next-web-app` | `8297c684` | `f97ba659` |
| sentinel | `stack-demo/sentinel` | `f2c46190` | level |
| storage | `stack-demo/storage` | `4ce8ece5` | `9f8cb532` |
| messenger | `stack-demo/messenger` | `fa47850d` | `e9421c68` |
| cms | `stack-demo/cms` | `ca50c817` | `f38c0c4a` |
| graphql-wundergraph | `stack-demo/graphql-wundergraph` | `60c229f3` | — |
| roadrunner | `stack-demo/roadrunner` | `87d8d443` | — |
| jobsimulation | `stack-demo/jobsimulation` | `462343b0` | `82cb66ec` |
| studio-desk | `stack-demo/studio-desk` | `41ee3575` | level |
| ant-academy | `stack-demo/ant-academy` | `22df69dd` | level |
| rext — **per-stack CONSUMPTION clone (the tree that settles "what the tooling does on a stack")** | `stack-demo/rosetta-extensions` | `09d06070` | — |
| rext — authoring copy | `.agentspace/rosetta-extensions` | `1dc1eb82` | — |

All fifteen match the addendum's superseded table exactly. Corpus HEAD `0f242db`, working tree clean
for all five assigned files.

**Which tree settled each rext claim:** every rext claim in this report was read in the **pinned
per-stack consumption clone** `stack-demo/rosetta-extensions @ 09d06070` (rule 45). Where the
authoring copy could plausibly differ I read both (the `ai_readiness_interview_report.go` KPI set) —
byte-identical, stated inline.

## Positive controls

| file | `wc -l` | read in full? |
|---|---|---|
| `corpus/services/ai-readiness.md` | 712 | yes |
| `corpus/services/cms.md` | 336 | yes |
| `corpus/services/chronos.md` | 245 | yes |
| `corpus/services/academy-backend.md` | 160 | yes |
| `corpus/services/README.md` | 90 | yes |

(`wc -l` counts newlines; the Read tool reports 713 total lines for `ai-readiness.md` because its
final line is unterminated. Same file.)

Search positive controls run in the same pass: `git grep -c "service_desired_count"` returned a hit
in `roadrunner`/`cms`/`storage@HEAD` and **exit 1 / no output** in `jobsimulation@origin/main` — so
the zero there is a measured zero, not a broken pipeline. `git log -S 'SKILLER_RPC'` returned **7**
against the same repo+ref where `-S 'MESSENGER_RPC'` returned **0**. The nested `app/studio` repo was
grepped **at its own ref `aeec036a`**, never through `app`'s HEAD (which returns a guaranteed 0 for
every predicate — `app/.gitignore:79` `studio/*`).

---

## Blockers

### B1 — the anchor-rot passage's OWN pins are the fourth generation, and they were already wrong in the commit that wrote them

- **Anchor:** `corpus/services/ai-readiness.md:55-58` —
  > "iter-102 replaced `:459` with **`:496`**; re-derived at iter-115, `:496` is the **closing line of
  > the `> **✅ CORRECTED M219 …**` blockquote** (`:476-496`) — the very blockquote the two sentences
  > above name as the *wrong* target for the previous generation. **The block opens at `:498` and the
  > quoted parenthetical is at `:500`.** **The pin is deleted rather than re-derived to `:498`**"

- **Why it is false:** every one of those four numbers names the wrong construct at corpus HEAD
  `0f242db`, measured with `awk 'NR>=N'` on the file on disk:

  | the passage says | what is actually there at HEAD | where the named construct actually is |
  |---|---|---|
  | `:476-496` = the `✅ CORRECTED M219` blockquote | `:476-496` is the middle of the `keepStartedMembers` correction bullet and the *"Closed cycle → the dashboard reads frozen snapshots"* bullet | the blockquote is **`:512-536`** |
  | `:496` = its closing line | `:496` = "`frozen `ai_readiness_snapshots`), after iters 03→06 falsified the active-signals path — on the premise that the`" | closing line is **`:536`** |
  | `:498` = the `⚠⚠ M51 iter-08/09` block opens | `:498` = "`per-object-RPC class). ⚠️ **That premise was refuted at M219: the recompute takes 2.09 s.**`" | the block opens at **`:538`** |
  | `:500` = the quoted parenthetical *"(now `aireadiness/readiness.go`, formerly `workforce/ai_readiness.go:512`)"* | `:500` = the `ops/demo/stories-spec.md:599` link | the parenthetical is at **`:540`** |

  **This is not drift from a later edit — the numbers were stale on arrival.** The file's most recent
  commit is `9d31cf1` *("fix(M257x/115): repair ai-readiness — … the third-generation pin is RETIRED,
  not re-derived")* and `git status` is clean, so HEAD content == `9d31cf1` content. At the *previous*
  commit `f8be5a1` all four were exact (verified: `git show f8be5a1:corpus/services/ai-readiness.md`
  → `:476` opens the blockquote, `:496` closes it, `:498` opens the M51 block, `:500` carries the
  parenthetical). `9d31cf1` inserted ~26 lines **above** them and re-pointed nothing below the edit —
  §5 rule 34 exactly, committed by the repair whose entire declared subject was this anchor's three
  prior generations.

  Material harm: `:498`/`:500` are stated in the present tense as *where the target is now*, and the
  final sentence — *"The pin is deleted rather than re-derived to `:498`"* — offers `:498` as the
  correct value. A reader following it lands 40 lines short, inside a retraction about live-recompute
  timing, which is a different claim entirely. The passage that says **"Search the construct"** is
  itself the fourth instance of the failure it documents.

- **Confidence:** high.

### B2 — `cms.md`'s "Data bullet, :44-47" points at the **Domain** bullet

- **Anchor:** `corpus/services/cms.md:216` — "*legacy `cms` schema is non-authoritative. Consistent
  with the **Data** bullet, :44-47 above*"
- **Why it is false:** at HEAD `:44` = "`> Where everything went:`", `:45` = blank, `:46-47` = the
  **Domain** bullet (`app/internal/cms/` … `wiring.go`). The **Data** bullet (`similarities`,
  `similarity_categories`, … `20260724132049_cms_data_model.sql`) is at **`:48-51`**. Same mechanism
  as B1 and the same commit family: at `f8be5a1` the Data bullet *was* at `:44-47`; `b4bdbfc`
  ("fix(M257x/115): repair P10 (11 sites, 5 files) and P20") inserted 4 lines above it and left the
  citation alone.
- **Why I hesitated, and why I still booked it:** the target is four lines below the anchor and a
  reader will find it. But the frozen instrument lists *"wrong line anchors that point at the wrong
  construct"* as a BLOCKER example and resolves doubt upward, and the anchor resolves onto a
  **different, adjacent bullet making a different claim** — the exact "resolves but names the wrong
  construct" class §5 rule 21 counts separately from self-contradiction.
- **Confidence:** high (that the anchor is wrong); medium (that it is a BLOCKER rather than a MINOR).

### B3 — `cms.md`'s "Studio bullet, :70-71" points at the **Events** bullet

- **Anchor:** `corpus/services/cms.md:240` — "*see the **Studio** bullet, :70-71 in the banner at the
  top of this doc*"
- **Why it is false:** at HEAD `:70-71` are the first two lines of the **Events** bullet ("`app` owns
  the `CMS_STREAM` subscriber… `.AddHandler(...)`"). The **Studio** bullet (*"the Python
  `anthropos-studio-room` project is now pulled into the **`app`** image via the CI `additional_repo`
  mechanism (app v1.360.1)"*) is at **`:75-76`**. Correct at `f8be5a1`; broken by the same `b4bdbfc`
  insertion. The sentence citing it is specifically routing the reader to the `additional_repo`
  evidence for a HISTORICAL warning, and `:70-71` contains no such evidence.
- **Confidence:** high (anchor wrong); medium (BLOCKER vs MINOR — same hesitation as B2).

---

## Minors

- `corpus/services/ai-readiness.md:499-500` — "*Retracted here and at
  [`ops/demo/stories-spec.md:599`]*". `stories-spec.md:599` is a line **stating** the retracted
  premise ("*path — believing the live-recompute never completed…*"); the **retraction** blockquote is
  at `:601-604`. Defensible as "the retraction is recorded at that claim's site", so not booked.
- `corpus/services/ai-readiness.md:316-317` — "*`aireadiness.Manager` … is constructed with `db`,
  `ent`, `orgRepo`, `ai.AI`, a pub `EventPublisher`, the `academy.EmbeddingsManager`, and the
  `WorkforceDirectory`*". The real signature is
  `NewManager(logger *slog.Logger, db *sql.DB, aiClient ai.AI, entClient *ent.Client, pub EventPublisher, academyEmbeddings *academy.EmbeddingsManager, directory WorkforceDirectory)`
  (`app/internal/aireadiness/manager.go:135` @ `ad9f3c49`). `orgRepo` is not a parameter — it is
  derived *inside* the constructor at `:140` — and `logger` + `assignmentRepo` are omitted. Not false
  (the fields do end up on the Manager), but an imprecise enumeration presented as the constructor's
  inputs.
- `corpus/services/ai-readiness.md:638-639` — "*`computeCycleTotals` (`how_we_measure.go:253-261`)*".
  `:253-254` is the tail of the *preceding* function (`return out, rows.Err()` / `}`); the
  doc-comment starts at `:256` and the func at `:260`. Range includes the target, so it resolves.
- `corpus/services/academy-backend.md:121-138` — the `cmd binaries` list omits
  `cmd/academy-asset-upload`, which exists at `ad9f3c49` alongside the four listed. Omitted list
  member; the list is not framed as exhaustive.
- `corpus/services/chronos.md:22`, `:171`, `:190` — "`stack-dev/chronos`" as the codebase path and
  `cd stack-dev/chronos` in two runnable blocks. No `chronos` clone exists in any `stack-*` workspace
  (`ls -d stack-*/chronos` → no matches), and the doc's own banner says `make init` no longer clones
  it. Historical-section prose, no factual consequence for a reader who read the banner.

---

## Positively cleared (audited zeros)

Labelled per the briefing. **ENUMERATED** = I derived the predicate's set from source and compared
cardinality. **RE-DERIVED** = I only re-computed arithmetic the document itself shows.

### ai-readiness.md

- **ENUMERATED** — the 31 default readiness skills. `app/internal/aireadiness/defaults.go:29-63` lists
  **19** entries at weight `1.0` and **12** at `0.5` — I counted the literal set, not the comment
  above it. Sum = `19·1.0 + 12·0.5 = 25.0`, the denominator the doc uses. `defaultReadinessSims`
  (`:75-79`) is **3** entries: `{StepSimulation,"tech"}`, `{StepSimulation,"business"}`,
  `{StepInterview,"both"}`. The rext seeder mirrors it verbatim
  (`stack-seeding/seeders/ai_readiness_config.go:88-96` @ `09d06070`, same three uuids).
- **ENUMERATED** — the five usage KPI ids. `usageDimSpecs` (`how_we_measure.go:1160-1181`) has exactly
  **5** entries: `avg_adoption`, `avg_transformation`, `avg_originality`, `avg_depth`, `avg_ownership`
  — and the seeder emits exactly those five at
  `stack-seeding/seeders/ai_readiness_interview_report.go:185-189` (**identical in both rext trees**,
  `09d06070` and `1dc1eb82`), with the comment at `:149` reading *"All five are always emitted here."*
  The doc's own note that this bullet *"said four until M257x"* is correct; the platform's own comment
  at `how_we_measure.go:1183` still says *"the four … tiles"* and is the stale one.
- **ENUMERATED** — the archetype bands. `scoring.go:26` `archetypeHighBand = 75`, `:30`
  `archetypeLowCeil = 50`, `classifyArchetype` at `:46-56` (Champion `>=75 && >=75`, Standby
  `<=50 && <=50`, tie → Explorer), buckets `bucketKnowledge` `:67-78` giving None 0-24 / Low 25-50 /
  Medium 51-74 / High 75-100. `knowledge = (step1+step2)/70×100` (`:92-95`), `usage = step3×100/30`
  (`:100-105`), `score = t1+t2+t3` (`:120`); maxes `readiness.go:29-31` = 30/40/30.
- **ENUMERATED** — the CSV column count. `AIReadinessCSVColumns` (`csv.go:17-33`) has **15** members,
  counted individually. The platform's own doc-comment at `:35` still says *"19-column"* and is the
  stale side; the corpus's "15, trimmed from 19" is right.
- **ENUMERATED** — every table in the Data-model table exists as an ent-generated relation in
  `internal/data/ent/migrate/schema.go` at `ad9f3c49`, including the plural forms the doc flags
  (`ai_readiness_user_step_progresses`, `..._notification_logs`, `..._notification_optouts`) and
  `public.interview_aggregated_reports`.
- **ENUMERATED** — the `interactions.action_type` enum is **exactly** `{email, call}`
  (`internal/data/ent/interaction/interaction.go:92-99` `ActionTypeValidator`). The doc's parenthetical
  is exact. (`internal/jobsimulation/entconverter/converter_gen.go` names more constants, but they are
  the proto-side type, not the ent enum the DB enforces — I read both before concluding.)
- Every `next-web-app` anchor in this doc, at `8297c684`: `AIReadinessClient.tsx` `:78`
  (`SHOW_SECONDARY_TABS`), `:135` (`useAiReadinessEnabled(true)`), `:155-156` (`effectiveCycleId`),
  `:171` (`enabled: featureOn && cyclesQ.isFetched`), `:601` (`SHOW_SECONDARY_TABS && tab.key ===
  'compare'`), and `:599` = `tab.key === 'how' ||` exactly as the retraction note says. `posthog` and
  `flag_ai_readiness` both **grep-count 0** in that 658-line file. The *"byte-identical at
  `origin/main` `f97ba659`"* claim is **measured**: both blobs sha1 `1fcd5466…`.
  `useAiReadinessActive.ts:19-32` with `:22`/`:25`/`:29`/`:32` all exact;
  `aiReadiness.constants.ts:26` exact; `urls.ts:51` = `AI_READINESS_URL` and `:52` = `TALK_TO_DATA_URL`
  exactly as the correction says; `useNavbarSections.tsx` `:4`/`:401-408`/`:403`/`:569` exact;
  `WorkforceNewClient.tsx:125-151` omits readiness (5 tabs, none of them AI-readiness);
  `useWorkforceAIReadiness.ts:23-27` has no `cycle` param (0 case-insensitive `cycle` hits in the whole
  file); `AIReadinessHero.tsx:88` = `if (!air.deadline) return null;`; `deriveMode` at
  `components/ai-readiness/useAIReadiness.ts:48-62`; `HowWeMeasureTab.tsx` `:1879`/`:1903`/`:1915`/
  `:1921`/`:1927` exact, file is **1,989** lines and `grep -c interviewQuestions` = **0**;
  `hooks/useAIReadiness.ts:326` inside `AIReadinessCycleTotals` (`:323-330`). A repo-wide
  `git grep AI_READINESS_URL` (node_modules excluded) returns exactly the 3 source hits + the KB page
  `knowledge/ai-readiness/frontend-architecture.md:15` the doc names.
- Every `app` anchor: `organization_settings.go:47`; `enum/ai_readiness.go` step types;
  `readiness.go` `:139` `computeTier1` (and the denominator really is the whole configured repertoire,
  `:158-168`), `:289`/`:290-297`/`:307-312`/`:314` `GetAIReadinessWithOptions`, `:330`
  `computeOrgBreakdowns`, `:388`+`:710` `keepInCycleStep1`, `:390`+`:730` `keepStartedMembers`, `:684`
  `queryInCycleStep1Completers`, `:825` `LoadMembers(ctx, orgID, "")` in `buildResponseFromSnapshots`;
  `steps.go:964` `queryReadinessStarters` with its SQL at `:969-974` (progress table, never
  `user_skill_evidences` — the doc's both-directions correction is right), `:962` the quoted comment,
  `:922` `queryReadinessSimCompleters`; `how_we_measure.go:285-287` the interactions join;
  `emailoverride/emailoverride.go:29` importing `app/internal/messenger/aireadinessemail` (and `:33`
  carrying the `pkg/` shorthand the correction blames), `git ls-tree ad9f3c49
  internal/messenger/aireadinessemail/` = **9** files; `compare.go` `AIReadinessCompareResponse` with
  exactly the 6 named sections; `provision.go` `GetAIReadinessSetupStatus`/`ProvisionAIReadiness`;
  `cycles.go:554` `CloseDueAIReadinessCycles`; `resolver_ai_readiness.go:37-43` returning
  `(false, nil)` for a non-enabled org.
- **ENUMERATED** — the one-active-cycle index really is *partial*, so seeding one `closed` + one
  `active` is legal: `schema/ai_readiness_cycle.go:127-129` `Unique().Annotations(IndexWhere("status
  = 'active'"))`, materialised at `migrate/schema.go:84-88` as
  `aireadinesscycle_organization_id … Where: "status = 'active'"`.
- **ENUMERATED** — the "5 Playthroughs" claim: `git grep "playthrough:" -- playthroughs/` over the
  pinned rext clone returns **4** in `manifest/ai-readiness.yaml` + **1**
  (`pt-onboarding-aireadiness-guided`) in `manifest/onboarding.yaml` = 5. The seat `pt-ai-onboard` it
  names exists (`playthroughs/seed/pt-world.seed.yaml:302`).
- rext side: `Persona.AIReadiness` (`blueprint/blueprint.go:200`), the closed enum
  `AIReadinessNotStarted = "not_started"` (`:316`), `aiReadinessStageFor`
  (`seeders/ai_readiness_funnel.go:205`) with the `stage == 0 → continue` skip,
  `TestAIReadinessFunnelSeeder_DayZeroHeroGetsNoSignals` (`ai_readiness_funnel_test.go:230`),
  `seat_append_test.go`, `contentref.go`, `ai_readiness_evidence.go`, `ai_readiness_sim_skills.go`,
  `cockpit.go:135` `LegacyReadinessPaths` / `:161` `ValidateCockpitManifest`, the demopatch manifest
  `app-aireadiness-snapshot-loadmembers.yaml` with `path: internal/aireadiness/readiness.go` at **`:42`**
  and the *"v2.7 M254 RE-POINT"* header at **`:33`**, and its fence
  `demo-stack/tests/test_aireadiness_snapshot_loadmembers_m254.py`.
- Version claims: `git tag --contains 4c28365f` → first tag `v1.351.1` ✔; the commit subject and date
  (2026-07-22) match verbatim; `v1.315.0` exists.
- **RE-DERIVED** (weakest clearance, labelled as such): `round(1.0/25.0*30)=1`, `round(0.5/25.0*30)=1`,
  `round(9.0/25.0*30)=11`, `156/199 = 78.4 %`. I also **ENUMERATED** the set behind the 25.0 (above),
  which is the half that matters.
- **ENUMERATED** — `catalog_charts` fallback direction. `how_we_measure.go:924-938`: Strengths falls
  back to `top_unexpected`, HoldsBack to `top_concerns`, and **UnexpectedAngles has no chart
  fallback**. The doc's counter-intuitive mapping is the code's, not an error.

### cms.md

- **ENUMERATED** — the compose census. `docker-compose.yml` at platform `0c91421d` declares **5**
  services (`sentinel`, `backend`, `studio-desk`, `next-web-app`, `gotenberg`) and `include: common.yml`
  adds `postgresql` + `redis` → **7 effective**; `repos.yml` has **4** `- name:` entries (app, sentinel,
  next-web-app, studio-desk). `git grep -c "_RPC_ADDR"` over compose+common at HEAD → **exit 1, no
  output** (a measured zero), and `AUTHORIZATION_ADDRESS=http://sentinel:8087` is at `:48`.
- **ENUMERATED** — the `d11a403` two-variable claim. On the `messenger` block at `d11a403^`:
  `BACKEND_USERS_RPC_ADDR` and `SKILLER_RPC_ADDR` **already** read `http://backend:8083` (`:255`,
  `:265`); the diff moves only `CMS_RPC_ADDR` (`:212→213`) and `JOBSIMULATION_RPC_ADDR` (`:215→216`).
  Exactly two. The doc's explicit refutation of the "all four" phrasing is right.
- **ENUMERATED** — the subgraph ladder, counted from `supergraph-config-prod.yaml` at each commit and
  its parent: `749dc86` **5→4**, `7c17e63` **4→3**, `915da06` **3→1**. `915da06`'s file list deletes
  *both* `schemas/cms.graphqls` and `schemas/jobsimulation.graphqls`, while its own subject line reads
  "supergraph 2→1" — precisely the trap the corpus flags.
- **ENUMERATED** — the terraform sweep. `git ls-files '*.tf'` per clone (all 15 git trees, nested
  `studio` repos included) totals **44**; a raw `find stack-demo -name '*.tf'` returns **59**; grepping
  the literal `backend.internal.anthropos:8081` across every `.tf` returns **0**; grepping it per-repo
  at each repo's own HEAD returns **6** total (app 1, rosetta-extensions 5), none of them terraform.
  All four figures reproduce exactly.
- `cms` repo: `terraform/main.tf` is **191** lines with `service_desired_count = 0` at **`:39`**;
  `go.mod:3` = `go 1.26.4`; `cmd/root.go:77`/`:78` = the 8080/8081 defaults; `Dockerfile:2`
  `golang:1.26-bookworm` / `:23` `python:3.11-slim`; `internal/studio/studioManager.go:967` @ `ca50c817`
  = `exec.Command("bash", "-c", command)` — the frozen-repo half of the M257x-iter-115 correction;
  `/webhooks/` mounted unauthenticated at `cmd/root.go:242`; `go.work` gitignored at `.gitignore:122`;
  `ent/schema/studioDocument.go` + `studioTask.go` present. `6efa1d5` exists on `origin/main`
  (`f38c0c4a`), deletes `.github/workflows/build-production.yml` (18 lines), and its body contains
  verbatim both quoted fragments.
- The shell-safety correction is exact at `ad9f3c49`: `studioManager.go:1096-1098` (the *"NEVER through
  a shell"* comment), `:1099-1101` (`exec.CommandContext(ctx, name, args...)`), `:100-103` (*"No `bash
  -c`"*), `:119` `studio/gen.py`, `:1045` `studio/postgen.py`, `:122-124` + `:126`/`:129` the venv
  argv. `git grep -c '"bash"' ad9f3c49 -- '*.go'` → **no output, exit 1** = 0 files (control: the same
  invocation against the `cms` repo returns the `studioManager.go:967` hit). `:119`/`:1045` also hold
  at the `b948604f` the doc pins them at.
- **ENUMERATED, at the nested repo's own ref** — `app/studio @ aeec036a`: `requirements.txt` is
  **9** packages (`openai anthropic rich pyyaml requests jinja2 mistralai pytest pytest-asyncio`), and
  `python-docx` is **absent**, exactly as the retraction says. `services/ai.py:705-709` is the
  three-entry provider dict; `:1-2` import only `openai`/`anthropic`.
  `git -C app/studio grep -ci mistral aeec036a` sums to **22 lines across 3 files**
  (`requirements.txt` 1, `tools/pdf2md.py` 18, `tools/r3.py` 3) — the doc's "22 hits in 3 files" is
  exact, and I used the corrected §5-rule-44 recipe (per-tree, own ref, `-i`, sum the counts).
  `tools/pdf2md.py:24`/`:96`/`:127` and `tools/r3.py:139`/`:190`/`:199-206` all exact.
  `gen.py:484-492` is exactly **nine** `parser.add_argument` calls and `gen.py:18-28` is
  `parse_argument` with `parse_known_args` + the merge — so the "`--template` is silently swallowed"
  claim holds. `studio/CLAUDE.md:12-14` is the quoted command block.
- `app` side of the Directus/webhook/RPC bullets: `internal/cms/wiring.go`, `cms_reader_switch.go`,
  `internal/cms/rpcsrv`, `terraform/migrations/20260724132049_cms_data_model.sql`,
  `REDIS_CMS_CACHE_INDEX` default 5 (`main.go:1107-1108`), `POST /api/webhook/directus`
  (`internal/web/backend/backend.go:324`) failing closed on `DIRECTUS_WEBHOOK_SECRET`
  (`main.go:1198-1199`), `main.go:1205-1211` @ `b948604f` carrying the stale *"DORMANT … until the
  M809 re-point"* comment, `internal/skillpath/session.go:205-207` with the *"cms-in-app deseam"*
  comment, `markdownManager.go:10`+`:30`, `studio/xlsx.go:13`, `studioManager.go:531`/`:583`,
  `knowledge/service-dependencies.md:52` in the past tense under *"There are no external callers of
  app's RPC mux left."*, `.gitignore:79` `studio/*`. `DIRECTUS_DATA_CONSUMERS = ("cms", "backend")`
  at `stack-core/gen_override.py:58` in the **pinned** rext clone — it does name both.
- Compose ports at `2adcf71`: `"8090:8090"`/`"8091:8091"` at `:154-155`, `PORT=8090` at `:169`,
  `RPC_PORT=8091` at `:173` — all four exact at the ref the passage names.

### academy-backend.md

- `git describe --tags ad9f3c49` → **`v1.369.0-7-gad9f3c498`**, and `CHANGELOG.md:5` reads
  `## v1.369.0 - 2026-08-05` — both stated figures reproduce. `9d00a313` is an **exact-match** tag for
  `v1.367.0`. `0e37771f` is PR **#903**, 2026-06-05, subject *"Academy backend v1.0 'ground truth'"*.
- **ENUMERATED** — `git grep -n STORAGE_RPC_ADDR 9d00a313 -- '*.go'` returns exactly **3** hits, and I
  opened all three: `internal/jobsimwiring/wiring.go:101`, `internal/storagens/callsites_test.go:189`,
  `main.go:451` — every one a `//` comment, zero reads. `main.go:471-472` @ `9d00a313` constructs
  `NewManager(storageBucket)` + `NewPublicManager(storagePublicBucket)`.
  `cmd/academyImport/main.go:239-243` is the quoted comment and `:244-246` the empty-bucket hard-error,
  both at the pinned `9d00a313`.
- **ENUMERATED** — all 11 named `academy_*` tables resolve in `internal/data/ent/migrate/schema.go`;
  the four `cmd/` binaries named all exist (plus one unlisted, booked as a MINOR); every
  `academy*` query/mutation the doc lists appears in `schemas/academy.graphqls` (`:640-712`), with
  `academyCertificate` and the catalog reads carrying `@public`; `/content/catalog.json`
  (`internal/web/backend/content.go:23`) and the `/content/admin` group behind
  `sharedTokenMiddleware(os.Getenv("ACADEMY_CONTENT_API_TOKEN"))` (`content_admin.go:35`);
  `academy_embedding_refresh` with the 03:00 cadence stated in-code at `embeddings.go:108` and
  `schema/academy_path_embedding.go:16`; `internal/aireadiness/suggested_path.go`; `SUPABASE_DB_CONN`
  in both cmd tools; the exact `academy-seed` flag set (`--user-email --user-id --fixture --reset
  --dry-run --list`, `main.go:51-56`) and `academyImport`'s `--manifest --content-root --checkpoint`.
- The FS-fallback retraction is right on both sides: `ant-academy` `code/src/lib/serverTenant.js`
  `:125-132` says *"there is NO FS-as-published fallback … the cutover is intentional, not
  reversible-on-error"* and `:143-146` resolves a null backend result to `emptyCatalogView()`; the rext
  patch `demo-stack/patches/academy-fs-published-fallback/` exists in the **pinned** clone; and the
  cross-reference `ant-academy.md:82-88` does state the same thing in bold at those lines.
- `platform 2adcf71` really is PR #23 *"remove the WunderGraph router entirely; local dev talks to
  backend"*, and `.env_example:97-98` records the name as historical while pointing at
  `http://localhost:8082/graphql/query`. The *"in production the router is still declared"* half is
  supported by `graphql-wundergraph/terraform/main.tf:10` `module "graphql"` / `:20`
  `service_desired_count = 1` — and the doc says **declared**, not *running*, which is the measurable
  half.

### README.md

- **ENUMERATED** — the index really is complete. `ls corpus/services/*.md` = **29** files; minus
  `README.md` + `TEMPLATE.md` = **27** service docs. The four tables list 10 + 5 + 8 + 4 = **27**, and
  every filename matches one-to-one. No doc is missing and none is listed twice.
- **ENUMERATED** — the "seven folded" set. `git ls-tree -d ad9f3c49 internal/<d>` returns a directory
  for all seven (`skiller skillpath jobsimulation cms storage messenger customeriosync`) and **not**
  for `roadrunner`. `git ls-tree -r --name-only ad9f3c49 | grep -ci roadrunner` = **0**, and
  `git log --all --name-only | grep -ci internal/roadrunner` = **0** — so *"exists at no ref"* is
  measured over history, not just HEAD. `internal/jobsimulation/runner` exists, and
  `internal/jobsimwiring/wiring.go:123` is
  `jsrunner.NewRunnerManager(getenv("JUDGE0_API_KEY"), getenv("JUDGE0_BASE_URL"))` under a comment that
  names the removed roadrunner RPC edge.
- **ENUMERATED** — the 21 messenger handlers. `messenger/internal/flow/flow.go` @ `fa47850`:
  `AddSubscriber("backend", …)` opens at `:72` and closes at `:104`; counting the
  `pubsub.EventHandler(...)` lines and **excluding the commented-out `:83`** gives exactly **21**.
  `MESSENGER_RPC_ADDR` returns **0** in all 15 git trees, each grepped at its own HEAD.
  `git log -S 'MESSENGER_RPC' --oneline 0c91421d` = **0** with the doc's own positive control
  `-S 'SKILLER_RPC'` = **7** — both reproduce to the number.
- `roadrunner/terraform/main.tf:19` = `service_desired_count = 1`, `git blame -L 19,19` →
  `84a4b4f1 (Mattia Sasso 2025-12-15)`, a dependency-bump commit — the doc's "not a decision about the
  fold" reading is supported by the commit's own subject.
- The jobsimulation and storage teardown rows are **true at each repo's `origin/main`** and false at
  the stale local checkout — I checked both and did **not** book them. `jobsimulation` @ `82cb66ec`:
  `service_desired_count` returns 0 hits under `terraform/` and `:15-22` is the M810 decommission
  comment naming exactly the buckets/SSM/atlas-tracker survivors the corpus lists; `storage` @
  `9f8cb532`: `terraform/main.tf` is **18** lines with the ECS block gone and the `prevent_destroy`
  rationale in-comment. At the frozen *checkouts* (`462343b0` / `4ce8ece5`) both still carry a module
  block (`= 0` and `= 1`). The claims name `6092c6d2` / `838d907`, and `6092c6d2` is the commit whose
  subject is *"remove the jobsimulation ECS service and ECR repository (M810)"* — so they are graded
  at the ref they name and they hold.
- `838d907` subject is verbatim *"chore(compose): drop the storage, messenger and customerio-sync
  containers"*; at `0dab54d` the `messenger` profile existed (`docker-compose.yml:195`) and the diff
  deletes it, along with `storage-legacy` (`:134`) and `customerio-sync` (`:154`, built from a git URL
  context). `d11a403` removes `cms`, `jobsimulation`, `roadrunner` from `repos.yml`.
  `platform-migration-status.md:101` is the `customerio-sync` row the banner cites it for, and
  `README.md:20-23` is the roadrunner ⚠️ that `:39` cites.
- **ENUMERATED, and it clears README.md rather than convicting it** — `README.md:55` "Next.js **16**"
  is correct: all four next-bearing app packages plus `packages/ui` declare `"next": "~16.2.12"` at
  `8297c684`. See the cross-scope note below.

---

## What I could not settle

- **GitHub repository archive state.** `README.md:54` ("graphql-wundergraph … repo ARCHIVED") and
  `README.md:81` ("chronos … **The GitHub repo is NOT archived** (last push 2026-04-23)") are claims
  about github.com metadata that no local clone carries. Both agree with
  `platform-migration-status.md:96`/`:99`, so the corpus is at least self-consistent. Settling them
  needs `gh api repos/anthropos-work/{graphql-wundergraph,chronos} --jq .archived`.
- **Most of `chronos.md`.** The `chronos` repo is in no clone set and no `stack-*` workspace, so the
  Go version, ports, sqlc/Cobra choices, `schema.sql` DDL, the triggerer's `LIMIT 10` /
  `FOR UPDATE SKIP LOCKED` / 3-second timeout, and the RPC/event shapes are unverifiable here. What I
  *could* check is true: the removal commits (`platform 045857c`, subject and file list;
  `jobsimulation 09631fb2`, exact subject), chronos's absence from compose/`repos.yml` at
  `0c91421`, and the Asynq successor at `app/internal/jobsimwiring/worker.go:24-35`.
- **`app/knowledge/ai-readiness/overview.md` as "source of truth"** and the per-DB row counts the doc
  quotes as measurements (`interview_extraction_results` 165 rows, `conversation_extractions` 0 rows,
  the 2.09 s / 24 ms / 180 s / 19 ms latencies, the 5→345 fan-out) — live-stack readings, not
  re-derivable from a clone. The doc says *"Re-measure before relying on either number"*, which is the
  right disclosure.
- **Whether `infrastructure` still declares `module.cms_euwest1`** — `cms.md:83-89` says explicitly
  that it is not visible from any clone set and asserts neither way. I confirmed the two opposing
  in-repo facts it reports (the intact 191-line module at `ca50c817` and the `6efa1d5` workflow
  deletion on `origin/main`) and agree the honest state is "report both, assert neither."
- **Cross-scope observation, not booked against my files.** The "Next.js 15" claim survives at
  **`CLAUDE.md`** ("Next Web App: Next.js 15 monorepo on Vercel") and
  **`corpus/ops/staging-bringup.md:141`** ("next-web-app/ # Next.js 15 frontend monorepo"), while all
  9 in-scope corpus sites — including my `README.md:55` — say 16, which is what the source says
  (`~16.2.12`). Both survivors sit **outside this reading's scope** (`corpus/services/**` +
  `corpus/architecture/**`), which is §5 rule 19's "a claim leaks to the edge of the previous repair's
  scope and stops there" — and `CLAUDE.md` is the highest-propagation site in the tree. Flagging it
  because no seat in this partition can book it.
- **A stale intra-corpus anchor pointing *into* my file set, from outside it.**
  `corpus/ops/demo/stories-spec.md:603` cites the live-recompute retraction as
  `services/ai-readiness.md:390,458`; at HEAD `:390` is a *Local development* sentence about PostHog
  and `:458` is the "Which table the dashboard reads" heading — the retraction is at `:426` and
  `:498-500`. Same class as B1/B2/B3 and probably the same commit family, but the offending text is in
  `corpus/ops/**`, outside this reading's scope, so I am recording it rather than booking it.
