# Seat D — reading #29/#30 (M257x iter-116)

## Ground truth actually used

Re-derived at this seat's open, matching the briefing ADDENDUM in every row:

| repo | path | sha read |
|---|---|---|
| app | `stack-demo/app` | `ad9f3c49` (== `origin/main`; `git describe` → `v1.369.0-7-gad9f3c498`) |
| app/studio (nested, own checkout) | `stack-demo/app/studio` | `aeec036` |
| platform | `stack-demo/platform` | `0c91421` |
| next-web-app | `stack-demo/next-web-app` | `8297c684` |
| sentinel | `stack-demo/sentinel` | `f2c46190` |
| studio-desk | `stack-demo/studio-desk` | `41ee3575` |
| ant-academy | `stack-demo/ant-academy` | `22df69dd` |
| storage / messenger / cms / graphql-wundergraph / roadrunner / jobsimulation | `stack-demo/*` | `4ce8ece5` / `fa47850d` / `ca50c817` / `60c229f3` / `87d8d443` / `462343b0` (plus `origin/main` where a claim names a commit past the checkout) |
| rosetta-extensions — **pinned per-stack consumption clone** | `stack-demo/rosetta-extensions` | `09d06070` |
| rosetta-extensions — authoring copy | `.agentspace/rosetta-extensions` | `1dc1eb82` |

**Tree used per claim (rule 45):** every `clerkenstein/`, `stack-seeding/` and `alignment/` claim in this
seat's files is a claim about **what the tooling does on a stack**, so all of them were settled in the
**pinned per-stack clone `stack-demo/rosetta-extensions @ 09d06070`**. No fence verdict was quoted, so
the authoring copy was read only to enumerate section directories (both clones agree: 12 dirs).

**Nested repos:** `stack-demo/app/studio` was grepped **at its own ref `aeec036`**, never at `app`'s.
`stack-demo/messenger` was checked for nested repos (`find -name .git`) → none, and for NUL-bearing
tracked files (`tr -dc '\000' | wc -c`) → none, before any absence was booked.

## Positive controls

| file | `wc -l` | read in full? |
|---|---|---|
| `corpus/architecture/service_taxonomy.md` | 523 | yes |
| `corpus/services/clerkenstein.md` | 374 | yes |
| `corpus/architecture/security_compliance.md` | 293 | yes |
| `corpus/services/sentinel.md` | 166 | yes |
| `corpus/services/ai-labs.md` | 158 | yes |
| `corpus/services/db-backup.md` | 31 | yes |

`corpus/ops/platform-alignment.md` (2,803 lines) §5 read in full before starting, as instructed.

---

## Blockers

### B1 — `sentinel.md:5` publishes a derivation whose stated result is 1; measured it is 0, on three instruments

- **Anchor:** `corpus/services/sentinel.md:5` —
  > "What survives, and was re-derived: messenger's Go source imports no authorization client
  > (`git grep "authorization\|AUTHORIZATION_ADDRESS" fa47850d -- '*.go' go.mod` returns **one unrelated
  > hit**, against `colony` present as a positive control)"

- **Why it is false:** the published command returns **zero**, not one. Run verbatim at the named ref in
  `stack-demo/messenger`:

  ```
  git grep "authorization\|AUTHORIZATION_ADDRESS" fa47850d -- '*.go' go.mod   → rc=1, no output
  git grep -in "authorization" fa47850d                                        → rc=1, no output (repo-wide, case-insensitive)
  git grep -n  "AUTHORIZATION_ADDRESS" fa47850d                                → no output (repo-wide)
  ```

  All three instruments of §5 rule 44 agree, and the absence is *established*, not assumed:
  * **positive controls pass** — `git grep -lc colony fa47850d` → **15 files**; `git grep -c func …
    '*.go'` → non-empty; so the pipeline, the ref and the pathspec all work.
  * **mechanism 1 (gitignored-but-tracked):** bare `grep -ric authorization --include='*.go' .` also
    returns 0 for every file it lists (`main.go:0`, `cmd/send.go:0`, …).
  * **mechanism 2 (NUL-bearing):** `git ls-files -z | … tr -dc '\000' | wc -c` → **no tracked file in
    `messenger` contains a NUL byte**, so neither `grep -I` nor `git grep` is skipping anything.
  * **mechanism 3 (nested repos):** `find . -name .git -maxdepth 3` → only the repo root. No sub-repo
    can be hiding a hit.

  So `messenger @ fa47850d` contains the token "authorization" **nowhere at all**, in any case.

- **Why I hesitated, stated plainly:** the error is in the *conservative* direction — the load-bearing
  proposition ("messenger's Go source imports no authorization client") is not merely unaffected, it is
  more strongly true than the corpus says. I book it anyway because this is a **published, runnable
  derivation whose stated result does not reproduce**, and the reader who re-runs it gets exactly the
  signature §5 rule 2 exists to warn about: a 0 that is indistinguishable from a broken pipeline. The
  briefing lists "wrong counts" as a BLOCKER example, and this sentence exists precisely to be re-run
  (it was rewritten at M257x iter-115 to replace a present-tense claim with a measured one).
- **Confidence:** high (on the measurement); medium (on the blocker/minor grade).

### B2 — `service_taxonomy.md:403` cites `:142`, which is a *different* blockquote; the archive-state note begins at `:148`

- **Anchor:** `corpus/architecture/service_taxonomy.md:403` —
  > "the GitHub repo was **archived 2026-07-30** (a dated snapshot — see the archive-state note above the
  > *Archived / merged* table, **`:142`**; the clone is consistent with it, no commit after that date)"

- **Why it is false:** `service_taxonomy.md:142` is inside the **"Two different fates shared this table"**
  blockquote (`:139-146`), and reads:
  ```
  142 | > so a bare `make up` started all three as unfederated husks — the `running_but_unfederated` state in
  ```
  That note is about *merged-into-app ≠ gone-from-compose*, not about archive state. The **archive-state
  note** the sentence points at is the *next* blockquote, `:148-160` (*"Every `ARCHIVED <date>` in this
  table is a DATED SNAPSHOT, not a derived fact."*). The anchor resolves and names the wrong construct.
- **What makes it a blocker rather than drift:** the file's own note at `:156-160` records that this
  exact class already rotted **twice** in this table ("iter-100's edit shifted this table … iter-115's
  repair shifted the table again and the Jobsimulation number landed on the table **header**") and
  concludes *"A same-file line pin into a growing table is not worth its self-heal — **search the row
  name**."* The iter-115 repair removed the row numbers and left this pin — a third generation of the
  same defect, surviving the repair whose subject it was. §5 rule 34 is the governing rule.
- **Confidence:** high.

### B3 — `service_taxonomy.md:407` cites `:67-68` for a sentence that is at `:74`; `:67-68` are the Characteristics bullets

- **Anchor:** `corpus/architecture/service_taxonomy.md:407` —
  > "Consistent with **`:67-68`** above (*"There is no `graphql` profile"*)."

- **Why it is false:** the quoted string occurs exactly once in the file, at `:74`:
  ```
   74 | `gotenberg` and the three always-on base services. **There is no `graphql` profile, and no cms /
  ```
  `:67-68` are two members of the *Tier-1 Characteristics* bullet list:
  ```
   67 | - **Communication**: HTTP/RPC + Redis Streams
   68 | - **Database**: PostgreSQL — **one schema, `public`, owned by `app`** …
  ```
  Neither line mentions profiles at all. A reader following the anchor to verify the "consistent with"
  cross-check lands on the DB/communication bullets and cannot find the quoted sentence — the anchor
  resolves but names the wrong construct (§5 rule 21's second class, §5 rule 46's second bullet).
- **Confidence:** high.

### B4 — `security_compliance.md:23-25` claims the derivation ranged over "all eight service terraform trees in the clone set"; the clone set holds **ten**

- **Anchor:** `corpus/architecture/security_compliance.md:23-25` —
  > "Re-derived across **all eight** service terraform trees in the clone set (`app`, `sentinel`,
  > `graphql-wundergraph`, `messenger`, `cms`, `roadrunner`, `storage`, `jobsimulation`): the token
  > `public_subnet` occurs **0 times**, and **every one of the eight** passes
  > `private_subnets_ids = var.platform_private_subnets_ids`"

- **Why it is false:** enumerated independently, **ten** clones in `stack-demo/` carry a `terraform/`
  directory — the eight named **plus `next-web-app` and `studio-desk`**:
  ```
  app HAS · sentinel HAS · graphql-wundergraph HAS · messenger HAS · cms HAS
  roadrunner HAS · storage HAS · jobsimulation HAS · next-web-app HAS · studio-desk HAS
  (ant-academy — none; platform — none)
  ```
  And `studio-desk` is not an off-predicate outlier: `studio-desk/terraform/main.tf:11` uses the **same**
  `infrastructure//modules/services/base_service?ref=main` module the passage cites for the router, and
  `:33` passes the **same** `private_subnets_ids = var.platform_private_subnets_ids`. It is a ninth
  member of the very predicate the sentence enumerates, and it was not counted.
- **Why I hesitated:** the *conclusion* is unaffected and reinforced — I measured `public_subnet` at **0**
  in both unlisted trees too (positive control: `subnet` matches 3–4 times per tree, so the pipeline is
  live). I book it because §5 rule 24 is explicit that the failure is never in the arithmetic but in
  **the SET the arithmetic ranges over**, and this passage's whole authority is the word *"all"* — a
  reader is invited to extend "all eight … in the clone set" to a completeness it does not have, in a
  paragraph whose subject is a retraction of a previously-unsourced claim.
- **Confidence:** medium.

---

## Minors

- `corpus/services/sentinel.md:85` — cites `architecture_overview.md:335` for the quote *"the only
  cross-process RPC edge out of backend on a core stack"*. `:335` is the prose lead-in (*"**On a local
  stack** (platform `2adcf71` deleted the router …)"*); the quoted string is inside the fenced diagram at
  `:339`. Same block, four lines of drift — no factual consequence.
- `corpus/services/sentinel.md:43-48` — *"only as a fixture string in sentinel's own tests
  (`internal/authorization/casbin_test.go`, `internal/rpcsrv/rpc_test.go`)"*. A **third** file uses it the
  same way: `internal/authorization/manager_test.go:21` `manager := "manager"`, fed as a `g2` role at
  `:30`. The scope claim ("only a fixture string") is true; the enumeration is short by one.
- `corpus/services/sentinel.md:15` — *"The sentinel binary's default is 8080 (per its own
  **README**/CLAUDE.md)"*. `git ls-files | grep -i readme` in `sentinel @ f2c46190` returns **nothing** —
  there is no README. The fact itself is right and sourced: `CLAUDE.md:101` (`PORT | No | 8080`) and
  `cmd/root.go:47` `cmp.Or(os.Getenv("PORT"), "8080")`.
- `corpus/services/clerkenstein.md:21` — *"ONE private monorepo with sections (`clerkenstein`,
  `demo-stack`, `stack-injection`, `stack-core`, `stack-seeding`, `alignment`)"*. Enumerated in **both**
  clones, there are 11 code sections plus `knowledge/`: the six named plus **`dev-stack`,
  `playthroughs`, `stack-secrets`, `stack-snapshot`, `stack-verify`**. Nothing in this doc depends on the
  list; root `CLAUDE.md` carries a fuller (still incomplete: 9) version.
- `corpus/architecture/security_compliance.md:232-233` — the historical footnote *"the anchor said
  `:489`, which **is** a TypeScript codegen comment"*. At corpus HEAD `external_services.md:489` is
  `**Environment**:`. The note is a record of a repaired defect (and correctly kept as such), but its
  present-tense "is" no longer describes anything a reader can open.
- `corpus/architecture/security_compliance.md:84-85` and `:116` cite `mixin.go:98` for
  `UserMixin.Policy()`; `:98` is that func's doc comment and `:99` is `func (UserMixin) Policy() …`.
  Cosmetic on its own, but note the sibling citation in the same blockquote (`mixin.go:126` for
  `OrganizationMixin.Policy()`) uses the **func-line** convention — two conventions in one paragraph.

---

## Positively cleared (audited zeros)

Labelled per the briefing. **ENUMERATED** = predicate re-derived from source independently and
cardinalities compared. **RE-DERIVED** = only the arithmetic the document displays was recomputed
(the weakest clearance) — I avoided leaving anything in that category alone.

### The multi-tenancy fence (`security_compliance.md:78-147`) — **ENUMERATED**, and it holds

This is the fence the protocol records as wrong four times and mis-cleared by three seats
simultaneously (§5 rule 24), so I enumerated every set from source at `app ad9f3c49` rather than
re-adding the document's numbers:

| predicate | corpus | measured |
|---|---|---|
| `.go` files in `internal/data/ent/schema/` | 139 | **139** |
| files declaring `ent.Schema` | 135 | **135** (the 4 non-schema: `database_types.go`, `mixin.go`, `skiller_mixins.go`, `skillpath_mixins.go`) |
| *mention* `OrganizationMixin{}` | 30 | **30** |
| *use* it (compiled) | 29 | **29** — the excluded one is exactly `user_resource.go:22` `// OrganizationMixin{},  // We need to work on this` |
| `OrganizationIDMixin{}` users | 7 | **7**, and the names match one-for-one: `category`, `jobrole`, `similarity`, `skill`, `specialization`, `studio_document`, `studio_task` |
| plain `organization_id`, neither mixin | 18 | **18**, via the document's own restricted derivation; the bare-glob variant it warns about returns **19** as it says |
| the 16 named unpoliced files | 16 names | the measured 18 minus `org_membership.go` and `academy_feedback.go` is **exactly** the 16 names printed |
| 31 org-auto-filtering | 29 + `Membership` + `Organization` | **31**; only four files in the directory declare any `Policy()` (`mixin.go`, `org_membership.go`, `organization.go`, `user.go`) and none of them is among the 29 |
| 135 − 31 − 23 = 81 remainder | 81 | **81** |

**The predicate, not just the count (§5 rule 17):** `OrganizationMixin.Policy()` (`mixin.go:126-143`)
applies `rule.FilterOrganizationEdges()` on the **Query** policy too (`:139`), not only on mutation — so
"auto-filter by organization" is a true description of the 29, not merely "has a policy". Every named
anchor resolves to the right construct: `org_membership.go:172` `func (Membership) Policy()` ending in
`privacy.AlwaysDenyRule()` at `:185` inside `:172-188`; `organization.go:56` `func (Organization)
Policy()` with `rule.FilterSameOrganizations()` at `:96`; `user.go:116` `func (User) Policy()`;
`mixin.go:98-99` `UserMixin.Policy()` with `rule.FilterOwnerRule()`. The four tenant-scoped remainder
members resolve exactly: `lab.go:63`, `academy_chapter.go:84`, `academy_skill_path.go:78`
(`field.String("tenant_eid")`) and `skill_path_session.go:43` (`field.UUID("tenant_id", …)`); and
`academy_feedback.go` does carry `organization_id` at `:129` under `UserMixin{}` at `:64`. The two
platform self-statements quoted are verbatim: `job_simulation_session.go:5` and `jobrole.go:18` /
`category.go:15`.

### `sentinel.md:5`'s compose-service count — **ENUMERATED**; the guard verdict named in the briefing addendum is indeed wrong

Independently of the addendum's note, I counted: `docker-compose.yml` at platform `0c91421d` declares
**five** services and the sentence's five anchors are byte-exact — `sentinel:` `:5`, `backend:` `:28`,
`studio-desk:` `:112`, `next-web-app:` `:143`, `gotenberg:` `:170`. `git grep -n messenger 0c91421d --
docker-compose.yml common.yml repos.yml` returns **only comments** (`:84`, `:91`, `:102`, `repos.yml:5`),
exactly as the sentence says. Nothing to book here. (This is a *measured* clearance, not deference to
the addendum.)

### Compose / profile topology (`service_taxonomy.md`) — **ENUMERATED**

Read `docker-compose.yml`, `common.yml`, `repos.yml` and `Makefile` in full at `0c91421`:
five declared + `common.yml`'s two = **seven** effective; `core` starts **five**; exactly four services
carry `profiles:` — `backend:110`, `studio-desk:141`, `next-web-app:168`, `gotenberg:183` — and
`sentinel` carries none, nor do `common.yml`'s `postgresql` (`:2`) and `redis` (`:24`); `Makefile:10`
`PROFILE ?= core`; `repos.yml` holds exactly four repos with `app` alone at `migrations: true`
(`:14-17`) and `sentinel` at `migrations: false` (`:18-20`) while keeping `search_path=sentinel`
(`docker-compose.yml:18`). The legal profile set is `all, backend, core, frontend, studio-desk`.
`AUTHORIZATION_ADDRESS` occurs **once** across `docker-compose.yml`/`common.yml`/`.env_example`
(positive control: `.env_example` is 108 lines with 61 `=` lines, so it was searched); **zero**
`*_RPC_ADDR` anywhere. `GOTENBERG_URL` `:57`, `JUDGE0_BASE_URL` `:59`,
`app/internal/converter/gotenberg.go:31`.

### `d11a403` moved **two** variables, not four — **ENUMERATED**

`git show d11a403 -- docker-compose.yml` has exactly two `+` RPC lines (`CMS_RPC_ADDR`,
`JOBSIMULATION_RPC_ADDR`); at `d11a403~1` `BACKEND_USERS_RPC_ADDR` and `SKILLER_RPC_ADDR` already read
`http://backend:8083`. `service_taxonomy.md:434`'s correction is right, and so is its refusal of the
agentive form.

### Historic compose states — **ENUMERATED**

`0dab54d:docker-compose.yml`: `:53` `DIRECTUS_PUBLIC_BASE_ADDR`, `:56` `ENVIRONMENT=development`,
`:112-113` storage `8300/8301`, `:144` customerio-sync `8080`, `:154` `profiles: [customerio-sync, all]`,
`:166-167` messenger `8200/8201`, `:134` `profiles: [storage-legacy]`, `:195` `profiles: [messenger]`.
`a2a3ee6~1:docker-compose.yml:384` `directus/directus:10.10.1`, `:386` `8055:8055`, `:409`
`ADMIN_PASSWORD=password`; `git log -S admin@example.com --all` → **empty**, so the "only the email is
unfound" clause is exact. `2adcf71~1:docker-compose.yml:14` `"5050:8080"`, and `5050` is **0** in compose
at `0c91421`.

### The subgraph ladder — **ENUMERATED** at each named commit

`supergraph-config-prod.yaml`: `749dc86~1` → 5 (backend, skiller, jobsimulation, cms, skillpath);
`749dc86` (2026-06-24) → 4; `7c17e63` (2026-07-21) → 3; `915da06` (2026-07-29) → **1**, and
`git show --stat 915da06` deletes **both** `schemas/cms.graphqls` and `schemas/jobsimulation.graphqls`
while its own subject line says "2→1" — exactly as `service_taxonomy.md:416` warns. All three configs at
`60c229f3` carry one `- name: backend`; `schemas/` holds one file.

### Router placement + ports — **ENUMERATED**

`graphql-wundergraph/terraform/main.tf:20` `service_desired_count = 1`; `:11` `base_service` (same module
as `app/terraform/main.tf:172`); `:13` `use_fargate = false`; `:31` `private_subnets_ids`; `:48-49`
container/host `${local.port}`; `locals.tf:8` `port = 8080`; `config.prod.yaml:5`
`listen_addr: 0.0.0.0:8080`. `public_subnet` = **0** in all ten terraform trees (see B4 for the
enumeration defect; the *conclusion* is unaffected).

### Terraform / M810 state — **ENUMERATED**, and the pinned form is correct

`jobsimulation/terraform/main.tf:15-40` **at the ref the cell names (`6092c6d2`)** is exactly the M810
decommission comment enumerating livekit/chime buckets, SSM and the atlas tracker; the file is 56 lines
there and the `module "jobsimulation"` block is gone (at `origin/main` `82cb66ec` the only match is that
comment). The checkout `462343b0` still carries the block at `:31`/`:40` — but the claim names its ref,
so it grades there and is TRUE (§5 rule 33). **Do not read this onto cms:** `cms/terraform/main.tf:39`
`service_desired_count = 0` in a **191-line** file at *both* `ca50c817` and `origin/main f38c0c4a`, and
`6efa1d5` (2026-08-04, merged `f38c0c4`) does delete `.github/workflows/build-production.yml` (18 lines)
under the quoted subject. `roadrunner/terraform/main.tf:19` `= 1`.

### Shared-library set — **ENUMERATED across all seven Go clones**

`ai` appears in **only** `cms/go.mod:9` and `jobsimulation/go.mod:11`, both `v1.40.2`;
`app` folded it in-tree at `1e457fa70` (2026-08-04, "refactor(ai): fold the ai library into app as
internal/ai") and `app/internal/ai/` exists. `authn`: **0** hits in all seven `go.mod`; control —
`colony` in **7/7** (`app`/`sentinel`/`messenger` `v0.35.2`, `cms`/`jobsimulation` `v0.35.1`,
`storage`/`roadrunner` `v0.34.3`).

### Studio-Room / studio-desk / ant-academy — **ENUMERATED at the nested ref**

`app/studio @ aeec036`: `services/ai.py:705-708` is the entire `providers` dict (openai/azure/anthropic);
`ai.py:1-2` imports only `openai`/`anthropic`; `mistralai` is imported **only** at `tools/pdf2md.py:24`;
`git grep -ci mistral aeec036` = 1 + 18 + 3 = **22 lines / 3 files**, matching the published figure;
`gen.py` registers exactly **nine** `add_argument`s (the nine named) and `parse_argument` uses
`parse_known_args` at `:19`, so `--template` is silently absorbed; `CLAUDE.md:12-14` is the quoted entry
point. studio-desk `@41ee3575`: `.env.example:44` `VITE_GRAPHQL_ENDPOINT=http://localhost:8082/graphql/query`,
`:45` `VITE_ENVIRONMENT=production`; **0** react/vue/angular in `package.json`, **0** `.tsx`/`.jsx`
tracked; 7 language flags; Copilot models are `gpt-5.2`/`gpt-5-mini`/`gpt-5-nano` (`src/services/ai/config.ts:21-31`).
ant-academy `@22df69dd`: `next ^16.2.6`, `react ^19.2.5`, `expo ~54.0.33`, `deploy-academy.yaml` present,
`server.js:14,18` (throws when unset), `backendContent.js:36,102-103`, `serverTenant.js:115-145` carrying
the quoted *"NO FS-as-published fallback … not reversible-on-error"* at `:129`/`:132`,
`beacon/route.js:36,41-55`, `next-scaffold.test.js:106,111`, `react-compiler-config.test.js:41`,
`layout.jsx:132` `manifest: "/academy-manifest.json"`, no serwist/workbox, `RegisterServiceWorker.jsx`
a kill-switch, `NEXT_PUBLIC_FEATURE_TRAINING_COACH` default-off (`featureFlags.js:5-8`), Cosmo on
`https://api.openai.com/v1/responses` with `MODEL = 'gpt-5.2'`
(`ucourses/ucourse-engine/assistant/agent.js:12-13`). `academy.graphqls` is 1 of **43** files in
`app/internal/web/backend/graphql/graph/schemas/`.

### `app`-side studio locators — **ENUMERATED**

`cms_queries.graphqls:106` `studioTask`, `:107` `studioTasks`; `archiveStudioTask` is a **mutation** at
`cms_mutations.graphqls:22` and occurs **nowhere** in `cms_queries.graphqls` — the iter-115 correction is
right. `studioManager.go:119` `s.runCommand(ctx, pyBin, append([]string{"studio/gen.py"}, tokens...))`;
`:1096-1098` is the `runCommand` contract comment *"NEVER through a shell"*.

### `customerio-sync` gating — **ENUMERATED**

`app/env_guards.go:37-44` `deployedEnvironment()`; `resolveSubsystemSwitch` at `:92-111`;
`main.go:394` `if customerIOSyncEnabled`; at the fold commit `3e5bc33ef:main.go:387`
`if deployedEnvironment() && os.Getenv("BREVO_KEY") != ""`. Compose sets `ENVIRONMENT=development`
(`:56`) and deliberately sets neither switch (`:84-92`). `internal/customeriosync/doc.go:15` corroborates
"on the asynq scheduler".

### EU-residency / EU-AI-Act block — **ENUMERATED**

`app/internal/jobsimulation/ai/ai.go`: `:85-88` `config.WithRegion("eu-west-1")` (Anthropic/Bedrock),
`:263-277` the `getClient` Azure case swapping `azureClientEu`→`azureClientUs` on the PostHog flag
`flag_use_azure_us`, `isThrottlingError` defined `:129` and called `:166`/`:325`.
`internal/coursebuilder/bedrock.go:109-112` `newUnderlyingClient` → `NewAnthropicClientWithModel`, `:100`
`return "anthropic-api"`. Validation: `v3/validator/skills.go:53` `calculateSkillScore` counting booleans,
`:75` the division; the dispatch is the hardcoded switch at `basevalidator/criterion.go:127`
(`case check.EngineLlm`), `NewLLMBulkChecker` at `:428`, and `templates/checkValidationBulk.tmpl:27` is
the prompt asking a model to *"assess whether the `<asset>` … meets or does not meet"*.
**`checkerEngines` really is write-only:** built at `v3/validator/validator.go:60-62` with a single
`check.EngineLlm` entry, passed at `:595`, stored at `criterion.go:90` — and there is **no** read of
`c.checkerEngines[...]` anywhere in `internal/` (positive control: the identifier itself has 6 hits).
The deterministic counterexample is real: `criterion.go:168` `validateCodeDiff`, `:450-475` setting
`success` from `cdiff.Diff == ""` with no model. Cross-anchors resolve: `external_services.md:579`
(*"There is **no ordered EU-first fallback chain**"*), `:567` (the Anthropic-Direct provider row),
`:602-607` (the four-live-plus-one-latent enumeration), `coursebuilder.md:48` (*"the shipped path is the
direct API"*), and `README.md:21` → `corpus/architecture/README.md:21`, whose bullet **does** close with
*"(e.g. cost tracking lives in `app`, not the `ai` library)"* — I opened it before concluding, and it
supports the claim rather than contradicting it.

### Org-switch identity claim — **ENUMERATED**

All six anchors byte-exact at `next-web-app 8297c684`, in **both** apps as the `{web,hiring}` brace
implies: `useOrgSelection.tsx:94` `await clerk.setActive({ organization: clerkOrgId });`,
`useResolveActiveOrg.tsx:107` `.setActive({ organization: candidate })`,
`useActivateMembershipOrg.tsx:81` `clerk.setActive({ organization: clerkOrgId }).catch(…)`.

### Sentinel internals — **ENUMERATED**

Casbin model `internal/authorization/casbin.go:14-44`: **6** request definitions (`r`, `r2`–`r6`), **6**
policy definitions (`p`, `p2`–`p6`), **3** role definitions (`g`, `g2`, `g3`), **6** matchers (`m`,
`m2`–`m6`) — the table's six rows map one-for-one, and `m`'s `TIER_FREE` really is substituted from the
proto enum (`:45` `authorizationv1.Tier_TIER_FREE.String()`). `go.mod:3` `go 1.26.0`; `Dockerfile:2` and
`Dockerfile.dev:2` `golang:1.26-bookworm`; `terraform/locals.tf:4-5` `service_cpu 256` /
`service_memory 128` (and `:3 port = 8080`). One `CREATE TABLE` in `init_policy.sql` —
`sentinel.casbin_rules`. `init_policy.sql:63-66` is the `taxonomy:write` note; `:88-118` is the
`content_creator` block; `local_superadmin_grants.sql` exists;
`internal/authorization/casbin_content_creator_test.go` exists. `manager` = **0** in `init_policy.sql`.
The four `MembershipRole` values are at `app/internal/data/ent/enum/membership.go:8-15`. The RPC table
lists **19** methods and `internal/rpcsrv/rpc.go` defines exactly those 19. The key-directory listing
matches the tree. `setup_guide.md` §6 (`:420-509`) does contain the schema-creation step (`:450-454`).

### Clerkenstein — **ENUMERATED in the pinned per-stack clone `09d06070`**

Gene counts derived by parsing the DNA JSONs, not read off the doc: `clerk-2.6.0` **27 genes / 14
capabilities**, `clerk-js-5` **9**, `clerk-multi-1` **9**, `clerk-deploy-1` **7**, `clerk-express-1`
**13 across 5 capabilities**. `alignment/cmd/` holds exactly `{clerkrun, jsfapirun, multirun, expressrun,
deployrun}`; `alignment/` has `dna/` (five files) + `golden{,-js,-multi,-express,-deploy}/` + `scripts/`,
while the sibling `rosetta-extensions/alignment/` section has **no** `scripts/` dir and `ALIGN_DIR`
defaults to `$base/../../alignment` (`gate.sh:30`, `drift-check.sh:16`); gate thresholds are 95/100
(`gate.sh:31-32`). `run.go:134-135` `ExitRegressed = 2` / `ExitUnmeasurable = 3`. `store.go:138`
`func (s *Store) SeedOrgIdentity`, `:151` `func (s *Store) LookupOrgEid`; `clerk-2.6.0.json:131` carries
the quoted *"M219 landed the fix … taking the Go surface 97.2% -> 100%"*. `clerk-js-5.json`'s `Me`
capability has variants `universal-user`/`unauthenticated` and **no** gene for
`/v1/me/organization_memberships` (grep across `alignment/dna/` = 0). Both membership routes exist:
`clerk-backend/server.go:47` and `clerk-frontend/server.go:186`. Single-seat model verified:
`registry.go:24,67,75` one `activeKey`; `server.go:102-108,125` one `signedIn`/`signedOut`/`clientID
"client_clerkenstein"`/`sessID`; `establishLocked` at `:663-670` mints `"sess_clerkenstein"`;
`handleClient:241`, `handleToken:467`, `handleMe:488`, `handleSignOut:554` all take `_ *http.Request`;
**`.Cookie(` appears in zero `.go` files in `clerkenstein/`** with `http.Request` at 55 as the positive
control; `handleSelectIdentity:627-657` sets `signedIn=false; sessID=""` **and** clears `signedOut` at
`:654`. All five named tests exist in `clerk-frontend/server_test.go` (`:256`, `:286`, `:390`, `:427`,
`:461`). `server.go:35-67` is the M220 block with `clerkJSFetchTimeout = 15 * time.Second` (`:59`),
`clerkJSClient` *"Explicitly NOT http.DefaultClient"* (`:65-68`) and the `FAKE_FAPI_CLERKJS_CACHE` dir.
Roster thread: `stack-seeding/seeders/roster.go:53,54,77` (`org_name`/`org_slug`/`org_is_hiring,omitempty`,
filled at `:140`/`:155` from `orgSlugFor` / `IsHiringOrg()`) ↔ `clerk-frontend/registry.go:125,126,144`
↔ `resources.go:37-43` + `orgNameDefault`/`orgSlugDefault` `"Clerkenstein Demo Org"`/`"clerkenstein-demo"`
at `:50-51` with the fallbacks at `:265`/`:269`. `clerkenstein/go.mod:8` `colony v0.34.3`; measured drift
is real — `sentinel/go.mod:8` is `colony v0.35.2` at `f2c46190` (taken there by `88036d7`, 2026-08-03),
`app/go.mod:15` likewise, and `app/go.mod:29` is `clerk-sdk-go/v2 v2.7.0`. `@clerk/express ^1.3.47`
appears in three KB files. `clerkenstein/.github/workflows/alignment.yml` is git-tracked (and therefore
inert, as claimed — it is not at the repository root). studio-desk's `STUDIO_ACCESS_ROLES` is byte-exact
at `src/index.ts:96` **and** `app/services/userService.ts:16`, both carrying the quoted comment.
`demo-stack/patches/academy-fs-published-fallback` exists.

### AI Labs / credits — **ENUMERATED**

`git describe --tags ad9f3c49` → `v1.369.0-7-gad9f3c498`, and `HEAD == origin/main`.
`internal/web/backend/credits/handler.go:12` is exactly *"POST /credits/purchase was removed in Wave 13"*.
`checkout.session.completed` = **0** in Go (control: `customer.created` hits `api.go:319`).
`internal/labs/` imports `internal/credits` **nowhere** (control: 3 files import other `internal/`
packages). The Stripe webhook switch (`api.go:317-…`) has exactly **two** `case` blocks —
`customer.created` and `customer.subscription.{created,updated,deleted}`. `cost.go:86-90` `creditCost`
= build **5** / refine **1** / translate **1** (the corpus is right; `cost.go:29`'s *own* header comment
saying "5 credits per refine turn" is a stale **platform-side** comment, contradicted by `:88`'s
`// D1 ruling: flat 1 credit per refine turn` — worth knowing when re-deriving, but it is not a corpus
defect). `purchasePackages` `:200-204` starter 50 / team 200 / scale 500; `PricePerCreditUSD = 0.45`
(`:142`); `MarginMarkup = 1.40` (`:133`); `DefaultSeedBalance int64 = 500` (`:231`), lazy-seeded at
`manager.go:106-122`. `LabsTier` essential/professional/frontier (`labs_budget.go:26-28`); the five
`CapReason`s at `:247-251`; `spend_reconciler.go:60` `Interval: 30 * time.Second`;
`labsapi/client.go:6` default `:7070` + `LABS_API_PLATFORM_TOKEN` (`main.go:855`);
`cmd/labsdemo/main.go:115` `envOr("LABSDEMO_ADDR", ":8099")`. All five migrations exist with the exact
filenames; all five Ent schema files exist; `lab_session.go:21-36` really does skip `PrimaryKeyMixin`
for a `field.String("id")`; `lab_body.go:12-13,48-50` is EN base + **6** overlays (de/es/fr/it/nl/pt).
`labs.graphqls` carries `labs`/`labBySlug`/`labSessions`/`mySessions`/`labSpendByDay` +
`createLabSession`/`cancelLabSession`. **"Credits has NO GraphQL" holds** — the only `credit` hits in the
schema dir are `organizationFeatureCredits`/`setOrganizationFeatureCredits`, which are Sentinel
org-feature budgets, not the ledger. `ReportEvent`'s allowlist is `status`/`grade`
(`labsession.go:383-402`); it is wrapped at `main.go:1338` in `authn.HTTPAuthnMiddleware`.
`stripe-go/v74 v74.30.0`; `TrialPeriodDays: 7` (`payments/handler.go:240`); `LookupKeys` `:193`;
`ParseEvent` `:273`; `HandleStripeSubscriptionEvent` `:324`; `emitMetaPurchase` `:447`;
`stripe/tests/stripe-test.sh:4` `--forward-to localhost:8080/api/webhook/stripe`; `internal/payments/`
contains **no** `_test.go`, matching "Payments has no Go unit test".

### Intra-corpus cross-references — **ENUMERATED** (every `<doc>.md:N` in my six files was opened)

Correct: `platform-migration-status.md:89` (the jobsimulation row), `frontend_architecture.md:11`,
`studio-desk.md:20`, `studio-desk.md:21`, `external_services.md:579`, `:567`, `:602-607`,
`coursebuilder.md:48`, `README.md:21` (→ `corpus/architecture/README.md:21`), and the unnumbered
`academy-backend.md` quote *"There is no separate 'academy subgraph'"* (at `:87`),
`cockpit-spec.md` § *"Limitation — one seat per stack"* (`:414`), `latency-budget.md` §"Time-to-usable"
(`:416`) and its `~4.05 s` figure (`:383`). Wrong: the three booked above (B2, B3, and the minor at
`sentinel.md:85`).

---

## What I could not settle

- **`ai-labs.md:16`** — *"`app` `knowledge/plan/releases/06.00-shared-purse/`, milestones M600–M607, all
  planned"*. HARD BAR 1 forbids reading, grepping or listing anything under `knowledge/plan/**`, and I
  read that literally rather than by intent, so I did not open the `app` clone's `knowledge/plan/` tree.
  Every *code-side* half of the surrounding claim was verified independently (no
  `checkout.session.completed`, no labs↔credits import, `/credits/purchase` removed) and all of it holds.
  Settled by: one `git ls-tree` of that path in `stack-demo/app` by a seat not under this bar.
- **`ai-labs.md:5`** — the existence and role of `anthropos-work/AI-Labs` as the live `labs-api` control
  plane. That repo is in no clone set. The `app`-side consumer half is fully verified
  (`internal/labs/labsapi/client.go`, `adapter/`, the `:7070` default, the `ReportEvent` callback).
  Settled by: cloning `AI-Labs`, or a `gh repo view`.
- **All `ARCHIVED <date>` assertions** (`service_taxonomy.md:166-168`, `:403`; `clerkenstein`/`cms`
  repo-state notes). `service_taxonomy.md:148-153` states the reason correctly — archive state lives in
  the GitHub org API, not in git objects. `gh` is not installed on this host and the repos are private, so
  I confirmed only the falsifier the corpus itself names: `jobsimulation` `origin/main` carries four
  commits dated 2026-08-04 (incl. merged PR #439), which an archived repo could not.
- **`security_compliance.md`'s non-code assertions** — DPA v1.4, 18 sub-processors, the sub-processor
  list, TLS 1.3 / AES-256 / KMS, CloudWatch 90-day retention, the 6-hour backup cadence and its three
  destinations, and the whole of `db-backup.md`. The `db-backup` repo is in no clone set and nothing else
  in the tree carries these facts. `db-backup.md` contains **no** citable `file:line` and I found nothing
  in it that contradicts `service_taxonomy.md:173`/`:521` or `security_compliance.md:13`/`:167`; its
  *"Deployment: Docker (scheduled, internal)"* vs the taxonomy's *"ECS scheduled task"* is compatible, not
  contradictory. Settled by: adding `db-backup` to the clone set.
- **`security_compliance.md:22-34`'s stated residual** — that `infrastructure`'s `base_service` module
  could place tasks other than where the callers' arguments imply. The doc discloses this itself. That
  repo has never been in a clone set.
- **`service_taxonomy.md:183` taxonomy figures** (≥42,790 skills / ≥22,470 job roles, "measured
  2026-06-29"). Pinned to a date and requires a DB read; out of reach here.
- **`service_taxonomy.md:110-118`'s "true at every ref between the fold and `838d907`"** — I verified the
  two endpoints (`3e5bc33ef:main.go:387` and `ad9f3c49:main.go:394`) but did not walk every intermediate
  `app` commit in that ~24-hour window.
