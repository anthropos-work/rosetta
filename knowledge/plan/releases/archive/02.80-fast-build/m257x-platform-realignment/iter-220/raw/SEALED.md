# iter-220 — sealed before landing

Measured at rosetta `e9e4a0d`-pending / rext `faa64e5`.

## V1 — direction B, and the scope lesson it taught on its first run

| pool the citation is checked against | dangling names |
|---|---|
| `stack-core/` only | **1** — `exposure_claim_guard.py` |
| the whole repo | **0** |

`exposure_claim_guard.py` is real and lives in **`stack-injection/`**. `stack-core/README.md` cites it
because the fence family spans sections. **The first probe's RED was the probe's, not the repo's** —
iters 209 and 214's class, committed again here, by the session that has now written it down four times.
It is kept as the fence's **scope control**: the same instrument returns **1** at the narrow scope and
**0** at the right one, which is the cheapest available proof that it can fire at all.

## V2 — the population

**32** `README.md` files in `rosetta-extensions`; **97** distinct `.py`/`.sh` filenames cited across
them; **0** dangling.

## V3 — the mirror direction, sized and NOT asserted

`stack-core/tests` holds **75** `test_*.py`; **12** are cited in `stack-core/README.md`, **63** are not.
This is `SURVEY-M257x-iter175`'s side of the registry and it is **not** landed here: iter-179 measured
that *"the `10 of 63` you get from all test modules is the wrong ratio"*, so the denominator is the open
question and asserting on it would pin a number nobody has justified. Printed, declared, routed.

## V4 — pre-registered stop condition

Direction B must be **0** on the whole-repo pool at land time, and the arm must fire on a staged
dangling citation. A non-zero that turns out to be a scope artefact is the probe's defect, not the
repo's, and is adjudicated by hand before anything is landed.
