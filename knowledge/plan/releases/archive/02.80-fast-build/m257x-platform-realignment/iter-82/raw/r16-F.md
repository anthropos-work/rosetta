# Seat F — reading #16

Ground-truth refs re-verified at open (all match the briefing table):
`app b948604ff` · `platform 0dab54d` · `next-web-app bb3313bc0` · `sentinel 88bc559` ·
`storage 4ce8ece` · `messenger fa47850` · `cms ca50c81` · `graphql-wundergraph 60c229f` ·
`roadrunner 87d8d44` · `jobsimulation 462343b0` · `studio-desk 14a5442` · `ant-academy 9c3843cd` ·
`rosetta-extensions (authoring) 4e6b64d`.
Corpus tree read at branch `m257x/platform-realignment`.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/services/ant-academy.md` | 437 | yes |
| `corpus/services/studio-desk.md` | 435 | yes |
| `corpus/services/chronos.md` | 245 | yes |
| `corpus/architecture/shared_libraries.md` | 242 | yes |
| `corpus/services/skillpath.md` | 110 | yes |
| `corpus/architecture/frontend_architecture.md` | 105 | yes |

Protocol read: `corpus/ops/platform-alignment.md` §5 in full (lines 240–1191, 1726 total).

Search-pipeline controls run in the same passes as every absence claim below:
`grep -rn usersv1connect app` → 4 (control for the `SkillPathSessionService` → 0 result);
`grep -rn JUDGE0_BASE_URL` at `app` `9d00a313` → 3 files (control for `ROADRUNNER_RPC_ADDR` → 0);
`grep -n JUDGE0_BASE_URL platform/docker-compose.yml` → 1 (control for `ROADRUNNER_RPC_ADDR` → 0 in platform);
`grep -n graphql-request packages/graphql/package.json` → 1 (control for the no-Connect/gRPC result).
No stderr was swallowed; every command ran with stderr merged (`2>&1`) and read.

---

## Blockers

### B1 — `frontend_architecture.md` undercounts the direct-REST surface by ~2×

- **Anchor:** `corpus/architecture/frontend_architecture.md:39` — *"**but there are direct REST/SSE calls**, ~15 sites hitting `NEXT_PUBLIC_BACKEND_API_URL` (`:8082`, `docker-compose.yml:246` runtime env, `:237` build arg): invitations (`invite/[token]/page.tsx`), assignment-builder (`useAssignmentBuilder.ts`), Stripe (`useStripe.tsx`), CSV bulk import, and the admin backfill tools."*
- **Why it is false:** I enumerated the predicate from source rather than re-deriving the doc's own number.
  At `next-web-app bb3313bc0`:
  `grep -rn NEXT_PUBLIC_BACKEND_API_URL --include=*.ts --include=*.tsx apps packages` → **32 occurrences across 23 files**; excluding `*.test.*` → **29 occurrences across 21 files**. Every reading of "site" (occurrence, file, non-test file) is ≥ 21, i.e. ≥ 40 % above the stated `~15` and on the occurrence reading more than double it.
  The set is also materially wider than the five named examples — it spans **three** apps and **three** packages, including surfaces the sentence does not hint at: `apps/web/.../ai-readiness/AIReadinessClient.tsx`, `.../ai-readiness/_components/EmailPreviewModal.tsx`, `.../reminders/[token]/unsubscribe/page.tsx`, `apps/hiring/src/hooks/{useAssignmentAsk,useAssignmentBuilder,useStripe}.ts`, `packages/core-js/src/{talkToData,workforce,coursebuilder,credits}/api.ts`, `packages/ui/src/{AISimulation/Onboarding/TestRuns/TestRunNetwork,EnterpriseAddMultipleEmployeeModal/EnterpriseAddMultipleEmployeeModal,Onboarding/OnboardingImportOptions/OnboardingImportResume}.tsx`, `packages/graphql/src/hooks/user/useManageSubscription.tsx`.
  The two `file:line` anchors in the same sentence are correct (`platform/docker-compose.yml:237` build arg, `:246` runtime env @ `0dab54d`) — this is rule 24's attractor exactly: the checkable parts check out and the SET does not.
- **Confidence:** high on the measurement; **medium** on the grade. I hesitated because the *conclusion* the number supports (*"GraphQL only" is the wrong mental model*) is TRUE and an undercount does not reverse it — under a doubt-downward rule this is an undercount and would be MINOR. I book it as a BLOCKER because a reader sizing the REST surface (e.g. to re-point a host, or to audit what bypasses the GraphQL auth path) acts on the number and the enumeration, and both are wrong by more than a rounding.

### B2 — `skillpath.md` cites `app/CLAUDE.md:72`; the line it means is `:80` (`:72` is a code-fence terminator)

- **Anchor:** `corpus/services/skillpath.md:34` — *"(`app/CLAUDE.md:72` and `app/knowledge/architecture.md:28` still list the service — Trap C, *the platform's planning docs lag its own code*. Grade against `main.go`.)"*
- **Why it is false:** The sentence names no ref, so it grades at the checkout `app b948604ff`. There, `app/CLAUDE.md:72` is the closing ` ``` ` of the `atlas migrate apply` bash block:
  ```
  70: # Apply DB migrations (versioned SQL lives in terraform/migrations/)
  71: atlas migrate apply --dir file://terraform/migrations
  72: ```
  73:
  74: ## Architecture
  ```
  The only `SkillPathSessionService` mention in that file is at **`:80`** (`grep -rn SkillPathSessionService --include=*.md app/` returns exactly two hits: `CLAUDE.md:80` and `knowledge/architecture.md:28`). The **sibling anchor in the same parenthesis is exactly right** — `knowledge/architecture.md:28` is the RPC table row — which is what makes the drifted one read as verified.
  The surrounding claim itself is TRUE and I re-derived it independently: `SkillPathSessionService` = **0** occurrences in `*.go` across the whole clone set (app · sentinel · storage · messenger · cms · jobsimulation · roadrunner), **0** `skillpathv1connect` imports, and `app/main.go` registers **six** handlers at `:1187 :1188 :1196 :1204 :1213 :1228` with no skillpath among them. Only the anchor is wrong.
- **Confidence:** high. I hesitated because 8 lines of drift into an *external repo's* markdown is low-impact; per the briefing ("wrong line anchors that point at the wrong construct") it is a BLOCKER, and the anchor points at a construct (a fence terminator) that carries no claim at all.

---

## Minors

- `corpus/services/ant-academy.md:144` — *"a separate, unauthenticated `code/public/catalog.json` (~2,667 entries)"*. Measured at `ant-academy 9c3843cd`: `json.load(...)['courses']` = **2,715** (7 languages). Stale approximation; the claim it supports (thousands of entries, unrelated to the grid) holds.
- `corpus/services/ant-academy.md:247` — *"~26 Playwright e2e spec files (tests/e2e/)"*. Measured: **31** `*.spec.js` under `code/tests/e2e/` (only one subdir, `widgets.spec.js-snapshots`, holds no specs). The paired claim "1000+ Vitest tests" is comfortably true (2,708 `it(`/`test(` call sites across 244 test files).
- `corpus/services/ant-academy.md:135` — writes the literal `emptyCatalogView() = { chapters: [], skillPaths: {}, series: [] }`. Source (`code/src/lib/serverTenant.js:115-117`) is `{ chapters: [], skillPaths: {}, series: [], bundles: PUBLIC_BUNDLES, catalogVersion: CATALOG_VERSION }` — two members elided from what is presented as an equality. The "→ 0 cards" consequence still holds: `BundleStripe` is *"just chrome"* and its cards arrive via `children` (`src/components/BundleStripe.jsx:10-13`), so an empty `skillPaths` yields no cards.
- `corpus/services/ant-academy.md:391` — the repo-local skills table omits **`export-path`** (present at `ant-academy/.claude/skills/export-path`), which is notable because `serverTenant.js:134-137` names `export-path` as the FS→DB publish step the draft layer exists around. (`_shared` is infrastructure, fairly omitted.)
- `corpus/architecture/frontend_architecture.md:44` and `:82` — *"read the supergraph schema from the **federated** GraphQL endpoint"* / *"regenerated from the upstream subgraph **schemas**"* (plural). At `next-web-app bb3313bc0`, `packages/graphql/codegen.ts` introspects `http://localhost:8082/graphql/query` and its own comment reads *"the sole subgraph since cms-in-app, so the WunderGraph router is no longer needed here."* Same file's `:32` already says *"the single `backend` subgraph"* — a mild self-contradiction in framing only; every endpoint value on the page is correct.
- `corpus/architecture/frontend_architecture.md:29-35` — the "Core Packages" table omits `packages/design` (`@anthropos/design`, a real workspace package). Arguably intentional given the "Core" label.
- **Outside my file set, reported because it is the same claim my file gets right:** root `CLAUDE.md` cites `corpus/architecture/external_services.md:546` **twice** for the "no ordered EU-first fallback ladder" retraction. `:546` is inside the `app/studio/**` in-image-path blockquote; the retraction is at **`:555`** — which is exactly what `shared_libraries.md:125` cites. One of the two is wrong and it is not mine.

---

## Positively cleared (audited zeros)

**ENUMERATED** (predicate re-derived from source independently, cardinality compared before any arithmetic):

- `shared_libraries.md:41,69,104,181` — every shared-library version pin, read from each repo's `go.mod` rather than from the doc's table. colony: app **v0.35.2**, messenger **v0.35.2**, sentinel **v0.34.3**, storage **v0.34.3**, cms/jobsim **v0.35.1**, roadrunner **v0.34.3** → the live split IS two-way. proto: app/messenger **v1.210.0**, sentinel **v1.200.0**, storage **v1.196.0**, cms **v1.207.0**, jobsim **v1.205.0**, roadrunner **v1.196.0**. ai **v1.40.2** at `app`, `cms/go.mod:9`, `jobsimulation/go.mod:11`. taxonomy **v1.2.0**: app + messenger direct, storage + sentinel `// indirect`, `cms/go.mod:13` + `jobsimulation/go.mod:15` direct-not-indirect. All 6 line anchors resolve.
- `shared_libraries.md:69` / `skillpath.md:45` — *"`d11a403` deleted all three `repos.yml` entries **and** their compose services in one commit"*. `git show d11a403 -- repos.yml` removes `- name: cms`, `- name: jobsimulation`, `- name: roadrunner`; `git show d11a403 -- docker-compose.yml` removes services `cms:`, `jobsimulation:`, `roadrunner:`. Same commit. (The commit *message* claims roadrunner's repos.yml entry "was already gone" — the diff says otherwise; the corpus is right and the platform author was not.) `repos.yml` = **9** entries @ `2adcf71`, **6** @ `0dab54d` — both pinned figures re-counted from the artifact.
- `shared_libraries.md:70` — the six registered Connect handlers in `app/main.go` @ `b948604`: `:1187` Users, `:1188` Organizations, `:1196` Skiller, `:1204` JobSimulation, `:1212-1214` `if cmsRPCServer != nil { CMSService }`, `:1228` LabSession. Enumerated the whole `mux.Handle` set — no seventh, no SkillPath, no RoadRunner.
- `shared_libraries.md:79` / `skillpath.md:30-33` — `SkillPathSessionService`: **0** in `*.go` across all 7 Go clones; `skillpathv1connect`: **0**. `ROADRUNNER_RPC_ADDR`: **0** in `app` at both the checkout and `9d00a313`, **0** in `platform` @ `0dab54d`. The correction *"this line long cited `docker-compose.yml:118`; that line sets `AWS_REGION`"* — confirmed, `:118` is `AWS_REGION=eu-west-1`.
- `ant-academy.md:257-268` — the complete `isPublic` matcher. I enumerated `code/proxy.js:112-188` independently before comparing: **25** static patterns + `DEV_LOGIN_ENABLED ? 2 : 0` + `VISUAL_BYPASS ? 3 : 0`. The doc's eight groups total exactly 25/2/3, member-for-member, with no extras and no omissions. The gate predicates match too (`VISUAL_BYPASS = BENCHMARK_VISUAL_BYPASS === "1" && NODE_ENV === "development"`, `proxy.js:62-64` — whitelisted, as documented).
- `ant-academy.md:194-197` — *"the **five** bring-up patches … wired at `:56`, `:66`, `:75`, `:84`, `:90`"*. `demo-stack/ant-academy.sh` @ rext `4e6b64d`: `:56` DEVORIGINS_PATCH, `:66` FSPUB_PATCH, `:75` FSBODY_PATCH, `:84` FSPUBLIC_PATCH, `:90` COCKPIT_PATCH. Five, exactly those lines; all five `stack-injection/apply-*.sh` appliers exist.
- `studio-desk.md:34` — the prod HTML entry set. Enumerated `vite.config.ts` `rollupOptions.input` (9 entries) against the doc's 9 names; `dev-accept` is spread in only when `!isProduction`. Exact match.
- `shared_libraries.md:196-213` — the taxonomy figures. Re-read the capture artifacts rather than the doc's arithmetic: `.agentspace/snapshots/taxonomy/*/manifest.json` gives `skills` **42,790**, `job_roles` **22,470**, `job_role_embeddings` **18,919**, `captured_at 2026-06-29`, `source primary-read`, `public_only true`, `predicate org-null`, filter `organization_id IS NULL`. Both digests agree (the second is the pre-M211 `skiller.*` re-key, byte-identical sha256s). The "18K came from `job_role_embeddings`" hypothesis is corroborated by the artifact.
- `shared_libraries.md:145` / `:162` — authn. `colony/authn` is imported in **129** `.go` files in `app` and **0** in sentinel/storage/messenger; the standalone `github.com/anthropos-work/authn` appears in **0** of the 7 `go.mod` files. Both halves of the correction hold.
- `frontend_architecture.md:19-35` — apps, ports and packages, enumerated from `package.json`/`pnpm-workspace.yaml`: web 3000, hiring 3001, integration 3002, mobile `expo start --port 3031`, maintenance no port; `!apps/mobile` present in the workspace excludes; `@anthropos/{i18n,tsconfig,ui,graphql,core-js}` names confirmed; 8 locale dirs `de en es fr it ja nl pt`; `next-intl ^4.11.0`; `next ^16.2.7` at `apps/web/package.json:46`; `pnpm@10.30.3`; `engines.node ">=24.0.0"`.
- `studio-desk.md:112` — the AI tier map, read from `src/services/ai/config.ts:20-38` and `:182`: OpenAI/Azure `thinking_slow`/`thinking_fast` = `gpt-5.2`, `fast` = `gpt-5-mini`, `instant` = `gpt-5-nano`; Anthropic `claude-opus-4-5-20251101` / `claude-sonnet-4-5-20250929` / `claude-haiku-4-5-20251001`; `.env.example:57` chain `azure-openai,openai`, `:61` `AI_DEFAULT_TIER=fast`, in-code fallback `'thinking_fast'`.
- `studio-desk.md:279-287` — the no-datastore claim. `grep` for every named driver in `package.json` → **0** (`pg`/`postgres`/`prisma`/`sqlite`/`mysql`/`mongo`/`knex`/`drizzle`/`typeorm`/`sequelize`); `app/services/studioDB.js` is present as the facade; `GET_/SET_USER_STUDIO_PREFERENCES` resolve in `app/services/userPreferencesService.js:1-2`.

**RE-DERIVED / anchor-verified** (weaker — I opened the cited construct and its surroundings, but did not independently enumerate a set):

- `skillpath.md:14,57` — `app/internal/skillpath/session.go:205-207` is the `// cms-in-app deseam: cms is in-process` comment + `u.cms.GetSkillPathDomain`; `app/internal/skillpaths/skillpaths.go:88-95` is the twin. Both exact.
- `skillpath.md:57` — `platform/docker-compose.yml:174` = `CMS_RPC_ADDR=http://backend:8083` @ `0dab54d` (and `:176` = `JOBSIMULATION_RPC_ADDR=http://backend:8083`). M809 has landed.
- `skillpath.md:75-77` — `InsightsSkillPathByMemberships` at `app/internal/organization/intelligence.go:1144`; the `SkillPathSession.Query()` predicate block is `:1159-1170` (`SkillPathID` + `StatusIn(Active, Completed)` + `HasUserWith` + the tenant `Or`). Line-exact on both ends.
- `skillpath.md:79-90` — the retraction. `app/terraform/migrations/20260729133514.sql:62` `DROP TABLE "local_jobsimulation_sessions"`, `:63` `DROP TABLE "local_skill_path_sessions"`; at `app 9d00a313` exactly three migrations post-date it (`20260731131307`, `20260731154527_academy_chapter_progress_completed_at`, `20260803143844_ai_readiness_recommendation_path`) — the pin makes that correct.
- `skillpath.md:95-102` — the unimplemented per-member drill-down. `InsightsBySkillPathStudentSimulationsContainer.tsx:31-34` returns `null as unknown as MembershipEnriched`; `:140-160` are the commented-out `<Table>` and totals block; `:137-139` renders `t('enterprise.insights.comingSoon')` = **"Coming soon"** (`configs/i18n/messages/en/enterprise.json`). No query touches a session row.
- `skillpath.md:66-70` — `EventSkillPathSessionUpdated` (`internal/skillpath/session.go:167`) + `EventChapterStepSessionCompleted` (`:178`); `SKILLPATH_STREAM` publisher `main.go:637`, subscriber `main.go:1274`; jobsim stream subscriber `main.go:1285`; in-process `jobsimulation.GetSessions` at `session.go:317`.
- `studio-desk.md:21,140` — `docker-compose.yml:223-225` `depends_on: backend` alone, `:226` `profiles: [studio-desk, all]`, `:204` build arg baking `:8082/graphql/query`, all @ `0dab54d`; `:337-341` @ `2adcf71` is the two-way `depends_on` incl. `cms`. The exit-1 claim holds structurally: `backend` carries `profiles: [core, backend, all]` (`:100`), so a `studio-desk`-only selection cannot resolve its dependency.
- `studio-desk.md:107` — `src/routes/youtube.ts:43` reads `process.env.YOUTUBE_API_KEY` (mock fallback at `:44-47`); `GCLOUD_SERVICE_ACCOUNT` at `.env.example:120` and `terraform/main.tf:129`, **0** reads anywhere in `src/`; `src/routes/skillpath.ts` is 61,080 bytes.
- `studio-desk.md:305,382-393` — `STUDIO_ACCESS_ROLES` at `src/index.ts:96` with all four literals; `preloadCriticalCSS()` at `app/core/main.ts:97`, `new PageWrapper()` at `:206`, the GlitchTip tailscale-funnel comment at `:105`; `clerk.load()` `:182`, `l12nService.init()` `:191`, `userService.canAccess()` `:199`.
- `studio-desk.md:322-328,346-370,401-415` — all five demopatch names, `build_frontend_studio_desk` (`demo-stack/up-injected.sh:881`), the chain note, the `frontend_lines()` existence-guarded `env_file` (`stack-injection/gen_injected_override.py:428-444`), both named regression tests (`stack-injection/tests/test_injection.py:1654`, `:1711`), and `stack-verify/e2e/run-studio-fcp.sh`.
- `ant-academy.md:63-75,412-420` — `code/app/api/academy/beacon/route.js` exists; `app/cmd/academy-seed/` exists; Ent schema files `academy_chapter_progress.go` / `academy_last_activity.go` (singular filenames, plural tables — as the doc says); `academyCatalogSeries` (`academy.graphqls:664`), `academyCatalogSkillPaths` (`:672`), `upsertChapterProgress` (`:694`), `setLastActivity` (`:700`); `demo-stack/patches/academy-fs-published-fallback` + `demo-stack/ant-academy.sh` wiring; `ensure-clones.sh` phase d2 clones into `stack-demo/ant-academy`.
- `ant-academy.md:67-68` — `serverTenant.js:115-145` genuinely spans `emptyCatalogView()` (`:115`) through `getServerCatalogView`'s `?? emptyCatalogView()` (`:145`), and the quoted *"the cutover is intentional, not reversible-on-error"* sits at `:132` inside it.
- `ant-academy.md:169-190,220-234` — `resolveServerChapterBody` (`src/lib/serverChapterBody.js:51-52`); `app/not-found.jsx:43` = "You wandered off the trail."; `src/i18n/LocaleSwitch.jsx:10-19` is a 2-way EN↔IT `<Link>`; `localeFromSearchParams` at `src/i18n/locale.js:42`; `translate.js:41-42` returns `defaultEn` on IT key miss; `proxy.js:241` sets `x-locale`; `tests/e2e/i18n-language-toggle.spec.js` exists.
- `ant-academy.md:241-249,290,325-346` — `code/package.json` `engines.node ">=22"`, `dev: next dev --port 3077`, `next ^16.2.6`, `react ^19.2.5`; `code/vercel.json` = `{"framework":"nextjs"}`; mobile `expo ~54.0.33` + port **8555**; FontAwesome vendored at `code/public/assets/fontawesome/{css,webfonts}`; manifest wired at `app/layout.jsx:132` with `display: standalone`; `code/.env` (not the repo root) is the app env file and is gitignored (`code/.gitignore:22`) — the upstream repo's own `CLAUDE.md:164` says *"Copy `.env.example` to `.env`"*, so this is right as written.
- `ant-academy.md:278-281` — no `/library/[slug]` route: `find code/app -path "*library*"` yields exactly `(public)/library/page.jsx` and `(public)/free/page.jsx`.
- `ant-academy.md:409` — Cosmo: `ucourses/ucourse-engine/assistant/agent.js:12` `https://api.openai.com/v1/responses`, `:13` `MODEL = 'gpt-5.2'`, `:32` `localStorage.getItem('openai_api_key')`.
- `chronos.md:9` — `app/internal/jobsimwiring/worker.go:24-35` spans the `hibiken/asynq` import through `StartWorkers` and the standard pool; platform `045857c` is *"chore: remove chronos service from orchestration…"* (removes 37 compose lines + 4 repos.yml lines); jobsimulation `09631fb2` is the matching *"remove Chronos references… Asynq integration for session timeout management"*; **0** `chronos` references remain in `platform/{docker-compose.yml,repos.yml,common.yml}` or in jobsimulation `*.go`.
- `frontend_architecture.md:11,39` — `docker-compose.yml:226` `profiles: [studio-desk, all]`; `repos.yml` @ `2adcf71` = 9 entries with no ant-academy; `5050:8080` at `2adcf71~1` confirms `:5050` was only ever a host mapping (the router listens on `8080` in `config.{dev,compose,prod}.yaml` and `terraform/locals.tf:8`); **0** `@connectrpc`/`grpc-web` deps anywhere in the monorepo; 363 commits in the last 30 days corroborates *"300+ commits / month"*.

---

## What I could not settle

- **Anything requiring the `colony`, `proto`, `ai`, `taxonomy` or `chronos` sources.** None is a clone in the ground-truth set, and all four libraries are pulled at Docker build via `GH_PAT` (which `shared_libraries.md:23-29` correctly documents). So these are unverified in both directions:
  - `shared_libraries.md:77-83` — *"**12** Connect-RPC services are defined"*. I can only RE-DERIVE the doc's own list (it does name 12); I cannot enumerate `proto/*/v1/*.proto` to test the SET. Per §5 rule 24 that is the weakest possible clearance, so I record it as unsettled, not cleared. **Settled by:** `git clone anthropos-work/proto && ls proto/*/v1/*.proto | xargs grep -l '^service '`.
  - `shared_libraries.md:48-59` (the colony package/behaviour table, the `rate.NewLimiter(1,5)` commented-out reject path, the Sentry `ENVIRONMENT=production || FORCE_SENTRY` gate), `:68` (buf CI `v1.57.0`), `:91-94` (`go/simulator/*` ~10 packages with no source `.proto` — though the *consumer* half is verified: `storage/internal/migration/migration.go:13` imports `go/simulator/storage/v1` as `legacyStorage`), `:107-135` (the `ai.AI` method set, the Mistral OCR-only `panic`s, the Anthropic `{"` prefill, the 10-attempt retry policy), `:179` + `:215-229` (taxonomy's 131 lines, `NodeID` regex, the three worked examples).
  - `shared_libraries.md:41` — the archived-chronos colony pin `v0.30.1`.
  - `chronos.md:14-230` in its entirety below the banner — the `timers` DDL, the four timer states, the RPC signatures, the `EventTimerWentOff` message, the triggerer's `LIMIT 10` / `FOR UPDATE SKIP LOCKED` / `scheduled_at < now() + interval '1 second'` window, ports 8080/8081, the env table and the two CLI flags. The banner itself is fully verified; everything under *"preserved for historical context"* is unreachable at these refs. **Settled by:** cloning `anthropos-work/chronos` at the pre-`045857c` state. The one sub-claim I would prioritise is the `feat/remove-chronos-and-realtime` **branch name** and **PR #395** at `chronos.md:7` — the commit sha is right, the PR/branch metadata is not in the clone.
- `ant-academy.md:74` — *"a grid of **65** real cards"*. This is a live-demo measurement; the FS content tree has 19 top-level and 78 second-level directories, and `catalog.json` carries 2,715 chapter×language rows, so 65 is not derivable from any static artifact I could reach. **Settled by:** a cold `/demo-up` + counting cards on the demo academy `/` with the `academy-fs-published-*` patches applied.
- `frontend_architecture.md:39,44` — *"the Cosmo Router at `:8080/graphql` in prod"*. The **port** is verified (`graphql-wundergraph` `config.prod.yaml:5` `listen_addr: 0.0.0.0:8080`, `terraform/locals.tf:8`), but whether that deployment is still *live in production* cannot be established from a repo that was deleted from local orchestration. **Settled by:** the infrastructure repo's `services.tf` / a prod `service_desired_count` read.
- `studio-desk.md:109` — the demo Directus token stamping via `directus/directus:11.6.1`'s `bootstrap/index.js:81`. The Directus image is third-party and not a clone here.
- `studio-desk.md:412`, `ant-academy.md:189-190,203,233` — the measured live numbers (studio FCP p95 4669→817 ms; academy 404→200 on `billion`; the M244 billion regression). Reproducible only on a live stack.
