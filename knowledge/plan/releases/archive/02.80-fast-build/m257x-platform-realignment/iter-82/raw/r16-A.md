# Seat A — reading #16

**Refs at time of reading** (re-derived at open, §5 rule 26):
corpus HEAD `1937e1f` (branch `m257x/platform-realignment`) ·
`app` `b948604f` (v1.366.0; `9d00a313`=v1.367.0 and `5ba17044`=v1.363.2 both reachable; origin/main `7177374`, 4 past v1.367.0) ·
`platform` `0dab54df` · `next-web-app` `bb3313bc` · `sentinel` `88bc5592` · `storage` `4ce8ece5` ·
`messenger` `fa47850d` (**3 behind origin/main `a0ec933`** — see *What I could not settle*) ·
`cms` `ca50c817` · `graphql-wundergraph` `60c229f3` · `roadrunner` `87d8d443` · `jobsimulation` `462343b0` ·
`studio-desk` `14a5442a` · `ant-academy` `9c3843cd` ·
`.agentspace/rosetta-extensions` `4e6b64d3` (authoring, `main`) ·
`stack-demo/rosetta-extensions` @ tag `fast-build-m257x-iter-58`

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/architecture/external_services.md` | 842 | yes |
| `corpus/architecture/security_compliance.md` | 281 | yes |
| `corpus/services/jobsimulation.md` | 273 | yes |
| `corpus/services/academy-backend.md` | 147 | yes |
| `corpus/services/messenger.md` | 146 | yes |
| `corpus/services/skiller.md` | 66 | yes |
| `corpus/services/TEMPLATE.md` | 46 | yes |

Protocol read: `corpus/ops/platform-alignment.md` §5 in full (lines 240–1191).

Search-pipeline controls run this pass (§5 rules 1–2): one zsh glob failure
(`--include=*.graphqls` unquoted → *"no matches found"*, exit 0, **0 results** — indistinguishable
from absence) and one zsh parameter-expansion failure (`$c:supergraph-config-prod.yaml` → *"bad
substitution"*, **count 0 on all four commits**) were both caught by reading stderr and re-run
quoted. Both would have produced confident false negatives. Every absence claim below is paired with
a named positive control.

---

## Blockers

### B1 — `external_services.md:208-209`: both rext anchors for the Directus re-point name the wrong construct

- **Anchor:** `corpus/architecture/external_services.md:208-209` —
  > "`rosetta-extensions/stack-injection/gen_injected_override.py:636-637` re-points every service in
  > `DIRECTUS_DATA_CONSUMERS`, which is **`("cms", "backend")`** (`:53`)"

- **Why it is false:** measured in **both** clones of `rosetta-extensions` (authoring copy
  `.agentspace/rosetta-extensions` @ `4e6b64d3` and per-stack copy `stack-demo/rosetta-extensions`
  @ tag `fast-build-m257x-iter-58`, which agree line-for-line — §5 rule 36, one witness):
  - `DIRECTUS_DATA_CONSUMERS = ("cms", "backend")` is at **`gen_injected_override.py:84`**, not `:53`.
    Line **53 is a blank line**; the nearest construct is `:52`
    `INJECTED = {"backend": "app", "cms": "cms", "jobsimulation": "jobsimulation"}` — a **different
    constant with a different meaning** (which services get an injected build), so a reader following
    the anchor lands on a plausible-looking but wrong tuple.
  - The re-point is at **`:669-670`** (`if with_directus and name in DIRECTUS_DATA_CONSUMERS:` →
    `env.append(f"DIRECTUS_BASE_ADDR={DIRECTUS_INNETWORK_ADDR}")`). Lines **`:636-637`** are a
    **CORS comment** — *"backend (app) CORS: the app's dev allowlist (app/internal/cors/cors.go)
    hardcodes the UN-offset frontend origins…"* — an unrelated mechanism.
  - The *substance* (the tuple's value, and that `backend` is the real reader) is **correct**; only
    the two anchors are wrong. `jobsimulation.md:132` states the same tuple without an anchor and is
    unaffected.
- **Confidence:** high

### B2 — `external_services.md:219`: `test_injection.py:1051` names a different test

- **Anchor:** `corpus/architecture/external_services.md:218-219` —
  > "replaced by `test_backend_the_actual_reader_is_repointed`
  > (`stack-injection/tests/test_injection.py:1051`), which asserts the opposite"

- **Why it is false:** `def test_backend_the_actual_reader_is_repointed(self):` is at
  **`test_injection.py:1108`** (both clones). Line **1051** sits inside
  `test_studio_desk_no_env_file_without_platform_dir` (declared `:1050`) and reads
  *"# The exposure_claim_guard seam: frontend_lines(n, offset) (no platform_dir) stays
  byte-identical…"* — an unrelated assertion about studio-desk env_file emission. The named test
  **does exist** and does assert the opposite of the retired one (`:1109` comment: *"M257x iter-24.
  This test replaces `test_only_cms_is_repointed_not_other_services`, which asserted…"*), so the
  claim's substance holds; the anchor resolves to the wrong construct.
- **Confidence:** high

### B3 — `academy-backend.md:15`: the self-citation `:74-76` points at the certificate-minting alphabet

- **Anchor:** `corpus/services/academy-backend.md:15` —
  > "The env-var *name* outlived the router; in production the router is still declared.
  > **(Consistent with :74-76 below.)**"

- **Why it is false:** `academy-backend.md:74-76` is the tail of the **Certificate minting** bullet —
  *"suffix from a 30-symbol ambiguity-free alphabet via `crypto/rand`. Public verify validates the id
  **before** any DB lookup…"* plus a blank line. It says nothing about the router,
  `NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT`, or the subgraph. The passage that *is* consistent with the
  claim is **`:80-83`** (*"…these types live in the `app`/`backend` federation subgraph — **the only
  subgraph left**. The frontend reaches them at (`NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT`)."*). The
  neighbouring file `ant-academy.md:74-76` is also unrelated (the demo presence-only academy story),
  so no alternative reading rescues it. This is §5 rule 34 (a corpus repair moved lines and
  invalidated an intra-corpus citation below the edit) — the anchor resolves, and reads as correct,
  because it landed on content.
  Both substantive halves of the sentence are TRUE: `2adcf71` deleted the router from local dev, and
  production still declares it (`graphql-wundergraph/terraform/main.tf:20` `service_desired_count = 1`).
- **Confidence:** high

### B4 — `academy-backend.md:20`: "currently `v1.363.2`" is a currency assertion the same file contradicts 35 lines later

- **Anchor:** `corpus/services/academy-backend.md:20` —
  > "the backend domain's labels are its own "v1.0/v1.05" line, distinct from `app` SemVer,
  > **currently `v1.363.2`** @ `5ba17044`."

- **Why it is false:** `5ba17044` **is** `chore(version): v1.363.2`, so the pinned half checks out —
  but the block asserts **currency**, and §5 rule 33 rules explicitly that *"a block that asserts
  currency (**current**, not stale, today) cannot be pinned into silence, because the pin and the
  word are making opposite claims and only one of them is what the reader acts on."* Measured:
  the ground-truth checkout is **v1.366.0** (`b948604f`), `9d00a313` is **v1.367.0**, and
  `origin/main` (`7177374`) is **4 commits past v1.367.0**. The contradiction is **internal to this
  file**: `:55` states *"the **storage-in-app v9.0 cutover** (`app` `9d00a313` = **v1.367.0**)"* —
  four minor versions ahead of the version `:20` calls "current". §5 rule 21 names self-contradiction
  as the cheapest and most-missed class; this is one, in a file that has both sides within 35 lines.
- **Hesitation, stated:** a charitable reading treats "currently … @ `5ba17044`" as "as of that ref",
  which would make it a MINOR. I booked BLOCKER because (a) rule 33 part 4 decides exactly this
  construction against the pin, and (b) the same document supplies the refuting figure, so a reader
  who reads both is left adjudicating rather than informed — the failure mode rule 19 says is worse
  than a uniformly-wrong corpus.
- **Confidence:** medium

---

## Minors

- `corpus/services/jobsimulation.md:216` — *"`backend` is the only **application** container the
  `core` profile starts (`sentinel` is authz-only)"*. `core` also starts **`gotenberg`**
  (`docker-compose.yml:268` `profiles: [core, backend, all]`); the parenthetical excuses `sentinel`
  and is silent on gotenberg. Root `CLAUDE.md` says *"`core` starts **five** containers — `backend`,
  `gotenberg` and the always-on floor"*. The load-bearing consequence (backend dying takes the whole
  platform's application surface) is unaffected.
- `corpus/architecture/external_services.md:675` — *"(`AI_PROVIDER_CHAIN`, default
  `azure-openai,openai`)"*. With the env var unset, `studio-desk/src/services/ai/config.ts:165-170`
  pushes azure, **then openai, then anthropic** — a three-rung default, not two. Note the code's own
  comment at `:166-167` says *"Azure first (if configured), then OpenAI"* and matches the doc, while
  the code below it does not; the same corpus sentence already names all three providers.
- `corpus/services/messenger.md:81` — *"messenger still constructs **four** Connect-RPC clients"*.
  There are **five** client constructions (`cmd/root.go:119`, `:124`, `:129`, `:134`, `:139`) over
  **four** distinct addresses (users and organizations both read `BACKEND_USERS_RPC_ADDR`). The
  "all four addresses" half — the one the passage exists for — is exactly right.
- `corpus/services/academy-backend.md:84-88` — the Queries list omits `academyPathTime`
  (`academy.graphqls:648`). Omitted list member; every other name listed verifies.

---

## Positively cleared (audited zeros)

**ENUMERATED** — predicate enumerated independently from source, cardinalities compared:

1. **`security_compliance.md:66-137` — the multi-tenancy fence, in full.** This is the passage §5
   rule 24 records as wrong in **four** consecutive generations, three of them by auditors who
   verified the displayed arithmetic instead of the set. I re-derived the **SET**:
   `ls *.go` = **139**; `grep -l 'ent.Schema'` = **135**; the 4 non-schemas are exactly
   `database_types.go`, `mixin.go`, `skiller_mixins.go`, `skillpath_mixins.go`.
   `OrganizationMixin{}` matches **30** files, of which `user_resource.go:22` is
   `// OrganizationMixin{},  // We need to work on this` → live set **29**.
   `OrganizationIDMixin{}` = **7**, and the named set (`category, jobrole, similarity, skill,
   specialization, studio_document, studio_task`) matches **exactly**.
   The document's own `comm`/`xargs` derivation returns **18**, and I then tested the **conjunction**
   rather than the count (rule 17): among those 18, `) Policy()` matches **only** `org_membership.go`
   and `UserMixin{}` matches **only** `academy_feedback.go` — so *"two of those 18 are policed by
   other means"* is complete, and the residual **16** matches the document's named 16 file-for-file.
   Only **four** files in the whole directory declare any `Policy()` (`mixin.go`, `org_membership.go`,
   `organization.go`, `user.go`). Anchors: `mixin.go:126` (OrganizationMixin Policy), `mixin.go:98`
   (UserMixin Policy doc-comment; func at `:99`), `org_membership.go:172` + `:179`
   `AllowCurrentOrgEdgesOrSkipRule()` + `:180`/`:185` `AlwaysDenyRule()`, `organization.go:56` +
   `:96` `FilterSameOrganizations()`, `user.go:116`. **29 + Membership + Organization = 31.**
   The fence is correct, and correct for the right reason this time.
2. **`external_services.md:364-377` — the subgraph-count table.** Re-derived the SET, not the sum, by
   reading `supergraph-config-prod.yaml` at each named commit: `749dc86~1` = **5**
   (backend/skiller/jobsimulation/cms/skillpath), `749dc86` = **4**, `7c17e63` = **3**,
   `915da06` = **1**. Members and dates match. `git show --name-status 915da06` marks **both**
   `schemas/cms.graphqls` and `schemas/jobsimulation.graphqls` `D`, and the commit subject really
   does read *"supergraph 2→1"* against a 3-entry tree — the corpus's *"do not take the count from
   the commit subject"* caution is verified from both sides.
3. **`external_services.md:378` — "no subscriptions", both independent checks.**
   `grep -rn "sse\|subscription" graphql-wundergraph/*.yaml` → exit 1 / no output, with the named
   positive control (`grep -rln backend …/*.yaml`) matching all three files.
   `git show 915da06~1:supergraph-config-prod.yaml` still reads `protocol: "sse_post"` (`:13`).
   `git log -S 'protocol: "ws"'` → **0**; control `-S sse_post` → **exactly 5** commits.
   `bba862f` merge-base rc **1**, `branch -a --contains` names `remotes/origin/feat/use-web-socket`
   and nothing else. `schemas/backend.graphqls`: `type Mutation` `:4053`, `type Query` `:4912`, **no**
   `type Subscription`; **6** `Subscription` substring hits, all Stripe/plan
   (`activeSubscription`, `type PlanSubscription`, `stripeSubscriptionId` ×3, `stripeSubscriptionItemId`).
4. **`external_services.md:640-645` — the env-var occurrence counts, re-derived at the pinned ref.**
   `git grep -how` at `5ba17044` over `*.go`: `OPENAI_KEY` **5**, `ANTHROPIC_API_KEY` **26**,
   `AZURE_OPENAI_KEY` **13**, `AZURE_OPENAI_ENDPOINT_URL` **13**; `OPENAI_ORG_ID` /
   `AZURE_OPENAI_ENDPOINT` / `AZURE_OPENAI_DEPLOYMENT` all **0**; `OPENAI_API_KEY` **2**. All eight
   exact.
5. **`external_services.md:543,591,612` — "all 30 `*_AI_*_MODEL` lines pin `azure`".** Enumerated
   across all three `app/studio/configs/*.ini`: **30** matching lines, and
   `grep -v "= azure,"` over them returns **empty**. `gen.py:41-53` overrides only
   `AZURE_API_KEY`/`AZURE_ENDPOINT`/`OPENAI_API_KEY`/`OPENAI_ENDPOINT`/`ANTHROPIC_API_KEY`/
   `ANTHROPIC_ENDPOINT` — **no service selector**, so the latent-arm claim holds exactly as stated.
6. **`jobsimulation.md:32-38` — "the 23 session/run tables".** `grep -c "CREATE TABLE"` over
   `20260722081626_jobsim_data_model.sql` = **23**, and the named members (`sessions`, `actors`,
   `interactions`, `validation_*`, `anticheat_*`, `recordings`, `chime_recordings`,
   `code_submissions`) are all in the enumerated list. `20260722104506.sql:2`
   `CREATE TABLE "job_simulation_sessions"` and `:79` `DROP TABLE "sessions"` both exact.
7. **`jobsimulation.md:43-44` / `external_services.md:170-173,296` — "eight services, ten effective".**
   Enumerated top-level `services:` keys in `docker-compose.yml` = **8** (`sentinel:5`, `backend:28`,
   `storage:102`, `customerio-sync:136`, `messenger:156`, `studio-desk:197`, `next-web-app:228`,
   `gotenberg:255`); `include: common.yml` adds `postgresql` + `redis` = **10**. `cms`,
   `jobsimulation`, `roadrunner`, `graphql`, `directus` are **not** among them. `repos.yml` = **6**
   entries (app, sentinel, storage, messenger, next-web-app, studio-desk), exactly as
   `jobsimulation.md:12` states.
8. **`jobsimulation.md:118` — "the file contains 0 `INSERT`s".** `grep -c "INSERT INTO"` on
   `20260729133514.sql` = **0**; the single case-insensitive "insert" hit is the identifier
   `on_insert_local_jobsimulation_sessions_update_memberships` at `:64`. The predicate (no
   back-fill) holds, not just the count.
9. **`external_services.md:695` — "there is no `anthropos-agent-eu`".** 0 hits anywhere in
   `stack-demo/**` platform source; the only hits in the tree are rext **test fixtures** that
   deliberately preserve the old corpus text (`stack-core/tests/fixtures/…`). Positive control
   (`anthropos-agent-us`) matches both livekit.go copies.
10. **`security_compliance.md:234-245` — the EU AI Act rubric derivation.** `checkerEngines`
    registers **exactly one** engine (`validator.go:60-62`, `check.EngineLlm`) and is **never
    indexed** — `grep "checkerEngines\["` returns 0 with a working positive control
    (`checksMap[` → `criterion.go:124`, `:252`). So *"stored and never read; cite the DISPATCH"* is
    correct as a predicate, not just as an anchor.
11. **`external_services.md:661-665` — the taxonomy manifest.** Digest directory
    `5afc0bccf1df7ef538b643321fc6362f` exists; `public_only: true`, `predicate: org-null`,
    captured `2026-06-29T14:43:00Z`; row counts `skills 42790`, `job_roles 22470`,
    `skill_embeddings 42790`, `job_role_embeddings 18919` — all four exact, and the
    18,919-transcribed-onto-role-count diagnosis is supported by the manifest itself.
    Same figures cross-check `skiller.md:40-49`.
12. **`external_services.md:142-148` — the Directus-compose history retraction-of-the-retraction.**
    `a2a3ee6` = 2026-02-27; `a2a3ee6^:docker-compose.yml` really does carry
    `:384 image: directus/directus:10.10.1`, `:386 8055:8055`, `:409 ADMIN_PASSWORD=password`;
    and `git log -S 'admin@example.com' --all` returns **nothing** (control: `-S ADMIN_PASSWORD`
    returns 3 commits) — so *"only the email is unfound in history"* is exactly right.
13. **`skiller.md:54-55` — "8 `ContentLanguage`s".** `assignments.graphqls:44-53` enumerates exactly
    8 (english, italian, spanish, french, german, dutch, japanese, portuguese).

**RE-DERIVED / anchor-verified** (opened the file and read ±15 lines around each — §5 rule 10).
Every one of the following resolved to the construct the corpus names:

- **`messenger.md`** — banner pinned `@ messenger a0ec933`: `terraform/main.tf:29`
  `service_desired_count = 0` ✓ and `:27-28` = *"The image and task definition stay declared: this is
  the rollback path…"* ✓ (both **only** at that ref — see below). `app` @ `9d00a313`: `main.go:15`
  `msgflow`, `:61` `msgadapters`, `:62` `msgsender` ✓; `:1330-1340` the NO-GAP comment ✓;
  `:1362-1365` `cmp.Or(os.Getenv("SERVICE_NAME"), "messenger")` ✓; `:1387` `msgflow.ConsumerGroup` ✓;
  `:1423` `msgadapters.Wire(` with `msgsender.NewFromEnv` at `:1425` ✓; `:1095` `CMS_STREAM`
  publisher ✓; `:1478-1484` the `streamNames{…}` binding ✓. `app` @ `b948604`: `main.go:1205-1211`
  the stale *"until the M809 re-point"* comment ✓ (the corpus flags it as stale in `app` and says to
  grade the address against compose — correct). `repos.yml:21-23` ✓; `docker-compose.yml:156`,
  `:171-172`, `:173`, `:174`, `:176`, `:183`, `:186-192`, `:195` ✓ all exact, and `messenger` is
  absent from `all`. `rpcsrv.go:25-30` (both Unimplemented stubs) ✓; `cmd/root.go:63` `PORT`→8080,
  `:64` `RPC_PORT`→8081, `:107` `REDIS_STREAMS_INDEX`→"2", `:147` `READONLY_DB_CONNECTION` ✓;
  `flow/flow.go:72-87` (`AddSubscriber("backend", …)` + the five `OrgSkillPath*` handlers) ✓;
  `flow/jobsimulations.go:140-151` (the 2h/12h staleness guards) ✓;
  `flow/assignments.go:828` `h.cms.GetSkillPath` inside `getSkillPath` ✓; Go 1.25.0,
  `getbrevo/brevo-go v1.1.3`, `osteele/liquid v1.8.1` ✓. `REDIS_WORKER_INDEX` is **0 hits** in the
  messenger repo (control: `REDIS_STREAMS_INDEX` → 3 hits) — the *"set but not read"* row is right.
- **`jobsimulation.md`** — `jobsimulation/terraform/main.tf:40` `= 0` ✓; `cmd/root.go:77`/`:78`
  8080/8081 defaults ✓; `Dockerfile`/`Dockerfile.dev` `ENTRYPOINT ["./application"]`, **no CMD**,
  `EXPOSE 8080` ✓; subcommands exactly `aggregate` / `clone-session` / `test-command` / `validate`
  (`cmd/*.go` `Use:` strings) ✓; `asynq v0.26.0` ✓ and commit `09631fb2` subject matches verbatim ✓;
  migrations `20260402145459` + `20260409131539_add_summary_to_extraction_results` ✓;
  `internal/simulator/manager/interview_report_csv.go` ✓; worker pools `Concurrency: 10` (`:27`) and
  `25` (`:104`) ✓; `internal/runner/runner.go` header *"formerly the standalone 'roadrunner'
  service"* ✓; `internal/graph/queries.resolvers.go:70` `JobSimulationResult` resolver, which
  delegates to `simManager.Get…BySession` — a persisted read, no recompute ✓;
  `internal/calls/livekit.go:131-135` the flag read and `:140-144` the `anthropos-agent` /
  `openai-hosted` override ✓; `ELEVENLABS_{,EU_}TEMPLATE_AGENT_ID` ✓;
  `CHIME_RECORDINGS_BUCKET_NAME=ant-prod-chime-demo` (`docker-compose.yml:50`) ✓;
  platform `05b4035` subject ✓; `cms/internal/directus/collections/jobsimulation.go:1594-1597`
  (`voiceEngineFromDirectus`, nil → `SimulationVoiceEngineGptrealtime`) ✓ — the corpus writes this
  path `cms/directus/collections/…`, which is the doc's own `internal/`-relative house style,
  consistent with `calls/livekit.go` in the same file, so **not** booked;
  `app/terraform/migrations/20260729133514.sql` `:58-62` (*"5. Drop the mirrors."*), `:15-23`,
  `:36-44`, `:62-63` ✓; `app/internal/organization/intelligence.go:1700`
  `m.ent.JobSimulationSession.Query()` ✓ exact; `app/main.go:1204` the in-app
  `JobSimulationServiceHandler` ✓ and `:980-982` the `DIRECTUS_BASE_ADDR` `log.Fatalf` ✓ (both
  @ `b948604`); rext `DIRECTUS_DATA_CONSUMERS = ("cms", "backend")` ✓.
- **`security_compliance.md`** — `jobsimulation/ai/ai.go:85-88`/`:85-95` (Bedrock `eu-west-1`),
  `:262-266` / `:263-277` (Azure EU default, `flag_use_azure_us` swap), `isThrottlingError` def
  `:129` and call sites `:166`, `:325` ✓; `coursebuilder/bedrock.go:109-112` + `:100`
  `"anthropic-api"` ✓; `skills.go:53-64` + `:75`, `criterion.go:127`, `:168`, `:428`, `:450-475`,
  and `templates/checkValidationBulk.tmpl` ✓; `job_simulation_session.go:5` *"L2: NO Ent privacy
  Policy"* ✓; `jobrole.go:18` / `category.go:15` ✓;
  `next-web-app/apps/{web,hiring}/src/hooks/useOrgSelection.tsx:94`, `useResolveActiveOrg.tsx:107`,
  `useActivateMembershipOrg.tsx:81` ✓ all six files, all exact.
  Its three cross-references into `external_services.md` all resolve correctly: `:555` = *"There is
  **no ordered EU-first fallback chain**"*, `:577-582` = the four-live-plus-one-latent enumeration,
  `:543` = the Anthropic-Direct provider row.
- **`external_services.md`** — `graphql-wundergraph/terraform/locals.tf:8` `port = 8080`,
  `terraform/main.tf:48-49` containerPort/hostPort, `config.prod.yaml:5` `listen_addr 0.0.0.0:8080`,
  `router:0.275.0`, `wgc@0.104.0` ✓; `Dockerfile.dev:18` COPY / `:19-20` comment / `:23` awk ✓;
  `graphql-wundergraph/CLAUDE.md:39` really does assert the opposite (*"builds from the production
  Dockerfile"*) ✓; `b56d731:docker-compose.yml` has `  graphql:` at `:22` and `360efd4` has none,
  with `  backend:` at `:28` as the stated positive control ✓;
  `docker-compose.yml:53` (`DIRECTUS_PUBLIC_BASE_ADDR`, and it is the **only** `DIRECTUS_*` in the
  file — so *"sets exactly ONE of the pair"* is exact), `:100`, `:268`, `:92-99` (redis/postgresql/
  sentinel; storage absent), `:204`/`:220` (studio-desk) and `:236`/`:245` (next-web-app), all four
  `:8082/graphql/query` ✓; `NEXT_PUBLIC_GRAPHQL_ENDPOINT` = **0 hits** in next-web-app (control:
  `NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT` matches turbo.json / terraform / Dockerfile.dev) ✓;
  `app/internal/web/backend/backend.go:130` `/api/webhook/clerk` ✓;
  `report_agent.go:31`, `askengine/bedrock.go:25`, `coursebuilder/bedrock.go:23,29`,
  `markdownManager.go:19`, `studioManager.go:583` ✓; `internal/cms/directus/collections/
  jobsimulation.go:905` (`AIVendor *AIVendor`) and `:1302-1305` (`aiVendor := simulation.Openai`) ✓;
  `simulator/ai/ai.go:58-59` and the `default:` arm `:114-115` ✓; `jobsimulation/ai/ai.go:30-33`
  (four vendor consts) and `:80` (`openai.NewOpenAI`) ✓; `app/studio/services/ai.py:383`,
  `:627` (`class AnthropicProvider`), `:704-724` (`get_client`, providers `{openai, azure,
  anthropic}`), `:706-708` ✓; `app/main.go:770` the coursebuilder-backend boot log ✓;
  `livekit.go:110`, `:115`, `:120`, `:126` ✓; the `:832` → `:522` self-citation ✓ (correct).
- **`academy-backend.md`** — `app` @ `9d00a313`: `main.go:471-472` the two in-process storage
  managers ✓, and `STORAGE_RPC_ADDR` in Go source = **exactly 3, all comments, zero reads**
  (`jobsimwiring/wiring.go:101`, `storagens/callsites_test.go:189`, `main.go:451`) — enumerated ✓;
  `cmd/academyImport/main.go:239-243` the *"needs the BUCKET, not an RPC address"* comment and
  `:244-246` the hard error ✓. Every `academy_*` table name verified plural against
  `terraform/migrations/` (`academy_chapter_progresses`, `academy_last_activities`,
  `academy_chapter_times`, `academy_certificates`, `academy_bookmarks`, `academy_feedbacks`,
  `academy_series`, `academy_skill_paths`, `academy_chapters`, `academy_chapter_bodies`,
  `academy_path_embeddings`) ✓; all six named test files exist ✓; all five cmd binaries exist ✓;
  every listed GraphQL query/mutation exists in `academy.graphqls` ✓; `certificate.go:31-32`
  (`AAS`, a **30**-symbol ambiguity-free alphabet `ABCDEFGHJKMNPQRSTVWXYZ23456789`) and `:171-184`
  (`crypto/rand`) ✓; `/content/catalog.json` (`content.go:23`), `/content/admin/*` +
  `ACADEMY_CONTENT_API_TOKEN` (`backend.go:286-287`) ✓; Asynq
  `academy_embedding_refresh` @ 03:00 (`worker/tasks/academy_embedding_refresh.go:10`) ✓;
  `internal/aireadiness/suggested_path.go` ✓; PR **#903** = `0e37771f`, **2026-06-05**, subject
  *"Academy backend v1.0 "ground truth" — server-owned academy domain (9 milestones, additive)"* ✓.
- **`skiller.md`** — `app/internal/rpc/skillerrpc/` ✓; all six named Ent schema files ✓;
  `internal/web/backend/graphql/graph/schemas/skiller_taxonomy.graphqls` ✓, whose own header
  confirms `categoryTree` / `fullCategoryTree` **stay unported** ✓; `SKILLER_RPC_ADDR=http://backend:8083`
  (`docker-compose.yml:183`) ✓; no `skiller` entry in `repos.yml` ✓.
- **`TEMPLATE.md`** — read in full; it is a fill-in-the-blanks skeleton with no factual assertions
  about the platform (every value is a `[placeholder]`). **Nothing to falsify; no findings.** Its one
  concrete token, `docker compose up -d [service_name]`, is a template, not a runnable dead command,
  so it does not trip §5 rule 30.

---

## What I could not settle

1. **`messenger.md`'s banner is graded at a ref the supplied ground-truth clone does not contain.**
   The banner pins `messenger a0ec933`; the clone at `stack-demo/messenger` is `fa47850d` (v0.42.0)
   and is **3 commits behind** `origin/main`, which **is** `a0ec933` (v0.42.1). At the checkout,
   `terraform/main.tf` has `service_desired_count = 1` at **line 19** — i.e. an auditor grading that
   claim against the provided clone alone would book a false blocker. At `a0ec933` the claim is exact
   (`:29` `= 0`; `:27-28` the rollback-path comment), so per §5 rule 33 it is **TRUE**. Flagging it
   because it is a §5 rule 26 hazard in the *apparatus*: the ground-truth table's messenger sha is
   stale relative to the ref the corpus cites, and nothing in the briefing says so. Settled by
   `git -C stack-demo/messenger log -1 origin/main`.
2. **`external_services.md:3` — "the repo is ARCHIVED on GitHub (2026-07-30)"** and
   **`jobsimulation.md:20,77` — "The GitHub repo was archived 2026-07-31"**. Archival status is a
   GitHub API property, not a property of the clone; I had no network. Settled by
   `gh repo view anthropos-work/{graphql-wundergraph,jobsimulation} --json isArchived,archivedAt`.
3. **`security_compliance.md:261` — "DPA v1.4 with 18 approved sub-processors"**, and the whole
   Backup/DR, VPC-CIDR, TLS-1.3 and monitoring-tool tables. These describe infrastructure and legal
   artifacts with no representation in any of the 13 ground-truth clones. Settled by the
   `infrastructure` terraform repo (not provided) and the DPA document itself.
4. **`external_services.md:590-593` cross-file consistency with `coursebuilder.md:48` and
   `ai_architecture.md`** — both are cited as agreeing with this file but are outside my partition; I
   did not open them (§5 rule 18(b) — I am deliberately not adjudicating another seat's files).

---

### Note on distribution (§5 rule 18)

All four blockers are **anchor/currency** defects — the two classes §5 rule 21 measures as
mechanically detectable (an anchor that resolves but names the wrong construct; a self-contradiction
between two passages of one file). **Zero** substantive claims in the seven assigned files were
refuted against platform source: every count I enumerated independently (135/30/29/7/18/16/31,
23 tables, 8/10 compose services, 5→4→3→1 subgraphs, 30 ini lines, 5/26/13/13 env occurrences,
0 INSERTs, 3 STORAGE_RPC_ADDR comments, 6 ContentLanguages… 8) matched, and every one of ~120
`file:line` anchors into platform repos resolved to the named construct. The three defective anchors
point into **`rosetta-extensions`** (2 of 4) and **into the corpus itself** (1 of 4) — i.e. into the
two trees the platform-source anchor checks do not cover.
