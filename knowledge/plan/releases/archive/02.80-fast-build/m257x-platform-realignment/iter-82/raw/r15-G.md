# Seat G — reading #15 (adversarial diff seat)

## Positive controls

**The diff.** `git diff f375738..HEAD -- corpus/ CLAUDE.md .claude/`

| measure | value |
|---|---|
| `--stat` totals | **33 files changed, 410 insertions(+), 251 deletions(-)** — matches the briefing exactly |
| raw diff line count (`wc -l`) | **1799** |
| commits in range | one — `328ece5 iter(M257x/81): repair the 11 predicates — 33 files, 7 parallel seats, all 5 corpus guards GREEN` (Wed Aug 5 13:10:22 2026 +0200) |

Read top-to-bottom in full (4 Read calls over the saved diff, offsets 1/450/900/1400).

**Whole files opened (`wc -l`):**

| file | lines |
|---|---|
| `corpus/architecture/external_services.md` | 842 |
| `corpus/architecture/service_taxonomy.md` | 459 |
| `corpus/architecture/dependency_map.md` | 103 |
| `corpus/architecture/architecture_overview.md` | 381 |
| `corpus/services/ai-readiness.md` | 651 |
| `corpus/services/roadrunner.md` | 199 |
| `corpus/services/storage.md` | 201 |
| `corpus/services/jobsimulation.md` | 273 |
| `stack-demo/platform/docker-compose.yml` | 271 (dumped in full) |
| `stack-demo/platform/common.yml` | 36 (dumped in full) |
| `stack-demo/platform/repos.yml` | 31 (dumped in full) |
| `stack-demo/platform/Makefile` | 185 |
| `stack-demo/app/main.go` @ `9d00a313` | 1569 |
| `stack-demo/app/internal/askengine/registry.go` | 686 |
| `.agentspace/rosetta-extensions/clerkenstein/alignment/scripts/gate.sh` | 87 |
| `.agentspace/rosetta-extensions/demo-stack/ant-academy.sh` | 717 |

**Search-pipeline positive controls** (run in the same passes as the absence claims):
`grep -c PORT stack-demo/platform/docker-compose.yml` → **10**; `grep -c CLERK stack-demo/platform/.env_example` → **10**. Both non-zero, so the zero results for `STORAGE_RPC_ADDR` (0 across compose/common/.env_example) and `ROADRUNNER_RPC_ADDR` (0) are real absences, not engine failures. All greps run with stderr visible; no engine rejections.

**Live compose oracle** (`docker compose --profile X config --services`, run against `stack-demo/platform`):
`core` → redis, postgresql, sentinel, backend, gotenberg (**5**) · `graphql` → postgresql, redis, sentinel, **exit 0** · `frontend` → *"service "next-web-app" depends on undefined service "backend": invalid compose project"* · `studio-desk` → same for studio-desk · `messenger` → same · `storage-legacy` → floor + storage · `all` → 8 services incl. customerio-sync.

---

## Blockers

### B1 — The `backend` Redis stream's new "and nothing else" over-corrects: **messenger subscribes to it**

- **Anchor:** `corpus/architecture/dependency_map.md:58` —
  > `| `backend` | App | App (cms **domain** in `app`) — **and nothing else**: the `cms` husk container that used to subscribe alongside it was deleted from compose by `d11a403`, so there is no second subscriber on this stream locally | User/org updates |`
- **Why it is false:** `messenger/internal/flow/flow.go:72` is `h.subServer.AddSubscriber("backend", sub.AddHandler(…))` — the `OrgSkillPath*`, `OrgJobSimulation*`, `OrgContent*`, `OrganizationMemberInvited`, `AIReadiness*` and Course-Builder handlers all hang off the **`backend`** stream (verified `stack-demo/messenger` @ `fa47850`, flow.go:70-104). Messenger subscribes to three streams — `backend` (`:72`), `jobsimulation` (`:105`), `cms` (`:109`) — and it is still a real compose service (`docker-compose.yml:156`, `profiles: [messenger]` `:195`; `docker compose --profile messenger config` resolves it). So the `backend` stream demonstrably has a second subscriber whenever messenger is up.
  The repair correctly removed the dead `cms` husk from this cell and then over-claimed the residue. It also **self-contradicts two things written in the same commit**: (a) `corpus/services/messenger.md:77` (this diff) — *"Skill-path notifications arrive as Redis Streams events on the `backend` subscriber (`OrgSkillPath*` handlers in `internal/flow/flow.go:72-87`)"*; and (b) the very next rows of the same table, where the `jobsimulation` row does list *"Messenger (if running)"*. The fix is the same parenthetical: `Messenger (if running)`.
- **Confidence:** high

### B2 — New provenance claim for `roadrunner/terraform/main.tf:19` names the wrong commit

- **Anchor:** `corpus/architecture/platform-migration-status.md:74` —
  > `` `roadrunner/terraform/main.tf:19` remains `service_desired_count = 1` — last changed at `e45eb61` (2026-05-27) and untouched by everything up to the repo's HEAD `87d8d44` (2026-06-19) ``
- **Why it is false:** `git -C stack-demo/roadrunner blame -L 19,19 terraform/main.tf` attributes line 19 to **`84a4b4f1` (Mattia Sasso, 2025-12-15)**, not `e45eb61`. `git show e45eb61 -- terraform/main.tf` shows that commit changing **exactly one line** — the module `source` URL at line 11 (`infrastructure-modules.git//services/base_internal_service` → `infrastructure.git//modules/services/base_internal_service`) — under its own message *"Module contents are identical; this is a pure source-URL swap."* It never touched `service_desired_count`. The three commits that have ever touched `terraform/main.tf` are `84a4b4f` (2025-12-15, file creation), `ae622e6` (2025-12-15, project rename) and `e45eb61` (2026-05-27, source swap).
  This is a repair-introduced regression in the precise sense the seat exists for: the prior text (*"has not been touched since `87d8d44`"*, still standing at `corpus/services/roadrunner.md:13-14`) was vague but not wrong; the repair replaced it with a specific, checkable, and incorrect attribution. The clause's own point — that the `= 1` is stale and pre-fold — survives; only the named commit is wrong (and `84a4b4f` makes the point *more* strongly: the line predates the fold by seven months, not two).
- **Confidence:** high

### B3 — "The rext source's own prose … still says *four*" is refuted at the rext checkout — both cited lines say **five**

- **Anchor:** `corpus/services/ai-readiness.md:219-220` —
  > `(This bullet said *"four"* until M257x; … The rext source's own prose at `:137` and `:139` still says *"four"* — stale on that side too, and out of this corpus's edit scope.)`
- **Why it is false:** the passage names **no ref** for the rext file, so it grades against the checkout — `.agentspace/rosetta-extensions` on `main`, HEAD `4e6b64d`, working tree clean. At that ref:
  - `stack-seeding/seeders/ai_readiness_interview_report.go:137` = `// --- the five "How they use AI — at a glance" tiles ---`
  - `:139` = `// aiReadinessUsageKPIs derives the five usage-dimension KPIs (0-100 cohort averages) from the org's`

  Both say **five**. The rext HEAD commit is literally `4e6b64d fix(stack-seeding): the usage-KPI comments said FOUR; the function emits FIVE`, authored **13 seconds after** the corpus repair commit (rext 13:10:35 vs corpus 13:10:22). The only "four" left anywhere near is `:143` — *"(The four band-triples this comment used to name …"* — which the source itself disclaims two lines later: *"They are not the current tile labels and are not a count of anything emitted here."* So a reader following the corpus's pointer finds the opposite of what the corpus asserts, plus a decoy "four" that means something else entirely. The trailing *"out of this corpus's edit scope"* is now moot as well.
  I confirmed the claim **was** true at `4e6b64d^` (both lines read "four" there), so this is drift-in-thirteen-seconds rather than sloppiness — but the sentence is false as it stands and is exactly the kind that survives future readings because it looks already-adjudicated.
- **Confidence:** high

### B4 — Both rext anchors in the same bullet drifted +4 and no longer resolve

- **Anchor:** `corpus/services/ai-readiness.md:214-216` —
  > `the set `aiReadinessUsageKPIs` returns at `stack-seeding/seeders/ai_readiness_interview_report.go:181-185`, whose own comment at `:145` reads *"All five are always emitted here"*`
- **Why it is false:** at the rext checkout (`main` @ `4e6b64d`):
  - the five-entry return set is at **`:184-190`** (`return []map[string]any{` at `:184`; the five `{"id": …}` entries at **`:185-189`**). The cited `:181-185` covers three lines of *comment* (`:181-183`) plus the `return` and only the **first** of the five ids — 4 of 5 ids fall outside it.
  - the sentence *"All five are always emitted here."* is at **`:149`**, not `:145`. `:145` reads `// were the PRE-M250 vocabulary, and were stale for the same reason the ids below were: see the M250`.

  Same +4 shift from `4e6b64d` as B3 (both anchors were correct at `4e6b64d^`). Booked separately from B3 because they are separate assertions and a reader can be misled by either alone; a single re-derivation fixes all three.
  *Hesitated:* these are anchor drifts rather than substantive falsehoods, but "wrong line anchors that point at the wrong construct" is BLOCKER-listed and a quoted sentence attributed to a line that does not contain it is not cosmetic.
- **Confidence:** high

### B5 — The storage row's residual-hit breakdown mis-classifies a `t.Fatal` as a `t.Setenv`

- **Anchor:** `corpus/architecture/platform-migration-status.md:76` —
  > `(the other 8 are two doc comments, two error strings and four `t.Setenv` calls)`
- **Why it is false:** I ran the row's own command. `git -C stack-demo/app grep -n STORAGE_RPC_ADDR b948604 -- '*.go'` returns **15** hits (correct), of which **7 are env lookups** (correct: `main.go:446`, `:524`, `:992`; `internal/jobsimwiring/wiring.go:115`; `cmd/academy-asset-upload/main.go:129`, `cmd/academyImport/main.go:231`, `cmd/import/main.go:50` — six `os.Getenv`, one lowercase `getenv` helper at wiring.go:115, exactly as claimed). The residual 8 are:
  - doc comments — `cmd/academy-asset-upload/main.go:28`, `cmd/academyImport/main.go:35` (**2** ✓)
  - `fmt.Errorf` strings — `cmd/academy-asset-upload/main.go:133`, `cmd/academyImport/main.go:235` (**2** ✓)
  - `t.Setenv` — `cmd/academy-asset-upload/main_test.go:41`, `:52`, `:64` (**3**, not 4)
  - `t.Fatal("expected error when STORAGE_RPC_ADDR is unset")` — `main_test.go:47` (**1**, uncounted)

  Read at `main_test.go:40-49`: line 47 is inside `if _, err := buildUploader(); err == nil { t.Fatal(…) }` — a failure message, not a `Setenv`. The arithmetic sums to 8 and the headline (15/7) is right, which is precisely the §5-rule-24 attractor: the sum checks out while one member of the set is mis-typed.
  *Hesitated:* this changes no operational conclusion — `STORAGE_RPC_ADDR` is genuinely read at `b948604` and genuinely not at `9d00a313`. But "wrong counts" is BLOCKER-listed and the whole cell exists to be re-derived, so a re-deriver hits a discrepancy and has to work out which side is wrong.
- **Confidence:** medium

### B6 — The re-pointed crash-diagnosis recipe now names `backend` but keeps the standalone binary's error signature

- **Anchor:** `corpus/services/jobsimulation.md:183-189` —
  > `**Always read the FIRST line of `docker logs`, never the help block** — from the container that actually runs the engine, which since the fold is `backend` …`
  > ```
  > docker logs demo-<N>-backend-1 2>&1 | head -3
  > # Error: can't init AI: can't load AWS config: failed to load shared config file, ...
  > ```
- **Why it is false:** the command was changed from `…-jobsimulation-1` to `…-backend-1`, but the expected output was not. `Error: can't init AI: …` is **cobra's** rendering of the error string `fmt.Errorf("can't init AI: %w", err)`, which exists only in the frozen repo (`stack-demo/jobsimulation/cmd/root.go:196`, `:211`, plus `aggregate.go:141`, `validate.go:137`, `test.go:152`, `clone_session.go:113`). In `app` there is **no such wrapper**: `internal/jobsimwiring/wiring.go:142-144` and `:150-152` return the AI-manager error bare (`return nil, err`), and `main.go:614` (@ `b948604`; `:670` @ `9d00a313`) turns it into
  `log.Fatalf("jobsim-in-app: engine wiring failed (is jobsim env provisioned?): %v", err)`.
  So `backend`'s first line is a Go `log` line beginning `jobsim-in-app: engine wiring failed …`, never `Error: can't init AI:`. `app` is not a cobra binary either, so the accompanying instruction *"never the help block"* describes a symptom `backend` cannot produce — while the preceding paragraph explicitly asserts *"the failure mode is inherited, only the container name changed"*, which is true of the **bind** and the **EISDIR** but not of the surface the reader is told to grep for. An operator diagnosing a dead demo searches for a string that is not there and concludes the mount is not the cause.
  (The bind itself checks out: `docker-compose.yml:91` `$HOME/.aws/credentials:/root/.aws/credentials:ro` under `backend`'s `volumes:` at `:90`, comment at `:88-89`, and it is the **only** match for `aws` in the whole file — verified. The blast-radius escalation is also mechanically sound: the error reaches `log.Fatalf`, and `core` starts only `backend` as an application container.)
- **Confidence:** medium

---

## Minors

- `corpus/services/ant-academy.md:195-197` — the five patch **names** and the five **line anchors** are printed as adjacent 5-lists in **different orders**. Names run fallback · public · chapter-body · dev-origins · back-to-cockpit; the file's assignments run `:56` dev-origins, `:66` fallback, `:75` chapter-body, `:84` public, `:90` back-to-cockpit. Only the last pairs positionally. Both lists are individually correct (verified against `demo-stack/ant-academy.sh`, which also reverts all five at `:207-231`); the juxtaposition invites a wrong 1:1 read.
- `corpus/architecture/alignment_testing.md:195` — *"the comment block at `:58-68`"*: `gate.sh:68` is `echo "==> capability-coverage check ($dna)"`, not a comment. The block is `:58-67`. (`:69` and the `:61`-is-inside-the-comment remark are both correct.)
- `corpus/services/jobsimulation.md:79` — *"consistent with the **Data** bullet, :31-38 above"*: the Data bullet is `:32-38`; `:31` is the tail of the preceding Domain/engine bullet. (`cms.md`'s twin edits, `:28-31` and `:52-53`, are both exact.)
- `corpus/architecture/architecture_overview.md:159` — the new PostgreSQL row reads `| Third-party image | Data store (custom image with `pgvector`) |`, which is internally at odds: `common.yml:3-4` **builds** it (`build: context: postgresql`) from `platform/postgresql/Dockerfile`, which compiles pgvector `v0.4.4` onto a `bitnamilegacy/postgresql:15` base. Redis (`common.yml:25`, `image: bitnamilegacy/redis:latest`) is the genuinely third-party one. `service_taxonomy.md:87` says "custom image with pgvector extension".
- `corpus/services/roadrunner.md:35` — *"What still separates roadrunner from that list…"* follows immediately after the six-entry `repos.yml` enumeration, so "that list" reads as the repos.yml list (which roadrunner is not in), when the intended antecedent is intelligence/skiller/skillpath eight lines up.
- `corpus/architecture/architecture_overview.md:25-28` — the rewritten "Off by default" bullet runs Storage's rationale into the next items with an em-dash (`… only as a rollback comparison — **Messenger** (Brevo email), **CustomerIO Sync**.`), so the three-item list reads as one sentence fragment.

---

## Positively cleared (audited zeros)

**ENUMERATED** (predicate re-derived from source independently, cardinalities compared):

- **`repos.yml` = 6 entries, 31 lines** (`app`·`sentinel`·`storage`·`messenger`·`next-web-app`·`studio-desk`), dumped in full. Every derived claim checks: `app` at `:11-14` (`migrations: true`, `schema: public`, and the **only** `migrations: true`), `sentinel` `:15-17`, `storage` `:18-20`, `messenger` `:21-23`, `next-web-app` `:26-28`, `studio-desk` `:29-31`; `:9-10` names infrastructure's `services.tf` until M810. The old `repos.yml:17-19` citation does now land on sentinel/storage, as `hiring.md` says. At `2adcf71` the file had **9** entries with `cms` `:14-16`, `jobsimulation` `:17-19`, `roadrunner` `:29-31` — all as cited.
- **`docker-compose.yml` declares 8 services at `0dab54d`** (sentinel, backend, storage, customerio-sync, messenger, studio-desk, next-web-app, gotenberg), **11 at `d11a403^`** — the repair's "one of eleven there; eight remain" is exact. Ten in the effective topology with `common.yml`'s postgresql+redis.
- **`profiles:` keys — all 7 enumerated**; the `graphql` token appears in none. Confirmed live: `--profile graphql` exits 0 and starts only postgresql/redis/sentinel.
- **`AUTHORIZATION_ADDRESS` = exactly 1 occurrence** across `docker-compose.yml` + `common.yml` + `.env_example`, at `:48` (backend). At `2adcf71` it was 3: `:45`/`:99`/`:160`.
- **`*_RPC_ADDR` = exactly 4 occurrences**, all on messenger: `:173`/`:174`/`:176`/`:183`, all `http://backend:8083`, under the comment at `:171-172`. `STORAGE_RPC_ADDR` and `ROADRUNNER_RPC_ADDR` = **0**.
- **`STORAGE_RPC_ADDR` in `app` Go source: 3 hits at `9d00a313`, all comments** (`main.go:451`, `internal/jobsimwiring/wiring.go:101`, `internal/storagens/callsites_test.go:189`) — zero reads; **15 hits at `b948604`, 7 of them env lookups**. Both sides of the repair's "state the ref or the sentence flips" hold. (The residual-8 sub-breakdown is B5.)
- **`d11a403`'s `repos.yml` diff deletes `- name: cms`, `- name: jobsimulation` AND `- name: roadrunner`** — while its own message claims roadrunner's entry *"was already gone"*. The repair's *"the message is wrong; the diff is the fact"* is correct, in both `platform-migration-status.md:74` and `roadrunner.md:16-19`.
- **`askengine/registry.go`: 60 `TableDef` entries in the `defs` slice, 57 `Schema: "public"`, 3 `Schema: "directus"`** — counted the slice members independently of the `Schema:` grep, both give 60. `:183-195` is indeed the jobsimulation-alias comment block asserting the dotted names RESOLVE TO the public tables and that `jobsimulation` stays in `rewriteSchemas` until M710.
- **`colony/authn` importers: app 129 files; sentinel 0; storage 0; messenger 0; cms 6; jobsimulation 8; roadrunner 0** — every number in `clerk-integration.md` and `shared_libraries.md`'s authn row is exact.
- **All shared-library pins** from each `go.mod`: colony app/messenger `v0.35.2`, sentinel/storage/roadrunner `v0.34.3`, cms/jobsimulation `v0.35.1` (the removed third pin); proto `v1.210.0`/`v1.210.0`/`v1.200.0`/`v1.196.0`/`v1.207.0`/`v1.205.0`/`v1.196.0`; ai `v1.40.2` in app:14, cms:9, jobsimulation:11 and **nowhere else**; taxonomy `v1.2.0` direct in app/messenger/cms:13/jobsimulation:15, `// indirect` in sentinel/storage, absent from roadrunner.
- **`app/studio/requirements.txt` = 9 packages** (openai, anthropic, rich, pyyaml, requests, jinja2, mistralai, pytest, pytest-asyncio), **`python-docx` absent**, and **byte-identical to `cms/studio/requirements.txt`** (md5 `20a5329a…` both). `.docx` appears exactly **twice** in the tree — `agents/simulation/guidelines.py:48` and `knowledge/development/asset-examples/README.md:59` — exactly the two the repair names. `bedrock|boto3` under `app/studio/` = **0**.
- **Clerkenstein: 5 DNAs, 5 runners** (`clerkrun`, `deployrun`, `expressrun`, `jsfapirun`, `multirun` vs `clerk-2.6.0`, `clerk-deploy-1`, `clerk-express-1`, `clerk-js-5`, `clerk-multi-1`) — "exactly one runner *per surface*" holds, and all four `clerkenstein/knowledge/*.md` targets exist.
- **Compose profile behaviour, run live** — every profile claim added by the repair reproduces, including the two `exits 1` claims whose error strings match the corpus verbatim.

**RE-DERIVED** (checked the arithmetic/anchor the document itself displays, without independently enumerating the set):

- Every `docker-compose.yml` line anchor at `0dab54d`: `:28`, `:48`, `:59`, `:82`, `:88-89`, `:90`, `:91`, `:92-99`, `:93`, `:100`, `:126-130`, `:131-133`, `:134`, `:156`, `:171-183`, `:186-192`, `:195`, `:197`, `:204`, `:220`, `:223-225`, `:226`, `:228`, `:236`, `:237`, `:245`, `:246`, `:250-252`, `:253`, `:268` — **all resolve to the cited construct**.
- Every `@ 2adcf71` back-reference: jobsim `:83`/`:93-94`/`:113`/`:119`/`:140`; cms `:154-155`/`:169`/`:173`; roadrunner `:291-292`/`:296-302`/`:298`/`:302`; storage `:213-217`; backend `:70-80`; studio-desk `:337-341`; next-web `:352`/`:361`/`:362`; `Makefile:10 PROFILE ?= graphql`. **All correct.**
- `common.yml:2` postgresql, `:22` `start_period: 120s`, `:24` redis (the `:20`→`:24` re-point is right). `Makefile:10 PROFILE ?= core`; `Makefile:119-120` `--profile core --profile frontend`.
- `app` @ `9d00a313`: `main.go:451`, `:471`, `:472`, `:494`, `:1048`, `:1095`, `:1099`, `:637`, `:668`, `:698`, `:1387`, `:1423`, `:1478-1484`; `internal/storage/service.go:22`/`:24`; `internal/jobsimwiring/wiring.go:101`/`:123`; `cmd/academyImport/main.go:239-243`/`:244-246` (quote matches). `app` @ `b948604`: `wiring.go:115`/`:118`; `main.go:1187`/`:1188`/`:1196`/`:1204`/`:1212-1214`/`:1228`.
- Migrations: `20260729133514.sql:62`/`:63` drop the two mirrors; **three** migrations post-date it at `9d00a313` (`20260731131307`, `20260731154527_…`, `20260803143844_…`) — only two at `b948604`, and the claim names `9d00a313`, so it stands. `20230817154747_supabase_baseline.sql:2` is the **only** `CREATE SCHEMA` in the whole set, and it is `auth`.
- Terraform at the refs the blocks name: `storage` @ `63bffc8` `main.tf:38 = 0`, ordering comment `:29-33`, prevent_destroy/M903 `:35-37`; `messenger` @ `a0ec933` `main.tf:29 = 0`, rollback comment `:27-28`; `jobsimulation/terraform/main.tf:40 = 0`; `cms/terraform/main.tf:39 = 0`. (Note the storage/messenger **checkouts** are older — `4ce8ece` v0.15.3 / `fa47850` v0.42.0 — where those lines differ; the blocks name their refs, so rule 33 settles them.)
- Legacy port defaults: `cms/cmd/root.go:77`/`:78` → 8080/8081; `jobsimulation/cmd/root.go:77`/`:78` → 8080/8081; `roadrunner/cmd/root.go:110` → 8080, `:84` → 8081. The roadrunner env table's "built-in default vs compose-supplied" split is right: only `PORT`/`RPC_PORT` use `cmp.Or`; `SENTRY_DSN` `:46`, `REDIS_ADDR`/`REDIS_STREAMS_INDEX` `:57`, `REDIS_WORKER_INDEX` `:68`, `JUDGE0_*` `:78-79` are bare `os.Getenv`.
- Self-referential corpus anchors, all re-pointed correctly: `external_services.md` `:546→:555` (the retraction sentence), `:542→:543` (the Anthropic-Direct provider row), `:570→:577-582` (the four-live-routes preamble), `:578-588→:594-604` (item 4), `:512→:522` (the Wundergraph-restart correction), `:296` (the eight/ten split). `service_taxonomy.md` `:75→:79` (the Studio-Desk row) and `:61→:58-59` (*"There is no `graphql` profile"*). `cms.md` `:27→:28-31` (Data bullet) and `:37→:52-53` (Studio bullet). `ai-readiness.md`'s `:533` → *part 5* of § *The FILLED-ness contract* resolves to `:620-628`.
- `app/internal/cms/worker/worker.go`: `Concurrency: 5` `:29`, `AiVideoQueue: 7` `:32`, `StudioQueue: 3` `:33` — studio-room.md's numbers.
- `alignctl` `dna.go:20-26` covers `list|diff|validate|coverage`; `gate.sh:69` is the `--if-declared` call.
- `storage/main.go` is **18 lines**, as storage.md's disambiguation note says.

---

## What I could not settle

- **Whether `d11a403`'s claim that roadrunner's clone entry "was already gone" refers to a *different* file or a stale local state.** The diff is unambiguous (that commit removes the entry), and the corpus grades it correctly by the diff. But no evidence in the clones explains why the author believed otherwise. Settled by reading the PR thread for `ef32d4c`, which is outside these clones.
- **Whether prod's `roadrunner` ECS service actually runs.** Both `platform-migration-status.md:74` and `roadrunner.md` say the authoritative rollback declaration lives in `infrastructure`'s `services.tf`, a repo present in **no** stack workspace here. `roadrunner/terraform/main.tf:19 = 1` is real (and, per B2, unchanged since 2025-12-15), but it may be overridden there. The corpus records this as unresolved, which is honest. A clone of `anthropos-work/infrastructure` would settle it.
- **B6's real `backend` failure line, observed rather than derived.** I traced the path statically (`wiring.go:142/150 → return nil, err` → `main.go:614` `log.Fatalf`) but did not reproduce the EISDIR crash on a live stack. Booting a demo with `$HOME/.aws/credentials` absent-as-a-directory and capturing `docker logs demo-N-backend-1 | head -3` would give the exact string the doc should print.
- **Whether the ai-readiness rext drift (B3/B4) was intended to be caught by a guard.** The repair explicitly declared the rext side *"out of this corpus's edit scope"* and rext was fixed 13 seconds later; I cannot tell from the corpus whether any of the five corpus guards is supposed to fence rext line anchors. Nothing under `knowledge/plan/` was read.
