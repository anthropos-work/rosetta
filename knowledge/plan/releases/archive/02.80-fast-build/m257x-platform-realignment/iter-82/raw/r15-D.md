# Seat D — reading #15

**Refs used (stated at the moment of measurement, per §5 rule 26):**
`app` checkout `b948604ff` (tag `v1.366.0`); `app` `origin/main` = `717737471`, four commits past
`9d00a3131` (v1.367.0) — both read where a claim named one. `platform` `0dab54d` (level with
origin/main). `cms` `ca50c817`, `roadrunner` `87d8d44`, `jobsimulation` `462343b0`,
`graphql-wundergraph` `60c229f3`, `next-web-app` `bb3313bc`, `ant-academy` `9c3843cd`,
`studio-desk` `14a5442a`, `sentinel` `88bc5592`, `storage` `4ce8ece5` (also read at `63bffc8`, the
ref the corpus names), `messenger` `fa47850d` (also read at `a0ec933`, likewise).

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/services/studio-room.md` | 474 | yes |
| `corpus/architecture/architecture_overview.md` | 381 | yes |
| `corpus/services/cms.md` | 270 | yes |
| `corpus/services/roadrunner.md` | 199 | yes |
| `corpus/services/clerk-integration.md` | 128 | yes |
| `corpus/services/gotenberg.md` | 82 | yes |
| `corpus/services/intelligence.md` | 18 | yes |

Also read in full as required by the briefing: `corpus/ops/platform-alignment.md` (1726 lines, §5
in full).

Search-pipeline controls run in the same pass (§5 rules 1/2/6): every `grep`/`git grep` had stderr
unredirected; each absence claim below was paired with a pattern known to match in the same tree
(e.g. `colony/authn` returned 0 in sentinel/storage/messenger while `anthropos-work/colony` returned
2/3/8; `graphql|directus` returned 0 across `app/studio` while `openai` returned 4 in
`services/ai.py`). One engine rejection was caught and re-run (`--include=*.go` unquoted under zsh
returned `no matches found` with exit 0 — re-run quoted).

---

## Blockers

### B1 — `cms.md` calls cms "the fourth and **last** engine consolidated into `app`" — two more have folded since

- **Anchor:** `corpus/services/cms.md:8` — *"subgraph is gone from the supergraph. It is the fourth
  and last engine consolidated into `app`, after [skiller], [skillpath] and [jobsimulation]."*
  (The sentence names no ref, so it grades at the checkout — and it is false there too.)
- **Why it is false:** the v9.0 "support-in-app" program folded **storage** and **messenger** into
  `app` after cms.
  - `stack-demo/platform/docker-compose.yml:131-133` (@ `0dab54d`): *"v9.0: NOT in the default
    profiles any more — app serves storage in-process, and running both means two writers on one
    bucket."*  `:134` moves it to `profiles: [storage-legacy]`.
  - `stack-demo/platform/docker-compose.yml:193-195`: *"v9.0: merged into backend. Removed from
    `all` because app now consumes messenger's OWN Redis consumer group."*
  - `stack-demo/app` @ `9d00a313` (v1.367.0) `main.go:450-451`: *"storage-in-app (v9.0 cutover): the
    in-process object managers. app IS storage now — the standalone service takes no traffic and
    STORAGE_RPC_ADDR is gone."*  I verified `STORAGE_RPC_ADDR` has **zero** non-comment reads at that
    ref (3 hits, all comments: `main.go:451`, `internal/jobsimwiring/wiring.go:101`,
    `internal/storagens/callsites_test.go:189`).
  - Prod compute stopped at the refs the corpus itself names: `storage/terraform/main.tf:38` `= 0`
    @ `storage 63bffc8`; `messenger/terraform/main.tf:29` `= 0` @ `messenger a0ec933`.
  - Corpus twins that say the opposite: root `CLAUDE.md:198-206` (*"Storage and Messenger are BOTH
    FOLDED INTO `app` — the v9.0 program landed 2026-08-04"*), `corpus/ops/platform-alignment.md:22`
    (*"v9.0 support-in-app IN FLIGHT — folds storage + messenger"*), and
    `corpus/architecture/platform-migration-status.md:76-77` (both rows `merged-into-app`).
  - Grepped the whole tree for the phrase: **1 instance only** (`cms.md:8`), so this is a lone
    survivor rather than a uniform corpus claim.
- **Why I hesitated:** the word "engine" could be read as excluding storage/messenger (they are not
  domain engines). But the sentence's own enumeration includes **jobsimulation** and the corpus
  counts **roadrunner** as folded too, so "fourth" is already loose; "last" is the load-bearing word
  and it is refuted by the platform's own compose comments. Booked as a BLOCKER because a reader
  planning against "the consolidation is done" would be wrong about the two most recent folds.
- **Confidence:** high

### B2 — `gotenberg.md` states the wrong purpose for the one consumer: the PDF is a throwaway text-extraction intermediate, not an artifact for "display and storage"

- **Anchor:** `corpus/services/gotenberg.md:7` — *"In the Anthropos platform it exists for one
  consumer: the backend service uses it to render user-uploaded documents into PDFs **for downstream
  display and storage**."*
- **Why it is false:** `converter.ConvertToPDF` has **exactly two** call sites in `app` @ `b948604`
  (enumerated: `git grep -n 'ConvertToPDF('` minus the definition), and in both the PDF is consumed
  immediately and discarded:
  1. `stack-demo/app/internal/web/backend/coursebuilder/extract.go:77` → the very next statement,
     `:81`, is `return converter.ConvertFromReader(bytes.NewReader(pdf), "application/pdf")`. The
     file's own header comment (`:11-15`) states the purpose: *"gotenbergMimes are the upload formats
     docconv can't read directly … We route them through Gotenberg (LibreOffice) -> PDF, then extract
     the PDF with docconv."*
  2. `stack-demo/app/internal/worker/tasks/user_import_resume_2d.go:68` → the PDF is assigned to
     `ocrInput` (`:72`) and handed to `h.ocrClient.OCRProcess(ctx, ocrInput)` (`:76`); the log line at
     `:71` reads *"DOCX converted to PDF for OCR"*. This branch runs **only** when
     `converter.ConvertFromReader` already failed to find readable text (`:56-57`).
  Nothing writes the PDF to storage and nothing renders it. The doc's own Usage section (`:44-47`)
  correctly cites `internal/converter/gotenberg.go` but never corrects the stated purpose.
- **Why I hesitated:** the mechanism (document → Gotenberg → PDF) is right; only the *why* is wrong.
  Booked as a BLOCKER because "for downstream display and storage" would send a reader hunting for a
  stored-PDF surface that does not exist, and because the doc presents this as the service's whole
  reason for being ("it exists for one consumer").
- **Confidence:** high

### B3 — the production request-flow block still lists `storage` as a live Connect-RPC target, contradicting the same file 20 lines later

- **Anchor:** `corpus/architecture/architecture_overview.md:295` — *"→ Connect-RPC to internal
  services (sentinel, storage)"*, inside the block headed **"In production"** (`:290`).
- **Why it is false / self-contradictory:**
  - The **same file** at `:315-316` states: *"The app side closed at **`app` `9d00a313`**
    (v1.367.0): `STORAGE_RPC_ADDR` has **zero reads** there, and `main.go:451` says 'the standalone
    service takes no traffic and STORAGE_RPC_ADDR is gone.'"* I re-derived that independently —
    3 hits at `9d00a313`, all comments. A repo with zero reads of the address cannot make the call in
    *either* environment; the retraction at `:311` scopes itself to *"Nor is `storage` one
    **locally**"*, but the evidence it cites is repo-wide and overshoots its own scoping.
  - Prod compute is stopped: `storage/terraform/main.tf:38` `service_desired_count = 0` @ `storage
    63bffc8` — the ref `corpus/architecture/platform-migration-status.md:76` names, and that row
    grades `storage` as `merged-into-app` in the **prod** column.
  - (At the *checkout* `app b948604` / `storage 4ce8ece5` the line would be true — `main.go:446`,
    `:524`, `:992` and `internal/jobsimwiring/wiring.go:115` still build a storage RPC client, and
    `storage/terraform/main.tf:19` still reads `= 1`. But the block is unpinned and sits in a file
    that elsewhere pins itself to `9d00a313`, so the reader gets two opposite answers from one page.)
- **Why I hesitated:** the block is unpinned, and §5 rule 33 says an unpinned claim grades at the
  checkout, where it is true. I booked it anyway because the contradiction is *intra-file* and the
  document itself elects `9d00a313` as the current app ref — a reader has no way to know which of the
  two passages is the live one.
- **Confidence:** medium

### B4 — `authn` is listed among the shared libraries "imported by services" / "Imported as private Go modules"; it is imported by nothing

- **Anchor:** `corpus/architecture/architecture_overview.md:74` — *"4. **Shared Libraries**: colony,
  authn, proto, ai, taxonomy (not deployed, **imported by services**)"*; the same over-claim heads the
  library table at `:215` — *"Imported as private Go modules — **not** cloned by `make init`."* —
  which lists `authn` as its 4th row (`:222`).
- **Why it is false:** the standalone module `github.com/anthropos-work/authn` appears in **zero**
  `go.mod` files and has **zero** source imports. Enumerated across all seven Go repos in
  `stack-demo/` (`app`, `sentinel`, `storage`, `messenger`, `cms`, `jobsimulation`, `roadrunner`):
  `grep -c 'anthropos-work/authn' <repo>/go.mod` → 0 in every one; `grep -rn 'anthropos-work/authn'
  --include="*.go"` → 0 hits. Positive control in the same pass:
  `grep -rn 'colony/authn' --include="*.go" app` → hits at `app/rpc.go:15`,
  `app/jobsim_users_reader.go:11` (129 files total).
  Corpus twins that say the opposite: root `CLAUDE.md` (*"four are imported as private Go modules
  (`ai`, `colony`, `proto`, `taxonomy`); **`authn` is imported by nothing**"*, stated twice), and
  `corpus/architecture/shared_libraries.md` per its index line. The file's own row at `:222` says
  *"standalone repo is legacy"* — which softens but does not correct the two blanket statements.
- **Why I hesitated:** `authn`'s *code* is imported, as `colony/authn`, so the header is arguably
  true-in-spirit; and `:222` disambiguates in place. But `:74` is a flat five-item list 148 lines
  earlier, in the tier summary a reader skims first, with no qualifier at all — and this is a claim
  the corpus's own instruction file flags twice as a known trap. Booked upward per the briefing.
- **Confidence:** medium

---

## Minors

- `corpus/services/clerk-integration.md:99` — *"gated on the `STUDIO_ACCESS_ROLES` set — `org:admin`
  **and** `content_creator`"*. The set has **four** members:
  `['admin', 'org:admin', 'content_creator', 'org:content_creator']`
  (`studio-desk/app/services/userService.ts:16` and `studio-desk/src/index.ts:96`, identical). The
  two bare forms are omitted, and the bolded "and" reads as a conjunction where the code is an
  `includes` disjunction. Non-material (both role families are named), but a reader matching only the
  prefixed forms would mis-gate.
- `corpus/services/clerk-integration.md:96` — the row covers `apps/web`, `apps/hiring` **and**
  `apps/integration` with *"`@clerk/nextjs` (+ `@clerk/localizations`)"*, but `apps/integration/
  package.json` installs `@clerk/nextjs` only (`:9`); `@clerk/localizations` is in `apps/web` (`:9`)
  and `apps/hiring` (`:9`) alone.
- `corpus/services/studio-room.md:60-93` — the project-structure tree omits `tools/`, `tests/`,
  `pytest.ini`, `cog.toml` and `changelog.md`, all present at `app/studio/`. It also shows
  `workspace/` as though checked in; it is created at runtime (`gen.py:450-455`) and is absent from
  the repo. Harmless, but the tree reads as exhaustive.
- `corpus/services/studio-room.md:85` — *"(`configs/local_*` and `configs/test_*` are gitignored)"*.
  `app/studio/.gitignore:4-6` also ignores `configs/config.ini`.
- `corpus/services/studio-room.md:425` — *"(preserving any `collaborative_*` / `asset_*` JSON already
  in the bundle)"*. `agents/simulation/postgen/exporter.py:537` also preserves `internal_*`.
- `corpus/services/intelligence.md:5` — *"removed … in **mid-2026**"*; `fdfa189` is dated
  **2026-04-17** (verified `git log -1 fdfa189`).
- `corpus/services/roadrunner.md:118` — *"Judge0 at `JUDGE0_BASE_URL=http://52.48.139.23:2358`
  (default in compose)"*. The value is right, but at `0dab54d` it is set on **`backend`**
  (`docker-compose.yml:59`); there is no roadrunner compose block to carry a default, as the same doc
  says at `:53-57`.

---

## Positively cleared (audited zeros)

Every item below is labelled with the strength of the clearance. **ENUMERATED** = I derived the
predicate's set from source independently and compared cardinalities. **RE-DERIVED** = I only
re-computed arithmetic the document itself displays (per §5 rule 24, the weakest form).

**ENUMERATED**

- `architecture_overview.md:328-334` / the multi-tenancy fence — the one §5 rule 24 says three seats
  got wrong. I enumerated the SET, not the sum: `internal/data/ent/schema/` holds **139** `.go`
  files, **135** of which contain `ent.Schema` (the 4 that do not are `database_types.go`,
  `mixin.go`, `skiller_mixins.go`, `skillpath_mixins.go`) — denominator correct. `OrganizationMixin{}`
  appears in **30** files; `user_resource.go:22` is `// OrganizationMixin{},  // We need to work on
  this` — commented out, so the live set is **29**. Files declaring their own `Policy() ent.Policy`
  are exactly **four** — `mixin.go`, `organization.go` (`:56`, `rule.FilterSameOrganizations()` at
  `:96`), `org_membership.go` (`:172`, `AllowCurrentOrgEdgesOrSkipRule`) and `user.go` (`:116`, which
  filters by **user** — `FilterSameUserRule`/`DenyNotSameUser` — and is correctly excluded).
  29 + Membership + Organization = **31 of 135** ✅. The second clause also holds: `OrganizationIDMixin{}`
  in **7** (`category`, `jobrole`, `similarity`, `skill`, `specialization`, `studio_document`,
  `studio_task`); a plain `"organization_id"` with neither mixin in **18** (a 19th hit,
  `skiller_mixins.go`, is not a schema); of those 18, `org_membership.go` polices itself and
  `academy_feedback.go:64` carries `UserMixin{}` → 16 unpoliced; 16 + 7 = **23** ✅, and 16 is indeed
  *the neither-mixin subset*, not the total. Consistent with `security_compliance.md:69-99`.
- `cms.md:41-46` + `architecture_overview.md:262` — the subgraph ladder. Enumerated the subgraph set
  at each ref from `supergraph-config-prod.yaml`: `749dc86^` = **5** (backend, skiller, jobsimulation,
  cms, skillpath) → `749dc86` = **4** → `7c17e63` = **3** (backend, jobsimulation, cms) → `915da06` =
  **1** (backend). `git show --stat 915da06` deletes **both** `schemas/cms.graphqls` (762 lines) and
  `schemas/jobsimulation.graphqls` (860) in one commit, so **3 → 1** is right and the commit's own
  subject line ("2→1") is wrong — exactly as both docs warn. `ls schemas/` = `backend.graphqls` alone.
- `cms.md:12-14`, `:32-37` — the four `*_RPC_ADDR` values. Enumerated `grep -n '_RPC_ADDR'
  docker-compose.yml` @ `0dab54d`: exactly four (`:173` BACKEND_USERS, `:174` CMS, `:176`
  JOBSIMULATION, `:183` SKILLER), all `http://backend:8083`. `:174` is the CMS one, as cited.
  `repos.yml` holds exactly **6** entries (app, sentinel, storage, messenger, next-web-app,
  studio-desk); no `cms`. Compose declares **8** services + 2 from `include: common.yml` = **10**
  effective, as `:35` says.
- `roadrunner.md:16-19` + `architecture_overview.md:198-200` — the "grade `d11a403` by its diff, not
  its message" claim. `git show d11a403 -- repos.yml` deletes `- name: cms`, `- name: jobsimulation`
  **and** `- name: roadrunner`, while the message asserts roadrunner's entry *"was already gone"*.
  Both halves verified.
- `roadrunner.md:24-26`, `:53-54`, `:117` — the orphan claim. `ROADRUNNER_RPC_ADDR`,
  `RoadRunnerService` and `roadrunner:10401` return **0** hits across `app` + `jobsimulation` Go
  source; `ROADRUNNER_RPC_ADDR` is absent from `platform/.env_example` (108 lines, only
  `JUDGE0_API_KEY:87` in that family) and from both compose files. The nine surviving `roadrunner`
  substring hits are all comments.
- `clerk-integration.md:37` — "**12 event types**". Enumerated the switch at
  `app/internal/clerk/events/events.go:165-240`: `user.{created,updated,deleted}` +
  `organization.{created,deleted,updated}` + `organizationInvitation.{accepted,created,revoked}` +
  `organizationMembership.{created,deleted,updated}` = **12**, no more.
- `clerk-integration.md:51`, `:91` — "**129** files in `app` vs **0** in each of the other three".
  `grep -rl 'colony/authn' --include="*.go"` → app **129**, sentinel **0**, storage **0**,
  messenger **0**; control `anthropos-work/colony` → 202 / 2 / 3 / 8. Also `AuthRole()` has **0**
  call sites anywhere in `app`, so `:73`'s "zero call sites on the allow/deny path" holds.
- `clerk-integration.md:41` — "8 locales". `apps/web/src/providers/Clerk.provider.tsx:9-18` imports
  deDE, enUS, esES, frFR, itIT, jaJP, nlNL, ptPT = **8**.
- `clerk-integration.md:33` — "`getToken()` … with **no template**". The only `getToken({…})` call in
  the monorepo is `apps/hiring/…/start-sim/TokenValidationClient.tsx:112`, passing `{ skipCache: true }`;
  every other call is bare. No `template:` key reaches a `getToken` anywhere.
- `studio-room.md:181-197` / `cms.md:237-242` — "exactly nine arguments (`gen.py:484-492`)". Read
  `app/studio/gen.py:480-493`: `add_argument` calls occupy `:484`–`:492` inclusive, nine of them, and
  `grep -c add_argument gen.py` = 9. Names, dests and defaults all match the doc's table.
  `parse_argument` is at `:18-28` and does call `parse_known_args` + merge leftovers; the only
  `template` readers in the tree are `translate_legacy_blueprint` (blueprint *field*) and tests.
- `studio-room.md:35` — the Asynq figures. `app/internal/cms/worker/worker.go:29` `Concurrency: 5`;
  `:32-33` `AiVideoQueue: 7`, `StudioQueue: 3`; `queues.go:4-5` gives the literals `"studio"` /
  `"ai_video"`. `studioManager.go:226` enqueues on `queues.StudioQueue`.
- `studio-room.md:161-175` — the post-gen target set. `agents/simulation/postgen/__init__.py:56-74`
  dispatches on exactly **four** keys — `toolkit`, `guidance`, `metadata`, `translation` — and
  `export` is not among them; `testing.py` exists as a module. `postgen.py:396-398` marks `--media`,
  `--simid`, `--target` `required=True`.
- `studio-room.md:388` — "studio-room makes no GraphQL or Directus calls". `grep -rni
  'graphql|directus' app/studio --include="*.py"` → **0**, control `openai` in `services/ai.py` → 4.
  Provider clients confirmed at `services/ai.py:1-2` (imports), `:383` `return OpenAI(...)`, `:530`
  `return AzureOpenAI(`, `:664` `return Anthropic(...)`; `services/taxonomy.py:11`
  `BASE_URL = "https://api.anthropos.work/api"`.
- `studio-room.md:306-308` — the worked-example inventory. `tests/e2e/blueprints/` = exactly
  `b2b, business, none_chat, none_voice, technical`; `knowledge/development/asset-examples/blueprints/`
  = exactly three, one each for code / micro / scenario.
- `architecture_overview.md:12`, `:66`, `:151-152` — "five containers". Enumerated compose: 8 services
  in `docker-compose.yml` + 2 in `common.yml`; the ones declaring **no** `profiles:` key are
  `sentinel`, `postgresql`, `redis`; `core` adds `backend` (`:100`) and `gotenberg` (`:268`). Total 5,
  of which 2 are our Go services. `Makefile:10` is `PROFILE ?= core`, and `0dab54d` is the commit
  titled *"run without the standalone storage; rename graphql -> core"*.
- `architecture_overview.md:172`, `:208`, `:362` — "23 run-state tables". `grep -c '^CREATE TABLE'
  app/terraform/migrations/20260722081626_jobsim_data_model.sql` = **23**.
- `architecture_overview.md:272` — "all three `app/studio/configs/*.ini` pin `azure`". Read all three
  in full: every `*_AI_*_MODEL` row in `config_template.ini`, `development_config.ini` and
  `production_config.ini` begins `azure,`. No `openai`/`anthropic` TARGET SERVICE is selected anywhere.
- `cms.md:116-121` — "9 packages, `python-docx` is NOT among them". `app/studio/requirements.txt`
  read verbatim: openai, anthropic, rich, pyyaml, requests, jinja2, mistralai, pytest,
  pytest-asyncio = **9**; no `python-docx`. `diff app/studio/requirements.txt
  cms/studio/requirements.txt` → identical, so `cms.md:224`'s "byte-identical" holds.
- `roadrunner.md:111` — every figure. `queues/queues.go` `DefaultQueue = "roadrunner:default"`;
  `runner.go:126` `asynq.MaxRetry(3)` inside `CreateSubmission` (`:48`, the only enqueue site, so
  both `Submission` and `SubmissionPackage` produce exactly one task); `worker.go:25`
  `Concurrency: 10`, `:48` binds `HandleSubmissionResultTask`; `runner.go:244` `maxRetries = 15`,
  `:273` `time.Sleep(1 * time.Second)`, `:277-278` publishes `RoadrunnerSubmissionCompleted`.
  RPC surface is exactly `Submission` / `SubmissionPackage` / `SubmissionResult`
  (`internal/rpcsrv/*.go:20,43,60`). Zero `*_test.go` files; `Dockerfile:18` is `RUN go test -v ./...`.
  Zero `.proto` files in-repo; `go.mod:8` imports `anthropos-work/proto v1.196.0`;
  `languages.go:26` `case "py":`.
- `gotenberg.md:11-30`, `:44-48` — everything except the purpose sentence. `docker-compose.yml:268`
  **is** the `profiles: [core, backend, all]` line; image `gotenberg/gotenberg:8`; the four command
  flags and `3200:3200` match; `GOTENBERG_URL=http://gotenberg:3200` at `docker-compose.yml:57` inside
  the `backend` block; `internal/converter/gotenberg.go:13` `Timeout: 90 * time.Second`, `:16` the
  exact signature, `:31` the `/forms/libreoffice/convert` path.
- `intelligence.md:5-8` — `fdfa189` is *"chore: remove intelligence service from local dev
  orchestration"*, touching `docker-compose.yml` (−25) and `repos.yml` (−3); no `intelligence` entry
  survives in `repos.yml` and no compose service depends on it.

**Line-anchor spot-checks that resolved to the right construct** (`sed -n 'N-15,N+5p'` in every case,
per the briefing's method rule — a resolving line number is not a resolving *claim*): `cms/terraform/
main.tf:39` `= 0`; `jobsimulation/terraform/main.tf:40` `= 0`; `graphql-wundergraph/terraform/
main.tf:20` `= 1`; `roadrunner/terraform/main.tf:19` `= 1`; `cms/go.mod:3` `go 1.26.4`;
`cms/cmd/root.go:77`/`:78`; `cms/Dockerfile:2`/`:23`; `roadrunner/cmd/root.go:84`/`:110`;
`app/main.go:1205-1211` (the DORMANT/M809 comment) @ `b948604`; `app/main.go:997-998`
(`REDIS_CMS_CACHE_INDEX`, default 5); `app/internal/skillpath/session.go:205-207`
(`GetSkillPathDomain`); `app/internal/jobsimwiring/wiring.go:118` (Judge0) and `:115`
(`STORAGE_RPC_ADDR`) @ `b948604`; `app/main.go:451` @ `9d00a313`; `docker-compose.yml:93`, `:131-133`,
`:134`; `app/internal/jobsimulation/ai/ai.go` `:129`, `:166`, `:325`, `:262-276`, `:85-88`;
`app/internal/cms/studio/markdownManager.go:11,19` and `studioManager.go:583`, `:94-96`, `:119`;
`app/studio/gen.py:18-28`, `:241-271`, `:273-282`, `:450-456`; `agents/simulation/postgen/
exporter.py:518-519` and `:513-550`; `agents/simulation/model.py:59` and `:467-469`;
`next-web-app/apps/mobile/package.json:6`, `ant-academy/mobile/package.json:18`,
`ant-academy/code/package.json:52`, `app/go.mod:31`. **All resolved and all named the construct the
prose claims** — including `translate_legacy_blueprint` at `gen.py:205-238`, where `:205` is the
`_LEGACY_TEMPLATE_DEFAULTS` dict whose three keys (`:206-208`) are the "exactly three legacy names"
the sentence enumerates, and the function runs `:212-238`.

**RE-DERIVED only (weak — flagged as such)**

- `clerk-integration.md:103` — the SDK-version block. I confirmed each figure the document displays
  (`^6.39.2` on all four `@clerk/nextjs` installs; `~2.6.18` vs `~2.19.36`, "thirteen minor versions
  apart"; `clerk-sdk-go/v2 v2.7.0` at `app/go.mod:31`, identical at `5ba17044` and at the checkout).
  I did **not** independently enumerate the set of Clerk packages the monorepo installs beyond the
  `apps/*/package.json` + `e2e/package.json` glob, so an install outside that glob would be invisible
  to this clearance.
- `architecture_overview.md:54` — `next: ^16.2.7` "across all four apps". The four Next apps in
  `next-web-app/apps/` (web, hiring, integration, maintenance) all read `^16.2.7`. Note `ant-academy/
  code/package.json:64` is `^16.2.6` — outside the stated scope, so not a finding, but the phrase
  "all four apps" only reads correctly if the reader knows it means next-web-app's four.

---

## What I could not settle

- **GitHub `archived` flags.** `architecture_overview.md:3` (graphql-wundergraph archived 2026-07-30),
  `:20` (jobsimulation archived 2026-07-31), `roadrunner.md:32,36,49-51` (skiller 2026-07-01,
  skillpath 2026-07-31, roadrunner **not** archived, chronos **not** archived) are all §4-signal-6
  claims that need `gh repo list anthropos-work` or the REST API. No network/`gh` was used in this
  reading. Settled by: `gh api repos/anthropos-work/<name> --jq .archived`.
- **`clerk-integration.md:67`** — *"`AuthRole()` is exposed as a getter and never enforced inside
  `authn`"*. `colony` is not among the ground-truth clones, so the library's internals are
  unreadable here. (The *consumer* half — 0 `AuthRole()` call sites in `app` — I did verify.)
- **`intelligence.md:14-19`** — the historical port/ticker/env-var details. The `intelligence` repo is
  not cloned; nothing in the provided trees can confirm or refute them. Settled by cloning
  `anthropos-work/intelligence` at any pre-`fdfa189` ref.
- **Taxonomy magnitudes** (`architecture_overview.md:13`, `:223` — ≥42,790 skills / ≥22,470 job
  roles) and **`cms.md:78`**'s prod Directus predicate (`private = false AND tenant_id IS NULL AND
  status = 'published'`) require a DB read, which this reading did not take.
- **A near-miss worth recording for the next seat (§5 rule 26 / rule 33 in action).** The provided
  ground-truth clones for `storage` (`4ce8ece5`) and `messenger` (`fa47850d`) are **behind** the refs
  the corpus names for those repos (`63bffc8` / `a0ec933`, per
  `platform-migration-status.md:76-77`). Graded at the *provided* clones, `storage/terraform/
  main.tf:38` and `messenger/terraform/main.tf:29` do not exist as `service_desired_count` lines at
  all — both files read `= 1` at **line 19** — and root `CLAUDE.md:204`'s "prod compute for both is
  stopped" would look like a clean BLOCKER with two wrong anchors. Read at the refs the corpus names,
  **both claims verify exactly** (`storage 63bffc8` `:38 = 0`; `messenger a0ec933` `:29 = 0`). I
  booked nothing. Any seat that grades those two rows against the checkout will book a false blocker.
