# Seat G — reading #16 (adversarial diff seat)

Scope: `git diff f375738..HEAD -- corpus/ CLAUDE.md .claude/` — the repair pass
`328ece5 iter(M257x/81): repair the 11 predicates — 33 files, 7 parallel seats, all 5 corpus guards GREEN`
(authored Wed Aug 5 13:10:22; base `f375738` Wed Aug 5 11:58:45). Read top-to-bottom, in full.

## Positive controls

| control | value |
|---|---|
| `git diff --stat` totals | **33 files changed, 410 insertions(+), 251 deletions(-)** — matches the brief exactly |
| raw diff line count (`wc -l`) | **1799** |
| commits in range | exactly 1 (`328ece5`) |

Live positive control on the search/tooling pipeline (a pattern I knew must match, run in the same pass):
`git grep -n STORAGE_RPC_ADDR b948604 -- '*.go'` → **15 hits** (non-empty ⇒ the grep pipeline works), and the
same query at `9d00a313` → **3 hits**. Both `grep -c` invocations that returned 0 exited 1 and I read the
stderr/exit code rather than treating the empty output as evidence (noted inline below where it mattered:
`STORAGE_RPC_ADDR` in the platform env files, `--include=*.go` zsh-glob rejection).

Second live control — I executed the compose selections rather than reasoning about them:

| command (in `stack-demo/platform`) | services returned | exit |
|---|---|---|
| `docker compose --profile core config --services` | postgresql, redis, sentinel, backend, gotenberg (**5**) | 0 |
| `docker compose --profile graphql config --services` | postgresql, redis, sentinel (**3**) | **0** |
| `docker compose --profile storage-legacy config --services` | redis, postgresql, sentinel, **storage** | 0 |
| `docker compose --profile frontend config --services` | *"service `next-web-app` depends on undefined service `backend`: invalid compose project"* | **1** |
| `docker compose --profile studio-desk config --services` | same shape, `studio-desk` | **1** |
| `docker compose --profile messenger config --services` | (rejected) | **1** |

Whole files / trees opened beyond the diff:

| file | `wc -l` |
|---|---|
| `stack-demo/platform/docker-compose.yml` | 271 |
| `stack-demo/platform/common.yml` | 36 |
| `stack-demo/platform/repos.yml` | 31 |
| `stack-demo/app/main.go` @ `9d00a313` | 1569 |
| `stack-demo/app/internal/askengine/registry.go` | 686 |
| `stack-demo/storage/main.go` | 18 |
| `.agentspace/rosetta-extensions/clerkenstein/alignment/scripts/gate.sh` | 87 |
| `.agentspace/rosetta-extensions/demo-stack/ant-academy.sh` | 717 |
| `corpus/architecture/external_services.md` | 842 |
| `corpus/services/ai-readiness.md` | 651 |
| `corpus/architecture/service_taxonomy.md` | 459 |
| `corpus/services/jobsimulation.md` | 273 |
| `corpus/services/roadrunner.md` | 199 |
| `corpus/architecture/platform-migration-status.md` | 263 |
| `corpus/architecture/dependency_map.md` | 103 |
| `corpus/services/cms.md` | 270 |
| `corpus/ops/platform-alignment.md` (§5 read) | 1726 |

---

## Blockers

### B1 — The repair tells a reader `storage` "would name nothing startable" — it is a declared, selectable, startable compose service

- **Anchor:** `corpus/architecture/external_services.md:821-823` — the new troubleshooting block:
  > ```
  > # Check backend's own dependencies are up (docker-compose.yml:92-99 — redis, postgresql, sentinel;
  > # `cms`, `jobsimulation` and `storage` are NOT among them and would name nothing startable)
  > docker compose ps postgresql redis sentinel
  > ```
- **Why it is false:** the clause is correct for `cms` and `jobsimulation` (both deleted from compose by
  `d11a403`) and **false for `storage`**, which is still a fully declared service in the same file the
  sentence cites: `stack-demo/platform/docker-compose.yml:102` (`storage:` block, ports 8300/8301,
  build context `../storage`), moved to `profiles: [storage-legacy]` at `:134`, under compose's own comment
  at `:131-133` that reads *"Kept startable for rollback comparison: `docker compose --profile
  storage-legacy up storage`"*. Measured live: `docker compose --profile storage-legacy config --services`
  → `redis / postgresql / sentinel / storage`, exit 0. `docker compose ps storage` names a real service.
- **It also self-contradicts four passages of the same repair pass**, which all insist storage remains
  startable:
  - `corpus/architecture/platform-migration-status.md:76` (storage row, changed line): *"`repos.yml:18-20`
    still clones the repo and **the standalone stays startable** — the rollback path"*
  - `corpus/architecture/architecture_overview.md` (changed): *"Off by default (opt-in via Docker profile):
    **Storage** — `profiles: [storage-legacy]`"*
  - `corpus/architecture/service_taxonomy.md:76` — the Storage row under *"Available, but NOT in the default
    `core` selection"*, profile `storage-legacy`
  - `corpus/services/storage.md:24` — *"the `storage` service moved to `profiles: [storage-legacy]` … so a
    default bring-up never starts it"*
  This is exactly the reverse error `platform-alignment.md` §5 warns about (*"merged-in-production is not
  removed-from-compose"*): the repair corrected the cms/jobsimulation half and over-swept storage with it.
- **Confidence:** high.

### B2 — The rext citations in the "five usage KPIs" bullet are stale, and its claim about the rext source is refuted at ground truth

- **Anchor:** `corpus/services/ai-readiness.md:213-220` —
  > "the set `aiReadinessUsageKPIs` returns at
  > `stack-seeding/seeders/ai_readiness_interview_report.go:181-185`, whose own comment at `:145` reads *"All
  > five are always emitted here"* … **The rext source's own prose at `:137` and `:139` still says *"four"***
  > — stale on that side too, and out of this corpus's edit scope."
- **Why it is false:** the passage names **no ref**, so it grades against the checkout — and the checkout
  named as ground truth is `.agentspace/rosetta-extensions` `main` = `4e6b64d` *"fix(stack-seeding): the
  usage-KPI comments said FOUR; the function emits FIVE"*. At that ref:
  - `ai_readiness_interview_report.go:137` reads `// --- the five "How they use AI — at a glance" tiles ---`
  - `:139` reads `// aiReadinessUsageKPIs derives the five usage-dimension KPIs …`
    → the *"still says four"* claim is **refuted**, in both anchors it names.
  - the five ids are returned at **`:185-189`**, not `:181-185` (`:181-183` is a comment, `:184` is
    `return []map[string]any{`).
  - *"All five are always emitted here"* sits at **`:149`**, not `:145` (`:145` is *"note. They are not the
    current tile labels…"*).
  Every anchor is off by exactly +4. I confirmed the passage was **true at authoring time**: at the previous
  rext commit `584f1fe` the anchors resolve exactly as written (`:137`/`:139` said "four", the ids sat at
  `:181-185`, the quote at `:145`). The rext fix landed at **13:10:35**, thirteen seconds after the corpus
  commit at **13:10:22** — so the corpus published a claim that a companion commit invalidated immediately.
  The defect is the missing ref pin: §5 rule 33 makes the pin the thing that would have kept this gradeable.
- **Confidence:** medium (the claim was true when written; it is false against the ground truth I was told
  to grade against, and the passage names no ref to escape into).

### B3 — `app/main.go:643` @ `b948604` is a blank line; the skillpath wiring site is `:642`

- **Anchor:** `corpus/architecture/platform-migration-status.md:71` (the `app` row, rewritten in this diff) —
  > "the first four stood at `:581`/`:612`/`:643`/`:1043` at `b948604` v1.366.0"
- **Why it is false:** measured at `b948604`:
  - `:581` = `skillerManager := skiller.NewSkillerManager(...)` ✓
  - `:612` = `jobsimDj, err := jobsimwiring.Wire(...)` ✓
  - `:643` = **an empty line**. `grep -n 'skillpath.NewSessionManager' main.go` → **`642`**.
  - `:1043` = `cmsManagers = appcms.Wire(appcms.Deps{` ✓
  The same quadruple for `5ba17044` in the same sentence (`:573`/`:604`/`:634`/`:1034`) is **exactly right**
  (verified all four), which is what makes the `b948604` member look checked.
- **Confidence:** high on the fact; I hesitated on the grade because it is an off-by-one inside a
  *historical* parenthetical whose load-bearing half (the four `9d00a313` sites `:637`/`:668`/`:698`/`:1099`)
  I verified as correct. Booked BLOCKER per "a line anchor that points at the wrong construct", since a
  reader re-deriving the row lands on whitespace and cannot tell whether the row or the ref moved.

### B4 — "every Go service repo the platform has ever cloned … 6 of 7" is a false universal, and contradicts the corpus's own archived-services roster

- **Anchor:** `corpus/architecture/shared_libraries.md:181` (taxonomy row, rewritten) —
  > "Counted over **every** Go service repo the platform has ever cloned, that is **6 of 7** (app, sentinel,
  > storage, messenger, cms, jobsimulation, roadrunner) — the sole exception is `roadrunner`"
- **Why it is false:** the platform has cloned strictly more than seven Go service repos, as this same corpus
  states in the files this diff also touched: `chronos` (removed from compose + `repos.yml` at platform
  `045857c` — `corpus/services/chronos.md:6-8`), `intelligence` (`fdfa189`), `skiller` (merged July 2026),
  `skillpath` (M502→M507) are all Go services that were in `repos.yml`. `architecture_overview.md`'s
  *Archived / merged* table (also edited by this pass) enumerates them. So "every Go service repo the
  platform has ever cloned" is a set of ≥11, not 7, and the 6/7 fraction is computed over a different set
  than the one the sentence names. The **values** are right (roadrunner is the only one of those seven with
  no `taxonomy` require — verified: `roadrunner/go.mod` requires only `colony v0.34.3` and
  `proto v1.196.0`); the **predicate** the count ranges over is wrong. §5's own rule — *"when a correction
  enumerates, re-derive the ENUMERATION"* — is the one this trips.
- **Confidence:** medium. I hesitated because the sentence enumerates its seven inline, so a careful reader
  can see the real set; booked BLOCKER because the quantifier is newly introduced by this repair (the prior
  text said "the 7 **live** Go service repos") and it contradicts sibling corpus pages.

---

## Minors

- `corpus/architecture/platform-migration-status.md:74` (roadrunner row, new text): *"`roadrunner/terraform/main.tf:19` remains `service_desired_count = 1` — **last changed at `e45eb61`** (2026-05-27)"* — `e45eb61` is the last commit that touched the **file**, and it changed **line 11** (`source = …infrastructure.git//modules/services/base_internal_service`), not line 19. Line 19's value dates to the file's creation (`84a4b4f`, Dec 2025 / renamed at `ae622e6`). Verified with `git log --oneline -- terraform/main.tf` (3 commits) and `git show e45eb61 -- terraform/main.tf`.
- `corpus/services/roadrunner.md:13-14` — twin drift left by a one-sided repair: this file still says the terraform *"has not been touched since `87d8d44` (2026-06-19)"* while the repaired twin at `platform-migration-status.md:74` now says *"last changed at `e45eb61` (2026-05-27)"*. Both point at `= 1`, but a reader comparing them gets two provenance stories.
- `corpus/architecture/platform-migration-status.md:76` (storage row): the `b948604` breakdown says the 8 non-lookup hits are *"two doc comments, two error strings and **four `t.Setenv` calls**"*. Measured: there are **3** `t.Setenv` (`cmd/academy-asset-upload/main_test.go:41`, `:52`, `:64`); the fourth test-file hit is `:47`, a `t.Fatal("expected error when STORAGE_RPC_ADDR is unset")` — a third string, not a fourth `t.Setenv`. The headline totals (**15** hits, **7** env lookups, 6 `os.Getenv` + 1 lowercase `getenv`) are all exactly right.
- `corpus/services/ant-academy.md:194-197` — the five patch **names** are listed in a different order than the five line anchors: `:56` = `ant-academy-dev-origins`, `:66` = `academy-fs-published-fallback`, `:75` = `academy-fs-published-chapter-body`, `:84` = `academy-fs-published-public`, `:90` = `ant-academy-back-to-cockpit`. A reader pairing them positionally mis-maps every one. (The **set** of five, and that all five revert on `--stop` — lines 210/215/221/226/231 — is correct.)
- `corpus/services/messenger.md:81` — *"messenger still constructs **four** Connect-RPC clients"*: it constructs **five** client objects (`messenger/cmd/root.go:118` cms, `:123` users, `:128` organizations, `:133` skiller, `:138` jobsimulation) over **four** distinct address env vars. The four-addresses claim is right; "four clients" is not.
- `corpus/services/jobsimulation.md:79` — *"consistent with the **Data** bullet, `:31-38` above"*: the Data bullet is `:32-38`; `:31` is the tail of the *Domain / engine* bullet.
- `corpus/architecture/external_services.md:175` — *"What survives is the *production* rollback module (teardown is M810) and a frozen repo on disk"*, applied to all three of cms / jobsimulation / roadrunner. For **roadrunner** the prod module is at `service_desired_count = 1` (`roadrunner/terraform/main.tf:19`) and the corpus elsewhere insists it is *orphaned*, not a rollback path, and that M810 is the **cms/jobsimulation** teardown.
- `corpus/services/roadrunner.md:168` — *"every other row is a bare `os.Getenv` with **no fallback at all**"*. True for `SENTRY_DSN`, `REDIS_ADDR`, `REDIS_STREAMS_INDEX`, `REDIS_WORKER_INDEX`, `JUDGE0_API_KEY`, `JUDGE0_BASE_URL` (verified: `grep -rn Getenv --include='*.go'` over the repo). **`ENVIRONMENT` is read by no `os.Getenv` in the roadrunner repo at all** — it reaches the binary through `colony`, so "a bare `os.Getenv`" is the wrong mechanism for that one row.
- (Outside the diff, observed while checking a re-pointed reference) `corpus/services/ai-readiness.md:542` — the heading reads *"three ways a readiness seed reads as real but is not"* while the section carries **five** numbered parts (`:548`, `:570`, `:581`, `:608`, `:620`). The repair's new cross-reference *"see *part 5* of § *The FILLED-ness contract* below"* is **correct** (part 5 exists at `:620` and says exactly what is claimed); the heading is the stale half.

---

## Positively cleared (audited zeros)

**ENUMERATED** — predicate re-derived from source independently, cardinality compared:

- **`repos.yml` = 6 entries** at `0dab54d`, and the exact six named (`app` `:11-14` `migrations: true`/`schema: public`, `sentinel` `:15-17`, `storage` `:18-20`, `messenger` `:21-23`, `next-web-app` `:26-28`, `studio-desk` `:29-31`), file is **31 lines**, `repos.yml:9-10` names infrastructure's `services.tf`. Every one of the ~15 places the diff re-asserts "6 entries" is right. At `2adcf71` the file has **9** (roadrunner at `:29-31`, jobsimulation at `:17-19`) — also right, wherever the corpus pins that ref.
- **compose declares 8 services / 10 effective / `core` starts 5.** Enumerated the top-level service keys: sentinel `:5`, backend `:28`, storage `:102`, customerio-sync `:136`, messenger `:156`, studio-desk `:197`, next-web-app `:228`, gotenberg `:255` = **8**; `common.yml` adds postgresql `:2` + redis `:24` = **10**. `--profile core` returns exactly 5. `sentinel`/`postgresql`/`redis` declare no `profiles:` key. The `graphql` token appears in **no** `profiles:` key and `--profile graphql` **exits 0** starting only those three. All as claimed in `service_taxonomy.md:55-63`, `external_services.md:288-297`, `architecture_overview.md`, `services/README.md`, `jobsimulation.md:43-44`, `graphql-wundergraph.md`.
- **`d11a403` deleted all three from BOTH files in one commit.** `git show d11a403 -- repos.yml` removes `- name: cms`, `- name: jobsimulation` **and** `- name: roadrunner`; `d11a403^` compose declares **11** services (incl. `jobsimulation:83`, `cms:144`, `roadrunner:281`), `d11a403` declares **8**. The repair's central corrective — *"the message says roadrunner's entry was already gone; the diff is the fact"* — is **correct**, and I read the commit message myself to confirm the mismatch.
- **`STORAGE_RPC_ADDR` at `9d00a313` = 3 hits, all comments**, at exactly `main.go:451`, `internal/jobsimwiring/wiring.go:101`, `internal/storagens/callsites_test.go:189`, with `main.go:451` reading verbatim *"the standalone service takes no traffic and STORAGE_RPC_ADDR is gone."* At `b948604` = **15** hits / **7** env lookups (main.go ×3 at `:446`/`:524`/`:992`, wiring.go `:115`, one in each of `cmd/academy-asset-upload`, `cmd/academyImport`, `cmd/import`), 6 spelled `os.Getenv` + 1 lowercase `getenv`. Also **0** occurrences across `docker-compose.yml`, `common.yml`, `.env_example`.
- **`AUTHORIZATION_ADDRESS` = exactly 1 occurrence** across `docker-compose.yml` (`:48`, backend), `common.yml` (0), `.env_example` (0) — `sentinel.md`'s "measured: 1" is right.
- **`askengine/registry.go`: 60 `TableDef` literals**, 57 `Schema: "public"` + 3 `Schema: "directus"`, zero entries lacking a `Schema` key; `:183-195` is exactly the jobsimulation transition-alias comment block ending *"`jobsimulation` stays in rewriteSchemas until M710"*. (I also read `:525-579` to confirm the skiller/skillpath aliases are **generated**, not among the 60 — so the partition the corpus states is complete.)
- **`app/studio/requirements.txt` = 9 packages, verbatim in the stated order, no `python-docx`**, and byte-identical to `cms/studio/requirements.txt` (`diff` clean). Zero `python-docx`/`import docx` hits under either tree.
- **`colony/authn` importers:** app **129** `.go` files, sentinel **0**, storage **0**, messenger **0**, cms **6**, jobsimulation **8** — every figure in `clerk-integration.md` and `shared_libraries.md`'s authn row.
- **Shared-library pins**, read from each `go.mod`: proto app/messenger `v1.210.0`, sentinel `v1.200.0`, storage `v1.196.0`; frozen cms `v1.207.0`, jobsimulation `v1.205.0`, roadrunner `v1.196.0`. ai `v1.40.2` at `app:14`, `cms/go.mod:9`, `jobsimulation/go.mod:11`. taxonomy `v1.2.0` direct in app/messenger, `// indirect` in sentinel/storage, direct-and-not-indirect at `cms/go.mod:13` + `jobsimulation/go.mod:15`, absent from roadrunner. colony app+messenger `v0.35.2`, sentinel+storage+roadrunner `v0.34.3`, cms+jobsimulation `v0.35.1` — so the "two-way, not three" split at `0dab54d` is right.
- **The five ant-academy demo-patches** exist as five distinct `apply-*.sh` scripts wired at `ant-academy.sh:56/66/75/84/90` and reverted at `:210/215/221/226/231` — the count `4 → 5` correction is right (ordering caveat in Minors).
- **The five clerkenstein runners** — `clerkenstein/alignment/cmd/` contains exactly `clerkrun`, `deployrun`, `expressrun`, `jsfapirun`, `multirun`; `multirun`'s header confirms it is a per-surface runner (the M37 multi-identity surface), so *"one runner per surface"* holds.
- **`app` reads none of the dropped RPC addresses.** `git grep` at `b948604`: `JOBSIMULATION_RPC_ADDR` **0**, `ROADRUNNER_RPC_ADDR` **0**, `CMS_RPC_ADDR` 1 (a comment at `main.go:1208`), `SKILLER_RPC_ADDR` 2 (both comments in `jobsimwiring/wiring.go`). So `jobsimulation.md`'s *"having verified zero reads in `app`"* is independently true, not merely repeated from the commit message.
- **App migrations:** `20260729133514.sql:58-61` = *"5. Drop the mirrors."*, `:62` `DROP TABLE "local_jobsimulation_sessions"`, `:63` `DROP TABLE "local_skill_path_sessions"`. Exactly **three** migrations post-date it at `9d00a313` (`20260731131307.sql`, `20260731154527_academy_chapter_progress_completed_at.sql`, `20260803143844_ai_readiness_recommendation_path.sql`) — the skillpath.md correction of "the last migration in the repo" is right, and right **at the ref it names** (only two post-date it at the checkout `b948604`, which is why the pin matters).
- **`CREATE SCHEMA`:** exactly one across the whole migration set — `20230817154747_supabase_baseline.sql:2` `CREATE SCHEMA IF NOT EXISTS auth;` — as `hiring.md` now cites.

**RE-DERIVED / anchor-verified** (opened the cited lines and read their surroundings; not an independent enumeration of a set):

- Every changed platform compose anchor at `0dab54d`: `:28` `:48` `:59` `:82` `:88-89` `:90` `:91` `:92-99` `:93` `:100` `:102` `:126-130` `:131-133` `:134` `:156` `:171-172` `:173` `:174` `:176` `:183` `:186-192` `:195` `:197` `:204` `:220` `:223-225` `:226` `:228` `:236` `:237` `:245` `:246` `:250-252` `:253` `:268`. **All resolve to the construct claimed.** The `:88-89` and `:93` comments are quoted verbatim-correct.
- Every re-anchored `@ 2adcf71` compose citation: `:93-94`, `:113`, `:119`, `:140`, `:154-155`, `:169`, `:173`, `:291-292`, `:296-302`, `:334`, `:337-341`, `:352`. All correct. `Makefile:10` reads `PROFILE ?= graphql` at `2adcf71` and `PROFILE ?= core` at `0dab54d`; `Makefile:119-120` is `up-frontend` → `--profile core --profile frontend`.
- `common.yml:2` postgresql, `:22` `start_period: 120s`, `:24` redis — the `:20 → :24` re-point is correct.
- Legacy binaries' own port defaults: `cms/cmd/root.go:77/:78`, `jobsimulation/cmd/root.go:77/:78`, `roadrunner/cmd/root.go:110` (PORT) / `:84` (RPC_PORT) — all `cmp.Or(..., "8080"/"8081")`; jobsimulation `Dockerfile`/`Dockerfile.dev` `EXPOSE 8080`. The 8090/8091, 8400/8401, 10400/10401 "compose-supplied, now historical" corrections are all sound.
- **`storage` and `messenger` terraform, at the refs the rows name.** The checkouts are older (`storage 4ce8ece` → `:19 = 1`; `messenger fa47850` → `:19 = 1`), which reads as a contradiction until you honour the pin. At `storage 63bffc8` (= `origin/main`, tag `v0.15.5`): `:38 service_desired_count = 0`, `:29-33` the ORDERING comment, `:35-37` the prevent_destroy / M903 note — all exact. At `messenger a0ec933` (= `origin/main`, `v0.42.1`): `:29 = 0`, `:27-28` the rollback-path comment. Both rows are **correct at the ref they name.**
- `messenger/cmd/root.go:120-140` genuinely reads all four addresses.
- `app` anchors at `9d00a313`: `:15` `:61` `:62` (messenger imports), `:451` `:471` `:472` `:494` `:1048` (storage), `:637` `:668` `:698` `:1099` (the four wiring sites), `:1095` (`CMS_STREAM` publisher), `:1387` `:1423` (messenger consumer group + Wire), `:1478-1484` (the stream binding). `internal/storage/service.go:22`/`:24` are the two bucket-env constants. `cmd/academyImport/main.go:239-243` carries the quoted comment and `:244-246` the empty-bucket hard error.
- `app` anchors at `b948604`: `main.go:980-982` (`DIRECTUS_BASE_ADDR` `log.Fatalf`), `:1204` (`NewJobSimulationServiceHandler`), `internal/jobsimwiring/wiring.go:115` (the `getenv("STORAGE_RPC_ADDR")` still-read site) and `:118` (Judge0 runner) — with the same runner at `:123` @ `9d00a313`. The "state the ref you mean" framing is materially correct.
- `app/internal/jobsimwiring/worker.go:24-35` is the Asynq wiring, and `:48` names *"chronos->asynq session timeout"* — the chronos.md re-point holds.
- `app/internal/cms/studio/studioManager.go:119` shells `studio/gen.py`; `app/internal/cms/worker/worker.go:29` `Concurrency: 5` — the studio-room.md "cms domain's Asynq worker inside `app`" rewrite holds.
- **Corpus-internal re-points, all verified against the current files:** `external_services.md:555` (the "no ordered EU-first fallback chain" retraction — the `:546 → :555` move in CLAUDE.md ×2, `security_compliance.md` ×2, `shared_libraries.md`, `architecture_overview.md`); `:543` (the Anthropic-Direct provider row, which does name Studio-Room and `app/studio/services/ai.py:627-664`); `:577-582` (the "Four things can send a request outside the EU" lead-in); `:594-604` (item 4, the `ai_vendor`-unset derivation — the `:578-588 → :594-604` move is exact); `:296` (the eight/ten split `service_taxonomy.md` now cites); `:522` (the "restart `backend`" correction). `service_taxonomy.md:52` (Database bullet), `:58-59` ("There is no `graphql` profile"), `:79` (the Studio-Desk row — the `:75 → :79` fix is exact). `cms.md:28-31` (Data bullet) and `:52-53` (Studio bullet). `ai-readiness.md` § *The FILLED-ness contract* **part 5** at `:620` says exactly what the two new cross-references claim.
- `.agentspace/rosetta-extensions/clerkenstein/knowledge/{kb-index,alignment,architecture}.md` all exist (authoring copy), and the pinned-tag clone carries the same tree at `stack-demo/rosetta-extensions/clerkenstein/…` — the clerkenstein.md path rewrite is right.
- `clerkenstein/alignment/scripts/gate.sh:69` is exactly `"$alignctl" dna coverage --dna "$base/$dna" --if-declared`, with the rationale comment at `:58-68` and `:61` sitting **inside** it — the `:61 → :69` correction and its explanation are precisely right. `RUNNER_PKG` defaults to `./cmd/clerkrun`, `ALIGN_DIR` to `$base/../../alignment`.
- `alignment/cmd/alignctl/dna.go:20-26` are the `list|diff|validate|coverage` switch cases — the added `coverage` verb is real.
- The two "exits 1" warnings added to `next-web-app.md` and `studio-desk.md` reproduce **compose's own error string verbatim**, and I confirmed exit code **1** for both and **0** for `graphql`.
- Backend's `depends_on` at `2adcf71` was `:70-80` and did include `cms:` (`:77-78`) and `storage:` (`:79-80`) — so `dependency_map.md`'s *"the cms and storage edges are gone as facts"* rewrite is founded.
- `docker-compose.yml:90-91` is the only bind mount in the file and the only AWS bind anywhere in compose — the jobsimulation.md "the bind moved onto `backend`" rewrite is correct, including the verbatim `:88-89` comment.

---

## What I could not settle

- **`platform-migration-status.md:110`** — *"**93 repositories** in `anthropos-work` (GitHub org API, 2026-08-01) … **46** are named by no corpus document at all."* The 93 and the 46 need a GitHub org API call plus a corpus-wide name sweep; I did not have network access to the org and did not attempt it. The **6** in the same sentence I did enumerate from the file and it is correct, and the accompanying reasoning (*"the 46 does not move, because all three are named by corpus documents"*) is sound on its face.
- **`roadrunner.md`'s M247 claim** that *"no `ROADRUNNER_RPC_ADDR` / `RoadRunnerService` / `roadrunner:10401` is read in any service's Go code"* — I verified the `app` half directly (0 hits at `b948604`) but did not re-grep every consolidated clone; the sentence is unchanged context in this diff.
- **`architecture_overview.md`'s "Two of the five are Go services of ours"** — I treated `gotenberg` (third-party image `gotenberg/gotenberg:8`), `postgresql` (bitnami-derived local build) and `redis` (`bitnamilegacy/redis`) as not-ours from the compose declarations, which supports the claim; I did not audit whether the corpus consistently classifies the custom `postgresql` build image the same way elsewhere.
- **`ENVIRONMENT`'s effective default in roadrunner** (Minor above) — I established it is read by no `os.Getenv` in the repo, but `colony` is a private module not present in the tree, so I could not determine whether colony supplies `development` as a fallback. Settling it needs the `colony` source at `v0.34.3`.
