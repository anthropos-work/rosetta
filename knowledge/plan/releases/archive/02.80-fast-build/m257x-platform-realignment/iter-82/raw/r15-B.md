# Seat B — reading #15

**Refs used (stated at the moment of measurement, §5 rule 26):**
`app` `b948604f` (v1.366.0; `origin/main` = `v1.367.0-4-g717737471`, 60 commits ahead) ·
`platform` `0dab54df` · `next-web-app` `bb3313bc` · `sentinel` `88bc5592` · `storage` `4ce8ece5` ·
`messenger` `fa47850d` · `cms` `ca50c817` · `graphql-wundergraph` `60c229f3` · `roadrunner` `87d8d443` ·
`jobsimulation` `462343b0` · `studio-desk` `14a5442a` · `ant-academy` `9c3843cd` ·
`rosetta-extensions` authoring copy `main` @ `4e6b64d3` (clean tree) ·
`stack-demo/rosetta-extensions` consumption copy @ tag `fast-build-m257x-iter-58`.

## Positive controls

| file | `wc -l` | read in full? |
|---|---|---|
| `corpus/services/ai-readiness.md` | 651 | yes |
| `corpus/architecture/ai_architecture.md` | 302 | yes |
| `corpus/services/graphql-wundergraph.md` | 272 | yes |
| `corpus/services/ai-labs.md` | 156 | yes |
| `corpus/services/coursebuilder.md` | 140 | yes |
| `corpus/services/customerio-sync.md` | 75 | yes |
| `corpus/architecture/README.md` | 38 | yes |

Protocol read first: `corpus/ops/platform-alignment.md` §5 (lines 240–1191), in full.

Search-pipeline positive controls run in the same passes as every "0" reported below:
`grep -c 'profiles:' docker-compose.yml` → 7 (while `graphql` → 0 profile hits);
`grep -ril 'anthropic' app/studio/` → 5 files (while `bedrock|boto3` → 0);
`git grep -c 'customer.created' origin/main -- '*.go'` → 3 (while `checkout.session.completed` → 0);
`grep -ln 'anthropos-work/colony' stack-demo/*/go.mod` → 7 (while `anthropos-work/authn` → 0);
`grep -c 'useFeatureFlagEnabled' useAiReadinessActive.ts` → 2 (while `flag_ai_readiness` in
`AIReadinessClient.tsx` → 0). `gh` is **not installed** in this sandbox — every GitHub-org-level claim
is listed under *What I could not settle*, not cleared.

---

## Blockers

### B1 — `graphql-wundergraph.md` says the `graphql` profile still exists and is the default; it does not, and the same file says so 170 lines later

- **Anchor:** `corpus/services/graphql-wundergraph.md:13-14` —
  > "**There is no `:5050` on a local stack.** The `graphql` *profile name* survives in compose and is now
  > simply the default profile — it no longer names a router service."

- **Why it is false:**
  - At the checkout `platform 0dab54df` the token `graphql` appears in **no `profiles:` key at all**.
    Measured: `grep -n 'graphql' docker-compose.yml common.yml` returns exactly four lines
    (`docker-compose.yml:204`, `:220`, `:236`, `:245`) and every one is a `VITE_GRAPHQL_ENDPOINT` /
    `NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT` **URL string**, not a profile. Positive control in the same pass:
    `grep -c 'profiles:' docker-compose.yml` → **7**. The seven profile lists are
    `[core, backend, all]` (`:100`), `[storage-legacy]` (`:134`), `[customerio-sync, all]` (`:154`),
    `[messenger]` (`:195`), `[studio-desk, all]` (`:226`), `[frontend, all]` (`:253`),
    `[core, backend, all]` (`:268`).
  - `platform/Makefile:10` reads `PROFILE ?= core`, not `graphql`.
  - `0dab54d`'s own commit message: *"graphql -> core. The profile was named when a WunderGraph router
    fronted several subgraphs. … Renamed across docker-compose.yml, the Makefile default and the README."*
  - **The same document contradicts it twice**: `:180-181` (*"`0dab54d` … then renamed the profile itself —
    `Makefile:10` now reads `PROFILE ?= core`"*) and `:182-183` (*"**⚠️ At `0dab54d` the `graphql` token
    appears in no `profiles:` key at all**"*). Root `CLAUDE.md` states the same correction.
  - **The `2adcf71` pin does not reach this sentence.** The ref is named in the *preceding* sentence
    ("Platform `b56d731` + `360efd4` (merged **`2adcf71`**…)"); §5 rule 33 scopes a pin to "a wrapped
    sentence in prose". It was true at `2adcf71` (measured: `git show 2adcf71:docker-compose.yml` has
    `profiles: [graphql, …]` at six sites, and `Makefile:10` read `PROFILE ?= graphql`), and false at
    `0dab54d`. Further, the table this blockquote heads is explicitly headed
    *"a fresh local stack **@ platform origin HEAD**"* and the sentence says *"is now"* — rule 33's
    currency clause: a block asserting currency cannot be pinned into silence.
  - Reader consequence is precisely §5 rule 30's: a reader who believes `graphql` is still the default
    profile runs a token that **exits 0 and starts the always-on floor only** — a stack that looks alive
    with no application in it.
- **Confidence:** high

### B2 — `ai-readiness.md` asserts in the present tense that the rext source "still says four"; at the rext witness both cited lines say **five**, and two anchors in the same bullet are off by four lines

- **Anchor:** `corpus/services/ai-readiness.md:213-220` —
  > "the **five** usage KPIs — `avg_adoption` · `avg_depth` · `avg_ownership` · `avg_transformation` ·
  > `avg_originality`, the set `aiReadinessUsageKPIs` returns at
  > `stack-seeding/seeders/ai_readiness_interview_report.go:181-185`, whose own comment at `:145` reads
  > *"All five are always emitted here"* … **The rext source's own prose at `:137` and `:139` still says
  > *"four"* — stale on that side too, and out of this corpus's edit scope.**"

- **Why it is false** (witness = `.agentspace/rosetta-extensions`, authoring copy `main` @ `4e6b64d3`,
  the ref the briefing designates for rext; §5 rule 36 — the authoring copy is the witness):
  - `stack-seeding/seeders/ai_readiness_interview_report.go:137` =
    `// --- the five "How they use AI — at a glance" tiles ---`
  - `:139` = `// aiReadinessUsageKPIs derives the five usage-dimension KPIs (0-100 cohort averages) …`
  - The repair is the **HEAD commit itself**: `4e6b64d` *"fix(stack-seeding): the usage-KPI comments said
    FOUR; the function emits FIVE"*, 2026-08-05 13:10:35 +0200.
  - Same bullet, anchor 2: the five ids are returned at **`:185-189`**, not `:181-185`
    (`:181-183` is a comment, `:184` is `return []map[string]any{`, so `:181-185` covers exactly **one**
    of the five).
  - Same bullet, anchor 3: *"All five are always emitted here"* is at **`:149`**, not `:145`.
    `:145` reads `// were the PRE-M250 vocabulary, and were stale for the same reason the ids below were:`
    — i.e. the anchor lands inside the paragraph whose neighbouring line `:143` **contains the word
    "four"**, so a reader who follows the citation is actively confirmed in the wrong reading.
- **Disclosure (I want this on the record, it is the reason for medium rather than high confidence):**
  all three claims are **TRUE** against the *pinned consumption copy*
  `stack-demo/rosetta-extensions @ fast-build-m257x-iter-58`, where `:137`/`:139` do still say "four" and
  the quoted sentence is at `:145`. The corpus block **names no ref**, and the word *"still"* plus
  *"out of this corpus's edit scope"* are standing/currency assertions (§5 rule 33), so it grades at the
  authoring copy and is false there. The whole-bullet offset is exactly **+4 lines**, the size of the
  comment the repair added.
- **Confidence:** medium (high on the measurement; medium on the adjudication between the two clones)

### B3 — Three `AIReadinessClient.tsx` anchors in `ai-readiness.md` resolve to the wrong constructs

- **Anchors:** `corpus/services/ai-readiness.md:322`, `:466`, `:467`. Measured at
  `next-web-app bb3313bc`, file
  `apps/web/src/app/(authenticated)/(verified)/ai-readiness/AIReadinessClient.tsx` (655 lines):

  | corpus claim | corpus anchor | what is actually there | true location |
  |---|---|---|---|
  | "`const SHOW_SECONDARY_TABS = false;` strips it from the tab list" | `:69` | `import { PeopleDrawer } from './_components/PeopleDrawer';` | **`:78`** — `const SHOW_SECONDARY_TABS: boolean = false;` (used at `:599`) |
  | "computes `effectiveCycleId = selectedCycle ?? activeCycle?.id ?? latestClosedCycle?.id`" | `:137-138` | `:137` blank; `:138` `const cyclesQ = useAIReadinessCycles({ enabled: featureOn });` | **`:153-154`** |
  | "gates the data GET on `cyclesQ.isFetched`" | `:150-154` | `:150-152` the `// Cycle preference:` comment; `:153-154` the `effectiveCycleId` assignment | **`:167-169`** (`const dataQ = useAIReadiness({ cycleId: effectiveCycleId, includePeople: true, enabled: featureOn && cyclesQ.isFetched })`; also `:176`) |

- **Why it matters:** `:137-138` is not an empty region — it lands on `useAIReadinessCycles`, a *different*
  hook call that a reader chasing "the data GET" would plausibly accept as the cited construct. This is
  §5 rule 21's second class verbatim ("an anchor that **resolves but names the wrong construct**").
  Sibling anchors in the *same* passage are exact, which is what makes the wrong ones dangerous:
  `AIReadinessHero.tsx:88`, `useWorkforceAIReadiness.ts:23-27`, `WorkforceNewClient.tsx:125-151`,
  `urls.ts:52`, `useNavbarSections.tsx:4/:398-400/:547`, `HowWeMeasureTab.tsx:1879/:1903/:1915/:1921/:1927`
  and `useAIReadiness.ts:274` all resolve **exactly** (verified line by line).
- **Confidence:** medium. I hesitated because this is line drift, which one grading tradition carves out as
  MINOR; I booked it because the briefing lists "wrong line anchors that point at the wrong construct"
  under BLOCKER, and because the `:137-138` case points at a plausible-but-wrong construct rather than at
  whitespace.

---

## Minors

- `graphql-wundergraph.md:81-82` — the self-citation *"`:174-176` of this same doc already said
  `localhost:5050` refuses the connection"* resolves to a blank line + the `## Local Development`
  heading; the claim now sits at `:185`. (Intra-corpus line drift, §5 rule 34.)
- `ai_architecture.md:232` — "the enum is `…/jobsimulation.go:983-990`" under-covers the `AIModel` const
  block, which is `:979-991` with its 11 members at `:980-990`; the cited range omits
  `Anthropic35SonnetAws` / `Anthropic37SonnetAws` / `Anthropic4SonnetAws`.
- `ai_architecture.md:210` — "all it does is swap the dispatched **endpoint** to `openai-hosted`":
  `internal/jobsimulation/calls/livekit.go:140-144` also resets `agentName` to `"anthropos-agent"`,
  overriding `anthropos-agent-chain` (`:115`) or `anthropos-agent-<location>` (`:126`). The doc's actual
  thesis (the flag does not select LiveKit over ElevenLabs) is correct.
- `ai_architecture.md:273` — "`calculateSkillScore` (`v3/validator/skills.go:53-64`)": the function spans
  `:53-62`; `:64` is the `func (s skillsValidator) run(...)` declaration.
- `coursebuilder.md:114-115` — "~55 test files" / "~25 boundary test files". Measured:
  `find internal/coursebuilder -name '*_test.go' | wc -l` = **61**;
  `find internal/web/backend/coursebuilder -name '*_test.go' | wc -l` = **33**.
- `customerio-sync.md:19-33` — the block headed "### Compose definition" omits the real service's
  `env_file: [.env]` and `networks: [app-network]` keys (`docker-compose.yml:136-154`).
- `ai-readiness.md:18-19` — "the `WorkforceDirectory` interface — `LoadMembers`/`LoadMembersByUserIDs`"
  names 2 of the interface's **4** methods; `BaseMembers` and `LevelsCount` are also on it
  (`app/internal/aireadiness/manager.go:38-49`).
- `ai-readiness.md:278-279` — "`aireadiness.Manager` … is constructed with `db`, `ent`, `orgRepo`, …":
  `orgRepo` is **not** a constructor parameter — `NewManager` derives it internally
  (`manager.go:95`, `:99` `orgRepo: repository.NewEntRepository(entClient).Organization`); `logger`,
  which *is* a parameter, is omitted from the list.
- `ai-readiness.md:195-197` — "reads **exactly one table**" for `computeInterviewInsightsV2`: it also
  calls `resolveSessionAuthors`, which joins `public.job_simulation_sessions` ⋈ `public.memberships` ⋈
  `skiller.job_roles` (`how_we_measure.go:1099-1107`). The doc discloses that join two table-rows later,
  so the phrase is loose rather than wrong.

---

## Positively cleared (audited zeros)

### ENUMERATED (predicate re-derived from source independently, cardinality stated before arithmetic)

- **`graphql-wundergraph.md:36-48` subgraph ladder 5 → 4 → 3 → 1.** Enumerated `- name:` entries in
  `supergraph-config-prod.yaml` at each named commit: `749dc86~1` = {backend, skiller, jobsimulation,
  cms, skillpath} = **5**; `749dc86` = **4**; `7c17e63` = **3**; `915da06` = **1**. `git show
  --name-status 915da06` marks **both** `schemas/cms.graphqls` and `schemas/jobsimulation.graphqls` `D`,
  while the commit subject reads "supergraph 2→1" — the doc's "the subject line is wrong" is correct.
- **`graphql-wundergraph.md:58-61` no `type Subscription` in `schemas/backend.graphqls`.** Enumerated:
  `^type (Query|Mutation|Subscription)` → `type Mutation` `:4053`, `type Query` `:4912`, no Subscription.
  All 6 `Subscription` substring hits are Stripe/plan names (`:1528`, `:1555`, `:4385`, `:4389`, `:4391`,
  `:4418`). `bba862f` exists only on `remotes/origin/feat/use-web-socket` (`git branch -a --contains`).
  No `supergraph-config-*.yaml` carries a `subscription:` block; at `915da06~1` it sat on the
  `jobsimulation` entry alone.
- **`ai_architecture.md:59-63` — Directus `AIVendor` has five members, the switch four cases.**
  Enumerated: `jobsimulation.go:970-974` = {Azure, Azureglobal `:971`, Openai, AnthropicAws, Anthropic};
  `simulator/ai/ai.go` cases at `:58`, `:69`, `:86`, `:102` + a `default:` at `:114`. `azureglobal` is
  the one with no case.
- **`ai_architecture.md:19-34` provider routing.** Enumerated the four vendor consts
  (`internal/jobsimulation/ai/ai.go:30-33`), `getClient` `:259-288`, the `flag_use_azure_us` read `:267`
  with the error path returning the EU client (`:270-272`), both anthropic arms → `a.anthropicClient`
  built on `config.WithRegion("eu-west-1")` (`:87`), and the sole `vendor = Openai` override guarded by
  `if throttled` (`:152-154`) fed only by `isThrottlingError` (`:166`, `:325`), which tests **only**
  `StatusTooManyRequests` (`:129-140`). Mistral: 8 hits in `app`, all `internal/cms/studio/`
  (`markdownManager.go:19`, `studioManager.go:583`) — 0 in the AI manager.
- **`ai_architecture.md:245-247` the three model defaults.** All nine anchors opened and read with
  context: `jobsimulation.go:1297` (`aiModel := simulation.Gpt5Point1`), `:1302` (`aiVendor :=
  simulation.Openai`), `:1307` (`sequences = append(…)`); `simulator/ai/ai.go:65-66`, `:82-83`,
  `:126-127` (all `openai.GPT4Dot1`), `:20-26` (`GetValidationAIVendorAndModel` → Azure / GPT4Dot1 /
  GPT4Dot1Mini), `:98-99` (Claude 3.7 Bedrock), `:110-111` (Claude 3.5), `:114-115`
  (`aiVendor = internalAi.Openai`).
- **`ai_architecture.md:264-268` `checkerEngines` is stored and never read.** Enumerated **every**
  reference repo-wide: `validator.go:43`, `:60`, `:595`; `criterion.go:34`, `:72`, `:90`. Zero indexing
  reads (`grep -rn 'checkerEngines\['` → empty). The live dispatch is the hardcoded `switch
  critCheck.Engine` at `criterion.go:126-153`.
- **`ai_architecture.md:112-133` studio slot table.** Enumerated `production_config.ini:26-36` and
  `development_config.ini:26-36` — byte-identical stable/experimental columns, all five slots matching the
  doc's table including the `low`/`medium` thinking values. `gpt-5.2` in **0** studio configs; its only
  hits are `studio/services/ai.py:356` and `:508`, both pricing entries. `config_template.ini:39`/`:40`
  are `azure, gpt-4o, none` as claimed. `grep -rin 'bedrock|boto3' app/studio/` → **0** (positive control
  `anthropic` → 5 files).
- **`ai_architecture.md:281-282` no 60/65/75/85/95 ladder.** `calculateCompetencyLevelScore`
  (`skills.go:40-51`) is exactly `20` when `<60 && isPassed`, `100` at `>=100`, else
  `max(0, score*2-100)`, carrying `// TODO fix this formula` at `:49`. `convertLevelTo100`
  (`skill.go:617-623`) is a plain division; FE `getSkillScoreForSimulation` likewise.
- **`ai-labs.md:12-18` the "no shared purse" claim, measured at `origin/main` as the doc says.**
  `git grep 'checkout.session.completed' origin/main -- '*.go'` → **0** (positive control
  `customer.created` → 3 hits in `api.go`). No `internal/credits` import anywhere under `internal/labs/`.
  `/credits/purchase` removed — `internal/web/backend/credits/handler.go:12` records the Wave-13 removal
  and only `GET /credits/balance` + `GET /credits/transactions` remain. `app` @ `5ba17044` is exactly
  `v1.363.2` (`git tag --points-at`).
- **`ai-labs.md:71-83` credit-ledger constants.** Enumerated `creditCost` (`cost.go:86-90`) = build 5 /
  refine 1 / translate 1; `purchasePackages` (`:200-204`) = 50/200/500; `MarginMarkup = 1.40` (`:133`);
  `PricePerCreditUSD = 0.45` (`:142`); `DefaultSeedBalance = 500` (`:231`); `TestPricingDerivation`
  (`cost_test.go:103`). Stripe webhook switch enumerated: `customer.created` + the three
  `customer.subscription.*` cases only (`api/api.go:321-…`).
- **`ai-labs.md` structural claims.** All five migrations exist by exact name; `lab_session.go:21-28`
  states the deliberate `PrimaryKeyMixin` omission and `:36` the `field.String("id")`; `lab_body.go:49`
  gives `de/es/fr/it/nl/pt` = EN base + **6** overlays; `LabSessionService` is wrapped in
  `authn.HTTPAuthnMiddleware` at `main.go:1228`; labs-api default `:7070` (`labsapi/client.go:6`);
  `LabsTier` = essential/professional/frontier (`labs_budget.go:26-28`); the five `CapReason`s
  (`:247-251`); reconciler `Interval: 30 * time.Second` (`spend_reconciler.go:60`); `labsdemo` `:8099`
  (`cmd/labsdemo/main.go:115`); `/v1/labs` group + `labs:write` + `workspace.tar.gz`
  (`labs_admin.go:31`, `:40`); `stripe-go/v74 v74.30.0` (`go.mod:62`); `internal/payments` has **no**
  `_test.go`.
- **`coursebuilder.md` LLM-backend + env claims.** `bedrock.go:98-103` `ModelBackendName`, `:105-114`
  `newUnderlyingClient`; `main.go:770` the boot log, `:774`/`:778` the two `routes disabled` warns,
  `:766-769` the tail of the comment; `main.go:824-826` reads `OPENAI_KEY` (and
  `COURSEBUILDER_OPENAI_IMAGE_KEY` survives **only** in `internal/coursebuilder/README.md:429` +
  `GO-LIVE-RUNBOOK.md:35`, exactly as claimed); `variables.tf:635-638` sensitive/no-default,
  `ssm.tf:328-…`, `main.tf:555`; `DefaultAuthorModelID`/`DefaultGraderModelID`/`defaultModel` =
  `eu.anthropic.claude-opus-4-8` / `eu.anthropic.claude-sonnet-4-6` / `gpt-image-2`;
  `DefaultMaxMonthlyCOGSUSD = 500.0`, `DefaultMaxDailyCOGSUSD = 0.0`,
  `DefaultSessionsPerOrgPerDay = 50`; Go **1.26.4**; migration `20260717151144.sql` creates all three
  tables (`:2`, `:40`, `:56`); worker `coursebuilderWorkerConcurrency = 20` (`main.go:173`),
  `TypeCoursebuilderRun`, `Retention(6*time.Hour)` (`run_task.go:204`); translate locale allow-list
  enumerated = {it, fr, de, es, nl, ja, pt} = **7**; every named route exists in `handler.go:Register`.
- **`ai-readiness.md` scoring + data model.** `archetypeHighBand = 75` / `archetypeLowCeil = 50`
  (`scoring.go:26`, `:30`); `classifyArchetype` `:46-57` exactly as documented including the tie→Explorer;
  bands None 0-24 / Low 25-50 / Medium 51-74 / High 75-100 (`bucketKnowledge` `:67-78`);
  `knowledge = (step1+step2)/70×100` (`:92-95`), `usage = step3×100/30` (`:101-106`);
  **enumerated** `defaultReadinessSkills` = 19 core @1.0 (`defaults.go:31-49`) + 12 enabling @0.5
  (`:51-62`) = **31**, denominator 19 + 6 = **25.0**; `defaultReadinessSims` = **3**
  (`:76-78`, tech / business / interview-both). All **13** `ai_readiness_*` tables enumerated from
  `internal/data/ent/schema/` and cross-checked against `CREATE TABLE "ai_readiness*"` in the migrations —
  the doc's list is exactly the set, plurals included. CSV columns **enumerated = 15**
  (`csv.go:18-32`); the app's own doc-comment at `csv.go:35` still says "19-column" — the corpus is right
  and the source comment is stale. `AIReadinessCompareResponse` = 6 sections + 2 cycle refs
  (`compare.go:32-41`). `interaction` `action_type` validator is **exactly** `case "email", "call"`
  (`ent/interaction/interaction.go:92-98`), and `interaction_source_target_must_be_different_check
  CHECK (source_id <> target_id)` is at `20260722081626_jobsim_data_model.sql:94` with both FKs to
  `actors`.
- **`ai-readiness.md` read-path anchors — every one opened with context.** `readiness.go:139`
  (`computeTier1`), `:289` / `:290-297` / `:307-312` / `:314` (`GetAIReadinessWithOptions`'s two frozen
  routes and the live fall-through), `:330` (`computeOrgBreakdowns`), `:388` / `:390` (the two filters),
  `:638-652` (`queryInCycleStep1Completers`, `step_type = skill_mapping` AND `status = completed` AND
  `completed_at >= since`), `:662-664` / `:684`; `steps.go:907-914` (the quoted *"This DB signal is the
  only real 'has started' check"* is at `:913`), `:915-938` (`queryReadinessStarters` —
  `SELECT DISTINCT user_id FROM public.ai_readiness_user_step_progresses … AND status <> 'not_started'`,
  no evidence read); `queryUserAISkills` selects exactly `user_id, skill_id, is_verified`
  (`readiness.go:88`); `how_we_measure.go:285-287` is verbatim
  `FROM public.interactions i / JOIN public.job_simulation_sessions s …`;
  `holdsBackFromInsights` filters `strings.Contains(strings.ToLower(in.Category), "risk")` (`:994`);
  `usageDimSpecs` **enumerated = 5** ids in the documented order (`:1138-1158`).
- **`ai-readiness.md` FE gate claims.** `AIReadinessClient.tsx` contains **0** `posthog` and **0**
  `flag_ai_readiness` (both `grep -c`). `AI_READINESS_FLAG` has exactly **one** consumer repo-wide
  (`useAiReadinessActive.ts:22`; the constant at `aiReadiness.constants.ts:26`), and `AI_READINESS_URL`
  appears in exactly the two non-`node_modules` files the doc names. `dae0fb2f7` deletes
  `AIReadinessContainer.tsx` (103) + `AIReadinessIntro.tsx` (220) + `AIReadinessView.tsx` (330) =
  **653** — the doc's "−653 lines" is the three-file sum, and the route directory
  `enterprise/workforce/ai-readiness` no longer exists. `e2e/specs/web.ai-readiness.spec.ts` is the only
  readiness reference under `e2e/`.
- **`ai-readiness.md` rext-seeder claims.** `aiReadinessStageFor` (`ai_readiness_funnel.go:205-216`):
  `not_started` → 0 checked **before** manager → 0, struggling → 1, default → 3;
  `IsAIReadinessNotStarted()` has exactly **one** non-test caller (`:208`); the enum is validated
  (`blueprint.go:618 validateEnum`) with the "fails the seed loudly" rationale at `:616-617`;
  `aiReadinessStartedHeroSkills = 9` with the source's own `round(9.0/25.0*30) = 11/30` derivation;
  population bands `stage 3: 19…31 / stage 2: 3…19 / stage 1: 1…3` (`:506-513`, via
  `aiReadinessCoreSkills`/`poolLen`); the config seeder's skill list **enumerated = 31** and identical to
  the platform's `defaults.go` node-ids in the same order (note: `grep -c 'K-'` returns 32 — the 32nd is
  `FK-enforced` at `:128`, which is exactly rule 24's trap); the three sim slugs; the demopatch manifest
  `app-aireadiness-snapshot-loadmembers.yaml:33` (`v2.7 M254 RE-POINT`) and `:42`
  (`path: internal/aireadiness/readiness.go`); and all seven named rext files/tests exist.
- **`architecture/README.md`.** All ten linked docs exist; `<a id="taxonomy-figures"></a>` exists at
  `shared_libraries.md:196`; `rosetta-extensions/alignment/`, `stack-seeding/dna/`,
  `stack-core/platform_alignment_guard.py` and `platform_predicate_guard.py` all exist. The `authn` claim
  is enumerable and true: `anthropos-work/authn` appears in **no** checked-out `go.mod`/`go.sum` across
  all 12 clones (positive control: `anthropos-work/colony` in **7** `go.mod`s), and `colony/authn` is the
  imported form.
- **`customerio-sync.md`.** `profiles: [customerio-sync, all]` is at `docker-compose.yml:154` exactly as
  cited; port 8080:8080; `search_path=public`; build context is the `git@github.com:…#main` URL; the repo
  is absent from `repos.yml` (which now lists exactly six: app, sentinel, storage, messenger,
  next-web-app, studio-desk), so `make init` genuinely does not clone it.
- **`graphql-wundergraph.md` repo facts.** router image `0.275.0`, `wgc@0.104.0`, `node:22.11-alpine`,
  `federation_version: =2.3.2`, `listen_addr 0.0.0.0:8080`, `graphql_path /graphql`, 35MB body,
  playground/introspection ON in `config.compose.yaml` and OFF in `config.prod.yaml`;
  `subgraphs.conf` = one `BACKEND=v1.360.0`; `ci/update-subgraph.sh:9` = exactly one
  `gh release download … -R anthropos-work/app`; `terraform/main.tf:20 service_desired_count = 1`;
  `package.json` = `{"name":"graphql-wundegraph"}` and the repo `CLAUDE.md` heading carries the same
  misspelling; `-local.yaml` reference at `CLAUDE.md:85`; the `2adcf71` `repos.yml` entry was
  `type: node-npm` and was deleted at `360efd4`. The whole compose-history blockquote (`:120-128`)
  verified commit by commit: `63d285c` and `d92e84e` have no `dockerfile:` key and a `git@…` context;
  `a2a3ee6` (13:54) moves it to `../graphql-wundergraph`; `719befb` (13:59) still has no `dockerfile:`;
  `2c85211` (16:01) adds `dockerfile: Dockerfile.dev`; `67ba772` moves context to `..`; `360efd4`
  deletes the block. `grep -c 5050 docker-compose.yml` → 0.
- **`graphql-wundergraph.md:199-231` the "rejection is healthy" probe.** `gqlauthz.go:175-186` is the
  three-way exemption + the `unknown viewer: Forbidden` return; `gqlauthz_test.go:132`
  `"bare __typename is not exempt"` and `:176 TestAnonymousRejectionLogsAtWarn` both exist;
  `backend.go:315` serves `/graphql` with `playground.ApolloSandboxHandler` and `:317` serves
  `/graphql/query` with the graph handler — the path split is exactly as documented.

### RE-DERIVED (arithmetic the document itself displays, recomputed)

- `ai-readiness.md:550-562` — `round(1.0/25.0*30) = 1`, `round(0.5/25.0*30) = 1`,
  `round(9.0/25.0*30) = 11`, `25.0/25.0*30 = 30`. (The SET behind the 25.0 was enumerated separately,
  above.)
- `ai-readiness.md:348-349` — `156/199 = 78.39 %`.
- `ai_architecture.md:143` — `score = 30+40+30 = 100`; the 30/40/30 split is independently confirmed by
  `ai_readiness.graphqls`'s own `maxScore` docstring.
- `credits`: `round2(1.60/5 × 1.40) = round2(0.448) = 0.45`.

---

## What I could not settle

1. **The five LiveKit agent repositories** (`ai_architecture.md:183-189`) and **`anthropos-work/AI-Labs`**
   (`ai-labs.md:5`) and **"graphql-wundergraph ARCHIVED on GitHub, 2026-07-30"**
   (`graphql-wundergraph.md:8`). `gh` is not installed in this sandbox (`command not found: gh`) and no
   org-level listing is reachable. *Partial corroboration only:* `360efd4`'s commit body says
   *"the graphql-wundergraph repo is being ARCHIVED AND DELETED"*, and `livekit.go:102-104` names the
   `azure-eu` / `azure-eu-fr` endpoints — but neither settles repo existence or archive state.
   **Would settle it:** `gh repo list anthropos-work --json name,isArchived --limit 200`.
2. **Row counts** — `public.skill_embeddings = 42,790`, `public.job_role_embeddings = 18,919`,
   `≥22,470` job roles (`ai_architecture.md:105,138,143-146`). Database measurements; no DB reachable
   from this seat. The *schema* shape (BIGSERIAL PK, uuid FK, `extensions.vector(1536)`, IVFFLAT) is
   confirmed at `terraform/migrations/20260615130000_skiller_taxonomy.sql:51-61`, and the corpus is
   internally consistent about which figure is which.
3. **The skiller-repo migration ids `20260417103036` / `20260417120309`**
   (`ai_architecture.md:151`). The `skiller` repo is decommissioned and not among the ground-truth
   clones; neither id exists under `app/terraform/migrations/`, which is expected given the doc says
   they are *skiller* migrations. The consequent claim (tables now live in `public`, owned by `app`) **is**
   confirmed.
4. **Live/perf measurements** quoted throughout `ai-readiness.md` — 2.09 s live recompute, 24 ms frozen
   read, 180 s → 19 ms demo-patch, `validation_attempt_results` 5 → 345, "47/47 landed", "8/8 sections
   green on billion". These need a running stack; §5 rule 27 classes them as prose-under-review and the
   doc does say *"Re-measure before relying on either number"*.
5. **`ai_architecture.md:83`'s OpenAI model inventory** (GPT-5.4 … O4-mini) — sourced from the private
   `ai` Go module, which is not cloned and not in the local module cache (only `ai@v1.40.1` for the
   *seeding* module is present, and it is a different consumer). The four Anthropic model names are
   corroborated by `simulator/ai/ai.go:90-97`/`:106-109`.
6. **Which rext clone a bare `stack-seeding/...` citation should be graded against** (see B2). I graded
   at the authoring copy per the briefing's ground-truth table and §5 rule 36; a ruling that bare rext
   citations are graded at the *stack's pinned tag* would convert B2 into a non-finding. Recording it so
   the adjudication is visible rather than silent.
