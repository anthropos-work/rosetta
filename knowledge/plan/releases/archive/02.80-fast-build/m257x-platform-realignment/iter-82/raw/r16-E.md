# Seat E — reading #16

**Refs used** (re-derived at open, `git rev-parse --short=8 HEAD` per clone):
`app` `b948604f` (v1.366.0; `9d00a313` = v1.367.0 and `5ba17044` also read where a passage pins them) ·
`platform` `0dab54df` · `next-web-app` `bb3313bc` · `sentinel` `88bc5592` · `storage` `4ce8ece5`
(`63bffc8` read where pinned) · `messenger` `fa47850d` (`a0ec933` read where pinned) · `cms` `ca50c817` ·
`graphql-wundergraph` `60c229f3` · `roadrunner` `87d8d443` · `jobsimulation` `462343b0` ·
`studio-desk` `14a5442a` · `ant-academy` `9c3843cd` · `rosetta-extensions` `4e6b64d3` (authoring copy).

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/architecture/service_taxonomy.md` | 459 | yes |
| `corpus/services/hiring.md` | 425 | yes |
| `corpus/architecture/platform-migration-status.md` | 263 | yes |
| `corpus/services/storage.md` | 201 | yes |
| `corpus/services/askengine.md` | 124 | yes |
| `corpus/architecture/dependency_map.md` | 103 | yes |

Protocol read first: `corpus/ops/platform-alignment.md` (1726 lines), §5 in full.

Search-pipeline controls run in-pass (§5 rules 1/2): every `grep` below had stderr read and an exit code
checked; `STORAGE_RPC_ADDR`-in-compose returned 0 **next to** `RPC_ADDR` returning 4 in the same file;
`SET "score"` returned 0 **next to** `SET "session_id"` returning 2; `grep -in hiring` returned 0 in
`intelligence.go` **next to** `JobSimulationSession` returning 44 in the same file. A live
`docker compose --profile <p> config --services` was run for all nine profile tokens.

---

## Blockers

### B1 — `dependency_map.md` cites `storage.md:14,21`; both anchors are non-content

- **Anchor:** `corpus/architecture/dependency_map.md:19` — *"Storage **reads** neither: no
  `DB_CONNECTION`/`REDIS_ADDR` in its compose env, no redis in its `go.mod`, **which is what its own doc
  says at [`storage.md:14,21`](../services/storage.md)**."*
- **Why it is false:** `corpus/services/storage.md:14` is a bare `>` — an empty blockquote line.
  `corpus/services/storage.md:21` is `> |---|---|`, the markdown delimiter row of the two-column
  side/measured table. Neither line says anything about `DB_CONNECTION`, `REDIS_ADDR`, or `go.mod`.
  The statements that *do* support the claim are `storage.md:37` (*"Storage is stateless and owns no
  database"*) and `storage.md:44` (*"**Database**: none"*). The underlying claim is TRUE against source
  (`stack-demo/platform/docker-compose.yml:116-123` sets neither variable; `stack-demo/storage/go.mod`
  has 0 redis entries, positive control: 15 other `require` lines read) — it is the citation that is
  dead. This is §5 rule 34 exactly: a corpus repair moved the target file's line count and the
  citation into it was never re-pointed.
- **Confidence:** high

### B2 — the `backend` Redis stream's *"and nothing else"* is refuted by `messenger`, and the named in-app consumer is the wrong one

- **Anchor:** `corpus/architecture/dependency_map.md:58` — *"| `backend` | App | App (cms **domain** in
  `app`) — **and nothing else**: the `cms` husk container that used to subscribe alongside it was deleted
  from compose by `d11a403`, so **there is no second subscriber on this stream locally** |"*
- **Why it is false:** two independent ways.
  1. `stack-demo/messenger/internal/flow/flow.go:72` is
     `h.subServer.AddSubscriber("backend", sub.AddHandler(…))` — messenger subscribes to the `backend`
     stream (and to `jobsimulation` at `:105` and `cms` at `:109`). `messenger` is a locally-startable
     opt-in profile that **this same file documents** at `:21`, and the `jobsimulation` stream row at
     `:60` uses the convention *"Messenger (if running)"* for precisely this. Bring the `messenger`
     profile up and there **is** a second subscriber on `backend`.
  2. The in-app subscriber on that stream is not the cms domain. At the checkout,
     `stack-demo/app/main.go:1313` is `backendSelfSub := aiReadinessNotifier.Subscriber()` and
     `:1320` is `subServer.AddSubscriber(serviceName, backendSelfSub)`; the cms Studio handler is
     *merged onto it* at `:1318` (`cmsManagers.Studio.OrganizationMemberDeletedHandler`), conditional on
     `cmsManagers != nil`. Naming "the cms domain" as the consumer inverts primary and merged-on.
  Additionally at `app` `9d00a313` `app` itself runs messenger's **own** second subscriber server on
  that same stream (`main.go:1445`, `msgflow.StreamBackend, msgflow.StreamJobSimulation,
  msgflow.StreamCMS`), so the emphatic clause fails even with no messenger container.
- **Confidence:** medium-high (high on the source facts; medium on whether the author meant
  *"in the default `core` selection"* — but the row is not so qualified, and the sibling row two lines
  down is).

### B3 — `storage.md`'s fold banner cites `anticheat.go:34`, which at the ref it names is an unrelated import

- **Anchor:** `corpus/services/storage.md:8-9` — *"(`app/internal/jobsimulation/recording/recording.go:12`,
  `anticheat.go:34`, `app/main.go:1048` `internalstorage.NewClient(storageManager, storagens.CMS)` @ `app`
  `9d00a313` v1.367.0)"*
- **Why it is false:** the block pins itself to `app` `9d00a313`. At that ref (and at the `b948604`
  checkout) there is **no** `app/internal/jobsimulation/recording/anticheat.go` — `git ls-tree -r
  --name-only 9d00a313 internal/jobsimulation/recording/` returns exactly `chime.go` and `recording.go`.
  Read instead as the sibling package, `internal/jobsimulation/anticheat/anticheat.go:34` is
  `"github.com/sourcegraph/conc/pool"`; the storage-SDK import in that file is at `:30`
  (`storagev1 "github.com/anthropos-work/storage/sdk/storage/v1"`). `:34` was correct at the **older**
  `5ba17044` and at no ref this document names. (`recording.go:12` and `main.go:1048` both verify at
  `9d00a313`, so the neighbours make the dead one read as checked — §5 rule 33's row-boundary hazard.)
- **Confidence:** high

### B4 — `hiring.md` cites `organization/manager.go:485` for a hard-error that lives 52 lines away, in a different function

- **Anchor:** `corpus/services/hiring.md:73-74` — *"as do `organization/manager.go:448` (a forced Clerk
  membership is created with role `candidate` instead of `member`) and `:485` + `siminvitationlink.go:62`
  (both **hard-error `"organization is not hiring"`** …)"*
- **Why it is false:** at the checkout `b948604` (and unchanged at `9d00a313`):
  - `stack-demo/app/internal/organization/manager.go:485` is `}` — the closing brace of the *private*
    `forceUserToOrganizationInClerk` (declared `:446`), which contains no such error. The
    `return fmt.Errorf("organization is not hiring")` is at **`:537`**, inside the exported
    `ForceUserToOrganization` (declared `:527`), preceded by `m.logger.Error(...)` at `:536`.
    Derivation: `grep -rn "is not hiring" --include='*.go' .` → 6 hits, of which the manager.go
    error-returns are `:536`/`:537` only.
  - `:448` in the same sentence is a **blank line**; the `switch org.IsHiring` is at `:450` and
    `antRole = enum.RoleCandidate` at `:453`.
  Both anchors were correct at `5ba17044` (`:448` = `switch org.IsHiring {`, `:485` = `if !org.IsHiring {`),
  the ref the file's **top banner** names — but that banner's own parenthetical says only *"the
  **platform-side** citations below were re-anchored"*, and per `platform-alignment.md` §5 rule 33 a pin's
  scope is the claim's own block, not a banner three sections up. (`siminvitationlink.go:62` in the same
  sentence is fine — `internal/organization/siminvitationlink.go:62` is the `if !org.IsHiring {` guard,
  `:63` the error.)
- **Confidence:** high on the source facts; medium on the grade, because a reader who honours the
  top-of-file pin would call these merely stale. Booking upward per the briefing.

### B5 — `service_taxonomy.md`'s mermaid edge `Room --> Desk` reverses the generation flow the same file describes

- **Anchor:** `corpus/architecture/service_taxonomy.md:38` — `    Room --> Desk`
- **Why it is false:** the flow runs Desk → cms → Room, never Room → Desk.
  - Same file, `:188`: Studio-Room's **Input** is *"Blueprints (StudioDocuments) created in Studio-Desk
    and stored via CMS"*.
  - Same file, `:221`: *"Studio-Desk creates the design (blueprint). The cms domain in `app` (Go)
    orchestrates `StudioTask` records; the studio-room Python code runs inside the same container to
    execute generation."*
  - `corpus/architecture/dependency_map.md:86`: *"`Studio Desk` → `app` (cms domain) → (in-process)
    `Studio Room`"*.
  - Source: `stack-demo/app/internal/cms/studio/` orchestrates the Python pipeline that CI bakes into the
    image (`stack-demo/app/.github/workflows/build-production.yml:29`,
    `additional_repo: "anthropos-studio-room:studio"`). Studio-Desk holds no reference to studio-room.
  The arrow both reverses the direction and omits the cms hop. This is the miss class
  `platform-alignment.md` names explicitly (*"an arrow in a mermaid diagram … contains no drift
  vocabulary at all, and survived three audits for that reason"*).
- **Confidence:** medium — I hesitated because a diagram edge could be read loosely as "related to";
  booked because it contradicts two prose statements in its own file and one in a sibling file, which is
  the self-contradiction class §5 rule 21 says is the cheapest and least-checked.

### B6 — the map's `b948604` skillpath anchor `app/main.go:643` is a blank line

- **Anchor:** `corpus/architecture/platform-migration-status.md:71` — *"the first four stood at
  `:581`/`:612`/**`:643`**/`:1043` at `b948604` v1.366.0"*
- **Why it is false:** `git -C stack-demo/app grep -n "skillpath.NewSessionManager" b948604 -- main.go`
  → `main.go:**642**`. Line `:643` at that ref is blank. The other eleven anchors in the same cell all
  verify exactly: `:581`/`:612`/`:1043` @ `b948604`; `:573`/`:604`/`:634` @ `5ba17044`;
  `:637`/`:668`/`:698`/`:1099`/`:471`/`:1423` @ `9d00a313`.
- **Confidence:** high on the fact. I hesitated on the grade — it is a one-line drift inside a
  historical aside, and the canonical rule quoted in §5 rule 25 carves "line drift" out as MINOR — but
  this briefing resolves doubt upward, and `anchor_construct_guard`'s own record (§5 rule 34) is that the
  blank-line landing is the single case it catches, so it is the one the fence would call.

---

## Minors

- `platform-migration-status.md:76` and `storage.md:26` (twin text): *"the bucket names are constants in
  `app/internal/storage/service.go:22`, `:24`"* — those constants hold the **env-var names**
  (`EnvBucket = "STORAGE_S3_BUCKET"` at `:22`, `EnvPublicBucket = "STORAGE_S3_PUBLIC_BUCKET"` at `:24`);
  the bucket *values* come from compose (`docker-compose.yml:82`, `:83`). Both cells make the env source
  clear elsewhere, so no reader is misrouted.
- `platform-migration-status.md:76`: *"the other 8 are two doc comments, two error strings and **four**
  `t.Setenv` calls"* — the 8 are 2 doc comments (`cmd/academy-asset-upload/main.go:28`,
  `cmd/academyImport/main.go:35`), 2 error strings (`:133`, `:235`), **3** `t.Setenv`
  (`cmd/academy-asset-upload/main_test.go:41,52,64`) and **1** `t.Fatal` message (`:47`). The totals
  (15 hits, 7 env lookups, 8 others) are all exactly right; only the sub-partition is off by one.
- `platform-migration-status.md:76`: cites `docker-compose.yml:82` for *both* `STORAGE_S3_BUCKET` and
  `STORAGE_S3_PUBLIC_BUCKET`; the second is `:83`.
- `hiring.md:204`: `persona_write.go:69-71,143-167` is offered for the three `validation_*` writes; the
  actual write rows are `:92-94`, and `:143-167` spans only `sessionCols()` + `attemptResultCols()`
  (`skillResultCols()` starts `:169`). `:69-71` is a comment naming the mapping, so the claim is
  supported, loosely.
- `dependency_map.md:61`: the `cms` stream row omits Messenger as a consumer
  (`messenger/internal/flow/flow.go:109`), the same omission as B2 without the emphatic clause.

---

## Positively cleared (audited zeros)

**ENUMERATED** (predicate re-derived from source independently, cardinality compared before any
arithmetic):

- `platform-migration-status.md:59-65` — the row-set completeness claim. `git log -p --follow -- repos.yml
  | grep '^[+-] *- name:' | sort -u` → **exactly the 14 named names**, across **9** commits touching the
  file, all 14 with rows. The compose-side command returns 26 tokens (25 services + the `app-network`
  key); every one has a row or is covered by a named row (`backend`→`app`, `graphql`/`wundergraph`→
  `graphql-wundergraph`).
- `platform-migration-status.md:74` — *"one of eleven there; eight remain"*: `d11a403^` compose declares
  11 services, `d11a403` declares 8 (my grep counts 12/9 because it also matches the `app-network` key
  under `networks:`). `git show d11a403 -- repos.yml` deletes `- name: cms`, `- name: jobsimulation`,
  `- name: roadrunner` in that one commit, exactly as the row says against its own commit message.
- `service_taxonomy.md:354` — the subgraph ladder. Counted `- name:` in
  `graphql-wundergraph/supergraph-config-prod.yaml` at each commit that touched it:
  `4be9e2b`…`1912b8b` = **5** (backend, skiller, jobsimulation, cms, skillpath) → `749dc86` (2026-06-24)
  = **4** (skiller gone) → `7c17e63` (2026-07-21) = **3** (skillpath gone) → `915da06` (2026-07-29)
  = **1**. So cms-in-app really is the **3→1** step, and `915da06`'s own subject line (*"supergraph
  2→1"*) is the wrong figure the corpus warns about. `schemas/` at `915da06` holds `backend.graphqls`
  alone; `subgraphs.conf` = `BACKEND=v1.360.0`.
- `service_taxonomy.md:55-57, 430-442, 452` — the profile table, measured **live** with
  `docker compose --profile <t> config --services` on `stack-demo/platform` @ `0dab54d`:
  `core`→5 (redis, postgresql, sentinel, backend, gotenberg) · `backend`→same 5 · `all`→8 ·
  `storage-legacy`→floor+storage · `customerio-sync`→floor+customerio-sync ·
  `frontend`/`studio-desk`/`messenger`→**rc=1**, *"depends on undefined service "backend": invalid
  compose project"* · retired `graphql`→**rc=0 and the 3-service floor**. Every cell matches, including
  "eight declared / ten effective".
- `askengine.md:47-48` — `registry.go`: `Name: "` = **60**, `{`-opened defs = **60**, commented-out
  `{Name:` = **0**, `Schema: "public"` = **57**, `Schema: "directus"` = **3**. The dotted
  `jobsimulation.*` / `skiller.*` / `skillpath.*` entries do carry `Schema: "public"` with a `RealName`
  (`registry.go:196-198`, `:551-577`), so *"transition aliases that RESOLVE TO the public tables"* is the
  correct reading.
- `askengine.md:104-109` — test counts by `grep -cE '^func Test'`: `sandbox_test.go` **49**,
  `executor_test.go` **13**, `prompt_test.go` **10**, `followups_test.go` **14**. All four exact.
- `askengine.md:43-44` — *"`WrapQuery` (defends **four** documented bypass vectors)"*: the doc-comment at
  `sandbox.go:350-362` lists **four** bullets while its own prose says *"three"*. The corpus has the
  bullet count right and the source comment is the stale one.
- `hiring.md:224-227` — *"one of **four** required-and-undefaulted columns"*: enumerating the
  `CREATE TABLE "job_simulation_sessions"` DDL (`20260722104506.sql:2-27`), the NOT NULL columns carrying
  no DEFAULT are exactly `owner_id` `:6`, `sim_id` `:7`, `sim_type` `:10`, `token` `:13`. Every other
  NOT NULL column (`id`, `created_at`, `updated_at`, `status`, `completion_status`,
  `interactions_progress`, `score`, `validation_version`) carries a DEFAULT. `token`'s UNIQUE index is
  `:29`.
- `hiring.md:68-70` — the positive controls it publishes: `OrgFeatureInsights` = **8** in
  `resolver_queries.go`, `JobSimulationSession` = **44** in `intelligence.go`, `grep -in hiring` in
  `resolver_queries.go` returns only the three sim-TYPE sites (`:811`, `:843`, `:991-992`) and **0** in
  `intelligence.go`. All four numbers reproduce exactly.
- `hiring.md:396` — *"the regression enumerates **all 8** generic seeders"*: `hiring_scope.go:24-27`
  lists PersonaSeeder, JobsimSessionsSeeder, SkillpathSessionsSeeder, AssignmentsSeeder, ActivitySeeder,
  HeroActivitySeeder, FeedbackSeeder, SuccessionSeeder = **8**.
- `service_taxonomy.md:107` — *"23 run-state tables → `public`"*: `grep -cE '^CREATE TABLE'
  20260722081626_jobsim_data_model.sql` = **23**, names enumerated.
- `service_taxonomy.md:210-216` — *"`gen.py`'s parser registers exactly **nine** arguments"*:
  `stack-demo/app/studio/gen.py:484-492` registers exactly the nine listed, and
  `parse_argument`/`parse_known_args` is at `:18-19` with the unknown-merge at `:22,:26`. No
  `--template`. (The example at `:205` passes `--evaluation_skills`, which is *not* registered but is
  consumed via that same merge — `gen.py:417` names it as a blueprint key — and the citation
  `studio/CLAUDE.md:12-14` quotes the repo's own entry point verbatim. Not a defect.)
- `service_taxonomy.md:160-163` — studio-desk *"0 react/vue/angular entries in `package.json`, 0
  `.tsx`/`.jsx` in the repo"*: both measured **0**.
- `service_taxonomy.md:173` — *"Multi-language support (7 languages)"*: `studio-desk/app/public/languages/`
  holds exactly 7 flags (france, germany, italy, japan, netherlands, spain, united-states).
- `platform-migration-status.md:76` / `storage.md:23,26` — the `STORAGE_RPC_ADDR` grep pair:
  **3** hits at `9d00a313`, every one a comment, at exactly `main.go:451`,
  `internal/jobsimwiring/wiring.go:101`, `internal/storagens/callsites_test.go:189`; **15** hits at
  `b948604`, of which **7** are env lookups (`main.go` ×3 at `:446`/`:524`/`:992`, `wiring.go:115`, one
  in each of the three `cmd/` tools) and six of the seven spelled `os.Getenv` with `wiring.go` the
  lowercase-`getenv` outlier. **0** occurrences across `docker-compose.yml`, `common.yml`,
  `.env_example`, and **0** `STORAGE*` keys in `platform/.env` (291 keys read, names only).

**RE-DERIVED** (arithmetic/anchors the document displays, re-checked without independently enumerating
the underlying set):

- All 20 `docker-compose.yml` citations in `platform-migration-status.md` §2 resolve to the construct
  claimed (`:5`, `:18`, `:28`, `:82`, `:92-99`, `:93`, `:126-130`, `:131-134`, `:136-138`, `:154`,
  `:156`, `:171-176`, `:183`, `:195`, `:197`, `:223-226`, `:228`, `:245`, `:253`, `:255-256`, `:268`),
  as do `common.yml:2,22,24` and `repos.yml:9-10,11-14,15-17,18-20,21-23,26-28,29-31`.
- Terraform desired-counts at the refs each row names: `cms:39`=0, `jobsimulation:40`=0,
  `roadrunner:19`=1, `sentinel:19`=1, `app:44`=1, `graphql-wundergraph:20`=1, and — at the pinned refs —
  `storage@63bffc8:38`=0 (with `:29-33` the ordering comment and `:35-37` the prevent_destroy note) and
  `messenger@a0ec933:29`=0 (with `:27-28` the rollback note). At the *checkout* shas those last two
  resolve to unrelated lines, which is why the pins are load-bearing and correct to carry.
- Every platform sha the map names resolves with the stated date/subject: `d11a403`, `ef32d4c`,
  `0dab54d`, `2adcf71`, `236771f`, `21429b7`, `a4db680`, `045857c`, `fdfa189`, `b56d731`, `360efd4`,
  `6060315`, `a2a3ee6`, `cb6ebf5`, `ef4b449`, `1474b1f`, `84862d1`, `b43b99a`, `c17cc9a`, `467965a`,
  `8770fe6`. `a2a3ee6` does delete the `directus:` service block (present at `a2a3ee6^:383`), and
  `a2a3ee6^` really does carry `:384 image: directus/directus:10.10.1`, `:386 - 8055:8055`,
  `:409 ADMIN_PASSWORD=password` — the M257x iter-46 correction-of-the-correction holds.
- `hiring.md`'s whole read-path table (steps 1–7) and every migration/Ent/enum/gqlgen anchor:
  `20260729133514.sql:58-62`, `20260722104506.sql:2,6,7,10,12,13,29,53,79`,
  `20260722081626_jobsim_data_model.sql:336/355/376`, `20250416091037.sql:5`,
  `20230817154747_supabase_baseline.sql:2` (the only `CREATE SCHEMA` set-wide), `atlas.hcl:8`,
  `resolver_queries.go:1034/1035/1053/1080/1085/1089`, `resolver_cms_queries.go:95,99-103,210,258,295`,
  `intelligence.go:1700/1738-1751/1820/1844/1846/885-886`,
  `ent/schema/job_simulation_session.go:39,45`, `enum/jobsimulation.go:29-35,37-43`,
  `ent/jobsimulationsession.go:181-186`, `ent/schema/anticheat_result.go:24`,
  `graph.go:129546-129554` and `:129392-129400`, `jobsimulations.graphqls:14,128`,
  `askengine/registry.go:192`, plus the frontend set
  (`useGetClerkOrganization.tsx:16-18,20-21`, `useNavbarSections.tsx:459-466,460`,
  apps/web `UserStatusContext.tsx:144-149,168-172`, apps/hiring `UserStatusContext.tsx:125,144-145`,
  `template.tsx:90`, `FreeTrialContainer.tsx:29`, `simulationScoreColumn.tsx:54,95-97`,
  `insights.ts:31-82`, apps/hiring `InsightsByMembersContainer.tsx:359` = `<Drawer`) and the rext set
  (`persona_write.go:91,152-158`, `hiring_config.go:99`, `blueprint.go:453-458`,
  `hiring_scope.go`, `render-hiring-comparison.spec.ts`, `RENDER_GATE_FLOOR`).
  `find apps -type d -name '(.)*' -o -name '(..)*'` → **0**, with `@tabs` dirs as the positive control,
  so the M231 intercepting-route refutation holds.
- `service_taxonomy.md`'s ant-academy block: `next ^16.2.6` / `react ^19.2.5`, `dev --port 3077`,
  mobile `PORT=8555`, 0 serwist/workbox, `graphql/server.js:14,18`, `serverTenant.js:115-145`,
  `backendContent.js:36,102-103`, `beacon/route.js:36,41-55`, `next-scaffold.test.js:106,111`,
  `react-compiler-config.test.js:41`, `layout.jsx:132` + `academy-manifest.json` `"display":
  "standalone"`, `deploy-academy.yaml`, `academy.graphqls`, `gpt-5.2`, and the rext demo-patch
  `demo-stack/patches/academy-fs-published-fallback`. All resolve.
- `service_taxonomy.md:351` — the router-port claim: `terraform/locals.tf:8` `port = 8080`,
  `terraform/main.tf:48-49` containerPort/hostPort = `local.port`, `config.prod.yaml:5`
  `listen_addr: 0.0.0.0:8080`; and `git log -p --follow -- docker-compose.yml` shows `5050` only ever as
  the host side of `"5050:8080"` plus the two client-URL vars, all removed. **0** occurrences of `5050`
  in the current compose/Makefile/.env_example.
- `dependency_map.md` remaining rows: `docker-compose.yml:92-99`/`:93`/`:126-130`/`:173-183`/`:223-225`,
  taxonomy direct-vs-indirect (`app`+`messenger` direct, `sentinel`+`storage` `// indirect` in go.mod),
  `AI_USAGE_STREAM=AI` (`:47`), `GOTENBERG_URL` (`:57`), `POST /api/webhook/directus`
  (`backend.go:324`) + `DIRECTUS_WEBHOOK_SECRET` (`main.go:1089`), and **0** roadrunner stream
  producer/consumer.
- `storage.md`'s own body: `main.go` = 18 lines, `Dockerfile:18` = `RUN go test -v ./...`, **0**
  `*_test.go`, **0** `//go:generate`, **0** gqlgen usage, `internal/storage/storage.go:122` = the
  `if s.S3Bucket == ""` presigned-URL guard, binary defaults `8080`/`8081` (`cmd/root.go:45-46`), the
  file tree exactly as listed, and — at its own pin `2adcf71` — `docker-compose.yml:210` =
  `STORAGE_S3_PUBLIC_BUCKET` with `:324` inside the studio-desk block (block opens `:311`).

---

## What I could not settle

- **GitHub archive states and push dates.** Every *"Repo ARCHIVED <date>"* / *"not archived"* claim in
  `platform-migration-status.md` §2 and `service_taxonomy.md` (skiller 2026-07-01, skillpath 2026-07-31,
  jobsimulation 2026-07-31, graphql-wundergraph 2026-07-30, intelligence 2026-04-02, chronos *not*
  archived, cms/roadrunner *not* archived) needs the org API. Local clone HEAD dates are consistent with
  all of them (e.g. graphql-wundergraph's last commit is 2026-07-30) but consistency is not the claim.
- **`platform-migration-status.md:110-117` §3 census** — *"93 repositories … 6 in `repos.yml` … 46 named
  by no corpus document"*, and every `last push` date. The 6 I enumerated from the file and they are
  right; the 93 and the 46 need the GitHub org API.
- **Production terraform outside the per-service repos.** `dependency_map.md:27`
  (`skiller_rpc_addr = http://backend:8081`), `roadrunner`'s *"authoritative rollback declaration lives
  in infrastructure's `services.tf`"*, and `db-backup`'s row all point at an `infrastructure` repo that
  is not in the ground-truth clone set. The map itself flags this one as unresolved.
- **Snapshot-derived counts in `hiring.md`** — *"87 real `SIMULATION_TYPE_HIRING` sims"* (`:146`),
  *"0 `job_position` rows"* (`:151`), the *"393/393 rows on `billion`"* historical empiric (`:263`), and
  the M228 render numbers (`:407-409`). These need the captured snapshot / a live stack.
- **`service_taxonomy.md:122` taxonomy figures** (*"≥42,790 skills, ≥22,470 job roles, measured
  2026-06-29"*) — a public-subset DB measurement; not derivable from any clone here.
- **Not booked, flagged for the repairer:** root `CLAUDE.md` (outside my file set) says under
  *Communication Patterns* that **"backend → storage is mid-fold, not live"**, with no ref, while its own
  storage banner and `platform-migration-status.md:37,76` record the fold as COMPLETED and `mid-fold` as
  held by no row. Both are true at *different* refs (`b948604` vs `9d00a313`); the unpinned present-tense
  form is what makes them read as contradictory. §5 rule 19's corollary — a claim leaks to the edge of
  the previous repair's scope and stops there, and `CLAUDE.md` is the highest-propagation site in the
  tree.
