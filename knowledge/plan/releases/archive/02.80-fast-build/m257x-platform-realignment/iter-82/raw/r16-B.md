# Seat B — reading #16

**Refs at time of reading (rule 26).** corpus HEAD `1937e1f` (branch `m257x/platform-realignment`).
Ground truth: `stack-demo/app` `b948604f` (v1.366.0, `git tag --points-at HEAD` = `v1.366.0`),
`stack-demo/platform` `0dab54df`, `stack-demo/next-web-app` `bb3313bc`, `stack-demo/sentinel` `88bc5592`,
`stack-demo/storage` `4ce8ece5`, `stack-demo/messenger` `fa47850d`, `stack-demo/cms` `ca50c817`,
`stack-demo/graphql-wundergraph` `60c229f3`, `stack-demo/roadrunner` `87d8d443`,
`stack-demo/jobsimulation` `462343b0`, `stack-demo/studio-desk` `14a5442a`, `stack-demo/ant-academy` `9c3843cd`,
`.agentspace/rosetta-extensions` (authoring copy, `main`) `4e6b64d3`.
All shas re-verified at open; every one matches the briefing's table.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/services/ai-readiness.md` | 651 | yes |
| `corpus/architecture/ai_architecture.md` | 302 | yes |
| `corpus/services/graphql-wundergraph.md` | 272 | yes |
| `corpus/services/ai-labs.md` | 156 | yes |
| `corpus/services/coursebuilder.md` | 140 | yes |
| `corpus/services/customerio-sync.md` | 75 | yes |
| `corpus/architecture/README.md` | 38 | yes |

(Also read in full as protocol input, not as an audited file: `corpus/ops/platform-alignment.md` §5, 1726 lines total.)

Search-pipeline controls run in-pass (§5 rules 1/2/3): every `grep` below was run with stderr visible and
with a paired positive control — e.g. `grep -rn 'isAIReadinessEnabled' .` returned 0 in
`internal/aireadiness/` on the first (wrongly-scoped) attempt and 9 hits repo-wide on the second, and the
`AIReadinessClient.tsx` "0 occurrences of posthog" reading was taken alongside a `grep -c` that returns
non-zero on a pattern known present in the same file.

---

## Blockers

### B1 — `graphql-wundergraph.md` says the `graphql` profile "survives in compose and is now the default profile"; it exists in no `profiles:` key at all, and the same file says so 169 lines later

- **Anchor:** `corpus/services/graphql-wundergraph.md:13-14` —
  > "The `graphql` *profile name* survives in compose and is now simply the default profile —
  > it no longer names a router service."
- **Why it is false:**
  - Measured at `stack-demo/platform` `0dab54df` (`git log -1` = *"chore(compose): run without the standalone
    storage; rename graphql -> core"*), **ENUMERATED** every `profiles:` key in the compose project:
    `docker-compose.yml:100` `[core, backend, all]`, `:134` `[storage-legacy]`, `:154`
    `[customerio-sync, all]`, `:195` `[messenger]`, `:226` `[studio-desk, all]`, `:253` `[frontend, all]`,
    `:268` `[core, backend, all]`. `common.yml` declares **no `profiles:` key at all** (`grep -n profiles
    common.yml` → exit 1, with `grep -n '^  [a-z]' common.yml` as the positive control returning
    `postgresql`/`redis`/`app-network`). **The token `graphql` appears in no profile anywhere.**
  - `platform/Makefile:10` reads `PROFILE ?= core`, not `?= graphql`.
  - **The same document contradicts this at `:182-183`**: *"⚠️ At `0dab54d` the `graphql` token appears in
    no `profiles:` key at all, so asking for it does not error: compose exits 0 and starts only the
    always-on floor."* Root `CLAUDE.md` states the same. So one file asserts both *"the `graphql` profile
    survives and is the default"* and *"the `graphql` token appears in no `profiles:` key at all"*.
  - This is the exact defect class §5 rule 30 exists for, published in the affirmative: a reader who trusts
    `:13` will run a `graphql` selection expecting the default stack and get three containers and no
    application.
  - Ref-pin check (rule 33): the preceding sentence pins `2adcf71`, but the offending sentence is its own
    wrapped sentence **and asserts currency** (*"survives"*, *"is now"*), which rule 33 holds cannot be
    pinned into silence. Graded at the checkout ref.
- **Confidence:** high

### B2 — `ai-readiness.md` claims the rext seeder's prose "still says four"; both cited lines say *five*

- **Anchor:** `corpus/services/ai-readiness.md:219-220` —
  > "(This bullet said *"four"* until M257x; … The rext source's own prose at `:137` and `:139` still says
  > *"four"* — stale on that side too, and out of this corpus's edit scope.)"
- **Why it is false:** measured in the authoring copy `.agentspace/rosetta-extensions` @ `main` `4e6b64d3`,
  file `stack-seeding/seeders/ai_readiness_interview_report.go` (430 lines):
  - `:137` = `// --- the five "How they use AI — at a glance" tiles ---`
  - `:139` = `// aiReadinessUsageKPIs derives the five usage-dimension KPIs (0-100 cohort averages) from the org's`

  Both say **five**. **ENUMERATED** the whole file with `grep -in 'four'` (stderr visible; positive control
  `grep -in 'five'` returns 8 hits): the only `four` occurrences are `:13` and `:27` — which are about the
  **four interview breakdown BLOCKS**, a different set — and `:143`, a historical note (*"The four
  band-triples this comment used to name … were the PRE-M250 vocabulary"*) that explicitly disclaims being
  a count of what is emitted. **Nothing in that file says the KPI set is four**, and the corpus instructs
  the reader that it does and that the corpus cannot fix it.
  This is a refutation that has expired in the direction §5 rule 31 warns about: the sentence's
  emphatic *"stale on that side too, and out of this corpus's edit scope"* reads as already-adjudicated and
  will survive readings.
- **Confidence:** high

### B3 — `ai-readiness.md` attributes a quote to `:145` of the rext seeder; that sentence is at `:149`

- **Anchor:** `corpus/services/ai-readiness.md:215-216` —
  > "… the set `aiReadinessUsageKPIs` returns at
  > `stack-seeding/seeders/ai_readiness_interview_report.go:181-185`, whose own comment at `:145` reads
  > *"All five are always emitted here"* …"
- **Why it is false:** in `.agentspace/rosetta-extensions/stack-seeding/seeders/ai_readiness_interview_report.go`
  @ `4e6b64d3`:
  - `:145` = `// were the PRE-M250 vocabulary, and were stale for the same reason the ids below were: see the M250`
  - `:149` = `// why the tile row rendered nothing at all. All five are always emitted here.`

  The anchor resolves to a line that says something else entirely — and, worse, `:145` is part of the very
  *historical* comment that B2's false "still says four" reading was drawn from, so a reader who follows
  `:145` lands on the sentence most likely to reinforce the wrong conclusion. Verified with
  `sed -n '137p;139p;145p;149p'`.
- **Confidence:** high

### B4 — `ai-readiness.md` cites `AIReadinessClient.tsx:69` for `SHOW_SECONDARY_TABS`; `:69` is an import, the constant is at `:78` and has a different declaration

- **Anchor:** `corpus/services/ai-readiness.md:321-322` —
  > "… **hard-gated off** — `AIReadinessClient.tsx:69` `const SHOW_SECONDARY_TABS = false;` strips it from
  > the tab list."
- **Why it is false:** in `next-web-app` `bb3313bc`,
  `apps/web/src/app/(authenticated)/(verified)/ai-readiness/AIReadinessClient.tsx` (655 lines):
  - `:69` = `import { PeopleDrawer } from './_components/PeopleDrawer';`
  - `:78` = `const SHOW_SECONDARY_TABS: boolean = false;`  (note the `: boolean` annotation, absent from the quote)

  `grep -n 'SHOW_SECONDARY_TABS'` returns exactly `:78` and `:599` (the `.filter()` predicate
  `(SHOW_SECONDARY_TABS && tab.key === 'compare')`). The *substance* of the claim is correct — the Compare
  tab is filtered out — but the anchor names the wrong construct and the quoted source text is not what the
  file contains. No ref is named in this passage, so it is graded at the checkout.
- **Confidence:** high

### B5 — the M219 "CORRECTED" blockquote's two `AIReadinessClient.tsx` anchors both name the wrong construct

- **Anchor:** `corpus/services/ai-readiness.md:465-467` —
  > "The **CURRENT** dashboard (`AIReadinessClient.tsx:137-138`) computes
  > `effectiveCycleId = selectedCycle ?? activeCycle?.id ?? latestClosedCycle?.id` and gates the data GET on
  > `cyclesQ.isFetched` (`:150-154`) …"
- **Why it is false:** measured in `next-web-app` `bb3313bc`:
  - `:137` is **blank**; `:138` = `const cyclesQ = useAIReadinessCycles({ enabled: featureOn });`.
    `effectiveCycleId` is declared at **`:153-154`** (`grep -n effectiveCycleId` → 153, 156, 165, 167, 175,
    290, 309, 374, 379, 407, 421, 428, 640, 649).
  - `:150-154` is the *cycle-preference comment plus the `effectiveCycleId` declaration* — not the
    `isFetched` gate. `grep -n isFetched` → `:164` (a comment), **`:169`** and **`:176`** (the two
    `enabled: featureOn && cyclesQ.isFetched` lines) and `:348`/`:355`.

  So the two anchors are each shifted and, read literally, the second one points at the construct the first
  one claims. Both conclusions the passage draws are true of the code; the citations are not. Booked as a
  BLOCKER rather than a MINOR because this blockquote is explicitly a **retraction** whose whole authority
  is *"here is where you can check it"*, and §5 rule 22 records that a wrong anchor inside a retraction is
  the highest-risk text in the corpus.
- **Confidence:** high

### B6 — cross-file contradiction: "recordings are stored in S3" vs "never in prod S3"; source backs the S3 side

- **Anchors (both):**
  - `corpus/architecture/ai_architecture.md:225` — "Both recordings are stored in S3 and linked to the
    simulation session."
  - `corpus/ops/demo/media-substrate-spec.md:33-34` — "**The bytes live on Bunny's CDN, never in the
    platform DB and never in prod S3** — which is why S3 read access (the old `DEF-M10-01`) is neither
    necessary nor sufficient to exhibit a recording."
- **Why it is false (the media-substrate side):** measured in `stack-demo/app` `b948604f` —
  - `internal/jobsimulation/recording/chime.go:34` `NewChimeRecordingManager(logger, bucketName, ent, region)`,
    `:38` `destinationArn: fmt.Sprintf("arn:aws:s3:::%s", bucketName)`, `:188`
    `SinkType: types.MediaPipelineSinkTypeS3Bucket`, `:264-265`
    `Type: types.ConcatenationSinkTypeS3Bucket` + `S3BucketSinkConfiguration` — the Chime media pipeline
    **writes the composited MP4 into an S3 bucket**.
  - `:324-336` reads it back: `// get media from s3` → `s3Obj, err := r.getMediaFromS3(ctx, recording.MediaPipelineID)`
    → `io.ReadAll(s3Obj.Body)`; `:341-350` `getMediaFromS3` issues `s3Client.GetObject(Bucket: r.bucketName)`.
    Only afterwards does `:361 SaveBunnyVideoId` record the Bunny id, and `:429-442` parses an **S3 event
    payload** (`rec.S3.Object.Key`, prefix `concatenated/composited-video/`).
  - LiveKit likewise: `internal/jobsimulation/calls/livekit.go:170-180`
    `Egress → RoomCompositeEgressRequest → EncodedFileOutput_S3{ S3: &livekit.S3Upload{…} }` over
    `m.s3AccessKey/s3Bucket/s3Region`.
  - Root `CLAUDE.md` independently states that `module.jobsimulation_euwest1` "still **owns the
    LiveKit/Chime recording S3 buckets** that `backend` reuses by literal name."

  `ai_architecture.md:225` is therefore the **correct** side and `media-substrate-spec.md:33-34` is refuted
  as written. The charitable reading — "the *served/rendered* bytes come from Bunny" — is a different
  claim, and the sentence is load-bearing for a safety disposition (it is used to argue an S3 grant is
  unnecessary), so the literal wording matters.
- **Confidence:** high on the contradiction; high that media-substrate-spec is the side that must move.

---

## Minors

- `corpus/services/ai-readiness.md:215` — "the set `aiReadinessUsageKPIs` returns at
  `…ai_readiness_interview_report.go:181-185`": the five ids are at **`:185-189`**; `:181-183` is a comment
  and `:184` is `return []map[string]any{`, so the cited range contains only one of the five. *(Hesitated
  toward BLOCKER — it is a wrong-construct anchor — but the range does overlap the return statement and the
  first element, so a reader lands inside the right function.)*
- `corpus/services/graphql-wundergraph.md:81` — "while `:174-176` of this same doc already said
  `localhost:5050` refuses the connection": `:174` is blank, `:175` is `## Local Development`, `:176` is
  blank. The `localhost:5050` sentence is at `:185`. (Rule-34 class: an intra-corpus citation invalidated
  by this file's own repairs.)
- `corpus/services/graphql-wundergraph.md:128` — "which is why a subgraph SDL change rebuilt the router, as
  `:84` describes": `:84` is the *"There is no application source here"* blockquote; the described mechanism
  is at `:108-111` / `:117`. Same rule-34 class.
- `corpus/services/ai-readiness.md:582-583` — "`computeCycleTotals` (`how_we_measure.go:253-261`)": the
  function signature is at `:260` and its doc comment at `:256-259`; `:253-255` are the tail of
  `computePerformanceBySkill` plus a blank. (The companion anchor `:285-287` is **exact**.)
- `corpus/architecture/ai_architecture.md:232` — "the enum is
  `app/internal/cms/directus/collections/jobsimulation.go:983-990`": the `AIModel` const block is
  `:979-991` and holds **11** members; `:983-990` covers 8 of them. All three named examples (`gpt-5`,
  `gpt-4.1`, `anthropic-45-sonnet-aws`) do fall inside the cited range.
- `corpus/architecture/ai_architecture.md:273` — "`calculateSkillScore` (`v3/validator/skills.go:53-64`)":
  the function is `:53-62`; `:63` is blank and `:64` is the next function's signature.
- `corpus/services/coursebuilder.md:84` — the Downstream bullet still lists "AWS Bedrock (Opus 4.8 + Sonnet
  4.6)" without the qualification that `:48` of the same file establishes (production is the first-party
  `api.anthropic.com` path; Bedrock is the fallback). Not false — Bedrock *is* still a downstream — but the
  two bullets read as disagreeing on which path ships.

---

## Positively cleared (audited zeros)

**ENUMERATED** (predicate re-derived from source independently, cardinality compared):

1. `ai-readiness.md:253` / `:356` — "31 readiness skills = 19 core @ 1.0 + 12 enabling @ 0.5". Counted the
   literal entries of `defaultReadinessSkills` in `app/internal/aireadiness/defaults.go:29-62`: 19 entries
   at weight `1.0`, 12 at `0.5`, 31 total. No commented-out members in the block (rule 24). **Correct.**
2. `ai-readiness.md:156` / `:361` — "`defaultReadinessSims` is 3 entries: `{simulation,"tech"}`,
   `{simulation,"business"}`, `{interview,"both"}`". `defaults.go:75-79` holds exactly those three.
   **Correct.**
3. `ai-readiness.md:276` — "CSV export is now 15 columns". Counted `AIReadinessCSVColumns`
   (`aireadiness/csv.go:17-33`): 15 string literals. The source comment at `:13-16` independently confirms
   the 19→15 trim. **Correct.** (Note the source's *own* `:35` doc comment still says "19-column CSV" —
   stale on the platform side, and the corpus is right.)
4. `ai-readiness.md:201` / `:622-624` — the five `catalog_kpis[]` ids. Counted `usageDimSpecs`
   (`aireadiness/how_we_measure.go:1138-1157`): exactly 5 specs, ids `avg_adoption`, `avg_transformation`,
   `avg_originality`, `avg_depth`, `avg_ownership`; each banded high ≥61 / mid ≥41. The corpus's "omits any
   KPI that is absent or non-numeric" matches `usageDimensionsFromReports` (`:1164-1197`). **Correct** —
   and the corpus is *ahead of* the source's own stale "the four … tiles" comment at `:1161`.
5. `ai-readiness.md:146-150` — archetype bands. `aireadiness/scoring.go:26` `archetypeHighBand = 75`, `:30`
   `archetypeLowCeil = 50`, `classifyArchetype` `:46-56` (Champion both ≥75, Standby both ≤50, tie →
   Explorer), `bucketKnowledge` `:67-79` giving None 0-24 / Low 25-50 / Medium 51-74 / High 75-100.
   **Correct in every clause.**
6. `graphql-wundergraph.md:36-48` / `:154-158` — the subgraph ladder. `git show <ref>:supergraph-config-prod.yaml`
   at four refs in `stack-demo/graphql-wundergraph` `60c229f3`:
   `749dc86^` → backend, skiller, jobsimulation, cms, skillpath (**5**);
   `749dc86` → backend, jobsimulation, cms, skillpath (**4**);
   `7c17e63` → backend, jobsimulation, cms (**3**);
   `915da06` → backend (**1**). `git show --name-status 915da06` marks **both** `schemas/cms.graphqls` and
   `schemas/jobsimulation.graphqls` `D`, and the commit subject really does read
   *"fold cms subgraph into backend (supergraph 2→1)"*. The corpus's 3→1 correction, and its warning not to
   take the count from the subject line, are **both correct**.
7. `graphql-wundergraph.md:57` — "no `supergraph-config-*.yaml` carries a `subscription:` block now":
   `grep -n subscription supergraph-config-*.yaml` → exit 1 (positive control: the same grep for `name:`
   returns hits). `bba862f` exists only on `remotes/origin/feat/use-web-socket`
   (`git branch -a --contains`). `schemas/backend.graphqls` declares `type Mutation` (`:4053`) and
   `type Query` (`:4912`) and **no** `type Subscription`; the `Subscription` substring hits are all Stripe/
   plan field names. **Correct.**
8. `ai_architecture.md:59-63` — "The Directus enum has **five** members and the switch **four** cases".
   Directus `AIVendor` (`app/internal/cms/directus/collections/jobsimulation.go:967-975`): Azure,
   Azureglobal, Openai, AnthropicAws, Anthropic = 5, with `azureglobal` at `:971`.
   `internal/jobsimulation/simulator/ai/ai.go` switch: `Openai` `:58`, `Azure` `:69`, `AnthropicAws` `:86`,
   `Anthropic` `:102`, plus `default:` `:114` = 4 cases. **Correct**, and every one of the seven line
   anchors in that section resolves exactly.
9. `ai-labs.md:109-111` — the Stripe webhook event set. `internal/web/backend/api/api.go:319-327` switches
   on exactly `"customer.created"`, `"customer.subscription.created"`, `"customer.subscription.updated"`,
   `"customer.subscription.deleted"`. Repo-wide `grep 'checkout\.session'` over non-test Go → **0 hits**.
   **Correct**, including the "no `checkout.session.completed`" negative.
10. `ai-labs.md:75-77` / `coursebuilder.md:103` — the credit cost model. `internal/credits/cost.go:86-90`
    `creditCost = {course.build: 5, course.refine: 1, course.translate: 1}`; `:200-204` `purchasePackages`
    = starter 50 / team 200 / scale 500; `:180` `MarginMarkup = 1.40`; `:186` `PricePerCreditUSD = 0.45`.
    **Correct.** (Worth noting for the next reader: the *package doc comment* at `cost.go:30` still says
    `course.refine → 5 credits per refine turn`, contradicting the map four dozen lines below it — a
    platform-side staleness, not a corpus one, and the corpus took the map.)
11. `architecture/README.md` — index completeness. `corpus/architecture/` holds exactly 10 non-README
    files; the README lists all 10 and lists nothing that does not exist. **Complete and correct.**
12. `architecture/README.md:21` / `shared_libraries` claim — "`authn` is not a dependency of any service":
    `grep -c 'anthropos-work/authn' <repo>/go.mod` = **0** for app, sentinel, storage, messenger, cms,
    jobsimulation, roadrunner; positive control `anthropos-work/colony` = 1 for each of app/sentinel/
    storage/messenger. **Correct.**
13. `ai-readiness.md:240-246` — the REST surface. `internal/web/backend/api/server.gen.go:84-114` registers
    `GET /workforce/ai-readiness`, `/compare`, `/cycles` (GET+POST), `/cycles/{cycleID}` (GET),
    `/cycles/{cycleID}/close` (POST), `/export.csv`, `/narrative` (POST), `/setup` (GET+POST),
    `/steps-completion` — every route the doc names, and no doc-named route missing. **Correct.**
14. `ai-readiness.md:590-599` — "`grep -c interviewQuestions` over that 1,989-line file returns **0**".
    `HowWeMeasureTab.tsx` is exactly 1989 lines and `grep -c interviewQuestions` = 0; the three rendered
    cells are at `:1915` / `:1921` / `:1927` under the `:1879` block with its label at `:1903`; the field
    does exist at `hooks/useAIReadiness.ts:274`. **Every clause correct, every anchor exact.**
15. `ai-readiness.md:76-81` — "`AIReadinessClient.tsx` contains no PostHog reference at all — 0 occurrences
    of `posthog` and 0 of `flag_ai_readiness`". `grep -c -i posthog` = 0, `grep -c flag_ai_readiness` = 0.
    **Correct.**
16. `graphql-wundergraph.md:79-82` — "There is no `5050` at platform HEAD — `grep -c 5050
    docker-compose.yml` returns 0." Re-run at `0dab54df`: **0**. Router config
    `listen_addr: 0.0.0.0:8080` / `graphql_path: /graphql` in both `config.compose.yaml:5,13` and
    `config.prod.yaml:5,13`; playground/introspection `true` in compose, `false` in prod. **Correct.**
17. `customerio-sync.md:14` + the compose block at `:19-33` — `docker-compose.yml:154`
    `profiles: [customerio-sync, all]`; `:136-153` matches the quoted YAML field-for-field
    (`context: git@github.com:anthropos-work/customerio-sync.git#main`, `ssh: ["default"]`,
    `VERSION: dev`, `GH_ACCESS_TOKEN: $GH_PAT`, `8080:8080`, the `DB_CONNECTION_BACKEND` DSN,
    `depends_on: postgresql: condition: service_healthy`). The doc's abridgement omits `env_file` and
    `networks` but misstates nothing. `repos.yml` (6 entries: app, sentinel, storage, messenger,
    next-web-app, studio-desk) contains no `customerio-sync`, so "not cloned by `make init`" holds.
    **Correct.**
18. `coursebuilder.md` — every platform anchor in it resolves *exactly*: `bedrock.go:98-104`
    (`ModelBackendName`), `:105-114` (`newUnderlyingClient`), `main.go:770` (the backend log line),
    `:766-769` (the comment), `:774` / `:778` (the two `routes disabled` Warn arms), `:824-826`
    (`OPENAI_KEY` + `CB_IMAGE_MODEL` into `NewOpenAIImageClient`), `terraform/variables.tf:635-638`
    (`sensitive`, no default), `ssm.tf:328-334`, `main.tf:555` (`ANTHROPIC_API_KEY` valueFrom).
    Constants: `DefaultSessionsPerOrgPerDay = 50` (`rate.go:28`), `DefaultMaxMonthlyCOGSUSD = 500.0` and
    `DefaultMaxDailyCOGSUSD = 0.0` (`budget.go:27`, `:41`), `coursebuilderWorkerConcurrency = 20`
    (`main.go:173`), `go 1.26.4`, model defaults `eu.anthropic.claude-opus-4-8` / `eu.anthropic.claude-sonnet-4-6`.
    `COURSEBUILDER_OPENAI_IMAGE_KEY` survives only in `internal/coursebuilder/README.md:429` and
    `GO-LIVE-RUNBOOK.md:35` — **zero Go references**, exactly as the doc says.
19. `ai-labs.md` — all five named migrations exist in `app/terraform/migrations/`
    (`20260529072659_add_lab_session.sql`, `20260617120000_add_lab_session_model.sql`,
    `20260617203555_add_labs_catalog.sql`, `20260626120000_add_org_subscriptions.sql`,
    `20260717151144.sql`). `LabsTier` = essential/professional/frontier (`labs_budget.go:26-28`); the five
    `CapReason` values match `:247-251`; reconciler `Interval: 30 * time.Second` (`spend_reconciler.go:60`);
    labs-api default `:7070` (`labsapi/client.go:6`); `labsdemo` `:8099` (`cmd/labsdemo/main.go:115`);
    `stripe-go/v74 v74.30.0` (`go.mod:62`); `POST /credits/purchase` removed in Wave 13
    (`internal/web/backend/credits/handler.go:12`). `app` @ `5ba17044` is tagged **`v1.363.2`**
    (`git tag --points-at`) — the pinned version string in `ai-labs.md:18` and `coursebuilder.md:131` is
    right.
20. `ai_architecture.md` §Provider Routing / §three defaults / §Evaluation — **26 of 26** platform line
    anchors resolve to the exact construct claimed, including `jobsimulation/ai/ai.go:259-289` (`getClient`,
    the full function), `:30-33` (the four vendor consts), `simulator/ai/ai.go:20-26` / `:58-59` / `:65-66`
    / `:69` / `:82-83` / `:86` / `:98-99` / `:102` / `:110-111` / `:114-115` / `:126-127`,
    `cms/directus/collections/jobsimulation.go:905` / `:971` / `:1297` / `:1302` / `:1302-1305` / `:1307` /
    `:1079-1085` / `:1594-1600`, `calls/livekit.go:131-135` + `:140-144`,
    `v3/validator/validator.go:43,60-61,595`, `basevalidator/criterion.go:168` / `:428` / `:450-475`,
    `v3/validator/skills.go:40-51` / `:75`, `skill/skill.go:617-623`,
    `next-web CompetencyReadLevel.tsx:18`, `cms/studio/markdownManager.go:19`, `studioManager.go:583`,
    `coursebuilder/bedrock.go:106-113`. The "`checkerEngines` is stored and never read" claim survives
    inspection: the map is passed to `NewCriterionValidator` (`criterion.go:72`) and stored (`:90`) and is
    **never indexed anywhere** — `validateLLM` constructs `NewLLMBulkChecker(c.logger)` fresh at `:428`.
21. `ai_architecture.md:113-132` — the studio slot table. `studio/configs/production_config.ini:26-36`
    gives FAST/STRICT `azure, gpt-5-mini, none`, EXECUTION `azure, gpt-5.4, none`, CREATIVE
    `azure, gpt-5.4, low`, REASONING `azure, gpt-5.4, medium`, with the experimental block identical;
    `development_config.ini:26-36` is byte-identical to it. `config_template.ini:37-41` still carries
    `gpt-4.1-mini` / `gpt-4.1` / `gpt-4o` / `gpt-4o` / `o3`, with `gpt-4o` at `:39` and `:40` exactly as
    claimed. `grep -rn 'gpt-5.2' studio/` → only `changelog.md`, two `knowledge/` docs and
    `services/ai.py:356,508` (pricing tables) — **no shipping config**. `grep -rin 'bedrock\|boto3'
    studio/` → **0**. **Every clause correct.**
22. `ai_architecture.md:153-169` — the embeddings DDL. `app/terraform/migrations/20260615130000_skiller_taxonomy.sql:51-61`
    and `:241-251` create `job_role_embeddings` / `skill_embeddings` with an identity bigint PK, the UUID FK,
    `extensions.vector(1536)` and an `USING IVFFLAT` index — the corpus's pseudo-DDL is faithful.
23. `ai-readiness.md:412-438` — the two membership filters, which the doc has been wrong about twice
    before. `readiness.go:387-391` really does branch `cyc != nil → keepInCycleStep1` / else
    `keepStartedMembers`; `queryInCycleStep1Completers` at **`:638-652`** demands
    `StepTypeEQ(StepSkillMapping)` AND `StatusEQ(StepCompleted)` AND `CompletedAtGTE(since)`;
    `keepInCycleStep1` at `:662-664`; `keepStartedMembers` at `:684`;
    `queryReadinessStarters` at **`steps.go:915-938`** is `SELECT DISTINCT user_id FROM
    public.ai_readiness_user_step_progresses … AND status <> 'not_started'` with **no** evidence read; the
    platform's own *"This DB signal is the only real 'has started' check"* sits at `:913`, inside the cited
    `:907-914`. Every anchor and every clause **correct** — this is the passage the protocol's rule 17/24
    history points at, and it now holds.
24. `ai-readiness.md:42-47` — the D-07 re-anchor. `demo-stack/patches/app-aireadiness-snapshot-loadmembers/app-aireadiness-snapshot-loadmembers.yaml:33`
    is the `# v2.7 M254 RE-POINT:` header line and `:42` is `path: internal/aireadiness/readiness.go`;
    `demo-stack/tests/test_aireadiness_snapshot_loadmembers_m254.py` exists. **Both anchors exact.**

**RE-DERIVED** (arithmetic the document itself displays — the weakest clearance, per rule 24; each is
listed only because the SET it ranges over was enumerated above):

- `ai-readiness.md:348` — 156 / 199 = 78.39 % ≈ "78.4%".
- `ai-readiness.md:550` — 19 × 1.0 + 12 × 0.5 = **25.0** (set enumerated at clearance 1).
- `ai-readiness.md:552`/`:561` — `round(1.0/25.0 × 30)` = 1, `round(0.5/25.0 × 30)` = 1,
  `round(9.0/25.0 × 30)` = 11, `round(25.0/25.0 × 30)` = 30. All four match the doc.
- `ai-readiness.md:568` — "held/25 × 100 = held × 4 is always integral for half-step weights": held is a
  multiple of 0.5, so held × 4 is a multiple of 2. Holds.

---

## What I could not settle

- **`ai_architecture.md:138`** — `public.skill_embeddings` = 42,790 rows and `public.job_role_embeddings` =
  18,919 rows (and the derived `≥42,790 skills / ≥22,470 job roles`). These are DB measurements; no
  database is reachable from this reading. *Settled by:* a read-only `SELECT count(*)` against the public
  subset (`organization_id IS NULL`), stated with its date and DSN class.
- **`ai_architecture.md:151`** — "skiller migrations `20260417103036` and `20260417120309`". Neither file
  exists under `app/terraform/migrations/` (no `20260417*` at all there), but the claim names them as
  **skiller-repo** migrations and `skiller` is decommissioned and not in the ground-truth clone set.
  *Settled by:* a clone of `anthropos-work/skiller` at any pre-merge sha, or the archived repo's migration
  directory listing.
- **`ai-readiness.md:587`** — "the platform's enum is exactly `{email, call}`" for `public.interactions`.
  The Ent field is `field.Enum("action_type").GoType(interactions.ActionType(""))`
  (`internal/data/ent/schema/interaction.go:29`) importing
  `github.com/anthropos-work/proto/go/domain/jobsimulation/v1/interactions`, which is a **private module
  absent from the local Go module cache**; the shipped DDL
  (`terraform/migrations/20260722081626_jobsim_data_model.sql:85`) declares a bare
  `character varying NOT NULL` with **no CHECK constraint**, so the DB cannot settle it either. (The
  companion claim — the DB-enforced `source_id <> target_id` check — **is** confirmed at
  `internal/data/ent/schema/interaction.go:50`.) *Settled by:* `go list -m`-resolving `proto` and reading
  the generated `ActionType` values, or `\dT` on a live schema if the enum is a Postgres type there.
- **`graphql-wundergraph.md:8`** ("ARCHIVED on GitHub, 2026-07-30"), **`ai-labs.md:5`**
  (`anthropos-work/AI-Labs` exists as a live repo), **`ai_architecture.md:183-189`** (five LiveKit agent
  repositories in the org). All three are assertions about GitHub org state; no network access in this
  reading. *Settled by:* `gh repo list anthropos-work` / `gh api repos/anthropos-work/<name>`.
- **`ai-readiness.md:468-479` and `:536`** — the measured latencies (frozen read "HTTP 200 in 24 ms", live
  recompute "2.09 s / 304 KB"). Live-stack measurements; the doc itself says to re-measure rather than
  re-derive from prose. No running demo stack was in scope for this reading.
