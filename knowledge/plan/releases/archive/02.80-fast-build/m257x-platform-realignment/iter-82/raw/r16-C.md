# Seat C — reading #16

**Refs at time of reading** (re-derived at open, `git rev-parse --short=8 HEAD` per clone):
`app b948604f` (v1.366.0; `origin/main` = `71773747`, 4 commits past `9d00a3131` v1.367.0) ·
`platform 0dab54df` · `next-web-app bb3313bc` · `sentinel 88bc5592` · `storage 4ce8ece5` ·
`messenger fa47850d` · `cms ca50c817` · `graphql-wundergraph 60c229f3` · `roadrunner 87d8d443` ·
`jobsimulation 462343b0` · `studio-desk 14a5442a` · `ant-academy 9c3843cd` ·
`.agentspace/rosetta-extensions 4e6b64d3` (authoring copy, `main`).

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/architecture/alignment_testing.md` | 524 | yes |
| `corpus/services/clerkenstein.md` | 370 | yes |
| `corpus/services/backend.md` | 271 | yes |
| `corpus/services/sentinel.md` | 166 | yes |
| `corpus/services/next-web-app.md` | 133 | yes |
| `corpus/services/README.md` | 81 | yes |
| `corpus/services/db-backup.md` | 31 | yes |

Search-pipeline positive controls run in-pass (§5 rule 2): `JobSimulationService` in `app` Go →
**7** (while `SkillPathSessionService` → 0); `CMS_STREAM` across clones → **10** (while
`SKILLER_STREAM` publisher → 0); `SKILLER_RPC_ADDR` across clones → **10** (while
`MESSENGER_RPC_ADDR` → 0); `content_creator` in `init_policy.sql` → **24** (while `manager` → 0).
One pipeline **did** fail silently and was caught by its control: `grep --include=*.go` unquoted
under zsh returned "no matches found" for *every* pattern; re-run quoted.

---

## Blockers

### B1 — `app` is claimed to be a PRODUCER of the `skiller` Redis Stream; it is a consumer only, and no producer exists anywhere

- **Anchor:** `corpus/services/backend.md:218` —
  > "app is **both producer and consumer** of all five application streams: `backend`, `skiller`, `skillpath`, `jobsimulation`, `cms`, plus the `AI`/`ai_usage` usage stream"

  Same claim restated in the merge banner, `corpus/services/backend.md:25` —
  > "**`app` owns the `skiller`, `skillpath`, `jobsimulation`, `cms` and `ai_usage` Redis Streams** — both producer and consumer are in-process."

  Neither block names a ref, so both grade at the checkout `b948604f`.

- **Why it is false:** I enumerated the **set** (§5 rule 24 — the predicate, not the arithmetic), not
  the count. Every `pubsub.NewPublisher` call site in `app` (non-test):

  | site | topic |
  |---|---|
  | `stack-demo/app/main.go:287` | `serviceName` (= `backend`) |
  | `stack-demo/app/main.go:637` | `SKILLPATH_STREAM` |
  | `stack-demo/app/main.go:1039` | `CMS_STREAM` |
  | `stack-demo/app/internal/jobsimwiring/wiring.go:127` | `AI_USAGE_STREAM` |
  | `stack-demo/app/internal/jobsimwiring/wiring.go:180` | `JOBSIMULATION_STREAM` |

  (`internal/deadletterqueue/dead_letter_queue.go:38` is a `redisstream.NewPublisher` for DLQ
  re-publication to `metadata["topic_poisoned"]`, not an application producer.)

  **No `SKILLER_STREAM` publisher exists.** `SKILLER_STREAM` occurs exactly **once** in `app` Go
  source — `stack-demo/app/main.go:1276`, `subServer.AddSubscriber(os.Getenv("SKILLER_STREAM"),
  roleManager.SkillerSubscriber())` — and four times in the whole ground-truth tree
  (`app/terraform/main.tf:229`, `app/main.go:1276`, `app/knowledge/skiller-domain.md:214`,
  `platform/docker-compose.yml:71`). None is a publisher. `app`'s own note at
  `app/knowledge/skiller-domain.md:214` calls it *"skiller event **subscriber**"*. Positive control
  in the same pass: `CMS_STREAM` → 10 hits.

  So 4 of the 5 application streams have `app` on both ends; `skiller` has `app` on the consuming end
  and **nothing** on the producing end (the standalone `skiller` that used to publish is
  decommissioned). A reader debugging "why does nothing arrive on the skiller stream" is sent
  looking for an in-process producer that does not exist.
- **Confidence:** high.

### B2 — `alignment_testing.md` states in the present tense that an unmeasurable express run exits rc=2 and that nothing treats it as a failure; both were fixed at M219, and the same file says so three times

- **Anchor:** `corpus/architecture/alignment_testing.md:360-366` —
  > "⚠ **This surface is DEPENDENCY-GATED, and today it is frequently UNMEASURED (corrected in M218).** The runner needs `@clerk/express` `node_modules` to build. Without them it exits **rc=2 and produces NO score** — and *nothing in the tooling treats that as a failure*. So on a box that lacks the Node modules, this gate silently contributes **nothing** … Routed forward as `TEST-M219-expressrun-dep-gate`: a missing dependency must **fail loud**."

  No ref pin anywhere in the block; present tense throughout ("today it is", "it exits", "nothing
  … treats"), and the closing "Routed forward as" reads as still-open work.
- **Why it is false:** at the authoring copy `4e6b64d3`,
  `.agentspace/rosetta-extensions/alignment/cmd/alignctl/run.go:133-136` declares
  `ExitRegressed = 2` / `ExitUnmeasurable = 3`, with the rationale comment at `:121-132`
  (*"M219 (`TEST-M219-expressrun-dep-gate`) … They used to be the SAME code (2), which is the whole
  bug"*). `unmeasurable()` at `:139-153` prints a boxed
  `UNMEASURABLE — the runner could not execute. THIS IS NOT A PASSING SCORE.` banner plus
  *"Do NOT record this run as a pass"*, and `return ExitUnmeasurable` (= 3).
  `clerkenstein/alignment/scripts/gate.sh:72-73` disables `set -e` precisely so it can *"take the
  code explicitly and re-raise it with a verdict."*

  It also contradicts **three other passages in the same file** — `:239-241`
  (*"2 = REGRESSED · 3 = UNMEASURABLE … 2 and 3 were the SAME code until M219"*), `:260`
  (*"rc=**3**, **no score** (was rc=2 … split at M219)"*), `:282-286` (*"✅ **RESOLVED M219**"*) —
  and the sibling doc `corpus/services/clerkenstein.md:45-47`, which carries the exact
  counter-warning: *"⚠️ **rc=2 is now `ExitRegressed` — a MEASURED regression** … **do not read a 2
  as a missing Node module.**"* That is the operational harm: this passage teaches the reader to do
  exactly what the sibling doc forbids.
- **Confidence:** high.

### B3 — wrong line anchor: `app/CLAUDE.md:72` is a code fence, not the service list it is cited for

- **Anchor:** `corpus/services/backend.md:43` —
  > "**⚠️ `app`'s OWN docs still list it** (`app/CLAUDE.md:72`, `app/knowledge/architecture.md:28`)"

  No ref named → graded at the checkout `b948604f`.
- **Why it is false:** `stack-demo/app/CLAUDE.md:72` is `` ``` `` — the closing fence of the bash
  block that begins at `:60` (`:68` is `make migrations …`, `:70` a comment, `:71`
  `atlas migrate apply …`, `:74` `## Architecture`). The sentence it is cited for is at
  **`app/CLAUDE.md:80`**: *"2. **RPC server** (port 8081) — one Connect-RPC mux carrying
  `BackendUsersService`, `BackendOrganizationsService`, `SkillerService`, `SkillPathSessionService`,
  `JobSimulationService`, `CMSService` and `lab.v1.LabSessionService`."* (grep over `app/CLAUDE.md`
  returns exactly one hit, at `:80`.) The companion anchor
  `app/knowledge/architecture.md:28` is **correct**.

  This is the §5 rule-21 class-3 defect (an anchor that resolves but names the wrong construct), and
  it is worse than plain line drift: the passage exists to tell a reader *"go read this and see the
  stale claim for yourself"*, and the line it sends them to is punctuation. I hesitated because
  pure line drift is carveable as MINOR; I booked BLOCKER because the resolved line is a different
  *kind* of construct entirely and the citation's whole function is to be followed.
- **Confidence:** high.

### B4 — the services index says other services reach messenger by RPC; no messenger RPC address or client exists anywhere in the platform

- **Anchor:** `corpus/services/README.md:37` —
  > "| [`messenger.md`](messenger.md) | Messenger | Centralized transactional email via Brevo + Liquid templates. Opt-in `messenger` profile; **other services fire an RPC rather than calling Brevo** |"
- **Why it is false:** `MESSENGER_RPC_ADDR` has **0** occurrences across every ground-truth clone
  (`app`, `messenger`, `storage`, `sentinel`, `platform`, and the legacy `cms` / `jobsimulation`
  husks), with `SKILLER_RPC_ADDR` → **10** as the positive control in the same command. `messengerv1`
  is imported only *inside* `messenger` itself (`cmd/send.go`, `cmd/root.go`,
  `internal/rpcsrv/rpcsrv.go`, `internal/flow/*`) — no other repo imports it. Messenger's
  `MessengerService` (`stack-demo/messenger/internal/rpcsrv/rpcsrv.go:25 CancelScheduledMessage`,
  `:28 Schedule`, `:31 Send`) therefore has **no caller**; messenger is driven by Redis Streams and
  itself calls **out** to `backend` over all four of its RPC addresses
  (`stack-demo/messenger/cmd/root.go:118-142`, all four env vars set to `http://backend:8083` at
  `platform/docker-compose.yml:173-183`).

  It also contradicts root `CLAUDE.md` (*"the only remaining cross-process RPC edges are **backend →
  sentinel** and **messenger → backend**"*) and `corpus/services/sentinel.md:5`, which reaches the
  same conclusion about messenger's edges from the other side. I hesitated because the clause can be
  read as *design intent* ("services don't talk to Brevo directly"), and messenger does still
  *expose* the surface — but an index one-liner is exactly where a reader forms a wiring model, and
  the edge it names is not in the build.
- **Confidence:** medium-high.

### B5 — **OUT OF MY ASSIGNED SET, reported because it is load-bearing and I hit it while grading a sibling claim.** Root `CLAUDE.md` says storage/messenger prod compute is stopped at `desired_count = 0`; both read `= 1`, and both anchors point at the wrong construct

- **Anchor:** `CLAUDE.md:203-204` —
  > "Prod compute for both is **stopped** (`storage/terraform/main.tf:38`, `messenger/terraform/main.tf:29`, each `service_desired_count = 0`)."
- **Why it is false:** `stack-demo/storage/terraform/main.tf:19` = `service_desired_count          = 1`
  and `stack-demo/messenger/terraform/main.tf:19` = `service_desired_count          = 1`. The cited
  lines name other constructs: `storage/terraform/main.tf:38` is `"containerPort": ${local.port},`
  and `messenger/terraform/main.tf:29` is `container_definitions          = <<EOF`. For contrast, the
  services that **are** stopped verify exactly as `corpus/services/README.md:17` states —
  `cms/terraform/main.tf:39` = `0`, `jobsimulation/terraform/main.tf:40` = `0`,
  `roadrunner/terraform/main.tf:19` = `1`.

  Why this matters for my scope: I came close to booking `corpus/services/README.md:11`
  (*"**Four** services in this index … are **folded into `app`** and no longer deploy separately"*)
  as a blocker for omitting storage + messenger. It is **correct** — on its own criterion — precisely
  *because* both still deploy at `= 1`. So the README is right and `CLAUDE.md`, the
  highest-propagation file in the tree (§5 rule 19 corollary), is wrong.
- **Confidence:** high.

---

## Minors

- `corpus/services/backend.md:253` — *"not in the top-level `migrations/` dir (which holds only `atlas.sum`)"*. No top-level `migrations/` dir exists in `app` at `b948604f` (`git ls-files 'migrations/*'` → empty); it was deleted at app commit `6a46e8445` *"chore(migrations): remove obsolete atlas.sum file"*. The only `atlas.sum` is `terraform/migrations/atlas.sum`. The load-bearing half of the sentence (migrations live in `terraform/migrations/`, per `app/atlas.hcl`) is correct.
- `corpus/services/backend.md:195` — *"`rpc.go` is the top-level wire-up. Look there for the implemented services."* The mux wire-up is in `app/main.go:1185-1228` (as this same doc correctly states at `:39`); `app/rpc.go` holds the `backendRPCServer` method implementations, not the registration.
- `corpus/services/clerkenstein.md:20-21` — the sections list names 6 (`clerkenstein`, `demo-stack`, `stack-injection`, `stack-core`, `stack-seeding`, `alignment`) of the **11** present in `.agentspace/rosetta-extensions/`: missing `dev-stack`, `playthroughs`, `stack-secrets`, `stack-snapshot`, `stack-verify`. (Root `CLAUDE.md` lists 9 — also incomplete, missing `playthroughs` and `stack-secrets`.)
- `corpus/services/clerkenstein.md:3` — *"**Last updated:** 2026-07-14"* while the body carries v2.8 M256 (`D81`, iter-16/25) and M257x iter-23 material.
- `corpus/architecture/alignment_testing.md:3` — *"**Last updated:** 2026-06-06"* while the body carries M218/M219 material.
- `corpus/architecture/alignment_testing.md:515-522` — the Layout block lists `internal/{dna,outcome,compare,report}`; `.agentspace/rosetta-extensions/alignment/internal/` also holds **`canon`** (5 packages, not 4).
- `corpus/architecture/alignment_testing.md:195` — *"its rationale is the comment block at `:58-68`"*: the comment block is `gate.sh:58-67`; `:68` is `echo "==> capability-coverage check ($dna)"`. (`:69` — the cited call — is exactly right.)
- `corpus/architecture/alignment_testing.md:516` — *"`dna list|diff|validate|coverage` (`dna.go:20-26`)"*: the switch cases run `:20-27` (`case "coverage":` at `:26`, `return dnaCoverage(...)` at `:27`). Off by one at the tail. (Unrelated tooling note, not a corpus defect: `dna.go:16`'s own usage string still reads `expected list|diff|validate` and omits `coverage`.)
- `corpus/services/sentinel.md:44` — *"only as a fixture string in sentinel's own tests (`internal/authorization/casbin_test.go`, `internal/rpcsrv/rpc_test.go`)"* omits `internal/authorization/manager_test.go:21` (`manager := "manager"`). The claim's substance (no `manager` Casbin role) is verified.
- `corpus/services/next-web-app.md:49` — names `apps/web/src/proxy.ts` and `apps/hiring/src/proxy.ts`; `apps/integration/src/proxy.ts` also exists (3, not 2). No `middleware.ts` anywhere, as claimed.
- `corpus/services/next-web-app.md:39-44` — the shared-packages table omits `packages/design` (`packages/` holds `core-js`, `design`, `graphql`, `ui`).
- `corpus/services/next-web-app.md:63` — quotes `engines.node` as `">=24"`; the literal is `">=24.0.0"`.

---

## Positively cleared (audited zeros)

**ENUMERATED** (predicate re-derived from source independently, cardinalities compared):

- `corpus/services/README.md:22-23` — *"At `0dab54d` compose declares **8** services (10 effective, with `common.yml`'s `postgresql` + `redis`) and `repos.yml` carries **6** entries."* Enumerated: compose services = `sentinel, backend, storage, customerio-sync, messenger, studio-desk, next-web-app, gotenberg` = **8**; `common.yml` = `postgresql, redis` → **10**; `repos.yml` = `app, sentinel, storage, messenger, next-web-app, studio-desk` = **6**. ✔
- `corpus/services/README.md:21-23` — *"platform `d11a403` (2026-08-03) deleted all three from `docker-compose.yml` **and** from `repos.yml`."* Read the diff: `d11a403` removes `- name: cms`, `- name: jobsimulation` **and** `- name: roadrunner` from `repos.yml` and all three services from compose. (Note: the commit **message** claims roadrunner's clone entry "was already gone" — the diff refutes its own message; the corpus is right and the commit message is wrong.) ✔
- `corpus/services/README.md:15-18` — `roadrunner/terraform/main.tf:19` = `service_desired_count = 1`; `cms/terraform/main.tf:39` = `0`; `jobsimulation/terraform/main.tf:40` = `0`. All three anchors resolve to the exact construct. And the *"one row where prod and `repos.yml` contradict each other"* predicate holds: roadrunner is the only name that is `=1` in prod **and** absent from `repos.yml` (storage/messenger are `=1` **and** present; cms/jobsim are `=0` **and** absent). ✔
- `corpus/services/README.md` completeness — 29 files in `corpus/services/`, minus `README.md` + `TEMPLATE.md` = **27** docs; README links all 27. ✔
- `corpus/services/backend.md:39` — mux handlers. Enumerated at `app/main.go`: `UsersService :1187`, `OrganizationsService :1188`, `SkillerService :1196`, `JobSimulationService :1204`, `lab.v1.LabSessionService :1228` (5 unconditional), plus `CMSService` inside `if cmsRPCServer != nil` at `:1212-1214`. Every anchor exact. ✔
- `corpus/services/backend.md:41` — *"There is no `SkillPathSessionService` — measured: **0** occurrences in Go source, and no `skillpath…v1connect` package is imported."* Both 0; positive control `JobSimulationService` = 7. ✔
- `corpus/services/backend.md:195` — the four messenger RPC addresses at `docker-compose.yml:173-183` under the comment at `:171-172`; the stale `app` comment at `main.go:1205-1211`; the 60 s write timeout at `main.go:1243` (`rpc.WithWriteTimeout(60*time.Second)`). All exact. ✔
- `corpus/services/backend.md:208` (pinned `@ app 9d00a313 v1.367.0`) — `internalstorage.NewManager` at `main.go:471`, `NewPublicManager` at `:472`; `STORAGE_RPC_ADDR` at that ref = exactly **3** occurrences, **all comments** (`internal/jobsimwiring/wiring.go:101`, `internal/storagens/callsites_test.go:189`, `main.go:451` — the last literally *"STORAGE_RPC_ADDR is gone"*), 0 read sites. Graded at its own pin per §5 rule 33 ⇒ TRUE. ✔ (See "could not settle" for the checkout-vs-pin wrinkle.)
- `corpus/services/backend.md:185` — **13** `ai_readiness_*` tables. Enumerated twice, independently: 13 `ai_readiness_*.go` files in `internal/data/ent/schema/`, and 13 distinct `ai_readiness_*` `CREATE TABLE` names in `terraform/migrations/`. The four the doc names as "the ones a 9 omits" all appear. ✔
- `corpus/services/backend.md:63-68` — all ten named moved taxonomy tables (`skills`, `job_roles`, `categories`, `specializations`, `skill_embeddings`, `job_role_embeddings`, `skill_translations`, `job_role_translations`, `job_role_skills`, `job_role_categories`) exist as `CREATE TABLE`s in `app/terraform/migrations/` (nine in `20260615130000_skiller_taxonomy.sql`, `skills` in the supabase baseline), with Ent schemas present under the repo's own filenames. ✔
- `corpus/services/backend.md:92-99, 115-116, 253` — no `skiller` in `repos.yml`/compose; migration `20260518125439.sql` present; ports 8082/8083/8084 with 8081/8082/8083 published (8081 unused); `profiles: [core, backend, all]` at `docker-compose.yml:100`; `Makefile:10 PROFILE ?= core`; `atlas.hcl` `dir = "file://terraform/migrations"` + `src = "ent://internal/data/ent/schema"`; `go 1.26.4`. ✔
- `corpus/services/sentinel.md:5, 85, 89` — `AUTHORIZATION_ADDRESS` = **exactly 1** occurrence across `docker-compose.yml` + `common.yml` + `.env_example`, at `docker-compose.yml:48`, in `backend`'s block. `messenger` and `storage` import no authorization client (0 hits; positive control 42 Go files in messenger). ✔
- `corpus/services/sentinel.md:26` — *"6 request types, 6 policy types, 3 role groupings, 6 matchers."* Enumerated from `sentinel/internal/authorization/casbin.go`'s `ModelConf`: `r,r2..r6` = 6; `p,p2..p6` = 6; `g,g2,g3` = 3; `m,m2..m6` = 6. ✔
- `corpus/services/sentinel.md:68-83` — the RPC method table. Enumerated `internal/rpcsrv/rpc.go`: exactly **19** exported handler methods, and the table names exactly those 19. ✔
- `corpus/services/sentinel.md:12, 22, 40, 43-48, 50-64, 122-135, 151` — `go.mod:3 go 1.26.0`; `Dockerfile:2` / `Dockerfile.dev:2` `golang:1.26-bookworm`; `terraform/locals.tf:4-5` = `service_cpu 256` / `service_memory 128`; `app/internal/data/ent/enum/membership.go:8-15` = the four `MembershipRole` values; `init_policy.sql:63-66` = the taxonomy:write NOTE; `:88-118` = the `content_creator` block; `casbin_content_creator_test.go` present; `manager` → **0** in `init_policy.sql` (control: `content_creator` → 24); key-directory listing matches the tree file-for-file; `Makefile:3` hard-codes `postgresql://postgres@localhost:5432/postgres?sslmode=disable`; one `CREATE TABLE` (`sentinel.casbin_rules`); `local_superadmin_grants.sql` content matches (p3 admin taxonomy:write + commented p4 block); `cmd/root.go:47` `cmp.Or(os.Getenv("PORT"), "8080")`. ✔
- `corpus/services/next-web-app.md:22-23, 31-35, 44, 48-49, 71-72, 83, 87, 92-96, 122-126` — next `^16.2.7` in **all four** apps; react/react-dom `^19.2.7`; `engines.node >=24.0.0`; `packageManager pnpm@10.30.3`; `turbo ^2.9.6`; package names + ports 3000/3001/3002 and mobile `--port 3031`; **8** locale dirs; `docker-compose.yml:236` bakes the endpoint; `proxy.ts` present / `middleware.ts` absent; public allowlist matches (`/login`, `/sign-up`, `/checkout`, `/free-trial`, `/monitoring`, `/print`, `/api/bunny/thumbnail`) + `validatePrintToken` import; no `storybook` script, no `.storybook/`, only `configs/tailwind/storybooks.css`; `Makefile:119-120` `--profile core --profile frontend`; `docker-compose.yml:250-252` `depends_on: backend` and `:100` backend's profiles (so `PROFILE=frontend` alone is an invalid project); `Dockerfile.dev` the only root Dockerfile; `antd ^6.3.7`; mobile excluded via `pnpm-workspace.yaml`'s `'!apps/mobile'`; repo `CLAUDE.md:15` = *"Next.js 16 App Router"*, `:55` = the proxy.ts rename, `:82-84` = the stale "Two Dockerfiles" note. ✔
- `corpus/services/clerkenstein.md:97-106` — the repo-structure table. Enumerated `clerkenstein/`: `authn/`, `clerk-backend/`, `clerk-frontend/`, `clerk-webhook/`, `shared/`, `deploy/`, `cmd/`, `alignment/` — exactly the 8 rows, no extras. `alignment/` holds `cmd/{clerkrun,deployrun,expressrun,jsfapirun,multirun}` (5), `dna/` (5 files), `golden{,-deploy,-express,-js,-multi}` (5), `scripts/`. ✔
- `corpus/services/clerkenstein.md:41-44, 74` **and** `corpus/architecture/alignment_testing.md:256-260` — the gene/capability counts. Parsed each DNA JSON: `clerk-2.6.0` **27 genes / 14 caps**, `clerk-js-5` **9 / 6**, `clerk-multi-1` **9 / 5** (incl. `Roster` with 2 variants), `clerk-express-1` **13 / 5**, `clerk-deploy-1` **7 / 3**. Every figure in both docs matches. ✔
- `corpus/services/clerkenstein.md:59-62` — `clerk-backend/store.go:138` = `func (s *Store) SeedOrgIdentity`, `:151` = `func (s *Store) LookupOrgEid`; `alignment/dna/clerk-2.6.0.json:131` is the `MembershipOrgIdentity` description containing *"taking the Go surface 97.2% -> 100%."* ✔
- `corpus/services/clerkenstein.md:46` — `alignment/cmd/alignctl/run.go:134-135` = `ExitRegressed = 2` / `ExitUnmeasurable = 3`. ✔
- `corpus/services/clerkenstein.md:104, 274-279` — `clerkenstein/go.mod:8` `colony v0.34.3`; `app/go.mod:16` `colony v0.35.2` and `:31` `clerk-sdk-go/v2 v2.7.0`; `sentinel/go.mod:8` and `storage/go.mod:7` both `colony v0.34.3` — so the "not wrong for the whole platform" qualifier holds. ✔
- `corpus/services/clerkenstein.md:121-147, 165-176, 206-209, 308-311` — both membership routes registered (`clerk-backend/server.go:47` BAPI, `clerk-frontend/server.go:186` FAPI) with `meorgmemberships_test.go` present; `registry.go` single `activeKey`; `server.go:125` `clientID: "client_clerkenstein"`, `:665` `sessID = "sess_clerkenstein"`, `signedOut` at `:107`; **`r.Cookie(`/`.Cookie(` = 0 occurrences in `clerkenstein/`** (control: 55 `http.Request` references); all five named `server_test.go` tests exist; `server.go:35-67` is the M220 disk-cache block with `clerkJSFetchTimeout = 15 * time.Second`, `FAKE_FAPI_CLERKJS_CACHE`, and the *"Explicitly NOT http.DefaultClient"* comment. ✔
- `corpus/services/clerkenstein.md:141-143` — `studio-desk/src/index.ts:96` and `studio-desk/app/services/userService.ts:16` both = `const STUDIO_ACCESS_ROLES = ['admin', 'org:admin', 'content_creator', 'org:content_creator'];`, each preceded by the quoted comment. ✔
- `corpus/services/clerkenstein.md:211-241` — roster threading. `stack-seeding/seeders/roster.go:47 org_eid`, `:53 org_name`, `:54 org_slug`, `:77 org_is_hiring,omitempty`; `clerk-frontend/registry.go:125-126` + `:144` the matching consumer fields; `resources.go:50-51` `orgNameDefault = "Clerkenstein Demo Org"` / `orgSlugDefault = "clerkenstein-demo"`; `resources.go:275` the conditional-emit `if u.OrgIsHiring`; `ResolvedStory.IsHiringOrg()` real (`stack-seeding/seeders/org.go:68`, `manifest/manifest.go:277`). ✔
- `corpus/architecture/alignment_testing.md:104-107, 162-169` — `internal/dna/dna.go:21` `idPattern = ^[A-Za-z0-9][A-Za-z0-9_-]*$`; `:24 maxWeight = 1_000_000` enforced at `:269-270` (*"weight must be in 1..%d"*); the zero-critical guard at `:274-281` (Validate rejects) plus `GateMet`'s scoring-time defence (pinned by `compare_test.go:132-141`). ✔
- `corpus/architecture/alignment_testing.md:192-210` — `DNA.Validate()` runs first in `dnaCoverage` (`cmd/alignctl/dna.go:64-71`); `--if-declared` downgrades **only** the no-`consumed_surface` case (`:72-81`, `return 0` under the flag, `return 2` without); `gate.sh:69` passes `--if-declared` before the score. And the load-bearing set claim: **exactly one** DNA declares a `consumed_surface` — `clerk-2.6.0.json` (1), the other four (0 each). ✔
- `corpus/architecture/alignment_testing.md:288-311` — the toy. `examples/toy/{source,mirror,runner,surface,cmd/toyrun,golden}` all present; `dna.json` = `Add` (critical, 3 variants) + `Greet` (standard, 3 variants); `alignment_test.go:21-22` `gateOverall = 80.0` / `gateCritical = 100.0`. **RE-DERIVED** (weaker, flagged as such): weights 3·3 + 2·3 = 15, one diverged standard gene (w2) ⇒ 13/15 = 86.67 % overall, 3/3 critical — matching the quoted `86.7% / 100.0% (5/6)` and `(exact, w2)`.
- `corpus/architecture/alignment_testing.md:322-346, 490-524` — `clerkenstein/alignment/scripts/{gate,drift-check}.sh` exist and default `ALIGN_DIR` to `$base/../../alignment`; the reusable `alignment/` section has **no** `scripts/` dir (it holds `cmd/`, `internal/`, `examples/`, `go.mod`, `README.md`); `clerkenstein/.github/workflows/alignment.yml` is git-tracked while the monorepo **root** has no `.github` at all ⇒ inert, exactly as described; `alignment/go.mod` = `module anthropos.dev/alignment` with **no** `require` block (stdlib-only). ✔
- `corpus/architecture/alignment_testing.md:405-488` — the data/snapshot dimensions. `stack-seeding/dna/` + `cmd/datadna` present; the four structural operators are literals at `dna/dna.go:78-81` (`type-match`, `constraint-satisfied`, `fk-valid`, `row-count`); the **five** snapshot operators at `dna/snapshot.go:36,39,42,45,48`; `datadna`'s exit contract *"0 ok / no drift, 1 conformance-fail / drift, 3 usage error"* at `cmd/datadna/main.go:100` and `:419`. And the coverage predicate: `data-dna.json` holds 10 surfaces — 4 `seeded-m7a`, 4 `seeded-m7c`, 1 `snapshot-seeded-m9b` (taxonomy), 1 `snapshot-seeded-m10` (content) — **zero `waived`**, so *"NOTHING is left waived → 100% coverage"* holds. ✔
- `corpus/services/backend.md:44` — `POST /api/webhook/directus` registered at `internal/web/backend/backend.go:324` (Clerk authn skipped at `:137-138`), handler built from `DIRECTUS_WEBHOOK_SECRET` at `main.go:1088-1089`. ✔

---

## What I could not settle

- **`corpus/services/db-backup.md` in its entirety.** There is no `db-backup` clone in the
  ground-truth set and the name appears in no `repos.yml`, compose file, or terraform I can read.
  Every claim in the file — Go, "every 6 hours", the S3 / Azure Blob / Hetzner triple, *"PostgreSQL
  RDS (all schemas)"*, *"Combined with RDS point-in-time recovery"* — is unfalsifiable from here.
  **Settled by:** a clone of `anthropos-work/db-backup` (or the infrastructure repo's schedule/task
  definition). I explicitly did **not** grade it clean; it is unmeasured, and per §5 rule 8 an
  unmeasured surface must not be reported as a pass.
- **The taxonomy magnitudes** at `corpus/services/backend.md:9, 36, 70-76` (42,790 skills / 22,470
  vs 22,490 job roles / 43,584 embeddings / 23 categories / 1,447 specializations). These are prod-DB
  measurements dated 2026-07-08 and 2026-06-29; no DB read was in scope for this pass. The tables
  they range over do all exist (verified above), and the doc's *"floors, not totals"* framing is
  self-consistent. **Settled by:** `/db-query` against prod with `organization_id IS NULL`.
- **The checkout-vs-pin storage split.** `corpus/services/backend.md:208` is pinned to
  `app 9d00a313` v1.367.0 and is true there, so I did not book it. But a reader on the **checkout**
  (`b948604f`, v1.366.0 — what `stack-demo/app` actually is) gets the opposite behaviour: `app`
  imports `github.com/anthropos-work/storage/sdk/storage` (`main.go:109`) and builds storage clients
  from `os.Getenv("STORAGE_RPC_ADDR")` at `main.go:446`, `:523-524`, `:992` — while
  `platform/docker-compose.yml` at `0dab54d` sets that variable **nowhere** and leaves `storage` in
  `profiles: [storage-legacy]`. So on a stock stack at these two refs the storage client is
  constructed against the empty string. Root `CLAUDE.md` calls this *"mid-fold"*; `backend.md` does
  not carry that caveat next to the pinned bullet. Not booked (the pin is a date, §5 rule 33), but a
  candidate for an explicit "at the checkout ref, X" sentence.
- **`corpus/services/backend.md:219`** — *"The one external producer left is **Directus**, whose
  webhooks feed the `cms` stream."* Mechanically, Directus makes an **HTTP webhook** call to
  `POST /api/webhook/directus` and it is `app`'s own `cmsPub` (`main.go:1039`, `:1089`) that writes
  the Redis Stream message; Directus never speaks to Redis. Whether calling that an "external
  producer" is a defect or an accepted shorthand is an editorial call I did not want to make
  unilaterally. **Settled by:** a ruling on whether "producer" in this corpus means *writes the
  stream* or *originates the event*.
