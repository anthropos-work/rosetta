# Seat F — reading #15

**Refs at the moment of measurement.** Corpus HEAD `1937e1f` (branch `m257x/platform-realignment`).
Ground-truth clones re-verified at open: `app b948604f`, `platform 0dab54df`, `next-web-app bb3313bc`,
`sentinel 88bc5592`, `storage 4ce8ece5`, `messenger fa47850d`, `cms ca50c817`,
`graphql-wundergraph 60c229f3`, `roadrunner 87d8d443`, `jobsimulation 462343b0`,
`studio-desk 14a5442a`, `ant-academy 9c3843cd`, `.agentspace/rosetta-extensions 4e6b64d3` (main).
Supplementary read-only sources: the Go module cache (`colony@v0.34.3`, `ai@v1.40.1` — the only
shared-lib artifacts on this box) and `.agentspace/snapshots/taxonomy/*/manifest.json`.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/services/ant-academy.md` | 437 | yes |
| `corpus/services/studio-desk.md` | 435 | yes |
| `corpus/services/chronos.md` | 245 | yes |
| `corpus/architecture/shared_libraries.md` | 242 | yes |
| `corpus/services/skillpath.md` | 110 | yes |
| `corpus/architecture/frontend_architecture.md` | 105 | yes |

(Also read in scope, per the briefing: `corpus/ops/platform-alignment.md` §5, 1726 lines total.)

Search-pipeline controls run in-pass: every `grep`/`git grep` that returned zero was paired with a
pattern known to match in the same tree (e.g. `REDIS_ADDR` beside `ROADRUNNER_RPC_ADDR`;
`JUDGE0` beside `ROADRUNNER_RPC_ADDR` in platform; `colony/authn` = 129 files in `app` beside the
standalone-`authn` zero). One pipeline **did** silently break mid-pass — a zsh `${c}:repos.yml`
history loop where `$c:r` was eaten as a zsh modifier, producing nine `fatal:` lines that would have
read as "no history" had stderr been swallowed. Reading that stderr is what produced **B1**.

## Blockers

### B1 — `shared_libraries.md` taxonomy row: the "6 of 7" set is 11, not 7

- **Anchor:** `corpus/architecture/shared_libraries.md:181` — *"Counted over **every** Go service repo
  the platform has ever cloned, that is **6 of 7** (app, sentinel, storage, messenger, cms,
  jobsimulation, roadrunner) — the sole exception is `roadrunner`, which requires only colony + proto."*
- **Why it is false:** The arithmetic over the seven repos named is exactly right (I re-derived it:
  app/messenger require `taxonomy v1.2.0` directly, storage/sentinel `// indirect`, `cms/go.mod:13` and
  `jobsimulation/go.mod:15` require it directly, `roadrunner/go.mod` does not). **The SET the
  arithmetic ranges over is wrong.** `platform/repos.yml` has nine revisions in history
  (`git log --oneline -- repos.yml`), and the earliest, `a2a3ee6:repos.yml`, declares
  **eleven** entries with `type: go`:

  `app · cms · jobsimulation · skiller · skillpath · chronos · sentinel · intelligence · storage ·
  messenger · roadrunner`

  (`next-web-app` and `studio-desk` are `node-pnpm`/`node-npm`; `graphql-wundergraph` is `node-npm`,
  so it is correctly outside a *Go*-repo count.) They were removed one at a time —
  `fdfa189` intelligence, `045857c` chronos, `21429b7` skiller, `a4db680` skillpath, `236771f`+
  `d11a403` the rest — each of which the corpus documents elsewhere as a real `repos.yml` entry
  (`skillpath.md:45` even counts `repos.yml` at two refs, and `chronos.md:9` says chronos "is no
  longer cloned by `make init`", which presupposes it once was).

  So **"every Go service repo the platform has ever cloned" = 11**, and the denominator 7 silently
  substitutes *the clone set that happens to be on this box* for the predicate the sentence claims.
  The true fraction is unknown and unmeasurable from here (no `skiller` / `skillpath` / `chronos` /
  `intelligence` clone exists to read a `go.mod` from), which is precisely what makes the confident
  "6 of 7" misleading: a reader takes it as a closed census.
- **Confidence:** high (on the set being wrong; the derivation is a `git show` over `repos.yml`
  history and is reproducible in one command).

### B2 — `ant-academy.md`: "not a dropdown menu" — a 7-locale dropdown language menu exists and is mounted

- **Anchor:** `corpus/services/ant-academy.md:227-229` — *"**The switcher
  (`src/i18n/LocaleSwitch.jsx`) is a 2-way EN↔IT toggle `<Link>`** that sets `?lang=it` on the current
  path (mounted in the public header), **not a dropdown menu**."* The section heading (`:220`) frames
  the whole block as the verdict that the M237 triage's *"the switcher shows no menu"* is
  **"not a code bug"**.
- **Why it is false:** The academy has **two** language-switch surfaces, and the doc enumerates one.
  - `ant-academy/code/src/i18n/LocaleSwitch.jsx` — the 2-way EN↔IT `<Link>` the doc describes,
    mounted at `src/views/public/PublicHeader.jsx:20`. That half is correct.
  - `ant-academy/code/src/components/LanguageSelector.jsx` — **a dropdown menu**: it holds
    `const [open, setOpen] = useState(false)`, attaches a deferred outside-`pointerdown` close and an
    `Escape` handler, and builds `LANGUAGES` from **all seven** `SUPPORTED_LOCALES`
    (`src/i18n/locale.js:10` → `['en','it','es','fr','de','nl','pt']`) with `LOCALE_LABELS` +
    `LOCALE_FLAGS`. It is mounted at `src/components/TopBar.jsx:76` — the **authed** shell, i.e. the
    surface a demo hero actually sees.

  A reader chasing "the switcher shows no menu" is sent by this passage to `LocaleSwitch.jsx` (which
  correctly has no menu, by design) and away from `LanguageSelector.jsx` (which is the menu, and is
  where such a symptom would live). The claim *"not a dropdown menu"* is false as written about the
  academy's language switching, and the "not a code bug" verdict rests on the incomplete enumeration.
- **Confidence:** high on the fact (the component exists, is a dropdown, and is mounted); high that
  the sentence as written is refuted; medium on how much a reader is harmed, which is why I state the
  routing consequence explicitly rather than leaning on it.

### B3 — `skillpath.md`: `app/CLAUDE.md:72` names a closing code fence, not the RPC list

- **Anchor:** `corpus/services/skillpath.md:34` — *"(`app/CLAUDE.md:72` and
  `app/knowledge/architecture.md:28` still list the service — Trap C, *the platform's planning docs lag
  its own code*. Grade against `main.go`.)"*
- **Why it is false:** The passage names no ref, so it grades at the checkout, `app b948604f`. There,
  `app/CLAUDE.md:72` is `` ``` `` — the closing fence of the `atlas migrate apply` block that ends the
  "Database migrations" section. The `SkillPathSessionService` mention is eight lines lower, at
  **`app/CLAUDE.md:80`** (*"2. **RPC server** (port 8081) — one Connect-RPC mux carrying
  `BackendUsersService`, … `SkillPathSessionService`, …"*). The twin anchor,
  `app/knowledge/architecture.md:28`, **is** correct — it is the RPC row of the server table, and it
  does list the service. So one of the pair resolves to the wrong construct and the other is right,
  which is the shape that survives a reading: the sentence looks corroborated.
- **Confidence:** high on the measurement. **Why I hesitated:** this is pure line drift — the file is
  right, the substance is right, and the canonical grading rule quoted in `platform-alignment.md` §5
  rule 25 explicitly carves *line drift* out as MINOR. My briefing resolves doubt upward and lists
  "wrong line anchors that point at the wrong construct" under BLOCKER, so I book it here and flag the
  disagreement rather than hiding it.

## Minors

- `corpus/services/ant-academy.md:135` — *"`emptyCatalogView() = { chapters: [], skillPaths: {},
  series: [] }`"* is presented as a literal equation but is an abbreviation. The real return
  (`ant-academy/code/src/lib/serverTenant.js:116`) is
  `{ chapters: [], skillPaths: {}, series: [], bundles: PUBLIC_BUNDLES, catalogVersion: CATALOG_VERSION }`,
  and `PUBLIC_BUNDLES` (`ucourses/catalog.js:956`) is a **non-empty committed array**. The material
  conclusion ("0 cards") survives — `BundleStripe` is "just chrome" and returns `null` on
  `pathCount === 0` — but the quoted object is not what the function returns.
- `corpus/services/ant-academy.md:247` — *"~26 Playwright e2e spec files (tests/e2e/)"*. Measured:
  **31** `*.spec.js` (34 entries in the directory). Hedged with "~", so undercount-class.
- `corpus/services/ant-academy.md:325` + `:346` — the doc says the app's env file is `code/.env`
  (`cp .env.example .env`; *"set `REQUIRE_ORGANIZATION_MEMBERSHIP=0` in `code/.env`"*), while root
  `CLAUDE.md` says `ant-academy/code/.env.local` and adds *"the React app reads **only** from
  `code/.env.local`"*. Next.js loads both (`.env.local` overriding `.env`), so neither instruction
  breaks — but the two docs disagree on the filename and CLAUDE.md's "only" is wrong. Booked MINOR
  because both paths work; noted because it is the self-contradiction class §5 rule 21 calls cheapest.
- `corpus/services/ant-academy.md:145` — *"`code/public/catalog.json` (~2,667 entries)"*. The
  checked-out artifact carries **2,715** `courses` (7 languages). It is a regenerated build artifact
  (`postinstall`/`predev` rebuild it), so drift here is expected and the "~" hedges it.
- `corpus/services/studio-desk.md:34` — *"each loading `app/core/main.ts` which bootstraps Clerk auth
  + scaffold (header/sidemenu/footer) + the page module"* has the last leg inverted. Each `*.html`
  entry loads its **own page module** (`app/home.html:15` → `home/home.js`;
  `app/sim-advanced-builder.html:16` → `sim-advanced-builder/simAdvancedBuilder.js`), and *that*
  module imports `initializeApp` from `../core/main.js`. `initializeApp` never loads a page module —
  `grep -l "core/main.ts" app/*.html` returns **0 of 11**. The same document states it correctly at
  `:382` (*"`core/main.ts` (imported by each page's entry)"*), so this is loose prose beside a correct
  twin, not a contradiction of substance.
- `corpus/architecture/frontend_architecture.md:39` — *"~15 sites hitting
  `NEXT_PUBLIC_BACKEND_API_URL`"*. Measured in `next-web-app @ bb3313bc`: **22** distinct `.ts`/`.tsx`
  files (44 occurrences tree-wide excluding `node_modules`/`.next`). Every example the sentence names
  resolves (`invite/[token]/page.tsx`, `useAssignmentBuilder.ts`, `useStripe.tsx`, the CSV bulk import
  in `EnterpriseAddMultipleEmployeeModal`, the admin backfill tools `MigrationRunnerBox` /
  `RecomputeAcademyEmbeddingsBox`); only the magnitude is low.

## Positively cleared (audited zeros)

Every row below states which of the two I did. **Only ENUMERATED is a clearance.**

### `shared_libraries.md`

- **ENUMERATED** — all four shared-lib pins across all seven Go clones, read straight from each
  `go.mod` rather than re-checked against the doc's own list. colony: `app`/`messenger` **v0.35.2**,
  `sentinel`/`storage` **v0.34.3**, frozen `cms`/`jobsimulation` **v0.35.1**, `roadrunner/go.mod:7`
  **v0.34.3**. proto: app/messenger **v1.210.0**, sentinel **v1.200.0**, storage **v1.196.0**,
  cms **v1.207.0**, jobsimulation **v1.205.0**, roadrunner **v1.196.0**. ai: **v1.40.2** in app,
  `cms/go.mod:9`, `jobsimulation/go.mod:11` — and *nowhere else*. taxonomy: `cms/go.mod:13`,
  `jobsimulation/go.mod:15`, both **v1.2.0**, neither `// indirect`; storage/sentinel `// indirect`;
  roadrunner absent. Every figure in the four "Version pin" / "Imported by" rows matches.
  (The *set* those figures range over is B1 — the numbers are right and the claim they support is not.)
- **ENUMERATED** — `:70`, the six Connect handlers in `app/main.go`. I listed **every** `mux.Handle`
  in the file rather than confirming the five the doc names: exactly six exist, at `:1187` Users,
  `:1188` Organizations, `:1196` Skiller, `:1204` JobSimulation, `:1213` CMSService, `:1228`
  LabSession. The conditional is real and its range is exact: `:1212` `if cmsRPCServer != nil` /
  `:1213` handle / `:1214` `}` — the doc's `1212-1214`.
- **ENUMERATED** — `:79`'s pinned claims, read at the ref each names. At `app 9d00a313`,
  `internal/jobsimwiring/wiring.go:123` is `runnerManager := jsrunner.NewRunnerManager(getenv("JUDGE0_API_KEY"), getenv("JUDGE0_BASE_URL"))`.
  `ROADRUNNER_RPC_ADDR`: **0** matches at `app 9d00a313` and **0** at `platform 0dab54d`, both with a
  live positive control in the same command. `app/internal/skillpaths/skillpaths.go:27-31` does carry
  *"the drop-in for the **removed** skillpath RPC client"*.
- **ENUMERATED** — `:145`, the authn importer counts. I counted the files myself per repo:
  `colony/authn` is imported by **cms 6** and **jobsimulation 8** `.go` files (exactly as claimed),
  **app 129**, and **0** in sentinel/storage/messenger/roadrunner. The standalone
  `github.com/anthropos-work/authn` is imported by **no** `.go` file in any clone and appears in **no**
  `go.mod` — so `:162` and *"Sentinel does not import authn"* (`:168`) both hold.
- **ENUMERATED** — `:202-203`, the taxonomy figures, against the provenance the doc itself cites.
  `.agentspace/snapshots/taxonomy/5afc0bccf1df7ef538b643321fc6362f/manifest.json`:
  `skills 42790`, `job_roles 22470`, `job_role_embeddings 18919`, `source primary-read`,
  `public_only true`, `predicate org-null`, `captured_at 2026-06-29`. All three numbers, the
  provenance triple and the date reproduce exactly. The "18K came from `job_role_embeddings`"
  hypothesis is supported by the artifact.
- **ENUMERATED** — `:107-109`, the `ai.AI` interface. I read the interface from
  `ai@v1.40.1/ai.go:8-18` and compared members, not names: `ChatCompletion`, `ChatCompletionStream`,
  `Response`, `CreateEmbeddings`, `CreateSpeech`, `OCRProcess`, `AudioTranscriptions`, `Tokenize`,
  `GetEndpoint` — nine, exactly the nine listed, no more. Constructors `openai.New`/`NewOpenAI`/
  `NewAzure`, `anthropic.NewAnthropic(cfg, key)`, `mistral.NewMistral` all present with the claimed
  shapes; `AzureDefaultAPIVersion = "2025-04-01-preview"` (`openai/completion.go:43`);
  `retry.Attempts(10)` + the `401, 403, 404` non-retry case (`retry.go:17`, `:38`); Anthropic panics
  **2** (`anthropic/completion.go:177,186`), Mistral **4**.
- **ENUMERATED** — `:48-59`, the colony surface, at the cached `v0.34.3`. Packages present:
  `authn/ authorization/ flags/ pubsub/ redis/ rpc/` (the six the table lists). `authorization`'s Go
  package really is `authorizer`. `NewHTTPServer` really serves `/_meta` (`http.go:20`). Sentry gates
  on `!IsProduction() && FORCE_SENTRY == ""` (`logging.go:47`). The DB health monitor is a **30 s**
  ticker cancelling at **`errCount >= 3`** (`db.go:46`, `:61`). The GraphQL public-middleware limiter
  is `rate.NewLimiter(1, 5)` at `graphql.go:239` and **both** of its reject paths are commented out —
  I read the surrounding 50 lines, not the matched line, and the "only logs" claim is exactly right.
- **ENUMERATED** — `:23-29`, the consumption model. None of the five is in `platform/repos.yml`
  (which holds six entries: app, sentinel, storage, messenger, next-web-app, studio-desk).
  `GH_ACCESS_TOKEN: $GH_PAT` appears as a build arg five times in `docker-compose.yml`, and
  `app/Dockerfile:11,13` carries both the `insteadOf` rewrite and `GOPRIVATE=github.com/anthropos-work/*`.
- **ENUMERATED** — `:92-94`. `storage/internal/migration/migration.go:13` imports
  `go/simulator/storage/v1` aliased `legacyStorage`, used at `:93`. Sole `go/simulator` reference in
  that repo.
- **ENUMERATED** — `:125`'s cross-file citation. `external_services.md:555` is literally
  *"There is **no ordered EU-first fallback chain.**"* (The stale `:546` I was carrying from a
  CLAUDE.md snapshot is **not** what the on-disk `CLAUDE.md` says — it says `:555` at both `:245` and
  `:259`. No finding; recorded so the next pass does not re-open it.)

### `skillpath.md`

- **ENUMERATED** — `:30-31`, the RPC-removal claim, over the whole clone set rather than the one repo:
  `SkillPathSessionService` appears in **0** `.go` files in app, sentinel, storage, messenger, cms,
  jobsimulation, roadrunner **and** next-web-app; the only hits anywhere are two markdown lines inside
  `app` (which is the doc's own Trap-C point, modulo B3). No `skillpath…v1connect` import exists.
- **ENUMERATED** — `:83`, the mirror-drop migration, by reading the lines rather than grepping to one:
  `app/terraform/migrations/20260729133514.sql:62` is `DROP TABLE "local_jobsimulation_sessions";` and
  `:63` is `DROP TABLE "local_skill_path_sessions";`, under a comment block that explains the trigger
  cleanup. No `local_skill_path_session.go` Ent schema exists.
- **ENUMERATED** — `:85-87`, the "three post-date it" correction, at the ref it names. At
  `app 9d00a313`, `git ls-tree` gives exactly three migrations sorting after `20260729133514.sql`:
  `20260731131307.sql`, `20260731154527_academy_chapter_progress_completed_at.sql`,
  `20260803143844_ai_readiness_recommendation_path.sql`. (Worth recording: the **checkout**
  `b948604f` has only the first two, so this clause is true *only* at its pin — a live demonstration
  of §5 rule 33 rather than a defect.)
- **ENUMERATED** — `:75-77`, the manager-view reader. `InsightsSkillPathByMemberships` is at
  `app/internal/organization/intelligence.go:1144`, and `:1159-1170` is the query — I read the whole
  predicate rather than the line: `SkillPathID(skillPathId)`,
  `StatusIn(SkillPathStatusActive, SkillPathStatusCompleted)`, `HasUserWith(user.IDIn(...))`,
  `Or(TenantIDIsNil(), TenantID(organizationID))`. Every conjunct the doc names is present and there
  is no fourth the doc omits.
- **ENUMERATED** — `:14` / `:57`, the two in-process cms reads.
  `app/internal/skillpath/session.go:205-207` = the `// cms-in-app deseam: cms is in-process` comment
  plus `u.cms.GetSkillPathDomain(ctx, skillPathId, version)`;
  `app/internal/skillpaths/skillpaths.go:88-95` = the same comment plus
  `u.cms.GetSkillPathDomain(ctx, id, nil)`. `docker-compose.yml:174` is
  `- CMS_RPC_ADDR=http://backend:8083`, on `messenger`.
- **ENUMERATED** — `:95-102`, the "Coming soon" verdict, followed all the way to the rendered string.
  `apps/web/.../skill-paths/[skillPathId]/[userId]/page.tsx:11` renders
  `InsightsBySkillPathStudentSimulationsContainer`; in that container `userData` is
  `return null as unknown as MembershipEnriched` (`:31-34`) with the real read commented out, the
  results `<Table>` and the totals block are both commented out, and the body renders
  `t('enterprise.insights.comingSoon')` — which resolves in
  `configs/i18n/messages/en/enterprise.json` to **`'Coming soon'`**. The doc says "renders the literal
  string"; it is a translation key that resolves to that literal, so the claim holds in effect.
- **ENUMERATED** — `:45`, both `repos.yml` counts: **9** entries at `2adcf71`, **6** at `0dab54d`,
  zero `skillpath` at either. `SKILLPATH_STREAM=skillpath` really is residual and really is still set
  (`docker-compose.yml:72`), read by `app/main.go:637` and `:1274`.

### `frontend_architecture.md`

- **ENUMERATED** — the whole monorepo table, derived from the tree rather than checked against the
  doc. `apps/` = `web hiring integration maintenance mobile` (five, matching), with dev ports read
  from each `package.json` `dev`/`dev:mobile` script: **3000 / 3001 / 3002 / — / 3031 (expo start
  --port 3031)**. `pnpm-workspace.yaml` really excludes `!apps/mobile` with the comment "exclude
  mobile app to speed up development". Package names `@anthropos/web-app`, `@anthropos/hiring-app`,
  `@anthropos/integration` match the three `--filter` invocations at `:75-77`.
  `configs/tsconfig` → `@anthropos/tsconfig` and `configs/i18n` → `@anthropos/i18n` both confirmed
  from their own `package.json` `name`. i18n locales enumerated from the directory:
  **de en es fr it ja nl pt** — eight, exactly the eight listed, in that order.
- **ENUMERATED** — `:48-52`. `apps/web/package.json:46` is `"next": "^16.2.7"` (line-exact);
  root `engines.node` `">=24.0.0"`; `packageManager` `pnpm@10.30.3`; `turbo ^2.9.6` (2.x);
  `typescript ^5.9.3`; `UPGRADE-IMPACT-next16.md` present at repo root; `check`, `check:lint`,
  `check:types`, `check:deprecations`, `codegen`, `codegen:watch`, `clean:quick` all present as
  scripts.
- **ENUMERATED** — `:39`'s port/path archaeology, which is the claim most likely to have rotted.
  `docker-compose.yml:237` is the `NEXT_PUBLIC_BACKEND_API_URL` **build arg** and `:246` the
  **runtime env**, both `http://${PUBLIC_HOST:-localhost}:8082` — the doc's two anchors, correct and
  correctly distinguished. `:5050` really was only ever the LOCAL host mapping: at `b56d731~1` and
  `2adcf71~1` compose line 14 reads `- "5050:8080"`, and the router's own configs
  (`config.prod.yaml:5` `listen_addr: 0.0.0.0:8080`, `:13` `graphql_path: /graphql`,
  `terraform/locals.tf:8` `port = 8080`) confirm `:8080/graphql` in prod. No production artifact
  mentions 5050.
- **ENUMERATED** — `:32`'s "single `backend` subgraph". `graphql-wundergraph/supergraph-config-prod.yaml`
  declares exactly **one** subgraph entry (`backend`), with the in-file comment
  *"cms-in-app (v8.0): the cms subgraph is folded into backend (supergraph 2→1)."*
- **ENUMERATED** — `:11`. `repos.yml @ 2adcf71` holds exactly nine entries and ant-academy is not
  among them; `docker-compose.yml:226` is `profiles: [studio-desk, all]`; studio-desk **is** in the
  current `repos.yml`.

### `ant-academy.md`

- **ENUMERATED** — `:251-281`, the `isPublic` matcher, derived by dumping the whole
  `createRouteMatcher([...])` array from `code/proxy.js:112` and comparing group by group. Every
  pattern in the doc's eight rows is in the source, both dev-only spreads are real
  (`DEV_LOGIN_ENABLED`; `VISUAL_BYPASS = BENCHMARK_VISUAL_BYPASS === "1" && …` at `:62-63`, whitelist
  not blacklist), and no pattern in the source is missing from the table. The doc's own warning is
  vindicated: this is the passage most likely to be paraphrased wrongly and it currently is not.
  `:280`'s claim also holds — `app/(public)/library` and `app/(public)/free` exist, there is **no**
  `/library/[slug]` directory anywhere, and the per-course route is `app/(authed)/courses/[slug]`.
- **ENUMERATED** — `:122-140`, the read chain, walked file by file rather than trusted:
  `getServerCatalogView()` at `serverTenant.js:143-146` is exactly
  `const view = (await getBackendCatalogView(eids)) ?? emptyCatalogView()`;
  `getBackendCatalogView` at `backendContent.js:94` calls `makeClient()` (`:54`) →
  `createServerGraphQLClient` (`:66`, imported from `../graphql/server.js`) and reads
  `academyCatalogSeries` (`:102`) + `academyCatalogSkillPaths` (`:103`); the in-code comment at
  `serverTenant.js:132` is verbatim *"cutover is intentional, not reversible-on-error"* and sits
  inside the doc's cited `115-145` window. All three failure legs are real and all three collapse to
  the same `??`.
- **ENUMERATED** — `:169-190`, the chapter-body half. `serverChapterBody.js::resolveServerChapterBody`
  opens with `const body = await getBackendChapterBody(slug, locale)`; the null branch goes
  `maybeResolveDraftBody` → `return { notFound: true }`; and `app/not-found.jsx:43` is literally
  *"You wandered off the trail."*
- **ENUMERATED** — `:194-198`, the five bring-up patches and their line anchors, checked against the
  script itself. `rosetta-extensions/demo-stack/ant-academy.sh` assigns
  `DEVORIGINS_PATCH` `:56`, `FSPUB_PATCH` `:66`, `FSBODY_PATCH` `:75`, `FSPUBLIC_PATCH` `:84`,
  `COCKPIT_PATCH` `:90` — all five anchors exact — and all five manifests exist under
  `demo-stack/patches/`.
- **ENUMERATED** — `:63`'s write path, verified on **both** sides. Client:
  `code/app/api/academy/beacon/route.js` exists and re-issues mutations to the supergraph. Server:
  `academy.graphqls:694` `upsertChapterProgress`, `:696` `upsertChapterProgressBatch`, `:698`
  `upsertChapterProgressBatchTolerant`, plus `setLastActivity` (resolver at
  `resolver_academy.go:175`). Tables `academy_chapter_progresses` and `academy_last_activities` both
  appear in `app/terraform/migrations/`, and `app/cmd/academy-seed` exists.
- **ENUMERATED** — the Tech-Stack row, item by item against source: `proxy.js` exists and
  `middleware.js` does **not** (the Next-16 rename is real); `next ^16.2.6`, `react ^19.2.5`,
  `babel-plugin-react-compiler ^1.0.0` with `reactCompiler: true` + `turbopack` in `next.config.js`;
  `marked ^17`; `@clerk/nextjs ^6.39.2`; expo `~54.0.33`; `engines.node ">=22"`;
  `vercel.json` is exactly `{"framework": "nextjs"}`; `layout.jsx:132` is
  `manifest: "/academy-manifest.json"` and that file declares `display: standalone`; the FA Pro icons
  really are vendored (`code/public/assets/fontawesome/{css,webfonts}` with `*.woff2` present), so the
  "token-less install works" claim holds; the `_meta` → `meta` rewrite really is in
  `next.config.js:137,141`, which is why both spellings are in the matcher.
- **ENUMERATED** — `:34`. `src/components/RegisterServiceWorker.jsx` is a kill-switch: its own header
  says *"This component no longer REGISTERS anything"*, and it deletes
  `OFFLINE_CACHES = ['academyChapters', 'academy-courses']`.
- **ENUMERATED** — `:26` / `:294` / `:424-427`. ant-academy is absent from `platform/repos.yml`, and
  `rosetta-extensions/demo-stack/ensure-clones.sh:150` is the labelled *"(d2) ant-academy explicit
  clone (M49 #5)"* phase, non-fatal, with the "absent from repos.yml so make init never clones it"
  rationale in-script.
- **ENUMERATED** — `:409`. Cosmo is OpenAI-direct: `ucourses/ucourse-engine/assistant/agent.js:12`
  `API_URL = 'https://api.openai.com/v1/responses'`, `:13` `MODEL = 'gpt-5.2'`, `:32`
  `localStorage.getItem('openai_api_key')`.
- **RE-DERIVED** (weaker, and labelled as such) — `:247`'s *"1000+ Vitest tests"*. I counted
  `it(`/`test(` call sites under `tests/` and got ~2850, which clears "1000+" but is a proxy for the
  runtime count rather than the count itself; I did not run the suite.

### `studio-desk.md`

- **ENUMERATED** — `:21`'s compose anchors, all four, at the refs named. At `0dab54d`,
  `docker-compose.yml:223-225` is `depends_on: / backend: / condition: service_started` and `:226` is
  `profiles: [studio-desk, all]`; `:204` is the `VITE_GRAPHQL_ENDPOINT` build arg defaulting to
  `http://localhost:8082/graphql/query`. The `cms` clause is pinned to `2adcf71` and is true there:
  `git show 2adcf71:docker-compose.yml` lines `337-341` are the `depends_on` block containing both
  `backend:` and `cms:`, and `d11a403` is the commit that removed it. The `depends_on`-without-
  `backend` failure mode is structurally confirmed (`frontend`, `studio-desk` and `messenger` each
  select a service declaring `depends_on: backend` that their own profile does not select).
- **ENUMERATED** — `:34`'s entry list, taken from `vite.config.ts` `rollupOptions.input` rather than
  from the doc: `home.html` (as `main`), `sim-advanced-builder`, `builder-skill-path`,
  `simulation-builder`, `sim-guided-builder`, `generation`, `catalog`, `academy`, `skills` — the nine
  the doc calls "the prod set" — plus `dev-accept` behind `...(isProduction ? {} : {…})`, exactly as
  described. (`app/index.html` exists on disk but is not an entry; the doc does not claim it is.)
- **ENUMERATED** — `:107`. `src/routes/` holds exactly the four files named (`ai.ts`, `dev.ts`,
  `skillpath.ts`, `youtube.ts`) and `skillpath.ts` is **61080 bytes** — "the largest backend route,
  ~61KB", literally. `pingSnapshotCapability` is exported from `skillpath.ts` and called at
  `src/index.ts:305` with a `directus_versions` write-permission warning at `:311-313`. The YouTube
  claim is line-exact: `src/routes/youtube.ts:43` is `const apiKey = process.env.YOUTUBE_API_KEY;`
  with the `_mock: true` fallback immediately below, and I read the surrounding 20 lines to confirm no
  second credential is consulted. `GCLOUD_SERVICE_ACCOUNT` is at `.env.example:120` and
  `terraform/main.tf:129` — **both anchors line-exact** — and is read by no file in `src/`.
- **ENUMERATED** — `:112`'s AI table, read from `src/services/ai/config.ts` rather than recomputed
  from the doc. The four tiers exist as claimed; OpenAI **and** Azure default maps are both
  `gpt-5.2 / gpt-5.2 / gpt-5-mini / gpt-5-nano` (`:20-32`); Anthropic is
  `claude-opus-4-5-20251101 / claude-sonnet-4-5-20250929 / claude-haiku-4-5-20251001` (`:34-38`);
  `.env.example:57` `AI_PROVIDER_CHAIN=azure-openai,openai` and `:61` `AI_DEFAULT_TIER=fast`;
  in-code fallback is `(env.AI_DEFAULT_TIER as ModelTier) || 'thinking_fast'` at `config.ts:182`.
  **False-absence caught here:** a naïve `grep AI_DEFAULT_TIER` "matches" a dozen times inside
  `OPEN**AI_DEFAULT_TIER**_MODELS` — the substring trap §5 rule 3 warns about. Word-bounded, it
  resolves to exactly two real sites, both as documented.
- **ENUMERATED** — `:305`. `STUDIO_ACCESS_ROLES` at `src/index.ts:96` is exactly
  `['admin', 'org:admin', 'content_creator', 'org:content_creator']`; `checkEnterpriseAndAdmin` at
  `:99`; the non-eligible redirect reads `process.env.WEB_APP_URL` at `:119`; `MOCK_CLERK` swaps the
  gate for `mockPassthrough` at `:133`.
- **ENUMERATED** — `:161` / `:172` / `:258`. `engines.node ">=24"` **and** `FROM node:24-alpine` in
  both `Dockerfile` and `Dockerfile.dev`. The PORT fallback really is the counter-intuitive 9100:
  `src/index.ts:60` `const backendPort = process.env.PORT || 9100`. In production
  `frontendPort = backendPort` (`:62`), which is what makes "serves from :9000" true.
- **ENUMERATED** — `:279-287`, the no-datastore claim, which is a negative and therefore the one most
  worth checking. `package.json` declares none of `pg`/`postgres`/`prisma`/`sqlite`/`mysql`/`mongo`/
  `knex`/`drizzle`/`typeorm`/`sequelize`; `app/services/studioDB.js` exists and is a 9.6 KB facade
  alongside `graphql/` and `content/`. `app/core/main.ts:105` is the sole tailscale-funnel mention and
  is the GlitchTip DSN comment (`:107` carries the `singularity-obs.taildc510.ts.net:10000` URL) —
  the doc points at the comment that makes the identification, which is the right construct.
- **ENUMERATED** — `:355` and `:366`. `aiService.getCompletion` loops `for (let i = 0; i <
  orderedProviders.length; i++)` with `continue` on a failed provider — "loops every configured
  provider within one request" is exact, not a paraphrase. Both named regression tests exist:
  `stack-injection/tests/test_injection.py:1654`
  `test_studio_desk_block_shape_single_port_clerkenstein_wired` and `:1711`
  `test_studio_desk_env_clerkenstein_no_mock_and_offset_sign_in`.
- **ENUMERATED** — `:388-393`, the M253 boot model. `preloadCriticalCSS();` is at
  `app/core/main.ts:97` and `new PageWrapper();` at `:206`, both line-exact, and there are exactly
  **three** blocking `await`s between them (`clerk.load()` inside `withClerkErrorHandling(…,
  { timeout: 10000 })` at `:182`, `l12nService.init()` at `:191`, `userService.canAccess()` at `:199`)
  — I counted the awaits in the function rather than accepting the diagram. The 10 s figure the doc
  parenthetically excludes is real and is the wrapper's timeout, so the "NOT its 10 s timeout"
  caveat is doing real work.
- **ENUMERATED** — `:56-93`, the project-structure block, `ls`'d directory by directory:
  `src/{index.ts,routes,services,prompts,lib,types}` with `lib/devLogin.ts` the only helper and
  `types/*.d.ts` ambient; `src/prompts/{start,sim-advanced-builder,sim-guided-builder,
  builder-skill-path,documents}` + loose `*.md` — the exact per-builder dir set claimed;
  `app/public/{fontawesome,l12n,templates,…}`; `tests/{e2e,frontend,integration,unit,utils}` — all
  five. `app/public/l12n/` holds **7** JSON dictionaries (de en es fr it ja nl), which is `:120`'s
  "Multi-language support (7 languages)".

### `chronos.md`

- **ENUMERATED** — the decommission banner, which is the only part of this file ground truth can
  reach. `045857c` in `platform` is *"chore: remove chronos service from orchestration and update
  related documentation"*; `chronos` appears **0** times in the current `docker-compose.yml`,
  `common.yml`, `repos.yml` and `Makefile` (with a positive control in the same command); no
  `stack-*/chronos` directory exists; and `chronosv1`/`ChronosService`/`TimerWentOff` appear in **0**
  `.go` files in `app`. The Asynq successor is real: `app/internal/jobsimwiring/worker.go` imports
  `github.com/hibiken/asynq` at `:24` and `StartWorkers` runs from `:27`, so the doc's `:24-35` window
  brackets the registration; the timeout use case is visible in the handler set
  (`JobsimulationValidateTimedOutSessionV3`, `jsqueues.TimersQueue`). `:186`'s "no such service" is
  therefore correct.

## What I could not settle

- **`shared_libraries.md:77-83` — "12 Connect-RPC services are defined".** The `proto` repo is not in
  the ground-truth clone set and is not in the local Go module cache (only `ai@v1.40.1` and
  `colony@v0.34.3` are), so I could not enumerate the `.proto` service definitions. I deliberately did
  **not** clear it on the strength of counting the twelve names in the sentence — that is the rule-24
  attractor exactly, and B1 is the same file's other instance of it. What settles it: a `proto` clone
  at the ref `app` pins (`v1.210.0`) and `grep -rn "^service " proto/**/*.proto`. The named services I
  *could* corroborate indirectly all resolve — `app` imports `cmsv1connect`, `jobsimulationv1connect`,
  `labv1connect`, `skillerv1connect`, `storagev1connect`; `AuthorizationService` is reachable through
  `colony/authorization`'s `NewSentinelAuthorizer`.
- **`shared_libraries.md:188` — "a **131-line** node-id library (`node.go`)", and `:224-229`'s ID
  format / worked examples (`K-GOXXXX-F63F`, `K-DOTNET-DDE9`, `K-CSHAXX-5F5B`).** No `taxonomy` clone
  and none in the module cache. Settled by a clone at `v1.2.0` plus `wc -l node.go` and a table-driven
  run of the three constructors.
- **`shared_libraries.md` colony/ai behavioural claims are graded at the CACHE's versions, not the
  pins.** Everything I verified for colony was read at `v0.34.3` (sentinel/storage's pin) and for ai
  at `v1.40.1`, while `app`/`messenger` pin `colony v0.35.2` and every ai consumer pins `v1.40.2`.
  The claims all held where I could read them, but a change in the intervening versions would not be
  visible to me. Settled by fetching the pinned versions.
- **`chronos.md:13-245` — essentially the entire technical body.** Ports 8080/8081, `search_path=chronos`,
  Go 1.25, sqlc-not-Ent, Cobra, the `timers` DDL and its four indexes, the `LIMIT 10` +
  `FOR UPDATE SKIP LOCKED` triggerer with a 3 s per-run timeout and a 1-minute Sentry heartbeat, the
  `--interval`/`--server` flags, and the six env vars are all unverifiable without a `chronos` clone,
  which no longer exists on this box and which the doc itself says is no longer cloned. I found no
  internal inconsistency in them (ports, flags and env vars agree across the file's four tables), but
  agreement is not verification. Settled by cloning `anthropos-work/chronos` at the sha the platform
  last built.
- **`ant-academy.md:74` — "a grid of 65 real cards"** (and the paired "0 Draft chips").** This is a
  live measurement on `billion` behind the `academy-fs-published-*` demo-patches, not derivable from
  the checkout. For calibration I counted the committed content tree: **19** series directories and
  **78** skill-path directories, neither of which is 65 — but the rendered grid is filtered
  (public/tier/tenant) so 78 ≠ the card count and the mismatch is not evidence either way. Settled by
  a live `/` render on a patched demo with a card-count assertion, which is what
  `autoverify.sh` assert *(f)* claims to do.
- **`studio-desk.md:132` / `:164` — "the Cosmo Router survives in production only".** I confirmed the
  router's prod config exists and listens on `:8080/graphql`, and that it is gone from local compose.
  Whether it is *deployed and serving* in production is not observable from these clones; the strongest
  local evidence is `graphql-wundergraph/terraform/locals.tf:8` and `supergraph-config-prod.yaml`,
  neither of which proves a running service.
