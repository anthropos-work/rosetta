# iter-214 — decisions

## `D-M257x-214-1` — the census lands as a running ARM, not as a widening of the guard's verdict

The finding is a **population** gap, and the two available vehicles are not equivalent. Widening
`claim_twin_guard.scan_targets` would change a shipped fence's RED surface to span a **second repo**
that is git-ignored in rosetta and absent on a fresh clone — a capability probe that fails OPEN disarms
the check it guards (`§5`). The arms live in `test_claim_twin_guard.py` instead, **inside the repo they
census**, so there is no presence probe to fail open and no conditional to go silent.

They run the guard's own `derive` / `normalize_document` / `find_form` / `_looks_retracted`. Only the
source set varies — iter-209's discipline: *when the question is about scope, hold the machinery fixed.*

## `D-M257x-214-2` — widening to `*.py` is REFUSED, and the refusal is re-derived on every run

**186 Python modules, 10 findings, 7 outside a retraction context.** Inspected, the bare ones are
fences **quoting the very claim they exist to catch**, inside their own rationale (`claim_ledger.py`,
`derived_value_guard.py`, `value_change_guard.py`). iter-209's precondition for widening any fence's
population is **zero false REDs measured first**; this half does not meet it.

The refusal is **not** a comment. `test_the_PYTHON_half_is_measured_and_its_refusal_is_SIZED` asserts
that such sites still exist, so the day the tooling stops quoting its subjects the arm goes RED and the
refusal must be revisited. `§5` — *a refusal gate is a derivation too*, and *a waiver outliving its
subject reads as coverage*.

## `D-M257x-214-3` — the limit on what this iter may claim

**iter-212's defect would not have been caught by this fence even widened.** That claim was retracted in
a **route ledger**; `claim_ledger` derives its 264 claims from the audits' **blocker-ledgers**, so it
was never in the population at any scope. Recorded in the arm's own docstring rather than only here,
because the arm is what a future reader will find. The route
`SURVEY-M257x-iter212-a-retraction-does-not-reach-the-code-that-acts-on-it` therefore stays **open** —
this iter censused a neighbouring population, it did not close that route.
