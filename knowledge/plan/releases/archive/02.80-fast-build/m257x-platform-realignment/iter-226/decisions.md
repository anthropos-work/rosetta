# iter-226 — decisions

## D-M257x-226-1 — the residual prose is COUNTED, not silently left

Two of the ~15 live `odysseus` claims were repaired; **~13 were not**, and the close section says so with
the count and the reason.

The two repaired are **actionable** — a headline assertion in the file every session loads, and a
copy-pasteable command. The residual is the doc's comparative **argument**, which is anchored to a host
throughout. Re-anchoring it to the sanctioned dev host would attach a live argument to a machine with **no
measured baseline** — swapping one un-grounded host for another, against this doc's own standing rule that
a wall-clock figure never transfers between hosts.

Routed as `ROUTE-M257x-226-build-budget-argues-for-a-retired-host`, explicitly **blocked behind**
`ROUTE-M257x-225-no-profile-for-sanctioned-host`. Not a deferral by patience: it is Fate 2 — the work has
a named prerequisite that is itself the gate-clause-1 blocker.

## D-M257x-226-2 — this iter's own sealed figure was published wrong, and the correction is published too

The seal recorded *"26 in build-budget.md"* from `grep -rc`, which counts **lines containing a match**.
Occurrences are **26 before iter-225 and 32 now** (iter-225's banner added 6).

The total (27) survives by coincidence; the per-file basis does not. Corrected in the close section with
the mechanism named rather than the number quietly restated — the same handling iter-224 gave its own
150/123 → 92/78 correction, and for the same reason: a figure whose derivation is wrong is wrong even when
it happens to land on the right value.

## D-M257x-226-3 — a fence for supersession is NAMED but not built here

Lesson 1 proposes a decidable check: a decision entry marked `SUPERSEDES <id>` whose superseded `<id>`
still appears **unqualified** in `corpus/`. That is mechanically decidable and would have caught this class
on 2026-07-31 rather than on 2026-08-09.

It is **not built in this iter**: the user's redirect ranks corpus and working-stack work above the
instruments that grade them, and it is a third line of investigation beyond this iter's census + repair
scope. Recorded here so the proposal is not lost with the lesson.
