# Seat D — reading #14

Refs at the moment of measurement (re-derived at open, re-checked at close):
`app` `b948604ff` · `platform` `0dab54d` · `next-web-app` `bb3313bc0` · `cms` `ca50c81` ·
`roadrunner` `87d8d44` · `graphql-wundergraph` `60c229f` · `studio-desk` `14a5442` ·
`sentinel` `88bc559` · `storage` `4ce8ece` · `messenger` `fa47850` ·
`rosetta-extensions` authoring copy at `.agentspace/rosetta-extensions` (`main`).

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/services/studio-room.md` | 473 | yes |
| `corpus/services/clerkenstein.md` | 366 | yes |
| `corpus/services/cms.md` | 259 | yes |
| `corpus/services/roadrunner.md` | 171 | yes |
| `corpus/services/next-web-app.md` | 126 | yes |
| `corpus/services/gotenberg.md` | 82 | yes |
| `corpus/services/intelligence.md` | 18 | yes |

(Also read, as instructed and in scope: `corpus/ops/platform-alignment.md` §5, lines 240–1147.)

---

## Blockers

### B1 — `roadrunner.md` asserts jobsimulation is still in `repos.yml` and `docker-compose.yml` and starts on a bare `make up` — none of it is true at `0dab54d`

- **Anchor:** `corpus/services/roadrunner.md:28-33` —
  > "**`jobsimulation` was previously listed above as also *removed* from both files; it is not (corrected
  > M257x iter-46).** Its GitHub repo IS archived (2026-07-31), but it remains in `repos.yml:17` **and** in
  > `docker-compose.yml:83` with `profiles: [graphql, jobsimulation, all]` — so its container **starts on a
  > bare `make up`**."
- **Why it is false:** at `platform` `0dab54d` (the checked-out ref; this passage carries no ref pin of its own):
  - `platform/repos.yml` has **6** entries — `app, sentinel, storage, messenger, next-web-app, studio-desk`
    (YAML-parsed). `jobsimulation` is not one of them. `repos.yml:17` is `    migrations: false` under the
    **sentinel** entry.
  - `platform/docker-compose.yml` declares **8** services (`yaml.safe_load`): `backend, customerio-sync,
    gotenberg, messenger, next-web-app, sentinel, storage, studio-desk`. There is no `jobsimulation`
    service. `docker-compose.yml:83` is `      - STORAGE_S3_BUCKET=production-storage2024…` inside the
    **backend** service.
  - There is **no `graphql` profile** anywhere: the only `profiles:` keys at `0dab54d` are
    `[core, backend, all]` (×2), `[storage-legacy]`, `[customerio-sync, all]`, `[messenger]`,
    `[studio-desk, all]`, `[frontend, all]`. Platform `0dab54d`'s own subject line is
    *"chore(compose): run without the standalone storage; rename graphql -> core"*.
  - `repos.yml:4-8` states the opposite in the platform's own words: *"skiller, skillpath, roadrunner,
    jobsimulation and cms … Those five repos are frozen legacy: they own no local schema, no compose
    service and no clone entry here."*
  - The three anchors were correct at `2adcf71` (`git show 2adcf71:repos.yml` line 17 = `- name:
    jobsimulation`; that compose did carry the container). The passage was written against that ref and
    never re-pinned — §5 rule 31/33.
  - It also **self-contradicts this same file**: `:57` says the `graphql` profile "which `0dab54d` renamed
    `core`", and `:15` says there is no roadrunner compose service "at platform `0dab54d`".
- **Confidence:** high

### B2 — `roadrunner.md` says roadrunner is "still in `repos.yml` (1 of the 9 repos)"

- **Anchor:** `corpus/services/roadrunner.md:25-26` —
  > "roadrunner is **still in `repos.yml` (1 of the 9 repos — the count dropped when platform `2adcf71`
  > deleted the router entry)**"
- **Why it is false:** unpinned present tense; graded at the checkout `platform` `0dab54d`, `repos.yml`
  contains **6** entries and roadrunner is not among them. Platform commit `d11a403`
  (*"chore(compose): drop roadrunner, prune dead env, repoint messenger"*, 2026-08-03) says in its own body:
  *"roadrunner is folded into app (backend calls Judge0 directly) and **its repos.yml clone entry was already
  gone**, so the ../roadrunner build context could no longer resolve."* The "9 repos" figure was true at
  `2adcf71` (9 entries there, verified by `git show 2adcf71:repos.yml`) and has not been true since.
  Contradicts `corpus/services/cms.md:12-13`, which correctly states the 6-entry set.
- **Confidence:** high

### B3 — `roadrunner.md` cites `repos.yml:29-31` for a string that is not there; that anchor is the `studio-desk` entry

- **Anchor:** `corpus/services/roadrunner.md:14` —
  > "while `repos.yml:29-31` says *"legacy — folded into app"* and there is **no `roadrunner` compose
  > service** at platform `0dab54d` to start locally"
- **Why it is false:** the sentence names `0dab54d`, so the pin's own block is graded there (§5 rule 33).
  At `0dab54d`, `platform/repos.yml:29-31` is
  ```
  29    - name: studio-desk
  30      type: node-npm
  31      migrations: false
  ```
  `grep -n legacy repos.yml` returns exactly one line — `:8`, `  # legacy: they own no local schema, no
  compose service and no clone entry here.` The quoted *"legacy — folded into app"* string exists **only**
  at `2adcf71` (`git show 2adcf71:repos.yml:31` = `    migrations: false # legacy — folded into app; backend
  calls Judge0 directly`). The anchor resolves but names the wrong construct — rule 21's second class.
- **Confidence:** high

### B4 — `roadrunner.md`'s ports 10400/10401 are attributed to a compose service that no longer exists; the code's own defaults are 8080/8081

- **Anchors:**
  - `corpus/services/roadrunner.md:56` — "**Ports**: 10400 (HTTP — `/_meta` health only), 10401
    (Connect-RPC) — same on host and inside container per `platform/docker-compose.yml` (`PORT=10400`,
    `RPC_PORT=10401`)"
  - `corpus/services/roadrunner.md:148-149` — "| `PORT` | `10400` | … |" / "| `RPC_PORT` | `10401` | … |"
    (a column headed **Default**)
- **Why it is false:** `grep -rn '10400\|10401' docker-compose.yml common.yml .env_example Makefile` at
  platform `0dab54d` returns **nothing** (exit 1) — the values were compose-supplied and went with the
  service `d11a403` deleted (they are present at `2adcf71`: `docker-compose.yml:298` `- PORT=10400`,
  `:302` `- RPC_PORT=10401`). The roadrunner binary's **actual** defaults are different:
  `roadrunner/cmd/root.go:84` `rpcPort := cmp.Or(os.Getenv("RPC_PORT"), "8081")` and
  `roadrunner/cmd/root.go:110` `port := cmp.Or(os.Getenv("PORT"), "8080")`.
  Consequence for a reader doing real work: the doc's own "Run natively" recipe (`:118-127`,
  `go run main.go` with the platform `.env` sourced — which sets neither var, verified) followed by the
  smoke test at `:135`/`:140` (`buf curl http://localhost:10401/…`) targets a port the process does not
  bind. `:56` also contradicts `:57` one line below ("**none — there is no `roadrunner` compose service**").
- **Confidence:** high

### B5 — "`0dab54d`'s compose declares nine services" — it declares 8 (or 10 with the `include:`d file); nine is not derivable

- **Anchor:** `corpus/services/cms.md:35` —
  > "`0dab54d`'s compose declares nine services and `cms` is not one of them."
- **Why it is false:** `yaml.safe_load` at `platform` `0dab54d`:
  `docker-compose.yml` `services:` = **8** (`backend, customerio-sync, gotenberg, messenger, next-web-app,
  sentinel, storage, studio-desk`); `docker-compose.yml:1-2` is `include: - common.yml`, and `common.yml`
  `services:` = **2** (`postgresql, redis`). So the resolved compose is **10**; the single file is **8**.
  There is no reading that yields 9 (the only other top-level key is `networks: app-network`).
  The predicate the count supports (*`cms` is not one of them*) is **true** — this is exactly §5 rule 17's
  shape, a correct claim resting on a wrong count. The same wrong figure is repeated verbatim at
  `corpus/architecture/external_services.md:296` and `corpus/services/jobsimulation.md:35`, so it is a
  corpus-wide scalar, not a typo.
- **Confidence:** high
- **Why I hesitated:** the conclusion drawn from the count is correct, so a reader is not misled about
  `cms`. Booked as a BLOCKER because it is a derived scalar contradicting source at the very ref it names,
  and a reader who *does* count (10 containers on `make up PROFILE=all`) is told the doc is wrong about
  something it presents as measured.

### B6 — `cms.md` lists `python-docx` as a studio-room requirement; it is in no `requirements.txt`

- **Anchor:** `corpus/services/cms.md:115` —
  > "`requirements.txt         openai, anthropic, mistralai, rich, pyyaml, python-docx, requests, jinja2,
  > pytest, pytest-asyncio (see studio/requirements.txt)`"
- **Why it is false:** `stack-demo/cms/studio/requirements.txt` (the file the line cites, at `cms`
  `ca50c81`) reads, in full: `openai, anthropic, rich, pyyaml, requests, jinja2, mistralai, pytest,
  pytest-asyncio` — **9 entries, no `python-docx`**. Identical content in the live copy
  `stack-demo/app/studio/requirements.txt` at `app` `b948604ff`. `grep -rni docx studio/` in the cms repo
  hits only prose inside generated-simulation fixtures and a prompt string
  (`studio/agents/simulation/guidelines.py:48`) — no dependency declaration anywhere.
  It also **contradicts `corpus/services/studio-room.md:345-356`**, which lists the same file correctly
  (and without `python-docx`).
- **Confidence:** high

---

## Minors

- `corpus/services/clerkenstein.md:18` — the `rosetta-extensions` sections are enumerated as
  "(`clerkenstein`, `demo-stack`, `stack-injection`, `stack-core`, `stack-seeding`, `alignment`)". The
  authoring copy has **11**: `alignment, clerkenstein, demo-stack, dev-stack, playthroughs, stack-core,
  stack-injection, stack-secrets, stack-seeding, stack-snapshot, stack-verify`. Omitted list members
  (`dev-stack`, `playthroughs`, `stack-secrets`, `stack-snapshot`, `stack-verify`). Root `CLAUDE.md`'s own
  list is also short (9 of 11) — same class, different site.
- `corpus/services/clerkenstein.md:101` and `:279` — `cmd/` is described as holding `mintpk` /
  `fake-fapi` / `fake-bapi`; `ls clerkenstein/cmd` returns **four**: `fake-bapi, fake-fapi, jwtkey, mintpk`.
  `jwtkey` is unmentioned.
- `corpus/services/clerkenstein.md:3` — "**Last updated:** 2026-07-14" while the body documents v2.8 M256
  (`signedOut` sticky flag, `TestServer_seatSwitchIsNotASignOut`) and v2.8 M257x iter-23 — all later work.
  Metadata staleness only; every substantive claim I checked in this file verified.
- `corpus/services/next-web-app.md:38-43` — the shared-packages table lists `@anthropos/graphql`,
  `@anthropos/ui`, `@anthropos/core-js`, `configs/*`; `ls packages/` at `next-web-app` `bb3313bc0` also has
  **`design`** (`packages/design`). Omitted list member.
- `corpus/services/next-web-app.md:82` — "`make up-frontend`  # next-web-app together with the **graphql**
  backend stack". The target at `platform` `0dab54d` `Makefile:119-120` is
  `$(COMPOSE) --profile core --profile frontend up …`. The command is right; the retired `graphql` token
  in its description is stale (§5 rule 30's naming half).
- `corpus/services/roadrunner.md:122` — "`make dev S=roadrunner`". `dev` iterates repos from `repos.yml`,
  which no longer contains `roadrunner`, and no `roadrunner` container exists to stop. Historical, and the
  file's banner covers it, but the recipe is not runnable.
- `corpus/services/studio-room.md:312` — "the in-code fallback is a literal `worklog/`". `gen.py:106` is
  `worklog_path = request.get('worklog_path', 'worklog')` — no trailing slash. Cosmetic.
- `corpus/services/intelligence.md:5` vs `:12` — "removed … in **mid-2026**" / "Historical Role
  (**pre-2026-Q2**)"; platform `fdfa189` is dated **2026-04-17**, i.e. inside Q2. Trivial internal
  inconsistency.

---

## Positively cleared (audited zeros)

**`studio-room.md` — ENUMERATED unless noted.**
- `gen.py:484-492` "**exactly nine** arguments" — ENUMERATED: `grep -n add_argument gen.py` returns exactly
  9 lines, `484`–`492`, and they are the 9 flags named. No 10th `add_argument` anywhere in the file.
- "`--template` sets a key **nothing in the codebase ever reads** (grep: zero consumers)" — ENUMERATED:
  `grep -rn template --include='*.py'` over the whole tree returns only (a) `translate_legacy_blueprint`'s
  own pop/warn sites, (b) the argparse *description* string at `:483`, (c) tests, (d) unrelated prose in
  `errors.py`/`benchmark/`/`story.py`. No read of `request['template']`.
- `parse_argument` `gen.py:18-28` (`parse_known_args` + unknown-pair merge) — ENUMERATED, exact.
- `translate_legacy_blueprint` `gen.py:205-238` — ENUMERATED: `_LEGACY_TEMPLATE_DEFAULTS` at `:205-209`
  holds **exactly three** legacy names (`micro challenge`, `scenario challenge`, `collaborative
  challenge`); the function is `:212-238`; the non-matching warning string is verbatim
  *"is ignored; asset type is now inferred from task interactions"* at `:227-228`.
- `validate_blueprint_exclusivity` `gen.py:241-271` — ENUMERATED: the `allowed_with_blueprint` set is
  exactly `{blueprint, media, forced, interactive, branch, pipeline, attachments_path, worklog_path,
  postgen_path, published_path}` = execution controls + the four `*_path` overrides, as the doc says at
  `:281-283`; error text matches verbatim.
- `setup_generation_request` `gen.py:273-282`; work_paths `gen.py:450-456`; `GenMode` five members +
  `DEFAULT = EXECUTION` (`services/ai.py:35-42`) — ENUMERATED.
- postgen targets — ENUMERATED: `agents/simulation/postgen/__init__.py:56-74` dispatches exactly
  `toolkit`/`guidance`/`metadata`/`translation`; `testing.py` exists as a module but is not dispatched;
  no `export`/`zip` target. `postgen.py:396-398` marks `--media`/`--simid`/`--target` `required=True`.
- exporter — ENUMERATED: `exporter.py:518-519` are the two zips; `:514-550` is
  `_create_export_package` (the file has 549 newlines and a final unterminated line 550);
  `simulation.json` is written into the scratch dir at `:541` and `rmtree`'d at `:550`; zip name keyed on
  `source_id` falling back to sim id (`:517`).
- `requirements.txt` — ENUMERATED, exact 9-entry match, `asyncio`/`aiohttp` correctly absent.
- configs — ENUMERATED: `max_tokens = 4000` in **all three** tracked configs
  (`config_template.ini:22`, `development_config.ini`, `production_config.ini`); the five
  `*_AI_STABLE_MODEL` rows and their `*_EXPERIMENTAL_` twins match the doc's values
  (`azure, gpt-5-mini/gpt-5.4`); the env-override key list at `gen.py:45-53` is exactly the six
  `{AZURE,OPENAI,ANTHROPIC}_{API_KEY,ENDPOINT}` names.
- Asynq wiring — ENUMERATED: `app/internal/cms/worker/worker.go:29` `Concurrency: 5`; `:31-34`
  `Queues{ai_video:7, studio:3}`; `queues/queues.go` defines exactly those two.
- `app/internal/cms/studio/studioManager.go:119` (`studio/gen.py` invocation) and `:94-96`
  (`studio/studio-venv` consts) — ENUMERATED, exact.
- blueprint fixture — ENUMERATED against `tests/e2e/blueprints/technical.json`; every quoted key/value
  matches; the five e2e blueprints and three `knowledge/…/asset-examples/blueprints/` files match the
  doc's enumeration exactly.
- `services/ai.py:1-2, 383, 530, 664` (OpenAI/AzureOpenAI/Anthropic client construction) and
  `agents/simulation/model.py:59, 467-469` (the taxonomy memo) — ENUMERATED, exact.
- `python:3.11-slim` (`app/Dockerfile:28`) and `additional_repo: "anthropos-studio-room:studio"` at
  `app/.github/workflows/build-production.yml:29`, landing in `v1.360.1` per `app/CHANGELOG.md:132-134` —
  ENUMERATED.

**`clerkenstein.md` — ENUMERATED unless noted.**
- All five DNA gene/capability counts — ENUMERATED by parsing the JSON: `clerk-2.6.0` **14 caps / 27
  genes**, `clerk-js-5` 9, `clerk-multi-1` 9, `clerk-express-1` **13 genes / 5 caps**, `clerk-deploy-1` 7.
  Every figure in the score table and in the "Driven to the gate across" paragraph matches.
- `alignment/cmd/alignctl/run.go:134-135` `ExitRegressed = 2` / `ExitUnmeasurable = 3` — ENUMERATED, exact.
- `clerk-backend/store.go:138` `SeedOrgIdentity` / `:151` `LookupOrgEid` — ENUMERATED, exact; both ship the
  roster org UUID as claimed.
- `alignment/dna/clerk-2.6.0.json:131` — ENUMERATED: line 131 is the `MembershipOrgIdentity` description
  and contains *"M219 landed the fix … taking the Go surface 97.2% -> 100%"* verbatim.
- The `/v1/me/organization_memberships` FAPI twin — ENUMERATED: registered at
  `clerk-frontend/server.go:186`, handler at `:500`, tests in `clerk-frontend/meorgmemberships_test.go`
  covering the paginated envelope / limit+offset / 401 cases the doc names.
- `alignment/dna/clerk-js-5.json` has a `Me` capability (`GET /v1/me`) and **no** gene for the memberships
  route — ENUMERATED by listing all 6 capabilities and their 9 variants.
- Single-tenant seat model — ENUMERATED: `clerk-frontend/registry.go:24` single `activeKey`, `:67`
  `active()`, `:75` `Select()`; `server.go:125` `clientID: "client_clerkenstein"`, `:665`
  `sessID = "sess_clerkenstein"` in `establishLocked`. **`r.Cookie(` appears nowhere** in
  `clerkenstein/` — `grep -rn '\.Cookie(' --include='*.go'` exits 1, with a positive control (`grep -rn
  'func '` = 633 hits, `grep -rni cookie` = many) proving the pipeline works.
- All five `server_test.go` test names at `:202-205` — ENUMERATED, all present (`:256, :286, :390, :427,
  :461`).
- `clerk-frontend/server.go:35-67` M220 block — ENUMERATED: `clerkJSFetchTimeout = 15 * time.Second`,
  `clerkJSClient` commented *"Explicitly NOT http.DefaultClient"*, `FAKE_FAPI_CLERKJS_CACHE` at `:72`,
  `defaultClerkJSCDN = "https://cdn.jsdelivr.net"` at `:22`, `FAKE_FAPI_CLERKJS_CDN` override at `:29`.
- Roster org threading — ENUMERATED: producer `stack-seeding/seeders/roster.go:53-54` (`org_name`/
  `org_slug`), `:77` (`org_is_hiring,omitempty`), filled at `:139-140` from `st.Org.Name` +
  `orgSlugFor`; consumer `clerk-frontend/registry.go:111,125-126,144,156,190` (`DisallowUnknownFields`);
  defaults `resources.go:50-51` `"Clerkenstein Demo Org"` / `"clerkenstein-demo"`; conditional emit
  `resources.go:274-277` (`if u.OrgIsHiring { orgPublicMeta["isHiring"] = true }`).
- The colony/clerk-sdk drift box at `:270-275` — ENUMERATED: `app/go.mod:16` `colony v0.35.2` and `:31`
  `clerk-sdk-go/v2 v2.7.0`; `sentinel/go.mod:8` and `storage/go.mod:7` both `v0.34.3`; the artifact pin
  `v0.34.3` appears at `alignment/dna/clerk-deploy-1.json:6`, `deployrun/main.go:6` and five KB sites.
  (Note: `messenger/go.mod:7` is also `v0.35.2` — not claimed either way here.)
- `dotless-pk-rejected` gene at `alignment/dna/clerk-express-1.json:24` — ENUMERATED.
- `clerkenstein/.github/workflows/alignment.yml` is git-tracked (`git ls-files` hit) and the
  `alignment/` harness section has **no** `scripts/` dir (`ls` = README, cmd, examples, go.mod, internal);
  `ALIGN_DIR` default `$base/../../alignment` at `gate.sh:30` / `drift-check.sh:16` — ENUMERATED.
- studio-desk `STUDIO_ACCESS_ROLES` at `src/index.ts:96` and `app/services/userService.ts:16`, both with
  the quoted "Both the prefixed (`org:*`) and bare role keys are accepted" comment — ENUMERATED, exact.

**`cms.md`.**
- `repos.yml` "6 entries: app, sentinel, storage, messenger, next-web-app, studio-desk" — ENUMERATED (YAML
  parse). Correct.
- "four `*_RPC_ADDR` values and **all four** read `http://backend:8083`" — ENUMERATED:
  `grep -n RPC_ADDR` over `docker-compose.yml` + `common.yml` returns **exactly four** lines
  (`:173` BACKEND_USERS, `:174` CMS, `:176` JOBSIMULATION, `:183` SKILLER), all `http://backend:8083`; a
  wider `grep -n ADDR` (8 hits) confirms nothing else is an RPC address. `CMS_RPC_ADDR` is at `:174`.
- `cms/terraform/main.tf:19` `service_desired_count = 0` — ENUMERATED, exact.
- `app/main.go:1205-1211` — ENUMERATED: the *"Additive + DORMANT … until the M809 re-point"* comment is at
  exactly those lines at `app` `b948604ff` (v1.366.0), and it is indeed stale against compose.
- The six re-created tables — ENUMERATED from
  `app/terraform/migrations/20260724132049_cms_data_model.sql`: `similarities, studio_documents,
  studio_tasks, similarity_categories, similarity_features, similarity_skills` — the exact six named.
- `graphql-wundergraph@915da06` — ENUMERATED via `git show --name-status`: `D schemas/cms.graphqls` **and**
  `D schemas/jobsimulation.graphqls` in one commit; ladder commits `749dc86` (remove skiller) and
  `7c17e63` (fold skillpath) exist and are dated as the doc says. The 3→1 correction holds, and the
  commit's own subject really does say "2→1".
- `app/internal/skillpath/session.go:205-207` — ENUMERATED: the *"cms-in-app deseam: cms is in-process"*
  comment + `u.cms.GetSkillPathDomain(...)` at exactly those lines.
- `REDIS_CMS_CACHE_INDEX` default 5 (`app/main.go:997-998`), `POST /api/webhook/directus`
  (`internal/web/backend/backend.go:138, :324`) fail-closed on `DIRECTUS_WEBHOOK_SECRET`
  (`main.go:1088-1089`), `app/cms_reader_switch.go` present, all ten named `app/internal/cms/*` subdirs
  plus `wiring.go` present — ENUMERATED.
- `cms/go.mod:3` `go 1.26.4`; `cms/Dockerfile:2` `golang:1.26-bookworm`, `:23` `python:3.11-slim` —
  ENUMERATED, exact.
- The `bash -c` note at `:235` — ENUMERATED against the **cms** copy of `studioManager.go`
  (`cms/internal/studio/studioManager.go:98,100,951,953,967`), which is the repo that section is about.
  (§5 rule 36's two-copies trap: the `app` copy at `internal/cms/studio/studioManager.go:99-119`
  deliberately removed `bash -c` under M809b H-1; the doc is talking about the legacy repo and is right.)
- "There is NO cms profile … it exits 0 and starts postgresql, redis and sentinel" (`:203-204`) —
  ENUMERATED: those three are exactly the services with **no** `profiles:` key across the resolved
  compose. `Makefile:10` `PROFILE ?= core`, so `:202`'s "`core` profile" is right.
- `DIRECTUS_DATA_CONSUMERS` "now names both" (`:77`) — ENUMERATED:
  `stack-injection/gen_injected_override.py:84` `= ("cms", "backend")`.

**`roadrunner.md` — everything below the banner cleared.**
- `roadrunner/terraform/main.tf:19` `service_desired_count = 1` — ENUMERATED, exact; repo HEAD is
  `87d8d44` dated **2026-06-19**, as claimed.
- "no `ROADRUNNER_RPC_ADDR` / `RoadRunnerService` / `roadrunner:10401` read in any service's Go code" —
  ENUMERATED: `grep -rn` across `app, jobsimulation, messenger, sentinel, storage` exits **1**
  (positive control on the same pass: `JobSimulationService` in `jobsimulation` returns 1 hit).
  `ROADRUNNER_RPC_ADDR` is likewise absent from `docker-compose.yml`, `common.yml` and `.env_example`.
- `d11a403` really is the commit that dropped the compose service (verified by `git show --stat` + body).
- `jobsimulation/internal/runner/runner.go:1-3` header comment *"(formerly the standalone "roadrunner"
  service)"* — ENUMERATED, verbatim.
- Go 1.25 (`go.mod:3` `go 1.25.0`), `asynq v0.25.1`, `gorilla/websocket v1.5.3` — ENUMERATED.
- Async-task facts — ENUMERATED: `queues.DefaultQueue = "roadrunner:default"`,
  `tasks.RoadrunnerSubmissionResult = "roadrunner:submissionresult"` (the only two consts in those
  packages), `asynq.MaxRetry(3)` at `internal/runner/runner.go:126`, `Concurrency: 10` at
  `internal/worker/worker.go:25`, `maxRetries = 15` at `runner.go:244` with `time.Sleep(1*time.Second)` at
  `:273`, `RoadrunnerSubmissionCompleted` published at `:277-278`.
- Three RPC methods exactly (`Submission`, `SubmissionPackage`, `SubmissionResult` — `internal/rpcsrv`
  lines 20/43/60) — ENUMERATED. `internal/lsp` is referenced from nowhere in `cmd/`, `main.go` or
  `internal/rpcsrv/` — ENUMERATED (grep exits clean with a working pipeline).
- Zero `*_test.go` files (`find … | wc -l` = 0) and `Dockerfile:18` = `RUN go test -v ./...` — ENUMERATED.
- `"py"` not `"python"` (`internal/runner/languages.go:26`), `godotenv/autoload` (`main.go:9`),
  `strconv.Atoi(os.Getenv("REDIS_WORKER_INDEX"))` (`cmd/root.go:68`) — ENUMERATED.
- `JUDGE0_BASE_URL=http://52.48.139.23:2358` at `docker-compose.yml:59` and `JUDGE0_API_KEY` present only
  in `.env_example:87` — ENUMERATED, matching `:104` and `:151`.

**`next-web-app.md`.**
- Next `^16.2.7` in **all four** apps' `package.json` (ENUMERATED per app), React `^19.2.7`,
  `pnpm@10.30.3`, `engines.node ">=24.0.0"`, `turbo ^2.9.6`, `UPGRADE-IMPACT-next16.md` present.
- **8** locales — ENUMERATED: `ls configs/i18n/messages/` = `de en es fr it ja nl pt`, exactly the eight
  named.
- Ports — ENUMERATED from each app's `dev` script: web 3000, hiring 3001, integration 3002, maintenance
  none, mobile `expo start --port 3031` (`apps/mobile/package.json:70`); mobile excluded from the
  workspace (`pnpm-workspace.yaml` `- '!apps/mobile'`).
- `proxy.ts` in both `apps/web/src` and `apps/hiring/src`; **no** `middleware.ts` anywhere outside
  `node_modules` — ENUMERATED. Repo `CLAUDE.md:55` says the Next-16 rename verbatim; `CLAUDE.md:15` says
  "Next.js 16 App Router"; `CLAUDE.md:82-84` really does still claim "Two Dockerfiles" while only
  `Dockerfile.dev` exists — all three sub-claims ENUMERATED.
- Public allowlist — ENUMERATED against `apps/web/src/proxy.ts:7-58`; all seven routes the doc names are
  present (it says "includes", and it is a subset of 20).
- No `storybook` script (ENUMERATED over the 40 root scripts) and no `.storybook/` dir; the only trace is
  `configs/tailwind/storybooks.css` — exactly as claimed.
- Data layer — ENUMERATED: `packages/graphql/package.json` has `graphql-request ^7.4.0` +
  `@tanstack/react-query ^5.99.2` and **zero** apollo packages. `packages/ui` has `antd ^6.3.7`.
- Env defaults — ENUMERATED against `apps/web/.env.example`:
  `NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT=http://localhost:8082/graphql/query` (`:62`),
  `NEXT_PUBLIC_BACKEND_API_URL=http://localhost:8082` (`:63`),
  `GRAPHQL_SCHEMA_FOR_GEN=…/graphql/query` (`:36`), `NEXT_PUBLIC_HOSTING_URL=http://localhost:3000`
  (`:52`); compose bakes it at `docker-compose.yml:236` as the doc says.
- `Dockerfile.dev` `FROM node:24-alpine` + `pnpm turbo build --filter=@anthropos/web-app` (`:32`);
  `pnpm test` → `turbo test`, jest only in web + hiring (integration/maintenance have no `test` script) —
  ENUMERATED. `e2e/` Playwright suite with `E2E_TEST_EMAIL` (`e2e/auth.setup.ts:37`).

**`gotenberg.md` — clean, ENUMERATED end to end.**
`docker-compose.yml:255-268`: image `gotenberg/gotenberg:8`, the four command tokens verbatim,
`ports ["3200:3200"]`, `profiles: [core, backend, all]` at exactly `:268`.
`GOTENBERG_URL=http://gotenberg:3200` at `:57` in backend's env. `app/internal/converter/gotenberg.go`
read in full: `gotenbergClient = &http.Client{Timeout: 90 * time.Second}`, `ConvertToPDF(ctx,
gotenbergURL, document, filename) ([]byte, error)`, `POST gotenbergURL+"/forms/libreoffice/convert"`.
"the only consumer" — ENUMERATED: `grep -rl GOTENBERG` across all nine cloned repos hits `app` (2 files)
and nothing else.

**`intelligence.md` — clean, ENUMERATED.**
`fdfa189` (2026-04-17) is *"chore: remove intelligence service from local dev orchestration"* and touches
exactly `docker-compose.yml` (−25) and `repos.yml` (−3). `grep -rn intelligence` over
`docker-compose.yml`, `common.yml`, `repos.yml`, `Makefile` at `0dab54d` exits 1.

---

## What I could not settle

- **`cms.md:53-55`** — "`module.cms_euwest1` is **still declared** in
  `infrastructure/terraform/production/services.tf` … Teardown is **M810**." The `infrastructure` repo is
  not among the ground-truth clones; I have no way to open `services.tf`. Settled by a clone of
  `anthropos-work/infrastructure` at a named sha. (Same for `roadrunner.md`'s implicit prod claims beyond
  `roadrunner/terraform/main.tf`, which I did read.)
- **GitHub archive states** — `roadrunner.md:24-25` ("skiller archived 2026-07-01, skillpath 2026-07-31",
  jobsimulation 2026-07-31) and `:35-37` ("`chronos` … **NOT** archived, last push 2026-04-23"). These are
  GitHub repository-metadata claims; nothing in the local clones can confirm or refute them. Settled by
  `gh api repos/anthropos-work/<name> --jq '.archived, .pushed_at'`.
- **`cms.md:77`** — "M257x iter-24 measured that as 96 all-403 Directus lines in `backend`'s log". A
  runtime measurement on a stack that no longer exists; unverifiable from source. (The mechanism it
  supports *is* verified: `DIRECTUS_DATA_CONSUMERS = ("cms", "backend")`.)
- **`clerkenstein.md:319-323`** — the M218 measurement that a slow jsdelivr was *not* the cause of the
  38 s login on `billion`. Runtime, single-host, unreproducible here.
- **Adjacent-file note (outside my file set, so not booked):** root `CLAUDE.md`'s profile table calls
  `graphql` "(default)". At `platform` `0dab54d` `Makefile:10` reads `PROFILE ?= core` and no `graphql`
  profile exists in either compose file. My assigned files (`cms.md:202`, `gotenberg.md:14`,
  `roadrunner.md:57`) all say `core` and are correct; the contradiction lives in `CLAUDE.md`.
