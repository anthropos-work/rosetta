# Seat G — reading #14 (adversarial diff seat)

Scope: `git diff 0e35b1a..HEAD -- corpus/ CLAUDE.md .claude/`

## Positive controls

`git diff --stat 0e35b1a..HEAD -- corpus/ CLAUDE.md .claude/` →
**32 files changed, 898 insertions(+), 257 deletions(-)** — matches the briefing exactly.

Diff captured to a file and read top-to-bottom: **2115 lines**.

| file | wc -l | read in full? |
|---|---|---|
| (the diff itself) | 2115 | yes |
| CLAUDE.md | 555 | modified regions + surrounding sections read in full |
| corpus/architecture/external_services.md | 840 | modified regions + surrounding sections |
| corpus/architecture/architecture_overview.md | 353 | modified regions + surrounding sections |
| corpus/architecture/service_taxonomy.md | 456 | modified regions + surrounding sections |
| corpus/architecture/platform-migration-status.md | 259 | yes (§1–§5 tables read in full) |
| corpus/architecture/dependency_map.md | 103 | yes |
| corpus/architecture/shared_libraries.md | 242 | modified regions + surrounding sections |
| corpus/services/messenger.md | 146 | modified regions + surrounding sections |
| corpus/services/storage.md | 201 | modified regions + surrounding sections |
| corpus/services/backend.md | 271 | modified regions + surrounding sections |
| corpus/services/cms.md | 259 | modified regions |
| corpus/services/jobsimulation.md | 233 | modified regions |
| corpus/services/roadrunner.md | 171 | modified regions |
| corpus/ops/platform-alignment.md | 1680 | modified regions (+495 lines of new §5 rules) |
| corpus/ops/demo/demo-up-defaults.md | 202 | yes |
| .claude/skills/dev-up/reference.md | 171 | modified region + full service-set table |
| corpus/ops/run_guide.md | 525 | modified regions |
| corpus/ops/platform_repo.md | 159 | modified regions |

Ground truth used (all re-verified at the open):
`stack-demo/platform` @ `0dab54d` (== `origin/main`, clean tree), `stack-demo/app` @ `b948604` (v1.366.0;
`9d00a313` v1.367.0 and `origin/main` `7177374` also read via `git show <ref>:<path>`),
`stack-demo/storage` @ `4ce8ece` (`origin/main` `63bffc8`), `stack-demo/messenger` @ `fa47850`
(`origin/main` `a0ec933`), `stack-demo/roadrunner` @ `87d8d44`,
`.agentspace/rosetta-extensions` @ `1334a4f`.

Profile semantics were **derived by execution**, not read: `docker compose --profile <t> config --services`
was run against `stack-demo/platform` for all 13 tokens the diff names.

---

## Blockers

### B1 — the repair re-pointed every `external_services.md` citation by +1 when the correct delta was +9; 9 citation sites now land on the wrong construct

- **Anchors (all ADDED/MODIFIED lines in this diff):**
  - `CLAUDE.md:245` — *"there is no ordered EU-first fallback ladder (`corpus/architecture/external_services.md:546`)"*
  - `CLAUDE.md:259` — *"**Not a fallback ladder** (`corpus/architecture/external_services.md:546`)"*
  - `corpus/architecture/security_compliance.md:7` — *"(`external_services.md:546`)"*
  - `corpus/architecture/security_compliance.md:198` — *"fallback chain (`external_services.md:546`)"*
  - `corpus/architecture/shared_libraries.md:125` — *"`external_services.md:546` retracts that chain"*
  - `corpus/architecture/architecture_overview.md:246` — *"was retracted at [`external_services.md:546`]"*
  - `corpus/architecture/architecture_overview.md:248` — *"[`external_services.md:570`] enumerates **four live** ways a request leaves the EU"*
  - `corpus/architecture/security_compliance.md:201` — *"[`external_services.md:570`] enumerates **four live** ways"*
  - `corpus/architecture/ai_architecture.md:57` — *"Full per-line derivation: [`external_services.md:578-588`]"*
- **Why it is false:** measured directly.
  - Pre-repair (`git show 0e35b1a:corpus/architecture/external_services.md`): `:545` = *"There is **no ordered
    EU-first fallback chain.**"*; `:569` = *"ones. This list **previously said five** …"*; `:577` / `:587` bound
    the `ai_vendor`-unset derivation.
  - The repair inserted **+1 line** in the `@@ -291,8 +291,9 @@` hunk and **+8 lines** in the `@@ -538,7 +539,15 @@`
    hunk (the new `app/studio/**` blockquote). Everything below old-`:541` therefore moved **+9**, not +1.
  - Post-repair, measured on disk: the retraction is at **`:554`**; the four-ways enumeration at **`:578`–`:579`**;
    the per-line derivation at **`:586`–`:596`**.
  - `:546` today reads `> \`app\` image as an \`additional_repo\` (app v1.360.1) — the source lives in` — a
    mid-sentence line of the *new* studio-room note. `:570` reads `attempt (\`:150-155\`, mirrored at
    \`:296-302\`/\`:326\`), i.e. **direct US OpenAI**. Nothing else —`.
  - The one anchor the repair got right is `:541 → :542` (the Anthropic Direct provider row), because the +8
    insertion lands *below* it. Verified: `:542` is that row.
- **This is the diff violating a rule it adds in the same commit** — new `platform-alignment.md` §5 rule 34
  (*"Line numbers move when YOU edit too … Re-derive the line map from `git diff -U0` and re-point in the same
  commit"*, and *"the re-point is **not idempotent**"*). A uniform +1 was applied where the map is piecewise.
- **Confidence:** high.

### B2 — `external_services.md:169-174`: the `core` profile is described as starting NINE containers including `jobsimulation`, `cms`, `storage` and `roadrunner`; measured, it starts FIVE and none of those four exists

- **Anchor:** `corpus/architecture/external_services.md:169-174` (line 169 is a **MODIFIED** line — the repair
  renamed `graphql` → `core` in this very sentence and left the rest):
  > "The default `core` profile is **not** just Postgres + `backend`: it starts **nine** containers —
  > `postgresql` + `redis` … and seven application services, `sentinel` · `backend` · `jobsimulation` · `cms` ·
  > `storage` · `roadrunner` · `gotenberg`. The `jobsimulation` and `cms` containers are **merged-into-`app`
  > husks kept live as the rollback path** (teardown is M810)…"
- **Why it is false:** `docker compose --profile core config --services` at `0dab54d` returns exactly
  `postgresql redis sentinel backend gotenberg` — **five**. `docker-compose.yml` at `0dab54d` declares 8 services
  (`sentinel:5`, `backend:28`, `storage:102`, `customerio-sync:136`, `messenger:156`, `studio-desk:197`,
  `next-web-app:228`, `gotenberg:255`); there is no `cms`, `jobsimulation` or `roadrunner` block at all
  (deleted by `d11a403`), and `storage` is `profiles: [storage-legacy]` (`:134`).
- **It also contradicts, in the same file 127 lines later, a line this same commit ADDED**:
  `external_services.md:296` — *"There is **no `cms` container left to start**"*. The mermaid diagram at
  `:176-196` still draws `CMSHusk[cms :8090-8091 — merged-into-app husk, rollback path]` with a live
  `CMSHusk -.-> Directus` edge, and the note **immediately beneath it** (`:198-204`) *was* re-pointed by this
  repair — so the repairer edited the text under the stale diagram and left both.
- It further contradicts `CLAUDE.md:198` (*"`core` starts **five** containers"*), `service_taxonomy.md:56-57`,
  `service_taxonomy.md:449` and `run_guide.md` (*"This starts **five** containers"*), all added in this commit.
- **Confidence:** high.

### B3 — "compose declares nine services" is a number that matches nothing (it is 10 with the `include:` resolved, 8 without)

- **Anchors (all ADDED lines):**
  - `corpus/architecture/external_services.md:296` — *"platform `0dab54d`'s compose declares nine services and `cms` is not one of them"*
  - `corpus/services/cms.md:35` — *"`0dab54d`'s compose declares nine services and `cms` is not one of them"*
  - `corpus/services/jobsimulation.md:35` — *"`0dab54d`'s compose declares nine services and `jobsimulation` is not one of them"*
- **Why it is false:** enumerated from source. `docker-compose.yml` declares **8** services; `common.yml`
  (pulled in by `include: - common.yml`, `docker-compose.yml:1-2`) declares **2** (`postgresql:2`, `redis:24`).
  Total **10**. Nine is neither.
- **Same commit, three different denominators for one file:** `service_taxonomy.md:55` says **ten**,
  `dependency_map.md:31` says **10 compose services**, `architecture_overview.md:191` says **8 services remain**.
  8 and 10 are each defensible depending on whether the `include:` is resolved; **9 is not derivable either way**,
  and the corpus's own new §5 rule 30 makes resolving the `include:` load-bearing.
- **Confidence:** high.

### B4 — "`STORAGE_RPC_ADDR` is read by `main.go`" is false at the ref the sentence names, and it is the evidence carrying the `mid-fold → merged-into-app` transition

- **Anchors (all ADDED lines):**
  - `corpus/architecture/platform-migration-status.md` `storage` row — *"**Consumer side — also removed:** … `STORAGE_RPC_ADDR` is read by `main.go` and by **none** of the three `cmd/` tools at this ref"*
  - `corpus/services/storage.md` **consumer** row — same sentence
  - `corpus/ops/platform-alignment.md` §6 — *"At `app` `9d00a313` v1.367.0 … `STORAGE_RPC_ADDR` is read by `main.go` and by **none** of the three CLIs; … the fold is complete on both sides"*
- **Why it is false:** at `9d00a313`, `git grep -c 'os.Getenv("STORAGE_RPC_ADDR")'` over `*.go` returns **0**
  (positive control `os.Getenv("REDIS_ADDR")` returns 4 files / 9 hits, so the pipeline works). The only Go-source
  mentions at that ref are comments: `internal/jobsimwiring/wiring.go:101`, `internal/storagens/callsites_test.go:189`,
  and `main.go:451` — which literally reads *"// now — the standalone service takes no traffic and STORAGE_RPC_ADDR
  is gone."* (`§5` rule: grep counts commented-out code; exclude what does not compile.)
- **It is self-refuting in its own sentence:** the clause is introduced by *"Consumer side — **also removed**"*
  and closed by *"the fold is complete on both sides"*. If `main.go` read the variable, neither would hold. The
  intended text is almost certainly *"read by **neither** `main.go` **nor** any of the three `cmd/` tools"* — but
  as written it asserts the opposite of the row's own verdict, and it is the load-bearing evidence for moving
  `storage` off `mid-fold`.
- **Confidence:** high.

### B5 — CLAUDE.md asserts, 58 lines apart and in the same commit, both that the storage fold LANDED and that "backend → storage is mid-fold, not live"

- **Anchors (both ADDED lines):**
  - `CLAUDE.md:197-199` — *"**⚠️ Storage and Messenger are BOTH FOLDED INTO `app` — the v9.0 program landed 2026-08-04** (platform `0dab54d` / `app` `9d00a313` v1.367.0)."*
  - `CLAUDE.md:256` — *"**backend → storage is mid-fold, not live**: `app` still calls it in code, but nothing sets `STORAGE_RPC_ADDR` …"*
- **Why it is false:** at `9d00a313` — the ref the banner itself names — `app` does **not** call storage over RPC
  (B4: 0 `os.Getenv("STORAGE_RPC_ADDR")`; `main.go:471`/`:472` construct `internalstorage.NewManager` /
  `NewPublicManager` in-process). The statement is true only at the older `b948604`, which the same file does not
  name for this claim.
- It also contradicts the **fenced** map, which this commit rewrote: `platform-migration-status.md` §1 —
  *"**No row carries [`mid-fold`] today** (M257x iter-68)"*. CLAUDE.md is carrying the retired state.
- **Confidence:** high.

### B6 — `architecture_overview.md` was repaired in four places and left six claims asserting the opposite, plus an out-of-range anchor

- **Anchors (UNMODIFIED lines, all inside sections whose neighbours this commit rewrote):**
  - `:17` — *"**Storage**: File/blob storage"*, listed as a member of *"the **local `core` profile**"* (the framing sentence at `:12` **is** a modified line)
  - `:18-19` — *"**Roadrunner**: **orphaned husk** — the container still starts"*
  - `:60` — *"**plus the three unfederated husks the default profile still starts**: `jobsimulation` (`docker-compose.yml:83`), `cms` (`:144`) and `roadrunner` (`:281`) … So a bare `make up` gives you **six Go services**, not three."* (the framing at `:59` **is** a modified line)
  - `:189` / `:190` — *"**Container still starts locally** (`docker-compose.yml:83`, default profile)"* / *"(`docker-compose.yml:144`, default profile)"*
- **Why it is false:** `--profile core` yields 5 containers, **2 of them Go services** (`sentinel`, `backend`) plus
  the third-party `gotenberg`; `storage` is `[storage-legacy]`; `cms`/`jobsimulation`/`roadrunner` are not in the
  compose file. The three cited anchors are all wrong: `docker-compose.yml:83` = `- STORAGE_S3_PUBLIC_BUCKET=…`
  (backend env), `:144` = `- "8080:8080"` (customerio-sync ports), and **`:281` is out of range** — the file is
  271 lines.
- **It contradicts two lines this commit repaired in the same file:** `:15` (*"The husk container is **gone from
  compose** at platform `0dab54d` — no `jobsimulation` service, no entry in `repos.yml`"*) and `:191` (*"there is
  **no `roadrunner` compose service at all**"*).
- **Confidence:** high.

### B7 — `.claude/skills/dev-up/reference.md`: the heading was renamed to `core` and the table under it still lists four containers that no longer exist

- **Anchor:** `.claude/skills/dev-up/reference.md:46` (**MODIFIED**) — *"### Expected service set (default `core` profile, main dev stack)"*, over a table (`:50-58`) whose rows include `anthropos-cms-1 | 8090-8091`,
  `anthropos-jobsimulation-1 | 8400-8401`, `anthropos-storage-1 | 8300-8301`, `anthropos-roadrunner-1 | 10400-10401`,
  and a blockquote at `:60-68` asserting *"cms / jobsimulation / roadrunner are HUSKS — … the containers survive
  only as the rollback path until M810"* and *"re-derived from the platform clone at origin `2adcf71`"*.
- **Why it is false:** the derived `core` set is `postgresql redis sentinel backend gotenberg`. Four of the nine
  rows name containers with no compose service; a fifth (`storage`) is behind `storage-legacy`.
- **Why this is worse than a stale table:** the one-line heading change makes the table read as freshly re-derived
  at `core` while its content is still dated `2adcf71`. This is the exact class the diff's own new §5 rule 33
  names (*a pin/label is a DATE, not an exemption, and it exempts a CLAIM, never a neighbourhood*).
- **Confidence:** high.

### B8 — `shared_libraries.md`: the repaired `colony` row and the untouched `proto` row assert opposite facts 27 lines apart

- **Anchor:** `corpus/architecture/shared_libraries.md:69` (UNMODIFIED) — *"The husk repos **still carry their own
  `go.mod`** — `repos.yml:14-19` still clones them and `docker-compose.yml:83,144` still builds them"*, versus
  `:41-42` (**MODIFIED** by this commit) — *"`d11a403` deleted both from compose and from `repos.yml`, so at
  platform `0dab54d` the split is **two-way, not three**"* / *"there is no profile that starts them and no
  `graphql` profile at all"*.
- **Why it is false:** `repos.yml` at `0dab54d` is 31 lines with 6 entries (app, sentinel, storage, messenger,
  next-web-app, studio-desk); `:14-19` now resolves to `schema: public` … `- name: storage` — a *different
  subject*, still non-blank, exactly the F-assertion failure mode. `docker-compose.yml:83` / `:144` are the
  unrelated env/port lines named in B6.
- **Confidence:** high.

### B9 — `dependency_map.md:13`: the Backend row still declares compose `depends_on` edges onto `cms` and `storage`, at an anchor that is backend's env block

- **Anchor:** `corpus/architecture/dependency_map.md:13` (UNMODIFIED, in a table whose rows 15/16/18/19/21 this
  commit rewrote) — *"Sentinel, **cms**, Storage (compose `depends_on`, `docker-compose.yml:70-80` — yes, the
  monolith still has a startup edge onto the cms **husk**)"*.
- **Why it is false:** `backend`'s `depends_on` is at `docker-compose.yml:92-99` and reads `redis`, `postgresql`,
  `sentinel` — with the platform's own in-file comment at `:93`: *"# storage removed at v9.0: served in-process by
  this container now."* `:70-80` is `SERVICE_NAME=backend` … `AWS_REGION=eu-west-1`.
- It directly contradicts row `:15` of the same table, which this commit rewrote to *"**no container** at platform
  `0dab54d` — the husk is gone from compose"*.
- **Confidence:** high.

### B10 — `dependency_map.md:19` is a MODIFIED line carrying two anchors this same commit invalidated

- **Anchor:** `corpus/architecture/dependency_map.md:19` — *"**Storage** | Postgres, Redis — **`depends_on` only**
  (`docker-compose.yml:213-217`, both `service_healthy`) | … which is what its own doc says at
  [`storage.md:14,21`]"*.
- **Why it is false:**
  - `storage`'s `depends_on` is at `docker-compose.yml:126-130`. `:213-217` is **studio-desk's** environment block
    (`CLERK_SIGN_IN_URL`, `DEFAULT_MODEL`, …).
  - `storage.md:14` and `:21` were the two sentences quoted, pre-repair (verified via
    `git show 0e35b1a:corpus/services/storage.md`). This commit added ~23 lines to that file's banner; those
    sentences are now at `:37` and `:44`, and `storage.md:14` / `:21` today read `>` and `> |---|---|`.
  - The repairer edited this exact line (replacing `service_taxonomy.md:418` with a bare link) and left both.
- **Confidence:** high.

### B11 — `dependency_map.md:24`: studio-desk's `depends_on` is claimed as `backend` + `cms`, at a line number past the end of the file

- **Anchor:** `corpus/architecture/dependency_map.md:24` (UNMODIFIED, same rewritten table) — *"Compose
  `depends_on` is **`backend` + `cms`** (`docker-compose.yml:337-341`), not `graphql`"*.
- **Why it is false:** studio-desk's `depends_on` is `docker-compose.yml:223-225` and names **`backend` only**.
  `:337-341` does not exist — `docker-compose.yml` at `0dab54d` is **271 lines**. There is no `cms` service to
  depend on.
- **Confidence:** high.

### B12 — the "correction of the correction" claims the map has no Directus row; the map has a Directus row saying exactly the disputed thing

- **Anchor:** `corpus/architecture/service_taxonomy.md:313-317` (**ADDED**) — *"That map has **no Directus row at
  all** — it maps repos, and Directus is an external service — so the anchor resolved to a row about something
  else entirely. `CHECK-M257x-iter64-pms-87-subject`, closed M257x iter-65."*
- **Why it is false:** `corpus/architecture/platform-migration-status.md:97` is
  `| \`directus\` | external | external | no | the headless CMS at \`content.anthropos.work\`. **Removed from
  compose** at \`a2a3ee6\` (2026-02-27); …` — a Directus row, in the map, asserting precisely the fact the passage
  says the map cannot speak to. The stated rationale (*"it maps repos"*) is also wrong on its face: the same §3
  census carries `postgresql`, `redis`, `gotenberg`, `ant-academy` and `chromedp` rows.
- This is a repair that closes a `CHECK-` item by publishing a new false claim in place of the old one.
- **Confidence:** high.

### B13 — the `roadrunner` row states as fact an inference drawn from a platform commit message that its own commit refutes

- **Anchor:** `corpus/architecture/platform-migration-status.md` `roadrunner` row (**ADDED**) — *"**Compose service
  and `repos.yml` entry both deleted by `d11a403`** — whose message states the clone entry *"was already gone, so
  the `../roadrunner` build context could no longer resolve"*, i.e. **the service had been unbuildable, not merely
  legacy**."*
- **Why it is false:** `git show d11a403 -- repos.yml` shows the commit **itself** deleting
  `- name: roadrunner / type: go / migrations: false # legacy — folded into app; backend calls Judge0 directly`.
  At the parent ref, `git show 2adcf71:repos.yml` lines **29-31** carry that entry. So `make init` *did* clone
  `roadrunner`, the `../roadrunner` build context *did* resolve, and **the service was buildable**. The commit
  message is wrong; the corpus promoted it to a fact ("the service had been unbuildable").
- The cell is also self-contradictory: it says the entry was deleted by `d11a403` and then quotes an assertion
  that it was already gone before `d11a403`.
- **Ironic and worth stating:** the same commit adds `platform-alignment.md` §6 *"The platform's CONFIG is its
  documentation of record; its NARRATIVE docs are not."* This is a narrative artifact (a commit message) beating
  the config in the one place that rule was written for.
- **Confidence:** high.

### B14 — the `redis` row's `common.yml:20` anchor was invalidated by the very platform commit the neighbouring (repaired) row documents

- **Anchor:** `corpus/architecture/platform-migration-status.md:96` — *"| `redis` | … | `common.yml:20`. Streams
  transport for the Watermill pub/sub |"*, immediately below `:95` (**MODIFIED** by this commit) — *"Its
  healthcheck gained a `start_period: 120s` at `6060315` (`common.yml:22`)"*.
- **Why it is false:** `git show 2adcf71:common.yml` has `redis:` at **line 20**. `6060315` adds 4 lines to
  postgresql's healthcheck; at `0dab54d`, `common.yml:20` is
  `# which takes well over the 25s (5s x 5) the retries alone allow once the data` — a comment **inside
  postgresql's block** — and `redis:` is at `:24`. `common.yml:22` (`start_period: 120s`) is correct.
- This is the map's own assertion-F failure mode (*"a citation slides into the neighbouring service — still
  resolving, still non-blank, silently about something else"*) landing one file outside F's declared reach — the
  same commit that adds the *"what is fenced"* table listing `common.yml` under **"resolution + range only"**.
- **Confidence:** high on the fact; the offending line is unmodified context, so I note the scope explicitly.

### B15 — `messenger.md:101` states messenger depends on `cms` and `jobsimulation`, five lines below the repair that says it depends on `backend`

- **Anchor:** `corpus/services/messenger.md:101` (UNMODIFIED) — *"Messenger depends on `backend`, `cms`,
  `jobsimulation` at startup (compose `depends_on`), so bringing it up implicitly brings the rest of the stack."*,
  versus `:95-96` (**ADDED**) — *"messenger declares `depends_on: backend`, which the `messenger` profile does not
  select"*.
- **Why it is false:** `docker-compose.yml:186-192` gives messenger `depends_on: redis, postgresql, backend`.
  There are no `cms` or `jobsimulation` services at `0dab54d`. (Measured: `docker compose --profile messenger
  config --services` → `rc=1`, *"service "messenger" depends on undefined service "backend""* — one dependency,
  named.)
- **Confidence:** high.

### B16 — `frontend-tier.md` names a profile studio-desk is not in, and the two rows of one table now describe two different compose files

- **Anchor:** `corpus/ops/demo/frontend-tier.md` UI-tier table (**both rows MODIFIED**):
  - next-web-app — *"in the demo's `core` profile"*
  - studio-desk — *"in the `frontend`/`studio-desk` profiles"*
- **Why it is false:** in the **platform** compose, `studio-desk` is `profiles: [studio-desk, all]`
  (`docker-compose.yml:226`) — **not** `frontend` (that is next-web-app's, `:253`). Measured: `--profile frontend`
  exits **1** and would not include studio-desk in any case. In the **demo** compose, both frontends are emitted
  by `rosetta-extensions/stack-injection/gen_injected_override.py:420` as
  `profiles: !override [<derived default>]` — i.e. **both** are `core`, which is what the next-web row says and
  the studio-desk row does not. Either reading makes the studio-desk cell false.
- **Confidence:** medium — I hesitated because the demo's injected compose is generated, but I read the generator
  and both readings fail, so I booked it.

### B17 — the map's app row cites a BLANK line for the skillpath wiring site in its own historical triple

- **Anchor:** `corpus/architecture/platform-migration-status.md` `app` row (**ADDED**) — *"the first four stood at
  `:581`/`:612`/`:643`/`:1043` at `b948604` v1.366.0"*.
- **Why it is false:** at `b948604`, `main.go:643` is a **blank line**; `skillpath.NewSessionManager` is at
  **`:642`** (verified by `git show b948604:main.go | grep -n`). The other three are correct
  (`:581` skiller, `:612` jobsimwiring.Wire, `:1043` appcms.Wire), as is the `5ba17044` triple
  (`:573`/`:604`/`:634`/`:1034`).
- **Why it matters more than one digit:** this aside exists *specifically* to demonstrate what a day of platform
  commits costs a line-number citation, and the same commit's new §5 rule 34 says the blank-line landing is the
  one case `anchor_construct_guard` reliably catches. It got through.
- **Confidence:** medium — historical/illustrative rather than operational, which is why I hesitated; booked
  because it is a wrong anchor in the map's most-scrutinised row and lands on nothing at all.

---

## Minors

- `platform-migration-status.md` `app` row cites `repos.yml:10-13` for *"(`migrations: true`, `schema: public`)"*.
  At `0dab54d` app's entry is **11-14**; `:10` is a comment and `:13` is `migrations: true`, so the range excludes
  `schema: public`, which the sentence names. Every *sibling* repos.yml anchor in the same table **was** correctly
  re-pointed (sentinel `15-17`, storage `18-20`, messenger `21-23`, next-web-app `26-28`, studio-desk `29-31`,
  the rollback comment `9-10` — all verified) — this one was not.
- `docker-compose.yml:130-133` is cited for the storage rationale comment in **three** places (`CLAUDE.md`,
  `platform_repo.md`, `platform-alignment.md` §6). `:130` is `condition: service_healthy`; the comment is
  `:131-133`, which is what `storage.md` correctly cites. Same commit, two anchors for one construct.
- `platform-migration-status.md` `storage` row: *"`STORAGE_S3_BUCKET`/`STORAGE_S3_PUBLIC_BUCKET` were added to
  `backend` (`docker-compose.yml:82`)"* — `:82` is the first, `:83` is the second.
- `platform-migration-status.md` / `storage.md`: *"the bucket names are constants in
  `app/internal/storage/service.go:22`, `:24`"*. Those constants are the **env-var names**
  (`EnvBucket = "STORAGE_S3_BUCKET"`, `EnvPublicBucket = "STORAGE_S3_PUBLIC_BUCKET"`); the bucket *names* are read
  from the environment at `main.go:463-464`. The anchor is right; the noun phrase is not.
- `platform-alignment.md:1090` cites `corpus/services/backend.md:175` for the "all four addresses" line. That
  claim now lives at `backend.md:195` (this commit rewrote it); `:175` is a directory listing
  (`worker/ Redis Streams consumers (Watermill)`). Historical narrative, so booked as minor.
- Three denominators for one compose file in one commit (8 / 9 / 10 — see B3). 8 and 10 are each defensible;
  they are never reconciled, and no passage says which convention it is using.
- `roadrunner.md` / the map: *"untouched since `87d8d44` (2026-06-19)"* — `87d8d44` is the roadrunner repo's HEAD
  commit; the last commit that actually touched `terraform/main.tf` is `e45eb61` (2026-05-27). The sentence is
  true if read as *"the repo has not moved since"*, false if read as *"that commit touched the file"*.

---

## Positively cleared (audited zeros)

**ENUMERATED** (predicate re-derived from source, independently, before any arithmetic):

- **Every profile claim in the diff.** I ran `docker compose --profile <t> config --services` against
  `stack-demo/platform` @ `0dab54d` for all 13 tokens the diff names. Results match the repair exactly:
  `core` → `postgresql redis sentinel backend gotenberg`; `backend` → identical set; `all` → those plus
  `studio-desk customerio-sync next-web-app`; `storage-legacy` → floor + `storage`; `customerio-sync` → floor +
  `customerio-sync`; `frontend` / `studio-desk` / `messenger` → **rc=1**, *"service "X" depends on undefined
  service "backend": invalid compose project"*; and the retired tokens `graphql` / `cms` / `jobsimulation` /
  `roadrunner` / `storage` → **rc=0 with exactly `postgresql redis sentinel`**. Every "exits 1", every "exits 0
  and starts the three-service floor", and `PROFILE ?= core` (`Makefile:10`) are correct. `core+studio-desk` and
  `core+messenger` combinations (recommended by `run_guide.md` / `messenger.md`) both succeed.
- **The 31 demo knobs.** Enumerated `DEMO_[A-Z0-9_]+` across `up-injected.sh` + `ensure-clones.sh` +
  `ant-academy.sh`: 34 raw matches, of which 4 are non-knobs (`DEMO_NO_` prefix fragment, `DEMO_FLAGS_ALL`,
  `DEMO_WS`, `DEMO_DISK_AVAIL_KB` test hook) → **30 `DEMO_*` + `STACK_PUBLIC_HOST` = 31**. The doc's tables carry
  exactly **31** distinct knob rows. The `30 → 31` bump (doc, `demo/README.md`, `CLAUDE.md`) is correct; the new
  member is `DEMO_ALLOW_MISSING_HOST_MOUNTS`.
- **All 23 re-pointed `up-injected.sh:N` anchors.** Every one resolves to the claimed knob:
  `:37 :43 :73 :74 :108 :221 :227 :233 :239 :252 :253 :255 :288 :369 :412 :548 :1406 :1505 :1627 :1628 :1922
  :2136 :2374 :2404 :2445 :2482 :2507 :2670`. Zero defects in the largest re-point block in the diff.
- **All `docker-compose.yml:N` anchors added or re-pointed by the diff.** Verified line-by-line against the file:
  `:1-2 :5 :18 :28 :53 :59 :82 :100 :131-133 :134 :136-138 :154 :156 :171-172 :173 :174 :176 :183 :195 :197 :204
  :220 :226 :228 :236 :245 :253 :255-256 :268`. All resolve to the claimed construct. The quoted compose comments
  (*"cms + jobsimulation are folded into app: all four RPC edges are the one backend mux"*, *"v9.0: NOT in the
  default profiles any more…"*) are verbatim.
- **`repos.yml` membership, both directions.** 6 entries (app, sentinel, storage, messenger, next-web-app,
  studio-desk); exactly 6 map rows claim `in repos.yml = yes`, and they are the same 6. Map assertions A and B
  hold. `cms`/`jobsimulation`/`roadrunner` correctly flipped to `no`. `cms.md`'s explicit enumeration of the 6 is
  correct.
- **The state vocabulary.** §1 lists exactly **8** states; assertion C's "eight" is right; and
  `rosetta-extensions/stack-core/platform_alignment_guard.py:81-85` `ALLOWED_STATES` contains `mid-fold`.
- **`app` citations at `b948604`** (the checkout, and the ref most cells name): `:581`, `:612`, `:743-746`,
  `:766-779`, `:770`, `:824-826` (`OPENAI_KEY` at `:825`), `:980-982` (`log.Fatalf` on `DIRECTUS_BASE_ADDR`),
  `:1043`, `:1187`, `:1188`, `:1196`, `:1204`, `:1205-1211` (the *"additive + DORMANT … until the M809 re-point"*
  comment, verbatim), `:1212-1214` (`if cmsRPCServer != nil`), `:1228`; `internal/jobsimwiring/wiring.go:118`.
  Also `main.go:446`/`:524`/`:992` and `cmd/academyImport/main.go:235`, `cmd/academy-asset-upload/main.go:133`,
  `cmd/import/main.go:50` — the whole mid-fold read-site set in §6 — all correct.
- **`app` citations at `9d00a313` v1.367.0:** `:15`, `:61`, `:62`, `:471`, `:472`, `:494`, `:637`, `:668`, `:698`,
  `:1048`, `:1330-1340`, `:1362-1365`, `:1387`, `:1423`; `internal/jobsimwiring/wiring.go:123`;
  `internal/storage/service.go:22`, `:24`. All resolve to the claimed construct.
- **Terraform:** `storage/terraform/main.tf:38` `= 0` + `:29-33` ordering comment + `:35-37` prevent_destroy/M903
  @ `63bffc8`; `messenger/terraform/main.tf:29` `= 0` + `:27-28` rollback comment @ `a0ec933`;
  `roadrunner/terraform/main.tf:19` `= 1`. All correct at the refs their cells name.
- **`messenger/cmd/root.go:120-140`** genuinely reads all four RPC addresses (`CMS_RPC_ADDR:120`,
  `BACKEND_USERS_RPC_ADDR:125`/`:130`, `SKILLER_RPC_ADDR:135`, `JOBSIMULATION_RPC_ADDR:140`) — the exact range
  cited, no slack.
- **`STORAGE_RPC_ADDR` and `ROADRUNNER_RPC_ADDR` = 0 occurrences** across `docker-compose.yml`, `common.yml`,
  `.env_example` (positive control: `SKILLER_RPC_ADDR` returns `docker-compose.yml:183`).
- **`external_services.md:542`** is the Anthropic-Direct provider row — the one anchor of the four re-pointed
  families that the +1 shift got right, and the self-reference at `:588` (*"`:542` above"*) is correct.
- **`service_taxonomy.md:308-317`**, cited from `external_services.md:144`, resolves to exactly the
  correction-of-the-correction block claimed.
- **`platform_repo.md`'s "clones the 6 sibling repos"** — correct.

**RE-DERIVED** (I re-computed arithmetic the document displays — the weakest clearance):

- `git rev-list --count b948604..9d00a313` = **56**, matching *"56 commits and one working morning later"*.
- The `2adcf71 → ef32d4c` narrative: `d11a403` does delete the cms, jobsimulation and roadrunner compose services
  *and* the cms + jobsimulation `repos.yml` entries (roadrunner's too — see **B13**), under
  `chore/prune-merged-services`; `6060315` is the postgres `start_period` commit; `0dab54d`'s subject is
  *"run without the standalone storage; rename graphql -> core"*.

---

## What I could not settle

- **"`docker-compose.yml` citations — 20 of them | fenced (F)"** and **"every other `file:line` … (28
  citations)"** in the map's new *what is fenced* table. I count 13 explicit `docker-compose.yml:N` strings plus
  ~8 bare `:N` continuations that F would presumably resolve against the same file (≈21). Settling this requires
  reproducing `platform_alignment_guard.py`'s citation-extraction predicate, which I did not read in depth.
- **"On first run it named 8 dead citations; a hand reading had found 2."** Tooling run history; not reproducible
  from the tree.
- **GitHub repo archival states** (jobsimulation ARCHIVED 2026-07-31, chronos NOT archived, cms not archived,
  graphql-wundergraph ARCHIVED 2026-07-30, skiller/skillpath ARCHIVED). No network access; the local clones
  cannot tell me.
- **Production rollback-path declarations** (`module.cms_euwest1`, `module.jobsimulation_euwest1`, and the
  `infrastructure` repo's `services.tf` that `repos.yml:9-10` now names as authoritative). The `infrastructure`
  repo is not in the clone set — the map says so itself, and the roadrunner row's *"still not verified"* framing
  is honest about it.
- **Whether the "nine services" figure was meant as something other than a compose-service count.** It is stated
  three times identically with no derivation shown, and no reading I tried produces 9.
- **`app` `origin/main` (`7177374`, 60 commits past `b948604`)** — I read it only to bound the ref question. No
  corpus claim in this diff names it, so nothing is graded there; but note that `app` has moved 4 commits past
  `9d00a313` since the repair, including `refactor(ai): fold the ai library into app as internal/ai`, which will
  put the `ai` library row of `shared_libraries.md` and the map's `ai | library` row under pressure at the next
  ref bump.
