# Seat B — reading #13

**Refs at the moment of measurement** (re-derived at open, all matched the briefing):
`app b948604f` · `platform 0dab54df` · `next-web-app bb3313bc` · `sentinel 88bc5592` ·
`storage 4ce8ece5` · `messenger fa47850d` · `cms ca50c817` · `graphql-wundergraph 60c229f3` ·
`roadrunner 87d8d443` · `jobsimulation 462343b0` · `studio-desk 14a5442a` · `ant-academy 9c3843cd` ·
`rosetta-extensions (authoring, main) 1334a4f5`. Corpus branch `m257x/platform-realignment`.

**Grading policy I applied** (stated so it is auditable): a claim that is *false against source* or
*contradicts another corpus claim* is a BLOCKER. A `file:line` anchor that has drifted but still lands
in/near the construct it names, or is out of range while the substantive claim it supports verifies
true, is a MINOR. Where I hesitated I say so per finding.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/services/ai-readiness.md` | 645 | yes |
| `corpus/architecture/ai_architecture.md` | 302 | yes |
| `corpus/services/graphql-wundergraph.md` | 265 | yes |
| `corpus/services/ai-labs.md` | 156 | yes |
| `corpus/services/coursebuilder.md` | 140 | yes |
| `corpus/services/customerio-sync.md` | 75 | yes |
| `corpus/architecture/README.md` | 38 | yes |

(Also read in full as instructed, not audited: `corpus/ops/platform-alignment.md` §5, lines 240–1147.)

---

## Blockers

### B1 — `graphql-wundergraph.md:13` — the `graphql` profile does not exist at platform HEAD, and is not the default

- **Anchor:** `corpus/services/graphql-wundergraph.md:13-14` —
  > "The `graphql` *profile name* survives in compose and is now simply the default profile —
  > it no longer names a router service."

- **Why it is false:** At `platform 0dab54df` — the checkout, and the ref this very blockquote's table
  column header names (*"a fresh local stack @ platform **origin HEAD**"*) — the token `graphql`
  appears in `docker-compose.yml` **only inside URL strings** (`:204`, `:220`, `:236`, `:245`) and in
  **no `profiles:` key at all**. The seven `profiles:` keys in the file are
  `[core, backend, all]` (`:100`), `[storage-legacy]` (`:134`), `[customerio-sync, all]` (`:154`),
  `[messenger]` (`:195`), `[studio-desk, all]` (`:226`), `[frontend, all]` (`:253`),
  `[core, backend, all]` (`:268`). `common.yml` declares none.
  `Makefile:10` reads **`PROFILE ?= core`**. The HEAD commit is literally
  `0dab54d chore(compose): run without the standalone storage; **rename graphql -> core**`.

  **Corpus self-contradiction:** `corpus/services/customerio-sync.md:14` already states the opposite
  and correctly — *"Not in the default profile, which is `core`, not `graphql`: `0dab54d` renamed it"*.

  The passage cannot be rescued by the `2adcf71` pin two sentences above it: per
  `platform-alignment.md` §5 rule 33 a pin's scope is the claim's own block, and this block asserts
  currency (*"survives"*, *"is now"*, and a table column headed *origin HEAD*).

- **Confidence:** high

### B2 — `graphql-wundergraph.md:177` — `make up` does not start a `graphql` profile

- **Anchor:** `corpus/services/graphql-wundergraph.md:177-178` —
  > "`make up` starts the `graphql` **profile**, which since `2adcf71` contains no router.
  > `make logs S=graphql` has nothing to tail…"

- **Why it is false:** `platform/Makefile:66` is
  `$(COMPOSE) --profile $(PROFILE) up --build -d --remove-orphans` with `PROFILE ?= core`
  (`Makefile:10`) at `0dab54df`. `make up` starts the **`core`** profile. And a reader who *does*
  spell `PROFILE=graphql` gets §5 rule 30's exact trap: `graphql` matches no `profiles:` key, so the
  selection collapses to the always-on floor (the services declaring no `profiles:` key — `sentinel`
  in `docker-compose.yml`, plus `postgresql`/`redis` from the `include:`d `common.yml`), which exits
  0 and starts containers. "Starts the `graphql` profile" reads as a working invocation; it is a dead
  token. Same predicate as B1, but a separately-actionable instruction sentence in a *Local
  Development* section, so a claim-scoped repair must fix both.
  (At `2adcf71` this was true: `PROFILE ?= graphql` and ten `profiles:` keys, six naming `graphql`.
  The sentence's pin covers only the subordinate "contains no router" clause.)

- **Confidence:** high

### B3 — `ai-readiness.md:213` — "the four usage KPIs" contradicts the five the same file enumerates 12 lines above

- **Anchor:** `corpus/services/ai-readiness.md:213` —
  > "- the **four** usage KPIs are **DERIVED from the org's own seeded Step-3 session scores** …"

- **Why it is false:** There are **five**. `app/internal/aireadiness/how_we_measure.go:1138-1159`
  declares `usageDimSpecs` with exactly five entries — `avg_adoption` (`:1139`),
  `avg_transformation` (`:1143`), `avg_originality` (`:1147`), `avg_depth` (`:1151`),
  `avg_ownership` (`:1155`) — and the seeder writes exactly five
  (`rosetta-extensions/stack-seeding/seeders/ai_readiness_interview_report.go:181-185`).
  `ai-readiness.md:201` itself names all five ids and calls the row *"(5 tiles)"*, and `:614-618`
  spells out that the M219 seeder's **four** old ids were the M250 defect ("4 of 5 tiles rendered
  empty"). `:213` is the surviving pre-M250 count inside the post-M250 honesty-rules list — i.e. the
  exact seeding mistake the section exists to record.
- **Why I hesitated:** it is an undercount, which the canonical grading rule carves out as MINOR. I
  booked it up because (a) it self-contradicts an enumerated list in the same file, and (b) the
  failure it would cause — seed 4, render 1 blank tile — is the documented M250 iter-07 regression.
- **Confidence:** high (on the fact); medium (on the grade)

### B4 — `ai-readiness.md:262` + `:315-317` — Compare is not "fully built"; 2 of its 6 sections are hardcoded-empty stubs

- **Anchor:** `corpus/services/ai-readiness.md:262-264` —
  > "- **Cross-cycle Compare is a fully-built backend** — `aireadiness/compare.go` `CompareCycles` → a
  > 6-section `AIReadinessCompareResponse` (Topline / Archetypes / a 4×4 Transitions matrix /
  > **TeamDelta** / SkillCoverage / **ThemesShift**); both cycles must be `closed`."
  >
  and `:315-317` — "a 4th manager tab, **Compare** … is **fully built** but hard-gated off …
  It is neither current nor legacy: **complete-but-disabled**."

- **Why it is false:** `app/internal/aireadiness/compare.go:190-198` returns **TeamDelta** and
  **ThemesShift** as literal empty structs carrying an explanatory `Note`:
  - `:190-193` — `Teams: []AIReadinessCompareTeamRow{}`, `Note: "Per-team compare not available in
    v3.10: snapshots do not freeze participant tags. Tracked as a follow-up (add frozen_tags jsonb to
    ai_readiness_snapshots)."`
  - `:194-198` — `Rising: []…{}`, `Falling: []…{}`, `Note: "Interview themes shift requires per-person
    interview insight extraction (backlog). Lights up once it ships AND ≥2 cycles have interview
    data."`

  The source says so in its own header comment too: `:21` *"TeamDelta — per-team aggregates
  (**placeholder in v3.10** — tags not in snapshot)"*, `:23` *"ThemesShift — interview themes movement
  (**placeholder until M305 lands**)"*. So 2 of the 6 sections the corpus enumerates return no data
  **by construction**. "Fully-built" / "complete-but-disabled" is the operative claim a reader acts on
  (un-gate `SHOW_SECONDARY_TABS` and ship); doing so ships two permanently blank panels.
  The other four are real: Topline/Archetypes/Transitions/SkillCoverage are computed, and the
  both-cycles-`closed` precondition verifies exactly (`compare.go:163-164`, `:170-171`).
- **Why I hesitated:** the *API surface* is complete — all six keys are present and typed. The
  over-claim is about data, not shape.
- **Confidence:** high

---

## Minors

- `ai-readiness.md:459` — cites `AIReadinessClient.tsx:137-138` for `effectiveCycleId = selectedCycle
  ?? activeCycle?.id ?? latestClosedCycle?.id`; at `next-web-app bb3313bc` that is **`:153-154`**
  (`:137` is blank, `:138` is `const cyclesQ = useAIReadinessCycles(…)`). The claim itself verifies.
- `ai-readiness.md:462` — cites `:150-154` for "gates the data GET on `cyclesQ.isFetched`"; the gate is
  at **`:166-170`** (`enabled: featureOn && cyclesQ.isFetched`). The claim itself verifies.
- `ai-readiness.md:316` — cites `AIReadinessClient.tsx:69` for `const SHOW_SECONDARY_TABS = false;`;
  it is at **`:78`** (declared `const SHOW_SECONDARY_TABS: boolean = false;`), and `:69` is an
  `import type { Cohort } …` line. The strip mechanism verifies exactly (`:594-599` filter keeps
  `dashboard`/`how`/`next` unconditionally and `compare` only when the flag is true).
- `ai-readiness.md:46` — "contradicted `:458` of this same file, which is already in the past tense";
  `:458` is a bare `>` blockquote continuation line. The past-tense demo-patch mention is at `:484`.
- `graphql-wundergraph.md:168` — cites `docker-compose.yml:334,352`; at `0dab54df` that file is **271
  lines**, so both anchors are out of range. The lines exist at `2adcf71` (which the sentence does not
  name); at the checkout the endpoints are `:236`/`:245` (next-web) and `:204`/`:220` (studio-desk).
  The substantive claim — locally the frontends hit `backend` at `:8082/graphql/query` — verifies.
- `graphql-wundergraph.md:128` — "as `:84` describes"; `:84` is the *"There is no application source
  here"* blockquote. The behaviour described (a subgraph SDL change rebuilding the router because the
  build context is the parent dir) is at `:110-111`.
- `graphql-wundergraph.md:81` — "`:174-176` of this same doc already said `localhost:5050` refuses the
  connection"; that statement is at `:178`. (`:174-176` is a blank line, the `### Run in Docker`
  heading, and a blank line.)
- `ai_architecture.md:84` — the Anthropic **Models** cell lists only *Claude 4.5 / 4 / 3.7 / 3.5
  Sonnet*, omitting **Sonnet 4.6** and **Opus 4.8** — the two models this corpus documents as Course
  Builder's production author/grader pair (`coursebuilder.md:49,51`; source
  `app/internal/coursebuilder/bedrock.go:23,29`) — in a row that itself names Course Builder. The four
  listed match the Directus `AIModel` enum exactly, so the cell is right for the *simulation* plane and
  under-scoped for a section headed "Model Inventory".
- `ai_architecture.md:210` — "all it does is swap the dispatched **endpoint** to `openai-hosted`";
  `app/internal/jobsimulation/calls/livekit.go:140-144` also resets `agentName` to `"anthropos-agent"`.
  The residency conclusion is unaffected.
- `ai_architecture.md:128` — "**`gpt-5.2` appears in no studio config at all** (only as a pricing entry
  in `studio/services/ai.py:356,508`)". The config half is exactly right (0 hits across all three
  `studio/configs/*.ini`); the parenthetical is loose — `gpt-5.2` also appears in
  `studio/changelog.md:106`, `studio/knowledge/operations/running-tests.md:32` and
  `studio/knowledge/concepts/tests-system.md:84`.
- `coursebuilder.md:114-115` — "~55 test files" / "~25 boundary test files"; measured **59** and
  **33**. Both are tilde-qualified; the second is ~32 % low.
- `architecture/README.md:19` — lists "the WunderGraph Cosmo GraphQL gateway" among the live
  third-party integrations with no note that it is deleted from local dev, while
  `graphql-wundergraph.md:3-14` opens with exactly that banner. Not false (prod still runs it:
  `graphql-wundergraph/terraform/main.tf:20 service_desired_count = 1`), but this index qualifies
  every other entry carefully.
- `ai_architecture.md:232` — cites `app/internal/cms/directus/collections/jobsimulation.go:983-990` for
  "the enum" of `ai_model`; the `AIModel` const block's members span **`:980-990`** (the cited range
  covers the three examples given but drops the first three members).

---

## Positively cleared (audited zeros)

Every item below I opened in platform source. Label per §5 rule 24.

**`ai-readiness.md`**
- **ENUMERATED** — `defaultReadinessSkills` is exactly **31** entries: 19 @ 1.0 (`defaults.go:31-49`)
  + 12 @ 0.5 (`:51-62`); denominator 19 + 6 = **25.0**. `defaultReadinessSims` is exactly **3**
  (`:75-79`: simulation/tech, simulation/business, interview/both). Both counts and both weights.
- **ENUMERATED** — archetype bands: `archetypeHighBand = 75` (`scoring.go:26`), `archetypeLowCeil = 50`
  (`:30`), Champion ≥75/≥75, Standby ≤50/≤50, tie → Explorer (`:46-56`); buckets None 0-24 / Low 25-50
  / Medium 51-74 / High 75-100 (`:65-77`).
- **ENUMERATED** — every table named in the data-model section exists in
  `internal/data/ent/migrate/schema.go` under the exact (plural) names claimed, including
  `ai_readiness_user_step_progresses`, `ai_readiness_notification_logs`,
  `ai_readiness_notification_optouts`, `ai_readiness_recommendations`, `ai_readiness_email_overrides`
  and `interview_aggregated_reports` (columns `sim_id`, `organization_id`, `report` JSON,
  `session_count` at `:2335-2343`).
- **ENUMERATED** — the REST surface: all ten routes (`/workforce/ai-readiness`, `/narrative`,
  `/steps-completion`, `/cycles` GET+POST, `/setup` GET+POST, `/cycles/:cycleID`, `/export.csv`,
  `/compare`, `/cycles/:cycleID/close`) at `internal/web/backend/api/server.gen.go:2396-2406`.
- **ENUMERATED** — `usageDimSpecs` = 5 ids (`how_we_measure.go:1138-1159`), matching `:201`'s list.
- **ENUMERATED** — the `interactions.action_type` enum is exactly `{email, call}`
  (`internal/data/ent/interaction/interaction.go:92-98`).
- **ENUMERATED** — `AI_READINESS_URL` occurs in exactly two non-`node_modules` files
  (`urls.ts:52`, `useNavbarSections.tsx:4/:398-400/:547`); positive control `WORKFORCE_URL` → 5 hits.
- **ENUMERATED** — `dae0fb2f7` deletes `AIReadinessContainer.tsx` (103) + `AIReadinessIntro.tsx` (220)
  + `AIReadinessView.tsx` (330) = **653** lines; all three files absent at HEAD; the legacy route
  directory does not exist.
- **ENUMERATED** — `AIReadinessClient.tsx` contains **0** occurrences of `posthog` and **0** of
  `flag_ai_readiness`; `HowWeMeasureTab.tsx` is **1,989** lines with **0** `interviewQuestions`, and
  `:1879`/`:1903`/`:1915`/`:1921`/`:1927` land exactly on the block open, the label, and the three
  cells (`skillsMapped`, `handsOnMinutes`, `interviewMinutes`).
- **ENUMERATED** — anchor-exact and construct-correct: `readiness.go:139` (`computeTier1`), `:289`
  (`GetAIReadinessWithOptions`), `:290-297`, `:307-312`, `:314`, `:330`, `:388`/`:390`
  (`keepInCycleStep1` / `keepStartedMembers`), `:638-652` (`queryInCycleStep1Completers`, all three
  predicates present), `:662-664`, `:684`, `:771`/`:779` (`buildResponseFromSnapshots` →
  `m.workforce.LoadMembers(ctx, orgID, "")`), `:82-91` (`queryUserAISkills` selects only
  `user_id, skill_id, is_verified`); `steps.go:907-914` (the quoted *"only real 'has started' check"*
  comment) and `:915-938`; `how_we_measure.go:285-287`;
  `enum/organization_settings.go:47`.
- **ENUMERATED** — `AIReadinessCSVColumns` is exactly **15** entries (`csv.go:17-32`). Worth noting the
  corpus is *right where the platform's own doc-comment is wrong*: `csv.go:35` still says "19-column".
- **ENUMERATED** — the demo-patch manifest `app-aireadiness-snapshot-loadmembers.yaml` has
  `path: internal/aireadiness/readiness.go` at **`:42`** and the *"v2.7 M254 RE-POINT"* header at
  **`:33`**; `demo-stack/tests/test_aireadiness_snapshot_loadmembers_m254.py` exists.
- **ENUMERATED** — `aiReadinessStageFor` (`stack-seeding/seeders/ai_readiness_funnel.go:205-225`):
  explicit `not_started` → 0 checked first, manager → 0, struggling → 1, default → 3;
  `AIReadinessNotStarted = "not_started"` (`blueprint.go:316`), `Persona.AIReadiness` (`:200`);
  `seat_append_test.go` + `TestAIReadinessFunnelSeeder_DayZeroHeroGetsNoSignals` both present.
- **ENUMERATED** — 5 AI-readiness Playthroughs: 4 in `playthroughs/manifest/ai-readiness.yaml` + the
  `pt-onboarding-aireadiness-guided` entry in `onboarding.yaml:361`.
- **ENUMERATED** — one-active-cycle partial unique index exists verbatim
  (`terraform/migrations/20260618160827.sql:18`).
- **ENUMERATED** — `ai_readiness_email_override.go` uses `OrganizationMixin{}` (`:38`) and documents
  "One row per (organization, email_type)" (`:20`, `:88`).

**`ai_architecture.md`**
- **ENUMERATED** — every one of the ~25 `simulator/ai/ai.go` and `jobsimulation/ai/ai.go` anchors is
  *exact*: vendor consts `:30-33`, `getClient` `:259-289` (with the failed-flag-keeps-EU branch at
  `:270-273`), `WithRegion("eu-west-1")` `:87`, `throttled → vendor = Openai` `:150-155`/`:297-301`;
  and `simulator/ai/ai.go` `:20-26`, `:58-59`, `:65-66`, `:69`, `:82-83`, `:86`, `:98-99`, `:102`,
  `:110-111`, `:114-115`, `:126-127`.
- **ENUMERATED** — the Directus `AIVendor` enum has **five** members (`jobsimulation.go:970-974`) and
  the runtime switch has **four** cases, so `azureglobal` (`:971`) is the one with no case. Both
  cardinalities re-derived from source, not from the document's arithmetic.
- **ENUMERATED** — `internal/ai/ai.go` does **not** exist in `app` and *does* exist in the frozen
  `jobsimulation` husk. Verified at both `b948604f` and the pinned `5ba17044`; the pinned anchors
  `jobsimulation/ai/ai.go:267,344` and `skillerai/ai.go:347` resolve at `5ba17044`.
- **ENUMERATED** — the OpenAI **Models** list is exactly the `ai@v1.40.1` `openai/config.go`
  inventory, 11 for 11 (GPT-5.4, 5.4-mini, 5.2, 5.1, 5, 5-mini, 5-nano, 4.1, 4.1-mini, O3, O4-mini).
- **ENUMERATED** — the studio slot table matches `studio/configs/production_config.ini:26-36` and
  `development_config.ini:26-36` cell-for-cell; `config_template.ini:39`/`:40` carry the `gpt-4o`
  EXECUTION/CREATIVE slots claimed; `grep -rin 'bedrock|boto3' studio/` = **0** (positive control:
  `anthropic` → 34).
- **ENUMERATED** — `checkerEngines` is stored at 6 sites and **read at none** (`checkerEngines[` → 0
  matches). `criterion.go:127` is the `case check.EngineLlm:` dispatch arm, `:168` the
  `validateCodeDiff` call, `:428` `NewLLMBulkChecker`, `:450-475` the pure string comparison; the two
  run concurrently under `pool.New().WithMaxGoroutines(2)` (`:162`). Temperature 0.0 at
  `bulkChecks.go:118`.
- **ENUMERATED** — `skills.go:40-51` `calculateCompetencyLevelScore` matches the corpus formula
  *including* the `// TODO fix this formula` comment; `:75` is the `passed/total*100`;
  `skill.go:617-623` `convertLevelTo100` is a plain division.
- **ENUMERATED** — `calls/livekit.go:131-135` (flag read) / `:140-144` (effect), inside
  `CreateAgentDispatch` (`:106`). `CreateSpeech` is Azure-only (`ai.go:243-253`, *"at the moment we
  only support azure voice"*). `markdownManager.go:19` and `studioManager.go:583` (Mistral OCR);
  `coursebuilder/bedrock.go:106-113`. Embeddings table DDL matches (`schema.go:2563-2567`,
  `:4238-4242`).

**`graphql-wundergraph.md`**
- **ENUMERATED** — the subgraph ladder re-derived by counting `- name:` in
  `supergraph-config-prod.yaml` at each commit: `749dc86~1` = **5** (backend, skiller, jobsimulation,
  cms, skillpath) · `749dc86` = **4** · `7c17e63` = **3** · `915da06` = **1**. Dates 2026-06-24 /
  2026-07-21 / 2026-07-29 all correct. `915da06`'s subject really does say *"supergraph 2→1"* and is
  wrong against its own parent tree; `git show --name-status 915da06` marks **both**
  `schemas/cms.graphqls` and `schemas/jobsimulation.graphqls` `D`. **The corpus's 3 → 1 correction is
  right and the commit message is the thing that is wrong.**
- **ENUMERATED** — `terraform/main.tf:20 service_desired_count = 1`; `schemas/` holds
  `backend.graphqls` alone; `subgraphs.conf` = a single `BACKEND=v1.360.0`; `package.json` is exactly
  `{"name": "graphql-wundegraph"}`; no `subscription:` in any `supergraph-config-*.yaml`; no
  `type Subscription` in `backend.graphqls` (only `PlanSubscription`/`activeSubscription`/
  `stripeSubscriptionId` substrings); `bba862f` is contained by `remotes/origin/feat/use-web-socket`
  only.
- **ENUMERATED** — pins: `ghcr.io/wundergraph/cosmo/router:0.275.0`, `wgc@0.104.0`,
  `node:22.11-alpine`, `federation_version: =2.3.2`; `listen_addr 0.0.0.0:8080` +
  `graphql_path /graphql` in all three config variants; `grep -c 5050 docker-compose.yml` = **0**.
- **ENUMERATED** — `ci/update-subgraph.sh:9` carries **exactly one** `gh release download`, and it is
  `-R anthropos-work/app`.
- **ENUMERATED** — `repos.yml` at `0dab54df` has exactly six entries (app, sentinel, storage,
  messenger, next-web-app, studio-desk) — no `graphql-wundergraph`, no `cms`, no `jobsimulation`,
  no `roadrunner`.
- **ENUMERATED** — `gqlauthz.go:186` is the `unknown viewer` rejection, guarded by the
  public/federation/dev-introspection triple at `:175-177`; `gqlauthz_test.go:132` is
  `"bare __typename is not exempt"` and `:176` is `TestAnonymousRejectionLogsAtWarn`.

**`ai-labs.md`**
- **ENUMERATED** — `creditCost` is exactly `{course.build: 5, course.refine: 1, course.translate: 1}`
  (`internal/credits/cost.go:86-90`). **This is the file's best moment:** `cost.go:29`'s own header
  comment still says *"course.refine → 5 credits per refine turn"*, and the corpus follows the map and
  the test (`cost_test.go:15`), not the stale comment.
- **ENUMERATED** — `DefaultSeedBalance int64 = 500` (`cost.go:231`), `MarginMarkup = 1.40` (`:133`),
  `PricePerCreditUSD = 0.45` (`:142`), packages starter 50 / team 200 / scale 500
  (`cost_test.go:34-36`).
- **ENUMERATED** — `LabsTier` = {essential, professional, frontier} (`labs_budget.go:26-28`);
  `CapReason` = exactly the five claimed (`:247-251`); reconciler `Interval: 30 * time.Second`
  (`spend_reconciler.go:60`); labs-api default `:7070` + `LABS_API_PLATFORM_TOKEN`
  (`labsapi/client.go:6,43`).
- **ENUMERATED** — `LabSessionService` methods are exactly Create/Get/List/Cancel/ReportEvent
  (`labsession.go:90,252,278,333,386`), and the ReportEvent allowlist is `status` + `grade` (`:383`).
- **ENUMERATED** — `checkout.session.completed` appears in **0** `.go` files (positive control:
  `customer.subscription.deleted` → 1); the handled set is `customer.created` +
  `customer.subscription.{created,updated,deleted}` (`api.go:319-327`). `stripe-go/v74 v74.30.0`.
  `/credits/purchase` removed in Wave 13 (`credits/handler.go:12`).
- **ENUMERATED** — all five named migrations exist verbatim under `terraform/migrations/`
  (`20260617203555_add_labs_catalog.sql`, `20260529072659_add_lab_session.sql`,
  `20260617120000_add_lab_session_model.sql`, `20260717151144.sql`,
  `20260626120000_add_org_subscriptions.sql`); all eight named tables exist in the ent migrate schema.
- **ENUMERATED** — `LabSession` deliberately omits `PrimaryKeyMixin` with a `field.String("id")`
  (`schema/lab_session.go:21-36`); `LabBody` is one row per `(lab_slug, locale)` with EN + **6**
  overlays (`de/es/fr/it/nl/pt`, `lab_body.go:49`); `cmd/labsdemo` binds `:8099`
  (`cmd/labsdemo/main.go:115`). `app` is **v1.363.2** at the pinned `5ba17044`.

**`coursebuilder.md`**
- **ENUMERATED** — every `main.go` anchor is exact at the pinned `b948604` v1.366.0: `:770` is the
  `logger.Info("coursebuilder model backend", …)`, `:766-769` the comment, `:774`/`:778` the two
  `routes disabled` Warn arms, `:824-826` the `imagegen.NewOpenAIImageClient(os.Getenv("OPENAI_KEY"),
  imagegen.WithModel(os.Getenv("CB_IMAGE_MODEL")))`.
- **ENUMERATED** — `COURSEBUILDER_OPENAI_IMAGE_KEY` occurs in **0** `.go` files and survives only in
  `internal/coursebuilder/{README,GO-LIVE-RUNBOOK}.md`; `git log -S … -- '*.go'` names exactly two
  commits, the later of which is `68c245123` — so "deleted at app `68c24512`" is precisely right.
- **ENUMERATED** — `variables.tf:635-638` (`sensitive`, no default), `ssm.tf:328-333`,
  `main.tf:555` (`"name": "ANTHROPIC_API_KEY"`); `bedrock.go:98-104` (`ModelBackendName`) and
  `:105-114` (`newUnderlyingClient`).
- **ENUMERATED** — `DefaultAuthorModelID = "eu.anthropic.claude-opus-4-8"` (`bedrock.go:23`),
  `DefaultGraderModelID = "eu.anthropic.claude-sonnet-4-6"` (`:29`), `defaultModel = "gpt-image-2"`
  (`imagegen/openai.go:22`), `DefaultSessionsPerOrgPerDay = 50` (`rate.go:28`),
  `DefaultMaxMonthlyCOGSUSD = 500.0` / `DefaultMaxDailyCOGSUSD = 0.0` (`budget.go`),
  `coursebuilderWorkerConcurrency = 20` (`main.go:173`), `TypeCoursebuilderRun` + `MaxRetry(1)` +
  `Retention(6*time.Hour)` (`run_task.go:49,199,204`); Go `1.26.4`.
- **ENUMERATED** — migration `20260717151144.sql` creates exactly the three tables claimed
  (`course_builder_sessions`, `credit_transactions`, `organization_credits`); all five named in-repo
  docs exist.

**`customerio-sync.md`** — **ENUMERATED, and the only assigned file I found nothing at all in.**
`profiles: [customerio-sync, all]` is at `docker-compose.yml:154` exactly; the quoted compose block
matches `:136-154` field-for-field (it omits `env_file`/`networks` as an excerpt); `8080:8080`;
`context: git@github.com:anthropos-work/customerio-sync.git#main` with `ssh: ["default"]` and
`GH_ACCESS_TOKEN: $GH_PAT`; absent from `repos.yml`. It is also the file that carries the **correct**
`core`-not-`graphql` statement that refutes B1.

**`architecture/README.md`**
- **ENUMERATED** — the index lists exactly the 10 non-README docs present in
  `corpus/architecture/`; nothing listed is missing and nothing present is unlisted.
- **ENUMERATED** — `github.com/anthropos-work/authn` appears in **0** `go.mod` *and* **0** `go.sum`
  across all seven checked-out Go repos (app, cms, jobsimulation, messenger, roadrunner, sentinel,
  storage); positive control `anthropos-work/colony` → 1 in `app/go.mod`. Both conjuncts of the
  sentence tested, per §5 rule 17.
- **ENUMERATED** — `rosetta-extensions/stack-core/platform_alignment_guard.py`,
  `rosetta-extensions/alignment/`, and `stack-seeding/dna/` + `stack-seeding/cmd/datadna` all exist.

---

## What I could not settle

- **`ai_architecture.md:183-189` — the "five LiveKit agent repositories"** (`livekit-agent`,
  `livekit-agent-chain`, `livekit-agent-azure-us`, `livekit-agent-azure-eu`,
  `livekit-agent-azure-eu-fr`) and **`ai-labs.md:5` — `anthropos-work/AI-Labs`**. Both are claims
  about the GitHub org's repo set. `gh` is not installed on this host and there is no network path I
  could use read-only. *Settled by:* `gh repo list anthropos-work --limit 200 --json name`, or the
  `platform-migration-status.md` §3 derivation these cite.
- **`graphql-wundergraph.md:8` — "the repo is ARCHIVED on GitHub, 2026-07-30"**. Same reason. The
  local clone shows nothing about archive state. *Settled by:* `gh repo view
  anthropos-work/graphql-wundergraph --json isArchived,archivedAt`.
- **`ai_architecture.md:138` — `skill_embeddings` = 42,790 rows / `job_role_embeddings` = 18,919 rows
  (measured 2026-06-29)**, and **`ai-readiness.md:342-344` — "78.4 % (≈156 of the 199 frozen
  snapshots)"**. Live-DB measurements; no DB reachable from this pass. The seeder constant that
  *produces* the second one does check out (`aiReadinessCompletedShare = 0.80`,
  `ai_readiness_funnel.go:89`), and 156/199 = 78.39 %. *Settled by:* `/db-query`, or a cold
  reset-to-seed count on a live stack.
- **`ai-labs.md:16` — `app knowledge/plan/releases/06.00-shared-purse/`, milestones M600–M607, all
  planned.** The path is under a `knowledge/plan/**` prefix, which the briefing bars unconditionally
  (it does not qualify the repo), so I did not `ls` or read it. *Settled by:* whoever holds a
  clarification that the bar is rosetta-scoped.
- **`ai-readiness.md:463,470-472` — the live latency figures (`HTTP 200 in 24 ms` frozen, `2.09 s /
  304 KB` live).** The doc itself says "Re-measure before relying on either number"; I had no running
  stack. *Settled by:* an authenticated `GET /api/workforce/ai-readiness` against a seeded 199-member
  org.
