# Seat D — reading #13

Refs at time of measurement (re-derived at open, all clean working trees):
`app` `b948604ff` · `platform` `0dab54d` · `next-web-app` `bb3313bc0` · `cms` `ca50c81` ·
`roadrunner` `87d8d44` · `jobsimulation` `462343b0` · `graphql-wundergraph` `60c229f` ·
`studio-desk` `14a5442a` · `.agentspace/rosetta-extensions` authoring copy @ `main`.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| corpus/services/studio-room.md | 473 | yes |
| corpus/services/clerkenstein.md | 366 | yes |
| corpus/services/cms.md | 259 | yes |
| corpus/services/roadrunner.md | 171 | yes |
| corpus/services/next-web-app.md | 126 | yes |
| corpus/services/gotenberg.md | 82 | yes |
| corpus/services/intelligence.md | 18 | yes |

(also read, as instructed: `corpus/ops/platform-alignment.md` §5, lines 240–1147 of 1680)

---

## Blockers

### B1 — roadrunner.md says roadrunner is still in `repos.yml`, "1 of the 9 repos"; it is not in `repos.yml` at all

- **Anchor:** `corpus/services/roadrunner.md:23-26` —
  > **ORPHANED, but NOT archived — a deliberate M247 resolution.** Unlike intelligence / skiller / skillpath
  > (which were *removed* from `repos.yml` + `docker-compose.yml` …), roadrunner is **still in `repos.yml` (1 of
  > the 9 repos — the count dropped when platform `2adcf71` deleted the router entry)**

- **Why it is false:** `platform/repos.yml` @ `0dab54d` has **six** entries — `app` (:11), `sentinel` (:15),
  `storage` (:18), `messenger` (:21), `next-web-app` (:26), `studio-desk` (:29). There is **no `roadrunner`
  entry**. `platform` `d11a403` ("chore(compose): drop roadrunner, prune dead env, repoint messenger",
  2026-08-03) removed it, together with the `cms` and `jobsimulation` entries. `roadrunner` now survives in
  `repos.yml` only as the word inside a **comment** at `:5` (*"skiller, skillpath, roadrunner, jobsimulation and
  cms are all served in-process"*) whose next lines say *"they own no local schema, no compose service and **no
  clone entry here**"* (`:7-8`).
  The claim was true at `2adcf71` (9 entries, roadrunner at `:29-31`), but the sentence is present-tense
  (*"is still in"*) and §5 rule 33 forbids pinning a currency assertion into silence — and the only ref it names
  is `2adcf71`, cited as the date the count *dropped to 9*, not as a measurement date.
  **Also a direct corpus self-contradiction:** `corpus/services/cms.md:12-13` states *"no `cms` entry in
  `repos.yml` (6 entries: app, sentinel, storage, messenger, next-web-app, studio-desk)"* — a six-member
  enumeration that excludes roadrunner.
- **Confidence:** high

### B2 — roadrunner.md quotes `repos.yml:29-31` as saying "legacy — folded into app"; at the ref the same sentence names, `:29-31` is the `studio-desk` entry

- **Anchor:** `corpus/services/roadrunner.md:12-15` —
  > *"There is no roadrunner service in production"* overstates it: `roadrunner/terraform/main.tf:19` still reads
  > `service_desired_count = 1` … while `repos.yml:29-31` says *"legacy — folded into app"* and there is **no
  > `roadrunner` compose service** at platform `0dab54d` to start locally.

- **Why it is false:** the pin `0dab54d` sits inside **this same wrapped sentence**, so under §5 rule 33 it is
  the ref that settles the `repos.yml:29-31` clause too. At `0dab54d`, `platform/repos.yml:29-31` is:
  ```
  29	  - name: studio-desk
  30	    type: node-npm
  31	    migrations: false
  ```
  The string *"legacy — folded into app"* appears **nowhere** in the file (verified by `grep -in legacy
  repos.yml` → only `:8`, *"legacy: they own no local schema, no compose service and no clone entry here"*;
  positive control `grep -c app repos.yml` → 5).
  It was exactly right at `2adcf71`, where `repos.yml:29-31` read
  `- name: roadrunner / type: go / migrations: false # legacy — folded into app; backend calls Judge0 directly`.
  Anchor + quotation both moved with `d11a403`.
  (`roadrunner/terraform/main.tf:19` `service_desired_count = 1` in the same sentence **is** correct — verified.)
- **Confidence:** high

### B3 — roadrunner.md asserts a live `jobsimulation` clone entry, compose service, `graphql` profile and auto-starting container; none exists at `0dab54d`

- **Anchor:** `corpus/services/roadrunner.md:28-33` —
  > **`jobsimulation` was previously listed above as also *removed* from both files; it is not (corrected
  > M257x iter-46).** Its GitHub repo IS archived (2026-07-31), but it remains in `repos.yml:17` **and** in
  > `docker-compose.yml:83` with `profiles: [graphql, jobsimulation, all]` — so its container **starts on a
  > bare `make up`**.

- **Why it is false** — four independent falsifications at `platform` `0dab54d` (the block names no ref of its
  own, so it grades at the checkout):
  1. **No `jobsimulation` entry in `repos.yml`.** `repos.yml:17` is `    migrations: false` (the third line of
     the **sentinel** entry). The six entries are enumerated in B1.
  2. **No `jobsimulation` compose service.** Parsing both compose files, `docker-compose.yml` declares 8
     services (`sentinel`, `backend`, `storage`, `customerio-sync`, `messenger`, `studio-desk`,
     `next-web-app`, `gotenberg`) and the `include:`d `common.yml` declares 2 (`postgresql`, `redis`).
     `docker-compose.yml:83` falls **inside the `backend` service block** (`backend:` at `:28`, `storage:` at
     `:102`). The only `jobsimulation` strings left in compose are `:58 JOBSIMULATION_STREAM=jobsimulation`,
     a comment at `:89`, and a comment at `:171`.
  3. **No `graphql` profile.** `0dab54d` is *"chore(compose): run without the standalone storage; **rename
     graphql -> core**"*; `Makefile:10` reads `PROFILE ?= core`. `graphql` is a retired token — which
     `roadrunner.md:57` itself says, four lines below.
  4. **Nothing starts.** Since there is no such service, `make up` cannot start it. (A `--profile
     jobsimulation` invocation exits 0 and starts the 3-service floor — §5 rule 30 — which is what
     `roadrunner.md:41-42` correctly says about roadrunner.)
  The passage is fortified anti-repair text ("corrected M257x iter-46 … listing it as removed contradicted
  `:26` of this very blockquote"), so it reads as adjudicated — §5 rule 31's exact failure mode. It also cites
  `corpus/services/README.md:20-21` as corroboration, and **that passage now says the opposite**: *"At platform
  `0dab54d` they are **gone from compose and from `repos.yml`** as well."*
- **Confidence:** high

### B4 — cms.md: "`0dab54d`'s compose declares nine services"

- **Anchor:** `corpus/services/cms.md:34-35` —
  > **the M809 re-point has landed**, and there is no husk container left to reach: `0dab54d`'s compose
  > declares nine services and `cms` is not one of them.

- **Why it is false:** at `platform` `0dab54d`, `docker-compose.yml` declares **8** services and its
  `include:`d `common.yml` declares **2** (`postgresql`, `redis`) — **10** in the resolved project, **8** in
  `docker-compose.yml` alone. Neither reading is nine. The likeliest origin is counting the nine top-level
  two-space keys in `docker-compose.yml`, of which the ninth (`app-network:` at `:271`) is a **network**, not a
  service — the same "resolve the `include:`" hazard §5 rule 30's first corollary names.
  **Corpus twin disagrees:** `corpus/architecture/architecture_overview.md:191` says, for the same ref,
  *"(deleted by `d11a403`; **8 services** remain)"*. The same "nine services" sentence also stands at
  `corpus/services/jobsimulation.md:35` and `corpus/architecture/external_services.md:296` (outside my file
  set — flagged for the repair pass, not adjudicated here beyond the count).
  I hesitated because the load-bearing half (*"`cms` is not one of them"*) is **true** — but the briefing books
  wrong counts as blockers, and a reader who counts to check will find 8 or 10 and distrust the paragraph.
- **Confidence:** medium-high

### B5 — cms.md lists `python-docx` in the studio pipeline's `requirements.txt`; it is not there

- **Anchor:** `corpus/services/cms.md:115` —
  > `  requirements.txt         openai, anthropic, mistralai, rich, pyyaml, python-docx, requests, jinja2, pytest, pytest-asyncio (see studio/requirements.txt)`

- **Why it is false:** `cms/studio/requirements.txt` (the file the line cites, `cms` @ `ca50c81`) contains
  exactly nine entries — `openai`, `anthropic`, `rich`, `pyyaml`, `requests`, `jinja2`, `mistralai`, `pytest`,
  `pytest-asyncio`. There is **no `python-docx`**. `grep -rn "docx" cms/studio/requirements.txt` → 0 with the
  positive control `grep -n openai …` → `1:openai`. The post-merge copy `app/studio/requirements.txt` @ `app`
  `b948604f` is byte-equivalent and likewise has no `docx`.
  **Self-contradiction:** `corpus/services/studio-room.md:345-356` publishes the same list **correctly** (nine
  entries, no `python-docx`), so the corpus states two different contents for one file.
  This matters operationally: `cms.md:220` and `studio-room.md:342` both instruct `pip install -r
  requirements.txt`, and a reader reconciling a missing DOCX capability against a phantom dependency chases a
  package the pipeline never had.
- **Confidence:** high

### B6 — roadrunner.md's env table gives `PORT`/`RPC_PORT` **defaults** of 10400/10401; the code defaults are 8080/8081, and no compose file supplies the override any more

- **Anchor:** `corpus/services/roadrunner.md:148-149` —
  > | `PORT` | `10400` | HTTP health port (`/_meta` only) |
  > | `RPC_PORT` | `10401` | Connect-RPC port |
  >
  > and `:56` — "**Ports**: 10400 …, 10401 … — same on host and inside container per
  > `platform/docker-compose.yml` (`PORT=10400`, `RPC_PORT=10401`)"

- **Why it is false:** `roadrunner/cmd/root.go` @ `87d8d443` reads
  `:84  rpcPort := cmp.Or(os.Getenv("RPC_PORT"), "8081")` and
  `:110 port := cmp.Or(os.Getenv("PORT"), "8080")`.
  The 10400/10401 values were **compose-supplied**, and the `roadrunner` compose block that supplied them
  (`PORT=10400`, `RPC_PORT=10401`, ports `10400:10400`/`10401:10401`, `profiles: [graphql, roadrunner, all]`)
  was deleted by `d11a403`; there is no roadrunner service in `docker-compose.yml` @ `0dab54d`. So the `:56`
  citation to `platform/docker-compose.yml` is present-tense and empty, and it contradicts `:57` of the same
  doc (*"none — there is no `roadrunner` compose service"*).
  Operationally: the doc's own **Run natively** recipe (`:120-127`, `go run main.go`, sourcing `platform/.env`,
  which sets no `PORT`) binds 8080/8081, and the **Smoke-test** block at `:135`/`:140` then curls
  `http://localhost:10401/...` and cannot connect.
  I hesitated because the banner at `:42-43` says *"Everything below describes the service as built, not as
  used"* — but "as built" is precisely the 8080/8081 reading, so the disclaimer makes the row **more** wrong,
  not less.
- **Confidence:** medium

---

## Minors

- `corpus/services/clerkenstein.md:3` — header says **"Last updated: 2026-07-14"** and the Status line stops at
  *"M218 the roster-aware fake BAPI"*, while the body documents M219 (`:51-59`), M220 (`:301-308`), M224
  (`:228-251`), v2.8 M256 (`:156-205`) and M257x iter-23 (`:270-275`). Stale metadata contradicting its own body.
- `corpus/services/clerkenstein.md:17-18` — *"ONE private monorepo with sections (`clerkenstein`, `demo-stack`,
  `stack-injection`, `stack-core`, `stack-seeding`, `alignment`)"* enumerates 6 of the authoring copy's 11
  top-level sections; missing `dev-stack`, `playthroughs`, `stack-secrets`, `stack-snapshot`, `stack-verify`.
- `corpus/services/clerkenstein.md:101` — the `cmd/` row lists `mintpk` / `fake-fapi` / `fake-bapi`; the dir also
  contains `jwtkey`.
- `corpus/services/next-web-app.md:82` — `# or: make up-frontend  # next-web-app together with the graphql
  backend stack`: the profile that target selects is `core` (`platform/Makefile:120`) since `0dab54d` renamed
  `graphql` → `core`.
- `corpus/services/next-web-app.md:38-43` — shared-packages table omits `packages/design` (present at
  `next-web-app` `bb3313bc`).
- `corpus/services/cms.md:90-117` — the key-directories tree omits `internal/{organization,templates,user,jobsimimport}`
  (all present in `cms` @ `ca50c81`; `jobsimimport` is named in the banner at `:26` but not in the tree).
- `corpus/services/gotenberg.md:19-28` — the illustrative YAML renders `command:` and `ports:` as block
  sequences; compose uses flow sequences. No factual consequence.
- `corpus/services/studio-room.md:181,199,230` — `parse_argument` is `gen.py:18-27` (line 28 is blank); cited as
  `:18-28`. Pure line drift, construct correct.

---

## Positively cleared (audited zeros)

All labels below are **ENUMERATED** unless marked otherwise — i.e. I derived the predicate/set from platform
source independently and compared, rather than re-running arithmetic the document displays.

**studio-room.md**
- ENUMERATED — *"registers **exactly nine** arguments (`gen.py:484-492`)"*: `grep -n add_argument
  app/studio/gen.py` returns exactly 9 hits, at `:484`–`:492`, matching the nine flags listed
  (`-i/--interactive`, `-m/--media`, `-f/--force`, `--simid`, `--branch`, `--prompt`, `--annotations`,
  `--pipeline`, `--blueprint`).
- ENUMERATED — *"`template` … a key nothing in the codebase ever reads (grep: zero consumers)"*: every
  `template` occurrence in `app/studio/**/*.py` is either `config_template.ini`, a jinja identifier, the
  argparse **description** string (`gen.py:483`), `translate_legacy_blueprint`'s own strip logic
  (`gen.py:212-238`), or a test of that logic. No read site.
- ENUMERATED — `_LEGACY_TEMPLATE_DEFAULTS` has exactly the three names claimed (`micro challenge`,
  `scenario challenge`, `collaborative challenge`), `gen.py:205-210`, and the else-branch emits the quoted
  *"is ignored; asset type is now inferred from task interactions"* warning.
- ENUMERATED — `GenMode` has exactly `FAST/STRICT/EXECUTION/CREATIVE/REASONING` + `DEFAULT = EXECUTION`
  (`services/ai.py:35-42`).
- ENUMERATED — `postgen.py` marks `--media`, `--simid`, `--target` `required=True` (`:396-398`) and no others.
- ENUMERATED — post-gen target modules = `{guidance, metadata, testing, toolkit, translation}` + `exporter`
  (not a target), matching the table + the "`testing` phase module also exists" note.
- ENUMERATED — `services/ai.py` client instantiation at `:1-2` (imports), `:383` `return OpenAI(...)`, `:530`
  `return AzureOpenAI(...)`, `:664` `return Anthropic(...)`.
- ENUMERATED — `agents/simulation/model.py:59` (`self._cache = {}`) and `:467-469` (the skill-id memo around
  `TaxonomyService.get_skill_by_id`); `postgen/exporter.py:513-550` is `_create_export_package` end-to-end and
  `:518-519` are the postgen/published zip paths; the `source_id`-else-`sim_id` zip naming is `:517`.
- ENUMERATED — `app/internal/cms/studio/studioManager.go:94-96` = `studioVenvDir/Python/Pip`
  (`"studio/studio-venv"`), `:119` = `runCommand(ctx, pyBin, append([]string{"studio/gen.py"}, tokens...))`.
- ENUMERATED — asynq `Concurrency: 5`, `AiVideoQueue: 7`, `StudioQueue: 3`
  (`app/internal/cms/worker/worker.go:29-34`).
- ENUMERATED — `python:3.11-slim` final stage (`app/Dockerfile:28`) + `COPY --from=build /build/studio ./studio`
  (`:45`); CI `additional_repo: "anthropos-studio-room:studio"` (`.github/workflows/build-production.yml:29`),
  landed in `v1.360.1` (`851cf3fb1 fix(ci): pull studio via additional_repo`).
- ENUMERATED — `max_tokens = 4000` in **all three** tracked configs (`config_template`, `development`,
  `production`); `worklog_path = workspace/trace` in all three; `configs/local_*` + `configs/test_*` gitignored
  (`.gitignore:5-6`); the in-code fallback is a literal `'worklog'` (`gen.py:106`).
- ENUMERATED — the blueprint example's keys all exist verbatim in `tests/e2e/blueprints/technical.json`; the
  five e2e blueprints and the three `knowledge/development/asset-examples/blueprints/` files are exactly as
  listed.
- ENUMERATED — `requirements.txt` = the 9 packages listed, no `aiohttp`.
- ENUMERATED — `--evaluation_skills` **is** consumed downstream by prep (`agents/simulation/prep.py:75-77`).

**clerkenstein.md**
- ENUMERATED — DNA gene/capability cardinalities, parsed from the JSON: `clerk-2.6.0` **14 caps / 27 genes**;
  `clerk-js-5` 9; `clerk-multi-1` 9 (Roster 2); `clerk-deploy-1` 7; `clerk-express-1` **13 genes / 5 caps**.
- ENUMERATED — `alignment/cmd/alignctl/run.go:134-135` = `ExitRegressed = 2` / `ExitUnmeasurable = 3`, with the
  surrounding block stating the exact rule the doc quotes.
- ENUMERATED — `clerk-backend/store.go:138` = `func (s *Store) SeedOrgIdentity`, `:151` =
  `func (s *Store) LookupOrgEid`; `alignment/dna/clerk-2.6.0.json:131` carries the quoted
  *"M219 landed the fix … taking the Go surface 97.2% -> 100%"*.
- ENUMERATED — `clerk-js-5.json` has a `Me` capability (`universal-user`, `unauthenticated`) and **zero**
  occurrences of `organization_memberships`, exactly as the "not yet a measured gene" box claims.
- ENUMERATED — the BAPI/FAPI route split: `clerk-backend/server.go:47` `GET /v1/users/{userID}/organization_memberships`;
  `clerk-frontend/server.go:186` `GET /v1/me/organization_memberships`; the three pinned properties each have a
  test in `clerk-frontend/meorgmemberships_test.go` (`paginatedEnvelope`, `unauthenticated401`,
  `offsetPastEndIsEmptyArray`, `followsActiveSeat`).
- ENUMERATED — `r.Cookie(` occurs **zero** times across all 63 `.go` files in `clerkenstein/`; positive control
  (`grep -rni cookie`) returns 20 hits, all emission-side. `handleSignOut(w, _ *http.Request)`
  (`clerk-frontend/server.go:554`) discards the request, so it does ignore its `{id}`.
- ENUMERATED — all five named regression tests exist in `clerk-frontend/server_test.go` (`:256`, `:286`, `:390`,
  `:427`, `:461`); `signedOut` field at `server.go:107`; `clientID: "client_clerkenstein"` at `:125`;
  `_method` dispatch whitelist at `:583-584`.
- ENUMERATED — `clerk-frontend/server.go:35-67` is the M220 disk-cache block; `clerkJSFetchTimeout = 15 *
  time.Second` (`:59`), `var clerkJSClient = &http.Client{Timeout: clerkJSFetchTimeout}` (`:68`) with the
  *"Explicitly NOT http.DefaultClient"* comment.
- ENUMERATED — colony pins: `clerkenstein/go.mod:8` `v0.34.3`; `app/go.mod:16` `v0.35.2`; `sentinel/go.mod:8`
  and `storage/go.mod:7` `v0.34.3`; `messenger/go.mod:7` `v0.35.2`; `app/go.mod:31`
  `clerk-sdk-go/v2 v2.7.0`. The ⚠️ drift box is correct at the checkout as well as at its pinned ref.
- ENUMERATED — `STUDIO_ACCESS_ROLES = ['admin','org:admin','content_creator','org:content_creator']` at
  **`studio-desk/src/index.ts:96`** and **`studio-desk/app/services/userService.ts:16`**, each preceded by the
  quoted "Both the prefixed (`org:*`) and bare role keys are accepted" comment.
- ENUMERATED — repo dirs (`authn`, `clerk-backend`, `clerk-frontend`, `clerk-webhook`, `shared`, `deploy`,
  `cmd`, `alignment`), `alignment/cmd/{clerkrun,deployrun,expressrun,jsfapirun,multirun}`, five DNAs, the five
  `golden*` dirs + `scripts/`; the sibling `rosetta-extensions/alignment/` section indeed has **no** `scripts/`;
  `clerkenstein/.github/workflows/alignment.yml` is git-tracked (and, being below the repo root, inert).
- ENUMERATED — `dotless-pk-rejected` gene present in `clerk-express-1.json:24`.

**cms.md**
- ENUMERATED — `repos.yml` **6 entries** exactly as listed, no `cms`.
- ENUMERATED — compose sets **four** `*_RPC_ADDR` values (`docker-compose.yml:173,174,176,183`) and **all four**
  read `http://backend:8083`; `:174` is `CMS_RPC_ADDR`.
- ENUMERATED — the "no cms profile; asking for one exits 0 and starts postgresql, redis and sentinel" claim:
  `docker-compose.yml` has 7 `profiles:` keys over 8 services (only `sentinel` lacks one) and `common.yml` has
  0 over 2, so the always-on floor is exactly `{sentinel, postgresql, redis}` — 3.
- ENUMERATED — `cms/terraform/main.tf:39` = `service_desired_count = 0` (the anchor cms.md:7 cites);
  `cms/go.mod:3` = `go 1.26.4`; `cms/Dockerfile:2` = `FROM golang:1.26-bookworm AS build`, `:23` =
  `FROM python:3.11-slim`.
- ENUMERATED — the supergraph ladder, from `supergraph-config-prod.yaml` at each parent:
  `749dc86^` = 5 subgraphs → `749dc86` removes `skiller` (4); `7c17e63^` = 4 → `7c17e63` removes `skillpath`
  (3); `915da06` deletes **both** `schemas/cms.graphqls` (762 lines) and `schemas/jobsimulation.graphqls`
  (860 lines) → 1. The corpus's *"not 2 to 1"* correction of the commit's own subject line is right.
- ENUMERATED — `REDIS_CMS_CACHE_INDEX` default 5 (`app/main.go:997-998`, `app/terraform/variables.tf:582`).
- ENUMERATED — `app/internal/skillpath/session.go:205-207` is exactly the quoted deseam comment + the
  `GetSkillPathDomain` call, and `u.cms` is typed `contentread.CmsContentReader` (`:50`).
- ENUMERATED — `app/main.go:1205-1211` @ the checkout is the *"Additive + DORMANT … until the M809 re-point"*
  comment; the corpus's *"that comment is stale in `app`; grade the address against compose"* is correct.
- ENUMERATED — `app/terraform/migrations/20260724132049_cms_data_model.sql` exists; `app/cms_reader_switch.go`
  exists; `DIRECTUS_DATA_CONSUMERS = ("cms", "backend")` (`stack-injection/gen_injected_override.py:84`).
- ENUMERATED — webhook: `POST /api/webhook/directus` registered at
  `app/internal/web/backend/backend.go:324`, Clerk-skipped at `:138`, handler built with
  `os.Getenv("DIRECTUS_WEBHOOK_SECRET")` (`app/main.go:1089`, fail-closed); the standalone receiver really was
  `:8090/webhooks/…` with no secret (`cms/cmd/root.go:242` + `cms/internal/directus/webhooks/webhooks.go`).
- ENUMERATED — cms ports 8090/8091 (from the historical compose block at `2adcf71`, `PORT=8090`/`RPC_PORT=8091`)
  and `ent/schema/studioDocument.go` + `studioTask.go` both present.
- ENUMERATED — `make up` = `PROFILE ?= core` (`platform/Makefile:10`), so *"the `core` profile"* at `:202` is
  right (and is the correct reading `CLAUDE.md`'s `graphql`-profile table has not caught up with).
- ENUMERATED — legacy `cms/internal/studio/studioManager.go:98,951,967` really does build a `bash -c` string
  with the venv bootstrap, matching the note at `:235`.

**roadrunner.md**
- ENUMERATED — `roadrunner/terraform/main.tf:19` = `service_desired_count = 1` (untouched at `87d8d44`).
- ENUMERATED — `ROADRUNNER_RPC_ADDR` / `RoadRunnerService` / `roadrunner:10401` occur **zero** times in
  `platform/**` and `app/**` (positive controls: `JUDGE0_BASE_URL` → `app/internal/jobsimwiring/wiring.go:118`;
  `JUDGE0_API_KEY` → `platform/.env_example:87`), and `roadrunner` appears **nowhere** in `platform/.env_example`.
- ENUMERATED — the in-process runner: `jobsimulation/internal/runner/runner.go:1-3` carries the quoted
  *"(formerly the standalone \"roadrunner\" service)"* header, and the same package now lives at
  `app/internal/jobsimulation/runner/`.
- ENUMERATED — `go 1.25.0`; `hibiken/asynq v0.25.1`; `gorilla/websocket v1.5.3`; **0** `*_test.go` files;
  `Dockerfile:18` = `RUN go test -v ./...`.
- ENUMERATED — `tasks.RoadrunnerSubmissionResult = "roadrunner:submissionresult"`,
  `queues.DefaultQueue = "roadrunner:default"`, `asynq.MaxRetry(3)` (`internal/runner/runner.go:126`),
  `Concurrency: 10` (`internal/worker/worker.go:25`), `maxRetries = 15` + `time.Sleep(1 * time.Second)`
  (`runner.go:244,273`), `Event_RoadrunnerSubmissionCompleted` publish (`runner.go:276-278`).
- ENUMERATED — the RPC surface is exactly `Submission`, `SubmissionPackage`, `SubmissionResult`
  (`internal/rpcsrv/*.go:20,43,60`); `internal/lsp/lsp.go` is referenced by no server bootstrap; the language
  map keys on `"py"` (`internal/runner/languages.go:26`).
- ENUMERATED — the 3-service floor claim at `:114-115` (same derivation as cms.md above).

**next-web-app.md**
- ENUMERATED — `"next": "^16.2.7"` in **all four** app package.jsons; `react ^19.2.7`; `node >=24.0.0`;
  `packageManager pnpm@10.30.3`; `turbo ^2.9.6`; `UPGRADE-IMPACT-next16.md` at the repo root.
- ENUMERATED — `apps/web/src/proxy.ts` and `apps/hiring/src/proxy.ts` exist; **no** `middleware.ts` anywhere
  outside `node_modules`; repo `CLAUDE.md:55` is verbatim the quoted Next-16 rename note and `CLAUDE.md:15` is
  *"…Next.js 16 App Router…"*.
- ENUMERATED — every route the doc names as public is in `isPublicRoute` (`/login`, `/sign-up`, `/checkout`,
  `/free-trial`, `/monitoring`, `/print`, `/api/bunny/thumbnail`); `PRINT_ROUTE_SECRET` is read by
  `packages/core-js/src/security/printToken.ts:16,49`.
- ENUMERATED — package names + ports: `@anthropos/web-app` 3000, `@anthropos/hiring-app` 3001,
  `@anthropos/integration` 3002, `@anthropos/maintenance-app` (no port), `@anthropos/mobile` expo `--port 3031`
  and `!apps/mobile` in `pnpm-workspace.yaml`.
- ENUMERATED — **8** locale dirs in `configs/i18n/messages/`: `de en es fr it ja nl pt`.
- ENUMERATED — `docker-compose.yml:236` is the `NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT` build arg
  `http://${PUBLIC_HOST:-localhost}:8082/graphql/query`; `apps/web/.env.example:62-63,36` carry the documented
  defaults and `packages/graphql/codegen.ts:8` carries the `// was 'http://localhost:5050/graphql'` note.
- ENUMERATED — `Dockerfile.dev` is the **only** Dockerfile at the repo root, `FROM node:24-alpine`, and builds
  `--filter=@anthropos/web-app` (`:32`); repo `CLAUDE.md:82-84` does claim "Two Dockerfiles", so the corpus's
  "stale" note is correct.
- ENUMERATED — no `.storybook/` dir and no `storybook` script anywhere; `configs/tailwind/storybooks.css` is the
  only trace, exactly as written.
- ENUMERATED — `test` scripts exist in `apps/web` (`:110`) and `apps/hiring` (`:105`) only, both `jest`;
  `integration`/`maintenance` have none. `e2e/` dir present.
- ENUMERATED — data layer: `graphql-request ^7.4.0` + `@tanstack/react-query ^5.99.2` +
  `@graphql-codegen/client-preset ^4.8.3`; **no** apollo dependency anywhere; `antd ^6.3.7` in `packages/ui`;
  codegen output at `packages/graphql/src/__generated__`.

**gotenberg.md**
- ENUMERATED — `docker-compose.yml:255-268`: `image: gotenberg/gotenberg:8`, the four command flags verbatim
  (`--api-port=3200`, `--api-timeout=60s`, `--libreoffice-restart-after=50`), `ports: 3200:3200`, and
  `profiles: [core, backend, all]` at exactly `:268`. `GOTENBERG_URL=http://gotenberg:3200` at `:57` on
  `backend`.
- ENUMERATED — `app/internal/converter/gotenberg.go`: `ConvertToPDF(ctx, gotenbergURL, document, filename)`
  (`:16`), `gotenbergURL+"/forms/libreoffice/convert"` (`:31`), `http.Client{Timeout: 90 * time.Second}`
  (`:13`). `GOTENBERG` appears only in `app/` (`main.go`, `internal/web/backend/coursebuilder/handler.go`) —
  `sentinel`, `storage`, `messenger` have zero hits, so "the only consumer" holds.

**intelligence.md**
- ENUMERATED — `platform` `fdfa189` ("chore: remove intelligence service from local dev orchestration") touched
  exactly `docker-compose.yml` (−25) and `repos.yml` (−3); `grep -rn intelligence` over
  `repos.yml/docker-compose.yml/common.yml/Makefile` returns **0** (positive control `grep -c app repos.yml`
  → 5). No service depends on it.

---

## What I could not settle

- **GitHub archival states** asserted in `roadrunner.md:23-26,29,35-37` — *"skiller 2026-07-01"*,
  *"skillpath 2026-07-31"*, *"[jobsimulation's] GitHub repo IS archived (2026-07-31)"*, *"chronos … last push
  2026-04-23, NOT archived"*, and `intelligence.md:8`'s *"The Intelligence GitHub repository still exists"*.
  These are org-API facts; nothing in the local clones records an archived flag. Settled by
  `gh repo view anthropos-work/<name> --json isArchived,pushedAt`.
- **Production RPC addresses** — `cms.md:36,162` *"`http://backend.internal.anthropos:8081` in production"*.
  `messenger/terraform/main.tf:74` sets `CMS_RPC_ADDR = "${var.cms_rpc_address}"` and
  `messenger/terraform/variables.tf:77-80` declares it with **no default**; the literal lives in the
  `infrastructure` repo, which is not cloned here. Same for `roadrunner.md`'s prod framing beyond
  `terraform/main.tf:19`.
- **`module.cms_euwest1` / `module.jobsimulation_euwest1` rollback declarations** (`cms.md:53-55`) — they cite
  `infrastructure/terraform/production/services.tf`, not present in `stack-demo/`. `cms.md:16-19` already flags
  this as unmeasured, which is the honest form.
- **Intelligence's historical internals** (`intelligence.md:14-19`: `DB_CONNECTION_BACKEND` /
  `DB_CONNECTION_SKILLER`, port 8080/`_meta`, 5-minute ticker) — no `intelligence` clone exists in
  `stack-demo/` or `stack-dev/`, so nothing in reach can confirm or refute them.
- **Provenance claims about prior milestones** — `roadrunner.md:5-6` (*"verified v2.5 M231 KB-6; re-verified
  v2.7 M247 … the ~386-commit `app` bump"*), `cms.md:77` (*"M257x iter-24 measured that as 96 all-403 Directus
  lines"*), `clerkenstein.md:121` (*"~4.05 s 3-attempt retry ladder"*). Each names a measurement I cannot
  re-run from the clones; §5 rule 27's *"quoting a prior finding forward is not evidence"* applies, but I have
  no counter-evidence either.
- **Directus collection names** (`cms.md:64`: `skill_paths`, `simulations`, `sequences`,
  `library_categories`, `library_macro_categories`, `resource`) — verifiable only against a running Directus or
  a snapshot manifest; the `cms` Go client's collection package names are consistent with them but that is
  weaker than an enumeration of the live schema.
