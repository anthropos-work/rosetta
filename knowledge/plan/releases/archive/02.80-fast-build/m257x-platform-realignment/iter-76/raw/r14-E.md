# Seat E — reading #14

Refs used (stated at the moment of measurement, §5 rule 26):

| repo | path | ref actually measured |
|---|---|---|
| app | `stack-demo/app` | `b948604ff` (v1.366.0) — checkout; also read `9d00a3131` (v1.367.0) and `origin/main` `717737471` where a claim pinned them |
| platform | `stack-demo/platform` | `0dab54d` "chore(compose): run without the standalone storage; rename graphql -> core" |
| next-web-app | `stack-demo/next-web-app` | `bb3313bc0` (v2.133.0) |
| storage | `stack-demo/storage` | `4ce8ece` (v0.15.3); also read `63bffc8` (v0.15.5) where a claim pinned it |
| messenger | `stack-demo/messenger` | `fa47850d` |
| cms / jobsimulation / roadrunner | `stack-demo/{cms,jobsimulation,roadrunner}` | as pinned in the briefing |
| graphql-wundergraph | `stack-demo/graphql-wundergraph` | `60c229f` |
| studio-desk | `stack-demo/studio-desk` | `14a5442` (v0.152.4) |
| ant-academy | `stack-demo/ant-academy` | `9c3843cd` (v2.34.2) |
| rosetta-extensions | `.agentspace/rosetta-extensions` | `1334a4f` (authoring copy, main) |
| corpus | `/Users/marco/workspace/anthropos/rosetta` | branch `m257x/platform-realignment`, HEAD `1937e1f` |

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/architecture/service_taxonomy.md` | 456 | yes |
| `corpus/services/hiring.md` | 416 | yes |
| `corpus/services/chronos.md` | 245 | yes |
| `corpus/services/storage.md` | 201 | yes |
| `corpus/services/askengine.md` | 121 | yes |
| `corpus/architecture/dependency_map.md` | 103 | yes |

Protocol file `corpus/ops/platform-alignment.md` (1680 lines) — §5 read in full (`:240-1147`) before starting.

Search positive controls run in-pass: `grep -c 'SET "'` over the migration set → 8 (against the
0-hit target `SET "score"`); `grep -c STORAGE_S3` in compose → 3 (against the 0-hit target
`STORAGE_RPC_ADDR`); `find apps -type d -name '@tabs'` → 3 (against the 0-hit target `(.)`/`(..)`
intercepting-route dirs); `grep -rn IsHiring` app → 144 (against per-file absence claims). All
stderr read; one zsh glob rejection (`--include=*.go` unquoted) was caught by reading stderr and
re-run quoted — an unread stderr there would have produced a false absence for
`"organization is not hiring"`, which is exactly the finding in B8.

---

## Blockers

### B1 — `dependency_map.md:13` — Backend's `depends_on` claimed to include **cms** and **Storage**; both wrong, and the anchor points at `environment:`

- **Anchor:** `corpus/architecture/dependency_map.md:13` —
  > "| **Backend** (`app`) — the monolith | Sentinel, **cms**, Storage (compose `depends_on`, `docker-compose.yml:70-80` — yes, the monolith still has a startup edge onto the cms **husk**); …"
- **Why it is false:** At platform `0dab54d`, `backend`'s `depends_on` is `docker-compose.yml:92-99` and
  contains exactly **redis, postgresql, sentinel**. There is no cms edge and no storage edge — the block
  carries an in-file comment at `:93` reading *"# storage removed at v9.0: served in-process by this
  container now."* There is no `cms` compose service at all: `d11a403` ("chore(compose): drop roadrunner,
  prune dead env, repoint messenger", merged `ef32d4c` 2026-08-03) deleted it, confirmed by
  `git log -S'  cms:' -- docker-compose.yml`. The cited range `:70-80` is a run of `environment:` entries
  (`SERVICE_NAME=backend`, `SKILLER_STREAM`, `SKILLPATH_STREAM`, the storage-in-app comment block,
  `AWS_REGION`), not `depends_on`. **Self-contradicts `dependency_map.md:15`, two lines below**, which
  says of CMS: *"(**no container** at platform `0dab54d` — the husk is gone from compose and from
  `repos.yml`…)"*.
- **Confidence:** high

### B2 — `dependency_map.md:24` — Studio-Desk `depends_on` cited at a line range past the end of the file, and names a service that does not exist

- **Anchor:** `corpus/architecture/dependency_map.md:24` —
  > "Compose `depends_on` is **`backend` + `cms`** (`docker-compose.yml:337-341`), not `graphql`"
- **Why it is false:** `stack-demo/platform/docker-compose.yml` is **271 lines** at `0dab54d`; `:337-341`
  does not exist. `studio-desk`'s actual `depends_on` is `:223-225` and lists **`backend` only**
  (`condition: service_started`). `cms` has no compose service (B1). Self-contradicts `:15` of the same
  file.
- **Confidence:** high

### B3 — `dependency_map.md:19` — the Storage row's two supporting anchors both land on unrelated text

- **Anchor:** `corpus/architecture/dependency_map.md:19` —
  > "| **Storage** | Postgres, Redis — **`depends_on` only** (`docker-compose.yml:213-217`, both `service_healthy`) | … which is what its own doc says at [`storage.md:14,21`](../services/storage.md)."
- **Why it is false:** Storage's `depends_on` is `docker-compose.yml:126-130` at `0dab54d`. `:213-217` is
  inside the **studio-desk** `environment:` block (`CLERK_SIGN_IN_URL`, `DEFAULT_MODEL`, `ENVIRONMENT`,
  `FRONTEND_PORT`, `NODE_ENV`). The corpus-internal anchor is worse: `storage.md:14` is a bare `>` (an
  empty blockquote line) and `storage.md:21` is `> |---|---|` (a markdown table separator) — neither says
  anything about `DB_CONNECTION`/`REDIS_ADDR`/`go.mod`. The claims themselves are TRUE (I verified:
  storage's compose env has no `DB_CONNECTION`/`REDIS_ADDR`, and `grep -c redis storage/go.mod` = 0) —
  only the evidence pointers are wrong, in both directions, in one cell.
- **Confidence:** high (I hesitated because the underlying facts hold; booked as a blocker because a
  reader following either pointer lands on text that cannot support the claim, and the compose anchor is
  87 lines off into a different service)

### B4 — `dependency_map.md:58` — the `backend` stream row names a subscriber that cannot exist and omits the one that does

- **Anchor:** `corpus/architecture/dependency_map.md:58` —
  > "| `backend` | App | App (cms **domain** in `app`; the `cms` husk also still subscribes until platform M810) | User/org updates |"
- **Why it is false:** (a) *"the `cms` husk also still subscribes"* is untrue on both sides. Locally there
  is no cms container at `0dab54d` (B1). In production `stack-demo/cms/terraform/main.tf:39` sets
  `service_desired_count = 0`, so no task runs and nothing subscribes. This contradicts `:15` of the same
  file. (b) The real consumer of the `backend` stream is **Messenger**, which the row does not list:
  `stack-demo/messenger/internal/flow/flow.go:72` — `h.subServer.AddSubscriber("backend", sub.AddHandler(…))`
  with 20 handlers (skill-path assignment, jobsim assignment, org member invited, AI-readiness, course
  builder). App itself self-subscribes at `app/main.go:1320` (`subServer.AddSubscriber(serviceName, backendSelfSub)`),
  publishing at `main.go:287` (`pubsub.NewPublisher(serviceName, …)` where `serviceName` comes from
  `SERVICE_NAME=backend`).
- **Confidence:** high

### B5 — `service_taxonomy.md:127` (twin `:391`) — the documented `make up` is annotated "(graphql profile)", a token that at `0dab54d` selects no application container

- **Anchor:** `corpus/architecture/service_taxonomy.md:127` —
  > "`make up                # Build from local code and start (graphql profile)`"
  and `:391` — "`make up                # Start all backend services (graphql profile)`"
- **Why it is false:** `platform/Makefile` line `PROFILE ?= core` at `0dab54d`; the commit subject is
  literally *"rename graphql -> core"*. Measured live (read-only) on this tree:
  `docker compose --profile graphql config --services` → `postgresql redis sentinel` — the 3-service floor,
  **no `backend`, no `gotenberg`**. `--profile core` → `postgresql redis sentinel backend gotenberg`.
  **This self-contradicts the same file twice**: `:428` states *"`core` (the Makefile default — `PROFILE ?= core`)"*
  and `:435-439` states *"`graphql` (renamed to `core` at platform `0dab54d`) … Selecting any of them exits 0
  and starts the 3-service floor and nothing else."* `:391`'s "Start all backend services" is separately
  wrong (`core` does not start storage or messenger). This is the §5 rule-30 failure mode the same file
  warns about, committed inside the file that warns about it.
- **Confidence:** high

### B6 — `storage.md:26` — "`STORAGE_RPC_ADDR` is read by `main.go`" is false at the ref the row pins, and inverted at the checkout

- **Anchor:** `corpus/services/storage.md:26` (the **consumer** row of a table whose header at `:20` reads
  *"measured at platform `0dab54d` / app `9d00a313` v1.367.0"*) —
  > "`STORAGE_RPC_ADDR` is read by `main.go` and by **none** of the three `cmd/` tools"
- **Why it is false:** At `app` `9d00a313` the literal `STORAGE_RPC_ADDR` occurs **three** times in the whole
  Go tree and **all three are comments**: `main.go:451` (*"the standalone service takes no traffic and
  STORAGE_RPC_ADDR is gone"*), `internal/jobsimwiring/wiring.go:101`, `internal/storagens/callsites_test.go:189`.
  There is no `os.Getenv("STORAGE_RPC_ADDR")` anywhere. `main.go` at that ref reads
  `os.Getenv(internalstorage.EnvBucket)` (`:463`) instead. At the checkout `b948604f` the claim is inverted
  in the other half: **all three `cmd/` tools DO read it** —
  `cmd/academy-asset-upload/main.go:129`, `cmd/academyImport/main.go:231`, `cmd/import/main.go:50` — alongside
  `main.go:446`, `:524`, `:992`. The stated combination (main.go yes / cmd tools no) holds at **neither**
  ref, and it contradicts `storage.md:28-30` nine lines below (*"there is no longer a client to build"*) and
  `storage.md:23` three lines above (the variable is set by no compose file).
- **Confidence:** high (I considered a charitable parse — "the only mention is in main.go" — but the
  parallel construction "read by X and by none of Y" asserts X reads it, and the row is the doc's
  authoritative statement of the completed fold)

### B7 — `storage.md:154` (twins `:55`, `:181`) — "the PRIVATE manager uses local FS / `STORAGE_S3_BUCKET` is unset in compose" is false at `0dab54d`, and the truth is a production-bucket hazard

- **Anchor:** `corpus/services/storage.md:154` —
  > "In local dev the PRIVATE manager falls back to `/tmp/anthropos-storage/` automatically (`STORAGE_S3_BUCKET` is unset in compose) … FOOTGUN: the PUBLIC manager is NOT sandboxed locally"
  and `:55` — "…while the PRIVATE manager uses local FS."
  and `:181` — "| `STORAGE_S3_BUCKET` | (empty) | … Absent from compose env and `.env` → local FS fallback at `/tmp/anthropos-storage/`. |"
- **Why it is false:** `platform/docker-compose.yml:82` at `0dab54d` sets
  `STORAGE_S3_BUCKET=production-storage20240826131618541000000005` on the **`backend`** service — the
  production private bucket — under an in-file warning at `:77-79`: *"These MUST be set … with empty
  buckets app boots 'fine' and silently writes every upload to the container's ephemeral disk."* Since
  storage is served **in-process by `app`** (this document's own banner, `:12-13`, and the compose comment
  at `:73-75`), the operative private manager on a default `make up` is `app`'s, reading that value —
  so a local stack's private writes go to a **production S3 bucket**, not `/tmp`. The doc inverts the
  sandboxing story exactly: it warns about the public bucket and reassures about the private one. The
  `storage-legacy` container is the only place the old "unset" reading still holds, and it is off by
  default (`profiles: [storage-legacy]`). Supporting (weaker, machine-local): `platform/.env` in this
  tree carries non-empty `AWS_ACCESS_KEY_ID` (len 20) and `AWS_SECRET_ACCESS_KEY` (len 40), contradicting
  `:154`'s *"(none are set in `platform/.env`)"* — checked values-blind, by length only.
- **Confidence:** high

### B8 — `hiring.md:69` — `organization/manager.go:485` cited as a `"organization is not hiring"` hard-error; that line is a closing brace 50 lines away

- **Anchor:** `corpus/services/hiring.md:69` —
  > "…as do `organization/manager.go:448` … and `:485` + `siminvitationlink.go:62` (both **hard-error `\"organization is not hiring\"`**…)"
- **Why it is false:** At the checkout `app` `b948604f`, `internal/organization/manager.go:485` is a bare
  `}` — the close of `forceUserToOrganizationInClerk`. The actual second hard-error site is
  `manager.go:535-537`: `if !org.IsHiring { m.logger.Error("organization is not hiring"); return
  fmt.Errorf("organization is not hiring") }` (enumerated with
  `grep -rn "organization is not hiring" --include='*.go'`, 6 hits, 2 of them in `manager.go`). The anchor
  **was** correct at `app` `5ba17044` (where `:485` is `if !org.IsHiring {`), but this passage sits in
  `## The org-type gate`, carries no ref of its own, and the doc's only pin (`:17`, *"against platform
  origin `2adcf71` / `app` @ `5ba17044`"*) scopes to the `:17-38` blockquote per §5 rule 33. `siminvitationlink.go:62`
  is correct (`if !org.IsHiring {`, error at `:63`).
- **Confidence:** high (hesitated only because the *fact* — two hard-error sites exist — is true; the
  pointer is 50 lines off into a different function, which is a wrong-construct anchor, not line drift)

### B9 — `hiring.md:166` and `hiring.md:311` — "`repos.yml:17-19` `migrations: false`" asserts a `jobsimulation` entry that was deleted from `repos.yml`

- **Anchor:** `corpus/services/hiring.md:166` —
  > "On a **local dev/demo stack it is not**: `repos.yml:17-19` `migrations: false`. Qualified M257x iter-52."
  and `:311` — "`platform/repos.yml:17-19` sets `migrations: false`, and app's only `CREATE SCHEMA` is `auth`"
- **Why it is false:** At platform `0dab54d`, `repos.yml` holds **six** entries — app, sentinel, storage,
  messenger, next-web-app, studio-desk — and **no `jobsimulation` entry at all** (`d11a403` removed it;
  the file's own header comment now says those five legacy repos *"own no local schema, no compose service
  and no clone entry here"*). `repos.yml:17-19` at `0dab54d` is `    migrations: false` (sentinel's) /
  `  - name: storage` / `    type: go` — three lines about two *other* services. The citation was correct
  at `2adcf71`, where `:17-19` **is** the jobsimulation entry (verified with `git show 2adcf71:repos.yml`),
  which is why the third instance at `hiring.md:32` — inside the blockquote explicitly pinned to
  `2adcf71` — is **not** booked. These two are unpinned, and they contradict
  `service_taxonomy.md:105` and `dependency_map.md:16`/`:31`, which state jobsimulation is gone *from
  `repos.yml`*. The conclusion the citation supports (no `jobsimulation` schema on a local stack) is still
  true — and in fact now true for a stronger reason.
- **Confidence:** high

---

## Minors

- `service_taxonomy.md:148` — "contradicted `:75` of this same file": `:75` is the **Messenger** table row;
  the Studio-Desk row the sentence means is `:77`.
- `service_taxonomy.md:342` — "Consistent with :61 above": `:61` is the paragraph tail `M257x iter-63.)`,
  not a claim. The supporting statement is at `:57-58`.
- `service_taxonomy.md:52` — `repos.yml:10-13` starts on a comment line; the `app` entry is `:11-14`
  (`migrations: true` is at `:13`, so the range does cover the load-bearing line).
- `storage.md:8` — `anticheat.go:34` cited beside `app/internal/jobsimulation/recording/recording.go:12`,
  implying a sibling. No `internal/jobsimulation/recording/anticheat.go` exists at `9d00a313` or
  `b948604f` (that dir holds only `chime.go` and `recording.go`); the file is
  `internal/jobsimulation/anticheat/anticheat.go`, where `:34` is the `conc/pool` import — the storage-SDK
  import is `:30`.
- `storage.md:26` — "bucket names are constants in `app/internal/storage/service.go:22`, `:24`". Those
  constants are **env-var names** (`EnvBucket = "STORAGE_S3_BUCKET"`, `EnvPublicBucket = "STORAGE_S3_PUBLIC_BUCKET"`);
  the bucket *names* come from compose (`docker-compose.yml:82`, `:83`). Anchors resolve; the noun is
  wrong. (Related to B7 — a reader chasing "where is the bucket name" is sent to the wrong file.)
- `storage.md:134-136` — "The `jobsimulation`/`cms` husk **containers** call nothing (teardown M810)"
  describes containers this same doc says at `:10-11` no longer exist.
- `hiring.md:64` / `:72` — `organization/manager.go:448` lands on a **blank line** at `b948604f`; the
  `switch org.IsHiring` it means is `:450`. (Correct at `5ba17044`.)
- `dependency_map.md:54` — "Stream names come from `*_STREAM` env vars in `platform/docker-compose.yml`" is
  not the source for the table's first row: the `backend` stream name comes from `SERVICE_NAME=backend`
  (`app/main.go:287`, `:1320`) and from hardcoded literals in messenger. There is no `BACKEND_STREAM`
  anywhere (0 hits in compose, 0 in app Go; positive control: 5 `*_STREAM` reads found).
- `dependency_map.md:61` — the `cms` stream row omits **Messenger**, which subscribes to it
  (`messenger/internal/flow/flow.go:109`). The `jobsimulation` row at `:60` does list Messenger, so the
  omission is inconsistent within the table.
- `dependency_map.md:76` ("`Frontend` -> `Backend` / `Jobsimulation`") and `:95-96` ("`Jobsimulation` ->
  `Redis Stream` …"; "**Jobsimulation** publishes an event") still name a merged service as a network peer
  and as a stream producer, contradicting `:9`, `:16` and `:60` ("Producer: App") of the same file. The
  prose immediately beneath each corrects it, which is why this is a minor and not a blocker — but it is
  precisely the "arrow in a diagram" class §5 records as surviving three audits.
- `chronos.md:9` — "have moved to **in-process Asynq** running inside **jobsimulation**". At `0dab54d`
  there is no jobsimulation service, container or `repos.yml` entry; the asynq usage now lives in
  `app/internal/jobsimulation/anticheat/anticheat.go:32`. This sits in the *current-status* banner, not
  the "preserved for historical context" body.
- `chronos.md:22`, `:171`, `:190` — `stack-dev/chronos` does not exist in this tree (`stack-dev/` holds
  only `HANDOFF-ant-mini.md` and `studio-desk`), and `make init` never creates it because chronos is in no
  `repos.yml`. `cd stack-dev/chronos` fails as written.
- Out of scope but worth routing: `rosetta-extensions/stack-core/platform_predicate_guard.py:377` says
  `STORAGE_RPC_ADDR` at `b948604` *"reads as a mid-fold with **three** app read sites"*, while
  `storage.md:31` says **six**. My independent enumeration of `os.Getenv("STORAGE_RPC_ADDR")` at
  `b948604f` returns **6** (`main.go` ×3 + `cmd/` ×3), so the **corpus is right and the tooling docstring
  is the narrower/stale figure** — the reverse of what a pattern-match would assume.

---

## Positively cleared (audited zeros)

**ENUMERATED** unless stated. I did not book any clearance on re-derived arithmetic alone.

1. **`service_taxonomy.md:55-56`** — *"compose declares ten services, the default profile is `core`, and
   `core` starts five"*. ENUMERATED: the service set is 8 in `docker-compose.yml` (sentinel, backend,
   storage, customerio-sync, messenger, studio-desk, next-web-app, gotenberg) + 2 in the `include:`d
   `common.yml` (postgresql, redis) = **10**; `docker compose --profile core config --services` →
   `postgresql redis sentinel backend gotenberg` = **5**.
2. **`service_taxonomy.md:425-439`** — the whole Profiles table, measured token-by-token with
   `docker compose --profile <X> config --services`: `core` → 5; `backend` → same; `all` →
   `postgresql redis sentinel backend next-web-app studio-desk customerio-sync gotenberg` (messenger
   correctly absent); `storage-legacy` → floor + storage; `frontend` / `studio-desk` / `messenger` → each
   errors *"service … depends on undefined service \"backend\": invalid compose project"* (the documented
   exit 1); `graphql` → `postgresql redis sentinel` (the 3-service floor, the rule-30 correction).
   ENUMERATED.
3. **`service_taxonomy.md:351`** — the subgraph ladder. ENUMERATED from
   `supergraph-config-prod.yaml` at each parent: `749dc86^` = 5 and the names match the doc's list exactly
   (backend, skiller, jobsimulation, cms, skillpath); `7c17e63^` = 4; `915da06^` = 3; HEAD = 1. `915da06`'s
   `--stat` shows it deletes **both** `schemas/cms.graphqls` and `schemas/jobsimulation.graphqls` in one
   commit. Dates 2026-06-24 / 2026-07-21 / 2026-07-29 all match.
4. **`service_taxonomy.md:348`** — router ports. ENUMERATED: `terraform/locals.tf:8` `port = 8080`;
   `terraform/main.tf:48-49` `containerPort`/`hostPort` = `${local.port}`; `config.prod.yaml:5`
   `listen_addr: 0.0.0.0:8080`; `terraform/main.tf:20` `service_desired_count = 1`.
5. **`service_taxonomy.md:208-214`** — the `--template` warning. ENUMERATED: `app/studio/gen.py` registers
   exactly **9** `add_argument` calls and they are the nine the doc names, in order; `parse_known_args` at
   `gen.py:19`; no `--template` anywhere.
6. **`service_taxonomy.md:158-161`** — studio-desk has no framework. ENUMERATED: 0 matches for
   `"(react|vue|@angular)` in `package.json`; 0 `.tsx`/`.jsx` files outside `node_modules`.
7. **`service_taxonomy.md:65-78`** — the two service tables' ports and profiles, row by row against
   compose (backend 8081-8083/`core,backend,all`; sentinel 8087/no `profiles:` key; gotenberg
   3200/`core,backend,all`; storage 8300-8301/`storage-legacy`; messenger 8200-8201/`messenger`;
   customerio-sync 8080/`customerio-sync,all`; studio-desk 9000,9100/`studio-desk,all`; next-web-app
   3000/`frontend,all`). ENUMERATED.
8. **`service_taxonomy.md:105-107`, `dependency_map.md:15-18`** — the `d11a403` attribution. ENUMERATED via
   `git log -S` per service: `d11a403` is the commit that removed the `cms:`, `jobsimulation:` **and**
   `roadrunner:` blocks from `docker-compose.yml`; merged as `ef32d4c` on 2026-08-03.
9. **`service_taxonomy.md:146`** — studio-desk is in compose at `docker-compose.yml:197` with
   `profiles: [studio-desk, all]` at `:226`. ENUMERATED (both anchors exact).
10. **`service_taxonomy.md:225-238` (Ant Academy)** — ENUMERATED: `code/src/graphql/server.js:14` is the
    `NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT` read and `:18` the `throw`; `serverTenant.js:115-145` contains the
    quoted *"NO FS-as-published fallback … not reversible-on-error"* and `:145` is
    `(await getBackendCatalogView(eids)) ?? emptyCatalogView()`; `app/api/academy/beacon/route.js:36` is the
    endpoint const and `:41-55` the `OPS` map with `UPSERT_CHAPTER_PROGRESS`/`SET_LAST_ACTIVITY`;
    `backendContent.js:36` and `:102-103` resolve; `tests/unit/next-scaffold.test.js:106,111` and
    `react-compiler-config.test.js:41` are the serwist-removal assertions; `app/layout.jsx:132` is
    `manifest: "/academy-manifest.json"`; `package.json` has `next ^16.2.6` / `react ^19.2.5` and **zero**
    serwist/workbox entries; `"dev": "next dev --port 3077"`; mobile `expo ~54.0.33`;
    `.github/workflows/deploy-academy.yaml` exists; `gpt-5.2` is real
    (`code/ucourses/ucourse-engine/assistant/agent.js:13`).
11. **`hiring.md:218-219`** — *"one of **four** required-and-undefaulted columns"*. ENUMERATED the SET, not
    the sum (§5 rule 24): I read all 24 columns of `20260722104506.sql`'s `CREATE TABLE` and the
    NOT NULL-without-DEFAULT set is exactly `owner_id` (`:6`), `sim_id` (`:7`), `sim_type` (`:10`),
    `token` (`:13`) — every other NOT NULL column carries a DEFAULT. `:29` is the UNIQUE index on `token`.
12. **`hiring.md:23`, `:159`** — *"`SET \"score\"` has 0 hits across the entire migration set"*.
    ENUMERATED: 0, with positive control `SET "` = 8 in the same tree.
13. **`hiring.md:169-179` (the read-path table) plus every app anchor in the doc** — ENUMERATED, each line
    opened with surrounding context: `intelligence.go:1700` = `m.ent.JobSimulationSession.Query()`;
    `:1820` = `score := RoundFloat(float64(ls.Score), 0)`; `:1846` = `Score: &score`; `:1844` = the
    `SessionCompletionStatus(...)` re-cast; `:885-886` = `InsightsSortFieldCompletitionStatus` →
    `FieldCompletionStatus`; `resolver_queries.go:1034` (decl), `:1035` (the `OrgFeatureInsights` gate,
    first statement), `:1053` (`StatusActive, StatusInvited`), `:1080` (the manager call), `:1085`
    (the neighbour's gate), `:1089` (`GetMembership`); `ent/schema/job_simulation_session.go:39`
    (`completion_status`) and `:45` (`field.Float32("score").Default(0).Min(0).Max(100)`);
    `enum/jobsimulation.go:29-35` (the closed 5-member const block) and `:37-43` (`Values()`);
    `ent/jobsimulationsession.go:181-186` (the unconditional `assignValues` cast);
    `graph.go:129546-129554` and `:129392-129400` (both bare `graphql.MarshalString(string(v))` marshalers,
    no membership check); `jobsimulations.graphqls:14` and `:128` (the two enums);
    `ent/schema/anticheat_result.go:24` (`field.Enum("summary")`);
    `20260722104506.sql:2/:12/:53/:79`; `20260729133514.sql:58-62` (*"5. Drop the mirrors."* →
    `DROP TABLE "local_jobsimulation_sessions"`); `20250416091037.sql:5` (`ADD COLUMN "anticheat_summary"`);
    `20260722081626_jobsim_data_model.sql:336/355/376` (the three `validation_*` CREATE TABLEs, and the
    middle one really is `validation_attempt_skill_results`); `atlas.hcl:8` (`search_path=public`);
    `askengine/registry.go:192` (the M710 comment, an LLM-facing alias, exactly as the doc qualifies it).
14. **`hiring.md:394`** — *"zero `(.)`/`(..)`-prefixed dirs exist anywhere in `apps/`"*. ENUMERATED: 0,
    with `@tabs` parallel-route dirs (3) as the positive control proving the search reaches route dirs.
15. **`hiring.md`'s next-web-app anchors** — ENUMERATED: `useGetClerkOrganization.tsx:16-18` (the hiring-org
    filter) and `:20-21` (`isHiringOrg` / `eid`); `useNavbarSections.tsx:459-466` /`:460` (the
    `results`/`activityDashboard` label ternary); `template.tsx:90`
    (`const isEnterprise = Boolean(organization)`); `FreeTrialContainer.tsx:29`
    (`Boolean(!isHiringOrg && organizationId)`); `apps/web/.../UserStatusContext.tsx:144-149`
    (`userHasAllHiringOrgs`) and `:168-172` (`buildSwitchHandoffUrl({targetProduct:'hiring'…})`);
    `apps/hiring/.../UserStatusContext.tsx:125` and `:144-145` (the symmetric `'workforce'` guard);
    `InsightsByMembersContainer.tsx:359` = `<Drawer` in **apps/hiring** (the recruiter drawer the passage
    describes); `simulationScoreColumn.tsx:95-97` (the `score` cell).
16. **`hiring.md:261-263`, `:71`, `:119`** — the rext seeder anchors. ENUMERATED at
    `.agentspace/rosetta-extensions` `1334a4f`: `persona_write.go:91` =
    `{"public", "job_simulation_sessions", sessionCols(), a.sessions}`; `:152` = `func sessionCols()`;
    `hiring_config.go:99` = `CopyRowsIdempotent(ctx, "public", "organization_sim_invitation_links", …)`;
    `localSessionCols` no longer exists anywhere in `stack-seeding/seeders/`.
17. **`askengine.md` — the cleanest file in my set; every scalar ENUMERATED and exact.**
    `maxAgenticIterations = 15` (`internal/web/backend/ask/handler.go:34`); `loopTimeout = 10 * time.Minute`
    (`:41`); `DefaultModelID = "eu.anthropic.claude-sonnet-4-6"` and `DefaultRegion = "eu-west-1"`
    (`bedrock.go:25-26`); `Temperature: param.NewOpt(float64(0))`; `stripAnthropicAuthMiddleware`;
    `askEngineMaxConns = 6` (`main.go:150`, applied at `:336`); `COPILOT_DB_CONN` (`main.go:313`);
    `QueryTimeout = 10 * time.Second` → `SET LOCAL statement_timeout` (`executor.go:24`, `:97`);
    `MaxInlineRows = 200` / `MaxCellLength = 400` (`executor.go:18`, `:21`); `rules.md` = **146,219 bytes**
    (doc says ~146 KB); `TableRegistry` = **exactly 60** `TableDef`s (doc says ~60); the 11 `/ask` routes
    match the doc **verbatim and in order** (`backend.go:171-187`), including the group's CORS+authn
    middleware and the *"skips OpenAPI validation"* comment; the 5 persistence tables have exactly 5 Ent
    schemas (`askautorule`, `askconversation`, `askmessage`, `askqueryexample`, `askquerylesson`);
    registry aliases cover `directus.*`, `jobsimulation.*`, `skiller.*` (`:560`) and
    `skillpath.skill_path_sessions` (`:574`); test counts are **exact**: `sandbox_test.go` 49,
    `executor_test.go` 13, `prompt_test.go` 10, `followups_test.go` 14. The handler-package test files the
    doc lists (sanitize, stream_registry, examples, lessons, distill, feedback, store, embed) all exist.
18. **`dependency_map.md:21`** — the messenger RPC row. ENUMERATED: `docker-compose.yml:173`
    (`BACKEND_USERS_RPC_ADDR`), `:174` (`CMS_RPC_ADDR`), `:176` (`JOBSIMULATION_RPC_ADDR`), `:183`
    (`SKILLER_RPC_ADDR`) — **all four** `= http://backend:8083`, under the comment at `:171-172` quoted
    verbatim. Every anchor exact.
19. **`dependency_map.md:31`** — *"10 compose services, 6 repo entries, neither list containing cms or
    jobsimulation"*. ENUMERATED both lists (see #1 and `repos.yml`: app, sentinel, storage, messenger,
    next-web-app, studio-desk).
20. **`dependency_map.md:50`** — taxonomy *"direct — app, messenger; indirect — storage, sentinel"*.
    ENUMERATED from each `go.mod`: `app:20` and `messenger:9` carry it in the direct require block;
    `storage:25` and `sentinel:21` carry `// indirect`. Exactly right, all four.
21. **`dependency_map.md:18` / `service_taxonomy.md:107`** — roadrunner prod terraform *"still reads `= 1`"*.
    ENUMERATED: `roadrunner/terraform/main.tf:19` `service_desired_count = 1`.
22. **`dependency_map.md:19`** (facts, not anchors) — storage has no `DB_CONNECTION`/`REDIS_ADDR` in its
    compose env (`:116-123` enumerated) and **0** redis entries in `go.mod`. ENUMERATED.
23. **`storage.md:22`** — the prod row, **correct at its own pinned ref** `storage 63bffc8` (v0.15.5):
    `terraform/main.tf:38` = `service_desired_count = 0`, and `:35-37` is the buckets/CloudFront/DNS
    `prevent_destroy` + M903 comment. ENUMERATED. (Note for the record: the ground-truth checkout
    `4ce8ece` v0.15.3 still reads `= 1` at `:19` — the pin is doing real work here, §5 rule 33.)
24. **`storage.md:23`** — `STORAGE_RPC_ADDR` set by no compose file and absent from `.env_example`:
    **0** occurrences across `docker-compose.yml`, `common.yml`, `.env_example`, with `STORAGE_S3` = 3 in
    compose as the positive control. ENUMERATED.
25. **`storage.md:24`, `:25`, `:46`** — `profiles: [storage-legacy]` at `docker-compose.yml:134` with the
    rationale comment at `:131-133`; `repos.yml:18-20` is the storage entry. ENUMERATED, all exact.
26. **`storage.md:154`, `:165`, `:179-180`, `:195`** — the storage repo's own facts. ENUMERATED:
    **0** `*_test.go` files in the repo; `Dockerfile:18` = `RUN go test -v ./...`; `cmd/root.go:45-46`
    gives binary defaults `PORT=8080` / `RPC_PORT=8081`; `internal/storage/storage.go:122` is the
    `if s.S3Bucket == ""` guard whose `return "", nil` (`:123`) is the empty-presigned-URL behaviour;
    Makefile has `gen`/`setup` and the repo has **0** `//go:generate` directives (gqlgen vestigial); Go
    1.25 in `go.mod`.
27. **`service_taxonomy.md:52`** — `sentinel` keeps `search_path=sentinel` at `docker-compose.yml:18`
    despite `migrations: false` (`repos.yml:17`). ENUMERATED, exact.
28. **`service_taxonomy.md:299-300`** — `DIRECTUS_PUBLIC_BASE_ADDR` on `backend` at `docker-compose.yml:53`
    @ `0dab54d`, pointing at `https://content.anthropos.work`; no directus service in the compose file.
    ENUMERATED.
29. **`hiring.md:352-354`** — the demo's GraphQL endpoint is `backend`'s own `:8082/graphql/query` since
    `2adcf71`. ENUMERATED against compose (`studio-desk` `VITE_GRAPHQL_ENDPOINT` `:204`/`:220`,
    `next-web-app` `NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT` `:236`/`:245`).
30. **`hiring.md:33-34`** — the twin citations `service_taxonomy.md:52` and `dependency_map.md:78` both
    resolve to lines that do say what the passage claims (the legacy-husk schemas / the non-authoritative
    `jobsimulation` schema). ENUMERATED — worth stating because most intra-corpus anchors I checked in
    this pass did **not** resolve (B3, B9, and the `:75`/`:61` minors).

---

## What I could not settle

- **`chronos.md` is almost entirely unverifiable from this tree.** The `chronos` repo is cloned nowhere
  (`stack-*/chronos` does not exist), so the ports 8080/8081, the sqlc/Cobra stack, the `timers` DDL and
  its four indexes, the triggerer's `LIMIT 10` + `FOR UPDATE SKIP LOCKED` + `scheduled_at < now() +
  interval '1 second'` window, the 3-second per-run timeout, the Sentry cron heartbeat, the CLI flags and
  the `ChronosService` RPC signatures are all unchecked. Settling them needs a clone at a named ref.
  I confirmed only the negative claims that are checkable here: no `chronos` service in
  `docker-compose.yml`, no `chronos` entry in `repos.yml`, no `chronos` stream env var, and `045857c` is a
  real platform commit.
- **`chronos.md:3` — "the GitHub repo is NOT archived".** Needs a GitHub API call; nothing in this tree can
  settle it. Note that the doc asserts a *current* fact about a third-party surface with no ref and no
  re-check date (§5 rule 27's third row).
- **`service_taxonomy.md:171` — studio-desk "Multi-language support (7 languages)".** I found no locale
  directory or supported-language constant in the `studio-desk` clone; the only `languages` hits are in
  generated GraphQL types. Settling it needs whichever file enumerates the set.
- **`askengine.md:113-114`** — "Shipped v1.267.0 (2026-05-07)" and "Author mode landed v1.340.0
  (2026-07-17)". Settling these means walking `app`'s tag history for the first appearance of
  `internal/askengine/` and of `coursebuilder_tool.go`; I did not spend the budget. Every other scalar in
  that file measured exact, which raises my prior that these are right too — but a prior is not a
  clearance.
- **`hiring.md:141`, `:144`** — "87 real `SIMULATION_TYPE_HIRING` sims" and "0 `job_position` rows" in the
  captured public snapshot. These are properties of the `.agentspace` snapshot cache, not of platform
  source; settling them needs `stacksnap status` against a populated cache.
- **`storage.md:154` — "(none are set in `platform/.env`)".** `platform/.env` is git-ignored and
  machine-local, so no ref settles it in general. In *this* tree both AWS keys are non-empty (checked
  values-blind, by length). The claim is not falsifiable as written; it should name the artifact it means
  (`.env_example`, where AWS creds genuinely are absent).
- **`dependency_map.md:31` — "the federation now composes **one** subgraph"** in *production*. I verified it
  in the archived `graphql-wundergraph` repo's `supergraph-config-prod.yaml`; whether the deployed router
  is running that composition is not observable from this tree.
