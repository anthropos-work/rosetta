# Seat C — reading #14

Ground truth re-verified at open: `stack-demo/app b948604f`, `stack-demo/platform 0dab54df`,
`next-web-app bb3313bc`, `sentinel 88bc5592`, `storage 4ce8ece5`, `messenger fa47850d`,
`cms ca50c817`, `graphql-wundergraph 60c229f3`, `roadrunner 87d8d443`, `jobsimulation 462343b0`,
`studio-desk 14a5442a`, `ant-academy 9c3843cd`, `.agentspace/rosetta-extensions 1334a4f5` (main).

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/architecture/alignment_testing.md` | 521 | yes |
| `corpus/architecture/architecture_overview.md` | 353 | yes |
| `corpus/architecture/platform-migration-status.md` | 259 | yes |
| `corpus/services/sentinel.md` | 166 | yes |
| `corpus/services/clerk-integration.md` | 128 | yes |
| `corpus/services/README.md` | 79 | yes |
| `corpus/services/db-backup.md` | 31 | yes |

Also read (protocol, in scope): `corpus/ops/platform-alignment.md` §5 (lines 240–1146).

---

## Blockers

### B1 — `architecture_overview.md:60` claims three husk containers the default profile "still starts", with three dead anchors

- **Anchor:** `corpus/architecture/architecture_overview.md:60` — *"**plus the three unfederated husks the default profile still starts**: `jobsimulation` (`docker-compose.yml:83`), `cms` (`:144`) and `roadrunner` (`:281`). … So a bare `make up` gives you **six Go services**, not three."*
- **Why it is false:** the sentence names no ref, so it grades at the checkout (`platform 0dab54d`).
  - `stack-demo/platform/docker-compose.yml` at `0dab54d` declares exactly 8 services — enumerated
    `grep -nE "^  [a-z0-9-]+:"` → `sentinel:5 backend:28 storage:102 customerio-sync:136 messenger:156
    studio-desk:197 next-web-app:228 gotenberg:255`. **There is no `jobsimulation`, `cms` or
    `roadrunner` service.** They were deleted by `d11a403` (verified: the same enumeration at `2adcf71`
    returns `jobsimulation:83 cms:144 … roadrunner:281`, and at `d11a403` returns none of the three).
  - The three cited anchors now name other constructs: `:83` = `- STORAGE_S3_PUBLIC_BUCKET=…` inside
    **backend**; `:144` = `- "8080:8080"` inside **customerio-sync**; `:281` is **past EOF** — the file
    is **271** lines (`wc -l`).
  - "six Go services" from a bare `make up`: `Makefile:10` is `PROFILE ?= core`; the always-on floor
    (no `profiles:` key) is `sentinel` + `postgresql`/`redis` (in `common.yml`), and `core` adds
    `backend` and `gotenberg`. That is **two** first-party Go services (sentinel, backend) plus a
    third-party Go image, not six.
- **Confidence:** high

### B2 — `architecture_overview.md:145` says `make up` runs "profile `graphql`"; no such profile exists, and the same file says `core` twice

- **Anchor:** `corpus/architecture/architecture_overview.md:145` — *"Default local development set (started by `make up`, profile `graphql`):"*
- **Why it is false:** `stack-demo/platform/Makefile:10` `PROFILE ?= core`; the token `graphql` appears
  in no `profiles:` list in `docker-compose.yml` at `0dab54d` (`grep -n "profiles:"` yields
  `core, backend, all` ×2, `storage-legacy`, `customerio-sync, all`, `messenger`, `studio-desk, all`,
  `frontend, all`). `git log --oneline -S"PROFILE ?= core" -- Makefile` → the single commit
  `0dab54d chore(compose): run without the standalone storage; **rename graphql -> core**`.
  Self-contradiction: the same file says *"the local `core` profile (renamed from `graphql` at platform
  `0dab54d`)"* at `:12` and *"local development reality, default `core` profile"* at `:59`.
  Per `platform-alignment.md` §5 rule 30 this is the worst shape of the class: `--profile graphql`
  exits 0 and starts the three always-on services, so it reads as a half-working stack.
- **Confidence:** high

### B3 — `architecture_overview.md` lists **Storage** among the services a default `make up` starts

- **Anchor:** `corpus/architecture/architecture_overview.md:17` (*"**Storage**: File/blob storage"*, inside
  the bullet list introduced at `:12` as *"the **local `core` profile** … what runs after a normal `make up`"*),
  repeated at `:60` (*"Backend/App (the monolith), Sentinel, Storage + Gotenberg"*) and `:153`
  (row **Storage** in the table headed *"Default local development set (started by `make up`…)"*).
- **Why it is false:** at `platform 0dab54d`, `docker-compose.yml:134` gives storage
  `profiles: [storage-legacy]`, with the in-file rationale at `:131-133`: *"v9.0: NOT in the default
  profiles any more — app serves storage in-process, and running both means two writers on one bucket."*
  `make up` (`PROFILE ?= core`) therefore never starts it. `STORAGE_RPC_ADDR` occurs **0** times across
  `docker-compose.yml`, `common.yml` and `.env_example` (positive control: `DIRECTUS` matches 3× in
  `.env_example`). The corpus's own fenced map says the same —
  `platform-migration-status.md:76`: storage = `merged-into-app (startable via storage-legacy)`.
- **Confidence:** high

### B4 — `architecture_overview.md:18-19` says the roadrunner container "still starts"; `:191` of the same file says it is gone

- **Anchor:** `corpus/architecture/architecture_overview.md:18` — *"**Roadrunner**: **orphaned husk** — the container still starts, but nothing calls it"*
- **Why it is false:** no `roadrunner` service exists in `docker-compose.yml` at `0dab54d` (enumeration in
  B1). Direct self-contradiction with `:191` in the same document: *"**Gone locally, orphaned in prod:**
  at platform `0dab54d` there is **no `roadrunner` compose service at all** (deleted by `d11a403`;
  8 services remain)"* — which is the correct statement (I counted 8).
- **Confidence:** high

### B5 — `architecture_overview.md:189-190` — "Container still starts locally" for jobsimulation and cms, and cms "still answers messenger's `CMS_RPC_ADDR`"

- **Anchor:** `corpus/architecture/architecture_overview.md:189` (*"**Container still starts locally** (`docker-compose.yml:83`, default profile) as an unfederated husk"*) and `:190` (*"**Container still starts locally** (`docker-compose.yml:144`, default profile) and still answers `messenger`'s `CMS_RPC_ADDR` until **M809**"*).
- **Why it is false:** both services are absent from compose at `0dab54d` (B1), and both anchors name
  unrelated lines. The `CMS_RPC_ADDR` half is separately false: `docker-compose.yml:174` is
  `- CMS_RPC_ADDR=http://backend:8083` (and `:176` `JOBSIMULATION_RPC_ADDR=http://backend:8083`), under
  the in-file comment at `:171-172` *"cms + jobsimulation are folded into app: all four RPC edges are the
  one backend mux."* The M809 re-point has landed; `messenger` addresses the monolith, not a cms husk.
  `platform-alignment.md` §5 rule 31 is the standing warning for exactly this passage class.
- **Confidence:** high

### B6 — `architecture_overview.md:181` attributes the husk removal to `2adcf71`; `:191` attributes it to `d11a403`

- **Anchor:** `corpus/architecture/architecture_overview.md:181` — *"they are merged into `app` … **and**, until platform `2adcf71`, still started by the default profile as unfederated husks"*, under the heading `:177` *"Archived / merged — **but three of these still start locally**"*.
- **Why it is false:** at `2adcf71` all three were **still present** (`git show 2adcf71:docker-compose.yml`
  → `jobsimulation:83 cms:144 roadrunner:281`); they were removed at `d11a403` (2026-08-03, merged
  `ef32d4c`), which is what `:191` of the same file states. So `:181` names the wrong commit, and the
  heading at `:177` asserts the present tense the paragraph beneath it denies.
- **Confidence:** high

### B7 — `sentinel.md:5` — the cms/jobsimulation husks "still start and still receive `AUTHORIZATION_ADDRESS` at `docker-compose.yml:99,160`"

- **Anchor:** `corpus/services/sentinel.md:5` — *"(The `cms` and `jobsimulation` husk containers still start and still receive `AUTHORIZATION_ADDRESS=http://sentinel:8087` at `docker-compose.yml:99,160`, but they sit off every request path.)"*
- **Why it is false:** neither container exists at `0dab54d` (B1). `AUTHORIZATION_ADDRESS` occurs
  **once** in the whole compose tree — `docker-compose.yml:48`, in `backend` (grep over
  `docker-compose.yml` + `common.yml`, rc=0 with the single hit). `:99` = `condition: service_started`
  (backend's `depends_on: sentinel`); `:160` = `dockerfile: Dockerfile.dev` inside messenger's build block.
- **Confidence:** high

### B8 — `sentinel.md:85` — "set in exactly **three** compose blocks — `docker-compose.yml:45`, `:99`, `:160`"

- **Anchor:** `corpus/services/sentinel.md:85` — *"Consumed via `AUTHORIZATION_ADDRESS=http://sentinel:8087`, set in exactly **three** compose blocks — `docker-compose.yml:45` (**backend**), `:99` (jobsimulation husk), `:160` (cms husk)… No other service sets it: `storage`, `messenger`, `roadrunner`, `customerio-sync`, `gotenberg`, `studio-desk` and `next-web-app` all have no such env"*
- **Why it is false:** measured — **one** block, **one** line, `docker-compose.yml:48`. `:45` is
  `- .env` (backend's `env_file` list), not the variable. The count (3), all three line numbers, and the
  two block attributions are wrong, and the "no other service sets it" list names `roadrunner`, which is
  not a compose service at this ref. (The *conclusion* — `app` is sentinel's only live caller — is
  correct and I confirmed it independently: `stack-demo/messenger` imports no authorization client;
  grep for `authorization|AUTHORIZATION_ADDRESS|sentinel` over its non-test Go returns one unrelated
  comment, positive control `connectrpc.com/connect` matches.)
- **Confidence:** high

### B9 — `services/README.md:20-22` — "three of the four … still start CONTAINERS locally", immediately retracted in the next sentence

- **Anchor:** `corpus/services/README.md:20-21` — *"And **three of the four** (cms, jobsimulation, roadrunner) still start CONTAINERS locally in the default then-default profile as unfederated husks. At platform `0dab54d` they are **gone from compose and from `repos.yml`** as well."*
- **Why it is false:** (a) present-tense "still start CONTAINERS locally" is false at `0dab54d` (B1);
  (b) "three of the four" is arithmetically incoherent against the same blockquote — `:11` defines "the
  four" as *skiller, skillpath, jobsimulation and cms*, and `:15` explicitly makes roadrunner **"the
  fifth"**, so roadrunner cannot be one of "the four" and skiller/skillpath never started containers;
  (c) the sentence contradicts the one directly after it. "in the default then-default profile" is also
  garbled. This is a half-applied repair of exactly the shape `platform-alignment.md` §5 rule 20 predicts.
- **Confidence:** high

### B10 — `services/README.md:33` — roadrunner "the container still starts locally"

- **Anchor:** `corpus/services/README.md:33` — *"…but prod terraform still reads `= 1` and the container still starts locally"*
- **Why it is false:** no `roadrunner` compose service at `0dab54d` (B1). Contradicts `:21` of the same
  file and `platform-migration-status.md:74`. (The prod half is correct: I opened
  `stack-demo/roadrunner/terraform/main.tf:19` → `service_desired_count = 1`, and
  `cms/terraform/main.tf:39` / `jobsimulation/terraform/main.tf:40` → `= 0`, so `:15-18` verifies.)
- **Confidence:** high

### B11 — `platform-migration-status.md:96` — `redis` cited at `common.yml:20`, which is a comment inside postgresql's healthcheck

- **Anchor:** `corpus/architecture/platform-migration-status.md:96` — *"| `redis` | external | external | no | `common.yml:20`. Streams transport for the Watermill pub/sub |"*
- **Why it is false:** `stack-demo/platform/common.yml:24` is `  redis:`. Line `:20` is
  `      # which takes well over the 25s (5s x 5) the retries alone allow once the data` — a comment in
  **postgresql**'s healthcheck block. The anchor resolves, is non-blank, and names the wrong service —
  the §5 rule 21 "right line, wrong construct" class, and exactly what the map's own assertion F was
  built for (F covers `docker-compose.yml` only; `:185` records `common.yml` as *resolution + range
  only*, so nothing catches it). The neighbouring `common.yml:2` (postgresql) and `common.yml:22`
  (`start_period: 120s`) at `:95` are both correct.
- **Confidence:** high

### B12 — `platform-migration-status.md:74` concludes roadrunner "had been unbuildable"; the same file's `:48-53` says the opposite, and the diff refutes it

- **Anchor:** `corpus/architecture/platform-migration-status.md:74` — *"**Compose service and `repos.yml` entry both deleted by `d11a403`** — whose message states the clone entry *"was already gone, so the `../roadrunner` build context could no longer resolve"*, i.e. **the service had been unbuildable, not merely legacy**."*
- **Why it is false:** `git show d11a403 -- repos.yml` shows `- name: roadrunner` being **removed by
  `d11a403` itself**; `git show d11a403^:repos.yml` lists it at line 29 (9 entries: app, cms,
  jobsimulation, sentinel, storage, messenger, roadrunner, next-web-app, studio-desk). `make init`
  clones from `repos.yml`, so `../roadrunner` resolved at every ref up to and including `d11a403`'s
  parent. The commit message's premise is wrong, and the map turns it into a factual conclusion
  ("i.e. …") without cross-checking. It also contradicts the map's own `:48-53`: *"Until `d11a403` the
  declared topology and the actual one disagreed — `repos.yml` called cms / jobsimulation / roadrunner
  'legacy' while compose still started all three. `d11a403` closed that gap by deleting **both sides**
  at once."*
- **Confidence:** high

### B13 — `platform-migration-status.md:111` — "**9** are in `repos.yml`"; there are 6, and §2's own fenced table says 6

- **Anchor:** `corpus/architecture/platform-migration-status.md:111` — *"of which **9** are in `repos.yml` and **46** are named by no corpus document at all"*
- **Why it is false:** `stack-demo/platform/repos.yml` at `0dab54d` declares **6** repos — app,
  sentinel, storage, messenger, next-web-app, studio-desk. §2's fenced table carries `in repos.yml =
  yes` on exactly those 6 rows, and §4's assertion B is defined to go RED on precisely this mismatch.
  9 was the count at `2adcf71` (verified: `git show 2adcf71:repos.yml | grep -c "^  - name:"` → 9).
  **Why I hesitated:** the sentence carries a parenthetical date, *"(GitHub org API, 2026-08-01…)"*, and
  on 2026-08-01 the platform was at `2adcf71`, so the figure was true then. But the parenthetical names
  its source as *the GitHub org API*, which is the source for the **93**, not for a `repos.yml` count;
  the document header re-dates the whole map to 2026-08-04 at `0dab54d`; and §2 was updated while §3 was
  not. Booked as a blocker per the standing instruction, at reduced confidence.
- **Confidence:** medium

### B14 — `alignment_testing.md:460` — "**five** two-sided operators"; six exist and the sixth is in use

- **Anchor:** `corpus/architecture/alignment_testing.md:460` — *"a **snapshot-fidelity gene class** (`dna/snapshot.go`) — **five** two-sided operators over a `FidelityProbe`"*, followed by the five-item enumeration.
- **Why it is false:** `.agentspace/rosetta-extensions/stack-seeding/dna/snapshot.go:33-57` declares
  **six** operator constants — the five named plus
  `OpSnapshotCrossSurfaceClosure = "snapshot-cross-surface-closure"` (`:49-56`, "M23") — and
  `validSnapshotOperators` (`:62-69`) admits all six. It is not dead code: `data-dna.json:444` lists it
  on the `content` surface. Related, same sentence-family: `:478` says the taxonomy surface *"carries
  all five fidelity operators"* — it carries 5 of the 6.
- **Confidence:** high

### B15 — `alignment_testing.md:479-480` — the M10 content surface "carrying four operators"; it declares five

- **Anchor:** `corpus/architecture/alignment_testing.md:479-480` — *"**M10** promotes the **content** surface … `waived-m7c → snapshot-seeded-m10`, **carrying four operators** (no `embedding-dim` — content has no vectors)"*
- **Why it is false:** enumerated from `stack-seeding/dna/data-dna.json`, the `content` surface's
  `operators` array is `["snapshot-row-count", "snapshot-structural", "snapshot-referential",
  "snapshot-public-only", "snapshot-cross-surface-closure"]` — **five**. (The parenthetical is right:
  `snapshot-embedding-dim` is indeed absent. The count is what is wrong.) For contrast, the `taxonomy`
  surface declares five and the four `seeded-m7a` surfaces declare 2–4 structural operators each.
- **Confidence:** high

### B16 — `alignment_testing.md:193` — the coverage call is at `gate.sh:69`, not `:61`

- **Anchor:** `corpus/architecture/alignment_testing.md:193` — *"`gate.sh:61` calls `alignctl dna coverage --dna … --if-declared`."*
- **Why it is false:** `.agentspace/rosetta-extensions/clerkenstein/alignment/scripts/gate.sh:69` is
  `"$alignctl" dna coverage --dna "$base/$dna" --if-declared`. `:61` is a comment line
  (*"# releases while it DID NOT EXIST. It now runs on every gate, BEFORE the score — because a score
  computed"*). The claim names no ref, so it grades at the authoring copy on `main` (`1334a4f5`).
  **Why I hesitated:** the surrounding comment block (`:58-67`) does describe the call, and everything
  the row *says about behaviour* is correct — I verified `--if-declared` at `cmd/alignctl/dna.go:44,51`
  and its two-sided semantics pinned by `coverage_test.go:70,81`. But the briefing lists "wrong line
  anchors that point at the wrong construct" as a blocker class, and this is one.
- **Confidence:** medium

---

## Minors

- `corpus/architecture/alignment_testing.md:3` — *"Last updated: 2026-06-06"* while the body documents
  M218/M219 (v2.3) and M23 work; the metadata asserts a currency the content contradicts.
- `corpus/architecture/alignment_testing.md:513` — the Layout block gives
  `cmd/alignctl  run | capture | dna list|diff|validate`, omitting `dna coverage`, which the same
  document's reference block at `:245` documents. (It mirrors `cmd/alignctl/main.go:22-24`, whose usage
  banner has the same hole.)
- `corpus/architecture/alignment_testing.md:514-517` — the `internal/` listing names `dna`, `outcome`,
  `compare`, `report`; `alignment/internal/` also contains `canon`.
- `corpus/architecture/alignment_testing.md:138` (*"Clerkenstein ships its own in its own repo"*) and
  `:316` (*"all live in the **`clerkenstein` repo**, not here"*) — `corpus/services/clerkenstein.md:4-6`
  states it is *"the `clerkenstein/` **section** of the `rosetta-extensions` monorepo … It is **not**
  'its own git'"*, which `alignment_testing.md:499-507` also says.
- `corpus/architecture/alignment_testing.md:325` — link text reads `knowledge/alignment.md` but the href
  is `../services/clerkenstein.md`. (The real file exists at
  `.agentspace/rosetta-extensions/clerkenstein/knowledge/alignment.md`.)
- `corpus/architecture/platform-migration-status.md:71` — `repos.yml:10-13` cited for
  `migrations: true` + `schema: public`; the app entry is `:11-14` (`:10` is a comment closing the
  header block, and `schema: public` is at `:14`, outside the cited range). Every other `repos.yml`
  range in the file (`:15-17`, `:18-20`, `:21-23`, `:26-28`, `:29-31`, `:9-10`) is exact.
- `corpus/architecture/platform-migration-status.md:71` — the `b948604` back-reference gives skillpath at
  `app/main.go:643`; measured, `skillpath.NewSessionManager` is at `:642` and `:643` is blank. The other
  three (`:581`, `:612`, `:1043`) and all six `9d00a313` sites (`:471`, `:472`, `:637`, `:668`, `:698`,
  `:1099`, `:1423`) verify exactly.
- `corpus/services/clerk-integration.md:73` — names `OrgCheckUserPermission` as a Connect-RPC to
  Sentinel. Sentinel's RPC surface has no such method (`internal/rpcsrv/rpc.go` declares
  `OrgCheckPermission`); `OrgCheckUserPermission` is `app`'s manager-side wrapper
  (`app/internal/authorization/sentinel.go:159,165` builds an `OrgCheckPermissionRequest`).
- `corpus/services/README.md:11` — *"**Four** services in this index … are folded into `app`"*, and the
  Messenger row at `:35` and the Storage row at `:32` carry no merge note, while
  `docker-compose.yml:73-75,131-134,193-195` records the v9.0 storage/messenger folds and
  `platform-migration-status.md:76-77` states both as `merged-into-app`.
- `corpus/architecture/architecture_overview.md:68` lists `authn` among libraries "imported by
  services", while `:203` of the same file says it ships inside colony and the standalone repo is legacy.
- `corpus/services/sentinel.md:31` describes matcher `m6` as *"Org-membership check via `g3(org, user)`"*
  while `:42` defines the same grouping as `g3(org, membership)`.

---

## Positively cleared (audited zeros)

**ENUMERATED** (predicate re-derived from source independently, cardinalities compared):

- `architecture_overview.md:299-307` — the multi-tenancy fence, the one §5 rules 17/24 flag as the
  perennial trap. I re-derived the **set**, not the sum:
  - **135 schemas**: `internal/data/ent/schema/` holds 139 `.go` files (no `_test.go` — `validation_test_run.go`
    merely matches the substring); 4 declare no schema (`database_types.go`, `mixin.go`,
    `skiller_mixins.go`, `skillpath_mixins.go`); an awk pass over both declaration forms
    (`type X struct {\n\tent.Schema` and the one-line `struct{ ent.Schema }`) returns **112 + 23 = 135**.
  - **29 live `OrganizationMixin{}`**: `grep -c` returns 30; `user_resource.go:22` is
    `// OrganizationMixin{},  // We need to work on this` — commented out. Live = 29.
  - **+2**: the whole directory declares only **4** `Policy()` methods outside `mixin.go` —
    `org_membership.go:172` (Membership, `rule.AllowCurrentOrgEdgesOrSkipRule`), `organization.go:56`
    (Organization, `rule.FilterSameOrganizations()` at `:96`), and `user.go:116` (User) — and I opened
    all three: **User filters by user** (`DenyIfNoUserInContext` / `FilterSameUserRule`), so its
    exclusion is correct. **31 of 135 is right, and the set behind it is right.**
  - **23 with an `organization_id` and no policy**: `comm` of the `"organization_id"` grep against the
    live-mixin set returns 23 files, but that naive 23 is a *different* 23 — it wrongly includes
    `skiller_mixins.go` (a mixin file, not a schema) and `org_membership.go` (which polices itself), and
    wrongly omits `similarity.go`, `studio_document.go`, `studio_task.go` (which carry the column via
    `OrganizationIDMixin{}`). Correcting both directions lands on **23** again — and on the *same* 23 the
    corpus's derivation in `security_compliance.md:88-104` names (16 neither-mixin + the 7
    `OrganizationIDMixin{}` users, which I listed from `grep -rn "OrganizationIDMixin{}"`). The
    parenthetical *"16 is the neither-mixin subset of those 23, not the total"* is exactly right.
- `alignment_testing.md:254-258` — the five DNA gene/capability counts. Parsed each JSON in
  `clerkenstein/alignment/dna/`: `clerk-2.6.0` 14 caps / **27** genes, `clerk-js-5` 6 / **9**,
  `clerk-multi-1` 5 / **9**, `clerk-deploy-1` 3 / **7**, `clerk-express-1` 5 / **13**. All five match.
- `alignment_testing.md:201` — *"Only `clerk-2.6.0` … declares a `consumed_surface` today."* Correct:
  `consumed_surface` is present in `clerk-2.6.0.json` and absent from the other four.
- `alignment_testing.md:322` — *"the reusable `alignment/` section has no `scripts/` dir at all (it holds
  `cmd/`, `internal/`, `examples/`, `go.mod`, `README.md`)"*. `ls alignment/` returns exactly those five.
- `alignment_testing.md:296-309` — the toy worked example. Re-derived from `examples/toy/dna.json`:
  `Add` critical ×3 variants (w3) + `Greet` standard ×3 (w2) → 15 total weight; one failing standard
  gene → 13/15 = **86.7%**, critical **3/3 = 100%**, `(5/6 genes aligned)`, and the divergence line's
  `(exact, w2)` all reproduce. `//go:build alignment` confirmed at
  `examples/toy/alignment_test.go:1`; the toy gate 80/100 at `:21-22`; `gate.sh:31-32` defaults 95/100.
- `alignment_testing.md:153-167` — the score definition and the zero-critical guard.
  `compare.go:101-103`: `Overall = pct(alignedW, sumW)` (weighted), `Critical = pct(critAligned,
  critTotal)` (unweighted count) — as documented. Both guard halves exist:
  `dna.go:279-281` (Validate rejects a zero-critical DNA) and `compare.go:56-60` (`GateMet` refuses a
  non-zero critical threshold at `CriticalGenes == 0`).
- `alignment_testing.md:238-239` — exit codes. `run.go:133-136` `ExitRegressed = 2` /
  `ExitUnmeasurable = 3`, with the banner at `:139-153`.
- `alignment_testing.md:104,107` — `dna.go:21` `idPattern = ^[A-Za-z0-9][A-Za-z0-9_-]*$`; `dna.go:24`
  `maxWeight = 1_000_000`, enforced at `:269-270`.
- `alignment_testing.md:333-337` — the inert workflow. `clerkenstein/.github/workflows/alignment.yml`
  exists and is git-tracked; `:10-11` reads *"NOTE: as a subdir workflow under clerkenstein/.github (not
  at the monorepo root), this is currently inert in the rosetta-extensions monorepo; it is kept
  illustrative of the per-mirror gate shape."* — quoted accurately.
- `alignment_testing.md:415,484` — the four structural operators (`dna.go:78-81`) and the coverage claim:
  all 10 surfaces in `data-dna.json` carry a `seeded-*` or `snapshot-seeded-*` status; **none** is
  `waived`.
- `platform-migration-status.md:60-62` — *"14 names"*. Ran the document's own command
  (`git log -p --follow -- repos.yml`, `- name:` extraction, `sort -u`) → exactly the 14 named, in the
  same order.
- `platform-migration-status.md:71-102` — every `docker-compose.yml` / `common.yml` / `repos.yml` /
  terraform citation I could resolve, opened individually: `docker-compose.yml` `:5 :18 :28 :59 :82
  :131-133 :134 :136-138 :154 :156 :174 :176 :195 :197 :226 :228 :245 :253 :255-256 :268`; `common.yml`
  `:2 :22`; `repos.yml` `:9-10 :15-17 :18-20 :21-23 :26-28 :29-31`; `app/terraform/main.tf:44` (= 1);
  `sentinel/terraform/main.tf:19` (= 1); `cms/terraform/main.tf:39` (= 0);
  `jobsimulation/terraform/main.tf:40` (= 0); `roadrunner/terraform/main.tf:19` (= 1);
  `graphql-wundergraph/terraform/main.tf:20` (= 1). Only `common.yml:20` (B11) and `repos.yml:10-13`
  (minor) miss.
- `platform-migration-status.md:76-77` — the storage and messenger rows. Both cells **pin their own
  refs** (`storage 63bffc8`, `messenger a0ec933`), which are descendants of the ground-truth checkouts
  and present in the clones. Graded at those refs: `storage/terraform/main.tf:38` `= 0` with the
  ordering comment at `:29-33` and the `prevent_destroy` note at `:35-37`; `messenger/terraform/main.tf:29`
  `= 0` with the rollback comment at `:27-28` — all verbatim. (At the checkout shas both read `:19 = 1`;
  the pin is doing real work here, and correctly.) `messenger/cmd/root.go:120-140` does read all four
  RPC addresses. All seven `app/main.go` sites at `9d00a313` resolve to the named constructs.
- `platform-migration-status.md:80` — the supergraph. `git show --stat 915da06` deletes **both**
  `schemas/cms.graphqls` and `schemas/jobsimulation.graphqls`; `git show 915da06^:supergraph-config-prod.yaml`
  lists **three** subgraphs; the post-commit configs list `backend` alone and `subgraphs.conf` is
  `BACKEND=v1.360.0`. So **3 → 1 at one commit** is right and the commit's own "2→1" subject is the
  misleading one, as the corpus says.
- `platform-migration-status.md` sha/date citations — all 16 I spot-checked resolve with matching dates:
  `a2a3ee6` 2026-02-27, `8770fe6` 2023-05-04, `cb6ebf5` 2023-04-30, `ef4b449` 2024-09-02, `1474b1f`
  2023-12-01, `84862d1` 2024-05-29, `b43b99a` 2026-02-26, `c17cc9a` 2026-04-15, `467965a` 2023-09-11,
  `045857c`/`fdfa189` 2026-04-17, `21429b7` 2026-07-07, `a4db680` 2026-07-21, `236771f` 2026-07-29,
  `6060315`/`ef32d4c` 2026-08-03. `d11a403`'s message is quoted verbatim and correctly at `:246-250`.
- `platform-migration-status.md:161` — assertion C's *"the **eight** in §1"*: the §1 vocabulary table
  lists exactly 8 states.
- `sentinel.md:26` — *"**6** request types, **6** policy types, **3** role groupings, **6** matchers"*.
  Opened `sentinel/internal/authorization/casbin.go:13-45`: `r,r2..r6` = 6; `p,p2..p6` = 6; `g,g2,g3` = 3;
  `m,m2..m6` = 6. Each matcher's description in the `:28-36` table matches its expression.
- `sentinel.md:68-83` — the RPC table. `grep -nE "^func \(.*\) [A-Z]" internal/rpcsrv/rpc.go` returns
  **19** exported handlers, and the table's 19 entries are exactly that set.
- `sentinel.md:43-48` — *"There is no `manager` role."* `grep -n manager init_policy.sql` → rc=1, with a
  positive control (`grep -c admin` → 46) proving the pipeline. `manager` appears only in
  `internal/authorization/casbin_test.go` / `internal/rpcsrv/rpc_test.go`.
- `sentinel.md:40` — `app/internal/data/ent/enum/membership.go:8-15` really does span the four
  `MembershipRole` values (admin `:8`, member `:9`, candidate `:10`, content_creator `:15`);
  `init_policy.sql:88-118` is precisely the `content_creator` block;
  `internal/authorization/casbin_content_creator_test.go` exists.
- `sentinel.md:12,14,16,22,50-64,125,129` — `go.mod:3` `go 1.26.0`; `Dockerfile:2` and
  `Dockerfile.dev:2` `golang:1.26-bookworm`; `casbin/v3 v3.10.0`; no Ent import anywhere;
  no `profiles:` key on the sentinel compose block (`docker-compose.yml:5-26`);
  `terraform/locals.tf:4-5` = 256 CPU / 128 MB (and `:3` port 8080, the "binary default");
  every file in the Key-directories block exists and nothing else does;
  `Makefile:3` `psql "postgresql://postgres@localhost:5432/postgres?sslmode=disable" < init_policy.sql`
  — the hard-coded DSN claim; `init_policy.sql:63-66` is the taxonomy:write NOTE;
  `local_superadmin_grants.sql` present.
- `clerk-integration.md:37` — *"Webhooks (svix, **12** event types)"*. `grep -n 'case "'
  app/internal/clerk/events/events.go` returns exactly 12: `user.{created,updated,deleted}`,
  `organization.{created,deleted,updated}`, `organizationInvitation.{accepted,created,revoked}`,
  `organizationMembership.{created,deleted,updated}`.
- `clerk-integration.md:41` — *"**8** locales"*: `apps/web/src/providers/Clerk.provider.tsx:10-18`
  imports `deDE, enUS, esES, frFR, itIT, jaJP, nlNL, ptPT`.
- `clerk-integration.md:103` — the SDK-version paragraph, every figure:
  `apps/{web,hiring,integration}/package.json` and `ant-academy/code/package.json:52` all
  `"@clerk/nextjs": "^6.39.2"`; `next-web-app/apps/mobile/package.json:6` `~2.6.18` vs
  `ant-academy/mobile/package.json:18` `~2.19.36` (13 minor versions); `app/go.mod:31`
  `clerk-sdk-go/v2 v2.7.0` — checked at **both** the named ref `5ba17044` and the checkout `b948604`,
  identical at each.
- `clerk-integration.md:21` — `apps/web/src/app/api/metabase/route.ts:35` gates on
  `authData.orgRole !== 'admin'` (the bare form).
- `clerk-integration.md:96-101` — `apps/{web,hiring,integration,mobile}` exist; `e2e/package.json:17-18`
  carries `@clerk/backend` + `@clerk/testing`; `studio-desk/package.json:27-28` `@clerk/clerk-js` +
  `@clerk/express` with `STUDIO_ACCESS_ROLES` at `src/index.ts:96` and `app/services/userService.ts:16`;
  ant-academy web uses `clerkMiddleware` in `code/proxy.js:53,264` and gates on
  `REQUIRE_ORGANIZATION_MEMBERSHIP` → `/no-organization`.
- `architecture_overview.md:19` — `app/internal/jobsimwiring/wiring.go:118` is the sole
  `JUDGE0_BASE_URL` read in the whole repo at the checkout, and `app/internal/roadrunner/` does not exist
  while `app/internal/jobsimulation/runner/` does.
- `architecture_overview.md:48` — `next: ^16.2.7` in all four Next apps (web, hiring, integration, maintenance).
- `architecture_overview.md:255-263` — every AI-routing anchor, opened with 15 lines of context each:
  `internal/jobsimulation/ai/ai.go:262-276` (`getClient`, `flag_use_azure_us`, `azureClientEu` →
  `azureClientUs`), `:129` (`isThrottlingError`), `:166` and `:325` (its two application sites),
  `:85-88` (`config.WithRegion("eu-west-1")` for the Anthropic Bedrock client);
  `internal/cms/studio/markdownManager.go:11,19` (Mistral + `MISTRAL_API_KEY`) and
  `studioManager.go:583`; and all **three** `app/studio/configs/*.ini` pin `azure` on every
  `*_AI_*_MODEL` line.
- `architecture_overview.md:12` — *"the local `core` profile (renamed from `graphql` at platform
  `0dab54d`)"*: confirmed by `git log --oneline -S"PROFILE ?= core" -- Makefile`.
- `architecture_overview.md:331-337` — the schema map against `repos.yml` (`app` is the only
  `migrations: true`, `schema: public`; `sentinel` is `migrations: false` yet owns its schema via
  `search_path=sentinel` at `docker-compose.yml:18`).
- `services/README.md` — the index is complete: 29 files in `corpus/services/`, minus `README.md` and
  `TEMPLATE.md` = **27** service docs, and the four tables enumerate 10 + 5 + 8 + 4 = 27 with no
  duplicate and no omission.

**RE-DERIVED** (the document's own arithmetic re-computed; the weaker clearance):

- `platform-migration-status.md:62-64` — *"26 names"* from `git log -p --follow -- docker-compose.yml`.
  My run returns 26 entries, but that set includes `app-network` (a network key, not a service) and both
  `graphql` and `wundergraph` for the one router. Every service name it returns has a row in §2 (with
  `backend` covered by the `app` row, as the doc states), so the completeness claim holds even though the
  denominator is soft.
- `alignment_testing.md:420` — *"(4 seeded in M7a, ~6 planned)"* against `data-dna.json`'s 4
  `seeded-m7a` + 4 `seeded-m7c` + 2 `snapshot-seeded-*`.

---

## What I could not settle

- **`corpus/services/db-backup.md` in its entirety.** There is no `db-backup` repo in the ground-truth
  clone set, no compose service, and no `repos.yml` entry, so *"Go"*, *"every 6 hours"*, *"S3, Azure,
  Hetzner"*, *"PostgreSQL RDS (all schemas)"* and *"Docker (scheduled, internal)"* have nothing to grade
  against. The one cross-checkable claim — *production-only, not in local compose* — is consistent with
  `platform-migration-status.md:86` and with compose. **What would settle it:** the `db-backup` repo, or
  the `infrastructure` repo's `services.tf` / its terraform module.
- **Every GitHub *archived* claim.** `architecture_overview.md:3` (router repo archived 2026-07-30),
  `:15` (jobsimulation archived 2026-07-31), `platform-migration-status.md:73,80-84`
  (skiller 2026-07-01, skillpath 2026-07-31, intelligence 2026-04-02, chronos **not** archived), and
  `services/README.md:43,70`. Archival state is not visible in a clone. **What would settle it:**
  `gh api /repos/anthropos-work/<name> --jq .archived`.
- **`platform-migration-status.md` §3 (the census).** *"93 repositories"*, *"46 named by no corpus
  document"*, and every `last push` date need the GitHub org API. (The `9 in repos.yml` sub-claim I did
  settle — B13.)
- **`platform-migration-status.md:74`'s prod contradiction** — the row itself says the authoritative
  rollback declaration lives in **infrastructure's `services.tf`**, "a repo this map has never read", and
  neither have I. The roadrunner-vs-cms/jobsim terraform asymmetry is real at the repo level; whether it
  is real in deployed prod is not decidable from here.
- **The alignment scores themselves** (`alignment_testing.md:254-258`: 100%/100% on four surfaces,
  UNMEASURABLE on `clerk-express-1`). The gene *counts*, gates, exit codes and DNA structure all verify
  statically, but the pass/fail values require running the five runners (and `expressrun` needs
  `@clerk/express` `node_modules`). I did not run them.
- **`alignment_testing.md:432`** — *"against the M7a-seeded `demo-1`, `measure` reports 100% / Critical
  100%"*: needs a live seeded stack.
- **`sentinel.md:45-46`** — *"a live stack's `select distinct v2 from sentinel.casbin_rules where
  p_type='g2'` returns `admin` / `member` / `candidate` only"*: needs a running stack. The static half
  (`manager` absent from `init_policy.sql`) I did verify. Note it is also in tension with the same
  document's `:40`, which says `init_policy.sql` seeds **four** roles including `content_creator` — a
  live query would presumably return four, not three, unless the quoted result predates that block.
- **The storage/messenger fold state, cross-ref.** `platform 0dab54d` assumes storage-in-app
  (`docker-compose.yml:73-79` says *"there is no storage service to address"*), while `app b948604`
  (v1.366.0) still reads `STORAGE_RPC_ADDR` at `internal/jobsimwiring/wiring.go:115`. The two
  ground-truth refs are one release out of step with each other, which is why I scoped B3 to the
  compose/profile claim only and did **not** book `architecture_overview.md:62-64`'s *"Five former
  microservices"* — at the unpinned grading ref (`b948604`) five is correct, and the map's *"six
  domains"* at `:71` is pinned at `9d00a313`, so the two are not actually in conflict. A reader who
  conflates the refs will still get it wrong; **what would settle it:** advancing the `app` clone to
  `9d00a313`/origin main and re-reading both passages.
