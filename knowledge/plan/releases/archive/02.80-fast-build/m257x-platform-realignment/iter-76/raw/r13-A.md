# Seat A — reading #13

Refs at the moment of measurement (§5 rule 26):

| repo | ref read |
|---|---|
| corpus (`rosetta`) | branch `m257x/platform-realignment`, HEAD `1937e1f` |
| `stack-demo/app` | `b948604f` (v1.366.0); `9d00a313` (v1.367.0) and `origin/main` `7177374` also read where a passage named them |
| `stack-demo/platform` | `0dab54df` |
| `stack-demo/messenger` | `fa47850d` (v0.42.0); `a0ec933` (v0.42.1 = origin/main) also read where the banner named it |
| `stack-demo/graphql-wundergraph` | `60c229f3` |
| `stack-demo/next-web-app` | `bb3313bc` |
| `stack-demo/studio-desk` | `14a5442a` |
| `.agentspace/rosetta-extensions` | authoring copy `main` @ `1334a4f` |

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/architecture/external_services.md` | 840 | yes |
| `corpus/architecture/security_compliance.md` | 281 | yes |
| `corpus/services/backend.md` | 271 | yes |
| `corpus/services/messenger.md` | 146 | yes |
| `corpus/services/academy-backend.md` | 141 | yes |
| `corpus/services/skiller.md` | 66 | yes |
| `corpus/services/TEMPLATE.md` | 46 | yes |

(Also read in scope, as the briefing directs: `corpus/ops/platform-alignment.md` §5, lines 240–1146.)

---

## Blockers

### B1 — `external_services.md`: the `core` profile is claimed to start NINE containers including `jobsimulation`, `cms`, `roadrunner` and `storage`; the real number is five and three of those services do not exist

- **Anchor:** `corpus/architecture/external_services.md:168-174`, and the mermaid at `:176-196`

  > "The default `core` profile is **not** just Postgres + `backend`: it starts **nine** containers — `postgresql` + `redis` (from the included `common.yml`, profile-less so they always start) and seven application services, `sentinel` · `backend` · `jobsimulation` · `cms` · `storage` · `roadrunner` · `gotenberg`. The `jobsimulation` and `cms` containers are **merged-into-`app` husks kept live as the rollback path** (teardown is M810) — merged in production is not the same claim as removed from compose"

  and inside the diagram:

  > `CMSHusk[cms :8090-8091 — merged-into-app husk, rollback path]`
  > `CMSHusk -.->|DIRECTUS_BASE_ADDR from compose| Directus`

- **Why it is false:** derived per-service from `platform/docker-compose.yml` @ `0dab54d` (plus its `include: common.yml`):

  | service | `profiles:` key |
  |---|---|
  | `sentinel` | *(none — always on)* |
  | `backend` | `[core, backend, all]` (`docker-compose.yml:100`) |
  | `storage` | `[storage-legacy]` (`:134`) |
  | `customerio-sync` | `[customerio-sync, all]` |
  | `messenger` | `[messenger]` |
  | `studio-desk` | `[studio-desk, all]` |
  | `next-web-app` | `[frontend, all]` |
  | `gotenberg` | `[core, backend, all]` (`:268`) |
  | `postgresql`, `redis` (`common.yml`) | *(none — always on)* |

  There is **no `jobsimulation`, `cms` or `roadrunner` service in the compose at all** — `platform@d11a403` ("chore(compose): drop roadrunner, prune dead env, repoint messenger", 2026-08-03) removed all three. `git show d11a403^:docker-compose.yml` lists eleven services including `jobsimulation:` (`:83`), `cms:` (`:144`) and `roadrunner:` (`:281`); at `0dab54d` none of the three exists. And `storage` is `storage-legacy`-only, so `core` does not start it either.

  **`core` therefore starts five containers**: `postgresql`, `redis`, `sentinel` (profile-less) + `backend`, `gotenberg`. The stale "nine" is exactly the `d11a403^` set — the passage was true one commit ago and was not re-derived.

  This also **contradicts the same file twice**: `:134-136` says the standalone `cms` service "is **gone from compose** (`d11a403`), so `backend` is the only consumer left and per-service re-point tooling has ONE target, not two", and `:291-296` says "There is **no `cms` container left to start**". A reader meets the retraction, then the diagram that asserts the retracted thing.

- **Confidence:** high

### B2 — `external_services.md:295`: "compose declares nine services" — it declares eight (or ten with `common.yml`)

- **Anchor:** `corpus/architecture/external_services.md:294-296`

  > "There is **no `cms` container left to start** — platform `0dab54d`'s compose declares nine services and `cms` is not one of them; every content read is `backend`'s own."

- **Why it is false:** the claim names its ref (`platform 0dab54d`) and is graded there. `docker-compose.yml` declares **8** services: `sentinel`, `backend`, `storage`, `customerio-sync`, `messenger`, `studio-desk`, `next-web-app`, `gotenberg`. Counting the `include: common.yml` pair (`postgresql`, `redis`) gives **10**. Nine is neither reading. (`app-network` is under `networks:`, not `services:`.)

  The *conclusion* — `cms` is not among them — is correct; the count that supports it is not. Booked because it is the same stale nine as B1 and because a reader who counts along will conclude one service is missing from the doc's list.

- **Confidence:** high

### B3 — `external_services.md:739`: two `docker-compose.yml` line anchors are past the end of the file

- **Anchor:** `corpus/architecture/external_services.md:737-739`

  > "`NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT=http://localhost:8082/graphql/query   # was :5050/graphql`
  > `# NB the var is WUNDERGRAPH, not GRAPHQL — `NEXT_PUBLIC_GRAPHQL_ENDPOINT` does not exist in`
  > `# next-web-app. Set on the image at docker-compose.yml:352 (build arg) and :361 (runtime env).`"

- **Why it is false:** `platform/docker-compose.yml` @ `0dab54d` is **271 lines**; `:352` and `:361` do not exist. The real sites are **`:236`** (build arg) and **`:245`** (runtime env) — verified by `grep -n "graphql/query" docker-compose.yml`. The passage names no ref, so it is graded at the checkout.

  `:352`/`:361` were correct at `d11a403^` (`git show d11a403^:docker-compose.yml | sed -n '352p;361p'` returns exactly those two lines), so this is `d11a403` drift.

  Same-file self-contradiction: `external_services.md:198` already gives the *correct* pair — "`docker-compose.yml:236`/`:245` for next-web-app, `:204`/`:220` for studio-desk" — all four of which I verified exact.

  (The surrounding claim is fine: `grep -rn NEXT_PUBLIC_GRAPHQL_ENDPOINT next-web-app/` returns nothing, positive control `NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT` matches 5+ files.)

- **Confidence:** high

### B4 — `external_services.md`: three later sites still instruct the reader to inspect a `cms` (and `jobsimulation`) container that does not exist

- **Anchors:** `corpus/architecture/external_services.md:722-728`, `:798-799`, `:821`

  > `:727-728` — "The platform compose has no `directus` service to start; **`cms` points `DIRECTUS_BASE_ADDR` at `content.anthropos.work`.**"
  > `:798-799` — "`docker compose logs backend` (not `directus`, and **not `cms` — that container is a merged husk** that no longer serves `backend`'s content reads)"
  > `:821` — "`docker compose ps backend cms jobsimulation storage`"

  (and `:722-724`'s comment, "NOT `cms` (**that container** is an unfederated HUSK that serves no subgraph)")

- **Why it is false:** there is no `cms` and no `jobsimulation` service at `platform 0dab54d` (B1's derivation; deleted by `d11a403`). `:727`'s present-tense "`cms` points `DIRECTUS_BASE_ADDR` at …" is refuted by the file's own `:132-136`, which states that `backend` is now the **only** `DIRECTUS_*` consumer and that the standalone `cms` service that carried both variables explicitly is gone. `:821`'s copy-pasteable `docker compose ps backend cms jobsimulation storage` errors with *no such service* — and `storage` is not running under `core` either.

  Grouped separately from B1 because these are the *instructional* sites: B1 is a description a reader may skim, these are commands a reader will run.

- **Confidence:** high

### B5 — `external_services.md:208-219`: all three `rosetta-extensions` anchors in the `--local-content` blockquotes name the wrong constructs

- **Anchor:** `corpus/architecture/external_services.md:208-211` and `:219`

  > "`rosetta-extensions/stack-injection/gen_injected_override.py:636-637` re-points every service in `DIRECTUS_DATA_CONSUMERS`, which is **`("cms", "backend")`** (`:53`)."
  > "…replaced by `test_backend_the_actual_reader_is_repointed` (`stack-injection/tests/test_injection.py:1051`)"

- **Why it is false:** measured in the authoring copy `.agentspace/rosetta-extensions` @ `1334a4f` (the ground-truth ref for this repo; the passage names no tooling ref):

  | corpus anchor | what is actually there | where the cited thing lives |
  |---|---|---|
  | `gen_injected_override.py:53` | `INJECTED = {"backend": "app", "cms": "cms", "jobsimulation": "jobsimulation"}` — a **different constant** | `DIRECTUS_DATA_CONSUMERS = ("cms", "backend")` is at **`:84`** |
  | `gen_injected_override.py:636-637` | the per-service CORS comment block (`# backend (app) CORS: …`) | the re-point is `if with_directus and name in DIRECTUS_DATA_CONSUMERS:` at **`:669`** |
  | `test_injection.py:1051` | an `assertLess(block.index(...))` inside an env_file-ordering test | `def test_backend_the_actual_reader_is_repointed` is at **`:1108`** |

  The *values* are all right — the tuple really is `("cms", "backend")`, the test really exists and really replaces `test_only_cms_is_repointed_not_other_services` (whose name survives only in the replacement's own comment at `:1109`). It is the three anchors that resolve to unrelated code.

  **Why I hesitated:** `rosetta-extensions` moves faster than the platform (its HEAD is this milestone's iter-75), so this is plausibly pure line drift rather than a wrong claim. I booked it BLOCKER because `:53` lands on a *different named constant* in the same file — the §5 rule 21 "resolves but names the wrong construct" class — which is the one an anchor-existence check cannot catch, and because all three drifted at once.

- **Confidence:** medium

### B6 — `security_compliance.md`: the residency claim's two cross-file anchors both point at unrelated paragraphs of `external_services.md`

- **Anchors:** `corpus/architecture/security_compliance.md:7`, `:198`, `:201`

  > `:7` — "there is **no ordered EU-first fallback chain** (`external_services.md:546`)"
  > `:197-198` — "**Not "routed through EU endpoints first"**: there is no ordered EU-first fallback chain (`external_services.md:546`)"
  > `:201` — "[`external_services.md:570`](./external_services.md) enumerates **four live** ways a request leaves the EU"

- **Why it is false:** measured against `corpus/architecture/external_services.md` at corpus HEAD:

  - `:546` is `> `app` image as an `additional_repo` (app v1.360.1) — the source lives in` — the **third line of the `app/studio/**` IN-IMAGE-path blockquote**, about citation resolution, not about routing. The sentence "There is **no ordered EU-first fallback chain.**" is at **`:554`** (section heading `### Routing: what is actually implemented` at `:552`).
  - `:570` is `   attempt (`:150-155`, mirrored at `:296-302`/`:326`), i.e. **direct US OpenAI**. Nothing else —` — the middle of item 4's 429-retry mechanics. The "**Four** things can send a request outside the EU" enumeration is at **`:579-581`**, its numbered list at **`:583-614`**.

  Corroborating drift: root `CLAUDE.md` cites **`corpus/architecture/external_services.md:545`** for the identical claim — a third value for the same target. Three citations, three different numbers, none of them right.

  **Why I hesitated between BLOCKER and MINOR:** the substantive claims are true (I verified the routing section end-to-end — see *Positively cleared*), and only the anchors drifted. I booked BLOCKER because these anchors are cited as the **evidentiary authority for a compliance statement**, twice in the document's own PM-facing summary paragraph, and a reader who follows either one lands on text that says nothing about residency and may reasonably conclude the claim is unsourced.

- **Confidence:** medium (high that the anchors are wrong; medium on the grade)

### B7 — `backend.md:228`: the documented `make up` comment names the retired `graphql` profile as the default

- **Anchor:** `corpus/services/backend.md:226-231`

  > ```bash
  > cd platform
  > make up                # default graphql profile — recommended
  > ```

- **Why it is false:** `platform/Makefile:10` reads **`PROFILE ?= core`**, and `:65-66` is `up: ## Start services (default: core profile)` / `$(COMPOSE) --profile $(PROFILE) up …`. No service in `docker-compose.yml` @ `0dab54d` carries a `graphql` profile — `0dab54d` is titled "chore(compose): run without the standalone storage; **rename graphql -> core**".

  **Direct self-contradiction 112 lines earlier in the same file**, `backend.md:116`:

  > "**Profile**: `core` (the default), `backend`, `all` — `profiles: [core, backend, all]` (`docker-compose.yml:100`…). **The default profile is `core`, not `graphql`: `0dab54d` renamed it. Corrected M257x iter-68**"

  This is also the §5 rule 30 failure mode in its most expensive form: the reader who trusts the comment and passes `PROFILE=graphql` gets **exit 0 and three running containers** (`postgresql`, `redis`, `sentinel` — the profile-less floor), i.e. a Postgres that answers and no application, which presents as a broken app rather than a bad invocation.

- **Confidence:** high

### B8 — `backend.md:79`: "all four occurrences" of `SKILLER_RPC_ADDR` — there is one

- **Anchor:** `corpus/services/backend.md:77-79`

  > "**RPC re-pointed** — the `SkillerService` Connect-RPC surface is served **by app itself** (`internal/rpc/skillerrpc/`). Consumers keep the env var, re-pointed: `SKILLER_RPC_ADDR=http://backend:8083` locally (**all four occurrences** in the merged `docker-compose.yml`), `http://backend:8081` in prod terraform."

- **Why it is false:** `grep -c SKILLER_RPC_ADDR platform/docker-compose.yml` @ `0dab54d` returns **1** (`:183`, in `messenger`'s environment). It returned **4** at `platform@0808b92` (`:89`, `:151`, `:204`, `:334`) — the ref the fact-sheet's opening paragraph names. The other three occurrences went with the `backend`/`cms`/`jobsimulation` services that `d11a403` deleted.

  **Why I hesitated:** `backend.md:58-61` opens the fact-sheet with "Verified 2026-07-08 against the re-synced stack-dev clone (`app@c3c45e01` v1.334.1, **`platform@0808b92`**)", and the claim is true at that ref. I booked it because §5 rule 33 fixes a pin's scope at *the claim's own block* — a ref in a paragraph three bullets up does not date this bullet — and because the bullet is written in the present tense ("Consumers **keep** the env var"), which rule 33's last clause says cannot be pinned into silence. A reader grepping today finds one occurrence and cannot tell whether three are missing or the doc is stale.

- **Confidence:** medium

### B9 — `messenger.md:101`: messenger's compose `depends_on` is claimed to include `cms` and `jobsimulation`

- **Anchor:** `corpus/services/messenger.md:101`

  > "Messenger depends on `backend`, `cms`, `jobsimulation` at startup (compose `depends_on`), so bringing it up implicitly brings the rest of the stack. (skillpath was removed from this `depends_on` list when it merged into `app`.)"

- **Why it is false:** `platform/docker-compose.yml:186-192` @ `0dab54d`:

  ```yaml
      depends_on:
        redis:
          condition: service_healthy
        postgresql:
          condition: service_healthy
        backend:
          condition: service_started
  ```

  Three entries, and neither `cms` nor `jobsimulation` is among them — nor could be, since neither service exists (B1). The parenthetical "so bringing it up implicitly brings the rest of the stack" is wrong for the same reason.

  **Self-contradiction, six lines above, in the same file** (`messenger.md:95-96`):

  > "`make up PROFILE=messenger` alone EXITS 1 — messenger declares **`depends_on: backend`**, which the `messenger` profile does not select"

  and the banner rows at `:125`/`:126` state flatly that "there is no `cms` container left to address" and "the husk container is gone".

- **Confidence:** high

---

## Minors

- `corpus/architecture/external_services.md:830` — "Consistent with :512 above, where the same correction is already recorded." `external_services.md:512` is `- Schema exploration`; the parallel `~~docker compose restart graphql~~` correction is at **`:521`**.
- `corpus/services/backend.md:43` — "`app`'s OWN docs still list it (`app/CLAUDE.md:72`, `app/knowledge/architecture.md:28`)". The `SkillPathSessionService` mention in `app/CLAUDE.md` is at **`:80`**; `:72` is `atlas migrate apply --dir file://terraform/migrations`. The claim itself is true and the sibling anchor `app/knowledge/architecture.md:28` is exact.
- `corpus/services/backend.md:25` vs `:218` — the banner lists app's streams as "`skiller`, `skillpath`, `jobsimulation`, `cms` and `ai_usage`" (omitting `backend`), while `:218` enumerates "`backend`, `skiller`, `skillpath`, `jobsimulation`, `cms`, plus the `AI`/`ai_usage`". `:218` is the correct one — `app/main.go` @ `b948604` makes six `AddSubscriber` calls: `SKILLPATH_STREAM` (`:1274`), `SKILLER_STREAM` (`:1276`), `JOBSIMULATION_STREAM` (`:1285`), `CMS_STREAM` (`:1303`), `AI_USAGE_STREAM` (`:1305`), and `serviceName` = `"backend"` (`:1320`). Omitted list member, so MINOR.
- `corpus/services/messenger.md:77` and `:81` — "Redis Streams events from other **services** (`jobsimulation`, `cms`, `backend`)" and "messenger calls out to `cms` … and `jobsimulation`". Those names are live *stream* names but no longer live *service* names at `0dab54d`; the file's own env table (`:125`, `:126`) already records that all four RPC addresses resolve to `backend:8083`. §5 rule 23 class ("a named-consumer list survives the merge that moved the consumer"), but the streams do exist under those names, so MINOR.
- `corpus/services/academy-backend.md:15` — "(Consistent with :74-76 below.)" `:74-76` is the `## Interface Discovery` heading, a blank line, and the `* **How to find the API**:` bullet. The statement being cross-referenced (the endpoint / "only subgraph left") is at `:77-80`.

---

## Positively cleared (audited zeros)

**ENUMERATED** — predicate re-derived from source independently, cardinality compared:

1. **`security_compliance.md:66-137` — the whole Layer-1 multi-tenancy fence.** This is the passage §5 rules 17/24 record as having failed four consecutive generations, so I enumerated the set rather than re-running the shown arithmetic. In `app/internal/data/ent/schema/` @ `b948604`: **139** `.go` files; **135** declare `ent.Schema` (the four that do not are `database_types.go`, `mixin.go`, `skiller_mixins.go`, `skillpath_mixins.go`); `grep -l 'OrganizationMixin{}'` → **30 files**, of which `user_resource.go:22` is `// OrganizationMixin{},  // We need to work on this` — **commented out**, so the live set is **29**; `OrganizationIDMixin{}` → **7**, and they are exactly the seven named (`category`, `jobrole`, `similarity`, `skill`, `specialization`, `studio_document`, `studio_task`); the document's own `comm`/`xargs` derivation returns **18**, and the 18 are the 16 named in the doc **plus** `org_membership.go` and `academy_feedback.go`, which is precisely the pair the doc subtracts. **Only four files declare any `Policy()`** — `mixin.go`, `org_membership.go`, `organization.go`, `user.go` — which I enumerated independently, not from the doc. So **29 + `Membership` + `Organization` = 31 org-filtering**, and **16 + 7 = 23 with an `organization_id` and no policy**. Every anchor exact: `mixin.go:126` (`func (OrganizationMixin) Policy()`), `mixin.go:98` (UserMixin `Policy` doc-comment, `rule.FilterOwnerRule()` at `:103`/`:113`), `org_membership.go:172-188` (own `Policy()` ending `privacy.AlwaysDenyRule()`), `organization.go:56` (`func (Organization) Policy()`) and `:96` (`rule.FilterSameOrganizations()`), `user.go:116`, `user_resource.go:22`, `job_simulation_session.go:5` ("L2: NO Ent privacy Policy; owner/org/tenant are plain fields"), `jobrole.go:18`, `category.go:15`. **The fence holds, in the set as well as the sum.**

2. **`external_services.md:363-376` — the subgraph-count history.** Re-derived from `supergraph-config-prod.yaml` at each named commit rather than from the table: `749dc86^` → **5** (`backend, skiller, jobsimulation, cms, skillpath`), `749dc86` → **4**, `7c17e63` → **3**, `915da06` → **1**. Commit dates match (2026-06-24 / 2026-07-21 / 2026-07-29). `git show --name-status 915da06` marks **both** `schemas/cms.graphqls` and `schemas/jobsimulation.graphqls` as `D`, so cms-in-app really is the **3 → 1** step and the jobsimulation subgraph really did outlive jobsim-in-app. The caution at `:374` is also right: `915da06`'s own subject line reads "(supergraph 2→1)" against a tree listing three.

3. **`external_services.md:377` — "no subscriptions".** `grep -rn "sse\|subscription" graphql-wundergraph/*.yaml` returns **nothing** (positive control: `grep -rln backend *.yaml` matches all three configs). `git log -S 'protocol: "ws"'` returns **nothing** (positive control: `-S sse_post` returns **5** commits). `915da06^:supergraph-config-prod.yaml:13` still reads `protocol: "sse_post"`. `bba862f` is **not** an ancestor of HEAD (`merge-base --is-ancestor` → rc 1) and `git branch -a --contains` names only `remotes/origin/feat/use-web-socket`. `schemas/backend.graphqls` has `type Mutation` at **`:4053`** and `type Query` at **`:4912`** and **no `type Subscription`**; all **6** `Subscription` occurrences are Stripe/plan field names (`:1528`, `:1555`, `:4385`, `:4389`, `:4391`, `:4418`).

4. **`external_services.md:639-644` — the AI env-var counts.** Re-derived with `command grep -rho '\b<NAME>\b' --include='*.go'` in `app`: `OPENAI_KEY` **5**, `ANTHROPIC_API_KEY` **26**, `AZURE_OPENAI_KEY` **13**, `AZURE_OPENAI_ENDPOINT_URL` **13**; `OPENAI_ORG_ID` / `AZURE_OPENAI_ENDPOINT` / `AZURE_OPENAI_DEPLOYMENT` all **0**; `OPENAI_API_KEY` **2**. Every figure reproduces.

5. **`external_services.md:542`, `:604-614` — the "all 30 `*_AI_*_MODEL` lines pin `azure`" latency for the Studio-Room arms.** `command grep -rh "_AI_.*_MODEL" app/studio/configs/*.ini` returns exactly **30** lines and **every one** has `= azure, …`. `gen.py:41-53` is exactly the `load_services_settings` override block, and its `secrets_keys` list contains **only** `*_API_KEY` / `*_ENDPOINT` — no service selector. `grep -rEc "bedrock|boto3"` under `app/studio/` returns **0 non-zero counts**, confirming "*Studio-Room was never on Bedrock*". So the "four live + one latent" count and the iter-52 correction both stand.

6. **`external_services.md:552-574` — the routing mechanics.** All anchors in `app/internal/jobsimulation/ai/ai.go` exact: vendor consts at `:30-33`; `isThrottlingError` at `:129-141`; the ChatCompletion throttle override at `:150-155`; `getClient` at `:259-289`; the Azure EU→US flag branch at `:264-276` (and the error path **keeps** the EU client); `AnthropicAws` and `Anthropic` both `client = a.anthropicClient` at `:280-283`; the `Response` mirror at `:296-302` with `isThrottlingError` at `:325`; `openai.NewOpenAI(openaiKey)` at `:80`; Bedrock `config.WithRegion("eu-west-1")` at `:85-88`. The caller-default chain is exact too: `collections/jobsimulation.go:905` (`AIVendor *AIVendor`), `:1302` (`aiVendor := simulation.Openai`), `simulator/ai/ai.go:58-59` and its `default:` arm at `:114-115`. Mistral's only two uses in all of `app` are `internal/cms/studio/markdownManager.go:19` and `studioManager.go:583` — enumerated, `grep -rln mistral --include='*.go'` returns exactly those two files.

7. **`external_services.md:694` — the LiveKit agent names.** `calls/livekit.go:110` `agentName := "anthropos-agent"`, `:115` `"anthropos-agent-chain"`, `:120` `"anthropos-agent"` (EU branch), `:126` `fmt.Sprintf("anthropos-agent-%s", *location)`. `grep -rn anthropos-agent-eu` over the whole `app` tree returns **nothing**, and `euAgentEndpoints` at `:101-104` is `{"azure-eu", "azure-eu-fr"}` — the split really is on the endpoint.

8. **`backend.md:39` — the RPC mux.** Enumerated from `main.go:1185-1228` @ `b948604`: `UsersService` `:1187`, `OrganizationsService` `:1188`, `SkillerService` `:1196`, `JobSimulationService` `:1204`, `lab.v1.LabSessionService` `:1228` — **five unconditional** — plus `if cmsRPCServer != nil { … }` at `:1212-1214`. Every one of the six anchors is exact to the line. `SkillPathSessionService` → **0** occurrences in Go source (positive control: `SkillerService` → 7), and `app/knowledge/architecture.md:28` does still list it, exactly as the corpus says.

9. **`backend.md:195` / `messenger.md:125-128` — the M809 re-point.** `docker-compose.yml:173-183` @ `0dab54d` sets `BACKEND_USERS_RPC_ADDR` (`:173`), `CMS_RPC_ADDR` (`:174`), `JOBSIMULATION_RPC_ADDR` (`:176`) and `SKILLER_RPC_ADDR` (`:183`) **all** to `http://backend:8083`, under the comment at `:171-172` quoted verbatim in the corpus. `app/main.go:1205-1211` @ `b948604` is exactly the stale "additive + DORMANT … until the M809 re-point" comment. `SKILLPATH_RPC_ADDR` → **0** occurrences in compose. All correct, including the instruction to grade the address against compose rather than the comment.

10. **`backend.md:185` — 13 `ai_readiness_*` tables.** `ls internal/data/ent/schema/ai_readiness*.go` returns **13**, and the four the doc says a "9" omits (`recommendation`, `email_override`, `notification_log`, `notification_optout`) plus the two it says were *not* omissions (`live_snapshot`, `text_translation`) are all present. `internal/aireadiness/` exists as its own top-level package; `ls internal/workforce/ | grep -i readi` returns **nothing**, exactly as claimed.

11. **`academy-backend.md:56-69` — the Ent label-vs-table split.** Enumerated: 11 `academy_*` schema files, and the generated `Table` consts are **all plural** and **all eleven names** the doc gives are exact — `academy_chapter_progresses`, `academy_last_activities`, `academy_chapter_times`, `academy_certificates`, `academy_bookmarks`, `academy_feedbacks`, `academy_series`, `academy_skill_paths`, `academy_chapters`, `academy_chapter_bodies`, `academy_path_embeddings`. `/content/catalog.json` (`internal/web/backend/content.go:23`), the `/content/admin/*` group + `ACADEMY_CONTENT_API_TOKEN` (`backend.go:286-287`), and `academy_embedding_refresh` @ 03:00 (`internal/worker/tasks/academy_embedding_refresh.go:10`) all verify. `0e37771f` is "Academy backend v1.0 'ground truth' … (#903)", 2026-06-05 — exact.

12. **`skiller.md`** — clean throughout. All six named Ent schema files exist; `internal/rpc/skillerrpc/` exists and implements all five externally-reached methods (`GetSkill:73`, `GetSkills:93`, `SearchSkill:116`, `MatchSkill:147`, `GetJobRole:187`); `categoryTree`/`fullCategoryTree` appear in the backend SDL **only** inside a comment saying they "stay unported — no consumers" (positive control: `mostPopularSkills`/`jobRoleMatch` do resolve, in `queries.graphqls`); `skill_translation.go` + `job_role_translation.go` exist; `enum ContentLanguage` in `assignments.graphqls:44-53` has exactly **8** members (english, italian, spanish, french, german, dutch, japanese, portuguese); `skiller` is absent from `repos.yml` (which lists app, sentinel, storage, messenger, next-web-app, studio-desk).

13. **`messenger.md:9-17` — the v9.0 fold banner, graded at the refs it names.** I nearly booked `terraform/main.tf:29 service_desired_count = 0` as false, because at the ground-truth checkout `fa47850d` that file reads `service_desired_count = 1` at **`:19`** and `:29` is a container-definition line. The banner names `messenger a0ec933`, and at **that** ref `terraform/main.tf:29` is exactly `service_desired_count = 0`, with `:27-28` reading "The image and task definition stay declared: this is the rollback path. Restoring service is a one-line revert plus an apply, not a re-provision." — the corpus's paraphrase, verbatim. Likewise every `app` anchor in that banner is exact **at `9d00a313`**: `main.go:15` (`msgflow` import), `:61`/`:62` (`msgadapters`/`msgsender`), `:1387` (`msgflow.ConsumerGroup`), `:1423` (`msgadapters.Wire`), `:1330-1340` (the "TAKES OVER … NO GAP" comment), `:1362-1365` (the `cmp.Or(os.Getenv("SERVICE_NAME"), "messenger")` literal rationale). `repos.yml:21-23` is the messenger entry; `docker-compose.yml:156` is `messenger:` and `:195` is `profiles: [messenger]`. **All correct.**

14. **`messenger.md` code anchors** — `internal/rpcsrv/rpcsrv.go:25-30` holds *both* Unimplemented stubs; `cmd/root.go:63` `PORT`→`8080`, `:64` `RPC_PORT`→`8081`, `:107` `REDIS_STREAMS_INDEX`→`"2"`, `:147` `READONLY_DB_CONNECTION`; `internal/flow/jobsimulations.go:140-151` is exactly the 2h/12h staleness pair; `internal/flow/flow.go:72-87` is the `AddSubscriber("backend", …)` block carrying the `OrgSkillPath*` handlers; `internal/flow/assignments.go:828` is `h.cms.GetSkillPath(...)` inside `getSkillPath`. `go.mod`: Go **1.25.0**, `brevo-go v1.1.3`, `liquid v1.8.1` — all exact.

15. **`external_services.md:129-140`, `:198-203`, `:241-254` — the Directus posture.** `grep -n DIRECTUS docker-compose.yml` @ `0dab54d` returns **exactly one line**: `:53 - DIRECTUS_PUBLIC_BASE_ADDR=https://content.anthropos.work`. So "`backend`'s compose `environment:` block sets exactly ONE of the pair" is enumerated-true, and there is no `directus` service. `app/main.go:980-982` is exactly the `DIRECTUS_BASE_ADDR` `log.Fatalf` boot guard. `app/cms_reader_switch.go:28-29` carries the quoted "*a DIRECT domain call — no proto round-trip … and no internal traffic to a standalone cms*". The `a2a3ee6^` archaeology is exact to the line: `:384 image: directus/directus:10.10.1`, `:386 - 8055:8055`, `:409 - ADMIN_PASSWORD=password`.

16. **`external_services.md:416-430`, `:434-448` — the three Dockerfile eras.** `2c85211^:docker-compose.yml` has `context: ../graphql-wundergraph` with **no `dockerfile:` key**; `2c85211` adds `dockerfile: Dockerfile.dev`; `1e8e754` lines 6-8 read `context: ..` / `dockerfile: graphql-wundergraph/Dockerfile.dev`. `b56d731:docker-compose.yml` still has `  graphql:` at `:22`; `360efd4` has no such key (positive control: `  backend:` at `:28`). `Dockerfile.dev` has the schema `COPY` at `:18`, the surviving comment at `:19-20`, the `awk` at `:23`, `wgc@0.104.0` at `:7`, router image `0.275.0` at `:29`. `graphql-wundergraph/CLAUDE.md:39` does assert the opposite, as the corpus warns. Terraform: `locals.tf:8 port = 8080`, `main.tf:20 service_desired_count = 1`, `main.tf:48-49` container/host `${local.port}`, `config.prod.yaml:5 listen_addr: 0.0.0.0:8080`. `5050:8080` entered at `ba0dbdb` and left at `360efd4`.

17. **`security_compliance.md:231-256` — the EU AI Act retraction.** `skills.go:53-62` is `calculateSkillScore` counting `check.Success` booleans and `:75` is the division `float32(passed)/float32(total)*100`; `criterion.go:127` is `case check.EngineLlm:` inside the hardcoded `switch critCheck.Engine`; `:428` is `bulkChecker := NewLLMBulkChecker(c.logger)`; `:168` dispatches `c.validateCodeDiff(...)`; `:450-475` is `validateCodeDiff`, whose `success` comes from `if cdiff.Diff == ""` — a pure string comparison, no model. `templates/checkValidationBulk.tmpl:27` really does ask a model to "assess whether the `<asset>` … meets or does not meet" the checks, and emits `check_id`/`success`. `checkerEngines` is declared (`criterion.go:34`), passed (`:72`) and stored (`:90`) but **never read** as `c.checkerEngines` anywhere — so "cite the DISPATCH, not that map" is correct. The "most, not all" hedge is the right one.

18. **`security_compliance.md:147` — org switching.** `next-web-app/apps/web/src/hooks/useOrgSelection.tsx:94` = `await clerk.setActive({ organization: clerkOrgId });`; `useResolveActiveOrg.tsx:107` = `.setActive({ organization: candidate })`; `useActivateMembershipOrg.tsx:81` = `clerk.setActive({ organization: clerkOrgId }).catch(…)`. All three exact.

19. **`security_compliance.md:212-219` — the Course Builder Anthropic-direct path.** `coursebuilder/bedrock.go:100` `return "anthropic-api"`, `:106-113` `newUnderlyingClient` (key set → `NewAnthropicClientWithModel`, unset → Bedrock), `:23` `DefaultAuthorModelID = "eu.anthropic.claude-opus-4-8"`, `:29` `DefaultGraderModelID = "eu.anthropic.claude-sonnet-4-6"`; `main.go:770` `logger.Info("coursebuilder model backend", "backend", coursebuilder.ModelBackendName())`; `askengine/bedrock.go:25` `DefaultModelID`; `jobsimulation/agent/report_agent.go:31` `defaultAgentModel`. `coursebuilder.md:48` does say "the shipped path is the direct API". All exact.

20. **`backend.md:116`, `:53`, `:253`, `:125-129`, key-directory listing.** `docker-compose.yml:100` is `profiles: [core, backend, all]`; `main.go:743-746` is exactly the `LABS_API_URL` conditional; `atlas.hcl:12` `src = "ent://internal/data/ent/schema"` and `:15` `dir = "file://terraform/migrations"`; `internal/academy/academy.go:6-9` states the `aiacademy` removal verbatim. **Every one of the 38 directories in the key-directory block exists.** `make up PROFILE=backend` really does also start `gotenberg` (`profiles: [core, backend, all]`) plus profile-less `sentinel`/`postgresql`/`redis`.

**RE-DERIVED (weaker — recorded as such)**: item 4 above (the env-var counts) is a re-computation of the numbers the document itself displays; I did enumerate the name set independently, but the counting method is the document's own.

`corpus/services/TEMPLATE.md` (46 lines) is a placeholder template with no factual claims. Nothing to grade.

---

## What I could not settle

- **`skiller.md:19` — `skiller_rpc_addr = http://backend:8081` in production terraform**, and the parallel prod-terraform claims in `backend.md:195` ("`http://backend.internal.anthropos:8081`") and `backend.md:79`. The `infrastructure` repo is not among the ground-truth clones, so the prod side is unverifiable from here. *Settled by:* a clone of `anthropos-work/infrastructure` (or the per-service `terraform/` in a service repo that carries the value).
- **`backend.md:70-76` prod row counts** (`public.skills WHERE organization_id IS NULL = 42,790`, `job_roles = 22,490`, `categories = 23`, `specializations = 1,447`, `skill_embeddings = 43,584`) and the identical figures in `skiller.md:40-49` / `external_services.md:658-671`. These are read-only prod measurements; no DB was reachable in this pass and the snapshot manifest they cite (`.agentspace/snapshots/taxonomy/…`) was not present. The *internal* arithmetic is consistent (public ⊆ total, 18,919 is an embedding count not a role count), and `skiller.md` and `external_services.md` agree with each other. *Settled by:* `/db-query` against prod, or the manifest file.
- **`external_services.md:3` — "the repo is ARCHIVED on GitHub (2026-07-30)"** for `graphql-wundergraph`. Not derivable from a local clone. *Settled by:* `gh repo view anthropos-work/graphql-wundergraph --json isArchived`.
- **`security_compliance.md:20-31, 151-173, 258-272`** — VPC CIDR, subnet layout, TLS/KMS specifics, backup cadence and destinations, DPA v1.4, "18 approved sub-processors". No infrastructure repo, no legal artifacts. *Settled by:* the `infrastructure` terraform and the DPA itself.
- **`academy-backend.md:100` / `backend.md:208` — "Downstream: the **storage** service (`STORAGE_RPC_ADDR`…)".** I chased this as a suspected merged-service claim and it is **correct at the checkout**: `app@b948604` still imports `github.com/anthropos-work/storage/sdk/storage` and calls `storage.NewClient(os.Getenv("STORAGE_RPC_ADDR"), …)` at `main.go:446` and `:992`. The fold lands one release later — at `app@9d00a313` (v1.367.0) those become `internalstorage.NewClient(storageManager, …)` (`:494`, `:1048`) under the comment "*STORAGE_RPC_ADDR is gone*". Meanwhile `platform@0dab54d` has **already** moved `storage` to `profiles: [storage-legacy]` and sets no `STORAGE_RPC_ADDR` on `backend`. So the corpus is right at the `app` ref this reading was pinned to and will be wrong at the next one. Not booked — recorded because it is a claim that will flip on the next pin advance, and because the ground-truth pair itself straddles the fold.
