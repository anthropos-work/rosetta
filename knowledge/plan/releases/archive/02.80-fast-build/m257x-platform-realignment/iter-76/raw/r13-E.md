# Seat E — reading #13

**Refs used (stated at the moment of measurement, §5 rule 26):**
`app` = `b948604f` (v1.366.0, the checkout) with `9d00a313` (v1.367.0) reachable and read where a
passage pinned it · `platform` = `0dab54df` (level with `origin/main`) · `next-web-app` = `bb3313bc` ·
`storage` = `4ce8ece5` (with `63bffc8` reachable and read where pinned) · `graphql-wundergraph` =
`60c229f3` · `cms` = `ca50c817` · `jobsimulation` = `462343b0` · `roadrunner` = `87d8d443` ·
`ant-academy` = `9c3843cd` · `rosetta-extensions` authoring copy = `1334a4f5` (main).
Corpus at branch `m257x/platform-realignment`, HEAD `1937e1f`.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/architecture/service_taxonomy.md` | 456 | yes |
| `corpus/services/hiring.md` | 416 | yes |
| `corpus/services/chronos.md` | 245 | yes |
| `corpus/services/storage.md` | 201 | yes |
| `corpus/services/askengine.md` | 121 | yes |
| `corpus/architecture/dependency_map.md` | 103 | yes |

Search-pipeline positive controls run in the same passes as every negative result below:
`grep -rn 'STORAGE_S3' platform/` (3 hits) beside the `STORAGE_RPC_ADDR` zero;
`git grep -c STORAGE_S3_BUCKET 9d00a313` (3 files) beside the `STORAGE_RPC_ADDR`-at-`9d00a313` zero;
`git ls-files | grep -E '(^|/)recording\.go$'` (5 hits) beside the `anticheat.go` path probe;
`grep -rn 'CREATE SCHEMA' terraform/migrations/` (1 hit) beside the `SET "score"` zero.
No stderr was swallowed; every command's exit status was read.

---

## Blockers

### B1 — `dependency_map.md:13` says `backend` still has a compose `depends_on` edge onto the cms husk and storage; it has neither, and the anchor names the environment block

- **Anchor:** `corpus/architecture/dependency_map.md:13` — *"**Backend** (`app`) — the monolith | Sentinel, **cms**, Storage (compose `depends_on`, `docker-compose.yml:70-80` — yes, the monolith still has a startup edge onto the cms **husk**)"*
- **Why it is false:** at `platform` `0dab54d`, `backend`'s `depends_on` is `docker-compose.yml:92-99` and names **`redis`, `postgresql`, `sentinel`** only. `:93` carries the platform's own comment *"# storage removed at v9.0: served in-process by this container now."* There is no `cms` service anywhere in the file (`d11a403` deleted it), so a `depends_on: cms` would make compose refuse the whole project. `docker-compose.yml:70-80` at `0dab54d` is the backend **environment** block (`SERVICE_NAME=backend`, `SKILLER_STREAM`, the storage-in-app comment, `AWS_REGION`) — not a `depends_on` at all.
  The cell is **frozen at `2adcf71`**, where `:70-80` *is* exactly that `depends_on` including `cms:` and `storage:` (`git show 2adcf71:docker-compose.yml | sed -n '70,80p'`). The cell names no ref, so it grades at the checkout.
  It also **self-contradicts the same file** at `:15` (*"**no container** at platform `0dab54d` — the husk is gone from compose and from `repos.yml`"*) and `:31` (*"at platform `0dab54d` they are gone from compose and from `repos.yml` too — 10 compose services, 6 repo entries, neither list containing cms or jobsimulation"*).
- **Confidence:** high

### B2 — `dependency_map.md:24` cites `docker-compose.yml:337-341` for a Studio-Desk `depends_on: backend + cms`; the file is 271 lines and the edge is `backend` alone

- **Anchor:** `corpus/architecture/dependency_map.md:24` — *"Compose `depends_on` is **`backend` + `cms`** (`docker-compose.yml:337-341`), not `graphql`"*
- **Why it is false:** `platform/docker-compose.yml` at `0dab54d` is **271 lines** (`wc -l`), so `:337-341` is **out of range entirely** — the citation cannot resolve. Studio-Desk's actual `depends_on` is `docker-compose.yml:223-225` and names **`backend` only**; there is no `cms` service to depend on. The claim is another frozen `2adcf71` reading — at that ref the file is 387 lines and `:337-341` is precisely `depends_on: backend + cms`. No ref is named in the cell.
  Same self-contradiction with `:15` and `:31` as B1.
- **Confidence:** high

### B3 — `storage.md:46` asserts *"there was never any `storage` profile at all"*; the platform's own history refutes the stronger form, and `service_taxonomy.md:436` says the opposite

- **Anchor:** `corpus/services/storage.md:46` — *"Corrected M257x iter-68: the old line named two profiles the platform does not have — there is no `graphql` profile (`0dab54d` renamed it `core`) and **there was never any `storage` profile at all**"*
- **Why it is false:** the `storage` service declared `profiles: [graphql, storage, all]` in `platform/docker-compose.yml` at **every commit up to and including `d11a403`** — the commit immediately preceding `0dab54d`. Measured by walking the file's history: `d11a403:119`, `360efd4:218`, `b56d731:251`, `a4db680:243`, `21429b7:287`, `781b03c:332`, `045857c:331`, `fdfa189:337`, … all `profiles: [graphql, storage, all]`. `0dab54d` is the commit that replaced it with `[storage-legacy]` (`git log -S'storage-legacy' -- docker-compose.yml` → the single commit *"chore(compose): run without the standalone storage; rename graphql -> core"*).
  This is the rule-20 over-correction class verbatim: *"does not exist now"* rewritten as *"never existed"*, with the platform's own history as the only thing that could refute it — and it does.
  It also **contradicts `service_taxonomy.md:436`** (in this reading's own scope): *"`graphql` (renamed to `core` at platform `0dab54d`), plus `cms`, `jobsimulation`, `roadrunner` and **`storage`**, are no longer profiles"* — which presupposes a `storage` profile existed. `service_taxonomy.md` is the correct side.
- **Confidence:** high

### B4 — `storage.md:26` states `STORAGE_RPC_ADDR` is read by `main.go`; at the row's own pinned ref nothing reads it, and the doc's own banner says there is no caller left

- **Anchor:** `corpus/services/storage.md:26` — *"| **consumer** | … `STORAGE_RPC_ADDR` is read by `main.go` and by **none** of the three `cmd/` tools |"* (the table column header at `:20` dates every cell *"measured at platform `0dab54d` / app `9d00a313` v1.367.0"*)
- **Why it is false:** at `app` `9d00a313` (v1.367.0) the token `STORAGE_RPC_ADDR` survives in **three places, all comments**, and is read by nothing:
  `git grep -n 'STORAGE_RPC_ADDR' 9d00a313 -- '*.go'` →
  `internal/jobsimwiring/wiring.go:101` (comment), `internal/storagens/callsites_test.go:189` (comment), `main.go:451` — which literally reads *"// … the standalone service takes no traffic and STORAGE_RPC_ADDR is gone."* There is **no `os.Getenv("STORAGE_RPC_ADDR")` anywhere at that ref.**
  The sentence is not merely stale — **no ref makes both of its clauses true.** At the checkout `b948604` the first clause is true (`main.go:446`, `:524`, `:992`) but the second is false (all three `cmd/` tools read it: `cmd/academyImport/main.go:231`, `cmd/academy-asset-upload/main.go:129`, `cmd/import/main.go:50`). At `9d00a313` the second clause is true and the first is false.
  It **self-contradicts the same document** at `:7` (*"there is **no live RPC caller at all**"*), `:23` (*"`STORAGE_RPC_ADDR` is set by **no** compose file"*) and `:28-30` (*"is gone, because there is no longer a client to build"*).
  I hesitated only because the sentence reads like a half-applied edit rather than a fresh assertion — but a reader debugging a "storage unreachable" symptom would go looking for a `main.go` read that does not exist. Booked as a BLOCKER.
- **Confidence:** high

### B5 — `hiring.md` cites `platform/repos.yml:17-19 migrations: false` three times; at `0dab54d` those lines are `sentinel`/`storage`, and there is no `jobsimulation` entry at all

- **Anchor:** `corpus/services/hiring.md:32-33`, `:166`, `:311` — e.g. `:311` *"`platform/repos.yml:17-19` sets `migrations: false`, and app's only `CREATE SCHEMA` is `auth`, so `to_regclass('jobsimulation.sessions')` is NULL"*
- **Why it is false:** `platform/repos.yml` at `0dab54d` is 31 lines and holds **six** entries — `app` (11-14), `sentinel` (15-17), `storage` (18-20), `messenger` (21-23), `next-web-app` (26-28), `studio-desk` (29-31). `:17-19` spans `migrations: false` (sentinel's), `- name: storage`, `type: go`. **There is no `jobsimulation` entry in the file**, so the anchor cannot be evidence about the jobsimulation schema.
  It was correct at `d11a403^`, where `git show d11a403^:repos.yml` gives exactly `17  - name: jobsimulation / 18    type: go / 19    migrations: false # legacy — folded into app (jobsim-in-app)`. `d11a403` deleted it.
  The **conclusion** (no local stack creates a `jobsimulation` schema) is still TRUE and independently confirmed — `app/atlas.hcl:8` pins `search_path=public` and the only `CREATE SCHEMA` in the whole migration set is `auth` (`terraform/migrations/20230817154747_supabase_baseline.sql:2`). Only the anchor is wrong, and it is now *understated*: the entry is gone entirely, which is stronger evidence than `migrations: false`. Three sites carry it, and one of them is inside a passage explicitly re-adjudicated twice ("qualified M257x iter-52"), which is what makes it read as freshly checked.
- **Confidence:** high

### B6 — `hiring.md:69` cites `organization/manager.go:485` for a `"organization is not hiring"` hard-error; at the checkout `:485` is a closing brace and the error is at `:536-537`, in a different function

- **Anchor:** `corpus/services/hiring.md:69` — *"as do `organization/manager.go:448` (a forced Clerk membership is created with role `candidate` instead of `member`) and **`:485`** + `siminvitationlink.go:62` (both **hard-error `"organization is not hiring"`** …)"*
- **Why it is false:** at `app` `b948604` (the checkout; this paragraph names no ref of its own — it dates itself *"M257x iter-52"*, a later iter than the doc's `5ba17044` banner at `:17`), `internal/organization/manager.go:485` is `}` — the closing brace of `forceUserToOrganizationInClerk`. The only `"organization is not hiring"` sites in that file are **`:536` / `:537`**, inside a *different* function, `ForceUserToOrganization` (guard at `:535`). Measured: `grep -rn 'organization is not hiring' --include='*.go'` → `siminvitationlink.go:63`, `manager.go:455` (a comment), `manager.go:536`, `:537`, plus two test lines.
  The sentence is **internally inconsistent about its ref**: its sibling anchor `:448` resolves correctly at the *checkout* (`antRole := enum.RoleMember`) and is off at `5ba17044` (where that line is `:447`), while `:485` resolves at *`5ba17044`* (`if !org.IsHiring {`) and is off by 51 at the checkout. No single ref makes both anchors right, and the two functions have near-identical names — a reader following `:448` and `:485` will believe they are in the same function.
- **Confidence:** medium — the anchor was true at the ref the doc's *top banner* names; I book it because §5 rule 33 scopes a pin to the claim's own block, and this block names iter-52 rather than that banner.

### B7 — `service_taxonomy.md:127` and `:391` annotate `make up` as running the "graphql profile"; the Makefile default is `core` and `graphql` is not a profile

- **Anchor:** `corpus/architecture/service_taxonomy.md:127` — ``make up                # Build from local code and start (graphql profile)`` — and `:391` — ``make up                # Start all backend services (graphql profile)``
- **Why it is false:** `platform/Makefile:10` is `PROFILE ?= core` and `:66` is `$(COMPOSE) --profile $(PROFILE) up --build -d --remove-orphans`, so a bare `make up` selects **`core`**. `docker-compose.yml` at `0dab54d` declares no `graphql` profile anywhere — `grep -n 'profiles:'` gives exactly `[core, backend, all]`, `[storage-legacy]`, `[customerio-sync, all]`, `[messenger]`, `[studio-desk, all]`, `[frontend, all]`, `[core, backend, all]`.
  Both lines **self-contradict the same file** at `:55-61` (*"the default profile is `core`, and `core` starts five"*), `:428` (*"`core` (the Makefile default — `PROFILE ?= core`)"*) and `:435-436` (*"`graphql` (renamed to `core` at platform `0dab54d`) … no longer profiles"*). This is §5 rule 29's named residual class — *a sentence in the tree that assumes a `graphql` profile exists* — surviving inside the very file that documents its retirement.
  `:391`'s prose is wrong on a second axis: `core` starts `backend` + `gotenberg` + the floor, **not** "all backend services" (storage and messenger are excluded).
- **Confidence:** high

### B8 — `dependency_map.md:19` cites `docker-compose.yml:213-217` for storage's `depends_on`, and `storage.md:14,21` for a claim those lines do not make

- **Anchor:** `corpus/architecture/dependency_map.md:19` — *"**Storage** | Postgres, Redis — **`depends_on` only** (`docker-compose.yml:213-217`, both `service_healthy`) … which is what its own doc says at [`storage.md:14,21`](../services/storage.md)"*
- **Why it is false:** two anchor defects in one cell, and the cell dates *itself* at `0dab54d` (*"At platform `0dab54d` the service moved to `profiles: [storage-legacy]`"*), so both must resolve there.
  1. `docker-compose.yml:213-217` at `0dab54d` is the **studio-desk environment block** (`CLERK_SIGN_IN_URL`, `DEFAULT_MODEL`, `ENVIRONMENT`, `FRONTEND_PORT`, `NODE_ENV`). Storage's `depends_on` is `:126-130`. The anchor is frozen at `2adcf71`, where `:213-217` *is* storage's `depends_on`.
  2. `storage.md:14` is `>` — an **empty blockquote line** — and `storage.md:21` is `> |---|---|`, a **markdown table separator**. Neither states anything about storage reading no DB/Redis; the supporting statements are at `storage.md:37` and `:44`. This is §5 rule 34's class exactly: a corpus-into-corpus citation invalidated by an edit that changed the target file's line count, landing on content and reading as correct.
  The cell's **substance** (postgres + redis, `depends_on` only, both `service_healthy`) is TRUE at `0dab54d` — which is why I hesitated. Booked because both citations name the wrong construct and one of them is the cell's sole authority for its most surprising claim.
- **Confidence:** medium

### B9 — `dependency_map.md:58` says the `cms` husk "also still subscribes" to the `backend` stream; there is no cms process anywhere

- **Anchor:** `corpus/architecture/dependency_map.md:58` — *"| `backend` | App | App (cms **domain** in `app`; **the `cms` husk also still subscribes until platform M810**) | User/org updates |"*
- **Why it is false:** there is no running `cms` process to subscribe. Locally, `d11a403` deleted the `cms` compose service and its `repos.yml` entry — the same file says so at `:15` and `:31`. In production, `cms/terraform/main.tf:39` is `service_desired_count = 0`, so the ECS module is a declared-but-stopped rollback path (as `:15` itself says: *"ECS module kept as the rollback path"*), not a live subscriber. A stream subscriber requires a process; neither environment has one.
  M810 is the *teardown* of a module that already runs zero tasks — it is not the date the subscription ends.
- **Confidence:** medium — I could not observe a live Redis consumer group, so this rests on "desired_count 0 + no container" rather than a direct measurement of consumer registration. I book it because the same file asserts both halves.

---

## Minors

- `corpus/architecture/service_taxonomy.md:52` — cites `repos.yml:10-13` for `app`'s migrations entry; the entry is `:11-14` (`:10` is a comment, and the cited range stops one short of `schema: public`). Line drift, right construct.
- `corpus/architecture/service_taxonomy.md:112` + `:451` — the shared-libraries table is headed *"imported as private Go modules … pulled at Docker build via `GH_PAT`/`GOPRIVATE`"* and lists `authn` as one of the five. Nothing imports it: `app/go.mod` names `ai`, `analytics-go`, `colony`, `messenger`, `proto`, `storage`, `taxonomy` and no `authn`. The row itself disclaims it (*"standalone repo is legacy"*), and `dependency_map.md:49` states it correctly (*"Imported via `colony/authn`"*), so the heading over-claims rather than misleads outright.
- `corpus/architecture/service_taxonomy.md:114-120` — the shared-library list omits `github.com/anthropos-work/analytics-go v0.3.1`, which `app/go.mod:15` imports under exactly the same "private module, not cloned by `make init`" predicate. Omitted list member.
- `corpus/services/storage.md:9` — cites a bare `anticheat.go:34` next to `app/internal/jobsimulation/recording/recording.go:12`, implying the same directory. That directory holds only `chime.go` and `recording.go`; the name resolves to **two** tracked files (`internal/jobsimulation/anticheat/anticheat.go`, `internal/jobsimulation/repository/anticheat.go`), and in the intended one the storage-SDK import that parallels `recording.go:12` is at `:30`, not `:34` (`:34` is `"github.com/sourcegraph/conc/pool"` at `b948604`, `)` at `9d00a313`).
- `corpus/services/storage.md:26` — *"bucket names are constants in `app/internal/storage/service.go:22`, `:24`"*. Those lines are `EnvBucket = "STORAGE_S3_BUCKET"` and `EnvPublicBucket = "STORAGE_S3_PUBLIC_BUCKET"` — **env-var-name** constants, not bucket names. The bucket names are compose values (`docker-compose.yml:82-83`).
- `corpus/services/chronos.md:9` — *"have moved to in-process Asynq **running inside jobsimulation**"*, present tense, of a service that has no container, repo entry or subgraph; the engine is `app/internal/jobsimulation/`.
- `corpus/services/chronos.md:186` — spells the dead command in runnable form (`docker compose up -d chronos`) inside the warning about it. The statement is factually true (a named non-existent *service* does error, unlike a retired *profile* token), but §5 rule 30's corollary asks that a dead command be named, not written.
- `corpus/services/chronos.md:5` — *"removed … in mid-2026"*; `045857c` is dated **2026-04-17**.
- `corpus/services/hiring.md:196` — cites `persona_write.go:69-71` for the three `validation_*` writes; at rext `1334a4f5` those lines are the tail of a doc comment (the table mapping is `:66-68`) and the writes themselves are the slice entries at `:92-94`.
- `corpus/services/hiring.md:173` — `simulationScoreColumn.tsx:54` is `accessorKey,` inside the column definition, not the score render; the companion `:95-97` (`const { score … } = info.row.original` / `formatNumberToString(score, 0)`) is exact.
- `corpus/services/askengine.md:108-109` — the handler-package test list (*"sanitize, stream_registry, examples, lessons, distill, feedback, store, embed"*) omits `coursebuilder_tool_test.go`, the ninth `_test.go` in `internal/web/backend/ask/`.

---

## Positively cleared (audited zeros)

**ENUMERATED** (predicate re-derived from source independently, cardinality stated before any arithmetic):

- `service_taxonomy.md:55` *"compose declares ten services"* — `docker-compose.yml` declares 8 (`sentinel`, `backend`, `storage`, `customerio-sync`, `messenger`, `studio-desk`, `next-web-app`, `gotenberg`) and the resolved `include: common.yml` adds 2 (`postgresql`, `redis`) = **10**. Resolving the include is load-bearing (§5 rule 30 corollary).
- `service_taxonomy.md:55-56, :427-432, :449` — the **floor** is exactly the three services declaring no `profiles:` key (`postgresql`, `redis`, `sentinel`); `core` = floor + `backend` + `gotenberg` = **5**; `all` = floor + backend, gotenberg, customerio-sync, next-web-app, studio-desk. Every profile membership in the table matches `grep -n 'profiles:' docker-compose.yml` one-for-one. `PROFILE ?= core` at `Makefile:10`.
- `service_taxonomy.md:433` — `frontend` / `studio-desk` / `messenger` each name a service declaring `depends_on: backend` (`:250-252`, `:223-225`, `:191-192`) that its own profile does not select. Predicate confirmed structurally.
- `service_taxonomy.md:351` — the subgraph ladder **5 → 4 (`749dc86`, 2026-06-24) → 3 (`7c17e63`, 2026-07-21) → 1 (`915da06`, 2026-07-29)**. Re-derived by counting `name:` in `supergraph-config-prod.yaml` at each commit's *parent*: 5, 4, 3, and `915da06` deletes both `schemas/cms.graphqls` and `schemas/jobsimulation.graphqls` in one commit (`git show --stat`). All three dates match. The corpus's warning that the commit's own subject says "2→1" is itself confirmed — that subject line is `feat(gql): fold cms subgraph into backend (supergraph 2→1)`.
- `service_taxonomy.md:348` — router port **8080 everywhere**: `terraform/locals.tf:8` `port = 8080`, `terraform/main.tf:48-49` containerPort/hostPort `= ${local.port}`, `config.prod.yaml:5` `listen_addr: 0.0.0.0:8080`. All three exact.
- `service_taxonomy.md:369` — **all four** `*_RPC_ADDR` values compose sets read `http://backend:8083`: `:173`, `:174`, `:176`, `:183`. Enumerated, not sampled.
- `hiring.md:217-219` — the **four required-and-undefaulted** columns on `job_simulation_sessions`. I enumerated every `NOT NULL` column in `20260722104506.sql:2-27` and partitioned by presence of `DEFAULT`: undefaulted = `owner_id` (`:6`), `sim_id` (`:7`), `sim_type` (`:10`), `token` (`:13`); defaulted = `id`, `created_at`, `updated_at`, `status`, `completion_status`, `interactions_progress`, `score`, `validation_version`. Cardinality **4**, and the doc's *"every other `NOT NULL` column carries a `DEFAULT`"* conjunct holds. `token`'s UNIQUE index is `:29`.
- `hiring.md:212-214` — `SessionCompletionStatus` is **exactly** `pending`/`passed`/`failed`/`discarded`/`timedout`, `enum/jobsimulation.go:29-35`, `Values()` `:37-43`. Both SDL enums declare the same five lowercase members (`jobsimulations.graphqls:14`, `:128`). No `SIMULATION…` member anywhere in the set.
- `hiring.md:394` — *"zero `(.)`/`(..)`-prefixed dirs exist anywhere in `apps/`"*. Re-derived: `find apps -type d | grep -E '/\(\.\.?\)'` returns empty against a directory tree that does contain route groups, so the pattern is exercised.
- `storage.md:23` — `STORAGE_RPC_ADDR` has **0** occurrences across the entire `platform` repo (not just the three named files), with `STORAGE_S3` returning 3 as the control in the same pass.
- `storage.md:195` / `:165` — `storage` ships **0** `*_test.go` files and **0** `//go:generate` directives, while `Dockerfile:18` is `RUN go test -v ./...`. Both zeros run with the file-enumeration control.
- `askengine.md:18` — the `TableRegistry` allowlist is **60** `TableDef` literals (`grep -c '{Name: "'`), split `Schema: "public"` ×57 / `Schema: "directus"` ×3, with 16 `jobsimulation.*` logical names among the public-schema rows plus the programmatic `skiller.*` / `skillpath.*` aliases built at `registry.go:560`, `:574`. *"~60"* holds.
- `askengine.md:101-106` — test counts re-derived by `grep -c '^func Test'`: `sandbox_test.go` **49**, `executor_test.go` **13**, `prompt_test.go` **10**, `followups_test.go` **14**. All four exact.
- `askengine.md:61-64` — all **11** `/ask` routes enumerated from `internal/web/backend/backend.go:171-188` and matched one-for-one against the doc's list, method and path. No route in the doc is absent from the code and none in the code is absent from the doc.
- `dependency_map.md:50` — taxonomy's usage split. `app/go.mod` and `messenger/go.mod` carry `taxonomy v1.2.0` **direct**; `storage/go.mod:25` and `sentinel/go.mod:21` carry it `// indirect`. Exactly *"direct — app, messenger; indirect — storage, sentinel."*
- `dependency_map.md:31` — *"10 compose services, 6 repo entries, neither list containing cms or jobsimulation"*: both cardinalities re-derived above; `repos.yml` holds `app`, `sentinel`, `storage`, `messenger`, `next-web-app`, `studio-desk`.
- `service_taxonomy.md:107` / `dependency_map.md:18` — `roadrunner/terraform/main.tf:19` is `service_desired_count = 1`; `ROADRUNNER_RPC_ADDR` has 0 occurrences in `platform`. (And `cms/terraform/main.tf:39` / `jobsimulation/terraform/main.tf:40` are both `= 0`, consistent with "rollback path, no traffic".)
- `storage.md:31-32` — *"at the demo's pinned build ref `b948604` the same guard still reports a mid-fold with **six** read sites"*. Enumerated `os.Getenv("STORAGE_RPC_ADDR")` in non-test Go at `b948604`: `main.go:446`, `:524`, `:992`, `cmd/academyImport/main.go:231`, `cmd/academy-asset-upload/main.go:129`, `cmd/import/main.go:50` = **6**. Exact.

**Exact-anchor clearances** (line anchors opened and read with `sed -n 'N-15,N+5p'`, construct confirmed — stronger than RE-DERIVED, weaker than a predicate enumeration):

- `hiring.md` app-side read-path, all exact at `b948604`: `resolver_queries.go:1034` (decl), `:1035` (the `OrgFeatureInsights` gate, first statement), `:1053` (`GetMemberships` with `{StatusActive, StatusInvited}`), `:1080` (manager call), `:1085`/`:1089` (the neighbouring `InsightsJobSimulationBySessions` gate and its `GetMembership` — the doc's disambiguation is right); `intelligence.go:1700` (`m.ent.JobSimulationSession.Query()`), `:1820` (`RoundFloat(float64(ls.Score), 0)`), `:1844` (the blind completion-status re-cast), `:1846` (`Score: &score`), `:885-886` (`insightsSortColumn` mapping `Completition` → `FieldCompletionStatus`); `ent/schema/job_simulation_session.go:39`, `:45`; `ent/jobsimulationsession.go:181-186` (the unconditional `assignValues` cast); `graph.go:129546-129554` and the proto-bound twin `:129392-129400` (both bare `graphql.MarshalString`); `ent/schema/anticheat_result.go:24` (`field.Enum("summary")`); migrations `20260722104506.sql:12/13/29/53/79`, `20260722081626_jobsim_data_model.sql:336/355/376`, `20260729133514.sql:58-62`, `20250416091037.sql:5`; and `SET "score"` = **0** hits set-wide.
- `hiring.md` next-web-app anchors, all exact at `bb3313bc`: `useGetClerkOrganization.tsx:16-18`, `:20-21` (the quoted block matches character-for-character); `useNavbarSections.tsx:459-466`, `:460`; `apps/web/src/context/UserStatusContext.tsx:144-149`, `:168-172`; `apps/hiring/src/context/UserStatusContext.tsx:125`, `:144-145`; `template.tsx:90`; `FreeTrialContainer.tsx:29`; `insights.ts:31-82` (the query block's exact span); `apps/hiring/.../InsightsByMembersContainer.tsx:359` (`<Drawer`); `resolver_cms_queries.go:95`, `:99-103`, `:210`, `:258`, `:295`.
- `storage.md` at its pinned refs: `app` `9d00a313` `main.go:471`, `:472`, `:494`, `:1048` (the last matching the doc's inline quote exactly); `internal/storage/service.go:22`, `:24`; `storage` `63bffc8` `terraform/main.tf:38`, `:35-37`; `internal/storage/storage.go:122`; `Dockerfile:18`.
- `service_taxonomy.md` compose anchors at `0dab54d`: `:18` (sentinel `search_path=sentinel`), `:53` (`DIRECTUS_PUBLIC_BASE_ADDR`), `:174` (`CMS_RPC_ADDR`), `:197`/`:226` (studio-desk block and profile), `:134` (storage profile). Plus `docker-compose.yml:210` and `:324` **at the ref the cell pins** (`2adcf71`) for `storage.md:182` — correct at its pin.
- `service_taxonomy.md` ant-academy anchors at `9c3843cd`: `code/src/graphql/server.js:14,18`; `code/src/lib/serverTenant.js:115-145` and `:145`; `code/src/lib/backendContent.js:36`, `:102-103`; `code/app/api/academy/beacon/route.js:36`, `:41-55`; `code/app/layout.jsx:132`; `public/academy-manifest.json` `"display": "standalone"`; `next ^16.2.6` / `react ^19.2.5`; zero `serwist`/`workbox` deps; `.github/workflows/deploy-academy.yaml`; and ant-academy absent from `repos.yml`.
- `askengine.md` constants: `bedrock.go:25-26` (`DefaultModelID = "eu.anthropic.claude-sonnet-4-6"`, `DefaultRegion = "eu-west-1"`), `Temperature: 0`; `executor.go:18` `MaxInlineRows = 200`, `:21` `MaxCellLength = 400`, `:24` `QueryTimeout = 10 * time.Second` applied as `SET LOCAL statement_timeout`; `handler.go:34` `maxAgenticIterations = 15`, `:41` `loopTimeout = 10 * time.Minute`, `:248` `context.WithoutCancel`; `main.go:150` `askEngineMaxConns = 6`; `rules.md` = **146,219 bytes** (*"~146 KB"*); the five `ask_*` tables all `CREATE TABLE`d in `public`; tags `v1.267.0` = 2026-05-07 and `v1.340.0` = 2026-07-17, both exact.
- `chronos.md:5-6` — `045857c` is *"chore: remove chronos service from orchestration…"* and its diffstat touches **both** `docker-compose.yml` (−37) and `repos.yml` (−4, deleting the `chronos` entry with `schema: chronos`). `fdfa189` is the intelligence removal.
- `dependency_map.md:13` (infrastructure half), `:61`, `:102-103` — pgvector columns really are `extensions.vector(1536)` (five migration sites), `DIRECTUS_WEBHOOK_SECRET` gates `POST /api/webhook/directus` (`app/main.go:1088-1089`, `backend.go:137-138`, `:324`), and `app/internal/converter/gotenberg.go:31` posts to `/forms/libreoffice/convert` with `GOTENBERG_URL=http://gotenberg:3200` at `docker-compose.yml:57`.

**RE-DERIVED only** (weakest clearance — recorded as such, per §5 rule 24): none. Where a passage showed its own arithmetic (the four undefaulted columns, the subgraph ladder, the six read sites, the 60 table defs) I enumerated the set from source before touching the sum, and each is listed under ENUMERATED above.

---

## What I could not settle

- `hiring.md:140-141` (*"87 real `SIMULATION_TYPE_HIRING` sims"* in the captured snapshot) and `:144-145` (*"0 `job_position` rows"*, *"the prod 443 was never captured"*). These are properties of the `.agentspace` snapshot cache and of prod, neither of which is in the ground-truth clone set. **Settled by:** `stacksnap status` against a populated cache, or a `--dsn` read under `AssertPublicOnly`.
- `hiring.md:361-400` — the M227/M228 render-gate numbers (5/5 positions, 8/8/9/9/8 candidates, scores 27–100, recruiter p95 1.27 s). These are live-stack measurements on `billion`; the spec file `stack-verify/e2e/tests/render-hiring-comparison.spec.ts` exists in the authoring copy, but the verdicts cannot be reproduced from a static tree. **Settled by:** a cold reset-to-seed run with the recorded invocation and vantage (§5 rule 12).
- `chronos.md` §§ *Architecture & Code Map* through *CLI Flags* (ports 8080/8081, `LIMIT 10` + `FOR UPDATE SKIP LOCKED`, the 3-second run timeout, the Sentry cron heartbeat, the `--interval`/`--server` flags, the `timers` DDL). `chronos` is not in `repos.yml` and is not among the 13 ground-truth clones, so none of it is checkable here. The doc is explicit that this is preserved history, but it is **prose-under-review with no re-check date** (§5 rule 27, row three) and no fence can reach it. **Settled by:** cloning `anthropos-work/chronos` and reading `internal/database/schema.sql` + `internal/triggerer/triggerer.go`.
- `chronos.md:3` (*"the GitHub repo is NOT archived"*) and `service_taxonomy.md:103-107` (repos ARCHIVED 2026-07-01 / 2026-07-31; cms *"frozen, **not** archived"*). Archive state is a GitHub API property, not a property of any clone. **Settled by:** `gh repo view anthropos-work/<name> --json isArchived`.
- `dependency_map.md:27` — *"production terraform: `skiller_rpc_addr = http://backend:8081`"*. The `infrastructure` repo is not in the clone set. **Settled by:** reading `infrastructure/.../services.tf`.
- `service_taxonomy.md:120` — *"≥42,790 skills / ≥22,470 job roles (public subset, measured 2026-06-29)"*. A prod DB measurement; not derivable from source. **Settled by:** `/db-query` against the public predicate, re-dated.
- `storage.md:12` explicitly declares its own gap — *"the production **M810** rollback-path teardown was not measured here"* — which I record rather than book, since the doc states the limit itself.
