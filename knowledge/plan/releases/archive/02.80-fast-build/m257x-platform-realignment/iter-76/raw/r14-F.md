# Seat F — reading #14

Refs at open (re-verified by `git rev-parse` in this session, all matching the briefing):
`app b948604f` · `platform 0dab54df` · `next-web-app bb3313bc` · `sentinel 88bc5592` ·
`storage 4ce8ece5` · `messenger fa47850d` · `cms ca50c817` · `graphql-wundergraph 60c229f3` ·
`roadrunner 87d8d443` · `jobsimulation 462343b0` · `studio-desk 14a5442a` · `ant-academy 9c3843cd` ·
`.agentspace/rosetta-extensions 1334a4f5` (authoring copy, main). Corpus read at repo HEAD
(`1937e1f`, branch `m257x/platform-realignment`).

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/services/ant-academy.md` | 436 | yes |
| `corpus/services/studio-desk.md` | 435 | yes |
| `corpus/architecture/shared_libraries.md` | 242 | yes |
| `corpus/services/jobsimulation.md` | 233 | yes |
| `corpus/services/skillpath.md` | 107 | yes |
| `corpus/architecture/frontend_architecture.md` | 105 | yes |

(Also read, as the briefing instructs: `corpus/ops/platform-alignment.md` §5 in full, 1680 lines.)

---

## Blockers

### B1 — `jobsimulation.md` banner asserts the jobsimulation husk container still starts locally; it does not, and the same file says so twice

- **Anchor:** `corpus/services/jobsimulation.md:11-14` —
  > "**⚠️ But locally the husk still starts.** `docker-compose.yml:83` @ platform `2adcf71` still defines a
  > `jobsimulation` service **in the default `graphql` profile**, and `repos.yml:17-19` still lists the repo
  > (marked `migrations: false # legacy`) … State: `running_but_unfederated`; container teardown is **M810**."
- **Why it is false:** At the checkout ref `platform 0dab54df` there is **no `jobsimulation` compose service
  and no `jobsimulation` entry in `repos.yml`**. Parsed `docker-compose.yml` + its `include: common.yml`:
  services are `sentinel · backend · storage · customerio-sync · messenger · studio-desk · next-web-app ·
  gotenberg` (8) + `postgresql · redis` (2) — no jobsimulation. `platform/repos.yml` holds 6 entries
  (`app · sentinel · storage · messenger · next-web-app · studio-desk`), and its own header comment says the
  five folded repos "own no local schema, **no compose service and no clone entry here**"
  (`stack-demo/platform/repos.yml:7-8`).
  The individual anchors are correct *at the named pin* (`git show 2adcf71:docker-compose.yml` line 83 is
  `jobsimulation:`, with `profiles: [graphql, jobsimulation, all]` at :140; `2adcf71:repos.yml:17-19` is the
  jobsimulation entry) — but §5 rule 33 is explicit that **a block asserting currency cannot be pinned into
  silence**, and this block's own headline verb is "*still starts*", plus a present-tense `State:` verdict.
  It is also a **self-contradiction**, twice over inside the same document:
  - `jobsimulation.md:35` — "`0dab54d`'s compose declares … `jobsimulation` is not one of them"
  - `jobsimulation.md:72` — "**Profile**: **none — there is no `jobsimulation` compose service.**"
  …and against the machine-fenced map: `corpus/architecture/platform-migration-status.md:73` records the
  local state as **`decommissioned`** — "**Compose service and `repos.yml` entry both deleted by `d11a403`.**"
- **Confidence:** high

### B2 — "`0dab54d`'s compose declares nine services" — it declares ten (or eight in the top-level file); neither is nine

- **Anchor:** `corpus/services/jobsimulation.md:35` — "`0dab54d`'s compose declares nine services and
  `jobsimulation` is not one of them."
- **Why it is false:** The claim names the checkout ref, so it is graded there. I enumerated the set from the
  source rather than trusting the sum (§5 rule 24): `stack-demo/platform/docker-compose.yml` declares **8**
  (`sentinel:5, backend:28, storage:102, customerio-sync:136, messenger:156, studio-desk:197,
  next-web-app:228, gotenberg:255`), and it opens with `include:` → `common.yml`
  (`docker-compose.yml:1-2`), which adds **`postgresql` + `redis`** = **10 total**. §5 rule 30's own corollary
  ("resolving compose's `include:` is load-bearing … two thirds of that floor lives in `common.yml`") is the
  reason the include cannot be dropped from the count. There is no reading that yields 9.
  The *point* the sentence supports (jobsimulation is absent) is true; the scalar is wrong.
- **Confidence:** high (I hesitated only because the wrong number does not change the conclusion of that
  sentence — but it is a derived scalar contradicting source, the class §5 rule 21 counts as a real finding)

### B3 — jobsimulation "Ports" row states, in the present tense and unpinned, that the platform publishes 8400/8401 — it publishes nothing, and the next bullet says so

- **Anchor:** `corpus/services/jobsimulation.md:71` — "**Ports**: 8400 (GraphQL/HTTP), 8401 (Connect-RPC) —
  **as deployed by the platform**, which sets `PORT=8400` / `RPC_PORT=8401` and publishes `8400:8400` /
  `8401:8401` (`platform/docker-compose.yml`)."
- **Why it is false:** No ref is named, so it grades at the checkout. At `platform 0dab54df` the strings
  `PORT=8400`, `RPC_PORT=8401`, `"8400:8400"` and `"8401:8401"` do **not occur** in `docker-compose.yml` at
  all (they occur only at `2adcf71`, lines 92-94/113/119). The very next bullet,
  `jobsimulation.md:72`, states "there is no `jobsimulation` compose service", so the two lines cannot both
  be true. The "use 8400/8401 for anything driven through `platform`, and add the stack offset for a
  `dev-N`/`demo-N`" instruction routes a reader to an offset port that nothing listens on.
- **Confidence:** medium-high (hesitated: the row could be read as archaeology of the frozen repo — but it
  says "as deployed by the platform" and gives an operational instruction)

### B4 — the `make migrate` warning cites `repos.yml:17-19` for jobsimulation; that anchor is `sentinel`/`storage`, and jobsimulation has no entry at all

- **Anchor:** `corpus/services/jobsimulation.md:224` — "`make migrate   # NOT S=jobsimulation.
  `repos.yml:17-19` sets `migrations: false`, so the migrating set is `app` ALONE.`"
- **Why it is false:** At `platform 0dab54df`, `repos.yml:17-19` reads
  `migrations: false` (the **sentinel** entry) / `- name: storage` / `type: go`. jobsimulation is **not in
  `repos.yml` at any line** (6 entries, enumerated above). The *conclusion* ("the migrating set is `app`
  alone") is correct — `MIGRATION_REPOS` derives from `migrations: true`, which only `app` carries
  (`stack-demo/platform/Makefile:13` + `repos.yml:11-14`) — but the cited evidence names the wrong rows and
  implies a jobsimulation entry that no longer exists. (The adjacent warning is still live: `make migrate
  S=jobsimulation` does not consult `repos.yml` at all — `Makefile:91-100` only tests that
  `$(PARENT_DIR)/jobsimulation` is a directory — so the "re-materialises the dead schema" hazard is real.)
- **Confidence:** medium-high

### B5 — the "Startup contract" / `$HOME/.aws/credentials` section diagnoses a container that cannot exist, and points the reader at the wrong service

- **Anchor:** `corpus/services/jobsimulation.md:143-194`, notably `:167`
  (`docker logs demo-<N>-jobsimulation-1 2>&1 | head -3`) and `:191-194`
  ("**Downstream while it is dead:** the AI-Simulations surface is gone; its GraphQL subgraph errors …").
- **Why it is false/misleading:** There is no `jobsimulation` compose service at `platform 0dab54df` (B1),
  so `demo-<N>-jobsimulation-1` is a container name that cannot be produced by any bring-up — the section is
  headed "**read this before diagnosing a crash**" and the first command it gives cannot return anything.
  The mechanism it documents is still live but has **moved**: the `$HOME/.aws/credentials:/root/.aws/
  credentials:ro` bind is now on **`backend`** (`stack-demo/platform/docker-compose.yml:91` — and it is
  indeed the file's only AWS bind, which I verified). `platform-alignment.md:791-809` (§5 rule 28) records
  iter-56 reproducing exactly this dead-2-line signature against the **`backend`** image. Also self-
  contradictory: ":191-194" says "its GraphQL subgraph errors", while `:39-40` and `:97` say the
  jobsimulation subgraph no longer exists (supergraph = one subgraph; confirmed against
  `stack-demo/graphql-wundergraph/supergraph-config-compose.yaml`, which lists `backend` only).
- **Confidence:** medium (hesitated: the section legitimately documents the frozen repo's cobra contract —
  but the diagnostic commands and the "while it is dead" paragraph are operational instructions for a
  container that is gone, and the real landmine's owner is un-named)

### B6 — `20260729133514.sql` is described as "the last migration in the repo"; two later migrations exist

- **Anchor:** `corpus/services/skillpath.md:82-84` — "`DROP TABLE "local_skill_path_sessions"` at
  `app/terraform/migrations/20260729133514.sql:63` (and `local_jobsimulation_sessions` at `:62`), **the last
  migration in the repo**".
- **Why it is false:** At the checkout `app b948604f`, `app/terraform/migrations/` ends with
  `20260729133514.sql`, **`20260731131307.sql`** (adds `brief` + `credits_spent` to `course_builder_sessions`)
  and **`20260731154527_academy_chapter_progress_completed_at.sql`**. Two migrations post-date it.
  The load-bearing half of the sentence is TRUE and I verified it line by line: `:62` is
  `DROP TABLE "local_jobsimulation_sessions";`, `:63` is `DROP TABLE "local_skill_path_sessions";`, and no
  `local_skill_path_session*.go` Ent schema exists (`ls app/internal/data/ent/schema/ | grep -i local` → 0).
  But "the last migration in the repo" is the clause a reader uses to conclude *nothing after it re-creates
  the tables*, and it is not true as written.
- **Confidence:** medium (hesitated between MINOR and BLOCKER: the surrounding retraction is correct, and
  this is a supporting detail — booked up per the briefing's tie-break rule)

### B7 — `app/CLAUDE.md:72` is cited as still listing `SkillPathSessionService`; that line is a closing code fence

- **Anchor:** `corpus/services/skillpath.md:34` — "(`app/CLAUDE.md:72` and `app/knowledge/architecture.md:28`
  still list the service — Trap C …)"
- **Why it is false:** At the checkout `app b948604f`, `app/CLAUDE.md:72` is the closing ` ``` ` of a bash
  block whose last statement is `atlas migrate apply --dir file://terraform/migrations` (:71). The line that
  actually names the service is **`app/CLAUDE.md:80`** — "…one Connect-RPC mux carrying `BackendUsersService`,
  `BackendOrganizationsService`, `SkillerService`, `SkillPathSessionService`, …". The **twin** anchor in the
  same sentence is exactly right (`app/knowledge/architecture.md:28` is the RPC table row naming
  `SkillPathSessionService`), which is why the sentence reads as verified. This is §5 rule 21's second class
  — an anchor that resolves but names the wrong construct.
- **Confidence:** high (the anchor is unpinned, so it grades at the checkout; the surrounding claim — 0
  occurrences of `SkillPathSessionService` in Go source — I independently confirmed, see clearances)

### B8 — `shared_libraries.md` proto row asserts the husk repos are still cloned and still built by compose; both anchors point at unrelated lines and the same table says the opposite 27 lines above

- **Anchor:** `corpus/architecture/shared_libraries.md:69` — "The husk repos **still carry their own
  `go.mod`** — `repos.yml:14-19` still clones them and `docker-compose.yml:83,144` still builds them"
- **Why it is false:** Unpinned present tense → graded at `platform 0dab54df`. There:
  `repos.yml:14-19` = `schema: public` (tail of the **app** entry) + the **sentinel** entry + the first two
  lines of **storage**; no cms/jobsimulation entry exists anywhere in the file.
  `docker-compose.yml:83` = `- STORAGE_S3_PUBLIC_BUCKET=production-storage-public…` (an env line of
  **backend**); `docker-compose.yml:144` = `- "8080:8080"` (a port of **customerio-sync**). Neither is a
  build stanza. (Both anchors *were* right at `2adcf71`: :83 = `jobsimulation:`, :144 = `cms:`.)
  Self-contradiction inside the same table: `shared_libraries.md:41` says "`d11a403` deleted both from
  compose and from `repos.yml`, so at platform `0dab54d` the split is **two-way, not three**", and `:42`
  says the husk containers "are **gone from compose** at `0dab54d`".
  The *first* half of the row is correct and I enumerated it: app/messenger `v1.210.0`, cms `v1.207.0`,
  jobsimulation `v1.205.0`, sentinel `v1.200.0`, storage/roadrunner `v1.196.0` — all seven `go.mod`s match.
- **Confidence:** high

### B9 — `shared_libraries.md` ai row attributes the `ai` dependency to "still-running cms and jobsimulation husk **containers**"

- **Anchor:** `corpus/architecture/shared_libraries.md:105` — "**and, as their own direct `go.mod` requires,
  the still-running `cms` and `jobsimulation` husk containers** (`cms/go.mod:9`,
  `jobsimulation/go.mod:11`, both `v1.40.2`)"
- **Why it is false:** No such containers exist at `platform 0dab54df` (enumerated in B1). The *module* facts
  are exactly right — `cms/go.mod:9` and `jobsimulation/go.mod:11` both pin `github.com/anthropos-work/ai
  v1.40.2`, and `roadrunner` requires neither — so the defect is purely the "still-running … containers"
  predicate, which contradicts `shared_libraries.md:42` ("**Every** live Go service: app, sentinel, storage,
  messenger — and **only** those four … gone from compose at `0dab54d`") in the same document.
- **Confidence:** high

### B10 — `shared_libraries.md` taxonomy row calls cms/jobsimulation/roadrunner "live Go service repos" and their containers "still-running"

- **Anchor:** `corpus/architecture/shared_libraries.md:181` — "**and the still-running `cms` +
  `jobsimulation` husk containers** … **6 of the 7 live Go service repos** (app, sentinel, storage,
  messenger, cms, jobsimulation, roadrunner)"
- **Why it is false:** Same missing containers as B9. Additionally the **set** the "6 of 7" ranges over is
  wrong (§5 rule 24 — re-derive the set, not the sum): at `platform 0dab54df` there are **4** Go service
  repos in `repos.yml` (app, sentinel, storage, messenger), and the file's own comment calls cms /
  jobsimulation / roadrunner "frozen legacy … no compose service and no clone entry here"
  (`repos.yml:7-8`). The arithmetic (2 direct + 2 direct + 2 indirect = 6, minus roadrunner) is internally
  consistent and every `go.mod` anchor is right — `cms/go.mod:13`, `jobsimulation/go.mod:15` (both
  `v1.2.0`, neither `// indirect`), `messenger/go.mod:9` direct, `sentinel/go.mod:21` and
  `storage/go.mod:25` `// indirect`, `app/go.mod:20`, roadrunner none. It is the predicate "live Go service
  repos" that is false, and it contradicts `:42` in the same document.
- **Confidence:** high

### B11 — the EU-first-ladder retraction is cited at `external_services.md:546`; it is at `:554`, and `:546` is inside an unrelated blockquote

- **Anchor:** `corpus/architecture/shared_libraries.md:125` — "(And what the consumers do is not an EU-first
  fallback *ladder* either — `external_services.md:546` retracts that chain.)"
- **Why it is false:** `corpus/architecture/external_services.md:554` is the retraction — "There is **no
  ordered EU-first fallback chain.** The corpus asserted one for several releases…". Line **546** is the
  third line of the `> **app/studio/** is an IN-IMAGE path…` blockquote ("`app` image as an
  `additional_repo` (app v1.360.1) — the source lives in"), a completely different construct. The corpus
  also disagrees with itself about this anchor: root `CLAUDE.md` cites **`external_services.md:545`** for the
  same retraction (also inside that blockquote), while `architecture_overview.md:246`,
  `security_compliance.md:7` and `security_compliance.md:198` all cite `:546`. This is §5 rule 34's exact
  mechanism (a corpus edit moved the target and every citation below it), and the class is wider than my
  scope — I book only the instance in my assigned file.
- **Confidence:** high

### B12 — `frontend_architecture.md` cites two `platform/docker-compose.yml` line numbers that are past the end of the file

- **Anchor:** `corpus/architecture/frontend_architecture.md:11` — "*does* have a `studio-desk` compose
  profile (`docker-compose.yml:311`)"; and `:39` — "~15 sites hitting `NEXT_PUBLIC_BACKEND_API_URL`
  (`:8082`, `docker-compose.yml:362`)".
- **Why it is false:** `stack-demo/platform/docker-compose.yml` at `0dab54df` is **271 lines**; both
  citations are out of range and resolve to nothing. Neither is inside the sentence that carries the
  `@ 2adcf71` pin (which is a *separate* sentence, about `repos.yml`'s 9 entries — §5 rule 33 scopes a pin
  to its own block/wrapped sentence). At `2adcf71` both were correct (`:311` = `studio-desk:`, `:362` =
  `- NEXT_PUBLIC_BACKEND_API_URL=…:8082`). Current locations: studio-desk block at `:197` with
  `profiles: [studio-desk, all]` at `:226`; `NEXT_PUBLIC_BACKEND_API_URL` at `:237` (build arg) and `:246`
  (env). The claims themselves remain true; the evidence does not resolve.
- **Confidence:** medium-high

### B13 — ant-academy's demo-config is described as "the four bring-up patches"; five are applied, and `demopatch-spec.md` says five

- **Anchor:** `corpus/services/ant-academy.md:196-197` — "The academy's **entire** demo-config — the **four**
  bring-up patches (`academy-fs-published-fallback` + `-public` + `-chapter-body` + `ant-academy-dev-origins`)
  applied to the **ephemeral clone** …"
- **Why it is false:** `rosetta-extensions/demo-stack/ant-academy.sh` wires **five** clone patches and
  applies/reconciles all of them at bring-up: `DEVORIGINS_PATCH` (:56), `FSPUB_PATCH` (:66),
  `FSBODY_PATCH` (:75), `FSPUBLIC_PATCH` (:84) and **`COCKPIT_PATCH` (:90 —
  `apply-ant-academy-back-to-cockpit.sh`, M249)**, with the cockpit patch re-applied in
  `reapply_clone_patches` (:178-180) and reverted on `--stop` (:212-215).
  `demo-stack/patches/` holds five ant-academy manifests, including `ant-academy-back-to-cockpit`.
  Direct corpus self-contradiction: `corpus/ops/demo/demopatch-spec.md:146` — "the **five** `ant-academy`
  patches (`ant-academy-dev-origins`, `academy-fs-published-fallback`, `academy-fs-published-public`,
  `academy-fs-published-chapter-body`, **`ant-academy-back-to-cockpit`** — M249 …)"; also listed in
  `frontend-tier.md:530`. `ant-academy.sh:180` even routes its own failure message to
  "see corpus/services/ant-academy.md", the one doc that does not mention it.
  The word "**entire**" is what makes this a completeness claim rather than an illustrative list.
- **Confidence:** medium-high (hesitated: an omitted list member is sometimes graded MINOR — booked up
  because the sentence claims exhaustiveness *and* a sibling corpus doc states the opposite count)

---

## Minors

- `ant-academy.md:246` — "~26 Playwright e2e spec files (tests/e2e/)". Measured at `ant-academy 9c3843cd`:
  **31** `tests/e2e/*.spec.js` (32 including the one non-`.spec.js`-suffixed spec). Approximate marker (`~`)
  and non-material.
- `ant-academy.md:137` — `emptyCatalogView() = { chapters: [], skillPaths: {}, series: [] }` is shown with an
  `=` as the literal definition; `code/src/lib/serverTenant.js:116` also returns `bundles: PUBLIC_BUNDLES,
  catalogVersion: CATALOG_VERSION`. The "0 cards" conclusion is unaffected. (The `:115-145` range and the
  quoted in-code sentence *"the cutover is intentional, not reversible-on-error."* at `:132` are both exact.)
- `ant-academy.md:324,328,345` — "The **app's** env file is `code/.env`" / `cp .env.example .env` /
  "set `REQUIRE_ORGANIZATION_MEMBERSHIP=0` in `code/.env`" vs root `CLAUDE.md` ("the React app reads **only**
  from `code/.env.local`") and this same file's `:197,:203,:319` (`code/.env.local`). Next.js reads both and
  both are gitignored (`code/.gitignore:22-24`), so nothing breaks — but `.env.local` **wins**, and it is
  what `stacksecrets` and `ant-academy.sh` write, so a dev following `:324` can be silently overridden.
- `jobsimulation.md:130` — path `cms/directus/collections/jobsimulation.go:1594-1597` omits `internal/`;
  the real path is `cms/internal/directus/collections/jobsimulation.go` (twin:
  `app/internal/cms/directus/collections/jobsimulation.go`). The line range is **exactly** right
  (`:1594` `func voiceEngineFromDirectus`, `:1595-1596` the nil → `SimulationVoiceEngineGptrealtime`
  default), so the claim is sound; only the path won't `open`.
- `skillpath.md:45` — "(**9 repos** @ platform `2adcf71`, 0 skillpath — skiller and the router have since
  left too)". The 9 is exact (enumerated at `2adcf71`), but skiller and `graphql-wundergraph` were **already
  absent at that pin**, and the clause omits the three that *did* leave since (cms, jobsimulation,
  roadrunner → 6 today), so a reader arrives at 7.
- `studio-desk.md:21` — "It `depends_on` **`backend` + `cms`** — `docker-compose.yml:337-341` @ platform
  `2adcf71`". True at the pin (verified: `:337-341` is exactly that `depends_on`); at `0dab54d` it is
  `backend` alone (`docker-compose.yml:222-224`) and there is no `cms` service to depend on. Pinned, so not
  booked as a blocker.
- `frontend_architecture.md:39` — "~15 sites hitting `NEXT_PUBLIC_BACKEND_API_URL`". Measured at
  `next-web-app bb3313bc`: **32 occurrences across 23 files** (13 of those files under `apps/`, which is
  probably the reading that produced ~15). The predicate ("there ARE direct REST/SSE calls") is true.
- `frontend_architecture.md:29-35` — the "Core Packages (`packages/`)" table lists `ui`, `graphql`,
  `core-js` (+ the two `configs/*`); `packages/design` exists and is not listed.

---

## Positively cleared (audited zeros)

**ENUMERATED** (predicate re-derived from source independently, cardinalities compared):

- `skillpath.md:30-33` — "there is NO `SkillPathSessionService` anywhere … **0** occurrences in Go source
  across the clone set and no `skillpath…v1connect` import." `grep -rn "SkillPathSessionService" stack-demo
  --include='*.go'` → 0; `grep -rn "skillpath.*v1connect" … --include='*.go'` → 0. Positive control in the
  same pass: `grep -rl "SkillPathSession" stack-demo/app --include='*.go'` → **79 files**, so the pipeline
  matches. (The first attempt of this grep died on zsh globbing of a bare `--include=*.go`; I re-ran quoted
  and read stderr — §5 rule 1.)
- `shared_libraries.md:70` — "six Connect handlers … five unconditionally (Users `:1187`, Organizations
  `:1188`, Skiller `:1196`, JobSimulation `:1204`, LabSession `:1228`) plus `CMSService` only when
  `cmsRPCServer != nil` (`:1212-1214`)". `grep -n "mux.Handle(" stack-demo/app/main.go` returns **exactly
  six** lines, at 1187, 1188, 1196, 1204, 1213, 1228 — every anchor lands on its named handler, and 1213 sits
  inside the `if cmsRPCServer != nil` guard. Neither a SkillPath nor a RoadRunner handler is present.
- `shared_libraries.md:69` (first half) + `:41` + `:104` + `:181` (the `go.mod` half) — I read all seven
  `go.mod`s. colony: app/messenger `v0.35.2`, sentinel/storage `v0.34.3`, cms/jobsimulation `v0.35.1`,
  roadrunner `v0.34.3`. proto: as quoted, all five groupings exact. ai: `v1.40.2` in app/cms/jobsimulation
  only. taxonomy: `v1.2.0` direct in app/cms/jobsimulation/messenger, `// indirect` in sentinel/storage,
  absent in roadrunner. Every cited `go.mod:N` resolves to the named require line.
- `shared_libraries.md:145` — "6 and 8 files at `cms ca50c817` / `jobsimulation 462343b0`" importing
  `colony/authn`: `grep -rl "colony/authn" cms --include='*.go' | wc -l` → **6**;
  same for jobsimulation → **8**. And `grep -rn "anthropos-work/authn"` across all seven Go repos (`*.go`
  and `go.mod`) → **0**, so `:162`'s "No checked-out service imports the standalone …" holds.
- `jobsimulation.md:24-30` — "the **23** session/run tables … re-created by
  `20260722081626_jobsim_data_model.sql`" and the rename. `grep -c '^CREATE TABLE'` → **23**, and the 23
  distinct names include `sessions`; the next migration `20260722104506.sql` creates
  `job_simulation_sessions` at **:2** and `DROP TABLE "sessions";` at **:79**, exactly as cited.
- `jobsimulation.md:98` — "`JOBSIMULATION_RPC_ADDR` … reads `http://backend:8083`, like **all four** values
  compose sets". Enumerated `grep -n "_RPC_ADDR" docker-compose.yml` at `0dab54d`: exactly four —
  `BACKEND_USERS_RPC_ADDR:173`, `CMS_RPC_ADDR:174`, `JOBSIMULATION_RPC_ADDR:176`, `SKILLER_RPC_ADDR:183` —
  all `http://backend:8083`. `:176` is the cited anchor and it is right.
- `jobsimulation.md:203-204` — "it exits 0 and starts only postgresql, redis and sentinel". The always-on
  floor is exactly the services with no `profiles:` key: `postgresql` + `redis` (common.yml) + `sentinel`
  (docker-compose.yml) = **3**. Every other service carries a `profiles:` key (grep enumerated: :100, :134,
  :154, :195, :226, :253, :268). Matches §5 rule 30's measured claim.
- `ant-academy.md:250-269` — the complete `isPublic` matcher. I enumerated `code/proxy.js:112-188`: **25
  static patterns + 2 `DEV_LOGIN_ENABLED` + 3 `VISUAL_BYPASS`**. Every row of the corpus table maps
  1-to-1 onto the source list with no member missing and none invented, including the `_meta`/`meta`
  double-spelling rationale and the `VISUAL_BYPASS = BENCHMARK_VISUAL_BYPASS==='1' && NODE_ENV==='development'`
  conjunction (`proxy.js:62-64` — whitelisted, as the doc says).
- `ant-academy.md:277-280` — "There is no `/library/[slug]` route … the per-course page is `/courses/[slug]`".
  `find app -path '*library*'` → only `app/(public)/library/page.jsx`; `/courses` pages are
  `app/(authed)/courses/page.jsx` and `app/(authed)/courses/[slug]/page.jsx`. Exact.
- `studio-desk.md:34` — the prod entry set in `vite.config.ts` `rollupOptions.input`. Enumerated: **9**
  (`home, sim-advanced-builder, builder-skill-path, simulation-builder, sim-guided-builder, generation,
  catalog, academy, skills`) with `dev-accept` behind `...(isProduction ? {} : {…})`. Matches the doc member
  for member.
- `studio-desk.md:120` — "Multi-language support (7 languages)": `app/public/l12n/` holds exactly 7
  dictionaries (de, en, es, fr, it, ja, nl).
- `studio-desk.md:279-285` — "no DB driver". Enumerated `package.json` against
  `pg|postgres|prisma|sqlite|mysql|mongodb|mongoose|knex|drizzle-orm|typeorm|sequelize` → **0 matches**
  (rc=1, read); `app/services/studioDB.js` exists as a facade and
  `GET_USER_STUDIO_PREFERENCES`/`SET_USER_STUDIO_PREFERENCES` are at `app/services/graphql/queries.ts:205`
  and `mutations.ts:58`.
- `frontend_architecture.md:35` — "8 locales (de, en, es, fr, it, ja, nl, pt)": `configs/i18n/messages/`
  holds exactly those 8 directories.
- `frontend_architecture.md:11` — "`repos.yml` @ `2adcf71` holds exactly **9** entries, and ant-academy is
  not one of them": `git show 2adcf71:repos.yml | grep -c '^  - name:'` → **9**; enumerated
  (app, cms, jobsimulation, sentinel, storage, messenger, roadrunner, next-web-app, studio-desk).
- `ant-academy.md:26,293` — ant-academy absent from `repos.yml` (all 6 entries enumerated) and cloned
  explicitly by `ensure-clones.sh` phase **d2** (`demo-stack/ensure-clones.sh:20,150-151`).

**Line-anchor / construct checks that resolved correctly** (opened the file and read ±15 lines, §5 rule 10):

- `skillpath.md:14,57` → `app/internal/skillpath/session.go:205-207` is the `// cms-in-app deseam` comment +
  `u.cms.GetSkillPathDomain(...)`; `app/internal/skillpaths/skillpaths.go:88-95` is the twin. Both exact.
- `skillpath.md:75-77` → `app/internal/organization/intelligence.go:1144` is
  `func (m IntelligenceManager) InsightsSkillPathByMemberships`, and `:1159-1170` is precisely the
  `m.ent.SkillPathSession.Query()` block with `SkillPathID`, `StatusIn(Active, Completed)` and the
  `TenantIDIsNil()/TenantID(orgID)` predicate. Exact on all three conjuncts.
- `skillpath.md:38-44` → `app/internal/web/backend/graphql/graph/schemas/skillpath_sessions.graphqls`
  contains all eight named operations at `:71-82`, with `createSkillPathSession` carrying `@deprecated`.
- `skillpath.md:92-99` → `InsightsBySkillPathStudentSimulationsContainer.tsx:31-34` hardcodes
  `return null as unknown as MembershipEnriched` for `userData`, the results table + totals block are
  commented out at `:140-160`, and the body renders `t('enterprise.insights.comingSoon')` ("Coming soon" in
  the en/it dictionaries). `apps/hiring` has no skill-paths route.
- `jobsimulation.md:105-110` → `20260729133514.sql`: `:15-23` are the two `UPDATE … SET session_id /
  js_session_id` remaps, `:36-44` the two orphan-NULLing UPDATEs, `:58` the `-- 5. Drop the mirrors.` comment,
  `:62-63` the two `DROP TABLE`s. **0 `INSERT` statements** (the single case-insensitive hit is the function
  name `on_insert_local_jobsimulation_sessions_update_memberships` at `:64`).
  `app/internal/organization/intelligence.go:1700` is `query := m.ent.JobSimulationSession.Query().`.
- `jobsimulation.md:103` → `jobsimulation/internal/graph/queries.resolvers.go:70` is
  `func (r *queryResolver) JobSimulationResult(...)`, whose `case 2, 3` calls
  `GetValidationAttemptResultBySession` — a persisted read, as claimed.
- `jobsimulation.md:37,119` → `app/main.go:1204` is the in-app `JobSimulationServiceHandler`;
  `app/main.go:980-982` is `directusBaseAddr := os.Getenv("DIRECTUS_BASE_ADDR")` + the `log.Fatalf`;
  `app/cms_reader_switch.go` exists; rext `stack-injection/gen_injected_override.py:84` sets
  `DIRECTUS_DATA_CONSUMERS = ("cms", "backend")` and a test fences both twins.
- `jobsimulation.md:8,133,123` → `jobsimulation/terraform/main.tf:40` `service_desired_count = 0`;
  `calls/livekit.go:131-135` is the `flag_use_realtime_openai` `IsFeatureEnabled` call and `:140-144` sets
  `agentName = "anthropos-agent"` + `agentEndpoint = "openai-hosted"`; `internal/runner/runner.go`'s package
  header reads *"(formerly the standalone \"roadrunner\" service)"*.
- `jobsimulation.md:71,84,141,145-153` → `cmd/root.go:77-78` falls back to `8080`/`8081`; both Dockerfiles
  `EXPOSE 8080` and `ENTRYPOINT ["./application"]` with no CMD; `internal/worker/worker.go:27,104` give
  `Concurrency: 10` and `25`; `pubsub.NewSubscriberServer` is in `cmd/root.go:121`; the only subcommands are
  `aggregate`, `test-command`, `clone-session`, `validate`; the repo's `CLAUDE.md:88` does document the
  non-existent `go run . serve`.
- `jobsimulation.md:91-93` → asynq `v0.26.0` in `go.mod:24`; commit `09631fb2` has the quoted subject;
  migrations `20260402145459.sql` + `20260409131539_add_summary_to_extraction_results.sql` exist; platform
  `05b4035` is *"chore(compose): add READONLY_DB_CONNECTION to jobsimulation service"*;
  `CHIME_RECORDINGS_BUCKET_NAME=ant-prod-chime-demo` at `docker-compose.yml:50`.
- `shared_libraries.md:79` → at the **named** ref `app 9d00a313 (v1.367.0)`,
  `internal/jobsimwiring/wiring.go:123` is `jsrunner.NewRunnerManager(getenv("JUDGE0_API_KEY"),
  getenv("JUDGE0_BASE_URL"))`, and `ROADRUNNER_RPC_ADDR` has **0** occurrences in `*.go` there and 0 in the
  whole `platform` tree at `0dab54d` (positive controls run in the same pass: `JUDGE0_BASE_URL` → 1 hit at
  that ref; `SKILLER_RPC_ADDR` → 2 hits in platform). The parenthetical is right too:
  `docker-compose.yml:118` at `0dab54d` is `- AWS_REGION=eu-west-1`.
  `app/internal/skillpaths/skillpaths.go:27-31` carries the quoted *"the drop-in for the **removed**
  skillpath RPC client"*.
- `shared_libraries.md:94,121,135,190-193,232` → `storage/internal/migration/migration.go:13` imports
  `go/simulator/storage/v1` as `legacyStorage`; `app/internal/aiusage/ai_usage.go`,
  `app/internal/askengine/bedrock.go`, `app/cmd/createTaxonomy/`, `app/internal/taxonomy/` all exist.
- `ant-academy.md:124-142,174-180` → the whole read chain resolves: `app/(authed)/page.jsx:80`
  `resolveCatalogView()`, `src/lib/backendContent.js:94` `getBackendCatalogView` querying
  `academyCatalogSeries` + `academyCatalogSkillPaths` (`:102-103`), `src/graphql/server.js:14-18`
  reading `NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT` and throwing when unset,
  `src/lib/serverChapterBody.js:51-67` (`resolveServerChapterBody` → `getBackendChapterBody` → draft path →
  `{ notFound: true }`), and `app/not-found.jsx:43` "You wandered off the trail."
- `ant-academy.md:239-248` → `code/package.json`: `engines.node` is `">=22"` (the doc's "Node `>= 22`" is
  right, and it is deliberately *not* studio-desk's `>=24`), `"dev": "next dev --port 3077"`,
  `next ^16.2.6`, `react ^19.2.5`, `expo ~54.0.33` (mobile), `vercel.json` = `{"framework":"nextjs"}`,
  `app/layout.jsx:132` `manifest: "/academy-manifest.json"`, FA vendored at
  `code/public/assets/fontawesome/{css,webfonts}`, all four named workflows present, `cog.toml` present,
  `RegisterServiceWorker.jsx` is a kill-switch (`unregister` + `caches.delete`).
- `ant-academy.md:219-233` → `src/i18n/locale.js:33` `coerceLocale` (falls back to `en`), `:42`
  `localeFromSearchParams`, `LocaleSwitch.jsx` is a **21-line** 2-way `locale === 'it' ? 'en' : 'it'` `<Link>`
  (not a menu), no `/[locale]` route exists, and `proxy.js:241` sets the `x-locale` header;
  `tests/e2e/i18n-language-toggle.spec.js` exists.
- `ant-academy.md:63-75` → `app/cmd/academy-seed/` and `app/internal/academy/` exist; Ent schemas
  `academy_chapter_progress.go` / `academy_last_activity.go` (tables pluralized) exist;
  `upsertChapterProgress` `:694` / `Batch` `:696` / `BatchTolerant` `:698` / `setLastActivity` `:700` in
  `academy.graphqls`; `code/app/api/academy/beacon/route.js` exists; the FS-published catalog patch
  `demo-stack/patches/academy-fs-published-fallback` and body patch `-chapter-body` exist and are applied by
  `ant-academy.sh`; `autoverify.sh:459-479` is the assert-(f) academy check.
- `studio-desk.md:107` → `src/routes/skillpath.ts` is **61 080 bytes** (largest route ✓);
  `src/routes/youtube.ts:43` is `const apiKey = process.env.YOUTUBE_API_KEY;` with the `_mock: true`
  fallback below it; `.env.example:120` is `GCLOUD_SERVICE_ACCOUNT=`; `terraform/main.tf:129` is its
  `"name": "GCLOUD_SERVICE_ACCOUNT"` secret injection; and `grep -rn GCLOUD_SERVICE_ACCOUNT src/` → **0**.
- `studio-desk.md:112,161,172,305,313-320,388-393` → `AI_PROVIDER_CHAIN=azure-openai,openai` at
  `.env.example:57`; `AI_DEFAULT_TIER=fast` at `.env.example:61` vs in-code `|| 'thinking_fast'` at
  `src/services/ai/config.ts:182`; the tier maps at `config.ts:20-39` match the quoted models;
  `engines.node ">=24"` + `node:24-alpine` in both Dockerfiles; `src/index.ts:60`
  `process.env.PORT || 9100`; `STUDIO_ACCESS_ROLES` at `src/index.ts:96` is exactly the four-element list;
  `app/core/scaffold/{pageWrapper.js,userProfile.js}` exist; `app/core/main.ts:97` is
  `preloadCriticalCSS();`, `:105` is the GlitchTip/tailscale-funnel comment, `:206` is `new PageWrapper();`.
- `studio-desk.md:322-328,401-415` → rext carries `demo-stack/patches/studio-desk-{back-to-cockpit,
  logout-url,logo-url,shell-first-paint,no-thirdparty}` (5), `build_frontend_studio_desk()` at
  `demo-stack/up-injected.sh:881`, `stack-verify/e2e/run-studio-fcp.sh`, the M252 `env_file` emission in
  `gen_injected_override.py:428-440`, and both named regression tests
  (`stack-injection/tests/test_injection.py:1654,1711`).
- `frontend_architecture.md:21-24,48-52,75-77` → apps/ports (`web` 3000, `hiring` 3001, `integration` 3002,
  `mobile` `expo start --port 3031`), `!apps/mobile` in `pnpm-workspace.yaml`,
  `apps/web/package.json:46` `"next": "^16.2.7"` (same in hiring/integration/maintenance),
  `packageManager "pnpm@10.30.3"`, `engines.node ">=24.0.0"`, `turbo ^2.9.6`, `typescript ^5.9.3`,
  `UPGRADE-IMPACT-next16.md`, the `check*/clean:quick/codegen*` scripts, the three `--filter` package names,
  and `packages/graphql/codegen.ts` (`documents: ['src/query/**/*.{ts,tsx}']`, `preset: 'client'`,
  `./src/__generated__/`).
- `jobsimulation.md:97` / `ant-academy.md:107` — "the supergraph is one subgraph (`backend.graphqls`)":
  `stack-demo/graphql-wundergraph/supergraph-config-compose.yaml` declares a single subgraph `backend`
  → `./schemas/backend.graphqls`, and `schemas/` contains only that file.

**RE-DERIVED only** (weaker — flagged as such per §5 rule 24):

- `shared_libraries.md:77-83` — "12 Connect-RPC services are defined". The listed names count to 12, but the
  `proto` repo is not in the clone set, so I could not enumerate the `.proto` service definitions
  independently. Recorded as re-derived, not cleared.

---

## What I could not settle

1. **Anything inside `colony`, `proto`, `taxonomy` and the standalone `authn`.** They are private Go modules,
   absent from `repos.yml` and from `stack-demo/` by design, so `shared_libraries.md:40` (`go 1.25.0` /
   `golang:1.26-bookworm`), the colony package table (`:48-54`), the rate-limiter and Sentry notes
   (`:56-59`), buf `v1.57.0` (`:68`), the 12-service proto count (`:77-83`), the `ai.AI` method list and
   per-provider constructor table (`:107-116`), the Anthropic `panic` / retry-policy gotchas (`:130-134`),
   `authn`'s missing-`return` defect (`:164`), the "131-line `node.go`" and the `NodeID` format/examples
   (`:188`, `:215-229`) are all unverifiable from this tree. Settling them needs the four module repos
   cloned at the shas the services pin (colony `v0.35.2`, proto `v1.210.0`, ai `v1.40.2`, taxonomy `v1.2.0`).
2. **The taxonomy figures (`shared_libraries.md:200-213`)** — 22,470 roles / 42,790 skills, and the
   `job_role_embeddings` 18,919 attribution. These come from a production read-only capture manifest under
   `.agentspace/snapshots/taxonomy/`; I did not have a live stack DB to re-run
   `select count(*) … where organization_id is null`. The *shape* of the correction (public ⊆ total, so 18K
   is below the floor) is internally sound.
3. **Production-side claims**: `jobsimulation.md:33` (`http://backend.internal.anthropos:8081` in prod),
   `:47-50` (`infrastructure/terraform/production/services.tf` declaring `module.jobsimulation_euwest1` and
   owning the LiveKit/Chime buckets), `studio-desk.md:132` ("the Cosmo Router survives in production only"),
   `frontend_architecture.md:39,44` (Cosmo Router at `:8080/graphql` in prod). The `infrastructure` repo is
   not in the clone set.
4. **"The GitHub repo was archived 2026-07-31"** (`jobsimulation.md:13`) — needs a GitHub API call.
5. **Render-time counts**: `ant-academy.md:74` "a grid of **65** real cards" (and root `CLAUDE.md`'s "65 real
   cards with 0 Draft chips"). This is a live-render measurement against a patched demo clone; the committed
   tree has 20 top-level series dirs / 78 second-level path dirs, which neither confirms nor refutes 65.
   Settling it needs a demo bring-up with `ACADEMY_DEMO_FS_PUBLISHED=1` and a DOM count.
6. **The M253 / M245 measured numbers** (`studio-desk.md:412` "p95 4669 ms → 817 ms";
   `:423` "4049 → 38 ms", "6936 → 2152 ms") — timing measurements on `demo-2` / `billion`, not reproducible
   from a static tree.
7. **Whether `jobsimulation.md:11-14`'s pin was *intended* to cover the whole block.** I graded it as a
   currency assertion under §5 rule 33 and booked B1. If the milestone's convention is that a leading
   `@ <sha>` pins an entire blockquote regardless of tense, B1 downgrades to "stale-but-pinned" — but the
   self-contradiction with `:35`, `:72` and `platform-migration-status.md:73` stands either way.
