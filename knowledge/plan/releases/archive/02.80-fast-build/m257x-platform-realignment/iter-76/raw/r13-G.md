# Seat G — reading #13 (adversarial diff seat)

## Positive controls

`git diff --stat 0e35b1a..HEAD -- corpus/ CLAUDE.md .claude/` →
**32 files changed, 898 insertions(+), 257 deletions(-)** — matches the briefing exactly.

Diff read in full (2,115 lines, dumped and read top-to-bottom in four passes).

| artefact | measure | read in full? |
|---|---|---|
| the repair diff (`0e35b1a..HEAD`, `corpus/ CLAUDE.md .claude/`) | 2,115 lines / 32 files / +898 −257 | yes |
| `corpus/ops/platform-alignment.md` (protocol §5, in scope) | 1,680 | §5 + §6 + §7 + §8 + the diff's additions |
| `CLAUDE.md` | 555 | all diff hunks + surrounding context |
| `corpus/architecture/architecture_overview.md` | 353 | all hunks + §Core-Backend / §Archived tables |
| `corpus/architecture/service_taxonomy.md` | 456 | all hunks + profile tables |
| `corpus/architecture/platform-migration-status.md` | 259 | whole file (fenced map) |
| `corpus/architecture/external_services.md` | 840 | all hunks + §Architecture + §Routing |
| `corpus/architecture/dependency_map.md` | 103 | whole file |
| `corpus/architecture/shared_libraries.md` | 242 | colony + proto sections |
| `corpus/architecture/security_compliance.md` | 281 | all hunks |
| `corpus/architecture/ai_architecture.md` | 302 | all hunks |
| `corpus/services/{cms,jobsimulation,roadrunner,storage,messenger,backend}.md` | 259/233/171/201/146/271 | all hunks + banners |
| `corpus/services/{README,coursebuilder,customerio-sync,gotenberg,next-web-app,studio-desk,studio-room}.md` | 79/140/75/82/126/435/473 | all hunks |
| `corpus/ops/{run_guide,setup_guide,platform_repo}.md` | 525/771/159 | all hunks |
| `corpus/ops/demo/{README,demo-up-defaults,frontend-tier,content-stories-routes}.md` | 353/202/676/477 | all hunks; demo-up-defaults whole |
| `.claude/skills/dev-up/reference.md` · `.claude/skills/db-query/SKILL.md` | 171 / 242 | all hunks + context |

**Ground truth actually opened** (not inferred): `stack-demo/platform` @ `0dab54d` (level with origin);
`stack-demo/app` @ `b948604` (v1.366.0) **and** `9d00a313` (v1.367.0) and `5ba17044` via `git show`;
`stack-demo/{storage,messenger,cms,jobsimulation,roadrunner,sentinel}` at checkout **and** at
`63bffc8`/`a0ec933`; `.agentspace/rosetta-extensions` @ `1334a4f`. Profile membership was **executed**
(`docker compose --profile X config --services`, Compose v5.3.1), and
`platform_alignment_guard.py` was run read-only.

---

## Blockers

### B1 — the default `core` profile is claimed to start NINE containers including cms/jobsim/roadrunner/storage
- **Anchor:** `corpus/architecture/external_services.md:169-172` (line 169 is a MODIFIED line in this diff)
  > "The default `core` profile is **not** just Postgres + `backend`: it starts **nine** containers — `postgresql` + `redis` … and seven application services, `sentinel` · `backend` · `jobsimulation` · `cms` · `storage` · `roadrunner` · `gotenberg`."
- **Why it is false:** the repair renamed `graphql`→`core` on the modified line and left the membership
  list untouched. Executed against the real compose:
  `docker compose --profile core config --services` → `postgresql sentinel redis backend gotenberg` — **five**.
  `jobsimulation`, `cms` and `roadrunner` are not services at all at `0dab54d`
  (`platform/docker-compose.yml` declares exactly 8: `:5 sentinel`, `:28 backend`, `:102 storage`,
  `:136 customerio-sync`, `:156 messenger`, `:197 studio-desk`, `:228 next-web-app`, `:255 gotenberg`),
  and `storage` sits in `profiles: [storage-legacy]` (`:134`). The mermaid diagram immediately below
  (`:176-196`) still draws `CMSHusk` inside `subgraph Docker[Docker Compose (local stack)]` and an edge
  `CMSHusk -.-> Directus` at `:194`. This contradicts the *same file's* repaired blockquote ~35 lines
  earlier ("The standalone `cms` service … is **gone from compose** (`d11a403`), so `backend` is the only
  consumer left"), and contradicts CLAUDE.md, `service_taxonomy.md`, `run_guide.md` and `platform_repo.md`
  — all repaired in this same diff to say **five**.
- **Confidence:** high

### B2 — "compose declares nine services" — a count that is neither 8 nor 10, asserted in three files
- **Anchors:**
  `corpus/architecture/external_services.md:296` — "declares nine services and `cms` is not one of them"
  `corpus/services/cms.md:35` — "declares nine services and `cms` is not one of them"
  `corpus/services/jobsimulation.md:35` — "`0dab54d`'s compose declares nine services and `jobsimulation` is not one of them"
- **Why it is false:** `platform/docker-compose.yml` @ `0dab54d` declares **8** services (enumerated above).
  Resolving its `include: - common.yml` (`:1-2`) adds `postgresql` (`common.yml:2`) and `redis`
  (`common.yml:24`) → **10** for the project. There is no reading that yields nine. The same repair
  states both other numbers elsewhere: `dependency_map.md:33` "10 compose services, 6 repo entries" and
  `architecture_overview.md:191` "deleted by `d11a403`; 8 services remain", and
  `service_taxonomy.md:55` "compose declares ten services". So the corpus now carries three
  mutually-exclusive counts for one derivable set, of which "nine" is the one that matches nothing.
- **Confidence:** high

### B3 — the `:545 → :546` re-point is wrong by 8 lines; six repaired citations now land in an unrelated blockquote
- **Anchors (all ADDED lines):** `CLAUDE.md:245`, `CLAUDE.md:259`,
  `corpus/architecture/architecture_overview.md:246`, `corpus/architecture/security_compliance.md:7`,
  `corpus/architecture/security_compliance.md:198`, `corpus/architecture/shared_libraries.md:125` —
  each citing `external_services.md:546` as the home of the *"no ordered EU-first fallback chain"* retraction.
- **Why it is false:** at `0e35b1a` that sentence was at `external_services.md:545` (verified:
  `git show 0e35b1a:corpus/architecture/external_services.md | sed -n '545p'`). This repair inserted a
  9-line blockquote at `:544-552` (the `app/studio/**` note) plus a +1 shift from the earlier hunk, so the
  sentence is now at **`:554`** (verified: `sed -n '554p'`). `:546` is
  `> \`app\` image as an \`additional_repo\` (app v1.360.1) — the source lives in` — a line about
  studio-room's provenance, not about EU routing. The repair applied only the +1 shift and missed its own
  +8 insertion. This is precisely the failure the diff's own new §5 rule 34 describes
  (*"Line numbers move when YOU edit too … re-derive the line map from `git diff -U0` and re-point in the
  same commit"*) — committed in the same commit that documents it.
- **Confidence:** high

### B4 — the `:569 → :570` re-point is wrong by 8 lines for the same reason
- **Anchors (ADDED):** `corpus/architecture/security_compliance.md:201` and
  `corpus/architecture/architecture_overview.md:248` — "[`external_services.md:570`] enumerates **four live** ways a request leaves the EU".
- **Why it is false:** that enumeration was at `:569` at `0e35b1a` and is now at **`:578`**
  ("iter-52). **Four** things can send a request outside the EU…"). `external_services.md:570` reads
  "attempt (`:150-155`, mirrored at `:296-302`/`:326`), i.e. **direct US OpenAI**." — item 4 of a
  *different* list (the 429 retry mechanics). Same missed +8.
- **Confidence:** high

### B5 — a correct cross-reference was broken by the repair's own line shift, then "explained" with a false claim, and a CHECK item closed on it
- **Anchor:** `corpus/architecture/service_taxonomy.md:313-317` (ADDED)
  > "(This passage used to appeal to [`platform-migration-status.md`] as *"the corpus's own fenced source of truth"* on the point, by `file:line`. That map has **no Directus row at all** — it maps repos, and Directus is an external service — so the anchor resolved to a row about something else entirely. `CHECK-M257x-iter64-pms-87-subject`, closed M257x iter-65.)"
- **Why it is false:** the map **does** have a Directus row —
  `corpus/architecture/platform-migration-status.md:97`:
  `| \`directus\` | external | external | no | the headless CMS at \`content.anthropos.work\`. **Removed from compose** at \`a2a3ee6\` (2026-02-27); …|`
  — i.e. exactly the fact the retracted citation was supporting. And the old anchor was **correct**:
  `git show 0e35b1a:corpus/architecture/platform-migration-status.md | sed -n '86p'` is that same
  `directus` row. This repair added ~11 lines above it (§1 `mid-fold` row, §2 rewrite), moving it 86→97,
  and then wrote a paragraph asserting the row never existed. The stated reason ("it maps repos … Directus
  is an external service") is refuted by the map's own §3 census, which is built for exactly this and
  carries `external` rows for `directus`, `gotenberg`, `ant-academy`, `postgresql` and `redis`.
- **Confidence:** high

### B6 — "there was never any `storage` profile at all" — over-correction, refuted by the platform's own history
- **Anchor:** `corpus/services/storage.md:46` (ADDED)
  > "Corrected M257x iter-68: the old line named two profiles the platform does not have — there is no `graphql` profile (`0dab54d` renamed it `core`) and **there was never any `storage` profile at all**"
- **Why it is false:** `git show 2adcf71:docker-compose.yml` line **218** reads
  `profiles: [graphql, storage, all]` on the `storage` service; the same at `ef32d4c` (`:119`).
  `git log -S"storage, all" -- docker-compose.yml` names `99cf733 feat: add storage service` as the
  commit that introduced it and `0dab54d` as the one that removed it. The half of the correction that was
  true ("there is no `graphql` profile") was extended to a second clause that is false — the exact
  over-correction pattern §6 of the protocol warns about. It also contradicts this repair's own
  `service_taxonomy.md:429-433`: *"Retired tokens … `graphql` …, plus `cms`, `jobsimulation`,
  `roadrunner` and **`storage`**, are no longer profiles"* (i.e. they *were*).
- **Confidence:** high

### B7 — "`STORAGE_RPC_ADDR` is read by `main.go`" at v1.367.0 — false, and contradicts the sentence next to it
- **Anchors (all ADDED):** `corpus/services/storage.md:26` (consumer cell),
  `corpus/architecture/platform-migration-status.md:76` (storage row: "…is read by `main.go` and by
  **none** of the three `cmd/` tools **at this ref**"), `corpus/ops/platform-alignment.md:1203`.
- **Why it is false:** at `app` `9d00a313` v1.367.0 the identifier has **zero** read sites.
  `git grep -n STORAGE_RPC_ADDR 9d00a313 -- '*.go'` returns three hits, **all comments**:
  `internal/jobsimwiring/wiring.go:101`, `internal/storagens/callsites_test.go:189`, and
  `main.go:451` — which literally reads *"…the standalone service takes no traffic and STORAGE_RPC_ADDR is
  gone."* There is no `os.Getenv("STORAGE_RPC_ADDR")` anywhere. (Positive control: the same query at
  `b948604` returns the 6 real read sites the *mid-fold* paragraph cites, so the pipeline works.)
  As written the cell asserts the consumer side is still live, under a heading that says
  **"Consumer side — also removed"**, and two lines below a sentence saying *"there is no longer a client
  to build."* Whether the intent was "read by main.go **no longer**", the published text says the
  opposite of the measurement and of its own surrounding claim.
- **Confidence:** high

### B8 — `architecture_overview.md`'s Tier-1 bullet list is relabelled `core` while still listing CMS, Storage and Roadrunner
- **Anchor:** `corpus/architecture/architecture_overview.md:12` (MODIFIED) — "The set below is the **local `core` profile** (renamed from `graphql` at platform `0dab54d`) — what runs after a normal `make up`." Members at `:16` **CMS**, `:17` **Storage**, `:18-19` **Roadrunner**: *"**orphaned husk** — the container still starts, but nothing calls it"*.
- **Why it is false:** `core` = `postgresql redis sentinel backend gotenberg` (executed). `cms` and
  `roadrunner` have no compose service at all (`d11a403`); `storage` is `profiles: [storage-legacy]`
  (`docker-compose.yml:134`). The repair fixed exactly one of the four stale bullets
  (`:15` Jobsimulation) and left the other three inside the list it had just re-labelled.
- **Confidence:** high

### B9 — the "Service Tiers" line says `core` and the sentence under it says three husks + six Go services
- **Anchor:** `corpus/architecture/architecture_overview.md:59` (MODIFIED) → `:60` (context)
  > "**plus the three unfederated husks the default profile still starts**: `jobsimulation` (`docker-compose.yml:83`), `cms` (`:144`) and `roadrunner` (`:281`). Messenger when opted in. … So a bare `make up` gives you **six Go services**, not three."
- **Why it is false:** `core` starts **two** Go services (`sentinel`, `backend`) plus the third-party
  `gotenberg`. `docker-compose.yml:83` is `- "8083:8083"` inside `backend`'s ports; `:144` is
  `- "8080:8080"` inside `customerio-sync`; `:281` is past EOF (the file is 271 lines). Three dead
  anchors and a refuted count on a line the repair re-headed.
- **Confidence:** high

### B10 — the removal is attributed to the wrong commit: "until platform `2adcf71`"
- **Anchor:** `corpus/architecture/architecture_overview.md:181` (MODIFIED)
  > "they are merged into `app` … **and**, until platform `2adcf71`, still started by the default profile as unfederated husks."
- **Why it is false:** `2adcf71` is precisely the ref at which they **were** still started —
  `git show 2adcf71:docker-compose.yml` has `jobsimulation` `:140`, `cms` `:187`, `roadrunner` `:309`,
  each `profiles: [graphql, …]`. The removal is `d11a403` (merged `ef32d4c`, 2026-08-03), which every
  other repaired site in this diff names correctly (`service_taxonomy.md:90`, the map's roadrunner row,
  `dependency_map.md:15-18`). The next context line, `:182`, then still says *"Only Chronos,
  Intelligence, Skiller and Skillpath are genuinely out of local orchestration"*, which is false for the
  same reason.
- **Confidence:** high

### B11 — the "Archived / merged" table and the "Default local development set" table still assert the husks run
- **Anchors:** `corpus/architecture/architecture_overview.md:177` ("but three of these still start locally"),
  `:189` Jobsimulation ("**Container still starts locally** (`docker-compose.yml:83`, default profile) as
  an unfederated husk"), `:190` CMS ("**Container still starts locally** (`docker-compose.yml:144`,
  default profile) and still answers `messenger`'s `CMS_RPC_ADDR` until **M809**"), plus `:145`
  ("Default local development set (started by `make up`, profile `graphql`)") and its rows `:150` CMS,
  `:152` Jobsimulation, `:153` Storage, `:154` Roadrunner.
- **Why it is false:** all four services are out of the default selection at `0dab54d` (executed), cms and
  jobsimulation have no compose service, and `CMS_RPC_ADDR` = `http://backend:8083`
  (`docker-compose.yml:174`) — M809 landed. The repair rewrote the **Roadrunner** row of that same table
  (`:191`, "Gone locally, orphaned in prod") and left its two siblings asserting the opposite, three
  lines away.
- **Confidence:** high

### B12 — `dependency_map.md`'s Backend row still claims a `depends_on` edge onto the cms husk, with a dead anchor
- **Anchor:** `corpus/architecture/dependency_map.md:13`
  > "Sentinel, **cms**, Storage (compose `depends_on`, `docker-compose.yml:70-80` — yes, the monolith still has a startup edge onto the cms **husk**)"
- **Why it is false:** at `0dab54d`, `backend`'s `depends_on` is `redis`/`postgresql`/`sentinel`
  (`docker-compose.yml:92-99`), and `:93` is the comment *"# storage removed at v9.0: served in-process by
  this container now."* Lines `:70-80` are `SERVICE_NAME`, `SKILLER_STREAM`, `SKILLPATH_STREAM`, the
  storage-in-app comment and `AWS_REGION` — backend's `environment:` block, not `depends_on`.
  The row directly contradicts `:15` and `:18` of the same table, both repaired in this diff to
  "**no container** at platform `0dab54d`".
- **Confidence:** high

### B13 — a MODIFIED row cites `docker-compose.yml:213-217` for storage's `depends_on`; that range is inside studio-desk
- **Anchor:** `corpus/architecture/dependency_map.md:19` (MODIFIED)
  > "| **Storage** | Postgres, Redis — **`depends_on` only** (`docker-compose.yml:213-217`, both `service_healthy`)"
- **Why it is false:** storage's `depends_on` is at `docker-compose.yml:126-130`. Lines `213-217` are
  `- CLERK_SIGN_IN_URL=…`, `- DEFAULT_MODEL=gpt-4o`, `- ENVIRONMENT=development`, `- FRONTEND_PORT=9100`,
  `- NODE_ENV=development` — the **studio-desk** environment block (`studio-desk` spans `:197-226`).
  This is the exact failure mode assertion **F** in `platform-migration-status.md` §4 was built to catch
  ("a citation slides into the neighbouring service — still resolving, still non-blank, silently about
  something else"); F only guards the map file, not this one.
- **Confidence:** high

### B14 — `dependency_map.md`'s Studio-Desk row claims a `cms` dependency and cites past EOF
- **Anchor:** `corpus/architecture/dependency_map.md:24`
  > "Compose `depends_on` is **`backend` + `cms`** (`docker-compose.yml:337-341`), not `graphql`"
- **Why it is false:** `studio-desk`'s `depends_on` at `0dab54d` is `backend` alone
  (`docker-compose.yml:223-225`); there is no `cms` service. `docker-compose.yml` is **271** lines, so
  `:337-341` resolves to nothing at all.
- **Confidence:** high

### B15 — `shared_libraries.md`'s proto row contradicts the colony row two lines above it
- **Anchor:** `corpus/architecture/shared_libraries.md:69`
  > "The husk repos **still carry their own `go.mod`** — `repos.yml:14-19` still clones them and `docker-compose.yml:83,144` still builds them"
- **Why it is false:** `repos.yml` @ `0dab54d` has 6 entries and none is cms/jobsimulation
  (`:11 app`, `:15 sentinel`, `:18 storage`, `:21 messenger`, `:26 next-web-app`, `:29 studio-desk`);
  `:14-19` spans `schema: public`/`sentinel`/`storage`. `docker-compose.yml:83` is a `backend` env line
  and `:144` a `customerio-sync` port. Line `:41` — the colony row, MODIFIED in this diff — says the
  opposite explicitly: *"`d11a403` deleted both from compose and from `repos.yml`, so at platform
  `0dab54d` the split is **two-way, not three**."* Two rows of one table, two incompatible topologies.
- **Confidence:** high

### B16 — `jobsimulation.md`'s banner says both "the husk still starts" and "there is no husk container left"
- **Anchor:** `corpus/services/jobsimulation.md:11-14`
  > "**⚠️ But locally the husk still starts.** `docker-compose.yml:83` @ platform `2adcf71` still defines a `jobsimulation` service **in the default `graphql` profile**, and `repos.yml:17-19` still lists the repo … State: `running_but_unfederated`"
  versus `:34-35` (ADDED, 21 lines later, same blockquote): "there is no husk container left to reach".
- **Why it is false:** no `jobsimulation` compose service and no `repos.yml` entry at `0dab54d`; the map's
  own row (`platform-migration-status.md:73`) now reads `decommissioned | no`. The `@ 2adcf71` pin does
  **not** exempt it under the corpus's own §5 rule 33, because the block asserts currency in the present
  tense ("still starts", "still lists", "State: `running_but_unfederated`") — rule 33's third clause is
  explicit that a currency-asserting block "cannot be pinned into silence". `repos.yml:17-19` carries no
  pin at all.
- **Confidence:** high

### B17 — the roadrunner row asserts `d11a403` deleted the `repos.yml` entry AND that it was already gone
- **Anchor:** `corpus/architecture/platform-migration-status.md:74` (ADDED)
  > "**Compose service and `repos.yml` entry both deleted by `d11a403`** — whose message states the clone entry *"was already gone, so the `../roadrunner` build context could no longer resolve"*, i.e. the service had been unbuildable, not merely legacy."
- **Why it is false:** the two halves cannot both hold, and git settles it against the second:
  `git show 2adcf71:repos.yml` (d11a403's parent) contains
  `- name: roadrunner / type: go / migrations: false # legacy — folded into app; backend calls Judge0 directly`
  at `:29-31`, and `git show d11a403 -- repos.yml` shows d11a403 deleting it. So the clone entry was
  **present** immediately before `d11a403`, `../roadrunner` **would** resolve after `make init`, and the
  derived conclusion — "the service had been unbuildable, not merely legacy" — is false. The corpus is
  transmitting an inaccurate platform commit message as adjudicated fact, which §6's own new
  "the platform's CONFIG is its documentation of record; its NARRATIVE docs are not" section (added in
  this very diff) exists to prevent — a commit *message* is narrative.
- **Confidence:** medium (booked as a BLOCKER: the cell is self-contradictory and its stated inference is
  refuted by the parent commit; I hesitated only because the quotation itself is faithful)

### B18 — the fence-coverage table's "28 citations" is refuted by the fence's own printed reach
- **Anchor:** `corpus/architecture/platform-migration-status.md:185` (ADDED)
  > "| every other `file:line` — terraform, `repos.yml`, `common.yml`, `app/…` (28 citations) | **resolution + range only.** …"
- **Why it is false:** run on this tree,
  `python3 .agentspace/rosetta-extensions/stack-core/platform_alignment_guard.py corpus/architecture/platform-migration-status.md stack-demo/platform/repos.yml`
  prints: *"assertion F resolved 74 citation(s) — 20 subject-checked, **53 range-only**, 1 outside any
  service block; 0 unresolvable"*. The compose half ("20 of them", `:184`) matches; the non-compose half
  is 53, not 28 — the storage/messenger rewrites in this same commit roughly doubled it. The section
  three lines below insists *"F's reach is printed on **every** run"*, so this number was one command
  from being right.
- **Confidence:** medium-high

### B19 — the illustrative line-drift citation is itself drifted: `:643` is a blank line
- **Anchor:** `corpus/architecture/platform-migration-status.md:71` (ADDED)
  > "the first four stood at `:581`/`:612`/`:643`/`:1043` at `b948604` v1.366.0 … which is what one working day costs a line-number citation"
- **Why it is false:** at `b948604`, `skillpath.NewSessionManager` is at **`main.go:642`**; `:643` is an
  empty line and `:644` opens a comment block. (`:581` skiller, `:612` jobsimwiring.Wire, `:1043`
  appcms.Wire all verified correct, as are all four at `5ba17044` and all six at `9d00a313`.) One wrong
  anchor in five is small, but the sentence's entire subject is the accuracy of line-number citations,
  and the sibling `anchor_construct_guard` is documented (§5 rule 34) as catching precisely the
  blank-line case — so this one should not have survived.
- **Confidence:** medium (booked as BLOCKER per the tie-break rule; it is a dead anchor in a claim about
  dead anchors)

### B20 — the `redis` census row's anchor was broken by the shift the row above it documents
- **Anchor:** `corpus/architecture/platform-migration-status.md:96` — "| `redis` | … | `common.yml:20`. Streams transport for the Watermill pub/sub |"
- **Why it is false:** `redis:` is at `common.yml:24`; `:20` is a comment line inside `postgresql`'s
  healthcheck. The shift was introduced by `6060315` (the 4-line `start_period` comment at
  `common.yml:19-22`) — the *immediately preceding* row, `:95`, was rewritten by this repair specifically
  to record that commit and cites `common.yml:22` correctly. The repair had the delta in hand and
  re-pointed the row above but not the row below.
- **Confidence:** medium (the line itself is unmodified; I book it because the adjacent repaired line
  proves the author was holding the exact shift that invalidates it)

### B21 — the demo UI-tier table names profiles neither frontend belongs to
- **Anchor:** `corpus/ops/demo/frontend-tier.md:46-47` (both MODIFIED)
  > "next-web-app … in the demo's **`core`** profile" / "studio-desk … in the **`frontend`**/`studio-desk` profiles"
- **Why it is false:** at `0dab54d`, `next-web-app` is `profiles: [frontend, all]`
  (`docker-compose.yml:253`) and `studio-desk` is `profiles: [studio-desk, all]` (`:226`). Executed:
  `--profile core` selects neither frontend. Each row names the *other* service's profile, and the demo
  starts them by explicit `--services` naming anyway (`up-injected.sh:2180` passes
  `--profile "$COMPOSE_PROFILE"` plus `STACK_SERVICES`), so "in the `core` profile" is wrong twice over.
- **Confidence:** medium-high

### B22 — the dev-up skill's expected-container table still lists four containers the `core` profile cannot start
- **Anchor:** `.claude/skills/dev-up/reference.md:46` (MODIFIED heading, "Expected service set (default
  `core` profile, main dev stack)") over the table at `:50-58`, which lists `anthropos-cms-1`,
  `anthropos-jobsimulation-1`, `anthropos-storage-1`, `anthropos-roadrunner-1`, and `:60-68` which
  re-derives the table "from the platform clone at origin `2adcf71`" and calls cms/jobsim/roadrunner
  "HUSKS … the containers survive only as the rollback path until M810".
- **Why it is false:** `core` starts 5 containers; `cms`/`jobsimulation`/`roadrunner` do not exist in
  compose and `storage` is `storage-legacy`-only. This is the operational doc an agent reads while
  bringing a stack up: it will report a healthy stack as 4-containers-short and go debugging. Line `:38`
  of the same file still prints `make up # build from local code + start (graphql profile)`.
- **Confidence:** high

---

## Minors

- `CLAUDE.md:191`, `corpus/ops/platform-alignment.md:1178`, `corpus/ops/platform_repo.md:80` cite
  `docker-compose.yml:130-133` for a comment that begins at `:131` (`:130` is
  `condition: service_healthy`); `storage.md:24` and `platform-migration-status.md:76` use `:131-133`
  correctly — the repair carries both forms.
- `corpus/architecture/platform-migration-status.md:71` cites `repos.yml:10-13` for `app`; the block is
  `:11-14`, so `:10` is a comment and the cited `schema: public` (`:14`) falls outside the range.
- `corpus/architecture/platform-migration-status.md:76` cites `docker-compose.yml:82` for
  "`STORAGE_S3_BUCKET`/`STORAGE_S3_PUBLIC_BUCKET`"; the second is at `:83`.
- `storage.md:26` and `platform-migration-status.md:76`: "bucket names are constants in
  `app/internal/storage/service.go:22`, `:24`" — those constants hold the **env-var names**
  (`EnvBucket = "STORAGE_S3_BUCKET"`), not bucket names; the bucket values come from compose `:82-83`.
- `corpus/services/README.md:21`: "still start CONTAINERS locally in the default then-default profile" —
  leftover "the default" from the edit.
- `CLAUDE.md:194` dates the v9.0 program "2026-08-04 (platform `0dab54d` / `app` `9d00a313`)", while
  `0dab54d` is committed 2026-08-03 12:31 UTC; `platform-migration-status.md:208` says "(2026-08-03)" for
  the same sha. (The `app`/terraform halves genuinely are 2026-08-04.)
- Unpinned cross-repo anchors: `CLAUDE.md:196` cites `storage/terraform/main.tf:38` and
  `messenger/terraform/main.tf:29` with no storage/messenger ref. Those resolve only at `63bffc8` /
  `a0ec933` (origin/main). At the ground-truth clone checkouts (`4ce8ece5` / `fa47850d`) both files read
  `service_desired_count = 1` at `:19`. `storage.md:22` and `messenger.md:12` name the refs in the
  banner but not in the cell, which under the repair's own §5 rule 33 ("a pin's scope is the claim's own
  block — a markdown CELL") does not date the cell.
- `platform-migration-status.md:74` cites `jobsimwiring/wiring.go:118` while `:71` cites `:123` for the
  same construct; each is right at its own ref (`b948604` / `9d00a313`) but the roadrunner cell names no
  `app` ref, so a reader at the map's own stated ref lands on a comment line.
- `corpus/ops/platform-alignment.md:1089-1092` (rule 30) says "`docker-compose.yml` at platform `0dab54d`
  gives `postgresql`, `redis` and `sentinel` no `profiles:` key at all" — two of the three are in
  `common.yml`, which the corollary two lines down states correctly.

---

## Positively cleared (audited zeros)

**ENUMERATED** (predicate re-derived from source, cardinality compared before any arithmetic):

- **Profile membership, all 13 tokens.** Executed `docker compose --profile X config --services` for
  `core, backend, all, storage-legacy, customerio-sync, frontend, studio-desk, messenger, graphql, cms,
  jobsimulation, roadrunner, storage`. Every claim in the repaired profile tables
  (`CLAUDE.md:301-310`, `service_taxonomy.md:427-438`, `platform_repo.md:75-84`, `run_guide.md:82,97-100`,
  `setup_guide.md:500-506`) is exactly right, including the two non-obvious ones: `frontend` /
  `studio-desk` / `messenger` **exit 1** with `service "X" depends on undefined service "backend":
  invalid compose project`, and the five retired tokens **exit 0** selecting exactly
  `postgresql redis sentinel`. The remedial invocations are also correct — `--profile core --profile
  studio-desk|messenger|frontend` all exit 0 with the expected 6 services.
- **`docker-compose.yml` service set and every per-service profile line** — 8 services, `:100`, `:134`,
  `:154`, `:195`, `:226`, `:253`, `:268` all verified against `backend.md:116`, `storage.md:46`,
  `customerio-sync.md:14`, `messenger.md:36`, `gotenberg.md:14`, `service_taxonomy.md`.
- **`repos.yml` = 6 entries** (app, sentinel, storage, messenger, next-web-app, studio-desk) — clears
  `cms.md:12`, `dependency_map.md:33`, `platform_repo.md:11` ("6 sibling repos"), and every
  `repos.yml:15-17 / 18-20 / 21-23 / 26-28 / 29-31` anchor in the map.
- **All four messenger RPC addresses** — `docker-compose.yml:173/:174/:176/:183` all
  `http://backend:8083`, under the comment at `:171-172`; and `messenger/cmd/root.go:120/125/130/135/140`
  genuinely reads all four. Clears `backend.md:191`, `dependency_map.md:21`, `cms.md:33-35`,
  `jobsimulation.md:31-33`, `messenger.md:125-126`, `service_taxonomy.md:369`.
- **`STORAGE_RPC_ADDR` / `ROADRUNNER_RPC_ADDR` = 0 occurrences** across `docker-compose.yml`,
  `common.yml`, `.env_example` (positive control: `STORAGE_S3_BUCKET` found at `:82`). Clears the config
  side of `storage.md:25`, `roadrunner.md:39-40,103`, `shared_libraries.md:79`.
- **The 31 demo env knobs.** Derived from the three knob scripts the guard names
  (`up-injected.sh`, `ensure-clones.sh`, `ant-academy.sh`) → 32 names, minus the one documented
  `_INTERNAL` carve-out (`DEMO_DISK_AVAIL_KB`, justified in `demo_knob_guard.py`) → **31**, set-identical
  to the 31 rows in `demo-up-defaults.md`. The 30→31 bump is `DEMO_ALLOW_MISSING_HOST_MOUNTS`.
- **All 28 re-pointed `up-injected.sh` line anchors** in `demo-up-defaults.md` resolve to the correct
  construct (`:221 DEMO_NO_UI`, `:252/:253/:255` stories, `:2136` the new mount pre-flight, `:2670`
  verify, …). This is the cleanest part of the repair.
- **The map's state vocabulary = 8 tokens** (`live-standalone`, `merged-into-app`,
  `running_but_unfederated`, `mid-fold`, `decommissioned`, `net-new`, `external`, `library`) — clears the
  "eight" in `platform-migration-status.md:169` and `platform-alignment.md:1296`.
- **`app/studio/**` is untracked in the `app` repo** — `git ls-files studio` = **0**, and
  `git show b948604:studio/services/ai.py` → *"exists on disk, but not in 'b948604'"*. The new blockquote
  at `external_services.md:544-551` is correct.
- **colony / proto pins** from each `go.mod`: app+messenger `v0.35.2`/`v1.210.0`, sentinel `v0.34.3`,
  storage `v0.34.3`, cms `v0.35.1`, jobsimulation `v0.35.1`, roadrunner `v0.34.3` — the repaired
  two-way-split claim at `shared_libraries.md:41` is correct as scoped.
- **All six terraform `service_desired_count` citations**: `app:44 =1`, `sentinel:19 =1`,
  `roadrunner:19 =1`, `cms:39 =0`, `jobsimulation:40 =0`, and `storage:38 =0` / `messenger:29 =0` at the
  named refs, with the `:29-33`/`:35-37` and `:27-28` in-comment rationales verified verbatim.
- **`d11a403`'s restored-env list** — `JUDGE0_BASE_URL`, `DIRECTUS_PUBLIC_BASE_ADDR`,
  `REDIS_WORKER_INDEX`, LiveKit, Chime, `$HOME/.aws/credentials` — verified against the commit message
  and against `docker-compose.yml:53,59,68,60-62,49-50,91`. Clears `platform-migration-status.md:212-216`.
- **All `app/main.go` citations at the ref each names**: `b948604` `:743-746`, `:766-779`, `:770`,
  `:774`, `:778`, `:824-826`, `:980-982`, `:1185-1228`, `:1187`, `:1188`, `:1196`, `:1204`,
  `:1212-1214`, `:1228`, `:1205-1211`; `9d00a313` `:15`, `:61`, `:62`, `:471`, `:472`, `:494`, `:637`,
  `:668`, `:698`, `:1048`, `:1099`, `:1330-1340`, `:1362-1365`, `:1387`, `:1423`; `5ba17044`
  `:573/:604/:634/:1034`; `jobsimwiring/wiring.go:118` @ `b948604` and `:123` @ `9d00a313`;
  `internal/storage/service.go:22,24`. **Every one lands on the named construct except `:643`** (B19).
- **The compose-side citations in the repaired docs**: `:28`, `:53`, `:59`, `:82`, `:100`, `:134`,
  `:136-138`, `:154`, `:156`, `:173-183`, `:195`, `:197`, `:204`, `:220`, `:226`, `:228`, `:236`,
  `:245`, `:253`, `:255-256`, `:268` — all correct.
- **`external_services.md:542`** (the Anthropic-Direct provider row) — the `:541→:542` half of the
  re-point **is** correct; the `:545→:546` and `:569→:570` halves are not (B3/B4).
- **`service_taxonomy.md:308-317`** is the passage `external_services.md:279` says it is.
- **`repos.yml:18-23` @ `ef32d4c`** = storage + messenger, both `migrations: false` — clears the §5
  "the signal was already dead when it was written" claim.
- **`platform_alignment_guard.py` runs GREEN** on this tree in both directions (A/B with a real
  `repos.yml`), so the fenced membership claims are genuinely fenced. The unfenced prose around them is
  where B1/B8–B16 all live — which is exactly what the repair's own new rule 27 predicts.

**RE-DERIVED only** (weakest clearance — arithmetic recomputed, set taken from the document):
the "3 → 1 subgraph" step attributed to `graphql-wundergraph@915da06`; the "4 live ways out of the EU"
enumeration; the "23 jobsim run-state tables"; the "≥42,790 skills / ≥22,470 job roles" figures. None of
these was re-derived from source in this pass.

---

## What I could not settle

1. **Whether `service_desired_count = 0` is live in production.** I read the terraform declarations, not
   AWS. Every "prod — stopped" cell rests on a declaration plus an unverified assumption that the apply
   ran. The map itself labels the prod column "prose-under-review", which is the honest posture.
2. **The demo's *injected* compose topology.** B21 is graded against the platform compose at `0dab54d`;
   `docker-compose.injected.yml` is generated per bring-up and no demo stack was up. A live `demo-N`
   plus `docker compose -p demo-N config --profiles` would settle whether the injected override moves
   either frontend into `core`.
3. **`platform-migration-status.md:74`'s appeal to infrastructure's `services.tf`.** The
   `infrastructure` repo is not among the ground-truth clones, so the roadrunner prod contradiction stays
   recorded-not-resolved, exactly as the row says.
4. **Whether "28 citations" (B18) was correct when written.** I can only measure HEAD, where the fence
   prints 53. If it was true of an intermediate tree, the row is stale rather than wrong — the finding
   stands either way, since the number describes the committed file.
5. **The historical/meta counts in `platform-alignment.md` §5** (the 43–48 % recall figures, "81 fresh
   sites across 21 files", "8 dead citations on first run", "−72 over ten readings", the 64→5 / 23→1 /
   21→0 / 92→0 collapses). These describe prior audit runs whose artefacts live under
   `knowledge/plan/**`, which I am barred from reading. Unverifiable by construction from this seat —
   worth noting, because a gate-bearing document's most quotable numbers are the ones no reader of the
   corpus can check.
