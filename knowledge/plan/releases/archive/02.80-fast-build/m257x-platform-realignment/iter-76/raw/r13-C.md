# Seat C — reading #13

Ground-truth shas re-verified at open, all matching the briefing:
`app b948604f` · `platform 0dab54df` · `next-web-app bb3313bc` · `sentinel 88bc5592` ·
`storage 4ce8ece5` · `messenger fa47850d` · `cms ca50c817` · `graphql-wundergraph 60c229f3` ·
`roadrunner 87d8d443` · `jobsimulation 462343b0` · `studio-desk 14a5442a` · `ant-academy 9c3843cd` ·
`.agentspace/rosetta-extensions` @ `1334a4f` (authoring copy, clean tree).

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/architecture/alignment_testing.md` | 521 | yes |
| `corpus/architecture/architecture_overview.md` | 353 | yes |
| `corpus/architecture/platform-migration-status.md` | 259 | yes |
| `corpus/services/sentinel.md` | 166 | yes |
| `corpus/services/clerk-integration.md` | 128 | yes |
| `corpus/services/README.md` | 79 | yes |
| `corpus/services/db-backup.md` | 31 | yes |

Protocol read first: `corpus/ops/platform-alignment.md` §5 (lines 240–1146).

---

## The one measurement most of this report turns on

`stack-demo/platform/docker-compose.yml` @ `0dab54d` is **271 lines** and declares **8** services:
`sentinel` (5) · `backend` (28) · `storage` (102) · `customerio-sync` (136) · `messenger` (156) ·
`studio-desk` (197) · `next-web-app` (228) · `gotenberg` (255), plus `postgresql` (2) and `redis` (24)
in the included `common.yml`. Profiles:

| service | `profiles:` |
|---|---|
| postgresql, redis, sentinel | **none — always-on floor** |
| backend, gotenberg | `[core, backend, all]` |
| storage | `[storage-legacy]` |
| messenger | `[messenger]` |
| customerio-sync | `[customerio-sync, all]` |
| studio-desk | `[studio-desk, all]` |
| next-web-app | `[frontend, all]` |

`Makefile:10` `PROFILE ?= core`; `Makefile:65-66` `make up` = `--profile core up`. So a bare `make up`
starts **5 containers** (postgresql, redis, sentinel, backend, gotenberg). There is **no `graphql`
profile** (the head commit's own subject: *"chore(compose): run without the standalone storage; rename
graphql -> core"*), **no `cms`, `jobsimulation` or `roadrunner` service**, and `storage` is not started
by default.

`repos.yml` @ `0dab54d` clones **6** repos: app, sentinel, storage, messenger, next-web-app, studio-desk.

---

## Blockers

### B1 — sentinel.md: `AUTHORIZATION_ADDRESS` is in ONE compose block, not three; all three anchors are wrong
- **Anchor:** `corpus/services/sentinel.md:85` — *"Consumed via `AUTHORIZATION_ADDRESS=http://sentinel:8087`, set in exactly **three** compose blocks — `docker-compose.yml:45` (**backend**), `:99` (jobsimulation husk), `:160` (cms husk)"*
  Same claim restated at `:5` — *"(The `cms` and `jobsimulation` husk containers still start and still receive `AUTHORIZATION_ADDRESS=http://sentinel:8087` at `docker-compose.yml:99,160`, but they sit off every request path.)"* — and at `:89` — *"the `cms` and `jobsimulation` husk containers still receive the address"*.
- **Why it is false:** `grep -n AUTHORIZATION_ADDRESS platform/docker-compose.yml` returns **exactly one** hit, `docker-compose.yml:48` (inside `backend`). Positive control in the same pass: `grep -c "PORT=" docker-compose.yml` → 10. There is no `cms` and no `jobsimulation` compose service at `0dab54d`. The three cited lines resolve to unrelated constructs: `:45` = `- .env`, `:99` = `condition: service_started` (inside `backend`'s `depends_on`), `:160` = `ssh: ["default"]` (inside `messenger`'s build args). The stated count, the stated consumers and all three anchors are wrong.
- **Confidence:** high

### B2 — services/README.md: the roadrunner container "still starts locally" — it has no compose service
- **Anchor:** `corpus/services/README.md:33` — *"…but prod terraform still reads `= 1` and **the container still starts locally**"*
- **Why it is false:** there is no `roadrunner` service in `platform/docker-compose.yml` @ `0dab54d` (8 services, enumerated above), and no `roadrunner` entry in `repos.yml`. The cell carries no ref pin, so it grades at the checkout. It also **contradicts line 21 of the same file**, eleven lines above it: *"At platform `0dab54d` they are **gone from compose and from `repos.yml`** as well."* (The prod half of the sentence is correct — `roadrunner/terraform/main.tf:19` = `service_desired_count = 1`, verified.)
- **Confidence:** high

### B3 — platform-migration-status.md: `common.yml:20` is a comment inside postgresql's healthcheck, not redis
- **Anchor:** `corpus/architecture/platform-migration-status.md:96` — *"| `redis` | external | external | no | `common.yml:20`. Streams transport for the Watermill pub/sub |"*
- **Why it is false:** `common.yml:20` is `      # which takes well over the 25s (5s x 5) the retries alone allow once the data` — the second line of the comment block inside **postgresql**'s `healthcheck`. `redis:` is at **`common.yml:24`**. The anchor resolves and is non-blank, so §4's assertion-D/F checks cannot see it (§4 line 185 explicitly puts `common.yml` in the *"resolution + range only"* bucket). Note the row directly above it cites `common.yml:2` and `common.yml:22` and **both are exact**, which is what makes this one read as verified.
- **Confidence:** high

### B4 — platform-migration-status.md: "`STORAGE_RPC_ADDR` is read by `main.go`" is false at the ref the cell itself names
- **Anchor:** `corpus/architecture/platform-migration-status.md:76` (storage row) — *"`STORAGE_RPC_ADDR` is read by `main.go` and by **none** of the three `cmd/` tools at this ref."*
- **Why it is false:** the cell names its ref in its own first sentence (*"re-derived M257x iter-68 at platform `0dab54d` / `app` `9d00a313` v1.367.0 / `storage` `63bffc8`"*), so `9d00a313` is the graded ref. `git grep -n STORAGE_RPC_ADDR 9d00a313` returns **no read site anywhere in Go source**: the only `main.go` hit is `main.go:451`, a **comment** that says *"…the standalone service takes no traffic and **STORAGE_RPC_ADDR is gone**."* (read with `sed -n '440,475p'` — the surrounding block constructs the in-process managers from `internalstorage.EnvBucket`/`EnvPublicBucket` instead). Positive control at the same ref: `git grep -c STORAGE_S3_BUCKET 9d00a313` → hits in `env_guards_test.go`, `internal/storage/service.go`, etc. The sentence also contradicts its own cell heading two clauses earlier — *"**Consumer side — also removed**"* — and `platform-alignment.md` §5 rule 32, which records that at `9d00a313` the storage fold *"deleted **both**"*. Everything else in this cell verifies exactly (`storage 63bffc8:terraform/main.tf:38` = `= 0`, `:29-33` = the ORDERING comment, `:35-37` = the prevent_destroy note, `main.go:471/:472/:494/:1048`, `internal/storage/service.go:22/:24`, `docker-compose.yml:82/:131-133/:134`, `repos.yml:18-20`), which is exactly why the one false clause reads as verified.
- **Confidence:** high

### B5 — platform-migration-status.md: the skillpath wiring anchor `:643 @ b948604` lands on a blank line
- **Anchor:** `corpus/architecture/platform-migration-status.md:71` (app row) — *"the first four stood at `:581`/`:612`/**`:643`**/`:1043` at `b948604` v1.366.0"*
- **Why it is false:** at the checkout `b948604`, `main.go:643` is **blank**. `skillpath.NewSessionManager` is at **`main.go:642`** (`grep -n "skillpath.NewSessionManager" main.go`; context read at `638,648`). The other three in the same list are exact (`:581` skiller, `:612` `jobsimwiring.Wire`, `:1043` `appcms.Wire`), as are all four at `5ba17044` (`:573`/`:604`/`:634`/`:1034`) and all six at `9d00a313` (`:637`/`:668`/`:698`/`:1099`/`:471`/`:1423`). This is `platform-alignment.md` §5 rule 34's own blank-line case, live in the map rule 34 was written from.
- **Confidence:** high (booked as BLOCKER rather than MINOR because the briefing lists "wrong line anchors that point at the wrong construct" under BLOCKER and this file's own contract is *"Every claim is cited to a sha or a `file:line`"*; I hesitated because it is an off-by-one inside a historical parenthetical)

### B6 — platform-migration-status.md: re-publishes the retracted "60K-skill" figure
- **Anchor:** `corpus/architecture/platform-migration-status.md:94` — *"private Go module — the `NodeID` type only. **Not** the 60K-skill dataset, which lives in `app`'s `public` schema"*
- **Why it is false:** `corpus/architecture/shared_libraries.md:197-213` (`#taxonomy-figures`) formally classifies **"60K skills"** as **UNVERIFIED**, records the measured floor as **42,790** public skills, and gives an explicit writing instruction: *"Say '≥22,470 job roles and ≥42,790 skills (the public subset…)' … **not** write '42,790 skills' as though it were the total."* Root `CLAUDE.md` repeats the retraction. The twin row in my own file set — `architecture_overview.md:204` — states it correctly (*"The long-quoted '60K skills / 18K roles' is not a measurement: 18K is refuted, 60K is unverified"*), so this is a corpus self-contradiction of the cheapest class (§5 rule 21) sitting inside the map that is meant to be the fenced reference.
- **Confidence:** medium (I hesitated because the sentence's *point* — that the library is not the dataset — is true, and "60K-skill" is used as a label rather than a measurement; but it asserts a specific cardinality the corpus has formally retracted)

### B7 — architecture_overview.md: the roadrunner container "still starts", contradicting line 191 of the same file
- **Anchor:** `corpus/architecture/architecture_overview.md:18-19` — *"**Roadrunner**: **orphaned husk** — **the container still starts**, but nothing calls it; `backend` reaches Judge0 directly (`app/internal/jobsimwiring/wiring.go:118`)"*, listed under *"the **local `core` profile** … what runs after a normal `make up`"* (line 12).
- **Why it is false:** no `roadrunner` service exists in `docker-compose.yml` @ `0dab54d`. Line **191** of this same file states the opposite and states it correctly: *"**Gone locally, orphaned in prod:** at platform `0dab54d` there is **no `roadrunner` compose service at all** (deleted by `d11a403`; 8 services remain)"* — and I confirm 8. (The `wiring.go:118` citation itself is correct at the checkout: `jsrunner.NewRunnerManager` is at `internal/jobsimwiring/wiring.go:118` @ `b948604`, and at `:123` @ `9d00a313`, matching the map's differently-pinned cell.)
- **Confidence:** high

### B8 — architecture_overview.md: "a bare `make up` gives you six Go services" + three dead compose anchors, one past EOF
- **Anchor:** `corpus/architecture/architecture_overview.md:60` — *"Backend/App (the monolith), Sentinel, **Storage** + Gotenberg … — **plus the three unfederated husks the default profile still starts**: `jobsimulation` (`docker-compose.yml:83`), `cms` (`:144`) and `roadrunner` (`:281`). … So a bare `make up` gives you **six Go services**, not three."*
- **Why it is false:** four separate errors. (a) `make up` = `--profile core` (`Makefile:10`, `:65-66`), which selects `backend` + `gotenberg` plus the no-`profiles:` floor `sentinel`/`postgresql`/`redis` — **5 containers, 2 Go platform services**, not six. (b) `storage` is `profiles: [storage-legacy]` (`docker-compose.yml:134`) and is not started by `make up` — the compose comment at `:131-133` says so explicitly. (c) There are no `jobsimulation`, `cms` or `roadrunner` services. (d) The anchors are dead: `:83` = `- STORAGE_S3_PUBLIC_BUCKET=…` inside `backend`, `:144` = `- "8080:8080"` inside `customerio-sync`, and **`:281` is past end-of-file** (the file is 271 lines). This is §5 rule 30's exact hazard: a reader who runs the retired token gets exit 0 and three containers.
- **Confidence:** high

### B9 — architecture_overview.md: the default-set heading names the retired `graphql` profile, and its table lists four services that are not in it
- **Anchor:** `corpus/architecture/architecture_overview.md:145` — *"Default local development set (started by `make up`, **profile `graphql`**):"* — followed by the table at `:149-155` listing **CMS**, **Jobsimulation**, **Storage** and **Roadrunner** as members of that set.
- **Why it is false:** the profile is `core` (`Makefile:10`; `docker-compose.yml:100`, `:268`), renamed from `graphql` by the head commit `0dab54d`. CMS, Jobsimulation and Roadrunner have no compose service; Storage is `storage-legacy` only. The heading **contradicts this same file twice** — line 12 (*"the **local `core` profile** (renamed from `graphql` at platform `0dab54d`)"*) and line 59 (*"local development reality, default `core` profile"*) — so the file names both profiles as current within 90 lines.
- **Confidence:** high

### B10 — architecture_overview.md: "three of these still start locally", with two rows citing dead compose anchors and an already-landed M809
- **Anchor:** `corpus/architecture/architecture_overview.md:177` — *"Archived / merged — **but three of these still start locally** (repos still exist):"*, whose own blockquote at `:181` then says *"…**and**, until platform `2adcf71`, still started by the default profile"*; then `:189` — *"**Container still starts locally** (`docker-compose.yml:83`, default profile)"* (Jobsimulation) and `:190` — *"**Container still starts locally** (`docker-compose.yml:144`, default profile) **and still answers `messenger`'s `CMS_RPC_ADDR` until M809**"* (CMS).
- **Why it is false:** none of the three has a compose service at `0dab54d`; `:83` and `:144` are dead anchors (see B8). The M809 re-point has **landed**: `docker-compose.yml:174` sets `CMS_RPC_ADDR=http://backend:8083` and `:176` sets `JOBSIMULATION_RPC_ADDR=http://backend:8083`, with the in-file comment *"cms + jobsimulation are folded into app: all four RPC edges are the one backend mux"*. The heading (present tense) and its own blockquote (past tense, *"until `2adcf71`"*) also contradict each other, and both are wrong about the ref — the removal was `d11a403`/`ef32d4c` (2026-08-03), not `2adcf71`. This is the "corrected banner at the top, stale prose below" pattern §5 names as the dominant miss.
- **Confidence:** high

### B11 — alignment_testing.md: `gate.sh:61` is a comment; the coverage call is at `:69`
- **Anchor:** `corpus/architecture/alignment_testing.md:193` — *"**`gate.sh:61`** calls `alignctl dna coverage --dna … --if-declared`."*
- **Why it is false:** in `rosetta-extensions/clerkenstein/alignment/scripts/gate.sh` (87 lines), the invocation `"$alignctl" dna coverage --dna "$base/$dna" --if-declared` is at **line 69**. Line 61 is `# over a DNA that is missing a consumed endpoint is exactly the thing that let Clerkenstein report 100% while` — a comment. The anchor resolves and is non-blank, and the surrounding comment block *is about* the coverage check, which is precisely why it reads as correct. The sentence's whole function is to point at the call site. Everything else in this row verifies: `--if-declared` downgrades exactly the no-`consumed_surface` case (`alignment/cmd/alignctl/coverage_test.go:42,52,61,70,81,91`), `gate.sh:33` `RUNNER_PKG` default `./cmd/clerkrun`, `:35` `DNA` default `dna/clerk-2.6.0.json`, `:30` `ALIGN_DIR` default `../../alignment`.
- **Confidence:** high

### B12 — alignment_testing.md: the snapshot gene class has SIX operators, not five; the content surface carries FIVE, not four
- **Anchor:** `corpus/architecture/alignment_testing.md:460` — *"a **snapshot-fidelity gene class** (`dna/snapshot.go`) — **five** two-sided operators over a `FidelityProbe`…"*, followed by a closed enumeration of exactly five (`snapshot-row-count`, `snapshot-structural`, `snapshot-referential`, `snapshot-embedding-dim`, `snapshot-public-only`).
  Second site: `:479` — *"**M10** promotes the **content** surface … carrying **four operators** (no `embedding-dim` — content has no vectors)"*.
- **Why it is false:** `rosetta-extensions/stack-seeding/dna/snapshot.go:35-68` declares **six** snapshot operator constants and registers all six in `validSnapshotOperators`: the five named plus **`OpSnapshotCrossSurfaceClosure = "snapshot-cross-surface-closure"`** (`:56`), added at **M23** with the in-code rationale *"CROSS-surface referential closure … the exact gap that produces the empty Assign-AI-Simulation picker"*. And `data-dna.json`'s **content** surface declares **five** operators, not four: `["snapshot-row-count","snapshot-structural","snapshot-referential","snapshot-public-only","snapshot-cross-surface-closure"]`. (The taxonomy surface's *"all five"* at `:478` is correct — it declares exactly the five named.) One un-propagated M23 addition, two false cardinalities in a doc whose own §"the M218 lesson" is about enumerations that stopped being complete.
- **Confidence:** high

### B13 — alignment_testing.md: Clerkenstein described as "its own repo", contradicting this file and `clerkenstein.md`
- **Anchor:** `corpus/architecture/alignment_testing.md:138` — *"**Clerkenstein ships its own** in **its own repo**."* and `:316` — *"The Clerk DNA, goldens, alignment tests, mirror, and runner all live in the **`clerkenstein` repo**, not here."*
- **Why it is false:** `clerkenstein` is a **section of the `rosetta-extensions` monorepo**, not a repo. Measured: `git -C .agentspace/rosetta-extensions ls-files clerkenstein/.github/` returns `clerkenstein/.github/workflows/alignment.yml` — i.e. the clerkenstein tree is tracked *inside* rosetta-extensions. This file itself says so three times (`:320-322` *"the reusable `alignment/` section"*; `:333-337` *"this one sits in a **subdirectory** of the monorepo"*; `:495-500` *"each **mirror** section (e.g. `clerkenstein/`) … Both are sections of the **rosetta-extensions** monorepo"*), and `corpus/services/clerkenstein.md:4-6` states it flatly: *"the `clerkenstein/` **section** of the `rosetta-extensions` monorepo … (It is **not** 'its own git')"*. Booked as a blocker because the *"subdirectory of the monorepo"* fact is exactly what makes the CI workflow inert — the paragraph at `:333-337` is unintelligible against the `:316` framing, and a reader sent to look for a `clerkenstein` repo will not find one.
- **Confidence:** high

---

## Minors

- `corpus/services/README.md:20` — *"And **three of the four** (cms, jobsimulation, roadrunner) still start CONTAINERS locally in the **default then-default profile**"*. Two defects, neither factual-consequence-bearing given the parenthetical: (a) "the four" are named nine lines above as *skiller, skillpath, jobsimulation and cms* — roadrunner is explicitly *"the fifth"*, so the set arithmetic is wrong; (b) *"the default then-default profile"* is a doubled/garbled phrase (an un-finished tense repair).
- `corpus/architecture/platform-migration-status.md:71` — *"`repos.yml:10-13` (`migrations: true`, `schema: public`)"*: off by one. At `0dab54d`, `:10` is a comment, `:11` = `- name: app`, `:13` = `migrations: true`, `:14` = `schema: public` — so the second quoted value sits **outside** the cited range; the correct range is `11-14`. Every sibling `repos.yml` citation in the same table is exact (`:15-17` sentinel, `:18-20` storage, `:21-23` messenger, `:26-28` next-web-app, `:29-31` studio-desk, `:9-10` the rollback comment).
- `corpus/architecture/platform-migration-status.md:111` — *"of which **9** are in `repos.yml`"*. True at the sentence's own stated date (`repos.yml` @ `2adcf71`, 2026-08-01, has 9 entries — verified) so it is graded correct under the ref rule, but the file's header says it was *"Re-measured 2026-08-04"* and the count is now **6**. A reader will not date-scope the "9".
- `corpus/architecture/architecture_overview.md:68` — *"**Shared Libraries**: colony, authn, proto, ai, taxonomy (not deployed, **imported by services**)"*. `authn` is imported by no service — root `CLAUDE.md` and this file's own line 203 (*"now shipped **inside** colony as `colony/authn` (standalone repo is legacy)"*) say so.
- `corpus/architecture/architecture_overview.md:276,284` — the Request Flow blocks both end *"Connect-RPC to … (sentinel, storage)"*. At `platform 0dab54d` storage is not started by `make up` and at `app 9d00a313` object storage is in-process; the edge is real only at the checkout `app b948604`. Genuinely mid-state, so booked minor.
- `corpus/architecture/alignment_testing.md:513` — the Layout block's `cmd/alignctl  run | capture | dna list|diff|validate` omits `dna coverage`, which this same file documents at `:245` and calls *"the check … [that] **binds**"*. The same block omits `internal/canon` (present in the tree).
- `corpus/architecture/alignment_testing.md:325` — link text says `knowledge/alignment.md` but the href is `../services/clerkenstein.md`; the same href is repeated as *"pointed to from [Clerkenstein]"* one line later, so one link renders twice with two different labels.
- `corpus/architecture/alignment_testing.md:3` — *"**Last updated:** 2026-06-06"* on a page carrying M218/M219/M23-era rewrites.
- `corpus/services/clerk-integration.md:73` and `:93` — list **jobsimulation, cms** as live authenticate-then-Sentinel *layers* and as current *dependent repos*, with no merge banner anywhere in the file. True of the frozen repos' source; false of the running platform. Booked minor rather than blocker because no anchor or count is falsified — the falsifiable version of the same drift is B1.
- `corpus/services/sentinel.md:43-48` — *"[`manager`] appears … **only** as a fixture string in sentinel's own tests (`internal/authorization/casbin_test.go`, `internal/rpcsrv/rpc_test.go`)"*. It is also a fixture string at `internal/authorization/manager_test.go:21` (and appears as a logger label at `manager.go:20`). Omitted list member; the load-bearing claim — zero occurrences in `init_policy.sql` — is correct.
- `corpus/services/sentinel.md:30` — the `m` matcher gloss omits the `r.feat == p.feat` conjunct that `casbin.go`'s `m` actually carries (the `m6` gloss on the next row does mention its `feat` match).

---

## Positively cleared (audited zeros)

**ENUMERATED** (predicate re-derived from source independently; cardinality stated before arithmetic):

- **`architecture_overview.md:299-307`, the multi-tenancy split — the one I most expected to break.** I did not re-run the document's arithmetic; I enumerated the set. The schema denominator is **135**, and reaching it required catching a wrong-regex false absence: `grep -c '^\tent\.Schema$'` returns **112**, because **23** schemas are written on one line as `type X struct{ ent.Schema }` (`AIInteraction`, `ActivityEvent`, `Actor`, … `ValidationTestRun`). 112 + 23 = **135** ✓. Numerator: `OrganizationMixin{}` appears in 30 files, of which **`user_resource.go:22` is commented out** (`// OrganizationMixin{},  // We need to work on this`, read in context at `:15-30`) → **29 live** ✓; `org_membership.go:172` (`Membership`) and `organization.go:56` (`Organization`) each declare their **own** `Policy()` and use no mixin — and `Organization`'s `Query` policy really is `rule.FilterSameOrganizations()` (read in full at `:56-100`) → **29 + 2 = 31** ✓. I also independently derived the unpoliced side: **29** `OrganizationMixin` / **7** `OrganizationIDMixin` / **18** with a bare `organization_id` and neither; removing `org_membership.go` (declares a fail-closed `Policy()`) and `skiller_mixins.go` (a mixin file, not one of the 135) gives **16** neither-mixin, and 16 + 7 = **23** — exactly what `:304-305` claims, including its parenthetical that *"16 is the neither-mixin subset of those 23, not the total."* Both cardinalities correct.
- **The five Clerkenstein DNAs**, parsed from `clerkenstein/alignment/dna/*.json`: `clerk-2.6.0` **14 caps / 27 genes**, `clerk-js-5` **6 / 9**, `clerk-multi-1` **5 / 9**, `clerk-deploy-1` **3 / 7**, `clerk-express-1` **5 / 13** — every figure in `alignment_testing.md:254-258` and `:351` matches. And **only `clerk-2.6.0` carries a `consumed_surface`**, exactly as `:201-204` claims.
- **The 12 Clerk webhook event types** (`clerk-integration.md:37`): enumerated from the switch in `app/internal/clerk/events/events.go:165-240` — `user.{created,updated,deleted}` · `organization.{created,deleted,updated}` · `organizationInvitation.{accepted,created,revoked}` · `organizationMembership.{created,deleted,updated}` = **12** ✓.
- **The 8 Clerk locales** (`clerk-integration.md:41`): `apps/web/src/providers/Clerk.provider.tsx:9-18` imports `deDE, enUS, esES, frFR, itIT, jaJP, nlNL, ptPT` = **8** ✓ (apps/hiring imports 7 — the doc's figure is the web app's).
- **The 14 names ever in `repos.yml`** (`platform-migration-status.md:60-62`): `git log -p --follow -- repos.yml | grep '^+.*- name:'` → exactly the 14 listed, in the listed order ✓. And **26 names ever in `docker-compose.yml`** ✓, including all five "pre-history" names the doc calls out (`nats`, `web-app`, `chromedp`, `simulator`, `realtime`).
- **Sentinel's Casbin model** (`sentinel.md:26`): `internal/authorization/casbin.go:13-42` declares **6** request definitions (r, r2–r6), **6** policy definitions (p, p2–p6), **3** role definitions (g, g2, g3) and **6** matchers (m, m2–m6) ✓. The **18 RPC methods** in the `sentinel.md:70-83` table match the receivers in `internal/rpcsrv/rpc.go` one-for-one, with no omissions and no inventions ✓.
- **The four `MembershipRole` values** at `app/internal/data/ent/enum/membership.go:8-15` ✓, and **no `manager` role in `init_policy.sql`** (grep returns only `admin` 42× / `content_creator` 23× / `member` 20× / `candidate` 9×) ✓. `init_policy.sql:63-66` is the taxonomy-write NOTE ✓; `:88-118` is the `content_creator` block ✓ (file is 119 lines).
- **The supergraph is one subgraph** (`architecture_overview.md:3,219,243`; `services/README.md:43`; `platform-migration-status.md:80`): `graphql-wundergraph/supergraph-config-prod.yaml` lists `backend` alone, `schemas/` holds `backend.graphqls` alone, `subgraphs.conf` = `BACKEND=v1.360.0` ✓.
- **The three `.ini` studio configs all pin `azure`** (`architecture_overview.md:253`): exactly three files exist and every `*_MODEL` line in each begins `azure,` ✓.
- **`data-dna.json` has nothing waived** (`alignment_testing.md:484`): 10 surfaces — 4 `seeded-m7a`, 1 `snapshot-seeded-m9b` (taxonomy), 1 `snapshot-seeded-m10` (content), 4 `seeded-m7c`; **zero** `waived` ✓.
- **The toy example's score** (`alignment_testing.md:296-305`): DNA enumerated from `examples/toy/dna.json` — `Add` critical ×3 variants, `Greet` standard ×3. Weights 3/2 → 9 + 6 = 15; one `Greet` gene diverges → 13/15 = **86.67%**, critical 3/3 = **100%**, **5/6 genes** ✓. Gate constants `gateOverall = 80.0` / `gateCritical = 100.0` at `examples/toy/alignment_test.go:21-22` ✓; build tag `//go:build alignment` ✓; goldens laid out as `golden/<Capability>/<variant>.json` ✓.
- **The DNA validation contract** (`alignment_testing.md:103-107`, `:160-167`): `internal/dna/dna.go:21` `idPattern = ^[A-Za-z0-9][A-Za-z0-9_-]*$` ✓, `:24` `maxWeight = 1_000_000` with `:269-270` *"weight must be in 1..1000000"* ✓, `:280` rejects a DNA with *"no critical genes"* ✓, and `compare.Report.CriticalGenes` is carried explicitly with the zero-critical guard commented at `compare.go:43,51` ✓.
- **`clerkenstein/.github/workflows/alignment.yml` is tracked and self-describes as inert at `:10-11`** ✓ — verbatim: *"as a subdir workflow under clerkenstein/.github (not at the monorepo root), this is currently inert…"*. `alignment/` really does hold only `cmd/`, `examples/`, `internal/`, `go.mod`, `README.md` — **no `scripts/`** ✓ (`alignment_testing.md:321-322`).
- **Storage/messenger prod-stop citations at their own pinned refs**: `storage 63bffc8:terraform/main.tf:38` = `service_desired_count = 0`, `:29-33` = the ORDERING comment, `:35-37` = the prevent_destroy/M903 note; `messenger a0ec933:terraform/main.tf:29` = `= 0`, `:27-28` = the rollback-path note. All exact ✓. `messenger/cmd/root.go:120-140` genuinely reads all four RPC addresses ✓ (`:120` `CMS_RPC_ADDR` … `:140` `JOBSIMULATION_RPC_ADDR`).
- **`app/main.go` at `9d00a313`**: all six wiring anchors exact — `:471` `internalstorage.NewManager`, `:637` `skiller.NewSkillerManager`, `:668` `jobsimwiring.Wire`, `:698` `skillpath.NewSessionManager`, `:1099` `appcms.Wire`, `:1423` `msgadapters.Wire`; plus `:15/:61/:62` the three messenger imports and `:1387`/`:1330-1340`/`:1362-1365` the consumer-group block ✓.
- **`STORAGE_RPC_ADDR` occurs 0 times in `docker-compose.yml`, `common.yml`, `.env_example`** ✓ (positive control in the same pass: `REDIS_ADDR` → 2 in `docker-compose.yml`).
- **Sentinel's own fact-sheet**: `go.mod:3` `go 1.26.0` ✓, `Dockerfile:2` and `Dockerfile.dev:2` `golang:1.26-bookworm` ✓, `terraform/locals.tf:4-5` `service_cpu = 256` / `service_memory = 128` ✓ (and `:3 port = 8080`, matching *"binary default is 8080"*), `Makefile:3` `make initdb` hard-codes `postgresql://postgres@localhost:5432/postgres?sslmode=disable` ✓, `local_superadmin_grants.sql` exists ✓, `internal/` holds only `authorization/` + `rpcsrv/` and no `ent/` ✓, `cmd/root.go` ✓, and the *"always on (no `profiles:` declared)"* claim at `:16` is correct ✓.
- **No Clerk/authn in sentinel, storage or messenger** (`clerk-integration.md:74,75,94,95`; `sentinel.md:5`): `git grep -in "clerk\|authn"` returns nothing for all three, under positive controls of 13 / 13 / 42 Go files respectively; messenger also has zero `authoriz` hits and no `depends_on: sentinel` ✓.
- **Clerk SDK versions** (`clerk-integration.md:103`): `app/go.mod:31 @ 5ba17044` = `clerk-sdk-go/v2 v2.7.0` ✓ (still v2.7.0 at the checkout); `@clerk/nextjs ^6.39.2` in `apps/{web,hiring,integration}` **and** `ant-academy/code/package.json:52` ✓; `@clerk/clerk-expo` `~2.6.18` at `next-web-app/apps/mobile/package.json:6` vs `~2.19.36` at `ant-academy/mobile/package.json:18` ✓.
- **`next: ^16.2.7` across all four Next apps** (`architecture_overview.md:48`): `apps/{web,hiring,integration,maintenance}` all `^16.2.7`; `apps/mobile` is Expo and declares no `next` ✓.
- **The AI-routing anchors** (`architecture_overview.md:255-262`): `internal/jobsimulation/ai/ai.go:262-276` is the `case Azure:` block defaulting to `azureClientEu` and swapping to `azureClientUs` under `flag_use_azure_us` ✓; `:129` `isThrottlingError` ✓; `:166` and `:325` its two application sites ✓; `:85-88` the `config.WithRegion("eu-west-1")` pin ✓. `internal/cms/studio/markdownManager.go:11` (`ai/mistral` import) and `:19` (`mistral.NewMistral(nil, os.Getenv("MISTRAL_API_KEY"))`) ✓; called from `studioManager.go:583` ✓.
- **27 service docs** (`services/README.md`, all four groups): `corpus/services/*.md` minus README/TEMPLATE = **27**, and every one is enumerated in the index — no orphan, no invention ✓.
- **Terraform desired-counts**: `roadrunner:19` = 1, `cms:39` = 0, `jobsimulation:40` = 0 (the exact anchors `services/README.md:17-18` cites), `app:44` = 1, `sentinel:19` = 1, `graphql-wundergraph:20` = 1 ✓.
- **`OrgCheckUserPermission`** (`clerk-integration.md:73`) — I initially flagged this as a non-existent RPC (sentinel's method is `OrgCheckPermission`), then opened the file: it is `app`'s own client wrapper at `internal/authorization/sentinel.go:158`, which builds an `OrgCheckPermissionRequest` and calls `a.Client.OrgCheckPermission`. The doc is right and the grep was wrong. **Not a finding.**

**RE-DERIVED** (arithmetic the document itself displays; weakest form of clearance, recorded as such):

- The toy score's 13/15 = 86.7% (the *set* behind it was enumerated from `dna.json`, so this row is stronger than a bare re-derivation, but the percentage itself is the doc's own).
- `platform-migration-status.md:161`'s *"every state is one of the **eight** in §1"* — §1 lists exactly 8 state tokens, and no row in §2 carries `mid-fold`, matching `:37`'s *"No row carries it today."*

---

## What I could not settle

- **`corpus/services/db-backup.md` in its entirety.** No `db-backup` repo is cloned in `stack-demo/` and the service is production-only (correctly stated at `:27` and in `platform-migration-status.md:86`, which pins that claim to `ef32d4c`). The 6-hour cadence, the S3/Azure/Hetzner triple and *"PostgreSQL RDS (all schemas)"* are internally consistent with `architecture_overview.md:349` and `services/README.md:37`, and contradict nothing — but nothing in reach can confirm them. **Settled by:** a clone of `anthropos-work/db-backup` (its cron/schedule config and uploader targets), or the infrastructure repo's `services.tf`.
- **`platform-migration-status.md:110-137`, the 93-repo org census.** Needs the GitHub org API; I have no network vantage. The one component I *could* check — *"9 are in `repos.yml`"* — reproduces at the sentence's own stated date (`2adcf71`, 2026-08-01) and is booked only as a minor for that reason.
- **Repo-archived states** (`jobsimulation` ARCHIVED 2026-07-31, `graphql-wundergraph` 2026-07-30, `skiller` 2026-07-01, `skillpath` 2026-07-31, `intelligence` 2026-04-02, `chronos` NOT archived). Local clones carry no archive bit. **Settled by:** `gh api repos/anthropos-work/<name> --jq .archived`.
- **`platform-migration-status.md:184-185`'s citation counts** (*"`docker-compose.yml` citations — 20 of them"* / *"28 citations"*). My hand count of compose citations in §2 comes to **21**, but the counting rule is not stated — `postgresql`'s `docker-compose.yml:1-2` cites the `include:` header rather than a service block and may legitimately sit outside assertion F's subject-checked set. **Settled by:** running `platform_alignment_guard.py`, which prints F's reach on every run.
- **`clerk-integration.md:52`** (`clerk-sdk-go/v2 jwt.Verify` + `jwks.Client`, 1-minute leeway) and **`:67`** (`AuthRole()` never enforced inside `authn`). `colony` is a private Go module pulled at Docker build and is not cloned anywhere in this tree. **Settled by:** a `colony` clone at the pin each service declares in `go.mod` (sentinel's is `v0.34.3`).
- **Whether `app`'s in-process storage manager is reachable on the *checkout* combination.** `platform 0dab54d` sets no `STORAGE_RPC_ADDR` and starts no `storage`, while `app b948604`'s `internal/jobsimwiring/wiring.go:112` still reads `storage.NewClient(getenv("STORAGE_RPC_ADDR"), …)`. The fold lands at `app 9d00a313`. This is a platform-side ref skew rather than a corpus claim, and I book no finding on it — but any corpus sentence about the storage edge is currently true at one ref and false at the other, which is worth knowing before repairing one.
