---
milestone: M257x
iter: 14
---

# iter-14 — progress

**Type:** tik

## Clause 1 is MET — three consecutive cold cycles, 0 warnings

| cycle | teardown | verdict |
|---|---|---|
| 1 | `--purge` → 0 containers | `{"warnings":0,"green":true,"ts":"2026-08-01T11:43:02Z"}` |
| 2 | `--purge` → 0 containers | `{"warnings":0,"green":true,"ts":"2026-08-01T11:53:04Z"}` |
| 3 | `--purge` → 0 containers | `{"warnings":0,"green":true,"ts":"2026-08-01T12:03:33Z"}` |

Each cycle was a full `demo-down 1 --purge` (data purged, images removed, verified down to **0 containers**)
followed by `demo-up 1` against **platform origin HEAD `2adcf71`**, consuming rext at
**`fast-build-m257x-iter-13`** from origin. ~11 minutes per cycle. No red cycle in between, so the count is
genuinely consecutive.

**The timestamps are load-bearing.** A green verdict left on disk by an earlier cycle reads exactly like a
fresh one; that is precisely M236's green-gate defect, where a stale verdict aged as *fresh* because a UTC
`ts` was parsed as local. Three distinct, monotonically-advancing timestamps ~10 minutes apart, each after a
purge, is what makes "three consecutive" a measurement instead of a claim. Checked in at
`evidence/av-cycle{1,2,3}.json`.

## What the live run proved that iter-13 could not

iter-13 closed on `docker compose config` **RC=0** — the project is *valid*. That is not the same as *the
stack comes up*, and iter-13 had changed four things no static check exercises: the SSR origin, the readiness
probe's port **and path**, the tailscale front list, and the clone pin. All held.

The arithmetic confirms the shape of the change: **15 containers, not 16** — exactly the deleted router. The
re-point removed a service rather than renaming one.

## Two open items, observed rather than assumed

- **The first teardown emitted a `graphql` error.** *"service `graphql` has neither an image nor a build
  context specified"* — because teardown reads the override **left on disk** by iter-12's killed cycle, which
  still named the router. It completed anyway (0 containers, images removed, lock released), and the two
  later teardowns — against overrides regenerated by iter-13's code — were **clean**. The diagnosis was
  confirmed by the next observation, not asserted from the first.
- **`CHECK-M257x-demopatch-pristine`** reproduced identically on all three cycles: `WARN … whole-file sha
  DRIFTED … but the anchor is intact (1x)` for `next-web-studio-url`, `next-web-public-website-url` and
  `next-web-back-to-cockpit`, each then applying. That is `demopatch-spec.md`'s documented **chain rule** and
  self-healing freshness gate — *the anchor is the contract; the whole-file sha is only a baseline*. Not a
  defect. The item stays open on its narrower ground: how the three states are **reported**.

## Close — 2026-08-01

**Outcome:** **Gate clause 1 is MET** — 3 of 3 consecutive cold `demo-down --purge` + `demo-up` cycles reached
`autoverify green:true / 0 warnings` against platform origin HEAD, on the dev host, with rext consumed at a
tag verified on origin. Clause 4 was already claimable, so **2 of the gate's 5 clauses now hold**; clauses 2
(Playthrough suite), 3 (migration-status map) and 5 (KB-fidelity) remain.
**Type:** tik
**Status:** closed-fixed
**Gate:** NOT MET
**Phase 5 grading:** (1) gate-met: n (clause 1 met, but the exit gate is all five clauses; 2/3/5 outstanding)
— (2) triggered-tok: n — (3) re-scope: n (still occurrence 1 of 2) — (4) user-blocker: n — (5) cap-reached: n
(3 tiks this session, cap is 5) — (6) protocol-stop: n — Outcome: continue
**Decisions:** D1–D5 (iter-14/decisions.md)
**Side-deliverables:** none — this iter was a measurement, and the thing being measured was other iters' work.
**Routes carried forward:**
- `FIX-M257x-iter13-freshness-vs-origin` — still open, unchanged, and still worth landing: the clone was
  brought to origin HEAD by hand in iter-12, not by the tooling. Clause 1 is green **because a human checked
  the ref**, which is not a property that survives.
- `DOC-M257x-iter14-corpus-router-drop` — 35 files / ~128 hits (clause 5).
- `CHECK-M257x-demopatch-pristine` — narrowed to reporting, evidence now spans three cold cycles.
**Lessons:**
1. **A static validity check is not a bring-up.** `compose config` RC=0 was necessary and nowhere near
   sufficient; four re-pointed surfaces were only exercised by the live run. Six-plus consecutive iters have
   now found their real signal in the wider/live run.
2. **Timestamp your green.** "Three consecutive greens" is indistinguishable from "one green read three
   times" unless the verdicts are provably distinct. Record the timestamps, not just the colour.
3. **Confirm a diagnosis with the next observation.** The stale-override theory for the first teardown's
   error cost nothing to test — the next teardown either repeats it or does not. It did not.
