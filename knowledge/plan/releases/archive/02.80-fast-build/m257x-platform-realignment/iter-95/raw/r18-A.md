# Seat A — reading #18

**Refs used** (re-verified at open; all match the addendum sheet):
`platform 0c91421d` · `app b948604f` (origin/main `2035f9a4`, fetched and readable) ·
`next-web-app bb3313bc` · `sentinel 88bc5592` · `storage 4ce8ece5` · `messenger fa47850d` ·
`cms ca50c817` · `graphql-wundergraph 60c229f3` · `roadrunner 87d8d443` · `jobsimulation 462343b0` ·
`studio-desk 14a5442a` · `ant-academy 9c3843cd` · `rosetta-extensions (authoring) 6130bfd8`.
Corpus HEAD `1937e1f`, branch `m257x/platform-realignment`, tree clean. Zero writes outside this file;
zero git state changes.

`corpus/ops/platform-alignment.md` (2139 lines) — **§5 read in full, lines 249-1362**, before starting.

## Positive controls

| file | `wc -l` | read in full? |
|---|---|---|
| `corpus/architecture/external_services.md` | 855 | yes |
| `corpus/services/cms.md` | 307 | yes |
| `corpus/services/backend.md` | 306 | yes |
| `corpus/services/ai-labs.md` | 156 | yes |
| `corpus/services/academy-backend.md` | 147 | yes |
| `corpus/services/skiller.md` | 69 | yes |
| `corpus/services/TEMPLATE.md` | 46 | yes |

Search-pipeline controls were run in the same pass as every negative result (§5 rules 1-4):
`SkillPathSessionService -> 0` paired with `SkillerService -> 7`;
`bedrock|boto3` under `app/studio` -> 0 paired with `openai` -> 5 files;
`checkout.session.completed` @ `2035f9a` -> rc 1 paired with `customer.subscription` -> 6 hits;
`git log -S 'protocol: "ws"'` -> empty paired with `-S sse_post` -> 5 commits.
Two zsh failures (`--include=*.go` glob; `$sha:path` parameter-modifier) were caught **because stderr was
read**, re-run under `bash -c`, and their empty first results discarded rather than booked.

---

## Blockers

### B1 — `external_services.md` says `backend`'s compose `environment:` block carries no `DIRECTUS_*`; it carries one, and the same file states the opposite 115 lines above

- **Anchor:** `corpus/architecture/external_services.md:248` — *"The only Directus-related platform config
  is the address `backend` points at — set in the shared `.env`, which `backend` consumes via `env_file:`
  **(its compose `environment:` block carries no `DIRECTUS_*`)**:"* — and the fence it introduces, `:251`:
  *"`# platform/.env` — backend reads these through `env_file: .env`, **NOT its compose environment:
  block**"*, listing **both** `DIRECTUS_BASE_ADDR` and `DIRECTUS_PUBLIC_BASE_ADDR`.
- **Why it is false:** `platform/docker-compose.yml:53` (checkout `0c91421`) sits inside `backend`'s
  `environment:` block (`:46 environment:` … `:95 networks:`) and reads
  `- DIRECTUS_PUBLIC_BASE_ADDR=https://content.anthropos.work`. It is the only `DIRECTUS_*` in that block,
  and it is not zero. Identical at `0dab54d` (`:53`, read via `git show`).
  **The same document already says so correctly** at `:132-136`: *"⚠️ `backend`'s compose `environment:`
  block sets exactly ONE of the pair. At platform `0dab54d` it sets
  `DIRECTUS_PUBLIC_BASE_ADDR=https://content.anthropos.work` (`docker-compose.yml:53`) and no
  `DIRECTUS_BASE_ADDR`."* So `:248`/`:251` contradict `:132-136` of their own file, and the fence comment
  mislabels **both** variables as env-file-only. Consequence for real work: the `--local-content` tooling
  re-points the data plane and deliberately leaves the asset plane on prod; a reader who believes neither
  var is set in compose will look for `DIRECTUS_PUBLIC_BASE_ADDR` in `.env` and never learn why prod asset
  URLs survive an override.
- **Confidence:** high.

### B2 — `backend.md` says the LabSession RPC handler is registered third after Users and Organizations; it is the sixth and last, and the same file enumerates it that way

- **Anchor:** `corpus/services/backend.md:224` — *"**Registered as a third RPC handler in `main.go` after
  Users and Organizations.**"*
- **Why it is false:** `app/main.go` @ `b948604` (the checkout; the sentence names no ref, so it grades
  there) contains exactly **six** `mux.Handle` calls, enumerated independently from source:
  `:1187` `UsersService` · `:1188` `OrganizationsService` · `:1196` `SkillerService` ·
  `:1204` `JobSimulationService` · `:1213` `CMSService` (guarded, `if cmsRPCServer != nil`, `:1212-1214`) ·
  `:1228` `labv1connect.NewLabSessionServiceHandler` wrapped in `authn.HTTPAuthnMiddleware`.
  LabSession is **last of six** — after Skiller, JobSimulation and CMS — not third. This contradicts
  `backend.md:66`, which enumerates the same mux correctly and places LabSession at `:1228`.
- **Why I hesitated:** the sentence sits in a *"Recent Feature Additions"* changelog bullet about PR #896
  and may have been true when that PR landed. It is written in the present tense with no ref-pin, so per
  the instrument it grades at the checkout and is false there, and a reader hunting the handler in
  `main.go` is sent ~40 lines wrong. Booked as a BLOCKER per *"when in doubt, book it."*
- **Confidence:** medium.

### B3 — `cms.md`'s `python-docx` retraction over-reaches: the tree does contain a live `python-docx` dependency, in a shipped tool that pip-installs it on demand

- **Anchor:** `corpus/services/cms.md:148-149` — *"(The only `.docx` in the tree is a filename in an
  authoring guideline and an asset-example README — **no dependency**.)"*
- **Why it is false:** the load-bearing half is right — `python-docx` really is absent from both
  `requirements.txt`, which I read verbatim (9 packages: openai, anthropic, rich, pyyaml, requests, jinja2,
  mistralai, pytest, pytest-asyncio; `diff cms/studio/requirements.txt app/studio/requirements.txt` =>
  identical). The **enumeration** is not. `studio/tools/any2pdf.py` is tracked in the studio-room repo
  (`git ls-files tools/any2pdf.py`), present in both `stack-demo/app/studio/` and `stack-demo/cms/studio/`,
  and holds 24 `docx` occurrences: `.docx` at `:664` (`'.docx': convert_docx_to_pdf`), `:742`, `:752`, and

      80:  # Check if python-docx is installed, if not, install it
      82:      import docx
      83:  except ImportError:
      85:      subprocess.check_call([sys.executable, "-m", "pip", "install", "python-docx"])
      86:      import docx

  So `python-docx` is a **runtime dependency the shipped tool installs on demand** — the strongest
  available counter-example to *"no dependency"*, inside the very tree the sentence ranges over. This is
  §5 rule 20's *"a retraction that over-reached"* and rule 24's *"re-derive the SET, not the sum"*.
- **Why I hesitated:** the actionable half (not in `requirements.txt`) is correct, so a reader who only
  needs the pip manifest is unharmed. A reader auditing the pipeline's supply chain is harmed — an
  undeclared, self-installing dependency is exactly what such an audit exists to find.
- **Confidence:** medium.

---

## Minors

- `corpus/architecture/external_services.md:206-211` — the `rosetta-extensions` anchors are stale against
  the copy the grading rule designates. `DIRECTUS_DATA_CONSUMERS = ("cms", "backend")` is cited at `:84`
  but sits at **`:86`** in `.agentspace/rosetta-extensions` (`6130bfd8`), where `:84` is a comment saying
  the asset plane is deliberately *not* re-pointed; the re-point is cited at `:669-670` but sits at
  **`:698`**, where `:669-670` is a CORS comment. **Both resolve verbatim in the `stack-demo/`
  consumption clone** (`:84`, `:669-670`), so this is clone-selection drift (§5 rule 36, "two clones of
  one repo are one witness"), not a false statement about the tooling — the mechanism described is real,
  and `test_injection.py:1109` in the same block resolves correctly in the authoring copy. Booked MINOR
  for that reason; read strictly against the designated clone, `:669-670` names the wrong construct.
- `corpus/services/cms.md:211` — *"Consistent with the **Data** bullet, `:28-31` above"*; the Data bullet
  is at **`:44-47`**. `:28-31` is the `CMS_RPC_ADDR` / M809 passage.
- `corpus/services/cms.md:229` — *"see the **Studio** bullet, `:52-53` in the banner"*; the Studio bullet
  is at **`:70-71`**. `:52-53` is the RPC bullet's M809 sentence.
- `corpus/services/academy-backend.md:15` — *"(Consistent with `:74-76` below.)"*; `:73-75` is the
  **Certificate minting** bullet. The `NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT` statement it means is at
  `:80-83`. (These three are §5 rule 34's exact prediction — a repair moved a file's line count and its
  intra-corpus self-citations were not re-pointed. All three land ~16-20 lines short.)
- `corpus/architecture/external_services.md:431` presents *"Since cms-in-app the platform compose
  `graphql` service builds from the **production** Dockerfile"* as a verbatim quotation of
  `graphql-wundergraph/CLAUDE.md:39`. The real line reads *"Since the compose stack builds from the
  production Dockerfile, a schema change in `app` does not appear at `:5050` …"*. Substance and line
  number are right; the quotation marks are not earned.
- `corpus/architecture/external_services.md:607` cites `config_template.ini:30-31`. The file is at
  `app/studio/`**`configs/`**`config_template.ini`; `:30-31` there is exactly
  `OPENAI_API_KEY = sk-aaa` / `OPENAI_ENDPOINT = https://api.openai.com/v1/chat/completions`, so only the
  path segment is dropped.
- Production RPC hostname is stated two ways across my file set, and neither is measurable from any clone:
  `skiller.md:19` and `backend.md:112` say `http://backend:8081`; `backend.md:229`, `cms.md:55` and
  `cms.md:196` say `http://backend.internal.anthropos:8081`. Both address the same `backend` RPC mux on
  `:8081`. Every `*_rpc_addr` in every cloned `terraform/variables.tf` (`app`, `cms`, `jobsimulation`,
  `messenger`) is a **defaultless variable**; the values live in `infrastructure`'s `services.tf`, which
  has never been in a clone set. Not booked as a BLOCKER because the two forms attach to *different*
  variables and could legitimately differ — but they are presented as flat facts with no unmeasurability
  note, unlike the M810 rows in the same files, which do carry one.

---

## Positively cleared (audited zeros)

**ENUMERATED** — predicate re-derived from source independently, cardinality stated before any arithmetic:

- **Supergraph subgraph ladder** — `external_services.md:368-371`, `cms.md:59-63`, `backend.md:113-119`.
  `git show <sha>:supergraph-config-prod.yaml | grep '^  - name:'` at each commit **and its parent**:
  `749dc86^` = 5 (backend, skiller, jobsimulation, cms, skillpath) -> `749dc86` = 4 -> `7c17e63` = 3 ->
  `915da06` = **1**. `git show --name-status 915da06` marks `schemas/cms.graphqls` **and**
  `schemas/jobsimulation.graphqls` both `D`. The "3 -> 1, not 2 -> 1" correction and the "the
  jobsimulation subgraph outlived jobsim-in-app" claim are both correct; `915da06`'s own subject line does
  read "2->1", exactly as the corpus warns.
- **Compose service set** — `external_services.md:171-175`/`:297`, `cms.md:27`/`:53`, `backend.md:19`.
  `0c91421` declares **5** services (sentinel, backend, studio-desk, next-web-app, gotenberg) + **2** from
  `include: common.yml` (postgresql, redis) = **7 effective**; `0dab54d` declared **8/10**. `repos.yml`
  holds **4** entries (app, sentinel, next-web-app, studio-desk). `d11a403`'s diff removes the `cms`,
  `jobsimulation` **and** `roadrunner` `repos.yml` entries plus their containers; `838d907` removes
  `storage`/`messenger`/`customerio-sync`.
- **Profile-less floor** — `external_services.md:409`. At `0dab54d` the `profiles:` keys are at
  `:100,134,154,195,226,253,268`; `sentinel` (`:5`) has none, and `postgresql`/`redis` come from
  `common.yml` with none. Exactly **three** services are in every selection, and `graphql` appears in no
  `profiles:` key anywhere. The "exits 0 and starts three" claim holds.
- **`app`'s RPC mux** — `backend.md:66`. Six `mux.Handle` calls at `b948604`; five unconditional plus
  `CMSService` behind `if cmsRPCServer != nil` (`:1212-1214`). `SkillPathSessionService` = **0**
  occurrences in Go (control: `SkillerService` = 7); `app/CLAUDE.md:109` and
  `app/knowledge/architecture.md:28` @ `2035f9a` do still list it, exactly as `backend.md:70` says.
- **Redis stream publishers** — `backend.md:252`. Every publisher constructor in `app` @ `b948604`:
  `main.go:287` (serviceName/`backend`), `:637` `SKILLPATH_STREAM`, `:1039` `CMS_STREAM`,
  `jobsimwiring/wiring.go:127` `AI_USAGE_STREAM`, `:180` `JOBSIMULATION_STREAM` — five, matching the
  document exactly. `git grep SKILLER_STREAM b948604 -- '*.go'` returns **one** line, `main.go:1276`, and
  it is an `AddSubscriber`. The subscribe-but-never-publish asymmetry is real.
- **`ai_readiness_*` = 13 tables** — `backend.md:219`. 13 Ent schema files (`ai_readiness_{cycle,
  diagnose_narrative, email_override, live_snapshot, notification_log, notification_optout,
  recommendation, sim, skill, snapshot, step, text_translation, user_step_progress}.go`) and 13 distinct
  base table names in `terraform/migrations/*.sql` once index/FK suffixes are excluded. The named "four a
  9 omits" are all present.
- **Studio-Room provider arms** — `external_services.md:543`/`:591`/`:612`, `cms.md:95`.
  `grep -hE '_AI_[A-Z]*_MODEL' configs/*.ini | wc -l` = **30**, and every one of the 30 values begins
  `azure`. Three config files exist. `services/ai.py:1-2` imports only `openai`/`anthropic`; the provider
  dict is `:705-708` inside `get_client` at `:704-724`; `AnthropicProvider` spans `:627-664` exactly;
  `OpenAI(api_key=…)` at `:383`. `gen.py:41-53` overrides only `*_API_KEY`/`*_ENDPOINT`. The
  "latent, not live" framing is correct, in both its Course-Builder-live / Studio-Room-latent halves.
- **`gen.py`'s nine arguments** — `cms.md:274-276`. `:484-492` is exactly nine `parser.add_argument` calls,
  and `parse_argument` at `:18-28` calls `parse_known_args` and merges leftovers into the dict. The
  silently-absorbed `--template` is real.
- **AI-provider env-var counts** — `external_services.md:640-645`. Re-derived at `5ba17044` from a clean
  `git archive` extract with `command grep -rho`: `OPENAI_KEY` **5**, `ANTHROPIC_API_KEY` **26**,
  `AZURE_OPENAI_KEY` **13**, `AZURE_OPENAI_ENDPOINT_URL` **13**,
  `OPENAI_ORG_ID`/`AZURE_OPENAI_ENDPOINT`/`AZURE_OPENAI_DEPLOYMENT` **0/0/0**, `OPENAI_API_KEY` **2**.
  Control: `os.Getenv` = 176. Exact on all eight.
- **Taxonomy figures** — `external_services.md:661-665`, `backend.md:9`/`:63`, `skiller.md:43-52`. The
  named manifest reads `public_only: true`, `predicate: org-null`, `captured_at 2026-06-29`, and
  `public.skills 42790` / `public.job_roles 22470` / `public.skill_embeddings 42790` /
  `public.job_role_embeddings 18919`, each with the `organization_id IS NULL` filter recorded per table.
  The "18,919 is the embedding row count transcribed onto the role count" diagnosis is exactly right.
- **Credits cost model** — `ai-labs.md:75-77`. The live `creditCost` map (`cost.go:86-90`) is
  build **5** / refine **1** / translate **1**, matching the corpus. Recorded for the next reader: the
  package doc comment at `cost.go:29` says *"course.refine -> 5 credits per refine turn"* and is **stale
  in `app`** — the corpus took the map, not the comment (§5 rule 24 applied correctly by the corpus, and
  the trap I would have fallen into had I stopped at the grep hit). `purchasePackages` = starter 50 /
  team 200 / scale 500 (`:200-204`); `PricePerCreditUSD = 0.45` (`:142`); `MarginMarkup = 1.40` (`:133`);
  `DefaultSeedBalance = 500` (`:231`).
- **Stripe webhook event set** — `ai-labs.md:109-111`. The top-level switch in
  `internal/web/backend/api/api.go` has exactly two cases: `:319 "customer.created"` and
  `:325-327 "customer.subscription.{created,updated,deleted}"` (the `case`s at `:377`/`:383` belong to the
  inner status switch). `checkout.session.completed` -> **0** hits at `2035f9a`. `/credits/purchase`
  survives only in a stale comment (`cost.go:199`); `web/backend/credits/handler.go:3-4` declares
  `GET /credits/balance` + `GET /credits/transactions` only.
- **Locale sets** — `ai-labs.md:52`, `academy-backend.md:71`. `lab_body.go:48-49`: `"en"` base plus
  `de/es/fr/it/nl/pt` = **EN + 6**.
- **`ContentLanguage` = 8** — `skiller.md:58`. `assignments.graphqls:44-53`: english, italian, spanish,
  french, german, dutch, japanese, portuguese.
- **`STORAGE_RPC_ADDR` residue** — `backend.md:242` (pinned `2035f9a`) and `academy-backend.md:55-58`
  (pinned `9d00a313`). At **both** refs `git grep` returns exactly **3** occurrences, all comments
  (`jobsimwiring/wiring.go:101`, `storagens/callsites_test.go:189`, `main.go:504` / `:451`), **0** reads.
  `git tag --points-at 9d00a313` -> `v1.367.0`, so that pin's version label is right; `main.go:471-472`
  @ `9d00a313` is the two manager constructors, `main.go:524-525` @ `2035f9a` is the same pair after the
  file moved. Two documents, two refs, both resolve.
- **`skiller` domain landing sites** — `skiller.md:13-27`. All six named Ent schema files exist;
  `internal/rpc/skillerrpc/` exists and implements `GetSkill`/`GetSkills`/`SearchSkill`/`MatchSkill`/
  `GetJobRole`; `skiller_taxonomy.graphqls` exists and its `:7` comment confirms
  `categoryTree`/`fullCategoryTree` "stay unported — no consumers" (control: `jobRoleMatch`,
  `similarJobRoles`, `mostPopularSkills`, `jobRoleCount` all present in `queries.graphqls`).

**Anchor-and-construct verified** — file opened and +/-15 lines read; the line names the claimed construct,
not merely exists:

`app/main.go:980-982` (Directus `log.Fatalf`) · `:770` (coursebuilder backend log) · `:743-746`
(`LABS_API_URL` conditional) · `:1205-1211` (the "additive + DORMANT … until the M809 re-point" comment,
correctly flagged stale in three separate files) · `internal/jobsimulation/ai/ai.go:30-33, 130-141,
150-155, 259-289, 264-276, 280-283, 296-302, 326` — all eight ranges land on the construct claimed ·
`internal/jobsimulation/simulator/ai/ai.go:58-59` and `:114-115` · `internal/jobsimulation/ai/ai.go:80` ·
`internal/cms/directus/collections/jobsimulation.go:905`, `:1302`, `:1595-1596` ·
`internal/cms/studio/markdownManager.go:19` @ checkout **and** `:10`/`:30` @ `2035f9a` — both states of the
key-plumbing fix, each correct at its own ref, `external_services.md` grading at the checkout and
`cms.md:215-218` pinning origin/main · `studioManager.go:531`/`:583` · `xlsx.go:13` ·
`internal/skillpath/session.go:205-207` · `internal/web/backend/backend.go:130`, `:315` (Apollo Sandbox at
`/graphql`), `:317` (`/graphql/query`) · `internal/jobsimulation/calls/livekit.go:110,115,120,126,133` ·
`docker-compose.yml:48, 53, 101-109, 110, 119, 135, 151, 160, 183` · `Makefile:10` · `atlas.hcl` ·
`cms/go.mod:3`, `cms/cmd/root.go:77-78`, `cms/Dockerfile:2`/`:23`, `cms/terraform/main.tf:39` in a whole
191-line module · `graphql-wundergraph/terraform/locals.tf:8`, `terraform/main.tf:20` (`= 1`), `:48-49`,
`config.prod.yaml:5`, `Dockerfile.dev:7/18/19-20/23/29`, `schemas/backend.graphqls:4053`/`:4912` with **no**
`type Subscription` (all 6 `Subscription` hits are Stripe/plan field names) ·
`jobsimulation/terraform/main.tf:15-22` and `:24-40` @ `6092c6d2` — the M810 comment block, which the
on-disk checkout `462343b0` does **not** contain, so it resolves only at the ref the claim names, which is
the rule · `rosetta-extensions/stack-injection/tests/test_injection.py:1109` ·
`studio-desk/.env.example:57` (`AI_PROVIDER_CHAIN=azure-openai,openai`).

**Historical/commit claims verified by `git show`:** `2c85211^` has **no** `dockerfile:` key while
`2c85211` has one (so the three-era table at `:419-423` and the caution at `:429` about `git log -S` being
structurally blind to an absent key are both right); `b56d731` still carries `graphql:` at `:22` behind
`profiles: [wundergraph-deprecated]` while `360efd4` has none (control: `backend:` at `:28`) — so
*"`b56d731` does not end the second era"* is correct; `1e8e754:6-8` is `context: ..` /
`dockerfile: graphql-wundergraph/Dockerfile.dev`; `bba862f` is **not** an ancestor of HEAD and lives only
on `remotes/origin/feat/use-web-socket`; `60c229f` is dated 2026-07-30 with the subject quoted; `6efa1d5`
deleted `build-production.yml` with the M810 subject and body quoted; `6092c6d2` is the jobsimulation M810
commit and deletes `module "jobsimulation"`; `0e37771f` is PR #903 dated 2026-06-05; `5ba17044` is tagged
**v1.363.2** (so `ai-labs.md:18` and `academy-backend.md:20` are both right). The historical routing URLs
`http://jobsimulation:8400/query` and `http://cms:8090/query` (`external_services.md:460-461`) are verbatim
from `fc580ed:supergraph-config-compose.yaml`, and `cms.md:113`'s `8090:8090`/`8091:8091`/`PORT=8090`/
`RPC_PORT=8091` anchors land exactly on `:154`/`:155`/`:169`/`:173` @ `2adcf71`.

**Explicitly re-tested rather than assumed:** `app/studio/**` really is untracked in `app`
(`git ls-files studio` -> **0**; `git show HEAD:studio/gen.py` -> *"exists on disk, but not in HEAD"*),
which is the claim at `external_services.md:545-551`.

---

## What I could not settle

- **Whether `graphql-wundergraph` is archived on GitHub (2026-07-30)** — `external_services.md:3`. The
  local clone carries no archive marker; only the GitHub API settles it.
- **Whether `infrastructure/terraform/production/services.tf` still declares `module.cms_euwest1`** —
  `cms.md:78-84`, `backend.md:36-49`, `external_services.md:175`. The corpus says this is unmeasurable and
  **it is right**: `infrastructure` is in no clone set. Both halves of the evidence it reports *are*
  confirmable and confirmed — `cms/terraform/main.tf:39` `= 0` in a whole 191-line module, and `6efa1d5`'s
  ECR-decommissioned subject and body. The "report both, assert neither" posture is correctly held in all
  three files.
- **The production `*_RPC_ADDR` values** (see the last Minor). Every cloned terraform variable is
  defaultless; `infrastructure`'s `services.tf` would settle both the value and which of the two hostnames
  the corpus uses is right.
- **Prod-measured taxonomy counts** at `backend.md:97-99` (43,584 total skills, 22,490 public job roles,
  794 org-private, `skill_embeddings` 43,584, `specializations` 1,447). Prod-only. The independent
  public-only snapshot agrees within the stated drift, which is as far as I can take it.
- **`ai-labs.md:16`'s `knowledge/plan/releases/06.00-shared-purse/` milestone list (M600-M607).** Not
  opened — the HARD BAR forbids `knowledge/plan/**` and I applied it to the `app` clone's copy too. The
  *consequences* the sentence draws (no `checkout.session.completed`, no labs<->credits linkage,
  `/credits/purchase` removed) were all independently verified at `2035f9a` and hold.
- **`anthropos-work/AI-Labs`** (`ai-labs.md:5`) — not in any clone set.
