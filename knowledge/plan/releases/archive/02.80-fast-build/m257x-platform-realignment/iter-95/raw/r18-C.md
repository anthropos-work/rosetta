# Seat C — reading #18

> Provenance: harness blocked this seat's file write; it returned the full report as text and the
> orchestrator persisted it verbatim.

## Compact summary

B1 | corpus/architecture/alignment_testing.md:25 | Claims the alignment harness "lives in rosetta"; it is in rosetta-extensions | high
B2 | corpus/services/messenger.md:17 | flow.go backend subscriber has 21 handlers at :72-104, not 20 at :70-95 | high
BLOCKERS=2 MINORS=10

## Platform baseline derived first (platform 0c91421d)

repos.yml = 4 repos (app, sentinel, next-web-app, studio-desk); compose = sentinel, backend,
studio-desk, next-web-app, gotenberg (186 lines) + postgresql, redis (common.yml); always-on floor
(no profiles: key) = postgresql, redis, sentinel, derived by parsing both files incl. include:;
ZERO *_RPC_ADDR anywhere, only AUTHORIZATION_ADDRESS=http://sentinel:8087 (:48); 838d907 subject =
"chore(compose): drop the storage, messenger and customerio-sync containers", merged 0c91421.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| alignment_testing.md | 537 | yes |
| clerkenstein.md | 370 | yes |
| ai_architecture.md | 302 | yes |
| messenger.md | 184 | yes |
| coursebuilder.md | 140 | yes |
| gotenberg.md | 84 | yes |
| db-backup.md | 31 | yes |
| (scope-read) platform-alignment.md | 2139 | Sec 5 in full |

## B1 — alignment_testing.md:25

"The framework is engine-agnostic and reusable — it lives in rosetta and knows nothing about Clerk."

In rosetta, git ls-files returns 2214 files and ZERO .go files; no alignment/ dir, no alignctl. The
harness is .agentspace/rosetta-extensions/alignment/ (cmd/alignctl, internal/{canon,compare,dna,
outcome,report}, examples/, go.mod = module anthropos.dev/alignment).

Self-contradicted in-file at :3 ("Reference implementation: rosetta-extensions/alignment/"),
:505-506, :510 (the table puts the harness in the rosetta-extensions column), and :515-516 ("rosetta
never contains executable alignment code"). Also contradicts clerkenstein.md:82 and root CLAUDE.md
policy. Materially misleading because it sits in the orientation section at line 25.
Confidence: high

## B2 — messenger.md:17

"(messenger/internal/flow/flow.go:70-95 adds a subscriber on the backend stream with 20 handlers...)"

The call is AddSubscriber("backend", sub.AddHandler(...)) spanning :72-104. Counted line-anchored and
name-independent (^[[:space:]]+pubsub\.EventHandler\():

| ref | live | commented-out |
|---|---|---|
| fa47850d (checkout) | 21 | 1 (:83) |
| e9421c68 (origin/main) | 21 | 1 |

21, not 20, at BOTH refs — the claim names no ref, so no pin rescues it. The window is independently
wrong: :70-95 holds 14 of 21; 7 sit at :96-103.

Provenance, and why it matters: a "...Handler"-suffixed regex returns exactly 20, because flow.go:91
registers h.OrganizationMemberInvitedHanler — a typo in platform source (also organizations.go:54).
I reproduced the corpus's 20 with my own grep before catching it: the wrong-field-name false absence,
almost certainly how "20" was derived. The commented-out handler at :83 is a second trap in the same
block. Confidence: high

## Minors

- alignment_testing.md:528-535 — Layout omits internal/canon (5 packages exist, 4 listed).
- alignment_testing.md:529 — dna.go:20-26 under-reaches; the coverage case body is :27.
- alignment_testing.md:368 — unmeasurable() cited :139-153; function is :139-154.
- clerkenstein.md:21 — names 6 of 12 rext sections.
- clerkenstein.md:276 — "sentinel and storage are still on v0.34.3" true of both go.mods, but storage
  left repos.yml+compose at 838d907. Conclusion survives on sentinel.
- ai_architecture.md:232 — AIModel enum cited :983-990; block is :979-991.
- ai_architecture.md:273 — calculateSkillScore cited :53-64; function is :53-62.
- messenger.md:103 — "four Connect-RPC clients": FIVE constructed (:118,:123,:128,:133,:138) reading
  four distinct addresses (BACKEND_USERS_RPC_ADDR twice). Enumeration correct; phrasing loose.
- coursebuilder.md:114-115 — "~55" / "~25" test files measure 61 / 33.
- coursebuilder.md:82 — setter is called from internal/web/backend/backend.go:259, not that file.

## Positively cleared — ENUMERATED

All five DNA gene/capability counts parsed independently: clerk-2.6.0 27/14, clerk-js-5 9/6,
clerk-multi-1 9/5, clerk-deploy-1 7/3, clerk-express-1 13/5 — source and both files agree. Only
clerk-2.6.0 declares consumed_surface (15 entries). All three covered_by targets exist verbatim.
M218 D15: exactly three genes name identity, all asserting universal-user. snapshot.go:62 =
validSnapshotOperators, exactly six named operators. data-dna.json:410/:439 land exactly on the two
"operators" arrays. All 10 data-dna surfaces: ZERO waived. r.Cookie(...) called nowhere in
clerkenstein (rc=1) — my first positive control also returned zero, so I did not accept it until a
working control (29 func in server.go) plus finding the real mechanism (Set-Cookie strings in the
handshake token, shared/rsa.go:115-129). REDIS_WORKER_INDEX read nowhere in messenger (control
REDIS_STREAMS_INDEX = 3 hits). SERVICE_NAME set nowhere in messenger terraform (control: 15 "name"
keys). checkerEngines "stored and never read" tested as a PREDICATE: exactly 6 occurrences in all of
app; never indexed or ranged (control c.aiManager = 3). Real dispatch is the hardcoded switch at
criterion.go:126. TRUE. gotenbergMimes = exactly 9 entries, prose reproduces the set member-for-
member. ConvertToPDF call sites at 9d00a313 = exactly two. Always-on floor derived, not assumed
(resolving include:). Credits: authoritative creditCost map (cost.go:86-90) = 5/1/1 — corpus correct;
NOTE cost.go:29's package comment says refine = 5, stale PLATFORM text contradicted by its own map.
The corpus matches the code, not the comment. The 0dab54d historical state: repos.yml:21-23,
profiles: [messenger], depends_on: backend, ports 8200/8201, exactly four *_RPC_ADDR all
http://backend:8083.

## A near-miss worth recording — the pin is what saved it

messenger.md:37 cites messenger/terraform/main.tf:29 = service_desired_count = 0. At the checkout
fa47850d that is false TWICE: :29 is container_definitions = <<EOF, and service_desired_count is at
:19 with value 1. I did NOT book it: the enclosing blockquote pins itself at :32-33 ("Re-derived at
... messenger e9421c6"), and at e9421c68 :29 IS service_desired_count = 0. Scale-to-zero landed in
5dd9f80, after the audit checkout. Graded at the ref it names -> true. Same shape, same resolution,
for app/env_guards.go:61 and app/main.go:295 — neither resolves at b948604f, but both resolve exactly
at app origin/main, the ref the file's own "consumer" row names.

## What I could not settle

db-backup.md entirely — no clone under stack-demo/, no compose/repos.yml entry. Cadence, the
S3/Azure/Hetzner triple, "Go", "all schemas" all unfalsifiable here. ai_architecture.md:138 — 42,790 /
18,919 row counts; tables and shape confirmed, row counts need a live DB. The five score VALUES —
denominators enumerated and the M219 fix confirmed in mirror source, but I did not run alignctl.
clerkenstein.md:124's "~4.05 s" ladder and :308-312's timings — need a live stack.
