# Seat G — reading #18 (adversarial diff seat)

## Positive controls

Scope is a **diff**, not a file partition. The control is the diff's own shape, observed directly:

| control | observed |
|---|---|
| `git diff --shortstat 8d6bb6c..HEAD -- corpus/ CLAUDE.md .claude/` | **54 files changed, 1642 insertions(+), 680 deletions(-)** |
| `git diff --name-only … | wc -l` | **54** |
| rosetta `HEAD` at read time | `1977224` (23 commits ahead of `8d6bb6c`) |
| raw unified diff | **4272 lines**, read top-to-bottom in six passes |
| hunks reviewed | **all** hunks in all 54 files (every `@@` block opened) |

Ground-truth clones re-derived at open, matching the addendum sheet exactly:

| clone | checkout | clone | checkout |
|---|---|---|---|
| `platform` | `0c91421d` | `messenger` | `fa47850d` |
| `app` | `b948604f` (origin/main `2035f9a4`) | `cms` | `ca50c817` |
| `next-web-app` | `bb3313bc` (origin/main `8297c684`) | `graphql-wundergraph` | `60c229f3` |
| `sentinel` | `88bc5592` | `roadrunner` | `87d8d443` |
| `storage` | `4ce8ece5` | `studio-desk` | `14a5442a` |
| `jobsimulation` | `462343b0` | `ant-academy` | `9c3843cd` |
| `rosetta-extensions` (authoring) | `6130bfd8` | | |

**General finding on the diff's quality, stated up front so the blockers are read in proportion:** this is
the most accurate repair pass I have graded on anchors. I opened ~70 platform `file:line` citations the diff
*introduced* and **all but three resolved to the exact construct named**. The failures below are concentrated
in (a) prose the repair wrote around correct anchors, (b) claims corrected at one site and left standing at
its twin, and (c) intra-corpus `<doc>.md:N` citations the repair's own line-count changes invalidated — which
is §5 rule 34, committed by the pass that ships rule 34.

---

## Blockers

### B1 — The repair propagated an M810/cms verdict into the two highest-propagation instruction files that its own canonical source forbids asserting

- **Anchor:** `CLAUDE.md:189-190` — *"teardown is **M810** — and it is **uneven**: it has **LANDED for
  jobsimulation** (`6092c6d2` …) and has **not moved for cms** (`cms/terraform/main.tf:39` is still
  `service_desired_count = 0`)."*
  Twin site: `corpus/README.md:16` — *"**not moved for cms** — see the fenced per-service statement in
  [`architecture/platform-migration-status.md`]"*.
- **Why it is false:**
  1. **The corpus's own canonical row says the opposite may not be asserted.**
     `corpus/architecture/platform-migration-status.md:88` (the `cms` row, rewritten in *this same diff*):
     *"**Still UNMEASURABLE here** … report both, assert neither."*
     `CLAUDE.md:241`, sixty lines below the banner it contradicts: *"**M810 for cms is IN PROGRESS and its
     prod state is NOT MEASURABLE from our clone set** … **Do not assert either way**."*
     `corpus/services/cms.md:19`: *"**Do not assert either way**."*
     `corpus/README.md:16` is worse than a bare contradiction — it asserts *"not moved for cms"* and then
     points the reader at the fenced statement **as its authority**, and that statement says the assertion
     is not available.
  2. **It is also unsupportable against ground truth.** M810 *has* moved for cms in the only place this
     corpus can see: `git -C stack-demo/cms show 6efa1d5` (merged `f38c0c4a`, 2026-08-04) deletes
     `.github/workflows/build-production.yml` under the subject *"the cms ECR repository is decommissioned
     (M810)"*, body: M810 *"deletes `module \"cms_euwest1\"` … which destroys the ECS service and the
     production-cms ECR repository"*, dropped because it *"would try to push an image into a registry that
     no longer exists."* (I verified `cms/terraform/main.tf:39` `service_desired_count = 0` at both the
     checkout `ca50c817` and `origin/main f38c0c4a` — the *other* half of the pair is real, which is
     precisely why the map says report both.)
  3. A third site repeats it inside the fenced file itself:
     `corpus/architecture/platform-migration-status.md:254` — *"while `cms` sits untouched at
     `service_desired_count = 0`"* — and `:89` (the `jobsimulation` row) — *"`cms` has not moved"*.
     So the fenced document hedges in one row and asserts in two others.
- **Why I hesitated:** one could read *"not moved"* narrowly as *"the terraform module block is unchanged"*,
  which is measured and true. But the banner's subject is **M810 teardown**, not a module block, and the
  same file 52 lines later forbids exactly that reading. This is the class §5 rule 40 was written for.
- **Confidence:** high

### B2 — `messenger.md` says the `backend` stream subscriber has 20 handlers; it has 21, and the same repair wrote 21 in the twin

- **Anchor:** `corpus/services/messenger.md:17` — *"(`messenger/internal/flow/flow.go:70-95` adds a
  subscriber on the `backend` stream with **20 handlers**; …)"*
- **Why it is false:** enumerated from source, `stack-demo/messenger` @ `fa47850d`:
  `internal/flow/flow.go:72` `h.subServer.AddSubscriber("backend", sub.AddHandler(` … closing `))` at `:104`.
  Live `pubsub.EventHandler(...)` entries in that call: **21**
  (`sed -n '73,103p' internal/flow/flow.go | grep -c '^\t\tpubsub.EventHandler'` -> 21), with **one**
  additional entry commented out at `:83`
  (`// pubsub.EventHandler(h.OrgJobSimulationAssignmentPastDueHandler), // not implemented`) — so 22 lines,
  21 that compile. The other two subscribers add 2 (`jobsimulation`, `:105`) and 1 (`cms`, `:109`), for
  **24 live handlers total** — independently corroborated by `app`'s own boot message at
  `app/main.go` @ `origin/main`: *"replayed through **24** email handlers"*.
  The same diff writes the correct figure in the twin: `corpus/architecture/dependency_map.md:58` —
  *"a literal `AddSubscriber(\"backend\", …)` over **21 live handlers**"*.
- **Confidence:** high

### B3 — The `/stack-update` reference now contradicts itself and the `/dev-up` reference on which repos have migrations

- **Anchor:** `.claude/skills/stack-update/reference.md:54` — `# Apply migrations (4 services: app, cms,
  jobsimulation, skillpath):` followed by a runnable `make migrate`.
  Same file, `:31`: `for repo in platform app cms jobsimulation next-web-app; do`.
- **Why it is false:** `platform/Makefile:14` derives
  `MIGRATION_REPOS := $(shell awk '/^  - name:/{name=$$3} /migrations: true/{print name}' repos.yml)`, and
  `repos.yml` @ `0c91421d` has exactly one `migrations: true` entry — `app` (`repos.yml:14-17`). `cms`,
  `jobsimulation` and `skillpath` are not in `repos.yml` at all (deleted at `d11a403` / `a4db680`), so
  `make migrate` iterates a one-element list and `cd cms` fails outright.
  This diff **rewrote the section 40 lines below it** (`:94-115`, "Repository update order") to say the
  clone set is four repos, and **wrote the correct claim in the sibling skill in the same commit**:
  `.claude/skills/dev-up/reference.md` — *"`make migrate` — `app` is the **ONLY** repo that has any
  (`repos.yml`)"*. A reader of the two skills now gets opposite instructions.
- **Why I hesitated:** the offending lines are pre-existing text, not new text. I book it because the repair
  opened, edited and re-verified this exact file for this exact predicate and left a runnable command that
  names three repos that cannot be cloned — the corpus is now self-contradicting where before it was
  uniformly stale (§5 rule 19).
- **Confidence:** high

### B4 — The repair moved a cited construct 10 lines and did not re-point the citation into it (§5 rule 34, committed by the pass that ships rule 34)

- **Anchor:** `corpus/services/hiring.md:38` — *"as the twins
  [`service_taxonomy.md:52`](../architecture/service_taxonomy.md) and
  [`dependency_map.md:78`](../architecture/dependency_map.md) already said."*
- **Why it is false:** at `8d6bb6c`, `service_taxonomy.md:52` was
  *"**Database**: PostgreSQL — one schema, `public`, owned by `app`, which is the only repo with migrations
  … the `cms`, `jobsimulation` and `skillpath` schemas are legacy husks"* — exactly the twin `hiring.md`
  invokes. This diff inserted a 10-line blockquote (the mermaid generation-edge correction,
  `service_taxonomy.md:40-49`) above it, moving that construct to **`:62`**. `service_taxonomy.md:52` now
  reads *"> then runs the pipeline as a **subprocess of its own container** —
  `app/internal/cms/studio/studioManager.go:119` execs `studio/gen.py`."* — an unrelated claim about
  studio-room. The second citation, `dependency_map.md:78`, did **not** move and still resolves.
  `hiring.md` was itself edited in this commit range (`:35`, `:179`), so this is a discharge miss, not a
  partition gap.
- **Confidence:** high

### B5 — Two re-pointed `rosetta-extensions` anchors resolve only in the stale consumption clone; against the authoring copy one lands on an unrelated CORS comment

- **Anchor:** `corpus/architecture/external_services.md:208-209` —
  *"`rosetta-extensions/stack-injection/gen_injected_override.py:**669-670**` re-points every service in
  `DIRECTUS_DATA_CONSUMERS`, which is **`("cms", "backend")`** (`:**84**`)"* (was `:636-637` / `:53`).
- **Why it is false:** graded against the authoring copy — the witness the instrument designates
  (`.agentspace/rosetta-extensions` @ `6130bfd8`) and the copy `resolve()` itself prefers (§5 rule 36):
  - `:86` is `DIRECTUS_DATA_CONSUMERS = ("cms", "backend")`. **`:84`** is
    `# (DIRECTUS_PUBLIC_BASE_ADDR) is deliberately NOT re-pointed — it stays on prod public links so` —
    a comment about the plane that is deliberately *not* re-pointed, i.e. the opposite of the cited claim.
  - The re-point is at `:698-699` (`if with_directus and name in DIRECTUS_DATA_CONSUMERS:` /
    `env.append(f"DIRECTUS_BASE_ADDR={DIRECTUS_INNETWORK_ADDR}")`). **`:669-670`** is
    `# CORS_EXTRA_ORIGINS in non-production (a runtime hook, no platform change), so feed it this stack's` /
    `# offset frontend origins.` — the CORS block, ~29 lines earlier.
  Both numbers *are* correct in `stack-demo/rosetta-extensions` (the older pinned consumption clone: `:84`
  and `:669`), so the repair re-derived against the wrong one of two clones of one repo.
- **Why I hesitated:** the numbers are not invented — they are right in a clone that exists on this box.
  Two clones of one repo are one witness (§5 rule 36), the instrument names the authoring copy, and
  `CLAUDE.md`'s own policy sends a reader to the authoring copy to read tooling. The sibling citation in the
  same paragraph (`stack-injection/tests/test_injection.py:1109`) resolves **correctly** against the
  authoring copy, so the block is internally inconsistent about which clone it means.
- **Confidence:** medium

### B6 — The media-substrate retraction attributes the `chime_status` write to a function that does not write it

- **Anchor:** `corpus/ops/demo/media-substrate-spec.md:37` — *"the SNS S3-event handler (`:381-426`) flips
  `chime_status` to `completed`"* (in `jobsimulation/internal/recording/chime.go`).
- **Why it is false:** `chime.go:381` is
  `func (r *ChimeRecordingManager) SnsChimeRecordingHandler(data []byte) ([]string, error)`. Read in full
  (`:381-426`) it unmarshals the SNS envelope, confirms a subscription, parses the S3 event records, strips
  the `concatenated/composited-video/` prefix and **returns a `[]string` of media-pipeline ids**. It touches
  no Ent client and no session row. The flip is done by its caller:
  `jobsimulation/internal/simulator/manager/manager.go:3235` —
  `m.repo.Sessions.UpdateSessionChimeStatus(ctx, *sessionId, enum.ChimeRecordingStatusCompleted)` inside
  `HandleChimeRecordingReady` (`:3223`). Verified by exhaustive grep: `SetChimeStatus`/`chime_status` appear
  nowhere in `chime.go` (positive control: `grep -rn chime_status .` returns 10+ hits elsewhere).
- **Why I hesitated:** the surrounding retraction — *"the bytes ARE in prod S3"* — is correct and very well
  evidenced (I verified `:166-167` capture sink, `:240-247` concatenation sink -> `/concatenated`, `:279` the
  only `Delete*` is `DeleteMeeting`, `:313-331` `getMediaFromS3` with key
  `concatenated/composited-video/%s.mp4`, `manager.go:3272-3291` `SaveRecording` -> Bunny upload, and
  `terraform/chime.tf` with zero `lifecycle`/`expiration` against a 21-hit `bucket` control). Only the one
  clause mis-names the writer — but the briefing lists "wrong line anchors that point at the wrong
  construct" as a blocker, and a reader tracing the render gate lands on a parser.
- **Confidence:** medium

### B7 — A new mermaid edge label says the generation pipeline runs "in-process"; it is an `exec.CommandContext` subprocess, and the same repair says so 11 lines below

- **Anchor:** `corpus/architecture/architecture_overview.md:128` —
  `Gateway -.->|spawns studio/gen.py in-process| Room`
  Twin site: `corpus/architecture/service_taxonomy.md:38` — `Backend -->|spawns studio/gen.py in-process| Room`
- **Why it is false:** `app/internal/cms/studio/studioManager.go:119`
  `return s.runCommand(ctx, pyBin, append([]string{"studio/gen.py"}, tokens...))` ->
  `studioManager.go:1101` `pycmd := exec.CommandContext(ctx, name, args...)`. It is a **subprocess** of the
  `app` container, not an in-process call — a Python file cannot run inside a Go process, and "in-process"
  is a load-bearing term of art throughout this corpus (it means *no network hop, same process*).
  Contradicted twice, both in text this diff wrote or kept:
  `corpus/architecture/platform-migration-status.md:103` — *"orchestrated from `app/internal/cms/studio/`,
  **which spawns it as a subprocess**"*; and `service_taxonomy.md:52`, fourteen lines under the offending
  edge — *"then runs the pipeline as a **subprocess of its own container**"*.
- **Why I hesitated:** the edge's *direction* was the bug being fixed (`Room --> Desk` was backwards) and the
  fix is right; only the label over-reaches. But the label is what a reader of the diagram takes away, and it
  contradicts its own explanatory note.
- **Confidence:** medium

### B8 — A stated positive control does not reproduce, and the absence claim it certifies has a counterexample

- **Anchor:** `corpus/services/messenger.md:20-22` — *"`MESSENGER_RPC_ADDR` occurs in **no** repo, and
  `git log -S MESSENGER_RPC` over the platform's entire history returns **0** commits (positive control:
  `-S SKILLER_RPC` returns **3**)."*
- **Why it is false:** measured across every clone on this box, at the sheet's refs:

  | clone | `git log -S MESSENGER_RPC` | `git log -S SKILLER_RPC` |
  |---|---|---|
  | `platform` | 0 | **7** |
  | `messenger` | 0 | 1 |
  | `app` | **1** | 26 |
  | `cms` / `jobsimulation` / `storage` / `sentinel` | 0 | 8 / 4 / 0 / 0 |

  No clone yields **3** — nor does `--all` (platform 8, messenger 2, app 34), nor
  `-S SKILLER_RPC_ADDR` (7 / 1 / 26), nor a `-- docker-compose.yml` pathspec (6). And
  `MESSENGER_RPC` *does* occur in one tracked `app` file (`grep -rl` names a path under
  `app/knowledge/plan/`; I did **not** open it — HARD BAR 1), so *"occurs in no repo"* has a counterexample.
  The passage also never names which clone or ref produced the numbers (§5 rule 12).
- **Why I hesitated:** the **substantive** claim — that no service ever constructed a `MessengerService`
  client, and that the traffic ran the other way (`messenger/cmd/root.go:118-142`, which I verified reads
  all four `*_RPC_ADDR` values) — survives intact. This is a defective control attached to a sound
  conclusion. I book it because a control that returns a different number than the document says is exactly
  the instrument-broken signal §5 rule 2 exists to catch.
- **Confidence:** medium

### B9 — §5 rule 37's own worked figures are now false, in the file the repair rewrote by +453 lines, and contradict the map's new count

- **Anchor:** `corpus/ops/platform-alignment.md:1031` — *"Here: every `- name:` ever written in `repos.yml`,
  across all **9** commits that touched it — **14 ever, 6 now, 8 removed**."*
- **Why it is false:** re-derived from `platform` @ `0c91421d` (union of `- name:` across every revision of
  `repos.yml`, `git log --follow`):
  - commits touching `repos.yml`: **10** (not 9)
  - names ever: **14** (correct) — `app · chronos · cms · graphql-wundergraph · intelligence · jobsimulation ·
    messenger · next-web-app · roadrunner · sentinel · skiller · skillpath · storage · studio-desk`
  - **now: 4**, not 6 (`app · sentinel · next-web-app · studio-desk`); **removed: 10**, not 8.
  This directly contradicts three claims the same diff wrote: `platform-migration-status.md:126` (*"of which
  **4** are in `repos.yml`"*), `:59-62` (*"took the clone set to **four**"*) and `frontend_architecture.md:11`
  (*"At `0c91421` `repos.yml` holds exactly **4** entries"*). The rule's own subject is *derive the vocabulary
  from HISTORY, because removals are the drift* — and its illustration is stale on the removal it was
  written for.
- **Why I hesitated:** this text is **unchanged** by the diff (verified: `git show 8d6bb6c:…` has the
  identical line), so it is a survival, not an induction. I book it because it is now a live
  self-contradiction inside the one file this pass rewrote most heavily, and it is a member of the exact
  predicate class the pass was discharging.
- **Confidence:** medium

---

## Minors

- `corpus/services/messenger.md` and `corpus/services/customerio-sync.md` cite `app/main.go:295` for
  the `BREVO_KEY` `log.Fatalf`. At `app` origin/main `:295` is the closing comment line; the guard is `:296`
  and the `log.Fatalf` is `:297`. Right block, two lines short.
- `corpus/ops/platform-alignment.md:1432` still carries *"Both prod counts are `0`"* for storage+messenger —
  the exact paired claim `platform-migration-status.md:92` says *"has to be split, not re-anchored"* because
  storage's service block is **deleted** (`storage/terraform/main.tf` is 18 lines at `9f8cb532`), not scaled.
  Dated by the paragraph's `app 9d00a313` framing, which is why it is a minor rather than a blocker.
- `corpus/ops/platform-alignment.md:1041` cites `storage.md:25` as *"a sentence [that] merely mentions two
  other repos in passing"*. At `8d6bb6c` `storage.md:25` was the `repos.yml` row (*"exactly as `cms` and
  `jobsimulation` are kept"*) — this repair rewrote the table and `:25` is now the **prod** row, which names
  no other repo. Same rule-34 mechanism as B4, on an illustrative rather than load-bearing citation.
- `corpus/architecture/external_services.md:543` still describes the Mistral OCR client as
  `mistral.NewMistral(...)` at `markdownManager.go:19` — true at the checkout `b948604` (verified), but
  superseded at `app` origin/main by `mistralocr.New(aiKey)` at `:30`, which **`corpus/services/cms.md`
  states explicitly in this same diff**. The Mistral-provider claim was repaired in four files and left in a
  fifth that the pass also edited.
- `corpus/ops/platform-alignment.md:1306` cites `corpus/services/backend.md:175` for *"a corpus line
  asserting messenger points all four addresses at `backend:8083`"*. `backend.md:175` is a directory-listing
  row (`cache/  Redis caching layer`); at `8d6bb6c` it was also a directory row. Pre-existing drift, pushed
  further by this diff's +40 net lines above it.
- `.claude/skills/stack-update/reference.md:96-106` heads a 5-item list *"the make-driven `pull` order"* and
  makes `platform` item 1. `make pull` walks `REPO_NAMES` (`Makefile:13`, from `repos.yml`), which does not
  contain `platform`; the same page draws exactly that inference for `ant-academy` two lines below.
- `corpus/architecture/architecture_overview.md` / `platform-migration-status.md:89` cite
  `jobsimulation/terraform/main.tf:24-40` for the surviving-ownership comment; the sentence it quotes runs
  to `:41`.
- `corpus/services/README.md:11` says *"**Seven** services in this index … are folded into `app`"* while
  `CLAUDE.md:182`, `backend.md:5` and `service_taxonomy.md:63` say **eight**. Not a defect — the README
  deliberately excludes `roadrunner` (which still deploys in prod at `= 1`) and calls it *"the eighth"* on
  the next line — but the two denominators sit three clicks apart with no marker.

---

## Positively cleared (audited zeros)

**ENUMERATED** (predicate independently derived from source, cardinalities compared) — these are clearances:

- **`repos.yml` = 4 entries** (`app`, `sentinel`, `next-web-app`, `studio-desk`), 28 lines @ `0c91421d`;
  only `app` carries `migrations: true` / `schema: public` at `:14-17`; `sentinel` at `:18-20`; header at
  `:2-10`; `:12-13` is the "sentinel is the one other Go service" sentence. Every one of the ~12 corpus
  citations into `repos.yml` in this diff resolves.
- **`docker-compose.yml` declares 5 services** (`sentinel:5`, `backend:28`, `studio-desk:112`,
  `next-web-app:143`, `gotenberg:170`), **186 lines**, + 2 from `common.yml` = **7 effective**.
- **The whole legal profile set is 5** — union of every `profiles:` key: `{core, backend, all, studio-desk,
  frontend}`, carried by exactly **4** services. No `storage-legacy` / `messenger` / `customerio-sync` /
  `graphql` / `cms` / `jobsimulation` / `roadrunner` token exists.
- **Zero `*_RPC_ADDR` in any compose file** (`grep -rn _RPC_ADDR *.yml Makefile` -> rc 1, positive control
  `ADDRESS` -> 1 hit), and exactly one service address, `AUTHORIZATION_ADDRESS=http://sentinel:8087` at
  `docker-compose.yml:48`.
- **`repos.yml` history**: 14 names ever; **11** of them `type: go`; the four beyond the seven on-disk Go
  clones are exactly `skiller`, `skillpath`, `chronos`, `intelligence` — `shared_libraries.md`'s
  *"that set is 11"* is right.
- **`authn` is imported by nothing**: `github.com/anthropos-work/authn` = 0 hits in all seven Go clones'
  `go.mod`; positive control `colony` = 1 hit in all seven.
- **Secret-DNA is 64 genes / 6 repos**: parsed `stack-secrets/secretdna/secret-dna.json` @ `6130bfd8` —
  version `fast-build-m256`, `profile` literally `graphql`, per-repo 32/10/2/7/7/6, status split
  44 required / 12 optional / 8 waived (1+5+1+1), required = 13 critical + 31 standard. Every figure in
  `secrets-spec.md` and its four index twins matches.
- **Six snapshot-fidelity operators** (`stack-seeding/dna/snapshot.go:33-57`), with the taxonomy surface
  carrying 5 of 6 at `data-dna.json:410` and the content surface carrying 5 (swapping `embedding-dim` for
  `cross-surface-closure`) at `:439` — `alignment_testing.md` and `snapshot-spec.md` both correct.
- **21 live handlers on messenger's `backend` subscriber / 24 across all three** (see B2).
- **9 gotenberg-routed MIME types** at `extract.go:17-27`, DOCX correctly excluded; both call sites discard
  the PDF (`extract.go:81`, `user_import_resume_2d.go:73`). `gotenberg.md`'s strong new claim — *never
  stored, never served, never displayed* — holds.
- **29 `NEXT_PUBLIC_BACKEND_API_URL` sites across 21 non-test files** @ `next-web-app bb3313bc`: raw count
  is 32 across 24, minus `apps/web/.env.example`, `apps/hiring/.env.example` and
  `packages/ui/src/Coursebuilder/BACKEND-SECURITY-CONTRACT.md` -> **29 / 21**, and the four `core-js` REST
  clients contribute exactly 12. `frontend_architecture.md` is exact.
- **7 supported academy locales** at `ant-academy code/src/i18n/locale.js:10`; `LanguageSelector.jsx:88`
  `role="menu"` / `:97` `role="menuitemradio"`; mounted at `TopBar.jsx:76`, `LocaleSwitch` at
  `PublicHeader.jsx:20`. The retraction's provenance claim also checks out: `5b05b7d9` (2026-05-05) really
  does add `LanguageSelector.jsx` (+66) and touch `TopBar.jsx` (+55) despite a commit subject about cover
  images.
- **Studio-room has no Mistral path**: `studio/services/ai.py:705-708` is
  `{'openai','azure','anthropic'}`; `ai.py:1-2` imports `openai`+`anthropic` only; `grep -rni mistral studio/`
  returns exactly one hit (`requirements.txt:8`). Go-side Mistral is OCR-only
  (`markdownManager.go:10`/`:30`, `studioManager.go:531`/`:583`, `xlsx.go:13`).
- **`jobsimulation` M810 landed**: `6092c6d2` (2026-08-04) deletes the `module "jobsimulation"` block;
  `service_desired_count` returns **0** hits in `terraform/` at `origin/main 82cb66ec` (positive control:
  `atlas_migration` = 4 hits); `main.tf` is 56 lines; `:15-22` is the decommission comment verbatim.
- **`storage` prod block deleted, `messenger` scaled to zero**: `storage/terraform/main.tf` = 18 lines at
  `origin/main 9f8cb532` with the quoted comment at `:9-11`; `messenger/terraform/main.tf:29`
  `service_desired_count = 0` in a 121-line module. The map is right to refuse to pair them.
- **The whole `app` wiring row**: `main.go` @ origin/main — `:285`/`:286` subsystem switches, `:395`
  `customeriosync.New`, `:504` the STORAGE_RPC_ADDR-is-gone comment, `:516`/`:517`/`:524`/`:525`/`:547`/
  `:690`/`:721`/`:751`/`:1102`/`:1153`/`:1384`/`:1445`/`:1450`/`:1471`/`:1473`/`:1552`, `:15`/`:62`/`:63`
  imports, `env_guards.go:60-62` and `:92-111`, `terraform/main.tf:181`/`:415-416`/`:419-420`,
  `internal/storage/service.go:22`/`:24`, `internal/messenger/adapters/skiller.go:11`,
  `internal/messenger/flow/streams.go:65`. **17 of 17 resolved to the exact construct named.**
- **`app/CLAUDE.md:109` and `app/knowledge/architecture.md:28` still list `SkillPathSessionService`** —
  Trap C confirmed; the re-point from `:72`/`:80` is correct.
- Compose anchor re-points: `:18`, `:37`, `:48`, `:59`, `:72`, `:73-75`, `:82`, `:83`, `:84-92`, `:101-109`,
  `:102-103`, `:110`, `:112`, `:119`, `:135`, `:138-140`, `:141`, `:143`, `:151`, `:152`, `:160`, `:161`,
  `:165-167`, `:168`, `:170-171`, `:183` — **26 of 26 correct.**
- `Makefile:10` `PROFILE ?= core`, `:31-45` `pull`, `:120` `--profile core --profile frontend`.
- rext: `alignment/cmd/alignctl/run.go:133-136` (`ExitRegressed=2`, `ExitUnmeasurable=3`) and `:139-153`;
  `stack-secrets/secretdna/dna.go:233`, `catalog.go:17`;
  `stack-seeding/seeders/ai_readiness_interview_report.go:185-189` + `:149`, and the claim that the source's
  own *"four"* prose is gone is correct; `stack-verify/lib/services.sh:31,49-52` and
  `readiness.sh:251` still carry the merged-away rows exactly as `verification.md` now says.
- next-web-app: `AIReadinessClient.tsx:78`, `:153-154`, `:166-170`, `:599`;
  `UserStatusContext.tsx:141-173` (the hiring eject) — all exact, and the M222->M224 falsification in
  `hiring.md` is correctly stated.
- `platform-alignment.md:1031`'s *"14 ever"* — the only figure of the three that survives (see B9).

**RE-DERIVED only** (the weakest clearance — recomputed arithmetic the document displays, without an
independent enumeration): the `service_taxonomy.md` / `platform_repo.md` "5 declared, 7 effective" pair;
the counts of what `--profile all` selects; the demopatch G5 rewrite (I read the spec's internal
consistency but did not run `demopatch`).

---

## What I could not settle

- **`cms`'s actual production state.** `infrastructure` has never been in any clone set, so the destruction
  of `module.cms_euwest1` is unobservable from here. The corpus's canonical position (*report both, assert
  neither*) is the right one; B1 is about three sites that abandon it, not about my being able to decide it.
- **`stack-core/platform_predicate_guard.py` reach figures** — *"`docker-compose.yml` citations — 23 of
  them"*, *"every other `file:line` … (66 citations)"* (`platform-migration-status.md:204-205`). These are
  outputs of a guard run I did not reproduce; settling them needs `guard_family.py` executed against this
  tree at these refs, with the refs recorded.
- **`app/main.go:1442`** (`dependency_map.md:58`, *"guards the takeover against `StreamBackend`"*). At the
  ref the sentence names (`9d00a313`) `:1442` is `if deployedEnvironment() {` — the guard's opening line,
  with `StreamBackend` three lines below at `:1445` — so it is defensible. At `origin/main` it is an
  unrelated comment and the construct is at `:1493`. Not booked, because the sentence pins its ref; flagged
  because the surrounding table otherwise grades at `0c91421`/origin-main and the mixed ref makes the block
  `ambiguous` to the corpus's own resolver (§5 rule 35).
- **Whether `MESSENGER_RPC`'s single `app` hit changes B8's substantive conclusion.** I identified the file
  only by name from `grep -rl` and did not open it (HARD BAR 1). Settling it needs someone outside the bar
  to read that one line and confirm it is a plan-doc mention rather than a consumer.
