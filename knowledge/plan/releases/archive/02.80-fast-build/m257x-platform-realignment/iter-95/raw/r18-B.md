# Seat B — reading #18

> Provenance: harness blocked this seat's file write; returned as text, persisted by the orchestrator
> (blockers/minors/unsettled verbatim; the long anchor-audit list condensed to its heads).

## Compact summary

B1 | corpus/architecture/platform-migration-status.md:92 | storage row cites M903 custody transfer; anchor wrong, plan never executed | high
B2 | corpus/architecture/platform-migration-status.md:110 | taxonomy row says "60K-skill dataset"; corpus retracted 60K everywhere else | high
B3 | corpus/services/ai-readiness.md:305 | AI_READINESS_URL cited at urls.ts:52; it is :50/:51, never :52 | high
B4 | corpus/architecture/platform-migration-status.md:205 | unfenced-citation count 66 contradicts its own guard's printed 68 | medium
B5 | corpus/architecture/platform-migration-status.md:115 | simulator row names compose service "jobsimulations"; diff renames to jobsimulation | medium
BLOCKERS=5 MINORS=9

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| corpus/services/ai-readiness.md | 651 | yes |
| corpus/architecture/platform-migration-status.md | 307 | yes |
| corpus/services/jobsimulation.md | 306 | yes |
| corpus/services/sentinel.md | 166 | yes |
| corpus/services/customerio-sync.md | 147 | yes |
| corpus/services/README.md | 83 | yes |
| corpus/architecture/README.md | 38 | yes |

Search-pipeline controls run in the same passes: SKILLER_RPC_ADDR / STORAGE_RPC_ADDR greps returned
hits where MESSENGER_RPC_ADDR returned none (so the zero is real); CMS_RPC_ADDR returned 9 commits
where MESSENGER_RPC_ADDR returned 0 over the same git log -S --all; a `nats`-must-be-present assert was
added to the compose-history enumerator after the first version silently dropped it (a top-level
comment line was resetting the parser's services: state — the first enumeration was WRONG and I caught
it with the control, not by inspection).

## B1 — platform-migration-status.md:92 — M903 stated as the forward custody path

Anchor (the storage row): "The module deliberately survives, because deleting the block would destroy
the two buckets, the CloudFront distribution and the media DNS record along with the prevent_destroy
guards that are read from configuration (:13-16); CUSTODY TRANSFER IS M903 (:18)."

False two ways:
1. WRONG CONSTRUCT. storage/terraform/main.tf:18 @ 9f8cb53 (the ref this cell names, and the only ref
   at which the file is 18 lines) reads "# See outputs.tf — consumers should reference these by
   output, never by literal name." Nothing about M903. Checked at the commit that CREATED the 18-line
   file (e6fac97) too — identical at :18. `git log --all -S "M903" -- terraform/main.tf` returns two
   commits; at neither does :18 mention M903. The only live M903 mention is storage/terraform/
   storage.tf:22.
2. SUBSTANCE INVERTED. storage/terraform/storage.tf:20-25 @ 9f8cb53: "(An earlier plan, M903, instead
   proposed moved-ing these resources into a local module in the infrastructure repo. THAT WAS NEVER
   EXECUTED, and the shipped design supersedes it...)" Written by d3e6d32 (2026-08-05), titled
   "correct the guard header, which contradicted the shipped design", body: "M903 never ran" and "A
   reader acting on the old instruction would relocate the assets and, in doing so, walk straight into
   the failure mode the header exists to prevent."

So the corpus carries the PRE-d3e6d32 framing — the one the platform deliberately deleted — and
presents a superseded, never-executed plan as the outstanding next step for assets the same sentence
says must not be destroyed. Confidence: high.

## B2 — platform-migration-status.md:110 — "the 60K-skill dataset" stated as fact

Anchor: "| taxonomy | library | library | no | private Go module — the NodeID type only. NOT the
60K-skill dataset, which lives in app's public schema |"

The corpus explicitly grades "60K skills" as not a measurement and instructs against writing it:
shared_libraries.md:203 ("60K skills | UNVERIFIED (not refuted) | 42,790 public skills ... Nothing
measured supports 60K"); ai_architecture.md:142 ("Do not write '60K skills and 18K job roles' here.");
service_taxonomy.md:145 and architecture_overview.md:236 carry the IDENTICAL taxonomy row and both
write ">=42,790 skills, >=22,470 job roles ... not a measurement"; root CLAUDE.md:197 likewise. A
repo-wide grep for 60K over corpus/ + CLAUDE.md returns 15 sites; THIS IS THE ONLY ONE that states the
figure as fact rather than naming it to retract it. Exact Sec5 rule-19 signature, standing in the file
the corpus calls "the index of truth". Confidence: high (citable from both sides, no ground-truth read
needed).

## B3 — ai-readiness.md:305 — AI_READINESS_URL cited at urls.ts:52

At checkout bb3313bc, urls.ts:50 is `export const AI_READINESS_URL = '/ai-readiness';` and :52 is
`export const ORGANIZATION_FEEDBACK_URL = '/enterprise/organization-feedback';`. At origin/main
8297c684 the constant is at :51 (a WORKFORCE_ANALYTICS_URL was inserted above) and :52 is
TALK_TO_DATA_URL. Walking the last 12 commits touching the file, AI_READINESS_URL is at :52 at NO ref.
Rule-21 "resolves but names the wrong construct", and load-bearing because this bullet is the doc's
evidence that the legacy dashboard was an unlinked orphan. Everything else in the bullet verified:
useNavbarSections.tsx:4, :398-400, :547, and the "exactly two non-node_modules files" grep.
Confidence: high.

## B4 — platform-migration-status.md:205 — unfenced-citation count 66 vs the guard's printed 68

Anchor: "| every other file:line ... (66 citations) | resolution + range only |" (paired with :204
"23 of them | fenced (F)" and :208 "F's reach is printed on EVERY run").

Re-derived the SET, not the sum, using the guard's own parse_citations over the fenced services table
(imported from stack-core/platform_alignment_guard.py @ 6130bfd8): 91 citations total — app/main.go 29
· docker-compose.yml 23 · repos.yml 10 · app/terraform/main.tf, jobsimwiring/wiring.go,
cms/terraform/main.tf, storage/terraform/main.tf, messenger/terraform/main.tf, common.yml 3 each ·
jobsimulation/terraform/main.tf, internal/storage/service.go, env_guards.go 2 each ·
roadrunner/sentinel/graphql-wundergraph terraform, storagens/callsites_test.go, messenger/cmd/root.go
1 each. Non-compose = 68, not 66. The guard's own reach line says the same: "assertion F resolved 91
citation(s) — 22 subject-checked, 68 range-only, 1 outside any service block" (exit 0, GREEN). The 23
reconciles as 22 + 1 unattributable (docker-compose.yml:1-2, the include: line, which the guard's
docstring counts as unchecked rather than guessed at) — so :204's "23 ... fenced" over-claims by that
one too.

Confidence: high on the fact (three agreeing derivations); MEDIUM on materiality — hesitated because
it changes no per-service fact. Booked because "wrong counts" is blocker-class, because it is
contradicted by the tool the same section tells you to run, and because a coverage denominator is
exactly what rule 24 says nobody checks.

## B5 — platform-migration-status.md:115 — compose service "jobsimulations" never existed

Anchor: "added 1474b1f (2023-12-01), REPLACED BY `jobsimulations` at 84862d1 (2024-05-29)"

84862d1's SUBJECT says "jobsimulations", but its DIFF renames the compose service simulator: ->
jobsimulation: (singular), with context jobsimulation.git, and re-points depends_on likewise.
Enumerated over all 85 commits touching docker-compose.yml, the distinct service keys are: backend ·
chromedp · chronos · cms · customerio-sync · directus · gotenberg · graphql · intelligence ·
jobsimulation · messenger · nats · next-web-app · postgresql · realtime · redis · roadrunner ·
sentinel · simulator · skiller · skillpath · storage · studio-desk · web-app · wundergraph.
`git log --all -S "jobsimulations:" -- docker-compose.yml` = 0.

This is the map violating the rule it states two rows above, in the roadrunner cell: "A commit message
is testimony, not evidence — grade a change by its diff." Matters slightly more than a typo because
app/internal/jobsimulations/ IS a real, separate directory in app today alongside
app/internal/jobsimulation/. Confidence: high on the fact; MEDIUM on grading — hesitated because the
correctly-named row exists, so no row is missing.

## Minors

- platform-migration-status.md:78-80 — "26 names" ever in docker-compose.yml. Enumerated: 25 distinct
  service keys. The doc's own naive command reproduces 26 because a grep '^[+-]  \w*:$' of git log -p
  also catches app-network, a NETWORK. No row is missing, so the completeness claim holds.
- sentinel.md:5 — "messenger is not a caller — ITS COMPOSE BLOCK sets no AUTHORIZATION_ADDRESS". At
  0c91421 there is no messenger compose block at all (838d907 deleted it), which the same file states
  correctly at :85 and :89. Conclusion correct, evidence sentence stale; hesitated toward blocker
  because it presupposes a construct that no longer exists.
- ai-readiness.md:46 — "contradicted :458 of this same file". Line 458 is a BLANK LINE; :456-457 are
  about the frozen-snapshot default route, unrelated to the D-07 re-anchor. Sec5 rule 34 drift.
- jobsimulation.md:92 — "consistent with the Data bullet, :31-38 above". The Data bullet is :36-42;
  :31 is "Where everything went:" and :38 truncates it.
- jobsimulation.md:157 — cites cms/directus/collections/jobsimulation.go:1594-1597; real path is
  cms/INTERNAL/directus/collections/... Content and line numbers verify EXACTLY at the corrected path.
- sentinel.md:44 — "only ... (casbin_test.go, rpc_test.go)". internal/authorization/manager_test.go:21
  is a third. The claim itself ("only in tests") is true; the enumeration omits a member.
- ai-readiness.md:277 — NewManager (manager.go:95) takes logger, db, aiClient, entClient, pub,
  academyEmbeddings, directory. orgRepo is derived inside from entClient, not passed; logger is
  missing from the doc's list. Fields match, parameters do not.
- ai-readiness.md:509 — bare useAIReadiness.ts:48-62. TWO files carry that name; deriveMode genuinely
  is at :48-62 in components/ai-readiness/useAIReadiness.ts, while the same doc cites
  hooks/useAIReadiness.ts:274 by full path. Not uniquely resolvable (Sec5 rule 36).
- jobsimulation.md:200-201 — "@ origin/main 7177374". That sha is 33 commits behind current origin/main
  (2035f9a), where func main() is at :229. Anchor correct at the named sha and the substantive claim
  still holds at 2035f9a; only the "origin/main" LABEL expired.

## Positively cleared — ENUMERATED (17 items, heads)

(1) repos.yml 14 names ever, derived over all 10 commits; all 14 have rows. (2) 5 declared / 7
effective / 4 repos.yml entries at 0c91421. (3) sentinel casbin: 6 request types, 6 policy types, 3
role groupings, 6 matchers, each matcher's prose also checked against its expression. (4) 19 exported
RPC handlers in internal/rpcsrv/rpc.go; the table lists exactly those 19. (5) 19 entries at weight 1.0
+ 12 at 0.5 = 31, denominator 25.0. (6) defaultReadinessSims exactly 3 entries. (7)
AIReadinessCSVColumns = 15 — note the SOURCE's own comment at csv.go:35 still says "19-column" and is
STALE; the corpus is right and explains the trim. (8) five KPI ids on both sides; the doc's own note
that this bullet "said four until M257x" is correct. (9) 23 CREATE TABLEs in the jobsim data model.
(10) "0 INSERTs" in 20260729133514.sql — grep -ci insert returns 1; opened it, the sole hit is a
FUNCTION NAME inside a DROP FUNCTION. (11) MESSENGER_RPC_ADDR 0 across all 11 clones with controls
returning hits; git log -S --all = 0 vs CMS_RPC_ADDR = 9. (12) AUTHORIZATION_ADDRESS 1/0/0 across
compose/common/.env_example. (13) four repo-wide AI_READINESS_FLAG hits, one consumer. (14)
AIReadinessClient.tsx: 0 posthog, 0 flag_ai_readiness on the real 655-line file. (15)
HowWeMeasureTab.tsx exactly 1,989 lines, grep -c interviewQuestions = 0. (16) interaction enum exactly
{email, call} with the source_id <> target_id CHECK. (17) compose service counts d11a403^ = 11,
d11a403 = 8, 0c91421 = 5 — matches the cell exactly.

RE-DERIVED only (weaker): the tier-1 weight arithmetic; 9->6->4 clone-set arithmetic; 78.4% ~ 156/199.
Also confirmed the PREDICATE by reading computeTier1's body (readiness.go:139-165), which does divide
by the sum of ALL configured weights and carries the quoted sentence.

Structurally cleared: architecture/README.md index complete (all 10 non-README files listed, no
phantoms). services/README.md — 29 files - README - TEMPLATE = 27 docs, index enumerates 10+5+8+4 =
27, none unindexed, none phantom. customerio-sync.md — NO FINDINGS AT ALL; every testable claim
verified at 2035f9a, including its honest "Scope note: whether its own ECS task / image / terraform
module have been torn down was NOT measured".

## What I could not settle

- jobsimulation.md:46-47 — "Production terraform still names http://backend.internal.anthropos:8081."
  Stated as fact, UNCITED. The literal appears NOWHERE in any clone's terraform: messenger
  terraform/main.tf:82 and cms terraform/main.tf:122 both set JOBSIMULATION_RPC_ADDR from an
  UNDEFAULTED variable, so the value comes from `infrastructure`, never in the clone set. The single
  occurrence anywhere is app/knowledge/service-dependencies.md:46 — a platform NARRATIVE doc, which
  platform-alignment.md Sec6 says is NOT documentation of record. Sits oddly beside this same file's
  :71-72 disclaimer that prod terraform is "not something this corpus can see".
- GitHub archive states/dates for 6 repos — no network. Settled by gh repo view --json isArchived.
- platform-migration-status.md:126-133, 139-157 — "93 repositories", "46 named by no corpus document",
  census push dates. Needs the GitHub org API + a whole-corpus mention index. The internally-checkable
  parts DO hold.
- ai-readiness.md live measurements — 2.09 s recompute, 24 ms frozen read, 180 s -> 19 ms demo-patch
  delta, "5 -> 345 validation_attempt_results", "78.4% (~156 of 199)". Runtime observations; the doc
  itself says "Re-measure before relying on either number", which is the right standing.
- ai-readiness.md:630-637 — "5 Playthroughs" and the M250/M254 live-green gate results. The manifest
  count is playthroughs.md's to own and is outside my file set.
- sentinel.md:45-46 — "a live stack's select distinct v2 ... where p_type='g2' returns admin/member/
  candidate only". Needs a running stack. The static half is confirmed (0 manager rows in
  init_policy.sql), but note content_creator IS seeded at init_policy.sql:88-118 — so on a stack that
  has run that seed the query would return FOUR values, not three. Could not tell whether the quoted
  result predates the content_creator block.
