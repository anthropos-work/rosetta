# Seat C — reading #17

> Provenance: harness blocked this seat's file write; returned as text, persisted verbatim by the
> orchestrator (clearance lists condensed; blockers/minors/unsettled verbatim).

## Compact summary

B1 | corpus/services/messenger.md:17 | backend subscriber has 21 handlers, not 20; cited range truncates list | high
B2 | corpus/services/messenger.md:22 | positive control "-S SKILLER_RPC returns 3" measures 7 at every ref | high
B3 | corpus/services/coursebuilder.md:131 | asserts "current app v1.363.2"; app is v1.366.0 / past v1.367.0 | medium
BLOCKERS=3 MINORS=9

## Refs used
platform 0c91421d (= origin/main) · app checkout b948604f / origin 2035f9a4 · messenger checkout
fa47850d / origin e9421c68 · next-web bb3313bc · studio-desk 14a5442a · sentinel 88bc5592 · storage
4ce8ece5 · rext AUTHORING 6130bfd8.

Independently confirmed at open: at platform 0c91421d, repos.yml holds 4 repos and docker-compose.yml
holds 5 services plus common.yml's postgresql/redis. storage, messenger, customerio-sync gone from
both. Always-on floor = exactly {postgresql, redis, sentinel}.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| alignment_testing.md | 537 | yes |
| clerkenstein.md | 370 | yes |
| ai_architecture.md | 302 | yes |
| messenger.md | 184 | yes |
| coursebuilder.md | 140 | yes |
| gotenberg.md | 84 | yes |
| db-backup.md | 31 | yes |
| (in-scope) platform-alignment.md Sec5 | 2139 total; Sec5 = :249-1361 | yes, Sec5 in full |

Search-pipeline controls run in the same pass, because three conclusions rest on an ABSENCE:
- grep -rl MESSENGER_RPC_ADDR stack-demo/ -> 0; control SKILLER_RPC_ADDR -> 5+ hits.
- grep -rn "r\.Cookie" clerkenstein -> 0; control "Cookie" -> 7 (all emit-side).
- grep -rin 'bedrock|boto3' app/studio/ -> 0; control 'anthropic' -> hits.
- One zsh glob failure (--include=*.go unquoted) produced an empty result FROM A FAILED COMMAND. Read
  the stderr, saw "no matches found", re-ran. Recorded because this is the rule the reading nearly
  tripped on.

## B1 — messenger.md:17 — 20 handlers claimed; 21 exist

"(messenger/internal/flow/flow.go:70-95 adds a subscriber on the backend stream with 20 handlers;
app runs that same subscriber now, on messenger's own consumer group)"

Enumerated the predicate from source rather than re-deriving a sum. At checkout fa47850d,
h.subServer.AddSubscriber("backend", sub.AddHandler( opens at :72 and closes at :104. The live
(compiling) pubsub.EventHandler(h....) members inside are 21: OrgSkillPathAssigned ·
OrgSkillPathUnassigned · OrgSkillPathAssignmentCompleted · OrgSkillPathAssignmentPastDue ·
OrgSkillPathAssignmentDueDateUpdated · OrgJobSimulationAssigned · OrgJobSimulationUnassigned ·
OrgJobSimulationAssignmentCompleted · OrgJobSimulationAssignmentDueDateUpdated · OrgContentAssigned ·
OrgContentCompleted · OrganizationMemberInvitedHanler · MemberInvitationReminder ·
AIReadinessCompleted · AIReadinessMemberInvited · AIReadinessMemberReminder · AIReadinessCycleLaunched
· AIReadinessManagerDigest · CourseBuildCompleted · CourseBuildFailed · CoursePublished.

A 22nd, OrgJobSimulationAssignmentPastDueHandler at flow.go:83, is COMMENTED OUT (// not implemented),
so a naive grep -c returns 22 — the trap rule 24 names verbatim. Neither 21 nor 22 is 20. Cross-check:
the file's other two subscribers are jobsimulation (2 handlers, :105-108) and cms (1, :109-111);
21+2+1 = 24, matching an independent whole-file count. Measured 21 at BOTH legitimate refs — checkout
fa47850d AND origin/main e9421c68 — so no ref choice rescues it. The sentence names no ref. It was 19
five commits back and 11 fifteen back; 20 is not the value at any ref a reader can reach.

SECOND defect in the same clause: the anchor flow.go:70-95 TRUNCATES the list it is offered as
evidence for. :95 is the "// AI Readiness Notifications (M400 stubs...)" comment; the range covers
handlers 1-14 of 21. The block actually runs :70-104. A reader who opens the cited range and counts
gets a third number. Confidence: high.

## B2 — messenger.md:21-22 — the positive control does not reproduce

"git log -S MESSENGER_RPC over the platform's entire history returns 0 commits (positive control:
-S SKILLER_RPC returns 3)"

The load-bearing half is CORRECT — git -C stack-demo/platform log --oneline -S MESSENGER_RPC | wc -l
-> 0, and grep -rl MESSENGER_RPC_ADDR stack-demo/ -> 0 across all 13 clones. The CONTROL value is not.
At platform 0c91421d: git log --oneline -S SKILLER_RPC | wc -l -> 7 (838d907, d11a403, 236771f,
b43b99a, c972512, ec14fc4, 688dcc5); with --all -> 8. I tried every reading that could plausibly yield
3 before booking: -S SKILLER_RPC_ADDR -> 7; at 0dab54d -> 6; at 2adcf71 -> 5; in the app clone -> 26.
NO ref, no pattern spelling and no clone gives 3.

Why not cosmetic: a positive control is the one figure whose entire job is to let the next reader
confirm their pipeline is not broken. Re-running it returns 7, the reader concludes their measurement
differs from the corpus's, and the control manufactures exactly the doubt it exists to remove. A wrong
control is a broken instrument, not a typo. Confidence: high on the arithmetic; hesitated because the
substantive claim is true and independently verified. Booked per the briefing's "Wrong counts" ->
BLOCKER and its resolve-doubt-upward instruction.

## B3 — coursebuilder.md:130-131 — asserts CURRENT version

"current app v1.363.2 @ 5ba17044 (2026-07-31)."

The pin itself is sound — git show 5ba17044:VERSION -> v1.363.2. The word CURRENT is what fails. The
app checkout is b948604f = v1.366.0, and origin/main is 2035f9a4, 93 commits further on and past
v1.367.0 (the v9.0 storage+messenger fold this same corpus documents as landed). So v1.363.2 is three
minor versions behind the tree on disk and further behind origin.

Why a pin does not rescue it: this is rule 33, mechanism 3 — "the pin is cited as evidence of
currency" — and the rule is explicit that a block asserting currency cannot be pinned into silence,
because the pin and the word are making opposite claims and only one of them is what the reader acts
on. Confidence: medium; a one-word repair (current -> as measured at) fixes it entirely.

## Minors

- alignment_testing.md:527-535 — Layout lists internal/{dna,outcome,compare,report}; five internal
  packages exist — canon omitted. canon implements the canonical-JSON claim at :120-121.
- alignment_testing.md:529 — dna.go:20-26 offered for run|capture|dna list|diff|validate|coverage;
  :20-26 runs case "list": -> case "coverage":, whose dispatch returns at :27. Off-by-one at the tail.
- alignment_testing.md:3 — Last updated 2026-06-06 while the body documents M218/M219 and an iter-85
  correction. Stale header only.
- clerkenstein.md:3 — Last updated 2026-07-14 while the body carries M256 iter-16/25 and M257x iter-23.
- clerkenstein.md:20-21 — names 6 sections; the authoring copy at 6130bfd8 holds 11.
- clerkenstein.md:275-277 — storage left repos.yml and compose at 838d907, so it is no longer part of
  "the whole platform". Conclusion survives; its membership set has moved.
- ai_architecture.md:232 — AIModel const block runs :979-991 with members at :980-990; cited range
  starts :983 and omits Anthropic35SonnetAws, Anthropic37SonnetAws, Anthropic4SonnetAws.
- ai_architecture.md:273 — calculateSkillScore cited :53-64; function is :53-62. Companion :75 exact.
- coursebuilder.md:114-115 — "~55"/"~25" test files measure 61 and 33. Sibling "~100 Go files"
  measures 98/102 — accurate.

## Positively cleared — ENUMERATED (condensed)

alignment_testing: all five DNA gene/capability counts parsed from JSON (clerk-2.6.0 14/27, clerk-js-5
6/9, clerk-multi-1 5/9, clerk-deploy-1 3/7, clerk-express-1 5/13) — all rows match; consumed_surface
present on exactly 1 of 5; six snapshot operators from validSnapshotOperators at the exact cited :62,
with the per-surface split at data-dna.json:410/:439 both landing on the "operators" key; ALL 10
surfaces enumerated by status, ZERO waived; four equivalence operators; id regex dna.go:21; weight
bound 1..1000000; --if-declared semantics verified in gate.sh:69 + dna.go:77-80; both zero-critical
guards; critical% unweighted vs overall weighted (compare.go:101-102); the toy example re-derived
after enumerating the DNA (SigmaW=15, 13/15=86.7%, critical 3/3, 5/6); the inert-CI passage all three
legs (exactly one workflow path, no root .github, the file's own :10-11 verbatim); ExitRegressed=2 /
ExitUnmeasurable=3; Validate-before-score at run.go:38; find corpus .claude -name '*.go' -> 0.

clerkenstein: BAPI/FAPI route table (exactly two registrations, in the two dirs claimed); the
single-tenant seat disclosure (one activeKey, one clientID/sessID, client_clerkenstein /
sess_clerkenstein in establishLocked) and the load-bearing absence r.Cookie( nowhere, control
confirming the grep works; five sticky-sign-out tests; store.go:138/:151 and clerk-2.6.0.json:131
verbatim; the colony pin drift box (app v0.35.2 + clerk-sdk-go v2.7.0, sentinel/storage v0.34.3);
STUDIO_ACCESS_ROLES line-exact at both cited studio-desk sites with the exact preceding comment; the
"not yet a measured gene" admission verified by enumerating every clerk-js-5 variant; M220's clerk-js
fix incl. the literal "Explicitly NOT http.DefaultClient"; dotless-pk-rejected, the demo control plane,
__clerk_identity, OrgIsHiring threading.

ai_architecture (~40 citations, densest in set): EVERY anchor opened resolved to the construct named.
Routing retraction (four vendor consts, getClient exactly :259-289, EU fallback on failed flag lookup,
AnthropicAws and Anthropic returning the SAME client built WithRegion("eu-west-1"), 429 path setting
vendor=Openai); app/internal/ai/ does not exist while jobsimulation/internal/ai/ai.go does; the pinned
@5ba17044 trio; unset-vendor route (five-member Directus enum vs four switch cases, Azureglobal the
uncased one); three-defaults table (six anchors exact); evaluation (checkerEngines stored and NEVER
indexed — tested as a predicate with control, dispatch is the hardcoded switch at criterion.go:126-127;
temp 0.0; skills.go:40-51 incl. the literal "// TODO fix this formula"; NO 60/65/75/85/95 ladder
exists, 0 hits); studio slots enumerated from the shipping configs; embeddings DDL both tables with
vector(1536) + IVFFLAT; voice (4-member enum, nil->gptrealtime, the flag read and its effect);
CreateSpeech Azure-only; Mistral OCR exactly two sites; cross-file consistency with
external_services.md:594-604 and :543 — the two files agree on mechanism, anchors and retraction. NO
self-contradiction found.

messenger (everything except B1/B2): the fold banner's app side, seven anchors seven exact at the ref
it pins. NEAR-MISS recorded in full: messenger.md:37 cites terraform/main.tf:29 service_desired_count
= 0; at the CHECKOUT that is false twice (:29 is container_definitions, and the count is at :19 = 1),
but at origin/main e9421c68 — the ref the banner three lines above names — :29 IS = 0 with :27-28 the
rollback comment VERBATIM. NOT booked: the clone is 7 commits stale, not a corpus defect. Same
disposition for app/env_guards.go:61. Also: root.go four env anchors line-exact; the five-clients /
four-addresses grouping (doc's own enumeration makes it explicit — not booked); zero *_RPC_ADDR at
0c91421d with control, and the historical four at 0dab54d read directly; the always-on floor DERIVED
by parsing both compose files rather than assumed; rpcsrv.go:25-30 both stubs; go.mod versions.

coursebuilder (except B3): model-backend selection — six exact anchors on one derivation; production
requirement (variables.tf:635-638 sensitive no default, ssm.tf:328-334, main.tf:555). CREDIT PRICES:
expected a blocker and found none — the authoritative creditCost map is {build:5, refine:1,
translate:1}, matching the doc exactly, while THE PLATFORM'S OWN doc-comment at cost.go:29 says refine
-> 5 and is wrong, contradicted by its own :63-67 ruling and by the map. Grading from the comment
would have booked a false blocker against a correct corpus. The defect is in the platform, not rosetta.
Storage migration creates exactly the three named tables; worker paragraph all five stated task
properties; caps and locales (50, 500.0, exactly 7 non-EN locales); the OPENAI_KEY correction
(COURSEBUILDER_OPENAI_IMAGE_KEY surviving in exactly two stale in-repo markdown files and ZERO .go);
all 14 route patterns + the five SSE event names verbatim; Go 1.26.4.

gotenberg — clean, and it is the file the last guard run flagged. The rule-42 RED citation
("cites docker-compose.yml:268") has been REPAIRED and is now correct: :14 cites :183 re-derived at
0c91421, and at 0c91421d line 183 is exactly `profiles: [core, backend, all]` inside the gotenberg
block. "the file is now 186 lines" is exact. Worth stating because a repaired anchor is the
highest-risk text and this one landed. Nine MIME types ENUMERATED from extract.go:17-27 = 9, and
wordprocessingml.document genuinely absent. Both call sites @9d00a313 discard the PDF as claimed.

db-backup.md — 31 lines, ZERO file:line citations, no clone in the ground-truth set. Nothing
falsifiable against available ground truth; does not contradict root CLAUDE.md. No findings, and no
clearance stronger than "internally consistent", which I am NOT counting as an audited zero.

## What I could not settle

1. skill_embeddings = 42,790 / job_role_embeddings = 18,919 (ai_architecture.md:138). Live-DB row
   counts. Would settle: /db-query SELECT count(*) WHERE organization_id IS NULL, re-dated. The doc is
   careful in the right direction — it calls 42,790 a FLOOR, the only claim a public capture supports.
2. The five LiveKit agent repos (:183-189) and the claim that azure-eu/azure-eu-fr "dispatch nothing".
   None is cloned. Would settle: gh repo list + a read of the two agents' dispatch config.
3. The skiller-era migration ids 20260417103036 / 20260417120309 (:151). skiller is decommissioned and
   not in the clone set. The SHAPE they are cited for I verified against the ported migration.
4. coursebuilder.md:130 "142 changelog lines" — could not recover the author's predicate. My grep
   gives 148, but that is MY predicate, not theirs. Not booked; stated so the next pass does not
   re-derive it from my number.
5. messenger.md:39's terraform row — settled at messenger origin/main but NOT at the clone on disk.
   Anyone re-reading must git fetch and grade at e9421c68 or they will book a false blocker. Would
   settle permanently: advancing the checkout, or the row carrying its own inline ref rather than
   inheriting the banner's.
