# Seat F — reading #18

**Refs used** (re-derived at open, 2026-08-05, matching the addendum sheet):
`platform` `0c91421d` (= origin/main) · `app` `b948604f` (origin/main `2035f9a4`) ·
`next-web-app` `bb3313bc` (origin/main `8297c684`) · `sentinel` `88bc5592` · `storage` `4ce8ece5` ·
`messenger` `fa47850d` · `cms` `ca50c817` · `jobsimulation` `462343b0` · `roadrunner` `87d8d443` ·
`graphql-wundergraph` `60c229f3` · `studio-desk` `14a5442a` · `ant-academy` `9c3843cd` ·
`rosetta-extensions` (authoring, `.agentspace/rosetta-extensions`) `6130bfd8`.
Shared Go libraries read from the local module cache: `colony@v0.34.3`, `ai@v1.40.1` (the only two
present; `proto` and `taxonomy` are **not** on disk anywhere — see *What I could not settle*).

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/services/ant-academy.md` | 448 | yes |
| `corpus/services/studio-desk.md` | 435 | yes |
| `corpus/services/chronos.md` | 245 | yes |
| `corpus/architecture/shared_libraries.md` | 242 | yes |
| `corpus/services/askengine.md` | 124 | yes |
| `corpus/services/skillpath.md` | 112 | yes |
| `corpus/ops/platform-alignment.md` §5 (in-scope instrument) | 2139 whole file; §5 = 249–1360 | yes |

**Search-pipeline positive control (§5 rules 1–4).** My first attempt to enumerate every `- name:`
ever written into `platform/repos.yml` returned **0** across all 11 commits while the same regex
returned 4 on HEAD. Two independent pipeline faults, both caught only by running the control:
BSD `grep -E` does not honour `\s` (fixed to `[[:space:]]`), and **zsh applied the `:r` history
modifier to `"$C:repos.yml"`**, silently turning the rev-spec into `<sha>epos.yml`. Every count in
this report was taken after the pipeline was proven against a known-positive input, and no
"absence" below rests on an unread stderr.

---

## Blockers

### B1 — `shared_libraries.md` names a Postgres table that does not exist (`ai_usage` vs `ai_usages`)

- **Anchor:** `corpus/architecture/shared_libraries.md:122` — *"Dollar cost is computed by the
  **consumer** in `app/internal/aiusage/ai_usage.go` (a hardcoded model→price switch) and written to
  the `ai_usage` Postgres table, fed by an `Event_AiUsage` published over Redis Streams."*
- **Why it is false:** the table is **`ai_usages`** (plural).
  `app/terraform/migrations/20250611092754.sql:1-2` — `-- Create "ai_usages" table` /
  `CREATE TABLE "ai_usages" (...)`. Derived independently:
  `grep -rhoE 'CREATE TABLE "[a-z_]*ai_usage[a-z_]*"' app/terraform/migrations/*.sql | sort -u`
  returns exactly one row, `CREATE TABLE "ai_usages"` — there is no singular table anywhere in the
  migration set. The **file** `app/internal/data/ent/schema/ai_usage.go` is singular (Ent schema
  files are), which is precisely the trap; the emitted table is pluralised.
  Everything else in the sentence checks out: `app/internal/aiusage/ai_usage.go:36` is the
  `switch model {` price table, `:98` is `AiUsageHandler(ctx, event *eventsv1.Event_AiUsage)`, and
  `Event_AiUsage` is published at `app/internal/jobsimulation/ai/ai.go:207`,
  `app/internal/skillerai/ai.go:236`, `app/internal/jobsimulation/agent/report_agent.go:636`.
  **It also self-contradicts:** `corpus/ops/db-access.md:33` writes the correct
  `public.ai_usages`, and `corpus/services/ant-academy.md:63` states the general rule this sentence
  breaks — *"(**plural** — Ent pluralizes; the singular forms are schema-file names, not table
  names)"*.
- **Why I hesitated:** it is a one-character difference. I booked it because the briefing lists
  *"a named service/table/schema/env-var/port that does not exist"* as a BLOCKER, because a reader
  doing real work types the name into `psql` and gets nothing, and because the corpus already
  carries an explicit warning about this exact class in a sibling file — a half-applied rule, §5
  rule 19's signature.
- **Confidence:** high

### B2 — the vendor-selection wrapper is cited at a path that does not exist in the live monolith

- **Anchor:** `corpus/architecture/shared_libraries.md:125-126` — *"Vendor selection lives in each
  consumer's own `internal/ai/ai.go` wrapper: an EU Azure client by default, a US Azure client gated
  by the PostHog flag `flag_use_azure_us`, and an Azure→direct-OpenAI fallback on HTTP 429."*
- **Why it is false:** `app/internal/ai/` **does not exist** (`ls app/internal/ai` →
  *No such file or directory*, at `app b948604f`). Enumerated: the files named `ai.go` in `app`
  (excluding vendor/node_modules) are exactly **three** — `app/internal/skillerai/ai.go`,
  `app/internal/jobsimulation/ai/ai.go`, `app/internal/jobsimulation/simulator/ai/ai.go`. The
  mechanism the sentence describes is real and lives in the first two:
  `app/internal/jobsimulation/ai/ai.go:259-289` (`getClient`, EU Azure default →
  `flag_use_azure_us` swaps in `azureClientUs`), `:340-352` in `AudioTranscriptions`,
  `:295-297` (`if throttled { vendor = Openai }`), and `app/internal/skillerai/ai.go:347` for the
  same flag. Of the seven Go clones on disk, the only one holding an `internal/ai/ai.go` is the
  **frozen** `jobsimulation` repo — a repo this very document says (`:105`, `:145`, `:181`) nothing
  builds or starts. The corpus's own canonical statement of the same fact gives the right path:
  `corpus/architecture/external_services.md:553` — *"The real mechanics, all in
  `app/internal/jobsimulation/ai/ai.go`"*.
- **Why I hesitated:** the path is written bare (no repo prefix) and "each consumer's own" can be
  read as describing the pre-merge, per-service shape. I booked it because the sentence is in the
  present tense inside a block headed *"Two corrections to long-standing corpus wording"*, and
  because it sends a reader to a directory that is not there in the only service that still does this.
- **Confidence:** medium-high

### B3 — "12 Connect-RPC services are defined" — the set omits `StorageService`

- **Anchor:** `corpus/architecture/shared_libraries.md:77-82` — *"12 Connect-RPC services are
  defined: `UsersService`, `OrganizationsService`, `CMSService`, `JobSimulationService`,
  `SkillerService` …, `SkillPathSessionService` …, `LabSessionService` …, `AuthorizationService`
  (Sentinel), `MessengerService`, `RoadRunnerService`, `RealtimeService`, `ChronosService`"*
- **Why it is false:** the **arithmetic is right** — the list has exactly 12 members — and the SET is
  incomplete, which is §5 rule 24's exact shape. `proto` defines a **`StorageService`**:
  - `storage/cmd/root.go:13` imports `github.com/anthropos-work/proto/go/storage/v1/storagev1connect`
    and `:63` serves `rpcMux.Handle(storagev1connect.NewStorageServiceHandler(...))`;
  - `storage/sdk/storage/client.go:6,11,28` and `storage/sdk/storage/v1/service.go:9,19,29,34` build
    `storagev1connect.NewStorageServiceClient` / take a `storagev1connect.StorageServiceClient`;
  - `app/internal/app/users/services/import_profile_test.go:25,158` declares a
    `storagev1connect.StorageServiceClient`.
  Derived independently (not from the list): the distinct
  `v1connect.New<X>Service{Handler,Client}` symbols across all seven Go clones are **10** —
  `Authorization, CMS, JobSimulation, LabSession, Messenger, Organizations, RoadRunner, Skiller,
  **Storage**, Users` — and `Storage` is the one the document's 12 never names. So the true
  cardinality is **≥ 13**, not 12.
- **Why I hesitated:** `proto` is not on this box (no clone, no module cache, no vendor dir), so I can
  prove the omission but not the true total. Also `storage` has just left `repos.yml` — but the row
  enumerates service **contracts**, and it explicitly includes two (`ChronosService`,
  `RoadRunnerService`) whose services are gone, so "no longer deployed" is not the exclusion criterion.
- **Confidence:** medium

### B4 — the "live Go service" set still names `storage` and `messenger`, which left the platform at `838d907`

- **Anchor:** `corpus/architecture/shared_libraries.md:70` — *"**Imported by** | every live Go service
  that does RPC (app, sentinel, storage, messenger)."* Same-predicate twin at `:42` — *"**Every**
  live Go service: app, sentinel, storage, messenger — and **only** those four."*
- **Why it is false:** at the graded platform checkout `0c91421d`, `838d907`
  (*"chore(compose): drop the storage, messenger and customerio-sync containers"*) removed both from
  `repos.yml` **and** deleted their compose services. Measured directly:
  - `git show HEAD:repos.yml | grep -E '^[[:space:]]*- name:'` → **`app, sentinel, next-web-app,
    studio-desk`** (4). Per-ref series: **9** @ `2adcf71` → **6** @ `0dab54d` → **4** @ `838d907` = HEAD.
  - compose services @ HEAD = `sentinel, backend, studio-desk, next-web-app, gotenberg`
    (+ `postgresql`, `redis` from `common.yml`); `docker compose --profile core config --services`
    returns exactly `postgresql redis sentinel backend gotenberg`.
  Applying the document's **own** definition of the other side of the line (`:42`: *"frozen legacy —
  still on GitHub as the pre-merge reference, but with no compose service, no `repos.yml` entry and
  nothing that starts them"*), `storage` and `messenger` now satisfy it exactly. The neighbouring
  cells that carve the date into the claim survive and are true at their pins (`:41`, `:69`, `:181`
  all say *"at platform `0dab54d`"*); these two do not.
- **Why I hesitated (a real hesitation):** the `:42` cell does contain the token `0dab54d`, so under
  §5 rule 33's *"a pin's scope is the claim's own block — a markdown CELL"* it is arguably exempt.
  Two things pushed me to book anyway: (i) `:70`'s **only** pin is `@ app b948604 v1.366.0`, an
  `app` ref, which cannot date a claim about `platform`'s clone set — rule 33's *"the pin crosses a
  cell boundary"* mechanism; and (ii) both blocks assert **currency** (*"every **live** Go service"*,
  *"a stack still clones and builds"*), and rule 33's fourth part is explicit that a block asserting
  currency *"cannot be pinned into silence"*. If the adjudication reads the `:42` pin as covering its
  cell, `:70` still stands alone.
- **Confidence:** medium

---

## Minors

- `corpus/services/ant-academy.md:135` — quotes the empty view as
  `emptyCatalogView() = { chapters: [], skillPaths: {}, series: [] }`. The literal at
  `ant-academy/code/src/lib/serverTenant.js:116` is
  `{ chapters: [], skillPaths: {}, series: [], bundles: PUBLIC_BUNDLES, catalogVersion: CATALOG_VERSION }`.
  The "0 cards" conclusion is unaffected (`BundleStripe` returns `null` on an empty `paths`), but the
  quotation is presented as an equality and is missing two keys.
- `corpus/services/ant-academy.md:49` — layout comment `│   ├── tools/  # offline-parity CLI`.
  `ant-academy/code/tools/` contains exactly one file, `apply-v3-metadata.mjs`, whose header says it
  *"Adds sidebarCategory, sidebarSubcategory, and audiences fields to every skill path in catalog.js"*
  — a catalog-metadata backfill, not an offline-parity tool (offline was removed at v0.5 M1).
- `corpus/services/ant-academy.md:258` — *"~26 Playwright e2e spec files (tests/e2e/)"*; measured
  **31** tracked `code/tests/e2e/*.spec.js`. The sibling *"1000+ Vitest tests"* is a safe floor
  (2,701 `it(`/`test(` call sites).
- `corpus/services/ant-academy.md:144` — *"`code/public/catalog.json` (~2,667 entries)"*; the file on
  disk holds **2,715** `courses` entries. It is a build artifact (`predev`/`postinstall` regenerate
  it), so the figure drifts with content — it wants a date, not just a `~`.
- `corpus/services/ant-academy.md:63` — describes progress as *"posted from
  `code/app/api/academy/beacon/route.js`"*. That route exists and does carry
  `UPSERT_CHAPTER_PROGRESS` / `SET_LAST_ACTIVITY`, but its own header calls it *"the on-unload
  last-ditch write flush … best-effort … if the tab closes mid-retry"* — the normal path goes
  straight to the supergraph. Naming the beacon as *the* post site sends a debugger to the fallback.
- `corpus/services/ant-academy.md:336`/`:357` say the app env file is `code/.env`; root `CLAUDE.md`
  says *"`ant-academy/code/.env.local` (not the repo root — the React app reads only from
  `code/.env.local`)"*. The repo's own `.env.example` agrees with ant-academy.md (*"NOT for the React
  app — that lives in code/.env"*), and Next.js loads **both**, so *"reads only from"* is wrong and
  the two corpus files disagree. No functional consequence either way.
- `corpus/services/askengine.md:104-109` — lists four `_test.go` files in `internal/askengine/`; there
  are **five** (`anthropic_test.go`, 1 test, unlisted). The four counts given are exact (49/13/10/14)
  and the passage does not claim to be exhaustive.

---

## Positively cleared (audited zeros)

**ENUMERATED** — predicate re-derived from source independently, cardinalities compared:

1. `shared_libraries.md:181` *"that set is 11"*. Derived over **all 11 commits** touching
   `repos.yml`: the union of `- name:` is **14** (matching §5 rule 37's *"14 ever"*), of which
   `type: go` is exactly **11** — app, chronos, cms, intelligence, jobsimulation, messenger,
   roadrunner, sentinel, skiller, skillpath, storage. Also confirmed: all four of
   skiller/skillpath/chronos/intelligence are present in the first revision `a2a3ee6`, and
   `skillpath` is the last of the four to go (`a4db680`, newer than `045857c`/`fdfa189`/`21429b7`).
2. `shared_libraries.md:181` *"6 of 7"*. Go clones on disk = 7; `taxonomy` in `go.mod`: app (direct),
   messenger (direct), sentinel (`// indirect`), storage (`// indirect`), cms `go.mod:13` (direct),
   jobsimulation `go.mod:15` (direct), roadrunner **absent** → 6 of 7, and roadrunner's requires are
   colony+proto only.
3. Every shared-library pin in `:41`, `:69`, `:104`, `:105`, `:180`, read from all seven `go.mod`s:
   colony `app/messenger v0.35.2`, `sentinel/storage/roadrunner v0.34.3`, `cms/jobsimulation v0.35.1`;
   proto `1.210.0 / 1.200.0 / 1.196.0 / 1.207.0 / 1.205.0 / 1.196.0`; ai `v1.40.2` in
   app+cms(`:9`)+jobsim(`:11`); taxonomy `v1.2.0`. Every cited line number lands on the cited require,
   including `roadrunner/go.mod:7` and `roadrunner/main.go:7` (colony imported for `NewVersionConfig`,
   used once, at `main.go:17`).
4. `shared_libraries.md:70` *"six Connect handlers … five unconditionally"*.
   `grep -rn "v1connect.New[A-Za-z]*ServiceHandler" app --include='*.go'` returns exactly six
   registration sites at **`:1187, :1188, :1196, :1204, :1213, :1227-1228`** — Users, Organizations,
   Skiller, JobSimulation, CMS (inside `if cmsRPCServer != nil` at `:1212-1214`), LabSession.
   `SkillPathSessionService` = **0** occurrences in `app` Go source; `ROADRUNNER_RPC_ADDR` = 0 in
   `app` at both `b948604` and `9d00a313`, and 0 in `docker-compose.yml` at `0dab54d` and HEAD;
   `0dab54d:docker-compose.yml:118` is indeed `- AWS_REGION=eu-west-1`.
   `app/internal/skillpaths/skillpaths.go:27-31` carries the quoted *"drop-in for the removed
   skillpath RPC client"*; `jsrunner.NewRunnerManager` is at `internal/jobsimwiring/wiring.go:123`
   @ `9d00a313` exactly.
5. `shared_libraries.md:145` *"6 and 8 `.go` files"* importing `colony/authn` in cms / jobsimulation —
   measured exactly 6 and 8. Standalone `github.com/anthropos-work/authn` imported by **zero** files
   across all seven clones; `sentinel` references `authn` nowhere; `gqlauthz.go` exists at
   `app/internal/authorization/gqlauthz/gqlauthz.go`. colony/authn itself: `NewManager(providers ...Provider)`
   at `authn/authn.go:14`, exactly two providers (`provider/clerk`, `dummy`), claim consts
   `eid`/`firstname`/`org_role` at `provider/clerk/clerk_user.go:27-32`, `AuthID()` in the `User`
   interface, and `net/http` + Echo + GraphQL entry points (`authn/http.go`, `authn/echo/`,
   `colony/graphql.go`).
6. `askengine.md:47-48` *"**60** `TableDef`s, **57** `public`, **3** `directus`"*. Re-derived the SET,
   not the sum: `grep -o '{Name:' registry.go | wc -l` = **60** entries in the `defs := []TableDef{`
   literal (`registry.go:96`), zero commented out; `Schema:` values = 57 `"public"` + 3 `"directus"`.
   (Note the trap I avoided: a bare `grep -c 'Name:'` also matches `RealName:`.) The 16 dotted
   `jobsimulation.*` names all carry `Schema: "public"`, confirming the alias claim; the anchor
   `registry.go:183-195` lands exactly on the comment block reading *"logical aliases that RESOLVE TO
   THE PUBLIC TABLES"*, and `skiller.*`/`skillpath.*` aliases are built at `:551-576` with
   `rewriteSchemas` at `sandbox.go:293-311`.
7. `askengine.md` route list — all 11 `/ask/*` routes match
   `app/internal/web/backend/backend.go:171-188` one-for-one, including the four admin auto-rules
   routes and the group's CORS + Clerk-authn middleware. `ValidateSQL`'s six rejections
   (`ErrSetStatement, ErrOnlySelect, ErrDataModification, ErrSystemCatalog, ErrSchemaPrefix,
   ErrMultiStatement`) match the prose. Constants: `maxAgenticIterations = 15` (`handler.go:34`),
   `loopTimeout = 10 * time.Minute` (`:41`), `context.WithoutCancel` (`:248`), `runAgenticLoop`
   (`:466`), `MaxInlineRows = 200` / `MaxCellLength = 400` / `QueryTimeout = 10 * time.Second`
   (`executor.go:18,21,24`), `askEngineMaxConns = 6` (`main.go:150`), `COPILOT_DB_CONN` (`:313`),
   `DefaultModelID = "eu.anthropic.claude-sonnet-4-6"` / `DefaultRegion = "eu-west-1"` /
   `stripAnthropicAuthMiddleware` / `Temperature: param.NewOpt(float64(0))` (`bedrock.go:25,26,185,315`),
   `rules.md` = 146,219 bytes (*"~146 KB"*). **`WrapQuery`'s doc-comment enumerates four bullets — the
   corpus's "four documented bypass vectors" is right and the source's own word "three" is the stale
   one.** All five `ask_*` tables exist in the migrations.
8. `askengine.md:104-109` test counts — `grep -cE '^func Test'`: sandbox 49, executor 13, prompt 10,
   followups 14, all exact; `RulesBlockIsIdentical` at `prompt_test.go:176`; the eight handler-package
   test files named (sanitize, stream_registry, examples, lessons, distill, feedback, store, embed)
   all present.
9. `askengine.md:116-117` ship versions — `git tag --contains` on the feature commits: `askengine`
   first appears in **v1.267.0** (tagged 2026-05-07) and `coursebuilder_tool.go` first in **v1.340.0**
   (tagged 2026-07-17). Both dates exact.
10. `skillpath.md:47` repos.yml counts — **9** @ `2adcf71` (0 skillpath *entries*; the single
    `skillpath` hit there is a comment on line 4), **6** @ `0dab54d`, **4** since `838d907`.
    `SKILLPATH_STREAM=skillpath` survives at `docker-compose.yml:72`.
11. `skillpath.md:39-46` — all **8** named GraphQL operations exist in
    `app/.../schemas/skillpath_sessions.graphqls:71-82`, and `createSkillPathSession` really carries
    `@deprecated(reason: "Use upgradeSkillPathSessionToLatest instead")`.
12. `skillpath.md` app anchors — `internal/skillpath/session.go:205-207` is exactly the
    *"cms-in-app deseam: cms is in-process"* comment plus `u.cms.GetSkillPathDomain(...)`;
    `internal/skillpaths/skillpaths.go:88-95` is its twin; `contentread.CmsContentReader` is a real
    type; `internal/organization/intelligence.go:1144` is
    `func (m IntelligenceManager) InsightsSkillPathByMemberships(...)` and `:1159-1170` is the
    `m.ent.SkillPathSession.Query()` with `StatusIn(Active, Completed)` + the tenant predicate;
    `terraform/migrations/20260729133514.sql:62`/`:63` are exactly
    `DROP TABLE "local_jobsimulation_sessions";` / `DROP TABLE "local_skill_path_sessions";`;
    **exactly three** migrations post-date it at `9d00a313`, with exactly the three quoted names;
    no `local_*` Ent schema exists. `app/CLAUDE.md:109` and `app/knowledge/architecture.md:28`
    @ `2035f9a` do still list `SkillPathSessionService` — the doc's "planning docs lag the code"
    note is correct at the ref it names. `apps/hiring` has no skill-paths tab (route tree checked).
13. `skillpath.md:97-104` — at `next-web-app origin/main`, `InsightsBySkillPathStudentSimulationsContainer.tsx`
    has `userData` as a `useMemo` returning `null as unknown as MembershipEnriched` (`:31-33`), its
    results table and totals block commented out (`:140`, `:150-160`), and renders
    `t('enterprise.insights.comingSoon')`, which `configs/i18n/messages/en/enterprise.json:1891`
    resolves to **"Coming soon"** — so the corpus's *"renders the literal string"* is correct about
    the rendered output (it is an i18n key in source).
14. `studio-desk.md:21` compose anchors, at three refs: @ `0c91421d` `depends_on: backend` at
    `:138-140` and `profiles: [studio-desk, all]` at `:141`; build-arg `VITE_GRAPHQL_ENDPOINT` at
    `:119` and the runtime copy at `:135`. @ `0dab54d` the same pair is at `:223-225`/`:226` and the
    build arg at `:204`. @ `2adcf71` `depends_on` at `:337-341` **did** list `cms` alongside `backend`.
    All six anchors resolve to exactly the named construct.
15. `studio-desk.md` repo facts — Node `>=24` + `node:24-alpine` (both Dockerfiles); the nine prod
    Vite entries in `rollupOptions.input` match one-for-one with `dev-accept` gated on
    `!isProduction`; `src/routes/` = ai/dev/skillpath/youtube with `skillpath.ts` at 61,080 bytes
    (*"~61KB"*); prompt dirs start/, sim-advanced-builder/, sim-guided-builder/, builder-skill-path/,
    documents/ + loose `*.md`; `src/routes/youtube.ts:43` is `const apiKey = process.env.YOUTUBE_API_KEY;`
    with the `_mock: true` fallback; `GCLOUD_SERVICE_ACCOUNT` at `.env.example:120` and
    `terraform/main.tf:129`, read by **zero** files in `src/`; `STUDIO_ACCESS_ROLES` at `src/index.ts:96`
    with exactly the four quoted values and `/api/ai`, `/api/skillpath`, `/api/youtube` plus the
    builder/catalog/skills pages all behind `authMiddleware, adminMiddleware`; PORT in-code fallback
    9100 (`src/index.ts:60`); AI tiers + `gpt-5.2/gpt-5-mini/gpt-5-nano` and
    `claude-opus-4-5/sonnet-4-5/haiku-4-5` in `src/services/ai/config.ts:20-38`, `.env.example`
    `AI_DEFAULT_TIER=fast` vs in-code `'thinking_fast'` (`config.ts:182`); **no** DB driver in
    `package.json` and no `DATABASE_URL` read; `GET_/SET_USER_STUDIO_PREFERENCES` in
    `app/services/graphql/{queries,mutations}.ts`; `app/core/main.ts:105` is the GlitchTip
    tailscale-funnel comment and the repo's only tailscale mention (2 lines, both in that block);
    boot order `preloadCriticalCSS()` at `:97` and `new PageWrapper()` at `:206`; the three
    hardcoded `https://app.anthropos.work` sites at `pageWrapper.js:149` and `userProfile.js:148,302`;
    7 l12n languages (de,en,es,fr,it,ja,nl); all five studio demopatch dirs and
    `stack-verify/e2e/run-studio-fcp.sh` present in the authoring rext copy; the router still runs in
    prod (`graphql-wundergraph/terraform/main.tf:20` `service_desired_count = 1`).
16. `ant-academy.md:262-279` — the whole `isPublic` matcher table, group by group, against
    `code/proxy.js:112-187`. Every pattern present, no pattern invented, both dev-only blocks and
    their gates correct (`DEV_LOGIN_ENABLED`; `BENCHMARK_VISUAL_BYPASS === "1" && NODE_ENV ===
    "development"`, `proxy.js:62-64`). `code/app/(public)/` holds exactly `library/page.jsx` and
    `free/page.jsx` — **no `/library/[slug]`** ✓.
17. `ant-academy.md:196-198` — the **five** demo patches wired by `demo-stack/ant-academy.sh` at
    exactly `:56, :66, :75, :84, :90`; all five patch dirs exist; autoverify assert *(f)* is at
    `stack-verify/live/autoverify.sh:459-523` and does exactly what the doc says (including the
    documented `/library`-only blind spot it was widened to cover).
18. `ant-academy.md` repo facts — port 3077 (`package.json` `dev`), 8555 (`mobile/scripts/dev-server.sh:7`),
    Next 16 / React 19.2 / Expo ~54.0.33, `engines.node >= 22`, `vercel.json` = `{"framework":"nextjs"}`,
    `manifest: "/academy-manifest.json"` at `app/layout.jsx:132` with `display: standalone`, Serwist
    absent from `package.json` with `RegisterServiceWorker.jsx` a kill-switch and a regression test at
    `tests/integration/RegisterServiceWorker.test.jsx`, `serverTenant.js:115-145` containing the
    verbatim *"the cutover is intentional, not reversible-on-error"*, `getServerCatalogView()` being
    literally `(await getBackendCatalogView(eids)) ?? emptyCatalogView()`, `serverChapterBody.js:51-67`
    returning `{ notFound: true }` and `app/not-found.jsx:43` reading *"You wandered off the trail."*,
    `SUPPORTED_LOCALES` (7) at `src/i18n/locale.js:10`, `LanguageSelector` mounted at `TopBar.jsx:76`
    with `role="menu"` + `role="menuitemradio"`, `LocaleSwitch` at `PublicHeader.jsx:20`, and **both
    provenance commits exact** — the dropdown is in `TopBar` at `5b05b7d9` (2026-05-05) and 7-locale
    at `e22f3230` (2026-05-18). Cosmo: `gpt-5.2` + `https://api.openai.com/v1/responses` +
    `localStorage('openai_api_key')` + `NEXT_PUBLIC_FEATURE_TRAINING_COACH`. `academyCertificate` is
    confirmed in `app` as *"the ONLY @public, unauthenticated GraphQL field"*
    (`verify_ratelimit.go:16,34`); `academy_chapter_progresses` + `academy_last_activities` exist
    (migration `20260603182114`); `cmd/academy-seed` exists; `ensure-clones.sh` phase d2 clones
    ant-academy explicitly and non-fatally (`:150-168`); ant-academy is in **no** revision of `repos.yml`.
    All 16 named repo-local skills exist; the three named workflows exist; `cog.toml` present.
19. `chronos.md:9` — `045857c` is *"chore: remove chronos service from orchestration and update
    related documentation"* (platform, 2026-04-17) and jobsimulation `09631fb2` is *"refactor: remove
    Chronos references and update documentation to reflect Asynq integration for session timeout
    management"* (2026-04-17), both verbatim. `chronos` appears in **zero** `*.yml`/`Makefile` lines
    in `platform` at HEAD, and `app/internal/jobsimwiring/worker.go:24-35` spans the
    `"github.com/hibiken/asynq"` import through `StartWorkers` and its first `asynq.NewServer`.
20. `skillpath.md:59` *"No compose file sets any `*_RPC_ADDR` now"* — `grep -c '_RPC_ADDR'` = **0** in
    both `docker-compose.yml` and `common.yml` at `0c91421d`; the one surviving service address is
    `AUTHORIZATION_ADDRESS=http://sentinel:8087` at `docker-compose.yml:48`.
21. `shared_libraries.md:23-31` — none of the five libraries appears in any revision of `repos.yml`
    (absent from the 14-name union) and none is a compose service; `app/Dockerfile.dev:4,11,13`
    carries `ARG GH_ACCESS_TOKEN`, the `url.insteadOf` rewrite and
    `ENV GOPRIVATE=github.com/anthropos-work/*`; `docker-compose.yml:12,39` pass
    `GH_ACCESS_TOKEN: $GH_PAT`; no `go.work` is committed in any service clone.
22. `shared_libraries.md:41-42,105,145,181` on `d11a403` — verified by diff: at `d11a403^` `repos.yml`
    named 9 and compose had `jobsimulation`, `cms`, `roadrunner`; at `d11a403` all three are gone from
    **both**. Every corpus sentence attributing that removal to `d11a403` is correct.
23. `colony` surface (at `v0.34.3`) — `authorization/` package name really is `authorizer`; the rate
    limiter `rate.NewLimiter(1, 5)` (`graphql.go:239`) has **both** reject paths commented out, so it
    only logs; Sentry gated on `!IsProduction() && FORCE_SENTRY == ""` (`logging.go:47`); the DB
    monitor is a `30 * time.Second` ticker returning after `errCount >= 3` (`db.go:46,61`); `/_meta`
    at `http.go:20`; PostHog `https://eu.i.posthog.com` + `pollingInterval = 5 * time.Minute`;
    `PoisonQueue` + `MaxRetries: 3, Multiplier: 3`; redis `UniversalClient` + startup `Ping`;
    `h2c.NewHandler` + `DefaultInterceptors`.
24. `ai` surface (at `v1.40.1`) — the `ai.AI` interface at `ai.go:8-18` has exactly the nine methods
    listed; the four constructors match; Azure default API version `2025-04-01-preview`; Anthropic
    `ChatCompletionStream`/`CreateSpeech` `panic` (`:177`, `:186`) while `CreateEmbeddings` returns an
    error; Mistral chat/stream/embeddings/speech all `panic` (OCR only); the `{"` JSON prefill
    (`:106`, `:143`); `retry.Attempts(10)`. `go 1.25.0` in both `ai` and `colony`.
    `storage/internal/migration/migration.go:13` imports `go/simulator/storage/v1` as `legacyStorage`.

**MEASURED (live command, not a source read):**

25. `studio-desk.md:21` *"Asking for `studio-desk` as the only profile exits 1"* with the quoted error.
    Invocation: `docker compose --profile studio-desk config --services` run in
    `stack-demo/platform` at platform `0c91421d` → **exit 1**, stderr
    `service "studio-desk" depends on undefined service "backend": invalid compose project` —
    byte-identical to the doc's quotation. `--profile frontend` also exits 1; `--profile core` exits 0
    and selects exactly `postgresql redis sentinel backend gotenberg`.

**RE-DERIVED (arithmetic only — the weaker clearance):**

26. `shared_libraries.md:200-213` taxonomy figures. **42,790** public skills / **22,470** public job
    roles / **18,919** `job_role_embeddings` reproduce **exactly** against
    `.agentspace/snapshots/taxonomy/5afc0bcc…/manifest.json`, whose header is `source: primary-read`,
    `public_only: true`, `predicate: org-null`, `captured_at: 2026-06-29T14:43:00Z` — all four
    provenance fields as quoted, and each table row carries the literal `organization_id IS NULL`
    filter. **This is a re-derivation against the corpus's own captured artifact, not an independent
    read of production**, so it is labelled RE-DERIVED, not ENUMERATED.

---

## What I could not settle

- **`proto` and `taxonomy` source is not on this box** — no clone, no `GOMODCACHE` entry
  (`~/go/pkg/mod/github.com/anthropos-work/` holds only `ai@v1.40.1` and `colony@v0.34.3`), no vendor
  dir in any service. So I could not enumerate the true Connect-service cardinality behind B3 (I can
  prove ≥13, not the exact number), nor check `proto/buf.yaml`'s module name, the absent
  `buf.work.yaml`, the CI `buf v1.57.0` pin, the *"~10 generated packages in `go/simulator/*` with no
  source `.proto`"*, nor any taxonomy claim (`131-line node.go`, README title *"nodeid"*, zero
  external deps, `go 1.21.0`, the `^[CSKJI]-[0-9A-Z_]{6}-[0-9A-F]{4}$` format and the three worked
  examples). **What would settle it:** clone `anthropos-work/proto` and `anthropos-work/taxonomy` at
  the pinned versions, or `go mod download` in `app`.
- **`colony@v0.35.2` was not readable** — only `v0.34.3` is cached, so every colony package clearance
  above is at the pin `sentinel`/`storage` use, not the one `app`/`messenger` use. Nothing I read
  looked version-sensitive. Likewise *"built with `golang:1.26-bookworm`"* (`:40`) is unverifiable:
  the module zip carries no Dockerfile. `(archived chronos pins v0.30.1)` is likewise unread.
- **The taxonomy scale figures are cleared only against the corpus's own snapshot manifest.** The
  manifest and the document agree exactly on all three numbers and all four provenance fields, and
  the *"REFUTED / UNVERIFIED"* split is internally coherent (a public subset is a floor, so
  18K < 22,470 is refuted while 60K is merely unsupported). What I could **not** do is re-run
  `select count(*) … where organization_id is null` against a live database, so I cannot rule out that
  the manifest itself is wrong. **What would settle it:** `/db-query` against prod read-only.
- **`chronos.md`'s body (lines 13–245) is unverifiable here** — no `chronos` clone exists in any
  `stack-*` dir, so Go 1.25, sqlc-not-Ent, Cobra, `search_path=chronos`, ports 8080/8081, the `timers`
  DDL and its four indexes, the `LIMIT 10` + `FOR UPDATE SKIP LOCKED` triggerer, the 3-second run
  timeout, the Sentry cron heartbeat, and the env-var/CLI-flag tables are all unread. The
  decommission banner itself **is** verified (cleared item 19). **What would settle the rest:**
  `git clone anthropos-work/chronos`.
- **The `ant-academy.md` / `studio-desk.md` live-proof claims** (*"Proven live on `billion`"*,
  *"skeleton-visible p95 4669 ms → 817 ms"*, *"canAccess 4049 → 38 ms"*, *"FCP 6936 → 2152 ms"*,
  *"the billion M244 regression"*) are measurements on a running remote stack; nothing on this box can
  confirm or refute them. The *artifacts* they name all exist.
- **`shared_libraries.md:164-165`** — *"[the standalone authn's] `HTTPAuthnMiddleware` has a missing
  `return` after the websocket-skip that colony's copy fixed"*. The standalone `authn` repo is not on
  disk; only colony's (fixed) copy is readable. Its consequence — *"no checked-out service imports the
  standalone module"* — **is** verified (0 hits across all seven clones).
- **The `:42`/`:70` pin question in B4 is a grading call, not a measurement gap.** The platform facts
  are settled and quoted; what an adjudicator must decide is whether §5 rule 33's cell-scoped pin
  exemption or its currency-assertion carve-out governs. I stated both readings rather than picking
  one silently.
- **Out of my file set, noted not booked:** root `CLAUDE.md`'s profile table still documents
  `storage-legacy`, `messenger` and `customerio-sync` as startable compose profiles. At platform
  `0c91421d` those three compose services were deleted by `838d907`, so all three tokens are now
  §5-rule-30 dead tokens (they exit 0 and start only the always-on floor). Same root predicate as B4.
