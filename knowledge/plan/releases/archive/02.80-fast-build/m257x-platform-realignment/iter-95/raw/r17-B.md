# Seat B — reading #17

Instrument: `.agentspace/reading-17-18/briefing.md` (frozen body + 2026-08-05 addendum), read in full
before any file was opened. `corpus/ops/platform-alignment.md` §5 read in full (lines 249–1362).

**Refs used** (re-verified at open with `git -C <clone> rev-parse`; they match the addendum sheet exactly):
`platform` `0c91421d` (= origin/main) · `app` `b948604f` checkout / `2035f9a4` origin/main ·
`next-web-app` `bb3313bc` / `8297c684` · `sentinel` `88bc5592` / `f2c46190` · `storage` `4ce8ece5` /
`9f8cb532` · `messenger` `fa47850d` / `e9421c68` · `cms` `ca50c817` / `f38c0c4a` ·
`jobsimulation` `462343b0` / `82cb66ec` · `roadrunner` `87d8d443` · `graphql-wundergraph` `60c229f3` ·
`studio-desk` `14a5442a` · `ant-academy` `9c3843cd` · `rosetta-extensions` authoring `6130bfd8`.

Grading rule applied: a claim naming a ref is graded at that ref; a claim naming none is graded at the
checkout, and where the checkout is behind (`app`, `next-web-app`, `storage`, `messenger`) I state which
ref settled it. HARD BARS observed: nothing under `knowledge/plan/**` or `.agentspace/scratch/` was
opened, listed or grepped (one `git grep` incidentally *printed* `app/knowledge/plan*` filenames in a
count line; no such file was read); no other seat's directory was listed or opened; no file was written
except this report; no git state was changed.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/services/ai-readiness.md` | 651 | yes |
| `corpus/architecture/platform-migration-status.md` | 307 | yes |
| `corpus/services/jobsimulation.md` | 306 | yes |
| `corpus/services/sentinel.md` | 166 | yes |
| `corpus/services/customerio-sync.md` | 147 | yes |
| `corpus/services/README.md` | 83 | yes |
| `corpus/architecture/README.md` | 38 | yes |
| *(in-scope protocol read)* `corpus/ops/platform-alignment.md` §5 | 2139 total; §5 = 249–1362 | §5 in full |

---

## Blockers

### B1 — `AI_READINESS_URL` cited at a line that holds a different constant, at both live refs

- **Anchor:** `corpus/services/ai-readiness.md:305` —
  > "- **`/ai-readiness` is the only readiness route the navbar links** — `AI_READINESS_URL`
  >   (`packages/core-js/src/constants/urls.ts:52`), consumed by …"

- **Why it is false:** The constant is at **`:50`** at the graded checkout and at **`:51`** at
  `origin/main`. Line `:52` names an unrelated constant at each ref.

  ```
  next-web-app @ bb3313bc (checkout)   :49 WORKFORCE_URL  :50 AI_READINESS_URL  :51 TALK_TO_DATA_URL
                                       :52 ORGANIZATION_FEEDBACK_URL
  next-web-app @ 8297c684 (origin/main):50 WORKFORCE_ANALYTICS_URL  :51 AI_READINESS_URL
                                       :52 TALK_TO_DATA_URL
  ```
  (`git show origin/main:packages/core-js/src/constants/urls.ts`; `git grep -n AI_READINESS_URL` returns
  `packages/core-js/src/constants/urls.ts:50` at the checkout.) The passage names **no ref**, so the
  briefing's rule grades it at the checkout — where it is wrong; I tried `origin/main` as well and it is
  wrong there too, in a *different* way (a third constant), so no ref rescues it.

  This is §5 rule 21's second class — *an anchor that resolves but names the wrong construct*. The
  sibling anchors in the same bullet (`useNavbarSections.tsx:4` import, `:398-400`
  `aiReadinessMenuItem`, `:547` `showAIReadiness ? aiReadinessMenuItem : null`) all resolve **exactly**,
  which is what makes the one wrong anchor read as verified.

- **Confidence:** high.

### B2 — the `storage` row's M903 citation names neither the construct nor the fact; the platform records M903 as never executed and superseded

- **Anchor:** `corpus/architecture/platform-migration-status.md:92` (the `storage` row) —
  > "The module deliberately survives, because deleting the block would destroy the two buckets, the
  > CloudFront distribution and the media DNS record along with the `prevent_destroy` guards that are
  > read from configuration (`:13-16`); **custody transfer is M903 (`:18`)**."

- **Why it is false:** two independent failures, in the same clause.

  **(a) The anchor names the wrong construct.** The cell itself fixes the ref by asserting the module is
  *"**18 lines**"* — true only at `storage` **origin/main `9f8cb53`**, where the whole file is:

  ```
  storage/terraform/main.tf @ 9f8cb532
   9-11  # The ECS service that used to live here is GONE (v9.0 "support-in-app") …   <- the cell's :9-11 OK
  13-16  # …`prevent_destroy` would NOT stop that: it is read from configuration…     <- the cell's :13-16 OK
    18   # See outputs.tf — consumers should reference these by output, never by literal name.
  ```
  Line 18 says nothing about custody, M903, or a transfer. (At the *checkout* `4ce8ece5` the file is 100
  lines and `:18` is `project = local.project` — so no ref resolves this citation to the claim either.)

  **(b) The substance is contradicted by the only place M903 exists.** `git grep -n "M903" origin/main`
  over the whole `storage` repo returns exactly **one** hit, `terraform/storage.tf:22`, and it says the
  opposite of what the map asserts:

  > "(An earlier plan, **M903**, instead proposed `moved`-ing these resources into a local module in the
  > infrastructure repo. **That was never executed, and the shipped design supersedes it**: the assets
  > stay here and the module stays declared. If the relocation is ever revisited, the guards must be
  > carried across in the same commit.)"

  So *"custody transfer is M903"* presents a superseded, never-executed plan as the standing disposition
  of the buckets/CDN/DNS — in the one row whose whole point is that deleting the module block destroys
  the `prevent_destroy` guards. A reader routing bucket-custody work to "M903" is routing it to a plan
  the platform has already retired.

- **Confidence:** high. (The map is otherwise the most accurate file in my set — every other
  `file:line` in the `storage` row verified, including the 3-hit / 15-hit `STORAGE_RPC_ADDR` split and
  its 7-env-lookup decomposition, exactly. That is why this one is worth naming rather than shrugging
  at.)

### B3 — sentinel.md rests its "messenger is not a caller" evidence on a compose block that no longer exists, and contradicts itself two sections later

- **Anchor:** `corpus/services/sentinel.md:5` —
  > "**`messenger` is not a caller** — **its compose block** sets no `AUTHORIZATION_ADDRESS` and
  > declares no `depends_on: sentinel`, and its Go source imports no authorization client at all"

- **Why it is false:** at the graded ref (`platform` checkout `0c91421`, which is also origin/main)
  **messenger has no compose block at all**. `838d907` (*"drop the storage, messenger and
  customerio-sync containers"*, merged `0c91421`) deleted the service outright; `docker-compose.yml` is
  186 lines and declares five services — `sentinel`, `backend`, `studio-desk`, `next-web-app`,
  `gotenberg` (`git show 0c91421:docker-compose.yml`, service-key count re-derived = 5).

  The pin that would rescue it does not reach: the immediately preceding **parenthetical** is pinned
  (*"so at `0dab54d` `AUTHORIZATION_ADDRESS` is set in exactly one block"*), and §5 rule 33 is explicit
  that a pin's scope is *"the claim's own block — a markdown CELL in a table, a **wrapped sentence** in
  prose."* This is a separate sentence and carries no ref.

  **And the same file says the opposite twice**, so this is also the self-contradiction class:
  - `sentinel.md:85` — *"set in exactly **one** compose block at platform `0c91421`"* … *"The blocks
    that used to carry it are gone rather than corrected: … then `storage`, `messenger` and
    `customerio-sync` at `838d907`"*;
  - `sentinel.md:89` — *"`messenger` and `storage` never called it, and **neither is a compose service
    any more** (deleted at `838d907`)"*.

  The *conclusion* is correct and I verified its surviving half independently: `git grep` over the
  `messenger` clone returns **0** hits for `authorization` in `*.go` and **0** for
  `AUTHORIZATION_ADDRESS` anywhere (positive control: `CMS_RPC_ADDR` = 2 files in the same clone).

- **Confidence:** medium. **Why I hesitated:** the sentence's conclusion is true and one of its two
  evidence legs still verifies, so no reader is misled about *whether* messenger calls sentinel — only
  about *what exists to check*. I booked it because a live present-tense assertion about a deleted
  compose construct is the exact founding class `platform-alignment.md` exists to fence (§5 rules 29,
  30), and because it contradicts two other passages of the same file — the class §5 rule 21 calls the
  cheapest one in any corpus.

---

## Minors

- `corpus/architecture/platform-migration-status.md:79` — *"every **service** that has ever appeared in
  `docker-compose.yml` (same command on that file -> **26** names)"*. Re-derived
  (`git log -p --follow -- docker-compose.yml`, all 2-space top-level keys ever added): **26 keys**, of
  which **`app-network` is a network, not a service** — so 25 services ever. Rule-24 shape: the count
  reproduces, the SET does not. The row set is nonetheless complete (all 25 have rows), but a reader
  re-running the audit the sentence invites will find `app-network` "missing a row".
- `corpus/architecture/platform-migration-status.md:205` — *"every other `file:line` … (**66**
  citations) | resolution + range only"*. The fence's own reach line, run as documented
  (`PLATFORM_REPOS_YML=stack-demo/platform/repos.yml python3 …/platform_alignment_guard.py`, exit 0),
  prints: *"assertion F resolved **91** citation(s) — **22** subject-checked, **68** range-only, **1**
  outside any service block"*. So the compose figure (23 = 22 + 1) is right and the "other" figure is
  **68**, not 66.
- `corpus/services/ai-readiness.md:198` — *"`computeInterviewInsightsV2` … reads **exactly one
  table**"*. It also issues `countInterviewSessionsInCycle` (`public.job_simulation_sessions` +
  `public.ai_readiness_sims`) and `resolveSessionAuthors` (`public.job_simulation_sessions` JOIN
  `public.memberships` LEFT JOIN `skiller.job_roles`) — the second of which **the next table row names**
  (`:202`). The load-bearing identification (`interview_aggregated_reports` is the sole *content*
  source for the four blocks) is correct and verified.
- `corpus/services/ai-readiness.md:307` — *"A repo-wide grep finds the constant in exactly those two
  non-`node_modules` files."* `git grep -n AI_READINESS_URL` returns **three** tracked non-node_modules
  files: `urls.ts`, `useNavbarSections.tsx`, and `knowledge/ai-readiness/frontend-architecture.md:15`.
  (Two *code* files, which is presumably the intent.)
- `corpus/services/ai-readiness.md:582` — `computeCycleTotals` cited at `how_we_measure.go:253-261`.
  At the checkout the function's doc comment runs `:256-259` and the signature is `:260`; `:253-255` is
  the tail of the preceding function. Range contains the construct but starts three lines early.
  (Its sibling `:285-287` is exact.)
- `corpus/services/jobsimulation.md:157` — cited path `cms/directus/collections/jobsimulation.go:1594-1597`
  is missing the `internal/` segment; the file is `cms/internal/directus/collections/jobsimulation.go`,
  where `:1594-1597` is `voiceEngineFromDirectus` returning `SimulationVoiceEngineGptrealtime` on nil —
  i.e. the line numbers and the claim are **exactly** right, only the path prefix is short.
- `corpus/services/jobsimulation.md:200` — *"`app/main.go:216` (@ `origin/main` `7177374` …)"*. At
  `7177374` line 216 **is** `func main() {`, but `7177374` is no longer origin/main: `app` origin/main
  is `2035f9a4`, **33** commits later, where `main.go:216` is `if strings.Contains(dsn, "://")` and
  `func main()` sits at `:229`. Labelling a fixed sha "origin/main" is a currency assertion that has
  expired (§5 rule 33, third mechanism). Booked MINOR because the sha is supplied, the anchor resolves
  at it, and the substantive claim (**no cobra root**; the only `spf13/cobra` import repo-wide is
  `cmd/createTaxonomy/main.go` — re-verified at *both* `b948604` and `origin/main`) holds at every ref.
- `corpus/services/sentinel.md:31` vs `:41` — `g3` is described as `g3(org, user)` in the m6 matcher row
  and `g3(org, membership)` in the role-grouping list. The model declares `g3 = _, _` and `m6` calls
  `g3(p6.org, r6.sub)`; harmless, but the two spellings are not the same word.

---

## Positively cleared (audited zeros)

**ENUMERATED** — predicate re-derived independently from source, cardinality compared before any
arithmetic:

1. `platform-migration-status.md:76-78` — *"14 names ever in `repos.yml`"*.
   `git log -p --follow -- repos.yml | grep '^+ *- name:'` -> **14**, and they are byte-for-byte the 14
   the sentence lists. All 14 have rows.
2. `platform-migration-status.md` §1 / §4 assertion C — *"every state is one of the **eight** in §1"*.
   The §1 table has exactly 8 rows (`live-standalone`, `merged-into-app`, `running_but_unfederated`,
   `mid-fold`, `decommissioned`, `net-new`, `external`, `library`), and no row in §2 uses a ninth.
3. `platform-migration-status.md:25`, `:129-130` — `repos.yml` **4** entries (app · sentinel ·
   next-web-app · studio-desk, 28 lines), and the 9 -> 6 -> 4 history: `d11a403^` = 9 entries,
   `d11a403` = 6, `838d907` = 4. All three counted from the file at each ref.
4. `platform-migration-status.md:26`, `:90` — compose **271 -> 186** lines; `838d907` numstat on that
   file alone = **+11 / -96**; service-key count `d11a403^` = 11 -> `d11a403` = 8 -> `0dab54d` = 8 ->
   `838d907` = **5**. Every figure in those two cells re-derived from the file, not from the message.
5. `platform-migration-status.md:87` — the `app` row's **seven** in-process domains and their seven
   distinct wiring call sites at `app` origin/main `2035f9a`: `:395` `customeriosync.New`, `:524`
   `internalstorage.NewManager`, `:690` `skiller.NewSkillerManager`, `:721` `jobsimwiring.Wire`, `:751`
   `skillpath.NewSessionManager`, `:1153` `appcms.Wire`, `:1471` `msgadapters.Wire` — **all seven
   resolve exactly**. `internal/{cms,jobsimulation,skiller,skillpath,storage,messenger,customeriosync}/`
   all exist; **`internal/roadrunner/` does not**, and `internal/jobsimulation/runner/` does, with
   `jsrunner.NewRunnerManager(getenv("JUDGE0_API_KEY"), getenv("JUDGE0_BASE_URL"))` at
   `internal/jobsimwiring/wiring.go:123` as claimed.
6. `platform-migration-status.md:92` — the `STORAGE_RPC_ADDR` two-ref split. At `origin/main`: **3**
   hits in `*.go`, **all comments**, at the three cited lines (`main.go:504`, `jobsimwiring/wiring.go:101`,
   `storagens/callsites_test.go:189`). At `b948604`: **15** hits, decomposing exactly as the cell says —
   **7** env lookups (`main.go` x3 at `:446/:524/:992`, `wiring.go` x1, one in each of `cmd/academy-asset-upload`,
   `cmd/academyImport`, `cmd/import`), 2 doc comments, 2 error strings, 3 `t.Setenv`, 1 `t.Fatal` = 8.
   And **six of the seven** are `os.Getenv`, the seventh a lowercase `getenv` helper (`wiring.go:115`).
   Also: `STORAGE_RPC_ADDR` = **0** across `docker-compose.yml`, `common.yml`, `.env_example`
   (positive control: `ENVIRONMENT` = 7 in the same file).
7. `platform-migration-status.md:93` / `jobsimulation.md:120` / `sentinel.md:85` — **zero** `*_RPC_ADDR`
   set by any platform config file; the only cross-process service address left is
   `AUTHORIZATION_ADDRESS=http://sentinel:8087` at `docker-compose.yml:48`. `messenger/cmd/root.go:120-140`
   does still read all four (`CMS_RPC_ADDR` :120, `BACKEND_USERS_RPC_ADDR` :125/:130,
   `SKILLER_RPC_ADDR` :135, `JOBSIMULATION_RPC_ADDR` :140).
8. **Every `docker-compose.yml` / `common.yml` / `repos.yml` line anchor** in
   `platform-migration-status.md` §2 opened and read in surroundings at `0c91421`: `:1-2`, `:5`, `:18`,
   `:28`, `:48`, `:59`, `:73-75`, `:82`, `:83`, `:84-92`, `:102-103`, `:112`, `:119`, `:135`, `:141`,
   `:143`, `:151`, `:160`, `:168`, `:170-171`, `:183`; `common.yml:2`, `:22`, `:24`;
   `repos.yml:2-10`, `:12-13`, `:14-17`, `:18-20`, `:23-25`, `:26-28`. **All resolve to the named
   construct.** So do the historical ones at their own pins (`0dab54d:88-91`; `2adcf71:83`, `:93-94`,
   `:113`, `:119`, `:140`; `Makefile:10` `PROFILE ?= graphql`; `repos.yml:17-19`).
9. Terraform: `app/terraform/main.tf:181` `= 1`, `:415-416` `MESSENGER_ENABLED "true"`, `:419-420`
   `CUSTOMERIO_SYNC_ENABLED "true"`; `sentinel/terraform/main.tf:19` `= 1`; `cms/terraform/main.tf:39`
   `= 0`; `roadrunner/terraform/main.tf:19` `= 1` with `git blame -L 19,19` -> **`84a4b4f` (2025-12-15)**
   and repo HEAD `87d8d44` (2026-06-19), exactly as the row derives (and `e45eb61`, 2026-05-27, is the
   file's most recent touch); `graphql-wundergraph/terraform/main.tf:20` `= 1`;
   `messenger/terraform/main.tf:29` `= 0` in a **121-line** module with `:19-25` and `:27-28` as
   described (at messenger origin/main); `storage/terraform/main.tf` is **18 lines** with `:9-11` and
   `:13-16` verbatim; `jobsimulation/terraform/main.tf:15-22` + `:24-40` verbatim at `82cb66e`, and
   `service_desired_count` occurs **nowhere** in that file.
10. `platform-migration-status.md:96` — supergraph is **one** subgraph:
    `supergraph-config-prod.yaml` lists `backend` alone, `schemas/` holds `backend.graphqls` alone,
    `subgraphs.conf` = `BACKEND=v1.360.0`. Enumerated from the tree, not from the sentence.
11. Every cited platform commit opened and its date/subject compared: `a2a3ee6`, `cb6ebf5`, `ef4b449`,
    `1474b1f`, `84862d1`, `b43b99a`, `c17cc9a`, `467965a`, `8770fe6`, `6060315`, `045857c`, `fdfa189`,
    `21429b7`, `a4db680`, `236771f`, `d11a403`, `b56d731`, `360efd4`, `2adcf71`, `838d907` — **all
    match**. `d11a403`'s own message *does* claim roadrunner's clone entry "was already gone" while its
    `repos.yml` diff deletes `- name: roadrunner` alongside cms and jobsimulation — the row's
    *"the message is wrong; the diff is the fact"* is correct.
12. `ai-readiness.md:252`, `:356`, `:550` — `defaultReadinessSkills` enumerated from
    `app/internal/aireadiness/defaults.go`: **19** core @1.0 + **12** enabling @0.5 = **31** nodes,
    weight **25.0**; `defaultReadinessSims` = **3** entries (tech sim, business sim, shared interview).
    Only after enumerating did I check the arithmetic (`round(1.0/25x30)=1`, `round(9.0/25x30)=11`,
    `25/25x30=30`, and `3/6.5x30=14` for the retired 8-skill set) — all correct.
13. `ai-readiness.md:144-150` — archetype bands read from `aireadiness/scoring.go`:
    `archetypeHighBand = 75` (`:26`), `archetypeLowCeil = 50` (`:30`), `classifyArchetype` (`:46`)
    Champion / Standby / `usage >= knowledge` -> Explorer (so an exact tie *is* Explorer) / default
    Hidden Talent; `bucketKnowledge` bands None 0-24 / Low 25-50 / Medium 51-74 / High 75-100.
14. `ai-readiness.md:167-183` — all **13** `ai_readiness_*` table names enumerated from
    `internal/data/ent/migrate/schema.go`; every one in the doc's table exists, including the three
    plural ent-generated names the doc flags. `public.interview_aggregated_reports` carries exactly
    `(organization_id, sim_id, report JSONB, session_count)` per its ent schema. `ai_readiness_cycles`
    has the **partial unique** "one active cycle per org" index, plus `track` and `launched_by`;
    `ai_readiness_sims` has `UNIQUE(organization_id, step_type, track)`.
15. `ai-readiness.md:275` — CSV is **15** columns: `AIReadinessCSVColumns` enumerated (Cycle · Cycle
    start · Cycle end · Person name · Role · Tags · score · knowledge · Archetype · Skill Mapping done ·
    Verified AI skills · Simulation done · Simulation score · Interview done · Usage band), with the
    source's own comment recording the drop from 19.
16. `ai-readiness.md:201`, `:213-219`, `:620-624` — the **five** usage KPI ids enumerated from
    `usageDimSpecs` (`how_we_measure.go:1138-1159`): `avg_adoption` · `avg_transformation` ·
    `avg_originality` · `avg_depth` · `avg_ownership`; the retired trio appears nowhere. The seeder
    emits exactly those five at `stack-seeding/seeders/ai_readiness_interview_report.go:185-189`, with
    its `:149` comment *"All five are always emitted here"*. `usageDimensionsFromReports` does `continue`
    on an absent id (`:1180-1182`) and on a non-numeric value (`:1169-1171`).
17. `ai-readiness.md:202-205` — the fallback wiring, which is counter-intuitive and **correct**:
    Strengths <- `narrative.patterns`, falling back to the **`top_unexpected`** chart (`:908`);
    HoldsBack <- `"risk"`-category insights (`:994`), falling back to **`top_concerns`** (`:916`);
    UnexpectedAngles has **no** fallback (`:910`, and the source comment at `:902-905` says so).
18. `ai-readiness.md:71-74`, `:76-81` — `flag_ai_readiness`'s **sole** consumer repo-wide is
    `useAiReadinessActive.ts:22` (`git grep AI_READINESS_FLAG` -> 4 lines in 2 code files, one of them
    the declaration at `aiReadiness.constants.ts:26`); `AIReadinessClient.tsx` contains **0**
    `posthog` and **0** `flag_ai_readiness` (positive control on the same file: `useMemo` = 4).
19. `ai-readiness.md:593-597` — `grep -c interviewQuestions` over the 1,989-line `HowWeMeasureTab.tsx`
    returns **0** (positive control `const` = 89), the block opens at `:1879`, label `:1903`, and the
    three cells are `skillsMapped` `:1915`, `handsOnMinutes` `:1921`, `interviewMinutes` `:1927` —
    exactly three, exactly those. The field does exist at `hooks/useAIReadiness.ts:274`.
20. `ai-readiness.md:587` — `public.interactions.action_type` is **exactly `{email, call}`**
    (`internal/data/ent/migrate/schema.go`, `InteractionsColumns`), and the DB check
    `source_id <> target_id` is declared at `schema/interaction.go:50`.
21. `ai-readiness.md:630-633` — **5** AI-readiness Playthroughs: 4 in
    `rext playthroughs/manifest/ai-readiness.yaml` + `pt-onboarding-aireadiness-guided` in
    `onboarding.yaml`, counted from the manifests.
22. `jobsimulation.md:36`, `:92` — **23** run-state tables: `grep -c '^CREATE TABLE'` on
    `app/terraform/migrations/20260722081626_jobsim_data_model.sql` = **23**, and the names include
    every one the doc lists. `20260722104506.sql` creates `job_simulation_sessions` at `:2` and
    `DROP TABLE "sessions"` at `:79`.
23. `jobsimulation.md:127-132` — `20260729133514.sql`: `:15-23` remaps the two link ids, `:36-44` NULLs
    the orphans, `:58-62` is the *"5. Drop the mirrors."* comment + the first DROP, `:62-63` drops both
    mirrors, and the file contains **0 `INSERT INTO`** (the one case-insensitive "insert" hit is the
    trigger-function name `on_insert_local_jobsimulation_sessions_update_memberships` at `:64`).
    `internal/organization/intelligence.go:1700` is `m.ent.JobSimulationSession.Query()`.
24. `jobsimulation.md:177-183` — the cobra contract: subcommands enumerated from `cmd/*.go` = exactly
    `aggregate`, `test-command`, `clone-session`, `validate` (root `Use: "jobimulation"`); **no**
    `serve`, **no** `run`; `SilenceUsage`/`SilenceErrors` = 0 occurrences; `Dockerfile:32` and
    `Dockerfile.dev:33` `EXPOSE 8080`; the repo's own `CLAUDE.md:88` does document `go run . serve`.
    `cmd/root.go:77`/`:78` are the `8080`/`8081` defaults.
25. `sentinel.md:26` — **6** request types (`r`,`r2`-`r6`), **6** policy types (`p`,`p2`-`p6`), **3**
    role groupings (`g`,`g2`,`g3`), **6** matchers (`m`,`m2`-`m6`), enumerated from
    `internal/authorization/casbin.go`'s `ModelConf`; each matcher's one-line description checks out.
26. `sentinel.md:43-48` — `grep -c manager init_policy.sql` = **0**; the four seeded roles enumerated
    from the p2/p3/p5 rows are `admin` (33), `content_creator` (14), `member` (11), `candidate` (4) —
    all four, matching `app/internal/data/ent/enum/membership.go:8-15`. `init_policy.sql:63-66` is the
    taxonomy:write omission note; `:88-118` is the content_creator block;
    `internal/authorization/casbin_content_creator_test.go` exists.
27. `sentinel.md:70-83` — the RPC table is **complete**: 19 exported handler methods in
    `internal/rpcsrv/rpc.go`, and all 19 appear in the doc's table with no invented ones.
28. `sentinel.md:12`, `:22` — `go.mod:3` `go 1.26.0`, `Dockerfile:2` / `Dockerfile.dev:2`
    `golang:1.26-bookworm`, `casbin/v3 v3.10.0`, `terraform/locals.tf:4-5` = 256 / 128 (and `:3`
    `port = 8080`, corroborating "binary default 8080"). `Makefile:2-3` `initdb` does hard-code
    `postgresql://postgres@localhost:5432/postgres?sslmode=disable`.
29. `customerio-sync.md` — every anchor: `internal/customeriosync/doc.go:4` *"the last of the Go
    services to be folded into app"* (and v0.19.3, the Brevo fossil, `public.customer_io_sync_table`,
    the asynq scheduler, the overlap window, `database/sql` not sqlc, and **7,392 of 7,397 rows**, all
    in `doc.go`); `env_guards.go:62`; `main.go:284`, `:286`, `:295`, `:393-396`, `:395`; the file set
    (`sync.go`, `store.go`, `sync_query.sql`, `doc.go`) + `cmd/customerio-resync`; `--dry-run`
    constructs no Brevo client (`cmd/customerio-resync/main.go:60-62`, with the comment saying why).
    The **HISTORICAL compose block quoted at `:60-74` matches `838d907^` line-for-line**, including
    `profiles: [customerio-sync, all]` and the git-URL build context — and it was the only such context
    in the file.
30. `services/README.md` — *"Every service doc in this directory, enumerated"*: 29 `.md` files, minus
    `README.md` and `TEMPLATE.md` = **27**, and all **27** are linked. `MESSENGER_RPC_ADDR` appears in
    **0** of the 12 platform clones (positive control `CMS_RPC_ADDR`: 17 files in `app`, 2 in
    `messenger`) and `git log -S` over `platform` returns **0** commits. `messenger/internal/flow/flow.go:70-95`
    is `Subscribe()` with `AddSubscriber("backend", …)` at `:72`.
31. `architecture/README.md` — the 10 listed docs are exactly the 10 non-README `.md` files in
    `corpus/architecture/`. *"`authn` is a dependency of no service"*: `grep -c anthropos-work/authn`
    over `go.mod` **and** `go.sum` of app / sentinel / storage / messenger / cms / jobsimulation /
    roadrunner = **0** everywhere (positive control: `anthropos-work/colony` = 1 in `app/go.mod`).
    `stack-core/platform_alignment_guard.py`, `stack-core/platform_predicate_guard.py`,
    `alignment/` and `stack-seeding/dna/` all exist in the authoring clone.
32. **Both fences run green on the tree I audited**, and I recorded the invocation (§5 rule 12):
    `PLATFORM_REPOS_YML=stack-demo/platform/repos.yml python3 …/platform_alignment_guard.py` ->
    *"91 citation(s) — 22 subject-checked, 68 range-only, 1 outside any service block; 0 unresolvable"*,
    exit 0; `PLATFORM_DIR=stack-demo/platform python3 …/platform_predicate_guard.py` -> OK, with a full
    reach line. Neither reaches any of B1-B3, which is the point of §5 rules 38 and 40(b).

**RE-DERIVED only** (weakest clearance — the arithmetic the document itself displays, over a set I
could not independently enumerate offline):

- `ai-readiness.md:348-349` — `156 / 199 = 78.4 %`.
- `ai-readiness.md:550`, `:560-568` — the tier-1 scoring worked examples (the *set* they range over,
  `defaultReadinessSkills`, **was** enumerated — see ENUMERATED #12 — so this one is stronger than a
  pure re-derivation).
- `jobsimulation.md:49-50` — *"five services compose declares … seven effective, once `include:
  common.yml` adds the postgresql/redis floor"* — 5 + 2 = 7, both counts enumerated from the files.

---

## What I could not settle

- **`platform-migration-status.md` §3 (the census).** *"93 repositories in `anthropos-work` … 4 in
  `repos.yml` … 46 named by no corpus document"*, the per-repo last-push dates, and every *"Repo
  ARCHIVED on GitHub"* / *"NOT archived"* claim across §2. These need the GitHub org API; the `4` half
  I did enumerate (correct). **Would settle it:** `gh api orgs/anthropos-work/repos --paginate`.
- **`jobsimulation.md:47`** — *"Production terraform still names `http://backend.internal.anthropos:8081`."*
  `JOBSIMULATION_RPC_ADDR` is set in `messenger/terraform/main.tf:92` and `cms/terraform/main.tf:122`
  as `${var.jobsimulation_rpc_address}`, a variable declared with **no default**
  (`messenger/terraform/variables.tf:92-95`). The literal lives in `infrastructure`'s `services.tf`,
  which is not in the clone set — exactly the blind spot the map's §5 row 3 declares.
- **`ai-readiness.md`'s live measurements** — 2.09 s / 304 KB live read, 24 ms frozen read, the 180 s
  timeout, 180 s -> 19 ms after the demo-patch, `5 -> 345` validation rows, 165 `interview_extraction_results`
  rows, and the M254 *"8/8 sections on billion"* gate. All require a running stack. The doc says so
  itself (*"Re-measure before relying on either number"*), which is the right disposition.
- **Version-ships** `app v1.315+` / `next-web-app v2.89.0+` (`ai-readiness.md:7`) — I confirmed the
  refactor's tag (`4c28365f` first appears in `v1.351.1`) and the checkout/HEAD tags (`b948604` =
  v1.366.0, `9d00a313` = v1.367.0, origin/main = `v1.369.0-2-g2035f9a40`), but not the first-ship tags.
- **`platform-migration-status.md:204`** — *"`docker-compose.yml` citations — 23 of them"*. I could not
  re-derive the guard's classifier independently; I accepted it because the guard's own reach line
  (22 + 1) agrees. The sibling figure did not, and is booked as a minor.
- **Ref-staleness disclosures, deliberately NOT booked** (a stale *clone* is not a corpus defect):
  - `ai-readiness.md:263` — *"switch-gated by `MESSENGER_ENABLED`"*. `MESSENGER_ENABLED` occurs **0**
    times in `app` at the checkout `b948604f` (the v9.0 messenger fold is past it) and is real at
    `origin/main` `2035f9a4` (`env_guards.go:61`, `main.go:285/:1445/:1552`). Graded true at
    origin/main.
  - The whole of `customerio-sync.md`'s `app/…` anchor set, likewise, exists only at `app`
    origin/main; every one resolves there.
  - `platform-migration-status.md:93` — `messenger/terraform/main.tf:29` and *"121 lines"* resolve at
    `messenger` **origin/main** `e9421c6`; at the checkout `fa47850d` the file is 111 lines and `:29` is
    `container_definitions`. The cell names no messenger ref. Not booked, on the same rule.
- **Out of my file set, reported not booked** (it belongs to whoever holds that file, and I do not want
  to double-count it): root `CLAUDE.md` still says *"`storage` sits in `profiles: [storage-legacy]` and
  `messenger` in `profiles: [messenger]`, both kept startable only as rollback paths"* and *"`core`
  starts **five** containers"*. `838d907` deleted both services and both profiles; `docker-compose.yml`
  at `0c91421` declares five services total and five legal profile tokens
  (`all`, `backend`, `core`, `frontend`, `studio-desk`). My assigned files (`platform-migration-status.md:92`,
  `:93`; `services/README.md:20-25`) all state the corrected form, so this is a cross-file drift **out**
  of my partition — §5 rule 19's *"a claim leaks to the EDGE of the previous repair's scope and stops
  there"*, landing on the highest-propagation file in the tree.
