# Seat E — reading #17

**Refs used** (re-verified at open against the addendum sheet; all matched):
`platform 0c91421d` · `app b948604f` (origin/main `2035f9a4`) · `next-web-app bb3313bc` (origin/main
`8297c684`) · `sentinel 88bc5592` · `storage 4ce8ece5` (origin/main `9f8cb532`) · `messenger fa47850d` ·
`cms ca50c817` · `graphql-wundergraph 60c229f3` · `roadrunner 87d8d443` · `jobsimulation 462343b0` ·
`studio-desk 14a5442a` · `ant-academy 9c3843cd` · `rosetta-extensions` (authoring) `6130bfd8`.
Additionally: `app/studio` (the `anthropos-studio-room` in-tree clone) is at `aeec036a` — the exact ref
`studio-room.md:359` pins.

Read `corpus/ops/platform-alignment.md` §5 in full (lines 249–1362) before starting.

## Positive controls

| file | `wc -l` | read in full? |
|---|---|---|
| `corpus/services/studio-room.md` | 483 | yes |
| `corpus/services/hiring.md` | 433 | yes |
| `corpus/services/graphql-wundergraph.md` | 279 | yes |
| `corpus/services/storage.md` | 210 | yes |
| `corpus/services/clerk-integration.md` | 128 | yes |
| `corpus/architecture/frontend_architecture.md` | 105 | yes |
| *(scope read, not assigned)* `corpus/ops/platform-alignment.md` | 2139 | §5 in full |

---

## Blockers

### B1 — `storage.md` presents M903 as the live custody-transfer plan; at the ref the cell itself names, M903 is recorded as never executed and superseded — and the anchor points at an unrelated line

- **Anchor:** `corpus/services/storage.md:25` — *"The module deliberately survives: deleting the block
  would destroy those assets along with their `prevent_destroy` guards, which are read from configuration
  (`storage/terraform/main.tf:13`). **Custody transfer is M903 (`:18`)**"*
  The cell's own header (`:23`) pins the whole row: *"measured at platform `0c91421` / app `2035f9a` /
  **storage `9f8cb53`**"*.
- **Why it is false:**
  - At `storage` `9f8cb532`, `terraform/storage.tf:20-25` reads: *"LIMIT 1 — `prevent_destroy` is read
    from CONFIGURATION … Keeping that block declared is therefore load-bearing, not cosmetic. **(An
    earlier plan, M903, instead proposed `moved`-ing these resources into a local module in the
    infrastructure repo. That was never executed, and the shipped design supersedes it: the assets stay
    here and the module stays declared.)**"* — i.e. at the pinned ref M903 is an **abandoned** plan, not
    the forward custody-transfer step.
  - The anchor is also wrong: `storage/terraform/main.tf` @ `9f8cb532` is exactly 18 lines and
    `:18` reads *"# See outputs.tf — consumers should reference these by output, never by literal
    name."* — no mention of M903 or of custody. `git grep M903 9f8cb532` returns **one** hit in the whole
    repo, `terraform/storage.tf:22`.
  - The wording the corpus reproduces is the **pre-fold** state: at the *checkout* `4ce8ece5`,
    `terraform/storage.tf:23` still says *"That is what M903's `moved` blocks are for — relocate the
    assets out of this module BEFORE M907"*. The corpus carried that forward into a cell measured at
    `9f8cb53`, where the same file now retracts it. §5 rule 31: a refutation/plan is a measurement and it
    expires.
- **Confidence:** high.

### B2 — `storage.md` says the PRIVATE storage manager falls back to local FS in the platform compose; since `0dab54d` compose points it at the **production private bucket**

- **Anchor:** `corpus/services/storage.md:58` — *"In the platform compose, `STORAGE_S3_PUBLIC_BUCKET` is
  hardcoded to the production public bucket, so locally the PUBLIC manager talks to real S3 …, **while the
  PRIVATE manager uses local FS**."* Same claim at `:40` — *"the private manager falls back to local
  filesystem in dev when `STORAGE_S3_BUCKET` is unset; the public manager is wired to production S3 in
  compose"*. Neither sentence names a ref.
- **Why it is false:** `platform/docker-compose.yml:82` @ `0c91421d` (present since `0dab54d`, which
  `git log -S` confirms is the commit that introduced it) sets, on the **`backend`** service:
  `- STORAGE_S3_BUCKET=production-storage20240826131618541000000005`, alongside
  `:83 - STORAGE_S3_PUBLIC_BUCKET=production-storage-public…`. The block's own comment (`:73-79`) is
  explicit: *"storage-in-app (v9.0): app owns the buckets in-process … These MUST be set."* The compose
  file also bind-mounts `$HOME/.aws/credentials` into `backend` (`:100`). Since the same doc states at
  `:44` that *"the live code is `app/internal/storage/`"*, the private manager a stock local stack runs is
  pointed at the **production private bucket**, not `/tmp/anthropos-storage/`.
  This is a safety-relevant inversion: the doc tells a reader that local private writes are sandboxed to
  `/tmp` when in fact they are addressed at production S3.
- **Confidence:** high. (I hesitated only over whether §"Two storage managers" should be read as
  describing the now-deleted standalone container; but the sentence says *"In the platform compose"* in
  the present tense, and the current platform compose sets the variable.)

### B3 — `storage.md`'s env table records the compose value of `ENVIRONMENT` as empty; the deleted `storage` block set it to `development`

- **Anchor:** `corpus/services/storage.md:193` — *"| `ENVIRONMENT` | (empty) | Environment name |"*, under
  a header (`:181-184`) stating *"The middle column records what `docker-compose.yml` **set** on the
  `storage` service block"*.
- **Why it is false:** the storage service block set it explicitly.
  `platform/docker-compose.yml:206` @ `2adcf71` and `:119` @ `0dab54d` both read
  `- ENVIRONMENT=development`. (The neighbouring rows in the same table — `PORT=8300` `:207`/`:120`,
  `RPC_PORT=8301` `:208`/`:121`, `SERVICE_NAME=storage` `:209`/`:122`,
  `STORAGE_S3_PUBLIC_BUCKET=…` `:210`/`:123`, `AWS_REGION`/`AWS_DEFAULT_REGION=eu-west-1` — are all
  correct, which is what makes the one wrong cell easy to trust.)
- **Confidence:** medium. Hesitated because the table is explicitly HISTORICAL and `ENVIRONMENT` is read
  by no `.go` file in the `storage` repo (it reaches the binary through `colony`), so the practical harm
  is small. Booked as a BLOCKER because it is a checkable, false value in a table whose entire stated
  purpose is to record those values.

### B4 — `graphql-wundergraph.md` cites its own lines `:174-176` for a statement that lives at `:192`

- **Anchor:** `corpus/services/graphql-wundergraph.md:86` — *"The host-`5050` mapping was published here in
  the present tense while **`:174-176` of this same doc** already said `localhost:5050` refuses the
  connection; corrected M257x iter-46"*.
- **Why it is false:** `graphql-wundergraph.md:174-176` reads *"`NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT` — each
  also baked as a build arg, at `:119` and `:151`), because / the router service no longer exists in
  compose. Those line numbers move on every compose / clean-up — they were `:220`/`:236` at `0dab54d` …"*.
  The *"`http://localhost:5050` refuses the connection"* sentence is at **`:192`**. The self-citation
  resolves to the wrong construct — §5 rule 34's exact class (*"line numbers move when YOU edit too"*),
  in the very sentence that records a repair.
- **Confidence:** medium. Hesitated because the substantive claim (the 5050 mapping was contradicted
  elsewhere in the doc) is true and the misdirected reader loses only a minute. Booked as a BLOCKER
  because the instrument names *"wrong line anchors that point at the wrong construct"* as a blocker
  class, and because an intra-corpus citation is the cheapest kind to keep correct.

### B5 — `hiring.md` states the hiring story's `role_mix` as `≈ 0.1 admin / 0.9 candidate`; the shipped preset is `0.14 / 0.86`, and `0.10` is the value the tooling records as a defect

- **Anchor:** `corpus/services/hiring.md:145` — *"**M223's hiring story simply sets `role_mix ≈ 0.1 admin /
  0.9 candidate`** (no `member`)"*.
- **Why it is false:** `rosetta-extensions` @ `6130bfd8`,
  `stack-seeding/presets/stories.seed.yaml:249` reads
  `role_mix: { admin: 0.14, member: 0.0, candidate: 0.86 }`, and the preset's own inline comment (added by
  `5d0297e`, *"M226 iter-04: fix the hiring org count → exactly 5 admin + 45 candidate"*) says in as many
  words: *"admin: 0.14 (**NOT 0.10**) — M226 iter-04 (the billion-proof count fix)"*. That commit message
  records why: with `admin: 0.10` the admin band is `int(50*0.10)=5` slots, of which slots 2 and 3 are the
  candidate heroes Cara+Cody and get overridden to `candidate`, yielding **3 real admins, not 5**. So the
  corpus's own `:338` figure (*"5 admins + 45 candidates"*) is unreachable from the ratio it prints 193
  lines earlier — the two are internally inconsistent, and the printed ratio is exactly the one that
  reproduces a fixed bug.
- **Confidence:** low. Hesitated hard: the `≈` hedges the value, and the clause is attributed to *M223*,
  at which point `0.10` genuinely was the setting. Booked per the instrument's under-booking rule, and
  because the sentence reads as present tense about the shipped story.

---

## Minors

- `corpus/services/studio-room.md:435` — *"(preserving any `collaborative_*` / `asset_*` JSON already in
  the bundle)"* omits `internal_*`, which the same filter also preserves
  (`app/studio/agents/simulation/postgen/exporter.py:537`). Nothing stated is false; the list is short one
  member.
- `corpus/services/studio-room.md:345-356` — the reproduced `requirements.txt` moves `mistralai` to
  position 3; in the file it sits at line 8, after a blank line. Contents otherwise exact and unpinned.
- `corpus/services/hiring.md:192` — `intelligence.go:1728-1735` is cited for *"best-attempt: `row_number()
  ORDER BY score DESC` per candidate"*. Lines `1728-1731` are the `onlyAssignments` id filter; the call to
  `usersBestOrFirstJobSimulationSession` is at `:1733`, and the actual
  `ROW_NUMBER() OVER (PARTITION BY sim_id, owner_id ORDER BY score DESC …)` is at **`:2158`**.
- `corpus/services/hiring.md:211` — `persona_write.go:69-71` is cited for the three `validation_*` tables;
  in the authoring copy `6130bfd8` they are named at **`:66-68`** (`:69` is a bare `//`, `:70-71` describe
  the removed `jobsimulation.sessions` step). The write sites themselves are `:92-94`.
- `corpus/services/hiring.md:78-79` — *"`PrivateJobSimulations` branches its result set on
  `GetOrganizationIsHiring` (`resolver_cms_queries.go:95,210,258,295`)"*: only `:95` is inside
  `PrivateJobSimulations` (declared `:89`); `:210`, `:258`, `:295` are the same call in three neighbouring
  resolvers. The count of read sites is right; the attribution to one resolver is not.
- `corpus/services/graphql-wundergraph.md:84` — *"**Ports**: **8080 → 8080**"*. No host→container mapping
  exists anywhere at HEAD (the compose service is deleted), and the one runnable path left, the repo's own
  `Makefile:10` `make run`, still publishes `5050:8080`. The parenthetical (`listen_addr 0.0.0.0:8080`,
  prod `service_port = local.port = 8080`) is correct, so the row is unclear rather than false.
- `corpus/services/graphql-wundergraph.md:104` — the `.github/workflows/` tree line names `release.yml` and
  `supergraph-update.yml`; the directory also holds `bump-version.yml`.
- `corpus/services/clerk-integration.md:99` — *"gated on the `STUDIO_ACCESS_ROLES` set — `org:admin` **and**
  `content_creator`"*. The set has four members:
  `['admin', 'org:admin', 'content_creator', 'org:content_creator']`
  (`studio-desk/app/services/userService.ts:16`, `studio-desk/src/index.ts:96`). Naming one prefixed and
  one bare form is confusing in a document whose §2 turns on that exact distinction.
- `corpus/services/storage.md:44` — *"the live code is `app/internal/storage/`"* names no ref. That
  directory exists at `app origin/main` (`2035f9a`) but **not** at the pinned checkout `b948604f`, where
  only `internal/storagens/` and `internal/publicstorage/` exist. Graded a minor because the doc's own
  measurement table pins the fold at `2035f9a` and the directory is real there.

---

## Positively cleared (audited zeros)

**ENUMERATED** — predicate enumerated independently from source, cardinalities compared:

1. `studio-room.md:181` — *"`gen.py` registers **exactly nine** arguments (`gen.py:484-492`)"*.
   Enumerated: `grep -n add_argument gen.py` @ `anthropos-studio-room aeec036` returns 9 lines, exactly
   `484`–`492`, and each flag/default in the corpus block matches the source verbatim (`-i/--interactive`,
   `-m/--media` default `simulation`, `-f/--force` `dest="forced"`, `--simid`, `--branch {stable,
   experimental}`, `--prompt`, `--annotations` default `"{}"`, `--pipeline` default `linear`,
   `--blueprint`). **ENUMERATED.**
2. `studio-room.md:199-215` — the `--template` swallow. `parse_argument` at `:18-28` calls
   `parse_known_args` and folds unknown `--key value` pairs into the request dict; the whitelist in
   `validate_blueprint_exclusivity` (`:241-271`) is exactly `{blueprint, media, forced, interactive,
   branch, pipeline, attachments_path, worklog_path, postgen_path, published_path}` and the error string
   is verbatim *"Cannot combine --blueprint with content parameters: …"*; `translate_legacy_blueprint`
   (`:205-238`, with `_LEGACY_TEMPLATE_DEFAULTS` at `:205-209`) covers exactly the three legacy names and
   emits the quoted *"is ignored; asset type is now inferred from task interactions"* warning.
   **ENUMERATED.**
3. `studio-room.md:359-366` — the `mistralai` retraction. `grep -rni mistral` over the whole
   `anthropos-studio-room` tree returns **exactly one** hit, `requirements.txt:8`; `services/ai.py:705-708`
   is `{'openai': OpenAIProvider, 'azure': AzureProvider, 'anthropic': AnthropicProvider}`; `ai.py:1-2`
   imports only `openai` and `anthropic`. Mistral in the platform is Go-side/OCR-only —
   `app/internal/cms/studio/markdownManager.go:11` (`github.com/anthropos-work/ai/mistral`), `:19`
   (`mistral.NewMistral`), `:26-27` `OCRProcess`. **ENUMERATED.**
4. `studio-room.md:35` — the asynq worker. `app/internal/cms/worker/worker.go:28` `Concurrency: 5`,
   `:31-33` `Queues: {AiVideoQueue: 7, StudioQueue: 3}`; `queues.go:4-5` maps those to `"studio"` /
   `"ai_video"`. **ENUMERATED.**
5. `studio-room.md:337-340` — `studioManager.go:119` is exactly
   `return s.runCommand(ctx, pyBin, append([]string{"studio/gen.py"}, tokens...))` and `:94-96` is the
   `studioVenvDir = "studio/studio-venv"` const block. **RE-DERIVED** (anchor + construct).
6. `hiring.md:74-77` — the *"no insights read path reads `is_hiring`"* conjunction, including its stated
   positive controls. `grep -in hiring` over `resolver_queries.go` returns 4 lines, all sim-TYPE filters
   (`:811`, `:843` content-permission lists; `:991-992` the `SimulationTypeHiring` filter); over
   `internal/organization/intelligence.go` it returns **0** (rc 1). Positive controls re-measured:
   `grep -c OrgFeatureInsights resolver_queries.go` = **8**, `grep -c JobSimulationSession
   intelligence.go` = **44** — both exactly as printed. **ENUMERATED.**
7. `hiring.md:232-234` — *"one of **four** required-and-undefaulted columns (`owner_id` `:6`, `sim_id`
   `:7`, `sim_type` `:10`, `token` `:13`; every other `NOT NULL` column in the DDL carries a `DEFAULT`)"*.
   Enumerated the whole `CREATE TABLE "job_simulation_sessions"` DDL in
   `app/terraform/migrations/20260722104506.sql:2-27`: the NOT NULL columns are id/created_at/updated_at/
   owner_id/sim_id/sim_type/status/completion_status/token/interactions_progress/score/
   validation_version, of which exactly owner_id, sim_id, sim_type and token lack a DEFAULT. `token`'s
   UNIQUE index is at `:29`. **ENUMERATED.**
8. `hiring.md:227-229` + `:239-260` — `completion_status` is a closed five-value enum
   (`ent/enum/jobsimulation.go:29-35` const block, `Values()` `:37-43`), the DDL column is a bare
   `character varying NOT NULL DEFAULT 'pending'` with no CHECK, Ent's `assignValues` casts without
   validation (`ent/jobsimulationsession.go:181-186`, `:185` is the unconditional cast), the gqlgen
   marshals are bare `graphql.MarshalString(string(v))` at `graph.go:129546-129554` and `:129392-129400`,
   and the SDL declares five lowercase members at `jobsimulations.graphqls:14` and `:128`. Every anchor
   in that block lands on the named construct. **ENUMERATED.**
9. `hiring.md:22-25` — *"`SET "score"` has **0 hits across the entire migration set**"*. Measured: 0, with
   a positive control (`SET ` matches in 39 migration files). The mirror drop is at
   `20260729133514.sql:58` (*"-- 5. Drop the mirrors"*) through `:62` (`DROP TABLE
   "local_jobsimulation_sessions";`). **ENUMERATED.**
10. `hiring.md:30-40` — *"the only `CREATE SCHEMA` in the entire migration set is `auth`"*. Measured: one
    hit, `20230817154747_supabase_baseline.sql:2`. `app/atlas.hcl:8` does pin `search_path=public`, and
    `20260722104506.sql:79` is a bare `DROP TABLE "sessions";`. **ENUMERATED.**
11. `hiring.md:35`, `:179-180`, `:328` — the `repos.yml` counts. Enumerated `- name:` entries:
    **6** at `0dab54d` (app, sentinel, storage, messenger, next-web-app, studio-desk), **4** at
    `0c91421d`; `app` is at `repos.yml:14-17` and is the only `migrations: true` entry; no
    `jobsimulation` entry at either ref. **ENUMERATED.**
12. `hiring.md:188` — *"The two copies are **byte-identical**, same line numbers"* for
    `simulationScoreColumn.tsx`. `diff` between `apps/web/…` and `apps/hiring/…` is empty; both 168 lines.
    **ENUMERATED.**
13. `hiring.md:404` — *"the regression enumerates **all 8** generic seeders"*. Enumerated from
    `stack-seeding/seeders/hiring_scope.go`'s own consult-list: PersonaSeeder, JobsimSessionsSeeder,
    SkillpathSessionsSeeder, AssignmentsSeeder, ActivitySeeder, HeroActivitySeeder, FeedbackSeeder,
    SuccessionSeeder = 8; `skipGenericActivityForHiringOrg` at `:45`,
    `TestGenericActivitySeeders_SkipHiringOrg` at `hiring_scope_test.go:75`. **ENUMERATED.**
14. `graphql-wundergraph.md:41-51` — the subgraph ladder. Enumerated `^  - name:` in
    `supergraph-config-prod.yaml` at each cited commit: `749dc86~1` = **5** (backend, skiller,
    jobsimulation, cms, skillpath), `749dc86` = **4**, `7c17e63` = **3**, `915da06` = **1**, HEAD = 1.
    Dates confirmed 2026-06-24 / 2026-07-21 / 2026-07-29. `git show --name-status 915da06` marks *both*
    `schemas/cms.graphqls` and `schemas/jobsimulation.graphqls` `D`, and that commit's own subject really
    does say *"supergraph 2→1"* — so the corpus's "3 → 1, and the subject line is wrong" is exactly
    right. **ENUMERATED.**
15. `graphql-wundergraph.md:15-19` — *"the **five** that exist are `core`, `backend`, `all`,
    `studio-desk`, `frontend`"* and *"(This list read 'the eight' until `838d907`)"*. Enumerated every
    `profiles:` key in `docker-compose.yml` @ `0c91421d` → `{core, backend, all, studio-desk, frontend}` =
    5; @ `0dab54d` → those plus `{storage-legacy, customerio-sync, messenger}` = 8. `838d907` deletes
    exactly the three services that declared the extra three. The always-on floor (services with **no**
    `profiles:` key) is `{postgresql, redis}` from `common.yml` + `{sentinel}` from `docker-compose.yml`
    = the three the doc names. **ENUMERATED.**
16. `graphql-wundergraph.md:59-68` — the subscriptions retraction. `grep -rn subscription
    supergraph-config-*.yaml` returns **0** at HEAD (rc 1); at `7c17e63` the block sat on the
    `jobsimulation` entry alone (`:12-13`). `schemas/backend.graphqls` declares `type Mutation` (`:4053`)
    and `type Query` (`:4912`) and **no** `type Subscription`; the 6 `Subscription` substring hits are
    `PlanSubscription` / `activeSubscription` / `stripeSubscriptionId` / `stripeSubscriptionItemId`.
    `bba862f` (2026-02-25) is contained by `remotes/origin/feat/use-web-socket` **only**. **ENUMERATED.**
17. `graphql-wundergraph.md:180` — *"`ci/update-subgraph.sh:9` carries **exactly one** `gh release
    download`, `-R anthropos-work/app`"*. `grep -c` = 1, and it is on line 9. **ENUMERATED.**
18. `graphql-wundergraph.md:179` — the historical `depends_on` list. Enumerated at `2adcf71~1`:
    `backend`, `jobsimulation`, `cms`, `storage` — exactly the four named, in that order.
    **ENUMERATED.**
19. `graphql-wundergraph.md:173-176` — the four compose anchors, at the **current** checkout `0c91421d`:
    `:119` studio-desk `VITE_GRAPHQL_ENDPOINT` build arg, `:135` its runtime env, `:151` next-web-app
    `NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT` build arg, `:160` its runtime env. All four land on the named
    construct. **RE-DERIVED.**
20. `storage.md:29` — *"`git grep -n STORAGE_RPC_ADDR -- '*.go'` returns **3 hits, all of them
    comments**"* @ app `2035f9a`. Measured at `origin/main`: exactly 3, at `main.go:504`,
    `internal/jobsimwiring/wiring.go:101`, `internal/storagens/callsites_test.go:189` — the three paths
    and lines the corpus prints, each a comment. **ENUMERATED.**
21. `storage.md:34-35` — *"at the demo's pinned build ref `b948604` the same guard still reports a
    mid-fold with **six** read sites"*. Re-derived the guard's own predicate
    (`_GO_GETENV = Getenv\(\s*["'](?P<name>[A-Z][A-Z0-9_]*_RPC_ADDR)["']\s*\)`,
    `rosetta-extensions/stack-core/platform_predicate_guard.py:681`) against `b948604f`: exactly **6**
    matches (`cmd/academy-asset-upload/main.go:129`, `cmd/academyImport/main.go:231`,
    `cmd/import/main.go:50`, `main.go:446`, `:524`, `:992`); `internal/jobsimwiring/wiring.go:115` uses
    lowercase `getenv(` and correctly falls outside; the `main_test.go` hits are `t.Setenv`. At
    `origin/main` the same predicate returns 0. **The corpus's "six" is right and the guard's own
    docstring at `platform_predicate_guard.py:704` ("three app read sites") is the stale one** — that is
    a tooling defect, not a corpus one, and outside my scope. **ENUMERATED.**
22. `storage.md:26` — *"0 occurrences across `docker-compose.yml`, `common.yml`, `.env_example`"*.
    `grep -rn STORAGE_RPC_ADDR` over the whole `platform` clone @ `0c91421d` returns 0, with a positive
    control (`SKILLER_STREAM` → `docker-compose.yml:71`). `.env_example` is tracked and present.
    **ENUMERATED.**
23. `storage.md:29` — the in-process wiring anchors @ app `2035f9a`: `main.go:504` (the comment),
    `:516`/`:517` (`os.Getenv(internalstorage.EnvBucket)` / `EnvPublicBucket`), `:524`
    (`internalstorage.NewManager`), `:525` (`NewPublicManager`), `:547` (`resource.NewManager`), `:1102`
    (`cmsStorage`), and `internal/storage/service.go:22` / `:24` (`EnvBucket = "STORAGE_S3_BUCKET"` /
    `EnvPublicBucket = "STORAGE_S3_PUBLIC_BUCKET"` — env-var **names**, as the corpus insists).
    All eight land on the named construct. **RE-DERIVED.**
24. `storage.md:7-13` — the `@ app 9d00a313 v1.367.0` block:
    `internal/jobsimulation/recording/recording.go:12` and
    `internal/jobsimulation/anticheat/anticheat.go:30` are both
    `storagev1 "github.com/anthropos-work/storage/sdk/storage/v1"`, and `main.go:1048` is
    `cmsStorage := internalstorage.NewClient(storageManager, storagens.CMS)`. The doc's own explanation of
    why it spells the path out (*"there is no `anticheat.go` beside `recording.go`"*) is correct — they
    are sibling packages. **RE-DERIVED.**
25. `storage.md:139` — the differing prod fates. `jobsimulation` `6092c6d2` is
    *"chore(terraform)!: remove the jobsimulation ECS service and ECR repository (M810)"*, and at
    `jobsimulation origin/main` `terraform/main.tf` is 56 lines with no `module` and no
    `service_desired_count`. `cms origin/main` still declares `module "cms"` with
    `service_desired_count = 0` (`terraform/main.tf:30`, `:39`), and `6efa1d5` is
    *"chore(ci): drop build-production — the cms ECR repository is decommissioned (M810)"* — the workflow
    is absent from `cms origin/main:.github/workflows/`. **ENUMERATED.**
26. `storage.md:48`, `:190-195`, `:204`, `:156`, `:45` — Go 1.25.0 (`go.mod:3`); **0** `*_test.go` files
    in the repo; `Dockerfile:18` is `RUN go test -v ./...`; no `//go:generate` anywhere and gqlgen is
    `// indirect` in `go.mod:23`; `internal/storage/storage.go:122` is
    `if s.S3Bucket == "" { return "", nil }` inside `GetPresignedUrl`; ports `8300:8300` / `8301:8301`
    with `PORT=8300` / `RPC_PORT=8301` on the (now-deleted) block. **ENUMERATED.**
27. `clerk-integration.md:51` + `:91` — *"129 files in `app` vs **0** in `sentinel`"* and *"sentinel /
    storage / messenger import it **0** times"*. Measured `grep -rl colony/authn --include='*.go'`:
    app **129**, sentinel **0**, storage **0**, messenger **0**; `grep -il clerk` over `sentinel/**.go`
    returns nothing. `repos.yml` lists exactly two Go repos. **ENUMERATED.**
28. `clerk-integration.md:37` + `:79` — *"Webhooks (svix, **12** event types)"*. Enumerated from
    `app/internal/clerk/events/events.go:166-235`: `user.{created,updated,deleted}`,
    `organization.{created,deleted,updated}`, `organizationInvitation.{accepted,created,revoked}`,
    `organizationMembership.{created,deleted,updated}` = **12**, and the four families are exactly the
    four the corpus names. **ENUMERATED.**
29. `clerk-integration.md:41` — *"Localization — **8** locales"*. Enumerated the
    `@clerk/localizations` import in `apps/web/src/providers/Clerk.provider.tsx:9-18`: deDE, enUS, esES,
    frFR, itIT, jaJP, nlNL, ptPT = 8. **ENUMERATED.**
30. `clerk-integration.md:103` — the SDK-version block, every figure and anchor:
    `@clerk/nextjs` `^6.39.2` in `apps/web/package.json:10`, `apps/hiring:10`, `apps/integration:9` and
    `ant-academy/code/package.json:52` (four, aligned); `next-web-app/apps/mobile/package.json:6` =
    `~2.6.18`; `ant-academy/mobile/package.json:18` = `~2.19.36` (2.6 → 2.19 is thirteen minor versions);
    `app/go.mod:31` @ `5ba17044` = `github.com/clerk/clerk-sdk-go/v2 v2.7.0` (same at the checkout).
    **ENUMERATED.**
31. `clerk-integration.md:21`/`:65`, `:46`, `:33`, `:100` — `apps/web/src/app/api/metabase/route.ts:35`
    tests `authData.orgRole !== 'admin'` (bare form); `ant-academy/mobile/src/components/
    ClerkSignInForm.tsx:49-52` accepts only `status === 'complete'` and otherwise errors *"Sign-in needs
    another step that this app does not support yet."*; every `getToken()` call in `apps/web` is
    template-less (the sole options object anywhere is `{skipCache: true}`); `ant-academy/code/proxy.js`
    runs `clerkMiddleware` and gates on `REQUIRE_ORGANIZATION_MEMBERSHIP` → `/no-organization`.
    **ENUMERATED.**
32. `frontend_architecture.md:39` — *"**29 of them across 21 non-test files**"* @ `bb3313bc`, and *"origin/
    main `ad767d1b` reads 31 across 22"*. Enumerated with `git grep` at each named ref, excluding
    test/spec/`e2e/`: **29 lines / 21 files** at `bb3313bc`; **31 / 22** at `ad767d1b`. The four
    `packages/core-js` clients (`coursebuilder`, `credits`, `talkToData`, `workforce`) carry 3 each =
    **12**, exactly as printed. The compose anchors `:161` (runtime env) and `:152` (build arg) both land
    on `NEXT_PUBLIC_BACKEND_API_URL` @ `0c91421d`. **ENUMERATED.**
33. `frontend_architecture.md:11` — *"At `0c91421` `repos.yml` holds exactly **4** entries … (it was **9**
    at `2adcf71`)"* and *"`docker-compose.yml:141`, `profiles: [studio-desk, all]` … it was 226 before"*.
    Enumerated: 4 at `0c91421d`, 9 at `2adcf71` (app, cms, jobsimulation, sentinel, storage, messenger,
    roadrunner, next-web-app, studio-desk); `:141` at `0c91421d` and `:226` at `0dab54d` both read
    `profiles: [studio-desk, all]`. **ENUMERATED.**
34. `frontend_architecture.md:24`, `:35`, `:48-53`, `:75-77` — `!apps/mobile` is in
    `pnpm-workspace.yaml`; `configs/i18n/messages` holds exactly de/en/es/fr/it/ja/nl/pt = 8;
    `apps/web/package.json:46` is `"next": "^16.2.7"` and hiring/integration/maintenance match;
    `UPGRADE-IMPACT-next16.md` exists at the repo root; root `package.json:20` is
    `"packageManager": "pnpm@10.30.3"` and `:14` is `"node": ">=24.0.0"`; turbo `^2.9.6`, typescript
    `^5.9.3`, `@commitlint/cli` + `husky` present; the `check` / `check:lint` / `check:types` /
    `check:deprecations` scripts all exist; the filter names `@anthropos/web-app`,
    `@anthropos/hiring-app`, `@anthropos/integration` are the real package names; dev ports 3000 / 3001 /
    3002 and Expo `--port 3031`. **ENUMERATED.**
35. `graphql-wundergraph.md:7`, `:80-84`, `:90`, `:103`, `:139` — `terraform/main.tf:20` is
    `service_desired_count = 1`; `locals.tf` gives `aws_region = "eu-west-1"`, `project = "wundergraph"`,
    `port = 8080`; `health_check_path = "/health"`; `terraform/tests` exists; `package.json` is exactly
    `{"name": "graphql-wundegraph"}` and `CLAUDE.md`'s heading carries the same misspelling;
    `Dockerfile:22`/`Dockerfile.dev:29` pin `ghcr.io/wundergraph/cosmo/router:0.275.0`, `:8`/`:7` pin
    `wgc@0.104.0`, both on `node:22.11-alpine`; `federation_version: =2.3.2`; `config.compose.yaml`
    playground/introspection **on**, `config.prod.yaml` **off**; `supergraph-config-prod.yaml` lists
    `backend` alone, `schemas/` holds `backend.graphqls` alone, `subgraphs.conf` is the single line
    `BACKEND=v1.360.0`; there is no `-local` config variant. **ENUMERATED.**
36. `graphql-wundergraph.md:187-193` — the Makefile history. `Makefile:10` reads `PROFILE ?= graphql` at
    `2adcf71` and `PROFILE ?= core` at `0dab54d`/`0c91421d`; `b56d731` (*"drop the WunderGraph router;
    point local dev at backend"*), `360efd4` (*"remove the router service and clone entry outright"*) and
    the merge `2adcf71` are all dated 2026-07-31; `d92e84e` really does rename the compose service
    `wundergraph` → `graphql` (and the profile with it); `63d285c` introduced the service with **no**
    `dockerfile:` key and a `git@…` context. **ENUMERATED.**
37. `graphql-wundergraph.md:206-238` — the smoke-test section.
    `app/internal/authorization/gqlauthz/gqlauthz.go:186` is
    `return errorResponse(fmt.Errorf("unknown viewer: %w", errForbidden))`, guarded at `:175-176` by
    `isPublicOnlyOperation || isFederationQuery || (environment == colony.Development &&
    isIntrospectionOnlyQuery)`; `gqlauthz_test.go:132` is the `"bare __typename is not exempt"` case and
    `:176` is `TestAnonymousRejectionLogsAtWarn`. **RE-DERIVED.**
38. `hiring.md` — the whole front-end read-path table and the M224 ejection mechanism.
    `useGetClerkOrganization.tsx:20-21` matches the quoted code **verbatim**, `:16-18` is the
    `regularOrganizationList` filter; `apps/web/src/context/UserStatusContext.tsx:141-173` is the effect,
    `:144-149` computes `userHasAllHiringOrgs`, `:168-172` is the
    `buildSwitchHandoffUrl({targetProduct:'hiring', …})` call; the symmetric guard is
    `apps/hiring/src/context/UserStatusContext.tsx:125` and `:144-145` (`targetProduct: 'workforce'`);
    `useNavbarSections.tsx:460` is the `isHiringOrg ? tNavbar('results') : tNavbar('activityDashboard')`
    label inside `:459-466`; `template.tsx:90` is `Boolean(organization)`; `FreeTrialContainer.tsx:29` is
    `Boolean(!isHiringOrg && organizationId)`; `packages/graphql/src/query/insights.ts:31-82` is exactly
    the `insightsJobSimulationByMemberships` document; `InsightsByMembersContainer.tsx:359` is the plain
    Ant `<Drawer>`; and a `find` for `(.)`/`(..)`-prefixed dirs under `apps/` returns **0** with a working
    positive control — the M231 *"zero intercepting routes"* refutation holds. **ENUMERATED.**
39. `hiring.md` resolver anchors — `resolver_queries.go:1034` (decl), `:1035` (the
    `OrgFeatureInsights` gate, first statement), `:1053` (`GetMemberships` with
    `{StatusActive, StatusInvited}`), `:1080` (the `IntelligenceManager` call), `:1085` (the *neighbouring*
    resolver's own gate), `:1089` (its `GetMembership`) — all six land exactly on the named construct,
    including the fine distinction the doc draws between `:1085` and `:1089`. Likewise
    `intelligence.go:1700` (`m.ent.JobSimulationSession.Query()`), `:1820` (`score := RoundFloat(...)`),
    `:1844` (the blind `SessionCompletionStatus` re-cast), `:1846` (`Score: &score`),
    `:885-886` (`insightsSortColumn` mapping `CompletitionStatus` → `FieldCompletionStatus`), and the
    default sort `score DESC, completition_status ASC, session_started_at DESC` at `:1752-1763`.
    **RE-DERIVED.**
40. `hiring.md:139`/`:93` and `clerk-integration.md:93` — `d11a403` really does delete the `jobsimulation`,
    `cms` **and** `roadrunner` compose services *and* their three `repos.yml` entries in one commit.
    **ENUMERATED.**

---

## What I could not settle

- `hiring.md:154` — *"The captured public snapshot carries **87 real `SIMULATION_TYPE_HIRING` sims**"* and
  `:158` — *"the captured snapshot has **0 `job_position` rows** (the prod '443' was never captured)"*.
  The taxonomy snapshot cache under `.agentspace/snapshots/taxonomy/` holds only the skills/job-role COPY
  files; the Directus half of the cache is where simulation rows would live and it is not populated on
  this box. Settling this needs a populated Directus snapshot manifest, or a `stacksnap status` against a
  cache that has one.
- `graphql-wundergraph.md:8` — *"the repo | **ARCHIVED on GitHub, 2026-07-30** (read-only)"*. Archive state
  is a GitHub API property, not visible in the clone.
  `gh api repos/anthropos-work/graphql-wundergraph --jq .archived` would settle it.
- `storage.md:15` and `:139` — *"the production **M810** rollback-path teardown was not measured here"* /
  the cms prod state being *"UNMEASURABLE"*. Both are self-declared as unmeasured; the deletion lands in
  the `infrastructure` repo, which is in no clone set on this box. Consistent with the corpus's own
  hedging, so nothing to book — but nothing cleared either.
- `hiring.md:378-417` — the M228 live-render numbers (`~8 candidates per position`, `Talent-Mgr 8,
  BD-Lead 8, Inside-Sales 9, Project-Mgr 9, AWS-Security 8`, `p95 1.27 s`, `4/4 flake`). These are
  measurements taken on a live `billion` demo; the harness (`hiringComparableFloor = 6` at
  `stack-seeding/seeders/contentref.go:158`, `RENDER_GATE_FLOOR ?? '6'` at
  `stack-verify/e2e/tests/render-hiring-comparison.spec.ts:56`) and the drift fence exist and agree, but
  the run itself cannot be re-derived from source.
- `storage.md:49` and `graphql-wundergraph.md:86-87` — the *"corrected M257x iter-NN"* provenance clauses.
  These reference prior iterations' ledgers, which live in the directory I am barred from reading. I
  graded only the technical claims around them.
