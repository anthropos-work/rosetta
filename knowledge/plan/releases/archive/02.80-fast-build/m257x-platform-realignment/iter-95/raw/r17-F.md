# Seat F — reading #17

**Refs used** (re-derived at open, 2026-08-05; they match the addendum sheet exactly):
`platform 0c91421d` (= origin/main) · `app b948604f` (origin/main `2035f9a4`) · `next-web-app bb3313bc`
(origin/main `8297c684`) · `sentinel 88bc5592` · `storage 4ce8ece5` · `messenger fa47850d` ·
`cms ca50c817` · `graphql-wundergraph 60c229f3` · `roadrunner 87d8d443` · `jobsimulation 462343b0` ·
`studio-desk 14a5442a` · `ant-academy 9c3843cd` · `rosetta-extensions` (authoring) `6130bfd8`.
Corpus HEAD `1937e1f`. `proto`, `taxonomy` and `chronos` are **not on disk** in any form (no clone, no
module cache) — see *What I could not settle*. `colony@v0.34.3` and `ai@v1.40.1` **are** in the local Go
module cache and were read directly.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/services/ant-academy.md` | 448 | yes |
| `corpus/services/studio-desk.md` | 435 | yes |
| `corpus/services/chronos.md` | 245 | yes |
| `corpus/architecture/shared_libraries.md` | 242 | yes |
| `corpus/services/askengine.md` | 124 | yes |
| `corpus/services/skillpath.md` | 112 | yes |

Also read in full, per instruction: `corpus/ops/platform-alignment.md` §5 (lines 249–1362 of 2139).

Search-pipeline hygiene: every `grep`/`git grep` that returned an empty result was run with stderr read
and paired with a positive control in the same invocation (e.g. the `SkillPathSessionService` = 0
measurement was taken alongside `SkillerServiceHandler` = 5). Two zsh failures were caught **because**
stderr was read — an unquoted `--include=*.go` (`no matches found`) and a history-modifier expansion
(`"$c:repos.yml"` → `:r` stripping the extension, producing `838d907epos.yml`). Both were re-run; neither
produced a recorded finding.

---

## Blockers

### B1 — `shared_libraries.md`'s "12 Connect-RPC services" enumeration omits `StorageService`; the count is wrong

- **Anchor:** `corpus/architecture/shared_libraries.md:77-83` —
  > "12 Connect-RPC services are defined: `UsersService`, `OrganizationsService`, `CMSService`,
  > `JobSimulationService`, `SkillerService` … `SkillPathSessionService` … `LabSessionService` …
  > `AuthorizationService` (Sentinel), `MessengerService`, `RoadRunnerService`, `RealtimeService`,
  > `ChronosService` (archived service, contract still present)."

- **Why it is false:** I did not re-derive the sum from the list (§5 rule 24); I enumerated the
  **predicate** — *"a Connect-RPC service defined in `github.com/anthropos-work/proto`"* — independently,
  from every consumer in the clone set, before counting anything:

      grep -rhoE '[a-z0-9]+v1connect\.[A-Za-z]+' --include='*.go' \
           app sentinel storage messenger cms jobsimulation roadrunner | sort -u

  That yields **10** distinct services with a real generated `New<X>ServiceHandler` /
  `New<X>ServiceClient` in `proto`: `AuthorizationService`, `CMSService`, `JobSimulationService`,
  `LabSessionService`, `MessengerService`, `OrganizationsService`, `RoadRunnerService`, `SkillerService`,
  **`StorageService`**, `UsersService`. Adding the three the document names that no consumer references
  (`SkillPathSessionService`, `RealtimeService`, `ChronosService`) gives a **floor of 13**, not 12 — and
  the one the document drops is `StorageService`.

  `StorageService` is unambiguously a `proto` Connect service, at **both** live proto pins:
  - `stack-demo/storage/cmd/root.go:63` — `rpcMux.Handle(storagev1connect.NewStorageServiceHandler(`
    (`storage/go.mod:8` pins `proto v1.196.0`)
  - `stack-demo/storage/sdk/storage/client.go:11`, `:28` — `storagev1connect.NewStorageServiceClient(`
  - `stack-demo/storage/sdk/storage/v1/service.go:19`, `:29`, `:34` — `storagev1connect.StorageServiceClient`
  - `stack-demo/app/internal/app/users/services/import_profile_test.go:25`, `:158` —
    `"github.com/anthropos-work/proto/go/storage/v1/storagev1connect"` /
    `var st storagev1connect.StorageServiceClient` (`app/go.mod:18` pins `proto v1.210.0`)

  The block names **no ref**, so it is graded at the checkout (briefing §"which ref settles a claim"); it
  is equally false at both proto pins in the tree. The omission is not cosmetic: `storage` is the service
  whose fold is the current headline, and a reader who consults this page for the storage RPC contract
  concludes there isn't one. Note the same cell already distinguishes the *legacy vendored*
  `go/simulator/storage/v1` stub (line 94, verified at `storage/internal/migration/migration.go:13`) from
  the real package — so the document is aware of two storage proto packages and still enumerates neither
  service.
- **Confidence:** high

### B2 — the canonical "five internal Go libraries" SET is the wrong five: `analytics-go` is a real internal Go library module and is absent from the enumeration

- **Anchor:** `corpus/architecture/shared_libraries.md:3` — *"The Anthropos Go services share **five**
  internal libraries."*; the 5-row summary table at `:14-20` (colony · proto · ai · authn · taxonomy);
  and the consumption blockquote at `:22-31` — *"**None of these are cloned by `make init`** … Each is
  pulled as a **private Go module** during a service's Docker build … **Without a valid `GH_PAT`, the
  build fails.**"*

- **Why it is false:** §5 rule 24 again — re-derive the SET, not the arithmetic. I enumerated every
  `github.com/anthropos-work/*` module requirement across all seven Go repos on disk
  (`grep -n 'anthropos-work' <repo>/go.mod`), then removed the entries that are *service* repos imported
  as modules by the fold (`app` requires `messenger v0.42.0` and `storage v0.15.2`). What is left — the
  non-service, internal, `GOPRIVATE`-fetched Go library modules — is:

  | module | pin | required by |
  |---|---|---|
  | `colony` | v0.35.2 / v0.35.1 / v0.34.3 | app, sentinel, storage, messenger, cms, jobsimulation, roadrunner |
  | `proto` | v1.210.0 / v1.207.0 / v1.205.0 / v1.200.0 / v1.196.0 | all seven |
  | `taxonomy` | v1.2.0 | app, messenger, cms, jobsimulation (direct); sentinel, storage (`// indirect`) |
  | `ai` | v1.40.2 | app, cms, jobsimulation |
  | **`analytics-go`** | **v0.3.1** | **app** (`app/go.mod:15`) |

  `analytics-go` is not a stub or a vendored third party — it is imported as real code, including a
  subpackage:
  - `app/internal/payments/handler.go:10` — `"github.com/anthropos-work/analytics-go"`
  - `app/internal/tracking/handler.go:4-5` — `a "github.com/anthropos-work/analytics-go"` and
    `brevo "github.com/anthropos-work/analytics-go/brevo"`

  It satisfies every criterion the page itself uses: an `anthropos-work` Go module, not a deployed
  service, no container/port/compose entry, absent from `platform/repos.yml` (verified at `0c91421d` and
  across all ten `repos.yml` revisions), therefore fetched over `GOPRIVATE` with `GH_PAT` at Docker build.
  It is imported by strictly **more** live services (one) than `authn`, which the page documents at length
  precisely *because* it is imported by none (`:19`, `:145`, `:161-166` — I confirmed 0 hits for
  `"github.com/anthropos-work/authn` across all seven repos; positive control: `colony/authn` = 129 files
  in `app`).

  The corpus has never classified it correctly anywhere: its **only** other mention in the tree is
  `corpus/architecture/platform-migration-status.md:157`, which lists it in the net-new-org-repo census as
  an *"analytics **service**"* — which it is not. So a reader has two corpus statements about
  `analytics-go` and both are wrong, in different directions.

  Consequence for real work: the blockquote at `:22-31` is the corpus's only inventory of the private
  modules a Docker build must be able to fetch. An engineer using it to scope a `GH_PAT` or a `go.work`
  will be one module short, and the failure lands at image-build time.
- **Confidence:** high

### B3 — "**Every** live Go service: app, sentinel, storage, messenger — and **only** those four" is an unpinned currency claim that is false at the platform checkout, and the fenced map says the opposite

- **Anchor:** `corpus/architecture/shared_libraries.md:42` (colony's **Imported by** cell), first sentence —
  > "**Every** live Go service: app, sentinel, storage, messenger — and **only** those four."

- **Why it is false:** at the platform checkout `0c91421d` (= `origin/main`; `838d907`, merged
  2026-08-05) `storage` and `messenger` are gone from **both** artefacts that define "live":

  - `repos.yml` at `0c91421d` contains exactly four entries — `app`, `sentinel` (`type: go`),
    `next-web-app`, `studio-desk`. I enumerated the whole revision history
    (`for c in $(git log --format=%h -- repos.yml); do git show "${c}:repos.yml"; done`, 10 revisions):
    `d11a403` still had `storage` + `messenger`; `838d907` removed them.
  - compose at `0c91421d` declares seven services in total —
    `postgresql`, `redis` (`common.yml:2`, `:24`), `sentinel:5`, `backend:28`, `studio-desk:112`,
    `next-web-app:143`, `gotenberg:170`. There is **no `storage` service, no `messenger` service and no
    `customerio-sync` service**, hence no `storage-legacy` and no `messenger` profile to start them with.
  - `repos.yml`'s own header at `0c91421d` puts `storage` and `messenger` in the bucket this cell calls
    *"frozen legacy"*: *"skiller, skillpath, roadrunner, jobsimulation, cms, **messenger, storage** and
    customerio-sync are all served in-process … Those repos are frozen legacy: they own no local schema,
    no compose service and no clone entry here."*

  So by the cell's own criteria, two of its four "live" members are frozen legacy.

  **Corpus self-contradiction, and it is with a fenced document:**
  `corpus/architecture/platform-migration-status.md:92` — *"`storage` | merged-into-app | decommissioned |
  no | … `838d907` (merged `0c91421`, 2026-08-05) deleted the compose service **and** the `repos.yml`
  clone entry in one commit, so `make init` no longer clones it and **the `storage-legacy` profile is
  gone**"* — and `:93` says the same for `messenger`.

  **Why I did not treat the cell's `0dab54d` pin as covering this.** The pin appears *later* in the cell,
  attached to a different clause (*"the `cms`, `jobsimulation` and `roadrunner` containers … are gone
  from compose at `0dab54d` (`d11a403`)"*). The quoted sentence names no ref and asserts currency
  (*"**live**"*, *"**only** those four"*) — which the addendum's rule 1 and `platform-alignment.md` §5
  rule 33 both say cannot be pinned into silence. Rule 33's own worked example of a pin crossing a row
  boundary is *this document's Imported-by row*, which makes this the second occurrence in the same cell.

  **What I deliberately did NOT book.** Three neighbouring claims are equally stale but name the ref
  **inside the clause that makes the claim**, so under the briefing's "a pin is a DATE, not an excuse"
  they grade TRUE at their pin, and I verified them there: `:69` (*"those four are the whole of the live
  skew at platform `0dab54d`"* — pins re-derived exactly: app/messenger `v1.210.0`, sentinel `v1.200.0`,
  storage `v1.196.0`, cms `v1.207.0`, jobsimulation `v1.205.0`, roadrunner `v1.196.0`), `:105` (ai), and
  `:181` (*"All four Go repos a stack still clones and builds at platform `0dab54d`"*).
- **Confidence:** medium — the ground truth is certain (I read `repos.yml` and both compose files at the
  checkout); the *grading* turns on whether a mid-cell pin reaches backwards over an unpinned,
  currency-asserting opening sentence. I hesitated, and I book it as a BLOCKER because a reader who acts
  on it will look for `stack-*/storage` and `stack-*/messenger` clones that no documented step creates,
  and because a fenced sibling document already states the opposite.

---

## Minors

- `corpus/services/ant-academy.md:135` — renders `emptyCatalogView()` as literally
  `{ chapters: [], skillPaths: {}, series: [] }`. Measured at `code/src/lib/serverTenant.js:115-117` it is
  `{ chapters: [], skillPaths: {}, series: [], bundles: PUBLIC_BUNDLES, catalogVersion: CATALOG_VERSION }`
  — two members dropped, and `PUBLIC_BUNDLES` comes from the committed FS catalog
  (`ucourses/catalog.js`), i.e. it is *not* empty. The "→ 0 cards" conclusion still holds for the course
  grid, so this is non-material.
- `corpus/services/ant-academy.md:144` — *"a separate, unauthenticated `code/public/catalog.json`
  (~2,667 entries)"*. Measured: the file's `courses` array holds **2,715** (`languages` = 7). Hedged with
  "~"; nothing depends on it.
- `corpus/services/ant-academy.md:258` — *"~26 Playwright e2e spec files (tests/e2e/)"*. Measured: **31**
  `tests/e2e/*.spec.js`, **32** counting all `*.spec.*`. Hedged with "~". (The paired *"1000+ Vitest
  tests"* clears — a raw `it(`/`test(` count over `*.test.js{,x}` is 2,701.)
- `corpus/services/ant-academy.md:402` — the "Other content-pipeline helpers" list omits **`export-path`**
  (`.claude/skills/export-path/` exists; `_shared/` is infrastructure and reasonably excluded).
- `corpus/services/ant-academy.md:336-340` — *"The **app's** env file is `code/.env`, not the repo root"* +
  `cp .env.example .env`. Root `CLAUDE.md` says the opposite shape: *"`ant-academy/code/.env.local` (not
  the repo root — the React app reads **only** from `code/.env.local`)"*. Both work (Next.js loads `.env`
  **and** `.env.local`), so ant-academy.md is the correct side and CLAUDE.md's "only" is the wrong word;
  recorded as a cross-file contradiction for the repair pass rather than a defect in my file. On disk the
  clone carries `code/.env.example` and `code/.env.local`, no `code/.env`.
- `corpus/architecture/shared_libraries.md:116` — *"`mistral.NewMistral` | Mistral — **OCR only**
  (chat/embeddings/speech `panic`)"*. Four methods panic, not three:
  `ai@v1.40.1/mistral/completion.go:40` `ChatCompletion`, `:44` `ChatCompletionStream`, `:48`
  `CreateEmbeddings`, `:52` `CreateSpeech`. "OCR only" conveys it correctly.
- `corpus/services/askengine.md:104-112` — the per-file test inventory omits `anthropic_test.go`
  (1 top-level `Test` func). The four counts it *does* give are exact (see cleared list).
- `corpus/services/chronos.md:22`, `:171`, `:190` — instruct `cd stack-dev/chronos`, but the banner at
  `:3-11` states chronos is no longer cloned by `make init`, and no documented step creates that
  directory (it exists in neither `stack-dev/` nor `stack-demo/`). The doc never says how to obtain the
  repo it then tells you to `cd` into.

---

## Positively cleared (audited zeros)

**ENUMERATED** — predicate re-derived from source independently, cardinality stated before any arithmetic:

- `shared_libraries.md:181` — union of `type: go` entries across the **whole** `repos.yml` history = **11**
  (`app, cms, jobsimulation, skiller, skillpath, chronos, sentinel, intelligence, storage, messenger,
  roadrunner`), all four of `skiller/skillpath/chronos/intelligence` present in the first revision
  `a2a3ee6`, skillpath the last of the four to go (dropped by `a4db680`). Derived by iterating all 10
  revisions of `repos.yml`. **Matches the document exactly, including "that set is 11".**
- `shared_libraries.md:181` — taxonomy importers over the 7 Go repos on disk = **6 of 7**; the exception is
  `roadrunner` (requires only colony + proto). Direct: app, messenger, cms (`go.mod:13`), jobsimulation
  (`go.mod:15`); `// indirect`: sentinel, storage. Every pin `v1.2.0`. **Exact.**
- `shared_libraries.md:70` — `app` registers **six** Connect handlers, enumerated by
  `grep -rn 'New[A-Za-z]*ServiceHandler(' --include='*.go' .` over the whole `app` tree (not just
  `main.go`): `:1187` Users, `:1188` Organizations, `:1196` Skiller, `:1204` JobSimulation, `:1213` CMS
  (inside `if cmsRPCServer != nil` at `:1212`, closing `:1214`), `:1227-1228` LabSession. Neither
  `SkillPathSessionService` nor a RoadRunner service is among them. **Every anchor exact at `b948604f`.**
- `shared_libraries.md:79` — `ROADRUNNER_RPC_ADDR`: **0** hits in `app` Go source at `9d00a313`
  (`git grep -c … 9d00a313 -- '*.go'` → exit 1) and **0** in `platform` at `0dab54d` (positive control:
  `AUTHORIZATION_ADDRESS` = 1). `docker-compose.yml:118` @ `0dab54d` really does set `AWS_REGION`, as the
  correction says. `internal/skillpaths/skillpaths.go:27-31` carries *"the drop-in for the **removed**
  skillpath RPC client"* verbatim at `:28`; `jsrunner.NewRunnerManager` is at
  `internal/jobsimwiring/wiring.go:123` @ `9d00a313`. **All exact.**
- `shared_libraries.md:104-105, 145` — `ai v1.40.2` in app/cms(`:9`)/jobsimulation(`:11`);
  `colony/authn` importers = app 129 files, cms **6**, jobsimulation **8**, and **0** in sentinel, storage,
  messenger, roadrunner; standalone `github.com/anthropos-work/authn` imported by **nothing** (positive
  control in the same pass). **Exact, including "6 and 8".**
- `shared_libraries.md:202-209` — taxonomy figures re-derived from the *primary artefact*, not the prose:
  `.agentspace/snapshots/taxonomy/5afc0bcc…/manifest.json` (`source: primary-read`, `public_only: true`,
  `predicate: org-null`, `captured_at 2026-06-29`) gives `public.skills` = **42,790**, `public.job_roles`
  = **22,470**, `public.job_role_embeddings` = **18,919**, each under `organization_id IS NULL`. The
  document's REFUTED/UNVERIFIED split and the "18K came from `job_role_embeddings`" hypothesis both stand.
- `askengine.md:47-48` — `TableRegistry`: the `defs := []TableDef{…}` literal at `registry.go:96-529`
  holds exactly **60** entries (`{Name:` lines), each with an explicit `Schema:` field — **57 `"public"`,
  3 `"directus"`**. No commented-out members (only `:25`, a doc comment). **Exact.** `registry.go:183-195`
  is precisely the jobsimulation transition-alias comment (*"RESOLVE TO THE PUBLIC TABLES"*, *"stays in
  rewriteSchemas until M710"*), and `rewriteSchemas` is at `sandbox.go:293` with
  `jobsimulation/skillpath/directus/skiller`.
- `askengine.md:104-109` — test counts by `grep -c '^func Test'`: `sandbox_test.go` **49**,
  `executor_test.go` **13**, `prompt_test.go` **10**, `followups_test.go` **14**. **All four exact.**
  `TestBuildSystemPrompt_RulesBlockIsIdentical` exists at `prompt_test.go:176`.
- `askengine.md:64-67` — the `/ask` route set enumerated from `internal/web/backend/backend.go:171-187`:
  group at `:171` with CORS + Echo authn, then `POST /stream`, `GET`/`POST /conversations`,
  `GET /conversations/:id`, `GET /conversations/:id/stream`, `DELETE /conversations/:id`,
  `POST /messages/:messageId/feedback`, `POST /admin/auto-rules/distill`, `GET /admin/auto-rules`,
  `POST /admin/auto-rules/:id/activate`, `DELETE /admin/auto-rules/:id`. **Complete and exact.**
- `askengine.md:35-38` — all five `ask_*` tables exist in `internal/data/ent/migrate/schema.go`:
  `ask_auto_rules:1304`, `ask_conversations:1332`, `ask_messages:1377`, `ask_query_examples:1416`,
  `ask_query_lessons:1454`.
- `skillpath.md:47` — `repos.yml` entry counts: **9** @ `2adcf71` (0 skillpath), **6** @ `0dab54d`,
  **4** @ `838d907` and at `0c91421`. **All four figures exact.**
- `skillpath.md:30-33` — `SkillPathSessionService`: **0** occurrences in Go source across the whole clone
  set; **0** `skillpath…v1connect` imports (positive control `SkillerServiceHandler` = 5, same pass).
- `skillpath.md:41-44` — all **8** named GraphQL operations exist in
  `internal/web/backend/graphql/graph/schemas/skillpath_sessions.graphqls` (`:71`, `:72`, `:73`, `:77`,
  `:78`, `:79` + `@deprecated` at `:80`, `:81`, `:82`).
- `skillpath.md:87-89` — exactly **three** migrations post-date `20260729133514.sql` at `app 9d00a313`:
  `20260731131307.sql`, `20260731154527_academy_chapter_progress_completed_at.sql`,
  `20260803143844_ai_readiness_recommendation_path.sql` (`atlas.sum` is not a migration). **Exact.**
- `studio-desk.md:34` — `vite.config.ts` `rollupOptions.input`: exactly the **9** prod entries listed
  (`home`, `sim-advanced-builder`, `builder-skill-path`, `simulation-builder`, `sim-guided-builder`,
  `generation`, `catalog`, `academy`, `skills`) plus `dev-accept` behind `...(isProduction ? {} : …)`.
  **Exact, including the dev-only carve-out.**
- `studio-desk.md:59` — `src/routes/` holds exactly `ai.ts`, `dev.ts`, `skillpath.ts`, `youtube.ts`.
  `skillpath.ts` is 61,080 bytes ("~61KB", the largest).
- `studio-desk.md:279-287` — no DB driver in `package.json` (0 hits over
  pg/postgres/prisma/sqlite/mysql/mongo/knex/drizzle/typeorm/sequelize), **0** `DATABASE_URL` reads in
  `src/`/`app/` (positive control in the same pass), and the repo's only tailscale mention is
  `app/core/main.ts:105-107` (GlitchTip DSN).
- `ant-academy.md:268-279` — the `isPublic` matcher: I enumerated the full `createRouteMatcher([…])` at
  `code/proxy.js:112-188`. **25 static patterns + 2 `DEV_LOGIN_ENABLED` + 3 `VISUAL_BYPASS` = 30**, and
  the doc's ten grouped rows reproduce all 30 with no additions and no omissions. `/api/chapters/*` is
  correctly *absent*. Gate semantics verified too: `VISUAL_BYPASS = BENCHMARK_VISUAL_BYPASS === "1" &&
  NODE_ENV === "development"` (`proxy.js:62-64`, whitelisted not blacklisted).
- `ant-academy.md:233-234` — `SUPPORTED_LOCALES = ['en','it','es','fr','de','nl','pt']` at
  `src/i18n/locale.js:10` (**7**), `role="menu"` at `LanguageSelector.jsx:88` with `role="menuitemradio"`
  at `:97`, mounted at `TopBar.jsx:76`; `LocaleSwitch` mounted at `PublicHeader.jsx:20`. The **retraction**
  at `:236-238` is correct on its own terms too: `5b05b7d9` (Tue 2026-05-05) already has
  `<LanguageSelector />` in `TopBar.jsx:48`, and `e22f3230` (Mon 2026-05-18) is where
  `SUPPORTED_LOCALES` goes to 7 — both refs and both dates check out.
- `studio-desk.md:21, 140` — every compose anchor at `0c91421d`: build arg `:119`, runtime env `:135`,
  `depends_on: backend` `:138-140`, `profiles: [studio-desk, all]` `:141`; and the historical ones —
  `:204`/`:223-225`/`:226` at `0dab54d`, and the `cms` `depends_on` really did span `:337-341` at
  `2adcf71`. **Every one exact.** This document's anchor discipline is the best in my set.

**RE-DERIVED** (weaker — I re-computed a figure the document displays, without independently enumerating
its set):

- `shared_libraries.md:41, 69` — colony and proto version pins per repo, read straight from each `go.mod`.
- `askengine.md:21` — `rules.md` is 146,219 bytes ("~146 KB").
- `ant-academy.md:412-413` — the three named workflows exist in `.github/workflows/`.

**Other point checks that cleared** (single-fact reads, listed for coverage): colony `go.mod` `go 1.25.0`;
`colony/authorization` package name really is `authorizer`; `rate.NewLimiter(1,5)` with the reject path
commented out at `graphql.go:255`; Sentry gated on `IsProduction() || FORCE_SENTRY` (`logging.go:46-47`);
30 s DB health ticker + `errCount >= 3` (`db.go:46`, `:61`); `/_meta` at `http.go:20`; pubsub
`PoisonQueue` / `MaxRetries:3` / `Multiplier:3`; `PosthogChecker` + `pollingInterval = 5 * time.Minute`;
`authn.NewManager` + exactly two providers (`dummy`, `provider/clerk`) + the `eid` claim const; the `ai.AI`
interface's **9** methods matching the doc's list one-for-one; `AzureDefaultAPIVersion =
"2025-04-01-preview"`; `retry.Attempts(10)` + skip on `401, 403, 404`; Anthropic
`ChatCompletionStream`/`CreateSpeech` panic; `external_services.md:555` is exactly the sentence
`shared_libraries.md:126` cites (the *"no ordered EU-first fallback chain"* retraction — note root
`CLAUDE.md` cites `:546` for the same thing, which now lands inside the studio-path blockquote, but that
file is not mine); `app/internal/aiusage/ai_usage.go` model→price `switch` at `:36` and `Event_AiUsage`
handler at `:98`; `askEngineMaxConns = 6` (`app/main.go:150`), `COPILOT_DB_CONN` (`:313`),
`QueryTimeout = 10 * time.Second` + `SET LOCAL statement_timeout`, `MaxInlineRows = 200`,
`MaxCellLength = 400`, `maxAgenticIterations = 15`, `loopTimeout = 10 * time.Minute`,
`OrgCheckFeaturePermission` at `ask/handler.go:718`, `MaxFollowupSuggestions = 4`, the four
`crossValidate` warnings, `text-embedding-3-small` + `embedding <=> $1::vector` cosine retrieval,
`DefaultModelID = "eu.anthropic.claude-sonnet-4-6"` / `DefaultRegion = "eu-west-1"` /
`Temperature: param.NewOpt(float64(0))` / `stripAnthropicAuthMiddleware`, and tags `v1.267.0` =
2026-05-07 and `v1.340.0` = 2026-07-17; `skillpath/session.go:205-207` and
`skillpaths/skillpaths.go:88-95` (both the `// cms-in-app deseam` comment and the `GetSkillPathDomain`
call, exact); `InsightsSkillPathByMemberships` at `organization/intelligence.go:1144` with the
skill_path_id + status ∈ {active, completed} + tenant predicate at `:1159-1170`; `DROP TABLE
"local_skill_path_sessions"` at `terraform/migrations/20260729133514.sql:63` and
`local_jobsimulation_sessions` at `:62`, with **no** `local_*` Ent schema; the next-web-app per-member
drill-down verified **at `origin/main` as the doc says** — `userData` is
`return null as unknown as MembershipEnriched`, the results table is commented out, and the body renders
`t('enterprise.insights.comingSoon')` = `"Coming soon"` (`configs/i18n/messages/en/enterprise.json:1891`);
`app/CLAUDE.md:109` and `app/knowledge/architecture.md:28` both still list `SkillPathSessionService` at
`app` origin/main `2035f9a` (exact lines); `SKILLPATH_STREAM=skillpath` at `docker-compose.yml:72` and
**zero** `*_RPC_ADDR` in any compose file at `0c91421d` (positive control `AUTHORIZATION_ADDRESS` at
`:48`); `graphql-wundergraph/terraform/main.tf:20` `service_desired_count = 1`, which supports
studio-desk.md's *"the Cosmo Router survives in production only"*; the five ant-academy demopatch wirings
in `rext demo-stack/ant-academy.sh` at exactly `:56`, `:66`, `:75`, `:84`, `:90`; autoverify assert *(f)*
at `stack-verify/live/autoverify.sh:459-523` (and it now probes `/` **and** `/library`, matching
`platform-alignment.md` §5 rule 11's repair); `ensure-clones.sh` phase **(d2)** at `:20`/`:150`;
`jobsimulation 09631fb2` = *"remove Chronos references and update documentation to reflect Asynq
integration for session timeout management"* (exact message, 2026-04-17) and platform `045857c` removing
chronos from `repos.yml`; `app/internal/jobsimwiring/worker.go:24-35` really is the Asynq pool
construction; ant-academy's `next ^16.2.6` / `react ^19.2.5` / `reactCompiler: true` /
`engines.node >=22` / `dev --port 3077` / mobile `dev:web` → `PORT=8555` / `vercel.json` =
`{"framework":"nextjs"}` / manifest at `app/layout.jsx:132` / vendored FA at
`code/public/assets/fontawesome/{css,webfonts}` / `RegisterServiceWorker.jsx` is a kill-switch / Cosmo
`gpt-5.2` + `https://api.openai.com/v1/responses` + `localStorage('openai_api_key')` +
`NEXT_PUBLIC_FEATURE_TRAINING_COACH` / `author-cover` on `gpt-image-2`; the whole DB-authoritative catalog
chain (`serverTenant.js:143-146` literal, the `serverChapterBody.js:51-67` body chain,
`app/not-found.jsx:43` *"You wandered off the trail."*, `draftMode.js:46-48`) and the beacon route's
`UPSERT_CHAPTER_PROGRESS`/`SET_LAST_ACTIVITY` imports; studio-desk `STUDIO_ACCESS_ROLES` at
`src/index.ts:96` verbatim, `PORT` in-code fallback `9100` at `:60`, `AI_DEFAULT_TIER` fallback
`'thinking_fast'` at `services/ai/config.ts:182` vs `.env.example:61` = `fast`, the four tier maps,
`node:24-alpine`, `YOUTUBE_API_KEY` at `routes/youtube.ts:43` with the `_mock: true` fallback, and
`GCLOUD_SERVICE_ACCOUNT` present at `.env.example:120` + `terraform/main.tf:129` but read by **no** code
in `src/`.

---

## What I could not settle

1. **Everything that requires the `proto` repo itself.** `proto` is not cloned (by design — it is a
   private Go module) and is **not** in the local module cache
   (`~/go/pkg/mod/.../anthropos-work/` holds only `ai@v1.40.1` and `colony@v0.34.3`). So
   `shared_libraries.md:91-94` — *"legacy buf v1 single-module layout (`proto/buf.yaml`, name
   `buf.build/anthropos/platform`) — no `buf.work.yaml`"*, *"`go/simulator/*` holds ~10 generated
   packages"* — and `:68`'s *"CI pins buf `v1.57.0`"* are unverifiable here. Likewise whether
   `RealtimeService` and `ChronosService` contracts still exist (note
   `app/internal/jobsimwiring/wiring.go:121-122` says the jobsim resync *"deleted chronos + realtime"*,
   which is about jobsim, not proto). **Settled by:** a `proto` clone at `v1.210.0`, or
   `GOPRIVATE=… go mod download github.com/anthropos-work/proto@v1.210.0` then reading the cache.
   *B1 does not depend on this* — it is proved from the consumer side, which is a lower bound.
2. **Everything that requires the `taxonomy` repo.** `shared_libraries.md:189` (*"a **131-line** node-id
   library (`node.go`)"*), `:179` (*"go 1.21.0, zero external dependencies"*), and the whole ID-format /
   constructor section `:215-229` (`K-GOXXXX-F63F`, `K-DOTNET-DDE9`, `K-CSHAXX-5F5B`). I confirmed only
   that `taxonomy.NodeID` is used as a *type* (`app/cms_skiller_reader.go:12,27,36,48`), which supports
   the library-not-a-dataset correction. **Settled by:** a `taxonomy@v1.2.0` clone or module download.
3. **Almost all of `chronos.md` §§ Architecture → Usage Example.** The `chronos` repo exists nowhere in
   this tree (`stack-demo/` and `stack-dev/` both lack it), so `schema.sql`, the `timers` table + its four
   indexes, ports `8080`/`8081`, `sqlc`/Cobra, the `--interval`/`--server` flags, the `LIMIT 10` +
   `FOR UPDATE SKIP LOCKED` batch, the 3-second run timeout, the 1-minute Sentry heartbeat, and the
   `EventTimerWentOff` message shape are all **untested** by me. What I *did* settle: the removal commits
   (`045857c`, `09631fb2`) and the Asynq destination. **Settled by:** cloning
   `git@github.com:anthropos-work/chronos.git`. This is the largest unmeasured surface in my set — 245
   lines of detail whose ground truth nobody in this reading can reach.
4. **`shared_libraries.md:40` "built with `golang:1.26-bookworm`"** (colony's row). colony is a library and
   its module zip carries no Dockerfile. The *services* split: `app` and `sentinel` build on
   `golang:1.26-bookworm`; `storage` and `messenger` on `golang:1.25-bookworm`. Defensible as written for
   the live pair, but it is a claim about colony's own CI that I could not reach.
5. **`shared_libraries.md:41`'s parenthetical "(archived `chronos` pins `v0.30.1`)"** and **`:143`'s
   standalone-`authn` tag `v1.7.0`** plus the *"missing `return` after the websocket-skip"* defect — both
   repos are absent.
6. **Live-measured demo numbers** in `ant-academy.md` (*"65 real cards with 0 Draft chips"*, `:74`) and
   `studio-desk.md` (*"skeleton-visible p95 4669 ms → 817 ms"*, `:412`; *"canAccess 4049 → 38 ms"*,
   `:423`). These require a running demo stack; nothing in the tree can falsify them statically.
7. **`studio-desk.md:21`'s "asking for `studio-desk` as the only profile exits 1"** — I read the compose
   source and the mechanism is exactly as described (`depends_on: backend` at `:138-140`,
   `profiles: [studio-desk, all]` at `:141`, and `backend` is in `[core, backend, all]` at `:110`), but I
   did not run `docker compose` to observe the exit code. Consistent with root `CLAUDE.md`'s profile table.
