# M257x — work-mstone-iters journal

- started: 2026-07-31T11:59:47Z
- worktree: /Users/kirality/Workspace/anthropos/rosetta
- branch: m257x/platform-realignment
- release: 02.80 "fast-build"
- mode: default (semi-autonomous)
- iteration_protocol: corpus/ops/platform-alignment.md (ABSENT — iter-01 authors it)
- gate: green cold cycle x3 on odysseus @ platform origin HEAD + full Playthrough suite + migration-status map fenced vs repos.yml + zero rext writes to dropped schemas + KB-fidelity green

## build-iter (run 1, started 11:59) — bootstrap tok

### iter-01 (bootstrap tok) — opened 12:04 UTC
- launched Phase 0b KB-fidelity gate + 4 measurement probes (platform truth / jobsim merge status / net-new repo census / rext write-surface)
- 12:05 UTC heartbeat: verified locally that stack-dev repos.yml is stale (skillpath still listed; cms+jobsim still migrations:true; roadrunner was ALREADY false). Blast radius measured: 52/88 corpus files mention jobsimulation, 38 mention skillpath. Search set clean (88 files, 0 binary-classified) — no NUL-byte trap. Probes still running.
- 12:06 UTC finding: gate clause 2's "30 live Playthroughs" VERIFIED correct (30 distinct pt- ids in manifests == 30 @pt: tags in e2e specs, two methods). CLAUDE.md's "18 live / playthroughs.md:108 authoritative" is STALE -> corpus-fidelity finding for clause 5. Self-caught a false-absence en route: my first regex matched 'id: pt-' but the field is 'playthrough:' -- files were clean UTF-8, no NUL trap. Discipline held.
- ROOT-CAUSE CANDIDATE: stack-demo/clones.pin.json pins platform at detached sha 28c5f0dd + every clones.lock.json entry has "behind": null. The lock file HAS a drift field and never fills it. No skillpath entry in the pin file (so pins are newer than stack-dev/platform/repos.yml). This is the enabling mechanism for all three occurrences -> natural home for the drift-detection half of platform-alignment.md.
- F-M257x-01 (ROOT CAUSE, measured): rext's clone-freshness subsystem computes behind-count ONLY when ref != "HEAD". Every pinned clone is DETACHED, so 11/11 demo clones report behind=null and 0/11 count as freshness problems -- while the bring-up prints "all clones provably fresh-or-pinned". DEMO_FRESHNESS_STRICT=1 (the documented HARD go/no-go gate) escalates only stale-by-neglect/pin-drift/unknown, so a pinned-tag repo 386 commits behind PASSES it. Pinning disables drift detection on exactly the repos we pin. Corrected my own earlier guess ("field never populated") -- it IS computed, just short-circuited. Written up: scratch/finding-pin-blindspot.md
- Confirmed demo-stack rext pin = fast-build-m256-iter-25b (7f0bc38), 25 commits behind authoring main (31d2b5d). The gap contains ALL of M256's close/harden-final AND every M257 fix (B1 dropped mirrors, B2 unobtainable studio, the gate-instrument fix). So a cold cycle at the current pin would re-hit both M257 blockers -> the pin MUST be advanced before gate clause 1 can be attempted.
- GOOD NEWS: fast-build-m257-iter-01/02/03 ARE on origin (git ls-remote verified). The "tagging is not publishing" trap is NOT currently active.
- A1 LANDED (platform truth). Platform origin HEAD 1e8e754 (2026-07-30). BIG: subgraph count is **1**, not 3 (since 915da06 2026-07-29) -> corpus wrong for 2 days. repos.yml @ HEAD: app is the ONLY migrating repo; cms/jobsim/roadrunner marked "legacy - folded into app". BUT docker-compose.yml has NOT moved since 07-21 and still runs cms/jobsim/roadrunner containers -- the compose fold is platform PR #20, still OPEN. So the platform repo is internally contradictory BY DESIGN (docs merged, compose deferred). app main contains internal/{cms,jobsimulation,skillpath,skiller}. STILL MOVING: app PR #1103 (v9.0) folds storage + messenger next. Also found: platform's own `make migrate` swallows failures with `|| true`. My roadrunner correction independently confirmed. Written up: scratch/finding-platform-truth.md
- 12:10 UTC heartbeat: A1 in. Still awaiting A2 (jobsim classification), A3 (net-new repo census), A4 (rext write surface + build/test), Phase 0b (KB-fidelity gate).
- A2 LANDED (Q2 ANSWERED): jobsimulation = **merged-into-app**, high confidence, NOT mid-merge. Standalone scaled to 0 (terraform/main.tf:40), app owns it unconditionally (main.go:604), 136 files in app/internal/jobsimulation. Tables re-created in **public** (20260722081626_jobsim_data_model.sql). Mirror drop confirmed in SQL (20260729133514.sql:62-64). NOTHING creates the jobsimulation schema locally now. FOUND THE PLATFORM'S OWN WRITTEN PLAN: roadmap-jobsim-in-app.md M701->M710, and the whole program v2.0 skiller -> v5.0 skillpath -> v7.0 jobsim -> v8.0 cms -> **v9.0 support-in-app = storage + messenger next** (PRs #1096/#1098/#1103 open). So occurrences #4/#5 are already named -> we can get AHEAD of the class.
- FENCE DESIGN CONSTRAINT (critical): `migrations: false` entails NOTHING on its own -- sentinel is also migrations:false yet alive with its own `sentinel` schema (compose:43 search_path=sentinel). A naive "migrations:false => don't write that schema" fence is WRONG, and tuning it to pass would tune it into missing jobsimulation. Fence must derive from what ACTUALLY creates schemas.
- NUL-byte trap CONFIRMED REAL in app/terraform/migrations (169 files, ~172 with NULs). `rg -a` mandatory in that tree.
- 12:11 UTC heartbeat: A1+A2 in. Awaiting A3, A4, Phase 0b.
- Verified independently: platform's own awk on origin repos.yml -> MIGRATION_REPOS == "app" only; only app has a schema: key (public). platform PR #20 OPEN (compose only). app PR #1103 (v9.0 storage+messenger hard cutover) OPEN, NOT a draft. FENCE REFERENCE DECIDED: empirical `information_schema.schemata` on a cold freshly-migrated stack, + a checked-in manifest fenced both ways against it (demo_knob_guard.py precedent). Written up: scratch/design-fence.md
- 12:12 UTC heartbeat: 3 of 5 open questions answered (Q1 program shape, Q2 merged-into-app, Q4 subgraphs=1). Holding TOK-01 authoring until A4 (rext) + Phase 0b land -- strategy must not commit to a re-point plan before the write-surface census is in.
- DOC-M257-studio-in-app GROUNDED: `app/internal/cms/studio/` exists on main (studioManager.go, markdownManager.go, xlsx.go + args_safety/subprocess_env tests) -> app embeds the studio orchestration and spawns the Python pipeline as a SUBPROCESS. `init-studio` is genuinely absent from app's Makefile (verified with a positive control: the 1787-byte/53-line fetch succeeded and 'migrate' matched, so the empty grep is a real absence not a failed fetch). 18 corpus files touch studio-room/cms-studio wording; the carry-forward's "5 places" undercounts the surface.
- Clause 5 blast radius measured: **9** corpus files assert the refuted 3-subgraph count (CLAUDE.md, architecture/{external_services,architecture_overview}, services/{cms,skillpath,backend,README,graphql-wundergraph}, ops/update_guide).
- 12:14 UTC heartbeat: drafted the clause-3 migration-status map (scratch/draft-migration-status-map.md, 68 lines) -- all rows cited to sha/file:line; outstanding = net-new rows (A3) + the machine fence. Still awaiting A3 + A4 + Phase 0b.
- Inherited-claim CORRECTION: overview says "app grew UNDOCUMENTED domains: coursebuilder / AI Labs+credits / askengine / server-owned academy". All five docs EXIST (corpus/services/{coursebuilder,ai-labs,askengine,academy-backend,ai-readiness}.md). That inherited gap is NOT real -- one less thing to backfill. Minor real gap: CLAUDE.md says "27 service docs", actual is 29.
- 12:15 UTC heartbeat: still awaiting A3 (net-new repo census) + A4 (rext write-surface/build/test) + Phase 0b (KB-fidelity gate). Noted rext already has an information_schema.schemata precedent (dev-stack/tests/test_migrate_dev_live.py:144) for the fence to follow. No existing platform-alignment doc in corpus/ or .claude/ -- confirmed net-new.
- My own pre-A4 sizing: 68 rext files reference `jobsimulation.`; ~13 real table names (activity/go/volumes are false matches). local_* mirror refs STILL PRESENT: 53 x local_jobsimulation_sessions + 19 x local_skill_path_sessions = 72. M257 claimed a 34-site/20-file re-point, so either most of these are explanatory comments or the re-point was incomplete -- A4 must split live code from prose. FLAGGED, not concluded.
- MAJOR de-risk for clause 4: a dropped-mirror fence ALREADY EXISTS (stack-core/tests/test_dropped_mirror_fence.py, M257 iter-03). It is well designed -- scores occurrences not lines, ALLOWS comments deliberately (so the dead table's rationale survives -> my 72-ref count is NOT 72 violations; 42 are comments BY DESIGN), asserts the POSITIVE replacement too (avoiding the M219/M222 re-empty trap), and HAS mutation-verify. Limitation: DROPPED_RELATIONS is a hardcoded 2-tuple at :66, table-level -- would NOT have caught the jobsimulation.* SCHEMA going away. So clause 4 = GENERALIZE it to schema granularity (static, offline) + ADD the live information_schema assert. Verified ZERO live writes to local_* remain: all 8 SQL-ish hits are the fence's own planted test fixtures or M257 attribution comments -> M257's re-point WAS complete. design-fence.md revised.
- Clause 3 fence precedent FOUND: stack-core/corpus_index_guard.py (M24b) is a rext-owned guard that runs against the rosetta CORPUS (CORPUS_ROOT=./corpus) -- and it exists because an unindexed-doc miss recurred across three straight releases, the same recurrence shape as this class. So the migration-status map's fence is rext-owned, lives in stack-core beside demo_knob_guard/dropped_mirror_fence/test_collection_fence, and rext already parses platform repos.yml in 4+ places. Consolidated 3-layer fence plan written into design-fence.md.
- 12:18 UTC heartbeat: design phase complete (all 3 fences have rext precedents; no invention needed). Still awaiting A3 (net-new repo census) + A4 (rext build/test/write-surface) + Phase 0b (KB-fidelity gate) before TOK-01 is authored.
- 12:18 UTC heartbeat: A3 + A4 + Phase 0b still running (~14 min elapsed). Both are heavy by design: A4 runs 6 Go module suites + 4 Python suites; Phase 0b sweeps 29 service docs + architecture. Not a stall.
- 12:19 UTC drafted corpus/ops/platform-alignment.md (181 lines, in scratch pending Phase 0b): §1 the class as a PROGRAM (v2.0->v9.0, next two folds named), §2 why it hid (pin blindness, measured 11/11), §3 six detection signals + the two traps (migrations:false entails nothing; the platform's own docs lag its code ~9 days), §4 classification incl. merged-into-app vs mid-merge as three separate questions, §5 re-point procedure, §6 the 3-layer fence with rext precedents, §7 cadence keyed to the named v9.0 fold, §8 reading list.
- EXECUTED all six §3 detection signals end-to-end to prove the drafted procedure actually runs (a procedure that has never been executed is a hypothesis). Results: signal1 repo set = 10 repos (app cms jobsimulation sentinel storage messenger roadrunner next-web-app studio-desk graphql-wundergraph); signal2 migrating = app ALONE; signal3 declared schemas = app->public ALONE; signal4 subgraph schemas = backend.graphqls ALONE; signal6 org repo count = **93**. Procedure verified executable.
- A3 LANDED (Q3 ANSWERED): 93 org repos, 10 in repos.yml, 27 documented, 50 absent from corpus entirely, **20 net-new & undocumented**. `auth`: **Clerk is NOT being replaced** (3-commit 8-hour spike, "Not yet deployed", BetterAuth not a dep anywhere in the org, all human activity stopped 2026-06-18, its fan-out PRs target now-decommissioned skiller/skillpath) -- relief for Clerkenstein; but a kb-migration-plan repo exists and colony/authn/provider/jwks DID ship in the prod binary unregistered. `AI-Labs` IS the labs-api the corpus names without a repo (live Go control plane behind anthroposlabs.com, Firecracker) -> highest-value doc gap. LiveKit agent family = 5 clones, corpus names NONE; azure-eu + azure-eu-fr dispatch nothing (decommissionable). sim-qa WRITES TO PROD unmarked. Corpus fidelity errors found: skillpath still ACTIVE not archived; chronos still ACTIVE not archived; `studio-room` is not a repo name (it's anthropos-studio-room).
- SIXTH NEAR-MISS, self-caught: A3's first run returned 0 hits for ALL 93 repos -- NOT NUL bytes but ripgrep's default engine REJECTING look-around while subprocess SWALLOWED stderr, so empty stdout read as "absent". Fixed with rg -aP + a positive control (Clerkenstein -> 203 files). PROTOCOL LESSON: a swallowed stderr turns an engine rejection into a false absence. Also 4 apparent corpus "mentions" were coincidental substring matches (incl. anthropos-work/auth matching `authn`).
- 12:21 UTC heartbeat: 4 of 5 open questions answered (Q1,Q2,Q3,Q4). Awaiting A4 (rext write-surface/build/test = Q5) + Phase 0b (KB gate). Both still running at ~18 min; A3 alone took 17 min so this is expected, not a stall.
- Phase 0b LANDED: **YELLOW** (severity warning), iter-01 may proceed. NOT RED -- correctly reasoned that RED would deadlock since the remedy for these blockers IS the milestone. Report at MILESTONE_DIR/kb-fidelity-audit.md; it also wrote decisions.md KB-1/2/3 + spec-notes ground-truth block.
- RESOLVED A CONTRADICTION BETWEEN TWO PROBES (measured, not adjudicated): Phase 0b's KB-2 claimed public holds TWO session tables so a naive re-point reads BLANK. FALSE -- `sessions` is created at 20260722081626:2 then DROPPED at 20260722104506:79 as the rename completes. public holds exactly ONE (job_simulation_sessions). A2 was right. This INVERTS the risk: a naive re-point FAILS LOUDLY, the safe mode -- had I inherited KB-2 I'd have guarded a silent failure that cannot occur.
- ALSO REFUTED A2: its "169 migration files, ~172 contain NUL bytes" is arithmetically impossible AND false -- measured 169 files, **0** with NULs, on A2's own clone.
- CORRECTED MYSELF: CLAUDE.md's "27 service docs" is RIGHT (29 .md minus README + TEMPLATE = 27). My earlier "off by two" was wrong; Phase 0b caught it independently.
- CONFIRMED Phase 0b's own refutations: next-web-app.md is clean UTF-8, rg counts identical with/without -a (20==20), NOT binary-skipped.
- BIG PROTOCOL LESSON: the NUL-byte trap is FOLKLORE. Three real false-absence incidents this milestone, NONE caused by NUL bytes (wrong regex / engine-rejection+swallowed-stderr / didn't happen), while the one place NULs were asserted had zero. Guidance now: measure NULs before blaming them; NEVER swallow a search's stderr (engine rejection == false absence); always run a positive control in the same pass; check the field name first. Written up: scratch/finding-claim-reconciliation.md
- A4 showed a 21-min transcript silence (background-stall signature). Nudged it for a PARTIAL report prioritised on the schema write-surface census + per-module build/test + pin state, with instructions to foreground long commands with explicit timeouts rather than background-and-yield. Also fed it my two measured corrections (public.sessions does NOT exist; 0 NUL files) so it does not report refuted claims.
- A4 LANDED (Q5 ANSWERED) and REFUTES the milestone's central premise. Everything offline is GREEN: 6/6 Go modules build+vet+gofmt clean, 5 Python suites (NOT 4 -- dev-stack is a fifth), **2,617 tests, 0 failures** (I independently reproduced: 6/6 builds, 58 ok pkgs, gofmt clean, stack-core 330 OK). So "less than we think runs" is WRONG at the unit level -- and right at the live level, because every seeder test asserts against a recording fake Conn that accepts any table name.
- THE REAL MECHANISM (verified myself): rext's demo-stack/migrate-demo.sh:81-85 CREATEs the legacy schemas ITSELF and :106 atlas-applies a **hardcoded 4-tuple** (app:public cms:cms jobsimulation:jobsimulation skillpath:skillpath), :108 gated on `[ -d $DEV/$r ]` -- it NEVER consults repos.yml's migrations: flag. So the schema isn't missing: rext creates it, from the still-cloned legacy repos, bypassing the file that declares the truth. The tuple is HAND-MAINTAINED -- the :96 comment shows someone edited it for skiller but nobody did for jobsim/cms. TIME BOMB: when M810 removes the legacy repos from the clone set, `[ -d ] || continue` silently skips them and 13 write targets 42P01 at once. CANARY ALREADY VISIBLE: skillpath is in the tuple but absent from origin repos.yml -> never cloned -> schema created empty, harmless only because rext writes 0 skillpath tables.
- Real jobsimulation surface = **12 tables (9 write / 3 read-only)**, not ~15 -- 2 of the inherited names are comments (conversation_extractions is explicitly labelled a red herring; activity is a test comment).
- IMMEDIATE GATE BLOCKER (verified myself): the rext pin is INCONSISTENT. .agentspace/rext.tag (SoT) = cockpit-deeplinks-v1, **63 commits behind main**; the stack-demo clone is checked out at a DIFFERENT tag (fast-build-m256-iter-25b). ensure-clones.sh:94-101 makes the mismatch **FATAL exit 1** (M217 WARN->FAIL), so /demo-up ABORTS on this box today. Two places still call it "non-fatal": ensure-clones.sh:68's own header AND corpus/ops/rosetta_demo.md:17-18.
- LANDED corpus/ops/platform-alignment.md (the milestone's iteration_protocol_ref, previously absent). PROVED the corpus_index_guard goes RED on it before adding the index row (rc=1, named the exact file), then GREEN after.
- [x] iter-01 (tok/bootstrap) — measured the platform at origin HEAD and found its microservice consolidation is a 5-service PROGRAM with the next two folds already in open PRs; authored the missing corpus/ops/platform-alignment.md and executed its own procedure; identified the class's root cause (pinning silently disables rext's drift detection: 11/11 clones report behind:null while the log claims "provably fresh", and the pin's SoT file is git-ignored so it never shows in a diff) and its local mechanism (migrate-demo.sh's hand-maintained 4-tuple creates the legacy schemas itself, bypassing repos.yml, armed to break 13 write targets at M810); refuted 5 inherited/audited claims by measurement including one from the KB audit that had inverted the planned guard. Gate reads: 0/5 clauses met; clause 1 BLOCKED because /demo-up aborts today on a FATAL rext-pin mismatch (SoT 63 commits behind main). rosetta 99f0aca pushed; rext tagged fast-build-m257x-iter-01 (verified on origin).
