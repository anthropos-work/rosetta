---
iter: 125
milestone: M257x
iteration_type: tik
status: archived
opened: 2026-08-07
---

# iter-125 — the second corpus, and the register entry re-derived at source

**Run 80, tik 2.** Priorities 2 and 3 of the user's directed scope. Two lines, both planned, both
closable in this iter — so the scope-creep tripwire is not engaged by the pair.

## Active strategy reference

No `TOK-09` and there will not be one (`TOK-08`'s sealed rule). This tik runs under **the user's directed
scope of run 80**. `F4` binds: nothing here is `P`, is `N`, or is a clause-5 verdict.

## Re-survey (Phase 1 Step 0)

Both targets confirmed still standing at iter-125 open, and one has **moved in a way that changes the
work**:

- **P2** — `org-repos.md` § 11 exists and is thorough (iter-123). But its **item 1 says the router
  residue question is *"Unresolved"* and that *"this corpus says the module is still declared"*** — which
  **§ 3 of the same file resolved**, and which iter-124 has since propagated to 24 sites. So § 11 now
  contradicts § 3 **inside one document**. That is a `fix`, and it was not in the directive's framing.
- **P3** — the `directus` register entry is open and unverified. The re-derivation is possible now that
  `infrastructure` is clonable.

## Targets

### P2 — state the contradiction where a reader meets it

The directive is precise about what this corpus can and cannot do: **it cannot edit AKB** (different repo,
outside this milestone's two-repo scope). What it can do:

1. State the contradiction **where a reader would meet it** — at
   `shared_libraries.md#taxonomy-figures`, which is where this corpus's own figures live and where every
   other doc points; § 11 of `org-repos.md` is a repo census a taxonomy reader will never open.
2. Make `toolchain_overview.md`'s **install recommendation carry the warning** — that is the sentence
   that puts the contradicting figure into an engineer's editor.
3. **File the reconciliation to the platform-defect register**, so it has an owner outside this milestone.
4. **Repair § 11 item 1**, and record that **AKB was RIGHT and this corpus WRONG** on the router residue.
   The directive is explicit that this is not one corpus correcting another.

### P3 — verify the directus finding is stated correctly AND completely

Re-derive the pin and the line at source, per `D-M257x-121-1` (*a defect report is re-derived at source
before it is filed, and a false premise is corrected IN the filing*). Then check nothing in the corpus
asserts the opposite.

## Hypothesis

The register entry's **conclusion** holds and at least one of its **anchors** does not — the standing
pattern for entries written from a partial clone set (`D-M257x-121-1` found exactly this, twice, in one
routing).

## Expected lift

No `N` movement and no reading. The deliverables are: the contradiction stated at the point of use, the
recommendation warned, the reconciliation filed with an owner, and the register entry either confirmed
verbatim or corrected with the correction stated first.

## Escalation conditions

**No legal/compliance/policy escalation** (§5 rule 48) — the register is filing, not escalation, and the
directive says so in as many words.

## Acceptable close-no-lift outcomes

If the register entry re-derives verbatim, "confirmed at source, nothing to correct" is the deliverable
and the iter closed its scope.
