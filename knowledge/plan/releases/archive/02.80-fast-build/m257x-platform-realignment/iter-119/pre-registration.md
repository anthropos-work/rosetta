# iter-119 pre-registration — written BEFORE the read, at corpus `194361e4`, platform `0c91421`

**This reading answers the one question `TOK-08` was authored to answer, and `TOK-08` is the USER's
re-scope, not an agent's: AFTER A FULL MECHANICAL SWEEP — every mechanically-decidable class censused
exhaustively rather than sampled — DOES THE POOL DRAIN?**

`TOK-07`'s own pre-registered falsification fired at iter-116 (`P = 37` against `P ≥ 15`, sealed at
`85f6f1c` before the first seat was dealt). That rule routed the milestone to a re-scope **conversation
with the user** rather than to an eighth agent-authored strategy. The user supplied `TOK-08` on
2026-08-07; iter-117 sealed it in its FIRST commit `577446b` before a single sweep line; iters 117 and 118
executed it.

| step | iter | what landed |
|---|---|---|
| 0 | 117 | **`TOK-08` sealed** (`577446b`), then **class 1 — intra-corpus citation resolution**: 1,520 citations over 92 docs, **8 false, 8 repaired**, fence reach **1,520/1,520 = 100 %**, ratchet at zero |
| 1 | 118 | **class 2 — platform-source citation resolution**: 1,070 candidates, **0 false**, reach **675/1,070 = 63.1 %**, denominator `citation-candidates-minus-non-citations` |
| **2** | **119** | **THIS READING, and it is the grading.** |

**The class list was fixed in iter-117's `overview.md`, could only GROW, and did not.** Both classes have
a fence that (a) enumerates its population corpus-wide, (b) stands at zero findings, and (c) ships with a
mutation control AND an anti-vacuity control that can actually fire. **The sweep is complete as
pre-registered**, so the trigger is armed and this reading grades it.

## What this reading can and cannot separate — say it before the number, not after

### The disclosure that matters most, and it is a warning against reading a flat `P` as an excuse

**The full mechanical sweep changed FIVE LINES of the audited scope.** 1,520 + 1,070 = 2,590 citations
enumerated; 8 false; **2 of the 8 land inside `corpus/services/**` + `corpus/architecture/**`**. The
in-scope line count is **identical** at 10,871.

That is not a hedge invented after a disappointing number — it was measured and written down twice before
this reading opened:

- **`D-M257x-117-2`, recorded at iter-117's close:** *"the machine-reachable half of class 1 is largely
  DISJOINT from the half iter-116's reading booked"* — band #7's 10 predicates are **construct** defects
  (a pin naming lines that hold something else), and of **387** lines carrying a bare `` `:NN` `` pin only
  **4** are machine-resolvable. **"Little `P` movement should be expected from this sweep."**
- iter-118's close restated it and named the residual it did not attempt.

**So a `P` near 37 is a PREDICTED outcome, not an explained-afterwards one — and `TOK-08`'s branch fires
on it anyway.** That is the correct behaviour of a pre-registered rule and this file will not be read as
softening it. The user pre-authorized the refutation branch in advance and stated they would carry it.

**What a flat `P` would then MEAN is the deliverable:** not *"enumeration does not work"* — it demonstrably
worked, closing 8 defects that survived four graded readings — but **"the mechanically-decidable classes
are not where the pool is."** The pool is semantic, and a census cannot reach a semantic claim.

### The second disclosure, and it is the reason this reading is worth more than iter-116's

**This is a TRUE SEAT-LEVEL REPLICATE — the first this milestone has ever taken.** Same 14 clone shas
(fourth consecutive frozen reading), same instrument, and — proven by re-running the partitioner at both
refs — **the identical partition, seat for seat**. iter-116 had to disclose that its partition was
recomputed and that seat-level numbers were therefore incomparable to iter-109's. This one does not.

**Test-retest reliability of this instrument becomes directly measurable for the first time** (band #3
below). Every number this milestone has published rests on the assumption that a reading of an unchanged
corpus would return roughly the same set. **That assumption has never been tested.** It is tested here,
and it is tested on the same reading that grades the strategy — so whichever way the primary lands, the
sheet also returns the first measurement of how much any single reading can be trusted.

## THE PRE-REGISTERED VERDICT RULE — `TOK-08`'s, RESTATED, NOT RE-CUT

**The threshold is already fixed** by `TOK-08` in milestone-root `decisions.md`, sealed at `577446b`
before the sweep began. It is restated here verbatim in substance. **It is not re-cut, re-centred, or
re-interpreted, and neither branch is softened.**

> **Baseline: `P = 37`** — iter-116's reading, corpus `f581de09`, rule sealed at `85f6f1c`.
>
> - **`P ≥ 19`** (i.e. `P` fell by **less than half** of 37) → **the mechanical classes are not the bulk
>   of the pool. Enumeration-first is REFUTED too. STOP. Do NOT author a successor strategy.** Report the
>   refutation and hand the milestone back for a **scope decision from the user**. The user has stated in
>   advance that this is a legitimate outcome and that they will carry it. **There is no `TOK-09`.**
> - **`P ≤ 18`** (fell by **half or more**) → the method is working. **Say so with the number** and
>   continue sweeping classes.

**`P` is the count of distinct false PREDICATES**, adjudicated, in-scope, upheld — the identical
definition used at iter-101, iter-103, iter-109 and iter-116. **`N`, the distinct-anchor count, is
reported alongside as the continuity metric** with its historical bands (`≤ 8` drains / `9–19` partial /
`≥ 20` does not drain), and **if `P` and `N` disagree, `P` governs and the disagreement is the headline.**

**Both are FLOORS in every branch.** They are unions of two passes whose measured per-pass recall has run
33–83 %. **No branch of this rule is a gate verdict.** Clause 5 is met by a reading that returns **zero**,
and by nothing else — four user rulings, not reopened here.

## Bands — 14 of them, each stated so it CAN fail

Prior readings graded **4 of 9**, **3 of 7**, **5 of 9**, **4 of 10**, **7 of 13** and **7 of 14**, with
*mechanism* claims mostly holding and *magnitude* guesses mostly failing (iter-116: 6 of 8 mechanism, **0
of 3 magnitude**). That split is the useful shape and these bands do not drift toward safe.

| # | prediction | band | kind |
|---|---|---|---|
| 1 | per-reading in-scope upheld BLOCKER count (n₁, n₂) | **[8, 34]** each | magnitude |
| 2 | **union `N`** (anchors) | **[22, 52]** | magnitude |
| **2p** | **union `P`** (predicates) — **the primary** | **[20, 44]** | magnitude |
| **3** | **overlap with iter-116's published 37 predicates**, matched on PREDICATE, blind — **the test-retest band** | **[10, 30]** | mechanism |
| 3b | **within-reading `m`** between passes #31 and #32, as a **share of the union** | **[10 %, 55 %]** | mechanism |
| 4 | adjudicator upheld rate (raw) | **[72 %, 94 %]** | mechanism |
| 5 | the two passes' recalls against the union differ by | **≥ 12 points** | mechanism |
| 6 | **wrong-tree rejections** (the briefing-defect class) | **[0, 3]** | mechanism |
| 7 | **intra-corpus citation** defects among upheld in-scope blockers | **≤ 6** | mechanism |
| 8 | **platform-drift share of upheld in-scope blockers** | **[20 %, 60 %]** | mechanism |
| 9 | per-seat booked spread over the 14 seats (max − min) | **≤ 8** | magnitude |
| 10 | **repair-induced** — upheld blockers whose anchor sits in prose iters 117–118 wrote | **[0, 1]** | mechanism |
| 11 | **anchors per predicate** (`N`/`P`) | **[1.00, 1.45]** | mechanism |
| 12 | **net-new** — upheld in-scope anchors sitting in a **multi-pin block** | **[2, 12]** | mechanism |
| **13** | **re-found `SMALL-CLASS-ADJUDICATED` predicates** — of iter-113's 15 judged-rare verdicts, how many does this reading book again | **[0, 4]** | mechanism |

## What each band is actually risking

**#1 / #2 / #2p — the magnitude guesses, and they are NOT centred on the answer the gate wants.** All
three magnitude bands failed at iter-116 and iter-109's `#2p` failed HIGH at 24 against `[3, 14]`. **The
honest centre for `P` here is ~34** — the corpus is 5 in-place lines from the corpus that measured 37, the
clones are frozen for a fourth reading, and nothing semantic was repaired. **`≤ 18` is therefore a genuine
long shot and the band is cut to say so** (`[20, 44]` does not even contain the `≤ 18` branch). If `P`
lands at or below 18, **the band FAILS and the strategy is VINDICATED** — and that combination is not a
contradiction, it is the single most informative outcome on this sheet: it would mean 2,590 enumerated
citations and 8 repairs drained a pool that four readings could not, which would be extraordinary and
would deserve extraordinary scrutiny of the reading itself before it was believed.

**#2p is the primary and #2 is the secondary — recorded here so the order cannot be chosen after the
fact.**

**#3 — the TEST-RETEST band, and it is the sharpest instrument on this sheet.** iter-116's #3 asked how
many of iter-109's predicates survived a repair that closed all 24; it got **3 of 24** and correctly read
that as *the repair held*. **This band asks the opposite question and it has never been asked before:**
with essentially NO repair between the two readings, how many of iter-116's 37 does a fresh 14-seat
reading find again? The pool is the same pool. So this measures **recall**, not repair.
- **A high overlap (≥ 25)** says the instrument is reliable and the 37 is a real, stable, standing set.
- **A low overlap (≤ 10)** is the alarming outcome and it would be the most important finding this
  milestone has produced: it would mean each reading samples a largely *different* slice of a much larger
  pool, that `P` is a **weak floor** rather than a near-complete count, and that every published number
  here — including the 37 the branch is graded against — understates the corpus's true defect load by an
  unknown multiple. **That would bear on the refutation more than the primary itself does.**
- The band `[10, 30]` is wide **because there is no prior measurement of this quantity**, and a narrow
  band on an unmeasured quantity is theatre.

**#3b — banded as a SHARE, held at the same cut for the fourth reading.** iter-101 measured 17 %,
iter-103 61 %, iter-109 held, iter-116 held. Held again unchanged, because nothing about the passes
changed.

**#4 — the precision band.** Readings: 92.1 / 93.0 / 92.7 / 93.1 / 78.3 / 77.8 / 97.9 / 91.4 / **92.6 %**.
Fails high at > 94 %, low at < 72 %. Reported **twice** — raw, and with the `wrong-tree` class separated.

**#5 — the asymmetry, predicted to return for the third reading running, and it has now FAILED twice.**
iter-101's passes recalled 83.3 % / 33.3 % (50 points); iter-103 collapsed to 9.1; iter-109 and iter-116
both failed this band. **A third consecutive failure says the mechanism is wrong, not the tuning**, and
this sheet will say so rather than re-cutting the band a fourth time.

**#6 — the briefing defect, measured for the sixth time.** Series **4 → 1 → 1 → 0 → 0**. Nothing about the
addendum's two-tree rule changed, so this is a clean replicate.

**#7 — RE-AIMED, and it is the direct in-reading test of the sweep.** iter-116's version cut at ≤ 4 and
measured 10 — which is what made intra-corpus mis-citation the largest class and what `TOK-08` was aimed
at. **Class 1 is now censused and fenced at 100 % reach with the ratchet at zero.** If the census reached
the class the reading books, this should fall hard — **`≤ 6` holds**. If it comes back near 10 again, that
is the sharpest possible confirmation of `D-M257x-117-2`: **the machine and the reader are looking at
different halves of one class**, and it will be reported as the finding rather than as noise.

**#8 — held two-sided at iter-116's cut.** Measured ~33 % (iter-109) and ~38 % (iter-116) over a frozen
subject. Fails LOW if drift somehow drained, HIGH if it is now the overwhelming majority.

**#9 — tests the partition, not the corpus.** The partition is identical to iter-116's, whose spread was
43 lines — the tightest of the four readings. A booked spread over 8 says seat-level variance dominates.

**#10 — the induction rate, measured for the ninth consecutive cycle, and the exposure is TINY.** iter-115
wrote +177 lines and induced 9 (22.0 %); iter-108 wrote +48 and induced 2 (5.6 %). **iters 117–118 wrote 5
changed lines in scope, all fragment repairs.** Band cut to **`[0, 1]`** accordingly. **`≥ 2` would be a
first-class finding** — it would say induction is not proportional to volume but closer to *per-edit*,
which would change how every future repair is sized.

**#11 — anchors per predicate.** History: 1.09 → 1.50 → 1.50 → **1.108**. iter-116's fall is attributed to
iter-115 having closed twins corpus-wide. Nothing has re-armed twins since, so ≤ 1.45 should hold; a
return to ≥ 1.45 would say twins re-arm without anyone writing them, which would be new.

**#12 — net-new at iter-116, where it measured 6 of 41 and FAILED its `[0, 4]` band.** Multi-pin blocks
concentrate defects. Band re-cut **upward** to `[2, 12]` on that evidence — the first band on this sheet
re-cut on measurement rather than held, and it is re-cut in the direction the evidence points, not the
direction that would make it easy to hold.

**#13 — NET-NEW, and it audits a judgement the milestone has been carrying unexamined.**
`FIX-M257x-iter113-adjudication-is-judgement` records that of iter-113's 16 "small class" verdicts,
**1 is `SMALL-CLASS-PROVEN`** (P20 — zero headroom, nothing judged) and **15 are
`SMALL-CLASS-ADJUDICATED`** — they rest on **254 candidate exclusions read by one agent**. All 16 were
repaired at iter-115. **If this reading re-books any of those 15 predicates, the exclusion that judged it
rare was wrong**, and the count is a direct measurement of that judgement's error rate. `≥ 5` would say
the judged half of that fence is not trustworthy and every "small class" verdict needs re-deriving.

## Binding conditions on the read itself

1. **The instrument is not touched.** Briefing byte-identical, sha `3858ec53…`, `git log --follow` showing
   one commit ever, **re-checked AFTER copying AND again after the addendum was appended** — both sides
   printed in `ground-truth.md`, `diff` empty both times. **The known defect at line 37 is delivered
   as-is** and routed, never edited. Superseded ground truth goes in the ADDENDUM, below the frozen text,
   never above it.
2. **Clause 5 is not re-cut, narrowed, argued, or read met any other way.** It is met only by a reading
   that returns **zero**. Four user rulings. **No branch of the primary rule above is a gate verdict.**
3. **No repair inside the measuring pass.** Anything found routes; nothing is fixed. **That explicitly
   includes the false-RED re-disclosed in `ground-truth.md`** — it stays
   `FIX-M257x-iter116-predicate-guard-takes-first-pin-in-block`, open, for the second reading running.
4. **Ground truth re-derived, not inherited** — every checkout's ref AND its `origin/main` AND its fetch
   time restated, guard family run **with its own fence tree printed**, the partition **reproduced at both
   refs as a control**, all before the seats are dealt.
5. **No clone is fetched while this reading is in flight** (§5 rule 41a). Fetch times are re-read at the
   close and published, so a mid-reading move is detectable rather than suspected.
6. **The upheld rate is reported TWICE** — raw, and with the `wrong-tree` class separated.
7. **Adjudicate before reporting `P` or `N`.** Always.
8. **Every seat is committed verbatim the moment it lands**, before adjudication.
9. **`P` and `N` are reported whatever they are.** A rise, a fall and a flat are each a real result. No
   rounding toward the answer the gate wants. **If the `P ≥ 19` branch fires it is reported first and
   loudest, `TOK-08` is recorded as REFUTED, NO successor strategy is authored, and the iter exits to the
   user for a scope decision.** The strategy is what falls, not the gate.
10. **No point estimate of the pool is quoted** — not in this file, not in the close, not in `state.md`.
    Chapman is retired for this milestone. **Only floors survive: ≥ 24 at `8f04d3a`, ≥ 33 at `e6aed2e`,
    ≥ 36 at `ac48e5b`, ≥ 37 at `f581de09`.**
11. **Grade by CONSEQUENCE, not by class.** iter-115 found a `bash -c` claim that had inverted a shipped
    security property. If this reading surfaces a defect of that severity it is reported plainly and
    first, regardless of which bucket it falls in or how small its class is.
