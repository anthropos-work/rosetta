# Seat B — reading #31

Ground truth re-verified at this reading's open (all 15 git trees under `stack-demo` + the rext
authoring copy), and they match the ADDENDUM exactly:

| repo | HEAD | origin/main |
|---|---|---|
| app | `ad9f3c49` | `ad9f3c49` |
| app/studio (nested, own repo) | `aeec036a` | `aeec036a` |
| cms | `ca50c817` | `f38c0c4a` |
| cms/studio (nested, own repo) | `aeec036a` | `aeec036a` |
| platform | `0c91421d` | `0c91421d` |
| next-web-app | `8297c684` | `f97ba659` |
| sentinel | `f2c46190` | `f2c46190` |
| storage | `4ce8ece5` | `9f8cb532` |
| messenger | `fa47850d` | `e9421c68` |
| jobsimulation | `462343b0` | `82cb66ec` |
| roadrunner | `87d8d443` | `87d8d443` |
| graphql-wundergraph | `60c229f3` | `60c229f3` |
| studio-desk | `41ee3575` | `41ee3575` |
| ant-academy | `22df69dd` | `22df69dd` |
| rosetta-extensions (per-stack, pinned) | `09d06070` | — |
| rosetta-extensions (authoring copy) | `43049308` | — |

Every rext claim below is settled against the **pinned per-stack clone `09d06070`** (§5 rule 45,
ADDENDUM), and I say so per finding. Nested `studio` repos were grepped at their **own** ref
`aeec036a`, never at the host's.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/services/ai-readiness.md` | 712 | yes |
| `corpus/services/cms.md` | 336 | yes |
| `corpus/services/chronos.md` | 245 | yes |
| `corpus/services/academy-backend.md` | 160 | yes |
| `corpus/services/README.md` | 90 | yes |

(`wc -l` counts newlines; three of these files have no trailing newline, so the last numbered line the
reader sees is `wc -l + 1`. Numbers above are the `wc -l` values as instructed.)

I also read `corpus/ops/platform-alignment.md` §5 in full (rules 1–46) before starting, per the briefing.

---

## Blockers

### B1 — `ai-readiness.md`'s "the third generation rotted too" paragraph shipped with all three of its own line pins already stale — in the same commit that moved them

- **Anchor:** `corpus/services/ai-readiness.md:55-58` —
  > "iter-102 replaced `:459` with **`:496`**; re-derived at iter-115, `:496` is the **closing line of the
  > `> **✅ CORRECTED M219 …**` blockquote** (`:476-496`) — the very blockquote the two sentences above name
  > as the *wrong* target for the previous generation. **The block opens at `:498` and the quoted
  > parenthetical is at `:500`.**"

- **Why it is false:** all three present-tense anchors name the wrong construct at corpus HEAD. Measured
  (`awk` over `corpus/services/ai-readiness.md` at HEAD `04cbcfc`):

  | claimed | actually at HEAD | what the claimed line really is |
  |---|---|---|
  | `✅ CORRECTED M219` blockquote = `:476-496` | **`:512-536`** | `:476` = *"row in the **progress** table, never a `user_skill_evidences` row…"*; `:496` = *"frozen `ai_readiness_snapshots`), after iters 03→06 falsified the active-signals path…"* |
  | "the block opens at `:498`" (the `⚠⚠ M51 iter-08/09` block) | **`:538`** | `:498` = *"per-object-RPC class). ⚠️ **That premise was refuted at M219: the recompute takes 2.09 s.**"* |
  | "the quoted parenthetical is at `:500`" | **`:540`** | `:500` = the `[ops/demo/stories-spec.md:599]` link line |

  This is not ordinary drift from a later edit — it was **born wrong**. `git blame -L 54,58` puts the whole
  paragraph in `9d31cf1` ("fix(M257x/115): … the third-generation pin is RETIRED, not re-derived",
  2026-08-07), and that **same commit** grew the file 672 → 712 lines (`47 insertions, 7 deletions`), all of
  the net growth above line 476. Verified directly: at `9d31cf1^`, `:496` **is** the closing line of the
  CORRECTED-M219 blockquote, `:498` **is** the `⚠⚠ M51 iter-08/09` opening and `:500` **is** the
  `(now aireadiness/readiness.go, formerly workforce/ai_readiness.go:512)` parenthetical — i.e. the author
  measured against the pre-edit file and shipped the post-edit file. Exactly `platform-alignment.md` §5
  rule 34 (*"Line numbers move when YOU edit too … re-derive the line map from `git diff -U0` and re-point
  in the same commit"*), violated inside the paragraph whose whole subject is a pin that keeps rotting —
  and the paragraph advertises itself as the fourth generation's fix.

  Consequence for a reader doing real work: the passage's own remedy is *"Search the construct"*, but a
  reader who trusts the three numbers as the record of what was measured is handed a **fourth** wrong
  generation, and the sentence *"neither line has ever carried the statement"* (`:50`) now applies to this
  sentence too.

- **Confidence:** high. (I hesitated only because the paragraph is a meta-narrative about the document
  rather than a platform claim — but the verbs are present tense (*"`:496` **is**"*, *"the block **opens
  at** `:498`"*), it is offered as a re-derivation, and it points at the wrong construct three times.)

---

### B2 — `cms.md` cites `:70-71` for "the **Studio** bullet"; `:70-71` is the **Events** bullet

- **Anchor:** `corpus/services/cms.md:240` —
  > "…the studio-room pipeline is pulled into the **`app`** image by CI via the `additional_repo` mechanism
  > (app v1.360.1) — **see the *Studio* bullet, `:70-71` in the banner at the top of this doc.**"

- **Why it is false:** measured at corpus HEAD, `cms.md:70-71` is the **Events** bullet:
  > `70| > * **Events** — `app` owns the `CMS_STREAM` subscriber. The folded similarity re-index + Studio handlers are`
  > `71| >   merged onto app's **existing** CMS subscriber via `.AddHandler(...)`; they act on disjoint rows, so they`

  The **Studio** bullet is at **`:75-76`**:
  > `75| > * **Studio** — the Python `anthropos-studio-room` project is now pulled into the **`app`** image via the CI`
  > `76| >   `additional_repo` mechanism (app v1.360.1), the same way `cms` used to do it.`

  The anchor *resolves* (line 70 exists) and even contains the word "Studio", which is why an
  anchor-existence check passes it — the §5 rule 46 / rule 21 "resolves but names the wrong construct"
  class. It was correct when authored: at `a9f8ed45` (2026-08-06) `:70-71` **was** the Studio bullet; the
  next commit to this file, `b4bdbfc` ("fix(M257x/115): repair P10 … and P20"), inserted 5 lines above it
  and did not re-point the self-citation.

- **Confidence:** high.

---

### B3 — `cms.md` cites `:44-47` for "the **Data** bullet"; `:44-47` is the section header plus the **Domain** bullet

- **Anchor:** `corpus/services/cms.md:215-216` —
  > "PostgreSQL (Ent ORM, **`public` schema** — the cms tables were re-created there at cms-in-app v8.0; the
  > legacy `cms` schema is non-authoritative. **Consistent with the *Data* bullet, `:44-47` above**)"

- **Why it is false:** measured at corpus HEAD, `:44-47` is:
  > `44| > Where everything went:`
  > `45| >`
  > `46| > * **Domain** — `app/internal/cms/` (directus, similarity, studio, library, importer/exporter, aivideo,`
  > `47| >   contentread, jobsimimport, rpcsrv, worker, …), wired from `app/internal/cms/wiring.go`.`

  The **Data** bullet is at **`:48-51`**. Same mechanism as B2 and the same repair commit: at `a9f8ed45`
  the range was exact; `b4bdbfc` inserted 4 lines above and left the citation behind. Booked separately
  from B2 because they are two distinct anchors with two distinct offsets (+4 vs +5), so a single
  re-point does not fix both — which is precisely how §5 rule 34 says this class survives.

- **Confidence:** high.

---

## Minors

- `ai-readiness.md:231` — *"`source_session_id` is what `resolveSessionAuthors` joins (`sessions → memberships`)"*. The
  measured join is `FROM public.job_simulation_sessions s JOIN public.memberships m`
  (`app/internal/aireadiness/how_we_measure.go:1122-1128` @ `ad9f3c49`). `public.sessions` does not exist
  (the corpus records the rename elsewhere), so the backticked shorthand names a table that isn't there.
  Non-material because the sentence is about the *shape* of the join, not the SQL.
- `ai-readiness.md:316-317` — *"`aireadiness.Manager` … is **constructed with** `db`, `ent`, `orgRepo`, `ai.AI`, a pub
  `EventPublisher`, the `academy.EmbeddingsManager`, and the `WorkforceDirectory`"*. Measured
  (`internal/aireadiness/manager.go:135` @ `ad9f3c49`): `NewManager(logger, db, aiClient, entClient, pub,
  academyEmbeddings, directory)` — `orgRepo` (and `assignmentRepo`) are **derived inside** the constructor,
  and `logger` is a parameter the list omits. Describes the struct rather than the signature.
- `ai-readiness.md:639` — *"`computeCycleTotals` (`how_we_measure.go:253-261`)"*. `:253-254` are the closing two
  lines of the *preceding* function; the doc comment starts at `:256` and the signature is `:260`. The
  range contains the construct, so the claim is checkable, but the two leading lines belong elsewhere.
- `ai-readiness.md:566` — *"`deriveMode` (`useAIReadiness.ts:48-62`)"*. Two files in `next-web-app` carry that
  basename (`apps/web/src/hooks/useAIReadiness.ts` and
  `apps/web/src/components/ai-readiness/useAIReadiness.ts`); the anchor is exact only in the second
  (`:48-62` = `function deriveMode(...)`, verified @ `8297c684`). Ambiguous-by-basename, resolvable.
- `ai-readiness.md:187` — *"a member carries a `stage` ∈ {1,2,3} (0 = none/done)"*. `classifyV2`
  (`internal/aireadiness/scoring.go:119-138`) never emits 0 — it starts at 1 and climbs. Stage 0 is a
  **seeder-side** convention (`aiReadinessStageFor` → manager / `not_started` ⇒ 0, and the funnel seeder then
  *skips* her, so no row is written at all). The parenthetical reads as a platform fact.
- `chronos.md:22`, `:171`, `:190` — *"Codebase: `stack-dev/chronos`"* / `cd stack-dev/chronos`. `chronos` is in no
  clone set on this box (`find . -maxdepth 3 -iname '*chronos*'` returns only the doc itself) and is not in
  `repos.yml` at `0c91421d`, so the path does not exist after a normal `make init`. The banner at `:9`
  discloses this, so it is signposted rather than misleading.

---

## Positively cleared (audited zeros)

Everything below is **ENUMERATED** unless it says RE-DERIVED. Where a claim names a ref, I graded it at
that ref and say so.

### `corpus/services/README.md`

- **ENUMERATED** — *"Every service doc in this directory, enumerated."* `ls corpus/services/*.md` = 29 files;
  minus `README.md` + `TEMPLATE.md` = **27** service docs. The four tables list 10 + 5 + 8 + 4 = **27**, and
  the set matches exactly (no doc missing, none listed twice, no phantom row).
- **ENUMERATED** — *"`docker-compose.yml` declares 5 services (7 effective, with `common.yml`'s `postgresql` +
  `redis`), and `repos.yml` carries 4 entries — app, sentinel, next-web-app, studio-desk."* Derived from
  `git -C stack-demo/platform show 0c91421d:docker-compose.yml`: `sentinel`, `backend`, `studio-desk`,
  `next-web-app`, `gotenberg` = 5; `common.yml` adds `postgresql`, `redis` = 7. `repos.yml` = exactly those
  4 names.
- **ENUMERATED** — the seven folded domains *"all present @ `app` `ad9f3c49`"*: `git ls-tree -d` confirms
  `internal/{skiller,skillpath,jobsimulation,cms,storage,messenger,customeriosync}` all exist.
- **ENUMERATED** — *"`app/internal/roadrunner/` exists at no ref — at `ad9f3c49` **no path in the whole tree
  matches `roadrunner`**"*: `git ls-tree -r --name-only ad9f3c49 | grep -i roadrunner` → 0 paths (positive
  control on the same pipeline: `grep -ci jobsimulation` → 170). Judge0 wiring confirmed at
  `internal/jobsimwiring/wiring.go:123` = `runnerManager := jsrunner.NewRunnerManager(getenv("JUDGE0_API_KEY"),
  getenv("JUDGE0_BASE_URL"))`, and `internal/jobsimulation/runner/{languages,runner}.go` exist.
- **ENUMERATED** — roadrunner *"still reads `service_desired_count = 1` (`roadrunner/terraform/main.tf:19`,
  unchanged since `84a4b4f` / 2025-12-15)"*: line 19 is exactly that, and
  `git blame -L 19,19 87d8d443` → `84a4b4f1 (Mattia Sasso 2025-12-15)`.
- **ENUMERATED** — cms `= 0` at `cms/terraform/main.tf:39` (true at **both** `ca50c817` and `f38c0c4a`).
- **ENUMERATED, graded at the ref the cell names** — jobsimulation *"`6092c6d2`; `service_desired_count` no
  longer appears in `jobsimulation/terraform/main.tf` at all — `:15-22`"*. `6092c6d2` is **not** an ancestor of
  the checkout `462343b0` (where the module block is intact at `= 0`) but **is** an ancestor of `origin/main`
  `82cb66ec`, where `git grep service_desired_count -- terraform/` returns **nothing** and `:15-22` is the
  M810-decommission comment block. Pinned ⇒ graded there ⇒ true.
- **ENUMERATED** — messenger's *"`MESSENGER_RPC_ADDR` appears in no repo — every clone at its own named ref,
  nested repos included"*. Ran the §5-rule-44 loop over **all 15** git trees (13 top-level + the two nested
  `studio` checkouts) at each tree's own HEAD: `MESSENGER_RPC_ADDR` = **0** everywhere. (The looser token
  `MESSENGER_RPC` returns 2 — both inside `app`'s own `knowledge/plan/roadmap-support-in-app.md`, both
  asserting the token's *absence*. The exact-token claim stands.) Positive control on the identical
  pipeline: `SKILLER_RPC` returns 26/12/9/4/20/1 across six trees.
  `git -C stack-demo/platform log -S 'MESSENGER_RPC' --oneline 0c91421d` = **0** commits; the stated positive
  control `-S 'SKILLER_RPC'` = **7**. Both reproduce exactly.
- **ENUMERATED** — messenger *"`messenger/internal/flow/flow.go:72-104` @ `fa47850`, `AddSubscriber("backend", …)`,
  21 live handlers"*. `:72` is the `AddSubscriber("backend", …)` call and `:104` its closing `))`. Counted the
  `pubsub.EventHandler(...)` members inside that block excluding the one commented-out line (`:83`,
  `OrgJobSimulationAssignmentPastDueHandler`): 5 + 4 + 2 + 2 + 5 + 3 = **21**. §5 rule 24 applies here and the
  count survives it: `grep -c` over the block would return **22**.
- **ENUMERATED** — graphql-wundergraph *"ONE subgraph (`backend`) since `915da06`"*: at `60c229f3` the only SDL
  in `schemas/` is `backend.graphqls`, and `915da06` deletes `schemas/cms.graphqls` (−762) **and**
  `schemas/jobsimulation.graphqls` (−860) in one commit.
- **ENUMERATED** — *"Next.js **16**"* for both next-web-app (`apps/web` + `apps/hiring` `"next": "~16.2.12"`) and
  ant-academy (`code/package.json` `"next": "^16.2.6"`).
- **ENUMERATED** — *"platform `d11a403` (2026-08-03) deleted all three from `docker-compose.yml` **and** from
  `repos.yml`"*: the `repos.yml` diff of `d11a403` removes `- name: cms`, `- name: jobsimulation`,
  `- name: roadrunner`. (NB the commit *message* says roadrunner's entry "was already gone"; the diff says
  otherwise — the corpus followed the diff, which is right.)
- **ENUMERATED** — `platform-migration-status.md:101` is indeed the `customerio-sync` row.
- **ENUMERATED** — `838d907` = *"chore(compose): drop the storage, messenger and customerio-sync containers"*,
  merged at `0c91421`; at `0c91421d` `docker-compose.yml` contains **zero** `*_RPC_ADDR` and exactly one
  service address, `AUTHORIZATION_ADDRESS=http://sentinel:8087` (`:48`).

### `corpus/services/cms.md`

- **ENUMERATED** — the "Where everything went" **Data** bullet: all six tables
  (`similarities`, `similarity_categories`, `similarity_features`, `similarity_skills`, `studio_documents`,
  `studio_tasks`) are `CREATE TABLE`d in `app/terraform/migrations/20260724132049_cms_data_model.sql`, one
  each, no more, no fewer.
- **ENUMERATED** — the **Domain** bullet's package list: every named subpackage
  (directus, similarity, studio, library, importer, exporter, aivideo, contentread, jobsimimport, rpcsrv,
  worker) exists under `app/internal/cms/` @ `ad9f3c49`, and `internal/cms/wiring.go` exists.
- **ENUMERATED, graded at the named ref** — *"`d11a403` moved exactly two variables on that block,
  `CMS_RPC_ADDR` and `JOBSIMULATION_RPC_ADDR`; the other two already read `http://backend:8083` at
  `d11a403^`"*. The messenger block at `d11a403^` reads `BACKEND_USERS_RPC_ADDR=http://backend:8083` and
  `SKILLER_RPC_ADDR=http://backend:8083`; the diff changes only `CMS_RPC_ADDR` (`cms:8091` → `backend:8083`)
  and `JOBSIMULATION_RPC_ADDR` (`jobsimulation:8401` → `backend:8083`). The retraction of "all four" is
  correct, in both directions.
- **ENUMERATED** — *"no `.tf` file in any clone names `http://backend.internal.anthropos:8081` (0 hits over all
  **44** tracked `.tf` files in the 13 `stack-demo` repos …; 0 again over the **59** `.tf` files a raw
  filesystem sweep finds … the literal does occur … **6 times, none of them terraform**)"*. Re-derived the SET,
  not the sum (§5 rule 24): per-repo tracked `.tf` counts 0+5+4+4+6+4+2+0+4+2+4+5+4 = **44**;
  `find stack-demo -name '*.tf'` = **59**; `git grep -ci` restricted to `-- '*.tf'` across all 15 trees at
  their own HEADs = **0**; unrestricted = 1 (`app`) + 5 (`rosetta-extensions`, pinned clone `09d06070`) = **6**.
  All four figures reproduce exactly.
- **ENUMERATED, graded at the named ref** — `app/knowledge/service-dependencies.md:52` @ `ad9f3c49` is the
  past-tense sentence quoted, under the heading *"There are no external callers of app's RPC mux left."*
- **ENUMERATED, graded at the named ref** — `app/main.go:1205-1211` @ `b948604` is exactly the
  *"Additive + DORMANT … until the M809 re-point"* comment block.
- **ENUMERATED** — the subgraph ladder: `749dc86` (2026-06-24) deletes `schemas/skiller.graphqls` (5→4),
  `7c17e63` (2026-07-21) deletes `schemas/skillpath.graphqls` (4→3), `915da06` deletes cms **and**
  jobsimulation (3→1). The "not 2 to 1" correction is right.
- **ENUMERATED** — `REDIS_CMS_CACHE_INDEX` default **5** (`app/main.go:1107-1108`, `terraform/variables.tf:706`).
- **ENUMERATED** — `cms/terraform/main.tf` is **191 lines** at both `ca50c817` and `f38c0c4a`, with `:39` = `0`.
- **ENUMERATED, graded at the named ref** — `6efa1d5` (2026-08-04, merged `f38c0c4`) deletes
  `.github/workflows/build-production.yml` (18 lines) under the exact subject and body the doc quotes,
  including *"would try to push an image into a registry that no longer exists."*
- **ENUMERATED** — the `bash -c` **retraction** (`:292-310`) is correct on both sides:
  `studioManager.go:1096-1098` is the *"argv (exec) form — NEVER through a shell"* comment, `:1101` is
  `pycmd := exec.CommandContext(ctx, name, args...)`, `:100-103` is the caller's *"No `bash -c`"* note, `:119`
  is the `studio/gen.py` dispatch, `:122-124`/`:126`/`:129` are the fixed-argv venv bootstrap — all @
  `ad9f3c49` — and `git grep -c '"bash"' ad9f3c49 -- '*.go'` summed across the tree = **0**. The frozen-repo
  half is also exact: `ca50c817:internal/studio/studioManager.go:967` = `pycmd := exec.Command("bash", "-c", command)`.
- **ENUMERATED** — Go execs exactly **two** studio scripts: the only `runCommand(ctx, pyBin, …)` call sites are
  `:119` (`studio/gen.py`) and `:1045` (`studio/postgen.py`), at **both** `b948604f` (the ref the sentence
  names) and `ad9f3c49`. `pdf2md.py` is not among them.
- **ENUMERATED, nested repo at its own ref** — the Mistral picture. `git -C stack-demo/app/studio grep -ci
  mistral aeec036a` = **22 lines across 3 files** (`requirements.txt`, `tools/pdf2md.py`, `tools/r3.py`) —
  reproduced exactly, and the host-ref trap is real: `git -C stack-demo/app grep -ci mistral ad9f3c49 --
  studio/` = **0**, because `app/.gitignore:79` is `studio/*`. `services/ai.py:705-708` is the three-entry
  provider dict; `services/ai.py:1-2` imports only `openai`/`anthropic`; `tools/pdf2md.py:24` =
  `from mistralai import Mistral`, `:96` = `client = Mistral(api_key=api_key)`, `:127` =
  `model="mistral-ocr-latest"`; `tools/r3.py:139` lists `pdf2md.py`, `:190` resolves it, `:199-206` is the
  step-2 exec — so the withdrawal of *"nothing dispatches it"* is itself correct.
- **ENUMERATED** — Go-side Mistral is OCR-only: `markdownManager.go:10` imports
  `internal/cms/studio/mistralocr`, `:30` is `return &MarkdownManager{ocr: mistralocr.New(aiKey)}, nil` inside
  `NewMarkdownManager`; `studioManager.go:531` = `// supported ocr content types for mistral ocr`, `:583` =
  `NewMarkdownManager(os.Getenv("MISTRAL_API_KEY"))`, `xlsx.go:13` = the "Mistral OCR doesn't accept xlsx"
  comment. All @ `ad9f3c49` (the block's own pin `2035f9a` is an ancestor and the doc's "both offsets still
  name the same two constructs at today's origin/main" reproduces).
- **ENUMERATED** — `requirements.txt` is **9 packages** verbatim in the stated order, `mistralai` at `:8`, and
  `python-docx` appears **nowhere** in it. The two copies (`app/studio` and `cms/studio`, both @ `aeec036a`)
  are byte-identical (`diff` empty), so *"the two `requirements.txt` are byte-identical"* holds.
- **ENUMERATED** — the legacy-repo facts: `cms/go.mod:3` = `go 1.26.4`; `cms/cmd/root.go:77` =
  `cmp.Or(os.Getenv("PORT"), "8080")`, `:78` = `cmp.Or(os.Getenv("RPC_PORT"), "8081")`; `cms/Dockerfile:2` =
  `FROM golang:1.26-bookworm AS build`, `:23` = `FROM python:3.11-slim`; the whole "Key directories" tree
  resolves (all 12 `internal/*` dirs, `ent/schema/studioDocument.go`, `ent/schema/studioTask.go`,
  `internal/graph/schemas/{simulation,skills,studio}.graphqls`, `cmd/`, `terraform/`).
- **ENUMERATED, graded at the named ref** — the 8090/8091 history: at `2adcf71` the `cms` compose block
  publishes `"8090:8090"` (`:154`) / `"8091:8091"` (`:155`) and sets `PORT=8090` (`:169`) / `RPC_PORT=8091`
  (`:173`). At `0c91421d` there is no `cms` service at all.
- **ENUMERATED** — `gen.py:484-492` registers **exactly nine** `add_argument` calls and the nine flag names
  match the doc one-for-one; `gen.py:18-28` is `parse_argument`, which calls `parse_known_args()` and merges
  the leftovers into the dict — so `--template foo` really does parse cleanly and set an unread key.
  `studio/CLAUDE.md:12-14` is the quoted command block, verbatim.
- **ENUMERATED** — the content/runtime split anchors: `app/internal/skillpath/session.go:205-207` is the
  *"cms-in-app deseam: cms is in-process"* comment + `u.cms.GetSkillPathDomain(...)`.
- **ENUMERATED** — the Directus webhook: `POST /api/webhook/directus` (`internal/web/backend/backend.go:324`)
  gated on `DIRECTUS_WEBHOOK_SECRET` (`main.go:1199`, *"fail-closed"* at `:1198`); the standalone receiver was
  `httpServer.Handler.Handle("/webhooks/", …)` (`cms/cmd/root.go:242`, unauthenticated).
- **ENUMERATED** — `app/cms_reader_switch.go` exists; `DIRECTUS_DATA_CONSUMERS = ("cms", "backend")` in the
  **pinned** rext clone `09d06070` (`stack-core/gen_override.py:58`).
- **ENUMERATED** — `CMS_STREAM=cms` is set on the `backend` block at `0c91421d` (`docker-compose.yml:51`), and
  backend publishes `8081/8082/8083` (`:41-43`), so *"the domain's live surface is `backend`'s (`:8082/graphql/query`,
  RPC on `:8083`)"* holds.

### `corpus/services/academy-backend.md`

This file audited clean. Everything I checked is ENUMERATED:

- `git describe --tags ad9f3c49` → **`v1.369.0-7-gad9f3c498`**, exactly as the doc's re-derivation states.
- All named `internal/academy/` files exist (`academy.go`, `content.go`, `body.go`, `content_import.go`,
  `embeddings.go`, `asset.go`, `certificate.go`, plus the four named test files
  `sqlite_test.go`, `certificate_mint_test.go`, `entitlement_parity_test.go`, `merge_test.go`, `time_test.go`,
  `progress_aggregate_test.go`).
- **All 11 table names** are correct and plural-as-warned: `academy_chapter_progresses`,
  `academy_last_activities`, `academy_chapter_times`, `academy_certificates`, `academy_bookmarks`,
  `academy_feedbacks`, `academy_series`, `academy_skill_paths`, `academy_chapters`, `academy_chapter_bodies`,
  `academy_path_embeddings` — each with exactly one `CREATE TABLE` in `app/terraform/migrations/`.
  `academy_chapter_progresses` carries `CREATE UNIQUE INDEX … ("user_id", "chapter_slug")`;
  `academy_skill_paths` has `"paid" boolean NOT NULL DEFAULT true`, nullable `tenant_eid`, `lifecycle`;
  `academy_path_embeddings` is `extensions.vector(1536)` with a `source_hash` column.
- **Graded at the ref the sentence names (`9d00a313`)**: `app/main.go:471-472` are
  `storageManager := internalstorage.NewManager(storageBucket)` /
  `publicStorageManager := internalstorage.NewPublicManager(storagePublicBucket)`, sourced from
  `internalstorage.EnvBucket`/`EnvPublicBucket` = `"STORAGE_S3_BUCKET"`/`"STORAGE_S3_PUBLIC_BUCKET"`
  (`internal/storage/service.go:22,24`). `STORAGE_RPC_ADDR` at that ref = **3 occurrences, all comments,
  zero reads** (`jobsimwiring/wiring.go:101`, `storagens/callsites_test.go:189`, `main.go:451`) — I enumerated
  them rather than trusting the count.
- **Graded at `9d00a313`**: `cmd/academyImport/main.go:239-243` is the *"the standalone storage service is gone …
  it therefore needs the BUCKET, not an RPC address"* comment + the `os.Getenv(appstorage.EnvPublicBucket)`
  read, and `:244-246` is the hard error on an empty bucket. Both exact.
- Every GraphQL query and mutation named in the doc exists in
  `internal/web/backend/graphql/graph/schemas/academy.graphqls` (`academyProgress`, `academyLastActivity`,
  `academyChapterTime(s)`, `academyPathProgress`, `academyCertificates`, `academyCertificate @public`,
  `academyBookmarks`, `academyFeedback(s)`, `academyCatalogSeries`, `academyCatalogSkillPaths`,
  `academySkillPath`, `academyChapter`, `academyChapterBody`; `upsertChapterProgress`/`…Batch`/`…BatchTolerant`,
  `setLastActivity`, `upsertChapterTimeBatch`, `claimPathCertificate`/`…FromLocal`,
  `add/update/removeAcademyBookmark`, `upsertAcademyFeedback`) — and `resolver_academy.go` exists.
- REST: `e.GET("/content/catalog.json", …)` (`internal/web/backend/content.go:23`) and
  `e.Group("/content/admin", sharedTokenMiddleware(os.Getenv("ACADEMY_CONTENT_API_TOKEN")))`
  (`content_admin.go:35`). *"No Connect-RPC for academy"* — `git grep AcademyService|academyv1connect` over
  `*.go` @ `ad9f3c49` = **0**.
- The nightly task: `TypeAcademyEmbeddingRefresh = "academy_embedding_refresh"` and
  `srv.scheduler.Register("0 3 * * *", tasks.NewAcademyEmbeddingRefreshTask())`
  (`internal/worker/worker.go:170`) — **03:00** confirmed from the cron string, not from a comment.
- `cmd/academy-seed` flags are exactly `--user-email`, `--user-id`, `--fixture`, `--reset`, `--dry-run`,
  `--list`; env is `SUPABASE_DB_CONN`. `cmd/academyImport`, `cmd/coursebuilder-e2e`, `cmd/coursebuilder-liverun`
  all exist.
- Certificates: `certIDPrefix = "AAS"`, `certIDRegex = ^AAS-\d{4}-[A-HJKMNP-TV-Z2-9]{6}$` (a 30-symbol
  ambiguity-free alphabet), `crypto/rand`, and the `:171` comment says so.
- The FS-fallback retraction is right on **both** sides: `serverTenant.js` @ ant-academy `22df69dd` says
  *"there is NO FS-as-published fallback"* and *"the cutover is intentional, not reversible-on-error"* (`:132`,
  inside the cited `:115-145` window), `getServerCatalogView()` resolves a null backend result via
  `?? emptyCatalogView()`; and the patch that restores it,
  `demo-stack/patches/academy-fs-published-fallback/…yaml`, exists in the **pinned** rext clone `09d06070`.
  The cross-file citation `ant-academy.md:82-88` lands on the passage that *"states the opposite in bold"*.
- `0e37771f` = *"Academy backend v1.0 'ground truth' … (#903)"*, 2026-06-05 — PR number, sha and date all match.
- The WunderGraph framing: `NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT=http://…:8082/graphql/query` in platform
  `.env_example:98` + `docker-compose.yml:151,160` @ `0c91421d`, with `.env_example:97` saying the name is
  historical; and *"in production the router is still declared"* is true —
  `graphql-wundergraph/terraform/main.tf:20` @ `60c229f3` is `service_desired_count = 1`.

### `corpus/services/ai-readiness.md` (everything except B1)

The density of exact anchors here is unusually high and they hold. All ENUMERATED:

- `internal/data/ent/enum/organization_settings.go:47` = `OrganizationSettingAIReadiness OrganizationSetting = "ai_readiness"`;
  `isAIReadinessEnabled` is in `internal/aireadiness/steps.go` (`:541`).
- The whole package-rename table resolves: every "New" filename exists under `internal/aireadiness/`
  (`readiness.go`, `scoring.go`, `csv.go`, `steps.go`, `narrative.go`, `how_we_measure.go`, `cycles.go`,
  `compare.go`, `diagnosis.go`, `provision.go`, `defaults.go`, `emailoverride/`, `emailpreview/`,
  `notifications/`, `testdb_test.go`), and `internal/workforce/ai_readiness.go` is gone.
  `4c28365f` is the named commit, dated 2026-07-22, and the first tag containing it is **v1.351.1** —
  so the version claim is right too.
- `WorkforceDirectory` lives in `aireadiness/manager.go:40` and exposes both `LoadMembers` **and**
  `LoadMembersByUserIDs`; the implementations did stay in `internal/workforce/members.go` (`:349`, `:353`);
  `buildResponseFromSnapshots` (`readiness.go:817`) calls `m.workforce.LoadMembers(ctx, orgID, "")` at `:825`.
  The demopatch manifest reads `path: internal/aireadiness/readiness.go` at **`:42`** and its header says
  *"v2.7 M254 RE-POINT"* at **`:33`**, with `demo-stack/tests/test_aireadiness_snapshot_loadmembers_m254.py`
  present — all in the **pinned** rext clone `09d06070`.
- `useAiReadinessActive.ts` @ `next-web-app` `8297c684`: `:19` opens the hook, `:22` `rawFlag`, `:25`
  `flagEnabled`, `:29` `useAiReadinessEnabled(flagEnabled)`, `:32` `active: flagEnabled && orgEnabled === true`.
  `aiReadiness.constants.ts:26` = `export const AI_READINESS_FLAG = 'flag_ai_readiness';`.
- **All four** re-pointed `AIReadinessClient.tsx` anchors are now exact @ `8297c684`: `:78`
  `const SHOW_SECONDARY_TABS: boolean = false;`, `:135` `const { orgEnabled } = useAiReadinessEnabled(true);`,
  `:155-156` the `effectiveCycleId` const, `:171` `enabled: featureOn && cyclesQ.isFetched,`, `:601`
  `(SHOW_SECONDARY_TABS && tab.key === 'compare')`. The doc's own *"until iter-115"* forensics also check out:
  `:153-154` is the comment above the const, `:168-170` is the `useAIReadiness({` call plus its first two
  options, `:599` is `tab.key === 'how' ||`. And *"no PostHog reference at all"* — `grep -ci
  'posthog|flag_ai_readiness'` over that 658-line file = **0**.
- The `urls.ts` correction is right in both directions: `:51` **is** `AI_READINESS_URL = '/ai-readiness'` at
  `8297c684`, `:52` is `TALK_TO_DATA_URL`; at `bb3313bc` the constant sat at `:50` and `:52` was
  `ORGANIZATION_FEEDBACK_URL` — exactly as claimed. Repo-wide, the constant appears in exactly two source
  files plus `knowledge/ai-readiness/frontend-architecture.md:15`.
  `useNavbarSections.tsx:4` (import), `:401-408` (`aiReadinessMenuItem`), `:403` (`key: AI_READINESS_URL`),
  `:569` (`showAIReadiness ? aiReadinessMenuItem : null`) all exact.
- `WorkforceNewClient.tsx:125-151` is the tab `items={[…]}` array with **no** readiness tab;
  `useWorkforceAIReadiness.ts:23-27` is the `callWorkforceAPI('/ai-readiness', token, { searchParams: { tag } })`
  call — **no `cycle` param, no `/cycles`**, as claimed.
- `dae0fb2f7` (2026-07-13) deletes `AIReadinessContainer.tsx` (103) + `AIReadinessIntro.tsx` (220) +
  `AIReadinessView.tsx` (330) = **653** lines from those three, and no
  `/enterprise/workforce/ai-readiness` route path survives at `8297c684`.
- Scoring: `step1MaxScore=30 / step2MaxScore=40 / step3MaxScore=30` (`readiness.go:29-31`);
  `computeKnowledgeAxis` = `(step1+step2)/70×100`; `computeUsageAxis` = `step3×100/30`;
  `classifyArchetype` = Champion (both ≥ `archetypeHighBand=75`) / Standby (both ≤ `archetypeLowCeil=50`) /
  `usage >= knowledge` → Explorer / else Hidden Talent — **tie → Explorer** confirmed by the `>=`;
  bands `None 0-24 / Low 25-50 / Medium 51-74 / High 75-100` are in `bucketKnowledge`'s own doc block.
  Tier2/Tier3 = `round(raw × maxScore / 100)` clamped.
- `defaults.go` — I **counted the entries**, not the comment: `defaultReadinessSkills` = **19** at weight 1.0
  (`:31-49`) + **12** at 0.5 (`:51-62`) = **31**, denominator 19 + 6 = **25.0** exactly as the doc's arithmetic
  needs. `defaultReadinessSims` = **3**: `{StepSimulation,"tech"}`, `{StepSimulation,"business"}`,
  `{StepInterview,"both"}`. RE-DERIVED on top of that: `round(1.0/25×30)=1`, `round(0.5/25×30)=1`,
  `round(9.0/25×30)=11`, `round(3.0/6.5×30)=14`, `156/199=78.4%`, and `held/25×100 = held×4` is integral for
  half-step weights — all five reproduce.
- `computeTier1` (`readiness.go:139`) really does divide by the **whole** configured repertoire
  (`:158-167`), and its own comment states *"a larger configured set makes a full score harder to reach"* —
  the doc quotes it correctly. `queryUserAISkills` (`:82-91`) selects only `user_id, skill_id, is_verified`
  from `public.user_skill_evidences`, so the "presence-based" claim holds.
- The filter correction is right and the wrong-one-of-two trap is avoided: `readiness.go:388` calls
  `keepInCycleStep1` (active-cycle branch, `func` at `:710`), `:390` calls `keepStartedMembers` (`func` at
  `:730`), `queryInCycleStep1Completers` is at `:684`, `computeOrgBreakdowns` at `:330`,
  `GetAIReadinessWithOptions` at `:289` with route 1 at `:290-297`, route 2 at `:307-312` and the fallthrough
  at `:314`. `steps.go:962` is *"This DB signal is the only real 'has started' check"*, `:964` is
  `queryReadinessStarters`, `:969-974` is the `SELECT DISTINCT user_id FROM
  public.ai_readiness_user_step_progresses … AND status <> 'not_started'` SQL, and `:922` is
  `queryReadinessSimCompleters` — so the doc's account of what *it* used to cite is also accurate.
- Data model: **all 15** named tables exist as `CREATE TABLE`s (`ai_readiness_*` ×13,
  `organization_settings`, `interview_aggregated_reports`). `interview_aggregated_reports` carries exactly
  `organization_id`, `sim_id`, `report jsonb`, `session_count`. `ai_readiness_sims` has
  `CREATE UNIQUE INDEX … ("organization_id","step_type","track")`. `ai_readiness_cycles` has the **partial**
  unique index `… ("organization_id") WHERE status = 'active'`, which is what makes "one closed + one active"
  legal.
- The Step-3 report contract is exact against `how_we_measure.go`: `computeInterviewInsightsV2` (`:832`)
  reads **one** table (`queryInterviewAggregatedReports`, `:1077` `FROM public.interview_aggregated_reports`);
  `usageDimSpecs` (`:1160-1181`) is **five** entries — `avg_adoption`, `avg_transformation`, `avg_originality`,
  `avg_depth`, `avg_ownership`; `usageDimensionsFromReports` `continue`s on a missing/non-numeric KPI (so
  omitting all of them yields no tile row at all); `holdsBackFromInsights` (`:1008`) filters on
  `strings.Contains(strings.ToLower(in.Category), "risk")` — the category string really is load-bearing;
  `aggPattern.Evidence`/`SourceSessionID` back the verbatim quote + author hydration; and the chart
  fallbacks are `top_unexpected` → Strengths (`:930`) and `top_concerns` → HoldsBack (`:938`), with
  **Unexpected angles deliberately having none** (`:924-932` says exactly that). The doc's five-row table is
  a faithful map of the code.
- The seeder side, in the **pinned** rext clone `09d06070`: `stack-seeding/seeders/ai_readiness_interview_report.go:185-189`
  emits exactly the five current ids, and `:149` reads *"All five are always emitted here."*
  `seeders/cockpit.go:135` `LegacyReadinessPaths()` / `:161` `ValidateCockpitManifest`,
  `seeders/ai_readiness_evidence.go`, `seeders/contentref.go`, `seeders/seat_append_test.go`,
  `seeders/ai_readiness_funnel_test.go:230 TestAIReadinessFunnelSeeder_DayZeroHeroGetsNoSignals`,
  `seeders/ai_readiness_sim_skills.go` (`AIReadinessSimSkillsSeeder`, registered at
  `cmd/stackseed/main.go:520`) and `stack-verify/e2e/lib/coverage-manifest.ts` all exist.
  `aiReadinessStageFor` (`ai_readiness_funnel.go:205`) maps exactly as documented — explicit
  `not_started` → 0 **checked first**, manager → 0, `TrajectoryStruggling` → 1, `default:` → 3 — and
  `Persona.AIReadiness` (`blueprint/blueprint.go:200`) is a closed enum run through `validateEnum`
  (`:618`), so the "a typo fails the seed loudly" clause holds.
- `interactions.action_type` really is exactly `{email, call}` — not from a DB CHECK (the column is
  `character varying`) but from the ent validator, `internal/data/ent/interaction/interaction.go:92-98`
  `case "email", "call":`. And `interaction_source_target_must_be_different_check CHECK (source_id <> target_id)`
  is in the migration, so the doc's "the DB enforces `source_id <> target_id`" is right.
- `HowWeMeasureTab.tsx` @ `8297c684` is **1,989** lines; `:1879` opens the *"Handled for you this cycle"*
  block, `:1903` is the label, `:1915`/`:1921`/`:1927` are `skillsMapped` / `handsOnMinutes` /
  `interviewMinutes`, and `grep -c interviewQuestions` over that file = **0**. `useAIReadiness.ts:326` is
  `interviewQuestions: number;` inside `AIReadinessCycleTotals` (`:323-330`). Every figure in that
  parenthetical reproduces.
- `AIReadinessHero.tsx:88` = `if (!air.deadline) return null;`; `deriveMode` at
  `components/ai-readiness/useAIReadiness.ts:48-62` treats a null deadline as "passed" (`: true` at `:57`),
  which is exactly the mechanism the CYCLE-STATE contract turns on; `queryActiveCycleEndDate` is in
  `steps.go` (`:316`).
- CSV: `AIReadinessCSVColumns` is **15** entries (`csv.go:18-32`) with the UTF-8 BOM written at `:44` —
  and note the *platform's own* doc comment at `csv.go:35` still says "19-column", so the corpus is right and
  the source comment is the stale one. Same shape at `how_we_measure.go:1183` ("four" tiles vs five specs).
- `emailoverride.go:29` imports `github.com/anthropos-work/app/internal/messenger/aireadinessemail`;
  `git ls-tree ad9f3c49 internal/messenger/aireadinessemail/` = **9 files**; and `:33` does use the
  `pkg/aireadinessemail` shorthand the doc blames for the old wrong path. The email admin routes exist
  (`internal/web/backend/emailpreview/handler.go:71-73`, GET/PUT/DELETE).
- Every REST route the doc lists exists in `internal/web/backend/api/server.gen.go:2396-2406`
  (`/workforce/ai-readiness`, `/cycles` GET+POST, `/cycles/{cycleID}` GET, `/cycles/{cycleID}/close` POST,
  `/steps-completion`, `/narrative` POST, `/compare`, `/export.csv`, `/setup` GET+POST).
  `RefreshLiveSnapshots` (`live_snapshots.go:71`) and `CloseDueAIReadinessCycles` (`cycles.go:554`) exist.
- `app/knowledge/ai-readiness/overview.md` exists (17 files in that directory).
- `conversation_extractions` and `interview_extraction_results` both exist as tables, and
  `stack-seeding/seeders/succession.go` (pinned clone) does write the latter — so the red-herring/blame
  narrative is anchored on real objects.

### `corpus/services/chronos.md`

- **ENUMERATED** — `045857c` = *"chore: remove chronos service from orchestration and update related
  documentation"* (2026-04-17), touching `docker-compose.yml` (−37) and `repos.yml` (−4). `chronos` appears in
  no `repos.yml` or `docker-compose.yml` at `0c91421d`.
- **ENUMERATED** — the Asynq claim: `app/internal/jobsimwiring/worker.go` @ `ad9f3c49` imports
  `github.com/hibiken/asynq` at `:24` and `StartWorkers` (`:27`) registers the dual pools; the cited
  `:24-35` window covers the import + the standard-pool construction. The workers do run inside `backend`.
- **ENUMERATED** — the corpus-internal consistency of the "repo is NOT archived" claim: `chronos.md:3` and
  `README.md:81` agree with `platform-migration-status.md:99` (*"Repo is NOT archived on GitHub (last push
  2026-04-23)"*). I could not verify the GitHub state itself (see below).

---

## What I could not settle

1. **`chronos.md`'s entire body (lines 13–245).** The `chronos` repo is in **no** clone set on this box
   (`find . -maxdepth 3 -iname '*chronos*'` returns only the corpus doc), so the schema DDL, the
   `timers_unique_scheduled_event` index, ports 8080/8081, "Go 1.25", sqlc/Cobra, the triggerer's `LIMIT 10` /
   `FOR UPDATE SKIP LOCKED` / 3-second timeout / `scheduled_at < now() + interval '1 second'` window, the
   env-var and CLI-flag tables, and the `ChronosService` RPC shapes are all **unverifiable from here**.
   What would settle it: a `chronos` clone at a named sha, or the removed content recovered from
   `platform@045857c^` (which only carries the compose/repos entries, not the service source).
2. **"The GitHub repo is NOT archived (last push 2026-04-23)"** (`chronos.md:3`, `README.md:81`). This is a
   claim about github.com state; I made no network call. What would settle it:
   `gh api repos/anthropos-work/chronos --jq '.archived, .pushed_at'`.
3. **`README.md:43`'s storage row is REF-SENSITIVE and I deliberately did not book it.** *"In prod the ECS
   service is **deleted**, not scaled to zero — the module survives only to keep the buckets/CDN under
   `prevent_destroy`"* names no ref. At the **checkout** `4ce8ece5` it is FALSE — `storage/terraform/main.tf`
   is **100 lines** and still declares `module "storage"` with `service_desired_count = 1`. At **origin/main**
   `9f8cb532` it is exactly TRUE — the file is **18 lines**, the module block is gone, and the comment says
   *"The ECS service that used to live here is GONE (v9.0 'support-in-app')… What remains is the ASSETS…
   `prevent_destroy` would NOT stop that: it is read from configuration"*. The intervening commits are
   `8e558ce` (scale to zero) → `e6fac97` (drop the ECS service, keep the assets) → `bef79bf`. Since the
   sentence is a claim about **production now**, and `CITE_REF=auto` resolves `origin/main` first
   (§5 rule 41), I graded it true rather than manufacture a booking off a lagging local checkout — but the
   frozen instrument's "no ref ⇒ grade at the checkout" rule points the other way, and a coordinator should
   decide. The cheap fix either way is to pin the cell (`@ storage 9f8cb532`) or advance the checkout.
   The same shape applies (benignly) to `README.md:24`'s jobsimulation cell and `cms.md:12`'s `6efa1d5`
   cell — both are explicitly pinned, so both grade true at the refs they name.
4. **Seeder-behaviour claims I did not execute**, only source-checked: the 199-frozen-snapshot / 78.4%
   figures, the *"stage 3: 19…31; stage 2: 3…19; stage 1: 1…3"* spread, `validation_attempt_results` 5 → 345,
   `interview_extraction_results` = 165 rows, the demo seeding **0** `ai_readiness_recommendations` rows,
   and the timing numbers (2.09 s live / 24 ms frozen / 180 s → 19 ms). These need a live seeded stack.
5. **`ai-readiness.md:230`'s tile display names** (*"Depth·Creation"*, *"Critical ownership"*). The Go
   `usageDimSpecs` keys are `depth`/`ownership`; the human labels are rendered FE-side from `key+band`
   (`how_we_measure.go:942-944` says the tiles are localized on the FE), which I did not trace into the
   i18n bundles. The five **ids** — the load-bearing part — are exact.
