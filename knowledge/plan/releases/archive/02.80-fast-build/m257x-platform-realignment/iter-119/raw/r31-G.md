# Seat G — reading #31

**Ground truth re-verified at open (all clones, HEAD + `origin/main`):**
`app ad9f3c49` (= origin/main) · `platform 0c91421d` (= origin/main) · `next-web-app 8297c684`
(origin/main `f97ba659`) · `sentinel f2c46190` (= origin/main) · `studio-desk 41ee3575` (= origin/main) ·
`ant-academy 22df69dd` (= origin/main) · `storage 4ce8ece5` · `messenger fa47850d` · `cms ca50c817` ·
`jobsimulation 462343b0` · `roadrunner 87d8d443` · `graphql-wundergraph 60c229f3` ·
`app/studio` + `cms/studio` both `aeec036a` (own checkouts) · **rext pinned per-stack clone
`stack-demo/rosetta-extensions 09d06070`** · rext authoring copy `.agentspace/rosetta-extensions 43049308`.
Matches the ADDENDUM exactly. **Every rext claim below was graded at the PINNED per-stack clone
`09d06070`** (§5 rule 45); where I compared, the authoring copy `43049308` was byte-identical in the
region and I say so.

**Instrument note (a false-absence I caught on myself, §5 rules 1/2):** in this zsh, `git show "$c:repos.yml"`
inside a loop is mangled by the `:r` history modifier (`${c:r}` + `epos.yml`) and, with `2>/dev/null`, returns
a silent empty result. Every repos.yml-history number below was re-derived with `"${c}:repos.yml"` after the
control caught it.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/services/hiring.md` | 483 | yes |
| `corpus/services/studio-desk.md` | 449 | yes (450 lines of content — the file has no trailing newline) |
| `corpus/architecture/shared_libraries.md` | 274 | yes |
| `corpus/services/next-web-app.md` | 164 | yes |
| `corpus/services/clerk-integration.md` | 128 | yes |
| `corpus/services/skiller.md` | 78 | yes |

Protocol read: `corpus/ops/platform-alignment.md` §5 (lines 249–1528) in full.

## Blockers

### B1 — the `ai` library section describes a 9-method interface and two `panic`s that exist in no readable tree

- **Anchor:** `corpus/architecture/shared_libraries.md:128-130` — *"A thin wrapper exposing **one interface,
  `ai.AI`** (`ChatCompletion`, **`ChatCompletionStream`**, `Response`, `CreateEmbeddings`, `CreateSpeech`,
  `OCRProcess`, `AudioTranscriptions`, `Tokenize`, `GetEndpoint`)"*
  and `:162-164` — *"capability is asymmetric … **Anthropic `ChatCompletionStream`/`CreateSpeech` `panic`**"*
  (also `:137` *"`mistral.NewMistral` | Mistral — **OCR only** (chat/embeddings/speech `panic`)"* and `:134`
  *"`openai.New` / `NewOpenAI`"*).
- **Why it is false:** neither block names a ref, so per the briefing they grade at the checkout
  `app ad9f3c49`, and the standalone `github.com/anthropos-work/ai` module is **in no clone set** — the only
  readable copy of this code is the in-tree fold the same document points at (`:126`, *"`app/internal/ai/`
  carries the library in-tree"*). Measured there:
  - `app/internal/ai/ai.go` @ `ad9f3c49` declares an **8-method** `AI` interface. `ChatCompletionStream` is
    **absent**. `git grep -rn ChatCompletionStream -- internal/ai/` returns exactly one line, a **comment** at
    `internal/ai/anthropic/characterization_test.go:313`: *"Two of them used to panic. **ChatCompletionStream
    left with the whole streaming path**; CreateSpeech was converted."*
  - The same test block at `:314-316`: *"Five methods on the ai.AI interface have no Anthropic implementation,
    and **ALL FIVE now return an error** … **This package now contains no `panic("not implemented")` at all**."*
    `git grep -rn 'panic(' -- internal/ai/` @ `ad9f3c49` returns two lines: that comment and an unrelated
    `panic(err)` in `internal/ai/prompt.go:37`.
  - `internal/ai/mistral/` **does not exist** at `ad9f3c49` (re-homed by `2b3a65cf0` to
    `internal/cms/studio/mistralocr/`), and `openai.New` is gone — `git grep -n '^func New' -- internal/ai/`
    yields only `NewAnthropic`, `NewOpenAI`, `NewAzure`.
  - The removals are one commit after the fold: `1e457fa70` *"fold the ai library into app as internal/ai"*
    then `9048ce1b4` *"refactor(ai): delete the streaming path and dead exports"* (both 2026-08-04) — the
    latter's stat shows `internal/ai/stream.go` (102 lines) and `internal/ai/mistral/*` going away.
  - **It also contradicts this same file three paragraphs up:** `:149-152` measures *"since the fold
    `app/internal/ai/ai.go` is **21 lines** declaring `type AI interface` + `type TokenEncoder interface`"* —
    21 lines is exactly the **8**-method form (I counted the file; the 9-method form at `1e457fa70` is 22).
- **Why I hesitated (stated per the instrument):** the section's *Version pin* row names **`v1.40.2`**, and at
  the fold commit `1e457fa70` — which *is* v1.40.2's tree, per
  `internal/ai/module_import_guard_test.go:14-18` *"before it was folded into app at tag v1.40.2"* — every one
  of these claims holds verbatim (`ChatCompletionStream` on the interface at `:9`; `panic("not implemented")`
  at `anthropic/completion.go:180,189` and `mistral/completion.go:40,44,48,52`; `openai.New` at
  `completion.go:25`). So the prose is true *of the frozen module cms/jobsimulation still require* and false
  *of the copy `app` actually compiles*. I book it because the blocks carry no pin of their own, because a
  reader working in `app` is exactly who reads this page, and because the file's own 21-line measurement makes
  the two halves mutually exclusive.
- **Confidence:** medium

### B2 — `hiring.md` cites `service_taxonomy.md:62` for a bullet that is at `:68`; `:62` is a section heading

- **Anchor:** `corpus/services/hiring.md:47-49` — *"as the twins
  [`service_taxonomy.md`](../architecture/service_taxonomy.md)'s Tier-1 **Database** characteristic bullet
  (`:62` — *"one schema, `public`, owned by `app`, which is the only repo with migrations"*, the `cms` /
  `jobsimulation` / `skillpath` schemas *"legacy husks"*)"*
- **Why it is false:** at rosetta HEAD, `corpus/architecture/service_taxonomy.md:62` is
  `### Tier 1: Core Backend Services (Dockerized Go Microservices)` — the section heading. `:64` is
  `**Characteristics**:`, `:65` Language, `:66` Deployment, `:67` Communication, and the **Database** bullet
  carrying both quoted fragments is `:68`. The quoted text is accurate; only the anchor is wrong.
  This is the same defect class the passage itself repaired one generation ago — its own parenthetical says
  *"This cited `service_taxonomy.md:52` until M257x iter-102 — that is the closing line of the `:44-52`
  blockquote … which says nothing about schemas"* — so the corpus's own precedent grades a mis-pointed
  citation of this exact shape as a defect, and the re-point to `:62` did not land on the bullet either.
- **Why I hesitated:** the passage deliberately names the bullet as well as numbering it (*"The bullet is named
  as well as numbered because `service_taxonomy.md` is edited concurrently and `:62` can move"*), so a reader
  six lines off will still find it. I book it because it is §5 rule 21's class 2 (*an anchor that resolves but
  names the wrong construct*) and because the file has now mis-pointed this one citation twice running.
- **Confidence:** medium

### B3 — "Sign-in tokens — **only** for app-native admin impersonation" is false; two other live consumers mint them

- **Anchor:** `corpus/services/clerk-integration.md:40` — *"**Sign-in tokens** — **only** for app-native admin
  impersonation (chosen over Enterprise-tier Actor Tokens)."*
- **Why it is false:** three call sites mint Clerk sign-in tokens across the clone set, not one:
  1. `app/internal/admin/impersonation/manager.go:101` (`m.signInTokenCl.Create`) — the documented one. Its
     package comment at `:6-11` confirms the *"chosen over Enterprise-tier Actor Tokens"* half verbatim
     (*"Actor Tokens would require Enterprise"*), so that clause is correct.
  2. `next-web-app/apps/web/src/app/api/dev/login-as/route.ts:79` —
     `await client.signInTokens.createSignInToken({…})`, the *"log in as a real Clerk user"* ticket flow
     (`apps/web/src/lib/devLogin.ts:12`), whose public route pair `('/api/dev/login-as(.*)', '/dev/accept(.*)')`
     is in `apps/web/src/proxy.ts:56`.
  3. `studio-desk/src/routes/dev.ts:83` — `clerkClient.signInTokens.createSignInToken({…})`, feeding
     `dev-accept.html`.
  Sites 2 and 3 are dev-gated, but they are real, present-tense consumers of the same Clerk Backend-API
  surface, and **the corpus documents them elsewhere in this seat's own file set** —
  `studio-desk.md:34` (*"`dev-accept.html` is dev-only"*) and `:90` (*"Dev-only acceptance harness"*), with the
  mechanism spelled out in `studio-desk/vite.config.ts:38-40` (*"the 'log in as a real Clerk user'
  ticket→session exchanger"*). `clerk-integration.md` mentions the harness nowhere
  (`grep -ni "sign-in token|devLogin|login-as|dev-accept"` over the file returns only line 40).
- **Why it matters, and why I hesitated:** the page bills itself as the *"single source of truth for how the
  Anthropos platform uses Clerk"*, and this list is what a Clerkenstein author reads to decide which Clerk
  surfaces the mock must reproduce; an emphatic bolded **only** excludes two flows that would break. Against
  that: both extra sites hard-404 in production, so a prod-scoped reading of the sentence is defensible, and
  §5 rule 25's canonical grading carves *omitted list member* out as MINOR.
- **Confidence:** medium

## Minors

- `corpus/architecture/shared_libraries.md:57` — *"`sentinel`'s `88036d7` … took it `v0.34.3 → v0.35.2`, **two
  commits past** the `88bc5592` this row used to cite."* Measured: `git log 88bc5592..f2c46190` is exactly two
  commits, `88036d7` then `f2c4619` — so `88036d7` is **one** commit past `88bc5592`; it is the *checkout* that
  is two past. The version transition itself is exact (`show 88036d7 -- go.mod` is `-v0.34.3 / +v0.35.2`).
- `corpus/architecture/shared_libraries.md:3-12` and `:38-45` — the inventory of `anthropos-work` Go modules a
  stack's Docker build pulls (*"the three a stack actually builds against — colony, proto, taxonomy"*) omits
  two further **direct** requires in `app/go.mod` @ `ad9f3c49`: `anthropos-work/analytics-go v0.3.1` (`:14`)
  and `anthropos-work/storage v0.15.2` (`:17`). Both are fetched by the same `GOPRIVATE`/`GH_PAT` path the
  blockquote describes. The document's own arithmetic is scoped to *"these five"* and is exactly right, so this
  is an incomplete inventory rather than a false count.
- `corpus/services/hiring.md:243` — *"the `PersonaSeeder` also writes (`persona_write.go:69-71,143-167`)"* for
  `validation_attempt_results` / `validation_attempt_skill_results` / `validation_criterion_results`. At rext
  **pinned** `09d06070` (byte-identical in the authoring copy `43049308`, checked) the actual write steps are
  `:92-94`; the comment naming the three tables is `:66-68`; `:69-71` is a blank comment line plus prose about
  the **removed** `jobsimulation.sessions` step. And `:143-167` covers `sessionCols()` (`:152`) +
  `attemptResultCols()` (`:161`) only — `skillResultCols()` is `:169`, `criterionResultCols()` is `:179`.
- `corpus/services/hiring.md:163` — *"M223's hiring story simply sets `role_mix ≈ 0.1 admin / 0.9 candidate`"*.
  The preset declares `admin: 0.14 / candidate: 0.86` (`stack-seeding/presets/stories.seed.yaml:249` @
  `09d06070`); the 5/45 outcome comes from two candidate-hero overrides on a 7-slot admin band, and rext's own
  fence spells the trap out — `blueprint/presets_test.go:203` *"admin: 0.14 (**NOT 0.10**)"*. The `≈` hedges
  it and the derived "5 admins + 45 candidates" figure (`:388`) is correct.
- `corpus/services/hiring.md:300` — the self-citation *"the **History** blockquote (**`:193-198`**)"* starts one
  line early: `:193` is blank; the blockquote is `:194-207`.
- `corpus/services/hiring.md:302` — the self-citation *"the `job_position` bullet, now `:173-175`"* also starts
  one line early: `:173` is the tail of the previous bullet; the `job_position` bullet begins at `:174`.
- `corpus/services/studio-desk.md` — 450 lines of content but `wc -l` reports 449; the file ends without a
  trailing newline. No factual consequence.

## Positively cleared (audited zeros)

**ENUMERATED** (predicate re-derived from source independently, cardinality stated before any arithmetic):

- `shared_libraries.md:22-25` — library requires across the **seven** Go repos on disk at their pinned refs. I
  read all seven `go.mod`s and listed every `anthropos-work/{colony,proto,taxonomy,ai,authn}` line:
  **colony 7/7, proto 7/7, taxonomy 6/7 (roadrunner is the sole exception), ai 2/7 (cms+jobsimulation only),
  authn 0/7.** Matches. Every per-repo `file:line` in the colony/proto/ai/taxonomy rows resolves exactly
  (`app/go.mod:15,16` @ `ad9f3c49`; `sentinel/go.mod:8,9` @ `f2c46190`; `cms/go.mod:9,10,11,13`;
  `jobsimulation/go.mod:11,12,13,15`; `roadrunner/go.mod:7,8`; `storage/go.mod:25 // indirect`;
  `messenger/go.mod:9` direct; `app/go.mod:20` @ `b948604f`; `sentinel/go.mod:21` @ `88bc5592 // indirect`).
- `shared_libraries.md:213` — *"the union of `type: go` entries across `repos.yml`'s whole history … that set is
  **11**"*. Re-derived by replaying all **10** commits that touched `repos.yml`: 14 names ever, **11** `type: go`
  (app, chronos, cms, intelligence, jobsimulation, messenger, roadrunner, sentinel, skiller, skillpath,
  storage). All four of skiller/skillpath/chronos/intelligence are in the first revision `a2a3ee6`, and
  skillpath is the last to go (`a4db680`, 2026-07-21).
- `shared_libraries.md:86` — *"`app/main.go` registers **six** Connect handlers @ `app` `b948604` v1.366.0"*.
  `git grep -n 'connect.New.*ServiceHandler(' b948604f -- '*.go'` (whole tree, tests/gen excluded) returns
  exactly six, at `:1187 :1188 :1196 :1204 :1213 :1227` — Users, Organizations, Skiller, JobSimulation,
  CMSService (inside `if cmsRPCServer != nil`, `:1212-1214`), LabSession (`mux.Handle` at `:1228`). Every cited
  line is the construct named. `SkillPathSessionService` and a RoadRunner service are absent.
- `shared_libraries.md:93-104` — the named Connect-RPC service list is **13**, matching *"at least 13"*.
  `storage.md:129` resolves to `### Connect-RPC (\`StorageService\`)`. The `ROADRUNNER_RPC_ADDR` clause holds:
  0 occurrences in `app` @ `9d00a313` and in `platform` @ `0dab54d` (positive control `JUDGE0` returns 3 files),
  and `docker-compose.yml:118` @ `0dab54d` is indeed `AWS_REGION=eu-west-1`. `jsrunner.NewRunnerManager` is at
  `internal/jobsimwiring/wiring.go:123` @ `9d00a313`; `internal/skillpaths/skillpaths.go:27-31` carries the
  *"drop-in for the removed skillpath RPC client"* comment verbatim.
- `shared_libraries.md:146-160` — the vendor-selection correction. All eleven anchors resolve at `ad9f3c49`:
  `jobsimulation/ai/ai.go` `getClient`@`:259`, `flag_use_azure_us`@`:267`,`:344`, `isThrottlingError`@`:129`
  used at `:166`,`:325`; `skillerai/ai.go` `getClient`@`:332`, flag@`:347`, `isThrottlingError`@`:128` used at
  `:176`; `internal/ai/ai.go` is 21 lines with the two interfaces; `external_services.md:579` is
  *"There is no ordered EU-first fallback chain."* — the exact sentence claimed.
- `shared_libraries.md:177` — *"6 and 8 `.go` files"* importing `colony/authn` in cms/jobsimulation: measured
  **6** and **8**. `clerk-integration.md:51/91` — **129** files in `app`, **0** in each of sentinel, storage,
  messenger.
- `hiring.md:257-270` — the *"four required-and-undefaulted columns"* predicate. Read the whole DDL
  (`20260722104506.sql:2-27`): NOT NULL ∧ no DEFAULT is exactly `owner_id :6`, `sim_id :7`, `sim_type :10`,
  `token :13`; every other NOT NULL column carries a DEFAULT; the table has **23** columns (matching
  `:180`), `token` is UNIQUE at `:29`, and `jobsimulation_id` does not exist on it. `SET "score"` is **0** hits
  set-wide (positive control: `SET ` matches many files). The only `CREATE SCHEMA` in the whole migration set
  is `auth` (`20230817154747_supabase_baseline.sql:2`), and **no** migration references the `jobsimulation`
  schema (positive control: `public.` matches 14 files).
- `hiring.md:259-261` — `completion_status` is a closed **5**-value enum, exactly
  pending/passed/failed/discarded/timedout (`enum/jobsimulation.go:29-35`, `Values()` `:37-43`), with **no**
  `SIMULATION…` member.
- `hiring.md:90-93` — the document's own positive controls re-derived at `ad9f3c49`: `OrgFeatureInsights`
  **×8** in `resolver_queries.go`, `JobSimulationSession` **×44** in `intelligence.go`, `grep -in hiring` in
  `intelligence.go` = **0**, and in `resolver_queries.go` only sim-TYPE filters (`:811`, `:843`, `:991-992`).
  The gate predicate itself checks out clause-by-clause: `:1035` Casbin, `:1053` status ∈ {active, invited},
  `:1085`/`:1089` belong to the neighbouring `InsightsJobSimulationBySessions`.
- `hiring.md:340-352` — the *"passed `false` in exactly **two** places, both in `apps/hiring`"* claim:
  `git grep 'showWorkforce={false}'` @ `8297c684` returns exactly two,
  `apps/hiring/…/template.tsx:167` and `:248`. `orgSectionVisibility` (`orgGroups.ts:48-65`) takes no
  `isHiringOrg` parameter and returns `intelligence: isAdmin` at `:61`; `showWorkforce` defaults `true` at
  `useNavbarSections.tsx:161` and gates the item at `:568`.
- `hiring.md:454` — *"the regression enumerates **all 8** generic seeders"*:
  `hiring_scope_test.go:81-95` @ rext `09d06070` lists exactly 8 (jobsim-sessions, activity,
  skillpath-sessions, assignments, hero-activity, persona, feedback, succession).
- `studio-desk.md:38-51` — the *"NOT a GraphQL client"* correction, re-derived at `41ee3575`:
  `git grep -in graphql -- 'src/*'` returns **exactly two** lines, `src/routes/skillpath.ts:374` and `:405`,
  both comments saying the opposite; `git grep -n 8082 -- 'src/*'` returns **0** (positive control: `directus`
  matches 2 files in `src/`); the four routers are at `src/index.ts:150,158,161,164`; and
  `new GraphQLClient(` appears at exactly the four frontend sites named
  (`userService.ts:20`, `taxonomyService.ts:43`, `userPreferencesService.js:13`,
  `content/simulationContentService.js:325`), fed by `app/services/config.ts:6`.
- `studio-desk.md:121` — `.env.example` is **131** lines; `:117` `YOUTUBE_API_KEY=`, `:118` the comment,
  `:119` `GCLOUD_SERVICE_ACCOUNT=`, `:120` blank — exactly as described. `terraform/main.tf:129` injects it;
  `git grep GCLOUD_SERVICE_ACCOUNT -- 'src/*'` = **0** (positive control `YOUTUBE_API_KEY` returns 2).
- `studio-desk.md:34` — the prod entry set in `vite.config.ts` `rollupOptions.input` is exactly the nine named
  (`home`, `sim-advanced-builder`, `builder-skill-path`, `simulation-builder`, `sim-guided-builder`,
  `generation`, `catalog`, `academy`, `skills`) with `dev-accept` behind `isProduction ? {} : {…}` at `:41`.
- `studio-desk.md:134` — **7** locales on disk (`app/public/l12n/{de,en,es,fr,it,ja,nl}.json`).
- `next-web-app.md:22` — all four apps declare the **tilde** `"next": "~16.2.12"`, `apps/mobile` declares none,
  and `pnpm-lock.yaml` resolves `next@16.2.12`. React `^19.2.7`, `engines.node ">=24.0.0"`,
  `packageManager "pnpm@10.30.3"`, `turbo "^2.9.6"`.
- `next-web-app.md:70` / `:155` — **8** locales: `configs/i18n/messages/{de,en,es,fr,it,ja,nl,pt}` and
  `configs/i18n/locale.ts` `SUPPORTED_LOCALES` lists the same eight.
- `next-web-app.md:75` — `proxy.ts` exists in `apps/{web,hiring,integration}/src/`; **0** files named
  `middleware.ts` anywhere in the repo; `CLAUDE.md:15` and `:55` read verbatim as quoted; all seven named
  public routes are in the `createRouteMatcher` list (`/login` `:22`, `/sign-up` `:25`, `/checkout` `:16`,
  `/free-trial` `:17`, `/monitoring` `:31`, `/print` `:33`, `/api/bunny/thumbnail` `:37`) and `/print` is
  HMAC-gated at `:59,66-70`.
- `clerk-integration.md:37` — **12** Clerk webhook event types: `internal/clerk/events/events.go`
  `:166,172,178,185,191,197,204,210,216,223,229,235`, spanning exactly the four families named.
  `svix-webhooks` is `go.mod:65`.
- `clerk-integration.md:41` — **8** Clerk locales imported at
  `apps/web/src/providers/Clerk.provider.tsx:10-17` (deDE, enUS, esES, frFR, itIT, jaJP, nlNL, ptPT).
- `clerk-integration.md:103` — every SDK version: next-web `apps/{web,hiring,integration}` `^6.39.6`,
  `apps/maintenance` declares no `@clerk/nextjs`, `ant-academy code/package.json:52` `^6.39.2`,
  `next-web-app/apps/mobile/package.json` `~2.6.18` vs `ant-academy/mobile/package.json:18` `~2.19.36`,
  `app/go.mod:31` @ `5ba17044` `clerk-sdk-go/v2 v2.7.0` (still v2.7.0 at `ad9f3c49`, moved to `:29`).
- `skiller.md:24-25` — *"0 hits over all **44** tracked `.tf` files in the 13 `stack-demo` repos"*. Enumerated
  the search set per-clone (app 5, cms 4, graphql-wundergraph 4, jobsimulation 6, messenger 4, next-web-app 2,
  roadrunner 4, rosetta-extensions 2, sentinel 4, storage 5, studio-desk 4 = **44**; ant-academy, platform and
  both nested `studio` checkouts have 0), grepped each at its own HEAD: 0 hits for
  `backend.internal.anthropos`.
- `skiller.md:66-67` — **8** `ContentLanguage`s (`assignments.graphqls:44-53`).

**RE-DERIVED** (arithmetic/line-anchor confirmation only — weaker, per §5 rule 24):

- All `app` anchors in `hiring.md`'s read-path table and blockquotes at `ad9f3c49`: `manager.go:450,453,537`
  (and the historical offsets `→448/451/487` at `5ba17044`, confirming the *"NOT uniform"* warning),
  `resolver_cms_queries.go:95,99-103,210,258,295` (all inside `PrivateJobSimulations`, declared `:89`),
  `siminvitationlink.go:62,63` (inside `CreateOrganizationSimInvitationLink`, `:56`),
  `intelligence.go:1700,1702,1709,1733,1739-1744,1751-1764,1820,1844,1846,2124,2158-2160,885-886`,
  `ent/schema/job_simulation_session.go:33,39,45`, `ent/jobsimulationsession.go:181-186`,
  `graph/graph.go:129392-129400` and `:129546-129554`, `jobsimulations.graphqls:14` and `:128`,
  `anticheat_result.go:24`, `askengine/registry.go:192`,
  migrations `20260722104506.sql:2,53,79`, `20260729133514.sql:58-62`, `20240527131926.sql:7`,
  `20250416091037.sql:5`, `20260722081626_jobsim_data_model.sql:336/355/376`, `atlas.hcl:8`, `repos.yml:14-17`.
- `next-web-app` anchors: `useGetClerkOrganization.tsx:16-18,20-21`,
  `apps/web/src/context/UserStatusContext.tsx:141-173,144-148,168-172`,
  `apps/hiring/src/context/UserStatusContext.tsx:125,144-145`,
  `useNavbarSections.tsx:249-256,329-331,340-343,391-398,475-482,476` (and `:460` at `bb3313bc`),
  `InsightsByMembersContainer.tsx:108` and `:359`, the `[simId]/page.tsx:14`,
  `template.tsx:90`, `FreeTrialContainer.tsx:29`, `insights.ts:31-82`, `metabase/route.ts:35`.
  The two `simulationScoreColumn.tsx` copies `diff` **empty** — byte-identical as claimed.
- Compose/Makefile: `docker-compose.yml` @ `0c91421` `:110,119,135,138-141,151,160,165-168`, and the
  historical `:204,220,223-226,236` @ `0dab54d` and `:337-341` @ `2adcf71`; `Makefile:119-120`;
  `repos.yml` entry counts 6 @ `0dab54d` → 4 @ `838d907` → 4 @ `0c91421`; `GH_ACCESS_TOKEN: $GH_PAT` at
  `:12` and `:39` with `GOPRIVATE`/`insteadOf` at `app/Dockerfile:11,13`.
- rext (**pinned `09d06070`**): `persona_write.go:91,152`, `hiring_config.go:99`, `hiring_scope.go:45`,
  `blueprint.go:453-458` (`RoleMix{Admin,Member,Candidate,AdminEmails}`), `contentref.go:158`
  (`hiringComparableFloor = 6`), `up-injected.sh:1076-1085` + `demo-stack/frontend/hiring.Dockerfile`,
  all five `studio-desk-*` patch dirs, both `test_studio_desk_*` names in
  `stack-injection/tests/test_injection.py`, `stack-verify/e2e/run-studio-fcp.sh`,
  `stack-verify/e2e/tests/render-hiring-comparison.spec.ts`.
- `sentinel/init_policy.sql:54` — `('p3','default','admin','org:feature:insights',…)`, confirming
  `hiring.md:393`'s *"no net-new grant"*.
- Intra-corpus anchors that DO resolve: `dependency_map.md:78`, `storage.md:129`,
  `shared_libraries.md#taxonomy` (`:206`) / `#taxonomy-figures` (`:228`) / `#authn` (`:171`),
  `backend.md#skiller-in-app-merge--fact-sheet-v21-quick-change` (`:123`),
  `stories-spec.md#the-m223-hiring-chain…` (`:657`), `seeding-spec.md#the-recruiter-vantage…` (`:375`).

## What I could not settle

- **Anything inside `colony`, `proto`, `taxonomy` or the standalone `ai`/`authn` modules.** None is in any
  clone set. So `shared_libraries.md`'s colony package table (`:64-70`), the Sentry/`FORCE_SENTRY` note, the
  `rate.NewLimiter(1,5)` *"only logs"* claim (`:73-75`), proto's `buf v1` layout / `buf v1.57.0` CI pin /
  `go/simulator/*` package count (`:112-115`), the `chronos v0.30.1` pin (`:57`), the authn `v1.7.0` tag and
  its missing-`return` bug (`:175`, `:196-198`), taxonomy's *"131-line `node.go`"* and every `NewSkillID`
  example (`:221`, `:247-261`) are unverifiable here. Two are *partly* corroborated: `storage` really does
  import `go/simulator/storage/v1` as `legacyStorage` (`storage/internal/migration/migration.go:13` @
  `4ce8ece5`), and the `ai` module's own guard comment confirms the v1.40.2 fold. Settling the rest needs the
  library repos cloned.
- **Production-only assertions.** `studio-desk.md:146` (*"the Cosmo Router survives in production only"*) and
  `next-web-app.md:162` (*"prod-only since `2adcf71`"*) are consistent with each other and with the deleted
  compose service, but prod state lives in the `infrastructure` repo, which is in no clone set — the same
  unmeasurable class `skiller.md:21-29` handles explicitly. I neither cleared nor booked them.
- **Snapshot- and run-derived figures.** `hiring.md:171-176` (*"87 real `SIMULATION_TYPE_HIRING` sims"*,
  *"0 `job_position` rows"*, *"the prod 443"*), `:428-467` (*"~8 per position"*, *"scores 27–100"*,
  *"5/5"*, *"p95 1.27 s"*), `studio-desk.md:426` (*"4669 ms → 817 ms"*) and `:437` (*"4049 → 38 ms"*).
  These come from a capture manifest under `.agentspace/snapshots/` and from live runs; settling them needs a
  live stack, not a clone.
- **Whether `shared_libraries.md`'s `ai` prose intends the frozen module or the in-tree fold** (B1). If the
  intent is the module, the fix is a `v1.40.2` pin on those two blocks plus a note that `app`'s copy diverged
  at `9048ce1b4`; if the intent is the fold, the prose needs rewriting. Either way the current text cannot be
  read correctly by someone working in `app`. A one-line pin would settle it.
