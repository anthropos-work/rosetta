# Seat F — reading #32

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/services/ant-academy.md` | 503 | yes |
| `corpus/architecture/architecture_overview.md` | 435 | yes |
| `corpus/services/storage.md` | 248 | yes |
| `corpus/services/roadrunner.md` | 216 | yes |
| `corpus/architecture/dependency_map.md` | 103 | yes |
| `corpus/services/TEMPLATE.md` | 46 | yes |
| **total** | **1,551** | — |

### Ground truth re-verified at this reading's open (all match the ADDENDUM)

`app ad9f3c49` · `platform 0c91421d` · `next-web-app 8297c684` · `sentinel f2c46190` · `studio-desk 41ee3575` ·
`ant-academy 22df69dd` · `storage 4ce8ece5` (origin/main `9f8cb532`) · `messenger fa47850d` · `cms ca50c817` ·
`graphql-wundergraph 60c229f3` · `roadrunner 87d8d443` · `jobsimulation 462343b0` (origin/main `82cb66ec`) ·
nested `stack-demo/app/studio aeec036a` · **pinned per-stack rext `stack-demo/rosetta-extensions 09d06070`** ·
authoring rext `.agentspace/rosetta-extensions 43049308`.

**Trees my numbers cover, stated per rule 44:** every rext claim below was graded against the **pinned
per-stack clone `09d06070`** (rule 45 — that is the code a stack runs), and I say so per finding. The
`mistral` predicate in `dependency_map.md:25` was graded at the **nested** `stack-demo/app/studio` repo's own
ref `aeec036a`, not at the `app` host ref, because a host-ref `git grep` over `studio/` returns a guaranteed 0.

---

## Blockers

### B1 — `roadrunner.md` cites its own `:124` for text that lives at `:134`; the anchor lands on the WebSocket-LSP bullet

- **Anchor:** `corpus/services/roadrunner.md:130` — final clause of the § Async tasks paragraph:

  > `…HandleCodeSubmissionResultTask` on `CodeRunQueue` — *"NOT stream handlers."* **Consistent with `:124` below (*"Upstream consumers: none (orphaned)"*).**

- **Why it is false:** the quoted string is at **`:134`**, not `:124`. Measured in the corpus itself:

  - `sed -n '124p' corpus/services/roadrunner.md` →
    `* The repo contains an experimental WebSocket LSP proxy (\`internal/lsp/lsp.go\`) that is NOT wired into any running server — there is no reachable LSP endpoint today.`
  - `sed -n '134p' corpus/services/roadrunner.md` →
    `* **Upstream consumers**: **none (orphaned — see the banner at the top).** …`
  - `grep -n 'Upstream consumers' corpus/services/roadrunner.md` → exactly two lines: **`130`** (the citing
    sentence's own quotation) and **`134`** (the cited claim). There is no third site, so `:124` cannot be
    a different-but-valid target.

  This is the §5 rule 21 class-2 defect verbatim — *an anchor that resolves but names the wrong construct*.
  It is not a dangling reference a reader would notice: `:124` is a real, well-formed bullet in the same
  document, so a reader following the pointer lands on a true sentence about an unwired LSP proxy and is given
  no signal that it is not the corroboration being promised. The citing sentence's whole rhetorical job is to
  say *"a second, independent site in this file agrees with me"*; the pointer it supplies does not go there.

  Off-by-ten, so it reads like line drift from an edit above `:130` — but the protocol's grading (§5 rule 34,
  and the briefing's own BLOCKER definition, *"wrong line anchors that point at the wrong construct"*) books
  the wrong-construct outcome rather than the cause.

- **Why I hesitated:** rule 25 records that the canonical grading rule carved **line drift** out as MINOR. I
  book it as a BLOCKER anyway because the resolved line is not blank and not adjacent — it is a *different,
  substantive claim about a different subsystem* (an LSP proxy vs. the consumer set), which is precisely the
  case rule 21 separates from harmless drift and which `anchor_construct_guard` was built for. The briefing
  also resolves doubt upward.
- **Confidence:** high.

---

## Minors

- `corpus/services/roadrunner.md:13-14` — doubled word across the line break: *"…`service_desired_count = 1` and **has** / **has** not been touched since `84a4b4f`…"*. (Missed by a within-line sweep; found by reading.) No factual consequence — the claim itself is correct (`git blame -L 19,19 87d8d443 -- terraform/main.tf` → `84a4b4f1`, 2025-12-15).
- `corpus/architecture/architecture_overview.md:348-360` — the blockquote opened at `:346` **loses its `>` prefix for 13 lines** and resumes at `:361`. CommonMark lazy continuation absorbs them into the same blockquote paragraph, so it renders correctly; the source structure is damaged, and any future edit that inserts a blank line in that span would silently split the quote. Formatting only.
- `corpus/services/ant-academy.md:304` — *"~26 Playwright e2e spec files (tests/e2e/)"*. Measured at `22df69dd`: `playwright.config.js:13` sets `testDir: './tests/e2e'`, and that directory holds **31** `*.spec.js` files (40 files total: 31 specs, 7 `.png` snapshots, 2 `_`-prefixed helpers). Tilde-hedged approximate count, drifted ~19 %; non-load-bearing Tech-Stack row.
- `corpus/services/ant-academy.md:176` — *"a separate, unauthenticated `code/public/catalog.json` (~2,667 entries)"*. Measured at `22df69dd`: the file is an object whose `courses` array holds **2,692** entries. Tilde-hedged, 0.9 % off.
- `corpus/architecture/dependency_map.md:19` — the storage row cites *"`docker-compose.yml:126-131` for the `depends_on`"* at platform `0dab54d`. The `depends_on:` block is **`:126-130`**; `:131` is the following comment line (`# v9.0: NOT in the default profiles any more…`). A one-line over-range, and notable only because the *same cell* carries a ⚠️ correcting an identical line-range-vs-set-cardinality error (`:116-123` → `:117-123`) — the sibling range beside the one it repaired was not re-derived.
- `corpus/services/ant-academy.md:474` — *"This document contradicted itself **213 lines earlier**"*. Exact for the literal phrase **"public-storefront"** (`:262`, 474−262 = 212; the enclosing sentence begins `:261`), but the same breath also cites *"Phase-1 public launch"* (`:317`, 157 lines earlier) and *"the public surface is much wider than 'a few auth pages'"* (`:298`, 176 lines earlier). One distance is offered for four sites at three different distances. Rhetorical pointer, no factual consequence.

---

## Positively cleared (audited zeros)

Labelled per the briefing. **ENUMERATED** = I derived the predicate's set from source independently and
compared cardinalities. **RE-DERIVED** = I only re-computed arithmetic the document displays.

### ENUMERATED

1. **`architecture_overview.md:381-387` — "Ent privacy policies auto-filter by organization on only 31 of 135 schemas."** The repeat-offender of §5 rule 24; I enumerated the set *and* tested the predicate, not the sum.
   - Denominator: `internal/data/ent/schema/` holds **139** `.go` files, of which **135** contain an `ent.Schema` embed (135 files, 135 lines — one each; 0 `_test.go`). **135 is right.**
   - `grep -c 'OrganizationMixin{}'` → **30**; `user_resource.go:22` is `// OrganizationMixin{},  // We need to work on this` — **commented out**, so **29 live**. The corpus states this exception explicitly and correctly.
   - I then enumerated **every** schema-level `Policy()` in the tree — five: `mixin.go:99` (`UserMixin`), `mixin.go:126` (`OrganizationMixin`), `org_membership.go:172` (`Membership`), `organization.go:56` (`Organization`), **`user.go:116` (`User`)**. The fifth is *not* in the corpus's 31, and that is **correct on the predicate**: `User.Policy()` is `DenyIfNoUserInContext` / `DenyNotSameUser` / `FilterSameUserRule` — it filters **by user, not by organization**. Same for `UserMixin.Policy()` (`FilterOwnerRule`). `Membership.Policy()` (`DenyMismatchedOrganization`, `AllowCurrentOrgEdgesOrSkipRule`) and `Organization.Policy()` (`rule.FilterSameOrganizations()`, `:96`) **do** org-filter.
   - **29 + Membership + Organization = 31 of 135.** Set, cardinality and predicate all hold.

2. **`architecture_overview.md:80` / `dependency_map.md:42,48,49,50` — the shared-library import matrix.** Read every `go.mod` across the seven Go clones at their pinned refs: colony **7/7**, proto **7/7**, taxonomy **6/7** (absent only in `roadrunner`), ai **2/7** (`cms/go.mod:9`, `jobsimulation/go.mod:11`, both `v1.40.2`), authn **0/7** (0 `go.mod` requires *and* 0 Go-source imports of `github.com/anthropos-work/authn`, against a positive control of colony required by all seven). `1e457fa70` (2026-08-04) is indeed *"refactor(ai): fold the ai library into app as internal/ai"*, and `internal/ai/module_import_guard_test.go` exists. `app/go.mod:20` @ `b948604f` and `sentinel/go.mod:21` @ `88bc5592` both resolve to `taxonomy v1.2.0` (direct / `// indirect`) at the refs those cells name. `colony/authn` file counts: app **129** @ `ad9f3c49`, cms **6**, jobsimulation **8** — exact.

3. **`ant-academy.md:273-279` — "`TopBar`'s surface set is SEVEN routes, not five."** Enumerated all non-test `<TopBar` mounts at `22df69dd` rather than trusting the list: `AcademyClient.jsx:1906` (mounted at `/` `page.jsx:151`, `/courses` `courses/page.jsx:92`, `/courses/[slug]` `courses/[slug]/page.jsx:219` — **3 routes**), `CourseClient.jsx:2091`/`:2141` (`/chapters/[slug]`), `LatestClient.jsx:128`, `BookmarksClient.jsx:508`, `MyActivityClient.jsx:161` — **7**. `MyCertificatesClient.jsx` imports no `TopBar` ✓. Every anchor byte-exact. The provenance claims hold too: `git log -S'LanguageSelector' -- code/src/components/TopBar.jsx` names **`5b05b7d9` (2026-05-05)** as the *first and only* commit introducing the mount, and `e22f3230` (2026-05-18) is *"feat(i18n): add locale support for es, fr, de, nl, pt"* — 7-locale, as claimed.

4. **`dependency_map.md:58` — "21 live handlers" on messenger's `backend` stream.** `messenger/internal/flow/flow.go:72` is `AddSubscriber("backend", sub.AddHandler(`; the block `:73-103` yields `grep -c 'pubsub.EventHandler('` = **22**, of which `:83` is `// pubsub.EventHandler(h.OrgJobSimulationAssignmentPastDueHandler), // not implemented` — commented out. **21 live.** The corpus excluded the dead one correctly (rule 24's *"`grep -c` over source counts commented-out code"* applied, and applied right). Corroborated in-platform: `app/internal/messenger/flow/streams.go:70` calls it *"the 21-handler set"*.

5. **`dependency_map.md:59` — the `skiller`-stream cell (the densest in my scope).** Every one of its ~15 assertions holds. At `b948604f` the publisher constructors are exactly `main.go:287`, `:637` (`SKILLPATH_STREAM`), `:1039` (`CMS_STREAM`), `wiring.go:127` (`AI_USAGE_STREAM`), `:180` (`JOBSIMULATION_STREAM`) — `SKILLER_STREAM` is not among them; its sole Go occurrence at that ref is `main.go:1276`, an `AddSubscriber`. At `ad9f3c49` those six anchors drift exactly as described (`:287` → `logger.Info("subsystem switches",`, `:637` → `)`, `:1039` → `serverContext,`, `:1276` → `apiKeyManager,`, `wiring.go:127` → asynq-client construction, `:180` → blank) and **the seventh holds**: `internal/roles/roles.go:791` is `func (r *RoleManager) SkillerSubscriber() *pubsub.Subscriber {` byte-identical at all three refs — so the cell's own **6-drift / 1-holds** retraction is accurate. `SKILLER_STREAM` at `ad9f3c49`: **6 Go lines across 3 files** (`main.go`, `subscriber_merge_test.go`, `subscriber_wiring.go`) and **6 files** off the `*.go` pathspec — both exact. No `NewPublisher` names it at either newer ref (it only fills `Skiller:` in `streamNamesForSubscribers`, `main.go:1537`). Compose still sets it: `docker-compose.yml:71` @ `0c91421`.

6. **The compose topology, everywhere it is asserted** (`dependency_map.md:7,13,31`; `architecture_overview.md:12,71,157,223`; `storage.md:44,66-68,86,189`). At `0c91421`: `docker-compose.yml` declares **5** services (`sentinel`, `backend`, `studio-desk`, `next-web-app`, `gotenberg`), `common.yml` adds `postgresql` + `redis` → **7 effective**; `repos.yml` has **4** entries. The always-on floor is derived, not assumed: `postgresql`, `redis` and `sentinel` carry **no `profiles:` key**, so a retired token exits 0 and starts three — the corpus's framing. **Zero `*_RPC_ADDR` in any compose file** (the only tracked hits are `platform/CLAUDE.md:34-35`, prose). Byte-exact: `:48` `AUTHORIZATION_ADDRESS`, `:57` `GOTENBERG_URL`, `:59` `JUDGE0_BASE_URL`, `:66` `REDIS_ADDR`, `:71` `SKILLER_STREAM`, `:82`/`:83` the two production buckets, `:73-79` the *"These MUST be set"* comment, `:84-92` the messenger/customerio rationale, `:93-94` the two DSNs, `:100` the `$HOME/.aws/credentials` mount, `:101-109` `depends_on` with `:102-103` in-line, `:56` `ENVIRONMENT=development`, `:138-140` studio-desk's sole `backend` edge, `:183` gotenberg on `core`.

7. **`dependency_map.md:21,27` / `architecture_overview.md:277-283` — "d11a403 moved only the MIDDLE TWO."** `git show d11a403 -- docker-compose.yml` changes exactly `CMS_RPC_ADDR` and `JOBSIMULATION_RPC_ADDR` → `http://backend:8083`; at `d11a403^` the messenger block (`:255`, `:256`, `:258`, `:265`) already had `BACKEND_USERS_RPC_ADDR` and `SKILLER_RPC_ADDR` at `backend:8083`. The **two-of-four** correction is right and the *"re-pointed all four"* form is correctly retracted.

8. **`roadrunner.md` — the whole orphan/de-orchestration case.** `terraform/main.tf:19` `= 1` ✓; `git blame -L 19,19 87d8d443` → `84a4b4f1` (2025-12-15), and `84a4b4f` is the commit that **created** `terraform/main.tf` (95 new lines) ✓; `87d8d443` touches exactly one file, `.github/workflows/bump-version.yml`, **3 insertions** ✓; the file's own most recent touch is `e45eb61` (2026-05-27), a single `source =` module swap ✓. Ports: `cmd/root.go:110` `PORT`→`8080`, `:84` `RPC_PORT`→`8081` ✓; at `2adcf71` compose published `10400:10400`/`10401:10401` at `:291-292` and set `PORT=10400` `:298` / `RPC_PORT=10401` `:302` inside the `:296-302` env block ✓. `repos.yml` at `2adcf71` has **9** entries with roadrunner at `:29-31` ✓; `d11a403` deletes `- name: cms`, `- name: jobsimulation` **and** `- name: roadrunner` while its own message says roadrunner's entry *"was already gone"* — **the diff is the fact**, exactly as the corpus insists ✓. Go-scope zero: `ROADRUNNER_RPC_ADDR` / `RoadRunnerService` / `roadrunner:10401` → **0** hits in `*.go` at both `app b948604f` and `jobsimulation 462343b0` ✓. Repo-wide case-insensitive sweep, each clone at its own ref: **app 25 · jobsimulation 8 · platform 3 · studio-desk 3 · next-web-app 1** — all five exact ✓. `jobsimulation` Go source: exactly one mention, `internal/runner/runner.go:3`, a comment ✓; repo-wide `grep -in` = **14 lines / 8 files**, exact-case **5 / 3** ✓. `app/internal/jobsimulation/simulator/stream_handlers.go:30-34` says *"NOT stream handlers"* ✓. Asynq `v0.25.1`, `gorilla/websocket`, `MaxRetry(3)`, `Concurrency: 10`, `roadrunner:default`, `roadrunner:submissionresult`, 0 `*_test.go`, `Dockerfile:18` = `RUN go test -v ./...` — all ✓.

9. **`storage.md` — the two-sided v9.0 fold.** Prod at `9f8cb53`: `terraform/main.tf` is **18 lines**, declares no service block, `:9-11` is the *"ECS service … is GONE"* comment, `:13-16` the `prevent_destroy`-read-from-configuration rationale, `:18` byte-exact *"See outputs.tf — consumers should reference these by output, never by literal name."*; **`custody` occurs 0 times** in the repo (exit 1, against a 30-file positive control on `storage`); **no `moved` block** exists; M903's only mention is `terraform/storage.tf:22` and the `:22-25` quotation is byte-exact. The ref-flip warning is real: at the checkout `4ce8ece5` (**20** behind, confirmed) `main.tf` is **100** lines and still declares the ECS module, and `storage.tf:23-24` still reads *"relocate the assets out of this module BEFORE M907"*; `d3e6d32` (2026-08-05) is the retirement and its body literally contains *"M903 never ran."* App side: `main.go:504`, `:516-517`, `:518-523`, `:524`, `:525`, `:529-535`, `:547`, `:1102`, `internal/storage/service.go:22`/`:24`, `env_guards.go:37-44`, `internal/storage/storage.go:193-200` — every anchor byte-exact. `STORAGE_RPC_ADDR`: **3 hits, all comments** at `2035f9a4` *and* identically at `ad9f3c49`; **15** at `b948604f`. I initially measured only **6** env reads there and my regex was wrong, not the corpus — `internal/jobsimwiring/wiring.go:115` uses the local `getenv(` helper, not `os.Getenv`; the true split is **7 env reads / 4 test / 2 comment / 2 error-string = 15**, so *"7 of them live env reads"* is **correct**. `storage.md`'s other externals also hold: `:210` and `:206` @ `2adcf71`, `:324` genuinely inside the studio-desk block, `:119` @ `0dab54d`, platform `README.md:81-87`, `storage.go:122` (presigned → `("", nil)`), Go 1.25, `viant/afs`, binary defaults 8080/8081 at `cmd/root.go:45-46`, 0 `*_test.go`, 0 `//go:generate`, and storage's own `main.go` is **18 lines** (so the cell's *"a bare `main.go` here resolves to storage's own 18-line main.go"* is right).

10. **`architecture_overview.md:20,221` + `dependency_map.md:31` — the jobsimulation-vs-cms M810 asymmetry.** Graded at the ref each clause names (rule 33), which matters here because the checkout `462343b0` still has the module. `6092c6d2` (2026-08-04, *"remove the jobsimulation ECS service and ECR repository (M810)"*) is on `origin/main` and at it `terraform/main.tf` is **56 lines** with the `module "jobsimulation"` block gone; `service_desired_count` no longer appears in that file at all (only in `CHANGELOG.md` — positive control: at `462343b0` it appears in both). `:15-22` is exactly the deletion comment naming ECS service + task definition + ECR repository; `:24-40` is exactly the survivals (livekit.tf, chime.tf, ssm.tf, atlas tracker + *"a separate M810 step"*). Both citations are correct at the ref cited. The cms side: `cms/terraform/main.tf:39` `service_desired_count = 0` ✓ **and** `6efa1d5` (2026-08-04) is *"chore(ci): drop build-production — the cms ECR repository is decommissioned (M810)"* ✓ — two measured facts pointing opposite ways, reported and not resolved, as the corpus does.

11. **`architecture_overview.md:3,222,251,294` — "supergraph 3 → 1 at `915da06`, and the commit's own subject is wrong."** `915da06` lives in **`graphql-wundergraph`**, not `platform` (I had to sweep all clones to find it). Dated 2026-07-29, subject *"fold cms subgraph into backend (supergraph **2→1**)"*, and its diff deletes **both** `schemas/cms.graphqls` (−762) **and** `schemas/jobsimulation.graphqls` (−860) from a config whose own removed CLAUDE.md line read *"Composes **3** subgraph services"*. The corpus's *"do not take the count from that commit's own subject line"* is independently confirmed. Also: `.graphqls` count in `app` @ `ad9f3c49` is **43**, and `academy.graphqls` is one of them — so `ant-academy.md:63`'s *"1 of 43"* is exact.

12. **`ant-academy.md` — the DB-authoritative catalog chain and the five-key retraction.** `serverTenant.js:145` is byte-exact `const view = (await getBackendCatalogView(eids)) ?? emptyCatalogView()`; `:115-117` returns **five** keys including `bundles: PUBLIC_BUNDLES, catalogVersion: CATALOG_VERSION`; `:111-113` is the *"carries no tenant metadata…"* justification; the *"cutover is intentional, not reversible-on-error"* line is at `:132`, inside the cited `:115-145` window. `ucourses/catalog.js:961` is `export const PUBLIC_BUNDLES = [` and `:31` is `export const CATALOG_VERSION = '1.0'`. `AcademyClient.jsx:1363-1365` is the `scopedChapters.filter` that drops empty paths — so the *"→ 0 cards conclusion is unaffected"* reasoning is sound. Write path: `store.js:150`/`:162`/`:169`/`:202`/`:210`/`:215` and `writeThrough/index.js:247`/`:259` all byte-exact, confirming the corrected client-harness-vs-beacon attribution.

13. **`ant-academy.md:308-327` — the complete `isPublic` matcher.** Read `code/proxy.js` `createRouteMatcher` (`:112`) end-to-end and compared set-to-set against the table: all eight always-public groups match exactly, including both `/api/_meta(.*)` and `/api/meta(.*)` spellings with the `next.config.js` pre-rewrite rationale, and both dev-only spreads (`DEV_LOGIN_ENABLED` → `/api/dev/login-as`, `/dev/accept`; `VISUAL_BYPASS` → `/my-certificates`, `/my-activity`, `/bookmarks`). `/api/chapters/*` is genuinely **not** in the matcher, as the caveat says.

14. **`ant-academy.md:474` — the retracted "domain-gated, external users cannot enter."** Every cited receipt is byte-exact at `22df69dd`: `platformUrls.js:1-32` (*"a storefront in front of the Anthropos platform"*, FLOW A *"a new visitor registers"*, FLOW B *"for an anonymous visitor"*), `pricing.js:21` `STANDARD_YEARLY = { usd: 399, eur: 349 }`, `TopBar.jsx:77-88` (the anonymous-only *"Buy AI Academy"* CTA → `platformCheckoutUrl()`), `schema.js:3` `SITE_URL = 'https://aiacademy.anthropos.work'`. `knowledge/user-types.md` enumerates exactly the four named types and contains **zero** occurrences of `anthropos.work`. The repo's own `CLAUDE.md:11` does still say *"internal learning portal for Anthropic employees"* — so the attribution of the inherited error is also correct.

15. **`ant-academy.md` rext claims — graded on the PINNED clone `09d06070` (rule 45).** `demo-stack/ant-academy.sh` wires the five patches at exactly `:56` (dev-origins), `:66` (fs-published-fallback), `:75` (fs-published-chapter-body), `:84` (fs-published-public), `:90` (back-to-cockpit) — the set and all five line numbers exact. `:576-583` is the M220 comment stating the `e2e_persona` bypass is gone, and the actual launch at `:585` sets only `NODE_ENV=development` — neither `BENCHMARK_VISUAL_BYPASS` nor `NEXT_PUBLIC_E2E_AUTH` appears outside comments. Both named regression tests exist: `demo-stack/tests/test_ant_academy.py:346` and `demo-stack/tests/test_ant_academy_clerk_wiring.py:102`. The cockpit's two cookie paths are real: `cockpit.py:812` (client-side, `_ACADEMY_JS`), `:1496` (`Set-Cookie` on the `/go` 302), `:327` naming both. `stack-verify/live/autoverify.sh:459` is assert *(f) THE ACADEMY RENDERS ITS CATALOG*.

16. **`dependency_map.md:25` — Studio-Room's provider registry and the live `mistralai` import.** Graded at the **nested repo's own ref** `aeec036a`: `services/ai.py:705-708` is the whole `providers` dict (`openai`/`azure`/`anthropic` — no Mistral), `tools/pdf2md.py:24` is `from mistralai import Mistral`, and `git -C stack-demo/app/studio grep -ci mistral aeec036a` sums to **22 lines across 3 files** (`requirements.txt`, `tools/pdf2md.py`, `tools/r3.py`) — the exact figure the cell publishes, and the exact predicate rule 44's worked example warns a host-ref grep would score 0 on.

17. **`architecture_overview.md` remaining externals.** `graphql-wundergraph/terraform/main.tf:20` `= 1` ✓ and its only subnet variable is `private_subnets_ids` (`:31`) with no public subnet anywhere in `*.tf` ✓ (so the `:427` correction holds). `platform/Makefile:10` `PROFILE ?= core` ✓. `next: "~16.2.12"` declared in exactly **four** apps — `web`, `hiring`, `integration`, `maintenance` — with `apps/mobile` declaring none ✓. `ai.go:262-276` (azureClientEu default, `flag_use_azure_us` → azureClientUs), `:129` `isThrottlingError`, applied at `:166` and `:325`, `:85-88` `config.WithRegion("eu-west-1")` ✓. `internal/cms/studio/markdownManager.go:29-30` `mistralocr.New(aiKey)` with the key supplied by the **caller** at `studioManager.go:583` `os.Getenv("MISTRAL_API_KEY")` ✓ — the corrected anchor names the right construct. `STORAGE_RPC_ADDR` @ `9d00a313`: **3 Go hits** (`main.go:451`, `wiring.go:101`, `callsites_test.go:189`), **29 lines across 18 files** repo-wide ✓ — both halves of the Go-scope-vs-repo-wide distinction exact. `63bffc8:terraform/main.tf:38` = `service_desired_count = 0` ✓. `app/cmd/academy-seed/` exists; `academy_chapter_progress.go` / `academy_last_activity.go` exist as **singular** schema files (so the corpus's *"Ent pluralizes; the singular forms are schema-file names"* pre-empts exactly the false-absence a literal table-name grep would produce); `academy.graphqls:694` `upsertChapterProgress`, `:700` `setLastActivity` ✓.

18. **`corpus/services/TEMPLATE.md`** — 46 lines, a pure placeholder scaffold. Every token is a bracketed placeholder or a generic instruction; it asserts **no** platform fact, names no real service, port, table or env var. The one concrete-looking line, `docker compose up -d [service_name]`, carries a placeholder rather than a retired profile token, so it cannot read as a runnable dead command (§5 rule 30). **Nothing to falsify; clean.**

### RE-DERIVED (weakest clearance — recorded as such)

- `dependency_map.md:19` — the storage compose env block: I confirmed the seven listed variables are exactly `:117-123` at `0dab54d` by reading the range, i.e. I re-derived the document's own displayed set. (I did *not* independently enumerate every variable the service could have had from another source.) The cell's own ⚠️ about `:116` being the `environment:` key is correct.
- `dependency_map.md:42` closing sentence — the "seven Go repos on disk" framing: I took the repo list from the clone set rather than deriving it from `repos.yml` history (rule 37 would want the historical vocabulary). The per-library counts themselves are ENUMERATED above.

---

## What I could not settle

- **`architecture_overview.md:387` — "23 schemas carry an `organization_id` with no policy at all (16 is the *neither-mixin* subset of those 23)."** The load-bearing 31/135 above is fully enumerated and correct, but this parenthetical explicitly defers its derivation to `security_compliance.md` (another seat's file, and I did not open it to avoid grading a derivation out of scope). My own crude proxy — files containing the literal string `organization_id` — gives 52 files, 19 with neither mixin, of which only `org_membership.go` declares a `Policy()` → 18 unpoliced-neither-mixin, plus 7 `OrganizationIDMixin{}` users → ~25. That is a *different predicate* from the corpus's (the literal string is absent from mixin-supplied fields and present in index/edge storage keys), so the mismatch is not evidence of an error. **What would settle it:** the exact derivation command `security_compliance.md` § Layer 1 ships, re-run at `ad9f3c49`, with the field-vs-index predicate stated.
- **`ant-academy.md:304` — "1000+ Vitest tests."** 245 `*.test.js(x)` files exist under `code/tests`; the claim counts individual `it`/`test` cases, which cannot be established without executing the suite. Plausible at ~4 cases/file but unverified. (The sibling `~26` e2e figure in the same row *is* measurable and is booked as a minor above.)
- **Archive states on GitHub** — `roadrunner.md:84` (*"not archived"*), `roadrunner.md:66-68` (chronos *"NOT archived, last push 2026-04-23"*), `architecture_overview.md:3` (graphql-wundergraph *"ARCHIVED 2026-07-30"*). Not derivable from any clone; needs the GitHub API. Note the corpus is itself disciplined here — `architecture_overview.md:20` and `roadrunner.md:56` both explicitly decline to assert jobsimulation's archive state and give the four-2026-08-04-commits reason, which I confirmed (`462343b0..origin/main` = 4 commits).
- **`ant-academy.md:90` — "a grid of 65 real cards" / `:221-222`, `:246-250` "proven live on `billion`."** Live-demo observations with no artifact in any clone; unverifiable from a static read.
- **Taxonomy magnitudes** (`architecture_overview.md:13,236`: ≥42,790 skills / ≥22,470 job roles) — a production DB measurement, out of reach here. The corpus correctly flags the "60K / 18K" pair as non-measurements.
- **Production-only infrastructure state** for cms's ECS module: both documents correctly say `infrastructure` has never been in a clone set and assert neither direction. I confirmed the two in-repo facts they cite point opposite ways; I cannot confirm which reflects deployed reality, and neither should.

---

**Result: BLOCKERS=1 · MINORS=6.**

One methodological note for the coordinator, since it bears on how this reading's zero-heavy result should be
weighted: the single blocker is a **corpus-internal** defect (a self-citation naming the wrong construct),
detectable with no ground-truth read at all — §5 rule 21's cheapest class, and the one it says almost nobody
checks. Every platform-facing claim I opened in these six files resolved, including several that are unusually
easy to get wrong (the 29-vs-30 commented mixin, the 21-vs-22 commented handler, the `getenv`-vs-`os.Getenv`
7th env read where **my** instrument was the one that failed, the `915da06` subject-line trap, and the
nested-`studio` `mistral` predicate). That pattern is consistent with the residual in this scope being
concentrated in intra-corpus references rather than in platform drift.
