# Seat D — reading #31

**Ground truth used (re-derived at open, per the ADDENDUM):**
`stack-demo/app` `ad9f3c498` (= origin/main) · `stack-demo/platform` `0c91421` (= origin/main) ·
`stack-demo/sentinel` `f2c4619` (= origin/main) · `stack-demo/next-web-app` `8297c684` (origin/main
`f97ba659`) · `stack-demo/studio-desk` `41ee357` (= origin/main) · `stack-demo/ant-academy` `22df69dd`
(= origin/main) · `stack-demo/storage` `4ce8ece` · `stack-demo/messenger` `fa47850` ·
`stack-demo/cms` `ca50c81` · `stack-demo/graphql-wundergraph` `60c229f` ·
`stack-demo/roadrunner` `87d8d44` · `stack-demo/jobsimulation` `462343b0` ·
nested `stack-demo/app/studio` + `stack-demo/cms/studio` each at their **own** ref `aeec036` ·
rext **pinned per-stack** clone `stack-demo/rosetta-extensions` `09d0607` (origin/main `4cb920a`) ·
rext **authoring** copy `.agentspace/rosetta-extensions` `4304930`.

Every rext claim below was settled at the **pinned per-stack clone** (`09d0607`) — §5 rule 45 — and I
verified that `clerkenstein/`'s tree object is byte-identical between the two clones, so no rext
finding here turns on which tree was read.

## Positive controls

| file | `wc -l` | read in full? |
|---|---|---|
| `corpus/architecture/service_taxonomy.md` | 523 | yes |
| `corpus/services/clerkenstein.md` | 374 | yes |
| `corpus/architecture/security_compliance.md` | 293 | yes |
| `corpus/services/sentinel.md` | 166 | yes |
| `corpus/services/ai-labs.md` | 158 | yes |
| `corpus/services/db-backup.md` | 31 | yes |

Also read in full as instructed: `corpus/ops/platform-alignment.md` §5 (lines 249–1663).

Search-pipeline controls actually run (two of them caught me being wrong mid-pass):
- `git grep "private_subnets_ids = var.platform_private_subnets_ids"` returned **0 across all ten
  terraform trees**. That was a broken pattern, not an absence — terraform-fmt aligns the `=` with
  runs of spaces. Re-run as `private_subnets_ids[[:space:]]*=[[:space:]]*var\.…` it returns 1 per tree.
- `git grep -rn "gpt-5" -- code/src code/app` in `ant-academy` returned **0**, which would have
  produced a false booking against `service_taxonomy.md:301`. The Cosmo client is not under
  `code/src/` at all — it is `code/ucourses/ucourse-engine/assistant/agent.js`, and it does say
  exactly what the corpus says (below).
- NUL-byte census on `messenger @ fa47850d`: **0 NUL bytes in every tracked blob** (measured with
  `tr -dc '\000' | wc -c`, not `grep -c`), 42 `.go` files — so the zero in B2 is not a NUL artifact.
- `git grep "Cookie"` in `clerkenstein/` returns 7 hits (control) while `r.Cookie(` returns 0 —
  the absence claim at `clerkenstein.md:173` is measured, not a dead pipeline.

## Blockers

### B1 — `service_taxonomy.md` names a 3-member private-module set that is really 5, and a sibling corpus file already says 5

- **Anchor:** `corpus/architecture/service_taxonomy.md:175` —
  > "**Shared Libraries** (imported as private Go modules — **not** cloned by `make init`; pulled at
  > Docker build via `GH_PAT`/`GOPRIVATE`). **`ai` is NO LONGER among them** … **the live
  > private-module set a stack builds is colony, proto, taxonomy**."

- **Why it is false:** enumerated from source rather than re-derived from the doc's own arithmetic.
  `app` is a service a stack builds, and `stack-demo/app` @ `ad9f3c49` `go.mod` **lines 14–18** are:

  ```
  14  github.com/anthropos-work/analytics-go v0.3.1
  15  github.com/anthropos-work/colony       v0.35.2
  16  github.com/anthropos-work/proto        v1.210.0
  17  github.com/anthropos-work/storage      v0.15.2
  18  github.com/anthropos-work/taxonomy     v1.2.0
  ```

  All five are in the **direct** `require` block (not `// indirect`), all five are private
  `anthropos-work` repos, none is in `repos.yml` @ `0c91421d`, so all five are pulled at Docker build
  via `GH_PAT`/`GOPRIVATE` — the exact predicate the sentence's own parenthetical states. They are
  really imported, not vestigial: `git -C stack-demo/app grep -c "anthropos-work/storage" HEAD --
  '*.go'` sums to **36 occurrences across 20+ files** (`internal/academy/asset.go`,
  `internal/cms/wiring.go`, `internal/jobsimulation/recording/recording.go`, …), and
  `anthropos-work/analytics-go` is imported by `internal/payments/handler.go` and
  `internal/tracking/handler.go`.

  **And the corpus already contains the correct set, in another file:**
  `corpus/architecture/external_services.md:554` states, verbatim,
  *"`app/go.mod:14-18` requires `analytics-go`, `colony`, `proto`, `storage`, `taxonomy` and nothing
  else"*. So this is simultaneously false against ground truth **and** a self-contradiction — the
  cheapest class (§5 rule 21), and one that a reader configuring `GOPRIVATE`/`GH_PAT` access acts on
  directly.

  Note the neighbouring twin at `service_taxonomy.md:518` (*"**5 libraries, 3 imported by a service a
  stack builds** — colony, proto, taxonomy"*) is **defensible**, because it scopes itself to the five
  named shared libraries; `:175` does not scope itself, it names "the live private-module set". I
  book `:175` only. `corpus/architecture/README.md:21` carries the same correctly-scoped form.

  Secondary consequence worth stating: `analytics-go` appears in the whole corpus in exactly **two**
  places — `platform-migration-status.md:157` (a bare row in the net-new-org-repos table) and the
  `external_services.md:554` sentence above. It is a live, direct, build-time dependency of the
  monolith and the shared-libraries page does not know it exists.

- **Confidence:** high

### B2 — `sentinel.md` publishes the result of a derivation it shows, and the result is wrong (1 vs 0)

- **Anchor:** `corpus/services/sentinel.md:5` —
  > "What survives, and was re-derived: messenger's Go source imports no authorization client
  > (`git grep "authorization\|AUTHORIZATION_ADDRESS" fa47850d -- '*.go' go.mod` **returns one
  > unrelated hit**, against `colony` present as a positive control)"

- **Why it is false:** run exactly as printed, at exactly the ref it names, in
  `stack-demo/messenger` (`fa47850d`), the command returns **zero** hits — `exit=1`, no output.
  Re-run case-insensitively (`-i`): still zero. Re-run without git, streaming every tracked blob at
  that ref through `grep -ain authorization`: still zero. The named positive control passes
  (`git grep -c "anthropos-work/colony" fa47850d -- go.mod` → `go.mod:1`), and the NUL census above
  rules out the mechanism-2 skip, so the zero is a measurement and not a broken pipe. The word
  `authorization` occurs **nowhere in the messenger repo at all**, in any file type.

- **Why I hesitated, stated as the instrument asks:** the *proposition* the parenthetical supports —
  *"messenger's Go source imports no authorization client"* — is TRUE, and the real measurement
  supports it **more** strongly than the published one. The sentence is also mildly ambiguous: "…
  returns one unrelated hit, against `colony` present as a positive control" could conceivably be
  read as the *control* returning one hit (which is exactly true: one `colony` line in `go.mod`).
  Under the natural reading the count belongs to the authorization grep and is wrong. I book it
  because the briefing lists "wrong counts" as a blocker example and resolves doubt upward, and
  because §5 rule 24 is explicit that a passage which *shows its own derivation* is exactly where a
  reader stops auditing — a published derivation whose printed result cannot be reproduced is the
  defect this pass exists to name. A repairer should change "one unrelated hit" to "zero hits", not
  touch the conclusion.

- **Confidence:** low

## Minors

- `corpus/architecture/security_compliance.md:85` and `:116` — both cite `mixin.go:98` for
  `UserMixin`'s `Policy()`. At `app` `ad9f3c49`, `internal/data/ent/schema/mixin.go:98` is the doc
  comment `// Policy defines the privacy policy of the UserMixin.`; the function is at `:99`. The
  sibling anchor in the same blockquote (`mixin.go:126` for `OrganizationMixin.Policy()`) correctly
  names the `func` line, so the file is internally inconsistent about the convention by one line.
  Non-material: `:98` still names the right construct.
- `corpus/services/sentinel.md:15` — *"The sentinel binary's default is 8080 (per its own
  **README**/CLAUDE.md)"*. There is **no `README.md`** in the sentinel repo at `f2c46190`
  (`git ls-tree -r HEAD --name-only | grep -i readme` → empty; `CLAUDE.md` is present and does say
  `PORT … 8080`). The substantive claim is right and is confirmed in code at
  `sentinel/cmd/root.go:47` (`port := cmp.Or(os.Getenv("PORT"), "8080")`); only the named source is
  half-imaginary.
- `corpus/architecture/service_taxonomy.md:85` — Backend/App *"Port(s)"* reads
  `8081-8083 (container: HTTP 8082, RPC 8083, meta 8084)`. Under the compose environment actually
  shipped (`docker-compose.yml:65` `PORT=8082`, `:69` `RPC_PORT=8083`, `:64` `META_PORT=8084`;
  published `:41-43` = `8081:8081`, `8082:8082`, `8083:8083`) the **meta port 8084 is not published
  at all**, and published `8081` has nothing listening on it. The cell reads as though the range
  covers the three named roles; it does not.
- `corpus/architecture/security_compliance.md:23-25` — *"Re-derived across **all eight** service
  terraform trees in the clone set"*, enumerating the eight Go services. The clone set actually
  contains **ten** `terraform/` trees; `studio-desk` is a ninth ECS-deployed service whose module
  passes the identical `private_subnets_ids = var.platform_private_subnets_ids`
  (`studio-desk/terraform/main.tf:33`), and `next-web-app` carries a tenth (Vercel, no ECS module).
  The set is under-enumerated. The **conclusion is unaffected and in fact reinforced**: I measured
  `public_subnet` at **0 occurrences across every clone in the tree**, nested repos included.
- `corpus/services/db-backup.md` (whole file) — the `db-backup` repo is in no clone set, so **not one
  claim on this page is gradeable from ground truth**. Nothing on it is contradicted by anything I
  read; it also carries no `file:line` citation, no ref, and no derivation. Recording it as a
  measurement gap rather than a defect (see *What I could not settle*).

## Positively cleared (audited zeros)

**ENUMERATED** (predicate re-derived from source, cardinality compared before any arithmetic):

- **`security_compliance.md:79-147`, the whole multi-tenancy fence** — the one §5 rule 24 says has
  now been wrong four times. Re-derived at `app` `ad9f3c49` (the ref the passage names), set-first:
  `internal/data/ent/schema/` holds **139 `.go` files**, **135** declare `ent.Schema`, and the four
  that do not are exactly `database_types.go`, `mixin.go`, `skiller_mixins.go`, `skillpath_mixins.go`.
  `OrganizationMixin{}` is **mentioned in 30 files and used in 29** — `user_resource.go:22` is
  `// OrganizationMixin{},  // We need to work on this`, verified by opening the file, not by
  `grep -c`. `OrganizationIDMixin{}` is used by exactly the seven named (`category`, `jobrole`,
  `similarity`, `skill`, `specialization`, `studio_document`, `studio_task`). The neither-mixin ∧
  `"organization_id"` set is **exactly 18**, and after removing `org_membership.go` (own `Policy()`)
  and `academy_feedback.go` (`UserMixin{}` at `:64`) the residue is **exactly the 16 files the
  document lists, name for name, with no extras in either direction**. I then tested the *predicate*
  rather than the substring on five of the sixteen: each is a real `field.UUID/String("organization_id", …)`
  declaration, not an index or `StorageKey` mention. Policy-declaring files at both `ad9f3c49` **and**
  the pinned `5ba17044`: exactly four (`mixin.go`, `org_membership.go`, `organization.go`, `user.go`),
  five declarations — the passage's "only FOUR files" is right at the ref it names. `Organization`
  uses neither mixin and declares `Policy()` at `organization.go:56` with
  `rule.FilterSameOrganizations()` at `:96`; `Membership` at `org_membership.go:172-188` ends in
  `privacy.AlwaysDenyRule()`. So **29 + Membership + Organization = 31**, **16 + 7 = 23**,
  **135 − 31 − 23 = 81**, and the four tenant-scoped members of the 81 resolve
  (`lab.go:63`, `academy_chapter.go:84`, `academy_skill_path.go:78` = `field.String("tenant_eid")`;
  `skill_path_session.go:43` = `field.UUID("tenant_id", …)`, and it carries `UserMixin{}` at `:26`,
  no org mixin — exactly as claimed). The fence is correct on the set, not just the sum.
- **`sentinel.md:26` Casbin model shape** — `internal/authorization/casbin.go:13-45` declares exactly
  **6 request definitions** (`r`,`r2`…`r6`), **6 policy definitions** (`p`,`p2`…`p6`), **3 role
  definitions** (`g = _,_` / `g2 = _,_,_` / `g3 = _,_`) and **6 matchers** (`m`,`m2`…`m6`). Every row
  of the matcher table matches its matcher's actual body, including `m6`'s `g3(p6.org, r6.sub)` and
  `m5`'s `keyMatch`. `g2`'s four roles are the four `MembershipRole` constants at
  `app/internal/data/ent/enum/membership.go:8-15`. `manager` occurs **0 times** in `init_policy.sql`.
- **`service_taxonomy.md:71-135` + `:489-506`, the compose topology** — `docker-compose.yml` @
  `0c91421` declares exactly **five** services (`sentinel:5`, `backend:28`, `studio-desk:112`,
  `next-web-app:143`, `gotenberg:170`); exactly **four** carry a `profiles:` key (`:110`, `:141`,
  `:168`, `:183`) and `sentinel` carries none; `common.yml` adds `postgresql:2` + `redis:24`, neither
  with a `profiles:` key → floor = 3, effective topology = 7, `core` = 5 containers. The legal token
  set really is `{all, backend, core, frontend, studio-desk}` and nothing else. `Makefile:10` is
  `PROFILE ?= core`. `repos.yml` lists four repos; `app` at `:14-17` is the only `migrations: true`;
  `sentinel` at `:18-20` is `migrations: false` while `docker-compose.yml:18` still gives it
  `search_path=sentinel`.
- **`service_taxonomy.md:271-277`, `gen.py`'s argument set** — `app/studio` @ its **own** ref
  `aeec036a` registers exactly **nine** `add_argument` calls at `gen.py:484-492`, and they are exactly
  the nine listed; `parse_argument` uses `parse_known_args` at `:19`. The repo's own
  `studio/CLAUDE.md:13` example passes `--evaluation_skills`, which is **not** one of the nine — the
  silent-swallow claim demonstrates itself.
- **`service_taxonomy.md:247`, the Mistral/OCR claim** — graded at the nested repo's own ref, per
  ADDENDUM mechanism 3. `git -C stack-demo/app/studio grep -ci mistral aeec036a` sums to **22 lines
  across 3 files** (`requirements.txt`, `tools/pdf2md.py`, `tools/r3.py`) — the published figure
  exactly. `services/ai.py:704-709` is the whole provider registry (`openai`/`azure`/`anthropic`);
  `ai.py:1-2` imports only `openai`/`anthropic`; the only `from mistralai import Mistral` is
  `tools/pdf2md.py:24` (`r3.py` only reads `MISTRAL_API_KEY` from env, it does not import).
- **`clerkenstein.md:41-44, 71-79`, the five DNA gene counts** — parsed each DNA at the pinned rext
  clone and counted variants, rather than trusting the doc:
  `clerk-2.6.0` **14 capabilities / 27 genes**, `clerk-js-5` 6/**9**, `clerk-multi-1` 5/**9**,
  `clerk-deploy-1` 3/**7**, `clerk-express-1` **5 capabilities / 13 genes**. All five match.
  `gate.sh:31-32` defaults are `GATE_OVERALL 95` / `GATE_CRITICAL 100`, matching *"≥95 / =100"*.
- **`clerkenstein.md:145-147`, the declared DNA gap** — `clerk-js-5.json` has a `Me` capability
  (`universal-user`, `unauthenticated`) and `organization_memberships` occurs **0 times** in that
  DNA. The gap is real and correctly declared.
- **`service_taxonomy.md:518`, `authn` is a dependency of nothing** — `anthropos-work/authn` returns
  **0** across `go.mod`+`go.sum` of **all seven** Go clones (`app`, `sentinel`, `storage`,
  `messenger`, `cms`, `jobsimulation`, `roadrunner`), with the stated control (`colony` present in
  all seven) passing. The `ai` module is likewise absent from `app` and `sentinel` and present only
  in the frozen `cms`/`jobsimulation` husks at `v1.40.2`.
- **`ai-labs.md:17-19`, the three "not built" negatives** — `checkout.session.completed` is **0
  occurrences** in `app` Go source (control: `customer.subscription` returns hits);
  `internal/labs/**` imports `internal/credits` **0 times** (control: it imports other
  `anthropos-work/app/internal` packages); `internal/web/backend/credits/handler.go:12` says the
  `POST /credits/purchase` route was removed in Wave 13. The Stripe webhook dispatch
  (`internal/web/backend/api/api.go:317-394`) handles `customer.created` +
  `customer.subscription.{created,updated,deleted}` and **nothing else**.

**RE-DERIVED** (I recomputed arithmetic the document displays — the weakest clearance, labelled as
such): `135 − 31 − 23 = 81`; `29 + 2 = 31`; `16 + 7 = 23`; the `5 → 4 → 3 → 1` subgraph ladder's
commit-by-commit deltas.

**Anchor-and-construct checks that resolved to the right construct** (opened, read ±15 lines):

- `service_taxonomy.md`: `studio-desk/.env.example:44,45` @ `41ee3575`; `app` @ `ad9f3c49`
  `cms_queries.graphqls:106,107` and `cms_mutations.graphqls:22` (the `archiveStudioTask` *mutation*
  — the corrected file is the right one); `studioManager.go:119` runs `studio/gen.py` via
  `runCommand`, whose "NEVER through a shell" contract is at `:1096-1098`; `env_guards.go:37-44` +
  `:92-111`; `main.go:394`; `3e5bc33ef:main.go:387`; `1e457fa70` is *"refactor(ai): fold the ai
  library into app as internal/ai"*, 2026-08-04; `academy.graphqls` is 1 of **43** files in that
  schemas dir; `graphql-wundergraph` `terraform/locals.tf:8`, `terraform/main.tf:20/:48-49`,
  `config.prod.yaml:5`, and all three supergraph configs carry a single `- name: backend`;
  `915da06` deletes `cms.graphqls` **and** `jobsimulation.graphqls` in one commit;
  `jobsimulation/terraform/main.tf:15-40` **at the ref the cell names (`6092c6d2`)** is precisely the
  M810 decommission comment block, and the module really is deleted there — while at the checkout
  `462343b0` (which the cell does not name) the module still stands, so the pin is load-bearing and
  correct; `cms/terraform/main.tf:39` is `service_desired_count = 0` in a 191-line file and `6efa1d5`
  is the workflow deletion it describes; `roadrunner/terraform/main.tf:19` is `= 1`; the pre-`2adcf71`
  compose really did map `"5050:8080"`; `d11a403` moved exactly **two** values on the messenger block
  and at `d11a403^` the other two already read `http://backend:8083`.
- `ant-academy` @ `22df69dd`: `code/src/graphql/server.js:14,18` (throws when unset);
  `backendContent.js:36,102-103`; `serverTenant.js:115-145` (the "NO FS-as-published fallback …
  not reversible-on-error" text is at `:129`/`:132`, `:145` is the `?? emptyCatalogView()`);
  `beacon/route.js:36,41-55`; `layout.jsx:132` `manifest: "/academy-manifest.json"`;
  `next-scaffold.test.js:106,111`; `react-compiler-config.test.js:41`; next `^16.2.6` / react
  `^19.2.5`; `dev": "next dev --port 3077"`; expo `~54.0.33`; mobile `:8555`. And the Cosmo claim,
  which I initially mis-measured: `code/ucourses/ucourse-engine/assistant/agent.js:12` is
  `const API_URL = 'https://api.openai.com/v1/responses'`, `:13` is `const MODEL = 'gpt-5.2'`, `:32`
  reads `localStorage.getItem('openai_api_key')` — **Responses API, gpt-5.2, per-user localStorage
  key, from the browser**, exactly as `service_taxonomy.md:301` says.
- `security_compliance.md`: `ai.go:85-88` (`config.WithRegion("eu-west-1")`), `:262-266`,
  `:263-277` (`flag_use_azure_us`), `isThrottlingError` at `:129`/`:166`/`:325`;
  `coursebuilder/bedrock.go:100` + `:109-112`; `skills.go:53-64` counts booleans and `:75` divides;
  `criterion.go:127` is the hardcoded `case check.EngineLlm:` in the dispatch switch, `:168`
  dispatches `validateCodeDiff`, `:428` is `NewLLMBulkChecker`, `:450-475` sets `success` from a pure
  string comparison; `checkValidationBulk.tmpl:27` really does ask a model to *"assess whether the
  `<asset>` … meets or does not meet"*; `checkerEngines` is written at `criterion.go:34/:72/:90` and
  **read nowhere** — the doc is right to say "cite the DISPATCH, not that map", and
  `validator.go:60-62` registers exactly one engine; all three `app/studio/configs/*.ini` pin `azure`
  on every `*_AI_*_MODEL` line; `useOrgSelection.tsx:94`, `useResolveActiveOrg.tsx:107`,
  `useActivateMembershipOrg.tsx:81` hold **in both `apps/web` and `apps/hiring`** at those exact
  lines. `README.md:21` resolves relative to `corpus/architecture/` and does carry the cost-tracking
  clause. The intra-corpus anchors `external_services.md:579`, `:602-607`, `:567` and
  `coursebuilder.md:48` all land on the sentences they are cited for.
- `sentinel.md`: `docker-compose.yml:48` is the **only** `AUTHORIZATION_ADDRESS` across
  `docker-compose.yml` + `common.yml` + `.env_example`, at **both** `0c91421` and the pinned
  `0dab54d`; `messenger` survives in those files only as comments; `go.mod:3` `go 1.26.0`;
  `Dockerfile:2` and `Dockerfile.dev:2` `golang:1.26-bookworm`; `terraform/locals.tf:4-5` = 256/128;
  `init_policy.sql:63-66` is the taxonomy-write omission note and `:88-118` the content_creator
  block; `local_superadmin_grants.sql` grants `p3 taxonomy:write` to `admin` and keeps the `p4` block
  commented out; `Makefile:3` hard-codes the DSN; all 19 RPC methods in the table exist in
  `internal/rpcsrv/rpc.go`; `app/internal/converter/gotenberg.go:31` is the consumption site;
  `architecture_overview.md:335` opens the block whose `:339` carries the quoted phrase.
- `clerkenstein.md`: `alignment/cmd/alignctl/run.go:134-135` (`ExitRegressed = 2`,
  `ExitUnmeasurable = 3`); `clerk-backend/store.go:138` (`SeedOrgIdentity`) and `:151`
  (`LookupOrgEid`); `alignment/dna/clerk-2.6.0.json:131` carries the quoted *"M219 landed the fix …
  97.2% -> 100%"* text; `clerk-frontend/server.go:35-67` (disk-first bundle, `clerkJSFetchTimeout =
  15 * time.Second`, *"Explicitly NOT http.DefaultClient"*, `FAKE_FAPI_CLERKJS_CACHE`);
  `registry.go` holds one `activeKey` with `active()`/`Select()`; `server.go` holds one `signedIn`,
  one `signedOut`, one `clientID` (`"client_clerkenstein"`, `:125`) and one `sessID`
  (`"sess_clerkenstein"`, minted at `:665`); `handleSelectIdentity` drops the session (`:646-647`)
  and clears rather than sets `signedOut` (`:654`); all five named `server_test.go` tests exist;
  `meorgmemberships_test.go` exists; `clerkenstein/go.mod` pins `colony v0.34.3` +
  `clerk-sdk-go/v2 v2.6.0` while `app/go.mod` is on `colony v0.35.2` + `clerk-sdk-go/v2 v2.7.0` and
  `sentinel/go.mod:8` is `colony v0.35.2` (taken there by `88036d7`, 2026-08-03) — so the
  "`sentinel` has moved too" retraction is correct and `storage` alone is still on `v0.34.3`;
  `@clerk/express ^1.3.47`; `studio-desk` `src/index.ts:96` and `app/services/userService.ts:16`
  both carry the four-element `STUDIO_ACCESS_ROLES`; the rext `alignment/` section has **no**
  `scripts/` dir and `gate.sh:29-30` resolves `ALIGN_DIR` as `<clerkenstein/alignment>/../../alignment`;
  `clerkenstein/.github/workflows/alignment.yml` is tracked below the repo root (hence inert);
  the org-name/org-slug/`org_is_hiring` thread exists on both producer
  (`stack-seeding/seeders/roster.go:53,77,140,155`) and consumer
  (`clerk-frontend/registry.go:125,144,162`, `resources.go:50-51,265-276`).
- `ai-labs.md`: `git describe --tags ad9f3c49` → `v1.369.0-7-gad9f3c498` and the newest tag really is
  `v1.369.0`; `2035f9a4` → `v1.369.0-2-g2035f9a40`; every named Ent schema
  (`lab.go`, `lab_body.go`, `lab_session.go`, `organization_credit.go`, `credit_transaction.go`)
  and every named migration exists with the exact filename
  (`20260617203555_add_labs_catalog.sql`, `20260529072659_add_lab_session.sql`,
  `20260617120000_add_lab_session_model.sql`, `20260717151144.sql`,
  `20260626120000_add_org_subscriptions.sql`); `lab_session.go` carries **no `PrimaryKeyMixin`** and
  declares `field.String("id")` at `:36` with the six-value status enum at `:86-88`; `lab_body.go`
  is EN base + exactly six overlays (`de/es/fr/it/nl/pt`); `labsapi/client.go:6` default `:7070` and
  `main.go:855` reads `LABS_API_PLATFORM_TOKEN`; `spend_reconciler.go:60` `Interval: 30 * time.Second`;
  `labs_budget.go:26-28` tiers and `:247-251` the five `CapReason` values;
  `stripe-go/v74 v74.30.0`; `labs.graphqls` carries every named query/mutation;
  `main.go:1338` wraps `LabSessionService` in `authn.HTTPAuthnMiddleware`;
  `labs_admin.go:31,40` mount `/v1/labs` behind `apiKeyAuthMiddleware(apiKeys, "labs:write")` and
  `GET /v1/labs/:slug/workspace.tar.gz`; `cmd/labsdemo/main.go:115` `:8099`; `stripe/` holds
  `setup.sh`, `setup-fixtures.json`, `teardown.sh`, `tests/stripe-test.sh`; the four named credits
  test files are exactly the four `_test.go` files in `internal/credits/`.
  **The one I nearly booked and then refuted by reading around the line** (§5 rule 10):
  `cost.go`'s *doc comment* at `:29` says *"course.refine → 5 credits per refine turn"*, which
  contradicts the corpus's `course.refine`=**1**. The corpus is right and the source's comment is the
  stale one — the live `creditCost` map at `:86-90` is `{build: 5, refine: 1, translate: 1}` with the
  inline note *"D1 ruling: flat 1 credit per refine turn"*, `PricePerCreditUSD = 0.45` at `:142`,
  `MarginMarkup = 1.40` at `:133`, `DefaultSeedBalance = 500` at `:231`, packages 50/200/500 at
  `:201-203`.

## What I could not settle

- **`ai-labs.md:16`** — *"a knowledge-plan release (`app` `knowledge/plan/releases/06.00-shared-purse/`,
  milestones M600–M607, all planned)"*. I did **not** open, list or grep it. HARD BAR 1 forbids
  reading anything under `knowledge/plan/**`, and although the prohibition's stated *reason* (prior
  audits' answer keys) is about this repo, the path glob also matches
  `stack-demo/app/knowledge/plan/**` and I would not risk it on an ambiguity. Settled by: anyone
  outside a reading window running `git -C stack-demo/app ls-tree HEAD knowledge/plan/releases/`.
  Every *measurable* half of the same bullet (the three "not built" negatives) I did verify — see
  the cleared list.
- **`ai-labs.md:5`** — *"`anthropos-work/AI-Labs` is the live Go control plane"*. That repo is in no
  clone set and `gh` is not installed on this host, so its existence, language and liveness are not
  observable from here. The `app`-side consumer half is fully consistent with the description
  (`labsapi/client.go` speaks HTTP to it and comments that **labs-api owns session-ID generation**).
- **GitHub archive state** — `service_taxonomy.md:166-168` (Skiller / Skillpath / Roadrunner
  `ARCHIVED <date>`), `:3` and `:403` (graphql-wundergraph archived 2026-07-30). Archive state lives
  in the org API, not in git objects; the file says so itself. The one falsifiable half I *could*
  check is consistent: `graphql-wundergraph`'s newest commit is `60c229f`, dated **2026-07-30**, with
  nothing after. (The `Jobsimulation` row's own retraction — four post-dated 2026-08-04 commits —
  reproduces exactly: `6092c6d2`, `caf36c96`, `1e40d184`, `82cb66ec`.)
- **Taxonomy figures** — `service_taxonomy.md:183` *"≥42,790 skills / ≥22,470 job roles"* needs a
  production DB read (`/db-query`), not a clone.
- **`corpus/services/db-backup.md` in its entirety** (31 lines) — the `db-backup` repo is in no clone
  set. Six-hour schedule, three targets, "Go", "all schemas", ECS-vs-Docker deployment: none is
  gradeable here. It is *consistent* with `security_compliance.md:167,174`, `service_taxonomy.md:173`
  and root `CLAUDE.md`, which is the most that can be said. Settled by adding the repo to a clone set
  or reading `infrastructure`.
- **`security_compliance.md` infrastructure/compliance surface** — VPC CIDR, NACL/SG posture, TLS 1.3
  cipher list, KMS/CloudTrail, the RDS/DR topology, DPA v1.4, "18 approved sub-processors", the 90-day
  retention numbers. All of this lives in `infrastructure`, which has never been in a clone set. The
  one bullet in that region that *is* falsifiable from clones — the Cosmo Router's subnet placement —
  I did re-derive, and it holds (see Minors for the set-size nit).
- **`clerkenstein.md` alignment scores as *scores*** — I enumerated the gene/capability counts and
  read the DNA text, but I did not execute `alignctl`, so *"100% / 100%"* per surface, and the
  `clerk-express-1` `rc=3` behaviour on a box without `@clerk/express`, are unrun. Note that the
  doc's own standing caveat at `:274-283` (re-run `deployrun` against `colony v0.35.2` before quoting
  7/7 as current) is **correct and still open**: I confirmed the artifact is pinned at `v0.34.3` while
  both live Go services are on `v0.35.2`.
- **`clerkenstein.md:124-125`** — the *"~4.05 s 3-attempt retry ladder"* and the latency numbers at
  `:312-316` are historical measurements on a live stack; not reproducible from a static tree.
