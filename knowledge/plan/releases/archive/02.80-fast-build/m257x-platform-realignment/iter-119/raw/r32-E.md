# Seat E — reading #32

**Ground truth re-verified at this reading's open** (all match the briefing ADDENDUM):
`app ad9f3c49` · `platform 0c91421d` · `next-web-app 8297c684` · `sentinel f2c46190` ·
`studio-desk 41ee3575` · `ant-academy 22df69dd` · `storage 4ce8ece5` · `messenger fa47850d` ·
`cms ca50c817` · `graphql-wundergraph 60c229f3` · `roadrunner 87d8d443` · `jobsimulation 462343b0` ·
nested `app/studio` + `cms/studio` `aeec036a` · rext **pinned per-stack** `stack-demo/rosetta-extensions 09d06070`
(authoring copy `.agentspace/rosetta-extensions 43049308` — read only where noted).
Every rext claim below was settled against the **pinned per-stack clone** (§5 rule 45).

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/services/studio-room.md` | 512 | yes |
| `corpus/services/backend.md` | 396 | yes |
| `corpus/services/graphql-wundergraph.md` | 280 | yes |
| `corpus/services/messenger.md` | 207 | yes |
| `corpus/architecture/frontend_architecture.md` | 116 | yes |
| `corpus/architecture/README.md` | 38 | yes |

(`corpus/ops/platform-alignment.md` §5 read in full as instructed — 2803 lines, §5 spans `:249-1406`.)

---

## Blockers

### B1 — `packages/graphql` does NOT contain generated hooks; the same file says so 23 lines later

- **Anchor:** `corpus/architecture/frontend_architecture.md:32` —
  > `| **GraphQL** | `packages/graphql` | Shared GraphQL definitions, **generated hooks**, and types. Populated by `pnpm codegen` against the supergraph — which is now the single `backend` subgraph, served locally by `backend` itself. |`

- **Why it is false:**
  - `next-web-app@8297c684:packages/graphql/codegen.ts` declares `preset: 'client'` with
    `generates: { './src/__generated__/': … }`. The **client-preset emits typed documents, not hooks.**
  - Enumerated the whole generated tree: `git ls-files packages/graphql/src/__generated__` returns
    **exactly four files** — `fragment-masking.ts`, `gql.ts`, `graphql.ts`, `index.ts`.
    `grep -rn 'useQuery\|useMutation\|useSuspenseQuery' packages/graphql/src/__generated__/ | wc -l`
    → **0**.
  - **Positive control in the same pass:** the same pattern over `packages/graphql/src/hooks/` matches
    **215** files (`useAcademyCompletedCourses.tsx`, … — all tracked, hand-authored, organised by domain:
    `academy/`, `aiReadiness/`, `aiSimulation/`, …). So the pipeline is not broken; the hooks simply are
    not generated.
  - **Self-contradiction inside the same document:** `frontend_architecture.md:55` states
    *"emits typed GraphQL documents into `packages/graphql/src/__generated__` via the client-preset
    (documents sourced from `src/query/**`). **React Query hooks are hand-authored on top of these typed
    documents.**"* The two sentences cannot both be true, and `:55` is the one that matches source.
  - The consequence for a reader doing real work is concrete and wrong-direction: a developer adding a
    query would run `pnpm codegen` and wait for a hook that will never appear, instead of hand-writing one
    in `packages/graphql/src/hooks/<domain>/`.
- **Confidence:** high

### B2 — LabSession is the SIXTH handler on `app`'s mux, not the third, and the same file already says six

- **Anchor:** `corpus/services/backend.md:314` —
  > `**AI Labs LabSession** (Phase B PR 2, #896): Connect-RPC `lab.v1.LabSessionService` (…) plus a new `lab_sessions` Ent table — … **Registered as a third RPC handler in `main.go` after Users and Organizations.**`

- **Why it is false:** the passage names no ref, so it is graded at the checkout `app ad9f3c49`
  (briefing §"Which ref settles an `app` claim"). I read `main.go:1295-1338` in full there. The mux
  registrations, in order:

  | # | handler | `ad9f3c49` | `b948604f` (the demo pin the file cites elsewhere) |
  |---|---|---|---|
  | 1 | `UsersService` | `main.go:1297` | `:1187` |
  | 2 | `OrganizationsService` | `:1298` | `:1188` |
  | 3 | `SkillerService` | `:1306` | `:1196` |
  | 4 | `JobSimulationService` | `:1314` | `:1204` |
  | 5 | `CMSService` (conditional, `if cmsRPCServer != nil`) | `:1322-1324` | `:1212-1214` |
  | 6 | `lab.v1.LabSessionService` | `:1338` | `:1228` |

  `lab.v1.LabSessionService` is **sixth**, and **three** handlers (Skiller, JobSimulation, CMS) sit
  between it and `OrganizationsService`, so *"a third RPC handler … after Users and Organizations"* is
  wrong on both the ordinal and the adjacency. The shape is identical at `b948604f`, so no reachable ref
  rescues it.
- **It also contradicts two passages of its own file**, both of which are correct:
  `backend.md:30` — *"The mux carries **six** handlers in total (`main.go:1297-1338` @ `app` `ad9f3c49`;
  `:1185-1228` @ the demo pin `b948604f`)"* — and `backend.md:106`, which enumerates the same six with
  LabSession **last** at `:1228`. This is the cheapest finding class in §5 rule 21 (the corpus
  contradicting itself, no ground-truth read required) and it survived anyway.
- **Confidence:** high

### B3 — the historical `NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT` anchors name the BUILD ARG where the sentence means the runtime env

- **Anchor:** `corpus/services/graphql-wundergraph.md:174-178` —
  > `(`docker-compose.yml:135` studio-desk's `VITE_GRAPHQL_ENDPOINT`, `:160` next-web-app's `NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT` — **each also baked as a build arg, at `:119` and `:151`**), … Those line numbers move on every compose clean-up — **they were `:220`/`:236` at `0dab54d` and `:334`/`:352` at `2adcf71`** — so grade the construct, not the offset.`

- **Why it is false:** the sentence explicitly separates two constructs — the **runtime-env** pair
  (`:135` / `:160`) and the **build-arg** pair (`:119` / `:151`) — and then says *"those line numbers"*
  (the runtime-env pair) *"were `:220`/`:236` … and `:334`/`:352`"*. Measured in
  `stack-demo/platform` with `git show <ref>:docker-compose.yml | grep -n`:

  | ref | `VITE_GRAPHQL_ENDPOINT` build arg / **env** | `NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT` build arg / **env** | doc's pair |
  |---|---|---|---|
  | `0dab54d` | `:204` / **`:220`** | `:236` / **`:245`** | `:220` ✓ env, `:236` ✗ **build arg** |
  | `2adcf71` | `:318` / **`:334`** | `:352` / **`:361`** | `:334` ✓ env, `:352` ✗ **build arg** |
  | `0c91421` (HEAD) | `:119` / **`:135`** | `:151` / **`:160`** | — (correct) |

  So each historical "pair" is **one env line plus one build-arg line** — it is neither the env pair
  (`:220`/`:245`, `:334`/`:361`) nor the build-arg pair (`:204`/`:236`, `:318`/`:352`). The studio-desk
  half is right at both refs; only the next-web half slips construct. This is §5 rule 21's second class —
  *an anchor that resolves but names the wrong construct* — and a line-existence check can never catch it,
  because both candidate lines contain the same env-var name.
- **Why I hesitated (and booked it anyway):** a reader chasing `:236` at `0dab54d` still lands on a
  `NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT` line, and the clause's own moral is *"grade the construct, not the
  offset"* — the damage is bounded. But the passage is a worked example whose entire subject is
  construct-vs-offset discipline, and it gets the construct wrong in the very demonstration; under the
  briefing's "when in doubt, book it as a BLOCKER" this is a BLOCKER.
- **Confidence:** medium

---

## Minors

- `corpus/services/backend.md:19` — *"compose now declares **five** services"* carries no *"seven
  effective"* qualifier, which **four sibling corpus sites do** (`service_taxonomy.md:72`,
  `external_services.md:305`, `jobsimulation.md:56-57`, `cms.md:58`, all phrased *"five … (seven
  effective, once `include: common.yml` adds the `postgresql`/`redis` floor)"*). Measured: five service
  keys in `docker-compose.yml` (`sentinel:5`, `backend:28`, `studio-desk:112`, `next-web-app:143`,
  `gotenberg:170`) + two in `common.yml` (`postgresql`, `redis`) = seven in the effective project.
  §5 rule 30 makes resolving `include:` load-bearing rather than tidy, so the bare "five" is the one
  phrasing in the family that a parser-minded reader can misapply.
- `corpus/services/backend.md:62` — *"The correctly-scoped model wording is
  [`architecture_overview.md:335`]"*. `:335` is the heading line
  *"**On a local stack** (platform `2adcf71` deleted the router — **there is no `:5050`**):"*; the
  correctly-scoped wording it means is `:338` (*"→ Connect-RPC to sentinel (the only cross-process RPC
  edge out of backend on a core stack)"*). Same block, off by three lines.
- `corpus/architecture/frontend_architecture.md:39` — *"`:5050` was **only ever** the deleted LOCAL
  compose host mapping, never a production port"*. The load-bearing half is TRUE
  (`graphql-wundergraph@60c229f3:config.prod.yaml:5` `listen_addr: 0.0.0.0:8080`,
  `terraform/main.tf` port 8080). But *"only ever"* over-reaches: `graphql-wundergraph@60c229f3:Makefile:10`
  still reads `docker run -it --rm -p 5050:8080 wundergraph-router` for `make run` — a second, live,
  non-compose 5050 host mapping. `graphql-wundergraph.md:84-85` scopes the same claim correctly
  (*"`grep -c 5050 docker-compose.yml` returns 0"*, which I re-ran: **0**).
- `corpus/architecture/frontend_architecture.md:29-35` — the "Core Packages" table omits
  `packages/design` (`@anthropos/design`, a real tracked workspace package under `packages/*` in
  `pnpm-workspace.yaml`). Omitted list member; the four rows present are all correct.
- `corpus/services/graphql-wundergraph.md:64-66` — *"the `Subscription` substring hits in that SDL are
  Stripe/plan **field names** — `activeSubscription`, `stripeSubscriptionId`, `type PlanSubscription`"*.
  The third is a **type**, not a field name (`schemas/backend.graphqls:1555` `type PlanSubscription {`).
  The predicate it supports (no `type Subscription`) is TRUE and I enumerated it: 6 substring hits,
  0 of them a `Subscription` root type.
- `corpus/services/studio-room.md:317` — *"the in-code fallback is a literal `worklog/` only if unset"*.
  The fallback is `worklog` with **no trailing slash**:
  `app/studio@aeec036a:gen.py:452` `path = sim_request.get(path_key, path_key.replace('_path', ''))`.
- `corpus/services/studio-room.md:351-362` — the `requirements.txt` excerpt reorders the file
  (`mistralai` shown third; it is line 8, after `jinja2` and a blank). The separately-cited
  `requirements.txt:8` is exact, so no consequence.

---

## Positively cleared (audited zeros)

**ENUMERATED** (predicate enumerated from source independently, cardinality stated before arithmetic):

1. **`frontend_architecture.md:39-50`, the whole REST/SSE table.** Re-derived the SET, not the sum, at
   `next-web-app 8297c684` over non-test `.ts`/`.tsx` with `e2e/` excluded:
   **22 files** naming `NEXT_PUBLIC_BACKEND_API_URL` (I listed all 22 and matched them one-for-one against
   the prose enumeration at `:39` — 4 core-js + 3 AI-readiness + invite + reminder-unsubscribe +
   MemberAnalyticsContainer + 4 assignment (×2 apps) + 3 Stripe + CSV bulk import + résumé import +
   TestRunNetwork + 2 admin backfill = 22), **31 occurrences**, **47 `fetch(` sites** in those files,
   **12 env / 27 calls** for the four `packages/core-js` clients (3/20, 3/3, 3/3, 3/1).
   `new EventSource(` = **0** tree-wide. `packages/core-js/src/talkToData/api.ts:214` is byte-exact.
2. **`graphql-wundergraph.md:41-53`, the subgraph ladder.** Enumerated `^  - name:` in
   `supergraph-config-prod.yaml` at each named commit: `749dc86^` = **5** (backend, skiller,
   jobsimulation, cms, skillpath), `749dc86` (2026-06-24) = **4**, `7c17e63` (2026-07-21) = **3**,
   `915da06` (2026-07-29) = **1**. `git show --name-status 915da06` marks **both**
   `schemas/cms.graphqls` and `schemas/jobsimulation.graphqls` `D`, and that commit's own subject line
   does read *"supergraph 2→1"* — so the doc's "3 → 1, and the subject line is wrong" is exactly right.
3. **The five compose profile tokens** (`graphql-wundergraph.md:15`): `grep -n 'profiles:'` over
   `docker-compose.yml` **and** `common.yml` (which declares none) → `core`, `backend`, `all`,
   `studio-desk`, `frontend`. `Makefile:10` = `PROFILE ?= core`. `grep -c 5050 docker-compose.yml` = 0.
   `repos.yml` = **4** entries at `0c91421` (app, sentinel, next-web-app, studio-desk), **9** at `2adcf71`.
4. **`messenger.md:17`, the 21/22 handler split.** `sed -n '72,104p' internal/flow/flow.go @ fa47850d`
   → **22** `pubsub.EventHandler(` lines, of which exactly one is commented out
   (`// pubsub.EventHandler(h.OrgJobSimulationAssignmentPastDueHandler), // not implemented`) → 21 live.
   §5 rule 24's "grep -c counts commented-out code" is what the doc is already accounting for.
5. **`messenger.md:20-27`, the `MESSENGER_RPC_ADDR` absence.** Ran the three-instrument recipe over
   **all 16 git trees** (13 stack-demo clones + the two nested `studio` repos + the rext authoring copy),
   each at its own HEAD: **0 everywhere**. Platform `git log -S 'MESSENGER_RPC' 0c91421d` = **0** over a
   **121**-commit history; positive control `-S 'SKILLER_RPC'` = **7**; `--all` → **8** and **0**, and the
   8th (`464dfe3`) is confirmed present in `--all` and absent from the `0c91421d` ancestry.
6. **`backend.md:157-171`, the `.tf` denominators and the 6-occurrence count.** Tracked `.tf` per clone
   summed to **44** (app 5, cms 4, graphql-wundergraph 4, jobsimulation 6, messenger 4, next-web-app 2,
   roadrunner 4, rosetta-extensions 2, sentinel 4, storage 5, studio-desk 4); filesystem `.tf` = **59**.
   `backend.internal.anthropos:8081` = **6** across the clone set — **1** in `app`
   (`knowledge/service-dependencies.md:52`, past tense, under *"There are no external callers of app's RPC
   mux left."*) and **5** in the **pinned** `stack-demo/rosetta-extensions 09d06070` (4 in the
   `repair_leak` pre/post fixtures' `corpus/services/cms.md`, 1 at
   `stack-core/tests/test_platform_predicate_guard.py:435`). **0 in terraform**, by both mechanisms.
7. **`backend.md:309`, the 13 `ai_readiness_*` tables.** Two independent enumerations at `ad9f3c49`:
   **13** ent schema files matching `ai_readiness_*` and **13** distinct
   `CREATE TABLE "ai_readiness_*"` in `terraform/migrations/`. The four the doc names as the "9 omits"
   (`recommendations`, `email_overrides`, `notification_logs`, `notification_optouts`) are present, and
   `live_snapshots` + `text_translations` are indeed *not* among them.
8. **`backend.md:342`, the publisher set.** `git grep -n 'pubsub.NewPublisher'` at **both** named refs:
   @ `b948604f` exactly five compiling sites — `main.go:287`, `:637`, `:1039`,
   `internal/jobsimwiring/wiring.go:127`, `:180`; @ `ad9f3c49` exactly five — `main.go:325`, `:746`,
   `:1149`, `wiring.go:132`, `:185`. The sixth `ad9f3c49` hit
   (`internal/messenger/flow/streams.go:36`) is inside a `//` comment and correctly excluded.
   **No `NewPublisher` names `SKILLER_STREAM` at either ref**, and `subs[d.Streams.Skiller]` is at
   `subscriber_wiring.go:209` — so "six subscribers, five publishers" holds.
9. **`backend.md:377-379`, the migration queue.** `git ls-tree --name-only <ref> terraform/migrations/`:
   **170** `.sql` at `ad9f3c49`, **169** at `b948604f`; **46** dated ≥ `20260601`; head at `ad9f3c49` =
   `20260803143844_ai_readiness_recommendation_path.sql`; head at `b948604f` =
   `20260731154527_academy_chapter_progress_completed_at.sql`; the three preceding it are exactly the
   three named. Top-level `migrations/` empty at both refs. `atlas.hcl` confirms
   `dir = "file://terraform/migrations"` and `src = "ent://internal/data/ent/schema"`.
10. **`studio-room.md:186`, "exactly nine arguments".** Enumerated `parser.add_argument` in
    `app/studio@aeec036a:gen.py` — nine calls occupying `:484-492` contiguously, matching the documented
    flags one-for-one including `dest="forced"` and the `{stable,experimental}` choices. No `--template`.
11. **`studio-room.md:367-379`, the `mistralai` measurement.** `git grep -ci mistral aeec036a` in the
    **nested repo's own ref** (the host `app` ref returns 0 for everything, exactly as the doc warns):
    `requirements.txt` 1 + `tools/pdf2md.py` 18 + `tools/r3.py` 3 = **22 lines across 3 files**.
    `pdf2md.py:24` `from mistralai import Mistral`, `:96` client, `:127` `model="mistral-ocr-latest"`,
    `r3.py:194` the `MISTRAL_API_KEY` gate — all exact. `services/ai.py:705-708` is the three-provider
    registry, `ai.py:1-2` imports only openai/anthropic. Both `.gitignore` mechanisms reproduce:
    `app/studio/.gitignore:2` = `tools/`, `app/.gitignore:79` = `studio/*`, `cms/.gitignore:129` = `studio/*`.
12. **`studio-room.md:255-264`, the config claims.** `max_tokens = 4000` in **all three** tracked configs
    (`config_template.ini:22`, `production_config.ini:21`, `development_config.ini:21`); the five
    `*_AI_STABLE_MODEL` rows are byte-identical to the doc's block, and `development_config.ini` carries
    the matching five `*_AI_EXPERIMENTAL_MODEL` rows.
13. **`architecture/README.md:21`, the shared-library count.** `app@ad9f3c49:go.mod` requires
    `colony v0.35.2`, `proto v1.210.0`, `taxonomy v1.2.0` and **no** `anthropos-work/ai` and **no**
    `anthropos-work/authn`. `1e457fa70` = *"refactor(ai): fold the ai library into app as internal/ai"*
    (2026-08-04) and `app/internal/ai/` exists. The only surviving `anthropos-work/ai` requirements are
    the frozen husks — `cms/go.mod:9` and `jobsimulation/go.mod:11`, both `v1.40.2` — exactly as the row
    says. (See "could not settle" for the root-`CLAUDE.md` divergence this exposes.)

**Verified by opening the cited construct and reading its surroundings** (`sed -n 'N-15,N+5p'`, per §5
rule 10) — every one resolved to the construct claimed:

- `messenger.md`: `flow.go:74-78` · `cmd/root.go:63`, `:64`, `:107`, `:118-142`, `:147` ·
  `internal/rpcsrv/rpcsrv.go:25-30` · `internal/flow/jobsimulations.go:140-151` ·
  `internal/flow/assignments.go:828` (the `h.cms.GetSkillPath` call inside `getSkillPath`) ·
  `terraform/main.tf:19` `= 1` @ the `fa47850d` checkout **and** `:29` `= 0` with the `:27-28` rollback
  comment @ `e9421c68` (= origin/main) — the doc's "which tree settles which row" split is correct in
  both directions · `go.mod` `go 1.25.0`, `brevo-go v1.1.3`, `liquid v1.8.1` ·
  `docker-compose.yml:84-92` (the messenger-in-app comment block) · `REDIS_WORKER_INDEX` read **nowhere**
  in messenger @ `fa47850d` (positive control: `REDIS_STREAMS_INDEX` → 3 hits) · the `0dab54d` messenger
  block reproduces `8200:8200`/`8201:8201`, `profiles: [messenger]`, `depends_on: backend`, and all four
  `*_RPC_ADDR` at `http://backend:8083` · `d11a403` moved exactly **two** (`CMS_RPC_ADDR`,
  `JOBSIMULATION_RPC_ADDR`); `BACKEND_USERS_RPC_ADDR` and `SKILLER_RPC_ADDR` were already `backend:8083`.
- `app` side of `messenger.md`: `main.go:15`, `:62`, `:63` (the three `internal/messenger/*` imports) ·
  `:295-296` (the `BREVO_KEY` fatal) · `:1416-1421` (the `cmp.Or(os.Getenv("SERVICE_NAME"), "messenger")`
  comment) · `:1450`, `:1471`, `:1473` · `env_guards.go:61` @ `ad9f3c49` **and absent at `b948604f`**
  (`git ls-tree b948604f -- env_guards.go` → empty), exactly as the ⚠️ says ·
  `git diff --stat 2035f9a4 ad9f3c49 -- main.go` **empty** · `main.go:1095` and `:1478-1484` @ `9d00a313`.
- `backend.md`: `main.go:1185-1228` @ `b948604f` and `:1297-1338` @ `ad9f3c49` (per-handler, above) ·
  `SkillPathSessionService` = **0** occurrences in Go @ `ad9f3c49` ·
  `storage@4ce8ece5:cmd/root.go:62-66` (the real `rpcMux.Handle`) and the `sdk/storage/v1/service.go`
  retraction — 47 lines, `Service` interface `:13-17`, `NewService` `:19`, `NewPublicService` `:34`,
  and `git grep 'Handler\|ServeMux'` on that file returns **nothing** ·
  `roadrunner@87d8d443:cmd/root.go:87` · `docker-compose.yml:48`, `:57`, `:59`, `:110`, `:183` ·
  `app/internal/converter/gotenberg.go:31` (the `/forms/libreoffice/convert` POST) ·
  `main.go:230-232`, `:325`, `:504` (*"STORAGE_RPC_ADDR is gone"*), `:524`, `:525`, `:743-746`,
  `:1534-1541`, `:1579-1581` · `subscriber_wiring.go:112-113`, `:203-248`, `:209`, `:248` ·
  `STORAGE_RPC_ADDR` in Go = **3**, all comments, **0** read sites ·
  `internal/academy/academy.go:6-9` · `app/CLAUDE.md:109` and `app/knowledge/architecture.md:28` do
  still list `SkillPathSessionService` (Trap C confirmed) · `app/terraform/locals.tf:6`, `:8`,
  `main.tf:58`, `:185-186`, `variables.tf:197`/`:230` @ `b948604f`, and at `ad9f3c49`
  `cms_rpc_address` at `:309` with `storage_rpc_addr` **deleted** in favour of
  `storage_s3_bucket`/`storage_s3_public_bucket` · `messenger/terraform/variables.tf:77,82,87,92`, all
  four with **no default** · `cms/terraform/main.tf:39` `= 0` @ `f38c0c4a`, and `6efa1d5` really is
  *"chore(ci): drop build-production — the cms ECR repository is decommissioned (M810)"* deleting
  `.github/workflows/build-production.yml` · `6092c6d2` (*"remove the jobsimulation ECS service and ECR
  repository (M810)"*, 2026-08-04) with `terraform/main.tf:15-22` and `:24-40` byte-exact **at that
  ref** — note it is **not** in the `462343b0` checkout's ancestry, only in `origin/main`, and the
  briefing's pin rule is what makes the citation legitimate · `1fc00c78` = *"Deprecate skiller schema…"* ·
  `categoryTree`/`fullCategoryTree` appear **only** in a comment saying they *"stay unported — no
  consumers"* (`skiller_taxonomy.graphqls:7`) · ports 8082/8083/8084 with the container publishing
  8081/8082/8083 · Go **1.26**.4.
- `graphql-wundergraph.md`: `terraform/main.tf:20` `= 1` · `Dockerfile`/`Dockerfile.dev`
  `node:22.11-alpine` + `wgc@0.104.0` + `cosmo/router:0.275.0` · `config.{compose,dev,prod}.yaml`
  `listen_addr 0.0.0.0:8080` / `graphql_path /graphql`, playground+introspection ON in compose/dev and
  OFF in prod · `package.json` = `{"name": "graphql-wundegraph"}` and `CLAUDE.md`'s heading carries the
  same misspelling · `ci/update-subgraph.sh:9` = the **one** `gh release download`, `-R anthropos-work/app` ·
  `subgraphs.conf` = the single line `BACKEND=v1.360.0`, byte-matching `CLAUDE.md:70-79`, so the M257x
  iter-115 correction (*"the Version-Tracking section is NOT stale"*) is upheld, while `CLAUDE.md:85`
  does still say `supergraph-config-local.yaml` against a directory holding only
  `-compose`/`-dev`/`-prod` · no `subscription:` block in any supergraph config · `bba862f` lives only on
  `remotes/origin/feat/use-web-socket` · `app/internal/authorization/gqlauthz/gqlauthz.go:186` is the
  `unknown viewer: Forbidden` return the smoke-test output quotes, and both named regression tests exist
  (`gqlauthz_test.go:132` *"bare __typename is not exempt"*, `:176`
  `TestAnonymousRejectionLogsAtWarn`) · `terraform/tests/` is the only test dir · `make run`/`make updatesubg`
  and the README's aarch note are as described · the current-ref compose anchors `:119`, `:135`, `:151`,
  `:160` are all exact (only the *historical* pair is B3).
- `studio-room.md`: `studioManager.go:93-96` (the venv const block), `:119` (`studio/gen.py`), `:1045`
  (`studio/postgen.py`), `runCommand` at `:1096-1101` — argv form, `exec.CommandContext`, no shell ·
  `internal/cms/worker/worker.go:29` `Concurrency: 5` with `Queues{AiVideoQueue: 7, StudioQueue: 3}` ·
  `.github/workflows/build-production.yml:29` `additional_repo: "anthropos-studio-room:studio"`, first
  contained in tag **`v1.360.1`** (commit `851cf3fb1`, 2026-07-29) — the version claim at `:5`/`:25`/`:37`
  is exactly right, and there is no `.gitmodules` despite the Dockerfile comment calling it a submodule ·
  `app/Dockerfile:28` `FROM python:3.11-slim`, `:45-46` copy + pip-install · `gen.py:18-28`
  (`parse_known_args`), `:205-238` (`translate_legacy_blueprint` + the three legacy names),
  `:241-271` (`validate_blueprint_exclusivity`), `:273-282` (`setup_generation_request`), `:450-456`
  (the four `work_paths`), `:116-117` (the two state files) · `CLAUDE.md:12-14` · `cert.py` has
  **0** hits for certificate/signing/private_key/x509 (the two tree-wide hits are substring artefacts
  inside "De**signing**" and "as**signing**") · `postgen.py:396-398` — `--media`/`--simid`/`--target`
  all `required=True`, and export runs unconditionally after the targets rather than as one ·
  `GenMode` = FAST/STRICT/EXECUTION/CREATIVE/REASONING with `DEFAULT = EXECUTION` ·
  `exporter.py:518-519` and `:513-550` (the unpack→write→re-archive→`rmtree`, preserving
  `collaborative_*`/`asset_*`/`internal_*` JSON, zip named for `source_id` falling back to sim id) ·
  `model.py:59` and `:467-469` (the per-run taxonomy memo) · `services/taxonomy.py:11`
  `BASE_URL = "https://api.anthropos.work/api"` · `ENVIRONMENT` defaults to `local` (`gen.py:31`) ·
  all five `tests/e2e/blueprints/*.json` and all three
  `knowledge/development/asset-examples/blueprints/` files present, and every key the doc quotes from
  `technical.json` is in the file.
- `frontend_architecture.md`: `docker-compose.yml:141` `profiles: [studio-desk, all]` (and `:226` at
  `0dab54d`, as claimed) · ports 3000/3001/3002 from each app's `dev` script, mobile `expo start --port
  3031`, maintenance none, mobile declaring no `next` · `!apps/mobile` in `pnpm-workspace.yaml` ·
  `@anthropos/tsconfig` / `@anthropos/i18n` under `configs/` · **8** locale dirs (de, en, es, fr, it, ja,
  nl, pt) · `apps/web/package.json:46` `"next": "~16.2.12"` — **tilde**, same in hiring/integration/
  maintenance · React `^19.2.7` · `packageManager: "pnpm@10.30.3"` · `engines.node: ">=24.0.0"` ·
  TypeScript `^5.9.3`, turbo `^2.9.6`, and all four `check*` scripts · `clean:quick` ·
  `codegen`/`codegen:watch` · the three `pnpm dev --filter=…` names.
- `architecture/README.md`: every one of the nine file links resolves, and the per-file characterisations
  (subgraph count, `2adcf71` router deletion, the not-an-EU-first-ladder wording, the fenced-map framing)
  agree with what I measured for the corresponding files.

**RE-DERIVED only** (weakest clearance — recorded as such per §5 rule 24): none. Where a passage showed
its own arithmetic (the REST/SSE table, the `.tf` denominators, the `ai_readiness` count, the subgraph
ladder, the handler counts, the 21/22 handler split) I enumerated the SET from source first and stated its
cardinality before comparing.

---

## What I could not settle

- **`graphql-wundergraph.md:8` — *"the repo | ARCHIVED on GitHub, 2026-07-30 (read-only)"*.** Not
  measurable from a local clone; needs `gh api repos/anthropos-work/graphql-wundergraph --jq .archived`.
  Everything measurable around it (no compose service, no `repos.yml` entry, not cloned by `make init`)
  holds.
- **`backend.md:76-96` — the M810 prod teardown for `cms`.** The doc says outright that it is NOT
  measurable from any clone set we have, and I confirm that: `infrastructure` is in no clone. What I could
  measure is exactly the two facts it reports and nothing more — `cms/terraform/main.tf:39`
  `service_desired_count = 0` still declared, and `6efa1d5` deleting the build-production workflow with
  the *"ECR repository is decommissioned"* subject. The doc asserts neither direction, which is the
  correct end state.
- **`backend.md:103`, `:136-143` — the taxonomy cardinalities** (42,790 / 22,470 / 43,584 …). These are
  read-only production DB measurements; nothing in the clone set can confirm or refute them. The doc's own
  framing ("floors, not totals", public predicate `organization_id IS NULL`) is internally consistent and
  the cross-reference to `shared_libraries.md#taxonomy-figures` resolves.
- **`backend.md:310`, `:313` — `feat/hiring-talk-to-data` and `feat/taxonomy-translations`.** Neither
  appears in `git branch -a` on the `app` clone (`feat/company-context-m1m2` at `:312` does). A missing
  remote branch is equally consistent with "merged and deleted" and "never fetched by this clone's
  refspec", so I cannot grade it; a `git ls-remote --heads origin` would settle it. Booked as neither.
- **`messenger.md:103` — *"Recent work in v0.34.0 added whitelabel support"*.** `whitelabel.go` and the
  `READONLY_DB_CONNECTION` read at `cmd/root.go:147` are both exactly as described; only the *version
  attribution* is unverifiable without the messenger CHANGELOG/tag history, which this frozen clone's
  tags do not obviously carry.
- **A cross-boundary divergence I am reporting rather than booking, because the wrong side is outside
  my file set.** My `corpus/architecture/README.md:21` is **correct** — `ai` is folded in-tree and
  **three** libraries are imported by a service a stack builds. The repo-root `CLAUDE.md` still says
  *"**four are imported as private Go modules** (`ai`, `colony`, `proto`, `taxonomy`)"* and, further down,
  *"AI providers via the `ai.AI` interface"* framed as the shared library. `app@ad9f3c49:go.mod` requires
  no `anthropos-work/ai`. Per §5 rule 19's corollary this is the highest-propagation site in the tree and
  the leak sits just outside the audited scope edge — which is the exact geometry that rule predicts.
  I did not book it as a blocker against my own files because the file I own states it right.
- **The guard verdict named in the briefing addendum (`corpus/services/sentinel.md:5`).** Out of my file
  set; I did not read it as a subject. I note only that the compose-service-count convention it turns on
  ("five declared / seven effective") is the same one my `backend.md:19` minor is about, and that
  `sentinel.md:5` — which I saw incidentally while grepping the corpus for that predicate — names
  `docker-compose.yml` explicitly, so it is the *better*-scoped of the two phrasings, not the worse.
