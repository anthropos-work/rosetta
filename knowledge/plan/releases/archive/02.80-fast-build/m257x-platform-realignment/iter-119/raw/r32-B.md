# Seat B — reading #32

**Refs used (stated per the addendum).** Corpus at rosetta `5b23559`, working tree clean for `corpus/`.
Platform clones: app `ad9f3c49`, platform `0c91421d`, next-web-app `8297c684`, sentinel `f2c46190`,
studio-desk `41ee3575`, ant-academy `22df69dd`, storage `4ce8ece5` (origin/main `9f8cb53`),
messenger `fa47850d`, cms `ca50c817` (origin/main `f38c0c4a`), graphql-wundergraph `60c229f3`,
roadrunner `87d8d443` (= origin/main), jobsimulation `462343b0` (origin/main `82cb66ec`),
nested `app/studio` + `cms/studio` at `aeec036a`.
`rosetta-extensions`: **pinned per-stack clone `09d06070`** was used for every "what the tooling does on
a stack" claim; the authoring copy `43049308` was not used to settle anything.
I made **zero** fetches and **zero** writes outside this report.

## Positive controls

| file | `wc -l` | read in full? |
|---|---|---|
| `corpus/services/ai-readiness.md` | 712 | yes |
| `corpus/services/cms.md` | 336 | yes |
| `corpus/services/chronos.md` | 245 | yes |
| `corpus/services/academy-backend.md` | 160 | yes |
| `corpus/services/README.md` | 90 | yes |

Also read in full as instructed: `corpus/ops/platform-alignment.md` §5 (lines 249–1665).

Search positive controls run in the same passes: `SKILLER_RPC` returned 26/12/9/4/20/1 across six trees
while `MESSENGER_RPC_ADDR` returned 0 everywhere (so the zero is a measurement, not a broken pipeline);
`git -C stack-demo/platform log -S 'SKILLER_RPC' --oneline 0c91421d` returned **7** against **0** for
`MESSENGER_RPC`. The tree enumeration used `find stack-demo -name .git -maxdepth 4` → **15** git trees
(13 top-level + the 2 nested `studio` checkouts), and every per-tree number below names the trees it covers.

---

## Blockers

### B1 — `ai-readiness.md`: the "third generation rotted too" paragraph has itself rotted — **all four** of its own line numbers are wrong at corpus HEAD

- **Anchor:** `corpus/services/ai-readiness.md:54-58` — verbatim:

  > ⚠️ **And the third generation rotted too, which is why this passage no longer carries a line pin at all
  > (M257x iter-115).** iter-102 replaced `:459` with **`:496`**; re-derived at iter-115, `:496` is the
  > **closing line of the `> **✅ CORRECTED M219 …**` blockquote** (`:476-496`) — the very blockquote the two
  > sentences above name as the *wrong* target for the previous generation. The block opens at `:498` and the
  > quoted parenthetical is at `:500`.

- **Why it is false:** measured against the same file at rosetta `5b23559` (`git status --porcelain corpus/`
  empty, so disk == commit). Every one of the four assertions names a different construct:

  | the passage says | what is actually at that line (`awk 'NR==N'`) | where the named construct actually is |
  |---|---|---|
  | `:496` is the **closing line** of the `✅ CORRECTED M219` blockquote | `frozen \`ai_readiness_snapshots\`), after iters 03→06 falsified the active-signals path — on the premise that the` — mid-sentence inside the **closed-cycle** bullet | the blockquote closes at **`:536`** |
  | that blockquote spans `:476-496` | `:476-496` spans the tail of the `keepStartedMembers` correction paragraph and the closed-cycle bullet — no blockquote at all | the blockquote is **`:512-536`** (`grep -n "CORRECTED M219"` → 512) |
  | "The block opens at `:498`" (the `⚠⚠ M51 iter-08/09` block) | `per-object-RPC class). ⚠️ **That premise was refuted at M219: the recompute takes 2.09 s.** The strategy M51` | `grep -n "M51 iter-08/09"` → the block opens at **`:538`** |
  | "the quoted parenthetical is at `:500`" | `[\`ops/demo/stories-spec.md:599\`](../ops/demo/stories-spec.md) at M257x iter-49.` | `grep -n "now \`aireadiness/readiness.go\`, formerly"` → **`:540`** |

  The offset is a uniform **+40** on three of the four (496→536, 498→538, 500→540) and +36 on the block
  open (476→512), i.e. content was inserted above the derivation point after the numbers were taken.

- **Why this is a BLOCKER and not line drift.** These are not off-by-one; each number lands on a *different
  claim* (rule 46's "resolves but names the wrong construct"). And the passage is **load-bearing about
  itself**: it exists to record that this anchor rotted three times and to tell the reader *"search the
  construct"* while simultaneously publishing a fourth generation of the same defect as a freshly
  "re-derived at iter-115" fact. A reader following `:498` lands two lines after a *different* ⚠️ marker and
  will believe they found the block.
- **Related, same cluster (booked here, not separately):** `:50-51` asserts, present tense, that *"`:459`
  merely opens the `> **✅ CORRECTED M219 …**` blockquote"*. At HEAD `:459` is `decision):**` — the second
  line of the *"⚠️ Which table the dashboard reads…"* heading. Its framing is partly historical
  ("after iter-100 mechanically re-pointed it"), which is the only reason I did not book it as its own B.
- **Confidence:** high.

### B2 — `cms.md:216`: the "Data bullet" cross-reference points at the **Domain** bullet

- **Anchor:** `corpus/services/cms.md:216` —
  > `(Ent ORM, **`public` schema** — the cms tables were re-created there at cms-in-app v8.0; the legacy `cms` schema is non-authoritative. Consistent with the **Data** bullet, :44-47 above)`
- **Why it is false:** `awk 'NR>=44&&NR<=51'` on the same file gives
  `:44` `> Where everything went:` · `:45` `>` · `:46-47` the **Domain** bullet
  (`* **Domain** — \`app/internal/cms/\` (…), wired from \`app/internal/cms/wiring.go\`.`).
  The **Data** bullet — the one that actually states the `public`-schema re-creation and names
  `app/terraform/migrations/20260724132049_cms_data_model.sql` — is **`:48-51`**. The cited range contains
  none of the claim it is offered as corroboration for.
- **Why I hesitated:** it is an intra-corpus anchor with no platform consequence. Booked as a BLOCKER
  because the briefing names "wrong line anchors that point at the wrong construct" as a blocker class and
  because the sentence *cites the range as evidence of consistency* — a reader who checks it finds an
  unrelated bullet and cannot tell whether the corpus is inconsistent or the pointer is.
- **Confidence:** high.

### B3 — `cms.md:240`: the "Studio bullet" cross-reference points at the **Events** bullet

- **Anchor:** `corpus/services/cms.md:240` —
  > `(app v1.360.1) — see the **Studio** bullet, :70-71 in the banner at the top of this doc.`
- **Why it is false:** `:70-71` is the **Events** bullet
  (`* **Events** — \`app\` owns the \`CMS_STREAM\` subscriber. The folded similarity re-index + Studio handlers are / merged onto app's **existing** CMS subscriber via \`.AddHandler(...)\`…`).
  The **Studio** bullet — *"the Python `anthropos-studio-room` project is now pulled into the **`app`** image
  via the CI `additional_repo` mechanism (app v1.360.1)"* — is at **`:75-76`**. `:74` is the **Caching**
  bullet. The trap is that `:70-71` *contains the word "Studio"* (in "Studio handlers"), so a skim-check
  passes it.
- **Confidence:** high.

---

## Minors

- `corpus/services/README.md:29-32` — *"`838d907` … then did the same to the last three"*, where "the same"
  is the preceding clause *"deleted all three from `docker-compose.yml` **and** from `repos.yml`"*.
  Measured: `git show 838d907 -- repos.yml` removes **only** `storage` and `messenger`;
  `customerio-sync` was never a `repos.yml` entry (it was built straight from a GitHub URL — which this same
  file says at `:47`, and `:15-17` says its repo "has never been in any clone set"). Compose removals are
  correct (3: `storage`, `customerio-sync`, `messenger`). Non-material — the banner's actual proposition
  ("none of them starts a container any more") is true.
- `corpus/services/academy-backend.md:121-138` — the **cmd binaries** list names `academy-seed`,
  `academyImport`, `coursebuilder-e2e`, `coursebuilder-liverun`; `git ls-tree ad9f3c49:cmd/` also carries
  **`academy-asset-upload`**. Omitted list member; the list does not claim exhaustiveness.
- `corpus/services/chronos.md:22`, `:171`, `:190` still tell the reader to work in `stack-dev/chronos`.
  `chronos` is in no clone set on this box and is not cloned by `make init` (the doc says so at `:9`), so
  the `cd stack-dev/chronos` recipes cannot be run as written. Harmless — the section is explicitly
  flagged historical.

**Observed but NOT booked (platform-side stale comments the corpus is right about):** `app`'s own
`internal/aireadiness/csv.go:35` says *"writes the 19-column CSV"* while `AIReadinessCSVColumns` (`:17-33`)
has **15** entries — the corpus's "15 columns" is correct and the platform comment is stale. Same shape at
`how_we_measure.go:1183` (*"builds the four … tiles"* over a 5-entry `usageDimSpecs`) and at
`rext stack-seeding/seeders/ai_readiness_funnel.go:448-452` (a pre-M250 band table above the live
constants). In all three the corpus tracks the code, not the comment.

**Out-of-scope pointer worth routing:** `corpus/ops/demo/stories-spec.md:603` cites the live-recompute
retraction as living at `services/ai-readiness.md:390,458`. At HEAD `:390` is the *Local development*
paragraph and `:458` is the *"⚠️ Which table the dashboard reads…"* heading; the retraction is at
**`:498-500`**. `stories-spec.md` is outside this reading's scope (`corpus/services/**` +
`corpus/architecture/**`), so I did not book it — but it is the same class as B1 and it points *into* my
file set.

---

## Positively cleared (audited zeros)

**ENUMERATED** (predicate re-derived from source, cardinality compared before any arithmetic):

- `README.md:31-32` / `cms.md:58` — compose declares **5** services at platform `0c91421d`
  (`sentinel`, `backend`, `studio-desk`, `next-web-app`, `gotenberg`), **7** effective with `common.yml`'s
  `postgresql` + `redis`; `repos.yml` carries **4** (`app`, `sentinel`, `next-web-app`, `studio-desk`).
  I also enumerated the `profiles:` keys: `sentinel`, `postgresql`, `redis` declare **none**, which is what
  makes `cms.md:257-258`'s "asking for a cms profile exits 0 and starts three containers" true.
- `README.md:39` — `app/internal/{skiller,skillpath,jobsimulation,cms,storage,messenger,customeriosync}`
  all present at `ad9f3c49` (`ls-tree -d`, 7/7); `internal/roadrunner/` absent and
  `git ls-tree -r --name-only ad9f3c49 | grep -ci roadrunner` = **0 paths** (the nested `app/studio` repo at
  `aeec036a` also returns 0, so the tree-wide zero names its sub-repo); Judge0 wiring is
  `internal/jobsimwiring/wiring.go:123` = `runnerManager := jsrunner.NewRunnerManager(...)`. Exact.
- `README.md:46` — `MESSENGER_RPC_ADDR` occurs **0** times across all 15 git trees at their own refs
  (nested `studio` checkouts included). The prefix `MESSENGER_RPC` returns 2 in `app`, both in
  `knowledge/plan/roadmap-support-in-app.md` and both *asserting* zero hits — not a consumer.
  `flow.go:72-104` @ `fa47850`: I counted the handler list line by line — **21** live
  `pubsub.EventHandler(...)` entries, with `:83` (`OrgJobSimulationAssignmentPastDueHandler`) correctly
  excluded as commented out (§5 rule 24's "`grep -c` counts commented-out code").
- `README.md:54` / `cms.md:64-68` / `cms.md:202` — the subgraph ladder, enumerated by
  `git ls-tree schemas/` at each ref in `graphql-wundergraph`: `749dc86^` = 5 → `749dc86` = 4 (skiller) →
  `7c17e63` = 3 (skillpath) → `915da06` = **1** (deletes `cms.graphqls` **and** `jobsimulation.graphqls` in
  one commit, 2026-07-29). "3 → 1, not 2 → 1" is exactly right, and `915da06`'s own subject line does say
  "2→1".
- `cms.md:60` / `:201` — **44** tracked `.tf` files across the **13** top-level `stack-demo` repos
  (per-repo: app 5, cms 4, gql 4, jobsim 6, messenger 4, next-web 2, roadrunner 4, rext 2, sentinel 4,
  storage 5, studio-desk 4, ant-academy 0, platform 0); **0** of them name
  `http://backend.internal.anthropos:8081`; a raw `find stack-demo -name '*.tf'` gives **59**; the literal
  occurs **6** times in the clone set, **none** terraform (1 × `app/knowledge/service-dependencies.md:52`,
  5 × `rosetta-extensions` fixtures/tests). Every figure exact.
- `cms.md:149-154` — `studio/requirements.txt` at `aeec036a` is byte-identical in `app/studio` and
  `cms/studio`, and enumerates exactly **9** packages in the stated order; `python-docx` absent (the only
  `.docx` tokens are a guideline sentence and an asset-example README row).
- `cms.md:100` / `:218-232` — `services/ai.py:705-708` is a **three**-entry provider dict
  (`openai`/`azure`/`anthropic`); `:1-2` imports only `openai`/`anthropic`;
  `git -C app/studio grep -ci mistral aeec036a` sums to **22 hits in 3 files**
  (`requirements.txt:8`, `tools/pdf2md.py`:18, `tools/r3.py`:3) — exactly as published, and the "22 vs 0"
  nested-repo trap is correctly handled by the doc.
- `cms.md:286-287` — `gen.py:484-492` is exactly **nine** `parser.add_argument` calls, no `--template`;
  `parse_argument` at `:18-28` calls `parse_known_args` and merges leftovers into the dict.
- `ai-readiness.md:281-285` / `:405` — `aireadiness/defaults.go` `defaultReadinessSkills` enumerates
  **19 @ 1.0 + 12 @ 0.5 = 31**; `defaultReadinessSims` is **3** (`tech`/`business` simulations + a `both`
  interview). Both counted from the literal, not from the file's own comment.
- `ai-readiness.md:230` / `:242-248` / `:681-685` — `usageDimSpecs` (`how_we_measure.go:1160-1181`) is
  **five** entries and their ids are exactly `avg_adoption` · `avg_transformation` · `avg_originality` ·
  `avg_depth` · `avg_ownership`; the seeder emits the same five at
  `rext(pinned) stack-seeding/seeders/ai_readiness_interview_report.go:185-189`, with the *"All five are
  always emitted here"* comment at `:149`. The corrected "five, not four" is right.
- `ai-readiness.md:314-315` — `AIReadinessCSVColumns` enumerates **15** columns.
- `ai-readiness.md:644` — the ent enum validator
  (`internal/data/ent/interaction/interaction.go:92-99`) accepts exactly `"email"` and `"call"`;
  `aiReadinessMinTurns = 6` / `aiReadinessMaxTurns = 11` in the pinned rext clone.
- `ai-readiness.md:85-88` / `:363` — `AI_READINESS_FLAG` has exactly **one** consumer repo-wide
  (`useAiReadinessActive.ts:22`); `AI_READINESS_URL` occurs in exactly two source files
  (`urls.ts:51`, `useNavbarSections.tsx:4,403`) plus `knowledge/ai-readiness/frontend-architecture.md:15`.
  `:52` is `TALK_TO_DATA_URL` at `8297c684`, exactly as the ⚠️ says.
- `ai-readiness.md:90-98` — `AIReadinessClient.tsx` at `8297c684` contains **0** `posthog` and **0**
  `flag_ai_readiness`; `:78`, `:135`, `:155-156`, `:171`, `:601` all name the constructs claimed, and
  `:599` is `tab.key === 'how' ||` as the ⚠️ states.
- `ai-readiness.md:194-212` — all **13** `ai_readiness_*` ent schemas exist and their ent-generated
  `Table =` names are exactly the plural forms the doc lists, including
  `ai_readiness_user_step_progresses`, `..._notification_logs`, `..._notification_optouts`;
  `interview_aggregated_reports` carries `(organization_id, sim_id, report JSONB, session_count)`.
- `ai-readiness.md:691-694` — **5** AI-readiness Playthroughs enumerated from the pinned clone's manifests:
  4 in `playthroughs/manifest/ai-readiness.yaml` + `pt-onboarding-aireadiness-guided` in `onboarding.yaml`
  (seat `pt-ai-onboard`).
- `ai-readiness.md:293-305` — `emailoverride.go:29` imports
  `github.com/anthropos-work/app/internal/messenger/aireadinessemail`;
  `git ls-tree ad9f3c49 internal/messenger/aireadinessemail/` lists exactly **9** files; `:33`'s comment
  does use the misleading `pkg/aireadinessemail` shorthand. The whole ⚠️ correction is accurate.
- `academy-backend.md:65-78` — all **11** academy ent table names verified against the generated
  `Table =` constants (`academy_chapter_progresses`, `academy_last_activities`, `academy_chapter_times`,
  `academy_certificates`, `academy_bookmarks`, `academy_feedbacks`, `academy_series`,
  `academy_skill_paths`, `academy_chapters`, `academy_chapter_bodies`, `academy_path_embeddings`);
  `UNIQUE(user_id, chapter_slug)` at `academy_chapter_progress.go:97`; `paid` default true;
  `source_hash = sha256(model | embed_text)`; `vector(1536)`.
- `academy-backend.md:62-64` / `:118-120` — at `9d00a313` (= `v1.367.0`, confirmed by `git describe`),
  `main.go:471-472` constructs both managers, `internal/storage/service.go:22,24` prove
  `EnvBucket = "STORAGE_S3_BUCKET"` / `EnvPublicBucket = "STORAGE_S3_PUBLIC_BUCKET"`, and
  `STORAGE_RPC_ADDR` has **exactly 3** occurrences in `*.go` — `wiring.go:101`,
  `storagens/callsites_test.go:189`, `main.go:451` — **all comments, zero reads**. Exact.
  `cmd/academyImport/main.go:239-243` and the `:244-246` hard-error are exact.

**RE-DERIVED** (arithmetic the document displays; the weakest clearance — recorded as such):

- `ai-readiness.md:397-399`: 156 / 199 = 78.39 %. I also enumerated the source constants
  (`aiReadinessCompletedShare = 0.80`, `aiReadinessStage2Share = 0.50`), which are consistent with a
  measured 78.4 %.
- `ai-readiness.md:607`, `:618`: `round(1.0/25.0*30)` = 1 and `round(9.0/25.0*30)` = 11, with
  `aiReadinessStartedHeroSkills = 9` and `aiReadinessCoreSkills = 19` enumerated from the pinned clone —
  which also makes the population bands `19…31 / 3…19 / 1…3` (`ai_readiness_funnel.go:506-514`) exact.
- `README.md`/`ai-readiness.md:324`: `dae0fb2f7` deletes 103 + 220 + 330 = **653** lines across the three
  named components (the commit's `-662` includes `HowWeMeasureTab.tsx`).

**Other exact anchor confirmations** (opened and read with surrounding context, `sed -n 'N-15,N+5p'`-style):
`readiness.go` `:139/:289/:290-297/:307-312/:314/:330/:388/:390/:655/:662-664/:684/:710/:730`;
`steps.go` `:922/:962/:964/:969-974`; `how_we_measure.go` `:253-261/:285-287`;
`studioManager.go` `:100-103/:119/:122-124/:126/:129/:531/:583/:1045/:1096-1098/:1099-1101` @ `ad9f3c49`
plus `:119`/`:1045` @ `b948604f`, and `git grep '"bash"' ad9f3c49 -- '*.go'` = **0**;
`cms ca50c817:internal/studio/studioManager.go:967` = `exec.Command("bash", "-c", command)` (the
retraction at `cms.md:292-310` is correct in both directions);
`markdownManager.go:10/:30`; `xlsx.go:13`; `skillpath/session.go:205-207`;
`app/main.go:1205-1211` @ `b948604` (the DORMANT/M809 comment);
`cms/cmd/root.go:77-78`; `cms/go.mod:3` = `go 1.26.4`; `cms/Dockerfile:2` + `:23`;
`docker-compose.yml` @ `2adcf71` `:154-155/:169/:173`;
`cms/terraform/main.tf:39` = `service_desired_count = 0` in a **191**-line file (identical at
`ca50c817` and `origin/main f38c0c4a`); `roadrunner/terraform/main.tf:19` = `= 1`, `git blame -L 19,19`
→ `84a4b4f1 … 2025-12-15` (roadrunner's checkout **is** origin/main, so no ref ambiguity);
jobsimulation `6092c6d2` — at that ref `service_desired_count` appears **nowhere** in
`terraform/main.tf` and `:15-22` is the M810 decommission comment (note: at the *checkout* `462343b0` the
module block is still whole, so this claim is true **only because it is pinned** — the pin is doing real
work here);
`6efa1d5`/`f38c0c4` (2026-08-04, `build-production.yml`, −18 lines);
`d11a403` (2026-08-03) removes `cms`/`jobsimulation`/`roadrunner` from **both** compose and `repos.yml`,
and moves **exactly two** messenger-block variables (`CMS_RPC_ADDR`, `JOBSIMULATION_RPC_ADDR`) while
`BACKEND_USERS_RPC_ADDR`/`SKILLER_RPC_ADDR` already read `http://backend:8083` at `d11a403^` — the
"middle two, not four" correction at `cms.md:29-33`/`:201` is exact;
at `0c91421d` compose sets **zero** `*_RPC_ADDR` and one service address, `:48`;
`chronos.md:7` (`09631fb2`, merged by `500b9761` = PR #395 on `feat/remove-chronos-and-realtime`) and
`:9` (`jobsimwiring/worker.go:24-35`);
next-web `useAiReadinessActive.ts:19-32/:22/:25/:29/:32`, `aiReadiness.constants.ts:26`,
`useAIReadiness.ts` (components) `:48-62` `deriveMode`, `AIReadinessHero.tsx:88`,
`WorkforceNewClient.tsx:125-151`, `useWorkforceAIReadiness.ts:23-27`,
`hooks/useAIReadiness.ts:323-330` with `interviewQuestions` at `:326` (and `:274` at `bb3313bc`, as the
⚠️ pin says), `HowWeMeasureTab.tsx` **1989** lines with `:1879/:1903/:1915/:1921/:1927` exact and
`interviewQuestions` count **0**, `Analytics.provider.tsx:27-28`,
`/enterprise/workforce/ai-readiness` gone from the route tree;
pinned-rext `app-aireadiness-snapshot-loadmembers.yaml:33` (*"v2.7 M254 RE-POINT"*) and `:42`
(`path: internal/aireadiness/readiness.go`) — the "re-anchor has LANDED" claim is true **in the tree that
settles it**, the pinned per-stack clone; `cockpit.go` `LegacyReadinessPaths`/`ValidateCockpitManifest`;
`seat_append_test.go`; `ai_readiness_funnel_test.go:230`
`TestAIReadinessFunnelSeeder_DayZeroHeroGetsNoSignals`; `blueprint.go:618` proves `ai_readiness` **is** a
validated closed enum (`stories_test.go:195-235` fences it), so `ai-readiness.md:440-443` holds;
`AIReadinessSimSkillsSeeder` registered at `cmd/stackseed/main.go:520` and the three sim slugs at
`ai_readiness_config.go:84-85`; `ai_readiness_sim.go:84` carries the
`UNIQUE(organization_id, step_type, track)` index the doc leans on;
`academy_embedding_refresh` @ 03:00 (`worker/tasks/academy_embedding_refresh.go:10`),
`/content/catalog.json` (`web/backend/content.go:23`) and the `/content/admin` group gated by
`ACADEMY_CONTENT_API_TOKEN` (`content_admin.go:35`), `AAS-\d{4}-[A-HJKMNP-TV-Z2-9]{6}` +
`crypto/rand` (`certificate.go:42`, `:171`), `SUPABASE_DB_CONN`, and all five `academy-seed` flags;
`0e37771f` = PR #903, 2026-06-05; `v1.369.0` dated 2026-08-05 in `CHANGELOG.md` and
`git describe ad9f3c49` = `v1.369.0-7-gad9f3c498` (the doc discloses the `-7-g` honestly).

**A ref-dependent clearance I want on the record.** `README.md:43` says storage's prod ECS service is
*"**deleted**, not scaled to zero"*. At the storage **checkout** `4ce8ece5` (v0.15.3, 2026-07-31) that is
**false** — `terraform/main.tf` is 100 lines with `module "storage"` intact and
`service_desired_count = 1`. It is **true** at storage `origin/main 9f8cb53`, where the file is 18 lines
and the ECS block is gone. The deletion (`e6fac97`) landed **2026-08-04**, i.e. *before* the `838d907`
(2026-08-05) pin the same table cell carries, so the claim is true as of its own cell's date and true of
production. I therefore did **not** book it — but a seat grading strictly "no ref named → grade at the
checkout" would, and the coordinator should know the two refs disagree.

---

## What I could not settle

- **`README.md:54` "repo ARCHIVED"** (graphql-wundergraph) and **`README.md:81` / `chronos.md:3`
  "the GitHub repo is NOT archived (last push 2026-04-23)"**. Both are GitHub-API facts, not repo facts.
  `gh` is not installed on this box (`command not found`) and the org is private, so neither is checkable
  from here. `platform-migration-status.md:99-100` states the same pair (chronos not archived /
  intelligence archived 2026-04-02), so the corpus is at least self-consistent. Settled by one
  `gh api repos/anthropos-work/{graphql-wundergraph,chronos} --jq .archived`.
- **`ai-readiness.md:224` "165 rows"**, **`:678` "5 → 345 / 897 / 787"**, **`:526` "HTTP 200 in 24 ms"**,
  **`:534` "2.09 s · 304 KB"**, **`:548` "180 s → 19 ms"**. All are live measurements against a running
  seeded demo; nothing in a source tree can confirm or refute them. The doc itself says *"Re-measure before
  relying on either number"*. The seeder that produces the 165 (`SuccessionSeeder` writing
  `interview_extraction_results`) does exist in the pinned clone, so the mechanism is real.
- **`cms.md:60` / `:201` "the deciding declaration lives in `infrastructure`"** — correct that
  `infrastructure` is in no clone set, so the cms M810 prod state genuinely cannot be settled here. The
  doc's "report both, assert neither" is the right posture and I have nothing to add.
- **`chronos.md` §§ Data Model / Interface / Triggerer** (`:40-160`) — the `chronos` repo is not cloned,
  so the schema DDL, the `LIMIT 10` + `FOR UPDATE SKIP LOCKED` behaviour, the Sentry heartbeat and the CLI
  flag defaults are all unverifiable from this box. Settled by cloning `anthropos-work/chronos` and reading
  `internal/database/schema.sql` + `internal/triggerer/triggerer.go`.
- **`README.md:46` "no service ever constructed a client for it"** (MessengerService) — the env-var half is
  proven (0 occurrences of `MESSENGER_RPC_ADDR` anywhere), but `messenger`'s own
  `cmd/send.go:24` does call `messengerv1connect.NewMessengerServiceClient`. That is the service's own CLI,
  not another service, so I read the sentence as true; flagging it because a stricter reading of "no
  service" would not.
