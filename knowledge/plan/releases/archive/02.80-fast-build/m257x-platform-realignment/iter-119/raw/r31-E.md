# Seat E — reading #31

## Refs actually used (stated at the moment of measurement, §5 rule 26)

Re-derived at this reading's open **and** re-checked at close; identical at both ends.

| repo | path | HEAD | origin/main | which I graded at |
|---|---|---|---|---|
| app | `stack-demo/app` | `ad9f3c49` | `ad9f3c49` | checkout (= origin/main); pinned claims graded at `b948604f`, `9d00a313`, `2035f9a4`, `1e457fa70` as named |
| app/studio (nested, own checkout) | `stack-demo/app/studio` | `aeec036a` | `aeec036a` | its **own** ref — `git -C app grep … -- studio/` returns 0 for everything (`app/.gitignore:79` = `studio/*`), verified |
| cms/studio (nested, own checkout) | `stack-demo/cms/studio` | `aeec036a` | `aeec036a` | same repo, same sha (`cms/.gitignore:129` = `studio/*`) |
| platform | `stack-demo/platform` | `0c91421d` | `0c91421d` | checkout; historic refs `0808b92`, `2adcf71`, `d11a403`, `0dab54d`, `3e85fce` read explicitly |
| next-web-app | `stack-demo/next-web-app` | `8297c684` | `f97ba659` | `8297c684` — the ref the doc names |
| sentinel | `stack-demo/sentinel` | `f2c46190` | `f2c46190` | checkout |
| graphql-wundergraph | `stack-demo/graphql-wundergraph` | `60c229f3` | `60c229f3` | checkout |
| messenger | `stack-demo/messenger` | `fa47850d` | `e9421c68` | **both** — the doc explicitly splits prod (`e9421c6`) from local (`fa47850d`) |
| cms | `stack-demo/cms` | `ca50c817` | `f38c0c4a` | both, as the doc names them |
| jobsimulation | `stack-demo/jobsimulation` | `462343b0` | `82cb66ec` | the claim names `6092c6d2`, so graded there (it is an ancestor of origin/main, **not** of the checkout) |
| storage | `stack-demo/storage` | `4ce8ece5` | `9f8cb532` | `4ce8ece5` — the ref the doc names |
| roadrunner | `stack-demo/roadrunner` | `87d8d443` | `87d8d443` | checkout |
| studio-desk | `stack-demo/studio-desk` | `41ee3575` | `41ee3575` | checkout |
| ant-academy | `stack-demo/ant-academy` | `22df69dd` | `22df69dd` | checkout |
| **rosetta-extensions — per-stack CONSUMPTION clone (pinned)** | `stack-demo/rosetta-extensions` | **`09d06070`** | `4cb920aa` | **this is the tree I used for every "what the tooling does" claim** (§5 rule 45) |
| rosetta-extensions — authoring copy | `.agentspace/rosetta-extensions` | `43049308` | — | used only to confirm the `backend.internal.anthropos:8081` census is identical in both trees |

Corpus HEAD: branch `m257x/platform-realignment`, tip `04cbcfc`. No file edited except this report.

## Positive controls

| file | `wc -l` | read in full? |
|---|---|---|
| `corpus/services/studio-room.md` | 512 | yes |
| `corpus/services/backend.md` | 396 | yes |
| `corpus/services/graphql-wundergraph.md` | 280 | yes |
| `corpus/services/messenger.md` | 207 | yes |
| `corpus/architecture/frontend_architecture.md` | 116 | yes |
| `corpus/architecture/README.md` | 38 | yes |

(Also read, per the briefing: `corpus/ops/platform-alignment.md` §5 in full — 2,803 lines total.)

Search-pipeline controls run in the same pass, per §5 rules 1–6 and 44:

- **Negative asserted with a paired positive.** `SkillPathSessionService` in `app` Go @ `ad9f3c49` → **0** (`rc=1`); control `JobSimulationService` in the same invocation → non-empty. `MESSENGER_RPC_ADDR` → **0 across all 15 git trees**; control `SKILLER_RPC_ADDR` in the identical loop → `app 21 · cms 12 · jobsimulation 9 · messenger 4 · platform 1 · rext 20`. `anthropos-work/authn` in every `go.mod`/`go.sum` → **0**; control `anthropos-work/colony` → 7 clones × 3.
- **Nested repos enumerated before any tree-wide zero.** `find stack-demo -name .git -maxdepth 4` returns **15** trees (13 top-level + `app/studio` + `cms/studio`); every per-tree number below names its own ref.
- **NUL-byte trap measured, not invoked.** `apps/web/src/hooks/useCoursebuilder.ts` @ `8297c684` carries **1** NUL in 55,476 bytes (`tr -dc '\000' | wc -c`), so `git grep` skips it. I re-read it NUL-stripped: **0** `NEXT_PUBLIC_BACKEND_API_URL`, **0** `fetch(`, **0** `EventSource`. It therefore changes none of `frontend_architecture.md`'s counts — the doc's figures survive the instrument that would have broken them.
- **Gitignored-but-tracked measured.** `app/studio/.gitignore:2` = `tools/`; the `mistralai` predicate returns 22 via the nested repo's own `git grep` and 1 via bare recursive `grep` — the exact 1/0/22 split §5 rule 44 describes.

---

## Blockers

**None booked.**

I hesitated on three items below and resolved each downward only after opening source. Each is recorded in *Minors* with the evidence, so a coordinator can re-grade upward without re-deriving. Per §5 rule 22/24 I treat this zero as the **weakest** possible output and flag it as such: a zero names nothing and is equally consistent with a clean partition and a poor pass. What partially offsets it here is that the clearances below are **ENUMERATED**, not re-derived — I rebuilt the predicate's set from source before comparing any cardinality.

---

## Minors

1. **`corpus/architecture/frontend_architecture.md:63`** — *"ESLint + Prettier + TypeScript 5.9, orchestrated by Turbo (`turbo check`, `check:lint`, `check:types`, `check:deprecations`)"*. Three of the four are Turbo tasks (`turbo.json:99`, `:104`, `:109` @ `8297c684`); **`check:deprecations` is not** — it is a root `package.json:35` script invoking `eslint --config configs/eslint-config/deprecations.mjs --no-config-lookup` directly, and `turbo.json` declares no such task. *Hesitation:* `turbo check:deprecations` would fail for a reader who took the sentence literally. Left MINOR because `pnpm check:deprecations` — the form the repo's own `.claude/commands/deprecations.md:7` documents — works, so the reader recovers in one step.
2. **`corpus/architecture/frontend_architecture.md:27–35`** — the *Core Packages* table lists 5 entries and omits **`packages/design`** (`@anthropos/design`, real workspace package, `packages/design/package.json` @ `8297c684`). Omitted list member; the table is not framed as exhaustive.
3. **`corpus/services/messenger.md:123`** — *"the binary still constructs four Connect-RPC clients — CMS, backend users + organizations, skiller, and jobsimulation (`cmd/root.go:118-142`)"*. The cited range constructs **five** client objects (`cmsV1Client` :118, `usersClient` :123, `organizationsClient` :128, `skillerClient` :133, `jobsimulationClient` :138) reading **four** distinct addresses (`BACKEND_USERS_RPC_ADDR` is read twice, at `:125` and `:130`). *Hesitation:* the noun is "clients" and the count is of addresses. Left MINOR because the dash-list immediately disambiguates by writing "backend users + organizations" as one item, and every other statement of the four in this file and in `backend.md` is address-scoped and correct.
4. **`corpus/services/backend.md:314`** — the `lab_sessions` field enumeration omits **`model`** (`internal/data/ent/schema/lab_session.go:70` @ `ad9f3c49`). The passage is explicitly historical ("Phase B PR 2, #896"), so the omission may be accurate as of that PR; flagged only because the list otherwise reads as a schema inventory.
5. **`corpus/services/studio-room.md:352–362`** — the reproduced `requirements.txt` places `mistralai` third; in the file @ `aeec036a` it is at **line 8** (the tracked order is `openai, anthropic, rich, pyyaml, requests, jinja2, <blank>, mistralai, <blank>, pytest, pytest-asyncio`). No factual consequence — the doc separately and **correctly** cites `requirements.txt:8` at `:368` — but the block is presented as the file's contents.
6. **Cross-file inconsistency where MY file is the correct side** (recorded so the repair routes outward, not inward — §5 rule 19's corollary). `corpus/architecture/README.md:21` states *"only **three** are imported as private modules by a service a stack builds (`colony`, `proto`, `taxonomy`)"* and I **measured it true**. Two sites outside the audited scope still say **four** (including `ai`):
   - `corpus/README.md:29` — *"only **four are imported as private modules** (`ai`, `colony`, `proto`, `taxonomy`)"*
   - root `CLAUDE.md:248` and `CLAUDE.md:413` — same claim, and `CLAUDE.md` is the highest-propagation site in the tree.

   Ground truth: `git show HEAD:go.mod` in each clone at its own ref — `app@ad9f3c49` requires `colony v0.35.2`, `proto v1.210.0`, `taxonomy v1.2.0` (plus `analytics-go`, `storage`) and **no `anthropos-work/ai`**; `sentinel@f2c46190` requires `colony`, `proto`, `taxonomy` (indirect). Only the frozen husks still require `ai`: `cms@ca50c817` and `jobsimulation@462343b0`, both `ai v1.40.2`. `app` folded it in-tree at `1e457fa70` *"refactor(ai): fold the ai library into app as internal/ai"* (2026-08-04) → `app/internal/ai/`. `corpus/architecture/service_taxonomy.md:175` already carries the corrected form, so the in-scope corpus is uniform and only the out-of-scope twins are stale.

---

## Positively cleared (audited zeros)

Labelled per the briefing. **ENUMERATED** = I rebuilt the predicate's set from source and compared cardinalities. **RE-DERIVED** = I only re-ran the arithmetic the document displays (the weakest clearance).

### `corpus/services/backend.md`

- **ENUMERATED — the RPC mux carries exactly six handlers, three of them from folded services** (`:29–34`, `:106`). `git grep "v1connect.New.*ServiceHandler" ad9f3c49 -- '*.go'` (non-test) returns exactly six `mux.Handle` registrations: `main.go:1297` Users, `:1298` Organizations, `:1306` Skiller, `:1314` JobSimulation, `:1322-1323` CMS behind `if cmsRPCServer != nil`, `:1337-1338` LabSession. The pinned twin `:1185-1228` @ `b948604f` resolves line-for-line (`:1187`, `:1188`, `:1196`, `:1204`, `:1212-1214`, `:1228`). Of the **eight** folded services, exactly `skiller`/`jobsimulation`/`cms` appear = 3. The negative half is also enumerated: `storagev1connect`/`messengerv1connect`/`roadrunnerv1connect` appear in `app` Go **only inside `_test.go` files as CLIENT types**, never as a handler.
- **ENUMERATED — `SkillPathSessionService` is 0 in `app` Go @ `ad9f3c49`**, and no `skillpath…v1connect` package is imported (both with the `JobSimulationService` positive control in the same invocation). The `⚠️` note that `app`'s **own** docs still list it is also true: `app/CLAUDE.md:109` and `app/knowledge/architecture.md:28` both name it, verbatim, at `ad9f3c49`.
- **ENUMERATED — five Redis publishers / six subscribers.** Every `pubsub.NewPublisher` in `app` @ `b948604f`: `main.go:287` (`serviceName`, defaulting to `backend` at `:230-232`), `:637` `SKILLPATH_STREAM`, `:1039` `CMS_STREAM`, `jobsimwiring/wiring.go:127` `AI_USAGE_STREAM`, `:180` `JOBSIMULATION_STREAM`. The same five @ `ad9f3c49` sit at `main.go:325`, `:746`, `:1149`, `wiring.go:132`, `:185` — exactly as the doc says. `SKILLER_STREAM` has **no** publisher at `b948604f`, `2035f9a4` or `ad9f3c49`; it has a subscriber (`main.go:1276` `AddSubscriber` @ `b948604f`; `subs[d.Streams.Skiller]` at `subscriber_wiring.go:209`, applied by the single loop at `main.go:1579-1581`, @ `ad9f3c49`). `subs[d.Streams.Backend]` is at `:248` and the self-stream comment at `:112-113` reads verbatim as quoted.
- **ENUMERATED — the `backend.internal.anthropos:8081` census: 6 occurrences, 0 in terraform.** Per-tree `git grep` at each clone's own HEAD: `app` **1** (`knowledge/service-dependencies.md:52`, past tense, under the heading *"There are no external callers of app's RPC mux left."*) + `stack-demo/rosetta-extensions@09d06070` **5** (4 in the frozen `repair_leak/{pre,post}` cms.md fixtures, 1 in `test_platform_predicate_guard.py:435`). The authoring copy `43049308` returns the same 5. The `.tf` denominators reproduce exactly: **44** tracked `.tf` across the 13 top-level clones (`app` 5, `cms` 4, `graphql-wundergraph` 4, `jobsimulation` 6, `messenger` 4, `next-web-app` 2, `roadrunner` 4, `rext` 2, `sentinel` 4, `storage` 5, `studio-desk` 4), **59** by raw `find`. Zero terraform hits either way.
- **ENUMERATED — `SKILLER_RPC_ADDR` occurrence ladder.** `docker-compose.yml`: **4** @ `0808b92` (`:89 :151 :204 :334`), **4** @ `2adcf71`, **1** @ `d11a403` (`:168`) and **1** @ `0dab54d` (`:183`), **0** @ `0c91421d`. And the load-bearing predicate, not just the count: `git show d11a403 -- docker-compose.yml` shows exactly **two** `+` lines, `CMS_RPC_ADDR` and `JOBSIMULATION_RPC_ADDR`, both to `http://backend:8083` — `SKILLER_RPC_ADDR` already read that value at `d11a403^`. The doc's insistence that `d11a403` moved two, not four, is correct. `git grep -E '[A-Z_]*_RPC_ADDR' 0c91421d` over the whole platform repo returns **two lines, both in `CLAUDE.md` prose saying the variables do not exist** — so "compose sets zero" is true of `docker-compose.yml`, `common.yml` and `.env_example` alike.
- **ENUMERATED — migrations.** `terraform/migrations/*.sql`: **170** @ `ad9f3c49`, **169** @ `b948604f`. Head @ `ad9f3c49` = `20260803143844_ai_readiness_recommendation_path.sql`; head @ `b948604f` = `20260731154527_academy_chapter_progress_completed_at.sql`, preceded by `20260731131307.sql`, `20260729133514.sql`, `20260728103254_ai_readiness_snapshot_frozen_matched_sources.sql`. **46** land after 2026-05-31. `git ls-tree b948604f migrations/` is empty and `6a46e8445` (2026-06-18) is the commit that deleted `migrations/atlas.sum`. `atlas.hcl` reads `dir = "file://terraform/migrations"` / `src = "ent://internal/data/ent/schema"`.
- **ENUMERATED — 13 `ai_readiness_*` tables.** Two independent derivations agree: 13 Ent schema files under `internal/data/ent/schema/ai_readiness_*.go`, and 13 distinct `CREATE TABLE "ai_readiness…"` names across `terraform/migrations/`. All four the doc names as the "omissions" (`recommendations`, `email_overrides`, `notification_logs`, `notification_optouts`) are in the set, and the two it says are **not** omissions (`live_snapshots`, `text_translations`) are also there. The engine is `internal/aireadiness/` (with `manager.go`, `cycles.go`, `diagnosis.go`, `compare.go`, `csv.go` all present) and `internal/workforce/` contains **no** `readi*` file — verified against a non-empty listing of that directory as the control.
- **ENUMERATED — `categoryTree`/`fullCategoryTree` were dropped, not ported.** Each returns exactly **1** hit in `app`'s 43 `.graphqls` files, and both hits are the same **comment line** `skiller_taxonomy.graphqls:7` — *"The remaining ones (…, categoryTree, fullCategoryTree) stay unported — no consumers."* No `type`/field declaration exists. This is the case §5 rule 10 is about: the substring is real and the conclusion drawn from it alone would be the opposite of the truth.
- **ENUMERATED — the "3 → 1" subgraph step.** `supergraph-config-prod.yaml` `- name:` entries: `749dc86~1` = 5 (backend, skiller, jobsimulation, cms, skillpath), `749dc86` = 4, `7c17e63` = 3, `915da06~1` = 3, `915da06` = **1**. `git show --name-status 915da06` marks **both** `schemas/cms.graphqls` and `schemas/jobsimulation.graphqls` `D`. Its own subject line does read *"supergraph 2→1"* and is wrong, exactly as the doc warns.
- **ENUMERATED — every `file:line` in the storage/messenger/roadrunner "own muxes" sentence.** `storage@4ce8ece5:cmd/root.go:62-66` is the `rpcMux.Handle(storagev1connect.NewStorageServiceHandler(rpcsrv.New(...)))` block; `sdk/storage/v1/service.go` is **47 lines**, `type Service interface {GetObject, PutObject, GetPresignedUrl}` at `:13-17`, `NewService` at `:19`, `NewPublicService` at `:34`, every `StorageService` token a `…Client` — the doc's own retraction of that citation is itself correct. `messenger@fa47850d:internal/rpcsrv/rpcsrv.go` declares `messenger.v1.MessengerService`; `roadrunner@87d8d443:cmd/root.go:87` is the `NewRoadRunnerServiceHandler` line.
- **ENUMERATED — the M810 unevenness, graded at the refs the doc names.** `cms/terraform/main.tf:39` is `service_desired_count = 0` at **both** `ca50c817` and `f38c0c4a`, and the module block is still declared. `jobsimulation`: the claim names `6092c6d2` and is graded there — at that commit `terraform/main.tf:15-22` is the M810 comment recording the `module "jobsimulation"` deletion (ECS service, task def, ECR repo, IAM roles, SG, Cloud Map entry, log group, alarms) and `:24-40` is the *"What this module still owns"* block naming livekit.tf / chime.tf / ssm.tf and the atlas tracker. Worth stating for the record: at the **checkout** `462343b0` the module block is still present at `:31` — `6092c6d2` is an ancestor of `origin/main` (`82cb66ec`) but **not** of the checkout. Under §5 rule 33 the claim is settled at the ref it names, so it stands; a reader who graded it against the checked-out tree would reach the opposite verdict, which is precisely why the doc's pin is load-bearing rather than decorative.
- **ENUMERATED — compose/Makefile anchors @ `0c91421`.** `AUTHORIZATION_ADDRESS=http://sentinel:8087` at `:48`; `GOTENBERG_URL=http://gotenberg:3200` at `:57`; `JUDGE0_BASE_URL` at `:59`; `backend` `profiles: [core, backend, all]` at `:110` (and at `:100` @ `0dab54d`, as claimed); `gotenberg` `profiles: [core, backend, all]` at `:183`; `Makefile:10` `PROFILE ?= core`. Ports: compose publishes 8081/8082/8083 and sets `PORT=8082`, `RPC_PORT=8083`, `META_PORT=8084` — so "8081 is reserved/unused" holds. `app/internal/converter/gotenberg.go:31` is the Gotenberg HTTP POST.
- **ENUMERATED — storage-in-app.** `main.go:524` `internalstorage.NewManager` / `:525` `NewPublicManager`; `:504` is the comment *"…STORAGE_RPC_ADDR is gone"*. `STORAGE_RPC_ADDR` in `app` **Go** = exactly 3, all comments (`main.go:504`, `jobsimwiring/wiring.go:101`, `storagens/callsites_test.go:189`). `git diff 2035f9a4 ad9f3c49 -- main.go` is empty, so the doc's byte-identical claim holds and every `2035f9a4`-pinned line number resolves at `ad9f3c49`.
- **ENUMERATED — app terraform derivation.** `locals.tf:6` `project = "backend"`, `:8` `rpc_port = 8081` mapped at `main.tf:185-186`; `main.tf:58` takes `service_discovery_namespace_id` as an input; `variables.tf:197` `cms_rpc_address` / `:230` `storage_rpc_addr` @ `b948604f`, and at `ad9f3c49` `cms_rpc_address` has moved to `:309` while `storage_rpc_addr` is **deleted** and replaced by `storage_s3_bucket` (`:349`) / `storage_s3_public_bucket` (`:354`). `messenger/terraform/variables.tf:77,82,87,92` are the four `*_rpc_address` variables, none with a default.
- **ENUMERATED — LabSession client wiring.** `main.go:743-746` @ `b948604f` is the `if labsAPIURL := os.Getenv("LABS_API_URL"); labsAPIURL != "" {…}` block; `internal/labs/{adapter,catalog,labsapi,session}` all exist; the `lab_sessions` Ent schema carries every field the doc lists (plus `model`, see Minors).
- **ENUMERATED — the key-directories block.** Every directory named exists under `internal/` @ `ad9f3c49`, including the five `askengine` files (`rules.md`, `bedrock.go`, `sandbox.go`, `executor.go`, `followups.go`) and `internal/academy/academy.go:6-9`, which states the `internal/aiacademy` removal in those exact terms; `internal/aiacademy` is indeed gone.
- **ENUMERATED — `DIRECTUS_WEBHOOK_SECRET` fails closed.** `main.go:1198-1199` (`// … fail-closed` + `cmswebhooks.Handler(os.Getenv("DIRECTUS_WEBHOOK_SECRET"), …, cmsPub, …)`), which is also the evidence for *"the one external producer left is Directus, feeding the `cms` stream."*
- **RE-DERIVED only** — the 60 s RPC write timeout (`main.go:1353` `rpc.WithWriteTimeout(60*time.Second)` against `internal/rpc/server.go:39`'s 10 s default) and the historical refs `c3c45e01` (v1.334.1), `1fc00c78` ("Deprecate skiller schema", 2026-06-29), `20260518125439.sql`, `cms 20250116133510.sql`. All resolve; I did not independently re-enumerate the sets they sit in.

### `corpus/services/graphql-wundergraph.md`

- **ENUMERATED — exactly five profile tokens, and the "eight" it corrects.** Distinct tokens across `docker-compose.yml` + `common.yml` @ `0c91421`: `core`, `backend`, `all`, `studio-desk`, `frontend` = **5**. @ `0dab54d` the set is those five plus `storage-legacy`, `customerio-sync`, `messenger` = **8**, exactly as the parenthetical says. `postgresql` and `redis` (`common.yml`) and `sentinel` (`docker-compose.yml:5-26`) declare **no `profiles:` key**, so the always-on floor really is those three and a retired token really does exit 0 while starting them. `Makefile:10` read `PROFILE ?= graphql` at `2adcf71` and reads `PROFILE ?= core` at `0c91421`.
- **ENUMERATED — the subscription retraction.** The `subscription: protocol: "sse_post"` block sits on the `jobsimulation` entry **alone** at `749dc86~1` … `915da06~1` and disappears with that entry at `915da06`; **no** `supergraph-config-*.yaml` @ `60c229f3` contains `subscription`. `schemas/backend.graphqls` declares `type Mutation` (`:4053`) and `type Query` (`:4912`) and **no `type Subscription`**; the residual `Subscription` substrings are `PlanSubscription`, `activeSubscription`, `stripeSubscriptionId`, `stripeSubscriptionItemId` — all Stripe/plan names, as characterised. `bba862f` (2026-02-25) exists only on `remotes/origin/feat/use-web-socket`.
- **ENUMERATED — repo facts @ `60c229f3`.** `terraform/main.tf:20` `service_desired_count = 1`; `terraform/locals.tf` `aws_region = "eu-west-1"`, `project = "wundergraph"`, `port = 8080`, `health_check_path = "/health"`. `subgraphs.conf` is the single 16-byte line `BACKEND=v1.360.0\n` and the repo's own `CLAUDE.md:70-79` *"Version Tracking"* section says exactly that — so the doc's M257x-iter-115 retraction (that this section is **not** stale) is correct; `git log -- CLAUDE.md` shows `60c229f` itself last rewrote it. `CLAUDE.md:85` **is** stale: it says `-i supergraph-config-local.yaml`, and the tracked variants are `-compose`, `-dev`, `-prod` only. `schemas/` holds `backend.graphqls` alone. `package.json` is `{"name": "graphql-wundegraph"}` and the repo `CLAUDE.md` heading carries the same misspelling. Pins: `ghcr.io/wundergraph/cosmo/router:0.275.0`, `wgc@0.104.0`, `node:22.11-alpine`, `federation_version: =2.3.2`. `config.compose.yaml` has `listen_addr: 0.0.0.0:8080` / `graphql_path: /graphql` / playground+introspection **on**; `config.prod.yaml` has both **off**. `ci/update-subgraph.sh` is 9 lines and carries **exactly one** `gh release download`, at `:9`, `-R anthropos-work/app`. `terraform/tests/` exists and there is no other test dir. Routing URLs: compose `http://backend:8082/graphql/query`, dev `http://host.docker.internal:8082/…`, prod `http://backend.internal.anthropos:8080/…` — the doc's "dev uses host.docker.internal / prod shares container port 8080" is exact.
- **ENUMERATED — `grep -c 5050 docker-compose.yml` @ `0c91421` returns 0.** Both self-citations in this file resolve to the construct they name: `:193` is the *"`http://localhost:5050` refuses the connection"* sentence, and `:114-117`/`:116-117` are the *Build-time, static composition* bullets including the struck-through *"`make up` rebuilds `graphql`"*; `:84` is the Ports bullet, which is what the note says it is **not**.
- **ENUMERATED — compose consumer anchors.** `:135` studio-desk `VITE_GRAPHQL_ENDPOINT` (env), `:119` the same as a build arg; `:160` next-web-app `NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT` (env), `:151` build arg. All four @ `0c91421`.
- **ENUMERATED — the platform-side history.** `b56d731` *"drop the WunderGraph router; point local dev at backend"* + `360efd4` *"remove the router service and clone entry outright"*, merged `2adcf71` (PR #23) — and `repos.yml` @ `2adcf71` has **9** entries with `graphql-wundergraph` absent, while `2adcf71^` still carries it as `type: node-npm`, which is exactly what `:273` claims.
- **Could not settle:** `:8` *"the repo is ARCHIVED on GitHub, 2026-07-30 (read-only)"* — archived-state is a GitHub API property, not a git one. Latest commit is dated 2026-07-30, which is consistent, not confirming. `gh api repos/anthropos-work/graphql-wundergraph --jq .archived` would settle it.

### `corpus/services/messenger.md`

- **ENUMERATED — 21 handlers from 22 lines.** `internal/flow/flow.go:72-104` @ `fa47850d` contains exactly **22** `pubsub.EventHandler(…)` lines, of which exactly **one** — `OrgJobSimulationAssignmentPastDueHandler` at `:83` — is commented out with `// not implemented`. 22 − 1 = 21. `:74-78` are exactly the five `OrgSkillPath*` handlers, as `:123` claims.
- **ENUMERATED — `MESSENGER_RPC_ADDR` is in no repo.** 0 across all **15** git trees at their own refs, nested `app/studio` and `cms/studio` @ `aeec036a` included, with the `SKILLER_RPC_ADDR` control non-zero in six of them in the same loop. `git -C stack-demo/platform log -S 'MESSENGER_RPC' --oneline 0c91421d | wc -l` = **0** over a **121**-commit history; control `-S 'SKILLER_RPC'` = **7**; with `--all` they become **0** and **8**, and the 8th is `464dfe3` (confirmed by `comm` against the `0c91421d`-reachable set) — every figure in that paragraph, including the scope caveat, reproduces.
- **ENUMERATED — the prod/local ref split, which is the sharpest thing in this file.** At `fa47850d` (the frozen local clone) `terraform/main.tf:19` is `service_desired_count = **1**` and `:29` is `container_definitions = <<EOF`. At `e9421c68` (`origin/main`) `:29` is `service_desired_count = **0**` and `:27-28` is the *"The image and task definition stay declared: this is the rollback path. Restoring service is a one-line revert plus an apply, not a re-provision."* comment — quoted almost verbatim in the table cell. Grading the prod row at the checkout would return the opposite verdict, exactly as the doc warns.
- **ENUMERATED — the consumer row's anchors @ `ad9f3c49`.** `main.go:15` `msgflow`, `:62` `msgadapters`, `:63` `msgsender`; `:1450` `pubsub.NewSubscriberServer(msgflow.ConsumerGroup, …)` (a **second, dedicated** server, gated by `if messengerEnabled` at `:1445`); `:1471` `msgadapters.Wire(...)`; `:1473` `Sender: msgsender.NewFromEnv(logger)`; `:1416-1421` the *"group name is messenger's, and it is a LITERAL on purpose … `cmp.Or(os.Getenv("SERVICE_NAME"), "messenger")` … nothing in terraform ever set SERVICE_NAME"* comment. `env_guards.go:61` is `envMessengerEnabled = "MESSENGER_ENABLED"`, and `git ls-tree b948604f -- env_guards.go` is **empty** — the file genuinely did not exist at the former pin, so the doc's insistence on the ref is load-bearing.
- **ENUMERATED — the console-sender non-port.** `main.go:295` is `if (messengerEnabled || customerIOSyncEnabled) && os.Getenv("BREVO_KEY") == ""` and `:296` the `log.Fatalf`; the comment above states *"the console fallback is deliberately not ported."*
- **ENUMERATED — the standalone binary's own numbers.** `cmd/root.go:63` `PORT` default `8080`, `:64` `RPC_PORT` default `8081`, `:107` `REDIS_STREAMS_INDEX` default `"2"`, `:147` `READONLY_DB_CONNECTION` under the comment *"Copilot read-only DB connection"*. `internal/rpcsrv/rpcsrv.go:25-30` holds both `CancelScheduledMessage` and `Schedule` returning `CodeUnimplemented`. `internal/flow/jobsimulations.go:140-151` is exactly the 2 h / 12 h staleness pair. `internal/flow/assignments.go:828` is the `h.cms.GetSkillPath(...)` call inside `getSkillPath` (declared at `:827`). `go.mod`: `go 1.25.0`, `getbrevo/brevo-go v1.1.3`, `osteele/liquid v1.8.1`. The *Key directories* block matches the tree file-for-file.
- **ENUMERATED — `BACKEND_USERS_RPC_ADDR` was never re-pointed.** `git log -S` back to `3e85fce` (2024-04-18, *"add messenger service to docker compose"*) shows it introduced as `http://backend:8081` — same host from birth, only the port moved. `:185` says exactly this.
- **ENUMERATED — `docker-compose.yml:84-92` @ `0c91421`** is the nine-line `messenger-in-app … deliberately have NO variables here … Pinning them to `false` here would override .env` comment on the `backend` block, precisely as `:148` describes.

### `corpus/services/studio-room.md`

- **ENUMERATED — `gen.py` registers exactly nine arguments.** `gen.py:484-492` @ `aeec036a` holds nine `parser.add_argument(...)` calls and `grep -c add_argument` over the whole file is also **9**, so the list is not merely nine-in-a-range but nine-in-the-file. Every flag, `dest`, default and `choices` in the doc's block matches (`-f` → `dest="forced"`; `--branch` `choices=['stable','experimental']` default `stable`; `--annotations` default `"{}"`; `--pipeline` default `linear`).
- **ENUMERATED — the `--template` swallow, in both directions.** `parse_argument` at `gen.py:18-28` really does `parse_known_args()` and fold unknown `--key value` pairs into the request dict; `validate_blueprint_exclusivity` at `:241-271` really does raise `"Cannot combine --blueprint with content parameters: …"`, and its `allowed_with_blueprint` set is exactly `{blueprint, media, forced, interactive, branch, pipeline, attachments_path, worklog_path, postgen_path, published_path}` — "only execution controls … and the four `*_path` overrides". `translate_legacy_blueprint` at `:205-238` pops `template`, and `_LEGACY_TEMPLATE_DEFAULTS` names exactly three keys: `micro challenge`, `scenario challenge`, `collaborative challenge`; anything else hits the *"is ignored; asset type is now inferred from task interactions"* warning, verbatim.
- **ENUMERATED — the `mistralai` predicate, with all three instruments.** `git grep -ci mistral aeec036a` (the nested repo's own ref) = **22** lines across **3** files: `requirements.txt` 1, `tools/pdf2md.py` 18, `tools/r3.py` 3. `pdf2md.py:24` is `from mistralai import Mistral`, `:96` `client = Mistral(api_key=api_key)`, `:127` `model="mistral-ocr-latest"`; `r3.py:194` is the `if "MISTRAL_API_KEY" not in env:` hard requirement. `services/ai.py:1-2` imports only `openai`/`anthropic`, and `get_client`'s provider registry at `:705-708` is exactly `{'openai', 'azure', 'anthropic'}`. Both ignore rules are real: `app/studio/.gitignore:2` = `tools/` and `app/.gitignore:79` = `studio/*`, and `cms/.gitignore:129` = `studio/*` hides the second copy at the same sha. Go execs exactly **two** studio scripts — `studio/gen.py` at `studioManager.go:119` and `studio/postgen.py` at `:1045`, both via `runCommand` — and neither is `pdf2md.py`.
- **ENUMERATED — the subprocess/venv contract.** `runCommand` is declared at `studioManager.go:1099` with its *"argv (exec) form — NEVER through a shell"* doc-comment at `:1096-1098`, and `exec.CommandContext(ctx, name, args...)` at `:1101`; the const block at `:93-96` defines `studioVenvDir = "studio/studio-venv"`. `internal/cms/worker/worker.go:29` sets `Concurrency: 5` and `:32`/`:33` set `AiVideoQueue: 7` / `StudioQueue: 3` — priority weights on one shared worker, exactly as the doc distinguishes.
- **ENUMERATED — the CI embed.** `.github/workflows/build-production.yml:29-30` @ `ad9f3c49` sets `additional_repo: "anthropos-studio-room:studio"` / `additional_repo_commit: latest`; `git tag --contains 851cf3fb1` (the commit that added it) returns `v1.360.1` as its **first** tag — so *"(`additional_repo`, app v1.360.1)"* is exact. `Dockerfile:45-46` copies `studio/` and pip-installs `studio/requirements.txt`; the runtime base is `FROM python:3.11-slim` at `:28`.
- **ENUMERATED — the flat output layout.** `gen.py:450-456` builds `work_paths` from the four `*_path` keys with a bare-name fallback (hence *"the in-code fallback is a literal `worklog/`"*). `exporter.py:518-519` are the `postgen_zip` / `published_zip` lines and `:513-550` is the whole `_create_export_package`, which unpacks into `published/{id}_simulation/`, preserves `collaborative_*`/`asset_*`/`internal_*` JSON (`:537`), writes `simulation.json` (`:541`), re-archives (`:547`) and `rmtree`s (`:550`) — and names the zip for `raw_data['source_id']` falling back to `sim_id` (`:517`). No `dialogue.json` / `scenarios.json` / `guidance.md` / `translations/` is written anywhere.
- **ENUMERATED — configs.** `max_tokens = 4000` in **all three** tracked configs (`config_template.ini:22`, `development_config.ini:21`, `production_config.ini:21`); the five `*_AI_STABLE_MODEL` rows are byte-identical to the doc's block, and a matching `*_AI_EXPERIMENTAL_MODEL` row exists per mode. `configs/local_*` and `configs/test_*` are gitignored (`.gitignore:5-6`). Shipped `worklog_path = workspace/trace` etc. match the *Output Structure* tree.
- **ENUMERATED — `cert.py` is the certification-benchmark CLI**, and the "0 hits for certificate/signing/private_key/x509" clause survives inspection: the only three case-insensitive matches in the whole repo are the substring inside *"Designing"*/*"designing"* (`story.py:123`, `:1071`, `knowledge/development/gaps.md:333`). Not one is a token match, so the claim is substantively right — and I opened them rather than trusting the count, because the count alone would have refuted it.
- **ENUMERATED — repo structure and worked examples.** Every top-level file/dir the tree-block names exists. `tests/e2e/blueprints/` holds exactly `b2b`, `business`, `none_chat`, `none_voice`, `technical`; `knowledge/development/asset-examples/blueprints/` holds exactly one each of code / micro / scenario. Every key quoted from `technical.json` is present with the quoted value. `CLAUDE.md:12-14` is the `python gen.py --media simulation --prompt "..." --evaluation_skills "…" --branch stable` fence. State files: `gen.py:116-117` write `{simid}_pre_generation.json` and `{simid}_task_state.json`; `:458` names `workspace/trace/<simid>_usage.json`. `model.py:59` is `self._cache = {}` and `:467-469` the skill-id → `TaxonomyService.get_skill_by_id` memo — the only cache. `services/taxonomy.py:11` is `BASE_URL = "https://api.anthropos.work/api"`.

### `corpus/architecture/frontend_architecture.md`

- **ENUMERATED — every figure in the corrected counts table, reproduced independently.** Over non-test `.ts`/`.tsx` at `8297c684` with `e2e/` excluded: **22** files naming `NEXT_PUBLIC_BACKEND_API_URL`, **31** occurrences, **47** `fetch(` sites in those files, and the four `packages/core-js` clients contribute **12** env / **27** calls. I did not take the sum from the doc's set — I rebuilt the file set first (all 34 hits repo-wide, then the exclusions), then counted. The prose enumeration at `:39` also partitions to exactly 22: 4 core-js + 3 AI-readiness + 1 invite + 1 unsubscribe + 1 member-analytics + 4 assignment-builder/ask (web **and** hiring) + 3 Stripe + 1 CSV import + 1 résumé import + 1 test-run network + 2 admin backfill.
- **ENUMERATED — `new EventSource(` is 0 everywhere, and I closed the NUL hole before saying so.** A NUL-tolerant scan of every tracked `.ts/.tsx/.js/.jsx` file at `8297c684` (piping each blob through `tr -d '\000'`) returns zero hits — including `apps/web/src/hooks/useCoursebuilder.ts`, the one NUL-bearing source file, which `git grep` silently skips. `packages/core-js/src/talkToData/api.ts:214` reads *"uses POST so we can send a JSON body — that rules out EventSource"*, matching the quote.
- **ENUMERATED — monorepo facts @ `8297c684`.** `apps/` = web/hiring/integration/mobile/maintenance; dev ports 3000/3001/3002 from each `package.json` `dev` script, and `3031` appears exactly once, in `apps/mobile/package.json:70` (`expo start --port 3031`). `pnpm-workspace.yaml` carries `- '!apps/mobile'` with the comment *"exclude mobile app to speed up development"*. `apps/web/package.json:46` is `"next": "~16.2.12"` — **tilde**, patch `12` — and `hiring`/`integration`/`maintenance` carry the identical string while `mobile` declares no `next`. `UPGRADE-IMPACT-next16.md` is at the repo root. Root `package.json`: `"packageManager": "pnpm@10.30.3"` (`:20`), `engines.node ">=24.0.0"` (`:14`), `typescript "^5.9.3"` (`:11`). `configs/i18n/messages/` holds exactly the eight locales `de en es fr it ja nl pt`; `configs/tsconfig` and `configs/i18n` publish as `@anthropos/tsconfig` / `@anthropos/i18n`. `packages/graphql/codegen.ts:11` sources `src/query/**/*.{ts,tsx}` and `:13` emits to `./src/__generated__/`. `SkillCard.tsx`, `SkillCardCallout.tsx` and `Onboarding/User/SkillsRefinement.tsx` all exist. Commit velocity: **455** commits in July 2026 and **340** in June — "300+ / month" holds.
- **ENUMERATED — the repos.yml / compose claims in the note.** `repos.yml` @ `0c91421` holds exactly **4** entries (`app`, `sentinel`, `next-web-app`, `studio-desk`) and **9** @ `2adcf71` (the extra five: `cms`, `jobsimulation`, `storage`, `messenger`, `roadrunner`); `ant-academy` is in neither. `docker-compose.yml:141` @ `0c91421` is `profiles: [studio-desk, all]`, and `:226` @ `0dab54d` is the same line — the doc's "only the line number keeps moving, it was 226" is exact. `:5050` was never a production port: `graphql-wundergraph/terraform` sets `service_port = local.port = 8080`.

### `corpus/architecture/README.md`

- **ENUMERATED — the private-module count, which is this file's only hard claim.** See Minor 6 for the full derivation. Three (`colony`, `proto`, `taxonomy`) by the two Go services a stack builds; `ai` folded in-tree at `1e457fa70` (2026-08-04); `authn` required by **no** `go.mod`/`go.sum` in any clone (with the `colony` control at 7×3).
- **ENUMERATED — the index is complete.** `corpus/architecture/` holds 11 files; the 10 non-`README` files are each listed exactly once. `platform_alignment_guard.py` exists in **both** rext trees (`stack-core/`), as does `alignment/` and `stack-seeding/dna/`.
- **RE-DERIVED only** — the one-line descriptions of `dependency_map.md`, `security_compliance.md`, `service_taxonomy.md` and `ai_architecture.md`; I confirmed each target file exists and its subject matches, but those files are other seats' and I did not audit their contents.

### Cross-cutting hygiene

- **ENUMERATED — every relative markdown link in all six files resolves** (both `../`-style and same-directory forms), and every intra-corpus `#fragment` I could reach resolves too: `shared_libraries.md#taxonomy-figures` → the explicit `<a id="taxonomy-figures"></a>` at `:228`; `#skiller-in-app-merge--fact-sheet-v21-quick-change` → `backend.md:123`; `external_services.md#graphql-gateway--wundergraph-cosmo-router` → `:358`; `studio-room.md#output-structure` → `:441`.
- `backend.md:62` points the "correctly-scoped model wording" at `architecture_overview.md:335`, which is the **heading line** of that block; the sentence it means (*"the only cross-process RPC edge out of backend on a core stack"*) is three lines further at `:339`. Block-start rather than construct-line — not booked, but it is the citation in these six files most likely to rot next.

---

## What I could not settle

1. **Prod DB cardinalities** — `backend.md:103`, `:137-143` (`public.skills WHERE organization_id IS NULL` = 42,790; `job_roles` = 22,490; `categories` = 23; `specializations` = 1,447; `skill_embeddings` = 43,584, measured 2026-07-08). Not derivable from any clone. Settled only by a read-only prod query via `/db-query`, and the doc already fences them as floors.
2. **GitHub archived state of `graphql-wundergraph`** (`:8`). Needs `gh api repos/anthropos-work/graphql-wundergraph --jq .archived`; git alone cannot see it.
3. **Whether `module.cms_euwest1` has actually been destroyed in production** (`backend.md:76-89`). The `infrastructure` repo is in no clone set, so the doc's own refusal to assert either way is the correct end state — I record it as unsettleable, not as a gap.
4. **`messenger.md:53`'s "the compute is stopped"** — I verified the terraform declaration (`service_desired_count = 0` @ `e9421c6`), not the live ECS state. Declared ≠ applied; only an AWS read settles it.
5. **The `customerio-sync` half of `backend.md:50-51`.** Its repo has never been in a clone set, so *"nothing can be said about its own surface from here"* is exactly right; what I could verify is the measurable half — `app`'s mux carries no handler for it.
6. **Soft/undated editorial claims** I read but cannot falsify: `apps/mobile` being a *"paused PoC"* (`frontend_architecture.md:24`), the *Recent UX Work (May 2026)* themes (`:108-117`), and `backend.md:310-313`'s branch-name feature list (`feat/hiring-talk-to-data` etc. — branches not present in the demo clone, which is a fetch scope artefact rather than evidence of absence).

---

## Method note on this zero

Per §5 rules 22–24 I record what would make this reading wrong rather than asserting the partition is clean:

- A **zero names nothing** and is the least corroborated value a reading can return. The counter-evidence I can offer is that **21 of my clearances are ENUMERATED** — the predicate's set rebuilt from source before any cardinality was compared — and only 6 are RE-DERIVED.
- The three claims in these files whose *arithmetic is displayed* (the mux handler count, the `NEXT_PUBLIC_BACKEND_API_URL` table, the profile-token ladder) are exactly where rule 24 says a wrong audited zero hides. For each I enumerated the set first: the six `mux.Handle` calls, the 22-file list, the 8-then-5 token sets. All three sets, not just their sums, reproduce.
- Two conjunctive claims got both conjuncts tested (rule 17): *"`SKILLER_STREAM` has a subscriber **and** no publisher"* (both halves, at three refs), and *"`MESSENGER_RPC_ADDR` is in no repo **and** `-S 'MESSENGER_RPC'` finds no commit"* (both, with controls).
- The single highest-risk residual I can name: `backend.md`'s `jobsimulation` M810 sentence is **true at the ref it cites (`6092c6d2`) and false against the checked-out tree (`462343b0`)**. It survives under §5 rule 33 and I did not book it — but it is the claim in my set that is one careless re-grade away from being reported as a defect, and one clone advance away from needing a re-pin.
