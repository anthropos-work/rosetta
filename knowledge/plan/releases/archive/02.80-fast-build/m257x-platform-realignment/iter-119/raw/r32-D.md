# Seat D — reading #32

Ground truth re-verified at this seat's open (all match the briefing ADDENDUM):
`app ad9f3c49` · `platform 0c91421d` · `next-web-app 8297c684` · `sentinel f2c46190` ·
`studio-desk 41ee3575` · `ant-academy 22df69dd` · `storage 4ce8ece5` · `messenger fa47850d` ·
`cms ca50c817` · `graphql-wundergraph 60c229f3` · `roadrunner 87d8d443` · `jobsimulation 462343b0` ·
nested `stack-demo/app/studio aeec036a` · rext **pinned consumption** `stack-demo/rosetta-extensions 09d06070`
· rext **authoring** `.agentspace/rosetta-extensions 43049308`.

Every rext claim below states which tree settled it. Every `git grep` reported as an absence was run
with a stated positive control in the same pass.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/architecture/service_taxonomy.md` | 523 | yes |
| `corpus/services/clerkenstein.md` | 374 | yes |
| `corpus/architecture/security_compliance.md` | 293 | yes |
| `corpus/services/sentinel.md` | 166 | yes |
| `corpus/services/ai-labs.md` | 158 | yes |
| `corpus/services/db-backup.md` | 31 | yes |

---

## Blockers

### B1 — `service_taxonomy.md` cites `:142` for the archive-state note; `:142` is a different blockquote

- **Anchor:** `corpus/architecture/service_taxonomy.md:403` —
  *"the GitHub repo was **archived 2026-07-30** (a dated snapshot — see the archive-state note above the
  *Archived / merged* table, `:142`; the clone is consistent with it, no commit after that date)"*
- **Why it is false:** the *archive-state* note (*"Every `ARCHIVED <date>` in this table is a DATED
  SNAPSHOT, not a derived fact."*) begins at **`service_taxonomy.md:148`** and runs to `:160`.
  `:142` lands inside the **preceding, unrelated** blockquote (*"⚠️ Two different fates shared this
  table…"*, `:139-146`); the exact text at `:142` is
  *"> so a bare `make up` started all three as unfederated husks — the `running_but_unfederated` state in"*.
  A reader following the pointer lands on the compose-fates note, not the archive-state caveat the
  sentence is leaning on for its "dated snapshot" hedge — i.e. the anchor resolves but names the wrong
  construct. Measured by direct `sed -n '137,150p'` on the file at rosetta HEAD (line count 523,
  unchanged this reading). This file's own `:156-160` records that its row-number self-pins have
  already rotted **twice**; this is a third live instance of the same class.
- **Confidence:** high

### B2 — `service_taxonomy.md` cites `:67-68` for *"There is no `graphql` profile"*; that sentence is at `:74`

- **Anchor:** `corpus/architecture/service_taxonomy.md:406-407` —
  *"**do not follow it as a local-development instruction.** Consistent with `:67-68` above
  (*"There is no `graphql` profile"*)."*
- **Why it is false:** `service_taxonomy.md:67` is the bullet
  *"- **Communication**: HTTP/RPC + Redis Streams"* and `:68` is the *"- **Database**: PostgreSQL — one
  schema, `public`, owned by `app`…"* bullet. Neither contains the quoted string or anything about
  profiles. The quoted sentence — *"**There is no `graphql` profile, and no cms / jobsimulation /
  roadrunner / storage / messenger / customerio-sync service of any kind.**"* — is at **`:74-75`**.
  The word *"above"* makes the reference unambiguously intra-file, so this is not an ambiguous-target
  case. Measured by direct `sed -n '64,78p'`.
- **Confidence:** high

### B3 — *"the live private-module set a stack builds is colony, proto, taxonomy"* omits two live private modules

- **Anchor:** `corpus/architecture/service_taxonomy.md:175` —
  *"**Shared Libraries** (imported as private Go modules — **not** cloned by `make init`; pulled at
  Docker build via `GH_PAT`/`GOPRIVATE`). **`ai` is NO LONGER among them** … **the live private-module
  set a stack builds is colony, proto, taxonomy**."*
- **Why it is false:** enumerated from source rather than re-derived from the document's own list.
  `app` — the one Go service every stack builds — declares in its **direct** `require` block at
  `app@ad9f3c49:go.mod:14-18`:
  `github.com/anthropos-work/analytics-go v0.3.1`, `…/colony v0.35.2`, `…/proto v1.210.0`,
  **`…/storage v0.15.2`**, `…/taxonomy v1.2.0`. Both omitted modules are real, private
  `anthropos-work` repos pulled at Docker build under the same `GH_PAT`/`GOPRIVATE` mechanism the
  sentence describes, and both are actually imported: `anthropos-work/storage` in **32** Go files
  (`git grep -l "anthropos-work/storage" HEAD -- '*.go' | wc -l` → 32, e.g.
  `internal/cms/wiring.go`, `internal/academy/asset.go`, `internal/jobsimulation/recording/recording.go`),
  `anthropos-work/analytics-go` in 2. So the live private-module set a stack builds is
  **{colony, proto, taxonomy, storage, analytics-go}**, not three.
  **Why I hesitated:** the sentence sits inside a paragraph headed *"Shared Libraries"* whose table
  names exactly five, so a charitable reading scopes the claim to *"of those five, three survive"* —
  which is true, and is how the sibling statements at `service_taxonomy.md:518` and
  `corpus/architecture/README.md:21` are worded (*"3 imported by a service a stack builds"* of the five).
  I booked it because the words as written are an unscoped universal about *the private-module set*,
  and the omission is load-bearing in the direction the corpus is most confused about: `storage` is
  described throughout as a frozen legacy repo *"which nothing clones or builds"*
  (`clerkenstein.md:279`) while `app` compiles against its module on every build.
- **Confidence:** medium

### B4 — `sentinel.md`'s published derivation says its grep *"returns one unrelated hit"*; at the named ref it returns **zero**

- **Anchor:** `corpus/services/sentinel.md:5` —
  *"What survives, and was re-derived: messenger's Go source imports no authorization client
  (`git grep "authorization\|AUTHORIZATION_ADDRESS" fa47850d -- '*.go' go.mod` returns one unrelated
  hit, against `colony` present as a positive control)"*
- **Why it is false:** ran the command verbatim in `stack-demo/messenger` at the ref the sentence
  itself names (`fa47850d`): **no output, exit status 1** (no matches). Same for `-E` alternation,
  for `-i`, for capital-`Authorization`, and for the whole tree with no pathspec:
  `git grep -Ein "authoriz" fa47850d | wc -l` → **0**, `git grep -Ein "authoriz" fa47850d -- '*.go' go.mod | wc -l` → **0**.
  The pipeline is not broken — two positive controls in the same pass:
  `git grep -c "func" fa47850d -- '*.go'` matches **39** files (the `'*.go'` pathspec resolves), and
  `git grep -c "colony" fa47850d -- '*.go' go.mod` returns `fa47850d:go.mod:1` (the literal `go.mod`
  pathspec resolves too). The only "one unrelated hit" in that tree is for a *different* pattern —
  `git grep -c sentinel fa47850d` → 1, at `pkg/aireadinessemail/override.go:378`
  (*"fallbackName is the sentinel used when the recipient has no display name"*), which is what the
  author appears to have measured before writing a different pattern into the document.
  **Why I hesitated:** the load-bearing proposition (*messenger imports no authorization client*) is
  **true**, and the measured reality is *stronger* than the published one, so the error does not
  mislead toward a false capability. I booked it because the corpus publishes this as a re-derivation
  command with its expected output, and a reader who re-runs it gets a result that disagrees with the
  document — the §5-rule-2 failure mode (an honest zero reading as a broken pipeline) manufactured by
  the corpus itself, inside the one sentence in this file that advertises itself as *"re-derived"*.
- **Confidence:** medium

---

## Minors

- `corpus/services/sentinel.md:85` — cites `architecture_overview.md:335` for the quote *"the only
  cross-process RPC edge out of backend on a core stack"*; `:335` is the block's intro line
  (*"**On a local stack** (platform `2adcf71` deleted the router…)"*) and the quoted string is at
  `architecture_overview.md:339`. Same short block, no factual consequence.
- `corpus/services/ai-labs.md:135` — *"`cmd/labsdemo` (standalone local Labs-catalog server on `:8099`,
  **in-memory store**, dev only)"*. Per `app@ad9f3c49:cmd/labsdemo/main.go:1-12` it *"connects to a
  local Postgres, ensures the labs / lab_bodies / api_keys tables exist … over an **in-memory
  workspace store**"*. Only the *workspace* store is in-memory; a local Postgres is a hard
  prerequisite. The compressed phrasing could send a reader to run it with no DB.
- `corpus/services/clerkenstein.md:21` — enumerates the monorepo's sections as *"(`clerkenstein`,
  `demo-stack`, `stack-injection`, `stack-core`, `stack-seeding`, `alignment`)"*. Both trees carry
  **11** section dirs; the five omitted are `dev-stack`, `playthroughs`, `stack-secrets`,
  `stack-snapshot`, `stack-verify`. Illustrative parenthetical, no count depends on it.
- `corpus/services/clerkenstein.md:105` — the `cmd/` row lists `mintpk` / `fake-fapi` / `fake-bapi`;
  the dir (pinned clone `09d06070`, identical in the authoring copy) also contains `jwtkey`.
- `corpus/services/sentinel.md:43-48` — *"only as a fixture string in sentinel's own tests
  (`internal/authorization/casbin_test.go`, `internal/rpcsrv/rpc_test.go`)"*. A third test file also
  carries it: `internal/authorization/manager_test.go:21` (`manager := "manager"`). The load-bearing
  claim (no `manager` Casbin role; 0 hits in `init_policy.sql`) is confirmed.
- `corpus/architecture/security_compliance.md:245-246` — cites `…/v3/validator/skills.go:53-64` for
  `calculateSkillScore`; the function is `:53-62` (`:63` blank, `:64` the next function). `:75` for
  the division is exact.

---

## Positively cleared (audited zeros)

**ENUMERATED** (predicate re-derived from source independently, cardinality stated before arithmetic):

1. **`security_compliance.md:78-147` — the multi-tenancy fence, the one that has been wrong four times.**
   Re-derived at `app ad9f3c49` (the ref the passage names):
   - `internal/data/ent/schema/*.go` = **139** files; **135** contain `ent.Schema`; the **4** that do
     not are exactly `database_types.go`, `mixin.go`, `skiller_mixins.go`, `skillpath_mixins.go`.
   - `OrganizationMixin{}` (word-boundary, excluding `OrganizationIDMixin{}`): **30** files, of which
     **one is commented out** — `user_resource.go:22` `// OrganizationMixin{},  // We need to work on this`
     (read in full: the file carries `UserMixin{}` and declares *no* `organization_id` field, only an
     `edge.From("organization", …)`, so it is correctly outside the 18 as well). Live set = **29**.
   - `OrganizationIDMixin{}`: **7**, and the named list is exact — `category, jobrole, similarity,
     skill, specialization, studio_document, studio_task`.
   - Schemas with a plain `"organization_id"` field and **neither** mixin: **18**, and the document's
     16-name list is exactly those 18 minus `org_membership.go` and `academy_feedback.go` — verified
     name-by-name.
   - `academy_feedback.go:64` is `UserMixin{}`; `:129` is `field.UUID("organization_id", …)`. ✔
   - `org_membership.go:172-188` is `func (Membership) Policy()`, ending `privacy.AlwaysDenyRule()`,
     with `rule.AllowCurrentOrgEdgesOrSkipRule()` at `:179`/`:183`. ✔
   - `organization.go:56` Policy, `rule.FilterSameOrganizations()` at `:96`; `user.go:116` Policy;
     `mixin.go:99` `UserMixin.Policy` (comment at `:98`) with `rule.FilterOwnerRule()` at `:103`/`:113`;
     `mixin.go:126` `OrganizationMixin.Policy`. ✔
   - **Exactly four files declare any `Policy()`** at BOTH `ad9f3c49` and the pinned `5ba17044`
     (`mixin.go`, `org_membership.go`, `organization.go`, `user.go`) — so the *"the first three declare
     no `Policy()` at all"* clause holds.
   - Tenant-scoped remainder: `field.(String|UUID)("tenant_…")` returns exactly six sites —
     `lab.go:63`, `academy_chapter.go:84`, `academy_skill_path.go:78`, `skill_path_session.go:43`
     (all four named), plus `job_simulation_session.go:35` (already inside the 23) and `mixin.go:152`
     (a mixin, not a schema). `skill_path_session.go` does carry `UserMixin{}` (`:26`). The *"at least
     four"* hedge is honest.
   - Totals: 29 + Membership + Organization = **31**; 16 + 7 = **23**; 135 − 31 − 23 = **81**.
     **The fence is correct, including the conjunct that broke it three generations running.**

2. **`security_compliance.md:22-34` — the VPC/router placement retraction.** Re-derived across the
   **eight** terraform trees the passage names, each at its own checkout ref: `public_subnet` = **0**
   occurrences in all eight; all eight pass `private_subnets_ids = var.platform_private_subnets_ids`
   (`graphql-wundergraph@60c229f3:terraform/main.tf:31`, `app:194`, `sentinel/messenger/roadrunner/storage:26`,
   `cms:46`, `jobsimulation:47`). Router `:11` is `base_service` — the same module `app` uses
   (`app:terraform/main.tf:172`) — and `:13` is `use_fargate = false`. ✔

3. **`service_taxonomy.md:71-135, 489-508, 512-523` — the compose/profile arithmetic at `0c91421`.**
   `docker-compose.yml` declares exactly **five** services (`sentinel:5`, `backend:28`,
   `studio-desk:112`, `next-web-app:143`, `gotenberg:170`); `common.yml` adds `postgresql:2` and
   `redis:24` → **seven** effective. Exactly **four** carry a `profiles:` key — `:110`, `:141`,
   `:168`, `:183` — and `sentinel` carries none, giving a **three-member** always-on floor and a
   **five-container** `core`. `Makefile:10` `PROFILE ?= core`. The legal token set is exactly
   `{all, backend, core, frontend, studio-desk}`. `repos.yml` lists exactly **4** repos with `app`
   at `:14-17` (`migrations: true`, `schema: public`) and `sentinel` at `:18-20` (`migrations: false`,
   yet `docker-compose.yml:18` gives it `search_path=sentinel`).

4. **`service_taxonomy.md:99-118` — the deleted support-service row.** At `0dab54d`: `storage` ports
   `8300/8301`, `messenger` `8200/8201`, `customerio-sync` `8080`; profiles `[storage-legacy]` (`:134`),
   `[customerio-sync, all]` (`:154`), `[messenger]` (`:195`). `838d907` deletes all three and edits
   `repos.yml`. The compose rationale comment is at `docker-compose.yml:84-92`; `ENVIRONMENT=development`
   at `:56` at both refs; `app/env_guards.go:37-44` `deployedEnvironment()`, `:92-111`
   `resolveSubsystemSwitch`, `main.go:394` `if customerIOSyncEnabled`; and at the fold commit itself
   `app 3e5bc33ef:main.go:387` is `if deployedEnvironment() && os.Getenv("BREVO_KEY") != ""`. All exact.

5. **`service_taxonomy.md:434` / `sentinel.md:5,85,89` — the RPC-edge claim.** At `0c91421`:
   **zero** `*_RPC_ADDR` in compose; `AUTHORIZATION_ADDRESS` occurs exactly **once** across
   `docker-compose.yml` + `common.yml` + `.env_example` (`.env_example` exists and is searchable —
   control: 10 `CLERK` hits), at `docker-compose.yml:48`; `GOTENBERG_URL` `:57`, `JUDGE0_BASE_URL` `:59`.
   `d11a403` changed exactly **two** values on the messenger block (`CMS_RPC_ADDR`,
   `JOBSIMULATION_RPC_ADDR`); at `d11a403^` the other two already read `http://backend:8083`, and
   `BACKEND_USERS_RPC_ADDR` was introduced at `3e85fce` as `http://backend:8081` — so *"it only ever
   moved ports"* is exact. `git grep messenger 0c91421d -- docker-compose.yml common.yml repos.yml`
   returns **only comments** (4 lines). The messenger block did begin at `0dab54d:docker-compose.yml:156`.

6. **`service_taxonomy.md:416` — the subgraph ladder 5 → 4 → 3 → 1.** Re-derived from
   `graphql-wundergraph` history: `749dc86^` prod config lists backend/skiller/jobsimulation/cms/skillpath
   (**5**), `749dc86` removes skiller (**4**), `7c17e63^` lists 4 → `7c17e63` removes skillpath (**3**),
   `915da06` deletes **both** `schemas/cms.graphqls` and `schemas/jobsimulation.graphqls` in one commit
   (**1**) — despite its own subject line saying *"2→1"*. At `60c229f3` all three supergraph configs
   carry a single `- name: backend` and `schemas/` holds one file.

7. **`service_taxonomy.md:413` — the router port.** `terraform/locals.tf:8` `port = 8080`;
   `terraform/main.tf:48-49` container 8080 → host 8080; `config.prod.yaml:5`
   `listen_addr: 0.0.0.0:8080`; `terraform/main.tf:20` `service_desired_count = 1`.

8. **`service_taxonomy.md:271-277` — the `--template` warning.** `gen.py` at `app/studio aeec036a`
   registers **exactly nine** `add_argument` calls (`:484-492`) and they are exactly the nine named;
   `parse_argument` uses `parse_known_args` and merges leftovers into the args dict (`:18-28`).
   (Aside, checked because it looked like a trap: the `--evaluation_skills` in the documented command
   at `:266` is *also* unregistered, but it is genuinely consumed through that same leftover merge —
   `agents/simulation/__init__.py:265` — so the documented command is valid.)

9. **`service_taxonomy.md:247` — Studio-Room's Mistral surface.** Grepped the nested repo at **its own
   ref** (`stack-demo/app/studio @ aeec036a`, invisible to `git -C app grep … HEAD -- studio/`):
   `git grep -ci mistral` sums to **22** across **3** files — `requirements.txt:1`, `tools/pdf2md.py:18`,
   `tools/r3.py:3`. `services/ai.py:705-708` is the entire provider registry (openai/azure/anthropic);
   `ai.py:1-2` imports only `openai`/`anthropic`; the sole `from mistralai import Mistral` is
   `tools/pdf2md.py:24`; `r3.py`'s three hits are `MISTRAL_API_KEY` env checks, no import.

10. **`service_taxonomy.md:295-301` — Ant Academy.** `code/package.json` `next ^16.2.6`,
    `react ^19.2.5`, dev port 3077; `mobile/package.json` `expo ~54.0.33`, port 8555.
    `code/src/graphql/server.js:14` reads the endpoint and `:18` throws when unset;
    `backendContent.js:36,102-103`; `serverTenant.js:115-145` carries the *"there is NO
    FS-as-published fallback … not reversible-on-error"* comment and `:145` is the
    `?? emptyCatalogView()` line; `app/api/academy/beacon/route.js:36` + `:41-55` is the `OPS` map with
    `UPSERT_CHAPTER_PROGRESS` / `SET_LAST_ACTIVITY`; `app/layout.jsx:132` `manifest:
    "/academy-manifest.json"`; `tests/unit/next-scaffold.test.js:106,111` and
    `react-compiler-config.test.js:41` fence the serwist removal.
    **The Cosmo claim is exact** — `code/ucourses/ucourse-engine/assistant/agent.js:12`
    `https://api.openai.com/v1/responses`, `:13` `MODEL = 'gpt-5.2'`, `:32`
    `localStorage.getItem('openai_api_key')`; gated by `NEXT_PUBLIC_FEATURE_TRAINING_COACH`
    (`featureFlags.js:8`, blank in `.env.example:85`). The `app/api/ai/chat/route.js` server proxy is
    a *different* consumer and says so at `:10-11`.
    The demo-patch `demo-stack/patches/academy-fs-published-fallback` exists in the **pinned per-stack
    clone `09d06070`** — the tree that settles what a stack runs.

11. **`service_taxonomy.md:518` — the `authn`/`ai` library statements.** Enumerated all **seven** Go
    clones' `go.mod`: `colony` required by **7/7** (control), standalone `authn` by **0/7**,
    `ai v1.40.2` by exactly `cms` and `jobsimulation` and by neither `app` nor `sentinel`.
    `app 1e457fa70` (*"refactor(ai): fold the ai library into app as internal/ai"*, 2026-08-04, 95 files)
    removed `anthropos-work/ai` from `app/go.mod`; `internal/ai/` exists.

12. **`clerkenstein.md` — all five DNA cardinalities, in BOTH rext trees** (identical): `clerk-2.6.0`
    **14 capabilities / 27 genes**, `clerk-js-5` 6/**9**, `clerk-multi-1` 5/**9** (incl. `Roster` 2/2),
    `clerk-express-1` **5**/**13** (incl. the `dotless-pk-rejected` variant), `clerk-deploy-1` 3/**7**.
    `clerk-js-5` has a `Me` capability (`universal-user`, `unauthenticated`) and **no** gene naming
    `organization_memberships` — the documented DNA gap. Gate defaults `95`/`100`
    (`alignment/scripts/gate.sh:31-32`). `alignment/cmd/alignctl/run.go:134-135` is
    `ExitRegressed = 2` / `ExitUnmeasurable = 3`.

13. **`clerkenstein.md:54-62, 160-209` — the M219 fix and the single-seat disclosure** (pinned clone
    `09d06070`; identical in authoring `43049308` where checked): `clerk-backend/store.go:138`
    `SeedOrgIdentity`, `:151` `LookupOrgEid`; `alignment/dna/clerk-2.6.0.json:131` contains
    *"taking the Go surface 97.2% -> 100%."* `clerk-frontend/registry.go:24,67,75-79`
    (`activeKey`/`active()`/`Select`); `server.go:102-109` one `signedIn`/`signedOut`/`clientID`/`sessID`,
    `:125` `clientID: "client_clerkenstein"`, `:663-670` `establishLocked` setting
    `sessID = "sess_clerkenstein"`; `handleSelectIdentity` `:646-647` sets `signedIn = false; sessID = ""`.
    **`r.Cookie(` / `.Cookie(` is called nowhere in `clerkenstein/`** — `git grep -n "\.Cookie(" HEAD --
    clerkenstein/` exits **1**, with the positive control `git grep -c Cookie` returning 5 files
    including `clerk-frontend/server.go`. All five named `server_test.go` tests exist
    (`:256, :286, :390, :427, :461`). Both membership routes are registered:
    `clerk-frontend/server.go:186` `GET /v1/me/organization_memberships`, `clerk-backend/server.go:47`
    `GET /v1/users/{userID}/organization_memberships`; `meorgmemberships_test.go` exists.
    `clerkenstein/go.mod:8` `colony v0.34.3` (the artifact pin) and `:9` `clerk-sdk-go/v2 v2.6.0`;
    `sentinel/go.mod:8` is `colony v0.35.2`, taken there by `88036d7` (2026-08-03) — so the
    *"`sentinel` has moved too"* retraction is exact, and only `storage`/`roadrunner` remain on
    `v0.34.3`. `app/go.mod` carries `colony v0.35.2` and `clerk-sdk-go/v2 v2.7.0`.
    `clerk-frontend/server.go:55-68` carries `clerkJSFetchTimeout = 15 * time.Second`,
    `clerkJSMaxBytes`, and `clerkJSClient` with the literal comment *"Explicitly NOT
    http.DefaultClient"*. `deploy/colony-authn` exists. `rosetta-extensions/alignment/` has **no**
    `scripts/` dir and `ALIGN_DIR` defaults to `$base/../../alignment`
    (`clerkenstein/alignment/scripts/gate.sh:30`); `clerkenstein/.github/workflows/alignment.yml` exists.
    `studio-desk@41ee3575:src/index.ts:96` and `app/services/userService.ts:16` both hold
    `STUDIO_ACCESS_ROLES = ['admin', 'org:admin', 'content_creator', 'org:content_creator']` with the
    quoted comment; `package.json` has `@clerk/express ^1.3.47`, **zero** react/vue/angular entries and
    **zero** `.tsx`/`.jsx` files.

14. **`security_compliance.md:208-233` — the AI-residency bullets.** `app/internal/jobsimulation/ai/ai.go`
    at `ad9f3c49`: `:85-88` `config.WithRegion("eu-west-1")` for the Bedrock/Anthropic client (`:85-95`);
    `:262-266` the `getClient` Azure branch reaching `azureClientEu`; `:263-277` the whole
    `flag_use_azure_us` PostHog swap to `azureClientUs`; `isThrottlingError` declared `:129` and called
    `:166` and `:325`. `app/internal/coursebuilder/bedrock.go:100` returns `"anthropic-api"` and
    `:109-112` is `newUnderlyingClient` → `NewAnthropicClientWithModel`. All three intra-corpus
    citations resolve: `external_services.md:579` (*"no ordered EU-first fallback chain"*), `:602-607`
    (the four-live-plus-one-latent list), `:567` (the Anthropic-Direct row); `coursebuilder.md:48`
    does say *"the shipped path"*.

15. **`security_compliance.md:243-257` — the EU-AI-Act premise retraction.**
    `…/v3/validator/skills.go:53` `calculateSkillScore` counts booleans, `:75` divides.
    `…/v3/validator/validator.go:60-61` registers exactly **one** engine (`check.EngineLlm`); the
    `checkerEngines` map is stored (`basevalidator/criterion.go:34,72,90`) and **never read** — the
    live dispatch is the hardcoded `switch critCheck.Engine` at `criterion.go:126` with
    `case check.EngineLlm:` at `:127`, reaching `NewLLMBulkChecker(c.logger)` at `:428`, whose prompt
    `basevalidator/templates/checkValidationBulk.tmpl` asks a model to evaluate each check.
    `criterion.go:142` `case check.EngineTextDiff:` → `:168` `validateCodeDiff` → `:450-475`, which sets
    `success` from a pure string comparison. So *both* halves — *"most, not all"* — check out.

16. **`security_compliance.md:159` — org switching.** `clerk.setActive({ organization: … })` at
    `useOrgSelection.tsx:94`, `useResolveActiveOrg.tsx:107`, `useActivateMembershipOrg.tsx:81` in
    **both** `apps/web` and `apps/hiring` at `next-web-app 8297c684`.

17. **`sentinel.md` — the Casbin model and RPC surface.** `internal/authorization/casbin.go:13-45`
    declares exactly **6** request definitions (`r`, `r2`–`r6`), **6** policy definitions, **3** role
    definitions (`g`, `g2`, `g3`) and **6** matchers, and each matcher's described semantics match its
    expression (incl. `m6`'s `g3(p6.org, r6.sub)` with no tier term and `m5`'s `keyMatch`).
    `internal/rpcsrv/rpc.go` exposes exactly the **19** methods the table lists — enumerated by
    `git grep -oE '^func \([^)]*\) [A-Za-z]+\('`, no extras, no omissions.
    `app/internal/data/ent/enum/membership.go:8-15` holds exactly the four `MembershipRole` values;
    `init_policy.sql:63-66` is the `taxonomy:write` NOTE; `:88-118` is the `content_creator` block;
    `internal/authorization/casbin_content_creator_test.go` exists; **`manager` appears 0 times in
    `init_policy.sql`**. `go.mod:3` `go 1.26.0`; `Dockerfile:2` and `Dockerfile.dev:2`
    `golang:1.26-bookworm`; `terraform/locals.tf:4-5` `service_cpu = 256` / `service_memory = 128`
    (and `:3` `port = 8080`, matching the *"binary default 8080"* claim, corroborated at
    `cmd/root.go:47`); `init_policy.sql:3` creates the single table `sentinel.casbin_rules`;
    `Makefile:3` hard-codes `postgresql://postgres@localhost:5432/postgres?sslmode=disable`;
    `local_superadmin_grants.sql` grants `p3 default admin org:feature:taxonomy:write` and carries the
    commented-out `p4` block exactly as described. `setup_guide.md` §6 does contain the
    schema-creation step (*"Prepare PostgreSQL Schemas (First Run Only)"*, `:445`).
    **`sentinel.md:5`'s five-service sentence is TRUE at the ref it names** (`0c91421d`) — I measured it
    independently before reading the briefing's note about the G10 guard verdict, and it holds.

18. **`ai-labs.md` — the whole doc.** At `app ad9f3c49`: `git describe --tags ad9f3c49` →
    **`v1.369.0-7-gad9f3c498`**, newest tag `v1.369.0`, and `2035f9a4` → `v1.369.0-2-g2035f9a40` —
    all three version strings exact. **`checkout.session.completed` = 0 occurrences in Go source**
    (control: `customer.subscription.*` matches at `internal/payments/handler.go:440` and
    `internal/web/backend/api/api.go:325-327`), and the Stripe webhook switch at `api.go:317-327`
    handles exactly `customer.created` + `customer.subscription.{created,updated,deleted}`.
    **`internal/labs/` imports `internal/credits` nowhere** (control: 10+ importers elsewhere,
    all under `internal/web/backend/`). `internal/web/backend/credits/handler.go:12` states the
    Wave-13 removal of `POST /credits/purchase`, and `Register` (`:43-46`) mounts only
    `/balance` + `/transactions` — matching the doc, *against* the file's own stale
    *"mounts the three"* comment at `:30`. `backend.go:223` confirms *"both /coursebuilder and
    /credits are org-admin"*. `knowledge/plan/releases/06.00-shared-purse/` exists with `m600`…`m607`.
    `internal/credits/cost.go`: `creditCost{build:5, refine:1, translate:1}` (`:86-90`) — the doc is
    right and the file's own header comment at `:29` (*"refine → 5"*) is the stale one;
    `purchasePackages` starter 50 / team 200 / scale 500 (`:200-204`); `MarginMarkup = 1.40` (`:133`);
    `PricePerCreditUSD = 0.45` (`:142`); `DefaultSeedBalance int64 = 500` (`:231`).
    `lab_session.go` deliberately omits `PrimaryKeyMixin` (`:21-28`), `field.String("id")` (`:36`),
    status enum exactly `booting/ready/grading/stopped/failed/cancelled` (`:87`), `grade_result` jsonb
    (`:124`). `lab_body.go:12,48-50` — one row per `(lab_slug, locale)`, `en` + `de/es/fr/it/nl/pt`
    = **6** overlays, one jsonb `{meta,brief,tutor,hints}`. All five migrations exist by the exact
    filenames given. `labsapi/client.go:6` default `:7070`. `LabsTier` = `essential/professional/frontier`
    (`labs_budget.go:26-28`). `cmd/labsimport`, `cmd/labskey`, `cmd/labsdemo` (`:8099`,
    `main.go:115`). `go.mod:64` `stripe-go/v74 v74.30.0`. `LabSessionService` = Create/Get/List/Cancel/
    ReportEvent (`labsession.go:90,252,278,333,386`). The test-file list is **exact**, including
    *"Payments has no Go unit test"* (0 `_test.go` under `internal/payments/`), and both named tests
    exist (`cost_test.go:103`, `privacy_backstop_test.go:25`). *"Credits has NO GraphQL"* holds: the
    only `credit` hits in the schemas dir are Sentinel **feature**-credit fields
    (`mutations.graphqls:190`, `queries.graphqls:98`) and an unrelated academy prose line.

19. **`service_taxonomy.md:44-58, 252, 361-397` — the generation edge, the CI pull, the Directus history.**
    `studio-desk@41ee3575:.env.example:44` bakes
    `VITE_GRAPHQL_ENDPOINT=http://localhost:8082/graphql/query`, `:45` `VITE_ENVIRONMENT=production`.
    `cms_queries.graphqls:106` `studioTask`, `:107` `studioTasks`; `archiveStudioTask` really is at
    `cms_mutations.graphqls:22` and occurs **0** times in `cms_queries.graphqls`, exactly as the
    correction states. `internal/cms/studio/studioManager.go:119` execs `studio/gen.py` via
    `runCommand`, whose contract comment at `:1096-1098` is *"in argv (exec) form — NEVER through a
    shell"*. `app/.github/workflows/build-production.yml:29` `additional_repo:
    "anthropos-studio-room:studio"`. `academy.graphqls` is 1 of **43** files in the schemas dir.
    `app/internal/converter/gotenberg.go:31` is the POST to `gotenbergURL`.
    The Directus retraction-of-the-retraction is exact: `platform a2a3ee6^:docker-compose.yml:384`
    `image: directus/directus:10.10.1`, `:386` `- 8055:8055`, `:409` `ADMIN_PASSWORD=password`, and
    `admin@example.com` returns **0** hits across all of platform history (`git log --all -S`).
    The per-stack Directus image `directus/directus:11.6.1` is in the **pinned** rext clone
    (`stack-core/gen_override.py:183`, `dev-stack/dev-setdress.sh:255`).

20. **`service_taxonomy.md:168-170` — the terraform rows.** Graded at the ref each cell names.
    `jobsimulation 6092c6d2` (*"remove the jobsimulation ECS service and ECR repository (M810)"*,
    2026-08-04, an ancestor of `origin/main`) — `terraform/main.tf:15-40` is **exactly** the M810
    decommission comment (module block deleted; livekit/chime buckets, SSM params and the atlas
    tracker retained). `cms@ca50c817:terraform/main.tf:39` is `service_desired_count = 0` inside an
    intact **191**-line file (identical at `origin/main f38c0c4a`), and `6efa1d5` really did delete
    `.github/workflows/build-production.yml` with that subject. `roadrunner`'s prod module still reads
    a non-zero desired count. The *"report both, assert neither"* framing is measured, not asserted.

**RE-DERIVED (weaker — arithmetic only, no independent set enumeration):** the
`135 − 31 − 23 = 81` remainder in `security_compliance.md:116`, and the `29 + 2 = 31` /
`16 + 7 = 23` sums (their SETS were separately enumerated above, so these carry the enumeration's
weight, but the subtraction itself is only re-computed).

---

## What I could not settle

- **`service_taxonomy.md:3, 166-168, 401-403` — GitHub archive state** (`graphql-wundergraph` archived
  2026-07-30, skiller 2026-07-01, skillpath 2026-07-31, and the jobsimulation row's deliberate
  non-assertion). The file says correctly that this lives in the GitHub org API and is invisible to a
  clone; `gh` is not installed on this host and the repos are private. What I *could* check is
  consistent: `graphql-wundergraph` has no commit after 2026-07-30, while `jobsimulation`'s
  `origin/main` carries four 2026-08-04 commits (including `6092c6d2`), which is why that row's
  refusal to assert is the right call. Settling the rest needs
  `gh api repos/anthropos-work/<repo> --jq .archived`.
- **`security_compliance.md:232` — *"the anchor said `:489`, which is a TypeScript codegen comment"***.
  A historical note about an anchor already corrected twice; the sentence never says which *file*
  `:489` was in. `external_services.md:489` today is the string `**Environment**:`, but that file's
  line map has moved since iter-46/48, so this is not gradeable now and is not an actionable anchor.
  Settling it needs the iter-46 corpus state.
- **`service_taxonomy.md:22-34` / `security_compliance.md:30-32` — prod ECS placement.** Both passages
  disclose the residual honestly: `infrastructure` (which defines `base_service` /
  `base_internal_service` and applies the M810 destroys) has never been in any clone set. What is
  measurable in the eight service trees says private-subnet and says the cms module is still declared;
  what actually got applied in prod is not readable from here.
- **`ai-labs.md:5-6` — `anthropos-work/AI-Labs` as the live `labs-api` control plane.** Consistent with
  everything readable in `app` (`internal/labs/labsapi/client.go`, `LABS_API_URL`,
  `LABS_API_PLATFORM_TOKEN`, labs-api-owned session ids), but the repo is not in the clone set, so its
  existence and Firecracker/LiteLLM internals are unverifiable here.
- **`db-backup.md` (all 31 lines).** No repo in the clone set, no `file:line` citations, no compose or
  terraform presence anywhere I can read. Every claim (6-hour cadence, S3/Azure/Hetzner targets, Go,
  scheduled-task deployment) is unfalsifiable from this ground truth. Its cross-file twins are
  consistent (`service_taxonomy.md:173`, `security_compliance.md:167,174`), so there is no
  self-contradiction to book — but this file is entirely un-audited, not cleared.
- **`security_compliance.md:7, 273` — DPA v1.4 and "18 approved sub-processors"**, plus the whole
  Network/Encryption/Access/Monitoring sections (`:21-68`, `:163-201`). These are contractual and
  infra-console facts with no representation in any clone. Not measurable; not booked.
