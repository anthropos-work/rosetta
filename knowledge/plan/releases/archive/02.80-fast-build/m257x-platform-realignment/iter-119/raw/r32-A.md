# Seat A — reading #32

**Ground truth actually read** (re-verified at this reading's open, all matching the ADDENDUM):
`stack-demo/app` `ad9f3c49` · `stack-demo/platform` `0c91421d` · `stack-demo/next-web-app` `8297c684` ·
`stack-demo/sentinel` `f2c46190` · `stack-demo/studio-desk` `41ee3575` · `stack-demo/ant-academy` `22df69dd` ·
`stack-demo/storage` `4ce8ece5` · `stack-demo/messenger` `fa47850d` · `stack-demo/cms` `ca50c817` ·
`stack-demo/graphql-wundergraph` `60c229f3` · `stack-demo/roadrunner` `87d8d443` ·
`stack-demo/jobsimulation` `462343b0` · `stack-demo/rosetta-extensions` (**pinned per-stack consumption clone**)
`09d06070` · `.agentspace/rosetta-extensions` (authoring) `43049308` · nested `stack-demo/app/studio` and
`stack-demo/cms/studio` **each at their own ref `aeec036a`** (grepped at that ref, never at the host's).

Where a claim named a ref I graded it there (`b948604`, `9d00a313`, `7177374`, `5ba17044`, `0dab54d`,
`2adcf71`, `1e8e754`, `a2a3ee6^`, `915da06^`, `6092c6d2`, `f38c0c4a`, `ab81527a`). Where it named none I
graded it at the checkout. Every "0 hits" below was taken across the enumerated **15** git trees at each
tree's own ref, with a positive control in the same pass.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| corpus/architecture/external_services.md | 887 | yes |
| corpus/services/jobsimulation.md | 316 | yes |
| corpus/services/customerio-sync.md | 165 | yes |
| corpus/services/askengine.md | 147 | yes |
| corpus/services/intelligence.md | 18 | yes |

## Blockers

### B1 — askengine still calls the `ai` library "shared" twice; it was folded into `app` and two siblings say so

- **Anchor:** `corpus/services/askengine.md:81` — *"…**Downstream**: … the shared **`ai`** library
  (`CreateEmbeddings`, text-embedding-3-small, for RAG golden-example retrieval)…"*
  Second instance, same predicate: `corpus/services/askengine.md:113` — *"…and an embeddings provider for the
  shared `ai` client."*
- **Why it is false:**
  - `app` folded the library into its own tree at `1e457fa70` (*"refactor(ai): fold the ai library into app as
    internal/ai"*). At `app` `ad9f3c49`, `git grep -n "anthropos-work/ai" ad9f3c49 -- go.mod go.sum` returns
    **nothing**; `app/go.mod:14-18` requires exactly `analytics-go`, `colony`, `proto`, `storage`, `taxonomy`
    (read directly). Positive control in the same pass: `anthropos-work/colony` is present in `go.mod` and in
    214 `.go` files.
  - The Ask Engine specifically imports the **in-tree** package, not a module:
    `app/internal/web/backend/ask/embed.go:8`, `examples.go:15`, `handler.go:20` all read
    `"github.com/anthropos-work/app/internal/ai"`, and the `CreateEmbeddings` call the sentence cites is
    `embed.go:23` on that client (`ai.WithEmbeddingModel(ai.EmbeddingDefaultModel)`, `:24`; the
    text-embedding-3-small default is documented at `embed.go:12`). So the very call named in `:81` is made
    against `app/internal/ai/`.
  - **Corpus self-contradiction, and the corpus itself names this as a repair that leaked.**
    `corpus/architecture/external_services.md:554` states the correction explicitly and lists the sites it was
    applied to (*"…**this sentence said "the shared `ai` library" until M257x iter-115** … `ai_architecture.md`,
    `shared_libraries.md#ai` and `architecture/README.md` all already carried the correction…"*), and
    `corpus/services/jobsimulation.md:168` repairs a third instance (*"through one `ai.AI` interface that is
    **in-tree, not a shared module**"*). `askengine.md` holds the identical predicate at two sites and was not
    swept — §5 rule 19's *repair by claim, not by file* leak, one file outside the previous sweep's edge.
  - `enumerated the exact importers: 67 `.go` files import `"github.com/anthropos-work/app/internal/ai"`
    (83 counting sub-packages), which is the figure `external_services.md:554` publishes — so the correct
    statement was already derived and available.
- **Why I hesitated (stated per the instrument):** the platform's own comment at `embed.go:12` says *"the
  shared AI manager"*. But "the shared `ai` library" is a **defined term** in this corpus with its own
  `shared_libraries.md#ai` section meaning the private Go module `github.com/anthropos-work/ai`, and that
  module is required by no repo a stack builds. A reader auditing dependencies from `askengine.md` gets the
  pre-fold answer with four siblings contradicting it.
- **Confidence:** high

### B2 — "The real mechanics, all in `app/internal/jobsimulation/ai/ai.go`" — there is a SECOND, live AI manager

- **Anchor:** `corpus/architecture/external_services.md:581` — *"…no such ladder exists in the code. The real
  mechanics, **all in `app/internal/jobsimulation/ai/ai.go`**:"* — the sentence that scopes the whole
  *Routing: what is actually implemented* section, which `:554` sends the reader to *"before relying on it for
  a residency argument."*
- **Why it is false:** `app` at `ad9f3c49` contains a second, independently constructed AI manager for the
  **skills (merged skiller) domain** — the very domain this same page names as an AI consumer at `:562`
  (*"Backend (app — merged skiller domain)"*) and `:686` (*"Skills Matching (Backend `app` — merged skiller
  domain)"*). It is **not** dead code and it duplicates every residency-relevant lever:

  | mechanism | jobsimulation manager (the file `:581` names) | `app/internal/skillerai/ai.go` (unnamed) |
  |---|---|---|
  | vendor consts | `:30-33` | `:57-61` (`azure`/`openai`/`anthropic-aws`/`anthropic`) |
  | Azure EU→US via `flag_use_azure_us` | `:264-276` | `:336-360` (same PostHog flag, own `IsFeatureEnabled` call) |
  | `isThrottlingError` (HTTP 429) | `:129-141` | `:128-140` |
  | 429 → `vendor = Openai` retry override | `:150-155` | `:163-165` |
  | direct-OpenAI client | `:80` `openai.NewOpenAI(openaiKey)` | `:107-113` `openai.NewOpenAI(*openaiKey)` |

  It is **live-wired**: `app/main.go:675` `skillerAIManager, err := skillerai.NewAIManager(...)`, fed from its
  **own env pair** read at `app/main.go:666`, `:669`, `:672` (`SKILLER_AZURE_OPENAI_KEY`,
  `SKILLER_AZURE_OPENAI_ENDPOINT_URL`, `SKILLER_OPENAI_KEY`), and imported by **22** `.go` files. Its package
  doc says so in as many words (`internal/skillerai/ai.go:3-9`: *"…Azure EU/US + OpenAI clients,
  retry/throttle->OpenAI fallback, PostHog-driven Azure-US selection … its own AIVendor/AI types. **Do not
  alias it onto backend's existing AI manager.**"*). I enumerated the construction sites rather than trusting
  the page: `git grep -n 'NewOpenAI(' ad9f3c49 -- '*.go'` returns exactly three — the constructor itself
  (`internal/ai/openai/completion.go:24`) and the two managers above.
- **Corpus self-contradiction, both directions available with no ground-truth read:** two sibling files
  already state **both** sites. `corpus/architecture/shared_libraries.md:153` — *"sites are
  `internal/jobsimulation/ai/ai.go` **and** `internal/skillerai/ai.go`, each with…"*, and `:156`/`:159` pin
  the flag site and the retry site in each. `corpus/architecture/ai_architecture.md:114` — *"in `app` @
  `5ba17044` that is **`app/internal/jobsimulation/ai/ai.go:267,344`** **and**
  **`app/internal/skillerai/ai.go:347`**"*. `external_services.md` is the one file asserting a single-file
  scope, and it reinforces it at `:597` (*"**Mistral is not in this manager at all.**"*).
- **The consequence is not cosmetic — it is the SET the section's arithmetic ranges over.** `:604-607`
  publishes *"**Four** things can send a request outside the EU"* and grounds items 1 and 2 (the
  `flag_use_azure_us` flip and the 429→direct-US-OpenAI retry) **solely** in the jobsim manager's anchors
  (`:588-596`). Both levers exist a second time, in a second domain, in a file the enumeration never opens —
  and `skillerai`'s Azure-US arm is additionally constructed with `nil` US key/endpoint at `main.go:675`, a
  fact no reader of the named file can reach. This is the *exact* failure the page's own caution box at
  `:645-650` says it already committed once (*"the vendor-defaulting happens one layer up … a reader auditing
  the AI manager has no reason to open [it]"*) — repeated on the domain axis instead of the layer axis.
- **Why I hesitated:** the four *levers* named at `:609-643` remain the four kinds of lever that exist, so one
  could read the enumeration as still complete "by kind". I book it anyway because (a) the sentence at `:581`
  is a flat, checkable, false scoping assertion, (b) it contradicts two sibling corpus files that name the
  second file explicitly, and (c) the section exists to support a residency argument, where "I read the one
  file the doc named" is precisely the work a reader will do.
- **Confidence:** high

## Minors

- `corpus/architecture/external_services.md:727` — *"There is no `anthropos-agent-eu` — the name appears
  nowhere in the platform (**0 hits across all 15 trees at their own refs**…)"*. Measured across the 15 trees
  (`find stack-demo -name .git -maxdepth 4`), 14 return 0 and **`stack-demo/rosetta-extensions` @ `09d06070`
  returns 7** — all in `stack-core/tests/fixtures/**` (`claim_twin_iter48/red/07.md:3`,
  `mechanical/{green,red}/…/external_services.md:652`, `repair_leak/{pre,post}/…`), i.e. verbatim copies of
  this corpus's own retracted sentence held as guard fixtures. The load-bearing predicate (*"nowhere in the
  platform"*) is correct — the platform trees are 0 and the code anchors at `calls/livekit.go:110/115/120/126`
  all verify — so this is a count that is literally wrong about a tree nobody would search, not a misleading
  claim.
- `corpus/architecture/external_services.md:554` — *"The **only repos** that still *require* the module are the
  frozen `cms` and `jobsimulation` husks."* Enumerated over every `go.mod` in the 15 trees:
  `cms` and `jobsimulation` (both `github.com/anthropos-work/ai v1.40.2`) **plus
  `rosetta-extensions/stack-seeding/go.mod`**, which the corpus itself documents as a deliberate dependency
  (`corpus/ops/demo/ai-generation-spec.md:8`). Non-material because the passage's own frame two clauses
  earlier is *"for any service a stack builds"*, and the tooling monorepo is not one.
- `corpus/architecture/external_services.md:455` — presents `graphql-wundergraph/CLAUDE.md:39` inside
  quotation marks as *"Since cms-in-app the platform compose `graphql` service builds from the **production**
  Dockerfile"*. The file at `60c229f3` actually reads *"**The committed `schemas/backend.graphqls` is now
  load-bearing locally too.** Since the compose stack builds from the production Dockerfile, a schema change in
  `app` does **not** appear at `:5050` until you regenerate and commit that file here."* Paraphrase rendered as
  verbatim quotation; the substance of the finding (that page asserts the opposite of this section, on
  2026-07-30, in a commit titled *"correct the compose build path"*) is exactly right — both verified.
- `corpus/architecture/external_services.md:350` — the Directus-webhook use-case list still says *"Invalidate
  CMS **service** cache when content updates"*; there has been no `cms` service since `d11a403`, as this same
  file says at `:173` and `:832`. Stale noun in a bullet list, no factual consequence.
- `corpus/services/askengine.md:127-135` — names four of the five `internal/askengine/*_test.go` files
  (`anthropic_test.go` is unmentioned). The four counts it does give are exact (below); the list is not
  claimed exhaustive.

## Positively cleared (audited zeros)

**ENUMERATED** — predicate re-derived from source independently, cardinality compared:

- `external_services.md:373-386` supergraph-count table. Read `supergraph-config-prod.yaml` at each named
  commit: `749dc86^` = **5** (backend/skiller/jobsimulation/cms/skillpath), `749dc86` = **4**, `7c17e63` =
  **3**, `915da06` = **1**. Dates match (2026-06-24 / 07-21 / 07-29). The "3→1, not 2→1" correction holds and
  `915da06`'s own subject really does say *"supergraph 2→1"*.
- `external_services.md:387` "no subscriptions". `git grep -nE 'sse|subscription' 60c229f3 -- '*.yaml'` → rc 1
  across all seven yamls (PC: `backend` matches all three supergraph configs). `schemas/backend.graphqls` has
  `type Mutation` at **:4053** and `type Query` at **:4912** and no `type Subscription`; the **6**
  `Subscription` hits are `activeSubscription`, `type PlanSubscription`, `stripeSubscriptionId` ×2,
  `stripeSubscriptionItemId` and one doc-comment. `git log -S 'protocol: "ws"'` on mainline → empty;
  `git log -S 'sse_post'` → **5** commits; `git merge-base --is-ancestor bba862f HEAD` → **rc 1**;
  `git branch -a --contains bba862f` → `remotes/origin/feat/use-web-socket` and nothing else.
- `external_services.md:169-175` + `:304-306` compose cardinality. At platform `0c91421d`,
  `docker-compose.yml` declares **5** services (`sentinel:5`, `backend:28`, `studio-desk:112`,
  `next-web-app:143`, `gotenberg:170`) + `common.yml`'s `postgresql`/`redis` = **7** effective; the profile-less
  floor is exactly `postgresql`/`redis`/`sentinel`, so `core` starts **5** containers. At `0dab54d` (== `838d907^`,
  verified by `rev-parse`) it was **8 / 10** — the figure `:306` gives. `d11a403` really did delete the
  `jobsimulation`, `cms` **and** `roadrunner` service blocks and their `repos.yml` entries; `838d907` deleted
  `storage`/`messenger`/`customerio-sync` and dropped `storage`+`messenger` from `repos.yml`. `repos.yml` @
  `0c91421d` = 4 entries (app, sentinel, next-web-app, studio-desk).
- `external_services.md:668-677` env-var grep counts, at the ref the block names (`app` `5ba17044`):
  `OPENAI_KEY` **5** / `ANTHROPIC_API_KEY` **26** / `AZURE_OPENAI_KEY` **13** / `AZURE_OPENAI_ENDPOINT_URL`
  **13**; `OPENAI_ORG_ID`, `AZURE_OPENAI_ENDPOINT`, `AZURE_OPENAI_DEPLOYMENT` all **0**; `OPENAI_API_KEY` **2**.
  Studio side read at the **nested repo's own ref** `aeec036a`: `gen.py:45-48` is the six-name
  `secrets_keys` list, exactly as enumerated.
- `external_services.md:567`/`:619`/`:640` "all **30** `*_AI_*_MODEL` lines pin `azure`". At
  `stack-demo/app/studio` `aeec036a`: 3 config files, **30** matching lines, **30/30** azure, 0 openai,
  0 anthropic. `gen.py:41-53` overrides only `*_API_KEY`/`*_ENDPOINT`, never the service selector.
- `external_services.md:691-704` taxonomy figures, read out of
  `.agentspace/snapshots/taxonomy/5afc0bcc…/manifest.json` (`public_only: true`, `predicate: org-null`,
  captured `2026-06-29`): skills **42,790**, job_roles **22,470**, skill_embeddings **42,790**,
  job_role_embeddings **18,919** — every figure exact, including the 18,919-transcribed-onto-18K diagnosis.
- `customerio-sync.md:85` the "18 distinct repo URLs" derivation. Re-derived independently over
  `git log --all -- docker-compose.yml`, every `context: git@github.com:…` line ever written, de-duplicated:
  **18**, and the member list matches the corpus's list name-for-name (app, chronos, cms, customerio-sync,
  graphql, graphql-wundergraph, graphqltmp, intelligence, jobsimulation, messenger, realtime, roadrunner,
  sentinel, simulator, skiller, skillpath, storage, studio-desk). `a2a3ee6` (2026-02-27) and `c17cc9a`
  (2026-04-15) are the commits claimed; at `838d907^` there are **7** `context:` lines of which exactly **1**
  is a git URL (customerio-sync).
- `customerio-sync.md:153` "no Customer.io section, no Brevo section". `external_services.md`'s per-service
  `##` headings are Clerk, Directus, GraphQL Gateway, AI Providers, LiveKit, AWS Chime; `grep -ci brevo` = **0**.
- `jobsimulation.md:39-45` "23 session/run tables". `20260722081626_jobsim_data_model.sql` has exactly **23**
  `^CREATE TABLE` statements, and the named samples are all members. The rename holds:
  `20260722104506.sql:2` creates `job_simulation_sessions`, `:79` `DROP TABLE "sessions"`.
- `jobsimulation.md:134-139` mirror-drop migration. `20260729133514.sql:58` is *"5. Drop the mirrors."*,
  `:62-63` drop both mirrors, `:15-23` are the two id remaps, `:36-44` the orphan NULLs, and **0**
  `INSERT INTO` statements (a naive `grep -ci insert` returns 1 — the string
  `on_insert_local_jobsimulation_sessions_update_memberships` at `:64` — which is why I matched the statement,
  not the substring).
- `jobsimulation.md:54` the terraform sweep. **44** tracked `.tf` files across the 13 `stack-demo` repos
  (per-repo breakdown re-derived) and **59** in a raw filesystem sweep — both exact. The literal
  `http://backend.internal.anthropos:8081` occurs **6** times in the clone set (app 1, rext 5), **none**
  terraform, and `app/knowledge/service-dependencies.md:52` is past-tense under the heading at `:50`
  *"There are no external callers of app's RPC mux left."*
- `askengine.md:47-51` registry cardinality: `registry.go` has **60** `TableDef`s, **57** `Schema: "public"`,
  **3** `Schema: "directus"`; `:183-195` is the jobsimulation transition-alias comment block.
- `askengine.md:127-132` test counts: `sandbox_test.go` **49**, `executor_test.go` **13**, `prompt_test.go`
  **10**, `followups_test.go` **14** `func Test` declarations — all four exact.
- `askengine.md:64-67` route list: every route re-derived from `internal/web/backend/backend.go:171-188`
  (group + 7 `/ask/*` + 4 admin auto-rules), and the OpenAPI-skip rationale is the comment at `:168-170`.
- `external_services.md:565`/`:597` Mistral: `NewMistral` is **0 hits repo-wide** at `ad9f3c49` (PC:
  `mistralocr` matches 5 files); `markdownManager.go:30` `mistralocr.New(aiKey)` inside `func
  NewMarkdownManager` at `:29`, field at `:14`, import at `:10`, and `:19` is indeed a doc-comment line;
  `studioManager.go:583`; at the nested ref `aeec036a`, `tools/pdf2md.py:24` `from mistralai import Mistral`
  with `mistral-ocr-latest` at `:127`, and `r3.py:139` / `:190` / `:199-206` do dispatch it as step 2.
- `external_services.md:479-485` historical subgraph URLs: at `915da06^`, `supergraph-config-compose.yaml`
  really lists `http://jobsimulation:8400/query` (with the only `subscription: protocol: "sse_post"` block) and
  `http://cms:8090/query`.
- `jobsimulation.md:100` ports: `cmd/root.go:77`/`:78` give 8080/8081 defaults; at `2adcf71`
  `docker-compose.yml:113` `PORT=8400`, `:119` `RPC_PORT=8401`, `:93-94` publish both, `:83` is the service key
  and `:140` its `profiles: [graphql, jobsimulation, all]`; `Makefile:10` `PROFILE ?= graphql` at that ref;
  `repos.yml:17-19` at that ref is the jobsimulation entry.

**ENUMERATED (anchor-resolution class)** — each `file:line` opened and the surrounding block read
(`sed -n 'N-15,N+5p'`), never grepped-to:

- `app` @ `b948604`: `main.go:212` `func main()`, `:614` the jobsim `log.Fatalf`, `:770`
  `coursebuilder.ModelBackendName()`, `:980-982` the `DIRECTUS_BASE_ADDR` `log.Fatalf`, `:1204` the
  `JobSimulationService` handler. @ `7177374`: `main.go:216` `func main()`. @ `9d00a313`: `main.go:670` the
  jobsim `log.Fatalf` (`jobsimulation.md:244-245`'s pin holds). @ `ad9f3c49`: `main.go:229` `func main()`,
  `:721`/`:723` `jobsimwiring.Wire` + its fatal, `:670` `skillerAzureEndpointEu = &v` (the iter-108 note is
  correct), `:286`, `:295-300`, `:393-396`, `:395`.
- `internal/jobsimulation/ai/ai.go`: `:30-33`, `:80`, `:90`, `:130-141`, `:150-155`, `:259-289`, `:264-276`,
  `:280-283`, `:296-302`, `:326` — every one names the construct claimed.
- `internal/jobsimulation/simulator/ai/ai.go:58-59` and `:114-115`;
  `internal/cms/directus/collections/jobsimulation.go:905`, `:1302-1305`, `:1594-1597`;
  `internal/coursebuilder/bedrock.go:23`, `:29`, `:106-113`; `internal/jobsimulation/agent/report_agent.go:31`;
  `internal/askengine/bedrock.go:25`, `:161`; `internal/askengine/executor.go:18`/`:21`;
  `internal/web/backend/ask/handler.go:34`/`:41`; `internal/web/backend/backend.go:130`;
  `internal/jobsimulation/calls/livekit.go:101-104`, `:110`, `:111`, `:115`, `:118-128`, `:120`, `:122-123`,
  `:126`, `:131-135`, `:140-144`.
- `app/studio` @ `aeec036a`: `services/ai.py:383`, `:627-664`, `:704-724`; `configs/config_template.ini:30-31`.
- platform: `docker-compose.yml:48/53/57/59/84-92/101-109/110/119/135/151/160/183` @ `0c91421d`;
  `:53` and `:88-91` @ `0dab54d`; `.env_example:91-92`. Historical: `1e8e754:docker-compose.yml:14` `5050:8080`,
  `:6-8` the `Dockerfile.dev` build, `:19-27` the four-member `depends_on`; `b56d731` still has `graphql:` at
  `:22` while `360efd4` does not (PC: `backend:` at `:28`); `a2a3ee6^` has `directus/directus:10.10.1` at
  `:384`, `8055:8055` at `:386`, `ADMIN_PASSWORD=password` at `:409`, and `admin@example.com` appears in **no**
  commit touching compose.
- graphql-wundergraph @ `60c229f3`: `terraform/main.tf:20` `service_desired_count = 1`, `terraform/locals.tf:8`
  `port = 8080`, `terraform/main.tf:48-49`, `config.prod.yaml:5`, `Dockerfile.dev:7/18/19-20/23/29`,
  `subgraphs.conf` = `BACKEND=v1.360.0`, `CLAUDE.md:39`.
- terraform teardown, **graded at the refs the claims name**: `jobsimulation/terraform/main.tf` at `6092c6d2`
  (and at origin/main `82cb66ec`) has **no** `module "jobsimulation"` and **no** `service_desired_count`;
  `:15-22` is the M810 comment, `:24-40` the "what this module still owns" block, `:38-40` the atlas/legacy-schema
  step. **The checkout `462343b0` is behind and still HAS the module at `:31` with `= 0` at `:40`** — so this
  row is correct only because the passage pins `6092c6d2`, and I record that explicitly.
  `cms/terraform/main.tf:39` `= 0` at both `ca50c817` and `f38c0c4a` (191 lines), and `6efa1d5` is real, with
  the quoted subject, deleting `build-production.yml`. The *report both, assert neither* framing holds.
- rext, read in the **pinned per-stack consumption clone** `09d06070` (the tree that settles what a stack
  does): `stack-injection/gen_injected_override.py:86` `DIRECTUS_DATA_CONSUMERS = ("cms", "backend")`,
  `:77-81` the inert-key rationale, `:698-699` the re-point; `stack-core/gen_override.py:58` the same tuple;
  `stack-injection/tests/test_injection.py:1109` `test_backend_the_actual_reader_is_repointed`. The prior-pin
  claim also holds: at `ab81527a` the same constructs are `:84` and `:669-670`. `gen_override.py:149-153`
  confirms the `!override` / `!reset null` volumes-drop pair; `pt-aisim-chat-launch` exists.
- customerio-sync: every claim in the file traces to `app/internal/customeriosync/doc.go` (`:4-5` "last of the
  Go services", `:15` the ticker→asynq move, `:19-31` the view replacement with **7,392 of 7,397**, `:38-44`
  the stateless window + `CreateContact`/`UpdateEnabled`, `:46-52` `database/sql` NULL handling, `:68`
  `--dry-run`) plus `env_guards.go:62`, `:84-90` (`log.Fatalf` at `:87`), `:92-106` (`case "":` at `:98`,
  deployed-error `:99-103`), and `main.go:284` being only the comment.
- Clerk SDKs (`external_services.md:69`), per-app `package.json` at each repo's own ref: `@clerk/nextjs` in
  `apps/{web,hiring,integration}` and `ant-academy/code`; `@clerk/clerk-expo` in `apps/mobile`;
  `@clerk/clerk-js` + `@clerk/express` in `studio-desk`; `clerk-sdk-go/v2 v2.7.0` in `app/go.mod`. `apps/hiring`
  exists. `NEXT_PUBLIC_GRAPHQL_ENDPOINT` is **0 hits** in `next-web-app` @ `8297c684` (PC: 15 files carry
  `NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT`). Sentinel has **0** webhook routes (PC: casbin matches 3 files).
- `intelligence.md`: `fdfa189` = *"chore: remove intelligence service from local dev orchestration"*,
  2026-04-17, touching **both** `docker-compose.yml` and `repos.yml`; the pre-removal block at `fdfa189^:61-82`
  carries `DB_CONNECTION_BACKEND` and `DB_CONNECTION_SKILLER` exactly as documented.
- Markdown integrity of all five files: every relative `.md` link resolves, and every `#fragment` (cross-file
  and in-file, including `:614-617`'s `:567`/`:543` self-anchors and `:877`'s `:546`) resolves to a real
  heading slug.

**RE-DERIVED (weaker — arithmetic the document itself displays, re-computed)**: none booked as a clearance on
its own. Where a document showed its derivation (`customerio-sync.md:85`'s 18 repos, `jobsimulation.md:54`'s
44/59/6, `external_services.md:668`'s 5/26/13/13, the 30 `.ini` lines, the 60/57/3 registry) I re-enumerated
the **set** from source first and compared cardinality, per §5 rule 24 — the sums are listed above as
ENUMERATED for that reason.

## What I could not settle

- **`intelligence.md:18`** — *"Exposed an HTTP server on :8080 (PORT env override) serving only a `/_meta`
  health endpoint for ECS checks; all real work ran on a 5-minute async ticker."* The `intelligence` repo is in
  **no** clone set, so the binary's default port, its route set and its ticker period are unverifiable here.
  What the platform *does* show cuts slightly against the flavour of the sentence: the compose block at
  `fdfa189^:docker-compose.yml:75` set `PORT=9002` and declared **no `ports:` key at all**, so nothing was
  host-reachable on any port. Settled by cloning `anthropos-work/intelligence` and reading `cmd/`.
- **`external_services.md:3`** — *"the repo is ARCHIVED on GitHub (2026-07-30)"*. GitHub archive state is not
  visible from a clone — the exact epistemic point `jobsimulation.md:24-27` makes for its own repo, and which
  it enforces by refusing the flat claim. Here the available evidence is at least *consistent*:
  `graphql-wundergraph` `origin/main` == `HEAD` == `60c229f3`, dated 2026-07-30, with **no** later commits (the
  post-archive-commit refutation that killed the jobsimulation claim does not fire). Settled by
  `gh api repos/anthropos-work/graphql-wundergraph --jq .archived`. Flagged rather than booked because the
  corpus applies two different epistemic standards to the same kind of claim in two of my files.
- **`external_services.md:293-296`, `:798-807`, `customerio-sync.md:143-145`** — production Directus S3/CDN
  wiring, prod deployment steps, and whether customerio-sync's own ECS task/module was torn down. All live in
  `infrastructure`, which has never been in a clone set. `customerio-sync.md:143-145` says so itself and
  correctly declines to assert; `external_services.md`'s prod sections do not carry the same caveat.
- **`external_services.md:707`** — studio-desk copilot *"circuit-breaker failover"* and *"via backend proxy"*.
  I verified `AI_PROVIDER_CHAIN=azure-openai,openai` (`.env.example:56`) and the model defaults
  (`src/services/ai/config.ts:21`, `:35-37` → `gpt-5.2`, `claude-opus-4-5-*`, `claude-sonnet-4-5-*`), but did
  not read the provider chain's failover implementation end to end, and "backend proxy" is ambiguous between
  studio-desk's own Express server and the platform `backend`.
- **`askengine.md:104`** — *"the Asynq pools and the Redis subscribers (`:1438` onward)"*. `:1438` at
  `ad9f3c49` is inside the comment block introducing the messenger consumer-group construction, whose actual
  code starts at `:1444-1456`. "onward" makes it defensible and I did not book it, but the anchor names a
  comment rather than the construct.
