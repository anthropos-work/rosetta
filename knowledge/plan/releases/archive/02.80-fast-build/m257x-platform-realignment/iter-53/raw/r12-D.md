# Seat D — reading #12

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| corpus/services/studio-room.md | 473 | yes |
| corpus/services/clerkenstein.md | 366 | yes |
| corpus/services/chronos.md | 245 | yes |
| corpus/services/roadrunner.md | 171 | yes |
| corpus/services/next-web-app.md | 126 | yes |
| corpus/services/gotenberg.md | 82 | yes |
| corpus/services/intelligence.md | 18 | yes |
| (protocol, read as instructed) corpus/ops/platform-alignment.md §5 | 1219 (§5 = 240–828) | yes |

Ground-truth shas confirmed in-repo before use: `stack-demo/app` `5ba17044`, `stack-demo/app/studio`
`aeec036`, `stack-demo/platform` `2adcf71`, `stack-demo/next-web-app` `bb3313bc0`,
`stack-demo/roadrunner` `87d8d44`, `stack-demo/jobsimulation` `462343b0`, `stack-demo/cms` `ca50c81`,
`.agentspace/rosetta-extensions` (authoring copy, main).

---

## Blockers

### B1 — `roadrunner.md` claims "no other platform repo references roadrunner at all", which is false and self-contradicted 18 lines later

- **Anchor:** `corpus/services/roadrunner.md:21` — *"…(M247 re-grepped `app` + `jobsimulation`
  on the consolidated clones — zero hits outside CHANGELOG), and no other platform repo references
  roadrunner at all."*
- **Why it is false:**
  - The `platform` repo references it in three places, all of which **this same blockquote cites**:
    `stack-demo/platform/repos.yml:29-31` (`- name: roadrunner … migrations: false # legacy — folded
    into app`), `stack-demo/platform/docker-compose.yml:281` (the `roadrunner:` service block, ports
    `10400:10400` / `10401:10401`, `profiles: [graphql, roadrunner, all]`), and
    `stack-demo/platform/docker-compose.yml:118` (`- ROADRUNNER_RPC_ADDR=http://roadrunner:10401`
    inside the **jobsimulation** service's `environment:`). Lines `:14-15`, `:26` and `:39-40` of the
    very same blockquote assert exactly these facts, so the file contradicts itself.
  - Two further service repos reference it in tracked files:
    `stack-demo/jobsimulation/knowledge/operational.md:68` (`| ROADRUNNER_RPC_ADDR | Roadrunner
    service address |`), `stack-demo/next-web-app/knowledge/service-dependencies.md`, and three
    `stack-demo/studio-desk/knowledge/**` docs.
  - A reader doing retirement work reads "no other platform repo references roadrunner **at all**"
    as a clearance to delete, when in fact the orchestrator still builds and starts it and a sibling
    service still carries its address.
- **Note on hesitation:** "platform repo" could be read narrowly as "service repo other than app and
  jobsimulation". Even under that reading `jobsimulation/knowledge/operational.md` falsifies it, and
  the absolute "at all" is not survivable next to `:39-40`. Booked as BLOCKER per the tie-break rule.
- **Confidence:** medium-high

### B2 — cross-file anchor `architecture_overview.md:188` names the Skiller row, not the Jobsimulation row it is cited for

- **Anchor:** `corpus/services/roadrunner.md:32-33` — *"…which is why listing it as removed
  contradicted `:26` of this very blockquote, plus `../architecture/architecture_overview.md:188`
  and `README.md:20-21`."*
- **Why it is false:** the citation is offered as evidence that **jobsimulation** is still in
  `repos.yml` + `docker-compose.yml`. `corpus/architecture/architecture_overview.md:188` is the
  **Skiller** table row (`| **Skiller** | Merged into Backend/App (July 2026) — repo
  legacy/decommissioned | …`), which says nothing about compose membership. The row that actually
  supports the claim is **`:189`** (`| **Jobsimulation** | … **Container still starts locally**
  (`docker-compose.yml:83`, default profile) as an unfederated husk; teardown **M810** |`). Verified
  by `grep -n '^| \*\*Skiller\*\*\|^| \*\*Jobsimulation\*\*' corpus/architecture/architecture_overview.md`
  → `188:` Skiller, `189:` Jobsimulation.
  The companion anchor in the same sentence, `corpus/services/README.md:20-21`, **does** resolve
  correctly (*"And three of the four (cms, jobsimulation, roadrunner) still start CONTAINERS locally
  in the default / `graphql` profile as unfederated husks…"*), which is what makes the wrong one easy
  to miss.
- **Note on hesitation:** it is an off-by-one, and the neighbouring row does say the right thing. I
  booked it because §5 rule 10 / the briefing name "a line anchor that resolves but points at the
  wrong construct" as exactly this class, and because this file's anchors are otherwise exact
  (`repos.yml:17`, `repos.yml:29-31`, `docker-compose.yml:83`, `docker-compose.yml:281`,
  `terraform/main.tf:19` all verified correct), so a reader has no reason to distrust this one.
- **Confidence:** high (that `:188` is Skiller); medium (on materiality)

---

## Minors

- `corpus/services/clerkenstein.md:3` — **`Last updated: 2026-07-14`** is stale against the file's own
  contents: it documents `fix/studio` dated **2026-07-27** (`:108`), v2.8 **M256** iter-16/iter-25
  (`:180-205`) and v2.8 **M257x iter-23** (`:270`).
- `corpus/services/clerkenstein.md:13` vs `:21` — `:13` sends the reader to
  `stack-demo/rosetta-extensions/clerkenstein/knowledge/kb-index.md` (the **pinned consumption**
  copy) while `:21` states *"the KB above is **read from the authoring copy**"* (i.e.
  `.agentspace/rosetta-extensions/`). Both paths exist and currently hold identical content, so no
  factual harm today, but the two sentences disagree about which clone is canonical — and `:6-7`
  records that this exact passage was repaired once before.
- `corpus/services/clerkenstein.md:18` — the monorepo section list enumerates **6** (`clerkenstein`,
  `demo-stack`, `stack-injection`, `stack-core`, `stack-seeding`, `alignment`); the authoring copy
  holds **11** (`alignment`, `clerkenstein`, `demo-stack`, `dev-stack`, `playthroughs`, `stack-core`,
  `stack-injection`, `stack-secrets`, `stack-seeding`, `stack-snapshot`, `stack-verify`). Reads as
  illustrative rather than exhaustive, and `CLAUDE.md` carries a fuller (still incomplete, 9) list.
- `corpus/services/roadrunner.md:20-21` — the parenthetical *"zero hits outside CHANGELOG"* over-claims:
  `stack-demo/jobsimulation/knowledge/operational.md:68` and an `app/knowledge/**` re-port checklist both
  mention `ROADRUNNER_RPC_ADDR`/`RoadRunnerService`. The load-bearing clause ("no … read in any service's
  **Go code**") is **true** — I grepped `stack-demo/app` and `stack-demo/jobsimulation` for all three
  tokens and found **zero `.go` hits** (positive control: `JUDGE0_BASE_URL` returns
  `app/internal/jobsimwiring/wiring.go:118`).
- `corpus/services/chronos.md:9` — *"moved to **in-process Asynq** running inside jobsimulation"* is
  pre-merge phrasing. The timers queue now runs in `app` — `stack-demo/app/internal/jobsimulation/worker/worker.go:20-34`
  (`Concurrency: 10`, `queues.TimersQueue: 5`). Not strictly false (the `jobsimulation` container still
  starts and still carries the code), but the corpus's own convention elsewhere is "the jobsimulation
  **domain** inside `app`". `roadrunner.md:6-9` carries the second half; `chronos.md` does not.
- `corpus/services/chronos.md:5` and `corpus/services/intelligence.md:5` — *"in mid-2026"*; both cited
  commits are **2026-04-17** (`045857c`, `fdfa189` — `git show` in `stack-demo/platform`).
- `corpus/services/next-web-app.md:21` — *"React 19.2.7"* holds for the four Next apps
  (`apps/{web,hiring,integration,maintenance}` all `^19.2.7`); `apps/mobile/package.json:31` declares
  `^19.1.0`. The row is scoped to the monorepo generally.
- `corpus/services/studio-room.md:35` — *"invoked … as a Python subprocess (`python3 studio/gen.py`)"*;
  in `colony.Development` the interpreter is the managed venv
  (`studio/studio-venv/bin/python3`, `app/internal/cms/studio/studioManager.go:94-96,112-119`). The same
  document states this correctly at `:337-340`, so the two are inconsistent in emphasis only.
- `corpus/services/studio-room.md:363` — *"`export ENVIRONMENT=local  # or production`"* omits
  `development`, which is one of the three tracked configs the same doc lists at `:82-85`.

---

## Positively cleared (audited zeros)

Labelled per the briefing. **ENUMERATED** = I built the predicate's set from source myself and compared
cardinalities. **RE-DERIVED** = I only re-ran arithmetic the document displays (weakest clearance).

### studio-room.md

- **ENUMERATED** — *"`gen.py` registers **exactly nine** arguments (`gen.py:484-492`)"*. I enumerated
  `grep -n add_argument gen.py` independently: exactly 9 `parser.add_argument` calls, at lines
  **484–492**, no commented-out siblings, matching the doc's flag list one-for-one
  (`-i/--interactive`, `-m/--media` default `simulation`, `-f/--force` `dest="forced"`, `--simid`,
  `--branch {stable,experimental}` default `stable`, `--prompt`, `--annotations` default `"{}"`,
  `--pipeline` default `linear`, `--blueprint`). `postgen.py` separately registers 5, with
  `--media`/`--simid`/`--target` all `required=True` (`postgen.py:395-399`) — the doc's `:159` claim.
- **ENUMERATED** — the `--template` warning box. `--template` appears in **no** `add_argument`;
  `parse_argument` (`gen.py:18-28`) is `parse_known_args` + `dict(zip(unknown[::2], unknown[1::2]))`,
  so a stray `--template foo` is silently folded into the request. `translate_legacy_blueprint`
  (`gen.py:212`, with `_LEGACY_TEMPLATE_DEFAULTS` at `:205`, block `205-238`) back-fills
  `max_tasks`/`duration` for exactly the **three** names the doc lists (`micro challenge`,
  `scenario challenge`, `collaborative challenge`) and warns *"is ignored; asset type is now inferred
  from task interactions"* for anything else — verbatim. `validate_blueprint_exclusivity`
  (`gen.py:241-271`) raises *"Cannot combine --blueprint with content parameters: …"* and its
  `allowed_with_blueprint` set is exactly the doc's list (`blueprint, media, forced, interactive,
  branch, pipeline` + the four `*_path`).
- **ENUMERATED** — the `GenMode` enum (`services/ai.py:35-42`): FAST/STRICT/EXECUTION/CREATIVE/REASONING
  + `DEFAULT = EXECUTION`. Five members, exactly as documented.
- **ENUMERATED** — post-gen targets. `agents/simulation/postgen/` holds `exporter.py`, `guidance.py`,
  `metadata.py`, `testing.py`, `toolkit.py`, `translation.py`; `__init__.py:52-74` dispatches on exactly
  `toolkit`, `guidance`, `metadata`, `translation` — the doc's four-row table — with `testing` present
  as a module but **not** a dispatch key, which is precisely what `:170-173` says. Export is not a
  selectable target (it is `SimulationExporter.export`, `postgen.py:370-373`).
- **ENUMERATED** — `requirements.txt` (10 non-blank lines): openai, anthropic, rich, pyyaml, requests,
  jinja2, mistralai, pytest, pytest-asyncio — matches the doc's list, and there is no `aiohttp`.
- **RE-DERIVED / anchor-checked** — every `file:line` in the file resolved to the right construct:
  `studioManager.go:119` (`runCommand(ctx, pyBin, append([]string{"studio/gen.py"}, tokens...))`),
  `studioManager.go:94-96` (the `studioVenvDir/Python/Pip` const block), `gen.py:273-282`
  (`setup_generation_request`), `gen.py:450-456` (the `work_paths` loop, fallback
  `path_key.replace('_path','')`), `exporter.py:518-519` (`postgen_zip`/`published_zip`),
  `exporter.py:513-550` (`_create_export_package`, ending at the `shutil.rmtree(target_dir)` on the
  final, newline-less line 550), `services/ai.py:1-2,383,530,664` (OpenAI / AzureOpenAI / Anthropic
  client construction), `model.py:59,467-469` (the `self._cache` taxonomy memo),
  `taxonomy.py:11` (`BASE_URL = "https://api.anthropos.work/api"`), repo `CLAUDE.md:12-14`.
- **ENUMERATED** — the Asynq wiring at `:35`: `app/internal/cms/worker/worker.go:29-32` gives
  `Concurrency: 5` with `AiVideoQueue: 7` / `StudioQueue: 3` — the doc's numbers and its correct
  reading of them as priority weights, not concurrency caps.
- **ENUMERATED** — the blueprint sample at `:285-303` is key-for-key the head+tail of
  `tests/e2e/blueprints/technical.json`; the five e2e blueprints (b2b, business, none_chat, none_voice,
  technical) and the three `knowledge/development/asset-examples/blueprints/` files all exist.
- **ENUMERATED** — `max_tokens = 4000` in **all three** tracked configs (`config_template.ini:22`,
  `development_config.ini:21`, `production_config.ini:21`); `.gitignore:5-6` ignores
  `configs/local_*` and `configs/test_*`; `load_services_settings` (`gen.py:41-53`) overrides exactly
  the six `*_API_KEY`/`*_ENDPOINT` keys from the environment, in code, not via interpolation.
- **ENUMERATED** — the merge banner: `stack-demo/app/.github/workflows/build-production.yml:29`
  reads `additional_repo: "anthropos-studio-room:studio"`; `stack-demo/app/Dockerfile:28` is
  `FROM python:3.11-slim`; `stack-demo/cms/studio/` still exists (the pre-merge home);
  `studio` appears nowhere in `platform/repos.yml`.

### clerkenstein.md

- **ENUMERATED** — the five DNA gene counts. I parsed each `alignment/dna/*.json` and counted
  `capabilities[].variants[]` (the field is `variants`, **not** `genes` — a wrong-field-name absence I
  caught with a structure dump before concluding anything): `clerk-2.6.0` **14 caps / 27 genes**,
  `clerk-js-5` 6 caps / **9**, `clerk-multi-1` 5 caps / **9**, `clerk-deploy-1` 3 caps / **7**,
  `clerk-express-1` **5 caps / 13**. Every figure in the ⚠ score box and in `:68-76` matches.
- **ENUMERATED** — the M219 org-eid fix. `clerk-backend/store.go:138` is `func (s *Store)
  SeedOrgIdentity`, `:151` is `func (s *Store) LookupOrgEid`; `alignment/dna/clerk-2.6.0.json:131` is
  the `MembershipOrgIdentity` description carrying the quoted *"M219 landed the fix … taking the Go
  surface 97.2% -> 100%."* The gene `real-org-eid` exists and is the sole variant.
- **ENUMERATED** — `alignment/cmd/alignctl/run.go:134-135` is exactly
  `ExitRegressed = 2` / `ExitUnmeasurable = 3`, inside a `const` block whose 14 preceding comment lines
  state the rule the doc paraphrases. (§5 rule 10 applied: I read `115-145`, not just the two lines.)
- **ENUMERATED** — the BAPI/FAPI twin table. `clerk-backend/server.go:47` registers
  `GET /v1/users/{userID}/organization_memberships`; `clerk-frontend/server.go:186` registers
  `GET /v1/me/organization_memberships` → `handleMeOrganizationMemberships` (`:500`);
  `clerk-frontend/meorgmemberships_test.go` exists. `clerk-js-5.json` has a `Me` capability with
  variants `universal-user` + `unauthenticated` and **zero** occurrences of the string
  `organization_memberships` — the "not yet a measured gene" caveat at `:141-143` is exact.
- **ENUMERATED** — the single-tenant seat limitation. `clerk-frontend/registry.go:24` (`activeKey`),
  `:67` (`active()`), `:79` (`Select`); `clerk-frontend/server.go:125`
  (`clientID: "client_clerkenstein"`), `:665` (`s.sessID = "sess_clerkenstein"` inside
  `establishLocked`), `:107` (`signedOut bool`), `:559` (`s.signedOut = true`), `:654`
  (`/v1/demo/select` clears it). `grep -rn '\.Cookie(' --include='*.go'` over `clerkenstein/`
  (non-test) returns **zero** — the doc's *"`r.Cookie(...)` is called nowhere"* is exact. All five
  named tests exist in `clerk-frontend/server_test.go` at 256/286/390/427/461.
- **ENUMERATED** — the M220 clerk-js fix. `clerk-frontend/server.go:35-67` is the block; `:57` is
  `clerkJSFetchTimeout = 15 * time.Second`, `:67` `var clerkJSClient = &http.Client{...}` with the
  literal comment *"Explicitly NOT http.DefaultClient"*; `FAKE_FAPI_CLERKJS_CACHE` and
  `FAKE_FAPI_CLERKJS_CDN` both exist.
- **ENUMERATED** — the M39/M224 roster thread, both ends: producer
  `stack-seeding/seeders/roster.go:53-54` (`org_name`/`org_slug`), `:77` (`org_is_hiring,omitempty`),
  `:140`/`:237` (`orgSlugFor`), `:155`/`:246` (`IsHiringOrg()`); consumer
  `clerk-frontend/registry.go:144,162` and `clerk-frontend/resources.go:32-43`, `:50-51`
  (`orgNameDefault = "Clerkenstein Demo Org"` / `orgSlugDefault = "clerkenstein-demo"`), `:275-276`
  (`if u.OrgIsHiring { orgPublicMeta["isHiring"] = true }` — the conditional-emit rule, verbatim).
- **ENUMERATED** — the colony pin drift box at `:270-275`. `.agentspace/rosetta-extensions/clerkenstein/go.mod:8`
  = `colony v0.34.3`; `stack-demo/app/go.mod:16` = `colony v0.35.2` and `:31` = `clerk-sdk-go/v2 v2.7.0`;
  `sentinel/go.mod:8` and `storage/go.mod:7` = `v0.34.3`. Every clause of the box holds, including
  *"`sentinel` and `storage` are still on `v0.34.3`"*. (`messenger` is `v0.35.2` — consistent with the
  box, which does not claim otherwise.)
- **ENUMERATED** — the studio-desk role list at `:136-139`: `src/index.ts:96` **and**
  `app/services/userService.ts:16` both read
  `const STUDIO_ACCESS_ROLES = ['admin', 'org:admin', 'content_creator', 'org:content_creator'];`
  each preceded by the quoted comment. Both line numbers exact.
- **ENUMERATED** — the repo-structure table: `authn/ clerk-backend/ clerk-frontend/ clerk-webhook/
  shared/ deploy/ cmd/ alignment/` are exactly the 8 non-`knowledge` dirs;
  `cmd/` holds `mintpk`, `fake-fapi`, `fake-bapi` (+ `jwtkey`, unlisted — the doc says
  *"standalone binaries: …"* not "only"); `alignment/cmd/` holds exactly the five named runners and
  `alignment/` holds exactly the five `golden*` dirs. `FAKE_FAPI_ROSTER` is read by
  `cmd/fake-fapi/main.go:25` and `cmd/fake-bapi/main.go:36`.
- **ENUMERATED** — `:352-357`: `clerkenstein/alignment/scripts/` exists (`gate.sh`, `drift-check.sh`)
  and reads `ALIGN_DIR` with default `$base/../../alignment` (`gate.sh:30`, `drift-check.sh:16`);
  the reusable `rosetta-extensions/alignment/` section has **no** `scripts/` dir;
  `clerkenstein/.github/workflows/alignment.yml` exists while the monorepo root has **no**
  `.github/workflows/` — so the "git-tracked but inert" claim holds. Gate thresholds
  `GATE_OVERALL:-95` / `GATE_CRITICAL:-100` (`gate.sh:31-32`) match *"Gate is ≥95 / =100"*.

### roadrunner.md

- **ENUMERATED** — `repos.yml` at `2adcf71` holds exactly **9** entries (app, cms, jobsimulation,
  sentinel, storage, messenger, roadrunner, next-web-app, studio-desk); `roadrunner` is at **29-31**
  with the quoted comment; `jobsimulation` at **17**. `git diff 1e8e754 2adcf71 -- repos.yml` shows the
  merge deleted the `graphql-wundergraph` entry — so *"the count dropped when platform `2adcf71`
  deleted the router entry"* is exactly right (10 → 9).
- **ENUMERATED** — `docker-compose.yml:281` is the `roadrunner:` service key; `:309`
  `profiles: [graphql, roadrunner, all]`; `:83` is `jobsimulation:` with `:140`
  `profiles: [graphql, jobsimulation, all]`; `:118` `ROADRUNNER_RPC_ADDR=http://roadrunner:10401`.
  The roadrunner env block sets `JUDGE0_BASE_URL=http://52.48.139.23:2358`, `PORT=10400`,
  `RPC_PORT=10401`, `REDIS_ADDR=redis:6379`, `REDIS_STREAMS_INDEX=4`, `REDIS_WORKER_INDEX=0`,
  `ENVIRONMENT=development` — the doc's whole env table — and **not** `JUDGE0_API_KEY`, which is the
  doc's `:151` claim, verified as an absence with the block read in full rather than grepped.
- **ENUMERATED** — `stack-demo/roadrunner/terraform/main.tf:19` is
  `service_desired_count          = 1`, inside `module "roadrunner"`. Repo HEAD `87d8d44` is dated
  **2026-06-19** — the doc's date is right.
- **ENUMERATED** — the async-task paragraph, every scalar: `internal/worker/queues/queues.go`
  defines the single `DefaultQueue = "roadrunner:default"`; `internal/worker/tasks/tasks.go` defines
  the single `RoadrunnerSubmissionResult = "roadrunner:submissionresult"`;
  `internal/worker/worker.go:25` `Concurrency: 10`; `internal/runner/runner.go:126`
  `asynq.NewTask(..., asynq.MaxRetry(3))` enqueued **once per `CreateSubmission`** (and both RPCs route
  through `CreateSubmission`, `rpcsrv/rpc.go:33,50`); `runner.go:244` `var maxRetries = 15`, `:273`
  `time.Sleep(1 * time.Second)`, `:277-278` publishes `RoadrunnerSubmissionCompleted`;
  handler `HandleSubmissionResultTask` at `runner.go:237` — i.e. in `internal/runner/runner.go`, exactly
  as the code-map note at `:77` says.
- **ENUMERATED** — the RPC surface is exactly **3** methods (`rpcsrv/rpc.go:20,43,60` —
  `Submission`, `SubmissionPackage`, `SubmissionResult`), matching the table, with `rpc.go` the only
  file in `internal/rpcsrv/`.
- **ENUMERATED** — zero test files: `find . -name '*_test.go' | wc -l` = **0**;
  `Dockerfile:18` is `RUN go test -v ./...`. Both `:160` claims exact.
- **ENUMERATED** — `go.mod:3` `go 1.25.0`, `:10` `hibiken/asynq v0.25.1`; `main.go:9`
  `_ "github.com/joho/godotenv/autoload"`; `cmd/root.go:68` `strconv.Atoi(os.Getenv("REDIS_WORKER_INDEX"))`
  with a hard return on error; `internal/lsp/lsp.go` exists and `grep -rn 'internal/lsp' --include='*.go'`
  returns **zero** importers — the "not wired into any running server" claim is a measured absence.
  `/_meta` comes from `colony.NewHTTPServer` (`cmd/root.go:110`) plus
  `terraform/main.tf` `health_check_path = "/_meta"`.
- **ENUMERATED (the load-bearing merge claim)** — `grep -rn 'ROADRUNNER_RPC_ADDR|RoadRunnerService|roadrunner:10401'`
  over `stack-demo/app` and `stack-demo/jobsimulation`: **zero `.go` hits** in either. Positive control in
  the same pass: `JUDGE0_BASE_URL` → `app/internal/jobsimwiring/wiring.go:118`
  (`jsrunner.NewRunnerManager(getenv("JUDGE0_API_KEY"), getenv("JUDGE0_BASE_URL"))`). So *"`backend` reads
  `JUDGE0_BASE_URL` and calls Judge0 directly"* is confirmed at the wiring site, not inferred.
- **ENUMERATED** — the "removed from both files" set: `grep -in 'skiller|skillpath|intelligence|chronos'`
  over `repos.yml` + `docker-compose.yml` at `2adcf71` returns **nothing** (positive control:
  `grep -c backend docker-compose.yml` = 16). So all four are genuinely out of orchestration.

### next-web-app.md

- **ENUMERATED** — `"next": "^16.2.7"` in exactly the **four** app package.jsons the doc names
  (`apps/{web,hiring,integration,maintenance}`); root `package.json` gives `turbo ^2.9.6`,
  `node >=24.0.0`, `packageManager pnpm@10.30.3`; `UPGRADE-IMPACT-next16.md` is at the repo root.
- **ENUMERATED** — `middleware.ts` does **not** exist anywhere outside `node_modules`
  (`find . -name middleware.ts` → empty); `apps/web/src/proxy.ts` and `apps/hiring/src/proxy.ts` both
  exist and both begin with `clerkMiddleware`. Repo `CLAUDE.md:55` is verbatim *"**Clerk middleware**
  lives in `src/proxy.ts` (Next 16 renamed the `middleware.ts` convention → `proxy.ts`)…"* and
  `CLAUDE.md:15` is *"Turborepo · pnpm · Next.js 16 App Router · TypeScript strict."* — both anchors
  exact, both quoted correctly.
- **ENUMERATED** — the public allowlist. I read `proxy.ts:7-58` in full; all seven routes the doc names
  (`/login`, `/sign-up`, `/checkout`, `/free-trial`, `/monitoring`, `/print`, `/api/bunny/thumbnail`)
  are present, and `/print` is handled by `isPrintRoute` → `validatePrintToken` (`proxy.ts:59,66-72`),
  implemented in `packages/core-js/src/security/printToken.ts:16,49` reading `process.env.PRINT_ROUTE_SECRET`.
  The doc's "includes" is honest — the real list is longer.
- **ENUMERATED** — 8 locales on disk: `configs/i18n/messages/{de,en,es,fr,it,ja,nl,pt}` = **8** dirs.
- **ENUMERATED** — storybook removal: no `.storybook` dir anywhere, no `storybook` key in any
  `package.json` script block, and `configs/tailwind/storybooks.css` is the sole remaining trace —
  exactly the three things `:70-71` asserts.
- **ENUMERATED** — `find . -name 'Dockerfile*'` (excluding node_modules) returns **only**
  `./Dockerfile.dev` (`FROM node:24-alpine`, `:32` `pnpm turbo build --filter=@anthropos/web-app`),
  while repo `CLAUDE.md:82-84` still claims two — so `:116` ("the repo `CLAUDE.md` 'two Dockerfiles'
  note is stale") is right in both directions.
- **ENUMERATED** — the endpoint migration. `docker-compose.yml:352` bakes
  `NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT: http://${PUBLIC_HOST:-localhost}:8082/graphql/query` (build arg)
  and `:361` sets the same at runtime; the `backend` service exposes `8082` with `PORT=8082`
  (`docker-compose.yml:39,55`); `app/internal/web/backend/backend.go:317` registers
  `e.Any("/graphql/query", …)`. There is **no `graphql` router service** left in the compose file, and
  `git show --stat 2adcf71` is the "remove the WunderGraph router entirely" merge. `:5050` survives in
  the repo only as a build-arg default (`Dockerfile.dev:23`), a commented note
  (`apps/web/.env.example:59`), a codegen comment (`packages/graphql/codegen.ts:8`) and stale
  `knowledge/**` docs — none of which the corpus page claims otherwise about.
- **ENUMERATED** — app ports and package names: `@anthropos/web-app` `--port 3000`,
  `@anthropos/hiring-app` `--port 3001`, `@anthropos/integration` `--port 3002`,
  `@anthropos/maintenance-app` (no dev port), `@anthropos/mobile` `expo start --port 3031` and
  excluded from the workspace via `pnpm-workspace.yaml`'s `'!apps/mobile'`. Test scripts exist in
  `apps/web` and `apps/hiring` only (of the four workspace apps) — the `:108-109` note.
- **ENUMERATED** — data layer: `packages/graphql/package.json` depends on `graphql-request ^7.4.0`,
  `@tanstack/react-query ^5.99.2`, `@graphql-codegen/client-preset ^4.8.3`, with generated output at
  `packages/graphql/src/__generated__/`; **no** `apollo` dependency anywhere. `packages/ui` +
  `apps/web` both pin `antd ^6.3.7`.
- **ENUMERATED** — AI-readiness gating surfaces exist as described:
  `apps/web/src/components/ai-readiness/aiReadiness.constants.ts:26`
  (`AI_READINESS_FLAG = 'flag_ai_readiness'`),
  `apps/web/src/app/(authenticated)/(verified)/ai-readiness/`, and the GraphQL
  `aiReadinessEnabled` query (`packages/graphql/src/hooks/aiReadiness/useAiReadinessEnabled.tsx:14-20`).
- **ENUMERATED** — `/logpoint/*` rewrites at `apps/web/next.config.mjs:197,201,205`;
  `apps/web/src/app/monitoring` route dir exists.
- **ENUMERATED** — `make up PROFILE=frontend` / `make up-frontend` are both real
  (`platform/Makefile:1,65-66,119`), and `next-web-app`'s compose profile is `[frontend, all]`
  (`docker-compose.yml:368`).

### gotenberg.md

- **ENUMERATED** — every claim in this 82-line file resolves. `docker-compose.yml:370-383`:
  `image: gotenberg/gotenberg:8`, command `["gotenberg","--api-port=3200","--api-timeout=60s",
  "--libreoffice-restart-after=50"]`, `ports: ["3200:3200"]`, `profiles: [graphql, backend, all]` —
  the doc's YAML block is a faithful transcription. Consumer:
  `app/internal/converter/gotenberg.go:13` (`&http.Client{Timeout: 90 * time.Second}`), `:16`
  (`func ConvertToPDF(ctx, gotenbergURL string, document []byte, filename string) ([]byte, error)`),
  `:31` (`gotenbergURL+"/forms/libreoffice/convert"`). `GOTENBERG_URL=http://gotenberg:3200` is set in
  the **backend** service's compose `environment:` (`docker-compose.yml:51`) and read at
  `app/main.go:243`. I also enumerated the consumer set rather than trusting *"the only consumer"*:
  `grep -rn 'ConvertToPDF' --include='*.go'` returns two call sites, both inside `app`
  (`internal/web/backend/coursebuilder/extract.go:77`, `internal/worker/tasks/user_import_resume_2d.go:68`),
  and no other repo in `stack-demo/` references it.

### chronos.md / intelligence.md

- **ENUMERATED** — both removal commits are real and their subjects are quoted verbatim:
  `platform` `045857c` *"chore: remove chronos service from orchestration and update related
  documentation"* (touches `docker-compose.yml -37`, `repos.yml -4`) and `fdfa189` *"chore: remove
  intelligence service from local dev orchestration"* (`docker-compose.yml -25`, `repos.yml -3`).
  Neither `chronos` nor `intelligence` appears in `repos.yml`/`docker-compose.yml`/`Makefile` at
  `2adcf71`, and nothing `depends_on` either.
- **ENUMERATED** — `jobsimulation` PR **#395** on branch `feat/remove-chronos-and-realtime` and commit
  **09631fb2** *"refactor: remove Chronos references and update documentation to reflect Asynq
  integration for session timeout management"* both exist in `stack-demo/jobsimulation`'s history —
  all three identifiers in `chronos.md:7` are correct.
- The Chronos data model, RPC signatures, triggerer behaviour (`LIMIT 10`, `FOR UPDATE SKIP LOCKED`,
  3 s run timeout, Sentry cron heartbeat), env-var table and CLI flags are **not verifiable** — see
  below.

---

## What I could not settle

1. **Everything below `chronos.md`'s banner.** The `chronos` repo is not cloned anywhere in this
   workspace (`stack-dev/` holds only `studio-desk`; `stack-demo/` has no `chronos`) and it is not in
   `repos.yml`, so `make init` will never produce it. The schema DDL, the `ScheduleTimer`/
   `CancelScheduledTimer` shapes, `Go 1.25`, `search_path=chronos`, ports 8080/8081, the `--interval`
   default and the `LIMIT 10 / FOR UPDATE SKIP LOCKED / scheduled_at < now() + interval '1 second'`
   triggerer detail are all unverifiable here. **Settled by:** a clone of
   `git@github.com:anthropos-work/chronos` at any sha, or a git-archive of `internal/database/schema.sql`
   + `internal/triggerer/triggerer.go`.
2. **GitHub archive states.** `roadrunner.md:24-25` asserts the skiller (2026-07-01) and skillpath
   (2026-07-31) repos "really are archived", `:29` that jobsimulation's is (2026-07-31), and `:35-36`
   that chronos's is **not** (last push 2026-04-23) — echoed by `chronos.md:3` and `intelligence.md:8`.
   None of this is derivable from local clones. **Settled by:** `gh api
   orgs/anthropos-work/repos --jq '.[]|[.name,.archived,.pushed_at]'` (network + org auth).
3. **The current alignment scores.** `clerkenstein.md`'s ⚠ box asserts four surfaces measured at
   100%/100% *today* and the express surface as unmeasurable. I verified the DNAs, the gene counts,
   the gate thresholds, the exit-code contract and the landed M219 fix, but not the scores — those
   require executing `alignctl run` per surface (and, for express, a box with `@clerk/express`
   `node_modules`). The doc's own `:270-275` warning that a `clerk-deploy-1` score taken against
   colony `v0.34.3` is not measuring `app`'s `v0.35.2` is **confirmed on the pins** and therefore
   makes the 7/7 figure stale-by-construction until re-run.
4. **`roadrunner.md:6`'s "~386-commit `app` bump" and the M247/M231 re-verification provenance** —
   a claim about a past audit's method, not about source.
5. **`clerkenstein.md:120-121`'s "~4.05 s 3-attempt retry ladder"** and the latency figures at
   `:310-312` (0.17–0.19 s healthy / ~127 s blackholed) — measurements, not source facts. The
   *mechanism* they describe (missing FAPI twin → 404 → clerk-js retry; unbounded `http.Get`) is
   confirmed in code and in the M220 fix comment.
