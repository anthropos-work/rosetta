# Seat F — reading #11

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/services/ant-academy.md` | 436 | yes |
| `corpus/services/studio-desk.md` | 435 | yes |
| `corpus/services/jobsimulation.md` | 229 | yes |
| `corpus/architecture/platform-migration-status.md` | 189 | yes |
| `corpus/services/skillpath.md` | 107 | yes |
| `corpus/architecture/frontend_architecture.md` | 105 | yes |

Also read in full as protocol input (in scope, not assigned): `corpus/ops/platform-alignment.md` §5 (lines 240–829 of 1219).

Ground-truth clones opened this pass: `stack-demo/app` (`5ba1704482cf812b130c2d3673afd09f4f7f22e5` = **v1.363.2**),
`stack-demo/app/studio` (`aeec036a51c8a4ae0c5b8f7d5d21cfa7086b658e`), `stack-demo/platform` (`2adcf714…`),
`stack-demo/next-web-app`, `stack-demo/ant-academy` (`9c3843cd35018c9c396fe7d511898d61dd7d260d`),
`stack-demo/studio-desk`, `stack-demo/jobsimulation`, `stack-demo/cms`, `stack-demo/graphql-wundergraph`
(`60c229f3…`), `stack-demo/messenger`, `.agentspace/rosetta-extensions`.

---

## Blockers

### B1 — `jobsimulation.md:95` names the WRONG consumer set for the jobsim RPC — it lists `Backend` (which the same file says makes no such call) and omits `cms` (which really does)

- **Anchor:** `corpus/services/jobsimulation.md:95` —
  > "**RPC**: `internal/rpcsrv` — consumed by Backend (incl. the in-process skill-path engine) and Messenger via `JOBSIMULATION_RPC_ADDR=http://jobsimulation:8401`."

- **Why it is false:**

  **(a) It self-contradicts the same file 60 lines earlier.** `jobsimulation.md:34-35` says:
  > "`app` itself makes **no** outbound jobsim RPC — those are in-process calls now."

  Both cannot be true. Measured: **no Go file in `app` reads `JOBSIMULATION_RPC_ADDR`** —
  `find . -name '*.go' -not -path './vendor/*' | xargs grep -ln JOBSIMULATION_RPC_ADDR` returns **0 files**
  (positive control in the same pass: the identical pipeline finds `SKILLER_RPC_ADDR` in
  `app/internal/jobsimwiring/wiring.go`). The only `app`-side hits are planning docs under
  `app/knowledge/plan/`. `docker-compose.yml:52` does set the var on the `backend` container, but nothing
  in the binary reads it — this is exactly §5 rule 23's "a named-consumer list survives the merge that
  moved the consumer": the var is there, the container is there, and the read is not.

  **(b) It omits `cms`, the one consumer besides messenger that actually reads the var.**
  - `stack-demo/cms/cmd/root.go:123` → `os.Getenv("JOBSIMULATION_RPC_ADDR")`
  - `stack-demo/cms/cmd/similarity/calculatesimilarity.go:69` → same
  - `stack-demo/platform/docker-compose.yml:167` sets `JOBSIMULATION_RPC_ADDR=http://jobsimulation:8401`
    inside the **`cms`** service block (`cms:` starts at `:144`, `storage:` at `:189`)
  - `stack-demo/app/main.go:1190-1191` names the callers explicitly:
    *"the in-app JobSimulationService edge — serves the externally-reached subset (**messenger's**
    GetSession/GetSessionInsights + **cms's** GetUserSessions)"*

  The correct set is **`cms` + `messenger`** (`docker-compose.yml:167` and `:258`), not "Backend and
  Messenger". A reader doing the M809/M810 re-point from this line would re-point a service that makes no
  call and leave the one that does.

- **Confidence:** high

### B2 — `ant-academy.md:196` says the academy's demo-config is "the **four** bring-up patches"; it is **five**, and two other corpus files already say five

- **Anchor:** `corpus/services/ant-academy.md:196-198` —
  > "The academy's entire demo-config — **the four bring-up patches** (`academy-fs-published-fallback` + `-public` + `-chapter-body` + `ant-academy-dev-origins`) applied to the **ephemeral clone**, plus the `code/.env.local` overlay … — is written and applied by `demo-stack/ant-academy.sh` at bring-up."

- **Why it is false:**

  `rosetta-extensions/demo-stack/ant-academy.sh` defines and applies **five** clone patches, not four. The
  fifth is `ant-academy-back-to-cockpit` (M249):
  - declared at `demo-stack/ant-academy.sh:90` — `COCKPIT_PATCH="$HERE/../stack-injection/apply-ant-academy-back-to-cockpit.sh"`
  - applied on the **launch** path at `:571` (`if [ -x "$COCKPIT_PATCH" ] && "$COCKPIT_PATCH" apply "$ACADEMY_ROOT"; then`),
    alongside the other four at `:521`, `:538`, `:548`, `:555`
  - applied **unconditionally** in the M245 reconcile at `:176-181` (the in-file comment: *"applied
    UNCONDITIONALLY (a nav item, not content fill)"*) — i.e. it is not even gated by
    `DEMO_NO_ACADEMY_FILL`, unlike three of the four the doc does list
  - reverted on `--stop` at `:214-216`
  - the applier exists: `stack-injection/apply-ant-academy-back-to-cockpit.sh`; the manifest exists:
    `demo-stack/patches/ant-academy-back-to-cockpit/`

  **And the corpus already contradicts itself on this number:**
  - `corpus/ops/demo/demopatch-spec.md:146` — *"the **five** `ant-academy` patches (`ant-academy-dev-origins`, `academy-fs-published-fallback`, `academy-fs-published-public`, `academy-fs-published-chapter-body`, **`ant-academy-back-to-cockpit`**…)"*
  - `corpus/ops/demo/demopatch-spec.md:227` — *"`ant-academy-back-to-cockpit` (native-run, **`ant-academy` goes 4 → 5**)"*
  - `corpus/ops/demo/frontend-tier.md:530` also lists it in the academy patch set

  `ant-academy.md` never mentions `ant-academy-back-to-cockpit` anywhere (grep over `corpus/` returns hits
  only in `demopatch-spec.md` and `frontend-tier.md`). The sentence claims to enumerate the academy's
  **entire** demo-config, so the omission is load-bearing: a reader auditing the academy's demo-patch
  surface from its own service doc gets 4 of 5, and the missing one is the only patch applied
  unconditionally.

- **Confidence:** high

---

## Minors

- `ant-academy.md:246` — "~26 Playwright e2e spec files (tests/e2e/)". Measured **31**
  (`find tests -name '*.spec.js' | wc -l` = 31, all under `code/tests/e2e/`). The sibling claim "1000+
  Vitest tests" is comfortably true (2708 `it(`/`test(` call sites across 244 test files).
- `ant-academy.md:137` — inline-quotes `emptyCatalogView() = { chapters: [], skillPaths: {}, series: [] }`.
  The real return (`code/src/lib/serverTenant.js:115-117`) is
  `{ chapters: [], skillPaths: {}, series: [], bundles: PUBLIC_BUNDLES, catalogVersion: CATALOG_VERSION }`.
  No consequence for the "0 cards" claim, but it is presented as a literal equation.
- `ant-academy.md:146` — "`code/public/catalog.json` (~2,667 entries)". The committed file carries
  **2715** `courses` entries (7 `languages`). Generated artifact; hedged with "~".
- `ant-academy.md:324` / `:345` — "The **app's** env file is `code/.env`, not the repo root" contradicts
  the repo-root `CLAUDE.md:255` — *"the React app reads **only** from `code/.env.local`"*. Next.js loads
  both, so the "only" in `CLAUDE.md` is the false half; `ant-academy.md` agrees with the academy's own
  `CLAUDE.md:129,164` (`code/.env`). Booked as a cross-file inconsistency, not a functional error.
- `ant-academy.md:390` — the "other content-pipeline helpers" list omits `export-path`, which is present in
  `ant-academy/.claude/skills/` (16 skill dirs incl. `_shared`).
- `jobsimulation.md:99` — the anchor `internal/graph/queries.resolvers.go:70` resolves correctly **in the
  frozen `jobsimulation` repo** (line 70 is exactly `func (r *queryResolver) JobSimulationResult(…)`), but
  the live code path is `app/internal/web/backend/graphql/graph/resolver_jobsimulations.go:893` — the same
  function, in the repo the doc's own banner says to grade against. `app/internal/jobsimulation/graph/`
  contains only `model/`. The claim itself (persisted read, plain Ent SELECTs, no recompute) is TRUE in
  both copies. Hesitated on BLOCKER; kept MINOR because the path is unqualified and correct for the repo
  the file is named after.
- `jobsimulation.md:69` — "Profile: `graphql` (default) and `jobsimulation`". `docker-compose.yml:140` is
  `profiles: [graphql, jobsimulation, all]` — `all` is omitted.
- `jobsimulation.md:67` — "consistent with the banner at :25-27". The legacy-schema statement is at
  **:30** (*"The old `jobsimulation` DB schema is legacy — no longer authoritative"*); :25-27 is the
  table-name paragraph.
- `frontend_architecture.md:39` — "~15 sites hitting `NEXT_PUBLIC_BACKEND_API_URL`". Measured **35
  occurrences across 25 files** (22 code files, plus 2 `.env.example` and 1 `.md`). All five named
  examples verified present. The undercount strengthens rather than weakens the claim it supports.
- `frontend_architecture.md:29-35` — the "Core Packages (`packages/`)" table omits `packages/design`
  (present in the tree alongside `core-js`, `graphql`, `ui`).
- `frontend_architecture.md:99` — "300+ commits / month". Measured on `next-web-app`: **247** for May 2026,
  **443** for July 2026. The May figure the sentence is scoped to ("Recent UX Work (May 2026)") is under 300.
- `platform-migration-status.md:17`, `:27`, `:143` — the cross-doc anchors
  `#4--detection--six-signals-cheapest-first`, `#6--classification--the-map`,
  `#8--fence--so-it-cannot-silently-recur` carry a doubled hyphen after the section number. The headings
  (`platform-alignment.md:180`, `:830`, `:883`, byte-dumped) slug to `#4-detection--six-signals-cheapest-first`
  etc. Broken in-repo links, no factual consequence.

---

## Positively cleared (audited zeros)

**ENUMERATED** — predicate re-derived from source independently, cardinality compared:

1. **`ant-academy.md:250-267` — the complete `isPublic` matcher.** Read `code/proxy.js` end-to-end and
   listed every entry of `createRouteMatcher([...])`. The doc's 9 groups cover **every** pattern with no
   extras and no omissions: `/api/_meta(.*)`, `/api/meta(.*)`, `/robots.txt`, `/sitemap.xml`,
   `/sitemap(.*)`, `/llms.txt`, `/llms-full.txt`, `/.well-known/(.*)`, `/courses`, `/courses/(.*)`,
   `/sign-in(.*)`, `/no-organization`, `/verify/(.*)`, `/api/verify/(.*)`, `/api/ai/chat`, `/library`,
   `/library/(.*)`, `/free`, `/free/(.*)`, `/local-content/(.*)`, `/catalog.json`,
   `/academy-manifest.json`, `/`, `/latest(.*)`, `/chapters/(.*)`, + the `DEV_LOGIN_ENABLED` pair and the
   `VISUAL_BYPASS` triple. `/api/chapters/*` is genuinely absent from the matcher, as the doc says.
2. **`ant-academy.md:280` — "There is no `/library/[slug]` route".** Enumerated `code/app/**` route dirs:
   `(public)/library`, `(public)/free`, `(authed)/courses/[slug]`, `(authed)/chapters/[slug]`,
   `(authed)/verify/[certId]`, `(authed)/latest`. No `[slug]` under `library` or `free`. Correct.
3. **`jobsimulation.md:24-30` — "the 23 session/run tables".** `grep -c '^CREATE TABLE'
   app/terraform/migrations/20260722081626_jobsim_data_model.sql` = **23**, and I listed all 23 names; every
   name the doc cites is in the set. The `sessions` → `job_simulation_sessions` rename is exact:
   `20260722104506.sql:2` creates the new table, `:79` (the file's last line, `wc -l` = 79) is
   `DROP TABLE "sessions";`, and no later migration re-creates `sessions` (`grep -rn 'CREATE TABLE
   "sessions"' terraform/migrations/` → one hit, the dropped original). `public.sessions` does not exist.
4. **`jobsimulation.md:101-106` — the mirror-drop migration.** Line-numbered
   `app/terraform/migrations/20260729133514.sql` (64 lines): `:15-23` = the two remap `UPDATE`s, `:36-44` =
   the two orphan-NULL `UPDATE`s, `:58` = `-- 5. Drop the mirrors.`, `:62-63` = the two `DROP TABLE`s.
   `grep -i insert` returns exactly **one** hit and it is inside the dropped trigger-function *name*
   (`on_insert_local_jobsimulation_sessions_update_memberships`) — so "0 INSERTs" holds.
   `internal/organization/intelligence.go:1700` is `query := m.ent.JobSimulationSession.Query().`.
5. **`jobsimulation.md:141-150` — the cobra startup contract.** `cmd/` holds exactly four subcommand files;
   `Use:` values enumerated: `aggregate`, `clone-session`, `test-command`, `validate` (root is
   `Use: "jobimulation"` [sic]). `Dockerfile`/`Dockerfile.dev` both `EXPOSE 8080` + `ENTRYPOINT
   ["./application"]` with no `CMD`; compose passes no `command:`. `cmd/root.go:77-78` fall back to
   `8080`/`8081`.
6. **`jobsimulation.md:169-185` — the AWS-bind landmine.** `grep -n '\.aws' docker-compose.yml` returns
   exactly **one** line (`:142`) — the doc's "only AWS bind" is right. rext's removal is exactly as
   described: `stack-injection/gen_injected_override.py:589` emits `volumes: !reset null` (demo) and
   `stack-core/gen_override.py:154-155` + `to_yaml():231` emit `volumes: !override []` (dev), with the
   in-code comment independently stating the bare-`[]`-does-not-remove fact.
7. **`platform-migration-status.md:49-51` — "14 names in `repos.yml` history".** Ran the document's own
   command (`git log -p --follow -- repos.yml | grep '^+  - name: '`): **14 unique**, and the set is
   character-for-character the list printed in §2. All 14 have rows.
8. **`platform-migration-status.md` §2 — every `service_desired_count` anchor.** Opened all eight terraform
   files: `app:44 = 1`, `cms:39 = 0`, `jobsimulation:40 = 0`, `roadrunner:19 = 1`, `sentinel:19 = 1`,
   `storage:19 = 1`, `messenger:19 = 1`, `graphql-wundergraph:20 = 1`. Every line number and value correct,
   including the deliberately-recorded `roadrunner` contradiction.
9. **`platform-migration-status.md:60` — the four `app` wiring call sites.** `main.go:573` =
   `skiller.NewSkillerManager`, `:604` = `jobsimwiring.Wire`, `:634` = `skillpath.NewSessionManager`,
   `:1034` = `appcms.Wire`. Four distinct sites, each naming the domain claimed. `app/internal/roadrunner/`
   does not exist; `internal/jobsimwiring/wiring.go:118` is
   `jsrunner.NewRunnerManager(getenv("JUDGE0_API_KEY"), getenv("JUDGE0_BASE_URL"))`.
10. **`platform-migration-status.md:69` — the supergraph is one subgraph.**
    `graphql-wundergraph/supergraph-config-prod.yaml` lists `backend` alone; `schemas/` contains
    `backend.graphqls` alone; `subgraphs.conf` is the single line `BACKEND=v1.360.0`; fold commit
    `915da06` = *"feat(gql): fold cms subgraph into backend (supergraph 2→1)"*, 2026-07-29.
11. **`platform-migration-status.md` — every compose line anchor.** Enumerated all service starts in
    `docker-compose.yml`: `sentinel:5`, `backend:28`, `jobsimulation:83`, `cms:144`, `storage:189`,
    `customerio-sync:220`, `messenger:240`, `roadrunner:281`, `studio-desk:311`, `next-web-app:344`,
    `gotenberg:371` — all eleven match the doc. Also verified `:18` `search_path=sentinel`,
    `:220-222` git-URL context, `:238` profile, `:352`/`:362` the two `NEXT_PUBLIC_*` sites,
    `:371-372` `gotenberg/gotenberg:8`, `common.yml:2` postgresql, `common.yml:20` redis,
    and that `docker-compose.yml` really has **no** `graphql` service.
12. **`platform-migration-status.md` — every cited platform sha.** `236771f` (2026-07-29 cms/jobsim fold),
    `21429b7` (2026-07-07 skiller), `a4db680` (2026-07-21 skillpath M507), `045857c` (2026-04-17 chronos),
    `fdfa189` (2026-04-17 intelligence), `b56d731`+`360efd4`+`2adcf71` (2026-07-31 router drop),
    `a2a3ee6` (2026-02-27 — and `git show` confirms it deletes the `directus:` service),
    `8770fe6` (2023-05-04 nats), `84862d1` (2024-05-29 simulator→jobsimulations), `cb6ebf5`, `ef4b449`,
    `1474b1f`, `b43b99a`, `c17cc9a`, `467965a`. All fifteen resolve with matching dates.
13. **`platform-migration-status.md` §4 — the fence actually runs green.** Executed the documented command
    (`PLATFORM_REPOS_YML=stack-demo/platform/repos.yml python3
    .agentspace/rosetta-extensions/stack-core/platform_alignment_guard.py`) → exit 0, *"OK — … agree in
    both directions."* §1 declares exactly **seven** states and assertion C says seven. Consistent.
14. **`skillpath.md:30-33` — "there is NO `SkillPathSessionService` anywhere".** `grep -rn
    SkillPathSessionService --include='*.go'` across `app`, `cms`, `jobsimulation`, `sentinel`, `storage`,
    `messenger` → **0** in every repo (positive control: `JobSimulationService` returns
    `app/cms_reader_switch.go`, `app/main.go`, `app/internal/jobsimulation/rpcsrv/rpcsrv.go`). The Trap-C
    counter-anchors are also exact: `app/CLAUDE.md:72` and `app/knowledge/architecture.md:28` both still
    list the service.
15. **`skillpath.md:39-42` — the eight folded GraphQL operations.** Opened
    `app/internal/web/backend/graphql/graph/schemas/skillpath_sessions.graphqls`: `:71`
    `getOrCreateSkillPathSession`, `:72` `skillPathActiveSessions`, `:73` `skillPathCompletedSessions`,
    `:77` `completeSkillPathStep`, `:78` `uncompleteSkillPathStep`, `:79-80` `createSkillPathSession`
    **with** `@deprecated(reason: "Use upgradeSkillPathSessionToLatest instead")`, `:81`
    `upgradeSkillPathSessionToLatest`, `:82` `upgradeAllSkillPathSessionsToLatest`. Exactly eight,
    exactly as listed.
16. **`skillpath.md:72-99` — the manager read path and the "Coming soon" page.**
    `intelligence.go:1144` is `func (m IntelligenceManager) InsightsSkillPathByMemberships(...)`;
    `:1159-1170` is `m.ent.SkillPathSession.Query()` filtered on `SkillPathID`, `StatusIn(Active,
    Completed)` and the tenant predicate. No `local_skill_path_session.go` Ent schema exists.
    `next-web-app` `.../skill-paths/[skillPathId]/[userId]` →
    `InsightsBySkillPathStudentSimulationsContainer.tsx`: `userData` is `return null as unknown as
    MembershipEnriched` (`:31-34`), the results table and totals block are commented out (`:140-160`), and
    the body renders `t('enterprise.insights.comingSoon')`, whose EN value is literally **"Coming soon"**.
    `apps/hiring` has no `skill-paths` directory.
17. **`studio-desk.md:21` — the compose block.** `docker-compose.yml:334` is
    `VITE_GRAPHQL_ENDPOINT=http://localhost:8082/graphql/query`; `:337-341` is the `depends_on` naming
    `backend` and `cms` and nothing else; ports `9000:9000`/`9100:9100` at `:322-323`.
18. **`studio-desk.md:34` — the Vite entry set.** `vite.config.ts:28-41` `rollupOptions.input` declares
    exactly the nine prod entries the doc names (`main: app/home.html` + the eight others), with
    `dev-accept` spread in only when `!isProduction`. Nine of nine.
19. **`studio-desk.md:107` — the skill-path route facts.** `src/routes/` holds exactly four files;
    `skillpath.ts` is **61080 bytes** (the largest, next is `ai.ts` at 5895). `youtube.ts:43` is
    `const apiKey = process.env.YOUTUBE_API_KEY;` with the `_mock: true` fallback three lines below.
    `GCLOUD_SERVICE_ACCOUNT` is at `.env.example:120` and `terraform/main.tf:129`, and
    `grep -rn GCLOUD src/ app/` returns **zero** — "read by no code in `src/`" holds.
    `pingSnapshotCapability` (`skillpath.ts:709`) hits `/permissions/me/directus_versions/create`;
    `FORCE_READ_ONLY` at `:88`; `BUNNY_LIBRARY_ID`/`BUNNY_LIBRARY_API_KEY` at `:1356-1357`.
20. **`studio-desk.md:305` — `STUDIO_ACCESS_ROLES`.** `src/index.ts:96` is verbatim
    `['admin', 'org:admin', 'content_creator', 'org:content_creator']`, used at `:115`, redirecting to
    `process.env.WEB_APP_URL` at `:119-121`. `adminMiddleware` (`:133`) is attached to `/api/ai` (`:158`),
    `/api/skillpath` (`:161`), `/api/youtube` (`:164`) and every builder/catalog/skills page (`:179-228`).
21. **`studio-desk.md:112` — the AI tier config.** `src/services/ai/config.ts:182` is
    `defaultTier: (env.AI_DEFAULT_TIER as ModelTier) || 'thinking_fast'`; `.env.example:61` is
    `AI_DEFAULT_TIER=fast`; the four tiers are `ModelTier` in `ai/types.ts:21`; tier model maps at
    `config.ts:20-38` match the doc's OpenAI/Azure and Anthropic values.
22. **`studio-desk.md:313-320` — "three places" hardcoding the prod host.** `grep -rn app.anthropos.work
    app/core/scaffold/` returns exactly three: `pageWrapper.js:149` (logo `<a>`), `userProfile.js:148`
    (the "Back" menu button), `userProfile.js:302` (logout). Three of three.
23. **`studio-desk.md:387-393` — the M253 boot ordering.** `app/core/main.ts`: `preloadCriticalCSS()` at
    **L97**, `Sentry.init` L104, `posthog.init` L117, `await l12nService.init()` L191,
    `await userService.canAccess()` L199, `new PageWrapper()` at **L206**. Both cited line numbers exact,
    ordering exact.
24. **`studio-desk.md:346-355` — the M252 `env_file` fix.** `stack-injection/gen_injected_override.py:342`
    is `def frontend_lines(...)`, `:360-406` implement the existence-guarded absolute
    `<platform_dir>/../studio-desk/.env`; both named regression tests exist
    (`stack-injection/tests/test_injection.py:1594`, `:1651`);
    `aiService.getCompletion` (`:108-128`) does loop every configured provider inside one request.
25. **`frontend_architecture.md:11` — "`repos.yml` @ `2adcf71` holds exactly 9 entries".** Enumerated:
    app, cms, jobsimulation, sentinel, storage, messenger, roadrunner, next-web-app, studio-desk = **9**,
    and `ant-academy` is not among them. `docker-compose.yml:311` is the studio-desk service.
26. **`frontend_architecture.md:19-35` — apps, ports, locales.** Package names + dev ports read from each
    `apps/*/package.json`: `@anthropos/web-app` 3000, `@anthropos/hiring-app` 3001,
    `@anthropos/integration` 3002, `@anthropos/mobile` `expo start --port 3031`,
    `@anthropos/maintenance-app` (no dev script). `pnpm-workspace.yaml` really does carry `!apps/mobile`.
    `configs/i18n/messages/` holds exactly the **8** locales listed (de, en, es, fr, it, ja, nl, pt).
27. **`frontend_architecture.md:39, 48-51` — the toolchain scalars.** `apps/web/package.json:46` is
    `"next": "^16.2.7"` (identical in hiring `:45`, integration `:28`, maintenance `:9`);
    root `engines.node` = `>=24.0.0`; `packageManager` = `pnpm@10.30.3`; `turbo ^2.9.6`;
    `typescript ^5.9.3`; `UPGRADE-IMPACT-next16.md` present; `clean:quick`, `codegen`, `codegen:watch`,
    `check`, `check:lint`, `check:types`, `check:deprecations` all present in root scripts.
    `docker-compose.yml:362` is `NEXT_PUBLIC_BACKEND_API_URL=http://${PUBLIC_HOST:-localhost}:8082`.
28. **`frontend_architecture.md:39` — "`:5050` was only ever the deleted LOCAL compose host mapping".**
    `git log -p --follow -- docker-compose.yml | grep 5050` shows the deleted `- "5050:8080"` host mapping
    and the deleted `VITE_GRAPHQL_ENDPOINT` default; the router's own `config.prod.yaml:5` is
    `listen_addr: 0.0.0.0:8080` with `graphql_path: /graphql` at `:13`. Correct in both halves.
29. **`frontend_architecture.md:44` — the codegen contract.** `packages/graphql/codegen.ts`:
    `schema: 'http://localhost:8082/graphql/query'` (with the in-file comment `// was
    'http://localhost:5050/graphql'`), `documents: ['src/query/**/*.{ts,tsx}']`, output
    `./src/__generated__/`, `preset: 'client'`. Also: **no** connect/gRPC dependency anywhere in the
    workspace manifests, and `packages/graphql/src/hooks/useGraphql.tsx:20` injects
    `Authorization: Bearer ${token}` over `graphql-request`.
30. **`ant-academy.md:63-75, 117-169, 171-192` — the whole DB-authoritative read chain.**
    `app/(authed)/page.jsx:80` `resolveCatalogView()`; `serverTenant.js:143-146`
    `const view = (await getBackendCatalogView(eids)) ?? emptyCatalogView()`;
    `backendContent.js:54-69` `makeClient()` returning `null` on a throwing
    `createServerGraphQLClient` (`graphql/server.js:16-18`, keyed on
    `NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT`); `getBackendCatalogView` `catch → return null` at `:105-110`;
    `academyCatalogSeries` + `academyCatalogSkillPaths` are the real query names
    (`graphql/schema.graphql:2190`, `:2192`). The `serverTenant.js:115-145` citation is exact — `:129`
    *"NO FS-as-published fallback"* and `:132` *"the cutover is intentional, not reversible-on-error."*
    Body half: `serverChapterBody.js:51-67` (`getBackendChapterBody` → `maybeResolveDraftBody` →
    `{ notFound: true }`), and `app/not-found.jsx:43` really reads **"You wandered off the trail."**
    All four rext patch dirs + all three `apply-academy-*` shell helpers exist, and
    `stack-verify/live/autoverify.sh:459-521` is the M245 assert *(f)*.
31. **`ant-academy.md:219-233` — the `?lang=` verdict.** `src/i18n/locale.js` declares locale as a query
    param only (in-file: *"Locale lives in the URL (?lang=<code>) only"*), `coerceLocale` falls back to
    `en`, and there is no `[locale]` route dir. `LocaleSwitch.jsx` is a single `<Link>` toggling
    `locale === 'it' ? 'en' : 'it'` — a 2-way toggle, not a dropdown.
32. **`ant-academy.md:26, 293-304, 421-427` — the "not in `repos.yml`" story.** `repos.yml` enumerated
    above: no `ant-academy`. `demo-stack/ensure-clones.sh:150-168` is the explicit phase-(d2) clone with
    the non-fatal failure branch, exactly as described.
33. **`ant-academy.md:63` — the WRITE half.** `code/app/api/academy/beacon/route.js` exists;
    `app/cmd/academy-seed` exists; the plural Ent tables `academy_chapter_progresses`
    (`20260603182114_add_academy_progress.sql`) and `academy_last_activities`
    (`20260615090000_…`) exist; `upsertChapterProgress` (`academy.graphqls:694`),
    `upsertChapterProgressBatch` (`:696`) and `setLastActivity` (`:700`) all exist.
34. **`ant-academy.md:239-248, 408` — the tech-stack row.** `next.config.js:39` `reactCompiler: true`,
    `:21` `trailingSlash: true`, the `_meta`→`meta` rewrite at `:131-137`; `react ^19.2.5`;
    `code/app/layout.jsx:132` `manifest: "/academy-manifest.json"` and the manifest's
    `display: "standalone"`; `mobile/package.json:22` `"expo": "~54.0.33"`; FA Pro vendored under
    `code/public/assets/fontawesome/{css,webfonts}` with `.env.example:97` `FONTAWESOME_NPM_AUTH_TOKEN`
    surviving as vestigial; `RegisterServiceWorker.jsx` present as the kill-switch. Cosmo:
    `ucourses/ucourse-engine/assistant/agent.js:12-13` is
    `API_URL = 'https://api.openai.com/v1/responses'` + `MODEL = 'gpt-5.2'`, key from
    `localStorage.getItem('openai_api_key')` (`:32`); the separate server route
    `app/api/ai/chat/route.js` is a different endpoint, as the doc says.
35. **`ant-academy.md:398-403` — deployment.** `.github/workflows/` contains `deploy-academy.yaml`,
    `sync-vercel-env.yml`, `sidebar-coverage-tests.yaml`; `cog.toml` present;
    `code/vercel.json` is exactly `{"framework": "nextjs"}`.
36. **`jobsimulation.md:126, 129` — the two AI anchors.**
    `app/internal/cms/directus/collections/jobsimulation.go:1594-1597` is `voiceEngineFromDirectus`
    returning `SimulationVoiceEngineGptrealtime` on nil (byte-identical in the `cms` repo).
    `calls/livekit.go:131-135` is the `flag_use_realtime_openai` `IsFeatureEnabled` call and `:140-144`
    is the branch that sets `agentEndpoint = "openai-hosted"` **and** resets `agentName =
    "anthropos-agent"`, overriding the `anthropos-agent-%s` US name assigned at `:126`. Exact.
37. **`jobsimulation.md:88-90, 115, 137` — the structural-change bullets.**
    `hibiken/asynq v0.26.0` in `go.mod:24`; worker pools `Concurrency: 10` (`worker.go:27`) and
    `Concurrency: 25` (`:104`); commit `09631fb2` is verbatim the chronos-removal commit; the two
    interview-extraction migrations exist by exact filename; platform `05b4035` is
    *"chore(compose): add READONLY_DB_CONNECTION to jobsimulation service"*.
    `cmd/root.go:284-285` adds the own-stream and `cms` subscribers via `pubsub.NewSubscriberServer`
    (`:121`). rext really does set `DIRECTUS_DATA_CONSUMERS = ("cms", "backend")` in **both** twins
    (`stack-injection/gen_injected_override.py:53`, `stack-core/gen_override.py:58`), and
    `app/main.go:971-973` really does `log.Fatalf` without `DIRECTUS_BASE_ADDR`.
    `jobsimulation/internal/runner/runner.go:1-3` carries the *"formerly the standalone 'roadrunner'
    service"* header, and no Go file in `jobsimulation` reads `ROADRUNNER_RPC_ADDR`.
    `pt-aisim-chat-launch` is a real declared Playthrough (`playthroughs/manifest/ai-simulations.yaml:49`).

**RE-DERIVED** (weaker — recorded as such per §5 rule 24):

- `platform-migration-status.md:51-53` — "26 names" from `git log -p --follow -- docker-compose.yml`.
  I re-ran the document's own command shape and got 26, but **the 26 includes `app-network`, which is a
  network, not a service** — so the service cardinality is 25 and the arithmetic only matches because the
  grep is loose. All five names the sentence calls out (`nats`, `web-app`, `chromedp`, `simulator`,
  `realtime`) are genuinely in the set, and every name in the set has a row, so the *claim* survives; the
  *derivation* does not reproduce cleanly. Flagging rather than booking, because the sentence says
  "every service that has ever appeared", and by that predicate the number is 25.

---

## What I could not settle

- **`platform-migration-status.md:99-101`** — "93 repositories in `anthropos-work` … 9 in `repos.yml` …
  46 named by no corpus document". The `9` I enumerated and it is right. The `93` and the `46` need the
  GitHub org API; `gh` is not installed on this host and no network probe was available. **Settled by:**
  `gh repo list anthropos-work --limit 200 --json name` plus a scripted mention-index over `corpus/**`.
- **Every "Repo ARCHIVED on GitHub <date>" claim** (`jobsimulation.md:13`;
  `platform-migration-status.md:62`, `:69`, `:70`, `:71`, `:72`, `:73`) — archive state is not visible from
  a clone. **Settled by:** `gh repo view <org>/<repo> --json isArchived,archivedAt`.
- **`platform-migration-status.md:65`, `:170`** — "`app` PR #1103 (v9.0 'support-in-app') folds storage +
  messenger". Not visible from the clones. **Settled by:** `gh pr view 1103 --repo anthropos-work/app`.
- **`jobsimulation.md:115`** — "measured live on `demo-1` at M257x iter-24 as 96 Directus log lines, all
  403". A historical live measurement; no running stack to reproduce it against.
- **`ant-academy.md:74`, `:192`, `:216-217`, `:232`** — the "65 real cards", the HTTP 404→200 chapter-body
  proof, and the `?lang=it` → 200 proof, all stated as measured live on `billion`. Reproducible only by
  bringing a demo up.
- **`ant-academy.md:24`, `:240`, `:407`** — "`@anthropos.work` domain restriction is enforced in the Clerk
  app". Enforced in Clerk dashboard config, not in the repo; nothing in `proxy.js` implements it (and the
  doc says so). **Settled by:** the Clerk dashboard's allow-list for the shared app.
- **`studio-desk.md:412`** — "skeleton-visible p95 4669 ms → 817 ms (5/5 cold loads)" and
  **`:423`** "canAccess leg 4049 → 38 ms, FCP 6936 → 2152 ms on billion". Measured numbers from a live
  tailnet VM; not reproducible here.
