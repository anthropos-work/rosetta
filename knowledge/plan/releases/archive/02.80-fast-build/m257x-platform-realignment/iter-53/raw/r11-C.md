# Seat C — reading #11

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| corpus/architecture/alignment_testing.md | 521 | yes |
| corpus/architecture/architecture_overview.md | 353 | yes |
| corpus/services/cms.md | 254 | yes |
| corpus/services/sentinel.md | 166 | yes |
| corpus/services/clerk-integration.md | 128 | yes |
| corpus/services/README.md | 79 | yes |
| corpus/services/db-backup.md | 31 | yes |

Protocol read first: `corpus/ops/platform-alignment.md` (1219 lines), §5 rules 1–24 in full.

Ground-truth clones opened (shas confirmed): `stack-demo/app` `5ba17044`, `stack-demo/app/studio`
`aeec036a`, `stack-demo/platform` `2adcf714`, `stack-demo/sentinel` `88bc5592`, `stack-demo/cms`
`ca50c817`, `stack-demo/graphql-wundergraph` `60c229f3`, `stack-demo/roadrunner` `87d8d443`,
`stack-demo/jobsimulation` `462343b0`, `stack-demo/next-web-app` `bb3313bc`,
`stack-demo/messenger` `fa47850d`, `stack-demo/studio-desk` `14a5442a`, `stack-demo/ant-academy`
`9c3843cd`, `.agentspace/rosetta-extensions` @ `f93ff73` (main).

---

## Blockers

### B1 — `alignment_testing.md` under-counts the snapshot-fidelity operator set: FIVE is now SIX, and the content surface carries five, not four

- **Anchor:** `corpus/architecture/alignment_testing.md:460` —
  *"a **snapshot-fidelity gene class** (`dna/snapshot.go`) — five two-sided operators over a `FidelityProbe`…"*
  and `:477-479` — *"…carries all five fidelity operators. **M10** promotes the **content** surface … carrying
  four operators (no `embedding-dim` — content has no vectors)…"*
- **Why it is false:** I enumerated the operator constants from source, not from the prose.
  `rosetta-extensions/stack-seeding/dna/snapshot.go:36,39,42,45,48,56` declares **six**
  snapshot-fidelity operators — `snapshot-row-count`, `snapshot-structural`, `snapshot-referential`,
  `snapshot-embedding-dim`, `snapshot-public-only`, and **`OpSnapshotCrossSurfaceClosure =
  "snapshot-cross-surface-closure"`** (added at M23), and all six are members of
  `validSnapshotOperators` (`snapshot.go:63-68`) and all six are dispatched in the measure switch
  (`snapshot.go:162-172`). The doc's enumeration at `:460-467` never names the sixth.
  Worse, the **content surface's actual operator array** in
  `rosetta-extensions/stack-seeding/dna/data-dna.json` is **five** entries —
  `["snapshot-row-count","snapshot-structural","snapshot-referential","snapshot-public-only",
  "snapshot-cross-surface-closure"]` — not the four the doc states. (The surface's own free-text
  `note` field says "four"; the machine-readable `operators` array, which is what `Validate`/`measure`
  read, says five. The corpus copied the stale note.)
  This is not cosmetic: the missing operator is the one whose own code comment says it catches
  *"the exact gap that produces the empty Assign-AI-Simulation picker (a public sim referencing a
  node-id the captured public taxonomy lacks)"* (`snapshot.go:49-55`). A reader authoring or auditing
  a snapshot DNA from this page would omit the gene that exists to catch that class.
  Method note: I re-derived the SET from source (rule 24), not the sum from the doc's own list — the
  doc's arithmetic (5 = the five it names) is internally consistent and checks out; the SET is wrong.
- **Confidence:** high

### B2 — `cms.md` names `python-docx` as a studio-room dependency; it is not in `requirements.txt` at the pinned sha

- **Anchor:** `corpus/services/cms.md:110` — *"`requirements.txt` openai, anthropic, mistralai, rich,
  pyyaml, **python-docx**, requests, jinja2, pytest, pytest-asyncio (see studio/requirements.txt)"*
- **Why it is false:** `stack-demo/app/studio/requirements.txt` @ `aeec036a` (and the identical
  `stack-demo/cms/studio/requirements.txt`, same sha) contains exactly **nine** entries:
  `openai, anthropic, rich, pyyaml, requests, jinja2, mistralai, pytest, pytest-asyncio`.
  `python-docx` is absent. It is not a mis-typed name either: `grep -rn 'docx'` over the whole
  studio tree returns exactly one hit — `agents/simulation/guidelines.py:48`, a *prompt string*
  forbidding `.docx` output. The claim is **stale, not invented**: `git show
  d436ee0:requirements.txt` in `app/studio` does list `python-docx`, so it was removed upstream and
  the corpus was never re-derived. The doc points the reader at the file (`see studio/requirements.txt`),
  which makes the divergence load-bearing — a developer running `pip install -r requirements.txt`
  and then wondering why `docx` is missing will chase a phantom.
- **Confidence:** high

### B3 — `cms.md`'s internal back-reference `:37` points at the GraphQL-subgraph bullet, not the Studio bullet it claims to cite

- **Anchor:** `corpus/services/cms.md:179-181` — *"Since cms-in-app v8.0 the studio-room pipeline is
  pulled into the **`app`** image by CI via the `additional_repo` mechanism (app v1.360.1) — **see :37
  in the banner at the top of this doc**."*
- **Why it is false:** `cms.md:37` reads *"`schemas/jobsimulation.graphqls`, taking the supergraph from
  **3 subgraphs to 1** — not 2 to 1. (The"* — the GraphQL bullet. The bullet that actually states the
  studio-room / `additional_repo` / v1.360.1 fact is the **Studio** bullet at `cms.md:46-47`
  (*"the Python `anthropos-studio-room` project is now pulled into the **`app`** image via the CI /
  `additional_repo` mechanism (app v1.360.1)"*). An anchor that resolves to a real line but names the
  wrong construct is rule-21's second class; the neighbouring back-reference in the same doc
  (`:170-171`, *"Consistent with :27 above"*) does resolve correctly, so the reader has no cue that
  this one does not.
  (The substance is true: `stack-demo/app/.github/workflows/build-production.yml:28-30` carries
  `additional_repo: "anthropos-studio-room:studio"`, and `app/CHANGELOG.md` v1.360.1 is
  *"(ci) pull studio via additional_repo like cms"*.)
- **Confidence:** high

### B4 — `alignment_testing.md` cites `gate.sh:61` for the coverage call; the call is at `gate.sh:69` and `:61` is a comment

- **Anchor:** `corpus/architecture/alignment_testing.md:193` — *"⚠ **Not the same as the bare command —
  do not conflate them.** **`gate.sh:61`** calls `alignctl dna coverage --dna … --if-declared`."*
- **Why it is false:** In `rosetta-extensions/clerkenstein/alignment/scripts/gate.sh` @ `main`, the
  invocation `"$alignctl" dna coverage --dna "$base/$dna" --if-declared` is at **line 69**
  (`grep -n 'coverage' gate.sh` → `58`, `68`, `69`). Line **61** is prose inside the explanatory
  comment block: `# over a DNA that is missing a consumed endpoint is exactly the thing that let
  Clerkenstein report 100% while`.
  The anchor was correct once and drifted: `git show dd65ad0:clerkenstein/alignment/scripts/gate.sh`
  has the call at `:61`; `git show 9974faa:…` (the M219 commit that added the alignctl build-to-binary
  block above it) moved it to `:69`. The doc was not re-derived.
  I hesitated between MINOR and BLOCKER because the *claim* is true and the file is right — but the
  briefing books "wrong line anchors that point at the wrong construct" as a BLOCKER, and this one
  lands on a comment sentence that reads like prose about a different thing, which is exactly the
  failure mode rule 21 class 2 describes.
- **Confidence:** high (on the fact); medium (on severity)

### B5 — `services/README.md` says "three of the four" and then names a set that is not a subset of its own four

- **Anchor:** `corpus/services/README.md:20-21` — *"And **three of the four** (cms, jobsimulation,
  roadrunner) still start CONTAINERS locally in the default `graphql` profile as unfederated husks"*
- **Why it is false:** Six lines earlier the same blockquote defines the four: `README.md:11-12` —
  *"**Four** services in this index — skiller, skillpath, jobsimulation and cms — are **folded into
  `app`**"* — and `:15` explicitly excludes roadrunner from them: *"**`roadrunner` is the fifth, and it
  is different: orphaned, not merged-and-undeployed.**" So `roadrunner ∉ {skiller, skillpath,
  jobsimulation, cms}`, and "three of the four" naming `(cms, jobsimulation, roadrunner)` is
  self-contradictory as written; the correct denominator is five.
  The *facts* are right and I verified them independently in `stack-demo/platform/docker-compose.yml`
  @ `2adcf714`: `cms` (`:144`), `jobsimulation` (`:83`) and `roadrunner` (`:281`) each declare
  `profiles: [graphql, …]`; `skiller` and `skillpath` have no compose block at all. The twin passage
  in my file set states it correctly — `architecture_overview.md:180-182`: *"CMS, Jobsimulation and
  Roadrunner appear in **both**… Only Chronos, Intelligence, Skiller and Skillpath are genuinely out
  of local orchestration."* So this is a corpus-internal contradiction with a settled correct form
  a few files away.
  I hesitated (the parenthetical names the right three) but booked it: `services/README.md` is the
  enumerated index every reader is told to start from, and a reader counting merged services off it
  gets an answer that cannot be reconciled with the same blockquote.
- **Confidence:** medium

---

## Minors

- `corpus/services/cms.md:39-40` — *"Public (unauthenticated) library content queries are preserved —
  see app **v1.360.2**/v1.360.3."* `app/CHANGELOG.md` v1.360.2 is *"(docker) restore wget in runtime
  image — unblock prod health checks"*, unrelated to library queries; only v1.360.3 (*"(cms) restore
  public (unauthenticated) access to library content queries"*) supports the sentence.
- `corpus/services/sentinel.md:31` describes matcher `m6` as *"Org-membership check via `g3(org,
  user)`"* while `:41` correctly says *"`g3(org, membership)`"*. `sentinel/internal/rpcsrv/rpc.go:302,
  315, 327` shows g3 tuples are `(organization_id, membership_id)`. Same file, two spellings.
- `corpus/services/sentinel.md:43-45` — *"only as a fixture string in sentinel's own tests
  (`internal/authorization/casbin_test.go`, `internal/rpcsrv/rpc_test.go`)"*. `grep -rn '"manager"'`
  also hits `internal/authorization/manager_test.go:21` (`manager := "manager"`). The claim holds; the
  parenthetical list is incomplete.
- `corpus/architecture/alignment_testing.md:513` — the Layout block describes `cmd/alignctl` as
  `run | capture | dna list|diff|validate`, omitting `coverage`, which the same doc documents at
  `:245` and which really exists (`alignment/cmd/alignctl/dna.go:26`). The same block omits
  `internal/canon`, which is present in `alignment/internal/`. (The tool's own top-level usage string
  is stale in the same way — that is a rext defect, not a corpus one.)
- `corpus/architecture/alignment_testing.md:3` — *"**Last updated:** 2026-06-06"* on a page whose body
  documents M218/M219 (v2.3) and the M23 cross-surface-closure operator.
- `corpus/services/cms.md:8` calls cms *"the fourth and last engine consolidated into `app`"* while
  `architecture_overview.md:62-64` says *"**Five** former microservices now run **inside**
  Backend/App: skiller, skillpath, **roadrunner**, jobsimulation and cms."* Defensible as a
  proxy-vs-engine distinction (and consistent with `services/README.md`'s four-plus-a-fifth framing),
  so booked as a minor rather than a contradiction.

---

## Positively cleared (audited zeros)

**ENUMERATED** — predicate enumerated from source independently, cardinality stated before arithmetic:

1. **`architecture_overview.md:299-307` — the multi-tenancy fence (`31 of 135`; `23` unpoliced; `16`
   neither-mixin).** This is the claim rules 17/22/24 say has been wrong four times, so I enumerated
   every clause of the conjunction.
   - Denominator: `grep -l 'ent.Schema' internal/data/ent/schema/*.go | wc -l` → **135** (139 `.go`
     files; the 4 non-schemas are `database_types.go`, `mixin.go`, `skillpath_mixins.go`,
     `skiller_mixins.go`). ✔
   - `OrganizationMixin{}` occurrences → 30 files; `user_resource.go:22` is
     `// OrganizationMixin{},  // We need to work on this` → **29 live**. ✔
   - Files declaring their own `) Policy() ent.Policy` → exactly **four**: `mixin.go`,
     `org_membership.go`, `organization.go`, `user.go`. `Organization` (`organization.go:56`,
     Query = `DenyIfNoOrganizationInContext` + `FilterSameOrganizations`) and `Membership`
     (`org_membership.go:172`, `AllowCurrentOrgEdgesOrSkipRule` … `AlwaysDenyRule`) filter by
     **organization**; `User` (`user.go:116`, `FilterSameUserRule`) filters by **user** and is
     correctly excluded. → **29 + 2 = 31**. ✔
   - Unpoliced set: **my first pass got this wrong and I caught it before booking.** A
     `field.UUID("organization_id"` regex returns 43 schemas and misses
     `job_role_skill_suggestion_cache.go:37`, which declares `field.String("organization_id")`.
     With `field\.(UUID|String)\(` the org_id-bearing set is **51** (44 direct incl. the mixin file,
     minus `skiller_mixins.go`, plus the 7 `OrganizationIDMixin{}` users:
     `category`, `jobrole`, `similarity`, `skill`, `specialization`, `studio_document`,
     `studio_task`). Four policed schemas carry no org column (`org_tag`, `org_membership_tag`,
     `org_membership_skill`, `organization`), so org-policed ∩ has-org-id = 27, leaving
     **24** carrying `organization_id` with no *organization* policy.
   - **The conjunct the rule warns about:** of those 24, exactly one — `academy_feedback.go:64` —
     carries `UserMixin{}`, whose `Policy()` (`mixin.go:99`, `rule.FilterOwnerRule()`) is a real
     row-level policy. So *"no policy at all"* = **24 − 1 = 23**. ✔ And the neither-mixin subset of
     those 23 = 23 − 7 = **16**. ✔
   - **Verdict: the corpus figure is correct, and so is its stated derivation.** Every clause of the
     conjunction was tested separately.
2. **`alignment_testing.md:254-258` — the five-surface score table.** Enumerated from the DNA JSON:
   `clerk-2.6.0` 14 capabilities / 27 genes / declares `consumed_surface`; `clerk-js-5` 6/9;
   `clerk-multi-1` 5/9; `clerk-deploy-1` 3/7; `clerk-express-1` 5/13; and **only** `clerk-2.6.0`
   declares a `consumed_surface` (the other four → `no`), exactly as `:201-204` claims.
   Then **measured live**, not just counted: built `alignctl` + each runner and ran all five gates.
   `clerk-2.6.0` → `overall 100.0% critical 100.0% (27/27)`, no divergences, rc 0;
   `clerk-js-5` → 9/9 100/100; `clerk-multi-1` → 9/9 100/100; `clerk-deploy-1` → 7/7 100/100;
   `clerk-express-1` → the `UNMEASURABLE — the runner could not execute. THIS IS NOT A PASSING SCORE.`
   banner (node/@clerk/express absent), never a score. Every number and every claim about the
   express surface reproduced.
3. **`alignment_testing.md:294-306` — the toy worked example.** Ran it verbatim; stdout is
   byte-for-byte what the doc prints, including `Score: overall 86.7%   critical 100.0%   (5/6 genes
   aligned)` and `value differs: source="Hello, El Nino!" mirror="Hello,   El  Nino  !"`.
4. **`cms.md:35-40` + `:158` + `architecture_overview.md:117-118,243` — the subgraph ladder 5→4→3→1.**
   Enumerated `git ls-tree schemas/` at each commit in `graphql-wundergraph`:
   before `749dc86` = {backend, cms, jobsimulation, skiller, skillpath} (5); before `7c17e63` = 4;
   before `915da06` = {backend, cms, jobsimulation} (3); after `915da06` = {backend} (1).
   `git show --stat 915da06` deletes **both** `schemas/cms.graphqls` and
   `schemas/jobsimulation.graphqls`. The corpus's correction of the commit's own "2→1" subject line
   is itself correct.
5. **`architecture_overview.md:160,189` + `cms.md:26-28` — the moved-table counts.**
   `app/terraform/migrations/20260722081626_jobsim_data_model.sql` → exactly **23** `CREATE TABLE`
   (enumerated by name). `…/20260724132049_cms_data_model.sql` → exactly the **six** named tables
   (`similarities`, `studio_documents`, `studio_tasks`, `similarity_categories`,
   `similarity_features`, `similarity_skills`).
6. **`sentinel.md:26` — "6 request types, 6 policy types, 3 role groupings, 6 matchers".** Counted
   from `sentinel/internal/authorization/casbin.go` `ModelConf`: `r,r2..r6` = 6; `p,p2..p6` = 6;
   `g,g2,g3` = 3; `m,m2..m6` = 6. Per-matcher descriptions match the expressions.
7. **`sentinel.md:85` — `AUTHORIZATION_ADDRESS` in "exactly three compose blocks — `:45`, `:99`,
   `:160`".** `grep -n` returns exactly those three lines, and the surrounding blocks are `backend`
   (service at `:28`), `jobsimulation` (`:83`), `cms` (`:144`). No fourth. `messenger` really imports
   no authorization client (`grep -rn 'authorization' --include='*.go'` → 0, against a `colony`
   positive control that returns hits).
8. **`sentinel.md:40,129` — `init_policy.sql` anchors.** `:63-66` is exactly the four-line NOTE
   explaining the omission of `org:feature:taxonomy:write`; `:88-118` is exactly the
   `content_creator` block, first line to last row. `grep 'manager'` → 0 hits (positive control:
   `grep -c admin` → 46). `MembershipRole` const block covers `app/internal/data/ent/enum/
   membership.go:8-15`, four values.
9. **`clerk-integration.md:37` — "Webhooks (svix, 12 event types)".** Enumerated the switch in
   `app/internal/clerk/events/events.go:120-195`: `user.{created,updated,deleted}` +
   `organization.{created,deleted,updated}` + `organizationInvitation.{accepted,created,revoked}` +
   `organizationMembership.{created,deleted,updated}` = **12**.
10. **`clerk-integration.md:103` — the SDK-version block.** `app/go.mod:31` is exactly
    `github.com/clerk/clerk-sdk-go/v2 v2.7.0`; `@clerk/nextjs ^6.39.2` in exactly four package.json
    (`apps/web:10`, `apps/hiring:10`, `apps/integration:9`, `ant-academy/code/package.json:52`);
    `next-web-app/apps/mobile/package.json:6` = `~2.6.18`, `ant-academy/mobile/package.json:18` =
    `~2.19.36` (13 minors apart). Every line number in the sentence resolves.
11. **`architecture_overview.md:60` — "a bare `make up` gives you six Go services".** Enumerated the
    `graphql` profile from compose: `sentinel` (no `profiles:` → always), `backend`, `jobsimulation`,
    `cms`, `storage`, `roadrunner` = **6** first-party Go services, plus `gotenberg` (third-party
    image), which the sentence itself excludes.
12. **`services/README.md:15-18` — the terraform desired-count row.** `roadrunner/terraform/main.tf:19`
    `service_desired_count = 1`; `cms/terraform/main.tf:39` `= 0`; `jobsimulation/terraform/main.tf:40`
    `= 0`. All three anchors land on the exact assignment.
13. **`services/README.md` — "all 27 service docs".** `ls corpus/services/*.md` → 29, minus
    `README.md` + `TEMPLATE.md` = **27**, and the four index tables sum 10 + 5 + 8 + 4 = 27 with no
    doc unlisted and no listed doc missing.
14. **`alignment_testing.md:320-322,489-507,511-519` — where things live.** `alignment/` contains
    exactly `README.md cmd examples go.mod internal` — **no `scripts/`**, as claimed; module line is
    `module anthropos.dev/alignment`. `clerkenstein/alignment/scripts/` holds `gate.sh`,
    `drift-check.sh` (+`drift-test.sh`); `gate.sh:33,35,30` default to `./cmd/clerkrun`,
    `dna/clerk-2.6.0.json`, `ALIGN_DIR=$base/../../alignment`; `GATE_OVERALL=95`/`GATE_CRITICAL=100`.
    Five DNAs in `clerkenstein/alignment/dna/`, five runners in `clerkenstein/alignment/cmd/`.
    Goldens really are `golden/<Capability>/<variant>.json`.
15. **`alignment_testing.md:327-343` — the inert CI workflow retraction.**
    `clerkenstein/.github/workflows/alignment.yml` exists, is git-tracked
    (`git ls-files` confirms), and lines **10-11** read *"NOTE: as a subdir workflow under
    clerkenstein/.github (not at the monorepo root), this is currently / inert in the
    rosetta-extensions monorepo; it is kept illustrative of the per-mirror gate shape."* — the exact
    quoted text at the exact cited lines. There is no `.github/workflows` at the monorepo root.
16. **`alignment_testing.md:160-167, 415, 429-430, 460-467, 476-484` — the harness contracts.**
    `GateMet` (`alignment/internal/compare/compare.go:56-60`) really refuses a non-zero critical
    threshold when `CriticalGenes == 0`; `dna.Validate` really errors *"no critical genes: a DNA must
    declare at least one `critical` capability"* (`internal/dna/dna.go:280`); the id pattern is
    literally `^[A-Za-z0-9][A-Za-z0-9_-]*$` (`dna.go:21`) and weight bound is `1..maxWeight`
    (`:270`); `Overall` is weighted / `Critical` is an unweighted count ratio
    (`compare.go:99-101`); `ExitRegressed=2` / `ExitUnmeasurable=3` (`cmd/alignctl/run.go:134-135`).
    The structural operators `type-match / constraint-satisfied / fk-valid / row-count` exist verbatim
    (`stack-seeding/dna/dna.go:78-81`), the `datadna` CLI is at `stack-seeding/cmd/datadna`, and
    `data-dna.json` has 10 surfaces with **nothing** `waived` (4 × `seeded-m7a`, 4 × `seeded-m7c`,
    `taxonomy: snapshot-seeded-m9b`, `content: snapshot-seeded-m10`) — the "100% coverage, nothing
    waived" claim holds.
17. **`architecture_overview.md:244-263` — the AI-routing paragraph.** Every `file:line` opened with
    `sed -n 'N-15,N+5p'`: `internal/jobsimulation/ai/ai.go` `getClient` at `:259` with the
    `azureClientEu` default and the `flag_use_azure_us` swap inside `:262-276`; `isThrottlingError`
    declared at `:129` and applied at `:166` and `:325`; `config.WithRegion("eu-west-1")` at `:87`
    inside `:85-88`. `internal/cms/studio/markdownManager.go:11` is the `ai/mistral` import and `:19`
    is `mistral.NewMistral(nil, os.Getenv("MISTRAL_API_KEY"))`; `studioManager.go:583` is the
    `NewMarkdownManager` call. All three `app/studio/configs/*.ini` pin `azure`. The cross-file
    anchors `external_services.md:545` (the ladder retraction) and `:569` (the four-live enumeration)
    both land on the right sentences, and the "fifth latent arm = Studio-Room's `openai` TARGET
    SERVICE" matches `external_services.md` item 5 — no contradiction between the two files.
18. **`cms.md:12-16,30-34,72,156-157,163-166` — the husk/RPC/webhook claims.**
    `docker-compose.yml:144` is the `cms:` service header; `:256` is messenger's
    `CMS_RPC_ADDR=http://cms:8091`; `repos.yml:14-16` is the cms entry with
    `migrations: false # legacy`. `app/main.go:1198-1199` carries *"Additive + DORMANT … until the
    M809 re-point"* (inside the cited `:1196-1202`). `app/internal/skillpath/session.go:205-207` is
    exactly the `// cms-in-app deseam: cms is in-process` comment + the
    `u.cms.GetSkillPathDomain` call. `app/cms_reader_switch.go` exists;
    `rosetta-extensions/stack-injection/gen_injected_override.py:53` really is
    `DIRECTUS_DATA_CONSUMERS = ("cms", "backend")`. The app-side webhook is
    `/api/webhook/directus` (`internal/web/backend/backend.go:138,324`) and really is gated on
    `DIRECTUS_WEBHOOK_SECRET` (`main.go:1080`), while the standalone one was
    `/webhooks/` on the HTTP server (`cms/cmd/root.go:242`) with no secret.
    `cms/go.mod:3` = `go 1.26.4`; `cms/Dockerfile:2` = `golang:1.26-bookworm`, `:23` =
    `python:3.11-slim`. All 11 `internal/cms/` subpackages named at `:24-25` exist, plus `wiring.go`.
19. **`cms.md:222-228` — the "no `--template` flag" callout.** `gen.py:484-492` is exactly nine
    `parser.add_argument` calls, in exactly the listed order; `parse_argument` is exactly
    `gen.py:18-28` and does call `parse_known_args()` then merge the leftovers into the dict. No
    `--template` argument is registered anywhere (only a legacy *blueprint field* at `:213-233`).
    `studio/CLAUDE.md:12-14` is the quoted `python gen.py --media simulation --prompt … --branch
    stable` fenced block.
20. **`architecture_overview.md:3,34-35,51,271-287` — the router's two states.** Platform `b56d731`
    ("drop the WunderGraph router; point local dev at backend") and `360efd4` ("remove the router
    service and clone entry outright") are real, both 2026-07-31, merged at `2adcf71`. There is no
    `graphql` service in `docker-compose.yml` and no `graphql-wundergraph` entry in `repos.yml`. The
    frontends' baked endpoint is `http://${PUBLIC_HOST:-localhost}:8082/graphql/query`, and
    studio-desk's is the same — so *"there is no `:5050` on a local stack"* holds (`:5050` survives
    only in the archived repo's own `Makefile:10`). Prod: `graphql-wundergraph/terraform/main.tf:20`
    is `service_desired_count = 1` and `terraform/locals.tf` `port = 8080`, matching the
    "Cosmo Router (port 8080)" in the prod request-flow block.
21. **`clerk-integration.md:21,33,46,99-101` — the frontend gates.**
    `apps/web/src/app/api/metabase/route.ts:35` really is `authData.orgRole !== 'admin'` (bare form);
    `getToken` is called with **no template** anywhere in `next-web-app/apps` or `ant-academy/code`
    (the single options call is `{skipCache:true}`); `studio-desk/src/index.ts:96` and
    `app/services/userService.ts:16` both define `STUDIO_ACCESS_ROLES = ['admin','org:admin',
    'content_creator','org:content_creator']`; `ant-academy/code/proxy.js` gates on
    `REQUIRE_ORGANIZATION_MEMBERSHIP` and redirects to `/no-organization` (`:91,131,318`);
    `ant-academy/mobile/src/components/ClerkSignInForm.tsx:48-51` really aborts on any non-`complete`
    status (*"Sign-in needs another step that this app does not support yet"*). `apps/web`'s Clerk
    provider imports exactly 8 locales.
22. **`architecture_overview.md:48` — `next: ^16.2.7` across all four apps.** Enumerated:
    `apps/web:46`, `apps/hiring:45`, `apps/integration:28`, `apps/maintenance:9` — four, all
    `^16.2.7`; `apps/mobile` is Expo and declares no `next`. React is `^19.2.7`.
23. **`sentinel.md:22,66-83` — cost + RPC surface.** `sentinel/terraform/locals.tf:4-5` is
    `service_cpu = 256` / `service_memory = 128`. All 19 methods in the doc's table exist as
    `RPCHandler` methods in `internal/rpcsrv/rpc.go`, with none missing and none extra.
    `go.mod:3` = `go 1.26.0`; `casbin/v3 v3.10.0`; `Makefile` `initdb` really hard-codes
    `postgresql://postgres@localhost:5432/postgres?sslmode=disable`; `local_superadmin_grants.sql`
    exists; `main.go` exists (so `go run main.go` is valid).
    `PolicyGrantsSeeder` (`stack-seeding/seeders/policy_grants.go`) and
    `stackseed --policy-check` (`cmd/stackseed/main.go:205`) both exist as cited.

**RE-DERIVED** (weakest clearance — recorded as such, not as a pass):

- `alignment_testing.md:97,153-157` — the `3/2/1` criticality→weight defaults and the
  `Σ(weight·aligned)/Σ(weight)` formula: I read `compare.go` and the arithmetic reproduces, but I did
  not enumerate every DNA's weight overrides.
- `services/README.md` group counts (10/5/8/4): re-added from the tables themselves *and*
  cross-checked against `ls`, so this one is closer to enumerated — but the tier assignment of each
  doc I took from the doc, not from compose.

---

## What I could not settle

- **`architecture_overview.md:13,204` — "≥42,790 skills / ≥22,470 job roles (measured public subset,
  2026-06-29)"** and the accompanying "60K/18K is refuted/unverified" claim. These are DB
  measurements; nothing in the clone set carries the taxonomy data. Settled only by a read-only
  count against the prod `public` schema (`/db-query`) or by the snapshot manifest's captured row
  counts.
- **"repo is ARCHIVED on GitHub"** — asserted for `graphql-wundergraph` at
  `architecture_overview.md:3` (dated 2026-07-30) and `services/README.md:43`, and for
  `jobsimulation` at `architecture_overview.md:15` (dated 2026-07-31). Local clones cannot show
  GitHub archive state. The nearest evidence I have is platform `360efd4`'s body — *"the
  graphql-wundergraph repo **is being** ARCHIVED AND DELETED"* — which is future tense on 2026-07-31
  and does not confirm either date. Settled by `gh api repos/anthropos-work/<name> --jq .archived`.
- **`corpus/services/db-backup.md` in its entirety** (31 lines) — every claim (Go, 6-hour schedule,
  S3 + Azure Blob + Hetzner targets, "PostgreSQL RDS (all schemas)", Docker-scheduled) is about a
  production-only service with **no clone in the ground-truth set** and no entry in
  `platform/repos.yml` or `docker-compose.yml`. The one thing I *could* check — that it is absent
  from local orchestration — is true. The rest is unfalsifiable from here; settled by the
  `db-backup` repo or the production terraform.
- **`cms.md:48-50` / `architecture_overview.md:189-191` — `module.cms_euwest1` and
  `module.jobsimulation_euwest1` "still declared in `infrastructure/terraform/production/
  services.tf` as the rollback path".** The `infrastructure` repo is not in the clone set; I could
  only verify each service's own `terraform/main.tf` desired-count (which I did — see cleared item
  12). Settled by opening `anthropos-work/infrastructure`.
- **`sentinel.md:45-46`** — *"a live stack's `select distinct v2 from sentinel.casbin_rules where
  p_type='g2'` returns `admin` / `member` / `candidate` only"*. No stack is running; the static
  evidence (`init_policy.sql` seeds those three plus `content_creator` in `p2`/`p3`/`p5` rows, and
  `g2` rows are written at runtime by `OrgAddUserToRole`) is consistent with the claim but does not
  establish it. Note the mild tension with `:40`, which says `g2` carries "the four `MembershipRole`
  values" — a live query would settle whether `content_creator` ever appears in `g2.v2`.
