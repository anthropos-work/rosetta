# Seat B — reading #12

Ground-truth shas confirmed at start of pass (all match the briefing):
`app 5ba17044` · `app/studio aeec036a` · `platform 2adcf714` · `next-web-app bb3313bc` · `sentinel 88bc5592` ·
`storage 4ce8ece5` · `messenger fa47850d` · `cms ca50c817` · `graphql-wundergraph 60c229f3` ·
`roadrunner 87d8d443` · `jobsimulation 462343b0` · `studio-desk 14a5442a` · `ant-academy 9c3843cd`.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| corpus/services/ai-readiness.md | 645 | yes |
| corpus/architecture/ai_architecture.md | 302 | yes |
| corpus/services/graphql-wundergraph.md | 265 | yes |
| corpus/services/ai-labs.md | 156 | yes |
| corpus/services/messenger.md | 128 | yes |
| corpus/services/customerio-sync.md | 75 | yes |
| corpus/architecture/README.md | 38 | yes |

(Also read, as instructed, `corpus/ops/platform-alignment.md` §5 in full — lines 240–834 of 1219.)

Search-pipeline note (§5 rules 1/2/4): two of my shells silently mis-fired and were caught by reading
stderr. (a) `--include=*.go` was eaten by zsh globbing (`no matches found`) — re-run with `rg`.
(b) `git show "$c:supergraph-config-prod.yaml"` raised zsh `bad substitution` on **every** ref, returning
`count=0` seven times in a row — which reads exactly like "the file never had subgraphs". Re-run from a
`bash` script file; the real counts are in the Positively-cleared section. An empty result from a FAILED
command is not evidence of absence.

---

## Blockers

### B1 — `ai_architecture.md` "The three simulation model defaults" enumerates a SET of three; the source file has **six** default-applying entry points, and one of the omitted ones is a *validation* default on **GPT-5.4** that contradicts the table's own validation row

- **Anchor:** `corpus/architecture/ai_architecture.md:238-247`, esp. `:240-241`
  > "### The three simulation model defaults
  >  The corpus asserted a single *"default: `gpt-5` via Azure"* for several releases. **`gpt-5` is not a default
  >  in any of the three places a default is applied** — measured at `app` HEAD:"

  and the table's third row `:247`:
  > "| **Scoring / validation** — not configurable at all, hardcoded | **Azure** | **`gpt-4.1`** (summarize: `gpt-4.1-mini`) | `simulator/ai/ai.go:20-26`, `GetValidationAIVendorAndModel` |"

  Restated as fact at `:232`: *"Three distinct defaults apply at three different points — see below"*.

- **Why it is false:** the SET is not three. `stack-demo/app/internal/jobsimulation/simulator/ai/ai.go`
  (165 lines) declares **six** exported vendor/model getters — `grep -n '^func '` gives
  `:20`, `:28`, `:35`, `:43`, `:51`, `:138` — and **five** of them apply a hardcoded or fallback default:

  | line | function | vendor / model it defaults to | live caller |
  |---|---|---|---|
  | `:20-26` | `GetValidationAIVendorAndModel` | Azure / `openai.GPT4Dot1` (+`GPT4Dot1Mini`) | the row the doc has |
  | **`:28-32`** | **`GetInterviewExtractionValidationAIVendorAndModel`** | **Azure / `openai.GPT5_4`** | `simulator/validation/v3/validator/validator.go:133`, `simulator/validation/basevalidator/extraction.go:515` |
  | **`:35-41`** | **`GetSuggestionAIVendorAndModel`** | **Azure / `openai.GPT4Dot1`**, hardcoded, `sequence` arg ignored | `simulator/outbound/prompts/suggestUserEmail.go:299` |
  | **`:43-49`** | **`GetReplyAudioAIVendorAndModel`** | **Azure / `openai.GPT4Dot1`**, hardcoded, `sequence` arg ignored | — |
  | `:51-136` | `GetAIVendorAndModel` | the per-arm switch defaults the doc *does* have | the row the doc has |
  | **`:138-165`** | **`GetReplyAIVendorAndModel`** | its own `default:` arm `:158-163` → **Azure / `openai.GPT5_4` / `openai.GPT5Mini`** | `simulator/outbound/outbound.go:287` |

  Two consequences, both material:
  1. The row that says validation is *"not configurable at all, hardcoded … `gpt-4.1`"* is only true of
     **one** of the two validation getters. The interview-extraction validator — wired into the v3 validator
     itself — is hardcoded to **GPT-5.4**. A reader auditing "what model grades a session" gets the wrong
     answer for the extraction half.
  2. The chat/voice **reply** path (`outbound.go:287`) has a *fourth* default (`GetReplyAIVendorAndModel`'s
     `default:` arm, Azure/GPT-5.4) that appears nowhere in the doc, and it is not reachable through any of
     the three enumerated points.

  This is precisely §5 rule 24's failure shape: the passage shows its derivation, every row of the table
  verifies (I opened and read all three: `:20-26` = Azure/GPT4Dot1/GPT4Dot1Mini ✓; `:65-66`/`:82-83`/`:126-127`
  = GPT4Dot1 ✓; `jobsimulation.go:1297` = `simulation.Gpt5Point1` ✓) — and the **set the arithmetic ranges
  over** is wrong. The three anchors are right; the cardinality is not.

  Note the sub-claim *"`gpt-5` is not a default anywhere"* survives (`enum.AIModelGpt5` maps to
  `openai.GPT5_4`, and the reply default is `GPT5_4`/`GPT5Mini`, never bare `gpt-5`). It is the enumeration,
  not that sentence, that is false.
- **Confidence:** high

### B2 — `ai-readiness.md` cites `AIReadinessClient.tsx:69` for `SHOW_SECONDARY_TABS`; line 69 is an import statement (actual: `:78`)

- **Anchor:** `corpus/services/ai-readiness.md:316`
  > "a 4th manager tab, **Compare** (cycle deltas), is fully built but **hard-gated off** —
  >  `AIReadinessClient.tsx:69` `const SHOW_SECONDARY_TABS = false;` strips it from the tab list."
- **Why it is false:** at `next-web-app@bb3313bc`,
  `apps/web/src/app/(authenticated)/(verified)/ai-readiness/AIReadinessClient.tsx:78` is
  `const SHOW_SECONDARY_TABS: boolean = false;`. Line **69** is
  `import { PeopleDrawer } from './_components/PeopleDrawer';`. Verified by `grep -n` plus `sed -n '69p'`.
  (The *mechanism* is correct — `:599` `.filter(… || (SHOW_SECONDARY_TABS && tab.key === 'compare'))` does
  strip the Compare tab. Only the anchor is wrong.)
- **Confidence:** high

### B3 — `ai-readiness.md`'s "✅ CORRECTED M219" blockquote — the two anchors it rests its retraction on both name the wrong construct (off by 16 lines)

- **Anchor:** `corpus/services/ai-readiness.md:460-462`
  > "The **CURRENT** dashboard (`AIReadinessClient.tsx:137-138`) computes
  >  `effectiveCycleId = selectedCycle ?? activeCycle?.id ?? latestClosedCycle?.id` and gates the data GET on
  >  `cyclesQ.isFetched` (`:150-154`)"
- **Why it is false:** measured at `next-web-app@bb3313bc`:
  - `const effectiveCycleId =` / `selectedCycle ?? activeCycle?.id ?? latestClosedCycle?.id;` is at
    **`:153-154`**. Line `:137` is blank and `:138` is `const cyclesQ = useAIReadinessCycles({ enabled: featureOn });`.
  - the data-GET gate `enabled: featureOn && cyclesQ.isFetched` is at **`:169`**, inside the
    `const dataQ = useAIReadiness({…})` block at `:166-170`. The cited `:150-154` is the *comment + the
    effectiveCycleId assignment* — i.e. it lands on the other claim's true location, which is the most
    confusing possible miss.
  - (`:133-134` for `const { orgEnabled } = useAiReadinessEnabled(true);` / `const featureOn = …`, cited at
    `ai-readiness.md:80`, **is** correct — so the drift is not uniform and cannot be dismissed as "the whole
    file shifted".)

  I hesitated between BLOCKER and MINOR because the *substance* of the retraction is right (the current
  dashboard does wait for `/cycles` and does pass `?cycle=`, and I confirmed both behaviours by reading
  `:150-170`). I booked it BLOCKER because this blockquote exists specifically to overturn an earlier claim
  by pointing at code, so its anchors are the load-bearing part, and because the briefing lists
  "wrong line anchors that point at the wrong construct" as a BLOCKER class.
- **Confidence:** high

### B4 — `graphql-wundergraph.md`'s self-anchor `:84` points at an unrelated blockquote

- **Anchor:** `corpus/services/graphql-wundergraph.md:128`
  > "So for its **final five months** compose built `Dockerfile.dev` — which is why a subgraph SDL change
  >  rebuilt the router, as `:84` describes."
- **Why it is false:** line 84 of this same file is
  `> **There is no application source here.** \`package.json\` is a stub` — the package.json-stub blockquote.
  It says nothing about SDL changes or rebuilds. The passage that *does* describe it is **`:110-111`**
  ("It *used to* rebuild whenever any subgraph schema changed, because the build context is the parent dir
  (`..`) holding all sibling repos."). Verified by `sed -n '84p;110,111p'`.
- **Confidence:** high on the fact; medium that it clears the "would mislead a reader doing real work" bar —
  it is an intra-document pointer with no consequence for the platform model. Booked as BLOCKER per the
  briefing's under-booking guidance; downgrade to MINOR without argument if the grader prefers.

---

## Minors

- `corpus/services/ai-readiness.md:302-303` — "A repo-wide grep finds the constant in exactly those **two**
  non-`node_modules` files." `rg -l "AI_READINESS_URL" -g '!node_modules'` returns **three**:
  `packages/core-js/src/constants/urls.ts`, `packages/ui/src/NavBar/useNavbarSections.tsx`, and
  `knowledge/ai-readiness/frontend-architecture.md`. The substantive point (only two *code* consumers) holds.
- `corpus/services/ai-readiness.md:316` — quotes `const SHOW_SECONDARY_TABS = false;`; the source reads
  `const SHOW_SECONDARY_TABS: boolean = false;`.
- `corpus/services/ai-readiness.md:576-577` — `computeCycleTotals` cited at `how_we_measure.go:253-261`;
  the doc-comment starts at `:256` and the `func` at `:260`. `:253-255` is the tail of the preceding
  function. Resolves, but starts three lines inside the wrong construct.
- `corpus/architecture/ai_architecture.md:232` — "the enum is
  `app/internal/cms/directus/collections/jobsimulation.go:983-990`". The `AIModel` const block is
  `:979-991` and holds **11** members; `:983-990` covers 8 of them (it starts at `Anthropic45SonnetAws`
  and omits the first three). All three examples the sentence gives do fall inside the cited range.
- `corpus/services/graphql-wundergraph.md:81` — self-anchor "`:174-176` of this same doc already said
  `localhost:5050` refuses the connection". `:174-176` is blank / the `### Run in Docker` heading / blank;
  the sentence is at `:178`. Same class as B4 but adjacent to the right section.
- `corpus/services/messenger.md:110` — "only the residual `SKILLPATH_STREAM=skillpath` remains", stated in a
  table whose header says *"Values shown are what docker-compose injects"* for **messenger**. That variable
  is on the **`backend`** service block (`platform/docker-compose.yml:64`); the messenger service block
  (`:240-278`) has no `SKILLPATH_*` key at all, and `rg -n "SKILLPATH" .` over the whole `messenger` repo
  returns **zero** hits (positive control: `SKILLER_RPC_ADDR` returns 2). Literally true of the compose file
  as a whole, misplaced for the row it sits in.
- `corpus/services/messenger.md:32-39` — the `internal/flow/` listing is stale: it omits `ai_readiness.go`,
  `coursebuilder.go`, `content_assigned.go`, `content_completed.go`, `invitation_reminders.go`. The
  `ai_readiness.go` omission is notable because `ai-readiness.md:256-258` asserts the readiness notification
  lifecycle emits "proto events consumed by the messenger service" — the file that consumes them is missing
  from messenger's own code map.
- `corpus/services/customerio-sync.md:19-33` — the block titled "### Compose definition" omits
  `env_file: - .env` and `networks: - app-network`, both present at `platform/docker-compose.yml:229-234`.
  It is a condensed paraphrase presented as the definition.
- `corpus/services/customerio-sync.md:13` vs `:44` — `:13` asserts "Local port: 8080 (HTTP — health/metrics)";
  `:44` hedges "Port 8080 **likely** serves a health/metrics endpoint". One of the two should move.

---

## Positively cleared (audited zeros)

Labelled per the briefing. **ENUMERATED** = I derived the predicate's membership from source independently
and compared cardinalities. **RE-DERIVED** = I only re-computed arithmetic the document displays.

### `graphql-wundergraph.md`

- **ENUMERATED — the subgraph ladder 5 → 4 → 3 → 1 (`:36-48`, `:154-158`).** I did not re-derive the sums;
  I listed the members at each commit from `git show <ref>:supergraph-config-prod.yaml | grep '^  - name:'`
  in `graphql-wundergraph@60c229f3`:
  `749dc86^` = 5 `[backend skiller jobsimulation cms skillpath]` · `749dc86` = 4 `[backend jobsimulation cms skillpath]` ·
  `7c17e63^` = 4 · `7c17e63` = 3 `[backend jobsimulation cms]` · `915da06^` = 3 · `915da06` = 1 `[backend]` ·
  `HEAD` = 1. The doc's "cms-in-app was the **3 → 1** step" and its charge that `915da06`'s own subject line
  ("supergraph 2→1") is wrong are both **correct**. `git show --name-status 915da06` marks
  `schemas/cms.graphqls` and `schemas/jobsimulation.graphqls` both `D`, in one commit — as claimed.
  I also swept the whole corpus for `2 → 1` / `3 → 1`: every non-retraction site says 3 → 1; there is **no**
  surviving "2→1" claim outside the four passages that exist to refute it. No cross-file drift here.
- **ENUMERATED — "there is no `:5050` on a local stack" / "no `graphql` compose service" / "no `repos.yml`
  entry" (`:7`, `:12`, `:79-82`).** `platform@2adcf714`: `grep -c 5050 docker-compose.yml` → **0**; the
  service list is `sentinel backend jobsimulation cms storage customerio-sync messenger roadrunner
  studio-desk next-web-app gotenberg` — no `graphql`; `repos.yml` lists 9 repos, none of them
  `graphql-wundergraph`. The `graphql` *profile name* does survive on 6 services, exactly as claimed.
- **ENUMERATED — "no `type Subscription`" in `schemas/backend.graphqls` (`:58-61`).** `grep -n '^type Subscription'`
  → no match; the 6 `Subscription` substring hits enumerate to `PlanSubscription`, `activeSubscription`,
  `stripeSubscriptionId`, `stripeSubscriptionItemId` — all Stripe/plan names, as claimed (the doc names three
  of the four; the fourth is the same family). No `subscription:` block in any `supergraph-config-*.yaml`.
  `git branch -a --contains bba862f` → `remotes/origin/feat/use-web-socket` only — the "never merged" claim holds.
- **ENUMERATED — `ci/update-subgraph.sh` carries exactly one `gh release download` (`:171`).** `grep -n` returns
  a single hit, line **9**, `-R anthropos-work/app`. Cited line number correct.
- Verified individually and correct: `terraform/main.tf:20 service_desired_count = 1`; `package.json` =
  `{"name": "graphql-wundegraph"}` (misspelling present); `CLAUDE.md` heading carries the same misspelling;
  cosmo router `0.275.0` / `wgc@0.104.0` / `node:22.11-alpine` in both Dockerfiles; `federation_version: =2.3.2`;
  `listen_addr 0.0.0.0:8080` + `graphql_path /graphql` + `max_request_body_size: 35MB` in both configs;
  playground/introspection `true` in compose, `false` in prod; `terraform/tests/` present; README's
  aarch-only `make run`; `docker-compose.yml:334,352` (the two frontend endpoint bakes) correct;
  `app/internal/authorization/gqlauthz/gqlauthz.go:186` = the `unknown viewer` rejection, with the exemption
  triple at `:175-176` and the two named tests present in `gqlauthz_test.go` (`:132`, `:176`).
  All nine platform shas the doc narrates (`b56d731 360efd4 2adcf71 d92e84e 63d285c 719befb a2a3ee6 2c85211
  67ba772`) resolve, with subjects consistent with the narrative.

### `ai-readiness.md`

- **ENUMERATED — the 31-skill / 25.0-denominator platform default (`:247`, `:350`, `:544`).** I listed
  `defaultReadinessSkills` from `app/internal/aireadiness/defaults.go:29-62`: **19** entries at weight 1.0 and
  **12** at 0.5 = 31 entries, weight sum 19 + 6 = **25.0**. `defaultReadinessSims` (`:75-79`) is exactly 3:
  two `StepSimulation` rows on tracks `tech`/`business` plus one `StepInterview` on `both`. Cardinalities match.
  (The dependent arithmetic then RE-DERIVES: `round(1.0/25*30)=1`, `round(0.5/25*30)=1`, `round(9/25*30)=11`,
  `25/25*30=30`; and "held×4 is always integral for half-step weights" is sound.)
- **ENUMERATED — the archetype bands and buckets (`:145-148`).** `aireadiness/scoring.go:26,30` declare
  `archetypeHighBand = 75` / `archetypeLowCeil = 50`; `classifyArchetype` `:46-56` is exactly
  Champion(≥75,≥75) / Standby(≤50,≤50) / `usage >= knowledge` → Explorer / else Hidden Talent, so the exact
  tie goes to Explorer as the doc says; `bucketKnowledge` `:66-77` yields None 0-24 / Low 25-50 / Medium 51-74
  / High 75-100. Every clause of the doc's conjunction tested separately (§5 rule 17).
- **ENUMERATED — the 13 tables in the data-model table (`:169-183`).** Each of
  `ai_readiness_{cycles,steps,skills,sims,user_step_progresses,live_snapshots,snapshots,diagnose_narratives,
  text_translations,recommendations,email_overrides,notification_logs,notification_optouts}` plus
  `organization_settings` returns exactly 1 hit for `Name:       "<t>"` in
  `internal/data/ent/migrate/schema.go`, and `interview_aggregated_reports` exists at
  `ent/interviewaggregatedreport/interviewaggregatedreport.go:34`. All 14 named tables exist; none is invented.
- **ENUMERATED — the 5 current usage-KPI ids (`:201`, `:616`).** `usageDimSpecs`
  (`aireadiness/how_we_measure.go:1138-1159`) has exactly five entries: `avg_adoption`, `avg_transformation`,
  `avg_originality`, `avg_depth`, `avg_ownership`. The retired `avg_frequency`/`avg_breadth`/`avg_context_fit`
  return **0** hits in non-test source. (Worth noting the *code's own* comment at `:1161` still says "four"
  tiles while the spec slice holds five — the corpus is right and the source comment is stale.)
- **ENUMERATED — the `interactions.action_type` enum is exactly `{email, call}` (`:581`).**
  `ent/migrate/schema.go:2286` — `Enums: []string{"email", "call"}`.
- **ENUMERATED — the two membership filters and which branch takes which (`:406-432`).** `readiness.go:387-391`
  is `if cyc != nil { keepInCycleStep1(…) } else { keepStartedMembers(…) }` — the doc's attribution (active →
  `keepInCycleStep1` at `:388`; no-active → `keepStartedMembers` at `:390`) is correct and its own retraction
  of the earlier inverted claim is sound. `queryReadinessStarters` (`steps.go:915-938`) is verbatim
  `SELECT DISTINCT user_id FROM public.ai_readiness_user_step_progresses … AND status <> 'not_started'` — no
  `user_skill_evidences` read, exactly as the doc's correction says; the quoted comment *"This DB signal is
  the only real 'has started' check"* is at `:913`, inside the cited `:907-914`.
    `queryInCycleStep1Completers` (`readiness.go:638-652`) does demand StepSkillMapping AND StepCompleted AND
  CompletedAtGTE(since) — all three conjuncts present.
- **ENUMERATED — the four `GetAIReadinessWithOptions` routes (`:443-451`).** `readiness.go:289` opens the func;
  `:291-297` is the cycle-scoped closed→snapshots route; `:309-312` is the default no-active/latest-closed
  →snapshots route; `:314` is the `buildLiveResponse` fallthrough. All four cited anchors land on the right
  construct.
- Verified individually and correct: `enum/organization_settings.go:47` =
  `OrganizationSettingAIReadiness = "ai_readiness"`; `computeTier1` at `readiness.go:139` (and its
  whole-repertoire denominator, with the platform's own "a larger configured set makes a full score harder to
  reach" comment); `csv.go:13` says verbatim "the 15-column export contract"; the eleven
  `/workforce/ai-readiness*` REST routes at `api/server.gen.go:1768-1778` (including `/setup` GET+POST and
  `/cycles/:cycleID/close`); all four GraphQL entries in `graph/schemas/ai_readiness.graphqls`;
  `ProvisionAIReadiness`, `GetAIReadinessSetupStatus`, `CloseDueAIReadinessCycles`, `RefreshLiveSnapshots`,
  `CompareCycles`, `recommendation_engine.go`, `recommendation_signals.go`, `emailoverride/`, `emailpreview/`,
  `notifications/` all present; commit `4c28365f` exists with the quoted subject; `WorkforceDirectory` is the
  seam and `LoadMembers`/`LoadMembersByUserIDs` implementations did stay in `workforce/members.go:349,353`;
  the whole file-rename table (`ai_readiness.go`→`readiness.go` etc.) matches the actual `internal/aireadiness/`
  listing, and `how_we_measure_v2.go` is indeed gone with only its `_test.go` remaining;
  `computeInterviewInsightsV2` at `how_we_measure.go:819` reads `public.interview_aggregated_reports` (`:1055`)
  and nothing else; `usageDimensionsFromReports` `:1164`, `holdsBackFromInsights` `:986`,
  `resolveSessionAuthors` `:1093`; `how_we_measure.go:285-287` = the `interactions`→`job_simulation_sessions`
  join; app `v1.351.1` (2026-07-23) and `v1.315.0` both exist in `CHANGELOG.md`; next-web-app is at `2.133.0`
  (≥ the claimed 2.89.0 floor); the D-07 re-anchor manifest reads `path: internal/aireadiness/readiness.go` at
  **`app-aireadiness-snapshot-loadmembers.yaml:42`** with the "v2.7 M254 RE-POINT" header at **`:33`** — both
  cited line numbers exact — and `demo-stack/tests/test_aireadiness_snapshot_loadmembers_m254.py` exists.
- next-web side: `AIReadinessClient.tsx` contains **0** occurrences of `posthog` and **0** of
  `flag_ai_readiness` (`rg` exit 1, positive control on `AIReadinessClient` returns hits) — the "manager
  dashboard does not check the flag" callout is correct; `useAiReadinessActive.ts:22` and
  `aiReadiness.constants.ts:26` are both exact; the four-clause TS gate quoted at `:101-104` matches the real
  hook line-for-line; `AIReadinessHero.tsx:88 if (!air.deadline) return null;` exact; `deriveMode` at
  `components/ai-readiness/useAIReadiness.ts:48-62` exact, and it *does* treat a null deadline as passed;
  `urls.ts:52`, `useNavbarSections.tsx:4/:398-400/:547` all exact; `useWorkforceAIReadiness.ts:23-27` is the
  `searchParams: { tag }` call with no `cycle` param and no `/cycles` call; `e2e/specs/web.ai-readiness.spec.ts`
  exists; the legacy `/enterprise/workforce/ai-readiness` directory is **gone** (the parent holds only
  `(new)/`, `WorkforceNewClient.tsx`, `layout.tsx`, `page.tsx`) so the 404 claim holds; `HowWeMeasureTab.tsx`
  is **1,989** lines with `grep -c interviewQuestions` = **0**, and all five cited anchors —
  `:1879` block open, `:1903` label, `:1915`/`:1921`/`:1927` the three cells — are exact. This last passage is
  the best-anchored in the file and stands in sharp contrast to B2/B3 two screens above it.

### `ai_architecture.md`

- **ENUMERATED — "Mistral is nowhere in the AI-manager path" (`:33-34`).** The only `mistral` constructions in
  `app` are `internal/cms/studio/markdownManager.go:19` (`mistral.NewMistral(nil, os.Getenv("MISTRAL_API_KEY"))`)
  and `studioManager.go:583` — both exact line numbers, both OCR, neither in `AIManager`.
- **ENUMERATED — the `getClient` switch (`:22-24`, `:28-29`).** `jobsimulation/ai/ai.go:30-33` declares exactly
  four vendor consts (`azure`, `openai`, `anthropic-aws`, `anthropic`); `getClient` at `:259-288` is a plain
  switch with `AnthropicAws` and `Anthropic` both returning `a.anthropicClient`; the `Azure` arm returns
  `a.azureClientEu` and returns it **unchanged** on a flag-lookup error — the doc's "a *failed* flag lookup
  logs and keeps the EU client" is exact. `internal/ai/` does **not** exist (the doc's negative claim holds);
  the two selection sites `jobsimulation/ai/ai.go:267,344` and `skillerai/ai.go:347` are all three the
  `"flag_use_azure_us"` literal, exactly as cited. `isThrottlingError` `:129` is 429-only and drives
  `throttled` → openai override at `:150-153` / `:297-299`.
- **ENUMERATED — the Directus `AIVendor` enum has five members and the runtime switch four cases (`:59-62`).**
  `cms/directus/collections/jobsimulation.go:967-975` = `{azure, azureglobal, openai, anthropic-aws, anthropic}`
  (`Azureglobal` at `:971`, as cited); `simulator/ai/ai.go` cases at `:58`, `:69`, `:86`, `:102` — four — with
  `default:` at `:114-115`. 5 − 4 = `azureglobal`, exactly the doc's derivation.
- **ENUMERATED — the `checkerEngines` map is stored and never read (`:264-266`).** Its only occurrences in
  non-test source are `v3/validator/validator.go:43` (field), `:60` (populate), `:595` (pass-through) and
  `basevalidator/criterion.go:34` (field), `:72` (param), `:90` (assign). It is never indexed. Dispatch really
  is the hardcoded `switch critCheck.Engine` at `criterion.go:126-127`.
- **ENUMERATED — `gpt-5.2` in no studio config (`:127-128`).** `grep -rn "gpt-5.2" studio/configs/` → exit 1
  (positive control: `gpt-5.4` returns 3/6/6 across the three ini files). Its only source occurrences are
  `studio/services/ai.py:356,508` — the two pricing entries the doc names — plus two `knowledge/*.md` files,
  which are not configs.
- **ENUMERATED — the studio slot table (`:117-123`).** `studio/configs/production_config.ini:26-36` matches the
  table cell-for-cell (FAST/STRICT = gpt-5-mini none; EXECUTION = gpt-5.4 none; CREATIVE = gpt-5.4 low;
  REASONING = gpt-5.4 medium) and stable ≡ experimental; `diff` of `:26-36` against
  `development_config.ini:26-36` is empty, so "identical to both" is exact. `config_template.ini:39/:40` are
  the two `azure, gpt-4o, none` slots, at precisely those line numbers.
- **ENUMERATED — `grep -rin 'bedrock|boto3' app/studio/` returns 0 (`:46`).** Confirmed (positive control:
  `anthropic` in `studio/services/ai.py` returns 8). The Studio-Room-was-never-on-Bedrock retraction holds.
- Verified individually and correct: `coursebuilder/bedrock.go:106-113` (key present → first-party Anthropic,
  absent → Bedrock); `jobsimulation.go:905 AIVendor *AIVendor`, `:1297` `simulation.Gpt5Point1`, `:1302`
  `simulation.Openai`, `:1307` the `Sequence{` build — the *nullable-on-the-DTO / replaced-before-the-build*
  mechanism is exactly as described; the `voice_engine` enum is a 4-member enum at `:1079-1085` with
  `voiceEngineFromDirectus` nil→`gptrealtime` inside `:1594-1600`; `calls/livekit.go:131-135` (the
  `flag_use_realtime_openai` read) and `:140-144` (endpoint→`openai-hosted`) are exact and the flag *is* read
  inside `CreateAgentDispatch` (`:106`); `criterion.go:127` / `:168` / `:428` / `validateCodeDiff` at `:450`;
  `skills.go:40-51` `calculateCompetencyLevelScore` (including the literal `// TODO fix this formula`) and
  `:53-64` `calculateSkillScore` and `:75` `float32(passed)/float32(total)*100` — all three exact;
  `skill/skill.go:617-623 convertLevelTo100`; `CompetencyReadLevel.tsx:18`;
  `internal/aiusage/ai_usage.go` with `Event_AiUsage` at `:98`; `GetJobSimulationCallSignedURL` /
  `GetJobSimulationCallConversationToken` both live in `resolver_jobsimulations.go`.
  I also swept the corpus for the retracted "Azure → Bedrock → Mistral → OpenAI → Anthropic" ladder: it
  survives at only two sites (`ai_architecture.md:16`, `external_services.md:546`), both retractions.
  Every cross-file anchor this doc cites resolves: `external_services.md:541` is the Anthropic-Direct row,
  `:577-587` the four-exit derivation, `shared_libraries.md` carries `<a id="taxonomy-figures">` at `:196`,
  and `security_compliance.md:226` is `### EU AI Act`.

### `ai-labs.md`

- **ENUMERATED — the Stripe webhook handles exactly `customer.created` + `customer.subscription.{created,
  updated,deleted}` (`:109-111`).** `api/api.go:315` and `:321-323` are the only two `case` labels in the
  event switch (the other two `case`s in range are a nested switch on `sub.Status`), and
  `rg -n "checkout.session.completed" internal/` returns **nothing** (exit 1) — the "no checkout.session.completed"
  negative claim holds.
- **ENUMERATED — the `creditCost` map and `purchasePackages` (`:75-76`).** `credits/cost.go:86-90` is exactly
  `{course.build: 5, course.refine: 1, course.translate: 1}` — three entries, no more; `:200-204` is exactly
  `{starter 50, team 200, scale 500}`. `MarginMarkup = 1.40` (`:133`), `PricePerCreditUSD = 0.45` (`:142`),
  `DefaultSeedBalance int64 = 500` (`:231`). All figures exact.
- **ENUMERATED — the five `CapError` reasons (`:60`).** `labs_budget.go:247-251` declares exactly
  `model_not_in_tier`, `per_session_cap`, `org_total`, `user_monthly`, `user_lifetime`; the three tiers at
  `:26-28` are `essential`/`professional`/`frontier`. Cardinalities match.
- Verified individually and correct: app is at **`v1.363.2`** (`CHANGELOG.md:5`, `git describe` = `v1.363.2`) —
  the doc's parenthetical is exact; `internal/{labs,credits,payments,subscriptions}` + `stripe/` all exist,
  `internal/labs/{adapter,catalog,labsapi,session}`; ent schemas `lab.go`, `lab_body.go`, `lab_session.go`,
  `organization_credit.go`, `credit_transaction.go`, `org_subscription.go`, `stripe_customer.go`,
  `subscription.go`; all five named migrations exist by exact filename
  (`20260617203555_add_labs_catalog.sql`, `20260529072659_add_lab_session.sql`,
  `20260617120000_add_lab_session_model.sql`, `20260717151144.sql`, `20260626120000_add_org_subscriptions.sql`);
  `LabSession` schema carries the explicit *"deliberately does NOT use PrimaryKeyMixin … id is supplied by
  labs-api"* comment and a `field.String("id")`; `spend_reconciler.go:60 Interval: 30 * time.Second`;
  `labsapi/client.go:6` "default :7070"; `cmd/{labsdemo,labsimport,labskey}` with `labsdemo` on `:8099`
  (`main.go:115`); `labs.graphqls` carries `labs`, `labBySlug`, `labSessions`, `mySessions`, `labSpendByDay`,
  `createLabSession`, `cancelLabSession`; `POST /credits/purchase was removed in Wave 13` is stated verbatim
  at `web/backend/credits/handler.go:12`. The doc's headline correction — that `anthropos-work/AI-Labs` is a
  real separate repo — is independently corroborated inside `app`: `ent/schema/lab_session.go:6` cites
  `AI-Labs/docs/PHASE_B_LABSESSION.md`.
- **This file produced no findings at all.**

### `messenger.md`

- **ENUMERATED — the compose env block (`:98-111`).** `platform/docker-compose.yml:240-278`: ports
  `8200:8200` + `8201:8201`; `CMS_RPC_ADDR=http://cms:8091` at **`:256`** and
  `JOBSIMULATION_RPC_ADDR=http://jobsimulation:8401` at **`:258`** — both cited line numbers exact, and both
  really do still address the husk containers while `SKILLER_RPC_ADDR=http://backend:8083` alone was
  re-pointed. No `SKILLPATH_RPC_ADDR`. `depends_on` = redis, postgresql, backend, cms, jobsimulation — no
  skillpath. `profiles: [messenger, all]`, not `graphql`. This is the exact claim §5's iter-22 note says two
  service docs got wrong; messenger.md has it right.
- **ENUMERATED — `REDIS_WORKER_INDEX` is set in compose but read nowhere (`:105`).** `rg -n "REDIS_WORKER_INDEX" .`
  over the whole `messenger` repo → exit 1, zero hits; the only stream index consumed is
  `REDIS_STREAMS_INDEX` at `cmd/root.go:107`.
- Verified individually and correct: Go **1.25.0** (`go.mod:3`), `getbrevo/brevo-go v1.1.3` (`:11`),
  `osteele/liquid v1.8.1` (`:14`); `rpcsrv.go:25-30` = the two `CodeUnimplemented` stubs (Cancel `:25-27`,
  Schedule `:28-30`) with `Send` implemented at `:31`; the three binary fallbacks `PORT=8080` (`root.go:63`),
  `RPC_PORT=8081` (`:64`), `REDIS_STREAMS_INDEX=2` (`:107`) all exact; `READONLY_DB_CONNECTION` at
  `root.go:147` exact; the 2 h / 12 h staleness guards at `flow/jobsimulations.go:140-151` exact (2 h at `:142`,
  12 h at `:148`); the five `OrgSkillPath*` handlers inside `flow/flow.go:72-87`; `getSkillPath` reaching CMS
  at `assignments.go:828`; the five RPC client addresses wired in `root.go:120-140`
  (`CMS_RPC_ADDR`, `BACKEND_USERS_RPC_ADDR` ×2, `SKILLER_RPC_ADDR`, `JOBSIMULATION_RPC_ADDR`) — matching the
  Dependencies bullet exactly; whitelabel landing in **v0.34.0** (`CHANGELOG.md:111`, 2026-04-24,
  "integrate Copilot DB for organization details and enhance email notifications with whitelabel support").

### `customerio-sync.md`

- Verified: the compose block at `docker-compose.yml:220-238` has
  `context: git@github.com:anthropos-work/customerio-sync.git#main`, `ssh: ["default"]`,
  `args: {VERSION: dev, GH_ACCESS_TOKEN: $GH_PAT}`, `ports: 8080:8080`, the exact `DB_CONNECTION_BACKEND` DSN
  with `search_path=public`, `profiles: [customerio-sync, all]`, `depends_on: postgresql (service_healthy)`.
- **ENUMERATED — "unique among Anthropos services" (`:15`).** It is the only service in
  `docker-compose.yml` whose `build.context` is a git URL (every other is a `../<repo>` path), and the only
  compose service absent from `repos.yml`'s 9 entries. Cardinality checked, claim holds.

### `architecture/README.md`

- **ENUMERATED — the index is complete.** `corpus/architecture/` holds 11 files; the README links all **10**
  non-README ones, with no dangling link to a file that does not exist.
- **ENUMERATED — "`authn` is not a dependency of any service" (`:21`).**
  `grep -c "anthropos-work/authn" go.mod` returns **0** for all seven checked-out Go repos
  (`app sentinel storage messenger cms jobsimulation roadrunner`); the positive control `colony/authn` returns
  live import sites inside `app/internal`. Claim holds.
- Verified: `rosetta-extensions/stack-core/platform_alignment_guard.py` exists;
  `rosetta-extensions/alignment/` exists (README, cmd, examples, go.mod, internal);
  `rosetta-extensions/stack-seeding/dna/` holds the `datadna` harness (`dna.go`, `catalog.go`, `diff.go`,
  `fidelity_probe.go`, `data-dna.json`).
- **This file produced no findings.**

---

## What I could not settle

1. **`ai_architecture.md:138` — `public.skill_embeddings` = 42,790 and `public.job_role_embeddings` = 18,919
   rows** (and the `≥22,470` public job-role figure at `:143-145`). These are production-DB measurements;
   I have no DB in this pass. **Settled by:** a read-only `SELECT count(*) … WHERE organization_id IS NULL`
   against prod (the `/db-query` path). Flagging only because §5 rule 17's canonical example is this exact
   family of figure and it has been wrong twice.
2. **`ai_architecture.md:183-189` — the five LiveKit agent repositories** (`livekit-agent`,
   `livekit-agent-chain`, `livekit-agent-azure-us`, `livekit-agent-azure-eu`, `livekit-agent-azure-eu-fr`)
   and the claim that `azure-eu`/`azure-eu-fr` "dispatch nothing". None is cloned under `stack-demo/`.
   **Settled by:** `gh repo list anthropos-work` + reading the two agents' dispatch config.
3. **`graphql-wundergraph.md:8` — "ARCHIVED on GitHub, 2026-07-30 (read-only)".** The local clone is at
   `60c229f3` and carries no archive marker; archive state is a GitHub API property.
   **Settled by:** `gh api repos/anthropos-work/graphql-wundergraph --jq .archived`.
4. **`ai-readiness.md:342` — "78.4% (≈156 of the 199 frozen snapshots)"** and the paired M219/M250 live
   measurements (`2.09 s` live recompute, `24 ms` frozen read, `180 s → 19 ms` demo-patch).
   `156/199 = 78.39%` RE-DERIVES, but neither the population nor the timings appear as literals in
   `rosetta-extensions/stack-seeding/seeders/ai_readiness_funnel.go`; they are emergent from a live seeded
   stack. The doc itself says *"Re-measure before relying on either number"*.
   **Settled by:** a cold reset-to-seed on a demo stack + the authenticated dual-endpoint probe.
5. **`ai-labs.md`'s labs-api–side claims** — that labs-api owns session-ID generation, runs a LiteLLM
   gateway, and defaults to `:7070`. The `:7070` default and the `LabsAPIClient` seam are visible in `app`;
   everything on the far side of the HTTP boundary lives in the un-cloned `anthropos-work/AI-Labs` repo.
6. **`messenger.md:7`'s "per-domain notification rules" completeness.** The doc's `internal/flow/` map is
   demonstrably stale (see Minors); I did not read the five undocumented flow files, so I cannot say whether
   any of them contradicts the Role section rather than merely extending it.
