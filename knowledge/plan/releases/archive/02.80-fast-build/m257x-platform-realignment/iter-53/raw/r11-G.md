# Seat G — reading #11 (adversarial diff seat)

Scope: `git diff 1255998..0e35b1a -- corpus/ CLAUDE.md .claude/` — 11 files, 131 insertions / 50 deletions.
(`CLAUDE.md` and `.claude/` are in the pathspec but were **not touched** by the range; the 11 touched files
are all under `corpus/`.)

Ground-truth shas verified before starting — all 13 clones match the briefing table exactly
(`app 5ba17044`, `app/studio aeec036a`, `platform 2adcf714`, `next-web-app bb3313bc`, `sentinel 88bc5592`,
`storage 4ce8ece5`, `messenger fa47850d`, `cms ca50c817`, `graphql-wundergraph 60c229f3`,
`roadrunner 87d8d443`, `jobsimulation 462343b0`, `studio-desk 14a5442a`, `ant-academy 9c3843cd`).

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/architecture/architecture_overview.md` | 353 | yes |
| `corpus/architecture/dependency_map.md` | 103 | yes |
| `corpus/architecture/external_services.md` | 831 | yes |
| `corpus/architecture/security_compliance.md` | 281 | yes |
| `corpus/architecture/shared_libraries.md` | 242 | yes |
| `corpus/ops/platform-alignment.md` | 1219 | yes |
| `corpus/services/ai-readiness.md` | 645 | yes |
| `corpus/services/hiring.md` | 416 | yes |
| `corpus/services/jobsimulation.md` | 229 | yes |
| `corpus/services/roadrunner.md` | 171 | yes |
| `corpus/services/studio-room.md` | 473 | yes |

Total 4963 lines. Each hunk was read with ±20 lines of surrounding context in the **current** file, not
only as diff lines.

---

## Blockers

### B1 — the correction left the refuted "30 use `OrganizationMixin{}`" standing 15 lines above its own retraction

- **Anchor:** `corpus/architecture/security_compliance.md:68` —
  > `> **30** use `OrganizationMixin{}` — the one that carries the privacy `Policy()` (`mixin.go:126`). Seven use`

  and, in the same blockquote, `:108` —
  > `> `OrganizationMixin{}` in 30, `OrganizationIDMixin{}` in 7, ...`

- **Why it is false:** The iter-52 repair inserted, at `:82-90` of the *same blockquote*:
  > *"`grep -c 'OrganizationMixin{}' schema/*.go` returns **30**; one of them, `user_resource.go:22`, is
  > **commented out** ... so the live count is **29**."*

  and the diff also rewrote `:76` ("**29** `OrganizationMixin{}` users") and `:135` ("the **29** live
  `OrganizationMixin{}` users (a 30th is commented out at `user_resource.go:22`)"). But `:68` — the
  *opening* measurement sentence of the fence, the one a reader hits first — was **not** touched and still
  says "**30** use `OrganizationMixin{}` — the one that carries the privacy `Policy()`", i.e. asserts that
  30 schemas carry the org privacy policy. That is exactly the claim the repair exists to refute.

  Ground truth (ENUMERATED, `app 5ba17044`):
  - `command grep -rl 'OrganizationMixin{}' internal/data/ent/schema/*.go | wc -l` → **30** files.
  - `internal/data/ent/schema/user_resource.go:22` is
    `\t\t// OrganizationMixin{},  // We need to work on this` — the only commented occurrence
    (verified by filtering all 30 hit lines for `//`; the other 12 with trailing `//` are inline
    trailing comments on live lines, not commented-out lines).
  - So live users = **29**.

  The file therefore asserts 30-and-policed at `:68`, 29-live at `:76`/`:83-85`/`:135`, and 30 again at
  `:108`. A reader who stops at the first measurement sentence — which is where the fence's own
  "**⚠️ Isolation is NOT automatic**" warning points — gets the refuted number.
- **Confidence:** high

### B2 — `OPENAI_API_KEY` was deleted from the `platform/.env` block using the wrong predicate; it *is* a `platform/.env` variable

- **Anchor:** `corpus/architecture/external_services.md:616-632` —
  > `AI services are configured via environment variables in `platform/.env`:`
  > ```bash
  > # OpenAI
  > OPENAI_KEY=sk-proj-xxxxx
  > ...
  > ```
  > `Re-derive with `command grep -rho '\b<NAME>\b' --include='*.go'` in `app` (`5ba17044`) ... `OPENAI_ORG_ID`, `AZURE_OPENAI_ENDPOINT` and `AZURE_OPENAI_DEPLOYMENT` return **0** and were removed; `OPENAI_API_KEY` returns 2, both the studio subprocess remap.`

- **Why it is false:** The block's stated predicate is *"environment variables in `platform/.env`"*. The
  correction's derivation ranges over a completely different set — token occurrences in `app`'s Go
  source — and on that basis **removed `OPENAI_API_KEY` from the block**. But `OPENAI_API_KEY` is a
  first-class, separately-documented `platform/.env` variable:

  `stack-demo/platform/.env_example` (`platform 2adcf714`):
  ```
  41:# ── CMS AI ──────────────────────────────────────────────────────────
  46:# CMS OpenAI (separate key from OPENAI_KEY)
  47:OPENAI_API_KEY=
  48:OPENAI_ENDPOINT=https://api.openai.com/v1/chat/completions
  ```
  The comment at `:46` explicitly distinguishes it from `OPENAI_KEY` — the very name the correction kept.

  The wider failure is the rule-24 one the corpus itself just wrote up: the SET was never enumerated.
  `.env_example`'s AI section declares **22** names (`OPENAI_KEY`, `AZURE_OPENAI_KEY`,
  `AZURE_OPENAI_ENDPOINT_URL`, and the `_EU`/`_US`/`_RESULTS`/`_VOICE` quadruples of the latter two,
  `ANTHROPIC_API_KEY`, `ANTHROPIC_ENDPOINT`, `MISTRAL_API_KEY`, `CMS_AZURE_OPENAI_KEY`,
  `CMS_AZURE_OPENAI_ENDPOINT_URL`, `OPENAI_API_KEY`, `OPENAI_ENDPOINT`, `AZURE_API_KEY`, `AZURE_ENDPOINT`);
  the block shows 4 and now carries an instruction telling the next auditor to re-derive it from Go greps,
  which will delete more real names on the next pass.

  (The four *retained* names are all correct — `.env_example:10,13,14,35` — and the arithmetic reproduces
  exactly: I re-ran the documented command and got 5 / 26 / 13 / 13, and 0 / 0 / 0, and
  `OPENAI_API_KEY` = 2. The count is right; the claim it supports is wrong.)
- **Confidence:** medium-high. Hesitated because the block is illustrative and never says "complete" —
  but the new sentence turns it into an audited set with a named derivation, and that derivation
  demonstrably removed a live variable.

### B3 — the `gen.py:45-47` "separate bare names" enumeration names 3 of 6, and one it omits is on the cited line

- **Anchor:** `corpus/architecture/external_services.md:634-635` —
  > `**This block covers `app`'s Go only**; `app/studio/gen.py:45-47` reads the separate bare names `AZURE_API_KEY` / `AZURE_ENDPOINT` / `OPENAI_ENDPOINT`.`

- **Why it is false:** `stack-demo/app/studio/gen.py` (`app/studio aeec036a`):
  ```
  45:    secrets_keys = [
  46:        'AZURE_API_KEY', 'AZURE_ENDPOINT',
  47:        'OPENAI_API_KEY', 'OPENAI_ENDPOINT',
  48:        'ANTHROPIC_API_KEY', 'ANTHROPIC_ENDPOINT']
  ```
  Three defects in one sentence:
  1. The list is **six** names, not three.
  2. The cited range `45-47` *contains* `OPENAI_API_KEY` (line 47) — the sentence walks past it while
     pointing at the line it sits on. This is the "cited line is one of four cases" failure.
  3. `ANTHROPIC_API_KEY` (line 48, outside the truncated range) is **not** a "separate bare name" at
     all — it is the *same* name `app`'s own coursebuilder ships, which is precisely why
     `app/internal/cms/studio/studioManager.go:1055-1062` strips every bare name from the child env
     before remapping: *"the coursebuilder's own key ships as ANTHROPIC_API_KEY ... Without the strip, a
     missing CMS_STUDIO_ANTHROPIC_API_KEY would silently hand Studio the coursebuilder's key."* Calling
     it "separate" inverts the documented collision hazard.
- **Confidence:** medium-high

### B4 — studio-room's outbound calls are enumerated as complete and are not; the new set contradicts `dependency_map.md:25`

- **Anchor:** `corpus/services/studio-room.md:389` —
  > `Its outbound calls are to the **AI providers** — `services/ai.py` instantiates OpenAI / AzureOpenAI / Anthropic clients (`:1-2, 383, 530, 664`), consistent with `:36` and `:261-266` above — plus the skills taxonomy service (`api.anthropos.work`) via `services/taxonomy.py`.`

- **Why it is false:** The old sentence ("its **only** outbound API call is to the skills taxonomy
  service") was indeed false; the replacement swapped one exhaustive claim for another, and the new one
  is also incomplete:
  - `app/studio/tools/pdf2md.py:24` `from mistralai import Mistral`, `:96` `client = Mistral(api_key=...)`,
    `:127` `model="mistral-ocr-latest"` — an outbound call to **Mistral's OCR API**, driven by
    `app/studio/tools/r3.py:139,190-200`. (`grep -rniE 'bedrock|boto3'` under `app/studio/` returns 0 —
    positive control in the same pass: 8 `.py` files match `openai` — so the *bedrock* half of the same
    paragraph family is genuinely clean; Mistral is not.)
  - `app/studio/agents/simulation/export.py:51` `response = requests.get(image_url, stream=True)` — an
    outbound HTTP fetch inside the **generation pipeline** (not `tools/`), against whatever host the
    generated-asset URL names.
  - It **contradicts an untouched corpus row**: `corpus/architecture/dependency_map.md:25` lists
    Studio-Room's external services as *"**OpenAI**, **Anthropic**, **Mistral**"*. Ground truth says
    Mistral is present in the repo; the correction says the outbound set is OpenAI/Azure/Anthropic +
    taxonomy. One of the two must move.
- **Confidence:** medium. Hesitated because `tools/pdf2md.py` is not on the `gen.py` path — but the
  sentence's subject is "studio-room", the corpus elsewhere credits it with Mistral, and the
  correction's whole purpose was to close an incomplete outbound-call enumeration.

### B5 — new cross-reference `:157-159` points at lines that carry one third of the claim attributed to them

- **Anchor:** `corpus/services/hiring.md:252-254` —
  > `> (This said *"neither table exists"* until M257x iter-52 — a flat contradiction of `:157-159` above, which is`
  > `> the adjudicated form: the mirror was dropped, `jobsimulation.sessions` was not, and no local stack creates`
  > `> that schema either way.)`

- **Why it is false:** `hiring.md:157-159` reads:
  ```
  157: > `20260729133514.sql` (2026-07-29) the score lived on `public.local_jobsimulation_sessions`, a `Float32`
  158: > **MIRROR** that shadowed a `jobsimulation.sessions` row. That migration **re-pointed the referencing
  159: > assignment-session link ids** and **dropped the mirror** (`:58-62`) — it did *not* back-fill (`SET "score"`
  ```
  Only the *first* of the three attributed facts ("the mirror was dropped") is in that range. "`jobsimulation.sessions`
  was not [dropped]" is at **`:162`**; "no local stack creates that schema either way" is at **`:165-166`**
  (`> it is not evidence that the schema is physically present. On a **local dev/demo stack it is not**: / >
  `repos.yml:17-19` `migrations: false`.`). The correct range for "the adjudicated form" is `:162-166`.
  The anchor is new text introduced by this diff, and it names the wrong three lines.
- **Confidence:** medium

### B6 — the repaired rule-24 ledger now contradicts its own conclusion: it says the audited zeros were right, then still convicts them under rule 17

- **Anchor:** `corpus/ops/platform-alignment.md:698-700` —
  > `Every seat verified **the arithmetic the document showed** instead of **the predicate the document`
  > `claimed**. That is rule 17 — *a count can be exactly right while the claim it supports is false* —`
  > `violated three times in one pass by auditors briefed on rule 17.`

- **Why it is false:** The diff rewrote `:683-689` to establish the opposite of what `:698-700`
  concludes:
  > `**All three were wrong about the SET — and right about the TOTAL** ... The true count is **29 +`
  > `` `Membership` + `Organization` = 31** — the number the corpus already had ``

  Rule 17's predicate is *"a count can be exactly right while **the claim it supports is false**."* Per
  the repaired text, the claim the count supported — *"31 of 135 schemas auto-filter by organization"* —
  is **TRUE**. So rule 17 was not violated here at all: what was wrong was the *derivation printed
  alongside* a true claim. `:698-700` survives the repair asserting a falsity the repair itself refutes,
  and the rule's headline at `:679` ("**An AUDITED ZERO can be wrong**") is no longer demonstrated by the
  example beneath it — the zeros were right about the claim they cleared. A seat reading this ledger to
  calibrate will conclude the "31" claim was found false. It was found true.
- **Confidence:** medium. Hesitated because this is a methodology ledger, not a platform fact — but it is
  the document the briefing sends every seat to read first, and it now teaches a refuted lesson about the
  exact number it was repaired over.

### B7 — "as the twins ... already said" now covers a claim neither twin makes

- **Anchor:** `corpus/services/hiring.md:31-33` —
  > `>    migration touches the `jobsimulation` schema at all**, and **in production** that schema **survives,`
  > `>    frozen, until M710** — while **no local dev/demo stack creates it at all** (`platform/repos.yml:17-19``
  > `>    `migrations: false`; qualified M257x iter-52) — as the twins [`service_taxonomy.md:52`](../architecture/service_taxonomy.md)`
  > `>    and [`dependency_map.md:78`](../architecture/dependency_map.md) already said.`

- **Why it is false:** The diff inserted the prod-vs-local split into a sentence whose closing clause
  attributes the whole compound to two named anchors. Neither anchor says it:
  - `corpus/architecture/service_taxonomy.md:52` — *"the `cms`, `jobsimulation` and `skillpath` schemas are
    legacy husks"*. No M710, no local/prod split.
  - `corpus/architecture/dependency_map.md:78` — *"(the legacy `jobsimulation` schema is
    non-authoritative)"*. No M710, no local/prod split.

  "Legacy husk" / "non-authoritative" is not "survives frozen in production until M710 and is absent
  from every local stack." The underlying local claim itself **is true** and I verified it independently
  (see clearances) — the defect is the attribution the correction widened.
- **Confidence:** low-medium. Hesitated because the "as the twins already said" clause pre-dates the
  diff; the correction widened its scope rather than creating it. Booked as a blocker per the briefing's
  under-booking rule.

---

## Minors

- `corpus/architecture/security_compliance.md:83` and `corpus/ops/platform-alignment.md:686` —
  *"`grep -c 'OrganizationMixin{}' schema/*.go` returns **30**"*. With a multi-file glob `grep -c` prints
  one `file:count` line per file (139 lines here), never the scalar `30`. The number is right; the recipe
  offered for reproducing it does not reproduce it. (`grep -l ... | wc -l` does.)
- `corpus/architecture/security_compliance.md:121-129` — the fence's own checked-in derivation still runs
  `grep -lE 'Organization(ID)?Mixin\{\}' *.go`, which matches the commented-out `user_resource.go:22` —
  the exact commented-code bug the new text at `:89-90` warns against, left un-annotated in the recipe
  three paragraphs below the warning. Result is unaffected (`user_resource.go` carries an `organization`
  *edge*, not an `"organization_id"` field, so it never entered the 18/23 anyway) — hence minor, not
  blocker. I re-ran the documented derivation verbatim: 135 / 7 / 18, all reproduce.
- `corpus/architecture/architecture_overview.md:248` and `corpus/architecture/security_compliance.md:202`
  both cite `external_services.md:569` for the four-live enumeration; the correction pushed
  *"**Four** things can send a request outside the EU"* down to **:570**, leaving `:569` on
  *"This list **previously said five** ..."*. The anchor was exact before the edit and is now off by one.
- `corpus/architecture/external_services.md:632` — *"`OPENAI_API_KEY` returns 2, both the studio
  subprocess remap."* One of the two hits (`app/internal/cms/studio/studioManager.go:1049`) is a doc
  comment describing the remap; only `:1067` is the remap.
- `corpus/services/ai-readiness.md:428-430` — the splice detached the imperative parenthetical from its
  antecedent. It now reads *"leaves the member **invisible** on an active cycle (write the real skill
  evidences + sim sessions + `ai_readiness_user_step_progresses`; reuse the existing verified-skill
  chain)"* — the instruction used to hang off *"not the evidences"* and now dangles off "invisible".
- `corpus/services/hiring.md:68-72` — the parenthetical opened at `:68` (*"(both **hard-error
  `"organization is not hiring"`**"*) now closes at the end of `:72`, after two complete inserted
  sentences and a full stop. Readable, but the parenthesis no longer parses as one.
- `corpus/architecture/architecture_overview.md:256` cites `:262-276` for the Azure/`flag_use_azure_us`
  branch. The branch is `internal/jobsimulation/ai/ai.go:263-277` (`:262` is `switch vendor {`, `:277` is
  the `return client, nil`). `security_compliance.md:200` has it right (`:263-277`);
  `external_services.md:554` has a third range (`:264-276`). Three ranges, one construct — untouched
  context, flagged for the sweep.
- `corpus/architecture/shared_libraries.md:145` (changed) now names the cms + jobsimulation husks as
  direct `colony/authn` importers. Repo-root `CLAUDE.md` still states *"Since the merges these libraries
  are imported by **app, sentinel, storage and messenger** only."* Stale in `CLAUDE.md`, not in the diff.

---

## Positively cleared (audited zeros)

**ENUMERATED** (predicate enumerated from source independently, cardinality compared):

1. **`31 = 29 + Membership + Organization`.** Enumerated all `Policy()` declarations in
   `internal/data/ent/schema/`: exactly **four** files declare one — `mixin.go:99` (`UserMixin`, owner
   filter), `mixin.go:126` (`OrganizationMixin`, org filter), `org_membership.go:172`
   (`rule.AllowCurrentOrgEdgesOrSkipRule()`), `organization.go:56` (`rule.FilterSameOrganizations()` at
   `:96`, and it uses neither mixin), `user.go:116` (user filter → correctly excluded). Enumerated the
   30 `OrganizationMixin{}` hits and filtered for comment prefixes: exactly one, `user_resource.go:22`.
   29 + 2 = 31. Every new anchor (`org_membership.go:172`, `organization.go:56`/`:96`, `user.go:116`,
   `user_resource.go:22`, `mixin.go:126`, `mixin.go:98`) opened and read in situ. **The corrected
   derivation is right; only `:68`/`:108` were left behind (B1).**
2. **`135 of 139`.** `ls internal/data/ent/schema/*.go` → 139; `grep -l 'ent.Schema' *.go` → 135;
   independently, entity type-files at `internal/data/ent/` root (excluding
   `_create/_delete/_query/_update/_custom` and the 11 utility files) → **135**. Two independent routes agree.
3. **7 `OrganizationIDMixin{}` / 18 neither-mixin.** Ran the document's own `comm`/`xargs` derivation
   verbatim → 18, and enumerated the mixin sets separately.
4. **"all 30 `*_AI_*_MODEL` lines pin `azure`."** Enumerated per file:
   `config_template.ini` 10, `development_config.ini` 10, `production_config.ini` 10 = **30**; every
   value's first comma-field is `azure`. There are exactly three `configs/*.ini`.
5. **`gen.py` never overrides the service selector.** Read `gen.py:30-58` in full: `load_config_section`
   picks `configs/{ENVIRONMENT}_config.ini`; `load_services_settings` env-overrides only the six
   `*_API_KEY`/`*_ENDPOINT` names at `:45-48`; the `*_MODEL` selector rows are never touched. `.env_example:4`
   sets `ENVIRONMENT=development` → `development_config.ini`, which pins `azure`. Claim holds.
6. **`services/ai.py` anchors.** `:1-2` the OpenAI/Anthropic imports, `:383` `return OpenAI(api_key=...)`,
   `:530` `return AzureOpenAI(`, `:664` `return Anthropic(api_key=...)`, `:627` `class AnthropicProvider`,
   `:704-724` `get_client` with `providers = {'openai':…, 'azure':…, 'anthropic':…}` at `:705-709`. All
   exact. `studio-room.md:36` (the AI-Providers table row) and `:261-266` (the API-keys block) also exact.
7. **Storage's compose edge.** `docker-compose.yml:213-217` is exactly `depends_on: redis:
   service_healthy / postgresql: service_healthy`. `.env_example` contains neither `DB_CONNECTION` nor
   `REDIS_ADDR`; `storage/go.mod` has no redis module (pgx is `// indirect`); zero Go references to either
   var in `storage`. `service_taxonomy.md:418` is exactly the `cms / jobsimulation / storage / roadrunner`
   profile row listing postgresql + redis. Every clause of the re-corrected row checks out.
8. **LiveKit agent names.** `calls/livekit.go:110` and `:120` bare `anthropos-agent`, `:115`
   `anthropos-agent-chain`, `:126` `fmt.Sprintf("anthropos-agent-%s", *location)`. `anthropos-agent-eu`
   returns 0 hits platform-wide. The added `-chain` clause is a genuine completion of the set.
9. **`flag_use_realtime_openai` selects no engine.** Exactly **one** read in `app`
   (`internal/jobsimulation/calls/livekit.go:133`), inside `CreateAgentDispatch`; `:131-135` is the flag
   read, `:140-144` is `agentName = "anthropos-agent"; agentEndpoint = "openai-hosted"`. It overrides the
   US agent name. Every clause of the rewritten PostHog bullet is exact.
10. **`voice_engine` nil default.** `internal/cms/directus/collections/jobsimulation.go:1594-1597` is
    exactly `func voiceEngineFromDirectus` / `if directusVoiceEngine == nil {` / `return
    simulation.SimulationVoiceEngineGptrealtime` / `}`. Independently confirmed the *client* applies the
    same default: `next-web-app/packages/graphql/src/hooks/aiSimulation/useGetSimulationFlagsAndFeatures.tsx:101-105`
    `?? SimulationVoiceEngine.Gptrealtime`, and the FE branches on `Elevenlabs`/`Livekitgptrealtime`/
    `Livekitchain` (`TestRunNetwork.tsx:225-230`, `AISimulationCallContainer.tsx:41-42`) — so "engine
    choice is the per-sequence field" is true, though the *server* branches only on `livekitchain`.
11. **`20260729133514.sql`.** 64 lines read in full. `:15-23` two id-remap `UPDATE`s, `:30-34` orphan
    `DELETE`, `:36-44` two orphan-nulling `UPDATE`s, `:58-61` the *"5. Drop the mirrors."* comment,
    `:62-63` both `DROP TABLE`s. **Zero `INSERT` statements** (`grep -cE '^\s*INSERT '` → 0; the single
    case-insensitive `insert` hit is the function name `on_insert_local_jobsimulation_sessions_...` at
    `:64` — checked, so the claim survives the obvious false positive). `intelligence.go:1700` is exactly
    `query := m.ent.JobSimulationSession.Query().`.
12. **"one of four required-and-undefaulted columns."** Enumerated the whole `job_simulation_sessions`
    DDL (`20260722104506.sql:2-27`): NOT NULL **without** DEFAULT = `owner_id` `:6`, `sim_id` `:7`,
    `sim_type` `:10`, `token` `:13` — exactly four, exactly the cited line numbers. Every other NOT NULL
    column (`:3,4,5,11,12,16,17,18`) carries a DEFAULT. `token` UNIQUE index at `:29`. The strongest
    correction in the diff.
13. **`HiringConfigSeeder` bypasses the RPC.** `rosetta-extensions/stack-seeding/seeders/hiring_config.go:99`
    is `n, err := c.CopyRowsIdempotent(ctx, "public", "organization_sim_invitation_links", cols, rows, "id")`.
    `app/internal/organization/siminvitationlink.go:62-63` is the `if !org.IsHiring {` /
    `return nil, fmt.Errorf("organization is not hiring")` guard inside
    `CreateOrganizationSimInvitationLink` (`:56`). The seeder never calls it. The refutation is correct.
14. **"no local dev/demo stack creates the `jobsimulation` schema."** `platform/repos.yml:17-19` is
    exactly the `jobsimulation` / `type: go` / `migrations: false` triple; `Makefile:14`
    `MIGRATION_REPOS := $(shell awk '/^  - name:/{name=$$3} /migrations: true/{print name}' ...)` → only
    `app`; `app/terraform/migrations/` contains exactly one `CREATE SCHEMA` (`auth`, in
    `20230817154747_supabase_baseline.sql:2`); the jobsimulation husk's `Dockerfile.dev` ENTRYPOINT is a
    bare `./application` with no boot migration. (One escape not mentioned by the corpus: `make migrate
    S=jobsimulation` bypasses `MIGRATION_REPOS` entirely and *would* create it via
    `jobsimulation/atlas.hcl:8` `search_path=jobsimulation` — a documented Makefile usage. Not booked;
    the claim is about what a stack bring-up does.)
15. **`askengine/registry.go:192`.** Exactly `// \`jobsimulation\` stays in rewriteSchemas until M710
    drops the (now-frozen,` — the M710 horizon, inside the comment block (`:183-193`) that says the
    `jobsimulation.*` names are aliases with `RealName: "job_simulation_sessions"` resolving to `public`.
    The new qualification is precisely right about what that anchor does and does not prove.
16. **`colony/authn` direct importers.** cms = **6** (`cmd/root.go`, `internal/auth/authManager.go`,
    `internal/graph/handler.go`, `simulation.resolvers.go`, `mutations.resolvers.go`,
    `internal/user/userManager.go`); jobsimulation = **8**. Also enumerated the complement:
    roadrunner / sentinel / storage / messenger = **0** each, app = 129. The "6 and 8" and the
    "only the skillpath usage is fully folded in" framing both hold.
17. **Roadrunner's two corrections.** `SubmissionPackage` (`internal/rpcsrv/rpc.go:43-57`) calls
    `CreateSubmission(ctx, "zip", nil, codePackagePtr, nil)` → a single POST to
    `%s/submissions?base64_encoded=true&wait=false` (`internal/runner/runner.go:62`) with an
    `additional_files` field, returning one token. Not a batch endpoint. And `CreateSubmission`
    enqueues exactly one `asynq.NewTask(tasks.RoadrunnerSubmissionResult, …)` per call (`:126-127`);
    the worker is `Concurrency: 10` on `roadrunner:default` (`worker/worker.go:24-28`,
    `worker/queues/queues.go:4`), consistent with the untouched § *Async tasks* at `roadrunner.md:97`.
18. **The ai-readiness `keepInCycleStep1` correction — every anchor exact.** `readiness.go:365`
    `cyc := m.activeCycle(...)`; `:387-390` is the `if cyc != nil { keepInCycleStep1 } else {
    keepStartedMembers }` fork; `:388` and `:390` land on the two calls. `:638-652` is
    `queryInCycleStep1Completers` through `.All(ctx)`, with `StepTypeEQ(enum.StepSkillMapping)`,
    `StatusEQ(enum.StepCompleted)`, `CompletedAtGTE(since)` at `:647-649` — all three conjuncts as
    claimed. `:662-664` is the `keepInCycleStep1` doc-comment + signature; `:684` is the
    `keepStartedMembers` signature. `steps.go:915-938` is `queryReadinessStarters` with the exact
    `SELECT DISTINCT user_id FROM public.ai_readiness_user_step_progresses … AND status <> 'not_started'`
    (`:920-925`); `steps.go:907-914` is the comment block containing *"This DB signal is the only real
    'has started' check"* at `:913`. `readiness.go:330` is `computeOrgBreakdowns`. This hunk is the
    cleanest in the diff.
19. **0 `bedrock|boto3` under `app/studio/`** (case-insensitive, all extensions). Positive control run in
    the same pass: 8 `.py` files match `openai`, so the pipeline was live.
20. **`external_services.md:541` self-reference** = the *Anthropic Direct (first-party API)* table row.
    Exact.

**RE-DERIVED** (arithmetic the document displays, recomputed — the weaker clearance):

- The four env-var grep counts (5 / 26 / 13 / 13), the three zeros, and `OPENAI_API_KEY` = 2. All
  reproduce exactly with `command grep`. Correct arithmetic over the wrong set — see **B2**.
- `30 + 7 + 18 = 55`; `18 − 2 = 16`; `16 + 7 = 23` (`security_compliance.md:67-100`). Internally
  consistent, and unaffected by the 30→29 repair because `user_resource.go` carries an `organization`
  edge rather than an `"organization_id"` field.

---

## What I could not settle

- **Whether `dependency_map.md:25`'s "Mistral" for Studio-Room or `studio-room.md:389`'s omission of it
  is the side that should move (B4).** Both are defensible readings of the same repo — `tools/pdf2md.py`
  really does call Mistral OCR, and it really is not on the `gen.py` path. Settled by a decision about
  whether the corpus's "Studio-Room" means the *repo* or the *generation pipeline*; the two touched files
  currently answer differently.
- **Whether the server ever branches on `voice_engine ∈ {elevenlabs, gptrealtime}`.** In `app` the only
  runtime consumers of the value are `replyCall.go:129` and `replyCallLivekit.go:26`, both testing for
  `livekitchain` only; the elevenlabs/gptrealtime discrimination I could locate is entirely client-side.
  The corrected sentences say "engine choice is per sequence, from the CMS `voice_engine` field" without
  saying *where* it is honoured, which is true but hides that the server ignores three of the four
  values. Not booked — it is a completeness gap in an otherwise-correct correction, and I could not rule
  out a consumer in `colony` or `proto`, which are not in the ground-truth clone set.
- **Whether the `platform/.env` block is meant as an exhaustive list at all** (B2's severity). Settled by
  whoever owns § *Configuration*: if illustrative, the fix is to say so and drop the Go-grep derivation;
  if audited, the fix is to derive it from `platform/.env_example` and list all 22 names.
