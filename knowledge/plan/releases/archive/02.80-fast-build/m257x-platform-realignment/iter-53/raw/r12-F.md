# Seat F — reading #12

Ground truth: all 13 clones verified at the briefing shas before reading
(`app 5ba17044`, `app/studio aeec036a`, `platform 2adcf714`, `next-web-app bb3313bc`,
`sentinel 88bc5592`, `storage 4ce8ece5`, `messenger fa47850d`, `cms ca50c817`,
`graphql-wundergraph 60c229f3`, `roadrunner 87d8d443`, `jobsimulation 462343b0`,
`studio-desk 14a5442a`, `ant-academy 9c3843cd`).

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/services/ant-academy.md` | 436 | yes |
| `corpus/services/studio-desk.md` | 435 | yes |
| `corpus/services/jobsimulation.md` | 229 | yes |
| `corpus/architecture/platform-migration-status.md` | 189 | yes |
| `corpus/services/skillpath.md` | 107 | yes |
| `corpus/architecture/frontend_architecture.md` | 105 | yes |

Search-pipeline controls run in-pass: every `grep`/`find` stderr read (two zsh globbing
rejections — `--include=*.js`, `--include=*.json` — caught and re-run quoted, so no
"0 matches" was booked from a failed command); the `SkillPathSessionService` absence
sweep was paired with a `JobSimulationService` positive control in the same invocation
(3 hits) before absence was accepted.

---

## Blockers

### B1 — `jobsimulation.md:126` cites a file path that does not exist, and contradicts five sibling corpus sites

- **Anchor:** `corpus/services/jobsimulation.md:126` — *"**when that field is nil the default is `gptrealtime`** (`cms/directus/collections/jobsimulation.go:1594-1597`), not ElevenLabs"*
- **Why it is false:** `stack-demo/cms/directus/collections/jobsimulation.go` does not exist (`sed` → *"No such file or directory"*). The file is at `cms/internal/directus/collections/jobsimulation.go` in the legacy repo, and — since cms-in-app — at **`app/internal/cms/directus/collections/jobsimulation.go`**. I opened both: lines 1594-1597 are identical in each and do say what the doc claims (`func voiceEngineFromDirectus(...)` / `if directusVoiceEngine == nil {` / `return simulation.SimulationVoiceEngineGptrealtime`). So the *content* verifies and only the **path** is wrong — which is exactly the "anchor resolves-but-doesn't" class the protocol's §5 rule 21 names.
  It is also a **corpus self-contradiction**: every other corpus reference to this same file uses the correct `app/internal/cms/...` form — `corpus/architecture/ai_architecture.md:53`, `:203`, `:232`, `:245` and `corpus/architecture/external_services.md:540`, `:586`. jobsimulation.md is the lone outlier, and it points at the *decommissioned* repo besides.
- **Confidence:** high

### B2 — `ant-academy.md:196` enumerates "the four bring-up patches" as the academy's *entire* demo-config; the tooling applies five

- **Anchor:** `corpus/services/ant-academy.md:196-198` — *"The academy's entire demo-config — the four bring-up patches (`academy-fs-published-fallback` + `-public` + `-chapter-body` + `ant-academy-dev-origins`) applied to the **ephemeral clone**, plus the `code/.env.local` overlay … — is written and applied by `demo-stack/ant-academy.sh` at bring-up."*
- **Why it is false:** `.agentspace/rosetta-extensions/demo-stack/ant-academy.sh` declares and applies a **fifth** clone patch — `ant-academy-back-to-cockpit` (M249):
  - `ant-academy.sh:85-90` — *"M249 (v2.7 'july jitter'): the ant-academy-back-to-cockpit demo-patch — a fail-closed 'Back to Cockpit' item in …"*, `COCKPIT_PATCH="$HERE/../stack-injection/apply-ant-academy-back-to-cockpit.sh"`
  - `ant-academy.sh:176-177` — `if [ -x "$COCKPIT_PATCH" ]; then / if "$COCKPIT_PATCH" apply "$ACADEMY_ROOT" …` — inside the same `reapply_clone_patches()` the doc is describing.
  - The patch directory confirms it: `demo-stack/patches/ant-academy-back-to-cockpit` exists alongside the four named.
  I re-derived the SET rather than the sum: enumerating `*_PATCH` variables actually invoked by `ant-academy.sh` gives **5** (`DEVORIGINS_PATCH`, `FSPUB_PATCH`, `FSPUBLIC_PATCH`, `FSBODY_PATCH`, `COCKPIT_PATCH`), not 4. `ant-academy.md` never mentions the cockpit patch anywhere in 436 lines (`grep -n "back-to-cockpit\|Back to Cockpit\|COCKPIT_URL"` → 0 hits), while its sibling `studio-desk.md:319-323` documents the studio-desk twin of the same M249 lane. The word *"entire"* makes this an enumeration claim, not a summary.
- **Confidence:** medium-high. Hesitated because the section is headed "(v2.6 M245)" and the fifth patch arrived at v2.7 — but the sentence is present-tense and claims completeness, and a reader auditing "what does the academy's demo-config consist of?" is handed a set that is 4/5.

### B3 — `ant-academy.md:137` states `emptyCatalogView()`'s return literal, and the literal is wrong

- **Anchor:** `corpus/services/ant-academy.md:136-137` — *"so on **any** failure the catalog becomes `emptyCatalogView() = { chapters: [], skillPaths: {}, series: [] }` → **0 cards**."*
- **Why it is false:** `stack-demo/ant-academy/code/src/lib/serverTenant.js:115-117`:
  ```js
  function emptyCatalogView() {
    return { chapters: [], skillPaths: {}, series: [], bundles: PUBLIC_BUNDLES, catalogVersion: CATALOG_VERSION }
  }
  ```
  Two keys are missing from the doc's literal, and one of them carries **content**: `PUBLIC_BUNDLES` is a populated array in `ucourses/catalog.js:956+` ("the curated narrative arcs that drive the For Beginners / For Business / For Engineers views … Each bundle renders as a 'stripe': a hero block (eyebrow + title + description + stats) followed by a horizontal scroller of its constituent path cards"). The code comment immediately above the function (`serverTenant.js:111-113`) exists specifically to flag this: *"`bundles` (PUBLIC_BUNDLES) carries no tenant metadata and is the one piece not yet modeled in the backend catalog, so it passes through verbatim."* The doc presents `emptyCatalogView()` as an equality, and this is the most load-bearing definition in the file — it is the root of the whole "empty grid" analysis the doc is built around.
- **Confidence:** medium. Hesitated because the downstream consequence (*"→ 0 cards"*) is very likely still true — with `skillPaths: {}` the bundle stripes have no path cards to scroll. But an operator debugging "the grid is empty yet I see bundle headings / a `catalogVersion`" is told by this doc that those fields cannot be present. Booked per the briefing's under-booking rule.

### B4 — `ant-academy.md` contradicts itself (and `CLAUDE.md`) on which env file the academy reads

- **Anchors:**
  - `corpus/services/ant-academy.md:324` — *"The **app's** env file is `code/.env`, not the repo root:"*, followed at `:327-328` by `cp .env.example .env`, and `:345` — *"set `REQUIRE_ORGANIZATION_MEMBERSHIP=0` in `code/.env`."*
  - `corpus/services/ant-academy.md:197` — *"plus the `code/.env.local` overlay (`REQUIRE_ORGANIZATION_MEMBERSHIP=0` + the **minted** Clerkenstein publishable key + `ACADEMY_DEMO_FS_PUBLISHED=1` …)"*, and `:319` — *"the flags live in the gitignored `code/.env.local`"*
  - `CLAUDE.md:255` (repo root, highest-propagation site) — *"**Ant Academy** requires its own `.env` file at `ant-academy/code/.env.local` (**not the repo root — the React app reads only from `code/.env.local`**)"*; and `CLAUDE.md:443` — `cp .env.example .env.local`.
- **Why it is false / contradictory:** the three passages cannot all be right. `CLAUDE.md`'s *"reads only from `code/.env.local`"* is flatly false — Next.js loads `.env`, `.env.local`, `.env.<NODE_ENV>` and `.env.<NODE_ENV>.local`, and the repo's own `.gitignore` ignores **both** `.env` and `.env.local`. Conversely, ant-academy.md's own §"durability fix" (`:210-212`) establishes that `ant-academy.sh` **truncating-writes `code/.env.local`** and is *"the single authoritative writer"* — and `.env.local` **overrides** `.env` in Next's precedence order. So an operator who follows §2 and puts credentials in `code/.env` on a machine that has ever run `/demo-up` has them silently shadowed, with no error. The repo ships `code/.env.example` (verified present) and the live clone carries `code/.env.local`, not `code/.env`.
- **Confidence:** medium. Hesitated because §2 is nominally the *dev* path and `code/.env` does work on a never-demoed box; booked because the same file asserts both forms within 130 lines, `CLAUDE.md` asserts a third ("only `.env.local`") that is false as written, and the precedence direction makes the disagreement consequential rather than cosmetic.

---

## Minors

- `corpus/services/ant-academy.md:246` — *"~26 Playwright e2e spec files (tests/e2e/)"*. Actual: **31** `*.spec.js` under `code/tests/e2e/` (40 files total in the dir).
- `corpus/services/ant-academy.md:147` — *"a separate, unauthenticated `code/public/catalog.json` (~2,667 entries)"*. Actual: `courses` array holds **2,715** entries (`json.load` → `len(d['courses'])`). A generated file; the `~` softens it.
- `corpus/services/jobsimulation.md:34` — *"The local re-point onto `app` is **M809**, not yet done — see `app/main.go:1196-1202`."* Those lines are the **cms-in-app M807** comment block (about `CMS_RPC_ADDR` and messenger's cms hop). The jobsim edge the sentence is about is `app/main.go:1190-1195`. The cited lines do mention "the M809 re-point", so the anchor is adjacent-but-off-construct.
- `corpus/services/jobsimulation.md:104` — cites `intelligence.go:1700` with no repo-relative path. The file is `app/internal/organization/intelligence.go`; line 1700 is correct (`query := m.ent.JobSimulationSession.Query().`).
- `corpus/architecture/platform-migration-status.md:51-53` — *"every service that has ever appeared in `docker-compose.yml` (same command on that file → 26 names …)"*. I reproduced the derivation exactly (26 names, and all five named pre-history entries — `nats`, `web-app`, `chromedp`, `simulator`, `realtime` — are in it). But one of the 26, **`app-network`**, is a *network*, not a service; the service cardinality is 25. Since the very next sentence says *"a name they return that has no row is a gap"*, the label over-states what the count ranges over.
- `corpus/architecture/frontend_architecture.md:39` — *"~15 sites hitting `NEXT_PUBLIC_BACKEND_API_URL`"*. Actual: 35 occurrences across 26 files; excluding the two `.env.example`s, one `.md` and two `.test.ts`, **21** source files reference it.
- `corpus/services/studio-desk.md:112` — *"Four model tiers (`thinking_slow`, `thinking_fast`, `fast`, `instant`) … Tier defaults: OpenAI/Azure `gpt-5.2` / `gpt-5-mini` / `gpt-5-nano`; Anthropic `claude-opus-4-5` / `claude-sonnet-4-5` / `claude-haiku-4-5`."* Three model names are offered for four declared tiers, inviting a wrong tier↔model mapping. `src/services/ai/config.ts:20-38`: OpenAI/Azure `thinking_slow=gpt-5.2`, `thinking_fast=gpt-5.2`, `fast=gpt-5-mini`, `instant=gpt-5-nano`; Anthropic `thinking_slow=claude-opus-4-5-20251101`, `thinking_fast=claude-sonnet-4-5-20250929`, `fast=claude-sonnet-4-5-20250929`, `instant=claude-haiku-4-5-20251001` (the real ids are date-suffixed).
- `corpus/services/skillpath.md:97` — *"the body renders the literal string **\"Coming soon\"**"*. It renders `t('enterprise.insights.comingSoon')`, a next-intl key. It resolves to exactly `"Coming soon"` in `configs/i18n/messages/en/enterprise.json` (verified by walking the JSON), so the rendered English output matches — but it is a translation key, not a literal, and in `it` it is `"Presto disponibile"`.

---

## Positively cleared (audited zeros)

**ENUMERATED** (predicate enumerated independently from source, cardinality compared before any arithmetic):

- **`ant-academy.md:250-267` — the complete `isPublic` matcher.** The table's stated purpose is completeness (*"Do not paraphrase this from memory"*). I extracted every entry from `code/proxy.js:112-188` and got **25 unconditional + 2 `DEV_LOGIN_ENABLED` + 3 `VISUAL_BYPASS` = 30**. Every one of the 30 appears in the doc's table, in the right group, and the table adds nothing that is not in the matcher. The two "what this does not mean" caveats also hold: `/api/chapters/*` is genuinely **not** in the matcher, and there is no `app/(public)/library/[slug]` route while `app/(authed)/courses/[slug]/page.jsx` exists.
- **`platform-migration-status.md:49-51` — the 14 historical `repos.yml` names.** Ran the document's own derivation (`git log -p --follow -- repos.yml`) on `stack-demo/platform` and enumerated the `+  - name:` set: exactly the 14 names listed, no more, no fewer, and all 14 have rows in §2.
- **`platform-migration-status.md` §2 evidence column, row by row.** Opened each cited artifact: `app/terraform/main.tf:44 =1`, `cms:39 =0`, `jobsimulation:40 =0`, `roadrunner:19 =1`, `sentinel:19 =1`, `storage:19 =1`, `messenger:19 =1`, `graphql-wundergraph/terraform/main.tf:20 =1` — all eight exact. Compose anchors `:5` (sentinel), `:18` (`search_path=sentinel`), `:28` (backend), `:83` (jobsimulation), `:144` (cms), `:189` (storage), `:220-222` (customerio-sync git context), `:238`, `:240`, `:281` (roadrunner), `:311`, `:344`, `:352`, `:371-372` (`gotenberg/gotenberg:8`) — all exact. `common.yml:2` = `postgresql:`, `:20` = `redis:` — exact, and the `include: - common.yml` at `docker-compose.yml:1-2` is real. All 12 resolvable cited platform commits verify with matching subjects **and dates** (`236771f` 2026-07-29 "fold jobsimulation + cms into backend (cms-in-app v8.0)", `045857c`/`fdfa189` 2026-04-17, `a2a3ee6` 2026-02-27, `84862d1` 2024-05-29, `8770fe6` 2023-05-04 vs first commit `cb6ebf5` 2023-04-30 = the claimed four days, `b56d731`/`360efd4` 2026-07-31, `21429b7` 2026-07-07, `a4db680` 2026-07-21); `915da06` is in the *router* repo, where it resolves to 2026-07-29 "fold cms subgraph into backend (supergraph 2→1)".
- **`platform-migration-status.md:60` — the four `app` wiring call sites.** This row explicitly records a prior correction ("An earlier revision attached `:604` alone to all four"), so I re-derived rather than trusted: `main.go:573` = `skillerManager := skiller.NewSkillerManager(...)`, `:604` = `jobsimDj, err := jobsimwiring.Wire(...)`, `:634` = `skillPathSessionManager := skillpath.NewSessionManager(...)`, `:1034` = `cmsManagers = appcms.Wire(appcms.Deps{`. All four distinct and correct. `app/internal/roadrunner/` genuinely does not exist (`ls` → No such file); `app/internal/jobsimulation/runner/` does; `internal/jobsimwiring/wiring.go:118` is exactly `runnerManager := jsrunner.NewRunnerManager(getenv("JUDGE0_API_KEY"), getenv("JUDGE0_BASE_URL"))`. `git describe` on `app` = `v1.363.2`, as cited.
- **`platform-migration-status.md:11,140-160` — the fence.** `.agentspace/rosetta-extensions/stack-core/platform_alignment_guard.py` exists and, run read-only against `stack-demo/platform/repos.yml`, exits **0**: *"platform-migration-status.md and repos.yml agree in both directions."* §1 declares exactly 7 states, matching assertion C's "seven". §3's "**9** are in `repos.yml`" matches the 9 entries I enumerated; none of the 19 census rows is in `repos.yml` (assertion E); and `livekit-agent-chain` is genuinely "one of **five**" (5 `livekit-agent*` rows listed).
- **The `running_but_unfederated` husk claim — the thing most likely to have been over-claimed by the merge narrative.** `docker-compose.yml` at `2adcf714` still defines `cms:` (`:144`, ports 8090/8091), `jobsimulation:` (`:83`, ports 8400/8401) and `roadrunner:` (`:281`, ports 10400/10401), **all three in the default `graphql` profile** (verified per-service via an `awk` pass over the profile lines), and `Makefile:10` sets `PROFILE ?= graphql`. So `make up` really does start them. `platform-migration-status.md:39-42` and `jobsimulation.md:11-14` both state this correctly and are the corpus's accurate sites for it. (Note for the record: the `CLAUDE.md` snapshot in my harness prompt asserts *"There is no cms / jobsimulation / … container, profile, port or subgraph"* — but the **on-disk** `CLAUDE.md` no longer says that, so there is no live contradiction to book.)
- **`skillpath.md:30-33` — "there is NO `SkillPathSessionService` anywhere … Measured: 0 occurrences."** Swept `--include=*.go` across `app sentinel storage messenger cms jobsimulation roadrunner` (vendor excluded): **0** hits, with `JobSimulationService` run in the same pass as a positive control (3 files) to prove the pipeline. `app/CLAUDE.md:72` and `app/knowledge/architecture.md:28` do both still list it in the RPC-mux enumeration — the Trap-C claim the doc makes is itself true.
- **`ant-academy.md:107` / `platform-migration-status.md:69` — "the supergraph is ONE subgraph".** `graphql-wundergraph/supergraph-config-prod.yaml` lists `backend` alone, `schemas/` contains `backend.graphqls` alone, `subgraphs.conf` is the single line `BACKEND=v1.360.0`.
- **`frontend_architecture.md:39,44` — the `:5050` retraction.** This is the class of claim the protocol says gets re-broken, so I enumerated: `graphql-wundergraph/config.prod.yaml:5` = `listen_addr: 0.0.0.0:8080`, `terraform/locals.tf:8` `port = 8080`, and `Makefile:10` = `docker run … -p 5050:8080`. The doc's *":5050 was only ever the deleted LOCAL compose host mapping, never a production port"* is exactly right.
- **`frontend_architecture.md` monorepo facts.** `apps/` = web·hiring·integration·maintenance·mobile (5, matching the 5 rows); dev ports 3000/3001/3002 read from each `package.json` `"dev"` script, mobile `--port 3031` from `dev:mobile`, maintenance has no dev script (doc says "—"); `!apps/mobile` is present in `pnpm-workspace.yaml`; `configs/i18n/messages/` holds exactly the **8** locales listed (de en es fr it ja nl pt); `packageManager: "pnpm@10.30.3"`, `engines.node: ">=24.0.0"`; `apps/web/package.json:46` is precisely `"next": "^16.2.7"`; `UPGRADE-IMPACT-next16.md` exists; the three `--filter` package names (`@anthropos/web-app`, `@anthropos/hiring-app`, `@anthropos/integration`) all match their `package.json` `name`s; `docker-compose.yml:362` is the `NEXT_PUBLIC_BACKEND_API_URL` line.
- **`jobsimulation.md:24-30` — the 23 tables and the `sessions` rename.** `grep -c '^CREATE TABLE'` on `20260722081626_jobsim_data_model.sql` = **23**, and I listed all 23 names rather than trusting the count. The next migration `20260722104506.sql` line **2** is `CREATE TABLE "job_simulation_sessions"` and line **79** is `DROP TABLE "sessions";` — both anchors exact.
- **`jobsimulation.md:101-106` / `skillpath.md:79-87` — the mirror-drop retraction, including its own "0 INSERTs" claim.** `20260729133514.sql` is 64 lines; `:15-23` are the two remap `UPDATE`s, `:36-44` the two orphan-NULLing `UPDATE`s, `:58-62` the *"5. Drop the mirrors."* block, `:62`/`:63` the two `DROP TABLE`s. On the INSERT claim I deliberately avoided the trap: a case-insensitive `grep -ci insert` returns **1** (matching `on_insert_local_jobsimulation_sessions_update_memberships()` on line 64), while `grep -n "INSERT INTO"` returns **0** — the doc's "0 INSERTs" is correct as a statement about statements. No `local_skill_path_session.go` exists under `app/internal/data/ent/schema/`.
- **`studio-desk.md` structural facts.** `vite.config.ts` `rollupOptions.input` holds exactly the 9 prod entries named plus the `isProduction`-gated `dev-accept`; `src/routes/` = ai.ts·dev.ts·skillpath.ts·youtube.ts with `skillpath.ts` at 61,080 bytes ("~61KB"); `engines.node >=24` + `node:24-alpine` in both Dockerfiles; `src/index.ts:60` `process.env.PORT || 9100` (the in-code 9100 fallback); `src/index.ts:96` `STUDIO_ACCESS_ROLES = ['admin','org:admin','content_creator','org:content_creator']` used by `checkEnterpriseAndAdmin`; `youtube.ts:43` is precisely `const apiKey = process.env.YOUTUBE_API_KEY;` with the `_mock: true` fallback below it, and `GCLOUD_SERVICE_ACCOUNT` sits at `.env.example:120`; 7 files in `app/public/l12n/` ("7 languages"); `app/services/studioDB.js` exists; `jest.config.js:26` has `projects:`; compose publishes both `9000:9000` and `9100:9100` and `depends_on: backend + cms` at `:337-341` with `VITE_GRAPHQL_ENDPOINT` at `:334`; `main.ts:97` is the bare `preloadCriticalCSS();` call and `:206` is `new PageWrapper();` with `await userService.canAccess()` at `:199` between them; `main.ts:105` is the sole tailscale-funnel comment (the GlitchTip DSN); the three prod-host ejects are real at `userProfile.js:148`, `:302` and `pageWrapper.js:149`; `canAccess()` does call `this.clerk?.user?.getOrganizationMemberships()` (`userService.ts:213`).
- **`jobsimulation.md:68,88,81,129,145` — misc.** `cmd/root.go:77-78` `cmp.Or(os.Getenv("PORT"),"8080")` / `RPC_PORT","8081"` with `EXPOSE 8080` in both Dockerfiles (the "repo defaults differ" claim); `hibiken/asynq v0.26.0` in `go.mod:24`; `internal/worker/worker.go:27` `Concurrency: 10` and `:104` `Concurrency: 25`; the four cobra subcommands are exactly `aggregate`·`clone-session`·`validate`·`test-command` (no `serve`, no `run`); `internal/calls/livekit.go:131-135` is the `flag_use_realtime_openai` read and `:140-144` is the block that sets `agentName = "anthropos-agent"` + `agentEndpoint = "openai-hosted"` — both anchors exact; `internal/graph/queries.resolvers.go:70` is `func (r *queryResolver) JobSimulationResult(...)`, the right construct; `internal/runner/runner.go`'s header does read *"formerly the standalone 'roadrunner' service"*; the `$HOME/.aws/credentials` bind is the file's only AWS bind, at `docker-compose.yml:142`, inside the jobsimulation block.
- **`skillpath.md` anchors.** `app/internal/skillpath/session.go:205-207` = the `// cms-in-app deseam` comment + `u.cms.GetSkillPathDomain(...)`; `app/internal/skillpaths/skillpaths.go:88-95` = the same deseam in `SkillPathManager.getSkillPath`; ent schemas `skill_path_session.go` + `skillpath_mixins.go` present; `internal/web/backend/graphql/graph/schemas/skillpath_sessions.graphqls` present; `intelligence.go:1144` is exactly `func (m IntelligenceManager) InsightsSkillPathByMemberships(...)` and `:1159-1170` is exactly the `m.ent.SkillPathSession.Query()` block with `SkillPathID` + `StatusIn(Active, Completed)` + the `TenantIDIsNil()/TenantID(organizationID)` tenant predicate. The next-web-app half also holds: the route `…/skill-paths/[skillPathId]/[userId]` exists, `InsightsBySkillPathStudentSimulationsContainer.tsx:31-34` has `userData` as a `useMemo` returning `null as unknown as MembershipEnriched` with the real query commented out, and the results table + totals block at `:135-159` are both commented out.
- **`ant-academy.md` read-chain and body-chain.** `getServerCatalogView()` at `serverTenant.js:143-147` is verbatim `const view = (await getBackendCatalogView(eids)) ?? emptyCatalogView()`; the *"the cutover is intentional, not reversible-on-error"* quote is at `:132`, inside the cited `115-145` range, and the surrounding docblock independently confirms *"there is NO FS-as-published fallback"*; `getPublicCatalogView` really is the empty-eid variant (`getBackendCatalogView(new Set())` at `:164`); `src/lib/serverChapterBody.js:51-67` is `resolveServerChapterBody(slug, locale)` → `getBackendChapterBody` → `maybeResolveDraftBody` → `{ notFound: true }`; `app/not-found.jsx` really does render `<h1>You wandered off the trail.</h1>`; `app/layout.jsx:132` is `manifest: "/academy-manifest.json"`; `RegisterServiceWorker.jsx` is a kill-switch, not a registrar, with the M1 provenance the doc gives; `"dev": "next dev --port 3077"`; `engines.node >= 22`; Cosmo is `ucourses/ucourse-engine/assistant/agent.js` `MODEL = 'gpt-5.2'` + `API_URL = 'https://api.openai.com/v1/responses'` + `localStorage.getItem('openai_api_key')`; `gpt-image-2` is the cover model in `.claude/skills/author-cover`; the org gate is fail-closed (`isOrgGateEnabled` treats anything but `0|false` as ON); `proxy.js:241` sets the `x-locale` header from `?lang=`; `src/i18n/LocaleSwitch.jsx` is a 2-way EN↔IT `<Link>` toggle with no `/[locale]` route anywhere; `tests/e2e/i18n-language-toggle.spec.js` exists; all four `academy-*` / `ant-academy-*` patch dirs named in the doc exist under `demo-stack/patches/`.

**RE-DERIVED only (weakest clearance — recorded as such):**

- `platform-migration-status.md:51-52`'s **26** compose-service names: I ran the document's own command shape and got 26, matching. But per §5 rule 24 I then checked the *predicate* rather than stopping at the match — and the set includes `app-network`, a network. Booked as a Minor above rather than as a clearance.

---

## What I could not settle

- **GitHub archive states.** `platform-migration-status.md` asserts archive dates for `jobsimulation` (2026-07-31), `graphql-wundergraph` (2026-07-30), `skiller` (2026-07-01), `skillpath` (2026-07-31), `intelligence` (2026-04-02), and *not*-archived for `cms`, `roadrunner`, `chronos`; `jobsimulation.md:13` repeats the jobsimulation one. Local clones carry no archive bit — settling these needs `gh api repos/anthropos-work/<name> --jq .archived`, which I did not run (no network use attempted). Same for §3's "**93 repositories** … 46 named by no corpus document" and every `last push` date in the census table.
- **`app` PR #1103 ("v9.0 support-in-app folds storage + messenger", `platform-migration-status.md:65,66,170`)** — a GitHub PR reference with no local artifact. `gh pr view 1103 --repo anthropos-work/app` would settle it.
- **`ant-academy.md:246` "1000+ Vitest tests."** I counted 244 `*.test.js*` files under `code/tests` and 300 files containing `describe(`/`it(`/`test(`, but the claim is about individual test *cases*, which needs an actual `vitest run --reporter=json` (an install + execution I did not perform). Directionally plausible; not verified.
- **`ant-academy.md:191-192` / `:232` "Proven live on `billion`" and `studio-desk.md:412` "demo-2 … p95 4669 ms → 817 ms"** — live-measurement claims against hosts not present in this environment. Only re-running `rext stack-verify/e2e/run-studio-fcp.sh` and the academy probes on those stacks would settle them.
- **`jobsimulation.md:88` PR `#395` / branch `feat/remove-chronos-and-realtime`, and commit `09631fb2`** — cited in the *jobsimulation* repo. The commit sha did not resolve in the clone I have (the clone is at `462343b0` and the sha may predate its fetch depth or live on a deleted branch); the asynq facts it is cited for (v0.26.0, in-process replacement) I verified independently and they hold.
- **`ant-academy.md:12,119` "since v0.5.1 (M7)" and the various `v0.5`/`M1`/`M2`/`M7`/`M8` milestone attributions** — internal to the ant-academy repo's own planning docs; the *behavior* each one attributes is verified in code, but the version attributions themselves are not checkable from source.
