# Seat A — reading #12

Ground-truth clones verified at the briefed shas before reading (all 13 matched:
`app 5ba17044`, `app/studio aeec036a`, `platform 2adcf714`, `next-web-app bb3313bc`,
`sentinel 88bc5592`, `storage 4ce8ece5`, `messenger fa47850d`, `cms ca50c817`,
`graphql-wundergraph 60c229f3`, `roadrunner 87d8d443`, `jobsimulation 462343b0`,
`studio-desk 14a5442a`, `ant-academy 9c3843cd`).

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| corpus/architecture/external_services.md | 831 | yes |
| corpus/architecture/security_compliance.md | 281 | yes |
| corpus/services/backend.md | 271 | yes |
| corpus/services/academy-backend.md | 141 | yes |
| corpus/services/coursebuilder.md | 139 | yes |
| corpus/services/skiller.md | 66 | yes |
| corpus/services/TEMPLATE.md | 46 | yes |

Protocol read: `corpus/ops/platform-alignment.md` §5 (lines 240–834) in full.

---

## Blockers

### B1 — `app` has no top-level `migrations/` directory; the corpus says it exists and names its contents

- **Anchor:** `corpus/services/backend.md:253` — *"Versioned Atlas migrations live in `terraform/migrations/`
  (per `atlas.hcl`: `dir = "file://terraform/migrations"`, source `ent://internal/data/ent/schema`), **not in
  the top-level `migrations/` dir (which holds only `atlas.sum`)**."*
- **Why it is false:** At `app @ 5ba17044` there is **no** `migrations/` directory at the repo root — not on
  disk (`ls migrations/` → *No such file or directory*; `find . -maxdepth 2 -type d -name migrations` returns
  only `./terraform/migrations`) and not in the index (`git ls-files "migrations/*"` → empty). It was deleted:
  `git log --diff-filter=D --name-only -- "migrations/*"` names `6a46e844` *"chore(migrations): remove obsolete
  atlas.sum file"* / `migrations/atlas.sum`. The surviving `atlas.sum` is
  `app/terraform/migrations/atlas.sum` (confirmed by `ls terraform/migrations/ | tail`). The rest of the
  sentence is correct — `app/atlas.hcl` does read `dir = "file://terraform/migrations"` and
  `src = "ent://internal/data/ent/schema"` — which is what makes the parenthetical hazardous: it reads as
  freshly verified. A reader hunting the migration-state file is sent to a path that does not exist.
- **Confidence:** high

### B2 — The corpus gives two incompatible production addresses for the re-pointed `SKILLER_RPC_ADDR`

- **Anchors (both in my file set, one file contradicting itself):**
  - `corpus/services/skiller.md:19` — *"`SKILLER_RPC_ADDR=http://backend:8083` locally; `skiller_rpc_addr =
    http://backend:8081` in production terraform."*
  - `corpus/services/backend.md:79` — *"…locally (all four occurrences in the merged `docker-compose.yml`),
    `http://backend:8081` in prod terraform."*
  - `corpus/services/backend.md:195` — *"In production terraform the re-pointed pair is at
    **`http://backend.internal.anthropos:8081`**."*
- **Why it is a finding:** These are the same claim about the same variable and they disagree on the hostname.
  The only production address evidence in any cloned repo supports the `.internal.anthropos` form:
  `graphql-wundergraph/supergraph-config-prod.yaml:6` routes to `http://backend.internal.anthropos:8080/graphql/query`
  (versus `supergraph-config-compose.yaml`'s docker-network `http://backend:8082/graphql/query`) — i.e. prod
  uses the ECS service-discovery namespace `internal.anthropos`, and a bare `backend` hostname is the
  docker-compose form. The three consumer terraform modules that carry the variable
  (`jobsimulation/terraform/variables.tf:163`, `cms/terraform/variables.tf:76`,
  `messenger/terraform/variables.tf:87`, plus the injection sites
  `jobsimulation/terraform/main.tf:120-121`, `cms/terraform/main.tf:106-107`,
  `messenger/terraform/main.tf:78-79`) declare it **with no default**, so the literal lives in the
  `anthropos-work/infrastructure` repo, which is not cloned — I cannot close it from source, only show the
  contradiction and which side the available evidence favours. `app/terraform/*.tf` contains no
  `skiller_rpc_addr` at all. The wrong form has propagated outside my scope too
  (`corpus/architecture/dependency_map.md:27` and repo-root `CLAUDE.md:189` both say `http://backend:8081`),
  which is §5 rule 19's cross-file drift.
- **Confidence:** medium (the contradiction is certain; which side is right is inferred, not measured)

### B3 — The Cosmo Router is listed in **public** subnets; its own terraform places it in private subnets, and the next line of the same doc says the opposite

- **Anchor:** `corpus/architecture/security_compliance.md:22` — *"**Public subnets**: Application Load Balancer
  (ALB), Cosmo Router"* — immediately followed by `:23` *"**Private subnets**: All microservices (no direct
  internet access)"*.
- **Why it is false:** `graphql-wundergraph/terraform/main.tf:31` passes
  `private_subnets_ids = var.platform_private_subnets_ids` and **that is the module's only subnet input** —
  `grep -n subnet terraform/*.tf` in that repo returns exactly two lines (`main.tf:31` and the variable
  declaration `variables.tf:52-54`); there is no public-subnet variable anywhere in the module. The placement
  is byte-for-byte the same shape as the services the corpus calls private:
  `sentinel/terraform/main.tf:26` and `app/terraform/main.tf:57` pass the identical
  `private_subnets_ids = var.platform_private_subnets_ids`. The router is deployed exactly like every other
  microservice, so it cannot be both the exception in `:22` and a member of "all microservices" in `:23`.
  (The ALB half of `:22` is fine; only the router is wrong.) I hesitated because the shared
  `infrastructure//modules/services/base_service` module is not cloned and I cannot see what it does with the
  ids — but nothing the router's own config supplies could put it in a public subnet, and it supplies strictly
  less than nothing extra relative to sentinel/app.
- **Confidence:** medium

---

## Minors

- `corpus/architecture/external_services.md:429` — anchors `graphql-wundergraph/CLAUDE.md:39` for the quote
  *"Since cms-in-app the platform compose `graphql` service builds from the **production** Dockerfile"*. That
  verbatim sentence is at **`CLAUDE.md:33`** (the build-path table row); `:39` is a bullet that restates the
  same claim (*"Since the compose stack builds from the production Dockerfile…"*). Anchor lands on supporting
  text, so no factual consequence — but it is not the quoted line. (The corpus's substantive point is correct:
  `platform` `git show 1e8e754:docker-compose.yml` lines 6–8 read `context: ..` / `dockerfile:
  graphql-wundergraph/Dockerfile.dev`, so the compose file did build from `Dockerfile.dev`, and the in-repo
  `CLAUDE.md` — written 2026-07-30 at `60c229f` — is the wrong one.)
- `corpus/architecture/external_services.md:665` — *"(`AI_PROVIDER_CHAIN`, default `azure-openai,openai`)"*.
  There is **no code default**: `studio-desk/src/services/ai/config.ts:124-125` returns `null` when the env is
  unset, and the legacy fallback at `:166-172` pushes azure, openai **and anthropic** (each if configured).
  Booked as a minor rather than a blocker because the string is verbatim the shipped template value —
  `studio-desk/.env.example:57` is literally `AI_PROVIDER_CHAIN=azure-openai,openai` — so "default" has a real
  referent for anyone who copies the example. Worth tightening to "the `.env.example` value" because the
  unset-path chain includes a US-terminating Anthropic arm, which matters in a doc whose neighbouring section
  enumerates EU-exit paths.
- `corpus/architecture/external_services.md:667` — *"`claude-sonnet-4-5` / `claude-opus-4-5` (Anthropic)"*. The
  shipped ids carry date suffixes: `studio-desk/src/services/ai/config.ts:35-38` pins
  `claude-opus-4-5-20251101`, `claude-sonnet-4-5-20250929`, `claude-haiku-4-5-20251001`.
- `corpus/services/coursebuilder.md:113-114` — *"~55 test files"* / *"~25 boundary test files"*. Measured at
  `5ba17044`: `internal/coursebuilder` has **61** `*_test.go` (of 102 `.go`); `internal/web/backend/coursebuilder`
  has **32** (of 47). The `~100 Go files` figure at `:30` is exact (102).
- `corpus/services/backend.md:39` — *"plus **`CMSService` only when the Directus edge is configured**
  (`if cmsRPCServer != nil`, `:1203-1205`)"*. The guard text is exact, but the branch is unreachable in the
  false direction: `app/main.go:971-973` `log.Fatalf`s when `DIRECTUS_BASE_ADDR` is empty, and `cmsRPCServer`
  is then assigned unconditionally at `:1075`. In practice `CMSService` is always registered. (The same doc set
  states the Fatalf correctly at `external_services.md:294`, so this is a phrasing gap, not a conflict.)
- `corpus/architecture/security_compliance.md:72` — anchors `mixin.go:98` for `UserMixin`'s `Policy()`; the
  function is at `mixin.go:99` (`:98` is its doc comment `// Policy defines the privacy policy of the
  UserMixin.`). The sibling anchor `mixin.go:126` for `OrganizationMixin.Policy()` is exact.
- `corpus/architecture/external_services.md:780` — anchors `app/internal/web/backend/backend.go:130` for
  *"Clerk user/org sync is app/backend's job"*. `:130` is `"/api/webhook/clerk",` inside the **authn-skip list**
  of `EchoAuthnMiddleware`, not the sync handler. It does prove `backend` serves the route, so the conclusion
  holds; the anchor names the wrong construct.

---

## Positively cleared (audited zeros)

**ENUMERATED** (predicate re-derived from source independently; cardinality compared):

1. **The multi-tenancy fence** (`security_compliance.md:66-135`) — the passage §5 rule 24 says has now been
   wrong four times and whose *"31"* three seats previously certified for the wrong reason. Re-derived the
   **set**, not the sum, in `app/internal/data/ent/schema/`: 139 `.go` files; 135 declare `ent.Schema`;
   `OrganizationMixin{}` matches **30** files of which **`user_resource.go:22` is commented out**
   (`// OrganizationMixin{},  // We need to work on this`) → **29 live**; `OrganizationIDMixin{}` in exactly
   the **7** named (`category`, `jobrole`, `similarity`, `skill`, `specialization`, `studio_document`,
   `studio_task`), and `skiller_mixins.go:148-160` confirms it is a plain nullable column that *"does NOT
   attach backend's OrganizationMixin privacy policy"*; the documented `comm`/`xargs` derivation returns
   **18**. I then tested the **conjunction** that failed before, per-file rather than per-count: of those 18,
   exactly **one** carries `UserMixin{}` (`academy_feedback.go`) and exactly **one** declares its own
   `Policy()` (`org_membership.go:172`) — I listed every mixin of all 18 to check this, and none of the
   remaining 16 carries either. The 16 named files match my derived set **exactly, name for name**. `Policy()`
   declarations in the whole directory: 5 functions in **4** files (`mixin.go:99` UserMixin, `mixin.go:126`
   OrganizationMixin, `org_membership.go:172`, `organization.go:56`, `user.go:116`) — the corpus's "only FOUR
   files" is right. `organization.go:96` is `rule.FilterSameOrganizations()`; `org_membership.go:172-188` ends
   in `privacy.AlwaysDenyRule()`. **31 = 29 + Membership + Organization** and **23 = 16 + 7** both hold on the
   enumerated sets. Also confirmed the doc's own caveat: `user_resource.go` contains no `"organization_id"`
   literal (it carries an `edge.From("organization", …)`), so the commented-out mixin does not silently move
   the 18.
2. **The subgraph count history** (`external_services.md:362-375`, `backend.md:16-20`, `:80-91`) — read
   `supergraph-config-prod.yaml` at each cited commit: `749dc86^` = 5 (backend, skiller, jobsimulation, cms,
   skillpath); `749dc86` = 4; `7c17e63` = 3; `915da06` = 1. `git show --name-status 915da06` marks **both**
   `schemas/cms.graphqls` and `schemas/jobsimulation.graphqls` `D` in one commit, and its subject really does
   say *"supergraph 2→1"* against a 3-entry tree. Present state: `schemas/` holds `backend.graphqls` alone;
   `subgraphs.conf` is `BACKEND=v1.360.0`. **cms-in-app was the 3→1 step** and the jobsimulation subgraph did
   outlive jobsim-in-app.
3. **"No subscriptions"** (`external_services.md:376`) — both halves. `grep -rn "sse\|subscription"
   graphql-wundergraph/*.yaml` returns rc=1 with stderr read (positive control `grep -rln backend …/*.yaml`
   matches all three supergraph configs). `git log -S 'protocol: "ws"'` on HEAD's history returns **0** and the
   `sse_post` control returns **5**; `bba862f` exists, `git merge-base --is-ancestor bba862f HEAD` → rc **1**,
   and `git branch -a --contains` names only `remotes/origin/feat/use-web-socket`. `915da06^`'s
   jobsimulation entry still reads `protocol: "sse_post"`. `schemas/backend.graphqls` has `type Mutation` at
   `:4053`, `type Query` at `:4912`, no `type Subscription`, and exactly **6** `Subscription` substring hits,
   all Stripe/plan field names.
4. **The `graphql` profile starts nine containers** (`external_services.md:168-174`) — enumerated from
   `platform/docker-compose.yml @ 2adcf714`: six services carry `graphql` in `profiles:` (backend `:81`,
   jobsimulation `:140`, cms `:187`, storage `:218`, roadrunner `:309`, gotenberg `:384`); `sentinel` has **no**
   `profiles:` key; `common.yml` adds profile-less `postgresql` + `redis`. 6 + 1 + 2 = **9**. There is no
   `graphql` service and no `directus` service; `repos.yml` has no `graphql-wundergraph` entry.
5. **All 30 `*_AI_*_MODEL` lines in `app/studio/configs/` pin `azure`** (`external_services.md:541`, `:600-605`,
   `security_compliance.md:205`) — listed every one across the three `.ini` files: 30 lines, 30 `azure`.
   `gen.py:41-53` overrides only `*_API_KEY`/`*_ENDPOINT`, never the service selector. `bedrock|boto3` under
   `app/studio/` returns **0** with an `anthropic` positive control returning 34. So the *four live + one
   latent* count is right and the *five* it retracts was wrong.
6. **The env-var occurrence counts** (`external_services.md:630-635`) — re-ran the document's own
   `command grep -rho '\b<NAME>\b' --include='*.go'` in `app @ 5ba17044`: OPENAI_KEY **5**,
   ANTHROPIC_API_KEY **26**, AZURE_OPENAI_KEY **13**, AZURE_OPENAI_ENDPOINT_URL **13**; OPENAI_ORG_ID,
   AZURE_OPENAI_ENDPOINT, AZURE_OPENAI_DEPLOYMENT all **0**; OPENAI_API_KEY **2**.
7. **The taxonomy figures** (`external_services.md:649-662`, `backend.md:9`/`:36`/`:69-76`, `skiller.md:40-49`)
   — opened `.agentspace/snapshots/taxonomy/5afc0bccf1df7ef538b643321fc6362f/manifest.json`: `public_only:
   True`, `predicate: org-null`, `captured_at 2026-06-29`, `skills 42790`, `job_roles 22470`,
   `skill_embeddings 42790`, `job_role_embeddings 18919`, `categories 23`, `specializations 1447`. Every
   quoted figure matches, including the "18,919 is the embedding row count, transcribed onto the role count"
   diagnosis.
8. **13 `ai_readiness_*` tables** (`backend.md:183-185`) — 13 ent schema files **and** 13 `CREATE TABLE
   "ai_readiness_*"` statements in `terraform/migrations/`, and the named four omissions
   (`recommendations`, `email_overrides`, `notification_logs`, `notification_optouts`) leave exactly the
   original 9. `internal/aireadiness/` exists with `compare.go`/`csv.go`/`cycles.go`/`diagnosis.go`;
   `internal/workforce/` has no `readi*` file; `ai_readiness.graphqls` and the
   `ai_readiness_refresh` Asynq task both exist.
9. **`SKILLER_RPC_ADDR=http://backend:8083` at all four compose occurrences** (`backend.md:79`) — grep returns
   exactly 4 (`:62`, `:121`, `:174`, `:265`), all `http://backend:8083`.
10. **Eight translation languages** (`skiller.md:54-55`) — `validTranslationLanguages` in
    `skiller_mixins.go:264-273` enumerates exactly 8 (english, italian, spanish, french, german, dutch,
    japanese, portuguese).
11. **The 30-symbol certificate alphabet** (`academy-backend.md:70-72`) — `certIDRegex`
    `^AAS-\d{4}-[A-HJKMNP-TV-Z2-9]{6}$` (`certificate.go:42`) expands to exactly 30 symbols; `crypto/rand` at
    `:5`, `certIDPrefix = "AAS"` at `:31`.
12. **Every Ent LABEL→TABLE pluralization in `academy-backend.md:56-69`** — all 11 `CREATE TABLE "academy_*"`
    statements in `terraform/migrations/` match the plural names the doc lists
    (`academy_chapter_progresses`, `academy_last_activities`, `academy_chapter_times`,
    `academy_certificates`, `academy_bookmarks`, `academy_feedbacks`, `academy_series`,
    `academy_skill_paths`, `academy_chapters`, `academy_chapter_bodies`, `academy_path_embeddings`), against
    11 singular schema files. `paid` default true and nullable `tenant_eid` are at
    `academy_skill_path.go:70`/`:78`; `index.Fields("user_id","chapter_slug").Unique()` at
    `academy_chapter_progress.go:87-88`; the `de/es/fr/it/nl/pt` overlay set at `academy_chapter_body.go:19`.
13. **`app`'s `internal/` directory map** (`backend.md:121-177`) — checked all 39 claimed subdirectories exist;
    **0 missing**. `go.mod` says `go 1.26.4`. `internal/academy/academy.go:6-9` does state the
    `internal/aiacademy` / `aiacademy_courses` removal.
14. **The five unconditional RPC handlers** (`backend.md:39`) — enumerated the `mux.Handle` calls in
    `main.go`: users `:1178`, organizations `:1179`, skiller `:1187`, jobsimulation `:1195`, labsession
    `:1219`; CMS guarded at `:1203-1205`. `SkillPathSessionService`: **0** Go occurrences (positive control
    `JobSimulationService` → 7); no `skillpath…v1connect` import (**0**). And the corpus's own caution
    verifies: `app/CLAUDE.md:72` and `app/knowledge/architecture.md:28` both still list it — exact anchors.
15. **The six Redis-stream subscribers** (`backend.md:218-220`) — `main.go` has exactly six `AddSubscriber`
    calls (`:1265` skillpath, `:1267` skiller, `:1276` jobsimulation, `:1294` cms, `:1296` ai_usage,
    `:1311` `serviceName` = `backend`), plus `.AddHandler` merges at `:1279`/`:1287`. This makes the corpus's
    "five application streams **including `backend`**" correct and `app/CLAUDE.md`'s four-stream list the
    stale one.
16. **The Course Builder constants** (`coursebuilder.md:36`, `:97-102`) — `terraform/migrations/20260717151144.sql`
    creates `course_builder_sessions` + `credit_transactions` + `organization_credits` (all three, nothing
    else); `DefaultSessionsPerOrgPerDay = 50` (`rate.go:28`); monthly COGS default **500** and daily default
    **0 = off** (`budget.go:30-53`); credit costs `course.build 5`, `course.refine 1`, `course.translate 1`
    (`credits/cost.go:87-91`) with units derived per chapter (`handler.go:518`
    `buildMaxChapters(depth) * perChapterUnits()`), so *"5/chapter"* is right; worker concurrency **20**
    (`main.go:172`); `gpt-image-2` default (`imagegen/openai.go:22`); the 7 author-target locales
    (it/fr/de/es/nl/ja/pt) at `handler.go:619-622`; `COURSEBUILDER_OPENAI_IMAGE_KEY` has **0** Go occurrences,
    was removed in `68c24512` (3 hits in that diff) and survives in exactly the two markdown files the doc
    names (`GO-LIVE-RUNBOOK.md`, `README.md`); `main.go:816-819` reads `OPENAI_KEY` + `CB_IMAGE_MODEL`.
    All 15 named engine files and all 5 named in-repo docs exist.

**Anchors opened and confirmed to name the cited construct** (not a set enumeration, so listed separately —
each was read with `sed -n 'N-15,N+5p'` context per §5 rule 10):
`graphql-wundergraph/terraform/main.tf:20` (`service_desired_count = 1`), `locals.tf:8` (`port = 8080`),
`main.tf:48-49` (containerPort/hostPort), `config.prod.yaml:5`; `Dockerfile.dev:18` (one schema COPY), `:19-20`
(the comment left by `915da06`), `:23` (one awk), `:29` (`router:0.275.0`), `:7` (`wgc@0.104.0`);
`platform/docker-compose.yml` `:43-67` (backend env, zero `DIRECTUS_*`), `:164-165` (cms's explicit pair),
`:255-265` (messenger's split four — two at `backend:8083`, `CMS_RPC_ADDR=http://cms:8091`,
`JOBSIMULATION_RPC_ADDR=http://jobsimulation:8401`), `:318`/`:334`/`:352`/`:361` (all four frontend endpoints
`:8082/graphql/query`); `git show 2c85211^` (no `dockerfile:` key) vs `2c85211` vs `1e8e754:6-8`;
`b56d731:docker-compose.yml` (`graphql:` still at `:22` behind `wundergraph-deprecated`) vs `360efd4`
(`backend:` at `:28`, no `graphql`); `63d285c` (service named `wundergraph`, git-url context, `"5050:8080"`);
`67ba772` (context `../graphql-wundergraph` → `..`);
`app/main.go:971-973` (`log.Fatalf` on empty `DIRECTUS_BASE_ADDR`), `:1075` (`cmsRPCServer` assignment),
`:1196-1202` (the verbatim *"additive + DORMANT … until the M809 re-point"*), `:735-738` (`LABS_API_URL`),
`:756-762` (`coursebuilder.ModelBackendName()` log), `:1234` (60 s RPC write timeout);
`app/cms_reader_switch.go` (*"a DIRECT domain call — no proto round-trip … and no internal traffic to a
standalone cms"*, verbatim);
`internal/jobsimulation/ai/ai.go:30-33`, `:80`, `:85-88` (Bedrock `eu-west-1`), `:129-141`, `:150-155`, `:166`,
`:259-289`, `:264-276`, `:280-283`, `:325`;
`internal/cms/directus/collections/jobsimulation.go:905` (`AIVendor *AIVendor`), `:1302-1305`
(`aiVendor := simulation.Openai`), `:1596`/`:1609` (nil voice engine → `gptrealtime`);
`internal/jobsimulation/simulator/ai/ai.go:58-59`, `:114-115`;
`internal/jobsimulation/calls/livekit.go:110`, `:115`, `:120`, `:126` (and `anthropos-agent-eu` returns **0**
tree-wide with an `anthropos-agent-chain` positive control), `:131-144` (`flag_use_realtime_openai` selects an
*endpoint*, not an engine);
`internal/coursebuilder/bedrock.go:23`, `:29`, `:98-104`, `:106-113`; `askengine/bedrock.go:25`;
`internal/cms/studio/markdownManager.go:19` + `studioManager.go:583` (the only two Mistral sites in `app`);
`app/terraform/variables.tf:635-638` + `ssm.tf:328-333` + `main.tf:555` (the prod `ANTHROPIC_API_KEY`);
`skills.go:53-62` + `:75`, `criterion.go:127`/`:142`/`:168`/`:428`/`:450-475`,
`templates/checkValidationBulk.tmpl:27` (verbatim *"meets or does not meet"*),
`v3/validator/validator.go:60-62` (one engine registered) and `checkerEngines` stored-never-read;
`next-web-app/apps/web/src/hooks/useOrgSelection.tsx:94`, `useResolveActiveOrg.tsx:107`,
`useActivateMembershipOrg.tsx:81` (all three exact, and the `apps/hiring` twins exist);
`internal/web/backend/backend.go:315` (Apollo Sandbox on `/graphql`), `content.go:23`
(`/content/catalog.json`), `content_admin.go:35` (`ACADEMY_CONTENT_API_TOKEN`);
`app/studio/services/ai.py:383`, `:626-664`, `:704-724`, `configs/config_template.ini:30-31`,
`gen.py:45-47`; per-app Clerk SDKs (`@clerk/nextjs` in web/hiring/integration + ant-academy,
`@clerk/clerk-expo` in mobile, `@clerk/clerk-js` + `@clerk/express` in studio-desk).
`NEXT_PUBLIC_GRAPHQL_ENDPOINT` returns **0** in `next-web-app` with a `NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT`
positive control. `academy-backend.md:16`'s `0e37771f` resolves to *"Academy backend v1.0 "ground truth"…(#903)"*
dated 2026-06-05; `app` HEAD is tagged **v1.363.2**, matching `academy-backend.md:20` and `coursebuilder.md:130`.

**`corpus/services/TEMPLATE.md`** — a bare authoring skeleton with no factual assertions; nothing to audit.
Clean.

---

## What I could not settle

- **The production literal for `SKILLER_RPC_ADDR` / `BACKEND_USERS_RPC_ADDR`** (B2). All three consumer
  terraform modules declare the variable with **no default**; the value is assigned in
  `anthropos-work/infrastructure`, which is not among the 13 ground-truth clones. `grep -rn skiller_rpc_addr
  --include='*.tf'` across `stack-demo` finds only declarations and injections. **What would settle it:** the
  `infrastructure` repo's module invocation, or `aws ecs describe-task-definition` for the messenger/cms/
  jobsimulation prod tasks.
- **`external_services.md:3` — "the repo is ARCHIVED on GitHub (2026-07-30)"**. Repository archive state is a
  GitHub API property, not visible in the clone; `60c229f` (2026-07-30) is the last commit, which is consistent
  but not proof. **What would settle it:** `gh repo view anthropos-work/graphql-wundergraph --json isArchived`.
- **`backend.md:70-72`'s prod row counts** (`public.skills` org-NULL = 42,790 / 43,584 total / 794 org-private;
  `job_roles` org-NULL = 22,490; `skill_embeddings` = 43,584, measured 2026-07-08). The 2026-06-29 snapshot
  manifest corroborates the *public* figures (42,790 / 22,470 / 42,790 skill-embeddings) but says **42,790**
  skill-embeddings where the doc's later prod read says **43,584** — the doc frames the gap as taxonomy drift,
  which is plausible but unverifiable offline. **What would settle it:** a read-only `select count(*)` against
  prod. Flagging only because the doc uses 43,584 for both `skills` total and `skill_embeddings`, which is the
  transcription pattern it elsewhere retracts for the 18,919 figure.
- **`security_compliance.md:261`** — "DPA v1.4 with 18 approved sub-processors", the 90-day deletion window,
  the 6-hour backup cadence, TLS/KMS specifics, and the `:264-272` sub-processor list. These are contractual /
  infrastructure facts with no representation in any cloned repo. **What would settle it:** the DPA document
  and the `db-backup` service's schedule config.
- **`external_services.md:685`'s claim that ElevenLabs credentials are "still wired for the call/reply
  pipeline"** — the compose does set `ELEVENLABS_TEMPLATE_AGENT_ID` / `ELEVENLABS_EU_TEMPLATE_AGENT_ID` on the
  `jobsimulation` husk (`docker-compose.yml:107-108`) but **not** on `backend`, which is where the merged
  engine now runs. I did not chase whether `backend` picks them up from the shared `env_file: .env` (the same
  mechanism `external_services.md:133-136` documents for `DIRECTUS_*`). Likely fine; not measured.
