# Seat E — reading #19

Corpus: `rosetta` @ `00be1ac`, tree clean (only my own untracked `iter-97/` dir).
Ground-truth clones re-derived at open — all matched the addendum table
(`platform 0c91421d`, `app b948604f` / origin `2035f9a4` (93 behind), `next-web-app bb3313bc`,
`storage 4ce8ece5` / origin `9f8cb532`, `messenger fa47850d`, `ant-academy 9c3843cd`,
`jobsimulation 462343b0`, `cms ca50c817`, `sentinel 88bc5592`, `studio-desk 14a5442a`,
`roadrunner 87d8d443`, `graphql-wundergraph 60c229f3`, `app/studio` + `cms/studio` `aeec036a`).

> ⚠️ **One ground-truth ref MOVED during this reading** (§5 rule 26). The
> `rosetta-extensions` **authoring** copy was `6130bfd8` at my first probe and
> `bc2ee74` ("fix(stack-core): anchor_construct_guard resolves NESTED repos at their own ref",
> 2026-08-06 00:56 +0200) minutes later. All rext citations below are graded at **`bc2ee74`**.
> No platform/app/next-web ref moved.

## Positive controls

| file | `wc -l` | read in full? |
|---|---|---|
| `corpus/architecture/service_taxonomy.md` | 484 | yes |
| `corpus/services/hiring.md` | 433 | yes |
| `corpus/services/graphql-wundergraph.md` | 279 | yes |
| `corpus/services/storage.md` | 232 | yes |
| `corpus/services/clerk-integration.md` | 128 | yes |
| `corpus/architecture/frontend_architecture.md` | 105 | yes |

(`storage.md` and `clerk-integration.md` render one line past their `wc -l` — no trailing
newline on the last line. Noted, not booked.)

Search-instrument controls run in the same passes, per the addendum:
- nested-repo enumeration done first (`find stack-demo -name .git -maxdepth 4` → **15** trees,
  including `stack-demo/app/studio` and `stack-demo/cms/studio`); the one tree-wide absence I
  relied on (`mistral` in the studio pipeline) was measured **at the nested repo's own ref**
  (`git -C app/studio grep -i mistral aeec036a` → 22 hits / 3 files), not at the host ref.
- every "0" claim I cleared was paired with a positive control in the same command
  (`custody` 0 vs `prevent_destroy` 5 in `storage@9f8cb53`; `SET "score"` 0 vs `DROP TABLE` 11
  across the app migration set; intercepting-route dirs 0 vs `@tabs` 46 in `next-web-app`).
- NUL hole named: `next-web-app/apps/web/src/hooks/useCoursebuilder.ts` (50,433 bytes) is
  invisible to `grep -I`/`git grep`; read with `grep -a` — it does **not** name
  `NEXT_PUBLIC_BACKEND_API_URL`, so it is correctly outside the count in
  `frontend_architecture.md:39`.

---

## Blockers

### B1 — `hiring.md:38` cites the wrong `service_taxonomy.md` line as its supporting twin
- **Anchor:** `corpus/services/hiring.md:38` —
  *"— as the twins [`service_taxonomy.md:52`](../architecture/service_taxonomy.md) and
  [`dependency_map.md:78`](../architecture/dependency_map.md) already said."*
  The claim being supported is bullet 2 at `:30`: **"Everything `app` writes is in `public`"**.
- **Why it is false:** `corpus/architecture/service_taxonomy.md:52` reads
  *"> [`dependency_map.md`](./dependency_map.md)'s content-generation flow, which had it right all
  along."* — it is the closing line of the **mermaid generation-edge** correction blockquote
  (`Desk → Backend → Room`), and says nothing about schemas. The statement hiring.md is invoking
  lives at **`service_taxonomy.md:62`** (*"**Database**: PostgreSQL — **one schema, `public`, owned
  by `app`**…"*). The sibling anchor is fine: `dependency_map.md:78` genuinely reads
  *"…or directly to the **`public`** schema (the legacy `jobsimulation` schema is
  non-authoritative)."*
  Class: §5 rule 34 — an intra-corpus citation invalidated by an edit that moved the file's line
  map; the anchor resolves and reads plausibly, which is why it survives.
- **Confidence:** high

### B2 — `graphql-wundergraph.md:86` self-citation `:174-176` points at the wrong construct
- **Anchor:** `corpus/services/graphql-wundergraph.md:86` — *"The host-`5050` mapping was published
  here in the present tense while `:174-176` of this same doc already said `localhost:5050` refuses
  the connection; corrected M257x iter-46"*
- **Why it is false:** `:174-176` of the same file is the **Dependencies** bullet about compose
  offsets (*"`NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT` — each also baked as a build arg, at `:119` and
  `:151`) … they were `:220`/`:236` at `0dab54d` and `:334`/`:352` at `2adcf71` — so grade the
  construct, not the offset"*). The sentence it names — *"`http://localhost:5050` refuses the
  connection."* — is at **`:192`**. The cited text is nowhere in `:174-176`.
  This is the doc's *own* self-consistency argument, so a reader checking the retraction lands on
  an unrelated paragraph and cannot verify it. Same class as B1.
- **Confidence:** high

### B3 — `graphql-wundergraph.md:133` self-citation `:84` points at the Ports bullet
- **Anchor:** `corpus/services/graphql-wundergraph.md:133` — *"So for its **final five months**
  compose built `Dockerfile.dev` — which is why a subgraph SDL change rebuilt the router, as `:84`
  describes."*
- **Why it is false:** `:84` is the **Ports** bullet — *"* **Ports**: **8080 → 8080** (router
  `listen_addr 0.0.0.0:8080`, `graphql_path /graphql`). **There is no `5050` at platform HEAD**…"*.
  It says nothing about rebuilds. The claim being pointed at ("a subgraph SDL change rebuilt the
  router, because the build context is the parent dir") is at **`:115-116`** (and restated at
  `:122`). Second stale self-anchor in the same document; both were plainly correct before an
  edit shifted the line map.
- **Confidence:** high

### B4 — `service_taxonomy.md` asserts GitHub archive dates for three repos while the same table declares archive state unknowable
- **Anchors (all in `corpus/architecture/service_taxonomy.md`):**
  - `:128` — Skiller: *"repo legacy/decommissioned, **ARCHIVED 2026-07-01**"*
  - `:129` — Skillpath: *"repo legacy, **ARCHIVED 2026-07-31**"*
  - `:3` and `:365` — graphql-wundergraph: *"the repo is **ARCHIVED on GitHub (2026-07-30)**"* /
    *"the GitHub repo was **archived 2026-07-30**"*
  - contradicted by `:130` — Jobsimulation: *"**repo archive state: report both, assert neither** —
    this cell asserted a GitHub archive on 2026-07-31, but `origin/main` carries four commits dated
    2026-08-04 … **archive state is not visible to this corpus (it lives in the GitHub org API, not
    a clone)**."*
- **Why it is a finding:** the Jobsimulation cell states a **general** epistemic rule about archive
  state and applies it only to its own row. Three sibling claims — one of them the file's opening
  banner, the highest-propagation position in the document — assert exactly the class of fact the
  same table says the corpus cannot see. I verified what *can* be verified: the jobsimulation
  refutation reproduces exactly (`HEAD..origin/main` = 4 commits, all `2026-08-04`, incl. merged
  PR #439 `caf36c96`); `graphql-wundergraph` is at `60c229f3` dated 2026-07-30 with origin/main
  level, so its date is at least not self-refuting; `skiller` and `skillpath` are **in no clone
  set at all**, so those two are unsupportable by construction. This is the exact
  "corrected at one site and left standing at another" class the briefing flags as the dominant
  repair-induced shape.
- **Why I hesitated:** the three archive dates may well be true as external observations; the
  defect is that the document simultaneously asserts them and rules them unknowable. Booked as a
  BLOCKER per the "contradicts another corpus claim" clause and the doubt-resolution rule.
- **Confidence:** medium

---

## Minors

- `storage.md:73-75` — *"app's two boot guards (`main.go:518-523` … `:529-535` `verifyBucketAccess`)
  … (`app/env_guards.go:37-44`)"*. This prose block names **no ref**, and by the briefing's default
  it grades at the checkout `b948604`, where **`env_guards.go` does not exist and `verifyBucketAccess`
  has 0 hits**. All three anchors resolve **exactly** at `app` origin/main `2035f9a`
  (`:516/:517` the two `os.Getenv`, `:518-523` the empty-bucket `log.Fatalf`, `:529-535`
  `verifyBucketAccess`, `env_guards.go:37-44` = the whole `deployedEnvironment()` func). Same for
  `storage.md:44`'s *"the live code is `app/internal/storage/`"* — that directory does not exist at
  `b948604`. The `:23` table cell does declare the refs; the surrounding blocks do not inherit it
  (§5 rule 33).
- `hiring.md:80` — `organization/manager.go:448` and `:485`. Both are **exact at the doc's banner
  ref `5ba17044`** (`:448` = `switch org.IsHiring {`, `:485` = `if !org.IsHiring {`). At the
  checkout `b948604` `:448` is a **blank line** (switch at `:450`) and `:485` is a closing brace
  (the hard-error moved to `:536-537`); at origin/main `2035f9a` `:448` is blank and the guard has
  left the function entirely. `siminvitationlink.go:62` is correct at all three refs.
- `hiring.md:211` — `persona_write.go:69-71,143-167`. At rext `bc2ee74` the three
  `validation_*` mappings are at **`:66-68`** (`:69-71` is the "the old jobsimulation.sessions step
  is REMOVED" comment) and the actual write steps are at **`:92-94`**; `:143-167` covers
  `sessionCols()` + `attemptResultCols()` only, stopping short of `skillResultCols` (`:169`). The
  substance is true (verified at `:92-94`); the offsets have drifted.
- `frontend_architecture.md:39` — *"**29 of them across 21 non-test files**"* describes *"direct
  REST/SSE calls"*, and *"the four `packages/core-js` REST clients … **12 sites** between them"*.
  Under the predicate **"occurrences of `NEXT_PUBLIC_BACKEND_API_URL` in non-test source"** the
  derivation reproduces **exactly** — 12 in the four core-js clients (3 each: comment, `const
  BACKEND_URL`, error string) + 17 elsewhere = **29 across 21 files**. Under the predicate the
  sentence actually names (REST/SSE *calls*) it under-counts by a third: at `bb3313bc` those four
  clients alone make **25** `await fetch(` calls (coursebuilder 18, credits 3, talkToData 3,
  workforce 1) → ≥42 total. Right number, loose noun. I considered booking this a BLOCKER
  ("wrong counts") and did not, because the derivation is exactly reproducible and the material
  claim (*"GraphQL only is the wrong mental model"*) holds; flagging the numbers here so it can be
  re-graded.
- `service_taxonomy.md:131` — *"`cms/terraform/main.tf:39` still declares `module "cms"` at
  `service_desired_count = 0` in an intact 191-line module"*. `:39` is
  `service_desired_count = 0`; `module "cms" {` is at **`:30`**. And 191 is the **file's** line
  count, not the module block's. (Both facts are true; the anchor names one of the two clauses.)
- `service_taxonomy.md:130` — *"the prod ECS service is **DESTROYED**"* asserts *applied*
  infrastructure state from a **code deletion** (`6092c6d2` removes the `module "jobsimulation"`
  block), two rows above a cell that explicitly says infrastructure-side deletion "is **not
  visible to this corpus**". The asymmetry is defensible (jobsim's block *is* gone from `main`,
  cms's is not), but "DESTROYED" over-reads what a clone can show.
- `hiring.md:145` — *"M223's hiring story simply sets `role_mix ≈ 0.1 admin / 0.9 candidate`"*.
  The shipped preset is `admin: 0.14 / member: 0.0 / candidate: 0.86` since rext `5d0297e`
  ("M226 iter-04: fix the hiring org count → exactly 5 admin + 45 candidate"). Historically
  accurate as a statement about M223; stale as a description of the preset. **The dependent claim
  at `:338` ("5 admins + 45 candidates") is CORRECT** — the 0.14 band is 7 slots minus the two
  candidate cockpit heroes = 5 net admins.
- `graphql-wundergraph.md:176` — the historical pair *"`:220`/`:236` at `0dab54d`"* mixes an
  **environment** entry (studio-desk `:220`) with a **build arg** (next-web `:236`), where the HEAD
  pair `:135`/`:160` are both environment entries. Both cited lines exist and carry the named
  variable; no factual consequence (the doc itself says "grade the construct, not the offset").

---

## Positively cleared (audited zeros)

**ENUMERATED** (predicate re-derived from source independently, cardinality compared before any
arithmetic):

- **`service_taxonomy.md:65-68, :453-460, :477`** — compose topology at `0c91421`. Independently
  enumerated `docker-compose.yml`: **5** declared services (`sentinel`, `backend`, `studio-desk`,
  `next-web-app`, `gotenberg`) + `include: common.yml`'s `postgresql`/`redis` = **7** effective;
  services carrying a `profiles:` key = **4**; the always-on floor (no `profiles:` key) =
  `{postgresql, redis, sentinel}` = **3**; `core` = floor + `backend` + `gotenberg` = **5
  containers**. Legal profile token set enumerated from the four `profiles:` arrays =
  `{core, backend, all, studio-desk, frontend}` = **5**. Every figure matches.
- **`service_taxonomy.md:479` / `clerk-integration.md:51,91`** — the `authn` claim. Enumerated per
  Go clone at its **own ref**: `anthropos-work/authn` in `go.mod` = **0** for all seven
  (app, sentinel, storage, messenger, cms, jobsimulation, roadrunner); control `colony` = **1**
  for all seven. And `colony/authn` importers in `app`: `git grep -l 'colony/authn' <ref> -- '*.go'`
  = **129** at *every* ref tried (b948604 / 2035f9a / 9d00a313 / 5ba17044) — the corpus's exact
  figure — vs **0** in sentinel/storage/messenger.
- **`service_taxonomy.md:377` + `graphql-wundergraph.md:41-53`** — the subgraph ladder. Re-derived
  the **set** at each commit by reading `supergraph-config-prod.yaml` at that tree, not by trusting
  the deltas: `749dc86^` = 5 {backend, skiller, jobsimulation, cms, skillpath}; `749dc86`
  (2026-06-24) = 4; `7c17e63` (2026-07-21) = 3; `915da06` (2026-07-29) = 1, and
  `git show --stat 915da06` marks **both** `schemas/cms.graphqls` and
  `schemas/jobsimulation.graphqls` deleted in that one commit. The intermediate
  `508ea37` ("supergraph 3->2") is **not an ancestor of HEAD** — it was squashed into `915da06`,
  which is exactly why the corpus's "3 → 1, not 2 → 1" reading is right and the commit's own
  subject line is wrong.
- **`clerk-integration.md:37`** — "Webhooks (svix, **12** event types)". Enumerated the switch in
  `app/internal/clerk/events/events.go` at `b948604`: `user.{created,updated,deleted}`,
  `organization.{created,deleted,updated}`, `organizationInvitation.{accepted,created,revoked}`,
  `organizationMembership.{created,deleted,updated}` = **12**.
- **`clerk-integration.md:41` / `frontend_architecture.md:35`** — 8 locales. Enumerated
  `@clerk/localizations` imports in `apps/web/src/providers/Clerk.provider.tsx` =
  {deDE,enUS,esES,frFR,itIT,jaJP,nlNL,ptPT} = **8**; `configs/i18n/messages/` = de,en,es,fr,it,ja,
  nl,pt = **8**.
- **`hiring.md:232-234`** — the four required-and-undefaulted columns. Enumerated every column of
  `CREATE TABLE "job_simulation_sessions"` (`20260722104506.sql:2-27`) and partitioned by
  NOT NULL ∧ no DEFAULT → exactly `{owner_id :6, sim_id :7, sim_type :10, token :13}` = **4**;
  every other NOT NULL column carries a DEFAULT. `token` UNIQUE confirmed at `:29`.
- **`hiring.md:227-229`** — the closed 5-value `completion_status` enum. Enumerated
  `internal/data/ent/enum/jobsimulation.go`: const block `:29-35` = pending/passed/failed/
  discarded/timedout, `Values()` at `:37-43` — **5**, no `SIMULATION…` member.
- **`hiring.md:74-77`** — the resolver gate. Enumerated: `OrgFeatureInsights` in
  `resolver_queries.go` = **8**; `JobSimulationSession` in `internal/organization/intelligence.go`
  = **44**; `grep -in hiring` over `intelligence.go` = **0** and over `resolver_queries.go` = 4
  hits, all sim-TYPE filters (`:811, :843, :991, :992`). Exactly as written.
- **`hiring.md:404`** — "the regression enumerates **all 8** generic seeders". Enumerated the table
  in `stack-seeding/seeders/hiring_scope_test.go` (`TestGenericActivitySeeders_SkipHiringOrg`):
  jobsim-sessions, activity, skillpath-sessions, assignments, hero-activity, persona, feedback,
  succession = **8**. Both M228 additions (`feedback.go:104`, `succession.go:81`) do consult
  `skipGenericActivityForHiringOrg`.
- **`hiring.md:40` / `:329`** — "the only `CREATE SCHEMA` in the entire migration set is `auth`".
  Enumerated across `app/terraform/migrations/`: exactly **1** hit,
  `20230817154747_supabase_baseline.sql:2`. Control in the same pass: `DROP TABLE` = 11.
- **`storage.md:29`** — the `STORAGE_RPC_ADDR` split. Enumerated at both refs:
  at `2035f9a` = **3** hits, all comments (`main.go:504`, `internal/jobsimwiring/wiring.go:101`,
  `internal/storagens/callsites_test.go:189`) — the exact three the doc names; ref-less at the
  checkout `b948604` = **15** hits of which **7** are live env reads
  (`cmd/academy-asset-upload/main.go:129`, `cmd/academyImport/main.go:231`, `cmd/import/main.go:50`,
  `internal/jobsimwiring/wiring.go:115`, `main.go:446`, `:524`, `:992`). Both figures exact.
- **`storage.md:35`** — "at the demo's pinned build ref `b948604` the same guard still reports a
  mid-fold with **six** read sites". Re-derived under the guard's *own* regex
  (`_GO_GETENV = Getenv\(\s*["'](…_RPC_ADDR)["']\s*\)`, which does not match the lowercase
  `getenv(` at `wiring.go:115`) → **6**. The corpus is right; note the **tooling's** docstring
  (`platform_predicate_guard.py::app_rpc_reads`) says *"three app read sites"* at that same ref and
  is the one that disagrees. Routed as an observation, not booked (out of corpus scope).
- **`graphql-wundergraph.md:59-69`** — the subscriptions retraction. `schemas/` holds
  `backend.graphqls` alone; **0** `type Subscription` in it (control: 7 `Subscription` substring
  hits, all Stripe/plan — `activeSubscription`, `PlanSubscription`, `stripeSubscriptionId`,
  `stripeSubscriptionItemId`); the `subscription: protocol: "sse_post"` block existed on the
  `jobsimulation` entry alone (verified at `7c17e63`) and died with it; `bba862f` is contained by
  **only** `remotes/origin/feat/use-web-socket`, never mainline.
- **`graphql-wundergraph.md:179`** — the historical `depends_on`. Enumerated at `b56d731^` (the
  last tree that had the service): exactly `{backend, jobsimulation, cms, storage}` — the corpus's
  four, in order, including the non-subgraph `storage`.
- **`graphql-wundergraph.md:180`** — `ci/update-subgraph.sh:9` is the file's **only**
  `gh release download` (count = 1) and targets `-R anthropos-work/app`.
- **`frontend_architecture.md:11`** — `repos.yml` entry counts: **4** at `0c91421`
  (app, sentinel, next-web-app, studio-desk), **9** at `2adcf71` (+cms, jobsimulation, storage,
  messenger, roadrunner), **6** at `0dab54d` (of which 4 Go) — the last also clears
  `hiring.md:35/:179/:328` and `clerk-integration.md:51`.
- **`service_taxonomy.md:209`** — the Studio-Room provider registry. `services/ai.py:705-708` is the
  whole `providers` dict {openai, azure, anthropic}; `ai.py:1-2` imports only `openai`/`anthropic`;
  and the Mistral surface measured **at the nested repo's own ref** (`app/studio` @ `aeec036a`) =
  **22 hits / 3 files** — `requirements.txt` 1, `tools/pdf2md.py` 18 (import at `:24`),
  `tools/r3.py` 3 — exactly the corpus's figure, and OCR-only.
- **`hiring.md:24`** — "`SET "score"` has **0** hits across the entire migration set" — 0, with the
  `DROP TABLE`=11 control in the same command; and `20260729133514.sql:58-62` is verbatim
  *"-- 5. Drop the mirrors."* … `DROP TABLE "local_jobsimulation_sessions";`.
- **`hiring.md:411`** — "zero `(.)`/`(..)`-prefixed dirs exist anywhere in `apps/`": **0**
  (control: 46 `@tabs` parallel-route paths in the same pass).
- **`storage.md:25`** — the prod side at `storage@9f8cb53`: `terraform/main.tf` is **18** lines with
  **no** module block; the quoted header is verbatim at `:9-11`; `:13` is the `prevent_destroy`-is-
  read-from-configuration clause; `:18` is verbatim *"See outputs.tf — consumers should reference
  these by output, never by literal name."*; *custody* = **0** occurrences (control
  `prevent_destroy` = 5); `storage.tf:22-25` carries the M903 passage verbatim; **no `moved` block
  exists**; `d3e6d32` (2026-08-05) says *"M903 never ran."*; and at the checked-out `4ce8ece5` the
  header still reads *"relocate the assets out of this module BEFORE M907"* with `main.tf` at
  **100** lines still declaring the ECS module. Every clause reproduces.
- **`storage.md:226,191`** — **0** `*_test.go` files in the repo; `Dockerfile:18` is
  `RUN go test -v ./...`; **0** `//go:generate`; gqlgen present only as an `// indirect` require.

**RE-DERIVED** (I recomputed arithmetic the document itself displays — the weakest clearance):

- `graphql-wundergraph.md:15-17` — "the **five** that exist … (this list read *"the eight"* until
  `838d907`)": 5 + the three retired tokens (`storage-legacy`, `customerio-sync`, `messenger`) = 8.
- `clerk-integration.md:103` — "thirteen minor versions apart": `~2.6.18` vs `~2.19.36`.
  Both pins verified exactly at `apps/mobile/package.json:6` and `ant-academy/mobile/package.json:18`;
  all four `@clerk/nextjs` pins verified `^6.39.2` (incl. `ant-academy/code/package.json:52`);
  `app/go.mod:31` @ `5ba17044` = `clerk-sdk-go/v2 v2.7.0` (same at `b948604`).

**Anchors opened and confirmed to name the claimed construct** (spot list; ~70 checked, all exact
unless booked above): `docker-compose.yml` `:18 :48 :53 :73-79 :82 :83 :100 :112 :119 :135 :141
:151 :152 :160 :161`; `Makefile:10`; `repos.yml:14-17,:18-20`; `graphql-wundergraph/terraform/
main.tf:20`, `locals.tf:8`, `main.tf:48-49`, `config.prod.yaml:5`; `jobsimulation/terraform/
main.tf:15-40` @ `6092c6d2`; app `main.go:504/516/517/524/525/547/1048/1102`,
`internal/storage/storage.go:193-200`, `internal/storage/service.go:22,:24`,
`internal/cms/studio/studioManager.go:119`, `cms_queries.graphqls:106`,
`gqlauthz.go:186` + both named tests, `resolver_queries.go:1034/1035/1053/1080/1085/1089`,
`resolver_cms_queries.go:95,99-103,210,258,295`, `siminvitationlink.go:62`,
`intelligence.go:885-886/1700/1738-1751/1820/1844/1846`, `ent/schema/job_simulation_session.go:39,:45`,
`ent/schema/anticheat_result.go:24`, `ent/jobsimulationsession.go:181-186`,
`graph.go:129392-129400` **and** `:129546-129554` (both marshal passthroughs, verbatim),
`jobsimulations.graphqls:14,:128`, `20260722104506.sql:2/6/7/10/12/13/29/53/79`,
`20260722081626_jobsim_data_model.sql:336/355/376`, `atlas.hcl:8`, `askengine/registry.go:192`;
next-web `UserStatusContext.tsx:141-173,144-149,168-172` (web) and `:125,144-145` (hiring),
`useGetClerkOrganization.tsx:16-18,:20-21`, `useNavbarSections.tsx:459-466,:460`,
`template.tsx:90`, `FreeTrialContainer.tsx:29`, `insights.ts:31-82`,
`InsightsByMembersContainer.tsx:359`, `simulationScoreColumn.tsx:95-97` (and the two copies are
byte-identical — same MD5), `metabase/route.ts` `orgRole !== 'admin'`; ant-academy
`graphql/server.js:14,:18`, `backendContent.js:36,102-103`, `serverTenant.js:115-145`,
`beacon/route.js:36,41-55`, `next-scaffold.test.js:106,111`, `react-compiler-config.test.js:41`,
`layout.jsx:132`, `academy-manifest.json` `display: standalone`; studio-desk
`.env.example:45`, `STUDIO_ACCESS_ROLES` = `['admin','org:admin','content_creator',
'org:content_creator']`; rext `persona_write.go:91,:152,:152-158`, `hiring_config.go:99`,
`hiringComparableFloor = 6`, `RENDER_GATE_FLOOR`, `RoleMix{Admin,Member,Candidate,AdminEmails}`,
`render-hiring-comparison.spec.ts`, both hiring demopatches; platform `README.md:81-87`;
`app/studio/CLAUDE.md:12-14`; `studio-desk.md:20,:21`; `frontend_architecture.md:11`;
`dependency_map.md:78`.

---

## What I could not settle

- **Taxonomy figures** (`service_taxonomy.md:145`: ≥42,790 skills / ≥22,470 job roles, "public
  subset, measured 2026-06-29"). Requires a prod-DB read; not derivable from any clone. The
  *negative* half (18K refuted / 60K unverified) is a pointer to `shared_libraries.md`, outside my
  file set.
- **Snapshot-derived counts** in `hiring.md:153-158`: "87 real `SIMULATION_TYPE_HIRING` sims" and
  "0 `job_position` rows (the prod '443' was never captured)". These are properties of the
  `.agentspace` snapshot cache, not of any repo. Settled only by `stacksnap status` against a
  populated cache.
- **GitHub archive state** for `skiller` and `skillpath` (B4). Neither repo is in any clone set;
  archive state lives in the GitHub org API. The corpus's own `:130` says so.
- **`storage.md:25`'s plan-directory citation** —
  `app/knowledge/plan/releases/09.00-support-in-app/m903-s3-custody/` and its `overview.md` /
  `progress.md` front-matter. **I did not open it**: HARD BAR 1 bans reading anything under
  `knowledge/plan/**` and I read the ban literally rather than scoping it to `rosetta`'s own tree.
  Everything else in that cell reproduced, including the `d3e6d32` message that says *"M903 never
  ran."*, so the surrounding claim is corroborated from a second source.
- **Prod infrastructure application state** for the M810 teardowns (`service_taxonomy.md:130-132`,
  `storage.md:161`). The `infrastructure` repo has never been in a clone set. `roadrunner`,
  `storage` and `messenger` all read `service_desired_count = 1` at their **checked-out** refs
  (`roadrunner/terraform/main.tf:19`, `storage/…:19`, `messenger/…:19`) and only `messenger`
  reaches `= 0` at origin/main (`:29`) while `storage`'s block is deleted outright at origin/main;
  I could not test whether any `terraform apply` has run. Noted because the root `CLAUDE.md`
  (outside my file set) cites `storage/terraform/main.tf:38` `service_desired_count = 0`, which
  resolves at **neither** storage ref — the checkout has `= 1` at `:19` in a 100-line file, and
  origin/main's file is 18 lines with no such key at all. Routed, not booked (not my file).
