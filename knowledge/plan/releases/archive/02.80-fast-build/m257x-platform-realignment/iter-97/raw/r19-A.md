# Seat A — reading #19

Corpus under audit: `rosetta` @ `00be1ac` (tree clean apart from this iter dir).
Ground truth re-derived at open, matching the ADDENDUM sheet exactly:

```
stack-demo/ant-academy           9c3843cd   stack-demo/next-web-app          bb3313bc
stack-demo/app                   b948604f   stack-demo/platform              0c91421d
stack-demo/app/studio            aeec036a   stack-demo/roadrunner            87d8d443
stack-demo/cms                   ca50c817   stack-demo/rosetta-extensions    ab81527a  (consumption clone)
stack-demo/cms/studio            aeec036a   stack-demo/sentinel              88bc5592
stack-demo/graphql-wundergraph   60c229f3   stack-demo/storage               4ce8ece5
stack-demo/jobsimulation         462343b0   stack-demo/studio-desk           14a5442a
stack-demo/messenger             fa47850d   .agentspace/rosetta-extensions   6130bfd8  (AUTHORING — the witness)
app origin/main = 2035f9a4
```

Search-set discipline: the nested repos `stack-demo/app/studio` and `stack-demo/cms/studio` were enumerated
with `find stack-demo -name .git -maxdepth 4` and grepped **at their own ref** (`aeec036a`) for every
studio-related predicate. `next-web-app/apps/web/src/hooks/useCoursebuilder.ts` was measured for NUL bytes
(`LC_ALL=C tr -dc '\000' | wc -c` → non-zero) and read separately with `grep -a` for the one absence claim that
ranges over it. `.gitignore`-hidden tracked files (`studio-desk/.env.example`, `next-web-app/apps/*/.env.example`)
were reached with `git grep`, not bare `grep`, and bare `grep` was verified to miss them.

## Positive controls

| file | `wc -l` | read in full? |
|---|---|---|
| `corpus/architecture/external_services.md` | 880 | yes |
| `corpus/services/cms.md` | 311 | yes |
| `corpus/services/jobsimulation.md` | 309 | yes |
| `corpus/services/ai-labs.md` | 156 | yes |
| `corpus/services/academy-backend.md` | 147 | yes |
| `corpus/services/skiller.md` | 69 | yes |
| `corpus/services/TEMPLATE.md` | 46 | yes |
| (protocol read, not assigned) `corpus/ops/platform-alignment.md` §5 | 2178 total | §5 in full (`:249-1408`) |

---

## Blockers

### B1 — "`pdf2md.py` … a standalone CLI **nothing dispatches**" is false; `tools/r3.py` dispatches it, and the corpus's own cited command shows it

- **Anchors (two sites, one claim):**
  - `corpus/architecture/external_services.md:565` — *"and `from mistralai import Mistral` at `app/studio/tools/pdf2md.py:24` (`mistral-ocr-latest`), a standalone PDF→markdown utility **nothing dispatches** (`git -C app/studio grep -i mistral aeec036a`)"*
  - `corpus/services/cms.md:223` — *"a standalone CLI **nothing dispatches** (`git -C app/studio grep -i mistral aeec036a` → 22 hits / 3 files …)"*
- **Why it is false:** `anthropos-studio-room` @ `aeec036a` (nested repo, grepped at its own ref) contains
  `tools/r3.py`, whose "Robust Ready RAG" pipeline **execs `pdf2md.py` as STEP 2**:
  `tools/r3.py:139` (`scripts = ["any2pdf.py", "pdf2md.py", "md2cleanMd.py"]`), `:189` (`# STEP 2: Convert PDF
  to Markdown with pdf2md.py`), `:190` (`pdf2md_script = get_script_path("pdf2md.py")`), `:199-201`
  (`step2_cmd = [str(pdf2md_script), "--input", …]`), `:206` (`run_step("PDF TO MARKDOWN", step2_cmd, env)`).
  It even gates on the key first (`:192-195`, *"MISTRAL_API_KEY environment variable not set. Required for PDF
  to Markdown conversion."*). **The very command both passages cite as their derivation returns those
  `tools/r3.py` lines** — the evidence refutes the sentence it is attached to.
- **It also contradicts the corpus:** `cms.md:95` states the correct thing 128 lines above its own twin —
  *"one leg of the `tools/r3.py` offline PDF→markdown chain, that nothing **on the generation path** calls"*.
  `external_services.md:599` likewise gets it right (*"a standalone CLI, **off the generation pipeline**"*).
  So this is the classic *corrected at one site, left standing at another* class: the correct, narrower
  predicate is *"nothing on the generation path calls it"*; two sites publish the absolute *"nothing
  dispatches"*.
- **What is unaffected:** the material residency/OCR conclusion (Mistral is OCR-only, never generation) is
  correct at every site; `r3.py` is itself dispatched by nothing (`git grep 'r3\.py' aeec036a` → 3 self-hits).
- **Confidence:** high.

### B2 — Five intra-corpus `see :N` self-citations resolve to the wrong construct

- **Anchor:** `corpus/services/cms.md:211` — *"Consistent with the **Data** bullet, **:28-31** above"*. Lines
  28-31 are the `CMS_RPC_ADDR` / M809 paragraph; the **Data** bullet is at `:44-47`.
- **The other four sites, same class:**
  | site | text | where it actually is |
  |---|---|---|
  | `corpus/services/cms.md:233` | *"see the **Studio** bullet, **:52-53** in the banner"* | `:52-53` is the "M809 re-point had already landed" prose; the **Studio** bullet is `:70-71` |
  | `corpus/services/jobsimulation.md:95` | *"consistent with the **Data** bullet, **:31-38** above"* | `:31-38` is the *Domain / engine* bullet; **Data** is `:39-45` |
  | `corpus/architecture/external_services.md:870` | *"Consistent with **:522** above, where the same correction is already recorded"* | `:522` is `// Types in app/__generated__/` inside a Studio-Desk code block; the `docker compose restart graphql` correction is at `:546` |
  | `corpus/services/academy-backend.md:15` | *"(Consistent with **:74-76** below.)"* | `:74-76` is the Certificate-minting bullet; the WunderGraph-endpoint/one-subgraph passage is `:80-83` |
- **Why it is a blocker rather than cosmetic:** each is an *anchor that resolves but names the wrong
  construct* — §5 rule 21's second class, and the briefing lists *"wrong line anchors that point at the wrong
  construct"* under BLOCKER explicitly. Three of the four host files (`external_services.md` +41,
  `cms.md` +10, `jobsimulation.md` +7) were edited by `00be1ac`, the repair immediately before this reading,
  so these are almost certainly repair-induced line drift (§5 rule 34: *re-derive the line map from
  `git diff -U0` and re-point in the same commit*). **`academy-backend.md` was NOT in `00be1ac`'s diff**, so
  that one is pre-existing — the class is not purely induced.
- **Why I hesitated:** the referenced content does exist in the same document a few lines away, so a reader is
  confused rather than misled. Booked up per the instrument's *resolve doubt upward* clause.
- **Confidence:** high that the anchors are wrong; medium on the BLOCKER/MINOR grading.

### B3 — "Go execs only `studio/gen.py`" is false, and the same document says so 190 lines later

- **Anchor:** `corpus/services/cms.md:95` — *"…and no Go caller exists (**Go execs only `studio/gen.py`**,
  `studioManager.go:119`)"*
- **Why it is false:** at `app` `b948604f`, `internal/cms/studio/studioManager.go` execs **two** Python
  entrypoints: `:119` `return s.runCommand(ctx, pyBin, append([]string{"studio/gen.py"}, tokens...))` **and**
  `:1045` `return s.runCommand(ctx, pyBin, append([]string{"studio/postgen.py"}, tokens...))`. The file's own
  comments say so at `:137` and `:1048` (*"the Studio Python subprocess (gen.py/postgen.py)"*).
- **Self-contradiction:** `cms.md:285` states the correct pair — *"invokes `python3 studio/gen.py …` /
  `studio/postgen.py`"*.
- **Materiality:** the conclusion the parenthetical supports (*no Go caller of `pdf2md.py`*) survives; the
  supporting fact does not, and it is the kind of "only" a reader will carry forward.
- **Confidence:** medium (false supporting clause inside a true conclusion; self-contradictory).

### B4 — `external_services.md:3` asserts a GitHub archive state that the same corpus, in my own file set, declares unmeasurable

- **Anchor:** `corpus/architecture/external_services.md:3` — *"In *production* the router is still declared
  (`graphql-wundergraph/terraform/main.tf:20` `= 1`), though **the repo is ARCHIVED on GitHub
  (2026-07-30)**."*
- **Why it is unsupportable:** GitHub archive state is not derivable from any clone. It is not in the clone
  set's data, `gh` is not installed on this host, and `git ls-remote` cannot distinguish an archived repo from
  a live one. Two other passages **in my own file set** say exactly this and retract the equivalent claim:
  - `corpus/services/jobsimulation.md:24-27` — *"**The repo's GitHub archive state is not visible to this
    corpus** — this banner asserted an archive on 2026-07-31; `origin/main` then landed four commits on
    2026-08-04 … so the flat claim cannot stand. Report both, assert neither."*
  - `corpus/services/cms.md:18` and `:82-84` apply the same "report both, assert neither" discipline to the
    `infrastructure` repo.
  The jobsimulation retraction was earned the hard way (I re-derived it: `6092c6d2`, `caf36c96` = PR #439,
  `1e40d184`, `82cb66ec`, all 2026-08-04, all on `origin/main`). The identical *flat* form survives in
  `external_services.md`'s banner — the highest-propagation position in an 880-line document — which is the
  half-repair pattern §5 rule 19 names.
- **What I could NOT do:** refute it. `graphql-wundergraph`'s clone has no commit after `60c229f3`
  (2026-07-30), which is *consistent with* an archive and proves nothing. I book it as unsupportable, not
  false.
- **Why I hesitated:** it may well be true. Booked because the corpus has already adjudicated that this class
  of claim may not be asserted flatly, and the adjudication lives in two of my seven files.
- **Confidence:** medium.

### B5 — the `rosetta-extensions` anchors in `external_services.md` name CORS code in the authoring copy

- **Anchor:** `corpus/architecture/external_services.md:206-211` — *"`rosetta-extensions/stack-injection/
  gen_injected_override.py:669-670` re-points every service in `DIRECTUS_DATA_CONSUMERS`, which is
  `("cms", "backend")` (`:84`)"*
- **Why it is wrong at the witness:** the ADDENDUM's rule 2 makes the **authoring copy** (`6130bfd8`) the
  witness for a rext citation whose block pins no ref, and this block pins none (the `rext f9ac72f` pin sits
  in the **next, separate** blockquote — §5 rule 33: a pin's scope is the claim's own block). At `6130bfd8`:
  - `DIRECTUS_DATA_CONSUMERS = ("cms", "backend")` is at **`:86`**; `:84` is a comment about
    `DIRECTUS_PUBLIC_BASE_ADDR` deliberately *not* being re-pointed.
  - the re-point is at **`:698-699`** (`if with_directus and name in DIRECTUS_DATA_CONSUMERS:` /
    `env.append(f"DIRECTUS_BASE_ADDR={DIRECTUS_INNETWORK_ADDR}")`); **`:669-670`** is inside the `backend`
    CORS `CORS_EXTRA_ORIGINS` block — a different mechanism entirely.
  - Traced over history, `:84`/`:669-670` was the state at rext `dfb0929`; two commits have moved it since.
    They still resolve in the **stack-demo consumption clone** (`ab81527a`), which is why nobody noticed.
- **What is unaffected:** the substantive claim (both `cms` and `backend` are re-pointed, and `backend` is the
  real reader) is TRUE at every ref, and `stack-injection/tests/test_injection.py:1109`
  (`def test_backend_the_actual_reader_is_repointed`) resolves **exactly** in the authoring copy.
- **Confidence:** medium (anchor drift only; correct against a second in-tree clone).

---

## Minors

- `corpus/architecture/external_services.md:633` cites `config_template.ini:30-31` with no directory; the file
  is `app/studio/**configs/**config_template.ini`. Lines 30-31 are correct there (`OPENAI_API_KEY` /
  `OPENAI_ENDPOINT = https://api.openai.com/v1/chat/completions`).
- `corpus/architecture/external_services.md:14` — *"GraphQL/Wundergraph - Unifies **all backend services**
  into a single API"* — stale PM-level phrasing; the supergraph has been one subgraph since `915da06`, as the
  same file says at `:3` and `:368`.
- `corpus/services/jobsimulation.md:25-27` breaks out of the surrounding blockquote (no `>` prefix on three
  lines mid-banner). Markdown *lazy continuation* keeps it rendering inside the quote, so there is no visible
  consequence — but it is a diff-fragile shape in a heavily-edited banner.
- `corpus/services/jobsimulation.md:203` labels `7177374` as *"@ `origin/main`"*; `origin/main` is now
  `2035f9a4`. The sha pin itself is sound (`main.go:216` is `func main()` at `7177374`, at `9d00a313`, and
  `:212` at `b948604`, all three verified).
- `corpus/services/ai-labs.md:18` and `corpus/services/academy-backend.md:20` both state *"`app` is at
  **`v1.363.2`** @ `5ba17044`"*. Ref-pinned, so gradeable and true at that sha — but the checkout is v1.366.0
  and `origin/main` is past v1.367.0, so the parenthetical reads as a currency statement in a sentence whose
  whole point is "this number is not the one you think it is".

---

## Positively cleared (audited zeros)

**ENUMERATED** — predicate enumerated from source independently, cardinality compared before any arithmetic:

1. **Supergraph subgraph ladder** (`external_services.md:373-386`, restated `cms.md:59-63`). I did not check
   the sum; I listed the `- name:` members of `supergraph-config-prod.yaml` at each named commit:
   `749dc86~1` = {backend, skiller, jobsimulation, cms, skillpath} = **5**; `749dc86` = **4** (skiller gone);
   `7c17e63` = **3** (skillpath gone); `915da06` = **1**; HEAD = **1**. `git show --name-status 915da06` marks
   **both** `schemas/cms.graphqls` and `schemas/jobsimulation.graphqls` `D`. The **3 → 1** claim and the
   explicit warning not to trust `915da06`'s own "2→1" subject line are both correct.
2. **No subscriptions in the supergraph.** `grep -rn "sse\|subscription" graphql-wundergraph/*.yaml` → rc 1,
   zero hits, with the positive control `grep -rln backend …*.yaml` matching all three supergraph configs in
   the same pass. `915da06~1:supergraph-config-prod.yaml:13` still reads `protocol: "sse_post"`;
   `git log -S 'protocol: "ws"'` → empty, `-S sse_post` → 5 commits; `bba862f` is **not** an ancestor of HEAD
   (rc 1) and `git branch -a --contains` names only `remotes/origin/feat/use-web-socket`. In
   `schemas/backend.graphqls`: `type Mutation` `:4053`, `type Query` `:4912`, **no** `type Subscription`, and
   all six `Subscription` substrings are Stripe/plan field names.
3. **Compose topology at platform `0c91421d`.** Five declared services (`sentinel:5`, `backend:28`,
   `studio-desk:112`, `next-web-app:143`, `gotenberg:170`) + two from `include: common.yml`
   (`postgresql`, `redis`) = **seven effective**; the profile-less floor is exactly {postgresql, redis,
   sentinel}; `core` members are `backend` (`:110`) and `gotenberg` (`:183`). At `0dab54d` it was **eight /
   ten**, exactly as `external_services.md:306` says. No `graphql` token in any `profiles:` key at `0dab54d`.
   Every cited line hit: `:53` `DIRECTUS_PUBLIC_BASE_ADDR`, block `:46-94`, `:48`
   `AUTHORIZATION_ADDRESS=http://sentinel:8087`, `:101-109` backend `depends_on` = {redis, postgresql,
   sentinel}, `:119`/`:135` studio-desk build-arg + runtime env, `:151`/`:160` next-web-app ditto — all four
   `:8082/graphql/query`.
4. **`repos.yml` membership.** 4 entries at HEAD (app, sentinel, next-web-app, studio-desk); **6** at
   `0dab54d` (+ storage, messenger). `d11a403`'s diff removes `- name: cms`, `- name: jobsimulation`,
   `- name: roadrunner` **and** the three matching compose service blocks (`-  jobsimulation:`, `-  cms:`,
   `-  roadrunner:`). `838d907` removes storage/messenger/customerio-sync.
5. **Taxonomy figures** (`external_services.md:684-697`, `skiller.md:43-52`). Read the manifest itself, not
   the prose: `public_only: true`, `predicate: "org-null"`, `captured_at 2026-06-29`, `skills.row_count`
   **42,790**, `job_roles` **22,470**, `skill_embeddings` **42,790**, `job_role_embeddings` **18,919**. The
   "18,919 was transcribed onto the role count" diagnosis is exactly reproducible.
6. **The four AI env-var counts** (`external_services.md:665-670`). Re-ran the document's own
   `command grep -rho '\b<NAME>\b' --include='*.go'` in `app`: OPENAI_KEY **5**, ANTHROPIC_API_KEY **26**,
   AZURE_OPENAI_KEY **13**, AZURE_OPENAI_ENDPOINT_URL **13**; OPENAI_ORG_ID / AZURE_OPENAI_ENDPOINT /
   AZURE_OPENAI_DEPLOYMENT **0/0/0**; OPENAI_API_KEY **2**. All eight match.
7. **All 30 `*_AI_*_MODEL` lines pin `azure`.** Enumerated per file: `configs/config_template.ini` 10,
   `development_config.ini` 10, `production_config.ini` 10 — every value's first field is `azure`. This is
   the load-bearing fact behind the *four live + one latent* residency count, and it holds.
8. **Stripe webhook event set** (`ai-labs.md:110-111`). Enumerated the `switch event.Type` in
   `internal/web/backend/api/api.go:319-390`: exactly two top-level cases, `"customer.created"` and
   `"customer.subscription.{created,updated,deleted}"`. **No `checkout.session.completed`** anywhere in Go
   source at `origin/main` (all hits are planning markdown). `POST /credits/purchase` is gone —
   `internal/web/backend/credits/handler.go:12` says *"removed in Wave 13"*.
9. **`STORAGE_RPC_ADDR` at `app` `9d00a313`** (`academy-backend.md:57-58`): exactly **3** occurrences in
   `*.go` — `internal/jobsimwiring/wiring.go:101`, `internal/storagens/callsites_test.go:189`, `main.go:451`
   — and all three are comments. Zero reads. `main.go:471-472` constructs the two managers as claimed.
10. **`gen.py` registers exactly nine arguments** at `:484-492` (`-i/--interactive`, `-m/--media`,
    `-f/--force`, `--simid`, `--branch`, `--prompt`, `--annotations`, `--pipeline`, `--blueprint`), and
    `parse_argument` at `:18-28` calls `parse_known_args` and merges leftovers. The "`--template` is silently
    swallowed" mechanism is exactly as described.
11. **`enum ContentLanguage` has 8 members** (`skiller.md:57-58`): english, italian, spanish, french, german,
    dutch, japanese, portuguese (`schemas/assignments.graphqls:44-53`).
12. **23 jobsim run-state tables**: `grep -c '^CREATE TABLE' app/terraform/migrations/20260722081626_
    jobsim_data_model.sql` → **23**, and the very next migration `20260722104506.sql` creates
    `job_simulation_sessions` at `:2` and `DROP TABLE "sessions";` at `:79`.
13. **`20260729133514.sql` contains 0 `INSERT`s** — case-sensitive count is 0; the single case-insensitive hit
    is the identifier `on_insert_local_jobsimulation_sessions_update_memberships()` at `:64`. The
    "back-fills then DROPs" retraction is correct. `:15-23` remap, `:36-44` orphan NULLs, `:58-62` the
    "5. Drop the mirrors" comment, `:62-63` the two `DROP TABLE`s — every range lands on its construct.
14. **`NEXT_PUBLIC_GRAPHQL_ENDPOINT` does not exist in `next-web-app`** (`external_services.md:764`).
    Established properly: `git grep` at the clone's own ref → 0, positive control
    `NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT` → 5+ files including three `.gitignore`-hidden `.env.example`s that
    bare `grep` cannot see; and the one NUL-bearing file in the tree
    (`apps/web/src/hooks/useCoursebuilder.ts`, NUL count verified non-zero) read separately with
    `grep -a` → 0.
15. **`app` at `9d00a313` has no cobra root** (`jobsimulation.md:202-208`): the only `spf13/cobra` importer in
    the whole repo is `cmd/createTaxonomy/main.go`; `main.go:216` is `func main()` at `7177374` **and** at
    `9d00a313`, `:212` at `b948604`; the fatal is `main.go:670` at `9d00a313` and `:614` at `b948604`.
    `wiring.go:147-148` is `if err != nil { return nil, err }` at the pinned `9d00a313` (it is argument lines
    at the checkout — the pin is doing real work here).
16. **`jobsimulation` origin/main took exactly four commits on 2026-08-04**: `6092c6d2`, `caf36c96` (PR #439),
    `1e40d184`, `82cb66ec`.

**RE-DERIVED** (weaker — I recomputed what the document displays, or confirmed a single cited construct):

- Every `file:line` in `external_services.md`'s AI section: `report_agent.go:31`, `askengine/bedrock.go:25`,
  `coursebuilder/bedrock.go:23`/`:29`/`:106-113`, `jobsimulation/ai/ai.go:30-33`/`:80`/`:130-141`/`:150-155`/
  `:259-289`/`:264-276`/`:280-283`/`:296-302`/`:326`, `simulator/ai/ai.go:58-59`/`:114-115`,
  `cms/directus/collections/jobsimulation.go:905`/`:1302-1305`, `cms/studio/markdownManager.go:19`,
  `studioManager.go:583`, `calls/livekit.go:110`/`:115`/`:120`/`:126`, `web/backend/backend.go:130`,
  `app/main.go:770`/`:980-982`, `studio/services/ai.py:383`/`:627-664`/`:704-724`/`:706-708`,
  `studio/gen.py:41-53`/`:45-47`, `studio/tools/pdf2md.py:24`. All resolve to the named construct.
- Historical compose claims: `1e8e754:docker-compose.yml:6-8` (`dockerfile: graphql-wundergraph/
  Dockerfile.dev`), `:14` (`"5050:8080"`), `:19-27` (four `depends_on`, each `service_started`);
  `b56d731` still has `  graphql:` at `:22` behind `profiles: [wundergraph-deprecated]`, `360efd4` has none
  and `  backend:` at `:28`; `a2a3ee6^:docker-compose.yml:384/:386/:409` = `directus/directus:10.10.1` /
  `8055:8055` / `ADMIN_PASSWORD=password` (the "it DID exist" correction-of-the-correction is right);
  `Makefile:10 PROFILE ?= graphql` @ `2adcf71`; `repos.yml:17-19` @ `2adcf71`; compose @ `2adcf71`
  `:93-94`/`:113`/`:119` (jobsim 8400/8401) and `:154-155`/`:169`/`:173` (cms 8090/8091).
- Terraform: `graphql-wundergraph/terraform/locals.tf:8` `port = 8080`, `main.tf:48-49`, `config.prod.yaml:5`;
  `cms/terraform/main.tf:39` `= 0` in a 191-line module, and `6efa1d5` (merged `f38c0c4`) really does delete
  only `.github/workflows/build-production.yml` under the M810 ECR subject — the "two measured facts point
  opposite ways, assert neither" framing is accurate; `jobsimulation/terraform/main.tf` at `6092c6d2` has
  **no** `service_desired_count` and `:15-22`, `:24-40`, `:38-40` all land on the constructs cited.
- `cms` repo: `cmd/root.go:77`/`:78`, `Dockerfile:2`/`:23`, `go.mod:3` `go 1.26.4`; `cms/studio/
  requirements.txt` is **byte-identical** to `app/studio/requirements.txt` (9 packages, no `python-docx`).
- `app`: `main.go:1204`, `:1205-1211` (the stale "DORMANT … until the M809 re-point" comment is genuinely
  still there), `internal/skillpath/session.go:205-207`, `internal/organization/intelligence.go:1700`,
  `jobsimulation/ai/ai.go:90`, `.gitignore:79` `studio/*`, `REDIS_CMS_CACHE_INDEX` default 5 (`main.go:997`).
- `ai-labs.md` constants: `creditCost{build:5, refine:1, translate:1}` (`cost.go:86-90`),
  `MarginMarkup = 1.40` (`:133`), `PricePerCreditUSD = 0.45` (`:142`), `purchasePackages` 50/200/500
  (`:201-203`), `DefaultSeedBalance = 500` (`:231`), `LabsTier` essential/professional/frontier, labsapi
  default `:7070`, `labsdemo` `:8099`, `stripe-go/v74 v74.30.0`, all five cited migration files present.
  > **Near-miss worth recording:** `cost.go:29`'s own doc-comment says *"course.refine → **5** credits per
  > refine turn"* while the live map at `:88` is `ActionCourseRefine: 1`. The corpus says **1** and is
  > **right**; the platform's comment is the stale one. A reader who checked the comment instead of the map
  > would have booked a false blocker against the corpus.
- `academy-backend.md`: `academy.graphqls` + `resolver_academy.go` present; 11 `academy_*` Ent schema files;
  `academy_embedding_refresh` at 03:00 (`internal/worker/tasks/academy_embedding_refresh.go:10`,
  `internal/academy/embeddings.go:108`); `extensions.vector(1536)`
  (`academy_path_embedding.go:62`); `/content/catalog.json` (`content.go:23`) and
  `/content/admin/{…}` + `ACADEMY_CONTENT_API_TOKEN` (`backend.go:286-287`); `0e37771f` = *"Academy backend
  v1.0 'ground truth' … (#903)"*; `cmd/academyImport/main.go:239-243` and `:244-246` @ `9d00a313` verbatim.
- `skiller.md`: `internal/rpc/skillerrpc/` exists; all six named Ent schema files exist;
  `skiller_taxonomy.graphqls` exists and its `:7` records `categoryTree, fullCategoryTree` as *"stay
  unported — no consumers"*; `extensions.vector(1536)` + IVFFLAT in `skiller_mixins.go:184`/`:171`.
- `corpus/services/TEMPLATE.md` is a pure skeleton with no factual assertions; nothing to falsify. Read in
  full for completeness.

---

## What I could not settle

- **GitHub archive state of `graphql-wundergraph`** (B4). Not derivable from a clone; `gh` is not installed on
  this host; `git ls-remote` cannot distinguish archived from live. Settled only by
  `gh api repos/anthropos-work/graphql-wundergraph --jq .archived` or the repo page.
- **`app/knowledge/plan/releases/06.00-shared-purse/`, milestones M600–M607** (`ai-labs.md:16`). HARD BAR 1
  forbids reading under `knowledge/plan/**`; I did not open it. (Incidental `git grep` output for the
  `checkout.session.completed` predicate showed such paths exist at `app` `origin/main`, which is consistent
  with the claim, but I did not read them and do not book it as cleared.)
- **`anthropos-work/AI-Labs` exists as a live Go control plane** (`ai-labs.md:5`). Not in the clone set.
- **`infrastructure/terraform/production/services.tf`** — `module.cms_euwest1`. The corpus says this is
  invisible to it, and that is correct: no `infrastructure` clone exists anywhere under `stack-demo/`.
- **`academy_chapter_bodies` locale set "EN base + de/es/fr/it/nl/pt"** — I found no enumerable locale list in
  Go source; `enum ContentLanguage` (8 members, incl. japanese) is a *different* enum on a different surface,
  so I could not confirm or refute the 6-overlay set. Would be settled by the manifest schema or a
  `SELECT DISTINCT locale FROM academy_chapter_bodies`.
- **PR `#395`, `#989`** (jobsimulation Chronos removal, app skiller-RPC retirement) — PR numbers are not
  derivable offline; the underlying commits (`09631fb2`, asynq `v0.26.0`) are verified.

### Out-of-scope observation (NOT booked — no file of mine asserts it)

Root `CLAUDE.md` states *"Prod compute for both is **stopped** (`storage/terraform/main.tf:38`,
`messenger/terraform/main.tf:29`, each `service_desired_count = 0`)"*. Measured this reading:
`messenger/terraform/main.tf:29` **= 0 at `origin/main` (`e9421c68`)** ✓, but the **checkout** `fa47850d`
reads `:19  service_desired_count = 1`; and for `storage`, `service_desired_count` appears at **`:19` = 1** at
the checkout `4ce8ece5` and **nowhere at all** in `terraform/main.tf` at `origin/main` (`9f8cb532`) — i.e. the
storage module block looks to have gone the `jobsimulation`/`6092c6d2` way. Neither `:38` nor `= 0` is
reachable for `storage` at either ref. Flagging for whichever seat owns `CLAUDE.md` / `storage.md` /
`messenger.md`; I did not book it because it is outside my assigned files and I did not read those files.
