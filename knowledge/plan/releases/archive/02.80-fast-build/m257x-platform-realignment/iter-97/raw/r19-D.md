# Seat D — reading #19

Corpus under audit: `rosetta` @ `00be1ac` (tree clean apart from this iter dir).
Ground truth re-derived at open, per the ADDENDUM sheet — all 15 trees enumerated with
`find stack-demo -name .git -maxdepth 4`, each grepped at **its own** ref:

```
stack-demo/ant-academy         9c3843cd     stack-demo/next-web-app        bb3313bc
stack-demo/app                 b948604f     stack-demo/platform            0c91421d
stack-demo/app/studio          aeec036a     stack-demo/roadrunner          87d8d443
stack-demo/cms                 ca50c817     stack-demo/rosetta-extensions  ab81527  (stack copy)
stack-demo/cms/studio          aeec036a     stack-demo/sentinel            88bc5592
stack-demo/graphql-wundergraph 60c229f3     stack-demo/storage             4ce8ece5
stack-demo/jobsimulation       462343b0     stack-demo/studio-desk         14a5442a
stack-demo/messenger           fa47850d
```

Note on `platform`: the addendum sheet's `0c91421d` is what is on disk, and it is **`838d907`**
(*"drop the storage, messenger and customerio-sync containers"*) merged. `docker-compose.yml` is
186 lines and declares **5** services; `common.yml` adds 2; `repos.yml` has **4** entries.

## Positive controls

| file | `wc -l` | read in full? |
|---|---|---|
| `corpus/services/studio-room.md` | 506 | yes |
| `corpus/architecture/architecture_overview.md` | 413 | yes |
| `corpus/architecture/security_compliance.md` | 281 | yes |
| `corpus/services/roadrunner.md` | 200 | yes |
| `corpus/services/next-web-app.md` | 133 | yes |
| `corpus/architecture/dependency_map.md` | 103 | yes |
| `corpus/services/intelligence.md` | 18 | yes |

Total 1,654 lines. `corpus/ops/platform-alignment.md` §5 (lines 249–1401) read in full before starting.

Search-instrument controls run in the same pass: `git grep -c JUDGE0_BASE_URL b948604f -- '*.go'`
→ 1 file (control for the `ROADRUNNER_RPC_ADDR` zero); `git grep -icl 'the' HEAD` in `app` → 2,631
files (control for the per-tree roadrunner census); `git grep -c atlas_migration 6092c6d2 --
terraform/main.tf` → 4 (control for the `service_desired_count` zero). No search below rests on an
unread stderr.

---

## Blockers

### B1 — `roadrunner.md` states the jobsimulation clone holds ONE `roadrunner` mention; it holds 14 across 8 files

- **Anchor:** `corpus/services/roadrunner.md:114` — *"The jobsimulation consumer was **deleted, not
  moved**: at `jobsimulation 462343b0` the whole repo contains one `roadrunner` mention
  (`internal/runner/runner.go:3`, a parenthetical), with no handler and no event reference"*
- **Why it is false:** the claim names its ref, so I graded it there.
  `git -C stack-demo/jobsimulation grep -in roadrunner 462343b0` returns **14 hits across 8 files**
  (case-sensitive lowercase: **5**). The full set:
  - `CHANGELOG.md:31, 1066, 1067, 1068, 1070, 1086`
  - `CLAUDE.md:112` — *"Sandboxed code execution, formerly the standalone Roadrunner RPC service…"*
  - `internal/runner/runner.go:3` — the one the corpus names
  - `knowledge/architecture.md:61`, `knowledge/features.md:70`, `knowledge/glossary.md:60, 87`,
    `knowledge/service-dependencies.md:23`
  - **`knowledge/operational.md:68`** — a live env-var table row: `| ROADRUNNER_RPC_ADDR | Roadrunner
    service address |`
  Only the **Go-source** count is 1 (`git grep -in roadrunner 462343b0 -- '*.go'` → exactly
  `internal/runner/runner.go:3`). The sentence says *"the whole repo"*, unqualified, and the fact it
  is used to establish (nothing in that repo still points at roadrunner) is the fact
  `knowledge/operational.md:68` contradicts. Unchanged at `origin/main` (`82cb66ec`): still 14 / 8.
- **Confidence:** high (that the count is false); medium (that it rises to BLOCKER). I hesitated
  because the *conclusion* the sentence draws — no handler, no event reference — is correct, and
  because a Go-only reading makes "one" right. I booked it because the claim is an explicit,
  ref-pinned, whole-repo cardinality, and because the hit it misses is a doc row that still
  advertises `ROADRUNNER_RPC_ADDR` as a live address of that service — precisely the residue a
  reader would consult this sentence to rule out.

### B2 — `roadrunner.md`'s banner asserts two zeros that both measure non-zero

- **Anchor:** `corpus/services/roadrunner.md:24-26` — *"On current `origin/main` there is **no
  `ROADRUNNER_RPC_ADDR` / `RoadRunnerService` / `roadrunner:10401` read in any service's Go code**
  (M247 re-grepped `app` + `jobsimulation` on the consolidated clones — **zero hits outside
  CHANGELOG**), and **no other platform repo references roadrunner at all**."*
- **Why it is false:** two separate measurements, both taken per-tree at each tree's own ref (§5
  rule 44 procedure), both non-zero. The **first half** of the sentence — *"read in any service's Go
  code"* — is TRUE and I confirmed it (0 hits for `ROADRUNNER_RPC_ADDR` and `RoadRunnerService` in
  `app` @ `b948604f -- '*.go'` and in `jobsimulation` @ `462343b0 -- '*.go'`, positive control
  `JUDGE0_BASE_URL` → 1). The two parentheticals are the false part:
  1. **"zero hits outside CHANGELOG"** — `jobsimulation/knowledge/operational.md:68` carries
     `ROADRUNNER_RPC_ADDR` at `462343b0` **and** at `origin/main` `82cb66ec` (there
     `CHANGELOG.md:1075` + `knowledge/operational.md:68`, i.e. 1 of 2 outside CHANGELOG). In `app`
     `RoadRunnerService` has one hit, `knowledge/plan/releases/07.00-jobsim-in-app/
     RE-PORT-CHECKLIST.md:10` — also outside CHANGELOG.
  2. **"no other platform repo references roadrunner at all"** — census over every tree at its own
     ref (excluding `roadrunner` itself and the two already named):

     | tree | ref | files | hits |
     |---|---|---|---|
     | `next-web-app` | `bb3313bc` | 1 | 1 — `knowledge/service-dependencies.md:77` |
     | `platform` | `0c91421` | 3 | 8 — `CLAUDE.md:17,61,62,122`, `README.md:5,99,148`, `repos.yml:5` |
     | `studio-desk` | `14a5442` | 3 | 4 |
     | `app` | `b948604f` | 25 | 46 |

     The `studio-desk` hits are the material ones: `knowledge/01-platform-context/
     anthropos-platform.md:53` lists **roadrunner** as a platform service, and **`:68`** lists it as
     a live target — *"Internal Connect-RPC to: sentinel, chronos, storage, messenger,
     roadrunner"* — with `knowledge/04-integration/platform-integration.md:137` and
     `knowledge/00-product/simulation-experience.md:226` saying the same. That is exactly the stale
     downstream reference this sentence certifies does not exist. (All hits are markdown/YAML; a
     Go-only reading would make the claim true, but the sentence's second clause drops the "Go code"
     qualifier that its first clause carries.)
- **Confidence:** high on the measurements; medium-high on the grading. Same hesitation as B1 — the
  load-bearing operational claim (nothing *calls* roadrunner) survives. I booked it because the
  corpus itself is the thing this sentence licenses a reader not to check, and a live "Connect-RPC
  to … roadrunner" line in a sibling repo's knowledge base is the sort of thing that gets copied
  forward.

---

## Minors

- `corpus/architecture/architecture_overview.md:336` — *"(3 hits repo-wide at `9d00a313`, every one a
  comment)"*. Measured at that ref: **29** hits repo-wide, **3** in Go source
  (`internal/jobsimwiring/wiring.go:101`, `internal/storagens/callsites_test.go:189`,
  `main.go:451`) — the other 26 are `knowledge/**` markdown/JSON, which are not "comments". The
  claim the parenthetical supports (*"`STORAGE_RPC_ADDR` has zero **reads** anywhere in `app`"*) is
  TRUE and independently verified at `9d00a313` and at `origin/main`. Same class as B1/B2 — a
  repo-wide count that is only correct for Go source — but here the count is a supporting aside,
  not the claim.
- `corpus/architecture/architecture_overview.md:293` cites `ai.go:262-276` for the `case Azure:`
  flag swap; `corpus/architecture/security_compliance.md:209` cites `:263-277` for the *same*
  construct. Source (`app/internal/jobsimulation/ai/ai.go` @ `b948604f`): `case Azure:` at **263**,
  `return client, nil` at **277**. The `security_compliance` range is exact; the
  `architecture_overview` range is off by one at both ends (262 is `switch vendor {`). Two files I
  hold disagreeing about one anchor; line drift only, no factual consequence.
- `corpus/architecture/security_compliance.md:185` — *"(consistent with `README.md:21` +
  `ai_architecture.md`)"*. `README.md` is not unique and neither candidate supports the claim:
  `rosetta/README.md:21` = *"Project Rosetta is a documentation repository…"*; `app/README.md:21` =
  `$ make migrations`. Presumably the (uncloned) `ai` library's README. Unresolvable without
  guessing — coverage, not a wrong fact. The substantive claim (`app/internal/aiusage` exists and
  owns cost tracking) is verified: `app/internal/aiusage/ai_usage.go`.
- `corpus/architecture/dependency_map.md:59` — *"Enumerated over every publisher constructor in
  `app` @ `b948604f` **and** origin/main `2035f9a4`: `backend` (`main.go:287`), `SKILLPATH_STREAM`
  (`:637`), `CMS_STREAM` (`:1039`), … (`internal/jobsimwiring/wiring.go:127`, `:180`)"*. The
  enumeration is **correct and complete** at `b948604f` (5 `pubsub.NewPublisher` calls, none
  `SKILLER_STREAM`) and the conclusion holds at `origin/main` too (5 calls, none `SKILLER_STREAM`),
  but the line numbers are the `b948604f` ones only — at `2035f9a4` they are `main.go:325/746/1149`
  and `wiring.go:132/185`. A sentence naming two refs and one set of anchors.
- `corpus/services/studio-room.md:348-359` — the `requirements.txt` block is presented as the file's
  contents but reorders it: `mistralai` is **line 8** (after `jinja2`), not third. The set and the
  `requirements.txt:8` anchor quoted 15 lines below are both correct.

---

## Positively cleared (audited zeros)

### ENUMERATED — predicate re-derived from source, cardinalities compared

1. **`security_compliance.md:64-137` + `architecture_overview.md:355-367`, the multi-tenancy fence
   (the passage that has been wrong four times).** Re-derived the SET, not the sum, per §5 rule 24:
   - `internal/data/ent/schema/`: **139** `.go` files; **135** contain `ent.Schema`; the 4 that do
     not are `database_types.go`, `mixin.go`, `skiller_mixins.go`, `skillpath_mixins.go`. Struct
     count agrees independently: 112 multi-line embeds + 23 `struct{ ent.Schema }` = **135**.
   - `OrganizationMixin{}` in **30** files; `user_resource.go:22` is
     `// OrganizationMixin{},  // We need to work on this` → live set **29**.
   - Exactly **four** files declare `) Policy() ent.Policy`: `mixin.go`, `organization.go`,
     `user.go`, `org_membership.go`. `Organization` (`organization.go:56`, `FilterSameOrganizations()`
     at `:96`) and `Membership` (`org_membership.go:172-188`, `AllowCurrentOrgEdgesOrSkipRule()`,
     ending `AlwaysDenyRule()`) filter by org; `User` (`user.go:116`) filters by **user**
     (`DenyIfNoUserInContext` / `FilterSameUserRule`) and is correctly excluded. **29 + 2 = 31 of 135.**
   - Ran the document's own derivation verbatim: schema-restricted → **18** files with a plain
     `organization_id` and neither mixin; the bare `*.go` glob → **19**, the extra one being
     `skiller_mixins.go`, exactly as the doc says. Minus `org_membership.go` (self-policed) and
     `academy_feedback.go` (`UserMixin{}` at `:64`, whose `Policy()` at `mixin.go:99` applies
     `FilterOwnerRule()`) → **16**, and the 16 filenames printed in the doc match the derived set
     **exactly, name for name**.
   - `OrganizationIDMixin{}` users: **7** — `category, jobrole, similarity, skill, specialization,
     studio_document, studio_task` — matching the doc's list exactly, defined at
     `skiller_mixins.go:148-162` as *"a plain, nullable `organization_id` UUID column + index …
     intentionally does NOT attach backend's OrganizationMixin privacy policy"*. **16 + 7 = 23.**
   - **Trap I fell into and climbed out of:** `grep -l 'UserMixin{}'` returns
     `category/jobrole/skill/specialization` — a **substring hit on `CreatedByUserMixin{}`**. Had I
     stopped there I would have booked the doc for excluding `academy_feedback.go` (UserMixin) while
     including four schemas with "the same" mixin. Reading the `Mixin()` blocks shows they carry
     `CreatedByUserMixin{}`, which declares no `Policy()`. The doc is right; the cheap regex was not.
   - Supporting anchors all resolve to the named construct: `job_simulation_session.go:5`
     (*"L2: NO Ent privacy Policy; owner/org/tenant are plain fields"*), `jobrole.go:18`,
     `category.go:15`, `mixin.go:126`.
2. **`architecture_overview.md:80` — `authn` imported by nothing.** Per-clone, per-ref:
   `github.com/anthropos-work/authn` → **0 hits repo-wide** in all seven Go clones (app, sentinel,
   storage, messenger, cms, jobsimulation, roadrunner); positive control
   `github.com/anthropos-work/colony` present in all seven `go.mod`.
3. **`architecture_overview.md:12, 71, 156-165, 183-186, 223` — the local topology.**
   `docker-compose.yml` @ `0c91421` declares **5** services (sentinel, backend, studio-desk,
   next-web-app, gotenberg); `common.yml` adds postgresql + redis → **7** effective. `sentinel`,
   `postgresql`, `redis` declare **no `profiles:` key** → in every selection. `backend`/`gotenberg`
   are `profiles: [core, backend, all]`; `Makefile:10` is `PROFILE ?= core` → a bare `make up` gives
   **five** containers, two of them ours. `next-web-app` `profiles: [frontend, all]` +
   `depends_on: backend` (`:165-167`); `studio-desk` `profiles: [studio-desk, all]` +
   `depends_on: backend` (`:138-140`) — neither profile selects `backend`, so each alone is an
   invalid project. No `storage`/`messenger`/`customerio-sync` service or profile survives; no
   `*_RPC_ADDR` anywhere; `AUTHORIZATION_ADDRESS=http://sentinel:8087` at `:48` is the only service
   address. Counter-check at `0dab54d`: 8 + 2 = **10** services and **6** `repos.yml` entries, which
   is what `dependency_map.md:31` says it was.
4. **`architecture_overview.md:20, 221` / `dependency_map.md:31` — M810 for jobsimulation.**
   `6092c6d2` **is** on `jobsimulation/origin/main` (4 commits ahead of the checkout, all dated
   2026-08-04, PR #439 among them — which is the fact `:20` uses to withhold the archive assertion).
   At that ref `terraform/main.tf` is 56 lines, `service_desired_count` occurs **0** times (control:
   `atlas_migration` 4), `:15-22` is the M810 decommission comment and `:24-40` the
   what-this-module-still-owns block. At the *checkout* `462343b0` the module is still there at 343
   lines — so grading at the named ref is load-bearing here, and the corpus named it correctly.
5. **`architecture_overview.md:222` / `dependency_map.md:31` — cms's two-facts-pointing-opposite-ways.**
   `cms/terraform/main.tf:39` = `service_desired_count = 0`; `6efa1d5` (*"drop build-production — the
   cms ECR repository is decommissioned (M810)"*, 18 deletions) merged as `f38c0c4a`, 2026-08-04.
   Both true; the corpus reports both and asserts neither, correctly.
6. **`architecture_overview.md:328-351` — storage.** At `9f8cb53` `storage/terraform/main.tf` is
   **18** lines; `:9-11` is *"The ECS service that used to live here is GONE (v9.0 'support-in-app')"*;
   `:13-16` is the `prevent_destroy` rationale. At the intermediate `63bffc8` it read
   `service_desired_count = 0` (`:38`). `app/main.go:504` @ `origin/main` = *"…the standalone service
   takes no traffic and STORAGE_RPC_ADDR is gone."*, and the same comment sat at `main.go:451` @
   `9d00a313` (main.go: 1361 / 1569 / 1639 lines at the three refs — the anchor moved without the
   code moving, exactly as documented). At `b948604` the variable IS still read,
   `internal/jobsimwiring/wiring.go:115`. Every ref-scoped claim in this blockquote lands.
7. **`architecture_overview.md:3, 251, 280` / `next-web-app.md:15` — the supergraph.** `915da06`'s
   diff removes **both** the `jobsimulation` and the `cms` subgraph entries (its first sub-commit is
   *"supergraph 3->2"*, its second *"2->1"*), while its squashed subject says *"2→1"* — the corpus's
   "do not take the count from the subject line" is verified against the diff.
   `graphql-wundergraph/terraform/main.tf:20` = `service_desired_count = 1`.
   `roadrunner/terraform/main.tf:19` = `= 1` (repo HEAD `87d8d44`, 2026-06-19).
8. **`architecture_overview.md:221, 394` / `dependency_map.md:13` — "23 jobsim run-state tables".**
   `jobsimulation/internal/ent/schema/` @ `462343b0`: 24 `.go` files, **23** `ent.Schema` structs
   (`mixin.go` declares none).
9. **`dependency_map.md:58, 60, 61` — the messenger-in-app second subscriber.** At the ref the block
   names (`app 9d00a313`): `internal/messenger/flow/streams.go:65` = `StreamBackend = "backend"`
   (exact); `main.go:1442` opens `verifyConsumerGroupExists(…, msgflow.StreamBackend,
   StreamJobSimulation, StreamCMS)`; `streams.go:70` calls `BackendStreamHandlers` *"the 21-handler
   set"*, matching the doc's "21 live handlers". In `messenger` @ `fa47850`,
   `internal/flow/flow.go:72/105/109` are the three `AddSubscriber("backend"/"jobsimulation"/"cms")`
   calls — exactly three, no more.
10. **`dependency_map.md:59` — the `skiller` stream has no producer.** Enumerated every
    `pubsub.NewPublisher` in `app` at **both** refs: `b948604f` → `main.go:287/637/1039`,
    `wiring.go:127/180`; `origin/main 2035f9a4` → `main.go:325/746/1149`, `wiring.go:132/185`. Five
    at each ref; `SKILLER_STREAM` is in neither. Its only Go occurrence is `main.go:1276`, an
    `AddSubscriber` whose handler is `internal/roles/roles.go:791`
    (`func (r *RoleManager) SkillerSubscriber()`). Compose still sets the name at
    `docker-compose.yml:71`. **A live subscriber on a stream nothing publishes to** — verified.
11. **`studio-room.md` in full, against the nested repo at its own ref (`app/studio` @ `aeec036a`) —
    the single densest file I read and I found nothing wrong in it.** `gen.py` registers **exactly
    nine** `add_argument` calls at `:484-492` and the nine flags match one-for-one;
    `parse_argument`/`parse_known_args` at `:18-28`; `translate_legacy_blueprint` `:205-238` with the
    three legacy names `micro challenge` / `scenario challenge` / `collaborative challenge` and the
    *"is ignored; asset type is now inferred from task interactions"* warning verbatim;
    `validate_blueprint_exclusivity` `:241-271` with exactly the whitelist quoted;
    `setup_generation_request` `:273-282`; `work_paths` `:450-456` (the `worklog` in-code fallback is
    real); `postgen.py` requires `--media`/`--simid`/`--target`; `services/ai.py:1-2` imports only
    openai+anthropic, clients at `:383/530/664`, registry at `:705-708` = `{openai, azure,
    anthropic}`; `GenMode` five members with `DEFAULT = EXECUTION`; `exporter.py:513-550` (the file
    is **550** lines — `rmtree` on the last one, so the range is exact, not an overshoot) and
    `:518-519`; `model.py:59, 467-469` memo; `max_tokens = 4000` in all three tracked configs; the
    blueprint keys match `tests/e2e/blueprints/technical.json`; the five e2e + three
    asset-example blueprints exist.
    **The Mistral callout is exactly right and I re-measured all of it**: `git grep -i mistral
    aeec036a` → **22 hits / 3 files** (`requirements.txt:8`, `tools/pdf2md.py` 18,
    `tools/r3.py` 3); `pdf2md.py:24` `from mistralai import Mistral`, `:96` client, `:127`
    `model="mistral-ocr-latest"`; `r3.py:194` hard-requires `MISTRAL_API_KEY`;
    `app/studio/.gitignore:2` = `tools/`; `app/.gitignore:79` = `studio/*`; `cms/.gitignore:129` =
    `studio/*`; `app/Dockerfile:45-46` copies `studio/` and pip-installs it. Go side:
    `markdownManager.go:11,19` + `studioManager.go:583` → `OCRProcess` at `:602`. Worker:
    `internal/cms/worker/worker.go:29` `Concurrency: 5`, `:32-33` `ai_video: 7 / studio: 3`.
    `studioManager.go:119` execs `studio/gen.py`, venv consts `:94-96`. Runtime image
    `python:3.11-slim` (`app/Dockerfile:28`). And the `additional_repo` mechanism the doc names is
    real — `.github/workflows/build-production.yml:29`
    `additional_repo: "anthropos-studio-room:studio"` — notwithstanding `app/Dockerfile:42`'s own
    stale *"git submodule"* comment (there is no `.gitmodules`); that contradiction is the
    platform's, not the corpus's.
12. **`next-web-app.md` end to end.** `next ^16.2.7` in all four app `package.json`s; `react ^19.2.7`;
    `antd ^6.3.7`; `node >=24.0.0`; `packageManager pnpm@10.30.3`; `turbo ^2.9.6`. At **origin/main**
    (`8297c684`, the ref the doc names for these): `apps/{web,hiring,integration}/src/proxy.ts` exist,
    **no `middleware.ts` anywhere**, **no `storybook` script and no `.storybook/`**, and
    `configs/tailwind/storybooks.css` survives — all three exactly as written. Repo `CLAUDE.md:15` =
    *"Next.js 16 App Router"*, `:55` = the `proxy.ts` rename note. Public allowlist in
    `apps/web/src/proxy.ts:7-57` contains every route the doc names; `/print` is separately
    HMAC-gated (`isPrintRoute` `:59`, `packages/core-js/src/security/printToken.ts:16,49` reading
    `PRINT_ROUTE_SECRET`). Ports 3000/3001/3002 from the `dev` scripts, mobile 3031. **8** locale
    dirs (de,en,es,fr,it,ja,nl,pt). One Dockerfile at root (`Dockerfile.dev`). `test` script in
    web+hiring only. `e2e/` exists. Compose anchors: `:151` build arg, `:160` runtime env, `:236` at
    `0dab54d`, `:110` backend profiles, `:165-167` depends_on. `Makefile:119-120` =
    `--profile core --profile frontend`.
13. **`roadrunner.md`'s repo facts** (everything except B1/B2): `cmd/root.go:110` `PORT`→8080,
    `:84` `RPC_PORT`→8081; `go 1.25.0`; `asynq v0.25.1`; `internal/worker/worker.go:25`
    `Concurrency: 10`; `runner.go:126` `MaxRetry(3)` on `queues.DefaultQueue` =
    `"roadrunner:default"`; `tasks.RoadrunnerSubmissionResult = "roadrunner:submissionresult"`;
    `maxRetries = 15` at `:244` with `time.Sleep(1 * time.Second)` at `:273`; **zero** `*_test.go`
    files against `Dockerfile:18` `RUN go test -v ./...`. Platform history: `repos.yml` @ `2adcf71`
    had **9** entries with roadrunner at `:29-31` and jobsimulation at `:17-19`; the jobsimulation
    compose service was at `:83` with `profiles: [graphql, jobsimulation, all]` (`:140`); the
    roadrunner block published `10400:10400`/`10401:10401` at `:291-292` and set `PORT` `:298` /
    `RPC_PORT` `:302` inside `:296-302`. `d11a403`'s **diff** deletes `- name: cms`,
    `- name: jobsimulation` **and** `- name: roadrunner` while its **message** says roadrunner's entry
    *"was already gone"* — the corpus's "grade that commit by its diff, not its message" is verified.
    `repos.yml` had **6** entries at `0dab54d` and has **4** now. `ROADRUNNER` absent from
    `.env_example` at both `0dab54d` and `0c91421`.
14. **`intelligence.md` (all 18 lines).** `fdfa189` = *"chore: remove intelligence service from local
    dev orchestration"* (2026-04-17); the sibling `045857c` = the chronos removal (same day),
    matching `architecture_overview.md:218-219`.
15. **`security_compliance.md`'s EU-AI-Act and residency anchors.**
    `validation/v3/validator/skills.go:53-62` `calculateSkillScore` counts booleans, `:75` divides;
    `basevalidator/criterion.go:127` `case check.EngineLlm:` is the real dispatch, `:428`
    `NewLLMBulkChecker`, and `templates/checkValidationBulk.tmpl` exists and does contain the
    `<asset>` framing; `:168` dispatches `validateCodeDiff`, `:450-475` sets `success` from a pure
    string comparison with no model — so "most, not all" is the right word. `checkerEngines` is
    assigned at `criterion.go:90` and never read there. `ai.go:85-88` pins Bedrock to `eu-west-1`;
    `isThrottlingError` `:129` applied at `:166` and `:325`. `coursebuilder/bedrock.go:109-112`
    (`newUnderlyingClient` → `NewAnthropicClientWithModel`) and `:100` (`return "anthropic-api"`),
    logged at `main.go:770`. All 30 `*_AI_*_MODEL` lines across the three `app/studio/configs/*.ini`
    pin `azure` — the "fifth arm is latent" claim, ENUMERATED. Org-switch anchors resolve in **both**
    apps: `apps/{web,hiring}/src/hooks/useOrgSelection.tsx:94`, `useResolveActiveOrg.tsx:107`,
    `useActivateMembershipOrg.tsx:81`, each a `clerk.setActive({ organization })`.
16. **`dependency_map.md` remaining anchors.** `docker-compose.yml:48`, `:101-109` (the whole of
    `backend`'s `depends_on`), `:102-103` (*"storage, messenger and customerio-sync are not services
    any more…"*, quoted verbatim and correctly), `:138-140`, `:71`, `GOTENBERG_URL` at `:57`;
    `app/internal/converter/gotenberg.go:31` `/forms/libreoffice/convert`;
    `app/go.mod:20` taxonomy direct; `sentinel/go.mod:21` taxonomy `// indirect`;
    `DIRECTUS_WEBHOOK_SECRET` at `app/main.go:1089` with the route at
    `internal/web/backend/backend.go:324`; `AI_PROVIDER_CHAIN` real in `studio-desk`
    (`.env.example:57`, `CLAUDE.md:65`, `terraform/main.tf:103`). Cross-file citations
    `external_services.md:579`, `:602-607`, `:567`, `coursebuilder.md:48`, `storage.md:40,47` all
    resolve to text that says what the citing sentence claims.

### RE-DERIVED — arithmetic the document itself displays (weakest form; listed separately on purpose)

- `architecture_overview.md:73-77` "**Eight** former microservices now run inside Backend/App" — the
  named list has eight members. (The membership of each is separately ENUMERATED above via
  `repos.yml`/compose; only the addition is re-derived.)
- `security_compliance.md:91-100` "16 + 7 = 23" — re-derived, but I state the ENUMERATED clearance
  above instead: I derived both sets from source and compared the 16 filenames one by one.
- `dependency_map.md:31` "7 compose services in the effective topology (5 declared)" — re-derived
  from the two files, and the counterfactual "10 and 6 at `0dab54d`" re-derived at that ref.

---

## What I could not settle

- **`≥42,790 skills / ≥22,470 job roles`** (`architecture_overview.md:13, 236`). A database
  measurement; no DSN is in scope for this reading. Settled only by a `SELECT count(*)` against
  `public.skills` / `public.job_roles` on the public subset, with the capture date stated.
- **Every GitHub *archive* claim.** `graphql-wundergraph` archived 2026-07-30
  (`architecture_overview.md:3`, `dependency_map.md:23`); skiller 2026-07-01 / skillpath 2026-07-31 /
  chronos NOT archived / roadrunner NOT archived (`roadrunner.md:29-30, 42-44, 66`). A clone cannot
  see its remote's archive bit. Settled by `gh repo view <r> --json isArchived,archivedAt`.
  `architecture_overview.md:20` and `roadrunner.md:39` explicitly *withhold* the jobsimulation
  archive assertion on exactly this ground and cite the four 2026-08-04 `origin/main` commits as the
  contrary evidence — which I confirmed (all four dated 2026-08-04, PR #439 among them). That is the
  correct posture and I note it approvingly rather than as a gap.
- **`skiller_rpc_addr = http://backend.internal.anthropos:8081`** (`dependency_map.md:27`). The
  literal appears in no clone; `messenger/terraform/main.tf:78-79` passes
  `${var.skiller_rpc_address}` and `variables.tf:87` declares it, but the *value* is supplied by the
  `infrastructure` repo, which has never been in any clone set. `dependency_map` routes the
  derivation to `backend.md`, which is outside my file set.
- **Compliance facts with no code surface**: DPA v1.4, "18 approved sub-processors", 90-day
  auto-deletion, the sub-processor list (`security_compliance.md:7, 259-272`); the VPC CIDR/subnet
  layout, TLS 1.3 cipher list, KMS/AES-256 at rest, 6-hourly three-site backups, 30 s ECS health
  checks (`:21-25, 38-43, 155-162, 173`; `architecture_overview.md:404-410`). All live in
  `infrastructure`/contracts, neither cloned. The EU-AI-Act *legal* conclusion is explicitly routed
  to counsel by the document itself and I did not attempt it — only its stated technical premise,
  which I confirmed is false in the way the blockquote says (and confirmed the "most, not all"
  hedge is the honest one).
- **Ant Academy's DB-authoritative catalog / empty-grid degradation** (`architecture_overview.md:40`).
  I verified only `ant-academy/code/package.json:64` `"next": "^16.2.6"` (major 16, consistent with
  "Next.js 16"). The catalog-source behaviour needs a running stack and is covered by
  `ant-academy.md`, outside my set.
