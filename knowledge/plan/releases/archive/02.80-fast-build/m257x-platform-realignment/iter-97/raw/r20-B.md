# Seat B — reading #20

Corpus @ `00be1ac` (tree clean apart from this iter dir). Ground-truth clones re-verified at open,
all matching the ADDENDUM sheet:
`platform 0c91421d` · `app b948604f` (origin/main `2035f9a4`) · `app/studio aeec036a` ·
`cms ca50c817` · `cms/studio aeec036a` · `next-web-app bb3313bc` (origin/main `8297c684`) ·
`sentinel 88bc5592` · `storage 4ce8ece5` (origin/main `9f8cb532`) · `messenger fa47850d`
(origin/main `e9421c68`) · `jobsimulation 462343b0` (origin/main `82cb66ec`) · `roadrunner 87d8d443` ·
`graphql-wundergraph 60c229f3` · `studio-desk 14a5442a` · `ant-academy 9c3843cd` ·
`.agentspace/rosetta-extensions 6130bfd8`.

Search set enumerated per the ADDENDUM: 15 git trees under `stack-demo` (nested `app/studio` and
`cms/studio` included), each grepped at **its own** ref, every absence claim run with a positive
control in the same pass. One control failure was caught and repaired mid-pass (see *Instrument
notes*).

## Positive controls

| file | `wc -l` | read in full? |
|---|---|---|
| `corpus/services/ai-readiness.md` | 651 | yes |
| `corpus/services/backend.md` | 318 | yes |
| `corpus/architecture/platform-migration-status.md` | 307 | yes |
| `corpus/services/sentinel.md` | 166 | yes |
| `corpus/services/customerio-sync.md` | 147 | yes |
| `corpus/services/README.md` | 83 | yes |
| `corpus/architecture/README.md` | 38 | yes |
| (protocol, read first) `corpus/ops/platform-alignment.md` §5 | 2178 total | §5 rules 1–35 read; 36+ not reached — see *What I could not settle* |

Total assigned: 1,710 lines. Confirmed.

---

## Blockers

### B1 — `backend.md` cites a top-level `migrations/` directory that has not existed since 2026-06-18

- **Anchor:** `corpus/services/backend.md:299` — *"Versioned Atlas migrations live in
  `terraform/migrations/` (per `atlas.hcl`: `dir = "file://terraform/migrations"`, source
  `ent://internal/data/ent/schema`), **not in the top-level `migrations/` dir (which holds only
  `atlas.sum`)**."*
- **Why it is false:** the block names no ref, so it grades at the checkout `b948604f`. There is no
  top-level `migrations/` directory there:
  `git -C stack-demo/app ls-tree --name-only b948604f` returns
  `… atlas.hcl cmd … knowledge main.go rpc.go … terraform testdata version.go` — no `migrations`.
  Nor at `origin/main 2035f9a4` (same enumeration, plus the v9.0 files). It was deleted at
  `app 6a46e8445` (2026-06-18) — subject *"chore(migrations): remove obsolete atlas.sum file"*,
  path `migrations/atlas.sum`. The only `atlas.sum` in the tree is
  `terraform/migrations/atlas.sum` (`git -C stack-demo/app ls-tree -r --name-only b948604f | grep atlas.sum`
  → exactly one hit). The operative half of the sentence is correct and re-verified:
  `atlas.hcl:15` `dir = "file://terraform/migrations"`, `:12` `src = "ent://internal/data/ent/schema"`,
  and `terraform/migrations/` holds 171 entries.
- **Why I hesitated:** the sentence's *actionable* half is right, and the falsehood sits in a
  disambiguating negative aside. Booked as a BLOCKER because it asserts the present existence of a
  path (and its contents) that the ground truth says was deleted seven weeks before the audited ref —
  the briefing lists "a named … path that does not exist" as a BLOCKER, and a reader cleaning up
  "the obsolete top-level atlas.sum" would be chasing a file that is already gone.
- **Confidence:** high on the ground truth; medium on the grading.

### B2 — `services/README.md` asserts a customerio-sync **production** fact that `customerio-sync.md` (and the map) explicitly refuse to assert

- **Anchor:** `corpus/services/README.md:11-13` — *"**Seven** services in this index — skiller,
  skillpath, jobsimulation, cms, storage, messenger and **customerio-sync** — are **folded into
  `app`** and **no longer deploy separately**."*
- **Contradicted by, in my own file set:**
  - `corpus/services/customerio-sync.md:132-134` — *"**Scope note:** whether its own ECS task / image /
    terraform module have been torn down was **not measured** in this pass — the fold and the
    container deletion are local-compose and `app`-source facts. **Do not read them as the production
    teardown.**"*
  - `corpus/architecture/platform-migration-status.md:101` — *"**The prod half is asserted from
    `app`'s side only:** the standalone's own terraform lives in a repo that has never been in the
    clone set and that this map has therefore never read — the same gap the `roadrunner` row
    carries, recorded rather than papered over."*
- **Why it matters:** the README's own framing separates the local claim (*"none of them starts a
  container any more"*, `:20`) from the deployment claim, so `:13` is doing prod work. It is exactly
  the "report both, assert neither" discipline the other two files apply — and the `roadrunner` row
  in this same README (`:15-17`) proves the distinction is live: a service can be code-folded and
  still deploy (`roadrunner/terraform/main.tf:19` `service_desired_count = 1`, verified; `git blame
  -L 19,19` → `84a4b4f` 2025-12-15, untouched to HEAD `87d8d44`). The customerio-sync repo has never
  been cloned, so no instrument in this environment can settle it either way — which is precisely
  what the other two files say and this one does not.
- **Why I hesitated:** the six other members of the list *are* settled (`cms/terraform/main.tf:39` = 0,
  `messenger/terraform/main.tf:29` = 0 @ `e9421c68`, `storage`'s module block deleted @ `9f8cb532`,
  `jobsimulation`'s deleted at `6092c6d2`, skiller/skillpath archived), so only one of seven is
  over-claimed. Booked because it is the "corrected at one site, left standing at another" class the
  ADDENDUM warns is 8-of-9 of the induced residual, and because the README is the enumerated index
  other docs are told to trust.
- **Confidence:** medium-high.

### B3 — `ai-readiness.md` anchors `AI_READINESS_URL` at a line that holds a different constant

- **Anchor:** `corpus/services/ai-readiness.md:304-305` — *"`AI_READINESS_URL`
  (`packages/core-js/src/constants/urls.ts:52`)"*.
- **Why it is false:** the block names no next-web ref, so it grades at the checkout `bb3313bc`,
  where the constant is at **`:50`**:
  `grep -n 'AI_READINESS_URL' packages/core-js/src/constants/urls.ts` →
  `50:export const AI_READINESS_URL = '/ai-readiness';`.
  Line **52** is `export const ORGANIZATION_FEEDBACK_URL = '/enterprise/organization-feedback';` —
  a different route constant, so the anchor *resolves*, is non-blank, and names the wrong construct.
  It is not rescued by the newer ref either: at `origin/main 8297c684` it is at `:51`
  (`git show 8297c684:packages/core-js/src/constants/urls.ts | grep -n AI_READINESS_URL`). Neither
  available ref gives `:52`.
  For contrast, every *other* anchor in the same bullet is exact and was opened:
  `useNavbarSections.tsx:4` (the import), `:398-400` (`const aiReadinessMenuItem` → `key:
  AI_READINESS_URL`), `:547` (`showAIReadiness ? aiReadinessMenuItem : null`).
- **Why I hesitated:** two lines of drift, and the surrounding claim (only the navbar links
  `/ai-readiness`) is true. Booked because the briefing's GRADING section names "wrong line anchors
  that point at the wrong construct" as a BLOCKER example and this one lands on a *different named
  export*, which is the failure §4-assertion-F of the map exists to catch in compose files.
- **Confidence:** high on the fact; medium on the grading.

---

## Minors

- `corpus/architecture/platform-migration-status.md:205` — *"every other `file:line` … (**66**
  citations)"*. The fence's own live output disagrees: running the documented command
  (`PLATFORM_REPOS_YML=stack-demo/platform/repos.yml python3
  .agentspace/rosetta-extensions/stack-core/platform_alignment_guard.py`) prints
  `assertion F resolved 92 citation(s) — 22 subject-checked, 69 range-only, 1 outside any service
  block; 0 unresolvable`, exit 0. `:204`'s **23** reconciles (22 subject-checked + 1 outside-block);
  the range-only figure is **69**, not 66. Non-material: no category assignment changes, and the doc
  itself says the reach is printed on every run. (Also worth noting for the next repairer: the
  "1 outside any service block" bucket has no row in this table.)
- `corpus/services/sentinel.md:43-44` — *"only as a fixture string in sentinel's own tests
  (`internal/authorization/casbin_test.go`, `internal/rpcsrv/rpc_test.go`)"*. Omitted list member:
  `git -C stack-demo/sentinel grep -n '"manager"' 88bc5592` also returns
  `internal/authorization/manager_test.go:21` — `manager := "manager"`, used at `:31`/`:38` as a
  `g2` role fixture exactly as `casbin_test.go` does. (`manager.go:20` is a logger label, correctly
  excluded.) The substantive claim — 0 occurrences in `init_policy.sql`, positive control `admin`
  = 46 — holds.
- `corpus/services/ai-readiness.md:307-308` — *"A repo-wide grep finds the constant in exactly those
  two non-`node_modules` files."* Measured at `bb3313bc`: three —
  `packages/core-js/src/constants/urls.ts`, `packages/ui/src/NavBar/useNavbarSections.tsx`, and
  `knowledge/ai-readiness/frontend-architecture.md:15` (a prose mention in the platform's own KB).
  Non-material if "the constant" is read as "code use".
- `corpus/services/ai-readiness.md:239-243` — the M407/M408 email-preview / email-override endpoints
  are listed under a bullet headed *"REST/workforce API (`app/internal/web/backend/api/`)"*, but they
  are registered in `app/internal/web/backend/emailpreview/handler.go:71-73`
  (`GET`/`PUT`/`DELETE /ai-readiness/email-overrides/:emailType`), attached from
  `internal/web/web.go:162`, and appear in **no** `api.yaml`/`server.gen.go` route. The endpoints
  exist; only the directory attribution is loose.
- `corpus/services/ai-readiness.md:581-582` — `computeCycleTotals` cited as
  `how_we_measure.go:253-261`; `:253-254` are the tail of the preceding function and the definition
  is `:256-260` (comment) / `:260` (signature). Range straddles a boundary; still lands on the named
  function. The companion anchor `:285-287` is exact.
- `corpus/services/ai-readiness.md:424` — `queryReadinessStarters` cited as `steps.go:915-938`; the
  function is `:915-939` (the closing brace). Off-by-one.

---

## Positively cleared (audited zeros)

Everything below was opened at the ref stated. Labels are as the briefing defines them.

### ENUMERATED (predicate re-derived from source, cardinality compared before any arithmetic)

- **`platform-migration-status.md:76-79` — the row-set completeness derivation.** Re-ran both
  commands rather than the sums. `git log -p --follow -- repos.yml | grep '^+ *- name:'` → **14**
  distinct names, and they are exactly the 14 listed (app · chronos · cms · graphql-wundergraph ·
  intelligence · jobsimulation · messenger · next-web-app · roadrunner · sentinel · skiller ·
  skillpath · storage · studio-desk). Same command on `docker-compose.yml` → **26** names, including
  the five pre-history ones the doc calls out (`nats`, `web-app`, `chromedp`, `simulator`,
  `realtime`). Every name returned has a row.
- **`repos.yml` membership, at four refs.** `2adcf71` = 9 (app cms jobsimulation sentinel storage
  messenger roadrunner next-web-app studio-desk) → `d11a403`/`ef32d4c`/`0dab54d` = 6 → `838d907`/
  `0c91421d` = **4** (app · sentinel · next-web-app · studio-desk), file 28 lines. This is the
  derivation behind `:25`, `:129-130`, `services/README.md:24-25` and `backend.md:19`; all four
  agree with the file.
- **Compose service cardinality.** `d11a403^` = **11** services + `app-network`; `d11a403` = **8**;
  `0dab54d` = 8 (271 lines); `838d907`/`0c91421d` = **5** — `sentinel backend studio-desk
  next-web-app gotenberg` — plus `common.yml`'s `postgresql` + `redis` = **7 effective**, file
  **186** lines. `838d907`'s stat on that file is 107 changed / net −85, consistent with the doc's
  "+11/−96". Clears `platform-migration-status.md:25-26`, `:90`, `services/README.md:24-25`,
  `backend.md:19`.
- **`MESSENGER_RPC_ADDR` appears in no repo** (`services/README.md:39`). Run per-tree at each tree's
  own ref across all **15** trees including the two nested `studio` repos: **0** files everywhere.
  Positive control in the same pass, same refs: `RPC_ADDR` → app 68, cms 10, jobsimulation 10,
  messenger 2, platform 1; tree sizes 3094/3306/4521/… files, so the pipeline was live. The
  document's own second control also reproduces: `git -C stack-demo/platform log -S 'MESSENGER_RPC'
  --oneline 0c91421d` → **0**, `-S 'SKILLER_RPC'` → **7**.
- **`STORAGE_RPC_ADDR`, both sides of the fold** (`platform-migration-status.md:92`,
  `backend.md:254`). At `2035f9a4`: exactly **3** Go hits, all comments —
  `internal/jobsimwiring/wiring.go:101`, `internal/storagens/callsites_test.go:189`, `main.go:504`
  (*"the standalone service takes no traffic and STORAGE_RPC_ADDR is gone"*). At `b948604`: **15**
  hits, of which **7** are env lookups (`main.go` ×3 at `:446`/`:524`/`:992`, `wiring.go:115`, and
  one each in `cmd/academy-asset-upload`, `cmd/academyImport`, `cmd/import`), the other 8 being 2 doc
  comments, 2 error strings, **3** `t.Setenv` and **1** `t.Fatal` message — the row's unusually
  precise breakdown, including the "counting all four as `t.Setenv` overstates by one" correction,
  is right to the hit.
- **`SkillPathSessionService` = 0 in Go source** (`backend.md:68`). 0 at `b948604` and 0 at
  `2035f9a4`; positive control `JobSimulationService` → 5 files. And the doc's Trap-C claim holds:
  `app/CLAUDE.md:109` and `app/knowledge/architecture.md:28` at `2035f9a` both still list it.
- **The RPC mux** (`backend.md:66`). Opened `main.go:1183-1232` @ `b948604`: `:1187` Users, `:1188`
  Organizations, `:1196` Skiller, `:1204` JobSimulation, `:1228` LabSession — five unconditional —
  and `:1212-1214` `if cmsRPCServer != nil { mux.Handle(cms…) }`. The stale comment quoted at
  `backend.md:241` is verbatim at `:1205-1211`.
- **Redis-stream producer/consumer split** (`backend.md:264`). Enumerated every constructor at
  `b948604`: publishers = `main.go:287` (`serviceName`), `:637` SKILLPATH, `:1039` CMS,
  `wiring.go:127` AI_USAGE, `:180` JOBSIMULATION (+ a DLQ `redisstream.NewPublisher`). Subscribers =
  `:1274` SKILLPATH, `:1276` SKILLER, `:1285` JOBSIMULATION, `:1303` CMS, `:1305` AI_USAGE, `:1320`
  `serviceName`. So app is producer **and** consumer of exactly four application streams and a
  consumer-only of `skiller` — and `SKILLER_STREAM` appears once in Go at `b948604`, at an
  `AddSubscriber`. At `2035f9a4` its only occurrences are in the subscriber stream-name struct
  (`main.go:1537`) and wiring tests; no `NewPublisher` names it at either ref.
- **The supergraph 4 → 3 → 1 chain** (`backend.md:22-26`, `:122-133`, `services/README.md:47`,
  `platform-migration-status.md:96`). `ls-tree` on `schemas/` per commit: `749dc86` (2026-06-24)
  leaves backend + cms + jobsimulation + skillpath = **4**; `7c17e63` (2026-07-21) leaves **3**;
  `915da06` (2026-07-29) leaves `backend.graphqls` alone = **1**, having deleted *both* cms and
  jobsimulation SDLs. At HEAD `60c229f3`: `supergraph-config-prod.yaml` lists `backend` only,
  `schemas/` holds one file, `subgraphs.conf` = `BACKEND=v1.360.0`.
- **The Casbin model shape** (`sentinel.md:26`). Counted from `internal/authorization/casbin.go`
  `ModelConf`: request definitions r,r2…r6 = **6**; policy definitions p,p2…p6 = **6**; role
  definitions g,g2,g3 = **3**; matchers m,m2…m6 = **6**. Each of the six matcher rows in the doc's
  table matches its expression, including `m` 's `TIER_FREE` substitution from
  `authorizationv1.Tier_TIER_FREE.String()` and `m6`'s no-tier `g3` form.
- **`app/internal/data/ent/enum/membership.go:8-15`** = the four `MembershipRole` values (admin,
  member, candidate, content_creator) — exactly the range cited by `sentinel.md:40`.
- **`init_policy.sql`** — `manager` = 0 occurrences (positive control `admin` = 46); `:63-66` is the
  four-line NOTE explaining why `org:feature:taxonomy:write` is not seeded; `:88-118` is the
  content_creator block from its `====` header through its last `p5` row; the dedicated
  `internal/authorization/casbin_content_creator_test.go` exists.
- **AI-readiness table set = 13** (`backend.md:231`, `ai-readiness.md:169-183`).
  `ls-tree internal/data/ent/schema/ | grep ai_readiness` @ `2035f9a4` returns exactly 13 files, and
  the doc's split (the original 9 + recommendations + email_overrides + the notification pair) is the
  correct partition of that set — `live_snapshots` and `text_translations` really are among the 9.
- **The 31-skill / 25.0 denominator** (`ai-readiness.md:252-256`, `:548-568`). `defaults.go` counted
  by hand: **19** entries at weight 1.0 and **12** at 0.5 = 31 nodes, Σ = **25.0**; `defaultReadinessSims`
  = 3 (`{simulation,tech}`, `{simulation,business}`, `{interview,both}`). The displayed arithmetic
  re-derives on that set: 1/25×30 → 1, 9/25×30 = 10.8 → 11, 25/25×30 = 30.
- **`authn` is a dependency of nothing** (`architecture/README.md:21`). Grepped
  `anthropos-work/authn` across the `go.mod` **and** `go.sum` of app, sentinel, storage, messenger,
  cms, jobsimulation, roadrunner: zero. Positive control in the same pass: `app/go.mod:16`
  `github.com/anthropos-work/colony v0.35.2`.
- **`architecture/README.md` completeness** — it enumerates all 10 non-README `.md` files in
  `corpus/architecture/`; `services/README.md` enumerates all 27 non-README/non-TEMPLATE docs in
  `corpus/services/` (29 files − README − TEMPLATE).

### Opened and confirmed exact (anchors + shas; not re-derived sets)

- **Every `app/main.go` anchor in the map's `app`, `storage`, `messenger` and `customerio-sync` rows,
  at the ref each names (`2035f9a4`)**: `:395` `customeriosync.New`, `:504`, `:516`, `:517`,
  `:518-523`, `:524`, `:525`, `:547`, `:690` `skiller.NewSkillerManager`, `:721` `jobsimwiring.Wire`,
  `:751` `skillpath.NewSessionManager`, `:1102` `cmsStorage`, `:1153` `appcms.Wire`, `:1416`,
  `:1445`, `:1450`, `:1471` `msgadapters.Wire`, `:1473` `msgsender.NewFromEnv`, `:1552`, `:285`,
  `:286`, `:394`, and the imports at `:15`/`:62`/`:63`. **All 23 resolve to the named construct.**
  Likewise `terraform/main.tf:181` (`= 1`), `:415-416` MESSENGER_ENABLED="true", `:419-420`
  CUSTOMERIO_SYNC_ENABLED="true", `env_guards.go:92-111` `resolveSubsystemSwitch`,
  `internal/jobsimwiring/wiring.go:101`/`:123`, `internal/storage/service.go:22`/`:24`, and
  `internal/roadrunner/` **absent** from the 74-package `internal/` listing.
- **Every `docker-compose.yml` anchor in my files** at `0c91421d`: `:5`, `:18`, `:28`, `:48`,
  `:59`, `:73-75`, `:82`, `:83`, `:84-92`, `:102-103`, `:110`, `:112`, `:119`, `:135`, `:141`,
  `:143`, `:151`, `:160`, `:168`, `:170-171`, `:183`; plus `common.yml:2`, `:22` (`start_period:
  120s`), `:24`; plus `repos.yml:2-10`, `:9-10` @ `0dab54d` (the infrastructure `services.tf`
  sentence, gone at `838d907`), `:12-13`, `:14-17`, `:18-20`, `:23-25`, `:26-28`.
  `AUTHORIZATION_ADDRESS` really is at `:48` at **both** `0dab54d` and `0c91421d`, and is the only
  occurrence across `docker-compose.yml` + `common.yml` + `.env_example`. `backend.md:162`'s
  "`:110` now, `:100` at `0dab54d`" checks at both refs.
- **`jobsimulation` M810** (`services/README.md:17`/`:35`, `backend.md:12`/`:50-55`,
  `platform-migration-status.md:89`). At the cited `6092c6d2` / `82cb66e`, `service_desired_count`
  is **absent** from `terraform/main.tf` and `:15-22` is the decommission comment naming the deleted
  `module "jobsimulation"`; `:24-40` is the livekit/chime/ssm/atlas-tracker justification. The four
  2026-08-04 commits are exactly `6092c6d2`, `caf36c96` (Merge PR #439, committer `GitHub`),
  `1e40d184`, `82cb66ec`, which is the archive-state argument the row makes. Note it is **not**
  true at the audited checkout `462343b0` (where `:40` still reads `= 0`) — but the cell names its
  ref, so it grades there and is true.
- **`cms` M810 double-signal** (`services/README.md:33`, `backend.md:36-49`,
  `platform-migration-status.md:88`): `cms/terraform/main.tf:39` `service_desired_count = 0` at the
  checkout, **and** `6efa1d5` (merged `f38c0c4`, 2026-08-04) deleting
  `.github/workflows/build-production.yml`, 18 deletions, subject verbatim. Both halves measured;
  the "report both, assert neither" framing is correct.
- **`storage`'s prod state** (`services/README.md:36`, `platform-migration-status.md:92`). At the ref
  the map's cell names (`9f8cb53`): `terraform/main.tf` is exactly **18** lines, declares no module,
  `:9-11` and `:13-16` and `:18` are the quoted comments verbatim; `git -C stack-demo/storage grep -n
  -i custody 9f8cb532` → **0**; the M903 text is at `storage/terraform/storage.tf:22-25` verbatim;
  no `moved` blocks; `d3e6d32` (2026-08-05) contains *"M903 never ran."*; the plan dir exists at
  `app 2035f9a` with `status: planned` and *"Planned, not started — no terraform applied, no branch"*.
- **`messenger`'s prod state** (`platform-migration-status.md:93`): at `origin/main e9421c68`,
  `terraform/main.tf` is **121** lines with `service_desired_count = 0` at `:29`, the reason comment
  at `:19-25` and the rollback sentence at `:27-28` — all exact. `cmd/root.go:120-140` reads all four
  `*_RPC_ADDR` at that ref (`:120` CMS, `:125`/`:130` BACKEND_USERS, `:135` SKILLER, `:140`
  JOBSIMULATION). `internal/flow/flow.go:70-95` is the `AddSubscriber("backend", …)` block.
  (Same ref caveat as `storage`: neither is true at the 7-behind checkout `fa47850d`.)
- **`roadrunner`'s line provenance** (`platform-migration-status.md:90`, `services/README.md:37`):
  `blame -L 19,19` → `84a4b4f1` Mattia Sasso 2025-12-15; `e45eb61` (2026-05-27) is a one-line
  `source =` swap at `:11` whose message says *"Module contents are identical"*; HEAD `87d8d44`
  2026-06-19. `d11a403`'s repos.yml diff does delete `- name: roadrunner` alongside cms and
  jobsimulation, so the row's "the message is wrong; the diff is the fact" holds.
- **16 platform shas + dates** (`a2a3ee6`, `cb6ebf5`, `ef4b449`, `1474b1f`, `84862d1`, `b43b99a`,
  `c17cc9a`, `467965a`, `8770fe6`, `b56d731`, `360efd4`, `21429b7`, `a4db680`, `fdfa189`, `045857c`,
  `236771f`) — every one exists with the subject and date the map gives, including "nats removed four
  days after the first commit" (2023-04-30 → 2023-05-04).
- **`backend.md`'s skiller fact-sheet numerics**: `SKILLER_RPC_ADDR` occurrences in compose =
  **4** @ `0808b92` (`:89`, `:151`, `:204`, `:334`), **1** @ `0dab54d` (`:183`), **0** @ `0c91421`;
  `app/terraform/locals.tf:6` `project = "backend"`, `:8` `rpc_port = 8081`, `main.tf:58` the
  service-discovery registration, `:185-186` the rpc_port container/host mapping;
  `messenger/terraform/variables.tf:77,82,87,92` and `app/terraform/variables.tf:197,230` are all
  `variable "…_rpc_address"` declarations **with no default**; merge commit `1fc00c78` exists with
  the quoted subject; `internal/web/backend/graphql/graph/schemas/skiller_taxonomy.graphqls` exists.
- **`sentinel.md` code map**: `go.mod:3` `go 1.26.0`; `Dockerfile:2` and `Dockerfile.dev:2`
  `golang:1.26-bookworm`; `terraform/locals.tf:4-5` = 256 CPU / 128 MB; `terraform/main.tf:19` = 1;
  binary default port `cmd/root.go:47` `cmp.Or(os.Getenv("PORT"), "8080")` vs compose's `8087`;
  `make initdb` really does hard-code `postgresql://postgres@localhost:5432/postgres?sslmode=disable`;
  `local_superadmin_grants.sql` exists; no `profiles:` on the sentinel compose block.
- **`customerio-sync.md`**: `internal/customeriosync/{doc.go,store.go,sync.go,sync_query.sql}` +
  `cmd/customerio-resync` all exist at `2035f9a4`; `doc.go:4-5` carries *"the last of the Go services
  to be folded into app"*; `env_guards.go:62` is the `CUSTOMERIO_SYNC_ENABLED` const; `main.go:284`
  the fatal-when-deployed comment, `:286` the resolve, `:295` the `BREVO_KEY` fatal, `:393-396` the
  `copilotDB`-backed construction. The "HISTORICAL" compose block at `:60-74` matches
  `0dab54d:docker-compose.yml:136-154` including `context: git@github.com:…#main`, `ports 8080:8080`,
  `DB_CONNECTION_BACKEND`, and it was the only service built from a URL.
- **`ai-readiness.md`'s platform anchors — 30+ opened, all exact.**
  `enum/organization_settings.go:47`; `enum/ai_readiness.go:9-11`;
  `readiness.go:139` `computeTier1` (with the *"a larger configured set makes a full score harder to
  reach"* comment immediately above), `:289` `GetAIReadinessWithOptions`, `:290-297` cycle-scoped
  route, `:307-312` default route, `:314` fallthrough, `:330` `computeOrgBreakdowns`, `:388`
  `keepInCycleStep1`, `:390` `keepStartedMembers`, `:638-652` `queryInCycleStep1Completers`
  (`StepTypeEQ(StepSkillMapping)` + `StatusEQ(StepCompleted)` + `CompletedAtGTE(since)` — the
  conjunction the doc claims), `:662-664`, `:684`; `steps.go:907-914` (*"This DB signal is the only
  real 'has started' check"*), `:915-938` `SELECT DISTINCT user_id … status <> 'not_started'`;
  `how_we_measure.go:285-287`; `scoring.go` `archetypeHighBand = 75` / `archetypeLowCeil = 50` /
  `classifyArchetype` with the exact-tie-to-Explorer rule / bands `0-24 / 25-50 / 51-74 / 75-100`;
  `computeKnowledgeAxis` = `(step1+step2)/70×100` and `computeUsageAxis` = step3×100/30;
  `usageDimSpecs` = the five current ids; `holdsBackFromInsights` filtering on `"risk"`;
  `findingsFromChart(…, "top_unexpected")` / `"top_concerns"` as the *no-narrative* fallbacks and
  the in-code comment *"Unexpected angles have no chart fallback"*; `resolveSessionAuthors` joining
  `job_simulation_sessions → memberships`; `interview_aggregated_report.go` fields
  `(sim_id, organization_id, report JSON, session_count)`; `ai_readiness_cycle.go:120-131` the
  partial unique `IndexWhere("status = 'active'")`; `interaction.go` `source_id <> target_id` CHECK +
  required Actor edges and the two-valued `interactions.ActionType` (`Call`/`Email`, per
  `entconverter/converter_gen.go:29-34`); `csv.go` header = **15** columns with the "19-column spec,
  4 dropped" comment; `RefreshLiveSnapshots`, `CloseDueAIReadinessCycles`, and the
  `ai_readiness_refresh` task type; `emailoverride/` + `emailpreview/`; `narrative.go` `openai.GPT5Mini`;
  `4c28365f` (2026-07-22) with the quoted subject, landing between `v1.351.0` and `v1.351.1`;
  `app/knowledge/ai-readiness/overview.md` + 16 sibling topic docs.
- **`ai-readiness.md`'s next-web anchors at `bb3313bc`** (all exact except B3):
  `useAiReadinessActive.ts:22` `useFeatureFlagEnabled(AI_READINESS_FLAG)` and the sticky-`=== true`
  shape the M219 correction quotes; `aiReadiness.constants.ts:26`;
  `AIReadinessClient.tsx:78` `SHOW_SECONDARY_TABS: boolean = false`, `:133-134`, `:153-154`
  `effectiveCycleId`, `:166-170` gated on `cyclesQ.isFetched`, `:599` the tab filter — and the file
  contains **0** `posthog` and **0** `flag_ai_readiness` matches, exactly as claimed;
  `useNavbarSections.tsx:4/:398-400/:547`; `useWorkforceAIReadiness.ts:23-27` with `searchParams:
  { tag }` and no `cycle`; `WorkforceNewClient.tsx:125-151` = five tabs, no readiness;
  `AIReadinessHero.tsx:88` `if (!air.deadline) return null;`; `useAIReadiness.ts:48-62` `deriveMode`;
  `hooks/useAIReadiness.ts:274` `interviewQuestions`; `HowWeMeasureTab.tsx` = **1,989** lines with
  `grep -c interviewQuestions` = **0** and `:1879`/`:1903`/`:1915`/`:1921`/`:1927` landing exactly on
  the block comment, the label and the three rendered cells; the legacy route directory
  `enterprise/workforce/ai-readiness/` genuinely does not exist; `dae0fb2f7` (2026-07-13) deletes
  `AIReadinessContainer.tsx` (103) + `AIReadinessIntro.tsx` (220) + `AIReadinessView.tsx` (330) =
  **653** lines, matching the doc's "−653" to the line; `e2e/specs/web.ai-readiness.spec.ts` exists.
- **`ai-readiness.md`'s `rosetta-extensions` anchors at the authoring copy `6130bfd8`**:
  `demo-stack/patches/app-aireadiness-snapshot-loadmembers/…yaml:33` (*"v2.7 M254 RE-POINT"*) and
  `:42` (`path: internal/aireadiness/readiness.go`);
  `demo-stack/tests/test_aireadiness_snapshot_loadmembers_m254.py`;
  `stack-seeding/seeders/ai_readiness_interview_report.go:185-189` = the five KPI ids in that order,
  with `:149` reading *"All five are always emitted here."*; `cockpit.go` `LegacyReadinessPaths()`
  (`:135`) + `ValidateCockpitManifest` (`:161`); `seat_append_test.go`;
  `ai_readiness_funnel_test.go:230 TestAIReadinessFunnelSeeder_DayZeroHeroGetsNoSignals`;
  `contentref.go`; `ai_readiness_evidence.go`; `ai_readiness_interview_report_test.go`;
  `stack-verify/e2e/lib/coverage-manifest.ts`;
  `demo-stack/patches/next-web-aireadiness-flag-gate/`; `alignment/` and `stack-seeding/dna/`
  (for `architecture/README.md:13`).
- **The fence itself is GREEN.** `platform_alignment_guard.py` exits 0 against
  `stack-demo/platform/repos.yml`, so §4's assertions A–F all hold on the current map.

### RE-DERIVED only (the weakest clearance — recorded as such)

- `services/README.md:24-25` / `backend.md:19` "5 services, 7 effective, 4 repos.yml entries" — the
  arithmetic reproduces, but the underlying sets are the ENUMERATED items above, so this is
  redundant with them.
- `ai-readiness.md:348-350` "78.4% (≈156 of the 199 frozen snapshots)" — 156/199 = 78.39 %, internally
  consistent; the *set* (what the seeder actually writes) is not measurable without a live stack.
- `backend.md:97-101` taxonomy counts — the two measurements (42,790 / 22,490 on 2026-07-08 vs
  42,790 / 22,470 / 18,919 on 2026-06-29) are mutually consistent as stated and consistent with
  `platform-migration-status.md:110`; neither is re-derivable offline.

---

## What I could not settle

- **Anything requiring the GitHub org API.** `platform-migration-status.md:126-133`'s *"93
  repositories … 4 in `repos.yml`, 46 named by no corpus document"*, the §3 census's 19 rows and
  their last-push dates, and every **archive-state** assertion (`skiller` 2026-07-01, `skillpath`
  2026-07-31, `intelligence` 2026-04-02, `chronos` NOT archived, `graphql-wundergraph` 2026-07-30,
  `roadrunner`/`storage`/`messenger` not archived). Git objects cannot carry archive state — which
  the `jobsimulation` row itself now says. Settled by `gh api repos/anthropos-work/<r> --jq .archived`
  plus `gh repo list anthropos-work`.
- **Every prod-terraform claim about `customerio-sync`** (B2 above) and the `roadrunner`↔platform
  disagreement's resolution. Both need the `infrastructure` repo (never in the clone set) and the
  `customerio-sync` repo (never cloned). The map records this limit; `services/README.md` does not.
- **`storage` and `messenger` prod state at the *checkout* refs.** Both clones are behind
  (`4ce8ece5` is 20 behind `9f8cb532`; `fa47850d` is 7 behind `e9421c68`), and at the checkout
  `storage/terraform/main.tf:19` still reads `service_desired_count = 1` while
  `messenger/terraform/main.tf:19` reads `= 1` in a 111-line file. Every corpus claim I checked is
  true at those repos' `origin/main` and the map's cell explicitly pins `9f8cb53`, so I cleared them
  — but a reader with the audited clones on disk will see numbers that contradict the corpus, and
  `services/README.md:36` and `platform-migration-status.md:93` do **not** name a repo ref for their
  prod sentences. Flagging as an environment/ref hazard rather than booking it.
- **`ai-readiness.md:3-7`'s ship versions** (`app v1.315+`, `next-web-app v2.89.0+`) and the M219/M250
  **live measurements** (2.09 s live recompute, 24 ms frozen read, 180 s → 19 ms patch effect,
  `validation_attempt_results` 5 → 345, "165 rows" of `interview_extraction_results`). These need a
  running demo stack; the doc itself says *"Re-measure before relying on either number."*
- **`platform-alignment.md` §5 rules 36–44.** I read §5 from line 249 through rule 35 (line ~948);
  §5 actually runs to line 1401. The repair commit's own message references a **rule 44** (the
  bare-grep/nested-repo absence rule), whose operative content the ADDENDUM already gave me and
  which I applied. Rules 36–43 were not read; I do not know whether any of them would have changed a
  grading call above.
- **`services/README.md:39`'s "`app` took over messenger's own consumer group"** and the claim that
  no service ever *constructed* a `MessengerService` client. The address absence is ENUMERATED
  (above); "never constructed a client" over the whole org would need the repos that were never
  cloned. Within the 15 audited trees it holds.
