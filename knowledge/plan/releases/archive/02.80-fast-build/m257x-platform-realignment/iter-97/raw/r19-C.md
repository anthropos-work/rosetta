# Seat C — reading #19

Corpus under audit: `rosetta` @ `00be1ac` (per briefing addendum). Ground-truth refs re-derived at open —
`platform 0c91421`, `app b948604f` (origin/main `2035f9a4`, 93 ahead), `app/studio aeec036a`,
`messenger fa47850d` (origin/main `e9421c68`), `studio-desk 14a5442a`, `next-web-app bb3313bc`,
`jobsimulation 462343b0`, `sentinel 88bc5592`, `storage 4ce8ece5`, `cms ca50c817`.
**Deviation from the addendum sheet:** the rext authoring copy measured `bc2ee74`, not `6130bfd8`
(`6130bfd` is its parent — `merge-base --is-ancestor` confirms). All rext citations graded at `bc2ee74`.

Search set enumerated first, nested repos included (15 trees):
`ant-academy · app · app/studio · cms · cms/studio · graphql-wundergraph · jobsimulation · messenger ·
next-web-app · platform · roadrunner · rosetta-extensions(stack) · sentinel · storage · studio-desk`,
plus `.agentspace/rosetta-extensions` (authoring). Every absence claim below was grepped per tree at
that tree's own ref, with a positive control in the same pass.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/architecture/alignment_testing.md` | 542 | yes |
| `corpus/services/clerkenstein.md` | 370 | yes |
| `corpus/architecture/ai_architecture.md` | 305 | yes |
| `corpus/services/messenger.md` | 189 | yes |
| `corpus/services/coursebuilder.md` | 140 | yes |
| `corpus/services/gotenberg.md` | 84 | yes |
| `corpus/services/db-backup.md` | 31 | yes |

(Also read in full as instructed: `corpus/ops/platform-alignment.md` §5, lines 249–1408.)

## Blockers

### B1 — messenger's `backend` subscriber: wrong handler count AND a line range that stops mid-block

- **Anchor:** `corpus/services/messenger.md:17` —
  > "Callers don't talk to Brevo directly — they **publish Redis Stream events that the messenger flow
  > consumes** (`messenger/internal/flow/flow.go:70-95` adds a subscriber on the `backend` stream with
  > **20 handlers**; `app` runs that same subscriber now, on messenger's own consumer group)."

- **Why it is false:** the block names no ref, so graded at the checkout `fa47850d`; I additionally
  re-measured at `HEAD`, `origin/main` and `e9421c68` and the answer is identical at all four.

  `messenger/internal/flow/flow.go:70` is `func (h *Manager) Subscribe()`. The `backend` subscriber is
  `h.subServer.AddSubscriber("backend", sub.AddHandler(…))` spanning **`:72–104`**, and it registers
  **21** live `pubsub.EventHandler` entries, not 20:

  | group | lines | n |
  |---|---|---|
  | skill paths | `:74,75,76,77,78` | 5 |
  | job simulation | `:80,81,82,84` (`:83` is **commented out**) | 4 |
  | content-agnostic assigned/completed | `:86,89` | 2 |
  | organization | `:91,92` | 2 |
  | AI-readiness completed | `:94` | 1 |
  | AI-readiness notifications | `:96,97,98,99` | 4 |
  | course builder | `:101,102,103` | 3 |
  | **total** | | **21** |

  Independent cross-check that does not reuse the same enumeration: the whole `Subscribe()` body holds
  **24** live handlers and **1** commented one — 21 (`backend`) + 2 (`jobsimulation`, `:105–108`) +
  1 (`cms`, `:109–111`) = 24. ✓

  The cited range `:70-95` is also wrong on its own terms: `:95` is a *comment* line
  (`// AI Readiness Notifications (M400 stubs — bodied in M402/M404)`) sitting inside the argument list,
  and the range encloses only **14** handlers — so it neither bounds the construct nor yields the
  stated 20. No ref, spelling or scope I tried reproduces 20 (`git log` over `internal/flow/flow.go`
  shows the count going 11→…→21; 20 never occurs at any recent commit).

  A reader auditing "which events reach the mail path" is handed a range that silently truncates the
  entire AI-readiness-notification and course-builder blocks — 9 of the 21 handlers.

- **Confidence:** high.

### B2 — `coursebuilder.md` asserts the app's **current** version is `v1.363.2`, six releases behind, and contradicts its own pins

- **Anchor:** `corpus/services/coursebuilder.md:130-131` —
  > "**Recently added + heavily iterated** — developed through numbered Waves 1→24 (142 changelog
  > lines); current app **`v1.363.2`** @ `5ba17044` (2026-07-31)."

- **Why it is false:** `5ba17044` really is `v1.363.2` (`git -C stack-demo/app tag --points-at 5ba17044`
  → `v1.363.2`), so the pin is internally consistent — but the sentence does not merely pin, it asserts
  **currency**, and that is false:
  - the ground-truth `app` **checkout** `b948604f` is tagged **`v1.366.0`** (`tag --points-at b948604f`);
  - `origin/main` (`2035f9a4`) is **93 commits ahead** of the checkout and carries no tag;
  - the newest three tags in the clone are **`v1.369.0`, `v1.368.0`, `v1.367.0`**
    (`git tag --sort=-creatordate | head -3`).

  Per `platform-alignment.md` §5 rule 33, *"a block that asserts currency (**current**, not stale, today)
  cannot be pinned into silence, because the pin and the word are making opposite claims and only one of
  them is what the reader acts on."* This is that exact shape.

  It is also a **self-contradiction inside the same file** (rule 21's cheapest class): three other
  passages of `coursebuilder.md` pin their claims at a *different, later* app version —
  `:48` (*"logged at `main.go:770` @ `app` `b948604` v1.366.0"*), `:71` (*"`main.go:766-779` @ `app`
  `b948604` v1.366.0"*) and `:99` (*"`main.go:824-826` @ `app` `b948604` v1.366.0"*). A reader is told
  in one paragraph that the app is at v1.366.0 and in another that it is *currently* at v1.363.2.

- **Confidence:** high. I hesitated only because a charitable reading treats "current" as "as of this
  doc's measurement" — but the file's own neighbouring pins say v1.366.0, which removes the charity, and
  §5 rule 33 rules on the word explicitly.

## Minors

- `corpus/services/messenger.md:108` — *"the binary still constructs **four** Connect-RPC clients — CMS,
  backend users + organizations, skiller, and jobsimulation (`cmd/root.go:118-142`)"*. That range
  constructs **five** client objects (`cmsV1Client :118`, `usersClient :123`, `organizationsClient :128`,
  `skillerClient :133`, `jobsimulationClient :138`) over **four** distinct env addresses. The prose
  groups "users + organizations", so the reader is not badly misled, but "four … clients" is literally an
  undercount.
- `corpus/services/coursebuilder.md:114-115` — *"~55 test files"* / *"~25 boundary test files"*.
  Measured at `b948604f`: **61** `_test.go` under `internal/coursebuilder/` and **33** under
  `internal/web/backend/coursebuilder/`. Both hedged with `~`, but the second is a 32 % undercount.
- `corpus/services/coursebuilder.md:77-79` — the SSE `event:` name list omits `steering_received` and
  `steering_applied` (`internal/web/backend/coursebuilder/handler.go:2744`, `:2754`), both live wire
  names on the same `/messages` stream the list claims to enumerate.
- `corpus/services/coursebuilder.md:130` — *"(142 changelog lines)"* does not reproduce at the ref the
  sentence names. At `5ba17044`, `internal/coursebuilder/SPEC.md`'s `## Changelog` section runs `:19-1524`
  (1,506 lines / 58 dated entries); app-root `CHANGELOG.md` has **145** coursebuilder-mentioning lines
  there and 147 at `b948604f`. Nothing measures 142.
- `corpus/architecture/ai_architecture.md:235` — *"the enum is `…/jobsimulation.go:983-990`"*. The
  `AIModel` const block is `:979-991` with its 11 members at `:980-990`; the cited range drops
  `Anthropic35SonnetAws` `:980`, `Anthropic37SonnetAws` `:981` and `Anthropic4SonnetAws` `:982`. The
  three examples the sentence gives all fall inside the cited range, so the anchor is usable, but a
  reader following it to "the enum" sees 8 of 11 members.
- `corpus/architecture/ai_architecture.md:34-35` — *"`studio/tools/pdf2md.py:24` (`mistral-ocr-latest`)"*.
  `:24` is `from mistralai import Mistral`; the string `mistral-ocr-latest` is at `:127`
  (`model="mistral-ocr-latest",`). Right file, right claim, the parenthetical is at a different line.
- `corpus/architecture/ai_architecture.md:99` — *"this line went on publishing it **68 lines below**"*.
  The retraction it refers to is at `:15-17`; this line is `:99`, i.e. 84 lines below. Line drift from a
  later repair (§5 rule 34).
- `corpus/services/clerkenstein.md:3` — *"**Last updated:** 2026-07-14"* and a Status list ending at
  "M218", while the body documents M219 (`:54-62`), M220 (`:305-312`), M224 (`:232-255`), "v2.8 M256"
  (`:160-161`, `:184`) and a section explicitly dated `2026-07-27` (`:112`).
- `corpus/architecture/alignment_testing.md:3` — *"**Last updated:** 2026-06-06"* while the body
  documents M218 (`:184`), M219 (`:276`, `:287`), M23 (`:483`) and a "Corrected M257x iter-85" note
  (`:377`).
- `corpus/services/clerkenstein.md:21` — the monorepo sections are listed as six
  (`clerkenstein, demo-stack, stack-injection, stack-core, stack-seeding, alignment`). The authoring copy
  has **11** section dirs at `bc2ee74` (adds `dev-stack`, `playthroughs`, `stack-secrets`,
  `stack-snapshot`, `stack-verify`); root `CLAUDE.md` lists nine.
- `corpus/services/clerkenstein.md:105` — the `cmd/` row lists `mintpk` / `fake-fapi` / `fake-bapi`;
  `clerkenstein/cmd/` also ships **`jwtkey`**.
- `corpus/architecture/alignment_testing.md:538` — the Layout block lists `internal/{dna,outcome,compare,report}`;
  `alignment/internal/` also holds **`canon`**.
- `corpus/architecture/alignment_testing.md:333` — link text says `clerkenstein/knowledge/alignment.md`
  but the href targets `../services/clerkenstein.md` (a different document).
- `corpus/architecture/alignment_testing.md:534` — `dna.go:20-26` covers `list`/`diff`/`validate` plus the
  `case "coverage":` line but stops before its body (`:26-27`).
- `corpus/services/db-backup.md:27` — *"runs in production **and staging** environments"* vs four corpus
  sites labelling it **Production-only** (`service_taxonomy.md:135,:482`, `platform-migration-status.md:102`,
  `architecture_overview.md:81`, `services/README.md:41`). Reconcilable — those sites gloss
  "Production-only" as "not in local compose" — so booked MINOR rather than a contradiction.

## Positively cleared (audited zeros)

**ENUMERATED** (predicate enumerated independently from source; cardinality stated before any arithmetic):

- `messenger.md:19-22` — *"`MESSENGER_RPC_ADDR` occurs in **no** repo"*. Grepped the exact string across
  **all 15 git trees at their own HEADs**, nested `app/studio` and `cms/studio` (`aeec036`) included:
  **0/15**. Positive control in the same pass (`REDIS`) returned 3–21 files per non-trivial tree. The
  looser `MESSENGER_RPC` returns 2 hits, both prose in `app/knowledge/plan/roadmap-support-in-app.md`
  (an in-`app` doc, not a read site) — consistent with the claim's wording.
- `messenger.md:23-27` — the `-S` history controls. `git -C stack-demo/platform log -S 'MESSENGER_RPC'
  --oneline 0c91421d | wc -l` → **0**; `-S 'SKILLER_RPC'` → **7**; over a **121**-commit history
  (`rev-list --count 0c91421d` = 121); `--all` → **8** and **0**, and the 8th is `464dfe3`
  ("compose: remove skiller/jobsimulation/cms/skillpath services"), off the `0c91421d` ancestry. Every
  figure in that blockquote reproduces exactly.
- `messenger.md:30, :108, :153-157` — *"since `838d907` no compose file sets any `*_RPC_ADDR` at all"*.
  `git -C platform grep -n '_RPC_ADDR' HEAD` returns only two prose lines in `platform/CLAUDE.md`;
  `docker-compose.yml`, `common.yml`, `.env_example` set **zero**. Positive control:
  `AUTHORIZATION_ADDRESS=http://sentinel:8087` at `docker-compose.yml:48`. Also enumerated the
  pre-deletion state at `0dab54d` (`:173,174,176,183` — all four `http://backend:8083`, on the
  `messenger` block, `profiles: [messenger]` at `:195`, `depends_on: backend` at `:191`).
- `messenger.md:122` — *"starts only the always-on floor (`postgresql`, `redis`, `sentinel`)"*. Derived
  the floor as *services declaring no `profiles:` key*, resolving compose's `include:` (§5 rule 30's
  corollary): `docker-compose.yml` declares `profiles:` on backend `:110`, studio-desk `:141`,
  next-web-app `:168`, gotenberg `:183` — `sentinel` has none; `common.yml` declares none on `postgresql`
  or `redis`. Floor = exactly {postgresql, redis, sentinel}.
- `messenger.md:43` — *"nothing in terraform ever set `SERVICE_NAME` for it"*. `git -C messenger grep -n
  'SERVICE_NAME' e9421c68 -- terraform/` → **0** (control: `REDIS_ADDR` → 1 file in the same dir);
  `main.go:16` reads `cmp.Or(os.Getenv("SERVICE_NAME"), "messenger")`. Note compose *did* set it at
  `0dab54d:182` — the claim correctly says *terraform*, not compose.
- `alignment_testing.md:261-265` + `clerkenstein.md:41-44,:74` — DNA gene/capability counts. Parsed all
  five JSON DNAs and counted capabilities and variants independently:
  `clerk-2.6.0` **14 caps / 27 genes**, `clerk-js-5` **6 / 9**, `clerk-multi-1` **5 / 9**,
  `clerk-deploy-1` **3 / 7**, `clerk-express-1` **5 / 13**. All five rows in both documents match exactly.
  Also enumerated `consumed_surface` presence: only `clerk-2.6.0` carries the key — confirming
  `alignment_testing.md:208`.
- `alignment_testing.md:300-316` (the toy example) — parsed `alignment/examples/toy/dna.json`
  independently: **6 genes** (`Add` critical ×3, `Greet` standard ×3), sum weight **15**. With only
  `Greet/padded-name` (w2) failing: overall `13/15` = **86.7 %**, critical `3/3` = **100 %**, 5/6 genes —
  byte-for-byte the transcript in the doc.
- `alignment_testing.md:475-486` — the six snapshot operators. `stack-seeding/dna/snapshot.go:62` is
  `validSnapshotOperators`, whose map holds exactly `snapshot-row-count`, `snapshot-structural`,
  `snapshot-referential`, `snapshot-embedding-dim`, `snapshot-public-only`,
  `snapshot-cross-surface-closure` (`:63-68`) — six, named exactly as the corpus lists them, and kept
  separate from `validOperators` as the doc claims (`:59-61`).
- `alignment_testing.md:433-434, :496-505` — parsed `stack-seeding/dna/data-dna.json`: **10** surfaces,
  status census `seeded-m7a`:4 · `seeded-m7c`:4 · `snapshot-seeded-m9b`:1 · `snapshot-seeded-m10`:1 —
  **0 waived**, confirming *"4 seeded in M7a, ~6 planned"* and *"NOTHING is left waived"*. `:410`
  (taxonomy) lists exactly the 5 operators minus `cross-surface-closure`; `:439` (content) lists exactly
  5 including `cross-surface-closure` and excluding `embedding-dim`. Both anchors land on the
  `"operators"` key itself.
- `ai_architecture.md:99` — *"not a bare `app/internal/ai/ai.go` (**no such file** — that path now
  resolves only inside the frozen `jobsimulation` husk repo)"*. `ls-tree -r` for `^internal/ai/ai\.go$`
  across all 15 trees at their own refs: exactly **one** hit, `stack-demo/jobsimulation` (`462343b0`).
- `ai_architecture.md:33-37` — *"`git -C app/studio grep -i mistral aeec036a`, 22 hits / 3 files"*. Ran it:
  **22 hits, 3 files** (`requirements.txt` 1, `tools/pdf2md.py` 18, `tools/r3.py` 3) — exact. Also
  enumerated the Go side: `internal/cms/studio/markdownManager.go:19` and `studioManager.go:583` both
  land on `os.Getenv("MISTRAL_API_KEY")` calls, and every `mistral` hit in `app/internal` is OCR.
- `ai_architecture.md:48-49` — *"`grep -rin 'bedrock\|boto3' app/studio/` returns 0 hits"*. Measured at
  the nested repo's own ref `aeec036a`: **0**, with a positive control (`openai`) returning 20 files in
  the same pass.
- `ai_architecture.md:116-135` — the studio slot table. Enumerated `_MODEL` lines in **all three** config
  files: `production_config.ini:26-36` matches the table row-for-row; `development_config.ini:26-36` is
  byte-identical (`diff` empty); `config_template.ini:39`/`:40` are the two `gpt-4o` `*_STABLE_MODEL`
  slots, and `gpt-4o` appears in **no** shipping-config slot. `gpt-5.2` appears in no config at all
  (`services/ai.py:356,508` are the pricing entries as claimed).
- `ai_architecture.md:62-66` — *"five members / four cases"*. Enumerated `AIVendor` in
  `internal/cms/directus/collections/jobsimulation.go:969-975`: Azure, **Azureglobal `:971`**, Openai,
  AnthropicAws, Anthropic = **5**; enumerated the switch in `simulator/ai/ai.go`: `:58`, `:69`, `:86`,
  `:102` = **4** cases + `default:` `:114-115`. `azureglobal` is the member with no case. ✓
- `ai_architecture.md:278-281` — *"There is no 60/65/75/85/95 threshold ladder … does not exist in `app`,
  `cms`, `jobsimulation` or `next-web-app`"*. Grepped the literal ladder form per tree at its own ref
  across all four **plus** the nested `app/studio`: **0/5**, with a positive control (`60` in the file the
  doc names as the real mechanism) returning a hit. Caveat recorded below.
- `gotenberg.md:7, :42, :47` — enumerated `ConvertToPDF` at `9d00a313`: **2** non-test call sites in the
  whole repo (`extract.go:77`, `user_import_resume_2d.go:68`) plus the definition and one markdown
  mention — so *"the only consumer"* and *"call sites (two)"* both hold. `extract.go:17-27` is the
  `gotenbergMimes` map with exactly **9** entries (`:18-26`), and DOCX
  (`…wordprocessingml.document`) is genuinely **absent** from it.

**RE-DERIVED** (arithmetic the document itself displays, recomputed):

- `gotenberg.md:14` — `docker-compose.yml:183` is `profiles: [core, backend, all]` in a **186**-line file
  at `0c91421`; the historical anchor `:268` at `0dab54d` also resolves, and `838d907` did delete three
  service blocks (storage, messenger, customerio-sync).
- `coursebuilder.md:101-103` — `budget.go:27` `DefaultMaxMonthlyCOGSUSD = 500.0` and `:41`
  `DefaultMaxDailyCOGSUSD = 0.0` (daily disabled by default). The corpus is **right** and the in-repo
  `SPEC.md:302` (*"default `50`"*) is the stale one.
- `coursebuilder.md:103` — `internal/credits/cost.go:86-90` `creditCost` map: build **5**, refine **1**,
  translate **1**. The corpus is **right** and `cost.go:29`'s own package doc (*"refine → 5"*) is the
  stale one — worth noting because the visible comment is the attractor here.

Anchors I opened and found correct, with the lines around them read (not exhaustive):
`app/main.go:15,:62,:63,:295,:1095,:1205-1211,:1416-1421,:1450,:1471,:1473,:1478-1484`;
`app/env_guards.go:61` (resolves at `origin/main` only — see below);
`messenger/terraform/main.tf:29,:27-28` (at the banner's named ref `e9421c6`; at the checkout `fa47850d`
the same file reads `service_desired_count = 1` at `:19`, so this row depends on reading the pinned ref);
`messenger/cmd/root.go:63,:64,:107,:118-142,:147`; `messenger/internal/rpcsrv/rpcsrv.go:25-30`;
`messenger/internal/flow/jobsimulations.go:140-151`; `messenger/internal/flow/assignments.go:828`;
`docker-compose.yml:84-92`; `app/internal/converter/gotenberg.go:13` (90 s);
`app/internal/coursebuilder/bedrock.go:98-104,:105-114`; `app/terraform/{variables.tf:635-638,
ssm.tf:328-334, main.tf:555}`; `app/main.go:766-779,:770,:774,:778,:824-826`;
`internal/worker/worker.go:199,:210`, `main.go:173` (concurrency 20), `run_task.go:49,:199,:204`;
`internal/data/ent/schema/coursebuildersession.go` (every named column present);
`terraform/migrations/20260717151144.sql` (creates all three named tables);
`simulator/ai/ai.go:20-26,:58-59,:65-66,:69,:82-83,:86,:98-99,:102,:110-111,:114-115,:126-127`;
`v3/validator/validator.go:43,:60-61,:595`; `basevalidator/criterion.go:127,:168,:428,:450-475`;
`v3/validator/skills.go:40-51,:53-64,:75`; `internal/skill/skill.go:617-623`;
`next-web-app packages/ui/src/Competency/CompetencyReadLevel.tsx:18`;
`jobsimulation.go:905,:971,:983-990,:1079-1085,:1297,:1302-1305,:1307,:1594-1600`;
`calls/livekit.go:131-135,:140-144`; `alignment/cmd/alignctl/run.go:133-136,:134-135,:139-153`;
`alignment/internal/dna/dna.go:21,:24,:269-270,:279-281,:290-315`; `compare.go:56-57,:101-103`;
`clerkenstein/alignment/scripts/gate.sh:58-68,:69`; `clerkenstein/clerk-backend/store.go:138,:151`;
`clerkenstein/alignment/dna/clerk-2.6.0.json:131`; `clerk-frontend/server.go:35-67`, `:125`, `:663-665`;
`clerk-frontend/registry.go:24,:67,:75`; `clerk-frontend/resources.go:50-51,:255,:276`;
`clerkenstein/.github/workflows/alignment.yml:10-11`; `studio-desk src/index.ts:96` and
`app/services/userService.ts:16` (both `STUDIO_ACCESS_ROLES` arrays identical, both carrying the quoted
comment); all five `TestServer_*` names in `clerk-frontend/server_test.go`; `r.Cookie(` = 0 across
`clerkenstein/`.

## What I could not settle

- **`corpus/services/db-backup.md` in its entirety.** No ground truth exists in this environment: the
  `db-backup` repo is not in the clone set, it has **never** appeared in `repos.yml` (`git log --all -S
  'db-backup' -- repos.yml` → empty, which per §5 rule 37 is the right way to ask), and
  `git -C platform grep -in 'db-backup' HEAD` returns **0**. So the 6-hour cadence, the
  S3/Azure/Hetzner triple, "Go", "PostgreSQL RDS (all schemas)" and the prod+staging deployment claim are
  all unverifiable from source. What I *could* do is check corpus-internal consistency, and the file
  agrees with its six sibling sites except for the staging wording listed under Minors. **Settling it
  needs the `anthropos-work/db-backup` repo (or the prod terraform root) in the clone set.**
- **`ai_architecture.md:141`** — `public.skill_embeddings` = 42,790 and `public.job_role_embeddings` =
  18,919 rows (measured 2026-06-29). Requires a read-only DB query; not attempted (read-only mandate + no
  live stack). The figures are self-consistent with `:108` and root `CLAUDE.md`.
- **`ai_architecture.md:154`** — skiller migrations `20260417103036` / `20260417120309`. The `skiller`
  repo is not cloned and no file with either timestamp exists under `app/terraform/migrations/` (the
  ported vector tables live in `20260615130000_skiller_taxonomy.sql`). Settling it needs the `skiller`
  repo at a ref.
- **`ai_architecture.md:187-192`** — the five named LiveKit agent repositories and their absence from
  `repos.yml`. `gh repo list anthropos-work` is unavailable in this sandbox. The `repos.yml` half is
  confirmed (only `app`, `sentinel`, `next-web-app`, `studio-desk` at `0c91421`); the *existence* of the
  five repos is not.
- **Every alignment SCORE (100 %/100 %, 97.2 %→100 %, the express rc=3).** I verified the DNA
  denominators, the operator sets, the gate wiring, the exit-code split and that the M219 fix is in the
  code — but I did not execute `alignctl`, so the reported percentages are unrun. Settling them means a
  `/align-run` on a box with `@clerk/express` `node_modules` (which is precisely the surface the doc
  says is dependency-gated).
- **`messenger.md:53` / `:8` — `app/env_guards.go:61` for `MESSENGER_ENABLED`.** The anchor resolves
  **exactly** at `app` `origin/main` (`2035f9a4:env_guards.go:61` = `envMessengerEnabled =
  "MESSENGER_ENABLED"`), but the block names no ref, and at the **checkout** `b948604f` the file
  `env_guards.go` does not exist and `MESSENGER_ENABLED` occurs **0 times in the whole repo**. At
  `9d00a313` — the ref the banner two rows above names as where the fold landed — `env_guards.go` exists
  but is a *different file* (S3 bucket probes, 132 lines) and `MESSENGER_ENABLED` again occurs **0**
  times. I did **not** book this, because (a) `platform/docker-compose.yml:84-92` at HEAD independently
  documents the gate under that exact name, and (b) the row directly above it warns *"Every anchor in
  this row moved between `9d00a313` and origin/main"*, so the document is deliberately origin/main-anchored
  (§5 rule 41). Flagging it because a reader grading at the checkout gets a hard miss, and because the
  banner's *"Re-derived at `app` `9d00a313` v1.367.0"* is false for this particular field.
- **The NUL-bearing hole in the `next-web-app` zeros.** `apps/web/src/hooks/useCoursebuilder.ts` (1,178
  NULs) is skipped by both `grep -I` and `git grep`, so my `next-web-app` component of the
  "no 60/65/75/85/95 ladder" zero is a zero with one known hole in it. A competency-threshold ladder in a
  coursebuilder hook is implausible but not excluded.
- **`messenger.md:88`** — *"Recent work in v0.34.0 added whitelabel support"*. The whitelabel code and the
  `READONLY_DB_CONNECTION` read (`cmd/root.go:147`) are confirmed; the *version* attribution is not (no
  messenger tag history read).
