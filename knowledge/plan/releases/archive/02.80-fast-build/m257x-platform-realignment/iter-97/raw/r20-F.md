# Seat F — reading #20

Corpus under audit: `rosetta` @ `00be1ac`, tree clean.
Ground truth re-derived at open (matches the addendum sheet):
`platform 0c91421` · `app b948604ff` (origin/main `2035f9a40`, 93 ahead) · `app/studio aeec036` ·
`cms ca50c81` / `cms/studio aeec036` · `next-web-app bb3313bc0` (origin/main `8297c684c`) ·
`storage 4ce8ece` · `messenger fa47850` · `ant-academy 9c3843cd` · `jobsimulation 462343b0` ·
`sentinel 88bc559` · `studio-desk 14a5442` · `roadrunner 87d8d44` · `graphql-wundergraph 60c229f` ·
`.agentspace/rosetta-extensions 6130bfd` (authoring) · `stack-demo/rosetta-extensions ab81527`.

Search-set enumeration (per the addendum's absence rules) was run for every tree-wide zero I
booked: `find stack-demo -name .git -maxdepth 4` → **15 trees**, nested `app/studio` and
`cms/studio` included, each grepped at **its own** ref. Positive controls stated inline below.

## Positive controls

| file | `wc -l` | read in full? |
|---|---|---|
| `corpus/services/ant-academy.md` | 448 | yes |
| `corpus/services/studio-desk.md` | 435 | yes |
| `corpus/services/chronos.md` | 245 | yes |
| `corpus/architecture/shared_libraries.md` | 242 | yes |
| `corpus/services/askengine.md` | 124 | yes |
| `corpus/services/skillpath.md` | 112 | yes |
| *(protocol read, not assigned)* `corpus/ops/platform-alignment.md` §5 | 2178 total | §5 in full |

Pipeline positive controls actually run (so no zero below is an unproven zero):
- `git grep -c 'external_services' -- CLAUDE.md` → 3 (proves `git grep` over the corpus works)
  before booking any corpus-side absence.
- `git grep -l 'anthropos-work/colony/authn' HEAD -- '*.go'` → `app`=129 (non-zero) in the same
  pass that returned 0 for `sentinel`/`storage`/`messenger`.
- A **failed** shell-`grep` was caught and discarded mid-pass: `grep -E '^\s+- name:'` over
  `repos.yml` history returned nothing (the shell `grep` is `ugrep -G`; `\s` did not apply). The
  enumeration was redone in Python and returned 14 names / 11 `type: go`. **The empty result was
  the instrument, not the corpus** — recording it because a report that does not name its failed
  commands is claiming a precision it did not have.

---

## Blockers

### B1 — `ant-academy.md:255` names two fonts the app does not load, and omits the one it does

- **Anchor:** `corpus/services/ant-academy.md:255`
  > `| **Fonts** | DM Sans + Instrument Serif + JetBrains Mono (via `next/font/google`) + Font Awesome Pro …`
- **Why it is false:** `ant-academy` @ `9c3843cd`, `code/app/layout.jsx:1` is the **only**
  `next/font/google` import in the repo:
  `import { Work_Sans, Instrument_Serif } from "next/font/google";`
  (`git grep -n "next/font" HEAD -- code` → one import site; the other hits are CSS comments and
  tests.) The file's own comment block at `code/app/layout.jsx:36-40` states it outright:
  *"Single Google font, matches apps/web in next-web-app (Work Sans, all weights). Display + mono
  usages fall back to system fonts … The legacy `--font-dm-sans` / `--font-instrument-serif` /
  `--font-jetbrains-mono` CSS vars are still aliased to keep the existing `var(--font-…)` chains
  working."* So **DM Sans and JetBrains Mono are dead CSS-variable names, not loaded webfonts**,
  and **Work Sans — the one family actually fetched — is absent from the row.**
  The repo carries a *regression test for exactly this*:
  `code/tests/unit/m1-app-shell.test.js:48` (*"imports Work Sans via next/font/google (single
  family, matches next-web-app)"*), `:316` (*"`--font-sans` uses Work Sans; `--font-serif` uses
  Instrument Serif via next/font; `--font-mono` falls back to system stack"*) and `:360` (asserts
  no CSS file may contain a bare `font-family: 'DM Sans'`).
  Residual "DM Sans" strings in the tree are a Chart.js `font.family` literal
  (`code/ucourses/ucourse-engine/content-widgets/ChartRenderer.jsx:92`) and a static SVG — neither
  loads a font. Instrument Serif is the only one of the three that is correct
  (`code/app/layout.jsx:51`).
  Only one site in the whole corpus (`git grep -in 'Work Sans\|DM Sans\|JetBrains' -- corpus/`
  → this line alone), so nothing else contradicts or rescues it.
- **Confidence:** high

### B2 — `shared_libraries.md:77-83` — "12 Connect-RPC services are defined" omits `StorageService`

- **Anchor:** `corpus/architecture/shared_libraries.md:77`
  > `12 Connect-RPC services are defined: `UsersService`, `OrganizationsService`, `CMSService`, `JobSimulationService`, `SkillerService` … `AuthorizationService`, `MessengerService`, `RoadRunnerService`, `RealtimeService`, `ChronosService` …`
- **Why it is false:** the `proto` module also defines **`StorageService`**, and the list does not
  contain it. Enumerated over all 15 trees at their own refs
  (`git -C <tree> grep -oh 'anthropos-work/proto/go/[A-Za-z0-9_/.-]*v1connect' HEAD -- '*.go'`),
  **10 distinct `*v1connect` packages** are referenced, one of which is
  `anthropos-work/proto/go/storage/v1/storagev1connect`. A `…v1connect` package exists only for a
  declared `service`, and both a handler constructor and a client interface are used:
  - `storage/cmd/root.go:63` — `rpcMux.Handle(storagev1connect.NewStorageServiceHandler(…))`
  - `storage/sdk/storage/client.go:11`, `:28` — `storagev1connect.NewStorageServiceClient(…)`
  - `app/internal/app/users/services/import_profile_test.go:158` — `var st storagev1connect.StorageServiceClient`
    (so **`app` itself**, the one repo the list is written around, names the type).
  The document's own inclusion rule makes the omission a defect rather than a scoping choice: it
  deliberately lists contracts whose service no longer runs — `SkillPathSessionService`
  (*"contract still in `proto`, but NO LONGER SERVED"*) and `ChronosService` (*"archived service,
  contract still present"*). `storage` is in exactly that state after the v9.0 fold, so it belongs
  on the list by the row's own criterion. The stated cardinality is therefore **≥ 13, not 12**.
  I could not put an exact number on the true total — the `proto` repo is not in the clone set
  (it is pulled as a private Go module, per this very document's lines 23-31), so 10 is a *floor*
  derived from importers, not a census. What is settled is that **the enumeration is incomplete
  and the count that labels it is wrong.**
- **Confidence:** high that `StorageService` is missing and "12" is wrong; medium on the true count.

### B3 — `ant-academy.md:336` tells the reader to configure `code/.env`, which the demo lifecycle in the same file (and two other corpus files) says is the wrong file

- **Anchor:** `corpus/services/ant-academy.md:336` (and the block it heads, `:338-353`)
  > `The **app's** env file is `code/.env`, not the repo root:` … `cp .env.example .env`
  and `:357` — *"set `REQUIRE_ORGANIZATION_MEMBERSHIP=0` in `code/.env`"*.
- **Why it is a finding:** three other authorities say `code/.env.local`, and one of them says
  **only**:
  - root `CLAUDE.md:292` (@ `00be1ac`) — *"requires its own `.env` file at
    `ant-academy/code/.env.local` (**not the repo root — the React app reads only from
    `code/.env.local`**)"*
  - `corpus/ops/setup_guide.md:387` — *"Provision it with `/stack-secrets` — it writes
    `ant-academy/code/.env.local`"* (and `:335` lists it in the target-file set)
  - **this same document**, `:198-199` and `:211-213`: the M245 section makes `code/.env.local`
    the demo's config file and calls `ant-academy.sh` *"the single authoritative writer"* of it.
  Both files are gitignored (`ant-academy/code/.gitignore:22-23`) and Next.js loads both, so
  neither instruction "fails" — which is what makes it a trap rather than an error message:
  **Next's precedence puts `.env.local` above `.env`**, so on any stack where `/stack-secrets` or
  `ant-academy.sh` has already written `code/.env.local`, everything a reader puts in `code/.env`
  following `:336-353` is silently shadowed. `CLAUDE.md`'s exclusive form (*"reads **only** from
  `code/.env.local`"*) is additionally false about Next.js as a statement of mechanism.
- **Why I hesitated:** neither side is *unsupportable* — both paths boot a solo dev instance, and
  the §2 block is a solo-dev recipe while the M245 block is demo tooling. I am booking it per the
  instruction to resolve doubt upward: it is a live corpus self-contradiction (two sections of one
  file, plus two other files), it has a real silent-shadowing consequence on a provisioned stack,
  and the "only" clause in the highest-propagation file in the tree is wrong on the mechanism.
- **Confidence:** medium

---

## Minors

- `ant-academy.md:135` — `emptyCatalogView() = { chapters: [], skillPaths: {}, series: [] }`; the
  source also returns `bundles: PUBLIC_BUNDLES, catalogVersion: CATALOG_VERSION`
  (`code/src/lib/serverTenant.js:120`). Omitted members; the load-bearing consequence (0 cards) is
  unaffected.
- `ant-academy.md:144` — *"`code/public/catalog.json` (~2,667 entries)"*; the committed file holds
  **2,715** `courses` entries (`json.load` of the tracked file; keys `version`, `generatedAt`,
  `languages`(7), `courses`(2715)). Tilde-approximated and stale, not material.
- `ant-academy.md:258` — *"~26 Playwright e2e spec files (tests/e2e/)"*; **31** `*.spec.js` files
  are tracked under `code/tests/e2e/` (the raw `git ls-files | grep -c spec.js` returns 38 because
  seven `…spec.js-snapshots/*.png` fixtures match — worth noting so the next reader does not
  "correct" it to 38).
- `ant-academy.md:49` — the layout tree annotates `code/tools/` as *"offline-parity CLI"*. That dir
  now holds exactly one file, `code/tools/apply-v3-metadata.mjs` (*"Adds sidebarCategory,
  sidebarSubcategory, and audiences fields to every skill path"*). The offline-parity harness went
  with the offline surface at v0.5 M1 — which this same document states at `:31`/`:34`.
- `ant-academy.md:394-402` — the repo-local skills table enumerates 14 skills; `.claude/skills/`
  holds **`export-path`** as well (plus the `_shared` helper dir). Omitted list member.
- `askengine.md:111-112` — *"the handler package … carries its own tests: sanitize,
  stream_registry, examples, lessons, distill, feedback, store, embed"* names 8 of the **9**
  `*_test.go` files in `app/internal/web/backend/ask/`; `coursebuilder_tool_test.go` is missing.

---

## Positively cleared (audited zeros)

Labelled per the briefing. **ENUMERATED** = I derived the predicate's set from source and compared
cardinalities. **RE-DERIVED** = I only re-computed arithmetic the document itself displays.

### ant-academy.md
- **ENUMERATED** — the complete `isPublic` matcher table (`:262-279`). I read
  `ant-academy/code/proxy.js:112-188` end to end and matched **every** pattern to a table row and
  every row to a pattern, including both conditional spreads (`DEV_LOGIN_ENABLED` →
  `/api/dev/login-as`, `/dev/accept`; `VISUAL_BYPASS` → `/my-certificates`, `/my-activity`,
  `/bookmarks`). No pattern is unlisted and no row is invented. This is the passage that warns
  *"Do not paraphrase this from memory"*; it is correct.
- **ENUMERATED** — the DB-authoritative catalog chain (`:122-140`).
  `app/(authed)/page.jsx:80-89` `resolveCatalogView()` branches signed-in →
  `getServerCatalogView()` / anonymous → `getPublicCatalogView()`;
  `src/lib/serverTenant.js:143-146` is *literally*
  `const view = (await getBackendCatalogView(eids)) ?? emptyCatalogView()`; the in-code sentence
  the doc quotes (*"the cutover is intentional, not reversible-on-error"*) is at
  `serverTenant.js:132`, inside the `:111-145` docblock the corpus cites.
- **ENUMERATED** — the M245 five-patch set and its anchors (`:194-198`). At rext authoring
  `6130bfd`, `demo-stack/ant-academy.sh` declares exactly five patch vars at exactly the cited
  lines: `:56` `DEVORIGINS_PATCH`, `:66` `FSPUB_PATCH`, `:75` `FSBODY_PATCH`, `:84`
  `FSPUBLIC_PATCH`, `:90` `COCKPIT_PATCH`; all five manifests exist under `demo-stack/patches/`;
  `reapply_clone_patches()` (`:158`) and the `--stop` reverts (`:207-228`) are as described; the
  standing academy assert is `stack-verify/live/autoverify.sh:459` *(f)*.
- **ENUMERATED** — the i18n verdict (`:220-245`). `SUPPORTED_LOCALES` is 7 at
  `src/i18n/locale.js:10`; `LocaleSwitch` is mounted at `src/views/public/PublicHeader.jsx:20`
  only; `LanguageSelector` at `src/components/TopBar.jsx:76`; `LanguageSelector.jsx:88` renders
  `role="menu"` and `:97` `role="menuitemradio"` over all 7. The retraction of the old *"not a
  dropdown menu"* half is correct.
- **ENUMERATED** — the route/absence claim at `:291`. `code/app/` has
  `(public)/library/page.jsx`, `(public)/free/page.jsx` and `(authed)/courses/[slug]/page.jsx`;
  there is **no** `library/[slug]` route in any tree.
- **ENUMERATED** — Cosmo (`:420`): `code/ucourses/ucourse-engine/assistant/agent.js:12` API URL
  `https://api.openai.com/v1/responses`, `:13` `MODEL = 'gpt-5.2'`, `:32`
  `localStorage.getItem('openai_api_key')`, flag at `src/lib/featureFlags.js:8`.
- **ENUMERATED** — FontAwesome vendoring (`:255`, `:301`): 99 tracked files under
  `code/public/assets/fontawesome/` incl. `css/all.min.css` + `webfonts/*.woff2`; **zero**
  `@fortawesome` entries in `code/package.json`; no `.npmrc`; `FONTAWESOME_NPM_AUTH_TOKEN` still
  present at `code/.env.example:97` (i.e. "vestigial" is exactly right).
- **ENUMERATED** — writes-to-backend (`:63`, `:428`): `code/app/api/academy/beacon/route.js`
  exists; `upsertChapterProgress` / `upsertChapterProgressBatch` / `setLastActivity` are at
  `src/graphql/query/academyProgress.js:55/19/74`; `app/cmd/academy-seed/{main,fixtures}.go` exist
  at `app b948604f`.
- **ENUMERATED** — chapter-body chain (`:169-190`):
  `src/lib/serverChapterBody.js:51-67` is `resolveServerChapterBody` → `getBackendChapterBody` →
  `maybeResolveDraftBody` → `{ notFound: true }`; the 404 `<h1>` reads *"You wandered off the
  trail."* at `code/app/not-found.jsx:43`.
- **ENUMERATED** — `repos.yml` absence (`:26`, `:305`, `:436`): `platform 0c91421` `repos.yml`
  lists exactly `app`, `sentinel`, `next-web-app`, `studio-desk`. No ant-academy entry, ever
  (checked across all 10 revisions of the file).
- **RE-DERIVED** — Next 16.2.6 / React 19.2.5 / `reactCompiler: true` / `turbopack` block /
  `engines.node >= 22` / `next dev --port 3077` / `code/vercel.json = {"framework":"nextjs"}` /
  manifest at `code/app/layout.jsx:132` / Expo `~54.0.33` with `mobile/scripts/dev-server.sh:7`
  `PORT=8555` / the 6 GitHub workflows / `RegisterServiceWorker.jsx` kill-switch /
  `x-locale` + `x-embed-mode` + `embed-mode` cookie at `code/proxy.js:239-241`.

### studio-desk.md
- **ENUMERATED** — the compose row (`:21`, `:140`). At `platform 0c91421`,
  `docker-compose.yml:112` opens the `studio-desk` service; `:119` is the
  `VITE_GRAPHQL_ENDPOINT` **build arg**; `:135` the runtime env; `:138-140` `depends_on: backend`;
  `:141` `profiles: [studio-desk, all]`. All four re-anchored citations resolve to the exact
  constructs claimed. I also **executed the predicate**: `docker compose --profile studio-desk
  config --services` in `stack-demo/platform` prints, verbatim,
  `service "studio-desk" depends on undefined service "backend": invalid compose project` — the
  error string the doc quotes.
- **ENUMERATED** — the Vite MPA entry set (`:34`). `vite.config.ts:28-42` declares exactly the
  nine prod entries listed, with `dev-accept` spread in only when `!isProduction`.
- **ENUMERATED** — the AI tier/provider block (`:112`, `:184-190`).
  `src/services/ai/config.ts:20-38` gives OpenAI/Azure `gpt-5.2` / `gpt-5.2` / `gpt-5-mini` /
  `gpt-5-nano` and Anthropic `claude-opus-4-5-*` / `claude-sonnet-4-5-*` / `claude-sonnet-4-5-*` /
  `claude-haiku-4-5-*`; `.env.example:53` `AI_PROVIDER_CHAIN=azure-openai,openai` and `:61`
  `AI_DEFAULT_TIER=fast`; the in-code fallback is `'thinking_fast'` at `config.ts:182`. Every
  clause of that sentence checks.
- **ENUMERATED** — the Skill-Path-Builder paragraph (`:107`). `src/routes/` holds exactly
  `ai.ts`(5,895 B) `dev.ts`(4,092) `skillpath.ts`(**61,080** = "~61KB", **and it is the largest**)
  `youtube.ts`(4,023); `YOUTUBE_API_KEY` is read at `src/routes/youtube.ts:43` with the `_mock:
  true` fallback immediately below; `GCLOUD_SERVICE_ACCOUNT` at `.env.example:120` and
  `terraform/main.tf:129` with **0** reads anywhere in `src/`; `pingSnapshotCapability` is
  imported at `src/index.ts:38` and invoked at `:305` guarded on `DIRECTUS_BASE_URL` +
  `DIRECTUS_TOKEN`; `BUNNY_LIBRARY_ID`/`BUNNY_LIBRARY_API_KEY` at `src/routes/skillpath.ts:1356-7`.
- **ENUMERATED** — the access gate (`:305`). `src/index.ts:96`
  `STUDIO_ACCESS_ROLES = ['admin','org:admin','content_creator','org:content_creator']`, consumed
  at `:115`; `checkEnterpriseAndAdmin` at `:99`; `WEB_APP_URL` redirect at `:119`; `MOCK_CLERK` at
  `:43` and `VITE_MOCK_CLERK` at `app/core/main.ts:128`.
- **ENUMERATED** — the M253 first-paint model (`:387-393`). `app/core/main.ts:97`
  `preloadCriticalCSS()`, `:182` `clerk.load()`, `:191` `l12nService.init()`, `:199`
  `userService.canAccess()`, `:206` `new PageWrapper()` — every cited line number names the cited
  construct, in the cited order.
- **ENUMERATED** — the rext side of M249/M252/M253. At authoring `6130bfd`:
  `demo-stack/patches/studio-desk-{back-to-cockpit,logo-url,logout-url,no-thirdparty,shell-first-paint}/`
  all exist; `build_frontend_studio_desk` is referenced by the manifests and by
  `demo-stack/tests/test_back_to_cockpit_m249.py:310-320`; `frontend_lines(...)` at
  `stack-injection/gen_injected_override.py:379` takes `platform_dir` and emits the
  existence-guarded `env_file` (`:398-445`); both named regression tests exist at
  `stack-injection/tests/test_injection.py:1657` and `:1714`;
  `stack-verify/e2e/run-studio-fcp.sh` exists.
- **ENUMERATED** — the "no datastore" paragraph (`:279-287`). No DB driver in `package.json`; the
  three scaffold/service filenames the doc spells with `.js` (`app/core/scaffold/pageWrapper.js`,
  `userProfile.js`, `app/services/studioDB.js`) are genuinely `.js` in a TypeScript repo; the sole
  tailscale-funnel string is the GlitchTip DSN comment at `app/core/main.ts:105`.
- **RE-DERIVED** — `node:24-alpine` in both Dockerfiles + `engines.node >= 24`; `PORT || 9100`
  fallback (`src/index.ts:60`) with `app.listen(backendPort)` at `:294`; Jest `projects:` with a
  `backend` display name (`jest.config.js`); 7 `app/public/l12n/*.json` locales.

### chronos.md
- **ENUMERATED** — the two decommission commits. `platform 045857c` = *"chore: remove chronos
  service from orchestration…"*, touching `docker-compose.yml` (−37) and `repos.yml` (−4);
  `jobsimulation 09631fb2` = *"refactor: remove Chronos references and update documentation to
  reflect Asynq integration for session timeout management"*, and `chronos` survives at
  `jobsimulation 462343b0` only in `CHANGELOG.md`.
- **ENUMERATED** — the Asynq re-host anchor (`:9`). `app/internal/jobsimwiring/worker.go:24-35`
  spans the `asynq` import through `StartWorkers(...)` and the standard pool's
  `asynq.NewServer(...)` — the anchor names the construct it claims, not merely a line that exists.
- **ENUMERATED** — *"no service in the current compose file depends on it"* (`:9`, `:186`).
  `platform 0c91421` `docker-compose.yml` declares exactly `sentinel`, `backend`, `studio-desk`,
  `next-web-app`, `gotenberg` (plus `common.yml`); zero `chronos`.
- **Could not settle (see below):** everything from `:13` to `:245` that describes chronos
  internals — there is no `chronos` clone in the ground-truth set.

### shared_libraries.md
- **ENUMERATED** — every version pin in the colony/proto/ai/taxonomy rows, read from each repo's
  own `go.mod` at its own ref, **with line numbers**: `app b948604ff` ai`:14` v1.40.2 ·
  colony`:16` v0.35.2 · proto`:18` v1.210.0 · taxonomy`:20` v1.2.0; `sentinel 88bc559` colony`:8`
  v0.34.3 · proto`:9` v1.200.0 · taxonomy`:21` v1.2.0 `// indirect`; `storage 4ce8ece` v0.34.3 /
  v1.196.0 / taxonomy`:25` `// indirect`; `messenger fa47850` v0.35.2 / v1.210.0 / taxonomy`:9`
  direct; `cms ca50c81` ai`:9` / proto`:11` v1.207.0 / taxonomy`:13`; `jobsimulation 462343b0`
  ai`:11` / proto`:13` v1.205.0 / taxonomy`:15`; `roadrunner 87d8d44` colony`:7` v0.34.3 /
  proto`:8` v1.196.0 / **no taxonomy** → the row's *"6 of 7"* is exact.
  `roadrunner/main.go:7` does import colony and `:17` uses `colony.NewVersionConfig`.
- **ENUMERATED** — the `repos.yml`-history claim at `:181` (*"that set is 11"*). Derived in Python
  over all **10** revisions of `platform/repos.yml`: **14** names ever, **11** of them `type: go`
  (`app · chronos · cms · intelligence · jobsimulation · messenger · roadrunner · sentinel ·
  skiller · skillpath · storage`). All four of `skiller/skillpath/chronos/intelligence` are
  present in the first revision `a2a3ee6`, and **skillpath is the last of the four to go**, at
  `a4db680` (intelligence `fdfa189` → chronos `045857c` → skiller `21429b7` → skillpath
  `a4db680`). Every clause of that long sentence holds.
- **ENUMERATED** — the authn row's file counts (`:145`). Per-tree at each tree's own ref,
  `colony/authn` importers among `*.go`: `app` 129, **`cms` 6**, **`jobsimulation` 8**, everything
  else 0 — the two numbers the row states, exactly. And the standalone
  `"github.com/anthropos-work/authn"` import is **0 across all 15 trees**, including the two
  nested `studio` repos at `aeec036`, which is what makes `:162` (*"No checked-out service imports
  the standalone …"*) a proven zero rather than an assumed one.
- **ENUMERATED** — the `ai` row (`:105`). `*.go` importers of `anthropos-work/ai`: `app` 59,
  `cms` 9, `jobsimulation` 20, everything else **0** — including `roadrunner` (0), which is the
  row's own named exception. `app/internal/aiusage/ai_usage.go` and
  `app/internal/askengine/bedrock.go` both exist.
- **ENUMERATED** — the six registered Connect handlers at `:70`. `app/main.go` @ `b948604f`:
  Users `:1187`, Organizations `:1188`, Skiller `:1196`, JobSimulation `:1204`, CMS `:1212-1214`
  under `if cmsRPCServer != nil`, LabSession `:1228`. Six, and **no** `SkillPathSessionServiceHandler`
  or `RoadRunnerServiceHandler` anywhere in the tree.
- **ENUMERATED** — the roadrunner/`ROADRUNNER_RPC_ADDR` clause at `:79`, graded **at the refs it
  names**: `app/internal/jobsimwiring/wiring.go:123` @ `app 9d00a313` is exactly
  `runnerManager := jsrunner.NewRunnerManager(getenv("JUDGE0_API_KEY"), getenv("JUDGE0_BASE_URL"))`;
  `ROADRUNNER_RPC_ADDR` is 0 occurrences in `app` at both `9d00a313` and `b948604f`, and 0 in
  `platform` at `0dab54d`; and the parenthetical's own correction is right —
  `docker-compose.yml:118` @ `0dab54d` is `AWS_REGION=eu-west-1`.
- **ENUMERATED** — the taxonomy ID format and **all three worked examples** (`:224-229`),
  independently recomputed rather than re-read: SHA-1 of the sanitized, space-separated,
  uppercased name, first 4 hex — `sha1("GO")[:4] = F63F` → `K-GOXXXX-F63F`;
  `sha1("DOT NET")[:4] = DDE9` → `K-DOTNET-DDE9`; `sha1("C SHARP")[:4] = 5F5B` → `K-CSHAXX-5F5B`.
  The word-part rule the doc states (6 uppercased alphanumerics, X-padded, 3 chars/word when
  multi-word, `.`→`DOT` / `#`→`SHARP`) reproduces `GOXXXX`, `DOTNET` and `CSHAXX` from those same
  inputs. This is a rare case where the corpus can be checked without the library.
- **RE-DERIVED** — the ai-library corrections (`:118-128`). The retraction anchor
  `external_services.md:579` **resolves to the right construct**: that line is *"There is **no
  ordered EU-first fallback chain.**"* (I checked this specifically because it is the class rule 21
  calls "an anchor that resolves but names the wrong construct"; here it does not.) Root
  `CLAUDE.md:269` and `:283` cite the same `:579` — consistent, not drifted. **NB for the next
  reader: the `CLAUDE.md` copy served in the session system-prompt still said `:546`, which is the
  pre-`00be1ac` text; `:546` is the WunderGraph-restart bullet. Read `CLAUDE.md` from disk, not
  from context, on this tree.**
- **ENUMERATED** — `proto`'s legacy `go/simulator` note (`:92-94`):
  `storage/internal/migration/migration.go:13` imports
  `legacyStorage "github.com/anthropos-work/proto/go/simulator/storage/v1"` and uses it at `:93`.

### askengine.md
- **ENUMERATED, with the set derived from source and the commented-out class explicitly excluded**
  — the registry claim at `:47-48` (*"**60** `TableDef`s, **57** with `Schema: "public"` and **3**
  with `Schema: "directus"`"*). Parsed `app/internal/askengine/registry.go` line by line in
  Python: **60** live `Schema:` fields (`Counter({'public': 57, 'directus': 3})`), **0** of them on
  a commented line, **60** live `Name:` fields, **0** duplicate names. This is the passage §5 rule
  24 exists for — `grep -c` and the parse agree here, and the parse is what I am reporting.
- **ENUMERATED** — the alias anchor `registry.go:183-195`: it is precisely the
  *"Jobsimulation (folded into public — jobsim-in-app M709)"* comment block that says the dotted
  names *"RESOLVE TO THE PUBLIC TABLES"* and that `jobsimulation` *"stays in rewriteSchemas until
  M710"*; `rewriteSchemas` itself is at `sandbox.go:293`.
- **ENUMERATED** — the four Bedrock/executor constants: `bedrock.go:25` `DefaultModelID =
  "eu.anthropic.claude-sonnet-4-6"`, `:26` `DefaultRegion = "eu-west-1"`, `:315`/`:405`
  `Temperature: param.NewOpt(float64(0))`, `:252` `stripAnthropicAuthMiddleware`, `:162-163`
  `AWS_REGION`/`ASK_MODEL_ID` envOr; `executor.go:18` `MaxInlineRows = 200`, `:21`
  `MaxCellLength = 400`, `:24` `QueryTimeout = 10 * time.Second`, applied as
  `SET LOCAL statement_timeout` at `:97`; `app/main.go:150` `askEngineMaxConns = 6` and `:313`
  `os.Getenv("COPILOT_DB_CONN")`.
- **ENUMERATED** — the full route list at `:64-67`, read from the **registration site**
  (`app/internal/web/backend/backend.go:171-188`), not from doc comments: `/ask` group with CORS +
  Clerk authn and the in-code note *"sibling group of /api that skips OpenAPI validation"*, then
  `POST /stream`, `GET`/`POST /conversations`, `GET /conversations/:id`,
  `GET /conversations/:id/stream`, `DELETE /conversations/:id`,
  `POST /messages/:messageId/feedback`, and `POST|GET|POST|DELETE /admin/auto-rules…`.
  **The corpus's `/ask/admin/auto-rules/...` spelling is the correct one**; the *platform's own*
  handler doc-comments (`ask/admin.go:77`, `:102`, `:129`, `:173`) say `/admin/ask/auto-rules…`
  and are the stale side. Recording it because the cheap read would have booked the corpus wrong.
- **ENUMERATED** — `handler.go:34` `maxAgenticIterations = 15`, `:41` `loopTimeout = 10 * time.Minute`,
  `:248` `context.WithTimeout(context.WithoutCancel(ctx), loopTimeout)`, `:57` `streamRegistry`.
- **ENUMERATED** — every test count at `:104-109`, counted as top-level `func Test`:
  `sandbox_test.go` **49**, `executor_test.go` **13**, `prompt_test.go` **10**,
  `followups_test.go` **14** — all four exact; `TestBuildSystemPrompt_RulesBlockIsIdentical` is at
  `prompt_test.go:176`.
- **ENUMERATED** — *"~146 KB"* rules.md (`:21`): `internal/askengine/rules.md` is **146,219 bytes**.
- **ENUMERATED** — the five persistence tables (`:36-38`): Ent schemas
  `askconversation.go`, `askmessage.go`, `askqueryexample.go`, `askquerylesson.go`,
  `askautorule.go` all exist under `app/internal/data/ent/schema/`.
- **ENUMERATED, and it is where reading around the line paid** — `:44`, *"`WrapQuery` (defends
  **four** documented bypass vectors)"*. `sandbox.go:350` says *"three"* — but the bullet list
  under it (`:352-362`) has **four** members and the function enforces four checks:
  `ErrQuotedIdentifier` (`:376`), `stripCommentsAndStrings` (`:371`), `ErrUnresolvedFromTarget`
  (`:382`), `ErrReservedCTEName` (`:397`). **The corpus is right and the source comment's word
  "three" is the stale one.** A grep-to-the-line audit would have booked this as a corpus blocker.
- **RE-DERIVED** — ship dates at `:116-117`: `app` tags `v1.267.0` = 2026-05-07 and `v1.340.0` =
  2026-07-17.
- **RE-DERIVED** — the `ValidateSQL` rejection set (`sandbox.go:107-110`, `:167-182`) and the
  three `OrgStrategy` values (`registry.go:15-20`); `$2`/`profile_histories` self-scope at
  `registry.go:118` + `sandbox.go:120-139`.

### skillpath.md
- **ENUMERATED** — the *"NO `SkillPathSessionService` anywhere"* claim at `:30-33`, which is a
  tree-wide zero and therefore run under the addendum's rules: **0** occurrences in `*.go` across
  **all 15 trees at their own refs**, nested `app/studio` and `cms/studio` (`aeec036`) included;
  **0** `skillpath…v1connect` imports anywhere; the only two non-Go hits in `app` are exactly the
  two the corpus itself names.
- **ENUMERATED** — the trap the same bullet documents (`:34-38`), graded at the ref it names:
  at `app` origin/main **`2035f9a`**, `CLAUDE.md:**109**` and `knowledge/architecture.md:**28**`
  both still list `SkillPathSessionService` in the RPC mux — and the corpus's warning that *"the
  CLAUDE.md line was `:80` when this was first measured"* is itself verifiable: at the checkout
  `b948604f` the identical sentence sits at `CLAUDE.md:80`. Both halves true.
- **ENUMERATED** — the in-process content read (`:14`, `:59`). `app/internal/skillpath/session.go`
  `:205-206` carry the exact comment *"cms-in-app deseam: cms is in-process — read the hydrated
  domain struct directly (no proto round-trip)"* and `:207` is
  `u.cms.GetSkillPathDomain(ctx, skillPathId, version)`; `app/internal/skillpaths/skillpaths.go:88-95`
  is the twin, ending in `GetSkillPathDomain`. Ent schemas
  `skill_path_session.go` + `skillpath_mixins.go` exist; the subgraph SDL is at
  `internal/web/backend/graphql/graph/schemas/skillpath_sessions.graphqls`.
- **ENUMERATED** — *"No compose file sets any `*_RPC_ADDR` now"* (`:59`): **0** at
  `platform 0c91421` across `docker-compose.yml` + `common.yml`; the one surviving service address
  is `AUTHORIZATION_ADDRESS=http://sentinel:8087` at `docker-compose.yml:48`.
- **ENUMERATED** — the manager-view reader (`:74-79`).
  `app/internal/organization/intelligence.go:1144` is
  `func (m IntelligenceManager) InsightsSkillPathByMemberships(...)` and `:1159-1170` is the
  `m.ent.SkillPathSession.Query()` with `SkillPathID` + `StatusIn(Active, Completed)` + the
  `TenantIDIsNil() ∨ TenantID(organizationID)` predicate — the anchor names the construct.
- **ENUMERATED** — the retraction's own evidence (`:81-89`).
  `app/terraform/migrations/20260729133514.sql:62` `DROP TABLE "local_jobsimulation_sessions";`
  and **`:63`** `DROP TABLE "local_skill_path_sessions";`; **no** `local_*` Ent schema exists; and
  the self-correction about "the last migration" is exact — at `app 9d00a313` precisely **three**
  migrations post-date it, and they are the three named (`20260731131307.sql`,
  `20260731154527_academy_chapter_progress_completed_at.sql`,
  `20260803143844_ai_readiness_recommendation_path.sql`; `atlas.sum` is not a migration).
- **ENUMERATED** — the `repos.yml` counts at `:47` (**9** @ `2adcf71`, **6** @ `0dab54d`, **4**
  since `838d907`), read off the same 10-revision reconstruction as above: `360efd4` (the
  `2adcf71` state) = 9 entries, `d11a403` = 6, `838d907` = 4. All three exact.
- **ENUMERATED** — the *"UNIMPLEMENTED per-member drill-down"* verdict at `:97-104`, graded at the
  ref it names (`next-web-app` **origin/main** `8297c684`). In
  `apps/web/src/components/containers/InsightsBySkillPathStudentSimulationsContainer.tsx`:
  `userData` is `return null as unknown as MembershipEnriched` (`:31-34`) with the real read
  commented out at `:32`; the results `<Table>` is commented out (`:140-149`) and the totals block
  too (`:150-160`); the body renders `t('enterprise.insights.comingSoon')` (`:137-139`), which
  resolves to `"Coming soon"` in `configs/i18n/messages/en/enterprise.json`. The route pair
  `…/skill-paths/[skillPathId]` and `…/[userId]` both exist under
  `apps/web/src/app/(authenticated)/(verified)/enterprise/activity-dashboard/@tabs/`.
  (Pedantic-only: the page renders an i18n **key** whose English value is "Coming soon", not a
  hard-coded literal. The rendered text is what the sentence is about, so not booked.)
- **ENUMERATED** — `SKILLPATH_STREAM` really is live residual plumbing, not just planning-doc
  residue: `app/main.go:637` `pubsub.NewPublisher(os.Getenv("SKILLPATH_STREAM"), …)` and `:1274`
  `subServer.AddSubscriber(os.Getenv("SKILLPATH_STREAM"), …)`.

---

## What I could not settle

1. **All of `chronos.md:13-245`** — the data model, RPC signatures, triggerer behaviour
   (`LIMIT 10` / `FOR UPDATE SKIP LOCKED` / 3-second run timeout / Sentry heartbeat), env-var and
   CLI-flag tables, ports 8080/8081, Go 1.25, sqlc/Cobra. **There is no `chronos` clone in the
   ground-truth set** (15 trees enumerated; chronos is not one). The header's *"the GitHub repo is
   NOT archived"* is likewise unverifiable offline. Settled by: cloning
   `anthropos-work/chronos` and reading `internal/database/schema.sql`,
   `internal/triggerer/triggerer.go`, `cmd/root.go` — or `gh repo view --json isArchived`.
2. **`shared_libraries.md`'s colony-internal and proto-internal claims** — `colony`'s
   `go.mod go 1.25.0` / `golang:1.26-bookworm` build image (`:40`), the package table (`:46-54`),
   the Sentry gate and the commented-out GraphQL rate-limiter reject path (`:56-59`); `proto`'s
   `buf v1.57.0` CI pin, single-module layout and *"~10 generated packages"* under `go/simulator`
   (`:68`, `:91-94`); `ai`'s Azure default API version `2025-04-01-preview`, the Mistral
   OCR-only `panic`s and the 10-attempt retry policy (`:114-134`); `taxonomy`'s *"131-line
   `node.go`, zero external dependencies, `go 1.21.0`"* (`:179`, `:188`). **These four repos are
   private Go modules and are deliberately not cloned** — the document says so itself at `:23-31`.
   Settled by: cloning them, or reading a service's module cache / `go.sum`-resolved source.
   This is also why **B2's true service count is a floor, not a census.**
3. **`shared_libraries.md:202`** — *"the 18K almost certainly came from `job_role_embeddings`
   (**18,919** rows)"* and the `42,790` / `22,470` public-subset figures. These are production-DB
   measurements; the cited provenance artifact
   (`.agentspace/snapshots/taxonomy/<digest>/manifest.json`) is not in the audited tree. Settled
   by: `/db-query` against prod with the stated `organization_id IS NULL` predicate.
4. **`ant-academy.md:258`, *"1000+ Vitest tests"*** — 244 tracked `*.test.js(x)` files under
   `code/tests/`, which makes 1000+ entirely plausible, but a file count is not a test count
   (§5 rule 16's "bad proxy"). Settled by: `cd code && npx vitest run --reporter=json`.
5. **`ant-academy.md:12`, `:74`, `:203` — the "65 real cards" / "0 course cards" demo numbers.**
   The committed FS tree holds **78** `<series>/<skill-path>` directories across **13** series
   (locale overlays excluded), so 65 is not simply "every FS skill path". These are live-render
   observations on a patched demo, and the two FS-published demopatches decide what is served.
   Settled by: a `/demo-up` with `ACADEMY_DEMO_FS_PUBLISHED=1` and a card count on `/library`.
6. **`studio-desk.md:109`** — `bootstrap/index.js:81` inside the pinned `directus/directus:11.6.1`
   image, and the `ADMIN_TOKEN` bootstrap behaviour. Third-party image internals, not in any tree.
   Settled by: `docker run --rm --entrypoint sed directus/directus:11.6.1 -n '75,85p' /directus/dist/…`.
7. **`studio-desk.md:412`, `:423`** — the measured first-paint and FCP numbers
   (`4669 → 817 ms`, `4049 → 38 ms`, `6936 → 2152 ms`). Historical measurements on specific hosts;
   not re-derivable from source. Settled by: re-running `rext stack-verify/e2e/run-studio-fcp.sh`
   and stating the host, per §5 rule 26.
8. **`chronos.md:3` vs the rest of the corpus on repo archival state** — I could confirm
   `graphql-wundergraph` is described as archived elsewhere but had no network to check either
   repo's GitHub archival flag.
