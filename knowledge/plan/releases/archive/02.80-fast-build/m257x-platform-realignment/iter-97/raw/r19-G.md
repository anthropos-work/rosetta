# Seat G — reading #19 (adversarial diff seat)

Scope: `git diff b7e6642..HEAD -- corpus/ CLAUDE.md .claude/` — the repair commit `00be1ac`.

## Positive controls

**Diff, measured myself:**

| metric | measured |
|---|---|
| files changed | **23** |
| insertions | **+230** |
| deletions | **−79** |
| hunks examined (`git diff -U0 … \| grep -c '^@@'`) | **66** |
| `.claude/` files in the diff | **0** (all 23 are `corpus/` + `CLAUDE.md`) |

**Files opened in surrounding context (every hunk read against the current file):**

| file | wc -l | read around every hunk? |
|---|---|---|
| CLAUDE.md | 578 | yes |
| corpus/architecture/ai_architecture.md | 305 | yes |
| corpus/architecture/alignment_testing.md | 542 | yes |
| corpus/architecture/architecture_overview.md | 413 | yes |
| corpus/architecture/dependency_map.md | 103 | yes |
| corpus/architecture/external_services.md | 880 | yes |
| corpus/architecture/platform-migration-status.md | 307 | yes |
| corpus/architecture/security_compliance.md | 281 | yes |
| corpus/architecture/service_taxonomy.md | 484 | yes |
| corpus/architecture/shared_libraries.md | 242 | yes |
| corpus/ops/demo/stories-spec.md | 840 | yes |
| corpus/ops/platform-alignment.md | 2178 | yes |
| corpus/ops/safety.md | 1077 | yes |
| corpus/ops/seeding-spec.md | 620 | yes |
| corpus/services/README.md | 83 | yes |
| corpus/services/backend.md | 318 | yes |
| corpus/services/cms.md | 311 | yes |
| corpus/services/jobsimulation.md | 309 | yes |
| corpus/services/messenger.md | 189 | yes |
| corpus/services/roadrunner.md | 200 | yes |
| corpus/services/skiller.md | 69 | yes |
| corpus/services/storage.md | 232 | yes |
| corpus/services/studio-room.md | 506 | yes |

**Tree enumeration (addendum instrument).** 15 git trees under `stack-demo`, nested repos included:
`ant-academy 9c3843cd · app b948604f · app/studio aeec036a · cms ca50c817 · cms/studio aeec036a ·
graphql-wundergraph 60c229f3 · jobsimulation 462343b0 · messenger fa47850d · next-web-app bb3313bc ·
platform 0c91421d · roadrunner 87d8d443 · rosetta-extensions ab81527a · sentinel 88bc5592 ·
storage 4ce8ece5 · studio-desk 14a5442a`. rext authoring copy `6130bfd8`. Every absence below was
established per-tree at each tree's own ref, with a positive control in the same pass.

---

## Blockers

### B1 — external_services.md's own INTERNAL anchor `:543` was left behind while all three cross-file copies were bumped

- **Anchor:** `corpus/architecture/external_services.md:614` — *"off it there (`:543` above; 0 hits for `bedrock|boto3` under `app/studio/`; corrected M257x iter-48)."*
- **Why it is false:** The repair moved every `external_services.md` anchor by +24 (two hunks, +9 at old :244 and +15 at old :408) and correctly rewrote the **cross-file** citations of this exact target — `ai_architecture.md:51` and `:87` and `security_compliance.md:218` all went `:543` → `:567`. The **same-file** self-reference did not. At `b7e6642`, `external_services.md:543` was the *"Anthropic Direct (first-party API)"* provider row — the row the sentence means. In the current file that row is at **:567**, and **:543 is now `"When backend services add new GraphQL types or operations:"`** — the *Schema Updates* section, an unrelated construct. A reader following the pointer lands in the GraphQL codegen instructions.
- Failure mode 4 (drift caused by the repair) compounding failure mode 1 (fixed at three sites, left standing at the fourth).
- **Confidence:** high

### B2 — the second internal anchor in the same file, `:522`, drifted the same way and to the same distance

- **Anchor:** `corpus/architecture/external_services.md:870` — *"> Consistent with :522 above, where the same correction is already recorded."*
- **Why it is false:** At `b7e6642`, line 522 was *"2. ~~**Restart Wundergraph**: `docker compose restart graphql`~~ — there is no `graphql` service locally any more; restart `backend`"* — precisely "the same correction". After the repair's +24 that line is at **:546**, and **:522 is now a TypeScript comment**, `// Queries in app/graphql/*.graphql`, inside the Studio-Desk codegen snippet. Verified by reading both revisions of the file.
- Booked separately from B1 because it is a distinct anchor a fixer must touch separately; the mechanism is identical.
- **Confidence:** high

### B3 — `platform-alignment.md`'s citation into `backend.md` was mechanically bumped 175 → 187 and now confidently names a directory-listing line

- **Anchor:** `corpus/ops/platform-alignment.md:1345` — *"The refuting citation iter-21 trusted was `corpus/services/backend.md:187` — a corpus line asserting messenger points *all four* addresses at `backend:8083`."*
- **Why it is false:** `corpus/services/backend.md:187` is `  authorization/            Sentinel client` — a row of the repo's directory-tree code block. It asserts nothing about messenger or RPC addresses. Nor did the pre-repair form: at `b7e6642`, `backend.md:175` was the *same* directory-tree line, `  authorization/            Sentinel client`. The claim the sentence describes actually lives at **`backend.md:241`** (*"`messenger` was the last, and it reached four surfaces — `BACKEND_USERS_RPC_ADDR`, `CMS_RPC_ADDR`, `JOBSIMULATION_RPC_ADDR`, `SKILLER_RPC_ADDR` — all pointed at `http://backend:8083` by `d11a403`…"*).
  The repair added +12 lines to `backend.md` above line 175 and applied +12 to this citation without opening the target. It converted a wrong anchor into a *differently* wrong anchor and gave it the appearance of maintenance (failure mode 5).
  The passage is self-undermining: seven lines below, at `:1352`, the same file says **"Anchors are cheap to verify and they were all fine."**
  Secondary: because the sentence is a *historical* record of what iter-21 cited, re-pointing it at today's line numbering is a category error even if a line number had been right.
- **Confidence:** high

### B4 — brand-new fence rule 44 states a NUL count that is off by three orders of magnitude; the number is the file's LINE count

- **Anchor:** `corpus/ops/platform-alignment.md:1236` — *"Two exist: `next-web-app/apps/web/src/hooks/useCoursebuilder.ts` (50,433 bytes, **1,178 NULs**) and `ant-academy/code/src/time/store.js`."*
- **Why it is false:** The file contains **exactly one** NUL byte. Measured four independent ways, all agreeing:
  - `python3 … d.count(b'\x00')` → **1**
  - `tr -dc '\000' < … | wc -c` → **1**
  - `od -An -tx1 -v … | tr ' ' '\n' | grep -c '^00$'` → **1**
  - against the git blob, `git -C stack-demo/next-web-app show bb3313bc:apps/web/src/hooks/useCoursebuilder.ts | tr -dc '\000' | wc -c` → **1**

  The single NUL sits at byte offset 2272, line 62, inside `].join('\x00');`. The byte count **50,433 is correct**. And **`wc -l` on the file is 1,178** — the repair transcribed the line count into the NUL field. (`ant-academy/code/src/time/store.js` likewise has 1 NUL in 15,307 bytes / 373 lines.)
  The *mechanism* the rule teaches is true and I confirmed it (`file -b` says `data` for both; `grep -I` and `git grep` skip both). But the rule exists to make auditors quantify their instruments, and its own headline quantity is fabricated by transcription. The briefing's grading list names "wrong counts" as a BLOCKER outright.
  Aggravating: this number has already propagated out of the corpus into the audit instrument itself.
- **Confidence:** high

### B5 — the new `skiller` stream cell names TWO `app` refs and then hangs five bare anchors off them; one of its sub-claims is false at one of the two refs

- **Anchor:** `corpus/architecture/dependency_map.md:59` — *"Enumerated over every publisher constructor in `app` @ `b948604f` **and** origin/main `2035f9a4`: `backend` (`main.go:287`), `SKILLPATH_STREAM` (`:637`), `CMS_STREAM` (`:1039`), `AI_USAGE_STREAM` + `JOBSIMULATION_STREAM` (`internal/jobsimwiring/wiring.go:127`, `:180`) — `SKILLER_STREAM` is not among them; **its one Go occurrence, `main.go:1276`, is an `AddSubscriber`**…"*
- **Why it is false / unsupportable:**
  1. **Every anchor resolves at `b948604f` only.** Enumerated at both refs: at `b948604f` the publishers are `main.go:287` / `:637` / `:1039` and `wiring.go:127` / `:180` ✓. At `2035f9a4` the same five constructors are at `main.go:325`, `:746`, `:1149`, `wiring.go:132`, `:185` — **none** of the printed numbers resolves. A block naming two refs makes every anchor in it ungradeable, and **this repair states that rule twice in its own text**: `platform-migration-status.md:92` (*"naming a second ref inside this cell makes every anchor in it ungradeable (the citation resolver reads one ref per block and reports `ambiguous` when a block names two — M257x run-53)"*) and `storage.md:29` (*"a block that names two refs makes every anchor in it ungradeable (M257x run-53)"*). The new cell does exactly what the repair forbids elsewhere in the same commit.
  2. **"its one Go occurrence" is outright FALSE at `2035f9a4`.** `git -C stack-demo/app grep -n SKILLER_STREAM 2035f9a4 -- '*.go'` returns **six** sites — `main.go:1532` (comment), `main.go:1537` (`Skiller: os.Getenv("SKILLER_STREAM")` in the stream struct), `subscriber_merge_test.go:907`, `:1015`, `:1038`, `subscriber_wiring.go:165`. Because the sentence names both refs, the reader cannot tell it is scoped to the older one.
  - The *conclusion* (nothing in `app` publishes to `SKILLER_STREAM`) is correct at both refs — I enumerated `pubsub.NewPublisher` at both — and `roles.go:791` `SkillerSubscriber()` / `SkillerCustomJobRoleCreatedHandler` → `MigrateUsersForCustomRole` and `docker-compose.yml:71 SKILLER_STREAM=skiller` all check out. The defect is the citation apparatus, not the finding.
- **Confidence:** high

### B6 — CLAUDE.md was edited in this very commit and left asserting, ref-lessly, the exact sentence `storage.md` was repaired to say flips without a ref — with an anchor that resolves at the other ref

- **Anchor:** `CLAUDE.md:280` — *"**backend → storage is no longer mid-fold either** — `STORAGE_RPC_ADDR` is set nowhere *and* read nowhere; `app` serves object storage in-process and says so at `app/main.go:504`."*
- **Why it is false:** The sentence names **no ref**, so per the instrument it grades against the **checkout**, `b948604f`. There:
  - `git -C stack-demo/app grep -n STORAGE_RPC_ADDR b948604 -- '*.go'` returns **15** hits, **7 of them live env reads** — `main.go:446`, `:524`, `:992`, `internal/jobsimwiring/wiring.go:115`, and one each in `cmd/academy-asset-upload/main.go:129`, `cmd/academyImport/main.go:231`, `cmd/import/main.go:50`. "read nowhere" is false at the checkout.
  - **`app/main.go:504` @ `b948604f` is `// jobsim-in-app (M709): app's OWN users + organizations RPC handlers, built here so the ported`** — a jobsimulation wiring comment. It says nothing about storage. The line the sentence means (*"the standalone service takes no traffic and STORAGE_RPC_ADDR is gone"*) is `main.go:504` **only at `2035f9a`**.
- **Why this is the repair's problem, not a pre-existing one:** this exact CLAUDE.md line was rewritten by the diff (its `skiller`-stream clause was replaced), and the same commit repaired `storage.md:29` to say, in as many words: *"**The ref is load-bearing and this cell states it deliberately:** run the same grep ref-less, on the older `app` checkout a demo pins, and it returns **15 hits, 7 of them live env reads** — a ref-less command contradicts this sentence on the clone a reader actually has."* `storage.md` and `platform-migration-status.md:92` both pin themselves to `2035f9a`; the index of truth, edited in the same commit, does not. Textbook failure mode 1, with the corpus itself supplying the refutation.
- **Confidence:** high

### B7 — the jobsimulation archive assertion was retracted at six sites and left standing, as the headline exemplar, in the methodology doc

- **Anchor:** `corpus/ops/platform-alignment.md:209` — *"Measured 2026-08-01: `jobsimulation` and `skillpath` were **archived 2026-07-31**, `graphql-wundergraph` **2026-07-30**, `skiller` **2026-07-01** — each within days of its fold…"*
- **Why it is a finding:** This commit removed exactly that assertion from **six** other places and replaced it with *"report both, assert neither"*: `architecture_overview.md:20`, `platform-migration-status.md:89`, `service_taxonomy.md:130`, `jobsimulation.md:24-27`, `jobsimulation.md:93`, `roadrunner.md:40`. The evidence it did so on is real — I confirmed `stack-demo/jobsimulation origin/main` carries **four commits dated 2026-08-04**: `6092c6d2` (M810 terraform delete), `caf36c96` (*Merge pull request #439*, committer `GitHub`), `1e40d184`, `82cb66ec` (v0.254.0) — and an archived GitHub repo takes no pushes and no PR merges. `platform-alignment.md:209` is the one surviving flat assertion, and it is worse placed than the others: it is the **methodology** doc, and jobsimulation is the *headline example* for census signal 6 (*"the cheapest fold-confirmation there is"*). An auditor learning the method from this passage learns it from the one example the corpus now says it cannot assert. Self-contradiction between two corpus passages, which the briefing books as a finding on its own.
- **Hesitation, stated:** the sentence is pinned (*"Measured 2026-08-01"*), and a dated snapshot is not automatically wrong. I book it anyway because the fenced map's own new wording (*"So either it was not archived on that date, or it was un-archived to land M810"*) makes "archived 2026-07-31" one of two mutually exclusive possibilities the corpus explicitly refuses to choose between — while this line chooses.
- **Confidence:** medium

---

## Minors

- `corpus/services/jobsimulation.md:95` — the intra-file self-anchor *"consistent with the **Data** bullet, :31-38 above"* drifted +3 with the banner rewrite; the Data bullet is now at **:39-48**, and :31-38 is the *"same pattern as skiller-in-app"* line plus the Domain/engine bullet. Self-correcting because it names the construct.
- `corpus/architecture/platform-migration-status.md:92` — *"It was true up to `9f8cb53`^"* (of `storage/terraform/main.tf:38` = `service_desired_count = 0`). It was true at **`8e558ce`** (2026-08-04) and removed at **`e6fac97`** (2026-08-04, *"drop the ECS service, keep the assets"*); by `9f8cb53^` (= `bef79bf`) `main.tf` is already 18 lines. The bracket is several commits too generous. (Unchanged text inside a rewritten cell.)
- `corpus/services/backend.md:114` — *"registers in the Cloud Map service-discovery namespace `internal.anthropos` (`app/terraform/main.tf:58`…)"*. Line 58 is `service_discovery_namespace_id = var.service_discovery_namespace_id` — a variable passthrough that names **no** namespace. The namespace string is real and *is* corroborated in the clone set (`app/knowledge/service-dependencies.md:46` @ `b948604f`: *"skiller RPC surfaces at `http://backend.internal.anthropos:8081`"*, plus `graphql-wundergraph/supergraph-config-prod.yaml:6`) — but that is not what the cell cites.
- `corpus/architecture/dependency_map.md:59` — *"**the fact was deleted, not moved**"* reads as a slip for "the producer/publisher was deleted".
- `corpus/services/storage.md:47` — *"**Database**: none — all state lives in S3 (or local filesystem in dev)"* sits seven lines below the repaired `:40`, which now says both managers are wired to production buckets on a stock stack. Not reconciled.
- `corpus/architecture/external_services.md:631` cites `app/studio/services/ai.py:704-724` for the provider set while `cms.md:95`, `service_taxonomy.md:209` and `studio-room.md:375` cite `:705-708`. Both resolve (`:704` is `def get_client`); cosmetic inconsistency only.

---

## Positively cleared (audited zeros)

**ENUMERATED** (predicate re-derived from source independently, cardinalities compared):

1. **The Mistral / `mistralai` reversal — the diff's biggest claim, and it holds completely.**
   `git -C stack-demo/app/studio grep -i -c mistral aeec036a` → `requirements.txt:1`, `tools/pdf2md.py:18`, `tools/r3.py:3` = **22 hits / 3 files**, exactly as claimed everywhere it is claimed. `tools/pdf2md.py:24` is a real `from mistralai import Mistral`; `:96` `Mistral(api_key=…)`; `:127` `model="mistral-ocr-latest"`. `tools/r3.py:194` hard-requires `MISTRAL_API_KEY`. `gen.py`'s imports are exactly `console`/`format`/`agents`/`services.ai`/`postgen`. `services/ai.py:1-2` imports only `openai`/`anthropic`; `:705-708` is the three-entry registry. Go side at the checkout: `markdownManager.go:19` = `mistral.NewMistral(nil, os.Getenv("MISTRAL_API_KEY"))`, `studioManager.go:583` = `NewMarkdownManager(…)`, `studioManager.go:119` = `runCommand(…, "studio/gen.py", …)`. At `2035f9a`: `markdownManager.go:10` imports `…/mistralocr`, `:30` `mistralocr.New(aiKey)` — both as cms.md says. `app/Dockerfile:45-46` copies `studio/` and pip-installs its requirements.
2. **Both instrument caveats in `studio-room.md` are reproducible.** `grep -rni mistral stack-demo/app/studio/` returns **1** (the `requirements.txt:8` line only). `git -C stack-demo/app grep -i mistral b948604f -- 'studio/'` returns **0** (exit 1). `app/studio/.gitignore:2` = `tools/`; `app/.gitignore:79` = `studio/*`; `cms/.gitignore:129` = `studio/*`. `app/studio` @ `aeec036a` = **177** files. `stack-demo/cms/studio` is the same repo at the same sha. No reference to `tools/` scripts exists outside `tools/` (with positive control: `pdf2md` matches inside `tools/`).
3. **Rule 44's gitignored-but-tracked census = 12.** `git ls-files | git check-ignore --stdin --no-index` per tree → 13 paths total; **12 non-empty text** (ant-academy ×2, app ×2, app/studio ×4, cms/studio ×4) and one binary (`roadrunner/cmd/.DS_Store`). The `--no-index` requirement is real.
4. **`STORAGE_RPC_ADDR` at both refs.** `2035f9a` → 3 hits, all comments (`main.go:504`, `jobsimwiring/wiring.go:101`, `storagens/callsites_test.go:189`). `b948604` → 15 hits; the breakdown in `platform-migration-status.md:92` is exact — 7 env lookups (3 in `main.go`, 1 in `wiring.go`, 1 in each of three `cmd/` tools), 2 doc comments, 2 error strings, 3 `t.Setenv`, 1 `t.Fatal`; and `wiring.go:115` genuinely uses a lowercase `getenv` helper, which is why an `os.Getenv`-fitted regex reports six.
5. **`MESSENGER_RPC_ADDR` absence.** 0 occurrences in **every** tree at its own ref, nested repos included (positive control: `app` returns 1 file for the looser `MESSENGER_RPC` — two prose lines in `knowledge/plan/roadmap-support-in-app.md`, not a use). `git -C stack-demo/platform log -S 'MESSENGER_RPC' --oneline 0c91421d` → **0**; control `-S 'SKILLER_RPC'` → **7**; `--all` → **0** and **8**, the 8th being `464dfe3`. Platform history at `0c91421d` is **121** commits. Every figure in the repaired `messenger.md` banner and `services/README.md` row is right.
6. **The pre-drop router `depends_on`.** `2adcf71^1` = `1e8e754`; `docker-compose.yml:19-27` there names **four** services (`backend`, `jobsimulation`, `cms`, `storage`), each `condition: service_started`. Its compose is byte-identical to `a4db680`'s. `git merge-base --is-ancestor 464dfe3 0c91421d` exits **1**; `464dfe3` is on `origin/feat/cms-in-app` only, dated 2026-07-28 — after the last mainline compose change (`a4db680`, 2026-07-21). Replaying the whole mainline compose history confirms the earlier members in order: `skillpath`, `skiller`, `chronos`, `intelligence`, and `simulator`.
7. **The Directus two-routes rewrite.** `backend`'s `environment:` block runs `:46-94` and carries exactly **one** `DIRECTUS_*`, `DIRECTUS_PUBLIC_BASE_ADDR` at `:53`. `.env_example:91-92` = `DIRECTUS_BASE_ADDR` + `DIRECTUS_TOKEN`; `DIRECTUS_PUBLIC_BASE_ADDR` is **not** in `.env_example`.
8. **The S3-private hazard, all of it.** `docker-compose.yml:82` = `STORAGE_S3_BUCKET=production-storage20240826131618541000000005`, `:83` = the public bucket, `:73-79` the in-comment reason (*"These MUST be set"*), `:100` mounts `$HOME/.aws/credentials:ro`, `README.md:81-87` instructs live AWS creds. The private line arrived exactly at `0dab54d` (verified by diffing `0dab54d^`). `storage.go:193-200` `getKeyPath` branches on `s3Bucket != ""`. `main.go:518-523` and `:529-535` both gate on `deployedEnvironment()`, which returns **false** for `development` (`env_guards.go:37-44`). `stack-seeding/isolation/audit.go:146` @ rext authoring `6130bfd8` forces **only** `STORAGE_S3_PUBLIC_BUCKET`; a repo-wide grep finds no forced set for the private one.
9. **The storage M903 / custody correction.** `storage/terraform/main.tf` @ `9f8cb532` is **18** lines; `:9-11` and `:13-16` and `:18` all read as quoted, and `:18` is indeed *not* a custody clause. `custody` → **0** hits in the whole storage repo at that ref (it existed at `8e558ce:main.tf:36-37` and left with `e6fac97`). M903's only occurrence is `terraform/storage.tf:22`, quoted correctly across `:22-25`; no `moved` block exists in any `.tf`. `d3e6d32`'s body contains both *"M903 never ran."* and the superseded *"relocate the assets out of this module BEFORE M907"*. At `4ce8ece5` `main.tf` is **100** lines and `storage.tf`'s header still carries the live-M903 instruction.
10. **All Go module pins and their line anchors.** `app/go.mod` @ `b948604f`: `:16` colony v0.35.2, `:18` proto v1.210.0, `:20` taxonomy v1.2.0 (direct). `sentinel/go.mod` @ `88bc5592`: `:8` colony v0.34.3, `:9` proto v1.200.0, `:21` taxonomy v1.2.0 `// indirect`. `storage/go.mod` @ `4ce8ece5`: colony v0.34.3, proto v1.196.0, taxonomy `:25 // indirect`. `messenger/go.mod` @ `fa47850d`: colony v0.35.2, proto v1.210.0, taxonomy `:9` direct. `repos.yml` @ `0c91421d` has exactly **4** entries (app, sentinel, next-web-app, studio-desk), **2** of type `go`.
11. **The cms M810 "report both, assert neither" pair.** `cms/terraform/main.tf` @ `f38c0c4a` is **191** lines, declares `module "cms"` at `:30`, `service_desired_count = 0` at `:39`. `6efa1d5` (merged `f38c0c4`, 2026-08-04) deletes `.github/workflows/build-production.yml` (18 lines, one file) with exactly the quoted subject and body naming `module "cms_euwest1"`.
12. **The jobsimulation M810 landing.** `jobsimulation/terraform/main.tf:15-22` @ `82cb66e` is the decommission comment; `:24-40` the surviving ownership; `service_desired_count` no longer appears.
13. **The new prod-terraform address.** `app/terraform/locals.tf:6` `project = "backend"`, `:8` `rpc_port = 8081`, mapped at `main.tf:185-186`; `messenger/terraform/variables.tf:77,82,87,92` and `app/terraform/variables.tf:197,230` are all `variable "*_rpc_addr…"` blocks with **no default**. `http://backend.internal.anthropos:8081` is corroborated in-clone at `app/knowledge/service-dependencies.md:46`. Every corpus site now agrees (dependency_map, skiller.md, backend.md ×2, cms.md ×2, jobsimulation.md); the only surviving `backend:8081` is the one that deliberately quotes the short form to disown it.
14. **The alignment_testing.md relocation.** rext's `alignment/go.mod` declares `module anthropos.dev/alignment`; `alignment/` holds `cmd`, `internal`, `examples`. `git ls-files` over rosetta shows no executable alignment code. The file's *Where things live* section (`:510-533`) already said the same thing — the new paragraph is consistent with it, not a second voice.
15. **No compose file sets any `*_RPC_ADDR` at `0c91421d`** (only two prose lines in platform's own `CLAUDE.md`), positive control `SKILLER_STREAM` matches at `docker-compose.yml:71`.

**RE-DERIVED only (weaker — recorded as such):**

- `external_services.md` cross-file anchors `:579` (the retraction sentence), `:567` (the Anthropic Direct row), `:602-607` (the four-live-EU-exit enumeration), `:619-629` (item 4) all land on the intended constructs; the +24 arithmetic reproduces.
- `seeding-spec.md:497-499` lands on the M219 retraction (`stories-spec.md:603`'s bumped anchor is right).
- `storage.md:25` and `:40,47` (cited from `platform-alignment.md:1041` and `dependency_map.md:19`) still resolve.

---

## What I could not settle

- **GitHub archive state for `jobsimulation`, `skillpath`, `skiller`, `graphql-wundergraph`, `chronos`.** Archive state lives in the org API (`gh api repos/anthropos-work/<r> --jq .archived`), not in git objects. I could only measure the *refutation* (four post-dated commits on jobsimulation's `origin/main`), not the state. The repair's "report both, assert neither" framing is the right disposition; B7 is about the one site that did not adopt it.
- **Whether `module.cms_euwest1` / the storage assets still stand in production.** `infrastructure` is in no clone set. Both sides of the cms contradiction are measured; the resolution is not measurable here, and the corpus correctly says so.
- **`DEF-M257x-iter80-storage-prod-bucket`** (cited in `safety.md:203,236,286`, `seeding-spec.md:102`, `storage.md:78`). The deferral ledger lives under `knowledge/plan/**`, which HARD BAR 1 forbids me to open. I verified the *technical* content of the exposure completely; I could not verify that the deferral id exists or carries severity `high`.
- **`app/knowledge/plan/releases/09.00-support-in-app/m903-s3-custody/` front-matter** (`status: planned`, *"Planned, not started"*). That path is inside `stack-demo/app`, i.e. a platform clone rather than rosetta's own `knowledge/plan/`, so reading it is permitted — but I judged the surrounding claim already settled by `storage.tf:22-25` and `d3e6d32`'s body, which I did read, and did not open it. A future seat wanting the last 5% should read those two files at `app` `2035f9a`.
- **`corpus/architecture/dependency_map.md:59`'s "the producer was the standalone skiller service"**. The `skiller` repo is in no clone set, so the *identity* of the historical publisher is unverifiable here. The load-bearing half — that nothing in `app` publishes to `SKILLER_STREAM` at either ref — I did enumerate, and it holds.
