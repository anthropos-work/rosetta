# Seat B — reading #19

Corpus under audit: `rosetta` @ `00be1ac`, tree clean.
Ground truth re-derived at open (matches the addendum sheet):
`platform 0c91421` · `app b948604f` (origin/main `2035f9a4`) · `app/studio aeec036` · `cms/studio aeec036` ·
`next-web-app bb3313bc0` (origin/main `8297c684`) · `storage 4ce8ece` (origin/main `9f8cb53`) ·
`messenger fa47850` (origin/main `e9421c6`) · `ant-academy 9c3843cd` · `jobsimulation 462343b0` (origin/main `82cb66ec`) ·
`cms ca50c81` (origin/main `f38c0c4`) · `sentinel 88bc559` (origin/main `f2c4619`) · `studio-desk 14a5442` ·
`roadrunner 87d8d44` · `graphql-wundergraph 60c229f`.

**Ref-drift note (rule 26, re-checked at close):** the `rosetta-extensions` **authoring** copy moved from
`6130bfd8` → `bc2ee74` *during* this reading. All rext citations below were graded at `6130bfd8` per the
addendum; `stack-core/platform_alignment_guard.py` is **byte-identical** between the two revisions
(`git diff --stat 6130bfd8 bc2ee74 -- stack-core/platform_alignment_guard.py` → empty), so the fence run
reported in B4 is valid at the graded ref.

**Search-set note (addendum §"how to establish an ABSENCE"):** every absence claim below was run
per-tree at each tree's own ref over the 15 trees returned by
`find stack-demo -name .git -maxdepth 4`, the nested `stack-demo/app/studio` and `stack-demo/cms/studio`
repos included, each with a positive control in the same pass.

## Positive controls

| file | `wc -l` | read in full? |
|---|---|---|
| `corpus/services/ai-readiness.md` | 651 | yes |
| `corpus/services/backend.md` | 318 | yes |
| `corpus/architecture/platform-migration-status.md` | 307 | yes |
| `corpus/services/sentinel.md` | 166 | yes |
| `corpus/services/customerio-sync.md` | 147 | yes |
| `corpus/services/README.md` | 83 | yes |
| `corpus/architecture/README.md` | 38 | yes |
| (protocol, read before starting) `corpus/ops/platform-alignment.md` §5 | 2178 total | §5 in full (249–1279) |

---

## Blockers

### B1 — `backend.md`'s banner table asserts cms's M810 teardown is "still pending", which the same file 30 lines later says is not assertable

- **Anchor:** `corpus/services/backend.md:13` — quoted verbatim:
  `> | [cms](./cms.md) | cms-in-app v8.0, app **v1.360.0** (prod teardown **M810 — still pending**) | content layer + Directus edge + Studio — `internal/cms/` |`
- **Why it is false:** the same blockquote at `corpus/services/backend.md:44-46` states the opposite as a
  standing instruction — *"**Whether that infrastructure-side deletion has actually been applied is NOT
  MEASURABLE from any clone set we have** — `infrastructure` has never been in one. So do not assert either
  way"* — and explicitly records that a **flat** form of this very claim was retracted at `:49`
  (*"This bullet previously read … as a flat fact, which the map already said it could not see"*). The fenced
  map says the same thing and is named as authoritative:
  `corpus/architecture/platform-migration-status.md:88` — *"Still UNMEASURABLE here … report both, assert
  neither."*
  The measured evidence is genuinely two-sided and I opened both halves:
  `cms/terraform/main.tf:39` @ `f38c0c4a` is still `service_desired_count = 0` (module declared), **and**
  `cms` commit `6efa1d5` (2026-08-04, merged `f38c0c4`) deletes `.github/workflows/build-production.yml`
  with subject *"drop build-production — the cms ECR repository is decommissioned (M810)"* and body
  *"M810 deletes `module \"cms_euwest1\"` from the platform's `services.tf`, which destroys the ECS service
  and the production-cms ECR repository … would try to push an image into a registry that no longer exists."*
  A reader of the table cell alone takes "still pending" as settled fact; the corpus's own rule for this row
  is that neither direction may be asserted. Repair corrected the prose and left the table cell — the
  mechanical class the addendum flags.
- **Confidence:** medium-high. (Hesitation: "M810 — still pending" *could* be read as "the M810 milestone has
  not closed", which is defensible. I book it because the neighbouring bullet exists precisely to forbid the
  reading a reader will actually take, and because the cell sits in a summary table that is read first.)

### B2 — `ai-readiness.md` cites `:458` of itself as the corroborating past-tense passage; line 458 is a blank line

- **Anchor:** `corpus/services/ai-readiness.md:46` — *"This paragraph stated completed work as outstanding and
  contradicted `:458` of this same file, which is already in the past tense; corrected M257x iter-46."*
- **Why it is false:** measured on the file under audit —
  `awk 'NR==458' corpus/services/ai-readiness.md` returns the **empty string**. `:456-457` end the
  *"Route 2 is exactly the shape M51 seeds"* paragraph and `:459` opens the M219 `✅ CORRECTED` blockquote,
  which is about the `?cycle=` pointer bug, not about the D-07 demo-patch re-anchor. The passage that is
  actually "already in the past tense" about `app-aireadiness-snapshot-loadmembers` is at **`:490`**
  (*"M51 iter-09 bounds it with the `app-aireadiness-snapshot-loadmembers` app read-path demo-patch …
  180 s → 19 ms"*) — a ~32-line drift. This is `platform-alignment.md` §5 rule 34's own failure mode
  (*"a repair that changes a file's line count silently invalidates every citation into that file below the
  edit"*), landed in a paragraph whose whole job is to certify that a correction was applied.
  Everything else in that paragraph verifies: the manifest really does read
  `path: internal/aireadiness/readiness.go` at
  `rosetta-extensions/demo-stack/patches/app-aireadiness-snapshot-loadmembers/app-aireadiness-snapshot-loadmembers.yaml:42`
  and its header really does say *"v2.7 M254 RE-POINT"* at `:33` (both opened at rext `6130bfd8`), and
  `demo-stack/tests/test_aireadiness_snapshot_loadmembers_m254.py` exists.
- **Confidence:** high.

### B3 — `ai-readiness.md`'s `AI_READINESS_URL` anchor points at a different constant

- **Anchor:** `corpus/services/ai-readiness.md:305` — *"`AI_READINESS_URL` (`packages/core-js/src/constants/urls.ts:52`)"*
- **Why it is false:** at `next-web-app` `bb3313bc0` (the checkout; the block names no ref, so it grades here)
  `packages/core-js/src/constants/urls.ts` reads
  `:49 export const WORKFORCE_URL`, **`:50 export const AI_READINESS_URL = '/ai-readiness';`**,
  `:51 export const TALK_TO_DATA_URL`, `:52 export const ORGANIZATION_FEEDBACK_URL = '/enterprise/organization-feedback';`.
  The cited line resolves and is non-blank, and it names **a different constant** — the exact
  "resolves but names the wrong construct" class §5 rule 21 isolates. Confirmed by
  `git grep -n AI_READINESS_URL bb3313bc0`, whose only code hits are `urls.ts:50` and
  `useNavbarSections.tsx:4,400`.
  The rest of the same bullet is correct and I opened each: `useNavbarSections.tsx:4` is the import,
  `:398-400` is `const aiReadinessMenuItem: MenuItemProps = { label: tNavbar('aiReadiness'), key: AI_READINESS_URL,`,
  `:547` is `showAIReadiness ? aiReadinessMenuItem : null,`, and `e2e/specs/web.ai-readiness.spec.ts` exists.
- **Confidence:** high.

### B4 — the migration map's own fence-coverage figure (66 range-only citations) is contradicted by the guard it ships the command for (69)

- **Anchor:** `corpus/architecture/platform-migration-status.md:205` —
  *"| every other `file:line` — terraform, `repos.yml`, `common.yml`, `app/…` (**66** citations) |
  **resolution + range only.** …"* (paired with `:204`, *"`docker-compose.yml` citations — **23** of them"*).
- **Why it is false:** I ran the document's own command verbatim from `:212-216`:
  ```
  PLATFORM_REPOS_YML=stack-demo/platform/repos.yml \
    python3 .agentspace/rosetta-extensions/stack-core/platform_alignment_guard.py
  ```
  → `assertion F resolved 92 citation(s) — 22 subject-checked, 69 range-only, 1 outside any service block;`
  `0 unresolvable; 0 read from the WORKTREE (no ref resolved)` … `OK` … `EXIT=0`.
  Reading `platform_alignment_guard.py:425-455`, `range_only` is incremented for exactly the non-compose
  citations (`if path != _COMPOSE_FILE: reach["range_only"] += 1`), and compose citations split into
  `subject_checked` + `unattributable`. So the doc's **23** reconciles (22 + 1 = 23) and its **66 does not**:
  the measured figure is **69**, and the doc's two numbers sum to 89 against the guard's 92.
  This is the one row in §4 whose entire purpose is to state the fence's *coverage*, in a section that
  argues (`:208-210`) *"F's reach is printed on **every** run … a fence whose coverage shrinks in silence is
  the failure this milestone has now found four times."* The number next to that sentence is
  hand-maintained and stale.
- **Confidence:** medium-high. (Hesitation: this is a coverage-accounting number rather than a claim about
  the platform. I book it because the briefing lists "wrong counts" as a blocker, because it is derivable in
  one command the document itself ships, and because the map was re-measured at `0c91421` in the repair
  immediately preceding this reading — so it is a repair-induced miss, not an old one.)

### B5 — `sentinel.md` cites messenger's compose block as evidence, in the present tense, for a block `838d907` deleted — contradicting two later passages of the same file

- **Anchor:** `corpus/services/sentinel.md:5` — *"**`messenger` is not a caller** — its compose block sets no
  `AUTHORIZATION_ADDRESS` and declares no `depends_on: sentinel`, and its Go source imports no authorization
  client at all"*
- **Why it is false:** at platform `0c91421` (the checkout) `docker-compose.yml` declares **five** services —
  `sentinel`, `backend`, `studio-desk`, `next-web-app`, `gotenberg` — and **no `messenger` block exists**
  (`838d907`, +11/−96 on that file, deleted it together with `storage` and `customerio-sync`). The pin in the
  preceding sentence is `0dab54d` and, per the addendum's pin-scope rule, a pin's scope is the claim's own
  wrapped sentence; this sentence carries no ref, so it grades at HEAD, where its premise does not exist.
  The same file states the correct fact twice: `:85` — *"The blocks that used to carry it are gone rather than
  corrected: `jobsimulation`, `cms` and `roadrunner` at `d11a403`, then `storage`, `messenger` and
  `customerio-sync` at `838d907`"* — and `:89` — *"`messenger` and `storage` never called it, and neither is a
  compose service any more (deleted at `838d907`)"*. A corrected claim left standing at a third site.
  For the record the *conclusion* is true and I verified its surviving half independently:
  `git -C stack-demo/messenger grep -n authorization e9421c68 -- '*.go'` returns **0** (positive control:
  `grep -c connect` on `cmd/root.go` returns 15), and at `0dab54d` messenger's block did set no
  `AUTHORIZATION_ADDRESS` and depended only on `redis`/`postgresql`/`backend`.
- **Confidence:** medium. (Hesitation stated above: the sentence's conclusion is correct and independently
  supported; only its cited evidence is an artifact that no longer exists. Booked per "when in doubt, book it
  as a BLOCKER", and because it is the file's own opening paragraph — the highest-propagation sentence in it.)

---

## Minors

- `corpus/services/README.md:32` — the `backend.md` one-liner enumerates the folded domains as
  *"skiller …, skillpath, jobsimulation, cms and roadrunner"*, omitting **storage, messenger and
  customerio-sync**, which the same file's banner 20 lines above (`:11-12`) names as folded. Incomplete
  enumeration in a summary cell; the banner prevents a reader from being misled.
- `corpus/services/ai-readiness.md:447` — *"Retracted here and at [`ops/demo/stories-spec.md:599`]"*.
  `stories-spec.md:599` is mid-sentence of the paragraph that *states* the falsified premise; the retraction
  blockquote begins at `:601`. 2-line drift inside the same block.
- `corpus/services/ai-readiness.md:581` — `computeCycleTotals` cited as `how_we_measure.go:253-261`. At
  `app b948604f`, `:253-254` are the tail of the *preceding* function; the doc comment runs `:256-259` and
  the `func` is at `:260`. The range straddles a function boundary but does contain the construct.
  (The sibling citation `:285-287` is correct — `FROM public.interactions i` / `JOIN public.job_simulation_sessions s`.)
- `corpus/services/ai-readiness.md:305` — *"A repo-wide grep finds the constant in exactly those two
  non-`node_modules` files."* Measured: three tracked files contain `AI_READINESS_URL` — the two named plus
  `knowledge/ai-readiness/frontend-architecture.md:15`. Defensible if "files" means code files; unstated.
- `corpus/services/ai-readiness.md:509` — bare `useAIReadiness.ts:48-62` for `deriveMode`. Two tracked files
  carry that basename; the correct one is `apps/web/src/components/ai-readiness/useAIReadiness.ts:48`
  (verified), while `:274` elsewhere in the doc (correctly path-qualified) is the *other* file,
  `apps/web/src/hooks/useAIReadiness.ts`. Ambiguous bare filename — §5 rule 36's two-clones-one-name hazard
  one level down.
- `corpus/services/backend.md:114` — `app/terraform/main.tf:58` is cited for *"registers in the Cloud Map
  service-discovery namespace `internal.anthropos`"*; `:58` is
  `service_discovery_namespace_id = var.service_discovery_namespace_id`, which does not carry the literal.
  The doc concedes four lines later that the literal lives in the un-cloned `infrastructure` root module, so
  this is under-specified rather than wrong.
- `corpus/services/sentinel.md:31` vs `:41` — the third role grouping is written `g3(org, user)` in the matcher
  table and `g3(org, membership)` in the grouping list. The model declares `g3 = _, _` and `m6` uses
  `g3(p6.org, r6.sub)`; harmless naming inconsistency.
- `corpus/architecture/platform-migration-status.md:78-81` — the stated derivation
  (`git log -p --follow -- docker-compose.yml` → "26 names") reproduces exactly 26 for me, but the set it
  returns includes `app-network`, a **network** key rather than a service, and `app-network` has no row —
  which the next sentence's own rule (*"a name they return that has no row is a gap"*) would flag.
- `corpus/services/ai-readiness.md:915-938`-class nit: `queryReadinessStarters` is cited as
  `aireadiness/steps.go:915-938`; the function runs `:915-939` (the closing brace is `:939`). One line short.

---

## Positively cleared (audited zeros)

**ENUMERATED** — predicate enumerated from source independently, cardinalities compared:

1. `repos.yml` @ `0c91421` is **28 lines / 4 entries** (`app` · `sentinel` · `next-web-app` · `studio-desk`) —
   map `:25`, `:128`, backend.md `:19`, services/README `:25`. Every-name-ever set from
   `git log -p --follow -- repos.yml` = **14**, exactly the 14 the map lists at `:76-78`, all 14 have rows.
2. `docker-compose.yml` @ `0c91421` declares **5** services (+ `postgresql`/`redis` in the included
   `common.yml` = 7 effective) and is **186 lines**; at `0dab54d` it was **271**; `838d907` is **+11/−96** on
   that file. Service counts across the chain measured directly: `d11a403^` = **11**, `d11a403` = **8**,
   `838d907`/`0c91421` = **5** — map `:90` ("one of eleven … left eight … taken that count to five") correct.
3. `d11a403 -- repos.yml` deletes exactly `- name: cms`, `- name: jobsimulation`, `- name: roadrunner` —
   confirming the map's refutation (`:90`) of that commit's own message.
4. `STORAGE_RPC_ADDR` in Go: **15** hits @ `b948604f`, of which **7** are env lookups (`main.go` ×3,
   `internal/jobsimwiring/wiring.go` ×1, one in each of the three `cmd/` tools) and the other **8** split
   2 doc comments / 2 error strings / **3** `t.Setenv` / **1** `t.Fatal` message; 6 of the 7 spelled
   `os.Getenv`, the 7th a lowercase `getenv`. @ `2035f9a4`: **3** hits, all comments
   (`main.go:504`, `jobsimwiring/wiring.go:101`, `internal/storagens/callsites_test.go:189`).
   Map `:92` reproduces every one of these figures exactly.
5. `custody` = **0** occurrences in the whole `storage` repo @ `9f8cb532` (positive control: `bucket` hits
   across 5+ files); **no `moved` blocks**; `storage/terraform/main.tf` is **18 lines**, `:9-11`/`:13-16`/`:18`
   all say what the map quotes; `storage/terraform/storage.tf:22-25` is the M903 supersession text verbatim;
   `d3e6d32` (2026-08-05) carries *"M903 never ran."*
6. `app` RPC mux @ `b948604f`: `:1185` `http.NewServeMux()`, `:1187` Users, `:1188` Organizations, `:1196`
   Skiller, `:1204` JobSimulation, `:1212-1214` the `if cmsRPCServer != nil` CMS registration, `:1228`
   LabSession — backend.md `:66` correct on all six.
7. `SkillPathSessionService` = **0** occurrences in Go at `b948604f` *and* `2035f9a4` (positive control
   `UsersService` non-zero), while `app/CLAUDE.md:109` and `app/knowledge/architecture.md:28` @ `2035f9a4`
   both still list it — backend.md `:68-70` correct including both stale-doc anchors.
8. Every `pubsub.NewPublisher` call site in `app` @ `b948604f` enumerated: `main.go:287` (`serviceName`),
   `:637` (`SKILLPATH_STREAM`), `:1039` (`CMS_STREAM`), `jobsimwiring/wiring.go:127` (`AI_USAGE_STREAM`),
   `:180` (`JOBSIMULATION_STREAM`). `SKILLER_STREAM` occurs **once** in Go and it is `main.go:1276`
   `subServer.AddSubscriber(...)`. At `2035f9a4` its only uses are the subscriber-name struct
   (`main.go:1537`) and tests — backend.md `:136-138`, `:264` correct.
9. `defaultReadinessSkills` = **19** entries @ 1.0 + **12** @ 0.5 = **31 / weight 25.0**;
   `defaultReadinessSims` = **3** (`{StepSimulation,"tech"}`, `{StepSimulation,"business"}`,
   `{StepInterview,"both"}`) — ai-readiness.md `:154`, `:252-253`, `:356`, `:361` correct.
   (`defaults.go`'s own package header says *"the two Directus simulations"* — a stale platform comment the
   corpus does **not** repeat.)
10. `AIReadinessCSVColumns` has **15** entries at both `b948604f` and `2035f9a4` — ai-readiness.md `:275`
    correct **against the predicate**, even though the platform's own `csv.go:35` doc comment still says
    *"writes the 19-column CSV"*. (Rule 17 in action; the corpus is right and the code comment is stale.)
11. Sentinel's Casbin `ModelConf`: **6** request definitions (`r`…`r6`), **6** policy definitions (`p`…`p6`),
    **3** role definitions (`g`,`g2`,`g3`), **6** matchers (`m`…`m6`) — sentinel.md `:26` correct.
    `manager` as a role: **0** matches in `init_policy.sql` (positive control: `content_creator` block found at
    `:88-118`, file is 118 lines, `casbin_content_creator_test.go` exists) — sentinel.md `:43-48` correct.
12. `AUTHORIZATION_ADDRESS`: **1** occurrence across `docker-compose.yml`, `common.yml` and `.env_example`
    @ `0c91421` (all three files verified present; positive control `CLERK` = 10 in `.env_example`), and
    **0** `*_RPC_ADDR` anywhere in those three — sentinel.md `:85`, backend.md `:31`, `:241` correct.
13. `github.com/anthropos-work/authn` = **0** in every `go.mod`/`go.sum` across all **15** trees at their own
    refs, nested `app/studio` + `cms/studio` included (positive control: `anthropos-work/colony` present in 7
    of them) — architecture/README.md `:21` correct.
14. `MESSENGER_RPC_ADDR` = **0** across all 15 trees at their own refs; the only `MESSENGER_RPC` substring
    anywhere is inside `app`'s own planning doc asserting the same zero. `git -C stack-demo/platform log -S
    'MESSENGER_RPC' --oneline 0c91421d` = **0** commits, positive control `-S 'SKILLER_RPC'` = **7** —
    services/README.md `:39` correct, including its own stated positive control.
15. **13** `ai_readiness_*` ent schema files at `2035f9a4` (cycle · diagnose_narrative · email_override ·
    live_snapshot · notification_log · notification_optout · recommendation · sim · skill · snapshot · step ·
    text_translation · user_step_progress) — backend.md `:231` correct, and its "original 9 + the 4 named
    omissions" decomposition is consistent with the enumerated set.
16. `AI_READINESS_FLAG`'s only functional consumer repo-wide is
    `apps/web/src/components/ai-readiness/data/useAiReadinessActive.ts:22`; the constant is declared at
    `aiReadiness.constants.ts:26` — ai-readiness.md `:71-74`, `:316` correct.
    `AIReadinessClient.tsx` contains **0** `posthog` and **0** `flag_ai_readiness` (positive control
    `useAiReadinessEnabled` = 2), and `:133-134` is `const { orgEnabled } = useAiReadinessEnabled(true);` —
    ai-readiness.md `:76-81` correct.
17. `next-web-app dae0fb2f7` (2026-07-13, *"drop orphaned container"*) deletes `AIReadinessContainer.tsx`
    (103) + `AIReadinessIntro.tsx` (220) + `AIReadinessView.tsx` (330) = **653** lines, and
    `apps/web/src/app/**/enterprise/workforce/ai-readiness/**` is absent at `bb3313bc0` —
    ai-readiness.md `:285-289`, `:294` correct.
18. **5** AI-readiness Playthroughs: 4 in `playthroughs/manifest/ai-readiness.yaml`
    (`pt-aireadiness-member-done`, `-member-progress`, `-manager-dashboard`, `-manager-howwemeasure`) +
    `pt-onboarding-aireadiness-guided` in `onboarding.yaml:361` on seat `pt-ai-onboard` —
    ai-readiness.md `:630-633` correct.
19. `SKILLER_RPC_ADDR` = **4** occurrences in `docker-compose.yml` @ platform `0808b92`, **1** @ `0dab54d`
    (messenger's), **0** @ `0c91421` — backend.md `:106-111` correct at each named ref.
20. `aiReadinessUsageKPIs` (rext `6130bfd8`, `stack-seeding/seeders/ai_readiness_interview_report.go:163`)
    returns exactly **5** ids at `:185-189` — `avg_adoption`/`avg_depth`/`avg_ownership`/`avg_transformation`/
    `avg_originality` — and `:149` reads *"All five are always emitted here."* The platform's
    `usageDimSpecs` (`app/internal/aireadiness/how_we_measure.go:1138`) keys on the same five with bands
    61/41 — ai-readiness.md `:201`, `:213-219`, `:620-624` correct.
21. `corpus/services/` holds **29** `.md` files; minus `README.md` + `TEMPLATE.md` = **27** service docs, and
    the index enumerates exactly 27 rows (10 core + 5 frontends/gateway + 8 cross-cutting + 4 archived).
22. `internal/workforce/` contains **no** `readi*` file at `2035f9a4` (20 files enumerated); every file
    ai-readiness.md names under `internal/aireadiness/` exists (`readiness.go`, `scoring.go`, `steps.go`,
    `cycles.go`, `compare.go`, `csv.go`, `narrative.go`, `provision.go`, `defaults.go`, `manager.go`,
    `how_we_measure.go`, `recommendation_engine.go`, `recommendation_signals.go`, `notifications/`,
    `emailoverride/`, `emailpreview/`) — backend.md `:229-231`, the ai-readiness refactor table `:25-35`.
23. `app/internal/roadrunner/` does **not** exist (73 entries under `internal/`), and the Judge0 runner is
    `jsrunner.NewRunnerManager(getenv("JUDGE0_API_KEY"), getenv("JUDGE0_BASE_URL"))` at
    `app/internal/jobsimwiring/wiring.go:123` — map `:87`, `:90` correct.
24. Historical compose provenance, all opened: `cb6ebf5` 2023-04-30 *"First commit"*; `8770fe6` 2023-05-04
    *"remove nats service from compose"* (four days later); `467965a` 2023-09-11; `1474b1f` 2023-12-01;
    `84862d1` 2024-05-29 *"replace simulator with jobsimulations"*; `ef4b449` 2024-09-02; `a2a3ee6` 2026-02-27;
    `b43b99a` 2026-02-26; `c17cc9a` 2026-04-15 — map `:113-118` correct on every date and subject.
25. Subgraph history: `749dc86` (2026-06-24) deletes `schemas/skiller.graphqls`; `7c17e63` (2026-07-21)
    deletes `schemas/skillpath.graphqls`; `915da06` (2026-07-29) deletes **both** `schemas/cms.graphqls`
    **and** `schemas/jobsimulation.graphqls` in one commit. At `60c229f3`, `supergraph-config-prod.yaml`
    lists `backend` alone, `schemas/` holds `backend.graphqls` alone, `subgraphs.conf` = `BACKEND=v1.360.0` —
    backend.md `:22-26`, `:122-133`, map `:96` correct.
26. Terraform desired counts, each opened in context: `app:181` = 1 · `sentinel:19` = 1 · `roadrunner:19` = 1 ·
    `graphql-wundergraph:20` = 1 · `cms:39` = 0 · `messenger:29` = 0 (module 121 lines, `:19-25` and `:27-28`
    as quoted) · `jobsimulation` — `service_desired_count` **absent from the whole repo's `.tf`** at
    `82cb66ec`, with `terraform/main.tf:15-22` the M810 delete comment and `:24-40` the retained-assets
    comment; the four 2026-08-04 commits (`6092c6d2`, `caf36c96` merge-button by `GitHub`, `1e40d184`,
    `82cb66ec` v0.254.0) are all present — map `:89`, `:90`, `:91`, `:92`, `:93`, `:96`, services/README `:17`.
27. `messenger/cmd/root.go:120-140` reads exactly the four `*_RPC_ADDR` values (`CMS_RPC_ADDR:120`,
    `BACKEND_USERS_RPC_ADDR:125` and `:130`, `SKILLER_RPC_ADDR:135`, `JOBSIMULATION_RPC_ADDR:140`) —
    map `:93` correct; and `messenger/internal/flow/flow.go:72` is `AddSubscriber("backend", …)` inside the
    `:70-95` `Subscribe()` body — services/README `:39` correct.
28. `customerio-sync.md` anchors all opened and correct: `internal/customeriosync/{doc.go,store.go,sync.go,
    sync_query.sql}` + `cmd/customerio-resync/main.go` exist; `doc.go:4` is the *"the last of the Go services
    to be folded into app"* provenance line; `env_guards.go:62` is `envCustomerIOSyncEnabled =
    "CUSTOMERIO_SYNC_ENABLED"`; `main.go:284` is the *"unset is off on a laptop and fatal in production"*
    comment; `:286` the switch; `:295` the `BREVO_KEY` fatal guard; `:393-396` the `copilotDB`-backed
    construction; `docker-compose.yml:84-92` the deliberate no-value comment. The HISTORICAL compose block
    matches `0dab54d:136-154` line for line, including `profiles: [customerio-sync, all]`.
29. `messenger`/`customerio-sync` prod switches: `app/terraform/main.tf:415-416` `MESSENGER_ENABLED="true"`,
    `:419-420` `CUSTOMERIO_SYNC_ENABLED="true"`; `app/env_guards.go:92-111` is exactly
    `resolveSubsystemSwitch`; `app/main.go:1416-1421` is the consumer-group literal comment — map `:93`,
    `:101` correct.
30. Sentinel misc: `go.mod:3` `go 1.26.0`; `Dockerfile:2` / `Dockerfile.dev:2` `golang:1.26-bookworm`;
    `terraform/locals.tf:4-5` = 256 CPU / 128 MB; binary default port 8080 at `cmd/root.go:47`;
    `init_policy.sql:63-66` the taxonomy:write NOTE; `local_superadmin_grants.sql` present;
    `app/internal/data/ent/enum/membership.go:8-15` = the four `MembershipRole` values.
31. `computeKnowledgeAxis` is literally `((step1+step2)/70)*100` and `computeUsageAxis` is
    `round(step3*100/30)`; `classifyArchetype` uses `archetypeHighBand = 75` / `archetypeLowCeil = 50` with
    Champion≥75/≥75, Standby≤50/≤50, tie→Explorer; `scoring.go:63-66` gives bands
    `None 0-24 / Low 25-50 / Medium 51-74 / High 75-100`; `computeTier1` (`readiness.go:139`) divides by the
    **sum of all configured weights** and carries the platform comment the corpus quotes verbatim
    (*"a larger configured set makes a full score harder to reach"*) — ai-readiness.md `:143-150`, `:548-557`.

**RE-DERIVED** — arithmetic the document itself displays, re-computed (weakest form of clearance):

- `156/199 = 78.39 %` (ai-readiness.md `:348`).
- `19×1.0 + 12×0.5 = 25.0`; `round(1.0/25×30)=1`; `round(0.5/25×30)=1`; `round(9/25×30)=11`;
  `round(3/6.5×30)=14`; `25/25×30=30` (ai-readiness.md `:548-568`).
- `103+220+330 = 653` (ai-readiness.md `:286`).
- `22 subject-checked + 69 range-only + 1 unattributable = 92`, vs the map's `23 + 66 = 89` (→ **B4**).

---

## What I could not settle

- **GitHub archive states.** The map and `services/README.md` assert archived/not-archived for
  `jobsimulation`, `chronos`, `skiller`, `skillpath`, `graphql-wundergraph`, `storage`, `messenger`, `cms`.
  Archive state lives in the org API, never in git objects — the map says so itself
  (`gh api repos/anthropos-work/<r> --jq .archived`). Not measurable from the clone set. The map's
  jobsimulation row already handles this correctly by reporting the four post-"archive-date" commits and
  refusing to conclude.
- **Prod terraform in the un-cloned `infrastructure` repo.** The `roadrunner` `= 1` vs `repos.yml`
  contradiction and `customerio-sync`'s prod state both terminate there, and the map declares the gap
  (`:90`, `:101`, `:260-265`). Would be settled by adding `infrastructure/services.tf` to a clone set.
- **`platform-migration-status.md:126-133` §3 census** — "93 repositories … 4 in `repos.yml` … 46 named by no
  corpus document". The 4 I enumerated; 93 and 46 need the GitHub org API at a fixed date.
- **`interactions.action_type` = exactly `{email, call}`** (ai-readiness.md `:587`). The ent field is
  `field.Enum("action_type").GoType(interactions.ActionType(""))` — the type comes from the private
  `anthropos-work/proto` module, which is not in the clone set (pulled at Docker build). Not measurable here.
- **`app/knowledge/plan/releases/09.00-support-in-app/m903-s3-custody/` front-matter** quoted at
  map `:92` (`status: planned`, *"Planned, not started"*). I deliberately did **not** open it — HARD BAR 1
  forbids reading under `knowledge/plan/**`, and I chose the conservative reading of that bar. The rest of
  the M903 claim (the `storage.tf:22-25` supersession text, the absence of `moved` blocks, `d3e6d32`'s
  *"M903 never ran."*) is verified independently and points the same way.
- **Live measurements**: 2.09 s live recompute, 24 ms / 19 ms / 180 s frozen reads,
  `validation_attempt_results` 5 → 345, 897/787 row counts, "78.4 % of 199", `2.09 s`-vs-`24 ms` cost line
  (ai-readiness.md `:377`, `:469`, `:476-479`, `:488-492`, `:536`, `:616-618`). These are demo-stack runtime
  numbers; settling them needs a cold reset-to-seed run, not a read.
- **`platform-migration-status.md:113`** — *"`directus` … Removed from compose at `a2a3ee6` (2026-02-27)"*.
  The commit exists at that date, but its subject is *"add Makefile, repos.yml, and switch to local
  Dockerfile.dev builds"*; I did not diff the compose file at that commit to confirm the directus removal is
  in it. Cheap to settle (`git show a2a3ee6 -- docker-compose.yml`), left unclaimed rather than assumed.
- **`ai-readiness.md:183`/`:195-197`** — *"reads **exactly one table**"* for `computeInterviewInsightsV2`.
  The report source really is only `public.interview_aggregated_reports`
  (`how_we_measure.go:1055`, the only read site in Go), but the same subsystem also joins
  `sessions → memberships` via `resolveSessionAuthors`, which the doc itself describes one row later. I read
  the two as compatible (one table for the *report*), but the sentence is looser than the rest of the section
  and a stricter reader could book it.
