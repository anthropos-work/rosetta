# Seat E — reading #20

Corpus under audit: `rosetta` @ `00be1ac` (tree clean). Ground-truth clones re-derived at open, matching
the briefing ADDENDUM sheet exactly:

```
stack-demo/ant-academy       9c3843cd     stack-demo/platform            0c91421
stack-demo/app               b948604ff    stack-demo/roadrunner          87d8d44
stack-demo/app/studio        aeec036      stack-demo/rosetta-extensions  ab81527  (per-stack copy — NOT used)
stack-demo/cms               ca50c81      stack-demo/sentinel            88bc559
stack-demo/cms/studio        aeec036      stack-demo/storage             4ce8ece
stack-demo/graphql-wundergraph 60c229f    stack-demo/studio-desk         14a5442
stack-demo/jobsimulation     462343b0     .agentspace/rosetta-extensions 6130bfd  (authoring — used)
stack-demo/messenger         fa47850      stack-demo/next-web-app        bb3313bc0
```

Search set enumerated with `find stack-demo -name .git -maxdepth 4` **before** any absence claim; the two
nested `studio` repos were grepped at their **own** ref (`aeec036a`), never at the host's.

## Positive controls

| file | `wc -l` | read in full? |
|---|---|---|
| `corpus/architecture/service_taxonomy.md` | 484 | yes |
| `corpus/services/hiring.md` | 433 | yes |
| `corpus/services/graphql-wundergraph.md` | 279 | yes |
| `corpus/services/storage.md` | 232 | yes |
| `corpus/services/clerk-integration.md` | 128 | yes |
| `corpus/architecture/frontend_architecture.md` | 105 | yes |
| **total** | **1,661** | |

Search-pipeline positive controls run in the same passes as every absence claim below:
`grep -ic storage` over `storage @9f8cb53` (non-zero) beside the `custody` zero; `grep -n '"next"'` over
`ant-academy/code/package.json` (hit) beside the `serwist|workbox` zero; `git grep colony` (7/7 clones)
beside the `authn` zero. NUL check on `next-web-app/apps/web/src/hooks/useCoursebuilder.ts` at this
checkout: **1** NUL byte, and `grep -a` confirms it carries **0** `NEXT_PUBLIC_BACKEND_API_URL` hits, so
it is not a hole in the frontend count re-derived below.

---

## Blockers

### B1 — hiring.md's "twin" citation into `service_taxonomy.md:52` names the wrong construct

- **Anchor:** `corpus/services/hiring.md:38` —
  > `— as the twins [`service_taxonomy.md:52`](../architecture/service_taxonomy.md) and [`dependency_map.md:78`](../architecture/dependency_map.md) already said.`

  The claim it is offered as a twin for is bullet 2 at `:30-41`: *"**Everything `app` writes is in
  `public`** — which is *not* the same claim as 'the `jobsimulation` schema is gone.'"*
- **Why it is false:** `corpus/architecture/service_taxonomy.md:52` is
  `> [`dependency_map.md`](./dependency_map.md)'s content-generation flow, which had it right all along.`
  — the closing line of the **Studio-Desk → Backend → Studio-Room generation-edge correction**, which has
  nothing to do with schemas. The statement hiring.md is appealing to is at
  `service_taxonomy.md:62` (*"**one schema, `public`, owned by `app`** … the `cms`, `jobsimulation` and
  `skillpath` schemas are legacy husks"*). The **second** half of the same sentence,
  `dependency_map.md:78`, **does** resolve correctly (*"…or directly to the **`public`** schema (the legacy
  `jobsimulation` schema is non-authoritative)"*), which is what makes the first half read as verified.
- **Why I hesitated:** this is line drift induced by a repair (§5 rule 34), and §5 rule 25 records that the
  canonical grading rule carved line drift out as MINOR. I book it because the pointer does not merely
  drift — it lands on a *different, unrelated adjudication* in a file the sentence names as corroborating
  authority, which is the "resolves but names the wrong construct" class the frozen GRADING section calls a
  BLOCKER.
- **Confidence:** high

### B2 — hiring.md's two `organization/manager.go` anchors resolve to a blank line and a closing brace

- **Anchor:** `corpus/services/hiring.md:80-82` —
  > `as do `organization/manager.go:448` (a forced Clerk membership is created with role `candidate` instead of `member`) and `:485` + `siminvitationlink.go:62` (both **hard-error `"organization is not hiring"`** …`
- **Why it is false:** at the ground-truth checkout `app` `b948604f`:
  - `app/internal/organization/manager.go:448` is a **blank line**. The `switch org.IsHiring` that picks
    `enum.RoleCandidate` over `enum.RoleMember` is at `:449-450`.
  - `app/internal/organization/manager.go:485` is `}` — the closing brace of
    `forceUserToOrganizationInClerk`. The **only** `"organization is not hiring"` hard-error in that file is
    `ForceUserToOrganization` at `:535-537` (`if !org.IsHiring { m.logger.Error(...); return
    fmt.Errorf("organization is not hiring") }`), fifty lines away. `grep -n "not hiring"` over the file
    returns exactly `:455` (a comment), `:536`, `:537`.
  - The third anchor in the same sentence, `app/internal/organization/siminvitationlink.go:62`
    (`if !org.IsHiring {`, error at `:63`), **is** correct.
- **Why I hesitated:** both anchors are exactly right at `app` `5ba17044` — I checked
  (`5ba17044:manager.go:448` = `switch org.IsHiring {`, `:485` = `if !org.IsHiring {`) — and the doc's
  **top-of-file** banner (`:17`) declares it re-grounded *"against platform origin `2adcf71` / `app` @
  `5ba17044`"*. But §5 rule 33 is explicit that a pin's scope is **the claim's own block**, and this claim
  sits ~45 lines further down in a separate section that names no ref; the briefing's rule for an unpinned
  passage is "grade against the checkout". Reinforcing that: the doc's *other* `app` anchors
  (`resolver_queries.go:1034/1035/1053/1080/1085/1089`, `intelligence.go:1700/1820/1844/1846/885-886`,
  `job_simulation_session.go:39/45`, `enum/jobsimulation.go:29-35/37-43`, `graph.go:129546-129554` /
  `:129392-129400`) all verify **at the checkout** — so the file is not uniformly pinned at `5ba17044`; these
  two are stragglers. That is the "corrected at one site, left standing at another" mechanism the ADDENDUM
  warns about.
- **Confidence:** medium-high

### B3 — graphql-wundergraph.md cites `:174-176` "of this same doc" for a sentence that is at `:192`

- **Anchor:** `corpus/services/graphql-wundergraph.md:85-87` —
  > `The host-`5050` mapping was published here in the present tense while `:174-176` of this same doc already said `localhost:5050` refuses the connection; corrected M257x iter-46`
- **Why it is false:** `graphql-wundergraph.md:174-176` reads:
  ```
  174   `NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT` — each also baked as a build arg, at `:119` and `:151`), because
  175   the router service no longer exists in compose. Those line numbers move on every compose
  176   clean-up — they were `:220`/`:236` at `0dab54d` and `:334`/`:352` at `2adcf71` — so grade the
  ```
  — a note about compose line-number drift, containing no `5050` and no "refuses the connection". The
  sentence being cited is at **`:192`**: *"`http://localhost:5050` refuses the connection."*
- **Why this is material:** the citation exists to establish an *internal self-contradiction* — the whole
  weight of the sentence is "this same doc already said the opposite". A reader who follows the pointer
  finds unrelated prose and cannot check the claim.
- **Confidence:** high

### B4 — graphql-wundergraph.md cites `:84` for a statement that is at `:115-116`

- **Anchor:** `corpus/services/graphql-wundergraph.md:132-133` —
  > `So for its **final five months** compose built `Dockerfile.dev` — which is why a subgraph SDL change rebuilt the router, as `:84` describes.`
- **Why it is false:** `graphql-wundergraph.md:84` is the **Ports** bullet
  (`* **Ports**: **8080 → 8080** (router `listen_addr 0.0.0.0:8080`, `graphql_path /graphql`)…`). The
  statement about an SDL change rebuilding the router is at `:115-116` (*"It *used to* rebuild whenever any
  subgraph schema changed, because the build context is the parent dir (`..`) holding all sibling repos."*).
- **Note:** the *substance* of the surrounding paragraph checks out against the platform — `2c85211`
  (2026-02-27, *"chore: update docker-compose and Makefile for improved service orchestration"*) does add
  `dockerfile: Dockerfile.dev`; `67ba772` (2026-03-02) moves the context; `360efd4` (2026-07-31, *"chore(compose):
  remove the router service and clone entry outright"*) deletes the block; 2026-02-27 → 2026-07-31 is five
  months. Only the self-pointer is wrong.
- **Confidence:** high

### B5 — hiring.md cites `:157-159` as "the adjudicated form" of a claim that is adjudicated at `:169-182`

- **Anchor:** `corpus/services/hiring.md:267-269` —
  > `(This said *"neither table exists"* until M257x iter-52 — a flat contradiction of `:157-159` above, which is the adjudicated form: the mirror was dropped, `jobsimulation.sessions` was not, and no local stack creates that schema either way.)`
- **Why it is false:** `hiring.md:157-159` is the **`job_position`** bullet — *"the comparison surface does
  **not** read it, and the captured snapshot has **0 `job_position` rows** … there is no `job_position`
  replay"*. It says nothing about the mirror or `jobsimulation.sessions`. The adjudicated form it describes
  is the *History* blockquote at `:169-182`, whose operative sentence is at `:175`:
  *"**`jobsimulation.sessions` itself was NOT dropped** — no `app` migration touches that schema…"*.
- **Why this is material:** identical to B3 — the anchor is the *evidence* for a self-contradiction verdict,
  so a wrong anchor makes the verdict uncheckable, and a reader landing on the `job_position` bullet will
  reasonably conclude the corpus is confused about which table is which.
- **Confidence:** high

---

## Minors

- `corpus/services/storage.md:44` — *"the live code is `app/internal/storage/` (+ `app/internal/storagens/`,
  `app/internal/publicstorage/`)"*. `app/internal/storage/` **does not exist** at the ground-truth `app`
  checkout `b948604f` (v1.366.0) — the storage fold created it at v1.367.0. It exists at `2035f9a`, and
  every *neighbouring* citation into that directory in this doc is explicitly pinned there
  (`:29` `service.go:22,:24`, `:58` `storage.go:193-200` — both verified at `2035f9a`). This one bullet is
  the only unpinned member of the set. Not booked as a blocker because the sentence is true at the
  platform's current origin HEAD, which §5 rule 41 makes the grading ref for a fetched clone.
- `corpus/architecture/service_taxonomy.md:48-49` — *"the `studioTask` / `studioTasks` /
  `archiveStudioTask` operations are `app`'s, in `…/schemas/cms_queries.graphqls:106`"*. `studioTask` (:106)
  and `studioTasks` (:107-111) are there; `archiveStudioTask` is a **mutation**, at
  `app/internal/web/backend/graphql/graph/schemas/cms_mutations.graphqls:22`. The sentence's actual claim
  ("these operations are app's") is true.
- `corpus/architecture/frontend_architecture.md:39` — the count re-derives **exactly** (29 occurrences /
  21 non-test files at `bb3313bc`; 31 / 22 at `ad767d1b` — both enumerated below), but the *list* that
  follows names only 19 of the 21 files. Missing: `packages/ui/src/AISimulation/Onboarding/TestRuns/
  TestRunNetwork.tsx` and `apps/web/src/app/(unauthenticated)/reminders/[token]/unsubscribe/page.tsx`.
  Omitted list member.
- `corpus/architecture/service_taxonomy.md:33` — the mermaid comment
  `%% A Storage node stood here until 838d907 folded it into Backend` credits the wrong commit with the
  fold: `838d907` deleted the *container*; the in-process fold is v9.0 (`app` `9d00a313` v1.367.0, compose
  `0dab54d`), which `storage.md:18-20` states correctly (*"the v9.0 fold COMPLETED on 2026-08-04 — the
  container went the next morning"*). Invisible in render.
- `corpus/architecture/service_taxonomy.md:37` — the mermaid edge label
  `Academy -->|GraphQL - academy subgraph| Backend` reads as a distinct subgraph; the same file at `:377`
  states `backend` is the **only** subgraph (1). `academy.graphqls` is one schema file inside it.
- `corpus/services/storage.md:25` — *"at the checked-out storage ref `4ce8ece5` … **the same header** still
  reads M903 as a live instruction"*. The preceding sentences are about `main.tf`; the header carrying the
  M903 instruction is `terraform/storage.tf`'s (`4ce8ece5:terraform/storage.tf:23-24`). Also `main.tf:13`
  opens the paragraph whose *"read from configuration"* clause is at `:15`. Everything else in that very
  dense cell verified (see below).
- `corpus/services/graphql-wundergraph.md:176` — *"they were `:220`/`:236` at `0dab54d`"*: at `0dab54d`
  `:220` is studio-desk's **environment** `VITE_GRAPHQL_ENDPOINT` while `:236` is next-web-app's **build
  arg** `NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT` — the pair is env/arg, whereas the current pair the sentence
  contrasts it with (`:135`/`:160`) is env/env. The sentence's own point ("grade the construct, not the
  offset") is sound.
- `corpus/architecture/service_taxonomy.md:374` — *"`5050` … was **only** the LOCAL compose host mapping"*.
  It also survives in the router repo's own `Makefile:10` (`docker run … -p 5050:8080 wundergraph-router`).
  The load-bearing predicate — *"`5050` was never a production port"* — is correct
  (`terraform/locals.tf:8` `port = 8080`, `config.prod.yaml:5` `listen_addr: 0.0.0.0:8080`).

---

## Positively cleared (audited zeros)

Labelled per the briefing: **ENUMERATED** = predicate re-derived from source independently and
cardinalities compared; **RE-DERIVED** = only the arithmetic the document itself displays was recomputed.

### Compose / profile / repo topology (all at platform `0c91421`)

- **ENUMERATED** — `service_taxonomy.md:65-68`: *"`docker-compose.yml` declares five services (seven in the
  effective topology…), the default profile is `core`, and `core` starts five."* Derived by listing every
  top-level service key in both files: `docker-compose.yml` = {sentinel, backend, studio-desk,
  next-web-app, gotenberg} = 5; `common.yml` = {postgresql, redis} = 2 → 7; `core` selects
  {backend, gotenberg} plus the three that declare **no** `profiles:` key → 5. The same derivation at
  `0dab54d` gives 8/10, confirming `:73`'s *"eight/ten at `0dab54d`"*.
- **ENUMERATED** — `service_taxonomy.md:459-460` / `graphql-wundergraph.md:15`: the legal profile set is
  exactly `{all, backend, core, frontend, studio-desk}` (5 tokens) and **four** services carry a
  `profiles:` key (backend `:110`, studio-desk `:141`, next-web-app `:168`, gotenberg `:183`).
- **ENUMERATED** — `service_taxonomy.md:395`: *"exactly one cross-process edge … `AUTHORIZATION_ADDRESS=
  http://sentinel:8087` (`docker-compose.yml:48`) and **zero** `*_RPC_ADDR` variables."* `grep -n RPC_ADDR`
  over `docker-compose.yml`, `common.yml` and `Makefile` returns **0** (positive control:
  `grep -n AUTHORIZATION` → `:48`, one hit).
- **ENUMERATED** — `frontend_architecture.md:11`: `repos.yml` holds exactly **4** entries at `0c91421`
  (`app`, `sentinel`, `next-web-app`, `studio-desk`) and **9** at `2adcf71` (re-derived from
  `git show 2adcf71:repos.yml`), with `ant-academy` in neither. `docker-compose.yml:141`
  `profiles: [studio-desk, all]` ✔ and `:226` at `0dab54d` ✔.
- **ENUMERATED** — `service_taxonomy.md:62`: `repos.yml:14-17` is the `app` entry with `migrations: true`;
  `repos.yml:18-20` is `sentinel` with `migrations: false`; `docker-compose.yml:18` carries
  `search_path=sentinel`. Trap-A case reproduces exactly.
- **ENUMERATED** — `service_taxonomy.md:93`, `:101-102`, `storage.md:48`/`:208-217`: at `0dab54d` the three
  deleted blocks published storage 8300/8301, customerio-sync 8080, messenger 8200/8201; their profiles
  were `[storage-legacy]`, `[customerio-sync, all]`, `[messenger]`. The storage block set
  `PORT/RPC_PORT/SERVICE_NAME/AWS_REGION/AWS_DEFAULT_REGION/STORAGE_S3_PUBLIC_BUCKET` and **never**
  `STORAGE_S3_BUCKET` — matching storage.md's env table row-for-row.
- **ENUMERATED** — `storage.md:60-75`: `docker-compose.yml:82`/`:83` set both buckets to production values
  on `backend`; `:73-79` carries the *"These MUST be set"* comment; `:100` mounts
  `$HOME/.aws/credentials:ro`; `README.md:81-87` @ `0c91421` is the AWS-credentials instruction;
  `app/main.go:518-523` (empty-bucket `log.Fatalf`) and `:529-535` (`verifyBucketAccess`) are both wrapped
  in `if deployedEnvironment()`, and `app/env_guards.go:37-44` returns **false** for `development`. The
  hazard is real as stated.

### The router, the supergraph, and the subgraph ladder

- **ENUMERATED** — `service_taxonomy.md:377` / `graphql-wundergraph.md:43-46`: re-derived the ladder by
  listing `^  - name:` in `supergraph-config-prod.yaml` at each named commit —
  `749dc86~1`→5 (backend, skiller, jobsimulation, cms, skillpath), `749dc86`→4, `7c17e63`→3, `915da06`→**1**.
  `915da06`'s own subject line does say *"supergraph 2→1"*, and `git show --stat 915da06` marks **both**
  `schemas/cms.graphqls` and `schemas/jobsimulation.graphqls` deleted. The corpus's **3 → 1** is the correct
  figure and its warning about the subject line is warranted.
- **ENUMERATED** — `graphql-wundergraph.md:59-69`: `schemas/backend.graphqls` declares `type Mutation`
  (:4053) and `type Query` (:4912) and **no** `type Subscription`; all 7 `Subscription` substring hits are
  `activeSubscription`, `type PlanSubscription`, `stripeSubscriptionId`, `stripeSubscriptionItemId`. No
  `supergraph-config-*.yaml` carries a `subscription:` block. `bba862f` ("change subscription protocol from
  sse_post to ws", 2026-02-25) is contained **only** by `remotes/origin/feat/use-web-socket`.
- **ENUMERATED** — `graphql-wundergraph.md:80-84`, `:103`, `:155`, `:165`, `:180`, `:271`:
  `ghcr.io/wundergraph/cosmo/router:0.275.0`, `wgc@0.104.0`, `node:22.11-alpine`,
  `federation_version: =2.3.2`, `terraform/locals.tf:8 port = 8080`, `terraform/main.tf:20
  service_desired_count = 1`, `:48-49` container 8080 → host 8080, `config.prod.yaml:5
  listen_addr: 0.0.0.0:8080`, `health_check_path = "/health"`, compose routing
  `http://backend:8082/graphql/query`, dev routing `host.docker.internal:8082`,
  `subgraphs.conf` = one line `BACKEND=v1.360.0`, `ci/update-subgraph.sh:9` = the single
  `gh release download … -R anthropos-work/app`, `package.json` = `{"name":"graphql-wundegraph"}` and the
  same misspelling in the repo's `CLAUDE.md` heading. `grep -c 5050 docker-compose.yml` @ HEAD = **0**.
- **ENUMERATED** — `graphql-wundergraph.md:125-133`: every named platform commit exists with the quoted
  behaviour — `63d285c` (2024-06-20 *"replace graphql with wundergraph cosmo router"*), `d92e84e`
  (*"restore graphql profile name"*), `719befb`, `a2a3ee6`, `2c85211` (the diff adds
  `+      dockerfile: Dockerfile.dev`), `67ba772`, `360efd4`, `b56d731`.
- **ENUMERATED** — `graphql-wundergraph.md:206-214`: `app/internal/authorization/gqlauthz/gqlauthz.go`
  exempts only `isPublicOnlyOperation || isFederationQuery || (colony.Development &&
  isIntrospectionOnlyQuery)` (`:175-177`) and rejects at `:186-187`; the regression tests
  `"bare __typename is not exempt"` (`gqlauthz_test.go:132`) and `TestAnonymousRejectionLogsAtWarn`
  (`:176`) both exist.

### The hiring read-model (the densest citation block in my set)

- **ENUMERATED** — `hiring.md:73-77`: `resolver_queries.go:1034` is the `InsightsJobSimulationByMemberships`
  declaration, `:1035` the `OrgFeatureInsights` gate, `:1053` the
  `GetMemberships(… {StatusActive, StatusInvited} …)` call, `:1080` the manager hand-off; `:1085`/`:1089`
  are the neighbouring `InsightsJobSimulationBySessions`'s gate and `GetMembership`. I re-derived the
  document's own displayed derivation **and its set**: `grep -in hiring` over `resolver_queries.go` returns
  **4** hits, all `simulation_type_hiring` / `SimulationTypeHiring` **sim-TYPE filters**, and **0** over
  `internal/organization/intelligence.go` — with the stated positive controls reproducing exactly
  (`OrgFeatureInsights` **×8**, `JobSimulationSession` **×44**).
- **ENUMERATED** — `hiring.md:222-238`, the write-set. `20260722104506.sql` CREATE TABLE
  `job_simulation_sessions` (`:2`) has exactly **four** `NOT NULL` columns with no `DEFAULT` —
  `owner_id` `:6`, `sim_id` `:7`, `sim_type` `:10`, `token` `:13` — enumerated against the full DDL (the
  other eight `NOT NULL` columns each carry a `DEFAULT`); `token`'s UNIQUE index is `:29`. The seeder does
  write it: `rosetta-extensions/stack-seeding/seeders/persona_write.go:152-158` (`sessionCols()`,
  authoring copy `6130bfd8`).
- **ENUMERATED** — `hiring.md:239-260`: `completion_status` is a plain `character varying … DEFAULT
  'pending'` with no CHECK (`:12`); `enum/jobsimulation.go:29-35` is the 5-member const block and `:37-43`
  its `Values()`; `ent/jobsimulationsession.go:181-186` casts unconditionally;
  `graph.go:129546-129554` and the proto-bound twin `:129392-129400` are bare
  `graphql.MarshalString(string(v))` with no membership test; the SDL declares five lowercase members at
  `jobsimulations.graphqls:14` and `:128`. `intelligence.go:885-886` maps
  `InsightsSortFieldCompletitionStatus` → `FieldCompletionStatus`.
- **ENUMERATED** — `hiring.md:20-32`, `:169-182`: `20260729133514.sql:58-62` is *"-- 5. Drop the mirrors."*
  through `DROP TABLE "local_jobsimulation_sessions";`; `grep -rn 'SET "score"'` over the **whole**
  migration set = **0**; `20260722104506.sql:79` is a bare `DROP TABLE "sessions";`; `app/atlas.hcl:8` pins
  `search_path=public`; the **only** `CREATE SCHEMA` in the entire set is
  `20230817154747_supabase_baseline.sql:2` (`auth`). `20250416091037.sql:5` did add `anticheat_summary` to
  the mirror. `anticheat_result.go:24` declares `summary`; `20260722104506.sql:53` re-points the
  `anticheat_results` FK. `20260722081626_jobsim_data_model.sql:336/355/376` are exactly
  `validation_attempt_results` / `validation_attempt_skill_results` / `validation_criterion_results`.
- **ENUMERATED** — `hiring.md:88-133`, `:188-199`, `:288-302`, `:352-376`, the next-web-app half at
  `bb3313bc`: `useGetClerkOrganization.tsx:16-18` and `:20-21`; `apps/web/…/UserStatusContext.tsx:141-173`
  (the whole `useEffect`), `:144-149`, `:168-172`; `apps/hiring/…/UserStatusContext.tsx:125` and `:144-145`
  (the symmetric `targetProduct:'workforce'` guard); `useNavbarSections.tsx:459-466` with the
  `isHiringOrg ? results : activityDashboard` label at `:460`; `template.tsx:90`
  `isEnterprise = Boolean(organization)`; `FreeTrialContainer.tsx:29`
  `isEnterprise = Boolean(!isHiringOrg && organizationId)`; `packages/graphql/src/query/insights.ts:31-82`
  is exactly the `insightsJobSimulationByMemberships` document;
  `InsightsByMembersContainer.tsx:359` in **apps/hiring** is `<Drawer`. The two
  `simulationScoreColumn.tsx` copies are **byte-identical** (`diff` empty, 168 lines each), as claimed.
  Zero Next.js intercepting-route directories exist anywhere under `apps/` (`find … | grep -cE
  '/\(\.{1,2}\)'` = **0**), confirming the M231 correction.
- **ENUMERATED** — `hiring.md:78-88`: `resolver_cms_queries.go` calls `GetOrganizationIsHiring` at exactly
  `:95, :210, :258, :295`, with the `hiringLibraryTypes` / `workforceLibraryTypes` branch at `:99-103`,
  inside `PrivateJobSimulations` (declared `:89`).
- **ENUMERATED** — `hiring.md:397-406`, the rext side @ `6130bfd8`: `hiring_config.go:99` is the
  `CopyRowsIdempotent(ctx, "public", "organization_sim_invitation_links", …)` call; `hiring_scope.go:45-46`
  defines `skipGenericActivityForHiringOrg` → `st.IsHiringOrg()`; **seven** seeders consult it
  (activity, assignments, feedback, hero_activity, jobsim_sessions, persona, succession) and
  `TestGenericActivitySeeders_SkipHiringOrg` enumerates **all 8** rows — jobsim-sessions, activity,
  skillpath-sessions, assignments, hero-activity, persona, feedback, succession. `blueprint.go:453-458` is
  `RoleMix{Admin, Member, Candidate, AdminEmails}`. `render-hiring-comparison.spec.ts` exists.
- **ENUMERATED** — `hiring.md:176-181`: `askengine/registry.go:192` is inside the alias comment stating
  `jobsimulation` stays in `rewriteSchemas` until M710, with `{Name: "jobsimulation.sessions", Schema:
  "public", RealName: "job_simulation_sessions"}` immediately below — exactly the *"names resolve to the
  public tables"* reading the corpus insists on.
- **ENUMERATED** — `hiring.md:35`, `:179`, `:328`: `repos.yml` carried **6** entries at `0dab54d` (app,
  sentinel, storage, messenger, next-web-app, studio-desk) and **4** since `838d907`; `jobsimulation` is in
  neither.

### Storage (both sides re-derived, at the refs the cell names)

- **ENUMERATED** — `storage.md:25`, prod side @ storage `9f8cb53`: `terraform/main.tf` is **18** lines, has
  **no** module/service block, and its comment reads verbatim as quoted; `:18` is *"See outputs.tf —
  consumers should reference these by output, never by literal name."*; `custody` = **0** occurrences
  repo-wide (positive control in the same pass); **no** `moved {` block in any `.tf`;
  `terraform/storage.tf:22-25` is the M903-superseded note, quoted correctly. At the checked-out
  `4ce8ece5`, `main.tf` is **100** lines and still declares `module "storage"`, and
  `terraform/storage.tf:23-24` still says *"relocate the assets out of this module BEFORE M907"*.
  `d3e6d32` (2026-08-05) contains the literal sentence **"M903 never ran."**
- **ENUMERATED** — `storage.md:29`, the load-bearing ref-sensitive grep: at `app` `2035f9a`,
  `git grep -n STORAGE_RPC_ADDR -- '*.go'` returns exactly **3** hits and all three are comments, at exactly
  the three paths named (`main.go:504`, `internal/jobsimwiring/wiring.go:101`,
  `internal/storagens/callsites_test.go:189`). Ref-less on the checkout it returns **15** hits, of which
  **7** are live `os.Getenv`/`getenv` reads (`cmd/academy-asset-upload/main.go:129`,
  `cmd/academyImport/main.go:231`, `cmd/import/main.go:50`, `jobsimwiring/wiring.go:115`, `main.go:446`,
  `:524`, `:992`). Both numbers reproduce, and the sentence's warning about the ref is correct.
- **ENUMERATED** — `storage.md:29`, `:58`, at `app` `2035f9a`: `main.go:516`/`:517` read the bucket env
  vars, `:524`/`:525` are `internalstorage.NewManager` / `NewPublicManager`, `:547` is
  `resource.NewManager(ent, internalstorage.NewClient(storageManager, serviceName).V1)`, `:1102` is
  `cmsStorage := internalstorage.NewClient(storageManager, storagens.CMS)`;
  `internal/storage/service.go:22`/`:24` are the two `Env…` **name** constants;
  `internal/storage/storage.go:193-200` is `getKeyPath` branching on `s3Bucket != ""`.
- **ENUMERATED** — `storage.md:7-16` at the ref that block names, `app` `9d00a313`:
  `internal/jobsimulation/recording/recording.go:12` and `internal/jobsimulation/anticheat/anticheat.go:30`
  are both `storagev1 "github.com/anthropos-work/storage/sdk/storage/v1"`, and `main.go:1048` is the
  `cmsStorage` line. The parenthetical's own defensive claim checks out: `recording/` contains only
  `chime.go` and `recording.go` — there is indeed no `anticheat.go` beside it.
- **ENUMERATED** — `storage.md:161`, `service_taxonomy.md:130-132`, the three prod-terraform fates, each
  graded at the ref its cell names: `jobsimulation` — `6092c6d2` (*"remove the jobsimulation ECS service and
  ECR repository (M810)"*) takes `terraform/main.tf` from 343 → **56** lines and deletes the
  `module "jobsimulation"` block, with `:15-40` at that ref being exactly the LiveKit/Chime/SSM/atlas
  survival comment (the commit is **not** an ancestor of the checkout `462343b0`, only of `origin/main` —
  the cell is right to name it); `cms` — `cms/terraform/main.tf` is **191** lines with `module "cms"` at
  `:30` and `service_desired_count = 0` at `:39`; `roadrunner` — `terraform/main.tf:19` still reads
  `service_desired_count = 1`. `service_taxonomy.md:130`'s *"report both, assert neither"* on the archive
  claim is also exact: `jobsimulation` origin/main carries **four** commits dated 2026-08-04, including
  merged PR **#439**.
- **ENUMERATED** — `storage.md:226`: the `storage` repo contains **0** `*_test.go` files
  (`git ls-files | grep -c _test.go`), and `Dockerfile:18` is `RUN go test -v ./...`.

### Clerk

- **ENUMERATED** — `clerk-integration.md:51`, `:91`: `colony/authn` is imported by **129** files in `app`
  and **0** in `sentinel`, `storage`, `messenger` and `roadrunner` (cms 6 / jobsimulation 8 — the frozen
  repos, correctly excluded by *"among the live Go services"*). `clerk-sdk-go/v2` appears in `app`'s
  `go.mod` only; in `cms`/`jobsimulation` it is `// indirect`, so *"none direct"* holds.
  `service_taxonomy.md:479`'s companion claim re-derives too: `authn` in **0** of 7 Go clones' `go.mod`,
  `colony` in **7** of 7.
- **ENUMERATED** — `clerk-integration.md:37`: the webhook switch in
  `app/internal/clerk/events/events.go` has exactly **12** `case "…"` arms (user ×3, organization ×3,
  organizationInvitation ×3, organizationMembership ×3).
- **ENUMERATED** — `clerk-integration.md:41`: `apps/web/src/providers/Clerk.provider.tsx:10-17` imports
  exactly **8** locales (deDE, enUS, esES, frFR, itIT, jaJP, nlNL, ptPT).
- **ENUMERATED** — `clerk-integration.md:103`: all four `@clerk/nextjs` pins are `^6.39.2`
  (`apps/{web,hiring,integration}/package.json` + `ant-academy/code/package.json:52`);
  `next-web-app/apps/mobile/package.json:6` = `~2.6.18` and `ant-academy/mobile/package.json:18` =
  `~2.19.36` (13 minor versions apart); `app/go.mod:31` @ `5ba17044` = `clerk-sdk-go/v2 v2.7.0` (and the
  same at the checkout).
- **ENUMERATED** — `clerk-integration.md:21`, `:46`, `:99`, `:100`:
  `apps/web/src/app/api/metabase/route.ts:35` is `authData.orgRole !== 'admin'`;
  `ant-academy/mobile/src/components/ClerkSignInForm.tsx:49-53` errors out on any status other than
  `'complete'`; `studio-desk/src/index.ts:96` `STUDIO_ACCESS_ROLES = ['admin','org:admin','content_creator',
  'org:content_creator']`; `ant-academy/code/proxy.js` uses `clerkMiddleware` and gates on
  `REQUIRE_ORGANIZATION_MEMBERSHIP` → `/no-organization`.

### Frontend / Studio / Academy

- **ENUMERATED** — `frontend_architecture.md:39`: I re-derived the **set**, not the sum. `git grep -c
  NEXT_PUBLIC_BACKEND_API_URL bb3313bc`, excluding `.env.example`, `Dockerfile.dev`, `turbo.json`,
  `terraform/`, `knowledge/*.md`, `*.md`, `e2e/` and `*.test.ts`, yields **21** files summing to **29**
  occurrences — and the doc's sub-claim *"the four `packages/core-js` REST clients … 12 sites between
  them"* re-derives as 4 × 3 = 12. At `ad767d1b` the same derivation yields **22 / 31**, exactly as the
  parenthetical states. `docker-compose.yml:161` (runtime env) and `:152` (build arg) both verify.
- **ENUMERATED** — `frontend_architecture.md:24`, `:35`, `:48-51`, and the port table: `!apps/mobile` is in
  `pnpm-workspace.yaml`; `configs/i18n/messages/` holds exactly the 8 locale dirs
  (de, en, es, fr, it, ja, nl, pt); `apps/web/package.json:46` = `"next": "^16.2.7"`;
  root `package.json` = `"node": ">=24.0.0"` and `"packageManager": "pnpm@10.30.3"`; dev ports 3000 / 3001 /
  3002 in `apps/{web,hiring,integration}/package.json`.
- **ENUMERATED** — `service_taxonomy.md:209`, the Studio-Room provider claim, grepped at the **nested
  repo's own ref** `aeec036a` per the ADDENDUM: `git -C app/studio grep -i mistral aeec036a` = **22 hits
  across 3 files** (`requirements.txt`, `tools/pdf2md.py`, `tools/r3.py`); `services/ai.py:705-708` is the
  whole `providers = {openai, azure, anthropic}` registry; `ai.py:1-2` imports only `openai`/`anthropic`;
  `tools/pdf2md.py:24` is the sole `from mistralai import Mistral` (r3.py only reads the env var name). The
  "OCR-only" reading is correct.
- **ENUMERATED** — `service_taxonomy.md:233-239`: `gen.py` registers exactly **nine** `add_argument` calls
  (`:484-492`), matching the listed nine, and `:19` uses `parse_known_args`. The example command at `:228`
  is verbatim from `studio/CLAUDE.md:12-14`, and its `--evaluation_skills` is a genuine (leftover-absorbed)
  parameter consumed at `agents/simulation/prep.py:75` — so the callout's mechanism is right.
- **ENUMERATED** — `service_taxonomy.md:47-51`, `:183-190`, `:214`:
  `studio-desk/.env.example:45` bakes `VITE_GRAPHQL_ENDPOINT=http://localhost:8082/graphql/query`;
  `cms_queries.graphqls:106` is `studioTask`; `app/internal/cms/studio/studioManager.go:119` execs
  `studio/gen.py`; studio-desk has **0** react/vue/angular deps and **0** tracked `.tsx`/`.jsx` files;
  `app/.github/workflows/build-production.yml:29-30` carries
  `additional_repo: "anthropos-studio-room:studio"`.
- **ENUMERATED** — `service_taxonomy.md:257-263`: `ant-academy/code/src/graphql/server.js:14`/`:18` (throws
  when unset), `src/lib/backendContent.js:36`/`:102-103`, `src/lib/serverTenant.js:115-145` (with the
  quoted *"NO FS-as-published fallback … not reversible-on-error"* inside it);
  **0** `serwist`/`workbox` deps in `code/package.json` (positive control `"next": "^16.2.6"` hit),
  `RegisterServiceWorker.jsx` is a kill-switch that unregisters, `tests/unit/next-scaffold.test.js:106`
  and `:111` and `react-compiler-config.test.js:41` are the regression fences,
  `code/app/layout.jsx:132` is `manifest: "/academy-manifest.json"` and that file declares
  `"display": "standalone"`; the Cosmo assistant is gated behind `NEXT_PUBLIC_FEATURE_TRAINING_COACH`
  (default OFF, `src/lib/featureFlags.js:8`) and calls `https://api.openai.com/v1/responses` with
  `MODEL = 'gpt-5.2'` and a per-user `localStorage.getItem('openai_api_key')`.
- **ENUMERATED** — `service_taxonomy.md:174` and `:185`, the two intra-corpus twins that **do** resolve:
  `studio-desk.md:21` is the compose-profile row and `studio-desk.md:20` is
  *"TypeScript, Vite, Express.js (vanilla TS frontend, no framework)"*. `service_taxonomy.md:367`'s
  self-citation `:67-68` also resolves correctly.
- **RE-DERIVED** — `clerk-integration.md:103`'s *"thirteen minor versions apart"* (2.6 → 2.19).
- **RE-DERIVED** — `graphql-wundergraph.md:132`'s *"final five months"* (2026-02-27 → 2026-07-31).

---

## What I could not settle

- **GitHub archive states.** `service_taxonomy.md:3`/`:128`/`:129`/`:366` and
  `graphql-wundergraph.md:8` assert archive dates for `graphql-wundergraph` (2026-07-30), `skiller`
  (2026-07-01) and `skillpath` (2026-07-31). Archive state lives in the GitHub org API, not in a clone.
  The only clone-visible consistency check I could run — is there a commit *after* the claimed archive
  date? — comes back clean for `graphql-wundergraph` (last commit `60c229f`, 2026-07-30 10:23, same day),
  and `skiller`/`skillpath` are not in any clone set. *`service_taxonomy.md:130` is the model here: it
  already refuses to assert jobsimulation's archive state for exactly this reason.* Settled by
  `gh api repos/anthropos-work/<repo> --jq .archived`.
- **The captured-snapshot figures.** `hiring.md:154` (*"87 real `SIMULATION_TYPE_HIRING` sims"*), `:158`
  (*"0 `job_position` rows"*, *"the prod 443"*) and `service_taxonomy.md:145` /
  `:377` taxonomy counts (≥42,790 skills / ≥22,470 job roles) describe a captured snapshot / a prod DB,
  neither of which is in the clone set. Settled by `stacksnap status` against the manifest cache, or a
  read-only `/db-query`.
- **`hiring.md:344`** — *"the org's `admin` members inherit `org:feature:insights` from the global `p3`
  admin policy via their standard g2 grant."* Requires reading Sentinel's seeded casbin rows on a live
  stack; `sentinel` @ `88bc5592` carries the engine, not the policy data.
- **`storage.md:25`'s `app/knowledge/plan/releases/09.00-support-in-app/m903-s3-custody/` citation.**
  HARD BAR 1 forbids reading anything under `knowledge/plan/**`; I did not open, list or grep it, so the
  `status: planned` / *"Planned, not started"* quotations are unverified by me. Everything else in that
  cell (both terraform sides, the `custody` zero, the absent `moved` block, `d3e6d32`) verified.
- **`service_taxonomy.md:131`'s cms prod-teardown state.** The cell itself says the deletion lands in
  `infrastructure`, which has never been in a clone set — I confirmed the two clone-visible halves
  (`cms/terraform/main.tf:39` `= 0` in an intact 191-line module; `6efa1d5`'s workflow deletion) and can
  confirm the cell's *"report both, assert neither"* framing is the honest one, but not the underlying
  prod fact.
