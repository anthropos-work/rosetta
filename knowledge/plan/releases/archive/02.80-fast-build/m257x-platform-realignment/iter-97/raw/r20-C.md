# Seat C — reading #20

**Corpus under audit:** `rosetta` @ `00be1ac`, tree clean.
**Ground truth re-verified at this reading's open** (matches the ADDENDUM sheet exactly):
`platform` `0c91421` (= origin/main) · `app` `b948604f` (origin/main `2035f9a4`) · `app/studio` + `cms/studio`
`aeec036a` (nested, own ref) · `next-web-app` `bb3313bc` · `messenger` `fa47850` (origin/main `e9421c6`) ·
`studio-desk` `14a5442` · `sentinel` `88bc559` · `storage` `4ce8ece` · `cms` `ca50c81` ·
`jobsimulation` `462343b0` · `roadrunner` `87d8d44` · `graphql-wundergraph` `60c229f` ·
`rosetta-extensions` **authoring copy** `6130bfd` (the `stack-demo/` copy is `ab81527`, an older clone —
per ADDENDUM rule 2 every rext citation below is graded at `6130bfd`).

**Search-set enumeration (ADDENDUM absence rules).** `find stack-demo -name .git -maxdepth 4` returns 15
trees, including the two nested `anthropos-studio-room` copies (`stack-demo/app/studio`,
`stack-demo/cms/studio`). `.agentspace/rosetta-extensions` contains **no** nested repo (`find … -name .git
-maxdepth 3` → one hit). Every zero I report below names the tree and the ref it was taken at, and was
taken with a positive control in the same pass.

## Positive controls

| file | `wc -l` | read in full? |
|---|---|---|
| `corpus/architecture/alignment_testing.md` | 542 | yes |
| `corpus/services/clerkenstein.md` | 370 | yes |
| `corpus/architecture/ai_architecture.md` | 305 | yes |
| `corpus/services/messenger.md` | 189 | yes |
| `corpus/services/coursebuilder.md` | 140 | yes |
| `corpus/services/gotenberg.md` | 84 | yes |
| `corpus/services/db-backup.md` | 31 | yes |
| **total** | **1,661** | |

---

## Blockers

### B1 — messenger's `backend` subscriber has 21 handlers, not 20, and the cited window stops 9 lines short

- **Anchor:** `corpus/services/messenger.md:17` — *"(`messenger/internal/flow/flow.go:**70-95**` adds a
  subscriber on the `backend` stream with **20 handlers**; `app` runs that same subscriber now, on
  messenger's own consumer group)"*
- **Why it is false:** the claim names no ref, so it grades at the `messenger` checkout `fa47850` — and it
  is identically wrong at `origin/main` `e9421c6`, so no pin rescues it.
  `messenger/internal/flow/flow.go:72` opens
  `h.subServer.AddSubscriber("backend", sub.AddHandler(` and the call **closes at `:104`**, not `:95`.
  The live (compiling) `pubsub.EventHandler(...)` entries inside it are **21**:
  `:74 :75 :76 :77 :78` (skill-path) · `:80 :81 :82` + `:84` (job-sim) · `:86` · `:89` (content-agnostic) ·
  `:91 :92` (organization) · `:94` · `:96 :97 :98 :99` (AI-readiness) · `:101 :102 :103` (course-builder).
  A 22nd, `OrgJobSimulationAssignmentPastDueHandler`, is **commented out at `:83`** — so a naive
  `grep -c` yields 22 and the compiling set is 21. **Neither number is 20.**
  The cited window `:70-95` contains only **14** of the 21 (`:74`–`:94`) and excludes the whole
  AI-readiness and course-builder blocks; the two subscribers that follow
  (`AddSubscriber("jobsimulation", …)` `:105-108`, `AddSubscriber("cms", …)` `:109-111`) fall outside it too.
  This is not stale-by-drift: over the last **8** revisions of the file the `backend` count and block extent
  were `21/72-104` (`88d84d8`), `14/72-95` (`13c8daa`), `19/72-99` (`66e5f10`), `15/72-94` (`468534b`),
  `13/72-92` (`aa9f28d`), `16/72-95` (`d59ce50`), `12/72-90` (`9a5e103`), `11/72-88` (`3cc90a5`) —
  **20 was never the count at any of them, at any block extent.**
  This is §5 rule 24's shape exactly: the surrounding predicate (*"a subscriber on the `backend` stream"*)
  is true and checkable; the **cardinality of the set it ranges over** is what is wrong.
- **Confidence:** high

### B2 — `coursebuilder.md` asserts the CURRENT `app` version is `v1.363.2`; it is `v1.366.0`, and the same file measures at `v1.366.0` three times

- **Anchor:** `corpus/services/coursebuilder.md:130-131` — *"**Recently added + heavily iterated** —
  developed through numbered Waves 1→24 (142 changelog lines); **current app `v1.363.2` @ `5ba17044`**
  (2026-07-31)."*
- **Why it is false:** `git -C stack-demo/app tag --points-at 5ba17044` → `v1.363.2`, so the *pin* is
  internally accurate — but the sentence **asserts currency**, and §5 rule 33 part 3 is explicit that a
  block asserting currency (*current*, *not stale*, *today*) cannot be pinned into silence, "because the pin
  and the word are making opposite claims and only one of them is what the reader acts on."
  Measured: `git tag --points-at b948604f` → **`v1.366.0`** (the audited checkout), and
  `git tag --sort=-creatordate | head -3` → **`v1.369.0`, `v1.368.0`, `v1.367.0`**. `app` is 93 commits
  past the checkout.
  It also **contradicts its own file three times**: `:48`, `:71` and `:99` each measure
  *"@ `app` `b948604` v1.366.0"*. And it contradicts my other assigned file,
  `corpus/services/messenger.md:37` (*"Re-derived at `app` `9d00a313` v1.367.0"*).
  This is the cheapest finding class in §5 rule 21 — a corpus contradicting itself, detectable with no
  ground-truth read at all — and it sits **83 lines below** one of the contradicting anchors.
- **Why I hesitated:** the substantive Course Builder claims all verify at the checkout (Waves 1→24 in
  `internal/coursebuilder/SPEC.md:383,398,432,462,486`; the parallel-author deletion; every `file:line`), so
  the falsehood is a version-metadata line rather than a mechanism described backwards. I book it because
  rule 33 was written for exactly this shape, and because the same false scalar has propagated to two
  further docs outside my scope (`ai-labs.md:18`, `academy-backend.md:20` — the latter contradicting
  *itself* at `:55`, which cites `9d00a313 = v1.367.0`). Under-booking it would leave a
  four-site self-contradiction unnamed.
- **Confidence:** high (on the falsity) / medium (on materiality)

### B3 — the CMS `ai_model` enum is cited as `:983-990`; the enum is `:979-991` and the window hides 3 of its 11 members

- **Anchor:** `corpus/architecture/ai_architecture.md:235` — *"Selected model from the CMS `ai_model` /
  `ai_vendor` fields per sequence (e.g. `gpt-5`, `gpt-4.1`, `anthropic-45-sonnet-aws` — **the enum is
  `app/internal/cms/directus/collections/jobsimulation.go:983-990`**)"*
- **Why it is false:** at the checkout `b948604f`, `internal/cms/directus/collections/jobsimulation.go`
  declares `type AIModel string` at `:977` and the const block runs `:979` (`const (`) → `:991` (`)`), with
  **11 members at `:980`–`:990`**. The cited `:983-990` opens *inside* the block at
  `Anthropic45SonnetAws` and silently excludes `Anthropic35SonnetAws` (`:980`),
  `Anthropic37SonnetAws` (`:981`) and `Anthropic4SonnetAws` (`:982`). A reader who opens the cited window
  to enumerate the legal `ai_model` values reads **8**, and the three missing ones are precisely the
  Bedrock-routed models whose fallbacks the same document discusses at `:259` (`anthropic-aws` → Claude 3.7
  Sonnet). This is not drift: the only two revisions of this file in the recent log
  (`1bfdcb42f`, `d8f07a83f`) both place the block at `977..991` with 11 members, so `:983-990` was not
  correct at an earlier ref either.
  The neighbouring `ai_vendor` citation in the same file (`:64`, `jobsimulation.go:971` = `Azureglobal`) is
  exact, and the 5-member `AIVendor` enum claim at `:63` re-derives correctly (`:969-975`), which is why
  this one reads as verified by adjacency.
- **Why I hesitated:** the anchor points at the *right construct*, merely truncated, and the three examples
  the sentence gives (`gpt-5` `:987`, `gpt-4.1` `:985`, `anthropic-45-sonnet-aws` `:983`) all fall inside
  the cited window — so it is closer to §5 rule 25's explicitly-MINOR "line drift" class than to "the wrong
  construct." I book it upward per the briefing's tie-break rule, and flag it as the lowest-severity of my
  three. **Downgrade to MINOR is defensible.**
- **Confidence:** low

---

## Minors

- `corpus/architecture/alignment_testing.md:3` — *"Last updated: 2026-06-06"* on a file whose body documents
  M218/M219 (v2.3) and carries an *"Corrected M257x iter-85"* note at `:378`. Stale metadata field.
- `corpus/architecture/alignment_testing.md:532-540` (Layout block) — lists `internal/{dna,outcome,compare,report}`;
  the harness also ships **`internal/canon`** (`alignment/internal/canon/canon.go`), which is the package the
  doc's own *"canonical and precision-preserving"* claim at `:125` and `alignctl dna`'s import
  (`cmd/alignctl/dna.go:10`) depend on. Omitted list member.
- `corpus/architecture/alignment_testing.md:333` — the link ``[`clerkenstein/knowledge/alignment.md`](../services/clerkenstein.md)``
  labels a rext KB file but hrefs the corpus page, and the immediately-following
  *"(pointed to from [Clerkenstein](../services/clerkenstein.md))"* repeats the **identical** href. Circular
  and non-navigable to the file it names (`clerkenstein/knowledge/alignment.md` does exist in the authoring copy).
- `corpus/services/clerkenstein.md:3` — *"Last updated: 2026-07-14"* on a file documenting v2.8 M256
  (`:160`, `:184-209`) and M257x iter-23 (`:274`).
- `corpus/services/clerkenstein.md:21` — the monorepo section list
  *"(`clerkenstein`, `demo-stack`, `stack-injection`, `stack-core`, `stack-seeding`, `alignment`)"* omits
  `dev-stack`, `playthroughs`, `stack-secrets`, `stack-snapshot`, `stack-verify` (the authoring copy holds
  12 section dirs). Root `CLAUDE.md` lists nine.
- `corpus/services/clerkenstein.md:105` — the `cmd/` row enumerates `mintpk`, `fake-fapi`, `fake-bapi`;
  `clerkenstein/cmd/` also holds **`jwtkey`**.
- `corpus/architecture/ai_architecture.md:35` — *"`studio/tools/pdf2md.py:24` (`mistral-ocr-latest`)"*:
  `:24` at `aeec036a` is `from mistralai import Mistral`; the `mistral-ocr-latest` literal is at **`:127`**.
  (Its twin, `external_services.md:565`, states the line's actual content explicitly, so the twin is clearer.)
- `corpus/services/coursebuilder.md:114-115` — *"~55 test files"* / *"~25 boundary test files"*; measured at
  `b948604f`, `internal/coursebuilder/**` holds **61** `_test.go` files and
  `internal/web/backend/coursebuilder/**` holds **33**. Hedged with `~`, so non-material, but the boundary
  figure is 32 % low.
- `corpus/services/messenger.md:159-171` — the *"Value in the deleted compose block"* table omits
  **`SERVICE_NAME=messenger`** (`docker-compose.yml:182` @ `0dab54d`) and `ENVIRONMENT=development` (`:175`).
  `SERVICE_NAME` is load-bearing: the app-side row at `:43` turns on the fact that nothing set it *in
  terraform*, and a reader of this table cannot see that compose did.
- `corpus/services/messenger.md:163` — `BREVO_KEY | (empty)` under *"Value in the deleted compose block"*;
  the block never set `BREVO_KEY` at all (`docker-compose.yml:156-191` @ `0dab54d` — it arrived via
  `env_file: .env`). "(empty)" reads as an explicit compose value.
- `corpus/services/messenger.md:108` — *"`OrgSkillPath*` handlers in `internal/flow/flow.go:72-87`"*: those
  five handlers are at `:74-78`; `:72-87` also spans the job-simulation and content-agnostic handlers.
  Range imprecision only — the named handlers are inside it.

---

## Positively cleared (audited zeros)

Labelled per the briefing. **ENUMERATED** = I derived the predicate's set from source independently and
compared cardinalities. **RE-DERIVED** = I only re-computed arithmetic the document itself displays.

### `alignment_testing.md`

- **ENUMERATED** — the five Clerkenstein DNAs and their gene/capability counts. I parsed each
  `clerkenstein/alignment/dna/*.json` with `json.load` and counted `len(capabilities)` and
  `sum(len(variants))` rather than trusting the table: `clerk-2.6.0` **14 caps / 27 genes**;
  `clerk-js-5` **6 / 9**; `clerk-multi-1` **5 / 9**; `clerk-deploy-1` **3 / 7**; `clerk-express-1`
  **5 / 13**. Every figure at `:41-45` of `clerkenstein.md`, `:261-265` and `:359`/`:383` of
  `alignment_testing.md` matches.
- **ENUMERATED** — *"Only `clerk-2.6.0` … declares a `consumed_surface` today"* (`:208-211`). Derived per
  file: `clerk-2.6.0` declares **15** entries; the other four have **no such key**. The four-of-five
  "warned but not enforced" statement is exactly right.
- **ENUMERATED** — the `--if-declared` semantics at `:199-200`, which is the most over-claimable paragraph
  in the file. Read `alignment/cmd/alignctl/dna.go:41-104` in full: `:72-81` downgrades **only** the
  "declares no `consumed_surface`" case to `return 0` under the flag; `:95-100` returns **2** for a declared
  hole under **any** flag; `:65-70` fails structural errors first. `gate.sh:69` does call it with
  `--if-declared`, and the rationale comment is `:58-67` (`:68` is the `echo`; the doc says `:58-68`).
  `:61` is indeed inside the comment, as the doc's own parenthetical says.
- **ENUMERATED** — the exit-code split. `alignment/cmd/alignctl/run.go:133-136` declares
  `ExitRegressed = 2` / `ExitUnmeasurable = 3`; `unmeasurable()` is `:139-154` and returns
  `ExitUnmeasurable` at `:153` after printing the boxed banner whose text the doc quotes verbatim.
  `gate.sh:82-88` reports the three verdicts separately. Both `alignment_testing.md:373-378` and
  `clerkenstein.md:45-47` (`run.go:134-135`) resolve.
- **ENUMERATED** — the zero-critical-genes guard (`:169-174`). `internal/dna/dna.go:279-281` rejects a DNA
  with capabilities and 0 critical **genes** (counting variants, not capabilities — `:249`), and
  `internal/compare/compare.go:56-59` `GateMet` refuses a non-zero critical threshold at
  `CriticalGenes == 0`. `validate_test.go:100-118` pins the variants-not-capabilities subtlety.
- **ENUMERATED** — the id regex `^[A-Za-z0-9][A-Za-z0-9_-]*$` is `internal/dna/dna.go:21` verbatim; weights
  3/2/1 are `Criticality.Weight()` `:37-49`; `critical %` is an **unweighted** count ratio
  (`compare.go:102-103` `pct(critAligned, critTotal)`) while `Overall` is weighted (`:101`), exactly as
  `:160-164` claims.
- **ENUMERATED** — the six snapshot-fidelity operators. `stack-seeding/dna/snapshot.go:62`
  (`var validSnapshotOperators`) holds exactly six keys, `snapshot-cross-surface-closure` among them.
  `data-dna.json:410` (taxonomy) lists **five** — all but `cross-surface-closure`; `:439` (content) lists
  **five** — no `embedding-dim`, with `cross-surface-closure` in its place. Both anchors land on the
  `"operators"` key of the right surface.
- **ENUMERATED** — *"NOTHING is left waived → 100% coverage"* (`:505`). I enumerated `data-dna.json`'s
  `surfaces[]`: **10 surfaces**, statuses `seeded-m7a`×4, `seeded-m7c`×4, `snapshot-seeded-m9b`×1,
  `snapshot-seeded-m10`×1 — **zero waived**. `dna.go:316-328` `Coverage()` skips waived and counts
  `IsSeeded() || IsSnapshotSeeded()`, as described. The directus public predicate quoted at `:501-502`
  is `contentsession/sourcing.go:46` and `dna/fidelity_probe.go:117` verbatim.
- **ENUMERATED** — the toy example (`:293-316`). `examples/toy/dna.json` has `Add` critical (3 genes,
  w3) and `Greet` standard (3 genes, w2); aligned = 9 + 4 of 15 = **86.7 %**, critical **3/3 = 100 %**,
  **5/6 genes** — and `examples/toy/alignment_test.go:21-22` sets the gate at 80/100, so the tolerated
  divergence really is non-fatal. (I re-derived the arithmetic *and* enumerated the gene set that feeds it.)
- **ENUMERATED** — the inert CI workflow (`:335-351`), the single hardest claim in the file to state
  correctly and the one the doc says took two attempts. `clerkenstein/.github/workflows/alignment.yml`
  **exists and is git-tracked** (`git ls-files` confirms); `.agentspace/rosetta-extensions/.github` does
  **not exist**; and the file's own `:10-11` reads *"as a subdir workflow under clerkenstein/.github (not at
  the monorepo root), this is currently inert…"* — matching the corpus quote word for word.
- **ENUMERATED** — the harness/mirror split (`:328-332`, `:513-518`). `alignment/` holds exactly
  `README.md cmd examples go.mod internal` — **no `scripts/`**; `clerkenstein/alignment/scripts/` holds
  `gate.sh drift-check.sh drift-test.sh`; `gate.sh:31-36` defaults are `RUNNER_PKG=./cmd/clerkrun`,
  `DNA=dna/clerk-2.6.0.json`, `ALIGN_DIR=$base/../../alignment`, `GATE_OVERALL/CRITICAL=95/100`. The five
  runners `clerkrun jsfapirun expressrun deployrun multirun` and five goldens
  `golden golden-js golden-multi golden-express golden-deploy` are all present.
- **ENUMERATED** — `alignctl dna` subcommands. `dna.go:20-26` dispatches `list`/`diff`/`validate`/`coverage`
  on exactly those lines. (`:16`'s usage string still says `list|diff|validate` — a platform-side nit, not
  a corpus one.) `run.go:38` calls `d.Validate()` before scoring, as `:198` claims.

### `clerkenstein.md`

- **ENUMERATED** — the M219 org-eid fix. `clerk-backend/store.go:138` is `func (s *Store) SeedOrgIdentity`
  and `:151` is `func (s *Store) LookupOrgEid`; `alignment/dna/clerk-2.6.0.json:131` contains the quoted
  *"M219 landed the fix … taking the Go surface 97.2% -> 100%"* string, and the
  `MembershipOrgIdentity/real-org-eid` gene is still present at `:130-137` — so the "gene stays as a
  permanent fence" claim holds.
- **ENUMERATED** — the single-tenant seat model (`:160-182`), which is the file's strongest claim.
  `clerk-frontend/registry.go:24` holds one `activeKey`, with `active()` `:67` and `Select()` `:75`;
  `server.go:102-109` declares one `signedIn`, one `signedOut`, one `clientID`, one `sessID`;
  `:125` fixes `clientID: "client_clerkenstein"`; `establishLocked()` `:663-670` sets
  `sessID = "sess_clerkenstein"`. **`.Cookie(` returns 0 across the whole `clerkenstein/` tree** at
  `6130bfd` (`git grep -n "\.Cookie(" HEAD -- clerkenstein/` → 0), with a positive control in the same pass
  (`git grep -c "Cookie"` → 5 files, incl. `server.go` ×2, both `Set-Cookie` *emissions* at `:348` and
  `:394`). The authoring copy has no nested sub-repo, so this zero covers the whole tree.
  `handleSelectIdentity` `:627-649` does set `signedIn=false; sessID=""` at `:646-647` and does **not** set
  `signedOut` — it clears it at `:648-649`, exactly as `:196-198` says.
- **ENUMERATED** — all five sticky-sign-out tests named at `:206-209` exist in
  `clerk-frontend/server_test.go` (`:256`, `:286`, `:390`, `:427`, `:461`).
- **ENUMERATED** — the BAPI≠FAPI table (`:119-122`).
  `clerk-backend/server.go:47` registers `GET /v1/users/{userID}/organization_memberships`;
  `clerk-frontend/server.go:186` registers `GET /v1/me/organization_memberships` →
  `handleMeOrganizationMemberships` (`:512`); `clerk-frontend/meorgmemberships_test.go` exercises the
  paginated envelope, `limit`/`offset` and the offset-past-end empty array. The "not yet a measured gene"
  disclosure at `:145-147` is correct: `clerk-js-5.json` has a `Me` capability with variants
  `universal-user`/`unauthenticated` only — **no gene names this route**.
- **ENUMERATED** — the studio-desk role acceptance (`:141-143`): `src/index.ts:96` **and**
  `app/services/userService.ts:16` at `studio-desk` `14a5442` each read
  `['admin','org:admin','content_creator','org:content_creator']`, each under the comment *"Both the
  prefixed (org:*) and bare role keys are accepted"* — both anchors exact.
- **ENUMERATED** — the colony pin drift box (`:104`, `:271-279`). `clerkenstein/go.mod:8` pins
  `colony v0.34.3`; `app/go.mod:16` reads `v0.35.2` (identical at `b948604f` **and** at `origin/main`);
  `sentinel/go.mod:8` and `storage/go.mod:7` are both `v0.34.3` — so *"the pin is not wrong for the whole
  platform"* is exactly right. `app/go.mod:31` is `clerk-sdk-go/v2 v2.7.0`, not `v2.6.0`, as `:279` says.
  (`messenger/go.mod:7` is also `v0.35.2`; the doc does not claim otherwise.)
- **ENUMERATED** — the M220 clerk-js cache fix (`:305-313`). `clerk-frontend/server.go:35-68` is the
  block: `clerkJSFetchTimeout = 15 * time.Second` `:59`, `clerkJSMaxBytes` `:62`,
  `clerkJSClient = &http.Client{Timeout: clerkJSFetchTimeout}` `:68` under the comment *"Explicitly NOT
  http.DefaultClient"* `:65`, and `FAKE_FAPI_CLERKJS_CACHE` at `:70`. The 0.2 s / ~127 s figures the corpus
  quotes are in the source comment `:40-42`.
- **ENUMERATED** — `dotless-pk-rejected` is a real variant of `ExpressRequest` in
  `clerk-express-1.json:24`; `MintPublishableKey` (`clerk-frontend/key.go:28-37`) rejects only an empty
  host and a `$`, i.e. it **is** permissive as `:301-302` claims; the dotted pre-check + hard refusal lives
  in `demo-stack/tailscale_autohost.py:25-26,188` with a RED-watched test
  (`tests/test_tailscale_autohost.py:155`).
- **ENUMERATED** — the multi-identity control plane (`:156`): `clerk-frontend/server.go:202-203` registers
  `GET /v1/demo/identities` and `POST /v1/demo/select`, documented in the comment block `:198-201`.

### `ai_architecture.md`

- **ENUMERATED** — the whole *"no EU-first ladder"* retraction (`:15-43`), re-derived from source rather
  than from the document's own summary. `internal/jobsimulation/ai/ai.go:29-34` declares exactly **four**
  vendor consts (`azure`, `openai`, `anthropic-aws`, `anthropic`); `getClient` is `:259-289`, a plain
  switch with **no ordering and no probing**; `:264-277` defaults to `azureClientEu` and swaps to
  `azureClientUs` only on `flag_use_azure_us`, and a **failed** lookup returns the EU client at `:272`;
  `:280-283` maps `AnthropicAws` *and* `Anthropic` to the same `a.anthropicClient`, built with
  `config.WithRegion("eu-west-1")` at `:87`. The 429 path is `throttled` — set at `:167`/`:326` by
  `isThrottlingError`, read at `:153`/`:299` under the comment *"override vendor to openai if we have
  throttled"* — and **nothing else writes it**, so "the one automatic fallback is 429-only" holds.
- **ENUMERATED** — the four-arm model-default table (`:246-260`), checked arm by arm at `b948604f`:
  `simulator/ai/ai.go:58-59` (`Openai` → `internalAi.Openai`), `:65-66` (`GPT4Dot1`), `:69`/`:82-83`
  (Azure), `:86`/`:98-99` (`Anthropic37SonnetAWS`), `:102`/`:110-111` (`Anthropic35Sonnet`),
  `:114-115` (`default:` → `internalAi.Openai`), `:126-127` (`GPT4Dot1`), and
  `GetValidationAIVendorAndModel` at `:20-26` returning `Azure` + `GPT4Dot1` + `GPT4Dot1Mini`.
  The content-side defaults are `collections/jobsimulation.go:1297` (`Gpt5Point1`) and `:1302`
  (`simulation.Openai`), replaced at `:1298-1305` **before** `simulation.Sequence` is built at `:1307`
  — so the *"nullable field is on the Directus DTO, not the domain Sequence"* correction at `:55-59`
  and `:68-71` is right in both halves.
- **ENUMERATED** — the vendor-enum arithmetic at `:62-66`. `AIVendor` at `:967-975` has **five** members
  (`azure`, `azureglobal` `:971`, `openai`, `anthropic-aws`, `anthropic`) against **four** switch cases;
  `azureglobal` is the one with no case. Both cardinalities derived from source, not from the sentence.
- **ENUMERATED** — the Mistral-is-OCR-only claim (`:33-37`, `:88`), taken **per tree at its own ref**.
  Host tree `stack-demo/app` @ `b948604f`: `internal/cms/studio/markdownManager.go:19`
  (`mistral.NewMistral(nil, os.Getenv("MISTRAL_API_KEY"))`) and `studioManager.go:583`
  (`NewMarkdownManager(os.Getenv("MISTRAL_API_KEY"))`) — both exact; every other host-tree hit is go.mod/
  go.sum/knowledge markdown. Nested tree `stack-demo/app/studio` @ its **own** ref `aeec036a`:
  `git grep -i mistral aeec036a` → **22 hits across 3 files** (`requirements.txt`, `tools/pdf2md.py`,
  `tools/r3.py`) — the doc's figure exactly, and the figure a host-ref `git grep -- studio/` would have
  reported as 0.
- **ENUMERATED** — *"`grep -rin 'bedrock\|boto3' app/studio/` returns 0"* (`:49`). Run at the nested repo's
  own ref: `git grep -in bedrock aeec036a` → **0**, `git grep -in boto3 aeec036a` → **0**, with a positive
  control in the same pass (`git grep -icn azure aeec036a` → non-empty). This is the exact predicate whose
  false clearance §5 rule 44 records, taken the way rule 44 prescribes.
- **ENUMERATED** — the studio slot table (`:120-135`). `configs/production_config.ini:26-36` @ `aeec036a`
  matches the five rows cell-for-cell, `development_config.ini:26-36` is byte-identical, and the stable and
  experimental columns are identical to each other. `configs/config_template.ini:39` and `:40` are the two
  `gpt-4o` `*_STABLE_MODEL` slots the doc names, and the template's stable column really is
  `gpt-4.1-mini / gpt-4.1 / gpt-4o / gpt-4o / o3`. **`gpt-5.2` appears in no config**: its only 5 hits at
  `aeec036a` are `changelog.md`, two `knowledge/` docs, and `services/ai.py:356` + `:508` — both **pricing
  entries**, exactly as `:130-131` says. The `configs/` dir holds only those three `.ini` files
  (`local_config.ini` does not exist on disk), so the zero has no hole.
- **ENUMERATED** — the Evaluation System (`:262-285`). `v3/validator/validator.go:43` declares
  `checkerEngines`, `:60-61` builds it, `:595` passes it — and the only other references
  (`basevalidator/criterion.go:34,72,90`) are the field, the parameter and the assignment. **It is never
  read**, as the doc asserts; dispatch is the hardcoded switch at `criterion.go:126-127`
  (`case check.EngineLlm:`). `:428` is `NewLLMBulkChecker(c.logger)`; the template
  `templates/checkValidationBulk.tmpl` exists and `bulkChecks.go:118` sets `ai.WithTemperature(0.0)`;
  `BulkCheckResult` `:55-60` carries `check_id`/`success`/`feedback`. The deterministic exception is real:
  `criterion.go:168` runs `validateCodeDiff` in a `pool.New().WithMaxGoroutines(2)` alongside `validateLLM`
  at `:175`, and `validateCodeDiff` `:450-475` sets `success` from `cdiff.Diff == ""` — a pure string
  comparison, no LLM. `EngineTextDiff` is real (`criterion.go:142`, `collections/jobsimulation.go:1002`).
  `skills.go:40-51` `calculateCompetencyLevelScore` is verbatim what the doc quotes, `// TODO fix this
  formula` at `:49` included; `calculateSkillScore` `:53-62`; `:75` is `passed/total*100`;
  `skill/skill.go:617-623` is `convertLevelTo100` as a plain division;
  `next-web-app` @ `bb3313bc`'s `packages/ui/src/Competency/CompetencyReadLevel.tsx:18` is the
  `getSkillScoreForSimulation(...)` call.
- **ENUMERATED** — the LiveKit half. `calls/livekit.go:131-135` is the `flag_use_realtime_openai` **read**
  and `:140-144` the **effect** (`agentName = "anthropos-agent"`, `agentEndpoint = "openai-hosted"`) —
  so the corrected claim at `:210-215` (the flag does not select LiveKit over ElevenLabs; it swaps the
  dispatched endpoint) is exact. `voice_engine` is a **4-member** enum at `:1079-1086` and
  `voiceEngineFromDirectus` `:1594-1597` supplies `gptrealtime` on nil — both as `:205-208` claims.
  ElevenLabs is still live (`calls/elevenlabs.go`), and `GetJobSimulationCallConversationToken` /
  `getJobSimulationCallSignedUrl` are both real mutations
  (`simulator/outbound/prompts/replyCall.go:88`, `web/backend/graphql/graph/graph.go:11132,11143`).
- **ENUMERATED (cross-doc)** — the five LiveKit agent repos at `:187-192` agree name-for-name and
  date-for-date with `platform-migration-status.md:143-147`, including the *"azure-eu + azure-eu-fr measured
  as dispatching nothing"* detail. No contradiction.
- **ENUMERATED** — `CreateSpeech` really is Azure-only (`ai.go:243-257`, `// at the moment we only support
  azure voice` `:251`); `coursebuilder/bedrock.go:106-113` is the `ANTHROPIC_API_KEY` either/or switch and
  is **not** reached from `AIManager`; `internal/aiusage/ai_usage.go` is the sole file in that package;
  studio-desk's `AI_PROVIDER_CHAIN` exists (`.env.example:57`, `CLAUDE.md:65`). The pinned citations at
  `:99` resolve **at the ref they name**: at `5ba17044`, `internal/jobsimulation/ai/ai.go:267` and `:344`
  and `internal/skillerai/ai.go:347` are each the `"flag_use_azure_us"` literal, and
  `git ls-files internal/ai/` at `b948604f` returns **0** files — *"no such file"* confirmed.
- **ENUMERATED (intra-corpus)** — every cross-reference resolves: `external_services.md:567` is the
  **Anthropic Direct** provider row; `:619-629` is exactly item 4 of the four EU-exit routes;
  `external_services.md#routing-what-is-actually-implemented` → `:577`;
  `security_compliance.md#eu-ai-act` → `:226`; `studio-room.md#ai-service-configuration` → `:247`;
  `shared_libraries.md#taxonomy-figures` → the `<a id>` at `:196`.

### `messenger.md`

- **ENUMERATED** — the `MESSENGER_RPC_ADDR` absence and its positive control (`:19-32`), re-run in full
  rather than quoted forward. At `stack-demo/platform`: `git rev-list --count 0c91421d` = **121** (the
  doc's figure); `git log -S 'MESSENGER_RPC' --oneline 0c91421d | wc -l` = **0**;
  `-S 'SKILLER_RPC'` = **7**; with `--all` they become **8** and **0**; and `comm` of the two SKILLER lists
  identifies the 8th as **`464dfe3`** *"compose: remove skiller/jobsimulation/cms/skillpath services"* — on
  a non-main branch, exactly as the doc says. `git grep -n "_RPC_ADDR" 0c91421d` hits only `CLAUDE.md:34-35`
  — **zero in `docker-compose.yml`, `common.yml` or `.env_example`**.
- **ENUMERATED** — the fold's two-sided state (`:34-44`). Prod: at the **ref the row names**
  (`messenger` `e9421c6`), `terraform/main.tf:29` is `service_desired_count = 0` and `:27-28` is verbatim
  *"The image and task definition stay declared: this is the rollback path. Restoring service is a one-line
  revert plus an apply, not a re-provision."* Consumer: at `app` `origin/main`, `main.go:15` is the
  `msgflow` import, `:62` `msgadapters`, `:63` `msgsender`; `:1450` builds
  `pubsub.NewSubscriberServer(msgflow.ConsumerGroup, …)`; `:1471` is `msgadapters.Wire(`; `:1473` is
  `Sender: msgsender.NewFromEnv(logger)`; `:1416-1421` is the comment naming
  `cmp.Or(os.Getenv("SERVICE_NAME"), "messenger")`. Local: `repos.yml` @ `0c91421d` names messenger only in
  a comment at `:5`, and there is no messenger compose service.
- **ENUMERATED** — the always-on floor (`:122`), derived the way §5 rule 30 requires, by resolving
  `include: common.yml`. Services declaring **no** `profiles:` key at `0c91421d`: `sentinel`
  (`docker-compose.yml:5`) plus `postgresql` (`common.yml:2`) and `redis` (`common.yml:24`) — **exactly the
  three named**, and two thirds of them are in the included file. `backend` and `gotenberg` are
  `[core, backend, all]`.
- **ENUMERATED** — the deleted compose block (`:44`, `:62-63`, `:124-129`, `:167-171`). At `0dab54d` the
  block is `docker-compose.yml:156-191`: `ports 8200:8200`/`8201:8201`, `profiles: [messenger]` **only**
  (so *"dropped from `all`"* is right), `depends_on: backend`, and all four `*_RPC_ADDR` set to
  `http://backend:8083` under compose's own comment. `repos.yml:21-23` @ `0dab54d` is the messenger entry.
  `git show d11a403 -- docker-compose.yml` shows the re-point (`-CMS_RPC_ADDR=http://cms:8091` →
  `+http://backend:8083`, same for `JOBSIMULATION_RPC_ADDR`) **and** the removal of the cms/jobsimulation/
  roadrunner blocks — so both the M809 attribution and the *"husk container went at `d11a403`"* claim hold.
- **ENUMERATED** — the standalone-side facts. `messenger` @ `fa47850`: `cmd/root.go:63` `PORT` → `8080`,
  `:64` `RPC_PORT` → `8081`, `:107` `REDIS_STREAMS_INDEX` → `"2"`, `:147` `READONLY_DB_CONNECTION`;
  `:118-142` constructs the four addressed clients (cms / backend-users ×2 / skiller / jobsimulation);
  `rpcsrv.go:25-30` returns `CodeUnimplemented` for **both** `CancelScheduledMessage` and `Schedule`;
  `flow/jobsimulations.go:140-151` is the 2 h / 12 h staleness pair verbatim;
  `flow/assignments.go:828` is `getSkillPath` reaching `h.cms.GetSkillPath`;
  `internal/messenger/console/` exists; `go.mod` is `go 1.25.0`, `brevo-go v1.1.3`, `liquid v1.8.1`;
  the three named `_test.go` files exist. There are exactly **three** `AddSubscriber` calls —
  `backend`, `jobsimulation`, `cms` — matching `:104`.
- **ENUMERATED** — the `app`-side anchors at `:104`, `:133`, `:149`, `:168`. `main.go:1095` @ **the ref the
  sentence names** (`9d00a313`) is the `CMS_STREAM` publisher and `:1478-1484` is
  `streamNamesForSubscribers` — both exact. `docker-compose.yml:84-92` @ `0c91421` is precisely the
  MESSENGER_ENABLED comment block, including *"Pinning them to `false` here would override .env"*.
  `main.go:1205-1211` @ **`b948604`, the ref the row names**, is the *"additive + DORMANT … until the M809
  re-point"* comment — so the doc's *"and is stale in `app`"* verdict is correctly attributed to the
  platform, not to itself.
- Two anchors resolve **only past the checkout**, and I judged neither a finding:
  `app/env_guards.go:61` (`:53`) and `app/main.go:295` (`:149`) do not exist at `b948604f` — the file
  `env_guards.go` is not in that tree at all — but at `origin/main` `:61` is
  `envMessengerEnabled = "MESSENGER_ENABLED"` and `:295` is the `BREVO_KEY` `log.Fatalf` guard, both exactly
  as described. §5 rule 41 makes `origin/main` the guards' own first resolution rung, and the checkout is
  93 commits stale, so this is clone staleness rather than a corpus defect. **Flagging it so the
  adjudicator can rule otherwise.**

### `gotenberg.md` — clean; every claim ENUMERATED

- `docker-compose.yml:183` @ `0c91421` is `profiles: [core, backend, all]`, and the file is **186 lines**,
  so the doc's own note that the anchor moved from `:268` (killed when `838d907` deleted three blocks) is
  itself correct. The compose block `:170-183` matches the quoted YAML including
  `--libreoffice-restart-after=50` and `3200:3200`.
- The **nine** gotenberg MIME types are `extract.go:17-27` @ `9d00a313` — I counted the map: `.xls`,
  `.xlsx`, `.ppt`, `.doc`, `.odt`, `.ods`, `.odp`, and RTF under **both** `application/rtf` and `text/rtf`.
  **DOCX is genuinely absent**, as the doc insists. `:59` is the `gotenbergURL != ""` guard, `:43-45` the
  degrade comment, `:77` the `ConvertToPDF` call and `:81` the immediate
  `ConvertFromReader(bytes.NewReader(pdf), "application/pdf")` — the "discarded in the next statement"
  claim is literally true.
- `worker/tasks/user_import_resume_2d.go:67-74`: DOCX-only, gated on `h.cfg.GotenbergURL != ""`, reached
  only after *"document has no readable text"* (`:65`), assigning `ocrInput = pdfBytes` at `:73` and merely
  logging on failure at `:70`. Both "OCR fallback" and "non-fatal" verified.
- `internal/converter/gotenberg.go`: `ConvertToPDF(ctx, gotenbergURL, document, filename)` `:16`,
  `Timeout: 90 * time.Second` `:13`, `POST …/forms/libreoffice/convert` `:31`.
  `GOTENBERG_URL=http://gotenberg:3200` is `docker-compose.yml:57` on the backend block.

### `coursebuilder.md` (apart from B2)

- **ENUMERATED** — the backend-selection paragraph (`:48`), the file's load-bearing claim.
  `internal/coursebuilder/bedrock.go:98-103` is `ModelBackendName()` returning `"anthropic-api"` /
  `"bedrock"`; `:105-114` is `newUnderlyingClient`, first-party API when the key is set, Bedrock otherwise;
  `main.go:770` @ **`b948604`, the named ref**, is the boot log line. Production requirement:
  `terraform/variables.tf:635-638` (`sensitive`, **no default**), `ssm.tf:328-333`, `main.tf:555`.
- **ENUMERATED** — the routes-unmount mechanism (`:66-72`): `main.go:766-769` is the comment,
  `:774` and `:778` are the two `logger.Warn(… /coursebuilder routes disabled)` arms — nested, so it really
  is *"neither backend can build a client"*. `backend.go:227` builds `courseBuilderAccessGate`.
  `handler.go:3143 Register` mounts every route the doc lists (plus `/sessions/:id/credits`,
  `/cover/generate`, `DELETE /cover`, which it does not claim to enumerate). Every SSE event name at
  `:77-79` is emitted in `handler.go` (`patch_applied` `:2614`, `preview_ready` `:2671`, `draft_kept`
  `:2678`, `translation_ready` `:2724`, `rebuild_required` `:2732`, …).
- **ENUMERATED** — the numeric claims, each derived from the **code constant**, never the prose beside it.
  `internal/credits/cost.go:86-90` `creditCost` = `{course.build: 5, course.refine: 1, course.translate: 1}`
  — the corpus is **right** and the file's own package-doc comment at `cost.go:29` (*"course.refine → 5
  credits"*) is the thing that is stale; a reader who audited the comment instead of the map would have
  mis-booked this. `budget.go:27` `DefaultMaxMonthlyCOGSUSD = 500.0`; `:41` `DefaultMaxDailyCOGSUSD = 0.0`
  (*"0 = off"*, and `SPEC.md:302`'s *"default 50"* is superseded prose inside the platform);
  `rate.go:28 DefaultSessionsPerOrgPerDay = 50`; `locale.go:22-30` `SupportedAuthorLocales` has **exactly 7**
  members (it/fr/de/es/nl/ja/pt), matching *"translate (7 locales)"*.
- **ENUMERATED** — structure: `go.mod` is `go 1.26.4`; `internal/data/ent/schema/coursebuildersession.go`
  and `terraform/migrations/20260717151144.sql` both exist, and that migration creates **all three** named
  tables (`course_builder_sessions` `:2`, `credit_transactions` `:40`, `organization_credits` `:56`);
  `internal/worker/worker.go:199` `CoursebuilderSrv`; `cmd/coursebuilder-liverun` + `cmd/coursebuilder-e2e`
  exist and `assertNotSharedDSN` lives only in the latter; all five named in-repo docs exist;
  `internal/coursebuilder/**` is **102** `.go` files (*"~100"*); models `eu.anthropic.claude-opus-4-8`
  (`bedrock.go:23`), `eu.anthropic.claude-sonnet-4-6` (`:29`), `gpt-image-2` (`imagegen/openai.go:22`);
  `ask/handler.go:82 SetCourseBuildStarter`.
- **ENUMERATED** — the `COURSEBUILDER_OPENAI_IMAGE_KEY` retraction at `:99`, which is the kind of claim
  that is usually left half-repaired. At `b948604f` that string survives in **exactly two markdown files**
  (`GO-LIVE-RUNBOOK.md:35`, `README.md:429`) and **no Go file**; `main.go:824-826` reads `OPENAI_KEY` +
  `CB_IMAGE_MODEL`; `68c24512` is a real commit. *"Setting it fixes nothing"* is correct.

### `db-backup.md`

- Nothing to enumerate: the file carries **no `file:line` citation, no sha, no repo path, and no count**,
  and there is no `db-backup` clone in the ground-truth set. It is internally consistent and consistent
  with root `CLAUDE.md`'s *"Production-only / deployed-only (not in local docker-compose)"*. Correctly
  absent from `platform` `repos.yml` and `docker-compose.yml` @ `0c91421d`. **Not a clearance — an
  unfalsifiable file.**

---

## What I could not settle

1. **`db-backup.md` in its entirety.** Every claim (6-hour cadence, S3 + Azure + Hetzner triple target,
   Go, "all schemas", "combined with RDS point-in-time recovery") is unverifiable from this environment —
   there is no `db-backup` repo among the 15 ground-truth trees and no terraform for it in `app` or
   `platform`. **What would settle it:** a clone of `anthropos-work/db-backup`, or the infrastructure
   repo's module for it. Per §5 rule 27 this is a document that is neither derived nor fenced and is not
   marked prose-under-review; that framing gap is worth a decision even though I cannot grade the content.
2. **`ai_architecture.md:141` embedding row counts** — `public.skill_embeddings` = 42,790 and
   `public.job_role_embeddings` = 18,919 *"measured 2026-06-29"*. These are prod-DB measurements; I did not
   query prod (and would not without a `/db-query` mandate). The claim is self-consistently pinned to a
   date and consistent with `shared_libraries.md#taxonomy-figures`. **What would settle it:** a read-only
   `SELECT count(*) … WHERE organization_id IS NULL` on the two tables.
3. **`alignment_testing.md:447-448` and `:505`** — *"against the M7a-seeded `demo-1`, `measure` reports
   100% / Critical 100% across the 4 seeded surfaces"*, and the M9b/M10 live-replay results. These are
   historical live-stack measurements; the DNA and the operators verify statically, the run does not.
   **What would settle it:** a `datadna measure-snapshot` run against a seeded stack.
4. **`coursebuilder.md:130` "142 changelog lines."** `internal/coursebuilder/` has no `CHANGELOG` file; the
   Wave history lives inline in `SPEC.md` (Wave 24 entries at `:383,398,432,462,486`). I could not
   determine which line set the 142 counts, so I neither cleared nor booked it.
5. **Out-of-scope self-contradiction I noticed and did not book.** Root `CLAUDE.md` states *"the only
   remaining cross-process RPC edges are **backend → sentinel** and **messenger → backend**"*, while
   `messenger.md:5-9,30-32,44` (my file, and correct against `0c91421d`) establishes there is no messenger
   process, no compose service, and **zero** `*_RPC_ADDR` set anywhere. `backend.md:241` already carries the
   corrected form. This is §5 rule 19's corollary — a claim repaired inside the audited scope and left
   standing at the **highest-propagation site in the tree** — but `CLAUDE.md` is not in my file set, so I
   report it rather than book it.
