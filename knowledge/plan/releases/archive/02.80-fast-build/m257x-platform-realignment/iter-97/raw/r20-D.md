# Seat D — reading #20

Corpus under audit: `rosetta` @ `00be1ac` (tree clean apart from this iter dir).
Ground-truth refs re-derived at open and identical to the ADDENDUM sheet:
`platform 0c91421d` · `app b948604f` (origin/main `2035f9a4`) · `app/studio aeec036a` ·
`cms/studio aeec036a` · `next-web-app bb3313bc` · `storage 4ce8ece5` · `messenger fa47850d` ·
`jobsimulation 462343b0` · `cms ca50c817` · `sentinel 88bc5592` · `roadrunner 87d8d443` ·
`graphql-wundergraph 60c229f3` · `studio-desk 14a5442a` · `ant-academy 9c3843cd` ·
`.agentspace/rosetta-extensions 6130bfd8`.

Search-set enumeration (rule 44) run at open: 15 git trees under `stack-demo`, **including the two
nested `anthropos-studio-room` copies** (`app/studio`, `cms/studio`). Every absence claim below was
measured per tree at that tree's own ref, with a positive control in the same pass.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/services/studio-room.md` | 506 | yes |
| `corpus/architecture/architecture_overview.md` | 413 | yes |
| `corpus/architecture/security_compliance.md` | 281 | yes |
| `corpus/services/roadrunner.md` | 200 | yes |
| `corpus/services/next-web-app.md` | 133 | yes |
| `corpus/architecture/dependency_map.md` | 103 | yes |
| `corpus/services/intelligence.md` | 18 | yes |
| **total** | **1,654** | |

Also read (in scope, not assigned): `corpus/ops/platform-alignment.md` §5 in full (lines 249–1402).

---

## Blockers

### B1 — `roadrunner.md:114` states the jobsimulation repo holds "one `roadrunner` mention"; it holds 5 (or 14)

- **Anchor:** `corpus/services/roadrunner.md:114` —
  > "The jobsimulation consumer was **deleted, not moved**: at `jobsimulation 462343b0` the whole repo
  > contains one `roadrunner` mention (`internal/runner/runner.go:3`, a parenthetical), with no handler
  > and no event reference."

- **Why it is false:** the claim pins its own ref (`462343b0`), which is the checkout, so no pin
  rescues it. Measured at that exact ref with the repo's own `git grep`:

  - case-sensitive `roadrunner` → **5 lines across 3 files**:
    `CHANGELOG.md:1066`, `:1067`, `:1070`; `internal/runner/runner.go:3`;
    `knowledge/architecture.md:61`.
  - case-insensitive → **14 lines across 8 files**, adding `CLAUDE.md:112`,
    `knowledge/features.md:70`, `knowledge/glossary.md`, `knowledge/operational.md`.

  So "the whole repo contains one mention" is wrong by 5×–14×, and the single site it names is not the
  only one even under the narrowest reading. `knowledge/architecture.md:61` in particular is a live
  architecture table row naming `roadrunner`, not history.

- **Hesitated because:** the *conclusion* the count supports survives — I confirmed there is no handler
  and no event reference (`RoadRunnerService`: 0 hits at `462343b0` and at `origin/main`;
  `10401`: 0 hits; and `app/internal/jobsimulation/simulator/stream_handlers.go:30-34` states the
  roadrunner-submission pubsub event was removed upstream, which I read in full). The defect is the
  stated measurement, not the verdict. Booked as a BLOCKER because the briefing lists "wrong counts"
  explicitly and because a reader re-deriving this exact grep is contradicted on the first command.

- **Confidence:** high (that the count is false); medium (that it changes a reader's action).

### B2 — `architecture_overview.md:334-336` says `STORAGE_RPC_ADDR` has "3 hits repo-wide" at `9d00a313`; there are 29

- **Anchor:** `corpus/architecture/architecture_overview.md:334-336` —
  > "And `STORAGE_RPC_ADDR` has **zero** reads anywhere in `app` (3 hits repo-wide at `9d00a313`, every
  > one a comment)."

- **Why it is false:** measured at `app` `9d00a313` (the ref the clause names):
  - `git grep -n 'STORAGE_RPC_ADDR' 9d00a313` → **29 lines across 18 files**.
  - Restricted to Go source (`-- '*.go'`) → **3 lines across 3 files**, and those three *are* all
    comments: `internal/jobsimwiring/wiring.go:101`, `internal/storagens/callsites_test.go:189`,
    `main.go:451`.

  The number 3 is right for *Go source* and wrong for *"repo-wide"* by ~10×; the other 26 lines are
  `knowledge/**` markdown, which are not "comments" either, so both halves of the parenthetical fail
  under the scope the sentence itself declares.

- **Hesitated because:** the operative predicate — *zero reads anywhere in `app`* — is **TRUE and
  correctly measured**, and the two anchors around it verify exactly (`main.go:451` at `9d00a313`
  and `main.go:504` at `origin/main` both carry *"the standalone service takes no traffic and
  STORAGE_RPC_ADDR is gone"*; `internal/jobsimwiring/wiring.go:115` at `b948604` really is the last
  live read). This is a wrong scope-word attached to a right conclusion. Booked as a BLOCKER on the
  briefing's "wrong counts" clause and its anti-under-booking instruction.

- **Confidence:** high (the count is false as scoped); low-medium (materiality).

---

## Minors

- `roadrunner.md:25-26` — *"M247 re-grepped `app` + `jobsimulation` on the consolidated clones — zero
  hits **outside CHANGELOG**"*. At `jobsimulation` origin/main `ROADRUNNER_RPC_ADDR` also lives in
  `knowledge/operational.md`, and at `app` origin/main in
  `knowledge/plan/releases/07.00-jobsim-in-app/RE-PORT-CHECKLIST.md`. The carve-out is not exhaustive.
  Kept MINOR (not B3) because the sentence's grammatical subject is *"read in any service's **Go
  code**"*, and that claim is **verified true**: `git grep -l 'ROADRUNNER_RPC_ADDR|RoadRunnerService'
  HEAD -- '*.go'` returns zero in `app`, against a positive control of `JUDGE0_BASE_URL` →
  `internal/jobsimwiring/wiring.go`.

- `studio-room.md:348-359` — the `requirements.txt` code block reproduces the file with `mistralai`
  in **3rd** position; in the file it is **line 8** (after a blank line). The *set* is exact
  (openai, anthropic, rich, pyyaml, requests, jinja2, mistralai, pytest, pytest-asyncio) and the
  callout immediately below correctly cites `requirements.txt:8`. Presentation only.

- `security_compliance.md:233` — the range `skills.go:53-64` for `calculateSkillScore`: the function
  is **53–62**; line 64 is the signature of the *next* function (`run`). The named construct and the
  `:75` divide anchor are both exact.

- `security_compliance.md:72-73` — `mixin.go:98` for `UserMixin`'s `Policy()`: `:98` is the doc
  comment, `:99` the `func`. Off by one onto its own comment.

- `roadrunner.md:61` (the project-structure block) labels the studio tree `studio-room/` while
  `studio-room.md:340` correctly states *"studio-room's root IS `app/studio/`"* — a label, explained
  three sections later; no factual consequence. (Same in `studio-room.md:61`.)

---

## Positively cleared (audited zeros)

Labelled per the briefing. Only **ENUMERATED** counts as a clearance.

### ENUMERATED

**Multi-tenancy / Layer 1 — the fence rule 24 names as having failed three generations.**
I re-derived the **SET**, not the sum, at `app b948604f`:
- `internal/data/ent/schema/`: **139** `.go` files, **135** declaring `ent.Schema`; the 4 that do not
  are `database_types.go`, `mixin.go`, `skiller_mixins.go`, `skillpath_mixins.go`. Denominator ✓.
- `grep -l 'OrganizationMixin{}'` → **30 files**; `user_resource.go:22` is
  `// OrganizationMixin{},  // We need to work on this` — commented out. Live set = **29** ✓.
- Exactly **four** files declare any `Policy()`: `organization.go`, `mixin.go`, `user.go`,
  `org_membership.go` ✓ (the doc's own claim). Of the schema-level ones, `Organization`
  (`organization.go:56`, `rule.FilterSameOrganizations()` at `:96`) and `Membership`
  (`org_membership.go:172-188`, ending `privacy.AlwaysDenyRule()`) filter by **organization**;
  `User` (`user.go:116`) filters by **user** (`rule.FilterSameUserRule()`) and is correctly excluded.
  **29 + 2 = 31 of 135** ✓, and no double-count (neither `organization.go` nor `org_membership.go`
  is in the mixin set).
- `OrganizationIDMixin{}` users: **7** — `category, jobrole, similarity, skill, specialization,
  studio_document, studio_task` ✓ exactly the seven named. Its definition
  (`skiller_mixins.go:148-162`) is verbatim *"a plain, nullable `organization_id` UUID column…
  intentionally does NOT attach backend's OrganizationMixin privacy policy"* ✓.
- I ran the document's own derivation verbatim and independently: neither-mixin schemas with a plain
  `organization_id` = **18**; minus `org_membership.go` (self-policed) and `academy_feedback.go`
  (`UserMixin{}` at `:64`) = **16**, and the 16 filenames the doc lists match my derived set
  **element for element**. **16 + 7 = 23** ✓.
- Supporting quotes exact: `job_simulation_session.go:5` *"L2: NO Ent privacy Policy; owner/org/tenant
  are plain fields"*; `jobrole.go:18` / `category.go:15` on the globally-readable taxonomy.
- `architecture_overview.md:359-366` restates all of this consistently — no drift between the twin
  sites.

**Studio-Room CLI + pipeline (`studio-room.md`), at `app/studio aeec036a` — the nested repo, greped at
its own ref.**
`gen.py:484-492` registers **exactly nine** `add_argument` calls (I enumerated all nine; there are no
others in the file) ✓ · `parse_argument` at `:18-28` really does `parse_known_args` + fold unknown
pairs ✓ · `validate_blueprint_exclusivity` `:241-271` with the exact allow-set and the exact error
string ✓ · `translate_legacy_blueprint` `:205-238` with **exactly three** legacy names ✓ ·
`setup_generation_request` `:273-282` ✓ · `work_paths` `:450-456` ✓ · `services/ai.py:705-708` is the
whole provider registry `{openai, azure, anthropic}` and `ai.py:1-2` imports only openai/anthropic ✓ ·
`exporter.py:518-519` / `:513-550` ✓ (incl. the `source_id`-then-simid zip naming at `:517`) ·
`model.py:59,467-469` taxonomy memo ✓ · `GenMode` five members with `DEFAULT = EXECUTION` ✓ ·
`max_tokens = 4000` in all three tracked configs and the `[SERVICES]` model rows verbatim ✓ ·
`worklog_path = workspace/trace` with an in-code `worklog` fallback ✓ · state files
`_pre_generation.json` / `_task_state.json` (`gen.py:116-117`) ✓ · `postgen.py` `--media/--simid/
--target` all `required=True` ✓ · blueprint keys match `tests/e2e/blueprints/technical.json` ✓ ·
5 e2e + 3 asset-example blueprints ✓ · repo `CLAUDE.md:12-14` entry point ✓ · directory tree matches
`git ls-tree` exactly ✓ · `template` has **zero** consumers outside the legacy-blueprint path + tests
(greped at the nested repo's own ref, which is the only instrument that sees gitignored `tools/`) ✓.

**The `mistralai` retraction (`studio-room.md:362-389`, echoed at `dependency_map.md:25`) — the one
rule 44 was written for.** `git -C app/studio grep -i mistral aeec036a` returns **22 hits across 3
files** — I counted them line by line: `requirements.txt:8` (1) + `tools/pdf2md.py` (18) +
`tools/r3.py` (3) = 22 ✓ exactly. `pdf2md.py:24` is a real `from mistralai import Mistral` ✓,
`:96` builds the client ✓, `:127` is `model="mistral-ocr-latest"` ✓, `r3.py:194` hard-requires
`MISTRAL_API_KEY` as step 2 of the any2pdf→pdf2md→md2cleanMd chain ✓. `gen.py` imports only
console/format/agents/services.ai/postgen ✓. Ignore-rule claims confirmed: `app/studio/.gitignore:2`
= `tools/`, `app/.gitignore:79` = `studio/*`, `cms/.gitignore:129` = `studio/*` ✓, and the second copy
`stack-demo/cms/studio` is the same repo at the same sha ✓.

**Studio-Room embedding.** `internal/cms/worker/worker.go:29` `Concurrency: 5`, `:32-33`
`AiVideoQueue: 7` / `StudioQueue: 3` ✓ exactly as claimed. `studioManager.go:119` execs
`studio/gen.py` ✓; `:94-96` is the `studio/studio-venv` const block ✓. `Dockerfile:45-46` copies the
tree + pip-installs ✓, base image `python:3.11-slim` (`:28`) ✓. The `additional_repo` claim is
confirmed at `.github/workflows/build-production.yml:29`
(`additional_repo: "anthropos-studio-room:studio"`) — note the Dockerfile's own comment at `:42`
calls it a git submodule, and there is **no `.gitmodules`** and **0 tracked files under `studio/`**,
so the corpus follows the correct one of the two in-repo statements.

**Platform topology at `0c91421`.** I enumerated compose directly: `docker-compose.yml` declares
**5** services (sentinel, backend, studio-desk, next-web-app, gotenberg); `common.yml` adds
postgresql + redis → **7 effective**. `repos.yml` has **4** entries (app, sentinel, next-web-app,
studio-desk). At `0dab54d` it was **8 + 2 = 10** and **6**. Every one of these figures is stated
correctly and consistently at `architecture_overview.md:12/71/157/223` and `dependency_map.md:31` ✓.
Only `backend`, `gotenberg`, `studio-desk`, `next-web-app` carry a `profiles:` key, so the
always-on floor is exactly {postgresql, redis, sentinel} ✓ — and `core` = 5 containers, 2 of them
ours ✓. `Makefile:10` `PROFILE ?= core` ✓. `838d907` deletes `storage`/`customerio-sync`/`messenger`
from compose and `storage`/`messenger` from `repos.yml` ✓; `d11a403` deletes `cms`/`jobsimulation`/
`roadrunner` from **both** files ✓ (its message's *"already gone"* really is contradicted by its own
diff, exactly as `roadrunner.md:17-19` and `architecture_overview.md:211-213` say).

**RPC-edge collapse.** At `0c91421` compose sets exactly one service address,
`AUTHORIZATION_ADDRESS=http://sentinel:8087` at `docker-compose.yml:48`, and **zero** `*_RPC_ADDR`
variables — I greped the whole compose set ✓. At `0dab54d` the messenger block set all four at
`http://backend:8083` (`:173/:174/:176/:183`) under compose's own comment at `:171-172` ✓, exactly as
`dependency_map.md:21` quotes it (and it deliberately cites no `file:line` for a deleted block —
correct discipline). `docker-compose.yml:101-109` is `backend`'s whole `depends_on`, with the
in-line comment at `:102-103` quoted verbatim ✓. `studio-desk`'s `depends_on` is `backend` and only
`backend` at `:138-140` ✓. `next-web-app` `depends_on: backend` at `:165-167` with `backend`'s
`profiles:` at `:110` ✓ — so `PROFILE=frontend` alone really does exit 1.

**Redis Streams rows (`dependency_map.md:58-64`).** `app/main.go:1442` @ `9d00a313` is the
`verifyConsumerGroupExists(... msgflow.StreamBackend, StreamJobSimulation, StreamCMS)` takeover guard
✓; `internal/messenger/flow/streams.go:65` is `StreamBackend = "backend"` ✓.
`messenger/internal/flow/flow.go:72 / :105 / :109` are the three `AddSubscriber` literals ✓.
**"21 live handlers"**: lines 72–104 contain **22** `pubsub.EventHandler(...)` occurrences of which
**one is commented out** (`OrgJobSimulationAssignmentPastDueHandler`, *"// not implemented"*) → **21
live** ✓ — the corpus applied the rule-24 exclusion correctly, and `streams.go:70` independently says
"the 21-handler set". The **skiller-stream** row is the strongest one: I enumerated **every**
publisher constructor in `app` @ `b948604f` — `main.go:287`, `:637`, `:1039`,
`internal/jobsimwiring/wiring.go:127`, `:180` — five, and `SKILLER_STREAM` is not among them; its
only Go occurrence is `main.go:1276`, an `AddSubscriber`, handler at `internal/roles/roles.go:791`
(`SkillerSubscriber`) ✓, while `docker-compose.yml:71` still sets the name ✓. A live subscriber on a
stream nothing publishes to — measured, not quoted.

**`authn` is imported by nothing (`architecture_overview.md:80`).** Greped **per tree at its own ref**
across all seven Go clones *and* both nested studio repos: `github.com/anthropos-work/authn` → **0
files everywhere**; positive control `github.com/anthropos-work/colony` in `go.mod` → **1 file in each
of the seven** ✓. Exactly the claim, exactly the denominator.

**`taxonomy` row (`dependency_map.md:50`).** `app/go.mod:20` @ `b948604f` = `taxonomy v1.2.0` (direct)
✓; `sentinel/go.mod:21` @ `88bc5592` = `taxonomy v1.2.0 // indirect` ✓. Both anchors exact.

**Jobsim "23 run-state tables"** (stated at `architecture_overview.md:179`, `:221`, `:394` and
`dependency_map.md:13`). I counted `CREATE TABLE` in
`app/terraform/migrations/20260722081626_jobsim_data_model.sql`: **23**, and listed all 23 names
(`sessions … validation_test_runs`) ✓.

**`next-web-app.md` — structural claims at origin/main `8297c684`** (the file asserts currency, so I
graded there): `apps/{web,hiring,integration,maintenance}/package.json` all declare `"next":
"^16.2.7"` — 4 apps, mobile carries none ✓ · React `^19.2.7` ✓ · `"node": ">=24.0.0"` ✓ ·
`packageManager: pnpm@10.30.3` ✓ · `turbo ^2.9.6` ✓ · `repos.yml type: node-pnpm` ✓ ·
`proxy.ts` exists in web/hiring/integration and **`middleware.ts` exists nowhere** ✓ · repo
`CLAUDE.md:15` = *"…Next.js 16 App Router…"* and `:55` = the proxy.ts rename note, both verbatim ✓ ·
**no** `storybook` script and **no** `.storybook/` dir, the sole trace being
`configs/tailwind/storybooks.css` ✓ · exactly one root Dockerfile (`Dockerfile.dev`, `node:24-alpine`,
`pnpm turbo build --filter=@anthropos/web-app`) ✓ · **8** locales `de,en,es,fr,it,ja,nl,pt` ✓ ·
`"test": "turbo test"` with jest in web+hiring only ✓ · `apps/mobile` excluded from the pnpm
workspace ✓ · **no `@apollo/client` anywhere**, `graphql-request` + `@tanstack/react-query` in
`packages/graphql/package.json` ✓ · `antd ^6.3.7` ✓ · package names + ports 3000/3001/3002/—/3031 ✓ ·
public allowlist `/login, /sign-up, /checkout, /free-trial, /monitoring, /print, /api/bunny/thumbnail`
all present in `apps/web/src/proxy.ts` ✓ · `Makefile:119-120` = `--profile core --profile frontend` ✓ ·
`docker-compose.yml:151` (build arg) and `:160` (runtime env), and the historical `:236` @ `0dab54d`,
all resolve ✓.

**`roadrunner.md` code claims at `87d8d443`** (repo is level with origin/main): `terraform/main.tf:19`
`service_desired_count = 1` ✓, HEAD dated 2026-06-19 ✓ · `cmd/root.go:110` PORT `cmp.Or(…, "8080")`
and `:84` RPC_PORT `cmp.Or(…, "8081")` ✓ · Go 1.25.0 ✓, asynq `v0.25.1` ✓, gorilla/websocket ✓ ·
worker `Concurrency: 10` ✓ · queue const `roadrunner:default` ✓ · task const
`roadrunner:submissionresult` ✓ · `asynq.MaxRetry(3)` at `runner.go:126` ✓ · `maxRetries = 15` at
`:244` with `time.Sleep(1 * time.Second)` at `:273` ✓ · the three RPC methods `Submission`,
`SubmissionPackage`, `SubmissionResult` ✓ · `internal/lsp/lsp.go` present and unwired ✓ ·
**zero** `*_test.go` files with `Dockerfile:18` = `RUN go test -v ./...` ✓ · `ROADRUNNER_RPC_ADDR`
absent from `.env_example` and every compose file (positive control: `JUDGE0` = 1 hit) ✓.
Historical anchors all exact: `repos.yml:29-31` @ `2adcf71` with **9** entries ✓;
`docker-compose.yml:291-292` port publishes, `:298` `PORT=10400`, `:302` `RPC_PORT=10401` @ `2adcf71`
✓; jobsimulation's service at `:83` with `profiles: [graphql, jobsimulation, all]` @ `2adcf71` ✓;
`repos.yml` = 6 entries @ `0dab54d`, 4 @ `0c91421` ✓ (both lists named correctly, element for
element).

**`security_compliance.md` beyond Layer 1.** `external_services.md:579` is exactly *"There is no
ordered EU-first fallback chain."* ✓; `:602-607` is exactly the four-live-plus-one-latent enumeration
✓; `:567` is the Anthropic-Direct provider row ✓; `coursebuilder.md:48` does contain *"so the shipped
path is the direct API"* ✓; the bare `README.md:21` resolves to the sibling
`corpus/architecture/README.md:21`, which does say cost tracking lives in `app`, not the `ai` library
✓. Code side: `ai.go:85-88` `config.WithRegion("eu-west-1")` under the *"anthropic client"* comment ✓;
`:129` `isThrottlingError`, applied at `:166` and `:325` ✓; `:262-277` the `flag_use_azure_us` swap
`azureClientEu → azureClientUs` ✓; `coursebuilder/bedrock.go:109-112` `newUnderlyingClient` →
`NewAnthropicClientWithModel`, `:100` returns `"anthropic-api"` ✓. EU-AI-Act blockquote:
`skills.go:53-62` counts booleans and `:75` divides ✓; `checkerEngines` is **assigned at
`criterion.go:90` and never read** ✓ (registered with exactly one engine at `validator.go:60-62`);
the real dispatch is the switch at `criterion.go:127` → `validateLLM` → `NewLLMBulkChecker` at `:428`
✓; the template `checkValidationBulk.tmpl` line 27 reads *"assess whether the `<asset>` … meets or
does not meet"* verbatim and returns `{"check_id","feedback","success"}` (`:92-105`) ✓; the
`EngineTextDiff` exception is real — `criterion.go:168` dispatches `validateCodeDiff`, `:450-475`
sets `success` from a pure `cdiff.Diff == ""` comparison with no model ✓. Layer 3:
`useOrgSelection.tsx:94`, `useResolveActiveOrg.tsx:107`, `useActivateMembershipOrg.tsx:81` are each a
`clerk.setActive({ organization })` call **in both `apps/web` and `apps/hiring`** ✓ — six anchors,
six exact.

**`architecture_overview.md` remaining.** `graphql-wundergraph/terraform/main.tf:20`
`service_desired_count = 1` ✓ · `915da06` (2026-07-29) deletes **both** `schemas/cms.graphqls` and
`schemas/jobsimulation.graphqls` in one commit, and its subject line really does say "2→1" while its
body records the 3→2 step — the corpus's "do not take the count from the subject line" warning is
exactly right, and `subgraphs.conf` now holds only `BACKEND=` ✓ · `app/internal/jobsimwiring/
wiring.go:118` @ `b948604` is the Judge0 runner ✓ · `internal/cms/studio/markdownManager.go:11,19`
builds the Mistral client, called from `studioManager.go:583` ✓ · `jobsimulation/terraform/main.tf`
@ origin/main: `6092c6d2` really did delete the `module "jobsimulation"` block, `:15-22` is the M810
comment and `:24-40` the surviving-ownership list ✓, and origin/main's **four** commits are all dated
**2026-08-04** including merged **PR #439** ✓ (so the archive-contradiction note is measured, not
asserted) · `cms/terraform/main.tf:39` `service_desired_count = 0` at both `ca50c817` and
`f38c0c4a`, while `6efa1d5` (merged `f38c0c4`) drops build-production citing M810 ✓ — the
"report both, assert neither" framing is the right call · storage terraform at `9f8cb53` is exactly
**18 lines**, `:9-11` is the *"ECS service … is GONE (v9.0 'support-in-app')"* comment and `:13-16`
the `prevent_destroy` rationale ✓, and `63bffc8:terraform/main.tf:38` did read
`service_desired_count = 0` ✓ · `app/internal/converter/gotenberg.go:31` hits
`/forms/libreoffice/convert` and compose sets `GOTENBERG_URL=http://gotenberg:3200` at `:57` ✓ ·
`extensions` schema really does host pgvector (`extensions.vector(1536)` in the migrations; custom
`postgresql/Dockerfile` builds pgvector) ✓ · `app` is the only `migrations: true` repo ✓.

**`intelligence.md`.** Platform `fdfa189` is verbatim *"chore: remove intelligence service from local
dev orchestration"* (2026-04-17) and its diff removes 25 lines from `docker-compose.yml` **and** 3
from `repos.yml` ✓ — both files, as the doc says. `045857c` is the chronos removal ✓. No
`intelligence` service, profile or repo entry survives at `0c91421` ✓.

### RE-DERIVED (weakest form — listed for completeness, not offered as clearance)

- `23 = 16 + 7` and `31 = 29 + 2` — but note I did **not** stop there: both SETs above were
  enumerated from source independently before the arithmetic, per §5 rule 24.
- `22 = 1 + 18 + 3` for the mistral hit distribution (the per-file counts were themselves enumerated).
- `7 effective = 5 declared + 2 from common.yml` (both terms enumerated).

---

## What I could not settle

- **Taxonomy magnitudes** — *"≥42,790 skills / ≥22,470 job roles"* (`architecture_overview.md:13`,
  `:236`). This is a production-DB measurement; no clone contains the data and I did not query prod.
  It is anchored to `shared_libraries.md#taxonomy-figures`, which exists (`shared_libraries.md:196`
  carries the `<a id="taxonomy-figures">` anchor), so the citation resolves. Settling it needs a
  read-only count against the public taxonomy tables.

- **GitHub repo archive states.** `architecture_overview.md:3` asserts graphql-wundergraph is
  *"ARCHIVED on GitHub (2026-07-30)"* and `roadrunner.md:31-32` asserts skiller (2026-07-01) and
  skillpath (2026-07-31) are archived, while the same corpus (correctly) says jobsimulation's archive
  state is *"not visible to this corpus"*. Archive state lives in the GitHub org API, not in a clone.
  I flag the **asymmetry** rather than a defect: three repos get a positive archive assertion from a
  vantage that is declared insufficient two paragraphs away. Settling it needs `gh api
  repos/anthropos-work/<name> --jq .archived` for each.

- **VPC / subnet topology** (`architecture_overview.md:405`, `security_compliance.md:21-24` —
  `10.0.0.0/16`, ALB + Cosmo Router in public subnets). The `infrastructure` repo has never been in
  any clone set, so no ground truth is reachable.

- **Prod terraform reality vs declaration.** `roadrunner.md:20-22` says prod and the platform's own
  declaration contradict each other and records it unresolved — I verified the declaration side
  (`= 1` in the repo) but cannot see whether an apply has run. The doc's own framing ("recorded, not
  resolved") is the correct posture.

- **`sub-processors = 18` and `DPA v1.4`** (`security_compliance.md:261`). Contractual facts with no
  in-repo artifact.

### Cross-file observation (outside my file set — for the adjudicator, not booked against my files)

Root `CLAUDE.md` cites the no-EU-ladder retraction as
`corpus/architecture/external_services.md:546`. Line 546 of that file is
*"~~Restart Wundergraph: docker compose restart graphql~~ — there is no `graphql` service locally any
more; restart `backend`"* — an unrelated construct. The correct anchor is **`:579`**, which is what
**both** of my files use (`security_compliance.md:7`, `:198`, `:200-201`; `architecture_overview.md:283`).
My files are right; the instruction file — §5 rule 19's "highest-propagation site in the tree" — is
the one carrying the stale anchor.
