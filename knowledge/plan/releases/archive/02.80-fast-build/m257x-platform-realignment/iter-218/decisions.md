# iter-218 — decisions

## `D-M257x-218-1` — the family is enumerated BY EFFECT, and the census corrected its own author

The iter opened with a hand-enumeration sealed at `0f991bc` naming **six** number-matching constructs
across four modules. The by-effect probe — import every `stack-core` module, walk its module-level
compiled `re.Pattern` objects, apply a `(plain, bolded)` probe pair to each — returned a **seventh**
(`suite_census.RE_PYTEST_TERM`) within a minute of existing. **No spelling of a pattern can evade it**,
because the probe is applied to the compiled object rather than to its source, and a matcher added
tomorrow enrols itself. The headline was corrected in place, appended and not substituted.

## `D-M257x-218-2` — the widening and the scoping are ONE edit, because the blind spot hid errors both ways

G2's two emphasis-blind matches point in opposite directions: `update_guide.md:96` is a real repo-count
claim that no check has ever read (a **latent** false-GREEN — it is TRUE today, stated in iter-213's
terms), and `org-repos.md:71` is **not** a clone-set claim and would have been a **false RED**. Landing
the widening alone would have taken the live corpus RED on a correct sentence, which iter-209's
precondition forbids. The possessive rule (`repos'` heads a longer noun phrase) costs **0** live matches
and resolves it; the same-line narrowing was tried first and **refuted before it was written** (it
separates nothing and costs 21 → 14).

## `D-M257x-218-3` — three matchers declared blind-by-design, each with the size that justifies it

`guard_family._STATED_COUNT` (reads stdout; **0** printed occurrences), `suite_census._DECLARED_GO_COUNT_RE`
(reads its own registry's reason strings; **0 of 21** emphasised), `suite_census.RE_PYTEST_TERM` (parses
pytest's summary line). Each is exposed only where it actually reads, and the exposure is measured there
rather than over the repo at large. **Reconciled both ways:** an undeclared blind matcher fails, and a
declared entry whose matcher has since become sighted fails too — *a waiver outliving its subject reads
as coverage*.

## `D-M257x-218-4` — `_N_OF_M` is ROUTED, not waived, and the reason is a unit

`derived_count_guard._N_OF_M` already reads `**28 of 29**`, the idiom this repo writes, so it is **not
blind by this census's measure**. It is blind to the **split** spelling `28** of **29`, live population
exactly **1**: `hardening-ledger.md:4766`, the harden's own `5 of 51`. Declaring it "emphasis-blind by
design" would have graded it against a probe set that never tests it, and parked a real gap where nobody
looks. Routed whole as `SURVEY-M257x-iter218-n-of-m-split-emphasis`, with its price stated: the widening
surfaces an **undispositioned** claim, so it turns the guard RED until a `N_OF_M_DISPOSITIONS` entry is
written — a repair with its own evidence, not a rider on this one.
