# Seat B — iter-48

Repo: `/Users/marco/workspace/anthropos/rosetta` @ `m257x/platform-realignment` (HEAD `cabc3b1`).
Ground truth: `stack-demo/app` @ `5ba17044` (`chore(version): v1.363.2`), plus `stack-demo/{next-web-app,studio-desk,platform,ant-academy,jobsimulation}` and the `.agentspace/rosetta-extensions` authoring copy.

## Coverage (file, wc -l, lines read)

| File | `wc -l` | Lines read |
|---|---|---|
| `corpus/services/ai-readiness.md` | 623 | **all 623**, top to bottom |
| `corpus/architecture/ai_architecture.md` | 292 | **all 292**, top to bottom |
| `corpus/architecture/security_compliance.md` | 263 | **all 263**, top to bottom |
| `corpus/services/ai-labs.md` | 156 | **all 156**, top to bottom |
| `corpus/services/clerk-integration.md` | 128 | **all 128**, top to bottom |
| `corpus/services/customerio-sync.md` | 75 | **all 75**, top to bottom |
| `corpus/architecture/README.md` | 38 | **all 38**, top to bottom |

Every file was read in one pass with the `Read` tool over the whole file (no `limit`/`offset`), and `wc -l` / `awk 'END{print NR}'` agree for all seven. No file was sampled.

**Search hygiene (rules 1 + 2).** Two searches in this pass were caught failing rather than "matching nothing":
- `grep -rn 'bunny' --include=*.go internal/` returned `zsh: no matches found: --include=*.go` — an **engine rejection**, not zero hits. Re-run quoted, it returned 20 hits.
- `grep -rn 'CloseDueAIReadinessCycles\|RefreshLiveSnapshots' … | head -5` appeared to show `CloseDueAIReadinessCycles` absent; it was `head -5` truncation. Re-run un-truncated: `internal/aireadiness/cycles.go:554`.
Positive controls were run for every negative result reported below (`grep 'anthropic' app/studio/` → 5 hits before asserting `bedrock|boto3` → 0; `grep 'anthropos-agent'` → 6 hits before asserting `anthropos-agent-eu` → 0; `grep 'anthropos-work/colony' */go.mod` → 7 files before asserting `anthropos-work/authn` → 0).

### What verified CLEAN (spot list, so the next pass does not re-spend it)

- **`security_compliance.md` Layer 1 (the four-times-wrong fence) re-derives EXACTLY.** In `app/internal/data/ent/schema/`: 139 `.go` files, 135 declare `ent.Schema`, 30 use `OrganizationMixin{}`, 7 use `OrganizationIDMixin{}` (`category`, `jobrole`, `similarity`, `skill`, `specialization`, `studio_document`, `studio_task` — exactly the seven named), only 4 files declare any `Policy()` (`organization.go`, `mixin.go`, `user.go`, `org_membership.go`). The doc's own `comm -23 … | xargs grep -l '"organization_id"'` derivation returns **18**, and subtracting `org_membership.go` (self-policing, `:172-188`, ends `privacy.AlwaysDenyRule()`) + `academy_feedback.go` (`UserMixin{}`, `mixin.go:98/99` `FilterOwnerRule`) gives **16** — matching the doc's 16-file list name for name. `mixin.go:126` is `func (OrganizationMixin) Policy()` exactly. `job_simulation_session.go:5`, `jobrole.go:18`, `category.go:15` are the quoted comments, verbatim, on those lines.
- **The whole `ai_architecture.md` routing rewrite.** `getClient` `:259-288`; vendor consts `:30-33`; `flag_use_azure_us` at `:267`/`:344`; both Anthropic vendors → the same `eu-west-1` Bedrock client (`:87`); `isThrottlingError` `:129`/`:166`/`:325`; `vendor = Openai` `:154`/`:300`. `app/internal/ai/ai.go` **does not exist** in `app` and **does** exist at `stack-demo/jobsimulation/internal/ai/ai.go` — the parenthetical is exactly right.
- **The `ai_vendor`-unset route.** `jobsimulation.go:905` `AIVendor *AIVendor`; `:1297` `aiModel := simulation.Gpt5Point1`; `:1302` `aiVendor := simulation.Openai`; `:1307` `sequences = append(…)`. Switch cases at `simulator/ai/ai.go:58`, `:69`, `:86`, `:102`, `default:` `:114-115`; model fallbacks `:65-66`, `:82-83`, `:98-99`, `:110-111`, `:126-127`; `GetValidationAIVendorAndModel` `:20-26` (Azure / GPT4Dot1 / GPT4Dot1Mini). `azureglobal` is the fifth Directus member with no case (`:971`).
- **Studio-Room was never on Bedrock.** `grep -rin 'bedrock|boto3' app/studio/` → **0** (positive control `anthropic` → 5). `studioManager.go:583` is the `NewMarkdownManager(os.Getenv("MISTRAL_API_KEY"))` line, `markdownManager.go:19` the `mistral.NewMistral` call.
- **The studio slot table.** `production_config.ini:26-36` and `development_config.ini:26-36` are byte-identical stable/experimental blocks matching the table row for row; `gpt-5.2` appears only at `studio/services/ai.py:356,508`; `gpt-4o` appears in `*_MODEL` slots only at `config_template.ini:39,40`.
- **The evaluation-system rewrite.** `checkerEngines` is declared (`validator.go:43`), built with exactly one entry (`:60-61`, `check.EngineLlm`), passed at `:595`, stored at `criterion.go:34/72/90`, and **never dereferenced** anywhere in `internal/jobsimulation/simulator/validation/`. The real dispatch is the hardcoded `switch critCheck.Engine` at `criterion.go:126-127` / `:142`; `validateLLM` `:404`, `NewLLMBulkChecker(c.logger)` `:428`, `bulkChecks.go:118` `ai.WithTemperature(0.0)`, template `checkValidationBulk.tmpl` present with `check_id`/`success`/`feedback`. `validateCodeDiff` `:450-475`, `success` from a pure string compare. `skills.go:40-51`/`53-62`, and **`:75` is exactly** `score = utils.RoundFloat(float32(passed)/float32(total)*100, 2)`. `// TODO fix this formula` is on `:49`. `skill.go:617-623` `convertLevelTo100`; `CompetencyReadLevel.tsx:18` is the `getSkillScoreForSimulation` call.
- **Course Builder.** `bedrock.go:98-103` `ModelBackendName()` returning `"anthropic-api"` at `:100`; `:109-113` `newUnderlyingClient`; those are the **only** two client constructors in the package, so "routes **every** model call" holds. `coursebuilder.md:48` does contain "the shipped path".
- **`external_services.md` cross-refs land:** `:541` is the Anthropic-Direct provider row, `:545` "There is no ordered EU-first fallback chain.", `:569` "**Four** things can send a request outside the EU", `:577-587` the per-line unset-vendor derivation. `architecture/README.md:21` does carry the "cost tracking lives in `app`" clause the security doc cites.
- **`README.md`**: all 10 non-README files listed exist and the directory holds no unlisted file. `anthropos-work/authn` appears in **0** of the 7 checked-out `go.mod`/`go.sum` (positive control: `colony` in all 7); `ai`/`proto`/`taxonomy`/`colony` are all really required.
- **`ai-labs.md`** — verified end to end: `internal/{labs,credits,payments,subscriptions}` + `stripe/` layout; `creditCost` map `{build:5, refine:1, translate:1}` (the doc is right and the platform's own stale header comment at `cost.go:29` saying "5" is wrong — `cost_test.go:10-15` says so explicitly); `purchasePackages` 50/200/500; `MarginMarkup = 1.40`; `PricePerCreditUSD = 0.45`; `DefaultSeedBalance`; `LabsTier` essential/professional/frontier; all five `CapReason`s; `spend_reconciler.go:60` `Interval: 30 * time.Second`; `labsapi/client.go:6` default `:7070` + `LABS_API_PLATFORM_TOKEN` (`main.go:737`); `lab_session.go:21-28` deliberately no `PrimaryKeyMixin` with `field.String("id")`; **all five migrations exist by name**; `labs.graphqls`+`billing.graphqls`+`organizations.graphqls`; `main.go:1219` wraps `LabSessionService` in `authn.HTTPAuthnMiddleware`; `cmd/labs{demo,import,key}` with `:8099` at `labsdemo/main.go:115`; **no `checkout.session.completed`** anywhere (positive control `customer.created` → `api.go:315`); `POST /credits/purchase` removed (`credits/handler.go:12`); `internal/payments` has **no** `_test.go`; `TestPricingDerivation` + `TestPrivacyBackstop_AutoScopesToCallerOrg` exist by name; `stripe-go/v74 v74.30.0`; `app` really is `v1.363.2` @ `5ba17044`.
- **`clerk-integration.md`** — `app/go.mod:31` = `clerk-sdk-go/v2 v2.7.0`; `@clerk/nextjs ^6.39.2` in exactly four package.jsons (web/hiring/integration + `ant-academy/code/package.json:52`); `apps/mobile/package.json:6` `~2.6.18` vs `ant-academy/mobile/package.json:18` `~2.19.36` (13 minors); **exactly 12** webhook event types (`internal/clerk/events/events.go:121-190`); **8** locales in `Clerk.provider.tsx:10-17`; `metabase/route.ts:35` `authData.orgRole !== 'admin'`; **`AuthRole()` has 0 call sites in `app`**; sign-in tokens only under `internal/admin/impersonation`; `ant-academy/code/proxy.js` `clerkMiddleware` + `REQUIRE_ORGANIZATION_MEMBERSHIP` → `/no-organization`; `e2e/package.json:17-18` `@clerk/backend` + `@clerk/testing`.
- **`customerio-sync.md`** — `platform/docker-compose.yml:220-238` matches the doc's snippet field for field (git-URL context, `ssh: ["default"]`, `VERSION: dev`, `GH_ACCESS_TOKEN: $GH_PAT`, `8080:8080`, the `search_path=public` DSN, `profiles: [customerio-sync, all]`, `postgresql: service_healthy`); it is the **only** git-URL `context:` of the 10 build contexts, and the only 8080 mapping; absent from `repos.yml`.
- **`ai-readiness.md`** — the vast majority verified exact: commit `4c28365f` (2026-07-22, that message); `internal/aireadiness/` with every named file; `enum/organization_settings.go:47`; `enum/ai_readiness.go:9-11`; `step1/2/3MaxScore = 30/40/30` (`readiness.go:29-31`); `scoring.go` `archetypeHighBand=75`/`archetypeLowCeil=50` + the four archetype rules + buckets None<25 / Low≥25 / Medium>50 / High≥75; `defaultReadinessSkills` = **19 core @1.0 + 12 enabling @0.5 = 31, weight 25.0** counted line by line; `defaultReadinessSims` = the 3 track-keyed entries; `computeTier1` `:139`; `GetAIReadinessWithOptions` `:289` with both snapshot routes at `:296-297` and `:309-311` and the `:314` fall-through; `computeOrgBreakdowns` `:330`; `buildResponseFromSnapshots` really does call `m.workforce.LoadMembers(ctx, orgID, "")` (`:779`) through `WorkforceDirectory`; the D-07 demopatch manifest reads `path: internal/aireadiness/readiness.go` at **`:42`** and `# v2.7 M254 RE-POINT` at **`:33`**, with `demo-stack/tests/test_aireadiness_snapshot_loadmembers_m254.py` present; **CSV is really 15 columns** (`AIReadinessCSVColumns`, counted — the platform's own `csv.go:35` comment still says "19-column" and is the stale one); `usageDimSpecs` (`how_we_measure.go:1138`) carries **exactly the five current ids**; `holdsBackFromInsights:994` filters `strings.Contains(strings.ToLower(in.Category), "risk")`; `queryInterviewAggregatedReports` reads `public.interview_aggregated_reports`; `how_we_measure.go:285-287` is exactly `FROM public.interactions i JOIN public.job_simulation_sessions s`; the DB really enforces `CHECK (source_id <> target_id)` (`20260722081626_jobsim_data_model.sql:94`); all 13 `ai_readiness_*` ent schemas + `interview_aggregated_report.go` exist; `CloseDueAIReadinessCycles` `cycles.go:554` under `privacy.Allow`; `narrative.go:85` `openai.GPT5Mini`; `CompareCycles` 6 sections. Frontend: `useAiReadinessActive.ts:22` and `aiReadiness.constants.ts:26` **exact**; `AIReadinessClient.tsx` has **0** `posthog` and **0** `flag_ai_readiness` and gates on `:133-134`; `AIReadinessHero.tsx:88` is verbatim `if (!air.deadline) return null;`; `useWorkforceAIReadiness.ts:23-27` **exact** with `grep -c cycle` = **0**; `urls.ts:52`, `useNavbarSections.tsx:4/398-400/547` **exact**; next-web `dae0fb2f7` (2026-07-13) deleted the three components for **−653 lines** (103+220+330) and the legacy route directory is gone; `HowWeMeasureTab.tsx` is **1989** lines with `grep -c interviewQuestions` = **0** and `:1879/1903/1915/1921/1927` all **exact**; `hooks/useAIReadiness.ts:274` is `interviewQuestions: number;`; `SHOW_SECONDARY_TABS` polarity confirmed non-inverted (`.filter(… || (SHOW_SECONDARY_TABS && tab.key === 'compare'))` with `false` strips Compare). Seeder side: `ai_readiness_{config,evidence,funnel,interview_report,sim_skills}.go`, `contentref.go`, `seat_append_test.go`, `TestAIReadinessFunnelSeeder_DayZeroHeroGetsNoSignals` (`ai_readiness_funnel_test.go:230`), `blueprint.go:316` `AIReadinessNotStarted = "not_started"`, `cockpit.go:135/154` `LegacyReadinessPaths`/`ValidateCockpitManifest` — all present.

---

## Blockers

| # | Site | The false claim (verbatim) | What is TRUE | Citation |
|---|---|---|---|---|
| 1 | `corpus/services/ai-readiness.md:410` | "and `keepStartedMembers` **excludes members with no step-1 signal** from the aggregate" | `keepStartedMembers` does **not** look at any step-1 signal. It filters on the presence of an **`ai_readiness_user_step_progresses` row whose `status <> 'not_started'`** — i.e. a *progress row*, not a `user_skill_evidences` row. The mechanism is wrong in **both** directions: a member with a step-1 evidence row but no progress row is **dropped**, and a member with an `in_progress` progress row and zero skill evidences is **kept**. The platform states this itself: *"This DB signal is the only real 'has started' check."* This matters because the sentence is the stated justification for the seeding contract three lines later. | `app/internal/aireadiness/readiness.go:684-698` (`keepStartedMembers` → `queryReadinessStarters` → `filterStartedMembers`) → `app/internal/aireadiness/steps.go:915-938`: `SELECT DISTINCT user_id FROM public.ai_readiness_user_step_progresses WHERE organization_id = $1 AND user_id = ANY($2::uuid[]) AND status <> 'not_started'`; doc comment `steps.go:907-914`. `app` @ `5ba17044` |
| 2 | `corpus/architecture/ai_architecture.md:202` | "LiveKit + OpenAI Realtime is the engine for **new** sessions (gated by the `flag_use_realtime_openai` PostHog flag)" | `flag_use_realtime_openai` does **not** select LiveKit over ElevenLabs, and does not gate "new sessions" at all. Which voice engine runs is decided **per sequence by the CMS `voice_engine` field** (`SimulationVoiceEngine` ∈ `{gptrealtime, elevenlabs, livekitgptrealtime, livekitchain}`, nil → `gptrealtime`). The flag lives **inside** the LiveKit path — `CreateAgentDispatch`, which has already been entered — and only swaps the dispatched agent's **endpoint** from `azure-eu`/`azure-eu-fr` (EU) or `azure-us` to `openai-hosted`. The platform's own comment: *"if the feature flag is enabled, use the livekit with openai key and not azure key."* This is also residency-relevant in a way the doc's routing section does not cover: flipping it moves a live voice session off an Azure-EU endpoint. | `app/internal/jobsimulation/calls/livekit.go:106-145` — flag read at `:131-135`, effect at `:140-144` (`agentName = "anthropos-agent"; agentEndpoint = "openai-hosted"`), EU endpoints at `:101-104`. Engine selection: `app/internal/cms/directus/collections/jobsimulation.go:1079-1085` (the 4-member enum) + `:1594-1600` `voiceEngineFromDirectus` (nil → `gptrealtime`), consumed at `simulator/outbound/prompts/replyCall.go:125-129` / `replyCallLivekit.go:26`. `app` @ `5ba17044` |

---

## Minors

1. **`ai_architecture.md:180`** — the diagram names the EU agent `anthropos-agent-eu`. That string appears **0 times** in `app`, `next-web-app` or `platform` (positive control: `anthropos-agent` → 6 hits). The dispatched names are bare **`anthropos-agent`** (EU), **`anthropos-agent-us`** (US) and **`anthropos-agent-chain`** (chain); `azure-eu` / `azure-eu-fr` / `azure-us` / `openai-hosted` are *endpoints*, not agent names (`calls/livekit.go:110,115,120,126,142`). Only the EU half of the pair is wrong.
2. **`ai_architecture.md:193, 197-201`** — the CMS voice-engine enum has **four** members; **`livekitchain`** (which dispatches `anthropos-agent-chain`, i.e. the undocumented `livekit-agent-chain` repo the doc's own ⚠️ callout at `:184-189` names) is absent from both the `Configuration` line and the Legacy/Transitioning table. Also unstated: an unset `voice_engine` defaults to **`gptrealtime`** — the row this table marks *"Deprecated"* (`jobsimulation.go:1079-1085`, `:1594-1600`).
3. **`ai_architecture.md:222`** — "the enum is `app/internal/cms/directus/collections/jobsimulation.go:983-990`". The `AIModel` const block is `:979-991` (11 members); the cited range omits `Anthropic35SonnetAws` `:980`, `Anthropic37SonnetAws` `:981`, `Anthropic4SonnetAws` `:982`. The three model names the sentence actually cites do fall inside the range.
4. **`ai_architecture.md:96`** — "this line went on publishing it **68 lines below**". `:15` → `:96` is **81** lines. Decorative narration, wrong arithmetic.
5. **`ai-readiness.md:554`** — "`computeCycleTotals` (`how_we_measure.go:253-261`)". The function is at **`:260`**; the doc-comment starts at `:256`; `:253` is `return out, rows.Err()` — the closing statement of the *previous* function. Correct span: `:256-261`. (The paired `:285-287` anchor in the same sentence is exact.)
6. **`ai-readiness.md:316`** — "`AIReadinessClient.tsx:69` `const SHOW_SECONDARY_TABS = false;`". The constant is at **`:78`** (and is typed: `const SHOW_SECONDARY_TABS: boolean = false;`); `:69` is `import { PeopleDrawer } …`. Claim and polarity both verified true (`:594-600` filter).
7. **`ai-readiness.md:438-439`** — "(`AIReadinessClient.tsx:137-138`) computes `effectiveCycleId = …` and gates the data GET on `cyclesQ.isFetched` (`:150-154`)". `effectiveCycleId` is assigned at **`:153-154`**; the gate is at **`:169`** and **`:176`** (`enabled: featureOn && cyclesQ.isFetched`). Both claims true, both anchors drifted ~15 lines.
8. **`ai-readiness.md:481`** — "`deriveMode` (`useAIReadiness.ts:48-62`)". There are **two** files named `useAIReadiness.ts`: `deriveMode` is at `apps/web/src/components/ai-readiness/useAIReadiness.ts:48-61`, while this same doc's other citation (`:567`, `interviewQuestions`) points at `apps/web/src/hooks/useAIReadiness.ts:274`. Both line numbers are right for their own file; the bare filename is ambiguous. Qualify the path. (Behaviour verified: `const deadlinePassed = deadline ? … : true`.)
9. **`ai-readiness.md:196-198`** — "reads **exactly one table**, and decodes its `report` JSONB". Strictly the Step-3 insights path also joins `sessions → memberships` via `resolveSessionAuthors` (`how_we_measure.go:880`, `:1093`) — which the doc itself describes two rows later at `:202`. "Exactly one *aggregate source* table" would be exact; as written the two sentences read as contradicting each other.
10. **`ai-readiness.md:270`** — the "**CSV export is now 15 columns**" claim is **correct** (`AIReadinessCSVColumns` has 15 entries; `csv.go:13` says "the 15-column export contract"). Recording it because the platform's own function doc-comment at **`csv.go:35` still says "the 19-column CSV"** — a future auditor grepping for the number will find the stale platform comment first and be tempted to "correct" the corpus in the wrong direction.
11. **`ai-readiness.md:272-273`** — "`aireadiness.Manager` … is constructed with `db`, `ent`, `orgRepo`, `ai.AI`, a pub `EventPublisher`, the `academy.EmbeddingsManager`, and the `WorkforceDirectory`". `orgRepo` is not a constructor parameter; it is derived inside: `NewManager(logger, db, aiClient, entClient, pub, academyEmbeddings, directory)` then `orgRepo: repository.NewEntRepository(entClient).Organization` (`internal/aireadiness/manager.go:95-106`).
12. **`ai_architecture.md:151`** — the two skiller migration ids (`20260417103036`, `20260417120309`) are in the decommissioned `skiller` repo, which is not cloned in either stack workspace; they are **unverifiable locally**, neither confirmed nor refuted. The app-side port that actually creates `job_role_embeddings` / `skill_embeddings` with `small_embedding3 extensions.vector(1536)` + IVFFLAT is `app/terraform/migrations/20260615130000_skiller_taxonomy.sql:50-61, 240-…` — worth naming alongside, since that one *is* checkable.
13. **`clerk-integration.md:99`** — "admin tooling gated on the `STUDIO_ACCESS_ROLES` set — `org:admin` **and** `content_creator`". The real set has **four** entries and accepts both the bare and the prefixed form: `['admin', 'org:admin', 'content_creator', 'org:content_creator']` (`studio-desk/src/index.ts:96`, `studio-desk/app/services/userService.ts:16`). Given that this same doc's headline finding is *bare-vs-prefixed `org_role`*, the compression is unlucky rather than merely imprecise — studio-desk is the one repo that already handles both.
14. **Cross-doc, routed OUT of my file set (my file is the correct one).** `ai_architecture.md:215` — "Both recordings are stored in S3 and linked to the simulation session" — is **TRUE** against source: the Chime media pipeline sinks to S3 (`app/internal/jobsimulation/recording/chime.go:38` `arn:aws:s3:::%s`, `:188-189` `SinkType: MediaPipelineSinkTypeS3Bucket`, `:262-265` the concatenation sink, `:325/341-350` `getMediaFromS3` reading key `concatenated/composited-video/…`), and LiveKit uploads via `EncodedFileOutput_S3` (`calls/livekit.go:178-184`). The doc it collides with is **`corpus/ops/demo/media-substrate-spec.md:33-34`** — *"The bytes live on Bunny's CDN, never in the platform DB and never in prod S3"* — which is false as stated about capture (Bunny is the **serving** layer; `bunny_video_id` is set by `SaveBunnyVideoId` at `chime.go:361` *after* the S3 read). Flagging so the pair is reconciled in the other seat's file rather than "fixed" in mine.
