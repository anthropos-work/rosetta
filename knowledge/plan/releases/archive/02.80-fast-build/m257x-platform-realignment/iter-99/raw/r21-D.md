# Seat D — reading #21

Ground truth re-verified at this reading's open (all match the briefing ADDENDUM):
`app b948604f` (origin/main **`2035f9a`**), `platform 0c91421d` (level with origin/main),
`next-web-app bb3313bc`, `sentinel 88bc5592`, `storage 4ce8ece5` (origin/main `9f8cb532`),
`messenger fa47850d` (origin/main `e9421c68`), `cms ca50c817` (origin/main `f38c0c4a`),
`graphql-wundergraph 60c229f3`, `roadrunner 87d8d443`, `jobsimulation 462343b0` (origin/main `82cb66ec`),
`studio-desk 14a5442a`, `ant-academy 9c3843cd`, nested `app/studio` and `cms/studio` both **`aeec036a`**.

Instrument notes honoured: this shell's `grep` **is** `ugrep --ignore-files` (confirmed:
`type grep` → shell function), so every absence claim below was taken with **both** bare `grep`
and a ref-named `git grep` **per tree**, including the two nested `studio` checkouts at their own
ref (§5 rule 44). Positive controls run in the same passes (e.g. `anthropos-work/colony` in
`app/go.mod` → 1, proving the `authn` zero is a real zero).

> **Instrument hazard found while working, worth recording:** the `CLAUDE.md` text served to me in
> the session's `system-reminder` is a **STALE snapshot** — it still describes `storage-legacy` /
> `messenger` / `customerio-sync` compose profiles and "`core` starts five containers … `storage`
> sits in `profiles: [storage-legacy]`". The **on-disk** `CLAUDE.md` (578 lines) is fully repaired
> and says the opposite. I nearly booked a blocker off the reminder. Grade `CLAUDE.md` by reading
> the file, never by the injected copy.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/services/studio-room.md` | 507 | yes |
| `corpus/architecture/platform-migration-status.md` | 323 | yes |
| `corpus/services/jobsimulation.md` | 309 | yes |
| `corpus/services/sentinel.md` | 166 | yes |
| `corpus/services/askengine.md` | 124 | yes |
| `corpus/architecture/README.md` | 38 | yes |
| **total** | **1467** | |

(`corpus/ops/platform-alignment.md` §5 read in full as instructed — 2,232 lines, rules 1–44.)

---

## Blockers

### B1 — the mirror tables are DROPPED, and six+ corpus docs still mandate seeding them

- **Anchor:** `corpus/services/jobsimulation.md:136` —
  *"**There is one row to seed, not a pair** — the older \"seed the mirror or the scoreboard is
  blank\" guidance is superseded."*
  Supported at `:130-134`: *"the mirrors are GONE … `DROP TABLE`s both `local_jobsimulation_sessions`
  and `local_skill_path_sessions` (`:62-63`) … **No session row is back-filled — the file contains 0
  `INSERT`s**."*

- **Which side is right:** **jobsimulation.md is right.** Ground truth, verified:
  - `app/terraform/migrations/20260729133514.sql` — `:15-23` re-maps the two FKs, `:36-44` NULLs the
    orphans, `:58-61` is the comment *"5. Drop the mirrors."*, and **`:62` `DROP TABLE
    "local_jobsimulation_sessions";` / `:63` `DROP TABLE "local_skill_path_sessions";`**. Read at both
    `b948604` and `2035f9a`.
  - `INSERT INTO` count in that file: **0** (`grep -ciE 'INSERT[[:space:]]+INTO'` → 0). A bare
    `grep -ci insert` returns **1** and it is `:64 DROP FUNCTION IF EXISTS
    on_insert_local_jobsimulation_sessions_update_memberships()` — a substring, not a statement.
  - No later migration recreates them: the only `CREATE TABLE "local_*"` statements in the whole
    migration tree are `20240312152917.sql:46` and `20240527131926.sql:2`, both years earlier.
  - `app` source agrees in words at `2035f9a`:
    `internal/data/ent/schema/org_membership.go:92` *"local_jobsimulation_sessions mirror is gone."*;
    `internal/data/ent/schema/skill_path_session.go:64` *"local_skill_path_sessions mirror was
    dropped"*. Those three comments are the **only** remaining Go references.
  - `internal/organization/intelligence.go:1700` is `m.ent.JobSimulationSession.Query()` — exactly as
    jobsimulation.md:133 says.

- **Why it is a finding:** the corpus asserts the opposite, as a **mandatory seeding landmine**, in at
  least eight live sites the supersession never reached:
  - `corpus/ops/demo/session-clone-spec.md:170` (*"public.local_jobsimulation_sessions ← THE MIRROR"*)
    and **`:175` — *"1. **Co-write the manager MIRROR** (`public.local_jobsimulation_sessions`) or the
    manager scoreboard is blank."*** (listed as landmine #1 of 3)
  - `corpus/ops/seeding-spec.md:388-391` (*"the `HiringFunnelSeeder` writes the **co-written PAIR** …
    **and** its `public.local_jobsimulation_sessions` mirror"*, *"A test RED-proves it"*), `:417`,
    `:529`, `:537`, `:189`, `:197`
  - `corpus/ops/verification.md:248` (*"`local_jobsimulation_sessions` **mirror** row (the score the
    scoreboard reads)"*), `:260` (a `≥ 40` cardinality gate on that table)
  - `corpus/ops/safety.md:211`, `:219` (both tables named in the write-surface allowlists)
  - `corpus/ops/demo/playthroughs.md:1570-1571` (*"Both suites depend on `local_jobsimulation_sessions`
    / `local_skill_path_session` being seeded"*)
  - `corpus/ops/demo/stories-spec.md:70`, `:110`, `:401`, `:410-411`
  - `CLAUDE.md:387` — the root instruction file, the highest-propagation site in the tree:
    *"the **generalized manager-view MIRROR trap** (`local_jobsimulation_sessions` /
    `local_skill_path_session` — seed the mirror or the manager scoreboard is blank)"*

  This is not cosmetic drift: a seeder built from `session-clone-spec.md` or `seeding-spec.md` writes
  `INSERT`s into a table that `DROP TABLE`d two migrations ago. §5 rule 19's exact shape — a claim
  repaired inside the audited scope (`corpus/services/**`) and left standing everywhere outside it,
  including `CLAUDE.md`.

- **Confidence:** high (on the ground truth and on the existence of the contradiction). I hesitated
  only over the anchor: my assigned file is the **correct** side. The instrument says
  *"Self-contradiction counts … say so and cite both anchors"*, so I book it here and name the
  counterparts rather than dropping it because the false half sits outside my partition.

### B2 — `jobsimulation.md` labels `7177374` as `origin/main`; it is `2035f9a`, where the anchor names a different construct

- **Anchor:** `corpus/services/jobsimulation.md:203-204` —
  *"`app` has **no cobra root command**: `app/main.go:216` (@ `origin/main` `7177374`, identical at
  `9d00a313` v1.367.0; `:212` at the older `b948604` v1.366.0) is a plain `func main()`"*

- **Why it is false:**
  - `app`'s **actual** `origin/main` at this reading is **`2035f9a`** (`git log origin/main`, re-checked
    at open). `7177374` is an ancestor (`git merge-base --is-ancestor 7177374 origin/main` → yes) but
    is **six commits back**, past `v1.368.0` and `v1.369.0`.
  - At the real `origin/main`, `main.go:216` is **`if strings.Contains(dsn, "://")`** — not
    `func main()`. `func main()` is at **`:229`** there. So a reader who resolves the label as written
    (`git show origin/main:main.go | sed -n 216p`) gets the wrong construct and concludes the corpus
    anchor is broken. Rule 21's class-2 defect, arriving through a stale ref *label* rather than a
    stale line number.
  - **It contradicts a sibling in this same reading's scope, four times.**
    `corpus/architecture/platform-migration-status.md:87` says *"Anchors re-resolved M257x iter-87 at
    `app` **origin/main `2035f9a`** (post-v1.369.0)"*, and `:89`, `:90`, `:92` each repeat
    *"@ `app` origin/main `2035f9a`"*. Two corpus files name two different shas as the same moving ref.
    Only one can be current, and it is not jobsimulation.md's.
  - `7177374` occurs **nowhere else** in `corpus/` or `CLAUDE.md` (grep, whole tree) — it is an
    orphaned pin nothing else corroborates.

- **What IS true:** `main.go:216` genuinely is `func main()` at `7177374` **and** at `9d00a313`, and
  `:212` at `b948604` — all three verified. Under §5 rule 33 the *sha-named* claim survives; what fails
  is the `origin/main` **label**, which is precisely rule 26's *"an input that can change without
  appearing in a diff"* and rule 33's *"a block that asserts currency cannot be pinned into silence."*

- **Confidence:** medium. I hesitated because the explicit sha makes the anchor gradeable and true at
  its named ref; I book it because (a) the sentence asserts *currency* by naming `origin/main`,
  (b) resolving it as written lands on the wrong construct, and (c) it is a flat cross-file
  contradiction with `platform-migration-status.md`, which the corpus's own cheapest-detection class
  (rule 21) says to report.

### B3 — "`cms` has not moved / sits untouched" contradicts the `cms` row in the same table

- **Anchors (three, two files):**
  - `corpus/architecture/platform-migration-status.md:89` — *"**Do not generalise M810 from this row:**
    `cms` **has not moved**, and is still `cms/terraform/main.tf:39` `service_desired_count = 0`."*
  - `corpus/architecture/platform-migration-status.md:270` — *"while `cms` **sits untouched** at
    `service_desired_count = 0`"*
  - `corpus/services/jobsimulation.md:12` — *"**Do not generalise this to `cms`**, which **has not
    moved** (`cms/terraform/main.tf:39` `service_desired_count = 0`)."*

- **What the same table says 1 row above:** `platform-migration-status.md:88` —
  *"**M257x iter-92 — cms has since taken an M810 step, and it points the OTHER way:** `6efa1d5`
  (merged `f38c0c4`, 2026-08-04) **deleted** `.github/workflows/build-production.yml` … So this repo
  now holds **two measured facts pointing opposite ways** … **report both, assert neither**."*

- **Ground truth (both halves confirmed):**
  - `cms/terraform/main.tf:39` @ `f38c0c4a` is indeed `service_desired_count = 0`.
  - `cms` `6efa1d5` (2026-08-04) is real, subject *"chore(ci): drop build-production — the cms ECR
    repository is decommissioned (M810)"*, `--stat` = `.github/workflows/build-production.yml | 18
    ------` (file deleted), body quotes verbatim as the row says.
  - So **cms HAS moved** — just in CI, not in terraform.

- **Why it is a blocker:** row 88 explicitly instructs *report both, assert neither*, and rows 89/270
  plus `jobsimulation.md:12` then **assert one side flatly**, in the imperative voice
  (*"Do not generalise…"*), which is the voice a reader acts on. This is §5 rule 19's half-repair:
  iter-92 corrected the `cms` row and left its three twins — two of them in the same file, one file
  away — restating what it had just retracted. A reader who reaches rows 89/270 first walks away
  believing cms is entirely untouched by M810.

- **Confidence:** medium. I hesitated because each offending sentence is immediately followed by a
  terraform citation that scopes it, so a very careful reader can reconcile them; I book it because
  the flat clause *"cms has not moved"* is exactly what row 88 spent a paragraph refusing to say, and
  because the corpus repeats it in a second file where row 88's qualification is not present at all.

---

## Minors

- `corpus/services/sentinel.md:44` — *"only as a fixture string in sentinel's own tests
  (`internal/authorization/casbin_test.go`, `internal/rpcsrv/rpc_test.go`)"* names 2 of **3** test
  files: `internal/authorization/manager_test.go:21` also does `manager := "manager"` and uses it as a
  `g2` role in `newTestEnforcer` (`:30`, `:37`). Omitted list member; the predicate (*"only a test
  fixture"*) still holds, and `init_policy.sql` really does contain **zero** occurrences of `manager`
  (verified: `git grep -n manager 88bc5592 -- init_policy.sql` → exit 1). Note `manager.go:20` also
  holds the literal, but as a **logger label** (`logger.With("service", "manager")`), not a role.
- `corpus/services/sentinel.md:5` — *"`messenger` is not a caller — **its compose block** sets no
  `AUTHORIZATION_ADDRESS` and declares no `depends_on: sentinel`"*: present tense about a block that
  `838d907` deleted; `sentinel.md:89` states the current fact correctly in the same file. The
  conclusion is right and independently proven (messenger's Go source imports no authorization client
  at all — `git grep -i authorization e9421c68 -- '*.go'` → 0 hits, positive control passes).
  Stale-but-harmless phrasing.
- `corpus/services/askengine.md:111-112` — the handler-package test list (*"sanitize, stream_registry,
  examples, lessons, distill, feedback, store, embed"*) names 8 of the **9** `_test.go` files in
  `internal/web/backend/ask/`; `coursebuilder_tool_test.go` is omitted.
- `corpus/services/jobsimulation.md:25-27` — three lines lose the `> ` blockquote prefix mid-paragraph
  (lines 24 and 28 have it). Renders correctly via CommonMark lazy continuation; structurally
  inconsistent only.
- `corpus/services/sentinel.md:151` — the env-var table gives `PORT` a **Default** of `8087` while the
  same row's description says *"binary default is 8080"*. `8087` is compose's explicit value, not a
  default; the binary's default is `cmp.Or(os.Getenv("PORT"), "8080")` at `sentinel/cmd/root.go:47`.
  The prose at `:15` gets it right.

---

## Positively cleared (audited zeros)

Labelled per the briefing. **ENUMERATED** = I enumerated the predicate from source and compared
cardinalities; **RE-DERIVED** = I only re-computed arithmetic the document displays. Only ENUMERATED
is a clearance.

### `platform-migration-status.md`

- **ENUMERATED** — *"14 names ever in `repos.yml`"* (§2). `git log -p --follow -- repos.yml | grep
  '^[+-]  - name:' | sort -u` → exactly **app · chronos · cms · graphql-wundergraph · intelligence ·
  jobsimulation · messenger · next-web-app · roadrunner · sentinel · skiller · skillpath · storage ·
  studio-desk**. All 14 have rows.
- **ENUMERATED** — *"26 names ever in `docker-compose.yml`"*, incl. `nats`, `web-app`, `chromedp`,
  `simulator`, `realtime`. Same command → 26 exactly (`app-network` is included by the naive command;
  the other 25 all map to rows, with `backend`→`app`, `graphql`/`wundergraph`→`graphql-wundergraph`,
  both stated in their rows).
- **ENUMERATED** — `repos.yml` cardinality over time: **9** @ `2adcf71` → **6** @ `d11a403`/`0dab54d`
  → **4** @ `0c91421` (app · sentinel · next-web-app · studio-desk), a **28-line** file. Every cited
  line range resolves: `:2-10` header, `:9-10` @ `0dab54d` (*"The prod rollback path lives in
  infrastructure's services.tf until M810"* — verbatim), `:12-13`, `:14-17`, `:18-20`, `:23-25`,
  `:26-28`.
- **ENUMERATED** — compose service-block counts: **11** @ `2adcf71` → **8** @ `d11a403` → **5** @
  `0c91421` (sentinel · backend · studio-desk · next-web-app · gotenberg). `docker-compose.yml` is
  **186** lines, down from **271** @ `0dab54d`; `838d907` is **+11/−96** on that file (`--numstat`).
- **ENUMERATED** — all 22 `docker-compose.yml` citations resolve into their claimed blocks; I read
  `:5`, `:18`, `:28`, `:48`, `:59`, `:73-75`, `:82`, `:83`, `:84-92`, `:102-103`, `:112`, `:119`,
  `:135`, `:141`, `:143`, `:151`, `:160`, `:168`, `:170-171`, `:183`, `:1-2` and `common.yml:2`,
  `:22`, `:24` directly. **I also ran the fence itself** (read-only; the guard contains no write
  path — `grep "open(.*'w'|write_text|json.dump"` → 0):
  `python3 .agentspace/rosetta-extensions/stack-core/platform_alignment_guard.py
  corpus/architecture/platform-migration-status.md stack-demo/platform/repos.yml`
  → `assertion F resolved 92 citation(s) — 22 subject-checked, 69 range-only, 1 outside any service
  block; 0 unresolvable; 0 read from the WORKTREE` … `OK`, **exit 0**. Byte-identical to the block
  quoted at `:220-222`, and 22+69+1 = 92 matches §4's fenced/prose table.
- **ENUMERATED** — the `storage` row's `STORAGE_RPC_ADDR` derivation, the highest-risk displayed
  arithmetic in the file, checked as a **set** not a sum (§5 rule 24): at `2035f9a`, `git grep -n
  STORAGE_RPC_ADDR -- '*.go'` → **3 hits, all comments** (`main.go:504`, `jobsimwiring/wiring.go:101`,
  `storagens/callsites_test.go:189`). At `b948604` → **15**; I classified all fifteen by hand:
  **7 env lookups** (`main.go` ×3 at `:446`, `:524`, `:992`; `wiring.go:115`; one each in
  `cmd/academy-asset-upload/main.go:129`, `cmd/academyImport/main.go:231`, `cmd/import/main.go:50`) +
  **2 doc comments** (`:28`, `:35`) + **2 error strings** (`:133`, `:235`) + **3 `t.Setenv`**
  (`main_test.go:41`, `:52`, `:64`) + **1 `t.Fatal` message** (`:47`) = 15. And the fine distinction
  the cell makes is right: 6 of the 7 are `os.Getenv`, the 7th (`wiring.go:115`) is a lowercase
  `getenv` helper. Config side: `STORAGE_RPC_ADDR` = **0** across `docker-compose.yml`, `common.yml`
  and `.env_example` @ `0c91421` (all three files exist; `.env_example` is 108 lines — a real zero,
  not a missing file).
- **ENUMERATED** — the `storage` prod cell. `storage/terraform/main.tf` @ `9f8cb532` is **18 lines**,
  declares no service, `:9-11` and `:13-16` and `:18` quote verbatim. `custody` → **0** hits in the
  whole repo (exit 1). **0** `moved` blocks. `storage.tf:22-25` carries the M903 supersession verbatim.
  `d3e6d32` (2026-08-05) is real and its body says *"M903 never ran."* The retracted `:38`
  `service_desired_count = 0` is indeed past EOF in an 18-line file.
- **ENUMERATED** — every `app/main.go` wiring anchor in the `app`/`storage`/`messenger`/
  `customerio-sync` rows, read at the named ref `2035f9a`: `:395` `customeriosync.New`, `:504` (the
  comment), `:516`/`:517` env reads, `:518-523` the fail-closed guard, `:524`/`:525` the two managers,
  `:547` the resource manager, `:690` `skiller.NewSkillerManager`, `:721` `jobsimwiring.Wire`, `:751`
  `skillpath.NewSessionManager`, `:1102` `cmsStorage`, `:1153` `appcms.Wire`, `:1416-1421` the
  consumer-group literal, `:1450` the second subscriber server, `:1471` `msgadapters.Wire`, `:1473`
  `msgsender.NewFromEnv`, `:285`/`:286` the two switches, `:394`/`:1445`/`:1552` their reads. Plus
  `env_guards.go:92-111` (`resolveSubsystemSwitch`), `internal/storage/service.go:22`/`:24` (the
  env-var **names**, as the row correctly insists), `jobsimwiring/wiring.go:101`/`:123`,
  `terraform/main.tf:181`/`:415-416`/`:419-420`. **All 7 domain dirs exist under `app/internal/`;
  `app/internal/roadrunner/` does NOT** (0 tracked files) — the row's claim.
- **ENUMERATED** — every other-repo terraform anchor: `cms:39`=0, `sentinel:19`=1, `messenger:29`=0 in
  a **121-line** module with `:19-25` the rationale and `:27-28` the rollback note, `roadrunner:19`=1,
  `graphql-wundergraph:20`=1, `jobsimulation` @ `82cb66e` `:15-22` + `:24-40`.
- **ENUMERATED** — the `roadrunner` provenance correction. `git blame -L 19,19 87d8d443 --
  terraform/main.tf` → **`84a4b4f1` (2025-12-15)**, the oldest commit touching the file; `e45eb61`
  (2026-05-27) is the file's most recent touch and is **1 insertion / 1 deletion**. Repo HEAD
  `87d8d44` is dated **2026-06-19**. The row's *"a file-level `git log` is not line provenance"* is
  demonstrated, not asserted.
- **ENUMERATED** — the `jobsimulation` four-commits-on-2026-08-04 set: `6092c6d2`, `caf36c96`
  (**committer `GitHub`**, *"Merge pull request #439"*), `1e40d184`, `82cb66ec` (v0.254.0) — all four
  dated 2026-08-04, exactly as listed.
- **ENUMERATED** — the `graphql-wundergraph` supergraph cell: `supergraph-config-prod.yaml` lists
  `backend` alone (its own comment says *"supergraph 2→1"*), `schemas/` holds `backend.graphqls`
  alone, `subgraphs.conf` is the single line `BACKEND=v1.360.0`, and `915da06` (2026-07-29) is the
  fold commit.
- **ENUMERATED** — every historical platform commit in the archived rows, by sha **and** date:
  `a2a3ee6` 2026-02-27 (and it really does remove `directus:` from compose — 1 block before, 0 after),
  `cb6ebf5` 2023-04-30 "First commit", `ef4b449` 2024-09-02, `1474b1f` 2023-12-01, `84862d1`
  2024-05-29 *"replace simulator with jobsimulations"*, `b43b99a` 2026-02-26, `c17cc9a` 2026-04-15,
  `467965a` 2023-09-11, `8770fe6` 2023-05-04 (**four days** after the first commit), `045857c` and
  `fdfa189` 2026-04-17, `21429b7` 2026-07-07, `a4db680` 2026-07-21, `236771f` 2026-07-29, `6060315`
  2026-08-03 (**touches `common.yml` only, +4 lines** — the `start_period: 120s` at `common.yml:22`).
- **ENUMERATED** — §1's state vocabulary is exactly **eight** tokens, and **no row's state column
  reads `mid-fold`** (storage's `mid-fold` text is historical narrative inside the evidence cell, and
  the row itself is `merged-into-app / decommissioned`), matching the §1 note.

### `jobsimulation.md`

- **ENUMERATED** — *"the 23 session/run tables"*: `20260722081626_jobsim_data_model.sql` has exactly
  **23** `^CREATE TABLE` statements, and every table the doc names in its abbreviated list
  (`sessions`, `actors`, `interactions`, `validation_*`, `anticheat_*`, `recordings`,
  `chime_recordings`, `code_submissions`) is among them.
- **ENUMERATED** — the headline-rename claim: `20260722104506.sql:2` `CREATE TABLE
  "job_simulation_sessions"` and `:79` `DROP TABLE "sessions";`. `public.sessions` really does not
  survive.
- **ENUMERATED** — the ports correction. `jobsimulation/cmd/root.go:77`
  `cmp.Or(os.Getenv("PORT"), "8080")` / `:78` `…("RPC_PORT"), "8081"`; both `Dockerfile:34` and
  `Dockerfile.dev:35` are `ENTRYPOINT ["./application"]` with no `CMD`; the in-repo `CLAUDE.md:124-125`
  documents `:8080`/`:8081`. The 8400/8401 pair is compose-supplied **at `2adcf71` only** —
  `:113` `PORT=8400`, `:119` `RPC_PORT=8401`, `:93-94` the published pair — and there is no
  `jobsimulation` service at `0c91421`.
- **ENUMERATED** — the cobra contract: exactly **four** `rootCmd.AddCommand` sites, with `Use:` strings
  `aggregate` / `clone-session` / `test-command` / `validate`; the root's own `Use:` is `jobimulation`
  (sic) and its `RunE` is the server. The repo's `CLAUDE.md:88` really does document the nonexistent
  `go run . serve`.
- **ENUMERATED** — the `$HOME/.aws/credentials` cell, at its named ref `0dab54d`: `:91` is the bind,
  under `volumes:` at `:90`, comment at `:88-89` quoted verbatim, and it is the **only** line matching
  `aws` in the whole compose file. Consequence chain verified: `jsai.NewAIManager` →
  `internal/jobsimulation/ai/ai.go:90` (`can't load AWS config: %w`) → returned unwrapped at
  `jobsimwiring/wiring.go:147-148` → `log.Fatalf` at `main.go:670` @ `9d00a313` (`:614` @ `b948604`).
- **ENUMERATED** — the rext mitigation: `DIRECTUS_DATA_CONSUMERS = ("cms", "backend")` at
  `stack-injection/gen_injected_override.py:86`; demo path emits `volumes: !reset null` (`:652`), dev
  path sets `entry["volumes"] = []` emitted with `!override` (`stack-core/gen_override.py:149-155`) —
  both exactly as described, including *"a bare `volumes: []` does NOT remove it"*.
- **ENUMERATED** — the result-page read model: `jobsimulation/internal/graph/queries.resolvers.go:70`
  is `func (r *queryResolver) JobSimulationResult(...)`, a plain Ent read path.
- **ENUMERATED** — worker pools `Concurrency: 10` (`internal/worker/worker.go:27`) and `25` (`:104`);
  `internal/runner/runner.go:1-3` header reads *"formerly the standalone \"roadrunner\" service"*;
  `internal/calls/livekit.go:131-135` reads `flag_use_realtime_openai` and `:140-144` sets
  `anthropos-agent` + `openai-hosted`, overriding a `-us` name exactly as described;
  `cms/internal/directus/collections/jobsimulation.go:1594-1597` defaults a nil `voice_engine` to
  `Gptrealtime`; `09631fb2` (2026-04-17) and PR **#395** `feat/remove-chronos-and-realtime` are real;
  migrations `20260402145459` and `20260409131539_add_summary_to_extraction_results` both exist and
  both name `interview_extraction_results`; the five `internal/graph/schemas/*.graphqls` files are
  `activites` (misspelled **in the source**, faithfully reproduced), `mutations`, `queries`, `schema`,
  `subscriptions`; `cmd/root.go:284-285` subscribes to its own stream **and** `CMS_STREAM`.
- **ENUMERATED** — *"five services … seven effective"* @ `0c91421`: 5 in `docker-compose.yml`, +2
  (`postgresql`, `redis`) from the included `common.yml`.

### `sentinel.md`

- **ENUMERATED** — the Casbin model, counted from `internal/authorization/casbin.go:13-45`:
  **6** request definitions (`r`…`r6`), **6** policy definitions (`p`…`p6`), **3** role definitions
  (`g`, `g2`, `g3`), **6** matchers (`m`…`m6`). Every per-matcher description checks out, including
  the fiddly ones: `m`'s free-tier disjunct is substituted from the proto enum
  (`authorizationv1.Tier_TIER_FREE.String()`), and `m6` is `g3(p6.org, r6.sub)` with no tier term.
- **ENUMERATED** — the RPC table: all **19** listed methods exist as `RPCHandler` methods in
  `internal/rpcsrv/rpc.go`, and there are no unlisted ones.
- **ENUMERATED** — `AUTHORIZATION_ADDRESS` occurs **exactly once** across `docker-compose.yml`,
  `common.yml` and `.env_example` — `docker-compose.yml:48`, backend's block — at **both** `0dab54d`
  (the `:5` pin) and `0c91421` (the `:85` pin). `*_RPC_ADDR` count in compose @ `0c91421`: **0**.
  Cross-checked the departure story: at `0dab54d` all four `*_RPC_ADDR` (`:173`, `:174`, `:176`,
  `:183`) sat in **messenger's** block, all four `=http://backend:8083`, and `messenger/cmd/root.go`
  reads all four at `:120`, `:125`/`:130`, `:135`, `:140`.
- **ENUMERATED** — `go.mod:3` `go 1.26.0`; `Dockerfile:2` and `Dockerfile.dev:2` `golang:1.26-bookworm`;
  `terraform/locals.tf:4-5` `service_cpu = 256` / `service_memory = 128` (and `:3 port = 8080`);
  `init_policy.sql` creates exactly **one** table, schema-qualified `sentinel.casbin_rules`; `:63-66`
  is the `taxonomy:write` omission note; `:88-118` is the `content_creator` block; `Makefile:3`
  hard-codes `postgresql://postgres@localhost:5432/postgres?sslmode=disable` and ignores
  `DB_CONNECTION`; `local_superadmin_grants.sql` grants p3 `taxonomy:write` to `admin` at `:26` and
  keeps the p4 block commented at `:37-42`. `app/internal/data/ent/enum/membership.go:8-15` holds
  exactly the four `MembershipRole` values.

### `askengine.md`

- **ENUMERATED** — the registry, checked as a **set**: `registry.go`'s `defs` slice holds **60**
  `TableDef` literals, **57** `Schema: "public"` + **3** `Schema: "directus"`, and **none is commented
  out** (§5 rule 24's `grep -c` trap explicitly checked: zero `//`-prefixed `{Name:` or `Schema:`
  lines). `:183-195` is the jobsimulation transition-alias comment, and *"`jobsimulation` stays in
  `rewriteSchemas` until M710"* is its own words. The `skiller.*`/`skillpath.*` aliases are added
  **after** the slice (`:551`, `:572`), so they correctly sit outside the 60.
- **ENUMERATED** — every constant: `maxAgenticIterations = 15` and `loopTimeout = 10 * time.Minute`
  (`web/backend/ask/handler.go:34`, `:41`), `askEngineMaxConns = 6` (`main.go:150`, applied at `:336`),
  `MaxInlineRows = 200` / `MaxCellLength = 400` / `QueryTimeout = 10 * time.Second`
  (`executor.go:18`, `:21`, `:24`, applied as `SET LOCAL statement_timeout` at `:97`),
  `DefaultModelID = "eu.anthropic.claude-sonnet-4-6"` / `DefaultRegion = "eu-west-1"`
  (`bedrock.go:25-26`), `Temperature: param.NewOpt(float64(0))` (`:315`, `:405`),
  `stripAnthropicAuthMiddleware` (`:252`, installed `:185`, `:228`). `rules.md` is **146,219 bytes** —
  "~146 KB" is exact.
- **ENUMERATED** — the route list. `internal/web/backend/backend.go:171-188` mounts the `/ask` group
  with CORS + Clerk authn and the comment *"sibling group of /api that skips OpenAPI validation"*, and
  every one of the 11 routes the doc names is there, with no extras.
- **ENUMERATED** — the test counts, by `grep -c '^func Test'`: sandbox **49**, executor **13**, prompt
  **10**, followups **14**. Exact.
- **ENUMERATED** — `ValidateSQL` (`sandbox.go:163-185`) runs precisely the six checks the doc lists, in
  that order; `$1`=orgID / `$2`=callerUserID self-scope for `profile_histories` is real
  (`queryConfig`, `WithAccess`, `registry.go:118`); the five `ask_*` tables all exist
  (`20260505133528.sql` ×2, `20260506145258.sql`, `20260518125439.sql` ×2); `v1.267.0` = 2026-05-07 and
  `v1.340.0` = 2026-07-17, both exact.
- **Worth flagging as a NON-finding, because it looks like one:** the doc says `WrapQuery` *"defends
  **four** documented bypass vectors"* while the source comment at `sandbox.go:350` says *"three
  sandbox-bypass vectors"* — and then **lists four bullets** (`:352-362`). The corpus counted the
  bullets; the platform's own prose is the stale number. The corpus is right.

### `studio-room.md`

- **ENUMERATED** — *"`gen.py` registers **exactly nine** arguments (`gen.py:484-492`)"*: lines 484–492
  are nine consecutive `parser.add_argument` calls and there are no others; the nine flags, defaults
  and `dest="forced"` all match the doc's block verbatim.
- **ENUMERATED** — the `--template` warning, the file's most load-bearing passage, checked end to end:
  `parse_argument` is `gen.py:18-28` and does fold unknown `--k v` pairs into the dict via
  `parse_known_args`; `translate_legacy_blueprint` + its defaults table is `:205-238` with exactly the
  **three** legacy names (`micro challenge`, `scenario challenge`, `collaborative challenge`) and the
  *"is ignored; asset type is now inferred from task interactions"* branch for everything else;
  `validate_blueprint_exclusivity` is `:241-271` and its allowlist is exactly the doc's
  `{blueprint, media, forced, interactive, branch, pipeline, + the four *_path}`; the error string
  matches. **`template` has zero consumers**: its only reads anywhere in the tree are inside the
  legacy-strip function and `tests/unit/test_asset_type.py`.
- **ENUMERATED** — the "mistral" block, using all three instruments as the addendum demands:
  `git grep -i mistral aeec036a` (nested repo's own ref) → **22 hits / 3 files**
  (`requirements.txt:8` — line 8 exactly, `tools/pdf2md.py` ×18, `tools/r3.py` ×3);
  bare recursive `grep -ri mistral stack-demo/app/studio/` → **1** (the false clearance, reproduced);
  `git -C stack-demo/app grep -i mistral b948604 -- studio/` → **0** (exit 1). `pdf2md.py:24`
  `from mistralai import Mistral`, `:96` `Mistral(api_key=…)`, `:127` `model="mistral-ocr-latest"`,
  `r3.py:194` the `MISTRAL_API_KEY` hard requirement — all four anchors land. `app/studio/.gitignore:2`
  = `tools/`; `app/.gitignore:79` = `studio/*`; `cms/.gitignore:129` = `studio/*`. The registry at
  `services/ai.py:705-708` is exactly `{openai, azure, anthropic}` and `ai.py:1-2` imports only
  `openai`/`anthropic`. Go execs exactly **two** studio scripts: `studio/gen.py`
  (`studioManager.go:119`) and `studio/postgen.py` (`:1045`). `app/Dockerfile:45-46` copies the tree and
  pip-installs the file. And `platform-alignment.md` §5 **rule 44 exists** (`:1224`) and is the right
  rule.
- **ENUMERATED** — the runtime/queue cell: `Dockerfile:28` `FROM python:3.11-slim`;
  `internal/cms/worker/worker.go:29` `Concurrency: 5` with `Queues: {AiVideoQueue: 7, StudioQueue: 3}`
  (`:32-33`) — priorities, not limits, exactly as the doc insists;
  `studioManager.go:94-96` the venv constants.
- **ENUMERATED** — the output-structure claim, which is a negative and therefore the risky one:
  `gen.py:450-455` builds the four `work_paths` from the config keys with the bare-name fallback;
  `exporter.py:514-550` is `_create_export_package`, writing the two zips at `:518-519`, writing
  `simulation.json` at `:541`, preserving `collaborative_*`/`asset_*`/`internal_*` JSON at `:537`, and
  `rmtree`ing the scratch dir at `:550`; the zip is named for `source_id` falling back to `sim_id`
  (`:517`). **No post-gen target drops its own file**: `run_postgen` (`postgen.py:363-373`) exports via
  `SimulationExporter`, **not** via `SimulationPostGenerator.export` — so the
  `{simid}_postgen_results.json` that method would write is unreachable dead code, and the doc's
  enumeration of on-disk artifacts holds.
- **ENUMERATED** — post-gen targets: the four selectable ones are exactly `toolkit`, `guidance`,
  `metadata`, `translation` (`agents/simulation/postgen/__init__.py::_get_generation_plan`); a
  `testing.py` module exists and is **not** selectable; `exporter` is not a target. `postgen.py:396-398`
  makes `--media`, `--simid`, `--target` all `required=True`.
- **ENUMERATED** — *"the only cache in the pipeline"*: every non-`tools/` `cache` occurrence in the
  whole studio tree is `model.py:59`, `:467`, `:468`, `:469` — the taxonomy memo. Nothing else.
- **ENUMERATED** — configs: `max_tokens = 4000` in **all three** tracked configs;
  `worklog_path = workspace/trace` in all three; the `[SERVICES]` key names + `*_ENDPOINT` pairs exist
  in `config_template.ini`; `configs/local_*` and `configs/test_*` are gitignored (`.gitignore:5-6`);
  the env override is code, not interpolation (`gen.py:41-53`, a six-key `secrets_keys` loop);
  `ENVIRONMENT` defaults to `local` (`:31`).
- **ENUMERATED** — the blueprint section: `tests/e2e/blueprints/technical.json` carries **every** key
  the doc's JSON block shows, with the same values (I diffed by eye against the real file); the worked
  examples are exactly `b2b · business · none_chat · none_voice · technical` and
  `code · micro · scenario`; `CLAUDE.md:12-14` is the fenced entry-point block quoted verbatim;
  `services/taxonomy.py:11` `BASE_URL = "https://api.anthropos.work/api"`; `--evaluation_skills` is
  really consumed by prep (`agents/simulation/prep.py:75`, `:1032`).
- **ENUMERATED** — the project-structure listing matches `git ls-tree` for the repo root,
  `agents/simulation/`, `services/`, `postgen/` and `tools/` (`any2pdf.py`, `md2cleanMd.py`,
  `pdf2md.py`, `r3.py`).
- **ENUMERATED** — `GenMode` is exactly the five listed values with `DEFAULT = EXECUTION`
  (`services/ai.py:35-42`); the three provider clients are at `ai.py:383`, `:530`, `:664` as cited;
  `usage.checkpoint()` / `usage.get_report()` / the module-level `usage` singleton all exist
  (`:140`, `:183`, `:229`); the `@generation_step` sync-or-async claim is `gen.py:284-289`
  (`inspect.isawaitable`).
- **ENUMERATED** — the merge dating: `v1.360.1` is exactly *"fix(ci): pull studio via additional_repo
  like cms"* (2026-07-29), so the doc's `(additional_repo, app v1.360.1)` is the right pin; the `cms`
  compose service was removed at `d11a403` (present at `360efd4`, absent at `d11a403`).

### `architecture/README.md`

- **ENUMERATED** — the index is **complete and exact**: `ls corpus/architecture/` returns 10 non-README
  files and all 10 have entries; no entry points at a file that does not exist.
- **ENUMERATED** — *"`authn` is not a dependency of any service"*: `git grep -c
  'anthropos-work/authn' <HEAD> -- go.mod go.sum` across **all seven** Go clones (`app`, `sentinel`,
  `storage`, `messenger`, `cms`, `jobsimulation`, `roadrunner`) → **0 everywhere**, with the positive
  control (`anthropos-work/colony` in `app/go.mod` → 1) passing in the same pass. `colony/authn` is
  imported in Go source. `app/go.mod` requires `ai`, `colony`, `proto`, `taxonomy` — the four claimed.

---

## What I could not settle

- **GitHub archive states.** Six rows assert or deny archive status (`graphql-wundergraph` ARCHIVED
  2026-07-30, `skiller` 2026-07-01, `skillpath` 2026-07-31, `intelligence` 2026-04-02, `chronos` NOT
  archived, `roadrunner`/`storage`/`messenger`/`cms` NOT archived). Archive state lives only in the
  GitHub org API and is invisible to a clone — the `jobsimulation` row says so explicitly and models
  the right behaviour. Settled by `gh api repos/anthropos-work/<r> --jq .archived`, which I have no
  network for.
- **`platform-migration-status.md:126` — "93 repositories … 46 named by no corpus document"** and the
  §3 census's per-repo last-push dates. Org-API facts; the `4 in repos.yml` half is ENUMERATED above.
- **`platform-migration-status.md:110` — "≥42,790 skills / ≥22,470 job roles"** (and the refutation of
  60K/18K). Needs a live DB read.
- **`jobsimulation.md:50` — "Production terraform still names `http://backend.internal.anthropos:8081`."**
  Not verifiable from the clone set, and stated more flatly than the evidence supports: the only
  terraform in the clone set that carries the variable is `messenger/terraform/main.tf:92-93`, whose
  value is `${var.jobsimulation_rpc_address}` with **no default** (`variables.tf:92-95`). The literal
  would live in `infrastructure/terraform/production/services.tf`, which the map itself repeatedly
  notes *"has never been in the clone set"*. The nearest corroboration I found is a secondary source
  in another repo — `app/knowledge/service-dependencies.md:46`, which names that address for the
  **skiller** surface, not jobsimulation's. I did not book it because it is unfalsifiable here, but it
  is the one sentence in my set that asserts prod terraform content the corpus cannot see, and
  `jobsimulation.md:74-75` two dozen lines later says exactly that limitation out loud — which makes
  the flat assertion at `:50` the odd one out. Worth a routed re-check.
- **`platform-migration-status.md:92`'s `app/knowledge/plan/releases/09.00-support-in-app/m903-s3-custody/`
  front-matter** (`status: planned`, *"Planned, not started"*). I deliberately did **not** open it:
  HARD BAR 1 forbids reading anything under `knowledge/plan/**`, and although that path is in a
  different repository and holds no audit answer key, the bar is written without qualification and I
  chose the conservative reading. Everything else in that sentence — that M903 never ran, that the
  storage repo supersedes it in `storage.tf:22-25`, and that `d3e6d32` retired it saying *"M903 never
  ran"* — is ENUMERATED above from the storage clone.
- **`sentinel.md:97`** — *"sentinel crash-loops with `pq: no schema has been selected`"* without the
  `sentinel` schema. A runtime behaviour; needs a stack, not a clone.
- **`sentinel.md:45-46`** — the live-stack query
  `select distinct v2 from sentinel.casbin_rules where p_type='g2'` returning `admin`/`member`/
  `candidate` only. I confirmed the static half (`init_policy.sql` seeds all four roles including
  `content_creator`, and never `manager`), but the DB half needs a running stack — and note the static
  seed contains `content_creator` p2/g2-adjacent rows, so a freshly-seeded stack plausibly returns
  four values, not three. Flagging as unsettled rather than booking it, since `v2` in a `g2` row is
  the **user**, not the role, and I could not confirm the column semantics without the DB.
