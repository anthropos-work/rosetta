# Seat E — reading #21

**Refs read (stated at the moment of measurement, §5 rule 26).** Corpus at working-tree HEAD
`1937e1f` (branch `m257x/platform-realignment`). Ground truth re-verified at this reading's open, all
matching the briefing ADDENDUM: `app` **b948604f**, `app/studio` **aeec036a** (its own checkout),
`platform` **0c91421d**, `next-web-app` **bb3313bc**, `sentinel` **88bc5592**, `storage` **4ce8ece5**,
`messenger` **fa47850d**, `cms` **ca50c817**, `graphql-wundergraph` **60c229f3**, `roadrunner`
**87d8d443**, `jobsimulation` **462343b0**, `studio-desk` **14a5442a**, `ant-academy` **9c3843cd**,
`.agentspace/rosetta-extensions` **5fb0915** (authoring copy, `main`).

**Instruments.** `git grep <ref>` per tree (never a bare tree-wide grep — this shell's `grep` is
`ugrep --ignore-files` and hides tracked-but-ignored files), plus bare `grep`/`awk` for line-exact
anchor resolution. Every absence below was taken with a positive control **in the same pass**; the
controls are named inline. `app/studio` was grepped **at its own ref `aeec036a`**, never at `app`'s.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/architecture/service_taxonomy.md` | **494** | yes |
| `corpus/services/clerkenstein.md` | **370** | yes |
| `corpus/architecture/security_compliance.md` | **281** | yes |
| `corpus/services/ai-labs.md` | **156** | yes |
| `corpus/services/clerk-integration.md` | **128** | yes |
| `corpus/services/db-backup.md` | **31** | yes |
| *(total)* | **1460** | — |

---

## Blockers

### B1 — The multi-tenancy fence's closing sentence mischaracterises the remainder: at least FOUR unpoliced schemas ARE org-scoped, via `tenant_eid` / `tenant_id`

- **Anchor:** `corpus/architecture/security_compliance.md:104-105` —
  > "The genuine remainder — global reference data with no `organization_id` at all — is what carries no org
  > column by design."

- **Why it is false.** The whole derivation in this blockquote is keyed on the **literal string
  `"organization_id"`** (the shipped command at `:126-127` is `xargs grep -l '"organization_id"'`). I
  enumerated the tenancy columns actually declared in `app/internal/data/ent/schema/` at `b948604f`:

  | column | field declarations | files |
  |---|---|---|
  | `organization_id` | 45 (44 `field.UUID`, 1 `field.String`) | the set the fence measures |
  | `tenant_eid` | 3 | `lab.go:63`, `academy_chapter.go:84`, `academy_skill_path.go:78` |
  | `tenant_id` | 3 | `mixin.go:152` (`TenantMixin`, **0 users** — verified), `job_simulation_session.go:35`, `skill_path_session.go:43` |
  | `organization` (String) | 1 | `user_volunteer.go:28` — free-text volunteer employer, correctly not tenancy |

  Four schemas sit in the *remainder* while carrying a live tenancy column and **no ORM policy**:

  - `lab.go` — `field.String("tenant_eid")` at `:63`, under the comment at `:60-62`: *"NULL/empty =
    public; non-null = scoped to that org's eid. Fail-closed match + hard-404 live in the **manager**;
    this is the stored scope only."* Mixins at `:37-39` are `PrimaryKeyMixin`/`CreatedAtMixin`/
    `UpdatedAtMixin` — neither org mixin — and it declares no `Policy()`.
  - `academy_chapter.go` — `tenant_eid` at `:84`, same three mixins at `:37-39`, no `Policy()`.
  - `academy_skill_path.go` — `tenant_eid` at `:78`, same three mixins at `:36-38`, no `Policy()`;
    the comment at `:75-77` says it *"scopes the path (and, by inheritance, its chapters) to one"* tenant.
  - `skill_path_session.go` — `field.UUID("tenant_id")` at `:43`, indexed at `:74`; mixins are
    `PrimaryKeyMixin`/`CreatedAt`/`UpdatedAt`/`UserMixin`/`SkillPathMixin` — `UserMixin` filters by
    **owner**, not organization (`mixin.go:99-116`, `rule.FilterOwnerRule()`), and this doc says so
    itself at `:72-74`.

  None of the four can declare its own `Policy()`, because I independently enumerated that **exactly four
  files in the directory declare any `Policy()` at all** — `mixin.go` (`:99`, `:126`), `organization.go`
  (`:56`), `user.go` (`:116`), `org_membership.go` (`:172`) — which is the same fact the document asserts
  at `:109-111`. So these four are org-scoped tables filtered **only by application code**, and the
  sentence places them in the "global reference data … by design" bucket.

- **Corpus self-contradiction, from inside my own partition.** `corpus/services/ai-labs.md:50` describes
  the very same table as *"`labs` (Ent `lab.go` — metadata: `slug`, **`tenant_eid` nullable = org-scoped
  else public**…)"*, and `:46-47` records that its isolation is *"**fail-closed** tenant-filtered reads"*
  in `ContentManager` — i.e. exactly the `:136-137` category ("*elsewhere isolation is enforced by Layer 2
  … and by explicit query scoping, not by the ORM*") that the `:104-105` sentence closes off.

- **Why this matters more than a wording nit.** The blockquote's own §"⚠️ This fence has now been wrong
  FOUR times" (`:114-119`) records that three of the four prior failures failed toward *"isolation is
  handled"*. This one fails in the same direction: it tells a reader that everything outside the 31+23 is
  global-by-design, when at least four of the remainder are per-tenant and unpoliced by the ORM.

- **Where I hesitated.** If *"org column"* is read strictly as *"a column literally spelled
  `organization_id`"*, the sentence is tautologically true of the remainder — but that reading makes it
  vacuous, and the operative words are "global reference data" and "by design", which are claims about
  what the remainder *is*. Booked as a BLOCKER on the operative reading.

- **Confidence:** high

---

### B2 — "exactly one cross-process edge left" / "compose sets a single service address" omits the live `backend → gotenberg` edge

- **Anchor:** `corpus/architecture/service_taxonomy.md:405` (under `### Core Services ↔ Core Services`) —
  > "**Synchronous**: HTTP RPC — and at platform `0c91421` there is **exactly one** cross-process edge
  > left, `backend → sentinel`. Compose sets a single service address,
  > `AUTHORIZATION_ADDRESS=http://sentinel:8087` (`docker-compose.yml:48`), and **zero `*_RPC_ADDR`
  > variables**."

- **Why it is false.** The `*_RPC_ADDR` half is correct — I measured `grep -n 'RPC_ADDR'
  docker-compose.yml common.yml` → **0 hits at `0c91421`**, with `AUTHORIZATION_ADDRESS` at `:48` as the
  positive control in the same pass. The other two clauses are not.

  `platform/docker-compose.yml:57` @ `0c91421` sets, on `backend`:

  ```
  57:      - GOTENBERG_URL=http://gotenberg:3200
  ```

  That is a **second compose-set service address**, to a **second core-tier container**, in the **default
  profile**. `gotenberg` carries `profiles: [core, backend, all]` (`docker-compose.yml:183`), and
  **this same document** lists it as a Tier-1 Core Backend Service at `:81` and counts it in the 5-container
  `core` selection at `:67`, `:464`, `:487`. The edge is live in code, not vestigial:
  `app/main.go:244` (`GotenbergURL: os.Getenv("GOTENBERG_URL")`),
  `app/internal/web/backend/coursebuilder/handler.go:244`, and the client itself at
  `app/internal/converter/gotenberg.go:16` (`ConvertToPDF … via the Gotenberg API`, HTTP multipart, 90 s
  client at `:13`).

  So under this section's own heading (*Core Services ↔ Core Services*) and this file's own Tier-1 roster,
  there are **two** cross-process edges, and compose sets **two** service addresses, not one.

- **Where I hesitated.** The bullet opens *"**Synchronous**: HTTP RPC"*, which could be read as scoping
  "cross-process edge" to Connect-RPC peers only (Gotenberg is a third-party REST image, not an RPC peer).
  I booked it anyway because (a) the sentence says "cross-process edge", not "RPC edge", and gives an
  absolute count; (b) *"Compose sets a single service address"* is false on its face against `:57`; and
  (c) a reader auditing the local topology from this bullet concludes `backend` has exactly one peer,
  which the same file's Tier-1 table contradicts.

- **Confidence:** medium

---

### B3 — `README.md:21` resolves to the wrong construct in every candidate file

- **Anchor:** `corpus/architecture/security_compliance.md:185` —
  > "| **AI Token Tracking** | Centralized usage, latency, and cost tracking in **`app/internal/aiusage`**
  > — **not** the shared `ai` library, which only wraps providers (consistent with `README.md:21` +
  > `ai_architecture.md`) |"

- **Why it is false.** The path is bare (`README.md`, no directory), so I resolved it against every
  plausible target and none of them says anything about `aiusage` at line 21:

  | candidate | line 21 |
  |---|---|
  | `corpus/architecture/README.md` (the sibling index) | the **`shared_libraries.md`** bullet — *"The five internal Go libraries … `authn` is not a dep…"* |
  | `corpus/README.md` | `### [Architecture](./architecture/)` |
  | repo-root `README.md` | *"Project Rosetta is a **documentation repository** - not the Anthropos platform itself."* |
  | `stack-demo/app/README.md` | `    $ make migrations` |

  The construct the sentence wants **does exist**, two lines further on: `corpus/architecture/README.md:23`
  — *"…and cost tracking (`app/internal/aiusage`)."* I confirmed by a tree-wide search that
  `corpus/architecture/README.md:23` is the **only** line in any corpus `README.md` mentioning `aiusage`
  (positive control: the same search returns that line, so the pipeline is live).

  The substantive claim (cost tracking lives in `app/internal/aiusage`, not the `ai` library) is **TRUE** —
  `app/internal/aiusage/ai_usage.go` exists and `corpus/architecture/ai_architecture.md:99,291` say the
  same. Only the corroborating anchor is wrong, and it lands on a claim about a different subject
  (`authn` not being a dependency).

- **Where I hesitated.** The consequence is a two-line miss, not a wrong fact, so this is close to the
  "line drift" class. I booked it because the anchor is offered **as the evidence** for the sentence
  ("consistent with …"), and at `:21` it names a wholly unrelated claim — the *"resolves but names the
  wrong construct"* class, which the briefing lists under BLOCKER.

- **Confidence:** medium

---

### B4 — "academy subgraph" names a federation construct that does not exist, in a file whose own §Subgraphs says `backend` is the only one

- **Anchors:**
  - `corpus/architecture/service_taxonomy.md:37` (the mermaid edge label) —
    `Academy -->|GraphQL - academy subgraph| Backend`
  - `corpus/architecture/service_taxonomy.md:266` — *"**A GraphQL client of the platform `app` academy
    subgraph at runtime**"*

- **Why it is false / self-contradictory.** The same file states at `:387` that the subgraph set is
  **"`backend` alone (1)"** since `915da06`, and at `:392` *"Backend (`app`) — the only subgraph left"*.
  I verified that ladder independently against `graphql-wundergraph` at `60c229f3`:
  `749dc86^` → 5 subgraphs (backend, skiller, jobsimulation, cms, skillpath), `7c17e63^` → 4,
  `915da06^` → 3, and `915da06` deletes **both** `schemas/cms.graphqls` and `schemas/jobsimulation.graphqls`
  in one commit (`git show --stat 915da06`), leaving `supergraph-config-prod.yaml` with exactly one
  `- name: backend`. Locally there is no federation at all — `2adcf71` deleted the router (`:3`, `:372`).

  On the `app` side, `academy` is one **schema file** among 42 in the single `backend` graph
  (`app/internal/web/backend/graphql/graph/schemas/academy.graphqls`), and `ant-academy` reaches it over
  the *same* `NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT` that everything else uses
  (`ant-academy/code/src/graphql/server.js:14`). There is no academy subgraph, routing_url, or SDL entry.

- **Where I hesitated — stated plainly.** This is shorthand that points the reader at the right target
  (Backend/`app`), and the substantive routing claim is correct; it fits the MINOR definition
  ("stale-but-harmless phrasing"). I went back and forth twice, which under the briefing's tie-break rule
  ("when in doubt, book it as a BLOCKER and say why you hesitated") means booking it. The reason it is not
  purely cosmetic: the corpus has already had to fight a wrong subgraph **count**, and a reader who
  believes there is an academy subgraph will look for a routing entry that does not exist.

- **Confidence:** low

---

## Minors

- `corpus/architecture/security_compliance.md:73` — cites `mixin.go:98` for `UserMixin`'s `Policy()`; at
  `b948604f` `:98` is the doc comment and the func is at **`:99`**. (Its twin at `:68`, `mixin.go:126` for
  `OrganizationMixin.Policy()`, is line-exact — the asymmetry is what makes this visible.)
- `corpus/architecture/service_taxonomy.md:109-111` — the **"Base services (no profile, always on…)"**
  section lists only PostgreSQL and Redis, while `:67`, `:463`, `:487` all say the always-on floor is
  **three** services including `sentinel`. `sentinel` is correctly marked always-on in the table at `:80`,
  so no factual consequence — but the file uses "base services" for both 2 and 3.
- `corpus/architecture/service_taxonomy.md:3` — the top banner states flatly *"the repo is ARCHIVED on
  GitHub (2026-07-30)"*, while `:124-131` mandates the hedged *"asserted on"* form for archive state
  (unmeasurable from any clone; `gh` unavailable) and `:373` correctly hedges the identical fact ("a dated
  snapshot — see the archive-state note at `:124`"). Same file, two epistemic standards for one fact.
- `corpus/architecture/service_taxonomy.md:456-457` — the native-run comment reads *"this one process
  covers skiller, skillpath, roadrunner, jobsimulation and cms too"*; since `838d907` (which the rest of
  this file records in detail) it also covers **storage, messenger and customerio-sync**.
- `corpus/architecture/service_taxonomy.md:146` — the Shared Libraries table is headed *"(imported as
  private Go modules)"* over five rows, one of which (`authn`) is imported by nothing; `:489` and the
  `authn` row itself both say so. Header over-reaches its own table.
- `corpus/services/clerkenstein.md:3` — *"**Last updated:** 2026-07-14"* and a Status version list ending
  at *"M218 the roster-aware fake BAPI"*, while the body documents M219 (`:54-62`), M220 (`:305-312`),
  v2.4 M224 (`:232-255`), v2.8 M256 (`:184-209`) and M257x iter-23 (`:274-279`).
- `corpus/services/clerkenstein.md:21` — *"ONE private monorepo with sections (`clerkenstein`,
  `demo-stack`, `stack-injection`, `stack-core`, `stack-seeding`, `alignment`)"* — six named; the
  authoring copy at `5fb0915` holds **eleven**: those six plus `dev-stack`, `playthroughs`,
  `stack-secrets`, `stack-snapshot`, `stack-verify` (root `CLAUDE.md` names nine, itself incomplete).
- `corpus/services/clerkenstein.md:105` — the `cmd/` row enumerates *"standalone binaries: `mintpk` …
  `fake-fapi` / `fake-bapi`"*; `clerkenstein/cmd/` also contains **`jwtkey`**.
- `corpus/services/clerk-integration.md:21` — *"e.g. `metabase/route.ts` checks `orgRole === 'admin'`"*;
  the actual guard is `next-web-app/apps/web/src/app/api/metabase/route.ts:35`
  `authData.orgRole !== 'admin' ||`. Same predicate, opposite polarity as transcribed. The point it
  supports (the server side sees the **bare** `admin`) is correct.

---

## Positively cleared (audited zeros)

Labelled per the briefing. **ENUMERATED** = I derived the predicate's set from source independently and
compared cardinalities. **RE-DERIVED** = I only re-ran the arithmetic the document displays. Only
ENUMERATED is a clearance.

### `security_compliance.md` — the multi-tenancy fence (the rule-24 trap)

- **ENUMERATED — 139 `.go` files / 135 Ent schemas / 4 non-schemas** (`:67-68`, `:107-109`). Measured:
  `ls *.go` → 139; `grep -l 'ent.Schema' *.go` → 135; the 4 that are not schemas are exactly
  `database_types.go`, `mixin.go`, `skiller_mixins.go`, `skillpath_mixins.go`.
- **ENUMERATED — 30 `OrganizationMixin{}` files, of which `user_resource.go:22` is commented out ⇒ 29
  live** (`:68`, `:82-90`, `:135`). I read all 30 matched lines; `user_resource.go:22` is
  `// OrganizationMixin{},  // We need to work on this` and is the only commented one.
- **ENUMERATED — 7 `OrganizationIDMixin{}` users**, and the doc's list at `:99-100` is exactly right:
  `category`, `jobrole`, `similarity`, `skill`, `specialization`, `studio_document`, `studio_task`.
- **ENUMERATED — the 18 "neither mixin + `organization_id`" set.** I ran the document's own derivation at
  `:123-129` from source and got 18, then compared members: `academy_feedback`, `admin_audit_log`,
  `ai_readiness_diagnose_narrative`, `ai_readiness_recommendation`, `api_key`,
  `assignment_invitation_link`, `interview_aggregated_report`, `job_role_skill_suggestion_cache`,
  `job_simulation_session`, `jobsimulation_feedback`, `lab_session`, `org_membership`,
  `org_membership_invitation`, `org_sim_link`, `org_subscription`, `organization_feature`,
  `organization_settings`, `profile_history`. Subtracting `org_membership` (own policy) and
  `academy_feedback` (`UserMixin{}` at `:64`) leaves **exactly** the 16 the doc names at `:92-97`, in
  content if not in order. 16 + 7 = **23** (`:91`, `:98`) ✅.
- **ENUMERATED — "only FOUR files declare any `Policy()`"** (`:109-111`): `grep -n 'func .*) Policy()'`
  returns five funcs in four files — `mixin.go:99` (UserMixin), `mixin.go:126` (OrganizationMixin),
  `org_membership.go:172` (Membership), `organization.go:56` (Organization), `user.go:116` (User).
- **ENUMERATED — the predicate behind "31 auto-filter by ORGANIZATION"** (`:76-80`, `:135`), not just its
  sum. `OrganizationMixin.Policy()`'s **Query** arm is `DenyIfNoOrganizationInContext →
  FilterOrganizationEdges → AlwaysAllow` (`mixin.go:137-141`) — it really does org-filter reads, not only
  writes. `Membership.Policy()` at `org_membership.go:172-188` ends in `privacy.AlwaysDenyRule()` on both
  arms (fail-closed) and uses `AllowCurrentOrgEdgesOrSkipRule()` at `:179`/`:183` — the doc's `:172-188`
  span and its "ending in `AlwaysDenyRule()`" are both exact. `Organization.Policy()` at
  `organization.go:56` has `rule.FilterSameOrganizations()` at **`:96`** — line-exact. `User.Policy()` at
  `user.go:116` filters by user and is correctly excluded. 29 + 2 = **31** ✅.
  *(The residual defect is not in this set — it is in the characterisation of what lies OUTSIDE it: B1.)*
- **ENUMERATED — `TenantMixin` has 0 users**, so it introduces no blind spot of its own (relevant to B1).
- **ENUMERATED — the platform's self-statements quoted at `:130-132`**: `job_simulation_session.go:5` is
  verbatim *"L2: NO Ent privacy Policy; owner/org/tenant are plain fields"*; `jobrole.go:18` and
  `category.go:15` are both the line *"backend's `public` schema: no UserMixin/OrganizationMixin and no
  Policy(), so"*. All three line-exact.
- **ENUMERATED — the EU-AI-Act retraction (`:231-245`).** `skills.go:53` is `calculateSkillScore`
  (counts `check.Success` booleans, `:54-61`) and `:75` is the division
  (`utils.RoundFloat(float32(passed)/float32(total)*100, 2)`). `criterion.go:127` is `case
  check.EngineLlm:` inside the hardcoded `switch critCheck.Engine` at `:126`; `:428` is
  `bulkChecker := NewLLMBulkChecker(c.logger)`; `templates/checkValidationBulk.tmpl:27` really does ask a
  model to *"assess whether the `<asset>` … meets or does not meet"* and `:92-97` returns
  `{"check_id", …, "success"}`. The `EngineTextDiff` counter-example holds: `criterion.go:142` is
  `case check.EngineTextDiff:`, `:168` dispatches `validateCodeDiff`, and `:450-475` sets `success` from
  `cdiff.Diff == ""` with no model. And the *"registers exactly ONE check engine"* claim: I read the map
  at `validator.go:60-62` — one entry, `check.EngineLlm`. `checkerEngines` is assigned to the
  `CriterionValidator` struct (`criterion.go:34`, `:90`) and never read back from it — "stored and never
  read" is accurate about the field the sentence is about.
- **ENUMERATED — the AI-residency anchors (`:196-224`).** In `app/internal/jobsimulation/ai/ai.go`:
  `:85-88` is `config.WithRegion("eu-west-1")` for the Bedrock/Anthropic client (the doc's `:85-95` span
  covers the whole client build); `:263-277` is the entire `case Azure:` arm that swaps `azureClientEu` →
  `azureClientUs` on `IsFeatureEnabled(..., "flag_use_azure_us", ...)` and **keeps the EU client on
  error** (`:270-272`); `isThrottlingError` is at `:129` and its two call sites are at `:166` and `:325` —
  all three line-exact. `coursebuilder/bedrock.go:109-112` is `newUnderlyingClient` → the
  `ANTHROPIC_API_KEY` branch → `NewAnthropicClientWithModel`, and `:100` is the `return "anthropic-api"`
  the sentence quotes. `external_services.md:579` is *"There is **no ordered EU-first fallback chain.**"* —
  the cross-file anchor resolves; `:567` is the **Anthropic Direct** provider row, also exact.
- **ENUMERATED — "all three `app/studio/configs/*.ini` pin `azure`"** (`:205`). Grepped at
  `app/studio`'s **own** ref: 3 `.ini` files (`config_template`, `development_config`,
  `production_config`); `AI_.*_MODEL` yields exactly **30** lines and every one of the 30 begins
  `= azure,`. The latent Studio-Room `openai` arm is correctly described as selectable by no shipped
  config.
- **ENUMERATED — the org-switch anchors (`:147`).** `next-web-app` @ `bb3313bc`:
  `apps/web/src/hooks/useOrgSelection.tsx:94`, `useResolveActiveOrg.tsx:107`,
  `useActivateMembershipOrg.tsx:81` are each the `clerk.setActive({ organization: … })` call, and
  `apps/hiring/src/hooks/useOrgSelection.tsx:94` is the same line — so the `{web,hiring}` brace expansion
  in the citation resolves on both sides. No sign-out, no re-authentication on that path.

### `service_taxonomy.md`

- **ENUMERATED — the compose topology at `0c91421`** (`:65-67`, `:104-107`, `:460-479`, `:487-488`).
  `docker-compose.yml` declares exactly 5 services (`sentinel`, `backend`, `studio-desk`, `next-web-app`,
  `gotenberg`); `include: common.yml` adds `postgresql` + `redis` ⇒ 7 effective. `postgresql`, `redis`
  and `sentinel` declare **no** `profiles:` key, so the floor is 3 and `core` starts 5. Exactly four
  services carry a `profiles:` key. The legal token set is exactly `{all, backend, core, frontend,
  studio-desk}`. `Makefile:10` is `PROFILE ?= core`.
- **ENUMERATED — the historical split `8/10 at 0dab54d`** (`:73`): `838d907^` **is** `0dab54d`, and its
  compose declares 8 services (+2 from `common.yml` = 10). ✅
- **ENUMERATED — the deletion commits.** `d11a403` (2026-08-03, merged `ef32d4c`) removes exactly
  `jobsimulation:`, `cms:`, `roadrunner:` from compose **and** their three `repos.yml` entries; at
  `d11a403^` all three carried `profiles: [graphql, …]` and `Makefile:10` was `PROFILE ?= graphql`, so
  *"in the then-default profile, so a bare `make up` started all three"* is exactly right. `838d907`
  (2026-08-05, merged `0c91421`) removes `storage`, `customerio-sync`, `messenger` from compose and drops
  `storage` + `messenger` from `repos.yml`; at `838d907^` `customerio-sync` carried
  `profiles: [customerio-sync, all]`, so *"still in the `all` profile until the deletion"* is right, and
  `customerio-sync` was never a `repos.yml` entry (verified at `0dab54d`). `docker-compose.yml:84-92` is
  precisely the `MESSENGER_ENABLED` / `CUSTOMERIO_SYNC_ENABLED` comment block the doc cites.
- **ENUMERATED — `repos.yml` is 4 repos** (`app`, `sentinel`, `next-web-app`, `studio-desk`), `app` the
  only `migrations: true` (`:14-17`), `sentinel` `migrations: false` (`:18-20`) while compose still gives
  it `search_path=sentinel` at `docker-compose.yml:18` — the Trap-A case, exactly as stated at `:62`.
- **ENUMERATED — zero `*_RPC_ADDR` in compose** (positive control `AUTHORIZATION_ADDRESS` → `:48` in the
  same pass). *(The rest of that bullet is B2.)*
- **ENUMERATED — the prod router row (`:3`, `:384`).** `graphql-wundergraph/terraform/main.tf:20` is
  `service_desired_count          = 1`; `terraform/locals.tf:8` is `port           = 8080`;
  `terraform/main.tf:48-49` maps `containerPort`/`hostPort` = `${local.port}`; `config.prod.yaml:5` is
  `listen_addr: 0.0.0.0:8080`. And `5050` was local-only: `2adcf71^:docker-compose.yml:14` is
  `"5050:8080"`, and `5050` returns **0 hits** anywhere in the current compose/`common.yml`/`.env_example`.
  Repo HEAD is 2026-07-30 with nothing after, consistent with the archive date.
- **ENUMERATED — the subgraph ladder 5→4→3→1** (`:387`); see B4 for the derivation. `915da06`'s own
  subject line says "2→1", which is why the doc warns not to take the count from it.
- **ENUMERATED — the Directus retraction-of-the-retraction (`:343-345`).** At `a2a3ee6^` the compose
  really did carry `:384 image: directus/directus:10.10.1`, `:386 - 8055:8055`, `:409
  - ADMIN_PASSWORD=password` — all three line-exact, and `a2a3ee6` is dated 2026-02-27 as claimed.
  `DIRECTUS_PUBLIC_BASE_ADDR` is at `docker-compose.yml:53` at `0dab54d` (the pinned ref) *and* at
  `0c91421`.
- **ENUMERATED — Studio-Room / Studio-Desk.** `app/studio` @ `aeec036a`: `services/ai.py:1-2` imports only
  `openai`/`anthropic`; the provider registry is the dict at `:705-708`
  (`openai`/`azure`/`anthropic`); `mistralai` is imported **only** at `tools/pdf2md.py:24`
  (`tools/r3.py`'s three hits are `MISTRAL_API_KEY` env checks, not an import), and
  `git -C app/studio grep -i mistral aeec036a` → **22 hits / 3 files**, matching the doc's own stated
  measurement exactly. `gen.py` registers **exactly nine** `add_argument` calls at `:484-492`, with the
  nine names the doc lists, and `parse_argument` uses `parse_known_args` at `gen.py:19` — so a stray
  `--template` really is silently absorbed. `app/internal/cms/studio/studioManager.go:119` is the
  `runCommand(ctx, pyBin, append([]string{"studio/gen.py"}, tokens...))` exec; `cms_queries.graphqls:106`
  is `studioTask(taskId: ID!, organizationId: ID): StudioTask!` (with `archiveStudioTask` at
  `cms_mutations.graphqls:22`); `studio-desk/.env.example:45` is
  `VITE_GRAPHQL_ENDPOINT=http://localhost:8082/graphql/query`. The generation-edge correction at `:44-52`
  is therefore sound in the direction it states.
- **ENUMERATED — Studio-Desk "no framework"** (`:192-195`): `git grep '"react|"vue|"angular'` over
  `package.json` → **0**; `.tsx`/`.jsx` tracked files → **0** (positive control: 71 tracked `.ts` files).
  *"7 languages"* (`:206`) → `app/public/languages/` holds exactly 7 flags (france, germany, italy, japan,
  netherlands, spain, united-states). *"GPT-5.x"* (`:203`) → `src/services/ai/config.ts:21-24` pins
  `gpt-5.2` / `gpt-5-mini` / `gpt-5-nano`.
- **ENUMERATED — Ant Academy** (`:259-273`). `code/package.json`: `next ^16.2.6`, `react ^19.2.5`,
  `"dev": "next dev --port 3077"`; `mobile/package.json`: `expo ~54.0.33`, `--port 8555`. No
  `serwist`/`workbox` dependency (positive control: `"next"`/`"react"` both match in the same grep);
  `tests/unit/next-scaffold.test.js:106` and `:111` are the two serwist-removal tests and
  `react-compiler-config.test.js:41` is `expect(src).not.toMatch(/from '@serwist\/next'/)` — all three
  line-exact. `public/academy-manifest.json` has `"display": "standalone"` and `app/layout.jsx:132` is
  `manifest: "/academy-manifest.json"`. `src/graphql/server.js:14` reads
  `NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT` and `:18` **throws** when unset; `src/lib/serverTenant.js:115-145`
  contains verbatim *"there is NO FS-as-published fallback"* and *"not reversible-on-error"*;
  `src/lib/backendContent.js:36` and `:102-103` resolve; `app/api/academy/beacon/route.js:36` is the
  endpoint const and `:41-55` is the `OPS` map with `UPSERT_CHAPTER_PROGRESS` / `SET_LAST_ACTIVITY`.
  `.github/workflows/deploy-academy.yaml` exists. `NEXT_PUBLIC_FEATURE_TRAINING_COACH` is read at
  `src/lib/featureFlags.js:8` and documented default-OFF; Cosmo is
  `code/ucourses/ucourse-engine/assistant/agent.js` — `API_URL = 'https://api.openai.com/v1/responses'`
  (`:12`), `MODEL = 'gpt-5.2'` (`:13`), `localStorage.getItem('openai_api_key')` (`:32`), and the route
  handler `app/api/ai/chat/route.js:10-11` independently confirms *"First-party Cosmo doesn't call this
  endpoint (it calls OpenAI directly from the browser)"*. The
  `demo-stack/patches/academy-fs-published-fallback` demo-patch exists in the rext authoring copy.
- **ENUMERATED — `authn` is imported by nothing** (`:489`): `anthropos-work/authn` appears in **0** of the
  seven Go clones' `go.mod` (`app`, `sentinel`, `storage`, `messenger`, `cms`, `jobsimulation`,
  `roadrunner`), while `anthropos-work/colony` appears in **all seven** — the doc's own stated control,
  re-run.
- **ENUMERATED — the archived/merged table's terraform citations.** `roadrunner/terraform/main.tf:19` is
  `service_desired_count = 1` (the doc's *"orphaned, not absent"*). `cms/terraform/main.tf:39` is
  `service_desired_count = 0` inside `module "cms"` (opened `:30`) in a 191-line file; `6efa1d5`
  (2026-08-04, merged `f38c0c4` = `cms` `origin/main`) is *"drop build-production — the cms ECR repository
  is decommissioned (M810)"*. The **Jobsimulation** row is the one worth naming: it is pinned to
  `6092c6d2`, which is **not** an ancestor of the audited checkout `462343b0` — read at its own ref per
  §5 rule 33, `terraform/main.tf` there is 56 lines, the `module "jobsimulation"` block is gone, and
  `:15-40` is exactly the comment block enumerating what survives (livekit.tf, chime.tf, ssm.tf, the atlas
  tracker). The claim is **true at the ref it names**, and false at the checkout — which is the rule
  working, not a defect.

### `clerkenstein.md`

- **ENUMERATED — every gene count** (`:41-44`, `:74`), parsed from the five DNA JSONs rather than
  re-added: `clerk-2.6.0` **14 capabilities / 27 variants**; `clerk-js-5` 6 / **9**; `clerk-multi-1` 5 /
  **9** (with `Roster` = **2**, matching `:250`); `clerk-express-1` **5 / 13**; `clerk-deploy-1` 3 / **7**.
  The gate thresholds *"≥95 / =100"* are `gate_overall="${GATE_OVERALL:-95}"` /
  `gate_critical="${GATE_CRITICAL:-100}"` at `alignment/scripts/gate.sh:31-32`.
- **ENUMERATED — the M219 fix anchors.** `alignment/cmd/alignctl/run.go:134-135` is exactly
  `ExitRegressed = 2` / `ExitUnmeasurable = 3`; `clerk-backend/store.go:138` is
  `func (s *Store) SeedOrgIdentity(...)` and `:151` is `func (s *Store) LookupOrgEid(...)`;
  `alignment/dna/clerk-2.6.0.json:131` is the `MembershipOrgIdentity` description containing verbatim
  *"M219 landed the fix … taking the Go surface 97.2% -> 100%."* All four line-exact.
- **ENUMERATED — the single-tenant seat disclosure (`:160-182`).** `clerk-frontend/registry.go:24` holds
  one `activeKey`, with `active()` at `:67` and `Select()` at `:75`. `clerk-frontend/server.go:99-110`
  declares one `signedIn`, one `signedOut`, one `clientID`, one `sessID`; `clientID` is the literal
  `"client_clerkenstein"` (`:125`) and `sessID` the literal `"sess_clerkenstein"` minted in
  `establishLocked` at `:665`. `handleSignOut` takes `_ *http.Request` (`:554`) — it really does ignore
  its route param. **`.Cookie(` returns 0 across all of `clerkenstein/`** under `git grep` at the rext
  ref, with `Set-Cookie` returning 5 hits as the positive control — cookies are emitted (as raw
  `Set-Cookie` strings inside the handshake JWT, `shared/rsa.go:115`) and never read. All five pinned
  test names exist in `clerk-frontend/server_test.go` (`:256`, `:286`, `:390`, `:427`, `:461`).
- **ENUMERATED — the M220 clerk-js fix (`:305-312`).** `clerk-frontend/server.go:35` opens the M220
  block and `:67` closes it with *"test asserts the package contains no `http.Get(` on the clerk-js
  path"*; `clerkJSFetchTimeout = 15 * time.Second`, `clerkJSClient` is commented *"Explicitly NOT
  http.DefaultClient"*, and `FAKE_FAPI_CLERKJS_CACHE` is the cache dir. `clerkjs_cache_test.go:196` is the
  source-level fence banning `http.Get(` / `http.DefaultClient`; the surviving `http.Get(` hits are all in
  *test clients*, not on the served path.
- **ENUMERATED — the colony/clerk drift note (`:274-279`).** `app/go.mod` reads
  `colony v0.35.2` **and** `clerk-sdk-go/v2 v2.7.0`; `sentinel` and `storage` both read `v0.34.3`;
  `clerkenstein/go.mod:8` pins `colony v0.34.3` and `deploy/colony-authn/clerk_test.go:612` names the same
  pin. Every clause of that warning holds. (`messenger` is also on `v0.35.2`, not mentioned — harmless.)
- **ENUMERATED — the studio-desk role set (`:140-143`).** `src/index.ts:96` and
  `app/services/userService.ts:16` each declare
  `['admin', 'org:admin', 'content_creator', 'org:content_creator']`, each preceded by the exact comment
  *"Both the prefixed (org:\*) and bare role keys are accepted."* Both line-exact.
- **ENUMERATED — layout + misc.** The `clerkenstein/` dir table (`:99-106`) matches disk exactly
  (`authn`, `clerk-backend`, `clerk-frontend`, `clerk-webhook`, `shared`, `deploy`, `cmd`, `alignment`);
  `alignment/` holds the five runners, five DNAs, `golden{,-js,-multi,-express,-deploy}/` and `scripts/`
  (`drift-check.sh`, `drift-test.sh`, `gate.sh`) exactly as described; `.github/workflows/alignment.yml`
  is git-tracked (`git ls-files` confirms) and, being below the repo root, inert as stated;
  `cmd/fake-fapi/main.go:25` reads `FAKE_FAPI_ROSTER`; the `dotless-pk-rejected` gene is real, in
  `clerk-express-1`'s `ExpressRequest` capability.

### `ai-labs.md`

- **ENUMERATED — the credit cost model (`:71-83`), which contains a live trap.** The live map is
  `internal/credits/cost.go:86-90`: `ActionCourseBuild: 5`, `ActionCourseRefine: **1**` (with the inline
  ruling `// D1 ruling: flat 1 credit per refine turn`), `ActionCourseTranslate: 1`. The **package doc
  comment 60 lines above, at `cost.go:29`, says `course.refine → 5 credits per refine turn`** — the
  corpus follows the compiled map and is **right**; a reader who grepped to the comment would have
  "corrected" it wrongly. `MarginMarkup = 1.40` (`:133`), `PricePerCreditUSD = 0.45` (`:142`),
  `DefaultSeedBalance int64 = 500` (`:231`), packages 50/200/500 (`:31-33`, `:73-75`) — all exact.
- **ENUMERATED — the Stripe webhook event set (`:109-111`).** The `switch event.Type` at
  `internal/web/backend/api/api.go:317` has **exactly two** case arms in its whole span: `:319`
  `"customer.created"` and `:325-327` `"customer.subscription.created"|"updated"|"deleted"`. And
  `checkout.session.completed` returns **0 hits** tree-wide in `app` (positive control:
  `customer.created` returns hits in the same pass) — the un-built shared-purse grant, exactly as stated.
- **ENUMERATED — the credits HTTP surface (`:106-107`).** `internal/web/backend/credits/handler.go:3-4`
  documents only `GET /credits/balance` and `GET /credits/transactions`, and `:10-13` states *"The mock
  POST /credits/purchase was removed in Wave 13"* — the corpus's Wave-13 attribution is the platform's own.
- **ENUMERATED — structure.** `internal/{labs,credits,payments,subscriptions}/` + `stripe/` all exist;
  `labs/` holds `adapter`, `catalog`, `labsapi`, `session`. Ent schemas `lab.go`, `lab_body.go`,
  `lab_session.go`, `organization_credit.go`, `credit_transaction.go`, `org_subscription.go` all exist,
  and all five cited migrations resolve by name (`20260617203555_add_labs_catalog.sql`,
  `20260529072659_add_lab_session.sql`, `20260617120000_add_lab_session_model.sql`, `20260717151144.sql`,
  `20260626120000_add_org_subscriptions.sql`). `lab_session.go` carries **no** `PrimaryKeyMixin` (only
  CreatedAt/UpdatedAt) ✅. `lab_body.go:12-13` says *"the EN base is locale 'en'; each translation overlay
  (de/es/fr/it/nl/pt)"* — **6** overlays ✅. `LabsTier` = `essential`/`professional`/`frontier`
  (`labs_budget.go:26-28`). `spend_reconciler.go:60` is `Interval: 30 * time.Second` ✅.
  `labsapi/client.go:6` documents the `:7070` default; `LABS_API_PLATFORM_TOKEN` is read at `main.go:745`.
  `cmd/` holds `labsdemo` (`:8099`, `main.go:115`), `labsimport`, `labskey`. `stripe-go/v74 v74.30.0` in
  `go.mod:62`; `payments/handler.go:193` `LookupKeys`, `:207` `CheckoutSessionModeSubscription`, `:240`
  `TrialPeriodDays: 7`. `labs.graphqls` carries `labs`/`labBySlug`/`labSessions`/`mySessions`/
  `labSpendByDay`/`createLabSession`/`cancelLabSession`; `billing.graphqls` exists; `Credits has NO
  GraphQL` holds (no credits schema file). `/v1/labs` is grouped behind
  `apiKeyAuthMiddleware(apiKeys, "labs:write")` at `labs_admin.go:31` with
  `/v1/labs/:slug/workspace.tar.gz` at `:40`. `lab.v1.LabSessionService` + `ReportEvent` at
  `labsession.go:1`, `:386`. Every named test file exists, and **`payments` genuinely has no `_test.go`**
  as stated.
- **ENUMERATED — the version claim (`:18`).** `app` `origin/main` is **`2035f9a4`** and
  `116645387 chore(version): v1.369.0` is in its history — *"`v1.369.0` @ origin/main `2035f9a4`, measured
  2026-08-06"* is current and correct. (Note for the orchestrator, §5 rule 32: the **briefing's** own
  statement that app's `origin/main` is *"four commits past `v1.367.0`"* is now stale — it is ~70 commits
  past. The corpus is fresher than the instrument here.)

### `clerk-integration.md`

- **ENUMERATED — the 12 webhook event types (`:37`, `:79`).** `app/internal/clerk/events/events.go`
  `:166-235` has exactly twelve cases: `user.{created,updated,deleted}`,
  `organization.{created,deleted,updated}`, `organizationInvitation.{accepted,created,revoked}`,
  `organizationMembership.{created,deleted,updated}`.
- **ENUMERATED — 8 locales (`:41`).** `next-web-app/apps/web/src/providers/Clerk.provider.tsx:10-17`
  imports `deDE, enUS, esES, frFR, itIT, jaJP, nlNL, ptPT` = 8. (`apps/hiring` imports 7 — the doc's
  figure is the web app's, which is the larger set.)
- **ENUMERATED — the import measurement (`:51`, `:91`).** `git grep -l 'anthropos-work/colony/authn'` at
  each clone's ref: `app` **129**, `sentinel` **0**, `storage` **0**, `messenger` **0** — with
  `anthropos-work/colony` returning 13 / 13 / 15 in the same repos as the positive control. `repos.yml`
  lists exactly two Go repos now; at `0dab54d` it listed four (app, sentinel, storage, messenger) — both
  halves of the parenthetical verified.
- **ENUMERATED — `AuthRole()` has zero call sites (`:73`).** `AuthRole` returns **0** occurrences
  anywhere in `app` at `b948604f` (positive control `AuthID` → hits) — stronger than the claim.
- **ENUMERATED — every SDK version in `:103`.** `@clerk/nextjs ^6.39.2` in `apps/web:10`,
  `apps/hiring:10`, `apps/integration:9`, `ant-academy/code/package.json:52` — four, aligned.
  `@clerk/clerk-expo ~2.6.18` at `next-web-app/apps/mobile/package.json:6` vs `~2.19.36` at
  `ant-academy/mobile/package.json:18` — **thirteen** minor versions, arithmetic correct.
  `app/go.mod:31` @ the pinned `5ba17044` is `github.com/clerk/clerk-sdk-go/v2 v2.7.0` — line-exact at
  the named ref. `studio-desk` installs `@clerk/clerk-js ^5.52.3` + `@clerk/express ^1.3.47` (`:27-28`);
  `e2e` installs `@clerk/backend` + `@clerk/testing` (`:17-18`); `@clerk/localizations ^4.5.2` in web +
  hiring.
- **ENUMERATED — "storage, messenger: no Clerk / no auth" (`:75`, `:94`).** `clerk` (case-insensitive)
  returns **0** in both clones at their refs, with `anthropos-work/colony` as the positive control in the
  same pass. `jobsimulation`/`cms` really have had no compose service and no `repos.yml` entry since
  `d11a403` (verified from the commit's own diff).
- **ENUMERATED — ant-academy's gates (`:100`).** `code/proxy.js:91` reads
  `REQUIRE_ORGANIZATION_MEMBERSHIP` and `:5`/`:25`/`:37` document the `/no-organization` redirect;
  `mobile/src/components/ClerkSignInForm.tsx` exists (the `:46` MFA caveat's subject).

---

## What I could not settle

- **`corpus/services/db-backup.md` in its entirety (31 lines).** There is no `db-backup` clone in the
  ground-truth set and no `db-backup` reference in `platform/repos.yml` or compose, so *"every 6 hours"*,
  the three targets (S3 / Azure / Hetzner-Germany), *"Go"*, and *"Docker (scheduled, internal)"* are
  unverifiable here. Nothing in it contradicts `security_compliance.md:13`/`:155` or
  `service_taxonomy.md:144`, which is the most I can say. **Settled by:** the `db-backup` repo (or the
  `infrastructure` repo's scheduled-task definition) being added to the clone set.
- **All GitHub *archive-state* assertions** (`service_taxonomy.md:3`, `:137`, `:138`, `:373`). The doc is
  right that this lives in the org API and not in git objects; `gh` is not installed on this host and the
  repos are private. I could only confirm the weaker consistency check the doc itself offers — that no
  clone carries a commit after its stated date (true for `graphql-wundergraph`: HEAD 2026-07-30; **false**
  for `jobsimulation`, whose `origin/main` is dated 2026-08-04, which is exactly what the `:139` cell
  already discloses). **Settled by:** `gh api repos/anthropos-work/<repo> --jq .archived`.
- **`ai-labs.md:16` — `app knowledge/plan/releases/06.00-shared-purse/`, milestones M600–M607.** I did
  **not** open, list, or grep it: the briefing's hard bar is written as a path pattern
  (`knowledge/plan/**`) and I chose the conservative reading rather than deciding that a different repo's
  tree of the same shape is exempt. The load-bearing half of that bullet — *no
  `checkout.session.completed` webhook, no labs↔credits linkage, `/credits/purchase` removed (Wave 13)* —
  I verified from code independently, and it holds. **Settled by:** an explicit exemption for
  `stack-demo/app/knowledge/plan/**`.
- **`ai-labs.md:4-6` — the existence of the `anthropos-work/AI-Labs` repo** as the `labs-api` control
  plane. Not in the clone set; `gh` unavailable. The `app`-side *consumer* half of the claim is fully
  verified (`labsapi/client.go`, `LABS_API_URL`/`LABS_API_PLATFORM_TOKEN`, `terraform/main.tf:455`,
  "labs-api owns session-ID generation"), which is consistent with — but not proof of — a separate repo.
- **`service_taxonomy.md:262` — Ant Academy Clerk "domain-gated to `@anthropos.work`".** The
  org-membership half is code-verified (`proxy.js:91`). The email-domain half is a Clerk **dashboard**
  restriction; the only `@anthropos.work` occurrences in the clone are dev-login helpers, e2e fixtures
  and a Brevo sender comment. Not contradicted, not confirmable from a clone.
- **`security_compliance.md` infrastructure and legal rows** — VPC CIDR `10.0.0.0/16`, subnet layout
  (`:21-25`), TLS 1.3 cipher profile (`:38`), AES-256/KMS (`:42`), the CloudWatch/Sentry/PostHog/Better
  Stack table (`:181-184`), 90-day retention, **DPA v1.4 / 18 sub-processors** (`:261`), the 6-h backup
  cadence and the US DR site (`:155-159`). All live in the `infrastructure` repo or in contracts, neither
  of which has ever been in a clone set. **Settled by:** adding `infrastructure` to ground truth, or
  marking these rows prose-under-review per §5 rule 27.
- **`clerk-integration.md:44` — the "Not used" list** (MFA/TOTP/passkeys, OAuth/social/SAML,
  multi-session, Clerk Billing, `has()`/`<Protect>`, Actor Tokens, `@clerk/themes`, most prebuilt
  components). These are dashboard toggles plus absence-of-import claims across five app trees; I spot-
  confirmed `@clerk/themes` is in no `package.json` and that sign-in tokens are used **only** by
  `app/internal/admin/impersonation/manager.go` (`:29`, `:74`, `:101`), which supports `:40`. A full
  clearance would need a per-repo enumeration of `@clerk/*` symbol imports, which I did not run.
- **Out-of-partition observation, recorded because it is a citation into a file I did audit.** Root
  `CLAUDE.md` anchors the no-EU-first-ladder claim at `corpus/architecture/external_services.md:546`;
  `:546` is *"2. ~~**Restart Wundergraph**…~~"* under **Schema Updates**. The correct anchor is `:579`,
  which is what my assigned `security_compliance.md:7` and `:198` both use — so the two sites disagree and
  the corpus's own highest-propagation instruction file holds the wrong one. Not booked, because
  `CLAUDE.md` is outside my file set; flagged for whoever owns it (§5 rule 19's corollary — a claim leaks
  to the edge of the previous repair's scope and stops there, and `CLAUDE.md` is exactly that edge).
