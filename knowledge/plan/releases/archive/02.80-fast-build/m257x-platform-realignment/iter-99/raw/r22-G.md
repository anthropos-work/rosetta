# Seat G — reading #22

Ground truth re-verified at this reading's open (all trees enumerated with
`find stack-demo -name .git -maxdepth 4`):

| repo | path | sha at read |
|---|---|---|
| app | `stack-demo/app` | `b948604f` (origin/main `2035f9a4`, `v1.369.0-2`) |
| platform | `stack-demo/platform` | `0c91421d` (per ADDENDUM; the frozen table's `0dab54df` is stale) |
| next-web-app | `stack-demo/next-web-app` | `bb3313bc` |
| studio-desk | `stack-demo/studio-desk` | `14a5442a` |
| graphql-wundergraph | `stack-demo/graphql-wundergraph` | `60c229f3` |
| cms / jobsimulation / roadrunner / storage / messenger / sentinel / ant-academy | — | as briefed |
| nested `app/studio` + `cms/studio` | — | `aeec036a` each (own ref) |
| rosetta-extensions (authoring) | `.agentspace/rosetta-extensions` | `5fb0915e` |

`corpus/ops/platform-alignment.md` §5 read in full (rules 1–44) before starting.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/services/studio-desk.md` | 435 | yes |
| `corpus/services/hiring.md` | 434 | yes |
| `corpus/services/graphql-wundergraph.md` | 280 | yes |
| `corpus/services/academy-backend.md` | 149 | yes |
| `corpus/services/next-web-app.md` | 133 | yes |

(Also opened, as ground truth / for the self-contradiction check, not as assigned scope:
`corpus/ops/platform-alignment.md` (2232), `corpus/architecture/service_taxonomy.md` (494),
`corpus/architecture/dependency_map.md`.)

---

## Blockers

### B1 — `hiring.md` cites `organization/manager.go:485` for a hard-error that is at `:535-537`, and `:448` for a branch at `:450-453`; both are drifted onto the wrong construct

- **Anchor:** `corpus/services/hiring.md:80-81` —
  > "…as do `organization/manager.go:448` (a forced Clerk membership is created with role `candidate`
  > instead of `member`) and `:485` + `siminvitationlink.go:62` (both **hard-error `"organization is
  > not hiring"`** — the latter is `CreateOrganizationSimInvitationLink`."

- **Why it is false:** at the checkout `app b948604f` (the passage names **no** ref of its own — the
  `5ba17044` pin lives in a different blockquote, in a different section, 40+ lines above, so §5 rule 33
  does not extend it here):
  - `app/internal/organization/manager.go:448` is a **blank line**. The `switch org.IsHiring` is at
    `:450`, `antRole = enum.RoleCandidate` at `:453`.
  - `app/internal/organization/manager.go:485` is `}` — the closing brace of
    `forceUserToOrganizationInClerk`. It does not hard-error anything. The only
    `"organization is not hiring"` in that file is at **`:535-537`**
    (`if !org.IsHiring { m.logger.Error(...); return fmt.Errorf("organization is not hiring") }`),
    50 lines further down, in a *different* function.
  - Derivation: `grep -rn "organization is not hiring" internal/` returns exactly two non-test
    production sites — `internal/organization/siminvitationlink.go:63` (the doc's `:62` is the
    `if !org.IsHiring` guard — correct in substance) and `internal/organization/manager.go:536,537`.
  - The anchors **were** right at `app 5ba17044` (`:448` = `switch org.IsHiring`, `:485` =
    `if !org.IsHiring`) and have drifted at every later ref I checked — `b948604f`, `9d00a313` and
    `origin/main` all read `:450` / `:535`. This is precisely §5 rule 34 / rule 33's "a pin is a date,
    not an exemption", and the passage does not carry the pin.
- **Confidence:** high

### B2 — `hiring.md` claims the `is_hiring` flip "gates Workforce Intelligence off"; no `isHiringOrg` branch anywhere does that

- **Anchor:** `corpus/services/hiring.md:302-303` —
  > "- **Also under `is_hiring`:** the nav trims the library to **AI-Simulations**, hides some member
  > surfaces for non-admins, and gates **Workforce Intelligence off**. None of these touch the
  > comparison scoreboard."

  The bullet list this sits in is introduced at `:289` as *"`isHiringOrg` is **client-derived**
  (`useGetClerkOrganization.tsx:20-21`, above). What the flip changes:"*.

- **Why it is false:** the first two effects are real `isHiringOrg` branches
  (`packages/ui/src/NavBar/useNavbarSections.tsx:339-348` trims the library to
  `librarySimulationsMenuItem` alone; `:326-328` gate `profile`/`skills`/`activities` on
  `!isHiringOrg || isAdmin`). The third is not:
  - The Workforce Intelligence entry is `enterpriseWorkforceMenuItem`
    (`useNavbarSections.tsx:388-395`), included at `:546` as `showWorkforce ? … : null`, inside
    `intelligence: orgVisible.groups.intelligence ? … : []` (`:543-550`).
  - `orgSectionVisibility({ isAdmin, showStudio })` returns `intelligence: isAdmin`
    (`packages/ui/src/NavBar/orgGroups.ts:48-64`) — **no `isHiringOrg` input at all**.
  - `showWorkforce` defaults to `true` (`useNavbarSections.tsx:158`, `NavbarItems.tsx:30`,
    `NavbarLeft.tsx:26`, `NavbarTop.tsx:54`). `grep -rn "showWorkforce" apps/` returns **only two
    hits, both in `apps/hiring`** (`apps/hiring/.../template.tsx:167` and `:248`, hardcoded
    `showWorkforce={false}`). `apps/web` never passes it, so it stays `true` for a hiring org.
  - Server side is the same: `git grep -ln "IsHiring" HEAD -- '*.go'` gives 31 files, and the only
    read-path consumers are `GetOrganizationIsHiring` in `resolver_cms_queries.go:95,210,258,295`
    (content library), `manager.go:450/535` and `siminvitationlink.go:62`. Nothing gates a workforce
    intelligence surface on it.
- **Why I hesitated:** the *statement* "a hiring org does not see Workforce Intelligence" is true of
  the app a genuine hiring org actually lands in (`apps/hiring`, by a hardcoded prop) — but the
  mechanism is not the flag, and the section is explicitly an enumeration of "what the flip changes",
  written so a future reader "doesn't 'fix' the divergence". Attributing an `apps/hiring` build-time
  constant to `isHiringOrg` sends anyone debugging a DB-only/Clerk-only hiring org to the wrong place —
  the exact failure the surrounding section exists to prevent. Booked as a blocker per the
  resolve-upward rule.
- **Confidence:** medium

### B3 — `hiring.md:38` cites `service_taxonomy.md:52` as a "twin" of the jobsimulation-schema claim; `:52` is about `studioManager.go` exec'ing `studio/gen.py`

- **Anchor:** `corpus/services/hiring.md:38` —
  > "— as the twins [`service_taxonomy.md:52`](../architecture/service_taxonomy.md)
  > and [`dependency_map.md:78`](../architecture/dependency_map.md) already said."

  supporting `:30-37`: *"**Everything `app` writes is in `public`** … **No `app` migration touches the
  `jobsimulation` schema at all**, and **in production** that schema **survives, frozen**, until M710 —
  while **no local dev/demo stack creates it at all**"*.

- **Why it is false:** `corpus/architecture/service_taxonomy.md:52` reads
  *"then runs the pipeline as a **subprocess of its own container** — `app/internal/cms/studio/studioManager.go:119`
  execs `studio/gen.py`."* — the studio-room embedding correction, unrelated to schemas.
  The claim the citation intends is at **`service_taxonomy.md:62`**: *"**one schema, `public`, owned by
  `app`** … the `cms`, `jobsimulation` and `skillpath` schemas are legacy husks"*. The sibling anchor
  `dependency_map.md:78` **is** correct (*"…or directly to the **`public`** schema (the legacy
  `jobsimulation` schema is non-authoritative)"*), so the pair is half-drifted — §5 rule 34's exact
  signature (a corpus repair moved the line and the citing site was not re-pointed).
- **Why I hesitated:** it is a supporting cross-reference rather than the load-bearing claim, and the
  claim itself is true. But a reader sent to `:52` to corroborate finds an unrelated paragraph and
  cannot tell whether the "twin" exists at all.
- **Confidence:** high

---

## Minors

- `corpus/services/graphql-wundergraph.md:177` — *"they were `:220`/`:236` at `0dab54d` and
  `:334`/`:352` at `2adcf71`"* names the four current anchors (`:135`/`:160` env, `:119`/`:151` args)
  and then gives two historical numbers that are **not a consistent pair**: at `0dab54d`, `:220` is
  studio-desk's *runtime env* line and `:236` is next-web's *build arg* (env is `:245`); at `2adcf71`,
  `:334` is studio-desk env and `:352` is next-web's build arg (env is `:361`). They look like the
  middle two of a 4-line `grep -n`. Both numbers do land on the named variable, so nothing misleads
  materially; `next-web-app.md:48` independently (and correctly) calls `:236` the build arg.
- `corpus/services/hiring.md:145` — *"M223's hiring story simply sets `role_mix ≈ 0.1 admin / 0.9
  candidate`"*. The shipped preset is `admin: 0.14, member: 0.0, candidate: 0.86`
  (`stack-seeding/presets/stories.seed.yaml:249`), and its own comment at `:211` reads *"admin: 0.14
  (NOT 0.10) — M226 iter-04 (the billion-proof count fix)"*, because 0.10 yields only 3 real admins.
  Hedged with `≈`, and the preset is the source of truth, so: minor.
- `corpus/services/hiring.md:211` — `persona_write.go:69-71,143-167` is imprecise: the three
  `validation_*` targets are named at `:66-68` (`:69-71` is the "the old `jobsimulation.sessions` step
  is REMOVED" note) and the write steps are `:92-94`; `:143-167` covers `sessionCols()` +
  `attemptResultCols()` but stops before `skillResultCols()`/`criterionResultCols()`. The
  neighbouring `persona_write.go:91` and `:152-158` cites are exact.
- `corpus/services/next-web-app.md:39-44` — the "Shared packages (`packages/` + `configs/`)" table
  omits `@anthropos/design` (`packages/design/package.json`), one of four workspace packages. It also
  omits `configs/github`. No factual consequence.
- `corpus/services/studio-desk.md:366-368` — the two pinned regression tests are attributed to
  "`stack-injection/gen_injected_override.py`'s"; they live in
  `stack-injection/tests/test_injection.py:1657` and `:1714`. Reads as a possessive about the module
  under test rather than a path, so: minor.
- `corpus/services/academy-backend.md:15` — *"(Consistent with :80-83 below.)"*; the
  `NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT` sentence it is pointing at is at `:85` (`:80` is blank).

---

## Positively cleared (audited zeros)

**ENUMERATED** (predicate re-derived from source independently, cardinality compared before any
arithmetic):

- **Compose profile tokens** (`graphql-wundergraph.md:15-19`, `:190-192`; `next-web-app.md:86-90`;
  `studio-desk.md:21`). `grep -n "profiles:" docker-compose.yml` @ `0c91421` → 4 keys, distinct token
  set **{core, backend, all, studio-desk, frontend} = 5**, exactly as claimed. `graphql` appears in
  none. At `0dab54d` the set was 8 (**+ storage-legacy, customerio-sync, messenger**), and `838d907`
  (2026-08-05) deleted the three services that declared them — the doc's "read *the eight* until
  `838d907`" is exact. The always-on floor is derived, not asserted: `sentinel` (docker-compose.yml:5)
  plus `postgresql` and `redis` from the resolved `include: common.yml` declare **no `profiles:` key**,
  so a retired token starts those three. `backend`'s `profiles: [core, backend, all]` is at `:110`;
  `next-web-app`'s `depends_on: backend` at `:165-167`; `Makefile:10` = `PROFILE ?= core`;
  `Makefile:119-120` = `up-frontend` → `--profile core --profile frontend`.
- **The subgraph ladder** (`graphql-wundergraph.md:41-53`, `:160-164`). Counted `- name:` in
  `supergraph-config-prod.yaml` at each named commit: `749dc86^` = **5** (backend, skiller,
  jobsimulation, cms, skillpath) · `749dc86` = **4** · `7c17e63` = **3** · `915da06^` = **3** ·
  `915da06` = **1**. Dates 2026-06-24 / 2026-07-21 / 2026-07-29 all match. `915da06`'s subject really
  does say *"supergraph 2→1"* and really is wrong. `git show --name-status 915da06` marks
  `schemas/cms.graphqls` and `schemas/jobsimulation.graphqls` both `D`. The one commit that would have
  taken jobsimulation out earlier (`508ea37`, *"supergraph 3->2"*) lives **only** on
  `remotes/origin/feat/cms-in-app` — `git branch -a --contains` confirms it never reached `main`, so
  the 3→1 claim survives the obvious counter-example.
- **`STORAGE_RPC_ADDR` in Go @ `9d00a313`** (`academy-backend.md:59-60`): `git grep -n
  "STORAGE_RPC_ADDR" 9d00a313 -- '*.go'` → exactly **3**, all comment lines
  (`internal/jobsimwiring/wiring.go:101`, `internal/storagens/callsites_test.go:189`, `main.go:451`),
  zero reads. Note the wider tree still carries it in `knowledge/plan*/**` markdown, which is not Go.
- **The academy table set** (`academy-backend.md:61-74`): 11 Ent schema files
  (`internal/data/ent/schema/academy_*.go`); **6** carry `UserMixin{}` (bookmark, certificate,
  chapter_progress, chapter_time, feedback, last_activity) and **5** do not (chapter, chapter_body,
  path_embedding, series, skill_path) — exactly the doc's per-user/catalog split. All 11 plural table
  names confirmed against `CREATE TABLE` in `terraform/migrations/`.
- **The four required-and-undefaulted columns** (`hiring.md:232-234`): read the whole
  `20260722104506.sql:2-27` DDL. NOT NULL without DEFAULT = `owner_id` `:6`, `sim_id` `:7`, `sim_type`
  `:10`, `token` `:13` — **4**, and every other NOT NULL column does carry a DEFAULT. `token`'s UNIQUE
  index is `:29`. This is the conjunction rule-17 case; I tested both clauses.
- **The completion-status enum** (`hiring.md:227-229`, `:245-247`): `enum/jobsimulation.go:29-35` is
  the 5-const block, `Values()` at `:37-43`; the SDL declares five lowercase members at
  `jobsimulations.graphqls:14` **and** `:128`; `ent/jobsimulationsession.go:181-186` casts
  unconditionally; `graph.go:129546-129554` and `:129392-129400` are bare `MarshalString` passthroughs.
  No CHECK on the column in the DDL. Every link of the "nothing catches it" chain verified.
- **The 8 generic seeders** (`hiring.md:405`): `TestGenericActivitySeeders_SkipHiringOrg`'s table has
  exactly 8 rows — jobsim-sessions, activity, skillpath-sessions, assignments, hero-activity, persona,
  feedback, succession.
- **`repos.yml` cardinality** (`hiring.md:35`, `:179`, `:329`): **6** `- name:` entries at `0dab54d`
  (app, sentinel, storage, messenger, next-web-app, studio-desk) → **4** now; `app` at `repos.yml:14-17`
  is the only `migrations: true`. `d11a403` really did delete cms + jobsimulation + roadrunner from
  *both* compose and repos.yml (9→6 entries), despite a subject line naming only roadrunner.
- **`SET "score"` = 0 across the migration set** and **`CREATE SCHEMA` = `auth` only**
  (`hiring.md:24`, `:40`, `:330`) — both re-derived over `terraform/migrations/`, with
  `20230817154747_supabase_baseline.sql:2` as the single hit. `atlas.hcl:8` does pin
  `search_path=public`.
- **studio-desk's prod entry set** (`studio-desk.md:34`): `vite.config.ts` `rollupOptions.input` has
  exactly the 9 named entries plus a production-excluded `dev-accept` — enumerated, not sampled.
- **studio-desk has no datastore** (`studio-desk.md:279-287`): the DB-driver grep over `package.json`
  returns 0 with `"express"` as the positive control in the same pass; `DATABASE_URL` 0 hits in
  `src/` and `app/`; `GET_/SET_USER_STUDIO_PREFERENCES` confirmed in
  `app/services/{queries,mutations}.ts` + `userPreferencesService.js`; `main.ts:105` is the GlitchTip
  ingest comment verbatim.
- **studio-desk locales = 7** (`studio-desk.md:120`): `app/public/l12n/` holds de, en, es, fr, it, ja,
  nl. **next-web locales = 8** (`next-web-app.md:44`, `:124`): `configs/i18n/messages/` adds `pt`.
- **`GCLOUD_SERVICE_ACCOUNT` is vestigial** (`studio-desk.md:107`, `:199`): declared at
  `.env.example:120` and `terraform/main.tf:129`, **0** reads in `src/` or `app/`.
- **The three studio prod-eject hardcodes** (`studio-desk.md:313-316`): scoped to the scaffold, exactly
  3 — `pageWrapper.js:149` (logo), `userProfile.js:148` ("Back"), `userProfile.js:302` (logout).
  (Other `app.anthropos.work` literals exist outside the scaffold; the doc's claim is scaffold-scoped
  and correct.)

**Exact line/construct anchors opened and confirmed** (each read with surrounding context per §5
rule 10, not grepped-to):

- `graphql-wundergraph.md`: `terraform/main.tf:20` `service_desired_count = 1`; `locals.tf` project
  `wundergraph` / eu-west-1 / port 8080 / `/health`; `ci/update-subgraph.sh:9` — exactly one
  `gh release download`, `-R anthropos-work/app`; router `0.275.0`, `wgc@0.104.0`, `node:22.11-alpine`,
  `federation_version: =2.3.2`; `package.json` = `{"name":"graphql-wundegraph"}`; the repo `CLAUDE.md`
  misspelt heading + stale "Version Tracking"/`-local.yaml` lines; `listen_addr 0.0.0.0:8080`,
  `graphql_path /graphql`, playground/introspection on in `.compose` and off in `.prod`, 35MB body;
  `grep -c 5050 docker-compose.yml` = 0; no `subscription:` block in any
  `supergraph-config-*.yaml`; `schemas/backend.graphqls` has `type Mutation` (:4053) + `type Query`
  (:4912) and **no** `type Subscription` (the hits are `activeSubscription` / `PlanSubscription` /
  `stripeSubscriptionId`); `bba862f` (2026-02-25) exists only on `remotes/origin/feat/use-web-socket`;
  the historical routing URLs `http://jobsimulation:8400/query` and `http://cms:8090/query` @
  `915da06^`; the full compose-history claim (`63d285c` no `dockerfile:` key from a `git@…` context →
  `a2a3ee6` `../graphql-wundergraph` → `2c85211` adds `Dockerfile.dev` → `67ba772` context `..` →
  deleted at `360efd4`/`2adcf71`, five months on `Dockerfile.dev`); the final `depends_on` before
  deletion really was `backend, jobsimulation, cms, storage` (`a4db680`); `gqlauthz.go:186` is the
  rejection and `:175-177` the exemption ladder, with `isIntrospectionOnlyQuery` accepting only
  `__schema`/`__type` (so `__typename` is genuinely excluded) and both named tests present;
  `backend.go:315` mounts the Apollo Sandbox on `/graphql` and `:317` the graph handler on
  `/graphql/query`; `/api/health` at `backend.go:140`; self-citation `:193` resolves.
- `next-web-app.md`: `next ^16.2.7` + `react ^19.2.7` in all four of web/hiring/integration/maintenance
  (mobile has neither), `node >=24.0.0`, `packageManager pnpm@10.30.3`, `turbo ^2.9.6`;
  `repos.yml` type `node-pnpm`; ports 3000/3001/3002 from each `dev` script and 3031 from
  `expo start --port 3031`; mobile excluded in `pnpm-workspace.yaml`; `graphql-request` + TanStack
  Query and no Apollo anywhere; `proxy.ts` exists in web/hiring/integration and `middleware.ts` does
  not; the public allowlist members all present; `PRINT_ROUTE_SECRET` in
  `packages/core-js/src/security/printToken.ts`; `/logpoint/*` rewrites in `next.config.mjs`;
  one root `Dockerfile.dev` on `node:24-alpine` building
  `pnpm turbo build --filter=@anthropos/web-app`; no storybook script or `.storybook/`, only
  `configs/tailwind/storybooks.css`; repo `CLAUDE.md:15` = "Next.js 16 App Router" and `:55` = the
  `proxy.ts` rename note; jest only in `apps/web` + `apps/hiring`; compose build arg `:151` / env
  `:160` @ `0c91421`, arg was `:236` @ `0dab54d`.
- `academy-backend.md`: `0e37771f` = PR #903, 2026-06-05, *Academy backend v1.0 "ground truth"*;
  `v1.369.0` at origin/main `2035f9a4` (`git describe` = `v1.369.0-2-g2035f9a40`), and `:57`'s
  `9d00a313` = `chore(version): v1.367.0`; `main.go:471-472` @ `9d00a313` constructs
  `NewManager(storageBucket)` / `NewPublicManager(storagePublicBucket)` from
  `internal/storage/service.go:22,24` = `STORAGE_S3_BUCKET` / `STORAGE_S3_PUBLIC_BUCKET`;
  `cmd/academyImport/main.go:239-243` carries the quoted comment + the `Getenv`, hard-erroring at
  `:244-246`; all named managers/files exist; every listed query and mutation exists in
  `academy.graphqls`; `/content/catalog.json` (`content.go:23`) and the
  `/content/admin` group guarded by `ACADEMY_CONTENT_API_TOKEN` (`content_admin.go:35`);
  `academy_embedding_refresh` registered `"0 3 * * *"` (`worker/worker.go:158`); cert regex
  `^AAS-\d{4}-[A-HJKMNP-TV-Z2-9]{6}$` = a 30-symbol alphabet, `crypto/rand`;
  `extensions.vector(1536)` + `source_hash`; `paid` `Default(true)`, optional `tenant_eid`,
  `lifecycle`; last-activity per-user unique; locale set `en` + de/es/fr/it/nl/pt;
  `SUPABASE_DB_CONN` and every `academy-seed` flag.
- `hiring.md`: `20260729133514.sql:58` = *"5. Drop the mirrors."* and `:62` = the `DROP TABLE`;
  `20250416091037.sql:5` = the `anticheat_summary` column on the mirror; `20260722104506.sql:79` =
  `DROP TABLE "sessions"`, `:2` = the new table, `:53` = the anticheat FK re-point, `:12` =
  `completion_status`; `20260722081626_jobsim_data_model.sql:336/355/376` = the three `validation_*`
  tables in that order; `resolver_queries.go:1034/1035/1053/1080` and the neighbour's `:1085/:1089`;
  the `grep -in hiring` result (4 sim-type hits in the resolver, **0** in `intelligence.go`) with the
  document's own positive controls reproducing exactly (`OrgFeatureInsights` ×8,
  `JobSimulationSession` ×44); `intelligence.go:1700` (`m.ent.JobSimulationSession.Query()`), `:1820`
  (`RoundFloat(float64(ls.Score),0)`), `:1844`, `:1846`, `:885-886` (`insightsSortColumn` mapping
  `Completition` → `FieldCompletionStatus`); the default sort really is score DESC /
  completition_status ASC / session_started_at DESC (`:1751-1764`);
  `job_simulation_session.go:45` = `field.Float32("score").Default(0).Min(0).Max(100)`, `:39` =
  completion_status, and `local_jobsimulation_session.go` no longer exists;
  `anticheat_result.go:24` = `field.Enum("summary")` and the session schema declares no anticheat
  field; `askengine/registry.go:192` is the M710 comment inside an alias map that resolves to public
  tables; `organizations.is_hiring boolean NOT NULL DEFAULT false` (renamed from `is_demo`);
  `useGetClerkOrganization.tsx:16-18` and `:20-21` (the quoted code block is byte-accurate);
  `apps/web/.../UserStatusContext.tsx:141-173`, `:144-149`, `:168-172`;
  `apps/hiring/.../UserStatusContext.tsx:125` and `:144-145`; `useNavbarSections.tsx:459-466` and
  `:460`; `template.tsx:90`; `FreeTrialContainer.tsx:29`; `simulationScoreColumn.tsx:54` and
  `:95-97`, and the two copies really are byte-identical (same sha1);
  `packages/graphql/src/query/insights.ts:31-82`; `InsightsByMembersContainer.tsx:359` = `<Drawer` in
  `apps/hiring`; **zero** `(.)`/`(..)` intercepting-route dirs anywhere under `apps/`;
  `queries.graphqls:189-193` declares `insightsJobSimulationByMemberships` with no directive;
  no `isHiring` reference anywhere under the `activity-dashboard` route tree;
  rext `persona_write.go:91` + `sessionCols()` at `:152` (with `token` at `:155`),
  `hiring_config.go:99` `CopyRowsIdempotent(… "organization_sim_invitation_links" …)`,
  `hiringComparableFloor = 6` (`contentref.go:158`) matching `RENDER_GATE_FLOOR ?? '6'`
  (`render-hiring-comparison.spec.ts:56`), `RoleMix{Admin, Member, Candidate, AdminEmails}`,
  both `next-hiring-*` patch dirs.
- `studio-desk.md`: compose `:138-140` depends_on backend + `:141` profiles @ `0c91421`, `:223-225`/
  `:226` @ `0dab54d`, and the cms entry at `:337-341` @ `2adcf71`; build arg `:119` / env `:135`, was
  `:204` @ `0dab54d`; `engines.node >=24` + `node:24-alpine`; jest `projects` = backend + frontend;
  `src/routes/skillpath.ts` is 61,080 bytes (the largest route) and `youtube.ts:43` reads
  `YOUTUBE_API_KEY` with a `_mock: true` fallback; `directus_versions` + `pingSnapshotCapability`
  (`skillpath.ts:709`) + `Bearer ${token}`; `FORCE_READ_ONLY`; Bunny library vars;
  `YouTubePicker.js`; `AI_DEFAULT_TIER || 'thinking_fast'` (`ai/config.ts:182`) vs `.env.example:61`
  `fast`; the legacy chain really is Azure→OpenAI→Anthropic (`config.ts:166-171`); tier model maps
  `gpt-5.2 / gpt-5-mini / gpt-5-nano` and `claude-opus-4-5 / claude-sonnet-4-5 / claude-haiku-4-5`;
  the rotate-to-end circuit breaker (`providerHealth.ts`); `src/index.ts:60`
  `process.env.PORT || 9100`; `STUDIO_ACCESS_ROLES` at `src/index.ts:96` with the four exact role
  keys and the `WEB_APP_URL` redirect at `:119-120`; `main.ts:97` `preloadCriticalCSS()`, `:182`
  `clerk.load()`, `:191` `l12nService.init()`, `:199` `canAccess()`, `:206` `new PageWrapper()` — the
  ladder is in the stated order; `PageWrapper` wipes `document.body.innerHTML` (`pageWrapper.js:122`);
  `config.WEBAPP_URL` ← `VITE_WEB_APP_URL`; all five `studio-desk-*` demopatch dirs;
  `frontend_lines()`'s M252 existence-guarded `env_file`; `build_frontend_studio_desk` in
  `up-injected.sh:881`; `run-studio-fcp.sh`; autoverify's studio-desk container key check
  (`autoverify.sh:528-543`).

**RE-DERIVED only** (weakest clearance — recorded as such): none. Every figure above was
re-enumerated from source rather than re-summed from the document's own set.

---

## What I could not settle

- **GitHub archive states.** `graphql-wundergraph.md:8` (*"the repo ARCHIVED on GitHub, 2026-07-30
  (read-only)"*) is not decidable from a clone — `gh` is not installed on this host and archive status
  lives in the org API. `service_taxonomy.md:139` (outside my file set) independently says archive
  state "is not visible to this corpus", which makes `graphql-wundergraph.md:8`'s flat assertion the
  odd one out. Settled by one `gh repo view anthropos-work/graphql-wundergraph --json isArchived`.
- **Captured-snapshot cardinalities** (`hiring.md:153-158`): "87 real `SIMULATION_TYPE_HIRING` sims",
  "0 `job_position` rows", "the prod 443 was never captured". These live in the `.agentspace` snapshot
  cache, not in any platform clone. Settled by `stacksnap status` / a manifest read.
- **Live measurements**: `hiring.md:379-418` (5/5 render, per-position counts 8/8/9/9/8, p95 1.27 s),
  `studio-desk.md:412` (4669 → 817 ms), `:423` (4049 → 38 ms, FCP 6936 → 2152 ms),
  `academy-backend.md:121-124` (the demo FS-catalog caveat). All require a running stack.
- **`hiring.md:344`** — *"the org's `admin` members inherit `org:feature:insights` from the global `p3`
  admin policy via their standard g2 grant"*. Needs the seeded `casbin_rules` table; not derivable
  from `sentinel`'s source alone within this budget.

### Out-of-scope observation (not booked — the file is not in my partition)

`graphql-wundergraph.md:15-19` is **correct** that only five profile tokens survive, and I verified it
from compose. Root `CLAUDE.md`'s profile table still lists `storage-legacy`, `customerio-sync` and
`messenger` as live rows (and the "⚠️ Storage and Messenger" banner still says `storage` "sits in
`profiles: [storage-legacy]`" and `messenger` in `profiles: [messenger]`, "both kept startable only as
rollback paths"). At platform `0c91421` those three service blocks were deleted outright by `838d907`,
so none of the three tokens selects anything. That is a live self-contradiction between my assigned
file and the repo's highest-propagation instruction file — flagged here per §5 rule 19's corollary
rather than booked, since `CLAUDE.md` is outside this reading's scope.
