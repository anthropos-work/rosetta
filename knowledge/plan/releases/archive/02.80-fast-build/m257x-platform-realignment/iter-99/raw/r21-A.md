# Seat A — reading #21

Ground truth re-verified at this reading's open (all shas match the ADDENDUM):
`stack-demo/platform` **`0c91421`** (supersedes the frozen table's `0dab54d`), `app` `b948604ff`,
`app/studio` **`aeec036`** (own checkout), `cms` `ca50c81`, `cms/studio` `aeec036`,
`graphql-wundergraph` `60c229f`, `jobsimulation` `462343b0`, `messenger` `fa47850`,
`next-web-app` `bb3313bc0`, `roadrunner` `87d8d44`, `sentinel` `88bc559`, `storage` `4ce8ece`,
`studio-desk` `14a5442`, `ant-academy` `9c3843cd`, `stack-demo/rosetta-extensions` `ab81527`
(tag `fast-build-m257x-iter-58`), `.agentspace/rosetta-extensions` `5fb0915` (authoring, `main`).

Refs used for pinned claims: `app` `9d00a313` (v1.367.0) and `app` origin/main `2035f9a4`
(93 commits ahead of the checkout); `messenger` origin/main `e9421c6`; `next-web-app` `ad767d1b`.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| corpus/architecture/external_services.md | 883 | yes |
| corpus/services/chronos.md | 245 | yes |
| corpus/services/messenger.md | 189 | yes |
| corpus/architecture/frontend_architecture.md | 105 | yes |
| corpus/services/gotenberg.md | 84 | yes |
| (protocol, read as instructed) corpus/ops/platform-alignment.md §5 | 2232 | §5 in full |

Total assigned = 1506 lines. Every file opened and read top-to-bottom.

---

## Blockers

### B1 — `chronos.md:27` states ports 8080/8081; the service's only measurable local deployment published 8500/8501

- **Anchor:** `corpus/services/chronos.md:27` — *"**Ports**: `8080` (HTTP/health), `8081` (RPC)"*,
  and the same figures reappear in the Environment Variables table at `:198-199`
  (`PORT` … `8080`, `RPC_PORT` … `8081`) and `:202` (`REDIS_STREAMS_INDEX` … `2`).
- **Why it is false (or at minimum unsupportable as written):** the only ground truth that exists for
  Chronos anywhere in the clone set is the compose block platform `045857c` deleted. At
  `045857c^`, `docker-compose.yml:339-368` publishes **`"8500:8500"` and `"8501:8501"`** and sets
  **`PORT=8500`**, **`RPC_PORT=8501`**, **`REDIS_STREAMS_INDEX=4`**, and
  `CHRONOS_RPC_ADDR=http://chronos:8501` on its consumers (visible in the `045857c` deletion diff).
  So every port a reader could ever have reached Chronos on locally was 8500/8501, and the doc names
  neither number anywhere.
- **Why I hesitated (stated per the briefing):** 8080/8081 are plausibly the *binary's built-in
  defaults*, and the doc is internally consistent with that reading (`:209` gives `--server` default
  `http://localhost:8081`). I cannot refute it directly, because `chronos` is in **no** clone set
  (`stack-demo` has 15 git trees; none is chronos) — so the binary is unmeasurable from here. What
  makes it bookable rather than merely thin is the contrast with the sibling doc: `messenger.md`
  handles exactly this situation correctly, giving a *"Value in the deleted compose block"* column
  (`:159-171`) **and** a separate *"the binary's built-in fallbacks"* sentence (`:173`).
  `chronos.md` collapses the two, under a heading (*Architecture & Code Map → Ports*) that reads as
  a statement about the deployed service.
- **Confidence:** medium

### B2 — `external_services.md:211` cites rext line anchors that name the wrong constructs in the designated ground-truth clone

- **Anchor:** `corpus/architecture/external_services.md:208-211` —
  *"`rosetta-extensions/stack-injection/gen_injected_override.py:669-670` re-points every service in
  `DIRECTUS_DATA_CONSUMERS`, which is **`("cms", "backend")`** (`:84`)"*.
- **Why it is false:** the briefing's ground-truth table names **`.agentspace/rosetta-extensions`
  (authoring copy, `main`)** as the rosetta-extensions witness, and §5 rule 36 makes the authoring
  copy authoritative when a repo is cloned twice. Measured there (`5fb0915`):
  - `DIRECTUS_DATA_CONSUMERS = ("cms", "backend")` is at **`:86`**; `:84` is a comment line
    (*"# studio-desk is wired separately (its own DIRECTUS_BASE_URL …)"*).
  - the re-point (`if with_directus and name in DIRECTUS_DATA_CONSUMERS:` /
    `env.append(f"DIRECTUS_BASE_ADDR={DIRECTUS_INNETWORK_ADDR}")`) is at **`:698-699`**; `:669-670`
    is 29 lines away, inside the **backend CORS** comment block
    (*"… CORS_EXTRA_ORIGINS in non-production (a runtime hook, no platform change) …"*).
  The cited numbers are exact in the *other* clone — `stack-demo/rosetta-extensions` @ tag
  `fast-build-m257x-iter-58` has the tuple at `:84` and the re-point at `:669-670`.
- **Why I hesitated:** two clones, one witness (§5 rule 36). The citation is right in the pinned
  per-stack copy and wrong in the authoring copy the briefing designates. `:84`→`:86` is 2 lines and
  harmless; `:669-670`→`:698-699` lands on an unrelated construct, which is rule 21's
  *"resolves but names the wrong construct"* class.
- **Confidence:** low

---

## Minors

- `external_services.md:849-852` — the bash comment block opens `(docker-compose.yml:101-109 — …` and
  never closes the parenthesis before `docker compose ps postgresql redis sentinel`. Cosmetic; the
  cited range itself is correct (`:101` `depends_on:`, `:104-109` redis/postgresql/sentinel @ `0c91421`).
- `external_services.md:10` — *"integrates with **four key external services**"* while the page
  documents six (Clerk, Directus, GraphQL gateway, AI providers, **LiveKit**, **AWS Chime**).
  Long-standing framing, no factual consequence.
- `external_services.md:703` — *"(`AI_PROVIDER_CHAIN`, default `azure-openai,openai`)"*. When the var
  is unset, `studio-desk/src/services/ai/config.ts:166-171` pushes **azure, openai and anthropic**
  (`if (anthropic) providerChain.push(anthropic);`). Omitted list member.
- `external_services.md:636` — cites `config_template.ini:30-31`; the file is
  `app/studio/**configs/**config_template.ini` (the two lines are correct there:
  `OPENAI_API_KEY = sk-aaa` / `OPENAI_ENDPOINT = https://api.openai.com/v1/chat/completions`).
  Path prefix dropped.
- `external_services.md:455` — presents a *quotation* of `graphql-wundergraph/CLAUDE.md:39` that is a
  paraphrase. The line actually reads *"**The committed `schemas/backend.graphqls` is now
  load-bearing locally too.** Since the compose stack builds from the production Dockerfile, a schema
  change in `app` does **not** appear at `:5050` …"*. Anchor and substance are right; the quote marks
  are not earned.
- `messenger.md:108` — *"the binary still constructs four Connect-RPC clients"*. `cmd/root.go:118-142`
  constructs **five** (`cmsV1Client`, `usersClient`, `organizationsClient`, `skillerClient`,
  `jobsimulationClient`) reading **four** distinct addresses. The doc's own enumeration
  ("backend users + organizations") shows it means edges, but the noun is wrong. `:28`'s
  *"four addresses"* is exactly right.
- `frontend_architecture.md:39` — *"Stripe (`useStripe.tsx`)"*: the `apps/hiring` twin is
  `useStripe.**ts**`. The same enumeration omits three of the 21 files it is enumerating —
  `apps/web/src/app/(unauthenticated)/reminders/[token]/unsubscribe/page.tsx`,
  `packages/graphql/src/hooks/user/useManageSubscription.tsx`, and
  `packages/ui/src/AISimulation/Onboarding/TestRuns/TestRunNetwork.tsx`.
- `frontend_architecture.md:29-35` — the Core Packages table omits `packages/design` (5 packages
  exist: `core-js`, `design`, `graphql`, `ui`, plus the `configs/*` two the table does list).
- `chronos.md:22`, `:171`, `:190` — `stack-dev/chronos` / `cd stack-dev/chronos`. `stack-dev/` on this
  box holds only `studio-desk` and `studio-room`; the doc's own banner (`:9`) says `make init` no
  longer clones chronos, so the runnable `cd` is dead as written.

---

## Positively cleared (audited zeros)

**ENUMERATED** — predicate re-derived from source independently, cardinalities compared:

1. **`core` starts five containers** (`external_services.md:169-175`). Derived the always-on floor as
   *services declaring no `profiles:` key*, resolving `include: common.yml`: `docker-compose.yml`
   @ `0c91421` declares `sentinel`(:5), `backend`(:28), `studio-desk`(:112), `next-web-app`(:143),
   `gotenberg`(:170); `profiles:` keys exist only at `:110`, `:141`, `:168`, `:183`. Floor =
   {postgresql, redis, sentinel}; `core` members = {backend, gotenberg}. **5**. The cited anchors
   `docker-compose.yml:110` and `:183` are the two `profiles: [core, backend, all]` lines — right
   construct.
2. **Compose service census** (`external_services.md:305-306`): **5 declared / 7 effective** at
   `0c91421`; **8 declared / 10 effective** at `0dab54d` (sentinel, backend, storage,
   customerio-sync, messenger, studio-desk, next-web-app, gotenberg). Both figures exact.
3. **Supergraph ladder** (`external_services.md:376-380`). `git show <sha>:supergraph-config-prod.yaml`
   per commit: `749dc86~1` = 5 (backend, skiller, jobsimulation, cms, skillpath) → `749dc86`
   (2026-06-24) = 4 → `7c17e63` (2026-07-21) = 3 → `915da06` (2026-07-29) = 1. `915da06`
   `--name-status` marks `schemas/cms.graphqls` **and** `schemas/jobsimulation.graphqls` `D`.
   **3 → 1 confirmed; the commit subject's "2→1" is indeed wrong.**
4. **No subscriptions** (`external_services.md:387`). `grep -rn "sse\|subscription" *.yaml` → rc 1,
   nothing (positive control `grep -rln backend *.yaml` → exactly the three supergraph configs).
   `schemas/backend.graphqls` (5795 lines): `type Mutation` at **:4053**, `type Query` at **:4912**,
   no `type Subscription`; the `Subscription` substring occurs on exactly **6** lines
   (1528, 1555, 4385, 4389, 4391, 4418), all Stripe/plan names. `git log -S 'protocol: "ws"'` → 0
   commits; `-S 'sse_post'` → **5**; `git merge-base --is-ancestor bba862f HEAD` → rc **1**;
   `git branch -a --contains bba862f` → `remotes/origin/feat/use-web-socket` only.
5. **Messenger's `backend` subscriber** (`messenger.md:17`). `internal/flow/flow.go:72-104` @ `fa47850`
   is exactly the `AddSubscriber("backend", …)` block; it contains **22** `pubsub.EventHandler(`
   lines (74-78, 80-84, 86, 89, 91, 92, 94, 96-99, 101-103) of which **one** (`:83`,
   `OrgJobSimulationAssignmentPastDueHandler`) is commented out → **21 live**. `:74-78` are exactly
   the five `OrgSkillPath*` handlers, as `:108` claims.
6. **`MESSENGER_RPC_ADDR` occurs in no repo** (`messenger.md:20-22`). Looped all **15** git trees
   found by `find stack-demo -name .git -maxdepth 4`, grepping each at **its own** HEAD (so both
   nested `studio` checkouts @ `aeec036` are covered): `MESSENGER_RPC_ADDR` → **0** everywhere. The
   only `MESSENGER_RPC` substring hits anywhere are 2 prose lines in `app`'s own
   `knowledge/plan/roadmap-support-in-app.md` (a mention, not a client). Positive control
   `SKILLER_RPC` in the same loop: app 22, cms 12, jobsimulation 9, messenger 4, platform 1,
   rosetta-extensions 17.
7. **Platform history counts** (`messenger.md:23-26`). `git log -S 'MESSENGER_RPC' --oneline 0c91421d
   | wc -l` → **0**; `-S 'SKILLER_RPC'` → **7**; with `--all` → **8** and **0**, and the 8th is
   exactly `464dfe3` (`comm`-diffed). `git rev-list --count 0c91421d` → **121**.
8. **Zero `*_RPC_ADDR` at platform HEAD** (`messenger.md:30`, `:108`, `:153-157`;
   `external_services.md:855-865`). `grep -rn "_RPC_ADDR" docker-compose.yml common.yml .env_example`
   → rc 1 (positive control: `AUTHORIZATION_ADDRESS=http://sentinel:8087` at `docker-compose.yml:48`).
   At `0dab54d` all four sat on the `messenger` block, all `http://backend:8083`.
9. **Gotenberg call sites are exactly two** (`gotenberg.md:47`). `git grep -n ConvertToPDF 9d00a313 --
   '*.go'` → the definition plus **2** call sites: `internal/web/backend/coursebuilder/extract.go:77`
   and `internal/worker/tasks/user_import_resume_2d.go:68`. **Nine** gotenberg MIME types
   (`extract.go:17-27`, 9 map entries: xls, xlsx, ppt, doc, odt, ods, odp, `application/rtf`,
   `text/rtf`); DOCX (`…wordprocessingml.document`) is **not** among them, as claimed. `:77-81`,
   `:59`, `:43-45`, `:68-74`, `:69-74` all name the right constructs. 90 s client timeout confirmed
   (`internal/converter/gotenberg.go:13`).
10. **All 30 `*_AI_*_MODEL` lines pin `azure`** (`external_services.md:567`, `:619`, `:640`).
    `grep -rn "_AI_.*_MODEL" configs/*.ini` in `app/studio` @ `aeec036` → **30** lines, every one
    `= azure, …`. `bedrock|boto3` under `app/studio` → **0** (positive control: `mistral` = 22 lines
    across 3 files at that same ref — the number `platform-alignment.md` §5 rule 44 publishes).
11. **AI env-var counts 5 / 26 / 13 / 13** (`external_services.md:668-673`). Re-derived at the named
    ref `5ba17044` *and* at the checkout `b948604f`; identical at both. `OPENAI_ORG_ID`,
    `AZURE_OPENAI_ENDPOINT`, `AZURE_OPENAI_DEPLOYMENT` → **0**; `OPENAI_API_KEY` → **2**, both in
    `internal/cms/studio/studioManager.go` (:1049 comment, :1067 the subprocess remap) exactly as
    stated.
12. **Every Mistral use in `app` Go is OCR** (`external_services.md:597-599`). 8 matching lines, all
    under `internal/cms/studio/` (`markdownManager.go:11,19,21`, `studioManager.go:531,583,585,589`,
    `xlsx.go:13`). `gen.py` and `services/ai.py` contain **0** mistral references.
13. **Taxonomy figures** (`external_services.md:687-700`). Parsed
    `.agentspace/snapshots/taxonomy/5afc0bccf1df7ef538b643321fc6362f/manifest.json`:
    `public_only: true`, `predicate: org-null`, `captured_at 2026-06-29`; skills **42,790**,
    job_roles **22,470**, skill_embeddings **42,790**, job_role_embeddings **18,919**. Exact.
14. **`repos.yml` census** (`frontend_architecture.md:11`). **4** entries at `0c91421`
    (app, sentinel, next-web-app, studio-desk); **9** at `2adcf71` (adds cms, jobsimulation, storage,
    messenger, roadrunner). ant-academy absent at both.
15. **21 non-test files carry `NEXT_PUBLIC_BACKEND_API_URL`** at `bb3313bc`
    (`frontend_architecture.md:39`) — enumerated the file set from `git grep -l`, excluding tests,
    `.env.example`, `knowledge/`, `Dockerfile.dev`, `terraform`, `turbo.json` and `.md`: exactly 21.
    At the pinned `ad767d1b` the same derivation gives **22**, matching the doc's own forward note.
    Also confirmed `apps/web/src/hooks/useCoursebuilder.ts` carries exactly **1 NUL byte**
    (`tr -dc '\000' < f | wc -c`) and does **not** reference the variable, so the NUL trap does not
    hide a 22nd site here.
16. **d11a403 / 838d907 deletion sets.** `d11a403` (2026-08-03) removed the `cms`, `jobsimulation`
    and `roadrunner` compose blocks **and** their `repos.yml` entries (before/after service lists
    diffed) — the subject line names only roadrunner, so the corpus is right and the subject is not.
    `838d907` (2026-08-05, merged `0c91421`) removed `storage`, `messenger`, `customerio-sync` and
    dropped storage+messenger from `repos.yml`.

**RE-DERIVED / anchor-verified** (opened the file and read ±15 lines around every cited line):

- `graphql-wundergraph`: `terraform/main.tf:20` = `service_desired_count = 1`; `locals.tf:8` =
  `port = 8080`; `main.tf:48-49` = containerPort/hostPort; `config.prod.yaml:5` =
  `listen_addr: 0.0.0.0:8080`; `Dockerfile.dev:18` = the single schema `COPY`, `:19-20` the fold
  comment, `:23` the single `awk`; `subgraphs.conf` = `BACKEND=v1.360.0`; `schemas/` = `backend.graphqls`
  alone; routing_url `http://backend.internal.anthropos:8080/graphql/query`. All correct.
- `platform` history: `1e8e754` = `2adcf71^1`; its `docker-compose.yml:19-27` is the four-service
  `depends_on` (backend, jobsimulation, cms, storage), `:6-8` = `context: ..` /
  `dockerfile: graphql-wundergraph/Dockerfile.dev`, `"5050:8080"` at `:14`. `b56d731` keeps
  `  graphql:` at `:22` behind `profiles: [wundergraph-deprecated]`; `360efd4` has no such key
  (`  backend:` at `:28`). `2c85211^` has no `dockerfile:` key; `2c85211` adds it. `63d285c`
  (2024-06-20) names the service `wundergraph:` with `context: git@github.com:…#main`.
  `a4db680` (2026-07-21) is where the four-service list settles. `merge-base --is-ancestor 464dfe3
  0c91421d` → rc **1**. `a2a3ee6^:docker-compose.yml` `:384` `directus/directus:10.10.1`, `:386`
  `8055:8055`, `:409` `ADMIN_PASSWORD=password`; `admin@example.com` unfound in history — the
  M257x-iter-48 correction-of-the-correction is right on every particular.
- `app` @ checkout `b948604f`: `main.go:980-982` is the `DIRECTUS_BASE_ADDR` `log.Fatalf`;
  `main.go:770` the coursebuilder-backend boot log; `main.go:1205-1211` the *"additive + DORMANT …
  until the M809 re-point"* comment. `internal/jobsimulation/ai/ai.go` `:30-33` (4 vendor consts),
  `:80` (`openai.NewOpenAI`), `:130-141` (`isThrottlingError`), `:150-155` + `:296-302`/`:326`
  (429 → `vendor = Openai`), `:259-289` (`getClient`), `:264-276` (Azure EU→US flag),
  `:280-283` (both Anthropic arms → `anthropicClient`). `internal/cms/directus/collections/
  jobsimulation.go:905` (`AIVendor *AIVendor`), `:1302-1305` (`aiVendor := simulation.Openai`),
  `:1596` (nil `voice_engine` → `Gptrealtime`). `internal/jobsimulation/simulator/ai/ai.go:58-59`
  and `:114-115`. `internal/jobsimulation/calls/livekit.go:110`, `:115`, `:120`, `:126` — the
  agent-name claim (`anthropos-agent` EU / `-chain` / `-us`, no `-eu`) holds, and
  `flag_use_realtime_openai` (`:133`) changes agent endpoint, not engine. `internal/coursebuilder/
  bedrock.go:23`, `:29`, `:106-113`; `internal/askengine/bedrock.go:25`;
  `internal/jobsimulation/agent/report_agent.go:31`. `internal/web/backend/backend.go:130`
  (`/api/webhook/clerk` in the authn skip list) and `:315`/`:317` (Apollo Sandbox at `/graphql`,
  endpoint at `/graphql/query` — so `external_services.md:533` is right).
- `app/studio` @ `aeec036`: `services/ai.py:383`, `:627-664` (`AnthropicProvider`), `:704-724`
  (`get_client`, provider set `{openai, azure, anthropic}`); `gen.py:41-53` (env override of
  `*_API_KEY`/`*_ENDPOINT` only — never the service selector) and `:45-47`; `tools/pdf2md.py:24`
  (`from mistralai import Mistral`) and `:127` (`mistral-ocr-latest`); `tools/r3.py:139`, `:190`,
  `:199-206` (the step-2 dispatch).
- `messenger` @ `fa47850`: `cmd/root.go:63`/`:64` (8080/8081 fallbacks), `:107`
  (`REDIS_STREAMS_INDEX` default `2`), `:118-142` (the RPC clients), `:147`
  (`READONLY_DB_CONNECTION`); `internal/rpcsrv/rpcsrv.go:25-30` (both Unimplemented stubs);
  `internal/flow/jobsimulations.go:140-151` (the 2 h / 12 h staleness guards);
  `internal/flow/assignments.go:828` (the `h.cms.GetSkillPath` read inside `getSkillPath`);
  `go.mod` `getbrevo/brevo-go v1.1.3`, `osteele/liquid v1.8.1`, `go 1.25.0`; `REDIS_WORKER_INDEX`
  read **nowhere** (rc 1, positive control `REDIS_STREAMS_INDEX` matches 3 sites). Whitelabel first
  lands in `d2d41e1`, tagged `v0.34.0` — `:86`'s "v0.34.0" is right.
- `messenger` prod row (`messenger.md:42`) graded at the ref its own block names
  (`messenger e9421c6` = origin/main): `terraform/main.tf:29` **is** `service_desired_count = 0`
  and `:27-28` **are** the *"image and task definition stay declared … one-line revert plus an
  apply"* comment lines. At the older checkout `fa47850` the same file reads `= 1` at `:19` — so
  this row is only true at the ref it cites, and it cites it.
- `app` @ `9d00a313`: `main.go:1095` (`CMS_STREAM` publisher), `:1478-1484`
  (`streamNamesForSubscribers`). `app` @ origin/main `2035f9a4`: `main.go:15`/`:62`/`:63` (the three
  `internal/messenger/*` imports), `:295` (the `BREVO_KEY`-required guard whose `log.Fatalf` is
  `:296`), `:1416-1421` (the `cmp.Or(SERVICE_NAME, "messenger")` note), `:1450`
  (`pubsub.NewSubscriberServer` on `msgflow.ConsumerGroup`), `:1471` (`msgadapters.Wire`),
  `:1473` (`msgsender.NewFromEnv`), `env_guards.go:61` (`envMessengerEnabled = "MESSENGER_ENABLED"`).
  Noted: `MESSENGER_ENABLED` does **not** exist at `9d00a313` (added at `3df469da8`), so
  `messenger.md:53`'s unpinned `env_guards.go:61` resolves only at origin/main — consistent with
  §5 rule 41's `CITE_REF=auto` ladder, so not booked.
- `platform` @ `0c91421`: `docker-compose.yml:53` (`DIRECTUS_PUBLIC_BASE_ADDR`, inside the
  `environment:` block `:46-94`), `:57` (`GOTENBERG_URL=http://gotenberg:3200`), `:84-92`
  (the messenger/customerio "deliberately no variables here" comment), `:101-109` (backend
  `depends_on`), `:119`/`:135` (studio-desk build arg + runtime env), `:151`/`:160`
  (`NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT`), `:152`/`:161` (`NEXT_PUBLIC_BACKEND_API_URL`), `:141`
  (studio-desk profile; `:226` at `0dab54d`), `:183` (gotenberg profile; `:268` at `0dab54d`),
  `.env_example:91-92` (the only two `DIRECTUS_*` entries). `repos.yml:21-23` = the messenger entry
  at `0dab54d`. `045857c` (2026-04-17) and jobsimulation `09631fb2` (same day) both match
  `chronos.md:6-7` verbatim in substance. `6efa1d5` (2026-08-04) deleted cms's
  `build-production.yml` with the quoted M810 subject; `cms/terraform/main.tf:39` is
  `service_desired_count = 0`; jobsimulation `6092c6d2` (2026-08-04) does delete the whole
  `module "jobsimulation"` block (no `module`/`service_desired_count` remains at that ref) — the
  checkout `462343b0` predates it, and the corpus cites the sha, so the claim is graded where it
  is pinned and holds.
- `next-web-app` @ `bb3313bc`: app names/ports (`@anthropos/web-app` 3000, `@anthropos/hiring-app`
  3001, `@anthropos/integration` 3002, `@anthropos/mobile` `expo start --port 3031`),
  `!apps/mobile` in `pnpm-workspace.yaml`, `packageManager: pnpm@10.30.3`, `engines.node >=24.0.0`,
  `turbo ^2.9.6`, `typescript ^5.9.3`, `apps/web/package.json:46` = `"next": "^16.2.7"`,
  `UPGRADE-IMPACT-next16.md` present, `configs/i18n` has exactly the 8 locales named,
  `@anthropos/tsconfig` / `@anthropos/i18n` workspace names, `packages/graphql/codegen.ts`
  (schema `http://localhost:8082/graphql/query`, `documents: src/query/**`, output
  `./src/__generated__/`, `preset: 'client'`), `clean:quick`, and the four `check:*` turbo tasks.
- Clerk SDK map (`external_services.md:69`): `@clerk/nextjs` in web/hiring/integration and
  ant-academy, `@clerk/clerk-expo` in mobile, `@clerk/clerk-js` + `@clerk/express` in studio-desk,
  `clerk-sdk-go/v2 v2.7.0` in `app/go.mod`. `sentinel` has **0** Go references to webhooks
  (positive control: `casbin` matches 3 Go files), so `:791-792`'s *"not Sentinel, which … exposes
  no webhook route"* holds.
- Chronos: `app/internal/jobsimwiring/worker.go:24-35` is the asynq import + `StartWorkers` server
  construction, supporting `chronos.md:9`. `chronos` appears **nowhere** in platform HEAD's
  `docker-compose.yml`/`common.yml`/`repos.yml`/`Makefile`/`.env_example` (positive control:
  `gotenberg` = 4 hits), so `:186`'s *"no such service"* is right.
- Structural: all five files have balanced code fences (42/14/6/8/8); no doubled words
  (regex scan, 0 hits); `external_services.md`'s two self-citations `:543` and `:567` both resolve
  to the constructs they name.

---

## What I could not settle

- **`external_services.md:3`, `:368`-area — *"the repo is ARCHIVED on GitHub (2026-07-30)"*.**
  Archive state is a GitHub API fact; nothing in the clone set records it. The clone's newest
  commit is `60c229f` (2026-07-30), which is consistent but not proof.
- **`chronos.md:14-72`, `:74-160`, `:194-245` — the entire Chronos internals** (sqlc/Cobra,
  `search_path=chronos`, the `timers` DDL and its four indexes, `ScheduleTimer`/
  `CancelScheduledTimer`, `EventTimerWentOff`, the triggerer's `LIMIT 10` +
  `FOR UPDATE SKIP LOCKED` + 3 s timeout + 1 min Sentry heartbeat, and the binary's default ports).
  `chronos` is in **no** clone set — 15 git trees under `stack-demo`, none of them chronos, and
  `stack-dev/` holds only `studio-desk` and `studio-room`. Settling any of it needs a clone of
  `anthropos-work/chronos`. This is also what keeps **B1** at medium rather than high.
- **`frontend_architecture.md:39` — the scalar "29" (and "31")**, as distinct from the file counts
  I did enumerate (21 / 22, both exact). "Direct REST/SSE call" is not a mechanically decidable
  predicate from the source: `fetch(` in the four core-js clients alone totals 25
  (coursebuilder 18, credits 3, talkToData 3, workforce 1), which cannot be reconciled with the
  doc's *"12 sites between them"* under any obvious reading. Settling it needs the original
  derivation command, which the passage does not ship. Not booked — the doc pins its ref and warns
  the figure is not current — but the derivation is not reproducible as written.
- **`external_services.md:428` — the pre-`a4db680` `depends_on` history** (*"earlier it also named
  `skillpath`, `skiller`, `chronos`, `intelligence` and `simulator` in turn"*). I verified the
  a4db680 state and spot-checked the 2023-2024 log, but did not replay every one of the ~40
  compose commits to confirm the full ordering.
- **`external_services.md:175` — the cms prod side.** The doc itself declares this **UNMEASURABLE**
  (the `infrastructure` repo is in no clone set) and reports both facts without asserting either;
  I confirmed both halves it reports and agree the verdict cannot be reached from here.
- **`external_services.md:293-297` (prod Directus on S3/CDN), `:739` (Chime composited MP4 grid),
  `:727` (ElevenLabs credentials still wired)** — production-infrastructure claims with no artifact
  in any clone set.
