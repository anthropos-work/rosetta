# Seat E — reading #22

**Refs at this reading (re-verified at open, per §5 rule 26):** platform `0c91421d` (level with
`origin/main`), app `b948604f`, next-web-app `bb3313bc`, sentinel `88bc5592`, storage `4ce8ece5`,
messenger `fa47850d`, cms `ca50c817`, graphql-wundergraph `60c229f3`, roadrunner `87d8d443`,
jobsimulation `462343b0`, studio-desk `14a5442a`, ant-academy `9c3843cd`, nested
`stack-demo/app/studio` + `stack-demo/cms/studio` both `aeec036a`, `.agentspace/rosetta-extensions`
authoring copy on `main`. All shas confirmed by `git rev-parse`, not assumed.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/architecture/service_taxonomy.md` | 494 | yes |
| `corpus/services/clerkenstein.md` | 370 | yes |
| `corpus/architecture/security_compliance.md` | 281 | yes |
| `corpus/services/ai-labs.md` | 156 | yes |
| `corpus/services/clerk-integration.md` | 128 | yes |
| `corpus/services/db-backup.md` | 31 | yes |
| **total** | **1460** | |

Also read (in scope, protocol input): `corpus/ops/platform-alignment.md` §5 in full (`:249-1296`).

---

## Blockers

### B1 — "exactly one cross-process edge" / "a single service address" is false: compose also addresses `gotenberg`

- **Anchor:** `corpus/architecture/service_taxonomy.md:405` —
  > "**Synchronous**: HTTP RPC — and at platform `0c91421` there is **exactly one** cross-process
  > edge left, `backend → sentinel`. Compose sets a single service address,
  > `AUTHORIZATION_ADDRESS=http://sentinel:8087` (`docker-compose.yml:48`), and **zero `*_RPC_ADDR`
  > variables**."

- **Why it is false:** the `*_RPC_ADDR` half is correct (I re-derived it: **0** `*_RPC_ADDR` lines in
  `stack-demo/platform/docker-compose.yml` @ `0c91421d`, all four having left with the `messenger`
  block at `838d907`). The other two clauses are not.

  `docker-compose.yml:57` sets **`GOTENBERG_URL=http://gotenberg:3200`** — a second service address,
  pointing at a second in-compose service, on the same `backend` env block as
  `AUTHORIZATION_ADDRESS` at `:48`. It is consumed synchronously over HTTP by `app`:
  `app/main.go:244` (`GotenbergURL: os.Getenv("GOTENBERG_URL")`) feeds
  `app/internal/converter/gotenberg.go:31`
  (`http.NewRequestWithContext(ctx, "POST", gotenbergURL+"/forms/libreoffice/convert", &body)`),
  with a dedicated `gotenbergClient` at `:13`. A second reader exists at
  `app/internal/web/backend/coursebuilder/handler.go:244`.

  This is not an out-of-scope third-party detail: **this same document classifies Gotenberg as a
  Tier-1 Core Backend Service** (`service_taxonomy.md:81`, row *"**Gotenberg** | 3200 | Office-doc →
  PDF conversion | core, backend, all"*) and lists it in the `core` selection at `:79-81`, `:464`,
  `:487`. So under the document's own taxonomy, the section heading *"Core Services ↔ Core
  Services"* has **two** synchronous cross-process edges at `0c91421`, not one, and compose sets
  **two** service addresses, not "a single" one.

  Reader impact is real: this is the passage an engineer reads to learn what still talks to what
  after the folds, and it says the answer is one edge. It is also freshly-written text (it names the
  newest platform sha), which §5 rule 18 flags as the highest-risk class.

- **Confidence:** high on *"Compose sets a single service address"* (directly refuted by
  `docker-compose.yml:57`); medium on *"exactly one cross-process edge"*, because a reader could
  argue "RPC" is meant narrowly as Connect-RPC — but the sentence says *cross-process edge* and
  *service address*, and the file's own Tier-1 table puts Gotenberg inside the set.

### B2 — `make up-all` did **not** start "a second Brevo contact pusher alongside `backend`'s own"

- **Anchor:** `corpus/architecture/service_taxonomy.md:101-102` —
  > "`customerio-sync` was still in the **`all`** profile until the deletion, so `make up-all`
  > started a second Brevo contact pusher alongside `backend`'s own."

- **Why it is false:** it contradicts the sentence **three lines above it in the same blockquote**
  (`:98-100`): *"The last two stay **OFF** on a developer machine behind `MESSENGER_ENABLED` /
  `CUSTOMERIO_SYNC_ENABLED`, which compose deliberately does not set."* If `backend`'s own
  customerio-sync is off, `make up-all` started **one** pusher (the standalone container), not a
  second one alongside a running first.

  Measured in platform source, not inferred. `app` @ `origin/main` `2035f9a4`:
  - `env_guards.go:61-62` declares `MESSENGER_ENABLED` / `CUSTOMERIO_SYNC_ENABLED`; the block above
    them states *"So they are OFF unless switched on BY NAME."*
  - `resolveSubsystemSwitch` (same file) returns **false for unset when `deployed == false`**; only
    a deployed environment turns unset into an error.
  - `docker-compose.yml:56` sets `ENVIRONMENT=development` on `backend`, and `:84-92` records that
    compose deliberately sets neither switch.
  - `main.go` builds the manager only under the switch —
    `internal/customeriosync/doc.go:59` (*"The Manager is built in main.go only when
    `CUSTOMERIO_SYNC_ENABLED` says so"*), `main.go:394-396` (`if customerIOSyncEnabled { … }`).

  Timeline closes the last escape: `app` `3e5bc33ef` folded customerio-sync in and `3df469da8`
  gated it — **the same day, 2026-08-04** — while the container was deleted at platform `838d907`,
  **2026-08-05**. There was no meaningful window in which a stock `make up-all` ran two live Brevo
  writers.

- **Confidence:** medium. **Why I hesitated:** the sentence is a near-verbatim lift of platform
  `838d907`'s own commit message (*"`customerio-sync` was still in the `all` profile, so `make
  up-all` started a second Brevo contact pusher alongside backend's own"*), so the corpus is
  repeating upstream testimony rather than inventing a claim — but it repeats it **unattributed and
  as fact**, and `platform-alignment.md` §6/§5 is explicit that *a commit message is testimony, not
  evidence; grade a change by its diff*. It also describes a now-deleted service, so a reader cannot
  be harmed operationally — only in their hazard model, which is exactly what the paragraph exists
  to teach. Booked per the briefing's tie-break rule.

### B3 — a US DR site sits in the same document as "no customer data stored in US" and an EU-exit list that omits it

- **Anchors (both, since I cannot tell which side is wrong):**
  - `corpus/architecture/security_compliance.md:158` — table row `| **DR site** | US AWS region |`
  - `corpus/architecture/security_compliance.md:222-224` — *"No customer data stored in US **by
    default** — but the residency guarantee is contingent on `flag_use_azure_us` being off, and that
    is a runtime flag, not a build-time property."*
  - supporting: `:10` (*"EU data residency (primary)"*), `:7` (*"All customer data is stored and
    processed in **EU-West-1 (Ireland)** by default"*), and `:200-206`, which routes the reader to
    `external_services.md:602-607` for the **complete** enumeration of ways data leaves the EU —
    *"**Four** things can send a request outside the EU"* plus one latent fifth, **all five of them
    AI-provider paths**.

- **Why it is a finding:** §*EU Data Residency* presents a closed, counted account of US exposure
  (four live + one latent, all inference traffic) and attributes the single residency caveat to one
  PostHog flag. A **standing US AWS DR region**, declared two sections earlier in the same file, is
  a storage-side exposure that appears in neither the count nor the caveat — and the backup row
  directly above it (`:155`) lists only *S3, Azure, Hetzner (Germany)*. Either the DR row is stale
  and should go, or the residency section's enumeration is incomplete and its *"contingent on
  `flag_use_azure_us`"* framing is wrong. A compliance reader working from §*EU Data Residency*
  alone would form a materially incomplete picture, and this document explicitly invites that
  ("Check the flag before asserting residency").

- **Confidence:** low. **Why I hesitated:** I cannot settle it against ground truth — the
  `infrastructure` repo and the `db-backup` repo are in no clone set, so nothing here can measure
  whether the US DR region holds data or is a cold standby, and *"by default"* is a real hedge.
  I book it under the briefing's explicit self-contradiction clause (*"that is a finding even if you
  cannot tell which side is right — say so and cite both anchors"*) rather than as a measured
  refutation.

---

## Minors

- `corpus/services/clerkenstein.md:3` — header metadata is stale: **"Last updated: 2026-07-14"** and
  a Status line that stops at *"M218 the roster-aware fake BAPI"*, while the body documents M219
  (`:54-62`), M220 (`:305-313`), M224 (`:232-255`) and **v2.8 M256** (`:160-209`). No factual
  consequence; the body is the newer text.
- `corpus/services/clerkenstein.md:21` — the `rosetta-extensions` section list names six
  (`clerkenstein`, `demo-stack`, `stack-injection`, `stack-core`, `stack-seeding`, `alignment`).
  The authoring copy holds **eleven** code sections: those six plus `dev-stack`, `playthroughs`,
  `stack-secrets`, `stack-snapshot`, `stack-verify`. Incomplete enumeration presented as the list.
- `corpus/services/clerkenstein.md:105` — the `cmd/` row enumerates *"`mintpk` · `fake-fapi` /
  `fake-bapi`"*; `clerkenstein/cmd/` also holds **`jwtkey`** (4 dirs, not 3).
- `corpus/architecture/security_compliance.md:72` — cites `mixin.go:98` for `UserMixin`'s
  `Policy()`; the function is at **`:99`** (`:98` is its doc comment). One line high, right
  construct.
- `corpus/services/clerkenstein.md:308` — the range `clerk-frontend/server.go:35-67` stops one line
  short of `clerkJSClient`, the identifier the same sentence names (declared at `:68`; the block
  itself starts at `:35` and `clerkJSFetchTimeout = 15 * time.Second` is at `:59`).
- `corpus/architecture/service_taxonomy.md:3` — the top banner asserts the router repo *"is ARCHIVED
  on GitHub (2026-07-30)"* flatly, in a file whose own note at `:124-131` requires every archive
  date be read as *"asserted on, never as is"* and whose `:374` applies exactly that hedge to the
  identical fact. The note scopes itself to *"this table"*, so the banner escapes it — inconsistent
  hedging of one unmeasurable claim inside one file.
- `corpus/architecture/service_taxonomy.md:109-111` — *"**Base services (no profile, always on with
  any `make up`)**"* lists only PostgreSQL and Redis. The floor is **three** — `sentinel` also
  declares no `profiles:` key — as this same file states at `:80` and again at `:463`.

---

## Positively cleared (audited zeros)

### ENUMERATED — predicate re-derived from source, cardinality compared

**The multi-tenancy fence** (`security_compliance.md:66-137`, the one §5 rule 24 records as wrong in
four consecutive generations). Derived at `app` `b948604f`, in
`internal/data/ent/schema/`, *before* touching the document's arithmetic:

| predicate | derived | doc |
|---|---|---|
| `*.go` files | **139** | 139 ✓ |
| files declaring `ent.Schema` | **135** | 135 (and "4 declare no schema") ✓ |
| files matching `OrganizationMixin{}` (raw) | **30** | 30 ✓ |
| …live (excluding `user_resource.go:22`, commented out) | **29** | 29 ✓ |
| `OrganizationIDMixin{}` users | **7** — `category`, `jobrole`, `similarity`, `skill`, `specialization`, `studio_task`, `studio_document` | same 7, same names ✓ |
| `ent.Schema` ∖ either mixin, carrying `"organization_id"` | **18** | 18 ✓ |
| …minus `org_membership.go` (own `Policy()`) and `academy_feedback.go` (`UserMixin{}`) | **16** — name-for-name the 16 listed at `:92-97` | 16 ✓ |
| files declaring any `Policy()` | **4** — `mixin.go`, `org_membership.go`, `organization.go`, `user.go` | 4, same names ✓ |
| org-auto-filtering total | **29 + `Membership` + `Organization` = 31** | 31 ✓ |

I ran the document's own `comm`/`xargs` derivation **and** the independent enumeration, and then
tested the *conjunct* the sentence actually claims (*"no policy of any kind"*), which is where the
previous three generations failed. **The trap I hit and had to back out of:** a bare
`grep -l 'UserMixin{}'` matches **`CreatedByUserMixin{}`** as a substring, and it fires on
`category.go`, `jobrole.go`, `skill.go`, `specialization.go` — four of the seven `OrganizationIDMixin`
users — which would have made the "23 unpoliced" claim false. Re-grepped with a word boundary
(`grep -nE '(^|[^a-zA-Z])UserMixin\{\}'`): those four carry only `CreatedByUserMixin{}`, which
declares **no** `Policy()` (`skiller_mixins.go:99`; the only `Policy()` funcs in `mixin.go` are
`UserMixin` `:99` and `OrganizationMixin` `:126`, and `skiller_mixins.go` has none). Conversely
`academy_feedback.go:64` does carry the real `UserMixin{}`, so the document's exclusion of it is
correct. **The fence holds at every clause.** Line anchors also verified individually:
`mixin.go:126`, `org_membership.go:172-188` (`AlwaysDenyRule` at `:180`/`:186`), `organization.go:56`
+ `:96` (`rule.FilterSameOrganizations()`), `user.go:116`, `user_resource.go:22`,
`job_simulation_session.go:5`, `jobrole.go:18`, `category.go:15`.

**Clerk webhook event types = 12** (`clerk-integration.md:37`). Enumerated from
`app/internal/clerk/events/events.go:166-235`: `user.{created,updated,deleted}`,
`organization.{created,deleted,updated}`, `organizationInvitation.{accepted,created,revoked}`,
`organizationMembership.{created,deleted,updated}` — exactly 12, and exactly the four families the
doc names at `:79`.

**Clerk localizations = 8** (`clerk-integration.md:41`). Enumerated from
`next-web-app/apps/web/src/providers/Clerk.provider.tsx:10-17`: `deDE, enUS, esES, frFR, itIT, jaJP,
nlNL, ptPT`.

**`colony/authn` consumer set** (`clerk-integration.md:51`, `:91`). Measured per-clone with
`git grep -l` at each clone's own HEAD: **app 129 `.go` files**, sentinel **0**, storage **0**,
messenger **0**. Positive control in the same pass: `anthropos-work/colony` matches **13** files in
sentinel, so the pipeline was not silently returning nothing.

**`authn` as a standalone module is imported by nothing** (`service_taxonomy.md:489`). `go.mod`
checked in all seven Go clones (app, sentinel, storage, messenger, cms, roadrunner, jobsimulation):
`anthropos-work/authn` **0/7**; control `anthropos-work/colony` **7/7**.

**Compose topology at `0c91421d`** (`service_taxonomy.md:65-67`, `:104-111`, `:460-477`).
`docker-compose.yml` declares **5** services (`sentinel`, `backend`, `studio-desk`, `next-web-app`,
`gotenberg`); `include: common.yml` adds `postgresql` + `redis` ⇒ **7** effective. Services with **no**
`profiles:` key = `sentinel`, `postgresql`, `redis` ⇒ floor of **3**. Legal profile token set derived
from the four `profiles:` keys = **{`all`, `backend`, `core`, `frontend`, `studio-desk`}** — five
tokens, four services carrying a key. `core` ⇒ backend + gotenberg + floor = **5 containers**. `all`
⇒ + next-web-app + studio-desk, **no customerio-sync**. `Makefile:10` `PROFILE ?= core`.
`grep '_RPC_ADDR'` ⇒ **0**. `repos.yml` = 4 entries (`app` `:14-17` `migrations: true schema: public`;
`sentinel` `:18-20` `migrations: false`); `docker-compose.yml:18` carries `search_path=sentinel`.

**Deleted-service ports** (`service_taxonomy.md:93`). Read at `0dab54d`: storage `8300:8300` +
`8301:8301`, messenger `8200:8200` + `8201:8201`, customerio-sync `8080:8080`, and
`profiles: [customerio-sync, all]` — the `all`-membership claim is right even though the consequence
drawn from it (B2) is not. The four `*_RPC_ADDR` vars (`BACKEND_USERS_`, `CMS_`, `JOBSIMULATION_`,
`SKILLER_`) all sat on the `messenger` block, all at `http://backend:8083`.

**Supergraph ladder 5 → 4 → 3 → 1** (`service_taxonomy.md:387`). Re-derived per ref from
`graphql-wundergraph`'s `supergraph-config-prod.yaml`: `749dc86~1` = 5 (backend, skiller,
jobsimulation, cms, skillpath) → `749dc86` = 4 → `7c17e63` = 3 → `915da06` = **1**. So `915da06` is
the **3 → 1** step, and its own subject line ("2→1") is the wrong number — exactly as the corpus
says. Router port: `terraform/locals.tf:8` `port = 8080`, `terraform/main.tf:48-49`, `config.prod.yaml:5`
`listen_addr: 0.0.0.0:8080`, `terraform/main.tf:20` `service_desired_count = 1`. `5050` exists only as
the deleted local host mapping (`2adcf71~1:docker-compose.yml:14` `"5050:8080"`) and the repo's own
`Makefile:10` `docker run … -p 5050:8080`.

**Studio-Desk is frameworkless** (`service_taxonomy.md:192-195`): `grep -i 'react|vue|angular'`
`package.json` ⇒ **0 hits, exit 1**; `git ls-files | grep -cE '\.(tsx|jsx)$'` ⇒ **0**.
`STUDIO_ACCESS_ROLES` verified at `src/index.ts:96` **and** `app/services/userService.ts:16`, both
`['admin','org:admin','content_creator','org:content_creator']` with the "both forms are accepted"
comment (`clerkenstein.md:141`).

**`gen.py` registers exactly nine arguments** (`service_taxonomy.md:242-248`). Enumerated at
`stack-demo/app/studio` @ `aeec036a`: `gen.py:484-492` — `-i/--interactive`, `-m/--media`,
`-f/--force`, `--simid`, `--branch`, `--prompt`, `--annotations`, `--pipeline`, `--blueprint`;
`parse_argument` at `:18-28` uses `parse_known_args` and merges leftovers into the dict (`:22-27`).
No `--template`. Grepped the **nested repo at its own ref**, not the host.

**Studio-Room provider registry** (`service_taxonomy.md:218`): `services/ai.py:705-708` is exactly
the `providers = {openai, azure, anthropic}` map; `ai.py:1-2` imports only `openai`/`anthropic`;
`tools/pdf2md.py:24` is `from mistralai import Mistral`; `git -C stack-demo/app/studio grep -ci
mistral aeec036a` sums to **22 lines across 3 files** — the figure the corpus publishes, taken with
the nested repo's own ref.

**Alignment DNA gene/capability counts** (`clerkenstein.md:41-44`, `:74`, `:106`). Parsed the five
JSON DNAs directly: `clerk-2.6.0` **27 genes / 14 capabilities**, `clerk-js-5` **9 / 6**,
`clerk-multi-1` **9 / 5**, `clerk-deploy-1` **7 / 3**, `clerk-express-1` **13 / 5**. `alignment/`
holds `cmd/{clerkrun,deployrun,expressrun,jsfapirun,multirun}`, `dna/` (five files),
`golden{,-deploy,-express,-js,-multi}/`, `scripts/` — the layout exactly as tabled. `gate.sh:31`
default `95` (and `GATE_CRITICAL` 100). `clerk-js-5.json` has a `Me` capability
(`universal-user`/`unauthenticated`) and **0** occurrences of `organization_memberships` — the DNA
gap `:145-147` declares is real.

**Credits cost model** (`ai-labs.md:75-76`, `:71`): `creditCost` map at `cost.go:86-90` is
`{course.build: 5, course.refine: 1, course.translate: 1}` — I checked the **map**, not the doc
comment, because `cost.go:29`'s own comment says *"course.refine → 5 credits per refine turn"* and
is stale; the corpus reports the map, correctly. `purchasePackages` = starter 50 / team 200 /
scale 500 (`:200-204`); `MarginMarkup = 1.40` (`:133`); `PricePerCreditUSD = 0.45` (`:142`);
`DefaultSeedBalance int64 = 500` (`:231`, pinned by `cost_test.go:100`).

**Stripe webhook event set** (`ai-labs.md:109-111`): `internal/web/backend/api/api.go:319-327`
handles `customer.created` and `customer.subscription.{created,updated,deleted}` and nothing else;
`checkout.session.completed` returns **0** hits tree-wide in `app`. Credits REST surface
(`internal/web/backend/credits/handler.go:43-45`) is `GET /balance` + `GET /transactions` only —
`/credits/purchase` survives only in comments, so *"removed, Wave 13"* holds.

### RE-DERIVED (weaker — arithmetic/anchor confirmation only)

Anchor-only confirmations, listed so the report is honest about which clearances are cheap:
`jobsimulation/terraform/main.tf:15-40` @ `6092c6d2` (the M810 decommission comment enumerating
LiveKit/Chime/SSM/atlas — and `origin/main` does carry four commits dated 2026-08-04 including merged
PR #439); `cms/terraform/main.tf:39` `service_desired_count = 0` in a 191-line file, plus `6efa1d5`
merged `f38c0c4` 2026-08-04 with the quoted body; `roadrunner/terraform/main.tf:19` `= 1`;
`a2a3ee6~1:docker-compose.yml` `:384`/`:386`/`:409` (directus `10.10.1`, `8055:8055`,
`ADMIN_PASSWORD=password`); `app/internal/jobsimulation/ai/ai.go:85-95`, `:129`, `:166`, `:263-277`,
`:325`; `app/internal/coursebuilder/bedrock.go:100`, `:109-112`;
`.../validation/v3/validator/skills.go:53-64` + `:75`; `basevalidator/criterion.go:127`, `:168`,
`:428`, `:450-475` (+ `checkValidationBulk.tmpl:27` carries the quoted prompt, and `checkerEngines`
has **no read site** — 6 occurrences, all declaration/param/assignment/pass-through);
`useOrgSelection.tsx:94`, `useResolveActiveOrg.tsx:107`, `useActivateMembershipOrg.tsx:81` (all three
present in **both** `apps/web` and `apps/hiring`, so the `{web,hiring}` brace is right);
`metabase/route.ts:35`; `clerk-frontend/{registry.go,server.go,resources.go}` seat-model anchors incl.
`clientID: "client_clerkenstein"` (`:125`), `sessID = "sess_clerkenstein"` in `establishLocked`
(`:665`), `handleSelectIdentity` setting `signedIn=false; sessID=""` (`:646-647`),
`handleClient`/`handleToken`/`handleMe`/`handleSignOut` all taking `_ *http.Request`, and **zero**
`.Cookie(` call sites in `clerkenstein/` (positive control: 29 `func ` in that file);
all five `server_test.go` test names at `:256/:286/:390/:427/:461`;
`clerk-backend/store.go:138`/`:151`; `alignment/dna/clerk-2.6.0.json:131`;
`alignment/cmd/alignctl/run.go:134-135` (`ExitRegressed = 2`, `ExitUnmeasurable = 3`);
`ALIGN_DIR` default `$base/../../alignment` (`gate.sh:30`) with no `scripts/` dir in the rext
`alignment/` section; colony pins (`app` `v0.35.2`, `sentinel`/`storage` `v0.34.3`, clerkenstein
`v0.34.3`) and `app/go.mod:31` `clerk-sdk-go/v2 v2.7.0` at both `b948604f` and `5ba17044`;
Clerk SDK versions (`@clerk/nextjs ^6.39.2` × 4 surfaces; `@clerk/clerk-expo ~2.6.18` at
`next-web-app/apps/mobile/package.json:6` vs `~2.19.36` at `ant-academy/mobile/package.json:18` =
13 minors); ant-academy anchors `graphql/server.js:14,18`, `backendContent.js:36,102-103`,
`serverTenant.js:115-145`, `beacon/route.js:36,41-55`, `layout.jsx:132`,
`next-scaffold.test.js:106,111`, `react-compiler-config.test.js:41`, `agent.js:13` (`gpt-5.2`),
`proxy.js` (`clerkMiddleware`, `REQUIRE_ORGANIZATION_MEMBERSHIP`, `/no-organization`),
`ClerkSignInForm.tsx:52` (aborts on any non-`complete` status), next `^16.2.6` / react `^19.2.5` /
expo `~54.0.33`, dev port 3077; `cms_queries.graphqls:106` + `cms_mutations.graphqls:22`
(`archiveStudioTask`); `cms/studio/studioManager.go:119` (`studio/gen.py` exec);
`app/.github/workflows/build-production.yml:29` (`additional_repo: "anthropos-studio-room:studio"`);
`ant-academy/.github/workflows/deploy-academy.yaml`; all five labs/credits/org-subscription
migrations present by filename; `lab_session.go` (no `PrimaryKeyMixin`, `field.String("id")`);
`lab_body.go:13` (EN + de/es/fr/it/nl/pt = 6 overlays); `labs_budget.go` tiers + five `CapReason`s;
`spend_reconciler.go:60` 30 s; `labsapi/client.go:6` `:7070`; `cmd/labsdemo/main.go:115` `:8099`;
`stripe-go/v74` (`go.mod:62`); `rext demo-stack/patches/academy-fs-published-fallback`.
Every intra-corpus anchor these files cite also resolved to the construct named:
`service_taxonomy.md` `:67-68`/`:124`/`:137`/`:138`/`:139`; `external_services.md:567`, `:579`,
`:602-607`; `coursebuilder.md:48`; `frontend_architecture.md:11`; `studio-desk.md:20`, `:21`;
`platform-migration-status.md:89`; `latency-budget.md:416`; `cockpit-spec.md:414`;
`alignment_testing.md` §393 / §318; `shared_libraries.md#authn` (:144) and `#taxonomy-figures`
(:201 anchor tag); `external_services.md#clerk-authentication-service` (:21). The ambiguous bare
`README.md:21` in `security_compliance.md:185` resolves correctly against **`corpus/architecture/README.md:21`**
(*"cost tracking lives in `app`, not the `ai` library"*), the sibling README — not repo-root
`README.md:21`, which is about Rosetta being a documentation repo.

---

## What I could not settle

- **`db-backup.md` in its entirety (all 31 lines).** The `db-backup` repo is in no clone set, so the
  6-hour schedule, the three targets (S3 / Azure / Hetzner-Germany), "Go", "Docker (scheduled,
  internal)" and "all schemas" are unverifiable here. They are at least self-consistent with
  `security_compliance.md:155,162` and `service_taxonomy.md:144,492` (which says *"ECS scheduled
  task"* where `db-backup.md:18` says *"Docker (scheduled, internal)"* — compatible, not identical).
  **What would settle it:** a clone of `anthropos-work/db-backup`, or the `infrastructure` repo's
  scheduled-task definition.
- **B3's underlying fact** — whether a US AWS DR region exists and whether it holds customer data.
  Needs `infrastructure` (never in any clone set).
- **Compliance scalars** — DPA v1.4, "18 approved sub-processors", 90-day post-contract deletion,
  TLS 1.3 cipher list, "monthly patch updates", "ECS health checks every 30 seconds"
  (`security_compliance.md:12,31,38,42,170,173,261`). None is derivable from any clone; all are
  prose-under-review in §5 rule 27's sense and are not marked as such.
- **`security_compliance.md:22`** — *"Public subnets: ALB, Cosmo Router"*. Consistent with
  `graphql-wundergraph/terraform/main.tf:11` using `base_service` (ALB listener + DNS) where the
  folded services use `base_internal_service`, but the subnet placement itself lives in
  `infrastructure`.
- **GitHub archive state** for `graphql-wundergraph`, `skiller`, `skillpath`, `jobsimulation`
  (`service_taxonomy.md:3,137,138,139`). As `:124-131` itself says, this lives in the GitHub org API
  and no clone can measure it. `gh` is not installed on this host and the repos are private. The
  clone evidence is *consistent* with the router date (last commit `60c229f` 2026-07-30, none after)
  and *inconsistent* with a flat jobsimulation archive on 2026-07-31 (four commits dated 2026-08-04,
  including merge commit `caf36c96` for PR #439) — which the row already discloses.
- **`service_taxonomy.md:206`** — Studio-Desk *"Multi-language support (7 languages)"*. I could not
  find a language list in `studio-desk` under `SUPPORTED_LANGUAGES`/`supportedLanguages` or an
  obvious locale array; absence here is a **failed search, not evidence** (§5 rule 2), so I neither
  cleared nor booked it.
- **`service_taxonomy.md:218`** — Studio-Room *"Python 3.11"*. `studio/requirements.txt` pins no
  interpreter version and I found no `python_requires`. Would be settled by the `app` Dockerfile's
  Python base image.
- **`clerkenstein.md:52`** — the `authn` provider's JWKS verification details (`jwt.Verify` +
  `jwks.Client`, 1-minute leeway) and **`clerk-integration.md:52`**'s twin. `colony` is a private Go
  module, pulled at Docker build, never cloned — so its internals cannot be read from this tree.
- **`ai-labs.md:4-6`** — the existence and contents of `anthropos-work/AI-Labs` / the `labs-api`
  control plane. Not in the clone set; `app`-side consumer evidence (`labsapi/client.go`,
  `LABS_API_URL`, `ReportEvent`) is consistent with it but does not prove the repo.
- **`storage` / `messenger` prod desired-count.** Not claimed in my six files, but worth handing
  forward since I measured it while checking `roadrunner`: at the **pinned checkouts**
  (`4ce8ece5`, `fa47850d`) both `terraform/main.tf:19` read `service_desired_count = 1`; at
  **`origin/main`** messenger reads `= 0` (`:29`, with a comment citing the cms precedent) and
  storage's `main.tf` no longer matches that grep at all. Any corpus row citing
  `storage/terraform/main.tf:38` or `messenger/terraform/main.tf:29` as `= 0` is grading at
  origin/main, not at the checkout — the two disagree, and §5 rule 41 says which one a guard sees
  depends on when someone last fetched.
