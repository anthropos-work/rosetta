# Seat C — reading #21

Instrument: `briefing-AS-DELIVERED.md` (222 lines, read in full) + `corpus/ops/platform-alignment.md` §5
(read in full, `:249`–`:1296`).

Ground truth re-verified at this reading's open (`git rev-parse --short=8 HEAD` per clone):
app `b948604f` · platform **`0c91421d`** (per ADDENDUM; the frozen table's `0dab54df` is stale and was
used only where a passage names it) · next-web-app `bb3313bc` · sentinel `88bc5592` · storage `4ce8ece5` ·
messenger `fa47850d` · cms `ca50c817` · graphql-wundergraph `60c229f3` · roadrunner `87d8d443` ·
jobsimulation `462343b0` · studio-desk `14a5442a` · ant-academy `9c3843cd` · **`stack-demo/app/studio`
`aeec036a`** (own checkout — grepped at its own ref, never at `app`'s) · rosetta-extensions authoring copy
`5fb0915` on `main`.

`app` `origin/main` is `2035f9a4` (93 commits ahead of the checkout, `v1.369.0-2-g2035f9a40`). Passages
naming a ref were graded **at that ref** (briefing §"Which ref settles an `app` claim"); passages naming
none were graded at the checkout `b948604f`.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/architecture/alignment_testing.md` | 542 | yes |
| `corpus/services/backend.md` | 318 | yes |
| `corpus/architecture/ai_architecture.md` | 305 | yes |
| `corpus/services/customerio-sync.md` | 147 | yes |
| `corpus/services/skillpath.md` | 112 | yes |
| `corpus/services/skiller.md` | 69 | yes |
| **total** | **1493** | — |

Search-pipeline controls run in the same pass: `git grep -c "UsersService" b948604f -- '*.go'` → 3 files
(positive control beside the `SkillPathSessionService` zero); the loose regex
`60.*65.*75.*85.*95` returned 12/58 hits in app/next-web while the tight
`\b60\b.{0,12}\b65\b…` returned 0 (so the "no threshold ladder" zero is a measured zero, not a broken
pipeline). All stderr read. zsh parameter-modifier trap hit twice (`$R:env_guards.go` → `nv_guards.go`,
`$r:repos.yml` → 0 entries) and corrected with `"${r}"':path'` — both apparent zeros were the shell, not
the tree.

---

## Blockers

### B1 — "All of their Connect-RPC surfaces are served on `app`'s single RPC mux" is false for 5 of the 8 merged services

- **Anchor:** `corpus/services/backend.md:29` — *"**All of their Connect-RPC surfaces are served on `app`'s
  single RPC mux — and nothing outside the process calls them.**"* The antecedent of *their* is the
  banner's own table at `:9-16`, which enumerates **eight** merged services: skiller, skillpath,
  roadrunner, jobsimulation, cms, storage, messenger, customerio-sync.
- **Why it is false:** the mux registers **six** handlers and only **three** of them belong to a merged
  service. Enumerated from `app/main.go` @ `b948604f` (`:1185`–`:1228`, read in full with surrounding
  comments):
  `UsersService` `:1187` · `OrganizationsService` `:1188` · `SkillerService` `:1196` ·
  `JobSimulationService` `:1204` · `CMSService` `:1212-1214` (conditional, `if cmsRPCServer != nil`) ·
  `lab.v1.LabSessionService` `:1228`.
  So **skillpath, roadrunner, storage, messenger and customerio-sync have no surface on that mux at all.**
  - `SkillPathSessionService`: **0** occurrences in Go source at `b948604f`
    (`git -C stack-demo/app grep -c "SkillPathSessionService" b948604f -- '*.go'` → exit 1, no matches;
    positive control on `UsersService` returns 3 files). The only two hits repo-wide are `CLAUDE.md:80` and
    `knowledge/architecture.md:28` — markdown, not code.
  - This document **contradicts itself 39 lines later**: `backend.md:68` — *"**There is no
    `SkillPathSessionService`** — measured: **0** occurrences in Go source"*.
  - And it contradicts the sibling doc: `corpus/architecture/shared_libraries.md:70` — *"**skillpath and
    roadrunner were REMOVED, not re-hosted** … neither `SkillPathSessionService` nor a RoadRunner service
    is among them"*, with the identical six-handler enumeration at the identical line anchors.
  - Storage/messenger/customerio-sync likewise register nothing on the mux at `app` origin/main
    `2035f9a4` (the ref at which those three packages exist at all).
- **Why I hesitated:** a charitable reading restricts *their surfaces* to *the surfaces that survived*.
  But the sentence sits under the heading *"Consequences that hold platform-wide"*, its sibling bullet
  `:27` (*"All of their tables live in `public`"*) really is universal over the same eight, and the
  universal quantifier is the load-bearing word. A reader wiring a client for skillpath or roadrunner is
  sent to a mux that has never served either.
- **Confidence:** medium-high

### B2 — the merge banner's Redis-stream ownership set omits `backend`, and asserts exhaustiveness while doing it

- **Anchor:** `corpus/services/backend.md:33-34` — *"**`app` owns the `skillpath`, `jobsimulation`, `cms`
  and `ai_usage` Redis Streams** — both producer and consumer in-process. **`skiller` is NOT a fifth
  member: it is consumer-only** (`:136`, `:264` below)."*
- **Why it is false:** `app` is producer **and** consumer of **five** streams, not four — the omitted one
  is its own `backend` stream. Enumerated independently from source (every publisher constructor and every
  `AddSubscriber` in `app` @ `b948604f`), not from the document's arithmetic:

  | stream | producer | consumer |
  |---|---|---|
  | `backend` (= `serviceName`, defaulted `"backend"` at `main.go:213-215`) | `main.go:287` `pubsub.NewPublisher(serviceName, …)` | `main.go:1320` `AddSubscriber(serviceName, backendSelfSub)` |
  | `SKILLPATH_STREAM` | `main.go:637` | `main.go:1274` |
  | `CMS_STREAM` | `main.go:1039` | `main.go:1303` |
  | `JOBSIMULATION_STREAM` | `internal/jobsimwiring/wiring.go:180` | `main.go:1285` |
  | `AI_USAGE_STREAM` | `internal/jobsimwiring/wiring.go:127` | `main.go:1305` |
  | `SKILLER_STREAM` | **none** | `main.go:1276` (consumer-only ✔) |

  The `NewPublisher` set is exactly those five (`git grep -n NewPublisher b948604f -- '*.go'`; the sixth
  hit, `internal/deadletterqueue/dead_letter_queue.go:38`, is the DLQ, not an application stream).
- **The self-contradiction:** the banner cites `:264` as its own authority, and `:264` states a **different
  four** — *"`backend`, `skillpath`, `jobsimulation`, `cms` — plus the `AI`/`ai_usage` usage stream."*
  Two passages of one file both say "four" over incompatible sets. `:264` is the correct one; the banner
  drops `backend` and then closes the set with *"`skiller` is NOT a fifth member"*, which makes the
  omission an explicit (and false) exhaustiveness claim — `backend` **is** a fifth member of the
  owned-both-ways set.
- **Why I hesitated:** one can read the banner's scope as "the merged services' streams", under which
  `backend` is out of scope — except `ai_usage` is not a merged service's stream either, so that reading
  does not survive the banner's own list.
- **Confidence:** medium

### B3 — LabSession is described as the third RPC handler; it is registered last, and this file's own enumeration says so

- **Anchor:** `corpus/services/backend.md:236` — *"Registered as a third RPC handler in `main.go` after
  Users and Organizations."*
- **Why it is false:** at `app` `b948604f`, `lab.v1.LabSessionService` is registered **last** —
  `main.go:1228`, after `SkillerService` (`:1196`), `JobSimulationService` (`:1204`) and the conditional
  `CMSService` (`:1212-1214`). Read with surroundings: `:1215-1227` construct a dedicated
  `authn.NewManager` and wrap only this handler's path in Clerk authn middleware, which is why it is
  placed at the end. The same file's `:66` enumerates the mux in order and puts LabSession fifth of the
  five unconditional handlers; `shared_libraries.md:70` gives the identical ordering.
- **Why I hesitated:** the bullet lives under *"Recent Feature Additions (Q1-Q2 2026)"* and names a PR
  (#896), so *"registered as a third handler"* may have been true **when that PR landed**, before the
  skiller/jobsim/cms folds added handlers ahead of it. It carries **no ref pin**, it is written in the
  present tense, and the same bullet is actively maintained for currency (*"**The real HTTP client has
  since LANDED**"*, with a `b948604` anchor). Per the briefing, an unpinned claim is graded at the
  checkout — where it is false and contradicted 170 lines above.
- **Confidence:** medium-low

---

## Minors

- `corpus/architecture/alignment_testing.md:3` — **`Last updated: 2026-06-06`** is stale by two months.
  The file's own body carries M218, M219 and *"Corrected M257x iter-85"* corrections, and its last commit
  is `00be1ac` (2026-08-06). Metadata only; no factual consequence.
- `corpus/architecture/alignment_testing.md:200` — *"its rationale is the comment block at `:58-68`"*.
  `gate.sh` `:58-67` is the comment block; `:68` is `echo "==> capability-coverage check ($dna)"`, not a
  comment. (`:69` and `:61` both check out exactly.)
- `corpus/architecture/alignment_testing.md:534-539` — the Layout block lists four `internal/` packages
  (`dna`, `outcome`, `compare`, `report`); the section holds **five** — `internal/canon` is omitted.
  Related nit at `:534`: `dna.go:20-26` covers the `list|diff|validate|coverage` case labels but the
  `coverage` dispatch is at `:27`.
- `corpus/architecture/ai_architecture.md:35` — *"`studio/tools/pdf2md.py:24` (`mistral-ocr-latest`)"*.
  At `stack-demo/app/studio` `aeec036a`, `:24` is `from mistralai import Mistral`; the model literal
  `model="mistral-ocr-latest"` is at **`:127`**. File and model are both right; the line pairing is not.
- `corpus/architecture/ai_architecture.md:35-36` — calls `pdf2md.py` *"a standalone CLI"*.
  `corpus/architecture/external_services.md:565` explicitly records the opposite framing as false:
  *"`tools/r3.py` DOES dispatch it, as step 2 of the offline chain … so *"nothing dispatches it"* is
  false"*. Verified: `r3.py:139`, `:190`, `:200` do invoke `pdf2md.py`. The substantive claims
  (not on the AI manager's path, not on the generation pipeline's) are both TRUE, so this is phrasing.
- `corpus/architecture/ai_architecture.md:196` vs `:205` — *"Voice engine is selectable **per simulation**
  in CMS"* against, nine lines later, *"**Engine choice is per SEQUENCE**, from the CMS `voice_engine`
  field"*. Measured: `voice_engine` is a field of the **sequence** DTO
  (`internal/cms/directus/collections/jobsimulation.go:911`, consumed at `:1350`
  `VoiceEngine: voiceEngineFromDirectus(seq.VoiceEngine)`). `:205` is right; `:196` (and `:206`'s
  *"on the authored simulation"*) are loose. Called MINOR because the authoritative statement is bolded
  immediately below and the mechanism is otherwise exact.
- `corpus/architecture/ai_architecture.md:235` — *"the enum is
  `…/collections/jobsimulation.go:983-990`"*. The `AIModel` const block is `:979-991` (11 members,
  `:980`–`:990`); the cited range covers 8 of them and drops `Anthropic35SonnetAws`,
  `Anthropic37SonnetAws`, `Anthropic4SonnetAws`. The three models the sentence names as examples are all
  inside the quoted range, so nothing downstream is wrong.
- `corpus/architecture/ai_architecture.md:276` — *"`calculateSkillScore` (`v3/validator/skills.go:53-64`)"*;
  the function is `:53-62` (`:64` opens `func (s skillsValidator) run`).
- `corpus/architecture/ai_architecture.md:60` — *"[`external_services.md:619-629`] (item 4 …)"*; item 4
  begins at `:622`, so the range opens inside item 3.
- `corpus/services/backend.md:167-223` ("Key directories") lists `cms/`, `jobsimulation/`, `jobsimwiring/`,
  `skiller/`, `skillpaths/` for the earlier folds but omits `storage/`, `messenger/` and `customeriosync/`,
  the three the same file's banner (`:14-16`) names as folded-in packages. They are genuinely absent at the
  checkout `b948604f` and present at `app` origin/main `2035f9a4`
  (`internal/{storage,messenger,customeriosync}` all exist there), so the listing is a ref-lag rather than
  an error — but it reads as a complete map.
- `corpus/services/backend.md:114` — *"registers in the Cloud Map service-discovery namespace
  `internal.anthropos` (`app/terraform/main.tf:58`…)"*. `:58` is
  `service_discovery_namespace_id = var.service_discovery_namespace_id` — the right construct, but the
  literal `internal.anthropos` occurs nowhere in `app/terraform` (only in `knowledge/**`). Consistent with
  the same bullet's own caveat at `:118` that the literal lives in the un-cloned `infrastructure` root
  module.

---

## Positively cleared (audited zeros)

**ENUMERATED** — predicate re-derived from source independently, cardinality compared:

1. **`alignment_testing.md:259-266`, the five-DNA score table** — gene and capability counts parsed from
   the DNA JSON itself, not from the document: `clerk-2.6.0` 14 caps / **27** genes · `clerk-js-5` 6/**9** ·
   `clerk-multi-1` 5/**9** · `clerk-deploy-1` 3/**7** · `clerk-express-1` 5/**13**. All five match.
   Golden-file counts match gene counts exactly (27 / 9 / 9 / 7 / 13).
2. **`alignment_testing.md:208-211`, "only `clerk-2.6.0` declares a `consumed_surface`"** — parsed all five
   DNAs: `clerk-2.6.0` has **15** entries; the other four have the key **absent**. The `covered_by` example
   quoted at `:201` (`clerk-express-1:ClerkClientBAPI/get-organization`) is a real entry.
3. **`alignment_testing.md:219-224`, "three genes name identity and all three assert `universal-user`"** —
   enumerated every `universal-user` variant across the five DNAs: exactly three, and they are
   `ExtractIdentity` (clerk-express-1), `Me` (clerk-js-5), `DeployIdentity` (clerk-deploy-1). `GetUser`
   really does carry the two-sided `hero-a`/`hero-b` variants.
4. **`ai_architecture.md:33-37`, "*every* use of Mistral in `app` is OCR"** — enumerated all Go hits at
   `b948604f`: `internal/cms/studio/markdownManager.go:{11,19,21}`, `studioManager.go:{531,583,585,589}`,
   `xlsx.go:13` — every one on the studio-attachment OCR path, none on a generation path. Python side
   grepped **at the nested repo's own ref**: `git -C stack-demo/app/studio grep -ci mistral aeec036a`
   summed to **22 lines across 3 files** (`requirements.txt` 1, `tools/pdf2md.py` 18, `tools/r3.py` 3) —
   the document's own "22 hits / 3 files" reproduces.
5. **`ai_architecture.md:62-66`, "Directus enum has five members, the switch four cases"** — Directus
   `AIVendor` consts `:969-975` = {Azure, **Azureglobal**, Openai, AnthropicAws, Anthropic} = 5;
   `simulator/ai/ai.go` cases at `:58`, `:69`, `:86`, `:102` = 4; `azureglobal` is the uncased member.
6. **`backend.md:231`, "13 `ai_readiness_*` ent tables"** — derived twice and independently of the
   document: 13 `internal/data/ent/schema/ai_readiness_*.go` files, and 13 distinct
   `CREATE TABLE … ai_readiness_*` in `terraform/migrations/*.sql`. All four "the four a 9 omits"
   (`recommendations`, `email_overrides`, `notification_logs`, `notification_optouts`) are present, as are
   `live_snapshots` and `text_translations`. `internal/workforce/` contains no `readi*` file.
7. **`backend.md:122-129` / `skillpath.md:39-40`, the subgraph ladder 4 → 3 → 1** — derived by listing
   `schemas/` at each named commit rather than trusting the counts:
   `749dc86` → {backend, cms, jobsimulation, skillpath} = 4 · `7c17e63` → {backend, cms, jobsimulation} = 3
   · `915da06` → {backend} = 1, and `915da06`'s diffstat shows **both** `cms.graphqls` (-762) and
   `jobsimulation.graphqls` (-860) deleted in the one commit.
8. **`skillpath.md:47`, `repos.yml` 9 / 6 / 4** — listed the entries at each ref:
   `2adcf71` = app, cms, jobsimulation, sentinel, storage, messenger, roadrunner, next-web-app, studio-desk
   (9, no skillpath) · `0dab54d` = 6 · `0c91421` = app, sentinel, next-web-app, studio-desk (4).
9. **`backend.md:241`, messenger's four `*_RPC_ADDR`** — all four exist on the `messenger` block **and
   nowhere else** at `0dab54d` (`:173`, `:174`, `:176`, `:183`), under the exact quoted comment at `:171`;
   at `0c91421` compose sets **zero** `*_RPC_ADDR` and exactly one service address,
   `AUTHORIZATION_ADDRESS=http://sentinel:8087` (`docker-compose.yml:48`).
10. **`customerio-sync.md:78-79,145-146`, "the only service built from a git URL"** — enumerated every
    `context:` in compose at `0dab54d` (7: six local siblings + the one
    `git@github.com:…/customerio-sync.git#main`) and at `0c91421` (4, all local siblings).
11. **`backend.md:254`, "`STORAGE_RPC_ADDR` has 0 read sites, 3 remaining occurrences, all comments"** —
    graded at the ref the bullet names (`app` origin/main `2035f9a`): Go hits are
    `main.go:504`, `internal/jobsimwiring/wiring.go:101`, `internal/storagens/callsites_test.go:189` —
    three, all comments, and `:504` reads *"the standalone service takes no traffic and
    STORAGE_RPC_ADDR is gone."* `:524`/`:525` are `internalstorage.NewManager` / `NewPublicManager`.
    (At the **checkout** `b948604f` the same grep returns 15 hits and `main.go:524` is a live
    `os.Getenv` — the bullet's pin is doing real work and is correctly placed.)
12. **`skiller.md:57-58`, "8 `ContentLanguage`s"** — enumerated from
    `graph/schemas/assignments.graphqls:44`: english, italian, spanish, french, german, dutch, japanese,
    portuguese = 8.
13. **`ai_architecture.md:278-281`, "no 60/65/75/85/95 threshold ladder"** — tight word-boundary regex
    across `app` and `next-web-app` at HEAD → 0, with the loose regex returning 70 as the pipeline control.
14. **`ai_architecture.md:115-126`, the studio slot table** — read
    `configs/production_config.ini:26-36` at `stack-demo/app/studio` `aeec036a`: all ten lines match the
    table cell-for-cell, stable == experimental, and `development_config.ini:26-36` is byte-identical.
    `config_template.ini:39`/`:40` really are the two `gpt-4o` stable slots. `gpt-5.2` appears in **no**
    config — only `services/ai.py:356` and `:508`, both pricing entries.

**RE-DERIVED** (weaker — arithmetic the document itself displays):

- `alignment_testing.md:303-312`, the toy score. Weights 3×3 (Add, critical) + 3×2 (Greet, standard) = 15;
  one failed `w2` gene ⇒ 13/15 = 86.7 %, critical 3/3 = 100 %, "5/6 genes". Reproduces. I also read the
  toy source/mirror: the divergence is exactly `Greet`'s missing `strings.Fields` normalization, and the
  quoted strings (`"Hello, El Nino!"` vs `"Hello,   El  Nino  !"`) are what those two implementations
  produce.
- `alignment_testing.md:261,269-274`, "was 97.2 % / 26-of-27". Weight sum 17×3 + 10×2 = 71;
  (71−2)/71 = 97.18 %. Reproduces, and `MembershipOrgIdentity` really is `standard` (weight 2).
- `backend.md:99`, 42,790 + 794 = 43,584. Reproduces; the underlying prod counts are not measurable here.

**Anchors opened and confirmed against their surroundings** (not just grepped to): `app/main.go`
`:213-215`, `:287`, `:504`, `:524-525`, `:637`, `:743-746`, `:1039`, `:1185-1228`, `:1242-1243`,
`:1274-1320`; `internal/jobsimwiring/wiring.go:127,180`; `internal/jobsimulation/ai/ai.go:30-33,87,
127-154,243-257,259-289`; `internal/jobsimulation/simulator/ai/ai.go:20-26,58-59,65-66,69,82-83,86,98-99,
102,110-111,114-115,126-127`; `internal/cms/directus/collections/jobsimulation.go:905,911,971,979-991,
1079-1086,1297,1302-1307,1350,1594-1600`; `internal/jobsimulation/calls/livekit.go:106,131-135,140-144`;
`…/v3/validator/validator.go:43,60-61,595`; `…/basevalidator/criterion.go:34,72,90,127,168,428,450-475`;
`…/basevalidator/bulkChecks.go:18,85,118`; `…/v3/validator/skills.go:40-51,53-62,75`;
`internal/skill/skill.go:617-623`; `internal/skillpath/session.go:204-212`;
`internal/skillpaths/skillpaths.go:87-95`; `internal/organization/intelligence.go:1144,1159-1170`;
`internal/coursebuilder/bedrock.go:98-114`; `internal/academy/academy.go:6-9`;
`env_guards.go:60-63`; `main.go:284,286,295,393-396` @ `2035f9a4`;
`terraform/{locals.tf:6,8; main.tf:58,185-186; variables.tf:197,230}`;
`terraform/migrations/20260729133514.sql:62-63`; `messenger/terraform/variables.tf:77,82,87,92`;
`cms/terraform/main.tf:39` @ `f38c0c4` + commit `6efa1d5`; `jobsimulation/terraform/main.tf:15-22,24-40`
@ `6092c6d2`; `platform/{docker-compose.yml:1-2,28-110,48,84-92,102-103; common.yml; Makefile:10,81,102;
repos.yml}`; `next-web-app/…/InsightsBySkillPathStudentSimulationsContainer.tsx:31-34,138,140` and
`packages/ui/src/Competency/CompetencyReadLevel.tsx:18`; rext
`alignment/cmd/alignctl/{dna.go:19-31,41-56; run.go:125-154}`, `alignment/internal/dna/dna.go:{21,24,
28-48,55-58,269-270,280,308}`, `alignment/internal/compare/compare.go:{23,41-43,52-60,82,94,101-103}`,
`alignment/examples/toy/{dna.json,alignment_test.go:21-22,source/source.go,mirror/mirror.go}`,
`clerkenstein/alignment/scripts/gate.sh:1-80`,
`clerkenstein/.github/workflows/alignment.yml:1-11` (exists, git-tracked, self-declares inert; no
`.github/workflows` at the monorepo root), `stack-seeding/dna/snapshot.go:45-69`,
`stack-seeding/dna/data-dna.json:{404-432,433-460}` (10 surfaces: 4 `seeded-m7a`, 4 `seeded-m7c`,
1 `snapshot-seeded-m9b`, 1 `snapshot-seeded-m10` — **nothing waived**, so the `:505` "100 % coverage"
claim holds).

Every one of these resolved, and resolved onto the construct the corpus says is there. Three refuted
claims deserve naming because they are the classes this instrument keeps catching and they did **not**
recur here: the `915da06` "3 → 1" subgraph count (its own commit subject says 2→1 and the corpus
correctly refuses it); the "unset `ai_vendor` routes through the `default:` arm" mechanism (the corpus
correctly routes it through `case simulation.Openai:` at `:58-59` and keeps `:114-115` as the separate
unrecognised-vendor door); and the `checkerEngines` map (grepped all references — `validator.go:43,60,595`
and `criterion.go:34,72,90` — it is stored, threaded and **never read**, exactly as claimed).

---

## What I could not settle

1. **The published alignment scores themselves** (`alignment_testing.md:259-266`: 100 % / 100 % on four
   surfaces, UNMEASURABLE on `clerk-express-1`). Running `gate.sh` writes a built runner binary into
   `clerkenstein/alignment/`, which the read-only bar forbids. I verified everything reachable without
   executing: gene counts, capability counts, criticality assignments, golden-file counts (27/9/9/7/13,
   one per gene), the `real-org-eid` golden holding the roster's real UUID
   (`1d0e6c22-3f45-4b98-8a7d-2c1b9e6f0a54`) rather than a fabricated string, and the
   `Store.SeedOrgIdentity` / `LookupOrgEid` wiring at `clerk-backend/store.go:138,151` +
   `cmd/fake-bapi/main.go:88`. A `go test -tags alignment` run in a scratch GOPATH would settle it.
2. **`ai_architecture.md:99` — "*not* a bare `app/internal/ai/ai.go` (**no such file** — that path **now**
   resolves only inside the frozen `jobsimulation` husk repo)".** True at the ref the sentence pins
   (`5ba17044`: `internal/ai/` absent) and true at the checkout `b948604f` (absent). **False at `app`
   origin/main `2035f9a4`**, where `internal/ai/ai.go` exists — an in-lined copy of the shared `ai.AI`
   interface (26 files under `internal/ai/`), **not** a vendor-selection wrapper, so the sentence's
   *purpose* survives while its literal does not. I did not book it: the briefing's ref rule says a
   pinned claim is graded at its pin. But the word **"now"** is a currency assertion, and
   `platform-alignment.md` §5 rule 33 part 4 holds that a block asserting currency cannot be pinned into
   silence. Whichever way that is adjudicated, it will be false in a matter of days. Settling evidence:
   a decision on whether §5 rule 33's currency carve-out overrides the briefing's flat pin rule.
3. **Prod row counts** — `backend.md:97-103` (42,790 / 43,584 / 22,490 / 23 / 1,447) and
   `ai_architecture.md:141` (skill_embeddings 42,790, job_role_embeddings 18,919). Not derivable from any
   clone; needs a `/db-query` read against prod or the snapshot manifest.
4. **`ai_architecture.md:187-192`, the five LiveKit agent repos**, and the archive states in the fenced
   map. These live in the GitHub org API, never in git objects. Internally consistent with
   `platform-migration-status.md:143-147`, which is where the doc points.
5. **`shared_libraries.md`'s "at least 13 Connect-RPC services"** and skiller.md's
   `skiller_rpc_addr = http://backend.internal.anthropos:8081`. `proto` and `infrastructure` are in no
   clone set; `backend.md:118-121` says so explicitly and I confirmed the cloned modules declare their
   `*_rpc_addr` variables with **no default** (`app/terraform/variables.tf:197,230`;
   `messenger/terraform/variables.tf:77,82,87,92`), so the literal is genuinely elsewhere.
6. **`customerio-sync.md:132-134`'s own scope note** — whether the standalone's ECS task / image /
   terraform module were torn down. The doc declares this unmeasured rather than asserting it, which is
   the correct handling; I confirmed the repo is in no clone set.
7. **`backend.md:87-88`'s historical verification claims** (`app@c3c45e01` v1.334.1, `platform@0808b92`,
   "86-commit merged image", the live 2026-07-08 bring-up). Historical narrative, pinned, not re-runnable.

---

**BLOCKERS=3 MINORS=11**
