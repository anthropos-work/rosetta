# Seat F — reading #22

Instrument: `briefing-AS-DELIVERED.md` (verbatim iter-76 body + iter-99 ADDENDUM).
Ground truth re-verified at this seat's open:
app `b948604f` · **platform `0c91421d`** (ADDENDUM supersession confirmed by `git rev-parse`) ·
next-web-app `bb3313bc` · sentinel `88bc5592` · storage `4ce8ece5` · messenger `fa47850d` ·
cms `ca50c817` · graphql-wundergraph `60c229f3` · roadrunner `87d8d443` · jobsimulation `462343b0` ·
studio-desk `14a5442a` · ant-academy `9c3843cd` · nested `stack-demo/app/studio` **`aeec036a`** (grepped at
its own ref, per the ADDENDUM) · rosetta-extensions authoring copy `5fb0915e` on `main`.

Where a passage names a ref, it was graded at **that** ref (§5 rule 33): `9d00a313`, `2035f9a4`,
`0dab54d`, `2adcf71`, `6092c6d2`, `9f8cb53`, `63bffc8`, `915da06`, `d11a403`, `838d907`, `a2a3ee6`,
`a4db680`, `e45eb61` were each read out of the relevant clone rather than assumed.

## Positive controls

| file | `wc -l` | read in full? |
|---|---|---|
| `corpus/services/ant-academy.md` | 457 | yes |
| `corpus/architecture/architecture_overview.md` | 417 | yes |
| `corpus/architecture/shared_libraries.md` | 247 | yes |
| `corpus/services/roadrunner.md` | 206 | yes |
| `corpus/architecture/dependency_map.md` | 103 | yes |
| `corpus/services/intelligence.md` | 18 | yes |
| **total** | **1448** | matches the assignment |

Search-pipeline positive controls run in the same passes as every absence claim below:
`STORAGE_RPC_ADDR` at `9d00a313` returned 3 (so the 0 for `ROADRUNNER_RPC_ADDR` at the same ref is real);
`RoadRunnerService` returned 1 file in the `roadrunner` clone (so the 0s in `app`/`jobsimulation` are real);
`colony` present in 7/7 `go.mod` (so the 0/7 for `anthropos-work/authn` is real);
`redis` present 3× in `app/go.mod` (so the 0 in `storage/go.mod` is real);
`STORAGE_S3_BUCKET` returned 1 in platform `0dab54d` (so the 0 for `ROADRUNNER_RPC_ADDR` there is real).

**One near-miss worth recording, because it is exactly §5 rule 3.** My first pass at the multi-tenancy
predicate used a substring match for `UserMixin{}`, which silently swallowed `CreatedByUserMixin{}` and
made 4 of the 7 `OrganizationIDMixin{}` schemas look policed. That would have produced a confident,
well-evidenced **false blocker** against `architecture_overview.md:368`. Re-run with a
`(?<![A-Za-z0-9_])` boundary the count dropped 37 → 33 and the corpus's figures reproduced exactly.
The naive predicate and the corrected one are both stated in "Positively cleared" below.

---

## Blockers

### B1 — `dependency_map.md:59` states the wrong FILE count for `SKILLER_STREAM` at the ref it pins

- **Anchor:** `corpus/architecture/dependency_map.md:59` — *"This cell also named origin/main `2035f9a4`
  until M257x iter-98, where **not one of those line numbers resolves** (`main.go:1276` is
  `apiKeyManager,`) and `SKILLER_STREAM` has **6** Go occurrences across 4 files, not one"*
- **Why it is false:** measured at the ref the sentence itself names.
  `git -C stack-demo/app grep -c SKILLER_STREAM 2035f9a4 -- '*.go'` returns **three** files —
  `main.go:2`, `subscriber_merge_test.go:3`, `subscriber_wiring.go:1` = **6 occurrences across 3 files**,
  not 4. `git grep -l` at the same ref lists exactly those three. (Widening off the `*.go` pathspec gives
  6 files, but three of the extra ones are `knowledge/**.md` and `terraform/main.tf` — not Go, and the
  sentence says "Go occurrences".) The two neighbouring assertions in the same cell are **correct** and I
  verified both: `2035f9a4:main.go:1276` really is `apiKeyManager,`, and at `b948604f` `SKILLER_STREAM`
  really does have exactly one Go occurrence (`main.go:1276`, an `AddSubscriber`).
- **Why I hesitated:** the occurrence count (6) is right and the load-bearing conclusion — *the sentence
  was false at the second of the two refs it cited* — survives intact; only the denominator of a
  supporting parenthetical is wrong. But the briefing lists "wrong counts" as a BLOCKER exemplar, this
  number sits inside a **retraction** (§5 rule 31: retraction prose reads as already-adjudicated and
  survives readings that would catch plain prose), and it is a re-derivable scalar with an explicit ref
  pin, so rule 33 offers it no shelter.
- **Confidence:** high (on the measurement); medium (on materiality).

### B2 — `ant-academy.md:63` attributes the academy's progress WRITES to the beacon fallback route

- **Anchor:** `corpus/services/ant-academy.md:63` — *"progress persists via GraphQL mutations
  (`upsertChapterProgress[Batch]` / `setLastActivity`, **posted from
  `code/app/api/academy/beacon/route.js`**) to Ent tables `academy_chapter_progresses` /
  `academy_last_activities`"*
- **Why it is false:** that route is the *exception*, not the path. Its own header
  (`stack-demo/ant-academy/code/app/api/academy/beacon/route.js:1-18`) states the mechanism in the
  opposite order: *"the real academy mutations go to the CROSS-ORIGIN WunderGraph supergraph with a Clerk
  Bearer token — and sendBeacon can't set an Authorization header. So a beacon can't hit the supergraph
  directly. This SAME-ORIGIN route is the beacon-able form … a best-effort last-ditch flush for a write
  that would otherwise be lost if the tab closes mid-retry."* It also declares itself fire-and-forget and
  fail-closed (`:16-18`). The normal write path is client-side: `src/progress/store.js:143` (*"fires an
  awaited `upsertChapterProgress` through the §1 harness"*) and `:199` (*"awaited `setLastActivity`
  through the harness"*), with `src/progress/useProgressSync.js:36`, against the documents in
  `src/graphql/query/academyProgress.js:55,74`. A reader debugging "progress isn't saving" who follows
  this sentence lands on the on-unload fallback and never opens the harness that issues 100 % of the
  in-session writes.
- **Why I hesitated:** the route named is real, it does re-issue exactly those two mutations, and the
  surrounding claim (backend-authoritative, server-owned, Ent-pluralized table names — all four verified:
  `app/internal/data/ent/schema/academy_chapter_progress.go`, `academy_last_activity.go`, and
  `app/cmd/academy-seed` all exist) is correct. This is a mechanism stated backwards inside an otherwise
  accurate sentence rather than an invented fact — booked per the briefing's "when in doubt, BLOCKER".
- **Confidence:** medium.

---

## Minors

- `ant-academy.md:135` — quotes `emptyCatalogView()` as literally
  `{ chapters: [], skillPaths: {}, series: [] }`. Source (`code/src/lib/serverTenant.js:116`) returns
  `{ chapters: [], skillPaths: {}, series: [], bundles: PUBLIC_BUNDLES, catalogVersion: CATALOG_VERSION }`.
  Partial quote presented with an `=`. The conclusion it supports ("→ 0 cards") is unaffected.
- `ant-academy.md:258` — *"~26 Playwright e2e spec files (tests/e2e/)"*. Measured **31**
  `code/tests/e2e/*.spec.js` (32 counting all `*.spec.*`). The tilde absorbs some of it; ~19 % is a lot
  of tilde.
- `ant-academy.md:145` — *"`code/public/catalog.json` (~2,667 entries)"*. Measured **2,715** entries in
  the `courses[]` array (`version 1.0`, 7 languages). ~1.8 % — noted only because 2,667 is an oddly exact
  number to prefix with `~`.
- `roadrunner.md:13-14` — *"`roadrunner/terraform/main.tf:19` still reads `service_desired_count = 1` and
  has not been touched since `87d8d44` (2026-06-19, before the fold)"*. `87d8d44` is the **repo tip** and
  never touched that file; its last modifying commit is `e45eb61` (2026-05-27,
  *"source base service module from consolidated infrastructure modules"*). The material claim — still
  `= 1`, unchanged since before the fold — is TRUE and I verified `:19` reads `= 1`; only the attribution
  of the last touch is loose.
- `architecture_overview.md:128` — mermaid edge label
  `Gateway -.->|spawns studio/gen.py in-process| Room`. It is an `os/exec` **subprocess** inside the same
  container (`app/internal/cms/studio/studioManager.go:119`:
  `return s.runCommand(ctx, pyBin, append([]string{"studio/gen.py"}, tokens...))`, image-side at
  `app/Dockerfile:42-46`), so "spawns … in-process" is self-cancelling. The intent (same container, no
  service hop) is unambiguous.
- `architecture_overview.md:285` — cites `external_services.md:602-607` as the block that *"enumerates
  four live ways a request leaves the EU"*. `:602-607` is the **Residency consequence** paragraph that
  *states* the count; the numbered enumeration starts at `:609`. The block does make the claim, so this
  is an anchor that is one paragraph short, not wrong.

---

## Positively cleared (audited zeros)

Labelled per the briefing. **ENUMERATED** = predicate re-derived from source independently and
cardinalities compared. **RE-DERIVED** = only the arithmetic the document displays was recomputed.

### The rule-24 trap: multi-tenancy — `architecture_overview.md:363-371` — **ENUMERATED**

The passage shows its own derivation, so per §5 rule 24 I enumerated the **set**, not the sum, from
`stack-demo/app/internal/data/ent/schema/`, excluding commented lines:

| predicate | corpus | measured |
|---|---|---|
| Ent schemas (files declaring `ent.Schema`) | 135 | **135** (139 `.go`, minus `database_types.go`, `mixin.go`, `skiller_mixins.go`, `skillpath_mixins.go`) |
| live `OrganizationMixin{}` | 29 | **29** (raw `grep -l` = 30; `user_resource.go:22` is `// OrganizationMixin{},  // We need to work on this` — **exactly the cited line**) |
| schemas declaring their own org-filtering `Policy()` | +2 (`Membership`, `Organization`) | **+2** — `org_membership.go:172` (`rule.AllowCurrentOrgEdgesOrSkipRule()`), `organization.go:56` with `rule.FilterSameOrganizations()` at `:96`. `user.go:116` also declares one but filters by **user** — correctly excluded |
| **auto-filter by organization** | **31 of 135** | **31 of 135** |
| `organization_id` + **no policy of any kind** | 23 | **23** |
| — the *neither-mixin* subset | 16 | **16** (the 18 bare-`organization_id` schemas minus `org_membership.go` own-Policy and `academy_feedback.go:64` real `UserMixin{}`) |
| — the `OrganizationIDMixin{}` remainder | 7 | **7** (`category`, `jobrole`, `similarity`, `skill`, `specialization`, `studio_document`, `studio_task`; `OrganizationIDMixin` is defined at `skiller_mixins.go:152` and declares no `Policy()` — the only `Policy()` decls in the whole directory are `mixin.go:99`, `mixin.go:126`, `org_membership.go:172`, `organization.go:56`, `user.go:116`) |

The fence the briefing says has been wrong four times **holds at this ref, on both conjuncts.** See the
near-miss note in Positive Controls for how the substring form of this predicate fabricates a blocker.

### Platform / compose topology (`architecture_overview.md`, `dependency_map.md`) — **ENUMERATED**

- `core` starts **five** containers. Enumerated the *floor* first (§5 rule 30): `sentinel` (docker-compose
  `:5`), `postgresql` + `redis` (`common.yml:2`, `:24`, reached through `include:` at `:1-2`) declare no
  `profiles:` key; the only `profiles:` keys are `:110`/`:183` `[core, backend, all]` (backend, gotenberg),
  `:141` `[studio-desk, all]`, `:168` `[frontend, all]`. Five containers, two of them ours.
- **5 services declared / 7 effective** at `0c91421` (sentinel, backend, studio-desk, next-web-app,
  gotenberg + postgresql, redis) — and **10 effective / 6 repo entries at `0dab54d`**, both exactly as
  `dependency_map.md:31` and `roadrunner.md:40-42` state.
- `repos.yml` at `0c91421` = **4** entries (app, sentinel, next-web-app, studio-desk); at `0dab54d` = **6**
  (+ storage, messenger); at `2adcf71` = **9**, with `roadrunner` at **`repos.yml:29-31`** verbatim. Only
  `app` carries `migrations: true` / `schema: public`.
- Exactly **one** service address in compose — `AUTHORIZATION_ADDRESS=http://sentinel:8087`
  (`docker-compose.yml:48`) — and **zero** `*_RPC_ADDR` variables.
- `docker-compose.yml:84-92` is the `MESSENGER_ENABLED` / `CUSTOMERIO_SYNC_ENABLED` comment, to the line.
- `docker-compose.yml:101-109` is `backend`'s whole `depends_on`, with `:102-103` the quoted
  *"storage, messenger and customerio-sync are not services any more"* comment — both anchors exact.
- `838d907` (2026-08-05, merged `0c91421`) deleted the three service definitions **and** the
  `storage`/`messenger` clone entries; its own body says so. `d11a403` (2026-08-03) deleted
  `- name: cms`, `- name: jobsimulation` **and** `- name: roadrunner` while its message claims
  roadrunner's entry "was already gone" — the corpus's *"the diff is the fact"* framing at
  `architecture_overview.md:211-213` and `roadrunner.md:17-19` is correct.
- `Makefile:10` `PROFILE ?= core`; studio-desk `depends_on: backend` at `:138-140`, next-web-app at
  `:165-167` — both profiles unusable alone, as claimed.
- `docker-compose.yml:71` `SKILLER_STREAM=skiller`; `:57` `GOTENBERG_URL=http://gotenberg:3200`.

### `app` source anchors — **ENUMERATED**

- Six Connect handlers at `b948604f`: Users `main.go:1187`, Organizations `:1188`, Skiller `:1196`,
  JobSimulation `:1204`, CMSService `:1212-1214` (`if cmsRPCServer != nil`), LabSession `:1228` — and
  **0** Go hits for `SkillPathSessionService` or a RoadRunner handler in `app`.
- Every `pubsub.NewPublisher` in `app` @ `b948604f`, enumerated rather than sampled: `main.go:287`
  (serviceName), `:637` `SKILLPATH_STREAM`, `:1039` `CMS_STREAM`, `jobsimwiring/wiring.go:127`
  `AI_USAGE_STREAM`, `:180` `JOBSIMULATION_STREAM`. `SKILLER_STREAM` is not among them; its single Go
  occurrence is `main.go:1276`, an `AddSubscriber`, handled by `internal/roles/roles.go:791`
  `SkillerSubscriber()`. **`app` really does run a subscriber on a stream nothing publishes to.**
- `STORAGE_RPC_ADDR` @ `9d00a313`: **3 Go lines, all comments** (`main.go:451`,
  `jobsimwiring/wiring.go:101`, `storagens/callsites_test.go:189`) vs **29 lines / 18 files** repo-wide —
  both figures exact, and the Go-vs-repo-wide distinction the corpus draws is the right one. At
  `origin/main` it is still 3 comment-only lines and `main.go:504` carries the quoted sentence verbatim.
  At `b948604f` `wiring.go:115` still *reads* it — the corpus's "state the ref you mean" is well taken.
- Judge0 direct: `jobsimwiring/wiring.go:118` @ `b948604f` and `:123` @ `9d00a313` — both anchors resolve
  to `jsrunner.NewRunnerManager(getenv("JUDGE0_API_KEY"), getenv("JUDGE0_BASE_URL"))`.
- `skillpaths.go:27-31` carries *"the drop-in for the **removed** skillpath RPC client"* verbatim.
- AI routing: `internal/jobsimulation/ai/ai.go` `getClient` `:262-276` (default `azureClientEu`, swap on
  `flag_use_azure_us`), `isThrottlingError` `:129` applied at `:166` and `:325`, Bedrock `eu-west-1` at
  `:85-88`. Mistral: `internal/cms/studio/markdownManager.go:11,19` + `studioManager.go:583`. All exact.
- messenger-in-app takeover: `main.go:1442` (`verifyConsumerGroupExists`, naming `msgflow.StreamBackend`
  at `:1445`) and `internal/messenger/flow/streams.go:65` `StreamBackend = "backend"` — both at the ref
  the cell pins (`9d00a313`); neither exists at `b948604f`, which is why the pin matters.
- **21 live handlers** on messenger's `backend` subscriber (`messenger/internal/flow/flow.go:72`): 22
  `pubsub.EventHandler` lines, one of them commented out (`OrgJobSimulationAssignmentPastDueHandler`,
  *"not implemented"*). Rule 24's "exclude what does not compile", applied by the corpus and confirmed
  here. `:105` and `:109` are the jobsimulation and cms subscribers, exact.

### Shared libraries — **ENUMERATED** (all seven Go clones' `go.mod`)

| lib | corpus | measured |
|---|---|---|
| colony | app `v0.35.2` `:16` · sentinel `v0.34.3` `:8` · storage `v0.34.3` · messenger `v0.35.2` · roadrunner `:7 v0.34.3` · cms+jobsim `v0.35.1` | all exact |
| proto | app `v1.210.0` `:18` · sentinel `v1.200.0` `:9` · storage `v1.196.0` · messenger `v1.210.0` · cms `v1.207.0` · jobsim `v1.205.0` · roadrunner `v1.196.0` | all exact |
| ai | `v1.40.2` in app, cms `:9`, jobsim `:11` — and only those three | exact; no other clone requires it |
| taxonomy | app `:20` direct · sentinel `:21 // indirect` · storage `:25 // indirect` · messenger `:9` direct · cms `:13` · jobsim `:15` · roadrunner none → **6 of 7** | exact |
| authn | imported by nothing | **0/7**, against a 7/7 colony control |
| repos.yml `type: go` union across all history | **11** | **11** at `a2a3ee6` (app, cms, jobsimulation, skiller, skillpath, chronos, sentinel, intelligence, storage, messenger, roadrunner); `a4db680` = *"decommission standalone skillpath service (M507)"*, deleting `- name: skillpath` |

`shared_libraries.md:79-80`'s pointer resolves exactly: `storage.md:115` is
`### Connect-RPC (\`StorageService\`)`. The "**at least 13** Connect-RPC services … a floor, not a count"
framing is honest: I enumerated **10** distinct Anthropos service symbols from the clones
(Authorization, CMS, JobSimulation, LabSession, Messenger, Organizations, RoadRunner, Skiller, Storage,
Users, across 10 `*v1connect` packages); the remaining three (`SkillPathSessionService`,
`RealtimeService`, `ChronosService`) are unreachable because `proto` is in no clone set — which is
precisely what the doc says. **RE-DERIVED** on 13; **ENUMERATED** on the 10.

### roadrunner.md — **ENUMERATED**, and the most accurate file in my set

Every measurable claim reproduced: `terraform/main.tf:19` `= 1`; `cmd/root.go:110`
`cmp.Or(os.Getenv("PORT"), "8080")` and `:84` `cmp.Or(os.Getenv("RPC_PORT"), "8081")`; compose
@ `2adcf71` `:291-292` published ports, `:298` `PORT=10400`, `:302` `RPC_PORT=10401`, and the whole
`:296-302` env block matching the table row-for-row (including `JUDGE0_API_KEY` and `SENTRY_DSN` being
the two the block never set); queue `roadrunner:default` + `MaxRetry(3)` (`internal/runner/runner.go:126`,
`worker/queues/queues.go:4`), task `roadrunner:submissionresult` (`worker/tasks/tasks.go:4`),
`Concurrency: 10` (`worker/worker.go:25`), `maxRetries = 15` at 1 s (`runner.go:244`, `:273`); three RPC
methods; `"py"` and no `"python"`; `internal/lsp` imported by nothing; **0** `*_test.go` with
`Dockerfile:18` `RUN go test -v ./...`; asynq `v0.25.1`, `go 1.25.0`.

The two counting claims are exact, and both are the kind that usually drift:
**0 Go hits** for all three of `ROADRUNNER_RPC_ADDR` / `RoadRunnerService` / `roadrunner:10401` at
*both* `app b948604f` and `jobsimulation 462343b0`; and the case-insensitive sweep at each clone's own ref
returns **app 25 / jobsimulation 8 / platform 3 / studio-desk 3 / next-web-app 1** — five for five.
Likewise `dependency_map.md`-adjacent: jobsimulation's Go source has **exactly one** `roadrunner` mention
(`internal/runner/runner.go:3`, the *"formerly the standalone 'roadrunner' service"* comment), against
**14 lines / 8 files** case-insensitive and **5 / 3** exact-case; and
`app/internal/jobsimulation/simulator/stream_handlers.go:30-34` carries the *"NOT stream handlers"*
sentence verbatim.

### Terraform + merge-state rows — **ENUMERATED**

- **`6092c6d2` is real, is on `jobsimulation`'s `origin/main`, and the claim is true AT THAT REF.** This
  is the one row where the ADDENDUM's checkout would have produced a false blocker: at the pinned
  checkout `462343b0` the `module "jobsimulation"` block is alive at `:31` with
  `service_desired_count = 0` at `:40`, in a 343-line file. At `6092c6d2` the file is **56 lines**,
  `service_desired_count` appears nowhere, `:15-22` is the M810 decommission comment and `:24-40` is the
  what-still-survives list (LiveKit/Chime buckets, `/production/jobsimulation/*` SSM, atlas tracker) —
  exactly as `architecture_overview.md:20`, `:221` and `dependency_map.md:31` describe. `462343b0` is
  simply *behind* `6092c6d2`; §5 rule 33 settles it in the corpus's favour.
- `graphql-wundergraph/terraform/main.tf:20` `= 1` ✓; `cms/terraform/main.tf:39` `= 0` ✓;
  `roadrunner/terraform/main.tf:19` `= 1` ✓.
- storage: `9f8cb53:terraform/main.tf` is **18 lines**, `:9-11` is the *"The ECS service that used to live
  here is GONE (v9.0 'support-in-app')"* comment and `:13-16` the `prevent_destroy` rationale — both
  exact; and `63bffc8` really is the intermediate with `service_desired_count = 0` (at `:38`, 119 lines).
- `915da06` is a **graphql-wundergraph** commit whose diff deletes the `jobsimulation` **and** cms
  subgraph entries and rewrites the README from *"Composes 3 subgraph services"* to *"the platform
  subgraph(s) … it is currently the sole subgraph"* — so **3 → 1**, and the commit's own
  "supergraph 2→1" subject is indeed wrong. Both files that say so are right.
- `jobsimulation` `origin/main` took **four** commits on 2026-08-04, merged PR **#439** among them — so
  `architecture_overview.md:20`'s *"report both, assert neither"* about the GitHub archive state is the
  correct disposition, not a hedge.

### ant-academy.md — **ENUMERATED**

Verified against `ant-academy 9c3843cd`: `getServerCatalogView()` is character-for-character
`const view = (await getBackendCatalogView(eids)) ?? emptyCatalogView()` (`serverTenant.js:145`), and
`:132` carries *"the cutover is intentional, not reversible-on-error"* inside the cited `:115-145` window;
the read chain (`backendContent.js` → `graphql/server.js`, endpoint `NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT` at
`:14`, throw at `:18`, queries `academyCatalogSeries` + `academyCatalogSkillPaths`) resolves end to end.
Fonts: `layout.jsx:1` imports **exactly** `{ Work_Sans, Instrument_Serif }`, at `:41` and `:51`, and
`academy.css:1` says display+mono *"fall back to system fonts"* — the M257x iter-98 correction holds and
neither DM Sans nor JetBrains Mono is loaded. Manifest wired at `layout.jsx:132`. The **whole** `isPublic`
matcher in `proxy.js:112-186` matches the table group-for-group, including both `_meta` spellings (with
`next.config.js:137,141` doing the rewrite), the dev-only `DEV_LOGIN_ENABLED` and
`BENCHMARK_VISUAL_BYPASS` arms (`proxy.js:63`, whitelisting `NODE_ENV==='development'`), and
`/api/chapters/*` being **absent** from it. No `/library/[slug]` exists; `app/(public)/library/page.jsx`,
`app/(public)/free/page.jsx` and `app/(authed)/courses/[slug]` are the real routes. Locale:
`SUPPORTED_LOCALES` is 7 wide at `locale.js:10`; `LocaleSwitch` is mounted **only** at
`PublicHeader.jsx:20`; `LanguageSelector` at `TopBar.jsx:76` renders `role="menu"` +
`role="menuitemradio"`; and `TopBar` really is on `/`, `/chapters/*`, `/latest`, `/bookmarks`,
`/my-activity` (traced through AcademyClient / CourseClient / LatestClient / BookmarksClient /
MyActivityClient). Cosmo: `gpt-5.2` at `https://api.openai.com/v1/responses` with
`localStorage('openai_api_key')` (`ucourses/ucourse-engine/assistant/agent.js:12,13,32`), gated by
`NEXT_PUBLIC_FEATURE_TRAINING_COACH` (`src/lib/featureFlags.js:8`). Serwist is **absent** from
`package.json` and `RegisterServiceWorker.jsx` is a kill-switch whose own header says so. FA Pro vendored
at `code/public/assets/fontawesome/{css,webfonts}`; `engines.node >=22`; `"dev": "next dev --port 3077"`;
`vercel.json` is exactly `{"framework": "nextjs"}`; all four named workflows exist; ant-academy is **not**
in `repos.yml`. Vitest "1000+" is a floor and holds (2,830 `it(`/`test(` call sites).
Tooling side, at rext `5fb0915e`: the **five** patch assignments are at `ant-academy.sh` `:56`, `:66`,
`:75`, `:84`, `:90` — all five exact — the M220 launch-env block is `:576-583`, and the cockpit's
`e2e_persona` sites are `cockpit.py:812` (client-side) and `:1496` (`Set-Cookie` on `/go`), with `:327`
naming both. Every anchor in that heavily-rewritten M257x block resolves.

### dependency_map.md + intelligence.md — **ENUMERATED**

Storage row: `storage/go.mod` has **no** redis (against a 3-hit control in `app/go.mod`), the `0dab54d`
storage block has `depends_on` on two `service_healthy` deps and no `DB_CONNECTION`/`REDIS_ADDR` in its
env, and `storage.md:40` / `:47` do say *"owns no database"* / *"Database: none"* — the cited pair is
exact. Studio-Room row: `app/studio/services/ai.py:705-708` is the **entire** provider registry
(`openai`, `azure`, `anthropic` — no Mistral) and `tools/pdf2md.py:24` is `from mistralai import Mistral`;
`git -C stack-demo/app/studio grep -i mistral aeec036a` returns **22 hits / 3 files**, exactly as claimed —
and note this only works because the nested repo was grepped at its **own** ref, per the ADDENDUM. All
three `app/studio/configs/*.ini` pin `azure` on **all 30** `*_MODEL` rows, so
`architecture_overview.md:288-291`'s "selected by no shipped config" is right. `intelligence.md` (18
lines) makes no claim I can falsify: it is correctly scoped as archived, and both env var names and the
`fdfa189` attribution are consistent with every sibling doc.

---

## What I could not settle

- **GitHub archive states.** `graphql-wundergraph` "ARCHIVED on GitHub (2026-07-30)"
  (`architecture_overview.md:3`), skiller 2026-07-01 / skillpath 2026-07-31 / chronos "NOT archived, last
  push 2026-04-23" (`roadrunner.md:38`, `:56-57`), and `roadrunner.md:74`'s "(**not** archived)". Archive
  state is repo metadata, invisible in a clone. Settled only by `gh api repos/anthropos-work/<r> --jq
  .archived`. Notably `architecture_overview.md:20` and `roadrunner.md:46` already *decline* to assert
  jobsimulation's, which is the right posture and makes the other four look under-hedged by comparison.
- **`infrastructure`-repo claims.** `module.cms_euwest1`'s actual disposition, and the prod
  `skiller_rpc_addr = http://backend.internal.anthropos:8081` at `dependency_map.md:27`. The repo has
  never been in any clone set; the corpus says so itself and marks these UNMEASURABLE.
- **`proto`, `colony`, `ai`, `taxonomy` internals.** Package inventories
  (`shared_libraries.md:46-59`), the `taxonomy` repo being "131 lines" (`:193`), the standalone `authn`
  tag `v1.7.0` and its missing-`return` bug (`:148`, `:169-171`), Mistral's panic-on-non-OCR
  (`:121`), the retry policy (`:138`). Private modules, in no clone set — pulled only at Docker build.
  A `GOPRIVATE` `go mod download` or a read-only clone would settle all of them.
- **The `>= 22,470 / >= 42,790` taxonomy figures** (`shared_libraries.md:207-214`,
  `architecture_overview.md:13`, `:236`). Provenance is a 2026-06-29 read-only prod capture manifest
  under `.agentspace/snapshots/`; re-deriving needs a live DB. The *internal* logic is sound and
  self-consistent across all three sites: 18K is below a measured floor (REFUTED), 60K is above it
  (UNVERIFIED), and both are consistently written as floors.
- **Live-demo behavioural claims** — "65 real cards", "0 course cards on `/library`", the M245
  durability reconciliation (`ant-academy.md:74`, `:203`, `:211-218`), the M238 HTTP 404→200 proof on
  `billion`. These need a running stack; the tooling-side anchors backing them all resolve.

### One out-of-scope observation, recorded because it is a live corpus self-contradiction

Not booked (root `CLAUDE.md` is outside `corpus/services/**` + `corpus/architecture/**`), but it
contradicts my assigned files and §5 rule 19's corollary says instruction files are the
highest-propagation site in the tree:

- Root `CLAUDE.md` still states *"`storage` sits in `profiles: [storage-legacy]` and `messenger` in
  `profiles: [messenger]`, both kept startable only as rollback paths"* and cites
  `storage/terraform/main.tf:38` / `messenger/terraform/main.tf:29` at `service_desired_count = 0`.
  `architecture_overview.md:25-33`, `:71`, `:188-195` and `dependency_map.md:19`, `:21` all say the
  opposite — `838d907` deleted both services **and** both profiles — and ground truth agrees
  (`grep profiles: docker-compose.yml` returns four lines, none of them those). The terraform anchor is
  stale in the other direction too: at the pinned checkout `4ce8ece5`, `storage/terraform/main.tf:19`
  reads `= 1`; `= 0` at `:38` was true only at `63bffc8`. **My assigned files are the correct side.**
- Root `CLAUDE.md` cites `corpus/architecture/external_services.md:546` twice for the "no ordered
  EU-first fallback ladder" claim. `:546` is a GraphQL codegen step (*"Restart Wundergraph"*, struck
  through). The correct anchor is `:579`, which both `architecture_overview.md:283` and
  `shared_libraries.md:130` cite correctly.

---

**BLOCKERS=2 MINORS=6**
