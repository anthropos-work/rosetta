# Seat A — reading #22

Instrument: `briefing-AS-DELIVERED.md` (222 lines) read in full, plus
`corpus/ops/platform-alignment.md` §5 (lines 249–925) read in full before starting.

Ground truth re-verified at this seat's open (`git -C <clone> rev-parse --short=8 HEAD`):

| repo | sha measured | matches briefing/ADDENDUM |
|---|---|---|
| app | `b948604f` | yes |
| platform | `0c91421d` | yes (ADDENDUM supersession) |
| next-web-app | `bb3313bc` | yes |
| sentinel | `88bc5592` | yes |
| storage | `4ce8ece5` | yes |
| messenger | `fa47850d` | yes |
| cms | `ca50c817` | yes |
| graphql-wundergraph | `60c229f3` | yes |
| roadrunner | `87d8d443` | yes |
| jobsimulation | `462343b0` | yes |
| studio-desk | `14a5442a` | yes |
| ant-academy | `9c3843cd` | yes |
| app/studio, cms/studio (nested) | `aeec036a` each | yes |
| rosetta-extensions (authoring) | `5fb0915e` on `main` | yes |

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/architecture/external_services.md` | 883 | yes |
| `corpus/services/chronos.md` | 245 | yes |
| `corpus/services/messenger.md` | 189 | yes |
| `corpus/architecture/frontend_architecture.md` | 105 | yes |
| `corpus/services/gotenberg.md` | 84 | yes |
| **total** | **1506** | — |

Search-hygiene controls run in the same pass (§5 rules 1–6, ADDENDUM "three mechanisms"):

- Every `git grep` in this pass ran **at a named ref**, and every absence claim was paired with a
  positive control that returned non-zero. Two of my own loop-based greps returned a **false
  all-zero** (`GOTENBERG` across 12 clones) and a **false all-one**; both were caught by the
  positive control and re-run per-repo with stderr visible. The corrected per-repo run is what the
  clearances below rest on. Recording this because an unreviewed loop is exactly rule 1's failure.
- NUL-byte measurement on the two Go files whose absence-arguments I leaned on
  (`app/internal/cms/studio/markdownManager.go`, `app/internal/jobsimulation/ai/ai.go`):
  `LC_ALL=C tr -dc '\000' < f | wc -c` → **0** each.
- Nested repos: every `app/studio` and `cms/studio` predicate was run against **`aeec036a` in
  those repos' own checkouts**, never against `app`'s HEAD.

---

## Blockers

### B1 — `external_services.md` cites the Directus re-point at a CORS comment block, 29 lines off, and the consumer tuple at the line that states its exception

- **Anchor:** `corpus/architecture/external_services.md:208-209` —
  > "…and `rosetta-extensions/stack-injection/gen_injected_override.py:669-670` re-points every
  > service in `DIRECTUS_DATA_CONSUMERS`, which is **`("cms", "backend")`** (`:84`)."

- **Why it is false:** measured in the briefing's own ground-truth authoring copy
  `.agentspace/rosetta-extensions` @ `5fb0915e` (`main`):

  - The re-point is at **`:698-699`**:
    `698: if with_directus and name in DIRECTUS_DATA_CONSUMERS:` /
    `699:     env.append(f"DIRECTUS_BASE_ADDR={DIRECTUS_INNETWORK_ADDR}")`.
  - **`:669-670`** sit inside an entirely different mechanism — the `backend` **CORS** comment
    block: `669: # CORS_EXTRA_ORIGINS in non-production (a runtime hook, no platform change), so
    feed it this stack's` / `670: # offset frontend origins. Additive env (list form merges with
    the base service env).` Nothing on those lines mentions Directus. The anchor resolves and names
    the wrong construct.
  - **`DIRECTUS_DATA_CONSUMERS = ("cms", "backend")` is at `:86`**, not `:84`.
    `:84` is `# (DIRECTUS_PUBLIC_BASE_ADDR) is deliberately NOT re-pointed — it stays on prod public
    links so` — the ASSET-plane *exception*, i.e. the line that says a Directus address is **not**
    re-pointed, cited as the line that says which services **are**.

  The passage names no ref, so per the briefing it grades at the checkout, which is what I read.

  Corroborating that this is drift the last repair missed rather than an authoring slip: the
  **third** anchor in the same passage — `stack-injection/tests/test_injection.py:1109`
  (`external_services.md:219`) — is **exact** (`1109: def
  test_backend_the_actual_reader_is_repointed(self):`). One of three re-pointed, two left. That is
  §5 rule 19's half-repair signature.

- **Confidence:** high.
- **Hesitation (stated per the briefing):** `rosetta-extensions` is an authoring copy on a moving
  `main`, so these two anchors may have been correct when written. I book it anyway because the
  briefing names that clone as ground truth, the passage carries no ref-pin, and `:84`'s content is
  not merely off-by-two — it states the opposite exception.

### B2 — `messenger.md` pins its `consumer` table row to `app` origin/main but leaves two prose anchors unpinned, and neither resolves at the graded ref (one names a file that does not exist there)

- **Anchor:** `corpus/services/messenger.md:53` (and the twin at `:149`) —
  > `:53` "…since the v9.0 fold `backend` does its work in-process, gated by `MESSENGER_ENABLED`
  > (`app/env_guards.go:61`), which defaults to **off** on a developer machine."
  >
  > `:149` "(That fallback is standalone-only: `app` did **not** port it — `app/main.go:295`
  > `log.Fatalf`s if `MESSENGER_ENABLED` is on with an empty key.)"

- **Why it is false:** neither block names a ref, so both grade at the `app` checkout `b948604f`
  (v1.366.0) — the tree on disk. Measured there:

  - `git ls-tree b948604f -- env_guards.go` → **empty**. `ls env_guards.go` → *No such file or
    directory*. The file **does not exist at all** at the graded ref.
  - `git ls-tree b948604f -- internal/messenger` → **empty**; `git grep -n MESSENGER_ENABLED
    b948604f` → **0 hits** across the whole tree (positive control: `internal/converter` resolves at
    the same ref).
  - `main.go:295` at `b948604f` is `if err != nil {` — the error branch of
    `openai.NewAzure(AZURE_OPENAI_KEY, AZURE_OPENAI_ENDPOINT_URL, nil)` at `:294`. Not a
    `log.Fatalf`, not messenger.
  - I then tried the **second** ref this same file names — `app` `9d00a313` v1.367.0 (banner,
    `:37-38`). It fails there too: `env_guards.go:61` is a prose comment
    (`// GetBucketLocation rather than HeadBucket, deliberately. …`) and `main.go:295` is `}`.
  - Both resolve only at **`app` origin/main** (`2035f9a4`): `env_guards.go:61` =
    `envMessengerEnabled      = "MESSENGER_ENABLED"`; `main.go:295-299` = the
    `if (messengerEnabled || customerIOSyncEnabled) && os.Getenv("BREVO_KEY") == "" { log.Fatalf(...)`.

  What makes this a defect rather than ordinary staleness is that **the same document already knows
  this**: the `consumer` row at `:43` writes "@ `app` origin/main" and adds "**Every anchor in this
  row moved between `9d00a313` and origin/main without the code moving**". The convention exists,
  was applied to the table, and was not applied to these two prose bullets. A reader on the stack's
  own `app` clone opens `env_guards.go` and finds nothing.

- **Confidence:** medium. The substantive claims (MESSENGER_ENABLED gates the fold and is unset/off
  locally; `app` fatals on an empty `BREVO_KEY`) are **true** at origin/main; it is the ref-pin that
  is missing. I hesitated between MINOR and BLOCKER and booked up because one of the two anchors
  names a file absent from the graded tree entirely, which reads to a reader as a fabricated
  citation rather than as line drift.

---

## Minors

- `corpus/services/messenger.md:42` — the `prod` row cites "`messenger/terraform/main.tf:29`
  `service_desired_count = 0` … a one-line revert plus an apply (`:27-28`)". At the audit's pinned
  `messenger` checkout `fa47850d`, `:29` is `container_definitions          = <<EOF` and
  `service_desired_count` is at **`:19` with the value `1`**; `:27-28` are
  `service_discovery_namespace_id` / `monitoring_sns_topic_arn`. Every anchor and the value are
  **exactly right at `messenger` `e9421c6`** (origin/main — `:19-28` is the v9.0 comment block,
  `:27-28` are literally "The image and task definition stay declared: this is the rollback path.
  / Restoring service is a one-line revert plus an apply, not a re-provision.", `:29` is
  `service_desired_count          = 0`), which the banner's lead sentence names four lines above the
  table (`:37-38`). Kept MINOR because that sentence introduces the table it dates; flagged because
  §5 rule 33's first mechanism is exactly a pin that lives outside the cell it is being read into,
  the cell asserts present tense ("the compute **is** stopped"), and the sibling `consumer` row
  demonstrates the per-row convention this row skipped. Same claim also stands **unpinned** in root
  `CLAUDE.md` ("Prod compute for both is **stopped** (`storage/terraform/main.tf:38`,
  `messenger/terraform/main.tf:29`, each `service_desired_count = 0`)") — out of my partition, but
  it is the higher-propagation site (§5 rule 19 corollary).
- `corpus/architecture/external_services.md:455` — the verbatim quotation *"Since cms-in-app the
  platform compose `graphql` service builds from the **production** Dockerfile"* is attributed to
  `graphql-wundergraph/CLAUDE.md:39`. At `60c229f3` that exact text is at **`:33`** (a table cell).
  `:39` makes the same wrong assertion in different words ("Since the compose stack builds from the
  production Dockerfile, a schema change in `app` does not appear at `:5050`…"), so the substance —
  that the archived repo contradicts this section — holds; only the quote attribution is off.
- `corpus/architecture/external_services.md:387` — "the six `Subscription` hits in that SDL are all
  Stripe/plan **field names**". Enumerated: `backend.graphqls` at `60c229f3` has exactly six
  (`:1528, :1555, :4385, :4389, :4391, :4418`) — count correct — but `:4391` is a doc-comment
  sentence ("Stripe customer id for the buyer. Optional companion to stripeSubscriptionId."), not a
  field name. Five of six.
- `corpus/architecture/frontend_architecture.md:39` — the count "**29 of them across 21 non-test
  files**" is exactly right (enumerated below), but the colon-introduced enumeration that follows
  accounts for only **25** of the 29. Unnamed: `apps/hiring/src/hooks/useStripe.ts`,
  `packages/graphql/src/hooks/user/useManageSubscription.tsx`,
  `packages/ui/src/AISimulation/Onboarding/TestRuns/TestRunNetwork.tsx`, and
  `apps/web/src/app/(unauthenticated)/reminders/[token]/unsubscribe/page.tsx`. (Omitted list
  members; the arithmetic and the denominator both verify.)
- `corpus/architecture/external_services.md:849-852` — unclosed parenthesis inside the bash block:
  "`# Check backend's own dependencies are up (docker-compose.yml:101-109 — …`" runs four comment
  lines and never closes. Cosmetic; the contents are all correct.

---

## Positively cleared (audited zeros)

Labelled per the briefing. **ENUMERATED** = I derived the predicate's set from source independently
and compared cardinalities. **RE-DERIVED** = I only re-ran the arithmetic the document shows.

### ENUMERATED

1. **`external_services.md:168-175, 306` — the compose service set and the `core` floor.**
   `git show 0c91421d:docker-compose.yml` declares exactly **5** services (`sentinel:5`,
   `backend:28`, `studio-desk:112`, `next-web-app:143`, `gotenberg:170`; `app-network:186` is a
   *network*, not a service) + `postgresql`/`redis` from `include: common.yml` = **7 effective**.
   Profile-less (therefore in every selection): `sentinel`, `postgresql`, `redis`. `core` members:
   `backend` (`:110 profiles: [core, backend, all]`) and `gotenberg` (`:183`, same). **Five
   containers.** At `0dab54d` the same enumeration gives **8/10** (adds `storage:102`,
   `customerio-sync:136`, `messenger:156`) — the document's "eight/ten at `0dab54d`" is exact. The
   `graphql` token appears in **no** `profiles:` key at either ref.
2. **`external_services.md:172, 197, 249, 768, 849` + `frontend_architecture.md:11, 39` +
   `gotenberg.md:14, 19-28, 50` — every platform compose line anchor.** All exact at `0c91421d`:
   `:53` `DIRECTUS_PUBLIC_BASE_ADDR` inside the `environment:` block that runs `:46-94`; `:101-109`
   backend `depends_on` = redis/postgresql/sentinel; `:110`/`:183` the two `core` profile lines;
   `:119`/`:135` studio-desk build-arg + runtime `VITE_GRAPHQL_ENDPOINT`; `:151`/`:160`
   next-web-app `NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT`; `:152`/`:161` `NEXT_PUBLIC_BACKEND_API_URL`;
   `:141` `profiles: [studio-desk, all]` (was `:226` at `0dab54d` — also verified); `:84-92` the
   messenger-in-app comment; gotenberg's whole `command:` block verbatim; `GOTENBERG_URL=http://
   gotenberg:3200` at `:57`. File length **186** lines, as `gotenberg.md:14` states. All four
   frontend GraphQL endpoints are `:8082/graphql/query`.
3. **`external_services.md:373-386` — the supergraph subgraph counts.** Read the *set* off
   `supergraph-config-prod.yaml` at each named commit, not the sum: `749dc86~1` = **5**
   {backend, skiller, jobsimulation, cms, skillpath}; `749dc86` = **4**; `7c17e63` = **3**;
   `915da06` = **1**. `git show --name-status 915da06` marks **both** `schemas/cms.graphqls` and
   `schemas/jobsimulation.graphqls` `D`, and its own subject really does read "supergraph 2→1"
   against a 3-entry tree — the document's warning is correct. `schemas/` holds `backend.graphqls`
   alone; `subgraphs.conf` = `BACKEND=v1.360.0`. Dates confirmed (2026-06-24 / 07-21 / 07-29).
4. **`external_services.md:387` — "no subscriptions", both halves.** Reproduced both greps
   verbatim including the positive control: `grep -rn "sse\|subscription" graphql-wundergraph/*.yaml`
   → rc 1, no output; `grep -rln backend graphql-wundergraph/*.yaml` → the three
   `supergraph-config-*.yaml`. `backend.graphqls` declares `type Mutation` at **`:4053`** and
   `type Query` at **`:4912`** and **no** `type Subscription`.
5. **`external_services.md:47-69, 791` — the Clerk SDK-per-app claim.** Enumerated from each repo's
   `package.json`/`go.mod`: next-web-app `@clerk/nextjs ^6.39.2` + `@clerk/clerk-expo ~2.6.18`;
   ant-academy `@clerk/nextjs` + `@clerk/clerk-expo`; studio-desk `@clerk/clerk-js ^5.52.3` +
   `@clerk/express ^1.3.47`; `app` `clerk-sdk-go/v2 v2.7.0` + `colony/authn`. `sentinel` at
   `88bc5592` has **no** HTTP webhook route (its only `webhook` hits are CI Mattermost URLs;
   positive control `casbin` matches). `backend.go:130` is the `/api/webhook/clerk` entry in the
   authn skip-list, and the POST route itself is registered at
   `internal/web/backend/api/server.gen.go:2372`.
6. **`external_services.md:554-643` — the four-live/one-latent EU-residency predicate.** Enumerated
   the vendor consts (`internal/jobsimulation/ai/ai.go:30-33` — exactly four: azure, openai,
   anthropic-aws, anthropic), the dispatch (`getClient` is `:259-289`; azure/flag `:264-276`;
   AnthropicAws+Anthropic → the same `anthropicClient` `:280-283`), the 429-only retry
   (`isThrottlingError` `:130-141`; `vendor = Openai` at `:150-155` and mirrored `:296-302`/`:326`),
   the plain OpenAI client at `:80`, the caller-side default (`internal/cms/directus/collections/
   jobsimulation.go:905` `AIVendor *AIVendor` nullable; `:1302` `aiVendor := simulation.Openai`;
   `internal/jobsimulation/simulator/ai/ai.go:58-59` and the `default:` arm `:114-115` → both
   `internalAi.Openai`), and the Studio-Room arms (`services/ai.py:627-664` AnthropicProvider,
   `:704-724` `get_client` with the three-provider dict `:706-708`, `:383` `return OpenAI(...)`,
   `configs/config_template.ini:30-31` `https://api.openai.com/...`). The latency claim that
   nothing selects the openai/anthropic arms is **enumerated, not asserted**: all **30**
   `*_AI_*_MODEL` lines across the **three** `configs/*.ini` have first token `azure`, and
   `gen.py:41-53` overrides only the six `*_API_KEY`/`*_ENDPOINT` names — never the service
   selector.
7. **`external_services.md:565, 597-599` — "every use of Mistral in `app` is OCR".** Enumerated all
   mistral hits in `app` @ `b948604f`: `go.mod`/`go.sum`, `internal/cms/studio/markdownManager.go`
   (`:19` `mistral.NewMistral`), `studioManager.go` (`:583` `NewMarkdownManager`), `xlsx.go` (a
   comment saying Mistral OCR rejects xlsx), terraform, and docs. Zero non-OCR call sites. In the
   nested `app/studio` @ `aeec036a`, `git grep -i mistral` returns only `requirements.txt`,
   `tools/pdf2md.py` and `tools/r3.py` — so "`tools/r3.py` DOES dispatch it, no Go caller and no
   `gen.py` path does" holds, and `r3.py:139`/`:190`/`:199-206` and `pdf2md.py:24` all land on the
   cited constructs.
8. **`external_services.md:723` — the LiveKit agent names.** `calls/livekit.go:110`
   `agentName := "anthropos-agent"`, `:115` `"anthropos-agent-chain"`, `:120` `"anthropos-agent"`,
   `:126` `fmt.Sprintf("anthropos-agent-%s", *location)`. `anthropos-agent-eu` returns **0** in all
   12 clones **and** both nested `studio` checkouts.
9. **`external_services.md:142-148` — the historical Directus compose block.** `a2a3ee6^`
   `docker-compose.yml` really does read `:384 image: directus/directus:10.10.1`, `:386 - 8055:8055`,
   `:409 - ADMIN_PASSWORD=password`; and `git log --all -S 'admin@example.com'` returns nothing while
   the positive control `-S 'ADMIN_PASSWORD'` returns three commits. The correction-of-the-correction
   is right on both sides.
10. **`messenger.md:17` — the 21/22 handler split.** Enumerated `pubsub.EventHandler(` lines inside
    `flow.go:72-104` @ `fa47850`: 22 lines, of which `:83` is `// pubsub.EventHandler(h.OrgJob
    SimulationAssignmentPastDueHandler), // not implemented` → **21 live**, and the commented-out one
    is exactly the handler named. `:72` is `h.subServer.AddSubscriber("backend", …)`. `:74-78` are
    exactly the five `OrgSkillPath*` handlers `messenger.md:108` cites.
11. **`messenger.md:19-32, 108` — "nobody fires a Messenger RPC".** `MESSENGER_RPC_ADDR` returns
    **0 files** in all 12 clones at their named refs **and** in `app/studio` + `cms/studio` @
    `aeec036a` (positive control: `SKILLER_RPC_ADDR` matches `messenger/cmd/root.go:135` and
    `messenger/terraform/main.tf:78`). The `-S` figures reproduce exactly: `MESSENGER_RPC` on
    `0c91421d` = **0**, `SKILLER_RPC` = **7**; with `--all`, **0** and **8**. Platform history is
    **121** commits. `*_RPC_ADDR` count in `docker-compose.yml` / `common.yml` / `.env_example` at
    `0c91421d` = **0 / 0 / 0**.
12. **`gotenberg.md:7, 42, 47` — "two call sites, `app` is the only consumer".** Enumerated:
    `git grep "ConvertToPDF(" -- '*.go'` at both `9d00a313` and `b948604f` returns the definition
    (`internal/converter/gotenberg.go:16`) plus exactly **two** callers —
    `internal/web/backend/coursebuilder/extract.go:77` and
    `internal/worker/tasks/user_import_resume_2d.go:68`. `gotenbergMimes` (`extract.go:17-27`) has
    exactly **nine** entries — xls, xlsx, ppt, doc, odt, ods, odp, `application/rtf`, `text/rtf` —
    and **no** wordprocessingml, so "DOCX is not among them" is right. Per-repo `gotenberg` search
    (re-run after my loop produced a false zero) matches only `app` and `platform`; zero in the two
    nested `studio` checkouts.
13. **`frontend_architecture.md:39` — the 29/21 REST surface.** Enumerated the whole set at
    `bb3313bc`: 44 total `NEXT_PUBLIC_BACKEND_API_URL` occurrences across 33 files; removing tests
    (`*.test.ts`, `e2e/`), docs (`knowledge/*.md`, `BACKEND-SECURITY-CONTRACT.md`) and config
    (`Dockerfile.dev`, two `.env.example`, `terraform/main.tf`, `turbo.json`) leaves **21 code files
    / 29 sites** — 13 files × 1 + 4 core-js clients × 3 + 4 files × 1 = 29. The pinned forward
    figure also verifies: at `ad767d1b` the set gains `MemberAnalyticsContainer.tsx:2` → **31 across
    22**, exactly as written.
14. **`frontend_architecture.md:11, 21-25, 34-35, 48-51` — the monorepo facts.** `repos.yml` at
    `0c91421` = exactly **4** entries (app, sentinel, next-web-app, studio-desk); at `2adcf71` = **9**;
    ant-academy in neither. `!apps/mobile` present in `pnpm-workspace.yaml`. Ports read off the dev
    scripts: web 3000, hiring 3001, integration 3002, mobile `expo start --port 3031`. Package names
    match the documented `--filter` targets (`@anthropos/web-app`, `@anthropos/hiring-app`,
    `@anthropos/integration`). `apps/web/package.json:46` = `"next": "^16.2.7"`;
    `packageManager: "pnpm@10.30.3"`; `"node": ">=24.0.0"`; `UPGRADE-IMPACT-next16.md` present;
    `configs/i18n/messages` holds exactly the 8 named locales; `packages/graphql/src/__generated__`
    exists.

### RE-DERIVED (weaker — flagged as such per §5 rule 24)

- `external_services.md:668-673` — the AI env-var counts. `command grep -rho '\b<NAME>\b'
  --include='*.go'` in `app` returns **5 / 26 / 13 / 13** for `OPENAI_KEY` /
  `ANTHROPIC_API_KEY` / `AZURE_OPENAI_KEY` / `AZURE_OPENAI_ENDPOINT_URL`, and **0 / 0 / 0** for
  `OPENAI_ORG_ID` / `AZURE_OPENAI_ENDPOINT` / `AZURE_OPENAI_DEPLOYMENT`, and **2** for
  `OPENAI_API_KEY` — i.e. every figure the document shows. This is the document's *own* derivation
  re-run; I did not independently enumerate "the set of AI env vars `app` reads", so treat it as the
  weakest clearance in this report.
- `external_services.md:441-455` — the three Dockerfile eras. Verified each transition
  (`2c85211^` has no `dockerfile:` key; `2c85211` introduces `Dockerfile.dev`; `67ba772` 2026-03-02
  raises the context; `b56d731` still has `  graphql:` at `:22` while `360efd4` has none, with
  `  backend:` at `:28` as the positive control; `1e8e754` lines 6-8 read
  `dockerfile: graphql-wundergraph/Dockerfile.dev`), and the pre-drop `depends_on` at
  `1e8e754:docker-compose.yml:19-27` naming backend/jobsimulation/cms/storage, unchanged since
  `a4db680` (2026-07-21). `464dfe3` is confirmed **non-ancestor** of `0c91421d` (rc 1) and lives only
  on `origin/feat/cms-in-app`. I re-ran the document's checks rather than independently enumerating
  the compose history.
- `chronos.md` — only two of its claims are reachable from the clone set at all, and both hold:
  platform `045857c` (2026-04-17, "remove chronos service from orchestration and update related
  documentation") and jobsimulation `09631fb2` (2026-04-17, "remove Chronos references and update
  documentation to reflect Asynq integration for session timeout management"), plus
  `app/internal/jobsimwiring/worker.go:24-35` (the `asynq` import through `StartWorkers`' first
  pool), which supports "those Asynq workers now run in the `backend` process".

---

## What I could not settle

- **`chronos.md` lines 13–245 (the bulk of the file).** The `chronos` repo is not in the clone set
  and the briefing names no ref for it, so the schema DDL, the `timers` indexes, the `ChronosService`
  RPC shapes, the triggerer's `LIMIT 10` / `FOR UPDATE SKIP LOCKED` / 3-second-timeout / 1-minute
  Sentry heartbeat behaviours, the ports (8080/8081) and the CLI flags are all **unverifiable from
  this vantage**. A `chronos` clone at a named sha would settle them in one pass. I did not find any
  internal contradiction in the file, and its two live cross-repo claims verify.
- **`external_services.md:3` — "the repo is ARCHIVED on GitHub (2026-07-30)".** Archival is a GitHub
  API property, not a git property; no offline evidence can settle it.
- **`external_services.md:175` — the prod-side cms/jobsimulation terraform state.** The document
  itself declares this UNMEASURABLE and it is right: `cms/terraform/main.tf:30,39` still declares
  `module "cms"` at `service_desired_count = 0` at **both** the checkout `ca50c817` and cms
  origin/main `f38c0c4a`, while `6efa1d5` (2026-08-04) really did delete cms's build-production
  workflow with that exact M810 wording; and `6092c6d2` really does remove the `module
  "jobsimulation"` block — but **it is one commit past the pinned jobsimulation checkout**
  (`462343b0`, where `terraform/main.tf:31` still declares the module at
  `service_desired_count = 0`). The claim is graded true at the ref it names; a reader on the pinned
  clone will see the opposite. Only an `infrastructure`-repo read or a live AWS query settles the
  actual prod state.
- **`external_services.md:725-739`** — LiveKit "Audio: Recorded as MP3", transcript generation, and
  the Chime "composited MP4 grid view" describe third-party runtime behaviour; nothing in the clone
  set asserts or refutes them.
- **Whether B1's two rext anchors were correct when written.** `.agentspace/rosetta-extensions` is a
  moving `main` and I did not (and per the read-only bar would not) walk its history to find the ref
  at which `:669-670` and `:84` were right. A ref-pin on that passage, or a `git log -L` over
  `gen_injected_override.py`, would settle it — and would also be the repair.
