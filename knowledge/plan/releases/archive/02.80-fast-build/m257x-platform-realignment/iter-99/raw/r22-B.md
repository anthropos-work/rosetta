# Seat B — reading #22

Ground truth read at the ADDENDUM shas: platform `0c91421`, app `b948604f` (origin/main `2035f9a4`,
newest tag `v1.369.0` @ `1166453`), next-web-app `bb3313bc`, cms `ca50c817`, storage `4ce8ece5`,
messenger `fa47850d`, jobsimulation `462343b0`, roadrunner `87d8d443`, graphql-wundergraph `60c229f3`,
app/studio `aeec036`, rosetta-extensions authoring copy `5fb0915`.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/services/ai-readiness.md` | 651 | yes |
| `corpus/services/cms.md` | 313 | yes |
| `corpus/services/storage.md` | 232 | yes |
| `corpus/services/coursebuilder.md` | 141 | yes |
| `corpus/services/README.md` | 88 | yes |
| `corpus/services/TEMPLATE.md` | 46 | yes |
| **total** | **1471** | |

Also read in full as instructed: `corpus/ops/platform-alignment.md` §5 (lines 249–948).

---

## Blockers

### B1 — `AI_READINESS_URL` cited at `urls.ts:52`; it lives at `:50` and `:52` is a different constant

- **Anchor:** `corpus/services/ai-readiness.md:304-306` —
  *"**`/ai-readiness` is the only readiness route the navbar links** — `AI_READINESS_URL`
  (`packages/core-js/src/constants/urls.ts:52`), consumed by …"*
- **Why it is false:** at next-web-app `bb3313bc`,
  `packages/core-js/src/constants/urls.ts:50` is `export const AI_READINESS_URL = '/ai-readiness';`.
  Line **52** is `export const ORGANIZATION_FEEDBACK_URL = '/enterprise/organization-feedback';`
  (`:49` = `WORKFORCE_URL`, `:51` = `TALK_TO_DATA_URL`, `:53` = `INSIGHTS_URL`).
  The anchor resolves but names the **wrong construct** — §5 rule 21's second class, and the passage
  is explicitly presented as *"still resolve at HEAD … so they remain checkable evidence"*
  (`ai-readiness.md:299-302`), which is exactly the promise this anchor breaks.
  The two other anchors in the same bullet DO resolve (`useNavbarSections.tsx:4` import,
  `:398-400` `aiReadinessMenuItem` / `key: AI_READINESS_URL` at `:400`, `:547`
  `showAIReadiness ? aiReadinessMenuItem : null`).
- **Hesitation:** it is 2 lines of drift, which the frozen instrument's own history records as
  sometimes-MINOR. Booked as a BLOCKER because the line the citation lands on *is* a live constant of
  the same file and family — a reader following it reads a real, wrong answer rather than noticing a
  miss, and the passage's whole rhetorical point is that these anchors were re-checked.
- **Confidence:** high

### B2 — intra-corpus self-citation `:458` lands on a blank line

- **Anchor:** `corpus/services/ai-readiness.md:46-47` —
  *"This paragraph stated completed work as outstanding and contradicted `:458` of this same file,
  which is already in the past tense; corrected M257x iter-46."*
- **Why it is false:** `ai-readiness.md:458` is **empty**. (`:457` = *"Route 2 is exactly the shape
  M51 seeds…"*, `:459` = the `> **✅ CORRECTED M219 …**` blockquote opener.) The past-tense statement
  about `app-aireadiness-snapshot-loadmembers` that the sentence is pointing at is at **`:490`**
  (*"**In the demo**, M51 iter-09 bounds it with the `app-aireadiness-snapshot-loadmembers` app
  read-path demo-patch"*) — the only past-tense site in the file (grep for `loadMembers`/`loadmembers`
  returns `:19,37,38,40,41,42,44,484,490,491,493,495`; none at or near 458).
  This is §5 rule 34 exactly: a repair that changed the file's line count silently invalidated the
  citation, and it landed on a blank line rather than on content.
- **Hesitation:** intra-corpus line drift with no factual consequence for the platform claim itself.
  Booked as a BLOCKER because the delivered instrument lists a wrong line anchor as a BLOCKER and the
  sentence's entire evidentiary weight is the cross-reference it cannot deliver.
- **Confidence:** high

### B3 — "Production terraform still names `http://backend.internal.anthropos:8081`" — no terraform in any clone names it, and cms.md elsewhere says that terraform is invisible to this corpus

- **Anchor:** `corpus/services/cms.md:196` —
  *"`messenger` was the last external one: M809 pointed its `CMS_RPC_ADDR` at `http://backend:8083`,
  and `838d907` deleted the messenger service and that variable with it, so compose sets it nowhere.
  **Production terraform still names `http://backend.internal.anthropos:8081`.**"*
  (Same literal, softer form, at `cms.md:55`: *"`http://backend.internal.anthropos:8081` in
  production."*)
- **Why it is false / unsupportable:**
  - The only production terraform that sets `CMS_RPC_ADDR` in any clone is
    `messenger/terraform/main.tf:74-75` @ `fa47850`, and it names a **variable**, not a literal:
    `{ "name": "CMS_RPC_ADDR", "value": "${var.cms_rpc_address}" }`. That variable is declared with
    **no default** (`messenger/terraform/variables.tf:77-80`, `type = string`, description only), so
    its value is supplied by the caller — the `infrastructure` repo.
  - Searched **every** clone at its own ref for `internal.anthropos`, `.tf` files included:
    `app platform next-web-app sentinel storage messenger cms graphql-wundergraph roadrunner
    jobsimulation studio-desk ant-academy`. **Zero `.tf` hits anywhere.** The literal exists in exactly
    two non-terraform places: `app/knowledge/service-dependencies.md:46`
    (*"messenger … reaches the users, cms, jobsimulation and skiller RPC surfaces at
    `http://backend.internal.anthropos:8081`"*) and, at port **8080** not 8081, the router's prod
    supergraph config (`graphql-wundergraph/supergraph-config-prod.yaml:6` +
    `CLAUDE.md:18`, `http://backend.internal.anthropos:8080/graphql/query`).
    Positive control on the same instrument: `git -C stack-demo/messenger grep -n "RPC_ADDR"
    fa47850 -- 'terraform/*'` returns 4 hits, so the pipeline was not silently empty.
  - **Self-contradiction inside the same file.** `cms.md:18` and `cms.md:83-84` state, twice and
    emphatically, that *"the deletion itself lands in `infrastructure`, **which has never been in any
    clone set we have**"* and *"Whether `infrastructure/terraform/production/services.tf` still
    declares `module.cms_euwest1` is **not visible to this corpus**"*. A doc that cannot see the
    production terraform cannot report what it "still names".
  - The *address* may well be right (app's own KB asserts it); what is unsupportable is the **source
    attribution**, which is the part a reader would act on.
- **Hesitation:** the underlying fact is corroborated by the platform's own KB, so this is a wrong
  provenance rather than a wrong value. Booked as a BLOCKER because §5's self-contradiction class
  (rule 21, "the cheapest one in any corpus") applies verbatim, and because the same file spends two
  paragraphs establishing the opposite epistemic limit.
- **Confidence:** medium-high

---

## Minors

- `corpus/services/ai-readiness.md:307-308` — *"A repo-wide grep finds the constant in exactly those
  two non-`node_modules` files."* Measured at `bb3313bc`: `AI_READINESS_URL` appears in **three**
  non-`node_modules` files — `packages/core-js/src/constants/urls.ts:50`,
  `packages/ui/src/NavBar/useNavbarSections.tsx:4,400`, and
  `knowledge/ai-readiness/frontend-architecture.md:15`. The third is a doc, so the intent is probably
  "source files"; the sentence as written is off by one.
- `corpus/services/coursebuilder.md:46` — *"`embed.go` (`//go:embed assets/*.md` rubric)"*. There is no
  `assets/*.md` glob in `internal/coursebuilder/embed.go` @ `b948604`; it carries **five** explicit
  directives (`:28` chapter-guideline.md, `:31` source-policy.md, `:34` content-format.md, `:37`
  scoring-rubric.md, `:48` widget-guidelines.md). The mechanism described is right; the quoted source
  text is not in the file.
- `corpus/services/coursebuilder.md:114-115` — *"~55 test files"* / *"~25 boundary test files"*.
  Measured @ `b948604`: `internal/coursebuilder/**` = **61** `_test.go` (59 at the top level);
  `internal/web/backend/coursebuilder/**` = **33**. Hedged with "~", but 25 → 33 is a 32 % gap.
- `corpus/services/README.md:41` (storage row) — *"In prod the ECS service is **deleted**, not scaled
  to zero — the module survives only to keep the buckets/CDN under `prevent_destroy`"*, with **no
  storage ref named** (the row's only ref, `838d907`, is a *platform* commit about the container).
  At the briefing's storage checkout `4ce8ece5`, `storage/terraform/main.tf` is **100 lines** and
  `:19` reads `service_desired_count = 1` — neither deleted nor zero. It is true only at `9f8cb53`
  (20 commits ahead, 18-line `main.tf`, no service block), which `storage.md:25` names *and warns
  about in as many words*: **"State the ref or this flips."** The index row is the site that flipped.
- `corpus/services/storage.md:44` — *"the live code is `app/internal/storage/` (+ `app/internal/storagens/`,
  `app/internal/publicstorage/`)"*, unpinned. `internal/storage/` **does not exist** at the app
  checkout `b948604f` (`git ls-tree -- internal/storage/` is empty); it exists at `2035f9a`
  (`doc.go`, `service.go`, `storage.go`, + 6 test files). Same class as the README row: true at
  origin/main, unstated ref. `storagens/` and `publicstorage/` do exist at both.
- `corpus/services/ai-readiness.md:581-582` — `computeCycleTotals` cited at `how_we_measure.go:253-261`.
  The doc-comment is `:256-259` and the signature is `:260`; `:253-254` are the tail of the *previous*
  function (`return out, rows.Err()` / `}`). The range contains the construct but opens inside a
  different one.
- `corpus/services/coursebuilder.md:132` — *"`app` is **`v1.369.0`** @ origin/main `2035f9a4`"*.
  `v1.369.0` is `1166453`; `2035f9a4` is origin/main and sits **2 commits past** that tag. Both halves
  are individually true; the `@` conflates a tag with a different sha.

---

## Positively cleared (audited zeros)

**ENUMERATED** (predicate re-derived from source independently, cardinality stated before arithmetic):

1. `README.md:3` *"Every service doc in this directory, enumerated."* — `ls corpus/services/*.md` = **29**
   files; minus `README.md` + `TEMPLATE.md` = **27** service docs. Extracted the 27 doc links from the
   README's own tables: the sets are identical. (Matches root `CLAUDE.md`'s "27 service docs".)
2. `README.md:11` *"**Seven** services in this index … folded into `app`"* — enumerated skiller,
   skillpath, jobsimulation, cms, storage, messenger, customerio-sync = **7**; roadrunner is separately
   named as the eighth. `app/internal/messenger/` and `app/internal/customeriosync/` both exist at
   `2035f9a`, gated by `MESSENGER_ENABLED` / `CUSTOMERIO_SYNC_ENABLED` (`app/env_guards.go:61-62`).
3. `README.md:29-30` / `cms.md:53-54` / `storage.md:28` — compose declares **5** services at
   `0c91421` (`sentinel:5`, `backend:28`, `studio-desk:112`, `next-web-app:143`, `gotenberg:170`),
   **7** effective with `common.yml`'s `postgresql:2` + `redis:24`; `repos.yml` carries **4** entries
   (app, sentinel, next-web-app, studio-desk) — read the file, not a count.
4. `cms.md:252-253` / `storage.md:175` — the always-on floor. Enumerated services with **no
   `profiles:` key** across `docker-compose.yml` **and** the resolved `common.yml`: `{sentinel,
   postgresql, redis}` = **3**. `backend` and `gotenberg` both carry `profiles: [core, backend, all]`
   (`:110`, `:183`), so a retired token really does start the floor and no application.
   `Makefile:10` `PROFILE ?= core`.
5. `cms.md:60-63` / `README.md:52` — the subgraph ladder. Counted `*.graphqls` at each ref in
   graphql-wundergraph: `749dc86~1`=**5** → `749dc86`=**4** → `7c17e63`=**3** → `915da06~1`=**3** →
   `915da06`=**1**. `915da06` deletes `schemas/cms.graphqls` (−762) **and**
   `schemas/jobsimulation.graphqls` (−860) in one commit — so **3→1**, and the commit's own subject
   *"(supergraph 2→1)"* is the wrong figure, exactly as the corpus says.
6. `README.md:44` — *"21 live handlers"* in `messenger/internal/flow/flow.go:72-104` @ `fa47850`.
   Enumerated the `pubsub.EventHandler(` lines in that exact range and **excluded the commented-out
   one** (`:83 // pubsub.EventHandler(h.OrgJobSimulationAssignmentPastDueHandler), // not implemented`):
   5+3+1+1+1+2+1+4+3 = **21**. `AddSubscriber("backend", …)` at `:72` ✓. (§5 rule 24 — `grep -c`
   would have returned 22.)
7. `README.md:44` — `MESSENGER_RPC_ADDR` absence. Grepped **all 12 clones at their own named refs**
   (`git grep`, so `.gitignore`d tracked files are visible): **0** hits everywhere.
   `git -C stack-demo/platform log -S 'MESSENGER_RPC' --oneline 0c91421d` = **0**; positive control
   `-S 'SKILLER_RPC'` at the same repo+ref = **7**. Nested `app/studio` and `cms/studio` are separate
   checkouts at `aeec036` and were grepped separately — 0 there too.
8. `ai-readiness.md:252-256, 356, 550` — the 31-skill default. Read
   `app/internal/aireadiness/defaults.go` and counted entries, not the prose: `:31-49` = **19** at
   weight 1.0, `:51-62` = **12** at 0.5 → **31**, denominator 19 + 6 = **25.0**. `defaultReadinessSims`
   (`:75-79`) = **3** entries with tracks `tech` / `business` / `both` ✓.
9. `ai-readiness.md:201, 213-219, 622-624` — the five usage KPI ids. Enumerated
   `usageDimSpecs` (`how_we_measure.go:1138-1159`) = **5**: `avg_adoption`, `avg_transformation`,
   `avg_originality`, `avg_depth`, `avg_ownership`. Enumerated the seeder's emitted ids
   (`rext stack-seeding/seeders/ai_readiness_interview_report.go:185-189`) = the same **5**; comment at
   `:149` reads *"All five are always emitted here."* verbatim. Note the *platform's own* comment at
   `how_we_measure.go:1161` still says "four" — the corpus's "5" is right and the source comment stale.
10. `ai-readiness.md:275` — CSV is 15 columns. Enumerated `AIReadinessCSVColumns`
    (`aireadiness/csv.go:17-33`) = **15** entries; the in-source comment records the drop from 19.
11. `ai-readiness.md:167-183` — the data-model table. Checked each of the **14** named tables against
    `internal/data/ent/migrate/schema.go` @ `b948604`; all 14 present, including the plural
    `ai_readiness_user_step_progresses`, `ai_readiness_notification_logs`,
    `ai_readiness_notification_optouts`, and `interview_aggregated_reports` with
    `(sim_id, organization_id, report JSON, session_count)` at `:2338-2341`.
12. `ai-readiness.md:239-243` — the REST surface. Enumerated the registered routes in
    `internal/web/backend/api/server.gen.go:2396-2404` + the interface comments at `:84-114`:
    `/workforce/ai-readiness`, `/compare`, `/cycles` (GET+POST), `/cycles/{cycleID}` (GET),
    `/cycles/{cycleID}/close` (POST), `/export.csv`, `/narrative` (POST), `/setup` (GET+POST),
    `/steps-completion` — every one the doc names, and nothing it names is missing.
13. `ai-readiness.md:71-74` — *"Its **sole consumer repo-wide**"*. Repo-wide grep of both
    `AI_READINESS_FLAG` and `flag_ai_readiness` at `bb3313bc`: the only `useFeatureFlagEnabled` call
    site is `useAiReadinessActive.ts:22`. (The file-count sentence is the MINOR above; the
    sole-consumer predicate itself holds.)
14. `ai-readiness.md:76-81` — *"the MANAGER dashboard does NOT check the flag"*.
    `AIReadinessClient.tsx` @ `bb3313bc`: `grep -ci posthog` = **0**, `grep -c flag_ai_readiness` = **0**;
    `:133-134` is `const { orgEnabled } = useAiReadinessEnabled(true);` + `featureOn`.
15. `storage.md:29` — the `STORAGE_RPC_ADDR` two-ref cell. At `2035f9a`, `git grep -n
    STORAGE_RPC_ADDR 2035f9a -- '*.go'` = **3**, all comments, at exactly `main.go:504`,
    `internal/jobsimwiring/wiring.go:101`, `internal/storagens/callsites_test.go:189` ✓.
    Ref-less on the checkout = **15** hits; enumerated the live env reads (excluding comments and
    `t.Setenv`) = **7**: `cmd/academy-asset-upload/main.go:129`, `cmd/academyImport/main.go:231`,
    `cmd/import/main.go:50`, `jobsimwiring/wiring.go:115`, `main.go:446`, `:524`, `:992`. Both halves
    of the cell reproduce exactly, including the 3-vs-15 flip it warns about.
16. `README.md:20-22, 40, 42` / `cms.md:8, 73-77` / `storage.md:161` — the three prod-terraform states.
    `roadrunner/terraform/main.tf:19` = `service_desired_count = 1` (counted lines to confirm the
    anchor). `cms/terraform/main.tf:39` = `= 0` in a **191-line** file. jobsimulation: at the pinned
    `6092c6d2` the string does not occur at all and `:15-22` is the M810 decommission comment block
    (*"The `module "jobsimulation"` block … was deleted here"*); at the checkout `462343b0` it is still
    at `:40`, and `6092c6d2` is exactly **1** commit ahead on `origin/main` — the pin is doing real work
    and is honoured.

**RE-DERIVED** (arithmetic the document itself shows — the weakest clearance, listed as such):

- `ai-readiness.md:143-150` composite/axis math, against `aireadiness/scoring.go:85-105`
  (`knowledge = (step1+step2)/70×100`, `usage = step3×100/30`, `score = t1+t2+t3`) and the band
  constants `archetypeHighBand = 75` / `archetypeLowCeil = 50` (`:26`, `:30`) with `classifyArchetype`
  (`:46-57`, exact tie → Explorer) and `bucketKnowledge` (`:63-66`, None 0-24 / Low 25-50 /
  Medium 51-74 / High 75-100). All exact.
- `ai-readiness.md:548-568` — `round(1.0/25.0*30)=1`, `round(0.5/25.0*30)=1`, `round(9.0/25.0*30)=11`,
  `round(3.0/6.5*30)=14`, and `held/25×100 = held×4` integral for half-step weights. All check.
- `ai-readiness.md:348-350, 379, 442` — 156/199 = 78.39 % ≈ **78.4 %**, self-consistent across the file
  and with `seeding-spec.md:492`; the seeder constant is `aiReadinessCompletedShare = 0.80`
  (`rext .../ai_readiness_funnel.go:89`), which is the *share*, not the measured outcome.
- `coursebuilder.md:103` credits. **Read the surroundings (§5 rule 10) and it mattered**: the package
  doc-comment at `internal/credits/cost.go:29` says *"course.refine → **5** credits per refine turn"*,
  which contradicts the corpus's "1". The live map at `:86-90` is
  `{ActionCourseBuild: 5, ActionCourseRefine: 1, ActionCourseTranslate: 1}`, and `:59-67` records the
  D1 ruling making refine a flat 1. **The corpus is right and the platform's own header comment is
  stale.** Quoting `:29` would have produced a false blocker.

**Additional exact-anchor verifications that resolved (not enumerations, listed for the record):**
`cms/cmd/root.go:77,78` (8080/8081); compose @ `2adcf71` `:154-155` published, `:169` PORT=8090,
`:173` RPC_PORT=8091, `:210` STORAGE_S3_PUBLIC_BUCKET, `:324` inside studio-desk; `cms/go.mod:3`
`go 1.26.4`; `cms/Dockerfile:2` `golang:1.26-bookworm`, `:23` `python:3.11-slim`;
`app/main.go:1205-1211` the *"Additive + DORMANT … until the **M809** re-point"* comment @ `b948604`;
`app/internal/skillpath/session.go:205-207`; `app/terraform/migrations/20260724132049_cms_data_model.sql`
creating exactly the 6 named tables; `app/studio` @ `aeec036` `services/ai.py:705-708` three-provider
registry, `:1-2` imports, `requirements.txt` = **9** packages verbatim with no `python-docx`,
`tools/pdf2md.py:24/96/127`, `tools/r3.py:139/190/199-206`, `gen.py:484-492` nine args +
`gen.py:18-28` `parse_known_args`, `git grep -i mistral aeec036a` = **22 hits / 3 files**;
`app/.gitignore:79` `studio/*`; `app/internal/cms/studio/markdownManager.go:10,30` @ `2035f9a`,
`studioManager.go:119` gen.py / `:1045` postgen.py / `:531` / `:583`, `xlsx.go:13`;
`platform README.md:81-87` AWS creds; `docker-compose.yml:73-79,82,83,100` @ `0c91421`;
`app/env_guards.go:37-44` `deployedEnvironment()`; `app/main.go:518-523` / `:529-535` / `:524-525` /
`:547` / `:1102` / `:516-517` @ `2035f9a`; `app/internal/storage/service.go:22,24` env-var *names*;
`app/internal/storage/storage.go:193-200` `getKeyPath`; storage repo: `go 1.25.0`, 18-line `main.go`,
**0** `*_test.go`, `Dockerfile:18` `RUN go test -v ./...`, `storage.go:122` empty presigned URL,
5 RPC methods matching both tables exactly, `viant/afs v1.30.0`;
`storage/terraform/main.tf` @ `9f8cb53` = **18** lines with the quoted comment at `:13`/`:18`,
`custody` **0** occurrences (rc=1 with a working positive control), no `moved` block,
`storage.tf:22-25` M903 text verbatim, `d3e6d32` body containing *"M903 never ran."*, and the
checkout's contradicting header at `storage.tf:21-24`;
`cms 6efa1d5` (merged `f38c0c4`) deleting `.github/workflows/build-production.yml` (−18);
platform `d11a403` removing `jobsimulation`/`cms`/`roadrunner` from **both** compose and `repos.yml`,
`838d907` removing `storage`/`customerio-sync`/`messenger`; **0** `*_RPC_ADDR` in compose @ `0c91421`;
coursebuilder `bedrock.go:98-104` / `:105-114`, `main.go:766-779` (comment `:766-769`, warns `:774`
and `:778`), `:770` log, `:824-826` `OPENAI_KEY`, `terraform/variables.tf:635-638` +
`ssm.tf:328-334` + `main.tf:555`, `68c24512` deleting the `COURSEBUILDER_OPENAI_IMAGE_KEY` read
(surviving only in 2 in-repo markdowns), `rate.go:28` `= 50`, `budget.go` 500/0 defaults,
`run_task.go:49` `TypeCoursebuilderRun`, `worker.go:199` `CoursebuilderSrv`,
`cmd/coursebuilder-{liverun,e2e}` + `assertNotSharedDSN`, `internal/askengine/coursebuilder_tool.go`,
`ask/handler.go:82` `SetCourseBuildStarter`, all 5 named in-repo docs present;
ai-readiness: `4c28365f` (2026-07-22) and every file in the rename table present under
`internal/aireadiness/`, `workforce/members.go:349,353` `LoadMembers`/`LoadMembersByUserIDs`,
`enum/organization_settings.go:47`, `enum/ai_readiness.go` step types, `readiness.go:139/289/290-297/
307-312/314/330/388/390/638-652/662-664/684`, `steps.go:907-914/915-938`,
`how_we_measure.go:819` `computeInterviewInsightsV2` (a **method** — `func computeInterviewInsightsV2`
returns 0 and is a wrong-regex false absence, §5 rule 3), `:986-994` the `"risk"` filter,
`:1055` the single-table read, `:253-261`/`:285-287` cycle totals, `narrative.go:85` `GPT5Mini`,
`interactions.action_type` enum `{email, call}` (`migrate/schema.go:2289`),
`AIReadinessClient.tsx:78/133-134/153-154/166-170/599`, `HowWeMeasureTab.tsx` **1989** lines with
`:1879/1903/1915/1921/1927` and `grep -c interviewQuestions` = **0**,
`hooks/useAIReadiness.ts:274`, `components/ai-readiness/useAIReadiness.ts:48-62` `deriveMode`,
`AIReadinessHero.tsx:88`, `useWorkforceAIReadiness.ts:23-27` (no cycle param, no `/cycles`),
`WorkforceNewClient.tsx:125-151` (5 tabs, readiness absent), the `(new)` route group,
`dae0fb2f7` deleting the 3 components at −103/−220/−330 = **−653**, no
`enterprise/workforce/ai-readiness` route at HEAD, `e2e/specs/web.ai-readiness.spec.ts`;
rext: `app-aireadiness-snapshot-loadmembers.yaml:42` (`path: internal/aireadiness/readiness.go`) and
`:33` (*"v2.7 M254 RE-POINT"*), `demo-stack/tests/test_aireadiness_snapshot_loadmembers_m254.py`,
`cockpit.go:135` `LegacyReadinessPaths` / `:161` `ValidateCockpitManifest`,
`ai_readiness_funnel.go:205-216` `aiReadinessStageFor` (day-0 → 0 checked **first**, manager → 0,
struggling → 1, default → 3), `blueprint.go:200/347` `Persona.AIReadiness` +
`IsAIReadinessNotStarted`, `ai_readiness_funnel_test.go:230` the day-0 test,
`ai_readiness_interview_report_test.go`, `seat_append_test.go`, `contentref.go` reserved tail,
`ai_readiness_evidence.go`, `stack-verify/e2e/lib/coverage-manifest.ts`,
`stack-seeding/presets/stories.seed.yaml`; `playthroughs/manifest/ai-readiness.yaml` = **4**
playthroughs + `pt-onboarding-aireadiness-guided` in `onboarding.yaml:361` and the `pt-ai-onboard`
seat at `:348` — the "5 journeys" phrasing in `ai-readiness.md:630-633` reconciles (2 member +
2 manager + 1 day-0) and does **not** contradict `CLAUDE.md`'s "4 AI-readiness" product count.
`platform-migration-status.md:101` is precisely the `customerio-sync` row `README.md:17` cites.
`corpus/services/TEMPLATE.md` is a pure skeleton with no factual claims — nothing to grade; every
section it declares (Role / Architecture & Code Map / Interface Discovery / Local Development /
Testing) is the set `README.md:4-5` says it declares.

---

## What I could not settle

- `README.md:52` *"repo ARCHIVED"* (graphql-wundergraph), `README.md:79` *"The GitHub repo is NOT
  archived (last push 2026-04-23)"* (chronos), and `README.md:78`/`storage.md` archival statuses
  generally. These are GitHub API facts; nothing in a local clone records archived-ness. Settled by
  `gh repo view anthropos-work/<name> --json isArchived,pushedAt`.
- `storage.md:25` — *"The plan itself is `app/knowledge/plan/releases/09.00-support-in-app/m903-s3-custody/`
  … `overview.md` front-matter says `status: planned`, `progress.md` says 'Planned, not started — no
  terraform applied, no branch'."* **Not read**: the hard bar forbids `knowledge/plan/**` and I chose
  the conservative reading of the glob rather than the narrow one, since the bar is stated absolutely.
  Everything else in that cell verified, including `d3e6d32`'s body asserting *"M903 never ran."*, so
  the surrounding claim is independently supported. Settled by reading those two files under an
  explicit exemption.
- `cms.md:16-18` / `:78-84` — whether `infrastructure/terraform/production/services.tf` still declares
  `module.cms_euwest1`. The corpus states plainly that it cannot see this, and neither can I: the
  `infrastructure` repo is in no clone set. The corpus's *"report both, assert neither"* posture is the
  correct one and I am recording it as correctly unresolved, not as a defect.
- `coursebuilder.md:130` — *"142 changelog lines"*, pinned to `app v1.363.2 @ 5ba17044`. There is no
  `CHANGELOG.md` under `internal/coursebuilder/` at `b948604` (the 5 markdowns are README, SPEC,
  GO-LIVE-RUNBOOK, STEERING_PLAN, MULTI-CORRECT-FEASIBILITY) and no "changelog" heading in README, so
  I could not locate the artifact the count ranges over. The passage explicitly flags itself as a
  reading at a stale ref and declines to re-derive, so I am not booking it; settled by naming the file.
- `cms.md:106` (the demo/dev set-dressing block) and `ai-readiness.md:365-370` — claims about
  `directus.sequences.evaluation_skills` / `directus.simulations.skills` being NULL in capture. Directus
  is not in any clone set and no live stack was queried. Settled by a read against a set-dressed stack.
- `ai-readiness.md:487-494` and `:536` — the 180 s / 19 ms / 2.09 s / 24 ms latency figures. These are
  live measurements against a running demo; the doc itself says *"Re-measure before relying on either
  number; do not re-derive them from prose."* Not reproducible from source.
