# Seat G — reading #27

**Ground truth re-verified at this reading's open** (all shas matched the ADDENDUM's superseded table
exactly; no clone had moved):

| clone | HEAD | origin/main |
|---|---|---|
| `stack-demo/app` | `ad9f3c49` | `ad9f3c49` |
| `stack-demo/app/studio` (own checkout) | `aeec036a` | `aeec036a` |
| `stack-demo/platform` | `0c91421d` | `0c91421d` |
| `stack-demo/next-web-app` | `8297c684` | `f97ba659` |
| `stack-demo/sentinel` | `f2c46190` | `f2c46190` |
| `stack-demo/storage` | `4ce8ece5` | `9f8cb532` |
| `stack-demo/messenger` | `fa47850d` | `e9421c68` |
| `stack-demo/cms` | `ca50c817` | `f38c0c4a` |
| `stack-demo/graphql-wundergraph` | `60c229f3` | `60c229f3` |
| `stack-demo/roadrunner` | `87d8d443` | `87d8d443` |
| `stack-demo/jobsimulation` | `462343b0` | `82cb66ec` |
| `stack-demo/studio-desk` | `41ee3575` | `41ee3575` |
| `stack-demo/ant-academy` | `22df69dd` | `22df69dd` |
| `stack-demo/rosetta-extensions` (**pinned per-stack consumption clone — the adjudicating tree for "what the tooling does on a stack"**) | `09d06070` | `4cb920aa` |
| `.agentspace/rosetta-extensions` (authoring copy) | `680e8529` | `680e8529` |

**Which tree settled each rext claim:** every rext claim in my file set is a claim about what the tooling
DOES ON A STACK, so I graded against the **pinned per-stack clone `09d06070`** (rule 45). For
`persona_write.go` I read **both** trees and they are byte-identical in the cited region; every other rext
anchor was read only in the pinned clone and is named as such below.

**Instrument hygiene notes from this pass** (both caught by reading stderr, §5 rule 1):
- `grep -rn ... --include=*.ts` is **rejected outright** by this shell's `grep` (ugrep):
  `(eval):1: no matches found: --include=*.ts`. An unread stderr here would have produced a false absence.
- **zsh applies history modifiers to `$var:r`.** `git show $c:repos.yml` in a loop silently becomes
  `git show <sha>epos.yml` and returns **0 rows with the error on stderr**. Two of my enumerations
  returned an empty set from this before I read the error. Use `${c}:repos.yml`.

## Positive controls

| file | `wc -l` | read in full? |
|---|---|---|
| `corpus/services/hiring.md` | 483 | yes |
| `corpus/services/studio-desk.md` | 435 | yes |
| `corpus/architecture/shared_libraries.md` | 274 | yes |
| `corpus/services/next-web-app.md` | 164 | yes |
| `corpus/services/skillpath.md` | 117 | yes |
| `corpus/services/gotenberg.md` | 84 | yes |
| **total** | **1557** | |

Search positive controls run in the same passes as the absence claims below:
`git grep -c "mux.Handle" b948604f -- '*.go'` → non-empty (proves the ref-named `git grep` works before I
believed the `SkillPathSessionService` / `ROADRUNNER_RPC_ADDR` zeros); `grep -n "ADDRESS" docker-compose.yml`
→ 1 hit (before believing the `*_RPC_ADDR` zero); `git -C sentinel grep -lc "anthropos-work/colony"` → 2
files (before believing the `colony/authn` zero in sentinel); `git -C platform grep -c "JUDGE0" 0dab54d` →
3 hits (before believing the `ROADRUNNER_RPC_ADDR` zero in compose); `grep -rc 'DROP TABLE'` over the
migration set → 11 (before believing the `SET "score"` zero).

---

## Blockers

### B1 — `shared_libraries.md`'s `ai` surface description is contradicted by the only copy of that code any clone contains

- **Anchor:** `corpus/architecture/shared_libraries.md:128-130` — *"A thin wrapper exposing **one interface,
  `ai.AI`** (`ChatCompletion`, **`ChatCompletionStream`**, `Response`, `CreateEmbeddings`, `CreateSpeech`,
  `OCRProcess`, `AudioTranscriptions`, `Tokenize`, `GetEndpoint`)"*; `:137` — *"| `mistral.NewMistral` |
  Mistral — **OCR only** (chat/embeddings/speech `panic`) |"*; `:163-164` — *"**Anthropic
  `ChatCompletionStream`/`CreateSpeech` `panic`**"*.
- **Why it is false:** the standalone `github.com/anthropos-work/ai` module is in **no clone set**, so none
  of these three statements is checkable against its own source. The document itself supplies the substitute
  ground truth at `:126`: *"`app/internal/ai/` carries the library in-tree"*. Measured at `app` `ad9f3c49`
  (the checkout, which is also `origin/main`), the in-tree copy contradicts all three:
  - `app/internal/ai/ai.go` is **21 lines** and declares `type AI interface` with **eight** methods —
    `ChatCompletion`, `Response`, `CreateEmbeddings`, `CreateSpeech`, `OCRProcess`, `AudioTranscriptions`,
    `Tokenize`, `GetEndpoint` (`:8-17`). **`ChatCompletionStream` is not a member.** `app` commit
    `9048ce1b4` is titled *"refactor(ai): delete the streaming path and dead exports"*, and
    `app/internal/ai/anthropic/characterization_test.go:313` records the removal in words:
    *"ChatCompletionStream left with the whole streaming path"*.
  - There is **no `internal/ai/mistral` package**. `ls internal/ai/` returns `anthropic` and `openai` only.
    The fold `1e457fa70` did bring `github.com/anthropos-work/ai/mistral -> app/internal/ai/mistral` in, and
    `2b3a65cf0` (*"refactor(cms): re-home Mistral OCR + fix the API-key bug pair"*) moved it out to
    `internal/cms/studio/mistralocr`, whose own test states the new shape:
    *"those methods do not exist, the interface is not implemented, and this package contains no panic at
    all. The surface is New + Process."* (`internal/cms/studio/mistralocr/mistralocr_test.go:266-272`).
  - The Anthropic panics are gone: `internal/ai/anthropic/characterization_test.go:310-316` —
    *"Five methods on the ai.AI interface have no Anthropic implementation, and ALL FIVE now return an
    error… This package now contains **no `panic("not implemented")` at all**."* `grep -rn "panic(" internal/ai/`
    returns exactly one live site, `internal/ai/prompt.go:37`, unrelated.
- **Why I hesitated (stated, per the briefing):** the section header is `## ai` and its **Module** row names
  `github.com/anthropos-work/ai`, whose `v1.40.2` is still `require`d by the frozen `cms` (`go.mod:9`) and
  `jobsimulation` (`go.mod:11`). Those three statements were plausibly true of `v1.40.2` and I cannot read
  that module to prove otherwise. What decides it for me is that the **same section** asserts the in-tree
  copy *is* the library, that the corrections blockquote directly below is explicitly *"Measured @ `app`
  `ad9f3c49`"*, and that the rest of the section's body **does** still verify against the in-tree copy
  (`internal/ai/retry.go`: `DefaultAttempts = 10` `:25`, `retry.BackOffDelay` `:32`, permanent-set
  `401/403/404` `:92`/`:121`, `context.Canceled`/`DeadlineExceeded` `:65` — every clause of `:165-166`
  holds). So a reader is invited to treat the body as current, and exactly the three items that post-fold
  `app` commits changed are the ones that are wrong. A reader adding streaming to a jobsim AI call, or
  looking for Mistral OCR under `internal/ai/mistral`, is sent to code that does not exist.
- **Confidence:** medium.

---

## Minors

- `corpus/services/studio-desk.md:107` — *"`GCLOUD_SERVICE_ACCOUNT` is declared in `.env.example:120`"*. It
  is declared at **`.env.example:119`**; `:120` is a blank line. (The commented header is `:118`,
  `terraform/main.tf:129` in the same sentence is exact, and *"no code in `src/` reads it"* is confirmed —
  `grep -rn GCLOUD_SERVICE_ACCOUNT` over the whole clone hits only `CHANGELOG.md`, `terraform/main.tf:129`
  and `.env.example:119`.)
- `corpus/architecture/shared_libraries.md:57` — *"`sentinel`'s `88036d7` … took it `v0.34.3 → v0.35.2`,
  **two commits past** the `88bc5592` this row used to cite."* Measured:
  `git rev-list --count 88bc5592..88036d7` = **1**. It is `f2c46190` (the row's own current ref) that is two
  commits past `88bc5592`. The dependency-bump content of `88036d7` is exact (`-colony v0.34.3 / +colony
  v0.35.2`, `-proto v1.200.0 / +proto v1.210.0`).
- `corpus/services/hiring.md:243` — the three `validation_*` tables are cited to
  `persona_write.go:69-71,143-167` (identical in both rext trees; read at pinned `09d06070`). `:69` is a
  bare `//`, `:70-71` is the note about the **removed** `jobsimulation.sessions` step — the three tables are
  named at `:66-68`; and `:143-167` covers `sessionCols()` plus only **one** of the three column builders
  (`attemptResultCols`, `:161-167`), while the actual writes are `:92-94`. The claim itself is true and I
  hesitated over rule-21 class 2 (*resolves but names the wrong construct*); I kept it a MINOR because both
  ranges land inside the function/column-list region of the right file and nothing downstream is misled.
- `corpus/services/hiring.md:289-292` — *"The `completition` misspelling exists **only** in the GraphQL
  sort-field enum (`enum.InsightsSortFieldCompletitionStatus`), its GraphQL member and a JSON tag."* At `app`
  `ad9f3c49` it occurs in **11 files**, including `internal/assignments/assignments.go:115,167,256,267,273,276`,
  `internal/assignments/assignments_test.go:183,186,197`, `internal/data/ent/repository/assignments.go:449`
  (an error string), the resolver's parameter names, and two historical migrations
  (`20240527131926.sql:10`, `20240809101650.sql:4-5` — both against the now-dropped mirror). I hesitated
  because the operative predicate the sentence exists to support — *"so it **never reaches SQL**"* — is
  **true**: every surviving occurrence is a Go identifier, comment or error string, and
  `insightsSortColumn` (`intelligence.go:885-886`) maps the enum back to `FieldCompletionStatus`. The word
  *only* is what is false.
- `corpus/services/hiring.md:300` and `:301-302` — both intra-corpus anchors are off by one at the start.
  The **History** blockquote it cites as `:193-198` runs `:194-207` (`:193` is blank); the `job_position`
  bullet it cites as `:173-175` runs `:174-178` (`:173` is the tail of the preceding bullet). The stated
  derivation (*"shifted them by +23 and +16"*) reproduces arithmetically from the old `:157-159`, so this is
  §5 rule 34 line drift after a later edit, not a bad computation.
- `corpus/services/hiring.md:459` — *"a plain Ant `<Drawer>` rendered by `InsightsByMembersContainer`
  (`InsightsByMembersContainer.tsx:359`)"*. That basename exists in **two** apps at `next-web-app`
  `8297c684`; `:359` is `<Drawer` only in `apps/hiring`, while `apps/web`'s `:359` is an `<Empty
  description=…>`. The surrounding paragraph is about the recruiter (hence `apps/hiring`), so the intent is
  recoverable, but the citation does not name its app the way the same document's other frontend citations do.

---

## Positively cleared (audited zeros)

### ENUMERATED — predicate re-derived from source, cardinality compared

1. **`shared_libraries.md:22-25` — the 5×7 shared-library matrix.** I enumerated
   `anthropos-work/(colony|proto|ai|authn|taxonomy)` across all seven on-disk Go repos' `go.mod` at the
   pinned refs, then compared: colony **7/7**, proto **7/7**, taxonomy **6/7** (roadrunner absent), ai
   **2/7** (cms + jobsimulation only), authn **0/7**. Matches the document exactly, including the derived
   *"four of five pulled by at least one repo, three by the two a stack builds"*.
2. **`shared_libraries.md:213` — *"that set is 11"* (every `type: go` repo ever cloned).** Enumerated the
   union of `type: go` entries across **all 10** commits that ever touched `platform/repos.yml`:
   `app, chronos, cms, intelligence, jobsimulation, messenger, roadrunner, sentinel, skiller, skillpath,
   storage` = **11**. The all-names union is **14**. The four named extras (`skiller`, `skillpath`,
   `chronos`, `intelligence`) are all present in the first revision `a2a3ee6`, and `skillpath` is the last
   of the four removed (`fdfa189` intelligence → `045857c` chronos → `21429b7` skiller → `a4db680`
   skillpath) — the doc's ordering claim holds.
3. **`shared_libraries.md:86` — *"`app/main.go` registers six Connect handlers @ `b948604` v1.366.0"*.**
   `git grep -n "mux.Handle" b948604f:main.go` returns exactly six: `:1187` Users, `:1188` Organizations,
   `:1196` Skiller, `:1204` JobSimulation, `:1213` CMSService (inside `if cmsRPCServer != nil` at
   `:1212-1214`), `:1228` LabSession. Five unconditional + one conditional, and neither
   `SkillPathSessionService` nor a RoadRunner service is among them.
4. **`shared_libraries.md:93-104` — *"At least 13 Connect-RPC services … the named ones"*.** The list names
   exactly 13 distinct services (Users, Organizations, CMS, JobSimulation, Skiller, SkillPathSession,
   LabSession, Authorization, Messenger, RoadRunner, Realtime, Chronos, Storage), consistent with its own
   floor. The `StorageService` cross-reference resolves: `storage.md:129` is the heading
   `### Connect-RPC (\`StorageService\`)`.
5. **`shared_libraries.md:177` — *"6 and 8 `.go` files"* importing `colony/authn`.**
   `git -C cms grep -l "anthropos-work/colony/authn" ca50c817 -- '*.go'` = **6**;
   `git -C jobsimulation … 462343b0` = **8**. Exact. And *"Sentinel does not import authn"* verified with a
   positive control (0 matches for `colony/authn`, 2 files matching `anthropos-work/colony`).
6. **`shared_libraries.md:234-241` — the taxonomy figures.** Re-derived from the capture manifest itself
   (`.agentspace/snapshots/taxonomy/5afc0bcc…/manifest.json`): `skills` **42,790**, `job_roles` **22,470**,
   `job_role_embeddings` **18,919**, `source: primary-read`, `public_only: true`, `predicate: org-null`,
   `captured_at: 2026-06-29`. Every filter is `organization_id IS NULL`. The document's verdicts (18K
   **REFUTED**, 60K **UNVERIFIED**) follow from those numbers.
7. **`hiring.md:171` — *"87 real `SIMULATION_TYPE_HIRING` sims"*.** Parsed the captured COPY payload
   `directus.simulations.copy` (307 rows, filter `private = false AND tenant_id IS NULL AND status =
   'published'`) and counted the `type` column: TRAINING 121 / ASSESSMENT 98 / **HIRING 87** / INTERVIEW 1.
   Exact. `hiring.md:175-176`'s *"0 `job_position` rows"* also holds — no `job_position` table appears in
   the manifest's 14 captured tables or in the payload directory.
8. **`hiring.md:264-266` — *"one of **four** required-and-undefaulted columns"*.** Enumerated every
   `NOT NULL` column of `20260722104506.sql:2-27`: `id`, `created_at`, `updated_at`, `status`,
   `completion_status`, `interactions_progress`, `score`, `validation_version` all carry `DEFAULT`;
   `owner_id` `:6`, `sim_id` `:7`, `sim_type` `:10`, `token` `:13` do not. **Exactly four.** `token`'s
   `UNIQUE` is `:29`. The table has **23** columns (`:3-25`), as `:180` claims.
9. **`hiring.md:259-261` — the closed 5-value `completion_status` enum.** `enum/jobsimulation.go:29-35`
   declares exactly `pending / passed / failed / discarded / timedout`; `Values()` at `:37-43` returns the
   same five. No `SIMULATION…` member. The DDL column is a bare `character varying` with no CHECK, and
   `ent/jobsimulationsession.go:181-186` casts unconditionally — the doc's "renders wrong, does not error"
   mechanism is exactly what the code does.
10. **`hiring.md:454` — *"the regression enumerates **all 8** generic seeders"*.**
    `TestGenericActivitySeeders_SkipHiringOrg` (pinned clone `09d06070`,
    `stack-seeding/seeders/hiring_scope_test.go:75-96`) has 8 table rows: jobsim-sessions, activity,
    skillpath-sessions, assignments, hero-activity, persona, feedback, succession — matching
    `hiring_scope.go:24-26`'s consult list exactly.
11. **`gotenberg.md:47` — *"the nine MIME types `docconv` can't read directly"* + *"two call sites"*.**
    `extract.go:17-27` @ `9d00a313` is a 9-entry map (`.xls`, `.xlsx`, `.ppt`, `.doc`, `.odt`, `.ods`,
    `.odp`, `application/rtf`, `text/rtf`) — DOCX genuinely absent.
    `git grep -n "ConvertToPDF" 9d00a313 -- '*.go'` returns the declaration plus **exactly two** call sites,
    `extract.go:77` and `user_import_resume_2d.go:68`; identical at `ad9f3c49`.
12. **`studio-desk.md:313-315` — *"hardcoded the production app host … in **three** places"*.**
    `grep -rn "app.anthropos.work" app/core/scaffold/` returns exactly three:
    `pageWrapper.js:149` (logo link), `userProfile.js:148` (the user-menu "Back" control),
    `userProfile.js:302` (logout redirect). Exactly as attributed.
13. **`studio-desk.md:34` — the prod HTML entry set.** `vite.config.ts` `rollupOptions.input` (`:28-42`)
    declares exactly the nine named entries and gates `dev-accept` behind `isProduction ? {} : {…}`.
14. **`studio-desk.md:120` (7 languages) vs `next-web-app.md:70,155` (8 locales).** `studio-desk`
    `app/public/l12n/` holds **7** (`de en es fr it ja nl`); `next-web-app` `configs/i18n/messages/` holds
    **8** (adds `pt`). Both counts correct and the two documents do not contradict each other.
15. **`next-web-app.md:22` — the version tuple.** All four of `apps/{web,hiring,integration,maintenance}`
    declare `"next": "~16.2.12"` (tilde), `apps/mobile` declares no `next`, and `pnpm-lock.yaml` resolves
    `next@16.2.12`. React `^19.2.7`, `engines.node ">=24.0.0"`, `packageManager pnpm@10.30.3`. All exact.
16. **`next-web-app.md:75,97-99` — the two absence claims.** `git ls-files | grep -i middleware.ts` = **0**
    (the three `src/proxy.ts` files exist); no `storybook` script in any `package.json` and no `.storybook/`
    dir — the sole trace is `configs/tailwind/storybooks.css`, exactly as stated. `Dockerfile.dev` is the
    only Dockerfile.
17. **`skillpath.md:30-40` + `shared_libraries.md:99` — the RPC-removal zeros.**
    `git grep -c "SkillPathSessionService" ad9f3c49 -- '*.go'` = **0** (and 0 at `b948604f`);
    `git grep -c "ROADRUNNER_RPC_ADDR"` = **0** at both `9d00a313` and `ad9f3c49`, and 0 in
    `platform` at `0dab54d`. `app/CLAUDE.md:109` and `app/knowledge/architecture.md:28` **do** still list
    `SkillPathSessionService` at both `ad9f3c49` and `2035f9a`, exactly as the parenthetical claims.
    `docker-compose.yml:118` @ `0dab54d` is indeed `AWS_REGION=eu-west-1`.
18. **`skillpath.md:88-91` — *"three [migrations] post-date it"*.** `ls terraform/migrations/` after
    `20260729133514.sql` yields exactly `20260731131307.sql`,
    `20260731154527_academy_chapter_progress_completed_at.sql`,
    `20260803143844_ai_readiness_recommendation_path.sql`. Exact, and no
    `local_skill_path_session.go` Ent schema exists.
19. **`skillpath.md:49` / `hiring.md:44,204,378` — the `repos.yml` entry counts.** `9` @ `2adcf71`,
    `6` @ `0dab54d`, `4` @ `838d907` and at HEAD `0c91421`. `app` at `repos.yml:14-17` is the only
    `migrations: true` entry. `platform` compose sets **zero** `*_RPC_ADDR` (positive control:
    `AUTHORIZATION_ADDRESS` at `:48`).
20. **`hiring.md:55-56` / `:379` — the schema claims.** `app/atlas.hcl:8` pins `search_path=public`; the
    **only** `CREATE SCHEMA` in the entire migration set is `20230817154747_supabase_baseline.sql:2`
    (`auth`).

### RE-DERIVED — anchor/quote checks (weakest form of clearance; listed for coverage, not as evidence)

Every remaining `file:line` in the six files was opened and its surroundings read (`sed`/`awk` windows,
never a bare grep hit). All of the following resolved to the construct named:

- **hiring.md:** the whole re-grounding banner including its **non-uniform** `5ba17044` offsets
  (`manager.go` `:450→:448`, `:453→:451`, `:537→:487` — I read all three at `5ba17044` and confirmed
  `:535` there is unrelated `BulkInviteMemberMessage` code, which is the point the banner makes);
  `20260729133514.sql:58-62`/`:63`; `SET "score"` = 0 hits set-wide; `20260722104506.sql:2,6,7,10,12,13,29,53,79`;
  `20260722081626_jobsim_data_model.sql:336/355/376`; `20240527131926.sql:7`; `20250416091037.sql:5`;
  `resolver_queries.go:1034,1035,1053,1080,1085,1089`; `resolver_cms_queries.go:95,99-103,210,258,295`;
  `siminvitationlink.go:62,63`; `intelligence.go:885-886,1700,1702,1709,1733,1738-1764,1739-1744,1820,1844,1846,2124,2158-2160`;
  `ent/schema/job_simulation_session.go:33,39,45`; `ent/schema/anticheat_result.go:24`;
  `ent/jobsimulationsession.go:181-186`; `graph.go:129392-129400` and `:129546-129554` (both bare
  `graphql.MarshalString` passthroughs); `jobsimulations.graphqls:14,128`; `askengine/registry.go:192`;
  `apps/web/src/context/UserStatusContext.tsx:141-173,144-149,168-172`;
  `apps/hiring/src/context/UserStatusContext.tsx:125,144-145`; `useGetClerkOrganization.tsx:16-18,20-21`
  (quoted verbatim, byte-exact); `useNavbarSections.tsx:161,249-256,329-331,340-343,391-398,475-482,476,568`
  and the `bb3313bc` predecessors `:459-466`/`:460`; `orgGroups.ts:48-65,61` (confirmed it takes **no**
  `isHiringOrg` parameter — the retraction at `:341-351` is correct); the two and only two
  `showWorkforce={false}` sites, both in `apps/hiring/.../template.tsx:167,248`; `template.tsx:90`;
  `FreeTrialContainer.tsx:29`; `simulationScoreColumn.tsx` (the two copies `diff` **clean** — the
  "byte-identical, same line numbers" claim is literally true); `insights.ts:31-82`;
  zero `(.)`/`(..)`-prefixed dirs anywhere under `apps/` (positive control: five `@tabs` parallel-route
  dirs found); rext `persona_write.go:91,152,152-158`, `hiring_config.go:99`,
  `blueprint/blueprint.go` `RoleMix{Admin,Member,Candidate,AdminEmails}` `:452-457`,
  `hiring_scope.go` `skipGenericActivityForHiringOrg`/`IsHiringOrg`,
  `contentref.go:158` `hiringComparableFloor = 6` **and** `render-hiring-comparison.spec.ts:56`
  `RENDER_GATE_FLOOR ?? '6'` (the two surfaces agree, which is what the drift fence asserts);
  the `stories.seed.yaml:249` Meridian Talent story (size 50, `admin: 0.14` → 7 slots − 2 hero overrides
  = the documented **5 admins + 45 candidates**, and the `p3`/`g2` insights inheritance is stated in the
  preset's own comments at `:206-208,252-253`); and the corpus cross-refs `service_taxonomy.md:62`,
  `service_taxonomy.md:44-52`, `dependency_map.md:78`.
- **studio-desk.md:** `docker-compose.yml:138-140,141` @ `0c91421` and `:223-225`/`:226` @ `0dab54d`
  (and the three deleted blocks — storage, customerio-sync, messenger — really were all *above* studio-desk,
  which is why the shift is exactly −85 lines on a file that went 271 → 186); `:337-341` @ `2adcf71`
  carrying the extra `cms` dependency; `:119`/`:135` and `:204` @ `0dab54d`; `youtube.ts:43` (+ the
  `_mock: true` fallback at `:46`, and `YOUTUBE_API_KEY` is the **only** credential the route reads);
  `terraform/main.tf:129`; `src/index.ts:60` (`PORT || 9100`), `:96` (`STUDIO_ACCESS_ROLES`, quoted
  verbatim), `:99` (`checkEnterpriseAndAdmin`), `:119` (`WEB_APP_URL`), `:305-313`
  (`pingSnapshotCapability` / `directus_versions`); `config.ts:21-38,182` (tier model maps + the
  `AI_DEFAULT_TIER || 'thinking_fast'` fallback) and `.env.example:56,60`; `providerHealth.ts:9,62,66-74`
  (the timeout→rotate-to-end circuit breaker) and `aiService.ts:108-175` (the loop really does try **every**
  configured provider inside one request, which is what `:352-355`'s "no provider-chain pin is needed"
  rests on); `app/core/main.ts:97,105,206` and the three-await ordering at `:181,191,199`;
  `pageWrapper.js:122` (`document.body.innerHTML = ''` — the de-dup claim);
  `skillpath.ts` = 61,080 bytes, the largest route; the no-datastore claim (zero DB drivers in
  `package.json`, zero `DATABASE_URL` in `src/`, `studioDB.js` present as a facade,
  `GET_/SET_USER_STUDIO_PREFERENCES` in `packages`-equivalent `app/services/graphql/`); the single
  Tailscale mention (`git grep -in tailscale HEAD` = 1 hit, `app/core/main.ts:105`); every npm script the
  doc spells (`test`, `test:e2e`, `test:e2e:headed`, `type-check`, `lint`, `check`, `build`, `start`,
  `dev`, `codegen`) exists; `jest.config.js:26-38` declares the two projects; and in the **pinned** rext
  clone: all five `studio-desk-*` patch dirs, `build_frontend_studio_desk`, `up-injected.sh:894-903`,
  `stack-injection/gen_injected_override.py` `frontend_lines()` `:379,398-400,433-444` (the
  existence-guarded `env_file`), both named regression tests in `stack-injection/tests/test_injection.py`,
  `stack-verify/e2e/run-studio-fcp.sh`, `autoverify.sh:528-540` (the container-side provider-key assert)
  and `clerk-frontend/server.go:186` (`GET /v1/me/organization_memberships` now registered, matching the
  doc's "fixed at the source").
- **shared_libraries.md:** `app/go.mod:15,16` and `:20` @ `b948604f`; `sentinel/go.mod:8,9` and `:21`
  @ `88bc5592`; every frozen-repo pin quoted beside its own ref (`storage v0.34.3/v1.196.0` @ `4ce8ece5`,
  `messenger v0.35.2/v1.210.0` @ `fa47850d`, `cms v0.35.1/v1.207.0/ai v1.40.2` @ `ca50c817`,
  `jobsimulation v0.35.1/v1.205.0/ai v1.40.2` @ `462343b0`, `roadrunner v0.34.3/v1.196.0` @ `87d8d443`);
  `roadrunner/main.go:7` + `:17` `colony.NewVersionConfig`; `1e457fa70`'s title and its
  `go.sum` consequence (**0** `anthropos-work/ai ` lines, 10 `anthropos-work/` lines total as control);
  `internal/ai/module_import_guard_test.go:16-17` quoted verbatim;
  `internal/jobsimulation/ai/ai.go:129,166,259,267,325,344` and
  `internal/skillerai/ai.go:128,176,332,347`; `internal/askengine/bedrock.go` exists;
  `internal/authorization/gqlauthz/gqlauthz.go` exists.
- **next-web-app.md:** `apps/web/src/context/UserStatusContext.tsx:144-148,168-172`;
  `apps/hiring/.../InsightsByMembersContainer.tsx:108`; `…/[simId]/page.tsx:14`;
  `docker-compose.yml:143,151,160,165-167,110` and `:236` @ `0dab54d`; `Makefile:119-120` and
  `Makefile:10` (`PROFILE ?= core`); `Dockerfile.dev:1` (`node:24-alpine`) and `:34`
  (`pnpm turbo build --filter=@anthropos/web-app`); `apps/web/src/proxy.ts:7-56` (all seven named public
  routes present, `isPrintRoute` at `:59`); `apps/web/next.config.mjs:197-207` (`/logpoint/*` → PostHog EU);
  `apps/web/src/providers/Query.provider.tsx:37-42` (`defaultOptions.queries.meta.getToken`); the repo's own
  `CLAUDE.md:15` and `:55`; `pnpm test` reaching jest in `apps/web` + `apps/hiring` only; and in the
  **pinned** rext clone `demo-stack/frontend/hiring.Dockerfile` + `up-injected.sh:1076-1085` (image
  `demo-$N-hiring`, port 3001).
- **skillpath.md:** `internal/skillpath/session.go:205-207`; `internal/skillpaths/skillpaths.go:88-95`;
  `ent/schema/skill_path_session.go` + `skillpath_mixins.go`;
  `graph/schemas/skillpath_sessions.graphqls`; `intelligence.go:1144,1159-1170`;
  `20260729133514.sql:62,63`; `SKILLPATH_STREAM` surviving (`main.go:746`, `terraform/main.tf:423`);
  the drill-down container `InsightsBySkillPathStudentSimulationsContainer.tsx:31-34` (`userData` really is
  `return null as unknown as MembershipEnriched`), `:140-160` (table + totals commented out), `:138`
  (`t('enterprise.insights.comingSoon')` → *"Coming soon"* in `en/misc.json:16`), and `apps/hiring` really
  has **no** `activity-dashboard/@tabs/skill-paths` route.
- **gotenberg.md:** `docker-compose.yml:183` and `:57` (`GOTENBERG_URL=http://gotenberg:3200`), the
  186-line file, `:268` @ `0dab54d` on a 271-line file; `extract.go:17-27,43-45,59,77,77-81`;
  `user_import_resume_2d.go:68,68-74,69-74`; `converter/gotenberg.go:13` (90 s), `:16` (signature),
  `:31` (`/forms/libreoffice/convert`).

### Cross-file consistency checks I ran and that came back clean

- `studio-desk.md:132` / `:46` (*"the Cosmo Router survives in production only"* / *"the router is
  prod-only"*) vs `external_services.md:368` (*"**prod-only** since platform `2adcf71`"*) and
  `graphql-wundergraph.md:3-9` (prod `service_desired_count = 1`, local deleted) — **consistent**, no
  self-contradiction.
- `hiring.md`'s *"the scoreboard is NOT reachable in `apps/web` for a genuine hiring org"* vs
  `next-web-app.md:37-61`'s retraction of the same sentence — **consistent and mutually citing**; both
  resolve against the same measured `UserStatusContext` guard. This is a retraction that agrees with its
  twin, which the briefing tells me not to book.
- `hiring.md` / `skillpath.md` / `shared_libraries.md` all state the `repos.yml` trajectory as 9 → 6 → 4
  and the *"no `*_RPC_ADDR` anywhere"* state — **uniform**, and both are what the platform measures.
- `skillpath.md:42` (*"3 subgraphs at the time … 1 now"*) vs `graphql-wundergraph.md:21-24` (*"a **3 → 1**
  step, not 2 → 1"*) — **consistent**.

---

## What I could not settle

1. **Whether the `ai` library at `v1.40.2` still has `ChatCompletionStream`, a `mistral` constructor and
   panicking Anthropic methods** (B1). `github.com/anthropos-work/ai` is a private Go module in **no clone
   set** — not cloned by `make init`, not in `repos.yml`, and pulled only at Docker build. The evidence that
   would settle it is one read of that repo at tag `v1.40.2`. Circumstantially, `1e457fa70`'s own commit
   body lists `github.com/anthropos-work/ai/mistral -> app/internal/ai/mistral` and claims *"diffing
   internal/ai against v1.40.2 shows only import lines"*, which implies the library **did** carry mistral
   (and, by `9048ce1b4`'s title, streaming) at the moment of the fold — i.e. the corpus text was true of the
   library and has been overtaken by in-`app` refactors. That is exactly why I graded B1 at *medium*.
2. **`shared_libraries.md`'s colony/proto/taxonomy internals** — the package table at `:62-70`, the
   `rate.NewLimiter(1,5)` "only logs" note at `:73-75`, colony's `go 1.25.0` / `golang:1.26-bookworm`, buf
   `v1.57.0`, the `taxonomy` repo being a *"131-line node-id library"*, and the `NodeID` format/examples at
   `:256-261`. All three repos are private modules absent from every clone. Settling them needs a clone of
   `colony`, `proto` and `taxonomy` at their pinned versions.
3. **`studio-desk.md:109`'s `bootstrap/index.js:81` in `directus/directus:11.6.1`** — inside a third-party
   container image, not on disk. Settling it needs `docker run --rm --entrypoint sh directus/directus:11.6.1`.
4. **Every measured latency / render number I encountered** — `hiring.md`'s *"~8 candidates per position,
   scores 27–100, 5/5, p95 1.27 s"*, `studio-desk.md`'s *"4669 → 817 ms"*, *"4049 → 38 ms"*, *"6936 → 2152
   ms"*, *"~140 ms / ~12 ms / ~3.9 s"*. These are live-stack measurements; nothing static can confirm or
   refute them. I did confirm the **harnesses** that produce them exist at the pinned rext clone
   (`run-studio-fcp.sh`, `render-hiring-comparison.spec.ts`, `RENDER_ONLY_SIM`).
5. **`hiring.md:460-463`'s *"detectable `.ant-drawer` only for the FIRST sim clicked per page-load"*** — a
   runtime/DOM behaviour of a running demo. The static evidence (a plain Ant `<Drawer>` on an ordinary leaf
   route, zero intercepting routes in `apps/`) is consistent with it but does not establish the
   first-click-only detectability.
6. **`shared_libraries.md:213`'s taxonomy anchors are pinned at superseded refs** (`app/go.mod:20` @
   `b948604f`, `sentinel/go.mod:21` @ `88bc5592`) while the sibling colony/proto rows use the current
   checkouts. Both pins **resolve exactly** at the refs they name, so under §5 rule 33 (*a pin is a date*)
   this is legal and I did not book it. Recording it because the corresponding current anchor has **moved**:
   at `ad9f3c49`, taxonomy is `app/go.mod:18`, not `:20` — the row will read as drifted to anyone who
   ignores the pin.
