# Seat E — reading #27

**Corpus HEAD:** `04cbcfc` (branch `m257x/platform-realignment`).
**Ground truth re-derived at open** and matching the ADDENDUM exactly:
`platform 0c91421d` (= origin/main) · `app ad9f3c49` (= origin/main) · `next-web-app 8297c684` (origin/main `f97ba659`) ·
`sentinel f2c46190` · `studio-desk 41ee3575` · `ant-academy 22df69dd` · `storage 4ce8ece5` (origin/main `9f8cb532`) ·
`messenger fa47850d` (origin/main `e9421c68`) · `cms ca50c817` (origin/main `f38c0c4a`) · `graphql-wundergraph 60c229f3` ·
`roadrunner 87d8d443` · `jobsimulation 462343b0` (origin/main `82cb66ec`) ·
nested `stack-demo/app/studio` and `stack-demo/cms/studio` both at **`aeec036a`** (own checkouts) ·
`stack-demo/rosetta-extensions 09d06070` (**pinned per-stack clone — the settling tree per §5 rule 45**) ·
`.agentspace/rosetta-extensions 680e8529` (authoring copy).

Every `git grep` in this report names the tree and the ref it ran at, and every claimed **zero** was run with a
positive control in the same pass (recorded inline below). The nested `studio` repos were grepped **at their own
ref**, never through the host (§5 rule 44).

## Positive controls

| file | `wc -l` | read in full? |
|---|---|---|
| `corpus/services/studio-room.md` | 509 | yes |
| `corpus/services/backend.md` | 379 | yes |
| `corpus/services/graphql-wundergraph.md` | 280 | yes |
| `corpus/services/roadrunner.md` | 206 | yes |
| `corpus/architecture/dependency_map.md` | 103 | yes |
| `corpus/services/skiller.md` | 76 | yes |
| **total** | **1553** | — |

(`graphql-wundergraph.md` and `roadrunner.md` have no trailing newline on their last line, so the editor shows
281 / 207 numbered lines against a `wc -l` of 280 / 206. Anchors below use the editor's numbering, which is what
the corpus's own `file:N` citations use.)

---

## Blockers

### B1 — `graphql-wundergraph.md` calls the repo's own `CLAUDE.md` "Version Tracking" line stale; it is current and says exactly what the corpus says is true

- **Anchor:** `corpus/services/graphql-wundergraph.md:274` —
  > "The repo's own `CLAUDE.md` "Version Tracking" line and `-local.yaml` references are **stale**; `subgraphs.conf`
  > is the real version source of truth and the config variants are `compose`/`dev`/`prod`."
- **Why it is false:** the sentence has two subjects and predicates staleness of both. The second is true; the first
  is not. At `graphql-wundergraph 60c229f3`, `CLAUDE.md:70-79` reads:
  *"## Version Tracking / Service versions are tracked in `subgraphs.conf`. There is exactly **one** pin now: /
  ```BACKEND=v1.360.0``` / The `CMS`, `JOBSIMULATION`, `SKILLER` and `SKILLPATH` entries were removed as each of
  those services merged into `app`."*
  `subgraphs.conf` at the same ref is the single line `BACKEND=v1.360.0` — a byte match. So the repo doc names
  `subgraphs.conf` as the source of truth, carries the correct single pin, and states the correct removal history:
  it is the *current* form of the very claim the corpus offers as the correction. Only `CLAUDE.md:85`
  (`wgc router compose -i supergraph-config-local.yaml`) is stale — `ls supergraph-config-*.yaml` returns
  `-compose`, `-dev`, `-prod` and no `-local`, exactly as the corpus's second half says.
  A reader is told to distrust an accurate section of the ground-truth repo, and told so in the same breath as a
  true statement, which is what makes it survive a skim.
- **Confidence:** high

### B2 — `roadrunner.md` dates the prod `service_desired_count = 1` to `87d8d44` (2026-06-19); that commit never touched the file, and the fenced map says `84a4b4f` (2025-12-15)

- **Anchor:** `corpus/services/roadrunner.md:13-14` —
  > "`roadrunner/terraform/main.tf:19` still reads `service_desired_count = 1` and has **not been touched since
  > `87d8d44` (2026-06-19, before the fold)**"
- **Why it is false:** measured at `roadrunner 87d8d443`:
  - `terraform/main.tf:19` is `  service_desired_count          = 1` — that half is correct.
  - `git blame -L 19,19 terraform/main.tf` → **`84a4b4f1 (Mattia Sasso 2025-12-15)`**.
  - `git show --stat 87d8d443` touches exactly one file: `.github/workflows/bump-version.yml | 3 +++`. It is the
    repo's **HEAD**, not a commit that went near terraform. "Not touched since HEAD" is vacuous by construction,
    and the parenthetical `(2026-06-19, before the fold)` presents it as the date of the last touch.
  - `git log --oneline -- terraform/main.tf` → the file's most recent touch is `e45eb61` (2026-05-27); before that
    `ae622e6` and `84a4b4f` (both 2025-12-15). So `87d8d44` is neither the line's provenance nor the file's.
- **It also contradicts the corpus's own fenced authority**, which roadrunner.md itself points at two lines below:
  `corpus/architecture/platform-migration-status.md:90` states *"last changed at **`84a4b4f` (2025-12-15)**, the
  commit that first added `terraform/main.tf`, and untouched by everything up to the repo's HEAD `87d8d44`
  (2026-06-19)"* — and adds, in the same row, *"`git blame -L 19,19` names `84a4b4f`; a file-level `git log` is not
  line provenance."*
- **Why it is material rather than cosmetic:** the map's whole interpretive point is that *"that count is not a
  decision about the fold; it predates it by seven months and nobody has been back."* roadrunner.md's phrasing
  dates the same declaration to three weeks *before* the fold, which reads as a live, recent decision to keep the
  service at 1 — the opposite inference, on the corpus's own single "prod and the platform's own declaration
  contradict each other" row.
- **Confidence:** high

### B3 — `dependency_map.md` says the storage compose block held "eight variables"; it held seven at both candidate refs (the eighth line is the `environment:` key)

- **Anchor:** `corpus/architecture/dependency_map.md:19` —
  > "…and **read neither**: no `DB_CONNECTION`/`REDIS_ADDR` among the eight variables in its compose env. Measured in
  > the platform's own compose at `0dab54d` — `docker-compose.yml:126-131` for the `depends_on`, `:116-123` for the
  > env block."
- **Why it is false:** at `platform 0dab54d`, `git show 0dab54d:docker-compose.yml` lines 116–123 are:
  ```
  116     environment:                     ← the key, not a variable
  117       - AWS_DEFAULT_REGION=eu-west-1
  118       - AWS_REGION=eu-west-1
  119       - ENVIRONMENT=development
  120       - PORT=8300
  121       - RPC_PORT=8301
  122       - SERVICE_NAME=storage
  123       - STORAGE_S3_PUBLIC_BUCKET=production-storage-public20240919130721114900000001
  ```
  **Seven** variables on eight lines. Enumerated the predicate first (§5 rule 24): the members are the seven
  `- NAME=` entries; the arithmetic then gives 7, not 8. The same block at `2adcf71` (`:203-210`) is byte-identical
  in membership — also seven — so no alternative ref rescues the count.
  `depends_on` at `:126-131` (`redis` + `postgresql`, both `service_healthy`) verifies exactly as written.
- **Why I booked it:** the sentence presents itself as a measurement (*"Measured in the platform's own compose"*)
  and supplies the range the count was taken from, so the count is load-bearing evidence rather than colour. I
  hesitated because the **predicate** it supports is true — neither `DB_CONNECTION` nor `REDIS_ADDR` is in that
  list — and the error is a one-off lines-for-members conflation of the corpus's own cited range, i.e. exactly the
  class §5 rule 24 names.
- **Confidence:** high (on the count), and I note explicitly that the claim it serves survives.

### B4 — `backend.md` cites storage's **client SDK** as the place storage "declares a Connect service … on their own muxes"

- **Anchor:** `corpus/services/backend.md:37-41` —
  > "Three of the others still *declare* a Connect service — but only in their own now-frozen repos, **on their own
  > muxes**, never on `app`'s: storage's `StorageService` (`storage@4ce8ece5:sdk/storage/v1/service.go`),
  > messenger's `MessengerService` (`messenger@fa47850d:internal/rpcsrv/rpcsrv.go`) and roadrunner's
  > `RoadRunnerService` (`roadrunner@87d8d443:cmd/root.go:87`)."
- **Why it is false:** the anchor resolves but names the wrong construct. At `storage 4ce8ece5`,
  `sdk/storage/v1/service.go` is the **consumer-side SDK**: it declares `type Service interface {GetObject,
  PutObject, GetPresignedUrl}` and constructors `NewService(c storagev1connect.StorageServiceClient, namespace
  string)` / `NewPublicService(...)` (`:13-17`, `:19`, `:34`). Every `StorageService` token in that file is
  `storagev1connect.StorageService**Client**` — the type an external caller imports to *dial* the service. It
  registers no handler and contains no mux.
  Storage's actual mux registration is `cmd/root.go:62-66`:
  `rpcMux := http.NewServeMux(); rpcMux.Handle(storagev1connect.NewStorageServiceHandler(rpcsrv.New(storageManager,
  storagePublicManager), rpc.DefaultInterceptors))`, and its server implementation is
  `internal/rpcsrv/rpcsrv.go` — the exact sibling of the messenger citation standing beside it in the same
  sentence, and the exact analogue of the roadrunner citation (`cmd/root.go:87`, verified: the
  `NewRoadRunnerServiceHandler` registration). So of the three parallel citations, two point at the construct the
  sentence claims and one points at its mirror image.
- **Why I hesitated:** the *predicate* — storage still declares a Connect service in its own frozen repo, on its
  own mux, not on `app`'s — is TRUE and independently verified (`cmd/root.go:63`). Only the anchor misdirects, and
  the file name contains the service's name, so a skim clears it. Booked because a reader checking the corpus's
  strongest structural claim about the eight folds would land on a client and find no mux, and because
  wrong-construct anchors are the class the protocol says a line-existence check can never catch (§5 rule 21,
  class 2).
- **Confidence:** medium

---

## Minors

- `corpus/services/backend.md:297` — *"Registered as a third RPC handler in `main.go` after Users and
  Organizations."* True of the PR the bullet is titled for (`app 9ecade240`, PR #896, 2026-05-29 — mux there is
  `main.go:240` Users, `:243` Organizations, `:249` LabSession) but of no ref in the clone set: at `b948604f` it is
  sixth (`:1228`, after Skiller `:1196`, JobSimulation `:1204`, CMS `:1212`) and at `ad9f3c49` sixth again
  (`:1338`), wrapped in `authn.HTTPAuthnMiddleware`. The same document states the current order correctly twice
  (`:30-34`, `:98`), so this is stale-inside-a-dated-bullet rather than a contradiction a reader would act on.
- `corpus/services/graphql-wundergraph.md:64-66` — *"the `Subscription` substring hits in that SDL are Stripe/plan
  field names — `activeSubscription`, `stripeSubscriptionId`, `type PlanSubscription`"*. Measured at
  `graphql-wundergraph 60c229f3`: `schemas/backend.graphqls` has **6** matching lines over **4** distinct names;
  `stripeSubscriptionItemId` is omitted. The claim's substance (all of them are Stripe/plan field names, none is a
  `type Subscription`) is exact — `grep -n '^type Subscription'` returns nothing, `^type Query` is `:4912` and
  `^type Mutation` is `:4053`.
- `corpus/services/studio-room.md:459-461` — *"(preserving any `collaborative_*` / `asset_*` JSON already in the
  bundle)"* omits the third preserved prefix: `exporter.py:537` @ `aeec036a` keeps files starting with
  `collaborative_`, `asset_` **or `internal_`** — which is how the `internal_localization.json` the same sentence
  writes survives a re-export.
- `corpus/services/studio-room.md:351-361` — the `requirements.txt` transcription reorders `mistralai` to third;
  the file at `aeec036a` is `openai, anthropic, rich, pyyaml, requests, jinja2, mistralai, pytest, pytest-asyncio`
  with `mistralai` at `:8`. Harmless — the doc cites `requirements.txt:8` correctly 5 lines later.
- `corpus/architecture/dependency_map.md:22` — *"`make up-all` started exactly **one** Brevo contact pusher:
  `backend`'s own in-process one is gated behind `CUSTOMERIO_SYNC_ENABLED`…"*. The colon reads as if backend's own
  were the one, when the point is the opposite (the one was the deleted `customerio-sync` container).
  `service_taxonomy.md:103-104` states the same fact unambiguously (*"exactly **one** Brevo contact pusher, the
  container: `backend`'s own was never on locally"*), so this is punctuation, not a factual split.

---

## Positively cleared (audited zeros)

Labelled per the briefing. **ENUMERATED** = the predicate was enumerated from source independently and
cardinalities compared. **RE-DERIVED** = only the arithmetic the document itself displays was recomputed.

### `corpus/services/studio-room.md`

- **ENUMERATED** — *"`gen.py` registers **exactly nine** arguments (`gen.py:484-492`)"*. Enumerated every
  `parser.add_argument` in `gen.py` @ `aeec036a` (the nested repo's own ref — `git -C stack-demo/app grep … --
  studio/` returns 0 for every predicate, per §5 rule 44, so the host was never used): lines 484–492 are
  `-i/--interactive`, `-m/--media`, `-f/--force` (dest `forced`), `--simid`, `--branch`, `--prompt`,
  `--annotations`, `--pipeline`, `--blueprint` — nine, and there is no tenth anywhere in the file.
- **ENUMERATED** — the `--template` warning. `parse_argument` is `gen.py:18-28` and does call `parse_known_args`
  then fold unknown `--k v` pairs into the dict; `validate_blueprint_exclusivity` is `gen.py:241-271` and raises
  the exact quoted string *"Cannot combine --blueprint with content parameters: …"*; its whitelist is
  `{blueprint, media, forced, interactive, branch, pipeline, attachments_path, worklog_path, postgen_path,
  published_path}` — the five execution controls plus the four `*_path` overrides the doc names.
  `translate_legacy_blueprint` (`gen.py:205-238`, the constant `_LEGACY_TEMPLATE_DEFAULTS` at `:205` and the
  function at `:212`) back-fills for exactly the three names `micro challenge` / `scenario challenge` /
  `collaborative challenge` and emits the quoted *"is ignored; asset type is now inferred from task
  interactions"* otherwise.
- **ENUMERATED** — the `mistralai` callout. `git -C stack-demo/app/studio grep -ci mistral aeec036a` →
  `requirements.txt:1`, `tools/pdf2md.py:18`, `tools/r3.py:3` = **22 lines / 3 files**, matching the doc exactly.
  `tools/pdf2md.py:24` is `from mistralai import Mistral`, `:96` `client = Mistral(api_key=api_key)`, `:127`
  `model="mistral-ocr-latest"`; `tools/r3.py:194` is the `if "MISTRAL_API_KEY" not in env:` hard requirement.
  `.gitignore:2` is `tools/` and `app/.gitignore:79` is `studio/*`, both as described. The provider registry at
  `services/ai.py:704-709` is `{'openai','azure','anthropic'}` and `ai.py:1-2` imports only openai/anthropic.
  Go execs exactly two studio scripts — `git -C stack-demo/app grep -n 'studio/[a-z0-9_]*\.py' HEAD -- '*.go'`
  returns `studioManager.go:119` (`studio/gen.py`) and `:1045` (`studio/postgen.py`), and both hold at the pinned
  `b948604f` the doc names. The Go-side Mistral path is real and is OCR-only: `internal/cms/studio/
  markdownManager.go:10,14,30` wires `mistralocr.New(aiKey)`.
- **ENUMERATED** — the embedding/deployment claims. `app/.github/workflows/build-production.yml:26-30` carries
  `additional_repo: "anthropos-studio-room:studio"` + `additional_repo_commit: latest`; `app/Dockerfile:28` is
  `FROM python:3.11-slim`, `:45-46` copy `studio/` and `pip install -r studio/requirements.txt`. The
  `additional_repo` change is `851cf3fb1`, which `git describe --contains` places inside **v1.360.1** — the
  version the banner names. `studioManager.go:94-96` is the `studio/studio-venv` constant block.
- **ENUMERATED** — the Asynq wiring. `internal/cms/worker/worker.go:29` `Concurrency: 5`; `:31-34`
  `{AiVideoQueue: 7, StudioQueue: 3}`; `internal/cms/worker/queues/queues.go` defines `StudioQueue = "studio"` /
  `AiVideoQueue = "ai_video"`; `studioManager.go:226` and `:702` enqueue on `queues.StudioQueue`. Priority
  weights, not concurrency limits, exactly as the doc says.
- **ENUMERATED** — post-gen targets. `agents/simulation/postgen/` holds `exporter.py`, `guidance.py`,
  `metadata.py`, `testing.py`, `toolkit.py`, `translation.py`; `SimulationPostGenerator._get_generation_plan`
  dispatches on exactly `toolkit`/`guidance`/`metadata`/`translation` — so the four-row table is the complete
  selectable set, `testing` is a module and not a target, and export is not a target (it is
  `SimulationExporter.export`, called by `run_postgen` at `postgen.py:371-373`). `postgen.py:396-398` marks
  `--media`, `--simid`, `--target` `required=True`.
- **ENUMERATED** — Output Structure. `exporter.py:514-550` is `_create_export_package`; `:518-519` build the
  postgen and published zips from `sim_source_id`; `:520` the scratch dir; `:541` writes `simulation.json`; `:547`
  re-archives; `:550` `rmtree`s. `gen.py:450-456` is the four-key `work_paths` loop with the
  `path_key.replace('_path','')` fallback the doc describes. I checked the one file the doc's *"none of them drops
  its own file"* could have missed — `SimulationPostGenerator.export()` writes `{simid}_postgen_results.json` —
  and it is **unreachable on the shipped path**: `run_postgen` constructs a `SimulationExporter` instead
  (`postgen.py:371-373`) and never calls it. The claim stands.
- **ENUMERATED** — configs. Three tracked configs (`config_template.ini`, `development_config.ini`,
  `production_config.ini`), `max_tokens = 4000` in all three; the `[SERVICES]` model rows are byte-exact
  (`FAST/STRICT = azure, gpt-5-mini, none`; `EXECUTION = azure, gpt-5.4, none`; `CREATIVE … low`;
  `REASONING … medium`), plus the matching `*_EXPERIMENTAL_*` row per mode. `load_services_settings`
  (`gen.py:41-53`) overrides the six INI keys from the OS environment in code, which is why `configparser`'s lack
  of `${VAR}` expansion is not a defect. `GenMode` (`services/ai.py:35-42`) is exactly FAST/STRICT/EXECUTION/
  CREATIVE/REASONING with `DEFAULT = EXECUTION`.
- **ENUMERATED** — blueprint + state. `tests/e2e/blueprints/` holds b2b/business/none_chat/none_voice/technical;
  `knowledge/development/asset-examples/blueprints/` holds one each for code/micro/scenario; every key in the
  quoted `technical.json` excerpt matches the file. State files `{simid}_pre_generation.json` /
  `_task_state.json` are `gen.py:116-117`; `_usage.json` is `services/usage_trace.py:302`. `cert.py`'s own
  docstring confirms the certification-benchmark reading. `services/taxonomy.py:11` is
  `BASE_URL = "https://api.anthropos.work/api"`. Studio-Room makes no GraphQL/Directus calls — `git -C
  stack-demo/app/studio grep -in "graphql\|directus" aeec036a` returns one line, in `changelog.md`.

### `corpus/services/backend.md`

- **ENUMERATED** — the RPC mux, both refs. Read `main.go:1184-1228` @ `b948604f` and `:1294-1338` @ `ad9f3c49`
  line by line: Users, Organizations, Skiller, JobSimulation, CMS (inside `if cmsRPCServer != nil`), LabSession —
  **six** handlers, five unconditional, at exactly the anchors given (`:1187/:1188/:1196/:1204/:1212-1214/:1228`
  and `:1297…:1338`). `SkillPathSessionService`: `git -C stack-demo/app grep -c … HEAD -- '*.go'` → **0 files**,
  positive control `JobSimulationService` → 3 files. `app`'s own docs do still list it —
  `CLAUDE.md:109` and `knowledge/architecture.md:28`, both at `ad9f3c49`, verbatim as quoted.
- **ENUMERATED** — the "three others still declare a Connect service" set. `roadrunner@87d8d443:cmd/root.go:87`
  is the `NewRoadRunnerServiceHandler` registration; `messenger@fa47850d:internal/rpcsrv/rpcsrv.go` returns
  `CodeUnimplemented` for `messenger.v1.MessengerService.Send/Schedule`. Storage's mux exists too — but at
  `cmd/root.go:63`, not the cited file (**B4**).
- **ENUMERATED** — Redis Streams, both refs. `git grep -n NewPublisher -- '*.go'` at `b948604f` returns exactly
  five `pubsub.NewPublisher` sites (`main.go:287`, `:637`, `:1039`, `wiring.go:127`, `:180`) and at `ad9f3c49`
  exactly five (`main.go:325`, `:746`, `:1149`, `wiring.go:132`, `:185`) — the sixth match is
  `redisstream.NewPublisher` in the DLQ, a different constructor. `buildStreamSubscribers`
  (`subscriber_wiring.go:203-250`) registers **six** subscribers — SkillPath `:207`, Skiller `:209`,
  JobSimulation `:214`, CMS `:233`, AIUsage `:236`, Backend `:248` — so *six subscribers / five publishers* is the
  measured split, and `skiller` is the consumer-only one. `subscriber_wiring.go:112-113` is the quoted self-stream
  comment; `main.go:230-232` is the `SERVICE_NAME` → `"backend"` default; `:1534-1541` the name struct; `:1579-1581`
  the single apply loop. At `b948604f`, `SKILLER_STREAM` has exactly **one** Go occurrence, `main.go:1276`, an
  `AddSubscriber`.
- **ENUMERATED** — the `http://backend.internal.anthropos:8081` derivation, the densest claim in my set and the
  one I most expected to fail. Enumerated the search set first: 13 top-level clones under `stack-demo`
  (15 git trees minus the two nested `studio` checkouts). `git ls-files '*.tf'` per clone sums to **44**;
  `find stack-demo -name '*.tf' -not -path '*/.git/*'` returns **59** — both figures exactly as published.
  Grepping the exact literal at each clone's own ref returns **6** lines: `app@ad9f3c49:knowledge/
  service-dependencies.md:52` (past tense, under the heading *"There are no external callers of app's RPC mux
  left."*) and five in the pinned `rosetta-extensions@09d06070` — `stack-core/tests/fixtures/repair_leak/
  {pre,post}/corpus/services/cms.md:32` and `:157` (four) plus `stack-core/tests/
  test_platform_predicate_guard.py:435`. **Zero** in `.tf` in any clone, positive control
  `service_desired_count` in `app` `.tf` → 1 file. The doc's self-refutation note about "only occurrence" over a
  scope that includes `rosetta-extensions` is itself correct.
  The terraform *shape* claims all resolve: `locals.tf:6` `project = "backend"`, `:8` `rpc_port = 8081`
  (identical at `b948604f` and `ad9f3c49`); `main.tf:58` `service_discovery_namespace_id`; `:185-186` the rpc_port
  mapping; `variables.tf:197` `cms_rpc_address` / `:230` `storage_rpc_addr` @ `b948604f`, and at `ad9f3c49`
  `cms_rpc_address` has moved to `:309` while `storage_rpc_addr` is deleted in favour of `storage_s3_bucket` /
  `storage_s3_public_bucket` (`:349`, `:354`) — the pin-and-drift note is exact.
- **ENUMERATED** — migrations. `git ls-tree --name-only <ref> terraform/migrations/ | grep -c '\.sql$'` → **170**
  at `ad9f3c49`, **169** at `b948604f`. Sorted, **46** have a `202606`+ prefix, and `20260529072659_add_lab_
  session.sql` is entry 124 of 170 — i.e. exactly 46 back from the head, so the doc's two figures are the same
  measurement seen twice. Heads verified byte-exact at both refs, as are the three predecessors
  (`20260731131307.sql`, `20260729133514.sql`, `20260728103254_ai_readiness_snapshot_frozen_matched_sources.sql`).
  `git ls-tree <ref> migrations/` is empty at `b948604f`, `2035f9a4` and `ad9f3c49`; `6a46e8445` is
  *"chore(migrations): remove obsolete atlas.sum file"* (2026-06-18); `atlas.hcl` env `local` carries
  `dir = "file://terraform/migrations"` and `src = "ent://internal/data/ent/schema"`.
- **ENUMERATED** — AI-readiness. `ls internal/data/ent/schema/ | grep ai_readiness` → **13** files, and the four
  the doc says a "9" omits (`recommendation`, `email_override`, `notification_log`, `notification_optout`) are all
  present, as are the two it says were *not* omissions (`live_snapshot`, `text_translation`). The engine is
  `internal/aireadiness/` with `manager.go`/`cycles.go`/`diagnosis.go`/`compare.go`/`csv.go`; `ls
  internal/workforce | grep -i readi` returns nothing; `graph/schemas/ai_readiness.graphqls` exists;
  `internal/worker/tasks/ai_readiness_refresh.go:10` defines the task type; `api.yaml` declares nine
  `/workforce/ai-readiness*` paths plus the email-preview route — "~10" is honest.
- **ENUMERATED** — LabSession. `main.go:743-746` @ `b948604f` is the exact `if labsAPIURL := os.Getenv("LABS_API_
  URL"); labsAPIURL != ""` block; `internal/labs/` holds `session`, `labsapi`, `adapter`, `catalog`;
  `internal/data/ent/schema/lab_session.go:34-124` matches every field the doc lists, including the 12-char-hex
  `id` comment, `mode` ∈ {test,build,teach} and `status` ∈ {booting,ready,grading,stopped,failed,cancelled}.
  The 60 s write timeout is `main.go:1353` (`rpc.WithWriteTimeout(60*time.Second)`).
- **ENUMERATED** — storage-in-app. `main.go:524`/`:525` are `internalstorage.NewManager` / `NewPublicManager`;
  `main.go` is byte-identical between `2035f9a4` and `ad9f3c49` (`git diff --stat` empty), so every `2035f9a4`
  pin in the corpus still resolves. `STORAGE_RPC_ADDR` in Go source at `ad9f3c49`: exactly **three**
  occurrences — `main.go:504`, `internal/jobsimwiring/wiring.go:101`, `internal/storagens/callsites_test.go:189`
  — all comments, `main.go:504` reading *"…and STORAGE_RPC_ADDR is gone."* Zero read sites.
- **ENUMERATED** — the subgraph ladder. Counted `- name:` in `supergraph-config-prod.yaml` at each named commit:
  `749dc86~1` = 5 (backend, skiller, jobsimulation, cms, skillpath), `749dc86` = 4, `7c17e63` = 3, `915da06` = 1.
  `git show --stat 915da06` deletes **both** `schemas/cms.graphqls` and `schemas/jobsimulation.graphqls`, and
  `git ls-tree 915da06^ schemas/` lists three files against `915da06`'s one — so **3 → 1** is measured, and the
  commit's own "2→1" subject is wrong exactly as the corpus says.
- **ENUMERATED** — the M810 split. `jobsimulation@6092c6d2:terraform/main.tf:15-22` is the M810 decommission
  comment stating the `module "jobsimulation"` deletion (ECS service, task definition, **ECR repository**, IAM
  roles, SG, Cloud Map, logs, alarms), and `:24-40` is the "what this module still owns" block naming livekit.tf,
  chime.tf, the `/production/jobsimulation/*` SSM parameters and the atlas tracker, with the legacy-schema drop
  called a separate step. `6092c6d2` is an ancestor of `origin/main` `82cb66ec` and **not** of the checkout
  `462343b0`, which still carries the module — so the corpus is right to grade this at the sha it names.
  On the cms side: `cms@f38c0c4a:terraform/main.tf:39` is still `service_desired_count = 0`, and `6efa1d5`
  (merged as `f38c0c4`, 2026-08-04) deletes `.github/workflows/build-production.yml` with the subject and body
  quoted verbatim. Two measured facts pointing opposite ways, exactly as described.
- **ENUMERATED** — compose/Make. `docker-compose.yml` @ `0c91421`: `:48` `AUTHORIZATION_ADDRESS=http://
  sentinel:8087`, `:57` `GOTENBERG_URL=http://gotenberg:3200`, `:59` `JUDGE0_BASE_URL`, `:66` `REDIS_ADDR`,
  `:93-94` the two Postgres DSNs, `:110` and `:183` `profiles: [core, backend, all]`, `:101-109` backend's
  `depends_on` with the `:102-103` in-line comment quoted exactly. `git grep -n _RPC_ADDR 0c91421d -- '*.yml'`
  → **zero**, positive control `AUTHORIZATION_ADDRESS` → 1. `Makefile:10` is `PROFILE ?= core`. Ports: compose
  publishes 8081/8082/8083 and sets `PORT=8082`, `RPC_PORT=8083`, `META_PORT=8084` — so 8081 really is
  published-but-unused. `internal/converter/gotenberg.go:31` is the `POST …/forms/libreoffice/convert` request.
  The intra-corpus pointer `architecture_overview.md:335` lands on the correctly-scoped
  *"the only cross-process RPC edge out of backend on a core stack"* line.
- **ENUMERATED** — misc. `internal/academy/academy.go:6-9` states the `internal/aiacademy` removal and
  `ls internal | grep aiacademy` is empty; `graph/schemas/skiller_taxonomy.graphqls` exists; `1e457fa70` is
  *"refactor(ai): fold the ai library into app as internal/ai"* (2026-08-04) and drops
  `github.com/anthropos-work/ai` from `go.mod` in that commit; `POST /api/webhook/directus` is fail-closed on
  `DIRECTUS_WEBHOOK_SECRET` (`main.go:1198-1199`, `internal/web/backend/backend.go:137`); every directory in the
  Key-directories block exists; `c3c45e01` really is v1.334.1, `platform 0808b92` really is the July-07 commit,
  `1fc00c78` really is *"Deprecate skiller schema"*, `9d00a313` really is v1.367.0, and `ad9f3c49` is exactly
  **5** commits past `2035f9a4` and **98** past `b948604f`.
- **RE-DERIVED** (weaker, flagged as such) — the *"compose now declares **five** services"* count at `:19`. Five
  is the count in `docker-compose.yml` itself (sentinel, backend, studio-desk, next-web-app, gotenberg); the
  resolved project is **seven** once `include: common.yml` adds postgresql + redis. I checked for a
  contradiction and found the corpus already draws the distinction in the same words —
  `service_taxonomy.md:66` reads *"declares five services (seven in the effective topology, once `include:
  common.yml` adds the …)"* — so `backend.md`'s "declares" is the narrower of two consistent readings, not a
  defect. Not booked.

### `corpus/services/graphql-wundergraph.md`

- **ENUMERATED** — profile tokens. `grep -o 'profiles: \[.*\]' docker-compose.yml` at `0c91421` yields exactly
  five distinct tokens (`all`, `backend`, `core`, `frontend`, `studio-desk`), and at `0dab54d` exactly eight (those
  five plus `customerio-sync`, `messenger`, `storage-legacy`) — so both the "five" and the retracted "eight" are
  measured, not asserted. `sentinel` (`:5-26`), `postgresql` and `redis` (`common.yml`) carry **no** `profiles:`
  key, so a retired token exits 0 and starts that three-service floor. `grep -c 5050 docker-compose.yml` → **0**.
- **ENUMERATED** — repo facts at `60c229f3`: `terraform/main.tf:20` `service_desired_count = 1`;
  `terraform/locals.tf` `aws_region = "eu-west-1"`, `project = "wundergraph"`, `port = 8080`;
  `terraform/main.tf:26` `health_check_path = "/health"`; `terraform/tests/` present and the only test artifact;
  `package.json` is `{"name": "graphql-wundegraph"}` and `CLAUDE.md:1` carries the same misspelling;
  `subgraphs.conf` is one line; `ci/` holds exactly `release-supergraph.sh`, `update-subgraph.sh`, `utils.sh` and
  `update-subgraph.sh:9` is the single `gh release download … -R anthropos-work/app`; `.github/workflows/` holds
  `release.yml`, `supergraph-update.yml` (a `workflow_dispatch` taking `backend_subgraph_version`) and
  `bump-version.yml`. Both Dockerfiles pin `node:22.11-alpine` + `wgc@0.104.0` +
  `ghcr.io/wundergraph/cosmo/router:0.275.0`; all three supergraph configs pin `federation_version: =2.3.2`;
  `config.compose.yaml` has `listen_addr: 0.0.0.0:8080`, `graphql_path: /graphql`, playground + introspection on,
  `max_request_body_size: 35MB`. Routing URLs: compose `http://backend:8082/graphql/query`, dev
  `http://host.docker.internal:8082/...`, prod `http://backend.internal.anthropos:8080/...` — matching the doc's
  three-mode description, and there is no `-local` variant.
- **ENUMERATED** — the subscription retraction. `grep -n '^type Subscription' schemas/backend.graphqls` → none
  (`type Mutation` `:4053`, `type Query` `:4912`); `grep -n subscription supergraph-config-*.yaml` → none, exit 1.
  `bba862f` exists, and `git branch -a --contains bba862f` returns **only**
  `remotes/origin/feat/use-web-socket`; `git merge-base --is-ancestor bba862f origin/main` → false.
- **ENUMERATED** — the compose-history paragraph, which is the easiest place in this file to be wrong and is not:
  every named sha resolves with the stated subject and date (`63d285c`, `d92e84e`, `719befb`, `a2a3ee6`,
  `2c85211` 2026-02-27, `67ba772` 2026-03-02, `b56d731` + `360efd4` + `2adcf71` all 2026-07-31, `0dab54d`
  2026-08-03, `838d907` + `0c91421` 2026-08-05). `git show 2c85211 -- docker-compose.yml` is literally
  `+      dockerfile: Dockerfile.dev` on the `graphql` service, and `67ba772` moves the context to `..`.
  At `2adcf71^` the router block publishes `"5050:8080"` and its `depends_on` is `backend, jobsimulation, cms,
  storage` — including the non-subgraph `storage` the doc calls out. `Makefile:10` is `PROFILE ?= graphql` at
  `2adcf71` and `PROFILE ?= core` at `0dab54d`; `repos.yml` still had `- name: graphql-wundergraph` /
  `type: node-npm` at `2adcf71^` and none at `2adcf71`.
- **ENUMERATED** — the healthy-rejection probe. `app/internal/authorization/gqlauthz/gqlauthz.go:186` is
  `return errorResponse(fmt.Errorf("unknown viewer: %w", errForbidden))`, and `:175-176` is the exact three-way
  exemption (`isPublicOnlyOperation || isFederationQuery || (environment == colony.Development &&
  isIntrospectionOnlyQuery)`). Both named regression tests exist: `gqlauthz_test.go:132` `"bare __typename is not
  exempt"` and `:176` `TestAnonymousRejectionLogsAtWarn`.
- **ENUMERATED** — the intra-doc self-citations, which are the class the protocol says rots silently. `:193`
  really is the `http://localhost:5050 refuses the connection` sentence; `:174-176` really is the compose
  line-number caveat; `:116-117` really is the struck-through *"`make up` rebuilds `graphql`"* bullet; `:84`
  really is the Ports bullet. Four for four.
- **ENUMERATED** — the frontend endpoints at `0c91421`: `docker-compose.yml:135` studio-desk
  `VITE_GRAPHQL_ENDPOINT`, `:160` next-web `NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT`, build args at `:119` and `:151`.

### `corpus/services/roadrunner.md`

- **ENUMERATED** — the Go-source zeros, with controls. `ROADRUNNER_RPC_ADDR`, `RoadRunnerService` and
  `roadrunner:10401` each return **0 files** in `-- '*.go'` at both `app b948604f` and `jobsimulation 462343b0`;
  positive control `JUDGE0_BASE_URL` in `app`'s Go → 1 file. The retraction of the repo-wide form is likewise
  measured: case-insensitive per-clone-at-own-ref file counts are **app 25, jobsimulation 8, platform 3,
  studio-desk 3, next-web-app 1** — five for five, exactly the published tuple. `jobsimulation`'s Go source has
  exactly one mention, `internal/runner/runner.go:3`, and `git -C stack-demo/jobsimulation grep -in roadrunner
  462343b0` returns **14 lines across 8 files**, of which **5 lines across 3 files** are exact-case — the doc's
  parenthetical, to the line. `jobsimulation@462343b0:knowledge/operational.md:68` is the `ROADRUNNER_RPC_ADDR`
  table row; `app/knowledge/plan/releases/07.00-jobsim-in-app/RE-PORT-CHECKLIST.md:10` does name
  `RoadRunnerService`; `app/internal/jobsimulation/simulator/stream_handlers.go:30-34` carries the quoted
  *"…NOT stream handlers"* note.
- **ENUMERATED** — `repos.yml` membership across refs: **9** entries at `2adcf71` with `- name: roadrunner` at
  `:29` (so `:29-31` is the entry), **6** at `0dab54d` (app, sentinel, storage, messenger, next-web-app,
  studio-desk), **4** at `838d907`/`0c91421`. `jobsimulation`'s entry is at `repos.yml:17` and its compose
  service at `docker-compose.yml:83` with `profiles: [graphql, jobsimulation, all]` @ `2adcf71`, all as stated.
- **ENUMERATED** — ports. `cmd/root.go:110` `cmp.Or(os.Getenv("PORT"), "8080")` and `:84`
  `cmp.Or(os.Getenv("RPC_PORT"), "8081")` are the only `cmp.Or` defaults; the compose block at `2adcf71`
  published `10400:10400` / `10401:10401` at `:291-292` and set its env at `:296-302` with `PORT=10400` at `:298`
  and `RPC_PORT=10401` at `:302`. Every row of the Environment-Variables table matches that block, including the
  two it says compose never set (`JUDGE0_API_KEY`, `SENTRY_DSN`).
- **ENUMERATED** — the async model. `internal/worker/queues` defines only `DefaultQueue = "roadrunner:default"`;
  `internal/worker/tasks` defines only `RoadrunnerSubmissionResult = "roadrunner:submissionresult"`;
  `worker.go` sets `Concurrency: 10` and maps that one queue; `runner.go` has exactly one `asynq.NewTask`
  (`:126`, `MaxRetry(3)`) and it sits inside `CreateSubmission` (`:48-151`); `HandleSubmissionResultTask`
  (`:237`) uses `maxRetries = 15` (`:244`) with `time.Sleep(1 * time.Second)` (`:273`) and publishes
  `Event_RoadrunnerSubmissionCompleted` (`:277-278`). `internal/rpcsrv` implements exactly
  `Submission`/`SubmissionPackage`/`SubmissionResult`. `internal/lsp/lsp.go` is referenced by nothing outside
  itself. `languages.go:26` is `case "py":`. `git ls-files '*_test.go'` → 0 and `Dockerfile:18` is
  `RUN go test -v ./...`, so the vacuous-pass claim holds. `go.mod`: `go 1.25.0`, `asynq v0.25.1`,
  `gorilla/websocket v1.5.3`.
- **ENUMERATED** — `d11a403` really does delete `- name: cms`, `- name: jobsimulation` **and**
  `- name: roadrunner` from `repos.yml` while its message says roadrunner's entry *"was already gone"*, and it is
  the commit at which the `cms:` compose service disappears (`git show d11a403^:docker-compose.yml` has
  `144:  cms:`; `d11a403` has none). The doc's *"grade that commit by its diff, not its message"* is correct.

### `corpus/architecture/dependency_map.md`

- **ENUMERATED** — the shared-library matrix, the one table here that is a pure cardinality claim. Read `go.mod`
  at each of the seven Go repos' own HEADs: **colony 7/7, proto 7/7, taxonomy 6/7 (absent only in roadrunner),
  ai 2/7 (cms + jobsimulation), authn 0/7** — all five figures reproduce. `cms/go.mod:9` and
  `jobsimulation/go.mod:11` are both `github.com/anthropos-work/ai v1.40.2`; `app/go.mod:20` @ `b948604f` is the
  direct `taxonomy v1.2.0` and `sentinel/go.mod:21` @ `88bc5592` the `// indirect` one — the doc pins sentinel at
  `88bc5592`, not the current `f2c46190`, and is graded there per the ref rule. The authn sub-claim is likewise
  enumerated, not re-derived: **0** Go-source imports of `anthropos-work/authn` in all seven repos, against
  `colony/authn` importers of **129** files in `app` @ `ad9f3c49`, **6** in cms, **8** in jobsimulation.
  `internal/ai/module_import_guard_test.go` exists.
- **ENUMERATED** — the two compose-topology counts. At `0c91421`: 5 services in `docker-compose.yml` + 2 in
  `common.yml` = **7** effective, `repos.yml` = **4**. At `0dab54d`: 8 + 2 = **10**, `repos.yml` = **6**. Both
  pairs match, and I resolved `include:` rather than reading `docker-compose.yml` alone (§5 rule 30's corollary).
- **ENUMERATED** — the skiller-stream cell, which carries the file's densest set of anchors and a bolded
  retraction. At `ad9f3c49`: `git grep -n SKILLER_STREAM -- '*.go'` → **6 lines** in **3 files** (`main.go`,
  `subscriber_merge_test.go`, `subscriber_wiring.go`); `git grep -l` with no pathspec → **6 files**. Both figures
  as published, and the "4 files" the cell retracts is reachable at no scope I could construct. The 6-drift/1-holds
  split verifies member by member: `main.go:287` is `logger.Info("subsystem switches",`, `:637` is `)`, `:1039` is
  `serverContext,`, `:1276` is `apiKeyManager,`, `wiring.go:127` is the asynq-client construction and `:180` is
  blank — while `internal/roles/roles.go:791` is
  `func (r *RoleManager) SkillerSubscriber() *pubsub.Subscriber {` byte-identical at `b948604f`, `2035f9a4` **and**
  `ad9f3c49`. `main.go:1537` is the `Skiller:` field; `docker-compose.yml:71` @ `0c91421` is
  `SKILLER_STREAM=skiller`.
- **ENUMERATED** — the messenger cells. `messenger@fa47850d:internal/flow/flow.go:72` is
  `AddSubscriber("backend", …)` and its handler list holds exactly **21** live `pubsub.EventHandler` entries
  (line `:83`, `OrgJobSimulationAssignmentPastDueHandler`, is commented out and correctly excluded — the
  `grep -c`-counts-commented-code trap, avoided); `:105` is the `jobsimulation` subscriber and `:109` the `cms`
  one. On the app side: `internal/messenger/flow/streams.go:65` `StreamBackend = "backend"`, `main.go:1493` the
  three-literal `verifyConsumerGroupExists` call, `main.go:1445` `if messengerEnabled {`, `:285`
  `mustSubsystemSwitch(envMessengerEnabled)`, `env_guards.go:61` the constant, `main.go:1437` the quoted
  *"ALL OF IT is behind MESSENGER_ENABLED."* Compose's own rationale is `docker-compose.yml:84-92` @ `0c91421`
  including *"which default to OFF on a developer machine"*. At `0dab54d` messenger's block does set all four
  `*_RPC_ADDR` to `http://backend:8083` under the quoted *"cms + jobsimulation are folded into app: all four RPC
  edges are the one backend mux"* comment.
- **ENUMERATED** — the withdrawn `storage.md` attribution. `grep -in 'depends_on|DB_CONNECTION|REDIS_ADDR|
  go.mod|redis|postgres' corpus/services/storage.md` returns **nothing** (exit 1), positive control
  `grep -c storage` → 49. The retraction is correct.
- **ENUMERATED** — cross-references. `service_taxonomy.md:101-103` really is the customerio-sync passage the row
  points at; the intra-doc `:9/:15/:31` pointers at `:90` all land on the in-process-cms statements they claim.
- **RE-DERIVED** only — the Studio-Room row's *"22 hits / 3 files"* is the same measurement as studio-room.md's
  and was enumerated there, so I count it once.

### `corpus/services/skiller.md`

- **ENUMERATED** — the taxonomy figures, against the capture rather than against the prose. Read
  `.agentspace/snapshots/taxonomy/5afc0bccf1df7ef538b643321fc6362f/manifest.json`: top level is
  `source: primary-read`, `public_only: true`, `predicate: org-null`, `captured_at:
  2026-06-29T14:43:00.076309Z` — all four attributes as published — and the table rows give
  `public.skills` **42,790**, `public.job_roles` **22,470**, `categories` 23, `specializations` 1,447,
  `job_role_embeddings` **18,919**. So the *"18,919 is the `job_role_embeddings` row count — a different table"*
  refutation of "18K roles" is measured, not inherited, and every figure `backend.md` repeats from the same
  capture agrees. (The sibling digest `c75ce94d…` is the pre-M211 `skiller.*` re-key of the identical payloads,
  which corroborates the doc's M209/M211 re-grounding note.)
- **ENUMERATED** — the merged shape. All six named Ent schema files exist (`skill.go`, `jobrole.go`,
  `skill_embeddings.go`, `job_role_embeddings.go`, `category.go`, `specialization.go`);
  `internal/rpc/skillerrpc/skiller.go:37` names exactly the five externally-reached methods
  `GetSkill, GetSkills, SearchSkill, MatchSkill, GetJobRole`; `graph/schemas/skiller_taxonomy.graphqls` exists and
  `categoryTree` / `fullCategoryTree` survive only inside its `:7` comment *"stay unported — no consumers"*
  (positive control: `mostPopularSkills` resolves in `queries.graphqls:102`). `skill_translation.go` and
  `job_role_translation.go` exist and `enum ContentLanguage` (`assignments.graphqls:44-53`) has exactly **8**
  members. `IVFFLAT` indexes on `skill_embeddings` and `job_role_embeddings` are
  `terraform/migrations/20260615130000_skiller_taxonomy.sql:251` and `:61`, with `vector(1536)` in the same file.
- **ENUMERATED** — the two intra-corpus anchors that a heading grep alone would have failed:
  `shared_libraries.md` has `## taxonomy` at `:206` (→ `#taxonomy`) and an explicit
  `<a id="taxonomy-figures"></a>` at `:228`, so `#taxonomy-figures` resolves despite matching no heading;
  `backend.md:115` is `## Skiller-in-app merge — fact-sheet (v2.1 "quick change")`, which slugifies to the
  fragment `skiller.md:42` uses.
- **ENUMERATED** — the `.tf` zero: 0 hits for the literal across all 44 tracked `.tf` files in the 13
  `stack-demo` repos, each at its own HEAD (same derivation as `backend.md`'s, run once and cited twice — the
  corpus's own instruction that the count be stated in one place and referenced elsewhere is honoured here).

---

## What I could not settle

- **GitHub archive states.** No network access and no clone-side metadata records them. Unsettled:
  `graphql-wundergraph.md:8` / `dependency_map.md:23` ("ARCHIVED on GitHub, 2026-07-30");
  `roadrunner.md:37-38` (skiller archived 2026-07-01, skillpath 2026-07-31); `roadrunner.md:43,74` (roadrunner
  **not** archived); `roadrunner.md:56-57` (chronos **not** archived, last push 2026-04-23);
  `roadrunner.md:46` (jobsimulation's archive state "not visible to this corpus" — which is itself a disclosure
  of the same limit and is consistent with what I can see). **What would settle it:** `gh api
  repos/anthropos-work/<name> --jq '.archived, .pushed_at'` per repo, recorded with its timestamp (§5 rule 49 —
  archive state is a concurrently-mutable surface).
- **Production-only figures.** `backend.md:129-131`'s 2026-07-08 prod counts (43,584 total skills incl. 794
  org-private, `job_roles` org-NULL 22,490, `skill_embeddings` 43,584) cannot be re-measured from any clone; the
  independent 2026-06-29 public capture corroborates the three figures it shares but says nothing about the
  org-private residual. Same for `skiller.md:35-37` (skiller's ECS service + terraform module removed from prod;
  ECR orphaned) — the `skiller` repo is in no clone set. **What would settle it:** a read-only `/db-query` run of
  the four counts, and the `infrastructure` repo in a clone set.
- **`dependency_map.md:13` — "the 23 jobsim run-state tables."** Not derivable without either a migrated database
  or a table-by-table attribution of the ported `internal/jobsimulation` Ent schemas to the pre-merge
  `jobsimulation` schema; I could not build that mapping reliably inside this pass and declined to assert a
  cardinality I had not enumerated. **What would settle it:** `select count(*) from information_schema.tables
  where table_schema='jobsimulation'` on a migrated stack, cross-checked against the `20260724…`-era migrations.
- **`backend.md:37-43` — `customerio-sync`'s own RPC surface.** The doc says nothing can be said about it because
  the repo has never been in a clone set; I confirmed the repo is absent from `stack-demo` and from `repos.yml` at
  every ref I read, so the disclosure is accurate and the underlying fact stays unmeasured here.
- **One thing worth flagging as a possible future gap rather than a defect.** `app/atlas.hcl` @ `ad9f3c49`
  declares a **second** Atlas env for *"sentinel-in-app v10.0 / M1001"*, under which `app` takes custody of the
  `sentinel` schema (`casbin_rules`). Nothing in my six files is falsified by it — `sentinel` is still a live
  compose service (`docker-compose.yml:5`) and still the one Connect-RPC peer — so I did not book it. But it is a
  ninth fold in flight that the merge banner at `backend.md:5-16` ("**Eight** former microservices") does not
  mention, and it will falsify that banner the moment the container goes.
