# Seat F — reading #27

**Refs read (re-derived at open, unchanged at close):** `stack-demo/app` `ad9f3c49` (= `origin/main`),
`stack-demo/platform` `0c91421d` (= `origin/main`), `stack-demo/next-web-app` `8297c684`,
`stack-demo/sentinel` `f2c46190`, `stack-demo/ant-academy` `22df69dd`, `stack-demo/storage` `4ce8ece5`
(origin/main `9f8cb532`), `stack-demo/messenger` `fa47850d`, `stack-demo/cms` `ca50c817`
(origin/main `f38c0c4a`), `stack-demo/graphql-wundergraph` `60c229f3`, `stack-demo/roadrunner` `87d8d443`,
`stack-demo/jobsimulation` `462343b0` (origin/main `82cb66ec`), `stack-demo/app/studio` `aeec036a`
(own nested checkout), `stack-demo/rosetta-extensions` `09d06070` (**pinned per-stack clone — the tree that
settles "what the tooling does on a stack"**), `.agentspace/rosetta-extensions` `680e8529` (authoring copy).
Where a claim named a ref, I graded it at that ref and say so.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/services/ant-academy.md` | 503 | yes |
| `corpus/architecture/architecture_overview.md` | 431 | yes |
| `corpus/services/storage.md` | 248 | yes |
| `corpus/services/sentinel.md` | 166 | yes |
| `corpus/services/ai-labs.md` | 158 | yes |
| **total** | **1506** | — |

Search-pipeline positive controls run in the same pass: `git grep -c ENVIRONMENT 0c91421d -- docker-compose.yml`
→ 7 (against the 0 returned for `STORAGE_RPC_ADDR` in the same three files); `git grep -c customer.subscription
ad9f3c49 -- '*.go'` → 3 files (against the 0 for `checkout.session.completed`). Nested repos enumerated with
`find stack-demo -name .git -maxdepth 4`; `app/studio` was grepped at **its own** ref `aeec036a`, never at
`app`'s.

---

## Blockers

### B1 — Ant Academy is described as an internal, `@anthropos.work`-gated employee portal; it is a public, paid consumer storefront

- **Anchor (primary):** `corpus/services/ant-academy.md:474` —
  > "**Clerk (shared)**: Uses the same Clerk app as the rest of the platform. Domain-gated to `@anthropos.work` so external users cannot enter."

  **Same predicate, same reading, other assigned sites:**
  - `corpus/services/ant-academy.md:5` — *"is the **internal learning portal** for Anthropos employees … to anyone with an `@anthropos.work` email."*
  - `corpus/services/ant-academy.md:31` — *"**Primary Goal**: Internal-only learning portal that delivers AI-engineering chapters to `@anthropos.work` employees"*
  - `corpus/services/ant-academy.md:24` — *"**Authentication** | Clerk (`@anthropos.work` domain gate + org-membership gate)"*
  - `corpus/architecture/architecture_overview.md:40` — *"**Ant Academy** (`ant-academy`): Internal learning portal … for `@anthropos.work` employees."*
  - `corpus/architecture/architecture_overview.md:260` — *"Internal learning portal for `@anthropos.work` employees (standalone, Vercel-deployed)"*

  (Out of my file set, so reported not booked: `corpus/architecture/frontend_architecture.md:9`,
  `corpus/ops/setup_guide.md:385`, root `CLAUDE.md:262` carry it too — the rule-19 corollary sites.)

- **Why it is false** — measured at `ant-academy` `22df69dd`, the checkout and `origin/main`:

  - `code/src/lib/platformUrls.js:4-7` — *"**The Academy is a storefront in front of the Anthropos platform**
    (`app.anthropos.work` …). Account + billing live on the platform, so every conversion CTA leaves the
    Academy and lands on a platform page"*. `:9-12` defines **FLOW A — Account gate**:
    `platformSignUpUrl(returnTo)` → `${STUDIO}/sign-up?redirect_url=…`, *"The PRIMARY CTA for this flow —
    **a new visitor registers**"*. `:17-27` defines **FLOW B — Checkout gate**: *"Checkout contextually
    registers → signs in → subscribes **for an anonymous visitor**"*. There is a complete public
    registration-and-payment funnel; "external users cannot enter" cannot be true beside it.
  - `knowledge/onboarding-and-conversion.md:10` (in the same repo) — *"The Academy is a **public storefront**
    sitting in front of the Anthropos platform"*; its three-concept table lists **Sign up** = *"Create a
    (free) platform account"*.
  - `knowledge/user-types.md` "The four user types" table — **Anonymous · Signed-in (free) · Subscriber ·
    Enterprise/Org member**. `Subscriber` is detected by `billingPremium()` → `billing.premium === true`.
    None of the four is a domain-restricted employee; there is no `@anthropos.work` predicate anywhere in
    the detection list.
  - `code/src/lib/pricing.js:21` `STANDARD_YEARLY = { usd: 399, eur: 349 }`; `:14`
    `LAUNCH_PROMO_ENABLED = true`; `:18` `LAUNCH_COUPON_CODE = 'aiacademylaunch'`; `:26-40` a *"$199 for
    everyone"* community offer with currency-aware pricing carried to Stripe checkout. `code/proxy.js:60`
    imports `currencyForCountry` from it, and `TopBar` renders `platformCheckoutUrl()` as a
    `topbar-buy-cta` for **anonymous** visitors (`src/components/TopBar.jsx:77-82`).
  - `code/src/lib/landingSeo.js` is a public SEO/marketing copy deck (*"AI training for sales teams … Hands-on,
    free to start"*, per-vendor and per-role landing sections); `code/src/lib/schema.js:3`
    `SITE_URL = 'https://aiacademy.anthropos.work'`. `code/.env.example:42-50` wires a **Meta Conversions API**
    funnel (PageView + **Lead**) for the AI Academy.
  - **Self-contradiction inside the same corpus file:** `ant-academy.md:316-317` already documents that
    anonymous visitors browse `/`, `/latest(.*)`, `/chapters/(.*)`, `/courses`, `/courses/(.*)` and that
    `/library`, `/free` are *"Phase-1 public launch: read-only catalog preview + the free-tier listing"*.
    I verified that matcher against `code/proxy.js:112-188` and it is complete and correct — which makes
    `:474`'s "external users cannot enter" contradict `:316` 158 lines above it.

- **Confidence:** high.
- **Why I hesitated (stated, per the instrument):** two mitigations, neither of which survives contact with
  the funnel. (1) The ant-academy repo's *own* `CLAUDE.md:11` still says *"AI Academy is an internal learning
  portal for Anthropic employees (`@anthropos.work` domain)"* — so the corpus is faithfully echoing a stale
  upstream doc rather than inventing the claim. (2) `ant-academy.md:298` locates the domain restriction *"in
  the Clerk app"*, and Clerk tenant config is in no clone, so I cannot read the gate's absence directly. But a
  product cannot simultaneously sell a $399/yr subscription to an anonymous visitor through
  `/checkout` and refuse entry to everyone without an `@anthropos.work` address; and the harm is material —
  this framing is what `safety.md`-class reasoning about demo exposure and about who may see academy content
  is built on. Booked as a BLOCKER.

---

## Minors

- `corpus/services/ant-academy.md:304` — *"~26 Playwright e2e spec files (tests/e2e/)"*. Measured at
  `22df69dd`: **31** `*.spec.js` under `code/tests/e2e/` (40 tracked entries there in total — 31 specs,
  2 `_`-prefixed helpers, 7 `widgets.spec.js-snapshots/*.png`). The `~` softens it, but 26 → 31 is ~19 % low.
- `corpus/services/ant-academy.md:176` — *"`code/public/catalog.json` (~2,667 entries)"*. The tracked file at
  `22df69dd` carries **2,692** `courses` entries (`version 1.0`, 7 languages). ~1 % drift under a `~`; harmless.
- `corpus/services/ant-academy.md:457` — the repo-local skills row omits `export-path` (16 dirs exist under
  `.claude/skills/`, incl. `_shared`). The row is explicitly a partial list (*"Other content-pipeline helpers"*),
  so this is an omitted list member, not a false claim.
- `corpus/services/ai-labs.md:135` — *"`cmd/labsdemo` (standalone local Labs-catalog server on `:8099`,
  **in-memory store**, dev only)"*. The **workspace** store is in-memory; the catalog is **local Postgres**:
  `cmd/labsdemo/main.go:1-7` — *"It **connects to a local Postgres**, ensures the labs / lab_bodies / api_keys
  tables exist (idempotent), wires the REAL handlers (backend.AttachLabsAdminRoutes) **over an in-memory
  workspace store**"*; the usage line `:11` reads *"`:8099`, local Postgres"*. A reader would infer labsdemo
  needs no DB, which is wrong. I hesitated toward BLOCKER; kept it MINOR because the compressed phrase is a
  direct lift of the source's own `"the in-memory store is non-durable"` and the first line of the file
  corrects it.
- `corpus/services/ant-academy.md:391` vs root `CLAUDE.md` — this doc says *"The **app's** env file is
  `code/.env`, not the repo root"* (and `:412` repeats `code/.env`), while `CLAUDE.md` says the React app
  *"reads **only** from `code/.env.local`"*. Both files are loaded by Next.js and both are gitignored
  (`.gitignore:9-10`), so `ant-academy.md` is the correct half and `CLAUDE.md`'s *"only"* is the wrong one —
  a cross-file wording inconsistency with no consequence for a reader following either. Not booked against
  my file.

---

## Positively cleared (audited zeros)

**ENUMERATED** — predicate re-derived from source independently, cardinality compared:

1. **`architecture_overview.md:377-384` — "only 31 of 135 schemas" auto-filter by organization.**
   I enumerated the schema dir at `ad9f3c49`: 139 non-test `.go` files, minus `mixin.go`,
   `database_types.go`, `skiller_mixins.go`, `skillpath_mixins.go` = **135**. `OrganizationMixin{}` occurs
   **30** times, of which `user_resource.go:22` is `// OrganizationMixin{},  // We need to work on this` →
   **29 live**. Exactly three schemas declare their own `Policy() ent.Policy`
   (`user.go:116`, `organization.go:56`, `org_membership.go:172`); of those, `User` filters by USER
   (`rule.FilterSameUserRule()` at `user.go:134`), while `Organization` (`rule.FilterSameOrganizations()`,
   `organization.go:96`) and `Membership` (`rule.AllowCurrentOrgEdgesOrSkipRule()`, `org_membership.go:183`)
   filter by ORG. `29 + 2 = 31`. `TenantMixin` carries **no** `Policy()` (the comment at `mixin.go:158` sits
   on `Indexes()`), and no other mixin embeds `OrganizationMixin`. Identical at `b948604`, `9d00a313`,
   `ad9f3c49`.
2. **`architecture_overview.md:382-383` — "23 schemas carry an `organization_id` with no policy at all
   (16 is the *neither-mixin* subset of those 23)".** This is the trap the instrument warns about, and my
   *first* derivation was the naive one: 51 files contain the literal, minus the 26 `OrganizationMixin` users
   that contain it, minus `org_membership.go` = **24**, of which **17** use neither mixin and **7** use
   `OrganizationIDMixin` (`skiller_mixins.go:152`, the deliberately policy-free port —
   `category.go`, `jobrole.go`, `similarity.go`, `skill.go`, `specialization.go`, `studio_document.go`,
   `studio_task.go`). The corpus's numbers are one lower on both, and **the corpus is right**: the sentence's
   predicate is *"no policy **at all**"*, and `academy_feedback.go` embeds `UserMixin{}`, which **does**
   declare a `Policy()` (`mixin.go:99`). Excluding it: **23 / 16 / 7** — byte-exact. `UserMixin` and
   `OrganizationMixin` are the only two policy-bearing mixins in the tree.
3. **`architecture_overview.md:20` / `:221` — jobsimulation M810.** The claim names `6092c6d2`, so it is
   graded there (rule 33). At `jobsimulation` `origin/main` `82cb66ec` (which contains `6092c6d2`,
   *"chore(terraform)!: remove the jobsimulation ECS service and ECR repository (M810)"*, 2026-08-04),
   `terraform/main.tf` is **56 lines**, `:15-22` is the M810 decommission comment naming exactly the ECS
   service / task definition / **ECR repository** deletion, and `:24-40` names what survives — the LiveKit +
   Chime buckets, the `/production/jobsimulation/*` SSM parameters and the atlas tracker, with the schema drop
   called out as *"a separate M810 step"*. **At the checked-out `462343b0` the same file is 343 lines and still
   declares `module "jobsimulation"` with `service_desired_count = 0` at `:40`** — an attractor I did not book,
   because the claim names its ref.
4. **`architecture_overview.md:20`'s own archive retraction** — jobsimulation `origin/main` carries exactly
   **four** 2026-08-04 commits (`6092c6d2`, `caf36c96` = merged **PR #439**, `1e40d184`, `82cb66ec`).
5. **`architecture_overview.md:59` — Next pins.** `apps/{web,hiring,integration,maintenance}` each declare
   `next: ~16.2.12`; `apps/mobile` declares none; `pnpm-lock.yaml` resolves `next@16.2.12`. 4/5 apps, exact.
6. **`architecture_overview.md:80` — `authn` imported by nothing.** 0 `anthropos-work/authn` hits in `go.mod`
   across all seven Go clones (app, sentinel, storage, messenger, cms, roadrunner, jobsimulation), against a
   positive control of `anthropos-work/colony` present in **all seven**.
7. **`architecture_overview.md:12` / `:71` / `:157` / `:223` — the compose topology.** `docker-compose.yml`
   at `0c91421d` is 186 lines and declares **5** services (sentinel, backend, studio-desk, next-web-app,
   gotenberg); `common.yml` adds postgresql + redis with **no `profiles:` key** → **7 effective**, floor of 3,
   `core` selects backend + gotenberg → **five containers**, two of them ours. `Makefile:10` `PROFILE ?= core`.
   `repos.yml` holds exactly 4 entries (app/sentinel/next-web-app/studio-desk); `app` is the only
   `migrations: true`, `schema: public`.
8. **`architecture_overview.md:412` / `:179` — "23 run-state tables".** `terraform/migrations/20260722081626_jobsim_data_model.sql`
   at `ad9f3c49`: **23** `CREATE TABLE`, **0** `DROP`.
9. **`architecture_overview.md:348-352` — `STORAGE_RPC_ADDR`.** At `9d00a313`: **3** Go hits, every one a
   comment, at exactly `main.go:451`, `internal/jobsimwiring/wiring.go:101`,
   `internal/storagens/callsites_test.go:189`; repo-wide **29 lines across 18 files**. At `ad9f3c49` the same
   3, with `main.go:504` carrying the *"the standalone service takes no traffic and STORAGE_RPC_ADDR is gone"*
   comment. At `b948604f`: **15** hits, of which exactly **7** are live env reads
   (`cmd/academy-asset-upload/main.go:129`, `cmd/academyImport/main.go:231`, `cmd/import/main.go:50`,
   `internal/jobsimwiring/wiring.go:115`, `main.go:446`, `:524`, `:992`) — which is also what makes
   `storage.md:29`'s *"the three `cmd/` tools"* the right enumeration.
10. **`sentinel.md:26` — the Casbin model.** `internal/authorization/casbin.go:13-45` declares exactly
    `r,r2..r6` (**6 request**), `p,p2..p6` (**6 policy**), `g,g2,g3` (**3 groupings**), `m,m2..m6`
    (**6 matchers**), and each matcher's semantics match its table row (incl. `m`'s `p.tier == '%s'`
    substituted from `authorizationv1.Tier_TIER_FREE`, and `m6`'s `g3(p6.org, r6.sub)` with no tier term).
11. **`sentinel.md:70-83` — the RPC table.** All 19 named methods exist on `RPCHandler`
    (`internal/rpcsrv/rpc.go`), and the file declares no method the table omits.
12. **`sentinel.md:85` / `:89` — `AUTHORIZATION_ADDRESS`.** Exactly **1** occurrence across
    `docker-compose.yml` + `common.yml` + `.env_example` at `0c91421d` **and** at `0dab54d`
    (`docker-compose.yml:48`, the `backend` block). `messenger` @ `fa47850d` imports no authorization client
    (0 Go files matching `authorization`). Self-consistent with `clerk-integration.md:75`/`:94`
    (*"storage, messenger | — | no auth"*).
13. **`sentinel.md:43-48` — "there is no `manager` role".** 0 occurrences of `manager` in
    `init_policy.sql`; it appears only as a fixture string in `casbin_test.go`, `rpc_test.go` (and, unnamed by
    the corpus, `manager_test.go:21`). `init_policy.sql:63-66` is exactly the taxonomy-write omission note;
    `:88-118` is exactly the `content_creator` block; the four `MembershipRole` values sit at
    `app/internal/data/ent/enum/membership.go:8-15`.
14. **`storage.md:219` — "the block's seven `environment:` entries".** At `0dab54d` the storage block sets
    exactly 7 (`AWS_DEFAULT_REGION`, `AWS_REGION`, `ENVIRONMENT`, `PORT`, `RPC_PORT`, `SERVICE_NAME`,
    `STORAGE_S3_PUBLIC_BUCKET`) — identical set at `2adcf71`. `STORAGE_S3_BUCKET` is **not** among them,
    exactly as `:228` says.
15. **`ai-labs.md:112-113` — the Stripe webhook event set.** `internal/web/backend/api/api.go:317-327`
    switches on exactly `customer.created` and `customer.subscription.{created,updated,deleted}`; 0 Go hits
    for `checkout.session.completed` at `ad9f3c49` (positive control: `customer.subscription` → 3 files).
16. **`ant-academy.md:308-327` — the complete `isPublic` matcher.** Every one of the 9 groups + 2 dev-only
    groups reproduces `code/proxy.js:112-188` exactly, in membership and in the conditional spreads
    (`DEV_LOGIN_ENABLED`, `VISUAL_BYPASS`). No pattern in the file is missing from the table and none in the
    table is absent from the file.
17. **`ant-academy.md:268-284` — the `TopBar` surface set.** Enumerated by mount site at `22df69dd`:
    `AcademyClient.jsx:1906` (reached by `app/(authed)/page.jsx:151`, `courses/page.jsx:92`,
    `courses/[slug]/page.jsx:219`), `CourseClient.jsx:2091`/`:2141` (`/chapters/[slug]`),
    `LatestClient.jsx:128`, `BookmarksClient.jsx:508`, `MyActivityClient.jsx:161` — **seven routes**;
    `MyCertificatesClient.jsx` imports no `TopBar`. `PublicHeader` has exactly one mount site,
    `app/(public)/library/page.jsx:28`; `app/(public)/free/page.jsx:18` is `redirect('/?tier=free')`.
    `SUPPORTED_LOCALES` (`src/i18n/locale.js:10`) is 7, and `LanguageSelector.jsx:88`/`:97` render a
    `role="menu"` of `role="menuitemradio"` options over it.
18. **`ai-labs.md:54` — "EN base + 6 overlays".** `lab_body.go:12-13`: *"the EN base is locale "en"; each
    translation overlay (de/es/fr/it/nl/pt) is its own row"* — 6.
19. **`architecture_overview.md:298-300` — the latent Studio-Room `openai` arm.** Grepped `app/studio` at its
    **own** ref `aeec036a` (a host-ref `git grep` would have returned a guaranteed 0): three `configs/*.ini`
    exist and **all three** pin `azure` on every `*_AI_*_MODEL` line.

**RE-DERIVED** — arithmetic the document displays, recomputed (weakest form of clearance):

- `ai-labs.md:20` — `git describe --tags ad9f3c49` → `v1.369.0-7-gad9f3c498`; `2035f9a4` →
  `v1.369.0-2-g2035f9a40`; newest tag `v1.369.0`. All three exact.
- `storage.md:25` — `git rev-list --count 4ce8ece5..9f8cb53` = **20**; `terraform/main.tf` is 100 lines at
  `4ce8ece5` and 18 at `9f8cb53`; *custody* occurs **0** times in the storage repo at `9f8cb53`;
  `storage/main.go` is indeed **18 lines** at both refs.

**Anchor sweeps that resolved byte-exact** (read with `sed -n 'N-15,N+5p'`, not grepped-to):
`app/main.go:516,517,518-523,524,525,529-535,547,1102` and `:1048`@`9d00a313`;
`app/env_guards.go:37-44`; `app/internal/storage/service.go:22,24`; `app/internal/storage/storage.go:193-200`;
`app/internal/converter/gotenberg.go:31`; `app/internal/jobsimwiring/wiring.go:115,118`@`b948604`;
`app/internal/jobsimulation/recording/recording.go:12` and `…/anticheat/anticheat.go:30`@`9d00a313`
(and there is indeed **no** `anticheat.go` beside `recording.go`);
`app/internal/jobsimulation/ai/ai.go:85-88,129,166,262-276,325`;
`app/internal/cms/studio/markdownManager.go:29-30` **and the retraction's own evidence** — `:11` is the
closing paren of the import block and `:19` is the comment recording that the `os.Getenv` read was removed —
plus `studioManager.go:583`; `docker-compose.yml:48,56,57,59,66,73-79,82,83,100,138-140,165-167,170,183`
@`0c91421`; `docker-compose.yml:119`@`0dab54d` and `:206`,`:210`,`:324`@`2adcf71`; `repos.yml:18-20`@`0dab54d`;
`platform/README.md:81-87`; `graphql-wundergraph/terraform/main.tf:20` `= 1`; `cms/terraform/main.tf:39` `= 0`
at both `ca50c817` and `f38c0c4a`; `roadrunner/terraform/main.tf:19` `= 1`;
`storage/terraform/main.tf:9-11,13-16,18`@`9f8cb53`, `storage/terraform/storage.tf:22-25`@`9f8cb53` and
`:21-24`@`4ce8ece5`; `sentinel/terraform/locals.tf:4-5`; `sentinel/Makefile:3` (hard-coded DSN);
`ant-academy` `src/progress/store.js:150,162,169,202,210,215`, `src/writeThrough/index.js:247,259`,
`app/api/academy/beacon/route.js:1-18`, `src/lib/serverTenant.js:111-113,115-117,145`,
`ucourses/catalog.js:31,961`, `src/views/academy/AcademyClient.jsx:1363-1365`,
`app/layout.jsx:1,41,51,132`, `academy.css:1`;
rext (**pinned** clone `09d06070`) `demo-stack/ant-academy.sh:56,66,75,84,90` (the five patches, in order)
and `:576-583` (the e2e-persona-bypass-gone block), plus `stack-verify/live/autoverify.sh:459` assert *(f)*;
rext (**authoring** copy `680e8529`) `demo-stack/cockpit.py:327,812,1496`.

**One near-miss worth recording** (§5 rule 10): `ai-labs.md:77` says `course.refine` = **1** credit. The
package header at `app/internal/credits/cost.go:29` says *"course.refine → **5** credits per refine turn"*.
The **map** — the source of truth — is `creditCost{ActionCourseBuild: 5, ActionCourseRefine: 1,
ActionCourseTranslate: 1}` at `:86-90`, and the const's own doc at `:59-67` states the D1 flat-1 ruling.
**The corpus is right and the platform's own header comment is the stale one.** I nearly booked the corpus
for it by reading the line the grep landed on.

---

## What I could not settle

- **`architecture_overview.md:3` — "the repo is ARCHIVED on GitHub (2026-07-30)"** (graphql-wundergraph).
  GitHub repo metadata is not in any clone. `origin/main`'s last commit is `60c229f3`, **2026-07-30** — which
  is *consistent* with an archive that day but is not evidence of one. Note this is the same class of claim
  the corpus itself retracted for jobsimulation at `:20` (where later commits refuted it); here nothing
  refutes it and nothing confirms it. Settled only by `gh api repos/anthropos-work/graphql-wundergraph
  --jq .archived`.
- **`ai-labs.md:5` — "`anthropos-work/AI-Labs` is the live Go control plane"**. Not in the clone set.
  Indirectly corroborated: `app/internal/data/ent/schema/lab_session.go:6` cites
  *"AI-Labs/docs/PHASE_B_LABSESSION.md"*, and `internal/labs/labsapi/client.go` is a real HTTP client to it.
  I could not confirm the repo's language or liveness. Settled by cloning it.
- **`ant-academy.md:298` — "`@anthropos.work` domain restriction is enforced in the Clerk app"**. Clerk tenant
  configuration is in no clone; nothing in `proxy.js` enforces a domain. This is the one half of B1 I could
  not read directly — see B1's hesitation note. Settled by the Clerk dashboard / a live sign-up attempt.
- **`ai-labs.md:16` (`app` `knowledge/plan/releases/06.00-shared-purse/`, M600–M607 "all planned"),
  `storage.md:25` (`app/knowledge/plan/releases/09.00-support-in-app/m903-s3-custody/` front-matter
  `status: planned`), `storage.md:92` (`knowledge/plan/platform-defect-register.md`).** HARD BAR 1 forbids
  `knowledge/plan/**`; I did not open any of them, in either repo, rather than guess at the carve-out's
  intended edge. Everything *around* these claims that lives outside `knowledge/plan/` I did verify and it
  holds (`storage.tf:22-25`'s *"That was never executed"*, `d3e6d32`'s subject *"correct the guard header,
  which contradicted the shipped design"*, 0 `moved` blocks in the storage repo, `internal/labs/` importing
  `internal/credits` nowhere, `handler.go:12`'s Wave-13 removal).
- **`ant-academy.md:304` — "1000+ Vitest tests"**. 245 `*.test.js(x)` files under `code/tests/` at
  `22df69dd`; the assertion count needs an actual `npm test` run.
- **`sentinel.md:97` — sentinel crash-loops with `pq: no schema has been selected`** without the schema.
  A runtime claim; needs a live stack with the `sentinel` schema absent.
- **`architecture_overview.md:222` — whether M810's `module.cms_euwest1` deletion has been applied in prod.**
  The corpus already says this is *"not visible to this corpus"* and instructs *report both, assert neither*;
  both halves it reports are true in the clones I read (`cms/terraform/main.tf:39` `= 0`; `6efa1d5`,
  *"chore(ci): drop build-production — the cms ECR repository is decommissioned (M810)"*, whose body names
  `module "cms_euwest1"`). `infrastructure` is in no clone set, so the disposition is genuinely unmeasurable.
