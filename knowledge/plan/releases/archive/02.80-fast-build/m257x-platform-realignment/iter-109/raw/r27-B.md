# Seat B — reading #27

**Ground truth re-verified at this reading's open** (all shas match the ADDENDUM's superseded table):
`app ad9f3c49` · `platform 0c91421d` · `next-web-app 8297c684` (origin/main `f97ba659`) · `sentinel f2c46190` ·
`storage 4ce8ece5` (origin/main `9f8cb532`) · `messenger fa47850d` · `cms ca50c817` (origin/main `f38c0c4a`) ·
`graphql-wundergraph 60c229f3` · `roadrunner 87d8d443` · `jobsimulation 462343b0` (origin/main `82cb66ec`) ·
`studio-desk 41ee3575` · `ant-academy 22df69dd` · nested `app/studio` + `cms/studio` both `aeec036a` ·
**rext per-stack consumption clone `stack-demo/rosetta-extensions 09d06070`** (the tree I used to settle every
"what the tooling does on a stack" claim) · rext authoring copy `.agentspace/rosetta-extensions 680e8529`
(not used to settle anything in this report).

Search-set discipline: every tree-wide count below was taken with the §5-rule-44 loop
(`find stack-demo -name .git -maxdepth 4`) — **15 git trees, the two nested `studio` repos included** — each
grepped at its **own** ref, stderr read. NUL control run on the two known files
(`next-web-app/apps/web/src/hooks/useCoursebuilder.ts` = 1 NUL byte, `ant-academy/code/src/time/store.js` = 1),
measured with `tr -dc '\000' | wc -c`, not `grep -c`.

One instrument failure caught mid-pass and worth recording: `git -C $N show $REF:apps/...` **silently
mangles** under zsh — `$REF:a` is consumed as the `:a` (absolute-path) parameter modifier, producing
`8297c684pps/web/...` and a `fatal: ambiguous argument`. Because the failure printed to stderr while the
`awk` pipe printed nothing, it read exactly like an empty file. Re-run with `${REF}:apps/...`. Every
next-web-app measurement below was retaken after the fix.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/services/ai-readiness.md` | 672 | yes |
| `corpus/services/cms.md` | 313 | yes |
| `corpus/services/chronos.md` | 245 | yes |
| `corpus/services/academy-backend.md` | 160 | yes |
| `corpus/services/README.md` | 88 | yes |
| `corpus/services/TEMPLATE.md` | 46 | yes |
| **total** | **1524** | |

## Blockers

### B1 — `ai-readiness.md` re-pins a self-citation to `:496`; the construct it names is at `:498`/`:500` — third generation of the same defect in one sentence

- **Anchor:** `corpus/services/ai-readiness.md:49-52` —
  > ⚠️ **The bare line number was itself the defect.** That cross-reference read `:458` when iter-46 wrote it and
  > `:459` after iter-100 mechanically re-pointed it, and **neither line has ever carried the statement** — `:459`
  > merely opens the `> **✅ CORRECTED M219 …**` blockquote, which is about an unrelated claim. The target is
  > therefore named by construct above and pinned at **`:496`** (M257x iter-102); when the two disagree, the name wins.

- **Why it is false:** the named target is the **⚠⚠ M51 iter-08/09** block, quoted at `:47-48` as
  *"(now `aireadiness/readiness.go`, formerly `workforce/ai_readiness.go:512`)"*. Measured in the file itself
  (`grep -n` + `sed -n '494,502p'` at corpus HEAD):

  | line | content |
  |---|---|
  | `:496` | `> relying on either number; do not re-derive them from prose.` — the **last line of the `✅ CORRECTED M219` blockquote**, i.e. the very blockquote the sentence complains `:459` wrongly pointed into |
  | `:498` | `**⚠⚠ M51 iter-08/09 — the frozen READ is ITSELF org-scale-slow …**` — the block being named |
  | `:500` | `(now `aireadiness/readiness.go`, formerly `workforce/ai_readiness.go:512`) …` — the exact quoted parenthetical |

  `grep -n "now \`aireadiness/readiness.go\`, formerly"` returns **`500`** and nothing else;
  `grep -n "M51 iter-08/09"` returns `46` (the citing sentence) and **`498`**. So the third pin lands two lines
  short of the block and four short of the quotation — inside the wrong blockquote, which is precisely the
  failure the sentence documents. The self-heal clause ("when the two disagree, the name wins") mitigates the
  reader's cost but does not make the anchor true, and the sentence is published as a *worked example of a
  repaired anchor*.

- **Confidence:** high.

### B2 — the `AIReadinessClient.tsx` anchor family is measured at `bb3313bc` and is 2 lines stale at this reading's checkout `8297c684`

- **Anchors (all in `corpus/services/ai-readiness.md`, none of them naming a ref):**
  - `:84` — *"It gates on `orgEnabled` alone (`:133-134`, `const { orgEnabled } = useAiReadinessEnabled(true)`)"*
  - `:482-484` — *"(`AIReadinessClient.tsx:153-154`) computes `effectiveCycleId = selectedCycle ?? activeCycle?.id ?? latestClosedCycle?.id` and gates the data GET on `cyclesQ.isFetched` (`:166-170`)"*
  - `:334-335` — *"`AIReadinessClient.tsx:78` `const SHOW_SECONDARY_TABS: boolean = false;`, read by the tab filter at `:599`"*

- **Why it is false:** measured on
  `next-web-app apps/web/src/app/(authenticated)/(verified)/ai-readiness/AIReadinessClient.tsx` at three refs:

  | construct | `bb3313bc` (prior reading) | **`8297c684` (this checkout)** | `f97ba659` (origin/main) |
  |---|---|---|---|
  | `const { orgEnabled } = useAiReadinessEnabled(true);` | **133** | **135** | 135 |
  | `const effectiveCycleId = …` | **153-154** | **155-156** | — |
  | `useAIReadiness({… enabled: featureOn && cyclesQ.isFetched })` | **166-170** (`enabled:` at 169) | **168-172** (`enabled:` at 171) | — |
  | `(SHOW_SECONDARY_TABS && tab.key === 'compare')` | **599** | **601** | 601 |
  | `const SHOW_SECONDARY_TABS: boolean = false;` | 78 | 78 | 78 |

  At `8297c684`, `:133-134` is comment prose (*"`featureOn` also keeps the dashboard queries from firing while /
  off (the backend would reject them with `ErrAIReadinessDisabled`)"*); `:153-154` is the comment
  *"The closed-snapshot path now ALSO joins current membership tags…"*; `:166-170` excludes the `enabled:` line
  the sentence is about; `:599` is `tab.key === 'how' ||`.

  This is a **systematic, single-cause family**, not four coincidences: every one of them is exact at
  `bb3313bc`, the ref the frozen instrument's own clone table names and the ref this file's `:317` cites for a
  *different* construct. The document explicitly re-pinned two other neighbourhoods at `8297c684` — `:307-309`
  (*"Every line number in the three bullets below is a reading at `next-web-app` `8297c684`"*, which I verified:
  `urls.ts:51` ✓, `:52 = TALK_TO_DATA_URL` ✓, `useNavbarSections.tsx:4/:401-408/:403/:569` ✓) and `:609`
  (the `HowWeMeasureTab.tsx` parenthetical, `:1879/:1903/:1915/:1921/:1927`, 1,989 lines, `grep -c
  interviewQuestions` = 0 — all ✓). The re-pin swept the blocks that carried a ref and missed the ones that did
  not, which is `§5 rule 40`'s discharge-post-condition gap in its most literal form.

- **Confidence:** medium. I hesitated because each individual drift is ≤2 lines and every quoted code string is
  verbatim correct, so the reader recovers — the canonical grading rule carves *line drift* out as MINOR. I book
  it because (a) the instrument I was handed resolves doubt upward, (b) it is a family, not a slip, and (c) the
  file spends three separate paragraphs (`:49-52`, `:308-309`, `:614-616`) warning against exactly this and
  presenting itself as having fixed it.

### B3 — `cms.md` states the studio Python subprocess is invoked "via `bash -c`"; the live code says "NEVER through a shell"

- **Anchor:** `corpus/services/cms.md:287` —
  > Note: when the Go service runs in development mode it auto-provisions a venv at `studio/studio-venv`, runs
  > `pip3 install -r studio/requirements.txt`, and invokes `python3 studio/gen.py ...` / `studio/postgen.py`
  > from the cms repo root **via `bash -c`** (paths are `studio/...`, not from inside `studio/`).

- **Why it is false:** in the **live** code (`app` `ad9f3c49`, `internal/cms/studio/studioManager.go`) the shell
  is gone and its removal is load-bearing:
  - `:1096` — *"runCommand executes name+args in argv (exec) form — **NEVER through a shell**. Callers pass a …"*
  - `:1101` — `pycmd := exec.CommandContext(ctx, name, args...)`
  - `:103` — *"argparse as inert literals instead of being interpreted. **No `bash -c`**."*
  - `:124` — *"the dev-only venv bootstrap **was previously chained into the same `bash -c` string** that carried the tainted args."*
  - `:119` / `:1045` — `s.runCommand(ctx, pyBin, append([]string{"studio/gen.py"}, tokens...))` /
    `…{"studio/postgen.py"}…`; `:126`/`:129` run `python3 -m venv` and `studio/studio-venv/bin/pip3 install -r
    studio/requirements.txt` as **argv**, not a `&&`-chain.

  The claim is still true of the **frozen** repo (`cms` `ca50c817`,
  `internal/studio/studioManager.go:98`/`:951` build a `python3 -m venv … && source … && python3 studio/gen.py %s`
  string and `:967` runs `exec.Command("bash", "-c", command)`) — which is what makes it dangerous rather than
  merely stale: the sentence is *correct about the dead code and wrong about the shipped code*, and it describes
  a **security property** (argv vs shell) that was deliberately inverted.

  Context that decides it: the note carries **no HISTORICAL marker**, unlike its two neighbours in the same
  section (`:233` *"⚠️ HISTORICAL — `cd cms; make init-studio` is NOT the onboarding path any more"* and `:291`
  *"HISTORICAL — this is no longer how the shipped pipeline is refreshed"*); the paragraph immediately above it
  (`:265-268`) re-points the reader to *"in `app/studio/`, not `cms/studio/`"* and the code block at `:271` opens
  `cd app/studio`; and the banner at `:110` states *"the live code is `app/internal/cms/`"*.

- **Confidence:** medium. I hesitated because the sentence says *"from the cms repo root"*, which can be read as
  scoping it to the frozen repo where it remains true. I book it because nothing marks it historical, everything
  around it was re-pointed at `app`, and "the Go service" in this document is now `backend`.

## Minors

- `corpus/services/README.md:41` — the storage row's prod claim (*"the ECS service is **deleted**, not scaled to
  zero — the module survives only to keep the buckets/CDN under `prevent_destroy`"*) is **true at `storage`
  `origin/main` `9f8cb532`** (`terraform/main.tf` = 18 lines, no `module "storage"`, the comment at `:9-18`
  saying exactly this) and **false at the checked-out `4ce8ece5`** (100 lines, `module "storage"` at `:10`,
  `service_desired_count = 1` at `:19`; `e6fac97` *"drop the ECS service, keep the assets"* lands between them).
  The cell names no `storage` ref, while its sibling jobsimulation row at `:22` **does** name `6092c6d2` for the
  identical kind of claim. Graded MINOR rather than BLOCKER because `§5 rule 41` establishes `origin/main`-first
  as this corpus's own citation-resolution ladder and the claim is about **prod**, which follows the repo head,
  not a lagging local checkout — but the row should carry `@ storage 9f8cb532` the way the jobsimulation row does.
- `corpus/services/ai-readiness.md:283` — *"`aireadiness.Manager` … is constructed with `db`, `ent`, `orgRepo`,
  `ai.AI`, a pub `EventPublisher`, the `academy.EmbeddingsManager`, and the `WorkforceDirectory`"*. `orgRepo` is
  **not** a constructor parameter: `manager.go:135` is
  `NewManager(logger, db, aiClient, entClient, pub, academyEmbeddings, directory)` and `orgRepo` is derived
  inside at `:140` (`repository.NewEntRepository(entClient).Organization`). A field list, not a parameter list.
- `corpus/services/ai-readiness.md:444-445` — *"this doc cited `:907-914` until M257x iter-108, which is the doc
  comment of `queryReadinessSimCompleters` (`:922`)"*. The doc comment is `steps.go:911-921`; `:907-910` is the
  tail of the preceding function's body. The `:922` function anchor is exact.
- `corpus/services/chronos.md:22` — *"**Codebase**: `stack-dev/chronos`"*. No `stack-dev/` workspace exists on
  this box and `chronos` is in no clone set at `platform 0c91421d`; the path is historical and unreachable.
  Harmless (the same line offers the git URL) but the sibling `cms.md:110` handles the equivalent case explicitly
  (*"Clone it by hand to read the pre-merge source"*).
- `corpus/services/ai-readiness.md:463-464` — *"Retracted here and at [`ops/demo/stories-spec.md:599`]"*. `:599`
  is the **claim being retracted** (*"believing the live-recompute never completed…"*); the retraction itself is
  the blockquote opening at `:601`. Lands in the right passage, points at the wrong half of it.

## Positively cleared (audited zeros)

**ENUMERATED** — predicate re-derived from source, cardinality stated before any arithmetic:

- `README.md:3` *"Every service doc in this directory, enumerated"* — `ls corpus/services/*.md` = **29** files;
  README's four tables carry **10 + 5 + 8 + 4 = 27** rows; the remaining two are `README.md` itself and
  `TEMPLATE.md` (listed under *Related*). Set membership checked name-by-name: **0 orphans, 0 phantoms.**
- `README.md:11` *"**Seven** services … skiller, skillpath, jobsimulation, cms, storage, messenger and
  customerio-sync"* — 7 names, and `git ls-tree ad9f3c49 internal/` shows all seven packages present
  (`internal/{skiller,skillpath,jobsimulation,cms,storage,messenger,customeriosync}`). Row `:37` asserts the same
  set; the two agree.
- `README.md:37` *"`app/internal/roadrunner/` exists at no ref — at `ad9f3c49` **no path in the whole tree
  matches `roadrunner`**"* — `git ls-tree -r --name-only ad9f3c49 | grep -i roadrunner` → **0** (rc=1); positive
  control `grep -ic skiller` on the same listing → **59**. Judge0 is where the row says: `jsrunner
  "github.com/anthropos-work/app/internal/jobsimulation/runner"` imported at `internal/jobsimwiring/wiring.go:34`
  and **`:123`** `runnerManager := jsrunner.NewRunnerManager(getenv("JUDGE0_API_KEY"), getenv("JUDGE0_BASE_URL"))`.
  Both files under `internal/jobsimulation/runner/` exist.
- `README.md:29-30` *"`docker-compose.yml` declares **5** services (7 effective, with `common.yml`'s `postgresql`
  + `redis`), and `repos.yml` carries **4** entries"* — enumerated at `platform 0c91421d`: compose = {sentinel,
  backend, studio-desk, next-web-app, gotenberg} = **5**; `common.yml` = {postgresql, redis} = **2**; `repos.yml`
  = {app, sentinel, next-web-app, studio-desk} = **4**. Also enumerated the always-on floor by predicate (*services
  declaring no `profiles:` key*) = {postgresql, redis, sentinel}, which is what makes `cms.md:252-253`'s
  "exits 0 and starts postgresql, redis and sentinel" true.
- `README.md:44` — messenger `flow.go:72-104` @ `fa47850`: `AddSubscriber("backend", …)` opens at **:72**, closes
  at **:104**, and I counted the members: 5 skill-path + 4 job-simulation (`:83` is commented out and excluded) +
  1 content-assigned + 1 content-completed + 2 organization + 1 AI-readiness-completed + 4 AI-readiness-notifications
  + 3 course-builder = **21 live handlers**. `MESSENGER_RPC_ADDR`: **0 across all 15 trees at their own refs**
  (positive control at the same invocation: `SKILLER_RPC` → app 26, cms 12, jobsim 9, messenger 4, platform 1,
  rext 20). `git -C stack-demo/platform log -S 'MESSENGER_RPC' --oneline 0c91421d` → **0**; control
  `-S 'SKILLER_RPC'` → **7**. Every figure in that cell reproduces exactly.
- `cms.md:55` / `:196` — the `.tf` census: **44** tracked `.tf` files across the 13 top-level `stack-demo` repos
  at each clone's own HEAD (per-repo: app 5, cms 4, gql 4, jobsim 6, messenger 4, next-web 2, roadrunner 4,
  rext 2, sentinel 4, storage 5, studio-desk 4, ant-academy 0, platform 0; nested `app/studio` + `cms/studio` = 0
  each), and **59** by raw filesystem sweep. `http://backend.internal.anthropos:8081` = **6 occurrences, 0 in
  terraform** — `app/knowledge/service-dependencies.md:52` (1) + the rext consumption clone's test fixtures and
  `test_platform_predicate_guard.py:435` (5). `:52` really is the past-tense sentence, under the heading
  *"**There are no external callers of app's RPC mux left.**"* at `:50`.
- `cms.md:44-47` — the six re-created tables: `CREATE TABLE` enumerated from
  `app/terraform/migrations/20260724132049_cms_data_model.sql` = `similarities` (`:2`), `studio_documents` (`:23`),
  `studio_tasks` (`:38`), `similarity_categories` (`:72`), `similarity_features` (`:85`), `similarity_skills`
  (`:99`) — **6, same names**, matching the doc's list exactly.
- `cms.md:144-149` — `app/studio/requirements.txt` @ `aeec036a` transcribed in full: openai, anthropic, rich,
  pyyaml, requests, jinja2, mistralai (`:8`), pytest, pytest-asyncio = **9 packages**; `python-docx` absent.
- `cms.md:95` / `:214-227` — the Mistral predicate, run with all three §5-rule-44 instruments:
  `git -C stack-demo/app/studio grep -ci mistral aeec036a` = **22 lines / 3 files** (`requirements.txt` 1,
  `tools/pdf2md.py` 18, `tools/r3.py` 3). `services/ai.py:705-708` is the literal three-entry provider dict;
  `:1-2` imports only `openai`/`anthropic`. `tools/pdf2md.py:24` `from mistralai import Mistral`, client `:96`,
  `model="mistral-ocr-latest"` `:127`. Go side: `markdownManager.go:10` import + `:30`
  `mistralocr.New(aiKey)` inside `NewMarkdownManager` — **identical at the pinned `2035f9a` and at `ad9f3c49`**,
  exactly as the doc claims. `studioManager.go:531` is the OCR content-type comment, `:583` builds the manager,
  `xlsx.go:13` states the xlsx exclusion. `app/.gitignore:79` = `studio/*`.
- `cms.md:59-63` — the subgraph ladder, enumerated from `graphql-wundergraph`'s own history:
  `749dc86` deletes `schemas/skiller.graphqls` (5→4), `7c17e63` deletes `schemas/skillpath.graphqls` (4→3),
  `915da06` deletes **both** `schemas/cms.graphqls` (−762) **and** `schemas/jobsimulation.graphqls` (−860)
  (3→1). `subgraphs.conf` at `60c229f3` = `BACKEND=v1.360.0`, one line.
- `ai-readiness.md:170-188` — the table set: `git grep 'Table = "ai_readiness'` over the ent-generated packages
  returns **13** distinct table names; the doc's table lists **13** and they match one-for-one
  (cycles · steps · skills · sims · user_step_progresses · live_snapshots · snapshots · diagnose_narratives ·
  text_translations · recommendations · email_overrides · notification_logs · notification_optouts), plural forms
  included. Plus `organization_settings` and `public.interview_aggregated_reports`, both real.
- `ai-readiness.md:257-260` / `:567` — `defaultReadinessSkills` enumerated from `internal/aireadiness/defaults.go`:
  **31 entries = 19 @ 1.0 + 12 @ 0.5**, so the denominator is **25.0** — the set, then the arithmetic.
  `defaultReadinessSims` = **3** (`{StepSimulation,"tech",634b9ffd…}`, `{StepSimulation,"business",a4113c6b…}`,
  `{StepInterview,"both",6d6cdf39…}`), and the demo seeder's `ai_readiness_config.go:93-95` pins the **same three
  uuids**.
- `ai-readiness.md:206` / `:641-645` — `usageDimSpecs` enumerated from `how_we_measure.go:1160-1181`: **5** specs,
  ids `avg_adoption` · `avg_transformation` · `avg_originality` · `avg_depth` · `avg_ownership`, every one banding
  at 61/41. The seeder emits exactly those five (`ai_readiness_interview_report.go:185-189` in the **pinned**
  rext clone), and its `:149` really does read *"All five are always emitted here"*. Note: the platform's own doc
  comment at `how_we_measure.go:1183` still says *"the **four** … tiles"* — the **corpus is right and the
  platform comment is stale**, which is the direction I did not expect and checked twice.
- `ai-readiness.md:280` — CSV columns enumerated from `csv.go:17-33`: **15**, and the code's own header comment
  `:13-16` names the four dropped ones ("Suggested action", "Keyword", "Suggested Anthropos Skill Path",
  "Company-side actions") from the original 19. (`ExportAIReadinessCSV`'s doc comment at `:35` still says
  "19-column" — again the platform, not the corpus.)
- `ai-readiness.md:272-274` — `AIReadinessCompareResponse` (`compare.go:32-41`) carries exactly the **6** named
  sections: Topline, Archetypes, Transitions, TeamDelta, SkillCoverage, ThemesShift.
- `ai-readiness.md:604` — `interactions.action_type` is a **closed set of exactly two**:
  `interaction.ActionTypeValidator` (`internal/data/ent/interaction/interaction.go:92-99`) accepts
  `"email", "call"` and nothing else; the seeder's `aiReadinessInteractionAction = "call"`. The
  `source_id <> target_id` CHECK is real (`terraform/migrations/20260722081626_jobsim_data_model.sql:94`) and the
  actors FKs are at `:91-92`. Turn count `6-11` = `aiReadinessMinTurns = 6` / `aiReadinessMaxTurns = 11`.
- `ai-readiness.md:397-412` — `aiReadinessStageFor` (pinned rext `ai_readiness_funnel.go:205-217`) maps exactly
  as described: explicit `not_started` → 0 **checked first**, manager → 0, `TrajectoryStruggling` → 1,
  `default:` → 3. `Persona.AIReadiness` is a real yaml field (`blueprint.go:200`) with a closed accessor
  (`:347 IsAIReadinessNotStarted`).
- `ai-readiness.md:577-579` — the held-skill bands re-derived from the code, not the code's comment (whose own
  doc-block at `:446-452` is stale): `aiReadinessHeldCount` gives stage 3 → `[aiReadinessCoreSkills, poolLen]`,
  stage 2 → `[3, aiReadinessCoreSkills]`, stage 1 → `[1,3]`, with `aiReadinessCoreSkills = 19`
  (`ai_readiness_config.go:29`) and poolLen 31 ⇒ **19…31 / 3…19 / 1…3**, exactly the doc's figures.
  `aiReadinessStartedHeroSkills = 9`.
- `academy-backend.md:65-78` — all **11** academy tables enumerated from the ent-generated `Table =` constants;
  every plural form the doc warns about is correct (`academy_chapter_progresses`, `academy_last_activities`,
  `academy_chapter_times`, `academy_certificates`, `academy_bookmarks`, `academy_feedbacks`, `academy_series`,
  `academy_skill_paths`, `academy_chapters`, `academy_chapter_bodies`, `academy_path_embeddings`).
- `academy-backend.md:63-64` — *"3 remaining occurrences at that ref, **all comments**, zero reads"*: at
  `9d00a313`, `git grep STORAGE_RPC_ADDR -- '*.go'` = **3** (`jobsimwiring/wiring.go:101`,
  `storagens/callsites_test.go:189`, `main.go:451`), each inside a `//` comment. Enumerated, not counted.
- `academy-backend.md:102` — *"No Connect-RPC for academy"*: `git grep -i AcademyService -- '*.go'` = **0**; the
  full `mux.Handle(` set in `main.go` is 6 handlers (users, organizations, skiller, jobsimulation, cms, labs) and
  contains no academy surface.
- `ai-readiness.md:241-248` — the GraphQL surface read **from the SDL itself**
  (`schemas/ai_readiness.graphqls`): queries `aiReadinessEnabled`, `aiReadinessUserPlanProgress`,
  `aiReadinessSkills`; mutation `completeAiReadinessSkillMapping` — the doc's list is the whole file, nothing
  omitted or invented. REST routes enumerated from `api/server.gen.go:2396-2406`: **11** registrations, and every
  path the doc names is among them (`/setup` GET+POST, `/cycles` GET+POST, `/cycles/:cycleID`,
  `/cycles/:cycleID/close`, `/steps-completion`, `/narrative`, `/compare`, `/export.csv`).

**RE-DERIVED** — arithmetic the document itself displays, recomputed (the weakest clearance in this report;
listed separately for that reason):

- `ai-readiness.md:568` `round(1.0/25.0*30)` = 1.2 → **1/30**; `:578` `round(9.0/25.0*30)` = 10.8 → **11/30**;
  `:361` 156/199 = **78.39 %**. All three reproduce. (The *sets* behind them — the 31-skill repertoire and the 9
  hero skills — are ENUMERATED above; these three lines are only the sums.)
- `ai-readiness.md:651` *"5 Playthroughs"* — `playthroughs/manifest/ai-readiness.yaml` carries **4**
  `playthrough:` keys (member-done, member-progress, manager-dashboard, manager-howwemeasure) and
  `pt-onboarding-aireadiness-guided` lives in `onboarding.yaml:361`; 4 + 1 = 5. Field name checked first
  (`playthrough:`, not `id: pt-`), positive control on `onboarding.yaml` = 6.
- `ai-readiness.md:290-291` *"(−653 lines)"* — `dae0fb2f7` deletes AIReadinessContainer 103 +
  AIReadinessIntro 220 + AIReadinessView 330 = **653** (commit total is 662; the extra 9 are
  `HowWeMeasureTab.tsx` edits, correctly excluded).

**Other exact anchors opened and confirmed** (abbreviated; each was read with surrounding context, never grepped
to a single line): `README.md` — `roadrunner/terraform/main.tf:19 = 1` (at checkout **and** origin/main),
`cms/terraform/main.tf:39 = 0` (at `ca50c817` **and** `f38c0c4a`), jobsimulation `6092c6d2` (`service_desired_count`
absent, `:15-22` = the M810 comment block, and `6092c6d2` **is an ancestor of** jobsim origin/main),
`platform-migration-status.md:101` = the customerio-sync row, `d11a403` removes cms+jobsim+roadrunner from **both**
compose and `repos.yml` (services 11→8, entries 9→6). `cms.md` — `6efa1d5`/`f38c0c4` (subject + body verbatim),
191-line module at both refs, `cms/go.mod:3 go 1.26.4`, `cms/cmd/root.go:77`/`:78`, `cms/Dockerfile:2`/`:23`,
compose `2adcf71:154-155/169/173`, `app/main.go:1205-1211` @ `b948604` (the *"Additive + DORMANT … until the M809
re-point"* comment), `skillpath/session.go:205-207`, `REDIS_CMS_CACHE_INDEX` default 5 (`main.go:1107-1108`),
`/api/webhook/directus` + `DIRECTUS_WEBHOOK_SECRET` fail-closed (`backend.go:137-138`, `main.go:1198-1199`) vs the
standalone `/webhooks/` mount (`cms/cmd/root.go:242`), `gen.py:484-492` (**9** `add_argument` calls) +
`gen.py:18-28` (`parse_known_args` merge), `tools/r3.py:139`/`:190`/`:199-206`, `studio/CLAUDE.md:12-14`,
studio version `v0.51.1`, `cms` Makefile still carrying `init-studio`/`update-studio`, `go.work` gitignored.
`academy-backend.md` — `0e37771f` (PR #903, 2026-06-05), `git describe ad9f3c49` = `v1.369.0-7-gad9f3c498` and
`CHANGELOG.md:5` = `## v1.369.0 - 2026-08-05`, `9d00a313` = `v1.367.0`, `main.go:471-472`,
`cmd/academyImport/main.go:239-243` (quote verbatim) + `:244-246` (the hard error), `academy.graphqls` +
`resolver_academy.go`, `/content/catalog.json` (`content.go:23`) + `/content/admin` group with
`sharedTokenMiddleware(os.Getenv("ACADEMY_CONTENT_API_TOKEN"))` (`content_admin.go:35`),
`academy_embedding_refresh` @ 03:00 (`worker/tasks/academy_embedding_refresh.go:10`), all five named cmd binaries,
all six named test files, `serverTenant.js:115-145` including the *"the cutover is intentional, not
reversible-on-error"* line at `:132`, the rext demo-patch
`demo-stack/patches/academy-fs-published-fallback/` present in the **pinned** clone, and the cross-file citation
`ant-academy.md:82-88` landing exactly on the passage it says states the opposite. `chronos.md` — platform
`045857c` and jobsimulation `09631fb2` subjects verbatim, `app/internal/jobsimwiring/worker.go:24-35` covering the
asynq import + `StartWorkers`. `ai-readiness.md` — `4c28365f` (2026-07-22) contained in tag **`v1.351.1`**
(`git describe --contains` → `v1.351.1~1^2`), every renamed file present under `internal/aireadiness/` and
`workforce/members.go` still holding `LoadMembers`, the demo-patch manifest `:42 path:
internal/aireadiness/readiness.go` + `:33 "v2.7 M254 RE-POINT"` + `demo-stack/tests/test_aireadiness_snapshot_
loadmembers_m254.py` (all in the **pinned** clone), `organization_settings.go:47`, `enum/ai_readiness.go:9-11`,
`scoring.go:26/:30/:46-56/:63-66` (bands **None 0-24 / Low 25-50 / Medium 51-74 / High 75-100** and the exact-tie
→ Explorer branch), `readiness.go:139/:289/:290-297/:307-312/:314/:330/:388/:390/:684/:710/:730/:817/:825`,
`steps.go:316/:337/:922/:962/:964/:969-974`, `how_we_measure.go:253-261/:285-287/:1077`,
`useAiReadinessActive.ts:22` (**sole** consumer — repo-wide grep returns only the constant, its import, a doc
comment and the platform KB page), `aiReadiness.constants.ts:26`, `AIReadinessHero.tsx:88`,
`useAIReadiness.ts:48-62` (`deriveMode` — note the bare filename is ambiguous between two real files; the
components-side one resolves), `hooks/useAIReadiness.ts:323-330` with `interviewQuestions` at `:326`,
`useWorkforceAIReadiness.ts:23-27` (no `cycle` param, no `/cycles` call), `WorkforceNewClient.tsx:125-151` (5 tabs,
readiness absent), `/enterprise/workforce/ai-readiness` route directory **gone** from the tree,
`e2e/specs/web.ai-readiness.spec.ts` present, `ai_readiness_sim.go:52` (`PLACEHOLDER-{slug}`),
`ai_readiness_cycles` partial unique index `WHERE status = 'active'` (`20260618160827.sql:18`),
GPT-5-Mini (`narrative.go:39`/`:85`), `messenger/pkg/aireadinessemail/`, `app/knowledge/ai-readiness/overview.md`,
and **0** rows of `ai_readiness_recommendations` written by any seeder in the pinned rext clone.

`TEMPLATE.md` — read in full; it is a placeholder skeleton. Its `docker compose up -d [service_name]` is a
metasyntactic placeholder, not a runnable retired token, so `§5 rule 30`'s "do not spell a dead command in
runnable form" does not bite. **No findings.**

## What I could not settle

- **GitHub repository metadata.** `README.md:79` and `chronos.md:3` assert *"The GitHub repo is NOT archived
  (last push 2026-04-23)"*, and `README.md:52` asserts graphql-wundergraph's *"repo ARCHIVED"*. Neither is
  derivable from any clone — archive state and `pushed_at` live only in the GitHub API. `gh` is **not installed**
  on this host (`command not found: gh`) and no network fetch tool was available to me. The corpus is at least
  **self-consistent** here: `platform-migration-status.md:99` states the same chronos fact in the same words, and
  `:96` the same graphql-wundergraph one (*"ARCHIVED on GitHub 2026-07-30"*). Settled by
  `gh api repos/anthropos-work/{chronos,graphql-wundergraph} --jq '{archived,pushed_at}'`.
- **The whole detail body of `chronos.md` (`:13-245`).** Ports 8080/8081, the `timers` DDL, the four timer states,
  the `ChronosService` RPC signatures, the `EventTimerWentOff` proto, the triggerer's `LIMIT 10` /
  `FOR UPDATE SKIP LOCKED` / 3-second timeout / 1-minute Sentry heartbeat, the six env vars and two CLI flags —
  the `chronos` repo is in **no** clone set and in no `repos.yml` at any platform ref I can reach, so none of it
  has ground truth here. Everything I *could* cross-check (the two removal commits, the Asynq destination) holds.
  Settled by cloning `anthropos-work/chronos`.
- **Live-stack measurements**, none re-derivable from source: `ai-readiness.md:390`/`:494` *2.09 s / HTTP 200 /
  304 KB*, `:486` *24 ms*, `:505`/`:509` *180 s → 19 ms*, `:637` *5 → 345 validation_attempt_results, +897
  skill-results, 787 evidences*, `:658` *8/8 sections cold on billion*, and `cms.md:106` *96 all-403 Directus
  lines in `backend`'s log*. Settled only by re-running the named harnesses on a live stack; the doc itself says
  *"Re-measure before relying on either number"* (`:495-496`), which is the right instruction.
- **`ai-readiness.md:7`** *"ships in `app` v1.315+ (backend) + `next-web-app` v2.89.0+ (UI)"* — I did not walk
  either tag ladder to find the first release containing the feature. Settled by
  `git log --oneline v1.314.0..v1.315.0` in `app` and the equivalent in `next-web-app`.
- **`README.md:45-46`** — customerio-sync's *"one-directional Postgres `public` → Brevo"* is corroborated from
  `app`'s side (`internal/customeriosync/sync.go:9` imports `brevo "github.com/getbrevo/brevo-go/lib"`,
  `:18-20` caps at Brevo's rate budget), but its **standalone repo has never been in any clone set**, so the
  *"unique build-straight-from-a-GitHub-URL compose pattern"* and its prod fate are asserted from one side only —
  exactly the limit `platform-migration-status.md:101` records. Same for db-backup's *"every 6 h to S3, Azure,
  Hetzner"*: production-only, no clone, unverifiable here.
- **`cms.md:195`'s *"the standalone receiver at `:8090/webhooks/` was unauthenticated"*.** I confirmed the mount
  (`cms/cmd/root.go:242`, `httpServer.Handler.Handle("/webhooks/", …)`) and that `8090` was the compose-supplied
  `PORT` at `2adcf71`, but I did not trace whether any middleware wrapped that particular `httpServer` handler
  chain, so the *unauthenticated* half is unconfirmed rather than confirmed. Settled by reading
  `cms/cmd/root.go`'s server construction in full.
