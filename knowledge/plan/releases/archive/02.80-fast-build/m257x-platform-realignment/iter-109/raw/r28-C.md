# Seat C — reading #28

**Ground truth re-derived at this reading's open** (`git rev-parse` over every `.git` under `stack-demo`,
plus the authoring rext copy). All shas matched the briefing ADDENDUM exactly, including the four clones
whose checkout is behind `origin/main`:

| clone | HEAD | origin/main |
|---|---|---|
| platform | `0c91421d` | `0c91421d` |
| app | `ad9f3c49` | `ad9f3c49` |
| next-web-app | `8297c684` | `f97ba659` |
| sentinel | `f2c46190` | `f2c46190` |
| studio-desk | `41ee3575` | `41ee3575` |
| ant-academy | `22df69dd` | `22df69dd` |
| storage | `4ce8ece5` | `9f8cb532` |
| messenger | `fa47850d` | `e9421c68` |
| cms | `ca50c817` | `f38c0c4a` |
| jobsimulation | `462343b0` | `82cb66ec` |
| roadrunner | `87d8d443` | `87d8d443` |
| graphql-wundergraph | `60c229f3` | `60c229f3` |
| app/studio, cms/studio (nested) | `aeec036a` | `aeec036a` |
| rosetta-extensions (per-stack, pinned) | `09d06070` | — |
| rosetta-extensions (authoring) | `680e8529` | — |

**Trees I read, named per rule 44/45/50:** platform-source claims graded at the ref the claim itself pins,
else at the checkout, else (for the four behind-clones whose cells pin no ref) I state the ref used in the
finding. rext claims about *what a stack runs* graded at the pinned clone `09d06070`; rext claims about the
alignment harness / DNA configuration graded at the authoring copy `680e8529` (rule 50 — a verdict is settled
by the tree its configuration lives in). I ran **`platform_alignment_guard.py` from the authoring copy**
(`/Users/marco/workspace/anthropos/rosetta/.agentspace/rosetta-extensions` @ `680e8529`, working tree clean):

```
fence-tree: .../.agentspace/rosetta-extensions @ 680e8529f · describe=fast-build-m257x-iter-101-6-g680e852
platform_alignment_guard: assertion F resolved 96 citation(s) — 25 subject-checked, 70 range-only,
  1 outside any service block; 0 unresolvable; 0 read from the WORKTREE (no ref resolved)
platform_alignment_guard: OK — platform-migration-status.md and repos.yml agree in both directions.
EXIT=0
```
Those four numbers are **identical** to the ones `platform-migration-status.md` §4 prints in its own recipe
block. Read that green as "green over its reach" (rule 46): F proves a citation *resolves and is in range*;
it never asks whether the line *says the thing*, which is what I checked by hand below.

## Positive controls

| file | `wc -l` | read in full? |
|---|---|---|
| `corpus/architecture/alignment_testing.md` | 542 | yes |
| `corpus/architecture/platform-migration-status.md` | 330 | yes |
| `corpus/services/jobsimulation.md` | 312 | yes |
| `corpus/services/customerio-sync.md` | 165 | yes |
| `corpus/services/askengine.md` | 147 | yes |
| `corpus/services/intelligence.md` | 18 | yes |
| **total** | **1514** | |

(Also read in full as required by the briefing: `corpus/ops/platform-alignment.md` §5, lines 249–1662.)

---

## Blockers

### B1 — a cited platform path that exists in no clone: `cms/directus/collections/jobsimulation.go`

- **Anchor:** `corpus/services/jobsimulation.md:160` —
  > *"Engine choice is per sequence, from the CMS `voice_engine` field, not from a flag; **when that field is
  > nil the default is `gptrealtime`** (`cms/directus/collections/jobsimulation.go:1594-1597`), not
  > ElevenLabs"*

- **Why it is false:** there is no `directus/` directory at the root of the `cms` repo.
  `git -C stack-demo/cms ls-tree --name-only ca50c817` returns
  `.agentspace .github .gitignore CHANGELOG.md CLAUDE.md Dockerfile Dockerfile.dev Makefile atlas.hcl cmd
  cog.toml ent go.mod go.sum internal knowledge main.go singularity-manifest.md terraform tools.go` — and
  `git -C stack-demo/cms ls-files | grep -c '^directus/'` returns **0**. The file the claim means is
  `cms/internal/directus/collections/jobsimulation.go` (checkout `ca50c817`) or its folded twin
  `app/internal/cms/directus/collections/jobsimulation.go` (@ `ad9f3c49`). Both carry
  `func voiceEngineFromDirectus(...)` at `:1594` with `return simulation.SimulationVoiceEngineGptrealtime`
  at `:1596` for the nil case, so the *substance* and the *line range* are right — only the path is not
  resolvable. Every other corpus site spells it correctly:
  `ai_architecture.md:56`, `:211`, `:240`, `:253` use `app/internal/cms/directus/collections/…` and
  `snapshot-spec.md:565`, `:665` use `cms/internal/directus/collections/…`; this line is the sole outlier
  across the tree (`grep -rn 'directus/collections/jobsimulation.go' corpus/`).
- **Why I hesitated:** the anchor's line numbers resolve, the fact asserted is true, and both candidate real
  paths differ from the quoted one by exactly one segment. I book it because the frozen instrument lists
  "wrong file paths" as a BLOCKER example verbatim, and because a reader who tries the path gets *no such
  file* rather than a redirect — which is the failure the corpus's own anchor fences exist to catch (this
  one is out of `platform_alignment_guard` F's reach, since F only subject-checks `docker-compose.yml`).
- **Confidence:** medium

---

## Minors

- `corpus/services/jobsimulation.md:25-27` — three consecutive lines inside the top banner lose their `>`
  blockquote prefix (*"2026-07-31; `origin/main` then landed…"* through *"State: **frozen legacy repo,"*),
  resuming at `:28`. It still renders as one blockquote by markdown lazy-continuation, so there is no
  factual consequence — formatting damage only.
- `corpus/services/jobsimulation.md:81` — *"is **owned by the CMS service**"* (bold). There is no CMS
  service; the same sentence corrects itself eleven words later (*"since the merge this is an in-process
  call into the folded cms domain"*), so the reader is not left wrong, but the emphasised noun phrase names
  a decommissioned container.
- `corpus/services/jobsimulation.md:255` — *"`backend` is the only *application* container the `core`
  profile starts (`sentinel` is authz-only)"*. `core` also starts **`gotenberg`**
  (`docker-compose.yml:170`, `profiles: [core, backend, all]` at `:183` @ `0c91421`) — which this same file
  states at `:146` and which `platform-migration-status.md:105` grades `live-standalone` on a fresh local
  stack. The parenthetical excuses `sentinel` and not `gotenberg`. I kept it MINOR because "application"
  is doing real work (gotenberg is a third-party utility image) and the passage's conclusion — *"the whole
  platform goes with it"* — is correct either way.
- `corpus/services/customerio-sync.md:44` vs `:96-97` — `:44` asserts *"The standalone published 8080
  (HTTP — health/metrics)"* as fact; `:96-97` says *"which most likely served health/metrics (never
  verified)"*. Same file, two confidence levels for one claim. (The `8080:8080` publish itself is real:
  `0dab54d:docker-compose.yml`, deleted at `838d907`.)
- `corpus/architecture/alignment_testing.md:3` — **`Last updated: 2026-06-06`** while the body documents
  M218, M219 and an explicit *"Corrected M257x iter-85"* (`:377`). The metadata line is stale by two
  releases.
- `corpus/architecture/alignment_testing.md:533-539` — the Layout block enumerates
  `cmd/alignctl · internal/dna · internal/outcome · internal/compare · internal/report · examples/toy`
  but omits **`internal/canon`**, which is present
  (`git -C .agentspace/rosetta-extensions ls-tree --name-only 680e8529 alignment/internal/` → `canon
  compare dna outcome report`) and is the package behind the doc's own *"Value comparison is canonical and
  precision-preserving"* claim at `:125`. Omitted list member.
- `corpus/architecture/platform-migration-status.md:50` — `merged-into-app` is defined as a three-way
  conjunction ending *"and the standalone is scaled to zero — **all three**"*. That third conjunct is
  literally false for two rows that carry the state: `storage`'s ECS service block is **deleted** (the same
  file's `:92` insists on the distinction — *"a sentence reading `each service_desired_count = 0` is half
  false"*), and `jobsimulation`'s `module "jobsimulation"` block is deleted outright at `6092c6d2`
  (`jobsimulation/terraform/main.tf` has 0 occurrences of `service_desired_count` at `6092c6d2` and at
  `82cb66ec`). The vocabulary in §1 (and its source, `platform-alignment.md` §6 line 1680-1682) was written
  for the fold phase and has not been widened for the teardown phase the map now documents. Booked MINOR,
  not BLOCKER, because "deleted" plainly satisfies the *intent* ("the standalone takes no traffic") and the
  rows themselves state the stronger fact.
- `corpus/architecture/platform-migration-status.md:92` (storage row) — *"This map and its neighbours cited
  line 38 of that file for `service_desired_count = 0` … **It was true up to `9f8cb53`^** and is now past
  EOF in an 18-line file."* At `9f8cb53^` (= `4a1cbf1`) `storage/terraform/main.tf` is **already the
  18-line asset-only module with no `service_desired_count` at all**. The line-38 citation last held at
  `8e558ce` (119 lines, `service_desired_count = 0` at `:38`) and died at `e6fac97`
  (*"drop the ECS service, keep the assets"*, 21 lines), several commits earlier. The operative conclusion
  ("not re-pointable, the construct is gone") is correct; only the boundary ref is wrong.
- `corpus/services/intelligence.md:5` — *"removed from `platform/docker-compose.yml` and `platform/repos.yml`
  in **mid-2026**"* for `fdfa189`, dated **2026-04-17** (verified: `chore: remove intelligence service from
  local dev orchestration`, `docker-compose.yml | 25 ----`, `repos.yml | 3 ---`). Relatedly the heading at
  `:12` reads *"Historical Role (pre-2026-Q2)"* for a service removed **inside** Q2. Cosmetic.

---

## Positively cleared (audited zeros)

Labelled per the briefing. **ENUMERATED** = I derived the predicate's set from source independently and
compared cardinalities. **RE-DERIVED** = I only recomputed arithmetic the document displays.

### `corpus/services/askengine.md`

- **ENUMERATED — the table registry.** `:47-48` claims *"**60** `TableDef`s, **57** with `Schema: "public"`
  and **3** with `Schema: "directus"`"*. Derived from `app/internal/askengine/registry.go` @ `ad9f3c49`:
  the file declares exactly one `TableDef` struct (`:31`) and one literal slice (`:96` `defs := []TableDef{`);
  `Name:` occurs 60×, `Schema: "public"` 57×, `Schema: "directus"` 3×, and **zero** of them are inside a
  comment (`grep -n '^\s*//.*Name:'` → empty), so no commented-out member inflates the count (§5 rule 24).
- **ENUMERATED — `registry.go:183-195`** is exactly the *"Jobsimulation (folded into public — jobsim-in-app
  M709)"* comment block that states the transition-alias/`rewriteSchemas`-until-M710 contract. The first
  aliased entry (`{Name: "jobsimulation.sessions", Schema: "public", RealName: "job_simulation_sessions"…}`)
  begins at `:196`, so the anchor names the justification, not the data — which is what the sentence claims.
- **ENUMERATED — the admin route prefix.** `:66-67` writes `POST /ask/admin/auto-rules/distill`. The
  platform's own doc comments in `internal/web/backend/ask/admin.go:77,102,129,173` say
  `/admin/ask/auto-rules/…` — **the platform's comments are the wrong ones**: the live mount is
  `e.Group("/ask", …)` at `internal/web/backend/backend.go:171` with
  `askRouter.POST("/admin/auto-rules/distill", …)` at `:185` (and `:186-188` for GET/activate/DELETE). All
  seven non-admin routes at `:175-181` match the doc line for line.
- **ENUMERATED — the four bypass vectors.** `:44` says `WrapQuery` *"defends four documented bypass
  vectors"*. `app/internal/askengine/sandbox.go:350` says **"three"** — and then lists **four** bullets
  (`:352-362`: quoted-identifier, comment-hidden target, unresolvable FROM/JOIN target, reserved CTE name),
  each with a live guard in the function body (`:376`, `:371`, `:382`, `:397`). The corpus counted the list;
  the platform comment did not. Corpus correct.
- **ENUMERATED — every scalar and every test count.** `askEngineMaxConns = 6` (`main.go:167`, applied
  `:374`, on its own pool built from the same `COPILOT_DB_CONN` DSN at `:370` — distinct from `copilotDB`
  at `:357`, which is what `customerio-sync.md` calls the *shared* pool, so the two docs agree);
  `MaxInlineRows = 200` / `MaxCellLength = 400` (`executor.go:18`, `:21`); `QueryTimeout = 10 * time.Second`
  with `SET LOCAL statement_timeout` (`executor.go:24`, `:97`); `maxAgenticIterations = 15` and
  `loopTimeout = 10 * time.Minute` (`handler.go:34`, `:41`); `DefaultModelID = "eu.anthropic.claude-sonnet-4-6"`,
  `DefaultRegion = "eu-west-1"` (`bedrock.go:25-26`); `rules.md` = **146,219 bytes** ≈ "~146 KB";
  `sandbox_test.go` **49** `func Test`, `executor_test.go` **13**, `prompt_test.go` **10**,
  `followups_test.go` **14** — all four exact.
- **ENUMERATED — the `NewBedrockClient` error surface.** `bedrock.go:161` is the function; its body has
  exactly one `return nil, err` path, `config.LoadDefaultConfig` at `:165-168`. `main.go:467-471` is the
  call site with the bare `return`; `func main()` is at `:229` and `grep '^func '` returns **no** function
  declaration between `:229` and `:467`, so the `return` really does leave `main`. `app/main.go:1295`
  (`mux := http.NewServeMux()`), `:1361` (meta HTTP server) and the Redis-subscriber block from `:1438`
  all sit after it. The log string *"bedrock client unavailable; talk-to-data disabled"* occurs **1×** in
  `app`'s Go source and **0×** in the nested `app/studio` repo at its own ref `aeec036a` (rule 44 —
  I greped the nested repo separately; `git -C stack-demo/app grep … HEAD -- studio/` is a guaranteed zero).
- Verdict: **no findings in this file.**

### `corpus/services/customerio-sync.md`

- **ENUMERATED** — `internal/customeriosync/` @ `ad9f3c49` holds exactly `doc.go · store.go · sync.go ·
  sync_query.sql · sync_test.go`, plus `cmd/customerio-resync/main.go` and
  `internal/worker/tasks/customerio_sync.go`. `doc.go:4-5` carries the *"last of the Go services to be
  folded into app"* sentence across the line break, verbatim.
- **ENUMERATED** — every `app` anchor at the file's declared ref `ad9f3c49`: `env_guards.go:62`
  (`envCustomerIOSyncEnabled = "CUSTOMERIO_SYNC_ENABLED"`), `:87` (`mustSubsystemSwitch`'s `log.Fatalf`),
  `:98-104` (`case "":` → deployed error); `main.go:284` (the *comment* pointing at
  `resolveSubsystemSwitch`), `:286` (the resolve), `:295` (the condition) / `:296` (the `log.Fatalf`),
  `:393-396` + `:395` (`customeriosync.New(logger, copilotDB, os.Getenv("BREVO_KEY"))`).
  `git ls-tree b948604f -- env_guards.go` is **empty**, exactly as `:13-14` states.
- **ENUMERATED** — `docker-compose.yml:84-92` @ `0c91421` is the messenger/customerio-sync "no variables
  here, deliberately" comment, word for word.
- **ENUMERATED** — the *"unique build pattern"* claim: `838d907`'s diff shows the deleted block with
  `context: git@github.com:anthropos-work/customerio-sync.git#main` and `profiles: [customerio-sync, all]`;
  no surviving compose service at `0c91421` uses a git-URL context (`backend` `../app`, `studio-desk`
  `../studio-desk`, `next-web-app` `../next-web-app`, `gotenberg` an image, `sentinel` `../sentinel`).
- **ENUMERATED** — the `external_services.md` cross-claim at `:151-156`: `grep -ic brevo` on that file
  returns **0** (positive control in the same pass: `grep -ic clerk` → 38), and its `##` headings are
  `Clerk · Directus · GraphQL Gateway — WunderGraph Cosmo Router · AI Providers · LiveKit · AWS Chime SDK`
  plus four non-per-service sections. Both halves true.
- Note: the HISTORICAL YAML block at `:67-81` is an *abridgement* — the real deleted block also carried
  `env_file: - .env`, `networks: - app-network` and `profiles: [customerio-sync, all]`. I did not book it;
  the block's stated purpose is the build pattern, and the profile is stated in prose at `:43`.

### `corpus/services/jobsimulation.md`

- **ENUMERATED — the M810 terraform state.** `jobsimulation/terraform/main.tf` @ `6092c6d2` (the ref the
  passage pins): `:15-22` is the *"M810 decommission — the standalone jobsimulation ECS service is gone"*
  comment enumerating ECS service / task definition / ECR repo / IAM roles / SG / Cloud Map / log group /
  alarms; `:24-40` is the *"What this module still owns"* block (livekit.tf, chime.tf, ssm.tf) ending with
  the atlas-tracker paragraph at `:38-40`. `service_desired_count` occurs **0×** in that file at `6092c6d2`
  and at `82cb66ec`. The four 2026-08-04 commits are exactly `6092c6d2`, `caf36c96` (*Merge pull request
  #439*), `1e40d184`, `82cb66ec` — no more, no fewer.
- **ENUMERATED — the cms counter-evidence.** `cms/terraform/main.tf:39` is `service_desired_count = 0` at
  **both** `ca50c817` (checkout) and `f38c0c4a` (origin/main), in a 191-line file; `6efa1d5` is
  *"chore(ci): drop build-production — the cms ECR repository is decommissioned (M810)"*, `-18` lines on
  `.github/workflows/build-production.yml`, body containing both quoted fragments. Both facts, both
  directions, as the banner says.
- **ENUMERATED — the `backend.internal.anthropos:8081` sweep.** Enumerated every git tree under
  `stack-demo` (15, including the two nested `studio` checkouts) and greped each at its own HEAD.
  **44** tracked `.tf` files across the 13 top-level repos, **0** hits; **59** `.tf` files by raw
  `find` + `xargs grep`, **0** hits (positive control: the same sweep for `service_desired_count` returns
  12 files). Total occurrences across the clone set: **6** — `app/knowledge/service-dependencies.md:52`
  (a markdown KB page, in the past tense under the heading *"There are no external callers of app's RPC
  mux left."*) plus 5 in the pinned rext clone (4 test fixtures + 1 guard test body). None terraform.
- **ENUMERATED — the 23 tables.** `app/terraform/migrations/20260722081626_jobsim_data_model.sql` has
  exactly **23** `^CREATE TABLE` statements (`sessions`, `actors`, `interactions`, `validation_*` ×6,
  `anticheat_*` ×2, `recordings`, `chime_recordings`, `code_submissions`, …). The very next migration
  `20260722104506.sql` creates `job_simulation_sessions` at `:2` and `DROP TABLE "sessions";` at `:79`.
- **ENUMERATED — the mirror drop.** `20260729133514.sql` is 64 lines: `:15-23` the two `UPDATE`
  re-points, `:36-44` the two orphan `NULL`s, `:58-61` the *"5. Drop the mirrors."* comment, `:62-63` the
  two `DROP TABLE`s. **`grep -c 'INSERT'` = 0** (case-sensitive; the single case-insensitive hit at `:64`
  is the function name `on_insert_local_jobsimulation_sessions_update_memberships`). And
  `internal/organization/intelligence.go:1700` really is `query := m.ent.JobSimulationSession.Query()`.
- **ENUMERATED — the frozen binary's cobra contract.** `Dockerfile:34` and `Dockerfile.dev:35` are both
  `ENTRYPOINT ["./application"]` with no `CMD`; `EXPOSE 8080` in both. `cmd/root.go:59` declares the root
  with `RunE` and **neither** `SilenceUsage` nor `SilenceErrors`. The four subcommands' `Use:` values are
  exactly `aggregate`, `clone-session`, `test-command`, `validate`. `cmd/root.go:77-78` are
  `cmp.Or(os.Getenv("PORT"), "8080")` / `cmp.Or(os.Getenv("RPC_PORT"), "8081")`, and the in-repo
  `CLAUDE.md:124-125` documents `:8080`/`:8081` and, at `:88`, `go run . serve`.
- **ENUMERATED — the historical `2adcf71` anchors.** `docker-compose.yml:83` (`jobsimulation:`),
  `:93-94` (`"8400:8400"` / `"8401:8401"`), `:113` (`PORT=8400`), `:119` (`RPC_PORT=8401`), `:140`
  (`profiles: [graphql, jobsimulation, all]`); `Makefile:10` `PROFILE ?= graphql`; `repos.yml:17-19`
  the jobsimulation entry with `migrations: false # legacy`. All seven exact.
- **ENUMERATED — the current compose facts @ `0c91421`.** 5 declared services + the 2-service `common.yml`
  floor = 7; `:48` `AUTHORIZATION_ADDRESS=http://sentinel:8087`; `:57` `GOTENBERG_URL`; `:59`
  `JUDGE0_BASE_URL`; `:183` gotenberg's `profiles: [core, backend, all]`; **0** occurrences of `_RPC_ADDR`
  in `docker-compose.yml` **and** in `common.yml`. `repos.yml` = 4 entries.
- **ENUMERATED — the AWS-bind landmine @ `0dab54d`.** `:88-89` the comment, `:90` `volumes:`, `:91` the
  bind, and exactly **1** `.aws/credentials` occurrence in the whole file.
- **ENUMERATED — the app-side wiring anchors.** `main.go:721` (`jobsimwiring.Wire`) / `:723` (`log.Fatalf`)
  @ `ad9f3c49`; the identical pair at `:668`/`:670` @ `9d00a313`; `:612`/`:614` @ `b948604`. `main.go:1204`
  @ `b948604` is `mux.Handle(jobsimulationv1connect.NewJobSimulationServiceHandler(...))`. `func main()` at
  `:216` @ `7177374` **and** @ `9d00a313`, `:212` @ `b948604`, `:229` @ `ad9f3c49`; `7177374` is exactly
  **38** commits behind `ad9f3c49`. `ad9f3c49:main.go:670` is `skillerAzureEndpointEu = &v`, precisely as
  the retraction at `:210-211` says. `internal/jobsimulation/ai/ai.go:90` is
  `fmt.Errorf("can't load AWS config: %w", err)`; `internal/jobsimwiring/wiring.go:147-148` is the bare
  `if err != nil { return nil, err }`. `main.go:980-982` @ `b948604` is the `DIRECTUS_BASE_ADDR` `log.Fatalf`.
  `app` contains **no** live `NewJobSimulationServiceClient` — the sole hit is a doc comment in
  `internal/messenger/adapters/doc.go:15`.
- **ENUMERATED — the PostHog flag.** `flag_use_realtime_openai` is read at `calls/livekit.go:131-135` and
  the override at `:140-144` (`agentName = "anthropos-agent"`, `agentEndpoint = "openai-hosted"`), inside
  `CreateAgentDispatch` (declared `:106`) — byte-identical in `app/internal/jobsimulation/calls/livekit.go`
  @ `ad9f3c49` and `jobsimulation/internal/calls/livekit.go` @ `462343b0`.
- **ENUMERATED — the result-render claim.** `jobsimulation/internal/graph/queries.resolvers.go:70` is
  `func (r *queryResolver) JobSimulationResult(...)`, whose v2/v3 branch calls
  `GetValidationAttemptResultBySession` — a persisted Ent read, no recompute.
- **ENUMERATED — the Makefile claims.** `MIGRATION_REPOS` derives from `migrations: true` → `app` alone;
  the `migrate` `S=` branch checks only that `../$(S)` is a **directory** and then runs
  `atlas migrate apply --env local` there, so *"forcing `S=jobsimulation` … re-materialises the dead
  schema"* is exactly right on a box that still has the clone. `make dev S=<x>` runs
  `-$(COMPOSE) stop $(x)` with the error-ignoring `-`, so *"it would stop nothing and exit 0"* is right too.
- **ENUMERATED — the rext claim.** `DIRECTUS_DATA_CONSUMERS = ("cms", "backend")` in **both** twins of the
  **pinned per-stack clone** `09d06070` (`stack-core/gen_override.py:58`,
  `stack-injection/gen_injected_override.py:86`), which is the tree that settles it (rule 45).
- **ENUMERATED** — `internal/runner/runner.go`'s package doc really does read *"(formerly the standalone
  \"roadrunner\" service)"*; worker pools are `Concurrency: 10` (`worker.go:27`) and `Concurrency: 25`
  (`:104`); `hibiken/asynq v0.26.0` in `go.mod`; `09631fb2` is the chronos-removal commit verbatim;
  migrations `20260402145459` and `20260409131539_add_summary_to_extraction_results` both exist;
  `internal/simulator/manager/interview_report_csv{,_test}.go` both exist; platform `05b4035` is
  *"add READONLY_DB_CONNECTION to jobsimulation service"*.
- **ENUMERATED** — `architecture_overview.md:335` is *"→ Connect-RPC to sentinel (the only cross-process RPC
  edge out of backend on a core stack)"* — the exact "correctly-scoped model wording" `:146` points at.

### `corpus/architecture/platform-migration-status.md`

- **ENUMERATED — §2's completeness claim, the passage that shows its own derivation.** I re-derived the SET,
  not the sum. `git log -p --follow -- repos.yml | grep '^[+-]  - name:'` → **14** distinct names, and they
  are exactly the 14 listed (app · chronos · cms · graphql-wundergraph · intelligence · jobsimulation ·
  messenger · next-web-app · roadrunner · sentinel · skiller · skillpath · storage · studio-desk), all 14
  with rows. The same command on `docker-compose.yml` with a **section-blind** two-space-key pattern → **26**
  tokens, of which the single non-service is `app-network` → **25** services, including `nats`, `web-app`,
  `chromedp`, `simulator`, `realtime`. All 25 have rows, three of them filed under the repo name exactly as
  stated (`backend`→`app`; `graphql`,`wundergraph`→`graphql-wundergraph`). `docker-compose.yml:185-186` @
  `0c91421` is `networks:` / `  app-network:`.
- **ENUMERATED — the `STORAGE_RPC_ADDR` derivation, the other self-showing one.** At `2035f9a`:
  **3** hits in `*.go`, all comments (`internal/jobsimwiring/wiring.go:101`,
  `internal/storagens/callsites_test.go:189`, `main.go:504` — the last reading *"the standalone service takes
  no traffic and STORAGE_RPC_ADDR is gone"*). At `b948604`: **15** hits, which I classified one by one —
  **7 env lookups** (`main.go` ×3 at `:446`, `:524`, `:992`; `jobsimwiring/wiring.go:115`; one each in
  `cmd/academy-asset-upload:129`, `cmd/academyImport:231`, `cmd/import:50`), of which **6** are spelled
  `os.Getenv` and the 7th uses the lowercase `getenv` helper — and **8** others: 2 doc comments (`:28`,
  `:35`), 2 error strings (`:133`, `:235`), **3** `t.Setenv` (`:41`, `:52`, `:64`) and **1** `t.Fatal`
  message (`:47`). Every clause of that cell, including the "counting all four as `t.Setenv` overstates the
  arrange side by one" aside, is exact.
- **ENUMERATED — the `app` row's ref hygiene.** `2035f9a` is 2 commits past `v1.369.0` and exactly **5**
  behind `ad9f3c49`; those five touch exactly `.claude/skills/publish/SKILL.md`, `CLAUDE.md`,
  `knowledge/deployment.md`, `terraform/main.tf`, `terraform/variables.tf` — **no Go source** — and the
  `terraform/main.tf` hunk is a single in-place rewrite of one `precondition` `error_message`, so
  `terraform/main.tf:181` (`service_desired_count = 1`) is identical at both refs. Tags: `5ba17044` =
  `v1.363.2`, `b948604` = `v1.366.0`, `9d00a313` = `v1.367.0`. All seven wiring call sites resolve at
  `2035f9a`: `:690` skiller, `:721` jobsim, `:751` skillpath, `:1153` cms, `:524` storage, `:1471`
  messenger, `:395` customerio-sync; `internal/roadrunner/` has **0** files while
  `internal/{cms,jobsimulation,skiller,skillpath,storage,messenger,customeriosync}/` all exist;
  `jobsimwiring/wiring.go:123` is `jsrunner.NewRunnerManager(getenv("JUDGE0_API_KEY"), getenv("JUDGE0_BASE_URL"))`.
- **ENUMERATED — the `roadrunner` provenance correction.** `git blame -L 19,19 87d8d443 -- terraform/main.tf`
  names **`84a4b4f1` (2025-12-15)**, which is also the commit that first added the file
  (`git log --diff-filter=A`); `e45eb61`'s only `terraform/main.tf` hunk is `@@ -8,7 +8,7 @@`, i.e. **line 11**
  alone, the module-source URL, with the *"Module contents are identical"* message; repo HEAD `87d8d44` is
  2026-06-19. The compose-service arithmetic checks too: 11 services at `d11a403^`, 8 at `d11a403`, 5 at
  `0c91421` (I subtracted the `app-network` key each time).
- **ENUMERATED — the storage row @ `9f8cb532`, the ref its own grep names.** `terraform/main.tf` is
  **18** lines; `:9-11` the *"ECS service … is GONE"* comment, `:13-16` the `prevent_destroy`-is-read-from-
  configuration rationale, `:18` the outputs sentence. `grep -i custody` over the repo at that ref → **no
  match**; `grep -E '(^|[^a-z])moved[[:space:]]*\{'` → **no match** (a naive `moved {` pattern *does* hit
  `removed { destroy = false }` in `CLAUDE.md`, which is why the corpus's phrasing needs the stricter test).
  `terraform/storage.tf:22-25` carries the M903 supersession paragraph verbatim. `d3e6d32` is
  *"docs(terraform): correct the guard header, which contradicted the shipped design"*.
- **ENUMERATED — the messenger row, graded at `messenger` `origin/main` `e9421c68` and SAID SO.** The cell
  names no messenger ref, and its anchors do **not** resolve at the checkout `fa47850d` (111 lines; `:19` is
  `service_desired_count = 1`; `:29` is `container_definitions`). At `e9421c68` they resolve exactly: 121
  lines, `:19-25` the scale-to-zero rationale comment, `:27-28` the rollback sentence, `:29`
  `service_desired_count = 0`. I did **not** book this: the corpus's own citation resolvers use
  `CITE_REF=auto` (origin/main first, §5 rule 41), the guard I ran resolved all 96 citations cleanly, and the
  claim is true of the world. Recording the ref rather than the verdict, per rule 46's second bullet.
  `messenger/cmd/root.go:120-140` @ the checkout does read all four `*_RPC_ADDR` values.
- **ENUMERATED — the compose anchors @ `0c91421`.** `:1-2` include/common.yml, `:5` sentinel, `:18`
  `search_path=sentinel`, `:28` backend, `:57`, `:59`, `:73-75`, `:82`, `:83`, `:102-103`, `:112`, `:119`,
  `:135`, `:141`, `:143`, `:151`, `:160`, `:168`, `:170-171`, `:183` — every one lands on the construct
  named. `common.yml:2` postgresql, `:22` `start_period: 120s`, `:24` redis. `docker-compose.yml` is
  **186** lines at `0c91421` and **271** at `0dab54d`; `838d907` is **+11/−96** on it. `repos.yml` is
  **28** lines with **4** entries; **9** entries at `d11a403^`; **6** at `0dab54d`; `repos.yml:18-23` @
  `ef32d4c` is storage+messenger both `migrations: false`, exactly as §5 row 1 says. `repos.yml:9-10` @
  `0dab54d` is the *"prod rollback path lives in infrastructure's services.tf until M810"* sentence.
- **ENUMERATED — the supergraph row.** `graphql-wundergraph` @ `60c229f3`: `terraform/main.tf:20` = `1`;
  `supergraph-config-prod.yaml` lists `backend` **alone**; `schemas/` holds `backend.graphqls` alone;
  `subgraphs.conf` is the single line `BACKEND=v1.360.0`; `915da06` is *"feat(gql): fold cms subgraph into
  backend (supergraph 2→1) — cms-in-app v8.0 (#293)"*, 2026-07-29. (The row does **not** restate the
  commit's own "2→1" figure, which `CLAUDE.md` flags as wrong — correctly avoided.)
- **ENUMERATED — every commit sha and date in §2/§5**: `a2a3ee6` 2026-02-27, `cb6ebf5` 2023-04-30,
  `ef4b449` 2024-09-02, `1474b1f` 2023-12-01, `84862d1` 2024-05-29 (*"replace simulator with
  jobsimulations"*), `b43b99a` 2026-02-26, `c17cc9a` 2026-04-15, `467965a` 2023-09-11, `8770fe6` 2023-05-04,
  `6060315` 2026-08-03, `21429b7` 2026-07-07, `a4db680` 2026-07-21, `236771f` 2026-07-29, `b56d731` +
  `360efd4` 2026-07-31, `045857c` 2026-04-17, `fdfa189` 2026-04-17. Sixteen for sixteen.
- **ENUMERATED — §5 row 5.** `d11a403`'s message contains the quoted *"silently dropped env that app still
  reads in-process"* and lists exactly `JUDGE0_BASE_URL`, `DIRECTUS_PUBLIC_BASE_ADDR`, `REDIS_WORKER_INDEX`,
  the LiveKit trio, the Chime pair, the ElevenLabs pair and the `$HOME/.aws/credentials` mount. `838d907`'s
  message does carry the *Kept* paragraph with *"Nothing in that block addressed a deleted container"*.
- **ENUMERATED — §5 row 4's rext half, at the pinned clone `09d06070`.** `REXT_INFRA_SCHEMAS="extensions
  sentinel"` (`stack-core/lib/repos_yml.sh:92`) + the `repos.yml`-declared `public` = the stated
  schema-create set; `REXT_TRANSITIONAL_SCHEMAS=""` (`:127`) — transitional debt genuinely empty.
- **ENUMERATED** — `anthropos-studio-room` really is pulled into the image by CI:
  `app/.github/workflows/build-production.yml:29` `additional_repo: "anthropos-studio-room:studio"`, and
  `app/internal/cms/studio/` holds the subprocess manager (`studioManager.go`, `subprocess_env_test.go`).
- **RE-DERIVED** — §4's coverage figures `25 subject-checked / 70 range-only / 1 outside / 96 total`; I did
  not merely re-add them, I re-ran the guard and it printed them (see the header).

### `corpus/architecture/alignment_testing.md`

- **ENUMERATED — the five DNAs' gene and capability counts**, parsed from the JSON at the authoring copy
  `680e8529`: `clerk-2.6.0` 14 caps / 27 genes; `clerk-js-5` 6 / 9; `clerk-multi-1` 5 / 9;
  `clerk-deploy-1` 3 / 7; `clerk-express-1` 5 / 13. All five match the doc's table exactly. The golden
  directories are the independent second witness and agree file-for-file: `golden` 27, `golden-js` 9,
  `golden-multi` 9, `golden-deploy` 7, `golden-express` 13, laid out as
  `golden/<Capability>/<variant>.json`.
- **ENUMERATED — "only `clerk-2.6.0` declares a `consumed_surface`."** Parsed all five: `clerk-2.6.0` has a
  15-entry `consumed_surface`; the other four have **none**. Exactly as `:208-211` states.
- **RE-DERIVED — the 97.2%.** Summing gene weights over `clerk-2.6.0` with the documented `3/2/1`
  criticality defaults gives **71**; `MembershipOrgIdentity/real-org-eid` is a `standard` gene (weight 2);
  `(71−2)/71 = 97.18% → 97.2%`, and 26 of 27 genes. Both figures in the doc reproduce.
- **ENUMERATED — the M218/D15 gene set.** Exactly three genes assert the `universal-user` variant, on
  exactly the three capabilities named: `ExtractIdentity` (clerk-express-1, critical), `Me` (clerk-js-5,
  standard), `DeployIdentity` (clerk-deploy-1, critical). And `GetUser` really does carry two critical
  variants `hero-a` / `hero-b`.
- **ENUMERATED — the harness layout and its negative claim.** `alignment/` @ `680e8529` contains
  `README.md · cmd · examples · go.mod · internal` — **no `scripts/`**, as `:329-330` asserts; the scripts
  are `clerkenstein/alignment/scripts/{gate,drift-check,drift-test}.sh`. Module line is
  `module anthropos.dev/alignment`.
- **ENUMERATED — the `alignctl` reference block.** `run.go`'s flag set is exactly `--dna --runner
  --golden-dir --source(default "golden") --report --gate-overall --gate-critical`;
  `run.go:133-136` is `const ( ExitRegressed = 2; ExitUnmeasurable = 3 )` and `unmeasurable()` spans
  `:139-153`, printing the boxed *"UNMEASURABLE — the runner could not execute. THIS IS NOT A PASSING
  SCORE."* banner and the *"Do NOT record this run as a pass"* line. `dna.go:20-26` is the
  `list|diff|validate|coverage` dispatch. `run.go:38` calls `d.Validate()` **before** either runner is
  invoked, as `:198` claims.
- **ENUMERATED — the DNA contract.** `idPattern = ^[A-Za-z0-9][A-Za-z0-9_-]*$` (`dna.go:21`);
  `maxWeight = 1_000_000` with `weight must be in 1..%d` (`:269-270`); `operator=normalized requires
  non-empty normalize` (`:266-267`); the no-critical-gene rejection (`:279-281`); the four operators
  `exact/shape/normalized/error_class` (`:57`, `:64`); the golden-path escape guard
  (`internal/outcome/outcome.go:46`, `"gene id %q escapes the golden dir"`);
  `GateMet` refusing a non-zero critical threshold at `CriticalGenes == 0`
  (`internal/compare/compare.go:56-57`) with `critical_genes` in the report JSON (`:43`); and the
  weighted-overall / **unweighted**-critical split (`compare.go:101-103`).
- **ENUMERATED — the gate/coverage claims.** `gate.sh:69` is
  `"$alignctl" dna coverage --dna "$base/$dna" --if-declared`, its rationale the comment block at
  `:58-68`; defaults `RUNNER_PKG=./cmd/clerkrun` (`:33`), `DNA=dna/clerk-2.6.0.json` (`:35`),
  `ALIGN_DIR` → `$base/../../alignment` (`:30`), gates 95/100 (`:31-32`). `dnaCoverage` exits 2 on an
  undeclared surface unless `--if-declared`, and **always** on a declared hole, printing the
  `NO COVERAGE CLAIM` warning — all as documented.
- **ENUMERATED — the inert workflow.** `clerkenstein/.github/workflows/alignment.yml` exists and is
  git-tracked; `git ls-files '.github/workflows/*'` at the monorepo **root** returns nothing; and the file's
  own `:10-11` reads *"as a subdir workflow under clerkenstein/.github (not at the monorepo root), this is
  currently inert in the rosetta-extensions monorepo; it is kept illustrative of the per-mirror gate
  shape."* The doc's two-step retraction is correct in its final form.
- **ENUMERATED — the toy.** `examples/toy/dna.json` has `Add` (critical, 3 variants) + `Greet` (standard,
  3 variants) → weights 9+6 = 15; one failing weight-2 gene ⇒ 13/15 = **86.7%**, 5/6 genes, critical 100%.
  `golden/Greet/padded-name.json` is `"Hello, El Nino!"`; `mirror/mirror.go:29` returns
  `"Hello, " + name + "!"` on the unnormalised `"  El  Nino  "`, i.e. `"Hello,   El  Nino  !"` — the exact
  two strings the doc's sample output prints. `alignment_test.go:21-22` sets the toy gate to 80/100.
- **ENUMERATED — the data/snapshot dimensions.** `stack-seeding/dna/snapshot.go:62` is
  `var validSnapshotOperators = map[string]bool{…}` holding exactly the **six** named operators.
  `data-dna.json:410` is the `taxonomy` surface's operator list — **5**, all but
  `snapshot-cross-surface-closure`; `:439` is `content`'s — **5**, no `embedding-dim`, with
  `snapshot-cross-surface-closure` in its place. The catalog holds **10** surfaces: 4 `seeded-m7a`,
  4 `seeded-m7c`, 1 `snapshot-seeded-m9b`, 1 `snapshot-seeded-m10` — **nothing `waived`**, which is the
  "100% coverage with nothing left waived" claim, and 10−4 = "~6 planned" after M7a.

### `corpus/services/intelligence.md`

- **ENUMERATED** — platform `fdfa189` is *"chore: remove intelligence service from local dev
  orchestration"*, `docker-compose.yml | 25 -------` and `repos.yml | 3 ---`, i.e. both files, one commit.
  `intelligence` appears in the historical `repos.yml` name set (it is one of the 14) and has a row in the
  map, and it is in neither `repos.yml` nor `docker-compose.yml` at `0c91421`. Only the date phrasing is
  loose (booked MINOR).

---

## What I could not settle

- **Every GitHub-org-API fact.** `platform-migration-status.md` §3's *"93 repositories … of which 4 are in
  `repos.yml` and 46 are named by no corpus document"*, the 19 census rows' `last push` dates, and every
  **archive-state** assertion in §2 (`skiller` ARCHIVED 2026-07-01, `skillpath` 2026-07-31,
  `graphql-wundergraph` 2026-07-30, `intelligence` 2026-04-02, `chronos` **not** archived / last push
  2026-04-23, `cms`/`roadrunner`/`storage`/`messenger` *not* archived). Archive state lives in the org API
  and never in the git objects, exactly as the `jobsimulation` row itself says. Settled only by
  `gh api repos/anthropos-work/<name> --jq .archived` with network access, which I did not have and which
  a read-only audit should not assume. I verified the **4** and the derivable half of the `46` claim's
  denominator (`repos.yml` @ `0c91421` = 4 entries, 28 lines) and left the rest open.
- **Anything in `infrastructure`.** Both the `cms` and `customerio-sync` prod-teardown questions terminate
  in `infrastructure/terraform/production/services.tf`, which has never been in a clone set. The corpus
  says *report both, assert neither* in three places and I agree — but I cannot confirm the underlying
  disposition either way, so the `merged-into-app` prod grade for `customerio-sync` rests on `app`-side
  evidence alone (which the row discloses).
- **`platform-migration-status.md:126`'s claim that the 93 "independently reproduc[es] iter-01's count".**
  Same network dependency; and per §5 rule 49 a repo census is a concurrently-mutable surface, so even a
  later count of mine would not refute the recorded one.
- **The `db-backup` row's `—` in the local column.** §4's assertion C says every state is one of the eight
  in §1, and `—` is not among them. The guard passes, so C evidently tolerates it — but the *doc's* wording
  of C does not say so. Settling this needs a read of `platform_alignment_guard`'s C implementation against
  the doc's phrasing, which is a fence-wording question rather than a corpus-fidelity one; I flag it rather
  than book it.
- **`alignment_testing.md:449` — the M7b live proof** (*"against the M7a-seeded `demo-1`, `measure` reports
  100% / Critical 100% … and `diff` flags an injected column"*). That is a measurement taken on a running
  stack; no stack was up during this reading and the artifacts are not in either rext tree. Would be
  settled by a `datadna measure`/`diff` run against a seeded stack, or by a checked-in receipt.
- **`customerio-sync.md:55` — "identical for 7,392 of 7,397 rows".** Reproduced verbatim from
  `app/internal/customeriosync/doc.go:29-31`, so the corpus faithfully quotes the platform; the underlying
  row comparison was run against production and is not re-derivable from source.
