# Seat E — reading #28

**Ground truth re-verified at this seat's open** (all shas matched the briefing ADDENDUM exactly; no clone
moved during the read — re-checked at close):
`platform 0c91421d` (= origin/main) · `app ad9f3c49` (= origin/main) · `app/studio aeec036a` (own checkout) ·
`cms ca50c817` (origin/main `f38c0c4a`) · `cms/studio aeec036a` · `graphql-wundergraph 60c229f3` ·
`jobsimulation 462343b0` (origin/main `82cb66ec`) · `messenger fa47850d` (origin/main `e9421c68`) ·
`next-web-app 8297c684` (origin/main `f97ba659`) · `roadrunner 87d8d443` · `sentinel f2c46190` ·
`storage 4ce8ece5` (origin/main `9f8cb532`) · `studio-desk 41ee3575` · `ant-academy 22df69dd` ·
`stack-demo/rosetta-extensions 09d06070` (pinned consumption clone) ·
`.agentspace/rosetta-extensions 680e8529` (authoring copy).

**Which tree settled what:** every platform claim was graded at the ref the claim itself named
(`b948604f` where pinned, `ad9f3c49` / checkout otherwise). The one `rosetta-extensions` claim I had to
settle (`backend.md`'s six-occurrence derivation) names `09d06070` explicitly and I read the **pinned
per-stack clone**, per §5 rule 45. The two nested `studio` repos were grepped **at their own ref**
(`aeec036a`), never through the host — `git -C app grep … -- studio/` returns 0 for everything and I
confirmed that (it does).

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/services/studio-room.md` | 509 | yes |
| `corpus/services/backend.md` | 379 | yes |
| `corpus/services/graphql-wundergraph.md` | 280 | yes |
| `corpus/services/roadrunner.md` | 206 | yes |
| `corpus/architecture/dependency_map.md` | 103 | yes |
| `corpus/services/skiller.md` | 76 | yes |
| **total** | **1553** | — |

Search-pipeline positive controls run in the same pass (§5 rule 2): `JUDGE0_BASE_URL` in `app` Go →
non-zero (against the three roadrunner zeros); `service_discovery_namespace_id` → 25 `.tf` files (against
the `backend.internal.anthropos` zero); `storage` → 56 lines in `storage.md` (against the six-term zero);
`grep -ci mistral` at `app/studio aeec036a` → 22 (against the bare-`grep` 1). No search's stderr was
swallowed.

---

## Blockers

### B1 — `backend.md`'s Migrations section is now incomplete: `app` grew a **SECOND** Atlas pipeline, undocumented, at the very ref the paragraph names

- **Anchor:** `corpus/services/backend.md:360` — *"Versioned Atlas migrations live in `terraform/migrations/`
  (per `atlas.hcl`: `dir = "file://terraform/migrations"`, source `ent://internal/data/ent/schema`).
  **There is no top-level `migrations/` dir** …"* — and the whole `### Migrations` recipe above it
  (`:355-358`: `make migrate S=app`; `:360`: `atlas migrate diff --env local` / `atlas migrate apply --env local`).
- **Why it is false:** at `app` `ad9f3c49` — the ref this same paragraph explicitly grades against
  (*"Measured 2026-08-06 … **170** migrations at `app` `ad9f3c49`"*) — `atlas.hcl` declares **two** envs, not
  one. `68272003f` (2026-08-04, *"feat(atlas): second Atlas pipeline for the sentinel schema (M1001)"*)
  added `env "sentinel"` with its **own** `dir = "file://terraform/migrations-sentinel"`, its own
  `revisions_schema = "sentinel"`, its own `src = "file://terraform/sentinel/schema.sql"` and its own
  `sentinel_url` variable. The directory exists and is non-empty:
  `app/terraform/migrations-sentinel/20260804151548_adopt_casbin_rules.sql` + `atlas.sum`
  (`git ls-tree ad9f3c49 terraform/migrations-sentinel/`; **0 entries at `b948604f`**, so this is net-new
  since the demo pin). `app/terraform/sentinel/schema.sql` is its source of truth. The atlas.hcl comment is
  emphatic that this is a deliberate second pipeline (*"app takes custody of the `sentinel` schema … a
  SEPARATE env with its OWN source, migration directory, atlas.sum and revisions table"*).
  The consequence is operational, not cosmetic: `platform/Makefile:87` and `:95` @ `0c91421` run
  **`atlas migrate apply --env local` only**, so `make migrate` — the command this section documents —
  does **not** apply `migrations-sentinel`. A reader following `backend.md`'s Migrations section end-to-end
  gets a DB with the second pipeline unapplied and nothing anywhere says so. The section's own
  *"**There is no top-level `migrations/` dir**"* shows it intends to be exhaustive about where app's
  migrations live; it now is not. (Related: `app/internal/sentineldsn/` is net-new too and unmentioned.)
- **Why I hesitated:** the quoted sentence is *true of `env "local"`*, which its parenthetical quotes
  verbatim — so this is an omission rather than a falsehood, and the canonical grading rule carves omitted
  list members out as MINOR. I booked it BLOCKER because (a) the paragraph is written as an exhaustive
  account of app's migration locations and pins itself to the exact ref where the second location exists,
  and (b) acting on it produces a wrong result, which is the briefing's materiality test.
- **Confidence:** medium

### B2 — `dependency_map.md` states a **variable** count that is a **line** count: storage's compose env block has 7 variables, not 8

- **Anchor:** `corpus/architecture/dependency_map.md:19` (the struck-through `Storage` row) — *"While the
  service existed it declared `depends_on: redis, postgresql` (both `service_healthy`) and **read
  neither**: no `DB_CONNECTION`/`REDIS_ADDR` among the eight variables in its compose env. Measured in the
  platform's own compose at `0dab54d` — `docker-compose.yml:126-131` for the `depends_on`, `:116-123` for
  the env block."*
- **Why it is false:** at `platform 0dab54d`, `docker-compose.yml:116` is the `environment:` **key** and
  `:117-123` are the entries — `AWS_DEFAULT_REGION`, `AWS_REGION`, `ENVIRONMENT`, `PORT`, `RPC_PORT`,
  `SERVICE_NAME`, `STORAGE_S3_PUBLIC_BUCKET`. That is **seven** variables over **eight** lines. The `8`
  is the cardinality of the *cited line range*, not of the set the sentence claims to range over — §5
  rule 24's exact failure (re-derive the SET, not the sum). The `depends_on` half (`:126-131`,
  `redis` + `postgresql`, both `service_healthy`) and the *predicate* half (neither `DB_CONNECTION` nor
  `REDIS_ADDR` appears) both verify exactly; only the cardinality is wrong.
- **Why I hesitated:** the predicate the count supports is true, and the row describes a service deleted
  two commits ago, so nothing a reader does turns on 7-vs-8. Booked BLOCKER because the briefing lists
  "wrong counts" as a blocker class outright, and because the number is presented as a *measurement* with
  its derivation shown.
- **Confidence:** high (that the count is false); low on materiality

### B3 — root `CLAUDE.md` still says the `ai` module is imported and pulled at Docker build; `dependency_map.md` (mine) and `shared_libraries.md` say the opposite, and the platform agrees with them

- **Anchor:** `CLAUDE.md:248` — *"**Shared Libraries** — five repos, of which **four are imported as private
  Go modules** (`ai`, `colony`, `proto`, `taxonomy`; **not** cloned by `make init`/`repos.yml` — pulled at
  Docker build via `GH_PAT`/`GOPRIVATE`)."*
  Contradicted by my own assigned file at `corpus/architecture/dependency_map.md:48` — *"**ai** | **No repo
  a stack builds** — `app/go.mod` required it up to `b948604f` and `1e457fa70` removed the requirement"* —
  and by `corpus/architecture/shared_libraries.md:126`, same verdict.
- **Why it is false:** measured at each clone's own ref. `git show ad9f3c49:go.mod` in `app` has **no**
  `github.com/anthropos-work/ai` line (it has `analytics-go`, `colony`, `proto`, `storage`, `taxonomy`);
  `1e457fa70` (2026-08-04, *"refactor(ai): fold the ai library into app as internal/ai"*) removed it, and
  `app/internal/ai/module_import_guard_test.go` is the one-way door. `sentinel/go.mod` @ `f2c46190` never
  had it. Enumerated across all seven Go repos on disk: colony 7/7, proto 7/7, taxonomy 6/7 (not
  roadrunner), **ai 2/7** — and both of those two (`cms/go.mod:9` @ `ca50c817`, `jobsimulation/go.mod:11`
  @ `462343b0`, both `v1.40.2`) are frozen repos with no compose service and no `repos.yml` entry, so
  `make init` never clones them and no Docker build ever resolves them. So at HEAD **nothing a stack
  builds pulls `ai` at Docker build via `GH_PAT`/`GOPRIVATE`** — the present-tense claim on the tree's
  highest-propagation instruction file is false, and it is the *only* surviving instance of a claim that
  two corpus documents have already corrected (§5 rule 19's corollary: the claim leaked to the edge of the
  repair's scope and stopped there).
- **Confidence:** high

---

## Minors

- `corpus/services/graphql-wundergraph.md:177` — *"they were `:220`/`:236` at `0dab54d` and `:334`/`:352`
  at `2adcf71`"*. The antecedent is the **environment-variable** pair (`:135` / `:160` at HEAD); the
  historical pairs given mix one env line with one **build-arg** line. Measured: at `0dab54d` the env pair
  is `:220` / **`:245`** (`:236` is the build arg); at `2adcf71` it is `:334` / **`:361`** (`:352` is the
  build arg). Non-material — both cited lines exist and carry the same variable and the same value, and the
  sentence's own point is *"grade the construct, not the offset."*
- `corpus/services/roadrunner.md:30-31` — *"a case-insensitive sweep of every clone at its own ref returns
  hits in app (25 files), jobsimulation (8), platform (3), studio-desk (3), next-web-app (1)"*. Re-derived
  per clone: those five figures are **exact**, but the sweep as stated ("every clone") also hits
  `stack-demo/rosetta-extensions` (**47** files) and `stack-demo/roadrunner` itself (23). Both omissions
  are defensible under the sentence's *platform-repo* framing; flagged only because the enumeration
  advertises its own scope.
- `corpus/services/studio-room.md:460` — *"(preserving any `collaborative_*` / `asset_*` JSON already in
  the bundle)"*. `agents/simulation/postgen/exporter.py:537` also preserves `internal_*`, which is how
  `internal_localization.json` survives the wipe the sentence describes. One-third of a three-member list.
- `corpus/services/graphql-wundergraph.md:84` — *"**Ports**: **8080 → 8080**"*. Read as a host→container
  publish this never existed (compose published `5050:8080` right up to `360efd4`); read as prod's ECS
  `service_port = local.port = 8080` (`terraform/locals.tf:8`) against container 8080 it is correct, and
  the fold above the line does scope the section to production + the archived repo. Ambiguous, not wrong.

---

## Positively cleared (audited zeros)

Everything below was checked against platform source at the ref the claim names. **ENUMERATED** means I
derived the predicate's set independently from source and compared cardinalities; **RE-DERIVED** means I
only re-computed arithmetic the document itself displays (the weaker clearance, §5 rule 24).

### `corpus/services/skiller.md`
- **ENUMERATED** — the "44 tracked `.tf` files in the 13 `stack-demo` repos, 0 hits for
  `http://backend.internal.anthropos:8081`". I enumerated every git tree under `stack-demo` (13 top-level +
  2 nested `studio` checkouts), counted `git ls-files '*.tf'` per tree at its own HEAD (5+4+4+6+4+2+4+2+4+5+4
  = **44**, the two nested repos contributing 0), and grepped each at its own ref: **0**. Positive control
  `service_discovery_namespace_id` → 25 files.
- **ENUMERATED** — "the skiller domain now lives in `app`": `internal/rpc/skillerrpc/skiller.go` exists and
  its own header names the five externally-reached methods (`GetSkill`, `GetSkills`, `SearchSkill`,
  `MatchSkill`, `GetJobRole`) plus the 8 `CodeUnimplemented` ones; all six named Ent schema files exist
  (`skill.go`, `jobrole.go`, `skill_embeddings.go`, `job_role_embeddings.go`, `category.go`,
  `specialization.go`); `graph/schemas/skiller_taxonomy.graphqls` exists.
- **ENUMERATED** — `categoryTree`/`fullCategoryTree` "dropped, not ported": the only occurrence anywhere in
  `app` @ `ad9f3c49` is `skiller_taxonomy.graphqls:7`, a comment saying they *"stay unported — no consumers."*
- **ENUMERATED** — "8 `ContentLanguage`s": the enum at `graph/schemas/assignments.graphqls:44-53` has exactly
  eight members (english, italian, spanish, french, german, dutch, japanese, portuguese).
- **ENUMERATED** — `skill_translations` / `job_role_translations` exist as Ent schemas
  (`skill_translation.go`, `job_role_translation.go`).
- **ENUMERATED** — "`SKILLER_RPC_ADDR` is now set by nothing locally": `git grep RPC_ADDR` over the whole
  `platform` tree @ `0c91421` returns **zero** compose hits (only prose in `platform/CLAUDE.md:34-35`);
  the four that existed at `0dab54d` (`:173`, `:174`, `:176`, `:183`) were all on the `messenger` block.

### `corpus/architecture/dependency_map.md`
- **ENUMERATED** — "zero `*_RPC_ADDR` variables anywhere in compose; `AUTHORIZATION_ADDRESS=http://sentinel:8087`
  (`:48`) is the only cross-process Connect-RPC address." Read all 186 lines of `docker-compose.yml` @
  `0c91421` plus the `include:`d `common.yml`. Also confirmed the *retraction* half: `GOTENBERG_URL` at `:57`,
  `gotenberg`'s `profiles: [core, backend, all]` at `:183`, `JUDGE0_BASE_URL` at `:59`, `REDIS_ADDR` at `:66`,
  `SUPABASE_DB_CONN`/`COPILOT_DB_CONN` at `:93-94` — every one lands on the named construct. The gotenberg
  consumer at `app/internal/converter/gotenberg.go:31` is a plain `http.NewRequestWithContext(… "/forms/
  libreoffice/convert")`, i.e. HTTP and not Connect-RPC, exactly as claimed.
- **ENUMERATED** — the effective topology "**7** compose services (5 declared in `docker-compose.yml`), 4
  repo entries": declared = sentinel, backend, studio-desk, next-web-app, gotenberg (**5**); `common.yml`
  adds postgresql + redis (**7**); `repos.yml` = app, sentinel, next-web-app, studio-desk (**4**).
- **ENUMERATED** — backend's whole `depends_on` at `:101-109` is redis + postgresql + sentinel, with the
  in-line comment at `:102-103` quoted verbatim and correctly.
- **ENUMERATED** — the shared-library matrix. Read every `go.mod` at its own pinned ref across all seven Go
  repos: colony **7/7**, proto **7/7**, taxonomy **6/7** (roadrunner is the miss), ai **2/7** (cms `:9`,
  jobsimulation `:11`, both `v1.40.2`), authn **0/7** (and 0 Go-source imports of the standalone module).
  The `colony/authn` importer counts also reproduce exactly: app **129** `.go` files @ `ad9f3c49`, cms **6**,
  jobsimulation **8**. `app/go.mod:20` @ `b948604f` = taxonomy; `sentinel/go.mod:21` @ `88bc5592` = taxonomy
  indirect; `1e457fa70` is dated 2026-08-04 with the quoted subject.
- **ENUMERATED** — the `backend`-stream cell. `messenger/internal/flow/flow.go:72` is literally
  `AddSubscriber("backend", …)`; I counted the live `pubsub.EventHandler(...)` members inside it and got
  **21** (line `:83` is commented out — and `app/internal/messenger/flow/streams.go:70-73` independently
  documents "21, not 22" for the same reason). `StreamBackend = "backend"` is `streams.go:65`; `main.go:1493`
  names it; the gate is `if messengerEnabled {` at `main.go:1445`, fed from `mustSubsystemSwitch(envMessengerEnabled)`
  at `:285`, with `envMessengerEnabled = "MESSENGER_ENABLED"` at `env_guards.go:61` and the in-line
  *"ALL OF IT is behind MESSENGER_ENABLED"* at `main.go:1437`. The retracted anchor `main.go:1442` is indeed a
  comment line at the newer refs and `main.go` is **1361** lines at `b948604f`, so it cannot exist there.
- **ENUMERATED** — the `skiller`-stream cell, which is the densest citation block in my partition and is
  correct in every part. At `b948604f`: `main.go:287` `pubsub.NewPublisher(serviceName, …)`, `:637`
  `SKILLPATH_STREAM`, `:1039` `CMS_STREAM`, `wiring.go:127` `AI_USAGE_STREAM`, `:180` `JOBSIMULATION_STREAM`,
  `:1276` the `AddSubscriber(os.Getenv("SKILLER_STREAM"), roleManager.SkillerSubscriber())`. The drift claim
  is exact too — at `2035f9a4` *and* `ad9f3c49`, `:287` = `logger.Info("subsystem switches",`, `:637` = `)`,
  `:1039` = `serverContext,`, `:1276` = `apiKeyManager,`, `wiring.go:127` = the asynq-client construction,
  `:180` = blank — and the seventh, `internal/roles/roles.go:791`
  `func (r *RoleManager) SkillerSubscriber() *pubsub.Subscriber {`, is byte-identical at all three refs, so
  the **6 drift / 1 holds** split is right. `SKILLER_STREAM` @ `ad9f3c49` = **6 Go occurrences / 3 files**
  (`main.go`, `subscriber_merge_test.go`, `subscriber_wiring.go`) and **6 files** off the pathspec; no
  `NewPublisher` names it at any ref (`main.go:1537` only fills the `Skiller` field of
  `streamNamesForSubscribers`). `app` is **98** commits past `b948604f`.
- **ENUMERATED** — `messenger/internal/flow/flow.go:105` (`jobsimulation`) and `:109` (`cms`).
- **ENUMERATED** — the `customerio-sync` row: `profiles: [customerio-sync, all]` is exactly
  `0dab54d:docker-compose.yml:154`; `docker-compose.yml:84-92` @ `0c91421` carries the *"default to OFF on a
  developer machine"* rationale; the cross-reference `service_taxonomy.md:101-103` resolves to the twin
  statement and agrees with it.
- **ENUMERATED** — the withdrawn `storage.md:40,47` attribution: `storage.md` contains **0** occurrences of
  each of `depends_on`, `DB_CONNECTION`, `REDIS_ADDR`, `go.mod`, `redis`, `postgres` (positive control:
  "storage" → 56). The withdrawal is correct.
- **ENUMERATED** — the Studio-Room row's Mistral clause: `services/ai.py:705-708` is the whole provider
  registry (`openai`/`azure`/`anthropic`); `tools/pdf2md.py:24` is `from mistralai import Mistral`;
  `git -C app/studio grep -i mistral aeec036a` → **22 hits / 3 files** (`requirements.txt` 1,
  `tools/pdf2md.py` 18, `tools/r3.py` 3).
- **ENUMERATED** — studio-desk's `depends_on` is `backend` and only `backend`, at `docker-compose.yml:138-140`.
- **RE-DERIVED** — the `architecture_overview.md` cross-reference: `:335` is exactly *"→ Connect-RPC to
  sentinel (the only cross-process RPC edge out of backend on a core stack)"*, so the scoping claim is
  faithful to its cited twin.

### `corpus/services/roadrunner.md`
- **ENUMERATED** — the three-string zero. `ROADRUNNER_RPC_ADDR`, `RoadRunnerService`, `roadrunner:10401` →
  **0 Go hits** at `app b948604f`, at `app ad9f3c49`, and at `jobsimulation 462343b0`. Positive control
  `JUDGE0_BASE_URL` → non-zero at the same invocation.
- **ENUMERATED** — the retraction's replacement figures. `git grep -in roadrunner 462343b0` in
  `jobsimulation` = **14 lines / 8 files**, exact-case **5 / 3**, and Go source holds exactly one:
  `internal/runner/runner.go:3`, a comment. The five per-repo file counts (app 25, jobsim 8, platform 3,
  studio-desk 3, next-web 1) reproduce exactly.
- **ENUMERATED** — `repos.yml` history. At `2adcf71` it has **9** entries with `roadrunner` at `:29-31` and
  `jobsimulation` at `:17-19`; `d11a403`'s diff removes `- name: cms`, `- name: jobsimulation` **and**
  `- name: roadrunner` — so the doc's "grade that commit by its diff, not its message" is right, and the
  commit message really does claim the entry *"was already gone."* At `0dab54d` there are **6** entries
  (app, sentinel, storage, messenger, next-web-app, studio-desk); at `0c91421` **4**.
- **ENUMERATED** — the ports story. `cmd/root.go:110` `cmp.Or(os.Getenv("PORT"), "8080")` and `:84`
  `cmp.Or(os.Getenv("RPC_PORT"), "8081")` are the **only** two `cmp.Or` reads in that file (I enumerated
  every `os.Getenv` there: 9 reads, 2 with defaults) — so the env table's blockquote is right that only
  those two have a built-in default. At `2adcf71` compose published `10400:10400`/`10401:10401` at
  `:291-292` and set `PORT=10400` at `:298`, `RPC_PORT=10401` at `:302`, inside the `:296-302` env block.
- **ENUMERATED** — the async-task description. `internal/runner/runner.go:126` is
  `asynq.NewTask(tasks.RoadrunnerSubmissionResult, payload, asynq.Queue(queues.DefaultQueue), asynq.MaxRetry(3))`
  inside `CreateSubmission` — the **only** `Enqueue` site, and `SubmissionPackage` reaches it too
  (`rpcsrv/rpc.go:50` calls `CreateSubmission(ctx, "zip", …)`), so "exactly one poll task per submission"
  holds for both entry points. `queues.DefaultQueue = "roadrunner:default"`,
  `tasks.RoadrunnerSubmissionResult = "roadrunner:submissionresult"`, `worker.go:25` `Concurrency: 10`,
  `runner.go:244` `maxRetries = 15` with `:273` `time.Sleep(1 * time.Second)`, `:276-278` the
  `RoadrunnerSubmissionCompleted` publish. Three RPC methods, matching the table.
- **ENUMERATED** — `terraform/main.tf:19` `service_desired_count = 1`; **0** `*_test.go` files;
  `Dockerfile:18` `RUN go test -v ./...`; `go.mod` `go 1.25.0` + `asynq v0.25.1` + `gorilla/websocket`;
  `internal/lsp/lsp.go` present and referenced from nothing; no vendored `.proto`; `py` (not `python`) in
  `languages.go`; `main.go` imports `godotenv/autoload`; `cmd/root.go:68` `strconv.Atoi(os.Getenv("REDIS_WORKER_INDEX"))`.
- **ENUMERATED** — `ROADRUNNER_RPC_ADDR` absent from `.env_example` at both `0dab54d` and `0c91421`, and
  from every compose file at `0dab54d` (positive control at the same invocation: the four messenger
  `*_RPC_ADDR` lines).
- **ENUMERATED** — jobsimulation "took four commits on 2026-08-04": `462343b0..origin/main` is exactly
  4 commits, all dated 2026-08-04, headed by the M810 ECS/ECR removal `6092c6d2`.

### `corpus/services/graphql-wundergraph.md`
- **ENUMERATED** — the subgraph ladder, by counting `^  - name:` in `supergraph-config-prod.yaml` at each
  named commit: `749dc86~1` → **5** (backend, skiller, jobsimulation, cms, skillpath, in that order),
  `749dc86` → **4**, `7c17e63` → **3**, `915da06` → **1**. `git show --name-status 915da06` marks
  `schemas/cms.graphqls` and `schemas/jobsimulation.graphqls` **`D`** in one commit, and its subject really
  does read *"supergraph 2→1"*, so the "3 → 1, and the subject line is wrong" claim is exactly right.
  Dates 2026-06-24 / 2026-07-21 / 2026-07-29 all check.
- **ENUMERATED** — `schemas/` holds `backend.graphqls` **alone**; `subgraphs.conf` is a single
  `BACKEND=v1.360.0`; `supergraph-config-prod.yaml` lists `backend` alone.
- **ENUMERATED** — the subscription retraction. **Zero** `subscription:` blocks in any
  `supergraph-config-*.yaml` at HEAD; at `7c17e63` the `subscription: protocol: "sse_post"` block sat on the
  `jobsimulation` entry **alone**. `schemas/backend.graphqls` has `type Query` (`:4912`) and `type Mutation`
  (`:4053`) and **no `type Subscription`**; the six `Subscription` substring hits are exactly the
  Stripe/plan names named (`activeSubscription`, `type PlanSubscription`, `stripeSubscriptionId`).
  `bba862f` exists, is dated 2026-02-25, carries the quoted subject, and `git branch -a --contains` returns
  **only** `remotes/origin/feat/use-web-socket`.
- **ENUMERATED** — the five surviving profile tokens. I read every `profiles:` key in `docker-compose.yml`
  @ `0c91421`: `{core, backend, all, studio-desk, frontend}` = **5**; at `0dab54d` the same enumeration
  gives **8** (adding `storage-legacy`, `customerio-sync`, `messenger`), so the *"read 'the eight' until
  `838d907`"* parenthetical is right too. The always-on floor is derivable and is exactly
  `{postgresql, redis, sentinel}` — `sentinel` declares no `profiles:` key in `docker-compose.yml` and the
  other two none in `common.yml`.
- **ENUMERATED** — `grep -c 5050 docker-compose.yml` @ `0c91421` = **0**, and `:193` of this same doc does
  say `http://localhost:5050` refuses the connection, so the self-citation resolves.
- **ENUMERATED** — the compose-history paragraph, commit by commit. `63d285c` introduces the service as
  `wundergraph` from a `git@…#main` context with **no `dockerfile:` key**; `d92e84e` renames it to
  `graphql` (the diff shows `-  wundergraph:` / `+  graphql:`); `a2a3ee6` moves the context to
  `../graphql-wundergraph`, still no `dockerfile:`; `719befb` unchanged; `2c85211` (2026-02-27) adds
  `dockerfile: Dockerfile.dev`; `67ba772` moves the context to `..` with
  `dockerfile: graphql-wundergraph/Dockerfile.dev`; `360efd4` deletes the 33-line service block. Feb 27 →
  Jul 31 is the claimed "final five months." Every link in that chain verified.
- **ENUMERATED** — `ci/update-subgraph.sh` carries **exactly one** `gh release download`, at `:9`, with
  `-R anthropos-work/app`.
- **ENUMERATED** — the historical compose block at `2adcf71^1`: `ports: - "5050:8080"`, `ENVIRONMENT: compose`
  + `ENVIRONMENT_CONFIG: compose`, `ssh: ["default"]`, `env_file: .env`, and `depends_on` = backend,
  jobsimulation, cms, **storage** — matching the Environment-Variables table and the `depends_on` bullet
  at `:180` including the "storage was not a subgraph" parenthetical.
- **ENUMERATED** — pins and stubs: `Dockerfile` = `node:22.11-alpine` + `wgc@0.104.0` +
  `ghcr.io/wundergraph/cosmo/router:0.275.0`; `federation_version: =2.3.2`; `package.json` is exactly
  `{"name": "graphql-wundegraph"}` and the repo `CLAUDE.md` heading carries the same misspelling;
  `terraform/locals.tf` gives `project = "wundergraph"`, `aws_region = "eu-west-1"`, `port = 8080`, and
  `main.tf:26` `health_check_path = "/health"`, `:20` `service_desired_count = 1`; only
  `terraform/tests/` exists as test material; routing URLs `http://backend:8082/graphql/query` (compose),
  `http://host.docker.internal:8082/…` (dev), `http://backend.internal.anthropos:8080/…` (prod) all
  verbatim, and there is no `-local` variant. `playground_enabled`/`introspection_enabled` are `true` in
  compose+dev and `false` in prod.
- **ENUMERATED** — `repos.yml` entry deletion: `360efd4`'s diff removes `- name: graphql-wundergraph` /
  `type: node-npm`, so *"used to tag it `type: node-npm`, deleted at `2adcf71`"* is right.
- **ENUMERATED** — the smoke-test passage. `app/internal/authorization/gqlauthz/gqlauthz.go:186` is the
  `unknown viewer: %w` error return, and the exemption ladder immediately above it (`:175-176`) is
  `isPublicOnlyOperation || isFederationQuery || (environment == colony.Development && isIntrospectionOnlyQuery)`
  — exactly the three the doc names. `gqlauthz.go:75` states `__typename` is *not* accepted, and both named
  regression tests exist (`gqlauthz_test.go:132`, `:176`). `backend.go:315` serves the Apollo Sandbox at
  `/graphql` and `:317` the handler at `/graphql/query`; `/api/health` is in the Clerk-skip list at `:140`.
- **ENUMERATED** — `Makefile:10` reads `PROFILE ?= core` at `0c91421` and `PROFILE ?= graphql` at `2adcf71`.
- **ENUMERATED** — repo `Makefile`: `updatesubg` concatenates `subgraphs/backend/…/schemas/*` into
  `schemas/backend.graphqls`; `run` does `docker run … -p 5050:8080`; README says *"This will only work on
  aarch based machines"*; `CLAUDE.md` still has a "Version Tracking" heading (`:70`) and a
  `supergraph-config-local.yaml` reference (`:85`), both stale as claimed.

### `corpus/services/backend.md`
- **ENUMERATED** — "the mux carries **six** handlers." I read the whole registration block at both named
  refs. At `b948604f` `:1185-1228`: `:1187` Users, `:1188` Organizations, `:1196` Skiller, `:1204`
  JobSimulation, `:1212-1214` CMS under `if cmsRPCServer != nil`, `:1228` LabSession — six, five
  unconditional. At `ad9f3c49` the identical block is `:1297-1338`. Both ranges land on the construct.
- **ENUMERATED** — "**0** occurrences of `SkillPathSessionService` in Go source": 0 in `*.go` at
  `ad9f3c49`, and 0 `skillpath…v1connect` imports; the only two hits in the whole repo are
  `CLAUDE.md:109` and `knowledge/architecture.md:28` — precisely the two anchors the ⚠ note cites, and
  both are byte-identical at `2035f9a4` and `ad9f3c49` as it claims. `2035f9a4..ad9f3c49` is exactly
  **5** commits.
- **ENUMERATED** — the six-occurrence derivation of `http://backend.internal.anthropos:8081`. I enumerated
  every git tree under `stack-demo` and grepped each at its own HEAD: **6** total —
  `app:knowledge/service-dependencies.md:52` (**1**) and, in the *pinned* rext clone `09d06070`, **5**:
  `stack-core/tests/fixtures/repair_leak/{pre,post}/corpus/services/cms.md:32` and `:157` (4) +
  `stack-core/tests/test_platform_predicate_guard.py:435`. Exactly the paths and line numbers the passage
  names. Both stated mechanisms reproduce: `git grep` over the **44** tracked `.tf` files → 0, and a raw
  filesystem sweep over **59** `.tf` files → 0, with the `service_discovery_namespace_id` control at **25**.
- **ENUMERATED** — the terraform derivation: `app/terraform/locals.tf:6` `project = "backend"`, `:8`
  `rpc_port = 8081` mapped at `main.tf:185-186`, `main.tf:58` takes
  `service_discovery_namespace_id` as a variable, and the namespace *name* `internal.anthropos` appears in
  **no** `.tf` file in any clone. `app/terraform/variables.tf:197` = `cms_rpc_address` and `:230` =
  `storage_rpc_addr` at `b948604f`; at `ad9f3c49` `cms_rpc_address` has moved to `:309` and the
  `storage_rpc_addr` **variable block is gone**, replaced by `storage_s3_bucket` (`:349`) /
  `storage_s3_public_bucket` (`:354`). NB the guard I nearly tripped: `git grep storage_rpc_addr` still
  returns `variables.tf:342` — reading the surrounding lines shows it is the *comment* explaining the
  deletion, not a declaration (§5 rule 10). `messenger/terraform/variables.tf:77,82,87,92` @ `fa47850d`
  are the four undefaulted `*_rpc_addr` declarations.
- **ENUMERATED** — "`STORAGE_RPC_ADDR` has **0 read sites**; its 3 remaining occurrences are comments."
  Go-source occurrences at `ad9f3c49` are exactly three — `main.go:504`, `internal/jobsimwiring/wiring.go:101`,
  `internal/storagens/callsites_test.go:189` — and I read the surrounding lines of each: all three are
  comments, and `main.go:504` does say *"STORAGE_RPC_ADDR is gone."* `main.go:524`/`:525` are
  `internalstorage.NewManager(...)` / `NewPublicManager(...)`. `main.go` at `ad9f3c49` and `2035f9a4` are
  the **same blob** (`c0e6c4a6…`), so "byte-identical" is literally true.
- **ENUMERATED** — the five-publisher / six-subscriber shape at `ad9f3c49`: publishers at `main.go:325`
  (self-stream, `serviceName` defaulted to `"backend"` at `:230-232`), `:746` `SKILLPATH_STREAM`, `:1149`
  `CMS_STREAM`, `wiring.go:132` `AI_USAGE_STREAM`, `:185` `JOBSIMULATION_STREAM`; subscribers built in
  `subscriber_wiring.go:203-250` — `make(map[string]*pubsub.Subscriber, **6**)` — with
  `subs[d.Streams.Skiller]` at `:209` and `subs[d.Streams.Backend]` at `:248`, the quoted
  *"Backend is app's OWN self-stream …"* comment at `:112-113`, the name map at `main.go:1534-1541`, and the
  single application loop at `:1579-1581`. Six subscribers, five publishers, `skiller` consumer-only.
- **ENUMERATED** — the "three of the eight have a handler" split, and the three still-declaring husks:
  `storage@4ce8ece5:sdk/storage/v1/service.go` declares `StorageService`,
  `messenger@fa47850d:internal/rpcsrv/rpcsrv.go` declares `MessengerService`, and
  `roadrunner@87d8d443:cmd/root.go:87` registers `RoadRunnerService` — all on their own muxes, none on app's.
- **ENUMERATED** — the migration figures. `terraform/migrations/*.sql` = **170** at `ad9f3c49` and **169** at
  `b948604f`; **46** filenames sort after `20260531`; the last May migration really is
  `20260529072659_add_lab_session.sql`; the head at `ad9f3c49` is
  `20260803143844_ai_readiness_recommendation_path.sql` and at `b948604f`
  `20260731154527_academy_chapter_progress_completed_at.sql`, with `20260731131307.sql`,
  `20260729133514.sql` and `20260728103254_ai_readiness_snapshot_frozen_matched_sources.sql` immediately
  before it. `git ls-tree migrations/` is empty at all three refs and `6a46e8445` is the 2026-06-18
  atlas.sum removal. (This clearance is what surfaced B1: the numbers are right, the *set of migration
  directories* is not.)
- **ENUMERATED** — **13** `ai_readiness_*` tables, derived from `CREATE TABLE` statements across
  `terraform/migrations/` rather than from the doc's list: cycles, diagnose_narratives, email_overrides,
  live_snapshots, notification_logs, notification_optouts, recommendations, sims, skills, snapshots, steps,
  text_translations, user_step_progresses. The named "four a 9 omits" are exactly the four that make 9 → 13.
  The 30/40/30 split is stated in source at `internal/aireadiness/steps.go:64`. `internal/aireadiness/`
  exists as a top-level package with `compare.go`/`csv.go`/`cycles.go`/`diagnosis.go`, and
  `internal/workforce/` contains **0** `readi*` files.
- **ENUMERATED** — every directory in the *Key directories* block exists under `internal/` at `ad9f3c49`
  (I listed all 62 and compared), plus `internal/storage`, `internal/storagens`, `internal/publicstorage`,
  `internal/messenger`, `internal/customeriosync` from the merge table, and the five named `askengine/`
  files. All ten "moved tables" appear in `internal/data/ent/migrate/schema.go`.
- **ENUMERATED** — `main.go:743-746` @ `b948604f` is exactly the conditional
  `if labsAPIURL := os.Getenv("LABS_API_URL"); labsAPIURL != "" { … }`, so "NOT unconditionally nil" holds.
  `main.go:1353` sets `rpc.WithWriteTimeout(60*time.Second)` with the 10s-default rationale at `:1352`.
  `DIRECTUS_WEBHOOK_SECRET` is read at `main.go:1199` under an in-line *"fail-closed"* note at `:1198`, and
  `/api/webhook/directus` is Clerk-skipped at `backend.go:138`.
- **ENUMERATED** — `1fc00c78` is *"Deprecate skiller schema…"*; `c3c45e01` is v1.334.1; the two named
  prerequisite migrations (`app 20260518125439`, `cms 20250116133510`) both exist; `go.mod` says
  `go 1.26.4`; the compose profile line is `docker-compose.yml:110` at `0c91421` and was `:100` at
  `0dab54d`; ports/`META_PORT=8084`/8081-unpublished all match compose.
- **ENUMERATED** — the subgraph history it restates: `schemas/skiller.graphqls` is the sole `D` in
  `749dc86` (subject verbatim), `schemas/skillpath.graphqls` the sole `D` in `7c17e63`.

### `corpus/services/studio-room.md`
- **ENUMERATED** — "`gen.py` registers **exactly nine** arguments (`gen.py:484-492`)": I read `main()` and
  counted nine `parser.add_argument` calls occupying exactly `:484-492`, matching the documented flags
  one-for-one including `-f/--force` writing to `dest="forced"`.
- **ENUMERATED** — the `--template` warning, in full. `parse_argument` at `:18-28` calls `parse_known_args`
  and folds unknown `--key value` pairs into the dict; `validate_blueprint_exclusivity` at `:241-271`
  raises with exactly the quoted message and whitelists exactly `{blueprint, media, forced, interactive,
  branch, pipeline, attachments_path, worklog_path, postgen_path, published_path}`;
  `_LEGACY_TEMPLATE_DEFAULTS` + `translate_legacy_blueprint` at `:205-238` cover exactly the three legacy
  names and emit the quoted *"is ignored; asset type is now inferred from task interactions"* warning for
  everything else. `setup_generation_request` is `:273-282`.
- **ENUMERATED** — the Mistral callout, which is the most instrument-sensitive claim in my partition and is
  right in all three of its measurements. `git -C app/studio grep -i mistral aeec036a` → **22 hits / 3
  files** (requirements.txt **1** at line 8, `tools/pdf2md.py` **18**, `tools/r3.py` **3**);
  `tools/pdf2md.py:24` `from mistralai import Mistral`, `:96` `Mistral(api_key=api_key)`, `:127`
  `model="mistral-ocr-latest"`; `tools/r3.py:194` hard-requires `MISTRAL_API_KEY`. And I reproduced both
  ignore-rule effects live: `app/studio/.gitignore:2` is `tools/`, so this shell's `grep -ri mistral
  stack-demo/app/studio` returns **1**; `app/.gitignore:79` is `studio/*`, so `git -C app grep … -- studio/`
  returns **0**; `cms/.gitignore:129` is `studio/*` and `stack-demo/cms/studio` is the same repo at the
  same sha `aeec036a`. `services/ai.py:1-2` imports only openai/anthropic; `:705-708` is the
  `{'openai','azure','anthropic'}` registry. Go execs exactly two studio scripts —
  `studioManager.go:119` (`studio/gen.py`) and `:1045` (`studio/postgen.py`) @ `b948604f` — and
  `app/Dockerfile:45-46` copies `studio/` and pip-installs `studio/requirements.txt`.
- **ENUMERATED** — `cert.py` is the certification-benchmark CLI, not certificates/signing. Its docstring
  describes running `benchmark/certification/test-set/*.json`. On the four negative terms:
  `certificate` 0, `private_key` 0, `x509` 0 — and `signing` returns 3 *substring* hits which are
  "Designing"/"designing"/"assigning"; `git grep -w signing` is **0**. The predicate holds. (I nearly
  booked this as a false zero; the word-boundary check is what settled it.)
- **ENUMERATED** — the asynq facts: `internal/cms/worker/worker.go:29` `Concurrency: 5` with
  `Queues: {AiVideoQueue: 7, StudioQueue: 3}` at `:32-33`, and `queues.go` defines `StudioQueue = "studio"`
  / `AiVideoQueue = "ai_video"`. "Scheduling priorities, not concurrency limits" is the correct reading of
  asynq's `Queues` map.
- **ENUMERATED** — the CI embedding: `.github/workflows/build-production.yml:29`
  `additional_repo: "anthropos-studio-room:studio"` with `additional_repo_commit: latest`, and
  `Dockerfile:28` `FROM python:3.11-slim`.
- **ENUMERATED** — the output-structure claim, which is the one most likely to be hand-waved and is not.
  `gen.py:450-456` builds `work_paths` from the four `*_path` keys with the `worklog` literal fallback;
  `exporter.py:518-519` are the two zip paths, named from `source_id` falling back to `sim_id` (`:517`);
  `_create_export_package` runs `:514-550`, unpacking into a scratch `published/{id}_simulation/`, writing
  `simulation.json` (`:541`) and `internal_localization.json` (`:511`), re-archiving (`:547`) and
  `rmtree`ing (`:550`). The four post-gen target modules exist alongside `exporter.py` and a `testing.py`;
  `postgen.py:396-398` makes `--media`, `--simid`, `--target` all `required=True`.
- **ENUMERATED** — the config claims: exactly three tracked configs (`config_template.ini`,
  `development_config.ini`, `production_config.ini`), `max_tokens = 4000` in all three, the
  `{MODE}_AI_{BRANCH}_MODEL = service, model, thinking` rows verbatim (`azure, gpt-5-mini, none` ×2;
  `azure, gpt-5.4, none|low|medium`) plus the matching `*_EXPERIMENTAL_*` set, `worklog_path =
  workspace/trace`, and `.gitignore` lines 5-6 gitignoring `configs/local_*` / `configs/test_*`.
  `load_services_settings` (`gen.py:41-53`) overrides exactly the six named keys from the environment,
  confirming the "code, not configparser interpolation" statement.
- **ENUMERATED** — `GenMode` at `services/ai.py:35-42` has exactly the five members listed with
  `DEFAULT = EXECUTION`; `generation_step` at `agents/__init__.py:198-216` attaches a `gen_instruction`
  dict; `gen.py:284-289` `_run_step_func` awaits when awaitable.
- **ENUMERATED** — every key quoted from `tests/e2e/blueprints/technical.json` is present with the exact
  value shown (down to `"salt": "0329"` and the simid); the five e2e blueprints are exactly b2b, business,
  none_chat, none_voice, technical; `knowledge/development/asset-examples/blueprints/` holds exactly three
  files, one each for code / micro / scenario.
- **ENUMERATED** — the taxonomy client is `services/taxonomy.py:11` `BASE_URL =
  "https://api.anthropos.work/api"`; the three AI clients are at `ai.py:383` (OpenAI), `:530` (AzureOpenAI),
  `:664` (Anthropic); `model.py:59` `self._cache = {}` and `:467-469` the skill-id memo are the only cache
  in the pipeline. `requirements.txt` is unpinned and holds exactly the nine packages listed. The repo
  `CLAUDE.md:12-14` is the quoted entry-point block.

---

## What I could not settle

- **GitHub archive state of any repo.** `graphql-wundergraph.md:8` (*"ARCHIVED on GitHub, 2026-07-30"*),
  `roadrunner.md:38` (skiller 2026-07-01 / skillpath 2026-07-31 archived; roadrunner *not* archived),
  `roadrunner.md:46` (jobsimulation's archive state *"not visible to this corpus"*), `roadrunner.md:56-57`
  (chronos not archived, last push 2026-04-23), and `dependency_map.md:23` (router repo archived
  2026-07-30) all depend on the GitHub API. Nothing in a local clone carries an archive bit. Settled only
  by `gh api repos/anthropos-work/<name> --jq .archived` with org credentials.
- **Any production/ECS assertion.** `cms/terraform/main.tf:39` `service_desired_count = 0` and
  `roadrunner/terraform/main.tf:19` `= 1` are measurable and I measured them; whether either has been
  *applied* is decided in `anthropos-work/infrastructure`, which is in no clone set. The corpus says this
  itself and asserts nothing further, which is the right end state.
- **Prod row counts.** `backend.md:129-133` (42,790 / 22,490 / 23 / 1,447 / 43,584, measured on prod
  2026-07-08) and `skiller.md:50-54` (the 2026-06-29 snapshot manifest) need read-only prod DB access or
  the capture manifest under `.agentspace/snapshots/`. The two figures the corpus reconciles (22,490 vs
  22,470) are internally consistent as written; I could not independently confirm either.
- **Whether `make migrate` is *supposed* to run the second Atlas env (B1).** `platform/Makefile:81-95`
  demonstrably runs `--env local` only, and `app/terraform/migrations-sentinel/` demonstrably exists and is
  non-empty. Whether M1001 intends a separate operator step, a later Makefile change, or a gap is a
  question for the `app` M1001 plan, which I did not open. What is certain is that `backend.md` describes
  neither the env nor the directory.
- **`app` PR/issue numbers** cited as provenance (`skiller.md:37` "app PR #989",
  `backend.md:297` "Phase B PR 2, #896", `roadrunner.md:5` "v2.5 M231 KB-6" / "M247 ~386-commit bump").
  Not resolvable from a clone.
- **`studio-room.md`'s "app v1.360.1" and "cms-in-app v8.0 = app v1.360.0" version stamps.** Consistent with
  `subgraphs.conf`'s `BACKEND=v1.360.0`, which is corroboration rather than confirmation; the tag itself is
  not in the `graphql-wundergraph` clone.
- **`dependency_map.md:13`'s "23 jobsim run-state tables."** Repeated identically in root `CLAUDE.md`, so a
  re-derivation would be worth doing, but it needs a migrated DB or a full `CREATE TABLE` census across the
  jobsim port migrations that I did not have budget to enumerate honestly. I did not clear it, and I am not
  booking it — an unenumerated number is neither a finding nor a clearance.
- **One observation, offered as context rather than a finding:** `app` at `ad9f3c49` carries the beginnings
  of a **ninth** fold — `sentinel-in-app v10.0 / M1001` (`atlas.hcl` env "sentinel", `terraform/sentinel/
  schema.sql`, `terraform/migrations-sentinel/`, `internal/sentineldsn/`). Nothing in my six files is
  falsified by it: `sentinel` still has its own compose service (`docker-compose.yml:5-26`), its own
  `repos.yml` entry, and `AUTHORIZATION_ADDRESS` is still read at `app/main.go:313`, so
  *"eight former microservices"* and *"`backend → sentinel` is the one cross-process RPC edge"* both stand.
  It is the reason B1 exists and is likely to become a broader corpus item next cycle.
