# Seat B — reading #28

**Ground truth used** (re-verified at this reading's open, `git rev-parse --short=8 HEAD` per tree):
`app ad9f3c49` · `platform 0c91421d` · `next-web-app 8297c684` (origin/main `f97ba659`) · `sentinel f2c46190` ·
`storage 4ce8ece5` (origin/main `9f8cb532`) · `messenger fa47850d` · `cms ca50c817` (origin/main `f38c0c4a`) ·
`graphql-wundergraph 60c229f3` · `roadrunner 87d8d443` · `jobsimulation 462343b0` (origin/main `82cb66ec`) ·
`studio-desk 41ee3575` · `ant-academy 22df69dd` · nested `app/studio` + `cms/studio` both `aeec036a` ·
`stack-demo/rosetta-extensions 09d06070` (pinned per-stack, the settling tree for tooling-on-a-stack claims) ·
`.agentspace/rosetta-extensions 680e8529` (authoring copy).

**Ground-truth hazards I hit and worked around — disclosed because they could have manufactured findings:**

1. **`stack-demo/ant-academy` has a DIRTY working tree.** `git status --short` shows four files modified
   (`code/public/catalog.json`, `code/src/components/UserMenu.jsx`, `code/src/lib/serverChapterBody.js`,
   `code/src/lib/serverTenant.js`) — the demo-patches are left applied on disk. Reading
   `serverTenant.js:145` from the working tree shows an `ACADEMY_DEMO_FS_PUBLISHED` fallback that is **not**
   at `HEAD`. I graded every ant-academy claim at `HEAD 22df69dd` via `git show`, where the corpus claim
   holds exactly. A seat reading the disk would have booked a false blocker against
   `academy-backend.md:130-133`.
2. **`grep -c` + `head` nearly produced a false blocker.** `git grep -n STORAGE_RPC_ADDR 9d00a313 | head -10`
   sorts `internal/` before `main.go`, so `main.go:451` fell off the page and the Go-source count read 2
   against the corpus's 3. Re-run unpaginated and restricted to `*.go`: exactly **3**, all comments. The
   corpus is right.
3. **Four clones are behind `origin/main`** (storage, messenger, cms, jobsimulation). Two claims in my file
   set are ref-sensitive; I state the ref used in each (see B-list / Minors / Cleared).

## Positive controls

| file | `wc -l` | read in full? |
|---|---|---|
| `corpus/services/ai-readiness.md` | 672 | yes |
| `corpus/services/cms.md` | 313 | yes |
| `corpus/services/chronos.md` | 245 | yes |
| `corpus/services/academy-backend.md` | 160 | yes |
| `corpus/services/README.md` | 88 | yes |
| `corpus/services/TEMPLATE.md` | 46 | yes |
| **total** | **1524** | — |

---

## Blockers

### B1 — the self-citation this passage re-pointed to STOP an anchor rotting is itself pointing at the wrong construct (third generation)

- **Anchor:** `corpus/services/ai-readiness.md:52` —
  > "The target is therefore named by construct above and pinned at **`:496`** (M257x iter-102); when the two disagree, the name wins."

  The construct being pinned is named at `:46-48`: *"the **⚠⚠ M51 iter-08/09** block below already described the same call site in its post-refactor location — *"(now `aireadiness/readiness.go`, formerly `workforce/ai_readiness.go:512`)"*"*.

- **Why it is false:** measured in the corpus itself (`sed -n '493,501p' corpus/services/ai-readiness.md`):
  - `:493-496` are the **last four lines of the `> **✅ CORRECTED M219 …**` blockquote**; `:496` reads
    `> relying on either number; do not re-derive them from prose.`
  - The `⚠⚠ M51 iter-08/09` block **opens at `:498`**, and the quoted phrase
    `(now `aireadiness/readiness.go`, formerly `workforce/ai_readiness.go:512`)` is at **`:500`**.

  So the re-point lands **inside the very blockquote the same paragraph identifies as the wrong target** —
  `:50-51` says of the previous attempt: *"`:459` merely opens the `> **✅ CORRECTED M219 …**` blockquote,
  which is about an unrelated claim."* `:496` closes that same blockquote. The anchor has now rotted three
  times (`:458` → `:459` → `:496`) and the third repair reproduced the second's failure mode.

- **Confidence:** high. No platform read is needed — this is settled entirely inside `corpus/services/ai-readiness.md`.
  I hesitated only because the sentence supplies a safety net (*"when the two disagree, the name wins"*); I
  booked it anyway because the passage's whole subject is that a bare line number is the defect, and the
  correct target is two lines away and cheap to state.

### B2 — four `AIReadinessClient.tsx` citations are stale by two lines and land on comments / the wrong filter clause (one drift, four anchors)

- **Anchors** (all in `corpus/services/ai-readiness.md`):
  - `:84` — "It gates on `orgEnabled` alone (`:133-134`, `const { orgEnabled } = useAiReadinessEnabled(true)`)"
  - `:335` — "read by the tab filter at `:599`, strips it from the tab list"
  - `:482` — "The **CURRENT** dashboard (`AIReadinessClient.tsx:153-154`) computes `effectiveCycleId = …`"
  - `:484` — "gates the data GET on `cyclesQ.isFetched` (`:166-170`)"

- **Why it is false** — `next-web-app/apps/web/src/app/(authenticated)/(verified)/ai-readiness/AIReadinessClient.tsx`,
  read at **both** the checkout `8297c684` **and** `origin/main f97ba659` (identical at every one of these
  offsets, so the finding is ref-independent):

  | doc says | what is actually there | where the cited construct really is |
  |---|---|---|
  | `:133-134` = `const { orgEnabled } = useAiReadinessEnabled(true)` | `:133-134` are two lines of the preceding `// Feature gate:` comment (`…keeps the dashboard queries from firing while / off (the backend would reject them with ErrAIReadinessDisabled).`) | **`:135`** |
  | `:153-154` = the `effectiveCycleId` assignment | `:153-154` are comment (`// The closed-snapshot path now ALSO joins current membership tags so the / // byTeam aggregate populates …`) | **`:155-156`** |
  | `:166-170` = the `cyclesQ.isFetched` gate | `:166-167` comment, `:168-170` = `const dataQ = useAIReadiness({` / `cycleId:` / `includePeople:` — the range **stops one line short of the gate** | **`:171`** (`enabled: featureOn && cyclesQ.isFetched,`) |
  | `:599` = the `SHOW_SECONDARY_TABS` read | `:599` is `tab.key === 'how' ||` — a different clause of the same `.filter()` | **`:601`** |

  All four were **exact at `bb3313bc`** (the ref the frozen instrument's own table named), i.e. the file
  gained 2 lines between ~`:80` and `:133` and nothing re-derived these. `:78`
  (`const SHOW_SECONDARY_TABS: boolean = false;`) sits *above* the drift point and is still correct, which is
  what makes the block read as verified.

  The same file **claims** to have re-derived at this exact sha 250 lines earlier (`:307-309`:
  *"**Every line number in the three bullets below is a reading at `next-web-app` `8297c684`**
  (re-derived 2026-08-06) — this block used to say 'at HEAD', and a bare moving label is what let the first
  anchor below rot through four readings"*). That re-derivation is real and correct **for the three bullets it
  scopes** (`urls.ts:51`, `useNavbarSections.tsx:4/:401-408/:403/:569`, `WorkforceNewClient.tsx:125-151`,
  `useWorkforceAIReadiness.ts:23-27` — I verified all of them). It simply did not reach these four.

- **Confidence:** high. Booked as **one block / four anchors** because it is one drift with one cause; I flag
  the anchor count explicitly so a repair pass does not fix one and call the predicate discharged.

### B3 — `messenger/pkg/aireadinessemail` names a path in the decommissioned `messenger` repo; the live validator imports `app/internal/messenger/aireadinessemail`

- **Anchor:** `corpus/services/ai-readiness.md:270` —
  > "per-org email-copy overrides (`ai_readiness_email_overrides`) validated against `messenger/pkg/aireadinessemail` placeholders, with an admin preview renderer."

- **Why it is false:** the code that performs that validation is
  `app/internal/aireadiness/emailoverride/emailoverride.go:29`, and it imports
  `aireadinessemail "github.com/anthropos-work/app/internal/messenger/aireadinessemail"` — i.e.
  **`app/internal/messenger/aireadinessemail`, with no `pkg/` segment** (`ls stack-demo/app/internal/messenger/aireadinessemail/` → 9 files at `app ad9f3c49`).
  Under this doc's own path convention (`aireadiness/emailoverride/` = `app/internal/aireadiness/emailoverride/`)
  the cited path resolves to nothing in `app`.
  The one place `messenger/pkg/aireadinessemail` *does* exist is the standalone `messenger` repo at
  `fa47850d` — a repo `838d907` removed from `repos.yml`, so `make init` does not clone it and a reader on a
  fresh box cannot open it at all. This is the *"a named-consumer list survives the merge that moved the
  consumer"* class: the same doc, two paragraphs earlier (`:268`), correctly says the messenger side is
  *"in-process inside `backend` since the v9.0 fold … not a separate service (its container went at `838d907`)"*.
- **Confidence:** medium. **Why I hesitated:** the *proposition* (overrides are validated against the
  aireadinessemail placeholder set) is TRUE, the frozen repo's copy is byte-equivalent modulo gofmt
  (`diff` of the two `renderer.go` → whitespace/alignment only), and the platform's **own** stale in-tree
  comment at `emailoverride.go:33` says *"the Go defaults in pkg/aireadinessemail"*, so the corpus mirrored
  source. I booked it because the briefing lists wrong file paths as BLOCKER-grade and because the path sends
  a reader into a repo the corpus itself documents as un-cloned legacy.

---

## Minors

- `corpus/services/ai-readiness.md:19` — the `WorkforceDirectory` enumeration is incomplete: the interface
  (`app/internal/aireadiness/manager.go:40-48`) declares **three** methods —
  `LoadMembers`, `LoadMembersByUserIDs` **and `BaseMembers`** (`:46-48`, "the cheap path for participant
  enumeration (notifications fan-out)"). `workforce/members.go:343-347` names all three in its own comment.
  Omitted list member; no factual consequence for the D-07 re-anchor the sentence is about.
- `corpus/services/ai-readiness.md:599` — `how_we_measure.go:253-261` starts three lines above its subject:
  `:253-255` are the tail of the previous function + a blank line; the doc-comment opens at `:256` and
  `func (m *Manager) computeCycleTotals(` is at `:260`. The range does contain the construct, so this is
  range slop rather than a wrong construct. (`:285-287` in the same sentence is exact.)
- `corpus/services/ai-readiness.md:246-248` — the M407/M408 email-preview + email-override admin endpoints are
  filed under the bullet headed "**REST/workforce API** (`app/internal/web/backend/api/`)", but they are
  mounted from `app/internal/web/backend/emailpreview/handler.go:64-73`
  (`GET|PUT|DELETE /ai-readiness/email-overrides/:emailType`) and appear nowhere in `api/api.yaml` or
  `api/server.gen.go`. Endpoints real, package attribution wrong.
- `corpus/services/cms.md:106` — "The app-Postgres **`cms.studio_*`** tables (`StudioDocument` / `StudioTask`)
  are 100% customer data and are never captured" names the **legacy husk schema**, while the same file at
  `:44-47` states those tables "were re-created in the **`public` schema** … The old `cms` DB schema is
  **legacy — no longer authoritative**" (confirmed: `app/terraform/migrations/20260724132049_cms_data_model.sql`
  creates `studio_documents` / `studio_tasks` in `public`). A self-contradiction inside one file. I kept it
  MINOR because the never-captured proposition holds under either schema name and the correction is stated
  prominently 60 lines above; I hesitated because it is a present-tense **safety** sentence.
- `corpus/services/chronos.md:22`, `:171`, `:190` — `stack-dev/chronos` does not exist on this box (nor
  `stack-demo/chronos`), and `repos.yml` at `0c91421` has 4 entries, none of them chronos, so `make init`
  never creates it. §"Running in Docker" (`:186`) carries the correct "will fail" warning; §"Running
  Standalone" gives a bare `cd stack-dev/chronos` + `go run .` with no clone-it-by-hand note (the note
  `cms.md:110` does carry). Historical-block framing at `:11` covers it, hence MINOR.
- `corpus/services/README.md:41` — the storage cell's "In prod the ECS service is **deleted**, not scaled to
  zero — the module survives only to keep the buckets/CDN under `prevent_destroy`" carries **no ref**, and it
  is true only at `storage origin/main 9f8cb532` (there `terraform/main.tf` is **18 lines**, module block gone,
  comment: *"The ECS service that used to live here is GONE (v9.0 …)"*). At the pinned checkout `4ce8ece5`
  (20 commits behind) that file is **100 lines** with `module "storage" {` at `:10` and
  `service_desired_count = 1` at `:19`. Sibling rows in the same banner *are* pinned (`6092c6d2`,
  `cms/terraform/main.tf:39`, `roadrunner/terraform/main.tf:19`), so this is an inconsistency in pin
  discipline rather than a false claim — §5 rule 26.
- `corpus/services/academy-backend.md:90` — the Queries enumeration omits `academyPathTime(pathSlug)`
  (`academy.graphqls:648`), listing `academyChapterTime(s)` and `academyPathProgress` but not the path-time
  query. Omitted list member.
- `corpus/services/academy-backend.md:121-138` — the "**cmd binaries**" list gives four
  (`academy-seed`, `academyImport`, `coursebuilder-e2e`, `coursebuilder-liverun`); `app/cmd/` also holds
  `academy-asset-upload`. Omitted list member.
- `corpus/services/README.md:4-5` and `TEMPLATE.md` — the index says every doc follows the template pattern
  "Role & Responsibility · Architecture & Code Map · Interface Discovery · Local Development · **Testing**",
  but in `TEMPLATE.md` Testing is a *subsection* (`### 3. Testing`, `:42`) of "Local Development (The
  'How-To')" (`:26`), not a peer section. Cosmetic.

---

## Positively cleared (audited zeros)

### ENUMERATED — predicate re-derived from source independently, cardinalities compared

1. **`defaultReadinessSkills` = 31 (19 core @1.0 + 12 enabling @0.5, denominator 25.0).** Enumerated
   `app/internal/aireadiness/defaults.go:31-49` (19 rows) and `:51-62` (12 rows). Then diffed the extracted
   node-id sequence against the rext mirror
   (`stack-demo/rosetta-extensions/stack-seeding/seeders/ai_readiness_config.go`): **identical set AND order,
   31 = 31** — so the doc's "mirrored **verbatim**" is exact, not approximate.
2. **`defaultReadinessSims` = 3, track-keyed** (`defaults.go:75-79`): `{simulation,tech,634b9ffd-…}`,
   `{simulation,business,a4113c6b-…}`, `{interview,both,6d6cdf39-…}`; the rext seeder pins the same three
   uuids at `ai_readiness_config.go:93-95` and names the same three slugs at `:84-85`.
3. **`AIReadinessCSVColumns` = 15**, enumerated from `csv.go:17-32`; UTF-8 BOM written at `:43`.
4. **`usageDimSpecs` = 5 ids** (`how_we_measure.go:1160-1181`): `avg_adoption`, `avg_transformation`,
   `avg_originality`, `avg_depth`, `avg_ownership` — exactly the five the doc names at `:206`/`:218-219`/`:643`,
   and none of the retired `avg_frequency`/`avg_breadth`/`avg_context_fit`. (Note the platform's own comment at
   `:1183` still says "four" tiles — the *corpus* is the one that is right.) Seeder side:
   `ai_readiness_interview_report.go:185-189` emits exactly those five, with the `:149` comment
   *"All five are always emitted here."* verbatim.
5. **`interactions.action_type` enum = exactly `{email, call}`** — `internal/data/ent/interaction/interaction.go:92-99`
   `switch at { case "email", "call": }`. The `source_id <> target_id` CHECK is at
   `schema/interaction.go:46-54`.
6. **Supergraph 3 → 1 at `graphql-wundergraph@915da06`.** Enumerated the SET, not the diffstat:
   `git ls-tree --name-only 915da06^ schemas/` = `{backend, cms, jobsimulation}.graphqls` (**3**);
   at `915da06` = `{backend}.graphqls` (**1**). `subgraphs.conf` before = `BACKEND=v1.358.0 /
   JOBSIMULATION=v0.252.1 / CMS=v0.254.2`, after = `BACKEND=v1.360.0`. The commit's own subject does say
   "2→1", exactly as `cms.md:59-64` warns. Ladder confirmed: `749dc86` deleted `skiller.graphqls` (5→4),
   `7c17e63` deleted `skillpath.graphqls` (4→3).
7. **Local topology at platform `0c91421`.** `repos.yml` = **4** entries (app, sentinel, next-web-app,
   studio-desk). `docker-compose.yml` declares **5** services (sentinel, backend, studio-desk, next-web-app,
   gotenberg); `common.yml` adds postgresql + redis → **7** effective. Only backend/studio-desk/next-web-app/
   gotenberg carry a `profiles:` key, so the always-on floor is **{postgresql, redis, sentinel}** and the
   profile-token set is **{core, backend, all, studio-desk, frontend}**; `Makefile:10` `PROFILE ?= core`.
   Every one of `cms.md:252-253`, `README.md:25-31` and `chronos.md:186` checks out.
8. **Zero `*_RPC_ADDR` in compose**, and exactly one service address:
   `docker-compose.yml:48 AUTHORIZATION_ADDRESS=http://sentinel:8087`. backend publishes 8081/8082/8083
   (`:40-43`); `/graphql/query` is real (`internal/web/backend/backend.go:317`).
9. **`MESSENGER_RPC_ADDR` = 0 occurrences across ALL 15 git trees**, each grepped at its own `HEAD`,
   nested `app/studio` + `cms/studio` included (enumerated via
   `find stack-demo -name .git -maxdepth 4`). `git -C stack-demo/platform log -S 'MESSENGER_RPC' --oneline 0c91421d`
   → **0**; the doc's own positive control `-S 'SKILLER_RPC'` → **7**, reproducing exactly.
   (`README.md:44`.)
10. **messenger `internal/flow/flow.go:72-104` @ `fa47850` = 21 LIVE handlers.** Counted the uncommented
    `pubsub.EventHandler(...)` lines: 5 skill-path + 3 jobsim (`:83` is `//`-commented and correctly excluded)
    + 1 due-date + 2 content-agnostic + 2 organization + 1 AI-readiness + 4 AI-readiness-notification
    + 3 course-builder = **21**. `AddSubscriber("backend", …)` is at `:72`. This is precisely the
    "`grep -c` counts commented-out code" trap and the corpus does not fall into it.
11. **The `.tf` census in `cms.md:55` / `:196`.** `git ls-files | grep '\.tf$'` per tree → **44** tracked
    `.tf` across the 13 `stack-demo` repos (app 5, cms 4, graphql-wundergraph 4, jobsimulation 6, messenger 4,
    next-web-app 2, roadrunner 4, rosetta-extensions 2, sentinel 4, storage 5, studio-desk 4; ant-academy and
    platform 0); raw `find` → **59**. `http://backend.internal.anthropos:8081` → **0** hits in `.tf` in every
    tree, and **6** total in the clone set (1 × `app/knowledge/service-dependencies.md:52`, 5 × rext test
    fixtures/tests), **none terraform**. `app/knowledge/service-dependencies.md:50-53` @ `ad9f3c49` does read
    *"There are no external callers of app's RPC mux left."* + the past-tense sentence, verbatim.
12. **`STORAGE_RPC_ADDR` @ `app 9d00a313` = exactly 3 Go-source occurrences, all comments, zero reads**
    (`internal/jobsimwiring/wiring.go:101`, `internal/storagens/callsites_test.go:189`, `main.go:451`);
    29 occurrences tree-wide, the other 26 all under `knowledge/`. (`academy-backend.md:60-64`.)
13. **Academy tables.** `grep -o 'CREATE TABLE "academy…"' terraform/migrations/*.sql` → 11 tables, matching
    `academy-backend.md:65-78` name-for-name: 6 per-user (`academy_chapter_progresses`,
    `academy_last_activities`, `academy_chapter_times`, `academy_certificates`, `academy_bookmarks`,
    `academy_feedbacks`) + 5 catalog (`academy_series`, `academy_skill_paths`, `academy_chapters`,
    `academy_chapter_bodies`, `academy_path_embeddings`). All 11 ent schema files present. The
    "Ent LABELS vs plural TABLE names" warning is correct.
14. **`gen.py:484-492` = exactly nine `add_argument` calls**, and they are exactly the nine the doc names
    (`-i/--interactive`, `-m/--media`, `-f/--force`, `--simid`, `--branch`, `--prompt`, `--annotations`,
    `--pipeline`, `--blueprint`); `parse_argument` at `gen.py:18-28` does call `parse_known_args` and merge
    the leftovers (`:19`, `:22`, `:26`). No `--template`. (`cms.md:279-285`.)
15. **`studio/requirements.txt` = 9 packages verbatim** (openai, anthropic, rich, pyyaml, requests, jinja2,
    mistralai, pytest, pytest-asyncio) — `mistralai` at line **8** as cited — and **no `python-docx`**.
    (`cms.md:144-149`.)
16. **The mistral predicate at the nested repo's OWN ref.** `git -C stack-demo/app/studio grep -ci mistral aeec036a`
    → `requirements.txt:1 · tools/pdf2md.py:18 · tools/r3.py:3` = **22 lines / 3 files**, exactly as
    `cms.md:95`/`:226` states. `tools/pdf2md.py:24` `from mistralai import Mistral`, `:96` `Mistral(api_key=…)`,
    `:127` `model="mistral-ocr-latest"` — all three exact. `tools/r3.py:139`/`:190` exact; `:199-206` is the
    `step2_cmd` construction (the exec itself is `run_step` at `:208`).
17. **Go execs exactly two studio scripts.** `git grep -n 'studio/.*\.py' -- internal/cms/studio/` at
    **both** `b948604f` and `HEAD` returns exactly `studioManager.go:119` (`studio/gen.py`) and `:1045`
    (`studio/postgen.py`) — the line numbers hold at both refs. Neither is `pdf2md.py`.
    Go-side Mistral is OCR-only: `markdownManager.go:10` imports `internal/cms/studio/mistralocr`, `:30` is
    `return &MarkdownManager{ocr: mistralocr.New(aiKey)}, nil` inside `NewMarkdownManager` — both offsets still
    name those constructs at `ad9f3c49`, as `cms.md:217` claims. `studioManager.go:531` is
    `// supported ocr content types for mistral ocr`, `:583` is the `NewMarkdownManager(os.Getenv("MISTRAL_API_KEY"))`
    call, `xlsx.go:13` is the "Mistral OCR doesn't accept xlsx" comment. `app/.gitignore:79` = `studio/*`.
18. **`corpus/services/` holds 29 `.md`; the README enumerates all 27 service docs** — 10 core + 5 frontends
    + 8 cross-cutting + 4 archived = 27, set-equal to the directory minus `README.md`/`TEMPLATE.md`. No
    phantom rows, no omissions.
19. **`app/internal/{skiller,skillpath,jobsimulation,cms,storage,messenger,customeriosync}` all present at
    `ad9f3c49`** (`git ls-tree -d`), and `git ls-tree -r --name-only ad9f3c49 | grep -ci roadrunner` → **0**:
    no path in the whole tree matches `roadrunner`, exactly as `README.md:37` states.
    `internal/jobsimulation/runner` exists and `internal/jobsimwiring/wiring.go:123` is
    `jsrunner.NewRunnerManager(getenv("JUDGE0_API_KEY"), getenv("JUDGE0_BASE_URL"))`.
20. **AI-readiness Playthroughs = 5.** `playthroughs/manifest/ai-readiness.yaml` declares 4
    (`pt-aireadiness-member-done`, `-member-progress`, `-manager-dashboard`, `-manager-howwemeasure`) and
    `onboarding.yaml:361` adds `pt-onboarding-aireadiness-guided` on the `pt-ai-onboard` seat
    (`seed/pt-world.seed.yaml:302`). (`ai-readiness.md:651-654`.)
21. **`AI_READINESS_URL` has occupied only lines 41 / 50 / 51 across every commit ever touching
    `packages/core-js/src/constants/urls.ts`** (iterated all such commits; 32 commits predate the constant).
    At `8297c684` it is `:51`; `:52` is `TALK_TO_DATA_URL` and was `ORGANIZATION_FEEDBACK_URL` at `bb3313bc`
    — the `ai-readiness.md:314-318` retraction is exactly right. `git grep AI_READINESS_URL` finds it in
    exactly two source files plus `knowledge/ai-readiness/frontend-architecture.md:15`.
22. **`AI_READINESS_FLAG` has exactly one consumer repo-wide**:
    `apps/web/src/components/ai-readiness/data/useAiReadinessActive.ts:22` (declared at
    `aiReadiness.constants.ts:26`), and the doc's four-line TS extract matches source lines 22/25/29/32
    verbatim in meaning. `AIReadinessClient.tsx` contains **0** occurrences of `posthog` and **0** of
    `flag_ai_readiness` (`grep -ci`), so `ai-readiness.md:81-86`'s manager-dashboard callout holds.
23. **Platform commit set.** `d11a403` (2026-08-03, "drop roadrunner, prune dead env, repoint messenger") does
    delete the `cms`/`jobsimulation`/`roadrunner` `repos.yml` entries (diff read) and 167 compose lines;
    `838d907` (2026-08-05) = "drop the storage, messenger and customerio-sync containers", merged `0c91421`
    (PR #26); `fdfa189` (2026-04-17) = intelligence; `045857c` (2026-04-17) = chronos, touching
    `docker-compose.yml` **and** `repos.yml`; `2adcf71` = PR #23 chore/drop-wundergraph (2026-07-31);
    `0dab54d` = "rename graphql -> core". jobsimulation `09631fb2` subject matches char-for-char and sits on
    PR **#395** / branch `feat/remove-chronos-and-realtime`.
24. **`README.md:22`'s jobsimulation-teardown claim is TRUE at the ref it names, and only there.**
    At the pinned checkout `462343b0`, `jobsimulation/terraform/main.tf` is **343 lines** with
    `module "jobsimulation"` at `:31` and `service_desired_count = 0` at `:40` — which reads like a blocker.
    At **`6092c6d2`** (the sha the sentence itself names; ancestor of `origin/main`, not of the checkout) the
    file is **56 lines**, `service_desired_count` is absent, and `:15-22` is exactly the quoted M810 comment
    block. Graded at the named ref per §5 rule 33 → **cleared**. Recorded because grading it at the checkout
    would have produced a confident false blocker.
25. **cms M810 both-ways evidence.** `cms/terraform/main.tf` is **191 lines** with `:39
    service_desired_count = 0` at the checkout `ca50c817` **and** at `origin/main f38c0c4a`; `6efa1d5`
    (2026-08-04, merged `f38c0c4`) does delete `.github/workflows/build-production.yml` (18 deletions) under
    the subject *"the cms ECR repository is decommissioned (M810)"*. Both halves of `cms.md:11-19`/`:78-84`
    are measured and the "assert neither" framing is the honest one. `roadrunner/terraform/main.tf:19` = 1 at
    both refs.
26. **`app` domain code map (cms.md).** `internal/cms/` contains directus, similarity, studio, library,
    importer, exporter, aivideo, contentread, jobsimimport, rpcsrv, worker + `wiring.go`;
    `20260724132049_cms_data_model.sql` creates exactly the 6 named tables in `public`;
    `REDIS_CMS_CACHE_INDEX` default **5** (`main.go:1107-1108`); `POST /api/webhook/directus`
    (`backend.go:324`) is built from `cmswebhooks.Handler(os.Getenv("DIRECTUS_WEBHOOK_SECRET"), …)`
    (`main.go:1199`) and is Clerk-exempt at `backend.go:137-138`; `cms/cmd/root.go:77`/`:78` are the
    8080/8081 `cmp.Or` defaults; `cms/Dockerfile:2` golang:1.26-bookworm, `:23` python:3.11-slim;
    `cms/go.mod:3` `go 1.26.4`; `docker-compose.yml` @ `2adcf71` has `8090:8090`/`8091:8091` at `:154-155`,
    `PORT=8090` at `:169`, `RPC_PORT=8091` at `:173` — every offset exact.
    `app/main.go:1205-1211` @ `b948604` carries the "Additive + DORMANT … until the M809 re-point" comment
    verbatim. `internal/skillpath/session.go:205-207` is the cms-in-app deseam comment +
    `u.cms.GetSkillPathDomain(…)`, and `u.cms` really is typed `contentread.CmsContentReader` (`:50`).
    `jobsimulation` has **0** occurrences of `DIRECTUS_BASE_ADDR`. `DIRECTUS_DATA_CONSUMERS = ("cms","backend")`
    (`stack-injection/gen_injected_override.py:86`, pinned clone) — "now names both" is right, and the public
    predicate `private = false AND tenant_id IS NULL AND status = 'published'` matches
    `stack-snapshot/cmd/stacksnap/adapters_harden_test.go:175`.
27. **AI-readiness platform surface.** `organization_settings.go:47` = `OrganizationSettingAIReadiness = "ai_readiness"`;
    `enum/ai_readiness.go:9-11` = the three step types; `scoring.go` has `archetypeHighBand = 75` (`:26`),
    `archetypeLowCeil = 50` (`:30`), `classifyArchetype` (`:46`) with tie→Explorer (`:37-38`, `:53`), and the
    bands `None 0-24 / Low 25-50 / Medium 51-74 / High 75-100` are the literal doc-comment at `:63-66`;
    `computeKnowledgeAxis` is `(step1+step2)/70*100` (`:87`, `:93`) and `computeUsageAxis` scales step-3 by
    `100/30` (`:102`); `computeTier1` at `readiness.go:139` divides by the whole configured repertoire
    (`:130-134`). `GetAIReadinessWithOptions` at `:289` with the two snapshot routes at `:290-297` and
    `:307-312` and the fallthrough at `:314` — every offset exact. `computeOrgBreakdowns` at `:330`.
    `keepInCycleStep1` call `:388` / func `:710`; `keepStartedMembers` call `:390` / func `:730`;
    `queryInCycleStep1Completers` func `:684` with the `StepSkillMapping ∧ StepCompleted ∧ CompletedAtGTE`
    triple at `:694-698`; `buildResponseFromSnapshots` at `:817` calling
    `m.workforce.LoadMembers(ctx, orgID, "")` at `:825`. `steps.go`: `queryReadinessStarters` func `:964`,
    SQL `:969-974` (`ai_readiness_user_step_progresses … status <> 'not_started'`, no evidences table),
    the *"only real 'has started' check"* comment at `:962`, `queryReadinessSimCompleters` at `:922`.
    `how_we_measure.go` `computeCycleTotals` counts `public.interactions` joined at `:285-287`.
    `CompareCycles` returns the six named sections and rejects non-`closed` cycles (`compare.go:163-171`).
    `NewManager` (`manager.go:135`) takes db/ent/ai/pub/academyEmbeddings/directory. The
    `ai_readiness_cycles` partial unique index is `IndexWhere("status = 'active'")`. All 14 AI-readiness ent
    schemas exist, and `interview_aggregated_report.go:23-26` carries exactly
    `(sim_id, organization_id, report JSONB, session_count)`. GraphQL: `ai_readiness.graphqls` declares
    `aiReadinessEnabled`, `aiReadinessUserPlanProgress`, `aiReadinessSkills` and
    `completeAiReadinessSkillMapping`, with the 30/40/30 split stated in its own docstring. REST: all nine
    cited paths exist in `api.yaml`/`server.gen.go`. `conversation_extractions` has **0** hits in
    `internal/aireadiness/` — the red-herring claim at `:197-199` is right.
28. **rext seeder side (pinned clone `09d06070`).** `aiReadinessStageFor` (`ai_readiness_funnel.go:205-226`)
    checks `IsAIReadinessNotStarted()` **first**, then manager→0, struggling→1, default→3 — matching
    `ai-readiness.md:397-414` including the ordering-is-the-contract point; `Persona.AIReadiness`
    (`blueprint.go:200`) is a closed enum (`validateEnum(… "", AIReadinessNotStarted)` at `:618-619`);
    `aiReadinessStartedHeroSkills = 9` with the exact 11/30 derivation in its own comment;
    the population windows are `stage3 ∈ [19,31] · stage2 ∈ [3,19] · stage1 ∈ [1,3]` (`:507-514`,
    `aiReadinessCoreSkills = 19`); `aiReadinessMinTurns = 6` / `aiReadinessMaxTurns = 11`; and the funnel
    writes **`public.actors` / `public.interactions`** (`CopyRowsIdempotent(ctx, "public", …)` at `:878`/`:886`)
    — the corpus is right and the *rext code comments* saying `jobsimulation.*` are the stale ones.
    `ai_readiness_recommendations` receives **0** writes. `cockpit.go` has `LegacyReadinessPaths()` (`:135`)
    and `ValidateCockpitManifest` (`:161`). `stories.seed.yaml` sends Dana→`/ai-readiness` (`:196`),
    Aria→`/home` (`:153`), Ben→`/home` (`:179`), Northwind size 200. The demopatch manifest reads
    `path: internal/aireadiness/readiness.go` at `:42` and *"v2.7 M254 RE-POINT"* at `:33` — **identical in
    both rext trees**, so the D-07 "re-anchor has LANDED" claim holds whichever tree adjudicates;
    `demo-stack/tests/test_aireadiness_snapshot_loadmembers_m254.py` exists.
29. **Academy backend.** `git describe --tags ad9f3c49` → `v1.369.0-7-gad9f3c498` and `CHANGELOG.md:5` reads
    `## v1.369.0 - 2026-08-05` — the doc's re-derivation reproduces character-for-character. PR #903 =
    `0e37771f` (2026-06-05, *"Academy backend v1.0 'ground truth'"*). `main.go:471-472` @ `9d00a313`
    constructs `internalstorage.NewManager` / `NewPublicManager`; `cmd/academyImport/main.go:239-243` carries
    the quoted comment and `:244-246` is the hard error. `/content/catalog.json` (`content.go:23`) and the
    `/content/admin` group behind `sharedTokenMiddleware(os.Getenv("ACADEMY_CONTENT_API_TOKEN"))`
    (`content_admin.go:35`) both exist. `academy_embedding_refresh` @ 03:00 is named in three source sites.
    `serverTenant.js` at ant-academy **HEAD `22df69dd`** has `emptyCatalogView()` at `:115` and
    `?? emptyCatalogView()` at `:145`, with *"the cutover is intentional, not reversible-on-error"* at `:132`
    — the `:115-145` range is exact; the rext patch `demo-stack/patches/academy-fs-published-fallback` exists;
    and `ant-academy.md:82-88` states the same thing in bold, so the claimed cross-file agreement holds.
30. **`README.md:53` "Next.js **16**" is correct** — `apps/web/package.json:46` and `apps/hiring/package.json:45`
    both pin `"next": "~16.2.12"`; ant-academy `code/package.json:64` is `"next": "^16.2.6"`.

### RE-DERIVED — I recomputed arithmetic the document itself displays (weakest form of clearance)

- `ai-readiness.md:565-585` FILLED-ness part 1: `round(1.0/25.0*30) = 1`, `round(0.5/25.0*30) = 1`,
  `round(9.0/25.0*30) = 11`, `round(3/6.5*30) = 14`, `25/25*30 = 30`; and the integrality argument
  (`held/25*100 = held*4`, integral for any half-step weight) holds.
- `ai-readiness.md:361-363` "78.4% (≈156 of the 199 frozen snapshots)": `0.784 × 199 = 156.0`, consistent with
  `aiReadinessCompletedShare = 0.80` as a hash-bucket share.
- `dae0fb2f7`'s "−653 lines": the three deleted components sum 103 + 220 + 330 = **653** (the commit's total
  is 662, the extra 9 being `HowWeMeasureTab.tsx` edits) — the doc's figure is the right decomposition.
- `HowWeMeasureTab.tsx` is **1,989** lines and `grep -c interviewQuestions` = **0**; `:1879`, `:1903`,
  `:1915`, `:1921`, `:1927` are all exact, and `hooks/useAIReadiness.ts:326` is `interviewQuestions: number;`
  inside `AIReadinessCycleTotals` (`:323-330`) exactly as pinned.

---

## What I could not settle

- **GitHub repo-archival state.** `README.md:52` says graphql-wundergraph's "repo ARCHIVED";
  `README.md:79` / `chronos.md:3` say chronos's repo is **NOT** archived, "last push 2026-04-23". Neither is
  derivable from a clone — it needs `gh api repos/anthropos-work/{graphql-wundergraph,chronos}` (`.archived`,
  `.pushed_at`). Flagging because the two rows make opposite claims about the same kind of fact from the same
  unverifiable source.
- **Feature-ship versions** `ai-readiness.md:6-7`: "`app` v1.315+ (backend) + `next-web-app` v2.89.0+ (UI)".
  Settling this needs `git tag --contains` on the first AI-readiness commit in each repo; I did not run it.
  (I did confirm `v1.351.1` exists as a tag and that `4c28365f` is the refactor commit, dated 2026-07-22 with
  the exact quoted subject.)
- **Everything in `chronos.md:13-245` below the banner.** The `chronos` repo is in **no** clone set
  (`stack-dev/chronos` and `stack-demo/chronos` both absent), so `schema.sql`, the `timers` DDL + four
  indexes, the state table, `ScheduleTimer`/`CancelScheduledTimer` signatures, the `LIMIT 10` /
  `FOR UPDATE SKIP LOCKED` batching, the 3-second run timeout, Go 1.25, sqlc, ports 8080/8081, the env-var
  and CLI-flag tables are all **unverifiable here**. Only the decommission banner (`045857c`, `09631fb2`,
  PR #395, `jobsimwiring/worker.go:24-35`, absence from compose/`repos.yml`) is measurable, and all of it
  checks out. A clone of `anthropos-work/chronos` at any ref would settle the rest.
- **Production/`infrastructure` state.** `cms.md`'s M810 question and `academy-backend.md:15`'s
  "in production the router is still declared" both terminate in `infrastructure/terraform/production/services.tf`,
  which has never been in a clone set — `cms.md:18`/`:82-84` says so itself. I could confirm only the
  per-repo module declarations (`graphql-wundergraph/terraform/main.tf:20` `service_desired_count = 1`).
- **Live measurements.** `ai-readiness.md`'s 2.09 s / 24 ms / 19 ms / 180 s, the "96 all-403 Directus lines"
  in `cms.md:106`, the `interview_extraction_results` 165-row count, and the 199/156 snapshot counts are
  runtime observations against a seeded stack. Nothing in a static tree can refute or confirm them; the doc
  correctly says "Re-measure before relying on either number."
- **`ai-readiness.md:500` "formerly `workforce/ai_readiness.go:512`"** — the file was deleted by the M247
  refactor, so the historical offset is unverifiable at any ref in this clone (the refactor commit `4c28365f`
  is reachable, but I did not reconstruct the pre-refactor file).
- **Out-of-scope observation, recorded not booked:** root `CLAUDE.md` describes next-web-app as a
  "Next.js **15** monorepo", contradicting `corpus/services/README.md:53` ("Next.js **16**"). Measured, the
  README is right (`~16.2.12`). The defect is in `CLAUDE.md`, which is outside this reading's file set — but
  it is §5 rule 19's "highest-propagation site in the tree", so it is worth routing.
