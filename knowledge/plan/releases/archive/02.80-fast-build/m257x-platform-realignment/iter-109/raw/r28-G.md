# Seat G — reading #28

**Trees read, with the ref each claim was graded at** (per the ADDENDUM's superseded ground-truth sheet):

| tree | path | ref used |
|---|---|---|
| app | `stack-demo/app` | checkout `ad9f3c49` (= `origin/main`); pinned claims graded at the ref they name — `9d00a313`, `b948604f`, `5ba17044`, `2035f9a`, `1e457fa70` |
| platform | `stack-demo/platform` | `0c91421`; historic claims at `2adcf71`, `0dab54d`, `d11a403`, `838d907`, `a2a3ee6`, `a4db680` |
| next-web-app | `stack-demo/next-web-app` | `8297c684` (checkout + demo build pin); historic at `bb3313bc` |
| studio-desk | `stack-demo/studio-desk` | `41ee3575` |
| sentinel | `stack-demo/sentinel` | `f2c46190`; historic at `88bc5592`, `88036d7` |
| storage / messenger / cms / jobsimulation / roadrunner | `stack-demo/*` | `4ce8ece5` / `fa47850d` / `ca50c817` / `462343b0` / `87d8d443` |
| rosetta-extensions | **pinned per-stack consumption clone** `stack-demo/rosetta-extensions` | `09d06070` — used for every claim about what the tooling *does on a stack* (§5 rule 45). The authoring copy was **not** read. |
| snapshots | `.agentspace/snapshots/{taxonomy,directus}` | manifests + `.copy` payloads on disk |

Nested repos enumerated before any tree-wide zero: `find stack-demo -name .git -maxdepth 4` →
15 trees including `stack-demo/app/studio` and `stack-demo/cms/studio`; the `SkillPathSessionService`
zero below is stated over all 15 at their own refs.

**Two instrument failures caught mid-pass and repaired before any conclusion was drawn** (recorded because
an unread failure is indistinguishable from an absence):
1. `git -C <repo> show "$r:repos.yml"` inside a zsh loop silently expands `$r:r` as a **history modifier**,
   producing `fatal: ambiguous argument '2adcf71epos.yml'` on stderr and an empty stdout. Every
   `repos.yml` count read **0** until re-run as `"${r}:repos.yml"`. All repos.yml counts in this report
   are from the repaired form.
2. `git -C next-web-app show "${R}:apps/.../simulationScoreColumn.tsx"` against a *guessed* path returned
   empty for both apps, and the two empty outputs hashed **identically** (`da39a3ee…` = sha1 of ""), which
   reads exactly like "byte-identical twins". Re-run at the real path
   (`components/molecules/Table/Definitions/InsightsByJobSimulationByMembershipsRow/`) the twins really are
   identical (`8b346c31…`), but the first result was a false confirmation.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/services/hiring.md` | 483 | yes |
| `corpus/services/studio-desk.md` | 435 | yes |
| `corpus/architecture/shared_libraries.md` | 274 | yes |
| `corpus/services/next-web-app.md` | 164 | yes |
| `corpus/services/skillpath.md` | 117 | yes |
| `corpus/services/gotenberg.md` | 84 | yes |
| **total** | **1557** | |

Also read in full, as the briefing requires: `corpus/ops/platform-alignment.md` §5 (lines 249–1660).

## Blockers

### B1 — studio-desk's Express backend is documented as the GraphQL client; it contains no GraphQL code at all, and the mermaid puts the arrow on the wrong node

- **Anchor:** `corpus/services/studio-desk.md:38` — *"2. **Backend**: Express.js API server … - GraphQL integration with the **cms domain** (in-process inside `backend`, `app/internal/cms` — there is no `cms` service)"*
  and `corpus/services/studio-desk.md:46` — the mermaid edge
  `Backend --> GraphQL[GraphQL :8082/graphql/query — backend directly; the router is prod-only]`,
  where `Backend` is defined at `:45` as `Backend[Express Backend :9000]`.

- **Why it is false:** measured at `studio-desk` **`41ee3575`** (checkout, no ref named in the passage):

  - `git grep -in "GraphQLClient|graphql-request|graphql/query|8082" 41ee3575 -- 'src/*'` returns **0**.
    The only two `graphql` hits anywhere in `src/` are `src/routes/skillpath.ts:374` and `:405`, and both
    are comments saying the opposite of the claim — *"We do NOT route this through the platform's
    `privateSkillPaths` GraphQL"*.
  - `src/index.ts` mounts exactly five API surfaces: `/api/health-check` (`:141`), `/api/dev` (`:150`),
    `/api/ai` (`:158`), `/api/skillpath` (`:161`), `/api/youtube` (`:164`). There is no GraphQL route and
    no GraphQL proxy. `src/routes/skillpath.ts:13` states the backend's actual remote dependency:
    *"Data: All data stored in Directus CMS (REST API with Bearer token)."*
  - The GraphQL client is entirely **frontend**: `app/services/config.ts:6` reads
    `VITE_GRAPHQL_ENDPOINT` (a `VITE_`-prefixed, browser-baked variable), and
    `new GraphQLClient(config.GRAPHQL_ENDPOINT)` is constructed in
    `app/services/userService.ts:20`, `app/services/taxonomyService.ts:43`,
    `app/services/userPreferencesService.js:13` and
    `app/services/content/simulationContentService.js:325`. `DIRECTUS_BASE_URL` appears only in
    `src/index.ts` and `src/routes/skillpath.ts` (2 hits each) — i.e. exactly the inverse split of what
    the diagram draws.
  - So the diagram asserts a **non-existent edge** (`Backend --> GraphQL`), **omits the real one**
    (`Frontend --> GraphQL`), and **omits `Backend --> Directus`** — the one edge the same document's
    § *Skill Path Builder* (`:107`) and § *Integration Points* (`:282-283`) both call out as the studio's
    write path (`Bearer ${DIRECTUS_TOKEN}`, `src/routes/skillpath.ts`), and the edge the demo's
    `DIRECTUS_TOKEN` strip (`:109`) exists to disarm. The diagram routes Directus through `CMS` instead.
  - This is therefore also a **self-contradiction inside one file**: `:38`/`:46` put the GraphQL client on
    the Express server, while `:135` (*"Example from app/services/graphql/"*), `:140`
    (*"Configured via `VITE_GRAPHQL_ENDPOINT`"*) and `:283-285` (studio preferences *"round-trip through the
    platform **GraphQL** API"* via `app/services/…`) all correctly put it in the frontend.

  Consequence for a reader doing real work: anyone debugging "the studio can't read the catalog" on a demo
  will inspect the container's server-side env for a GraphQL address that is never read, instead of the
  **build-time-baked** `VITE_GRAPHQL_ENDPOINT` in the browser bundle — and anyone reasoning about the
  demo's prod-write disarm from this diagram will conclude the studio reaches Directus only via `app`'s cms
  domain, when it holds a direct Directus admin token.

- **Confidence:** high

### B2 — `GCLOUD_SERVICE_ACCOUNT` is cited at `.env.example:120`; that line is blank, the declaration is at `:119`

- **Anchor:** `corpus/services/studio-desk.md:107` — *"`GCLOUD_SERVICE_ACCOUNT` is declared in `.env.example:120` and injected by `terraform/main.tf:129`, but **no code in `src/` reads it**"*
  (restated at `:199` in the `.env` block, without a line number).
- **Why it is false:** at `studio-desk` `41ee3575` (no ref named ⇒ graded at the checkout),
  `git grep -n GCLOUD_SERVICE_ACCOUNT` returns `.env.example:**119**`. `sed -n '112,126p'` confirms
  `:117 YOUTUBE_API_KEY=…`, `:118` the comment *"# Google Cloud service account JSON (used for YouTube
  Data API)"*, `:119 GCLOUD_SERVICE_ACCOUNT=`, and **`:120` is an empty line** (`:121` begins the
  `LEGACY VARIABLES` banner). The file is 131 lines, so `:120` is in range and resolves — to nothing.
  The other two halves of the same sentence are correct: `terraform/main.tf:129` is
  `"name": "GCLOUD_SERVICE_ACCOUNT"`, and no file under `src/` names the variable (only `.env.example`,
  `CHANGELOG.md:158` and `terraform/main.tf`).
- **Why I hesitated (stated as the briefing requires):** the variable name is right, the neighbouring
  citation is right, and a reader lands one line below the declaration. I booked it as a BLOCKER rather
  than a MINOR because it is a **point** citation offered as the evidence for the claim, it lands on a
  blank line (the one wrong-anchor shape `anchor_construct_guard` is documented as catching — §5 rule 34),
  and the briefing's grading section names "wrong line anchors" under BLOCKER explicitly. If the house
  rule resolves line drift downward, demote it.
- **Confidence:** medium

## Minors

- `corpus/architecture/shared_libraries.md:57` — *"`sentinel`'s `88036d7` … took it `v0.34.3 → v0.35.2`, **two commits past** the `88bc5592` this row used to cite"*. Measured: `git -C stack-demo/sentinel rev-list --count 88bc5592..88036d7` = **1**; it is `f2c46190` (the current checkout) that is two past. The sha, the direction and the version pair are all correct — only the distance is off by one.
- `corpus/services/hiring.md:300` — cites the **History** blockquote as `` `:193-198` ``. In the file as it stands, `:193` is a **blank line** and the blockquote runs `:194-207`; the cited range covers 5 of its 14 lines. Same edit that produced it also produced the next item, and the passage itself says both numbers were derived by adding `+23` / `+16` deltas rather than re-measured (§5 rule 34's exact failure).
- `corpus/services/hiring.md:302` — cites the `job_position` bullet as *"now `:173-175`"*. `:173` is the **last line of the preceding bullet** (*"(real content, zero synth — M222 BA-6)."*); the `job_position` bullet runs `:174-185`.
- `corpus/services/next-web-app.md:89` — the comment quotes `engines.node ">=24"`; the actual value at `8297c684` is `">=24.0.0"` (`package.json:14`). The stated requirement (Node 24+) is right.
- `corpus/services/studio-desk.md:112` — *"Four model tiers … Tier defaults: OpenAI/Azure `gpt-5.2` / `gpt-5-mini` / `gpt-5-nano`; Anthropic `claude-opus-4-5` / `claude-sonnet-4-5` / `claude-haiku-4-5`"* lists **three** names against **four** tiers without saying which tier doubles up. Source (`src/services/ai/config.ts:20-38`): OpenAI/Azure `thinking_slow` **and** `thinking_fast` are both `gpt-5.2`; Anthropic `thinking_fast` **and** `fast` are both `claude-sonnet-4-5-20250929`. The Anthropic ids in source also carry date suffixes the doc drops. Non-material, but the mapping is not recoverable from the doc.
- `corpus/services/gotenberg.md:19-28` — the compose block is transcribed as a YAML block sequence and omits `networks: [app-network]`; the real block (`docker-compose.yml:170-183` @ `0c91421`) uses flow style. No factual consequence; every flag and port matches.

## Positively cleared (audited zeros)

Labelled per the briefing. **ENUMERATED** = I derived the predicate's member set from source and compared
cardinalities. **RE-DERIVED** = I only re-computed arithmetic the document displays (the weakest clearance).

### ENUMERATED

- **`shared_libraries.md:22-25` — the 7-repo `go.mod` census.** I read all seven `go.mod`s at their stated refs and listed every `anthropos-work/*` require. colony **7/7**, proto **7/7**, taxonomy **6/7** (absent only from roadrunner), ai **2/7** (cms `ca50c817` + jobsimulation `462343b0` only), authn **0/7**. Every per-row line anchor and version in the colony / proto / ai / taxonomy tables verifies exactly (`app/go.mod:15,16` @ `ad9f3c49`; `sentinel/go.mod:8,9` @ `f2c46190`; `app/go.mod:20` @ `b948604f`; `sentinel/go.mod:21` @ `88bc5592`; `storage/go.mod:25 //indirect`; `messenger/go.mod:9` direct; `cms/go.mod:9,10,11,13`; `jobsimulation/go.mod:11,12,13,15`; `roadrunner/go.mod:7,8`). The `v0.35.1` "third pin" really is cms+jobsimulation.
- **`shared_libraries.md:213` — "that set is 11".** Derived from `repos.yml`'s whole **history** (10 commits touch it): the union of `type: go` names is exactly `{app, chronos, cms, intelligence, jobsimulation, messenger, roadrunner, sentinel, skiller, skillpath, storage}` = **11**, all four of `skiller/skillpath/chronos/intelligence` present in the first revision `a2a3ee6`, and skillpath the last of the four removed (`a4db680`, newer than `21429b7`/`045857c`/`fdfa189`).
- **`shared_libraries.md:86` / `skillpath.md:30` — "six Connect handlers", "`SkillPathSessionService` has 0 occurrences".** `main.go` @ `b948604f` contains exactly **6** `mux.Handle` calls, at `:1187, :1188, :1196, :1204, :1213, :1228`, five unconditional and `CMSService` behind `if cmsRPCServer != nil` (`:1212-1214`); neither a skillpath nor a roadrunner service is among them. `SkillPathSessionService` returns **0** in Go source across **all 15 git trees** at their own refs, nested `app/studio` and `cms/studio` included.
- **`shared_libraries.md:93-104` — "At least 13".** The named list contains exactly 13 distinct service names. Correctly framed as a floor (`proto` is in no clone set), and `storage.md:129` really is the `### Connect-RPC (`StorageService`)` heading. `roadrunner/main.go:7` really is the lone `colony` import, used at `:17` for `NewVersionConfig`.
- **`shared_libraries.md:234-241` — the taxonomy figures.** Read from `.agentspace/snapshots/taxonomy/5afc0bcc…/manifest.json` directly: `public.skills` **42,790**, `public.job_roles` **22,470**, `public.job_role_embeddings` **18,919**, all under `filter: organization_id IS NULL`, `source: primary-read`, `public_only: true`, `predicate: org-null`, `captured_at 2026-06-29`. Every figure and every provenance field matches.
- **`hiring.md:171` — "87 real `SIMULATION_TYPE_HIRING` sims".** Parsed `directus.simulations.copy` (307 rows, 0 malformed) and tallied the `type` column: TRAINING 121 / ASSESSMENT 98 / **HIRING 87** / INTERVIEW 1. The companion claim *"0 `job_position` rows"* holds as stated — the `job_position` collection is not one of the 14 captured tables (5 simulation rows carry a non-null FK into a table that was never captured, which is what makes the sims-are-the-positions decision correct).
- **`hiring.md:264-266` — "four required-and-undefaulted columns".** Enumerated the whole DDL (`20260722104506.sql:2-27`): **23 columns** (matching `:180`), and the NOT-NULL-without-DEFAULT set is exactly `{owner_id :6, sim_id :7, sim_type :10, token :13}`; every other NOT NULL column (`id, created_at, updated_at, status, completion_status, interactions_progress, score, validation_version`) carries a DEFAULT. `token` is UNIQUE at `:29`. `completion_status` is a bare `character varying` with **no CHECK**.
- **`hiring.md:259-261, :279` — the closed 5-value enum.** `enum/jobsimulation.go:29-35` declares exactly `pending/passed/failed/discarded/timedout`; `Values()` at `:37-43` returns the same five; both SDL enums (`jobsimulations.graphqls:14-20` and `:128-134`) declare exactly those five lowercase members and no `SIMULATION…` member.
- **`hiring.md:454` — "the regression enumerates all 8 generic seeders".** `stack-seeding/seeders/hiring_scope_test.go:75-96` (pinned rext `09d06070`) lists exactly 8 cases: jobsim-sessions, activity, skillpath-sessions, assignments, hero-activity, persona, **feedback**, **succession**.
- **`hiring.md:388` — "5 admins + 45 candidates".** I nearly booked this: `role_mix.admin` is **0.14** at `stories.seed.yaml:249` and `roleForIndex` (`users.go:571-582`) computes `int(50*0.14) = 7` admin-band slots, so the arithmetic alone says 7/43. The **predicate** is what makes it 5/45: the two CANDIDATE cockpit heroes ride admin-band slots and `roleForHero` (`users.go:541-548`) overrides them to `candidate`, which is exactly why M226 iter-04 raised 0.10 → 0.14. Net **5 real admins + 45 candidates**. The doc is right and the visible ratio is the attractor (§5 rule 24).
- **`hiring.md:93` — the positive-control counts.** `OrgFeatureInsights` = **8** lines in `resolver_queries.go`; `JobSimulationSession` = **44** lines in `intelligence.go`; `grep -in hiring` over `resolver_queries.go` returns only sim-TYPE filters (`:811, :843, :991, :992`) and over `intelligence.go` returns **0**.
- **`next-web-app.md:22` — the Next/React/pnpm/Turbo quartet.** All four apps (`web/hiring/integration/maintenance`) declare `"next": "~16.2.12"` — tilde, confirmed — `apps/mobile` declares none; `pnpm-lock.yaml` resolves `next@16.2.12` (two entries); `react` is `^19.2.7` in all four and the lock resolves `react@19.2.7`; root `packageManager: pnpm@10.30.3`, `engines.node ">=24.0.0"`, `turbo ^2.9.6`. The stated correction is also exact: `^16.2.7` is the range on `@next/third-parties` and `@next/mdx`, not on `next`.
- **`next-web-app.md:70` / `studio-desk.md:120` — locale counts.** `configs/i18n/messages/` holds exactly **8** dirs (de,en,es,fr,it,ja,nl,pt); `studio-desk app/public/l12n/` holds exactly **7** (no `pt`).
- **`next-web-app.md:54-55` — the compose service set.** `docker-compose.yml` @ `0c91421` declares exactly `sentinel(:5)`, `backend(:28)`, `studio-desk(:112)`, `next-web-app(:143)`, `gotenberg(:170)`; `include: [common.yml]` at `:1-2` carries the rest of the always-on floor.
- **`gotenberg.md:47` — "nine MIME types … DOCX is not among them".** `extract.go:17-27` @ `9d00a313` is a 9-entry map: `.xls, .xlsx, .ppt, .doc, .odt, .ods, .odp`, and RTF under both `application/rtf` and `text/rtf`. DOCX (`…wordprocessingml.document`) is absent, and `:83` sends everything else straight to `docconv`.
- **`shared_libraries.md:126-137` — the `ai` library surface at v1.40.2.** Not gradeable from the standalone repo (not cloned), but the fold commit `1e457fa70` **is** the v1.40.2 content: its `internal/ai/ai.go` interface carries all nine listed methods **including `ChatCompletionStream`**, and `internal/ai/mistral/completion.go` exists with `ChatCompletion/ChatCompletionStream/CreateEmbeddings/CreateSpeech` all `panic("not implemented")` and `OCRProcess` implemented. Constructors `openai.New`/`NewOpenAI` (`:25,:29`), `openai.NewAzure` (`:48`) with `AzureDefaultAPIVersion = "2025-04-01-preview"` (`:43`), `anthropic.NewAnthropic(cfg, key)` (`:48`), `mistral.NewMistral` (`:25`); `retry.go` = 10 attempts, never on `401,403,404` or `context.Canceled/DeadlineExceeded`. (Worth noting for a future repair, **not** a defect today: `app`'s in-tree copy at `ad9f3c49` has since removed the whole streaming path and the entire `mistral/` package, and its own test says *"This package now contains no `panic(\"not implemented\")` at all"* — so this section's surface description will become false for `app` the moment anyone reads it as describing the in-tree library rather than the module the frozen repos pin.)

### RE-DERIVED (arithmetic only — weakest clearance)

- `hiring.md`'s 40+ `app` `file:line` citations: **every one resolves to the named construct** at the ref it names — `manager.go:450/:453/:537` @ `ad9f3c49` **and** the non-uniform offsets back to `5ba17044` (`:448`, `:451`, `:487` — the `−50` really is `−50`); `intelligence.go:1700, :1702, :1709, :1733, :1738-1764, :1820, :1844, :1846, :2124, :2158-2160, :885-886`; `resolver_queries.go:1034, :1035, :1053, :1080, :1085, :1089`; `resolver_cms_queries.go:95/210/258/295` + `:99-103`; `siminvitationlink.go:62/:63` inside `CreateOrganizationSimInvitationLink`; `ent/schema/job_simulation_session.go:33/:39/:45`; `ent/jobsimulationsession.go:181-186`; `graph.go:129546-129554` + `:129392-129400`; `anticheat_result.go:24`; `atlas.hcl:8`; `askengine/registry.go:192`; migrations `20260729133514.sql:58-62/:62`, `20260722104506.sql:2/:12/:13/:29/:53/:79`, `20260722081626_jobsim_data_model.sql:336/:355/:376`, `20250416091037.sql:5`, `20240527131926.sql:7`; `SET "score"` = **0** hits set-wide against a `SET ` positive control that hits; only `CREATE SCHEMA` in the set is `auth` (`20230817154747_supabase_baseline.sql:2`).
- `hiring.md`'s `next-web-app` citations @ `8297c684`: `UserStatusContext.tsx:141-173/:144-149/:168-172`; hiring twin `:125/:144-145`; `useGetClerkOrganization.tsx:16-18/:20-21`; `useNavbarSections.tsx:249-256, :329-331, :340-343, :391-398, :475-482, :476, :161, :568`; the `bb3313bc` back-anchors `:459-466/:460`; `orgGroups.ts:48-65` with `intelligence: isAdmin` at `:61` and **no `isHiringOrg` parameter**; `showWorkforce={false}` in exactly **two** places, both `apps/hiring/…/template.tsx:167` and `:248`; `template.tsx:90` `Boolean(organization)`; `FreeTrialContainer.tsx:29` `Boolean(!isHiringOrg && organizationId)`; `simulationScoreColumn.tsx` twins byte-identical (`8b346c31…`) with `:54` = `accessorKey` (defaulted `'score'` at `:33`) and `:95-97` the `row.original.score` read; `insights.ts:31-82`; `InsightsByMembersContainer.tsx:359` a plain Ant `<Drawer>`; **0** intercepting-route dirs under `apps/`; `useTablePagination(initialLimit = 20)`.
- `hiring.md`'s rext citations @ pinned `09d06070`: `hiring_config.go:99` `CopyRowsIdempotent(… "organization_sim_invitation_links" …)`; `persona_write.go:91` writes `{"public","job_simulation_sessions",sessionCols(),…}` and `:152` declares `sessionCols()` (18 cols, `token` at `:155`); `contentref.go:158` `hiringComparableFloor = 6`; `stack-verify/e2e/tests/render-hiring-comparison.spec.ts` exists.
- `hiring.md`'s intra-corpus citations: `service_taxonomy.md:62` is the Database bullet naming `repos.yml:14-17`; `dependency_map.md:78` carries the quoted *"directly to the `public` schema (the legacy `jobsimulation` schema is non-authoritative)"*.
- `skillpath.md`: `session.go:205-207` (the `cms-in-app deseam` comment + `GetSkillPathDomain`), `skillpaths.go:88-95`, `intelligence.go:1144` + `:1159-1170`, `skill_path_session.go`/`skillpath_mixins.go` present and `local_skill_path_session.go` absent, `20260729133514.sql:62/:63` the two DROPs, the **three** migrations post-dating it at both `9d00a313` and `ad9f3c49`, `skillpath_sessions.graphqls` carrying all 8 named operations with `createSkillPathSession` `@deprecated` at `:80`, `app/CLAUDE.md:109` + `knowledge/architecture.md:28` still listing the service at `ad9f3c49` **and** at `2035f9a`, `SKILLPATH_STREAM=skillpath` surviving at `docker-compose.yml:72`, **zero** `*_RPC_ADDR` in the whole platform repo's YAML against an `AUTHORIZATION_ADDRESS` positive control, and repos.yml **9 → 6 → 4** at `2adcf71` / `0dab54d` / `838d907`. The next-web half is exact: `InsightsBySkillPathStudentSimulationsContainer.tsx` hardcodes `userData = null`, comments out the results table and the totals block, and renders `t('enterprise.insights.comingSoon')` = `"Coming soon"` in `en/enterprise.json:2101`.
- `studio-desk.md`: `docker-compose.yml:138-140` + `:141` @ `0c91421`, the historic `:337-341` (backend **+ cms**) @ `2adcf71`, `:119`/`:135` and the historic `:204` @ `0dab54d`; `vite.config.ts` rollupOptions.input = the 9 named prod entries + `dev-accept` behind `isProduction`; `src/routes/{ai,dev,skillpath,youtube}.ts`; `skillpath.ts` = 61,080 bytes; `youtube.ts:43` reads `YOUTUBE_API_KEY` only, `_mock: true` at `:46`/`:94`; `STUDIO_ACCESS_ROLES` at `src/index.ts:96` byte-for-byte as quoted, `checkEnterpriseAndAdmin` at `:99`, redirect to `WEB_APP_URL` at `:119-120`; `PORT` in-code fallback `9100` at `:60`; `engines.node ">=24"` + `node:24-alpine`; no DB driver in `package.json` and no `DATABASE_URL` in `src/`; `app/services/studioDB.js` + `USER_STUDIO_PREFERENCES` in `app/services/graphql/{queries,mutations}.ts`; `main.ts:97` `preloadCriticalCSS()` → `:104/:117` Sentry/posthog → `:182` `clerk.load()` → `:191` `l12nService.init()` → `:199` `canAccess()` → `:206` `new PageWrapper()`, i.e. exactly the three blocking awaits and both quoted line numbers, with the GlitchTip tailscale-funnel DSN at `:105-107`; the three hardcoded `https://app.anthropos.work` sites (`pageWrapper.js:149`, `userProfile.js:148`, `userProfile.js:302`); all **five** studio demopatches present in pinned rext plus `run-studio-fcp.sh`, `frontend_lines()` at `gen_injected_override.py:379` with the M252 clone-`.env` note, both named injection tests, and the `autoverify.sh:550/:557` container-side provider-key check.
- `next-web-app.md`: `Dockerfile.dev` = the only Dockerfile, `FROM node:24-alpine`, `pnpm turbo build --filter=@anthropos/web-app`; the repo `CLAUDE.md:15` *"Next.js 16 App Router"* and `:55` the `middleware.ts → proxy.ts` sentence (and `:82-84` really does still say "Two Dockerfiles", which the corpus correctly flags as stale); `proxy.ts` in web/hiring/integration and **no** `middleware.ts` anywhere; no `.storybook/` and no `storybook` script, only `configs/tailwind/storybooks.css`; the public allowlist members; `/print` gated by `validatePrintToken` at `proxy.ts:66-72` with `PRINT_ROUTE_SECRET` read in `packages/core-js/src/security/printToken.ts:16,:49`; `test: jest` in web+hiring only; dev ports 3000/3001/3002 and Expo `--port 3031`; `apps/mobile` excluded from `pnpm-workspace.yaml`; `antd ^6.3.7`; `graphql-request` + `@tanstack/react-query` + codegen client-preset with **zero** `@apollo/client`; `/logpoint/*` rewrites and `content.anthropos.work` in `next.config.mjs`; compose `:151`/`:160` @ `0c91421` and `:236` @ `0dab54d`; `depends_on: backend` at `:165-167` with backend `profiles: [core, backend, all]` at `:110`; `Makefile:119-120` `up-frontend` = `--profile core --profile frontend`, `PROFILE ?= core` at `:10`.
- `gotenberg.md`: `docker-compose.yml:183` `profiles: [core, backend, all]` with the file at **186** lines; image/command/ports byte-equivalent; `GOTENBERG_URL=http://gotenberg:3200` at `:57`; `extract.go:77-81` convert-then-`ConvertFromReader`, `:59` and `:43-45` the blank-URL degradation; `user_import_resume_2d.go:68` and `:69-74` the OCR-only fallback with `ocrInput = pdfBytes`; `converter/gotenberg.go:13` `Timeout: 90 * time.Second` and `:31` `POST …/forms/libreoffice/convert` with the documented signature.

## What I could not settle

1. **Everything internal to the five library repos.** None of `colony`, `proto`, `ai`, `authn`, `taxonomy` is in the clone set (correctly — `shared_libraries.md:36-37` says so). So `:56` (colony `go 1.25.0` / `golang:1.26-bookworm`), the whole colony package table `:64-70`, `:72-75` (Sentry gating, `rate.NewLimiter(1,5)` logging-only), `:84` (buf CI `v1.57.0`), `:112-115` (`buf.yaml` layout, `~10` `go/simulator/*` packages), `:175` (authn tag `v1.7.0`, the missing `return` after the websocket skip), `:212` (taxonomy `v1.2.0`), `:220` (a **131-line** `node.go`), `:247-261` (the ID grammar and the three worked examples) are **unverifiable from this tree**. Two consumer-side corroborations only: `storage/internal/migration/migration.go:13` really does alias `go/simulator/storage/v1` as `legacyStorage`, and `app/internal/authorization/sentinel.go:13` imports `colony/authorization` under an alias while sibling files call `authorizer.*` — consistent with the package being named `authorizer`. Settled by: cloning the five repos at the pins the rows name, or a fence that reads them at build time.
2. **`chronos` colony `v0.30.1`** (`shared_libraries.md:57`) — the repo is archived and not on disk.
3. **`skillpath.md:28` — "the table was kept but holds 0 rows"** in the legacy `skillpath` schema, and `hiring.md:42/:201` — the `jobsimulation` schema surviving **frozen in production** until M710, plus the prod *"443"* `job_position` rows. All three are statements about a live production database; nothing in this tree can measure them. `askengine/registry.go:192` corroborates the M710 *horizon* (the doc says so explicitly and correctly declines to read it as evidence of physical presence).
4. **Live-run measurements.** `hiring.md:428-467` (the 5/5 render gate, per-sim cohort sizes 8/8/9/9/8, scores 27–100, recruiter p95 1.27 s), `studio-desk.md:412` (skeleton p95 4669 → 817 ms on demo-2) and `:423` (`canAccess` 4049 → 38 ms, FCP 6936 → 2152 ms on `billion`) are timestamped measurements on hosts that no longer exist to me. The *harnesses* they name all exist in the pinned rext clone (`render-hiring-comparison.spec.ts`, `run-studio-fcp.sh`), which is as far as a read-only pass reaches. Settled by: a re-run recording its own refs (§5 rules 12 and 26).
5. **`gotenberg.md:64` — `curl http://localhost:3200/health` returning `{"status":"up"…}`.** `gotenberg/gotenberg:8` is a third-party image and I did not start a stack. The compose flags (`--api-port=3200`, `--api-timeout=60s`, `--libreoffice-restart-after=50`) all verify against `docker-compose.yml`.
6. **`next-web-app.md:80` — the downstream third-party list** (Metabase, Bunny token-signing, Stripe, Better Stack). Each name appears in the repo, but "which of these the app actually calls at runtime" needs a running app; I did not attempt it.
7. **`shared_libraries.md:126` — "`sentinel` never required it [`ai`]".** Verified true at `f2c46190` and `88bc5592`; the universal "never" would need sentinel's full history, which I did not walk.
