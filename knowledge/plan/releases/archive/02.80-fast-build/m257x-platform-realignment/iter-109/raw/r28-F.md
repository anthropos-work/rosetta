# Seat F — reading #28

**Ground truth re-verified at this reading's open** (all clones at the ADDENDUM's superseded shas, no drift):
`platform 0c91421d` (= origin/main) · `app ad9f3c49` (= origin/main) · `next-web-app 8297c684` (origin/main `f97ba659`) ·
`sentinel f2c46190` · `studio-desk 41ee3575` · `ant-academy 22df69dd` · `storage 4ce8ece5` (origin/main `9f8cb532`) ·
`messenger fa47850d` (origin/main `e9421c68`) · `cms ca50c817` (origin/main `f38c0c4a`) · `graphql-wundergraph 60c229f3` ·
`roadrunner 87d8d443` · `jobsimulation 462343b0` (origin/main `82cb66ec`) · nested `app/studio` + `cms/studio` @ `aeec036a`.
rext: pinned per-stack clone `stack-demo/rosetta-extensions 09d06070` · authoring copy `.agentspace/rosetta-extensions 680e8529`.
**Every rext claim below was checked in BOTH trees and they agree at every anchor I read.**

Method note on absences: every "0 hits" below was taken with `git grep` **at a named ref**, paired with a positive
control in the same pass, and — where the predicate could reach the nested repos — re-run inside
`stack-demo/app/studio` at its own ref `aeec036a` (§5 rule 44). Two of my own instruments failed mid-pass and were
repaired before any conclusion was drawn: a `git grep -q … -- "$dir/$f"` loop that double-prefixed the path (produced a
bogus "135 files declare no schema") and a `UserMixin{}` grep that was silently matching **`CreatedByUserMixin{}`** — the
latter would have manufactured a false BLOCKER against the tenancy fence.

## Positive controls

| file | `wc -l` | read in full? |
|---|---|---|
| `corpus/services/ant-academy.md` | 503 | yes |
| `corpus/architecture/architecture_overview.md` | 431 | yes |
| `corpus/services/storage.md` | 248 | yes |
| `corpus/services/sentinel.md` | 166 | yes |
| `corpus/services/ai-labs.md` | 158 | yes |
| *(total)* | **1506** | |

## Blockers

### B1 — sentinel.md asserts a `messenger` compose block that no longer exists, and contradicts itself twice in the same file

- **Anchor:** `corpus/services/sentinel.md:5` —
  > **`messenger` is not a caller** — its compose block sets no `AUTHORIZATION_ADDRESS` and declares no `depends_on: sentinel`, and its Go source imports no authorization client at all

- **Why it is false:** the sentence is present-tense and unpinned (the ref `0dab54d` in the *preceding* parenthetical
  is a different claim in a different sentence — §5 rule 33: a pin's scope is the claim's own block), so it grades at
  the platform checkout `0c91421d`. **There is no `messenger` service block there at all.** Measured:
  `git -C stack-demo/platform grep -n messenger 0c91421d -- docker-compose.yml common.yml repos.yml` returns only
  three *comments* on the `backend` block (`docker-compose.yml:84`, `:91`, `:102`) and one comment in `repos.yml:5`;
  `git show 0c91421d:docker-compose.yml | grep -E '^  [a-z…]*:$'` enumerates **five** services —
  `sentinel`, `backend`, `studio-desk`, `next-web-app`, `gotenberg`. The block was deleted by `838d907`
  (*"chore(compose): drop the storage, messenger and customerio-sync containers"*, merged `0c91421`, PR #26), whose own
  body says it removed *"the `storage`, `messenger` and `customerio-sync` service definitions, with their build
  contexts, env blocks, ports … depends_on edges and the `storage-legacy` / `messenger` / `customerio-sync` profiles."*
  (It was true at `0dab54d`, where messenger's block starts at `docker-compose.yml:156` and sets no
  `AUTHORIZATION_ADDRESS` — only `:48` does. So the sentence is a refutation that expired, §5 rule 31.)

  **And the same document says the opposite twice.** `sentinel.md:85`: *"The blocks that used to carry it are **gone**
  rather than corrected: `jobsimulation`, `cms` and `roadrunner` at `d11a403`, then `storage`, **`messenger`** and
  `customerio-sync` at `838d907`."* `sentinel.md:89`: *"`messenger` and `storage` never called it, and **neither is a
  compose service any more** (deleted at `838d907`)."* A reader reconciling `:5` with `:85`/`:89` is left adjudicating
  rather than reading.

  The **third** conjunct of the sentence is true and I verified it separately, which is why this is a defective
  *evidence clause* rather than a wrong conclusion: `git -C stack-demo/messenger grep -n "authorization\|AUTHORIZATION_ADDRESS\|sentinel" fa47850d -- '*.go' go.mod` returns exactly one hit, an unrelated comment
  (`pkg/aireadinessemail/override.go:378`, *"fallbackName is the sentinel used when…"*), against a positive control of
  `colony` present in `go.mod`.

- **Why I hesitated:** the *conclusion* ("messenger is not a caller") is TRUE, and the false clause is supporting
  evidence rather than the headline. Booked as a BLOCKER under the instrument's tie-break rule and because it is the
  self-contradiction class (§5 rule 21), which is the cheapest class in any corpus and the one an intra-file check
  should never miss.
- **Confidence:** medium

## Minors

- `corpus/services/sentinel.md:44` — *"only as a fixture string in sentinel's own tests
  (`internal/authorization/casbin_test.go`, `internal/rpcsrv/rpc_test.go`)"* omits a **third** file:
  `internal/authorization/manager_test.go:21` is literally `manager := "manager"`, used as a `g2` role fixture at
  `:30`/`:37`. Enumerated at sentinel `f2c46190`: quoted `"manager"` occurs in `casbin_test.go` (4), `rpc_test.go` (5),
  `manager_test.go` (1), plus `manager.go:20` (a logger label, not a role). The headline claim — *there is no `manager`
  role*, 0 hits in `init_policy.sql` — is correct. Omitted list member ⇒ MINOR.
- `corpus/services/storage.md:29` (cell end) — *"**Zero** `os.Getenv` sites, in `main.go` or in any of the three
  `cmd/` tools"*. Read literally this is false: `storage/main.go:16` @ `9f8cb532` is
  `cmp.Or(os.Getenv("SERVICE_NAME"), "storage")`, and `cmd/root.go` holds 5 more. The elided referent is plainly
  `STORAGE_RPC_ADDR` (0 hits repo-wide at `9f8cb532`, positive control: 30 files match "storage"), and the whole cell is
  about that variable — but the sentence has drifted far enough from its subject to read as an absolute.
- `corpus/services/ant-academy.md:304` — *"~26 Playwright e2e spec files (tests/e2e/)"*. Measured at `22df69dd`:
  **31** `*.spec.js` under `code/tests/e2e/` (40 entries total; 9 are helpers `_helpers.js`/`_academyBackendMock.js` and
  PNG snapshots under `widgets.spec.js-snapshots/`). Undercount ⇒ MINOR. (The sibling *"1000+ Vitest tests"* in the same
  cell is true — 2,707 `it(`/`test(` call sites across `tests/{unit,integration,api}`.)
- `corpus/services/ant-academy.md:176` — *"`code/public/catalog.json` (~2,667 entries)"*. Measured at `22df69dd`:
  `len(json["courses"]) == 2692`. ~1 % off under a `~`; non-material.
- `corpus/services/ant-academy.md:457` — the "Other content-pipeline helpers" row names 7 skills; `.claude/skills/`
  holds **16** directories and `export-path` is not named anywhere in the table (nor `_shared`, which is not a skill).
  `export-path` is load-bearing enough to appear in the repo-root `.env.example` as the owner of the content-write API.
- `corpus/architecture/architecture_overview.md:128` — the mermaid edge
  `Gateway -.->|spawns studio/gen.py in-process| Room`. The mechanism is `os/exec`, i.e. a **subprocess**, not
  in-process: `app/internal/cms/studio/studioManager.go:119` runs `s.runCommand(ctx, pyBin, append([]string{"studio/gen.py"}, tokens…))`.
  "In the same container / not a network hop" is what is meant, but *spawns … in-process* is self-cancelling.
- `corpus/architecture/architecture_overview.md:344-356` — the blockquote loses its `> ` prefix for thirteen lines
  mid-paragraph (`344` starts *"longer lists it, because…"*) and resumes at `:357`. CommonMark lazy continuation keeps
  it inside the quote, so nothing renders wrong today; the source is inconsistent and one inserted blank line would
  split the block.
- **Out-of-scope twin worth naming.** Root `CLAUDE.md` states ant-academy *"reads only from `code/.env.local`"*, while
  `ant-academy.md:391` states *"The **app's** env file is `code/.env`, not the repo root"* and drives the whole §2 with
  `cp .env.example .env`. **My file is on the correct side** — the repo's own root `.env.example:1` says
  *"NOT for the React app — that lives in `code/.env`"*, and Next.js loads both files. Not booked against my file set;
  recorded because the contradiction is live and `CLAUDE.md` is the highest-propagation site in the tree (§5 rule 19).

## Positively cleared (audited zeros)

**ENUMERATED** (predicate re-derived from source, cardinality compared before any arithmetic):

- **The multi-tenancy fence** — `architecture_overview.md:377-385`, *"only 31 of 135 schemas … the **29** live
  `OrganizationMixin{}` users … plus `Membership` and `Organization` … **23 schemas carry an `organization_id` with no
  policy at all** (16 is the neither-mixin subset of those 23)."* This is §5 rule 24's three-times-wrong claim, so I
  built the sets rather than the sums, at `app ad9f3c49`:
  - schema files = `git grep -l 'ent.Schema' -- internal/data/ent/schema/` → **135** (139 `.go` files; the 4 non-schemas
    are `mixin.go`, `database_types.go`, `skiller_mixins.go`, `skillpath_mixins.go`).
  - `OrganizationMixin{}` → 30 files, of which `user_resource.go:22` is `// OrganizationMixin{},  // We need to work on this`
    → **29 live**, listed one per file.
  - only **5** `Policy() ent.Policy` declarations exist in the whole directory (`mixin.go` ×2, `org_membership.go`,
    `organization.go`, `user.go`). `Organization.Policy()` filters by org (`organization.go:96`
    `rule.FilterSameOrganizations()`); `Membership.Policy()` filters by org (`org_membership.go:172`,
    `rule.AllowCurrentOrgEdgesOrSkipRule()`, ending `privacy.AlwaysDenyRule()`); `User.Policy()` filters by **user**
    (`user.go:116`, `rule.FilterSameUserRule()`) and is correctly excluded. Neither `organization.go` nor
    `org_membership.go` uses the mixin, so there is no double count → **29 + 2 = 31**. ✔
  - unpoliced side: 49 files contain `"organization_id"`; minus the 31 policed and minus the non-schema
    `skiller_mixins.go` → 21 schemas; plus the 3 `OrganizationIDMixin{}`-only schemas that never spell the string
    (`similarity`, `studio_document`, `studio_task`) → **24**; minus `academy_feedback.go`, the one member carrying
    `UserMixin{}` (owner-scoped policy) → **23**. ✔ And the split: 7 `OrganizationIDMixin{}` users
    (`category`, `jobrole`, `similarity`, `skill`, `specialization`, `studio_document`, `studio_task`) + **16**
    neither-mixin — my 16 is name-for-name identical to the 16 `security_compliance.md` lists. ✔
    **Both conjuncts of the sentence hold** (§5 rule 17): `OrganizationIDMixin` and `CreatedByUserMixin` declare no
    `Policy()`, so "no policy at all" is literally true of all 23.
- **`architecture_overview.md:80` — `authn` is not a fifth imported library.** Enumerated every clone with a `go.mod`:
  exactly **7** (`app`, `cms`, `jobsimulation`, `messenger`, `roadrunner`, `sentinel`, `storage`).
  `github.com/anthropos-work/authn` → **0/7**; positive control `colony` → **7/7**. ✔
- **`architecture_overview.md:59` — `next: ~16.2.12`, tilde, in four `apps/*`.** `apps/` holds exactly 5 dirs;
  `hiring`, `integration`, `maintenance`, `web` each declare `"next": "~16.2.12"`; `apps/mobile` declares none;
  `pnpm-lock.yaml:10780` resolves `next@16.2.12`. All at `next-web-app 8297c684`. ✔
- **`architecture_overview.md:223` — "5 services remain declared, 7 in the effective topology".**
  `docker-compose.yml` @ `0c91421d` declares `sentinel`/`backend`/`studio-desk`/`next-web-app`/`gotenberg`;
  `include: common.yml` adds `postgresql` + `redis` (and **nothing else** — `common.yml` does *not* contain `sentinel`).
  5 + 2 = 7. ✔ The **`core` = five containers** claim follows from the only four `profiles:` keys in the file
  (`:110`, `:141`, `:168`, `:183`) plus the three services with no key. ✔
- **`sentinel.md` Casbin model — "6 request types, 6 policy types, 3 role groupings, 6 matchers".**
  `internal/authorization/casbin.go` @ `f2c46190`: `r,r2..r6` = 6; `p,p2..p6` = 6; `g,g2,g3` = 3; `m,m2..m6` = 6. ✔
  Each matcher row of the table matches its expression, incl. `m`'s `p.tier == 'TIER_FREE'` substitution from
  `authorizationv1.Tier_TIER_FREE.String()`.
- **`sentinel.md` RPC table — 19 methods.** `internal/rpcsrv/rpc.go` declares exactly 19 `RPCHandler` methods; the
  table's 19 names are the same 19, no extras, no misses. ✔
- **`sentinel.md:85` — `AUTHORIZATION_ADDRESS` in exactly one block.** 1 occurrence across `docker-compose.yml`,
  `common.yml`, `.env_example` @ `0c91421d` (`docker-compose.yml:48`), and the residual-check list
  (`gotenberg`, `studio-desk`, `next-web-app`) is exactly the complement of `{sentinel, backend}` in the 5-service set. ✔
  Companion anchors all resolve: `GOTENBERG_URL` `:57`, gotenberg declared `:170` / `profiles: [core, backend, all]`
  `:183`, `JUDGE0_BASE_URL` `:59`, `REDIS_ADDR` `:66`, `app/internal/converter/gotenberg.go:31` @ `ad9f3c49`, and the
  cross-reference `architecture_overview.md:335` is byte-exact.
- **`sentinel.md:40` role groupings** — `app/internal/data/ent/enum/membership.go:8-15` spans all four
  `MembershipRole` values; `init_policy.sql:88-118` is exactly the `content_creator` block (file is 119 lines);
  `init_policy.sql:63-66` is exactly the *"org:feature:taxonomy:write is NOT seeded as a default"* note. ✔
- **`storage.md` RPC surface — 5 methods.** `internal/rpcsrv/rpcsrv.go` @ `9f8cb532` declares `PutObject`, `GetObject`,
  `PutPublicObject`, `GetPresignedUrl`, `GetPublicObject`; the doc's 3 private + 2 public tables are that set. ✔
- **`storage.md:219` — "the block's seven `environment:` entries are identical at `2adcf71`".** `0dab54d:117-123` and
  `2adcf71:204-210` are the same 7 keys in the same order. ✔ The env table's 7 non-`(empty)` claims line up with them.
- **`ant-academy.md:308-325` — the complete `isPublic` matcher.** Enumerated from `code/proxy.js:112-188` @ `22df69dd`:
  25 unconditional patterns + 2 under `DEV_LOGIN_ENABLED` + 3 under `VISUAL_BYPASS` = 30. The doc's nine groups name
  **all 30 and nothing else**, and `/api/chapters/*` is indeed absent from the matcher. ✔
- **`ant-academy.md:273-280` — "`TopBar`'s surface set is SEVEN routes, not five".** `TopBar` is mounted by exactly 5
  components (`AcademyClient.jsx:1906`, `BookmarksClient.jsx:508`, `CourseClient.jsx:2091` + `:2141`,
  `LatestClient.jsx:128`, `MyActivityClient.jsx:161`); `MyCertificatesClient.jsx` imports none. `AcademyClient` is
  rendered by exactly three routes — `app/(authed)/page.jsx:151`, `courses/page.jsx:92`, `courses/[slug]/page.jsx:219`.
  3 + 1 + 3 = **7**. ✔ And `PublicHeader` has exactly **one** mount site (`app/(public)/library/page.jsx:28`); `/free`'s
  entire body is `redirect('/?tier=free')` at `app/(public)/free/page.jsx:18`. ✔
- **`ant-academy.md:63` — "`academy.graphqls`, 1 of 43".** `app/internal/web/backend/graphql/graph/schemas/` holds
  exactly 43 `.graphqls`, one of them `academy.graphqls`. ✔
- **`ant-academy.md:226-229` — the five bring-up patches at `:56`, `:66`, `:75`, `:84`, `:90`.** Verified in **both**
  rext trees: `demo-stack/ant-academy.sh` assigns `DEVORIGINS_PATCH` `:56`, `FSPUB_PATCH` `:66`, `FSBODY_PATCH` `:75`,
  `FSPUBLIC_PATCH` `:84`, `COCKPIT_PATCH` `:90`, and all five revert under `--stop`; `demo-stack/patches/` contains the
  five named directories. ✔
- **`ai-labs.md:20` — the un-built shared purse.** `checkout.session.completed` → **0** hits in Go source at
  `ad9f3c49` (positive control: `internal/credits` imported by 5+ files); `internal/labs/` imports `internal/credits`
  **nowhere**; `git describe --tags ad9f3c49` → `v1.369.0-7-gad9f3c498` and `2035f9a4` → `v1.369.0-2-g2035f9a40`, both
  exactly as written; `knowledge/plan/releases/06.00-shared-purse/` exists with milestone dirs **m600 … m607**
  (+ `m601b`) — directory *names* only, no file read.

**RE-DERIVED** (arithmetic/anchors re-checked, the weakest form of clearance — listed for completeness):

- Every `file:line` I opened in the four platform-source-heavy passages resolved to the construct named, at the ref the
  passage names: `jobsimulation/terraform/main.tf:15-22` and `:24-40` **at `6092c6d2`** (the checkout `462343b0` still
  has the module — the pin is doing real work, and `service_desired_count` is indeed absent at `6092c6d2`);
  `cms/terraform/main.tf:39` `= 0` at both `ca50c817` and `f38c0c4a`; `6efa1d5` subject verbatim;
  `graphql-wundergraph/terraform/main.tf:20` `= 1` and `locals.tf` `port = 8080`; `roadrunner terraform/main.tf:19` `= 1`;
  `storage/terraform/main.tf` = 18 lines at `9f8cb532` with `:9-11` and `:13-16` as quoted, `= 0` at `63bffc8`,
  100 lines at `4ce8ece5` (which is exactly **20** behind), `storage.tf:22-25` the M903 note, `d3e6d32` (2026-08-05)
  containing *"M903 never ran."*, `custody` **0** occurrences in the repo, no `moved` block;
  `STORAGE_RPC_ADDR` = 3 comment hits at `9d00a313` (`main.go:451`, `wiring.go:101`, `callsites_test.go:189`) and
  29 lines / 18 files repo-wide; 15 hits / **7 live env reads** at `b948604f`, where `env_guards.go` genuinely does not
  exist and `:518-523` / `:529-535` genuinely name the public-storage clients and the academy asset uploader;
  `main.go:504` @ `ad9f3c49` carries the quoted comment and `git diff --stat 2035f9a4 ad9f3c49 -- main.go internal/storage/`
  is empty; `env_guards.go:37-44` `deployedEnvironment()`; `docker-compose.yml:56/73-79/82/83/100` and `README.md:81-87`;
  `:210` and `:324` @ `2adcf71` (the latter genuinely inside `studio-desk`, block `:311-343`); `:119` @ `0dab54d`;
  `repos.yml:18-20` @ `0dab54d`; `app/internal/jobsimulation/ai/ai.go` `:85-88`, `:129`, `:166`, `:262-276`, `:325`;
  `internal/cms/studio/markdownManager.go:29-30`, `studioManager.go:583`; all three `app/studio/configs/*.ini` pin
  `azure` (read at the nested repo's own ref `aeec036a`); `d11a403`'s repos.yml diff deleting all three `- name:`
  entries while its message says roadrunner's was already gone; `ant-academy` `store.js:150/162/169/202/210/215`,
  `writeThrough/index.js:247/259`, `beacon/route.js:1-18`, `serverTenant.js:111-113/115-117/145`,
  `ucourses/catalog.js:31/961`, `AcademyClient.jsx:1363-1365`, `LanguageSelector.jsx:88/97`, `TopBar.jsx:76`,
  `i18n/locale.js:10`, `PublicHeader.jsx:20`, `layout.jsx:1/41/51/132`, `academy.css:1`,
  `5b05b7d9` (2026-05-05, the *only* commit that ever touched `LanguageSelector` in `TopBar.jsx`) and `e22f3230`
  (2026-05-18, the 5-extra-locale commit); `cockpit.py:327/812/1496` and `ant-academy.sh:576-583` in both rext trees.
- `ai-labs.md`'s numeric surface: `creditCost{build:5, refine:1, translate:1}`, `purchasePackages{50,200,500}`,
  `MarginMarkup = 1.40`, `PricePerCreditUSD = 0.45`, `DefaultSeedBalance` 500, `stripe-go/v74`, labs-api default
  `:7070`, `labsdemo` `:8099`, the 30 s reconciler interval, the five `CapReason` values, the three `LabsTier` values,
  all five migration filenames, `TestPricingDerivation` / `TestPrivacyBackstop_AutoScopesToCallerOrg`,
  `internal/payments` having no `_test.go`, and the Stripe webhook handling `customer.created` +
  `customer.subscription.{created,updated,deleted}` only (`internal/web/backend/api/api.go:319-327`).

## What I could not settle

- **`architecture_overview.md:3` — *"the repo is ARCHIVED on GitHub (2026-07-30)"* (graphql-wundergraph).** Archive
  state is a GitHub API property, not a clone property. *Consistent with* the clone: `origin/main` `60c229f3` is dated
  **2026-07-30** and nothing lands after it. Settled by `gh api repos/anthropos-work/graphql-wundergraph --jq .archived`.
  (The same document's jobsimulation row at `:20` handles the symmetric case correctly — I confirmed the four
  **2026-08-04** commits incl. `Merge pull request #439`, which is exactly the evidence it cites.)
- **`architecture_overview.md:186` / `:29` / `storage.md:189` — compose exit codes** (*"selecting `frontend` alone
  exits 1"*, *"asking for a retired token exits 0 and starts only the floor"*). Both are runtime behaviours of
  `docker compose`; I have no daemon. The *structural* premises are verified (`depends_on: backend` at `:138-140`
  and `:165-167`; `postgresql`/`redis`/`sentinel` declare no `profiles:`), and the claims are uniform across
  `sentinel.md`, `architecture_overview.md`, `storage.md` and `CLAUDE.md`. Settled by two `docker compose --profile … config`/`up` runs.
- **`storage.md:25`'s quotations from `app/knowledge/plan/releases/09.00-support-in-app/m903-s3-custody/`**
  (`overview.md` front-matter `status: planned`; `progress.md` *"Planned, not started — no terraform applied, no
  branch"*). The directory and both named files **exist** at `app 2035f9a4` (verified by `git ls-tree`, names only).
  I did not open them: HARD BAR 1 is path-scoped and I read it strictly. Settled by anyone permitted to read that path.
- **Taxonomy magnitudes** — `architecture_overview.md:13`/`:236` *"≥42,790 skills / ≥22,470 job roles (measured public
  subset, 2026-06-29)"*. Needs a DB read (`/db-query`), not available from clones.
- **`ant-academy.md:90` — *"a grid of 65 real cards"*** and `:222` *"HTTP 404 → HTTP 200, proven live on `billion`"*.
  Live-stack observations; not reproducible from clones. (The mechanism behind them *is* verified: the
  `academy-fs-published-*` patches exist and are wired at the five lines cited.)
- **`ai-labs.md:5-6` — the `anthropos-work/AI-Labs` repo.** Not in any clone set, so its existence, language and role
  are unmeasurable here. Everything on the `app` side of that boundary (`LABS_API_URL`, `labsapi/client.go`,
  `LABS_API_PLATFORM_TOKEN`, labs-api owning session-ID generation, `ReportEvent` as the callback) is verified.
- **`architecture_overview.md:423`** — VPC/subnet layout ("public subnets (ALB, Cosmo Router)"). The
  `infrastructure` repo has never been in a clone set; the document says as much elsewhere (`:222`).
- **`architecture_overview.md:179`/`:221` — "its 23 run-state tables"** (jobsimulation → `public`). I did not find a
  derivation I trusted inside my budget; it needs the jobsim migration set diffed against `public`, and a wrong
  enumeration here would be worse than a silence (§5 rule 24).
