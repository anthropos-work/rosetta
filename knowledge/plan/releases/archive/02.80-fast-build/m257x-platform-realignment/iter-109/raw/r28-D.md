# Seat D — reading #28

**Ground truth verified at open AND close** (`git rev-parse` over every nested tree; all identical to the
ADDENDUM's superseded table): platform `0c91421d` (= origin/main) · app `ad9f3c49` (= origin/main) ·
app/studio `aeec036a` · next-web-app `8297c684` (origin/main `f97ba659`) · sentinel `f2c46190` ·
studio-desk `41ee3575` · ant-academy `22df69dd` · storage `4ce8ece5` (origin/main `9f8cb532`) ·
messenger `fa47850d` (origin/main `e9421c68`) · cms `ca50c817` (origin/main `f38c0c4a`) · cms/studio
`aeec036a` · graphql-wundergraph `60c229f3` · roadrunner `87d8d443` · jobsimulation `462343b0`
(origin/main `82cb66ec`) · **rext pinned consumption clone `stack-demo/rosetta-extensions` `09d06070`**
· rext authoring copy `.agentspace/rosetta-extensions` `680e8529`.

**Which rext tree I read, per rule 45 / the ADDENDUM:** every claim in `clerkenstein.md` about what the
mirror/tooling *does on a stack* was settled at the **pinned per-stack clone `09d06070`**. Where the claim
could plausibly be about authoring state (the alignment DNAs, `alignctl`'s exit codes, `store.go`'s M219
fix) I read **both** trees and they are byte-identical on every anchor cited — stated so a reader does not
have to guess which one carried the verdict.

**Note on a DIRTY working tree:** `stack-demo/ant-academy` has 4 modified files (demopatch residue:
`code/public/catalog.json`, `code/src/components/UserMenu.jsx`, `code/src/lib/serverChapterBody.js`,
`code/src/lib/serverTenant.js`). Every ant-academy anchor below was therefore graded with
`git show HEAD:<path>`, not from the worktree — the corpus's `serverTenant.js:145` claim is TRUE at HEAD
and would have read FALSE off the dirty file.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/architecture/service_taxonomy.md` | 514 | yes |
| `corpus/services/clerkenstein.md` | 374 | yes |
| `corpus/architecture/security_compliance.md` | 281 | yes |
| `corpus/services/messenger.md` | 204 | yes |
| `corpus/architecture/frontend_architecture.md` | 116 | yes |
| `corpus/services/db-backup.md` | 31 | yes |
| *(total)* | **1520** | |

Search-pipeline controls run in the same pass, per §5 rules 1/2/44: the shell's `grep` is `ugrep` and
**rejected `--include=*.go` with a stderr I read** (`no matches found: --include=*.go`) — the first cookie-cutter
"0 hits" of this pass was a broken pipeline, not an absence. Re-run with `git grep` + a positive control
(`SetCookie` → 0 but `Set-Cookie` → 5, `func ` → non-zero) before any zero below was written down. Every
tree-wide zero enumerates its search set across **all 15 git trees including the two nested
`studio` checkouts at `aeec036a`**.

---

## Blockers

### B1 — `security_compliance.md:22` places the Cosmo Router in the PUBLIC subnets; its own terraform puts it in the private ones

- **Anchor:** `corpus/architecture/security_compliance.md:22` —
  `- **Public subnets**: Application Load Balancer (ALB), Cosmo Router`
  (immediately followed by `:23` `- **Private subnets**: All microservices (no direct internet access)`)
- **Why it is false:** the router's own deployment declaration passes **only** private subnets:
  `graphql-wundergraph/terraform/main.tf:31` → `private_subnets_ids = var.platform_private_subnets_ids`,
  and the module takes no public-subnet argument at all. I enumerated the predicate rather than sampling
  it: **`public_subnet` occurs 0 times across all eight service terraform trees** in the clone set
  (`app`, `sentinel`, `graphql-wundergraph`, `messenger`, `cms`, `roadrunner`, `storage`,
  `jobsimulation`), and **every one of the eight** passes exactly the same
  `private_subnets_ids = var.platform_private_subnets_ids`.
  The distinguishing property of the router is *not* subnet placement but ALB fronting — and that is not
  distinguishing either: `graphql-wundergraph/terraform/main.tf:24-25` (`alb_listener_arn`,
  `listener_rule_priority`) is **identical** to `app/terraform/main.tf:185-186`, and both use
  `modules/services/base_service` (vs `base_internal_service` for sentinel/cms/messenger/roadrunner/
  storage/jobsimulation). So the corpus singles out the router for a public-subnet placement it shares
  with nothing, while `backend` — which has byte-identical ALB wiring — is filed under
  *"Private subnets: All microservices."* The two bullets are also mutually inconsistent on their own
  terms once the router is read as a service.
  Corpus-wide, not a local typo: `architecture_overview.md:423` repeats it
  (*"public subnets (ALB, Cosmo Router), private subnets (all microservices)"*).
- **Why I hesitated:** the `infrastructure` repo that defines `base_service` has never been in any clone
  set, so I cannot read what the module does with the ids it is handed. What I *can* measure is that the
  router is handed private subnet ids and nothing else, exactly like every internal service. Booked
  because a security document's network-placement statement is acted on directly, and because the only
  measurable evidence points the other way.
- **Confidence:** medium

### B2 — `security_compliance.md:252` fences "Both bullets above" over a list of THREE bullets, leaving the Limited-Risk classification itself asserted in the corpus's own voice

- **Anchor:** `corpus/architecture/security_compliance.md:252` —
  `> **Both bullets above are what is STATED, not what this corpus asserts** — including the consequence`
  `> bullet.`
- **Why it is false:** the `### EU AI Act` list above it has **three** members, not two —
  `:227` *"AI Simulations classified as **Limited Risk** (not High Risk)"*, `:228` *"Stated reason: …"*,
  `:229` *"Stated consequence of that classification: …"*. The sentence's own gloss (*"including the
  consequence bullet"*) resolves the pair as `:228` + `:229`, which leaves `:227` — the classification
  itself, and the only one of the three that is not self-labelled *"Stated"* — **outside** the fence and
  therefore reading as the corpus asserting a legal classification.
  That is precisely the failure this blockquote exists to prevent: the same blockquote, sixteen lines
  earlier at `:248`, says *"**Do not cite this section as evidence of a Limited-Risk classification** —
  re-derive it."* And `:7` of this same file states the classification unhedged in the summary
  (*"AI Simulations are classified as **Limited Risk** under the EU AI Act"*), so the un-fenced bullet is
  not an isolated slip. The retraction and the thing it retracts disagree about their own scope.
  (This is a *documentation-scope* defect, not a legal determination — §5 rule 48's routing rule does not
  apply, and the repair is a quantifier, not a compliance opinion.)
- **Why I hesitated:** "Both" can be read charitably as "both *rationale* bullets", under which the count
  is defensible and only the fence's reach is ambiguous. The frozen instrument's grading rule resolves
  doubt upward, and the ambiguity lands on the one bullet the section says must not be cited.
- **Confidence:** medium

---

## Minors

- `clerkenstein.md:21` — *"ONE private monorepo with sections (`clerkenstein`, `demo-stack`,
  `stack-injection`, `stack-core`, `stack-seeding`, `alignment`)"*: the tree carries **11** sections at
  `09d06070` and `680e8529` alike — the six named plus `dev-stack`, `playthroughs`, `stack-secrets`,
  `stack-snapshot`, `stack-verify`. Omitted list members, no factual consequence for the page's subject.
- `clerkenstein.md:105` and `:286-288` — `cmd/` is described as `mintpk` + `fake-fapi` / `fake-bapi`;
  the directory also holds **`jwtkey`** (4 binaries, not 3).
- `security_compliance.md:73` and `:105` — cite **`mixin.go:98`** for `UserMixin`'s `Policy()`. The
  function is at `:99`; `:98` is its doc comment (`// Policy defines the privacy policy of the UserMixin.`).
  The twin citation in the same blockquote, `mixin.go:126` for `OrganizationMixin.Policy()`, lands on the
  `func` line, so the two are anchored one line apart from each other by construction. Both identify the
  right construct; line drift only.
- `security_compliance.md:201` — cites `external_services.md:602-607` as the passage that *"enumerates
  four live ways a request leaves the EU"*. `:602-607` is the paragraph that **states** the count; the
  enumeration itself is `:609` onward (items 1–5). Range points one paragraph short.
- `security_compliance.md:232-233` — `skills.go:53-64` for `calculateSkillScore`: the function is
  `:53-62`; `:63` is blank and `:64` is the next function's signature (`func (s skillsValidator) run`).
  Two-line overshoot. The `:75` divide-anchor in the same sentence is exact.
- `service_taxonomy.md:79` — the Backend/App row reads `8081-8083 (container: HTTP 8082, RPC 8083,
  meta 8084)`. `META_PORT=8084` (`docker-compose.yml:64`) is **not published** — compose maps only
  `8081:8081`, `8082:8082`, `8083:8083` — so `:8084` is unreachable from the host on a local stack. The
  parenthetical is honest about being container-side; flagged because a reader will try the port.
- `service_taxonomy.md:3` — the top banner asserts the router repo *"is ARCHIVED on GitHub (2026-07-30)"*
  flat. The *"read every date as 'asserted on', never as 'is'"* note at `:142` scopes itself to
  *"Every `ARCHIVED <date>` in this table"* — the banner sits above the table and outside that scope.
  (`:394` restates the same fact **with** the snapshot framing, so the disclosure exists; only the banner
  omits it. Archive state is unmeasurable from any clone, as the note itself says.)
- `messenger.md:120` — *"the binary still constructs four Connect-RPC clients"*. `cmd/root.go:118-142`
  constructs **five** client objects (`cmsV1Client` :118, `usersClient` :123, `organizationsClient` :128,
  `skillerClient` :133, `jobsimulationClient` :138) over **four** distinct addresses — `usersClient` and
  `organizationsClient` both read `BACKEND_USERS_RPC_ADDR`. The prose's own enumeration
  (*"CMS, backend users + organizations, skiller, and jobsimulation"*) groups them deliberately, and
  `:28`'s *"four addresses"* is exactly right, so there is no factual consequence.
- `messenger.md:79-96` — the *"Key directories"* tree lists 6 of the 12 `internal/flow/` source files.
  The five it omits (`ai_readiness.go`, `content_assigned.go`, `content_completed.go`, `coursebuilder.go`,
  `invitation_reminders.go`) are the homes of 9 of the 21 handlers the same document enumerates at `:17`.
  Selective by label; noted because the two passages are in the same file.

---

## Positively cleared (audited zeros)

Labelled per the briefing. **ENUMERATED** = I derived the predicate's set from source independently and
compared cardinalities. **RE-DERIVED** = I only re-computed arithmetic the document displays (the weakest
clearance; I avoided leaving anything here alone).

### The multi-tenancy fence — `security_compliance.md:66-137` — **ENUMERATED, and it HOLDS**

This is the fence the briefing flags as wrong four times and as the site of three simultaneous wrong
audited zeros (§5 rule 24). I enumerated every set from `stack-demo/app/internal/data/ent/schema/` at
`ad9f3c49` **before** touching any arithmetic:

| predicate | corpus | derived | method |
|---|---|---|---|
| `.go` files in the schema dir | 139 | **139** | `ls *.go` |
| files declaring `ent.Schema` | 135 | **135** | `grep -l 'ent.Schema'` |
| files declaring none | 4 | **4** — `database_types.go`, `mixin.go`, `skiller_mixins.go`, `skillpath_mixins.go` | `comm -23` |
| *mention* `OrganizationMixin{}` | 30 | **30** | `grep -l`, excluding `OrganizationIDMixin` |
| *use* it (compiles) | 29 | **29** | the one commented occurrence is exactly `user_resource.go:22` `// OrganizationMixin{},  // We need to work on this` |
| `OrganizationIDMixin{}` users | 7 | **7** — `category`, `jobrole`, `similarity`, `specialization`, `studio_task`, `skill`, `studio_document` | matches the corpus's named list member-for-member |
| plain `organization_id`, neither mixin | 18 | **18** | the doc's own `comm`/`xargs` derivation |
| of those, policed by other means | 2 | **2** — `org_membership.go` (only file of the 18 declaring `Policy()`) and `academy_feedback.go` (only file of the 18 carrying a **word-boundary** `UserMixin{}`) | per-file test, not a substring grep |
| unpoliced-with-`organization_id` (16 named) | 16 | **16** — the corpus's list of 16 filenames matches the derived set **exactly** | set difference |
| files declaring any `Policy()` | 4 (@ `5ba17044`) | **4** at `5ba17044` *and* at `ad9f3c49` — `mixin.go`, `org_membership.go`, `organization.go`, `user.go` | `grep -lE 'func \(…\) Policy\(\)'` |
| auto-filter by ORGANIZATION | 31 | **31** = 29 + `Membership` + `Organization` | see below |

Sub-anchors all resolve to the right construct, not merely to an existing line:
`mixin.go:126` = `func (OrganizationMixin) Policy()`; `org_membership.go:172-188` = `func (Membership)
Policy()` through its close, and it does end in `privacy.AlwaysDenyRule()` (`:185`), with
`rule.AllowCurrentOrgEdgesOrSkipRule()` at `:179`/`:183`; `organization.go:56` = `func (Organization)
Policy()` with `rule.FilterSameOrganizations()` at exactly `:96`, and `organization.go` uses **neither**
org mixin (its `Mixin()` at `:17` is `PrimaryKeyMixin`/`CreatedAtMixin`/`UpdatedAtMixin`); `user.go:116`
= `func (User) Policy()`. The pinned block at `:107-112` also re-derives at its own named ref
`5ba17044` — 139 / 30 / 7, and the bare-`*.go` glob really does return **19** there with
`skiller_mixins.go` as the 19th.

The four tenant-scoped members of the 81-schema remainder are real and correctly classed:
`lab.go:63`, `academy_chapter.go:84`, `academy_skill_path.go:78` each declare `field.String("tenant_eid")`,
carry **no** org mixin, **no** `organization_id` and **no** `Policy()`; `skill_path_session.go:43`
declares `field.UUID("tenant_id", …)` and carries `UserMixin{}` at `:26` — owner-filtered, never
tenant-filtered. `academy_feedback.go` does carry `organization_id` (`:129`) and `UserMixin{}` (`:64`),
exactly as `:105` says. The arithmetic 135 − 31 − 23 = 81 closes, and the sets that produce it are the
sets I derived.

**One trap I fell into and had to back out of**, worth recording because it is rule 3 in live form: a
naive `grep -l 'UserMixin{}'` returns **40** files — it matches **`CreatedByUserMixin{}`**, which
`category.go:30`, `jobrole.go:33`, `skill.go:33` and `specialization.go:31` all carry. The word-boundary
count is **36**. Had I used the naive number, I would have "found" the taxonomy schemas owner-policed and
booked the corpus wrong. The corpus's numbers are right at the boundary my first probe was wrong at.

### Other ENUMERATED clearances

- **The subgraph ladder** (`service_taxonomy.md:407`): re-derived from `supergraph-config-prod.yaml`'s
  own history rather than from the sentence — `749dc86^` = 5 (backend, skiller, jobsimulation, cms,
  skillpath) → `749dc86` = 4 → `7c17e63` = 3 → `915da06` = **1**. `915da06`'s diffstat deletes **both**
  `schemas/cms.graphqls` and `schemas/jobsimulation.graphqls`, so cms-in-app is the **3→1** step and the
  commit's own subject line (*"supergraph 2→1"*) is the wrong figure, exactly as the corpus says. Dates
  2026-06-24 / 2026-07-21 / 2026-07-29 all exact. All three configs at `60c229f3` carry one
  `- name: backend`; `schemas/` holds one file.
- **`MESSENGER_RPC_ADDR` occurs in no repo** (`messenger.md:20-22`): **0 across all 15 git trees at each
  tree's own ref, including both nested `studio` checkouts**, with `SKILLER_RPC_ADDR` as the positive
  control (app 21, cms 12, jobsimulation 9, messenger 4, rext 20). The platform-history control
  reproduces exactly: `git log -S 'MESSENGER_RPC' 0c91421d` → **0**, `-S 'SKILLER_RPC'` → **7**, with
  `--all` → **0** and **8**, over a **121**-commit history. Every figure in that blockquote is the figure
  I measured.
- **Zero `*_RPC_ADDR` in compose** (`service_taxonomy.md:425`, `messenger.md:120`, `:168-172`): the only
  `_RPC_ADDR` occurrences anywhere in the `platform` repo at `0c91421d` are two lines of `CLAUDE.md`
  prose *saying they do not exist*. `docker-compose.yml`, `common.yml` and `.env_example` carry none.
- **The messenger `backend`-stream handler count** (`messenger.md:17`): counted line by line in
  `internal/flow/flow.go` @ `fa47850d` — **22** `pubsub.EventHandler(` lines between `:72` and `:104`,
  of which `:83` is `// pubsub.EventHandler(h.OrgJobSimulationAssignmentPastDueHandler), // not
  implemented` → **21** live. The `OrgSkillPath*` sub-range `:74-78` is exactly five handlers. Three
  `AddSubscriber` calls (`backend` :72, `jobsimulation` :105, `cms` :109) match `app/main.go:1462-1465`'s
  *"messenger's three AddSubscriber calls"*.
- **The frontend REST/SSE table** (`frontend_architecture.md:41-50`): re-derived at **both** named refs
  over non-test `.ts`/`.tsx`, `e2e/` excluded. `bb3313bc` → 21 files / 29 occurrences / 43 `fetch(` /
  core-js 12 env / 25 calls. `8297c684` → **22 / 31 / 47 / 12 env / 27 calls**. `f97ba659` (origin/main,
  4 commits further) → identical 22 / 31 / 47 / 12 / 27, as the passage claims. `new EventSource(` = **0**
  repo-wide with a working control. The **prose enumeration accounts for all 22 files exactly**
  (4 core-js + 3 AI-readiness + invite + unsubscribe + member-analytics + 4 assignment + 3 Stripe +
  CSV-import + résumé-import + test-run panel + 2 admin backfill = 22). `talkToData/api.ts:214` carries
  the quoted sentence verbatim.
- **Alignment DNA gene/capability counts** (`clerkenstein.md:41-44`, `:74`, `:106`, `:250`): parsed the
  five JSON DNAs in the **pinned** clone — `clerk-2.6.0` **14 capabilities / 27 genes**; `clerk-js-5`
  **9**; `clerk-multi-1` **9** with a `Roster` capability of **2**; `clerk-deploy-1` **7**;
  `clerk-express-1` **13 across 5**. `cmd/` holds exactly `{clerkrun, jsfapirun, multirun, expressrun,
  deployrun}`; `dna/` five files; `golden{,-js,-multi,-express,-deploy}/` all five present; `scripts/`
  present **here** and absent from the sibling `rosetta-extensions/alignment/` harness, as `:359-361`
  says. `SessionToken/decoded-identity` is `criticality: critical` with `operator: exact`;
  `Client/signed-in` and `Me/universal-user` are both `shape`-graded — so `:246`'s conditional-emit
  argument names the right two. `clerk-js-5` has a `Me` capability and **0** occurrences of
  `organization_memberships`, confirming `:145-147`'s declared DNA gap.
- **`authn` is a dependency of nothing** (`service_taxonomy.md:509`): 0 `anthropos-work/authn` requires
  across all seven Go `go.mod`s; control — `colony` is required by all seven. `ai` survives only in the
  frozen `cms` and `jobsimulation` husks (both `v1.40.2`); `app` carries it in-tree at `internal/ai/`
  since `1e457fa70` (2026-08-04). Live pins: app + sentinel `colony v0.35.2`, storage + roadrunner
  `v0.34.3`, clerkenstein's artifact `go.mod:8` `v0.34.3` — so `clerkenstein.md:274-283`'s
  "artifact is behind the whole live platform" is exactly right, including `clerk-sdk-go/v2 v2.7.0`
  in `app/go.mod:29`.
- **The compose topology** (`service_taxonomy.md:65-129`, `:480-497`): `docker-compose.yml` at `0c91421`
  declares **5** services, `include: common.yml` adds 2 → **7** effective; exactly **4** carry a
  `profiles:` key, at the four cited lines `:110`/`:141`/`:168`/`:183`, and `sentinel` (`:5`) plus
  `common.yml`'s `postgresql` (`:2`) / `redis` (`:24`) carry none → the 3-service floor and `core` = 5
  containers. The declared/effective split really was **8/10** at `0dab54d`. `frontend`/`studio-desk`
  each declare `depends_on: backend` (`:165-167` / `:138-140`) their own profile does not select.
  Retired-token behaviour is what the doc says. `repos.yml` = 4 entries at `0c91421`, **9** at
  `2adcf71`. `d11a403` deleted the cms/jobsimulation/roadrunner compose blocks **and** their `repos.yml`
  entries in one commit, and at `d11a403^` all three carried the then-default `graphql` profile with
  `Makefile:10` `PROFILE ?= graphql` — so *"a bare `make up` started all three"* is measured, not inferred.
- **The deleted support containers** (`service_taxonomy.md:93-112`): at `0dab54d`, storage `8300-8301`,
  messenger `8200-8201`, customerio-sync `8080`, and `profiles: [customerio-sync, all]` at exactly
  `:154`. `838d907` deletes all three. The "one Brevo pusher, not two" claim holds at every ref in the
  window: `ENVIRONMENT=development` at `:56` → `deployedEnvironment()` false (`env_guards.go:37-44`) →
  unset switch resolves `(false, nil)` (`resolveSubsystemSwitch`, `:92-111`) → `main.go:394`
  `if customerIOSyncEnabled` never fires; and at the fold commit `3e5bc33ef:main.go:387` the gate was
  `deployedEnvironment() && os.Getenv("BREVO_KEY") != ""`.
- **The historical Directus service** (`service_taxonomy.md:362-366`): `a2a3ee6^:docker-compose.yml`
  `:384 image: directus/directus:10.10.1`, `:386 - 8055:8055`, `:409 - ADMIN_PASSWORD=password` — the
  correction-of-the-correction is right, line for line. The `:5050` retraction is right too:
  `2adcf71^` really did map `"5050:8080"` and bake `http://localhost:5050/graphql`.
- **jobsimulation's landed M810 vs cms's unsettled one** (`service_taxonomy.md:159-160`): at `6092c6d2`
  `terraform/main.tf` is 56 lines with **no** `module "jobsimulation"` block, and `:15-40` is exactly the
  decommission comment enumerating the LiveKit/Chime buckets, the SSM parameters and the atlas tracker.
  `origin/main` carries exactly **four** 2026-08-04 commits including **PR #439**. Meanwhile
  `cms/terraform/main.tf:39` is `service_desired_count = 0` in a **191**-line file at *both* `ca50c817`
  and `f38c0c4a`, and `6efa1d5`'s body says M810 *"deletes `module \"cms_euwest1\"` … which destroys the
  ECS service and the production-cms ECR repository."* Two measured facts, opposite directions,
  reported-both/asserted-neither — correctly.
- **messenger's prod/local split** (`messenger.md:40-52`): the ref discipline is load-bearing and correct.
  At `e9421c6` (= origin/main) `terraform/main.tf:29` is `service_desired_count = 0` with `:27-28` the
  in-file rollback comment; at the checkout `fa47850d` `:19` is `= 1` and `:29` is
  `container_definitions = <<EOF`. Grading the prod row at the clone genuinely returns the opposite
  verdict, and the clone is exactly **7** commits behind. `0dab54d:repos.yml:21-23` is the messenger
  entry; `0dab54d` really did move it `[messenger, all]` → `[messenger]`, with the two-consumers-on-one-
  group rationale in-comment at `:193-194`.
- **The `app`-side messenger consumer** (`messenger.md:51`): `main.go:15`/`:62`/`:63` are the three
  `internal/messenger/{flow,adapters,sender}` imports; `:1450` `pubsub.NewSubscriberServer`; `:1471`
  `msgadapters.Wire`; `:1473` `msgsender.NewFromEnv`; `:1416-1421` the literal-group comment quoting
  `cmp.Or(os.Getenv("SERVICE_NAME"), "messenger")`. `git diff --stat 2035f9a4 ad9f3c49 -- main.go` is
  **empty**, so the "resolves at both refs" claim is measured. `env_guards.go:61` is
  `envMessengerEnabled = "MESSENGER_ENABLED"`, and `git ls-tree b948604f -- env_guards.go` is empty —
  the file genuinely did not exist at the former pin, which is why `:62-65`'s insistence on the ref is
  correct rather than pedantic. The two pinned anchors also hold at their own refs:
  `main.go:1095` @ `9d00a313` is the `CMS_STREAM` publisher, `:1478-1484` the `streamNames{…}` binding,
  and `main.go:1205-1211` @ `b948604` is the *"Additive + DORMANT … until the M809 re-point"* comment.
- **The EU AI Act technical premise** (`security_compliance.md:231-245`): `skills.go:75` is the divide;
  the LLM dispatch really is the hardcoded switch — `criterion.go:127` `case check.EngineLlm:` →
  `validateLLM` → `NewLLMBulkChecker(c.logger)` at exactly `:428`; the template's `:27` reads *"assess
  whether the `<asset>` … meets or does not meet"* and it returns `check_id`/`feedback`/`success`.
  `checkerEngines` is stored at `criterion.go:34`/`:72`/`:90` and `validator.go:43`/`:60`/`:595` and
  **never indexed anywhere** — "stored and never read" is enumerated, not asserted. The deterministic
  exception is real: `criterion.go:168` dispatches `validateCodeDiff`, whose body `:450-475` sets
  `success` from `cdiff.Diff == ""`, no model. So *"most, not all"* is the honest word and the corpus
  uses it.
- **The residency claims** (`security_compliance.md:196-224`): `ai.go:85-88` is
  `config.WithRegion("eu-west-1")`; `:263-277` is `getClient`'s Azure arm swapping `azureClientEu` →
  `azureClientUs` on `flag_use_azure_us`; `isThrottlingError` is defined at `:129` and called at `:166`
  and `:325`; and the retry really does re-target OpenAI — `:153-154` `if throttled { vendor = Openai }`.
  Course Builder: `bedrock.go:109-112` is `newUnderlyingClient` → `NewAnthropicClientWithModel`, `:100`
  is `return "anthropic-api"`. All three cross-file anchors resolve to the right construct:
  `external_services.md:579` is the *"no ordered EU-first fallback chain"* sentence, `:567` the
  Anthropic-Direct provider row, `coursebuilder.md:48` the *"shipped path"* bullet. The "fifth latent
  arm" is consistent between the two files (both mean Studio-Room's **`openai`** `TARGET SERVICE`, item 5
  at `external_services.md:609+`), and all **three** `app/studio/configs/*.ini` pin every one of their
  10 `*_AI_*_MODEL` lines (30 total) to `azure` — 0 non-azure.
- **The org-switch mechanism** (`security_compliance.md:147`): all three hooks resolve at the exact cited
  lines in **both** `apps/web` and `apps/hiring` — `useOrgSelection.tsx:94` `clerk.setActive({
  organization: clerkOrgId })`, `useResolveActiveOrg.tsx:107`, `useActivateMembershipOrg.tsx:81`.
- **`README.md:21`** (`security_compliance.md:185`): resolves as a **sibling** reference —
  `corpus/architecture/README.md:21` ends *"(e.g. cost tracking lives in `app`, not the `ai` library)"*,
  which is the corroborating claim, and `:23` names `app/internal/aiusage`. Read against the repo-root
  `README.md` it would land on an unrelated line; the sibling reading is the correct one and the
  citation is sound.
- **Studio-Room** (`service_taxonomy.md:238-273`): grepped at the nested repo's **own** ref `aeec036a`
  (a host-ref `git grep` returns a guaranteed 0 here). `mistral` → **22 hits / 3 files**
  (`requirements.txt` 1, `tools/pdf2md.py` 18, `tools/r3.py` 3); `tools/pdf2md.py:24` is
  `from mistralai import Mistral`; `r3.py` only reads `MISTRAL_API_KEY`, so *"imported only by pdf2md"*
  holds. `services/ai.py`'s provider registry is `{openai, azure, anthropic}` and `ai.py:1-2` imports
  only `openai`/`anthropic`. `gen.py` registers **exactly nine** arguments — the nine named — and uses
  `parse_known_args` at `:19`, so the `--template` warning is exact. `studio/CLAUDE.md:12-14` is the
  quoted invocation. `additional_repo: "anthropos-studio-room:studio"` in `app`'s
  `.github/workflows/build-production.yml:29`; `Dockerfile:28` `FROM python:3.11-slim`.
- **Ant Academy** (`service_taxonomy.md:275-310`): every anchor exact at `22df69dd` **HEAD** —
  `server.js:14` the endpoint read / `:18` the throw; `backendContent.js:36` and `:102-103` the two
  catalog reads; `serverTenant.js:145` the unconditional DB-authoritative read with `emptyCatalogView()`
  as the only fallback, and `:115-145` containing both quoted phrases; `beacon/route.js:36` + `:41-55`
  the `OPS` map with `UPSERT_CHAPTER_PROGRESS` / `SET_LAST_ACTIVITY`; `next-scaffold.test.js:106`/`:111`
  and `react-compiler-config.test.js:41` the three serwist-removal fences; `layout.jsx:132`
  `manifest: "/academy-manifest.json"` with `"display": "standalone"`; 0 serwist/workbox in
  `code/package.json`; `RegisterServiceWorker.jsx` is a kill-switch that calls `r.unregister()`;
  `deploy-academy.yaml` exists; Next 16 / React 19.2 / Expo `~54.0.33`; port 3077. The Cosmo claim is
  corroborated by the repo's **own** `CLAUDE.md:255` word-for-word — *"`AgentRunner` calls the OpenAI
  Responses API (`gpt-5.2`) directly from the browser using a key from `localStorage('openai_api_key')`"*
  — and by `app/api/ai/chat/route.js`'s header comment. The demo-patch
  `demo-stack/patches/academy-fs-published-fallback` exists in the pinned rext clone, as does
  `ant-academy-dev-origins`; `ensure-clones.sh` phase **(d2)** is the explicit non-fatal ant-academy clone.
- **Clerkenstein internals** (pinned clone `09d06070`, cross-checked against `680e8529` — identical):
  `alignctl/run.go:134-135` = `ExitRegressed = 2` / `ExitUnmeasurable = 3`; `clerk-backend/store.go:138`
  = `func (s *Store) SeedOrgIdentity`, `:151` = `func (s *Store) LookupOrgEid`;
  `alignment/dna/clerk-2.6.0.json:131` is the description carrying *"M219 landed the fix … taking the Go
  surface 97.2% -> 100%"*. The single-tenant disclosure is measured, not asserted:
  `registry.go:24` one `activeKey`, `:67` `active()`, `:75` `Select()`; `server.go:102-109` one
  `signedIn`/`signedOut`/`clientID`/`sessID`, `:125` the `"client_clerkenstein"` constant, `:665`
  `"sess_clerkenstein"` inside `establishLocked` (`:663`); `handleMe` (`:488`), `handleToken` (`:467`),
  `handleClient` (`:241`) and `handleSignOut` (`:554`) all take `_ *http.Request`;
  `handleMeOrganizationMemberships` (`:512`) uses `r` only for `limit`/`offset` and answers from
  `activeUserLocked()` (`:131`); **`.Cookie(` is 0 across the whole section** with `Set-Cookie` as the
  live control, so *"cookies are only ever emitted"* is enumerated. `handleSelectIdentity` (`:627`) does
  set `signedIn = false; sessID = ""` (`:646-647`) **and** clear `signedOut` (`:654`) — both halves of
  the D81 rules. All five named `server_test.go` tests exist. `meorgmemberships_test.go` exists and the
  handler implements exactly the three properties claimed (paginated envelope, honoured `limit`/`offset`
  with a true `total_count`, 401-never-404, `[]` never `null`). `STUDIO_ACCESS_ROLES` is at
  `studio-desk/src/index.ts:96` **and** `app/services/userService.ts:16`, both with the quoted
  both-forms comment. `clerk-frontend/server.go:35-67` is the M220 block, `clerkJSFetchTimeout = 15 *
  time.Second`, `clerkJSClient` commented *"Explicitly NOT http.DefaultClient"*, `FAKE_FAPI_CLERKJS_CACHE`
  the disk cache. The roster thread is a real paired change: `roster.go:53-54`/`:77` produce
  `org_name`/`org_slug`/`org_is_hiring`, `registry.go:125-126`/`:144` consume them, `:190`
  `DisallowUnknownFields()`, `resources.go:50-51` the `"Clerkenstein Demo Org"`/`"clerkenstein-demo"`
  defaults and `:275-276` the conditional `isHiring` emit — and `next-web-app`'s
  `apps/web/src/hooks/useGetClerkOrganization.tsx:20` really does read
  `Boolean(organization?.publicMetadata?.isHiring)`. `@clerk/express (^1.3.47, installed 1.7.79)` and the
  `dotless-pk-rejected` gene are both in `clerk-express-1.json`; `up-injected.sh:139` sets the dotted
  `FAPI_HOST` default `127.0.0.1` with that exact rationale in-comment. `clerkenstein/.github/workflows/
  alignment.yml` is tracked and there is **no** repo-root `.github/`, so *"git-tracked but inert"* holds.
  `gate.sh` defaults are `95`/`100`.
- **Frontend monorepo facts** (`frontend_architecture.md:19-65`): ports web 3000 / hiring 3001 /
  integration 3002 / mobile 3031 (`expo start --port 3031`) / maintenance none; `!apps/mobile` in
  `pnpm-workspace.yaml`; `configs/i18n/messages` holds exactly the 8 named locales; workspace names match
  the three `pnpm dev --filter=` invocations; `turbo ^2.9.6`; `packageManager: "pnpm@10.30.3"`;
  `engines.node: ">=24.0.0"`; `typescript ^5.9.3`; `clean:quick` drops exactly node_modules/.next/.turbo/
  dist; `packages/graphql/src/__generated__` and `src/query` both exist; `UPGRADE-IMPACT-next16.md`
  exists. **The tilde correction is right**: `apps/web/package.json:46` is `"next": "~16.2.12"`, same in
  hiring/integration/maintenance, none in mobile, and the lockfile resolves `16.2.12`. The router's prod
  path is `graphql_path: /graphql` on `listen_addr: 0.0.0.0:8080`, so *":8080/graphql in prod"* is exact.
  451 commits in July 2026 → *"300+ / month"*.
- **Intra-corpus link targets**: `shared_libraries.md#taxonomy-figures` resolves — it is an explicit
  `<a id="taxonomy-figures">` at `:228`, not a heading. `studio-room.md#blueprints-not-templates`
  resolves to `### Blueprints (not "templates")` at `:281` under GitHub slugification.
  `external_services.md#routing-what-is-actually-implemented` = `:577`.
  `alignment_testing.md#how-m1-m1b-m2-and-m2c-consume-this` = `:318`;
  `#what-alignment-proves--and-what-it-doesnt-the-m3-lesson` = `:393`.
  `cockpit-spec.md` § *Limitation — one seat per stack* = `:414`. `latency-budget.md`
  § *Time-to-usable* = `:416`, and the ~4.05 s figure is at `:383`.
  `platform-migration-status.md:89` is the `jobsimulation` row. `frontend_architecture.md:11`,
  `studio-desk.md:20` and `:21` all say what they are cited as saying.
  `service_taxonomy.md`'s self-references `:142`, `:157`, `:158`, `:159`, `:67-68` all land correctly.
- **Cross-file consistency** (the cheapest class, §5 rule 21): the tenancy figure is **31** in all three
  places that state it (`security_compliance.md:76`/`:135`, `architecture_overview.md:377-381`); the
  `core` = **five containers** claim is identical in `service_taxonomy.md` and
  `architecture_overview.md:12`/`:71`; the **4**-entry `repos.yml` is identical across `CLAUDE.md:310`,
  `service_taxonomy.md:180`/`:446`, `staging-bringup.md`, `update_guide.md`; db-backup's
  "every 6h → S3/Azure/Hetzner" is identical across 7 sites. I found **no** self-contradiction in my file
  set beyond the two booked above.

### RE-DERIVED only (weakest clearance — listed so it is not mistaken for the above)

- `security_compliance.md:104`'s `135 − 31 − 23 = 81`. I enumerated all three inputs independently
  (above), so this row is arithmetic-on-a-verified-set rather than arithmetic alone — but the **81** as a
  named membership list was never spelled out by the document and I did not enumerate its 81 members, only
  the five it names.
- `service_taxonomy.md:73-75`'s declared/effective split `8/10 → 5/7`: I counted the compose service keys
  at both refs, which is an enumeration, but the *"three of them unfederated husks"* historical
  characterisation at `:70-71` I only checked for plausibility.

---

## What I could not settle

- **`db-backup.md` in its entirety.** There is no `db-backup` clone in `stack-demo/` or `.agentspace/`,
  so *"every 6 hours"*, *"S3 / Azure Blob / Hetzner"*, *"Go"*, *"Docker (scheduled, internal)"* and
  *"runs in production and staging"* are unverifiable from ground truth. The file is internally
  consistent and consistent with all six sibling sites in the corpus; `platform-migration-status.md:102`
  correctly records it as production-only with no compose service and no `repos.yml` entry (which I
  **did** verify). One dangling difference worth an owner's eye, too small to book: this file's
  *"Deployment | Docker (scheduled, internal)"* vs `service_taxonomy.md:512`'s *"ECS scheduled task"* —
  compatible, but only one of them tells you where it runs. **What would settle it:** a `db-backup`
  clone, or its terraform.
- **B1's residual.** `modules/services/base_service` lives in `anthropos-work/infrastructure`, which has
  never been in a clone set. I can prove the router is handed private subnet ids and no public ones; I
  cannot prove the module does not place the task elsewhere. **What would settle it:** the
  `infrastructure` repo, or a `describe-services --network-configuration` against the prod cluster.
- **Whether the Cosmo Router is deployed at all.** `terraform/main.tf:20` `= 1` is the declaration; the
  repo's last commit is 2026-07-30 and the corpus says it was archived that day. Declaration ≠ applied
  state, and `infrastructure`'s `services.tf` — which the corpus repeatedly notes is the authoritative
  place — is unreadable here. Both `service_taxonomy.md` and `security_compliance.md` describe a live
  prod router on that basis.
- **Archive state for `graphql-wundergraph`, `skiller`, `skillpath`.** Lives in the GitHub org API, never
  in git objects; `gh` is not installed and the repos are private. The corpus says exactly this at
  `service_taxonomy.md:142-151` and I concur. The `jobsimulation` row's refutation-by-post-dated-commits
  is the one case where the clone *can* speak, and I verified it (4 commits on 2026-08-04, incl. a merge
  commit).
- **Unverifiable compliance scalars** in `security_compliance.md`: *"DPA v1.4"*, *"18 approved
  sub-processors"*, *"90-day auto-deletion"*, *"90-day log retention"*, *"TLS 1.3 (ECDHE … AES-128+)"*,
  *"AES-256 on RDS/EBS/S3"*, *"ECS health checks every 30 seconds"*, *"DR site: US AWS region"*, and the
  sub-processor list. None is derivable from any clone. Per §5 rule 48 I did not escalate any of these;
  they are recorded as prose-under-review, not booked.
- **`clerkenstein.md`'s live score claims.** The DNAs, gene counts, operators and criticalities are all
  enumerable and correct, but a *score* (`100.0%/100.0%`, `27/27`) is the output of a **run**, and I did
  not run `alignctl` — nor could I meaningfully, since `@clerk/express` is dependency-gated and the doc's
  own point at `:45-52` is that that surface frequently produces no number. The document's honesty about
  this is the thing I could check, and it checks out. **What would settle it:** an `/align-run` receipt
  carrying its fence tree's path + sha (§5 rule 50), which no artifact I may read carries.
- **`messenger.md:100`'s *"2026-Q2"* whitelabel dating.** `CHANGELOG.md:111` puts `v0.34.0` at
  **2026-04-24**, which is Q2, so the label is right — but *"Recent work"* is a standing claim about a
  repo that has been frozen since `838d907`, and nothing in the file dates it.
