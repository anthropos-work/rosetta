# Seat D — reading #27

**Ground truth used (re-derived at this reading's open, and re-checked at close — no clone moved):**
`stack-demo/platform` `0c91421d` (= origin/main) · `stack-demo/app` `ad9f3c49` (= origin/main) ·
`stack-demo/app/studio` `aeec036a` (own checkout) · `stack-demo/cms` `ca50c817` (origin/main `f38c0c4a`) ·
`stack-demo/cms/studio` `aeec036a` · `stack-demo/next-web-app` `8297c684` (origin/main `f97ba659`) ·
`stack-demo/sentinel` `f2c46190` · `stack-demo/storage` `4ce8ece5` (origin/main `9f8cb532`) ·
`stack-demo/messenger` `fa47850d` (origin/main `e9421c68`) · `stack-demo/jobsimulation` `462343b0`
(origin/main `82cb66ec`) · `stack-demo/roadrunner` `87d8d443` · `stack-demo/graphql-wundergraph` `60c229f3` ·
`stack-demo/studio-desk` `41ee3575` · `stack-demo/ant-academy` `22df69dd` ·
`stack-demo/rosetta-extensions` **`09d06070`** (pinned per-stack consumption clone) ·
`.agentspace/rosetta-extensions` **`680e8529`** (authoring copy).

For every `rosetta-extensions` claim below I read **both** trees and say which; all of them were
byte-identical on the cited constructs, so the rule-45 distinction did not bite this seat.
No fence/guard was run, so no guard-verdict provenance (rule 50) is claimed anywhere in this report.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/architecture/service_taxonomy.md` | 514 | yes |
| `corpus/services/clerkenstein.md` | 374 | yes |
| `corpus/architecture/security_compliance.md` | 281 | yes |
| `corpus/services/messenger.md` | 204 | yes |
| `corpus/architecture/frontend_architecture.md` | 116 | yes |
| `corpus/services/db-backup.md` | 31 | yes |
| **total** | **1520** | |

Search-pipeline controls run in the same pass (§5 rules 1–6, 44):
- Every `git grep` was run **per tree at that tree's own ref**, including the two nested
  `anthropos-studio-room` checkouts (`app/studio`, `cms/studio` @ `aeec036a`), which a host-ref grep
  returns 0 for by construction.
- Positive control on the absence claim I checked hardest: `MESSENGER_RPC_ADDR` = **0 hits in all 15
  trees**, while the control token `STORAGE_RPC_ADDR` returns 35/6/10/31 in app/cms/jobsimulation/rext —
  i.e. the pipeline can find this shape of token.
- One regex failure was caught by control and corrected mid-pass: `git grep -E '^\s*//.*OrganizationMixin\{\}'`
  returned 0 (POSIX ERE has no `\s`); the `[[:space:]]` form returns the expected hits. An uncontrolled
  version of that search would have "refuted" a true corpus claim.
- Live `docker compose --profile X config --services` run against `stack-demo/platform` @ `0c91421` for
  every profile token the corpus names, exit codes captured separately from output (rule 11).

## Blockers

### B1 — `archiveStudioTask` is cited to the queries SDL; it is a mutation in a different file

- **Anchor:** `corpus/architecture/service_taxonomy.md:49` — *"the `studioTask` / `studioTasks` /
  `archiveStudioTask` operations are `app`'s, in
  `app/internal/web/backend/graphql/graph/schemas/cms_queries.graphqls:106`"*
- **Why it is false:** the citation is correct for two of the three named operations and wrong for the
  third. At `app` `ad9f3c49`:
  - `cms_queries.graphqls:106` → `studioTask(taskId: ID!, organizationId: ID): StudioTask!`
  - `cms_queries.graphqls:107` → `studioTasks(`
  - `archiveStudioTask` is **not in that file at all** — it is
    `cms_mutations.graphqls:22` → `archiveStudioTask(taskId: ID!, organizationId: ID): Boolean!`
  (derivation: `git -C stack-demo/app grep -n "archiveStudioTask\|studioTasks\|studioTask(" HEAD --
  internal/web/backend/graphql/graph/schemas/` returns exactly those three lines, in two files.)
  The load-bearing proposition of the sentence — *these operations are `app`'s, not studio-desk's and
  not a standalone cms's* — is **true**; what is false is the locator, for one of the three constructs
  it names, and it is a wrong **file**, not line drift.
- **Why I hesitated:** the anchor resolves and names a real, correct construct for the first two
  operations, and no operational conclusion changes. Under the canonical grading rule this would sit
  close to the "line drift" MINOR carve-out. I booked it because the briefing names *"wrong file paths"*
  explicitly as a BLOCKER class and resolves doubt upward, and because a reader who follows the
  citation to find the archive mutation will not find it there.
- **Confidence:** medium

### B2 — `d11a403` is credited with re-pointing all four `*_RPC_ADDR`; it re-pointed two

- **Anchor:** `corpus/architecture/service_taxonomy.md:425` — *"The last four `*_RPC_ADDR`
  (`BACKEND_USERS_`, `CMS_`, `JOBSIMULATION_`, `SKILLER_`) were `messenger`'s, all re-pointed to
  `http://backend:8083` by `d11a403` — the M809 re-point landed —"*
- **Why it is false:** measured in `stack-demo/platform`. `git show d11a403 -- docker-compose.yml` touches
  exactly two of the four values:
  ```
  -      - CMS_RPC_ADDR=http://cms:8091
  +      - CMS_RPC_ADDR=http://backend:8083
  -      - JOBSIMULATION_RPC_ADDR=http://jobsimulation:8401
  +      - JOBSIMULATION_RPC_ADDR=http://backend:8083
  ```
  At the parent ref `2adcf71`, `SKILLER_RPC_ADDR` (`docker-compose.yml:174`, messenger block) and
  `BACKEND_USERS_RPC_ADDR` (`:162`) **already** read `http://backend:8083`; `BACKEND_USERS_RPC_ADDR` never
  addressed anything else at any ref. So `d11a403` re-pointed the **middle two**.
  The corpus states the precise form elsewhere and this line contradicts it: root
  `CLAUDE.md` (Communication Patterns) says *"`d11a403` had re-pointed the **middle two** at `backend` —
  M809"*, and `corpus/ops/platform-alignment.md` §5 (the iter-22 worked example) records that at `2adcf71`
  *"only `SKILLER_RPC_ADDR` was re-pointed"* — i.e. the corpus's own protocol doc treats exactly this
  distinction as load-bearing, because it is the example that taught rule 31.
  The END state the sentence describes is correct and I verified it: at `0dab54d` the messenger block
  carries exactly four `*_RPC_ADDR`, all `http://backend:8083` (`:173`, `:174`, `:176`, `:183`); at
  `0c91421` there are **zero** across `docker-compose.yml`, `common.yml` and `.env_example`.
- **Why I hesitated:** *"by `d11a403`"* can be read temporally (*"by the time of"*) rather than agentively,
  under which reading the sentence is true. The surrounding clause *"— the M809 re-point landed —"* pushes
  the agentive reading, and the corpus contradicts itself across two files on the same fact either way.
- **Confidence:** low-to-medium

## Minors

- `corpus/services/clerkenstein.md:283-284` — no blank line between the end of the `⚠️ v0.34.3 pin`
  blockquote (`:283`) and the paragraph that follows (`:284`, *"That drop-in is **identity-agnostic**…"*).
  Markdown lazy continuation pulls that paragraph (through `:288`) **inside** the warning box. Formatting
  only; no factual consequence.
- `corpus/services/clerkenstein.md:21` — the rext section enumeration lists **6**
  (`clerkenstein, demo-stack, stack-injection, stack-core, stack-seeding, alignment`); the tree carries
  **11** section dirs at both refs — the five omitted are `dev-stack`, `playthroughs`, `stack-secrets`,
  `stack-snapshot`, `stack-verify`. Omitted list members.
- `corpus/services/clerkenstein.md:105` — the `cmd/` row enumerates `mintpk`, `fake-fapi`, `fake-bapi`;
  the dir also holds `jwtkey` (both trees). Omitted list member.
- `corpus/services/messenger.md:178` — the `BREVO_KEY` row's *"Value in the deleted compose block"* cell
  reads `(empty)`. `git show 0dab54d:docker-compose.yml | grep BREVO` returns **nothing** — the block never
  declared it; it arrived through `env_file: .env`. "(empty)" is the effective default, not what the block set.
- `corpus/architecture/service_taxonomy.md:397` — cites `:67-68` for *"There is no `graphql` profile"*;
  that sentence begins on **line 68** and finishes on line 69. The quoted fragment is inside the cited
  range, so it resolves; the range simply stops one line short of the claim.
- `corpus/architecture/security_compliance.md:233` — `calculateSkillScore` cited as `skills.go:53-64`;
  the function spans **:53-62** (`:64` opens `func (s skillsValidator) run`). Two-line over-reach; the
  arithmetic claim (`:75` divides) is exact.
- `corpus/architecture/frontend_architecture.md:108` — heading *"Recent UX Work (May 2026)"* is three
  months stale as of 2026-08-06. Explicitly dated, so harmless.

## Positively cleared (audited zeros)

### ENUMERATED — predicate re-derived from source, cardinality stated before any arithmetic

1. **`security_compliance.md:66-132` — the multi-tenancy fence (the one wrong four times).**
   I enumerated the SET, not the sum, and excluded non-compiling lines (rule 24). At `app` `ad9f3c49`:
   - `internal/data/ent/schema/` holds **139** `.go` files; **135** declare `ent.Schema`; the 4 that do not
     are exactly `database_types.go`, `mixin.go`, `skiller_mixins.go`, `skillpath_mixins.go`.
   - `OrganizationMixin{}` is **mentioned in 30** files and **used in 29** — the 30th is
     `user_resource.go:22`, verbatim `// OrganizationMixin{},  // We need to work on this`. Confirmed by
     reading the file, not by `grep -c`.
   - `OrganizationIDMixin{}`: **7**, and they are exactly the 7 the doc names
     (`category, jobrole, similarity, skill, specialization, studio_document, studio_task`); none commented.
   - The two mixin sets are **disjoint** (`comm -12` → empty), and neither contains `organization.go` or
     `org_membership.go`, so there is no double-count in the 31.
   - The doc's own derivation reproduces: `ent.Schema` files minus `Organization(ID)?Mixin\{\}` users,
     `xargs grep -l '"organization_id"'` → **18**, and the bare `*.go` glob → **19**, the 19th being
     `skiller_mixins.go`, exactly as the doc warns. `user_resource.go` is *not* a hidden 19th: it carries an
     `organization` **edge** (`:44`), not an `"organization_id"` field.
   - The 16 named unpoliced files are precisely those 18 minus `academy_feedback.go` and `org_membership.go`.
   - Exactly **four** files in the directory declare `Policy() ent.Policy` (`mixin.go`, `org_membership.go`,
     `organization.go`, `user.go`) — at `ad9f3c49` **and** at the pinned `5ba17044` the re-measurement
     paragraph names, where 139/30/7 also all reproduce.
   - Every cited construct opened and read in context: `mixin.go:126` (OrganizationMixin `Policy`),
     `mixin.go:98/:103` (UserMixin `FilterOwnerRule`), `organization.go:56` + `FilterSameOrganizations` at
     `:96` (and it uses neither mixin), `org_membership.go:172-188` ending in `privacy.AlwaysDenyRule()` at
     `:185` with `AllowCurrentOrgEdgesOrSkipRule()` at `:179`, `user.go:116`.
   - The conjunction the fence failed on last time (rule 17) tests true this time: `31 = 29 + Membership +
     Organization`, `23 = 16 + 7`, `135 − 31 − 23 = 81`, and the three partitions are pairwise disjoint.
   - The remainder's tenant-scoped members: I enumerated **every** schema declaring a tenant field —
     `field.String("tenant_eid")` in `lab.go:63`, `academy_chapter.go:84`, `academy_skill_path.go:78`;
     `field.UUID("tenant_id")` in `skill_path_session.go:43` and `job_simulation_session.go`. Five in total;
     four fall in the 81-remainder and the fifth (`job_simulation_session.go`) is in the 23, exactly as
     written. `TenantMixin{}` has **zero** users. The first three declare no `Policy()`; `skill_path_session.go`
     carries `UserMixin{}` (owner-scoped) as claimed. `academy_feedback.go` carries `UserMixin{}` at `:64` and
     `organization_id` at `:129`. **This fence is correct as it stands.**
2. **`security_compliance.md:196-256` — the AI-residency and EU-AI-Act blocks.** Opened and read around
   every anchor: `jobsimulation/ai/ai.go:85-88` (`config.WithRegion("eu-west-1")` at `:87`), `:263-277`
   (`azureClientEu` default at `:264`, `flag_use_azure_us` at `:267`, swap at `:275`), `isThrottlingError`
   at `:129` with call sites `:166` and `:325`; `coursebuilder/bedrock.go:100` returning `"anthropic-api"`
   and `:109-112` `newUnderlyingClient → NewAnthropicClientWithModel`. The validator claim re-derived:
   `v3/validator/validator.go:60-62` registers **exactly one** engine; `checkerEngines` occurs 6 times
   across 2 files and is **only** declared/assigned/passed — never dereferenced for dispatch, so the
   corpus is right to say *cite the dispatch, not that map*; `basevalidator/criterion.go:127` is the
   `case check.EngineLlm:` arm of a hardcoded switch, `:428` is `NewLLMBulkChecker(c.logger)`, `:168`
   dispatches `validateCodeDiff`, and `:450-475` is a pure string comparison with no model. The prompt at
   `templates/checkValidationBulk.tmpl:27` does say *"assess whether the `<asset>` … meets or does not
   meet"*, and it emits `check_id` / `feedback` / `success`. Studio-Room's latent `openai` arm: all **three**
   `app/studio/configs/*.ini` pin `azure` on every `*_AI_*_MODEL` line (read at the nested repo's own ref
   `aeec036a` — a host-ref grep would have returned a false zero). Org switching: `setActive` resolves at
   `useOrgSelection.tsx:94`, `useResolveActiveOrg.tsx:107`, `useActivateMembershipOrg.tsx:81` in **both**
   `apps/web` and `apps/hiring` at `8297c684`.
3. **`service_taxonomy.md` — the whole compose/profile surface, derived and then executed.** From
   `stack-demo/platform` `0c91421`: `docker-compose.yml` declares **5** services; `common.yml` adds 2 → **7**
   effective; exactly **4** carry a `profiles:` key, at `:110`, `:141`, `:168`, `:183`, and `sentinel`
   (`:5`) plus `common.yml`'s two carry none — so the floor is **three**. `Makefile:10` is `PROFILE ?= core`.
   `repos.yml` has **4** entries with `app` at `:14-17` (`migrations: true`, `schema: public`) and `sentinel`
   at `:18-20`; it was **9** at `2adcf71`. Then I ran the thing rather than reasoning about it
   (§5 rule 14 — be a client): `core` → `{redis, postgresql, sentinel, backend, gotenberg}` = **5**, rc 0;
   `all` → **7**, rc 0; `frontend` and `studio-desk` → **rc 1**, `service "…" depends on undefined service
   "backend": invalid compose project`; and each of `graphql, cms, jobsimulation, roadrunner, storage,
   storage-legacy, messenger, customerio-sync` → **rc 0 with exactly the 3-service floor**. Every one of
   those eight retired-token verdicts matches `:492-497` and `:483-487`.
4. **`service_taxonomy.md` — the supergraph ladder.** Enumerated from `graphql-wundergraph` `60c229f3`:
   all three `supergraph-config-*.yaml` carry a single `- name: backend`; `schemas/` holds exactly one file.
   `915da06` deletes **both** `schemas/cms.graphqls` and `schemas/jobsimulation.graphqls` in one commit, and
   `915da06^:supergraph-config-prod.yaml` lists 3 (`backend`, `jobsimulation`, `cms`) → so the step is
   **3 → 1**, and the corpus is right that the commit's own subject line ("2→1") is the wrong figure.
   `749dc86` removes skiller; `7c17e63` removes skillpath. Ports: `terraform/locals.tf:8 port = 8080`,
   `terraform/main.tf:48-49` maps 8080→8080, `config.prod.yaml:5 listen_addr: 0.0.0.0:8080`,
   `terraform/main.tf:20 service_desired_count = 1`, and `graphql_path: /graphql` at `config.prod.yaml:13`.
   `5050:8080` existed only as the local host mapping (`2adcf71^:docker-compose.yml:14`), with studio-desk
   baked at `http://localhost:5050/graphql` there — so *"never a production port"* holds.
5. **`service_taxonomy.md` — shared libraries.** `go.mod` of all **7** Go clones enumerated:
   `authn` = **0** in every `go.mod` *and* every `go.sum`; control `colony` = 7/7. `ai` survives only in the
   frozen `cms`/`jobsimulation` husks at `v1.40.2`; `app` carries `internal/ai/` in-tree and `1e457fa70`
   is real (*"refactor(ai): fold the ai library into app as internal/ai"*, 2026-08-04).
6. **`service_taxonomy.md` — studio-room's provider set.** At the **nested** repo's own ref `aeec036a`:
   `services/ai.py:705-708` is the whole registry (`openai`/`azure`/`anthropic`), `ai.py:1-2` imports only
   `openai`/`anthropic`, `tools/pdf2md.py:24` is `from mistralai import Mistral`, and
   `git -C app/studio grep -ci mistral aeec036a` sums to **22 lines / 3 files** — the third file
   (`tools/r3.py`) references only the env var, not the import, so *"imported only by pdf2md.py"* is exact.
7. **`service_taxonomy.md` — Ant Academy.** Every anchor opened: `code/src/graphql/server.js:14,18`
   (throws when unset), `backendContent.js:36,102-103`, `serverTenant.js:115-145` (the verbatim
   *"there is NO FS-as-published fallback … not reversible-on-error"*), `beacon/route.js:36,41-55`,
   `layout.jsx:132` (`manifest: "/academy-manifest.json"`, `display: standalone`),
   `next-scaffold.test.js:106,111`, `react-compiler-config.test.js:41`, `RegisterServiceWorker.jsx` is a
   kill-switch, zero `serwist`/`workbox` in `code/package.json`, `next ^16.2.6` + `react ^19.2.5`,
   expo `~54.0.33`, ports 3077/8555, `deploy-academy.yaml` present, and
   `NEXT_PUBLIC_FEATURE_TRAINING_COACH` default-OFF via `featureFlags.js`. The Cosmo claim is exact and
   was the one I most expected to be wrong: `ucourses/ucourse-engine/assistant/agent.js:12` is
   `https://api.openai.com/v1/responses`, `:13` is `const MODEL = 'gpt-5.2'`, `:32` is
   `localStorage.getItem('openai_api_key')`, and `app/api/ai/chat/route.js:10-11` independently confirms
   *"First-party Cosmo doesn't call this endpoint (it calls OpenAI directly from the browser)"*.
8. **`clerkenstein.md` — the score table and the DNA cardinalities.** Parsed each DNA's capability/variant
   tree in **both** rext trees (identical): `clerk-2.6.0` **14 caps / 27 genes**, `clerk-deploy-1` 3/**7**,
   `clerk-express-1` **5 caps / 13 genes**, `clerk-js-5` 6/**9**, `clerk-multi-1` 5/**9**. The gate
   thresholds are `GATE_OVERALL:-95` / `GATE_CRITICAL:-100` (`alignment/scripts/gate.sh:31-32`) — so
   *"≥95 / =100"* is derived, not asserted. `alignctl/run.go:134-135` is exactly
   `ExitRegressed = 2` / `ExitUnmeasurable = 3`. The M219 fix: `clerk-backend/store.go:138`
   `SeedOrgIdentity` and `:151` `LookupOrgEid`, and `clerk-2.6.0.json:131` carries the quoted
   *"taking the Go surface 97.2% -> 100%"* verbatim. `clerk-js-5.json` has a `Me` capability
   (`universal-user`, `unauthenticated`) and **no** gene anywhere in `dna/` for
   `me/organization_memberships` — the documented DNA gap is real.
9. **`clerkenstein.md` — the single-tenant seat disclosure.** Read the code rather than the summary:
   `registry.go:24` single `activeKey`, `:67` `active()`, `:75` `Select()`; `server.go:99` `type Server`
   with one `signedIn`, one `signedOut`, one `clientID`, one `sessID`; `:125` `clientID:
   "client_clerkenstein"`; `establishLocked` at `:663-670` sets `sessID = "sess_clerkenstein"` and clears
   `signedOut`; `handleSelectIdentity` (`:627`) sets `signedIn = false; sessID = ""` and deliberately does
   **not** set `signedOut`. `handleClient` (`:241`), `handleToken` (`:467`), `handleMe` (`:488`) and
   `handleSignOut` (`:554`) all take `_ *http.Request`, and **`.Cookie(` appears nowhere under
   `clerkenstein/`** — so *"per-browser `storageState` cannot isolate two identities"* is sound. All five
   named `server_test.go` tests exist. Routes: `clerk-backend/server.go:47` (BAPI) and
   `clerk-frontend/server.go:186` (FAPI twin), matching the table exactly.
10. **`clerkenstein.md` — the version-drift warning.** `clerkenstein/go.mod:8` is `colony v0.34.3`;
    `app/go.mod:15` and `sentinel/go.mod:8` are both `colony v0.35.2`; `88036d7` in `sentinel` is
    *"chore(deps): update dependencies to latest versions"*. `app/go.mod:29` is
    `clerk-sdk-go/v2 v2.7.0`, not `v2.6.0`. Every clause of the drift box measures true.
11. **`messenger.md` — the whole citation set.** `flow.go:72-104` re-derived by counting the predicate,
    not trusting the number: **22** `pubsub.EventHandler(` lines in that range, of which `:83`
    (`OrgJobSimulationAssignmentPastDueHandler`) is commented `// not implemented` → **21 live**;
    `:74-78` are the five `OrgSkillPath*` handlers. `cmd/root.go:63/:64/:107/:147` all exact
    (8080 / 8081 / `REDIS_STREAMS_INDEX` default `"2"` / `READONLY_DB_CONNECTION`); `:118-142` builds the
    RPC clients over four distinct env addresses. `REDIS_WORKER_INDEX` has **0** occurrences in the
    messenger repo. `rpcsrv.go:25-30` is the two `Unimplemented` stubs; `jobsimulations.go:140-151` are the
    2 h / 12 h staleness guards; `assignments.go:828` is inside `getSkillPath`. `go.mod`: `go 1.25.0`,
    `getbrevo/brevo-go v1.1.3`, `osteele/liquid v1.8.1`. Both terraform readings are right and they are
    right *only* because the doc says which tree settles which: at the demo checkout `fa47850d`
    `terraform/main.tf:19` is `service_desired_count = 1` and `:29` is `container_definitions`, while at
    `origin/main` `e9421c68` `:29` is `service_desired_count = 0` with the rollback comment at `:27-28`.
12. **`messenger.md:19-32` — the *"nobody fires a Messenger RPC"* absence.** This is an absence claim, so
    it got the full three-instrument treatment: **0** hits for `MESSENGER_RPC_ADDR` in all 15 trees
    including both nested `studio` repos, with `STORAGE_RPC_ADDR` as a live control; and the platform-history
    figures reproduce **exactly** — `log -S 'MESSENGER_RPC' 0c91421d` = **0**, control `-S 'SKILLER_RPC'` =
    **7**, `--all` gives **0** and **8**, the 8th being `464dfe3`, which `git branch -a --contains` puts on
    `remotes/origin/feat/cms-in-app` — a non-main branch, as stated. The platform history is **121** commits.
13. **`messenger.md` — the `app` consumer row.** `main.go:15` (`msgflow`), `:62` (`msgadapters`), `:63`
    (`msgsender`); `:1450` `pubsub.NewSubscriberServer(msgflow.ConsumerGroup, …)`, `:1471`
    `msgadapters.Wire`, `:1473` `msgsender.NewFromEnv`; `:1416-1421` is the literal-group-name rationale;
    and `internal/messenger/flow/streams.go:62` is `const ConsumerGroup = "messenger"`. `:295` is the
    `(messengerEnabled || customerIOSyncEnabled) && BREVO_KEY == ""` condition with `log.Fatalf` at `:296`.
    `env_guards.go:61` is `envMessengerEnabled = "MESSENGER_ENABLED"`, and
    `git ls-tree b948604f -- env_guards.go` is **empty** — the doc's insistence that the ref is not optional
    on that anchor is correct. `git diff --stat 2035f9a4 ad9f3c49 -- main.go` is **empty**, so the
    "resolves at both refs" clause holds. The two `9d00a313`-pinned anchors were graded **at that pin**
    (rule 33): `:1095` is the `CMS_STREAM` publisher and `:1478-1484` is the `streamNames` binding there.
14. **`frontend_architecture.md` — the 22 / 31 / 47 / 12 / 27 table.** I re-derived the SET from source at
    both refs rather than checking the arithmetic. At `8297c684`, over non-test `.ts`/`.tsx` with `e2e/`
    excluded: **22** files, **31** occurrences, **47** `fetch(` sites; the four `packages/core-js` clients
    contribute **12 env / 27 calls**. At `bb3313bc` the historical column reproduces exactly: **21 / 29 /
    43**, core-js **12 / 25**. At `origin/main` `f97ba659` it is still **22 / 31 / 47**, as the doc says.
    `bb3313bc..8297c684` is **41** commits / **192** files; `8297c684..f97ba659` is **4**.
    `new EventSource(` is **0** repo-wide, and `talkToData/api.ts:213-214` carries the quoted rationale.
    I also checked the prose list against the derived file set: the 15 described surfaces map onto exactly
    those 22 files with nothing left over and nothing missing.
15. **`frontend_architecture.md` — the stack facts.** `apps/web/package.json:46` is `"next": "~16.2.12"`
    (tilde, `.12`) and `hiring`/`integration`/`maintenance` match while `mobile` declares none;
    root `package.json` gives `packageManager: "pnpm@10.30.3"`, `"node": ">=24.0.0"`,
    `turbo ^2.9.6`, `typescript ^5.9.3`, `@commitlint/cli`, `husky`, and `cog.toml` exists.
    `pnpm-workspace.yaml` carries `'!apps/mobile'`. Ports 3000/3001/3002 come from each app's `dev` script
    and 3031 from `dev:mobile` (`expo start --port 3031`). Package names `@anthropos/{web-app,hiring-app,
    integration,tsconfig,i18n,ui,graphql,core-js}` all match. `configs/i18n/messages/` holds exactly the
    **8** locales listed (de, en, es, fr, it, ja, nl, pt). `packages/graphql/codegen.ts:9` is
    `http://localhost:8082/graphql/query`. `UPGRADE-IMPACT-next16.md` exists. *"300+ commits/month"*
    measures 343 / 373 / 247 / 340 / 455 over Mar–Jul 2026 — a fair characterization.
16. **`service_taxonomy.md` — the per-service terraform rows.** `jobsimulation`: `6092c6d2` is
    *"remove the jobsimulation ECS service and ECR repository (M810)"*, and `terraform/main.tf:15-40` at
    origin/main is exactly the survivor-inventory comment (LiveKit + Chime buckets, `/production/jobsimulation/*`
    SSM, the atlas tracker, and the note that dropping the schema is a separate step). `origin/main` carries
    **four** commits dated 2026-08-04 including merged PR #439 — so the row's *"report both, assert neither"*
    on archive state is the correct posture. `cms`: `terraform/main.tf:39` is `service_desired_count = 0` in a
    **191**-line file at both refs, and `6efa1d5` (subject naming M810 and the decommissioned ECR) deleted
    `.github/workflows/build-production.yml`, merged as `f38c0c4` on 2026-08-04. `roadrunner/terraform/main.tf:19`
    still reads `= 1`.
17. **`service_taxonomy.md` — the support-container deletion note.** Storage `8300-8301`, messenger
    `8200-8201`, customerio-sync `8080` all verified at `0dab54d`; `customerio-sync` was
    `profiles: [customerio-sync, all]` at `0dab54d:docker-compose.yml:154`; `ENVIRONMENT=development` at
    `:56` then and at `0c91421` now; `env_guards.go:37-44` `deployedEnvironment()` returns false for
    `development`, `resolveSubsystemSwitch` at `:92-111` maps unset→`(false, nil)` when not deployed, and
    `main.go:394` is `if customerIOSyncEnabled`. The pre-switch form is real too:
    `3e5bc33ef:main.go:387` is `if deployedEnvironment() && os.Getenv("BREVO_KEY") != ""`.
    `docker-compose.yml:84-92` is the comment block the doc quotes as the rationale.
18. **`service_taxonomy.md` — Directus.** `git show a2a3ee6^:docker-compose.yml` gives `:384
    image: directus/directus:10.10.1`, `:386 8055:8055`, `:409 ADMIN_PASSWORD=password`, and
    `admin@example.com` has **0** hits in the platform's whole history — so the retraction-of-the-retraction
    is exactly right in both directions. `DIRECTUS_PUBLIC_BASE_ADDR` is on `backend` at `:53`;
    `DIRECTUS_BASE_ADDR` is read from `.env` (`app/main.go:1090`, fatal if absent at `:1092`);
    `app/internal/cms/directus/` exists.
19. **`service_taxonomy.md` — studio-desk and the generation edge.** `.env.example:44/45` at `41ee3575` are
    exactly `VITE_GRAPHQL_ENDPOINT=http://localhost:8082/graphql/query` and `VITE_ENVIRONMENT=production`;
    `app/internal/cms/studio/studioManager.go:119` execs `studio/gen.py`; `additional_repo:
    "anthropos-studio-room:studio"` at `app/.github/workflows/build-production.yml:29`; the app image is
    `FROM python:3.11-slim`. `package.json` has **0** react/vue/angular entries and the repo has **0**
    `.tsx`/`.jsx` files, so *"no framework"* is derived; 7 language flags under `app/public/languages/`
    back *"7 languages"*; `gpt-5.2` is the dominant model literal, supporting *"GPT-5.x"*.
    `STUDIO_ACCESS_ROLES` is byte-identical at `src/index.ts:96` and `app/services/userService.ts:16`,
    each under the quoted comment.
20. **`clerkenstein.md` / `service_taxonomy.md` — the rext artifacts a stack actually consumes.** Read in
    the **pinned** clone `09d06070` (rule 45): `demo-stack/patches/academy-fs-published-fallback/…yaml`
    exists and its header describes precisely the FS-as-published restoration `service_taxonomy.md:289`
    attributes to it; `ant-academy-dev-origins` exists; `clerkenstein/.github/workflows/alignment.yml` is
    tracked while the repo root has **no** `.github/`, so *"git-tracked but inert"* is right;
    `alignment/` (the harness section) has **no** `scripts/` dir and `ALIGN_DIR` defaults to
    `$base/../../alignment` in both `gate.sh:30` and `drift-check.sh:16`.

### RE-DERIVED — arithmetic the document itself displays (weakest form of clearance; listed for completeness)

- `service_taxonomy.md:73-74` — the declared/effective split `8/10 → 5/7` after `838d907`.
- `security_compliance.md:104` — `135 − 31 − 23 = 81`.
- `frontend_architecture.md:11` — `repos.yml` `9 → 4`.

## What I could not settle

- **`db-backup.md` in its entirety (all 31 lines).** The `db-backup` repo is in no clone set and in no
  `repos.yml` at any ref I can reach, so nothing in it — the 6-hour cadence, the S3/Azure/Hetzner triple,
  *"PostgreSQL RDS (all schemas)"* — is checkable from here. It carries **no** `file:line` citations and no
  refs, which is honest but leaves it entirely prose-under-review (§5 rule 27, row three). It is at least
  *internally consistent* with `service_taxonomy.md:164`/`:512` and `security_compliance.md:155,162`; the one
  wording gap (taxonomy says *"ECS scheduled task"*, db-backup.md says *"Docker (scheduled, internal)"*) is
  compatible, not contradictory. **Evidence that would settle it:** a clone of `anthropos-work/db-backup`,
  or its terraform, in the ground-truth set.
- **Every `ARCHIVED <date>` assertion**, including the flat present-tense form in the
  `service_taxonomy.md:3` banner (*"the repo is ARCHIVED on GitHub (2026-07-30)"*) and the Skiller/Skillpath
  rows at `:157`/`:158`. The file's own note at `:142-151` says this lives only in the GitHub org API; I
  confirmed `gh` is not on this host and the repos are private. I did apply the one available falsifier —
  post-dated commits — and `graphql-wundergraph`'s last commit is 2026-07-30 10:23, consistent with the
  claim; unlike `jobsimulation`, which is why that row's retraction exists. **Not booked**, because a claim
  I cannot measure in either direction is not a refutation. **Evidence that would settle it:**
  `gh api repos/anthropos-work/<repo> --jq .archived`.
- **`security_compliance.md:20-32, 35-45, 151-162, 166-174, 258-272`** — VPC CIDR, subnet placement
  (including *"Public subnets: … Cosmo Router"*), TLS ciphers, KMS, backup cadence, DR site, patch policy,
  ECS health-check interval, DPA v1.4, *"18 approved sub-processors"*. All live in `infrastructure`, which
  has never been in any clone set. Per §5 rule 48 I did not escalate the compliance items; the corpus's own
  retraction of the EU-AI-Act *technical premise* is correctly scoped to a technical claim and **is**
  measurable — and I measured it (cleared, above).
- **`service_taxonomy.md:174` — the taxonomy figures** (`≥42,790` skills / `≥22,470` job roles). These are
  DB row counts against production; no clone can carry them. The doc is explicit that this is a measurement
  with a date and that the "60K/18K" figure is not one, which is the right shape. **Evidence:** a `/db-query`
  count against the public subset.
- **`clerkenstein.md:41-52`, the score table's *numbers* as opposed to its *cardinalities*.** I verified the
  DNA gene counts, the gate thresholds, the exit-code contract and the M219 fix in code, but a *score*
  (100.0% / 97.2%) is the output of a run, and running `alignctl` was out of scope for a read-only pass —
  and per §5 rule 50 a verdict is settled by the tree its configuration lives in, which would need to be
  stated with the result. The `@clerk/express` row's own point is that this surface frequently produces
  **no** number, so I would not have been able to close it from a single run either.
- **One cross-file contradiction that does not live in my file set**, recorded because rule 21 says the
  self-contradiction class is the cheapest and almost nobody checks it:
  `corpus/architecture/dependency_map.md:25` says the `pdf2md` OCR utility is one *"that nothing in the
  pipeline dispatches"*, while `corpus/architecture/external_services.md:567` says *"`tools/r3.py` DOES
  dispatch it … so *'nothing dispatches it'* is false"*. Measured: `tools/r3.py:139,190,199-206` at
  `aeec036a` does dispatch it. **My** file (`service_taxonomy.md:238`) says *"off the generation path"*,
  which is compatible with both and is the accurate form — so there is nothing to book against my
  partition. Flagged for whoever owns those two files.
