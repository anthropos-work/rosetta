---
iter: 283
milestone: M257x
iteration_type: tok
tok_flavor: user-re-scope
status: closed
opened: 2026-08-11
---

# iter-283 — TOK-09: the user closed the scope and put a clock on it

**Not a triggered tok.** The 3-no-prog streak did not fire — iter-282 closed `closed-fixed` with a
measured lift. This tok exists because **the user re-scoped the milestone**, exactly as `TOK-08` did:
recorded in the chain's next slot so every following tik can name its controlling strategy, and **the
agent did not author it**.

See `TOK-09` in the milestone-root `decisions.md` for the strategy itself.
