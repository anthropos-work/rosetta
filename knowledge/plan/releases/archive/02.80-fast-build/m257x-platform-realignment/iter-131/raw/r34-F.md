# Seat F — reading #33/#34 (M257x iter-131)

Ground truth re-verified at this reading's open, against the ADDENDUM table (not the frozen table):
`app ad9f3c498` · `platform 0c91421df` · `next-web-app 8297c684c` · `sentinel f2c461903` ·
`studio-desk 41ee3575d` · `ant-academy 22df69dd8` · `storage 4ce8ece52` · `messenger fa47850d9` ·
`cms ca50c8170` · `graphql-wundergraph 60c229f39` · `roadrunner 87d8d4438` · `jobsimulation 462343b05` ·
`stack-demo/rosetta-extensions 09d06070f` (pinned consumption clone) · `.agentspace/rosetta-extensions f2ea567b3`
(authoring). Nested repos read at their own ref: `stack-demo/app/studio` and `stack-demo/cms/studio`, both
`aeec036a5`.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/services/studio-room.md` | 512 | yes |
| `corpus/architecture/architecture_overview.md` | 463 | yes |
| `corpus/services/jobsimulation.md` | 324 | yes |
| `corpus/services/roadrunner.md` | 216 | yes |
| `corpus/architecture/frontend_architecture.md` | 116 | yes |
| `corpus/services/skiller.md` | 78 | yes |

Total 1,709 lines. Search-pipeline positive controls run in-pass: every "0 hits" below was paired with a
pattern known to match on the same engine + same ref (e.g. `new EventSource(` = 0 alongside `EventSource`
= 1 in `packages/core-js/src/talkToData/api.ts`; `_RPC_ADDR` = 0 in compose alongside `profiles:` = 4).

---

## Blockers

### B1 — `ai` is listed as one of "four imported private modules"; it is in NO live `go.mod`

- **Anchor:** `corpus/architecture/architecture_overview.md:83` —
  > "4. **Shared Libraries**: **four** imported private modules — colony, proto, ai, taxonomy (not deployed;
  > pulled at Docker build). **`authn` is not a fifth**: it ships inside colony as `colony/authn`, and no
  > service's `go.mod` requires the standalone module — 0 hits for `github.com/anthropos-work/authn` across
  > all seven Go clones, against a positive control of `colony` required by all seven"

  Same claim, same file, two more sites: `:233` ("> Imported as private Go modules — **not** cloned by
  `make init`") heading a table whose `**ai**` row is `:239`.

- **Why it is false:** I enumerated `github.com/anthropos-work/<lib>` in every Go clone's `go.mod`, each at
  its own ref (`app ad9f3c49`, `sentinel f2c46190`, `storage 4ce8ece5`, `messenger fa47850d`, `cms ca50c817`,
  `jobsimulation 462343b0`, `roadrunner 87d8d443`):

  | lib | requirers (of 7) |
  |---|---|
  | colony | 7 |
  | proto | 7 |
  | taxonomy | 6 (not roadrunner) |
  | **ai** | **2 — `cms` and `jobsimulation` only** |
  | authn | 0 |

  `app` dropped it at `1e457fa70` (*"refactor(ai): fold the ai library into app as internal/ai"*); the code
  is in-tree at `app/internal/ai/ai.go`. `sentinel` never required it. The only two requirers are **frozen
  legacy repos** — no compose service at platform `0c91421`, no `repos.yml` entry — i.e. nothing builds them.

  The sentence applies exactly the test that excludes `authn` ("no service's `go.mod` requires the standalone
  module") and then keeps `ai` in the four. Under the document's own seven-clone denominator, `ai` is 2/7 and
  both requirers are dead.

  **It also contradicts two other corpus passages, one of them in my own file set:**
  - `corpus/architecture/shared_libraries.md:73` — *"**ai** | … **Folded into `app` at `1e457fa70`; in no
    live `go.mod`**"* — and `:170` **Imported by** — *"**No repo a stack builds** (corrected M257x
    iter-102…)"*. This is the reference `architecture_overview.md:233` itself links to as authoritative.
  - `corpus/services/jobsimulation.md:176` — *"**AI providers** — through one `ai.AI` interface that is
    **in-tree, not a shared module**: `app` folded the `ai` library in at `1e457fa70`, so `app/go.mod` @
    `ad9f3c49` does not require `github.com/anthropos-work/ai`…"*

  This is the rule-19 half-repair signature: the `authn` correction landed at this site, the `ai` correction
  landed in `shared_libraries.md` and `jobsimulation.md` and never reached `architecture_overview.md`.
  A reader asking "where does `app`'s AI wrapper come from?" is sent to a private module `app` does not pull.

- **Confidence:** high

---

### B2 — `jobsimulation.md` says cms's prod state is unassertable *because* `infrastructure` was never cloned; the same file (and `architecture_overview.md`) then assert it from a measurement of that repo

- **Anchor:** `corpus/services/jobsimulation.md:12` —
  > "**Do not generalise this to `cms`, in EITHER direction** … The deciding declaration is in
  > `infrastructure`, which has never been in any clone set: **report both, assert neither** — see
  > [`cms.md`](./cms.md) and the fenced map."

- **Why it is false / contradictory:**
  - **Same file, 74 lines down**, `corpus/services/jobsimulation.md:78-86`:
    > "**MEASURED at `infrastructure` `13c248e6` (iter-123), and it confirms this bullet exactly:**
    > `module "jobsimulation_euwest1"` **IS still declared**, at `terraform/production/services.tf:475` …
    > **So the surviving module block is not a surviving service** — the same shape as `storage`, and the
    > opposite of `cms`, **whose block is gone**."

    `:86` asserts precisely the cms verdict that `:12` forbids, on the strength of a measurement of the very
    repo `:12` says has never been in any clone set.
  - **Cross-file**, `corpus/architecture/architecture_overview.md:227` (also my seat) states the opposite
    verdict outright and explicitly retracts `:12`'s framing:
    > "the prod ECS service is **DESTROYED** (corrected M257x iter-127 — `infrastructure` @ `13c248e6`
    > declares no `module "cms"` …). **This cell read *"not a settled rollback path — report both, assert
    > neither"* on the strength of `cms/terraform/main.tf:39` …, which is orphaned dead code.** … (This cell
    > said *"not visible to this corpus — `infrastructure` has never been in any clone set"*; that was a
    > clone-set limit, not a measurement limit, and the fix was to clone the repo.)"

  I cannot settle which side is right, and that is the point: **`infrastructure` is in no clone in this
  tree.** I enumerated every git tree under the repo root (`find . -name .git -maxdepth 4`): 19 trees —
  `.agentspace/rosetta-extensions`, 15 under `stack-demo/` (including the two nested `studio` checkouts), and
  3 under `stack-dev/`. None is `infrastructure`. So the `13c248e6` anchors at `architecture_overview.md:3`,
  `:47`, `:227`, `:256`, `:332`, `:337` and `jobsimulation.md:78-87` are unverifiable from here, while the
  competing "never been in any clone set" statements stand un-retracted at `jobsimulation.md:12`.

  The measured cms-repo facts both sides quote **do** check out and are not the problem:
  `cms/terraform/main.tf:39` = `service_desired_count = 0` in a 191-line file at `f38c0c4a` (I read both),
  and `6efa1d5`/`f38c0c4` deleted the build-production workflow. The disagreement is purely about whether the
  deciding `infrastructure` declaration has been read.

  Grading note per the ADDENDUM: `architecture_overview.md:227` is a *retraction*, which the instrument says
  not to book on its own. I am booking the **surviving retracted twin** at `jobsimulation.md:12`, which is a
  live claim, not a retraction.

- **Confidence:** high (that the corpus contradicts itself); the underlying platform fact is unsettleable
  from this tree.

---

### B3 — `jobsimulation.md:54` declares the production RPC declaration "not measurable … never been in any clone set", 24 lines above its own measurement of that repo

- **Anchor:** `corpus/services/jobsimulation.md:54` —
  > "**And the production declaration is not measurable from this repo at all:** it lives in
  > `infrastructure`, which has never been in any clone set — so no *"still names"* claim can be made here in
  > either direction."

- **Why it is false / contradictory:** the same bullet block at `:78-87` opens **"MEASURED at
  `infrastructure` `13c248e6` (iter-123)"** and quotes `terraform/production/services.tf:475` and `:455-474`
  verbatim. A file cannot both be outside every clone set and be quoted by line number. Booked separately
  from B2 because it is a different sentence with a different consequence (the RPC-address surface, not
  cms's ECS verdict) and needs its own repair — `platform-alignment.md` §5 rule 40(a): a finding may be
  declined, but not silently, and a per-site discharge is the post-condition.

  The rest of that same sentence is **correct and I re-derived it independently**: no `.tf` file in any clone
  names `http://backend.internal.anthropos:8081` — 0 hits over **44** tracked `.tf` files across the 13
  `stack-demo` repos at their own HEADs (my count: app 5, cms 4, graphql-wundergraph 4, jobsimulation 6,
  messenger 4, next-web-app 2, roadrunner 4, rosetta-extensions 2, sentinel 4, storage 5, studio-desk 4 = 44;
  ant-academy and platform 0; the two nested `studio` repos 0) and **59** on a raw filesystem sweep. The
  literal occurs **6** times clone-set-wide, none in terraform: `app/knowledge/service-dependencies.md:52`
  (past tense, exactly as described) plus 5 in `rosetta-extensions` test fixtures.

- **Confidence:** high

---

### B4 — `skiller.md:26` repeats the same retracted "`infrastructure` has never been in any clone set"

- **Anchor:** `corpus/services/skiller.md:24-26` —
  > "No `.tf` file in any clone names that literal (0 hits over all 44 tracked `.tf` files in the 13
  > `stack-demo` repos, each at its own HEAD, 2026-08-06), and the deciding declaration lives in the
  > `infrastructure` repo, **which has never been in any clone set** — see [`backend.md`](./backend.md)…"

- **Why it is false / contradictory:** same predicate as B2/B3, third site, third file. It is contradicted by
  `architecture_overview.md:227`'s explicit retraction (*"that was a clone-set limit, not a measurement
  limit, and the fix was to clone the repo"*) and by the eleven corpus files that now cite measurements at
  `infrastructure` `13c248e6` (`grep -rl 13c248e6 corpus/`: `service_taxonomy.md`, `architecture_overview.md`,
  `platform-migration-status.md`, `dependency_map.md`, `org-repos.md`, `external_services.md`,
  `messenger.md`, `graphql-wundergraph.md`, `backend.md`, `jobsimulation.md`, `cms.md`).

  Booked as its own line because it is in a **different file from a different owner** — this is the exact
  cross-file drift `platform-alignment.md` §5 rule 19 measured at 5-of-8 for the last repair pass.

  The **44 `.tf` / 0-hit** half of this sentence I re-derived independently and it is **exact** (see B3).

- **Confidence:** high

---

### B5 — "seven routes mounted on the root outside any group" — measured 8 under the same scope that yields the 11-group denominator

- **Anchor:** `corpus/architecture/architecture_overview.md:404-408` —
  > "…the REST surface has **no BLANKET authz middleware**: authorization there is opt-in per group or per
  > handler, and only 2 of its **11** Echo groups carry a group-level one (`cbGate`,
  > `internal/web/backend/gate.go:27-49`) — **plus seven routes mounted on the root outside any group, so no
  > group-level statement reaches them at all** (⚠️ this line said *"6 Echo groups"* until M257x run 82; the
  > denominator was re-measured repo-wide at `app` `ad9f3c498`…)"

- **Why it is false:** I enumerated both quantities at `app ad9f3c49`, excluding `_test.go` and the separate
  `cmd/labsdemo` binary.

  **Echo groups = 11** ✓ (`.Group(` on the root Echo, excluding the `graph.go:15466` complexity false
  positive and the `emailpreview/handler.go:6` comment): `internal/invitations/handlers.go:31` `/api/invitations`,
  `academy_embeddings_admin.go:41`, `backend.go:121` `/api`, `:171` `/ask`, `:194` `/assignment-builder`,
  `:210` `/admin/backfill`, `:229` `/coursebuilder`, `:273` `/credits`, `content_admin.go:35`,
  `emailpreview/handler.go:66`, `labs_admin.go:31`. And exactly **2** carry `cbGate` — `:232` and `:276` ✓.
  `gate.go:27-49` is `courseBuilderAccessGate` ✓.

  **Root-mounted routes = 8, not 7.** Enumerating every `e.<METHOD>(` on the root instance across
  `internal/**`:
  1. `internal/aireadiness/notifications/handlers.go:41` — `e.GET(unsubscribePath, …)` (`/ai-readiness/unsubscribe/:token`)
  2. `internal/web/backend/backend.go:117` — `/api/schema.json`
  3. `backend.go:309` — `/api/*` (dev-only)
  4. `backend.go:315` — `/graphql` playground (dev-only)
  5. `backend.go:317` — `/graphql/query`
  6. `backend.go:324` — `/api/webhook/directus`
  7. `internal/web/backend/content.go:23` — `/content/catalog.json`
  8. `internal/web/backend/labs_admin.go:40` — `/v1/labs/:slug/workspace.tar.gz`

  #1 is registered on the same root Echo (`internal/web/web.go:153` → `RegisterRoutes(srv.e, …)`), and its
  own doc comment says *"Public / unauthenticated by design"*. **7 is reachable only by scoping to
  `internal/web/backend/` — a scope that would also drop `internal/invitations/handlers.go:31` and take the
  group count from 11 to 10.** One sentence, two incompatible scopes; the sentence explicitly says the group
  denominator was "re-measured **repo-wide**", so the root count is the stale half.

  The direction of the error matters: it **undercounts the routes that escape group-level authorization** by
  one, in a security passage whose whole purpose is to enumerate that escape surface.

  **Why I hesitated:** it is a ±1 count and the sentence's load-bearing predicate ("no blanket authz on
  REST; it is opt-in per group or per handler") is TRUE and well-supported — `resolver_skiller_taxonomy_authz.go:53-66`
  says *"that gate … FAILS OPEN … Do NOT rely on the blanket gate for this surface"*, verbatim, exactly as
  cited. The instrument lists "wrong counts" under BLOCKER and says to resolve doubt upward, so I booked it
  there rather than as a MINOR.

- **Confidence:** medium

---

## Minors

- `corpus/services/roadrunner.md:13-14` — **doubled word**: *"…still reads `service_desired_count = 1` and has
  / has **not been touched since `84a4b4f` (2025-12-15)**"*. The claim itself is exactly right —
  `git blame -L 19,19 87d8d443 -- terraform/main.tf` → `84a4b4f1 … 2025-12-15`, and `87d8d443` really does
  touch only `.github/workflows/bump-version.yml` (+3), and the file's own most recent touch really is
  `e45eb61` (2026-05-27, the module-source swap).
- `corpus/services/studio-room.md:351-362` — the `requirements.txt` listing reorders the file: the shipped
  order at `aeec036a` is `openai, anthropic, rich, pyyaml, requests, jinja2, mistralai, pytest,
  pytest-asyncio` (mistralai is line **8**, which the callout at `:368` cites correctly). The doc prints
  `mistralai` third. Same set, no factual consequence.
- `corpus/services/roadrunner.md:48,53,66-67,84` asserts GitHub **archive** states as fact (skiller archived
  2026-07-01, skillpath 2026-07-31, roadrunner "**not** archived", chronos "last push 2026-04-23") while
  `corpus/services/jobsimulation.md:24` declares the same class of fact *"not visible to this corpus"*. Not
  booked as a BLOCKER: archive state is unverifiable from a clone in **either** direction, so I can refute
  neither side, and no platform-state claim is misrepresented.

---

## Positively cleared (audited zeros)

**ENUMERATED** — predicate re-derived from source independently, cardinality compared:

1. **`architecture_overview.md:389-395` — the multi-tenancy split, all four figures.** I did not re-run the
   document's arithmetic; I rebuilt the SET. `internal/data/ent/schema/` at `ad9f3c49` holds **139** `.go`
   files, of which **135** declare `ent.Schema` (the 4 that do not: `database_types.go`, `mixin.go`,
   `skiller_mixins.go`, `skillpath_mixins.go`) → the **135** denominator is right. `OrganizationMixin{}`
   matches **30** lines, of which `user_resource.go:22` is `// OrganizationMixin{},  // We need to work on
   this` → **29** live. `Membership` (`org_membership.go:172`) and `Organization` (`organization.go:56`) each
   declare their own `Policy()` → **31**. Independently, the org-id **carrier** set (declares
   `field.UUID("organization_id")` **or** uses `OrganizationIDMixin{}` **or** uses `OrganizationMixin{}`,
   restricted to the 135 schemas) is **54**; 54 − 31 = **23** carriers with no organization policy, of which
   **7** use `OrganizationIDMixin{}` (`category`, `jobrole`, `similarity`, `skill`, `specialization`,
   `studio_document`, `studio_task`) and **16** use no mixin at all — the 17th of that group,
   `academy_feedback.go`, uses `UserMixin{}`, which **does** declare a `Policy()` (`mixin.go:99`), so it is
   correctly excluded from "no policy at all". **23 = 16 + 7, and 31 + 23 = 54.** Every figure closes.
2. **`architecture_overview.md:400-402` — "six paths that reach the resolver before the single Sentinel
   call."** `gqlauthz.go` has exactly **six** `return next(ctx)` before `:222`'s
   `OrgCheckUserPermission`: `:161`, `:177`, `:191`, `:197`, `:203`, `:219`. `:190-191` is the no-active-org
   short-circuit; `:196-197` is the `errUnknownTarget` short-circuit. Exact.
3. **`architecture_overview.md:83` `authn` half.** 0 hits for `github.com/anthropos-work/authn` in all
   **seven** Go clones' `go.mod` at their own refs, against `colony` = 7/7. Exact. (The `ai` half is B1.)
4. **`architecture_overview.md:12,73,161-170,228` — "five containers / two Go services / 5 declared, 7
   effective."** `docker-compose.yml` at `0c91421d` declares **5** services (`sentinel`, `backend`,
   `studio-desk`, `next-web-app`, `gotenberg`); `include: common.yml` adds `postgresql` + `redis` → **7**
   effective. `profiles:` appears at exactly 4 lines (`:110` backend, `:141` studio-desk, `:168`
   next-web-app, `:183` gotenberg), so `sentinel`/`postgresql`/`redis` are the profile-less floor and `core`
   selects **5**. `Makefile:10` `PROFILE ?= core`. Exact.
5. **`architecture_overview.md:270-289` / `jobsimulation.md:158` — the compose address inventory.**
   `AUTHORIZATION_ADDRESS=http://sentinel:8087` at `:48`, `GOTENBERG_URL` at `:57`, `JUDGE0_BASE_URL` at
   `:59`, `REDIS_ADDR` at `:66`, gotenberg's `profiles: [core, backend, all]` at `:183`; **zero** `_RPC_ADDR`
   in any `.yml` at `0c91421d` (positive control: `profiles:` returns 4 on the same engine/ref).
6. **The `d11a403` "MIDDLE TWO" re-point** (`skiller.md:19-20`, `jobsimulation.md:48-50,135`,
   `architecture_overview.md:283-287`). At `d11a403^` the messenger block reads `BACKEND_USERS=backend:8083`,
   `CMS=cms:8091`, `JOBSIMULATION=jobsimulation:8401`, `SKILLER=backend:8083`; at `d11a403` all four read
   `http://backend:8083`. Exactly two moved, and they are CMS + JOBSIMULATION. Exact.
7. **`frontend_architecture.md:41-50` — the whole REST/SSE table, both refs.** Independently derived over
   non-test `.ts`/`.tsx`, `e2e/` excluded: `bb3313bc` → **21** files / **29** occurrences / **43** `fetch(` /
   core-js **12 env / 25 calls**; `8297c684` → **22 / 31 / 47 / 12 / 27**; `origin/main f97ba659` → 22 / 31.
   **Every cell reproduces.** `new EventSource(` = 0 files (positive control: `EventSource` = 1, in
   `packages/core-js/src/talkToData/api.ts`, whose `:213-214` carries the quoted rationale verbatim).
   The 22-file list matches the prose enumeration item for item.
8. **`frontend_architecture.md:59` / `architecture_overview.md:60` — `next: ~16.2.12` in four `apps/*`.**
   `apps/{web,hiring,integration,maintenance}/package.json` each declare `"next": "~16.2.12"` (web at `:46`);
   `apps/mobile` declares none; lockfile resolves `16.2.12`. Tilde, not caret. Exact. Also 8 i18n locales
   (`de en es fr it ja nl pt`), `pnpm@10.30.3`, `engines.node >=24.0.0`, TS `^5.9.3`, `!apps/mobile` in
   `pnpm-workspace.yaml`, `UPGRADE-IMPACT-next16.md` present, ports 3000/3001/3002 and mobile Expo `3031`.
9. **`jobsimulation.md:39-45` — "the 23 session/run tables."** `terraform/migrations/20260722081626_jobsim_data_model.sql`
   contains exactly **23** `CREATE TABLE` statements, and the names match the prose sample. The very next
   migration `20260722104506.sql` creates `job_simulation_sessions` at `:2` and `DROP TABLE "sessions"` at
   `:79`. Exact.
10. **`jobsimulation.md:142-147` — the mirror-drop migration.** `20260729133514.sql` is 64 lines: `:15-23`
    the two id remaps, `:36-44` the two orphan-NULLing UPDATEs, `:58-62` the *"5. Drop the mirrors."* block,
    `:62-63` both `DROP TABLE`s. **`INSERT INTO` count = 0** (a naive `grep -ci insert` returns 1 — the
    substring inside `on_insert_local_jobsimulation_sessions_update_memberships()` at `:64`; the corpus's
    "0 INSERTs" is the correct reading). `intelligence.go:1700` is `m.ent.JobSimulationSession.Query()`.
11. **`roadrunner.md:36,41,130` — every roadrunner-mention count.** 0 Go hits for `ROADRUNNER_RPC_ADDR` /
    `RoadRunnerService` / `roadrunner:10401` at `app b948604f` **and** `jobsimulation 462343b0` (still 0 at
    `app ad9f3c49`). Case-insensitive per-clone sweep at each own HEAD: **app 25 files, jobsimulation 8,
    platform 3, studio-desk 3, next-web-app 1** — all five figures exact. `jobsimulation` Go source: exactly
    **one** mention, `internal/runner/runner.go:3`, a comment; repo-wide 14 lines / 8 files case-insensitive,
    5 lines / 3 files exact-case. All exact.
12. **`skiller.md:52-61` — the taxonomy figures.** Read straight out of the checked-in capture manifest
    (`.agentspace/snapshots/taxonomy/…/manifest.json`, `source: primary-read`, `public_only: true`,
    `predicate: org-null`, `captured_at 2026-06-29T14:43:00Z`): `public.skills` **42790**, `public.job_roles`
    **22470**, both under `organization_id IS NULL`; `public.job_role_embeddings` **18919** — confirming that
    18,919 is a *different table's* row count, exactly as the retraction of "18K roles" says. The second
    manifest is the pre-M209 `skiller.*`-schema capture with identical counts, corroborating the
    "re-grounded to `public`" claim. Also verified: `skill_embeddings`/`job_role_embeddings` carry
    `extensions.vector(1536)` with `USING IVFFLAT` indexes (`20260615130000_skiller_taxonomy.sql:53,61,243,251`);
    `internal/content/language.go:12-19` declares exactly **8** `Language` values.
13. **`skiller.md:32-36`.** All five named RPC methods (`GetSkills`, `GetSkill`, `SearchSkill`, `MatchSkill`,
    `GetJobRole`) are implemented in `app/internal/rpc/skillerrpc/skiller.go`; `skiller_taxonomy.graphqls`
    is at `internal/web/backend/graphql/graph/schemas/`; its `:7` says `categoryTree, fullCategoryTree`
    *"stay unported — no consumers"*. Exact. All named Ent schema files exist.
14. **`studio-room.md:186` — "exactly nine arguments."** `git grep -n add_argument aeec036a -- gen.py`
    returns exactly **9**, at `:484-492`, matching the documented flag list one for one. No other
    `add_argument` anywhere in `gen.py`.
15. **`studio-room.md:366-395` — the whole `mistralai` callout, the §5-rule-44 worked example.**
    `git grep -ci mistral aeec036a` in `stack-demo/app/studio` at its **own** ref: `requirements.txt` 1 +
    `tools/pdf2md.py` 18 + `tools/r3.py` 3 = **22 lines / 3 files**, exact. `pdf2md.py:24` is
    `from mistralai import Mistral`; `:96` `client = Mistral(api_key=api_key)`; `:127`
    `model="mistral-ocr-latest"`; `r3.py:194` `if "MISTRAL_API_KEY" not in env:`. `app/studio/.gitignore:2`
    is `tools/`; `app/.gitignore:79` is `studio/*`; `cms/.gitignore:129` is `studio/*`. The provider registry
    at `services/ai.py:705-708` is exactly `{'openai','azure','anthropic'}` and `:1-2` imports only
    openai/anthropic. `tools/` holds exactly `any2pdf.py`, `md2cleanMd.py`, `pdf2md.py`, `r3.py`.
    `app/Dockerfile:45-46` copies `studio/` and pip-installs. Exact throughout.
16. **`studio-room.md:35,344-346,375-377` — the invocation contract.** `studioManager.go:93-96` is the const
    block with `studioVenvDir = "studio/studio-venv"`; `:119` is
    `return s.runCommand(ctx, pyBin, append([]string{"studio/gen.py"}, tokens...))`; `:1045` is the
    `studio/postgen.py` twin; `runCommand` is `:1099-1101` with its no-shell comment at `:1096-1098`
    (both cited ranges resolve, they name the comment and the function respectively). Asynq: `worker.go:29`
    `Concurrency: 5`, `:32` `AiVideoQueue: 7`, `:33` `StudioQueue: 3`. Exact.
17. **`studio-room.md:166-180` — the post-gen target set.** `agents/simulation/postgen/` holds exactly
    `guidance.py`, `metadata.py`, `translation.py`, `toolkit.py` (+ `exporter.py`, `testing.py`) — the four
    documented targets, plus the documented "`testing` phase module also exists" and the correct claim that
    export is not a selectable target. `postgen.py:396-398` marks `--media`, `--simid`, `--target` all
    `required=True`. Exact.
18. **`jobsimulation.md:9,73,77` / `architecture_overview.md:20,226` — the M810 jobsim teardown.** Graded at
    the ref the claim names, `6092c6d2` (which is on `origin/main`, 4 commits past the `462343b0` checkout —
    at the *checkout*, `module "jobsimulation"` is still declared with `service_desired_count = 0` at `:40`,
    which is exactly the trap here). At `6092c6d2`, `terraform/main.tf` is 56 lines: `:15-22` is the
    M810-decommission header naming the deleted `module "jobsimulation"` block and the destroyed ECS
    service / task definition / ECR repository; `:24-40` enumerates the surviving LiveKit + Chime buckets,
    the SSM parameters and the atlas tracker; `:38-40` says dropping the legacy schema is a separate step;
    `service_desired_count` appears nowhere. Every clause verbatim-correct. The four 2026-08-04 commits
    (`6092c6d2`, `caf36c96` = PR #439, `1e40d184`, `82cb66ec`) are exact.
19. **`architecture_overview.md:299` / `:3` — the 3 → 1 supergraph.** `915da06`'s subject says "2→1" while
    its diff deletes **both** `- name: jobsimulation` **and** `- name: cms` from configs that listed three,
    and deletes `schemas/{cms,jobsimulation}.graphqls`. Only `schemas/backend.graphqls` survives at
    `60c229f3`. Dated 2026-07-29. The corpus's correction of the commit's own subject line is right.
20. **`roadrunner.md:87,128,169,205` — the service internals.** `cmd/root.go:110` `PORT`→8080, `:84`
    `RPC_PORT`→8081; compose at `2adcf71` `:291-292` published `10400`/`10401`, `:298` `PORT=10400`, `:302`
    `RPC_PORT=10401`, env block `:296-302`. `queues.go:4 DefaultQueue = "roadrunner:default"`,
    `runner.go:126 asynq.MaxRetry(3)`, `worker.go:25 Concurrency: 10`, `runner.go:244 maxRetries = 15` with
    `:273 time.Sleep(1 * time.Second)`. Exactly 3 RPC methods. `internal/lsp` imported by nothing (0 hits).
    `languages.go:26 case "py"`. **Zero** `_test.go` files, `Dockerfile:18` = `RUN go test -v ./...`.
    Go 1.25.0, asynq v0.25.1, gorilla/websocket. Exact.
21. **`architecture_overview.md:317-322` — the Mistral-OCR retraction.** `markdownManager.go:29-30` is
    `NewMarkdownManager(aiKey)` → `mistralocr.New(aiKey)`; `studioManager.go:583` is
    `NewMarkdownManager(os.Getenv("MISTRAL_API_KEY"))`. The retraction's own evidence checks: `:11` really is
    the closing paren of the import block and `:19` really is the doc comment recording that the `os.Getenv`
    read *was removed*. Exact.
22. **`architecture_overview.md:311-316` — the AI-routing anchors.** `internal/jobsimulation/ai/ai.go:262-276`
    is the Azure-EU default + `flag_use_azure_us` swap; `:129` `isThrottlingError`; applied at `:166` and
    `:325`; `:85-88` `config.WithRegion("eu-west-1")` for the Bedrock/Anthropic client. All three shipped
    `app/studio/configs/*.ini` pin `azure` for every mode. Exact.
23. **`jobsimulation.md:215-223,250-253` — the cobra-signature block across three refs.** `main.go` `func main()`
    at `:216` @ `7177374`, `:212` @ `b948604`, `:229` @ `ad9f3c49`; the only `spf13/cobra` importer in the
    whole repo is `cmd/createTaxonomy/main.go`; `jobsimwiring.Wire` at `:721` with its `log.Fatalf` at `:723`
    @ `ad9f3c49`, `:670` @ `9d00a313`, `:614` @ `b948604`; and `:670` @ `ad9f3c49` really is
    `skillerAzureEndpointEu = &v`. Both the pinned `:670` (at `9d00a313`) and the corrected `:721/:723`
    (at `ad9f3c49`) are simultaneously true — the two sentences are not in conflict.
24. **`architecture_overview.md:356-379` — the storage retraction.** `storage/terraform/main.tf` @ `9f8cb53`
    is exactly **18** lines, `:9-11` the *"The ECS service that used to live here is GONE"* comment, `:13-16`
    the `prevent_destroy` rationale; `63bffc8` really does read `service_desired_count = 0`.
    `STORAGE_RPC_ADDR` @ `9d00a313`: **3** Go hits, all comments (`main.go:451`,
    `internal/jobsimwiring/wiring.go:101`, `internal/storagens/callsites_test.go:189`), against **29 lines /
    18 files** repo-wide. `main.go:504` at `origin/main` carries the quoted comment. `wiring.go:115` @
    `b948604` is a live `getenv("STORAGE_RPC_ADDR")` read. Every distinction the passage draws is exact.
25. **`architecture_overview.md:449` and `:3`.** `graphql-wundergraph/terraform/main.tf:31` passes
    `private_subnets_ids` and names no public subnet; `:20` is `service_desired_count = 1`. Exact.
26. **`studio-room.md:430` — "no `cms` compose service at all since platform `d11a403`."** `d11a403^` declares
    `cms:` at `:144`; `d11a403` does not. (Its subject names only roadrunner; the diff removes cms +
    jobsimulation + roadrunner. Both `roadrunner.md:27-29` and `architecture_overview.md:216-218` say
    "grade the commit by its diff, not its message" — correct.)
27. **`frontend_architecture.md:11,39` and `roadrunner.md:50-52`.** `repos.yml` entry counts: **9** at
    `2adcf71`, **6** at `0dab54d` (app, sentinel, storage, messenger, next-web-app, studio-desk), **4** at
    `0c91421`. `docker-compose.yml:141` `profiles: [studio-desk, all]` at `0c91421`, and `:226` at `0dab54d`
    — the "only the line number keeps moving" claim is exact. `:152` build arg / `:161` runtime env for
    `NEXT_PUBLIC_BACKEND_API_URL`. `5050:8080` at `2adcf71^` — so `:5050` really was a host mapping only.
28. **`architecture_overview.md:40,265` — ant-academy as a public storefront.** Read via
    `git show 22df69dd8:<path>` (the tree is dirty). `code/proxy.js` lists `/`, `/library`, `/library/(.*)`,
    `/free`, `/catalog.json`, `/api/verify/(.*)` as public-by-design, with `robots.js`/`sitemap`/`llms.txt`
    pointing at a public host. The refutation of "`@anthropos.work`-only" holds.

**RE-DERIVED (weaker — flagged as such):** none. Where a passage displayed its own derivation
(`:389-395`, `:83`, `:41-50`), I rebuilt the SET from source before touching the arithmetic, per §5 rule 24.

---

## What I could not settle

1. **Everything cited to the `infrastructure` repo.** `infrastructure` is in **no clone** in this tree (I
   enumerated all 19 git trees). So `architecture_overview.md:3`, `:47`, `:227`, `:256`, `:332`, `:337` and
   `jobsimulation.md:78-87` — every `13c248e6` / `terraform/production/services.tf:475`, `:455-474`,
   `:509-517`, `:64-70` anchor — are unverifiable from here in **either** direction. This is the substrate of
   B2/B3/B4. **What would settle it:** the `infrastructure` clone at `13c248e6` in the ground-truth set, with
   its sha and fetch time recorded in the reading's sheet like every other clone (§5 rule 26).
2. **`architecture_overview.md:206,453-459` — db-backup.** "Bash (43 lines, Alpine; zero `.go` files)",
   "`rate(12 hours)`", "trigger commented out since `7dd1b80` (2025-05-29)", "never Azure". The `db-backup`
   repo is in no clone set. **Settled by:** cloning it and reading the EventBridge rule + `git log`.
3. **GitHub archive states** — `roadrunner.md:48` (skiller 2026-07-01, skillpath 2026-07-31, intelligence),
   `:53`/`:84` (roadrunner not archived), `:66-67` (chronos not archived, last push 2026-04-23);
   `jobsimulation.md:24` and `architecture_overview.md:20` correctly decline to assert it for jobsimulation.
   None of this is derivable from a clone. **Settled by:** one `gh api repos/anthropos-work/<name>` per repo,
   recorded with its timestamp.
4. **`architecture_overview.md:13,241` / `skiller.md:52-56` — whether the taxonomy floors are still current.**
   The manifest figures reproduce **exactly** (42,790 / 22,470 / 18,919), but the capture is dated
   2026-06-29 and is public-only by construction. The corpus is careful to call them floors; I can confirm
   the capture, not the live database.
5. **`architecture_overview.md:460`** — "ECS health checks every 30 seconds with automated rollback on
   failure." No terraform in any clone states a 30 s interval; the module lives in `infrastructure`.
6. **`studio-room.md:5,25,37` — "app **v1.360.1**" as the exact version that introduced `additional_repo`.**
   The mechanism verifies (`.github/workflows/build-production.yml:29`
   `additional_repo: "anthropos-studio-room:studio"`, commit `851cf3fb1` *"fix(ci): pull studio via
   additional_repo like cms"*, and the `v1.360.1` tag exists), but I did not confirm that commit is the one
   `v1.360.1` points at.

---

## Summary

**BLOCKERS = 5 · MINORS = 3**

Four of the five are the **self-contradiction class** — `platform-alignment.md` §5 rule 21's cheapest
instrument, needing no ground-truth read — and all four are the same shape: a claim corrected at one site
while its twin stands un-retracted at another (B1: `ai` corrected in `shared_libraries.md` +
`jobsimulation.md`, not in `architecture_overview.md`; B2/B3/B4: the `infrastructure` clone-set predicate
retracted in `architecture_overview.md:227` while three sites in two files still assert it — one of them
74 lines from its own contradiction). That is rule 19's cross-file drift, measured again.
