# Seat D — reading #34 (M257x iter-131)

## Ground truth re-verified at this seat's open

All shas match the ADDENDUM: app `ad9f3c49`, platform `0c91421d`, next-web-app `8297c684`,
sentinel `f2c46190`, studio-desk `41ee3575`, ant-academy `22df69dd`, storage `4ce8ece5`,
messenger `fa47850d`, cms `ca50c817`, graphql-wundergraph `60c229f3`, roadrunner `87d8d443`,
jobsimulation `462343b0`, rext consumption `09d06070`, rext authoring `f2ea567b`,
nested `app/studio` `aeec036a`.

`corpus/ops/platform-alignment.md` §5 read in full (rules 1–44, lines 259–1354) before starting.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/architecture/service_taxonomy.md` | 530 | yes |
| `corpus/architecture/security_compliance.md` | 441 | yes |
| `corpus/architecture/shared_libraries.md` | 353 | yes |
| `corpus/services/ai-labs.md` | 171 | yes |
| `corpus/services/customerio-sync.md` | 165 | yes |
| `corpus/services/intelligence.md` | 18 | yes |

Search positive controls run in the same pass: `gen.py` → 19 files in the nested `app/studio`
repo at `aeec036a` (so the zeros reported below over that tree are real, not engine failures);
`colony` → required by 7/7 Go clones (so the `authn` 0/7 is real).

---

## Blockers

### B1 — `service_taxonomy.md` asserts `app` builds against three private Go modules; it builds against five

- **Anchor:** `corpus/architecture/service_taxonomy.md:175` — *"**Shared Libraries** (imported as
  private Go modules — **not** cloned by `make init`; pulled at Docker build via
  `GH_PAT`/`GOPRIVATE`). **`ai` is NO LONGER among them** … **the live private-module set a stack
  builds is colony, proto, taxonomy**."* Second site, same file: `:525` — *"**5 libraries, 3
  imported by a service a stack builds** — colony, proto, taxonomy"*.
- **Why it is false:** `app/go.mod:14-18` @ `ad9f3c49` requires **five** `anthropos-work` modules,
  all direct, none `// indirect`:
  ```
  14  github.com/anthropos-work/analytics-go v0.3.1
  15  github.com/anthropos-work/colony       v0.35.2
  16  github.com/anthropos-work/proto        v1.210.0
  17  github.com/anthropos-work/storage      v0.15.2
  18  github.com/anthropos-work/taxonomy     v1.2.0
  ```
  `go.sum:64-73` carries exactly those five, two lines each. All five are org-private and all five
  are pulled by the *same* mechanism the sentence describes: `app/Dockerfile:13` and
  `app/Dockerfile.dev:13` both set `ENV GOPRIVATE=github.com/anthropos-work/*`. So "the live
  private-module set a stack builds" is `{analytics-go, colony, proto, storage, taxonomy}`, not
  `{colony, proto, taxonomy}`.
- **And the corpus already says so, one file away.** `shared_libraries.md:65-67` states the rule
  this sentence breaks, verbatim: *"**Those ratios are over THE FIVE, and the denominator hides two
  live modules** — `app` also requires `storage` and `analytics-go` … so *"three"* is the answer to
  *"how many of the five"*, never to *"what does `app` depend on"*."* `shared_libraries.md:21-23`
  adds the consequence: *"Read the two as one rule and you conclude `storage` is deletable, which is
  false twice over."* `service_taxonomy.md` never scopes the claim to "of the five" at `:175`, and
  mentions **`analytics-go` 0 times** and the **`storage` module 0 times** in the whole file
  (`grep -n 'analytics-go' corpus/architecture/service_taxonomy.md` → exit 1;
  `grep -n 'anthropos-work/storage'` → exit 1). Its shared-libraries table (`:177-183`) lists only
  colony/proto/ai/authn/taxonomy. This is rule 19's cross-file half-repair: `shared_libraries.md`
  was corrected at iter-123 and its twin was not.
- **Confidence:** high

### B2 — `main.go:507-508` is cited as the analytics-go wiring; at every reachable ref it is a storage-in-app comment

- **Anchor:** `corpus/architecture/shared_libraries.md:77` — *"`analytics-go` … Wrapped by
  `app/internal/tracking`; the load-bearing consumer is **Stripe subscription lifecycle → Brevo**,
  `app/internal/payments/handler.go:302-316` (seven event names switched off `entSub.Status`),
  **wired `main.go:507-508`**."*
- **Why it is false:** the cell names no ref, so it grades at the checkout `ad9f3c49`. There,
  `main.go:507-508` reads:
  ```
  507  // public clients, cmsStorage and jobsimwiring.Wire). Per-namespace clients are
  508  // derived at each consumer via internalstorage.NewClient / NewPublicClient.
  ```
  — the middle of the **storage-in-app** comment block (`:503-518`), an unrelated construct. The
  actual wiring is fourteen lines earlier:
  ```
  494  trackingManager := tracking.New(os.Getenv("BREVO_KEY"))
  495  paymentsManager := payments.New(environment, os.Getenv("STRIPE_SECRET_KEY"), os.Getenv("STRIPE_ENDPOINT_SECRET"), ent, authz, trackingManager)
  ```
  and `internal/payments/handler.go:89` takes it as `analyticsManager *tracking.Manager`, stored at
  `:100`, used at `:302`. I also checked the two other legitimate refs: at `b948604f`,
  `main.go:507-508` is a jobsim-in-app comment about `BACKEND_USERS_RPC_ADDR`; at `9d00a313` it is
  an AI-Readiness auto-assign comment. **The anchor names the wrong construct at all three refs.**
  (The rest of the cell is exact: `handler.go:302-316` is the `analyticsManager.Track(analytics.Event{…})`
  call, the switch at `:283` is on `entSub.Status` with seven cases `:284-297`, and the `analytics`
  import at `handler.go:10` is `github.com/anthropos-work/analytics-go`.)
- **Confidence:** high

### B3 — the banner's whole measurement is pinned to an `app` sha that exists in no clone

- **Anchor:** `corpus/architecture/shared_libraries.md:6` — *"`app`'s actual org-private module
  requirements, measured at `app` **`3eaadae6`** (v1.371.1), `app/go.mod:14-18`"*; repeated at
  `:38` — *"at `ad9f3c49` and `3eaadae6` it is the five above."*
- **Why it is unsupportable:** `git -C stack-demo/app cat-file -t 3eaadae6` → `fatal: Not a valid
  object name`. `git rev-parse v1.371.1` → unknown revision. `app`'s newest tag in the clone is
  **`v1.369.0`** and `origin/main` is **`ad9f3c49`** (2026-08-06), which the ADDENDUM names as this
  reading's ground truth. I tried the sha in **all eleven** platform clones (`app`, `platform`,
  `next-web-app`, `sentinel`, `storage`, `messenger`, `cms`, `graphql-wundergraph`, `roadrunner`,
  `jobsimulation`, `studio-desk`, `ant-academy`) — not a valid object in any of them. The entire
  five-module table, the `go.sum:64-73` claim and the `:295` `replace` claim hang off a
  measurement no reader of this ground truth can reproduce.
- **Why I hesitated, and booked it anyway:** every stated *value* re-derives correctly at
  `ad9f3c49` (analytics-go `:14`, colony `:15`, proto `:16`, storage `:17`, taxonomy `:18`; `go.sum`
  lines 64–73 exactly; `go.mod:295` is the `getsentry/sentry-go/echo` replace and the file is 295
  lines), so the substance is true — it is the *pin* that is unresolvable. The briefing is explicit:
  *"If a passage's line anchor resolves at neither, that is a finding, and say which refs you
  tried."* §5 rule 26 is the same point (*"a measurement that does not name its refs is an
  anecdote"* — here it names a ref nothing can resolve). Note the pin is not isolated:
  `corpus/ops/observability.md:15` asserts *"`origin/main` was `3eaadae6` on 2026-08-07"*, i.e. the
  corpus was written against a **fetched-forward `app` clone** that this reading's frozen ground
  truth does not contain (§5 rule 41a).
- **Confidence:** medium

### B4 — the cross-file citation for the *"only"* defect lands on an unrelated bullet

- **Anchor:** `corpus/architecture/security_compliance.md:156` — *"The same class as the
  **`clerk-integration.md:40`** *"only"* and the `cms.md` `bash -c` inversion: an absolute
  quantifier over a security surface, published unhedged."*
- **Why it is false:** `corpus/services/clerk-integration.md:40` is
  *"- **Org invitations** — backend create/revoke (+ a hand-rolled bulk call); frontend
  list/accept."* — it contains no absolute quantifier and no *"only"*. The *"only"* the sentence is
  pointing at is four lines down, at `clerk-integration.md:44`: *"**Sign-in tokens** — **five** live
  minting sites … **See the enumeration below — this bullet used to say "only", and it was
  false.**"* The anchor resolves, and names the wrong construct — the exact class this same file
  describes at `:282-283` (*"a citation that stops one line short of its own subject is exactly the
  wrong-construct class `anchor_construct_guard` does not detect"*), and rule 34's class (a corpus
  repair moved lines and the self-citation was not re-derived).
- **Why I hesitated:** the citation supports an illustrative analogy, not a load-bearing platform
  fact, so a reader following it loses a minute rather than being misled about the system. Booked
  per the briefing's *"when in doubt, book it as a BLOCKER."*
- **Confidence:** medium

---

## Minors

- `shared_libraries.md:76` — *"`STORAGE_RPC_ADDR` occurs in zero Go source"*. It occurs in **two**
  `.go` files at `ad9f3c49`: `internal/jobsimwiring/wiring.go:101` and
  `internal/storagens/callsites_test.go:189`, both **comments** explaining its absence. The
  load-bearing proposition (no read site, no SDK RPC client) is true and re-derived; root
  `CLAUDE.md` states the accurate form (*"set nowhere and read nowhere"*). Over-strong phrasing only.
  (Checked at both the `app` tree ref and the nested `app/studio` repo at `aeec036a` — 0 there.)
- `shared_libraries.md:101` — *"`88036d7` … **two commits past** the `88bc5592` this row used to
  cite"*. `git rev-list 88bc5592..88036d7 --count` = **1**; `88036d7` is the immediate successor and
  `f2c46190` (the checkout) is the one that is two past. The version claim it supports
  (`v0.34.3 → v0.35.2`, and proto `v1.200.0 → v1.210.0`) is exact.
- `security_compliance.md:85` and `:116` — `mixin.go:98` cited for `UserMixin`'s `Policy()`. At
  `ad9f3c49` (and at `5ba17044`) `:98` is the doc comment *"// Policy defines the privacy policy of
  the UserMixin."* and the `func (UserMixin) Policy()` is at **`:99`**. The sibling cite in the same
  blockquote (`mixin.go:126` for `OrganizationMixin`) lands on the `func` line correctly, so the two
  are inconsistent with each other.
- `security_compliance.md:249` — *"`git grep -nE '\.Group\("' ad9f3c498 -- '*.go' | grep -v
  _test.go` returns **exactly those 11**"*. Re-run: the command returns **12** lines; the 12th is
  `internal/web/backend/emailpreview/handler.go:6`, a **comment** (`// runtime (they're
  oapi-registered under apiRouter = e.Group("/api", ...)`). The conclusion (11 real groups) is
  correct and I enumerated them independently; only the stated derivation is one line off, and it is
  the §5 rule 24 *"grep counts commented-out code"* trap in the doc's own published command.
- `service_taxonomy.md:527` records that the Summary-Table label was corrected at iter-130 because
  Ant Academy is *"**not** internal-only"*, while `:23` (the mermaid subgraph) and `:204` (the Tier-2
  heading) still read *"Studio Services & Standalone **Internal** Apps"*. The Tier-2 body corrects it
  two lines later (`:214-218`) and the tier does contain two genuinely internal apps, so I kept this
  minor rather than booking it.

---

## Positively cleared (audited zeros)

All labelled **ENUMERATED** re-derive the *set* from source independently, then compare cardinality
(§5 rule 24). Nothing below is a bare "I re-derived the arithmetic and it matches."

**`security_compliance.md` — the multi-tenancy fence (Layer 1), the passage rules 17/24 name as
having failed four times.** I enumerated every predicate independently before touching any sum:
- **ENUMERATED** — 139 `.go` files in `internal/data/ent/schema/` @ `ad9f3c49`; **135** declare
  `ent.Schema`; the **4** that do not are `database_types.go`, `mixin.go`, `skiller_mixins.go`,
  `skillpath_mixins.go`. Matches `:79`.
- **ENUMERATED** — **30** files match `OrganizationMixin{}`; `user_resource.go:22` is
  `// OrganizationMixin{},  // We need to work on this` (read in context, `:16-24`), so the live set
  is **29**. Matches `:80` and `:95-97`.
- **ENUMERATED** — **7** `OrganizationIDMixin{}` users, and they are exactly the seven named at
  `:111-112`: `category`, `jobrole`, `similarity`, `skill`, `specialization`, `studio_document`,
  `studio_task`.
- **ENUMERATED** — **18** `ent.Schema` files with a plain `"organization_id"` and neither mixin. My
  list minus `org_membership.go` and `academy_feedback.go` is **exactly** the 16 named at
  `:104-109`, name for name.
- **ENUMERATED, and this is the conjunct the last three audits skipped** — I enumerated **every**
  `func (…) Policy()` in the directory rather than only the ones the document names:
  `mixin.go:99` (UserMixin), `mixin.go:126` (OrganizationMixin), `org_membership.go:172`,
  `organization.go:56`, `user.go:116` — **5 declarations across 4 files**, identical at `5ba17044`.
  So no schema outside the document's list can be adding an org filter, and **31 = 29 + Membership +
  Organization** is right *for the right reason*. `organization.go:96` is
  `rule.FilterSameOrganizations()`; `org_membership.go:172-188` ends in `privacy.AlwaysDenyRule()`;
  `user.go:116` filters by owner, correctly excluded. Partition checks out: 31 + 23 + 81 = 135.
- **ENUMERATED** — every `field.(String|UUID)("tenant_(eid|id)")` in the directory: `lab.go:63`,
  `academy_chapter.go:84`, `academy_skill_path.go:78`, `skill_path_session.go:43` and
  `job_simulation_session.go:35`. The document's *"at least four"* remainder members are the first
  four (the fifth is already inside the 23), `skill_path_session.go` does carry `UserMixin{}` (`:26`),
  and `academy_feedback.go` carries `UserMixin{}` (`:64`) with `organization_id` (`:129`). Exact.
- **ENUMERATED** — the `5ba17044` re-measurement block (`:119-124`): 139 files / 30 / 7 / 18 and
  four Policy-declaring files all reproduce at that ref; `skiller_mixins.go:158` is the 19th
  `organization_id` hit and is a mixin definition, as stated.

**`security_compliance.md` — Layer 2.**
- **ENUMERATED** — all six pre-Sentinel escape paths in `gqlauthz.go` verified in context:
  `:160-161`, `:174-178`, `:190-191`, `:196-197`, `:202-203`, `:209-219`, with the single
  `OrgCheckUserPermission` at `:222` and the doc comment at `:149`. `gqlauthz/target.go:11`
  `grapqlTargetVar = "userId"`.
- **ENUMERATED** — `git grep -nE '\.Group\("' … | grep -v _test.go` → **11** real groups (12 lines,
  1 comment); every declaration line and mount line in both tables verified individually
  (`backend.go:121/171/194/210/229/273`; `invitations/handlers.go:31` ← `web.go:148`;
  `content_admin.go:35` ← `backend.go:289`; `labs_admin.go:31` ← `backend.go:301`;
  `academy_embeddings_admin.go:41` ← `backend.go:295`; `emailpreview/handler.go:66` ← `web.go:162`).
  The `/api` skip list is exactly **10**: 8 webhooks + `/api/skills` + `/api/health`
  (`backend.go:129-140`).
- **ENUMERATED** — the 7 root mounts: `backend.go:317`, `:324`, `:117`, `:315`, `:309`,
  `content.go:23`, `notifications/handlers.go:41` (← `web.go:153`). `content_admin.go:32` says
  *"stays open by design"* verbatim.
- **ENUMERATED** — `AuthzMiddleware|EchoAuthzMiddleware` → **0** hits over `*.go` @ `ad9f3c49`
  (positive control in the same pass: `.Group("` → 12).
- **ENUMERATED** — `ValidateToken` → **8** hits, all in `token.go` + `token_test.go`. Token minting
  `token.go:29-34`, filters `invite.go:159`/`:194`, privacy bypass `:157`/`:190`, *"token possession
  is the authorization"* `:154-155`/`:187-188`, boot guard `main.go:423-427`. Every anchor exact.
- **ENUMERATED** — `cbGate` at `backend.go:227`, defined `gate.go:27-49` with the
  `OrgCheckFeaturePermission(OrgFeatureMembersEdit, orgID)` at `:43`, carried by exactly two groups
  (`:232`, `:276`).
- **ENUMERATED** — the eight service terraform trees (`app`, `sentinel`, `graphql-wundergraph`,
  `messenger`, `cms`, `roadrunner`, `storage`, `jobsimulation`): `public_subnet` in **0** files,
  and **8/8** pass `private_subnets_ids = var.platform_private_subnets_ids`. The router is at
  `graphql-wundergraph@60c229f3:terraform/main.tf:31`, `base_service` at `:11`,
  `use_fargate = false` at `:13`. Exactly as `:22-34` states.
- **ENUMERATED** — the EU-AI-Act retraction: `skills.go:53-62` counts booleans, `:75` divides;
  `checkerEngines` is declared `criterion.go:34`, assigned `:90`, and **never read** (no
  `c.checkerEngines` anywhere); the live dispatch is `criterion.go:126-127` `case check.EngineLlm:`
  → `validateLLM` (`:175`, defined `:404`) → `NewLLMBulkChecker` (`:428`); the template
  `templates/checkValidationBulk.tmpl:27` asks a model to *"assess whether the `<asset>` … meets or
  does not meet"*; `EngineTextDiff` really is deterministic (`:168` → `validateCodeDiff`,
  `:450-475`, pure string comparison). The v3 validator registers exactly **one** engine
  (`validator.go:60-62`).
- **ENUMERATED** — residency: `jobsim ai.go:85-88` Bedrock `eu-west-1`; `:262-266` Azure-EU;
  `:263-277` the `flag_use_azure_us` swap; `isThrottlingError` `:129` used `:166`/`:325`;
  `coursebuilder/bedrock.go:109-112` + `ModelBackendName()` returning `"anthropic-api"` at `:100`,
  identical at the named pin `2035f9a` and at `ad9f3c49`; all 30 `*_AI_*_MODEL` lines across the
  **three** `app/studio/configs/*.ini` pin `azure`. The four cross-file anchors
  (`external_services.md:554`, `:567`, `:579`, `:602-607`) all resolve to the constructs claimed.
- **ENUMERATED** — Layer 3: `useOrgSelection.tsx:94`, `useResolveActiveOrg.tsx:107`,
  `useActivateMembershipOrg.tsx:81` @ next-web-app `8297c684` are each a
  `clerk.setActive({ organization })`.

**`shared_libraries.md`.**
- **ENUMERATED** — the `go.mod` matrix over the **seven** Go clones at their pinned refs:
  colony **7/7**, proto **7/7**, taxonomy **6/7** (missing only `roadrunner`), ai **2/7**
  (`cms/go.mod:9`, `jobsimulation/go.mod:11`, both `v1.40.2`), authn **0/7**. Every ratio at
  `:62-63` reproduces from an independent enumeration, not from the document's arithmetic.
- **ENUMERATED** — `"github.com/anthropos-work/authn` imported in **0** `.go` files across all
  seven clones (positive control: `colony` present in all seven). `colony/authn` in **6** cms files
  and **8** jobsimulation files — exact.
- **ENUMERATED** — the taxonomy figures at `:278-279`, re-derived from the capture manifest rather
  than from the prose: `.agentspace/snapshots/taxonomy/5afc0bcc…/manifest.json` gives
  `skills 42790`, `job_roles 22470`, `job_role_embeddings 18919`, with
  `source: primary-read`, `public_only: true`, `predicate: org-null`,
  `captured_at 2026-06-29T14:43:00Z`. Including the "18K came from `job_role_embeddings`"
  hypothesis — that table really does hold 18,919.
- **ENUMERATED** — `sdk/storage` imports: **32** files / **36** import lines (the document's exact
  pair). `internal/storage/service.go:48-56` is the `sdkstorage.Client{V1: NewService(...)}` /
  `PublicClient` pair. `storage` repo: `origin/main` `9f8cb53` dated **2026-08-05**, tags to
  **`v0.15.8`**, six past `app`'s `v0.15.2`.
- **ENUMERATED** — the six Connect handler registrations at the named pin `b948604`:
  `main.go:1187`, `:1188`, `:1196`, `:1204`, `:1228` unconditional and `:1212-1214`
  `if cmsRPCServer != nil`. Read with surrounding context.
- **ENUMERATED** — `SkillPathSessionService` occurs in **0** Go source (2 prose hits only:
  `CLAUDE.md`, `knowledge/architecture.md`); `ROADRUNNER_RPC_ADDR` **0** @ `app 9d00a313` and
  **0** in platform `docker-compose.yml` @ `0dab54d`, where `:118` really is `AWS_REGION=eu-west-1`;
  `skillpaths.go:27-31` is the *"drop-in for the removed skillpath RPC client"* comment;
  `jobsimwiring/wiring.go:123` @ `9d00a313` is `jsrunner.NewRunnerManager(...)`. Nested `app/studio`
  grepped separately at `aeec036a` for each zero.
- **ENUMERATED** — `internal/ai/ai.go` is exactly **21** lines and declares only `type AI interface`
  + `type TokenEncoder interface`; the two real vendor-selection sites are
  `jobsimulation/ai/ai.go` `getClient` `:259` (flag `:267`, `:344`) and `skillerai/ai.go` `:332`
  (flag `:347`), with `isThrottlingError` at `:129`/`:128` used at `:166`/`:325` and `:176`. All
  eleven line anchors in that blockquote resolve to the constructs named.
- **ENUMERATED** — `1e457fa70` is *"refactor(ai): fold the ai library into app as internal/ai"*
  (2026-08-04); `module_import_guard_test.go:15-17` carries the quoted rationale;
  `app/CLAUDE.md:289-294` is the *"Don't delete the `ai` repo"* block; rext `stack-seeding/go.mod:8`
  pins `ai v1.40.1` in **both** trees (consumption `09d06070` and authoring `f2ea567b`).
- **ENUMERATED** — the 13 named Connect-RPC services are 13; `storage/internal/migration/migration.go:13`
  really imports `go/simulator/storage/v1` as `legacyStorage`; `app/cmd/createTaxonomy`,
  `internal/aiusage/ai_usage.go`, `internal/askengine/bedrock.go` all exist.
- **ENUMERATED** — sentinel `go.mod:8`/`:9` at `f2c46190` (colony `v0.35.2`, proto `v1.210.0`);
  `:21` taxonomy `v1.2.0 // indirect` at the pinned `88bc5592`; `88036d7` is
  *"chore(deps): update dependencies to latest versions"*. `app/go.mod:20` = taxonomy at the pinned
  `b948604f`, where the block is indeed **seven** with `ai` at `:14` and `messenger` at `:17`.

**`service_taxonomy.md`.**
- **ENUMERATED** — compose @ `0c91421d`: `docker-compose.yml` declares **5** services
  (`sentinel:5`, `backend:28`, `studio-desk:112`, `next-web-app:143`, `gotenberg:170`), `common.yml`
  adds **2** (`postgresql:2`, `redis:24`) → **7** effective; exactly **4** carry `profiles:`
  (`:110`, `:141`, `:168`, `:183`) and `sentinel` carries none → the floor is **3**; the legal token
  set is `{all, backend, core, frontend, studio-desk}` = **5**; `Makefile:10` `PROFILE ?= core`;
  `core` starts **5** containers. Every number in `:71-135` and `:496-513`.
- **ENUMERATED** — the `0dab54d` "eight/ten" split: 8 declared services + 2 = 10; storage
  `8300-8301` (`:112-113`), customerio-sync `8080` (`:144`), messenger `8200-8201` (`:166-167`);
  `:154` `profiles: [customerio-sync, all]`; `:56` `ENVIRONMENT=development`; `:53`
  `DIRECTUS_PUBLIC_BASE_ADDR`. `838d907^` **is** `0dab54df`.
- **ENUMERATED** — `*_RPC_ADDR` count in compose @ `0c91421` = **0**. `d11a403` moved **exactly
  two** values on the messenger block (`CMS_RPC_ADDR`, `JOBSIMULATION_RPC_ADDR`); the diff shows
  only those two `+` lines. `BACKEND_USERS_RPC_ADDR` was introduced at `3e85fce` as
  `http://backend:8081` and its complete value history across all compose commits is exactly two
  strings differing only in port — so *"it only ever moved ports"* is exact, and the retracted
  agentive form was right to retract.
- **ENUMERATED** — the subgraph ladder, re-derived per commit from
  `supergraph-config-prod.yaml`: `749dc86^` = 5 (backend, skiller, jobsimulation, cms, skillpath) →
  `749dc86` = 4 (2026-06-24) → `7c17e63` = 3 (2026-07-21) → `915da06` = **1** (2026-07-29), and
  `915da06 --stat` deletes **both** `schemas/cms.graphqls` and `schemas/jobsimulation.graphqls`. All
  three configs at `60c229f3` carry one `- name: backend`; `schemas/` holds one file.
  `terraform/locals.tf:8` `port = 8080`; `terraform/main.tf:20` `= 1`.
- **ENUMERATED** — studio-room: `gen.py` registers exactly **nine** arguments (`:484-492`) and uses
  `parse_known_args` (`:19`) — no `--template`. `services/ai.py:705-708` is the whole
  three-entry provider registry; `ai.py:1-2` imports only `openai`/`anthropic`; `mistral` in the
  nested repo at its **own** ref `aeec036a` = **22 hits / 3 files**
  (`requirements.txt`, `tools/pdf2md.py`, `tools/r3.py`) and the only `mistralai` *import* is
  `pdf2md.py:24`. The quoted `gen.py` invocation is verbatim `studio/CLAUDE.md:12-14`. The
  `app` image really is `python:3.11-slim` (`Dockerfile:24-28`) and CI pulls the repo via
  `additional_repo: "anthropos-studio-room:studio"` (`build-production.yml:29-30`).
- **ENUMERATED** — the generation edge: `studio-desk/.env.example:44` bakes
  `VITE_GRAPHQL_ENDPOINT=http://localhost:8082/graphql/query` and `:45` is
  `VITE_ENVIRONMENT=production`; compose bakes the same at `:119`/`:135`; `studioTask` =
  `cms_queries.graphqls:106`, `studioTasks` = `:107`, `archiveStudioTask` = **`cms_mutations.graphqls:22`**
  (a mutation — the corrected claim is right); `studioManager.go:119` runs `studio/gen.py` via
  `runCommand`, whose `:1096-1098` contract says *"NEVER through a shell"*.
- **ENUMERATED** — Ant Academy: `code/proxy.js:293-329` @ `22df69dd8` (read via `git show`, never
  the dirty tree) contains sign-in + an env-toggleable org-membership redirect and **no
  email/domain predicate**; `:298` is the org-gate comment. `d5875e34` is *"feat(auth): replace
  @anthropos.work email gate with Clerk org-membership check"*. `graphql/server.js:14,18` throws when
  the endpoint is unset; `serverTenant.js:115-145` carries the *"NO FS-as-published fallback …
  not reversible-on-error"* text and `:145` is the `?? emptyCatalogView()` line;
  `backendContent.js:36,102-103`; `beacon/route.js:36,41-55`. No `serwist`/`workbox` in
  `code/package.json`; `RegisterServiceWorker.jsx` is a kill-switch; `next-scaffold.test.js:106,111`
  and `react-compiler-config.test.js:41` are the regression fences; `academy-manifest.json:7`
  `"display": "standalone"` declared at `layout.jsx:132`. Next `^16.2.6`, React `^19.2.5`, dev port
  **3077**, Expo `~54.0.33`, mobile web `:8555`. `gpt-5.2` + `localStorage('openai_api_key')` at
  `ucourses/ucourse-engine/assistant/agent.js:13,:32`. The rext patch
  `demo-stack/patches/academy-fs-published-fallback` exists in the **pinned per-stack clone**
  `09d06070`.
- **ENUMERATED** — Studio-Desk: **0** react/vue/angular entries in `package.json` and **0**
  `.tsx`/`.jsx` files at `41ee3575`; **7** language flags in `app/public/languages/`;
  `src/services/ai/config.ts:21-31` pins `gpt-5.2`/`gpt-5-mini`/`gpt-5-nano` (so *"GPT-5.x"* holds
  despite compose's stale `DEFAULT_MODEL=gpt-4o`); ports `9000`/`9100`; `depends_on: backend` at
  `:138-140`. Next-web-app `3000`, `depends_on: backend` at `:165-166`.
- **ENUMERATED** — the archived/merged rows: `cms/terraform/main.tf:39` `service_desired_count = 0`;
  `6efa1d5` (merged `f38c0c4`, 2026-08-04) *"the cms ECR repository is decommissioned (M810)"*;
  `roadrunner/terraform/main.tf:19` `= 1`; jobsimulation's M810 delete `6092c6d2`
  (2026-08-04) is **not** an ancestor of the `462343b0` checkout but the cell names it, and at
  `82cb66ec` `terraform/main.tf:15-40` is exactly the decommission comment describing both the
  deletion and the surviving bucket/SSM/atlas ownership — so the pinned claim holds
  (§5 rule 33). `graphql-wundergraph`'s newest commit is 2026-07-30, consistent with the archive date.
- **ENUMERATED** — `repos.yml` @ `0c91421d`: four entries, `app` the only `migrations: true`
  (`:14-17`), `sentinel` `migrations: false` (`:18-20`) while `docker-compose.yml:18` still gives it
  `search_path=sentinel` — the Trap-A case, stated correctly.

**`ai-labs.md`.**
- **ENUMERATED** — `git describe --tags ad9f3c49` → `v1.369.0-7-gad9f3c498` and
  `2035f9a4` → `v1.369.0-2-g2035f9a40`, character-for-character as printed.
- **ENUMERATED** — `checkout.session.completed` = **0** hits in Go source (and 0 in the nested
  studio repo); `internal/labs/` imports `internal/credits` **nowhere**;
  `internal/web/backend/credits/handler.go:12` says the mock `POST /credits/purchase` was removed in
  Wave 13.
- **ENUMERATED** — the cost model read from source, not from the doc: `creditCost` @ `cost.go:86-90`
  is `{build:5, refine:1, translate:1}`, `purchasePackages` @ `:200-204` is
  `{starter:50, team:200, scale:500}`, `MarginMarkup = 1.40` `:133`, `PricePerCreditUSD = 0.45`
  `:142`, `DefaultSeedBalance int64 = 500` `:231`, and `TestPricingDerivation` exists at
  `cost_test.go:103`. **Worth recording:** the package's own doc comment at `cost.go:29` says
  *"course.refine → 5 credits per refine turn"* and contradicts its own live map — the corpus
  followed the **map** and is right; a reader who checked only the comment would wrongly "correct"
  the corpus.
- **ENUMERATED** — `s3_workspace_store.go:19` and `:45` carry the `<prefix>/<slug>/workspace.tar.gz`
  key, and *grader*/*solution* occur **0** times in that file, while `schema/lab.go:113-116` is the
  `{id,url}` asset-ref block including *"solution is PRIVATE — never served to learners"*. The
  corrected attribution is exact.
- **ENUMERATED** — `LabSessionService` methods Create/Get/List/Cancel/ReportEvent at
  `labsession.go:90/252/278/333/386`; `labsapi/client.go:6` default `:7070`;
  `lab_body.go:49` locales `en` + `de/es/fr/it/nl/pt` = **6** overlays; the Stripe webhook switch
  at `api/api.go:319-327` handles `customer.created` + `customer.subscription.{created,updated,deleted}`
  and nothing else; all five named migration files exist; `cmd/{labsdemo,labsimport,labskey}` exist
  and `labsdemo` binds `:8099` (`main.go:115`); `labs.graphqls`, `billing.graphqls`,
  `organizations.graphqls` all present.

**`customerio-sync.md`.**
- **ENUMERATED** — `env_guards.go:62` `envCustomerIOSyncEnabled`; `main.go:286`
  `mustSubsystemSwitch(envCustomerIOSyncEnabled)`; `:284` is the **comment** as the doc says;
  `:295-300` with the condition at `:295` and `log.Fatalf` at `:296`; `:395`
  `customeriosync.New(logger, copilotDB, os.Getenv("BREVO_KEY"))` inside `:393-396`;
  `env_guards.go:98-104` `case "":` → error when deployed, via `mustSubsystemSwitch`'s
  `log.Fatalf` at `:87`; `deployedEnvironment()` `:37-44`. `git ls-tree b948604f -- env_guards.go`
  is **empty** — the file genuinely did not exist at the old pin, exactly as the ref-pinning note
  claims.
- **ENUMERATED, and this is the strongest clearance in the seat** — the *"LAST of its kind, never
  the only one"* correction at `:85`. I re-derived the set independently over the whole platform
  history (every `context: git@github.com:…` line in every commit that touched
  `docker-compose.yml`, de-duplicated): **18** distinct repos, and the same 18 names —
  app, chronos, cms, customerio-sync, graphql, graphql-wundergraph, graphqltmp, intelligence,
  jobsimulation, messenger, realtime, roadrunner, sentinel, simulator, skiller, skillpath, storage,
  studio-desk. `a2a3ee6` (2026-02-27) is *"switch to local Dockerfile.dev builds"*, `c17cc9a` is
  2026-04-15, and at `838d907^` `customerio-sync` is the **only** git-URL context among the seven
  services that carry a `build:` block. Every element of a five-clause correction re-derived.
- **ENUMERATED** — the historical compose block reproduces `0dab54d:docker-compose.yml:136-154`
  faithfully (context, ssh, args, `8080:8080`, `DB_CONNECTION_BACKEND`, `depends_on`).
  `internal/customeriosync/` holds `doc.go`, `store.go`, `sync.go`, `sync_query.sql`;
  `doc.go:4-5` is the *"last of the Go services to be folded into app"* sentence wrapping the line
  break; `doc.go:30` carries `7,392 of 7,397`; `cmd/customerio-resync/main.go:33` declares
  `--dry-run`. `brevo` = **0** occurrences in `external_services.md`, whose per-service `##`
  sections are exactly Clerk, Directus, the Cosmo router, AI Providers, LiveKit, AWS Chime.
- **ENUMERATED** — `3e5bc33ef:main.go:387` is `if deployedEnvironment() && os.Getenv("BREVO_KEY") != ""`,
  which is what `service_taxonomy.md:115-116` quotes.

**`intelligence.md`.**
- **ENUMERATED** — `fdfa189` is *"chore: remove intelligence service from local dev orchestration"*
  (2026-04-17, so *"mid-2026"* holds); it is the commit that removes `intelligence` from **both**
  `repos.yml` and `docker-compose.yml`, and the same commit removes `DB_CONNECTION_SKILLER` from
  compose. `045857c` (chronos) is real and same-dated. Nothing in this 18-line file is false against
  what the platform clone can show.

---

## What I could not settle

- **`db-backup`** — every claim about it (`security_compliance.md:13`, `:315`, `:322`;
  `service_taxonomy.md:173`, `:528`): Bash not Go, S3 + Hetzner and never Azure, the EventBridge
  trigger commented out at `7dd1b80` (2025-05-29), the `rate(12 hours)` disabled value. **There is
  no `db-backup` clone in the ground-truth set.** Settled by cloning `anthropos-work/db-backup` and
  reading its terraform at the ref production pins.
- **`infrastructure`** — `service_taxonomy.md:3`, `:169`, `:530` and `security_compliance.md:322`
  cite `infrastructure/terraform/production/services.tf:509-517`, `:64-70`, `:571` and
  `modules/core/storage/rds.tf:6,19,78-89` @ `13c248e6`. That repo has never been in a clone set —
  the corpus says so itself — so the *whole* production-destruction story for the router and for
  `cms` is unverifiable from here. I note only that it is now **load-bearing**: both the
  banner's headline retraction and the cms row's *"it HAS been applied"* rest entirely on it.
- **`colony`, `proto`, `authn`, `taxonomy` internals** — `shared_libraries.md:100`, `:104-119`,
  `:128`, `:156-159`, `:219`, `:229-235`, `:255`, `:264`, `:326-340`. These are private Go modules
  in no clone set (the doc says as much at `:137-140`). I verified every claim about *who imports
  them at what version* from the seven `go.mod`s; I could verify nothing about what is *inside*
  them (the `rate.NewLimiter(1,5)` reject path, the buf layout, the 131-line `node.go`, the ID
  format). Settled by adding the four repos to a clone set.
- **GitHub archive state** — every `ARCHIVED <date>` in `service_taxonomy.md:162-170` and the
  wundergraph banner. `gh` is not installed on this host and the repos are private, so the org API
  is unreachable; the corpus's own note (`:148-160`) says this correctly and marks them as dated
  snapshots. I confirmed only the falsifiable half: `jobsimulation`'s `origin/main` really does
  carry post-dated commits, and `graphql-wundergraph`'s does not.
- **`AI-Labs` repo** (`ai-labs.md:5-14`) — 96 scenario templates vs the README's "15", ~8 kLOC,
  Ansible/systemd/Cloudflare Tunnel/Caddy, `STATUS.md:3-6`/`:11`. No clone.
- **`anthropos-knowledge-base`** (`shared_libraries.md:291-324`) — the "14 places", the four
  customer-facing comparison tables, the plugin packaging. No clone. The passage also routes to
  `knowledge/plan/platform-defect-register.md`, which **hard bar #1 forbids me to open**, so I
  cannot confirm the filing exists either.
- **`intelligence.md:18`** — the `:8080` HTTP server, the `/_meta`-only surface and the 5-minute
  ticker. No `intelligence` clone; the compose history confirms only the env vars and the removal.
- **External/legal facts** — DPA v1.4, 18 approved sub-processors, TLS/KMS/CloudTrail posture,
  90-day deletion, the sub-processor list (`security_compliance.md:7`, `:47-68`, `:418-432`), and
  the Limited-Risk classification itself. Not in any repo. The corpus is right to say the legal
  question is counsel's; I verified only that its *technical* premise is refuted in code, which it is.
