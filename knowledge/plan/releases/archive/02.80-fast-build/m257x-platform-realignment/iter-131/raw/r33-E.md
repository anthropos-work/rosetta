# Seat E — reading #33/#34 (M257x iter-131)

## Refs actually read (stated per §5 rule 26)

| tree | ref used | note |
|---|---|---|
| `stack-demo/app` | `ad9f3c498` (checkout = origin/main) | plus pinned reads at `b948604f`, `9d00a313` where a claim named them |
| `stack-demo/platform` | `0c91421` | plus `0dab54d` where a claim named it |
| `stack-demo/ant-academy` | `22df69dd8` — **read via `git show`, never the dirty tree** | |
| `stack-demo/studio-desk` | `41ee3575` | |
| `stack-demo/graphql-wundergraph` | `60c229f3` | |
| `stack-demo/messenger` | `fa47850d` (checkout) **and** `e9421c68` (origin/main) — stated per claim | |
| `stack-demo/rosetta-extensions` | `09d06070` (pinned per-stack CONSUMPTION clone) | used for every "what the tooling does on a stack" claim |
| `.agentspace/rosetta-extensions` | `f2ea567b` (authoring `main`) | cross-checked cockpit.py anchors; identical |

`stack-demo/` holds 13 platform clones + `rosetta-extensions` + 2 lock files. **`infrastructure` is NOT among them** — see *What I could not settle*.

## Positive controls

| file | `wc -l` | read in full? |
|---|---|---|
| `corpus/services/ant-academy.md` | 529 | yes |
| `corpus/services/studio-desk.md` | 449 | yes |
| `corpus/services/graphql-wundergraph.md` | 288 | yes |
| `corpus/services/messenger.md` | 219 | yes |
| `corpus/services/academy-backend.md` | 167 | yes |
| `corpus/services/TEMPLATE.md` | 46 | yes |

Search positive controls run in-pass: `git grep -n "STORAGE_RPC_ADDR" ad9f3c49 -- '*.go'` → 3 hits (non-empty, so the pipeline works); `git -C stack-demo/platform log -S 'SKILLER_RPC' --oneline 0c91421d | wc -l` → 7 (the corpus's own declared control, reproduced); `git grep -n "liquid" e9421c68 -- '*.go'` → 8 hits across 3 non-test files (so the zero in `brevo.go` is a measured zero, not a broken search). All stderr read.

---

## Blockers

### B1 — `messenger.md:121` re-publishes the exact Liquid claim the same file retracts 100 lines above, and it is false

- **Anchor:** `corpus/services/messenger.md:121` — *"Messages carry user info, template ID, and template params; **the body is rendered through Liquid against those params before the Brevo send.**"*
- **Why it is false:** The file's own iter-129 retraction at `:15-21` says the opposite in its own voice: *"'Liquid templating for the bodies': the Brevo sender posts a `TemplateId` + `Params` (`brevo.go:288-310`) and **Brevo** renders; Liquid is used only by the console sender (`console/console.go:71`) and for inline-composed whitelabel / CMS bodies."* Measured at `messenger` `e9421c68` (origin/main; identical at the checkout `fa47850d`): `internal/messenger/brevo/brevo.go` contains **zero** occurrences of `liquid` (`git show e9421c68:internal/messenger/brevo/brevo.go | grep -c -i liquid` → `0`), and `brevo.go:288-310` is `func (s *brevoSender) send(...)` building a `brevo.SendSmtpEmail{…, TemplateId: templateId, Params: props}` posted to `TransactionalEmailsApi.SendTransacEmail` at `:310`. The complete non-test Liquid surface in the repo is exactly three files — `internal/flow/assignments.go:18,:489`, `internal/flow/whitelabel.go:8,:17`, `internal/messenger/console/console.go:16,:26,:33,:71`. `internal/messenger/message/` (the package the sentence is about — it sits directly under *Connect-RPC (`MessengerService`)* and describes the `Send` path) contains no `liquid`, `render` or `template` reference in any of `errors.go`, `message.go`, `validator.go`. The RPC `Send` path therefore posts a template id + params to Brevo and renders nothing locally.
- **Confidence:** high

### B2 — `messenger.md:96` — the key-directories block attributes Liquid rendering to `message/`, which does no rendering

- **Anchor:** `corpus/services/messenger.md:96` — ``    message/                 Message types + Liquid rendering``
- **Why it is false:** Same measurement as B1. `internal/messenger/message/` at `e9421c68` holds `errors.go`, `message.go`, `message_test.go`, `validator.go`; grepping each for `liquid|render|template` returns **nothing**. The Liquid engine is constructed in `internal/messenger/console/console.go:26` (`liquid: liquid.NewEngine()`) and in `internal/flow/{whitelabel.go:17, assignments.go:489}` — never in `message/`. This is the second surviving site of the claim the file retracts at `:15-21` (`§5` rule 54 / rule 19: a correction that reaches one cell is not a correction). Booked separately from B1 because it is a distinct assertion about a distinct package with its own evidence.
- **Confidence:** high

### B3 — `academy-backend.md:66` cites a file path that does not exist anywhere in `app`

- **Anchor:** `corpus/services/academy-backend.md:65-67` — *"`STORAGE_RPC_ADDR` is gone from the Go source (3 remaining occurrences at that ref — `main.go:504`, **`env/callsites_test.go:189`**, `internal/jobsimwiring/wiring.go:101` — all comments, zero reads)."*
- **Why it is false:** Measured at `app` `ad9f3c49` (the ref the sentence pins, `:62`): `git grep -n "STORAGE_RPC_ADDR" ad9f3c49 -- '*.go'` returns exactly three lines, and the middle one is **`internal/storagens/callsites_test.go:189`**, not `env/callsites_test.go:189`. There is no `env/` directory in the repo at all (`git ls-files | grep -E '^env/'` → empty), and `git ls-files | grep callsites_test.go` returns the single path `internal/storagens/callsites_test.go`. The other two anchors (`main.go:504`, `internal/jobsimwiring/wiring.go:101`) resolve exactly, and the "all comments, zero reads" predicate holds for all three — so the count and the predicate are right and only the path is wrong, which is precisely the shape §5 rule 17 warns about. A reader following the citation finds nothing.
- **Confidence:** high

### B4 — `academy-backend.md:15` — "(Consistent with :85-89 below.)" points at the certificate-minting bullet, not at the endpoint claim

- **Anchor:** `corpus/services/academy-backend.md:12-15` — *"…serves them to the frontend through the **`app` GraphQL subgraph**, reached at `NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT` — which on a local stack is **`backend`'s own `:8082/graphql/query`** … **(Consistent with :85-89 below.)**"*
- **Why it is false:** `academy-backend.md:85-89` reads: `:85` ``        `source_hash` skip-key).`` (the tail of the `academy_path_embeddings` Ent bullet), `:86-88` the **Certificate minting** bullet (`AAS-YYYY-XXXXXX`, 30-symbol alphabet, `crypto/rand`), `:89` blank. Nothing in that window mentions the endpoint, the router, or the subgraph. The passage the sentence is claiming consistency with is `:93-96` (*"**Primary — GraphQL, on the `app` (backend) subgraph.** … **There is no separate 'academy subgraph'** … The frontend reaches them at (`NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT`)"*). The anchor resolves and reads plausibly — which is exactly why it survives — but names the wrong construct.
- **Confidence:** medium — I hesitated because the underlying *claim* is true and only the self-citation is misdirected; booked as a BLOCKER per the instrument's "wrong line anchors that point at the wrong construct" class and its doubt-resolves-upward rule.

### B5 — `academy-backend.md:136` cites `ant-academy.md:82-88` for a statement that lives at `ant-academy.md:96-99`

- **Anchor:** `corpus/services/academy-backend.md:133-136` — *"**It does NOT 'serve its committed FS catalog': there is no FS-as-published fallback in the app** — this passage said there was until M257x iter-108, contradicting [`ant-academy.md`](./ant-academy.md)**`:82-88`**, which states the opposite in bold."*
- **Why it is false:** `ant-academy.md:82-88` is inside the **write-path** blockquote — `saveChapterProgress` (`:150`) / `UPSERT_CHAPTER_PROGRESS` (`:162`) / `saveLastActivity` (`:202`), the `beacon:` option at `store.js:169`/`:215`, `src/writeThrough/index.js:247`/`:259`. It says nothing about the FS catalog fallback. The bolded statement being cited is at `ant-academy.md:96-99`: *"**⚠️ It does *not* 'fall back to the committed FS catalog'** — there is **no FS-as-published fallback** in the app; `getServerCatalogView()` resolves a null backend result to the **empty view**…"*. A cross-file citation off by 14 lines onto an unrelated blockquote, in a sentence whose whole purpose is to route the reader to the authoritative twin.
- **Confidence:** high

### B6 — `graphql-wundergraph.md:136` names `:116-117` as the struck-through bullet; that bullet is `:118-119`

- **Anchor:** `corpus/services/graphql-wundergraph.md:136` — *"…as the *Build-time, static composition* bullets at `:114-117` of this doc describe (specifically **`:116-117`, the struck-through *"`make up` rebuilds `graphql`"* bullet**: *"It **used to** rebuild whenever any subgraph schema changed, because the build context is the parent dir (`..`) holding all sibling repos"*). **Not `:84`**, which is the *Ports* bullet … — corrected M257x iter-102."*
- **Why it is false:** In this file, `:116-117` is a **different** bullet — *"Adding/changing a subgraph **or a single field** requires re-running `wgc compose` / and **rebuilding + restarting** the image — there is **no hot reload**."* The struck-through `~~`make up` rebuilds `graphql`~~` bullet, including the verbatim text the sentence quotes (*"It *used to* rebuild whenever any subgraph schema changed, because the build / context is the parent dir (`..`) holding all sibling repos"*), is at **`:118-119`**. The wider range `:114-117` is also wrong: `:114` is the closing prose line *"**not** live-introspect running subgraphs. Consequences:"* and the bullet list runs `:116-119`. The sentence explicitly identifies the anchor with a named construct and quotes that construct's text, so this is not ambiguity — it is a mis-anchor introduced by the very repair (iter-102) that the sentence advertises.
- **Confidence:** high

### B7 — `graphql-wundergraph.md:88` cites `:193` for a sentence that is at `:196`

- **Anchor:** `corpus/services/graphql-wundergraph.md:87-90` — *"The host-`5050` mapping was published here in the present tense while **`:193`** of this same doc already said `localhost:5050` refuses the connection (this cited `:174-176` until M257x iter-98 …); corrected M257x iter-46"*
- **Why it is false:** `grep -n "refuses the connection" corpus/services/graphql-wundergraph.md` returns `:89` (this sentence itself) and **`:196`**. Line `:193` is *"**⚠️ At `0dab54d` the `graphql` token appears in no `profiles:` key at all**, so asking for it does not"* — the retired-profile-token claim, a different construct. This is the second re-pointing of the same citation (it was `:174-176`, then `:193`) and it is still off, by three lines.
- **Confidence:** medium — I hesitated because `:193` sits inside the same seven-line paragraph as the target, so a reader would probably still find the sentence; booked because the doc cites a specific line and attributes specific content to it, and because §5 rule 33 fixes a pin's scope at *the claim's own block*, which a wrapped sentence is.

### B8 — `ant-academy.md:63` names `code/tools/` as the "offline-parity CLI", which was deleted at v0.5 M1 and is regression-fenced as absent

- **Anchor:** `corpus/services/ant-academy.md:63` — ``│   ├── tools/             # offline-parity CLI``
- **Why it is false:** At `ant-academy` `22df69dd8`, `git ls-tree -r --name-only 22df69dd8 -- code/tools/` returns exactly one file: `code/tools/apply-v3-metadata.mjs`, whose own header says it *"Adds sidebarCategory, sidebarSubcategory, and audiences fields to every skill path in catalog.js"* — a metadata migration, not an offline-parity CLI. The offline-parity CLIs (`tools/offline-parity/{lib,capture-baseline,check-parity}.mjs`) were removed at v0.5 M1 and their **absence is asserted by a live repo test**: `code/tests/unit/next-scaffold.test.js:111` `it('v0.5 M1: offline-parity scripts + serwist/esbuild deps removed', …)` with those three paths enumerated at `:161-163`. This also self-contradicts the same corpus file at `:48` (*"~~Cache chapters offline via a Serwist-built service worker~~ — **REMOVED at v0.5 M1**"*) and `:316` (*"The Serwist 9 service worker was removed at v0.5 M1 and is regression-fenced against"*).
- **Confidence:** medium — I hesitated because it is a one-line annotation in an ASCII layout diagram rather than a load-bearing sentence; booked because it names an artifact that provably does not exist, contradicts the file's own text twice, and would send a reader looking for a removed offline subsystem.

---

## Minors

- `corpus/services/ant-academy.md:191` — *"a separate, unauthenticated `code/public/catalog.json` (~2,667 entries)"*. Measured at `22df69dd8`: the file's `courses` array holds **2,692** entries (`git show 22df69dd8:code/public/catalog.json | python3 -c "…len(d['courses'])"`). ~1 % off, tilde-qualified, and the sentence's point ("thousands of entries") survives — cosmetic staleness only. (Note: the working tree's copy is one of the three dirty files; this is the committed one.)
- `corpus/services/ant-academy.md:483` — the skills table's catch-all row enumerates seven helpers and omits **`export-path`** (and the `_shared` helper dir), both present in `.claude/skills/` at `22df69dd8`. The row is explicitly a non-exhaustive "Other content-pipeline helpers" row, so this is an omitted list member, not a false claim.
- `corpus/services/graphql-wundergraph.md:136` — the wider self-citation `:114-117` for "the *Build-time, static composition* bullets" is also off (the bullets are `:116-119`); folded into B6 rather than booked twice.

---

## Positively cleared (audited zeros)

Every item below was checked against ground truth at the ref stated. Labels are per the briefing: **ENUMERATED** = I derived the predicate's set from source independently and compared cardinalities; **RE-DERIVED** = I re-computed arithmetic the document itself displays (the weaker clearance).

**`ant-academy.md`**
- **ENUMERATED** — the complete `isPublic` matcher table (`:329-339`). I read `code/proxy.js`'s `createRouteMatcher([...])` in full at `22df69dd8` and enumerated all 24 static patterns + both conditional blocks; the doc's nine groups cover **every** entry with none invented and none missing, including the `DEV_LOGIN_ENABLED` and `BENCHMARK_VISUAL_BYPASS` spreads and the `/api/_meta`+`/api/meta` double-spelling with its `next.config.js` rewrite rationale (`next.config.js:137,:141`).
- **ENUMERATED** — the `TopBar` surface set is **seven** routes (`:287-293`). `git grep -n "<TopBar"` at `22df69dd8` returns 5 non-test mount sites (`AcademyClient.jsx:1906`, `BookmarksClient.jsx:508`, `CourseClient.jsx:2091` and `:2141`, `LatestClient.jsx:128`, `MyActivityClient.jsx:161`); `AcademyClient` is mounted by `app/(authed)/page.jsx:151`, `courses/page.jsx:92`, `courses/[slug]/page.jsx:219` and `CourseClient` by `chapters/[slug]/page.jsx:226` → `/`, `/courses`, `/courses/[slug]`, `/chapters/[slug]`, `/latest`, `/bookmarks`, `/my-activity` = 7. `MyCertificatesClient.jsx` contains 0 `TopBar` references, as claimed.
- **ENUMERATED** — `emptyCatalogView()` is **five** keys (`:170-180`). `serverTenant.js:115-117` returns `{ chapters: [], skillPaths: {}, series: [], bundles: PUBLIC_BUNDLES, catalogVersion: CATALOG_VERSION }`; `PUBLIC_BUNDLES` is a populated exported array at `ucourses/catalog.js:961` and `CATALOG_VERSION = '1.0'` at `:31`; the in-code justification is verbatim at `:111-113`. The `→ 0 cards` consequence at `AcademyClient.jsx:1363-1365` (`scopedChapters.filter(...)`, `if (chapters.length === 0) return []`) is exactly as described.
- **ENUMERATED** — `getServerCatalogView()`'s byte-exact quote resolves at `serverTenant.js:145` (function at `:143`), and the *"cutover is intentional, not reversible-on-error"* sentence is at `:132`, inside the cited `:115-145` window.
- **ENUMERATED** — the mobile bundler is inoperative (`:20-23`, `:451-459`). `bundle-content.ts:27-28` computes `REPO_ROOT = join(SCRIPT_DIR,'..','..')` then `CONTENT_DIR = join(REPO_ROOT,'content')`; `git ls-tree -r --name-only 22df69dd8 -- content/` → **0**, `-- code/public/content/` → **3,406**; `findJsonFiles` does a bare `readdirSync(dir)` with no existence check.
- **ENUMERATED** — the fonts row (`:315`): `code/app/layout.jsx:1` imports exactly `{ Work_Sans, Instrument_Serif }`, declared at `:41` and `:51`; `code/academy.css:1` says *"Display + mono usages fall back to system fonts."*; Font Awesome is vendored (99 files under `code/public/assets/fontawesome/`, incl. `css/all.min.css`).
- **ENUMERATED** — the test counts (`:318`). `git ls-tree -r --name-only 22df69dd8 | grep -cE '^code/tests/e2e/[^/]*\.spec\.js$'` → **31** (the doc's own stated invocation, reproduced); `.test.jsx?` files under `code/tests/` → **245**; `it(`/`test(` blocks across those 245 files → **2,714** ("~2,700"). `playwright.config.js:13` is `testDir: './tests/e2e'`.
- **ENUMERATED** — the write path (`:79-91`): `store.js:150/:162/:202/:210` (mutations fired from the client harness), `:169/:215` (the `beacon:` option), `writeThrough/index.js:247` (`navigator.sendBeacon`) and `:259` (`keepalive: true`), and the beacon route's header comment at `route.js:1-18` including the quoted *"best-effort last-ditch flush…"* at `:13-14`.
- **ENUMERATED** — the storefront refutation (`:500`): `platformUrls.js:1-32` carries *"a storefront in front of the Anthropos platform"*, FLOW A (*"the PRIMARY CTA for this flow — a new visitor registers"*) and FLOW B (*"…subscribes for an anonymous visitor"*); `pricing.js:21` `STANDARD_YEARLY = { usd: 399, eur: 349 }`; `TopBar.jsx:77-88` renders the `{anonymous ? …}` "Buy AI Academy" CTA; `schema.js:3` `SITE_URL = 'https://aiacademy.anthropos.work'`; `knowledge/user-types.md` enumerates exactly four types (Anonymous / Signed-in-free / Subscriber / Enterprise-Org member) with no `@anthropos.work` predicate; the repo's `CLAUDE.md:11` does say *"internal learning portal for Anthropic employees"* — verbatim, including "Anthropic".
- **ENUMERATED** — the locale switchers (`:274-300`): `LocaleSwitch` is mounted once, at `PublicHeader.jsx:20`, and `PublicHeader` itself once, at `app/(public)/library/page.jsx:28`; `app/(public)/free/page.jsx:18` is `redirect('/?tier=free')`; `LanguageSelector.jsx:88` is `role="menu"` and `:97` `role="menuitemradio"`; `locale.js:10` is the 7-entry `SUPPORTED_LOCALES`; `TopBar.jsx:76` mounts `<LanguageSelector />`.
- **ENUMERATED** — the org gate (`:364`): `proxy.js:2-5` header, `:73`/`:91` the `REQUIRE_ORGANIZATION_MEMBERSHIP` toggle and its read, `:298` *"Organization-membership gate. Any membership (active or not) passes."*; `code/.env.example:66` points sign-up at `app.anthropos.work`.
- **ENUMERATED** — the five demo patches (`:240-244`) at the **pinned** rext clone `09d06070`: `demo-stack/ant-academy.sh` defines them at `:56` (dev-origins), `:66` (fs-published), `:75` (fs-published-body), `:84` (fs-published-public), `:90` (back-to-cockpit), and all five manifests exist under `demo-stack/patches/`. `:576-583` is the M220 comment block that removes `BENCHMARK_VISUAL_BYPASS`/`NEXT_PUBLIC_E2E_AUTH` from the launch env. `cockpit.py:812`, `:1496`, `:327` all carry `e2e_persona` — **identical in both trees** (pinned `09d06070` and authoring `f2ea567b`), so the tree choice does not change this answer. The autoverify academy assert *(f)* exists at `stack-verify/live/autoverify.sh:459+`.
- **ENUMERATED** — Cosmo (`:501`): `ucourses/ucourse-engine/assistant/agent.js:12` `API_URL='https://api.openai.com/v1/responses'`, `:13` `MODEL='gpt-5.2'`, `:32` `localStorage.getItem('openai_api_key')`; flag at `featureFlags.js:8`. Embed coupling (`:503`): `proxy.js:211` `EMBED_COOKIE="embed-mode"`, `:239` `x-embed-mode`, `:241` `x-locale`; `layout.jsx:168/:170` `data-theme`/`data-embed`.
- **ENUMERATED** — stack facts: Next `^16.2.6`, React `^19.2.5`, `reactCompiler: true` (`next.config.js:39`), Expo `~54.0.33`, `engines.node ">=22"`, `vercel.json` = `{"framework":"nextjs"}`, `academy-manifest.json` `display: standalone` wired at `layout.jsx:132`, `RegisterServiceWorker.jsx` is a documented kill-switch, `deploy-academy.yaml` / `sync-vercel-env.yml` / `sidebar-coverage-tests.yaml` / `cog.toml` all present, `tests/e2e/i18n-language-toggle.spec.js` present.
- **ENUMERATED** — the backend side of the academy claims at `app` `ad9f3c49`: `academy.graphqls` is **1 of 43** `*.graphqls`; `internal/academy/` exists; `cmd/academy-seed` exists; `academyCatalogSeries` (`:664`) + `academyCatalogSkillPaths` (`:672`) + `academyCertificate(certId) @public` (`:654`) + `upsertChapterProgress` (`:694`) / `setLastActivity` (`:700`) all present; Ent schema files `academy_chapter_progress.go` / `academy_last_activity.go` present (tables plural, as the doc insists).
- **ENUMERATED** — ant-academy is **not** in `repos.yml` (`:40`, `:369`, `:517-519`): `repos.yml` @ `0c91421` lists exactly `app`, `sentinel`, `next-web-app`, `studio-desk`.

**`studio-desk.md`**
- **ENUMERATED** — the "backend is NOT a GraphQL client" correction (`:38-51`) reproduces exactly at `41ee3575`: `git grep -in graphql -- 'src/*'` → **2** lines (`src/routes/skillpath.ts:374`, `:405`), both comments saying the opposite; `git grep -n 8082 -- 'src/*'` → **0** (exit 1, stderr read); `src/index.ts` mounts four API routers at `:150`, `:158`, `:161`, `:164`; all four `new GraphQLClient(...)` sites are in `app/` at `userService.ts:20`, `taxonomyService.ts:43`, `userPreferencesService.js:13`, `content/simulationContentService.js:325`, fed by `app/services/config.ts:6`.
- **ENUMERATED** — compose anchors at platform `0c91421`: build arg `:119`, runtime env `:135`, `depends_on: backend` `:138-140`, `profiles: [studio-desk, all]` `:141`. The `frontend`-alone and `studio-desk`-alone exit-1 reasoning holds (`next-web-app` `depends_on` at `:165-167`).
- **ENUMERATED** — `vite.config.ts` `rollupOptions.input` holds exactly the nine prod entries the doc lists, plus the `isProduction ? {} : {'dev-accept': …}` spread.
- **ENUMERATED** — `.env.example` is **131** lines and `:117/:118/:119/:120` are `YOUTUBE_API_KEY=` / the comment / `GCLOUD_SERVICE_ACCOUNT=` / blank, exactly as the iter-115 correction states; `terraform/main.tf:129` is the `GCLOUD_SERVICE_ACCOUNT` SSM entry; `git grep GCLOUD_SERVICE_ACCOUNT -- src/` → 0 reads.
- **ENUMERATED** — `STUDIO_ACCESS_ROLES = ['admin','org:admin','content_creator','org:content_creator']` at `src/index.ts:96`, used at `:115`. `src/routes/youtube.ts:43` reads `YOUTUBE_API_KEY` with the `_mock: true` fallback at `:45`.
- **RE-DERIVED / ENUMERATED** — AI tiers: `src/services/ai/config.ts:21-24` and `:28-31` give `gpt-5.2 / gpt-5.2 / gpt-5-mini / gpt-5-nano` for OpenAI and Azure, `:35-38` `claude-opus-4-5-* / claude-sonnet-4-5-* / claude-haiku-4-5-*`; `.env.example:56` `AI_PROVIDER_CHAIN=azure-openai,openai`, `:60` `AI_DEFAULT_TIER=fast`, and the in-code fallback is `'thinking_fast'` at `config.ts:182` — every clause of the doc's parenthetical checks.
- **ENUMERATED** — Node `>=24` in `package.json` engines and `node:24-alpine` in **both** Dockerfiles; `process.env.PORT || 9100` at `src/index.ts:60` (the in-code fallback the doc calls out); 7 l12n bundles (`de,en,es,fr,it,ja,nl`); the repo's only Tailscale mention is `app/core/main.ts:105` (GlitchTip); `main.ts:97` `preloadCriticalCSS();` and `:206` `new PageWrapper();`; `app/core/scaffold/{pageWrapper.js,userProfile.js}` both exist; `src/routes/skillpath.ts` is 61,080 bytes ("~61KB"); Directus reads at `skillpath.ts:44-47` and `index.ts:303-310` with `pingSnapshotCapability` at `:305`.

**`graphql-wundergraph.md`**
- **ENUMERATED** — the supergraph is **one** subgraph: `supergraph-config-prod.yaml` @ `60c229f3` lists `backend` alone with `federation_version: =2.3.2`; `schemas/` holds `backend.graphqls` alone; `subgraphs.conf` is the single line `BACKEND=v1.360.0`. `schemas/backend.graphqls` declares `type Mutation` (`:4053`) and `type Query` (`:4912`) and **no** `type Subscription`; all six `Subscription` substring hits are Stripe/plan names (`activeSubscription`, `type PlanSubscription`, `stripeSubscriptionId` ×2 + one doc line + `stripeSubscriptionItemId`) — the doc's own enumeration, reproduced.
- **ENUMERATED** — the **five** surviving profile tokens. `docker-compose.yml` @ `0c91421` has exactly four `profiles:` keys (`:110` `[core, backend, all]`, `:141` `[studio-desk, all]`, `:168` `[frontend, all]`, `:183` `[core, backend, all]`) → token set `{core, backend, all, studio-desk, frontend}`; `common.yml` declares `postgresql`/`redis` with **no** `profiles:` key (the always-on floor); `Makefile:10` is `PROFILE ?= core`; `grep -c 5050 docker-compose.yml` → **0**.
- **ENUMERATED** — `ci/update-subgraph.sh` carries **exactly one** `gh release download`, on line `:9`, with `-R anthropos-work/app`. `package.json` is `{"name":"graphql-wundegraph"}` (misspelled) and `CLAUDE.md`'s heading carries the same misspelling. `CLAUDE.md:85` is `wgc router compose -i supergraph-config-local.yaml` while `ls supergraph-config-*.yaml` returns only `-compose`, `-dev`, `-prod` — the iter-115 correction's surviving half is exact, and the Version-Tracking section it *un*-retracts is a byte match against `subgraphs.conf`.
- **ENUMERATED** — pinned versions: `ghcr.io/wundergraph/cosmo/router:0.275.0`, `wgc@0.104.0` on `node:22.11-alpine` (both Dockerfiles); `config.compose.yaml` `listen_addr: 0.0.0.0:8080`, `graphql_path: /graphql`, `playground_enabled`/`introspection_enabled` true, `max_request_body_size: 35MB`, plus the CORS block. `terraform/locals.tf` gives `project = "wundergraph"`, `aws_region = "eu-west-1"`, `port = 8080`, and `main.tf:26` `health_check_path = "/health"`; `main.tf:20` is `service_desired_count = 1` (the orphaned line the doc calls dead). `terraform/tests/defaults/test_main.tf` is the only test artifact in the repo.
- **ENUMERATED** — `bba862f` is reachable from `remotes/origin/feat/use-web-socket` **only** (`git branch -a --contains`), subject *"change subscription protocol from sse_post to ws in configuration files"*.
- **ENUMERATED** — the `backend` smoke-test section at `app` `ad9f3c49`: `internal/authorization/gqlauthz/gqlauthz.go:186` is the `unknown viewer: Forbidden` return, with the three exemptions (`isPublicOnlyOperation` / `isFederationQuery` / `colony.Development && isIntrospectionOnlyQuery`) at `:175-176`; the two named regression tests exist (`gqlauthz_test.go:132` `"bare __typename is not exempt"`, `:176` `TestAnonymousRejectionLogsAtWarn`). `internal/web/backend/backend.go:312-317` is exactly the dev-only Apollo Sandbox gate, with the quoted comment verbatim at `:312-313`, `if environment == colony.Development` at `:314`, `/graphql` at `:315`, and the unconditional `/graphql/query` at `:317`.
- **ENUMERATED** — the moving compose anchors at `:177-178`: `:135` (studio VITE_GRAPHQL_ENDPOINT runtime), `:160` (next-web NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT runtime), `:119` and `:151` (the two build args) — all four resolve at `0c91421`.
- **ENUMERATED** — `:182`'s cross-references are correct: `:45` is the 5-subgraph ladder row and `:162-166` the block-quote, and `§5` rule **54** does exist in `platform-alignment.md` (`:1767`) and is the right class.

**`messenger.md`**
- **ENUMERATED** — `flow.go:72-104` @ `fa47850d` is `AddSubscriber("backend", …)` with **22** `pubsub.EventHandler(` lines of which exactly one (`:83`, `OrgJobSimulationAssignmentPastDueHandler`) is commented `// not implemented` → **21** live handlers. I counted the lines in the window rather than trusting the doc's total. `:74-78` are precisely the five `OrgSkillPath*` handlers `:129` names.
- **ENUMERATED** — the `MESSENGER_RPC` absence and its stated control, reproduced command-for-command at `stack-demo/platform`: `log -S 'MESSENGER_RPC' --oneline 0c91421d | wc -l` → **0**; `-S 'SKILLER_RPC'` → **7**; `git rev-list --count 0c91421d` → **121**; with `--all` → **8** and **0**; and the extra 8th commit is `464dfe3` (isolated by diffing the two hash lists). `RPC_ADDR` count is **0** in each of `docker-compose.yml`, `common.yml`, `.env_example`.
- **ENUMERATED** — the two-tree terraform split: at `fa47850d` `terraform/main.tf:19` is `service_desired_count = 1` and `:29` is `container_definitions = <<EOF` (no desired-count declaration); at `e9421c68` `:29` is `service_desired_count = 0` with the rollback comment at `:27-28` verbatim. The doc's warning that grading the prod row against the demo clone inverts the answer is exactly right.
- **ENUMERATED** — no scheduler: `git grep -nE "cron|Ticker|asynq|time\.AfterFunc" e9421c68 -- '*.go'` minus `_test.go` → **0** hits; `rpcsrv.go:29` is `Schedule` → `connect.CodeUnimplemented` at **both** refs, and `:25-30` covers `CancelScheduledMessage` + `Schedule`.
- **ENUMERATED** — `cmd/root.go` at `e9421c68`: `:63` `PORT`→8080, `:64` `RPC_PORT`→8081, `:107` `REDIS_STREAMS_INDEX`→"2", `:107-116` redis client → `pubsub.NewSubscriberServer` with no other consumer, `:118-142` the five client constructions over four distinct env addresses (`CMS_RPC_ADDR`, `BACKEND_USERS_RPC_ADDR` ×2, `SKILLER_RPC_ADDR`, `JOBSIMULATION_RPC_ADDR`), `:144` `cache.New(5m,10m)` (`patrickmn/go-cache`), `:147` `READONLY_DB_CONNECTION`. go.mod: `go 1.25.0`, `getbrevo/brevo-go v1.1.3`, `osteele/liquid v1.8.1`.
- **ENUMERATED** — the `app`-side consumer row at `ad9f3c49`: `main.go:15` (`msgflow`), `:62` (`msgadapters`), `:63` (`msgsender`), `:1416-1421` (the literal-consumer-group comment), `:1450` (`pubsub.NewSubscriberServer`), `:1471` (`msgadapters.Wire`), `:1473` (`msgsender.NewFromEnv`), `:295` (the `(messengerEnabled || customerIOSyncEnabled) && BREVO_KEY == ""` condition) and `:296` (`log.Fatalf`). `git diff --stat 2035f9a4 ad9f3c49 -- main.go` is **empty**, so the doc's "every anchor resolves at both refs" claim holds. `env_guards.go:61` is `envMessengerEnabled = "MESSENGER_ENABLED"`, and `git ls-tree b948604f -- env_guards.go` is empty — the "the ref is not optional on this anchor" caveat is correct.
- **ENUMERATED** — the pinned-ref anchors: `app/main.go:1095` @ `9d00a313` is the `CMS_STREAM` publisher; `:1478-1484` @ `9d00a313` is the `streamNames{…}` subscriber binding; `app/main.go:1205-1211` @ `b948604f` is the *"Additive + DORMANT … until the M809 re-point"* comment. All three graded at the ref the cell names (§5 rule 33).
- **ENUMERATED** — flow anchors at `e9421c68`: `brevo.go:288-310` (`TemplateId`+`Params` → `SendTransacEmail`), `console/console.go:71`, `whitelabel.go:17`, `assignments.go:489`, `assignments.go:828` (`getSkillPath` via the cms client), `jobsimulations.go:140-151` (the 2 h / 12 h staleness guards, both verbatim). The Key-directories block's file list matches `git ls-tree -r` exactly apart from B2's annotation. `repos.yml:21-23` @ `0dab54d` is the messenger entry; the deleted compose block published `8200:8200`/`8201:8201`; `compose:84-92` @ `0c91421` is the "no variables here, defaults OFF" comment the doc quotes.

**`academy-backend.md`**
- **ENUMERATED** — the version line: `git describe --tags ad9f3c49` → `v1.369.0-7-gad9f3c498`, and `CHANGELOG.md:5` is `## v1.369.0 - 2026-08-05`; `:47` is `v1.367.0 - 2026-08-04`, matching the `:61` cell. `0e37771f` is *"Academy backend v1.0 'ground truth' … (#903)"*, 2026-06-05.
- **ENUMERATED** — the storage-in-app anchors at `ad9f3c49`: `main.go:516-517` read `internalstorage.EnvBucket`/`EnvPublicBucket`, `:524-525` construct `NewManager`/`NewPublicManager`; and the doc's own negative claim about `:471-472` is **correct** — at `ad9f3c49` those two lines are the closing `}` of the Bedrock-error branch (`:469-471`) plus a blank line.
- **ENUMERATED** — the Ent table set: all 11 `academy_*` schema files exist under `internal/data/ent/schema/`, matching the doc's per-user (6) + catalog (5) split; `academy_path_embedding.go:62` declares `dialect.Postgres: "extensions.vector(1536)"`; `academy.go`, `content.go`, `body.go`, `content_import.go`, `embeddings.go`, `asset.go`, `certificate.go` all present.
- **ENUMERATED** — the GraphQL surface. I read `academy.graphqls`'s `extend type Query` / `extend type Mutation` blocks and matched every name the doc lists: `academyProgress` (`:640`), `academyLastActivity` (`:642`), `academyChapterTime` (`:644`) + `academyChapterTimes`, `academyPathProgress`, `academyCertificates` (`:652`), `academyCertificate @public` (`:654`), `academyBookmarks`, `academyFeedback(s)`, `academyCatalogSeries @public` (`:664`), `academyCatalogSkillPaths @public` (`:672`), `academySkillPath`, `academyChapter`, `academyChapterBody`; mutations `upsertChapterProgress`/`Batch`/`BatchTolerant` (`:694-698`), `setLastActivity` (`:700`), `upsertChapterTimeBatch`, `claimPathCertificate`/`FromLocal`, `add/update/removeAcademyBookmark` (`:713/:715/:717`), `upsertAcademyFeedback` (`:719`). None missing, none invented. `resolver_academy.go` exists.
- **ENUMERATED** — REST + background: `internal/web/backend/content.go:23` registers `GET /content/catalog.json`; `content_admin.go:35` is `e.Group("/content/admin", sharedTokenMiddleware(os.Getenv("ACADEMY_CONTENT_API_TOKEN")))`; the Asynq task type is literally `"academy_embedding_refresh"` (`internal/worker/tasks/academy_embedding_refresh.go:10`) and the 03:00 cadence is stated in-code at `internal/academy/embeddings.go:108` and `schema/academy_path_embedding.go:16`.
- **ENUMERATED** — certificate minting: `certificate.go:24` format comment, `:32` `certIDAlphabet = "ABCDEFGHJKMNPQRSTVWXYZ23456789"` (**30** symbols, counted, ambiguity-free), `:5` `crypto/rand`, `:42` the id regex, `:171-184` the draw. `cmd/academy-seed/main.go:51-56` declares exactly the six flags the doc lists. `cmd/academyImport/main.go:239-243` @ `9d00a313` carries the quoted comment and `:244-246` is the hard error on an empty bucket — both exact. Test files `sqlite_test.go`, `certificate_mint_test.go`, `entitlement_parity_test.go`, `merge_test.go`, `time_test.go`, `progress_aggregate_test.go` all present.

**`TEMPLATE.md`** — read in full (46 lines). It is a pure skeleton of bracketed placeholders with no assertion about the platform, so there is nothing to falsify. Cross-checked one thing that *could* have been a finding: the template fixes **Downstream = services this calls / Upstream = services that call this** (`:23-24`), and all three of my docs that use those headings follow it (`academy-backend.md:113/:116`, `graphql-wundergraph.md:174/:182`, `messenger.md:130`). No inversion. **0 findings.**

---

## What I could not settle

1. **The `infrastructure` citations.** `graphql-wundergraph.md:7` and `:27` cite `infrastructure/terraform/production/services.tf:509-517` and `:521` @ `13c248e6`; `messenger.md:59` cites `services.tf:622` @ the same sha. **`infrastructure` is not in `stack-demo/`** (the 13 clones are app, ant-academy, cms, graphql-wundergraph, jobsimulation, messenger, next-web-app, platform, roadrunner, sentinel, storage, studio-desk + rosetta-extensions), so I could not open any of them. This is in tension with root `CLAUDE.md`, which states in its cms row that M810's prod state is *"**NOT MEASURABLE from our clone set** — `infrastructure` has never been in one"* — while three cells across my files present `services.tf` line anchors as measured. Either a clone existed transiently at iter-123/124 or the anchors are unverifiable; I cannot tell which, and I decline to book it either way. **What would settle it:** a read-only clone of `anthropos-work/infrastructure` at `13c248e6`, or an explicit statement in the corpus of how those anchors were obtained.
2. **Live-render / perf measurements.** `ant-academy.md:104` (*"a grid of 65 real cards"*), `:235-236` (404→200 on `billion`), `:249` (0 cards over HTTP 200), `studio-desk.md:426` (p95 4669→817 ms), `:437` (4049→38 ms, FCP 6936→2152 ms), `graphql-wundergraph.md`'s curl transcripts. These are observations on a running stack, not derivable from source at any ref. I verified the *mechanisms* they describe (the FS-published patch set, the `canAccess` FAPI route, the `main.ts` await chain, the gqlauthz default-closed gate) and all of those hold; the numbers themselves I neither confirm nor dispute.
3. **`ant-academy.md:15`** links to `iter-128/progress.md` under `knowledge/plan/**`. Out of bounds under hard bar #1 — not opened, not graded.
4. **Narrative distance claims.** `ant-academy.md:9` (*"sat 469 lines below"*) and `:500` (*"contradicted itself 213 lines earlier"*) are prose descriptions of the file's state when the defect was found, and the file has grown since. 213 lines earlier from `:500` lands at `:287`, inside the region (`:276`–`:337`) that carries the "public-storefront" / "Phase-1 public launch" / "much wider than a few auth pages" phrases it cites, so it is not misleading; 469 from `:9` does not land cleanly on anything. I treated both as historical narrative rather than citations and did not book them — flagging so a repairer can decide whether such phrasing should be pinned or removed.
