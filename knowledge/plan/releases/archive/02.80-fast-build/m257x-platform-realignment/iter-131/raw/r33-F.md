# Seat F — reading #33/#34 (M257x iter-131)

**Refs I read (re-derived at my open, matching the ADDENDUM):**
`app` `ad9f3c49` · `platform` `0c91421d` · `next-web-app` `8297c684` (origin/main `f97ba659`) ·
`sentinel` `f2c46190` · `studio-desk` `41ee3575` · `ant-academy` `22df69dd` · `storage` `4ce8ece5` ·
`messenger` `fa47850d` · `cms` `ca50c817` · `graphql-wundergraph` `60c229f3` · `roadrunner` `87d8d443` ·
`jobsimulation` `462343b0` (origin/main `82cb66ec`) · rext consumption `09d06070` / authoring `f2ea567b`.
Nested repos read **at their own ref**: `stack-demo/app/studio` and `stack-demo/cms/studio`, both `aeec036a5`.

Pinned refs I additionally resolved because a claim named them: `app` `b948604f`, `app` `9d00a313`,
`app` `7177374`, `platform` `2adcf71`, `platform` `0dab54d`, `platform` `d11a403`, `platform` `838d907`,
`platform` `05b4035`, `cms` `f38c0c4a`, `cms` `6efa1d5`, `storage` `9f8cb532`, `storage` `63bffc8`,
`roadrunner` `84a4b4f` / `e45eb61`, `jobsimulation` `6092c6d2`, `graphql-wundergraph` `915da06`.

**Instrument note.** Every absence claim below was measured with all three instruments named in
`platform-alignment.md` §5 rule 44 where it could matter: `git grep` at a named ref, `git grep -a`
(NUL-bearing files), and the nested repos at their own ref. Positive controls are stated inline.

## Positive controls

| file | `wc -l` | read in full? |
|---|---|---|
| `corpus/services/studio-room.md` | 512 | yes |
| `corpus/architecture/architecture_overview.md` | 463 | yes |
| `corpus/services/jobsimulation.md` | 324 | yes |
| `corpus/services/roadrunner.md` | 216 | yes |
| `corpus/architecture/frontend_architecture.md` | 116 | yes |
| `corpus/services/skiller.md` | 78 | yes |
| *(total)* | 1709 | |

---

## Blockers

### B1 — `jobsimulation.md` tells the reader cms's production state is undecidable, on a premise the corpus measured away at iter-123 — and which its own §Infrastructure bullet 66 lines below contradicts

- **Anchor:** `corpus/services/jobsimulation.md:12` —
  > "**Do not generalise this to `cms`, in EITHER direction** — `cms`'s two measured facts point **opposite ways** … `cms/terraform/main.tf:39` still reads `service_desired_count = 0` in an otherwise-whole 191-line module … **The deciding declaration is in `infrastructure`, which has never been in any clone set: report both, assert neither** — see [`cms.md`](./cms.md) and the fenced map."

- **Why it is false:**

  **(a) It contradicts the fenced map it points at.** `corpus/architecture/platform-migration-status.md:88`
  (cms row) now reads: *"**RESOLVED at M257x iter-123 — the ECS service is DESTROYED.** `infrastructure`
  was cloned and read (`13c248e6`, 2026-08-07): there is **no `module "cms"` declaration anywhere in it**
  … **The blocker this cell named for four iterations — "the destruction happens in infrastructure's
  `services.tf`, which we cannot read" — was a clone-set limit, not a measurement limit, and the fix was
  to clone the repo.**"* The same verdict is carried at `corpus/services/cms.md:8` (*"DESTROYED — corrected
  M257x iter-127"*), `corpus/architecture/service_taxonomy.md:169`, `corpus/architecture/architecture_overview.md:227`
  and `corpus/architecture/org-repos.md:140` (§ 3 table row: `cms/terraform/main.tf:39` `= 0` → **orphaned**).
  Two of those sites explicitly record that they *used to* say what `jobsimulation.md:12` still says —
  `architecture_overview.md:227`: *"(This cell said "not visible to this corpus — `infrastructure` has never
  been in any clone set"; that was a clone-set limit, not a measurement limit, and the fix was to clone
  the repo.)"* This is the un-swept twin of a repair that landed everywhere else.

  **(b) It contradicts itself, within one file.** `jobsimulation.md:78-87` opens with
  *"**MEASURED at `infrastructure` `13c248e6` (iter-123), and it confirms this bullet exactly:**
  `module "jobsimulation_euwest1"` **IS still declared**, at `terraform/production/services.tf:475`
  (`ref=v0.254.0`) … (`:455-474`)"*. A document cannot simultaneously hold that `infrastructure` has
  *never been in any clone set* and quote `file:line` from it at a named sha.

  **(c) The two facts `:12` presents as pointing "opposite ways" do not.** I re-measured both at the ref
  the passage names: `cms` `f38c0c4a` — `terraform/main.tf` is 191 lines and `:39` is
  `service_desired_count = 0` (true), and `6efa1d5` (2026-08-04) deletes
  `.github/workflows/build-production.yml`, 18 deletions, subject *"drop build-production — the cms ECR
  repository is decommissioned (M810)"* (true). Both are true; the corpus's own measurement says the
  first one **describes nothing**, so the pair was never a contradiction and "assert neither" is the
  wrong instruction.

  A reader working on cms teardown is sent away from a settled answer and told to hedge.

- **Confidence:** high

---

### B2 — Same false premise at a second anchor: `jobsimulation.md` declares a production question *unmeasurable* because "`infrastructure` has never been in any clone set", 24 lines above its own reading of that tree

- **Anchor:** `corpus/services/jobsimulation.md:54` —
  > "**And the production declaration is not measurable from this repo at all:** it lives in `infrastructure`, which has never been in any clone set — so no *"still names"* claim can be made here in either direction."

- **Why it is false:** the stated *reason* is refuted by `jobsimulation.md:78` (same file), by
  `platform-migration-status.md:88`, by `org-repos.md:134` (*"Measured at `infrastructure` **`13c248e6`** —
  `grep -n '^module "' terraform/production/services.tf` returns **exactly ten** declarations…"*), and by
  `security_compliance.md:322`, which cites `infrastructure/terraform/production/services.tf:571`. The
  tree has been read; the literal simply has not been grepped there. *Unanswered* and *unanswerable* are
  different states with different next actions (§5 rule 14's *"name the states distinctly"*), and this
  sentence tells the reader to stop looking.

  Corroborating that this is a partial sweep rather than a considered position: `corpus/services/backend.md:13`
  carries the identical stale premise for the same cms row, while five other sites do not.

  **What I positively cleared in the same passage** (so the finding is scoped to the premise, not the
  arithmetic): the `.tf` counts are exact. **44** tracked `.tf` files across the 13 `stack-demo` repos
  (per-repo: app 5, sentinel 4, storage 5, messenger 4, cms 4, graphql-wundergraph 4, roadrunner 4,
  jobsimulation 6, studio-desk 4, next-web-app 2, rosetta-extensions 2, platform 0, ant-academy 0);
  **59** `.tf` files by raw filesystem sweep; **0** hits for the literal in either set; and the literal
  `http://backend.internal.anthropos:8081` occurs **exactly 6 times**, none terraform
  (`app/knowledge/service-dependencies.md:52` ×1 + `rosetta-extensions` ×5), with
  `service-dependencies.md:52` quoted verbatim-correct and under the heading the doc names.

- **Confidence:** medium-high (I hesitated because the *conclusion* about that specific literal may still
  hold; I booked it because the premise is measurably false, the file refutes itself, and the sentence's
  operative effect on a reader is to close a question that is open. Per the briefing's tie-break, that is
  a blocker.)

---

### B3 — `roadrunner.md`'s top banner asserts the prod question is unresolved and calls the *true* statement an overstatement; the fenced map it cites twice records the opposite, measured

- **Anchors** (one claim, four sites in one file):
  - `corpus/services/roadrunner.md:13` — *"**"There is no roadrunner service in production"* overstates it: `roadrunner/terraform/main.tf:19` still reads `service_desired_count = 1`…"*
  - `:30-32` — *"This is the **one row where prod and the platform's own declaration contradict each other** — recorded, not resolved. Say *orphaned* (nothing calls it), not *absent*. See [`platform-migration-status.md`]."*
  - `:53` — *"What still separates roadrunner from that list is only its **GitHub repo, which is NOT archived**, and prod's `service_desired_count = 1`."*
  - `:74` — *"Treat retirement as pending, not done."*

- **Why it is false:** `corpus/architecture/platform-migration-status.md:90` — the fenced authority this
  banner directs the reader to, twice — records:
  > "**MEASURED AT LAST (M257x iter-123, `infrastructure` `13c248e6`):** a service repo's own
  > `service_desired_count` is **not evidence of production state** … **`roadrunner` appears in NO
  > terraform in `infrastructure` at all** — 7 org-wide hits, all of them `judge0_*` secret names still
  > labelled *"Roadrunner"* in two CI workflows plus one KB line. **There is no roadrunner ECS service,
  > and `roadrunner/terraform/main.tf:19` describes nothing.** The whole line of enquiry below is
  > therefore SETTLED."

  `corpus/architecture/org-repos.md:134-146` gives the derivation: the ten `module "…"` declarations in
  `terraform/production/services.tf` **are** the production service set, and roadrunner is not among them
  — the section's headline rule being *"a service repo's `service_desired_count` is not evidence of
  production state."* So `:13`'s "overstates it" inverts the measured verdict; `:31`'s "recorded, not
  resolved" contradicts "SETTLED"; `:53`'s "**prod's** `service_desired_count = 1`" attributes a
  repo-local, uninstantiated module input to production — the exact error class § 3 exists to close; and
  `:74`'s "retirement as pending" is the conclusion drawn from all three.

  **What I positively cleared in the same banner, so the finding is scoped:** `roadrunner/terraform/main.tf:19`
  really does read `service_desired_count = 1` at `87d8d443`; `git blame -L 19,19 87d8d443 -- terraform/main.tf`
  names `84a4b4f` (2025-12-15); `87d8d44` touches exactly one file, `.github/workflows/bump-version.yml`,
  3 insertions; and `e45eb61` (2026-05-27) is the file's most recent touch, a 1-line module-source swap
  whose message says *"Module contents are identical; this is a pure source-URL swap."* Every one of those
  is right. It is the inference from them to production state that the corpus has already refuted.

  This is the same file that `platform-migration-status.md:90` calls out by name for having been *"WRONG
  for four readings"* on the neighbouring sha claim; that half was repaired at iter-115 and this half was not.

- **Confidence:** high

---

### B4 — `architecture_overview.md`'s Roadrunner row still frames the repo's own dead module input as "prod terraform", one row below the CMS row that was fully corrected from the same measurement

- **Anchor:** `corpus/architecture/architecture_overview.md:228` —
  > "| **Roadrunner** | … **Gone locally, orphaned in prod:** at platform `0c91421` there is **no `roadrunner` compose service at all** (deleted by `d11a403`; **5** services remain declared, 7 in the effective topology) **while prod terraform still reads `= 1`** |"

- **Why it is false:** there is no roadrunner terraform in production. `platform-migration-status.md:90`
  and `org-repos.md:140` both measure that `roadrunner` appears in **no** terraform in `infrastructure`
  @ `13c248e6`; the `= 1` lives in `roadrunner/terraform/main.tf:19`, a module block no root module
  instantiates. Calling it "prod terraform" tells a reader an ECS service is declared at desired-count 1
  in production, which is precisely the false picture `org-repos.md` § 3 was written to remove.

  This is a **half-applied repair inside one table**: the CMS row immediately above (`:227`) carries the
  full iter-123/127 correction — *"the prod ECS service is **DESTROYED** … `cms/terraform/main.tf:39` is
  **orphaned dead code**"* — while the Roadrunner row one line below still carries the pre-iter-123
  framing. `platform-migration-status.md:96` records the identical miss happening once already
  (*"iter-123 corrected the sibling rows (`cms`, `roadrunner`, `messenger`) and this one was missed"*).

  **Positively cleared in the same cell:** at `0c91421d` `docker-compose.yml` declares exactly **5**
  services (`sentinel`, `backend`, `studio-desk`, `next-web-app`, `gotenberg`) and `include: common.yml`
  adds `postgresql` + `redis` → **7** effective. `d11a403` does delete the compose service. Those figures
  are exact.

- **Confidence:** medium-high (I hesitated because the cell does say *"orphaned in prod"*, which is the
  corrected word; I booked it because the clause after it re-asserts the refuted evidence in the
  present tense, and the row is the first thing a reader consults.)

---

## Minors

- `corpus/services/roadrunner.md:13-14` — doubled word across the line break: *"…`service_desired_count = 1` and has / has **not been touched since…**"*.
- `corpus/services/jobsimulation.md:267` — *"`backend` is the only *application* container the `core` profile starts (`sentinel` is authz-only)"* silently drops `gotenberg`, which **is** in the default `core` profile (`docker-compose.yml:183` @ `0c91421d`) — a fact this same file states 109 lines earlier at `:158` (*"a live container in the default `core` profile"*). Non-material: the conclusion (backend down ⇒ platform down) holds, and gotenberg is a third-party converter with no user surface.
- `corpus/architecture/frontend_architecture.md:93` — *"regenerated from the upstream subgraph schemas"* (plural). There is one subgraph; the same file says so at `:32` and `:55`, and `packages/graphql/codegen.ts` @ `8297c684` introspects `http://localhost:8082/graphql/query` directly. Stale-but-harmless phrasing under a code block that is correct.

---

## Positively cleared (audited zeros)

**ENUMERATED** — predicate re-derived from source independently, cardinality compared:

1. **`architecture_overview.md:389-395` — "31 of 135 schemas", "29 live `OrganizationMixin{}` … a 30th commented out at `user_resource.go:22`", "23 with `organization_id` and no policy (16 the neither-mixin subset)".** This is §5 rule 24's own fixture and I treated it as such. Measured @ `app` `ad9f3c49`, `internal/data/ent/schema/`: **139** `.go` files; **135** lines matching `ent.Schema` = **112** multi-line embeds + **23** single-line `struct{ ent.Schema }` declarations, i.e. 139 − 4 files declaring no schema — **135 is the schema-type count, not a `grep -c` artefact**. `OrganizationMixin{}` matches **30** lines in **30** files, of which exactly one is commented out (`user_resource.go:22`, `// OrganizationMixin{},  // We need to work on this`) → **29** live. I then enumerated **every** `Policy() ent.Policy` declaration in the directory rather than trusting the stated pair: there are **5** — `UserMixin` (`mixin.go:99`), `OrganizationMixin` (`:126`), `Membership` (`org_membership.go:172`), `Organization` (`organization.go:56`), `User` (`user.go:116`). I read all three schema-level ones: `Membership` and `Organization` filter by organization (`rule.DenyIfNoOrganizationInContext` / `FilterOrganizationEdges` / `DenyNotSameOrganization`); **`User`'s filters by USER** (`rule.FilterSameUserRule`, `DenyNotSameUser`) and is therefore correctly excluded from an *auto-filter-by-organization* predicate. 29 + 2 = **31**. Sub-split: `OrganizationIDMixin{}` in **7** files (`category`, `jobrole`, `similarity`, `skill`, `specialization`, `studio_document`, `studio_task`); a declared `field.*("organization_id")` with neither mixin in **19** files, of which `skiller_mixins.go` is the mixin *definition* file, not a schema → **18**, matching `security_compliance.md:82`; 18 − 2 policed = **16**; 16 + 7 = **23**. Every figure holds and the predicate holds with it.

2. **`frontend_architecture.md:41-50` — the `NEXT_PUBLIC_BACKEND_API_URL` table.** Re-derived at both refs over non-test `.ts`/`.tsx`, `e2e/` excluded, **using `git grep -a`** so the NUL trap could not silently shrink the set: `8297c684` → **22** files / **31** occurrences / **47** `fetch(` sites / core-js **12 env / 27 calls**; `f97ba659` → identical 22 / 31 / 47 / 12 / 27, exactly as the note claims. `new EventSource(` = **0** tree-wide. Positive control on the NUL mechanism: `apps/web/src/hooks/useCoursebuilder.ts` carries **1** NUL byte (`tr -dc '\000' | wc -c`) and returns 0 for the env var with *and* without `-a` — a genuine zero, not an instrument zero. I also checked the **prose** list at `:39` against the derived file set: its eleven groups sum to 4+3+1+1+1+4+3+1+1+1+2 = **22** and name the same files. `packages/core-js/src/talkToData/api.ts:214` carries the quoted sentence.

3. **`architecture_overview.md:299` / `:256` — supergraph "3 → 1", and the commit subject being wrong.** At `915da06^`, `supergraph-config-prod.yaml` lists **3** subgraphs (`backend`, `jobsimulation`, `cms`); `915da06`'s `--name-status` shows `D schemas/cms.graphqls` **and** `D schemas/jobsimulation.graphqls`; its own subject reads *"supergraph 2→1"*. At `60c229f3`: one subgraph, `schemas/backend.graphqls` alone, `subgraphs.conf` = `BACKEND=v1.360.0`. The corpus is right and correctly flags the commit message as the source of the wrong figure.

4. **`skiller.md:52-61` — taxonomy figures.** Read `.agentspace/snapshots/taxonomy/*/manifest.json` directly: `source: primary-read`, `public_only: true`, `predicate: org-null`, `captured_at 2026-06-29T14:43:00Z`; `public.skills` **42,790**, `public.job_roles` **22,470**, `public.job_role_embeddings` **18,919** (the "different table" the doc names as the source of the refuted "18K"). Both digests agree; one is the `skiller`-schema capture, one the `public` re-ground, same sha256s per table — consistent with the M209 re-ground the doc describes.

5. **`roadrunner.md:130` — jobsimulation's roadrunner mentions.** `git grep -in roadrunner 462343b0` = **14** lines / **8** files; exact-case = **5** lines / **3** files; Go source = **1**, `internal/runner/runner.go:3`, a comment. Exactly as claimed, and the doc's own warning that the repo-wide form differs from the Go-scoped form is the correct reading.

6. **`roadrunner.md:41` — the case-insensitive sweep.** Per-clone file counts at each clone's own HEAD: app **25**, jobsimulation **8**, platform **3**, studio-desk **3**, next-web-app **1**; sentinel/storage/messenger/cms/graphql-wundergraph/ant-academy **0**. app reads 25 at `b948604f` too, so the figure is stable across the checkout move.

7. **`studio-room.md:186` — "exactly nine arguments (`gen.py:484-492`)".** `add_argument` count in `gen.py` @ `aeec036a` = **9**, at lines **484-492** inclusive, in the order and with the defaults the doc's option block lists (`-f` → `dest: forced`, `--annotations` default `"{}"`, `--pipeline` default `linear`, `--branch` choices `{stable,experimental}`).

8. **`studio-room.md:365-395` — the `mistralai` callout, i.e. the trap this seat was warned about.** Grepped the nested repo **at its own ref**: `git grep -i mistral aeec036a` = **22** lines / **3** files — `requirements.txt` **1** (line 8), `tools/pdf2md.py` **18**, `tools/r3.py` **3**. `pdf2md.py:24` = `from mistralai import Mistral`; `:96` = `client = Mistral(api_key=api_key)`; `:127` = `model="mistral-ocr-latest"`; `r3.py:194` = `if "MISTRAL_API_KEY" not in env:`. The instrument caveat is also true as stated: `app/studio/.gitignore:2` is `tools/`, and a recursive shell `grep` over the working tree returns exactly **1** (the requirements line); `app/.gitignore:79` and `cms/.gitignore:129` are both `studio/*`. The engine registry at `services/ai.py:704-708` is `{openai, azure, anthropic}` and `ai.py:1-2` imports only `openai`/`anthropic`. Go execs exactly two studio scripts — `studio/gen.py` at `studioManager.go:119`, `studio/postgen.py` at `:1045` — both via `runCommand`, whose definition at `:1099` (`exec.CommandContext` at `:1101`, argv-form doc-comment at `:1096-1098`) confirms both of the doc's two citation windows (`:1099-1101` at `:35`, `:1096-1101` at `:345`) are correct rather than contradictory.

9. **`jobsimulation.md:142-147` — the mirror-drop migration, including "0 `INSERT`s".** `app/terraform/migrations/20260729133514.sql` @ `ad9f3c49`: `:58` = `-- 5. Drop the mirrors.`, `:62`/`:63` = the two `DROP TABLE`s, `:15-23` = the two id-remap `UPDATE`s, `:36-44` = the two orphan-NULLing `UPDATE`s. `INSERT INTO` count = **0**; the single case-insensitive `insert` hit is the function name `on_insert_local_jobsimulation_sessions_update_memberships` at `:64` — i.e. the doc's zero survives the `grep -c`-counts-lines trap. `internal/organization/intelligence.go:1700` = `query := m.ent.JobSimulationSession.Query().`

10. **`jobsimulation.md:39-45` — the 23 tables and the renamed session table.** `20260722081626_jobsim_data_model.sql` contains exactly **23** `CREATE TABLE` statements; `20260722104506.sql:2` creates `job_simulation_sessions` and `:79` is `DROP TABLE "sessions";`. `public.sessions` does not exist.

11. **`architecture_overview.md:83` — "four imported private modules; `authn` is not a fifth".** `go.mod` across all **seven** Go clones: `github.com/anthropos-work/authn` = **0** in every one; positive control `github.com/anthropos-work/colony` = present in all seven.

12. **`architecture_overview.md:404-407` — "2 of its 11 Echo groups".** Enumerated every `e.Group(` in app @ `ad9f3c49`: **11** real groups (`/api/invitations`, `/academy/embeddings`, `/api`, `/ask`, `/assignment-builder`, `/admin/backfill`, `/coursebuilder`, `/credits`, `/content/admin`, `/api/workforce`, `/v1/labs`) — one further `.Group(` hit is a gqlgen complexity field in `graph.go` and one is a comment. `cbGate` (`courseBuilderAccessGate`, `gate.go:27-49`) is applied at exactly **2**: `backend.go:232` and `:276`. Matches `security_compliance.md`'s 6+5 split.

13. **`roadrunner.md` §Async tasks + §Ports + §Testing.** `queues.DefaultQueue = "roadrunner:default"` (`queues.go:4`), `asynq.MaxRetry(3)` (`runner.go:126`), `Concurrency: 10` (`worker.go:25`), `maxRetries = 15` (`runner.go:244`) with `time.Sleep(1 * time.Second)` (`:273`), `RoadrunnerSubmissionCompleted` published at `:277-278`, handler registered at `worker.go:48`; `cmd/root.go:110`/`:84` for `PORT`/`RPC_PORT` defaults; compose @ `2adcf71` `:291-292` ports and `:296-302` env; **0** `*_test.go` files with `RUN go test -v ./...` at `Dockerfile:18`; task-type constant `"roadrunner:submissionresult"`; no `.proto` vendored; `internal/lsp` referenced by nothing outside itself.

14. **`repos.yml` membership across refs** (asserted in all four service docs): **9** entries @ `2adcf71` (roadrunner at `:29-31`, jobsimulation at `:17-19`), **6** @ `0dab54d`, **4** @ `0c91421d` (`app`, `sentinel`, `next-web-app`, `studio-desk`). `d11a403`'s diff deletes `- name: cms`, `- name: jobsimulation` **and** `- name: roadrunner` while its message says roadrunner's entry *"was already gone"* — the corpus's *grade-it-by-the-diff* reading is correct.

15. **App `main.go` anchors at four refs** (`jobsimulation.md:215-223`, `:250-253`): `func main()` at `:229` @ `ad9f3c49`, `:216` @ `7177374`, `:212` @ `b948604f`; `jobsimwiring.Wire` at `:721` with its `log.Fatalf` at `:723` @ `ad9f3c49`, `:670` @ `9d00a313`, `:614` @ `b948604f` — all three resolve to the identical statement. Only `cmd/createTaxonomy/main.go` imports `spf13/cobra`.

16. **`STORAGE_RPC_ADDR`** (`architecture_overview.md:360-379`): @ `9d00a313`, **3** Go hits, every one a comment — `main.go:451`, `internal/jobsimwiring/wiring.go:101`, `internal/storagens/callsites_test.go:189` — and **29** lines across **18** files repo-wide. @ `ad9f3c49` (origin/main) the same 3 Go hits, with `main.go:504` carrying the quoted *"the standalone service takes no traffic and STORAGE_RPC_ADDR is gone."* @ `b948604f` it is still **read** at `wiring.go:115`. `storage/terraform/main.tf` @ `9f8cb532` is exactly **18** lines with `:9-11` and `:13-16` as quoted; `63bffc8` has `service_desired_count = 0`.

17. **`jobsimulation.md:9` / `architecture_overview.md:20`, `:226` — M810 for jobsimulation.** Graded at the ref the claim names (`6092c6d2`, an ancestor of `origin/main` `82cb66ec`): `terraform/main.tf` is 56 lines, `service_desired_count` appears **nowhere**, `:15-22` is the M810 decommission comment naming exactly the destroyed resources, `:24-40` the surviving ownership, `:38-40` the still-pending schema drop. The four 2026-08-04 commits are `6092c6d2`, `caf36c96` (PR #439), `1e40d184`, `82cb66ec` — exactly as listed. (At the *checkout* `462343b0` the module still stands at `:31` with `= 0` at `:40`; §5 rule 33 makes the named ref decisive, and I state both.)

18. **Frontend stack facts** (`frontend_architecture.md` + `architecture_overview.md:60`): `"next": "~16.2.12"` — tilde — at `apps/web/package.json:46` and in `hiring`/`integration`/`maintenance`; `apps/mobile` declares none; lockfile resolves `next@16.2.12` with `react@19.2.7`; `packageManager: "pnpm@10.30.3"`; `engines.node: ">=24.0.0"`; `typescript: ^5.9.3`; `!apps/mobile` in `pnpm-workspace.yaml`; ports 3000/3001/3002/3031; filter names `@anthropos/web-app`, `@anthropos/hiring-app`, `@anthropos/integration`; **8** locale dirs `de,en,es,fr,it,ja,nl,pt`; `UPGRADE-IMPACT-next16.md` present; `clean:quick` drops exactly node_modules/.next/.turbo/dist; `check`/`check:lint`/`check:types`/`check:deprecations` all present.

19. **Studio-Room config + structure** (`studio-room.md:35, 67, 84-97, 108, 164-178, 251-273, 290-322, 443-468, 502-506`): cms Asynq worker `Concurrency: 5` with `AiVideoQueue: 7` / `StudioQueue: 3` (`internal/cms/worker/worker.go:29-34`); venv consts at `studioManager.go:93-96`; `cert.py` = certification-benchmark CLI with **0** hits for certificate/signing/private_key/x509; `max_tokens = 4000` in all three tracked configs and the five `*_AI_STABLE_MODEL` rows verbatim; `worklog_path = workspace/trace` (+ the other three paths) and `gen.py:450-455` deriving them; `parse_argument` `:18-28`, `translate_legacy_blueprint` `:212-238` with `_LEGACY_TEMPLATE_DEFAULTS` `:205-209` carrying exactly the three legacy names, `validate_blueprint_exclusivity` `:241-271` with the quoted error string, `setup_generation_request` `:273`; postgen targets = `guidance`/`metadata`/`translation`/`toolkit` with a separate `testing.py` and export done by `exporter.py` (`:518-519` the two zips, `:513-550` the unpack/write/re-archive/`rmtree`, `sim_source_id` fallback at `:517`); `model.py:59` `self._cache = {}` and `:467-469` the memo; `services/ai.py` `:383`/`:530`/`:664` = OpenAI/AzureOpenAI/Anthropic constructors; `taxonomy.py:11` `BASE_URL = "https://api.anthropos.work/api"`; blueprint sets exactly as listed (`b2b, business, none_chat, none_voice, technical`; `code/micro/scenario`) and `technical.json`'s keys all real; `postgen.py` `--media`/`--simid`/`--target` all `required=True`; `app/Dockerfile:45-46` copies `studio/` and pip-installs it, runtime `FROM python:3.11-slim` at `:28`; CI `additional_repo: "anthropos-studio-room:studio"`.

20. **Compose topology @ `0c91421d`** (asserted in three of my files): `AUTHORIZATION_ADDRESS=http://sentinel:8087` `:48`, `GOTENBERG_URL=http://gotenberg:3200` `:57`, `JUDGE0_BASE_URL` `:59`, gotenberg `profiles: [core, backend, all]` `:183`, `NEXT_PUBLIC_BACKEND_API_URL` build arg `:152` / runtime `:161`, studio-desk `depends_on: backend` `:138-140` + profiles `:141` (was `:226` @ `0dab54d`), next-web-app `depends_on` `:165-167` + profiles `:168`, `Makefile:10` `PROFILE ?= core`, floor = `postgresql`+`redis` (`common.yml`, no `profiles:` key) + `sentinel`. **Zero** `*_RPC_ADDR` anywhere. `838d907` (2026-08-05, merged `0c91421`, PR #26) deletes `storage`, `messenger`, `customerio-sync` and drops the first two from `repos.yml`.

21. **`jobsimulation.md:240-244` — the `$HOME/.aws` bind, graded at its own pin.** @ `0dab54d`: comment `:88-89`, `volumes:` `:90`, bind `:91`, under `backend`. Still present at `0c91421d`, moved to `:99`/`:100` — so the pinned anchors are right and the hazard is still live; both stated.

**RE-DERIVED** (arithmetic the document displays, re-computed — the weakest clearance, listed separately as the briefing requires): none. Where a document showed its own derivation (items 1, 2 and 12 above) I enumerated the predicate from source first and only then compared totals.

---

## What I could not settle

- **Every `infrastructure` @ `13c248e6` anchor.** `terraform/production/services.tf:475`, `:455-474` (`jobsimulation.md:78-87`), `:64-70` (`architecture_overview.md:227`), `:509-517` (`architecture_overview.md:3, 47, 332`, `frontend_architecture.md:39`). **No `infrastructure` clone exists anywhere in this tree** — I checked `stack-demo/`, `stack-dev/` and a `find -maxdepth 3 -type d -name infrastructure`. Five corpus sites agree on the shas, line numbers and quoted text, which is strong internal corroboration but is not ground truth. *What would settle it:* adding `infrastructure` to the clone set at `13c248e6` — which is exactly what makes B1/B2 findings rather than acknowledged gaps, since the corpus records elsewhere that this was already done once.
- **GitHub archive states.** `roadrunner.md:84` (*"not archived"*), `architecture_overview.md:3, 227` (`graphql-wundergraph` archived 2026-07-30), `roadrunner.md:66-68` (`chronos` not archived), the jobsimulation archive question. Not derivable from a clone. *What would settle it:* `gh repo view --json isArchived` per repo.
- **`architecture_overview.md:206` and `:453-459` — the whole `db-backup` row** (Bash, 43 lines, zero `.go`, S3+Hetzner never Azure, trigger commented out since `7dd1b80` 2025-05-29, `rate(12 hours)`, prod pinning that commit). The repo is in no clone set. `security_compliance.md:322` cites `infrastructure/.../services.tf:571` and `infrastructure/modules/core/storage/rds.tf:6,19,78-89`, both unreachable here. The two sites are mutually consistent, which is all I can say.
- **`architecture_overview.md:405-407` — "seven routes mounted on the root outside any group".** I enumerated the 11 groups and the 2 `cbGate` applications directly; I did not independently enumerate the root-mounted route set. It is consistent with `security_compliance.md`'s second table but I did not derive it. *What would settle it:* enumerating `e.GET/POST/...` calls on the root `e` in `internal/web/backend/`.
- **`studio-room.md:5, 25, 37` — "app v1.360.1" as the version at which the `additional_repo` pull landed.** The mechanism is verified (`.github/workflows/build-production.yml:29-30`); the version at which it first appeared is not, and `app`'s tags were not part of my read.
