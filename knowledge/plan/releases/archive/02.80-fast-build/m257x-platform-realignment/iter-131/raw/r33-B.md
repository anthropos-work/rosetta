# Seat B — reading #33/#34 (M257x iter-131)

**Refs actually read** (re-checked at this seat's open, per the ADDENDUM):
`app` `ad9f3c498` · `platform` `0c91421` · `next-web-app` `8297c684` (and `bb3313bc`, `f97ba659` where a
claim pins them) · `sentinel` `f2c46190` (and `88bc5592` where pinned) · `studio-desk` `41ee3575` ·
`storage` `4ce8ece5` (+ `origin/main` `9f8cb532`) · `messenger` `fa47850d` · `cms` `ca50c817` ·
`jobsimulation` `462343b0` (+ `6092c6d2`, `82cb66ec`) · `roadrunner` `87d8d443` ·
`graphql-wundergraph` `60c229f3` · `app/studio` `aeec036a` ·
`rosetta-extensions` **pinned per-stack clone** `09d06070` (all tooling claims below were settled there;
the authoring copy `f2ea567b` was read only to confirm the section list is identical in both).

No fetch was performed by this seat. `gh` is **not installed** on this box, so no GitHub
repo-metadata claim (archived / last-push) was settleable — see *What I could not settle*.

## Positive controls

| file | wc -l | read in full? |
|---|---|---|
| `corpus/services/ai-readiness.md` | 719 | yes |
| `corpus/services/clerkenstein.md` | 383 | yes |
| `corpus/services/chronos.md` | 252 | yes |
| `corpus/services/sentinel.md` | 171 | yes |
| `corpus/architecture/dependency_map.md` | 103 | yes |
| `corpus/services/README.md` | 90 | yes |

(Also read in full, as the briefing requires: `corpus/ops/platform-alignment.md` §5, rules 1–57.)

## Blockers

### B1 — `sentinel.md` publishes a re-derivation receipt whose stated result is wrong: the grep returns ZERO, not "one unrelated hit"

- **Anchor:** `corpus/services/sentinel.md:5` — *"What survives, and was re-derived: messenger's Go source
  imports no authorization client (`git grep "authorization\|AUTHORIZATION_ADDRESS" fa47850d -- '*.go'
  go.mod` returns **one unrelated hit**, against `colony` present as a positive control)"*
- **Why it is false:** I ran the **literal** command at the **named ref**:
  - `git -C stack-demo/messenger grep "authorization\|AUTHORIZATION_ADDRESS" fa47850d -- '*.go' go.mod`
    → **exit 1, zero lines**.
  - Widened three ways, all zero: `-E "authorization|AUTHORIZATION_ADDRESS"` over the same pathspec → 0;
    `grep -in "authorization"` over the **whole tree** at `fa47850d` → 0;
    `grep -il "authoriz"` over the whole tree → **0 files**. There is no occurrence of the substring
    `authoriz` anywhere in `messenger` at `fa47850d` (61 tracked files, 42 `.go`).
  - Positive controls in the same pass, same repo/ref/pathspec: `colony` → 5+ files
    (`cmd/root.go`, `cmd/send.go`, `cmd/trigger.go`, `go.mod`, `internal/flow/assignments_test.go`);
    `package` over `'*.go'` → matches; `brevo` → 5 files. The pipeline is not broken.
  - The nearest thing to a hit is `messenger/go.mod:67` `golang.org/x/oauth2 v0.36.0 // indirect`, which
    the quoted pattern does **not** match.
- **Why it is a BLOCKER and not a MINOR (I hesitated):** the *conclusion* the sentence supports —
  messenger imports no authorization client — is **true, and in fact stronger than stated**. What is
  false is the **published measurement**, in a clause explicitly framed as *"was re-derived"*. A reader
  re-running it gets 0 and lands exactly in §5 rule 2's trap (*is my pipeline broken, or the corpus?*),
  and §5 rule 24's lesson applies inverted: a wrong audited **one** is evidence pointing the wrong way
  in the same manner as a wrong audited zero. `sentinel.md:5` is also the only place in my file set that
  ships a runnable derivation for this claim, so nothing else corrects it.
- **Confidence:** medium

### B2 — `dependency_map.md` still lists `roadrunner` as one of EIGHT domains "inside Backend (`app`)"; `services/README.md` and the fenced map both say SEVEN and name that exact phrasing as the defect they corrected

- **Anchor:** `corpus/architecture/dependency_map.md:9` — *"Since the monolith merge most of this matrix
  collapsed: `skiller`, `skillpath`, **`roadrunner`**, `jobsimulation`, `cms`, `storage`, `messenger` and
  `customerio-sync` are all **domains inside Backend (`app`)**, so their edges are in-process calls, not
  dependencies."*
- **Contradicted by, inside my own file set:** `corpus/services/README.md:39` — *"**plus** the **seven**
  folded domains this index's banner names — skiller …, skillpath, jobsimulation, cms, storage,
  messenger, customerio-sync … — **`roadrunner` is NOT one of them**: `app/internal/roadrunner/` exists at
  no ref … **This row listed a "roadrunner domain" until M257x iter-102**, contradicting the ⚠️ at
  `:20-23` of this same file and the `app` row of `platform-migration-status.md`."*
  README's banner at `:20` seconds it: *"`roadrunner` is **the eighth, and it is different**: orphaned,
  not merged-and-undeployed."*
- **And by the fenced map:** `corpus/architecture/platform-migration-status.md:87` (the `app` row) —
  *"Owns **seven** domains in-process … **`app/internal/roadrunner/` does not exist** — the Judge0 runner
  was absorbed as `app/internal/jobsimulation/runner/`."*
- **Ground truth settles which side is right:** at `app` `ad9f3c49`,
  `git ls-tree -r --name-only ad9f3c49 | grep -i roadrunner` → **exit 1, no path** (positive control
  `jobsimwiring` → 3 paths); the nested `app/studio` repo at `aeec036a` → **0** as well. `git log --all
  --diff-filter=A -- 'internal/roadrunner*'` → **0 commits, ever**. The seven README names all exist:
  `internal/{skiller,skillpath,jobsimulation,cms,storage,messenger,customeriosync}/`. The Judge0 runner is
  `internal/jobsimulation/runner/`, constructed at `internal/jobsimwiring/wiring.go:123`
  (`jsrunner.NewRunnerManager(getenv("JUDGE0_API_KEY"), getenv("JUDGE0_BASE_URL"))` — verified byte-exact).
- **Why it matters to a reader doing real work:** `dependency_map.md` is the doc a reader opens to learn
  *what lives where*. Its list sends a reader looking for `app/internal/roadrunner/`, which has never
  existed at any ref. This is §5 rule 19's half-repair class precisely — the claim was corrected in
  `README.md` and in the fenced map at iter-102 and left standing in the twin that no partition owned.
- **Confidence:** medium (I hesitated because `dependency_map.md:18`'s own Roadrunner row phrases it more
  defensibly — *"Merged into `app` with jobsim-in-app — `backend` calls Judge0 directly"* — so one could
  read `:9`'s "domains" loosely. But README:39 books that exact loose reading as the defect it repaired,
  which makes this a live self-contradiction whichever way you resolve it.)

## Minors

- `sentinel.md:85` — *"the model at [`architecture_overview.md:343`] — "the only cross-process RPC edge out
  of backend on a core stack""*. The quoted string is at **`:347`**; `:343` is the sentence
  `**On a local stack** (platform \`2adcf71\` deleted the router …)` that introduces the block. The reader
  lands 4 lines above the quote, inside the same passage — drift, not a wrong passage.
- `sentinel.md:43-44` — *"[`manager`] appears … only as a fixture string in sentinel's own tests
  (`internal/authorization/casbin_test.go`, `internal/rpcsrv/rpc_test.go`)"*. A third test file does the
  same: `internal/authorization/manager_test.go:21` `manager := "manager"`, used as a **role** at `:31`
  (`{org2, manager, member, …}`). The load-bearing claim (*there is no `manager` role*) is correct —
  0 hits in `init_policy.sql`, verified — the parenthetical enumeration is just short one member.
- `sentinel.md:156` — the env-var table's `PORT` row puts **`8087`** in the **Default** column while its
  own description says *"binary default is 8080"*. Measured: `cmd/root.go:47`
  `port := cmp.Or(os.Getenv("PORT"), "8080")`. The cell corrects itself, but the column is wrong.
- `ai-readiness.md:573` — `deriveMode` (**`useAIReadiness.ts:48-62`**) is a **bare basename with two
  tracked files**: `apps/web/src/components/ai-readiness/useAIReadiness.ts` (where `deriveMode` really is
  at `:48-62`, byte-verified, including the `: true` null-deadline branch at `:57`) and
  `apps/web/src/hooks/useAIReadiness.ts` — and **this same document cites the `hooks/` one by full path at
  `:660`**. A reader who resolves the bare name the way the doc taught them lands on an unrelated
  interface. §5 rule 36's *resolve only on uniqueness*.
- `dependency_map.md:19` — *"`docker-compose.yml:126-131` for the `depends_on`"* (@ `0dab54d`). The
  `depends_on` block is `:126-130`; `:131` is the first line of the `# v9.0: NOT in the default profiles`
  comment. One-line overshoot. The paired env-block citation `:117-123` is **exact** and its seven-name
  list is exact and in order.
- `ai-readiness.md:646` — `computeCycleTotals` (**`how_we_measure.go:253-261`**). The func is at `:260`;
  `:253-254` are the tail (`return out, rows.Err()` / `}`) of the *previous* function and `:255` is blank.
  The range does contain the construct; it just starts seven lines inside a different one. (The paired
  citation `:285-287` is byte-exact.)
- `ai-readiness.md:275-279` — the bullet scopes the REST surface to `app/internal/web/backend/api/` and
  then folds in *"the M407/M408 email-preview + email-override admin endpoints"*. Those are **not** in
  `api/` (`api.yaml` has no `email-preview`/`email-override` path); they are a separate echo handler,
  `internal/web/backend/emailpreview/handler.go:1`, serving
  `/api/workforce/ai-readiness/cycles/{id}/email-preview`.
- `clerkenstein.md:109` — the `cmd/` row lists *"`mintpk` · `fake-fapi` / `fake-bapi`"*; the dir also
  holds **`jwtkey`** (4 entries, not 3). Reads as enumerative; harmless.

## Positively cleared (audited zeros)

Every item below is labelled **ENUMERATED** (predicate re-derived from source independently, cardinality
stated before any arithmetic) or **RE-DERIVED** (only the document's own arithmetic re-computed). Only the
first is a clearance.

**`corpus/services/README.md`**
- **ENUMERATED** — *"Every service doc in this directory, enumerated."* `ls corpus/services/*.md` = **29**;
  minus `README.md` + `TEMPLATE.md` = **27**. Index rows: 10 core + 5 frontends + 8 cross-cutting +
  4 archived = **27**. Sets match, not just totals.
- **ENUMERATED** — *"`docker-compose.yml` declares **5** services (7 effective, with `common.yml`'s
  `postgresql` + `redis`), and `repos.yml` carries **4** entries — app, sentinel, next-web-app,
  studio-desk."* @ `0c91421`: compose = sentinel `:5`, backend `:28`, studio-desk `:112`,
  next-web-app `:143`, gotenberg `:170`; `common.yml` = postgresql, redis (+ the network);
  `repos.yml` = exactly those four names.
- **ENUMERATED** — `:39` `app/internal/{skiller,skillpath,jobsimulation,cms,storage,messenger,customeriosync}/`
  **all present @ `ad9f3c49`** (`ls-tree internal/`), and no `roadrunner` path anywhere (see B2).
  `internal/jobsimulation/runner/` present; `jobsimwiring/wiring.go:123` is the
  `jsrunner.NewRunnerManager(…)` line, byte-exact.
- **ENUMERATED** — `:46` messenger row. `messenger/internal/flow/flow.go:72` is literally
  `h.subServer.AddSubscriber("backend", sub.AddHandler(` and the block closes at `:104`; I counted the
  handlers inside it: **21 live**, with `OrgJobSimulationAssignmentPastDueHandler` commented out at `:83`
  and correctly excluded (§5 rule 24's *`grep -c` counts commented-out code* avoided). `MESSENGER_RPC_ADDR`
  → **0 hits in all 15 git trees** (13 clones + the two nested `studio` repos, each grepped at its own ref;
  `MESSENGER_RPC` matches only two lines of `app`'s own `knowledge/plan/` prose).
  `git -C platform log -S 'MESSENGER_RPC' --oneline 0c91421d` → **0**; positive control
  `-S 'SKILLER_RPC'` → **7**. Both figures exact.
- **ENUMERATED** — `:22-24` prod terraform. `roadrunner/terraform/main.tf:19` **is**
  `service_desired_count = 1`, and `git blame -L 19,19` names **`84a4b4f1`, 2025-12-15** — exact.
  `cms/terraform/main.tf:39` **is** `service_desired_count = 0`. `jobsimulation` @ the *cited* commit
  `6092c6d2`: `module "jobsimulation"` is deleted, `service_desired_count` grep → **exit 1, 0 hits**
  (positive control: `module` → 4), and `:15-22` is the `# M810 decommission …` comment. Note the
  jobsimulation **checkout** is `462343b0`, which still carries the block — the claim names `6092c6d2`,
  so rule 33 grades it there and it is true.
- **ENUMERATED** — `:28-30` `d11a403` deleted cms + jobsimulation + roadrunner from **both**
  `docker-compose.yml` (11 services → 8) **and** `repos.yml` (9 → 6). Graded on the **diff**, not the
  commit message, which says roadrunner's clone entry *"was already gone"* — it was not.
  `838d907` (2026-08-05) then removed the last three.
- **ENUMERATED** — `:54` graphql row. `915da06` is a **`graphql-wundergraph`** commit (resolves in no other
  clone), and the squash contains both the jobsim-subgraph removal and the cms fold → **one** subgraph.

**`corpus/services/sentinel.md`**
- **ENUMERATED** — the Casbin model. `internal/authorization/casbin.go`: request defs `r…r6` = **6**,
  policy defs `p…p6` = **6**, role defs `g,g2,g3` = **3**, matchers `m…m6` = **6**. Every matcher row of
  the table checked against its actual expression.
- **ENUMERATED** — `:22` *"256 CPU / 128 MB — `terraform/locals.tf:4-5`"*: `:4` `service_cpu = 256`,
  `:5` `service_memory = 128`. Exact.
- **ENUMERATED** — `:40` the four `MembershipRole` values at
  `app/internal/data/ent/enum/membership.go:8-15` (RoleAdmin/RoleMember/RoleCandidate/RoleContentCreator
  — the range spans exactly the four consts); `init_policy.sql:88-118` is exactly the `content_creator`
  block (I counted the file's lines to place both ends); `internal/authorization/casbin_content_creator_test.go`
  exists.
- **ENUMERATED** — `:43` *no `manager` role*: `git grep -in manager … init_policy.sql` → **exit 1, 0**.
- **ENUMERATED** — `:68-83` the RPC table. `internal/rpcsrv/rpc.go` exposes **19** handler methods
  (21 `^func` minus `NewRPCHandler` and `convertPolicy`); the table names exactly those 19, no more, no
  fewer.
- **ENUMERATED** — `:85` `AUTHORIZATION_ADDRESS` = **1 occurrence**, `docker-compose.yml:48`, across
  `docker-compose.yml` + `common.yml` + `.env_example` @ `0c91421` — **and** the same at `0dab54d`.
  `GOTENBERG_URL` at `:57`, gotenberg declared `:170`, `profiles: [core, backend, all]` at `:183`,
  `JUDGE0_BASE_URL` at `:59` — all four byte-exact. `app/internal/converter/gotenberg.go:31` is the
  `http.NewRequestWithContext(…, gotenbergURL+"/forms/libreoffice/convert", …)` line.
- **ENUMERATED** — `:5` the messenger retraction's *other* halves: compose @ `0c91421` declares five
  services at the five stated lines; `git grep -n messenger … docker-compose.yml common.yml repos.yml`
  returns **4 hits, all comment lines**; and the messenger block **did** begin at
  `docker-compose.yml:156` at `0dab54d`. The one clause that fails is B1.
- **ENUMERATED** — `:12` `go.mod:3` `go 1.26.0`, `Dockerfile:2` and `Dockerfile.dev:2` both
  `FROM golang:1.26-bookworm`. `:14` one table, `init_policy.sql` creates only `sentinel.casbin_rules`.
  `:16` sentinel declares **no `profiles:` key** — always-on floor. `:52-64` every listed path exists.
  `:130` `make initdb` is a hard-coded
  `psql "postgresql://postgres@localhost:5432/postgres?sslmode=disable" < init_policy.sql` — no
  `DB_CONNECTION`. `:134` `init_policy.sql:63-66` is exactly the *taxonomy:write is NOT seeded* NOTE.
- **ENUMERATED** — `:98-102` the M257x-iter-130 correction. `68272003` (2026-08-04) is
  *"feat(atlas): second Atlas pipeline for the sentinel schema (M1001)"*; `app/atlas.hcl:50` opens
  `env "sentinel" {` and `:64` is its `revisions_schema = "sentinel"`; `app/Makefile:59-60` is verbatim
  *"Applying needs no such setup: `atlas migrate apply --env sentinel` creates the / schema itself, and
  that is what local/CI actually run."*; `sentinel` @ `f2c461903` has **no** `atlas.hcl` and no
  `terraform/migrations/` (`ls-files | grep -iE atlas|migrations` → exit 1), and `repos.yml` marks it
  `migrations: false`.

**`corpus/services/chronos.md`**
- **ENUMERATED** — `045857c` = *"remove chronos service from orchestration…"* (2026-04-17);
  jobsim `09631fb2` = *"refactor: remove Chronos references…Asynq…"*, merged by `500b9761` from
  **`feat/remove-chronos-and-realtime`** (PR **#395**). `git grep -ci chronos 0c91421d` over the whole
  `platform` repo → **exit 1, 0 hits**; positive control `redis` → 5+ files.
- **ENUMERATED** — the three `app` anchors, all byte-exact @ `ad9f3c498`:
  `internal/jobsimwiring/wiring.go:192` = *"// read-only DB; chronos/roadrunner/realtime clients are
  gone."*; `internal/jobsimulation/simulator/stream_handlers.go:30-31` = the two-line
  *"chronos, realtime, and the roadrunner-submission … removed upstream"* NOTE;
  `internal/jobsimwiring/worker.go:24-35` spans the `hibiken/asynq` import through
  `std := asynq.NewServer(...)` inside `StartWorkers` — i.e. the Asynq workers do run in `backend`.
- Note: the historical body (ports, sqlc, schema, CLI flags) is **unsettleable** — `chronos` is in no
  clone set. It is correctly framed as historical.

**`corpus/architecture/dependency_map.md`**
- **ENUMERATED** — `:7` every compose anchor: `:48` AUTHORIZATION_ADDRESS, `:57` GOTENBERG_URL,
  `:59` JUDGE0_BASE_URL, `:66` REDIS_ADDR, `:93-94` SUPABASE_DB_CONN / COPILOT_DB_CONN, `:183` gotenberg's
  `profiles: [core, backend, all]`. All six exact.
- **ENUMERATED** — `:13` backend `depends_on` = `:101-109` exactly; `:102-103` is verbatim
  *"# storage, messenger and customerio-sync are not services any more — this one / # container serves all
  three in-process."*
- **ENUMERATED** — `:19` storage. @ `0dab54d` the env block `:117-123` is **exactly seven** variables in
  exactly the order the cell lists, `:116` is the `environment:` key (so the cell's own retraction of
  *"eight … `:116-123`"* is itself correct), and neither `DB_CONNECTION` nor `REDIS_ADDR` is among them
  while `depends_on` names `redis` + `postgresql` `service_healthy`. I also re-derived the withdrawn
  attribution: `corpus/services/storage.md` contains **none** of `depends_on` / `DB_CONNECTION` /
  `REDIS_ADDR` — the withdrawal is correct.
- **ENUMERATED** — `:22` customerio-sync: `profiles: [customerio-sync, all]` **is** at
  `0dab54d:docker-compose.yml:154`; the *"one pusher, not two"* half is supported by
  `docker-compose.yml:84-92` @ `0c91421`, whose `:87-88` reads *"which default to OFF on a developer
  machine"*.
- **ENUMERATED** — `:24` studio-desk `depends_on` = `:138-140`, **`backend` and only `backend`**; and
  `:135` `VITE_GRAPHQL_ENDPOINT=http://localhost:8082/graphql/query`.
- **ENUMERATED** — `:25` studio-room. At the **nested repo's own ref** `aeec036a` (invisible to
  `git -C app grep`): `services/ai.py:705-708` is the whole `providers = {openai, azure, anthropic}` dict
  — no mistral; `tools/pdf2md.py:24` is `from mistralai import Mistral`;
  `git grep -ci mistral aeec036a` summed = **22 lines / 3 files**
  (`requirements.txt`, `tools/pdf2md.py`, `tools/r3.py`). The figure and the file count are both exact.
- **ENUMERATED** — `:42`/`:48`/`:49`/`:50` the shared-library census. Seven Go repos on disk
  (`graphql-wundergraph` has **no `go.mod`** — verified by `ls-tree`, so it is correctly excluded).
  Per-repo `go.mod` requires: **colony 7/7 · proto 7/7 · taxonomy 6/7 (roadrunner is the miss) ·
  ai 2/7 (cms `go.mod:9`, jobsimulation `go.mod:11`, both `v1.40.2`) · authn 0/7** — every figure
  reproduced. `app` dropped `ai` at **`1e457fa70`** (2026-08-04, *"refactor(ai): fold the ai library into
  app as internal/ai"*) and `internal/ai/module_import_guard_test.go` exists.
  `colony/authn`-importing `.go` files: **app 129 · cms 6 · jobsimulation 8** — exact.
  taxonomy: `app/go.mod:20` @ `b948604f` (direct) and `sentinel/go.mod:21` @ `88bc5592`
  (`// indirect`) — both exact at the refs the cell names.
- **ENUMERATED** — `:58` the messenger-in-app cell, the most anchor-dense in my set, **all seven exact**
  at `ad9f3c49` *and* independently at `2035f9a4`: `internal/messenger/flow/streams.go:65`
  `StreamBackend = "backend"`; `main.go:1493` naming the three stream literals; `main.go:1445`
  `if messengerEnabled {`; `main.go:285` `mustSubsystemSwitch(envMessengerEnabled)`;
  `env_guards.go:61` `envMessengerEnabled = "MESSENGER_ENABLED"`; `main.go:1437` *"ALL OF IT is behind
  MESSENGER_ENABLED."*; `docker-compose.yml:84-92`. The retraction's own evidence also holds:
  `main.go:1442` is a comment line at the newer refs, and `main.go` @ `b948604f` is **1361 lines**
  (so `:1442` does not exist there) vs 1639 @ `ad9f3c49`.
- **ENUMERATED** — `:59` the skiller-stream cell, the 6-drift/1-holds split. @ `b948604f`:
  `main.go:287` `pubsub.NewPublisher(serviceName, …)`, `:637` `SKILLPATH_STREAM`, `:1039` `CMS_STREAM`,
  `wiring.go:127` `AI_USAGE_STREAM`, `:180` `JOBSIMULATION_STREAM`, `main.go:1276`
  `subServer.AddSubscriber(os.Getenv("SKILLER_STREAM"), roleManager.SkillerSubscriber())` — and
  `SKILLER_STREAM` has **exactly one** `.go` occurrence at that ref. @ `2035f9a4` **and** `ad9f3c49` those
  six drift to precisely the constructs the cell names (`logger.Info("subsystem switches",` / `)` /
  `serverContext,` / `apiKeyManager,` / an asynq-client construction / a blank line), while
  `internal/roles/roles.go:791` is `func (r *RoleManager) SkillerSubscriber() *pubsub.Subscriber {` at
  **all three** refs — so the "6 drift / 1 holds" retraction is exact. @ `ad9f3c49`:
  `SKILLER_STREAM` = **6 lines / 3 `.go` files** (`main.go`, `subscriber_merge_test.go`,
  `subscriber_wiring.go`) and **6 files** off the pathspec; `main.go:1537` is
  `Skiller: os.Getenv("SKILLER_STREAM"),` inside the stream-names struct; **no** `NewPublisher` names it
  at either newer ref; `docker-compose.yml:71` sets `SKILLER_STREAM=skiller`.
- **ENUMERATED** — `:60`/`:61` `messenger/internal/flow/flow.go:105` is
  `AddSubscriber("jobsimulation", …)` and `:109` is `AddSubscriber("cms", …)`. Both exact.
- **ENUMERATED** — `:31` the counts: @ `0c91421` 5 declared + 2 from `common.yml` = **7 effective**,
  `repos.yml` **4**; @ `0dab54d` 8 + 2 = **10** and `repos.yml` **6**; and @ `2adcf71` compose still
  declares **all three** of `cms:`, `jobsimulation:`, `roadrunner:`. Every number reproduced.

**`corpus/services/ai-readiness.md`** (the densest file; I opened every `file:line` it cites)
- **ENUMERATED** — the package refactor: commit `4c28365f0` (2026-07-22) exists with the quoted subject and
  is contained in tag **`v1.351.1`**; `internal/aireadiness/` exists with **every** rename-table target
  (`readiness.go`, `scoring.go`, `csv.go`, `steps.go`, `narrative.go`, `how_we_measure.go`, `cycles.go`,
  `compare.go`, `diagnosis.go`, `provision.go`, `defaults.go`, `emailoverride/`, `emailpreview/`,
  `notifications/`, `testdb_test.go`), and `internal/workforce/members.go` is still where `LoadMembers`
  lives.
- **ENUMERATED** — the gates. `enum/organization_settings.go:47` is
  `OrganizationSettingAIReadiness OrganizationSetting = "ai_readiness"`; `isAIReadinessEnabled` is at
  `internal/aireadiness/steps.go:541`. In `next-web-app` @ `8297c684`:
  `AIReadinessClient.tsx` has **0** `posthog` and **0** `flag_ai_readiness` (positive control:
  `useAiReadinessEnabled` → 2 hits), `:135` is exactly
  `const { orgEnabled } = useAiReadinessEnabled(true);`, `:78` is
  `const SHOW_SECONDARY_TABS: boolean = false;`, `:601` is
  `(SHOW_SECONDARY_TABS && tab.key === 'compare')`, `:155-156` is the
  `effectiveCycleId = selectedCycle ?? activeCycle?.id ?? latestClosedCycle?.id` pair and `:171` is
  `enabled: featureOn && cyclesQ.isFetched,`. **All four re-pointed anchors resolve, and so does the
  un-drifted `:78`** — i.e. the iter-115 correction is itself correct.
  `useAiReadinessActive.ts:19/22/25/29/32` are byte-exact, and `AI_READINESS_FLAG` has exactly **two**
  non-`node_modules` source sites (`aiReadiness.constants.ts:26` declares it, `useAiReadinessActive.ts`
  consumes it) — the *"sole consumer repo-wide"* claim is enumerated, not asserted.
- **ENUMERATED** — the scoring contract. `scoring.go:26` `archetypeHighBand = 75`, `:30`
  `archetypeLowCeil = 50`, `:46` `classifyArchetype`, and the code's own band comment
  (`:63-66`) is verbatim **None 0-24 / Low 25-50 / Medium 51-74 / High 75-100**.
  `enum/ai_readiness.go:9-11` = the three step types.
  `defaults.go`: `defaultReadinessSkills` weights → **19 × `1.0}` + 12 × `0.5}` = 31 entries**, i.e.
  `19 + 6 = 25.0` denominator (the set enumerated before the arithmetic, per §5 rule 24); and
  `defaultReadinessSims` at `:75-79` is **exactly three** entries — `{StepSimulation,"tech"}`,
  `{StepSimulation,"business"}`, `{StepInterview,"both"}`.
- **ENUMERATED** — the branch contract, every anchor byte-exact @ `ad9f3c49`: `readiness.go:330`
  `computeOrgBreakdowns`, `:388` the `keepInCycleStep1` call, `:390` the `keepStartedMembers` call,
  `:710`/`:730` their `func` lines, `:289` `GetAIReadinessWithOptions`, `:290-297` the cycle-scoped route,
  `:307-312` the default route, `:314` `buildLiveResponse`, `:139` `computeTier1`, `:655` `audienceScope`
  with `:662-664` its body, `:684` `queryInCycleStep1Completers`, `:825`
  `members, _ := m.workforce.LoadMembers(ctx, orgID, "")` inside `buildResponseFromSnapshots` (`:817`).
  `steps.go:922` `queryReadinessSimCompleters`, `:962` *"This DB signal is the only real 'has started'
  check"*, `:964` `queryReadinessStarters`, `:969-974` its raw SQL (`status <> 'not_started'` on
  `ai_readiness_user_step_progresses`, no evidence table) — so the iter-49/52/108 corrections all hold.
- **ENUMERATED** — the data model. **All 13** `ai_readiness_*` tables the table lists exist in
  `internal/data/ent/migrate/schema.go` (3 hits each — const, table var, name), and
  `interview_aggregated_reports` carries `organization_id`, `sim_id`, `report` (JSON), `session_count`
  exactly as claimed. `usageDimensionsFromReports` (`how_we_measure.go:1186`), `resolveSessionAuthors`
  (`:1115`), `holdsBackFromInsights` (`:1008`), `computeInterviewInsightsV2` (`:832`) all exist.
  `usageDimSpecs` (`:1160`) keys on **avg_adoption / avg_transformation / avg_originality / avg_depth /
  avg_ownership**, and the retired `avg_frequency|avg_breadth|avg_context_fit` → **0 hits** in
  `internal/`. `computeCycleTotals`'s question query at `:285-287` is verbatim
  `FROM public.interactions i JOIN public.job_simulation_sessions s`. The `interactions.action_type`
  enum is **exactly `{"email","call"}`** (`migrate/schema.go:2291`) and the `source_id <> target_id`
  CHECK is real (`schema/interaction.go:50`).
- **ENUMERATED** — the interfaces. `ai_readiness.graphqls` exposes **exactly** `aiReadinessEnabled`,
  `aiReadinessUserPlanProgress`, `aiReadinessSkills` + the mutation
  `completeAiReadinessSkillMapping` — the four the doc names, and no fifth. `api.yaml` declares
  **all nine** REST paths the doc lists (`/workforce/ai-readiness`, `/narrative`, `/steps-completion`,
  `/cycles`, `/setup`, `/cycles/{cycleID}`, `/export.csv`, `/compare`, `/cycles/{cycleID}/close`).
  `CloseDueAIReadinessCycles` at `cycles.go:554`.
  **`AIReadinessCSVColumns` = 15 entries** (csv.go `:18-32`) — and the code's own doc-comment still says
  *"19-column"*, which is precisely what the corpus flags as *"dropped from the earlier 19"*.
  `AIReadinessCompareResponse` carries exactly Topline / Archetypes / Transitions / TeamDelta /
  SkillCoverage / ThemesShift = **6 sections** (+ the two cycle refs).
  `emailoverride.go:29` imports `github.com/anthropos-work/app/internal/messenger/aireadinessemail`
  (the corrected path) while `:33` still uses the `pkg/` shorthand the corpus says the error came from,
  and `internal/messenger/aireadinessemail/` holds **exactly 9 files**.
- **ENUMERATED** — the UI/surfaces block. `urls.ts:51` **is** `AI_READINESS_URL` @ `8297c684`, `:52` is
  `TALK_TO_DATA_URL`, and @ `bb3313bc` `:52` **is** `ORGANIZATION_FEEDBACK_URL` — the retraction is exact
  in both directions. `useNavbarSections.tsx:4` / `:401-408` / `:403` / `:569` all exact;
  `AI_READINESS_URL` appears in **exactly** two source files + `knowledge/ai-readiness/frontend-architecture.md:15`;
  `e2e/specs/web.ai-readiness.spec.ts` exists. `WorkforceNewClient.tsx:125-151` is exactly the five-tab
  `items=[…]` array with no readiness tab. `useWorkforceAIReadiness.ts:23-27` is the
  `callWorkforceAPI('/ai-readiness', token, { searchParams: { tag }, signal })` call — **no `cycle`
  param**. `AIReadinessHero.tsx:88` is `if (!air.deadline) return null;`. `dae0fb2f7` (2026-07-13) deletes
  `AIReadinessContainer.tsx` (103) + `AIReadinessIntro.tsx` (220) + `AIReadinessView.tsx` (330) =
  **653** — the doc's figure, and correctly *not* the commit's 662 total.
- **ENUMERATED** — the "Handled for you" retraction. `HowWeMeasureTab.tsx` is **1,989 lines**;
  `:1879` opens the `{/* ===== C · Handled for you this cycle ===== */}` block, `:1903` is the label,
  and the three cells are `skillsMapped` `:1915`, `handsOnMinutes` `:1921`, `interviewMinutes` `:1927`;
  `grep -c interviewQuestions` over that file → **0**. `apps/web/src/hooks/useAIReadiness.ts:326` is
  `interviewQuestions: number;` inside `AIReadinessCycleTotals` (`:323-330`) — **and** at `bb3313bc` the
  same field is at `:274`, exactly as the pin-warning says.
- **ENUMERATED** (pinned per-stack clone `09d06070`) — the seeder side.
  `ai_readiness_interview_report.go:185-189` is exactly the five `{"id": "avg_*"}` maps and `:149` is
  verbatim *"All five are always emitted here."*; `cockpit.go:135` `LegacyReadinessPaths` and `:161`
  `ValidateCockpitManifest`; `ai_readiness_funnel.go:205` `aiReadinessStageFor`;
  `blueprint.go:200` `AIReadiness string \`yaml:"ai_readiness"\`` + `:347` `IsAIReadinessNotStarted`;
  `seat_append_test.go` and `ai_readiness_funnel_test.go:230`
  `TestAIReadinessFunnelSeeder_DayZeroHeroGetsNoSignals`; `contentref.go`; `ai_readiness_evidence.go`;
  `app-aireadiness-snapshot-loadmembers.yaml:42` `path: internal/aireadiness/readiness.go` and `:33`
  *"v2.7 M254 RE-POINT"*; `demo-stack/tests/test_aireadiness_snapshot_loadmembers_m254.py`.
- **RE-DERIVED** (weakest clearance, flagged as such) — the seeded-population arithmetic the doc displays:
  156/199 = 78.39 %; `round(1.0/25.0*30)=1`; `round(0.5/25.0*30)=1`; `round(9.0/25.0*30)=11`;
  `round(3.0/6.5*30)=14`; `25/25*30=30`; `30+40+30=100`; `(30+40)=70`; `held/25×100 = held×4` integral for
  half-step weights. I could **not** enumerate the seeded set itself (it needs a live stack) — see below.

**`corpus/services/clerkenstein.md`**
- **ENUMERATED** — *"eleven sections"*: `git ls-tree` on **both** rext clones returns the identical
  eleven (`alignment`, `clerkenstein`, `demo-stack`, `dev-stack`, `playthroughs`, `stack-core`,
  `stack-injection`, `stack-secrets`, `stack-seeding`, `stack-snapshot`, `stack-verify`) plus the
  non-code `knowledge/`. And the twin claim checks out: **`CLAUDE.md` on disk now reads "eleven"** at
  `CLAUDE.md:141-145` (⚠ the `CLAUDE.md` copy carried in my session context is a *stale snapshot*
  that still says nine — I graded the file, not the snapshot). `§5` rule **54** exists
  (`platform-alignment.md`, *"A correction that reaches ONE cell is not a correction"*) and is the right
  rule to cite; §5 runs to rule **57**.
- **ENUMERATED** — every DNA gene count, parsed from the JSON rather than read from prose:
  `clerk-2.6.0` **27 genes / 14 capabilities**, `clerk-js-5` **9**, `clerk-multi-1` **9**,
  `clerk-express-1` **13 / 5 capabilities**, `clerk-deploy-1` **7**. All five match the doc exactly.
  `clerk-js-5`'s `Me` capability has variants `universal-user` + `unauthenticated` only — **no** gene for
  `/v1/me/organization_memberships`, so the *"not yet a measured gene"* caveat is enumerated.
  `clerk-2.6.0.json:131` contains verbatim *"M219 landed the fix … taking the Go surface 97.2% -> 100%."*
- **ENUMERATED** — `alignment/cmd/alignctl/run.go:134-135` is exactly
  `ExitRegressed = 2` / `ExitUnmeasurable = 3`. `clerk-backend/store.go:138` is
  `func (s *Store) SeedOrgIdentity(org, eid string)` and `:151` is
  `func (s *Store) LookupOrgEid(org string)`.
- **ENUMERATED** — the single-tenant disclosure, which is the load-bearing part: `clerk-frontend/registry.go`
  has one `activeKey` (`:24`), `active()` (`:67`) and `Select()` (`:75`); `server.go` has one `signedIn`
  (`:102`), one `signedOut` (`:107`), `clientID: "client_clerkenstein"` (`:125`) and
  `sessID = "sess_clerkenstein"` set in `establishLocked` (`:663-665`, which also clears `signedOut`).
  **`r.Cookie(` → 0 hits across the whole `clerkenstein/` section**, and the only two `Cookie` lines in
  `server.go` are *Set-Cookie* comments — the *"cookies are only ever emitted"* claim is enumerated, not
  asserted. All **five** named regression tests exist in `clerk-frontend/server_test.go`.
- **ENUMERATED** — the M220 fix: `clerk-frontend/server.go:35-67` is the M220 block, with
  `clerkJSFetchTimeout = 15 * time.Second` (`:59`), `FAKE_FAPI_CLERKJS_CACHE` (`:45`) and
  `clerkJSClient` *"Explicitly NOT http.DefaultClient"* (`:65-68`).
- **ENUMERATED** — the dir table: `authn/`, `clerk-backend/`, `clerk-frontend/`, `clerk-webhook/`,
  `shared/`, `deploy/`, `cmd/`, `alignment/` all exist; `deploy/colony-authn/` exists;
  `alignment/cmd/{clerkrun,deployrun,expressrun,jsfapirun,multirun}` = exactly five;
  `golden`, `golden-js`, `golden-multi`, `golden-express`, `golden-deploy` = exactly five;
  `dna/` holds exactly five; `clerkenstein/alignment/scripts/` exists while the **sibling**
  `rosetta-extensions/alignment/` section has **no** `scripts/` dir — exactly as `:368-370` says.
  `clerkenstein/.github/workflows/alignment.yml` is git-tracked.
- **ENUMERATED** — the colony/clerk-sdk drift callout, which is the one most likely to rot:
  `clerkenstein/go.mod:8` = `colony v0.34.3` (the artifact's pin); `app/go.mod` = `colony v0.35.2` **and**
  `clerk-sdk-go/v2 v2.7.0` at *both* `b948604f` and `ad9f3c49`; **`sentinel/go.mod:8` = `colony v0.35.2`**
  @ `f2c46190`, moved there by `88036d7` (2026-08-03, *"chore(deps): update dependencies to latest
  versions"*) — so the retraction is right that the softening clause is false for sentinel; and
  `storage/go.mod:7` = `colony v0.34.3`, so *"rests on the frozen `storage` repo alone"* is right too.
- **ENUMERATED** — `studio-desk` @ `41ee3575`: `src/index.ts:96` **and** `app/services/userService.ts:16`
  are both
  `const STUDIO_ACCESS_ROLES = ['admin', 'org:admin', 'content_creator', 'org:content_creator'];`
  each preceded by the quoted *"Both the prefixed (org:*) and bare role keys are accepted"* comment.
- **ENUMERATED** — the default-on retraction: `demo-stack/up-injected.sh:27-40` carries the D-DESIGN-3
  flip and the `--no-public-host` / `--public-host` parsing (with the `exit 1` on anything else at `:41`),
  and `:108-116` is `resolve_public_host()` with the auto-discovery fallback. Both ranges exact.

## What I could not settle

1. **`services/README.md:48` + `dependency_map.md:38` — the `db-backup` claims** (*43-line Bash*,
   *S3 + Hetzner, never Azure*, *0 Azure hits across all 157 objects*, *schedule commented out at
   `7dd1b80` / 2025-05-29*). `db-backup` is in **no clone set** (`ls stack-demo/`, `find -name
   infrastructure`). Settled by cloning `anthropos-work/db-backup` and running `git rev-list --objects
   --all | wc -l` + `git log -S Azure --all` + `git show 7dd1b80`.
2. **`services/README.md:54` + `dependency_map.md:23` — `infrastructure/terraform/production/services.tf:509-517`
   @ `13c248e6`.** There is no `infrastructure` clone anywhere under the repo root, so the *"destroyed in
   production"* citation is unverifiable from this tree. It is *internally* consistent with
   `service_taxonomy.md:3` and `platform-migration-status.md:96`, both of which quote the same lines — but
   three sites quoting one unread file is one witness, not three. **Note the standing tension** this
   creates with the `cms` row's *"the destruction itself happens in infrastructure's `services.tf`, which
   we cannot read"* (`CLAUDE.md`, out of my read scope, so not booked): either infrastructure was read at
   iter-123/124 — in which case the cms row's "cannot read" is stale — or it was not, in which case the
   graphql rows cite a file nobody opened. Settled by adding `infrastructure` to a clone set, or by
   recording *how* `13c248e6` was read.
3. **GitHub repo metadata** — `chronos.md:3` / `README.md:81` *"the GitHub repo is NOT archived (last push
   2026-04-23)"*, and `README.md:54` *"repo ARCHIVED"* for `graphql-wundergraph`. **`gh` is not installed**
   on this box (`command not found`), and neither repo is in the clone set with remote refs that carry
   archive state. Settled by `gh api repos/anthropos-work/{chronos,graphql-wundergraph} --jq '.archived,
   .pushed_at'`.
4. **`chronos.md:14-72, 97-252` — the whole historical body** (ports 8080/8081, sqlc, the `timers` DDL and
   its four indexes, the `ChronosService` RPC shapes, the triggerer's `LIMIT 10` / `FOR UPDATE SKIP
   LOCKED` / 3-second timeout / 1-minute Sentry heartbeat, the env-var and CLI-flag tables). `chronos` is
   in no clone set. The page frames all of it as preserved history, which is the right framing, but none
   of it is checkable here. Settled by a `chronos` clone at any ref.
5. **`dependency_map.md:13` — *"the 23 jobsim run-state tables"*.** I did not find a derivation I trusted:
   the legacy `jobsimulation` schema, the ent schemas moved, and the tables now in `public` are three
   different sets and the sentence does not say which. Settled by naming the predicate (e.g.
   *tables created by `jobsimulation/terraform/migrations/` that now exist in `public`*) and shipping the
   command beside it — §5 rule 24.
6. **`ai-readiness.md:404-406, 600-602` — the seeded figures** (78.4 % / ≈156 of 199 / `byTeam` 13 /
   `people` 199 / the 24 ms and 2.09 s timings / the 180 s → 19 ms demo-patch delta). These are
   measurements against a **running seeded stack**, not against source. The arithmetic re-derives (above),
   which is the weak clearance; the SET does not. Settled by a cold reset-to-seed on a demo plus the two
   authenticated GETs.
7. **`sentinel.md:5` — *"Ten other corpus sites already recorded the deletion, two of them in this same
   file"*.** The *"two in this same file"* half is true (`:85` and `:89` both attribute the deletion to
   `838d907`). I did not attempt the tree-wide count of ten, because the predicate (*"recorded the
   deletion"*) is prose-shaped and not greppable without inventing it.
8. **`clerkenstein.md:45-56, 253-255` — the live `/align-run` scores.** I verified every **gene count** and
   the DNA/runner inventory from source, and the `ExitRegressed`/`ExitUnmeasurable` contract. The
   **percentages** (100 %/100 % on four surfaces, `clerk-express-1` unmeasurable) are the output of a run I
   did not perform, and the doc itself warns at `:278-287` that a `clerk-deploy-1` score taken against
   `colony v0.34.3` is not measuring the binary under test — a caveat I confirmed is *arithmetically*
   live (artifact `v0.34.3` vs both live services on `v0.35.2`).
9. **`storage`'s prod terraform state is ref-dependent and I did not book it.** `dependency_map.md:19` and
   `services/README.md:43` say the ECS service is **deleted, not scaled**. At the frozen **checkout**
   `4ce8ece5`, `storage/terraform/main.tf` is **100 lines** and still declares
   `module "storage" { … service_desired_count = 1 }`. At `origin/main` `9f8cb532` it is **18 lines** with
   the module gone and the buckets/CloudFront comment in its place — i.e. the claim is **true at
   origin/main and false at the checkout**. I did not book it: the corpus's own citation resolver defaults
   to `CITE_REF=auto` → `origin/main` first (§5 rule 41), a prod-state claim is about what deploys, and a
   frozen legacy repo's checkout is an artifact of when the demo stack pinned it. Recording it because the
   next reading should decide this deliberately rather than inherit my judgement.
