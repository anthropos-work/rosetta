# Seat E — reading #33/34 (blind; I do not know which)

**Refs I graded against** (re-derived at my open, from the working tree):
`app ad9f3c498` (= origin/main) · `platform 0c91421d` (= origin/main) · `next-web-app 8297c684c` (origin/main `f97ba659`) ·
`sentinel f2c46190` · `studio-desk 41ee3575` · `ant-academy 22df69dd` (DIRTY tree — every read via `git show 22df69dd:<path>`) ·
`storage 4ce8ece5` · `messenger fa47850d` (origin/main `e9421c68`) · `cms ca50c817` · `graphql-wundergraph 60c229f3` ·
`roadrunner 87d8d443` · `jobsimulation 462343b0` · nested `app/studio` + `cms/studio` both `aeec036a` ·
`stack-demo/rosetta-extensions 09d06070` (pinned consumption clone) · `.agentspace/rosetta-extensions f2ea567b` (authoring).
Where a claim named its own ref (`b948604`, `9d00a313`, `2035f9a4`, `0dab54d`, `2adcf71`, `d11a403`, `749dc86`, `7c17e63`,
`915da06`, `e9421c68`) I read **that** ref. **I state which rext tree settles each rext claim below; for every rext claim I
checked, the pinned clone and the authoring copy were byte-identical, so the distinction changed no verdict.**

## Positive controls

| file | `wc -l` | read in full? |
|---|---|---|
| `corpus/services/ant-academy.md` | **529** | yes |
| `corpus/services/studio-desk.md` | **449** | yes |
| `corpus/services/graphql-wundergraph.md` | **288** | yes |
| `corpus/services/messenger.md` | **219** | yes |
| `corpus/services/academy-backend.md` | **167** | yes |
| `corpus/services/TEMPLATE.md` | **46** | yes |

Protocol read: `corpus/ops/platform-alignment.md` §5 (lines 259–1889), 3029 lines total.

**Two instrument failures I caught in-pass and corrected** (recorded because §5 rule 2 says a broken pipeline reads
like a clean corpus):
1. `git show "$R:code/..."` in zsh silently applied the `:c` modifier and mangled the path into a bogus rev
   (`22df69ddode/src/...`). Quoting did not help; only a fully-quoted literal `'22df69dd:code/...'` works. Every earlier
   result from that form would have read as "file absent".
2. A `git grep … | head -3` truncated the alphabetically-last hit and made `STUDIO_ACCESS_ROLES` look absent from
   `src/index.ts`. It is at `src/index.ts:96`. I nearly booked a false BLOCKER; re-running without `head` refuted it.

---

## Blockers

### B1 — `messenger.md`: the Brevo body is rendered by Brevo, not by Liquid — the file retracts this at :15-21 and re-publishes it at :121

- **Anchor:** `corpus/services/messenger.md:121` —
  *"Messages carry user info, template ID, and template params; the body is rendered through Liquid against those params
  **before the Brevo send**."*
- **Why it is false:** `messenger/internal/messenger/brevo/brevo.go:288-310` @ `fa47850d` builds
  `brevo.SendSmtpEmail{ … TemplateId: templateId (`:306`), Params: props (`:307`) }` and posts it to
  `s.brevo.TransactionalEmailsApi.SendTransacEmail` (`:310`). **Brevo renders.** `git grep -n "Render\|liquid" fa47850d --
  internal/messenger/brevo/brevo.go` returns **exit 1 / zero hits**.
  **ENUMERATED:** Liquid is used at exactly **three** non-test sites in the whole messenger repo —
  `internal/flow/assignments.go:489`, `internal/flow/whitelabel.go:17`, `internal/messenger/console/console.go:71`
  (`git grep -n liquid fa47850d -- 'internal/*'`, minus `_test`). None is in the Brevo path.
  This is a **self-contradiction inside one file**: `:15-21` explicitly retracts this exact wording —
  *"⚠️ Two words here were wrong until M257x iter-129 … *'Liquid templating for the bodies'*: the Brevo sender posts a
  `TemplateId` + `Params` (`brevo.go:288-310`) and **Brevo** renders"* — and the repair reached the Role paragraph but not
  the Interface-Discovery sentence 100 lines below. §5 rule 54's reach failure, exact shape.
- **Confidence:** high

### B2 — `messenger.md`: the `message/` package does no Liquid rendering

- **Anchor:** `corpus/services/messenger.md:96` (key-directories block) —
  `    message/                 Message types + Liquid rendering`
- **Why it is false:** `internal/messenger/message/` @ `fa47850d` contains exactly `errors.go`, `message.go`,
  `validator.go` (+ `message_test.go`). Its only exported functions are `New`, `NewWithDefaultSender`, `ConvertProps`
  (`message.go:12`, `:21`, `:33`) and `ValidateMessage`, `ValidateUser` (`validator.go:9`, `:27`).
  `git grep -in "template\|render" fa47850d -- internal/messenger/message/` (non-test) returns **exit 1**;
  `git grep -n liquid` over the same path returns nothing. Liquid rendering lives in `console/` and `flow/` (see B1).
  Second surviving site of the predicate the same file retracts at `:15-21`.
- **Confidence:** high

### B3 — `academy-backend.md`: `env/callsites_test.go` does not exist in `app`

- **Anchor:** `corpus/services/academy-backend.md:66` — *"(3 remaining occurrences at that ref — `main.go:504`,
  **`env/callsites_test.go:189`**, `internal/jobsimwiring/wiring.go:101` — all comments, zero reads)"*
- **Why it is false:** `git -C stack-demo/app ls-tree -r --name-only ad9f3c49 | grep callsites_test` returns exactly one
  path: **`internal/storagens/callsites_test.go`**. No `env/` directory exists anywhere in `app`
  (`ls-tree -r … | grep -E '^env/|/env/'` → empty), at `ad9f3c49`, `2035f9a4` **or** `9d00a313`.
  The line number is right for the real file (`internal/storagens/callsites_test.go:189` = *"…the standalone storage
  service is scaled to 0 and STORAGE_RPC_ADDR is"*), and the **count of 3 is correct** — the Go-source occurrences at
  `ad9f3c49` are `main.go:504`, `internal/jobsimwiring/wiring.go:101`, `internal/storagens/callsites_test.go:189`.
  So the arithmetic checks out and the **path does not resolve**: a reader who opens it finds nothing and cannot tell
  whether the claim or the tree is wrong. The instrument names "wrong file paths" as a BLOCKER explicitly.
- **Confidence:** high

### B4 — `academy-backend.md`: the cross-file citation into `ant-academy.md` lands on an unrelated blockquote

- **Anchor:** `corpus/services/academy-backend.md:136` — *"contradicting [`ant-academy.md`](./ant-academy.md)`:82-88`,
  which states the opposite in bold."*
- **Why it is false:** `ant-academy.md:82-88` is the **store.js / beacon write-path** blockquote (*"`saveChapterProgress`
  (`:150`) calls the injected authed requester with `UPSERT_CHAPTER_PROGRESS` at `:162` … reached only by
  `navigator.sendBeacon` / `fetch({keepalive:true})` on pagehide"*). It says nothing about an FS-as-published fallback.
  The bolded statement the sentence means is at **`ant-academy.md:96-97`**: *"**⚠️ It does *not* 'fall back to the
  committed FS catalog'** — there is **no FS-as-published fallback** in the app"*. The citation resolves and names the
  wrong construct — §5 rule 21's class 2, and precisely the failure the addendum warns about (*"a pin that resolves to a
  different construct than the sentence describes is a BLOCKER"*).
- **Confidence:** high

### B5 — `academy-backend.md`: "(Consistent with :85-89 below.)" points at certificate minting

- **Anchor:** `corpus/services/academy-backend.md:15` — *"The env-var *name* outlived the router, which is now destroyed
  in production as well (iter-124). **(Consistent with :85-89 below.)**"*
- **Why it is false:** `academy-backend.md:85-89` is the tail of the `academy_path_embeddings` bullet (`source_hash`
  skip-key), the **Certificate minting** bullet (`AAS-YYYY-XXXXXX`, `crypto/rand`, PII-minimized verify), and a blank
  line. Nothing there concerns `NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT`, the subgraph, or the router. The passage that is
  actually consistent with it is `:93-96` (*"**There is no separate 'academy subgraph'** … The frontend reaches them at
  (`NEXT_PUBLIC_WUNDERGRAPH_ENDPOINT`)"*). The pointer is off by ~8 lines onto a different subject.
- **Confidence:** medium-high (I hesitated only because the misdirection is a cross-reference rather than a factual
  assertion about the platform — but the sentence makes a checkable claim about this document's own content, and it is
  false)

### B6 — `graphql-wundergraph.md:136`: two self-anchors in one sentence, both naming the wrong bullet

- **Anchor:** `corpus/services/graphql-wundergraph.md:136` — *"…as the *Build-time, static composition* bullets at
  **`:114-117`** of this doc describe (specifically **`:116-117`**, the struck-through *"`make up` rebuilds `graphql`"*
  bullet: *"It **used to** rebuild whenever any subgraph schema changed, because the build context is the parent dir
  (`..`) holding all sibling repos"*). **Not `:84`**, which is the *Ports* bullet and is about `8080`/`5050`…"*
- **Why it is false:** measured in the file itself —
  * `:116-117` is the **"Adding/changing a subgraph **or a single field** requires re-running `wgc compose` … there is
    **no hot reload**"** bullet. The struck-through *"`make up` rebuilds `graphql`"* bullet — and the sentence's own
    verbatim quote — are at **`:118-119`**.
  * `:84` is **`* **Federation**: Apollo Federation v2, `federation_version: =2.3.2` (pinned)`**. The **Ports** bullet
    (`8080 → 8080` … `5050`) is at **`:86-90`**.
  Both anchors resolve to lines that exist and both name a construct the sentence explicitly describes and that is not
  there. §5 rule 34 (a repair moves line numbers, and line numbers are cited) — this is the correction note itself
  having rotted.
- **Confidence:** high

### B7 — `graphql-wundergraph.md:88`: `:193` does not say what it is cited for

- **Anchor:** `corpus/services/graphql-wundergraph.md:88` — *"The host-`5050` mapping was published here in the present
  tense while **`:193`** of this same doc already said `localhost:5050` refuses the connection"*
- **Why it is false:** `:193` is *"**⚠️ At `0dab54d` the `graphql` token appears in no `profiles:` key at all**, so asking
  for it does not"*. The `localhost:5050`-refuses statement is at **`:196`** (*"`http://localhost:5050` refuses the
  connection."*). Off by three onto a different claim.
  (Same sentence, `:89`, also describes `:174-176` as *"the compose line-number caveat"*; `:174-176` is the head of the
  **Upstream consumers** bullet and the caveat sentence is `:179-181` — booked as a MINOR below rather than doubled up
  here.)
- **Confidence:** high (the *materiality* is lower than B6's, which is my only hesitation — but the instrument lists
  wrong line anchors pointing at the wrong construct as a BLOCKER without a materiality escape, and a reader chasing the
  cross-reference lands on an unrelated warning)

### B8 — `ant-academy.md:45`: "delivered … offline on the Expo mobile bundle" — the bundler ships nothing, as this file says twice

- **Anchor:** `corpus/services/ant-academy.md:45` — *"Chapters are delivered online (… but **no longer offline on the
  web** …) **and offline on the Expo mobile bundle**."*
- **Why it is false:** measured @ `ant-academy` `22df69dd`, `mobile/scripts/bundle-content.ts:27-28` is
  `const REPO_ROOT = join(SCRIPT_DIR, '..', '..');` / `const CONTENT_DIR = join(REPO_ROOT, 'content');` — the **repo
  root**, not `code/public/`. `git ls-tree -r --name-only 22df69dd -- content/` → **0** entries (deleted at `8199ea5d`,
  2026-05-20, *"remove orphaned root content/ tree and empty content~HEAD* dirs"*), against **3,406** under
  `code/public/content/`. `findJsonFiles` does a bare `readdirSync(dir)` with no existence check and is called with
  `CONTENT_DIR` at `:53`, so `pnpm run dev:bundle` **ENOENTs and bundles nothing**.
  The same file states this **twice**: `:19-23` (*"⚠️ its bundler is inoperative at `22df69dd8` … so it ENOENTs and ships
  nothing"*, with the hedge *"**intended to** bundle"*) and `:451-459` (*"Two things a reader wrongly concluded: that the
  mobile artifact ships the live catalog…"*). The iter-129 repair reached the summary bullet and the deep-dive and left
  the Role-&-Responsibility sentence asserting delivery in the present tense, without the "intended to" hedge its own
  sibling carries. (`:49`, *"Bundle the same chapter JSON into the iOS / Android Expo app at build time"*, is the same
  predicate in a Key-Functions list — weaker, so I fold it in here rather than book it separately.)
- **Confidence:** medium — **I hesitated** because `:45` sits under *Primary Goal* and could be read as product design
  rather than measured behaviour. What decided it: the immediately adjacent bullet at `:19` **does** carry the
  "intended to" hedge for exactly this claim, so the unhedged form at `:45` is a distinguishable assertion, not a shared
  register. Booked per the instrument's "when in doubt, book it".

---

## Minors

- `ant-academy.md:10` — *"which then sat **469 lines below** an opening that still contradicted it"*: the Clerk bullet is
  at `:500`, the opening at `:5-15` → ~485–495 lines. Self-referential distance, now stale by line drift.
- `ant-academy.md:500` — *"This document contradicted itself **213 lines earlier**"*: the four passages it enumerates sit
  at `:276` ("public-storefront"), `:312` ("much wider than 'a few auth pages'"), `:330` (anonymous browsing) and `:331`
  ("Phase-1 public launch") — i.e. 169–224 lines earlier, and `:287` (=500−213) is inside the LanguageSelector
  paragraph. Same class as the line above.
- `ant-academy.md:190` — *"`code/public/catalog.json` (~2,667 entries)"*: measured at `22df69dd` the file's `courses`
  array holds **2,692** (`version`/`generatedAt`/`languages`/`courses`, 7 languages). The `~` covers it; noting for
  re-derivation only.
- `graphql-wundergraph.md:89` — *"(this cited `:174-176` until M257x iter-98 — that is the compose line-number caveat)"*:
  `:174-176` is the opening of the **Upstream consumers** bullet; the line-number caveat is `:179-181`. Same bullet,
  wrong lines.
- `ant-academy.md:459` — *"The script does a bare `readdirSync` with no existence check (`:38`, `:53`)"*: the
  `readdirSync` call is at `:40`; `:38` is the enclosing `function findJsonFiles(...)` declaration and `:53` the call
  site `findJsonFiles(CONTENT_DIR)`. Names the right constructs, one line short of the operative one.
- `messenger.md:193` — the `BREVO_KEY` row's middle column ("Value in the deleted compose block") reads *"(empty)"*, but
  `BREVO_KEY` was never in the `messenger` block at `0dab54d`; it arrived via `env_file: .env`. Non-material — the
  effective value is the one stated.
- `corpus/services/TEMPLATE.md` — pure scaffold, no gradeable claim. Its Upstream/Downstream convention (`:22-24`) is
  used consistently by both `academy-backend.md:113/116` and `graphql-wundergraph.md:174/182`. Clean.

---

## Positively cleared (audited zeros)

**ENUMERATED** (predicate re-derived from source independently, cardinalities compared):

- `ant-academy.md:322-339` — the complete `isPublic` matcher. I read `code/proxy.js:112-188` @ `22df69dd` end to end:
  **26 unconditional patterns + 2 conditional groups**. Every one of the doc's nine table rows matches, in both
  directions — no pattern in the matcher is missing from the table and no table entry is absent from the matcher.
  `VISUAL_BYPASS` is `BENCHMARK_VISUAL_BYPASS === "1" && NODE_ENV === "development"` (`:62-64`), whitelisted not
  blacklisted, as `:339` claims.
- `ant-academy.md:290-293` — the `TopBar` surface set. `git grep -n "<TopBar" 22df69dd -- 'code/*'` returns exactly the
  seven non-test mounts the doc names: `AcademyClient.jsx:1906` (rendered by `app/(authed)/page.jsx:151`,
  `courses/page.jsx:92`, `courses/[slug]/page.jsx:219` — all three verified), `CourseClient.jsx:2091`/`:2141`,
  `LatestClient.jsx:128`, `BookmarksClient.jsx:508`, `MyActivityClient.jsx:161`. `MyCertificatesClient.jsx` imports no
  `TopBar` (`grep` exit 1). `PublicHeader` has exactly one mount, `library/page.jsx:28`; `free/page.jsx:18` is
  `redirect('/?tier=free')`. `SUPPORTED_LOCALES` at `locale.js:10` is 7 entries; `LanguageSelector.jsx:88` `role="menu"`,
  `:97` `role="menuitemradio"`.
- `ant-academy.md:318` — **31** e2e spec files (`git ls-tree -r --name-only 22df69dd | grep -cE
  '^code/tests/e2e/[^/]*\.spec\.js$'` → 31; the naive pattern the doc warns about returns 38), **245** Vitest test files,
  and ~**2,714** `it(`/`test(` cases across them (doc says ~2,700). `playwright.config.js:13` is `testDir: './tests/e2e'`.
- `graphql-wundergraph.md:43-48` — the subgraph ladder. `grep -c '  - name:' supergraph-config-prod.yaml` at each named
  commit: `749dc86~1` → **5** (`backend, skiller, jobsimulation, cms, skillpath`), `749dc86` → **4**, `7c17e63` → **3**,
  `915da06` → **1**, `60c229f3` → **1**. Dates match. `git show --name-status 915da06` marks **both**
  `schemas/cms.graphqls` and `schemas/jobsimulation.graphqls` `D`, and its own subject line does say *"supergraph 2→1"* —
  so the doc's **3 → 1** correction and its "the subject line is wrong" note are both right.
- `graphql-wundergraph.md:15` — **five** profile tokens. Resolving compose's `include: common.yml`:
  `docker-compose.yml` @ `0c91421d` declares `sentinel`, `backend`, `studio-desk`, `next-web-app`, `gotenberg`;
  `common.yml` declares `postgresql`, `redis`. `profiles:` keys appear at `:110`, `:141`, `:168`, `:183` → union
  `{core, backend, all, studio-desk, frontend}` = **5**. `sentinel`/`postgresql`/`redis` carry no `profiles:` key →
  the always-on floor is exactly the three the doc names.
- `messenger.md:23` — **21** handlers / **22** `pubsub.EventHandler(…)` lines in `internal/flow/flow.go:72-104`
  @ `fa47850d`, the commented-out one being `OrgJobSimulationAssignmentPastDueHandler` at `:83` (`// … // not
  implemented`). Counted line by line. `:74-78` are the five `OrgSkillPath*` handlers, as `:129` claims.
- `messenger.md:26-33` — `MESSENGER_RPC_ADDR` occurs in **no** repo. Run per §5 rule 44: `find stack-demo -name .git
  -maxdepth 4` enumerates **15** git trees including the two nested `anthropos-studio-room` checkouts; each grepped at
  its **own** HEAD → 0/15. Positive control on the nested pair: `mistral` returns **22** lines in each of
  `app/studio` and `cms/studio` @ `aeec036`, matching §5 rule 44's own worked example, so those trees were genuinely
  searched. `git -C stack-demo/platform log -S 'MESSENGER_RPC' --oneline 0c91421d` → **0** over **121** commits;
  control `-S 'SKILLER_RPC'` → **7**; with `--all` → **0** and **8**, the 8th being `464dfe3` on a non-main branch.
  Every figure in that blockquote reproduces exactly.
- `messenger.md:36-37, :197, :200` — the `d11a403` re-point. At `d11a403^` the messenger block holds
  `CMS_RPC_ADDR=http://cms:8091`, `JOBSIMULATION_RPC_ADDR=http://jobsimulation:8401`,
  `BACKEND_USERS_RPC_ADDR=http://backend:8083`, `SKILLER_RPC_ADDR=http://backend:8083`; at `d11a403` the first two move
  to `backend:8083`. **Two moved, two already held it** — exactly as stated, against the more natural (and wrong)
  "all four" reading the doc explicitly forbids.
- `messenger.md:38-39, :183-187` — at `0dab54d` all four `_RPC_ADDR` lines (`:173`, `:174`, `:176`, `:183`) sit inside
  the `messenger:` block (`:156-196`) and nowhere else; at `0c91421d` `docker-compose.yml`, `common.yml` and
  `.env_example` each contain **0**. `AUTHORIZATION_ADDRESS=http://sentinel:8087` is the sole service address, at
  `docker-compose.yml:48`.
- `studio-desk.md:38-51` — the "NOT a GraphQL client" correction. `git grep -in graphql 41ee3575 -- 'src/*'` returns
  **exactly two** lines, both comments, at `src/routes/skillpath.ts:374` and `:405`; `git grep -n 8082 -- 'src/*'`
  returns **0** (positive control: `directus` returns 9 + 78 lines in the same paths). `src/index.ts` mounts exactly the
  four routers at `:150`, `:158`, `:161`, `:164`. `git grep -n "new GraphQLClient" 41ee3575` returns **exactly four**,
  all in `app/services/…` at the four line numbers the doc lists, all fed by `config.ts:6`.
- `studio-desk.md:34` — `vite.config.ts` `rollupOptions.input` holds exactly the nine prod entries named, plus
  `dev-accept` behind `isProduction ? {} : {…}`.
- `studio-desk.md:327-334` — **three** hardcoded `https://app.anthropos.work` sites in the scaffold:
  `pageWrapper.js:149` (logo link), `userProfile.js:148` (the "Back" menu item), `userProfile.js:302` (logout redirect).
  `git grep` over `app/core/scaffold/*` returns those three and nothing else.
- `studio-desk.md:293-299` — no DB driver. `package.json` @ `41ee3575` matches none of
  `pg|postgres|prisma|sqlite|mysql|mongo|knex|drizzle-orm|typeorm|sequelize` (grep exit 1).
- `academy-backend.md:73-85` — the eleven `academy_*` tables. `migrate/schema.go` @ `ad9f3c49` declares exactly
  `academy_bookmarks, academy_certificates, academy_chapter_bodies, academy_chapter_progresses, academy_chapter_times,
  academy_chapters, academy_feedbacks, academy_last_activities, academy_path_embeddings, academy_series,
  academy_skill_paths` — plural, matching the doc's warning, and the eleven singular Ent schema files exist alongside.
- `academy-backend.md:97-104` — every GraphQL query and mutation named is present in
  `internal/web/backend/graphql/graph/schemas/academy.graphqls` @ `ad9f3c49`, with `@public` exactly where the doc puts
  it (`academyCertificate` `:654`; the four catalog queries `:664`–`:689`).
- `ant-academy.md:77` / `academy-backend.md:94` — `academy.graphqls` is **1 of 43** `.graphqls` files in `app`
  (`ls-tree -r --name-only ad9f3c49 | grep -c '\.graphqls$'` → 43), at
  `internal/web/backend/graphql/graph/schemas/academy.graphqls`.
- `ant-academy.md:472` — rosetta ships **16** skills in `.claude/skills/`, none beginning `ant-`.
- `ant-academy.md:240-243` — the **five** bring-up patches. `demo-stack/ant-academy.sh` (717 lines, byte-identical in the
  pinned clone `09d06070` and the authoring copy `f2ea567b`) declares exactly five `*_PATCH` variables, at `:56`
  (dev-origins), `:66` (fs-published-fallback), `:75` (fs-published-chapter-body), `:84` (fs-published-public),
  `:90` (back-to-cockpit) — the five names and the five line numbers both correct. **Read in both trees.**
- `ant-academy.md:388` — `demo-stack/ant-academy.sh:576-583` is the M220 comment block stating the `e2e_persona` bypass
  is gone from the launch env; the launch `env` at `:584-586` sets neither `BENCHMARK_VISUAL_BYPASS` nor
  `NEXT_PUBLIC_E2E_AUTH`. `cockpit.py:812` (client-side `document.cookie`), `:1496` (`Set-Cookie` on the 302) and `:327`
  (naming both) all resolve. **Read in both rext trees; identical.**

**RE-DERIVED** (arithmetic the document itself displays, or a single anchor opened and confirmed — the weaker clearance):

- Every `file:line` in `ant-academy.md`'s write-path blockquote (`store.js:150/:162/:169/:202/:210/:215`,
  `writeThrough/index.js:247/:259`, `route.js:1-18`), the `emptyCatalogView` correction (`serverTenant.js:115-117`,
  `:111-113`, `:145` byte-exact, `catalog.js:961`/`:31 = '1.0'`, `AcademyClient.jsx:1363-1365`), the chapter-body chain
  (`serverChapterBody.js:52`, `:113-115`, `not-found.jsx:43`), fonts (`layout.jsx:1/:41/:51`, `academy.css:1`),
  PWA (`layout.jsx:132`, manifest `display: standalone`), org gate (`proxy.js:2-5/:73/:91/:298`), Cosmo
  (`agent.js:12/:13/:32`), storefront (`platformUrls.js:1-32`, `pricing.js:21`, `schema.js:3`,
  `knowledge/user-types.md`, repo `CLAUDE.md:11`), `next ^16.2.6` / `react ^19.2.5` / `reactCompiler: true` /
  `engines.node >=22` / `vercel.json = {"framework":"nextjs"}`, vendored FA (99 files), `.env.example:31` empty
  endpoint and `:66` sign-up at `app.anthropos.work`, `draftsEnabled()`, `RegisterServiceWorker.jsx` kill-switch
  (serwist absent from `package.json`), `ensure-clones.sh` phase d2, `autoverify.sh` assert *(f)* at port `3077+offset`.
- `studio-desk.md`: compose `:119`/`:135`/`:138-140`/`:141` @ `0c91421d`, the historical `:337-341` @ `2adcf71` (with
  `cms`) and `:204`/`:223-226` @ `0dab54d`; `.env.example` 131 lines with `:117/:118/:119/:120` exactly as described;
  `youtube.ts:43`; `terraform/main.tf:129`; `GCLOUD_SERVICE_ACCOUNT` read by no code in `src/`; `skillpath.ts` = 61,080
  bytes (largest route); AI chain `.env.example:56/:60` + in-code `'thinking_fast'` fallback (`ai/config.ts:182`) + tier
  models (`:20-38`); `engines.node >=24` + `node:24-alpine`; `PORT || 9100` (`src/index.ts:60`);
  `STUDIO_ACCESS_ROLES` + `checkEnterpriseAndAdmin` at `src/index.ts:96`/`:99`, redirect to `WEB_APP_URL` at `:119-120`;
  `main.ts:97`/`:206`/`:105`; `gen_injected_override.py` `frontend_lines()` + the two named regression tests +
  `run-studio-fcp.sh` (**both rext trees**).
- `graphql-wundergraph.md`: router `0.275.0`, `wgc 0.104.0`, `node:22.11-alpine`, `federation_version: =2.3.2`,
  `listen_addr 0.0.0.0:8080` / `graphql_path /graphql`, `package.json = {"name":"graphql-wundegraph"}`,
  `subgraphs.conf = BACKEND=v1.360.0` (single line), `schemas/` = `backend.graphqls` alone, config variants
  `compose|dev|prod` with **no** `-local`, `CLAUDE.md:85` still naming `-local.yaml`, `CLAUDE.md` §Version Tracking
  reading exactly as quoted, `ci/update-subgraph.sh:9` carrying **one** `gh release download -R anthropos-work/app`,
  `terraform/main.tf:20 service_desired_count = 1`, no `subscription:` block and **no `type Subscription`** in
  `backend.graphqls` (positive control: `type Query`/`type Mutation` found at `:4912`/`:4053`; the six `Subscription`
  substrings are `PlanSubscription`/`activeSubscription`/`stripeSubscription*`), `bba862f` reachable only from
  `remotes/origin/feat/use-web-socket`, `Makefile:10` = `PROFILE ?= core` @ `0c91421d` and `PROFILE ?= graphql` @
  `2adcf71`, `grep -c 5050 docker-compose.yml` = **0** (control: `8082` = 8), compose `:151`/`:160`,
  the historical `graphql` block at `2adcf71~1` (`ssh: ["default"]`, `5050:8080`, `depends_on` = backend +
  jobsimulation + cms + storage — the exact four `:183` names), `gqlauthz.go:186`, `backend.go:312-317` @ `ad9f3c498`,
  the two named `gqlauthz_test.go` tests.
- `messenger.md`: `rpcsrv.go:25-30`/`:29` (byte-identical at `fa47850d` and `e9421c68`), 0 non-test hits for
  `cron|Ticker|asynq|time.AfterFunc` @ `e9421c68` (control: `time.` matches), `brevo.go:288-310`, `console.go:71`,
  `whitelabel.go:17`, `assignments.go:489` and `:828`, `jobsimulations.go:140-151` (the 2 h / 12 h staleness guards),
  `cmd/root.go:63/:64/:107/:107-116/:118-142/:144/:147`, `REDIS_WORKER_INDEX` read nowhere, `SERVICE_NAME` absent from
  messenger's terraform and read at `main.go:16`, go.mod `go 1.25.0` / `brevo-go v1.1.3` / `liquid v1.8.1`,
  `terraform/main.tf:19 = 1` @ `fa47850d` with **no `:29` declaration** and `:29 = 0` with the `:27-28` rollback comment
  @ `e9421c68`, `repos.yml:21-23` @ `0dab54d` + absent @ `0c91421d`, `profiles: [messenger]` (dropped from `all`),
  ports `8200:8200`/`8201:8201`, `depends_on: backend`, `docker-compose.yml:84-92`, `app/main.go:15/:62/:63/:295/:296/
  :1416-1421/:1450/:1471/:1473` @ `ad9f3c49` with `git diff --stat 2035f9a4 ad9f3c49 -- main.go` **empty**,
  `app/main.go:1095`/`:1478-1484` @ `9d00a313`, `app/main.go:1205-1211` @ `b948604`, `env_guards.go:61` @ `ad9f3c49`
  and **absent** at `b948604f`.
- `academy-backend.md`: `git describe --tags ad9f3c49` → `v1.369.0-7-gad9f3c498` and `CHANGELOG.md:5` = *"## v1.369.0 -
  2026-08-05"*; `0e37771f` = PR #903, 2026-06-05, *"Academy backend v1.0 'ground truth'"*; `main.go:471-472` @
  `ad9f3c49` **is** the closing brace of the Bedrock-error branch plus a blank line (exactly as the doc's own correction
  says); `:516-517` bucket reads and `:524-525` `NewManager`/`NewPublicManager`;
  `cmd/academyImport/main.go:239-243` @ `9d00a313` (*"it therefore needs the BUCKET, not an RPC address"*) with the
  hard error at `:244-246`; all fifteen `internal/academy/*.go` files named; `academy_embedding_refresh` at
  `internal/worker/tasks/academy_embedding_refresh.go:10` with the "03:00" comment at `embeddings.go:108`;
  `ACADEMY_CONTENT_API_TOKEN` + constant-time shared-token middleware on the `/content/admin` group
  (`content_admin.go:35`); `GET /content/catalog.json` (`content.go:23`); `resolver_academy.go`; all six named test
  files.

---

## What I could not settle

- **`graphql-wundergraph.md:7`, `:27` and `messenger.md:59`** — every claim resting on `infrastructure` @ `13c248e6`
  (`terraform/production/services.tf:509-517`, `:521`, `:622`; the destroyed `module.wundergraph_euwest1` and
  `module.messenger_euwest1`). **`infrastructure` is not in the clone set**: `find stack-demo -name .git -maxdepth 4`
  enumerates fifteen trees and none is it. Settled only by cloning `anthropos-work/infrastructure` at that sha and
  reading those lines. I note that §5 rule 56 records the same substrate gap, so this is a known, disclosed limit rather
  than a hole I found.
- **`graphql-wundergraph.md:8`** — *"ARCHIVED on GitHub, 2026-07-30 (read-only)"*. A GitHub repo-metadata read
  (`gh repo view anthropos-work/graphql-wundergraph --json isArchived,archivedAt`); nothing in any local clone carries
  it. The sibling half of the row (*"not cloned by `make init`"*) I did verify: no `graphql-wundergraph` entry in
  `repos.yml` @ `0c91421d`.
- **`studio-desk.md:123`** — `bootstrap/index.js:81` inside the pinned `directus/directus:11.6.1` image. Third-party
  image, not in the clone set; would need `docker run --rm --entrypoint cat directus/directus:11.6.1
  /directus/dist/../bootstrap/index.js` or equivalent.
- **`ant-academy.md:104`** — *"a grid of 65 real cards"*, and `:426` / `:437` in `studio-desk.md` (the p95 timing
  numbers). Runtime measurements on a live demo; not derivable from any tree.
- **`ant-academy.md:15`** — the link to `iter-128/progress.md` under `knowledge/plan/**`. The briefing bars me from
  reading it (hard bar #1), so I can neither confirm the file exists nor that it says what the sentence attributes to it.
  Flagging only that a **published corpus page links into the audit plan directory**; whether that is intended is a
  question for the coordinator, not a finding I can grade.
- **`ant-academy.md:318`'s "~2,700 Vitest cases"** — I approximated by counting `^\s*(it|test)(\.each|.skip|…)?\s*\(`
  across the 245 files (2,714). A real count needs `npx vitest run --reporter=json`; the doc's own figure and my proxy
  agree to within 0.5 %, which is why I cleared it as RE-DERIVED and not ENUMERATED.
