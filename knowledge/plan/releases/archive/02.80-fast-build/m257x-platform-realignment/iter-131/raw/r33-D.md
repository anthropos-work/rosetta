# Seat D — reading #33/#34 (M257x iter-131)

**Ground truth actually used (re-derived at this seat's open, before any grading):**
`stack-demo/app` `ad9f3c49` (== `origin/main`, `git describe` → `v1.369.0-7-gad9f3c498`) ·
`stack-demo/platform` `0c91421d` · `stack-demo/next-web-app` `8297c684` · `stack-demo/sentinel` `f2c46190` ·
`stack-demo/storage` `4ce8ece5` · `stack-demo/messenger` `fa47850d` · `stack-demo/cms` `ca50c817` ·
`stack-demo/graphql-wundergraph` `60c229f3` · `stack-demo/roadrunner` `87d8d443` ·
`stack-demo/jobsimulation` `462343b0` · `stack-demo/studio-desk` `41ee3575` ·
`stack-demo/ant-academy` `22df69dd` (dirty tree — every read done via `git show 22df69dd:<path>`) ·
`stack-demo/app/studio` and `stack-demo/cms/studio` `aeec036a` (own checkouts) ·
`stack-demo/rosetta-extensions` `09d06070` (pinned consumption clone) ·
`.agentspace/rosetta-extensions` `f2ea567b` (authoring copy).
I read **no** `rosetta-extensions` tree for any finding below — none of my six files makes a tooling-behaviour
claim I graded. Protocol read in full first: `corpus/ops/platform-alignment.md` §5 (lines 259–1889).

## Positive controls

| file | `wc -l` | read in full? |
|---|---|---|
| `corpus/architecture/service_taxonomy.md` | 530 | yes |
| `corpus/architecture/security_compliance.md` | 441 | yes |
| `corpus/architecture/shared_libraries.md` | 353 | yes |
| `corpus/services/ai-labs.md` | 171 | yes |
| `corpus/services/customerio-sync.md` | 165 | yes |
| `corpus/services/intelligence.md` | 18 | yes |

Search-pipeline controls run in-pass (§5 rules 1/2): three of my derivations returned a *false* zero and
were repaired before any conclusion was drawn — (a) a `git grep -nE '\be\.(GET|…)\('` root-route
enumeration returned 0 while `e.GET("/api/schema.json"` demonstrably exists (`\b` unsupported by the
engine); (b) a `for c in …; do git show $c:repos.yml` loop returned 0 for all 10 revisions because **zsh
parsed `$c:r` as a history modifier** — `${c}:repos.yml` fixes it; (c) a `comm`-based schema derivation
silently emptied. Every "zero" below carries a stated positive control.

---

## Blockers

### B1 — the REST root-mount enumeration says SEVEN; measured EIGHT, and the missing one is an optional-auth route

- **Anchor:** `corpus/architecture/security_compliance.md:250` — *"But `app` also mounts **seven routes
  directly on the root `e`, inside no group at all**, so no group-level middleware statement reaches
  them:"*, its table at `:253-261`, and the restatements at `:265` (*"11 groups + 7 ungrouped root
  mounts"*) and `:293-294` (*"seven further routes are mounted on the root `e` inside no group, two of
  them open by design"*).
- **Why it is false:** enumerated independently from source at `app` `ad9f3c49` (the ref the passage
  itself names, `ad9f3c498`), with a working positive control:

  ```
  git grep -nE '(^|[^.a-zA-Z])e\.(GET|POST|PUT|DELETE|PATCH|Any|Add|File|Static)\(' ad9f3c49 -- '*.go' | grep -v _test.go
  ```

  Excluding the standalone dev binary `cmd/labsdemo/main.go` (a separate `main`, not the app server),
  the root-mounted routes on the one server Echo instance are **eight**:

  | # | site | in the doc's table? |
  |---|---|---|
  | 1 | `internal/aireadiness/notifications/handlers.go:41` → `/ai-readiness/unsubscribe/:token` | yes |
  | 2 | `internal/web/backend/backend.go:117` → `/api/schema.json` | yes |
  | 3 | `internal/web/backend/backend.go:309` → `/api/*` (dev only) | yes |
  | 4 | `internal/web/backend/backend.go:315` → `/graphql` (dev only) | yes |
  | 5 | `internal/web/backend/backend.go:317` → `/graphql/query` | yes |
  | 6 | `internal/web/backend/backend.go:324` → `/api/webhook/directus` | yes |
  | 7 | `internal/web/backend/content.go:23` → `/content/catalog.json` | yes |
  | **8** | **`internal/web/backend/labs_admin.go:40` → `GET /v1/labs/:slug/workspace.tar.gz`** | **NO** |

  Row 8 is on the same root instance: `backend.go:301` calls `AttachLabsAdminRoutes(e, …)`, and that `e`
  is `srv.e` (`internal/web/web.go:124` `backend.Attach(srv.e, …)`). It is **deliberately outside** the
  `/v1/labs` group the doc's own row at `:212` describes — `labs_admin.go:31` declares
  `g := e.Group("/v1/labs", apiKeyAuthMiddleware(apiKeys, "labs:write"))`, and `:36-39` states:
  *"Serve is OUTSIDE the write group — it has OPTIONAL auth (a public Lab's workspace is served to
  anyone; a tenant-private Lab requires a key with access)."*

  So the doc (i) undercounts the ungrouped surface 7→8, (ii) says *"two of them open by design"* when a
  third serves public content to anyone, and (iii) its `/v1/labs` row asserts a *group-level* API-key +
  scope gate that this route does not carry. This is precisely the §5-rule-57 class the paragraph was
  written to close (*"the widest instrument that can see the population is rarely the one you widened to
  last time"*), one level further out: the run-82 repair widened *groups → routes* but enumerated the
  routes by hand rather than by search.
- **Confidence:** high.

### B2 — `shared_libraries.md`'s analytics-go wiring anchor names a different construct at every readable ref

- **Anchor:** `corpus/architecture/shared_libraries.md:77` (the `analytics-go` row) — *"…the load-bearing
  consumer is **Stripe subscription lifecycle → Brevo**, `app/internal/payments/handler.go:302-316`
  (seven event names switched off `entSub.Status`), **wired `main.go:507-508`**."*
- **Why it is false:** the cell names no ref of its own, so under the briefing's rule (and §5 rule 33 —
  *a pin's scope is the claim's own block, a markdown CELL in a table*) it grades at the checkout.
  - At `ad9f3c49`, `main.go:507-508` reads
    `// public clients, cmsStorage and jobsimwiring.Wire). Per-namespace clients are` /
    `// derived at each consumer via internalstorage.NewClient / NewPublicClient.` — the middle of the
    **storage-in-app** comment block (`:503-512`). Nothing to do with analytics/tracking.
  - The real wiring is `main.go:494` `trackingManager := tracking.New(os.Getenv("BREVO_KEY"))` and
    `main.go:495` `paymentsManager := payments.New(…, trackingManager)`.
  - At the other ref this file uses, `b948604f`, `main.go:507-508` is a **jobsim-in-app** comment about
    users/orgs RPC handlers; the wiring there is `main.go:437`.
  The rest of the cell verifies exactly: `app/go.mod:14` `analytics-go v0.3.1` ✓; `handler.go:302-316`
  is the `analyticsManager.Track(analytics.Event{…})` call ✓; the switch at `:283-300` is on
  `entSub.Status` with **exactly seven** cases ✓ (I enumerated them). Only the `main.go` anchor is wrong.
- **Caveat, stated rather than hidden:** the file's banner (`:6`) says the module block was measured at
  `app` `3eaadae6` (v1.371.1) — a ref **not reachable in this reading's clone set** (see *What I could
  not settle*). The anchor may well resolve there. Under §5 rule 49 I cannot refute a measurement of a
  moving surface with my own snapshot; what I can state is that it resolves at neither ref a reader with
  this ground truth can open. The identical `main.go:507-508` anchor also stands in root `CLAUDE.md`'s
  Shared Libraries block (out of my file set, and in no seat's set).
- **Confidence:** medium.

### B3 — the `clerk-integration.md:40` cross-reference lands on an unrelated bullet

- **Anchor:** `corpus/architecture/security_compliance.md:156` — *"The same class as the
  `clerk-integration.md:40` *"only"* and the `cms.md` `bash -c` inversion…"*
- **Why it is false:** `corpus/services/clerk-integration.md:40` is
  `- **Org invitations** — backend create/revoke (+ a hand-rolled bulk call); frontend list/accept.`
  It contains no *"only"* and makes no quantifier claim. The defect being cited lives at **`:44`**
  (*"**See the enumeration below — this bullet used to say "only", and it was false.**"*), with its
  correction box at **`:64`** (*"CORRECTED M257x iter-120 — this list said sign-in tokens are minted
  "only" for app-native admin…"*). Derived by `grep -n '"only"\|used to say' corpus/services/clerk-integration.md`
  over the whole 210-line file — three hits, at `:44`, `:64`, `:94`, none at `:40`.
  This is the *anchor resolves but names the wrong construct* class (§5 rule 21's second bucket, and the
  one `anchor_construct_guard` is documented as unable to see because it lands on prose, `:1584`).
- **Why I hesitated:** the citation is illustrative (a precedent for a defect class), not load-bearing on
  a platform fact. I book it because the briefing enumerates wrong-construct line anchors as BLOCKERs and
  because a reader checking the cited precedent finds nothing there.
- **Confidence:** high.

---

## Minors

- `corpus/architecture/security_compliance.md:249-250` — the stated derivation
  `git grep -nE '\.Group\("' ad9f3c498 -- '*.go' | grep -v _test.go` is said to *"return exactly those
  11"*. Run verbatim it returns **12** lines; the 12th is `internal/web/backend/emailpreview/handler.go:6`,
  a **comment** (`// runtime (they're oapi-registered under apiRouter = e.Group("/api", ...)),`). The
  substantive count of **11** real groups is correct and I enumerated it. Recorded because §5 rule 24 is
  explicit that *a grep over source counts commented-out code*, and this recipe is published as a
  re-derivation aid.
- `corpus/architecture/shared_libraries.md:101` — *"`sentinel`'s `88036d7` … took it `v0.34.3 → v0.35.2`,
  **two commits past** the `88bc5592` this row used to cite."* Measured: `git rev-list --count
  88bc5592..88036d7` = **1**. (It is the *checkout* `f2c46190` that is two past `88bc5592`.) Everything
  else in the sentence verifies: `88036d7` is *"chore(deps): update dependencies to latest versions"*,
  and it moved `sentinel/go.mod:8` `v0.34.3 → v0.35.2` and `:9` `v1.200.0 → v1.210.0`.
- `corpus/architecture/shared_libraries.md:76` — *"`STORAGE_RPC_ADDR` occurs in **zero** Go source."* It
  occurs in **three** `.go` files at `ad9f3c49` — `internal/jobsimwiring/wiring.go:101`,
  `internal/storagens/callsites_test.go:189`, `main.go:504` — all three in comments saying the variable is
  gone. The load-bearing claim (*no SDK RPC client is constructed*; nothing **reads** it) is TRUE and I
  verified it. Root `CLAUDE.md` carries the identical sentence, so a repair should sweep both. The
  accurate form already exists in the corpus: *"set nowhere **and** read nowhere"*.
- `corpus/services/ai-labs.md:148` — `cmd/labsdemo` described as *"standalone local Labs-catalog server on
  `:8099`, **in-memory store**, dev only"*. Measured (`cmd/labsdemo/main.go:1-12`): it **connects to a
  local Postgres** and creates the `labs`/`lab_bodies`/`api_keys` tables; only the **workspace** store is
  in-memory. A developer reading the doc would not expect the Postgres prerequisite. `:8099` ✓
  (`main.go:115` `envOr("LABSDEMO_ADDR", ":8099")`).

---

## Positively cleared (audited zeros)

Everything below is **ENUMERATED** unless marked otherwise — I derived the predicate's members from
source independently and compared cardinalities, rather than re-computing the arithmetic the document
displays (§5 rule 24).

### `service_taxonomy.md`
- **ENUMERATED — the compose topology.** `docker-compose.yml` @ `0c91421d` declares exactly **five**
  services (`sentinel`, `backend`, `studio-desk`, `next-web-app`, `gotenberg`); `include: common.yml`
  adds `postgresql` + `redis` → **seven** effective; `core` starts **five** (`backend`, `gotenberg` +
  the floor). Exactly **four** carry a `profiles:` key, at exactly the cited lines: `backend` `:110`,
  `studio-desk` `:141`, `next-web-app` `:168`, `gotenberg` `:183`; `sentinel` (`:5`) and both
  `common.yml` services (`:2`, `:24`) carry none. Legal profile-token set = `{all, backend, core,
  frontend, studio-desk}`. `Makefile:10` `PROFILE ?= core`.
- **ENUMERATED — `repos.yml`.** Four entries; `app` `:14-17` (`migrations: true`, `schema: public`),
  `sentinel` `:18-20` (`migrations: false`), and `docker-compose.yml:18` carries
  `search_path=sentinel` — the Trap-A case, exactly as described.
- **ENUMERATED — the deleted support tier.** At `0dab54d`: `storage` ports `8300:8300`/`8301:8301`
  (`profiles: [storage-legacy]`), `customerio-sync` `8080:8080` (`profiles: [customerio-sync, all]`,
  `:154`), `messenger` `8200`/`8201`. `838d907` deletes all three. `:53` `DIRECTUS_PUBLIC_BASE_ADDR`,
  `:56` `ENVIRONMENT=development` verified at BOTH `0dab54d` and `0c91421`.
- **ENUMERATED — the `d11a403` RPC re-point.** That commit changed exactly **two** values on the
  `messenger` block (`CMS_RPC_ADDR` `http://cms:8091`→`http://backend:8083`, `JOBSIMULATION_RPC_ADDR`
  `http://jobsimulation:8401`→`http://backend:8083`); at `d11a403^` the other two already read
  `http://backend:8083`; `BACKEND_USERS_RPC_ADDR` was introduced at `3e85fce` as
  `http://backend:8081` (port move only). The file's own correction of the agentive form is right.
- **ENUMERATED — `0c91421` service addresses.** `AUTHORIZATION_ADDRESS=http://sentinel:8087` `:48`;
  `GOTENBERG_URL` `:57`; `JUDGE0_BASE_URL` `:59`; **zero** `*_RPC_ADDR` (control: `CMS_RPC_ADDR` = 2
  hits at `0dab54d`, 0 at `0c91421`). `app/internal/converter/gotenberg.go:31` is the gotenberg call.
- **ENUMERATED — the router.** `graphql-wundergraph` `60c229f3` holds one schema file
  (`schemas/backend.graphqls`) and all three supergraph configs carry a single `- name: backend`
  (`prod:5`, `dev:7`, `compose:9`). Ladder re-derived from history: `749dc86^`=5 → `749dc86`=4 →
  `7c17e63`=3 → `915da06`=1, and `915da06^` lists `backend, jobsimulation, cms` — so **3→1** is right
  and the commit's own *"2→1"* subject is wrong, as the corpus says. `terraform/locals.tf:8` `port =
  8080`, `terraform/main.tf:48-49` container/host 8080, `config.prod.yaml:5` `listen_addr 0.0.0.0:8080`,
  `terraform/main.tf:20` `service_desired_count = 1`, `:11` `base_service`, `:13` `use_fargate = false`.
- **ENUMERATED — studio.** `studio-desk/.env.example:44/:45` @ `41ee3575` exactly as quoted.
  `studioTask` `cms_queries.graphqls:106`, `studioTasks` `:107`, `archiveStudioTask`
  **`cms_mutations.graphqls:22`** — the file-not-line correction is right.
  `internal/cms/studio/studioManager.go:119` runs `studio/gen.py` via `runCommand`, whose contract at
  `:1096-1098` says *"NEVER through a shell"*. `studio/CLAUDE.md:12-14` is the `gen.py` entry point.
  studio-room: `services/ai.py:705-708` is the whole provider registry (`openai`/`azure`/`anthropic`),
  `ai.py:1-2` imports only `openai`/`anthropic`, `tools/pdf2md.py:24` is `from mistralai import Mistral`,
  and `git -C stack-demo/app/studio grep -ci mistral aeec036a` sums to **22 lines / 3 files** exactly.
  studio-desk: **0** react/vue/angular entries in `package.json`, **0** `.tsx`/`.jsx` files, **7**
  language flags (`app/public/languages/flag-{france,germany,italy,japan,netherlands,spain,united-states}.svg`),
  Copilot on `gpt-5.2`/`gpt-5-mini` (`src/services/ai/config.ts`).
- **ENUMERATED — Ant Academy.** `code/proxy.js:298` is *"// Organization-membership gate. Any membership
  (active or not) passes."*; `:293-329` contains sign-in + the `REQUIRE_ORGANIZATION_MEMBERSHIP`-gated
  org redirect and **no** email/domain predicate. `next ^16.2.6` / `react ^19.2.5`; Expo `~54.0.33`;
  mobile port 8555; `NEXT_PUBLIC_FEATURE_TRAINING_COACH` empty in `.env.example:85` (default off);
  `graphql/server.js:14` reads the endpoint and `:18` throws; `backendContent.js:36`/`:102-103`;
  `serverTenant.js:115-145` carries *"there is NO FS-as-published fallback"* and *"not
  reversible-on-error"*, `:145` is the DB-authoritative read; beacon `route.js:36`/`:41-55`;
  `layout.jsx:132` `manifest: "/academy-manifest.json"`; `next-scaffold.test.js:106,111` and
  `react-compiler-config.test.js:41` are the serwist-removal fences; **0** serwist/workbox deps.
  `academy.graphqls` is 1 of **43** files in the schemas dir.
- **ENUMERATED — libraries.** `app`'s org-private requires at `ad9f3c49` are `analytics-go :14`,
  `colony :15`, `proto :16`, `storage :17`, `taxonomy :18`; **no `ai`, no `authn`**; `internal/ai/`
  exists in-tree; `1e457fa70` is *"refactor(ai): fold the ai library into app as internal/ai"*
  (2026-08-04). `ai v1.40.2` survives only in `cms/go.mod:9` and `jobsimulation/go.mod:11`. `authn`:
  **0/7** Go clones (control: `colony` 7/7).
- **ENUMERATED — the jobsimulation M810 row.** Graded at the ref the cell names: at `6092c6d2`,
  `terraform/main.tf:15-40` is exactly the decommission comment naming the LiveKit/Chime buckets, the
  SSM parameters and the atlas tracker. (`6092c6d2` is an ancestor of `origin/main` but **not** of the
  checkout `462343b0` — the pin is what makes the cell gradeable, and it holds.)
- **RE-DERIVED** — the `customerio-sync`-never-ran-locally argument (`env_guards.go:37-44`
  `deployedEnvironment`, `:92-111` `resolveSubsystemSwitch`, `main.go:394` `if customerIOSyncEnabled`,
  and `3e5bc33ef:main.go:387`'s pre-switch `deployedEnvironment() && BREVO_KEY != ""`). All four anchors
  land on the exact constructs claimed.

### `security_compliance.md`
- **ENUMERATED — the Layer-1 multi-tenancy fence, in full, set-first (this is the fence §5 rule 24 says
  has been wrong three generations running).** `internal/data/ent/schema/` @ `ad9f3c49`: **139** `.go`
  files, **135** declare `ent.Schema` (the 4 that do not: `database_types.go`, `mixin.go`,
  `skiller_mixins.go`, `skillpath_mixins.go`). `OrganizationMixin{}` appears in **30** schema files and
  **`user_resource.go:22` is `// OrganizationMixin{},  // We need to work on this`** — commented, so
  the live set is **29**. `OrganizationIDMixin{}` in exactly **7** (`category`, `jobrole`, `similarity`,
  `skill`, `specialization`, `studio_document`, `studio_task`). Files with neither mixin **and** a
  `"organization_id"` field: **18** — and the 16 the document names are *exactly* my 18 minus
  `academy_feedback.go` and `org_membership.go`, name for name. `func … Policy() ent.Policy` is declared
  in exactly **four** files (`mixin.go:99` UserMixin, `mixin.go:126` OrganizationMixin,
  `org_membership.go:172` Membership, `organization.go:56` Organization, `user.go:116` User — four files,
  five declarations), identical at the pinned `5ba17044`. So **31 org-filtering** (29 + Membership +
  Organization) and **23 unpoliced-with-`organization_id`** (16 + 7) both hold, and `135 − 31 − 23 = 81`.
  The four tenant-scoped remainder members all resolve: `lab.go:63`, `academy_chapter.go:84`,
  `academy_skill_path.go:78` (`field.String("tenant_eid")`), `skill_path_session.go:43`
  (`field.UUID("tenant_id", …)`, and it does carry `UserMixin{}` at `:26`);
  `academy_feedback.go:64` `UserMixin{}` and `:129` `organization_id`. Platform self-statements verified
  verbatim: `job_simulation_session.go:5`, `jobrole.go:18`, `category.go:15`.
- **ENUMERATED — the GraphQL viewer gate.** `gqlauthz.go:149` is the *"gates every operation on a
  viewer"* doc comment; the single Sentinel call is `OrgCheckUserPermission` at `:222`; and all six
  early-return paths land exactly where the table says (`:160-161`, `:174-178`, `:190-191`, `:196-197`,
  `:202-203`, `:209-219`). `gqlauthz/target.go:11` is `grapqlTargetVar = "userId"`.
  `AuthzMiddleware|EchoAuthzMiddleware` → **0** hits repo-wide (control: `.Group("` → 12 lines).
  `resolver_skiller_taxonomy_authz.go:53-66` carries the quoted post-mortem verbatim.
- **ENUMERATED — the eleven groups**, each at its cited declaration and mount:
  `/api` `backend.go:121-141` with exactly **10** skipped paths (8 webhooks + `/api/skills` +
  `/api/health`); `/ask` `:171-173` (+ `:183-185` per-handler `requireAdmin` note);
  `/assignment-builder` `:194-196`; `/admin/backfill` `:210-212` (+ `:207-209`); `/coursebuilder`
  `:229-232`; `/credits` `:273-276`; `/api/invitations` `internal/invitations/handlers.go:31` mounted
  `web.go:148`; `/content/admin` `content_admin.go:35` mounted `backend.go:289`; `/v1/labs`
  `labs_admin.go:31` mounted `backend.go:301`; `/academy/embeddings`
  `academy_embeddings_admin.go:41` mounted `backend.go:295`; `/api/workforce`
  `emailpreview/handler.go:66` mounted `web.go:162`. `cbGate` `backend.go:227`, defined
  `gate.go:27-49`, is a real Sentinel-backed group middleware (`OrgCheckFeaturePermission(
  OrgFeatureMembersEdit, orgID)`), on exactly two groups.
- **ENUMERATED — the run-82 `/api/invitations` correction.** `web.go:145-146` is the cross-origin
  rationale; `handlers.go:31-33` mounts `GET /:token` + `POST /:token/opt-out`; `token.go:29-34` is
  `base64url(HMAC-SHA256(email|org_id|invited_at, SECRET))`; `invite.go:154-155` and `:187-188` both
  state *"token possession is the authorization"*; `:157`/`:190` are the `privacy.Allow` bypasses;
  `:159`/`:194` the `Token(token)` filters; `main.go:423-427` the boot refusal; `token.go:38`
  `ValidateToken` with **8** repo-wide hits confined to `token.go` + `token_test.go`; the token-logging
  sites `handlers.go:59,62,99` and `invite.go:203,218` all present. The `/ai-readiness/unsubscribe`
  contrast holds: `notifications/handlers.go:41` mount, `:71-81` the 401 paths, `:156` the verifier.
- **ENUMERATED — the network-placement retraction.** Across **all eight** service terraform trees in the
  clone set the token `public_subnet` occurs **0** times and every one passes
  `private_subnets_ids = var.platform_private_subnets_ids` (router at
  `graphql-wundergraph@60c229f3:terraform/main.tf:31`).
- **ENUMERATED — AI residency.** `jobsimulation/ai/ai.go:85-88` is the Bedrock `eu-west-1` config and
  `:85-95` the Anthropic client; `:262-266` the Azure-EU default inside `getClient` (`:259`);
  `:263-277` the `flag_use_azure_us` swap; `isThrottlingError` `:129` used at `:166`/`:325`, with
  `ChatCompletion`'s `// override vendor to openai if we have throttled` at `:152-155` — so *retry
  target, not fallback rung* is exact. `coursebuilder/bedrock.go:109-112` @ the pinned `2035f9a`
  resolves byte-identically (and at the checkout): `newUnderlyingClient` → `NewAnthropicClientWithModel`;
  `ModelBackendName()` at `:98-103` returns `"anthropic-api"`. Intra-corpus targets checked and correct:
  `external_services.md:579` ("no ordered EU-first fallback chain"), `:602-607` (the four-live-plus-one
  enumeration), `:567` (the Anthropic-Direct provider row), `coursebuilder.md:48` ("the shipped path").
- **ENUMERATED — the EU-AI-Act premise.** `v3/validator/skills.go:53-62` counts booleans and `:75`
  divides; the real dispatch is the hardcoded switch `basevalidator/criterion.go:126-127`
  (`case check.EngineLlm:`) → `:428` `NewLLMBulkChecker`, sending
  `templates/checkValidationBulk.tmpl`, whose `:27` reads *"assess whether the `<asset>` … meets or does
  not meet the … checks"*. `checkerEngines` is registered once (`v3/validator/validator.go:60-61`) and
  stored-not-read on `CriterionValidator` (`criterion.go:34,72,90`). The *"most, not all"* hedge is
  earned: `criterion.go:142` `case check.EngineTextDiff:` dispatches `:168` → `validateCodeDiff`
  `:450-475`, a pure string comparison.
- **ENUMERATED — Layer 3.** `useOrgSelection.tsx:94` (`apps/web` and `apps/hiring`),
  `useResolveActiveOrg.tsx:107`, `useActivateMembershipOrg.tsx:81` — all six files, all
  `clerk.setActive({ organization … })`, at next-web-app checkout `8297c684`.
- **ENUMERATED — the db-backup retraction is corpus-wide consistent.** I grepped `corpus/**` and
  `CLAUDE.md` for the retracted *"every 6 hours / Azure / three geographies"* predicate: every surviving
  instance is inside an explicit retraction. Root `CLAUDE.md:250` carries the **corrected** form. (Note
  for the coordinator: the CLAUDE.md snapshot embedded in this session's system context is **stale** —
  it still reads *"db-backup: Scheduled PostgreSQL backups (every 6h) to S3, Azure, Hetzner"*. The file
  on disk does not. I verified with `grep -n "Scheduled PostgreSQL backups" CLAUDE.md` → no match,
  control `grep -c db-backup` → 1.)

### `shared_libraries.md`
- **ENUMERATED — the 7-repo ratios.** colony **7/7**, proto **7/7**, taxonomy **6/7** (all but
  roadrunner), ai **2/7** (cms + jobsimulation only), authn **0/7** — derived from all seven Go clones'
  `go.mod` at their own HEADs, with every cited line number correct (`app:15/:16/:18`,
  `sentinel:8/:9/:21`, `storage:7/:8/:25`, `messenger:7/:8/:9`, `cms:9/:10/:11/:13`,
  `jobsimulation:11/:12/:15`, `roadrunner:7/:8`). `app/go.sum:64-73` is exactly the five modules, two
  lines each, no `ai`/`authn`/`messenger`. At `b948604f` it was **seven** with `ai :14` and
  `messenger :17` and `taxonomy :20` — exactly as the banner and the taxonomy row say.
- **ENUMERATED — `repos.yml`'s whole history.** 10 commits touched it; the union of `type: go` names is
  **11** (app, cms, jobsimulation, skiller, skillpath, chronos, sentinel, intelligence, storage,
  messenger, roadrunner); all four of skiller/skillpath/chronos/intelligence are present in the first
  revision `a2a3ee6`; and skillpath is indeed the last of the four to go, dropped at `a4db680`
  (2026-07-21, after skiller at `21429b7`, 2026-07-07).
- **ENUMERATED — the six Connect handlers** @ the pinned `b948604`: `mux.Handle` occurs exactly six
  times in `main.go`, at `:1187`, `:1188`, `:1196`, `:1204`, `:1213` (inside `if cmsRPCServer != nil` at
  `:1212`), `:1228` — no `SkillPathSessionService`, no RoadRunner. `ROADRUNNER_RPC_ADDR` → **0**
  occurrences at `app 9d00a313` (control: `JUDGE0_BASE_URL` → 1) and **0** at `platform 0dab54d`
  (control: `CMS_RPC_ADDR` → 2). `jobsimwiring/wiring.go:123` @ `9d00a313` is
  `jsrunner.NewRunnerManager(…)`; `skillpaths/skillpaths.go:27-31` carries *"the drop-in for the removed
  skillpath RPC client"*.
- **ENUMERATED — the two real vendor-selection sites.** `jobsimulation/ai/ai.go` `getClient` `:259`,
  flag `:267` and `:344`; `skillerai/ai.go` `getClient` `:332`, flag `:347`, `isThrottlingError` `:128`
  used at `:176`. All five anchors exact.
- **ENUMERATED — the storage shim.** `sdk/storage` imports: **32 files / 36 import lines** exactly;
  `internal/storage/service.go:48-56` holds both `sdkstorage.Client{V1: NewService(…)}` (`:48-50`) and
  the public twin (`:54-56`).
- **ENUMERATED — the `ai` one-way door.** `internal/ai/module_import_guard_test.go:15-17` and
  `app/CLAUDE.md:289-294` both say what the doc quotes, and CLAUDE.md pins `ai v1.40.1` for
  rosetta-extensions/stack-seeding.
- **ENUMERATED — the storage repo's liveness.** `origin/main` `9f8cb53` dated **2026-08-05**; newest tag
  **`v0.15.8`**, exactly **six** tags past `app`'s `v0.15.2` pin.
- Intra-corpus targets checked: `storage.md:129` is `### Connect-RPC (StorageService)` ✓;
  `external_services.md:554` does carry the `app/go.mod:14-18` enumeration ✓.

### `ai-labs.md`
- **ENUMERATED** — `checkout.session.completed` → **0** in Go source (control: `customer.subscription`
  → 5 hits); `internal/labs/` imports `internal/credits` in **0** files (control: 10 importing files
  listed, none under `internal/labs`); `credits/handler.go:12` states the Wave-13 removal.
  The Stripe webhook switch (`api.go:317-334`) has exactly two top-level cases —
  `customer.created` and `customer.subscription.{created,updated,deleted}`.
- **ENUMERATED** — `s3_workspace_store.go:19`/`:45` are the workspace-tarball path and key, and the
  words *grader*/*solution* occur **0** times in that file (control: 4 in
  `schema/lab.go`); `schema/lab.go:113-116` is the `{id,url}` asset-refs block ending *"solution is
  PRIVATE — never served to learners."*
- **ENUMERATED — the credit cost model, from the map not the comment.** `creditCost` (`cost.go:86-89`):
  `course.build` **5**, `course.refine` **1** (`// D1 ruling: flat 1 credit per refine turn`),
  `course.translate` **1** — the doc is right and the package's own doc comment at `cost.go:29`
  (*"5 credits per refine turn"*) is the stale one. `MarginMarkup = 1.40` `:133`,
  `PricePerCreditUSD = 0.45` `:142`, `DefaultSeedBalance = 500` `:231`, packages 50/200/500. All five
  cited migrations exist under `terraform/migrations/`. `lab_session.go` declares no `PrimaryKeyMixin`
  (`:21-28`) and `field.String("id")` (`:36`). `labsapi/client.go:6` default `:7070`, token read at
  `main.go:855`. `cmd/labsimport|labskey|labsdemo` all exist. `git describe --tags ad9f3c49` →
  `v1.369.0-7-gad9f3c498`, exactly as the doc states.

### `customerio-sync.md`
- **ENUMERATED** — `internal/customeriosync/doc.go:4-5` carries *"the last of the Go services to be
  folded into app"* across the line break; `env_guards.go:62` is `envCustomerIOSyncEnabled`;
  `main.go:286` the resolve; `main.go:284` is only the pointing comment (the anchor correction is right);
  `main.go:295` the condition and `:296` the `log.Fatalf`; `env_guards.go:87` `mustSubsystemSwitch`'s
  `log.Fatalf` and `:98-104` the deployed-unset error; `main.go:395` the `BREVO_KEY` construction;
  `main.go:393-396` byte-identical at the pinned `2035f9a` and at `ad9f3c49`;
  `docker-compose.yml:84-92` @ `0c91421` is the deliberate no-value block. `cmd/customerio-resync` with
  `--dry-run` at `main.go:33`.
- **ENUMERATED — the 18 git-URL build contexts.** Derived over all **80** commits touching
  `docker-compose.yml`: exactly the 18 the doc lists, name for name. `realtime`'s git URL disappears at
  `c17cc9a` (2026-04-15) as claimed. At `838d907^` there are **7** `context:` lines and
  `customerio-sync` is the only git URL among them.
- **ENUMERATED** — `external_services.md`'s per-service `##` sections are exactly Clerk, Directus, the
  Cosmo router, AI Providers, LiveKit, AWS Chime, and `brevo` occurs **0** times in that file **at
  current corpus HEAD**, not merely at iter-102.

### `intelligence.md`
- **ENUMERATED** — `fdfa189` is *"chore: remove intelligence service from local dev orchestration"* and
  its diffstat is exactly `docker-compose.yml | 25 ---` + `repos.yml | 3 ---`; `intelligence` is present
  in `repos.yml` at `a2a3ee6` and absent from `fdfa189` onward; it is in no compose file at `0c91421`.

---

## What I could not settle

1. **`shared_libraries.md:6` (and root `CLAUDE.md`) cite `app` `3eaadae6` (v1.371.1) — a ref that does
   not exist in this reading's clone set.** `git cat-file -t 3eaadae6` → *"Not a valid object name"*;
   `git rev-list --all | grep -c '^3eaadae'` → 0; `v1.371.1` is not a tag; the newest tag is
   **`v1.369.0`** and `HEAD == origin/main == ad9f3c49` (`v1.369.0-7`). **Every value the banner asserts
   is byte-true at `ad9f3c49`** — `go.mod:14-18` = analytics-go/colony/proto/storage/taxonomy at
   v0.3.1/v0.35.2/v1.210.0/v0.15.2/v1.2.0, all direct, and the single `replace` at `:295` is
   `getsentry/sentry-go/echo`. I did **not** book it: §5 rule 49 forbids refuting another observer's
   report of a concurrently-writable surface (`origin/main`) with my own — here *older* — snapshot, and
   the briefing's ADDENDUM freezes the clones rather than the platform. What would settle it: one
   `git ls-remote origin refs/tags/v1.371.1` **between** readings, recorded in the next ground-truth
   sheet. If the ref is real, B2's anchor may resolve there and should be re-graded; if it is not, the
   banner and CLAUDE.md's Shared Libraries block both need a re-pin. Flagging it because the same ref
   now carries two documents' most emphatic correction, and nothing in either is re-checkable from the
   ground truth I was given (§5 rule 50).
2. **`service_taxonomy.md:305` attributes the browser-direct OpenAI call to "Cosmo".** The mechanism is
   real and exactly as described — `ant-academy` `CLAUDE.md:255` @ `22df69dd`: *"`AgentRunner` calls the
   OpenAI Responses API (`gpt-5.2`) **directly from the browser** using a key from
   `localStorage('openai_api_key')`. It does NOT use the `app/api/ai/chat/route.js` proxy."* But the
   repo attributes it to **`AgentRunner`**, while "Cosmo" is the `NEXT_PUBLIC_FEATURE_TRAINING_COACH`
   surface that has its own `code/app/api/ai/chat/route.js` proxy — which the same sentence says
   `AgentRunner` bypasses. Whether they are one subsystem or two is not decidable from the strings; I
   found no `gpt-5.2` or `/v1/responses` reference under `code/src` or `code/app` outside test fixtures.
   Reading the Cosmo client component would settle it.
3. **`service_taxonomy.md:173` / `:528` and `security_compliance.md:315,322` on `db-backup`** — the
   `db-backup` and `infrastructure` repos are in no clone set, so `terraform/main.tf:13`,
   `services.tf:571`, `modules/core/storage/rds.tf:6,19,78-89`, *"157 objects"* and the `7dd1b80`
   date are unverifiable from here. This is the §5-rule-56 *unclonable* class, not the *unresolvable*
   one; the corpus's internal statements of it are mutually consistent (I checked all seven sites).
4. **`service_taxonomy.md:3` and `:410`, `:530`; `:169`** — the `infrastructure` `13c248e6` claims
   (`services.tf:509-517`, `:64-70`, the cms module's absence) are likewise unclonable here. What IS
   checkable and correct: `graphql-wundergraph/terraform/main.tf:20` really does still read
   `service_desired_count = 1`, so the corpus's *"orphaned dead code"* framing is at least internally
   coherent with what a clone can see.
5. **GitHub archive state** (`ARCHIVED <date>` in the *Archived / merged* table) — lives in the org API,
   `gh` is unavailable, repos are private. The doc says so itself and I confirmed only the git-visible
   half (e.g. `graphql-wundergraph` `60c229f3` carries no commit after 2026-07-30; `jobsimulation`
   `origin/main` **does** carry post-2026-08-04 commits, which is the retraction that row publishes).
6. **`shared_libraries.md:100,116-119,128,156-159,255,267,331-340` (colony/proto/taxonomy internals),
   `:77`'s *"untouched since 2025-02-12"*, `:169`'s chronos `v0.30.1`, and `customerio-sync.md:55`'s
   *"7,392 of 7,397 rows"*** — all describe repos in no clone set (`colony`, `proto`, `taxonomy`,
   `analytics-go`, `chronos`, `customerio-sync`) or a one-off measurement. Unverifiable here by
   construction; the doc marks the proto service list as *"a floor, not a count"* for exactly this reason.
7. **`service_taxonomy.md:251`'s "Python 3.11"** for studio-room — no `python_requires`, no version pin
   in `requirements.txt`, and nothing in `studio/CLAUDE.md` states it at `aeec036a`. Neither confirmed
   nor refuted; the Dockerfile that bakes studio into the `app` image would settle it.
