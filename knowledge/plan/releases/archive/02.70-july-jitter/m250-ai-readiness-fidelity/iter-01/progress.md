**Type:** tok (bootstrap) — authors the initial strategy TOK-01 from the milestone overview + platform contract.

# iter-01 — progress

Bootstrap tok. Read the platform contract (`defaults.go` etc.), the current demo seeders, and the 5-part gate;
authored TOK-01 (arithmetic-spine-first → set-dress → distribute → render loop). See milestone-root `decisions.md`.

## Close — 2026-07-24

**Outcome:** TOK-01 authored — the initial strategy for M250 (arithmetic-spine-first, then net-new Directus
set-dress, then evidence-distribution join, then the serial render loop on demo-2).
**Type:** tok (bootstrap)
**Status:** closed-fixed
**Gate:** N/A for tok
**Phase 5 grading:** (1) gate-met: n — (2) triggered-tok: n (bootstrap does NOT terminate) — (3) re-scope: n — (4) user-blocker: n — (5) cap-reached: n — (6) protocol-stop: n — Outcome: continue (into iter-02 tik under TOK-01)
**Decisions:** D1 (31 node-ids captured verbatim from platform), D2 (evaluated-skills track is a name-heuristic, not the track column), D3 (Directus set-dress is net-new — no replay seam).
**Routes carried forward:** iter-02 = arithmetic-spine atomic edit (Lane A); iter-03 = Directus set-dress (Lane B); iter-04 = evidence-distribution join; iter-05+ = live reset-to-seed render loop on demo-2.
**Lessons:** the "tech/business" label the page shows is driven by the skill NAMES set-dressed into
`directus.simulations.skills` via `techTrackRe`, not by the sim's `track` column — confirm at live render before
fighting the platform pins.
