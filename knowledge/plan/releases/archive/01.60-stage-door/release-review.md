# Release Review: v1.6 "stage door"

**Date:** 2026-06-14  ·  **Milestones:** M27 → M28 → M29 → M30  ·  **Overall verdict: YELLOW** (shippable; zero blocking; should-fix polish only)

> Generated by `/developer-kit:close-release` — a 9-sweep parallel review (Phases 0–5) across the two-repo split
> (rosetta docs/skills/planning + the ext `stack-secrets` code), then synthesized. Per-phase verdicts:
> P0 supply-chain GREEN · P1 scope GREEN · P1b deferral re-audit GREEN · P2 code-quality GREEN · P3 docs GREEN ·
> P3b KB-consolidation YELLOW · P4 tests GREEN · P4b metrics GREEN · P5 decisions GREEN. Zero blocking items.

## Scope

v1.6 "stage door" — the secret-provisioning release — delivered exactly what the roadmap promised across all 4 section milestones M27→M30, each closed GREEN per-milestone with its own review + deferral audit + harden pass. The roadmap thesis (ingest a secret source → provision every repo's `.env` → a values-blind secret-coverage DNA → demo-aware pre-flight wired into `/dev-up` + `/demo-up` → a live field-bake) is owned end-to-end.

- [x] **GREEN — every milestone's full "In" scope delivered Fate-1.** M27 12/12 boxes, M28 12/12, M29 8/8, M30 7/7 — all checked. No escape-hatch / dropped / UNACCOUNTED items among deliverables. (`knowledge/plan/releases/01.60-stage-door/{m27,m28,m29,m30}/progress.md`; `roadmap.md:148-302`)
- [x] **GREEN — on-disk verification confirms every load-bearing claim** in both repos. Committed `secret-dna.json` = the M30-final 55 genes / 6 repos (28/5/2/7/7/6) / 39 required·8 optional·8 waived / 12 critical, version `stage-door-m30`, profile `graphql` (synthesizer re-parsed). Ext tags resolve exactly: `stage-door-m27`→537aeff, `m28`→9742126, `m30`→29c922b (= current HEAD); M29 correctly added no ext tag (rosetta-only). stack-secrets is stdlib-only (no require block, no `go.sum` present — re-confirmed). CLI exposes all 6 verbs (list/check/measure/introspect/diff/provision).
- [x] **GREEN — the M27→M28 scope shift was reconciled, not silently dropped.** The check/measure scorer fold (roadmap originally placed it in M28; M27's build folded it in early per M27-D3.2) is recorded via roadmap trim commit `3b528f8` + M28 `overview.md` scope_note + M27 `decisions.md` M27-D3.2. M28's actual scope (demo-aware variant + wiring + the whole provision engine) was correctly reduced and all 12 boxes delivered.
- [x] **GREEN — no promised capability lacks an owner; zero Fate-3 promised-but-undelivered.**

## Code Quality

Reviewed the full ext diff `fbb8783..29c922b` for the stdlib-only `stack-secrets` Go section (provision/, secretdna/, source/, cmd/stacksecrets/) plus the bring-up wiring (preflight.sh, demo-stack/up-injected.sh) and the rosetta `/stack-secrets` skill.

- [x] **GREEN — clean and cohesive.** `go build ./...`, `go vet ./...`, `gofmt -l .` all pass with no output; `go test ./... -count=1` GREEN across all 4 packages (160 top-level PASS, ~190 incl. subtests, 0 FAIL/SKIP). Cross-milestone consistency strong: M28 provision builds on the M27 DNA+source readers; the 0/1/3 exit contract is uniform; naming/error-handling consistent.
- [x] **GREEN — the values-blind boundary is rigorously isolated** to `provision/io.go`'s `sourceValues→writeTargetFile` path (the ONE value-carrying seam), proven by a reflection-based sentinel test (`provision_safety_test.go`) that walks every Report/error/plan field incl. unexported strings. No value reaches stdout/log/error anywhere.
- [x] **GREEN — the DIRECTUS_TOKEN non-rearm logic is correct + defense-in-depth.** `StripOnNonProdKeys` → write-blank on non-prod, layered with the injection override (fix16/17 class). Field-verified BLANK in live demo-3 containers.
- [ ] **should-fix — stale README test metadata (ext).** `stack-secrets/README.md:108` claims "113 tests across the three packages"; there are now FOUR packages and 160 top-level PASS. The close(M27) reconcile (94→113) was never re-run after M28 added provision/ + cmd/.
- [ ] **should-fix — `introspect --write` help/behavior mismatch (ext).** Usage + flag help (`main.go:92/100/278`) say `--write` writes the refreshed provenance line back to the DNA, but the body (`main.go:320-325`) only prints a no-op message; `DNA.Save` (`dna.go:202`) is dead code (0 non-test callers). Implement the write via the existing Save, or change the help to "planned/no-op".
- [x] **nice-to-have — minor cleanups.** `secretdna/source.go:116 sortedKeys` is test-only helper in production code; the .env line-parse loop is independently reimplemented in three readers (behaviorally consistent; `maxLine` const only in `provision/io.go`, the other two hardcode `1024*1024`).
- [x] **info — intentional, verified.** The `check` verb returns exit 1 (not 3) on a nonexistent `--from` path (test-pinned `main_test.go:160`) — a missing source is a runtime coverage failure by design.

## Documentation

Read all five release-modified docs end-to-end (secrets-spec.md, safety.md §2.8/§2.9, setup_guide.md, CLAUDE.md, SKILL.md) and cross-checked every load-bearing claim against the engine at tag `stage-door-m30`. Doc-to-code fidelity is excellent — every hard number in secrets-spec.md matches the code exactly.

- [x] **GREEN — doc-to-code fidelity verified clean.** 55 genes, 6-repo split 28/5/2/7/7/6, status 39/8/8, 12 critical, 8 waived (4 classes), gh-token 3-member alias family, 6 minted keys, 3 strip-on-non-prod Directus keys, target-file map, 0/1/3 exit contract, 5 verbs, stdlib-only, 160 tests — all match. The M30 field-bake wiring (demo auto-provision + DEMO_NO_PROVISION + the preflight path-doubling fix + the bash-3.2 array-expansion guard + the named regression test) is all real and accurately described. The line-447 manual-.env TODO is confirmed removed (now provision prose). No stale release-branch / in-progress-milestone / TODO/WIP refs in any release doc; cross-ref anchors resolve.
- [ ] **should-fix — safety.md top-level framing seam.** The title (`:3` "Two inviolable guarantees") and PM paragraph (`:25-35` "two dangerous-sounding things: it reads… and it writes…") predate the newly-appended §2.9, which introduces secret provisioning as "the one new write-side actor." §2.9 folds itself under the existing write-side promise (not a contradiction), but a reader of just the intro would not know secret-byte movement is in scope. Widen the intro to name the third surface, or add one clause to the PM paragraph pointing at §2.9. (Synthesizer confirmed: the diff was purely additive — safety.md +58/-0; intro/Part-headers untouched.)
- [ ] **nice-to-have — demo family index discoverability.** `corpus/ops/demo/README.md` (end-to-end flow `:19-25`, Skills line `:124`) and `corpus/ops/rosetta_demo.md` contain no reference to `/stack-secrets` or secrets-spec.md, even though M30 wired auto-provision INTO `/demo-up`. Localized gap in the demo hub; CLAUDE.md + setup_guide.md surface it correctly.
- [x] **info — milestone-attribution in headers verified intentional (mechanism milestones), not stale.** secrets-spec.md's Status section + the DNA version both say `stage-door-m30`.

## Tests & Benchmarks

Ran the full suites for everything v1.6 touched, across both repos.

- [x] **GREEN — stack-secrets passes clean.** 160 top-level test funcs / ~190 incl. subtests, 0 failures, 4 packages. Stdlib-only confirmed (no require block, no go.sum), `go vet` clean, `-race` run clean (no data races, no flakiness).
- [x] **GREEN — ext Python all green.** 464 passed / 8 skipped (benign conditional) in one combined run across 5 sections (stack-core 85, stack-injection 102, stack-verify 104, dev-stack 74, demo-stack 99).
- [x] **GREEN — rosetta corpus README-index guard passes (16 tests).**
- [x] **GREEN — no regression in the 4 untouched ext Go modules** (alignment 49, clerkenstein 215, stack-seeding 258, stack-snapshot 328 — all ok).
- [x] **GREEN — field-bake regressions present and actually executing (not skipped).** bash-3.2 set -u test ran against real /bin/bash 3.2.57; lib-only seam test pins preflight ACTION below the lib-only return; the preflight REPO_ROOT path-fix kills the doubled `.agentspace/.agentspace/secrets` bug; the sentinel/DB_CONNECTION waive is pinned. Core invariants covered: 9 values-blind tests + 7 never-re-arm-DIRECTUS tests.
- [x] **GREEN — review was read-only/test-only; the live demo-3 was untouched.** The two potentially-live demo-stack tests are well isolated (own throwaway Postgres / stubbed docker on PATH).

## Decision Consolidation

Read all 4 milestone decisions files (13 decisions: M27×3, M28×3, M29×4, M30×3) and verified each "blend into knowledge" decision against its named target doc + checked cross-milestone conflicts.

- [x] **GREEN — the three load-bearing decisions are coherently reflected everywhere.** M27-D3.2 scorer-fold (check/measure in M27, gate plumbing in M28 — no double-claim of ownership); M27-D1 renumber (M26→M27 due to the orphaned self-contained-demo M26 collision, M26 preserved with its tag); M30-D1 sentinel/DB_CONNECTION critical/required → waived-config (exactly reflected in the committed DNA + the spec's 39/8/8+12-crit counts + the FROM-40/8/7+13 reconciliation note).
- [x] **GREEN — no cross-milestone conflicts** on the three conflict-prone axes: gate semantics (M27-D2 two-tier model everywhere), source layout (by-repo target paths + default `.agentspace/secrets` uniform), profile scoping (settled to `graphql`; no per-gene profile tag claimed — M27-D3.4 deferred + M28 discharged without triggering).
- [ ] **should-fix — CLAUDE.md:220 ↔ M29-D4 stale ant-academy path.** M29-D4 corrected ant-academy's secret target to `code/.env.local` ("the verified live truth; `.env` absent") and that landed in its named targets (setup_guide.md, secrets-spec.md, the DNA gene target_file). But the pre-v1.6 Environment Configuration paragraph in CLAUDE.md still says `ant-academy/code/.env` — the now-falsified path, stated **twice** (synthesizer confirmed 2 `code/.env` occurrences on the line). An agent following CLAUDE.md to hand-provision ant-academy would write to the wrong (gitignored) file. Not introduced by this release (the line predates v1.6, outside the M29 sweep); the authoritative provisioning surfaces are all correct — so should-fix, not blocking. Fix: `code/.env` → `code/.env.local` (both occurrences).
- [x] **nice-to-have — version-label lag (cosmetic).** The feature one-liners (CLAUDE.md:279, corpus/README.md:44, corpus/ops/README.md:26, safety.md §2.9 header) read "v1.6/M27–M28"; M30 added no new doc surface, so the §2.9 contract genuinely is M27/M28 — the spec body correctly carries the M30 reclassification. Optional polish to read M27–M30.

## Supply Chain

- [x] **GREEN — exactly one new code module, stdlib-only, first-party.** `stack-secrets` go.mod has no require block; no go.sum present or generated by `go build ./...`. `go list -m all` → only the module; `go list -deps` → stdlib + 4 internal packages; `go mod graph` → only self + toolchain. **Zero third-party deps → zero third-party license or CVE surface.**
- [x] **GREEN — no dependency bumps anywhere.** The only go.mod change in the code diff is the +5 lines creating `stack-secrets/go.mod`. The 4 other ext Go modules (alignment, clerkenstein, stack-seeding, stack-snapshot) were not touched. The rosetta release diff is docs/skills/planning only — zero committed code files, no dependency manifests of its own.
- [x] **info — benign Go patch-version skew.** go.mod declares `go 1.25.0` / `toolchain go1.25.11`; audit box runs go1.25.3 (same 1.25.x minor; build passes). Noted for reproducibility, not a vuln.
- [x] Artifact written: `knowledge/plan/releases/01.60-stage-door/dependencies.lock`.

## Deferrals

Ran the release-wide deferral re-audit across all 4 milestones + the 3 inherited release-level backlog items.

- [x] **GREEN — zero new deferrals introduced by v1.6, zero repeat-deferrals, zero aged-out, zero chronic/drift patterns.** Every item has a clear fate.
- [x] **GREEN — both within-release items resolved.** DEF-M27-01 (encrypted-zip) DROPPED at M27 close as a documented v1 scope boundary (a complete landing would pull a crypto dep contradicting the stdlib-only values-blind design; no live consumer). DEF-M27-02 (per-gene profile-tag) Fate-2 DISCHARGED at M28 close (the conditional never triggered — M28 used the default graphql profile).
- [x] **GREEN — 3 inherited backlog items carried unchanged with sign-off (escape hatch).** DEF-M10-01 (cloud store/S3 blob bytes, S3-access-gated, long-lived since v1.2 but NOT a v1.6 repeat-defer), DEF-M21-01 (replayCmd hermetic test), M25-D9 (dev taxonomy rc=4) — all Fate-1-infeasible, orthogonal to the entire v1.6 diff, re-signed fresh at the v1.5 close (2026-06-14).
- [x] **info — zero TODO/FIXME/HACK/XXX markers** in the entire stack-secrets section (no code-level deferral disguised as a marker).
- [x] **info — waived secret-DNA classes are waived-not-deferred** (excluded from the gate denominator by design; no fate decision applies).
- [x] Artifact: `knowledge/plan/releases/01.60-stage-door/audit-deferrals/deferral-audit-2026-06-14-release-close.md`.

## Metrics

- [x] **GREEN — all three regression gates PASS** vs the v1.5 "prop room" baseline: no test-count decrease, no coverage drop >2pp, flake = 0.
- [x] **GREEN — Go test funcs 867 → 1027 (+160), independently verified** by grepping Test/Fuzz func-decls at ext HEAD 29c922b: alignment 52 + clerkenstein 223 + stack-seeding 259 + stack-snapshot 333 + stack-secrets 160 = 1027 (exact match). The +160 is entirely the net-new stdlib-only stack-secrets section (113@M27 → 160@M28; M29/M30 +0 top-level). Python held at 459 collected (the v1.6 growth surface is Go).
- [x] **nice-to-have — pre-existing metrics-history.md formatting (out of v1.6 scope).** The v1.5 row is malformed (6 columns under the 8-column header, missing Codename + Gates cells, sitting below the `_Baseline release_` note that belongs under v1.0). The appended v1.6 row is correctly 8-column. Synthesizer confirmed on disk. One-line cleanup if a tidy table is wanted.
- [x] Artifacts: `knowledge/plan/releases/01.60-stage-door/metrics.json`, `knowledge/plan/metrics-history.md`.