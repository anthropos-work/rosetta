# M20 — Decisions

_Implementation decisions with rationale. ID scheme: M20-D1, M20-D2, … Open questions from `overview.md` get resolved here during build._

| ID | Decision | Rationale | Date |
|----|----------|-----------|------|
| M20-D1 | **Reuse the M13 `dev-setdress.sh` pass verbatim for demo, made stack-type-aware** (`--stack-type dev\|demo`, default `dev`). `up-injected.sh` chains it via `--stack-type demo --force`. | The overview mandates "REUSE M13's proven pass, do not reinvent it". One engine, two lifecycles = the convergence by construction; the underlying `stacksnap`/`provision-plan`/`stackseed` CLIs already accept `demo-N` via the shared `ParseStackN` grammar, so only the prefix + default-preset needed parameterizing. All safety (n=0 guard, prod-Directus firewall, never-capture) preserved byte-for-byte. | 2026-06-09 |
| M20-D2 | **Demo default preset = `small-200`** (vs dev's `dev-min`). | Overview lean confirmed: demos want a fuller world than dev's ~10-user "never empty" floor. `small-200` (200 users / 3 months) is the lightest demo preset, seeds in <1s, still exercises the role mix. An explicit `--seed` always overrides. | 2026-06-09 |
| M20-D3 | **Atomicity contract = seed-always-after-replay (the seed is the floor).** A catalog-replayed-but-unseeded stack is the worst state (real skills in the view but every authorized route 403s — no org/identity/casbin grant). The pass runs the (optional, cache-first) replay then ALWAYS seeds; a replay miss degrades to a structural-only world that still logs in 200. The M17 re-run guards (idempotent TRUNCATE-then-reload replay + idempotent seed COPY) make a partial set-dress repairable by re-running. | Overview: "a partial snapshot with no seed = 403s, so it's both-or-neither". The existing pass structure already enforced seed-after-replay; M20 makes it explicit (comment + a dedicated atomicity-floor test on both dev + demo). | 2026-06-09 |
| M20-D4 | **Cold-start capture: DOCUMENT the sanctioned DSN-export / restore-a-`pg_dump`-then-`--dsn` path; do NOT build the MCP-paging capture adapter.** (ISSUE-13 spike resolved.) | Spike evidence: the capture path's load-bearing `Capturer.CopyPublic` returns **raw Postgres COPY text-format bytes** (`COPY (...) TO STDOUT`), which the manifest checksum + the snapshot-fidelity gate require to be byte-identical. The wired `postgres` MCP (`mcp__postgres__query`) is a JSON-row query tool — it cannot emit COPY-format bytes. An MCP adapter would have to **re-serialize JSON rows back into exact COPY text format** (NULL `\N` sentinels, tab/newline/backslash escaping, vector/timestamp/tsvector formatting) AND add paging for the ~2.1 GB taxonomy + embeddings — fragile, byte-fidelity-critical work for ZERO capability gain (the snapshot is identical to the `--dsn` path, exactly M9b-D9's reasoning). NOT cheap → document the DSN-export route, don't build. | 2026-06-09 |
