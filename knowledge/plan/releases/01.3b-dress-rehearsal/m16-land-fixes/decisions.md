# M16 — Decisions

_Implementation decisions with rationale. ID scheme: M16-D1, M16-D2, … Open questions from `overview.md` get resolved here during build._

| ID | Decision | Rationale | Date |
|----|----------|-----------|------|
| M16-D1 | **Re-tag/version scheme: per-milestone `dress-rehearsal-mNN` tags + a final `v1.3.1` release tag at close.** M16's tag is `dress-rehearsal-m16`. The local-only `stack-party-devpath-fix` tag is **superseded** (no longer used) but **not deleted** — local remains, remote was never touched. | Matches the established convention (`stack-party-mNN` → `v1.3`, `stack-snapshot-mNN` → `v1.2`). Resolves overview.md open-question #1. | 2026-06-08 |
| M16-D2 | **Keep the `anthropos-dev` back-compat fallback in the scripts forever (don't sunset).** It survives as the **single intentional legacy-alias** — a one-line auto-detect (`stack-dev` preferred, `anthropos-dev` fallback) in `up-injected.sh`, `migrate-demo.sh`, `rosetta-demo` (×2), `dev-stack`, + the `clone_repos.py --dev-root` help. | Costs nothing, protects an older on-disk layout from the exact bring-up failure ISSUE-1 hit. Resolves overview.md open-question #2. | 2026-06-08 |
| M16-D3 | **Tag the fixes + the doc truth-up as ONE coherent milestone tag** (`dress-rehearsal-m16` at the doc-commit HEAD), not just the 2 stranded fixes. Pushed `main` + tag together, then re-consumed. | The fixes alone are incomplete without the matching corrected docs; a single tag = a single reproducible M16 consumption point. | 2026-06-08 |
| M16-D4 | **Milestone tag moved forward (same-session) to capture the PR-review sweep** (`fa20658`→`23bddee`, commit `f1ee81a`→`44edc09`). Branch push was a clean fast-forward; only the tag ref was force-updated. | The PR-review fix is part of the same "restore doc truth" deliverable; a milestone tag should mark the *finished* milestone. No branch history rewritten (no forbidden force-push of `main`). | 2026-06-08 |
| M16-D5 | **PR-review-surfaced stale facts in `demo-stack/README.md` landed in-milestone (Fate 1).** The first sweep missed them because they used `anthropos-demo/` (the workspace name) not `anthropos-dev/` (the dev-workspace name); also stale: the version label, two `78-test` repeats, and a pre-M5/M6 `lib/` layout list. | Same doc-staleness class M16 targets, in the file under active sweep — Fate-1 by definition. The `clerkenstein/knowledge/glossary.md:19` `anthropos-demo` mention is in the clerkenstein section's own KB (out of M16's enumerated scope) — surfaced, not touched (would expand scope into a different section's docs). | 2026-06-08 |
| M16-D6 | **Only `stack-demo/rosetta-extensions` re-consumed.** `stack-dev/` has no `rosetta-extensions` clone (dev-stack uses the platform compose directly); it's the only per-stack consumption copy on this box. | Verified by inventory — no other `stack-*/rosetta-extensions` `.git` repo exists. | 2026-06-08 |
| M16-D7 | **Close deferral re-audit (Fate-2 confirm): the LIVE docker-harness migrate-race *behavior* test → M17.** M16 landed the static regression fence (`TestMigrateRaceGuard`, 3 tests); the runtime behavior test (needs a docker harness + live race window) is confirmed covered by M17's `In:` scope (the `set -e` first-run-race audit + the per-component tested idempotency contract that "trips on a real 2nd run"). No M17 `overview.md` edit — already owned. Audit verdict **GREEN** (0 repeat, 0 aged-out). | Closure is the deferral re-audit moment. The live test belongs with M17's harness, not split across milestones; landing it in a docs/rename milestone with no harness in scope would duplicate M17 setup. Inherited DEF-M10-01 (S3/cloud store → v1.4, signed) is orthogonal + not aged. Audit: [audit-deferrals/deferral-audit-2026-06-08-m16-close.md](audit-deferrals/deferral-audit-2026-06-08-m16-close.md). | 2026-06-08 |
| M16-D8 | **Close-review reversal of M16-D5's clerkenstein boundary call → landed Fate-1.** `clerkenstein/knowledge/glossary.md:19` was the **last** non-fallback stale workspace-name reference in the entire extensions repo (verified by a repo-wide grep: every other `anthropos-(dev\|demo)` hit is an intentional fallback/legacy-marked line or a test). It claimed clerkenstein is "gitignored locally under `anthropos-demo/`"; corrected to "consumed per-stack into the gitignored `stack-demo/` workspace; legacy layout: `anthropos-demo/`". | A one-word-class doc fix in the exact doc-truth class M16 owns; leaving it would let a "restore doc truth" milestone ship with one stale workspace name still in the repo. Build-time M16-D5 deferred it as cross-section scope-expansion risk; at close the cheaper, truer call is to land it — it doesn't touch clerkenstein *behavior* or alignment, only a stale layout note. Committed in the extensions repo as part of the close (the doc-truth completion). | 2026-06-08 |

## Adversarial review (close Phase 2c)

| Scenario | Module | Outcome |
|----------|--------|---------|
| **Both `stack-dev/` AND `anthropos-dev/` exist on the same box** (mid-migration). The resolver is `DEV="$REPO_ROOT/stack-dev"; [ -d "$DEV" ] \|\| DEV=…/anthropos-dev`. | the 5 workspace-resolvers (`up-injected.sh`, `migrate-demo.sh`, `rosetta-demo` ×2, `dev-stack`) | **Handled — deterministic + correct.** `[ -d "$DEV" ]` short-circuits the moment `stack-dev/` is a directory, so `stack-dev/` always wins when both exist; the legacy clone is silently ignored, never half-resolved. This is the intended precedence (the new name is authoritative) and is pinned by `TestRenameDrift` ("`stack-dev` must lead `anthropos-dev` in every resolver"). No silent-wrong-root failure mode. The only residual is operator hygiene (a stale `anthropos-dev/` left on disk is harmless), not a code defect — no fix needed. |
| **`migrate-demo.sh` count query runs before `casbin_rules` exists** (the ISSUE-7 race) under `set -e -o pipefail`. | `migrate-demo.sh` | **Handled + fenced.** `\|\| echo 0` keeps the pipeline from aborting; the empty-result path falls through to `init_policy.sql` (`CREATE TABLE IF NOT EXISTS` + `INSERT`). Pinned by `TestMigrateRaceGuard` (negative-tested: removing `\|\| echo 0` trips the guard). The *live* docker-harness behavior test is Fate-2 → M17 (M16-D7). |
