# State

**Active version:** **v1.1 "show floor"** — IN DEVELOPMENT on `release/01.10-show-floor`. **Refactored 2026-06-04** from "disposable demo stacks" into **the platform-operations extension framework**: collapse the repo constellation into 2 repos (`rosetta` + the `rosetta-extensions` monorepo), then finish the seeded-demo capability and generalize to dev. M3 ✅ → M4 ✅ → M5 ✅ → M6 ✅ → **M7a ✅ → M7b ✅ → M7c ✅ → M8 ✅**. Zero platform-repo change. **ALL v1.1 MILESTONES DONE** — the release is ready to ship.
**Active milestone:** _(none — all milestones closed)._ **M8 "Corpus + use-case recipes + polish"** closed 2026-06-05 (the last v1.1 milestone): the `corpus/ops/demo/` family + 3 recipes (incl. the 2 M3 injection recipes) + 3 seed presets (2 seed-proven) + the `/demo-seed` skill + the express-gate CI carry-forward (validated 9/9) + cross-linking.
**Next up:** **`/developer-kit:close-release`** — review `release/01.10-show-floor` as one PR, merge → `main`, tag **`v1.1` "show floor"**, archive the release records.
**Last shipped:** **v1.0 "body double"** — 2026-06-03 (tag `v1.0`). · **Last closed milestone:** M7b — 2026-06-04.
**Paused:** _(none)_

## Shipped releases
- **v1.0 "body double"** — **2026-06-03**, tag `v1.0`. The alignment-testing framework (`test/alignment/`) + **Clerkenstein**, a *measured* drop-in Clerk mock at **100%/100% on all three surfaces** (Go · JS/FAPI · `@clerk/express`), zero platform-code change. 6 milestones (M0→M2c). close-release caught + fixed 1 blocker (an `@clerk/express` gate regression from the M2c close). Records: [releases/archive/01.00-body-double/](releases/archive/01.00-body-double/) (review · retro · metrics · stats · lockfile).

## Recently closed
- **M8 "Corpus + use-case recipes + polish"** (2026-06-05, section — the LAST v1.1 milestone) — the consolidation/discoverability layer: a `corpus/ops/demo/` family (index + 3 end-to-end recipes — enterprise-onboarding, skill-progression, browser-login [which lands the 2 M3-deferred injection recipes: the `api.clerk.com` cert-redirect + the browser-login walk-through]); 3 seed presets (small/mid/large, `mid-500` + `large-1k` seed-proven end-to-end on demo-1); the `/demo-seed` skill + the CLAUDE.md skill table; the v1.0 **express-gate CI carry-forward** wired into clerkenstein `alignment.yml` (**validated 9/9** locally); cross-linking from corpus/README + root README + CLAUDE.md (all doc links resolve). roadmap.md § M8.
- **M7c "The seeder fleet, to a coverage gate"** (2026-06-05, iterative — gate-met-over-reachable + waiver) — built the seeder fleet across 5 iters: TOK-01 (reachability strategy) → `jobsim-sessions` → `skillpath-sessions` → `assignments` → `activity`, each a deterministic **backdated-activity** seeder (time-distributed, pass/fail per `pass_rate`; content refs as free values → the believability core is reachable **without** the shared Directus). Data-DNA coverage **40%→80%**, each surface promoted planned→seeded + conformance-gated. **Gate: 3 of 4 met outright** (login→**200**; full 8-seeder seed **0.69s**/~8500 rows; isolation **clean**); coverage **100% over the 8 reachable surfaces / critical 100%**, with **taxonomy + content waived** (the hard line: skiller snapshot + shared Directus; Re-scope trigger, user-confirmed → ~v1.2; recorded as `waived-m7c` in the data-DNA). Caught + fixed 2 live bugs (skillpath UNIQUE; introspect-load). **20 seeder / 145 module tests.** roadmap.md § M7c.
- **M7b "The data-alignment dimension (data DNA)"** (2026-06-04, section) — extended the M0 alignment framework to **data**: the `datadna` harness (`rosetta-extensions/stack-seeding/dna/`) that enumerates the seedable surfaces (**4 seeded + 6 planned** — the M7c checklist) and measures a seeder's output conforms to the platform's **current schema** via **structural operators** (type-match / constraint-satisfied [NOT-NULL + UNIQUE] / fk-valid / row-count) + **schema-as-source via introspection**; drift is a flagged `diff` (exit-code contract). A separate harness, not an alignctl runner (M7b-D3). **PROVEN live** on the M7a-seeded `demo-1`: `measure` **100% / Critical 100%** (4 seeded surfaces); `diff` flags an injected column (exit 1) + reads clean on revert. Caught + fixed the planned-surface introspection bug; hardened the UNIQUE leg (M7b-D4). **dna 49 + cmd/datadna 10 + pg 17 tests**, all gates green. roadmap.md § M7b.
- **M7a "Seeding framework + production-isolation safety"** (2026-06-04, section) — built `rosetta-extensions/stack-seeding/`: a host Go module that seeds a stack by talking **directly to its Postgres** (offset port, `COPY` — *not* ent-linking, which is blocked: `app/internal/bootstrap` is internal, M7a-D3) behind a **3-layer production-isolation guard** (CheckWrite blocks shared/external writes on non-prod regardless of opt-in · PreflightEnv forces the S3-public override + rejects real-Clerk/Directus · AssertClean proves zero pollution). **LIVE-PROVEN** on a fresh injected `demo-1` (no manual SQL): `migrate-demo.sh` (now bootstraps the global Sentinel policy) → `stackseed` (org + 1000 users + the real `user_clerkenstein` identity + the casbin `g2` grant; isolation audit clean) → authenticated login returns **HTTP 200** (`membershipsCount: 1001`). The proof caught + fixed **2 real bugs** (the casbin `g2` arg-order; the missing global-policy bootstrap — M7a-D4). **68 tests** (guard 97% covered), all gates green. roadmap.md § M7a.
- **M6 "`dev-stack`: tooled local dev environment"** (2026-06-04, section) — extracted the shared port-offset engine into a new `rosetta-extensions/stack-core/` section (demo + dev share it, M6-D1) + added a focused `dev-stack/`: isolated dev stacks (`dev-N`, guarded `-p dev-N`), real Clerk default, optional Clerkenstein injection (reuses stack-injection). Scoped to proven value (M6-D2). **87 tests** (+9), flake 3/3, deploy gate 100%/100%. roadmap.md § M6.
- **M5 "Extract the reusable `stack-injection` layer"** (2026-06-04, section) — pulled the generic Clerk-mock injection (`inject.py`, `gen_injected_override.py`, `apply-authn.sh`) out of demo-stack into `rosetta-extensions/stack-injection/`, consumable by any stack with a **demo-ON / dev-OFF** toggle. The mock stayed in clerkenstein (M5-D1); the port-offset engine stayed in demo-stack (M5-D2). Split the tests + repointed the consumers — **78 preserved**, flake 3/3, shellcheck clean, deploy gate 100%/100%. roadmap.md § M5.
- **M4 "Consolidate into the `rosetta-extensions` monorepo"** (2026-06-04, section) — collapsed the constellation into 2 repos: created the **private** `anthropos-work/rosetta-extensions` monorepo (73 commits), `git subtree`-imported `clerkenstein` + `rosetta-demo`(→`demo-stack`) **with full history**, stood up the `knowledge/` nav, thinned rosetta to pointers, fixed a +1-depth path break the verify gate caught (M4-D4), and **removed the old `clerkenstein` + `rosetta-demo` repos** (local + org). Deploy gate held 100%/100% (7/7); demo-stack 78 tests green under the new paths. roadmap.md § M4.
- **M3 "Disposable multi-instance demo stacks"** (2026-06-03; extended work closed 2026-06-04, section) — the rosetta-demo layer: bring up `demo-N` isolated alongside the dev stack (additive compose override with a port-offset + `!override` + per-stack project/data, clone-each-repo-at-its-release-tag, the 4 Clerkenstein injection recipes wired) — **zero read-only-platform change**. Tooling in the gitignored `anthropos-demo/rosetta-demo/` repo (M3-D4, renamed from demo-stacks M3-D6) + `/demo-*` skills + `corpus/ops/rosetta_demo.md`. **78 rosetta-demo tests** + the clerkenstein deployment/injection surface. **Extended (2026-06-04):** the FULL Clerk-free injected stack is LIVE-PROVEN (`up-injected.sh` + `migrate-demo.sh` → sentinel healthy, `/api/health` 200, a live app accepts a Clerkenstein token 403-not-401) + the deployment alignment surface (`clerk-deploy-1`, 7/7) + a harden pass + the close (found+fixed a real ×100 port-offset collision). 2-concurrent + browser-login → M5. roadmap.md § M3.
_(v1.0's milestones M0→M2c are in [roadmap.md](roadmap.md) § Done — v1.0.)_

## Headline numbers (inherited from v1.0 close — baseline for v1.1)
- **Alignment gates (v1.0 — held green):** **100%/100%** on all three surfaces — Go (22/22, `clerk@2.6.0`), JS/FAPI (9/9, `clerk-js@5`), `@clerk/express` (9/9, RS256/JWKS). Clerkenstein: 8 packages, 128 Go test/fuzz funcs, flake 0. v1.1 must keep these green (the demo stacks consume Clerkenstein, they don't change it).
- **rosetta framework:** `test/alignment/` 43 test + 3 fuzz, stdlib-only.
- **v1.1 (M3–M6 landed):** the `rosetta-extensions` monorepo (75 commits, 5 sections: clerkenstein · demo-stack · stack-injection · stack-core · dev-stack) — **87 unit tests** (shellcheck + py_compile clean); clerkenstein at **218 Go test/fuzz funcs**, **4 alignment gates all 100%/100%** (Go 22/22, JS 9/9, `@clerk/express` 9/9, deploy 7/7). Flake 0. Seeding (M7a/b/c) is the next surface.

## v1.1 "show floor" milestones
**M3** ✅ → **M4** ✅ (consolidate → `rosetta-extensions` monorepo) → **M5** ✅ (stack-injection) → **M6** ✅ (dev-stack + stack-core) → **M7a** (framework+safety, section) → **M7b** (data-DNA, section) → **M7c** (seeder fleet, iterative) → **M8** (recipes). Sequential. Full design + execution graph + risks in [roadmap.md](roadmap.md) § In Development — v1.1.

## v1.1 decisions locked
- **M3-D1 — per-demo service-repo clones** (user): each stack re-clones the service repos (full isolation) rather than sharing `anthropos-dev/*`.
- **M3-D2 — manual teardown only** (user): `/demo-down [N]` is the only reclaim path.
- **M4-D1…D4 — the 2-repo consolidation:** `git subtree` history-preserving import into `rosetta-extensions`; **delete the old `clerkenstein`+`rosetta-demo` repos** (user); the alignment framework stays in rosetta; the +1-depth path fix.
- **M7a-D1 — seeding split into M7a/M7b/M7c, all in v1.1** (user, 2026-06-04): make seeding robust/drift-proof/fast/**production-safe**; section + section + iterative "mix". M7a-D2 the isolation guard is load-bearing; M7b-D1 extend M0 to a data dimension; M7c-D1 the fleet is iterative on data-DNA coverage.
- **Carried hard constraint:** **zero changes to any read-only platform repo** — all extension work is additive overlay/orchestration under the gitignored `anthropos-demo/` (the `rosetta-extensions` monorepo). Clerkenstein (v1.0) is consumed, not modified.
- **Express-gate CI carry-forward** (from v1.0) lands in **M8**.

## Branch model
**v1.0 SHIPPED + pushed:** `release/01.00-body-double` merged `--no-ff` → `main`, tagged **`v1.0`**; release branch deleted.
**v1.1 IN DEVELOPMENT:** `release/01.10-show-floor` (cut from `main` 2026-06-03, pushed to `origin`). M3–M6 closed + merged in. The seeding redesign (M7a/M7b/M7c) + M8 scaffolds live on it. `main` stays at the v1.0-shipped state until v1.1 closes. Milestone branches `m{7a,7b,7c,8}/{slug}` cut from the release branch by `/developer-kit:work-milestone`; merged back at each close; the release merges → `main` + tags `v1.1` at `/developer-kit:close-release`. _(Extensions code is the **private** `anthropos-work/rosetta-extensions` monorepo, gitignored at `anthropos-demo/`; the old standalone `clerkenstein`+`rosetta-demo` repos were deleted at M4.)_

_Last updated: 2026-06-04 (seeding redesigned — M7 split into M7a framework+safety / M7b data-DNA / M7c seeder-fleet, all in v1.1; M7a is next)._
