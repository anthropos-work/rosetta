# State

**Active version:** **v1.3b "dress rehearsal"** — **IN DEVELOPMENT** (designed 2026-06-08; branch `release/01.3b-dress-rehearsal` cut from `main`). A **field-hardening release** between shipped v1.3 and future v1.4: it makes `/demo-up` produce a **full, populated, verified, demoable** stack — addressing the 14 issues the first real `/demo-up` run surfaced ([`.agentspace/demo-up-issue.md`](../../.agentspace/demo-up-issue.md)). 5 milestones M16→M20: land the applied fixes + doc truth → re-run safety (idempotency) → verification net → frontend tier → lifecycle convergence (auto set-dress + cold-start). **Tooling + docs only — zero platform-repo edits.** v1.4 (cloud store / S3 / AI content / shareability — incl. the signed DEF-M10-01) stays in [`roadmap-vision.md`](roadmap-vision.md), untouched.
**Active branch:** `release/01.3b-dress-rehearsal` (cut from `main` 2026-06-08; no milestone branch yet).
**Active milestone:** **M16 — Land the field fixes + restore doc truth** (`planned`; not yet started). Push the local devpath + migrate-race fixes (authoring copy is 2 commits ahead of `origin`), finish the `anthropos-dev → stack-dev` rename as the documented default, clear the stale GUIDE/README facts.
**Next up:** **`/developer-kit:build-milestone`** (M16) — creates `m16/<slug>` from the release branch.
**Phase:** **v1.3b designed + scaffolded; M16 ready to build.**
**Paused:** _(none)_

## Recently shipped releases
- **v1.3 "stack party"** — **2026-06-07**, tag `v1.3`. The **dev/demo convergence**: dev stacks become first-class peers (a **unified first-available-N stack registry** [M12], a **local per-stack Directus + auto-snapshot + light `dev-min` seed** on a `dev-stack up` [M13]), one **generic `stack-*` skill set** (`stack-list`/`seed`/`snapshot`/`update` + `dev-up`/`dev-down`, **hard-renamed**, no aliases [M14]), and a net-new **code-cited `corpus/ops/safety.md`** — the tooling's two guarantees (never reads private data / never touches prod), each load-bearing claim pinned to source by a fail-closed drift guard [M15]. Go test funcs 693→**713** (+20) + **+52 net-new Python** (the unified registry, 174 total); flake 0; triple-clean 3/3; 0 called third-party CVEs; all-permissive licenses. close-release GREEN (0 must-fix; docs YELLOW→fixed; deferral re-audit YELLOW→GREEN — DEF-M10-01 formally signed → v1.4). 1 escape-hatch (S3 blob bytes + cloud store → v1.4, signed). Records: [releases/archive/01.30-stack-party/](releases/archive/01.30-stack-party/) (review · retro · metrics · lockfile).
- **v1.2 "set dressing"** — **2026-06-07**, tag `v1.2`. The **snapshot mechanism**: a dedicated `stack-snapshot` extension that captures the **public** reference library (skills taxonomy + Directus content) read-only from prod, manifest-caches it in `.agentspace`, and replays it per-stack behind a tested **tenant-data firewall** (customer data never copied) — lifting M7c's two `waived` surfaces to **100% data-DNA coverage**. 4 milestones (M9a→M9b→M10→M11). Go test funcs 409→**708** (+73%); flake 0; triple-clean 3/3. Records: [releases/archive/01.20-set-dressing/](releases/archive/01.20-set-dressing/).
- **v1.1 "show floor"** — **2026-06-05**, tag `v1.1`. The **platform-operations extension framework**: disposable, Clerk-free, **production-safely-seeded** demo stacks (+ dev), in a 2-repo constellation (`rosetta` + the private `rosetta-extensions` monorepo). 8 milestones (M3→M8). Test funcs 175→**409**; all 4 Clerkenstein alignment gates held **100%/100%**; flake 0. Records: [releases/archive/01.10-show-floor/](releases/archive/01.10-show-floor/).

## Headline numbers (v1.3 close — 2026-06-07 — the baseline for v1.4)
- **Go test funcs:** **713** total (test-only `^func Test` matcher; +20 vs v1.2's test-only 693). Per-module: `rosetta-extensions/alignment` 43 · clerkenstein 210 · stack-seeding **236** (+5 across v1.3) · stack-snapshot **224** (+15 across v1.3 — the M13 `cmd/provision-plan` runner, the M14 rename guard, the M15 safety.md↔code drift guards). All 4 Go modules pass `-race -count=1`; gofmt + `go vet` clean.
- **Python test funcs:** **174** (stack-core 54 · dev-stack 38 · demo-stack 13 · stack-injection 69). **+52 net new** across v1.3 — M12's unified-registry suite (`test_stack_registry.py` 0→50 + the ERR-trap-frees paths). All 3 CLIs shellcheck-CLEAN; py_compile CLEAN.
- **The v1.3 thesis (the headline):** dev and demo are now **one operating model** — a unified N-pool registry (no port collisions), dev as a full-fidelity data peer (local Directus + auto-snapshot + `dev-min` seed by default, replay-only — capture never runs on dev), one generic `stack-*` skill set (hard-renamed), and a code-cited `safety.md` whose every load-bearing claim is a fail-closed drift test against `firewall.go`/`isolation.go`/`audit.go`/`source.go`.
- **Coverage:** **100% of the full data-DNA catalog** (inherited from v1.2; unchanged — nothing waived).
- **Alignment gates (held green since v1.0):** **100%/100%** on all 4 Clerkenstein surfaces — Go (22/22), JS/FAPI (9/9), `@clerk/express` (9/9), deployment/injection (7/7). v1.3 touched no clerkenstein. 3 wired in CI; deploy is a local gate.
- **Test health:** flake **0**; triple-clean **3/3** (4 Go `-race -shuffle` + 4 Python suites); supply-chain GREEN (0 called third-party CVEs; the 12 called vulns are Go stdlib @go1.25.3 → cleared by go1.25.11+; all-permissive licenses).

## Branch model
**v1.3b IN DEVELOPMENT:** `release/01.3b-dress-rehearsal` cut from `main` (2026-06-08) by `/developer-kit:design-roadmap`; milestone branches `m16/…`→`m20/…` will be cut from it as each is built; merges `--no-ff` → `main` + tags (lean `v1.3.1`) at `/developer-kit:close-release`. The 14 demo-up fixes are **tooling** (built + tested in the authoring copy `.agentspace/rosetta-extensions/`, **tagged** `dress-rehearsal-mNN`, consumed per-stack) **+ docs** (this corpus's `corpus/*` + `.claude/skills/*`). **Pending publish (M16):** the authoring copy is **2 commits ahead of `origin`** (the applied ISSUE-1 devpath + ISSUE-7 migrate-race fixes, local-only tag `stack-party-devpath-fix`) — M16 pushes + re-tags + re-consumes.
**v1.3 SHIPPED:** `release/01.30-stack-party` merged `--no-ff` → `main`, tagged **`v1.3`** (2026-06-07); release branch deleted; all 4 milestone branches (`m12/stack-registry`, `m13/dev-peers`, `m14/unified-skills`, `m15/safety-doc`) merged + deleted. The stack tooling lives in the **private** `anthropos-work/rosetta-extensions` monorepo — authored + tagged in the authoring copy at `.agentspace/rosetta-extensions/`, then consumed per-stack at a pinned tag. v1.3 extensions markers: per-milestone **`stack-party-m12`** @ `be1c979` · **`stack-party-m13`** @ `cca4464` · **`stack-party-m14`** @ `33fc525` · **`stack-party-m15`** @ `51ca18b`, plus the release tag **`v1.3.0`** @ the post-close-fix HEAD. Snapshot **payloads** live in a gitignored `.agentspace/snapshots/` cache (never committed; cloud/S3 store = v1.4, DEF-M10-01).
**v1.2 SHIPPED + pushed:** tagged **`v1.2`** (2026-06-07). **v1.1 SHIPPED + pushed:** tagged **`v1.1`**. **v1.0 SHIPPED:** tagged `v1.0` (2026-06-03).

_Last updated: 2026-06-08 (**v1.3b "dress rehearsal" DESIGNED** via `/developer-kit:design-roadmap` — the field-hardening release for the 14 `/demo-up` issues; branch `release/01.3b-dress-rehearsal` cut from `main`; 5 milestones M16→M20 scaffolded [land fixes+doc truth → re-run safety → verify net → frontend tier → auto set-dress+cold-start]; Phase 0a deferral audit GREEN [DEF-M10-01 orthogonal → v1.4]; Phase 0b blind areas [frontend tier + verification → M19/M18 Delivers]; all 14 issues verified against live code, default-on+skippable heavy features, zero platform-repo edits. Headline numbers below are the inherited v1.3-close baseline, unchanged until M16+ land. **Next: `/developer-kit:build-milestone` M16.**)_

<!-- prior: 2026-06-07 (**v1.3 "stack party" SHIPPED** via `/developer-kit:close-release` — merged `--no-ff` → `main`, tagged `v1.3`. The dev/demo convergence: unified registry [M12] + dev-as-peer [M13] + hard-renamed `stack-*` skills [M14] + code-cited safety.md [M15]. close-release: 0 must-fix; Phase 0 supply-chain GREEN [0 called third-party CVEs, stdlib-only go1.25.11+ item, all-permissive licenses, lockfile written]; Phase 1b deferral re-audit YELLOW→GREEN [DEF-M10-01 formalized as a signed RELEASE-SCOPE-DEFER → v1.4, DRIFT_DEFER classification + date fix]; Phase 2 code-quality GREEN [stack-list SKILL over-claim trimmed]; Phase 3 docs YELLOW→GREEN [recipe v1.3→v1.4 stamps, safety.md index rows, README banner]; Phase 4b metrics GREEN [+20 Go / +52 Python, no regression]; triple-clean 3/3; stats snapshot + release-retro written; CHANGELOG v1.3 entry. Release dir archived → `releases/archive/01.30-stack-party/`. **Next: `/developer-kit:design-roadmap`** to scope v1.4 [the seeds — cloud store/S3 blobs, AI content, shareability, more mirrors — in `roadmap-vision.md`].). -->
