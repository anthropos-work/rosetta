# State

**Active version:** **v1.2 "set dressing"** — in development on `release/01.20-set-dressing` (designed 2026-06-05, **refined 2026-06-06** against live prod). Richer demo worlds: a **dedicated `stack-snapshot` extension** lifts M7c's two `waived` surfaces (`taxonomy` + `content`) to **100% data-DNA coverage** — capture the real **public** taxonomy + content library once from a **safe, low-impact source** (default: an existing prod `pg_dump`; fallback: a throttled read-only pull — MVCC = no write blocking), replay per-stack, measured-faithful via a snapshot-fidelity dimension, with a tested **tenant-data firewall** (never customer data) + a **`.agentspace` manifest cache** (snapshots never enter git). 4 milestones, all `section`: **M9a** (framework + capture-safety + firewall + store + `/db-query` port + fidelity-DNA) → **M9b** (taxonomy surface) → **M10** (public Directus content) → **M11** (recipes + presets + corpus). AI-content + shareability + cloud-store held to v1.3 (user, 2026-06-05/06).
**Active milestone:** **M9b — Taxonomy snapshot (the first real surface)** (`built` · `section` · large). Next: **`/developer-kit:harden-milestone`** then **`/developer-kit:close-milestone`** (the taxonomy surface + parent-scope firewall + fidelity measure + DAG node landed; tag `stack-snapshot-m9b` to be set at close).
**Next up:** **M10** (public Directus content → 100% coverage) → **M11** (richer-world recipes + corpus) → `/developer-kit:close-release`.
**Phase:** M9a closed + merged to `release/01.20-set-dressing` (2026-06-06) — the `stack-snapshot` extension (9 Go pkgs + `stacksnap` CLI, tag `stack-snapshot-m9a` @ `1cc4dd2`) + the data-DNA snapshot dimension + the `corpus/ops/snapshot-spec.md` / `db-access.md` / alignment_testing snapshot deliverables all landed. Starting M9b.
**Paused:** _(none)_

## Recently shipped releases
- **v1.1 "show floor"** — **2026-06-05**, tag `v1.1`. The **platform-operations extension framework**: disposable, Clerk-free, **production-safely-seeded** demo stacks (+ dev), in a 2-repo constellation (`rosetta` + the private `rosetta-extensions` monorepo). 8 milestones (M3→M8): repo consolidation (M4), demo/dev stack tooling (M3/M5/M6), the seeding stack (M7a framework + 3-layer isolation guard · M7b data-DNA conformance/drift · M7c backdated-activity seeder fleet), and the product layer (M8 corpus recipes + presets + `/demo-seed` + the express-gate CI carry-forward). Test funcs 175→**409**; all 4 Clerkenstein alignment gates held **100%/100%** (Go 22/22 · JS 9/9 · `@clerk/express` 9/9 · deploy 7/7); flake 0; supply chain clean. close-release: scope/code/tests GREEN, docs YELLOW→fixed (5 minor findings); 0 escape-hatch beyond the 2 user-waived surfaces (taxonomy + content → v1.2). Records: [releases/archive/01.10-show-floor/](releases/archive/01.10-show-floor/) (review · retro · metrics · lockfile).
- **v1.0 "body double"** — **2026-06-03**, tag `v1.0`. The alignment-testing framework (`rosetta-extensions/alignment/`) + **Clerkenstein**, a *measured* drop-in Clerk mock at **100%/100% on all three surfaces** (Go · JS/FAPI · `@clerk/express`), zero platform-code change. 6 milestones (M0→M2c). Records: [releases/archive/01.00-body-double/](releases/archive/01.00-body-double/).

## Headline numbers (M9a close — 2026-06-06)
- **Test funcs:** **556** Go total (+147 over v1.1's 409) — `rosetta-extensions/alignment` 46 · clerkenstein 218 · stack-seeding **164** (+19, the data-DNA snapshot extension) · **stack-snapshot 128** (new: 127 test + 1 fuzz, 9 pkgs) + the demo tooling 87 (shell/py). Flake **0** (5× shuffled, both Go modules). Both modules pass `-race`; gofmt + `go vet` clean.
- **Alignment gates (held green through v1.1):** **100%/100%** on all 4 surfaces — Go (22/22, `clerk@2.6.0`), JS/FAPI (9/9, `clerk-js@5`), `@clerk/express` (9/9, RS256/JWKS), deployment/injection (7/7, `clerk-deploy-1`, colony @ v0.34.3). 3 wired in CI; deploy is a local gate (needs colony via GH_PAT).
- **Seeding gates:** the 3-layer production-isolation guard (audit-proven zero pollution) · data-DNA `measure` 100%/critical 100% over the 8 reachable surfaces · drift detection · login→200 · full seed ~0.7s (<2min).

## Branch model
**v1.2 IN DEVELOPMENT:** `release/01.20-set-dressing` cut from `main` (2026-06-05). Milestone branches `m{N}/{slug}` (`m9a/snapshot-framework`, `m9b/taxonomy-snapshot`, `m10/content-snapshot`, `m11/richer-recipes`) branch from it; merge back at close-milestone; the release merges `--no-ff` → `main` + tags `v1.2` at close-release. The snapshot code lands in a **dedicated `stack-snapshot/` section** of the **private** `anthropos-work/rosetta-extensions` monorepo — authored + tagged in the authoring copy at `.agentspace/rosetta-extensions/`, then consumed per-stack: each gitignored `stack-*/` workspace (`stack-dev`, `stack-demo`, …) holds its OWN clone pinned at a tag (`stack-demo/rosetta-extensions @ tag`). Snapshot **payloads** live in a gitignored `.agentspace/snapshots/` cache (never committed; cloud/S3 store = v1.3). The rosetta-side milestone records + corpus updates land on the `m{N}/…` branches.
**v1.1 SHIPPED + pushed:** `release/01.10-show-floor` merged `--no-ff` → `main`, tagged **`v1.1`**; release branch deleted.
**v1.0 SHIPPED:** tagged `v1.0` (2026-06-03).

_Last updated: 2026-06-06 (**M9a closed + merged** to `release/01.20-set-dressing` via `/developer-kit:close-milestone` — the `stack-snapshot` framework + capture-safety + tenant firewall + `.agentspace` store + fidelity-DNA + `/db-query` port. Close: scope/code/docs/tests GREEN, 1 finding fixed [tag re-point], deferral audit GREEN. Go test funcs 409→556. Active milestone → M9b. Full narrative in roadmap.md `### M9a`.)._
