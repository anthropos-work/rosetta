# State

**Active version:** **v1.3b "dress rehearsal"** — **ALL MILESTONES CLOSED, awaiting `/developer-kit:close-release`** (designed 2026-06-08; branch `release/01.3b-dress-rehearsal` cut from `main`). A **field-hardening release** between shipped v1.3 and future v1.4: it makes `/demo-up` produce a **full, populated, verified, demoable** stack — addressing the 14 issues the first real `/demo-up` run surfaced ([`.agentspace/demo-up-issue.md`](../../.agentspace/demo-up-issue.md)). 5 milestones M16→M20: land the applied fixes + doc truth → re-run safety (idempotency) → verification net → frontend tier → lifecycle convergence (auto set-dress + cold-start). **Tooling + docs only — zero platform-repo edits.** v1.4 (cloud store / S3 / AI content / shareability — incl. the signed DEF-M10-01) stays in [`roadmap-vision.md`](roadmap-vision.md), untouched.
**Active branch:** `release/01.3b-dress-rehearsal` (M16+M17+M18+M19+M20 all merged `--no-ff`; **5/5 closed**).
**Active milestone:** **(between milestones — v1.3b complete)** — all 5 milestones M16→M20 are closed + merged to the release branch.
**Next up:** **`/developer-kit:close-release` v1.3b** — the release-level review + merge `--no-ff` → `main` + tag (lean `v1.3.1`). (NOT a milestone — the user runs it.)
**Phase:** **v1.3b in development; M16+M17+M18+M19+M20 closed (5/5) — the FINAL milestone is done; release-close is the only remaining step.**
**Paused:** _(none)_

## Recently closed milestones
- **M20 "lifecycle convergence"** (v1.3b) — **2026-06-09**, closed → `release/01.3b-dress-rehearsal`. **The FINAL v1.3b milestone.** Converged the **lifecycle**: `/demo-up` now auto-set-dresses by default (cache-first snapshot **replay** → a `small-200` light seed) at the bring-up tail, exactly as `/dev-up` since M13, by **reusing the same `dev-setdress.sh` engine via `--stack-type demo`** — ONE engine, two lifecycles (not a second implementation). Plus the net-new `corpus/ops/snapshot-cold-start.md` (the fresh-box capture workflow + why the wired `postgres` MCP is NOT a capture source — the ISSUE-13 spike resolved **document-only**) + safety §2.7 + demo recipe/skill updates. **Prod-safety verified + test-pinned on both happy + cache-miss branches:** capture NEVER on a bring-up (replay-only), per-stack Directus firewall-checked, offset-DSN isolation, n=0-across-types, the atomicity seed-floor, M15 drift guards GREEN after §2.7. Close: 5 findings (1 docs DOC-1 + 4 decision-triage; 0 scope/code/adversarial-new/tests). Tag `dress-rehearsal-m20` reconciled `e4d2f9b → 51a07cb` + re-consumed. Deferral re-audit GREEN — the **release-wide M16→M20 pre-close sweep**: 0 new; DEF-M10-01 → v1.4 untouched across all of v1.3b + not aged. Go **736** (unchanged); Python 338→**360** collected (+22); flake 0. Full detail in roadmap.md `### M20`.
- **M19 "the demo-up frontend tier"** (v1.3b) — **2026-06-09**, closed → `release/01.3b-dress-rehearsal`. Completed the demo family — `/demo-up` brings up the full UI: next-web + studio-desk per-demo (a cached image from the unmodified Dockerfile, offset ports, minted-pk baked) + ant-academy natively (Clerk-free), with a non-fatal 12 GB VM pre-flight + a `--no-ui` escape — all **zero platform-repo edits** (`TestZeroPlatformRepoEdit` held). Net-new `corpus/ops/demo/frontend-tier.md`. Close: 6 findings, all decision-triage. Tag `dress-rehearsal-m19` reconciled `32b1ae8 → 4f96ddd`. Go 736; Python 273→338 (+65); flake 0. Detail in roadmap.md `### M19`.
- **M18 "the verification safety net"** (v1.3b) — **2026-06-09**, closed. Made a bring-up self-verifying: `stack-verify` taught to target an *offset* stack + scope to the brought-up services, the `$DEVDIR → $STACK_ROOT` fix, the cheap-win `/api/health` + `casbin_rules > 0` asserts (the ISSUE-7 catcher), and a default-on + NON-FATAL `verify live` auto-wired at both bring-up tails + net-new `corpus/ops/verification.md`. Close: 3 findings (FINDING-A1 the non-numeric-offset crash + regression + ref-tags). Tag reconciled `594b9cf → 777723a`. Go 736; Python 191→273 (+82); flake 0. Detail in roadmap.md `### M18`.
- **M17 "bring-up re-run safety"** (v1.3b) — **2026-06-09**, closed. Made the bring-up primitives re-run-safe: the `set -e` first-run-race audit, the `stacksnap replay` per-stack-isolated TRUNCATE-then-reload guard, the `stackseed` idempotent COPY + casbin guard + fixed `--reset`, the live docker migrate-race harness, + net-new `corpus/ops/idempotency.md`. Tag reconciled `dcef026 → 0d36251`. Go 713→736 (+23); Python 182→191 (+9); flake 0. Detail in roadmap.md `### M17`.

## Recently shipped releases
- **v1.3 "stack party"** — **2026-06-07**, tag `v1.3`. The **dev/demo convergence**: dev stacks become first-class peers (a **unified first-available-N stack registry** [M12], a **local per-stack Directus + auto-snapshot + light `dev-min` seed** on a `dev-stack up` [M13]), one **generic `stack-*` skill set** [M14], and a net-new **code-cited `corpus/ops/safety.md`** — the tooling's two guarantees, each load-bearing claim pinned to source by a fail-closed drift guard [M15]. close-release GREEN; 1 escape-hatch (S3 blob bytes + cloud store → v1.4, signed = DEF-M10-01). Records: [releases/archive/01.30-stack-party/](releases/archive/01.30-stack-party/).
- **v1.2 "set dressing"** — **2026-06-07**, tag `v1.2`. The **snapshot mechanism**: a dedicated `stack-snapshot` extension that captures the **public** reference library read-only from prod, manifest-caches it in `.agentspace`, replays it per-stack behind a tested **tenant-data firewall** — lifting M7c's two `waived` surfaces to **100% data-DNA**. Records: [releases/archive/01.20-set-dressing/](releases/archive/01.20-set-dressing/).
- **v1.1 "show floor"** — **2026-06-05**, tag `v1.1`. The **platform-operations extension framework**: disposable, Clerk-free, production-safely-seeded demo stacks (+ dev), in a 2-repo constellation. Records: [releases/archive/01.10-show-floor/](releases/archive/01.10-show-floor/).

## Headline numbers (v1.3b/M20 close — 2026-06-09)
<!-- M20 close: Go 736 unchanged (no Go touched); Python 338->360 collected (+22, the set-dress convergence suites: dev-stack stack-type/atomicity + the demo chain). -->

- **Go test funcs:** **736** total (test-only `^func Test` matcher; unchanged by M20 — M20 touched only bash + docs, no Go). Per-module: `rosetta-extensions/alignment` 43 · clerkenstein 210 · stack-seeding 252 · stack-snapshot 231. All 4 Go modules build + pass `-count=1`. The M15 `safety.md` drift guards re-ran GREEN after the §2.7 edit.
- **Python test funcs:** **360** collected (stack-core 54 · dev-stack 50 · demo-stack 84 · stack-injection 85 · stack-verify 87). **+22 in v1.3b/M20** — the set-dress convergence coverage: `dev-stack/tests/test_dev_stack.py` class `DevSetdress` (stack-type plumbing dev|demo, the small-200 vs dev-min default, the n=0 guard across types, the atomicity seed-floor, the needval/non-integer-N guards, capture-never-on-the-degraded-path) + `demo-stack/tests/test_frontend_build.py` class `TestSetdressChainContract` (7 static fences + 3 behavioural: non-fatal-under-set-e, skipped-when-NO_SETDRESS, success-threads-the-offset-DSN). The 2 touched bash scripts shellcheck-CLEAN; py_compile CLEAN. _(Python counts are pytest-collected, so parametrized cases count individually.)_
- **The v1.3b thesis (the headline):** `/demo-up` now produces a **full, populated, verified, demoable** stack — the full UI tier (M19) + a self-verifying bring-up (M18) + re-run-safe primitives (M17) + **auto-set-dress (M20)** that reuses the dev pass byte-for-byte, all on a 16 GB Mac, **zero platform-repo edits**. The dev↔demo convergence is now complete: one operating model, one set-dress engine, two lifecycles.
- **Coverage:** **100% of the full data-DNA catalog** (inherited from v1.2; unchanged — nothing waived).
- **Alignment gates (held green since v1.0):** **100%/100%** on all 4 Clerkenstein surfaces — Go (22/22), JS/FAPI (9/9), `@clerk/express` (9/9), deployment/injection (7/7). v1.3b touched no clerkenstein. 3 wired in CI; deploy is a local gate.
- **Test health:** flake **0** (5/5 the 2 M20-touched suites); triple-clean **3/3** (4 Go `-race -shuffle` + the Python suites); supply-chain GREEN (0 called third-party CVEs; all-permissive licenses).

## Branch model
**v1.3b ALL MILESTONES CLOSED:** `release/01.3b-dress-rehearsal` cut from `main` (2026-06-08); milestone branches `m16/…`→`m20/…` cut from it as each was built, all merged `--no-ff` + branches deleted (5/5). Next: `/developer-kit:close-release` merges `--no-ff` → `main` + tags (lean `v1.3.1`). The 14 demo-up fixes are **tooling** (built + tested in the authoring copy `.agentspace/rosetta-extensions/`, **tagged** `dress-rehearsal-mNN`, consumed per-stack) **+ docs** (this corpus). v1.3b extensions markers: **`dress-rehearsal-m16`** @ `e6161b0` · **`dress-rehearsal-m17`** @ `0d36251` · **`dress-rehearsal-m18`** @ `777723a` · **`dress-rehearsal-m19`** @ `4f96ddd` · **`dress-rehearsal-m20`** @ `51a07cb` (reconciled from `e4d2f9b`), extensions `main` at `51a07cb` on `origin`; `stack-demo/rosetta-extensions` re-consumed to `dress-rehearsal-m20`. The local-only `stack-party-devpath-fix` tag is superseded (never pushed).
**v1.3 SHIPPED:** `release/01.30-stack-party` merged `--no-ff` → `main`, tagged **`v1.3`** (2026-06-07); release branch + all 4 milestone branches deleted. The stack tooling lives in the **private** `anthropos-work/rosetta-extensions` monorepo — authored + tagged in the authoring copy, consumed per-stack at a pinned tag. v1.3 markers: **`stack-party-m12`** @ `be1c979` · **`stack-party-m13`** @ `cca4464` · **`stack-party-m14`** @ `33fc525` · **`stack-party-m15`** @ `51ca18b`, plus the release tag **`v1.3.0`**. Snapshot **payloads** live in a gitignored `.agentspace/snapshots/` cache (never committed; cloud/S3 store = v1.4, DEF-M10-01).
**v1.2 SHIPPED + pushed:** tagged **`v1.2`** (2026-06-07). **v1.1 SHIPPED + pushed:** tagged **`v1.1`**. **v1.0 SHIPPED:** tagged `v1.0` (2026-06-03).

_Last updated: 2026-06-09 (**v1.3b/M20 CLOSED** via `/developer-kit:close-milestone` — the FINAL v1.3b milestone. The lifecycle is converged: `/demo-up` auto-set-dresses by default (cache-first replay → small-200 seed) by reusing the same `dev-setdress.sh` engine via `--stack-type demo` — one engine, two lifecycles. Prod-safety verified + test-pinned: capture NEVER on a bring-up (replay-only), firewall-checked per-stack Directus, offset-DSN isolation, n=0-across-types, the atomicity floor, M15 drift guards GREEN after §2.7. Net-new `corpus/ops/snapshot-cold-start.md` (the ISSUE-13 spike → document-only: the MCP is not a capture source). Close: 5 findings (1 docs DOC-1 + 4 decision-triage); tag `dress-rehearsal-m20` reconciled `e4d2f9b → 51a07cb` + re-consumed; deferral re-audit GREEN as the release-wide M16→M20 sweep (0 new; DEF-M10-01 → v1.4 untouched + not aged); Go 736 unchanged, Python 338→360 (+22), flake 0 (5/5). All 5 v1.3b milestones closed. **Next: `/developer-kit:close-release` v1.3b — the user runs it.**)_
