# State

**Active version:** **v1.3 "stack party"** — in development on `release/01.30-stack-party` (designed 2026-06-07). The **dev/demo convergence**: dev stacks become first-class peers (a **local per-stack Directus** + **auto-snapshot** of the real reference data + a **light `dev-min` seed** on build), a **unified stack registry** allocates the first-available N across dev+demo (no port collisions), one **generic `stack-*` skill set** (`stack-list`/`seed`/`snapshot`/`update` + `dev-up`/`dev-down`, **hard-renamed**, no aliases), and a dedicated **safety & security doc** + a refresh of **both** knowledge bases. 4 milestones, all `section`: **M12** (unified registry + first-free-N) → **M13** (dev peers: local Directus + snapshot + light seed) → **M14** (unified stack-* skills) → **M15** (safety doc + dual-repo KB). Former v1.3 seeds (cloud store / S3 blobs, AI content, shareability, more mirrors) → **v1.4** (user, 2026-06-07).
**Active milestone:** **M14 — Unified `stack-*` skills + `dev-up`/`dev-down` (hard-renamed)** (`build complete` · `section` · large). Next: **`/developer-kit:close-milestone`** (optionally `/developer-kit:harden-milestone` first — but M14 is docs/skills-only, no code-coverage surface).
**Next up:** **M15** (safety doc + dual-repo KB) → `/developer-kit:close-release`.
**Phase:** **M14 BUILD COMPLETE 2026-06-07** (branch `m14/unified-skills`, not yet merged) — the skill set converged: `dev-up` (consolidates the former setup-platform + start-platform; drives the M13 dev set-dress flow) + new `dev-down`; the ops skills hard-renamed (no aliases) to generic `dev-N|demo-N` targets — `stack-list` (←demo-status), `stack-seed` (←demo-seed), `stack-snapshot` (←demo-snapshot), `stack-update` (←update-platform); the 6 old skill dirs removed; `demo-up`/`demo-down` retained + aligned with the dev lifecycle. Reference sweep done across CLAUDE.md (rewritten skill table), root+corpus READMEs, all `corpus/ops/` guides + the `demo/` recipe family, and the extensions clone (commit `b37e831` on `main`). KB-fidelity GREEN; PR review 1 finding fixed (`stack-seed --preset` shorthand → M14-D5); 0 deferrals (all Fate-1). 5 rosetta commits (`e6c2da6`→`28cb5c4`). M13 closed (tag `stack-party-m13` @ `cca4464`); M12 closed (tag `stack-party-m12` @ `be1c979`).
**Paused:** _(none)_

## Recently shipped releases
- **v1.2 "set dressing"** — **2026-06-07**, tag `v1.2`. The **snapshot mechanism**: a dedicated `stack-snapshot` extension that captures the **public** reference library (skills taxonomy + Directus content) read-only from prod, manifest-caches it in `.agentspace`, and replays it per-stack behind a tested **tenant-data firewall** (customer data never copied) — lifting M7c's two `waived` surfaces to **100% data-DNA coverage**. 4 milestones (M9a framework + `/db-query` port → M9b taxonomy → M10 public Directus content → M11 product layer + `/demo-snapshot`). Go test funcs 409→**708** (+73%); flake 0; triple-clean 3/3; 0 third-party CVEs; all-permissive licenses. close-release GREEN (2 must-fix docs fixed); 2 v1.3 escape-hatch (S3 blob bytes + cloud store, signed) + 1 user-drop (offline pg_dump-FILE reader). Records: [releases/archive/01.20-set-dressing/](releases/archive/01.20-set-dressing/) (review · retro · metrics · lockfile).
- **v1.1 "show floor"** — **2026-06-05**, tag `v1.1`. The **platform-operations extension framework**: disposable, Clerk-free, **production-safely-seeded** demo stacks (+ dev), in a 2-repo constellation (`rosetta` + the private `rosetta-extensions` monorepo). 8 milestones (M3→M8): repo consolidation (M4), demo/dev stack tooling (M3/M5/M6), the seeding stack (M7a framework + 3-layer isolation guard · M7b data-DNA conformance/drift · M7c backdated-activity seeder fleet), and the product layer (M8 corpus recipes + presets + `/demo-seed` + the express-gate CI carry-forward). Test funcs 175→**409**; all 4 Clerkenstein alignment gates held **100%/100%** (Go 22/22 · JS 9/9 · `@clerk/express` 9/9 · deploy 7/7); flake 0; supply chain clean. close-release: scope/code/tests GREEN, docs YELLOW→fixed (5 minor findings); 0 escape-hatch beyond the 2 user-waived surfaces (taxonomy + content → v1.2). Records: [releases/archive/01.10-show-floor/](releases/archive/01.10-show-floor/) (review · retro · metrics · lockfile).
- **v1.0 "body double"** — **2026-06-03**, tag `v1.0`. The alignment-testing framework (`rosetta-extensions/alignment/`) + **Clerkenstein**, a *measured* drop-in Clerk mock at **100%/100% on all three surfaces** (Go · JS/FAPI · `@clerk/express`), zero platform-code change. 6 milestones (M0→M2c). Records: [releases/archive/01.00-body-double/](releases/archive/01.00-body-double/).

## Headline numbers (M13 close — 2026-06-07)
- **Go test funcs:** **720** total (+12 — M13 added the `stack-snapshot/cmd/provision-plan` runner). `rosetta-extensions/alignment` 46 · clerkenstein 218 · stack-seeding **233** (+1, the `dev-min` preset pin → a 4-preset ordered set) · stack-snapshot **223** (+11, the provision-plan runner: recipe printer + `--check-env` firewall). Both modules pass `-race -count=1`; gofmt + `go vet` clean.
- **Python test funcs (M13 — the dev set-dressing surface):** **dev-stack/tests 38** (33→38: `DevStackSetdressWiring` + `DevSetdress` — non-fatal failed set-dress, the re-run-hint flag-fidelity regression, `--no-setdress` skips, the trailing-value-flag regression, value-flag happy path). All Python suites green (stack-core+demo-stack 67, injection+verify 69). `cmd/provision-plan` 93% statements (residual: the `os.Exit` shim + an invariant-pinned defensive branch); `directus` + `blueprint` 100%. Flake **0** (5/5 — Go shuffled + dev-stack sequential); both CLIs shellcheck-CLEAN; py_compile CLEAN.
- **Coverage:** **100% of the full data-DNA catalog** — both formerly-`waived` surfaces promoted: `taxonomy` (M9b) + `content` (M10) → `snapshot-seeded`. Nothing waived; the v1.2 set-dressing thesis is structurally complete.
- **Alignment gates (held green through v1.1):** **100%/100%** on all 4 surfaces — Go (22/22, `clerk@2.6.0`), JS/FAPI (9/9, `clerk-js@5`), `@clerk/express` (9/9, RS256/JWKS), deployment/injection (7/7, `clerk-deploy-1`, colony @ v0.34.3). 3 wired in CI; deploy is a local gate (needs colony via GH_PAT).
- **Seeding gates:** the 3-layer production-isolation guard (audit-proven zero pollution) · data-DNA `measure` 100%/critical 100% over the 8 reachable surfaces · drift detection · login→200 · full seed ~0.7s (<2min).

## Branch model
**v1.3 IN DEVELOPMENT:** `release/01.30-stack-party` cut from `main` (2026-06-07). Milestone branches `m{N}/{slug}` (`m14/unified-skills`, `m15/safety-doc`) branch from it; merge back at close-milestone; the release merges `--no-ff` → `main` + tags `v1.3` at close-release. **M13 CLOSED 2026-06-07:** `m13/dev-peers` merged `--no-ff` → `release/01.30-stack-party`, branch deleted; extensions tag **`stack-party-m13`** @ `cca4464` (v1.3 release-and-milestone tag naming `stack-party-mN`, matching the M12 precedent). **M12 CLOSED 2026-06-07:** `m12/stack-registry` merged `--no-ff` → `release/01.30-stack-party`, branch deleted; extensions tag **`stack-party-m12`** @ `be1c979`. **v1.2 SHIPPED + pushed:** `release/01.20-set-dressing` merged `--no-ff` → `main`, tagged **`v1.2`** (2026-06-07), pushed; all 4 milestone branches merged + deleted. The stack tooling lives in the **private** `anthropos-work/rosetta-extensions` monorepo — authored + tagged in the authoring copy at `.agentspace/rosetta-extensions/`, then consumed per-stack: each gitignored `stack-*/` workspace (`stack-dev`, `stack-demo`, …) holds its OWN clone pinned at a tag (`stack-demo/rosetta-extensions @ tag`). Snapshot **payloads** live in a gitignored `.agentspace/snapshots/` cache (never committed; cloud/S3 store = v1.3). The rosetta-side milestone records + corpus updates land on the `m{N}/…` branches.
**v1.1 SHIPPED + pushed:** `release/01.10-show-floor` merged `--no-ff` → `main`, tagged **`v1.1`**; release branch deleted.
**v1.0 SHIPPED:** tagged `v1.0` (2026-06-03).

_Last updated: 2026-06-07 (**M13 CLOSED** via `/developer-kit:close-milestone` — dev stacks become full-fidelity peers of demo for DATA: a `dev-stack up` set-dressing pass [per-stack Directus via the NEW executable `stack-snapshot/cmd/provision-plan` runner that makes the library-only M10 contract a CLI · cache-first `stacksnap replay` of public taxonomy+directus · the new `dev-min` light seed], default-on + non-fatal, n=0-dev guard doubled. Review GREEN; 6 findings [1 doc must-fix — the missing `cmd/provision-plan` README index row; 5 GREEN/no-action incl. 4 adversarial + 3 decision-triage blends ref-tagged]; deferral re-audit GREEN [1 inherited DEF-M10-01 → v1.4 signed/unchanged, 0 new — M13 added zero deferrals]. Go 708→720; dev-stack pytest 33→38; flake 0; `-race`/gofmt/vet/shellcheck/py_compile clean. Merged `--no-ff` → `release/01.30-stack-party`, branch deleted; extensions tag `stack-party-m13` @ `cca4464`. **Next: `/developer-kit:build-milestone` for M14** [unified `stack-*` skills + `dev-up`/`dev-down`, hard-renamed]. Prior — **M12 CLOSED**: the unified dev+demo stack registry + first-available-N allocator [`stack-core/stack_registry.py`], tag `stack-party-m12` @ `be1c979`; see roadmap.md `### M12`. Earlier — **v1.3 designed** + **v1.2 SHIPPED** [tag `v1.2`]; see roadmap.md.)._
