# State

**Active version:** **v1.3b "dress rehearsal"** — **IN DEVELOPMENT** (designed 2026-06-08; branch `release/01.3b-dress-rehearsal` cut from `main`). A **field-hardening release** between shipped v1.3 and future v1.4: it makes `/demo-up` produce a **full, populated, verified, demoable** stack — addressing the 14 issues the first real `/demo-up` run surfaced ([`.agentspace/demo-up-issue.md`](../../.agentspace/demo-up-issue.md)). 5 milestones M16→M20: land the applied fixes + doc truth → re-run safety (idempotency) → verification net → frontend tier → lifecycle convergence (auto set-dress + cold-start). **Tooling + docs only — zero platform-repo edits.** v1.4 (cloud store / S3 / AI content / shareability — incl. the signed DEF-M10-01) stays in [`roadmap-vision.md`](roadmap-vision.md), untouched.
**Active branch:** `release/01.3b-dress-rehearsal` (M16+M17+M18+M19 merged; the next milestone branch `m20/…` is cut from it when M20 starts).
**Active milestone:** _(between milestones — see Next up)_ — **M19 CLOSED 2026-06-09** (merged `--no-ff` → `release/01.3b-dress-rehearsal`; `m19/frontend-tier` deleted). M20 (the next + **final** milestone of v1.3b) not yet started.
**Next up:** **`/developer-kit:build-milestone` M20 — Lifecycle convergence** (demo-up auto set-dress + cold-start capture), the last v1.3b milestone. Then `/developer-kit:close-release` v1.3b.
**Phase:** **v1.3b in development; M16+M17+M18+M19 closed (4/5 done); M20 next + final.**
**Paused:** _(none)_

## Recently closed milestones
- **M19 "the demo-up frontend tier"** (v1.3b) — **2026-06-09**, closed → `release/01.3b-dress-rehearsal`. Completed the demo family — `/demo-up` now brings up the full UI: **next-web + studio-desk** per-demo (a **cached** Docker image from the **unmodified** Dockerfile, offset ports, minted-pk + offset-URL baked) + **ant-academy** natively (Clerk-free via `BENCHMARK_VISUAL_BYPASS`), with a non-fatal 12 GB VM pre-flight, a `--no-ui` escape, and the frontend ports registered in M18's verify net — all **zero platform-repo edits** (the pk rides a gitignored `.env.local`, the `.dockerignore` is transient + `RETURN`-trap-removed, the Dockerfiles unmodified). The load-bearing zero-platform-edit invariant held (`TestZeroPlatformRepoEdit`: real-git-repo `git status`-clean on success AND failed-build paths). Net-new `corpus/ops/demo/frontend-tier.md` + the `demo-up` SKILL update. Close: 6 findings, **all decision-triage** (0 scope/code/docs/tests — the cleanest shape); 5 `(#M19-D3..D7)` ref-tags + 3 re-examined adversarial scenarios. Tag `dress-rehearsal-m19` reconciled `32b1ae8 → 4f96ddd` + re-consumed; deferral re-audit GREEN (0 new; DEF-M10-01 → v1.4 untouched; true-zero-rebuild PR = documented OUT). Go **736** (unchanged); Python 273→**338** collected (+65); flake 0. Full detail in roadmap.md `### M19`.
- **M18 "the verification safety net"** (v1.3b) — **2026-06-09**, closed → `release/01.3b-dress-rehearsal`. Made a bring-up self-verifying: taught `stack-verify` to target an *offset* stack (new `lib/target.sh` + a non-fatal base-port-band cross-check vs the registry's recorded ports) + scope to the services actually brought up (BOTH liveness + readiness honour `STACK_SERVICES`), fixed the `$DEVDIR → $STACK_ROOT` bug, added the cheap-win `/api/health` + `casbin_rules > 0` asserts (the ISSUE-7 catcher) in `live/autoverify.sh`, and auto-wired a **default-on + NON-FATAL** `verify live` at both bring-up tails — so "UP" means *verified-working*. Plus the net-new `corpus/ops/verification.md`. The non-fatal invariant verified (always exit 0; offset cross-checked not formula'd; both phases scoped). Close: 3 findings — FINDING-A1 (adversarial: a non-numeric offset crashed the arithmetic under `set -u`, silently skipping the asserts; fixed at the resolution boundary, M18-D7) + its regression + decision ref-tags. Tag `dress-rehearsal-m18` reconciled `594b9cf → 777723a` + re-consumed; deferral re-audit GREEN (0 new; frontend ports = Fate-2 → M19). Go **736** (unchanged); Python 191→**273** collected (+82, the net-new stack-verify suite); flake 0. Full detail in roadmap.md `### M18`.
- **M17 "bring-up re-run safety"** (v1.3b) — **2026-06-09**, closed → `release/01.3b-dress-rehearsal`. Made the bring-up primitives re-run-safe: the `set -e` first-run-race audit across the 4 bring-up scripts (loud `GH_PAT`, `DEV_PROJECT` fallback, the `migrate-demo.sh` wait-for-ready + a 4th latent schema-create site), the `stacksnap replay` per-stack-isolated TRUNCATE-then-reload guard (target-class pinned), the `stackseed` idempotent COPY + casbin `WHERE NOT EXISTS` + the fixed FK-ordered `--reset` fleet, the live docker migrate-race harness (the M16 Fate-2 LANDED), and the net-new `corpus/ops/idempotency.md`. Prod-safety verified (TRUNCATE per-stack-only; M15 drift guards green). Close: 1 Fate-1 doc fix (decision ref-tags); tag `dress-rehearsal-m17` reconciled `dcef026 → 0d36251` + re-consumed; deferral re-audit GREEN (0 new). Go 713→**736** (+23); Python 182→**191** collected (+9); flake 0. Full detail in roadmap.md `### M17`.
- **M16 "land the field fixes + restore doc truth"** (v1.3b) — **2026-06-08**, closed → `release/01.3b-dress-rehearsal`. Published the 2 stranded fixes (devpath + migrate-race) to `origin`, finished the `anthropos-dev → stack-dev` rename as the documented default (legacy fallback retained), cleared the stale GUIDE/README facts, added the corpus layout note + extensions KB version-jump note. Close: 1 Fate-1 doc fix (the last stale workspace name — clerkenstein glossary), tag `dress-rehearsal-m16` reconciled `44edc09 → e6161b0` + re-consumed; deferral re-audit GREEN. Python 174→**182** (+8 guards); Go 713 unchanged; flake 0. Full detail in roadmap.md `### M16`.

## Recently shipped releases
- **v1.3 "stack party"** — **2026-06-07**, tag `v1.3`. The **dev/demo convergence**: dev stacks become first-class peers (a **unified first-available-N stack registry** [M12], a **local per-stack Directus + auto-snapshot + light `dev-min` seed** on a `dev-stack up` [M13]), one **generic `stack-*` skill set** (`stack-list`/`seed`/`snapshot`/`update` + `dev-up`/`dev-down`, **hard-renamed**, no aliases [M14]), and a net-new **code-cited `corpus/ops/safety.md`** — the tooling's two guarantees (never reads private data / never touches prod), each load-bearing claim pinned to source by a fail-closed drift guard [M15]. Go test funcs 693→**713** (+20) + **+52 net-new Python** (the unified registry, 174 total); flake 0; triple-clean 3/3; 0 called third-party CVEs; all-permissive licenses. close-release GREEN (0 must-fix; docs YELLOW→fixed; deferral re-audit YELLOW→GREEN — DEF-M10-01 formally signed → v1.4). 1 escape-hatch (S3 blob bytes + cloud store → v1.4, signed). Records: [releases/archive/01.30-stack-party/](releases/archive/01.30-stack-party/) (review · retro · metrics · lockfile).
- **v1.2 "set dressing"** — **2026-06-07**, tag `v1.2`. The **snapshot mechanism**: a dedicated `stack-snapshot` extension that captures the **public** reference library (skills taxonomy + Directus content) read-only from prod, manifest-caches it in `.agentspace`, and replays it per-stack behind a tested **tenant-data firewall** (customer data never copied) — lifting M7c's two `waived` surfaces to **100% data-DNA coverage**. 4 milestones (M9a→M9b→M10→M11). Go test funcs 409→**708** (+73%); flake 0; triple-clean 3/3. Records: [releases/archive/01.20-set-dressing/](releases/archive/01.20-set-dressing/).
- **v1.1 "show floor"** — **2026-06-05**, tag `v1.1`. The **platform-operations extension framework**: disposable, Clerk-free, **production-safely-seeded** demo stacks (+ dev), in a 2-repo constellation (`rosetta` + the private `rosetta-extensions` monorepo). 8 milestones (M3→M8). Test funcs 175→**409**; all 4 Clerkenstein alignment gates held **100%/100%**; flake 0. Records: [releases/archive/01.10-show-floor/](releases/archive/01.10-show-floor/).

## Headline numbers (v1.3b/M19 close — 2026-06-09)
<!-- M19 close: Go 736 unchanged (no Go touched); Python 273->338 collected (+65, the M19 frontend-build + ant-academy + injection-frontend + verify-frontend tests). -->

- **Go test funcs:** **736** total (test-only `^func Test` matcher; unchanged by M19 — M19 touched only `stack-injection`/`demo-stack`/`stack-verify` Python + bash, no Go code). Per-module: `rosetta-extensions/alignment` 43 · clerkenstein 210 · stack-seeding 252 · stack-snapshot 231. All 4 Go modules build + pass `-count=1`; gofmt + `go vet` clean.
- **Python test funcs:** **338** collected (stack-core 54 · dev-stack 38 · demo-stack ~95 · stack-injection ~89 · stack-verify ~62). **+65 in v1.3b/M19** — the per-demo frontend build + zero-platform-edit guard (`demo-stack/tests/test_frontend_build.py`), the native-academy launcher/fallback (`test_ant_academy.py`), the frontend-emitting override (`stack-injection/tests/test_injection.py` TestFrontendTier), and the frontend-port registration (`stack-verify/tests/test_verify.py` TestFrontendTierRegistration). `gen_injected_override.py` 98% (only the `__main__` guard). The 4 touched bash scripts shellcheck-CLEAN; py_compile CLEAN. _(Python counts are pytest-collected, so parametrized cases count individually.)_
- **The v1.3 thesis (the headline):** dev and demo are now **one operating model** — a unified N-pool registry (no port collisions), dev as a full-fidelity data peer (local Directus + auto-snapshot + `dev-min` seed by default, replay-only — capture never runs on dev), one generic `stack-*` skill set (hard-renamed), and a code-cited `safety.md` whose every load-bearing claim is a fail-closed drift test against `firewall.go`/`isolation.go`/`audit.go`/`source.go`.
- **Coverage:** **100% of the full data-DNA catalog** (inherited from v1.2; unchanged — nothing waived).
- **Alignment gates (held green since v1.0):** **100%/100%** on all 4 Clerkenstein surfaces — Go (22/22), JS/FAPI (9/9), `@clerk/express` (9/9), deployment/injection (7/7). v1.3 touched no clerkenstein. 3 wired in CI; deploy is a local gate.
- **Test health:** flake **0**; triple-clean **3/3** (4 Go `-race -shuffle` + 4 Python suites); supply-chain GREEN (0 called third-party CVEs; the 12 called vulns are Go stdlib @go1.25.3 → cleared by go1.25.11+; all-permissive licenses).

## Branch model
**v1.3b IN DEVELOPMENT:** `release/01.3b-dress-rehearsal` cut from `main` (2026-06-08) by `/developer-kit:design-roadmap`; milestone branches `m16/…`→`m20/…` cut from it as each is built (M16+M17+M18+M19 merged `--no-ff` + branches deleted); merges `--no-ff` → `main` + tags (lean `v1.3.1`) at `/developer-kit:close-release`. The 14 demo-up fixes are **tooling** (built + tested in the authoring copy `.agentspace/rosetta-extensions/`, **tagged** `dress-rehearsal-mNN`, consumed per-stack) **+ docs** (this corpus's `corpus/*` + `.claude/skills/*`). v1.3b extensions markers: **`dress-rehearsal-m16`** @ `e6161b0` · **`dress-rehearsal-m17`** @ `0d36251` · **`dress-rehearsal-m18`** @ `777723a` · **`dress-rehearsal-m19`** @ `4f96ddd` (reconciled from `32b1ae8`), extensions `main` at `4f96ddd` on `origin`; `stack-demo/rosetta-extensions` re-consumed to `dress-rehearsal-m19`. The local-only `stack-party-devpath-fix` tag is superseded (never pushed).
**v1.3 SHIPPED:** `release/01.30-stack-party` merged `--no-ff` → `main`, tagged **`v1.3`** (2026-06-07); release branch deleted; all 4 milestone branches (`m12/stack-registry`, `m13/dev-peers`, `m14/unified-skills`, `m15/safety-doc`) merged + deleted. The stack tooling lives in the **private** `anthropos-work/rosetta-extensions` monorepo — authored + tagged in the authoring copy at `.agentspace/rosetta-extensions/`, then consumed per-stack at a pinned tag. v1.3 extensions markers: per-milestone **`stack-party-m12`** @ `be1c979` · **`stack-party-m13`** @ `cca4464` · **`stack-party-m14`** @ `33fc525` · **`stack-party-m15`** @ `51ca18b`, plus the release tag **`v1.3.0`** @ the post-close-fix HEAD. Snapshot **payloads** live in a gitignored `.agentspace/snapshots/` cache (never committed; cloud/S3 store = v1.4, DEF-M10-01).
**v1.2 SHIPPED + pushed:** tagged **`v1.2`** (2026-06-07). **v1.1 SHIPPED + pushed:** tagged **`v1.1`**. **v1.0 SHIPPED:** tagged `v1.0` (2026-06-03).

_Last updated: 2026-06-09 (**v1.3b/M19 CLOSED** via `/developer-kit:close-milestone` — the demo family is complete: `/demo-up` brings up the full UI tier (next-web + studio-desk per-demo + ant-academy native), all Clerk-free, on offset ports, on a 16 GB Mac, with **zero platform-repo edits**. Close was the cleanest shape — 6 findings, all decision-triage (0 scope/code/docs/tests); the load-bearing zero-platform-edit invariant held (`TestZeroPlatformRepoEdit`); tag `dress-rehearsal-m19` reconciled `32b1ae8 → 4f96ddd` + re-consumed; deferral re-audit GREEN; Go 736 unchanged, Python 273→338 collected (+65), flake 0 (5/5). Merged `--no-ff` → `release/01.3b-dress-rehearsal`; `m19/frontend-tier` deleted. **Next: `/developer-kit:build-milestone` M20 — the final v1.3b milestone.**)_
